(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CompleteAttachmentUpload: (CompleteAttachmentUploadRequest.t,
  CompleteAttachmentUploadResponse.t, CompleteAttachmentUploadResponse.error)
  t 
  | CreateParticipantConnection: (CreateParticipantConnectionRequest.t,
  CreateParticipantConnectionResponse.t,
  CreateParticipantConnectionResponse.error) t 
  | DisconnectParticipant: (DisconnectParticipantRequest.t,
  DisconnectParticipantResponse.t, DisconnectParticipantResponse.error) t 
  | GetAttachment: (GetAttachmentRequest.t, GetAttachmentResponse.t,
  GetAttachmentResponse.error) t 
  | GetTranscript: (GetTranscriptRequest.t, GetTranscriptResponse.t,
  GetTranscriptResponse.error) t 
  | SendEvent: (SendEventRequest.t, SendEventResponse.t,
  SendEventResponse.error) t 
  | SendMessage: (SendMessageRequest.t, SendMessageResponse.t,
  SendMessageResponse.error) t 
  | StartAttachmentUpload: (StartAttachmentUploadRequest.t,
  StartAttachmentUploadResponse.t, StartAttachmentUploadResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CompleteAttachmentUpload -> `POST
  | CreateParticipantConnection -> `POST
  | DisconnectParticipant -> `POST
  | GetAttachment -> `POST
  | GetTranscript -> `POST
  | SendEvent -> `POST
  | SendMessage -> `POST
  | StartAttachmentUpload -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CompleteAttachmentUpload ->
          (Format.kasprintf Uri.of_string)
            "/participant/complete-attachment-upload"
      | CreateParticipantConnection ->
          (Format.kasprintf Uri.of_string) "/participant/connection"
      | DisconnectParticipant ->
          (Format.kasprintf Uri.of_string) "/participant/disconnect"
      | GetAttachment ->
          (Format.kasprintf Uri.of_string) "/participant/attachment"
      | GetTranscript ->
          (Format.kasprintf Uri.of_string) "/participant/transcript"
      | SendEvent -> (Format.kasprintf Uri.of_string) "/participant/event"
      | SendMessage ->
          (Format.kasprintf Uri.of_string) "/participant/message"
      | StartAttachmentUpload ->
          (Format.kasprintf Uri.of_string)
            "/participant/start-attachment-upload")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CompleteAttachmentUpload ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amz-Bearer",
                     (ParticipantToken.to_header
                        req.CompleteAttachmentUploadRequest.connectionToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("AttachmentIds",
                           (AttachmentIdList.to_value
                              req.CompleteAttachmentUploadRequest.attachmentIds));
                      Some
                        ("ClientToken",
                          (NonEmptyClientToken.to_value
                             req.CompleteAttachmentUploadRequest.clientToken))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateParticipantConnection ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amz-Bearer",
                     (ParticipantToken.to_header
                        req.CreateParticipantConnectionRequest.participantToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Type",
                           (ConnectionTypeList.to_value
                              req.CreateParticipantConnectionRequest.type_));
                      Option.map
                        req.CreateParticipantConnectionRequest.connectParticipant
                        ~f:(fun x ->
                              ("ConnectParticipant", (Bool.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DisconnectParticipant ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amz-Bearer",
                     (ParticipantToken.to_header
                        req.DisconnectParticipantRequest.connectionToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DisconnectParticipantRequest.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAttachment ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amz-Bearer",
                     (ParticipantToken.to_header
                        req.GetAttachmentRequest.connectionToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("AttachmentId",
                           (ArtifactId.to_value
                              req.GetAttachmentRequest.attachmentId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTranscript ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amz-Bearer",
                     (ParticipantToken.to_header
                        req.GetTranscriptRequest.connectionToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetTranscriptRequest.contactId
                         ~f:(fun x -> ("ContactId", (ContactId.to_value x)));
                      Option.map req.GetTranscriptRequest.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.GetTranscriptRequest.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.GetTranscriptRequest.scanDirection
                        ~f:(fun x ->
                              ("ScanDirection", (ScanDirection.to_value x)));
                      Option.map req.GetTranscriptRequest.sortOrder
                        ~f:(fun x -> ("SortOrder", (SortKey.to_value x)));
                      Option.map req.GetTranscriptRequest.startPosition
                        ~f:(fun x ->
                              ("StartPosition", (StartPosition.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SendEvent ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amz-Bearer",
                     (ParticipantToken.to_header
                        req.SendEventRequest.connectionToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ContentType",
                           (ChatContentType.to_value
                              req.SendEventRequest.contentType));
                      Option.map req.SendEventRequest.content
                        ~f:(fun x -> ("Content", (ChatContent.to_value x)));
                      Option.map req.SendEventRequest.clientToken
                        ~f:(fun x ->
                              ("ClientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SendMessage ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amz-Bearer",
                     (ParticipantToken.to_header
                        req.SendMessageRequest.connectionToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ContentType",
                           (ChatContentType.to_value
                              req.SendMessageRequest.contentType));
                      Some
                        ("Content",
                          (ChatContent.to_value
                             req.SendMessageRequest.content));
                      Option.map req.SendMessageRequest.clientToken
                        ~f:(fun x ->
                              ("ClientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartAttachmentUpload ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amz-Bearer",
                     (ParticipantToken.to_header
                        req.StartAttachmentUploadRequest.connectionToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ContentType",
                           (ContentType.to_value
                              req.StartAttachmentUploadRequest.contentType));
                      Some
                        ("AttachmentSizeInBytes",
                          (AttachmentSizeInBytes.to_value
                             req.StartAttachmentUploadRequest.attachmentSizeInBytes));
                      Some
                        ("AttachmentName",
                          (AttachmentName.to_value
                             req.StartAttachmentUploadRequest.attachmentName));
                      Some
                        ("ClientToken",
                          (NonEmptyClientToken.to_value
                             req.StartAttachmentUploadRequest.clientToken))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CompleteAttachmentUpload ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (CompleteAttachmentUploadResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error
             (Some CompleteAttachmentUploadResponse.error_of_json))
  | CreateParticipantConnection ->
      if is_success
      then
        Ok
          (CreateParticipantConnectionResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some CreateParticipantConnectionResponse.error_of_json))
  | DisconnectParticipant ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DisconnectParticipantResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some DisconnectParticipantResponse.error_of_json))
  | GetAttachment ->
      if is_success
      then Ok (GetAttachmentResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetAttachmentResponse.error_of_json))
  | GetTranscript ->
      if is_success
      then Ok (GetTranscriptResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetTranscriptResponse.error_of_json))
  | SendEvent ->
      if is_success
      then Ok (SendEventResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SendEventResponse.error_of_json))
  | SendMessage ->
      if is_success
      then Ok (SendMessageResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SendMessageResponse.error_of_json))
  | StartAttachmentUpload ->
      if is_success
      then Ok (StartAttachmentUploadResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StartAttachmentUploadResponse.error_of_json))
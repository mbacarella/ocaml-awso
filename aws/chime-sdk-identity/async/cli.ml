(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_app_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_instance
           (Values.CreateAppInstanceRequest.make ?metadata
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~clientRequestToken ())
           (Some Values.CreateAppInstanceResponse.to_json)
           (Some Values.CreateAppInstanceResponse.error_to_json)])
let create_app_instance_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceAdminArn =
         flag "app-instance-admin-arn" (required string)
           ~doc:"STRING ChimeArn"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_instance_admin
           (Values.CreateAppInstanceAdminRequest.make ~appInstanceAdminArn
              ~appInstanceArn ())
           (Some Values.CreateAppInstanceAdminResponse.to_json)
           (Some Values.CreateAppInstanceAdminResponse.error_to_json)])
let create_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceUserId =
         flag "app-instance-user-id" (required string) ~doc:"STRING UserId"
       and name = flag "name" (required string) ~doc:"STRING UserName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_instance_user
           (Values.CreateAppInstanceUserRequest.make ?metadata
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~appInstanceArn ~appInstanceUserId ~name ~clientRequestToken ())
           (Some Values.CreateAppInstanceUserResponse.to_json)
           (Some Values.CreateAppInstanceUserResponse.error_to_json)])
let delete_app_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_instance
           (Values.DeleteAppInstanceRequest.make ~appInstanceArn ()) None
           None])
let delete_app_instance_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceAdminArn =
         flag "app-instance-admin-arn" (required string)
           ~doc:"STRING ChimeArn"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_instance_admin
           (Values.DeleteAppInstanceAdminRequest.make ~appInstanceAdminArn
              ~appInstanceArn ()) None None])
let delete_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_instance_user
           (Values.DeleteAppInstanceUserRequest.make ~appInstanceUserArn ())
           None None])
let deregister_app_instance_user_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING SensitiveChimeArn"
       and endpointId =
         flag "endpoint-id" (required string) ~doc:"STRING SensitiveString64" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_app_instance_user_endpoint
           (Values.DeregisterAppInstanceUserEndpointRequest.make
              ~appInstanceUserArn ~endpointId ()) None None])
let describe_app_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_instance
           (Values.DescribeAppInstanceRequest.make ~appInstanceArn ())
           (Some Values.DescribeAppInstanceResponse.to_json)
           (Some Values.DescribeAppInstanceResponse.error_to_json)])
let describe_app_instance_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceAdminArn =
         flag "app-instance-admin-arn" (required string)
           ~doc:"STRING ChimeArn"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_instance_admin
           (Values.DescribeAppInstanceAdminRequest.make ~appInstanceAdminArn
              ~appInstanceArn ())
           (Some Values.DescribeAppInstanceAdminResponse.to_json)
           (Some Values.DescribeAppInstanceAdminResponse.error_to_json)])
let describe_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_instance_user
           (Values.DescribeAppInstanceUserRequest.make ~appInstanceUserArn ())
           (Some Values.DescribeAppInstanceUserResponse.to_json)
           (Some Values.DescribeAppInstanceUserResponse.error_to_json)])
let describe_app_instance_user_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING SensitiveString1600"
       and endpointId =
         flag "endpoint-id" (required string) ~doc:"STRING SensitiveString64" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_instance_user_endpoint
           (Values.DescribeAppInstanceUserEndpointRequest.make
              ~appInstanceUserArn ~endpointId ())
           (Some Values.DescribeAppInstanceUserEndpointResponse.to_json)
           (Some Values.DescribeAppInstanceUserEndpointResponse.error_to_json)])
let get_app_instance_retention_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_instance_retention_settings
           (Values.GetAppInstanceRetentionSettingsRequest.make
              ~appInstanceArn ())
           (Some Values.GetAppInstanceRetentionSettingsResponse.to_json)
           (Some Values.GetAppInstanceRetentionSettingsResponse.error_to_json)])
let list_app_instance_admins =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_instance_admins
           (Values.ListAppInstanceAdminsRequest.make ?maxResults ?nextToken
              ~appInstanceArn ())
           (Some Values.ListAppInstanceAdminsResponse.to_json)
           (Some Values.ListAppInstanceAdminsResponse.error_to_json)])
let list_app_instance_user_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING SensitiveChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_instance_user_endpoints
           (Values.ListAppInstanceUserEndpointsRequest.make ?maxResults
              ?nextToken ~appInstanceUserArn ())
           (Some Values.ListAppInstanceUserEndpointsResponse.to_json)
           (Some Values.ListAppInstanceUserEndpointsResponse.error_to_json)])
let list_app_instance_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_instance_users
           (Values.ListAppInstanceUsersRequest.make ?maxResults ?nextToken
              ~appInstanceArn ())
           (Some Values.ListAppInstanceUsersResponse.to_json)
           (Some Values.ListAppInstanceUsersResponse.error_to_json)])
let list_app_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_instances
           (Values.ListAppInstancesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAppInstancesResponse.to_json)
           (Some Values.ListAppInstancesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_app_instance_retention_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceRetentionSettings =
         flag "app-instance-retention-settings" (required json_arg)
           ~doc:"JSON AppInstanceRetentionSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_app_instance_retention_settings
           (Values.PutAppInstanceRetentionSettingsRequest.make
              ~appInstanceArn
              ~appInstanceRetentionSettings:(Values.AppInstanceRetentionSettings.of_json
                                               appInstanceRetentionSettings)
              ())
           (Some Values.PutAppInstanceRetentionSettingsResponse.to_json)
           (Some Values.PutAppInstanceRetentionSettingsResponse.error_to_json)])
let register_app_instance_user_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING SensitiveString1600"
       and allowMessages =
         flag "allow-messages" (optional json_arg) ~doc:"JSON AllowMessages"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING SensitiveChimeArn"
       and type_ =
         flag "type-" (required json_arg)
           ~doc:"JSON AppInstanceUserEndpointType"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING SensitiveChimeArn"
       and endpointAttributes =
         flag "endpoint-attributes" (required json_arg)
           ~doc:"JSON EndpointAttributes"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_app_instance_user_endpoint
           (Values.RegisterAppInstanceUserEndpointRequest.make ?name
              ?allowMessages:(Option.map ~f:Values.AllowMessages.of_json
                                allowMessages) ~appInstanceUserArn
              ~type_:(Values.AppInstanceUserEndpointType.of_json type_)
              ~resourceArn
              ~endpointAttributes:(Values.EndpointAttributes.of_json
                                     endpointAttributes) ~clientRequestToken
              ())
           (Some Values.RegisterAppInstanceUserEndpointResponse.to_json)
           (Some Values.RegisterAppInstanceUserEndpointResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ChimeArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ChimeArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_app_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName"
       and metadata =
         flag "metadata" (required string) ~doc:"STRING Metadata" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app_instance
           (Values.UpdateAppInstanceRequest.make ~appInstanceArn ~name
              ~metadata ()) (Some Values.UpdateAppInstanceResponse.to_json)
           (Some Values.UpdateAppInstanceResponse.error_to_json)])
let update_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn"
       and name = flag "name" (required string) ~doc:"STRING UserName"
       and metadata =
         flag "metadata" (required string) ~doc:"STRING Metadata" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app_instance_user
           (Values.UpdateAppInstanceUserRequest.make ~appInstanceUserArn
              ~name ~metadata ())
           (Some Values.UpdateAppInstanceUserResponse.to_json)
           (Some Values.UpdateAppInstanceUserResponse.error_to_json)])
let update_app_instance_user_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING SensitiveString1600"
       and allowMessages =
         flag "allow-messages" (optional json_arg) ~doc:"JSON AllowMessages"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING SensitiveChimeArn"
       and endpointId =
         flag "endpoint-id" (required string) ~doc:"STRING SensitiveString64" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app_instance_user_endpoint
           (Values.UpdateAppInstanceUserEndpointRequest.make ?name
              ?allowMessages:(Option.map ~f:Values.AllowMessages.of_json
                                allowMessages) ~appInstanceUserArn
              ~endpointId ())
           (Some Values.UpdateAppInstanceUserEndpointResponse.to_json)
           (Some Values.UpdateAppInstanceUserEndpointResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-app-instance", create_app_instance);
    ("create-app-instance-admin", create_app_instance_admin);
    ("create-app-instance-user", create_app_instance_user);
    ("delete-app-instance", delete_app_instance);
    ("delete-app-instance-admin", delete_app_instance_admin);
    ("delete-app-instance-user", delete_app_instance_user);
    ("deregister-app-instance-user-endpoint",
      deregister_app_instance_user_endpoint);
    ("describe-app-instance", describe_app_instance);
    ("describe-app-instance-admin", describe_app_instance_admin);
    ("describe-app-instance-user", describe_app_instance_user);
    ("describe-app-instance-user-endpoint",
      describe_app_instance_user_endpoint);
    ("get-app-instance-retention-settings",
      get_app_instance_retention_settings);
    ("list-app-instance-admins", list_app_instance_admins);
    ("list-app-instance-user-endpoints", list_app_instance_user_endpoints);
    ("list-app-instance-users", list_app_instance_users);
    ("list-app-instances", list_app_instances);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-app-instance-retention-settings",
      put_app_instance_retention_settings);
    ("register-app-instance-user-endpoint",
      register_app_instance_user_endpoint);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-app-instance", update_app_instance);
    ("update-app-instance-user", update_app_instance_user);
    ("update-app-instance-user-endpoint", update_app_instance_user_endpoint)]
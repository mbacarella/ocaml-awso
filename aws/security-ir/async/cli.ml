(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_member_account_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipId =
         flag "membership-id" (required string) ~doc:"STRING MembershipId"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AWSAccountIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_member_account_details
           (Values.BatchGetMemberAccountDetailsRequest.make ~membershipId
              ~accountIds:(Values.AWSAccountIds.of_json accountIds) ())
           (Some Values.BatchGetMemberAccountDetailsResponse.to_json)
           (Some Values.BatchGetMemberAccountDetailsResponse.error_to_json)])
let cancel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipId =
         flag "membership-id" (required string) ~doc:"STRING MembershipId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_membership
           (Values.CancelMembershipRequest.make ~membershipId ())
           (Some Values.CancelMembershipResponse.to_json)
           (Some Values.CancelMembershipResponse.error_to_json)])
let close_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.close_case (Values.CloseCaseRequest.make ~caseId ())
           (Some Values.CloseCaseResponse.to_json)
           (Some Values.CloseCaseResponse.error_to_json)])
let create_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING CreateCaseRequestClientTokenString"
       and threatActorIpAddresses =
         flag "threat-actor-ip-addresses" (optional json_arg)
           ~doc:"JSON ThreatActorIpList"
       and impactedServices =
         flag "impacted-services" (optional json_arg)
           ~doc:"JSON ImpactedServicesList"
       and impactedAwsRegions =
         flag "impacted-aws-regions" (optional json_arg)
           ~doc:"JSON ImpactedAwsRegionList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and resolverType =
         flag "resolver-type" (required json_arg) ~doc:"JSON ResolverType"
       and title = flag "title" (required string) ~doc:"STRING CaseTitle"
       and description =
         flag "description" (required string) ~doc:"STRING CaseDescription"
       and engagementType =
         flag "engagement-type" (required json_arg)
           ~doc:"JSON EngagementType"
       and reportedIncidentStartDate =
         flag "reported-incident-start-date" (required json_arg)
           ~doc:"JSON Timestamp"
       and impactedAccounts =
         flag "impacted-accounts" (required json_arg)
           ~doc:"JSON ImpactedAccounts"
       and watchers =
         flag "watchers" (required json_arg) ~doc:"JSON Watchers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_case
           (Values.CreateCaseRequest.make ?clientToken
              ?threatActorIpAddresses:(Option.map
                                         ~f:Values.ThreatActorIpList.of_json
                                         threatActorIpAddresses)
              ?impactedServices:(Option.map
                                   ~f:Values.ImpactedServicesList.of_json
                                   impactedServices)
              ?impactedAwsRegions:(Option.map
                                     ~f:Values.ImpactedAwsRegionList.of_json
                                     impactedAwsRegions)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~resolverType:(Values.ResolverType.of_json resolverType) ~title
              ~description
              ~engagementType:(Values.EngagementType.of_json engagementType)
              ~reportedIncidentStartDate:(Values.Timestamp.of_json
                                            reportedIncidentStartDate)
              ~impactedAccounts:(Values.ImpactedAccounts.of_json
                                   impactedAccounts)
              ~watchers:(Values.Watchers.of_json watchers) ())
           (Some Values.CreateCaseResponse.to_json)
           (Some Values.CreateCaseResponse.error_to_json)])
let create_case_comment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING CreateCaseCommentRequestClientTokenString"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and body = flag "body" (required string) ~doc:"STRING CommentBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_case_comment
           (Values.CreateCaseCommentRequest.make ?clientToken ~caseId ~body
              ()) (Some Values.CreateCaseCommentResponse.to_json)
           (Some Values.CreateCaseCommentResponse.error_to_json)])
let create_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING CreateMembershipRequestClientTokenString"
       and optInFeatures =
         flag "opt-in-features" (optional json_arg) ~doc:"JSON OptInFeatures"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and coverEntireOrganization =
         flag "cover-entire-organization" (optional bool) ~doc:"BOOL Boolean"
       and membershipName =
         flag "membership-name" (required string)
           ~doc:"STRING MembershipName"
       and incidentResponseTeam =
         flag "incident-response-team" (required json_arg)
           ~doc:"JSON IncidentResponseTeam" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_membership
           (Values.CreateMembershipRequest.make ?clientToken
              ?optInFeatures:(Option.map ~f:Values.OptInFeatures.of_json
                                optInFeatures)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?coverEntireOrganization ~membershipName
              ~incidentResponseTeam:(Values.IncidentResponseTeam.of_json
                                       incidentResponseTeam) ())
           (Some Values.CreateMembershipResponse.to_json)
           (Some Values.CreateMembershipResponse.error_to_json)])
let get_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_case (Values.GetCaseRequest.make ~caseId ())
           (Some Values.GetCaseResponse.to_json)
           (Some Values.GetCaseResponse.error_to_json)])
let get_case_attachment_download_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_case_attachment_download_url
           (Values.GetCaseAttachmentDownloadUrlRequest.make ~caseId
              ~attachmentId ())
           (Some Values.GetCaseAttachmentDownloadUrlResponse.to_json)
           (Some Values.GetCaseAttachmentDownloadUrlResponse.error_to_json)])
let get_case_attachment_upload_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING GetCaseAttachmentUploadUrlRequestClientTokenString"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and fileName =
         flag "file-name" (required string) ~doc:"STRING FileName"
       and contentLength =
         flag "content-length" (required json_arg) ~doc:"JSON ContentLength" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_case_attachment_upload_url
           (Values.GetCaseAttachmentUploadUrlRequest.make ?clientToken
              ~caseId ~fileName
              ~contentLength:(Values.ContentLength.of_json contentLength) ())
           (Some Values.GetCaseAttachmentUploadUrlResponse.to_json)
           (Some Values.GetCaseAttachmentUploadUrlResponse.error_to_json)])
let get_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipId =
         flag "membership-id" (required string) ~doc:"STRING MembershipId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_membership
           (Values.GetMembershipRequest.make ~membershipId ())
           (Some Values.GetMembershipResponse.to_json)
           (Some Values.GetMembershipResponse.error_to_json)])
let list_case_edits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListCaseEditsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCaseEditsRequestMaxResultsInteger"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_case_edits
           (Values.ListCaseEditsRequest.make ?nextToken ?maxResults ~caseId
              ()) (Some Values.ListCaseEditsResponse.to_json)
           (Some Values.ListCaseEditsResponse.error_to_json)])
let list_cases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListCasesRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCasesRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cases
           (Values.ListCasesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListCasesResponse.to_json)
           (Some Values.ListCasesResponse.error_to_json)])
let list_comments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListCommentsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCommentsRequestMaxResultsInteger"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_comments
           (Values.ListCommentsRequest.make ?nextToken ?maxResults ~caseId ())
           (Some Values.ListCommentsResponse.to_json)
           (Some Values.ListCommentsResponse.error_to_json)])
let list_investigations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListInvestigationsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListInvestigationsRequestMaxResultsInteger"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_investigations
           (Values.ListInvestigationsRequest.make ?nextToken ?maxResults
              ~caseId ()) (Some Values.ListInvestigationsResponse.to_json)
           (Some Values.ListInvestigationsResponse.error_to_json)])
let list_memberships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListMembershipsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMembershipsRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_memberships
           (Values.ListMembershipsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListMembershipsResponse.to_json)
           (Some Values.ListMembershipsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let send_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING FeedbackComment"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and resultId =
         flag "result-id" (required string) ~doc:"STRING ResultId"
       and usefulness =
         flag "usefulness" (required json_arg) ~doc:"JSON UsefulnessRating" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_feedback
           (Values.SendFeedbackRequest.make ?comment ~caseId ~resultId
              ~usefulness:(Values.UsefulnessRating.of_json usefulness) ())
           (Some Values.SendFeedbackResponse.to_json)
           (Some Values.SendFeedbackResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and title = flag "title" (optional string) ~doc:"STRING CaseTitle"
       and description =
         flag "description" (optional string) ~doc:"STRING CaseDescription"
       and reportedIncidentStartDate =
         flag "reported-incident-start-date" (optional json_arg)
           ~doc:"JSON Timestamp"
       and actualIncidentStartDate =
         flag "actual-incident-start-date" (optional json_arg)
           ~doc:"JSON Timestamp"
       and engagementType =
         flag "engagement-type" (optional json_arg)
           ~doc:"JSON EngagementType"
       and watchersToAdd =
         flag "watchers-to-add" (optional json_arg) ~doc:"JSON Watchers"
       and watchersToDelete =
         flag "watchers-to-delete" (optional json_arg) ~doc:"JSON Watchers"
       and threatActorIpAddressesToAdd =
         flag "threat-actor-ip-addresses-to-add" (optional json_arg)
           ~doc:"JSON ThreatActorIpList"
       and threatActorIpAddressesToDelete =
         flag "threat-actor-ip-addresses-to-delete" (optional json_arg)
           ~doc:"JSON ThreatActorIpList"
       and impactedServicesToAdd =
         flag "impacted-services-to-add" (optional json_arg)
           ~doc:"JSON ImpactedServicesList"
       and impactedServicesToDelete =
         flag "impacted-services-to-delete" (optional json_arg)
           ~doc:"JSON ImpactedServicesList"
       and impactedAwsRegionsToAdd =
         flag "impacted-aws-regions-to-add" (optional json_arg)
           ~doc:"JSON ImpactedAwsRegionList"
       and impactedAwsRegionsToDelete =
         flag "impacted-aws-regions-to-delete" (optional json_arg)
           ~doc:"JSON ImpactedAwsRegionList"
       and impactedAccountsToAdd =
         flag "impacted-accounts-to-add" (optional json_arg)
           ~doc:"JSON ImpactedAccounts"
       and impactedAccountsToDelete =
         flag "impacted-accounts-to-delete" (optional json_arg)
           ~doc:"JSON ImpactedAccounts"
       and caseMetadata =
         flag "case-metadata" (optional json_arg) ~doc:"JSON CaseMetadata"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_case
           (Values.UpdateCaseRequest.make ?title ?description
              ?reportedIncidentStartDate:(Option.map
                                            ~f:Values.Timestamp.of_json
                                            reportedIncidentStartDate)
              ?actualIncidentStartDate:(Option.map
                                          ~f:Values.Timestamp.of_json
                                          actualIncidentStartDate)
              ?engagementType:(Option.map ~f:Values.EngagementType.of_json
                                 engagementType)
              ?watchersToAdd:(Option.map ~f:Values.Watchers.of_json
                                watchersToAdd)
              ?watchersToDelete:(Option.map ~f:Values.Watchers.of_json
                                   watchersToDelete)
              ?threatActorIpAddressesToAdd:(Option.map
                                              ~f:Values.ThreatActorIpList.of_json
                                              threatActorIpAddressesToAdd)
              ?threatActorIpAddressesToDelete:(Option.map
                                                 ~f:Values.ThreatActorIpList.of_json
                                                 threatActorIpAddressesToDelete)
              ?impactedServicesToAdd:(Option.map
                                        ~f:Values.ImpactedServicesList.of_json
                                        impactedServicesToAdd)
              ?impactedServicesToDelete:(Option.map
                                           ~f:Values.ImpactedServicesList.of_json
                                           impactedServicesToDelete)
              ?impactedAwsRegionsToAdd:(Option.map
                                          ~f:Values.ImpactedAwsRegionList.of_json
                                          impactedAwsRegionsToAdd)
              ?impactedAwsRegionsToDelete:(Option.map
                                             ~f:Values.ImpactedAwsRegionList.of_json
                                             impactedAwsRegionsToDelete)
              ?impactedAccountsToAdd:(Option.map
                                        ~f:Values.ImpactedAccounts.of_json
                                        impactedAccountsToAdd)
              ?impactedAccountsToDelete:(Option.map
                                           ~f:Values.ImpactedAccounts.of_json
                                           impactedAccountsToDelete)
              ?caseMetadata:(Option.map ~f:Values.CaseMetadata.of_json
                               caseMetadata) ~caseId ())
           (Some Values.UpdateCaseResponse.to_json)
           (Some Values.UpdateCaseResponse.error_to_json)])
let update_case_comment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and commentId =
         flag "comment-id" (required string) ~doc:"STRING CommentId"
       and body = flag "body" (required string) ~doc:"STRING CommentBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_case_comment
           (Values.UpdateCaseCommentRequest.make ~caseId ~commentId ~body ())
           (Some Values.UpdateCaseCommentResponse.to_json)
           (Some Values.UpdateCaseCommentResponse.error_to_json)])
let update_case_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and caseStatus =
         flag "case-status" (required json_arg)
           ~doc:"JSON SelfManagedCaseStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_case_status
           (Values.UpdateCaseStatusRequest.make ~caseId
              ~caseStatus:(Values.SelfManagedCaseStatus.of_json caseStatus)
              ()) (Some Values.UpdateCaseStatusResponse.to_json)
           (Some Values.UpdateCaseStatusResponse.error_to_json)])
let update_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and membershipName =
         flag "membership-name" (optional string)
           ~doc:"STRING MembershipName"
       and incidentResponseTeam =
         flag "incident-response-team" (optional json_arg)
           ~doc:"JSON IncidentResponseTeam"
       and optInFeatures =
         flag "opt-in-features" (optional json_arg) ~doc:"JSON OptInFeatures"
       and membershipAccountsConfigurationsUpdate =
         flag "membership-accounts-configurations-update" (optional json_arg)
           ~doc:"JSON MembershipAccountsConfigurationsUpdate"
       and undoMembershipCancellation =
         flag "undo-membership-cancellation" (optional bool)
           ~doc:"BOOL Boolean"
       and membershipId =
         flag "membership-id" (required string) ~doc:"STRING MembershipId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_membership
           (Values.UpdateMembershipRequest.make ?membershipName
              ?incidentResponseTeam:(Option.map
                                       ~f:Values.IncidentResponseTeam.of_json
                                       incidentResponseTeam)
              ?optInFeatures:(Option.map ~f:Values.OptInFeatures.of_json
                                optInFeatures)
              ?membershipAccountsConfigurationsUpdate:(Option.map
                                                         ~f:Values.MembershipAccountsConfigurationsUpdate.of_json
                                                         membershipAccountsConfigurationsUpdate)
              ?undoMembershipCancellation ~membershipId ())
           (Some Values.UpdateMembershipResponse.to_json)
           (Some Values.UpdateMembershipResponse.error_to_json)])
let update_resolver_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caseId = flag "case-id" (required string) ~doc:"STRING CaseId"
       and resolverType =
         flag "resolver-type" (required json_arg) ~doc:"JSON ResolverType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resolver_type
           (Values.UpdateResolverTypeRequest.make ~caseId
              ~resolverType:(Values.ResolverType.of_json resolverType) ())
           (Some Values.UpdateResolverTypeResponse.to_json)
           (Some Values.UpdateResolverTypeResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-member-account-details", batch_get_member_account_details);
    ("cancel-membership", cancel_membership);
    ("close-case", close_case);
    ("create-case", create_case);
    ("create-case-comment", create_case_comment);
    ("create-membership", create_membership);
    ("get-case", get_case);
    ("get-case-attachment-download-url", get_case_attachment_download_url);
    ("get-case-attachment-upload-url", get_case_attachment_upload_url);
    ("get-membership", get_membership);
    ("list-case-edits", list_case_edits);
    ("list-cases", list_cases);
    ("list-comments", list_comments);
    ("list-investigations", list_investigations);
    ("list-memberships", list_memberships);
    ("list-tags-for-resource", list_tags_for_resource);
    ("send-feedback", send_feedback);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-case", update_case);
    ("update-case-comment", update_case_comment);
    ("update-case-status", update_case_status);
    ("update-membership", update_membership);
    ("update-resolver-type", update_resolver_type)]
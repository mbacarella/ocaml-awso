(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_create_bill_scenario_commitment_modification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId"
       and commitmentModifications =
         flag "commitment-modifications" (required json_arg)
           ~doc:"JSON BatchCreateBillScenarioCommitmentModificationEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_bill_scenario_commitment_modification
           (Values.BatchCreateBillScenarioCommitmentModificationRequest.make
              ?clientToken ~billScenarioId
              ~commitmentModifications:(Values.BatchCreateBillScenarioCommitmentModificationEntries.of_json
                                          commitmentModifications) ())
           (Some
              Values.BatchCreateBillScenarioCommitmentModificationResponse.to_json)
           (Some
              Values.BatchCreateBillScenarioCommitmentModificationResponse.error_to_json)])
let batch_create_bill_scenario_usage_modification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId"
       and usageModifications =
         flag "usage-modifications" (required json_arg)
           ~doc:"JSON BatchCreateBillScenarioUsageModificationEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_bill_scenario_usage_modification
           (Values.BatchCreateBillScenarioUsageModificationRequest.make
              ?clientToken ~billScenarioId
              ~usageModifications:(Values.BatchCreateBillScenarioUsageModificationEntries.of_json
                                     usageModifications) ())
           (Some
              Values.BatchCreateBillScenarioUsageModificationResponse.to_json)
           (Some
              Values.BatchCreateBillScenarioUsageModificationResponse.error_to_json)])
let batch_create_workload_estimate_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and workloadEstimateId =
         flag "workload-estimate-id" (required string)
           ~doc:"STRING ResourceId"
       and usage =
         flag "usage" (required json_arg)
           ~doc:"JSON BatchCreateWorkloadEstimateUsageEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_workload_estimate_usage
           (Values.BatchCreateWorkloadEstimateUsageRequest.make ?clientToken
              ~workloadEstimateId
              ~usage:(Values.BatchCreateWorkloadEstimateUsageEntries.of_json
                        usage) ())
           (Some Values.BatchCreateWorkloadEstimateUsageResponse.to_json)
           (Some
              Values.BatchCreateWorkloadEstimateUsageResponse.error_to_json)])
let batch_delete_bill_scenario_commitment_modification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId"
       and ids =
         flag "ids" (required json_arg)
           ~doc:"JSON BatchDeleteBillScenarioCommitmentModificationEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_bill_scenario_commitment_modification
           (Values.BatchDeleteBillScenarioCommitmentModificationRequest.make
              ~billScenarioId
              ~ids:(Values.BatchDeleteBillScenarioCommitmentModificationEntries.of_json
                      ids) ())
           (Some
              Values.BatchDeleteBillScenarioCommitmentModificationResponse.to_json)
           (Some
              Values.BatchDeleteBillScenarioCommitmentModificationResponse.error_to_json)])
let batch_delete_bill_scenario_usage_modification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId"
       and ids =
         flag "ids" (required json_arg)
           ~doc:"JSON BatchDeleteBillScenarioUsageModificationEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_bill_scenario_usage_modification
           (Values.BatchDeleteBillScenarioUsageModificationRequest.make
              ~billScenarioId
              ~ids:(Values.BatchDeleteBillScenarioUsageModificationEntries.of_json
                      ids) ())
           (Some
              Values.BatchDeleteBillScenarioUsageModificationResponse.to_json)
           (Some
              Values.BatchDeleteBillScenarioUsageModificationResponse.error_to_json)])
let batch_delete_workload_estimate_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadEstimateId =
         flag "workload-estimate-id" (required string)
           ~doc:"STRING ResourceId"
       and ids =
         flag "ids" (required json_arg)
           ~doc:"JSON BatchDeleteWorkloadEstimateUsageEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_workload_estimate_usage
           (Values.BatchDeleteWorkloadEstimateUsageRequest.make
              ~workloadEstimateId
              ~ids:(Values.BatchDeleteWorkloadEstimateUsageEntries.of_json
                      ids) ())
           (Some Values.BatchDeleteWorkloadEstimateUsageResponse.to_json)
           (Some
              Values.BatchDeleteWorkloadEstimateUsageResponse.error_to_json)])
let batch_update_bill_scenario_commitment_modification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId"
       and commitmentModifications =
         flag "commitment-modifications" (required json_arg)
           ~doc:"JSON BatchUpdateBillScenarioCommitmentModificationEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_bill_scenario_commitment_modification
           (Values.BatchUpdateBillScenarioCommitmentModificationRequest.make
              ~billScenarioId
              ~commitmentModifications:(Values.BatchUpdateBillScenarioCommitmentModificationEntries.of_json
                                          commitmentModifications) ())
           (Some
              Values.BatchUpdateBillScenarioCommitmentModificationResponse.to_json)
           (Some
              Values.BatchUpdateBillScenarioCommitmentModificationResponse.error_to_json)])
let batch_update_bill_scenario_usage_modification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId"
       and usageModifications =
         flag "usage-modifications" (required json_arg)
           ~doc:"JSON BatchUpdateBillScenarioUsageModificationEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_bill_scenario_usage_modification
           (Values.BatchUpdateBillScenarioUsageModificationRequest.make
              ~billScenarioId
              ~usageModifications:(Values.BatchUpdateBillScenarioUsageModificationEntries.of_json
                                     usageModifications) ())
           (Some
              Values.BatchUpdateBillScenarioUsageModificationResponse.to_json)
           (Some
              Values.BatchUpdateBillScenarioUsageModificationResponse.error_to_json)])
let batch_update_workload_estimate_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadEstimateId =
         flag "workload-estimate-id" (required string)
           ~doc:"STRING ResourceId"
       and usage =
         flag "usage" (required json_arg)
           ~doc:"JSON BatchUpdateWorkloadEstimateUsageEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_workload_estimate_usage
           (Values.BatchUpdateWorkloadEstimateUsageRequest.make
              ~workloadEstimateId
              ~usage:(Values.BatchUpdateWorkloadEstimateUsageEntries.of_json
                        usage) ())
           (Some Values.BatchUpdateWorkloadEstimateUsageResponse.to_json)
           (Some
              Values.BatchUpdateWorkloadEstimateUsageResponse.error_to_json)])
let create_bill_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId"
       and name =
         flag "name" (required string) ~doc:"STRING BillEstimateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bill_estimate
           (Values.CreateBillEstimateRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~billScenarioId
              ~name ()) (Some Values.CreateBillEstimateResponse.to_json)
           (Some Values.CreateBillEstimateResponse.error_to_json)])
let create_bill_scenario =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and groupSharingPreference =
         flag "group-sharing-preference" (optional json_arg)
           ~doc:"JSON GroupSharingPreferenceEnum"
       and costCategoryGroupSharingPreferenceArn =
         flag "cost-category-group-sharing-preference-arn" (optional string)
           ~doc:"STRING CostCategoryArn"
       and name =
         flag "name" (required string) ~doc:"STRING BillScenarioName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bill_scenario
           (Values.CreateBillScenarioRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?groupSharingPreference:(Option.map
                                         ~f:Values.GroupSharingPreferenceEnum.of_json
                                         groupSharingPreference)
              ?costCategoryGroupSharingPreferenceArn ~name ())
           (Some Values.CreateBillScenarioResponse.to_json)
           (Some Values.CreateBillScenarioResponse.error_to_json)])
let create_workload_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and rateType =
         flag "rate-type" (optional json_arg)
           ~doc:"JSON WorkloadEstimateRateType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name =
         flag "name" (required string) ~doc:"STRING WorkloadEstimateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workload_estimate
           (Values.CreateWorkloadEstimateRequest.make ?clientToken
              ?rateType:(Option.map
                           ~f:Values.WorkloadEstimateRateType.of_json
                           rateType)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ())
           (Some Values.CreateWorkloadEstimateResponse.to_json)
           (Some Values.CreateWorkloadEstimateResponse.error_to_json)])
let delete_bill_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bill_estimate
           (Values.DeleteBillEstimateRequest.make ~identifier ())
           (Some Values.DeleteBillEstimateResponse.to_json)
           (Some Values.DeleteBillEstimateResponse.error_to_json)])
let delete_bill_scenario =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bill_scenario
           (Values.DeleteBillScenarioRequest.make ~identifier ())
           (Some Values.DeleteBillScenarioResponse.to_json)
           (Some Values.DeleteBillScenarioResponse.error_to_json)])
let delete_workload_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workload_estimate
           (Values.DeleteWorkloadEstimateRequest.make ~identifier ())
           (Some Values.DeleteWorkloadEstimateResponse.to_json)
           (Some Values.DeleteWorkloadEstimateResponse.error_to_json)])
let get_bill_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bill_estimate
           (Values.GetBillEstimateRequest.make ~identifier ())
           (Some Values.GetBillEstimateResponse.to_json)
           (Some Values.GetBillEstimateResponse.error_to_json)])
let get_bill_scenario =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bill_scenario
           (Values.GetBillScenarioRequest.make ~identifier ())
           (Some Values.GetBillScenarioResponse.to_json)
           (Some Values.GetBillScenarioResponse.error_to_json)])
let get_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_preferences (Values.GetPreferencesRequest.make ())
           (Some Values.GetPreferencesResponse.to_json)
           (Some Values.GetPreferencesResponse.error_to_json)])
let get_workload_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workload_estimate
           (Values.GetWorkloadEstimateRequest.make ~identifier ())
           (Some Values.GetWorkloadEstimateResponse.to_json)
           (Some Values.GetWorkloadEstimateResponse.error_to_json)])
let list_bill_estimate_commitments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and billEstimateId =
         flag "bill-estimate-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bill_estimate_commitments
           (Values.ListBillEstimateCommitmentsRequest.make ?nextToken
              ?maxResults ~billEstimateId ())
           (Some Values.ListBillEstimateCommitmentsResponse.to_json)
           (Some Values.ListBillEstimateCommitmentsResponse.error_to_json)])
let list_bill_estimate_input_commitment_modifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and billEstimateId =
         flag "bill-estimate-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bill_estimate_input_commitment_modifications
           (Values.ListBillEstimateInputCommitmentModificationsRequest.make
              ?nextToken ?maxResults ~billEstimateId ())
           (Some
              Values.ListBillEstimateInputCommitmentModificationsResponse.to_json)
           (Some
              Values.ListBillEstimateInputCommitmentModificationsResponse.error_to_json)])
let list_bill_estimate_input_usage_modifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListUsageFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and billEstimateId =
         flag "bill-estimate-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bill_estimate_input_usage_modifications
           (Values.ListBillEstimateInputUsageModificationsRequest.make
              ?filters:(Option.map ~f:Values.ListUsageFilters.of_json filters)
              ?nextToken ?maxResults ~billEstimateId ())
           (Some
              Values.ListBillEstimateInputUsageModificationsResponse.to_json)
           (Some
              Values.ListBillEstimateInputUsageModificationsResponse.error_to_json)])
let list_bill_estimate_line_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListBillEstimateLineItemsFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and billEstimateId =
         flag "bill-estimate-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bill_estimate_line_items
           (Values.ListBillEstimateLineItemsRequest.make
              ?filters:(Option.map
                          ~f:Values.ListBillEstimateLineItemsFilters.of_json
                          filters) ?nextToken ?maxResults ~billEstimateId ())
           (Some Values.ListBillEstimateLineItemsResponse.to_json)
           (Some Values.ListBillEstimateLineItemsResponse.error_to_json)])
let list_bill_estimates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListBillEstimatesFilters"
       and createdAtFilter =
         flag "created-at-filter" (optional json_arg)
           ~doc:"JSON FilterTimestamp"
       and expiresAtFilter =
         flag "expires-at-filter" (optional json_arg)
           ~doc:"JSON FilterTimestamp"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bill_estimates
           (Values.ListBillEstimatesRequest.make
              ?filters:(Option.map ~f:Values.ListBillEstimatesFilters.of_json
                          filters)
              ?createdAtFilter:(Option.map ~f:Values.FilterTimestamp.of_json
                                  createdAtFilter)
              ?expiresAtFilter:(Option.map ~f:Values.FilterTimestamp.of_json
                                  expiresAtFilter) ?nextToken ?maxResults ())
           (Some Values.ListBillEstimatesResponse.to_json)
           (Some Values.ListBillEstimatesResponse.error_to_json)])
let list_bill_scenario_commitment_modifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bill_scenario_commitment_modifications
           (Values.ListBillScenarioCommitmentModificationsRequest.make
              ?nextToken ?maxResults ~billScenarioId ())
           (Some
              Values.ListBillScenarioCommitmentModificationsResponse.to_json)
           (Some
              Values.ListBillScenarioCommitmentModificationsResponse.error_to_json)])
let list_bill_scenario_usage_modifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListUsageFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and billScenarioId =
         flag "bill-scenario-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bill_scenario_usage_modifications
           (Values.ListBillScenarioUsageModificationsRequest.make
              ?filters:(Option.map ~f:Values.ListUsageFilters.of_json filters)
              ?nextToken ?maxResults ~billScenarioId ())
           (Some Values.ListBillScenarioUsageModificationsResponse.to_json)
           (Some
              Values.ListBillScenarioUsageModificationsResponse.error_to_json)])
let list_bill_scenarios =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListBillScenariosFilters"
       and createdAtFilter =
         flag "created-at-filter" (optional json_arg)
           ~doc:"JSON FilterTimestamp"
       and expiresAtFilter =
         flag "expires-at-filter" (optional json_arg)
           ~doc:"JSON FilterTimestamp"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bill_scenarios
           (Values.ListBillScenariosRequest.make
              ?filters:(Option.map ~f:Values.ListBillScenariosFilters.of_json
                          filters)
              ?createdAtFilter:(Option.map ~f:Values.FilterTimestamp.of_json
                                  createdAtFilter)
              ?expiresAtFilter:(Option.map ~f:Values.FilterTimestamp.of_json
                                  expiresAtFilter) ?nextToken ?maxResults ())
           (Some Values.ListBillScenariosResponse.to_json)
           (Some Values.ListBillScenariosResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_workload_estimate_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListUsageFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT WorkloadEstimateUsageMaxResults"
       and workloadEstimateId =
         flag "workload-estimate-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workload_estimate_usage
           (Values.ListWorkloadEstimateUsageRequest.make
              ?filters:(Option.map ~f:Values.ListUsageFilters.of_json filters)
              ?nextToken ?maxResults ~workloadEstimateId ())
           (Some Values.ListWorkloadEstimateUsageResponse.to_json)
           (Some Values.ListWorkloadEstimateUsageResponse.error_to_json)])
let list_workload_estimates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and createdAtFilter =
         flag "created-at-filter" (optional json_arg)
           ~doc:"JSON FilterTimestamp"
       and expiresAtFilter =
         flag "expires-at-filter" (optional json_arg)
           ~doc:"JSON FilterTimestamp"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListWorkloadEstimatesFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workload_estimates
           (Values.ListWorkloadEstimatesRequest.make
              ?createdAtFilter:(Option.map ~f:Values.FilterTimestamp.of_json
                                  createdAtFilter)
              ?expiresAtFilter:(Option.map ~f:Values.FilterTimestamp.of_json
                                  expiresAtFilter)
              ?filters:(Option.map
                          ~f:Values.ListWorkloadEstimatesFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.ListWorkloadEstimatesResponse.to_json)
           (Some Values.ListWorkloadEstimatesResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON ResourceTagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.ResourceTagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_bill_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING BillEstimateName"
       and expiresAt =
         flag "expires-at" (optional json_arg) ~doc:"JSON Timestamp"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bill_estimate
           (Values.UpdateBillEstimateRequest.make ?name
              ?expiresAt:(Option.map ~f:Values.Timestamp.of_json expiresAt)
              ~identifier ())
           (Some Values.UpdateBillEstimateResponse.to_json)
           (Some Values.UpdateBillEstimateResponse.error_to_json)])
let update_bill_scenario =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING BillScenarioName"
       and expiresAt =
         flag "expires-at" (optional json_arg) ~doc:"JSON Timestamp"
       and groupSharingPreference =
         flag "group-sharing-preference" (optional json_arg)
           ~doc:"JSON GroupSharingPreferenceEnum"
       and costCategoryGroupSharingPreferenceArn =
         flag "cost-category-group-sharing-preference-arn" (optional string)
           ~doc:"STRING CostCategoryArn"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bill_scenario
           (Values.UpdateBillScenarioRequest.make ?name
              ?expiresAt:(Option.map ~f:Values.Timestamp.of_json expiresAt)
              ?groupSharingPreference:(Option.map
                                         ~f:Values.GroupSharingPreferenceEnum.of_json
                                         groupSharingPreference)
              ?costCategoryGroupSharingPreferenceArn ~identifier ())
           (Some Values.UpdateBillScenarioResponse.to_json)
           (Some Values.UpdateBillScenarioResponse.error_to_json)])
let update_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managementAccountRateTypeSelections =
         flag "management-account-rate-type-selections" (optional json_arg)
           ~doc:"JSON RateTypes"
       and memberAccountRateTypeSelections =
         flag "member-account-rate-type-selections" (optional json_arg)
           ~doc:"JSON RateTypes"
       and standaloneAccountRateTypeSelections =
         flag "standalone-account-rate-type-selections" (optional json_arg)
           ~doc:"JSON RateTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_preferences
           (Values.UpdatePreferencesRequest.make
              ?managementAccountRateTypeSelections:(Option.map
                                                      ~f:Values.RateTypes.of_json
                                                      managementAccountRateTypeSelections)
              ?memberAccountRateTypeSelections:(Option.map
                                                  ~f:Values.RateTypes.of_json
                                                  memberAccountRateTypeSelections)
              ?standaloneAccountRateTypeSelections:(Option.map
                                                      ~f:Values.RateTypes.of_json
                                                      standaloneAccountRateTypeSelections)
              ()) (Some Values.UpdatePreferencesResponse.to_json)
           (Some Values.UpdatePreferencesResponse.error_to_json)])
let update_workload_estimate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING WorkloadEstimateName"
       and expiresAt =
         flag "expires-at" (optional json_arg) ~doc:"JSON Timestamp"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workload_estimate
           (Values.UpdateWorkloadEstimateRequest.make ?name
              ?expiresAt:(Option.map ~f:Values.Timestamp.of_json expiresAt)
              ~identifier ())
           (Some Values.UpdateWorkloadEstimateResponse.to_json)
           (Some Values.UpdateWorkloadEstimateResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-create-bill-scenario-commitment-modification",
       batch_create_bill_scenario_commitment_modification);
    ("batch-create-bill-scenario-usage-modification",
      batch_create_bill_scenario_usage_modification);
    ("batch-create-workload-estimate-usage",
      batch_create_workload_estimate_usage);
    ("batch-delete-bill-scenario-commitment-modification",
      batch_delete_bill_scenario_commitment_modification);
    ("batch-delete-bill-scenario-usage-modification",
      batch_delete_bill_scenario_usage_modification);
    ("batch-delete-workload-estimate-usage",
      batch_delete_workload_estimate_usage);
    ("batch-update-bill-scenario-commitment-modification",
      batch_update_bill_scenario_commitment_modification);
    ("batch-update-bill-scenario-usage-modification",
      batch_update_bill_scenario_usage_modification);
    ("batch-update-workload-estimate-usage",
      batch_update_workload_estimate_usage);
    ("create-bill-estimate", create_bill_estimate);
    ("create-bill-scenario", create_bill_scenario);
    ("create-workload-estimate", create_workload_estimate);
    ("delete-bill-estimate", delete_bill_estimate);
    ("delete-bill-scenario", delete_bill_scenario);
    ("delete-workload-estimate", delete_workload_estimate);
    ("get-bill-estimate", get_bill_estimate);
    ("get-bill-scenario", get_bill_scenario);
    ("get-preferences", get_preferences);
    ("get-workload-estimate", get_workload_estimate);
    ("list-bill-estimate-commitments", list_bill_estimate_commitments);
    ("list-bill-estimate-input-commitment-modifications",
      list_bill_estimate_input_commitment_modifications);
    ("list-bill-estimate-input-usage-modifications",
      list_bill_estimate_input_usage_modifications);
    ("list-bill-estimate-line-items", list_bill_estimate_line_items);
    ("list-bill-estimates", list_bill_estimates);
    ("list-bill-scenario-commitment-modifications",
      list_bill_scenario_commitment_modifications);
    ("list-bill-scenario-usage-modifications",
      list_bill_scenario_usage_modifications);
    ("list-bill-scenarios", list_bill_scenarios);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-workload-estimate-usage", list_workload_estimate_usage);
    ("list-workload-estimates", list_workload_estimates);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-bill-estimate", update_bill_estimate);
    ("update-bill-scenario", update_bill_scenario);
    ("update-preferences", update_preferences);
    ("update-workload-estimate", update_workload_estimate)]
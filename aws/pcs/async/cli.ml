(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and slurmConfiguration =
         flag "slurm-configuration" (optional json_arg)
           ~doc:"JSON ClusterSlurmConfigurationRequest"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON RequestTagMap"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING ClusterName"
       and scheduler =
         flag "scheduler" (required json_arg) ~doc:"JSON SchedulerRequest"
       and size = flag "size" (required json_arg) ~doc:"JSON Size"
       and networking =
         flag "networking" (required json_arg) ~doc:"JSON NetworkingRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make
              ?slurmConfiguration:(Option.map
                                     ~f:Values.ClusterSlurmConfigurationRequest.of_json
                                     slurmConfiguration) ?clientToken
              ?tags:(Option.map ~f:Values.RequestTagMap.of_json tags)
              ~clusterName
              ~scheduler:(Values.SchedulerRequest.of_json scheduler)
              ~size:(Values.Size.of_json size)
              ~networking:(Values.NetworkingRequest.of_json networking) ())
           (Some Values.CreateClusterResponse.to_json)
           (Some Values.CreateClusterResponse.error_to_json)])
let create_compute_node_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amiId = flag "ami-id" (optional string) ~doc:"STRING AmiId"
       and purchaseOption =
         flag "purchase-option" (optional json_arg)
           ~doc:"JSON PurchaseOption"
       and spotOptions =
         flag "spot-options" (optional json_arg) ~doc:"JSON SpotOptions"
       and slurmConfiguration =
         flag "slurm-configuration" (optional json_arg)
           ~doc:"JSON ComputeNodeGroupSlurmConfigurationRequest"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON RequestTagMap"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and computeNodeGroupName =
         flag "compute-node-group-name" (required string)
           ~doc:"STRING ComputeNodeGroupName"
       and subnetIds =
         flag "subnet-ids" (required json_arg) ~doc:"JSON StringList"
       and customLaunchTemplate =
         flag "custom-launch-template" (required json_arg)
           ~doc:"JSON CustomLaunchTemplate"
       and iamInstanceProfileArn =
         flag "iam-instance-profile-arn" (required string)
           ~doc:"STRING InstanceProfileArn"
       and scalingConfiguration =
         flag "scaling-configuration" (required json_arg)
           ~doc:"JSON ScalingConfigurationRequest"
       and instanceConfigs =
         flag "instance-configs" (required json_arg) ~doc:"JSON InstanceList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_compute_node_group
           (Values.CreateComputeNodeGroupRequest.make ?amiId
              ?purchaseOption:(Option.map ~f:Values.PurchaseOption.of_json
                                 purchaseOption)
              ?spotOptions:(Option.map ~f:Values.SpotOptions.of_json
                              spotOptions)
              ?slurmConfiguration:(Option.map
                                     ~f:Values.ComputeNodeGroupSlurmConfigurationRequest.of_json
                                     slurmConfiguration) ?clientToken
              ?tags:(Option.map ~f:Values.RequestTagMap.of_json tags)
              ~clusterIdentifier ~computeNodeGroupName
              ~subnetIds:(Values.StringList.of_json subnetIds)
              ~customLaunchTemplate:(Values.CustomLaunchTemplate.of_json
                                       customLaunchTemplate)
              ~iamInstanceProfileArn
              ~scalingConfiguration:(Values.ScalingConfigurationRequest.of_json
                                       scalingConfiguration)
              ~instanceConfigs:(Values.InstanceList.of_json instanceConfigs)
              ()) (Some Values.CreateComputeNodeGroupResponse.to_json)
           (Some Values.CreateComputeNodeGroupResponse.error_to_json)])
let create_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and computeNodeGroupConfigurations =
         flag "compute-node-group-configurations" (optional json_arg)
           ~doc:"JSON ComputeNodeGroupConfigurationList"
       and slurmConfiguration =
         flag "slurm-configuration" (optional json_arg)
           ~doc:"JSON QueueSlurmConfigurationRequest"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON RequestTagMap"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and queueName =
         flag "queue-name" (required string) ~doc:"STRING QueueName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_queue
           (Values.CreateQueueRequest.make
              ?computeNodeGroupConfigurations:(Option.map
                                                 ~f:Values.ComputeNodeGroupConfigurationList.of_json
                                                 computeNodeGroupConfigurations)
              ?slurmConfiguration:(Option.map
                                     ~f:Values.QueueSlurmConfigurationRequest.of_json
                                     slurmConfiguration) ?clientToken
              ?tags:(Option.map ~f:Values.RequestTagMap.of_json tags)
              ~clusterIdentifier ~queueName ())
           (Some Values.CreateQueueResponse.to_json)
           (Some Values.CreateQueueResponse.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster
           (Values.DeleteClusterRequest.make ?clientToken ~clusterIdentifier
              ()) (Some Values.DeleteClusterResponse.to_json)
           (Some Values.DeleteClusterResponse.error_to_json)])
let delete_compute_node_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and computeNodeGroupIdentifier =
         flag "compute-node-group-identifier" (required string)
           ~doc:"STRING ComputeNodeGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_compute_node_group
           (Values.DeleteComputeNodeGroupRequest.make ?clientToken
              ~clusterIdentifier ~computeNodeGroupIdentifier ())
           (Some Values.DeleteComputeNodeGroupResponse.to_json)
           (Some Values.DeleteComputeNodeGroupResponse.error_to_json)])
let delete_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and queueIdentifier =
         flag "queue-identifier" (required string)
           ~doc:"STRING QueueIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queue
           (Values.DeleteQueueRequest.make ?clientToken ~clusterIdentifier
              ~queueIdentifier ()) (Some Values.DeleteQueueResponse.to_json)
           (Some Values.DeleteQueueResponse.error_to_json)])
let get_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cluster
           (Values.GetClusterRequest.make ~clusterIdentifier ())
           (Some Values.GetClusterResponse.to_json)
           (Some Values.GetClusterResponse.error_to_json)])
let get_compute_node_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and computeNodeGroupIdentifier =
         flag "compute-node-group-identifier" (required string)
           ~doc:"STRING ComputeNodeGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_compute_node_group
           (Values.GetComputeNodeGroupRequest.make ~clusterIdentifier
              ~computeNodeGroupIdentifier ())
           (Some Values.GetComputeNodeGroupResponse.to_json)
           (Some Values.GetComputeNodeGroupResponse.error_to_json)])
let get_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and queueIdentifier =
         flag "queue-identifier" (required string)
           ~doc:"STRING QueueIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_queue
           (Values.GetQueueRequest.make ~clusterIdentifier ~queueIdentifier
              ()) (Some Values.GetQueueResponse.to_json)
           (Some Values.GetQueueResponse.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersRequest.make ?nextToken ?maxResults ())
           (Some Values.ListClustersResponse.to_json)
           (Some Values.ListClustersResponse.error_to_json)])
let list_compute_node_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_compute_node_groups
           (Values.ListComputeNodeGroupsRequest.make ?nextToken ?maxResults
              ~clusterIdentifier ())
           (Some Values.ListComputeNodeGroupsResponse.to_json)
           (Some Values.ListComputeNodeGroupsResponse.error_to_json)])
let list_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queues
           (Values.ListQueuesRequest.make ?nextToken ?maxResults
              ~clusterIdentifier ()) (Some Values.ListQueuesResponse.to_json)
           (Some Values.ListQueuesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let register_compute_node_group_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and bootstrapId =
         flag "bootstrap-id" (required string) ~doc:"STRING BootstrapId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_compute_node_group_instance
           (Values.RegisterComputeNodeGroupInstanceRequest.make
              ~clusterIdentifier ~bootstrapId ())
           (Some Values.RegisterComputeNodeGroupInstanceResponse.to_json)
           (Some
              Values.RegisterComputeNodeGroupInstanceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON RequestTagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.RequestTagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and slurmConfiguration =
         flag "slurm-configuration" (optional json_arg)
           ~doc:"JSON UpdateClusterSlurmConfigurationRequest"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster
           (Values.UpdateClusterRequest.make ?clientToken
              ?slurmConfiguration:(Option.map
                                     ~f:Values.UpdateClusterSlurmConfigurationRequest.of_json
                                     slurmConfiguration) ~clusterIdentifier
              ()) (Some Values.UpdateClusterResponse.to_json)
           (Some Values.UpdateClusterResponse.error_to_json)])
let update_compute_node_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amiId = flag "ami-id" (optional string) ~doc:"STRING AmiId"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON StringList"
       and customLaunchTemplate =
         flag "custom-launch-template" (optional json_arg)
           ~doc:"JSON CustomLaunchTemplate"
       and purchaseOption =
         flag "purchase-option" (optional json_arg)
           ~doc:"JSON PurchaseOption"
       and spotOptions =
         flag "spot-options" (optional json_arg) ~doc:"JSON SpotOptions"
       and scalingConfiguration =
         flag "scaling-configuration" (optional json_arg)
           ~doc:"JSON ScalingConfigurationRequest"
       and iamInstanceProfileArn =
         flag "iam-instance-profile-arn" (optional string)
           ~doc:"STRING InstanceProfileArn"
       and slurmConfiguration =
         flag "slurm-configuration" (optional json_arg)
           ~doc:"JSON UpdateComputeNodeGroupSlurmConfigurationRequest"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and computeNodeGroupIdentifier =
         flag "compute-node-group-identifier" (required string)
           ~doc:"STRING ComputeNodeGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_compute_node_group
           (Values.UpdateComputeNodeGroupRequest.make ?amiId
              ?subnetIds:(Option.map ~f:Values.StringList.of_json subnetIds)
              ?customLaunchTemplate:(Option.map
                                       ~f:Values.CustomLaunchTemplate.of_json
                                       customLaunchTemplate)
              ?purchaseOption:(Option.map ~f:Values.PurchaseOption.of_json
                                 purchaseOption)
              ?spotOptions:(Option.map ~f:Values.SpotOptions.of_json
                              spotOptions)
              ?scalingConfiguration:(Option.map
                                       ~f:Values.ScalingConfigurationRequest.of_json
                                       scalingConfiguration)
              ?iamInstanceProfileArn
              ?slurmConfiguration:(Option.map
                                     ~f:Values.UpdateComputeNodeGroupSlurmConfigurationRequest.of_json
                                     slurmConfiguration) ?clientToken
              ~clusterIdentifier ~computeNodeGroupIdentifier ())
           (Some Values.UpdateComputeNodeGroupResponse.to_json)
           (Some Values.UpdateComputeNodeGroupResponse.error_to_json)])
let update_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and computeNodeGroupConfigurations =
         flag "compute-node-group-configurations" (optional json_arg)
           ~doc:"JSON ComputeNodeGroupConfigurationList"
       and slurmConfiguration =
         flag "slurm-configuration" (optional json_arg)
           ~doc:"JSON UpdateQueueSlurmConfigurationRequest"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING SBClientToken"
       and clusterIdentifier =
         flag "cluster-identifier" (required string)
           ~doc:"STRING ClusterIdentifier"
       and queueIdentifier =
         flag "queue-identifier" (required string)
           ~doc:"STRING QueueIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue
           (Values.UpdateQueueRequest.make
              ?computeNodeGroupConfigurations:(Option.map
                                                 ~f:Values.ComputeNodeGroupConfigurationList.of_json
                                                 computeNodeGroupConfigurations)
              ?slurmConfiguration:(Option.map
                                     ~f:Values.UpdateQueueSlurmConfigurationRequest.of_json
                                     slurmConfiguration) ?clientToken
              ~clusterIdentifier ~queueIdentifier ())
           (Some Values.UpdateQueueResponse.to_json)
           (Some Values.UpdateQueueResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-cluster", create_cluster);
    ("create-compute-node-group", create_compute_node_group);
    ("create-queue", create_queue);
    ("delete-cluster", delete_cluster);
    ("delete-compute-node-group", delete_compute_node_group);
    ("delete-queue", delete_queue);
    ("get-cluster", get_cluster);
    ("get-compute-node-group", get_compute_node_group);
    ("get-queue", get_queue);
    ("list-clusters", list_clusters);
    ("list-compute-node-groups", list_compute_node_groups);
    ("list-queues", list_queues);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-compute-node-group-instance",
      register_compute_node_group_instance);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-cluster", update_cluster);
    ("update-compute-node-group", update_compute_node_group);
    ("update-queue", update_queue)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_accounts
           (Values.AssociateAccountsRequest.make ?clientToken
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.AssociateAccountsResponse.to_json)
           (Some Values.AssociateAccountsResponse.error_to_json)])
let create_automation_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING RuleDescription"
       and organizationConfiguration =
         flag "organization-configuration" (optional json_arg)
           ~doc:"JSON OrganizationConfiguration"
       and priority = flag "priority" (optional string) ~doc:"STRING String"
       and criteria =
         flag "criteria" (optional json_arg) ~doc:"JSON Criteria"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name = flag "name" (required string) ~doc:"STRING RuleName"
       and ruleType =
         flag "rule-type" (required json_arg) ~doc:"JSON RuleType"
       and recommendedActionTypes =
         flag "recommended-action-types" (required json_arg)
           ~doc:"JSON RecommendedActionTypeList"
       and schedule =
         flag "schedule" (required json_arg) ~doc:"JSON Schedule"
       and status = flag "status" (required json_arg) ~doc:"JSON RuleStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_automation_rule
           (Values.CreateAutomationRuleRequest.make ?description
              ?organizationConfiguration:(Option.map
                                            ~f:Values.OrganizationConfiguration.of_json
                                            organizationConfiguration)
              ?priority
              ?criteria:(Option.map ~f:Values.Criteria.of_json criteria)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~name ~ruleType:(Values.RuleType.of_json ruleType)
              ~recommendedActionTypes:(Values.RecommendedActionTypeList.of_json
                                         recommendedActionTypes)
              ~schedule:(Values.Schedule.of_json schedule)
              ~status:(Values.RuleStatus.of_json status) ())
           (Some Values.CreateAutomationRuleResponse.to_json)
           (Some Values.CreateAutomationRuleResponse.error_to_json)])
let delete_automation_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and ruleArn = flag "rule-arn" (required string) ~doc:"STRING RuleArn"
       and ruleRevision =
         flag "rule-revision" (required json_arg) ~doc:"JSON Long" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_automation_rule
           (Values.DeleteAutomationRuleRequest.make ?clientToken ~ruleArn
              ~ruleRevision:(Values.Long.of_json ruleRevision) ())
           (Some Values.DeleteAutomationRuleResponse.to_json)
           (Some Values.DeleteAutomationRuleResponse.error_to_json)])
let disassociate_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_accounts
           (Values.DisassociateAccountsRequest.make ?clientToken
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.DisassociateAccountsResponse.to_json)
           (Some Values.DisassociateAccountsResponse.error_to_json)])
let get_automation_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventId = flag "event-id" (required string) ~doc:"STRING EventId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automation_event
           (Values.GetAutomationEventRequest.make ~eventId ())
           (Some Values.GetAutomationEventResponse.to_json)
           (Some Values.GetAutomationEventResponse.error_to_json)])
let get_automation_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleArn = flag "rule-arn" (required string) ~doc:"STRING RuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automation_rule
           (Values.GetAutomationRuleRequest.make ~ruleArn ())
           (Some Values.GetAutomationRuleResponse.to_json)
           (Some Values.GetAutomationRuleResponse.error_to_json)])
let get_enrollment_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_enrollment_configuration
           (Values.GetEnrollmentConfigurationRequest.make ())
           (Some Values.GetEnrollmentConfigurationResponse.to_json)
           (Some Values.GetEnrollmentConfigurationResponse.error_to_json)])
let list_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAccountsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accounts
           (Values.ListAccountsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAccountsResponse.to_json)
           (Some Values.ListAccountsResponse.error_to_json)])
let list_automation_event_steps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAutomationEventStepsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and eventId = flag "event-id" (required string) ~doc:"STRING EventId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automation_event_steps
           (Values.ListAutomationEventStepsRequest.make ?maxResults
              ?nextToken ~eventId ())
           (Some Values.ListAutomationEventStepsResponse.to_json)
           (Some Values.ListAutomationEventStepsResponse.error_to_json)])
let list_automation_event_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON AutomationEventFilterList"
       and startDateInclusive =
         flag "start-date-inclusive" (optional string) ~doc:"STRING String"
       and endDateExclusive =
         flag "end-date-exclusive" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAutomationEventSummariesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automation_event_summaries
           (Values.ListAutomationEventSummariesRequest.make
              ?filters:(Option.map
                          ~f:Values.AutomationEventFilterList.of_json filters)
              ?startDateInclusive ?endDateExclusive ?maxResults ?nextToken ())
           (Some Values.ListAutomationEventSummariesResponse.to_json)
           (Some Values.ListAutomationEventSummariesResponse.error_to_json)])
let list_automation_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON AutomationEventFilterList"
       and startTimeInclusive =
         flag "start-time-inclusive" (optional json_arg)
           ~doc:"JSON Timestamp"
       and endTimeExclusive =
         flag "end-time-exclusive" (optional json_arg) ~doc:"JSON Timestamp"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAutomationEventsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automation_events
           (Values.ListAutomationEventsRequest.make
              ?filters:(Option.map
                          ~f:Values.AutomationEventFilterList.of_json filters)
              ?startTimeInclusive:(Option.map ~f:Values.Timestamp.of_json
                                     startTimeInclusive)
              ?endTimeExclusive:(Option.map ~f:Values.Timestamp.of_json
                                   endTimeExclusive) ?maxResults ?nextToken
              ()) (Some Values.ListAutomationEventsResponse.to_json)
           (Some Values.ListAutomationEventsResponse.error_to_json)])
let list_automation_rule_preview =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationScope =
         flag "organization-scope" (optional json_arg)
           ~doc:"JSON OrganizationScope"
       and criteria =
         flag "criteria" (optional json_arg) ~doc:"JSON Criteria"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAutomationRulePreviewRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ruleType =
         flag "rule-type" (required json_arg) ~doc:"JSON RuleType"
       and recommendedActionTypes =
         flag "recommended-action-types" (required json_arg)
           ~doc:"JSON RecommendedActionTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automation_rule_preview
           (Values.ListAutomationRulePreviewRequest.make
              ?organizationScope:(Option.map
                                    ~f:Values.OrganizationScope.of_json
                                    organizationScope)
              ?criteria:(Option.map ~f:Values.Criteria.of_json criteria)
              ?maxResults ?nextToken
              ~ruleType:(Values.RuleType.of_json ruleType)
              ~recommendedActionTypes:(Values.RecommendedActionTypeList.of_json
                                         recommendedActionTypes) ())
           (Some Values.ListAutomationRulePreviewResponse.to_json)
           (Some Values.ListAutomationRulePreviewResponse.error_to_json)])
let list_automation_rule_preview_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationScope =
         flag "organization-scope" (optional json_arg)
           ~doc:"JSON OrganizationScope"
       and criteria =
         flag "criteria" (optional json_arg) ~doc:"JSON Criteria"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAutomationRulePreviewSummariesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ruleType =
         flag "rule-type" (required json_arg) ~doc:"JSON RuleType"
       and recommendedActionTypes =
         flag "recommended-action-types" (required json_arg)
           ~doc:"JSON RecommendedActionTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automation_rule_preview_summaries
           (Values.ListAutomationRulePreviewSummariesRequest.make
              ?organizationScope:(Option.map
                                    ~f:Values.OrganizationScope.of_json
                                    organizationScope)
              ?criteria:(Option.map ~f:Values.Criteria.of_json criteria)
              ?maxResults ?nextToken
              ~ruleType:(Values.RuleType.of_json ruleType)
              ~recommendedActionTypes:(Values.RecommendedActionTypeList.of_json
                                         recommendedActionTypes) ())
           (Some Values.ListAutomationRulePreviewSummariesResponse.to_json)
           (Some
              Values.ListAutomationRulePreviewSummariesResponse.error_to_json)])
let list_automation_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAutomationRulesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automation_rules
           (Values.ListAutomationRulesRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.ListAutomationRulesResponse.to_json)
           (Some Values.ListAutomationRulesResponse.error_to_json)])
let list_recommended_action_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON RecommendedActionFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRecommendedActionSummariesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommended_action_summaries
           (Values.ListRecommendedActionSummariesRequest.make
              ?filters:(Option.map
                          ~f:Values.RecommendedActionFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListRecommendedActionSummariesResponse.to_json)
           (Some Values.ListRecommendedActionSummariesResponse.error_to_json)])
let list_recommended_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON RecommendedActionFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRecommendedActionsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommended_actions
           (Values.ListRecommendedActionsRequest.make
              ?filters:(Option.map
                          ~f:Values.RecommendedActionFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListRecommendedActionsResponse.to_json)
           (Some Values.ListRecommendedActionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING RuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let rollback_automation_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and eventId = flag "event-id" (required string) ~doc:"STRING EventId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rollback_automation_event
           (Values.RollbackAutomationEventRequest.make ?clientToken ~eventId
              ()) (Some Values.RollbackAutomationEventResponse.to_json)
           (Some Values.RollbackAutomationEventResponse.error_to_json)])
let start_automation_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and recommendedActionId =
         flag "recommended-action-id" (required string)
           ~doc:"STRING RecommendedActionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_automation_event
           (Values.StartAutomationEventRequest.make ?clientToken
              ~recommendedActionId ())
           (Some Values.StartAutomationEventResponse.to_json)
           (Some Values.StartAutomationEventResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING RuleArn"
       and ruleRevision =
         flag "rule-revision" (required json_arg) ~doc:"JSON Long"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ?clientToken ~resourceArn
              ~ruleRevision:(Values.Long.of_json ruleRevision)
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING RuleArn"
       and ruleRevision =
         flag "rule-revision" (required json_arg) ~doc:"JSON Long"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ?clientToken ~resourceArn
              ~ruleRevision:(Values.Long.of_json ruleRevision)
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_automation_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RuleName"
       and description =
         flag "description" (optional string) ~doc:"STRING RuleDescription"
       and ruleType =
         flag "rule-type" (optional json_arg) ~doc:"JSON RuleType"
       and organizationConfiguration =
         flag "organization-configuration" (optional json_arg)
           ~doc:"JSON OrganizationConfiguration"
       and priority = flag "priority" (optional string) ~doc:"STRING String"
       and recommendedActionTypes =
         flag "recommended-action-types" (optional json_arg)
           ~doc:"JSON RecommendedActionTypeList"
       and criteria =
         flag "criteria" (optional json_arg) ~doc:"JSON Criteria"
       and schedule =
         flag "schedule" (optional json_arg) ~doc:"JSON Schedule"
       and status = flag "status" (optional json_arg) ~doc:"JSON RuleStatus"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and ruleArn = flag "rule-arn" (required string) ~doc:"STRING RuleArn"
       and ruleRevision =
         flag "rule-revision" (required json_arg) ~doc:"JSON Long" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_automation_rule
           (Values.UpdateAutomationRuleRequest.make ?name ?description
              ?ruleType:(Option.map ~f:Values.RuleType.of_json ruleType)
              ?organizationConfiguration:(Option.map
                                            ~f:Values.OrganizationConfiguration.of_json
                                            organizationConfiguration)
              ?priority
              ?recommendedActionTypes:(Option.map
                                         ~f:Values.RecommendedActionTypeList.of_json
                                         recommendedActionTypes)
              ?criteria:(Option.map ~f:Values.Criteria.of_json criteria)
              ?schedule:(Option.map ~f:Values.Schedule.of_json schedule)
              ?status:(Option.map ~f:Values.RuleStatus.of_json status)
              ?clientToken ~ruleArn
              ~ruleRevision:(Values.Long.of_json ruleRevision) ())
           (Some Values.UpdateAutomationRuleResponse.to_json)
           (Some Values.UpdateAutomationRuleResponse.error_to_json)])
let update_enrollment_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and status =
         flag "status" (required json_arg) ~doc:"JSON EnrollmentStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_enrollment_configuration
           (Values.UpdateEnrollmentConfigurationRequest.make ?clientToken
              ~status:(Values.EnrollmentStatus.of_json status) ())
           (Some Values.UpdateEnrollmentConfigurationResponse.to_json)
           (Some Values.UpdateEnrollmentConfigurationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-accounts", associate_accounts);
    ("create-automation-rule", create_automation_rule);
    ("delete-automation-rule", delete_automation_rule);
    ("disassociate-accounts", disassociate_accounts);
    ("get-automation-event", get_automation_event);
    ("get-automation-rule", get_automation_rule);
    ("get-enrollment-configuration", get_enrollment_configuration);
    ("list-accounts", list_accounts);
    ("list-automation-event-steps", list_automation_event_steps);
    ("list-automation-event-summaries", list_automation_event_summaries);
    ("list-automation-events", list_automation_events);
    ("list-automation-rule-preview", list_automation_rule_preview);
    ("list-automation-rule-preview-summaries",
      list_automation_rule_preview_summaries);
    ("list-automation-rules", list_automation_rules);
    ("list-recommended-action-summaries", list_recommended_action_summaries);
    ("list-recommended-actions", list_recommended_actions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("rollback-automation-event", rollback_automation_event);
    ("start-automation-event", start_automation_event);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-automation-rule", update_automation_rule);
    ("update-enrollment-configuration", update_enrollment_configuration)]
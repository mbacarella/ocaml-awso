(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_scaling_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (required json_arg)
           ~doc:"JSON ScalableDimension" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scaling_policy
           (Values.DeleteScalingPolicyRequest.make ~policyName
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ~resourceId
              ~scalableDimension:(Values.ScalableDimension.of_json
                                    scalableDimension) ())
           (Some Values.DeleteScalingPolicyResponse.to_json)
           (Some Values.DeleteScalingPolicyResponse.error_to_json)])
let delete_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace"
       and scheduledActionName =
         flag "scheduled-action-name" (required string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (required json_arg)
           ~doc:"JSON ScalableDimension" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scheduled_action
           (Values.DeleteScheduledActionRequest.make
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ~scheduledActionName
              ~resourceId
              ~scalableDimension:(Values.ScalableDimension.of_json
                                    scalableDimension) ())
           (Some Values.DeleteScheduledActionResponse.to_json)
           (Some Values.DeleteScheduledActionResponse.error_to_json)])
let deregister_scalable_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (required json_arg)
           ~doc:"JSON ScalableDimension" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_scalable_target
           (Values.DeregisterScalableTargetRequest.make
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ~resourceId
              ~scalableDimension:(Values.ScalableDimension.of_json
                                    scalableDimension) ())
           (Some Values.DeregisterScalableTargetResponse.to_json)
           (Some Values.DeregisterScalableTargetResponse.error_to_json)])
let describe_scalable_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIds =
         flag "resource-ids" (optional json_arg)
           ~doc:"JSON ResourceIdsMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (optional json_arg)
           ~doc:"JSON ScalableDimension"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scalable_targets
           (Values.DescribeScalableTargetsRequest.make
              ?resourceIds:(Option.map
                              ~f:Values.ResourceIdsMaxLen1600.of_json
                              resourceIds)
              ?scalableDimension:(Option.map
                                    ~f:Values.ScalableDimension.of_json
                                    scalableDimension) ?maxResults ?nextToken
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ())
           (Some Values.DescribeScalableTargetsResponse.to_json)
           (Some Values.DescribeScalableTargetsResponse.error_to_json)])
let describe_scaling_activities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (optional string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (optional json_arg)
           ~doc:"JSON ScalableDimension"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scaling_activities
           (Values.DescribeScalingActivitiesRequest.make ?resourceId
              ?scalableDimension:(Option.map
                                    ~f:Values.ScalableDimension.of_json
                                    scalableDimension) ?maxResults ?nextToken
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ())
           (Some Values.DescribeScalingActivitiesResponse.to_json)
           (Some Values.DescribeScalingActivitiesResponse.error_to_json)])
let describe_scaling_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyNames =
         flag "policy-names" (optional json_arg)
           ~doc:"JSON ResourceIdsMaxLen1600"
       and resourceId =
         flag "resource-id" (optional string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (optional json_arg)
           ~doc:"JSON ScalableDimension"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scaling_policies
           (Values.DescribeScalingPoliciesRequest.make
              ?policyNames:(Option.map
                              ~f:Values.ResourceIdsMaxLen1600.of_json
                              policyNames) ?resourceId
              ?scalableDimension:(Option.map
                                    ~f:Values.ScalableDimension.of_json
                                    scalableDimension) ?maxResults ?nextToken
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ())
           (Some Values.DescribeScalingPoliciesResponse.to_json)
           (Some Values.DescribeScalingPoliciesResponse.error_to_json)])
let describe_scheduled_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledActionNames =
         flag "scheduled-action-names" (optional json_arg)
           ~doc:"JSON ResourceIdsMaxLen1600"
       and resourceId =
         flag "resource-id" (optional string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (optional json_arg)
           ~doc:"JSON ScalableDimension"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING XmlString"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scheduled_actions
           (Values.DescribeScheduledActionsRequest.make
              ?scheduledActionNames:(Option.map
                                       ~f:Values.ResourceIdsMaxLen1600.of_json
                                       scheduledActionNames) ?resourceId
              ?scalableDimension:(Option.map
                                    ~f:Values.ScalableDimension.of_json
                                    scalableDimension) ?maxResults ?nextToken
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ())
           (Some Values.DescribeScheduledActionsResponse.to_json)
           (Some Values.DescribeScheduledActionsResponse.error_to_json)])
let put_scaling_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyType =
         flag "policy-type" (optional json_arg) ~doc:"JSON PolicyType"
       and stepScalingPolicyConfiguration =
         flag "step-scaling-policy-configuration" (optional json_arg)
           ~doc:"JSON StepScalingPolicyConfiguration"
       and targetTrackingScalingPolicyConfiguration =
         flag "target-tracking-scaling-policy-configuration"
           (optional json_arg)
           ~doc:"JSON TargetTrackingScalingPolicyConfiguration"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (required json_arg)
           ~doc:"JSON ScalableDimension" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_scaling_policy
           (Values.PutScalingPolicyRequest.make
              ?policyType:(Option.map ~f:Values.PolicyType.of_json policyType)
              ?stepScalingPolicyConfiguration:(Option.map
                                                 ~f:Values.StepScalingPolicyConfiguration.of_json
                                                 stepScalingPolicyConfiguration)
              ?targetTrackingScalingPolicyConfiguration:(Option.map
                                                           ~f:Values.TargetTrackingScalingPolicyConfiguration.of_json
                                                           targetTrackingScalingPolicyConfiguration)
              ~policyName
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ~resourceId
              ~scalableDimension:(Values.ScalableDimension.of_json
                                    scalableDimension) ())
           (Some Values.PutScalingPolicyResponse.to_json)
           (Some Values.PutScalingPolicyResponse.error_to_json)])
let put_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING ResourceIdMaxLen1600"
       and timezone =
         flag "timezone" (optional string) ~doc:"STRING ResourceIdMaxLen1600"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TimestampType"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON TimestampType"
       and scalableTargetAction =
         flag "scalable-target-action" (optional json_arg)
           ~doc:"JSON ScalableTargetAction"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace"
       and scheduledActionName =
         flag "scheduled-action-name" (required string)
           ~doc:"STRING ScheduledActionName"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (required json_arg)
           ~doc:"JSON ScalableDimension" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_scheduled_action
           (Values.PutScheduledActionRequest.make ?schedule ?timezone
              ?startTime:(Option.map ~f:Values.TimestampType.of_json
                            startTime)
              ?endTime:(Option.map ~f:Values.TimestampType.of_json endTime)
              ?scalableTargetAction:(Option.map
                                       ~f:Values.ScalableTargetAction.of_json
                                       scalableTargetAction)
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ~scheduledActionName
              ~resourceId
              ~scalableDimension:(Values.ScalableDimension.of_json
                                    scalableDimension) ())
           (Some Values.PutScheduledActionResponse.to_json)
           (Some Values.PutScheduledActionResponse.error_to_json)])
let register_scalable_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and minCapacity =
         flag "min-capacity" (optional int) ~doc:"INT ResourceCapacity"
       and maxCapacity =
         flag "max-capacity" (optional int) ~doc:"INT ResourceCapacity"
       and roleARN =
         flag "role-a-r-n" (optional string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and suspendedState =
         flag "suspended-state" (optional json_arg)
           ~doc:"JSON SuspendedState"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING ResourceIdMaxLen1600"
       and scalableDimension =
         flag "scalable-dimension" (required json_arg)
           ~doc:"JSON ScalableDimension" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_scalable_target
           (Values.RegisterScalableTargetRequest.make ?minCapacity
              ?maxCapacity ?roleARN
              ?suspendedState:(Option.map ~f:Values.SuspendedState.of_json
                                 suspendedState)
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ~resourceId
              ~scalableDimension:(Values.ScalableDimension.of_json
                                    scalableDimension) ())
           (Some Values.RegisterScalableTargetResponse.to_json)
           (Some Values.RegisterScalableTargetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("delete-scaling-policy", delete_scaling_policy);
    ("delete-scheduled-action", delete_scheduled_action);
    ("deregister-scalable-target", deregister_scalable_target);
    ("describe-scalable-targets", describe_scalable_targets);
    ("describe-scaling-activities", describe_scaling_activities);
    ("describe-scaling-policies", describe_scaling_policies);
    ("describe-scheduled-actions", describe_scheduled_actions);
    ("put-scaling-policy", put_scaling_policy);
    ("put-scheduled-action", put_scheduled_action);
    ("register-scalable-target", register_scalable_target)]
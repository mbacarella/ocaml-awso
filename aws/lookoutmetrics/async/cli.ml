(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let activate_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_anomaly_detector
           (Values.ActivateAnomalyDetectorRequest.make ~anomalyDetectorArn ())
           (Some Values.ActivateAnomalyDetectorResponse.to_json)
           (Some Values.ActivateAnomalyDetectorResponse.error_to_json)])
let back_test_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.back_test_anomaly_detector
           (Values.BackTestAnomalyDetectorRequest.make ~anomalyDetectorArn ())
           (Some Values.BackTestAnomalyDetectorResponse.to_json)
           (Some Values.BackTestAnomalyDetectorResponse.error_to_json)])
let create_alert =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alertDescription =
         flag "alert-description" (optional string)
           ~doc:"STRING AlertDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and alertName =
         flag "alert-name" (required string) ~doc:"STRING AlertName"
       and alertSensitivityThreshold =
         flag "alert-sensitivity-threshold" (required int)
           ~doc:"INT SensitivityThreshold"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn"
       and action = flag "action" (required json_arg) ~doc:"JSON Action" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_alert
           (Values.CreateAlertRequest.make ?alertDescription
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~alertName
              ~alertSensitivityThreshold ~anomalyDetectorArn
              ~action:(Values.Action.of_json action) ())
           (Some Values.CreateAlertResponse.to_json)
           (Some Values.CreateAlertResponse.error_to_json)])
let create_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorDescription =
         flag "anomaly-detector-description" (optional string)
           ~doc:"STRING AnomalyDetectorDescription"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and anomalyDetectorName =
         flag "anomaly-detector-name" (required string)
           ~doc:"STRING AnomalyDetectorName"
       and anomalyDetectorConfig =
         flag "anomaly-detector-config" (required json_arg)
           ~doc:"JSON AnomalyDetectorConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_anomaly_detector
           (Values.CreateAnomalyDetectorRequest.make
              ?anomalyDetectorDescription ?kmsKeyArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~anomalyDetectorName
              ~anomalyDetectorConfig:(Values.AnomalyDetectorConfig.of_json
                                        anomalyDetectorConfig) ())
           (Some Values.CreateAnomalyDetectorResponse.to_json)
           (Some Values.CreateAnomalyDetectorResponse.error_to_json)])
let create_metric_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metricSetDescription =
         flag "metric-set-description" (optional string)
           ~doc:"STRING MetricSetDescription"
       and offset = flag "offset" (optional int) ~doc:"INT Offset"
       and timestampColumn =
         flag "timestamp-column" (optional json_arg)
           ~doc:"JSON TimestampColumn"
       and dimensionList =
         flag "dimension-list" (optional json_arg) ~doc:"JSON DimensionList"
       and metricSetFrequency =
         flag "metric-set-frequency" (optional json_arg)
           ~doc:"JSON Frequency"
       and timezone =
         flag "timezone" (optional string) ~doc:"STRING Timezone"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn"
       and metricSetName =
         flag "metric-set-name" (required string) ~doc:"STRING MetricSetName"
       and metricList =
         flag "metric-list" (required json_arg) ~doc:"JSON MetricList"
       and metricSource =
         flag "metric-source" (required json_arg) ~doc:"JSON MetricSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_metric_set
           (Values.CreateMetricSetRequest.make ?metricSetDescription ?offset
              ?timestampColumn:(Option.map ~f:Values.TimestampColumn.of_json
                                  timestampColumn)
              ?dimensionList:(Option.map ~f:Values.DimensionList.of_json
                                dimensionList)
              ?metricSetFrequency:(Option.map ~f:Values.Frequency.of_json
                                     metricSetFrequency) ?timezone
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~anomalyDetectorArn ~metricSetName
              ~metricList:(Values.MetricList.of_json metricList)
              ~metricSource:(Values.MetricSource.of_json metricSource) ())
           (Some Values.CreateMetricSetResponse.to_json)
           (Some Values.CreateMetricSetResponse.error_to_json)])
let deactivate_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_anomaly_detector
           (Values.DeactivateAnomalyDetectorRequest.make ~anomalyDetectorArn
              ()) (Some Values.DeactivateAnomalyDetectorResponse.to_json)
           (Some Values.DeactivateAnomalyDetectorResponse.error_to_json)])
let delete_alert =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alertArn = flag "alert-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alert (Values.DeleteAlertRequest.make ~alertArn ())
           (Some Values.DeleteAlertResponse.to_json)
           (Some Values.DeleteAlertResponse.error_to_json)])
let delete_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_anomaly_detector
           (Values.DeleteAnomalyDetectorRequest.make ~anomalyDetectorArn ())
           (Some Values.DeleteAnomalyDetectorResponse.to_json)
           (Some Values.DeleteAnomalyDetectorResponse.error_to_json)])
let describe_alert =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alertArn = flag "alert-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_alert (Values.DescribeAlertRequest.make ~alertArn ())
           (Some Values.DescribeAlertResponse.to_json)
           (Some Values.DescribeAlertResponse.error_to_json)])
let describe_anomaly_detection_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timestamp =
         flag "timestamp" (optional string) ~doc:"STRING TimestampString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_anomaly_detection_executions
           (Values.DescribeAnomalyDetectionExecutionsRequest.make ?timestamp
              ?maxResults ?nextToken ~anomalyDetectorArn ())
           (Some Values.DescribeAnomalyDetectionExecutionsResponse.to_json)
           (Some
              Values.DescribeAnomalyDetectionExecutionsResponse.error_to_json)])
let describe_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_anomaly_detector
           (Values.DescribeAnomalyDetectorRequest.make ~anomalyDetectorArn ())
           (Some Values.DescribeAnomalyDetectorResponse.to_json)
           (Some Values.DescribeAnomalyDetectorResponse.error_to_json)])
let describe_metric_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metricSetArn =
         flag "metric-set-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_metric_set
           (Values.DescribeMetricSetRequest.make ~metricSetArn ())
           (Some Values.DescribeMetricSetResponse.to_json)
           (Some Values.DescribeMetricSetResponse.error_to_json)])
let get_anomaly_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyGroupId =
         flag "anomaly-group-id" (required string) ~doc:"STRING UUID"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_anomaly_group
           (Values.GetAnomalyGroupRequest.make ~anomalyGroupId
              ~anomalyDetectorArn ())
           (Some Values.GetAnomalyGroupResponse.to_json)
           (Some Values.GetAnomalyGroupResponse.error_to_json)])
let get_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn"
       and anomalyGroupTimeSeriesFeedback =
         flag "anomaly-group-time-series-feedback" (required json_arg)
           ~doc:"JSON AnomalyGroupTimeSeries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_feedback
           (Values.GetFeedbackRequest.make ?maxResults ?nextToken
              ~anomalyDetectorArn
              ~anomalyGroupTimeSeriesFeedback:(Values.AnomalyGroupTimeSeries.of_json
                                                 anomalyGroupTimeSeriesFeedback)
              ()) (Some Values.GetFeedbackResponse.to_json)
           (Some Values.GetFeedbackResponse.error_to_json)])
let get_sample_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3SourceConfig =
         flag "s3-source-config" (optional json_arg)
           ~doc:"JSON SampleDataS3SourceConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sample_data
           (Values.GetSampleDataRequest.make
              ?s3SourceConfig:(Option.map
                                 ~f:Values.SampleDataS3SourceConfig.of_json
                                 s3SourceConfig) ())
           (Some Values.GetSampleDataResponse.to_json)
           (Some Values.GetSampleDataResponse.error_to_json)])
let list_alerts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_alerts
           (Values.ListAlertsRequest.make ?anomalyDetectorArn ?nextToken
              ?maxResults ()) (Some Values.ListAlertsResponse.to_json)
           (Some Values.ListAlertsResponse.error_to_json)])
let list_anomaly_detectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_anomaly_detectors
           (Values.ListAnomalyDetectorsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAnomalyDetectorsResponse.to_json)
           (Some Values.ListAnomalyDetectorsResponse.error_to_json)])
let list_anomaly_group_related_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relationshipTypeFilter =
         flag "relationship-type-filter" (optional json_arg)
           ~doc:"JSON RelationshipType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn"
       and anomalyGroupId =
         flag "anomaly-group-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_anomaly_group_related_metrics
           (Values.ListAnomalyGroupRelatedMetricsRequest.make
              ?relationshipTypeFilter:(Option.map
                                         ~f:Values.RelationshipType.of_json
                                         relationshipTypeFilter) ?maxResults
              ?nextToken ~anomalyDetectorArn ~anomalyGroupId ())
           (Some Values.ListAnomalyGroupRelatedMetricsResponse.to_json)
           (Some Values.ListAnomalyGroupRelatedMetricsResponse.error_to_json)])
let list_anomaly_group_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn"
       and sensitivityThreshold =
         flag "sensitivity-threshold" (required int)
           ~doc:"INT SensitivityThreshold" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_anomaly_group_summaries
           (Values.ListAnomalyGroupSummariesRequest.make ?maxResults
              ?nextToken ~anomalyDetectorArn ~sensitivityThreshold ())
           (Some Values.ListAnomalyGroupSummariesResponse.to_json)
           (Some Values.ListAnomalyGroupSummariesResponse.error_to_json)])
let list_anomaly_group_time_series =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn"
       and anomalyGroupId =
         flag "anomaly-group-id" (required string) ~doc:"STRING UUID"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_anomaly_group_time_series
           (Values.ListAnomalyGroupTimeSeriesRequest.make ?maxResults
              ?nextToken ~anomalyDetectorArn ~anomalyGroupId ~metricName ())
           (Some Values.ListAnomalyGroupTimeSeriesResponse.to_json)
           (Some Values.ListAnomalyGroupTimeSeriesResponse.error_to_json)])
let list_metric_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (optional string) ~doc:"STRING Arn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_metric_sets
           (Values.ListMetricSetsRequest.make ?anomalyDetectorArn ?maxResults
              ?nextToken ()) (Some Values.ListMetricSetsResponse.to_json)
           (Some Values.ListMetricSetsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn"
       and anomalyGroupTimeSeriesFeedback =
         flag "anomaly-group-time-series-feedback" (required json_arg)
           ~doc:"JSON AnomalyGroupTimeSeriesFeedback" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_feedback
           (Values.PutFeedbackRequest.make ~anomalyDetectorArn
              ~anomalyGroupTimeSeriesFeedback:(Values.AnomalyGroupTimeSeriesFeedback.of_json
                                                 anomalyGroupTimeSeriesFeedback)
              ()) (Some Values.PutFeedbackResponse.to_json)
           (Some Values.PutFeedbackResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and anomalyDetectorDescription =
         flag "anomaly-detector-description" (optional string)
           ~doc:"STRING AnomalyDetectorDescription"
       and anomalyDetectorConfig =
         flag "anomaly-detector-config" (optional json_arg)
           ~doc:"JSON AnomalyDetectorConfig"
       and anomalyDetectorArn =
         flag "anomaly-detector-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_anomaly_detector
           (Values.UpdateAnomalyDetectorRequest.make ?kmsKeyArn
              ?anomalyDetectorDescription
              ?anomalyDetectorConfig:(Option.map
                                        ~f:Values.AnomalyDetectorConfig.of_json
                                        anomalyDetectorConfig)
              ~anomalyDetectorArn ())
           (Some Values.UpdateAnomalyDetectorResponse.to_json)
           (Some Values.UpdateAnomalyDetectorResponse.error_to_json)])
let update_metric_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metricSetDescription =
         flag "metric-set-description" (optional string)
           ~doc:"STRING MetricSetDescription"
       and metricList =
         flag "metric-list" (optional json_arg) ~doc:"JSON MetricList"
       and offset = flag "offset" (optional int) ~doc:"INT Offset"
       and timestampColumn =
         flag "timestamp-column" (optional json_arg)
           ~doc:"JSON TimestampColumn"
       and dimensionList =
         flag "dimension-list" (optional json_arg) ~doc:"JSON DimensionList"
       and metricSetFrequency =
         flag "metric-set-frequency" (optional json_arg)
           ~doc:"JSON Frequency"
       and metricSource =
         flag "metric-source" (optional json_arg) ~doc:"JSON MetricSource"
       and metricSetArn =
         flag "metric-set-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_metric_set
           (Values.UpdateMetricSetRequest.make ?metricSetDescription
              ?metricList:(Option.map ~f:Values.MetricList.of_json metricList)
              ?offset
              ?timestampColumn:(Option.map ~f:Values.TimestampColumn.of_json
                                  timestampColumn)
              ?dimensionList:(Option.map ~f:Values.DimensionList.of_json
                                dimensionList)
              ?metricSetFrequency:(Option.map ~f:Values.Frequency.of_json
                                     metricSetFrequency)
              ?metricSource:(Option.map ~f:Values.MetricSource.of_json
                               metricSource) ~metricSetArn ())
           (Some Values.UpdateMetricSetResponse.to_json)
           (Some Values.UpdateMetricSetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("activate-anomaly-detector", activate_anomaly_detector);
    ("back-test-anomaly-detector", back_test_anomaly_detector);
    ("create-alert", create_alert);
    ("create-anomaly-detector", create_anomaly_detector);
    ("create-metric-set", create_metric_set);
    ("deactivate-anomaly-detector", deactivate_anomaly_detector);
    ("delete-alert", delete_alert);
    ("delete-anomaly-detector", delete_anomaly_detector);
    ("describe-alert", describe_alert);
    ("describe-anomaly-detection-executions",
      describe_anomaly_detection_executions);
    ("describe-anomaly-detector", describe_anomaly_detector);
    ("describe-metric-set", describe_metric_set);
    ("get-anomaly-group", get_anomaly_group);
    ("get-feedback", get_feedback);
    ("get-sample-data", get_sample_data);
    ("list-alerts", list_alerts);
    ("list-anomaly-detectors", list_anomaly_detectors);
    ("list-anomaly-group-related-metrics",
      list_anomaly_group_related_metrics);
    ("list-anomaly-group-summaries", list_anomaly_group_summaries);
    ("list-anomaly-group-time-series", list_anomaly_group_time_series);
    ("list-metric-sets", list_metric_sets);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-feedback", put_feedback);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-anomaly-detector", update_anomaly_detector);
    ("update-metric-set", update_metric_set)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_tracker_consumer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consumerArn =
         flag "consumer-arn" (required string) ~doc:"STRING Arn"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_tracker_consumer
           (Values.AssociateTrackerConsumerRequest.make ~consumerArn
              ~trackerName ())
           (Some Values.AssociateTrackerConsumerResponse.to_json)
           (Some Values.AssociateTrackerConsumerResponse.error_to_json)])
let batch_delete_device_position_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceIds =
         flag "device-ids" (required json_arg)
           ~doc:"JSON BatchDeleteDevicePositionHistoryRequestDeviceIdsList"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_device_position_history
           (Values.BatchDeleteDevicePositionHistoryRequest.make
              ~deviceIds:(Values.BatchDeleteDevicePositionHistoryRequestDeviceIdsList.of_json
                            deviceIds) ~trackerName ())
           (Some Values.BatchDeleteDevicePositionHistoryResponse.to_json)
           (Some
              Values.BatchDeleteDevicePositionHistoryResponse.error_to_json)])
let batch_delete_geofence =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName"
       and geofenceIds =
         flag "geofence-ids" (required json_arg)
           ~doc:"JSON BatchDeleteGeofenceRequestGeofenceIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_geofence
           (Values.BatchDeleteGeofenceRequest.make ~collectionName
              ~geofenceIds:(Values.BatchDeleteGeofenceRequestGeofenceIdsList.of_json
                              geofenceIds) ())
           (Some Values.BatchDeleteGeofenceResponse.to_json)
           (Some Values.BatchDeleteGeofenceResponse.error_to_json)])
let batch_evaluate_geofences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName"
       and devicePositionUpdates =
         flag "device-position-updates" (required json_arg)
           ~doc:"JSON BatchEvaluateGeofencesRequestDevicePositionUpdatesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_evaluate_geofences
           (Values.BatchEvaluateGeofencesRequest.make ~collectionName
              ~devicePositionUpdates:(Values.BatchEvaluateGeofencesRequestDevicePositionUpdatesList.of_json
                                        devicePositionUpdates) ())
           (Some Values.BatchEvaluateGeofencesResponse.to_json)
           (Some Values.BatchEvaluateGeofencesResponse.error_to_json)])
let batch_get_device_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceIds =
         flag "device-ids" (required json_arg)
           ~doc:"JSON BatchGetDevicePositionRequestDeviceIdsList"
       and trackerName =
         flag "tracker-name" (required string)
           ~doc:"STRING BatchGetDevicePositionRequestTrackerNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_device_position
           (Values.BatchGetDevicePositionRequest.make
              ~deviceIds:(Values.BatchGetDevicePositionRequestDeviceIdsList.of_json
                            deviceIds) ~trackerName ())
           (Some Values.BatchGetDevicePositionResponse.to_json)
           (Some Values.BatchGetDevicePositionResponse.error_to_json)])
let batch_put_geofence =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON BatchPutGeofenceRequestEntriesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_geofence
           (Values.BatchPutGeofenceRequest.make ~collectionName
              ~entries:(Values.BatchPutGeofenceRequestEntriesList.of_json
                          entries) ())
           (Some Values.BatchPutGeofenceResponse.to_json)
           (Some Values.BatchPutGeofenceResponse.error_to_json)])
let batch_update_device_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName"
       and updates =
         flag "updates" (required json_arg)
           ~doc:"JSON BatchUpdateDevicePositionRequestUpdatesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_device_position
           (Values.BatchUpdateDevicePositionRequest.make ~trackerName
              ~updates:(Values.BatchUpdateDevicePositionRequestUpdatesList.of_json
                          updates) ())
           (Some Values.BatchUpdateDevicePositionResponse.to_json)
           (Some Values.BatchUpdateDevicePositionResponse.error_to_json)])
let calculate_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and carModeOptions =
         flag "car-mode-options" (optional json_arg)
           ~doc:"JSON CalculateRouteCarModeOptions"
       and departNow = flag "depart-now" (optional bool) ~doc:"BOOL Boolean"
       and departureTime =
         flag "departure-time" (optional json_arg) ~doc:"JSON Timestamp"
       and distanceUnit =
         flag "distance-unit" (optional json_arg) ~doc:"JSON DistanceUnit"
       and includeLegGeometry =
         flag "include-leg-geometry" (optional bool) ~doc:"BOOL Boolean"
       and travelMode =
         flag "travel-mode" (optional json_arg) ~doc:"JSON TravelMode"
       and truckModeOptions =
         flag "truck-mode-options" (optional json_arg)
           ~doc:"JSON CalculateRouteTruckModeOptions"
       and waypointPositions =
         flag "waypoint-positions" (optional json_arg)
           ~doc:"JSON CalculateRouteRequestWaypointPositionsList"
       and calculatorName =
         flag "calculator-name" (required string) ~doc:"STRING ResourceName"
       and departurePosition =
         flag "departure-position" (required json_arg) ~doc:"JSON Position"
       and destinationPosition =
         flag "destination-position" (required json_arg) ~doc:"JSON Position" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.calculate_route
           (Values.CalculateRouteRequest.make
              ?carModeOptions:(Option.map
                                 ~f:Values.CalculateRouteCarModeOptions.of_json
                                 carModeOptions) ?departNow
              ?departureTime:(Option.map ~f:Values.Timestamp.of_json
                                departureTime)
              ?distanceUnit:(Option.map ~f:Values.DistanceUnit.of_json
                               distanceUnit) ?includeLegGeometry
              ?travelMode:(Option.map ~f:Values.TravelMode.of_json travelMode)
              ?truckModeOptions:(Option.map
                                   ~f:Values.CalculateRouteTruckModeOptions.of_json
                                   truckModeOptions)
              ?waypointPositions:(Option.map
                                    ~f:Values.CalculateRouteRequestWaypointPositionsList.of_json
                                    waypointPositions) ~calculatorName
              ~departurePosition:(Values.Position.of_json departurePosition)
              ~destinationPosition:(Values.Position.of_json
                                      destinationPosition) ())
           (Some Values.CalculateRouteResponse.to_json)
           (Some Values.CalculateRouteResponse.error_to_json)])
let calculate_route_matrix =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and carModeOptions =
         flag "car-mode-options" (optional json_arg)
           ~doc:"JSON CalculateRouteCarModeOptions"
       and departNow = flag "depart-now" (optional bool) ~doc:"BOOL Boolean"
       and departureTime =
         flag "departure-time" (optional json_arg) ~doc:"JSON Timestamp"
       and distanceUnit =
         flag "distance-unit" (optional json_arg) ~doc:"JSON DistanceUnit"
       and travelMode =
         flag "travel-mode" (optional json_arg) ~doc:"JSON TravelMode"
       and truckModeOptions =
         flag "truck-mode-options" (optional json_arg)
           ~doc:"JSON CalculateRouteTruckModeOptions"
       and calculatorName =
         flag "calculator-name" (required string) ~doc:"STRING ResourceName"
       and departurePositions =
         flag "departure-positions" (required json_arg)
           ~doc:"JSON CalculateRouteMatrixRequestDeparturePositionsList"
       and destinationPositions =
         flag "destination-positions" (required json_arg)
           ~doc:"JSON CalculateRouteMatrixRequestDestinationPositionsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.calculate_route_matrix
           (Values.CalculateRouteMatrixRequest.make
              ?carModeOptions:(Option.map
                                 ~f:Values.CalculateRouteCarModeOptions.of_json
                                 carModeOptions) ?departNow
              ?departureTime:(Option.map ~f:Values.Timestamp.of_json
                                departureTime)
              ?distanceUnit:(Option.map ~f:Values.DistanceUnit.of_json
                               distanceUnit)
              ?travelMode:(Option.map ~f:Values.TravelMode.of_json travelMode)
              ?truckModeOptions:(Option.map
                                   ~f:Values.CalculateRouteTruckModeOptions.of_json
                                   truckModeOptions) ~calculatorName
              ~departurePositions:(Values.CalculateRouteMatrixRequestDeparturePositionsList.of_json
                                     departurePositions)
              ~destinationPositions:(Values.CalculateRouteMatrixRequestDestinationPositionsList.of_json
                                       destinationPositions) ())
           (Some Values.CalculateRouteMatrixResponse.to_json)
           (Some Values.CalculateRouteMatrixResponse.error_to_json)])
let create_geofence_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and pricingPlanDataSource =
         flag "pricing-plan-data-source" (optional string)
           ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_geofence_collection
           (Values.CreateGeofenceCollectionRequest.make ?description
              ?kmsKeyId
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan) ?pricingPlanDataSource
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~collectionName ())
           (Some Values.CreateGeofenceCollectionResponse.to_json)
           (Some Values.CreateGeofenceCollectionResponse.error_to_json)])
let create_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON MapConfiguration"
       and mapName =
         flag "map-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_map
           (Values.CreateMapRequest.make ?description
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~configuration:(Values.MapConfiguration.of_json configuration)
              ~mapName ()) (Some Values.CreateMapResponse.to_json)
           (Some Values.CreateMapResponse.error_to_json)])
let create_place_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceConfiguration =
         flag "data-source-configuration" (optional json_arg)
           ~doc:"JSON DataSourceConfiguration"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and dataSource =
         flag "data-source" (required string) ~doc:"STRING String"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_place_index
           (Values.CreatePlaceIndexRequest.make
              ?dataSourceConfiguration:(Option.map
                                          ~f:Values.DataSourceConfiguration.of_json
                                          dataSourceConfiguration)
              ?description
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~dataSource
              ~indexName ()) (Some Values.CreatePlaceIndexResponse.to_json)
           (Some Values.CreatePlaceIndexResponse.error_to_json)])
let create_route_calculator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and calculatorName =
         flag "calculator-name" (required string) ~doc:"STRING ResourceName"
       and dataSource =
         flag "data-source" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_route_calculator
           (Values.CreateRouteCalculatorRequest.make ?description
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~calculatorName ~dataSource ())
           (Some Values.CreateRouteCalculatorResponse.to_json)
           (Some Values.CreateRouteCalculatorResponse.error_to_json)])
let create_tracker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and positionFiltering =
         flag "position-filtering" (optional json_arg)
           ~doc:"JSON PositionFiltering"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and pricingPlanDataSource =
         flag "pricing-plan-data-source" (optional string)
           ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tracker
           (Values.CreateTrackerRequest.make ?description ?kmsKeyId
              ?positionFiltering:(Option.map
                                    ~f:Values.PositionFiltering.of_json
                                    positionFiltering)
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan) ?pricingPlanDataSource
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~trackerName
              ()) (Some Values.CreateTrackerResponse.to_json)
           (Some Values.CreateTrackerResponse.error_to_json)])
let delete_geofence_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_geofence_collection
           (Values.DeleteGeofenceCollectionRequest.make ~collectionName ())
           (Some Values.DeleteGeofenceCollectionResponse.to_json)
           (Some Values.DeleteGeofenceCollectionResponse.error_to_json)])
let delete_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mapName =
         flag "map-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_map (Values.DeleteMapRequest.make ~mapName ())
           (Some Values.DeleteMapResponse.to_json)
           (Some Values.DeleteMapResponse.error_to_json)])
let delete_place_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_place_index
           (Values.DeletePlaceIndexRequest.make ~indexName ())
           (Some Values.DeletePlaceIndexResponse.to_json)
           (Some Values.DeletePlaceIndexResponse.error_to_json)])
let delete_route_calculator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and calculatorName =
         flag "calculator-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_calculator
           (Values.DeleteRouteCalculatorRequest.make ~calculatorName ())
           (Some Values.DeleteRouteCalculatorResponse.to_json)
           (Some Values.DeleteRouteCalculatorResponse.error_to_json)])
let delete_tracker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tracker
           (Values.DeleteTrackerRequest.make ~trackerName ())
           (Some Values.DeleteTrackerResponse.to_json)
           (Some Values.DeleteTrackerResponse.error_to_json)])
let describe_geofence_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_geofence_collection
           (Values.DescribeGeofenceCollectionRequest.make ~collectionName ())
           (Some Values.DescribeGeofenceCollectionResponse.to_json)
           (Some Values.DescribeGeofenceCollectionResponse.error_to_json)])
let describe_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mapName =
         flag "map-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_map (Values.DescribeMapRequest.make ~mapName ())
           (Some Values.DescribeMapResponse.to_json)
           (Some Values.DescribeMapResponse.error_to_json)])
let describe_place_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_place_index
           (Values.DescribePlaceIndexRequest.make ~indexName ())
           (Some Values.DescribePlaceIndexResponse.to_json)
           (Some Values.DescribePlaceIndexResponse.error_to_json)])
let describe_route_calculator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and calculatorName =
         flag "calculator-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_route_calculator
           (Values.DescribeRouteCalculatorRequest.make ~calculatorName ())
           (Some Values.DescribeRouteCalculatorResponse.to_json)
           (Some Values.DescribeRouteCalculatorResponse.error_to_json)])
let describe_tracker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tracker
           (Values.DescribeTrackerRequest.make ~trackerName ())
           (Some Values.DescribeTrackerResponse.to_json)
           (Some Values.DescribeTrackerResponse.error_to_json)])
let disassociate_tracker_consumer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consumerArn =
         flag "consumer-arn" (required string) ~doc:"STRING Arn"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_tracker_consumer
           (Values.DisassociateTrackerConsumerRequest.make ~consumerArn
              ~trackerName ())
           (Some Values.DisassociateTrackerConsumerResponse.to_json)
           (Some Values.DisassociateTrackerConsumerResponse.error_to_json)])
let get_device_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceId = flag "device-id" (required string) ~doc:"STRING Id"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_position
           (Values.GetDevicePositionRequest.make ~deviceId ~trackerName ())
           (Some Values.GetDevicePositionResponse.to_json)
           (Some Values.GetDevicePositionResponse.error_to_json)])
let get_device_position_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endTimeExclusive =
         flag "end-time-exclusive" (optional json_arg) ~doc:"JSON Timestamp"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetDevicePositionHistoryRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and startTimeInclusive =
         flag "start-time-inclusive" (optional json_arg)
           ~doc:"JSON Timestamp"
       and deviceId = flag "device-id" (required string) ~doc:"STRING Id"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_position_history
           (Values.GetDevicePositionHistoryRequest.make
              ?endTimeExclusive:(Option.map ~f:Values.Timestamp.of_json
                                   endTimeExclusive) ?maxResults ?nextToken
              ?startTimeInclusive:(Option.map ~f:Values.Timestamp.of_json
                                     startTimeInclusive) ~deviceId
              ~trackerName ())
           (Some Values.GetDevicePositionHistoryResponse.to_json)
           (Some Values.GetDevicePositionHistoryResponse.error_to_json)])
let get_geofence =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName"
       and geofenceId = flag "geofence-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_geofence
           (Values.GetGeofenceRequest.make ~collectionName ~geofenceId ())
           (Some Values.GetGeofenceResponse.to_json)
           (Some Values.GetGeofenceResponse.error_to_json)])
let get_map_glyphs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fontStack =
         flag "font-stack" (required string) ~doc:"STRING String"
       and fontUnicodeRange =
         flag "font-unicode-range" (required string)
           ~doc:"STRING GetMapGlyphsRequestFontUnicodeRangeString"
       and mapName =
         flag "map-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_map_glyphs
           (Values.GetMapGlyphsRequest.make ~fontStack ~fontUnicodeRange
              ~mapName ()) (Some Values.GetMapGlyphsResponse.to_json)
           (Some Values.GetMapGlyphsResponse.error_to_json)])
let get_map_sprites =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileName =
         flag "file-name" (required string)
           ~doc:"STRING GetMapSpritesRequestFileNameString"
       and mapName =
         flag "map-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_map_sprites
           (Values.GetMapSpritesRequest.make ~fileName ~mapName ())
           (Some Values.GetMapSpritesResponse.to_json)
           (Some Values.GetMapSpritesResponse.error_to_json)])
let get_map_style_descriptor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mapName =
         flag "map-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_map_style_descriptor
           (Values.GetMapStyleDescriptorRequest.make ~mapName ())
           (Some Values.GetMapStyleDescriptorResponse.to_json)
           (Some Values.GetMapStyleDescriptorResponse.error_to_json)])
let get_map_tile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mapName =
         flag "map-name" (required string) ~doc:"STRING ResourceName"
       and x =
         flag "x" (required string) ~doc:"STRING GetMapTileRequestXString"
       and y =
         flag "y" (required string) ~doc:"STRING GetMapTileRequestYString"
       and z =
         flag "z" (required string) ~doc:"STRING GetMapTileRequestZString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_map_tile
           (Values.GetMapTileRequest.make ~mapName ~x ~y ~z ())
           (Some Values.GetMapTileResponse.to_json)
           (Some Values.GetMapTileResponse.error_to_json)])
let list_device_positions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListDevicePositionsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_positions
           (Values.ListDevicePositionsRequest.make ?maxResults ?nextToken
              ~trackerName ())
           (Some Values.ListDevicePositionsResponse.to_json)
           (Some Values.ListDevicePositionsResponse.error_to_json)])
let list_geofence_collections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListGeofenceCollectionsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_geofence_collections
           (Values.ListGeofenceCollectionsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListGeofenceCollectionsResponse.to_json)
           (Some Values.ListGeofenceCollectionsResponse.error_to_json)])
let list_geofences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_geofences
           (Values.ListGeofencesRequest.make ?nextToken ~collectionName ())
           (Some Values.ListGeofencesResponse.to_json)
           (Some Values.ListGeofencesResponse.error_to_json)])
let list_maps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMapsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_maps
           (Values.ListMapsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListMapsResponse.to_json)
           (Some Values.ListMapsResponse.error_to_json)])
let list_place_indexes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListPlaceIndexesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_place_indexes
           (Values.ListPlaceIndexesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPlaceIndexesResponse.to_json)
           (Some Values.ListPlaceIndexesResponse.error_to_json)])
let list_route_calculators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRouteCalculatorsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_route_calculators
           (Values.ListRouteCalculatorsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListRouteCalculatorsResponse.to_json)
           (Some Values.ListRouteCalculatorsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_tracker_consumers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTrackerConsumersRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tracker_consumers
           (Values.ListTrackerConsumersRequest.make ?maxResults ?nextToken
              ~trackerName ())
           (Some Values.ListTrackerConsumersResponse.to_json)
           (Some Values.ListTrackerConsumersResponse.error_to_json)])
let list_trackers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTrackersRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_trackers
           (Values.ListTrackersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListTrackersResponse.to_json)
           (Some Values.ListTrackersResponse.error_to_json)])
let put_geofence =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName"
       and geofenceId = flag "geofence-id" (required string) ~doc:"STRING Id"
       and geometry =
         flag "geometry" (required json_arg) ~doc:"JSON GeofenceGeometry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_geofence
           (Values.PutGeofenceRequest.make ~collectionName ~geofenceId
              ~geometry:(Values.GeofenceGeometry.of_json geometry) ())
           (Some Values.PutGeofenceResponse.to_json)
           (Some Values.PutGeofenceResponse.error_to_json)])
let search_place_index_for_position =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and language =
         flag "language" (optional string) ~doc:"STRING LanguageTag"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT PlaceIndexSearchResultLimit"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING ResourceName"
       and position =
         flag "position" (required json_arg) ~doc:"JSON Position" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_place_index_for_position
           (Values.SearchPlaceIndexForPositionRequest.make ?language
              ?maxResults ~indexName
              ~position:(Values.Position.of_json position) ())
           (Some Values.SearchPlaceIndexForPositionResponse.to_json)
           (Some Values.SearchPlaceIndexForPositionResponse.error_to_json)])
let search_place_index_for_suggestions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and biasPosition =
         flag "bias-position" (optional json_arg) ~doc:"JSON Position"
       and filterBBox =
         flag "filter-b-box" (optional json_arg) ~doc:"JSON BoundingBox"
       and filterCountries =
         flag "filter-countries" (optional json_arg)
           ~doc:"JSON CountryCodeList"
       and language =
         flag "language" (optional string) ~doc:"STRING LanguageTag"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT SearchPlaceIndexForSuggestionsRequestMaxResultsInteger"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING ResourceName"
       and text =
         flag "text" (required string)
           ~doc:"STRING SyntheticSearchPlaceIndexForSuggestionsRequestString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_place_index_for_suggestions
           (Values.SearchPlaceIndexForSuggestionsRequest.make
              ?biasPosition:(Option.map ~f:Values.Position.of_json
                               biasPosition)
              ?filterBBox:(Option.map ~f:Values.BoundingBox.of_json
                             filterBBox)
              ?filterCountries:(Option.map ~f:Values.CountryCodeList.of_json
                                  filterCountries) ?language ?maxResults
              ~indexName ~text ())
           (Some Values.SearchPlaceIndexForSuggestionsResponse.to_json)
           (Some Values.SearchPlaceIndexForSuggestionsResponse.error_to_json)])
let search_place_index_for_text =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and biasPosition =
         flag "bias-position" (optional json_arg) ~doc:"JSON Position"
       and filterBBox =
         flag "filter-b-box" (optional json_arg) ~doc:"JSON BoundingBox"
       and filterCountries =
         flag "filter-countries" (optional json_arg)
           ~doc:"JSON CountryCodeList"
       and language =
         flag "language" (optional string) ~doc:"STRING LanguageTag"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT PlaceIndexSearchResultLimit"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING ResourceName"
       and text =
         flag "text" (required string)
           ~doc:"STRING SyntheticSearchPlaceIndexForTextRequestString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_place_index_for_text
           (Values.SearchPlaceIndexForTextRequest.make
              ?biasPosition:(Option.map ~f:Values.Position.of_json
                               biasPosition)
              ?filterBBox:(Option.map ~f:Values.BoundingBox.of_json
                             filterBBox)
              ?filterCountries:(Option.map ~f:Values.CountryCodeList.of_json
                                  filterCountries) ?language ?maxResults
              ~indexName ~text ())
           (Some Values.SearchPlaceIndexForTextResponse.to_json)
           (Some Values.SearchPlaceIndexForTextResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_geofence_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and pricingPlanDataSource =
         flag "pricing-plan-data-source" (optional string)
           ~doc:"STRING String"
       and collectionName =
         flag "collection-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_geofence_collection
           (Values.UpdateGeofenceCollectionRequest.make ?description
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan) ?pricingPlanDataSource
              ~collectionName ())
           (Some Values.UpdateGeofenceCollectionResponse.to_json)
           (Some Values.UpdateGeofenceCollectionResponse.error_to_json)])
let update_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and mapName =
         flag "map-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_map
           (Values.UpdateMapRequest.make ?description
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan) ~mapName ())
           (Some Values.UpdateMapResponse.to_json)
           (Some Values.UpdateMapResponse.error_to_json)])
let update_place_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceConfiguration =
         flag "data-source-configuration" (optional json_arg)
           ~doc:"JSON DataSourceConfiguration"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_place_index
           (Values.UpdatePlaceIndexRequest.make
              ?dataSourceConfiguration:(Option.map
                                          ~f:Values.DataSourceConfiguration.of_json
                                          dataSourceConfiguration)
              ?description
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan) ~indexName ())
           (Some Values.UpdatePlaceIndexResponse.to_json)
           (Some Values.UpdatePlaceIndexResponse.error_to_json)])
let update_route_calculator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and calculatorName =
         flag "calculator-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_route_calculator
           (Values.UpdateRouteCalculatorRequest.make ?description
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan) ~calculatorName ())
           (Some Values.UpdateRouteCalculatorResponse.to_json)
           (Some Values.UpdateRouteCalculatorResponse.error_to_json)])
let update_tracker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and positionFiltering =
         flag "position-filtering" (optional json_arg)
           ~doc:"JSON PositionFiltering"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and pricingPlanDataSource =
         flag "pricing-plan-data-source" (optional string)
           ~doc:"STRING String"
       and trackerName =
         flag "tracker-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_tracker
           (Values.UpdateTrackerRequest.make ?description
              ?positionFiltering:(Option.map
                                    ~f:Values.PositionFiltering.of_json
                                    positionFiltering)
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan) ?pricingPlanDataSource
              ~trackerName ()) (Some Values.UpdateTrackerResponse.to_json)
           (Some Values.UpdateTrackerResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-tracker-consumer", associate_tracker_consumer);
    ("batch-delete-device-position-history",
      batch_delete_device_position_history);
    ("batch-delete-geofence", batch_delete_geofence);
    ("batch-evaluate-geofences", batch_evaluate_geofences);
    ("batch-get-device-position", batch_get_device_position);
    ("batch-put-geofence", batch_put_geofence);
    ("batch-update-device-position", batch_update_device_position);
    ("calculate-route", calculate_route);
    ("calculate-route-matrix", calculate_route_matrix);
    ("create-geofence-collection", create_geofence_collection);
    ("create-map", create_map);
    ("create-place-index", create_place_index);
    ("create-route-calculator", create_route_calculator);
    ("create-tracker", create_tracker);
    ("delete-geofence-collection", delete_geofence_collection);
    ("delete-map", delete_map);
    ("delete-place-index", delete_place_index);
    ("delete-route-calculator", delete_route_calculator);
    ("delete-tracker", delete_tracker);
    ("describe-geofence-collection", describe_geofence_collection);
    ("describe-map", describe_map);
    ("describe-place-index", describe_place_index);
    ("describe-route-calculator", describe_route_calculator);
    ("describe-tracker", describe_tracker);
    ("disassociate-tracker-consumer", disassociate_tracker_consumer);
    ("get-device-position", get_device_position);
    ("get-device-position-history", get_device_position_history);
    ("get-geofence", get_geofence);
    ("get-map-glyphs", get_map_glyphs);
    ("get-map-sprites", get_map_sprites);
    ("get-map-style-descriptor", get_map_style_descriptor);
    ("get-map-tile", get_map_tile);
    ("list-device-positions", list_device_positions);
    ("list-geofence-collections", list_geofence_collections);
    ("list-geofences", list_geofences);
    ("list-maps", list_maps);
    ("list-place-indexes", list_place_indexes);
    ("list-route-calculators", list_route_calculators);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-tracker-consumers", list_tracker_consumers);
    ("list-trackers", list_trackers);
    ("put-geofence", put_geofence);
    ("search-place-index-for-position", search_place_index_for_position);
    ("search-place-index-for-suggestions",
      search_place_index_for_suggestions);
    ("search-place-index-for-text", search_place_index_for_text);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-geofence-collection", update_geofence_collection);
    ("update-map", update_map);
    ("update-place-index", update_place_index);
    ("update-route-calculator", update_route_calculator);
    ("update-tracker", update_tracker)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID"
       and hierarchyId =
         flag "hierarchy-id" (required string) ~doc:"STRING ID"
       and childAssetId =
         flag "child-asset-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_assets
           (Values.AssociateAssetsRequest.make ?clientToken ~assetId
              ~hierarchyId ~childAssetId ()) None None])
let associate_time_series_to_asset_property =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and alias = flag "alias" (required string) ~doc:"STRING PropertyAlias"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_time_series_to_asset_property
           (Values.AssociateTimeSeriesToAssetPropertyRequest.make
              ?clientToken ~alias ~assetId ~propertyId ()) None None])
let batch_associate_project_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and projectId = flag "project-id" (required string) ~doc:"STRING ID"
       and assetIds = flag "asset-ids" (required json_arg) ~doc:"JSON IDs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_associate_project_assets
           (Values.BatchAssociateProjectAssetsRequest.make ?clientToken
              ~projectId ~assetIds:(Values.IDs.of_json assetIds) ())
           (Some Values.BatchAssociateProjectAssetsResponse.to_json)
           (Some Values.BatchAssociateProjectAssetsResponse.error_to_json)])
let batch_disassociate_project_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and projectId = flag "project-id" (required string) ~doc:"STRING ID"
       and assetIds = flag "asset-ids" (required json_arg) ~doc:"JSON IDs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disassociate_project_assets
           (Values.BatchDisassociateProjectAssetsRequest.make ?clientToken
              ~projectId ~assetIds:(Values.IDs.of_json assetIds) ())
           (Some Values.BatchDisassociateProjectAssetsResponse.to_json)
           (Some Values.BatchDisassociateProjectAssetsResponse.error_to_json)])
let batch_put_asset_property_value =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON PutAssetPropertyValueEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_asset_property_value
           (Values.BatchPutAssetPropertyValueRequest.make
              ~entries:(Values.PutAssetPropertyValueEntries.of_json entries)
              ()) (Some Values.BatchPutAssetPropertyValueResponse.to_json)
           (Some Values.BatchPutAssetPropertyValueResponse.error_to_json)])
let create_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and accessPolicyIdentity =
         flag "access-policy-identity" (required json_arg)
           ~doc:"JSON Identity"
       and accessPolicyResource =
         flag "access-policy-resource" (required json_arg)
           ~doc:"JSON Resource"
       and accessPolicyPermission =
         flag "access-policy-permission" (required json_arg)
           ~doc:"JSON Permission" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_policy
           (Values.CreateAccessPolicyRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~accessPolicyIdentity:(Values.Identity.of_json
                                       accessPolicyIdentity)
              ~accessPolicyResource:(Values.Resource.of_json
                                       accessPolicyResource)
              ~accessPolicyPermission:(Values.Permission.of_json
                                         accessPolicyPermission) ())
           (Some Values.CreateAccessPolicyResponse.to_json)
           (Some Values.CreateAccessPolicyResponse.error_to_json)])
let create_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and assetName = flag "asset-name" (required string) ~doc:"STRING Name"
       and assetModelId =
         flag "asset-model-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_asset
           (Values.CreateAssetRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~assetName
              ~assetModelId ()) (Some Values.CreateAssetResponse.to_json)
           (Some Values.CreateAssetResponse.error_to_json)])
let create_asset_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetModelDescription =
         flag "asset-model-description" (optional string)
           ~doc:"STRING Description"
       and assetModelProperties =
         flag "asset-model-properties" (optional json_arg)
           ~doc:"JSON AssetModelPropertyDefinitions"
       and assetModelHierarchies =
         flag "asset-model-hierarchies" (optional json_arg)
           ~doc:"JSON AssetModelHierarchyDefinitions"
       and assetModelCompositeModels =
         flag "asset-model-composite-models" (optional json_arg)
           ~doc:"JSON AssetModelCompositeModelDefinitions"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and assetModelName =
         flag "asset-model-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_asset_model
           (Values.CreateAssetModelRequest.make ?assetModelDescription
              ?assetModelProperties:(Option.map
                                       ~f:Values.AssetModelPropertyDefinitions.of_json
                                       assetModelProperties)
              ?assetModelHierarchies:(Option.map
                                        ~f:Values.AssetModelHierarchyDefinitions.of_json
                                        assetModelHierarchies)
              ?assetModelCompositeModels:(Option.map
                                            ~f:Values.AssetModelCompositeModelDefinitions.of_json
                                            assetModelCompositeModels)
              ?clientToken ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~assetModelName ())
           (Some Values.CreateAssetModelResponse.to_json)
           (Some Values.CreateAssetModelResponse.error_to_json)])
let create_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dashboardDescription =
         flag "dashboard-description" (optional string)
           ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and projectId = flag "project-id" (required string) ~doc:"STRING ID"
       and dashboardName =
         flag "dashboard-name" (required string) ~doc:"STRING Name"
       and dashboardDefinition =
         flag "dashboard-definition" (required string)
           ~doc:"STRING DashboardDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dashboard
           (Values.CreateDashboardRequest.make ?dashboardDescription
              ?clientToken ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~projectId ~dashboardName ~dashboardDefinition ())
           (Some Values.CreateDashboardResponse.to_json)
           (Some Values.CreateDashboardResponse.error_to_json)])
let create_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and gatewayName =
         flag "gateway-name" (required string) ~doc:"STRING Name"
       and gatewayPlatform =
         flag "gateway-platform" (required json_arg)
           ~doc:"JSON GatewayPlatform" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gateway
           (Values.CreateGatewayRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~gatewayName
              ~gatewayPlatform:(Values.GatewayPlatform.of_json
                                  gatewayPlatform) ())
           (Some Values.CreateGatewayResponse.to_json)
           (Some Values.CreateGatewayResponse.error_to_json)])
let create_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalDescription =
         flag "portal-description" (optional string)
           ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and portalLogoImageFile =
         flag "portal-logo-image-file" (optional json_arg)
           ~doc:"JSON ImageFile"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and portalAuthMode =
         flag "portal-auth-mode" (optional json_arg) ~doc:"JSON AuthMode"
       and notificationSenderEmail =
         flag "notification-sender-email" (optional string)
           ~doc:"STRING Email"
       and alarms = flag "alarms" (optional json_arg) ~doc:"JSON Alarms"
       and portalName =
         flag "portal-name" (required string) ~doc:"STRING Name"
       and portalContactEmail =
         flag "portal-contact-email" (required string) ~doc:"STRING Email"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_portal
           (Values.CreatePortalRequest.make ?portalDescription ?clientToken
              ?portalLogoImageFile:(Option.map ~f:Values.ImageFile.of_json
                                      portalLogoImageFile)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?portalAuthMode:(Option.map ~f:Values.AuthMode.of_json
                                 portalAuthMode) ?notificationSenderEmail
              ?alarms:(Option.map ~f:Values.Alarms.of_json alarms)
              ~portalName ~portalContactEmail ~roleArn ())
           (Some Values.CreatePortalResponse.to_json)
           (Some Values.CreatePortalResponse.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectDescription =
         flag "project-description" (optional string)
           ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and portalId = flag "portal-id" (required string) ~doc:"STRING ID"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectRequest.make ?projectDescription ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~portalId
              ~projectName ()) (Some Values.CreateProjectResponse.to_json)
           (Some Values.CreateProjectResponse.error_to_json)])
let delete_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and accessPolicyId =
         flag "access-policy-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_policy
           (Values.DeleteAccessPolicyRequest.make ?clientToken
              ~accessPolicyId ())
           (Some Values.DeleteAccessPolicyResponse.to_json)
           (Some Values.DeleteAccessPolicyResponse.error_to_json)])
let delete_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_asset
           (Values.DeleteAssetRequest.make ?clientToken ~assetId ())
           (Some Values.DeleteAssetResponse.to_json)
           (Some Values.DeleteAssetResponse.error_to_json)])
let delete_asset_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assetModelId =
         flag "asset-model-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_asset_model
           (Values.DeleteAssetModelRequest.make ?clientToken ~assetModelId ())
           (Some Values.DeleteAssetModelResponse.to_json)
           (Some Values.DeleteAssetModelResponse.error_to_json)])
let delete_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and dashboardId =
         flag "dashboard-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dashboard
           (Values.DeleteDashboardRequest.make ?clientToken ~dashboardId ())
           (Some Values.DeleteDashboardResponse.to_json)
           (Some Values.DeleteDashboardResponse.error_to_json)])
let delete_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId = flag "gateway-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway (Values.DeleteGatewayRequest.make ~gatewayId ())
           None None])
let delete_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and portalId = flag "portal-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_portal
           (Values.DeletePortalRequest.make ?clientToken ~portalId ())
           (Some Values.DeletePortalResponse.to_json)
           (Some Values.DeletePortalResponse.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and projectId = flag "project-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project
           (Values.DeleteProjectRequest.make ?clientToken ~projectId ())
           (Some Values.DeleteProjectResponse.to_json)
           (Some Values.DeleteProjectResponse.error_to_json)])
let delete_time_series =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alias = flag "alias" (optional string) ~doc:"STRING PropertyAlias"
       and assetId = flag "asset-id" (optional string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (optional string) ~doc:"STRING ID"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_time_series
           (Values.DeleteTimeSeriesRequest.make ?alias ?assetId ?propertyId
              ?clientToken ()) None None])
let describe_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessPolicyId =
         flag "access-policy-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_access_policy
           (Values.DescribeAccessPolicyRequest.make ~accessPolicyId ())
           (Some Values.DescribeAccessPolicyResponse.to_json)
           (Some Values.DescribeAccessPolicyResponse.error_to_json)])
let describe_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_asset (Values.DescribeAssetRequest.make ~assetId ())
           (Some Values.DescribeAssetResponse.to_json)
           (Some Values.DescribeAssetResponse.error_to_json)])
let describe_asset_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetModelId =
         flag "asset-model-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_asset_model
           (Values.DescribeAssetModelRequest.make ~assetModelId ())
           (Some Values.DescribeAssetModelResponse.to_json)
           (Some Values.DescribeAssetModelResponse.error_to_json)])
let describe_asset_property =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_asset_property
           (Values.DescribeAssetPropertyRequest.make ~assetId ~propertyId ())
           (Some Values.DescribeAssetPropertyResponse.to_json)
           (Some Values.DescribeAssetPropertyResponse.error_to_json)])
let describe_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dashboardId =
         flag "dashboard-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dashboard
           (Values.DescribeDashboardRequest.make ~dashboardId ())
           (Some Values.DescribeDashboardResponse.to_json)
           (Some Values.DescribeDashboardResponse.error_to_json)])
let describe_default_encryption_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_default_encryption_configuration
           (Values.DescribeDefaultEncryptionConfigurationRequest.make ())
           (Some
              Values.DescribeDefaultEncryptionConfigurationResponse.to_json)
           (Some
              Values.DescribeDefaultEncryptionConfigurationResponse.error_to_json)])
let describe_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId = flag "gateway-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_gateway
           (Values.DescribeGatewayRequest.make ~gatewayId ())
           (Some Values.DescribeGatewayResponse.to_json)
           (Some Values.DescribeGatewayResponse.error_to_json)])
let describe_gateway_capability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId = flag "gateway-id" (required string) ~doc:"STRING ID"
       and capabilityNamespace =
         flag "capability-namespace" (required string)
           ~doc:"STRING CapabilityNamespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_gateway_capability_configuration
           (Values.DescribeGatewayCapabilityConfigurationRequest.make
              ~gatewayId ~capabilityNamespace ())
           (Some
              Values.DescribeGatewayCapabilityConfigurationResponse.to_json)
           (Some
              Values.DescribeGatewayCapabilityConfigurationResponse.error_to_json)])
let describe_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_logging_options
           (Values.DescribeLoggingOptionsRequest.make ())
           (Some Values.DescribeLoggingOptionsResponse.to_json)
           (Some Values.DescribeLoggingOptionsResponse.error_to_json)])
let describe_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalId = flag "portal-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_portal
           (Values.DescribePortalRequest.make ~portalId ())
           (Some Values.DescribePortalResponse.to_json)
           (Some Values.DescribePortalResponse.error_to_json)])
let describe_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectId = flag "project-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_project
           (Values.DescribeProjectRequest.make ~projectId ())
           (Some Values.DescribeProjectResponse.to_json)
           (Some Values.DescribeProjectResponse.error_to_json)])
let describe_storage_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_storage_configuration
           (Values.DescribeStorageConfigurationRequest.make ())
           (Some Values.DescribeStorageConfigurationResponse.to_json)
           (Some Values.DescribeStorageConfigurationResponse.error_to_json)])
let describe_time_series =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alias = flag "alias" (optional string) ~doc:"STRING PropertyAlias"
       and assetId = flag "asset-id" (optional string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (optional string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_time_series
           (Values.DescribeTimeSeriesRequest.make ?alias ?assetId ?propertyId
              ()) (Some Values.DescribeTimeSeriesResponse.to_json)
           (Some Values.DescribeTimeSeriesResponse.error_to_json)])
let disassociate_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID"
       and hierarchyId =
         flag "hierarchy-id" (required string) ~doc:"STRING ID"
       and childAssetId =
         flag "child-asset-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_assets
           (Values.DisassociateAssetsRequest.make ?clientToken ~assetId
              ~hierarchyId ~childAssetId ()) None None])
let disassociate_time_series_from_asset_property =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and alias = flag "alias" (required string) ~doc:"STRING PropertyAlias"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_time_series_from_asset_property
           (Values.DisassociateTimeSeriesFromAssetPropertyRequest.make
              ?clientToken ~alias ~assetId ~propertyId ()) None None])
let get_asset_property_aggregates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetId = flag "asset-id" (optional string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (optional string) ~doc:"STRING ID"
       and propertyAlias =
         flag "property-alias" (optional string)
           ~doc:"STRING AssetPropertyAlias"
       and qualities =
         flag "qualities" (optional json_arg) ~doc:"JSON Qualities"
       and timeOrdering =
         flag "time-ordering" (optional json_arg) ~doc:"JSON TimeOrdering"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and aggregateTypes =
         flag "aggregate-types" (required json_arg)
           ~doc:"JSON AggregateTypes"
       and resolution =
         flag "resolution" (required string) ~doc:"STRING Resolution"
       and startDate =
         flag "start-date" (required json_arg) ~doc:"JSON Timestamp"
       and endDate =
         flag "end-date" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_asset_property_aggregates
           (Values.GetAssetPropertyAggregatesRequest.make ?assetId
              ?propertyId ?propertyAlias
              ?qualities:(Option.map ~f:Values.Qualities.of_json qualities)
              ?timeOrdering:(Option.map ~f:Values.TimeOrdering.of_json
                               timeOrdering) ?nextToken ?maxResults
              ~aggregateTypes:(Values.AggregateTypes.of_json aggregateTypes)
              ~resolution ~startDate:(Values.Timestamp.of_json startDate)
              ~endDate:(Values.Timestamp.of_json endDate) ())
           (Some Values.GetAssetPropertyAggregatesResponse.to_json)
           (Some Values.GetAssetPropertyAggregatesResponse.error_to_json)])
let get_asset_property_value =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetId = flag "asset-id" (optional string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (optional string) ~doc:"STRING ID"
       and propertyAlias =
         flag "property-alias" (optional string)
           ~doc:"STRING AssetPropertyAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_asset_property_value
           (Values.GetAssetPropertyValueRequest.make ?assetId ?propertyId
              ?propertyAlias ())
           (Some Values.GetAssetPropertyValueResponse.to_json)
           (Some Values.GetAssetPropertyValueResponse.error_to_json)])
let get_asset_property_value_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetId = flag "asset-id" (optional string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (optional string) ~doc:"STRING ID"
       and propertyAlias =
         flag "property-alias" (optional string)
           ~doc:"STRING AssetPropertyAlias"
       and startDate =
         flag "start-date" (optional json_arg) ~doc:"JSON Timestamp"
       and endDate =
         flag "end-date" (optional json_arg) ~doc:"JSON Timestamp"
       and qualities =
         flag "qualities" (optional json_arg) ~doc:"JSON Qualities"
       and timeOrdering =
         flag "time-ordering" (optional json_arg) ~doc:"JSON TimeOrdering"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_asset_property_value_history
           (Values.GetAssetPropertyValueHistoryRequest.make ?assetId
              ?propertyId ?propertyAlias
              ?startDate:(Option.map ~f:Values.Timestamp.of_json startDate)
              ?endDate:(Option.map ~f:Values.Timestamp.of_json endDate)
              ?qualities:(Option.map ~f:Values.Qualities.of_json qualities)
              ?timeOrdering:(Option.map ~f:Values.TimeOrdering.of_json
                               timeOrdering) ?nextToken ?maxResults ())
           (Some Values.GetAssetPropertyValueHistoryResponse.to_json)
           (Some Values.GetAssetPropertyValueHistoryResponse.error_to_json)])
let get_interpolated_asset_property_values =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetId = flag "asset-id" (optional string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (optional string) ~doc:"STRING ID"
       and propertyAlias =
         flag "property-alias" (optional string)
           ~doc:"STRING AssetPropertyAlias"
       and startTimeOffsetInNanos =
         flag "start-time-offset-in-nanos" (optional int)
           ~doc:"INT OffsetInNanos"
       and endTimeOffsetInNanos =
         flag "end-time-offset-in-nanos" (optional int)
           ~doc:"INT OffsetInNanos"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxInterpolatedResults"
       and intervalWindowInSeconds =
         flag "interval-window-in-seconds" (optional json_arg)
           ~doc:"JSON IntervalWindowInSeconds"
       and startTimeInSeconds =
         flag "start-time-in-seconds" (required json_arg)
           ~doc:"JSON TimeInSeconds"
       and endTimeInSeconds =
         flag "end-time-in-seconds" (required json_arg)
           ~doc:"JSON TimeInSeconds"
       and quality = flag "quality" (required json_arg) ~doc:"JSON Quality"
       and intervalInSeconds =
         flag "interval-in-seconds" (required json_arg)
           ~doc:"JSON IntervalInSeconds"
       and type_ =
         flag "type-" (required string) ~doc:"STRING InterpolationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_interpolated_asset_property_values
           (Values.GetInterpolatedAssetPropertyValuesRequest.make ?assetId
              ?propertyId ?propertyAlias ?startTimeOffsetInNanos
              ?endTimeOffsetInNanos ?nextToken ?maxResults
              ?intervalWindowInSeconds:(Option.map
                                          ~f:Values.IntervalWindowInSeconds.of_json
                                          intervalWindowInSeconds)
              ~startTimeInSeconds:(Values.TimeInSeconds.of_json
                                     startTimeInSeconds)
              ~endTimeInSeconds:(Values.TimeInSeconds.of_json
                                   endTimeInSeconds)
              ~quality:(Values.Quality.of_json quality)
              ~intervalInSeconds:(Values.IntervalInSeconds.of_json
                                    intervalInSeconds) ~type_ ())
           (Some Values.GetInterpolatedAssetPropertyValuesResponse.to_json)
           (Some
              Values.GetInterpolatedAssetPropertyValuesResponse.error_to_json)])
let list_access_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityType =
         flag "identity-type" (optional json_arg) ~doc:"JSON IdentityType"
       and identityId =
         flag "identity-id" (optional string) ~doc:"STRING IdentityId"
       and resourceType =
         flag "resource-type" (optional json_arg) ~doc:"JSON ResourceType"
       and resourceId = flag "resource-id" (optional string) ~doc:"STRING ID"
       and iamArn = flag "iam-arn" (optional string) ~doc:"STRING ARN"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_policies
           (Values.ListAccessPoliciesRequest.make
              ?identityType:(Option.map ~f:Values.IdentityType.of_json
                               identityType) ?identityId
              ?resourceType:(Option.map ~f:Values.ResourceType.of_json
                               resourceType) ?resourceId ?iamArn ?nextToken
              ?maxResults ())
           (Some Values.ListAccessPoliciesResponse.to_json)
           (Some Values.ListAccessPoliciesResponse.error_to_json)])
let list_asset_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_asset_models
           (Values.ListAssetModelsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListAssetModelsResponse.to_json)
           (Some Values.ListAssetModelsResponse.error_to_json)])
let list_asset_relationships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID"
       and traversalType =
         flag "traversal-type" (required json_arg) ~doc:"JSON TraversalType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_asset_relationships
           (Values.ListAssetRelationshipsRequest.make ?nextToken ?maxResults
              ~assetId
              ~traversalType:(Values.TraversalType.of_json traversalType) ())
           (Some Values.ListAssetRelationshipsResponse.to_json)
           (Some Values.ListAssetRelationshipsResponse.error_to_json)])
let list_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assetModelId =
         flag "asset-model-id" (optional string) ~doc:"STRING ID"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ListAssetsFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assets
           (Values.ListAssetsRequest.make ?nextToken ?maxResults
              ?assetModelId
              ?filter:(Option.map ~f:Values.ListAssetsFilter.of_json filter)
              ()) (Some Values.ListAssetsResponse.to_json)
           (Some Values.ListAssetsResponse.error_to_json)])
let list_associated_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hierarchyId =
         flag "hierarchy-id" (optional string) ~doc:"STRING ID"
       and traversalDirection =
         flag "traversal-direction" (optional json_arg)
           ~doc:"JSON TraversalDirection"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associated_assets
           (Values.ListAssociatedAssetsRequest.make ?hierarchyId
              ?traversalDirection:(Option.map
                                     ~f:Values.TraversalDirection.of_json
                                     traversalDirection) ?nextToken
              ?maxResults ~assetId ())
           (Some Values.ListAssociatedAssetsResponse.to_json)
           (Some Values.ListAssociatedAssetsResponse.error_to_json)])
let list_dashboards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and projectId = flag "project-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dashboards
           (Values.ListDashboardsRequest.make ?nextToken ?maxResults
              ~projectId ()) (Some Values.ListDashboardsResponse.to_json)
           (Some Values.ListDashboardsResponse.error_to_json)])
let list_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateways
           (Values.ListGatewaysRequest.make ?nextToken ?maxResults ())
           (Some Values.ListGatewaysResponse.to_json)
           (Some Values.ListGatewaysResponse.error_to_json)])
let list_portals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_portals
           (Values.ListPortalsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListPortalsResponse.to_json)
           (Some Values.ListPortalsResponse.error_to_json)])
let list_project_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and projectId = flag "project-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_project_assets
           (Values.ListProjectAssetsRequest.make ?nextToken ?maxResults
              ~projectId ()) (Some Values.ListProjectAssetsResponse.to_json)
           (Some Values.ListProjectAssetsResponse.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and portalId = flag "portal-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsRequest.make ?nextToken ?maxResults ~portalId
              ()) (Some Values.ListProjectsResponse.to_json)
           (Some Values.ListProjectsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_time_series =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assetId = flag "asset-id" (optional string) ~doc:"STRING ID"
       and aliasPrefix =
         flag "alias-prefix" (optional string) ~doc:"STRING PropertyAlias"
       and timeSeriesType =
         flag "time-series-type" (optional json_arg)
           ~doc:"JSON ListTimeSeriesType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_time_series
           (Values.ListTimeSeriesRequest.make ?nextToken ?maxResults ?assetId
              ?aliasPrefix
              ?timeSeriesType:(Option.map
                                 ~f:Values.ListTimeSeriesType.of_json
                                 timeSeriesType) ())
           (Some Values.ListTimeSeriesResponse.to_json)
           (Some Values.ListTimeSeriesResponse.error_to_json)])
let put_default_encryption_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and encryptionType =
         flag "encryption-type" (required json_arg)
           ~doc:"JSON EncryptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_default_encryption_configuration
           (Values.PutDefaultEncryptionConfigurationRequest.make ?kmsKeyId
              ~encryptionType:(Values.EncryptionType.of_json encryptionType)
              ())
           (Some Values.PutDefaultEncryptionConfigurationResponse.to_json)
           (Some
              Values.PutDefaultEncryptionConfigurationResponse.error_to_json)])
let put_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loggingOptions =
         flag "logging-options" (required json_arg)
           ~doc:"JSON LoggingOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_logging_options
           (Values.PutLoggingOptionsRequest.make
              ~loggingOptions:(Values.LoggingOptions.of_json loggingOptions)
              ()) (Some Values.PutLoggingOptionsResponse.to_json)
           (Some Values.PutLoggingOptionsResponse.error_to_json)])
let put_storage_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and multiLayerStorage =
         flag "multi-layer-storage" (optional json_arg)
           ~doc:"JSON MultiLayerStorage"
       and disassociatedDataStorage =
         flag "disassociated-data-storage" (optional json_arg)
           ~doc:"JSON DisassociatedDataStorageState"
       and retentionPeriod =
         flag "retention-period" (optional json_arg)
           ~doc:"JSON RetentionPeriod"
       and storageType =
         flag "storage-type" (required json_arg) ~doc:"JSON StorageType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_storage_configuration
           (Values.PutStorageConfigurationRequest.make
              ?multiLayerStorage:(Option.map
                                    ~f:Values.MultiLayerStorage.of_json
                                    multiLayerStorage)
              ?disassociatedDataStorage:(Option.map
                                           ~f:Values.DisassociatedDataStorageState.of_json
                                           disassociatedDataStorage)
              ?retentionPeriod:(Option.map ~f:Values.RetentionPeriod.of_json
                                  retentionPeriod)
              ~storageType:(Values.StorageType.of_json storageType) ())
           (Some Values.PutStorageConfigurationResponse.to_json)
           (Some Values.PutStorageConfigurationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and accessPolicyId =
         flag "access-policy-id" (required string) ~doc:"STRING ID"
       and accessPolicyIdentity =
         flag "access-policy-identity" (required json_arg)
           ~doc:"JSON Identity"
       and accessPolicyResource =
         flag "access-policy-resource" (required json_arg)
           ~doc:"JSON Resource"
       and accessPolicyPermission =
         flag "access-policy-permission" (required json_arg)
           ~doc:"JSON Permission" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_access_policy
           (Values.UpdateAccessPolicyRequest.make ?clientToken
              ~accessPolicyId
              ~accessPolicyIdentity:(Values.Identity.of_json
                                       accessPolicyIdentity)
              ~accessPolicyResource:(Values.Resource.of_json
                                       accessPolicyResource)
              ~accessPolicyPermission:(Values.Permission.of_json
                                         accessPolicyPermission) ())
           (Some Values.UpdateAccessPolicyResponse.to_json)
           (Some Values.UpdateAccessPolicyResponse.error_to_json)])
let update_asset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID"
       and assetName = flag "asset-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_asset
           (Values.UpdateAssetRequest.make ?clientToken ~assetId ~assetName
              ()) (Some Values.UpdateAssetResponse.to_json)
           (Some Values.UpdateAssetResponse.error_to_json)])
let update_asset_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetModelDescription =
         flag "asset-model-description" (optional string)
           ~doc:"STRING Description"
       and assetModelProperties =
         flag "asset-model-properties" (optional json_arg)
           ~doc:"JSON AssetModelProperties"
       and assetModelHierarchies =
         flag "asset-model-hierarchies" (optional json_arg)
           ~doc:"JSON AssetModelHierarchies"
       and assetModelCompositeModels =
         flag "asset-model-composite-models" (optional json_arg)
           ~doc:"JSON AssetModelCompositeModels"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assetModelId =
         flag "asset-model-id" (required string) ~doc:"STRING ID"
       and assetModelName =
         flag "asset-model-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_asset_model
           (Values.UpdateAssetModelRequest.make ?assetModelDescription
              ?assetModelProperties:(Option.map
                                       ~f:Values.AssetModelProperties.of_json
                                       assetModelProperties)
              ?assetModelHierarchies:(Option.map
                                        ~f:Values.AssetModelHierarchies.of_json
                                        assetModelHierarchies)
              ?assetModelCompositeModels:(Option.map
                                            ~f:Values.AssetModelCompositeModels.of_json
                                            assetModelCompositeModels)
              ?clientToken ~assetModelId ~assetModelName ())
           (Some Values.UpdateAssetModelResponse.to_json)
           (Some Values.UpdateAssetModelResponse.error_to_json)])
let update_asset_property =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and propertyAlias =
         flag "property-alias" (optional string) ~doc:"STRING PropertyAlias"
       and propertyNotificationState =
         flag "property-notification-state" (optional json_arg)
           ~doc:"JSON PropertyNotificationState"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assetId = flag "asset-id" (required string) ~doc:"STRING ID"
       and propertyId = flag "property-id" (required string) ~doc:"STRING ID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_asset_property
           (Values.UpdateAssetPropertyRequest.make ?propertyAlias
              ?propertyNotificationState:(Option.map
                                            ~f:Values.PropertyNotificationState.of_json
                                            propertyNotificationState)
              ?clientToken ~assetId ~propertyId ()) None None])
let update_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dashboardDescription =
         flag "dashboard-description" (optional string)
           ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and dashboardId =
         flag "dashboard-id" (required string) ~doc:"STRING ID"
       and dashboardName =
         flag "dashboard-name" (required string) ~doc:"STRING Name"
       and dashboardDefinition =
         flag "dashboard-definition" (required string)
           ~doc:"STRING DashboardDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dashboard
           (Values.UpdateDashboardRequest.make ?dashboardDescription
              ?clientToken ~dashboardId ~dashboardName ~dashboardDefinition
              ()) (Some Values.UpdateDashboardResponse.to_json)
           (Some Values.UpdateDashboardResponse.error_to_json)])
let update_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId = flag "gateway-id" (required string) ~doc:"STRING ID"
       and gatewayName =
         flag "gateway-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway
           (Values.UpdateGatewayRequest.make ~gatewayId ~gatewayName ()) None
           None])
let update_gateway_capability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId = flag "gateway-id" (required string) ~doc:"STRING ID"
       and capabilityNamespace =
         flag "capability-namespace" (required string)
           ~doc:"STRING CapabilityNamespace"
       and capabilityConfiguration =
         flag "capability-configuration" (required string)
           ~doc:"STRING CapabilityConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_capability_configuration
           (Values.UpdateGatewayCapabilityConfigurationRequest.make
              ~gatewayId ~capabilityNamespace ~capabilityConfiguration ())
           (Some Values.UpdateGatewayCapabilityConfigurationResponse.to_json)
           (Some
              Values.UpdateGatewayCapabilityConfigurationResponse.error_to_json)])
let update_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalDescription =
         flag "portal-description" (optional string)
           ~doc:"STRING Description"
       and portalLogoImage =
         flag "portal-logo-image" (optional json_arg) ~doc:"JSON Image"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and notificationSenderEmail =
         flag "notification-sender-email" (optional string)
           ~doc:"STRING Email"
       and alarms = flag "alarms" (optional json_arg) ~doc:"JSON Alarms"
       and portalId = flag "portal-id" (required string) ~doc:"STRING ID"
       and portalName =
         flag "portal-name" (required string) ~doc:"STRING Name"
       and portalContactEmail =
         flag "portal-contact-email" (required string) ~doc:"STRING Email"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_portal
           (Values.UpdatePortalRequest.make ?portalDescription
              ?portalLogoImage:(Option.map ~f:Values.Image.of_json
                                  portalLogoImage) ?clientToken
              ?notificationSenderEmail
              ?alarms:(Option.map ~f:Values.Alarms.of_json alarms) ~portalId
              ~portalName ~portalContactEmail ~roleArn ())
           (Some Values.UpdatePortalResponse.to_json)
           (Some Values.UpdatePortalResponse.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectDescription =
         flag "project-description" (optional string)
           ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and projectId = flag "project-id" (required string) ~doc:"STRING ID"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectRequest.make ?projectDescription ?clientToken
              ~projectId ~projectName ())
           (Some Values.UpdateProjectResponse.to_json)
           (Some Values.UpdateProjectResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-assets", associate_assets);
    ("associate-time-series-to-asset-property",
      associate_time_series_to_asset_property);
    ("batch-associate-project-assets", batch_associate_project_assets);
    ("batch-disassociate-project-assets", batch_disassociate_project_assets);
    ("batch-put-asset-property-value", batch_put_asset_property_value);
    ("create-access-policy", create_access_policy);
    ("create-asset", create_asset);
    ("create-asset-model", create_asset_model);
    ("create-dashboard", create_dashboard);
    ("create-gateway", create_gateway);
    ("create-portal", create_portal);
    ("create-project", create_project);
    ("delete-access-policy", delete_access_policy);
    ("delete-asset", delete_asset);
    ("delete-asset-model", delete_asset_model);
    ("delete-dashboard", delete_dashboard);
    ("delete-gateway", delete_gateway);
    ("delete-portal", delete_portal);
    ("delete-project", delete_project);
    ("delete-time-series", delete_time_series);
    ("describe-access-policy", describe_access_policy);
    ("describe-asset", describe_asset);
    ("describe-asset-model", describe_asset_model);
    ("describe-asset-property", describe_asset_property);
    ("describe-dashboard", describe_dashboard);
    ("describe-default-encryption-configuration",
      describe_default_encryption_configuration);
    ("describe-gateway", describe_gateway);
    ("describe-gateway-capability-configuration",
      describe_gateway_capability_configuration);
    ("describe-logging-options", describe_logging_options);
    ("describe-portal", describe_portal);
    ("describe-project", describe_project);
    ("describe-storage-configuration", describe_storage_configuration);
    ("describe-time-series", describe_time_series);
    ("disassociate-assets", disassociate_assets);
    ("disassociate-time-series-from-asset-property",
      disassociate_time_series_from_asset_property);
    ("get-asset-property-aggregates", get_asset_property_aggregates);
    ("get-asset-property-value", get_asset_property_value);
    ("get-asset-property-value-history", get_asset_property_value_history);
    ("get-interpolated-asset-property-values",
      get_interpolated_asset_property_values);
    ("list-access-policies", list_access_policies);
    ("list-asset-models", list_asset_models);
    ("list-asset-relationships", list_asset_relationships);
    ("list-assets", list_assets);
    ("list-associated-assets", list_associated_assets);
    ("list-dashboards", list_dashboards);
    ("list-gateways", list_gateways);
    ("list-portals", list_portals);
    ("list-project-assets", list_project_assets);
    ("list-projects", list_projects);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-time-series", list_time_series);
    ("put-default-encryption-configuration",
      put_default_encryption_configuration);
    ("put-logging-options", put_logging_options);
    ("put-storage-configuration", put_storage_configuration);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-access-policy", update_access_policy);
    ("update-asset", update_asset);
    ("update-asset-model", update_asset_model);
    ("update-asset-property", update_asset_property);
    ("update-dashboard", update_dashboard);
    ("update-gateway", update_gateway);
    ("update-gateway-capability-configuration",
      update_gateway_capability_configuration);
    ("update-portal", update_portal);
    ("update-project", update_project)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_traces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and traceIds =
         flag "trace-ids" (required json_arg) ~doc:"JSON TraceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_traces
           (Values.BatchGetTracesRequest.make ?nextToken
              ~traceIds:(Values.TraceIdList.of_json traceIds) ())
           (Some Values.BatchGetTracesResult.to_json)
           (Some Values.BatchGetTracesResult.error_to_json)])
let create_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterExpression =
         flag "filter-expression" (optional string)
           ~doc:"STRING FilterExpression"
       and insightsConfiguration =
         flag "insights-configuration" (optional json_arg)
           ~doc:"JSON InsightsConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group
           (Values.CreateGroupRequest.make ?filterExpression
              ?insightsConfiguration:(Option.map
                                        ~f:Values.InsightsConfiguration.of_json
                                        insightsConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~groupName ())
           (Some Values.CreateGroupResult.to_json)
           (Some Values.CreateGroupResult.error_to_json)])
let create_sampling_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and samplingRule =
         flag "sampling-rule" (required json_arg) ~doc:"JSON SamplingRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sampling_rule
           (Values.CreateSamplingRuleRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~samplingRule:(Values.SamplingRule.of_json samplingRule) ())
           (Some Values.CreateSamplingRuleResult.to_json)
           (Some Values.CreateSamplingRuleResult.error_to_json)])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and groupARN =
         flag "group-a-r-n" (optional string) ~doc:"STRING GroupARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group
           (Values.DeleteGroupRequest.make ?groupName ?groupARN ())
           (Some Values.DeleteGroupResult.to_json)
           (Some Values.DeleteGroupResult.error_to_json)])
let delete_sampling_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleName = flag "rule-name" (optional string) ~doc:"STRING String"
       and ruleARN = flag "rule-a-r-n" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sampling_rule
           (Values.DeleteSamplingRuleRequest.make ?ruleName ?ruleARN ())
           (Some Values.DeleteSamplingRuleResult.to_json)
           (Some Values.DeleteSamplingRuleResult.error_to_json)])
let get_encryption_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_encryption_config
           (Values.GetEncryptionConfigRequest.make ())
           (Some Values.GetEncryptionConfigResult.to_json)
           (Some Values.GetEncryptionConfigResult.error_to_json)])
let get_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and groupARN =
         flag "group-a-r-n" (optional string) ~doc:"STRING GroupARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group (Values.GetGroupRequest.make ?groupName ?groupARN ())
           (Some Values.GetGroupResult.to_json)
           (Some Values.GetGroupResult.error_to_json)])
let get_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GetGroupsNextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_groups (Values.GetGroupsRequest.make ?nextToken ())
           (Some Values.GetGroupsResult.to_json)
           (Some Values.GetGroupsResult.error_to_json)])
let get_insight =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and insightId =
         flag "insight-id" (required string) ~doc:"STRING InsightId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insight (Values.GetInsightRequest.make ~insightId ())
           (Some Values.GetInsightResult.to_json)
           (Some Values.GetInsightResult.error_to_json)])
let get_insight_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetInsightEventsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and insightId =
         flag "insight-id" (required string) ~doc:"STRING InsightId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insight_events
           (Values.GetInsightEventsRequest.make ?maxResults ?nextToken
              ~insightId ()) (Some Values.GetInsightEventsResult.to_json)
           (Some Values.GetInsightEventsResult.error_to_json)])
let get_insight_impact_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and insightId =
         flag "insight-id" (required string) ~doc:"STRING InsightId"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insight_impact_graph
           (Values.GetInsightImpactGraphRequest.make ?nextToken ~insightId
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.GetInsightImpactGraphResult.to_json)
           (Some Values.GetInsightImpactGraphResult.error_to_json)])
let get_insight_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON InsightStateList"
       and groupARN =
         flag "group-a-r-n" (optional string) ~doc:"STRING GroupARN"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetInsightSummariesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insight_summaries
           (Values.GetInsightSummariesRequest.make
              ?states:(Option.map ~f:Values.InsightStateList.of_json states)
              ?groupARN ?groupName ?maxResults ?nextToken
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.GetInsightSummariesResult.to_json)
           (Some Values.GetInsightSummariesResult.error_to_json)])
let get_sampling_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sampling_rules
           (Values.GetSamplingRulesRequest.make ?nextToken ())
           (Some Values.GetSamplingRulesResult.to_json)
           (Some Values.GetSamplingRulesResult.error_to_json)])
let get_sampling_statistic_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sampling_statistic_summaries
           (Values.GetSamplingStatisticSummariesRequest.make ?nextToken ())
           (Some Values.GetSamplingStatisticSummariesResult.to_json)
           (Some Values.GetSamplingStatisticSummariesResult.error_to_json)])
let get_sampling_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and samplingStatisticsDocuments =
         flag "sampling-statistics-documents" (required json_arg)
           ~doc:"JSON SamplingStatisticsDocumentList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sampling_targets
           (Values.GetSamplingTargetsRequest.make
              ~samplingStatisticsDocuments:(Values.SamplingStatisticsDocumentList.of_json
                                              samplingStatisticsDocuments) ())
           (Some Values.GetSamplingTargetsResult.to_json)
           (Some Values.GetSamplingTargetsResult.error_to_json)])
let get_service_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and groupARN =
         flag "group-a-r-n" (optional string) ~doc:"STRING GroupARN"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_graph
           (Values.GetServiceGraphRequest.make ?groupName ?groupARN
              ?nextToken ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.GetServiceGraphResult.to_json)
           (Some Values.GetServiceGraphResult.error_to_json)])
let get_time_series_service_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and groupARN =
         flag "group-a-r-n" (optional string) ~doc:"STRING GroupARN"
       and entitySelectorExpression =
         flag "entity-selector-expression" (optional string)
           ~doc:"STRING EntitySelectorExpression"
       and period = flag "period" (optional int) ~doc:"INT NullableInteger"
       and forecastStatistics =
         flag "forecast-statistics" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_time_series_service_statistics
           (Values.GetTimeSeriesServiceStatisticsRequest.make ?groupName
              ?groupARN ?entitySelectorExpression ?period ?forecastStatistics
              ?nextToken ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.GetTimeSeriesServiceStatisticsResult.to_json)
           (Some Values.GetTimeSeriesServiceStatisticsResult.error_to_json)])
let get_trace_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and traceIds =
         flag "trace-ids" (required json_arg) ~doc:"JSON TraceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_trace_graph
           (Values.GetTraceGraphRequest.make ?nextToken
              ~traceIds:(Values.TraceIdList.of_json traceIds) ())
           (Some Values.GetTraceGraphResult.to_json)
           (Some Values.GetTraceGraphResult.error_to_json)])
let get_trace_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timeRangeType =
         flag "time-range-type" (optional json_arg) ~doc:"JSON TimeRangeType"
       and sampling =
         flag "sampling" (optional bool) ~doc:"BOOL NullableBoolean"
       and samplingStrategy =
         flag "sampling-strategy" (optional json_arg)
           ~doc:"JSON SamplingStrategy"
       and filterExpression =
         flag "filter-expression" (optional string)
           ~doc:"STRING FilterExpression"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_trace_summaries
           (Values.GetTraceSummariesRequest.make
              ?timeRangeType:(Option.map ~f:Values.TimeRangeType.of_json
                                timeRangeType) ?sampling
              ?samplingStrategy:(Option.map
                                   ~f:Values.SamplingStrategy.of_json
                                   samplingStrategy) ?filterExpression
              ?nextToken ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.GetTraceSummariesResult.to_json)
           (Some Values.GetTraceSummariesResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_encryption_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId =
         flag "key-id" (optional string) ~doc:"STRING EncryptionKeyId"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON EncryptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_encryption_config
           (Values.PutEncryptionConfigRequest.make ?keyId
              ~type_:(Values.EncryptionType.of_json type_) ())
           (Some Values.PutEncryptionConfigResult.to_json)
           (Some Values.PutEncryptionConfigResult.error_to_json)])
let put_telemetry_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eC2InstanceId =
         flag "e-c2-instance-id" (optional string)
           ~doc:"STRING EC2InstanceId"
       and hostname =
         flag "hostname" (optional string) ~doc:"STRING Hostname"
       and resourceARN =
         flag "resource-a-r-n" (optional string) ~doc:"STRING ResourceARN"
       and telemetryRecords =
         flag "telemetry-records" (required json_arg)
           ~doc:"JSON TelemetryRecordList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_telemetry_records
           (Values.PutTelemetryRecordsRequest.make ?eC2InstanceId ?hostname
              ?resourceARN
              ~telemetryRecords:(Values.TelemetryRecordList.of_json
                                   telemetryRecords) ())
           (Some Values.PutTelemetryRecordsResult.to_json)
           (Some Values.PutTelemetryRecordsResult.error_to_json)])
let put_trace_segments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and traceSegmentDocuments =
         flag "trace-segment-documents" (required json_arg)
           ~doc:"JSON TraceSegmentDocumentList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_trace_segments
           (Values.PutTraceSegmentsRequest.make
              ~traceSegmentDocuments:(Values.TraceSegmentDocumentList.of_json
                                        traceSegmentDocuments) ())
           (Some Values.PutTraceSegmentsResult.to_json)
           (Some Values.PutTraceSegmentsResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING GroupName"
       and groupARN =
         flag "group-a-r-n" (optional string) ~doc:"STRING GroupARN"
       and filterExpression =
         flag "filter-expression" (optional string)
           ~doc:"STRING FilterExpression"
       and insightsConfiguration =
         flag "insights-configuration" (optional json_arg)
           ~doc:"JSON InsightsConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group
           (Values.UpdateGroupRequest.make ?groupName ?groupARN
              ?filterExpression
              ?insightsConfiguration:(Option.map
                                        ~f:Values.InsightsConfiguration.of_json
                                        insightsConfiguration) ())
           (Some Values.UpdateGroupResult.to_json)
           (Some Values.UpdateGroupResult.error_to_json)])
let update_sampling_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and samplingRuleUpdate =
         flag "sampling-rule-update" (required json_arg)
           ~doc:"JSON SamplingRuleUpdate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sampling_rule
           (Values.UpdateSamplingRuleRequest.make
              ~samplingRuleUpdate:(Values.SamplingRuleUpdate.of_json
                                     samplingRuleUpdate) ())
           (Some Values.UpdateSamplingRuleResult.to_json)
           (Some Values.UpdateSamplingRuleResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-get-traces", batch_get_traces);
    ("create-group", create_group);
    ("create-sampling-rule", create_sampling_rule);
    ("delete-group", delete_group);
    ("delete-sampling-rule", delete_sampling_rule);
    ("get-encryption-config", get_encryption_config);
    ("get-group", get_group);
    ("get-groups", get_groups);
    ("get-insight", get_insight);
    ("get-insight-events", get_insight_events);
    ("get-insight-impact-graph", get_insight_impact_graph);
    ("get-insight-summaries", get_insight_summaries);
    ("get-sampling-rules", get_sampling_rules);
    ("get-sampling-statistic-summaries", get_sampling_statistic_summaries);
    ("get-sampling-targets", get_sampling_targets);
    ("get-service-graph", get_service_graph);
    ("get-time-series-service-statistics",
      get_time_series_service_statistics);
    ("get-trace-graph", get_trace_graph);
    ("get-trace-summaries", get_trace_summaries);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-encryption-config", put_encryption_config);
    ("put-telemetry-records", put_telemetry_records);
    ("put-trace-segments", put_trace_segments);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-group", update_group);
    ("update-sampling-rule", update_sampling_rule)]
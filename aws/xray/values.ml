(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.xray
let apiVersion = "2016-04-12"
let endpointPrefix = "xray"
let serviceFullName = "AWS X-Ray"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module RootCauseException =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the exception."];
      message: String_.t option [@ocaml.doc "The message of the exception."]}
    let make ?name = fun ?message -> fun () -> { name; message }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?message ?name ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?message ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The exception associated with a root cause."]
module NullableBoolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module RootCauseExceptions =
  struct
    type nonrec t = RootCauseException.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RootCauseException.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RootCauseException.of_xml)
    let of_json j =
      list_of_json ~kind:"RootCauseExceptions"
        ~of_json:RootCauseException.of_json j
    let to_json v = composed_to_json to_value v
  end
module NullableDouble =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module ServiceNames =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceNames" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorRootCauseEntity =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the entity."];
      exceptions: RootCauseExceptions.t option
        [@ocaml.doc "The types and messages of the exceptions."];
      remote: NullableBoolean.t option
        [@ocaml.doc "A flag that denotes a remote subsegment."]}
    let make ?name =
      fun ?exceptions ->
        fun ?remote -> fun () -> { name; exceptions; remote }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Exceptions",
          (Option.map x.exceptions ~f:RootCauseExceptions.to_value));
        ("Remote", (Option.map x.remote ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let remote =
        (Option.map ~f:NullableBoolean.of_xml) (Xml.child xml_arg0 "Remote") in
      let exceptions =
        (Option.map ~f:RootCauseExceptions.of_xml)
          (Xml.child xml_arg0 "Exceptions") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?remote ?exceptions ?name ()
    let of_json json =
      let remote = field_map json "Remote" NullableBoolean.of_json in
      let exceptions =
        field_map json "Exceptions" RootCauseExceptions.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?remote ?exceptions ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A collection of segments and corresponding subsegments associated to a trace summary error."]
module FaultRootCauseEntity =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the entity."];
      exceptions: RootCauseExceptions.t option
        [@ocaml.doc "The types and messages of the exceptions."];
      remote: NullableBoolean.t option
        [@ocaml.doc "A flag that denotes a remote subsegment."]}
    let make ?name =
      fun ?exceptions ->
        fun ?remote -> fun () -> { name; exceptions; remote }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Exceptions",
          (Option.map x.exceptions ~f:RootCauseExceptions.to_value));
        ("Remote", (Option.map x.remote ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let remote =
        (Option.map ~f:NullableBoolean.of_xml) (Xml.child xml_arg0 "Remote") in
      let exceptions =
        (Option.map ~f:RootCauseExceptions.of_xml)
          (Xml.child xml_arg0 "Exceptions") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?remote ?exceptions ?name ()
    let of_json json =
      let remote = field_map json "Remote" NullableBoolean.of_json in
      let exceptions =
        field_map json "Exceptions" RootCauseExceptions.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?remote ?exceptions ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A collection of segments and corresponding subsegments associated to a trace summary fault error."]
module ResponseTimeRootCauseEntity =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the entity."];
      coverage: NullableDouble.t option
        [@ocaml.doc "The type and messages of the exceptions."];
      remote: NullableBoolean.t option
        [@ocaml.doc "A flag that denotes a remote subsegment."]}
    let make ?name =
      fun ?coverage -> fun ?remote -> fun () -> { name; coverage; remote }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Coverage", (Option.map x.coverage ~f:NullableDouble.to_value));
        ("Remote", (Option.map x.remote ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let remote =
        (Option.map ~f:NullableBoolean.of_xml) (Xml.child xml_arg0 "Remote") in
      let coverage =
        (Option.map ~f:NullableDouble.of_xml) (Xml.child xml_arg0 "Coverage") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?remote ?coverage ?name ()
    let of_json json =
      let remote = field_map json "Remote" NullableBoolean.of_json in
      let coverage = field_map json "Coverage" NullableDouble.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?remote ?coverage ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A collection of segments and corresponding subsegments associated to a response time warning."]
module ServiceId =
  struct
    type nonrec t =
      {
      name: String_.t option ;
      names: ServiceNames.t option ;
      accountId: String_.t option ;
      type_: String_.t option }
    let make ?name =
      fun ?names ->
        fun ?accountId ->
          fun ?type_ -> fun () -> { name; names; accountId; type_ }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Names", (Option.map x.names ~f:ServiceNames.to_value));
        ("AccountId", (Option.map x.accountId ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let accountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccountId") in
      let names =
        (Option.map ~f:ServiceNames.of_xml) (Xml.child xml_arg0 "Names") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?type_ ?accountId ?names ?name ()
    let of_json json =
      let type_ = field_map json "Type" String_.of_json in
      let accountId = field_map json "AccountId" String_.of_json in
      let names = field_map json "Names" ServiceNames.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?type_ ?accountId ?names ?name ()
    let to_json v = composed_to_json to_value v
  end
module ErrorRootCauseEntityPath =
  struct
    type nonrec t = ErrorRootCauseEntity.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ErrorRootCauseEntity.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ErrorRootCauseEntity.of_xml)
    let of_json j =
      list_of_json ~kind:"ErrorRootCauseEntityPath"
        ~of_json:ErrorRootCauseEntity.of_json j
    let to_json v = composed_to_json to_value v
  end
module FaultRootCauseEntityPath =
  struct
    type nonrec t = FaultRootCauseEntity.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FaultRootCauseEntity.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FaultRootCauseEntity.of_xml)
    let of_json j =
      list_of_json ~kind:"FaultRootCauseEntityPath"
        ~of_json:FaultRootCauseEntity.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResponseTimeRootCauseEntityPath =
  struct
    type nonrec t = ResponseTimeRootCauseEntity.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResponseTimeRootCauseEntity.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResponseTimeRootCauseEntity.of_xml)
    let of_json j =
      list_of_json ~kind:"ResponseTimeRootCauseEntityPath"
        ~of_json:ResponseTimeRootCauseEntity.of_json j
    let to_json v = composed_to_json to_value v
  end
module AliasNames =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"AliasNames" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module NullableLong =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AnnotationValue =
  struct
    type nonrec t =
      {
      numberValue: NullableDouble.t option
        [@ocaml.doc "Value for a Number annotation."];
      booleanValue: NullableBoolean.t option
        [@ocaml.doc "Value for a Boolean annotation."];
      stringValue: String_.t option
        [@ocaml.doc "Value for a String annotation."]}
    let make ?numberValue =
      fun ?booleanValue ->
        fun ?stringValue ->
          fun () -> { numberValue; booleanValue; stringValue }
    let to_value x =
      structure_to_value
        [("NumberValue",
           (Option.map x.numberValue ~f:NullableDouble.to_value));
        ("BooleanValue",
          (Option.map x.booleanValue ~f:NullableBoolean.to_value));
        ("StringValue", (Option.map x.stringValue ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stringValue =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StringValue") in
      let booleanValue =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "BooleanValue") in
      let numberValue =
        (Option.map ~f:NullableDouble.of_xml)
          (Xml.child xml_arg0 "NumberValue") in
      make ?stringValue ?booleanValue ?numberValue ()
    let of_json json =
      let stringValue = field_map json "StringValue" String_.of_json in
      let booleanValue =
        field_map json "BooleanValue" NullableBoolean.of_json in
      let numberValue = field_map json "NumberValue" NullableDouble.of_json in
      make ?stringValue ?booleanValue ?numberValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Value of a segment annotation. Has one of three value types: Number, Boolean, or String."]
module ServiceIds =
  struct
    type nonrec t = ServiceId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ServiceId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServiceId.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceIds" ~of_json:ServiceId.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorRootCauseService =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The service name."];
      names: ServiceNames.t option
        [@ocaml.doc "A collection of associated service names."];
      type_: String_.t option
        [@ocaml.doc "The type associated to the service."];
      accountId: String_.t option
        [@ocaml.doc "The account ID associated to the service."];
      entityPath: ErrorRootCauseEntityPath.t option
        [@ocaml.doc "The path of root cause entities found on the service."];
      inferred: NullableBoolean.t option
        [@ocaml.doc
          "A Boolean value indicating if the service is inferred from the trace."]}
    let make ?name =
      fun ?names ->
        fun ?type_ ->
          fun ?accountId ->
            fun ?entityPath ->
              fun ?inferred ->
                fun () ->
                  { name; names; type_; accountId; entityPath; inferred }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Names", (Option.map x.names ~f:ServiceNames.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("AccountId", (Option.map x.accountId ~f:String_.to_value));
        ("EntityPath",
          (Option.map x.entityPath ~f:ErrorRootCauseEntityPath.to_value));
        ("Inferred", (Option.map x.inferred ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inferred =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "Inferred") in
      let entityPath =
        (Option.map ~f:ErrorRootCauseEntityPath.of_xml)
          (Xml.child xml_arg0 "EntityPath") in
      let accountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccountId") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let names =
        (Option.map ~f:ServiceNames.of_xml) (Xml.child xml_arg0 "Names") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?inferred ?entityPath ?accountId ?type_ ?names ?name ()
    let of_json json =
      let inferred = field_map json "Inferred" NullableBoolean.of_json in
      let entityPath =
        field_map json "EntityPath" ErrorRootCauseEntityPath.of_json in
      let accountId = field_map json "AccountId" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let names = field_map json "Names" ServiceNames.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?inferred ?entityPath ?accountId ?type_ ?names ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A collection of fields identifying the services in a trace summary error."]
module FaultRootCauseService =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The service name."];
      names: ServiceNames.t option
        [@ocaml.doc "A collection of associated service names."];
      type_: String_.t option
        [@ocaml.doc "The type associated to the service."];
      accountId: String_.t option
        [@ocaml.doc "The account ID associated to the service."];
      entityPath: FaultRootCauseEntityPath.t option
        [@ocaml.doc "The path of root cause entities found on the service."];
      inferred: NullableBoolean.t option
        [@ocaml.doc
          "A Boolean value indicating if the service is inferred from the trace."]}
    let make ?name =
      fun ?names ->
        fun ?type_ ->
          fun ?accountId ->
            fun ?entityPath ->
              fun ?inferred ->
                fun () ->
                  { name; names; type_; accountId; entityPath; inferred }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Names", (Option.map x.names ~f:ServiceNames.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("AccountId", (Option.map x.accountId ~f:String_.to_value));
        ("EntityPath",
          (Option.map x.entityPath ~f:FaultRootCauseEntityPath.to_value));
        ("Inferred", (Option.map x.inferred ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inferred =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "Inferred") in
      let entityPath =
        (Option.map ~f:FaultRootCauseEntityPath.of_xml)
          (Xml.child xml_arg0 "EntityPath") in
      let accountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccountId") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let names =
        (Option.map ~f:ServiceNames.of_xml) (Xml.child xml_arg0 "Names") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?inferred ?entityPath ?accountId ?type_ ?names ?name ()
    let of_json json =
      let inferred = field_map json "Inferred" NullableBoolean.of_json in
      let entityPath =
        field_map json "EntityPath" FaultRootCauseEntityPath.of_json in
      let accountId = field_map json "AccountId" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let names = field_map json "Names" ServiceNames.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?inferred ?entityPath ?accountId ?type_ ?names ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A collection of fields identifying the services in a trace summary fault."]
module ResponseTimeRootCauseService =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The service name."];
      names: ServiceNames.t option
        [@ocaml.doc "A collection of associated service names."];
      type_: String_.t option
        [@ocaml.doc "The type associated to the service."];
      accountId: String_.t option
        [@ocaml.doc "The account ID associated to the service."];
      entityPath: ResponseTimeRootCauseEntityPath.t option
        [@ocaml.doc "The path of root cause entities found on the service."];
      inferred: NullableBoolean.t option
        [@ocaml.doc
          "A Boolean value indicating if the service is inferred from the trace."]}
    let make ?name =
      fun ?names ->
        fun ?type_ ->
          fun ?accountId ->
            fun ?entityPath ->
              fun ?inferred ->
                fun () ->
                  { name; names; type_; accountId; entityPath; inferred }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Names", (Option.map x.names ~f:ServiceNames.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("AccountId", (Option.map x.accountId ~f:String_.to_value));
        ("EntityPath",
          (Option.map x.entityPath
             ~f:ResponseTimeRootCauseEntityPath.to_value));
        ("Inferred", (Option.map x.inferred ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inferred =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "Inferred") in
      let entityPath =
        (Option.map ~f:ResponseTimeRootCauseEntityPath.of_xml)
          (Xml.child xml_arg0 "EntityPath") in
      let accountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccountId") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let names =
        (Option.map ~f:ServiceNames.of_xml) (Xml.child xml_arg0 "Names") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?inferred ?entityPath ?accountId ?type_ ?names ?name ()
    let of_json json =
      let inferred = field_map json "Inferred" NullableBoolean.of_json in
      let entityPath =
        field_map json "EntityPath" ResponseTimeRootCauseEntityPath.of_json in
      let accountId = field_map json "AccountId" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let names = field_map json "Names" ServiceNames.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?inferred ?entityPath ?accountId ?type_ ?names ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A collection of fields identifying the service in a response time warning."]
module Alias =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The canonical name of the alias."];
      names: AliasNames.t option
        [@ocaml.doc
          "A list of names for the alias, including the canonical name."];
      type_: String_.t option [@ocaml.doc "The type of the alias."]}
    let make ?name =
      fun ?names -> fun ?type_ -> fun () -> { name; names; type_ }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Names", (Option.map x.names ~f:AliasNames.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let names =
        (Option.map ~f:AliasNames.of_xml) (Xml.child xml_arg0 "Names") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?type_ ?names ?name ()
    let of_json json =
      let type_ = field_map json "Type" String_.of_json in
      let names = field_map json "Names" AliasNames.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?type_ ?names ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An alias for an edge."]
module ErrorStatistics =
  struct
    type nonrec t =
      {
      throttleCount: NullableLong.t option
        [@ocaml.doc
          "The number of requests that failed with a 419 throttling status code."];
      otherCount: NullableLong.t option
        [@ocaml.doc
          "The number of requests that failed with untracked 4xx Client Error status codes."];
      totalCount: NullableLong.t option
        [@ocaml.doc
          "The total number of requests that failed with a 4xx Client Error status code."]}
    let make ?throttleCount =
      fun ?otherCount ->
        fun ?totalCount ->
          fun () -> { throttleCount; otherCount; totalCount }
    let to_value x =
      structure_to_value
        [("ThrottleCount",
           (Option.map x.throttleCount ~f:NullableLong.to_value));
        ("OtherCount", (Option.map x.otherCount ~f:NullableLong.to_value));
        ("TotalCount", (Option.map x.totalCount ~f:NullableLong.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "TotalCount") in
      let otherCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "OtherCount") in
      let throttleCount =
        (Option.map ~f:NullableLong.of_xml)
          (Xml.child xml_arg0 "ThrottleCount") in
      make ?totalCount ?otherCount ?throttleCount ()
    let of_json json =
      let totalCount = field_map json "TotalCount" NullableLong.of_json in
      let otherCount = field_map json "OtherCount" NullableLong.of_json in
      let throttleCount = field_map json "ThrottleCount" NullableLong.of_json in
      make ?totalCount ?otherCount ?throttleCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about requests that failed with a 4xx Client Error status code."]
module FaultStatistics =
  struct
    type nonrec t =
      {
      otherCount: NullableLong.t option
        [@ocaml.doc
          "The number of requests that failed with untracked 5xx Server Error status codes."];
      totalCount: NullableLong.t option
        [@ocaml.doc
          "The total number of requests that failed with a 5xx Server Error status code."]}
    let make ?otherCount =
      fun ?totalCount -> fun () -> { otherCount; totalCount }
    let to_value x =
      structure_to_value
        [("OtherCount", (Option.map x.otherCount ~f:NullableLong.to_value));
        ("TotalCount", (Option.map x.totalCount ~f:NullableLong.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "TotalCount") in
      let otherCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "OtherCount") in
      make ?totalCount ?otherCount ()
    let of_json json =
      let totalCount = field_map json "TotalCount" NullableLong.of_json in
      let otherCount = field_map json "OtherCount" NullableLong.of_json in
      make ?totalCount ?otherCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about requests that failed with a 5xx Server Error status code."]
module HistogramEntry =
  struct
    type nonrec t =
      {
      value: Double.t option [@ocaml.doc "The value of the entry."];
      count: Integer.t option [@ocaml.doc "The prevalence of the entry."]}
    let make ?value = fun ?count -> fun () -> { value; count }
    let to_value x =
      structure_to_value
        [("Value", (Option.map x.value ~f:Double.to_value));
        ("Count", (Option.map x.count ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let count = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Count") in
      let value = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "Value") in
      make ?count ?value ()
    let of_json json =
      let count = field_map json "Count" Integer.of_json in
      let value = field_map json "Value" Double.of_json in
      make ?count ?value ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An entry in a histogram for a statistic. A histogram maps the range of observed values on the X axis, and the prevalence of each value on the Y axis."]
module ValueWithServiceIds =
  struct
    type nonrec t =
      {
      annotationValue: AnnotationValue.t option
        [@ocaml.doc "Values of the annotation."];
      serviceIds: ServiceIds.t option
        [@ocaml.doc "Services to which the annotation applies."]}
    let make ?annotationValue =
      fun ?serviceIds -> fun () -> { annotationValue; serviceIds }
    let to_value x =
      structure_to_value
        [("AnnotationValue",
           (Option.map x.annotationValue ~f:AnnotationValue.to_value));
        ("ServiceIds", (Option.map x.serviceIds ~f:ServiceIds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceIds =
        (Option.map ~f:ServiceIds.of_xml) (Xml.child xml_arg0 "ServiceIds") in
      let annotationValue =
        (Option.map ~f:AnnotationValue.of_xml)
          (Xml.child xml_arg0 "AnnotationValue") in
      make ?serviceIds ?annotationValue ()
    let of_json json =
      let serviceIds = field_map json "ServiceIds" ServiceIds.of_json in
      let annotationValue =
        field_map json "AnnotationValue" AnnotationValue.of_json in
      make ?serviceIds ?annotationValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a segment annotation."]
module ErrorRootCauseServices =
  struct
    type nonrec t = ErrorRootCauseService.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ErrorRootCauseService.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ErrorRootCauseService.of_xml)
    let of_json j =
      list_of_json ~kind:"ErrorRootCauseServices"
        ~of_json:ErrorRootCauseService.of_json j
    let to_json v = composed_to_json to_value v
  end
module FaultRootCauseServices =
  struct
    type nonrec t = FaultRootCauseService.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FaultRootCauseService.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FaultRootCauseService.of_xml)
    let of_json j =
      list_of_json ~kind:"FaultRootCauseServices"
        ~of_json:FaultRootCauseService.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResponseTimeRootCauseServices =
  struct
    type nonrec t = ResponseTimeRootCauseService.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResponseTimeRootCauseService.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResponseTimeRootCauseService.of_xml)
    let of_json j =
      list_of_json ~kind:"ResponseTimeRootCauseServices"
        ~of_json:ResponseTimeRootCauseService.of_json j
    let to_json v = composed_to_json to_value v
  end
module AliasList =
  struct
    type nonrec t = Alias.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Alias.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Alias.of_xml)
    let of_json j = list_of_json ~kind:"AliasList" ~of_json:Alias.of_json j
    let to_json v = composed_to_json to_value v
  end
module EdgeStatistics =
  struct
    type nonrec t =
      {
      okCount: NullableLong.t option
        [@ocaml.doc
          "The number of requests that completed with a 2xx Success status code."];
      errorStatistics: ErrorStatistics.t option
        [@ocaml.doc
          "Information about requests that failed with a 4xx Client Error status code."];
      faultStatistics: FaultStatistics.t option
        [@ocaml.doc
          "Information about requests that failed with a 5xx Server Error status code."];
      totalCount: NullableLong.t option
        [@ocaml.doc "The total number of completed requests."];
      totalResponseTime: NullableDouble.t option
        [@ocaml.doc "The aggregate response time of completed requests."]}
    let make ?okCount =
      fun ?errorStatistics ->
        fun ?faultStatistics ->
          fun ?totalCount ->
            fun ?totalResponseTime ->
              fun () ->
                {
                  okCount;
                  errorStatistics;
                  faultStatistics;
                  totalCount;
                  totalResponseTime
                }
    let to_value x =
      structure_to_value
        [("OkCount", (Option.map x.okCount ~f:NullableLong.to_value));
        ("ErrorStatistics",
          (Option.map x.errorStatistics ~f:ErrorStatistics.to_value));
        ("FaultStatistics",
          (Option.map x.faultStatistics ~f:FaultStatistics.to_value));
        ("TotalCount", (Option.map x.totalCount ~f:NullableLong.to_value));
        ("TotalResponseTime",
          (Option.map x.totalResponseTime ~f:NullableDouble.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalResponseTime =
        (Option.map ~f:NullableDouble.of_xml)
          (Xml.child xml_arg0 "TotalResponseTime") in
      let totalCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "TotalCount") in
      let faultStatistics =
        (Option.map ~f:FaultStatistics.of_xml)
          (Xml.child xml_arg0 "FaultStatistics") in
      let errorStatistics =
        (Option.map ~f:ErrorStatistics.of_xml)
          (Xml.child xml_arg0 "ErrorStatistics") in
      let okCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "OkCount") in
      make ?totalResponseTime ?totalCount ?faultStatistics ?errorStatistics
        ?okCount ()
    let of_json json =
      let totalResponseTime =
        field_map json "TotalResponseTime" NullableDouble.of_json in
      let totalCount = field_map json "TotalCount" NullableLong.of_json in
      let faultStatistics =
        field_map json "FaultStatistics" FaultStatistics.of_json in
      let errorStatistics =
        field_map json "ErrorStatistics" ErrorStatistics.of_json in
      let okCount = field_map json "OkCount" NullableLong.of_json in
      make ?totalResponseTime ?totalCount ?faultStatistics ?errorStatistics
        ?okCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Response statistics for an edge."]
module Histogram =
  struct
    type nonrec t = HistogramEntry.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:HistogramEntry.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HistogramEntry.of_xml)
    let of_json j =
      list_of_json ~kind:"Histogram" ~of_json:HistogramEntry.of_json j
    let to_json v = composed_to_json to_value v
  end
module NullableInteger =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for NullableInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module AttributeKey =
  struct
    type nonrec t = string
    let context_ = "AttributeKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeKey" j
    let to_json = simple_to_json to_value
  end
module AttributeValue =
  struct
    type nonrec t = string
    let context_ = "AttributeValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeValue" j
    let to_json = simple_to_json to_value
  end
module SegmentDocument =
  struct
    type nonrec t = string
    let context_ = "SegmentDocument"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SegmentDocument" j
    let to_json = simple_to_json to_value
  end
module SegmentId =
  struct
    type nonrec t = string
    let context_ = "SegmentId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SegmentId" j
    let to_json = simple_to_json to_value
  end
module AnnotationKey =
  struct
    type nonrec t = string
    let context_ = "AnnotationKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AnnotationKey" j
    let to_json = simple_to_json to_value
  end
module ValuesWithServiceIds =
  struct
    type nonrec t = ValueWithServiceIds.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ValueWithServiceIds.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValueWithServiceIds.of_xml)
    let of_json j =
      list_of_json ~kind:"ValuesWithServiceIds"
        ~of_json:ValueWithServiceIds.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorRootCause =
  struct
    type nonrec t =
      {
      services: ErrorRootCauseServices.t option
        [@ocaml.doc
          "A list of services corresponding to an error. A service identifies a segment and it contains a name, account ID, type, and inferred flag."];
      clientImpacting: NullableBoolean.t option
        [@ocaml.doc
          "A flag that denotes that the root cause impacts the trace client."]}
    let make ?services =
      fun ?clientImpacting -> fun () -> { services; clientImpacting }
    let to_value x =
      structure_to_value
        [("Services",
           (Option.map x.services ~f:ErrorRootCauseServices.to_value));
        ("ClientImpacting",
          (Option.map x.clientImpacting ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientImpacting =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "ClientImpacting") in
      let services =
        (Option.map ~f:ErrorRootCauseServices.of_xml)
          (Xml.child xml_arg0 "Services") in
      make ?clientImpacting ?services ()
    let of_json json =
      let clientImpacting =
        field_map json "ClientImpacting" NullableBoolean.of_json in
      let services = field_map json "Services" ErrorRootCauseServices.of_json in
      make ?clientImpacting ?services ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The root cause of a trace summary error."]
module FaultRootCause =
  struct
    type nonrec t =
      {
      services: FaultRootCauseServices.t option
        [@ocaml.doc
          "A list of corresponding services. A service identifies a segment and it contains a name, account ID, type, and inferred flag."];
      clientImpacting: NullableBoolean.t option
        [@ocaml.doc
          "A flag that denotes that the root cause impacts the trace client."]}
    let make ?services =
      fun ?clientImpacting -> fun () -> { services; clientImpacting }
    let to_value x =
      structure_to_value
        [("Services",
           (Option.map x.services ~f:FaultRootCauseServices.to_value));
        ("ClientImpacting",
          (Option.map x.clientImpacting ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientImpacting =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "ClientImpacting") in
      let services =
        (Option.map ~f:FaultRootCauseServices.of_xml)
          (Xml.child xml_arg0 "Services") in
      make ?clientImpacting ?services ()
    let of_json json =
      let clientImpacting =
        field_map json "ClientImpacting" NullableBoolean.of_json in
      let services = field_map json "Services" FaultRootCauseServices.of_json in
      make ?clientImpacting ?services ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The root cause information for a trace summary fault."]
module ResponseTimeRootCause =
  struct
    type nonrec t =
      {
      services: ResponseTimeRootCauseServices.t option
        [@ocaml.doc
          "A list of corresponding services. A service identifies a segment and contains a name, account ID, type, and inferred flag."];
      clientImpacting: NullableBoolean.t option
        [@ocaml.doc
          "A flag that denotes that the root cause impacts the trace client."]}
    let make ?services =
      fun ?clientImpacting -> fun () -> { services; clientImpacting }
    let to_value x =
      structure_to_value
        [("Services",
           (Option.map x.services ~f:ResponseTimeRootCauseServices.to_value));
        ("ClientImpacting",
          (Option.map x.clientImpacting ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientImpacting =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "ClientImpacting") in
      let services =
        (Option.map ~f:ResponseTimeRootCauseServices.of_xml)
          (Xml.child xml_arg0 "Services") in
      make ?clientImpacting ?services ()
    let of_json json =
      let clientImpacting =
        field_map json "ClientImpacting" NullableBoolean.of_json in
      let services =
        field_map json "Services" ResponseTimeRootCauseServices.of_json in
      make ?clientImpacting ?services ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The root cause information for a response time warning."]
module AvailabilityZoneDetail =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc "The name of a corresponding Availability Zone."]}
    let make ?name = fun () -> { name }
    let to_value x =
      structure_to_value [("Name", (Option.map x.name ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?name ()
    let of_json json =
      let name = field_map json "Name" String_.of_json in make ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of Availability Zones corresponding to the segments in a trace."]
module InstanceIdDetail =
  struct
    type nonrec t =
      {
      id: String_.t option
        [@ocaml.doc "The ID of a corresponding EC2 instance."]}
    let make ?id = fun () -> { id }
    let to_value x =
      structure_to_value [("Id", (Option.map x.id ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Id") in
      make ?id ()
    let of_json json =
      let id = field_map json "Id" String_.of_json in make ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of EC2 instance IDs corresponding to the segments in a trace."]
module ResourceARNDetail =
  struct
    type nonrec t =
      {
      aRN: String_.t option
        [@ocaml.doc "The ARN of a corresponding resource."]}
    let make ?aRN = fun () -> { aRN }
    let to_value x =
      structure_to_value [("ARN", (Option.map x.aRN ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let aRN = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ARN") in
      make ?aRN ()
    let of_json json =
      let aRN = field_map json "ARN" String_.of_json in make ?aRN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of resources ARNs corresponding to the segments in a trace."]
module TraceUser =
  struct
    type nonrec t =
      {
      userName: String_.t option [@ocaml.doc "The user's name."];
      serviceIds: ServiceIds.t option
        [@ocaml.doc "Services that the user's request hit."]}
    let make ?userName =
      fun ?serviceIds -> fun () -> { userName; serviceIds }
    let to_value x =
      structure_to_value
        [("UserName", (Option.map x.userName ~f:String_.to_value));
        ("ServiceIds", (Option.map x.serviceIds ~f:ServiceIds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceIds =
        (Option.map ~f:ServiceIds.of_xml) (Xml.child xml_arg0 "ServiceIds") in
      let userName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "UserName") in
      make ?serviceIds ?userName ()
    let of_json json =
      let serviceIds = field_map json "ServiceIds" ServiceIds.of_json in
      let userName = field_map json "UserName" String_.of_json in
      make ?serviceIds ?userName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a user recorded in segment documents."]
module Edge =
  struct
    type nonrec t =
      {
      referenceId: NullableInteger.t option
        [@ocaml.doc "Identifier of the edge. Unique within a service map."];
      startTime: Timestamp.t option
        [@ocaml.doc "The start time of the first segment on the edge."];
      endTime: Timestamp.t option
        [@ocaml.doc "The end time of the last segment on the edge."];
      summaryStatistics: EdgeStatistics.t option
        [@ocaml.doc "Response statistics for segments on the edge."];
      responseTimeHistogram: Histogram.t option
        [@ocaml.doc
          "A histogram that maps the spread of client response times on an edge."];
      aliases: AliasList.t option [@ocaml.doc "Aliases for the edge."]}
    let make ?referenceId =
      fun ?startTime ->
        fun ?endTime ->
          fun ?summaryStatistics ->
            fun ?responseTimeHistogram ->
              fun ?aliases ->
                fun () ->
                  {
                    referenceId;
                    startTime;
                    endTime;
                    summaryStatistics;
                    responseTimeHistogram;
                    aliases
                  }
    let to_value x =
      structure_to_value
        [("ReferenceId",
           (Option.map x.referenceId ~f:NullableInteger.to_value));
        ("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("SummaryStatistics",
          (Option.map x.summaryStatistics ~f:EdgeStatistics.to_value));
        ("ResponseTimeHistogram",
          (Option.map x.responseTimeHistogram ~f:Histogram.to_value));
        ("Aliases", (Option.map x.aliases ~f:AliasList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let aliases =
        (Option.map ~f:AliasList.of_xml) (Xml.child xml_arg0 "Aliases") in
      let responseTimeHistogram =
        (Option.map ~f:Histogram.of_xml)
          (Xml.child xml_arg0 "ResponseTimeHistogram") in
      let summaryStatistics =
        (Option.map ~f:EdgeStatistics.of_xml)
          (Xml.child xml_arg0 "SummaryStatistics") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let referenceId =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "ReferenceId") in
      make ?aliases ?responseTimeHistogram ?summaryStatistics ?endTime
        ?startTime ?referenceId ()
    let of_json json =
      let aliases = field_map json "Aliases" AliasList.of_json in
      let responseTimeHistogram =
        field_map json "ResponseTimeHistogram" Histogram.of_json in
      let summaryStatistics =
        field_map json "SummaryStatistics" EdgeStatistics.of_json in
      let endTime = field_map json "EndTime" Timestamp.of_json in
      let startTime = field_map json "StartTime" Timestamp.of_json in
      let referenceId = field_map json "ReferenceId" NullableInteger.of_json in
      make ?aliases ?responseTimeHistogram ?summaryStatistics ?endTime
        ?startTime ?referenceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a connection between two services."]
module AttributeMap =
  struct
    type nonrec t = (AttributeKey.t * AttributeValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:5); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((AttributeKey.of_string chopped),
                              (AttributeValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (AttributeKey.to_value x) |>
                    (fun x ->
                       (AttributeValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:AttributeKey.of_string
        ~of_json:AttributeValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module FixedRate =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:1.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module HTTPMethod =
  struct
    type nonrec t = string
    let context_ = "HTTPMethod"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:10); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HTTPMethod" j
    let to_json = simple_to_json to_value
  end
module Host =
  struct
    type nonrec t = string
    let context_ = "Host"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:64); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Host" j
    let to_json = simple_to_json to_value
  end
module Priority =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:9999) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Priority" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ReservoirSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ReservoirSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ResourceARN =
  struct
    type nonrec t = string
    let context_ = "ResourceARN"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:500); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceARN" j
    let to_json = simple_to_json to_value
  end
module RuleName =
  struct
    type nonrec t = string
    let context_ = "RuleName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RuleName" j
    let to_json = simple_to_json to_value
  end
module ServiceName =
  struct
    type nonrec t = string
    let context_ = "ServiceName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:64); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceName" j
    let to_json = simple_to_json to_value
  end
module ServiceType =
  struct
    type nonrec t = string
    let context_ = "ServiceType"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:64); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceType" j
    let to_json = simple_to_json to_value
  end
module URLPath =
  struct
    type nonrec t = string
    let context_ = "URLPath"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:128); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"URLPath" j
    let to_json = simple_to_json to_value
  end
module Version =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Version" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AnomalousService =
  struct
    type nonrec t = {
      serviceId: ServiceId.t option }
    let make ?serviceId = fun () -> { serviceId }
    let to_value x =
      structure_to_value
        [("ServiceId", (Option.map x.serviceId ~f:ServiceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceId =
        (Option.map ~f:ServiceId.of_xml) (Xml.child xml_arg0 "ServiceId") in
      make ?serviceId ()
    let of_json json =
      let serviceId = field_map json "ServiceId" ServiceId.of_json in
      make ?serviceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The service within the service graph that has anomalously high fault rates."]
module InsightCategory =
  struct
    type nonrec t =
      | FAULT 
      | Non_static_id of string 
    let make i = i
    let to_string = function | FAULT -> "FAULT" | Non_static_id s -> s
    let of_string = function | "FAULT" -> FAULT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InsightCategory" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InsightCategory" j)
    let to_json = simple_to_json to_value
  end
module InsightImpactGraphEdge =
  struct
    type nonrec t =
      {
      referenceId: NullableInteger.t option
        [@ocaml.doc "Identifier of the edge. Unique within a service map."]}
    let make ?referenceId = fun () -> { referenceId }
    let to_value x =
      structure_to_value
        [("ReferenceId",
           (Option.map x.referenceId ~f:NullableInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let referenceId =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "ReferenceId") in
      make ?referenceId ()
    let of_json json =
      let referenceId = field_map json "ReferenceId" NullableInteger.of_json in
      make ?referenceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The connection between two service in an insight impact graph."]
module Segment =
  struct
    type nonrec t =
      {
      id: SegmentId.t option [@ocaml.doc "The segment's ID."];
      document: SegmentDocument.t option [@ocaml.doc "The segment document."]}
    let make ?id = fun ?document -> fun () -> { id; document }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:SegmentId.to_value));
        ("Document", (Option.map x.document ~f:SegmentDocument.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let document =
        (Option.map ~f:SegmentDocument.of_xml)
          (Xml.child xml_arg0 "Document") in
      let id = (Option.map ~f:SegmentId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?document ?id ()
    let of_json json =
      let document = field_map json "Document" SegmentDocument.of_json in
      let id = field_map json "Id" SegmentId.of_json in make ?document ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A segment from a trace that has been ingested by the X-Ray service. The segment can be compiled from documents uploaded with PutTraceSegments, or an inferred segment for a downstream service, generated from a subsegment sent by the service that called it. For the full segment document schema, see Amazon Web Services X-Ray Segment Documents in the Amazon Web Services X-Ray Developer Guide."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module BackendConnectionErrors =
  struct
    type nonrec t =
      {
      timeoutCount: NullableInteger.t option ;
      connectionRefusedCount: NullableInteger.t option ;
      hTTPCode4XXCount: NullableInteger.t option ;
      hTTPCode5XXCount: NullableInteger.t option ;
      unknownHostCount: NullableInteger.t option ;
      otherCount: NullableInteger.t option }
    let make ?timeoutCount =
      fun ?connectionRefusedCount ->
        fun ?hTTPCode4XXCount ->
          fun ?hTTPCode5XXCount ->
            fun ?unknownHostCount ->
              fun ?otherCount ->
                fun () ->
                  {
                    timeoutCount;
                    connectionRefusedCount;
                    hTTPCode4XXCount;
                    hTTPCode5XXCount;
                    unknownHostCount;
                    otherCount
                  }
    let to_value x =
      structure_to_value
        [("TimeoutCount",
           (Option.map x.timeoutCount ~f:NullableInteger.to_value));
        ("ConnectionRefusedCount",
          (Option.map x.connectionRefusedCount ~f:NullableInteger.to_value));
        ("HTTPCode4XXCount",
          (Option.map x.hTTPCode4XXCount ~f:NullableInteger.to_value));
        ("HTTPCode5XXCount",
          (Option.map x.hTTPCode5XXCount ~f:NullableInteger.to_value));
        ("UnknownHostCount",
          (Option.map x.unknownHostCount ~f:NullableInteger.to_value));
        ("OtherCount", (Option.map x.otherCount ~f:NullableInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let otherCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "OtherCount") in
      let unknownHostCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "UnknownHostCount") in
      let hTTPCode5XXCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "HTTPCode5XXCount") in
      let hTTPCode4XXCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "HTTPCode4XXCount") in
      let connectionRefusedCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "ConnectionRefusedCount") in
      let timeoutCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "TimeoutCount") in
      make ?otherCount ?unknownHostCount ?hTTPCode5XXCount ?hTTPCode4XXCount
        ?connectionRefusedCount ?timeoutCount ()
    let of_json json =
      let otherCount = field_map json "OtherCount" NullableInteger.of_json in
      let unknownHostCount =
        field_map json "UnknownHostCount" NullableInteger.of_json in
      let hTTPCode5XXCount =
        field_map json "HTTPCode5XXCount" NullableInteger.of_json in
      let hTTPCode4XXCount =
        field_map json "HTTPCode4XXCount" NullableInteger.of_json in
      let connectionRefusedCount =
        field_map json "ConnectionRefusedCount" NullableInteger.of_json in
      let timeoutCount =
        field_map json "TimeoutCount" NullableInteger.of_json in
      make ?otherCount ?unknownHostCount ?hTTPCode5XXCount ?hTTPCode4XXCount
        ?connectionRefusedCount ?timeoutCount ()
    let to_json v = composed_to_json to_value v
  end
module Annotations =
  struct
    type nonrec t = (AnnotationKey.t * ValuesWithServiceIds.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types AnnotationKey ValuesWithServiceIds"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (AnnotationKey.to_value x) |>
                    (fun x ->
                       (ValuesWithServiceIds.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:AnnotationKey.of_string
        ~of_json:ValuesWithServiceIds.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorRootCauses =
  struct
    type nonrec t = ErrorRootCause.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ErrorRootCause.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ErrorRootCause.of_xml)
    let of_json j =
      list_of_json ~kind:"ErrorRootCauses" ~of_json:ErrorRootCause.of_json j
    let to_json v = composed_to_json to_value v
  end
module FaultRootCauses =
  struct
    type nonrec t = FaultRootCause.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FaultRootCause.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FaultRootCause.of_xml)
    let of_json j =
      list_of_json ~kind:"FaultRootCauses" ~of_json:FaultRootCause.of_json j
    let to_json v = composed_to_json to_value v
  end
module Http =
  struct
    type nonrec t =
      {
      httpURL: String_.t option [@ocaml.doc "The request URL."];
      httpStatus: NullableInteger.t option
        [@ocaml.doc "The response status."];
      httpMethod: String_.t option [@ocaml.doc "The request method."];
      userAgent: String_.t option
        [@ocaml.doc "The request's user agent string."];
      clientIp: String_.t option
        [@ocaml.doc "The IP address of the requestor."]}
    let make ?httpURL =
      fun ?httpStatus ->
        fun ?httpMethod ->
          fun ?userAgent ->
            fun ?clientIp ->
              fun () ->
                { httpURL; httpStatus; httpMethod; userAgent; clientIp }
    let to_value x =
      structure_to_value
        [("HttpURL", (Option.map x.httpURL ~f:String_.to_value));
        ("HttpStatus", (Option.map x.httpStatus ~f:NullableInteger.to_value));
        ("HttpMethod", (Option.map x.httpMethod ~f:String_.to_value));
        ("UserAgent", (Option.map x.userAgent ~f:String_.to_value));
        ("ClientIp", (Option.map x.clientIp ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientIp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ClientIp") in
      let userAgent =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "UserAgent") in
      let httpMethod =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "HttpMethod") in
      let httpStatus =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "HttpStatus") in
      let httpURL =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "HttpURL") in
      make ?clientIp ?userAgent ?httpMethod ?httpStatus ?httpURL ()
    let of_json json =
      let clientIp = field_map json "ClientIp" String_.of_json in
      let userAgent = field_map json "UserAgent" String_.of_json in
      let httpMethod = field_map json "HttpMethod" String_.of_json in
      let httpStatus = field_map json "HttpStatus" NullableInteger.of_json in
      let httpURL = field_map json "HttpURL" String_.of_json in
      make ?clientIp ?userAgent ?httpMethod ?httpStatus ?httpURL ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about an HTTP request."]
module ResponseTimeRootCauses =
  struct
    type nonrec t = ResponseTimeRootCause.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResponseTimeRootCause.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResponseTimeRootCause.of_xml)
    let of_json j =
      list_of_json ~kind:"ResponseTimeRootCauses"
        ~of_json:ResponseTimeRootCause.of_json j
    let to_json v = composed_to_json to_value v
  end
module TraceAvailabilityZones =
  struct
    type nonrec t = AvailabilityZoneDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AvailabilityZoneDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AvailabilityZoneDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"TraceAvailabilityZones"
        ~of_json:AvailabilityZoneDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module TraceId =
  struct
    type nonrec t = string
    let context_ = "TraceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:35) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TraceId" j
    let to_json = simple_to_json to_value
  end
module TraceInstanceIds =
  struct
    type nonrec t = InstanceIdDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceIdDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceIdDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"TraceInstanceIds" ~of_json:InstanceIdDetail.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module TraceResourceARNs =
  struct
    type nonrec t = ResourceARNDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceARNDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceARNDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"TraceResourceARNs"
        ~of_json:ResourceARNDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module TraceUsers =
  struct
    type nonrec t = TraceUser.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TraceUser.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TraceUser.of_xml)
    let of_json j =
      list_of_json ~kind:"TraceUsers" ~of_json:TraceUser.of_json j
    let to_json v = composed_to_json to_value v
  end
module EdgeList =
  struct
    type nonrec t = Edge.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Edge.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Edge.of_xml)
    let of_json j = list_of_json ~kind:"EdgeList" ~of_json:Edge.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceStatistics =
  struct
    type nonrec t =
      {
      okCount: NullableLong.t option
        [@ocaml.doc
          "The number of requests that completed with a 2xx Success status code."];
      errorStatistics: ErrorStatistics.t option
        [@ocaml.doc
          "Information about requests that failed with a 4xx Client Error status code."];
      faultStatistics: FaultStatistics.t option
        [@ocaml.doc
          "Information about requests that failed with a 5xx Server Error status code."];
      totalCount: NullableLong.t option
        [@ocaml.doc "The total number of completed requests."];
      totalResponseTime: NullableDouble.t option
        [@ocaml.doc "The aggregate response time of completed requests."]}
    let make ?okCount =
      fun ?errorStatistics ->
        fun ?faultStatistics ->
          fun ?totalCount ->
            fun ?totalResponseTime ->
              fun () ->
                {
                  okCount;
                  errorStatistics;
                  faultStatistics;
                  totalCount;
                  totalResponseTime
                }
    let to_value x =
      structure_to_value
        [("OkCount", (Option.map x.okCount ~f:NullableLong.to_value));
        ("ErrorStatistics",
          (Option.map x.errorStatistics ~f:ErrorStatistics.to_value));
        ("FaultStatistics",
          (Option.map x.faultStatistics ~f:FaultStatistics.to_value));
        ("TotalCount", (Option.map x.totalCount ~f:NullableLong.to_value));
        ("TotalResponseTime",
          (Option.map x.totalResponseTime ~f:NullableDouble.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalResponseTime =
        (Option.map ~f:NullableDouble.of_xml)
          (Xml.child xml_arg0 "TotalResponseTime") in
      let totalCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "TotalCount") in
      let faultStatistics =
        (Option.map ~f:FaultStatistics.of_xml)
          (Xml.child xml_arg0 "FaultStatistics") in
      let errorStatistics =
        (Option.map ~f:ErrorStatistics.of_xml)
          (Xml.child xml_arg0 "ErrorStatistics") in
      let okCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "OkCount") in
      make ?totalResponseTime ?totalCount ?faultStatistics ?errorStatistics
        ?okCount ()
    let of_json json =
      let totalResponseTime =
        field_map json "TotalResponseTime" NullableDouble.of_json in
      let totalCount = field_map json "TotalCount" NullableLong.of_json in
      let faultStatistics =
        field_map json "FaultStatistics" FaultStatistics.of_json in
      let errorStatistics =
        field_map json "ErrorStatistics" ErrorStatistics.of_json in
      let okCount = field_map json "OkCount" NullableLong.of_json in
      make ?totalResponseTime ?totalCount ?faultStatistics ?errorStatistics
        ?okCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Response statistics for a service."]
module ForecastStatistics =
  struct
    type nonrec t =
      {
      faultCountHigh: NullableLong.t option
        [@ocaml.doc "The upper limit of fault counts for a service."];
      faultCountLow: NullableLong.t option
        [@ocaml.doc "The lower limit of fault counts for a service."]}
    let make ?faultCountHigh =
      fun ?faultCountLow -> fun () -> { faultCountHigh; faultCountLow }
    let to_value x =
      structure_to_value
        [("FaultCountHigh",
           (Option.map x.faultCountHigh ~f:NullableLong.to_value));
        ("FaultCountLow",
          (Option.map x.faultCountLow ~f:NullableLong.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let faultCountLow =
        (Option.map ~f:NullableLong.of_xml)
          (Xml.child xml_arg0 "FaultCountLow") in
      let faultCountHigh =
        (Option.map ~f:NullableLong.of_xml)
          (Xml.child xml_arg0 "FaultCountHigh") in
      make ?faultCountLow ?faultCountHigh ()
    let of_json json =
      let faultCountLow = field_map json "FaultCountLow" NullableLong.of_json in
      let faultCountHigh =
        field_map json "FaultCountHigh" NullableLong.of_json in
      make ?faultCountLow ?faultCountHigh ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The predicted high and low fault count. This is used to determine if a service has become anomalous and if an insight should be created."]
module BorrowCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BorrowCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ClientID =
  struct
    type nonrec t = string
    let context_ = "ClientID"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:24) >>=
             (fun () -> check_string_min i ~min:24));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientID" j
    let to_json = simple_to_json to_value
  end
module RequestCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for RequestCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SampledCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SampledCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SamplingRule =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t option
        [@ocaml.doc
          "The name of the sampling rule. Specify a rule by either name or ARN, but not both."];
      ruleARN: String_.t option
        [@ocaml.doc
          "The ARN of the sampling rule. Specify a rule by either name or ARN, but not both."];
      resourceARN: ResourceARN.t
        [@ocaml.doc
          "Matches the ARN of the Amazon Web Services resource on which the service runs."];
      priority: Priority.t [@ocaml.doc "The priority of the sampling rule."];
      fixedRate: FixedRate.t
        [@ocaml.doc
          "The percentage of matching requests to instrument, after the reservoir is exhausted."];
      reservoirSize: ReservoirSize.t
        [@ocaml.doc
          "A fixed number of matching requests to instrument per second, prior to applying the fixed rate. The reservoir is not used directly by services, but applies to all services using the rule collectively."];
      serviceName: ServiceName.t
        [@ocaml.doc
          "Matches the name that the service uses to identify itself in segments."];
      serviceType: ServiceType.t
        [@ocaml.doc
          "Matches the origin that the service uses to identify its type in segments."];
      host: Host.t [@ocaml.doc "Matches the hostname from a request URL."];
      hTTPMethod: HTTPMethod.t
        [@ocaml.doc "Matches the HTTP method of a request."];
      uRLPath: URLPath.t [@ocaml.doc "Matches the path from a request URL."];
      version: Version.t
        [@ocaml.doc "The version of the sampling rule format (1)."];
      attributes: AttributeMap.t option
        [@ocaml.doc "Matches attributes derived from the request."]}
    let context_ = "SamplingRule"
    let make ?ruleName =
      fun ?ruleARN ->
        fun ?attributes ->
          fun ~resourceARN ->
            fun ~priority ->
              fun ~fixedRate ->
                fun ~reservoirSize ->
                  fun ~serviceName ->
                    fun ~serviceType ->
                      fun ~host ->
                        fun ~hTTPMethod ->
                          fun ~uRLPath ->
                            fun ~version ->
                              fun () ->
                                {
                                  ruleName;
                                  ruleARN;
                                  attributes;
                                  resourceARN;
                                  priority;
                                  fixedRate;
                                  reservoirSize;
                                  serviceName;
                                  serviceType;
                                  host;
                                  hTTPMethod;
                                  uRLPath;
                                  version
                                }
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:RuleName.to_value));
        ("RuleARN", (Option.map x.ruleARN ~f:String_.to_value));
        ("ResourceARN", (Some (ResourceARN.to_value x.resourceARN)));
        ("Priority", (Some (Priority.to_value x.priority)));
        ("FixedRate", (Some (FixedRate.to_value x.fixedRate)));
        ("ReservoirSize", (Some (ReservoirSize.to_value x.reservoirSize)));
        ("ServiceName", (Some (ServiceName.to_value x.serviceName)));
        ("ServiceType", (Some (ServiceType.to_value x.serviceType)));
        ("Host", (Some (Host.to_value x.host)));
        ("HTTPMethod", (Some (HTTPMethod.to_value x.hTTPMethod)));
        ("URLPath", (Some (URLPath.to_value x.uRLPath)));
        ("Version", (Some (Version.to_value x.version)));
        ("Attributes", (Option.map x.attributes ~f:AttributeMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        (Option.map ~f:AttributeMap.of_xml) (Xml.child xml_arg0 "Attributes") in
      let version =
        Version.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Version") in
      let uRLPath =
        URLPath.of_xml (Xml.child_exn ~context:context_ xml_arg0 "URLPath") in
      let hTTPMethod =
        HTTPMethod.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HTTPMethod") in
      let host =
        Host.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Host") in
      let serviceType =
        ServiceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceType") in
      let serviceName =
        ServiceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceName") in
      let reservoirSize =
        ReservoirSize.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReservoirSize") in
      let fixedRate =
        FixedRate.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FixedRate") in
      let priority =
        Priority.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Priority") in
      let resourceARN =
        ResourceARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      let ruleARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleARN") in
      let ruleName =
        (Option.map ~f:RuleName.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?attributes ~version ~uRLPath ~hTTPMethod ~host ~serviceType
        ~serviceName ~reservoirSize ~fixedRate ~priority ~resourceARN
        ?ruleARN ?ruleName ()
    let of_json json =
      let attributes = field_map json "Attributes" AttributeMap.of_json in
      let version = field_map_exn json "Version" Version.of_json in
      let uRLPath = field_map_exn json "URLPath" URLPath.of_json in
      let hTTPMethod = field_map_exn json "HTTPMethod" HTTPMethod.of_json in
      let host = field_map_exn json "Host" Host.of_json in
      let serviceType = field_map_exn json "ServiceType" ServiceType.of_json in
      let serviceName = field_map_exn json "ServiceName" ServiceName.of_json in
      let reservoirSize =
        field_map_exn json "ReservoirSize" ReservoirSize.of_json in
      let fixedRate = field_map_exn json "FixedRate" FixedRate.of_json in
      let priority = field_map_exn json "Priority" Priority.of_json in
      let resourceARN = field_map_exn json "ResourceARN" ResourceARN.of_json in
      let ruleARN = field_map json "RuleARN" String_.of_json in
      let ruleName = field_map json "RuleName" RuleName.of_json in
      make ?attributes ~version ~uRLPath ~hTTPMethod ~host ~serviceType
        ~serviceName ~reservoirSize ~fixedRate ~priority ~resourceARN
        ?ruleARN ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A sampling rule that services use to decide whether to instrument a request. Rule fields can match properties of the service, or properties of a request. The service can ignore rules that don't match its properties."]
module AnomalousServiceList =
  struct
    type nonrec t = AnomalousService.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AnomalousService.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AnomalousService.of_xml)
    let of_json j =
      list_of_json ~kind:"AnomalousServiceList"
        ~of_json:AnomalousService.of_json j
    let to_json v = composed_to_json to_value v
  end
module GroupARN =
  struct
    type nonrec t = string
    let context_ = "GroupARN"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:400) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupARN" j
    let to_json = simple_to_json to_value
  end
module GroupName =
  struct
    type nonrec t = string
    let context_ = "GroupName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GroupName" j
    let to_json = simple_to_json to_value
  end
module InsightCategoryList =
  struct
    type nonrec t = InsightCategory.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InsightCategory.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InsightCategory.of_xml)
    let of_json j =
      list_of_json ~kind:"InsightCategoryList"
        ~of_json:InsightCategory.of_json j
    let to_json v = composed_to_json to_value v
  end
module InsightId =
  struct
    type nonrec t = string
    let context_ = "InsightId"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-[1-5][0-9a-fA-F]{3}-[89abAB][0-9a-fA-F]{3}-[0-9a-fA-F]{12}");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InsightId" j
    let to_json = simple_to_json to_value
  end
module InsightState =
  struct
    type nonrec t =
      | ACTIVE 
      | CLOSED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | CLOSED -> "CLOSED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "CLOSED" -> CLOSED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InsightState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InsightState" j)
    let to_json = simple_to_json to_value
  end
module InsightSummaryText =
  struct
    type nonrec t = string
    let context_ = "InsightSummaryText"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InsightSummaryText" j
    let to_json = simple_to_json to_value
  end
module RequestImpactStatistics =
  struct
    type nonrec t =
      {
      faultCount: NullableLong.t option
        [@ocaml.doc "The number of requests that have resulted in a fault,"];
      okCount: NullableLong.t option
        [@ocaml.doc "The number of successful requests."];
      totalCount: NullableLong.t option
        [@ocaml.doc "The total number of requests to the service."]}
    let make ?faultCount =
      fun ?okCount ->
        fun ?totalCount -> fun () -> { faultCount; okCount; totalCount }
    let to_value x =
      structure_to_value
        [("FaultCount", (Option.map x.faultCount ~f:NullableLong.to_value));
        ("OkCount", (Option.map x.okCount ~f:NullableLong.to_value));
        ("TotalCount", (Option.map x.totalCount ~f:NullableLong.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "TotalCount") in
      let okCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "OkCount") in
      let faultCount =
        (Option.map ~f:NullableLong.of_xml) (Xml.child xml_arg0 "FaultCount") in
      make ?totalCount ?okCount ?faultCount ()
    let of_json json =
      let totalCount = field_map json "TotalCount" NullableLong.of_json in
      let okCount = field_map json "OkCount" NullableLong.of_json in
      let faultCount = field_map json "FaultCount" NullableLong.of_json in
      make ?totalCount ?okCount ?faultCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Statistics that describe how the incident has impacted a service."]
module InsightImpactGraphEdgeList =
  struct
    type nonrec t = InsightImpactGraphEdge.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InsightImpactGraphEdge.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InsightImpactGraphEdge.of_xml)
    let of_json j =
      list_of_json ~kind:"InsightImpactGraphEdgeList"
        ~of_json:InsightImpactGraphEdge.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventSummaryText =
  struct
    type nonrec t = string
    let context_ = "EventSummaryText"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EventSummaryText" j
    let to_json = simple_to_json to_value
  end
module InsightsConfiguration =
  struct
    type nonrec t =
      {
      insightsEnabled: NullableBoolean.t option
        [@ocaml.doc
          "Set the InsightsEnabled value to true to enable insights or false to disable insights."];
      notificationsEnabled: NullableBoolean.t option
        [@ocaml.doc
          "Set the NotificationsEnabled value to true to enable insights notifications. Notifications can only be enabled on a group with InsightsEnabled set to true."]}
    let make ?insightsEnabled =
      fun ?notificationsEnabled ->
        fun () -> { insightsEnabled; notificationsEnabled }
    let to_value x =
      structure_to_value
        [("InsightsEnabled",
           (Option.map x.insightsEnabled ~f:NullableBoolean.to_value));
        ("NotificationsEnabled",
          (Option.map x.notificationsEnabled ~f:NullableBoolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationsEnabled =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "NotificationsEnabled") in
      let insightsEnabled =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "InsightsEnabled") in
      make ?notificationsEnabled ?insightsEnabled ()
    let of_json json =
      let notificationsEnabled =
        field_map json "NotificationsEnabled" NullableBoolean.of_json in
      let insightsEnabled =
        field_map json "InsightsEnabled" NullableBoolean.of_json in
      make ?notificationsEnabled ?insightsEnabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The structure containing configurations related to insights."]
module SegmentList =
  struct
    type nonrec t = Segment.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Segment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Segment.of_xml)
    let of_json j =
      list_of_json ~kind:"SegmentList" ~of_json:Segment.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module AmazonResourceName =
  struct
    type nonrec t = string
    let context_ = "AmazonResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1011) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AmazonResourceName" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t
        [@ocaml.doc
          "A tag key, such as Stage or Name. A tag key cannot be empty. The key can be a maximum of 128 characters, and can contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : /"];
      value: TagValue.t
        [@ocaml.doc
          "An optional tag value, such as Production or test-only. The value can be a maximum of 255 characters, and contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : /"]}
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_json json =
      let value = field_map_exn json "Value" TagValue.of_json in
      let key = field_map_exn json "Key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A map that contains tag keys and tag values to attach to an Amazon Web Services X-Ray group or sampling rule. For more information about ways to use tags, see Tagging Amazon Web Services resources in the Amazon Web Services General Reference. The following restrictions apply to tags: Maximum number of user-applied tags per resource: 50 Tag keys and values are case sensitive. Don't use aws: as a prefix for keys; it's reserved for Amazon Web Services use. You cannot edit or delete system tags."]
module UnprocessedTraceSegment =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The segment's ID."];
      errorCode: String_.t option
        [@ocaml.doc "The error that caused processing to fail."];
      message: String_.t option [@ocaml.doc "The error message."]}
    let make ?id =
      fun ?errorCode -> fun ?message -> fun () -> { id; errorCode; message }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:String_.to_value));
        ("ErrorCode", (Option.map x.errorCode ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let errorCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ErrorCode") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Id") in
      make ?message ?errorCode ?id ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let errorCode = field_map json "ErrorCode" String_.of_json in
      let id = field_map json "Id" String_.of_json in
      make ?message ?errorCode ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a segment that failed processing."]
module TraceSegmentDocument =
  struct
    type nonrec t = string
    let context_ = "TraceSegmentDocument"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TraceSegmentDocument" j
    let to_json = simple_to_json to_value
  end
module TelemetryRecord =
  struct
    type nonrec t =
      {
      timestamp: Timestamp.t ;
      segmentsReceivedCount: NullableInteger.t option ;
      segmentsSentCount: NullableInteger.t option ;
      segmentsSpilloverCount: NullableInteger.t option ;
      segmentsRejectedCount: NullableInteger.t option ;
      backendConnectionErrors: BackendConnectionErrors.t option }
    let context_ = "TelemetryRecord"
    let make ?segmentsReceivedCount =
      fun ?segmentsSentCount ->
        fun ?segmentsSpilloverCount ->
          fun ?segmentsRejectedCount ->
            fun ?backendConnectionErrors ->
              fun ~timestamp ->
                fun () ->
                  {
                    segmentsReceivedCount;
                    segmentsSentCount;
                    segmentsSpilloverCount;
                    segmentsRejectedCount;
                    backendConnectionErrors;
                    timestamp
                  }
    let to_value x =
      structure_to_value
        [("Timestamp", (Some (Timestamp.to_value x.timestamp)));
        ("SegmentsReceivedCount",
          (Option.map x.segmentsReceivedCount ~f:NullableInteger.to_value));
        ("SegmentsSentCount",
          (Option.map x.segmentsSentCount ~f:NullableInteger.to_value));
        ("SegmentsSpilloverCount",
          (Option.map x.segmentsSpilloverCount ~f:NullableInteger.to_value));
        ("SegmentsRejectedCount",
          (Option.map x.segmentsRejectedCount ~f:NullableInteger.to_value));
        ("BackendConnectionErrors",
          (Option.map x.backendConnectionErrors
             ~f:BackendConnectionErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backendConnectionErrors =
        (Option.map ~f:BackendConnectionErrors.of_xml)
          (Xml.child xml_arg0 "BackendConnectionErrors") in
      let segmentsRejectedCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "SegmentsRejectedCount") in
      let segmentsSpilloverCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "SegmentsSpilloverCount") in
      let segmentsSentCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "SegmentsSentCount") in
      let segmentsReceivedCount =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "SegmentsReceivedCount") in
      let timestamp =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Timestamp") in
      make ?backendConnectionErrors ?segmentsRejectedCount
        ?segmentsSpilloverCount ?segmentsSentCount ?segmentsReceivedCount
        ~timestamp ()
    let of_json json =
      let backendConnectionErrors =
        field_map json "BackendConnectionErrors"
          BackendConnectionErrors.of_json in
      let segmentsRejectedCount =
        field_map json "SegmentsRejectedCount" NullableInteger.of_json in
      let segmentsSpilloverCount =
        field_map json "SegmentsSpilloverCount" NullableInteger.of_json in
      let segmentsSentCount =
        field_map json "SegmentsSentCount" NullableInteger.of_json in
      let segmentsReceivedCount =
        field_map json "SegmentsReceivedCount" NullableInteger.of_json in
      let timestamp = field_map_exn json "Timestamp" Timestamp.of_json in
      make ?backendConnectionErrors ?segmentsRejectedCount
        ?segmentsSpilloverCount ?segmentsSentCount ?segmentsReceivedCount
        ~timestamp ()
    let to_json v = composed_to_json to_value v
  end
module EncryptionStatus =
  struct
    type nonrec t =
      | UPDATING 
      | ACTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UPDATING -> "UPDATING"
      | ACTIVE -> "ACTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "UPDATING" -> UPDATING
      | "ACTIVE" -> ACTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EncryptionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EncryptionStatus" j)
    let to_json = simple_to_json to_value
  end
module EncryptionType =
  struct
    type nonrec t =
      | NONE 
      | KMS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | NONE -> "NONE" | KMS -> "KMS" | Non_static_id s -> s
    let of_string =
      function | "NONE" -> NONE | "KMS" -> KMS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EncryptionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EncryptionType" j)
    let to_json = simple_to_json to_value
  end
module TraceSummary =
  struct
    type nonrec t =
      {
      id: TraceId.t option
        [@ocaml.doc
          "The unique identifier for the request that generated the trace's segments and subsegments."];
      duration: NullableDouble.t option
        [@ocaml.doc
          "The length of time in seconds between the start time of the root segment and the end time of the last segment that completed."];
      responseTime: NullableDouble.t option
        [@ocaml.doc
          "The length of time in seconds between the start and end times of the root segment. If the service performs work asynchronously, the response time measures the time before the response is sent to the user, while the duration measures the amount of time before the last traced activity completes."];
      hasFault: NullableBoolean.t option
        [@ocaml.doc "The root segment document has a 500 series error."];
      hasError: NullableBoolean.t option
        [@ocaml.doc "The root segment document has a 400 series error."];
      hasThrottle: NullableBoolean.t option
        [@ocaml.doc
          "One or more of the segment documents has a 429 throttling error."];
      isPartial: NullableBoolean.t option
        [@ocaml.doc "One or more of the segment documents is in progress."];
      http: Http.t option
        [@ocaml.doc
          "Information about the HTTP request served by the trace."];
      annotations: Annotations.t option
        [@ocaml.doc "Annotations from the trace's segment documents."];
      users: TraceUsers.t option
        [@ocaml.doc "Users from the trace's segment documents."];
      serviceIds: ServiceIds.t option
        [@ocaml.doc "Service IDs from the trace's segment documents."];
      resourceARNs: TraceResourceARNs.t option
        [@ocaml.doc
          "A list of resource ARNs for any resource corresponding to the trace segments."];
      instanceIds: TraceInstanceIds.t option
        [@ocaml.doc
          "A list of EC2 instance IDs for any instance corresponding to the trace segments."];
      availabilityZones: TraceAvailabilityZones.t option
        [@ocaml.doc
          "A list of Availability Zones for any zone corresponding to the trace segments."];
      entryPoint: ServiceId.t option [@ocaml.doc "The root of a trace."];
      faultRootCauses: FaultRootCauses.t option
        [@ocaml.doc
          "A collection of FaultRootCause structures corresponding to the trace segments."];
      errorRootCauses: ErrorRootCauses.t option
        [@ocaml.doc
          "A collection of ErrorRootCause structures corresponding to the trace segments."];
      responseTimeRootCauses: ResponseTimeRootCauses.t option
        [@ocaml.doc
          "A collection of ResponseTimeRootCause structures corresponding to the trace segments."];
      revision: Integer.t option
        [@ocaml.doc "The revision number of a trace."];
      matchedEventTime: Timestamp.t option
        [@ocaml.doc "The matched time stamp of a defined event."]}
    let make ?id =
      fun ?duration ->
        fun ?responseTime ->
          fun ?hasFault ->
            fun ?hasError ->
              fun ?hasThrottle ->
                fun ?isPartial ->
                  fun ?http ->
                    fun ?annotations ->
                      fun ?users ->
                        fun ?serviceIds ->
                          fun ?resourceARNs ->
                            fun ?instanceIds ->
                              fun ?availabilityZones ->
                                fun ?entryPoint ->
                                  fun ?faultRootCauses ->
                                    fun ?errorRootCauses ->
                                      fun ?responseTimeRootCauses ->
                                        fun ?revision ->
                                          fun ?matchedEventTime ->
                                            fun () ->
                                              {
                                                id;
                                                duration;
                                                responseTime;
                                                hasFault;
                                                hasError;
                                                hasThrottle;
                                                isPartial;
                                                http;
                                                annotations;
                                                users;
                                                serviceIds;
                                                resourceARNs;
                                                instanceIds;
                                                availabilityZones;
                                                entryPoint;
                                                faultRootCauses;
                                                errorRootCauses;
                                                responseTimeRootCauses;
                                                revision;
                                                matchedEventTime
                                              }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:TraceId.to_value));
        ("Duration", (Option.map x.duration ~f:NullableDouble.to_value));
        ("ResponseTime",
          (Option.map x.responseTime ~f:NullableDouble.to_value));
        ("HasFault", (Option.map x.hasFault ~f:NullableBoolean.to_value));
        ("HasError", (Option.map x.hasError ~f:NullableBoolean.to_value));
        ("HasThrottle",
          (Option.map x.hasThrottle ~f:NullableBoolean.to_value));
        ("IsPartial", (Option.map x.isPartial ~f:NullableBoolean.to_value));
        ("Http", (Option.map x.http ~f:Http.to_value));
        ("Annotations", (Option.map x.annotations ~f:Annotations.to_value));
        ("Users", (Option.map x.users ~f:TraceUsers.to_value));
        ("ServiceIds", (Option.map x.serviceIds ~f:ServiceIds.to_value));
        ("ResourceARNs",
          (Option.map x.resourceARNs ~f:TraceResourceARNs.to_value));
        ("InstanceIds",
          (Option.map x.instanceIds ~f:TraceInstanceIds.to_value));
        ("AvailabilityZones",
          (Option.map x.availabilityZones ~f:TraceAvailabilityZones.to_value));
        ("EntryPoint", (Option.map x.entryPoint ~f:ServiceId.to_value));
        ("FaultRootCauses",
          (Option.map x.faultRootCauses ~f:FaultRootCauses.to_value));
        ("ErrorRootCauses",
          (Option.map x.errorRootCauses ~f:ErrorRootCauses.to_value));
        ("ResponseTimeRootCauses",
          (Option.map x.responseTimeRootCauses
             ~f:ResponseTimeRootCauses.to_value));
        ("Revision", (Option.map x.revision ~f:Integer.to_value));
        ("MatchedEventTime",
          (Option.map x.matchedEventTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let matchedEventTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "MatchedEventTime") in
      let revision =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Revision") in
      let responseTimeRootCauses =
        (Option.map ~f:ResponseTimeRootCauses.of_xml)
          (Xml.child xml_arg0 "ResponseTimeRootCauses") in
      let errorRootCauses =
        (Option.map ~f:ErrorRootCauses.of_xml)
          (Xml.child xml_arg0 "ErrorRootCauses") in
      let faultRootCauses =
        (Option.map ~f:FaultRootCauses.of_xml)
          (Xml.child xml_arg0 "FaultRootCauses") in
      let entryPoint =
        (Option.map ~f:ServiceId.of_xml) (Xml.child xml_arg0 "EntryPoint") in
      let availabilityZones =
        (Option.map ~f:TraceAvailabilityZones.of_xml)
          (Xml.child xml_arg0 "AvailabilityZones") in
      let instanceIds =
        (Option.map ~f:TraceInstanceIds.of_xml)
          (Xml.child xml_arg0 "InstanceIds") in
      let resourceARNs =
        (Option.map ~f:TraceResourceARNs.of_xml)
          (Xml.child xml_arg0 "ResourceARNs") in
      let serviceIds =
        (Option.map ~f:ServiceIds.of_xml) (Xml.child xml_arg0 "ServiceIds") in
      let users =
        (Option.map ~f:TraceUsers.of_xml) (Xml.child xml_arg0 "Users") in
      let annotations =
        (Option.map ~f:Annotations.of_xml) (Xml.child xml_arg0 "Annotations") in
      let http = (Option.map ~f:Http.of_xml) (Xml.child xml_arg0 "Http") in
      let isPartial =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "IsPartial") in
      let hasThrottle =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "HasThrottle") in
      let hasError =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "HasError") in
      let hasFault =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "HasFault") in
      let responseTime =
        (Option.map ~f:NullableDouble.of_xml)
          (Xml.child xml_arg0 "ResponseTime") in
      let duration =
        (Option.map ~f:NullableDouble.of_xml) (Xml.child xml_arg0 "Duration") in
      let id = (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?matchedEventTime ?revision ?responseTimeRootCauses
        ?errorRootCauses ?faultRootCauses ?entryPoint ?availabilityZones
        ?instanceIds ?resourceARNs ?serviceIds ?users ?annotations ?http
        ?isPartial ?hasThrottle ?hasError ?hasFault ?responseTime ?duration
        ?id ()
    let of_json json =
      let matchedEventTime =
        field_map json "MatchedEventTime" Timestamp.of_json in
      let revision = field_map json "Revision" Integer.of_json in
      let responseTimeRootCauses =
        field_map json "ResponseTimeRootCauses"
          ResponseTimeRootCauses.of_json in
      let errorRootCauses =
        field_map json "ErrorRootCauses" ErrorRootCauses.of_json in
      let faultRootCauses =
        field_map json "FaultRootCauses" FaultRootCauses.of_json in
      let entryPoint = field_map json "EntryPoint" ServiceId.of_json in
      let availabilityZones =
        field_map json "AvailabilityZones" TraceAvailabilityZones.of_json in
      let instanceIds = field_map json "InstanceIds" TraceInstanceIds.of_json in
      let resourceARNs =
        field_map json "ResourceARNs" TraceResourceARNs.of_json in
      let serviceIds = field_map json "ServiceIds" ServiceIds.of_json in
      let users = field_map json "Users" TraceUsers.of_json in
      let annotations = field_map json "Annotations" Annotations.of_json in
      let http = field_map json "Http" Http.of_json in
      let isPartial = field_map json "IsPartial" NullableBoolean.of_json in
      let hasThrottle = field_map json "HasThrottle" NullableBoolean.of_json in
      let hasError = field_map json "HasError" NullableBoolean.of_json in
      let hasFault = field_map json "HasFault" NullableBoolean.of_json in
      let responseTime = field_map json "ResponseTime" NullableDouble.of_json in
      let duration = field_map json "Duration" NullableDouble.of_json in
      let id = field_map json "Id" TraceId.of_json in
      make ?matchedEventTime ?revision ?responseTimeRootCauses
        ?errorRootCauses ?faultRootCauses ?entryPoint ?availabilityZones
        ?instanceIds ?resourceARNs ?serviceIds ?users ?annotations ?http
        ?isPartial ?hasThrottle ?hasError ?hasFault ?responseTime ?duration
        ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Metadata generated from the segment documents in a trace."]
module SamplingStrategyName =
  struct
    type nonrec t =
      | PartialScan 
      | FixedRate 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PartialScan -> "PartialScan"
      | FixedRate -> "FixedRate"
      | Non_static_id s -> s
    let of_string =
      function
      | "PartialScan" -> PartialScan
      | "FixedRate" -> FixedRate
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SamplingStrategyName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SamplingStrategyName" j)
    let to_json = simple_to_json to_value
  end
module Service =
  struct
    type nonrec t =
      {
      referenceId: NullableInteger.t option
        [@ocaml.doc
          "Identifier for the service. Unique within the service map."];
      name: String_.t option
        [@ocaml.doc "The canonical name of the service."];
      names: ServiceNames.t option
        [@ocaml.doc
          "A list of names for the service, including the canonical name."];
      root: NullableBoolean.t option
        [@ocaml.doc
          "Indicates that the service was the first service to process a request."];
      accountId: String_.t option
        [@ocaml.doc
          "Identifier of the Amazon Web Services account in which the service runs."];
      type_: String_.t option
        [@ocaml.doc
          "The type of service. Amazon Web Services Resource - The type of an Amazon Web Services resource. For example, AWS::EC2::Instance for an application running on Amazon EC2 or AWS::DynamoDB::Table for an Amazon DynamoDB table that the application used. Amazon Web Services Service - The type of an Amazon Web Services service. For example, AWS::DynamoDB for downstream calls to Amazon DynamoDB that didn't target a specific table. client - Represents the clients that sent requests to a root service. remote - A downstream service of indeterminate type."];
      state: String_.t option [@ocaml.doc "The service's state."];
      startTime: Timestamp.t option
        [@ocaml.doc
          "The start time of the first segment that the service generated."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "The end time of the last segment that the service generated."];
      edges: EdgeList.t option
        [@ocaml.doc "Connections to downstream services."];
      summaryStatistics: ServiceStatistics.t option
        [@ocaml.doc "Aggregated statistics for the service."];
      durationHistogram: Histogram.t option
        [@ocaml.doc "A histogram that maps the spread of service durations."];
      responseTimeHistogram: Histogram.t option
        [@ocaml.doc
          "A histogram that maps the spread of service response times."]}
    let make ?referenceId =
      fun ?name ->
        fun ?names ->
          fun ?root ->
            fun ?accountId ->
              fun ?type_ ->
                fun ?state ->
                  fun ?startTime ->
                    fun ?endTime ->
                      fun ?edges ->
                        fun ?summaryStatistics ->
                          fun ?durationHistogram ->
                            fun ?responseTimeHistogram ->
                              fun () ->
                                {
                                  referenceId;
                                  name;
                                  names;
                                  root;
                                  accountId;
                                  type_;
                                  state;
                                  startTime;
                                  endTime;
                                  edges;
                                  summaryStatistics;
                                  durationHistogram;
                                  responseTimeHistogram
                                }
    let to_value x =
      structure_to_value
        [("ReferenceId",
           (Option.map x.referenceId ~f:NullableInteger.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Names", (Option.map x.names ~f:ServiceNames.to_value));
        ("Root", (Option.map x.root ~f:NullableBoolean.to_value));
        ("AccountId", (Option.map x.accountId ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("State", (Option.map x.state ~f:String_.to_value));
        ("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("Edges", (Option.map x.edges ~f:EdgeList.to_value));
        ("SummaryStatistics",
          (Option.map x.summaryStatistics ~f:ServiceStatistics.to_value));
        ("DurationHistogram",
          (Option.map x.durationHistogram ~f:Histogram.to_value));
        ("ResponseTimeHistogram",
          (Option.map x.responseTimeHistogram ~f:Histogram.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let responseTimeHistogram =
        (Option.map ~f:Histogram.of_xml)
          (Xml.child xml_arg0 "ResponseTimeHistogram") in
      let durationHistogram =
        (Option.map ~f:Histogram.of_xml)
          (Xml.child xml_arg0 "DurationHistogram") in
      let summaryStatistics =
        (Option.map ~f:ServiceStatistics.of_xml)
          (Xml.child xml_arg0 "SummaryStatistics") in
      let edges =
        (Option.map ~f:EdgeList.of_xml) (Xml.child xml_arg0 "Edges") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let state = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "State") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let accountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccountId") in
      let root =
        (Option.map ~f:NullableBoolean.of_xml) (Xml.child xml_arg0 "Root") in
      let names =
        (Option.map ~f:ServiceNames.of_xml) (Xml.child xml_arg0 "Names") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let referenceId =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "ReferenceId") in
      make ?responseTimeHistogram ?durationHistogram ?summaryStatistics
        ?edges ?endTime ?startTime ?state ?type_ ?accountId ?root ?names
        ?name ?referenceId ()
    let of_json json =
      let responseTimeHistogram =
        field_map json "ResponseTimeHistogram" Histogram.of_json in
      let durationHistogram =
        field_map json "DurationHistogram" Histogram.of_json in
      let summaryStatistics =
        field_map json "SummaryStatistics" ServiceStatistics.of_json in
      let edges = field_map json "Edges" EdgeList.of_json in
      let endTime = field_map json "EndTime" Timestamp.of_json in
      let startTime = field_map json "StartTime" Timestamp.of_json in
      let state = field_map json "State" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let accountId = field_map json "AccountId" String_.of_json in
      let root = field_map json "Root" NullableBoolean.of_json in
      let names = field_map json "Names" ServiceNames.of_json in
      let name = field_map json "Name" String_.of_json in
      let referenceId = field_map json "ReferenceId" NullableInteger.of_json in
      make ?responseTimeHistogram ?durationHistogram ?summaryStatistics
        ?edges ?endTime ?startTime ?state ?type_ ?accountId ?root ?names
        ?name ?referenceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about an application that processed requests, users that made requests, or downstream services, resources, and applications that an application used."]
module TimeSeriesServiceStatistics =
  struct
    type nonrec t =
      {
      timestamp: Timestamp.t option
        [@ocaml.doc
          "Timestamp of the window for which statistics are aggregated."];
      edgeSummaryStatistics: EdgeStatistics.t option ;
      serviceSummaryStatistics: ServiceStatistics.t option ;
      serviceForecastStatistics: ForecastStatistics.t option
        [@ocaml.doc "The forecasted high and low fault count values."];
      responseTimeHistogram: Histogram.t option
        [@ocaml.doc "The response time histogram for the selected entities."]}
    let make ?timestamp =
      fun ?edgeSummaryStatistics ->
        fun ?serviceSummaryStatistics ->
          fun ?serviceForecastStatistics ->
            fun ?responseTimeHistogram ->
              fun () ->
                {
                  timestamp;
                  edgeSummaryStatistics;
                  serviceSummaryStatistics;
                  serviceForecastStatistics;
                  responseTimeHistogram
                }
    let to_value x =
      structure_to_value
        [("Timestamp", (Option.map x.timestamp ~f:Timestamp.to_value));
        ("EdgeSummaryStatistics",
          (Option.map x.edgeSummaryStatistics ~f:EdgeStatistics.to_value));
        ("ServiceSummaryStatistics",
          (Option.map x.serviceSummaryStatistics
             ~f:ServiceStatistics.to_value));
        ("ServiceForecastStatistics",
          (Option.map x.serviceForecastStatistics
             ~f:ForecastStatistics.to_value));
        ("ResponseTimeHistogram",
          (Option.map x.responseTimeHistogram ~f:Histogram.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let responseTimeHistogram =
        (Option.map ~f:Histogram.of_xml)
          (Xml.child xml_arg0 "ResponseTimeHistogram") in
      let serviceForecastStatistics =
        (Option.map ~f:ForecastStatistics.of_xml)
          (Xml.child xml_arg0 "ServiceForecastStatistics") in
      let serviceSummaryStatistics =
        (Option.map ~f:ServiceStatistics.of_xml)
          (Xml.child xml_arg0 "ServiceSummaryStatistics") in
      let edgeSummaryStatistics =
        (Option.map ~f:EdgeStatistics.of_xml)
          (Xml.child xml_arg0 "EdgeSummaryStatistics") in
      let timestamp =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "Timestamp") in
      make ?responseTimeHistogram ?serviceForecastStatistics
        ?serviceSummaryStatistics ?edgeSummaryStatistics ?timestamp ()
    let of_json json =
      let responseTimeHistogram =
        field_map json "ResponseTimeHistogram" Histogram.of_json in
      let serviceForecastStatistics =
        field_map json "ServiceForecastStatistics" ForecastStatistics.of_json in
      let serviceSummaryStatistics =
        field_map json "ServiceSummaryStatistics" ServiceStatistics.of_json in
      let edgeSummaryStatistics =
        field_map json "EdgeSummaryStatistics" EdgeStatistics.of_json in
      let timestamp = field_map json "Timestamp" Timestamp.of_json in
      make ?responseTimeHistogram ?serviceForecastStatistics
        ?serviceSummaryStatistics ?edgeSummaryStatistics ?timestamp ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A list of TimeSeriesStatistic structures."]
module SamplingTargetDocument =
  struct
    type nonrec t =
      {
      ruleName: String_.t option
        [@ocaml.doc "The name of the sampling rule."];
      fixedRate: Double.t option
        [@ocaml.doc
          "The percentage of matching requests to instrument, after the reservoir is exhausted."];
      reservoirQuota: NullableInteger.t option
        [@ocaml.doc
          "The number of requests per second that X-Ray allocated for this service."];
      reservoirQuotaTTL: Timestamp.t option
        [@ocaml.doc "When the reservoir quota expires."];
      interval: NullableInteger.t option
        [@ocaml.doc
          "The number of seconds for the service to wait before getting sampling targets again."]}
    let make ?ruleName =
      fun ?fixedRate ->
        fun ?reservoirQuota ->
          fun ?reservoirQuotaTTL ->
            fun ?interval ->
              fun () ->
                {
                  ruleName;
                  fixedRate;
                  reservoirQuota;
                  reservoirQuotaTTL;
                  interval
                }
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:String_.to_value));
        ("FixedRate", (Option.map x.fixedRate ~f:Double.to_value));
        ("ReservoirQuota",
          (Option.map x.reservoirQuota ~f:NullableInteger.to_value));
        ("ReservoirQuotaTTL",
          (Option.map x.reservoirQuotaTTL ~f:Timestamp.to_value));
        ("Interval", (Option.map x.interval ~f:NullableInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let interval =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "Interval") in
      let reservoirQuotaTTL =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ReservoirQuotaTTL") in
      let reservoirQuota =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "ReservoirQuota") in
      let fixedRate =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "FixedRate") in
      let ruleName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?interval ?reservoirQuotaTTL ?reservoirQuota ?fixedRate ?ruleName
        ()
    let of_json json =
      let interval = field_map json "Interval" NullableInteger.of_json in
      let reservoirQuotaTTL =
        field_map json "ReservoirQuotaTTL" Timestamp.of_json in
      let reservoirQuota =
        field_map json "ReservoirQuota" NullableInteger.of_json in
      let fixedRate = field_map json "FixedRate" Double.of_json in
      let ruleName = field_map json "RuleName" String_.of_json in
      make ?interval ?reservoirQuotaTTL ?reservoirQuota ?fixedRate ?ruleName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Temporary changes to a sampling rule configuration. To meet the global sampling target for a rule, X-Ray calculates a new reservoir for each service based on the recent sampling results of all services that called GetSamplingTargets."]
module UnprocessedStatistics =
  struct
    type nonrec t =
      {
      ruleName: String_.t option
        [@ocaml.doc "The name of the sampling rule."];
      errorCode: String_.t option [@ocaml.doc "The error code."];
      message: String_.t option [@ocaml.doc "The error message."]}
    let make ?ruleName =
      fun ?errorCode ->
        fun ?message -> fun () -> { ruleName; errorCode; message }
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:String_.to_value));
        ("ErrorCode", (Option.map x.errorCode ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let errorCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ErrorCode") in
      let ruleName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?message ?errorCode ?ruleName ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let errorCode = field_map json "ErrorCode" String_.of_json in
      let ruleName = field_map json "RuleName" String_.of_json in
      make ?message ?errorCode ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sampling statistics from a call to GetSamplingTargets that X-Ray could not process."]
module SamplingStatisticsDocument =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t [@ocaml.doc "The name of the sampling rule."];
      clientID: ClientID.t
        [@ocaml.doc "A unique identifier for the service in hexadecimal."];
      timestamp: Timestamp.t [@ocaml.doc "The current time."];
      requestCount: RequestCount.t
        [@ocaml.doc "The number of requests that matched the rule."];
      sampledCount: SampledCount.t
        [@ocaml.doc "The number of requests recorded."];
      borrowCount: BorrowCount.t option
        [@ocaml.doc
          "The number of requests recorded with borrowed reservoir quota."]}
    let context_ = "SamplingStatisticsDocument"
    let make ?borrowCount =
      fun ~ruleName ->
        fun ~clientID ->
          fun ~timestamp ->
            fun ~requestCount ->
              fun ~sampledCount ->
                fun () ->
                  {
                    borrowCount;
                    ruleName;
                    clientID;
                    timestamp;
                    requestCount;
                    sampledCount
                  }
    let to_value x =
      structure_to_value
        [("RuleName", (Some (RuleName.to_value x.ruleName)));
        ("ClientID", (Some (ClientID.to_value x.clientID)));
        ("Timestamp", (Some (Timestamp.to_value x.timestamp)));
        ("RequestCount", (Some (RequestCount.to_value x.requestCount)));
        ("SampledCount", (Some (SampledCount.to_value x.sampledCount)));
        ("BorrowCount", (Option.map x.borrowCount ~f:BorrowCount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let borrowCount =
        (Option.map ~f:BorrowCount.of_xml) (Xml.child xml_arg0 "BorrowCount") in
      let sampledCount =
        SampledCount.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SampledCount") in
      let requestCount =
        RequestCount.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RequestCount") in
      let timestamp =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Timestamp") in
      let clientID =
        ClientID.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ClientID") in
      let ruleName =
        RuleName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      make ?borrowCount ~sampledCount ~requestCount ~timestamp ~clientID
        ~ruleName ()
    let of_json json =
      let borrowCount = field_map json "BorrowCount" BorrowCount.of_json in
      let sampledCount =
        field_map_exn json "SampledCount" SampledCount.of_json in
      let requestCount =
        field_map_exn json "RequestCount" RequestCount.of_json in
      let timestamp = field_map_exn json "Timestamp" Timestamp.of_json in
      let clientID = field_map_exn json "ClientID" ClientID.of_json in
      let ruleName = field_map_exn json "RuleName" RuleName.of_json in
      make ?borrowCount ~sampledCount ~requestCount ~timestamp ~clientID
        ~ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request sampling results for a single rule from a service. Results are for the last 10 seconds unless the service has been assigned a longer reporting interval after a previous call to GetSamplingTargets."]
module SamplingStatisticSummary =
  struct
    type nonrec t =
      {
      ruleName: String_.t option
        [@ocaml.doc "The name of the sampling rule."];
      timestamp: Timestamp.t option
        [@ocaml.doc "The start time of the reporting window."];
      requestCount: Integer.t option
        [@ocaml.doc "The number of requests that matched the rule."];
      borrowCount: Integer.t option
        [@ocaml.doc
          "The number of requests recorded with borrowed reservoir quota."];
      sampledCount: Integer.t option
        [@ocaml.doc "The number of requests recorded."]}
    let make ?ruleName =
      fun ?timestamp ->
        fun ?requestCount ->
          fun ?borrowCount ->
            fun ?sampledCount ->
              fun () ->
                {
                  ruleName;
                  timestamp;
                  requestCount;
                  borrowCount;
                  sampledCount
                }
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:String_.to_value));
        ("Timestamp", (Option.map x.timestamp ~f:Timestamp.to_value));
        ("RequestCount", (Option.map x.requestCount ~f:Integer.to_value));
        ("BorrowCount", (Option.map x.borrowCount ~f:Integer.to_value));
        ("SampledCount", (Option.map x.sampledCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sampledCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "SampledCount") in
      let borrowCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "BorrowCount") in
      let requestCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "RequestCount") in
      let timestamp =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "Timestamp") in
      let ruleName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?sampledCount ?borrowCount ?requestCount ?timestamp ?ruleName ()
    let of_json json =
      let sampledCount = field_map json "SampledCount" Integer.of_json in
      let borrowCount = field_map json "BorrowCount" Integer.of_json in
      let requestCount = field_map json "RequestCount" Integer.of_json in
      let timestamp = field_map json "Timestamp" Timestamp.of_json in
      let ruleName = field_map json "RuleName" String_.of_json in
      make ?sampledCount ?borrowCount ?requestCount ?timestamp ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Aggregated request sampling data for a sampling rule across all services for a 10-second window."]
module SamplingRuleRecord =
  struct
    type nonrec t =
      {
      samplingRule: SamplingRule.t option [@ocaml.doc "The sampling rule."];
      createdAt: Timestamp.t option [@ocaml.doc "When the rule was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "When the rule was last modified."]}
    let make ?samplingRule =
      fun ?createdAt ->
        fun ?modifiedAt -> fun () -> { samplingRule; createdAt; modifiedAt }
    let to_value x =
      structure_to_value
        [("SamplingRule",
           (Option.map x.samplingRule ~f:SamplingRule.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let samplingRule =
        (Option.map ~f:SamplingRule.of_xml)
          (Xml.child xml_arg0 "SamplingRule") in
      make ?modifiedAt ?createdAt ?samplingRule ()
    let of_json json =
      let modifiedAt = field_map json "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      let samplingRule = field_map json "SamplingRule" SamplingRule.of_json in
      make ?modifiedAt ?createdAt ?samplingRule ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A SamplingRule and its metadata."]
module InsightSummary =
  struct
    type nonrec t =
      {
      insightId: InsightId.t option
        [@ocaml.doc "The insights unique identifier."];
      groupARN: GroupARN.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the group that the insight belongs to."];
      groupName: GroupName.t option
        [@ocaml.doc "The name of the group that the insight belongs to."];
      rootCauseServiceId: ServiceId.t option ;
      categories: InsightCategoryList.t option
        [@ocaml.doc
          "Categories The categories that label and describe the type of insight."];
      state: InsightState.t option
        [@ocaml.doc "The current state of the insight."];
      startTime: Timestamp.t option
        [@ocaml.doc "The time, in Unix seconds, at which the insight began."];
      endTime: Timestamp.t option
        [@ocaml.doc "The time, in Unix seconds, at which the insight ended."];
      summary: InsightSummaryText.t option
        [@ocaml.doc "A brief description of the insight."];
      clientRequestImpactStatistics: RequestImpactStatistics.t option
        [@ocaml.doc
          "The impact statistics of the client side service. This includes the number of requests to the client service and whether the requests were faults or okay."];
      rootCauseServiceRequestImpactStatistics:
        RequestImpactStatistics.t option
        [@ocaml.doc
          "The impact statistics of the root cause service. This includes the number of requests to the client service and whether the requests were faults or okay."];
      topAnomalousServices: AnomalousServiceList.t option
        [@ocaml.doc
          "The service within the insight that is most impacted by the incident."];
      lastUpdateTime: Timestamp.t option
        [@ocaml.doc
          "The time, in Unix seconds, that the insight was last updated."]}
    let make ?insightId =
      fun ?groupARN ->
        fun ?groupName ->
          fun ?rootCauseServiceId ->
            fun ?categories ->
              fun ?state ->
                fun ?startTime ->
                  fun ?endTime ->
                    fun ?summary ->
                      fun ?clientRequestImpactStatistics ->
                        fun ?rootCauseServiceRequestImpactStatistics ->
                          fun ?topAnomalousServices ->
                            fun ?lastUpdateTime ->
                              fun () ->
                                {
                                  insightId;
                                  groupARN;
                                  groupName;
                                  rootCauseServiceId;
                                  categories;
                                  state;
                                  startTime;
                                  endTime;
                                  summary;
                                  clientRequestImpactStatistics;
                                  rootCauseServiceRequestImpactStatistics;
                                  topAnomalousServices;
                                  lastUpdateTime
                                }
    let to_value x =
      structure_to_value
        [("InsightId", (Option.map x.insightId ~f:InsightId.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:GroupARN.to_value));
        ("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("RootCauseServiceId",
          (Option.map x.rootCauseServiceId ~f:ServiceId.to_value));
        ("Categories",
          (Option.map x.categories ~f:InsightCategoryList.to_value));
        ("State", (Option.map x.state ~f:InsightState.to_value));
        ("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("Summary", (Option.map x.summary ~f:InsightSummaryText.to_value));
        ("ClientRequestImpactStatistics",
          (Option.map x.clientRequestImpactStatistics
             ~f:RequestImpactStatistics.to_value));
        ("RootCauseServiceRequestImpactStatistics",
          (Option.map x.rootCauseServiceRequestImpactStatistics
             ~f:RequestImpactStatistics.to_value));
        ("TopAnomalousServices",
          (Option.map x.topAnomalousServices ~f:AnomalousServiceList.to_value));
        ("LastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastUpdateTime") in
      let topAnomalousServices =
        (Option.map ~f:AnomalousServiceList.of_xml)
          (Xml.child xml_arg0 "TopAnomalousServices") in
      let rootCauseServiceRequestImpactStatistics =
        (Option.map ~f:RequestImpactStatistics.of_xml)
          (Xml.child xml_arg0 "RootCauseServiceRequestImpactStatistics") in
      let clientRequestImpactStatistics =
        (Option.map ~f:RequestImpactStatistics.of_xml)
          (Xml.child xml_arg0 "ClientRequestImpactStatistics") in
      let summary =
        (Option.map ~f:InsightSummaryText.of_xml)
          (Xml.child xml_arg0 "Summary") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let state =
        (Option.map ~f:InsightState.of_xml) (Xml.child xml_arg0 "State") in
      let categories =
        (Option.map ~f:InsightCategoryList.of_xml)
          (Xml.child xml_arg0 "Categories") in
      let rootCauseServiceId =
        (Option.map ~f:ServiceId.of_xml)
          (Xml.child xml_arg0 "RootCauseServiceId") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      let groupARN =
        (Option.map ~f:GroupARN.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let insightId =
        (Option.map ~f:InsightId.of_xml) (Xml.child xml_arg0 "InsightId") in
      make ?lastUpdateTime ?topAnomalousServices
        ?rootCauseServiceRequestImpactStatistics
        ?clientRequestImpactStatistics ?summary ?endTime ?startTime ?state
        ?categories ?rootCauseServiceId ?groupName ?groupARN ?insightId ()
    let of_json json =
      let lastUpdateTime = field_map json "LastUpdateTime" Timestamp.of_json in
      let topAnomalousServices =
        field_map json "TopAnomalousServices" AnomalousServiceList.of_json in
      let rootCauseServiceRequestImpactStatistics =
        field_map json "RootCauseServiceRequestImpactStatistics"
          RequestImpactStatistics.of_json in
      let clientRequestImpactStatistics =
        field_map json "ClientRequestImpactStatistics"
          RequestImpactStatistics.of_json in
      let summary = field_map json "Summary" InsightSummaryText.of_json in
      let endTime = field_map json "EndTime" Timestamp.of_json in
      let startTime = field_map json "StartTime" Timestamp.of_json in
      let state = field_map json "State" InsightState.of_json in
      let categories =
        field_map json "Categories" InsightCategoryList.of_json in
      let rootCauseServiceId =
        field_map json "RootCauseServiceId" ServiceId.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      let groupARN = field_map json "GroupARN" GroupARN.of_json in
      let insightId = field_map json "InsightId" InsightId.of_json in
      make ?lastUpdateTime ?topAnomalousServices
        ?rootCauseServiceRequestImpactStatistics
        ?clientRequestImpactStatistics ?summary ?endTime ?startTime ?state
        ?categories ?rootCauseServiceId ?groupName ?groupARN ?insightId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information that describes an insight."]
module InsightImpactGraphService =
  struct
    type nonrec t =
      {
      referenceId: NullableInteger.t option
        [@ocaml.doc
          "Identifier for the service. Unique within the service map."];
      type_: String_.t option
        [@ocaml.doc
          "Identifier for the service. Unique within the service map. Amazon Web Services Resource - The type of an Amazon Web Services resource. For example, AWS::EC2::Instance for an application running on Amazon EC2 or AWS::DynamoDB::Table for an Amazon DynamoDB table that the application used. Amazon Web Services Service - The type of an Amazon Web Services service. For example, AWS::DynamoDB for downstream calls to Amazon DynamoDB that didn't target a specific table. Amazon Web Services Service - The type of an Amazon Web Services service. For example, AWS::DynamoDB for downstream calls to Amazon DynamoDB that didn't target a specific table. remote - A downstream service of indeterminate type."];
      name: String_.t option
        [@ocaml.doc "The canonical name of the service."];
      names: ServiceNames.t option
        [@ocaml.doc
          "A list of names for the service, including the canonical name."];
      accountId: String_.t option
        [@ocaml.doc
          "Identifier of the Amazon Web Services account in which the service runs."];
      edges: InsightImpactGraphEdgeList.t option
        [@ocaml.doc "Connections to downstream services."]}
    let make ?referenceId =
      fun ?type_ ->
        fun ?name ->
          fun ?names ->
            fun ?accountId ->
              fun ?edges ->
                fun () ->
                  { referenceId; type_; name; names; accountId; edges }
    let to_value x =
      structure_to_value
        [("ReferenceId",
           (Option.map x.referenceId ~f:NullableInteger.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Names", (Option.map x.names ~f:ServiceNames.to_value));
        ("AccountId", (Option.map x.accountId ~f:String_.to_value));
        ("Edges",
          (Option.map x.edges ~f:InsightImpactGraphEdgeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let edges =
        (Option.map ~f:InsightImpactGraphEdgeList.of_xml)
          (Xml.child xml_arg0 "Edges") in
      let accountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AccountId") in
      let names =
        (Option.map ~f:ServiceNames.of_xml) (Xml.child xml_arg0 "Names") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let referenceId =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "ReferenceId") in
      make ?edges ?accountId ?names ?name ?type_ ?referenceId ()
    let of_json json =
      let edges = field_map json "Edges" InsightImpactGraphEdgeList.of_json in
      let accountId = field_map json "AccountId" String_.of_json in
      let names = field_map json "Names" ServiceNames.of_json in
      let name = field_map json "Name" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let referenceId = field_map json "ReferenceId" NullableInteger.of_json in
      make ?edges ?accountId ?names ?name ?type_ ?referenceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about an application that processed requests, users that made requests, or downstream services, resources, and applications that an application used."]
module InsightEvent =
  struct
    type nonrec t =
      {
      summary: EventSummaryText.t option
        [@ocaml.doc "A brief description of the event."];
      eventTime: Timestamp.t option
        [@ocaml.doc
          "The time, in Unix seconds, at which the event was recorded."];
      clientRequestImpactStatistics: RequestImpactStatistics.t option
        [@ocaml.doc
          "The impact statistics of the client side service. This includes the number of requests to the client service and whether the requests were faults or okay."];
      rootCauseServiceRequestImpactStatistics:
        RequestImpactStatistics.t option
        [@ocaml.doc
          "The impact statistics of the root cause service. This includes the number of requests to the client service and whether the requests were faults or okay."];
      topAnomalousServices: AnomalousServiceList.t option
        [@ocaml.doc
          "The service during the event that is most impacted by the incident."]}
    let make ?summary =
      fun ?eventTime ->
        fun ?clientRequestImpactStatistics ->
          fun ?rootCauseServiceRequestImpactStatistics ->
            fun ?topAnomalousServices ->
              fun () ->
                {
                  summary;
                  eventTime;
                  clientRequestImpactStatistics;
                  rootCauseServiceRequestImpactStatistics;
                  topAnomalousServices
                }
    let to_value x =
      structure_to_value
        [("Summary", (Option.map x.summary ~f:EventSummaryText.to_value));
        ("EventTime", (Option.map x.eventTime ~f:Timestamp.to_value));
        ("ClientRequestImpactStatistics",
          (Option.map x.clientRequestImpactStatistics
             ~f:RequestImpactStatistics.to_value));
        ("RootCauseServiceRequestImpactStatistics",
          (Option.map x.rootCauseServiceRequestImpactStatistics
             ~f:RequestImpactStatistics.to_value));
        ("TopAnomalousServices",
          (Option.map x.topAnomalousServices ~f:AnomalousServiceList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topAnomalousServices =
        (Option.map ~f:AnomalousServiceList.of_xml)
          (Xml.child xml_arg0 "TopAnomalousServices") in
      let rootCauseServiceRequestImpactStatistics =
        (Option.map ~f:RequestImpactStatistics.of_xml)
          (Xml.child xml_arg0 "RootCauseServiceRequestImpactStatistics") in
      let clientRequestImpactStatistics =
        (Option.map ~f:RequestImpactStatistics.of_xml)
          (Xml.child xml_arg0 "ClientRequestImpactStatistics") in
      let eventTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EventTime") in
      let summary =
        (Option.map ~f:EventSummaryText.of_xml)
          (Xml.child xml_arg0 "Summary") in
      make ?topAnomalousServices ?rootCauseServiceRequestImpactStatistics
        ?clientRequestImpactStatistics ?eventTime ?summary ()
    let of_json json =
      let topAnomalousServices =
        field_map json "TopAnomalousServices" AnomalousServiceList.of_json in
      let rootCauseServiceRequestImpactStatistics =
        field_map json "RootCauseServiceRequestImpactStatistics"
          RequestImpactStatistics.of_json in
      let clientRequestImpactStatistics =
        field_map json "ClientRequestImpactStatistics"
          RequestImpactStatistics.of_json in
      let eventTime = field_map json "EventTime" Timestamp.of_json in
      let summary = field_map json "Summary" EventSummaryText.of_json in
      make ?topAnomalousServices ?rootCauseServiceRequestImpactStatistics
        ?clientRequestImpactStatistics ?eventTime ?summary ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "X-Ray reevaluates insights periodically until they are resolved, and records each intermediate state in an event. You can review incident events in the Impact Timeline on the Inspect page in the X-Ray console."]
module GroupSummary =
  struct
    type nonrec t =
      {
      groupName: String_.t option
        [@ocaml.doc "The unique case-sensitive name of the group."];
      groupARN: String_.t option
        [@ocaml.doc "The ARN of the group generated based on the GroupName."];
      filterExpression: String_.t option
        [@ocaml.doc
          "The filter expression defining the parameters to include traces."];
      insightsConfiguration: InsightsConfiguration.t option
        [@ocaml.doc
          "The structure containing configurations related to insights. The InsightsEnabled boolean can be set to true to enable insights for the group or false to disable insights for the group. The NotificationsEnabled boolean can be set to true to enable insights notifications. Notifications can only be enabled on a group with InsightsEnabled set to true."]}
    let make ?groupName =
      fun ?groupARN ->
        fun ?filterExpression ->
          fun ?insightsConfiguration ->
            fun () ->
              { groupName; groupARN; filterExpression; insightsConfiguration
              }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:String_.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:String_.to_value));
        ("FilterExpression",
          (Option.map x.filterExpression ~f:String_.to_value));
        ("InsightsConfiguration",
          (Option.map x.insightsConfiguration
             ~f:InsightsConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let insightsConfiguration =
        (Option.map ~f:InsightsConfiguration.of_xml)
          (Xml.child xml_arg0 "InsightsConfiguration") in
      let filterExpression =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "FilterExpression") in
      let groupARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let groupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?insightsConfiguration ?filterExpression ?groupARN ?groupName ()
    let of_json json =
      let insightsConfiguration =
        field_map json "InsightsConfiguration" InsightsConfiguration.of_json in
      let filterExpression =
        field_map json "FilterExpression" String_.of_json in
      let groupARN = field_map json "GroupARN" String_.of_json in
      let groupName = field_map json "GroupName" String_.of_json in
      make ?insightsConfiguration ?filterExpression ?groupARN ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details for a group without metadata."]
module Trace =
  struct
    type nonrec t =
      {
      id: TraceId.t option
        [@ocaml.doc
          "The unique identifier for the request that generated the trace's segments and subsegments."];
      duration: NullableDouble.t option
        [@ocaml.doc
          "The length of time in seconds between the start time of the root segment and the end time of the last segment that completed."];
      limitExceeded: NullableBoolean.t option
        [@ocaml.doc
          "LimitExceeded is set to true when the trace has exceeded one of the defined quotas. For more information about quotas, see Amazon Web Services X-Ray endpoints and quotas."];
      segments: SegmentList.t option
        [@ocaml.doc
          "Segment documents for the segments and subsegments that comprise the trace."]}
    let make ?id =
      fun ?duration ->
        fun ?limitExceeded ->
          fun ?segments ->
            fun () -> { id; duration; limitExceeded; segments }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:TraceId.to_value));
        ("Duration", (Option.map x.duration ~f:NullableDouble.to_value));
        ("LimitExceeded",
          (Option.map x.limitExceeded ~f:NullableBoolean.to_value));
        ("Segments", (Option.map x.segments ~f:SegmentList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let segments =
        (Option.map ~f:SegmentList.of_xml) (Xml.child xml_arg0 "Segments") in
      let limitExceeded =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "LimitExceeded") in
      let duration =
        (Option.map ~f:NullableDouble.of_xml) (Xml.child xml_arg0 "Duration") in
      let id = (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?segments ?limitExceeded ?duration ?id ()
    let of_json json =
      let segments = field_map json "Segments" SegmentList.of_json in
      let limitExceeded =
        field_map json "LimitExceeded" NullableBoolean.of_json in
      let duration = field_map json "Duration" NullableDouble.of_json in
      let id = field_map json "Id" TraceId.of_json in
      make ?segments ?limitExceeded ?duration ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A collection of segment documents with matching trace IDs."]
module InvalidRequestException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request is missing required parameters or has invalid parameters."]
module ThrottledException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request exceeds the maximum number of requests per second."]
module SamplingRuleUpdate =
  struct
    type nonrec t =
      {
      ruleName: RuleName.t option
        [@ocaml.doc
          "The name of the sampling rule. Specify a rule by either name or ARN, but not both."];
      ruleARN: String_.t option
        [@ocaml.doc
          "The ARN of the sampling rule. Specify a rule by either name or ARN, but not both."];
      resourceARN: ResourceARN.t option
        [@ocaml.doc
          "Matches the ARN of the Amazon Web Services resource on which the service runs."];
      priority: NullableInteger.t option
        [@ocaml.doc "The priority of the sampling rule."];
      fixedRate: NullableDouble.t option
        [@ocaml.doc
          "The percentage of matching requests to instrument, after the reservoir is exhausted."];
      reservoirSize: NullableInteger.t option
        [@ocaml.doc
          "A fixed number of matching requests to instrument per second, prior to applying the fixed rate. The reservoir is not used directly by services, but applies to all services using the rule collectively."];
      host: Host.t option
        [@ocaml.doc "Matches the hostname from a request URL."];
      serviceName: ServiceName.t option
        [@ocaml.doc
          "Matches the name that the service uses to identify itself in segments."];
      serviceType: ServiceType.t option
        [@ocaml.doc
          "Matches the origin that the service uses to identify its type in segments."];
      hTTPMethod: HTTPMethod.t option
        [@ocaml.doc "Matches the HTTP method of a request."];
      uRLPath: URLPath.t option
        [@ocaml.doc "Matches the path from a request URL."];
      attributes: AttributeMap.t option
        [@ocaml.doc "Matches attributes derived from the request."]}
    let make ?ruleName =
      fun ?ruleARN ->
        fun ?resourceARN ->
          fun ?priority ->
            fun ?fixedRate ->
              fun ?reservoirSize ->
                fun ?host ->
                  fun ?serviceName ->
                    fun ?serviceType ->
                      fun ?hTTPMethod ->
                        fun ?uRLPath ->
                          fun ?attributes ->
                            fun () ->
                              {
                                ruleName;
                                ruleARN;
                                resourceARN;
                                priority;
                                fixedRate;
                                reservoirSize;
                                host;
                                serviceName;
                                serviceType;
                                hTTPMethod;
                                uRLPath;
                                attributes
                              }
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:RuleName.to_value));
        ("RuleARN", (Option.map x.ruleARN ~f:String_.to_value));
        ("ResourceARN", (Option.map x.resourceARN ~f:ResourceARN.to_value));
        ("Priority", (Option.map x.priority ~f:NullableInteger.to_value));
        ("FixedRate", (Option.map x.fixedRate ~f:NullableDouble.to_value));
        ("ReservoirSize",
          (Option.map x.reservoirSize ~f:NullableInteger.to_value));
        ("Host", (Option.map x.host ~f:Host.to_value));
        ("ServiceName", (Option.map x.serviceName ~f:ServiceName.to_value));
        ("ServiceType", (Option.map x.serviceType ~f:ServiceType.to_value));
        ("HTTPMethod", (Option.map x.hTTPMethod ~f:HTTPMethod.to_value));
        ("URLPath", (Option.map x.uRLPath ~f:URLPath.to_value));
        ("Attributes", (Option.map x.attributes ~f:AttributeMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        (Option.map ~f:AttributeMap.of_xml) (Xml.child xml_arg0 "Attributes") in
      let uRLPath =
        (Option.map ~f:URLPath.of_xml) (Xml.child xml_arg0 "URLPath") in
      let hTTPMethod =
        (Option.map ~f:HTTPMethod.of_xml) (Xml.child xml_arg0 "HTTPMethod") in
      let serviceType =
        (Option.map ~f:ServiceType.of_xml) (Xml.child xml_arg0 "ServiceType") in
      let serviceName =
        (Option.map ~f:ServiceName.of_xml) (Xml.child xml_arg0 "ServiceName") in
      let host = (Option.map ~f:Host.of_xml) (Xml.child xml_arg0 "Host") in
      let reservoirSize =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "ReservoirSize") in
      let fixedRate =
        (Option.map ~f:NullableDouble.of_xml)
          (Xml.child xml_arg0 "FixedRate") in
      let priority =
        (Option.map ~f:NullableInteger.of_xml)
          (Xml.child xml_arg0 "Priority") in
      let resourceARN =
        (Option.map ~f:ResourceARN.of_xml) (Xml.child xml_arg0 "ResourceARN") in
      let ruleARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleARN") in
      let ruleName =
        (Option.map ~f:RuleName.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?attributes ?uRLPath ?hTTPMethod ?serviceType ?serviceName ?host
        ?reservoirSize ?fixedRate ?priority ?resourceARN ?ruleARN ?ruleName
        ()
    let of_json json =
      let attributes = field_map json "Attributes" AttributeMap.of_json in
      let uRLPath = field_map json "URLPath" URLPath.of_json in
      let hTTPMethod = field_map json "HTTPMethod" HTTPMethod.of_json in
      let serviceType = field_map json "ServiceType" ServiceType.of_json in
      let serviceName = field_map json "ServiceName" ServiceName.of_json in
      let host = field_map json "Host" Host.of_json in
      let reservoirSize =
        field_map json "ReservoirSize" NullableInteger.of_json in
      let fixedRate = field_map json "FixedRate" NullableDouble.of_json in
      let priority = field_map json "Priority" NullableInteger.of_json in
      let resourceARN = field_map json "ResourceARN" ResourceARN.of_json in
      let ruleARN = field_map json "RuleARN" String_.of_json in
      let ruleName = field_map json "RuleName" RuleName.of_json in
      make ?attributes ?uRLPath ?hTTPMethod ?serviceType ?serviceName ?host
        ?reservoirSize ?fixedRate ?priority ?resourceARN ?ruleARN ?ruleName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A document specifying changes to a sampling rule's configuration."]
module Group =
  struct
    type nonrec t =
      {
      groupName: String_.t option
        [@ocaml.doc "The unique case-sensitive name of the group."];
      groupARN: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the group generated based on the GroupName."];
      filterExpression: String_.t option
        [@ocaml.doc
          "The filter expression defining the parameters to include traces."];
      insightsConfiguration: InsightsConfiguration.t option
        [@ocaml.doc
          "The structure containing configurations related to insights. The InsightsEnabled boolean can be set to true to enable insights for the group or false to disable insights for the group. The NotificationsEnabled boolean can be set to true to enable insights notifications through Amazon EventBridge for the group."]}
    let make ?groupName =
      fun ?groupARN ->
        fun ?filterExpression ->
          fun ?insightsConfiguration ->
            fun () ->
              { groupName; groupARN; filterExpression; insightsConfiguration
              }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:String_.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:String_.to_value));
        ("FilterExpression",
          (Option.map x.filterExpression ~f:String_.to_value));
        ("InsightsConfiguration",
          (Option.map x.insightsConfiguration
             ~f:InsightsConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let insightsConfiguration =
        (Option.map ~f:InsightsConfiguration.of_xml)
          (Xml.child xml_arg0 "InsightsConfiguration") in
      let filterExpression =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "FilterExpression") in
      let groupARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let groupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?insightsConfiguration ?filterExpression ?groupARN ?groupName ()
    let of_json json =
      let insightsConfiguration =
        field_map json "InsightsConfiguration" InsightsConfiguration.of_json in
      let filterExpression =
        field_map json "FilterExpression" String_.of_json in
      let groupARN = field_map json "GroupARN" String_.of_json in
      let groupName = field_map json "GroupName" String_.of_json in
      make ?insightsConfiguration ?filterExpression ?groupARN ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details and metadata for a group."]
module FilterExpression =
  struct
    type nonrec t = string
    let context_ = "FilterExpression"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FilterExpression" j
    let to_json = simple_to_json to_value
  end
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceName: AmazonResourceName.t option }
    let make ?message =
      fun ?resourceName -> fun () -> { message; resourceName }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("ResourceName",
          (Option.map x.resourceName ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "ResourceName") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceName ?message ()
    let of_json json =
      let resourceName =
        field_map json "ResourceName" AmazonResourceName.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceName ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The resource was not found. Verify that the name or Amazon Resource Name (ARN) of the resource is correct."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TooManyTagsException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceName: AmazonResourceName.t option }
    let make ?message =
      fun ?resourceName -> fun () -> { message; resourceName }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("ResourceName",
          (Option.map x.resourceName ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "ResourceName") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceName ?message ()
    let of_json json =
      let resourceName =
        field_map json "ResourceName" AmazonResourceName.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceName ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You have exceeded the maximum number of tags you can apply to this resource."]
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module UnprocessedTraceSegmentList =
  struct
    type nonrec t = UnprocessedTraceSegment.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:UnprocessedTraceSegment.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UnprocessedTraceSegment.of_xml)
    let of_json j =
      list_of_json ~kind:"UnprocessedTraceSegmentList"
        ~of_json:UnprocessedTraceSegment.of_json j
    let to_json v = composed_to_json to_value v
  end
module TraceSegmentDocumentList =
  struct
    type nonrec t = TraceSegmentDocument.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TraceSegmentDocument.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TraceSegmentDocument.of_xml)
    let of_json j =
      list_of_json ~kind:"TraceSegmentDocumentList"
        ~of_json:TraceSegmentDocument.of_json j
    let to_json v = composed_to_json to_value v
  end
module EC2InstanceId =
  struct
    type nonrec t = string
    let context_ = "EC2InstanceId"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:20); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EC2InstanceId" j
    let to_json = simple_to_json to_value
  end
module Hostname =
  struct
    type nonrec t = string
    let context_ = "Hostname"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:255); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Hostname" j
    let to_json = simple_to_json to_value
  end
module TelemetryRecordList =
  struct
    type nonrec t = TelemetryRecord.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TelemetryRecord.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TelemetryRecord.of_xml)
    let of_json j =
      list_of_json ~kind:"TelemetryRecordList"
        ~of_json:TelemetryRecord.of_json j
    let to_json v = composed_to_json to_value v
  end
module EncryptionConfig =
  struct
    type nonrec t =
      {
      keyId: String_.t option
        [@ocaml.doc
          "The ID of the KMS key used for encryption, if applicable."];
      status: EncryptionStatus.t option
        [@ocaml.doc
          "The encryption status. While the status is UPDATING, X-Ray may encrypt data with a combination of the new and old settings."];
      type_: EncryptionType.t option
        [@ocaml.doc
          "The type of encryption. Set to KMS for encryption with KMS keys. Set to NONE for default encryption."]}
    let make ?keyId =
      fun ?status -> fun ?type_ -> fun () -> { keyId; status; type_ }
    let to_value x =
      structure_to_value
        [("KeyId", (Option.map x.keyId ~f:String_.to_value));
        ("Status", (Option.map x.status ~f:EncryptionStatus.to_value));
        ("Type", (Option.map x.type_ ~f:EncryptionType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:EncryptionType.of_xml) (Xml.child xml_arg0 "Type") in
      let status =
        (Option.map ~f:EncryptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let keyId = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "KeyId") in
      make ?type_ ?status ?keyId ()
    let of_json json =
      let type_ = field_map json "Type" EncryptionType.of_json in
      let status = field_map json "Status" EncryptionStatus.of_json in
      let keyId = field_map json "KeyId" String_.of_json in
      make ?type_ ?status ?keyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A configuration document that specifies encryption configuration settings."]
module EncryptionKeyId =
  struct
    type nonrec t = string
    let context_ = "EncryptionKeyId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:3000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EncryptionKeyId" j
    let to_json = simple_to_json to_value
  end
module TraceSummaryList =
  struct
    type nonrec t = TraceSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TraceSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TraceSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TraceSummaryList" ~of_json:TraceSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module SamplingStrategy =
  struct
    type nonrec t =
      {
      name: SamplingStrategyName.t option
        [@ocaml.doc "The name of a sampling rule."];
      value: NullableDouble.t option
        [@ocaml.doc "The value of a sampling rule."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:SamplingStrategyName.to_value));
        ("Value", (Option.map x.value ~f:NullableDouble.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:NullableDouble.of_xml) (Xml.child xml_arg0 "Value") in
      let name =
        (Option.map ~f:SamplingStrategyName.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?value ?name ()
    let of_json json =
      let value = field_map json "Value" NullableDouble.of_json in
      let name = field_map json "Name" SamplingStrategyName.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The name and value of a sampling rule to apply to a trace summary."]
module TimeRangeType =
  struct
    type nonrec t =
      | TraceId 
      | Event 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TraceId -> "TraceId"
      | Event -> "Event"
      | Non_static_id s -> s
    let of_string =
      function
      | "TraceId" -> TraceId
      | "Event" -> Event
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TimeRangeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TimeRangeType" j)
    let to_json = simple_to_json to_value
  end
module ServiceList =
  struct
    type nonrec t = Service.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Service.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Service.of_xml)
    let of_json j =
      list_of_json ~kind:"ServiceList" ~of_json:Service.of_json j
    let to_json v = composed_to_json to_value v
  end
module TraceIdList =
  struct
    type nonrec t = TraceId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TraceId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TraceId.of_xml)
    let of_json j =
      list_of_json ~kind:"TraceIdList" ~of_json:TraceId.of_json j
    let to_json v = composed_to_json to_value v
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module TimeSeriesServiceStatisticsList =
  struct
    type nonrec t = TimeSeriesServiceStatistics.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TimeSeriesServiceStatistics.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TimeSeriesServiceStatistics.of_xml)
    let of_json j =
      list_of_json ~kind:"TimeSeriesServiceStatisticsList"
        ~of_json:TimeSeriesServiceStatistics.of_json j
    let to_json v = composed_to_json to_value v
  end
module EntitySelectorExpression =
  struct
    type nonrec t = string
    let context_ = "EntitySelectorExpression"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:500) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EntitySelectorExpression" j
    let to_json = simple_to_json to_value
  end
module SamplingTargetDocumentList =
  struct
    type nonrec t = SamplingTargetDocument.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SamplingTargetDocument.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SamplingTargetDocument.of_xml)
    let of_json j =
      list_of_json ~kind:"SamplingTargetDocumentList"
        ~of_json:SamplingTargetDocument.of_json j
    let to_json v = composed_to_json to_value v
  end
module UnprocessedStatisticsList =
  struct
    type nonrec t = UnprocessedStatistics.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:UnprocessedStatistics.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UnprocessedStatistics.of_xml)
    let of_json j =
      list_of_json ~kind:"UnprocessedStatisticsList"
        ~of_json:UnprocessedStatistics.of_json j
    let to_json v = composed_to_json to_value v
  end
module SamplingStatisticsDocumentList =
  struct
    type nonrec t = SamplingStatisticsDocument.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:25); i
    let to_value xs =
      (xs |> (List.map ~f:SamplingStatisticsDocument.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SamplingStatisticsDocument.of_xml)
    let of_json j =
      list_of_json ~kind:"SamplingStatisticsDocumentList"
        ~of_json:SamplingStatisticsDocument.of_json j
    let to_json v = composed_to_json to_value v
  end
module SamplingStatisticSummaryList =
  struct
    type nonrec t = SamplingStatisticSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SamplingStatisticSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SamplingStatisticSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SamplingStatisticSummaryList"
        ~of_json:SamplingStatisticSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module SamplingRuleRecordList =
  struct
    type nonrec t = SamplingRuleRecord.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SamplingRuleRecord.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SamplingRuleRecord.of_xml)
    let of_json j =
      list_of_json ~kind:"SamplingRuleRecordList"
        ~of_json:SamplingRuleRecord.of_json j
    let to_json v = composed_to_json to_value v
  end
module InsightSummaryList =
  struct
    type nonrec t = InsightSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InsightSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InsightSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"InsightSummaryList" ~of_json:InsightSummary.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Token =
  struct
    type nonrec t = string
    let context_ = "Token"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Token" j
    let to_json = simple_to_json to_value
  end
module GetInsightSummariesMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for GetInsightSummariesMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module InsightStateList =
  struct
    type nonrec t = InsightState.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:InsightState.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InsightState.of_xml)
    let of_json j =
      list_of_json ~kind:"InsightStateList" ~of_json:InsightState.of_json j
    let to_json v = composed_to_json to_value v
  end
module Insight =
  struct
    type nonrec t =
      {
      insightId: InsightId.t option
        [@ocaml.doc "The insights unique identifier."];
      groupARN: GroupARN.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the group that the insight belongs to."];
      groupName: GroupName.t option
        [@ocaml.doc "The name of the group that the insight belongs to."];
      rootCauseServiceId: ServiceId.t option ;
      categories: InsightCategoryList.t option
        [@ocaml.doc
          "The categories that label and describe the type of insight."];
      state: InsightState.t option
        [@ocaml.doc "The current state of the insight."];
      startTime: Timestamp.t option
        [@ocaml.doc "The time, in Unix seconds, at which the insight began."];
      endTime: Timestamp.t option
        [@ocaml.doc "The time, in Unix seconds, at which the insight ended."];
      summary: InsightSummaryText.t option
        [@ocaml.doc "A brief description of the insight."];
      clientRequestImpactStatistics: RequestImpactStatistics.t option
        [@ocaml.doc
          "The impact statistics of the client side service. This includes the number of requests to the client service and whether the requests were faults or okay."];
      rootCauseServiceRequestImpactStatistics:
        RequestImpactStatistics.t option
        [@ocaml.doc
          "The impact statistics of the root cause service. This includes the number of requests to the client service and whether the requests were faults or okay."];
      topAnomalousServices: AnomalousServiceList.t option
        [@ocaml.doc
          "The service within the insight that is most impacted by the incident."]}
    let make ?insightId =
      fun ?groupARN ->
        fun ?groupName ->
          fun ?rootCauseServiceId ->
            fun ?categories ->
              fun ?state ->
                fun ?startTime ->
                  fun ?endTime ->
                    fun ?summary ->
                      fun ?clientRequestImpactStatistics ->
                        fun ?rootCauseServiceRequestImpactStatistics ->
                          fun ?topAnomalousServices ->
                            fun () ->
                              {
                                insightId;
                                groupARN;
                                groupName;
                                rootCauseServiceId;
                                categories;
                                state;
                                startTime;
                                endTime;
                                summary;
                                clientRequestImpactStatistics;
                                rootCauseServiceRequestImpactStatistics;
                                topAnomalousServices
                              }
    let to_value x =
      structure_to_value
        [("InsightId", (Option.map x.insightId ~f:InsightId.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:GroupARN.to_value));
        ("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("RootCauseServiceId",
          (Option.map x.rootCauseServiceId ~f:ServiceId.to_value));
        ("Categories",
          (Option.map x.categories ~f:InsightCategoryList.to_value));
        ("State", (Option.map x.state ~f:InsightState.to_value));
        ("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("Summary", (Option.map x.summary ~f:InsightSummaryText.to_value));
        ("ClientRequestImpactStatistics",
          (Option.map x.clientRequestImpactStatistics
             ~f:RequestImpactStatistics.to_value));
        ("RootCauseServiceRequestImpactStatistics",
          (Option.map x.rootCauseServiceRequestImpactStatistics
             ~f:RequestImpactStatistics.to_value));
        ("TopAnomalousServices",
          (Option.map x.topAnomalousServices ~f:AnomalousServiceList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topAnomalousServices =
        (Option.map ~f:AnomalousServiceList.of_xml)
          (Xml.child xml_arg0 "TopAnomalousServices") in
      let rootCauseServiceRequestImpactStatistics =
        (Option.map ~f:RequestImpactStatistics.of_xml)
          (Xml.child xml_arg0 "RootCauseServiceRequestImpactStatistics") in
      let clientRequestImpactStatistics =
        (Option.map ~f:RequestImpactStatistics.of_xml)
          (Xml.child xml_arg0 "ClientRequestImpactStatistics") in
      let summary =
        (Option.map ~f:InsightSummaryText.of_xml)
          (Xml.child xml_arg0 "Summary") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let state =
        (Option.map ~f:InsightState.of_xml) (Xml.child xml_arg0 "State") in
      let categories =
        (Option.map ~f:InsightCategoryList.of_xml)
          (Xml.child xml_arg0 "Categories") in
      let rootCauseServiceId =
        (Option.map ~f:ServiceId.of_xml)
          (Xml.child xml_arg0 "RootCauseServiceId") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      let groupARN =
        (Option.map ~f:GroupARN.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let insightId =
        (Option.map ~f:InsightId.of_xml) (Xml.child xml_arg0 "InsightId") in
      make ?topAnomalousServices ?rootCauseServiceRequestImpactStatistics
        ?clientRequestImpactStatistics ?summary ?endTime ?startTime ?state
        ?categories ?rootCauseServiceId ?groupName ?groupARN ?insightId ()
    let of_json json =
      let topAnomalousServices =
        field_map json "TopAnomalousServices" AnomalousServiceList.of_json in
      let rootCauseServiceRequestImpactStatistics =
        field_map json "RootCauseServiceRequestImpactStatistics"
          RequestImpactStatistics.of_json in
      let clientRequestImpactStatistics =
        field_map json "ClientRequestImpactStatistics"
          RequestImpactStatistics.of_json in
      let summary = field_map json "Summary" InsightSummaryText.of_json in
      let endTime = field_map json "EndTime" Timestamp.of_json in
      let startTime = field_map json "StartTime" Timestamp.of_json in
      let state = field_map json "State" InsightState.of_json in
      let categories =
        field_map json "Categories" InsightCategoryList.of_json in
      let rootCauseServiceId =
        field_map json "RootCauseServiceId" ServiceId.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      let groupARN = field_map json "GroupARN" GroupARN.of_json in
      let insightId = field_map json "InsightId" InsightId.of_json in
      make ?topAnomalousServices ?rootCauseServiceRequestImpactStatistics
        ?clientRequestImpactStatistics ?summary ?endTime ?startTime ?state
        ?categories ?rootCauseServiceId ?groupName ?groupARN ?insightId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "When fault rates go outside of the expected range, X-Ray creates an insight. Insights tracks emergent issues within your applications."]
module InsightImpactGraphServiceList =
  struct
    type nonrec t = InsightImpactGraphService.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InsightImpactGraphService.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InsightImpactGraphService.of_xml)
    let of_json j =
      list_of_json ~kind:"InsightImpactGraphServiceList"
        ~of_json:InsightImpactGraphService.of_json j
    let to_json v = composed_to_json to_value v
  end
module InsightEventList =
  struct
    type nonrec t = InsightEvent.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InsightEvent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InsightEvent.of_xml)
    let of_json j =
      list_of_json ~kind:"InsightEventList" ~of_json:InsightEvent.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetInsightEventsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for GetInsightEventsMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module GroupSummaryList =
  struct
    type nonrec t = GroupSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:GroupSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GroupSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"GroupSummaryList" ~of_json:GroupSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetGroupsNextToken =
  struct
    type nonrec t = string
    let context_ = "GetGroupsNextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GetGroupsNextToken" j
    let to_json = simple_to_json to_value
  end
module RuleLimitExceededException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "You have reached the maximum number of sampling rules."]
module TraceList =
  struct
    type nonrec t = Trace.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Trace.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Trace.of_xml)
    let of_json j = list_of_json ~kind:"TraceList" ~of_json:Trace.of_json j
    let to_json v = composed_to_json to_value v
  end
module UnprocessedTraceIdList =
  struct
    type nonrec t = TraceId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TraceId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TraceId.of_xml)
    let of_json j =
      list_of_json ~kind:"UnprocessedTraceIdList" ~of_json:TraceId.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateSamplingRuleResult =
  struct
    type nonrec t =
      {
      samplingRuleRecord: SamplingRuleRecord.t option
        [@ocaml.doc "The updated rule definition and metadata."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?samplingRuleRecord = fun () -> { samplingRuleRecord }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SamplingRuleRecord",
           (Option.map x.samplingRuleRecord ~f:SamplingRuleRecord.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let samplingRuleRecord =
        (Option.map ~f:SamplingRuleRecord.of_xml)
          (Xml.child xml_arg0 "SamplingRuleRecord") in
      make ?samplingRuleRecord ()
    let of_json json =
      let samplingRuleRecord =
        field_map json "SamplingRuleRecord" SamplingRuleRecord.of_json in
      make ?samplingRuleRecord ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies a sampling rule's configuration."]
module UpdateSamplingRuleRequest =
  struct
    type nonrec t =
      {
      samplingRuleUpdate: SamplingRuleUpdate.t
        [@ocaml.doc "The rule and fields to change."]}
    let context_ = "UpdateSamplingRuleRequest"
    let make ~samplingRuleUpdate = fun () -> { samplingRuleUpdate }
    let to_value x =
      structure_to_value
        [("SamplingRuleUpdate",
           (Some (SamplingRuleUpdate.to_value x.samplingRuleUpdate)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let samplingRuleUpdate =
        SamplingRuleUpdate.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SamplingRuleUpdate") in
      make ~samplingRuleUpdate ()
    let of_json json =
      let samplingRuleUpdate =
        field_map_exn json "SamplingRuleUpdate" SamplingRuleUpdate.of_json in
      make ~samplingRuleUpdate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Modifies a sampling rule's configuration."]
module UpdateGroupResult =
  struct
    type nonrec t =
      {
      group: Group.t option
        [@ocaml.doc
          "The group that was updated. Contains the name of the group that was updated, the ARN of the group that was updated, the updated filter expression, and the updated insight configuration assigned to the group."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?group = fun () -> { group }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Group", (Option.map x.group ~f:Group.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group = (Option.map ~f:Group.of_xml) (Xml.child xml_arg0 "Group") in
      make ?group ()
    let of_json json =
      let group = field_map json "Group" Group.of_json in make ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates a group resource."]
module UpdateGroupRequest =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc "The case-sensitive name of the group."];
      groupARN: GroupARN.t option
        [@ocaml.doc "The ARN that was generated upon creation."];
      filterExpression: FilterExpression.t option
        [@ocaml.doc
          "The updated filter expression defining criteria by which to group traces."];
      insightsConfiguration: InsightsConfiguration.t option
        [@ocaml.doc
          "The structure containing configurations related to insights. The InsightsEnabled boolean can be set to true to enable insights for the group or false to disable insights for the group. The NotifcationsEnabled boolean can be set to true to enable insights notifications for the group. Notifications can only be enabled on a group with InsightsEnabled set to true."]}
    let make ?groupName =
      fun ?groupARN ->
        fun ?filterExpression ->
          fun ?insightsConfiguration ->
            fun () ->
              { groupName; groupARN; filterExpression; insightsConfiguration
              }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:GroupARN.to_value));
        ("FilterExpression",
          (Option.map x.filterExpression ~f:FilterExpression.to_value));
        ("InsightsConfiguration",
          (Option.map x.insightsConfiguration
             ~f:InsightsConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let insightsConfiguration =
        (Option.map ~f:InsightsConfiguration.of_xml)
          (Xml.child xml_arg0 "InsightsConfiguration") in
      let filterExpression =
        (Option.map ~f:FilterExpression.of_xml)
          (Xml.child xml_arg0 "FilterExpression") in
      let groupARN =
        (Option.map ~f:GroupARN.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?insightsConfiguration ?filterExpression ?groupARN ?groupName ()
    let of_json json =
      let insightsConfiguration =
        field_map json "InsightsConfiguration" InsightsConfiguration.of_json in
      let filterExpression =
        field_map json "FilterExpression" FilterExpression.of_json in
      let groupARN = field_map json "GroupARN" GroupARN.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?insightsConfiguration ?filterExpression ?groupARN ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates a group resource."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes tags from an Amazon Web Services X-Ray group or sampling rule. You cannot edit or delete system tags (those with an aws: prefix)."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Number (ARN) of an X-Ray group or sampling rule."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "Keys for one or more tags that you want to remove from an X-Ray group or sampling rule."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceARN =
      fun ~tagKeys -> fun () -> { resourceARN; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (AmazonResourceName.to_value x.resourceARN)));
        ("TagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resourceARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tagKeys ~resourceARN ()
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let resourceARN =
        field_map_exn json "ResourceARN" AmazonResourceName.of_json in
      make ~tagKeys ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes tags from an Amazon Web Services X-Ray group or sampling rule. You cannot edit or delete system tags (those with an aws: prefix)."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottledException of ThrottledException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Applies tags to an existing Amazon Web Services X-Ray group or sampling rule."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Number (ARN) of an X-Ray group or sampling rule."];
      tags: TagList.t
        [@ocaml.doc
          "A map that contains one or more tag keys and tag values to attach to an X-Ray group or sampling rule. For more information about ways to use tags, see Tagging Amazon Web Services resources in the Amazon Web Services General Reference. The following restrictions apply to tags: Maximum number of user-applied tags per resource: 50 Maximum tag key length: 128 Unicode characters Maximum tag value length: 256 Unicode characters Valid values for key and value: a-z, A-Z, 0-9, space, and the following characters: _ . : / = + - and \\@ Tag keys and values are case sensitive. Don't use aws: as a prefix for keys; it's reserved for Amazon Web Services use. You cannot edit or delete system tags."]}
    let context_ = "TagResourceRequest"
    let make ~resourceARN = fun ~tags -> fun () -> { resourceARN; tags }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (AmazonResourceName.to_value x.resourceARN)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tags ~resourceARN ()
    let of_json json =
      let tags = field_map_exn json "Tags" TagList.of_json in
      let resourceARN =
        field_map_exn json "ResourceARN" AmazonResourceName.of_json in
      make ~tags ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Applies tags to an existing Amazon Web Services X-Ray group or sampling rule."]
module PutTraceSegmentsResult =
  struct
    type nonrec t =
      {
      unprocessedTraceSegments: UnprocessedTraceSegmentList.t option
        [@ocaml.doc "Segments that failed processing."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?unprocessedTraceSegments =
      fun () -> { unprocessedTraceSegments }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("UnprocessedTraceSegments",
           (Option.map x.unprocessedTraceSegments
              ~f:UnprocessedTraceSegmentList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unprocessedTraceSegments =
        (Option.map ~f:UnprocessedTraceSegmentList.of_xml)
          (Xml.child xml_arg0 "UnprocessedTraceSegments") in
      make ?unprocessedTraceSegments ()
    let of_json json =
      let unprocessedTraceSegments =
        field_map json "UnprocessedTraceSegments"
          UnprocessedTraceSegmentList.of_json in
      make ?unprocessedTraceSegments ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Uploads segment documents to Amazon Web Services X-Ray. The X-Ray SDK generates segment documents and sends them to the X-Ray daemon, which uploads them in batches. A segment document can be a completed segment, an in-progress segment, or an array of subsegments. Segments must include the following fields. For the full segment document schema, see Amazon Web Services X-Ray Segment Documents in the Amazon Web Services X-Ray Developer Guide. Required segment document fields name - The name of the service that handled the request. id - A 64-bit identifier for the segment, unique among segments in the same trace, in 16 hexadecimal digits. trace_id - A unique identifier that connects all segments and subsegments originating from a single client request. start_time - Time the segment or subsegment was created, in floating point seconds in epoch time, accurate to milliseconds. For example, 1480615200.010 or 1.480615200010E9. end_time - Time the segment or subsegment was closed. For example, 1480615200.090 or 1.480615200090E9. Specify either an end_time or in_progress. in_progress - Set to true instead of specifying an end_time to record that a segment has been started, but is not complete. Send an in-progress segment when your application receives a request that will take a long time to serve, to trace that the request was received. When the response is sent, send the complete segment to overwrite the in-progress segment. A trace_id consists of three numbers separated by hyphens. For example, 1-58406520-a006649127e371903a2de979. This includes: Trace ID Format The version number, for instance, 1. The time of the original request, in Unix epoch time, in 8 hexadecimal digits. For example, 10:00AM December 2nd, 2016 PST in epoch time is 1480615200 seconds, or 58406520 in hexadecimal. A 96-bit identifier for the trace, globally unique, in 24 hexadecimal digits."]
module PutTraceSegmentsRequest =
  struct
    type nonrec t =
      {
      traceSegmentDocuments: TraceSegmentDocumentList.t
        [@ocaml.doc
          "A string containing a JSON document defining one or more segments or subsegments."]}
    let context_ = "PutTraceSegmentsRequest"
    let make ~traceSegmentDocuments = fun () -> { traceSegmentDocuments }
    let to_value x =
      structure_to_value
        [("TraceSegmentDocuments",
           (Some (TraceSegmentDocumentList.to_value x.traceSegmentDocuments)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceSegmentDocuments =
        TraceSegmentDocumentList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TraceSegmentDocuments") in
      make ~traceSegmentDocuments ()
    let of_json json =
      let traceSegmentDocuments =
        field_map_exn json "TraceSegmentDocuments"
          TraceSegmentDocumentList.of_json in
      make ~traceSegmentDocuments ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Uploads segment documents to Amazon Web Services X-Ray. The X-Ray SDK generates segment documents and sends them to the X-Ray daemon, which uploads them in batches. A segment document can be a completed segment, an in-progress segment, or an array of subsegments. Segments must include the following fields. For the full segment document schema, see Amazon Web Services X-Ray Segment Documents in the Amazon Web Services X-Ray Developer Guide. Required segment document fields name - The name of the service that handled the request. id - A 64-bit identifier for the segment, unique among segments in the same trace, in 16 hexadecimal digits. trace_id - A unique identifier that connects all segments and subsegments originating from a single client request. start_time - Time the segment or subsegment was created, in floating point seconds in epoch time, accurate to milliseconds. For example, 1480615200.010 or 1.480615200010E9. end_time - Time the segment or subsegment was closed. For example, 1480615200.090 or 1.480615200090E9. Specify either an end_time or in_progress. in_progress - Set to true instead of specifying an end_time to record that a segment has been started, but is not complete. Send an in-progress segment when your application receives a request that will take a long time to serve, to trace that the request was received. When the response is sent, send the complete segment to overwrite the in-progress segment. A trace_id consists of three numbers separated by hyphens. For example, 1-58406520-a006649127e371903a2de979. This includes: Trace ID Format The version number, for instance, 1. The time of the original request, in Unix epoch time, in 8 hexadecimal digits. For example, 10:00AM December 2nd, 2016 PST in epoch time is 1480615200 seconds, or 58406520 in hexadecimal. A 96-bit identifier for the trace, globally unique, in 24 hexadecimal digits."]
module PutTelemetryRecordsResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Used by the Amazon Web Services X-Ray daemon to upload telemetry."]
module PutTelemetryRecordsRequest =
  struct
    type nonrec t =
      {
      telemetryRecords: TelemetryRecordList.t ;
      eC2InstanceId: EC2InstanceId.t option ;
      hostname: Hostname.t option ;
      resourceARN: ResourceARN.t option }
    let context_ = "PutTelemetryRecordsRequest"
    let make ?eC2InstanceId =
      fun ?hostname ->
        fun ?resourceARN ->
          fun ~telemetryRecords ->
            fun () ->
              { eC2InstanceId; hostname; resourceARN; telemetryRecords }
    let to_value x =
      structure_to_value
        [("TelemetryRecords",
           (Some (TelemetryRecordList.to_value x.telemetryRecords)));
        ("EC2InstanceId",
          (Option.map x.eC2InstanceId ~f:EC2InstanceId.to_value));
        ("Hostname", (Option.map x.hostname ~f:Hostname.to_value));
        ("ResourceARN", (Option.map x.resourceARN ~f:ResourceARN.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceARN =
        (Option.map ~f:ResourceARN.of_xml) (Xml.child xml_arg0 "ResourceARN") in
      let hostname =
        (Option.map ~f:Hostname.of_xml) (Xml.child xml_arg0 "Hostname") in
      let eC2InstanceId =
        (Option.map ~f:EC2InstanceId.of_xml)
          (Xml.child xml_arg0 "EC2InstanceId") in
      let telemetryRecords =
        TelemetryRecordList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TelemetryRecords") in
      make ?resourceARN ?hostname ?eC2InstanceId ~telemetryRecords ()
    let of_json json =
      let resourceARN = field_map json "ResourceARN" ResourceARN.of_json in
      let hostname = field_map json "Hostname" Hostname.of_json in
      let eC2InstanceId =
        field_map json "EC2InstanceId" EC2InstanceId.of_json in
      let telemetryRecords =
        field_map_exn json "TelemetryRecords" TelemetryRecordList.of_json in
      make ?resourceARN ?hostname ?eC2InstanceId ~telemetryRecords ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Used by the Amazon Web Services X-Ray daemon to upload telemetry."]
module PutEncryptionConfigResult =
  struct
    type nonrec t =
      {
      encryptionConfig: EncryptionConfig.t option
        [@ocaml.doc "The new encryption configuration."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?encryptionConfig = fun () -> { encryptionConfig }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("EncryptionConfig",
           (Option.map x.encryptionConfig ~f:EncryptionConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionConfig =
        (Option.map ~f:EncryptionConfig.of_xml)
          (Xml.child xml_arg0 "EncryptionConfig") in
      make ?encryptionConfig ()
    let of_json json =
      let encryptionConfig =
        field_map json "EncryptionConfig" EncryptionConfig.of_json in
      make ?encryptionConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the encryption configuration for X-Ray data."]
module PutEncryptionConfigRequest =
  struct
    type nonrec t =
      {
      keyId: EncryptionKeyId.t option
        [@ocaml.doc
          "An Amazon Web Services KMS key in one of the following formats: Alias - The name of the key. For example, alias/MyKey. Key ID - The KMS key ID of the key. For example, ae4aa6d49-a4d8-9df9-a475-4ff6d7898456. Amazon Web Services X-Ray does not support asymmetric KMS keys. ARN - The full Amazon Resource Name of the key ID or alias. For example, arn:aws:kms:us-east-2:123456789012:key/ae4aa6d49-a4d8-9df9-a475-4ff6d7898456. Use this format to specify a key in a different account. Omit this key if you set Type to NONE."];
      type_: EncryptionType.t
        [@ocaml.doc
          "The type of encryption. Set to KMS to use your own key for encryption. Set to NONE for default encryption."]}
    let context_ = "PutEncryptionConfigRequest"
    let make ?keyId = fun ~type_ -> fun () -> { keyId; type_ }
    let to_value x =
      structure_to_value
        [("KeyId", (Option.map x.keyId ~f:EncryptionKeyId.to_value));
        ("Type", (Some (EncryptionType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        EncryptionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let keyId =
        (Option.map ~f:EncryptionKeyId.of_xml) (Xml.child xml_arg0 "KeyId") in
      make ~type_ ?keyId ()
    let of_json json =
      let type_ = field_map_exn json "Type" EncryptionType.of_json in
      let keyId = field_map json "KeyId" EncryptionKeyId.of_json in
      make ~type_ ?keyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the encryption configuration for X-Ray data."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagList.t option
        [@ocaml.doc
          "A list of tags, as key and value pairs, that is associated with the specified X-Ray group or sampling rule."];
      nextToken: String_.t option
        [@ocaml.doc
          "A pagination token. If multiple pages of results are returned, use the NextToken value returned with the current page of results to get the next page of results."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun ?nextToken -> fun () -> { tags; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?nextToken ?tags ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let tags = field_map json "Tags" TagList.of_json in
      make ?nextToken ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of tags that are applied to the specified Amazon Web Services X-Ray group or sampling rule."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Number (ARN) of an X-Ray group or sampling rule."];
      nextToken: String_.t option
        [@ocaml.doc
          "A pagination token. If multiple pages of results are returned, use the NextToken value returned with the current page of results as the value of this parameter to get the next page of results."]}
    let context_ = "ListTagsForResourceRequest"
    let make ?nextToken =
      fun ~resourceARN -> fun () -> { nextToken; resourceARN }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (AmazonResourceName.to_value x.resourceARN)));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let resourceARN =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ?nextToken ~resourceARN ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let resourceARN =
        field_map_exn json "ResourceARN" AmazonResourceName.of_json in
      make ?nextToken ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of tags that are applied to the specified Amazon Web Services X-Ray group or sampling rule."]
module GetTraceSummariesResult =
  struct
    type nonrec t =
      {
      traceSummaries: TraceSummaryList.t option
        [@ocaml.doc
          "Trace IDs and annotations for traces that were found in the specified time frame."];
      approximateTime: Timestamp.t option
        [@ocaml.doc "The start time of this page of results."];
      tracesProcessedCount: NullableLong.t option
        [@ocaml.doc
          "The total number of traces processed, including traces that did not match the specified filter expression."];
      nextToken: String_.t option
        [@ocaml.doc
          "If the requested time frame contained more than one page of results, you can use this token to retrieve the next page. The first page contains the most recent results, closest to the end of the time frame."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?traceSummaries =
      fun ?approximateTime ->
        fun ?tracesProcessedCount ->
          fun ?nextToken ->
            fun () ->
              {
                traceSummaries;
                approximateTime;
                tracesProcessedCount;
                nextToken
              }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TraceSummaries",
           (Option.map x.traceSummaries ~f:TraceSummaryList.to_value));
        ("ApproximateTime",
          (Option.map x.approximateTime ~f:Timestamp.to_value));
        ("TracesProcessedCount",
          (Option.map x.tracesProcessedCount ~f:NullableLong.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let tracesProcessedCount =
        (Option.map ~f:NullableLong.of_xml)
          (Xml.child xml_arg0 "TracesProcessedCount") in
      let approximateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ApproximateTime") in
      let traceSummaries =
        (Option.map ~f:TraceSummaryList.of_xml)
          (Xml.child xml_arg0 "TraceSummaries") in
      make ?nextToken ?tracesProcessedCount ?approximateTime ?traceSummaries
        ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let tracesProcessedCount =
        field_map json "TracesProcessedCount" NullableLong.of_json in
      let approximateTime =
        field_map json "ApproximateTime" Timestamp.of_json in
      let traceSummaries =
        field_map json "TraceSummaries" TraceSummaryList.of_json in
      make ?nextToken ?tracesProcessedCount ?approximateTime ?traceSummaries
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves IDs and annotations for traces available for a specified time frame using an optional filter. To get the full traces, pass the trace IDs to BatchGetTraces. A filter expression can target traced requests that hit specific service nodes or edges, have errors, or come from a known user. For example, the following filter expression targets traces that pass through api.example.com: service(\"api.example.com\") This filter expression finds traces that have an annotation named account with the value 12345: annotation.account = \"12345\" For a full list of indexed fields and keywords that you can use in filter expressions, see Using Filter Expressions in the Amazon Web Services X-Ray Developer Guide."]
module GetTraceSummariesRequest =
  struct
    type nonrec t =
      {
      startTime: Timestamp.t
        [@ocaml.doc
          "The start of the time frame for which to retrieve traces."];
      endTime: Timestamp.t
        [@ocaml.doc
          "The end of the time frame for which to retrieve traces."];
      timeRangeType: TimeRangeType.t option
        [@ocaml.doc
          "A parameter to indicate whether to query trace summaries by TraceId or Event time."];
      sampling: NullableBoolean.t option
        [@ocaml.doc
          "Set to true to get summaries for only a subset of available traces."];
      samplingStrategy: SamplingStrategy.t option
        [@ocaml.doc
          "A parameter to indicate whether to enable sampling on trace summaries. Input parameters are Name and Value."];
      filterExpression: FilterExpression.t option
        [@ocaml.doc
          "Specify a filter expression to retrieve trace summaries for services or requests that meet certain requirements."];
      nextToken: String_.t option
        [@ocaml.doc
          "Specify the pagination token returned by a previous request to retrieve the next page of results."]}
    let context_ = "GetTraceSummariesRequest"
    let make ?timeRangeType =
      fun ?sampling ->
        fun ?samplingStrategy ->
          fun ?filterExpression ->
            fun ?nextToken ->
              fun ~startTime ->
                fun ~endTime ->
                  fun () ->
                    {
                      timeRangeType;
                      sampling;
                      samplingStrategy;
                      filterExpression;
                      nextToken;
                      startTime;
                      endTime
                    }
    let to_value x =
      structure_to_value
        [("StartTime", (Some (Timestamp.to_value x.startTime)));
        ("EndTime", (Some (Timestamp.to_value x.endTime)));
        ("TimeRangeType",
          (Option.map x.timeRangeType ~f:TimeRangeType.to_value));
        ("Sampling", (Option.map x.sampling ~f:NullableBoolean.to_value));
        ("SamplingStrategy",
          (Option.map x.samplingStrategy ~f:SamplingStrategy.to_value));
        ("FilterExpression",
          (Option.map x.filterExpression ~f:FilterExpression.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filterExpression =
        (Option.map ~f:FilterExpression.of_xml)
          (Xml.child xml_arg0 "FilterExpression") in
      let samplingStrategy =
        (Option.map ~f:SamplingStrategy.of_xml)
          (Xml.child xml_arg0 "SamplingStrategy") in
      let sampling =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "Sampling") in
      let timeRangeType =
        (Option.map ~f:TimeRangeType.of_xml)
          (Xml.child xml_arg0 "TimeRangeType") in
      let endTime =
        Timestamp.of_xml (Xml.child_exn ~context:context_ xml_arg0 "EndTime") in
      let startTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      make ?nextToken ?filterExpression ?samplingStrategy ?sampling
        ?timeRangeType ~endTime ~startTime ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let filterExpression =
        field_map json "FilterExpression" FilterExpression.of_json in
      let samplingStrategy =
        field_map json "SamplingStrategy" SamplingStrategy.of_json in
      let sampling = field_map json "Sampling" NullableBoolean.of_json in
      let timeRangeType =
        field_map json "TimeRangeType" TimeRangeType.of_json in
      let endTime = field_map_exn json "EndTime" Timestamp.of_json in
      let startTime = field_map_exn json "StartTime" Timestamp.of_json in
      make ?nextToken ?filterExpression ?samplingStrategy ?sampling
        ?timeRangeType ~endTime ~startTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves IDs and annotations for traces available for a specified time frame using an optional filter. To get the full traces, pass the trace IDs to BatchGetTraces. A filter expression can target traced requests that hit specific service nodes or edges, have errors, or come from a known user. For example, the following filter expression targets traces that pass through api.example.com: service(\"api.example.com\") This filter expression finds traces that have an annotation named account with the value 12345: annotation.account = \"12345\" For a full list of indexed fields and keywords that you can use in filter expressions, see Using Filter Expressions in the Amazon Web Services X-Ray Developer Guide."]
module GetTraceGraphResult =
  struct
    type nonrec t =
      {
      services: ServiceList.t option
        [@ocaml.doc
          "The services that have processed one of the specified requests."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?services = fun ?nextToken -> fun () -> { services; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Services", (Option.map x.services ~f:ServiceList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let services =
        (Option.map ~f:ServiceList.of_xml) (Xml.child xml_arg0 "Services") in
      make ?nextToken ?services ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let services = field_map json "Services" ServiceList.of_json in
      make ?nextToken ?services ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a service graph for one or more specific trace IDs."]
module GetTraceGraphRequest =
  struct
    type nonrec t =
      {
      traceIds: TraceIdList.t
        [@ocaml.doc
          "Trace IDs of requests for which to generate a service graph."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    let context_ = "GetTraceGraphRequest"
    let make ?nextToken = fun ~traceIds -> fun () -> { nextToken; traceIds }
    let to_value x =
      structure_to_value
        [("TraceIds", (Some (TraceIdList.to_value x.traceIds)));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let traceIds =
        TraceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TraceIds") in
      make ?nextToken ~traceIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let traceIds = field_map_exn json "TraceIds" TraceIdList.of_json in
      make ?nextToken ~traceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a service graph for one or more specific trace IDs."]
module GetTimeSeriesServiceStatisticsResult =
  struct
    type nonrec t =
      {
      timeSeriesServiceStatistics: TimeSeriesServiceStatisticsList.t option
        [@ocaml.doc "The collection of statistics."];
      containsOldGroupVersions: Boolean.t option
        [@ocaml.doc
          "A flag indicating whether or not a group's filter expression has been consistent, or if a returned aggregation might show statistics from an older version of the group's filter expression."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?timeSeriesServiceStatistics =
      fun ?containsOldGroupVersions ->
        fun ?nextToken ->
          fun () ->
            {
              timeSeriesServiceStatistics;
              containsOldGroupVersions;
              nextToken
            }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TimeSeriesServiceStatistics",
           (Option.map x.timeSeriesServiceStatistics
              ~f:TimeSeriesServiceStatisticsList.to_value));
        ("ContainsOldGroupVersions",
          (Option.map x.containsOldGroupVersions ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let containsOldGroupVersions =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ContainsOldGroupVersions") in
      let timeSeriesServiceStatistics =
        (Option.map ~f:TimeSeriesServiceStatisticsList.of_xml)
          (Xml.child xml_arg0 "TimeSeriesServiceStatistics") in
      make ?nextToken ?containsOldGroupVersions ?timeSeriesServiceStatistics
        ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let containsOldGroupVersions =
        field_map json "ContainsOldGroupVersions" Boolean.of_json in
      let timeSeriesServiceStatistics =
        field_map json "TimeSeriesServiceStatistics"
          TimeSeriesServiceStatisticsList.of_json in
      make ?nextToken ?containsOldGroupVersions ?timeSeriesServiceStatistics
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Get an aggregation of service statistics defined by a specific time range."]
module GetTimeSeriesServiceStatisticsRequest =
  struct
    type nonrec t =
      {
      startTime: Timestamp.t
        [@ocaml.doc
          "The start of the time frame for which to aggregate statistics."];
      endTime: Timestamp.t
        [@ocaml.doc
          "The end of the time frame for which to aggregate statistics."];
      groupName: GroupName.t option
        [@ocaml.doc
          "The case-sensitive name of the group for which to pull statistics from."];
      groupARN: GroupARN.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the group for which to pull statistics from."];
      entitySelectorExpression: EntitySelectorExpression.t option
        [@ocaml.doc
          "A filter expression defining entities that will be aggregated for statistics. Supports ID, service, and edge functions. If no selector expression is specified, edge statistics are returned."];
      period: NullableInteger.t option
        [@ocaml.doc "Aggregation period in seconds."];
      forecastStatistics: NullableBoolean.t option
        [@ocaml.doc
          "The forecasted high and low fault count values. Forecast enabled requests require the EntitySelectorExpression ID be provided."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    let context_ = "GetTimeSeriesServiceStatisticsRequest"
    let make ?groupName =
      fun ?groupARN ->
        fun ?entitySelectorExpression ->
          fun ?period ->
            fun ?forecastStatistics ->
              fun ?nextToken ->
                fun ~startTime ->
                  fun ~endTime ->
                    fun () ->
                      {
                        groupName;
                        groupARN;
                        entitySelectorExpression;
                        period;
                        forecastStatistics;
                        nextToken;
                        startTime;
                        endTime
                      }
    let to_value x =
      structure_to_value
        [("StartTime", (Some (Timestamp.to_value x.startTime)));
        ("EndTime", (Some (Timestamp.to_value x.endTime)));
        ("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:GroupARN.to_value));
        ("EntitySelectorExpression",
          (Option.map x.entitySelectorExpression
             ~f:EntitySelectorExpression.to_value));
        ("Period", (Option.map x.period ~f:NullableInteger.to_value));
        ("ForecastStatistics",
          (Option.map x.forecastStatistics ~f:NullableBoolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let forecastStatistics =
        (Option.map ~f:NullableBoolean.of_xml)
          (Xml.child xml_arg0 "ForecastStatistics") in
      let period =
        (Option.map ~f:NullableInteger.of_xml) (Xml.child xml_arg0 "Period") in
      let entitySelectorExpression =
        (Option.map ~f:EntitySelectorExpression.of_xml)
          (Xml.child xml_arg0 "EntitySelectorExpression") in
      let groupARN =
        (Option.map ~f:GroupARN.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      let endTime =
        Timestamp.of_xml (Xml.child_exn ~context:context_ xml_arg0 "EndTime") in
      let startTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      make ?nextToken ?forecastStatistics ?period ?entitySelectorExpression
        ?groupARN ?groupName ~endTime ~startTime ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let forecastStatistics =
        field_map json "ForecastStatistics" NullableBoolean.of_json in
      let period = field_map json "Period" NullableInteger.of_json in
      let entitySelectorExpression =
        field_map json "EntitySelectorExpression"
          EntitySelectorExpression.of_json in
      let groupARN = field_map json "GroupARN" GroupARN.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      let endTime = field_map_exn json "EndTime" Timestamp.of_json in
      let startTime = field_map_exn json "StartTime" Timestamp.of_json in
      make ?nextToken ?forecastStatistics ?period ?entitySelectorExpression
        ?groupARN ?groupName ~endTime ~startTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Get an aggregation of service statistics defined by a specific time range."]
module GetServiceGraphResult =
  struct
    type nonrec t =
      {
      startTime: Timestamp.t option
        [@ocaml.doc
          "The start of the time frame for which the graph was generated."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "The end of the time frame for which the graph was generated."];
      services: ServiceList.t option
        [@ocaml.doc
          "The services that have processed a traced request during the specified time frame."];
      containsOldGroupVersions: Boolean.t option
        [@ocaml.doc
          "A flag indicating whether the group's filter expression has been consistent, or if the returned service graph may show traces from an older version of the group's filter expression."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?startTime =
      fun ?endTime ->
        fun ?services ->
          fun ?containsOldGroupVersions ->
            fun ?nextToken ->
              fun () ->
                {
                  startTime;
                  endTime;
                  services;
                  containsOldGroupVersions;
                  nextToken
                }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("Services", (Option.map x.services ~f:ServiceList.to_value));
        ("ContainsOldGroupVersions",
          (Option.map x.containsOldGroupVersions ~f:Boolean.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let containsOldGroupVersions =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "ContainsOldGroupVersions") in
      let services =
        (Option.map ~f:ServiceList.of_xml) (Xml.child xml_arg0 "Services") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      make ?nextToken ?containsOldGroupVersions ?services ?endTime ?startTime
        ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let containsOldGroupVersions =
        field_map json "ContainsOldGroupVersions" Boolean.of_json in
      let services = field_map json "Services" ServiceList.of_json in
      let endTime = field_map json "EndTime" Timestamp.of_json in
      let startTime = field_map json "StartTime" Timestamp.of_json in
      make ?nextToken ?containsOldGroupVersions ?services ?endTime ?startTime
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a document that describes services that process incoming requests, and downstream services that they call as a result. Root services process incoming requests and make calls to downstream services. Root services are applications that use the Amazon Web Services X-Ray SDK. Downstream services can be other applications, Amazon Web Services resources, HTTP web APIs, or SQL databases."]
module GetServiceGraphRequest =
  struct
    type nonrec t =
      {
      startTime: Timestamp.t
        [@ocaml.doc
          "The start of the time frame for which to generate a graph."];
      endTime: Timestamp.t
        [@ocaml.doc
          "The end of the timeframe for which to generate a graph."];
      groupName: GroupName.t option
        [@ocaml.doc
          "The name of a group based on which you want to generate a graph."];
      groupARN: GroupARN.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of a group based on which you want to generate a graph."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    let context_ = "GetServiceGraphRequest"
    let make ?groupName =
      fun ?groupARN ->
        fun ?nextToken ->
          fun ~startTime ->
            fun ~endTime ->
              fun () ->
                { groupName; groupARN; nextToken; startTime; endTime }
    let to_value x =
      structure_to_value
        [("StartTime", (Some (Timestamp.to_value x.startTime)));
        ("EndTime", (Some (Timestamp.to_value x.endTime)));
        ("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:GroupARN.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let groupARN =
        (Option.map ~f:GroupARN.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      let endTime =
        Timestamp.of_xml (Xml.child_exn ~context:context_ xml_arg0 "EndTime") in
      let startTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      make ?nextToken ?groupARN ?groupName ~endTime ~startTime ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let groupARN = field_map json "GroupARN" GroupARN.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      let endTime = field_map_exn json "EndTime" Timestamp.of_json in
      let startTime = field_map_exn json "StartTime" Timestamp.of_json in
      make ?nextToken ?groupARN ?groupName ~endTime ~startTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a document that describes services that process incoming requests, and downstream services that they call as a result. Root services process incoming requests and make calls to downstream services. Root services are applications that use the Amazon Web Services X-Ray SDK. Downstream services can be other applications, Amazon Web Services resources, HTTP web APIs, or SQL databases."]
module GetSamplingTargetsResult =
  struct
    type nonrec t =
      {
      samplingTargetDocuments: SamplingTargetDocumentList.t option
        [@ocaml.doc
          "Updated rules that the service should use to sample requests."];
      lastRuleModification: Timestamp.t option
        [@ocaml.doc
          "The last time a user changed the sampling rule configuration. If the sampling rule configuration changed since the service last retrieved it, the service should call GetSamplingRules to get the latest version."];
      unprocessedStatistics: UnprocessedStatisticsList.t option
        [@ocaml.doc
          "Information about SamplingStatisticsDocument that X-Ray could not process."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?samplingTargetDocuments =
      fun ?lastRuleModification ->
        fun ?unprocessedStatistics ->
          fun () ->
            {
              samplingTargetDocuments;
              lastRuleModification;
              unprocessedStatistics
            }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SamplingTargetDocuments",
           (Option.map x.samplingTargetDocuments
              ~f:SamplingTargetDocumentList.to_value));
        ("LastRuleModification",
          (Option.map x.lastRuleModification ~f:Timestamp.to_value));
        ("UnprocessedStatistics",
          (Option.map x.unprocessedStatistics
             ~f:UnprocessedStatisticsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unprocessedStatistics =
        (Option.map ~f:UnprocessedStatisticsList.of_xml)
          (Xml.child xml_arg0 "UnprocessedStatistics") in
      let lastRuleModification =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastRuleModification") in
      let samplingTargetDocuments =
        (Option.map ~f:SamplingTargetDocumentList.of_xml)
          (Xml.child xml_arg0 "SamplingTargetDocuments") in
      make ?unprocessedStatistics ?lastRuleModification
        ?samplingTargetDocuments ()
    let of_json json =
      let unprocessedStatistics =
        field_map json "UnprocessedStatistics"
          UnprocessedStatisticsList.of_json in
      let lastRuleModification =
        field_map json "LastRuleModification" Timestamp.of_json in
      let samplingTargetDocuments =
        field_map json "SamplingTargetDocuments"
          SamplingTargetDocumentList.of_json in
      make ?unprocessedStatistics ?lastRuleModification
        ?samplingTargetDocuments ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Requests a sampling quota for rules that the service is using to sample requests."]
module GetSamplingTargetsRequest =
  struct
    type nonrec t =
      {
      samplingStatisticsDocuments: SamplingStatisticsDocumentList.t
        [@ocaml.doc
          "Information about rules that the service is using to sample requests."]}
    let context_ = "GetSamplingTargetsRequest"
    let make ~samplingStatisticsDocuments =
      fun () -> { samplingStatisticsDocuments }
    let to_value x =
      structure_to_value
        [("SamplingStatisticsDocuments",
           (Some
              (SamplingStatisticsDocumentList.to_value
                 x.samplingStatisticsDocuments)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let samplingStatisticsDocuments =
        SamplingStatisticsDocumentList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "SamplingStatisticsDocuments") in
      make ~samplingStatisticsDocuments ()
    let of_json json =
      let samplingStatisticsDocuments =
        field_map_exn json "SamplingStatisticsDocuments"
          SamplingStatisticsDocumentList.of_json in
      make ~samplingStatisticsDocuments ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Requests a sampling quota for rules that the service is using to sample requests."]
module GetSamplingStatisticSummariesResult =
  struct
    type nonrec t =
      {
      samplingStatisticSummaries: SamplingStatisticSummaryList.t option
        [@ocaml.doc
          "Information about the number of requests instrumented for each sampling rule."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?samplingStatisticSummaries =
      fun ?nextToken -> fun () -> { samplingStatisticSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SamplingStatisticSummaries",
           (Option.map x.samplingStatisticSummaries
              ~f:SamplingStatisticSummaryList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let samplingStatisticSummaries =
        (Option.map ~f:SamplingStatisticSummaryList.of_xml)
          (Xml.child xml_arg0 "SamplingStatisticSummaries") in
      make ?nextToken ?samplingStatisticSummaries ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let samplingStatisticSummaries =
        field_map json "SamplingStatisticSummaries"
          SamplingStatisticSummaryList.of_json in
      make ?nextToken ?samplingStatisticSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about recent sampling results for all sampling rules."]
module GetSamplingStatisticSummariesRequest =
  struct
    type nonrec t =
      {
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    let make ?nextToken = fun () -> { nextToken }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?nextToken ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves information about recent sampling results for all sampling rules."]
module GetSamplingRulesResult =
  struct
    type nonrec t =
      {
      samplingRuleRecords: SamplingRuleRecordList.t option
        [@ocaml.doc "Rule definitions and metadata."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?samplingRuleRecords =
      fun ?nextToken -> fun () -> { samplingRuleRecords; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SamplingRuleRecords",
           (Option.map x.samplingRuleRecords
              ~f:SamplingRuleRecordList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let samplingRuleRecords =
        (Option.map ~f:SamplingRuleRecordList.of_xml)
          (Xml.child xml_arg0 "SamplingRuleRecords") in
      make ?nextToken ?samplingRuleRecords ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let samplingRuleRecords =
        field_map json "SamplingRuleRecords" SamplingRuleRecordList.of_json in
      make ?nextToken ?samplingRuleRecords ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves all sampling rules."]
module GetSamplingRulesRequest =
  struct
    type nonrec t =
      {
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    let make ?nextToken = fun () -> { nextToken }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?nextToken ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves all sampling rules."]
module GetInsightSummariesResult =
  struct
    type nonrec t =
      {
      insightSummaries: InsightSummaryList.t option
        [@ocaml.doc
          "The summary of each insight within the group matching the provided filters. The summary contains the InsightID, start and end time, the root cause service, the root cause and client impact statistics, the top anomalous services, and the status of the insight."];
      nextToken: Token.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?insightSummaries =
      fun ?nextToken -> fun () -> { insightSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("InsightSummaries",
           (Option.map x.insightSummaries ~f:InsightSummaryList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let insightSummaries =
        (Option.map ~f:InsightSummaryList.of_xml)
          (Xml.child xml_arg0 "InsightSummaries") in
      make ?nextToken ?insightSummaries ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let insightSummaries =
        field_map json "InsightSummaries" InsightSummaryList.of_json in
      make ?nextToken ?insightSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the summaries of all insights in the specified group matching the provided filter values."]
module GetInsightSummariesRequest =
  struct
    type nonrec t =
      {
      states: InsightStateList.t option
        [@ocaml.doc "The list of insight states."];
      groupARN: GroupARN.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the group. Required if the GroupName isn't provided."];
      groupName: GroupName.t option
        [@ocaml.doc
          "The name of the group. Required if the GroupARN isn't provided."];
      startTime: Timestamp.t
        [@ocaml.doc
          "The beginning of the time frame in which the insights started. The start time can't be more than 30 days old."];
      endTime: Timestamp.t
        [@ocaml.doc
          "The end of the time frame in which the insights ended. The end time can't be more than 30 days old."];
      maxResults: GetInsightSummariesMaxResults.t option
        [@ocaml.doc "The maximum number of results to display."];
      nextToken: Token.t option [@ocaml.doc "Pagination token."]}
    let context_ = "GetInsightSummariesRequest"
    let make ?states =
      fun ?groupARN ->
        fun ?groupName ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun ~startTime ->
                fun ~endTime ->
                  fun () ->
                    {
                      states;
                      groupARN;
                      groupName;
                      maxResults;
                      nextToken;
                      startTime;
                      endTime
                    }
    let to_value x =
      structure_to_value
        [("States", (Option.map x.states ~f:InsightStateList.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:GroupARN.to_value));
        ("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("StartTime", (Some (Timestamp.to_value x.startTime)));
        ("EndTime", (Some (Timestamp.to_value x.endTime)));
        ("MaxResults",
          (Option.map x.maxResults ~f:GetInsightSummariesMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:GetInsightSummariesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let endTime =
        Timestamp.of_xml (Xml.child_exn ~context:context_ xml_arg0 "EndTime") in
      let startTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      let groupARN =
        (Option.map ~f:GroupARN.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let states =
        (Option.map ~f:InsightStateList.of_xml) (Xml.child xml_arg0 "States") in
      make ?nextToken ?maxResults ~endTime ~startTime ?groupName ?groupARN
        ?states ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let maxResults =
        field_map json "MaxResults" GetInsightSummariesMaxResults.of_json in
      let endTime = field_map_exn json "EndTime" Timestamp.of_json in
      let startTime = field_map_exn json "StartTime" Timestamp.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      let groupARN = field_map json "GroupARN" GroupARN.of_json in
      let states = field_map json "States" InsightStateList.of_json in
      make ?nextToken ?maxResults ~endTime ~startTime ?groupName ?groupARN
        ?states ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the summaries of all insights in the specified group matching the provided filter values."]
module GetInsightResult =
  struct
    type nonrec t =
      {
      insight: Insight.t option
        [@ocaml.doc "The summary information of an insight."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?insight = fun () -> { insight }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Insight", (Option.map x.insight ~f:Insight.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let insight =
        (Option.map ~f:Insight.of_xml) (Xml.child xml_arg0 "Insight") in
      make ?insight ()
    let of_json json =
      let insight = field_map json "Insight" Insight.of_json in
      make ?insight ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the summary information of an insight. This includes impact to clients and root cause services, the top anomalous services, the category, the state of the insight, and the start and end time of the insight."]
module GetInsightRequest =
  struct
    type nonrec t =
      {
      insightId: InsightId.t
        [@ocaml.doc
          "The insight's unique identifier. Use the GetInsightSummaries action to retrieve an InsightId."]}
    let context_ = "GetInsightRequest"
    let make ~insightId = fun () -> { insightId }
    let to_value x =
      structure_to_value
        [("InsightId", (Some (InsightId.to_value x.insightId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let insightId =
        InsightId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InsightId") in
      make ~insightId ()
    let of_json json =
      let insightId = field_map_exn json "InsightId" InsightId.of_json in
      make ~insightId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the summary information of an insight. This includes impact to clients and root cause services, the top anomalous services, the category, the state of the insight, and the start and end time of the insight."]
module GetInsightImpactGraphResult =
  struct
    type nonrec t =
      {
      insightId: InsightId.t option
        [@ocaml.doc "The insight's unique identifier."];
      startTime: Timestamp.t option [@ocaml.doc "The provided start time."];
      endTime: Timestamp.t option [@ocaml.doc "The provided end time."];
      serviceGraphStartTime: Timestamp.t option
        [@ocaml.doc
          "The time, in Unix seconds, at which the service graph started."];
      serviceGraphEndTime: Timestamp.t option
        [@ocaml.doc
          "The time, in Unix seconds, at which the service graph ended."];
      services: InsightImpactGraphServiceList.t option
        [@ocaml.doc
          "The Amazon Web Services instrumented services related to the insight."];
      nextToken: Token.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?insightId =
      fun ?startTime ->
        fun ?endTime ->
          fun ?serviceGraphStartTime ->
            fun ?serviceGraphEndTime ->
              fun ?services ->
                fun ?nextToken ->
                  fun () ->
                    {
                      insightId;
                      startTime;
                      endTime;
                      serviceGraphStartTime;
                      serviceGraphEndTime;
                      services;
                      nextToken
                    }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("InsightId", (Option.map x.insightId ~f:InsightId.to_value));
        ("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("ServiceGraphStartTime",
          (Option.map x.serviceGraphStartTime ~f:Timestamp.to_value));
        ("ServiceGraphEndTime",
          (Option.map x.serviceGraphEndTime ~f:Timestamp.to_value));
        ("Services",
          (Option.map x.services ~f:InsightImpactGraphServiceList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let services =
        (Option.map ~f:InsightImpactGraphServiceList.of_xml)
          (Xml.child xml_arg0 "Services") in
      let serviceGraphEndTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ServiceGraphEndTime") in
      let serviceGraphStartTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ServiceGraphStartTime") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let insightId =
        (Option.map ~f:InsightId.of_xml) (Xml.child xml_arg0 "InsightId") in
      make ?nextToken ?services ?serviceGraphEndTime ?serviceGraphStartTime
        ?endTime ?startTime ?insightId ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let services =
        field_map json "Services" InsightImpactGraphServiceList.of_json in
      let serviceGraphEndTime =
        field_map json "ServiceGraphEndTime" Timestamp.of_json in
      let serviceGraphStartTime =
        field_map json "ServiceGraphStartTime" Timestamp.of_json in
      let endTime = field_map json "EndTime" Timestamp.of_json in
      let startTime = field_map json "StartTime" Timestamp.of_json in
      let insightId = field_map json "InsightId" InsightId.of_json in
      make ?nextToken ?services ?serviceGraphEndTime ?serviceGraphStartTime
        ?endTime ?startTime ?insightId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a service graph structure filtered by the specified insight. The service graph is limited to only structural information. For a complete service graph, use this API with the GetServiceGraph API."]
module GetInsightImpactGraphRequest =
  struct
    type nonrec t =
      {
      insightId: InsightId.t
        [@ocaml.doc
          "The insight's unique identifier. Use the GetInsightSummaries action to retrieve an InsightId."];
      startTime: Timestamp.t
        [@ocaml.doc
          "The estimated start time of the insight, in Unix time seconds. The StartTime is inclusive of the value provided and can't be more than 30 days old."];
      endTime: Timestamp.t
        [@ocaml.doc
          "The estimated end time of the insight, in Unix time seconds. The EndTime is exclusive of the value provided. The time range between the start time and end time can't be more than six hours."];
      nextToken: Token.t option
        [@ocaml.doc
          "Specify the pagination token returned by a previous request to retrieve the next page of results."]}
    let context_ = "GetInsightImpactGraphRequest"
    let make ?nextToken =
      fun ~insightId ->
        fun ~startTime ->
          fun ~endTime ->
            fun () -> { nextToken; insightId; startTime; endTime }
    let to_value x =
      structure_to_value
        [("InsightId", (Some (InsightId.to_value x.insightId)));
        ("StartTime", (Some (Timestamp.to_value x.startTime)));
        ("EndTime", (Some (Timestamp.to_value x.endTime)));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let endTime =
        Timestamp.of_xml (Xml.child_exn ~context:context_ xml_arg0 "EndTime") in
      let startTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      let insightId =
        InsightId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InsightId") in
      make ?nextToken ~endTime ~startTime ~insightId ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let endTime = field_map_exn json "EndTime" Timestamp.of_json in
      let startTime = field_map_exn json "StartTime" Timestamp.of_json in
      let insightId = field_map_exn json "InsightId" InsightId.of_json in
      make ?nextToken ~endTime ~startTime ~insightId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a service graph structure filtered by the specified insight. The service graph is limited to only structural information. For a complete service graph, use this API with the GetServiceGraph API."]
module GetInsightEventsResult =
  struct
    type nonrec t =
      {
      insightEvents: InsightEventList.t option
        [@ocaml.doc
          "A detailed description of the event. This includes the time of the event, client and root cause impact statistics, and the top anomalous service at the time of the event."];
      nextToken: Token.t option
        [@ocaml.doc
          "Use this token to retrieve the next page of insight events."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?insightEvents =
      fun ?nextToken -> fun () -> { insightEvents; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("InsightEvents",
           (Option.map x.insightEvents ~f:InsightEventList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let insightEvents =
        (Option.map ~f:InsightEventList.of_xml)
          (Xml.child xml_arg0 "InsightEvents") in
      make ?nextToken ?insightEvents ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let insightEvents =
        field_map json "InsightEvents" InsightEventList.of_json in
      make ?nextToken ?insightEvents ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "X-Ray reevaluates insights periodically until they're resolved, and records each intermediate state as an event. You can review an insight's events in the Impact Timeline on the Inspect page in the X-Ray console."]
module GetInsightEventsRequest =
  struct
    type nonrec t =
      {
      insightId: InsightId.t
        [@ocaml.doc
          "The insight's unique identifier. Use the GetInsightSummaries action to retrieve an InsightId."];
      maxResults: GetInsightEventsMaxResults.t option
        [@ocaml.doc
          "Used to retrieve at most the specified value of events."];
      nextToken: Token.t option
        [@ocaml.doc
          "Specify the pagination token returned by a previous request to retrieve the next page of events."]}
    let context_ = "GetInsightEventsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~insightId -> fun () -> { maxResults; nextToken; insightId }
    let to_value x =
      structure_to_value
        [("InsightId", (Some (InsightId.to_value x.insightId)));
        ("MaxResults",
          (Option.map x.maxResults ~f:GetInsightEventsMaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:GetInsightEventsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let insightId =
        InsightId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InsightId") in
      make ?nextToken ?maxResults ~insightId ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let maxResults =
        field_map json "MaxResults" GetInsightEventsMaxResults.of_json in
      let insightId = field_map_exn json "InsightId" InsightId.of_json in
      make ?nextToken ?maxResults ~insightId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "X-Ray reevaluates insights periodically until they're resolved, and records each intermediate state as an event. You can review an insight's events in the Impact Timeline on the Inspect page in the X-Ray console."]
module GetGroupsResult =
  struct
    type nonrec t =
      {
      groups: GroupSummaryList.t option
        [@ocaml.doc "The collection of all active groups."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?groups = fun ?nextToken -> fun () -> { groups; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Groups", (Option.map x.groups ~f:GroupSummaryList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let groups =
        (Option.map ~f:GroupSummaryList.of_xml) (Xml.child xml_arg0 "Groups") in
      make ?nextToken ?groups ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let groups = field_map json "Groups" GroupSummaryList.of_json in
      make ?nextToken ?groups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves all active group details."]
module GetGroupsRequest =
  struct
    type nonrec t =
      {
      nextToken: GetGroupsNextToken.t option [@ocaml.doc "Pagination token."]}
    let make ?nextToken = fun () -> { nextToken }
    let to_value x =
      structure_to_value
        [("NextToken",
           (Option.map x.nextToken ~f:GetGroupsNextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:GetGroupsNextToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      make ?nextToken ()
    let of_json json =
      let nextToken = field_map json "NextToken" GetGroupsNextToken.of_json in
      make ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves all active group details."]
module GetGroupResult =
  struct
    type nonrec t =
      {
      group: Group.t option
        [@ocaml.doc
          "The group that was requested. Contains the name of the group, the ARN of the group, the filter expression, and the insight configuration assigned to the group."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?group = fun () -> { group }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Group", (Option.map x.group ~f:Group.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group = (Option.map ~f:Group.of_xml) (Xml.child xml_arg0 "Group") in
      make ?group ()
    let of_json json =
      let group = field_map json "Group" Group.of_json in make ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves group resource details."]
module GetGroupRequest =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc "The case-sensitive name of the group."];
      groupARN: GroupARN.t option
        [@ocaml.doc "The ARN of the group that was generated on creation."]}
    let make ?groupName = fun ?groupARN -> fun () -> { groupName; groupARN }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:GroupARN.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupARN =
        (Option.map ~f:GroupARN.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?groupARN ?groupName ()
    let of_json json =
      let groupARN = field_map json "GroupARN" GroupARN.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?groupARN ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves group resource details."]
module GetEncryptionConfigResult =
  struct
    type nonrec t =
      {
      encryptionConfig: EncryptionConfig.t option
        [@ocaml.doc "The encryption configuration document."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?encryptionConfig = fun () -> { encryptionConfig }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("EncryptionConfig",
           (Option.map x.encryptionConfig ~f:EncryptionConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionConfig =
        (Option.map ~f:EncryptionConfig.of_xml)
          (Xml.child xml_arg0 "EncryptionConfig") in
      make ?encryptionConfig ()
    let of_json json =
      let encryptionConfig =
        field_map json "EncryptionConfig" EncryptionConfig.of_json in
      make ?encryptionConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the current encryption configuration for X-Ray data."]
module GetEncryptionConfigRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the current encryption configuration for X-Ray data."]
module DeleteSamplingRuleResult =
  struct
    type nonrec t =
      {
      samplingRuleRecord: SamplingRuleRecord.t option
        [@ocaml.doc "The deleted rule definition and metadata."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?samplingRuleRecord = fun () -> { samplingRuleRecord }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SamplingRuleRecord",
           (Option.map x.samplingRuleRecord ~f:SamplingRuleRecord.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let samplingRuleRecord =
        (Option.map ~f:SamplingRuleRecord.of_xml)
          (Xml.child xml_arg0 "SamplingRuleRecord") in
      make ?samplingRuleRecord ()
    let of_json json =
      let samplingRuleRecord =
        field_map json "SamplingRuleRecord" SamplingRuleRecord.of_json in
      make ?samplingRuleRecord ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a sampling rule."]
module DeleteSamplingRuleRequest =
  struct
    type nonrec t =
      {
      ruleName: String_.t option
        [@ocaml.doc
          "The name of the sampling rule. Specify a rule by either name or ARN, but not both."];
      ruleARN: String_.t option
        [@ocaml.doc
          "The ARN of the sampling rule. Specify a rule by either name or ARN, but not both."]}
    let make ?ruleName = fun ?ruleARN -> fun () -> { ruleName; ruleARN }
    let to_value x =
      structure_to_value
        [("RuleName", (Option.map x.ruleName ~f:String_.to_value));
        ("RuleARN", (Option.map x.ruleARN ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleARN") in
      let ruleName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleName") in
      make ?ruleARN ?ruleName ()
    let of_json json =
      let ruleARN = field_map json "RuleARN" String_.of_json in
      let ruleName = field_map json "RuleName" String_.of_json in
      make ?ruleARN ?ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a sampling rule."]
module DeleteGroupResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a group resource."]
module DeleteGroupRequest =
  struct
    type nonrec t =
      {
      groupName: GroupName.t option
        [@ocaml.doc "The case-sensitive name of the group."];
      groupARN: GroupARN.t option
        [@ocaml.doc "The ARN of the group that was generated on creation."]}
    let make ?groupName = fun ?groupARN -> fun () -> { groupName; groupARN }
    let to_value x =
      structure_to_value
        [("GroupName", (Option.map x.groupName ~f:GroupName.to_value));
        ("GroupARN", (Option.map x.groupARN ~f:GroupARN.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupARN =
        (Option.map ~f:GroupARN.of_xml) (Xml.child xml_arg0 "GroupARN") in
      let groupName =
        (Option.map ~f:GroupName.of_xml) (Xml.child xml_arg0 "GroupName") in
      make ?groupARN ?groupName ()
    let of_json json =
      let groupARN = field_map json "GroupARN" GroupARN.of_json in
      let groupName = field_map json "GroupName" GroupName.of_json in
      make ?groupARN ?groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a group resource."]
module CreateSamplingRuleResult =
  struct
    type nonrec t =
      {
      samplingRuleRecord: SamplingRuleRecord.t option
        [@ocaml.doc "The saved rule definition and metadata."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `RuleLimitExceededException of RuleLimitExceededException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?samplingRuleRecord = fun () -> { samplingRuleRecord }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "RuleLimitExceededException" ->
          `RuleLimitExceededException
            (RuleLimitExceededException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "RuleLimitExceededException" ->
          `RuleLimitExceededException (RuleLimitExceededException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `RuleLimitExceededException e ->
          `Assoc
            [("error", (`String "RuleLimitExceededException"));
            ("details", (RuleLimitExceededException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SamplingRuleRecord",
           (Option.map x.samplingRuleRecord ~f:SamplingRuleRecord.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let samplingRuleRecord =
        (Option.map ~f:SamplingRuleRecord.of_xml)
          (Xml.child xml_arg0 "SamplingRuleRecord") in
      make ?samplingRuleRecord ()
    let of_json json =
      let samplingRuleRecord =
        field_map json "SamplingRuleRecord" SamplingRuleRecord.of_json in
      make ?samplingRuleRecord ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a rule to control sampling behavior for instrumented applications. Services retrieve rules with GetSamplingRules, and evaluate each rule in ascending order of priority for each request. If a rule matches, the service records a trace, borrowing it from the reservoir size. After 10 seconds, the service reports back to X-Ray with GetSamplingTargets to get updated versions of each in-use rule. The updated rule contains a trace quota that the service can use instead of borrowing from the reservoir."]
module CreateSamplingRuleRequest =
  struct
    type nonrec t =
      {
      samplingRule: SamplingRule.t [@ocaml.doc "The rule definition."];
      tags: TagList.t option
        [@ocaml.doc
          "A map that contains one or more tag keys and tag values to attach to an X-Ray sampling rule. For more information about ways to use tags, see Tagging Amazon Web Services resources in the Amazon Web Services General Reference. The following restrictions apply to tags: Maximum number of user-applied tags per resource: 50 Maximum tag key length: 128 Unicode characters Maximum tag value length: 256 Unicode characters Valid values for key and value: a-z, A-Z, 0-9, space, and the following characters: _ . : / = + - and \\@ Tag keys and values are case sensitive. Don't use aws: as a prefix for keys; it's reserved for Amazon Web Services use."]}
    let context_ = "CreateSamplingRuleRequest"
    let make ?tags = fun ~samplingRule -> fun () -> { tags; samplingRule }
    let to_value x =
      structure_to_value
        [("SamplingRule", (Some (SamplingRule.to_value x.samplingRule)));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let samplingRule =
        SamplingRule.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SamplingRule") in
      make ?tags ~samplingRule ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let samplingRule =
        field_map_exn json "SamplingRule" SamplingRule.of_json in
      make ?tags ~samplingRule ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a rule to control sampling behavior for instrumented applications. Services retrieve rules with GetSamplingRules, and evaluate each rule in ascending order of priority for each request. If a rule matches, the service records a trace, borrowing it from the reservoir size. After 10 seconds, the service reports back to X-Ray with GetSamplingTargets to get updated versions of each in-use rule. The updated rule contains a trace quota that the service can use instead of borrowing from the reservoir."]
module CreateGroupResult =
  struct
    type nonrec t =
      {
      group: Group.t option
        [@ocaml.doc
          "The group that was created. Contains the name of the group that was created, the Amazon Resource Name (ARN) of the group that was generated based on the group name, the filter expression, and the insight configuration that was assigned to the group."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?group = fun () -> { group }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Group", (Option.map x.group ~f:Group.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let group = (Option.map ~f:Group.of_xml) (Xml.child xml_arg0 "Group") in
      make ?group ()
    let of_json json =
      let group = field_map json "Group" Group.of_json in make ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a group resource with a name and a filter expression."]
module CreateGroupRequest =
  struct
    type nonrec t =
      {
      groupName: GroupName.t
        [@ocaml.doc
          "The case-sensitive name of the new group. Default is a reserved name and names must be unique."];
      filterExpression: FilterExpression.t option
        [@ocaml.doc
          "The filter expression defining criteria by which to group traces."];
      insightsConfiguration: InsightsConfiguration.t option
        [@ocaml.doc
          "The structure containing configurations related to insights. The InsightsEnabled boolean can be set to true to enable insights for the new group or false to disable insights for the new group. The NotifcationsEnabled boolean can be set to true to enable insights notifications for the new group. Notifications may only be enabled on a group with InsightsEnabled set to true."];
      tags: TagList.t option
        [@ocaml.doc
          "A map that contains one or more tag keys and tag values to attach to an X-Ray group. For more information about ways to use tags, see Tagging Amazon Web Services resources in the Amazon Web Services General Reference. The following restrictions apply to tags: Maximum number of user-applied tags per resource: 50 Maximum tag key length: 128 Unicode characters Maximum tag value length: 256 Unicode characters Valid values for key and value: a-z, A-Z, 0-9, space, and the following characters: _ . : / = + - and \\@ Tag keys and values are case sensitive. Don't use aws: as a prefix for keys; it's reserved for Amazon Web Services use."]}
    let context_ = "CreateGroupRequest"
    let make ?filterExpression =
      fun ?insightsConfiguration ->
        fun ?tags ->
          fun ~groupName ->
            fun () ->
              { filterExpression; insightsConfiguration; tags; groupName }
    let to_value x =
      structure_to_value
        [("GroupName", (Some (GroupName.to_value x.groupName)));
        ("FilterExpression",
          (Option.map x.filterExpression ~f:FilterExpression.to_value));
        ("InsightsConfiguration",
          (Option.map x.insightsConfiguration
             ~f:InsightsConfiguration.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let insightsConfiguration =
        (Option.map ~f:InsightsConfiguration.of_xml)
          (Xml.child xml_arg0 "InsightsConfiguration") in
      let filterExpression =
        (Option.map ~f:FilterExpression.of_xml)
          (Xml.child xml_arg0 "FilterExpression") in
      let groupName =
        GroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GroupName") in
      make ?tags ?insightsConfiguration ?filterExpression ~groupName ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let insightsConfiguration =
        field_map json "InsightsConfiguration" InsightsConfiguration.of_json in
      let filterExpression =
        field_map json "FilterExpression" FilterExpression.of_json in
      let groupName = field_map_exn json "GroupName" GroupName.of_json in
      make ?tags ?insightsConfiguration ?filterExpression ~groupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a group resource with a name and a filter expression."]
module BatchGetTracesResult =
  struct
    type nonrec t =
      {
      traces: TraceList.t option
        [@ocaml.doc "Full traces for the specified requests."];
      unprocessedTraceIds: UnprocessedTraceIdList.t option
        [@ocaml.doc "Trace IDs of requests that haven't been processed."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ThrottledException of ThrottledException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?traces =
      fun ?unprocessedTraceIds ->
        fun ?nextToken ->
          fun () -> { traces; unprocessedTraceIds; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ThrottledException" ->
          `ThrottledException (ThrottledException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ThrottledException e ->
          `Assoc
            [("error", (`String "ThrottledException"));
            ("details", (ThrottledException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Traces", (Option.map x.traces ~f:TraceList.to_value));
        ("UnprocessedTraceIds",
          (Option.map x.unprocessedTraceIds
             ~f:UnprocessedTraceIdList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let unprocessedTraceIds =
        (Option.map ~f:UnprocessedTraceIdList.of_xml)
          (Xml.child xml_arg0 "UnprocessedTraceIds") in
      let traces =
        (Option.map ~f:TraceList.of_xml) (Xml.child xml_arg0 "Traces") in
      make ?nextToken ?unprocessedTraceIds ?traces ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let unprocessedTraceIds =
        field_map json "UnprocessedTraceIds" UnprocessedTraceIdList.of_json in
      let traces = field_map json "Traces" TraceList.of_json in
      make ?nextToken ?unprocessedTraceIds ?traces ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a list of traces specified by ID. Each trace is a collection of segment documents that originates from a single request. Use GetTraceSummaries to get a list of trace IDs."]
module BatchGetTracesRequest =
  struct
    type nonrec t =
      {
      traceIds: TraceIdList.t
        [@ocaml.doc
          "Specify the trace IDs of requests for which to retrieve segments."];
      nextToken: String_.t option [@ocaml.doc "Pagination token."]}
    let context_ = "BatchGetTracesRequest"
    let make ?nextToken = fun ~traceIds -> fun () -> { nextToken; traceIds }
    let to_value x =
      structure_to_value
        [("TraceIds", (Some (TraceIdList.to_value x.traceIds)));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let traceIds =
        TraceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TraceIds") in
      make ?nextToken ~traceIds ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let traceIds = field_map_exn json "TraceIds" TraceIdList.of_json in
      make ?nextToken ~traceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a list of traces specified by ID. Each trace is a collection of segment documents that originates from a single request. Use GetTraceSummaries to get a list of trace IDs."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_phone_number_with_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING String"
       and userId = flag "user-id" (required string) ~doc:"STRING String"
       and e164PhoneNumber =
         flag "e164-phone-number" (required string)
           ~doc:"STRING E164PhoneNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_phone_number_with_user
           (Values.AssociatePhoneNumberWithUserRequest.make ~accountId
              ~userId ~e164PhoneNumber ())
           (Some Values.AssociatePhoneNumberWithUserResponse.to_json)
           (Some Values.AssociatePhoneNumberWithUserResponse.error_to_json)])
let associate_phone_numbers_with_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceAssociate =
         flag "force-associate" (optional bool) ~doc:"BOOL NullableBoolean"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_phone_numbers_with_voice_connector
           (Values.AssociatePhoneNumbersWithVoiceConnectorRequest.make
              ?forceAssociate ~voiceConnectorId
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some
              Values.AssociatePhoneNumbersWithVoiceConnectorResponse.to_json)
           (Some
              Values.AssociatePhoneNumbersWithVoiceConnectorResponse.error_to_json)])
let associate_phone_numbers_with_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceAssociate =
         flag "force-associate" (optional bool) ~doc:"BOOL NullableBoolean"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_phone_numbers_with_voice_connector_group
           (Values.AssociatePhoneNumbersWithVoiceConnectorGroupRequest.make
              ?forceAssociate ~voiceConnectorGroupId
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some
              Values.AssociatePhoneNumbersWithVoiceConnectorGroupResponse.to_json)
           (Some
              Values.AssociatePhoneNumbersWithVoiceConnectorGroupResponse.error_to_json)])
let associate_signin_delegate_groups_with_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and signinDelegateGroups =
         flag "signin-delegate-groups" (required json_arg)
           ~doc:"JSON SigninDelegateGroupList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_signin_delegate_groups_with_account
           (Values.AssociateSigninDelegateGroupsWithAccountRequest.make
              ~accountId
              ~signinDelegateGroups:(Values.SigninDelegateGroupList.of_json
                                       signinDelegateGroups) ())
           (Some
              Values.AssociateSigninDelegateGroupsWithAccountResponse.to_json)
           (Some
              Values.AssociateSigninDelegateGroupsWithAccountResponse.error_to_json)])
let batch_create_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendees =
         flag "attendees" (required json_arg)
           ~doc:"JSON CreateAttendeeRequestItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_attendee
           (Values.BatchCreateAttendeeRequest.make ~meetingId
              ~attendees:(Values.CreateAttendeeRequestItemList.of_json
                            attendees) ())
           (Some Values.BatchCreateAttendeeResponse.to_json)
           (Some Values.BatchCreateAttendeeResponse.error_to_json)])
let batch_create_channel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ChannelMembershipType"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArns =
         flag "member-arns" (required json_arg) ~doc:"JSON MemberArns" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_channel_membership
           (Values.BatchCreateChannelMembershipRequest.make
              ?type_:(Option.map ~f:Values.ChannelMembershipType.of_json
                        type_) ?chimeBearer ~channelArn
              ~memberArns:(Values.MemberArns.of_json memberArns) ())
           (Some Values.BatchCreateChannelMembershipResponse.to_json)
           (Some Values.BatchCreateChannelMembershipResponse.error_to_json)])
let batch_create_room_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString"
       and membershipItemList =
         flag "membership-item-list" (required json_arg)
           ~doc:"JSON MembershipItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_room_membership
           (Values.BatchCreateRoomMembershipRequest.make ~accountId ~roomId
              ~membershipItemList:(Values.MembershipItemList.of_json
                                     membershipItemList) ())
           (Some Values.BatchCreateRoomMembershipResponse.to_json)
           (Some Values.BatchCreateRoomMembershipResponse.error_to_json)])
let batch_delete_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberIds =
         flag "phone-number-ids" (required json_arg)
           ~doc:"JSON NonEmptyStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_phone_number
           (Values.BatchDeletePhoneNumberRequest.make
              ~phoneNumberIds:(Values.NonEmptyStringList.of_json
                                 phoneNumberIds) ())
           (Some Values.BatchDeletePhoneNumberResponse.to_json)
           (Some Values.BatchDeletePhoneNumberResponse.error_to_json)])
let batch_suspend_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and userIdList =
         flag "user-id-list" (required json_arg) ~doc:"JSON UserIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_suspend_user
           (Values.BatchSuspendUserRequest.make ~accountId
              ~userIdList:(Values.UserIdList.of_json userIdList) ())
           (Some Values.BatchSuspendUserResponse.to_json)
           (Some Values.BatchSuspendUserResponse.error_to_json)])
let batch_unsuspend_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and userIdList =
         flag "user-id-list" (required json_arg) ~doc:"JSON UserIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_unsuspend_user
           (Values.BatchUnsuspendUserRequest.make ~accountId
              ~userIdList:(Values.UserIdList.of_json userIdList) ())
           (Some Values.BatchUnsuspendUserResponse.to_json)
           (Some Values.BatchUnsuspendUserResponse.error_to_json)])
let batch_update_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updatePhoneNumberRequestItems =
         flag "update-phone-number-request-items" (required json_arg)
           ~doc:"JSON UpdatePhoneNumberRequestItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_phone_number
           (Values.BatchUpdatePhoneNumberRequest.make
              ~updatePhoneNumberRequestItems:(Values.UpdatePhoneNumberRequestItemList.of_json
                                                updatePhoneNumberRequestItems)
              ()) (Some Values.BatchUpdatePhoneNumberResponse.to_json)
           (Some Values.BatchUpdatePhoneNumberResponse.error_to_json)])
let batch_update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and updateUserRequestItems =
         flag "update-user-request-items" (required json_arg)
           ~doc:"JSON UpdateUserRequestItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_user
           (Values.BatchUpdateUserRequest.make ~accountId
              ~updateUserRequestItems:(Values.UpdateUserRequestItemList.of_json
                                         updateUserRequestItems) ())
           (Some Values.BatchUpdateUserResponse.to_json)
           (Some Values.BatchUpdateUserResponse.error_to_json)])
let create_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING AccountName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_account (Values.CreateAccountRequest.make ~name ())
           (Some Values.CreateAccountResponse.to_json)
           (Some Values.CreateAccountResponse.error_to_json)])
let create_app_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_instance
           (Values.CreateAppInstanceRequest.make ?metadata
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~clientRequestToken ())
           (Some Values.CreateAppInstanceResponse.to_json)
           (Some Values.CreateAppInstanceResponse.error_to_json)])
let create_app_instance_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceAdminArn =
         flag "app-instance-admin-arn" (required string)
           ~doc:"STRING ChimeArn"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_instance_admin
           (Values.CreateAppInstanceAdminRequest.make ~appInstanceAdminArn
              ~appInstanceArn ())
           (Some Values.CreateAppInstanceAdminResponse.to_json)
           (Some Values.CreateAppInstanceAdminResponse.error_to_json)])
let create_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceUserId =
         flag "app-instance-user-id" (required string) ~doc:"STRING UserId"
       and name = flag "name" (required string) ~doc:"STRING UserName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_instance_user
           (Values.CreateAppInstanceUserRequest.make ?metadata
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~appInstanceArn ~appInstanceUserId ~name ~clientRequestToken ())
           (Some Values.CreateAppInstanceUserResponse.to_json)
           (Some Values.CreateAppInstanceUserResponse.error_to_json)])
let create_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON AttendeeTagList"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and externalUserId =
         flag "external-user-id" (required string)
           ~doc:"STRING ExternalUserIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_attendee
           (Values.CreateAttendeeRequest.make
              ?tags:(Option.map ~f:Values.AttendeeTagList.of_json tags)
              ~meetingId ~externalUserId ())
           (Some Values.CreateAttendeeResponse.to_json)
           (Some Values.CreateAttendeeResponse.error_to_json)])
let create_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain =
         flag "domain" (optional string) ~doc:"STRING NonEmptyString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING SensitiveString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bot
           (Values.CreateBotRequest.make ?domain ~accountId ~displayName ())
           (Some Values.CreateBotResponse.to_json)
           (Some Values.CreateBotResponse.error_to_json)])
let create_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON ChannelMode"
       and privacy =
         flag "privacy" (optional json_arg) ~doc:"JSON ChannelPrivacy"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel
           (Values.CreateChannelRequest.make
              ?mode:(Option.map ~f:Values.ChannelMode.of_json mode)
              ?privacy:(Option.map ~f:Values.ChannelPrivacy.of_json privacy)
              ?metadata ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?chimeBearer ~appInstanceArn ~name ~clientRequestToken ())
           (Some Values.CreateChannelResponse.to_json)
           (Some Values.CreateChannelResponse.error_to_json)])
let create_channel_ban =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_ban
           (Values.CreateChannelBanRequest.make ?chimeBearer ~channelArn
              ~memberArn ()) (Some Values.CreateChannelBanResponse.to_json)
           (Some Values.CreateChannelBanResponse.error_to_json)])
let create_channel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ChannelMembershipType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_membership
           (Values.CreateChannelMembershipRequest.make ?chimeBearer
              ~channelArn ~memberArn
              ~type_:(Values.ChannelMembershipType.of_json type_) ())
           (Some Values.CreateChannelMembershipResponse.to_json)
           (Some Values.CreateChannelMembershipResponse.error_to_json)])
let create_channel_moderator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelModeratorArn =
         flag "channel-moderator-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_moderator
           (Values.CreateChannelModeratorRequest.make ?chimeBearer
              ~channelArn ~channelModeratorArn ())
           (Some Values.CreateChannelModeratorResponse.to_json)
           (Some Values.CreateChannelModeratorResponse.error_to_json)])
let create_media_capture_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and chimeSdkMeetingConfiguration =
         flag "chime-sdk-meeting-configuration" (optional json_arg)
           ~doc:"JSON ChimeSdkMeetingConfiguration"
       and sourceType =
         flag "source-type" (required json_arg)
           ~doc:"JSON MediaPipelineSourceType"
       and sourceArn = flag "source-arn" (required string) ~doc:"STRING Arn"
       and sinkType =
         flag "sink-type" (required json_arg)
           ~doc:"JSON MediaPipelineSinkType"
       and sinkArn = flag "sink-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_media_capture_pipeline
           (Values.CreateMediaCapturePipelineRequest.make ?clientRequestToken
              ?chimeSdkMeetingConfiguration:(Option.map
                                               ~f:Values.ChimeSdkMeetingConfiguration.of_json
                                               chimeSdkMeetingConfiguration)
              ~sourceType:(Values.MediaPipelineSourceType.of_json sourceType)
              ~sourceArn
              ~sinkType:(Values.MediaPipelineSinkType.of_json sinkType)
              ~sinkArn ())
           (Some Values.CreateMediaCapturePipelineResponse.to_json)
           (Some Values.CreateMediaCapturePipelineResponse.error_to_json)])
let create_meeting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and externalMeetingId =
         flag "external-meeting-id" (optional string)
           ~doc:"STRING ExternalMeetingIdType"
       and meetingHostId =
         flag "meeting-host-id" (optional string)
           ~doc:"STRING ExternalUserIdType"
       and mediaRegion =
         flag "media-region" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON MeetingTagList"
       and notificationsConfiguration =
         flag "notifications-configuration" (optional json_arg)
           ~doc:"JSON MeetingNotificationConfiguration"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_meeting
           (Values.CreateMeetingRequest.make ?externalMeetingId
              ?meetingHostId ?mediaRegion
              ?tags:(Option.map ~f:Values.MeetingTagList.of_json tags)
              ?notificationsConfiguration:(Option.map
                                             ~f:Values.MeetingNotificationConfiguration.of_json
                                             notificationsConfiguration)
              ~clientRequestToken ())
           (Some Values.CreateMeetingResponse.to_json)
           (Some Values.CreateMeetingResponse.error_to_json)])
let create_meeting_dial_out =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and fromPhoneNumber =
         flag "from-phone-number" (required string)
           ~doc:"STRING E164PhoneNumber"
       and toPhoneNumber =
         flag "to-phone-number" (required string)
           ~doc:"STRING E164PhoneNumber"
       and joinToken =
         flag "join-token" (required string) ~doc:"STRING JoinTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_meeting_dial_out
           (Values.CreateMeetingDialOutRequest.make ~meetingId
              ~fromPhoneNumber ~toPhoneNumber ~joinToken ())
           (Some Values.CreateMeetingDialOutResponse.to_json)
           (Some Values.CreateMeetingDialOutResponse.error_to_json)])
let create_meeting_with_attendees =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and externalMeetingId =
         flag "external-meeting-id" (optional string)
           ~doc:"STRING ExternalMeetingIdType"
       and meetingHostId =
         flag "meeting-host-id" (optional string)
           ~doc:"STRING ExternalUserIdType"
       and mediaRegion =
         flag "media-region" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON MeetingTagList"
       and notificationsConfiguration =
         flag "notifications-configuration" (optional json_arg)
           ~doc:"JSON MeetingNotificationConfiguration"
       and attendees =
         flag "attendees" (optional json_arg)
           ~doc:"JSON CreateMeetingWithAttendeesRequestItemList"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_meeting_with_attendees
           (Values.CreateMeetingWithAttendeesRequest.make ?externalMeetingId
              ?meetingHostId ?mediaRegion
              ?tags:(Option.map ~f:Values.MeetingTagList.of_json tags)
              ?notificationsConfiguration:(Option.map
                                             ~f:Values.MeetingNotificationConfiguration.of_json
                                             notificationsConfiguration)
              ?attendees:(Option.map
                            ~f:Values.CreateMeetingWithAttendeesRequestItemList.of_json
                            attendees) ~clientRequestToken ())
           (Some Values.CreateMeetingWithAttendeesResponse.to_json)
           (Some Values.CreateMeetingWithAttendeesResponse.error_to_json)])
let create_phone_number_order =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and productType =
         flag "product-type" (required json_arg)
           ~doc:"JSON PhoneNumberProductType"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_phone_number_order
           (Values.CreatePhoneNumberOrderRequest.make
              ~productType:(Values.PhoneNumberProductType.of_json productType)
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some Values.CreatePhoneNumberOrderResponse.to_json)
           (Some Values.CreatePhoneNumberOrderResponse.error_to_json)])
let create_proxy_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ProxySessionNameString"
       and expiryMinutes =
         flag "expiry-minutes" (optional int) ~doc:"INT PositiveInteger"
       and numberSelectionBehavior =
         flag "number-selection-behavior" (optional json_arg)
           ~doc:"JSON NumberSelectionBehavior"
       and geoMatchLevel =
         flag "geo-match-level" (optional json_arg) ~doc:"JSON GeoMatchLevel"
       and geoMatchParams =
         flag "geo-match-params" (optional json_arg)
           ~doc:"JSON GeoMatchParams"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and participantPhoneNumbers =
         flag "participant-phone-numbers" (required json_arg)
           ~doc:"JSON ParticipantPhoneNumberList"
       and capabilities =
         flag "capabilities" (required json_arg) ~doc:"JSON CapabilityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_proxy_session
           (Values.CreateProxySessionRequest.make ?name ?expiryMinutes
              ?numberSelectionBehavior:(Option.map
                                          ~f:Values.NumberSelectionBehavior.of_json
                                          numberSelectionBehavior)
              ?geoMatchLevel:(Option.map ~f:Values.GeoMatchLevel.of_json
                                geoMatchLevel)
              ?geoMatchParams:(Option.map ~f:Values.GeoMatchParams.of_json
                                 geoMatchParams) ~voiceConnectorId
              ~participantPhoneNumbers:(Values.ParticipantPhoneNumberList.of_json
                                          participantPhoneNumbers)
              ~capabilities:(Values.CapabilityList.of_json capabilities) ())
           (Some Values.CreateProxySessionResponse.to_json)
           (Some Values.CreateProxySessionResponse.error_to_json)])
let create_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and name = flag "name" (required string) ~doc:"STRING SensitiveString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_room
           (Values.CreateRoomRequest.make ?clientRequestToken ~accountId
              ~name ()) (Some Values.CreateRoomResponse.to_json)
           (Some Values.CreateRoomResponse.error_to_json)])
let create_room_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and role =
         flag "role" (optional json_arg) ~doc:"JSON RoomMembershipRole"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_room_membership
           (Values.CreateRoomMembershipRequest.make
              ?role:(Option.map ~f:Values.RoomMembershipRole.of_json role)
              ~accountId ~roomId ~memberId ())
           (Some Values.CreateRoomMembershipResponse.to_json)
           (Some Values.CreateRoomMembershipResponse.error_to_json)])
let create_sip_media_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsRegion =
         flag "aws-region" (required string) ~doc:"STRING String"
       and name =
         flag "name" (required string) ~doc:"STRING SipMediaApplicationName"
       and endpoints =
         flag "endpoints" (required json_arg)
           ~doc:"JSON SipMediaApplicationEndpointList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sip_media_application
           (Values.CreateSipMediaApplicationRequest.make ~awsRegion ~name
              ~endpoints:(Values.SipMediaApplicationEndpointList.of_json
                            endpoints) ())
           (Some Values.CreateSipMediaApplicationResponse.to_json)
           (Some Values.CreateSipMediaApplicationResponse.error_to_json)])
let create_sip_media_application_call =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipHeaders =
         flag "sip-headers" (optional json_arg) ~doc:"JSON SipHeadersMap"
       and fromPhoneNumber =
         flag "from-phone-number" (required string)
           ~doc:"STRING E164PhoneNumber"
       and toPhoneNumber =
         flag "to-phone-number" (required string)
           ~doc:"STRING E164PhoneNumber"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sip_media_application_call
           (Values.CreateSipMediaApplicationCallRequest.make
              ?sipHeaders:(Option.map ~f:Values.SipHeadersMap.of_json
                             sipHeaders) ~fromPhoneNumber ~toPhoneNumber
              ~sipMediaApplicationId ())
           (Some Values.CreateSipMediaApplicationCallResponse.to_json)
           (Some Values.CreateSipMediaApplicationCallResponse.error_to_json)])
let create_sip_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and disabled =
         flag "disabled" (optional bool) ~doc:"BOOL NullableBoolean"
       and name = flag "name" (required string) ~doc:"STRING SipRuleName"
       and triggerType =
         flag "trigger-type" (required json_arg)
           ~doc:"JSON SipRuleTriggerType"
       and triggerValue =
         flag "trigger-value" (required string) ~doc:"STRING NonEmptyString"
       and targetApplications =
         flag "target-applications" (required json_arg)
           ~doc:"JSON SipRuleTargetApplicationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sip_rule
           (Values.CreateSipRuleRequest.make ?disabled ~name
              ~triggerType:(Values.SipRuleTriggerType.of_json triggerType)
              ~triggerValue
              ~targetApplications:(Values.SipRuleTargetApplicationList.of_json
                                     targetApplications) ())
           (Some Values.CreateSipRuleResponse.to_json)
           (Some Values.CreateSipRuleResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and username = flag "username" (optional string) ~doc:"STRING String"
       and email = flag "email" (optional string) ~doc:"STRING EmailAddress"
       and userType =
         flag "user-type" (optional json_arg) ~doc:"JSON UserType"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?username ?email
              ?userType:(Option.map ~f:Values.UserType.of_json userType)
              ~accountId ()) (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let create_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsRegion =
         flag "aws-region" (optional json_arg)
           ~doc:"JSON VoiceConnectorAwsRegion"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceConnectorName"
       and requireEncryption =
         flag "require-encryption" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_voice_connector
           (Values.CreateVoiceConnectorRequest.make
              ?awsRegion:(Option.map
                            ~f:Values.VoiceConnectorAwsRegion.of_json
                            awsRegion) ~name ~requireEncryption ())
           (Some Values.CreateVoiceConnectorResponse.to_json)
           (Some Values.CreateVoiceConnectorResponse.error_to_json)])
let create_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorItems =
         flag "voice-connector-items" (optional json_arg)
           ~doc:"JSON VoiceConnectorItemList"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceConnectorGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_voice_connector_group
           (Values.CreateVoiceConnectorGroupRequest.make
              ?voiceConnectorItems:(Option.map
                                      ~f:Values.VoiceConnectorItemList.of_json
                                      voiceConnectorItems) ~name ())
           (Some Values.CreateVoiceConnectorGroupResponse.to_json)
           (Some Values.CreateVoiceConnectorGroupResponse.error_to_json)])
let delete_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account (Values.DeleteAccountRequest.make ~accountId ())
           (Some Values.DeleteAccountResponse.to_json)
           (Some Values.DeleteAccountResponse.error_to_json)])
let delete_app_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_instance
           (Values.DeleteAppInstanceRequest.make ~appInstanceArn ()) None
           None])
let delete_app_instance_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceAdminArn =
         flag "app-instance-admin-arn" (required string)
           ~doc:"STRING ChimeArn"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_instance_admin
           (Values.DeleteAppInstanceAdminRequest.make ~appInstanceAdminArn
              ~appInstanceArn ()) None None])
let delete_app_instance_streaming_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_instance_streaming_configurations
           (Values.DeleteAppInstanceStreamingConfigurationsRequest.make
              ~appInstanceArn ()) None None])
let delete_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_instance_user
           (Values.DeleteAppInstanceUserRequest.make ~appInstanceUserArn ())
           None None])
let delete_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendeeId =
         flag "attendee-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_attendee
           (Values.DeleteAttendeeRequest.make ~meetingId ~attendeeId ()) None
           None])
let delete_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel
           (Values.DeleteChannelRequest.make ?chimeBearer ~channelArn ())
           None None])
let delete_channel_ban =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_ban
           (Values.DeleteChannelBanRequest.make ?chimeBearer ~channelArn
              ~memberArn ()) None None])
let delete_channel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_membership
           (Values.DeleteChannelMembershipRequest.make ?chimeBearer
              ~channelArn ~memberArn ()) None None])
let delete_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_message
           (Values.DeleteChannelMessageRequest.make ?chimeBearer ~channelArn
              ~messageId ()) None None])
let delete_channel_moderator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelModeratorArn =
         flag "channel-moderator-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_moderator
           (Values.DeleteChannelModeratorRequest.make ?chimeBearer
              ~channelArn ~channelModeratorArn ()) None None])
let delete_events_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and botId =
         flag "bot-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_events_configuration
           (Values.DeleteEventsConfigurationRequest.make ~accountId ~botId ())
           None None])
let delete_media_capture_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mediaPipelineId =
         flag "media-pipeline-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_media_capture_pipeline
           (Values.DeleteMediaCapturePipelineRequest.make ~mediaPipelineId ())
           None None])
let delete_meeting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_meeting (Values.DeleteMeetingRequest.make ~meetingId ())
           None None])
let delete_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberId =
         flag "phone-number-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_phone_number
           (Values.DeletePhoneNumberRequest.make ~phoneNumberId ()) None None])
let delete_proxy_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and proxySessionId =
         flag "proxy-session-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_proxy_session
           (Values.DeleteProxySessionRequest.make ~voiceConnectorId
              ~proxySessionId ()) None None])
let delete_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_room
           (Values.DeleteRoomRequest.make ~accountId ~roomId ()) None None])
let delete_room_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_room_membership
           (Values.DeleteRoomMembershipRequest.make ~accountId ~roomId
              ~memberId ()) None None])
let delete_sip_media_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sip_media_application
           (Values.DeleteSipMediaApplicationRequest.make
              ~sipMediaApplicationId ()) None None])
let delete_sip_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipRuleId =
         flag "sip-rule-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sip_rule
           (Values.DeleteSipRuleRequest.make ~sipRuleId ()) None None])
let delete_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector
           (Values.DeleteVoiceConnectorRequest.make ~voiceConnectorId ())
           None None])
let delete_voice_connector_emergency_calling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_emergency_calling_configuration
           (Values.DeleteVoiceConnectorEmergencyCallingConfigurationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_group
           (Values.DeleteVoiceConnectorGroupRequest.make
              ~voiceConnectorGroupId ()) None None])
let delete_voice_connector_origination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_origination
           (Values.DeleteVoiceConnectorOriginationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_proxy
           (Values.DeleteVoiceConnectorProxyRequest.make ~voiceConnectorId ())
           None None])
let delete_voice_connector_streaming_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_streaming_configuration
           (Values.DeleteVoiceConnectorStreamingConfigurationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_termination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_termination
           (Values.DeleteVoiceConnectorTerminationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_termination_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and usernames =
         flag "usernames" (required json_arg) ~doc:"JSON SensitiveStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_termination_credentials
           (Values.DeleteVoiceConnectorTerminationCredentialsRequest.make
              ~voiceConnectorId
              ~usernames:(Values.SensitiveStringList.of_json usernames) ())
           None None])
let describe_app_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_instance
           (Values.DescribeAppInstanceRequest.make ~appInstanceArn ())
           (Some Values.DescribeAppInstanceResponse.to_json)
           (Some Values.DescribeAppInstanceResponse.error_to_json)])
let describe_app_instance_admin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceAdminArn =
         flag "app-instance-admin-arn" (required string)
           ~doc:"STRING ChimeArn"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_instance_admin
           (Values.DescribeAppInstanceAdminRequest.make ~appInstanceAdminArn
              ~appInstanceArn ())
           (Some Values.DescribeAppInstanceAdminResponse.to_json)
           (Some Values.DescribeAppInstanceAdminResponse.error_to_json)])
let describe_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_instance_user
           (Values.DescribeAppInstanceUserRequest.make ~appInstanceUserArn ())
           (Some Values.DescribeAppInstanceUserResponse.to_json)
           (Some Values.DescribeAppInstanceUserResponse.error_to_json)])
let describe_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel
           (Values.DescribeChannelRequest.make ?chimeBearer ~channelArn ())
           (Some Values.DescribeChannelResponse.to_json)
           (Some Values.DescribeChannelResponse.error_to_json)])
let describe_channel_ban =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_ban
           (Values.DescribeChannelBanRequest.make ?chimeBearer ~channelArn
              ~memberArn ()) (Some Values.DescribeChannelBanResponse.to_json)
           (Some Values.DescribeChannelBanResponse.error_to_json)])
let describe_channel_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and memberArn =
         flag "member-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_membership
           (Values.DescribeChannelMembershipRequest.make ?chimeBearer
              ~channelArn ~memberArn ())
           (Some Values.DescribeChannelMembershipResponse.to_json)
           (Some Values.DescribeChannelMembershipResponse.error_to_json)])
let describe_channel_membership_for_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_membership_for_app_instance_user
           (Values.DescribeChannelMembershipForAppInstanceUserRequest.make
              ?chimeBearer ~channelArn ~appInstanceUserArn ())
           (Some
              Values.DescribeChannelMembershipForAppInstanceUserResponse.to_json)
           (Some
              Values.DescribeChannelMembershipForAppInstanceUserResponse.error_to_json)])
let describe_channel_moderated_by_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_moderated_by_app_instance_user
           (Values.DescribeChannelModeratedByAppInstanceUserRequest.make
              ?chimeBearer ~channelArn ~appInstanceUserArn ())
           (Some
              Values.DescribeChannelModeratedByAppInstanceUserResponse.to_json)
           (Some
              Values.DescribeChannelModeratedByAppInstanceUserResponse.error_to_json)])
let describe_channel_moderator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and channelModeratorArn =
         flag "channel-moderator-arn" (required string)
           ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel_moderator
           (Values.DescribeChannelModeratorRequest.make ?chimeBearer
              ~channelArn ~channelModeratorArn ())
           (Some Values.DescribeChannelModeratorResponse.to_json)
           (Some Values.DescribeChannelModeratorResponse.error_to_json)])
let disassociate_phone_number_from_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING String"
       and userId = flag "user-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_phone_number_from_user
           (Values.DisassociatePhoneNumberFromUserRequest.make ~accountId
              ~userId ())
           (Some Values.DisassociatePhoneNumberFromUserResponse.to_json)
           (Some Values.DisassociatePhoneNumberFromUserResponse.error_to_json)])
let disassociate_phone_numbers_from_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_phone_numbers_from_voice_connector
           (Values.DisassociatePhoneNumbersFromVoiceConnectorRequest.make
              ~voiceConnectorId
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some
              Values.DisassociatePhoneNumbersFromVoiceConnectorResponse.to_json)
           (Some
              Values.DisassociatePhoneNumbersFromVoiceConnectorResponse.error_to_json)])
let disassociate_phone_numbers_from_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_phone_numbers_from_voice_connector_group
           (Values.DisassociatePhoneNumbersFromVoiceConnectorGroupRequest.make
              ~voiceConnectorGroupId
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some
              Values.DisassociatePhoneNumbersFromVoiceConnectorGroupResponse.to_json)
           (Some
              Values.DisassociatePhoneNumbersFromVoiceConnectorGroupResponse.error_to_json)])
let disassociate_signin_delegate_groups_from_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and groupNames =
         flag "group-names" (required json_arg)
           ~doc:"JSON NonEmptyStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_signin_delegate_groups_from_account
           (Values.DisassociateSigninDelegateGroupsFromAccountRequest.make
              ~accountId
              ~groupNames:(Values.NonEmptyStringList.of_json groupNames) ())
           (Some
              Values.DisassociateSigninDelegateGroupsFromAccountResponse.to_json)
           (Some
              Values.DisassociateSigninDelegateGroupsFromAccountResponse.error_to_json)])
let get_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account (Values.GetAccountRequest.make ~accountId ())
           (Some Values.GetAccountResponse.to_json)
           (Some Values.GetAccountResponse.error_to_json)])
let get_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_settings
           (Values.GetAccountSettingsRequest.make ~accountId ())
           (Some Values.GetAccountSettingsResponse.to_json)
           (Some Values.GetAccountSettingsResponse.error_to_json)])
let get_app_instance_retention_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_instance_retention_settings
           (Values.GetAppInstanceRetentionSettingsRequest.make
              ~appInstanceArn ())
           (Some Values.GetAppInstanceRetentionSettingsResponse.to_json)
           (Some Values.GetAppInstanceRetentionSettingsResponse.error_to_json)])
let get_app_instance_streaming_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app_instance_streaming_configurations
           (Values.GetAppInstanceStreamingConfigurationsRequest.make
              ~appInstanceArn ())
           (Some Values.GetAppInstanceStreamingConfigurationsResponse.to_json)
           (Some
              Values.GetAppInstanceStreamingConfigurationsResponse.error_to_json)])
let get_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendeeId =
         flag "attendee-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_attendee
           (Values.GetAttendeeRequest.make ~meetingId ~attendeeId ())
           (Some Values.GetAttendeeResponse.to_json)
           (Some Values.GetAttendeeResponse.error_to_json)])
let get_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and botId =
         flag "bot-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bot (Values.GetBotRequest.make ~accountId ~botId ())
           (Some Values.GetBotResponse.to_json)
           (Some Values.GetBotResponse.error_to_json)])
let get_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_channel_message
           (Values.GetChannelMessageRequest.make ?chimeBearer ~channelArn
              ~messageId ()) (Some Values.GetChannelMessageResponse.to_json)
           (Some Values.GetChannelMessageResponse.error_to_json)])
let get_events_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and botId =
         flag "bot-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_events_configuration
           (Values.GetEventsConfigurationRequest.make ~accountId ~botId ())
           (Some Values.GetEventsConfigurationResponse.to_json)
           (Some Values.GetEventsConfigurationResponse.error_to_json)])
let get_global_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_global_settings (Fn.id ())
           (Some Values.GetGlobalSettingsResponse.to_json)
           (Some Values.GetGlobalSettingsResponse.error_to_json)])
let get_media_capture_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mediaPipelineId =
         flag "media-pipeline-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_media_capture_pipeline
           (Values.GetMediaCapturePipelineRequest.make ~mediaPipelineId ())
           (Some Values.GetMediaCapturePipelineResponse.to_json)
           (Some Values.GetMediaCapturePipelineResponse.error_to_json)])
let get_meeting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_meeting (Values.GetMeetingRequest.make ~meetingId ())
           (Some Values.GetMeetingResponse.to_json)
           (Some Values.GetMeetingResponse.error_to_json)])
let get_messaging_session_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_messaging_session_endpoint
           (Values.GetMessagingSessionEndpointRequest.make ())
           (Some Values.GetMessagingSessionEndpointResponse.to_json)
           (Some Values.GetMessagingSessionEndpointResponse.error_to_json)])
let get_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberId =
         flag "phone-number-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_phone_number
           (Values.GetPhoneNumberRequest.make ~phoneNumberId ())
           (Some Values.GetPhoneNumberResponse.to_json)
           (Some Values.GetPhoneNumberResponse.error_to_json)])
let get_phone_number_order =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberOrderId =
         flag "phone-number-order-id" (required string)
           ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_phone_number_order
           (Values.GetPhoneNumberOrderRequest.make ~phoneNumberOrderId ())
           (Some Values.GetPhoneNumberOrderResponse.to_json)
           (Some Values.GetPhoneNumberOrderResponse.error_to_json)])
let get_phone_number_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_phone_number_settings (Fn.id ())
           (Some Values.GetPhoneNumberSettingsResponse.to_json)
           (Some Values.GetPhoneNumberSettingsResponse.error_to_json)])
let get_proxy_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and proxySessionId =
         flag "proxy-session-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_proxy_session
           (Values.GetProxySessionRequest.make ~voiceConnectorId
              ~proxySessionId ())
           (Some Values.GetProxySessionResponse.to_json)
           (Some Values.GetProxySessionResponse.error_to_json)])
let get_retention_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_retention_settings
           (Values.GetRetentionSettingsRequest.make ~accountId ())
           (Some Values.GetRetentionSettingsResponse.to_json)
           (Some Values.GetRetentionSettingsResponse.error_to_json)])
let get_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_room (Values.GetRoomRequest.make ~accountId ~roomId ())
           (Some Values.GetRoomResponse.to_json)
           (Some Values.GetRoomResponse.error_to_json)])
let get_sip_media_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sip_media_application
           (Values.GetSipMediaApplicationRequest.make ~sipMediaApplicationId
              ()) (Some Values.GetSipMediaApplicationResponse.to_json)
           (Some Values.GetSipMediaApplicationResponse.error_to_json)])
let get_sip_media_application_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sip_media_application_logging_configuration
           (Values.GetSipMediaApplicationLoggingConfigurationRequest.make
              ~sipMediaApplicationId ())
           (Some
              Values.GetSipMediaApplicationLoggingConfigurationResponse.to_json)
           (Some
              Values.GetSipMediaApplicationLoggingConfigurationResponse.error_to_json)])
let get_sip_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipRuleId =
         flag "sip-rule-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sip_rule (Values.GetSipRuleRequest.make ~sipRuleId ())
           (Some Values.GetSipRuleResponse.to_json)
           (Some Values.GetSipRuleResponse.error_to_json)])
let get_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and userId =
         flag "user-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user (Values.GetUserRequest.make ~accountId ~userId ())
           (Some Values.GetUserResponse.to_json)
           (Some Values.GetUserResponse.error_to_json)])
let get_user_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING String"
       and userId = flag "user-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user_settings
           (Values.GetUserSettingsRequest.make ~accountId ~userId ())
           (Some Values.GetUserSettingsResponse.to_json)
           (Some Values.GetUserSettingsResponse.error_to_json)])
let get_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector
           (Values.GetVoiceConnectorRequest.make ~voiceConnectorId ())
           (Some Values.GetVoiceConnectorResponse.to_json)
           (Some Values.GetVoiceConnectorResponse.error_to_json)])
let get_voice_connector_emergency_calling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_emergency_calling_configuration
           (Values.GetVoiceConnectorEmergencyCallingConfigurationRequest.make
              ~voiceConnectorId ())
           (Some
              Values.GetVoiceConnectorEmergencyCallingConfigurationResponse.to_json)
           (Some
              Values.GetVoiceConnectorEmergencyCallingConfigurationResponse.error_to_json)])
let get_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_group
           (Values.GetVoiceConnectorGroupRequest.make ~voiceConnectorGroupId
              ()) (Some Values.GetVoiceConnectorGroupResponse.to_json)
           (Some Values.GetVoiceConnectorGroupResponse.error_to_json)])
let get_voice_connector_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_logging_configuration
           (Values.GetVoiceConnectorLoggingConfigurationRequest.make
              ~voiceConnectorId ())
           (Some Values.GetVoiceConnectorLoggingConfigurationResponse.to_json)
           (Some
              Values.GetVoiceConnectorLoggingConfigurationResponse.error_to_json)])
let get_voice_connector_origination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_origination
           (Values.GetVoiceConnectorOriginationRequest.make ~voiceConnectorId
              ()) (Some Values.GetVoiceConnectorOriginationResponse.to_json)
           (Some Values.GetVoiceConnectorOriginationResponse.error_to_json)])
let get_voice_connector_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_proxy
           (Values.GetVoiceConnectorProxyRequest.make ~voiceConnectorId ())
           (Some Values.GetVoiceConnectorProxyResponse.to_json)
           (Some Values.GetVoiceConnectorProxyResponse.error_to_json)])
let get_voice_connector_streaming_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_streaming_configuration
           (Values.GetVoiceConnectorStreamingConfigurationRequest.make
              ~voiceConnectorId ())
           (Some
              Values.GetVoiceConnectorStreamingConfigurationResponse.to_json)
           (Some
              Values.GetVoiceConnectorStreamingConfigurationResponse.error_to_json)])
let get_voice_connector_termination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_termination
           (Values.GetVoiceConnectorTerminationRequest.make ~voiceConnectorId
              ()) (Some Values.GetVoiceConnectorTerminationResponse.to_json)
           (Some Values.GetVoiceConnectorTerminationResponse.error_to_json)])
let get_voice_connector_termination_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_termination_health
           (Values.GetVoiceConnectorTerminationHealthRequest.make
              ~voiceConnectorId ())
           (Some Values.GetVoiceConnectorTerminationHealthResponse.to_json)
           (Some
              Values.GetVoiceConnectorTerminationHealthResponse.error_to_json)])
let invite_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userType =
         flag "user-type" (optional json_arg) ~doc:"JSON UserType"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and userEmailList =
         flag "user-email-list" (required json_arg) ~doc:"JSON UserEmailList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invite_users
           (Values.InviteUsersRequest.make
              ?userType:(Option.map ~f:Values.UserType.of_json userType)
              ~accountId
              ~userEmailList:(Values.UserEmailList.of_json userEmailList) ())
           (Some Values.InviteUsersResponse.to_json)
           (Some Values.InviteUsersResponse.error_to_json)])
let list_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AccountName"
       and userEmail =
         flag "user-email" (optional string) ~doc:"STRING EmailAddress"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ProfileServiceMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accounts
           (Values.ListAccountsRequest.make ?name ?userEmail ?nextToken
              ?maxResults ()) (Some Values.ListAccountsResponse.to_json)
           (Some Values.ListAccountsResponse.error_to_json)])
let list_app_instance_admins =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_instance_admins
           (Values.ListAppInstanceAdminsRequest.make ?maxResults ?nextToken
              ~appInstanceArn ())
           (Some Values.ListAppInstanceAdminsResponse.to_json)
           (Some Values.ListAppInstanceAdminsResponse.error_to_json)])
let list_app_instance_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_instance_users
           (Values.ListAppInstanceUsersRequest.make ?maxResults ?nextToken
              ~appInstanceArn ())
           (Some Values.ListAppInstanceUsersResponse.to_json)
           (Some Values.ListAppInstanceUsersResponse.error_to_json)])
let list_app_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_app_instances
           (Values.ListAppInstancesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAppInstancesResponse.to_json)
           (Some Values.ListAppInstancesResponse.error_to_json)])
let list_attendee_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendeeId =
         flag "attendee-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attendee_tags
           (Values.ListAttendeeTagsRequest.make ~meetingId ~attendeeId ())
           (Some Values.ListAttendeeTagsResponse.to_json)
           (Some Values.ListAttendeeTagsResponse.error_to_json)])
let list_attendees =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attendees
           (Values.ListAttendeesRequest.make ?nextToken ?maxResults
              ~meetingId ()) (Some Values.ListAttendeesResponse.to_json)
           (Some Values.ListAttendeesResponse.error_to_json)])
let list_bots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bots
           (Values.ListBotsRequest.make ?maxResults ?nextToken ~accountId ())
           (Some Values.ListBotsResponse.to_json)
           (Some Values.ListBotsResponse.error_to_json)])
let list_channel_bans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_bans
           (Values.ListChannelBansRequest.make ?maxResults ?nextToken
              ?chimeBearer ~channelArn ())
           (Some Values.ListChannelBansResponse.to_json)
           (Some Values.ListChannelBansResponse.error_to_json)])
let list_channel_memberships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ChannelMembershipType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_memberships
           (Values.ListChannelMembershipsRequest.make
              ?type_:(Option.map ~f:Values.ChannelMembershipType.of_json
                        type_) ?maxResults ?nextToken ?chimeBearer
              ~channelArn ())
           (Some Values.ListChannelMembershipsResponse.to_json)
           (Some Values.ListChannelMembershipsResponse.error_to_json)])
let list_channel_memberships_for_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (optional string)
           ~doc:"STRING ChimeArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_memberships_for_app_instance_user
           (Values.ListChannelMembershipsForAppInstanceUserRequest.make
              ?appInstanceUserArn ?maxResults ?nextToken ?chimeBearer ())
           (Some
              Values.ListChannelMembershipsForAppInstanceUserResponse.to_json)
           (Some
              Values.ListChannelMembershipsForAppInstanceUserResponse.error_to_json)])
let list_channel_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and notBefore =
         flag "not-before" (optional json_arg) ~doc:"JSON Timestamp"
       and notAfter =
         flag "not-after" (optional json_arg) ~doc:"JSON Timestamp"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_messages
           (Values.ListChannelMessagesRequest.make
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?notBefore:(Option.map ~f:Values.Timestamp.of_json notBefore)
              ?notAfter:(Option.map ~f:Values.Timestamp.of_json notAfter)
              ?maxResults ?nextToken ?chimeBearer ~channelArn ())
           (Some Values.ListChannelMessagesResponse.to_json)
           (Some Values.ListChannelMessagesResponse.error_to_json)])
let list_channel_moderators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_moderators
           (Values.ListChannelModeratorsRequest.make ?maxResults ?nextToken
              ?chimeBearer ~channelArn ())
           (Some Values.ListChannelModeratorsResponse.to_json)
           (Some Values.ListChannelModeratorsResponse.error_to_json)])
let list_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and privacy =
         flag "privacy" (optional json_arg) ~doc:"JSON ChannelPrivacy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels
           (Values.ListChannelsRequest.make
              ?privacy:(Option.map ~f:Values.ChannelPrivacy.of_json privacy)
              ?maxResults ?nextToken ?chimeBearer ~appInstanceArn ())
           (Some Values.ListChannelsResponse.to_json)
           (Some Values.ListChannelsResponse.error_to_json)])
let list_channels_moderated_by_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (optional string)
           ~doc:"STRING ChimeArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels_moderated_by_app_instance_user
           (Values.ListChannelsModeratedByAppInstanceUserRequest.make
              ?appInstanceUserArn ?maxResults ?nextToken ?chimeBearer ())
           (Some
              Values.ListChannelsModeratedByAppInstanceUserResponse.to_json)
           (Some
              Values.ListChannelsModeratedByAppInstanceUserResponse.error_to_json)])
let list_media_capture_pipelines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_media_capture_pipelines
           (Values.ListMediaCapturePipelinesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListMediaCapturePipelinesResponse.to_json)
           (Some Values.ListMediaCapturePipelinesResponse.error_to_json)])
let list_meeting_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_meeting_tags
           (Values.ListMeetingTagsRequest.make ~meetingId ())
           (Some Values.ListMeetingTagsResponse.to_json)
           (Some Values.ListMeetingTagsResponse.error_to_json)])
let list_meetings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_meetings
           (Values.ListMeetingsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListMeetingsResponse.to_json)
           (Some Values.ListMeetingsResponse.error_to_json)])
let list_phone_number_orders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_phone_number_orders
           (Values.ListPhoneNumberOrdersRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListPhoneNumberOrdersResponse.to_json)
           (Some Values.ListPhoneNumberOrdersResponse.error_to_json)])
let list_phone_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON PhoneNumberStatus"
       and productType =
         flag "product-type" (optional json_arg)
           ~doc:"JSON PhoneNumberProductType"
       and filterName =
         flag "filter-name" (optional json_arg)
           ~doc:"JSON PhoneNumberAssociationName"
       and filterValue =
         flag "filter-value" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_phone_numbers
           (Values.ListPhoneNumbersRequest.make
              ?status:(Option.map ~f:Values.PhoneNumberStatus.of_json status)
              ?productType:(Option.map
                              ~f:Values.PhoneNumberProductType.of_json
                              productType)
              ?filterName:(Option.map
                             ~f:Values.PhoneNumberAssociationName.of_json
                             filterName) ?filterValue ?maxResults ?nextToken
              ()) (Some Values.ListPhoneNumbersResponse.to_json)
           (Some Values.ListPhoneNumbersResponse.error_to_json)])
let list_proxy_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ProxySessionStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_proxy_sessions
           (Values.ListProxySessionsRequest.make
              ?status:(Option.map ~f:Values.ProxySessionStatus.of_json status)
              ?nextToken ?maxResults ~voiceConnectorId ())
           (Some Values.ListProxySessionsResponse.to_json)
           (Some Values.ListProxySessionsResponse.error_to_json)])
let list_room_memberships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_room_memberships
           (Values.ListRoomMembershipsRequest.make ?maxResults ?nextToken
              ~accountId ~roomId ())
           (Some Values.ListRoomMembershipsResponse.to_json)
           (Some Values.ListRoomMembershipsResponse.error_to_json)])
let list_rooms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberId = flag "member-id" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rooms
           (Values.ListRoomsRequest.make ?memberId ?maxResults ?nextToken
              ~accountId ()) (Some Values.ListRoomsResponse.to_json)
           (Some Values.ListRoomsResponse.error_to_json)])
let list_sip_media_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sip_media_applications
           (Values.ListSipMediaApplicationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListSipMediaApplicationsResponse.to_json)
           (Some Values.ListSipMediaApplicationsResponse.error_to_json)])
let list_sip_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (optional string)
           ~doc:"STRING NonEmptyString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sip_rules
           (Values.ListSipRulesRequest.make ?sipMediaApplicationId
              ?maxResults ?nextToken ())
           (Some Values.ListSipRulesResponse.to_json)
           (Some Values.ListSipRulesResponse.error_to_json)])
let list_supported_phone_number_countries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and productType =
         flag "product-type" (required json_arg)
           ~doc:"JSON PhoneNumberProductType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_supported_phone_number_countries
           (Values.ListSupportedPhoneNumberCountriesRequest.make
              ~productType:(Values.PhoneNumberProductType.of_json productType)
              ())
           (Some Values.ListSupportedPhoneNumberCountriesResponse.to_json)
           (Some
              Values.ListSupportedPhoneNumberCountriesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userEmail =
         flag "user-email" (optional string) ~doc:"STRING EmailAddress"
       and userType =
         flag "user-type" (optional json_arg) ~doc:"JSON UserType"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ProfileServiceMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?userEmail
              ?userType:(Option.map ~f:Values.UserType.of_json userType)
              ?maxResults ?nextToken ~accountId ())
           (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let list_voice_connector_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_voice_connector_groups
           (Values.ListVoiceConnectorGroupsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListVoiceConnectorGroupsResponse.to_json)
           (Some Values.ListVoiceConnectorGroupsResponse.error_to_json)])
let list_voice_connector_termination_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_voice_connector_termination_credentials
           (Values.ListVoiceConnectorTerminationCredentialsRequest.make
              ~voiceConnectorId ())
           (Some
              Values.ListVoiceConnectorTerminationCredentialsResponse.to_json)
           (Some
              Values.ListVoiceConnectorTerminationCredentialsResponse.error_to_json)])
let list_voice_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_voice_connectors
           (Values.ListVoiceConnectorsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListVoiceConnectorsResponse.to_json)
           (Some Values.ListVoiceConnectorsResponse.error_to_json)])
let logout_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and userId =
         flag "user-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.logout_user
           (Values.LogoutUserRequest.make ~accountId ~userId ())
           (Some Values.LogoutUserResponse.to_json)
           (Some Values.LogoutUserResponse.error_to_json)])
let put_app_instance_retention_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceRetentionSettings =
         flag "app-instance-retention-settings" (required json_arg)
           ~doc:"JSON AppInstanceRetentionSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_app_instance_retention_settings
           (Values.PutAppInstanceRetentionSettingsRequest.make
              ~appInstanceArn
              ~appInstanceRetentionSettings:(Values.AppInstanceRetentionSettings.of_json
                                               appInstanceRetentionSettings)
              ())
           (Some Values.PutAppInstanceRetentionSettingsResponse.to_json)
           (Some Values.PutAppInstanceRetentionSettingsResponse.error_to_json)])
let put_app_instance_streaming_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and appInstanceStreamingConfigurations =
         flag "app-instance-streaming-configurations" (required json_arg)
           ~doc:"JSON AppInstanceStreamingConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_app_instance_streaming_configurations
           (Values.PutAppInstanceStreamingConfigurationsRequest.make
              ~appInstanceArn
              ~appInstanceStreamingConfigurations:(Values.AppInstanceStreamingConfigurationList.of_json
                                                     appInstanceStreamingConfigurations)
              ())
           (Some Values.PutAppInstanceStreamingConfigurationsResponse.to_json)
           (Some
              Values.PutAppInstanceStreamingConfigurationsResponse.error_to_json)])
let put_events_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outboundEventsHTTPSEndpoint =
         flag "outbound-events-h-t-t-p-s-endpoint" (optional string)
           ~doc:"STRING SensitiveString"
       and lambdaFunctionArn =
         flag "lambda-function-arn" (optional string)
           ~doc:"STRING SensitiveString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and botId =
         flag "bot-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_events_configuration
           (Values.PutEventsConfigurationRequest.make
              ?outboundEventsHTTPSEndpoint ?lambdaFunctionArn ~accountId
              ~botId ()) (Some Values.PutEventsConfigurationResponse.to_json)
           (Some Values.PutEventsConfigurationResponse.error_to_json)])
let put_retention_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and retentionSettings =
         flag "retention-settings" (required json_arg)
           ~doc:"JSON RetentionSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_retention_settings
           (Values.PutRetentionSettingsRequest.make ~accountId
              ~retentionSettings:(Values.RetentionSettings.of_json
                                    retentionSettings) ())
           (Some Values.PutRetentionSettingsResponse.to_json)
           (Some Values.PutRetentionSettingsResponse.error_to_json)])
let put_sip_media_application_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationLoggingConfiguration =
         flag "sip-media-application-logging-configuration"
           (optional json_arg)
           ~doc:"JSON SipMediaApplicationLoggingConfiguration"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_sip_media_application_logging_configuration
           (Values.PutSipMediaApplicationLoggingConfigurationRequest.make
              ?sipMediaApplicationLoggingConfiguration:(Option.map
                                                          ~f:Values.SipMediaApplicationLoggingConfiguration.of_json
                                                          sipMediaApplicationLoggingConfiguration)
              ~sipMediaApplicationId ())
           (Some
              Values.PutSipMediaApplicationLoggingConfigurationResponse.to_json)
           (Some
              Values.PutSipMediaApplicationLoggingConfigurationResponse.error_to_json)])
let put_voice_connector_emergency_calling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and emergencyCallingConfiguration =
         flag "emergency-calling-configuration" (required json_arg)
           ~doc:"JSON EmergencyCallingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_emergency_calling_configuration
           (Values.PutVoiceConnectorEmergencyCallingConfigurationRequest.make
              ~voiceConnectorId
              ~emergencyCallingConfiguration:(Values.EmergencyCallingConfiguration.of_json
                                                emergencyCallingConfiguration)
              ())
           (Some
              Values.PutVoiceConnectorEmergencyCallingConfigurationResponse.to_json)
           (Some
              Values.PutVoiceConnectorEmergencyCallingConfigurationResponse.error_to_json)])
let put_voice_connector_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and loggingConfiguration =
         flag "logging-configuration" (required json_arg)
           ~doc:"JSON LoggingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_logging_configuration
           (Values.PutVoiceConnectorLoggingConfigurationRequest.make
              ~voiceConnectorId
              ~loggingConfiguration:(Values.LoggingConfiguration.of_json
                                       loggingConfiguration) ())
           (Some Values.PutVoiceConnectorLoggingConfigurationResponse.to_json)
           (Some
              Values.PutVoiceConnectorLoggingConfigurationResponse.error_to_json)])
let put_voice_connector_origination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and origination =
         flag "origination" (required json_arg) ~doc:"JSON Origination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_origination
           (Values.PutVoiceConnectorOriginationRequest.make ~voiceConnectorId
              ~origination:(Values.Origination.of_json origination) ())
           (Some Values.PutVoiceConnectorOriginationResponse.to_json)
           (Some Values.PutVoiceConnectorOriginationResponse.error_to_json)])
let put_voice_connector_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fallBackPhoneNumber =
         flag "fall-back-phone-number" (optional string)
           ~doc:"STRING E164PhoneNumber"
       and disabled = flag "disabled" (optional bool) ~doc:"BOOL Boolean"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and defaultSessionExpiryMinutes =
         flag "default-session-expiry-minutes" (required int)
           ~doc:"INT Integer"
       and phoneNumberPoolCountries =
         flag "phone-number-pool-countries" (required json_arg)
           ~doc:"JSON CountryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_proxy
           (Values.PutVoiceConnectorProxyRequest.make ?fallBackPhoneNumber
              ?disabled ~voiceConnectorId ~defaultSessionExpiryMinutes
              ~phoneNumberPoolCountries:(Values.CountryList.of_json
                                           phoneNumberPoolCountries) ())
           (Some Values.PutVoiceConnectorProxyResponse.to_json)
           (Some Values.PutVoiceConnectorProxyResponse.error_to_json)])
let put_voice_connector_streaming_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and streamingConfiguration =
         flag "streaming-configuration" (required json_arg)
           ~doc:"JSON StreamingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_streaming_configuration
           (Values.PutVoiceConnectorStreamingConfigurationRequest.make
              ~voiceConnectorId
              ~streamingConfiguration:(Values.StreamingConfiguration.of_json
                                         streamingConfiguration) ())
           (Some
              Values.PutVoiceConnectorStreamingConfigurationResponse.to_json)
           (Some
              Values.PutVoiceConnectorStreamingConfigurationResponse.error_to_json)])
let put_voice_connector_termination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and termination =
         flag "termination" (required json_arg) ~doc:"JSON Termination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_termination
           (Values.PutVoiceConnectorTerminationRequest.make ~voiceConnectorId
              ~termination:(Values.Termination.of_json termination) ())
           (Some Values.PutVoiceConnectorTerminationResponse.to_json)
           (Some Values.PutVoiceConnectorTerminationResponse.error_to_json)])
let put_voice_connector_termination_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and credentials =
         flag "credentials" (optional json_arg) ~doc:"JSON CredentialList"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_termination_credentials
           (Values.PutVoiceConnectorTerminationCredentialsRequest.make
              ?credentials:(Option.map ~f:Values.CredentialList.of_json
                              credentials) ~voiceConnectorId ()) None None])
let redact_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.redact_channel_message
           (Values.RedactChannelMessageRequest.make ?chimeBearer ~channelArn
              ~messageId ())
           (Some Values.RedactChannelMessageResponse.to_json)
           (Some Values.RedactChannelMessageResponse.error_to_json)])
let redact_conversation_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and conversationId =
         flag "conversation-id" (required string)
           ~doc:"STRING NonEmptyString"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.redact_conversation_message
           (Values.RedactConversationMessageRequest.make ~accountId
              ~conversationId ~messageId ())
           (Some Values.RedactConversationMessageResponse.to_json)
           (Some Values.RedactConversationMessageResponse.error_to_json)])
let redact_room_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.redact_room_message
           (Values.RedactRoomMessageRequest.make ~accountId ~roomId
              ~messageId ()) (Some Values.RedactRoomMessageResponse.to_json)
           (Some Values.RedactRoomMessageResponse.error_to_json)])
let regenerate_security_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and botId =
         flag "bot-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.regenerate_security_token
           (Values.RegenerateSecurityTokenRequest.make ~accountId ~botId ())
           (Some Values.RegenerateSecurityTokenResponse.to_json)
           (Some Values.RegenerateSecurityTokenResponse.error_to_json)])
let reset_personal_p_i_n =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and userId =
         flag "user-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_personal_p_i_n
           (Values.ResetPersonalPINRequest.make ~accountId ~userId ())
           (Some Values.ResetPersonalPINResponse.to_json)
           (Some Values.ResetPersonalPINResponse.error_to_json)])
let restore_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberId =
         flag "phone-number-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_phone_number
           (Values.RestorePhoneNumberRequest.make ~phoneNumberId ())
           (Some Values.RestorePhoneNumberResponse.to_json)
           (Some Values.RestorePhoneNumberResponse.error_to_json)])
let search_available_phone_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and areaCode = flag "area-code" (optional string) ~doc:"STRING String"
       and city = flag "city" (optional string) ~doc:"STRING String"
       and country =
         flag "country" (optional string) ~doc:"STRING Alpha2CountryCode"
       and state = flag "state" (optional string) ~doc:"STRING String"
       and tollFreePrefix =
         flag "toll-free-prefix" (optional string)
           ~doc:"STRING TollFreePrefix"
       and phoneNumberType =
         flag "phone-number-type" (optional json_arg)
           ~doc:"JSON PhoneNumberType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PhoneNumberMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_available_phone_numbers
           (Values.SearchAvailablePhoneNumbersRequest.make ?areaCode ?city
              ?country ?state ?tollFreePrefix
              ?phoneNumberType:(Option.map ~f:Values.PhoneNumberType.of_json
                                  phoneNumberType) ?maxResults ?nextToken ())
           (Some Values.SearchAvailablePhoneNumbersResponse.to_json)
           (Some Values.SearchAvailablePhoneNumbersResponse.error_to_json)])
let send_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and content =
         flag "content" (required string) ~doc:"STRING NonEmptyContent"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ChannelMessageType"
       and persistence =
         flag "persistence" (required json_arg)
           ~doc:"JSON ChannelMessagePersistenceType"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_channel_message
           (Values.SendChannelMessageRequest.make ?metadata ?chimeBearer
              ~channelArn ~content
              ~type_:(Values.ChannelMessageType.of_json type_)
              ~persistence:(Values.ChannelMessagePersistenceType.of_json
                              persistence) ~clientRequestToken ())
           (Some Values.SendChannelMessageResponse.to_json)
           (Some Values.SendChannelMessageResponse.error_to_json)])
let start_meeting_transcription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and transcriptionConfiguration =
         flag "transcription-configuration" (required json_arg)
           ~doc:"JSON TranscriptionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_meeting_transcription
           (Values.StartMeetingTranscriptionRequest.make ~meetingId
              ~transcriptionConfiguration:(Values.TranscriptionConfiguration.of_json
                                             transcriptionConfiguration) ())
           (Some Values.StartMeetingTranscriptionResponse.to_json)
           (Some Values.StartMeetingTranscriptionResponse.error_to_json)])
let stop_meeting_transcription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_meeting_transcription
           (Values.StopMeetingTranscriptionRequest.make ~meetingId ())
           (Some Values.StopMeetingTranscriptionResponse.to_json)
           (Some Values.StopMeetingTranscriptionResponse.error_to_json)])
let tag_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendeeId =
         flag "attendee-id" (required string) ~doc:"STRING GuidString"
       and tags = flag "tags" (required json_arg) ~doc:"JSON AttendeeTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_attendee
           (Values.TagAttendeeRequest.make ~meetingId ~attendeeId
              ~tags:(Values.AttendeeTagList.of_json tags) ()) None None])
let tag_meeting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and tags = flag "tags" (required json_arg) ~doc:"JSON MeetingTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_meeting
           (Values.TagMeetingRequest.make ~meetingId
              ~tags:(Values.MeetingTagList.of_json tags) ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ()) None None])
let untag_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendeeId =
         flag "attendee-id" (required string) ~doc:"STRING GuidString"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON AttendeeTagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_attendee
           (Values.UntagAttendeeRequest.make ~meetingId ~attendeeId
              ~tagKeys:(Values.AttendeeTagKeyList.of_json tagKeys) ()) None
           None])
let untag_meeting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON MeetingTagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_meeting
           (Values.UntagMeetingRequest.make ~meetingId
              ~tagKeys:(Values.MeetingTagKeyList.of_json tagKeys) ()) None
           None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AccountName"
       and defaultLicense =
         flag "default-license" (optional json_arg) ~doc:"JSON License"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account
           (Values.UpdateAccountRequest.make ?name
              ?defaultLicense:(Option.map ~f:Values.License.of_json
                                 defaultLicense) ~accountId ())
           (Some Values.UpdateAccountResponse.to_json)
           (Some Values.UpdateAccountResponse.error_to_json)])
let update_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and accountSettings =
         flag "account-settings" (required json_arg)
           ~doc:"JSON AccountSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_settings
           (Values.UpdateAccountSettingsRequest.make ~accountId
              ~accountSettings:(Values.AccountSettings.of_json
                                  accountSettings) ())
           (Some Values.UpdateAccountSettingsResponse.to_json)
           (Some Values.UpdateAccountSettingsResponse.error_to_json)])
let update_app_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and appInstanceArn =
         flag "app-instance-arn" (required string) ~doc:"STRING ChimeArn"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app_instance
           (Values.UpdateAppInstanceRequest.make ?metadata ~appInstanceArn
              ~name ()) (Some Values.UpdateAppInstanceResponse.to_json)
           (Some Values.UpdateAppInstanceResponse.error_to_json)])
let update_app_instance_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and appInstanceUserArn =
         flag "app-instance-user-arn" (required string)
           ~doc:"STRING ChimeArn"
       and name = flag "name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app_instance_user
           (Values.UpdateAppInstanceUserRequest.make ?metadata
              ~appInstanceUserArn ~name ())
           (Some Values.UpdateAppInstanceUserResponse.to_json)
           (Some Values.UpdateAppInstanceUserResponse.error_to_json)])
let update_bot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and disabled =
         flag "disabled" (optional bool) ~doc:"BOOL NullableBoolean"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and botId =
         flag "bot-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bot
           (Values.UpdateBotRequest.make ?disabled ~accountId ~botId ())
           (Some Values.UpdateBotResponse.to_json)
           (Some Values.UpdateBotResponse.error_to_json)])
let update_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and name =
         flag "name" (required string) ~doc:"STRING NonEmptyResourceName"
       and mode = flag "mode" (required json_arg) ~doc:"JSON ChannelMode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel
           (Values.UpdateChannelRequest.make ?metadata ?chimeBearer
              ~channelArn ~name ~mode:(Values.ChannelMode.of_json mode) ())
           (Some Values.UpdateChannelResponse.to_json)
           (Some Values.UpdateChannelResponse.error_to_json)])
let update_channel_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and content = flag "content" (optional string) ~doc:"STRING Content"
       and metadata =
         flag "metadata" (optional string) ~doc:"STRING Metadata"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel_message
           (Values.UpdateChannelMessageRequest.make ?content ?metadata
              ?chimeBearer ~channelArn ~messageId ())
           (Some Values.UpdateChannelMessageResponse.to_json)
           (Some Values.UpdateChannelMessageResponse.error_to_json)])
let update_channel_read_marker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chimeBearer =
         flag "chime-bearer" (optional string) ~doc:"STRING ChimeArn"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChimeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel_read_marker
           (Values.UpdateChannelReadMarkerRequest.make ?chimeBearer
              ~channelArn ())
           (Some Values.UpdateChannelReadMarkerResponse.to_json)
           (Some Values.UpdateChannelReadMarkerResponse.error_to_json)])
let update_global_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and businessCalling =
         flag "business-calling" (optional json_arg)
           ~doc:"JSON BusinessCallingSettings"
       and voiceConnector =
         flag "voice-connector" (optional json_arg)
           ~doc:"JSON VoiceConnectorSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_global_settings
           (Values.UpdateGlobalSettingsRequest.make
              ?businessCalling:(Option.map
                                  ~f:Values.BusinessCallingSettings.of_json
                                  businessCalling)
              ?voiceConnector:(Option.map
                                 ~f:Values.VoiceConnectorSettings.of_json
                                 voiceConnector) ()) None None])
let update_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and productType =
         flag "product-type" (optional json_arg)
           ~doc:"JSON PhoneNumberProductType"
       and callingName =
         flag "calling-name" (optional string) ~doc:"STRING CallingName"
       and phoneNumberId =
         flag "phone-number-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_phone_number
           (Values.UpdatePhoneNumberRequest.make
              ?productType:(Option.map
                              ~f:Values.PhoneNumberProductType.of_json
                              productType) ?callingName ~phoneNumberId ())
           (Some Values.UpdatePhoneNumberResponse.to_json)
           (Some Values.UpdatePhoneNumberResponse.error_to_json)])
let update_phone_number_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callingName =
         flag "calling-name" (required string) ~doc:"STRING CallingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_phone_number_settings
           (Values.UpdatePhoneNumberSettingsRequest.make ~callingName ())
           None None])
let update_proxy_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expiryMinutes =
         flag "expiry-minutes" (optional int) ~doc:"INT PositiveInteger"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and proxySessionId =
         flag "proxy-session-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and capabilities =
         flag "capabilities" (required json_arg) ~doc:"JSON CapabilityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_proxy_session
           (Values.UpdateProxySessionRequest.make ?expiryMinutes
              ~voiceConnectorId ~proxySessionId
              ~capabilities:(Values.CapabilityList.of_json capabilities) ())
           (Some Values.UpdateProxySessionResponse.to_json)
           (Some Values.UpdateProxySessionResponse.error_to_json)])
let update_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING SensitiveString"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_room
           (Values.UpdateRoomRequest.make ?name ~accountId ~roomId ())
           (Some Values.UpdateRoomResponse.to_json)
           (Some Values.UpdateRoomResponse.error_to_json)])
let update_room_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and role =
         flag "role" (optional json_arg) ~doc:"JSON RoomMembershipRole"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and roomId =
         flag "room-id" (required string) ~doc:"STRING NonEmptyString"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_room_membership
           (Values.UpdateRoomMembershipRequest.make
              ?role:(Option.map ~f:Values.RoomMembershipRole.of_json role)
              ~accountId ~roomId ~memberId ())
           (Some Values.UpdateRoomMembershipResponse.to_json)
           (Some Values.UpdateRoomMembershipResponse.error_to_json)])
let update_sip_media_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING SipMediaApplicationName"
       and endpoints =
         flag "endpoints" (optional json_arg)
           ~doc:"JSON SipMediaApplicationEndpointList"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sip_media_application
           (Values.UpdateSipMediaApplicationRequest.make ?name
              ?endpoints:(Option.map
                            ~f:Values.SipMediaApplicationEndpointList.of_json
                            endpoints) ~sipMediaApplicationId ())
           (Some Values.UpdateSipMediaApplicationResponse.to_json)
           (Some Values.UpdateSipMediaApplicationResponse.error_to_json)])
let update_sip_media_application_call =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString"
       and transactionId =
         flag "transaction-id" (required string) ~doc:"STRING NonEmptyString"
       and arguments =
         flag "arguments" (required json_arg)
           ~doc:"JSON SMAUpdateCallArgumentsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sip_media_application_call
           (Values.UpdateSipMediaApplicationCallRequest.make
              ~sipMediaApplicationId ~transactionId
              ~arguments:(Values.SMAUpdateCallArgumentsMap.of_json arguments)
              ()) (Some Values.UpdateSipMediaApplicationCallResponse.to_json)
           (Some Values.UpdateSipMediaApplicationCallResponse.error_to_json)])
let update_sip_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and disabled =
         flag "disabled" (optional bool) ~doc:"BOOL NullableBoolean"
       and targetApplications =
         flag "target-applications" (optional json_arg)
           ~doc:"JSON SipRuleTargetApplicationList"
       and sipRuleId =
         flag "sip-rule-id" (required string) ~doc:"STRING NonEmptyString"
       and name = flag "name" (required string) ~doc:"STRING SipRuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sip_rule
           (Values.UpdateSipRuleRequest.make ?disabled
              ?targetApplications:(Option.map
                                     ~f:Values.SipRuleTargetApplicationList.of_json
                                     targetApplications) ~sipRuleId ~name ())
           (Some Values.UpdateSipRuleResponse.to_json)
           (Some Values.UpdateSipRuleResponse.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseType =
         flag "license-type" (optional json_arg) ~doc:"JSON License"
       and userType =
         flag "user-type" (optional json_arg) ~doc:"JSON UserType"
       and alexaForBusinessMetadata =
         flag "alexa-for-business-metadata" (optional json_arg)
           ~doc:"JSON AlexaForBusinessMetadata"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING NonEmptyString"
       and userId =
         flag "user-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make
              ?licenseType:(Option.map ~f:Values.License.of_json licenseType)
              ?userType:(Option.map ~f:Values.UserType.of_json userType)
              ?alexaForBusinessMetadata:(Option.map
                                           ~f:Values.AlexaForBusinessMetadata.of_json
                                           alexaForBusinessMetadata)
              ~accountId ~userId ()) (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let update_user_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING String"
       and userId = flag "user-id" (required string) ~doc:"STRING String"
       and userSettings =
         flag "user-settings" (required json_arg) ~doc:"JSON UserSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_settings
           (Values.UpdateUserSettingsRequest.make ~accountId ~userId
              ~userSettings:(Values.UserSettings.of_json userSettings) ())
           None None])
let update_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceConnectorName"
       and requireEncryption =
         flag "require-encryption" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_voice_connector
           (Values.UpdateVoiceConnectorRequest.make ~voiceConnectorId ~name
              ~requireEncryption ())
           (Some Values.UpdateVoiceConnectorResponse.to_json)
           (Some Values.UpdateVoiceConnectorResponse.error_to_json)])
let update_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceConnectorGroupName"
       and voiceConnectorItems =
         flag "voice-connector-items" (required json_arg)
           ~doc:"JSON VoiceConnectorItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_voice_connector_group
           (Values.UpdateVoiceConnectorGroupRequest.make
              ~voiceConnectorGroupId ~name
              ~voiceConnectorItems:(Values.VoiceConnectorItemList.of_json
                                      voiceConnectorItems) ())
           (Some Values.UpdateVoiceConnectorGroupResponse.to_json)
           (Some Values.UpdateVoiceConnectorGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-phone-number-with-user", associate_phone_number_with_user);
    ("associate-phone-numbers-with-voice-connector",
      associate_phone_numbers_with_voice_connector);
    ("associate-phone-numbers-with-voice-connector-group",
      associate_phone_numbers_with_voice_connector_group);
    ("associate-signin-delegate-groups-with-account",
      associate_signin_delegate_groups_with_account);
    ("batch-create-attendee", batch_create_attendee);
    ("batch-create-channel-membership", batch_create_channel_membership);
    ("batch-create-room-membership", batch_create_room_membership);
    ("batch-delete-phone-number", batch_delete_phone_number);
    ("batch-suspend-user", batch_suspend_user);
    ("batch-unsuspend-user", batch_unsuspend_user);
    ("batch-update-phone-number", batch_update_phone_number);
    ("batch-update-user", batch_update_user);
    ("create-account", create_account);
    ("create-app-instance", create_app_instance);
    ("create-app-instance-admin", create_app_instance_admin);
    ("create-app-instance-user", create_app_instance_user);
    ("create-attendee", create_attendee);
    ("create-bot", create_bot);
    ("create-channel", create_channel);
    ("create-channel-ban", create_channel_ban);
    ("create-channel-membership", create_channel_membership);
    ("create-channel-moderator", create_channel_moderator);
    ("create-media-capture-pipeline", create_media_capture_pipeline);
    ("create-meeting", create_meeting);
    ("create-meeting-dial-out", create_meeting_dial_out);
    ("create-meeting-with-attendees", create_meeting_with_attendees);
    ("create-phone-number-order", create_phone_number_order);
    ("create-proxy-session", create_proxy_session);
    ("create-room", create_room);
    ("create-room-membership", create_room_membership);
    ("create-sip-media-application", create_sip_media_application);
    ("create-sip-media-application-call", create_sip_media_application_call);
    ("create-sip-rule", create_sip_rule);
    ("create-user", create_user);
    ("create-voice-connector", create_voice_connector);
    ("create-voice-connector-group", create_voice_connector_group);
    ("delete-account", delete_account);
    ("delete-app-instance", delete_app_instance);
    ("delete-app-instance-admin", delete_app_instance_admin);
    ("delete-app-instance-streaming-configurations",
      delete_app_instance_streaming_configurations);
    ("delete-app-instance-user", delete_app_instance_user);
    ("delete-attendee", delete_attendee);
    ("delete-channel", delete_channel);
    ("delete-channel-ban", delete_channel_ban);
    ("delete-channel-membership", delete_channel_membership);
    ("delete-channel-message", delete_channel_message);
    ("delete-channel-moderator", delete_channel_moderator);
    ("delete-events-configuration", delete_events_configuration);
    ("delete-media-capture-pipeline", delete_media_capture_pipeline);
    ("delete-meeting", delete_meeting);
    ("delete-phone-number", delete_phone_number);
    ("delete-proxy-session", delete_proxy_session);
    ("delete-room", delete_room);
    ("delete-room-membership", delete_room_membership);
    ("delete-sip-media-application", delete_sip_media_application);
    ("delete-sip-rule", delete_sip_rule);
    ("delete-voice-connector", delete_voice_connector);
    ("delete-voice-connector-emergency-calling-configuration",
      delete_voice_connector_emergency_calling_configuration);
    ("delete-voice-connector-group", delete_voice_connector_group);
    ("delete-voice-connector-origination",
      delete_voice_connector_origination);
    ("delete-voice-connector-proxy", delete_voice_connector_proxy);
    ("delete-voice-connector-streaming-configuration",
      delete_voice_connector_streaming_configuration);
    ("delete-voice-connector-termination",
      delete_voice_connector_termination);
    ("delete-voice-connector-termination-credentials",
      delete_voice_connector_termination_credentials);
    ("describe-app-instance", describe_app_instance);
    ("describe-app-instance-admin", describe_app_instance_admin);
    ("describe-app-instance-user", describe_app_instance_user);
    ("describe-channel", describe_channel);
    ("describe-channel-ban", describe_channel_ban);
    ("describe-channel-membership", describe_channel_membership);
    ("describe-channel-membership-for-app-instance-user",
      describe_channel_membership_for_app_instance_user);
    ("describe-channel-moderated-by-app-instance-user",
      describe_channel_moderated_by_app_instance_user);
    ("describe-channel-moderator", describe_channel_moderator);
    ("disassociate-phone-number-from-user",
      disassociate_phone_number_from_user);
    ("disassociate-phone-numbers-from-voice-connector",
      disassociate_phone_numbers_from_voice_connector);
    ("disassociate-phone-numbers-from-voice-connector-group",
      disassociate_phone_numbers_from_voice_connector_group);
    ("disassociate-signin-delegate-groups-from-account",
      disassociate_signin_delegate_groups_from_account);
    ("get-account", get_account);
    ("get-account-settings", get_account_settings);
    ("get-app-instance-retention-settings",
      get_app_instance_retention_settings);
    ("get-app-instance-streaming-configurations",
      get_app_instance_streaming_configurations);
    ("get-attendee", get_attendee);
    ("get-bot", get_bot);
    ("get-channel-message", get_channel_message);
    ("get-events-configuration", get_events_configuration);
    ("get-global-settings", get_global_settings);
    ("get-media-capture-pipeline", get_media_capture_pipeline);
    ("get-meeting", get_meeting);
    ("get-messaging-session-endpoint", get_messaging_session_endpoint);
    ("get-phone-number", get_phone_number);
    ("get-phone-number-order", get_phone_number_order);
    ("get-phone-number-settings", get_phone_number_settings);
    ("get-proxy-session", get_proxy_session);
    ("get-retention-settings", get_retention_settings);
    ("get-room", get_room);
    ("get-sip-media-application", get_sip_media_application);
    ("get-sip-media-application-logging-configuration",
      get_sip_media_application_logging_configuration);
    ("get-sip-rule", get_sip_rule);
    ("get-user", get_user);
    ("get-user-settings", get_user_settings);
    ("get-voice-connector", get_voice_connector);
    ("get-voice-connector-emergency-calling-configuration",
      get_voice_connector_emergency_calling_configuration);
    ("get-voice-connector-group", get_voice_connector_group);
    ("get-voice-connector-logging-configuration",
      get_voice_connector_logging_configuration);
    ("get-voice-connector-origination", get_voice_connector_origination);
    ("get-voice-connector-proxy", get_voice_connector_proxy);
    ("get-voice-connector-streaming-configuration",
      get_voice_connector_streaming_configuration);
    ("get-voice-connector-termination", get_voice_connector_termination);
    ("get-voice-connector-termination-health",
      get_voice_connector_termination_health);
    ("invite-users", invite_users);
    ("list-accounts", list_accounts);
    ("list-app-instance-admins", list_app_instance_admins);
    ("list-app-instance-users", list_app_instance_users);
    ("list-app-instances", list_app_instances);
    ("list-attendee-tags", list_attendee_tags);
    ("list-attendees", list_attendees);
    ("list-bots", list_bots);
    ("list-channel-bans", list_channel_bans);
    ("list-channel-memberships", list_channel_memberships);
    ("list-channel-memberships-for-app-instance-user",
      list_channel_memberships_for_app_instance_user);
    ("list-channel-messages", list_channel_messages);
    ("list-channel-moderators", list_channel_moderators);
    ("list-channels", list_channels);
    ("list-channels-moderated-by-app-instance-user",
      list_channels_moderated_by_app_instance_user);
    ("list-media-capture-pipelines", list_media_capture_pipelines);
    ("list-meeting-tags", list_meeting_tags);
    ("list-meetings", list_meetings);
    ("list-phone-number-orders", list_phone_number_orders);
    ("list-phone-numbers", list_phone_numbers);
    ("list-proxy-sessions", list_proxy_sessions);
    ("list-room-memberships", list_room_memberships);
    ("list-rooms", list_rooms);
    ("list-sip-media-applications", list_sip_media_applications);
    ("list-sip-rules", list_sip_rules);
    ("list-supported-phone-number-countries",
      list_supported_phone_number_countries);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-users", list_users);
    ("list-voice-connector-groups", list_voice_connector_groups);
    ("list-voice-connector-termination-credentials",
      list_voice_connector_termination_credentials);
    ("list-voice-connectors", list_voice_connectors);
    ("logout-user", logout_user);
    ("put-app-instance-retention-settings",
      put_app_instance_retention_settings);
    ("put-app-instance-streaming-configurations",
      put_app_instance_streaming_configurations);
    ("put-events-configuration", put_events_configuration);
    ("put-retention-settings", put_retention_settings);
    ("put-sip-media-application-logging-configuration",
      put_sip_media_application_logging_configuration);
    ("put-voice-connector-emergency-calling-configuration",
      put_voice_connector_emergency_calling_configuration);
    ("put-voice-connector-logging-configuration",
      put_voice_connector_logging_configuration);
    ("put-voice-connector-origination", put_voice_connector_origination);
    ("put-voice-connector-proxy", put_voice_connector_proxy);
    ("put-voice-connector-streaming-configuration",
      put_voice_connector_streaming_configuration);
    ("put-voice-connector-termination", put_voice_connector_termination);
    ("put-voice-connector-termination-credentials",
      put_voice_connector_termination_credentials);
    ("redact-channel-message", redact_channel_message);
    ("redact-conversation-message", redact_conversation_message);
    ("redact-room-message", redact_room_message);
    ("regenerate-security-token", regenerate_security_token);
    ("reset-personal-p-i-n", reset_personal_p_i_n);
    ("restore-phone-number", restore_phone_number);
    ("search-available-phone-numbers", search_available_phone_numbers);
    ("send-channel-message", send_channel_message);
    ("start-meeting-transcription", start_meeting_transcription);
    ("stop-meeting-transcription", stop_meeting_transcription);
    ("tag-attendee", tag_attendee);
    ("tag-meeting", tag_meeting);
    ("tag-resource", tag_resource);
    ("untag-attendee", untag_attendee);
    ("untag-meeting", untag_meeting);
    ("untag-resource", untag_resource);
    ("update-account", update_account);
    ("update-account-settings", update_account_settings);
    ("update-app-instance", update_app_instance);
    ("update-app-instance-user", update_app_instance_user);
    ("update-bot", update_bot);
    ("update-channel", update_channel);
    ("update-channel-message", update_channel_message);
    ("update-channel-read-marker", update_channel_read_marker);
    ("update-global-settings", update_global_settings);
    ("update-phone-number", update_phone_number);
    ("update-phone-number-settings", update_phone_number_settings);
    ("update-proxy-session", update_proxy_session);
    ("update-room", update_room);
    ("update-room-membership", update_room_membership);
    ("update-sip-media-application", update_sip_media_application);
    ("update-sip-media-application-call", update_sip_media_application_call);
    ("update-sip-rule", update_sip_rule);
    ("update-user", update_user);
    ("update-user-settings", update_user_settings);
    ("update-voice-connector", update_voice_connector);
    ("update-voice-connector-group", update_voice_connector_group)]
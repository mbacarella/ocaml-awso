(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_key_deletion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_key_deletion
           (Values.CancelKeyDeletionRequest.make ~keyId ())
           (Some Values.CancelKeyDeletionResponse.to_json)
           (Some Values.CancelKeyDeletionResponse.error_to_json)])
let connect_custom_key_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customKeyStoreId =
         flag "custom-key-store-id" (required string)
           ~doc:"STRING CustomKeyStoreIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.connect_custom_key_store
           (Values.ConnectCustomKeyStoreRequest.make ~customKeyStoreId ())
           (Some Values.ConnectCustomKeyStoreResponse.to_json)
           (Some Values.ConnectCustomKeyStoreResponse.error_to_json)])
let create_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasNameType"
       and targetKeyId =
         flag "target-key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_alias
           (Values.CreateAliasRequest.make ~aliasName ~targetKeyId ()) None
           None])
let create_custom_key_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customKeyStoreName =
         flag "custom-key-store-name" (required string)
           ~doc:"STRING CustomKeyStoreNameType"
       and cloudHsmClusterId =
         flag "cloud-hsm-cluster-id" (required string)
           ~doc:"STRING CloudHsmClusterIdType"
       and trustAnchorCertificate =
         flag "trust-anchor-certificate" (required string)
           ~doc:"STRING TrustAnchorCertificateType"
       and keyStorePassword =
         flag "key-store-password" (required string)
           ~doc:"STRING KeyStorePasswordType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_key_store
           (Values.CreateCustomKeyStoreRequest.make ~customKeyStoreName
              ~cloudHsmClusterId ~trustAnchorCertificate ~keyStorePassword ())
           (Some Values.CreateCustomKeyStoreResponse.to_json)
           (Some Values.CreateCustomKeyStoreResponse.error_to_json)])
let create_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retiringPrincipal =
         flag "retiring-principal" (optional string)
           ~doc:"STRING PrincipalIdType"
       and constraints =
         flag "constraints" (optional json_arg) ~doc:"JSON GrantConstraints"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and name = flag "name" (optional string) ~doc:"STRING GrantNameType"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and granteePrincipal =
         flag "grantee-principal" (required string)
           ~doc:"STRING PrincipalIdType"
       and operations =
         flag "operations" (required json_arg) ~doc:"JSON GrantOperationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_grant
           (Values.CreateGrantRequest.make ?retiringPrincipal
              ?constraints:(Option.map ~f:Values.GrantConstraints.of_json
                              constraints)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ?name ~keyId ~granteePrincipal
              ~operations:(Values.GrantOperationList.of_json operations) ())
           (Some Values.CreateGrantResponse.to_json)
           (Some Values.CreateGrantResponse.error_to_json)])
let create_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy = flag "policy" (optional string) ~doc:"STRING PolicyType"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionType"
       and keyUsage =
         flag "key-usage" (optional json_arg) ~doc:"JSON KeyUsageType"
       and customerMasterKeySpec =
         flag "customer-master-key-spec" (optional json_arg)
           ~doc:"JSON CustomerMasterKeySpec"
       and keySpec = flag "key-spec" (optional json_arg) ~doc:"JSON KeySpec"
       and origin = flag "origin" (optional json_arg) ~doc:"JSON OriginType"
       and customKeyStoreId =
         flag "custom-key-store-id" (optional string)
           ~doc:"STRING CustomKeyStoreIdType"
       and bypassPolicyLockoutSafetyCheck =
         flag "bypass-policy-lockout-safety-check" (optional bool)
           ~doc:"BOOL BooleanType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and multiRegion =
         flag "multi-region" (optional bool) ~doc:"BOOL NullableBooleanType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_key
           (Values.CreateKeyRequest.make ?policy ?description
              ?keyUsage:(Option.map ~f:Values.KeyUsageType.of_json keyUsage)
              ?customerMasterKeySpec:(Option.map
                                        ~f:Values.CustomerMasterKeySpec.of_json
                                        customerMasterKeySpec)
              ?keySpec:(Option.map ~f:Values.KeySpec.of_json keySpec)
              ?origin:(Option.map ~f:Values.OriginType.of_json origin)
              ?customKeyStoreId ?bypassPolicyLockoutSafetyCheck
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?multiRegion
              ()) (Some Values.CreateKeyResponse.to_json)
           (Some Values.CreateKeyResponse.error_to_json)])
let decrypt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionContext =
         flag "encryption-context" (optional json_arg)
           ~doc:"JSON EncryptionContextType"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (optional string) ~doc:"STRING KeyIdType"
       and encryptionAlgorithm =
         flag "encryption-algorithm" (optional json_arg)
           ~doc:"JSON EncryptionAlgorithmSpec"
       and ciphertextBlob =
         flag "ciphertext-blob" (required json_arg)
           ~doc:"JSON CiphertextType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.decrypt
           (Values.DecryptRequest.make
              ?encryptionContext:(Option.map
                                    ~f:Values.EncryptionContextType.of_json
                                    encryptionContext)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ?keyId
              ?encryptionAlgorithm:(Option.map
                                      ~f:Values.EncryptionAlgorithmSpec.of_json
                                      encryptionAlgorithm)
              ~ciphertextBlob:(Values.CiphertextType.of_json ciphertextBlob)
              ()) (Some Values.DecryptResponse.to_json)
           (Some Values.DecryptResponse.error_to_json)])
let delete_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alias (Values.DeleteAliasRequest.make ~aliasName ())
           None None])
let delete_custom_key_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customKeyStoreId =
         flag "custom-key-store-id" (required string)
           ~doc:"STRING CustomKeyStoreIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_key_store
           (Values.DeleteCustomKeyStoreRequest.make ~customKeyStoreId ())
           (Some Values.DeleteCustomKeyStoreResponse.to_json)
           (Some Values.DeleteCustomKeyStoreResponse.error_to_json)])
let delete_imported_key_material =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_imported_key_material
           (Values.DeleteImportedKeyMaterialRequest.make ~keyId ()) None None])
let describe_custom_key_stores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customKeyStoreId =
         flag "custom-key-store-id" (optional string)
           ~doc:"STRING CustomKeyStoreIdType"
       and customKeyStoreName =
         flag "custom-key-store-name" (optional string)
           ~doc:"STRING CustomKeyStoreNameType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_key_stores
           (Values.DescribeCustomKeyStoresRequest.make ?customKeyStoreId
              ?customKeyStoreName ?limit ?marker ())
           (Some Values.DescribeCustomKeyStoresResponse.to_json)
           (Some Values.DescribeCustomKeyStoresResponse.error_to_json)])
let describe_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_key
           (Values.DescribeKeyRequest.make
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ~keyId ())
           (Some Values.DescribeKeyResponse.to_json)
           (Some Values.DescribeKeyResponse.error_to_json)])
let disable_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_key (Values.DisableKeyRequest.make ~keyId ()) None None])
let disable_key_rotation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_key_rotation
           (Values.DisableKeyRotationRequest.make ~keyId ()) None None])
let disconnect_custom_key_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customKeyStoreId =
         flag "custom-key-store-id" (required string)
           ~doc:"STRING CustomKeyStoreIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disconnect_custom_key_store
           (Values.DisconnectCustomKeyStoreRequest.make ~customKeyStoreId ())
           (Some Values.DisconnectCustomKeyStoreResponse.to_json)
           (Some Values.DisconnectCustomKeyStoreResponse.error_to_json)])
let enable_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_key (Values.EnableKeyRequest.make ~keyId ()) None None])
let enable_key_rotation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_key_rotation
           (Values.EnableKeyRotationRequest.make ~keyId ()) None None])
let encrypt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionContext =
         flag "encryption-context" (optional json_arg)
           ~doc:"JSON EncryptionContextType"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and encryptionAlgorithm =
         flag "encryption-algorithm" (optional json_arg)
           ~doc:"JSON EncryptionAlgorithmSpec"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and plaintext =
         flag "plaintext" (required json_arg) ~doc:"JSON PlaintextType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.encrypt
           (Values.EncryptRequest.make
              ?encryptionContext:(Option.map
                                    ~f:Values.EncryptionContextType.of_json
                                    encryptionContext)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens)
              ?encryptionAlgorithm:(Option.map
                                      ~f:Values.EncryptionAlgorithmSpec.of_json
                                      encryptionAlgorithm) ~keyId
              ~plaintext:(Values.PlaintextType.of_json plaintext) ())
           (Some Values.EncryptResponse.to_json)
           (Some Values.EncryptResponse.error_to_json)])
let generate_data_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionContext =
         flag "encryption-context" (optional json_arg)
           ~doc:"JSON EncryptionContextType"
       and numberOfBytes =
         flag "number-of-bytes" (optional int) ~doc:"INT NumberOfBytesType"
       and keySpec =
         flag "key-spec" (optional json_arg) ~doc:"JSON DataKeySpec"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_data_key
           (Values.GenerateDataKeyRequest.make
              ?encryptionContext:(Option.map
                                    ~f:Values.EncryptionContextType.of_json
                                    encryptionContext) ?numberOfBytes
              ?keySpec:(Option.map ~f:Values.DataKeySpec.of_json keySpec)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ~keyId ())
           (Some Values.GenerateDataKeyResponse.to_json)
           (Some Values.GenerateDataKeyResponse.error_to_json)])
let generate_data_key_pair =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionContext =
         flag "encryption-context" (optional json_arg)
           ~doc:"JSON EncryptionContextType"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and keyPairSpec =
         flag "key-pair-spec" (required json_arg) ~doc:"JSON DataKeyPairSpec" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_data_key_pair
           (Values.GenerateDataKeyPairRequest.make
              ?encryptionContext:(Option.map
                                    ~f:Values.EncryptionContextType.of_json
                                    encryptionContext)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ~keyId
              ~keyPairSpec:(Values.DataKeyPairSpec.of_json keyPairSpec) ())
           (Some Values.GenerateDataKeyPairResponse.to_json)
           (Some Values.GenerateDataKeyPairResponse.error_to_json)])
let generate_data_key_pair_without_plaintext =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionContext =
         flag "encryption-context" (optional json_arg)
           ~doc:"JSON EncryptionContextType"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and keyPairSpec =
         flag "key-pair-spec" (required json_arg) ~doc:"JSON DataKeyPairSpec" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_data_key_pair_without_plaintext
           (Values.GenerateDataKeyPairWithoutPlaintextRequest.make
              ?encryptionContext:(Option.map
                                    ~f:Values.EncryptionContextType.of_json
                                    encryptionContext)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ~keyId
              ~keyPairSpec:(Values.DataKeyPairSpec.of_json keyPairSpec) ())
           (Some Values.GenerateDataKeyPairWithoutPlaintextResponse.to_json)
           (Some
              Values.GenerateDataKeyPairWithoutPlaintextResponse.error_to_json)])
let generate_data_key_without_plaintext =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionContext =
         flag "encryption-context" (optional json_arg)
           ~doc:"JSON EncryptionContextType"
       and keySpec =
         flag "key-spec" (optional json_arg) ~doc:"JSON DataKeySpec"
       and numberOfBytes =
         flag "number-of-bytes" (optional int) ~doc:"INT NumberOfBytesType"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_data_key_without_plaintext
           (Values.GenerateDataKeyWithoutPlaintextRequest.make
              ?encryptionContext:(Option.map
                                    ~f:Values.EncryptionContextType.of_json
                                    encryptionContext)
              ?keySpec:(Option.map ~f:Values.DataKeySpec.of_json keySpec)
              ?numberOfBytes
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ~keyId ())
           (Some Values.GenerateDataKeyWithoutPlaintextResponse.to_json)
           (Some Values.GenerateDataKeyWithoutPlaintextResponse.error_to_json)])
let generate_random =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and numberOfBytes =
         flag "number-of-bytes" (optional int) ~doc:"INT NumberOfBytesType"
       and customKeyStoreId =
         flag "custom-key-store-id" (optional string)
           ~doc:"STRING CustomKeyStoreIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_random
           (Values.GenerateRandomRequest.make ?numberOfBytes
              ?customKeyStoreId ())
           (Some Values.GenerateRandomResponse.to_json)
           (Some Values.GenerateRandomResponse.error_to_json)])
let get_key_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_key_policy
           (Values.GetKeyPolicyRequest.make ~keyId ~policyName ())
           (Some Values.GetKeyPolicyResponse.to_json)
           (Some Values.GetKeyPolicyResponse.error_to_json)])
let get_key_rotation_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_key_rotation_status
           (Values.GetKeyRotationStatusRequest.make ~keyId ())
           (Some Values.GetKeyRotationStatusResponse.to_json)
           (Some Values.GetKeyRotationStatusResponse.error_to_json)])
let get_parameters_for_import =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and wrappingAlgorithm =
         flag "wrapping-algorithm" (required json_arg)
           ~doc:"JSON AlgorithmSpec"
       and wrappingKeySpec =
         flag "wrapping-key-spec" (required json_arg)
           ~doc:"JSON WrappingKeySpec" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_parameters_for_import
           (Values.GetParametersForImportRequest.make ~keyId
              ~wrappingAlgorithm:(Values.AlgorithmSpec.of_json
                                    wrappingAlgorithm)
              ~wrappingKeySpec:(Values.WrappingKeySpec.of_json
                                  wrappingKeySpec) ())
           (Some Values.GetParametersForImportResponse.to_json)
           (Some Values.GetParametersForImportResponse.error_to_json)])
let get_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_public_key
           (Values.GetPublicKeyRequest.make
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ~keyId ())
           (Some Values.GetPublicKeyResponse.to_json)
           (Some Values.GetPublicKeyResponse.error_to_json)])
let import_key_material =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and validTo = flag "valid-to" (optional json_arg) ~doc:"JSON DateType"
       and expirationModel =
         flag "expiration-model" (optional json_arg)
           ~doc:"JSON ExpirationModelType"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and importToken =
         flag "import-token" (required json_arg) ~doc:"JSON CiphertextType"
       and encryptedKeyMaterial =
         flag "encrypted-key-material" (required json_arg)
           ~doc:"JSON CiphertextType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_key_material
           (Values.ImportKeyMaterialRequest.make
              ?validTo:(Option.map ~f:Values.DateType.of_json validTo)
              ?expirationModel:(Option.map
                                  ~f:Values.ExpirationModelType.of_json
                                  expirationModel) ~keyId
              ~importToken:(Values.CiphertextType.of_json importToken)
              ~encryptedKeyMaterial:(Values.CiphertextType.of_json
                                       encryptedKeyMaterial) ())
           (Some Values.ImportKeyMaterialResponse.to_json)
           (Some Values.ImportKeyMaterialResponse.error_to_json)])
let list_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (optional string) ~doc:"STRING KeyIdType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_aliases
           (Values.ListAliasesRequest.make ?keyId ?limit ?marker ())
           (Some Values.ListAliasesResponse.to_json)
           (Some Values.ListAliasesResponse.error_to_json)])
let list_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType"
       and grantId =
         flag "grant-id" (optional string) ~doc:"STRING GrantIdType"
       and granteePrincipal =
         flag "grantee-principal" (optional string)
           ~doc:"STRING PrincipalIdType"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_grants
           (Values.ListGrantsRequest.make ?limit ?marker ?grantId
              ?granteePrincipal ~keyId ())
           (Some Values.ListGrantsResponse.to_json)
           (Some Values.ListGrantsResponse.error_to_json)])
let list_key_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_key_policies
           (Values.ListKeyPoliciesRequest.make ?limit ?marker ~keyId ())
           (Some Values.ListKeyPoliciesResponse.to_json)
           (Some Values.ListKeyPoliciesResponse.error_to_json)])
let list_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_keys (Values.ListKeysRequest.make ?limit ?marker ())
           (Some Values.ListKeysResponse.to_json)
           (Some Values.ListKeysResponse.error_to_json)])
let list_resource_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_tags
           (Values.ListResourceTagsRequest.make ?limit ?marker ~keyId ())
           (Some Values.ListResourceTagsResponse.to_json)
           (Some Values.ListResourceTagsResponse.error_to_json)])
let list_retirable_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType"
       and retiringPrincipal =
         flag "retiring-principal" (required string)
           ~doc:"STRING PrincipalIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_retirable_grants
           (Values.ListRetirableGrantsRequest.make ?limit ?marker
              ~retiringPrincipal ()) (Some Values.ListGrantsResponse.to_json)
           (Some Values.ListGrantsResponse.error_to_json)])
let put_key_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bypassPolicyLockoutSafetyCheck =
         flag "bypass-policy-lockout-safety-check" (optional bool)
           ~doc:"BOOL BooleanType"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyNameType"
       and policy = flag "policy" (required string) ~doc:"STRING PolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_key_policy
           (Values.PutKeyPolicyRequest.make ?bypassPolicyLockoutSafetyCheck
              ~keyId ~policyName ~policy ()) None None])
let re_encrypt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceEncryptionContext =
         flag "source-encryption-context" (optional json_arg)
           ~doc:"JSON EncryptionContextType"
       and sourceKeyId =
         flag "source-key-id" (optional string) ~doc:"STRING KeyIdType"
       and destinationEncryptionContext =
         flag "destination-encryption-context" (optional json_arg)
           ~doc:"JSON EncryptionContextType"
       and sourceEncryptionAlgorithm =
         flag "source-encryption-algorithm" (optional json_arg)
           ~doc:"JSON EncryptionAlgorithmSpec"
       and destinationEncryptionAlgorithm =
         flag "destination-encryption-algorithm" (optional json_arg)
           ~doc:"JSON EncryptionAlgorithmSpec"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and ciphertextBlob =
         flag "ciphertext-blob" (required json_arg)
           ~doc:"JSON CiphertextType"
       and destinationKeyId =
         flag "destination-key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.re_encrypt
           (Values.ReEncryptRequest.make
              ?sourceEncryptionContext:(Option.map
                                          ~f:Values.EncryptionContextType.of_json
                                          sourceEncryptionContext)
              ?sourceKeyId
              ?destinationEncryptionContext:(Option.map
                                               ~f:Values.EncryptionContextType.of_json
                                               destinationEncryptionContext)
              ?sourceEncryptionAlgorithm:(Option.map
                                            ~f:Values.EncryptionAlgorithmSpec.of_json
                                            sourceEncryptionAlgorithm)
              ?destinationEncryptionAlgorithm:(Option.map
                                                 ~f:Values.EncryptionAlgorithmSpec.of_json
                                                 destinationEncryptionAlgorithm)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens)
              ~ciphertextBlob:(Values.CiphertextType.of_json ciphertextBlob)
              ~destinationKeyId ()) (Some Values.ReEncryptResponse.to_json)
           (Some Values.ReEncryptResponse.error_to_json)])
let replicate_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy = flag "policy" (optional string) ~doc:"STRING PolicyType"
       and bypassPolicyLockoutSafetyCheck =
         flag "bypass-policy-lockout-safety-check" (optional bool)
           ~doc:"BOOL BooleanType"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and replicaRegion =
         flag "replica-region" (required string) ~doc:"STRING RegionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.replicate_key
           (Values.ReplicateKeyRequest.make ?policy
              ?bypassPolicyLockoutSafetyCheck ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~keyId
              ~replicaRegion ()) (Some Values.ReplicateKeyResponse.to_json)
           (Some Values.ReplicateKeyResponse.error_to_json)])
let retire_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantToken =
         flag "grant-token" (optional string) ~doc:"STRING GrantTokenType"
       and keyId = flag "key-id" (optional string) ~doc:"STRING KeyIdType"
       and grantId =
         flag "grant-id" (optional string) ~doc:"STRING GrantIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retire_grant
           (Values.RetireGrantRequest.make ?grantToken ?keyId ?grantId ())
           None None])
let revoke_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and grantId =
         flag "grant-id" (required string) ~doc:"STRING GrantIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_grant
           (Values.RevokeGrantRequest.make ~keyId ~grantId ()) None None])
let schedule_key_deletion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pendingWindowInDays =
         flag "pending-window-in-days" (optional int)
           ~doc:"INT PendingWindowInDaysType"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.schedule_key_deletion
           (Values.ScheduleKeyDeletionRequest.make ?pendingWindowInDays
              ~keyId ()) (Some Values.ScheduleKeyDeletionResponse.to_json)
           (Some Values.ScheduleKeyDeletionResponse.error_to_json)])
let sign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messageType =
         flag "message-type" (optional json_arg) ~doc:"JSON MessageType"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and message =
         flag "message" (required json_arg) ~doc:"JSON PlaintextType"
       and signingAlgorithm =
         flag "signing-algorithm" (required json_arg)
           ~doc:"JSON SigningAlgorithmSpec" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.sign
           (Values.SignRequest.make
              ?messageType:(Option.map ~f:Values.MessageType.of_json
                              messageType)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ~keyId
              ~message:(Values.PlaintextType.of_json message)
              ~signingAlgorithm:(Values.SigningAlgorithmSpec.of_json
                                   signingAlgorithm) ())
           (Some Values.SignResponse.to_json)
           (Some Values.SignResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~keyId
              ~tags:(Values.TagList.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~keyId
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (required string) ~doc:"STRING AliasNameType"
       and targetKeyId =
         flag "target-key-id" (required string) ~doc:"STRING KeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_alias
           (Values.UpdateAliasRequest.make ~aliasName ~targetKeyId ()) None
           None])
let update_custom_key_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newCustomKeyStoreName =
         flag "new-custom-key-store-name" (optional string)
           ~doc:"STRING CustomKeyStoreNameType"
       and keyStorePassword =
         flag "key-store-password" (optional string)
           ~doc:"STRING KeyStorePasswordType"
       and cloudHsmClusterId =
         flag "cloud-hsm-cluster-id" (optional string)
           ~doc:"STRING CloudHsmClusterIdType"
       and customKeyStoreId =
         flag "custom-key-store-id" (required string)
           ~doc:"STRING CustomKeyStoreIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_key_store
           (Values.UpdateCustomKeyStoreRequest.make ?newCustomKeyStoreName
              ?keyStorePassword ?cloudHsmClusterId ~customKeyStoreId ())
           (Some Values.UpdateCustomKeyStoreResponse.to_json)
           (Some Values.UpdateCustomKeyStoreResponse.error_to_json)])
let update_key_description =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and description =
         flag "description" (required string) ~doc:"STRING DescriptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_key_description
           (Values.UpdateKeyDescriptionRequest.make ~keyId ~description ())
           None None])
let update_primary_region =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and primaryRegion =
         flag "primary-region" (required string) ~doc:"STRING RegionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_primary_region
           (Values.UpdatePrimaryRegionRequest.make ~keyId ~primaryRegion ())
           None None])
let verify =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messageType =
         flag "message-type" (optional json_arg) ~doc:"JSON MessageType"
       and grantTokens =
         flag "grant-tokens" (optional json_arg) ~doc:"JSON GrantTokenList"
       and keyId = flag "key-id" (required string) ~doc:"STRING KeyIdType"
       and message =
         flag "message" (required json_arg) ~doc:"JSON PlaintextType"
       and signature =
         flag "signature" (required json_arg) ~doc:"JSON CiphertextType"
       and signingAlgorithm =
         flag "signing-algorithm" (required json_arg)
           ~doc:"JSON SigningAlgorithmSpec" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.verify
           (Values.VerifyRequest.make
              ?messageType:(Option.map ~f:Values.MessageType.of_json
                              messageType)
              ?grantTokens:(Option.map ~f:Values.GrantTokenList.of_json
                              grantTokens) ~keyId
              ~message:(Values.PlaintextType.of_json message)
              ~signature:(Values.CiphertextType.of_json signature)
              ~signingAlgorithm:(Values.SigningAlgorithmSpec.of_json
                                   signingAlgorithm) ())
           (Some Values.VerifyResponse.to_json)
           (Some Values.VerifyResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-key-deletion", cancel_key_deletion);
    ("connect-custom-key-store", connect_custom_key_store);
    ("create-alias", create_alias);
    ("create-custom-key-store", create_custom_key_store);
    ("create-grant", create_grant);
    ("create-key", create_key);
    ("decrypt", decrypt);
    ("delete-alias", delete_alias);
    ("delete-custom-key-store", delete_custom_key_store);
    ("delete-imported-key-material", delete_imported_key_material);
    ("describe-custom-key-stores", describe_custom_key_stores);
    ("describe-key", describe_key);
    ("disable-key", disable_key);
    ("disable-key-rotation", disable_key_rotation);
    ("disconnect-custom-key-store", disconnect_custom_key_store);
    ("enable-key", enable_key);
    ("enable-key-rotation", enable_key_rotation);
    ("encrypt", encrypt);
    ("generate-data-key", generate_data_key);
    ("generate-data-key-pair", generate_data_key_pair);
    ("generate-data-key-pair-without-plaintext",
      generate_data_key_pair_without_plaintext);
    ("generate-data-key-without-plaintext",
      generate_data_key_without_plaintext);
    ("generate-random", generate_random);
    ("get-key-policy", get_key_policy);
    ("get-key-rotation-status", get_key_rotation_status);
    ("get-parameters-for-import", get_parameters_for_import);
    ("get-public-key", get_public_key);
    ("import-key-material", import_key_material);
    ("list-aliases", list_aliases);
    ("list-grants", list_grants);
    ("list-key-policies", list_key_policies);
    ("list-keys", list_keys);
    ("list-resource-tags", list_resource_tags);
    ("list-retirable-grants", list_retirable_grants);
    ("put-key-policy", put_key_policy);
    ("re-encrypt", re_encrypt);
    ("replicate-key", replicate_key);
    ("retire-grant", retire_grant);
    ("revoke-grant", revoke_grant);
    ("schedule-key-deletion", schedule_key_deletion);
    ("sign", sign);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-alias", update_alias);
    ("update-custom-key-store", update_custom_key_store);
    ("update-key-description", update_key_description);
    ("update-primary-region", update_primary_region);
    ("verify", verify)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aPIVersion =
         flag "a-p-i-version" (optional string) ~doc:"STRING APIVersion"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_job (Values.CancelJobInput.make ?aPIVersion ~jobId ())
           (Some Values.CancelJobOutput.to_json)
           (Some Values.CancelJobOutput.error_to_json)])
let create_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and manifestAddendum =
         flag "manifest-addendum" (optional string)
           ~doc:"STRING ManifestAddendum"
       and aPIVersion =
         flag "a-p-i-version" (optional string) ~doc:"STRING APIVersion"
       and jobType = flag "job-type" (required json_arg) ~doc:"JSON JobType"
       and manifest =
         flag "manifest" (required string) ~doc:"STRING Manifest"
       and validateOnly =
         flag "validate-only" (required bool) ~doc:"BOOL ValidateOnly" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job
           (Values.CreateJobInput.make ?manifestAddendum ?aPIVersion
              ~jobType:(Values.JobType.of_json jobType) ~manifest
              ~validateOnly ()) (Some Values.CreateJobOutput.to_json)
           (Some Values.CreateJobOutput.error_to_json)])
let get_shipping_label =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING name"
       and company = flag "company" (optional string) ~doc:"STRING company"
       and phoneNumber =
         flag "phone-number" (optional string) ~doc:"STRING phoneNumber"
       and country = flag "country" (optional string) ~doc:"STRING country"
       and stateOrProvince =
         flag "state-or-province" (optional string)
           ~doc:"STRING stateOrProvince"
       and city = flag "city" (optional string) ~doc:"STRING city"
       and postalCode =
         flag "postal-code" (optional string) ~doc:"STRING postalCode"
       and street1 = flag "street1" (optional string) ~doc:"STRING street1"
       and street2 = flag "street2" (optional string) ~doc:"STRING street2"
       and street3 = flag "street3" (optional string) ~doc:"STRING street3"
       and aPIVersion =
         flag "a-p-i-version" (optional string) ~doc:"STRING APIVersion"
       and jobIds = flag "job-ids" (required json_arg) ~doc:"JSON JobIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_shipping_label
           (Values.GetShippingLabelInput.make ?name ?company ?phoneNumber
              ?country ?stateOrProvince ?city ?postalCode ?street1 ?street2
              ?street3 ?aPIVersion ~jobIds:(Values.JobIdList.of_json jobIds)
              ()) (Some Values.GetShippingLabelOutput.to_json)
           (Some Values.GetShippingLabelOutput.error_to_json)])
let get_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aPIVersion =
         flag "a-p-i-version" (optional string) ~doc:"STRING APIVersion"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_status (Values.GetStatusInput.make ?aPIVersion ~jobId ())
           (Some Values.GetStatusOutput.to_json)
           (Some Values.GetStatusOutput.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxJobs = flag "max-jobs" (optional int) ~doc:"INT MaxJobs"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and aPIVersion =
         flag "a-p-i-version" (optional string) ~doc:"STRING APIVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsInput.make ?maxJobs ?marker ?aPIVersion ())
           (Some Values.ListJobsOutput.to_json)
           (Some Values.ListJobsOutput.error_to_json)])
let update_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aPIVersion =
         flag "a-p-i-version" (optional string) ~doc:"STRING APIVersion"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and manifest =
         flag "manifest" (required string) ~doc:"STRING Manifest"
       and jobType = flag "job-type" (required json_arg) ~doc:"JSON JobType"
       and validateOnly =
         flag "validate-only" (required bool) ~doc:"BOOL ValidateOnly" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job
           (Values.UpdateJobInput.make ?aPIVersion ~jobId ~manifest
              ~jobType:(Values.JobType.of_json jobType) ~validateOnly ())
           (Some Values.UpdateJobOutput.to_json)
           (Some Values.UpdateJobOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("cancel-job", cancel_job);
    ("create-job", create_job);
    ("get-shipping-label", get_shipping_label);
    ("get-status", get_status);
    ("list-jobs", list_jobs);
    ("update-job", update_job)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let describe_entities_detection_v2_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entities_detection_v2_job
           (Values.DescribeEntitiesDetectionV2JobRequest.make ~jobId ())
           (Some Values.DescribeEntitiesDetectionV2JobResponse.to_json)
           (Some Values.DescribeEntitiesDetectionV2JobResponse.error_to_json)])
let describe_i_c_d10_c_m_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_i_c_d10_c_m_inference_job
           (Values.DescribeICD10CMInferenceJobRequest.make ~jobId ())
           (Some Values.DescribeICD10CMInferenceJobResponse.to_json)
           (Some Values.DescribeICD10CMInferenceJobResponse.error_to_json)])
let describe_p_h_i_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_p_h_i_detection_job
           (Values.DescribePHIDetectionJobRequest.make ~jobId ())
           (Some Values.DescribePHIDetectionJobResponse.to_json)
           (Some Values.DescribePHIDetectionJobResponse.error_to_json)])
let describe_rx_norm_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_rx_norm_inference_job
           (Values.DescribeRxNormInferenceJobRequest.make ~jobId ())
           (Some Values.DescribeRxNormInferenceJobResponse.to_json)
           (Some Values.DescribeRxNormInferenceJobResponse.error_to_json)])
let describe_s_n_o_m_e_d_c_t_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_s_n_o_m_e_d_c_t_inference_job
           (Values.DescribeSNOMEDCTInferenceJobRequest.make ~jobId ())
           (Some Values.DescribeSNOMEDCTInferenceJobResponse.to_json)
           (Some Values.DescribeSNOMEDCTInferenceJobResponse.error_to_json)])
let detect_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string) ~doc:"STRING BoundedLengthString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_entities (Values.DetectEntitiesRequest.make ~text ())
           (Some Values.DetectEntitiesResponse.to_json)
           (Some Values.DetectEntitiesResponse.error_to_json)])
let detect_entities_v2 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string) ~doc:"STRING BoundedLengthString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_entities_v2
           (Values.DetectEntitiesV2Request.make ~text ())
           (Some Values.DetectEntitiesV2Response.to_json)
           (Some Values.DetectEntitiesV2Response.error_to_json)])
let detect_p_h_i =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string) ~doc:"STRING BoundedLengthString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_p_h_i (Values.DetectPHIRequest.make ~text ())
           (Some Values.DetectPHIResponse.to_json)
           (Some Values.DetectPHIResponse.error_to_json)])
let infer_i_c_d10_c_m =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string)
           ~doc:"STRING OntologyLinkingBoundedLengthString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.infer_i_c_d10_c_m (Values.InferICD10CMRequest.make ~text ())
           (Some Values.InferICD10CMResponse.to_json)
           (Some Values.InferICD10CMResponse.error_to_json)])
let infer_rx_norm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string)
           ~doc:"STRING OntologyLinkingBoundedLengthString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.infer_rx_norm (Values.InferRxNormRequest.make ~text ())
           (Some Values.InferRxNormResponse.to_json)
           (Some Values.InferRxNormResponse.error_to_json)])
let infer_s_n_o_m_e_d_c_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string)
           ~doc:"STRING OntologyLinkingBoundedLengthString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.infer_s_n_o_m_e_d_c_t
           (Values.InferSNOMEDCTRequest.make ~text ())
           (Some Values.InferSNOMEDCTResponse.to_json)
           (Some Values.InferSNOMEDCTResponse.error_to_json)])
let list_entities_detection_v2_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ComprehendMedicalAsyncJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entities_detection_v2_jobs
           (Values.ListEntitiesDetectionV2JobsRequest.make
              ?filter:(Option.map
                         ~f:Values.ComprehendMedicalAsyncJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListEntitiesDetectionV2JobsResponse.to_json)
           (Some Values.ListEntitiesDetectionV2JobsResponse.error_to_json)])
let list_i_c_d10_c_m_inference_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ComprehendMedicalAsyncJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_i_c_d10_c_m_inference_jobs
           (Values.ListICD10CMInferenceJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.ComprehendMedicalAsyncJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListICD10CMInferenceJobsResponse.to_json)
           (Some Values.ListICD10CMInferenceJobsResponse.error_to_json)])
let list_p_h_i_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ComprehendMedicalAsyncJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_p_h_i_detection_jobs
           (Values.ListPHIDetectionJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.ComprehendMedicalAsyncJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListPHIDetectionJobsResponse.to_json)
           (Some Values.ListPHIDetectionJobsResponse.error_to_json)])
let list_rx_norm_inference_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ComprehendMedicalAsyncJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rx_norm_inference_jobs
           (Values.ListRxNormInferenceJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.ComprehendMedicalAsyncJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListRxNormInferenceJobsResponse.to_json)
           (Some Values.ListRxNormInferenceJobsResponse.error_to_json)])
let list_s_n_o_m_e_d_c_t_inference_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ComprehendMedicalAsyncJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_s_n_o_m_e_d_c_t_inference_jobs
           (Values.ListSNOMEDCTInferenceJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.ComprehendMedicalAsyncJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListSNOMEDCTInferenceJobsResponse.to_json)
           (Some Values.ListSNOMEDCTInferenceJobsResponse.error_to_json)])
let start_entities_detection_v2_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_entities_detection_v2_job
           (Values.StartEntitiesDetectionV2JobRequest.make ?jobName
              ?clientRequestToken ?kMSKey
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartEntitiesDetectionV2JobResponse.to_json)
           (Some Values.StartEntitiesDetectionV2JobResponse.error_to_json)])
let start_i_c_d10_c_m_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_i_c_d10_c_m_inference_job
           (Values.StartICD10CMInferenceJobRequest.make ?jobName
              ?clientRequestToken ?kMSKey
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartICD10CMInferenceJobResponse.to_json)
           (Some Values.StartICD10CMInferenceJobResponse.error_to_json)])
let start_p_h_i_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_p_h_i_detection_job
           (Values.StartPHIDetectionJobRequest.make ?jobName
              ?clientRequestToken ?kMSKey
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartPHIDetectionJobResponse.to_json)
           (Some Values.StartPHIDetectionJobResponse.error_to_json)])
let start_rx_norm_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_rx_norm_inference_job
           (Values.StartRxNormInferenceJobRequest.make ?jobName
              ?clientRequestToken ?kMSKey
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartRxNormInferenceJobResponse.to_json)
           (Some Values.StartRxNormInferenceJobResponse.error_to_json)])
let start_s_n_o_m_e_d_c_t_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and kMSKey = flag "k-m-s-key" (optional string) ~doc:"STRING KMSKey"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_s_n_o_m_e_d_c_t_inference_job
           (Values.StartSNOMEDCTInferenceJobRequest.make ?jobName
              ?clientRequestToken ?kMSKey
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartSNOMEDCTInferenceJobResponse.to_json)
           (Some Values.StartSNOMEDCTInferenceJobResponse.error_to_json)])
let stop_entities_detection_v2_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_entities_detection_v2_job
           (Values.StopEntitiesDetectionV2JobRequest.make ~jobId ())
           (Some Values.StopEntitiesDetectionV2JobResponse.to_json)
           (Some Values.StopEntitiesDetectionV2JobResponse.error_to_json)])
let stop_i_c_d10_c_m_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_i_c_d10_c_m_inference_job
           (Values.StopICD10CMInferenceJobRequest.make ~jobId ())
           (Some Values.StopICD10CMInferenceJobResponse.to_json)
           (Some Values.StopICD10CMInferenceJobResponse.error_to_json)])
let stop_p_h_i_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_p_h_i_detection_job
           (Values.StopPHIDetectionJobRequest.make ~jobId ())
           (Some Values.StopPHIDetectionJobResponse.to_json)
           (Some Values.StopPHIDetectionJobResponse.error_to_json)])
let stop_rx_norm_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_rx_norm_inference_job
           (Values.StopRxNormInferenceJobRequest.make ~jobId ())
           (Some Values.StopRxNormInferenceJobResponse.to_json)
           (Some Values.StopRxNormInferenceJobResponse.error_to_json)])
let stop_s_n_o_m_e_d_c_t_inference_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_s_n_o_m_e_d_c_t_inference_job
           (Values.StopSNOMEDCTInferenceJobRequest.make ~jobId ())
           (Some Values.StopSNOMEDCTInferenceJobResponse.to_json)
           (Some Values.StopSNOMEDCTInferenceJobResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("describe-entities-detection-v2-job",
       describe_entities_detection_v2_job);
    ("describe-i-c-d10-c-m-inference-job",
      describe_i_c_d10_c_m_inference_job);
    ("describe-p-h-i-detection-job", describe_p_h_i_detection_job);
    ("describe-rx-norm-inference-job", describe_rx_norm_inference_job);
    ("describe-s-n-o-m-e-d-c-t-inference-job",
      describe_s_n_o_m_e_d_c_t_inference_job);
    ("detect-entities", detect_entities);
    ("detect-entities-v2", detect_entities_v2);
    ("detect-p-h-i", detect_p_h_i);
    ("infer-i-c-d10-c-m", infer_i_c_d10_c_m);
    ("infer-rx-norm", infer_rx_norm);
    ("infer-s-n-o-m-e-d-c-t", infer_s_n_o_m_e_d_c_t);
    ("list-entities-detection-v2-jobs", list_entities_detection_v2_jobs);
    ("list-i-c-d10-c-m-inference-jobs", list_i_c_d10_c_m_inference_jobs);
    ("list-p-h-i-detection-jobs", list_p_h_i_detection_jobs);
    ("list-rx-norm-inference-jobs", list_rx_norm_inference_jobs);
    ("list-s-n-o-m-e-d-c-t-inference-jobs",
      list_s_n_o_m_e_d_c_t_inference_jobs);
    ("start-entities-detection-v2-job", start_entities_detection_v2_job);
    ("start-i-c-d10-c-m-inference-job", start_i_c_d10_c_m_inference_job);
    ("start-p-h-i-detection-job", start_p_h_i_detection_job);
    ("start-rx-norm-inference-job", start_rx_norm_inference_job);
    ("start-s-n-o-m-e-d-c-t-inference-job",
      start_s_n_o_m_e_d_c_t_inference_job);
    ("stop-entities-detection-v2-job", stop_entities_detection_v2_job);
    ("stop-i-c-d10-c-m-inference-job", stop_i_c_d10_c_m_inference_job);
    ("stop-p-h-i-detection-job", stop_p_h_i_detection_job);
    ("stop-rx-norm-inference-job", stop_rx_norm_inference_job);
    ("stop-s-n-o-m-e-d-c-t-inference-job",
      stop_s_n_o_m_e_d_c_t_inference_job)]
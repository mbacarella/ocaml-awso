(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let analyze_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and humanLoopConfig =
         flag "human-loop-config" (optional json_arg)
           ~doc:"JSON HumanLoopConfig"
       and document =
         flag "document" (required json_arg) ~doc:"JSON Document"
       and featureTypes =
         flag "feature-types" (required json_arg) ~doc:"JSON FeatureTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.analyze_document
           (Values.AnalyzeDocumentRequest.make
              ?humanLoopConfig:(Option.map ~f:Values.HumanLoopConfig.of_json
                                  humanLoopConfig)
              ~document:(Values.Document.of_json document)
              ~featureTypes:(Values.FeatureTypes.of_json featureTypes) ())
           (Some Values.AnalyzeDocumentResponse.to_json)
           (Some Values.AnalyzeDocumentResponse.error_to_json)])
let analyze_expense =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and document =
         flag "document" (required json_arg) ~doc:"JSON Document" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.analyze_expense
           (Values.AnalyzeExpenseRequest.make
              ~document:(Values.Document.of_json document) ())
           (Some Values.AnalyzeExpenseResponse.to_json)
           (Some Values.AnalyzeExpenseResponse.error_to_json)])
let analyze_i_d =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentPages =
         flag "document-pages" (required json_arg) ~doc:"JSON DocumentPages" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.analyze_i_d
           (Values.AnalyzeIDRequest.make
              ~documentPages:(Values.DocumentPages.of_json documentPages) ())
           (Some Values.AnalyzeIDResponse.to_json)
           (Some Values.AnalyzeIDResponse.error_to_json)])
let detect_document_text =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and document =
         flag "document" (required json_arg) ~doc:"JSON Document" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_document_text
           (Values.DetectDocumentTextRequest.make
              ~document:(Values.Document.of_json document) ())
           (Some Values.DetectDocumentTextResponse.to_json)
           (Some Values.DetectDocumentTextResponse.error_to_json)])
let get_document_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_document_analysis
           (Values.GetDocumentAnalysisRequest.make ?maxResults ?nextToken
              ~jobId ()) (Some Values.GetDocumentAnalysisResponse.to_json)
           (Some Values.GetDocumentAnalysisResponse.error_to_json)])
let get_document_text_detection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_document_text_detection
           (Values.GetDocumentTextDetectionRequest.make ?maxResults
              ?nextToken ~jobId ())
           (Some Values.GetDocumentTextDetectionResponse.to_json)
           (Some Values.GetDocumentTextDetectionResponse.error_to_json)])
let get_expense_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_expense_analysis
           (Values.GetExpenseAnalysisRequest.make ?maxResults ?nextToken
              ~jobId ()) (Some Values.GetExpenseAnalysisResponse.to_json)
           (Some Values.GetExpenseAnalysisResponse.error_to_json)])
let start_document_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and jobTag = flag "job-tag" (optional string) ~doc:"STRING JobTag"
       and notificationChannel =
         flag "notification-channel" (optional json_arg)
           ~doc:"JSON NotificationChannel"
       and outputConfig =
         flag "output-config" (optional json_arg) ~doc:"JSON OutputConfig"
       and kMSKeyId =
         flag "k-m-s-key-id" (optional string) ~doc:"STRING KMSKeyId"
       and documentLocation =
         flag "document-location" (required json_arg)
           ~doc:"JSON DocumentLocation"
       and featureTypes =
         flag "feature-types" (required json_arg) ~doc:"JSON FeatureTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_document_analysis
           (Values.StartDocumentAnalysisRequest.make ?clientRequestToken
              ?jobTag
              ?notificationChannel:(Option.map
                                      ~f:Values.NotificationChannel.of_json
                                      notificationChannel)
              ?outputConfig:(Option.map ~f:Values.OutputConfig.of_json
                               outputConfig) ?kMSKeyId
              ~documentLocation:(Values.DocumentLocation.of_json
                                   documentLocation)
              ~featureTypes:(Values.FeatureTypes.of_json featureTypes) ())
           (Some Values.StartDocumentAnalysisResponse.to_json)
           (Some Values.StartDocumentAnalysisResponse.error_to_json)])
let start_document_text_detection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and jobTag = flag "job-tag" (optional string) ~doc:"STRING JobTag"
       and notificationChannel =
         flag "notification-channel" (optional json_arg)
           ~doc:"JSON NotificationChannel"
       and outputConfig =
         flag "output-config" (optional json_arg) ~doc:"JSON OutputConfig"
       and kMSKeyId =
         flag "k-m-s-key-id" (optional string) ~doc:"STRING KMSKeyId"
       and documentLocation =
         flag "document-location" (required json_arg)
           ~doc:"JSON DocumentLocation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_document_text_detection
           (Values.StartDocumentTextDetectionRequest.make ?clientRequestToken
              ?jobTag
              ?notificationChannel:(Option.map
                                      ~f:Values.NotificationChannel.of_json
                                      notificationChannel)
              ?outputConfig:(Option.map ~f:Values.OutputConfig.of_json
                               outputConfig) ?kMSKeyId
              ~documentLocation:(Values.DocumentLocation.of_json
                                   documentLocation) ())
           (Some Values.StartDocumentTextDetectionResponse.to_json)
           (Some Values.StartDocumentTextDetectionResponse.error_to_json)])
let start_expense_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and jobTag = flag "job-tag" (optional string) ~doc:"STRING JobTag"
       and notificationChannel =
         flag "notification-channel" (optional json_arg)
           ~doc:"JSON NotificationChannel"
       and outputConfig =
         flag "output-config" (optional json_arg) ~doc:"JSON OutputConfig"
       and kMSKeyId =
         flag "k-m-s-key-id" (optional string) ~doc:"STRING KMSKeyId"
       and documentLocation =
         flag "document-location" (required json_arg)
           ~doc:"JSON DocumentLocation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_expense_analysis
           (Values.StartExpenseAnalysisRequest.make ?clientRequestToken
              ?jobTag
              ?notificationChannel:(Option.map
                                      ~f:Values.NotificationChannel.of_json
                                      notificationChannel)
              ?outputConfig:(Option.map ~f:Values.OutputConfig.of_json
                               outputConfig) ?kMSKeyId
              ~documentLocation:(Values.DocumentLocation.of_json
                                   documentLocation) ())
           (Some Values.StartExpenseAnalysisResponse.to_json)
           (Some Values.StartExpenseAnalysisResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("analyze-document", analyze_document);
    ("analyze-expense", analyze_expense);
    ("analyze-i-d", analyze_i_d);
    ("detect-document-text", detect_document_text);
    ("get-document-analysis", get_document_analysis);
    ("get-document-text-detection", get_document_text_detection);
    ("get-expense-analysis", get_expense_analysis);
    ("start-document-analysis", start_document_analysis);
    ("start-document-text-detection", start_document_text_detection);
    ("start-expense-analysis", start_expense_analysis)]
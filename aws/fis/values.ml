(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.fis
let apiVersion = "2020-12-01"
let endpointPrefix = "fis"
let serviceFullName = "AWS Fault Injection Simulator"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "FIS"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ExperimentTemplateTargetFilterValue =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateTargetFilterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateTargetFilterValue" j
    let to_json = simple_to_json to_value
  end
module ExperimentTargetFilterValue =
  struct
    type nonrec t = string
    let context_ = "ExperimentTargetFilterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTargetFilterValue" j
    let to_json = simple_to_json to_value
  end
module ReportConfigurationCloudWatchDashboardIdentifier =
  struct
    type nonrec t = string
    let context_ = "ReportConfigurationCloudWatchDashboardIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ReportConfigurationCloudWatchDashboardIdentifier"
        j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateTargetFilterPath =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateTargetFilterPath"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateTargetFilterPath" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateTargetFilterValues =
  struct
    type nonrec t = ExperimentTemplateTargetFilterValue.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentTemplateTargetFilterValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ExperimentTemplateTargetFilterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTemplateTargetFilterValues"
        ~of_json:ExperimentTemplateTargetFilterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTargetFilterPath =
  struct
    type nonrec t = string
    let context_ = "ExperimentTargetFilterPath"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTargetFilterPath" j
    let to_json = simple_to_json to_value
  end
module ExperimentTargetFilterValues =
  struct
    type nonrec t = ExperimentTargetFilterValue.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentTargetFilterValue.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentTargetFilterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTargetFilterValues"
        ~of_json:ExperimentTargetFilterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTemplateActionParameter =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateActionParameter"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateActionParameter" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateActionParameterName =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateActionParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateActionParameterName" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateActionStartAfter =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateActionStartAfter"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateActionStartAfter" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateActionTargetName =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateActionTargetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateActionTargetName" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateTargetName =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateTargetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTemplateTargetName" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateReportConfigurationCloudWatchDashboard =
  struct
    type nonrec t =
      {
      dashboardIdentifier:
        ReportConfigurationCloudWatchDashboardIdentifier.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the CloudWatch dashboard to include in the experiment report."]}
    let make ?dashboardIdentifier = fun () -> { dashboardIdentifier }
    let to_value x =
      structure_to_value
        [("dashboardIdentifier",
           (Option.map x.dashboardIdentifier
              ~f:ReportConfigurationCloudWatchDashboardIdentifier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dashboardIdentifier =
        (Option.map
           ~f:ReportConfigurationCloudWatchDashboardIdentifier.of_xml)
          (Xml.child xml_arg0 "dashboardIdentifier") in
      make ?dashboardIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dashboardIdentifier =
        field_map json__ "dashboardIdentifier"
          ReportConfigurationCloudWatchDashboardIdentifier.of_json in
      make ?dashboardIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The CloudWatch dashboards to include as data sources in the experiment report."]
module ReportConfigurationS3OutputPrefix =
  struct
    type nonrec t = string
    let context_ = "ReportConfigurationS3OutputPrefix"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ReportConfigurationS3OutputPrefix" j
    let to_json = simple_to_json to_value
  end
module S3BucketName =
  struct
    type nonrec t = string
    let context_ = "S3BucketName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:63) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3BucketName" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateTargetFilter =
  struct
    type nonrec t =
      {
      path: ExperimentTemplateTargetFilterPath.t option
        [@ocaml.doc "The attribute path for the filter."];
      values: ExperimentTemplateTargetFilterValues.t option
        [@ocaml.doc "The attribute values for the filter."]}
    let make ?path = fun ?values -> fun () -> { path; values }
    let to_value x =
      structure_to_value
        [("path",
           (Option.map x.path ~f:ExperimentTemplateTargetFilterPath.to_value));
        ("values",
          (Option.map x.values
             ~f:ExperimentTemplateTargetFilterValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:ExperimentTemplateTargetFilterValues.of_xml)
          (Xml.child xml_arg0 "values") in
      let path =
        (Option.map ~f:ExperimentTemplateTargetFilterPath.of_xml)
          (Xml.child xml_arg0 "path") in
      make ?values ?path ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let values =
        field_map json__ "values"
          ExperimentTemplateTargetFilterValues.of_json in
      let path =
        field_map json__ "path" ExperimentTemplateTargetFilterPath.of_json in
      make ?values ?path ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a filter used for the target resources in an experiment template."]
module ExperimentTemplateTargetParameterName =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateTargetParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateTargetParameterName" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateTargetParameterValue =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateTargetParameterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateTargetParameterValue" j
    let to_json = simple_to_json to_value
  end
module ResourceArn =
  struct
    type nonrec t = string
    let context_ = "ResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceArn" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module ExperimentActionParameter =
  struct
    type nonrec t = string
    let context_ = "ExperimentActionParameter"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentActionParameter" j
    let to_json = simple_to_json to_value
  end
module ExperimentActionParameterName =
  struct
    type nonrec t = string
    let context_ = "ExperimentActionParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentActionParameterName" j
    let to_json = simple_to_json to_value
  end
module ExperimentActionStartAfter =
  struct
    type nonrec t = string
    let context_ = "ExperimentActionStartAfter"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentActionStartAfter" j
    let to_json = simple_to_json to_value
  end
module ExperimentActionStatus =
  struct
    type nonrec t =
      | Pending 
      | Initiating 
      | Running 
      | Completed 
      | Cancelled 
      | Stopping 
      | Stopped 
      | Failed 
      | Skipped 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pending -> "pending"
      | Initiating -> "initiating"
      | Running -> "running"
      | Completed -> "completed"
      | Cancelled -> "cancelled"
      | Stopping -> "stopping"
      | Stopped -> "stopped"
      | Failed -> "failed"
      | Skipped -> "skipped"
      | Non_static_id s -> s
    let of_string =
      function
      | "pending" -> Pending
      | "initiating" -> Initiating
      | "running" -> Running
      | "completed" -> Completed
      | "cancelled" -> Cancelled
      | "stopping" -> Stopping
      | "stopped" -> Stopped
      | "failed" -> Failed
      | "skipped" -> Skipped
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ExperimentActionStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ExperimentActionStatus" j)
    let to_json = simple_to_json to_value
  end
module ExperimentActionStatusReason =
  struct
    type nonrec t = string
    let context_ = "ExperimentActionStatusReason"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentActionStatusReason" j
    let to_json = simple_to_json to_value
  end
module ExperimentActionTargetName =
  struct
    type nonrec t = string
    let context_ = "ExperimentActionTargetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentActionTargetName" j
    let to_json = simple_to_json to_value
  end
module ExperimentTargetName =
  struct
    type nonrec t = string
    let context_ = "ExperimentTargetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTargetName" j
    let to_json = simple_to_json to_value
  end
module ExperimentReportS3ReportArn =
  struct
    type nonrec t = string
    let context_ = "ExperimentReportS3ReportArn"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[\\s\\S]+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentReportS3ReportArn" j
    let to_json = simple_to_json to_value
  end
module ExperimentReportS3ReportType =
  struct
    type nonrec t = string
    let context_ = "ExperimentReportS3ReportType"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[\\s\\S]+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentReportS3ReportType" j
    let to_json = simple_to_json to_value
  end
module ExperimentReportErrorCode =
  struct
    type nonrec t = string
    let context_ = "ExperimentReportErrorCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentReportErrorCode" j
    let to_json = simple_to_json to_value
  end
module ExperimentReportConfigurationCloudWatchDashboard =
  struct
    type nonrec t =
      {
      dashboardIdentifier:
        ReportConfigurationCloudWatchDashboardIdentifier.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the CloudWatch dashboard to include in the experiment report."]}
    let make ?dashboardIdentifier = fun () -> { dashboardIdentifier }
    let to_value x =
      structure_to_value
        [("dashboardIdentifier",
           (Option.map x.dashboardIdentifier
              ~f:ReportConfigurationCloudWatchDashboardIdentifier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dashboardIdentifier =
        (Option.map
           ~f:ReportConfigurationCloudWatchDashboardIdentifier.of_xml)
          (Xml.child xml_arg0 "dashboardIdentifier") in
      make ?dashboardIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dashboardIdentifier =
        field_map json__ "dashboardIdentifier"
          ReportConfigurationCloudWatchDashboardIdentifier.of_json in
      make ?dashboardIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the CloudWatch dashboard to include in the experiment report. The dashboard widgets will be captured as snapshot graphs within the report."]
module ExperimentTargetFilter =
  struct
    type nonrec t =
      {
      path: ExperimentTargetFilterPath.t option
        [@ocaml.doc "The attribute path for the filter."];
      values: ExperimentTargetFilterValues.t option
        [@ocaml.doc "The attribute values for the filter."]}
    let make ?path = fun ?values -> fun () -> { path; values }
    let to_value x =
      structure_to_value
        [("path", (Option.map x.path ~f:ExperimentTargetFilterPath.to_value));
        ("values",
          (Option.map x.values ~f:ExperimentTargetFilterValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:ExperimentTargetFilterValues.of_xml)
          (Xml.child xml_arg0 "values") in
      let path =
        (Option.map ~f:ExperimentTargetFilterPath.of_xml)
          (Xml.child xml_arg0 "path") in
      make ?values ?path ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let values =
        field_map json__ "values" ExperimentTargetFilterValues.of_json in
      let path = field_map json__ "path" ExperimentTargetFilterPath.of_json in
      make ?values ?path ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a filter used for the target resources in an experiment."]
module ExperimentTargetParameterName =
  struct
    type nonrec t = string
    let context_ = "ExperimentTargetParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTargetParameterName" j
    let to_json = simple_to_json to_value
  end
module ExperimentTargetParameterValue =
  struct
    type nonrec t = string
    let context_ = "ExperimentTargetParameterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTargetParameterValue" j
    let to_json = simple_to_json to_value
  end
module ExperimentErrorAccountId =
  struct
    type nonrec t = string
    let context_ = "ExperimentErrorAccountId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentErrorAccountId" j
    let to_json = simple_to_json to_value
  end
module ExperimentErrorCode =
  struct
    type nonrec t = string
    let context_ = "ExperimentErrorCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentErrorCode" j
    let to_json = simple_to_json to_value
  end
module ExperimentErrorLocation =
  struct
    type nonrec t = string
    let context_ = "ExperimentErrorLocation"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentErrorLocation" j
    let to_json = simple_to_json to_value
  end
module TargetResourceTypeId =
  struct
    type nonrec t = string
    let context_ = "TargetResourceTypeId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TargetResourceTypeId" j
    let to_json = simple_to_json to_value
  end
module ActionId =
  struct
    type nonrec t = string
    let context_ = "ActionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionId" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateActionDescription =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateActionDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateActionDescription" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateActionParameterMap =
  struct
    type nonrec t =
      (ExperimentTemplateActionParameterName.t *
        ExperimentTemplateActionParameter.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ExperimentTemplateActionParameterName.of_string
                                chopped),
                              (ExperimentTemplateActionParameter.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateActionParameterName.to_value x) |>
                    (fun x ->
                       (ExperimentTemplateActionParameter.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json
        ~key_of_string:ExperimentTemplateActionParameterName.of_string
        ~of_json:ExperimentTemplateActionParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTemplateActionStartAfterList =
  struct
    type nonrec t = ExperimentTemplateActionStartAfter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentTemplateActionStartAfter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ExperimentTemplateActionStartAfter.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTemplateActionStartAfterList"
        ~of_json:ExperimentTemplateActionStartAfter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTemplateActionTargetMap =
  struct
    type nonrec t =
      (ExperimentTemplateActionTargetName.t * ExperimentTemplateTargetName.t)
        list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ExperimentTemplateActionTargetName.of_string
                                chopped),
                              (ExperimentTemplateTargetName.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateActionTargetName.to_value x) |>
                    (fun x ->
                       (ExperimentTemplateTargetName.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json
        ~key_of_string:ExperimentTemplateActionTargetName.of_string
        ~of_json:ExperimentTemplateTargetName.of_json j
    let to_json v = composed_to_json to_value v
  end
module CloudWatchLogGroupArn =
  struct
    type nonrec t = string
    let context_ = "CloudWatchLogGroupArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CloudWatchLogGroupArn" j
    let to_json = simple_to_json to_value
  end
module S3ObjectKey =
  struct
    type nonrec t = string
    let context_ = "S3ObjectKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:700) >>=
                  (fun () -> check_pattern i ~pattern:"[\\s\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3ObjectKey" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateReportConfigurationCloudWatchDashboardList =
  struct
    type nonrec t =
      ExperimentTemplateReportConfigurationCloudWatchDashboard.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map
            ~f:ExperimentTemplateReportConfigurationCloudWatchDashboard.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ExperimentTemplateReportConfigurationCloudWatchDashboard.of_xml)
    let of_json j =
      list_of_json
        ~kind:"ExperimentTemplateReportConfigurationCloudWatchDashboardList"
        ~of_json:ExperimentTemplateReportConfigurationCloudWatchDashboard.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ReportConfigurationS3Output =
  struct
    type nonrec t =
      {
      bucketName: S3BucketName.t option
        [@ocaml.doc
          "The name of the S3 bucket where the experiment report will be stored."];
      prefix: ReportConfigurationS3OutputPrefix.t option
        [@ocaml.doc
          "The prefix of the S3 bucket where the experiment report will be stored."]}
    let make ?bucketName = fun ?prefix -> fun () -> { bucketName; prefix }
    let to_value x =
      structure_to_value
        [("bucketName", (Option.map x.bucketName ~f:S3BucketName.to_value));
        ("prefix",
          (Option.map x.prefix ~f:ReportConfigurationS3OutputPrefix.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:ReportConfigurationS3OutputPrefix.of_xml)
          (Xml.child xml_arg0 "prefix") in
      let bucketName =
        (Option.map ~f:S3BucketName.of_xml) (Xml.child xml_arg0 "bucketName") in
      make ?prefix ?bucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefix =
        field_map json__ "prefix" ReportConfigurationS3OutputPrefix.of_json in
      let bucketName = field_map json__ "bucketName" S3BucketName.of_json in
      make ?prefix ?bucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the S3 destination for the experiment report."]
module StopConditionSource =
  struct
    type nonrec t = string
    let context_ = "StopConditionSource"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StopConditionSource" j
    let to_json = simple_to_json to_value
  end
module StopConditionValue =
  struct
    type nonrec t = string
    let context_ = "StopConditionValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"[\\s\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StopConditionValue" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateTargetFilterList =
  struct
    type nonrec t = ExperimentTemplateTargetFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentTemplateTargetFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentTemplateTargetFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTemplateTargetFilterList"
        ~of_json:ExperimentTemplateTargetFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTemplateTargetParameterMap =
  struct
    type nonrec t =
      (ExperimentTemplateTargetParameterName.t *
        ExperimentTemplateTargetParameterValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ExperimentTemplateTargetParameterName.of_string
                                chopped),
                              (ExperimentTemplateTargetParameterValue.of_string
                                 v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateTargetParameterName.to_value x) |>
                    (fun x ->
                       (ExperimentTemplateTargetParameterValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json
        ~key_of_string:ExperimentTemplateTargetParameterName.of_string
        ~of_json:ExperimentTemplateTargetParameterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTemplateTargetSelectionMode =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateTargetSelectionMode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ExperimentTemplateTargetSelectionMode" j
    let to_json = simple_to_json to_value
  end
module ResourceArnList =
  struct
    type nonrec t = ResourceArn.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:5); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ResourceArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceArn.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceArnList" ~of_json:ResourceArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportConfigurationCloudWatchDashboardInput =
  struct
    type nonrec t =
      {
      dashboardIdentifier:
        ReportConfigurationCloudWatchDashboardIdentifier.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the CloudWatch dashboard to include in the experiment report."]}
    let make ?dashboardIdentifier = fun () -> { dashboardIdentifier }
    let to_value x =
      structure_to_value
        [("dashboardIdentifier",
           (Option.map x.dashboardIdentifier
              ~f:ReportConfigurationCloudWatchDashboardIdentifier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dashboardIdentifier =
        (Option.map
           ~f:ReportConfigurationCloudWatchDashboardIdentifier.of_xml)
          (Xml.child xml_arg0 "dashboardIdentifier") in
      make ?dashboardIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dashboardIdentifier =
        field_map json__ "dashboardIdentifier"
          ReportConfigurationCloudWatchDashboardIdentifier.of_json in
      make ?dashboardIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the CloudWatch dashboard for the experiment report."]
module ExperimentTemplateTargetInputFilter =
  struct
    type nonrec t =
      {
      path: ExperimentTemplateTargetFilterPath.t
        [@ocaml.doc "The attribute path for the filter."];
      values: ExperimentTemplateTargetFilterValues.t
        [@ocaml.doc "The attribute values for the filter."]}
    let context_ = "ExperimentTemplateTargetInputFilter"
    let make ~path = fun ~values -> fun () -> { path; values }
    let to_value x =
      structure_to_value
        [("path",
           (Some (ExperimentTemplateTargetFilterPath.to_value x.path)));
        ("values",
          (Some (ExperimentTemplateTargetFilterValues.to_value x.values)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        ExperimentTemplateTargetFilterValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "values") in
      let path =
        ExperimentTemplateTargetFilterPath.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "path") in
      make ~values ~path ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let values =
        field_map_exn json__ "values"
          ExperimentTemplateTargetFilterValues.of_json in
      let path =
        field_map_exn json__ "path"
          ExperimentTemplateTargetFilterPath.of_json in
      make ~values ~path ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a filter used for the target resource input in an experiment template. For more information, see Resource filters in the Fault Injection Service User Guide."]
module ExperimentActionDescription =
  struct
    type nonrec t = string
    let context_ = "ExperimentActionDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentActionDescription" j
    let to_json = simple_to_json to_value
  end
module ExperimentActionEndTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ExperimentActionParameterMap =
  struct
    type nonrec t =
      (ExperimentActionParameterName.t * ExperimentActionParameter.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ExperimentActionParameterName.of_string chopped),
                              (ExperimentActionParameter.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentActionParameterName.to_value x) |>
                    (fun x ->
                       (ExperimentActionParameter.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentActionParameterName.of_string
        ~of_json:ExperimentActionParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentActionStartAfterList =
  struct
    type nonrec t = ExperimentActionStartAfter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentActionStartAfter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentActionStartAfter.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentActionStartAfterList"
        ~of_json:ExperimentActionStartAfter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentActionStartTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ExperimentActionState =
  struct
    type nonrec t =
      {
      status: ExperimentActionStatus.t option
        [@ocaml.doc "The state of the action."];
      reason: ExperimentActionStatusReason.t option
        [@ocaml.doc "The reason for the state."]}
    let make ?status = fun ?reason -> fun () -> { status; reason }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ExperimentActionStatus.to_value));
        ("reason",
          (Option.map x.reason ~f:ExperimentActionStatusReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:ExperimentActionStatusReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let status =
        (Option.map ~f:ExperimentActionStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?reason ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason =
        field_map json__ "reason" ExperimentActionStatusReason.of_json in
      let status = field_map json__ "status" ExperimentActionStatus.of_json in
      make ?reason ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the state of an action."]
module ExperimentActionTargetMap =
  struct
    type nonrec t =
      (ExperimentActionTargetName.t * ExperimentTargetName.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ExperimentActionTargetName.of_string chopped),
                              (ExperimentTargetName.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentActionTargetName.to_value x) |>
                    (fun x ->
                       (ExperimentTargetName.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentActionTargetName.of_string
        ~of_json:ExperimentTargetName.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentReportS3Report =
  struct
    type nonrec t =
      {
      arn: ExperimentReportS3ReportArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the generated report."];
      reportType: ExperimentReportS3ReportType.t option
        [@ocaml.doc "The report type for the experiment report."]}
    let make ?arn = fun ?reportType -> fun () -> { arn; reportType }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:ExperimentReportS3ReportArn.to_value));
        ("reportType",
          (Option.map x.reportType ~f:ExperimentReportS3ReportType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportType =
        (Option.map ~f:ExperimentReportS3ReportType.of_xml)
          (Xml.child xml_arg0 "reportType") in
      let arn =
        (Option.map ~f:ExperimentReportS3ReportArn.of_xml)
          (Xml.child xml_arg0 "arn") in
      make ?reportType ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reportType =
        field_map json__ "reportType" ExperimentReportS3ReportType.of_json in
      let arn = field_map json__ "arn" ExperimentReportS3ReportArn.of_json in
      make ?reportType ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the S3 destination for the report."]
module ExperimentReportError =
  struct
    type nonrec t =
      {
      code: ExperimentReportErrorCode.t option
        [@ocaml.doc
          "The error code for the failed experiment report generation."]}
    let make ?code = fun () -> { code }
    let to_value x =
      structure_to_value
        [("code", (Option.map x.code ~f:ExperimentReportErrorCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let code =
        (Option.map ~f:ExperimentReportErrorCode.of_xml)
          (Xml.child xml_arg0 "code") in
      make ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let code = field_map json__ "code" ExperimentReportErrorCode.of_json in
      make ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the error when experiment report generation has failed."]
module ExperimentReportReason =
  struct
    type nonrec t = string
    let context_ = "ExperimentReportReason"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentReportReason" j
    let to_json = simple_to_json to_value
  end
module ExperimentReportStatus =
  struct
    type nonrec t =
      | Pending 
      | Running 
      | Completed 
      | Cancelled 
      | Failed 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pending -> "pending"
      | Running -> "running"
      | Completed -> "completed"
      | Cancelled -> "cancelled"
      | Failed -> "failed"
      | Non_static_id s -> s
    let of_string =
      function
      | "pending" -> Pending
      | "running" -> Running
      | "completed" -> Completed
      | "cancelled" -> Cancelled
      | "failed" -> Failed
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ExperimentReportStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ExperimentReportStatus" j)
    let to_json = simple_to_json to_value
  end
module ExperimentReportConfigurationCloudWatchDashboardList =
  struct
    type nonrec t = ExperimentReportConfigurationCloudWatchDashboard.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map
            ~f:ExperimentReportConfigurationCloudWatchDashboard.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ExperimentReportConfigurationCloudWatchDashboard.of_xml)
    let of_json j =
      list_of_json
        ~kind:"ExperimentReportConfigurationCloudWatchDashboardList"
        ~of_json:ExperimentReportConfigurationCloudWatchDashboard.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentReportConfigurationOutputsS3Configuration =
  struct
    type nonrec t =
      {
      bucketName: S3BucketName.t option
        [@ocaml.doc
          "The name of the S3 bucket where the experiment report will be stored."];
      prefix: ReportConfigurationS3OutputPrefix.t option
        [@ocaml.doc
          "The prefix of the S3 bucket where the experiment report will be stored."]}
    let make ?bucketName = fun ?prefix -> fun () -> { bucketName; prefix }
    let to_value x =
      structure_to_value
        [("bucketName", (Option.map x.bucketName ~f:S3BucketName.to_value));
        ("prefix",
          (Option.map x.prefix ~f:ReportConfigurationS3OutputPrefix.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:ReportConfigurationS3OutputPrefix.of_xml)
          (Xml.child xml_arg0 "prefix") in
      let bucketName =
        (Option.map ~f:S3BucketName.of_xml) (Xml.child xml_arg0 "bucketName") in
      make ?prefix ?bucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefix =
        field_map json__ "prefix" ReportConfigurationS3OutputPrefix.of_json in
      let bucketName = field_map json__ "bucketName" S3BucketName.of_json in
      make ?prefix ?bucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the S3 destination for the experiment report."]
module ExperimentTargetFilterList =
  struct
    type nonrec t = ExperimentTargetFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentTargetFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentTargetFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTargetFilterList"
        ~of_json:ExperimentTargetFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTargetParameterMap =
  struct
    type nonrec t =
      (ExperimentTargetParameterName.t * ExperimentTargetParameterValue.t)
        list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ExperimentTargetParameterName.of_string chopped),
                              (ExperimentTargetParameterValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTargetParameterName.to_value x) |>
                    (fun x ->
                       (ExperimentTargetParameterValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentTargetParameterName.of_string
        ~of_json:ExperimentTargetParameterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTargetSelectionMode =
  struct
    type nonrec t = string
    let context_ = "ExperimentTargetSelectionMode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTargetSelectionMode" j
    let to_json = simple_to_json to_value
  end
module AccountTargeting =
  struct
    type nonrec t =
      | Single_account 
      | Multi_account 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Single_account -> "single-account"
      | Multi_account -> "multi-account"
      | Non_static_id s -> s
    let of_string =
      function
      | "single-account" -> Single_account
      | "multi-account" -> Multi_account
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AccountTargeting" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AccountTargeting" j)
    let to_json = simple_to_json to_value
  end
module ActionsMode =
  struct
    type nonrec t =
      | Skip_all 
      | Run_all 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Skip_all -> "skip-all"
      | Run_all -> "run-all"
      | Non_static_id s -> s
    let of_string =
      function
      | "skip-all" -> Skip_all
      | "run-all" -> Run_all
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ActionsMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ActionsMode" j)
    let to_json = simple_to_json to_value
  end
module EmptyTargetResolutionMode =
  struct
    type nonrec t =
      | Fail 
      | Skip 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Fail -> "fail" | Skip -> "skip" | Non_static_id s -> s
    let of_string =
      function | "fail" -> Fail | "skip" -> Skip | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EmptyTargetResolutionMode" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"EmptyTargetResolutionMode" j)
    let to_json = simple_to_json to_value
  end
module ExperimentError =
  struct
    type nonrec t =
      {
      accountId: ExperimentErrorAccountId.t option
        [@ocaml.doc
          "The Amazon Web Services Account ID where the experiment failure occurred."];
      code: ExperimentErrorCode.t option
        [@ocaml.doc "The error code for the failed experiment."];
      location: ExperimentErrorLocation.t option
        [@ocaml.doc
          "Context for the section of the experiment template that failed."]}
    let make ?accountId =
      fun ?code -> fun ?location -> fun () -> { accountId; code; location }
    let to_value x =
      structure_to_value
        [("accountId",
           (Option.map x.accountId ~f:ExperimentErrorAccountId.to_value));
        ("code", (Option.map x.code ~f:ExperimentErrorCode.to_value));
        ("location",
          (Option.map x.location ~f:ExperimentErrorLocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:ExperimentErrorLocation.of_xml)
          (Xml.child xml_arg0 "location") in
      let code =
        (Option.map ~f:ExperimentErrorCode.of_xml)
          (Xml.child xml_arg0 "code") in
      let accountId =
        (Option.map ~f:ExperimentErrorAccountId.of_xml)
          (Xml.child xml_arg0 "accountId") in
      make ?location ?code ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let location =
        field_map json__ "location" ExperimentErrorLocation.of_json in
      let code = field_map json__ "code" ExperimentErrorCode.of_json in
      let accountId =
        field_map json__ "accountId" ExperimentErrorAccountId.of_json in
      make ?location ?code ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the error when an experiment has failed."]
module ExperimentStatus =
  struct
    type nonrec t =
      | Pending 
      | Initiating 
      | Running 
      | Completed 
      | Stopping 
      | Stopped 
      | Failed 
      | Cancelled 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pending -> "pending"
      | Initiating -> "initiating"
      | Running -> "running"
      | Completed -> "completed"
      | Stopping -> "stopping"
      | Stopped -> "stopped"
      | Failed -> "failed"
      | Cancelled -> "cancelled"
      | Non_static_id s -> s
    let of_string =
      function
      | "pending" -> Pending
      | "initiating" -> Initiating
      | "running" -> Running
      | "completed" -> Completed
      | "stopping" -> Stopping
      | "stopped" -> Stopped
      | "failed" -> Failed
      | "cancelled" -> Cancelled
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ExperimentStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExperimentStatus" j)
    let to_json = simple_to_json to_value
  end
module ExperimentStatusReason =
  struct
    type nonrec t = string
    let context_ = "ExperimentStatusReason"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentStatusReason" j
    let to_json = simple_to_json to_value
  end
module TargetInformationKey =
  struct
    type nonrec t = string
    let context_ = "TargetInformationKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TargetInformationKey" j
    let to_json = simple_to_json to_value
  end
module TargetInformationValue =
  struct
    type nonrec t = string
    let context_ = "TargetInformationValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TargetInformationValue" j
    let to_json = simple_to_json to_value
  end
module ActionTarget =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t option
        [@ocaml.doc "The resource type of the target."]}
    let make ?resourceType = fun () -> { resourceType }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Option.map x.resourceType ~f:TargetResourceTypeId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:TargetResourceTypeId.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      make ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType =
        field_map json__ "resourceType" TargetResourceTypeId.of_json in
      make ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a target for an action."]
module ActionTargetName =
  struct
    type nonrec t = string
    let context_ = "ActionTargetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionTargetName" j
    let to_json = simple_to_json to_value
  end
module TargetResourceTypeParameterDescription =
  struct
    type nonrec t = string
    let context_ = "TargetResourceTypeParameterDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"TargetResourceTypeParameterDescription" j
    let to_json = simple_to_json to_value
  end
module TargetResourceTypeParameterRequired =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ActionParameterDescription =
  struct
    type nonrec t = string
    let context_ = "ActionParameterDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionParameterDescription" j
    let to_json = simple_to_json to_value
  end
module ActionParameterRequired =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module SafetyLeverStatus =
  struct
    type nonrec t =
      | Disengaged 
      | Engaged 
      | Engaging 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Disengaged -> "disengaged"
      | Engaged -> "engaged"
      | Engaging -> "engaging"
      | Non_static_id s -> s
    let of_string =
      function
      | "disengaged" -> Disengaged
      | "engaged" -> Engaged
      | "engaging" -> Engaging
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SafetyLeverStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SafetyLeverStatus" j)
    let to_json = simple_to_json to_value
  end
module SafetyLeverStatusReason =
  struct
    type nonrec t = string
    let context_ = "SafetyLeverStatusReason"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SafetyLeverStatusReason" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateAction =
  struct
    type nonrec t =
      {
      actionId: ActionId.t option [@ocaml.doc "The ID of the action."];
      description: ExperimentTemplateActionDescription.t option
        [@ocaml.doc "A description for the action."];
      parameters: ExperimentTemplateActionParameterMap.t option
        [@ocaml.doc "The parameters for the action."];
      targets: ExperimentTemplateActionTargetMap.t option
        [@ocaml.doc "The targets for the action."];
      startAfter: ExperimentTemplateActionStartAfterList.t option
        [@ocaml.doc
          "The name of the action that must be completed before the current action starts."]}
    let make ?actionId =
      fun ?description ->
        fun ?parameters ->
          fun ?targets ->
            fun ?startAfter ->
              fun () ->
                { actionId; description; parameters; targets; startAfter }
    let to_value x =
      structure_to_value
        [("actionId", (Option.map x.actionId ~f:ActionId.to_value));
        ("description",
          (Option.map x.description
             ~f:ExperimentTemplateActionDescription.to_value));
        ("parameters",
          (Option.map x.parameters
             ~f:ExperimentTemplateActionParameterMap.to_value));
        ("targets",
          (Option.map x.targets ~f:ExperimentTemplateActionTargetMap.to_value));
        ("startAfter",
          (Option.map x.startAfter
             ~f:ExperimentTemplateActionStartAfterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startAfter =
        (Option.map ~f:ExperimentTemplateActionStartAfterList.of_xml)
          (Xml.child xml_arg0 "startAfter") in
      let targets =
        (Option.map ~f:ExperimentTemplateActionTargetMap.of_xml)
          (Xml.child xml_arg0 "targets") in
      let parameters =
        (Option.map ~f:ExperimentTemplateActionParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let description =
        (Option.map ~f:ExperimentTemplateActionDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let actionId =
        (Option.map ~f:ActionId.of_xml) (Xml.child xml_arg0 "actionId") in
      make ?startAfter ?targets ?parameters ?description ?actionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let startAfter =
        field_map json__ "startAfter"
          ExperimentTemplateActionStartAfterList.of_json in
      let targets =
        field_map json__ "targets" ExperimentTemplateActionTargetMap.of_json in
      let parameters =
        field_map json__ "parameters"
          ExperimentTemplateActionParameterMap.of_json in
      let description =
        field_map json__ "description"
          ExperimentTemplateActionDescription.of_json in
      let actionId = field_map json__ "actionId" ActionId.of_json in
      make ?startAfter ?targets ?parameters ?description ?actionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes an action for an experiment template."]
module ExperimentTemplateActionName =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateActionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTemplateActionName" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateCloudWatchLogsLogConfiguration =
  struct
    type nonrec t =
      {
      logGroupArn: CloudWatchLogGroupArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the destination Amazon CloudWatch Logs log group."]}
    let make ?logGroupArn = fun () -> { logGroupArn }
    let to_value x =
      structure_to_value
        [("logGroupArn",
           (Option.map x.logGroupArn ~f:CloudWatchLogGroupArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logGroupArn =
        (Option.map ~f:CloudWatchLogGroupArn.of_xml)
          (Xml.child xml_arg0 "logGroupArn") in
      make ?logGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logGroupArn =
        field_map json__ "logGroupArn" CloudWatchLogGroupArn.of_json in
      make ?logGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the configuration for experiment logging to Amazon CloudWatch Logs."]
module ExperimentTemplateS3LogConfiguration =
  struct
    type nonrec t =
      {
      bucketName: S3BucketName.t option
        [@ocaml.doc "The name of the destination bucket."];
      prefix: S3ObjectKey.t option [@ocaml.doc "The bucket prefix."]}
    let make ?bucketName = fun ?prefix -> fun () -> { bucketName; prefix }
    let to_value x =
      structure_to_value
        [("bucketName", (Option.map x.bucketName ~f:S3BucketName.to_value));
        ("prefix", (Option.map x.prefix ~f:S3ObjectKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:S3ObjectKey.of_xml) (Xml.child xml_arg0 "prefix") in
      let bucketName =
        (Option.map ~f:S3BucketName.of_xml) (Xml.child xml_arg0 "bucketName") in
      make ?prefix ?bucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefix = field_map json__ "prefix" S3ObjectKey.of_json in
      let bucketName = field_map json__ "bucketName" S3BucketName.of_json in
      make ?prefix ?bucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the configuration for experiment logging to Amazon S3."]
module LogSchemaVersion =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for LogSchemaVersion" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateReportConfigurationDataSources =
  struct
    type nonrec t =
      {
      cloudWatchDashboards:
        ExperimentTemplateReportConfigurationCloudWatchDashboardList.t option
        [@ocaml.doc
          "The CloudWatch dashboards to include as data sources in the experiment report."]}
    let make ?cloudWatchDashboards = fun () -> { cloudWatchDashboards }
    let to_value x =
      structure_to_value
        [("cloudWatchDashboards",
           (Option.map x.cloudWatchDashboards
              ~f:ExperimentTemplateReportConfigurationCloudWatchDashboardList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cloudWatchDashboards =
        (Option.map
           ~f:ExperimentTemplateReportConfigurationCloudWatchDashboardList.of_xml)
          (Xml.child xml_arg0 "cloudWatchDashboards") in
      make ?cloudWatchDashboards ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cloudWatchDashboards =
        field_map json__ "cloudWatchDashboards"
          ExperimentTemplateReportConfigurationCloudWatchDashboardList.of_json in
      make ?cloudWatchDashboards ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the data sources for the experiment report."]
module ExperimentTemplateReportConfigurationOutputs =
  struct
    type nonrec t =
      {
      s3Configuration: ReportConfigurationS3Output.t option
        [@ocaml.doc "The S3 destination for the experiment report."]}
    let make ?s3Configuration = fun () -> { s3Configuration }
    let to_value x =
      structure_to_value
        [("s3Configuration",
           (Option.map x.s3Configuration
              ~f:ReportConfigurationS3Output.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Configuration =
        (Option.map ~f:ReportConfigurationS3Output.of_xml)
          (Xml.child xml_arg0 "s3Configuration") in
      make ?s3Configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Configuration =
        field_map json__ "s3Configuration"
          ReportConfigurationS3Output.of_json in
      make ?s3Configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output destinations of the experiment report."]
module ReportConfigurationDuration =
  struct
    type nonrec t = string
    let context_ = "ReportConfigurationDuration"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReportConfigurationDuration" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateStopCondition =
  struct
    type nonrec t =
      {
      source: StopConditionSource.t option
        [@ocaml.doc "The source for the stop condition."];
      value: StopConditionValue.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the CloudWatch alarm, if applicable."]}
    let make ?source = fun ?value -> fun () -> { source; value }
    let to_value x =
      structure_to_value
        [("source", (Option.map x.source ~f:StopConditionSource.to_value));
        ("value", (Option.map x.value ~f:StopConditionValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:StopConditionValue.of_xml)
          (Xml.child xml_arg0 "value") in
      let source =
        (Option.map ~f:StopConditionSource.of_xml)
          (Xml.child xml_arg0 "source") in
      make ?value ?source ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" StopConditionValue.of_json in
      let source = field_map json__ "source" StopConditionSource.of_json in
      make ?value ?source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a stop condition for an experiment template."]
module ExperimentTemplateTarget =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t option
        [@ocaml.doc "The resource type."];
      resourceArns: ResourceArnList.t option
        [@ocaml.doc "The Amazon Resource Names (ARNs) of the targets."];
      resourceTags: TagMap.t option
        [@ocaml.doc "The tags for the target resources."];
      filters: ExperimentTemplateTargetFilterList.t option
        [@ocaml.doc
          "The filters to apply to identify target resources using specific attributes."];
      selectionMode: ExperimentTemplateTargetSelectionMode.t option
        [@ocaml.doc
          "Scopes the identified resources to a specific count or percentage."];
      parameters: ExperimentTemplateTargetParameterMap.t option
        [@ocaml.doc "The resource type parameters."]}
    let make ?resourceType =
      fun ?resourceArns ->
        fun ?resourceTags ->
          fun ?filters ->
            fun ?selectionMode ->
              fun ?parameters ->
                fun () ->
                  {
                    resourceType;
                    resourceArns;
                    resourceTags;
                    filters;
                    selectionMode;
                    parameters
                  }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Option.map x.resourceType ~f:TargetResourceTypeId.to_value));
        ("resourceArns",
          (Option.map x.resourceArns ~f:ResourceArnList.to_value));
        ("resourceTags", (Option.map x.resourceTags ~f:TagMap.to_value));
        ("filters",
          (Option.map x.filters
             ~f:ExperimentTemplateTargetFilterList.to_value));
        ("selectionMode",
          (Option.map x.selectionMode
             ~f:ExperimentTemplateTargetSelectionMode.to_value));
        ("parameters",
          (Option.map x.parameters
             ~f:ExperimentTemplateTargetParameterMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:ExperimentTemplateTargetParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let selectionMode =
        (Option.map ~f:ExperimentTemplateTargetSelectionMode.of_xml)
          (Xml.child xml_arg0 "selectionMode") in
      let filters =
        (Option.map ~f:ExperimentTemplateTargetFilterList.of_xml)
          (Xml.child xml_arg0 "filters") in
      let resourceTags =
        (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "resourceTags") in
      let resourceArns =
        (Option.map ~f:ResourceArnList.of_xml)
          (Xml.child xml_arg0 "resourceArns") in
      let resourceType =
        (Option.map ~f:TargetResourceTypeId.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      make ?parameters ?selectionMode ?filters ?resourceTags ?resourceArns
        ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parameters =
        field_map json__ "parameters"
          ExperimentTemplateTargetParameterMap.of_json in
      let selectionMode =
        field_map json__ "selectionMode"
          ExperimentTemplateTargetSelectionMode.of_json in
      let filters =
        field_map json__ "filters" ExperimentTemplateTargetFilterList.of_json in
      let resourceTags = field_map json__ "resourceTags" TagMap.of_json in
      let resourceArns =
        field_map json__ "resourceArns" ResourceArnList.of_json in
      let resourceType =
        field_map json__ "resourceType" TargetResourceTypeId.of_json in
      make ?parameters ?selectionMode ?filters ?resourceTags ?resourceArns
        ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a target for an experiment template."]
module ReportConfigurationCloudWatchDashboardInputList =
  struct
    type nonrec t = ReportConfigurationCloudWatchDashboardInput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map ~f:ReportConfigurationCloudWatchDashboardInput.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ReportConfigurationCloudWatchDashboardInput.of_xml)
    let of_json j =
      list_of_json ~kind:"ReportConfigurationCloudWatchDashboardInputList"
        ~of_json:ReportConfigurationCloudWatchDashboardInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportConfigurationS3OutputInput =
  struct
    type nonrec t =
      {
      bucketName: S3BucketName.t option
        [@ocaml.doc
          "The name of the S3 bucket where the experiment report will be stored."];
      prefix: ReportConfigurationS3OutputPrefix.t option
        [@ocaml.doc
          "The prefix of the S3 bucket where the experiment report will be stored."]}
    let make ?bucketName = fun ?prefix -> fun () -> { bucketName; prefix }
    let to_value x =
      structure_to_value
        [("bucketName", (Option.map x.bucketName ~f:S3BucketName.to_value));
        ("prefix",
          (Option.map x.prefix ~f:ReportConfigurationS3OutputPrefix.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:ReportConfigurationS3OutputPrefix.of_xml)
          (Xml.child xml_arg0 "prefix") in
      let bucketName =
        (Option.map ~f:S3BucketName.of_xml) (Xml.child xml_arg0 "bucketName") in
      make ?prefix ?bucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefix =
        field_map json__ "prefix" ReportConfigurationS3OutputPrefix.of_json in
      let bucketName = field_map json__ "bucketName" S3BucketName.of_json in
      make ?prefix ?bucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the S3 destination for the experiment report."]
module ExperimentTemplateTargetFilterInputList =
  struct
    type nonrec t = ExperimentTemplateTargetInputFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentTemplateTargetInputFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ExperimentTemplateTargetInputFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTemplateTargetFilterInputList"
        ~of_json:ExperimentTemplateTargetInputFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentAction =
  struct
    type nonrec t =
      {
      actionId: ActionId.t option [@ocaml.doc "The ID of the action."];
      description: ExperimentActionDescription.t option
        [@ocaml.doc "The description for the action."];
      parameters: ExperimentActionParameterMap.t option
        [@ocaml.doc "The parameters for the action."];
      targets: ExperimentActionTargetMap.t option
        [@ocaml.doc "The targets for the action."];
      startAfter: ExperimentActionStartAfterList.t option
        [@ocaml.doc
          "The name of the action that must be completed before this action starts."];
      state: ExperimentActionState.t option
        [@ocaml.doc "The state of the action."];
      startTime: ExperimentActionStartTime.t option
        [@ocaml.doc "The time that the action started."];
      endTime: ExperimentActionEndTime.t option
        [@ocaml.doc "The time that the action ended."]}
    let make ?actionId =
      fun ?description ->
        fun ?parameters ->
          fun ?targets ->
            fun ?startAfter ->
              fun ?state ->
                fun ?startTime ->
                  fun ?endTime ->
                    fun () ->
                      {
                        actionId;
                        description;
                        parameters;
                        targets;
                        startAfter;
                        state;
                        startTime;
                        endTime
                      }
    let to_value x =
      structure_to_value
        [("actionId", (Option.map x.actionId ~f:ActionId.to_value));
        ("description",
          (Option.map x.description ~f:ExperimentActionDescription.to_value));
        ("parameters",
          (Option.map x.parameters ~f:ExperimentActionParameterMap.to_value));
        ("targets",
          (Option.map x.targets ~f:ExperimentActionTargetMap.to_value));
        ("startAfter",
          (Option.map x.startAfter ~f:ExperimentActionStartAfterList.to_value));
        ("state", (Option.map x.state ~f:ExperimentActionState.to_value));
        ("startTime",
          (Option.map x.startTime ~f:ExperimentActionStartTime.to_value));
        ("endTime",
          (Option.map x.endTime ~f:ExperimentActionEndTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endTime =
        (Option.map ~f:ExperimentActionEndTime.of_xml)
          (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:ExperimentActionStartTime.of_xml)
          (Xml.child xml_arg0 "startTime") in
      let state =
        (Option.map ~f:ExperimentActionState.of_xml)
          (Xml.child xml_arg0 "state") in
      let startAfter =
        (Option.map ~f:ExperimentActionStartAfterList.of_xml)
          (Xml.child xml_arg0 "startAfter") in
      let targets =
        (Option.map ~f:ExperimentActionTargetMap.of_xml)
          (Xml.child xml_arg0 "targets") in
      let parameters =
        (Option.map ~f:ExperimentActionParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let description =
        (Option.map ~f:ExperimentActionDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let actionId =
        (Option.map ~f:ActionId.of_xml) (Xml.child xml_arg0 "actionId") in
      make ?endTime ?startTime ?state ?startAfter ?targets ?parameters
        ?description ?actionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endTime =
        field_map json__ "endTime" ExperimentActionEndTime.of_json in
      let startTime =
        field_map json__ "startTime" ExperimentActionStartTime.of_json in
      let state = field_map json__ "state" ExperimentActionState.of_json in
      let startAfter =
        field_map json__ "startAfter" ExperimentActionStartAfterList.of_json in
      let targets =
        field_map json__ "targets" ExperimentActionTargetMap.of_json in
      let parameters =
        field_map json__ "parameters" ExperimentActionParameterMap.of_json in
      let description =
        field_map json__ "description" ExperimentActionDescription.of_json in
      let actionId = field_map json__ "actionId" ActionId.of_json in
      make ?endTime ?startTime ?state ?startAfter ?targets ?parameters
        ?description ?actionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the action for an experiment."]
module ExperimentActionName =
  struct
    type nonrec t = string
    let context_ = "ExperimentActionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentActionName" j
    let to_json = simple_to_json to_value
  end
module ExperimentCloudWatchLogsLogConfiguration =
  struct
    type nonrec t =
      {
      logGroupArn: CloudWatchLogGroupArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the destination Amazon CloudWatch Logs log group."]}
    let make ?logGroupArn = fun () -> { logGroupArn }
    let to_value x =
      structure_to_value
        [("logGroupArn",
           (Option.map x.logGroupArn ~f:CloudWatchLogGroupArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logGroupArn =
        (Option.map ~f:CloudWatchLogGroupArn.of_xml)
          (Xml.child xml_arg0 "logGroupArn") in
      make ?logGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logGroupArn =
        field_map json__ "logGroupArn" CloudWatchLogGroupArn.of_json in
      make ?logGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the configuration for experiment logging to Amazon CloudWatch Logs."]
module ExperimentS3LogConfiguration =
  struct
    type nonrec t =
      {
      bucketName: S3BucketName.t option
        [@ocaml.doc "The name of the destination bucket."];
      prefix: S3ObjectKey.t option [@ocaml.doc "The bucket prefix."]}
    let make ?bucketName = fun ?prefix -> fun () -> { bucketName; prefix }
    let to_value x =
      structure_to_value
        [("bucketName", (Option.map x.bucketName ~f:S3BucketName.to_value));
        ("prefix", (Option.map x.prefix ~f:S3ObjectKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:S3ObjectKey.of_xml) (Xml.child xml_arg0 "prefix") in
      let bucketName =
        (Option.map ~f:S3BucketName.of_xml) (Xml.child xml_arg0 "bucketName") in
      make ?prefix ?bucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefix = field_map json__ "prefix" S3ObjectKey.of_json in
      let bucketName = field_map json__ "bucketName" S3BucketName.of_json in
      make ?prefix ?bucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the configuration for experiment logging to Amazon S3."]
module ExperimentReportS3ReportList =
  struct
    type nonrec t = ExperimentReportS3Report.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentReportS3Report.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentReportS3Report.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentReportS3ReportList"
        ~of_json:ExperimentReportS3Report.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentReportState =
  struct
    type nonrec t =
      {
      status: ExperimentReportStatus.t option
        [@ocaml.doc "The state of the experiment report generation."];
      reason: ExperimentReportReason.t option
        [@ocaml.doc
          "The reason for the state of the experiment report generation."];
      error: ExperimentReportError.t option
        [@ocaml.doc
          "The error information of the experiment when the experiment report generation has failed."]}
    let make ?status =
      fun ?reason -> fun ?error -> fun () -> { status; reason; error }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ExperimentReportStatus.to_value));
        ("reason", (Option.map x.reason ~f:ExperimentReportReason.to_value));
        ("error", (Option.map x.error ~f:ExperimentReportError.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let error =
        (Option.map ~f:ExperimentReportError.of_xml)
          (Xml.child xml_arg0 "error") in
      let reason =
        (Option.map ~f:ExperimentReportReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let status =
        (Option.map ~f:ExperimentReportStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?error ?reason ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let error = field_map json__ "error" ExperimentReportError.of_json in
      let reason = field_map json__ "reason" ExperimentReportReason.of_json in
      let status = field_map json__ "status" ExperimentReportStatus.of_json in
      make ?error ?reason ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the state of the experiment report generation."]
module ExperimentReportConfigurationDataSources =
  struct
    type nonrec t =
      {
      cloudWatchDashboards:
        ExperimentReportConfigurationCloudWatchDashboardList.t option
        [@ocaml.doc
          "The CloudWatch dashboards to include as data sources in the experiment report."]}
    let make ?cloudWatchDashboards = fun () -> { cloudWatchDashboards }
    let to_value x =
      structure_to_value
        [("cloudWatchDashboards",
           (Option.map x.cloudWatchDashboards
              ~f:ExperimentReportConfigurationCloudWatchDashboardList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cloudWatchDashboards =
        (Option.map
           ~f:ExperimentReportConfigurationCloudWatchDashboardList.of_xml)
          (Xml.child xml_arg0 "cloudWatchDashboards") in
      make ?cloudWatchDashboards ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cloudWatchDashboards =
        field_map json__ "cloudWatchDashboards"
          ExperimentReportConfigurationCloudWatchDashboardList.of_json in
      make ?cloudWatchDashboards ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the data sources for the experiment report."]
module ExperimentReportConfigurationOutputs =
  struct
    type nonrec t =
      {
      s3Configuration:
        ExperimentReportConfigurationOutputsS3Configuration.t option
        [@ocaml.doc "The S3 destination for the experiment report."]}
    let make ?s3Configuration = fun () -> { s3Configuration }
    let to_value x =
      structure_to_value
        [("s3Configuration",
           (Option.map x.s3Configuration
              ~f:ExperimentReportConfigurationOutputsS3Configuration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Configuration =
        (Option.map
           ~f:ExperimentReportConfigurationOutputsS3Configuration.of_xml)
          (Xml.child xml_arg0 "s3Configuration") in
      make ?s3Configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Configuration =
        field_map json__ "s3Configuration"
          ExperimentReportConfigurationOutputsS3Configuration.of_json in
      make ?s3Configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the output destinations of the experiment report."]
module ExperimentStopCondition =
  struct
    type nonrec t =
      {
      source: StopConditionSource.t option
        [@ocaml.doc "The source for the stop condition."];
      value: StopConditionValue.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the CloudWatch alarm, if applicable."]}
    let make ?source = fun ?value -> fun () -> { source; value }
    let to_value x =
      structure_to_value
        [("source", (Option.map x.source ~f:StopConditionSource.to_value));
        ("value", (Option.map x.value ~f:StopConditionValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:StopConditionValue.of_xml)
          (Xml.child xml_arg0 "value") in
      let source =
        (Option.map ~f:StopConditionSource.of_xml)
          (Xml.child xml_arg0 "source") in
      make ?value ?source ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" StopConditionValue.of_json in
      let source = field_map json__ "source" StopConditionSource.of_json in
      make ?value ?source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the stop condition for an experiment."]
module ExperimentTarget =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t option
        [@ocaml.doc "The resource type."];
      resourceArns: ResourceArnList.t option
        [@ocaml.doc "The Amazon Resource Names (ARNs) of the resources."];
      resourceTags: TagMap.t option
        [@ocaml.doc "The tags for the target resources."];
      filters: ExperimentTargetFilterList.t option
        [@ocaml.doc
          "The filters to apply to identify target resources using specific attributes."];
      selectionMode: ExperimentTargetSelectionMode.t option
        [@ocaml.doc
          "Scopes the identified resources to a specific count or percentage."];
      parameters: ExperimentTargetParameterMap.t option
        [@ocaml.doc "The resource type parameters."]}
    let make ?resourceType =
      fun ?resourceArns ->
        fun ?resourceTags ->
          fun ?filters ->
            fun ?selectionMode ->
              fun ?parameters ->
                fun () ->
                  {
                    resourceType;
                    resourceArns;
                    resourceTags;
                    filters;
                    selectionMode;
                    parameters
                  }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Option.map x.resourceType ~f:TargetResourceTypeId.to_value));
        ("resourceArns",
          (Option.map x.resourceArns ~f:ResourceArnList.to_value));
        ("resourceTags", (Option.map x.resourceTags ~f:TagMap.to_value));
        ("filters",
          (Option.map x.filters ~f:ExperimentTargetFilterList.to_value));
        ("selectionMode",
          (Option.map x.selectionMode
             ~f:ExperimentTargetSelectionMode.to_value));
        ("parameters",
          (Option.map x.parameters ~f:ExperimentTargetParameterMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:ExperimentTargetParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let selectionMode =
        (Option.map ~f:ExperimentTargetSelectionMode.of_xml)
          (Xml.child xml_arg0 "selectionMode") in
      let filters =
        (Option.map ~f:ExperimentTargetFilterList.of_xml)
          (Xml.child xml_arg0 "filters") in
      let resourceTags =
        (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "resourceTags") in
      let resourceArns =
        (Option.map ~f:ResourceArnList.of_xml)
          (Xml.child xml_arg0 "resourceArns") in
      let resourceType =
        (Option.map ~f:TargetResourceTypeId.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      make ?parameters ?selectionMode ?filters ?resourceTags ?resourceArns
        ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parameters =
        field_map json__ "parameters" ExperimentTargetParameterMap.of_json in
      let selectionMode =
        field_map json__ "selectionMode"
          ExperimentTargetSelectionMode.of_json in
      let filters =
        field_map json__ "filters" ExperimentTargetFilterList.of_json in
      let resourceTags = field_map json__ "resourceTags" TagMap.of_json in
      let resourceArns =
        field_map json__ "resourceArns" ResourceArnList.of_json in
      let resourceType =
        field_map json__ "resourceType" TargetResourceTypeId.of_json in
      make ?parameters ?selectionMode ?filters ?resourceTags ?resourceArns
        ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a target for an experiment."]
module TargetResourceTypeDescription =
  struct
    type nonrec t = string
    let context_ = "TargetResourceTypeDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TargetResourceTypeDescription" j
    let to_json = simple_to_json to_value
  end
module RoleArn =
  struct
    type nonrec t = string
    let context_ = "RoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RoleArn" j
    let to_json = simple_to_json to_value
  end
module TargetAccountConfigurationDescription =
  struct
    type nonrec t = string
    let context_ = "TargetAccountConfigurationDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"TargetAccountConfigurationDescription" j
    let to_json = simple_to_json to_value
  end
module TargetAccountId =
  struct
    type nonrec t = string
    let context_ = "TargetAccountId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:12) >>=
             (fun () ->
                (check_string_max i ~max:48) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TargetAccountId" j
    let to_json = simple_to_json to_value
  end
module CreationTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ExperimentId =
  struct
    type nonrec t = string
    let context_ = "ExperimentId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentId" j
    let to_json = simple_to_json to_value
  end
module ExperimentOptions =
  struct
    type nonrec t =
      {
      accountTargeting: AccountTargeting.t option
        [@ocaml.doc "The account targeting setting for an experiment."];
      emptyTargetResolutionMode: EmptyTargetResolutionMode.t option
        [@ocaml.doc "The empty target resolution mode for an experiment."];
      actionsMode: ActionsMode.t option
        [@ocaml.doc
          "The actions mode of the experiment that is set from the StartExperiment API command."]}
    let make ?accountTargeting =
      fun ?emptyTargetResolutionMode ->
        fun ?actionsMode ->
          fun () ->
            { accountTargeting; emptyTargetResolutionMode; actionsMode }
    let to_value x =
      structure_to_value
        [("accountTargeting",
           (Option.map x.accountTargeting ~f:AccountTargeting.to_value));
        ("emptyTargetResolutionMode",
          (Option.map x.emptyTargetResolutionMode
             ~f:EmptyTargetResolutionMode.to_value));
        ("actionsMode", (Option.map x.actionsMode ~f:ActionsMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let actionsMode =
        (Option.map ~f:ActionsMode.of_xml) (Xml.child xml_arg0 "actionsMode") in
      let emptyTargetResolutionMode =
        (Option.map ~f:EmptyTargetResolutionMode.of_xml)
          (Xml.child xml_arg0 "emptyTargetResolutionMode") in
      let accountTargeting =
        (Option.map ~f:AccountTargeting.of_xml)
          (Xml.child xml_arg0 "accountTargeting") in
      make ?actionsMode ?emptyTargetResolutionMode ?accountTargeting ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let actionsMode = field_map json__ "actionsMode" ActionsMode.of_json in
      let emptyTargetResolutionMode =
        field_map json__ "emptyTargetResolutionMode"
          EmptyTargetResolutionMode.of_json in
      let accountTargeting =
        field_map json__ "accountTargeting" AccountTargeting.of_json in
      make ?actionsMode ?emptyTargetResolutionMode ?accountTargeting ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the options for an experiment."]
module ExperimentState =
  struct
    type nonrec t =
      {
      status: ExperimentStatus.t option
        [@ocaml.doc "The state of the experiment."];
      reason: ExperimentStatusReason.t option
        [@ocaml.doc "The reason for the state."];
      error: ExperimentError.t option
        [@ocaml.doc
          "The error information of the experiment when the action has failed."]}
    let make ?status =
      fun ?reason -> fun ?error -> fun () -> { status; reason; error }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:ExperimentStatus.to_value));
        ("reason", (Option.map x.reason ~f:ExperimentStatusReason.to_value));
        ("error", (Option.map x.error ~f:ExperimentError.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let error =
        (Option.map ~f:ExperimentError.of_xml) (Xml.child xml_arg0 "error") in
      let reason =
        (Option.map ~f:ExperimentStatusReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let status =
        (Option.map ~f:ExperimentStatus.of_xml) (Xml.child xml_arg0 "status") in
      make ?error ?reason ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let error = field_map json__ "error" ExperimentError.of_json in
      let reason = field_map json__ "reason" ExperimentStatusReason.of_json in
      let status = field_map json__ "status" ExperimentStatus.of_json in
      make ?error ?reason ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the state of an experiment."]
module ExperimentTemplateId =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTemplateId" j
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateDescription =
  struct
    type nonrec t = string
    let context_ = "ExperimentTemplateDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExperimentTemplateDescription" j
    let to_json = simple_to_json to_value
  end
module LastUpdateTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module TargetInformationMap =
  struct
    type nonrec t = (TargetInformationKey.t * TargetInformationValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TargetInformationKey.of_string chopped),
                              (TargetInformationValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TargetInformationKey.to_value x) |>
                    (fun x ->
                       (TargetInformationValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TargetInformationKey.of_string
        ~of_json:TargetInformationValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module TargetName =
  struct
    type nonrec t = string
    let context_ = "TargetName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TargetName" j
    let to_json = simple_to_json to_value
  end
module ActionDescription =
  struct
    type nonrec t = string
    let context_ = "ActionDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionDescription" j
    let to_json = simple_to_json to_value
  end
module ActionTargetMap =
  struct
    type nonrec t = (ActionTargetName.t * ActionTarget.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ActionTargetName ActionTarget"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ActionTargetName.to_value x) |>
                    (fun x -> (ActionTarget.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ActionTargetName.of_string
        ~of_json:ActionTarget.of_json j
    let to_json v = composed_to_json to_value v
  end
module TargetResourceTypeParameter =
  struct
    type nonrec t =
      {
      description: TargetResourceTypeParameterDescription.t option
        [@ocaml.doc "A description of the parameter."];
      required: TargetResourceTypeParameterRequired.t option
        [@ocaml.doc "Indicates whether the parameter is required."]}
    let make ?description =
      fun ?required -> fun () -> { description; required }
    let to_value x =
      structure_to_value
        [("description",
           (Option.map x.description
              ~f:TargetResourceTypeParameterDescription.to_value));
        ("required",
          (Option.map x.required
             ~f:TargetResourceTypeParameterRequired.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let required =
        (Option.map ~f:TargetResourceTypeParameterRequired.of_xml)
          (Xml.child xml_arg0 "required") in
      let description =
        (Option.map ~f:TargetResourceTypeParameterDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      make ?required ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let required =
        field_map json__ "required"
          TargetResourceTypeParameterRequired.of_json in
      let description =
        field_map json__ "description"
          TargetResourceTypeParameterDescription.of_json in
      make ?required ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the parameters for a resource type. Use parameters to determine which tasks are identified during target resolution."]
module TargetResourceTypeParameterName =
  struct
    type nonrec t = string
    let context_ = "TargetResourceTypeParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TargetResourceTypeParameterName" j
    let to_json = simple_to_json to_value
  end
module ActionParameter =
  struct
    type nonrec t =
      {
      description: ActionParameterDescription.t option
        [@ocaml.doc "The parameter description."];
      required: ActionParameterRequired.t option
        [@ocaml.doc "Indicates whether the parameter is required."]}
    let make ?description =
      fun ?required -> fun () -> { description; required }
    let to_value x =
      structure_to_value
        [("description",
           (Option.map x.description ~f:ActionParameterDescription.to_value));
        ("required",
          (Option.map x.required ~f:ActionParameterRequired.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let required =
        (Option.map ~f:ActionParameterRequired.of_xml)
          (Xml.child xml_arg0 "required") in
      let description =
        (Option.map ~f:ActionParameterDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      make ?required ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let required =
        field_map json__ "required" ActionParameterRequired.of_json in
      let description =
        field_map json__ "description" ActionParameterDescription.of_json in
      make ?required ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a parameter for an action."]
module ActionParameterName =
  struct
    type nonrec t = string
    let context_ = "ActionParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionParameterName" j
    let to_json = simple_to_json to_value
  end
module ExceptionMessage =
  struct
    type nonrec t = string
    let context_ = "ExceptionMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_pattern i ~pattern:"[\\s\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionMessage" j
    let to_json = simple_to_json to_value
  end
module SafetyLeverId =
  struct
    type nonrec t = string
    let context_ = "SafetyLeverId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"[\\S]+"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SafetyLeverId" j
    let to_json = simple_to_json to_value
  end
module SafetyLeverState =
  struct
    type nonrec t =
      {
      status: SafetyLeverStatus.t option
        [@ocaml.doc "The state of the safety lever."];
      reason: SafetyLeverStatusReason.t option
        [@ocaml.doc "The reason for the state of the safety lever."]}
    let make ?status = fun ?reason -> fun () -> { status; reason }
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:SafetyLeverStatus.to_value));
        ("reason", (Option.map x.reason ~f:SafetyLeverStatusReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:SafetyLeverStatusReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let status =
        (Option.map ~f:SafetyLeverStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?reason ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason = field_map json__ "reason" SafetyLeverStatusReason.of_json in
      let status = field_map json__ "status" SafetyLeverStatus.of_json in
      make ?reason ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the state of the safety lever."]
module SafetyLeverStatusInput =
  struct
    type nonrec t =
      | Disengaged 
      | Engaged 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Disengaged -> "disengaged"
      | Engaged -> "engaged"
      | Non_static_id s -> s
    let of_string =
      function
      | "disengaged" -> Disengaged
      | "engaged" -> Engaged
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SafetyLeverStatusInput" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SafetyLeverStatusInput" j)
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateActionMap =
  struct
    type nonrec t =
      (ExperimentTemplateActionName.t * ExperimentTemplateAction.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExperimentTemplateActionName ExperimentTemplateAction"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateActionName.to_value x) |>
                    (fun x ->
                       (ExperimentTemplateAction.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentTemplateActionName.of_string
        ~of_json:ExperimentTemplateAction.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTemplateExperimentOptions =
  struct
    type nonrec t =
      {
      accountTargeting: AccountTargeting.t option
        [@ocaml.doc
          "The account targeting setting for an experiment template."];
      emptyTargetResolutionMode: EmptyTargetResolutionMode.t option
        [@ocaml.doc
          "The empty target resolution mode for an experiment template."]}
    let make ?accountTargeting =
      fun ?emptyTargetResolutionMode ->
        fun () -> { accountTargeting; emptyTargetResolutionMode }
    let to_value x =
      structure_to_value
        [("accountTargeting",
           (Option.map x.accountTargeting ~f:AccountTargeting.to_value));
        ("emptyTargetResolutionMode",
          (Option.map x.emptyTargetResolutionMode
             ~f:EmptyTargetResolutionMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emptyTargetResolutionMode =
        (Option.map ~f:EmptyTargetResolutionMode.of_xml)
          (Xml.child xml_arg0 "emptyTargetResolutionMode") in
      let accountTargeting =
        (Option.map ~f:AccountTargeting.of_xml)
          (Xml.child xml_arg0 "accountTargeting") in
      make ?emptyTargetResolutionMode ?accountTargeting ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let emptyTargetResolutionMode =
        field_map json__ "emptyTargetResolutionMode"
          EmptyTargetResolutionMode.of_json in
      let accountTargeting =
        field_map json__ "accountTargeting" AccountTargeting.of_json in
      make ?emptyTargetResolutionMode ?accountTargeting ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the experiment options for an experiment template."]
module ExperimentTemplateLogConfiguration =
  struct
    type nonrec t =
      {
      cloudWatchLogsConfiguration:
        ExperimentTemplateCloudWatchLogsLogConfiguration.t option
        [@ocaml.doc
          "The configuration for experiment logging to Amazon CloudWatch Logs."];
      s3Configuration: ExperimentTemplateS3LogConfiguration.t option
        [@ocaml.doc "The configuration for experiment logging to Amazon S3."];
      logSchemaVersion: LogSchemaVersion.t option
        [@ocaml.doc "The schema version."]}
    let make ?cloudWatchLogsConfiguration =
      fun ?s3Configuration ->
        fun ?logSchemaVersion ->
          fun () ->
            { cloudWatchLogsConfiguration; s3Configuration; logSchemaVersion
            }
    let to_value x =
      structure_to_value
        [("cloudWatchLogsConfiguration",
           (Option.map x.cloudWatchLogsConfiguration
              ~f:ExperimentTemplateCloudWatchLogsLogConfiguration.to_value));
        ("s3Configuration",
          (Option.map x.s3Configuration
             ~f:ExperimentTemplateS3LogConfiguration.to_value));
        ("logSchemaVersion",
          (Option.map x.logSchemaVersion ~f:LogSchemaVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logSchemaVersion =
        (Option.map ~f:LogSchemaVersion.of_xml)
          (Xml.child xml_arg0 "logSchemaVersion") in
      let s3Configuration =
        (Option.map ~f:ExperimentTemplateS3LogConfiguration.of_xml)
          (Xml.child xml_arg0 "s3Configuration") in
      let cloudWatchLogsConfiguration =
        (Option.map
           ~f:ExperimentTemplateCloudWatchLogsLogConfiguration.of_xml)
          (Xml.child xml_arg0 "cloudWatchLogsConfiguration") in
      make ?logSchemaVersion ?s3Configuration ?cloudWatchLogsConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logSchemaVersion =
        field_map json__ "logSchemaVersion" LogSchemaVersion.of_json in
      let s3Configuration =
        field_map json__ "s3Configuration"
          ExperimentTemplateS3LogConfiguration.of_json in
      let cloudWatchLogsConfiguration =
        field_map json__ "cloudWatchLogsConfiguration"
          ExperimentTemplateCloudWatchLogsLogConfiguration.of_json in
      make ?logSchemaVersion ?s3Configuration ?cloudWatchLogsConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the configuration for experiment logging."]
module ExperimentTemplateReportConfiguration =
  struct
    type nonrec t =
      {
      outputs: ExperimentTemplateReportConfigurationOutputs.t option
        [@ocaml.doc
          "Describes the output destinations of the experiment report."];
      dataSources: ExperimentTemplateReportConfigurationDataSources.t option
        [@ocaml.doc "The data sources for the experiment report."];
      preExperimentDuration: ReportConfigurationDuration.t option
        [@ocaml.doc
          "The duration before the experiment start time for the data sources to include in the report."];
      postExperimentDuration: ReportConfigurationDuration.t option
        [@ocaml.doc
          "The duration after the experiment end time for the data sources to include in the report."]}
    let make ?outputs =
      fun ?dataSources ->
        fun ?preExperimentDuration ->
          fun ?postExperimentDuration ->
            fun () ->
              {
                outputs;
                dataSources;
                preExperimentDuration;
                postExperimentDuration
              }
    let to_value x =
      structure_to_value
        [("outputs",
           (Option.map x.outputs
              ~f:ExperimentTemplateReportConfigurationOutputs.to_value));
        ("dataSources",
          (Option.map x.dataSources
             ~f:ExperimentTemplateReportConfigurationDataSources.to_value));
        ("preExperimentDuration",
          (Option.map x.preExperimentDuration
             ~f:ReportConfigurationDuration.to_value));
        ("postExperimentDuration",
          (Option.map x.postExperimentDuration
             ~f:ReportConfigurationDuration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let postExperimentDuration =
        (Option.map ~f:ReportConfigurationDuration.of_xml)
          (Xml.child xml_arg0 "postExperimentDuration") in
      let preExperimentDuration =
        (Option.map ~f:ReportConfigurationDuration.of_xml)
          (Xml.child xml_arg0 "preExperimentDuration") in
      let dataSources =
        (Option.map
           ~f:ExperimentTemplateReportConfigurationDataSources.of_xml)
          (Xml.child xml_arg0 "dataSources") in
      let outputs =
        (Option.map ~f:ExperimentTemplateReportConfigurationOutputs.of_xml)
          (Xml.child xml_arg0 "outputs") in
      make ?postExperimentDuration ?preExperimentDuration ?dataSources
        ?outputs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let postExperimentDuration =
        field_map json__ "postExperimentDuration"
          ReportConfigurationDuration.of_json in
      let preExperimentDuration =
        field_map json__ "preExperimentDuration"
          ReportConfigurationDuration.of_json in
      let dataSources =
        field_map json__ "dataSources"
          ExperimentTemplateReportConfigurationDataSources.of_json in
      let outputs =
        field_map json__ "outputs"
          ExperimentTemplateReportConfigurationOutputs.of_json in
      make ?postExperimentDuration ?preExperimentDuration ?dataSources
        ?outputs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the experiment report configuration. For more information, see Experiment report configurations for AWS FIS."]
module ExperimentTemplateStopConditionList =
  struct
    type nonrec t = ExperimentTemplateStopCondition.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentTemplateStopCondition.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentTemplateStopCondition.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTemplateStopConditionList"
        ~of_json:ExperimentTemplateStopCondition.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTemplateTargetMap =
  struct
    type nonrec t =
      (ExperimentTemplateTargetName.t * ExperimentTemplateTarget.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExperimentTemplateTargetName ExperimentTemplateTarget"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateTargetName.to_value x) |>
                    (fun x ->
                       (ExperimentTemplateTarget.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentTemplateTargetName.of_string
        ~of_json:ExperimentTemplateTarget.of_json j
    let to_json v = composed_to_json to_value v
  end
module TargetAccountConfigurationsCount =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:0L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module UpdateExperimentTemplateActionInputItem =
  struct
    type nonrec t =
      {
      actionId: ActionId.t option [@ocaml.doc "The ID of the action."];
      description: ExperimentTemplateActionDescription.t option
        [@ocaml.doc "A description for the action."];
      parameters: ExperimentTemplateActionParameterMap.t option
        [@ocaml.doc "The parameters for the action, if applicable."];
      targets: ExperimentTemplateActionTargetMap.t option
        [@ocaml.doc "The targets for the action."];
      startAfter: ExperimentTemplateActionStartAfterList.t option
        [@ocaml.doc
          "The name of the action that must be completed before the current action starts. Omit this parameter to run the action at the start of the experiment."]}
    let make ?actionId =
      fun ?description ->
        fun ?parameters ->
          fun ?targets ->
            fun ?startAfter ->
              fun () ->
                { actionId; description; parameters; targets; startAfter }
    let to_value x =
      structure_to_value
        [("actionId", (Option.map x.actionId ~f:ActionId.to_value));
        ("description",
          (Option.map x.description
             ~f:ExperimentTemplateActionDescription.to_value));
        ("parameters",
          (Option.map x.parameters
             ~f:ExperimentTemplateActionParameterMap.to_value));
        ("targets",
          (Option.map x.targets ~f:ExperimentTemplateActionTargetMap.to_value));
        ("startAfter",
          (Option.map x.startAfter
             ~f:ExperimentTemplateActionStartAfterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startAfter =
        (Option.map ~f:ExperimentTemplateActionStartAfterList.of_xml)
          (Xml.child xml_arg0 "startAfter") in
      let targets =
        (Option.map ~f:ExperimentTemplateActionTargetMap.of_xml)
          (Xml.child xml_arg0 "targets") in
      let parameters =
        (Option.map ~f:ExperimentTemplateActionParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let description =
        (Option.map ~f:ExperimentTemplateActionDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let actionId =
        (Option.map ~f:ActionId.of_xml) (Xml.child xml_arg0 "actionId") in
      make ?startAfter ?targets ?parameters ?description ?actionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let startAfter =
        field_map json__ "startAfter"
          ExperimentTemplateActionStartAfterList.of_json in
      let targets =
        field_map json__ "targets" ExperimentTemplateActionTargetMap.of_json in
      let parameters =
        field_map json__ "parameters"
          ExperimentTemplateActionParameterMap.of_json in
      let description =
        field_map json__ "description"
          ExperimentTemplateActionDescription.of_json in
      let actionId = field_map json__ "actionId" ActionId.of_json in
      make ?startAfter ?targets ?parameters ?description ?actionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies an action for an experiment template."]
module ExperimentTemplateCloudWatchLogsLogConfigurationInput =
  struct
    type nonrec t =
      {
      logGroupArn: CloudWatchLogGroupArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the destination Amazon CloudWatch Logs log group."]}
    let context_ = "ExperimentTemplateCloudWatchLogsLogConfigurationInput"
    let make ~logGroupArn = fun () -> { logGroupArn }
    let to_value x =
      structure_to_value
        [("logGroupArn",
           (Some (CloudWatchLogGroupArn.to_value x.logGroupArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logGroupArn =
        CloudWatchLogGroupArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "logGroupArn") in
      make ~logGroupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logGroupArn =
        field_map_exn json__ "logGroupArn" CloudWatchLogGroupArn.of_json in
      make ~logGroupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the configuration for experiment logging to Amazon CloudWatch Logs."]
module ExperimentTemplateS3LogConfigurationInput =
  struct
    type nonrec t =
      {
      bucketName: S3BucketName.t
        [@ocaml.doc "The name of the destination bucket."];
      prefix: S3ObjectKey.t option [@ocaml.doc "The bucket prefix."]}
    let context_ = "ExperimentTemplateS3LogConfigurationInput"
    let make ?prefix = fun ~bucketName -> fun () -> { prefix; bucketName }
    let to_value x =
      structure_to_value
        [("bucketName", (Some (S3BucketName.to_value x.bucketName)));
        ("prefix", (Option.map x.prefix ~f:S3ObjectKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:S3ObjectKey.of_xml) (Xml.child xml_arg0 "prefix") in
      let bucketName =
        S3BucketName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "bucketName") in
      make ?prefix ~bucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let prefix = field_map json__ "prefix" S3ObjectKey.of_json in
      let bucketName = field_map_exn json__ "bucketName" S3BucketName.of_json in
      make ?prefix ~bucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the configuration for experiment logging to Amazon S3."]
module ExperimentTemplateReportConfigurationDataSourcesInput =
  struct
    type nonrec t =
      {
      cloudWatchDashboards:
        ReportConfigurationCloudWatchDashboardInputList.t option
        [@ocaml.doc
          "The CloudWatch dashboards to include as data sources in the experiment report."]}
    let make ?cloudWatchDashboards = fun () -> { cloudWatchDashboards }
    let to_value x =
      structure_to_value
        [("cloudWatchDashboards",
           (Option.map x.cloudWatchDashboards
              ~f:ReportConfigurationCloudWatchDashboardInputList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cloudWatchDashboards =
        (Option.map ~f:ReportConfigurationCloudWatchDashboardInputList.of_xml)
          (Xml.child xml_arg0 "cloudWatchDashboards") in
      make ?cloudWatchDashboards ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cloudWatchDashboards =
        field_map json__ "cloudWatchDashboards"
          ReportConfigurationCloudWatchDashboardInputList.of_json in
      make ?cloudWatchDashboards ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the data sources for the experiment report."]
module ExperimentTemplateReportConfigurationOutputsInput =
  struct
    type nonrec t =
      {
      s3Configuration: ReportConfigurationS3OutputInput.t option
        [@ocaml.doc "The S3 destination for the experiment report."]}
    let make ?s3Configuration = fun () -> { s3Configuration }
    let to_value x =
      structure_to_value
        [("s3Configuration",
           (Option.map x.s3Configuration
              ~f:ReportConfigurationS3OutputInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Configuration =
        (Option.map ~f:ReportConfigurationS3OutputInput.of_xml)
          (Xml.child xml_arg0 "s3Configuration") in
      make ?s3Configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Configuration =
        field_map json__ "s3Configuration"
          ReportConfigurationS3OutputInput.of_json in
      make ?s3Configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the outputs for the experiment templates."]
module UpdateExperimentTemplateStopConditionInput =
  struct
    type nonrec t =
      {
      source: StopConditionSource.t
        [@ocaml.doc
          "The source for the stop condition. Specify aws:cloudwatch:alarm if the stop condition is defined by a CloudWatch alarm. Specify none if there is no stop condition."];
      value: StopConditionValue.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the CloudWatch alarm."]}
    let context_ = "UpdateExperimentTemplateStopConditionInput"
    let make ?value = fun ~source -> fun () -> { value; source }
    let to_value x =
      structure_to_value
        [("source", (Some (StopConditionSource.to_value x.source)));
        ("value", (Option.map x.value ~f:StopConditionValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:StopConditionValue.of_xml)
          (Xml.child xml_arg0 "value") in
      let source =
        StopConditionSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      make ?value ~source ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" StopConditionValue.of_json in
      let source = field_map_exn json__ "source" StopConditionSource.of_json in
      make ?value ~source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a stop condition for an experiment. You can define a stop condition as a CloudWatch alarm."]
module UpdateExperimentTemplateTargetInput =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t
        [@ocaml.doc
          "The resource type. The resource type must be supported for the specified action."];
      resourceArns: ResourceArnList.t option
        [@ocaml.doc "The Amazon Resource Names (ARNs) of the targets."];
      resourceTags: TagMap.t option
        [@ocaml.doc "The tags for the target resources."];
      filters: ExperimentTemplateTargetFilterInputList.t option
        [@ocaml.doc
          "The filters to apply to identify target resources using specific attributes."];
      selectionMode: ExperimentTemplateTargetSelectionMode.t
        [@ocaml.doc
          "Scopes the identified resources to a specific count or percentage."];
      parameters: ExperimentTemplateTargetParameterMap.t option
        [@ocaml.doc "The resource type parameters."]}
    let context_ = "UpdateExperimentTemplateTargetInput"
    let make ?resourceArns =
      fun ?resourceTags ->
        fun ?filters ->
          fun ?parameters ->
            fun ~resourceType ->
              fun ~selectionMode ->
                fun () ->
                  {
                    resourceArns;
                    resourceTags;
                    filters;
                    parameters;
                    resourceType;
                    selectionMode
                  }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Some (TargetResourceTypeId.to_value x.resourceType)));
        ("resourceArns",
          (Option.map x.resourceArns ~f:ResourceArnList.to_value));
        ("resourceTags", (Option.map x.resourceTags ~f:TagMap.to_value));
        ("filters",
          (Option.map x.filters
             ~f:ExperimentTemplateTargetFilterInputList.to_value));
        ("selectionMode",
          (Some
             (ExperimentTemplateTargetSelectionMode.to_value x.selectionMode)));
        ("parameters",
          (Option.map x.parameters
             ~f:ExperimentTemplateTargetParameterMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:ExperimentTemplateTargetParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let selectionMode =
        ExperimentTemplateTargetSelectionMode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "selectionMode") in
      let filters =
        (Option.map ~f:ExperimentTemplateTargetFilterInputList.of_xml)
          (Xml.child xml_arg0 "filters") in
      let resourceTags =
        (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "resourceTags") in
      let resourceArns =
        (Option.map ~f:ResourceArnList.of_xml)
          (Xml.child xml_arg0 "resourceArns") in
      let resourceType =
        TargetResourceTypeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceType") in
      make ?parameters ~selectionMode ?filters ?resourceTags ?resourceArns
        ~resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parameters =
        field_map json__ "parameters"
          ExperimentTemplateTargetParameterMap.of_json in
      let selectionMode =
        field_map_exn json__ "selectionMode"
          ExperimentTemplateTargetSelectionMode.of_json in
      let filters =
        field_map json__ "filters"
          ExperimentTemplateTargetFilterInputList.of_json in
      let resourceTags = field_map json__ "resourceTags" TagMap.of_json in
      let resourceArns =
        field_map json__ "resourceArns" ResourceArnList.of_json in
      let resourceType =
        field_map_exn json__ "resourceType" TargetResourceTypeId.of_json in
      make ?parameters ~selectionMode ?filters ?resourceTags ?resourceArns
        ~resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a target for an experiment. You must specify at least one Amazon Resource Name (ARN) or at least one resource tag. You cannot specify both."]
module ExperimentActionMap =
  struct
    type nonrec t = (ExperimentActionName.t * ExperimentAction.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExperimentActionName ExperimentAction"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentActionName.to_value x) |>
                    (fun x ->
                       (ExperimentAction.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentActionName.of_string
        ~of_json:ExperimentAction.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentEndTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ExperimentLogConfiguration =
  struct
    type nonrec t =
      {
      cloudWatchLogsConfiguration:
        ExperimentCloudWatchLogsLogConfiguration.t option
        [@ocaml.doc
          "The configuration for experiment logging to Amazon CloudWatch Logs."];
      s3Configuration: ExperimentS3LogConfiguration.t option
        [@ocaml.doc "The configuration for experiment logging to Amazon S3."];
      logSchemaVersion: LogSchemaVersion.t option
        [@ocaml.doc "The schema version."]}
    let make ?cloudWatchLogsConfiguration =
      fun ?s3Configuration ->
        fun ?logSchemaVersion ->
          fun () ->
            { cloudWatchLogsConfiguration; s3Configuration; logSchemaVersion
            }
    let to_value x =
      structure_to_value
        [("cloudWatchLogsConfiguration",
           (Option.map x.cloudWatchLogsConfiguration
              ~f:ExperimentCloudWatchLogsLogConfiguration.to_value));
        ("s3Configuration",
          (Option.map x.s3Configuration
             ~f:ExperimentS3LogConfiguration.to_value));
        ("logSchemaVersion",
          (Option.map x.logSchemaVersion ~f:LogSchemaVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logSchemaVersion =
        (Option.map ~f:LogSchemaVersion.of_xml)
          (Xml.child xml_arg0 "logSchemaVersion") in
      let s3Configuration =
        (Option.map ~f:ExperimentS3LogConfiguration.of_xml)
          (Xml.child xml_arg0 "s3Configuration") in
      let cloudWatchLogsConfiguration =
        (Option.map ~f:ExperimentCloudWatchLogsLogConfiguration.of_xml)
          (Xml.child xml_arg0 "cloudWatchLogsConfiguration") in
      make ?logSchemaVersion ?s3Configuration ?cloudWatchLogsConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logSchemaVersion =
        field_map json__ "logSchemaVersion" LogSchemaVersion.of_json in
      let s3Configuration =
        field_map json__ "s3Configuration"
          ExperimentS3LogConfiguration.of_json in
      let cloudWatchLogsConfiguration =
        field_map json__ "cloudWatchLogsConfiguration"
          ExperimentCloudWatchLogsLogConfiguration.of_json in
      make ?logSchemaVersion ?s3Configuration ?cloudWatchLogsConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the configuration for experiment logging."]
module ExperimentReport =
  struct
    type nonrec t =
      {
      state: ExperimentReportState.t option
        [@ocaml.doc "The state of the experiment report."];
      s3Reports: ExperimentReportS3ReportList.t option
        [@ocaml.doc "The S3 destination of the experiment report."]}
    let make ?state = fun ?s3Reports -> fun () -> { state; s3Reports }
    let to_value x =
      structure_to_value
        [("state", (Option.map x.state ~f:ExperimentReportState.to_value));
        ("s3Reports",
          (Option.map x.s3Reports ~f:ExperimentReportS3ReportList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Reports =
        (Option.map ~f:ExperimentReportS3ReportList.of_xml)
          (Xml.child xml_arg0 "s3Reports") in
      let state =
        (Option.map ~f:ExperimentReportState.of_xml)
          (Xml.child xml_arg0 "state") in
      make ?s3Reports ?state ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Reports =
        field_map json__ "s3Reports" ExperimentReportS3ReportList.of_json in
      let state = field_map json__ "state" ExperimentReportState.of_json in
      make ?s3Reports ?state ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the experiment report."]
module ExperimentReportConfiguration =
  struct
    type nonrec t =
      {
      outputs: ExperimentReportConfigurationOutputs.t option
        [@ocaml.doc "The output destinations of the experiment report."];
      dataSources: ExperimentReportConfigurationDataSources.t option
        [@ocaml.doc "The data sources for the experiment report."];
      preExperimentDuration: ReportConfigurationDuration.t option
        [@ocaml.doc
          "The duration before the experiment start time for the data sources to include in the report."];
      postExperimentDuration: ReportConfigurationDuration.t option
        [@ocaml.doc
          "The duration after the experiment end time for the data sources to include in the report."]}
    let make ?outputs =
      fun ?dataSources ->
        fun ?preExperimentDuration ->
          fun ?postExperimentDuration ->
            fun () ->
              {
                outputs;
                dataSources;
                preExperimentDuration;
                postExperimentDuration
              }
    let to_value x =
      structure_to_value
        [("outputs",
           (Option.map x.outputs
              ~f:ExperimentReportConfigurationOutputs.to_value));
        ("dataSources",
          (Option.map x.dataSources
             ~f:ExperimentReportConfigurationDataSources.to_value));
        ("preExperimentDuration",
          (Option.map x.preExperimentDuration
             ~f:ReportConfigurationDuration.to_value));
        ("postExperimentDuration",
          (Option.map x.postExperimentDuration
             ~f:ReportConfigurationDuration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let postExperimentDuration =
        (Option.map ~f:ReportConfigurationDuration.of_xml)
          (Xml.child xml_arg0 "postExperimentDuration") in
      let preExperimentDuration =
        (Option.map ~f:ReportConfigurationDuration.of_xml)
          (Xml.child xml_arg0 "preExperimentDuration") in
      let dataSources =
        (Option.map ~f:ExperimentReportConfigurationDataSources.of_xml)
          (Xml.child xml_arg0 "dataSources") in
      let outputs =
        (Option.map ~f:ExperimentReportConfigurationOutputs.of_xml)
          (Xml.child xml_arg0 "outputs") in
      make ?postExperimentDuration ?preExperimentDuration ?dataSources
        ?outputs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let postExperimentDuration =
        field_map json__ "postExperimentDuration"
          ReportConfigurationDuration.of_json in
      let preExperimentDuration =
        field_map json__ "preExperimentDuration"
          ReportConfigurationDuration.of_json in
      let dataSources =
        field_map json__ "dataSources"
          ExperimentReportConfigurationDataSources.of_json in
      let outputs =
        field_map json__ "outputs"
          ExperimentReportConfigurationOutputs.of_json in
      make ?postExperimentDuration ?preExperimentDuration ?dataSources
        ?outputs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the report configuration for the experiment. For more information, see Experiment report configurations for AWS FIS."]
module ExperimentStartTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ExperimentStopConditionList =
  struct
    type nonrec t = ExperimentStopCondition.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentStopCondition.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentStopCondition.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentStopConditionList"
        ~of_json:ExperimentStopCondition.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExperimentTargetMap =
  struct
    type nonrec t = (ExperimentTargetName.t * ExperimentTarget.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExperimentTargetName ExperimentTarget"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTargetName.to_value x) |>
                    (fun x ->
                       (ExperimentTarget.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentTargetName.of_string
        ~of_json:ExperimentTarget.of_json j
    let to_json v = composed_to_json to_value v
  end
module TargetResourceTypeSummary =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t option
        [@ocaml.doc "The resource type."];
      description: TargetResourceTypeDescription.t option
        [@ocaml.doc "A description of the resource type."]}
    let make ?resourceType =
      fun ?description -> fun () -> { resourceType; description }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Option.map x.resourceType ~f:TargetResourceTypeId.to_value));
        ("description",
          (Option.map x.description ~f:TargetResourceTypeDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:TargetResourceTypeDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let resourceType =
        (Option.map ~f:TargetResourceTypeId.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      make ?description ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description" TargetResourceTypeDescription.of_json in
      let resourceType =
        field_map json__ "resourceType" TargetResourceTypeId.of_json in
      make ?description ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a resource type."]
module TargetAccountConfigurationSummary =
  struct
    type nonrec t =
      {
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role for the target account."];
      accountId: TargetAccountId.t option
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."];
      description: TargetAccountConfigurationDescription.t option
        [@ocaml.doc "The description of the target account."]}
    let make ?roleArn =
      fun ?accountId ->
        fun ?description -> fun () -> { roleArn; accountId; description }
    let to_value x =
      structure_to_value
        [("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("accountId", (Option.map x.accountId ~f:TargetAccountId.to_value));
        ("description",
          (Option.map x.description
             ~f:TargetAccountConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:TargetAccountConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let accountId =
        (Option.map ~f:TargetAccountId.of_xml)
          (Xml.child xml_arg0 "accountId") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      make ?description ?accountId ?roleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description"
          TargetAccountConfigurationDescription.of_json in
      let accountId = field_map json__ "accountId" TargetAccountId.of_json in
      let roleArn = field_map json__ "roleArn" RoleArn.of_json in
      make ?description ?accountId ?roleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provides a summary of a target account configuration."]
module ExperimentSummary =
  struct
    type nonrec t =
      {
      id: ExperimentId.t option [@ocaml.doc "The ID of the experiment."];
      arn: ResourceArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the experiment."];
      experimentTemplateId: ExperimentTemplateId.t option
        [@ocaml.doc "The ID of the experiment template."];
      state: ExperimentState.t option
        [@ocaml.doc "The state of the experiment."];
      creationTime: CreationTime.t option
        [@ocaml.doc "The time that the experiment was created."];
      tags: TagMap.t option [@ocaml.doc "The tags for the experiment."];
      experimentOptions: ExperimentOptions.t option
        [@ocaml.doc "The experiment options for the experiment."]}
    let make ?id =
      fun ?arn ->
        fun ?experimentTemplateId ->
          fun ?state ->
            fun ?creationTime ->
              fun ?tags ->
                fun ?experimentOptions ->
                  fun () ->
                    {
                      id;
                      arn;
                      experimentTemplateId;
                      state;
                      creationTime;
                      tags;
                      experimentOptions
                    }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ExperimentId.to_value));
        ("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("experimentTemplateId",
          (Option.map x.experimentTemplateId ~f:ExperimentTemplateId.to_value));
        ("state", (Option.map x.state ~f:ExperimentState.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:CreationTime.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("experimentOptions",
          (Option.map x.experimentOptions ~f:ExperimentOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentOptions =
        (Option.map ~f:ExperimentOptions.of_xml)
          (Xml.child xml_arg0 "experimentOptions") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let creationTime =
        (Option.map ~f:CreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let state =
        (Option.map ~f:ExperimentState.of_xml) (Xml.child xml_arg0 "state") in
      let experimentTemplateId =
        (Option.map ~f:ExperimentTemplateId.of_xml)
          (Xml.child xml_arg0 "experimentTemplateId") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      let id = (Option.map ~f:ExperimentId.of_xml) (Xml.child xml_arg0 "id") in
      make ?experimentOptions ?tags ?creationTime ?state
        ?experimentTemplateId ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentOptions =
        field_map json__ "experimentOptions" ExperimentOptions.of_json in
      let tags = field_map json__ "tags" TagMap.of_json in
      let creationTime = field_map json__ "creationTime" CreationTime.of_json in
      let state = field_map json__ "state" ExperimentState.of_json in
      let experimentTemplateId =
        field_map json__ "experimentTemplateId" ExperimentTemplateId.of_json in
      let arn = field_map json__ "arn" ResourceArn.of_json in
      let id = field_map json__ "id" ExperimentId.of_json in
      make ?experimentOptions ?tags ?creationTime ?state
        ?experimentTemplateId ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provides a summary of an experiment."]
module ExperimentTemplateSummary =
  struct
    type nonrec t =
      {
      id: ExperimentTemplateId.t option
        [@ocaml.doc "The ID of the experiment template."];
      arn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the experiment template."];
      description: ExperimentTemplateDescription.t option
        [@ocaml.doc "The description of the experiment template."];
      creationTime: CreationTime.t option
        [@ocaml.doc "The time that the experiment template was created."];
      lastUpdateTime: LastUpdateTime.t option
        [@ocaml.doc
          "The time that the experiment template was last updated."];
      tags: TagMap.t option
        [@ocaml.doc "The tags for the experiment template."]}
    let make ?id =
      fun ?arn ->
        fun ?description ->
          fun ?creationTime ->
            fun ?lastUpdateTime ->
              fun ?tags ->
                fun () ->
                  { id; arn; description; creationTime; lastUpdateTime; tags
                  }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ExperimentTemplateId.to_value));
        ("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("description",
          (Option.map x.description ~f:ExperimentTemplateDescription.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:CreationTime.to_value));
        ("lastUpdateTime",
          (Option.map x.lastUpdateTime ~f:LastUpdateTime.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let lastUpdateTime =
        (Option.map ~f:LastUpdateTime.of_xml)
          (Xml.child xml_arg0 "lastUpdateTime") in
      let creationTime =
        (Option.map ~f:CreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let description =
        (Option.map ~f:ExperimentTemplateDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:ExperimentTemplateId.of_xml) (Xml.child xml_arg0 "id") in
      make ?tags ?lastUpdateTime ?creationTime ?description ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let lastUpdateTime =
        field_map json__ "lastUpdateTime" LastUpdateTime.of_json in
      let creationTime = field_map json__ "creationTime" CreationTime.of_json in
      let description =
        field_map json__ "description" ExperimentTemplateDescription.of_json in
      let arn = field_map json__ "arn" ResourceArn.of_json in
      let id = field_map json__ "id" ExperimentTemplateId.of_json in
      make ?tags ?lastUpdateTime ?creationTime ?description ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provides a summary of an experiment template."]
module ExperimentTargetAccountConfigurationSummary =
  struct
    type nonrec t =
      {
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role for the target account."];
      accountId: TargetAccountId.t option
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."];
      description: TargetAccountConfigurationDescription.t option
        [@ocaml.doc "The description of the target account."]}
    let make ?roleArn =
      fun ?accountId ->
        fun ?description -> fun () -> { roleArn; accountId; description }
    let to_value x =
      structure_to_value
        [("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("accountId", (Option.map x.accountId ~f:TargetAccountId.to_value));
        ("description",
          (Option.map x.description
             ~f:TargetAccountConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:TargetAccountConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let accountId =
        (Option.map ~f:TargetAccountId.of_xml)
          (Xml.child xml_arg0 "accountId") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      make ?description ?accountId ?roleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description"
          TargetAccountConfigurationDescription.of_json in
      let accountId = field_map json__ "accountId" TargetAccountId.of_json in
      let roleArn = field_map json__ "roleArn" RoleArn.of_json in
      make ?description ?accountId ?roleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provides a summary of a target account configuration."]
module ResolvedTarget =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t option
        [@ocaml.doc "The resource type of the target."];
      targetName: TargetName.t option [@ocaml.doc "The name of the target."];
      targetInformation: TargetInformationMap.t option
        [@ocaml.doc "Information about the target."]}
    let make ?resourceType =
      fun ?targetName ->
        fun ?targetInformation ->
          fun () -> { resourceType; targetName; targetInformation }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Option.map x.resourceType ~f:TargetResourceTypeId.to_value));
        ("targetName", (Option.map x.targetName ~f:TargetName.to_value));
        ("targetInformation",
          (Option.map x.targetInformation ~f:TargetInformationMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetInformation =
        (Option.map ~f:TargetInformationMap.of_xml)
          (Xml.child xml_arg0 "targetInformation") in
      let targetName =
        (Option.map ~f:TargetName.of_xml) (Xml.child xml_arg0 "targetName") in
      let resourceType =
        (Option.map ~f:TargetResourceTypeId.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      make ?targetInformation ?targetName ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetInformation =
        field_map json__ "targetInformation" TargetInformationMap.of_json in
      let targetName = field_map json__ "targetName" TargetName.of_json in
      let resourceType =
        field_map json__ "resourceType" TargetResourceTypeId.of_json in
      make ?targetInformation ?targetName ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a resolved target."]
module ActionSummary =
  struct
    type nonrec t =
      {
      id: ActionId.t option [@ocaml.doc "The ID of the action."];
      arn: ResourceArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the action."];
      description: ActionDescription.t option
        [@ocaml.doc "The description for the action."];
      targets: ActionTargetMap.t option
        [@ocaml.doc "The targets for the action."];
      tags: TagMap.t option [@ocaml.doc "The tags for the action."]}
    let make ?id =
      fun ?arn ->
        fun ?description ->
          fun ?targets ->
            fun ?tags -> fun () -> { id; arn; description; targets; tags }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ActionId.to_value));
        ("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("description",
          (Option.map x.description ~f:ActionDescription.to_value));
        ("targets", (Option.map x.targets ~f:ActionTargetMap.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let targets =
        (Option.map ~f:ActionTargetMap.of_xml) (Xml.child xml_arg0 "targets") in
      let description =
        (Option.map ~f:ActionDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      let id = (Option.map ~f:ActionId.of_xml) (Xml.child xml_arg0 "id") in
      make ?tags ?targets ?description ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let targets = field_map json__ "targets" ActionTargetMap.of_json in
      let description =
        field_map json__ "description" ActionDescription.of_json in
      let arn = field_map json__ "arn" ResourceArn.of_json in
      let id = field_map json__ "id" ActionId.of_json in
      make ?tags ?targets ?description ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Provides a summary of an action."]
module TargetResourceTypeParameterMap =
  struct
    type nonrec t =
      (TargetResourceTypeParameterName.t * TargetResourceTypeParameter.t)
        list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types TargetResourceTypeParameterName TargetResourceTypeParameter"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TargetResourceTypeParameterName.to_value x) |>
                    (fun x ->
                       (TargetResourceTypeParameter.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TargetResourceTypeParameterName.of_string
        ~of_json:TargetResourceTypeParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ActionParameterMap =
  struct
    type nonrec t = (ActionParameterName.t * ActionParameter.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ActionParameterName ActionParameter"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ActionParameterName.to_value x) |>
                    (fun x ->
                       (ActionParameter.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ActionParameterName.of_string
        ~of_json:ActionParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateExperimentTemplateActionInput =
  struct
    type nonrec t =
      {
      actionId: ActionId.t
        [@ocaml.doc
          "The ID of the action. The format of the action ID is: aws:service-name:action-type."];
      description: ExperimentTemplateActionDescription.t option
        [@ocaml.doc "A description for the action."];
      parameters: ExperimentTemplateActionParameterMap.t option
        [@ocaml.doc "The parameters for the action, if applicable."];
      targets: ExperimentTemplateActionTargetMap.t option
        [@ocaml.doc "The targets for the action."];
      startAfter: ExperimentTemplateActionStartAfterList.t option
        [@ocaml.doc
          "The name of the action that must be completed before the current action starts. Omit this parameter to run the action at the start of the experiment."]}
    let context_ = "CreateExperimentTemplateActionInput"
    let make ?description =
      fun ?parameters ->
        fun ?targets ->
          fun ?startAfter ->
            fun ~actionId ->
              fun () ->
                { description; parameters; targets; startAfter; actionId }
    let to_value x =
      structure_to_value
        [("actionId", (Some (ActionId.to_value x.actionId)));
        ("description",
          (Option.map x.description
             ~f:ExperimentTemplateActionDescription.to_value));
        ("parameters",
          (Option.map x.parameters
             ~f:ExperimentTemplateActionParameterMap.to_value));
        ("targets",
          (Option.map x.targets ~f:ExperimentTemplateActionTargetMap.to_value));
        ("startAfter",
          (Option.map x.startAfter
             ~f:ExperimentTemplateActionStartAfterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startAfter =
        (Option.map ~f:ExperimentTemplateActionStartAfterList.of_xml)
          (Xml.child xml_arg0 "startAfter") in
      let targets =
        (Option.map ~f:ExperimentTemplateActionTargetMap.of_xml)
          (Xml.child xml_arg0 "targets") in
      let parameters =
        (Option.map ~f:ExperimentTemplateActionParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let description =
        (Option.map ~f:ExperimentTemplateActionDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let actionId =
        ActionId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "actionId") in
      make ?startAfter ?targets ?parameters ?description ~actionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let startAfter =
        field_map json__ "startAfter"
          ExperimentTemplateActionStartAfterList.of_json in
      let targets =
        field_map json__ "targets" ExperimentTemplateActionTargetMap.of_json in
      let parameters =
        field_map json__ "parameters"
          ExperimentTemplateActionParameterMap.of_json in
      let description =
        field_map json__ "description"
          ExperimentTemplateActionDescription.of_json in
      let actionId = field_map_exn json__ "actionId" ActionId.of_json in
      make ?startAfter ?targets ?parameters ?description ~actionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies an action for an experiment template. For more information, see Actions in the Fault Injection Service User Guide."]
module CreateExperimentTemplateStopConditionInput =
  struct
    type nonrec t =
      {
      source: StopConditionSource.t
        [@ocaml.doc
          "The source for the stop condition. Specify aws:cloudwatch:alarm if the stop condition is defined by a CloudWatch alarm. Specify none if there is no stop condition."];
      value: StopConditionValue.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the CloudWatch alarm. This is required if the source is a CloudWatch alarm."]}
    let context_ = "CreateExperimentTemplateStopConditionInput"
    let make ?value = fun ~source -> fun () -> { value; source }
    let to_value x =
      structure_to_value
        [("source", (Some (StopConditionSource.to_value x.source)));
        ("value", (Option.map x.value ~f:StopConditionValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:StopConditionValue.of_xml)
          (Xml.child xml_arg0 "value") in
      let source =
        StopConditionSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      make ?value ~source ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" StopConditionValue.of_json in
      let source = field_map_exn json__ "source" StopConditionSource.of_json in
      make ?value ~source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies a stop condition for an experiment template."]
module CreateExperimentTemplateTargetInput =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t
        [@ocaml.doc
          "The resource type. The resource type must be supported for the specified action."];
      resourceArns: ResourceArnList.t option
        [@ocaml.doc "The Amazon Resource Names (ARNs) of the resources."];
      resourceTags: TagMap.t option
        [@ocaml.doc "The tags for the target resources."];
      filters: ExperimentTemplateTargetFilterInputList.t option
        [@ocaml.doc
          "The filters to apply to identify target resources using specific attributes."];
      selectionMode: ExperimentTemplateTargetSelectionMode.t
        [@ocaml.doc
          "Scopes the identified resources to a specific count of the resources at random, or a percentage of the resources. All identified resources are included in the target. ALL - Run the action on all identified targets. This is the default. COUNT(n) - Run the action on the specified number of targets, chosen from the identified targets at random. For example, COUNT(1) selects one of the targets. PERCENT(n) - Run the action on the specified percentage of targets, chosen from the identified targets at random. For example, PERCENT(25) selects 25% of the targets."];
      parameters: ExperimentTemplateTargetParameterMap.t option
        [@ocaml.doc "The resource type parameters."]}
    let context_ = "CreateExperimentTemplateTargetInput"
    let make ?resourceArns =
      fun ?resourceTags ->
        fun ?filters ->
          fun ?parameters ->
            fun ~resourceType ->
              fun ~selectionMode ->
                fun () ->
                  {
                    resourceArns;
                    resourceTags;
                    filters;
                    parameters;
                    resourceType;
                    selectionMode
                  }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Some (TargetResourceTypeId.to_value x.resourceType)));
        ("resourceArns",
          (Option.map x.resourceArns ~f:ResourceArnList.to_value));
        ("resourceTags", (Option.map x.resourceTags ~f:TagMap.to_value));
        ("filters",
          (Option.map x.filters
             ~f:ExperimentTemplateTargetFilterInputList.to_value));
        ("selectionMode",
          (Some
             (ExperimentTemplateTargetSelectionMode.to_value x.selectionMode)));
        ("parameters",
          (Option.map x.parameters
             ~f:ExperimentTemplateTargetParameterMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:ExperimentTemplateTargetParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let selectionMode =
        ExperimentTemplateTargetSelectionMode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "selectionMode") in
      let filters =
        (Option.map ~f:ExperimentTemplateTargetFilterInputList.of_xml)
          (Xml.child xml_arg0 "filters") in
      let resourceTags =
        (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "resourceTags") in
      let resourceArns =
        (Option.map ~f:ResourceArnList.of_xml)
          (Xml.child xml_arg0 "resourceArns") in
      let resourceType =
        TargetResourceTypeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceType") in
      make ?parameters ~selectionMode ?filters ?resourceTags ?resourceArns
        ~resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parameters =
        field_map json__ "parameters"
          ExperimentTemplateTargetParameterMap.of_json in
      let selectionMode =
        field_map_exn json__ "selectionMode"
          ExperimentTemplateTargetSelectionMode.of_json in
      let filters =
        field_map json__ "filters"
          ExperimentTemplateTargetFilterInputList.of_json in
      let resourceTags = field_map json__ "resourceTags" TagMap.of_json in
      let resourceArns =
        field_map json__ "resourceArns" ResourceArnList.of_json in
      let resourceType =
        field_map_exn json__ "resourceType" TargetResourceTypeId.of_json in
      make ?parameters ~selectionMode ?filters ?resourceTags ?resourceArns
        ~resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a target for an experiment. You must specify at least one Amazon Resource Name (ARN) or at least one resource tag. You cannot specify both ARNs and tags. For more information, see Targets in the Fault Injection Service User Guide."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource cannot be found."]
module TargetAccountConfiguration =
  struct
    type nonrec t =
      {
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role for the target account."];
      accountId: TargetAccountId.t option
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."];
      description: TargetAccountConfigurationDescription.t option
        [@ocaml.doc "The description of the target account."]}
    let make ?roleArn =
      fun ?accountId ->
        fun ?description -> fun () -> { roleArn; accountId; description }
    let to_value x =
      structure_to_value
        [("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("accountId", (Option.map x.accountId ~f:TargetAccountId.to_value));
        ("description",
          (Option.map x.description
             ~f:TargetAccountConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:TargetAccountConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let accountId =
        (Option.map ~f:TargetAccountId.of_xml)
          (Xml.child xml_arg0 "accountId") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      make ?description ?accountId ?roleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description"
          TargetAccountConfigurationDescription.of_json in
      let accountId = field_map json__ "accountId" TargetAccountId.of_json in
      let roleArn = field_map json__ "roleArn" RoleArn.of_json in
      make ?description ?accountId ?roleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a target account configuration."]
module ValidationException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified input is not valid, or fails to satisfy the constraints for the request."]
module ConflictException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request could not be processed because of a conflict."]
module SafetyLever =
  struct
    type nonrec t =
      {
      id: SafetyLeverId.t option [@ocaml.doc "The ID of the safety lever."];
      arn: ResourceArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the safety lever."];
      state: SafetyLeverState.t option
        [@ocaml.doc "The state of the safety lever."]}
    let make ?id = fun ?arn -> fun ?state -> fun () -> { id; arn; state }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:SafetyLeverId.to_value));
        ("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("state", (Option.map x.state ~f:SafetyLeverState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state =
        (Option.map ~f:SafetyLeverState.of_xml) (Xml.child xml_arg0 "state") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      let id = (Option.map ~f:SafetyLeverId.of_xml) (Xml.child xml_arg0 "id") in
      make ?state ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let state = field_map json__ "state" SafetyLeverState.of_json in
      let arn = field_map json__ "arn" ResourceArn.of_json in
      let id = field_map json__ "id" SafetyLeverId.of_json in
      make ?state ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a safety lever."]
module UpdateSafetyLeverStateInput =
  struct
    type nonrec t =
      {
      status: SafetyLeverStatusInput.t
        [@ocaml.doc "The updated state of the safety lever."];
      reason: SafetyLeverStatusReason.t
        [@ocaml.doc "The reason for updating the state of the safety lever."]}
    let context_ = "UpdateSafetyLeverStateInput"
    let make ~status = fun ~reason -> fun () -> { status; reason }
    let to_value x =
      structure_to_value
        [("status", (Some (SafetyLeverStatusInput.to_value x.status)));
        ("reason", (Some (SafetyLeverStatusReason.to_value x.reason)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        SafetyLeverStatusReason.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reason") in
      let status =
        SafetyLeverStatusInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      make ~reason ~status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason =
        field_map_exn json__ "reason" SafetyLeverStatusReason.of_json in
      let status =
        field_map_exn json__ "status" SafetyLeverStatusInput.of_json in
      make ~reason ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies a state for a safety lever."]
module ExperimentTemplate =
  struct
    type nonrec t =
      {
      id: ExperimentTemplateId.t option
        [@ocaml.doc "The ID of the experiment template."];
      arn: ResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the experiment template."];
      description: ExperimentTemplateDescription.t option
        [@ocaml.doc "The description for the experiment template."];
      targets: ExperimentTemplateTargetMap.t option
        [@ocaml.doc "The targets for the experiment."];
      actions: ExperimentTemplateActionMap.t option
        [@ocaml.doc "The actions for the experiment."];
      stopConditions: ExperimentTemplateStopConditionList.t option
        [@ocaml.doc "The stop conditions for the experiment."];
      creationTime: CreationTime.t option
        [@ocaml.doc "The time the experiment template was created."];
      lastUpdateTime: LastUpdateTime.t option
        [@ocaml.doc "The time the experiment template was last updated."];
      roleArn: RoleArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of an IAM role."];
      tags: TagMap.t option
        [@ocaml.doc "The tags for the experiment template."];
      logConfiguration: ExperimentTemplateLogConfiguration.t option
        [@ocaml.doc "The configuration for experiment logging."];
      experimentOptions: ExperimentTemplateExperimentOptions.t option
        [@ocaml.doc "The experiment options for an experiment template."];
      targetAccountConfigurationsCount:
        TargetAccountConfigurationsCount.t option
        [@ocaml.doc
          "The count of target account configurations for the experiment template."];
      experimentReportConfiguration:
        ExperimentTemplateReportConfiguration.t option
        [@ocaml.doc
          "Describes the report configuration for the experiment template."]}
    let make ?id =
      fun ?arn ->
        fun ?description ->
          fun ?targets ->
            fun ?actions ->
              fun ?stopConditions ->
                fun ?creationTime ->
                  fun ?lastUpdateTime ->
                    fun ?roleArn ->
                      fun ?tags ->
                        fun ?logConfiguration ->
                          fun ?experimentOptions ->
                            fun ?targetAccountConfigurationsCount ->
                              fun ?experimentReportConfiguration ->
                                fun () ->
                                  {
                                    id;
                                    arn;
                                    description;
                                    targets;
                                    actions;
                                    stopConditions;
                                    creationTime;
                                    lastUpdateTime;
                                    roleArn;
                                    tags;
                                    logConfiguration;
                                    experimentOptions;
                                    targetAccountConfigurationsCount;
                                    experimentReportConfiguration
                                  }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ExperimentTemplateId.to_value));
        ("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("description",
          (Option.map x.description ~f:ExperimentTemplateDescription.to_value));
        ("targets",
          (Option.map x.targets ~f:ExperimentTemplateTargetMap.to_value));
        ("actions",
          (Option.map x.actions ~f:ExperimentTemplateActionMap.to_value));
        ("stopConditions",
          (Option.map x.stopConditions
             ~f:ExperimentTemplateStopConditionList.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:CreationTime.to_value));
        ("lastUpdateTime",
          (Option.map x.lastUpdateTime ~f:LastUpdateTime.to_value));
        ("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("logConfiguration",
          (Option.map x.logConfiguration
             ~f:ExperimentTemplateLogConfiguration.to_value));
        ("experimentOptions",
          (Option.map x.experimentOptions
             ~f:ExperimentTemplateExperimentOptions.to_value));
        ("targetAccountConfigurationsCount",
          (Option.map x.targetAccountConfigurationsCount
             ~f:TargetAccountConfigurationsCount.to_value));
        ("experimentReportConfiguration",
          (Option.map x.experimentReportConfiguration
             ~f:ExperimentTemplateReportConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentReportConfiguration =
        (Option.map ~f:ExperimentTemplateReportConfiguration.of_xml)
          (Xml.child xml_arg0 "experimentReportConfiguration") in
      let targetAccountConfigurationsCount =
        (Option.map ~f:TargetAccountConfigurationsCount.of_xml)
          (Xml.child xml_arg0 "targetAccountConfigurationsCount") in
      let experimentOptions =
        (Option.map ~f:ExperimentTemplateExperimentOptions.of_xml)
          (Xml.child xml_arg0 "experimentOptions") in
      let logConfiguration =
        (Option.map ~f:ExperimentTemplateLogConfiguration.of_xml)
          (Xml.child xml_arg0 "logConfiguration") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let lastUpdateTime =
        (Option.map ~f:LastUpdateTime.of_xml)
          (Xml.child xml_arg0 "lastUpdateTime") in
      let creationTime =
        (Option.map ~f:CreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let stopConditions =
        (Option.map ~f:ExperimentTemplateStopConditionList.of_xml)
          (Xml.child xml_arg0 "stopConditions") in
      let actions =
        (Option.map ~f:ExperimentTemplateActionMap.of_xml)
          (Xml.child xml_arg0 "actions") in
      let targets =
        (Option.map ~f:ExperimentTemplateTargetMap.of_xml)
          (Xml.child xml_arg0 "targets") in
      let description =
        (Option.map ~f:ExperimentTemplateDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:ExperimentTemplateId.of_xml) (Xml.child xml_arg0 "id") in
      make ?experimentReportConfiguration ?targetAccountConfigurationsCount
        ?experimentOptions ?logConfiguration ?tags ?roleArn ?lastUpdateTime
        ?creationTime ?stopConditions ?actions ?targets ?description ?arn ?id
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentReportConfiguration =
        field_map json__ "experimentReportConfiguration"
          ExperimentTemplateReportConfiguration.of_json in
      let targetAccountConfigurationsCount =
        field_map json__ "targetAccountConfigurationsCount"
          TargetAccountConfigurationsCount.of_json in
      let experimentOptions =
        field_map json__ "experimentOptions"
          ExperimentTemplateExperimentOptions.of_json in
      let logConfiguration =
        field_map json__ "logConfiguration"
          ExperimentTemplateLogConfiguration.of_json in
      let tags = field_map json__ "tags" TagMap.of_json in
      let roleArn = field_map json__ "roleArn" RoleArn.of_json in
      let lastUpdateTime =
        field_map json__ "lastUpdateTime" LastUpdateTime.of_json in
      let creationTime = field_map json__ "creationTime" CreationTime.of_json in
      let stopConditions =
        field_map json__ "stopConditions"
          ExperimentTemplateStopConditionList.of_json in
      let actions =
        field_map json__ "actions" ExperimentTemplateActionMap.of_json in
      let targets =
        field_map json__ "targets" ExperimentTemplateTargetMap.of_json in
      let description =
        field_map json__ "description" ExperimentTemplateDescription.of_json in
      let arn = field_map json__ "arn" ResourceArn.of_json in
      let id = field_map json__ "id" ExperimentTemplateId.of_json in
      make ?experimentReportConfiguration ?targetAccountConfigurationsCount
        ?experimentOptions ?logConfiguration ?tags ?roleArn ?lastUpdateTime
        ?creationTime ?stopConditions ?actions ?targets ?description ?arn ?id
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes an experiment template."]
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "You have exceeded your service quota."]
module UpdateExperimentTemplateActionInputMap =
  struct
    type nonrec t =
      (ExperimentTemplateActionName.t *
        UpdateExperimentTemplateActionInputItem.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExperimentTemplateActionName UpdateExperimentTemplateActionInputItem"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateActionName.to_value x) |>
                    (fun x ->
                       (UpdateExperimentTemplateActionInputItem.to_value y)
                         |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentTemplateActionName.of_string
        ~of_json:UpdateExperimentTemplateActionInputItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateExperimentTemplateExperimentOptionsInput =
  struct
    type nonrec t =
      {
      emptyTargetResolutionMode: EmptyTargetResolutionMode.t option
        [@ocaml.doc
          "The empty target resolution mode of the experiment template."]}
    let make ?emptyTargetResolutionMode =
      fun () -> { emptyTargetResolutionMode }
    let to_value x =
      structure_to_value
        [("emptyTargetResolutionMode",
           (Option.map x.emptyTargetResolutionMode
              ~f:EmptyTargetResolutionMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emptyTargetResolutionMode =
        (Option.map ~f:EmptyTargetResolutionMode.of_xml)
          (Xml.child xml_arg0 "emptyTargetResolutionMode") in
      make ?emptyTargetResolutionMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let emptyTargetResolutionMode =
        field_map json__ "emptyTargetResolutionMode"
          EmptyTargetResolutionMode.of_json in
      make ?emptyTargetResolutionMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies an experiment option for an experiment template."]
module UpdateExperimentTemplateLogConfigurationInput =
  struct
    type nonrec t =
      {
      cloudWatchLogsConfiguration:
        ExperimentTemplateCloudWatchLogsLogConfigurationInput.t option
        [@ocaml.doc
          "The configuration for experiment logging to Amazon CloudWatch Logs."];
      s3Configuration: ExperimentTemplateS3LogConfigurationInput.t option
        [@ocaml.doc "The configuration for experiment logging to Amazon S3."];
      logSchemaVersion: LogSchemaVersion.t option
        [@ocaml.doc "The schema version."]}
    let make ?cloudWatchLogsConfiguration =
      fun ?s3Configuration ->
        fun ?logSchemaVersion ->
          fun () ->
            { cloudWatchLogsConfiguration; s3Configuration; logSchemaVersion
            }
    let to_value x =
      structure_to_value
        [("cloudWatchLogsConfiguration",
           (Option.map x.cloudWatchLogsConfiguration
              ~f:ExperimentTemplateCloudWatchLogsLogConfigurationInput.to_value));
        ("s3Configuration",
          (Option.map x.s3Configuration
             ~f:ExperimentTemplateS3LogConfigurationInput.to_value));
        ("logSchemaVersion",
          (Option.map x.logSchemaVersion ~f:LogSchemaVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logSchemaVersion =
        (Option.map ~f:LogSchemaVersion.of_xml)
          (Xml.child xml_arg0 "logSchemaVersion") in
      let s3Configuration =
        (Option.map ~f:ExperimentTemplateS3LogConfigurationInput.of_xml)
          (Xml.child xml_arg0 "s3Configuration") in
      let cloudWatchLogsConfiguration =
        (Option.map
           ~f:ExperimentTemplateCloudWatchLogsLogConfigurationInput.of_xml)
          (Xml.child xml_arg0 "cloudWatchLogsConfiguration") in
      make ?logSchemaVersion ?s3Configuration ?cloudWatchLogsConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logSchemaVersion =
        field_map json__ "logSchemaVersion" LogSchemaVersion.of_json in
      let s3Configuration =
        field_map json__ "s3Configuration"
          ExperimentTemplateS3LogConfigurationInput.of_json in
      let cloudWatchLogsConfiguration =
        field_map json__ "cloudWatchLogsConfiguration"
          ExperimentTemplateCloudWatchLogsLogConfigurationInput.of_json in
      make ?logSchemaVersion ?s3Configuration ?cloudWatchLogsConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the configuration for experiment logging."]
module UpdateExperimentTemplateReportConfigurationInput =
  struct
    type nonrec t =
      {
      outputs: ExperimentTemplateReportConfigurationOutputsInput.t option
        [@ocaml.doc
          "Describes the output destinations of the experiment report."];
      dataSources:
        ExperimentTemplateReportConfigurationDataSourcesInput.t option
        [@ocaml.doc "The data sources for the experiment report."];
      preExperimentDuration: ReportConfigurationDuration.t option
        [@ocaml.doc
          "The duration before the experiment start time for the data sources to include in the report."];
      postExperimentDuration: ReportConfigurationDuration.t option
        [@ocaml.doc
          "The duration after the experiment end time for the data sources to include in the report."]}
    let make ?outputs =
      fun ?dataSources ->
        fun ?preExperimentDuration ->
          fun ?postExperimentDuration ->
            fun () ->
              {
                outputs;
                dataSources;
                preExperimentDuration;
                postExperimentDuration
              }
    let to_value x =
      structure_to_value
        [("outputs",
           (Option.map x.outputs
              ~f:ExperimentTemplateReportConfigurationOutputsInput.to_value));
        ("dataSources",
          (Option.map x.dataSources
             ~f:ExperimentTemplateReportConfigurationDataSourcesInput.to_value));
        ("preExperimentDuration",
          (Option.map x.preExperimentDuration
             ~f:ReportConfigurationDuration.to_value));
        ("postExperimentDuration",
          (Option.map x.postExperimentDuration
             ~f:ReportConfigurationDuration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let postExperimentDuration =
        (Option.map ~f:ReportConfigurationDuration.of_xml)
          (Xml.child xml_arg0 "postExperimentDuration") in
      let preExperimentDuration =
        (Option.map ~f:ReportConfigurationDuration.of_xml)
          (Xml.child xml_arg0 "preExperimentDuration") in
      let dataSources =
        (Option.map
           ~f:ExperimentTemplateReportConfigurationDataSourcesInput.of_xml)
          (Xml.child xml_arg0 "dataSources") in
      let outputs =
        (Option.map
           ~f:ExperimentTemplateReportConfigurationOutputsInput.of_xml)
          (Xml.child xml_arg0 "outputs") in
      make ?postExperimentDuration ?preExperimentDuration ?dataSources
        ?outputs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let postExperimentDuration =
        field_map json__ "postExperimentDuration"
          ReportConfigurationDuration.of_json in
      let preExperimentDuration =
        field_map json__ "preExperimentDuration"
          ReportConfigurationDuration.of_json in
      let dataSources =
        field_map json__ "dataSources"
          ExperimentTemplateReportConfigurationDataSourcesInput.of_json in
      let outputs =
        field_map json__ "outputs"
          ExperimentTemplateReportConfigurationOutputsInput.of_json in
      make ?postExperimentDuration ?preExperimentDuration ?dataSources
        ?outputs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the input for the experiment report configuration."]
module UpdateExperimentTemplateStopConditionInputList =
  struct
    type nonrec t = UpdateExperimentTemplateStopConditionInput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map ~f:UpdateExperimentTemplateStopConditionInput.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:UpdateExperimentTemplateStopConditionInput.of_xml)
    let of_json j =
      list_of_json ~kind:"UpdateExperimentTemplateStopConditionInputList"
        ~of_json:UpdateExperimentTemplateStopConditionInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateExperimentTemplateTargetInputMap =
  struct
    type nonrec t =
      (ExperimentTemplateTargetName.t *
        UpdateExperimentTemplateTargetInput.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExperimentTemplateTargetName UpdateExperimentTemplateTargetInput"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateTargetName.to_value x) |>
                    (fun x ->
                       (UpdateExperimentTemplateTargetInput.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentTemplateTargetName.of_string
        ~of_json:UpdateExperimentTemplateTargetInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module Experiment =
  struct
    type nonrec t =
      {
      id: ExperimentId.t option [@ocaml.doc "The ID of the experiment."];
      arn: ResourceArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the experiment."];
      experimentTemplateId: ExperimentTemplateId.t option
        [@ocaml.doc "The ID of the experiment template."];
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role that grants the FIS service permission to perform service actions on your behalf."];
      state: ExperimentState.t option
        [@ocaml.doc "The state of the experiment."];
      targets: ExperimentTargetMap.t option
        [@ocaml.doc "The targets for the experiment."];
      actions: ExperimentActionMap.t option
        [@ocaml.doc "The actions for the experiment."];
      stopConditions: ExperimentStopConditionList.t option
        [@ocaml.doc "The stop conditions for the experiment."];
      creationTime: CreationTime.t option
        [@ocaml.doc "The time that the experiment was created."];
      startTime: ExperimentStartTime.t option
        [@ocaml.doc "The time that the experiment started."];
      endTime: ExperimentEndTime.t option
        [@ocaml.doc "The time that the experiment ended."];
      tags: TagMap.t option [@ocaml.doc "The tags for the experiment."];
      logConfiguration: ExperimentLogConfiguration.t option
        [@ocaml.doc "The configuration for experiment logging."];
      experimentOptions: ExperimentOptions.t option
        [@ocaml.doc "The experiment options for the experiment."];
      targetAccountConfigurationsCount:
        TargetAccountConfigurationsCount.t option
        [@ocaml.doc
          "The count of target account configurations for the experiment."];
      experimentReportConfiguration: ExperimentReportConfiguration.t option
        [@ocaml.doc
          "The experiment report configuration for the experiment."];
      experimentReport: ExperimentReport.t option
        [@ocaml.doc "The experiment report for the experiment."]}
    let make ?id =
      fun ?arn ->
        fun ?experimentTemplateId ->
          fun ?roleArn ->
            fun ?state ->
              fun ?targets ->
                fun ?actions ->
                  fun ?stopConditions ->
                    fun ?creationTime ->
                      fun ?startTime ->
                        fun ?endTime ->
                          fun ?tags ->
                            fun ?logConfiguration ->
                              fun ?experimentOptions ->
                                fun ?targetAccountConfigurationsCount ->
                                  fun ?experimentReportConfiguration ->
                                    fun ?experimentReport ->
                                      fun () ->
                                        {
                                          id;
                                          arn;
                                          experimentTemplateId;
                                          roleArn;
                                          state;
                                          targets;
                                          actions;
                                          stopConditions;
                                          creationTime;
                                          startTime;
                                          endTime;
                                          tags;
                                          logConfiguration;
                                          experimentOptions;
                                          targetAccountConfigurationsCount;
                                          experimentReportConfiguration;
                                          experimentReport
                                        }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ExperimentId.to_value));
        ("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("experimentTemplateId",
          (Option.map x.experimentTemplateId ~f:ExperimentTemplateId.to_value));
        ("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("state", (Option.map x.state ~f:ExperimentState.to_value));
        ("targets", (Option.map x.targets ~f:ExperimentTargetMap.to_value));
        ("actions", (Option.map x.actions ~f:ExperimentActionMap.to_value));
        ("stopConditions",
          (Option.map x.stopConditions
             ~f:ExperimentStopConditionList.to_value));
        ("creationTime",
          (Option.map x.creationTime ~f:CreationTime.to_value));
        ("startTime",
          (Option.map x.startTime ~f:ExperimentStartTime.to_value));
        ("endTime", (Option.map x.endTime ~f:ExperimentEndTime.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("logConfiguration",
          (Option.map x.logConfiguration
             ~f:ExperimentLogConfiguration.to_value));
        ("experimentOptions",
          (Option.map x.experimentOptions ~f:ExperimentOptions.to_value));
        ("targetAccountConfigurationsCount",
          (Option.map x.targetAccountConfigurationsCount
             ~f:TargetAccountConfigurationsCount.to_value));
        ("experimentReportConfiguration",
          (Option.map x.experimentReportConfiguration
             ~f:ExperimentReportConfiguration.to_value));
        ("experimentReport",
          (Option.map x.experimentReport ~f:ExperimentReport.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentReport =
        (Option.map ~f:ExperimentReport.of_xml)
          (Xml.child xml_arg0 "experimentReport") in
      let experimentReportConfiguration =
        (Option.map ~f:ExperimentReportConfiguration.of_xml)
          (Xml.child xml_arg0 "experimentReportConfiguration") in
      let targetAccountConfigurationsCount =
        (Option.map ~f:TargetAccountConfigurationsCount.of_xml)
          (Xml.child xml_arg0 "targetAccountConfigurationsCount") in
      let experimentOptions =
        (Option.map ~f:ExperimentOptions.of_xml)
          (Xml.child xml_arg0 "experimentOptions") in
      let logConfiguration =
        (Option.map ~f:ExperimentLogConfiguration.of_xml)
          (Xml.child xml_arg0 "logConfiguration") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let endTime =
        (Option.map ~f:ExperimentEndTime.of_xml)
          (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:ExperimentStartTime.of_xml)
          (Xml.child xml_arg0 "startTime") in
      let creationTime =
        (Option.map ~f:CreationTime.of_xml)
          (Xml.child xml_arg0 "creationTime") in
      let stopConditions =
        (Option.map ~f:ExperimentStopConditionList.of_xml)
          (Xml.child xml_arg0 "stopConditions") in
      let actions =
        (Option.map ~f:ExperimentActionMap.of_xml)
          (Xml.child xml_arg0 "actions") in
      let targets =
        (Option.map ~f:ExperimentTargetMap.of_xml)
          (Xml.child xml_arg0 "targets") in
      let state =
        (Option.map ~f:ExperimentState.of_xml) (Xml.child xml_arg0 "state") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let experimentTemplateId =
        (Option.map ~f:ExperimentTemplateId.of_xml)
          (Xml.child xml_arg0 "experimentTemplateId") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      let id = (Option.map ~f:ExperimentId.of_xml) (Xml.child xml_arg0 "id") in
      make ?experimentReport ?experimentReportConfiguration
        ?targetAccountConfigurationsCount ?experimentOptions
        ?logConfiguration ?tags ?endTime ?startTime ?creationTime
        ?stopConditions ?actions ?targets ?state ?roleArn
        ?experimentTemplateId ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentReport =
        field_map json__ "experimentReport" ExperimentReport.of_json in
      let experimentReportConfiguration =
        field_map json__ "experimentReportConfiguration"
          ExperimentReportConfiguration.of_json in
      let targetAccountConfigurationsCount =
        field_map json__ "targetAccountConfigurationsCount"
          TargetAccountConfigurationsCount.of_json in
      let experimentOptions =
        field_map json__ "experimentOptions" ExperimentOptions.of_json in
      let logConfiguration =
        field_map json__ "logConfiguration"
          ExperimentLogConfiguration.of_json in
      let tags = field_map json__ "tags" TagMap.of_json in
      let endTime = field_map json__ "endTime" ExperimentEndTime.of_json in
      let startTime =
        field_map json__ "startTime" ExperimentStartTime.of_json in
      let creationTime = field_map json__ "creationTime" CreationTime.of_json in
      let stopConditions =
        field_map json__ "stopConditions" ExperimentStopConditionList.of_json in
      let actions = field_map json__ "actions" ExperimentActionMap.of_json in
      let targets = field_map json__ "targets" ExperimentTargetMap.of_json in
      let state = field_map json__ "state" ExperimentState.of_json in
      let roleArn = field_map json__ "roleArn" RoleArn.of_json in
      let experimentTemplateId =
        field_map json__ "experimentTemplateId" ExperimentTemplateId.of_json in
      let arn = field_map json__ "arn" ResourceArn.of_json in
      let id = field_map json__ "id" ExperimentId.of_json in
      make ?experimentReport ?experimentReportConfiguration
        ?targetAccountConfigurationsCount ?experimentOptions
        ?logConfiguration ?tags ?endTime ?startTime ?creationTime
        ?stopConditions ?actions ?targets ?state ?roleArn
        ?experimentTemplateId ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes an experiment."]
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module StartExperimentExperimentOptionsInput =
  struct
    type nonrec t =
      {
      actionsMode: ActionsMode.t option
        [@ocaml.doc "Specifies the actions mode for experiment options."]}
    let make ?actionsMode = fun () -> { actionsMode }
    let to_value x =
      structure_to_value
        [("actionsMode", (Option.map x.actionsMode ~f:ActionsMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let actionsMode =
        (Option.map ~f:ActionsMode.of_xml) (Xml.child xml_arg0 "actionsMode") in
      make ?actionsMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let actionsMode = field_map json__ "actionsMode" ActionsMode.of_json in
      make ?actionsMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies experiment options for running an experiment."]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module TargetResourceTypeSummaryList =
  struct
    type nonrec t = TargetResourceTypeSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TargetResourceTypeSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TargetResourceTypeSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TargetResourceTypeSummaryList"
        ~of_json:TargetResourceTypeSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTargetResourceTypesMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListTargetResourceTypesMaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TargetAccountConfigurationList =
  struct
    type nonrec t = TargetAccountConfigurationSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TargetAccountConfigurationSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:TargetAccountConfigurationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TargetAccountConfigurationList"
        ~of_json:TargetAccountConfigurationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTargetAccountConfigurationsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListTargetAccountConfigurationsMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ExperimentSummaryList =
  struct
    type nonrec t = ExperimentSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentSummaryList"
        ~of_json:ExperimentSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListExperimentsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListExperimentsMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ExperimentTemplateSummaryList =
  struct
    type nonrec t = ExperimentTemplateSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExperimentTemplateSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExperimentTemplateSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTemplateSummaryList"
        ~of_json:ExperimentTemplateSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListExperimentTemplatesMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListExperimentTemplatesMaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ExperimentTargetAccountConfigurationList =
  struct
    type nonrec t = ExperimentTargetAccountConfigurationSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map ~f:ExperimentTargetAccountConfigurationSummary.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ExperimentTargetAccountConfigurationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ExperimentTargetAccountConfigurationList"
        ~of_json:ExperimentTargetAccountConfigurationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResolvedTargetList =
  struct
    type nonrec t = ResolvedTarget.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ResolvedTarget.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResolvedTarget.of_xml)
    let of_json j =
      list_of_json ~kind:"ResolvedTargetList" ~of_json:ResolvedTarget.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ListExperimentResolvedTargetsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListExperimentResolvedTargetsMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ActionSummaryList =
  struct
    type nonrec t = ActionSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ActionSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ActionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ActionSummaryList" ~of_json:ActionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListActionsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListActionsMaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TargetResourceType =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t option
        [@ocaml.doc "The resource type."];
      description: TargetResourceTypeDescription.t option
        [@ocaml.doc "A description of the resource type."];
      parameters: TargetResourceTypeParameterMap.t option
        [@ocaml.doc "The parameters for the resource type."]}
    let make ?resourceType =
      fun ?description ->
        fun ?parameters ->
          fun () -> { resourceType; description; parameters }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Option.map x.resourceType ~f:TargetResourceTypeId.to_value));
        ("description",
          (Option.map x.description ~f:TargetResourceTypeDescription.to_value));
        ("parameters",
          (Option.map x.parameters ~f:TargetResourceTypeParameterMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:TargetResourceTypeParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let description =
        (Option.map ~f:TargetResourceTypeDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let resourceType =
        (Option.map ~f:TargetResourceTypeId.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      make ?parameters ?description ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parameters =
        field_map json__ "parameters" TargetResourceTypeParameterMap.of_json in
      let description =
        field_map json__ "description" TargetResourceTypeDescription.of_json in
      let resourceType =
        field_map json__ "resourceType" TargetResourceTypeId.of_json in
      make ?parameters ?description ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a resource type."]
module ExperimentTargetAccountConfiguration =
  struct
    type nonrec t =
      {
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role for the target account."];
      accountId: TargetAccountId.t option
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."];
      description: TargetAccountConfigurationDescription.t option
        [@ocaml.doc "The description of the target account."]}
    let make ?roleArn =
      fun ?accountId ->
        fun ?description -> fun () -> { roleArn; accountId; description }
    let to_value x =
      structure_to_value
        [("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("accountId", (Option.map x.accountId ~f:TargetAccountId.to_value));
        ("description",
          (Option.map x.description
             ~f:TargetAccountConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:TargetAccountConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let accountId =
        (Option.map ~f:TargetAccountId.of_xml)
          (Xml.child xml_arg0 "accountId") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      make ?description ?accountId ?roleArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description"
          TargetAccountConfigurationDescription.of_json in
      let accountId = field_map json__ "accountId" TargetAccountId.of_json in
      let roleArn = field_map json__ "roleArn" RoleArn.of_json in
      make ?description ?accountId ?roleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a target account configuration for an experiment."]
module Action =
  struct
    type nonrec t =
      {
      id: ActionId.t option [@ocaml.doc "The ID of the action."];
      arn: ResourceArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the action."];
      description: ActionDescription.t option
        [@ocaml.doc "The description for the action."];
      parameters: ActionParameterMap.t option
        [@ocaml.doc "The action parameters, if applicable."];
      targets: ActionTargetMap.t option
        [@ocaml.doc "The supported targets for the action."];
      tags: TagMap.t option [@ocaml.doc "The tags for the action."]}
    let make ?id =
      fun ?arn ->
        fun ?description ->
          fun ?parameters ->
            fun ?targets ->
              fun ?tags ->
                fun () -> { id; arn; description; parameters; targets; tags }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ActionId.to_value));
        ("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("description",
          (Option.map x.description ~f:ActionDescription.to_value));
        ("parameters",
          (Option.map x.parameters ~f:ActionParameterMap.to_value));
        ("targets", (Option.map x.targets ~f:ActionTargetMap.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let targets =
        (Option.map ~f:ActionTargetMap.of_xml) (Xml.child xml_arg0 "targets") in
      let parameters =
        (Option.map ~f:ActionParameterMap.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let description =
        (Option.map ~f:ActionDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      let id = (Option.map ~f:ActionId.of_xml) (Xml.child xml_arg0 "id") in
      make ?tags ?targets ?parameters ?description ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let targets = field_map json__ "targets" ActionTargetMap.of_json in
      let parameters =
        field_map json__ "parameters" ActionParameterMap.of_json in
      let description =
        field_map json__ "description" ActionDescription.of_json in
      let arn = field_map json__ "arn" ResourceArn.of_json in
      let id = field_map json__ "id" ActionId.of_json in
      make ?tags ?targets ?parameters ?description ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes an action. For more information, see FIS actions in the Fault Injection Service User Guide."]
module CreateExperimentTemplateActionInputMap =
  struct
    type nonrec t =
      (ExperimentTemplateActionName.t *
        CreateExperimentTemplateActionInput.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExperimentTemplateActionName CreateExperimentTemplateActionInput"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateActionName.to_value x) |>
                    (fun x ->
                       (CreateExperimentTemplateActionInput.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentTemplateActionName.of_string
        ~of_json:CreateExperimentTemplateActionInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateExperimentTemplateExperimentOptionsInput =
  struct
    type nonrec t =
      {
      accountTargeting: AccountTargeting.t option
        [@ocaml.doc
          "Specifies the account targeting setting for experiment options."];
      emptyTargetResolutionMode: EmptyTargetResolutionMode.t option
        [@ocaml.doc
          "Specifies the empty target resolution mode for experiment options."]}
    let make ?accountTargeting =
      fun ?emptyTargetResolutionMode ->
        fun () -> { accountTargeting; emptyTargetResolutionMode }
    let to_value x =
      structure_to_value
        [("accountTargeting",
           (Option.map x.accountTargeting ~f:AccountTargeting.to_value));
        ("emptyTargetResolutionMode",
          (Option.map x.emptyTargetResolutionMode
             ~f:EmptyTargetResolutionMode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emptyTargetResolutionMode =
        (Option.map ~f:EmptyTargetResolutionMode.of_xml)
          (Xml.child xml_arg0 "emptyTargetResolutionMode") in
      let accountTargeting =
        (Option.map ~f:AccountTargeting.of_xml)
          (Xml.child xml_arg0 "accountTargeting") in
      make ?emptyTargetResolutionMode ?accountTargeting ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let emptyTargetResolutionMode =
        field_map json__ "emptyTargetResolutionMode"
          EmptyTargetResolutionMode.of_json in
      let accountTargeting =
        field_map json__ "accountTargeting" AccountTargeting.of_json in
      make ?emptyTargetResolutionMode ?accountTargeting ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies experiment options for an experiment template."]
module CreateExperimentTemplateLogConfigurationInput =
  struct
    type nonrec t =
      {
      cloudWatchLogsConfiguration:
        ExperimentTemplateCloudWatchLogsLogConfigurationInput.t option
        [@ocaml.doc
          "The configuration for experiment logging to Amazon CloudWatch Logs."];
      s3Configuration: ExperimentTemplateS3LogConfigurationInput.t option
        [@ocaml.doc "The configuration for experiment logging to Amazon S3."];
      logSchemaVersion: LogSchemaVersion.t [@ocaml.doc "The schema version."]}
    let context_ = "CreateExperimentTemplateLogConfigurationInput"
    let make ?cloudWatchLogsConfiguration =
      fun ?s3Configuration ->
        fun ~logSchemaVersion ->
          fun () ->
            { cloudWatchLogsConfiguration; s3Configuration; logSchemaVersion
            }
    let to_value x =
      structure_to_value
        [("cloudWatchLogsConfiguration",
           (Option.map x.cloudWatchLogsConfiguration
              ~f:ExperimentTemplateCloudWatchLogsLogConfigurationInput.to_value));
        ("s3Configuration",
          (Option.map x.s3Configuration
             ~f:ExperimentTemplateS3LogConfigurationInput.to_value));
        ("logSchemaVersion",
          (Some (LogSchemaVersion.to_value x.logSchemaVersion)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logSchemaVersion =
        LogSchemaVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "logSchemaVersion") in
      let s3Configuration =
        (Option.map ~f:ExperimentTemplateS3LogConfigurationInput.of_xml)
          (Xml.child xml_arg0 "s3Configuration") in
      let cloudWatchLogsConfiguration =
        (Option.map
           ~f:ExperimentTemplateCloudWatchLogsLogConfigurationInput.of_xml)
          (Xml.child xml_arg0 "cloudWatchLogsConfiguration") in
      make ~logSchemaVersion ?s3Configuration ?cloudWatchLogsConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let logSchemaVersion =
        field_map_exn json__ "logSchemaVersion" LogSchemaVersion.of_json in
      let s3Configuration =
        field_map json__ "s3Configuration"
          ExperimentTemplateS3LogConfigurationInput.of_json in
      let cloudWatchLogsConfiguration =
        field_map json__ "cloudWatchLogsConfiguration"
          ExperimentTemplateCloudWatchLogsLogConfigurationInput.of_json in
      make ~logSchemaVersion ?s3Configuration ?cloudWatchLogsConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the configuration for experiment logging."]
module CreateExperimentTemplateReportConfigurationInput =
  struct
    type nonrec t =
      {
      outputs: ExperimentTemplateReportConfigurationOutputsInput.t option
        [@ocaml.doc "The output destinations of the experiment report."];
      dataSources:
        ExperimentTemplateReportConfigurationDataSourcesInput.t option
        [@ocaml.doc "The data sources for the experiment report."];
      preExperimentDuration: ReportConfigurationDuration.t option
        [@ocaml.doc
          "The duration before the experiment start time for the data sources to include in the report."];
      postExperimentDuration: ReportConfigurationDuration.t option
        [@ocaml.doc
          "The duration after the experiment end time for the data sources to include in the report."]}
    let make ?outputs =
      fun ?dataSources ->
        fun ?preExperimentDuration ->
          fun ?postExperimentDuration ->
            fun () ->
              {
                outputs;
                dataSources;
                preExperimentDuration;
                postExperimentDuration
              }
    let to_value x =
      structure_to_value
        [("outputs",
           (Option.map x.outputs
              ~f:ExperimentTemplateReportConfigurationOutputsInput.to_value));
        ("dataSources",
          (Option.map x.dataSources
             ~f:ExperimentTemplateReportConfigurationDataSourcesInput.to_value));
        ("preExperimentDuration",
          (Option.map x.preExperimentDuration
             ~f:ReportConfigurationDuration.to_value));
        ("postExperimentDuration",
          (Option.map x.postExperimentDuration
             ~f:ReportConfigurationDuration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let postExperimentDuration =
        (Option.map ~f:ReportConfigurationDuration.of_xml)
          (Xml.child xml_arg0 "postExperimentDuration") in
      let preExperimentDuration =
        (Option.map ~f:ReportConfigurationDuration.of_xml)
          (Xml.child xml_arg0 "preExperimentDuration") in
      let dataSources =
        (Option.map
           ~f:ExperimentTemplateReportConfigurationDataSourcesInput.of_xml)
          (Xml.child xml_arg0 "dataSources") in
      let outputs =
        (Option.map
           ~f:ExperimentTemplateReportConfigurationOutputsInput.of_xml)
          (Xml.child xml_arg0 "outputs") in
      make ?postExperimentDuration ?preExperimentDuration ?dataSources
        ?outputs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let postExperimentDuration =
        field_map json__ "postExperimentDuration"
          ReportConfigurationDuration.of_json in
      let preExperimentDuration =
        field_map json__ "preExperimentDuration"
          ReportConfigurationDuration.of_json in
      let dataSources =
        field_map json__ "dataSources"
          ExperimentTemplateReportConfigurationDataSourcesInput.of_json in
      let outputs =
        field_map json__ "outputs"
          ExperimentTemplateReportConfigurationOutputsInput.of_json in
      make ?postExperimentDuration ?preExperimentDuration ?dataSources
        ?outputs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the configuration for experiment reports."]
module CreateExperimentTemplateStopConditionInputList =
  struct
    type nonrec t = CreateExperimentTemplateStopConditionInput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |>
         (List.map ~f:CreateExperimentTemplateStopConditionInput.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:CreateExperimentTemplateStopConditionInput.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateExperimentTemplateStopConditionInputList"
        ~of_json:CreateExperimentTemplateStopConditionInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateExperimentTemplateTargetInputMap =
  struct
    type nonrec t =
      (ExperimentTemplateTargetName.t *
        CreateExperimentTemplateTargetInput.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExperimentTemplateTargetName CreateExperimentTemplateTargetInput"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExperimentTemplateTargetName.to_value x) |>
                    (fun x ->
                       (CreateExperimentTemplateTargetInput.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExperimentTemplateTargetName.of_string
        ~of_json:CreateExperimentTemplateTargetInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateTargetAccountConfigurationResponse =
  struct
    type nonrec t =
      {
      targetAccountConfiguration: TargetAccountConfiguration.t option
        [@ocaml.doc "Information about the target account configuration."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetAccountConfiguration =
      fun () -> { targetAccountConfiguration }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetAccountConfiguration",
           (Option.map x.targetAccountConfiguration
              ~f:TargetAccountConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetAccountConfiguration =
        (Option.map ~f:TargetAccountConfiguration.of_xml)
          (Xml.child xml_arg0 "targetAccountConfiguration") in
      make ?targetAccountConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetAccountConfiguration =
        field_map json__ "targetAccountConfiguration"
          TargetAccountConfiguration.of_json in
      make ?targetAccountConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the target account configuration for the specified experiment template."]
module UpdateTargetAccountConfigurationRequest =
  struct
    type nonrec t =
      {
      experimentTemplateId: ExperimentTemplateId.t
        [@ocaml.doc "The ID of the experiment template."];
      accountId: TargetAccountId.t
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."];
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role for the target account."];
      description: TargetAccountConfigurationDescription.t option
        [@ocaml.doc "The description of the target account."]}
    let context_ = "UpdateTargetAccountConfigurationRequest"
    let make ?roleArn =
      fun ?description ->
        fun ~experimentTemplateId ->
          fun ~accountId ->
            fun () ->
              { roleArn; description; experimentTemplateId; accountId }
    let to_value x =
      structure_to_value
        [("id",
           (Some (ExperimentTemplateId.to_value x.experimentTemplateId)));
        ("accountId", (Some (TargetAccountId.to_value x.accountId)));
        ("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("description",
          (Option.map x.description
             ~f:TargetAccountConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:TargetAccountConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let accountId =
        TargetAccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      let experimentTemplateId =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?description ?roleArn ~accountId ~experimentTemplateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description"
          TargetAccountConfigurationDescription.of_json in
      let roleArn = field_map json__ "roleArn" RoleArn.of_json in
      let accountId =
        field_map_exn json__ "accountId" TargetAccountId.of_json in
      let experimentTemplateId =
        field_map_exn json__ "experimentTemplateId"
          ExperimentTemplateId.of_json in
      make ?description ?roleArn ~accountId ~experimentTemplateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the target account configuration for the specified experiment template."]
module UpdateSafetyLeverStateResponse =
  struct
    type nonrec t =
      {
      safetyLever: SafetyLever.t option
        [@ocaml.doc "Information about the safety lever."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?safetyLever = fun () -> { safetyLever }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("safetyLever", (Option.map x.safetyLever ~f:SafetyLever.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let safetyLever =
        (Option.map ~f:SafetyLever.of_xml) (Xml.child xml_arg0 "safetyLever") in
      make ?safetyLever ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let safetyLever = field_map json__ "safetyLever" SafetyLever.of_json in
      make ?safetyLever ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the specified safety lever state."]
module UpdateSafetyLeverStateRequest =
  struct
    type nonrec t =
      {
      id: SafetyLeverId.t [@ocaml.doc "The ID of the safety lever."];
      state: UpdateSafetyLeverStateInput.t
        [@ocaml.doc "The state of the safety lever."]}
    let context_ = "UpdateSafetyLeverStateRequest"
    let make ~id = fun ~state -> fun () -> { id; state }
    let to_value x =
      structure_to_value
        [("id", (Some (SafetyLeverId.to_value x.id)));
        ("state", (Some (UpdateSafetyLeverStateInput.to_value x.state)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state =
        UpdateSafetyLeverStateInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "state") in
      let id =
        SafetyLeverId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~state ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let state =
        field_map_exn json__ "state" UpdateSafetyLeverStateInput.of_json in
      let id = field_map_exn json__ "id" SafetyLeverId.of_json in
      make ~state ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the specified safety lever state."]
module UpdateExperimentTemplateResponse =
  struct
    type nonrec t =
      {
      experimentTemplate: ExperimentTemplate.t option
        [@ocaml.doc "Information about the experiment template."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experimentTemplate = fun () -> { experimentTemplate }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experimentTemplate",
           (Option.map x.experimentTemplate ~f:ExperimentTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentTemplate =
        (Option.map ~f:ExperimentTemplate.of_xml)
          (Xml.child xml_arg0 "experimentTemplate") in
      make ?experimentTemplate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentTemplate =
        field_map json__ "experimentTemplate" ExperimentTemplate.of_json in
      make ?experimentTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the specified experiment template."]
module UpdateExperimentTemplateRequest =
  struct
    type nonrec t =
      {
      id: ExperimentTemplateId.t
        [@ocaml.doc "The ID of the experiment template."];
      description: ExperimentTemplateDescription.t option
        [@ocaml.doc "A description for the template."];
      stopConditions: UpdateExperimentTemplateStopConditionInputList.t option
        [@ocaml.doc "The stop conditions for the experiment."];
      targets: UpdateExperimentTemplateTargetInputMap.t option
        [@ocaml.doc "The targets for the experiment."];
      actions: UpdateExperimentTemplateActionInputMap.t option
        [@ocaml.doc "The actions for the experiment."];
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role that grants the FIS service permission to perform service actions on your behalf."];
      logConfiguration:
        UpdateExperimentTemplateLogConfigurationInput.t option
        [@ocaml.doc "The configuration for experiment logging."];
      experimentOptions:
        UpdateExperimentTemplateExperimentOptionsInput.t option
        [@ocaml.doc "The experiment options for the experiment template."];
      experimentReportConfiguration:
        UpdateExperimentTemplateReportConfigurationInput.t option
        [@ocaml.doc
          "The experiment report configuration for the experiment template."]}
    let context_ = "UpdateExperimentTemplateRequest"
    let make ?description =
      fun ?stopConditions ->
        fun ?targets ->
          fun ?actions ->
            fun ?roleArn ->
              fun ?logConfiguration ->
                fun ?experimentOptions ->
                  fun ?experimentReportConfiguration ->
                    fun ~id ->
                      fun () ->
                        {
                          description;
                          stopConditions;
                          targets;
                          actions;
                          roleArn;
                          logConfiguration;
                          experimentOptions;
                          experimentReportConfiguration;
                          id
                        }
    let to_value x =
      structure_to_value
        [("id", (Some (ExperimentTemplateId.to_value x.id)));
        ("description",
          (Option.map x.description ~f:ExperimentTemplateDescription.to_value));
        ("stopConditions",
          (Option.map x.stopConditions
             ~f:UpdateExperimentTemplateStopConditionInputList.to_value));
        ("targets",
          (Option.map x.targets
             ~f:UpdateExperimentTemplateTargetInputMap.to_value));
        ("actions",
          (Option.map x.actions
             ~f:UpdateExperimentTemplateActionInputMap.to_value));
        ("roleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("logConfiguration",
          (Option.map x.logConfiguration
             ~f:UpdateExperimentTemplateLogConfigurationInput.to_value));
        ("experimentOptions",
          (Option.map x.experimentOptions
             ~f:UpdateExperimentTemplateExperimentOptionsInput.to_value));
        ("experimentReportConfiguration",
          (Option.map x.experimentReportConfiguration
             ~f:UpdateExperimentTemplateReportConfigurationInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentReportConfiguration =
        (Option.map
           ~f:UpdateExperimentTemplateReportConfigurationInput.of_xml)
          (Xml.child xml_arg0 "experimentReportConfiguration") in
      let experimentOptions =
        (Option.map ~f:UpdateExperimentTemplateExperimentOptionsInput.of_xml)
          (Xml.child xml_arg0 "experimentOptions") in
      let logConfiguration =
        (Option.map ~f:UpdateExperimentTemplateLogConfigurationInput.of_xml)
          (Xml.child xml_arg0 "logConfiguration") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "roleArn") in
      let actions =
        (Option.map ~f:UpdateExperimentTemplateActionInputMap.of_xml)
          (Xml.child xml_arg0 "actions") in
      let targets =
        (Option.map ~f:UpdateExperimentTemplateTargetInputMap.of_xml)
          (Xml.child xml_arg0 "targets") in
      let stopConditions =
        (Option.map ~f:UpdateExperimentTemplateStopConditionInputList.of_xml)
          (Xml.child xml_arg0 "stopConditions") in
      let description =
        (Option.map ~f:ExperimentTemplateDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let id =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?experimentReportConfiguration ?experimentOptions
        ?logConfiguration ?roleArn ?actions ?targets ?stopConditions
        ?description ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentReportConfiguration =
        field_map json__ "experimentReportConfiguration"
          UpdateExperimentTemplateReportConfigurationInput.of_json in
      let experimentOptions =
        field_map json__ "experimentOptions"
          UpdateExperimentTemplateExperimentOptionsInput.of_json in
      let logConfiguration =
        field_map json__ "logConfiguration"
          UpdateExperimentTemplateLogConfigurationInput.of_json in
      let roleArn = field_map json__ "roleArn" RoleArn.of_json in
      let actions =
        field_map json__ "actions"
          UpdateExperimentTemplateActionInputMap.of_json in
      let targets =
        field_map json__ "targets"
          UpdateExperimentTemplateTargetInputMap.of_json in
      let stopConditions =
        field_map json__ "stopConditions"
          UpdateExperimentTemplateStopConditionInputList.of_json in
      let description =
        field_map json__ "description" ExperimentTemplateDescription.of_json in
      let id = field_map_exn json__ "id" ExperimentTemplateId.of_json in
      make ?experimentReportConfiguration ?experimentOptions
        ?logConfiguration ?roleArn ?actions ?targets ?stopConditions
        ?description ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the specified experiment template."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes the specified tags from the specified resource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      tagKeys: TagKeyList.t option [@ocaml.doc "The tag keys to remove."]}
    let context_ = "UntagResourceRequest"
    let make ?tagKeys =
      fun ~resourceArn -> fun () -> { tagKeys; resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("tagKeys", (Option.map x.tagKeys ~f:TagKeyList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        (Option.map ~f:TagKeyList.of_xml) (Xml.child xml_arg0 "tagKeys") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ?tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map json__ "tagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ?tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes the specified tags from the specified resource."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Applies the specified tags to the specified resource."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      tags: TagMap.t [@ocaml.doc "The tags for the resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagMap.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Applies the specified tags to the specified resource."]
module StopExperimentResponse =
  struct
    type nonrec t =
      {
      experiment: Experiment.t option
        [@ocaml.doc "Information about the experiment."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experiment = fun () -> { experiment }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experiment", (Option.map x.experiment ~f:Experiment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experiment =
        (Option.map ~f:Experiment.of_xml) (Xml.child xml_arg0 "experiment") in
      make ?experiment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experiment = field_map json__ "experiment" Experiment.of_json in
      make ?experiment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stops the specified experiment."]
module StopExperimentRequest =
  struct
    type nonrec t =
      {
      id: ExperimentId.t [@ocaml.doc "The ID of the experiment."]}
    let context_ = "StopExperimentRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (ExperimentId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ExperimentId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" ExperimentId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stops the specified experiment."]
module StartExperimentResponse =
  struct
    type nonrec t =
      {
      experiment: Experiment.t option
        [@ocaml.doc "Information about the experiment."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experiment = fun () -> { experiment }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experiment", (Option.map x.experiment ~f:Experiment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experiment =
        (Option.map ~f:Experiment.of_xml) (Xml.child xml_arg0 "experiment") in
      make ?experiment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experiment = field_map json__ "experiment" Experiment.of_json in
      make ?experiment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts running an experiment from the specified experiment template."]
module StartExperimentRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      experimentTemplateId: ExperimentTemplateId.t
        [@ocaml.doc "The ID of the experiment template."];
      experimentOptions: StartExperimentExperimentOptionsInput.t option
        [@ocaml.doc "The experiment options for running the experiment."];
      tags: TagMap.t option
        [@ocaml.doc "The tags to apply to the experiment."]}
    let context_ = "StartExperimentRequest"
    let make ?experimentOptions =
      fun ?tags ->
        fun ~clientToken ->
          fun ~experimentTemplateId ->
            fun () ->
              { experimentOptions; tags; clientToken; experimentTemplateId }
    let to_value x =
      structure_to_value
        [("clientToken", (Some (ClientToken.to_value x.clientToken)));
        ("experimentTemplateId",
          (Some (ExperimentTemplateId.to_value x.experimentTemplateId)));
        ("experimentOptions",
          (Option.map x.experimentOptions
             ~f:StartExperimentExperimentOptionsInput.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let experimentOptions =
        (Option.map ~f:StartExperimentExperimentOptionsInput.of_xml)
          (Xml.child xml_arg0 "experimentOptions") in
      let experimentTemplateId =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "experimentTemplateId") in
      let clientToken =
        ClientToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clientToken") in
      make ?tags ?experimentOptions ~experimentTemplateId ~clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let experimentOptions =
        field_map json__ "experimentOptions"
          StartExperimentExperimentOptionsInput.of_json in
      let experimentTemplateId =
        field_map_exn json__ "experimentTemplateId"
          ExperimentTemplateId.of_json in
      let clientToken =
        field_map_exn json__ "clientToken" ClientToken.of_json in
      make ?tags ?experimentOptions ~experimentTemplateId ~clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts running an experiment from the specified experiment template."]
module ListTargetResourceTypesResponse =
  struct
    type nonrec t =
      {
      targetResourceTypes: TargetResourceTypeSummaryList.t option
        [@ocaml.doc "The target resource types."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetResourceTypes =
      fun ?nextToken -> fun () -> { targetResourceTypes; nextToken }
    let error_of_json name json =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetResourceTypes",
           (Option.map x.targetResourceTypes
              ~f:TargetResourceTypeSummaryList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let targetResourceTypes =
        (Option.map ~f:TargetResourceTypeSummaryList.of_xml)
          (Xml.child xml_arg0 "targetResourceTypes") in
      make ?nextToken ?targetResourceTypes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let targetResourceTypes =
        field_map json__ "targetResourceTypes"
          TargetResourceTypeSummaryList.of_json in
      make ?nextToken ?targetResourceTypes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the target resource types."]
module ListTargetResourceTypesRequest =
  struct
    type nonrec t =
      {
      maxResults: ListTargetResourceTypesMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults
              ~f:ListTargetResourceTypesMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListTargetResourceTypesMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListTargetResourceTypesMaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the target resource types."]
module ListTargetAccountConfigurationsResponse =
  struct
    type nonrec t =
      {
      targetAccountConfigurations: TargetAccountConfigurationList.t option
        [@ocaml.doc "The target account configurations."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetAccountConfigurations =
      fun ?nextToken -> fun () -> { targetAccountConfigurations; nextToken }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetAccountConfigurations",
           (Option.map x.targetAccountConfigurations
              ~f:TargetAccountConfigurationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let targetAccountConfigurations =
        (Option.map ~f:TargetAccountConfigurationList.of_xml)
          (Xml.child xml_arg0 "targetAccountConfigurations") in
      make ?nextToken ?targetAccountConfigurations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let targetAccountConfigurations =
        field_map json__ "targetAccountConfigurations"
          TargetAccountConfigurationList.of_json in
      make ?nextToken ?targetAccountConfigurations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the target account configurations of the specified experiment template."]
module ListTargetAccountConfigurationsRequest =
  struct
    type nonrec t =
      {
      experimentTemplateId: ExperimentTemplateId.t
        [@ocaml.doc "The ID of the experiment template."];
      maxResults: ListTargetAccountConfigurationsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let context_ = "ListTargetAccountConfigurationsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~experimentTemplateId ->
          fun () -> { maxResults; nextToken; experimentTemplateId }
    let to_value x =
      structure_to_value
        [("id",
           (Some (ExperimentTemplateId.to_value x.experimentTemplateId)));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListTargetAccountConfigurationsMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListTargetAccountConfigurationsMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let experimentTemplateId =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?nextToken ?maxResults ~experimentTemplateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListTargetAccountConfigurationsMaxResults.of_json in
      let experimentTemplateId =
        field_map_exn json__ "experimentTemplateId"
          ExperimentTemplateId.of_json in
      make ?nextToken ?maxResults ~experimentTemplateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the target account configurations of the specified experiment template."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagMap.t option [@ocaml.doc "The tags for the resource."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags for the specified resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags for the specified resource."]
module ListExperimentsResponse =
  struct
    type nonrec t =
      {
      experiments: ExperimentSummaryList.t option
        [@ocaml.doc "The experiments."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experiments =
      fun ?nextToken -> fun () -> { experiments; nextToken }
    let error_of_json name json =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experiments",
           (Option.map x.experiments ~f:ExperimentSummaryList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let experiments =
        (Option.map ~f:ExperimentSummaryList.of_xml)
          (Xml.child xml_arg0 "experiments") in
      make ?nextToken ?experiments ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let experiments =
        field_map json__ "experiments" ExperimentSummaryList.of_json in
      make ?nextToken ?experiments ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists your experiments."]
module ListExperimentsRequest =
  struct
    type nonrec t =
      {
      maxResults: ListExperimentsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      experimentTemplateId: ExperimentTemplateId.t option
        [@ocaml.doc "The ID of the experiment template."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?experimentTemplateId ->
          fun () -> { maxResults; nextToken; experimentTemplateId }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults ~f:ListExperimentsMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("experimentTemplateId",
          (Option.map x.experimentTemplateId ~f:ExperimentTemplateId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentTemplateId =
        (Option.map ~f:ExperimentTemplateId.of_xml)
          (Xml.child xml_arg0 "experimentTemplateId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListExperimentsMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?experimentTemplateId ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentTemplateId =
        field_map json__ "experimentTemplateId" ExperimentTemplateId.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "maxResults" ListExperimentsMaxResults.of_json in
      make ?experimentTemplateId ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists your experiments."]
module ListExperimentTemplatesResponse =
  struct
    type nonrec t =
      {
      experimentTemplates: ExperimentTemplateSummaryList.t option
        [@ocaml.doc "The experiment templates."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experimentTemplates =
      fun ?nextToken -> fun () -> { experimentTemplates; nextToken }
    let error_of_json name json =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experimentTemplates",
           (Option.map x.experimentTemplates
              ~f:ExperimentTemplateSummaryList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let experimentTemplates =
        (Option.map ~f:ExperimentTemplateSummaryList.of_xml)
          (Xml.child xml_arg0 "experimentTemplates") in
      make ?nextToken ?experimentTemplates ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let experimentTemplates =
        field_map json__ "experimentTemplates"
          ExperimentTemplateSummaryList.of_json in
      make ?nextToken ?experimentTemplates ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists your experiment templates."]
module ListExperimentTemplatesRequest =
  struct
    type nonrec t =
      {
      maxResults: ListExperimentTemplatesMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults
              ~f:ListExperimentTemplatesMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListExperimentTemplatesMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListExperimentTemplatesMaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists your experiment templates."]
module ListExperimentTargetAccountConfigurationsResponse =
  struct
    type nonrec t =
      {
      targetAccountConfigurations:
        ExperimentTargetAccountConfigurationList.t option
        [@ocaml.doc "The target account configurations."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetAccountConfigurations =
      fun ?nextToken -> fun () -> { targetAccountConfigurations; nextToken }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetAccountConfigurations",
           (Option.map x.targetAccountConfigurations
              ~f:ExperimentTargetAccountConfigurationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let targetAccountConfigurations =
        (Option.map ~f:ExperimentTargetAccountConfigurationList.of_xml)
          (Xml.child xml_arg0 "targetAccountConfigurations") in
      make ?nextToken ?targetAccountConfigurations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let targetAccountConfigurations =
        field_map json__ "targetAccountConfigurations"
          ExperimentTargetAccountConfigurationList.of_json in
      make ?nextToken ?targetAccountConfigurations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the target account configurations of the specified experiment."]
module ListExperimentTargetAccountConfigurationsRequest =
  struct
    type nonrec t =
      {
      experimentId: ExperimentId.t [@ocaml.doc "The ID of the experiment."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let context_ = "ListExperimentTargetAccountConfigurationsRequest"
    let make ?nextToken =
      fun ~experimentId -> fun () -> { nextToken; experimentId }
    let to_value x =
      structure_to_value
        [("id", (Some (ExperimentId.to_value x.experimentId)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let experimentId =
        ExperimentId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?nextToken ~experimentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let experimentId =
        field_map_exn json__ "experimentId" ExperimentId.of_json in
      make ?nextToken ~experimentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the target account configurations of the specified experiment."]
module ListExperimentResolvedTargetsResponse =
  struct
    type nonrec t =
      {
      resolvedTargets: ResolvedTargetList.t option
        [@ocaml.doc "The resolved targets."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?resolvedTargets =
      fun ?nextToken -> fun () -> { resolvedTargets; nextToken }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("resolvedTargets",
           (Option.map x.resolvedTargets ~f:ResolvedTargetList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let resolvedTargets =
        (Option.map ~f:ResolvedTargetList.of_xml)
          (Xml.child xml_arg0 "resolvedTargets") in
      make ?nextToken ?resolvedTargets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let resolvedTargets =
        field_map json__ "resolvedTargets" ResolvedTargetList.of_json in
      make ?nextToken ?resolvedTargets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the resolved targets information of the specified experiment."]
module ListExperimentResolvedTargetsRequest =
  struct
    type nonrec t =
      {
      experimentId: ExperimentId.t [@ocaml.doc "The ID of the experiment."];
      maxResults: ListExperimentResolvedTargetsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."];
      targetName: TargetName.t option [@ocaml.doc "The name of the target."]}
    let context_ = "ListExperimentResolvedTargetsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ?targetName ->
          fun ~experimentId ->
            fun () -> { maxResults; nextToken; targetName; experimentId }
    let to_value x =
      structure_to_value
        [("id", (Some (ExperimentId.to_value x.experimentId)));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListExperimentResolvedTargetsMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("targetName", (Option.map x.targetName ~f:TargetName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetName =
        (Option.map ~f:TargetName.of_xml) (Xml.child xml_arg0 "targetName") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListExperimentResolvedTargetsMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let experimentId =
        ExperimentId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?targetName ?nextToken ?maxResults ~experimentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetName = field_map json__ "targetName" TargetName.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListExperimentResolvedTargetsMaxResults.of_json in
      let experimentId =
        field_map_exn json__ "experimentId" ExperimentId.of_json in
      make ?targetName ?nextToken ?maxResults ~experimentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the resolved targets information of the specified experiment."]
module ListActionsResponse =
  struct
    type nonrec t =
      {
      actions: ActionSummaryList.t option [@ocaml.doc "The actions."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?actions = fun ?nextToken -> fun () -> { actions; nextToken }
    let error_of_json name json =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("actions", (Option.map x.actions ~f:ActionSummaryList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let actions =
        (Option.map ~f:ActionSummaryList.of_xml)
          (Xml.child xml_arg0 "actions") in
      make ?nextToken ?actions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let actions = field_map json__ "actions" ActionSummaryList.of_json in
      make ?nextToken ?actions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the available FIS actions."]
module ListActionsRequest =
  struct
    type nonrec t =
      {
      maxResults: ListActionsMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value."];
      nextToken: NextToken.t option
        [@ocaml.doc "The token for the next page of results."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults ~f:ListActionsMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListActionsMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults =
        field_map json__ "maxResults" ListActionsMaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the available FIS actions."]
module GetTargetResourceTypeResponse =
  struct
    type nonrec t =
      {
      targetResourceType: TargetResourceType.t option
        [@ocaml.doc "Information about the resource type."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetResourceType = fun () -> { targetResourceType }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetResourceType",
           (Option.map x.targetResourceType ~f:TargetResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetResourceType =
        (Option.map ~f:TargetResourceType.of_xml)
          (Xml.child xml_arg0 "targetResourceType") in
      make ?targetResourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetResourceType =
        field_map json__ "targetResourceType" TargetResourceType.of_json in
      make ?targetResourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the specified resource type."]
module GetTargetResourceTypeRequest =
  struct
    type nonrec t =
      {
      resourceType: TargetResourceTypeId.t [@ocaml.doc "The resource type."]}
    let context_ = "GetTargetResourceTypeRequest"
    let make ~resourceType = fun () -> { resourceType }
    let to_value x =
      structure_to_value
        [("resourceType",
           (Some (TargetResourceTypeId.to_value x.resourceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        TargetResourceTypeId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceType") in
      make ~resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType =
        field_map_exn json__ "resourceType" TargetResourceTypeId.of_json in
      make ~resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the specified resource type."]
module GetTargetAccountConfigurationResponse =
  struct
    type nonrec t =
      {
      targetAccountConfiguration: TargetAccountConfiguration.t option
        [@ocaml.doc "Information about the target account configuration."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetAccountConfiguration =
      fun () -> { targetAccountConfiguration }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetAccountConfiguration",
           (Option.map x.targetAccountConfiguration
              ~f:TargetAccountConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetAccountConfiguration =
        (Option.map ~f:TargetAccountConfiguration.of_xml)
          (Xml.child xml_arg0 "targetAccountConfiguration") in
      make ?targetAccountConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetAccountConfiguration =
        field_map json__ "targetAccountConfiguration"
          TargetAccountConfiguration.of_json in
      make ?targetAccountConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the specified target account configuration of the experiment template."]
module GetTargetAccountConfigurationRequest =
  struct
    type nonrec t =
      {
      experimentTemplateId: ExperimentTemplateId.t
        [@ocaml.doc "The ID of the experiment template."];
      accountId: TargetAccountId.t
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."]}
    let context_ = "GetTargetAccountConfigurationRequest"
    let make ~experimentTemplateId =
      fun ~accountId -> fun () -> { experimentTemplateId; accountId }
    let to_value x =
      structure_to_value
        [("id",
           (Some (ExperimentTemplateId.to_value x.experimentTemplateId)));
        ("accountId", (Some (TargetAccountId.to_value x.accountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        TargetAccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      let experimentTemplateId =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~accountId ~experimentTemplateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accountId =
        field_map_exn json__ "accountId" TargetAccountId.of_json in
      let experimentTemplateId =
        field_map_exn json__ "experimentTemplateId"
          ExperimentTemplateId.of_json in
      make ~accountId ~experimentTemplateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the specified target account configuration of the experiment template."]
module GetSafetyLeverResponse =
  struct
    type nonrec t =
      {
      safetyLever: SafetyLever.t option
        [@ocaml.doc "Information about the safety lever."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?safetyLever = fun () -> { safetyLever }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("safetyLever", (Option.map x.safetyLever ~f:SafetyLever.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let safetyLever =
        (Option.map ~f:SafetyLever.of_xml) (Xml.child xml_arg0 "safetyLever") in
      make ?safetyLever ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let safetyLever = field_map json__ "safetyLever" SafetyLever.of_json in
      make ?safetyLever ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the specified safety lever."]
module GetSafetyLeverRequest =
  struct
    type nonrec t =
      {
      id: SafetyLeverId.t [@ocaml.doc "The ID of the safety lever."]}
    let context_ = "GetSafetyLeverRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (SafetyLeverId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        SafetyLeverId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" SafetyLeverId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the specified safety lever."]
module GetExperimentTemplateResponse =
  struct
    type nonrec t =
      {
      experimentTemplate: ExperimentTemplate.t option
        [@ocaml.doc "Information about the experiment template."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experimentTemplate = fun () -> { experimentTemplate }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experimentTemplate",
           (Option.map x.experimentTemplate ~f:ExperimentTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentTemplate =
        (Option.map ~f:ExperimentTemplate.of_xml)
          (Xml.child xml_arg0 "experimentTemplate") in
      make ?experimentTemplate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentTemplate =
        field_map json__ "experimentTemplate" ExperimentTemplate.of_json in
      make ?experimentTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the specified experiment template."]
module GetExperimentTemplateRequest =
  struct
    type nonrec t =
      {
      id: ExperimentTemplateId.t
        [@ocaml.doc "The ID of the experiment template."]}
    let context_ = "GetExperimentTemplateRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value
        [("id", (Some (ExperimentTemplateId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" ExperimentTemplateId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the specified experiment template."]
module GetExperimentTargetAccountConfigurationResponse =
  struct
    type nonrec t =
      {
      targetAccountConfiguration:
        ExperimentTargetAccountConfiguration.t option
        [@ocaml.doc "Information about the target account configuration."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetAccountConfiguration =
      fun () -> { targetAccountConfiguration }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetAccountConfiguration",
           (Option.map x.targetAccountConfiguration
              ~f:ExperimentTargetAccountConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetAccountConfiguration =
        (Option.map ~f:ExperimentTargetAccountConfiguration.of_xml)
          (Xml.child xml_arg0 "targetAccountConfiguration") in
      make ?targetAccountConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetAccountConfiguration =
        field_map json__ "targetAccountConfiguration"
          ExperimentTargetAccountConfiguration.of_json in
      make ?targetAccountConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the specified target account configuration of the experiment."]
module GetExperimentTargetAccountConfigurationRequest =
  struct
    type nonrec t =
      {
      experimentId: ExperimentId.t [@ocaml.doc "The ID of the experiment."];
      accountId: TargetAccountId.t
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."]}
    let context_ = "GetExperimentTargetAccountConfigurationRequest"
    let make ~experimentId =
      fun ~accountId -> fun () -> { experimentId; accountId }
    let to_value x =
      structure_to_value
        [("id", (Some (ExperimentId.to_value x.experimentId)));
        ("accountId", (Some (TargetAccountId.to_value x.accountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        TargetAccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      let experimentId =
        ExperimentId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~accountId ~experimentId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accountId =
        field_map_exn json__ "accountId" TargetAccountId.of_json in
      let experimentId =
        field_map_exn json__ "experimentId" ExperimentId.of_json in
      make ~accountId ~experimentId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the specified target account configuration of the experiment."]
module GetExperimentResponse =
  struct
    type nonrec t =
      {
      experiment: Experiment.t option
        [@ocaml.doc "Information about the experiment."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experiment = fun () -> { experiment }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experiment", (Option.map x.experiment ~f:Experiment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experiment =
        (Option.map ~f:Experiment.of_xml) (Xml.child xml_arg0 "experiment") in
      make ?experiment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experiment = field_map json__ "experiment" Experiment.of_json in
      make ?experiment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the specified experiment."]
module GetExperimentRequest =
  struct
    type nonrec t =
      {
      id: ExperimentId.t [@ocaml.doc "The ID of the experiment."]}
    let context_ = "GetExperimentRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (ExperimentId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ExperimentId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" ExperimentId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the specified experiment."]
module GetActionResponse =
  struct
    type nonrec t =
      {
      action: Action.t option [@ocaml.doc "Information about the action."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?action = fun () -> { action }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("action", (Option.map x.action ~f:Action.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let action =
        (Option.map ~f:Action.of_xml) (Xml.child xml_arg0 "action") in
      make ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let action = field_map json__ "action" Action.of_json in
      make ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the specified FIS action."]
module GetActionRequest =
  struct
    type nonrec t = {
      id: ActionId.t [@ocaml.doc "The ID of the action."]}
    let context_ = "GetActionRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (ActionId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ActionId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" ActionId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets information about the specified FIS action."]
module DeleteTargetAccountConfigurationResponse =
  struct
    type nonrec t =
      {
      targetAccountConfiguration: TargetAccountConfiguration.t option
        [@ocaml.doc "Information about the target account configuration."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetAccountConfiguration =
      fun () -> { targetAccountConfiguration }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetAccountConfiguration",
           (Option.map x.targetAccountConfiguration
              ~f:TargetAccountConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetAccountConfiguration =
        (Option.map ~f:TargetAccountConfiguration.of_xml)
          (Xml.child xml_arg0 "targetAccountConfiguration") in
      make ?targetAccountConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetAccountConfiguration =
        field_map json__ "targetAccountConfiguration"
          TargetAccountConfiguration.of_json in
      make ?targetAccountConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified target account configuration of the experiment template."]
module DeleteTargetAccountConfigurationRequest =
  struct
    type nonrec t =
      {
      experimentTemplateId: ExperimentTemplateId.t
        [@ocaml.doc "The ID of the experiment template."];
      accountId: TargetAccountId.t
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."]}
    let context_ = "DeleteTargetAccountConfigurationRequest"
    let make ~experimentTemplateId =
      fun ~accountId -> fun () -> { experimentTemplateId; accountId }
    let to_value x =
      structure_to_value
        [("id",
           (Some (ExperimentTemplateId.to_value x.experimentTemplateId)));
        ("accountId", (Some (TargetAccountId.to_value x.accountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        TargetAccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      let experimentTemplateId =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~accountId ~experimentTemplateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accountId =
        field_map_exn json__ "accountId" TargetAccountId.of_json in
      let experimentTemplateId =
        field_map_exn json__ "experimentTemplateId"
          ExperimentTemplateId.of_json in
      make ~accountId ~experimentTemplateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified target account configuration of the experiment template."]
module DeleteExperimentTemplateResponse =
  struct
    type nonrec t =
      {
      experimentTemplate: ExperimentTemplate.t option
        [@ocaml.doc "Information about the experiment template."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experimentTemplate = fun () -> { experimentTemplate }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experimentTemplate",
           (Option.map x.experimentTemplate ~f:ExperimentTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentTemplate =
        (Option.map ~f:ExperimentTemplate.of_xml)
          (Xml.child xml_arg0 "experimentTemplate") in
      make ?experimentTemplate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentTemplate =
        field_map json__ "experimentTemplate" ExperimentTemplate.of_json in
      make ?experimentTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified experiment template."]
module DeleteExperimentTemplateRequest =
  struct
    type nonrec t =
      {
      id: ExperimentTemplateId.t
        [@ocaml.doc "The ID of the experiment template."]}
    let context_ = "DeleteExperimentTemplateRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value
        [("id", (Some (ExperimentTemplateId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" ExperimentTemplateId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified experiment template."]
module CreateTargetAccountConfigurationResponse =
  struct
    type nonrec t =
      {
      targetAccountConfiguration: TargetAccountConfiguration.t option
        [@ocaml.doc "Information about the target account configuration."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?targetAccountConfiguration =
      fun () -> { targetAccountConfiguration }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("targetAccountConfiguration",
           (Option.map x.targetAccountConfiguration
              ~f:TargetAccountConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetAccountConfiguration =
        (Option.map ~f:TargetAccountConfiguration.of_xml)
          (Xml.child xml_arg0 "targetAccountConfiguration") in
      make ?targetAccountConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetAccountConfiguration =
        field_map json__ "targetAccountConfiguration"
          TargetAccountConfiguration.of_json in
      make ?targetAccountConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a target account configuration for the experiment template. A target account configuration is required when accountTargeting of experimentOptions is set to multi-account. For more information, see experiment options in the Fault Injection Service User Guide."]
module CreateTargetAccountConfigurationRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      experimentTemplateId: ExperimentTemplateId.t
        [@ocaml.doc "The experiment template ID."];
      accountId: TargetAccountId.t
        [@ocaml.doc
          "The Amazon Web Services account ID of the target account."];
      roleArn: RoleArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role for the target account."];
      description: TargetAccountConfigurationDescription.t option
        [@ocaml.doc "The description of the target account."]}
    let context_ = "CreateTargetAccountConfigurationRequest"
    let make ?clientToken =
      fun ?description ->
        fun ~experimentTemplateId ->
          fun ~accountId ->
            fun ~roleArn ->
              fun () ->
                {
                  clientToken;
                  description;
                  experimentTemplateId;
                  accountId;
                  roleArn
                }
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("id", (Some (ExperimentTemplateId.to_value x.experimentTemplateId)));
        ("accountId", (Some (TargetAccountId.to_value x.accountId)));
        ("roleArn", (Some (RoleArn.to_value x.roleArn)));
        ("description",
          (Option.map x.description
             ~f:TargetAccountConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:TargetAccountConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let roleArn =
        RoleArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "roleArn") in
      let accountId =
        TargetAccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      let experimentTemplateId =
        ExperimentTemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?description ~roleArn ~accountId ~experimentTemplateId
        ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description"
          TargetAccountConfigurationDescription.of_json in
      let roleArn = field_map_exn json__ "roleArn" RoleArn.of_json in
      let accountId =
        field_map_exn json__ "accountId" TargetAccountId.of_json in
      let experimentTemplateId =
        field_map_exn json__ "experimentTemplateId"
          ExperimentTemplateId.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      make ?description ~roleArn ~accountId ~experimentTemplateId
        ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a target account configuration for the experiment template. A target account configuration is required when accountTargeting of experimentOptions is set to multi-account. For more information, see experiment options in the Fault Injection Service User Guide."]
module CreateExperimentTemplateResponse =
  struct
    type nonrec t =
      {
      experimentTemplate: ExperimentTemplate.t option
        [@ocaml.doc "Information about the experiment template."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?experimentTemplate = fun () -> { experimentTemplate }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("experimentTemplate",
           (Option.map x.experimentTemplate ~f:ExperimentTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentTemplate =
        (Option.map ~f:ExperimentTemplate.of_xml)
          (Xml.child xml_arg0 "experimentTemplate") in
      make ?experimentTemplate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentTemplate =
        field_map json__ "experimentTemplate" ExperimentTemplate.of_json in
      make ?experimentTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an experiment template. An experiment template includes the following components: Targets: A target can be a specific resource in your Amazon Web Services environment, or one or more resources that match criteria that you specify, for example, resources that have specific tags. Actions: The actions to carry out on the target. You can specify multiple actions, the duration of each action, and when to start each action during an experiment. Stop conditions: If a stop condition is triggered while an experiment is running, the experiment is automatically stopped. You can define a stop condition as a CloudWatch alarm. For more information, see experiment templates in the Fault Injection Service User Guide."]
module CreateExperimentTemplateRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      description: ExperimentTemplateDescription.t
        [@ocaml.doc "A description for the experiment template."];
      stopConditions: CreateExperimentTemplateStopConditionInputList.t
        [@ocaml.doc "The stop conditions."];
      targets: CreateExperimentTemplateTargetInputMap.t option
        [@ocaml.doc "The targets for the experiment."];
      actions: CreateExperimentTemplateActionInputMap.t
        [@ocaml.doc "The actions for the experiment."];
      roleArn: RoleArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of an IAM role that grants the FIS service permission to perform service actions on your behalf."];
      tags: TagMap.t option
        [@ocaml.doc "The tags to apply to the experiment template."];
      logConfiguration:
        CreateExperimentTemplateLogConfigurationInput.t option
        [@ocaml.doc "The configuration for experiment logging."];
      experimentOptions:
        CreateExperimentTemplateExperimentOptionsInput.t option
        [@ocaml.doc "The experiment options for the experiment template."];
      experimentReportConfiguration:
        CreateExperimentTemplateReportConfigurationInput.t option
        [@ocaml.doc
          "The experiment report configuration for the experiment template."]}
    let context_ = "CreateExperimentTemplateRequest"
    let make ?targets =
      fun ?tags ->
        fun ?logConfiguration ->
          fun ?experimentOptions ->
            fun ?experimentReportConfiguration ->
              fun ~clientToken ->
                fun ~description ->
                  fun ~stopConditions ->
                    fun ~actions ->
                      fun ~roleArn ->
                        fun () ->
                          {
                            targets;
                            tags;
                            logConfiguration;
                            experimentOptions;
                            experimentReportConfiguration;
                            clientToken;
                            description;
                            stopConditions;
                            actions;
                            roleArn
                          }
    let to_value x =
      structure_to_value
        [("clientToken", (Some (ClientToken.to_value x.clientToken)));
        ("description",
          (Some (ExperimentTemplateDescription.to_value x.description)));
        ("stopConditions",
          (Some
             (CreateExperimentTemplateStopConditionInputList.to_value
                x.stopConditions)));
        ("targets",
          (Option.map x.targets
             ~f:CreateExperimentTemplateTargetInputMap.to_value));
        ("actions",
          (Some (CreateExperimentTemplateActionInputMap.to_value x.actions)));
        ("roleArn", (Some (RoleArn.to_value x.roleArn)));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("logConfiguration",
          (Option.map x.logConfiguration
             ~f:CreateExperimentTemplateLogConfigurationInput.to_value));
        ("experimentOptions",
          (Option.map x.experimentOptions
             ~f:CreateExperimentTemplateExperimentOptionsInput.to_value));
        ("experimentReportConfiguration",
          (Option.map x.experimentReportConfiguration
             ~f:CreateExperimentTemplateReportConfigurationInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let experimentReportConfiguration =
        (Option.map
           ~f:CreateExperimentTemplateReportConfigurationInput.of_xml)
          (Xml.child xml_arg0 "experimentReportConfiguration") in
      let experimentOptions =
        (Option.map ~f:CreateExperimentTemplateExperimentOptionsInput.of_xml)
          (Xml.child xml_arg0 "experimentOptions") in
      let logConfiguration =
        (Option.map ~f:CreateExperimentTemplateLogConfigurationInput.of_xml)
          (Xml.child xml_arg0 "logConfiguration") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let roleArn =
        RoleArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "roleArn") in
      let actions =
        CreateExperimentTemplateActionInputMap.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "actions") in
      let targets =
        (Option.map ~f:CreateExperimentTemplateTargetInputMap.of_xml)
          (Xml.child xml_arg0 "targets") in
      let stopConditions =
        CreateExperimentTemplateStopConditionInputList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stopConditions") in
      let description =
        ExperimentTemplateDescription.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "description") in
      let clientToken =
        ClientToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clientToken") in
      make ?experimentReportConfiguration ?experimentOptions
        ?logConfiguration ?tags ~roleArn ~actions ?targets ~stopConditions
        ~description ~clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let experimentReportConfiguration =
        field_map json__ "experimentReportConfiguration"
          CreateExperimentTemplateReportConfigurationInput.of_json in
      let experimentOptions =
        field_map json__ "experimentOptions"
          CreateExperimentTemplateExperimentOptionsInput.of_json in
      let logConfiguration =
        field_map json__ "logConfiguration"
          CreateExperimentTemplateLogConfigurationInput.of_json in
      let tags = field_map json__ "tags" TagMap.of_json in
      let roleArn = field_map_exn json__ "roleArn" RoleArn.of_json in
      let actions =
        field_map_exn json__ "actions"
          CreateExperimentTemplateActionInputMap.of_json in
      let targets =
        field_map json__ "targets"
          CreateExperimentTemplateTargetInputMap.of_json in
      let stopConditions =
        field_map_exn json__ "stopConditions"
          CreateExperimentTemplateStopConditionInputList.of_json in
      let description =
        field_map_exn json__ "description"
          ExperimentTemplateDescription.of_json in
      let clientToken =
        field_map_exn json__ "clientToken" ClientToken.of_json in
      make ?experimentReportConfiguration ?experimentOptions
        ?logConfiguration ?tags ~roleArn ~actions ?targets ~stopConditions
        ~description ~clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an experiment template. An experiment template includes the following components: Targets: A target can be a specific resource in your Amazon Web Services environment, or one or more resources that match criteria that you specify, for example, resources that have specific tags. Actions: The actions to carry out on the target. You can specify multiple actions, the duration of each action, and when to start each action during an experiment. Stop conditions: If a stop condition is triggered while an experiment is running, the experiment is automatically stopped. You can define a stop condition as a CloudWatch alarm. For more information, see experiment templates in the Fault Injection Service User Guide."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_incident_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and findingIds =
         flag "finding-ids" (required json_arg) ~doc:"JSON FindingIdList"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_incident_findings
           (Values.BatchGetIncidentFindingsInput.make
              ~findingIds:(Values.FindingIdList.of_json findingIds)
              ~incidentRecordArn ())
           (Some Values.BatchGetIncidentFindingsOutput.to_json)
           (Some Values.BatchGetIncidentFindingsOutput.error_to_json)])
let create_replication_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and regions =
         flag "regions" (required json_arg) ~doc:"JSON RegionMapInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_replication_set
           (Values.CreateReplicationSetInput.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~regions:(Values.RegionMapInput.of_json regions) ())
           (Some Values.CreateReplicationSetOutput.to_json)
           (Some Values.CreateReplicationSetOutput.error_to_json)])
let create_response_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actions =
         flag "actions" (optional json_arg) ~doc:"JSON ActionsList"
       and chatChannel =
         flag "chat-channel" (optional json_arg) ~doc:"JSON ChatChannel"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING ResponsePlanDisplayName"
       and engagements =
         flag "engagements" (optional json_arg) ~doc:"JSON EngagementSet"
       and integrations =
         flag "integrations" (optional json_arg) ~doc:"JSON Integrations"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and incidentTemplate =
         flag "incident-template" (required json_arg)
           ~doc:"JSON IncidentTemplate"
       and name =
         flag "name" (required string) ~doc:"STRING ResponsePlanName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_response_plan
           (Values.CreateResponsePlanInput.make
              ?actions:(Option.map ~f:Values.ActionsList.of_json actions)
              ?chatChannel:(Option.map ~f:Values.ChatChannel.of_json
                              chatChannel) ?clientToken ?displayName
              ?engagements:(Option.map ~f:Values.EngagementSet.of_json
                              engagements)
              ?integrations:(Option.map ~f:Values.Integrations.of_json
                               integrations)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~incidentTemplate:(Values.IncidentTemplate.of_json
                                   incidentTemplate) ~name ())
           (Some Values.CreateResponsePlanOutput.to_json)
           (Some Values.CreateResponsePlanOutput.error_to_json)])
let create_timeline_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and eventReferences =
         flag "event-references" (optional json_arg)
           ~doc:"JSON EventReferenceList"
       and eventData =
         flag "event-data" (required string) ~doc:"STRING EventData"
       and eventTime =
         flag "event-time" (required json_arg) ~doc:"JSON Timestamp"
       and eventType =
         flag "event-type" (required string) ~doc:"STRING TimelineEventType"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_timeline_event
           (Values.CreateTimelineEventInput.make ?clientToken
              ?eventReferences:(Option.map
                                  ~f:Values.EventReferenceList.of_json
                                  eventReferences) ~eventData
              ~eventTime:(Values.Timestamp.of_json eventTime) ~eventType
              ~incidentRecordArn ())
           (Some Values.CreateTimelineEventOutput.to_json)
           (Some Values.CreateTimelineEventOutput.error_to_json)])
let delete_incident_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_incident_record
           (Values.DeleteIncidentRecordInput.make ~arn ())
           (Some Values.DeleteIncidentRecordOutput.to_json)
           (Some Values.DeleteIncidentRecordOutput.error_to_json)])
let delete_replication_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_replication_set
           (Values.DeleteReplicationSetInput.make ~arn ())
           (Some Values.DeleteReplicationSetOutput.to_json)
           (Some Values.DeleteReplicationSetOutput.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyInput.make ~policyId ~resourceArn ())
           (Some Values.DeleteResourcePolicyOutput.to_json)
           (Some Values.DeleteResourcePolicyOutput.error_to_json)])
let delete_response_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_response_plan
           (Values.DeleteResponsePlanInput.make ~arn ())
           (Some Values.DeleteResponsePlanOutput.to_json)
           (Some Values.DeleteResponsePlanOutput.error_to_json)])
let delete_timeline_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventId = flag "event-id" (required string) ~doc:"STRING UUID"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_timeline_event
           (Values.DeleteTimelineEventInput.make ~eventId ~incidentRecordArn
              ()) (Some Values.DeleteTimelineEventOutput.to_json)
           (Some Values.DeleteTimelineEventOutput.error_to_json)])
let get_incident_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_incident_record
           (Values.GetIncidentRecordInput.make ~arn ())
           (Some Values.GetIncidentRecordOutput.to_json)
           (Some Values.GetIncidentRecordOutput.error_to_json)])
let get_replication_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_replication_set
           (Values.GetReplicationSetInput.make ~arn ())
           (Some Values.GetReplicationSetOutput.to_json)
           (Some Values.GetReplicationSetOutput.error_to_json)])
let get_resource_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policies
           (Values.GetResourcePoliciesInput.make ?maxResults ?nextToken
              ~resourceArn ())
           (Some Values.GetResourcePoliciesOutput.to_json)
           (Some Values.GetResourcePoliciesOutput.error_to_json)])
let get_response_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_response_plan (Values.GetResponsePlanInput.make ~arn ())
           (Some Values.GetResponsePlanOutput.to_json)
           (Some Values.GetResponsePlanOutput.error_to_json)])
let get_timeline_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventId = flag "event-id" (required string) ~doc:"STRING UUID"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_timeline_event
           (Values.GetTimelineEventInput.make ~eventId ~incidentRecordArn ())
           (Some Values.GetTimelineEventOutput.to_json)
           (Some Values.GetTimelineEventOutput.error_to_json)])
let list_incident_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListIncidentFindingsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_incident_findings
           (Values.ListIncidentFindingsInput.make ?maxResults ?nextToken
              ~incidentRecordArn ())
           (Some Values.ListIncidentFindingsOutput.to_json)
           (Some Values.ListIncidentFindingsOutput.error_to_json)])
let list_incident_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_incident_records
           (Values.ListIncidentRecordsInput.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken ())
           (Some Values.ListIncidentRecordsOutput.to_json)
           (Some Values.ListIncidentRecordsOutput.error_to_json)])
let list_related_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_related_items
           (Values.ListRelatedItemsInput.make ?maxResults ?nextToken
              ~incidentRecordArn ())
           (Some Values.ListRelatedItemsOutput.to_json)
           (Some Values.ListRelatedItemsOutput.error_to_json)])
let list_replication_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_replication_sets
           (Values.ListReplicationSetsInput.make ?maxResults ?nextToken ())
           (Some Values.ListReplicationSetsOutput.to_json)
           (Some Values.ListReplicationSetsOutput.error_to_json)])
let list_response_plans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_response_plans
           (Values.ListResponsePlansInput.make ?maxResults ?nextToken ())
           (Some Values.ListResponsePlansOutput.to_json)
           (Some Values.ListResponsePlansOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_timeline_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON TimelineEventSort"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_timeline_events
           (Values.ListTimelineEventsInput.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxResults ?nextToken
              ?sortBy:(Option.map ~f:Values.TimelineEventSort.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ~incidentRecordArn ())
           (Some Values.ListTimelineEventsOutput.to_json)
           (Some Values.ListTimelineEventsOutput.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy = flag "policy" (required string) ~doc:"STRING Policy"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyInput.make ~policy ~resourceArn ())
           (Some Values.PutResourcePolicyOutput.to_json)
           (Some Values.PutResourcePolicyOutput.error_to_json)])
let start_incident =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and impact = flag "impact" (optional int) ~doc:"INT Impact"
       and relatedItems =
         flag "related-items" (optional json_arg) ~doc:"JSON RelatedItemList"
       and title = flag "title" (optional string) ~doc:"STRING IncidentTitle"
       and triggerDetails =
         flag "trigger-details" (optional json_arg)
           ~doc:"JSON TriggerDetails"
       and responsePlanArn =
         flag "response-plan-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_incident
           (Values.StartIncidentInput.make ?clientToken ?impact
              ?relatedItems:(Option.map ~f:Values.RelatedItemList.of_json
                               relatedItems) ?title
              ?triggerDetails:(Option.map ~f:Values.TriggerDetails.of_json
                                 triggerDetails) ~responsePlanArn ())
           (Some Values.StartIncidentOutput.to_json)
           (Some Values.StartIncidentOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_deletion_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and deletionProtected =
         flag "deletion-protected" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_deletion_protection
           (Values.UpdateDeletionProtectionInput.make ?clientToken ~arn
              ~deletionProtected ())
           (Some Values.UpdateDeletionProtectionOutput.to_json)
           (Some Values.UpdateDeletionProtectionOutput.error_to_json)])
let update_incident_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatChannel =
         flag "chat-channel" (optional json_arg) ~doc:"JSON ChatChannel"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and impact = flag "impact" (optional int) ~doc:"INT Impact"
       and notificationTargets =
         flag "notification-targets" (optional json_arg)
           ~doc:"JSON NotificationTargetSet"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON IncidentRecordStatus"
       and summary =
         flag "summary" (optional string) ~doc:"STRING IncidentSummary"
       and title = flag "title" (optional string) ~doc:"STRING IncidentTitle"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_incident_record
           (Values.UpdateIncidentRecordInput.make
              ?chatChannel:(Option.map ~f:Values.ChatChannel.of_json
                              chatChannel) ?clientToken ?impact
              ?notificationTargets:(Option.map
                                      ~f:Values.NotificationTargetSet.of_json
                                      notificationTargets)
              ?status:(Option.map ~f:Values.IncidentRecordStatus.of_json
                         status) ?summary ?title ~arn ())
           (Some Values.UpdateIncidentRecordOutput.to_json)
           (Some Values.UpdateIncidentRecordOutput.error_to_json)])
let update_related_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn"
       and relatedItemsUpdate =
         flag "related-items-update" (required json_arg)
           ~doc:"JSON RelatedItemsUpdate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_related_items
           (Values.UpdateRelatedItemsInput.make ?clientToken
              ~incidentRecordArn
              ~relatedItemsUpdate:(Values.RelatedItemsUpdate.of_json
                                     relatedItemsUpdate) ())
           (Some Values.UpdateRelatedItemsOutput.to_json)
           (Some Values.UpdateRelatedItemsOutput.error_to_json)])
let update_replication_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and actions =
         flag "actions" (required json_arg)
           ~doc:"JSON UpdateReplicationSetInputActionsList"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_replication_set
           (Values.UpdateReplicationSetInput.make ?clientToken
              ~actions:(Values.UpdateReplicationSetInputActionsList.of_json
                          actions) ~arn ())
           (Some Values.UpdateReplicationSetOutput.to_json)
           (Some Values.UpdateReplicationSetOutput.error_to_json)])
let update_response_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actions =
         flag "actions" (optional json_arg) ~doc:"JSON ActionsList"
       and chatChannel =
         flag "chat-channel" (optional json_arg) ~doc:"JSON ChatChannel"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING ResponsePlanDisplayName"
       and engagements =
         flag "engagements" (optional json_arg) ~doc:"JSON EngagementSet"
       and incidentTemplateDedupeString =
         flag "incident-template-dedupe-string" (optional string)
           ~doc:"STRING DedupeString"
       and incidentTemplateImpact =
         flag "incident-template-impact" (optional int) ~doc:"INT Impact"
       and incidentTemplateNotificationTargets =
         flag "incident-template-notification-targets" (optional json_arg)
           ~doc:"JSON NotificationTargetSet"
       and incidentTemplateSummary =
         flag "incident-template-summary" (optional string)
           ~doc:"STRING IncidentSummary"
       and incidentTemplateTags =
         flag "incident-template-tags" (optional json_arg)
           ~doc:"JSON TagMapUpdate"
       and incidentTemplateTitle =
         flag "incident-template-title" (optional string)
           ~doc:"STRING IncidentTitle"
       and integrations =
         flag "integrations" (optional json_arg) ~doc:"JSON Integrations"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_response_plan
           (Values.UpdateResponsePlanInput.make
              ?actions:(Option.map ~f:Values.ActionsList.of_json actions)
              ?chatChannel:(Option.map ~f:Values.ChatChannel.of_json
                              chatChannel) ?clientToken ?displayName
              ?engagements:(Option.map ~f:Values.EngagementSet.of_json
                              engagements) ?incidentTemplateDedupeString
              ?incidentTemplateImpact
              ?incidentTemplateNotificationTargets:(Option.map
                                                      ~f:Values.NotificationTargetSet.of_json
                                                      incidentTemplateNotificationTargets)
              ?incidentTemplateSummary
              ?incidentTemplateTags:(Option.map
                                       ~f:Values.TagMapUpdate.of_json
                                       incidentTemplateTags)
              ?incidentTemplateTitle
              ?integrations:(Option.map ~f:Values.Integrations.of_json
                               integrations) ~arn ())
           (Some Values.UpdateResponsePlanOutput.to_json)
           (Some Values.UpdateResponsePlanOutput.error_to_json)])
let update_timeline_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and eventData =
         flag "event-data" (optional string) ~doc:"STRING EventData"
       and eventReferences =
         flag "event-references" (optional json_arg)
           ~doc:"JSON EventReferenceList"
       and eventTime =
         flag "event-time" (optional json_arg) ~doc:"JSON Timestamp"
       and eventType =
         flag "event-type" (optional string) ~doc:"STRING TimelineEventType"
       and eventId = flag "event-id" (required string) ~doc:"STRING UUID"
       and incidentRecordArn =
         flag "incident-record-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_timeline_event
           (Values.UpdateTimelineEventInput.make ?clientToken ?eventData
              ?eventReferences:(Option.map
                                  ~f:Values.EventReferenceList.of_json
                                  eventReferences)
              ?eventTime:(Option.map ~f:Values.Timestamp.of_json eventTime)
              ?eventType ~eventId ~incidentRecordArn ())
           (Some Values.UpdateTimelineEventOutput.to_json)
           (Some Values.UpdateTimelineEventOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-incident-findings", batch_get_incident_findings);
    ("create-replication-set", create_replication_set);
    ("create-response-plan", create_response_plan);
    ("create-timeline-event", create_timeline_event);
    ("delete-incident-record", delete_incident_record);
    ("delete-replication-set", delete_replication_set);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-response-plan", delete_response_plan);
    ("delete-timeline-event", delete_timeline_event);
    ("get-incident-record", get_incident_record);
    ("get-replication-set", get_replication_set);
    ("get-resource-policies", get_resource_policies);
    ("get-response-plan", get_response_plan);
    ("get-timeline-event", get_timeline_event);
    ("list-incident-findings", list_incident_findings);
    ("list-incident-records", list_incident_records);
    ("list-related-items", list_related_items);
    ("list-replication-sets", list_replication_sets);
    ("list-response-plans", list_response_plans);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-timeline-events", list_timeline_events);
    ("put-resource-policy", put_resource_policy);
    ("start-incident", start_incident);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-deletion-protection", update_deletion_protection);
    ("update-incident-record", update_incident_record);
    ("update-related-items", update_related_items);
    ("update-replication-set", update_replication_set);
    ("update-response-plan", update_response_plan);
    ("update-timeline-event", update_timeline_event)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateReplicationSet: (CreateReplicationSetInput.t,
  CreateReplicationSetOutput.t, CreateReplicationSetOutput.error) t 
  | CreateResponsePlan: (CreateResponsePlanInput.t,
  CreateResponsePlanOutput.t, CreateResponsePlanOutput.error) t 
  | CreateTimelineEvent: (CreateTimelineEventInput.t,
  CreateTimelineEventOutput.t, CreateTimelineEventOutput.error) t 
  | DeleteIncidentRecord: (DeleteIncidentRecordInput.t,
  DeleteIncidentRecordOutput.t, DeleteIncidentRecordOutput.error) t 
  | DeleteReplicationSet: (DeleteReplicationSetInput.t,
  DeleteReplicationSetOutput.t, DeleteReplicationSetOutput.error) t 
  | DeleteResourcePolicy: (DeleteResourcePolicyInput.t,
  DeleteResourcePolicyOutput.t, DeleteResourcePolicyOutput.error) t 
  | DeleteResponsePlan: (DeleteResponsePlanInput.t,
  DeleteResponsePlanOutput.t, DeleteResponsePlanOutput.error) t 
  | DeleteTimelineEvent: (DeleteTimelineEventInput.t,
  DeleteTimelineEventOutput.t, DeleteTimelineEventOutput.error) t 
  | GetIncidentRecord: (GetIncidentRecordInput.t, GetIncidentRecordOutput.t,
  GetIncidentRecordOutput.error) t 
  | GetReplicationSet: (GetReplicationSetInput.t, GetReplicationSetOutput.t,
  GetReplicationSetOutput.error) t 
  | GetResourcePolicies: (GetResourcePoliciesInput.t,
  GetResourcePoliciesOutput.t, GetResourcePoliciesOutput.error) t 
  | GetResponsePlan: (GetResponsePlanInput.t, GetResponsePlanOutput.t,
  GetResponsePlanOutput.error) t 
  | GetTimelineEvent: (GetTimelineEventInput.t, GetTimelineEventOutput.t,
  GetTimelineEventOutput.error) t 
  | ListIncidentRecords: (ListIncidentRecordsInput.t,
  ListIncidentRecordsOutput.t, ListIncidentRecordsOutput.error) t 
  | ListRelatedItems: (ListRelatedItemsInput.t, ListRelatedItemsOutput.t,
  ListRelatedItemsOutput.error) t 
  | ListReplicationSets: (ListReplicationSetsInput.t,
  ListReplicationSetsOutput.t, ListReplicationSetsOutput.error) t 
  | ListResponsePlans: (ListResponsePlansInput.t, ListResponsePlansOutput.t,
  ListResponsePlansOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | ListTimelineEvents: (ListTimelineEventsInput.t,
  ListTimelineEventsOutput.t, ListTimelineEventsOutput.error) t 
  | PutResourcePolicy: (PutResourcePolicyInput.t, PutResourcePolicyOutput.t,
  PutResourcePolicyOutput.error) t 
  | StartIncident: (StartIncidentInput.t, StartIncidentOutput.t,
  StartIncidentOutput.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateDeletionProtection: (UpdateDeletionProtectionInput.t,
  UpdateDeletionProtectionOutput.t, UpdateDeletionProtectionOutput.error) t 
  | UpdateIncidentRecord: (UpdateIncidentRecordInput.t,
  UpdateIncidentRecordOutput.t, UpdateIncidentRecordOutput.error) t 
  | UpdateRelatedItems: (UpdateRelatedItemsInput.t,
  UpdateRelatedItemsOutput.t, UpdateRelatedItemsOutput.error) t 
  | UpdateReplicationSet: (UpdateReplicationSetInput.t,
  UpdateReplicationSetOutput.t, UpdateReplicationSetOutput.error) t 
  | UpdateResponsePlan: (UpdateResponsePlanInput.t,
  UpdateResponsePlanOutput.t, UpdateResponsePlanOutput.error) t 
  | UpdateTimelineEvent: (UpdateTimelineEventInput.t,
  UpdateTimelineEventOutput.t, UpdateTimelineEventOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateReplicationSet -> `POST
  | CreateResponsePlan -> `POST
  | CreateTimelineEvent -> `POST
  | DeleteIncidentRecord -> `POST
  | DeleteReplicationSet -> `POST
  | DeleteResourcePolicy -> `POST
  | DeleteResponsePlan -> `POST
  | DeleteTimelineEvent -> `POST
  | GetIncidentRecord -> `GET
  | GetReplicationSet -> `GET
  | GetResourcePolicies -> `POST
  | GetResponsePlan -> `GET
  | GetTimelineEvent -> `GET
  | ListIncidentRecords -> `POST
  | ListRelatedItems -> `POST
  | ListReplicationSets -> `POST
  | ListResponsePlans -> `POST
  | ListTagsForResource -> `GET
  | ListTimelineEvents -> `POST
  | PutResourcePolicy -> `POST
  | StartIncident -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateDeletionProtection -> `POST
  | UpdateIncidentRecord -> `POST
  | UpdateRelatedItems -> `POST
  | UpdateReplicationSet -> `POST
  | UpdateResponsePlan -> `POST
  | UpdateTimelineEvent -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateReplicationSet ->
          (Format.kasprintf Uri.of_string) "/createReplicationSet"
      | CreateResponsePlan ->
          (Format.kasprintf Uri.of_string) "/createResponsePlan"
      | CreateTimelineEvent ->
          (Format.kasprintf Uri.of_string) "/createTimelineEvent"
      | DeleteIncidentRecord ->
          (Format.kasprintf Uri.of_string) "/deleteIncidentRecord"
      | DeleteReplicationSet ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/deleteReplicationSet")
            (List.filter_opt [Some ("arn", (Arn.to_header x.arn))])
      | DeleteResourcePolicy ->
          (Format.kasprintf Uri.of_string) "/deleteResourcePolicy"
      | DeleteResponsePlan ->
          (Format.kasprintf Uri.of_string) "/deleteResponsePlan"
      | DeleteTimelineEvent ->
          (Format.kasprintf Uri.of_string) "/deleteTimelineEvent"
      | GetIncidentRecord ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/getIncidentRecord")
            (List.filter_opt [Some ("arn", (Arn.to_header x.arn))])
      | GetReplicationSet ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/getReplicationSet")
            (List.filter_opt [Some ("arn", (Arn.to_header x.arn))])
      | GetResourcePolicies ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/getResourcePolicies")
            (List.filter_opt
               [Some ("resourceArn", (Arn.to_header x.resourceArn))])
      | GetResponsePlan ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/getResponsePlan")
            (List.filter_opt [Some ("arn", (Arn.to_header x.arn))])
      | GetTimelineEvent ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/getTimelineEvent")
            (List.filter_opt
               [Some ("eventId", (UUID.to_header x.eventId));
               Some
                 ("incidentRecordArn", (Arn.to_header x.incidentRecordArn))])
      | ListIncidentRecords ->
          (Format.kasprintf Uri.of_string) "/listIncidentRecords"
      | ListRelatedItems ->
          (Format.kasprintf Uri.of_string) "/listRelatedItems"
      | ListReplicationSets ->
          (Format.kasprintf Uri.of_string) "/listReplicationSets"
      | ListResponsePlans ->
          (Format.kasprintf Uri.of_string) "/listResponsePlans"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.ListTagsForResourceRequest.resourceArn)
      | ListTimelineEvents ->
          (Format.kasprintf Uri.of_string) "/listTimelineEvents"
      | PutResourcePolicy ->
          (Format.kasprintf Uri.of_string) "/putResourcePolicy"
      | StartIncident -> (Format.kasprintf Uri.of_string) "/startIncident"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (String_.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateDeletionProtection ->
          (Format.kasprintf Uri.of_string) "/updateDeletionProtection"
      | UpdateIncidentRecord ->
          (Format.kasprintf Uri.of_string) "/updateIncidentRecord"
      | UpdateRelatedItems ->
          (Format.kasprintf Uri.of_string) "/updateRelatedItems"
      | UpdateReplicationSet ->
          (Format.kasprintf Uri.of_string) "/updateReplicationSet"
      | UpdateResponsePlan ->
          (Format.kasprintf Uri.of_string) "/updateResponsePlan"
      | UpdateTimelineEvent ->
          (Format.kasprintf Uri.of_string) "/updateTimelineEvent")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateReplicationSet ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateReplicationSetInput.clientToken
                         ~f:(fun x ->
                               ("clientToken", (ClientToken.to_value x)));
                      Some
                        ("regions",
                          (RegionMapInput.to_value
                             req.CreateReplicationSetInput.regions))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateResponsePlan ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateResponsePlanInput.actions
                         ~f:(fun x -> ("actions", (ActionsList.to_value x)));
                      Option.map req.CreateResponsePlanInput.chatChannel
                        ~f:(fun x ->
                              ("chatChannel", (ChatChannel.to_value x)));
                      Option.map req.CreateResponsePlanInput.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)));
                      Option.map req.CreateResponsePlanInput.displayName
                        ~f:(fun x ->
                              ("displayName",
                                (ResponsePlanDisplayName.to_value x)));
                      Option.map req.CreateResponsePlanInput.engagements
                        ~f:(fun x ->
                              ("engagements", (EngagementSet.to_value x)));
                      Some
                        ("incidentTemplate",
                          (IncidentTemplate.to_value
                             req.CreateResponsePlanInput.incidentTemplate));
                      Some
                        ("name",
                          (ResponsePlanName.to_value
                             req.CreateResponsePlanInput.name));
                      Option.map req.CreateResponsePlanInput.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateTimelineEvent ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateTimelineEventInput.clientToken
                         ~f:(fun x ->
                               ("clientToken", (ClientToken.to_value x)));
                      Some
                        ("eventData",
                          (EventData.to_value
                             req.CreateTimelineEventInput.eventData));
                      Some
                        ("eventTime",
                          (Timestamp.to_value
                             req.CreateTimelineEventInput.eventTime));
                      Some
                        ("eventType",
                          (TimelineEventType.to_value
                             req.CreateTimelineEventInput.eventType));
                      Some
                        ("incidentRecordArn",
                          (Arn.to_value
                             req.CreateTimelineEventInput.incidentRecordArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteIncidentRecord ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("arn",
                           (Arn.to_value req.DeleteIncidentRecordInput.arn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteReplicationSet ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteResourcePolicy ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("policyId",
                           (PolicyId.to_value
                              req.DeleteResourcePolicyInput.policyId));
                      Some
                        ("resourceArn",
                          (Arn.to_value
                             req.DeleteResourcePolicyInput.resourceArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteResponsePlan ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("arn",
                           (Arn.to_value req.DeleteResponsePlanInput.arn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteTimelineEvent ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("eventId",
                           (UUID.to_value
                              req.DeleteTimelineEventInput.eventId));
                      Some
                        ("incidentRecordArn",
                          (Arn.to_value
                             req.DeleteTimelineEventInput.incidentRecordArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetIncidentRecord ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetReplicationSet ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetResourcePolicies ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetResourcePoliciesInput.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.GetResourcePoliciesInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetResponsePlan ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTimelineEvent ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListIncidentRecords ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListIncidentRecordsInput.filters
                         ~f:(fun x -> ("filters", (FilterList.to_value x)));
                      Option.map req.ListIncidentRecordsInput.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListIncidentRecordsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListRelatedItems ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("incidentRecordArn",
                           (Arn.to_value
                              req.ListRelatedItemsInput.incidentRecordArn));
                      Option.map req.ListRelatedItemsInput.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListRelatedItemsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListReplicationSets ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListReplicationSetsInput.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListReplicationSetsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListResponsePlans ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListResponsePlansInput.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListResponsePlansInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTimelineEvents ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListTimelineEventsInput.filters
                         ~f:(fun x -> ("filters", (FilterList.to_value x)));
                      Some
                        ("incidentRecordArn",
                          (Arn.to_value
                             req.ListTimelineEventsInput.incidentRecordArn));
                      Option.map req.ListTimelineEventsInput.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListTimelineEventsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListTimelineEventsInput.sortBy
                        ~f:(fun x ->
                              ("sortBy", (TimelineEventSort.to_value x)));
                      Option.map req.ListTimelineEventsInput.sortOrder
                        ~f:(fun x -> ("sortOrder", (SortOrder.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutResourcePolicy ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("policy",
                           (Policy.to_value req.PutResourcePolicyInput.policy));
                      Some
                        ("resourceArn",
                          (Arn.to_value
                             req.PutResourcePolicyInput.resourceArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartIncident ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.StartIncidentInput.clientToken
                         ~f:(fun x ->
                               ("clientToken", (ClientToken.to_value x)));
                      Option.map req.StartIncidentInput.impact
                        ~f:(fun x -> ("impact", (Impact.to_value x)));
                      Option.map req.StartIncidentInput.relatedItems
                        ~f:(fun x ->
                              ("relatedItems", (RelatedItemList.to_value x)));
                      Some
                        ("responsePlanArn",
                          (Arn.to_value
                             req.StartIncidentInput.responsePlanArn));
                      Option.map req.StartIncidentInput.title
                        ~f:(fun x -> ("title", (IncidentTitle.to_value x)));
                      Option.map req.StartIncidentInput.triggerDetails
                        ~f:(fun x ->
                              ("triggerDetails", (TriggerDetails.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateDeletionProtection ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("arn",
                           (Arn.to_value
                              req.UpdateDeletionProtectionInput.arn));
                      Option.map
                        req.UpdateDeletionProtectionInput.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)));
                      Some
                        ("deletionProtected",
                          (Boolean.to_value
                             req.UpdateDeletionProtectionInput.deletionProtected))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateIncidentRecord ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("arn",
                           (Arn.to_value req.UpdateIncidentRecordInput.arn));
                      Option.map req.UpdateIncidentRecordInput.chatChannel
                        ~f:(fun x ->
                              ("chatChannel", (ChatChannel.to_value x)));
                      Option.map req.UpdateIncidentRecordInput.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)));
                      Option.map req.UpdateIncidentRecordInput.impact
                        ~f:(fun x -> ("impact", (Impact.to_value x)));
                      Option.map
                        req.UpdateIncidentRecordInput.notificationTargets
                        ~f:(fun x ->
                              ("notificationTargets",
                                (NotificationTargetSet.to_value x)));
                      Option.map req.UpdateIncidentRecordInput.status
                        ~f:(fun x ->
                              ("status", (IncidentRecordStatus.to_value x)));
                      Option.map req.UpdateIncidentRecordInput.summary
                        ~f:(fun x ->
                              ("summary", (IncidentSummary.to_value x)));
                      Option.map req.UpdateIncidentRecordInput.title
                        ~f:(fun x -> ("title", (IncidentTitle.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateRelatedItems ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.UpdateRelatedItemsInput.clientToken
                         ~f:(fun x ->
                               ("clientToken", (ClientToken.to_value x)));
                      Some
                        ("incidentRecordArn",
                          (Arn.to_value
                             req.UpdateRelatedItemsInput.incidentRecordArn));
                      Some
                        ("relatedItemsUpdate",
                          (RelatedItemsUpdate.to_value
                             req.UpdateRelatedItemsInput.relatedItemsUpdate))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateReplicationSet ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("actions",
                           (UpdateReplicationSetInputActionsList.to_value
                              req.UpdateReplicationSetInput.actions));
                      Some
                        ("arn",
                          (Arn.to_value req.UpdateReplicationSetInput.arn));
                      Option.map req.UpdateReplicationSetInput.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateResponsePlan ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.UpdateResponsePlanInput.actions
                         ~f:(fun x -> ("actions", (ActionsList.to_value x)));
                      Some
                        ("arn",
                          (Arn.to_value req.UpdateResponsePlanInput.arn));
                      Option.map req.UpdateResponsePlanInput.chatChannel
                        ~f:(fun x ->
                              ("chatChannel", (ChatChannel.to_value x)));
                      Option.map req.UpdateResponsePlanInput.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)));
                      Option.map req.UpdateResponsePlanInput.displayName
                        ~f:(fun x ->
                              ("displayName",
                                (ResponsePlanDisplayName.to_value x)));
                      Option.map req.UpdateResponsePlanInput.engagements
                        ~f:(fun x ->
                              ("engagements", (EngagementSet.to_value x)));
                      Option.map
                        req.UpdateResponsePlanInput.incidentTemplateDedupeString
                        ~f:(fun x ->
                              ("incidentTemplateDedupeString",
                                (DedupeString.to_value x)));
                      Option.map
                        req.UpdateResponsePlanInput.incidentTemplateImpact
                        ~f:(fun x ->
                              ("incidentTemplateImpact", (Impact.to_value x)));
                      Option.map
                        req.UpdateResponsePlanInput.incidentTemplateNotificationTargets
                        ~f:(fun x ->
                              ("incidentTemplateNotificationTargets",
                                (NotificationTargetSet.to_value x)));
                      Option.map
                        req.UpdateResponsePlanInput.incidentTemplateSummary
                        ~f:(fun x ->
                              ("incidentTemplateSummary",
                                (IncidentSummary.to_value x)));
                      Option.map
                        req.UpdateResponsePlanInput.incidentTemplateTitle
                        ~f:(fun x ->
                              ("incidentTemplateTitle",
                                (IncidentTitle.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateTimelineEvent ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.UpdateTimelineEventInput.clientToken
                         ~f:(fun x ->
                               ("clientToken", (ClientToken.to_value x)));
                      Option.map req.UpdateTimelineEventInput.eventData
                        ~f:(fun x -> ("eventData", (EventData.to_value x)));
                      Some
                        ("eventId",
                          (UUID.to_value req.UpdateTimelineEventInput.eventId));
                      Option.map req.UpdateTimelineEventInput.eventTime
                        ~f:(fun x -> ("eventTime", (Timestamp.to_value x)));
                      Option.map req.UpdateTimelineEventInput.eventType
                        ~f:(fun x ->
                              ("eventType", (TimelineEventType.to_value x)));
                      Some
                        ("incidentRecordArn",
                          (Arn.to_value
                             req.UpdateTimelineEventInput.incidentRecordArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | CreateReplicationSet ->
      (match resp with
       | Error err ->
           handle_error err (Some CreateReplicationSetOutput.error_of_json)
       | Ok resp ->
           Ok (CreateReplicationSetOutput.of_json (response_to_json resp)))
  | CreateResponsePlan ->
      (match resp with
       | Error err ->
           handle_error err (Some CreateResponsePlanOutput.error_of_json)
       | Ok resp ->
           Ok (CreateResponsePlanOutput.of_json (response_to_json resp)))
  | CreateTimelineEvent ->
      (match resp with
       | Error err ->
           handle_error err (Some CreateTimelineEventOutput.error_of_json)
       | Ok resp ->
           Ok (CreateTimelineEventOutput.of_json (response_to_json resp)))
  | DeleteIncidentRecord ->
      (match resp with
       | Error err ->
           handle_error err (Some DeleteIncidentRecordOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeleteIncidentRecordOutput.of_header_and_body (headers, ())))
  | DeleteReplicationSet ->
      (match resp with
       | Error err ->
           handle_error err (Some DeleteReplicationSetOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeleteReplicationSetOutput.of_header_and_body (headers, ())))
  | DeleteResourcePolicy ->
      (match resp with
       | Error err ->
           handle_error err (Some DeleteResourcePolicyOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeleteResourcePolicyOutput.of_header_and_body (headers, ())))
  | DeleteResponsePlan ->
      (match resp with
       | Error err ->
           handle_error err (Some DeleteResponsePlanOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeleteResponsePlanOutput.of_header_and_body (headers, ())))
  | DeleteTimelineEvent ->
      (match resp with
       | Error err ->
           handle_error err (Some DeleteTimelineEventOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeleteTimelineEventOutput.of_header_and_body (headers, ())))
  | GetIncidentRecord ->
      (match resp with
       | Error err ->
           handle_error err (Some GetIncidentRecordOutput.error_of_json)
       | Ok resp ->
           Ok (GetIncidentRecordOutput.of_json (response_to_json resp)))
  | GetReplicationSet ->
      (match resp with
       | Error err ->
           handle_error err (Some GetReplicationSetOutput.error_of_json)
       | Ok resp ->
           Ok (GetReplicationSetOutput.of_json (response_to_json resp)))
  | GetResourcePolicies ->
      (match resp with
       | Error err ->
           handle_error err (Some GetResourcePoliciesOutput.error_of_json)
       | Ok resp ->
           Ok (GetResourcePoliciesOutput.of_json (response_to_json resp)))
  | GetResponsePlan ->
      (match resp with
       | Error err ->
           handle_error err (Some GetResponsePlanOutput.error_of_json)
       | Ok resp ->
           Ok (GetResponsePlanOutput.of_json (response_to_json resp)))
  | GetTimelineEvent ->
      (match resp with
       | Error err ->
           handle_error err (Some GetTimelineEventOutput.error_of_json)
       | Ok resp ->
           Ok (GetTimelineEventOutput.of_json (response_to_json resp)))
  | ListIncidentRecords ->
      (match resp with
       | Error err ->
           handle_error err (Some ListIncidentRecordsOutput.error_of_json)
       | Ok resp ->
           Ok (ListIncidentRecordsOutput.of_json (response_to_json resp)))
  | ListRelatedItems ->
      (match resp with
       | Error err ->
           handle_error err (Some ListRelatedItemsOutput.error_of_json)
       | Ok resp ->
           Ok (ListRelatedItemsOutput.of_json (response_to_json resp)))
  | ListReplicationSets ->
      (match resp with
       | Error err ->
           handle_error err (Some ListReplicationSetsOutput.error_of_json)
       | Ok resp ->
           Ok (ListReplicationSetsOutput.of_json (response_to_json resp)))
  | ListResponsePlans ->
      (match resp with
       | Error err ->
           handle_error err (Some ListResponsePlansOutput.error_of_json)
       | Ok resp ->
           Ok (ListResponsePlansOutput.of_json (response_to_json resp)))
  | ListTagsForResource ->
      (match resp with
       | Error err ->
           handle_error err (Some ListTagsForResourceResponse.error_of_json)
       | Ok resp ->
           Ok (ListTagsForResourceResponse.of_json (response_to_json resp)))
  | ListTimelineEvents ->
      (match resp with
       | Error err ->
           handle_error err (Some ListTimelineEventsOutput.error_of_json)
       | Ok resp ->
           Ok (ListTimelineEventsOutput.of_json (response_to_json resp)))
  | PutResourcePolicy ->
      (match resp with
       | Error err ->
           handle_error err (Some PutResourcePolicyOutput.error_of_json)
       | Ok resp ->
           Ok (PutResourcePolicyOutput.of_json (response_to_json resp)))
  | StartIncident ->
      (match resp with
       | Error err ->
           handle_error err (Some StartIncidentOutput.error_of_json)
       | Ok resp -> Ok (StartIncidentOutput.of_json (response_to_json resp)))
  | TagResource ->
      (match resp with
       | Error err ->
           handle_error err (Some TagResourceResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (TagResourceResponse.of_header_and_body (headers, ())))
  | UntagResource ->
      (match resp with
       | Error err ->
           handle_error err (Some UntagResourceResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (UntagResourceResponse.of_header_and_body (headers, ())))
  | UpdateDeletionProtection ->
      (match resp with
       | Error err ->
           handle_error err
             (Some UpdateDeletionProtectionOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok
             (UpdateDeletionProtectionOutput.of_header_and_body (headers, ())))
  | UpdateIncidentRecord ->
      (match resp with
       | Error err ->
           handle_error err (Some UpdateIncidentRecordOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (UpdateIncidentRecordOutput.of_header_and_body (headers, ())))
  | UpdateRelatedItems ->
      (match resp with
       | Error err ->
           handle_error err (Some UpdateRelatedItemsOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (UpdateRelatedItemsOutput.of_header_and_body (headers, ())))
  | UpdateReplicationSet ->
      (match resp with
       | Error err ->
           handle_error err (Some UpdateReplicationSetOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (UpdateReplicationSetOutput.of_header_and_body (headers, ())))
  | UpdateResponsePlan ->
      (match resp with
       | Error err ->
           handle_error err (Some UpdateResponsePlanOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (UpdateResponsePlanOutput.of_header_and_body (headers, ())))
  | UpdateTimelineEvent ->
      (match resp with
       | Error err ->
           handle_error err (Some UpdateTimelineEventOutput.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (UpdateTimelineEventOutput.of_header_and_body (headers, ())))
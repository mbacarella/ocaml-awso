(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DeleteLexicon: (DeleteLexiconInput.t, DeleteLexiconOutput.t,
  DeleteLexiconOutput.error) t 
  | DescribeVoices: (DescribeVoicesInput.t, DescribeVoicesOutput.t,
  DescribeVoicesOutput.error) t 
  | GetLexicon: (GetLexiconInput.t, GetLexiconOutput.t,
  GetLexiconOutput.error) t 
  | GetSpeechSynthesisTask: (GetSpeechSynthesisTaskInput.t,
  GetSpeechSynthesisTaskOutput.t, GetSpeechSynthesisTaskOutput.error) t 
  | ListLexicons: (ListLexiconsInput.t, ListLexiconsOutput.t,
  ListLexiconsOutput.error) t 
  | ListSpeechSynthesisTasks: (ListSpeechSynthesisTasksInput.t,
  ListSpeechSynthesisTasksOutput.t, ListSpeechSynthesisTasksOutput.error) t 
  | PutLexicon: (PutLexiconInput.t, PutLexiconOutput.t,
  PutLexiconOutput.error) t 
  | StartSpeechSynthesisTask: (StartSpeechSynthesisTaskInput.t,
  StartSpeechSynthesisTaskOutput.t, StartSpeechSynthesisTaskOutput.error) t 
  | SynthesizeSpeech: (SynthesizeSpeechInput.t, SynthesizeSpeechOutput.t,
  SynthesizeSpeechOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DeleteLexicon -> `DELETE
  | DescribeVoices -> `GET
  | GetLexicon -> `GET
  | GetSpeechSynthesisTask -> `GET
  | ListLexicons -> `GET
  | ListSpeechSynthesisTasks -> `GET
  | PutLexicon -> `PUT
  | StartSpeechSynthesisTask -> `POST
  | SynthesizeSpeech -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DeleteLexicon ->
          (Format.kasprintf Uri.of_string) "/v1/lexicons/%s"
            (LexiconName.to_header x.DeleteLexiconInput.name)
      | DescribeVoices ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v1/voices")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("Engine", (Engine.to_header v)))
                  x.engine;
               Option.map
                 ~f:(fun v -> ("LanguageCode", (LanguageCode.to_header v)))
                 x.languageCode;
               Option.map
                 ~f:(fun v ->
                       ("IncludeAdditionalLanguageCodes",
                         (IncludeAdditionalLanguageCodes.to_header v)))
                 x.includeAdditionalLanguageCodes;
               Option.map
                 ~f:(fun v -> ("NextToken", (NextToken.to_header v)))
                 x.nextToken])
      | GetLexicon ->
          (Format.kasprintf Uri.of_string) "/v1/lexicons/%s"
            (LexiconName.to_header x.GetLexiconInput.name)
      | GetSpeechSynthesisTask ->
          (Format.kasprintf Uri.of_string) "/v1/synthesisTasks/%s"
            (TaskId.to_header x.GetSpeechSynthesisTaskInput.taskId)
      | ListLexicons ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v1/lexicons")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("NextToken", (NextToken.to_header v)))
                  x.nextToken])
      | ListSpeechSynthesisTasks ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v1/synthesisTasks")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("MaxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("NextToken", (NextToken.to_header v)))
                 x.nextToken;
               Option.map ~f:(fun v -> ("Status", (TaskStatus.to_header v)))
                 x.status])
      | PutLexicon ->
          (Format.kasprintf Uri.of_string) "/v1/lexicons/%s"
            (LexiconName.to_header x.PutLexiconInput.name)
      | StartSpeechSynthesisTask ->
          (Format.kasprintf Uri.of_string) "/v1/synthesisTasks"
      | SynthesizeSpeech -> (Format.kasprintf Uri.of_string) "/v1/speech")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DeleteLexicon -> Awso.Http.Request.make (method_of_endpoint endp)
  | DescribeVoices ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetLexicon ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSpeechSynthesisTask ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListLexicons ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSpeechSynthesisTasks ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutLexicon -> Awso.Http.Request.make (method_of_endpoint endp)
  | StartSpeechSynthesisTask ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.StartSpeechSynthesisTaskInput.engine
                         ~f:(fun x -> ("Engine", (Engine.to_value x)));
                      Option.map
                        req.StartSpeechSynthesisTaskInput.languageCode
                        ~f:(fun x ->
                              ("LanguageCode", (LanguageCode.to_value x)));
                      Option.map
                        req.StartSpeechSynthesisTaskInput.lexiconNames
                        ~f:(fun x ->
                              ("LexiconNames", (LexiconNameList.to_value x)));
                      Some
                        ("OutputFormat",
                          (OutputFormat.to_value
                             req.StartSpeechSynthesisTaskInput.outputFormat));
                      Some
                        ("OutputS3BucketName",
                          (OutputS3BucketName.to_value
                             req.StartSpeechSynthesisTaskInput.outputS3BucketName));
                      Option.map
                        req.StartSpeechSynthesisTaskInput.outputS3KeyPrefix
                        ~f:(fun x ->
                              ("OutputS3KeyPrefix",
                                (OutputS3KeyPrefix.to_value x)));
                      Option.map req.StartSpeechSynthesisTaskInput.sampleRate
                        ~f:(fun x -> ("SampleRate", (SampleRate.to_value x)));
                      Option.map
                        req.StartSpeechSynthesisTaskInput.snsTopicArn
                        ~f:(fun x ->
                              ("SnsTopicArn", (SnsTopicArn.to_value x)));
                      Option.map
                        req.StartSpeechSynthesisTaskInput.speechMarkTypes
                        ~f:(fun x ->
                              ("SpeechMarkTypes",
                                (SpeechMarkTypeList.to_value x)));
                      Some
                        ("Text",
                          (Text.to_value
                             req.StartSpeechSynthesisTaskInput.text));
                      Option.map req.StartSpeechSynthesisTaskInput.textType
                        ~f:(fun x -> ("TextType", (TextType.to_value x)));
                      Some
                        ("VoiceId",
                          (VoiceId.to_value
                             req.StartSpeechSynthesisTaskInput.voiceId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SynthesizeSpeech ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.SynthesizeSpeechInput.engine
                         ~f:(fun x -> ("Engine", (Engine.to_value x)));
                      Option.map req.SynthesizeSpeechInput.languageCode
                        ~f:(fun x ->
                              ("LanguageCode", (LanguageCode.to_value x)));
                      Option.map req.SynthesizeSpeechInput.lexiconNames
                        ~f:(fun x ->
                              ("LexiconNames", (LexiconNameList.to_value x)));
                      Some
                        ("OutputFormat",
                          (OutputFormat.to_value
                             req.SynthesizeSpeechInput.outputFormat));
                      Option.map req.SynthesizeSpeechInput.sampleRate
                        ~f:(fun x -> ("SampleRate", (SampleRate.to_value x)));
                      Option.map req.SynthesizeSpeechInput.speechMarkTypes
                        ~f:(fun x ->
                              ("SpeechMarkTypes",
                                (SpeechMarkTypeList.to_value x)));
                      Some
                        ("Text",
                          (Text.to_value req.SynthesizeSpeechInput.text));
                      Option.map req.SynthesizeSpeechInput.textType
                        ~f:(fun x -> ("TextType", (TextType.to_value x)));
                      Some
                        ("VoiceId",
                          (VoiceId.to_value req.SynthesizeSpeechInput.voiceId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | DeleteLexicon ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteLexiconOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteLexiconOutput.error_of_json))
  | DescribeVoices ->
      if is_success
      then Ok (DescribeVoicesOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DescribeVoicesOutput.error_of_json))
  | GetLexicon ->
      if is_success
      then Ok (GetLexiconOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetLexiconOutput.error_of_json))
  | GetSpeechSynthesisTask ->
      if is_success
      then Ok (GetSpeechSynthesisTaskOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetSpeechSynthesisTaskOutput.error_of_json))
  | ListLexicons ->
      if is_success
      then Ok (ListLexiconsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListLexiconsOutput.error_of_json))
  | ListSpeechSynthesisTasks ->
      if is_success
      then
        Ok (ListSpeechSynthesisTasksOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListSpeechSynthesisTasksOutput.error_of_json))
  | PutLexicon ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PutLexiconOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some PutLexiconOutput.error_of_json))
  | StartSpeechSynthesisTask ->
      if is_success
      then
        Ok (StartSpeechSynthesisTaskOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some StartSpeechSynthesisTaskOutput.error_of_json))
  | SynthesizeSpeech ->
      if is_success
      then
        let body = AudioStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (SynthesizeSpeechOutput.of_header_and_body (headers, body))
      else
        Error (parse_aws_error (Some SynthesizeSpeechOutput.error_of_json))
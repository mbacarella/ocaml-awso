(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactChannelId =
         flag "contact-channel-id" (optional string)
           ~doc:"STRING SsmContactsArn"
       and note = flag "note" (optional string) ~doc:"STRING ReceiptInfo"
       and acceptCodeValidation =
         flag "accept-code-validation" (optional json_arg)
           ~doc:"JSON AcceptCodeValidation"
       and pageId =
         flag "page-id" (required string) ~doc:"STRING SsmContactsArn"
       and acceptType =
         flag "accept-type" (required json_arg) ~doc:"JSON AcceptType"
       and acceptCode =
         flag "accept-code" (required string) ~doc:"STRING AcceptCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_page
           (Values.AcceptPageRequest.make ?contactChannelId ?note
              ?acceptCodeValidation:(Option.map
                                       ~f:Values.AcceptCodeValidation.of_json
                                       acceptCodeValidation) ~pageId
              ~acceptType:(Values.AcceptType.of_json acceptType) ~acceptCode
              ()) (Some Values.AcceptPageResult.to_json)
           (Some Values.AcceptPageResult.error_to_json)])
let activate_contact_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactChannelId =
         flag "contact-channel-id" (required string)
           ~doc:"STRING SsmContactsArn"
       and activationCode =
         flag "activation-code" (required string)
           ~doc:"STRING ActivationCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_contact_channel
           (Values.ActivateContactChannelRequest.make ~contactChannelId
              ~activationCode ())
           (Some Values.ActivateContactChannelResult.to_json)
           (Some Values.ActivateContactChannelResult.error_to_json)])
let create_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ContactName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsList"
       and idempotencyToken =
         flag "idempotency-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and alias = flag "alias" (required string) ~doc:"STRING ContactAlias"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON ContactType"
       and plan = flag "plan" (required json_arg) ~doc:"JSON Plan" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_contact
           (Values.CreateContactRequest.make ?displayName
              ?tags:(Option.map ~f:Values.TagsList.of_json tags)
              ?idempotencyToken ~alias
              ~type_:(Values.ContactType.of_json type_)
              ~plan:(Values.Plan.of_json plan) ())
           (Some Values.CreateContactResult.to_json)
           (Some Values.CreateContactResult.error_to_json)])
let create_contact_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deferActivation =
         flag "defer-activation" (optional bool) ~doc:"BOOL DeferActivation"
       and idempotencyToken =
         flag "idempotency-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING SsmContactsArn"
       and name = flag "name" (required string) ~doc:"STRING ChannelName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON ChannelType"
       and deliveryAddress =
         flag "delivery-address" (required json_arg)
           ~doc:"JSON ContactChannelAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_contact_channel
           (Values.CreateContactChannelRequest.make ?deferActivation
              ?idempotencyToken ~contactId ~name
              ~type_:(Values.ChannelType.of_json type_)
              ~deliveryAddress:(Values.ContactChannelAddress.of_json
                                  deliveryAddress) ())
           (Some Values.CreateContactChannelResult.to_json)
           (Some Values.CreateContactChannelResult.error_to_json)])
let deactivate_contact_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactChannelId =
         flag "contact-channel-id" (required string)
           ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_contact_channel
           (Values.DeactivateContactChannelRequest.make ~contactChannelId ())
           (Some Values.DeactivateContactChannelResult.to_json)
           (Some Values.DeactivateContactChannelResult.error_to_json)])
let delete_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_contact (Values.DeleteContactRequest.make ~contactId ())
           (Some Values.DeleteContactResult.to_json)
           (Some Values.DeleteContactResult.error_to_json)])
let delete_contact_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactChannelId =
         flag "contact-channel-id" (required string)
           ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_contact_channel
           (Values.DeleteContactChannelRequest.make ~contactChannelId ())
           (Some Values.DeleteContactChannelResult.to_json)
           (Some Values.DeleteContactChannelResult.error_to_json)])
let describe_engagement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engagementId =
         flag "engagement-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_engagement
           (Values.DescribeEngagementRequest.make ~engagementId ())
           (Some Values.DescribeEngagementResult.to_json)
           (Some Values.DescribeEngagementResult.error_to_json)])
let describe_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageId =
         flag "page-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_page (Values.DescribePageRequest.make ~pageId ())
           (Some Values.DescribePageResult.to_json)
           (Some Values.DescribePageResult.error_to_json)])
let get_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact (Values.GetContactRequest.make ~contactId ())
           (Some Values.GetContactResult.to_json)
           (Some Values.GetContactResult.error_to_json)])
let get_contact_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactChannelId =
         flag "contact-channel-id" (required string)
           ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact_channel
           (Values.GetContactChannelRequest.make ~contactChannelId ())
           (Some Values.GetContactChannelResult.to_json)
           (Some Values.GetContactChannelResult.error_to_json)])
let get_contact_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactArn =
         flag "contact-arn" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact_policy
           (Values.GetContactPolicyRequest.make ~contactArn ())
           (Some Values.GetContactPolicyResult.to_json)
           (Some Values.GetContactPolicyResult.error_to_json)])
let list_contact_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contact_channels
           (Values.ListContactChannelsRequest.make ?nextToken ?maxResults
              ~contactId ()) (Some Values.ListContactChannelsResult.to_json)
           (Some Values.ListContactChannelsResult.error_to_json)])
let list_contacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and aliasPrefix =
         flag "alias-prefix" (optional string) ~doc:"STRING ContactAlias"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON ContactType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contacts
           (Values.ListContactsRequest.make ?nextToken ?maxResults
              ?aliasPrefix
              ?type_:(Option.map ~f:Values.ContactType.of_json type_) ())
           (Some Values.ListContactsResult.to_json)
           (Some Values.ListContactsResult.error_to_json)])
let list_engagements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and incidentId =
         flag "incident-id" (optional string) ~doc:"STRING IncidentId"
       and timeRangeValue =
         flag "time-range-value" (optional json_arg) ~doc:"JSON TimeRange" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engagements
           (Values.ListEngagementsRequest.make ?nextToken ?maxResults
              ?incidentId
              ?timeRangeValue:(Option.map ~f:Values.TimeRange.of_json
                                 timeRangeValue) ())
           (Some Values.ListEngagementsResult.to_json)
           (Some Values.ListEngagementsResult.error_to_json)])
let list_page_receipts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and pageId =
         flag "page-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_page_receipts
           (Values.ListPageReceiptsRequest.make ?nextToken ?maxResults
              ~pageId ()) (Some Values.ListPageReceiptsResult.to_json)
           (Some Values.ListPageReceiptsResult.error_to_json)])
let list_pages_by_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pages_by_contact
           (Values.ListPagesByContactRequest.make ?nextToken ?maxResults
              ~contactId ()) (Some Values.ListPagesByContactResult.to_json)
           (Some Values.ListPagesByContactResult.error_to_json)])
let list_pages_by_engagement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and engagementId =
         flag "engagement-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pages_by_engagement
           (Values.ListPagesByEngagementRequest.make ?nextToken ?maxResults
              ~engagementId ())
           (Some Values.ListPagesByEngagementResult.to_json)
           (Some Values.ListPagesByEngagementResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let put_contact_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactArn =
         flag "contact-arn" (required string) ~doc:"STRING SsmContactsArn"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_contact_policy
           (Values.PutContactPolicyRequest.make ~contactArn ~policy ())
           (Some Values.PutContactPolicyResult.to_json)
           (Some Values.PutContactPolicyResult.error_to_json)])
let send_activation_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactChannelId =
         flag "contact-channel-id" (required string)
           ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_activation_code
           (Values.SendActivationCodeRequest.make ~contactChannelId ())
           (Some Values.SendActivationCodeResult.to_json)
           (Some Values.SendActivationCodeResult.error_to_json)])
let start_engagement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and publicSubject =
         flag "public-subject" (optional string) ~doc:"STRING PublicSubject"
       and publicContent =
         flag "public-content" (optional string) ~doc:"STRING PublicContent"
       and incidentId =
         flag "incident-id" (optional string) ~doc:"STRING IncidentId"
       and idempotencyToken =
         flag "idempotency-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING SsmContactsArn"
       and sender = flag "sender" (required string) ~doc:"STRING Sender"
       and subject = flag "subject" (required string) ~doc:"STRING Subject"
       and content = flag "content" (required string) ~doc:"STRING Content" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_engagement
           (Values.StartEngagementRequest.make ?publicSubject ?publicContent
              ?incidentId ?idempotencyToken ~contactId ~sender ~subject
              ~content ()) (Some Values.StartEngagementResult.to_json)
           (Some Values.StartEngagementResult.error_to_json)])
let stop_engagement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason = flag "reason" (optional string) ~doc:"STRING StopReason"
       and engagementId =
         flag "engagement-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_engagement
           (Values.StopEngagementRequest.make ?reason ~engagementId ())
           (Some Values.StopEngagementResult.to_json)
           (Some Values.StopEngagementResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagsList.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let update_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ContactName"
       and plan = flag "plan" (optional json_arg) ~doc:"JSON Plan"
       and contactId =
         flag "contact-id" (required string) ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact
           (Values.UpdateContactRequest.make ?displayName
              ?plan:(Option.map ~f:Values.Plan.of_json plan) ~contactId ())
           (Some Values.UpdateContactResult.to_json)
           (Some Values.UpdateContactResult.error_to_json)])
let update_contact_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ChannelName"
       and deliveryAddress =
         flag "delivery-address" (optional json_arg)
           ~doc:"JSON ContactChannelAddress"
       and contactChannelId =
         flag "contact-channel-id" (required string)
           ~doc:"STRING SsmContactsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_channel
           (Values.UpdateContactChannelRequest.make ?name
              ?deliveryAddress:(Option.map
                                  ~f:Values.ContactChannelAddress.of_json
                                  deliveryAddress) ~contactChannelId ())
           (Some Values.UpdateContactChannelResult.to_json)
           (Some Values.UpdateContactChannelResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-page", accept_page);
    ("activate-contact-channel", activate_contact_channel);
    ("create-contact", create_contact);
    ("create-contact-channel", create_contact_channel);
    ("deactivate-contact-channel", deactivate_contact_channel);
    ("delete-contact", delete_contact);
    ("delete-contact-channel", delete_contact_channel);
    ("describe-engagement", describe_engagement);
    ("describe-page", describe_page);
    ("get-contact", get_contact);
    ("get-contact-channel", get_contact_channel);
    ("get-contact-policy", get_contact_policy);
    ("list-contact-channels", list_contact_channels);
    ("list-contacts", list_contacts);
    ("list-engagements", list_engagements);
    ("list-page-receipts", list_page_receipts);
    ("list-pages-by-contact", list_pages_by_contact);
    ("list-pages-by-engagement", list_pages_by_engagement);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-contact-policy", put_contact_policy);
    ("send-activation-code", send_activation_code);
    ("start-engagement", start_engagement);
    ("stop-engagement", stop_engagement);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-contact", update_contact);
    ("update-contact-channel", update_contact_channel)]
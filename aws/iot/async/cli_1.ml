(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
open Cli_0[@@warning "-33"]
let get_indexing_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_indexing_configuration
           (Values.GetIndexingConfigurationRequest.make ())
           (Some Values.GetIndexingConfigurationResponse.to_json)
           (Some Values.GetIndexingConfigurationResponse.error_to_json)])
let get_job_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_document (Values.GetJobDocumentRequest.make ~jobId ())
           (Some Values.GetJobDocumentResponse.to_json)
           (Some Values.GetJobDocumentResponse.error_to_json)])
let get_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_logging_options (Values.GetLoggingOptionsRequest.make ())
           (Some Values.GetLoggingOptionsResponse.to_json)
           (Some Values.GetLoggingOptionsResponse.error_to_json)])
let get_o_t_a_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and otaUpdateId =
         flag "ota-update-id" (required string) ~doc:"STRING OTAUpdateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_o_t_a_update
           (Values.GetOTAUpdateRequest.make ~otaUpdateId ())
           (Some Values.GetOTAUpdateResponse.to_json)
           (Some Values.GetOTAUpdateResponse.error_to_json)])
let get_percentiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and aggregationField =
         flag "aggregation-field" (optional string)
           ~doc:"STRING AggregationField"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and percents =
         flag "percents" (optional json_arg) ~doc:"JSON PercentList"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_percentiles
           (Values.GetPercentilesRequest.make ?indexName ?aggregationField
              ?queryVersion
              ?percents:(Option.map ~f:Values.PercentList.of_json percents)
              ~queryString ()) (Some Values.GetPercentilesResponse.to_json)
           (Some Values.GetPercentilesResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy (Values.GetPolicyRequest.make ~policyName ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let get_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and policyVersionId =
         flag "policy-version-id" (required string)
           ~doc:"STRING PolicyVersionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_version
           (Values.GetPolicyVersionRequest.make ~policyName ~policyVersionId
              ()) (Some Values.GetPolicyVersionResponse.to_json)
           (Some Values.GetPolicyVersionResponse.error_to_json)])
let get_registration_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_registration_code
           (Values.GetRegistrationCodeRequest.make ())
           (Some Values.GetRegistrationCodeResponse.to_json)
           (Some Values.GetRegistrationCodeResponse.error_to_json)])
let get_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and aggregationField =
         flag "aggregation-field" (optional string)
           ~doc:"STRING AggregationField"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_statistics
           (Values.GetStatisticsRequest.make ?indexName ?aggregationField
              ?queryVersion ~queryString ())
           (Some Values.GetStatisticsResponse.to_json)
           (Some Values.GetStatisticsResponse.error_to_json)])
let get_topic_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_topic_rule (Values.GetTopicRuleRequest.make ~ruleName ())
           (Some Values.GetTopicRuleResponse.to_json)
           (Some Values.GetTopicRuleResponse.error_to_json)])
let get_topic_rule_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING AwsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_topic_rule_destination
           (Values.GetTopicRuleDestinationRequest.make ~arn ())
           (Some Values.GetTopicRuleDestinationResponse.to_json)
           (Some Values.GetTopicRuleDestinationResponse.error_to_json)])
let get_v2_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_v2_logging_options
           (Values.GetV2LoggingOptionsRequest.make ())
           (Some Values.GetV2LoggingOptionsResponse.to_json)
           (Some Values.GetV2LoggingOptionsResponse.error_to_json)])
let list_active_violations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (optional string)
           ~doc:"STRING DeviceDefenderThingName"
       and securityProfileName =
         flag "security-profile-name" (optional string)
           ~doc:"STRING SecurityProfileName"
       and behaviorCriteriaType =
         flag "behavior-criteria-type" (optional json_arg)
           ~doc:"JSON BehaviorCriteriaType"
       and listSuppressedAlerts =
         flag "list-suppressed-alerts" (optional bool)
           ~doc:"BOOL ListSuppressedAlerts"
       and verificationState =
         flag "verification-state" (optional json_arg)
           ~doc:"JSON VerificationState"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_active_violations
           (Values.ListActiveViolationsRequest.make ?thingName
              ?securityProfileName
              ?behaviorCriteriaType:(Option.map
                                       ~f:Values.BehaviorCriteriaType.of_json
                                       behaviorCriteriaType)
              ?listSuppressedAlerts
              ?verificationState:(Option.map
                                    ~f:Values.VerificationState.of_json
                                    verificationState) ?nextToken ?maxResults
              ()) (Some Values.ListActiveViolationsResponse.to_json)
           (Some Values.ListActiveViolationsResponse.error_to_json)])
let list_attached_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recursive = flag "recursive" (optional bool) ~doc:"BOOL Recursive"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and target =
         flag "target" (required string) ~doc:"STRING PolicyTarget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attached_policies
           (Values.ListAttachedPoliciesRequest.make ?recursive ?marker
              ?pageSize ~target ())
           (Some Values.ListAttachedPoliciesResponse.to_json)
           (Some Values.ListAttachedPoliciesResponse.error_to_json)])
let list_audit_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (optional string) ~doc:"STRING AuditTaskId"
       and checkName =
         flag "check-name" (optional string) ~doc:"STRING AuditCheckName"
       and resourceIdentifier =
         flag "resource-identifier" (optional json_arg)
           ~doc:"JSON ResourceIdentifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and listSuppressedFindings =
         flag "list-suppressed-findings" (optional bool)
           ~doc:"BOOL ListSuppressedFindings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audit_findings
           (Values.ListAuditFindingsRequest.make ?taskId ?checkName
              ?resourceIdentifier:(Option.map
                                     ~f:Values.ResourceIdentifier.of_json
                                     resourceIdentifier) ?maxResults
              ?nextToken
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?listSuppressedFindings ())
           (Some Values.ListAuditFindingsResponse.to_json)
           (Some Values.ListAuditFindingsResponse.error_to_json)])
let list_audit_mitigation_actions_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionStatus =
         flag "action-status" (optional json_arg)
           ~doc:"JSON AuditMitigationActionsExecutionStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MitigationActionsTaskId"
       and findingId =
         flag "finding-id" (required string) ~doc:"STRING FindingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audit_mitigation_actions_executions
           (Values.ListAuditMitigationActionsExecutionsRequest.make
              ?actionStatus:(Option.map
                               ~f:Values.AuditMitigationActionsExecutionStatus.of_json
                               actionStatus) ?maxResults ?nextToken ~taskId
              ~findingId ())
           (Some Values.ListAuditMitigationActionsExecutionsResponse.to_json)
           (Some
              Values.ListAuditMitigationActionsExecutionsResponse.error_to_json)])
let list_audit_mitigation_actions_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and auditTaskId =
         flag "audit-task-id" (optional string) ~doc:"STRING AuditTaskId"
       and findingId =
         flag "finding-id" (optional string) ~doc:"STRING FindingId"
       and taskStatus =
         flag "task-status" (optional json_arg)
           ~doc:"JSON AuditMitigationActionsTaskStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audit_mitigation_actions_tasks
           (Values.ListAuditMitigationActionsTasksRequest.make ?auditTaskId
              ?findingId
              ?taskStatus:(Option.map
                             ~f:Values.AuditMitigationActionsTaskStatus.of_json
                             taskStatus) ?maxResults ?nextToken
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.ListAuditMitigationActionsTasksResponse.to_json)
           (Some Values.ListAuditMitigationActionsTasksResponse.error_to_json)])
let list_audit_suppressions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checkName =
         flag "check-name" (optional string) ~doc:"STRING AuditCheckName"
       and resourceIdentifier =
         flag "resource-identifier" (optional json_arg)
           ~doc:"JSON ResourceIdentifier"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audit_suppressions
           (Values.ListAuditSuppressionsRequest.make ?checkName
              ?resourceIdentifier:(Option.map
                                     ~f:Values.ResourceIdentifier.of_json
                                     resourceIdentifier) ?ascendingOrder
              ?nextToken ?maxResults ())
           (Some Values.ListAuditSuppressionsResponse.to_json)
           (Some Values.ListAuditSuppressionsResponse.error_to_json)])
let list_audit_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskType =
         flag "task-type" (optional json_arg) ~doc:"JSON AuditTaskType"
       and taskStatus =
         flag "task-status" (optional json_arg) ~doc:"JSON AuditTaskStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audit_tasks
           (Values.ListAuditTasksRequest.make
              ?taskType:(Option.map ~f:Values.AuditTaskType.of_json taskType)
              ?taskStatus:(Option.map ~f:Values.AuditTaskStatus.of_json
                             taskStatus) ?nextToken ?maxResults
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.ListAuditTasksResponse.to_json)
           (Some Values.ListAuditTasksResponse.error_to_json)])
let list_authorizers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON AuthorizerStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_authorizers
           (Values.ListAuthorizersRequest.make ?pageSize ?marker
              ?ascendingOrder
              ?status:(Option.map ~f:Values.AuthorizerStatus.of_json status)
              ()) (Some Values.ListAuthorizersResponse.to_json)
           (Some Values.ListAuthorizersResponse.error_to_json)])
let list_billing_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and namePrefixFilter =
         flag "name-prefix-filter" (optional string)
           ~doc:"STRING BillingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_billing_groups
           (Values.ListBillingGroupsRequest.make ?nextToken ?maxResults
              ?namePrefixFilter ())
           (Some Values.ListBillingGroupsResponse.to_json)
           (Some Values.ListBillingGroupsResponse.error_to_json)])
let list_c_a_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_c_a_certificates
           (Values.ListCACertificatesRequest.make ?pageSize ?marker
              ?ascendingOrder ())
           (Some Values.ListCACertificatesResponse.to_json)
           (Some Values.ListCACertificatesResponse.error_to_json)])
let list_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_certificates
           (Values.ListCertificatesRequest.make ?pageSize ?marker
              ?ascendingOrder ())
           (Some Values.ListCertificatesResponse.to_json)
           (Some Values.ListCertificatesResponse.error_to_json)])
let list_certificates_by_c_a =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder"
       and caCertificateId =
         flag "ca-certificate-id" (required string)
           ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_certificates_by_c_a
           (Values.ListCertificatesByCARequest.make ?pageSize ?marker
              ?ascendingOrder ~caCertificateId ())
           (Some Values.ListCertificatesByCAResponse.to_json)
           (Some Values.ListCertificatesByCAResponse.error_to_json)])
let list_custom_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_metrics
           (Values.ListCustomMetricsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListCustomMetricsResponse.to_json)
           (Some Values.ListCustomMetricsResponse.error_to_json)])
let list_detect_mitigation_actions_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (optional string)
           ~doc:"STRING MitigationActionsTaskId"
       and violationId =
         flag "violation-id" (optional string) ~doc:"STRING ViolationId"
       and thingName =
         flag "thing-name" (optional string)
           ~doc:"STRING DeviceDefenderThingName"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_detect_mitigation_actions_executions
           (Values.ListDetectMitigationActionsExecutionsRequest.make ?taskId
              ?violationId ?thingName
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?maxResults ?nextToken ())
           (Some Values.ListDetectMitigationActionsExecutionsResponse.to_json)
           (Some
              Values.ListDetectMitigationActionsExecutionsResponse.error_to_json)])
let list_detect_mitigation_actions_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_detect_mitigation_actions_tasks
           (Values.ListDetectMitigationActionsTasksRequest.make ?maxResults
              ?nextToken ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.ListDetectMitigationActionsTasksResponse.to_json)
           (Some
              Values.ListDetectMitigationActionsTasksResponse.error_to_json)])
let list_dimensions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dimensions
           (Values.ListDimensionsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDimensionsResponse.to_json)
           (Some Values.ListDimensionsResponse.error_to_json)])
let list_domain_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and serviceType =
         flag "service-type" (optional json_arg) ~doc:"JSON ServiceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_configurations
           (Values.ListDomainConfigurationsRequest.make ?marker ?pageSize
              ?serviceType:(Option.map ~f:Values.ServiceType.of_json
                              serviceType) ())
           (Some Values.ListDomainConfigurationsResponse.to_json)
           (Some Values.ListDomainConfigurationsResponse.error_to_json)])
let list_fleet_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fleet_metrics
           (Values.ListFleetMetricsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListFleetMetricsResponse.to_json)
           (Some Values.ListFleetMetricsResponse.error_to_json)])
let list_indices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT QueryMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_indices
           (Values.ListIndicesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListIndicesResponse.to_json)
           (Some Values.ListIndicesResponse.error_to_json)])
let list_job_executions_for_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON JobExecutionStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LaserMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_executions_for_job
           (Values.ListJobExecutionsForJobRequest.make
              ?status:(Option.map ~f:Values.JobExecutionStatus.of_json status)
              ?maxResults ?nextToken ~jobId ())
           (Some Values.ListJobExecutionsForJobResponse.to_json)
           (Some Values.ListJobExecutionsForJobResponse.error_to_json)])
let list_job_executions_for_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON JobExecutionStatus"
       and namespaceId =
         flag "namespace-id" (optional string) ~doc:"STRING NamespaceId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LaserMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and jobId = flag "job-id" (optional string) ~doc:"STRING JobId"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_executions_for_thing
           (Values.ListJobExecutionsForThingRequest.make
              ?status:(Option.map ~f:Values.JobExecutionStatus.of_json status)
              ?namespaceId ?maxResults ?nextToken ?jobId ~thingName ())
           (Some Values.ListJobExecutionsForThingResponse.to_json)
           (Some Values.ListJobExecutionsForThingResponse.error_to_json)])
let list_job_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LaserMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_templates
           (Values.ListJobTemplatesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListJobTemplatesResponse.to_json)
           (Some Values.ListJobTemplatesResponse.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status = flag "status" (optional json_arg) ~doc:"JSON JobStatus"
       and targetSelection =
         flag "target-selection" (optional json_arg)
           ~doc:"JSON TargetSelection"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LaserMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and thingGroupName =
         flag "thing-group-name" (optional string)
           ~doc:"STRING ThingGroupName"
       and thingGroupId =
         flag "thing-group-id" (optional string) ~doc:"STRING ThingGroupId"
       and namespaceId =
         flag "namespace-id" (optional string) ~doc:"STRING NamespaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsRequest.make
              ?status:(Option.map ~f:Values.JobStatus.of_json status)
              ?targetSelection:(Option.map ~f:Values.TargetSelection.of_json
                                  targetSelection) ?maxResults ?nextToken
              ?thingGroupName ?thingGroupId ?namespaceId ())
           (Some Values.ListJobsResponse.to_json)
           (Some Values.ListJobsResponse.error_to_json)])
let list_managed_job_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING ManagedJobTemplateName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT LaserMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_job_templates
           (Values.ListManagedJobTemplatesRequest.make ?templateName
              ?maxResults ?nextToken ())
           (Some Values.ListManagedJobTemplatesResponse.to_json)
           (Some Values.ListManagedJobTemplatesResponse.error_to_json)])
let list_metric_values =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dimensionName =
         flag "dimension-name" (optional string) ~doc:"STRING DimensionName"
       and dimensionValueOperator =
         flag "dimension-value-operator" (optional json_arg)
           ~doc:"JSON DimensionValueOperator"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and thingName =
         flag "thing-name" (required string)
           ~doc:"STRING DeviceDefenderThingName"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING BehaviorMetric"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_metric_values
           (Values.ListMetricValuesRequest.make ?dimensionName
              ?dimensionValueOperator:(Option.map
                                         ~f:Values.DimensionValueOperator.of_json
                                         dimensionValueOperator) ?maxResults
              ?nextToken ~thingName ~metricName
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.ListMetricValuesResponse.to_json)
           (Some Values.ListMetricValuesResponse.error_to_json)])
let list_mitigation_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionType =
         flag "action-type" (optional json_arg)
           ~doc:"JSON MitigationActionType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_mitigation_actions
           (Values.ListMitigationActionsRequest.make
              ?actionType:(Option.map ~f:Values.MitigationActionType.of_json
                             actionType) ?maxResults ?nextToken ())
           (Some Values.ListMitigationActionsResponse.to_json)
           (Some Values.ListMitigationActionsResponse.error_to_json)])
let list_o_t_a_updates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and otaUpdateStatus =
         flag "ota-update-status" (optional json_arg)
           ~doc:"JSON OTAUpdateStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_o_t_a_updates
           (Values.ListOTAUpdatesRequest.make ?maxResults ?nextToken
              ?otaUpdateStatus:(Option.map ~f:Values.OTAUpdateStatus.of_json
                                  otaUpdateStatus) ())
           (Some Values.ListOTAUpdatesResponse.to_json)
           (Some Values.ListOTAUpdatesResponse.error_to_json)])
let list_outgoing_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_outgoing_certificates
           (Values.ListOutgoingCertificatesRequest.make ?pageSize ?marker
              ?ascendingOrder ())
           (Some Values.ListOutgoingCertificatesResponse.to_json)
           (Some Values.ListOutgoingCertificatesResponse.error_to_json)])
let list_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies
           (Values.ListPoliciesRequest.make ?marker ?pageSize ?ascendingOrder
              ()) (Some Values.ListPoliciesResponse.to_json)
           (Some Values.ListPoliciesResponse.error_to_json)])
let list_policy_principals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_principals
           (Values.ListPolicyPrincipalsRequest.make ?marker ?pageSize
              ?ascendingOrder ~policyName ())
           (Some Values.ListPolicyPrincipalsResponse.to_json)
           (Some Values.ListPolicyPrincipalsResponse.error_to_json)])
let list_policy_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_versions
           (Values.ListPolicyVersionsRequest.make ~policyName ())
           (Some Values.ListPolicyVersionsResponse.to_json)
           (Some Values.ListPolicyVersionsResponse.error_to_json)])
let list_principal_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_principal_policies
           (Values.ListPrincipalPoliciesRequest.make ?marker ?pageSize
              ?ascendingOrder ~principal ())
           (Some Values.ListPrincipalPoliciesResponse.to_json)
           (Some Values.ListPrincipalPoliciesResponse.error_to_json)])
let list_principal_things =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_principal_things
           (Values.ListPrincipalThingsRequest.make ?nextToken ?maxResults
              ~principal ())
           (Some Values.ListPrincipalThingsResponse.to_json)
           (Some Values.ListPrincipalThingsResponse.error_to_json)])
let list_provisioning_template_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioning_template_versions
           (Values.ListProvisioningTemplateVersionsRequest.make ?maxResults
              ?nextToken ~templateName ())
           (Some Values.ListProvisioningTemplateVersionsResponse.to_json)
           (Some
              Values.ListProvisioningTemplateVersionsResponse.error_to_json)])
let list_provisioning_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioning_templates
           (Values.ListProvisioningTemplatesRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListProvisioningTemplatesResponse.to_json)
           (Some Values.ListProvisioningTemplatesResponse.error_to_json)])
let list_role_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_role_aliases
           (Values.ListRoleAliasesRequest.make ?pageSize ?marker
              ?ascendingOrder ())
           (Some Values.ListRoleAliasesResponse.to_json)
           (Some Values.ListRoleAliasesResponse.error_to_json)])
let list_scheduled_audits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_scheduled_audits
           (Values.ListScheduledAuditsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListScheduledAuditsResponse.to_json)
           (Some Values.ListScheduledAuditsResponse.error_to_json)])
let list_security_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and dimensionName =
         flag "dimension-name" (optional string) ~doc:"STRING DimensionName"
       and metricName =
         flag "metric-name" (optional string) ~doc:"STRING MetricName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_profiles
           (Values.ListSecurityProfilesRequest.make ?nextToken ?maxResults
              ?dimensionName ?metricName ())
           (Some Values.ListSecurityProfilesResponse.to_json)
           (Some Values.ListSecurityProfilesResponse.error_to_json)])
let list_security_profiles_for_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and recursive = flag "recursive" (optional bool) ~doc:"BOOL Recursive"
       and securityProfileTargetArn =
         flag "security-profile-target-arn" (required string)
           ~doc:"STRING SecurityProfileTargetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_profiles_for_target
           (Values.ListSecurityProfilesForTargetRequest.make ?nextToken
              ?maxResults ?recursive ~securityProfileTargetArn ())
           (Some Values.ListSecurityProfilesForTargetResponse.to_json)
           (Some Values.ListSecurityProfilesForTargetResponse.error_to_json)])
let list_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ascendingOrder =
         flag "ascending-order" (optional bool) ~doc:"BOOL AscendingOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_streams
           (Values.ListStreamsRequest.make ?maxResults ?nextToken
              ?ascendingOrder ()) (Some Values.ListStreamsResponse.to_json)
           (Some Values.ListStreamsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn__lc1 =
         flag "resource-arn--lc1" (optional string) ~doc:"STRING ResourceArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?resourceArn__lc1
              ?nextToken ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_targets_for_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_targets_for_policy
           (Values.ListTargetsForPolicyRequest.make ?marker ?pageSize
              ~policyName ())
           (Some Values.ListTargetsForPolicyResponse.to_json)
           (Some Values.ListTargetsForPolicyResponse.error_to_json)])
let list_targets_for_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and securityProfileName =
         flag "security-profile-name" (required string)
           ~doc:"STRING SecurityProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_targets_for_security_profile
           (Values.ListTargetsForSecurityProfileRequest.make ?nextToken
              ?maxResults ~securityProfileName ())
           (Some Values.ListTargetsForSecurityProfileResponse.to_json)
           (Some Values.ListTargetsForSecurityProfileResponse.error_to_json)])
let list_thing_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and parentGroup =
         flag "parent-group" (optional string) ~doc:"STRING ThingGroupName"
       and namePrefixFilter =
         flag "name-prefix-filter" (optional string)
           ~doc:"STRING ThingGroupName"
       and recursive =
         flag "recursive" (optional bool) ~doc:"BOOL RecursiveWithoutDefault" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_thing_groups
           (Values.ListThingGroupsRequest.make ?nextToken ?maxResults
              ?parentGroup ?namePrefixFilter ?recursive ())
           (Some Values.ListThingGroupsResponse.to_json)
           (Some Values.ListThingGroupsResponse.error_to_json)])
let list_thing_groups_for_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_thing_groups_for_thing
           (Values.ListThingGroupsForThingRequest.make ?nextToken ?maxResults
              ~thingName ())
           (Some Values.ListThingGroupsForThingResponse.to_json)
           (Some Values.ListThingGroupsForThingResponse.error_to_json)])
let list_thing_principals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_thing_principals
           (Values.ListThingPrincipalsRequest.make ?nextToken ?maxResults
              ~thingName ())
           (Some Values.ListThingPrincipalsResponse.to_json)
           (Some Values.ListThingPrincipalsResponse.error_to_json)])
let list_thing_registration_task_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId"
       and reportType =
         flag "report-type" (required json_arg) ~doc:"JSON ReportType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_thing_registration_task_reports
           (Values.ListThingRegistrationTaskReportsRequest.make ?nextToken
              ?maxResults ~taskId
              ~reportType:(Values.ReportType.of_json reportType) ())
           (Some Values.ListThingRegistrationTaskReportsResponse.to_json)
           (Some
              Values.ListThingRegistrationTaskReportsResponse.error_to_json)])
let list_thing_registration_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and status = flag "status" (optional json_arg) ~doc:"JSON Status" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_thing_registration_tasks
           (Values.ListThingRegistrationTasksRequest.make ?nextToken
              ?maxResults
              ?status:(Option.map ~f:Values.Status.of_json status) ())
           (Some Values.ListThingRegistrationTasksResponse.to_json)
           (Some Values.ListThingRegistrationTasksResponse.error_to_json)])
let list_thing_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and thingTypeName =
         flag "thing-type-name" (optional string) ~doc:"STRING ThingTypeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_thing_types
           (Values.ListThingTypesRequest.make ?nextToken ?maxResults
              ?thingTypeName ()) (Some Values.ListThingTypesResponse.to_json)
           (Some Values.ListThingTypesResponse.error_to_json)])
let list_things =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and attributeName =
         flag "attribute-name" (optional string) ~doc:"STRING AttributeName"
       and attributeValue =
         flag "attribute-value" (optional string)
           ~doc:"STRING AttributeValue"
       and thingTypeName =
         flag "thing-type-name" (optional string) ~doc:"STRING ThingTypeName"
       and usePrefixAttributeValue =
         flag "use-prefix-attribute-value" (optional bool)
           ~doc:"BOOL usePrefixAttributeValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_things
           (Values.ListThingsRequest.make ?nextToken ?maxResults
              ?attributeName ?attributeValue ?thingTypeName
              ?usePrefixAttributeValue ())
           (Some Values.ListThingsResponse.to_json)
           (Some Values.ListThingsResponse.error_to_json)])
let list_things_in_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and billingGroupName =
         flag "billing-group-name" (required string)
           ~doc:"STRING BillingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_things_in_billing_group
           (Values.ListThingsInBillingGroupRequest.make ?nextToken
              ?maxResults ~billingGroupName ())
           (Some Values.ListThingsInBillingGroupResponse.to_json)
           (Some Values.ListThingsInBillingGroupResponse.error_to_json)])
let list_things_in_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recursive = flag "recursive" (optional bool) ~doc:"BOOL Recursive"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RegistryMaxResults"
       and thingGroupName =
         flag "thing-group-name" (required string)
           ~doc:"STRING ThingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_things_in_thing_group
           (Values.ListThingsInThingGroupRequest.make ?recursive ?nextToken
              ?maxResults ~thingGroupName ())
           (Some Values.ListThingsInThingGroupResponse.to_json)
           (Some Values.ListThingsInThingGroupResponse.error_to_json)])
let list_topic_rule_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT TopicRuleDestinationMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_topic_rule_destinations
           (Values.ListTopicRuleDestinationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListTopicRuleDestinationsResponse.to_json)
           (Some Values.ListTopicRuleDestinationsResponse.error_to_json)])
let list_topic_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topic = flag "topic" (optional string) ~doc:"STRING Topic"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT TopicRuleMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and ruleDisabled =
         flag "rule-disabled" (optional bool) ~doc:"BOOL IsDisabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_topic_rules
           (Values.ListTopicRulesRequest.make ?topic ?maxResults ?nextToken
              ?ruleDisabled ()) (Some Values.ListTopicRulesResponse.to_json)
           (Some Values.ListTopicRulesResponse.error_to_json)])
let list_v2_logging_levels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetType =
         flag "target-type" (optional json_arg) ~doc:"JSON LogTargetType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT SkyfallMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_v2_logging_levels
           (Values.ListV2LoggingLevelsRequest.make
              ?targetType:(Option.map ~f:Values.LogTargetType.of_json
                             targetType) ?nextToken ?maxResults ())
           (Some Values.ListV2LoggingLevelsResponse.to_json)
           (Some Values.ListV2LoggingLevelsResponse.error_to_json)])
let list_violation_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (optional string)
           ~doc:"STRING DeviceDefenderThingName"
       and securityProfileName =
         flag "security-profile-name" (optional string)
           ~doc:"STRING SecurityProfileName"
       and behaviorCriteriaType =
         flag "behavior-criteria-type" (optional json_arg)
           ~doc:"JSON BehaviorCriteriaType"
       and listSuppressedAlerts =
         flag "list-suppressed-alerts" (optional bool)
           ~doc:"BOOL ListSuppressedAlerts"
       and verificationState =
         flag "verification-state" (optional json_arg)
           ~doc:"JSON VerificationState"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_violation_events
           (Values.ListViolationEventsRequest.make ?thingName
              ?securityProfileName
              ?behaviorCriteriaType:(Option.map
                                       ~f:Values.BehaviorCriteriaType.of_json
                                       behaviorCriteriaType)
              ?listSuppressedAlerts
              ?verificationState:(Option.map
                                    ~f:Values.VerificationState.of_json
                                    verificationState) ?nextToken ?maxResults
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.ListViolationEventsResponse.to_json)
           (Some Values.ListViolationEventsResponse.error_to_json)])
let put_verification_state_on_violation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and verificationStateDescription =
         flag "verification-state-description" (optional string)
           ~doc:"STRING VerificationStateDescription"
       and violationId =
         flag "violation-id" (required string) ~doc:"STRING ViolationId"
       and verificationState =
         flag "verification-state" (required json_arg)
           ~doc:"JSON VerificationState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_verification_state_on_violation
           (Values.PutVerificationStateOnViolationRequest.make
              ?verificationStateDescription ~violationId
              ~verificationState:(Values.VerificationState.of_json
                                    verificationState) ())
           (Some Values.PutVerificationStateOnViolationResponse.to_json)
           (Some Values.PutVerificationStateOnViolationResponse.error_to_json)])
let register_c_a_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and setAsActive =
         flag "set-as-active" (optional bool) ~doc:"BOOL SetAsActive"
       and allowAutoRegistration =
         flag "allow-auto-registration" (optional bool)
           ~doc:"BOOL AllowAutoRegistration"
       and registrationConfig =
         flag "registration-config" (optional json_arg)
           ~doc:"JSON RegistrationConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and caCertificate =
         flag "ca-certificate" (required string) ~doc:"STRING CertificatePem"
       and verificationCertificate =
         flag "verification-certificate" (required string)
           ~doc:"STRING CertificatePem" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_c_a_certificate
           (Values.RegisterCACertificateRequest.make ?setAsActive
              ?allowAutoRegistration
              ?registrationConfig:(Option.map
                                     ~f:Values.RegistrationConfig.of_json
                                     registrationConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~caCertificate ~verificationCertificate ())
           (Some Values.RegisterCACertificateResponse.to_json)
           (Some Values.RegisterCACertificateResponse.error_to_json)])
let register_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and caCertificatePem =
         flag "ca-certificate-pem" (optional string)
           ~doc:"STRING CertificatePem"
       and setAsActive =
         flag "set-as-active" (optional bool) ~doc:"BOOL SetAsActiveFlag"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON CertificateStatus"
       and certificatePem =
         flag "certificate-pem" (required string)
           ~doc:"STRING CertificatePem" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_certificate
           (Values.RegisterCertificateRequest.make ?caCertificatePem
              ?setAsActive
              ?status:(Option.map ~f:Values.CertificateStatus.of_json status)
              ~certificatePem ())
           (Some Values.RegisterCertificateResponse.to_json)
           (Some Values.RegisterCertificateResponse.error_to_json)])
let register_certificate_without_c_a =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON CertificateStatus"
       and certificatePem =
         flag "certificate-pem" (required string)
           ~doc:"STRING CertificatePem" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_certificate_without_c_a
           (Values.RegisterCertificateWithoutCARequest.make
              ?status:(Option.map ~f:Values.CertificateStatus.of_json status)
              ~certificatePem ())
           (Some Values.RegisterCertificateWithoutCAResponse.to_json)
           (Some Values.RegisterCertificateWithoutCAResponse.error_to_json)])
let register_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and templateBody =
         flag "template-body" (required string) ~doc:"STRING TemplateBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_thing
           (Values.RegisterThingRequest.make
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ~templateBody ()) (Some Values.RegisterThingResponse.to_json)
           (Some Values.RegisterThingResponse.error_to_json)])
let reject_certificate_transfer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rejectReason =
         flag "reject-reason" (optional string) ~doc:"STRING Message"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_certificate_transfer
           (Values.RejectCertificateTransferRequest.make ?rejectReason
              ~certificateId ()) None None])
let remove_thing_from_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingGroupName =
         flag "billing-group-name" (optional string)
           ~doc:"STRING BillingGroupName"
       and billingGroupArn =
         flag "billing-group-arn" (optional string)
           ~doc:"STRING BillingGroupArn"
       and thingName =
         flag "thing-name" (optional string) ~doc:"STRING ThingName"
       and thingArn =
         flag "thing-arn" (optional string) ~doc:"STRING ThingArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_thing_from_billing_group
           (Values.RemoveThingFromBillingGroupRequest.make ?billingGroupName
              ?billingGroupArn ?thingName ?thingArn ())
           (Some Values.RemoveThingFromBillingGroupResponse.to_json)
           (Some Values.RemoveThingFromBillingGroupResponse.error_to_json)])
let remove_thing_from_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingGroupName =
         flag "thing-group-name" (optional string)
           ~doc:"STRING ThingGroupName"
       and thingGroupArn =
         flag "thing-group-arn" (optional string) ~doc:"STRING ThingGroupArn"
       and thingName =
         flag "thing-name" (optional string) ~doc:"STRING ThingName"
       and thingArn =
         flag "thing-arn" (optional string) ~doc:"STRING ThingArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_thing_from_thing_group
           (Values.RemoveThingFromThingGroupRequest.make ?thingGroupName
              ?thingGroupArn ?thingName ?thingArn ())
           (Some Values.RemoveThingFromThingGroupResponse.to_json)
           (Some Values.RemoveThingFromThingGroupResponse.error_to_json)])
let replace_topic_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName"
       and topicRulePayload =
         flag "topic-rule-payload" (required json_arg)
           ~doc:"JSON TopicRulePayload" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.replace_topic_rule
           (Values.ReplaceTopicRuleRequest.make ~ruleName
              ~topicRulePayload:(Values.TopicRulePayload.of_json
                                   topicRulePayload) ()) None None])
let search_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT QueryMaxResults"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_index
           (Values.SearchIndexRequest.make ?indexName ?nextToken ?maxResults
              ?queryVersion ~queryString ())
           (Some Values.SearchIndexResponse.to_json)
           (Some Values.SearchIndexResponse.error_to_json)])
let set_default_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorizerName =
         flag "authorizer-name" (required string)
           ~doc:"STRING AuthorizerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_default_authorizer
           (Values.SetDefaultAuthorizerRequest.make ~authorizerName ())
           (Some Values.SetDefaultAuthorizerResponse.to_json)
           (Some Values.SetDefaultAuthorizerResponse.error_to_json)])
let set_default_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and policyVersionId =
         flag "policy-version-id" (required string)
           ~doc:"STRING PolicyVersionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_default_policy_version
           (Values.SetDefaultPolicyVersionRequest.make ~policyName
              ~policyVersionId ()) None None])
let set_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loggingOptionsPayload =
         flag "logging-options-payload" (required json_arg)
           ~doc:"JSON LoggingOptionsPayload" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_logging_options
           (Values.SetLoggingOptionsRequest.make
              ~loggingOptionsPayload:(Values.LoggingOptionsPayload.of_json
                                        loggingOptionsPayload) ()) None None])
let set_v2_logging_level =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logTarget =
         flag "log-target" (required json_arg) ~doc:"JSON LogTarget"
       and logLevel =
         flag "log-level" (required json_arg) ~doc:"JSON LogLevel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_v2_logging_level
           (Values.SetV2LoggingLevelRequest.make
              ~logTarget:(Values.LogTarget.of_json logTarget)
              ~logLevel:(Values.LogLevel.of_json logLevel) ()) None None])
let set_v2_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING AwsArn"
       and defaultLogLevel =
         flag "default-log-level" (optional json_arg) ~doc:"JSON LogLevel"
       and disableAllLogs =
         flag "disable-all-logs" (optional bool) ~doc:"BOOL DisableAllLogs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_v2_logging_options
           (Values.SetV2LoggingOptionsRequest.make ?roleArn
              ?defaultLogLevel:(Option.map ~f:Values.LogLevel.of_json
                                  defaultLogLevel) ?disableAllLogs ()) None
           None])
let start_audit_mitigation_actions_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MitigationActionsTaskId"
       and target =
         flag "target" (required json_arg)
           ~doc:"JSON AuditMitigationActionsTaskTarget"
       and auditCheckToActionsMapping =
         flag "audit-check-to-actions-mapping" (required json_arg)
           ~doc:"JSON AuditCheckToActionsMapping"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_audit_mitigation_actions_task
           (Values.StartAuditMitigationActionsTaskRequest.make ~taskId
              ~target:(Values.AuditMitigationActionsTaskTarget.of_json target)
              ~auditCheckToActionsMapping:(Values.AuditCheckToActionsMapping.of_json
                                             auditCheckToActionsMapping)
              ~clientRequestToken ())
           (Some Values.StartAuditMitigationActionsTaskResponse.to_json)
           (Some Values.StartAuditMitigationActionsTaskResponse.error_to_json)])
let start_detect_mitigation_actions_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and violationEventOccurrenceRange =
         flag "violation-event-occurrence-range" (optional json_arg)
           ~doc:"JSON ViolationEventOccurrenceRange"
       and includeOnlyActiveViolations =
         flag "include-only-active-violations" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and includeSuppressedAlerts =
         flag "include-suppressed-alerts" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MitigationActionsTaskId"
       and target =
         flag "target" (required json_arg)
           ~doc:"JSON DetectMitigationActionsTaskTarget"
       and actions =
         flag "actions" (required json_arg)
           ~doc:"JSON DetectMitigationActionsToExecuteList"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_detect_mitigation_actions_task
           (Values.StartDetectMitigationActionsTaskRequest.make
              ?violationEventOccurrenceRange:(Option.map
                                                ~f:Values.ViolationEventOccurrenceRange.of_json
                                                violationEventOccurrenceRange)
              ?includeOnlyActiveViolations ?includeSuppressedAlerts ~taskId
              ~target:(Values.DetectMitigationActionsTaskTarget.of_json
                         target)
              ~actions:(Values.DetectMitigationActionsToExecuteList.of_json
                          actions) ~clientRequestToken ())
           (Some Values.StartDetectMitigationActionsTaskResponse.to_json)
           (Some
              Values.StartDetectMitigationActionsTaskResponse.error_to_json)])
let start_on_demand_audit_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetCheckNames =
         flag "target-check-names" (required json_arg)
           ~doc:"JSON TargetAuditCheckNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_on_demand_audit_task
           (Values.StartOnDemandAuditTaskRequest.make
              ~targetCheckNames:(Values.TargetAuditCheckNames.of_json
                                   targetCheckNames) ())
           (Some Values.StartOnDemandAuditTaskResponse.to_json)
           (Some Values.StartOnDemandAuditTaskResponse.error_to_json)])
let start_thing_registration_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateBody =
         flag "template-body" (required string) ~doc:"STRING TemplateBody"
       and inputFileBucket =
         flag "input-file-bucket" (required string)
           ~doc:"STRING RegistryS3BucketName"
       and inputFileKey =
         flag "input-file-key" (required string)
           ~doc:"STRING RegistryS3KeyName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_thing_registration_task
           (Values.StartThingRegistrationTaskRequest.make ~templateBody
              ~inputFileBucket ~inputFileKey ~roleArn ())
           (Some Values.StartThingRegistrationTaskResponse.to_json)
           (Some Values.StartThingRegistrationTaskResponse.error_to_json)])
let stop_thing_registration_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_thing_registration_task
           (Values.StopThingRegistrationTaskRequest.make ~taskId ())
           (Some Values.StopThingRegistrationTaskResponse.to_json)
           (Some Values.StopThingRegistrationTaskResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn__lc1 =
         flag "resource-arn--lc1" (optional string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ?resourceArn__lc1
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let test_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principal =
         flag "principal" (optional string) ~doc:"STRING Principal"
       and cognitoIdentityPoolId =
         flag "cognito-identity-pool-id" (optional string)
           ~doc:"STRING CognitoIdentityPoolId"
       and clientId =
         flag "client-id" (optional string) ~doc:"STRING ClientId"
       and policyNamesToAdd =
         flag "policy-names-to-add" (optional json_arg)
           ~doc:"JSON PolicyNames"
       and policyNamesToSkip =
         flag "policy-names-to-skip" (optional json_arg)
           ~doc:"JSON PolicyNames"
       and authInfos =
         flag "auth-infos" (required json_arg) ~doc:"JSON AuthInfos" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_authorization
           (Values.TestAuthorizationRequest.make ?principal
              ?cognitoIdentityPoolId ?clientId
              ?policyNamesToAdd:(Option.map ~f:Values.PolicyNames.of_json
                                   policyNamesToAdd)
              ?policyNamesToSkip:(Option.map ~f:Values.PolicyNames.of_json
                                    policyNamesToSkip)
              ~authInfos:(Values.AuthInfos.of_json authInfos) ())
           (Some Values.TestAuthorizationResponse.to_json)
           (Some Values.TestAuthorizationResponse.error_to_json)])
let test_invoke_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and token = flag "token" (optional string) ~doc:"STRING Token"
       and tokenSignature =
         flag "token-signature" (optional string)
           ~doc:"STRING TokenSignature"
       and httpContext =
         flag "http-context" (optional json_arg) ~doc:"JSON HttpContext"
       and mqttContext =
         flag "mqtt-context" (optional json_arg) ~doc:"JSON MqttContext"
       and tlsContext =
         flag "tls-context" (optional json_arg) ~doc:"JSON TlsContext"
       and authorizerName =
         flag "authorizer-name" (required string)
           ~doc:"STRING AuthorizerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_invoke_authorizer
           (Values.TestInvokeAuthorizerRequest.make ?token ?tokenSignature
              ?httpContext:(Option.map ~f:Values.HttpContext.of_json
                              httpContext)
              ?mqttContext:(Option.map ~f:Values.MqttContext.of_json
                              mqttContext)
              ?tlsContext:(Option.map ~f:Values.TlsContext.of_json tlsContext)
              ~authorizerName ())
           (Some Values.TestInvokeAuthorizerResponse.to_json)
           (Some Values.TestInvokeAuthorizerResponse.error_to_json)])
let transfer_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transferMessage =
         flag "transfer-message" (optional string) ~doc:"STRING Message"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId"
       and targetAwsAccount =
         flag "target-aws-account" (required string)
           ~doc:"STRING AwsAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.transfer_certificate
           (Values.TransferCertificateRequest.make ?transferMessage
              ~certificateId ~targetAwsAccount ())
           (Some Values.TransferCertificateResponse.to_json)
           (Some Values.TransferCertificateResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn__lc1 =
         flag "resource-arn--lc1" (optional string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ?resourceArn__lc1
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_account_audit_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and auditNotificationTargetConfigurations =
         flag "audit-notification-target-configurations" (optional json_arg)
           ~doc:"JSON AuditNotificationTargetConfigurations"
       and auditCheckConfigurations =
         flag "audit-check-configurations" (optional json_arg)
           ~doc:"JSON AuditCheckConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_audit_configuration
           (Values.UpdateAccountAuditConfigurationRequest.make ?roleArn
              ?auditNotificationTargetConfigurations:(Option.map
                                                        ~f:Values.AuditNotificationTargetConfigurations.of_json
                                                        auditNotificationTargetConfigurations)
              ?auditCheckConfigurations:(Option.map
                                           ~f:Values.AuditCheckConfigurations.of_json
                                           auditCheckConfigurations) ())
           (Some Values.UpdateAccountAuditConfigurationResponse.to_json)
           (Some Values.UpdateAccountAuditConfigurationResponse.error_to_json)])
let update_audit_suppression =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expirationDate =
         flag "expiration-date" (optional json_arg) ~doc:"JSON Timestamp"
       and suppressIndefinitely =
         flag "suppress-indefinitely" (optional bool)
           ~doc:"BOOL SuppressIndefinitely"
       and description =
         flag "description" (optional string) ~doc:"STRING AuditDescription"
       and checkName =
         flag "check-name" (required string) ~doc:"STRING AuditCheckName"
       and resourceIdentifier =
         flag "resource-identifier" (required json_arg)
           ~doc:"JSON ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_audit_suppression
           (Values.UpdateAuditSuppressionRequest.make
              ?expirationDate:(Option.map ~f:Values.Timestamp.of_json
                                 expirationDate) ?suppressIndefinitely
              ?description ~checkName
              ~resourceIdentifier:(Values.ResourceIdentifier.of_json
                                     resourceIdentifier) ())
           (Some Values.UpdateAuditSuppressionResponse.to_json)
           (Some Values.UpdateAuditSuppressionResponse.error_to_json)])
let update_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorizerFunctionArn =
         flag "authorizer-function-arn" (optional string)
           ~doc:"STRING AuthorizerFunctionArn"
       and tokenKeyName =
         flag "token-key-name" (optional string) ~doc:"STRING TokenKeyName"
       and tokenSigningPublicKeys =
         flag "token-signing-public-keys" (optional json_arg)
           ~doc:"JSON PublicKeyMap"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON AuthorizerStatus"
       and enableCachingForHttp =
         flag "enable-caching-for-http" (optional bool)
           ~doc:"BOOL EnableCachingForHttp"
       and authorizerName =
         flag "authorizer-name" (required string)
           ~doc:"STRING AuthorizerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_authorizer
           (Values.UpdateAuthorizerRequest.make ?authorizerFunctionArn
              ?tokenKeyName
              ?tokenSigningPublicKeys:(Option.map
                                         ~f:Values.PublicKeyMap.of_json
                                         tokenSigningPublicKeys)
              ?status:(Option.map ~f:Values.AuthorizerStatus.of_json status)
              ?enableCachingForHttp ~authorizerName ())
           (Some Values.UpdateAuthorizerResponse.to_json)
           (Some Values.UpdateAuthorizerResponse.error_to_json)])
let update_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and billingGroupName =
         flag "billing-group-name" (required string)
           ~doc:"STRING BillingGroupName"
       and billingGroupProperties =
         flag "billing-group-properties" (required json_arg)
           ~doc:"JSON BillingGroupProperties" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_billing_group
           (Values.UpdateBillingGroupRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~billingGroupName
              ~billingGroupProperties:(Values.BillingGroupProperties.of_json
                                         billingGroupProperties) ())
           (Some Values.UpdateBillingGroupResponse.to_json)
           (Some Values.UpdateBillingGroupResponse.error_to_json)])
let update_c_a_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newStatus =
         flag "new-status" (optional json_arg)
           ~doc:"JSON CACertificateStatus"
       and newAutoRegistrationStatus =
         flag "new-auto-registration-status" (optional json_arg)
           ~doc:"JSON AutoRegistrationStatus"
       and registrationConfig =
         flag "registration-config" (optional json_arg)
           ~doc:"JSON RegistrationConfig"
       and removeAutoRegistration =
         flag "remove-auto-registration" (optional bool)
           ~doc:"BOOL RemoveAutoRegistration"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_c_a_certificate
           (Values.UpdateCACertificateRequest.make
              ?newStatus:(Option.map ~f:Values.CACertificateStatus.of_json
                            newStatus)
              ?newAutoRegistrationStatus:(Option.map
                                            ~f:Values.AutoRegistrationStatus.of_json
                                            newAutoRegistrationStatus)
              ?registrationConfig:(Option.map
                                     ~f:Values.RegistrationConfig.of_json
                                     registrationConfig)
              ?removeAutoRegistration ~certificateId ()) None None])
let update_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId"
       and newStatus =
         flag "new-status" (required json_arg) ~doc:"JSON CertificateStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_certificate
           (Values.UpdateCertificateRequest.make ~certificateId
              ~newStatus:(Values.CertificateStatus.of_json newStatus) ())
           None None])
let update_custom_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName"
       and displayName =
         flag "display-name" (required string)
           ~doc:"STRING CustomMetricDisplayName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_metric
           (Values.UpdateCustomMetricRequest.make ~metricName ~displayName ())
           (Some Values.UpdateCustomMetricResponse.to_json)
           (Some Values.UpdateCustomMetricResponse.error_to_json)])
let update_dimension =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DimensionName"
       and stringValues =
         flag "string-values" (required json_arg)
           ~doc:"JSON DimensionStringValues" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dimension
           (Values.UpdateDimensionRequest.make ~name
              ~stringValues:(Values.DimensionStringValues.of_json
                               stringValues) ())
           (Some Values.UpdateDimensionResponse.to_json)
           (Some Values.UpdateDimensionResponse.error_to_json)])
let update_domain_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorizerConfig =
         flag "authorizer-config" (optional json_arg)
           ~doc:"JSON AuthorizerConfig"
       and domainConfigurationStatus =
         flag "domain-configuration-status" (optional json_arg)
           ~doc:"JSON DomainConfigurationStatus"
       and removeAuthorizerConfig =
         flag "remove-authorizer-config" (optional bool)
           ~doc:"BOOL RemoveAuthorizerConfig"
       and domainConfigurationName =
         flag "domain-configuration-name" (required string)
           ~doc:"STRING ReservedDomainConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_configuration
           (Values.UpdateDomainConfigurationRequest.make
              ?authorizerConfig:(Option.map
                                   ~f:Values.AuthorizerConfig.of_json
                                   authorizerConfig)
              ?domainConfigurationStatus:(Option.map
                                            ~f:Values.DomainConfigurationStatus.of_json
                                            domainConfigurationStatus)
              ?removeAuthorizerConfig ~domainConfigurationName ())
           (Some Values.UpdateDomainConfigurationResponse.to_json)
           (Some Values.UpdateDomainConfigurationResponse.error_to_json)])
let update_dynamic_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and queryString =
         flag "query-string" (optional string) ~doc:"STRING QueryString"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and thingGroupName =
         flag "thing-group-name" (required string)
           ~doc:"STRING ThingGroupName"
       and thingGroupProperties =
         flag "thing-group-properties" (required json_arg)
           ~doc:"JSON ThingGroupProperties" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dynamic_thing_group
           (Values.UpdateDynamicThingGroupRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ?indexName ?queryString
              ?queryVersion ~thingGroupName
              ~thingGroupProperties:(Values.ThingGroupProperties.of_json
                                       thingGroupProperties) ())
           (Some Values.UpdateDynamicThingGroupResponse.to_json)
           (Some Values.UpdateDynamicThingGroupResponse.error_to_json)])
let update_event_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventConfigurations =
         flag "event-configurations" (optional json_arg)
           ~doc:"JSON EventConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_configurations
           (Values.UpdateEventConfigurationsRequest.make
              ?eventConfigurations:(Option.map
                                      ~f:Values.EventConfigurations.of_json
                                      eventConfigurations) ())
           (Some Values.UpdateEventConfigurationsResponse.to_json)
           (Some Values.UpdateEventConfigurationsResponse.error_to_json)])
let update_fleet_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryString =
         flag "query-string" (optional string) ~doc:"STRING QueryString"
       and aggregationType =
         flag "aggregation-type" (optional json_arg)
           ~doc:"JSON AggregationType"
       and period = flag "period" (optional int) ~doc:"INT FleetMetricPeriod"
       and aggregationField =
         flag "aggregation-field" (optional string)
           ~doc:"STRING AggregationField"
       and description =
         flag "description" (optional string)
           ~doc:"STRING FleetMetricDescription"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and unit = flag "unit" (optional json_arg) ~doc:"JSON FleetMetricUnit"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING FleetMetricName"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING IndexName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fleet_metric
           (Values.UpdateFleetMetricRequest.make ?queryString
              ?aggregationType:(Option.map ~f:Values.AggregationType.of_json
                                  aggregationType) ?period ?aggregationField
              ?description ?queryVersion
              ?unit:(Option.map ~f:Values.FleetMetricUnit.of_json unit)
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~metricName ~indexName ())
           None None])
let update_indexing_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingIndexingConfiguration =
         flag "thing-indexing-configuration" (optional json_arg)
           ~doc:"JSON ThingIndexingConfiguration"
       and thingGroupIndexingConfiguration =
         flag "thing-group-indexing-configuration" (optional json_arg)
           ~doc:"JSON ThingGroupIndexingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_indexing_configuration
           (Values.UpdateIndexingConfigurationRequest.make
              ?thingIndexingConfiguration:(Option.map
                                             ~f:Values.ThingIndexingConfiguration.of_json
                                             thingIndexingConfiguration)
              ?thingGroupIndexingConfiguration:(Option.map
                                                  ~f:Values.ThingGroupIndexingConfiguration.of_json
                                                  thingGroupIndexingConfiguration)
              ()) (Some Values.UpdateIndexingConfigurationResponse.to_json)
           (Some Values.UpdateIndexingConfigurationResponse.error_to_json)])
let update_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING JobDescription"
       and presignedUrlConfig =
         flag "presigned-url-config" (optional json_arg)
           ~doc:"JSON PresignedUrlConfig"
       and jobExecutionsRolloutConfig =
         flag "job-executions-rollout-config" (optional json_arg)
           ~doc:"JSON JobExecutionsRolloutConfig"
       and abortConfig =
         flag "abort-config" (optional json_arg) ~doc:"JSON AbortConfig"
       and timeoutConfig =
         flag "timeout-config" (optional json_arg) ~doc:"JSON TimeoutConfig"
       and namespaceId =
         flag "namespace-id" (optional string) ~doc:"STRING NamespaceId"
       and jobExecutionsRetryConfig =
         flag "job-executions-retry-config" (optional json_arg)
           ~doc:"JSON JobExecutionsRetryConfig"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job
           (Values.UpdateJobRequest.make ?description
              ?presignedUrlConfig:(Option.map
                                     ~f:Values.PresignedUrlConfig.of_json
                                     presignedUrlConfig)
              ?jobExecutionsRolloutConfig:(Option.map
                                             ~f:Values.JobExecutionsRolloutConfig.of_json
                                             jobExecutionsRolloutConfig)
              ?abortConfig:(Option.map ~f:Values.AbortConfig.of_json
                              abortConfig)
              ?timeoutConfig:(Option.map ~f:Values.TimeoutConfig.of_json
                                timeoutConfig) ?namespaceId
              ?jobExecutionsRetryConfig:(Option.map
                                           ~f:Values.JobExecutionsRetryConfig.of_json
                                           jobExecutionsRetryConfig) ~jobId
              ()) None None])
let update_mitigation_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and actionParams =
         flag "action-params" (optional json_arg)
           ~doc:"JSON MitigationActionParams"
       and actionName =
         flag "action-name" (required string)
           ~doc:"STRING MitigationActionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_mitigation_action
           (Values.UpdateMitigationActionRequest.make ?roleArn
              ?actionParams:(Option.map
                               ~f:Values.MitigationActionParams.of_json
                               actionParams) ~actionName ())
           (Some Values.UpdateMitigationActionResponse.to_json)
           (Some Values.UpdateMitigationActionResponse.error_to_json)])
let update_provisioning_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING TemplateDescription"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Enabled"
       and defaultVersionId =
         flag "default-version-id" (optional int)
           ~doc:"INT TemplateVersionId"
       and provisioningRoleArn =
         flag "provisioning-role-arn" (optional string) ~doc:"STRING RoleArn"
       and preProvisioningHook =
         flag "pre-provisioning-hook" (optional json_arg)
           ~doc:"JSON ProvisioningHook"
       and removePreProvisioningHook =
         flag "remove-pre-provisioning-hook" (optional bool)
           ~doc:"BOOL RemoveHook"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_provisioning_template
           (Values.UpdateProvisioningTemplateRequest.make ?description
              ?enabled ?defaultVersionId ?provisioningRoleArn
              ?preProvisioningHook:(Option.map
                                      ~f:Values.ProvisioningHook.of_json
                                      preProvisioningHook)
              ?removePreProvisioningHook ~templateName ())
           (Some Values.UpdateProvisioningTemplateResponse.to_json)
           (Some Values.UpdateProvisioningTemplateResponse.error_to_json)])
let update_role_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and credentialDurationSeconds =
         flag "credential-duration-seconds" (optional int)
           ~doc:"INT CredentialDurationSeconds"
       and roleAlias =
         flag "role-alias" (required string) ~doc:"STRING RoleAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_role_alias
           (Values.UpdateRoleAliasRequest.make ?roleArn
              ?credentialDurationSeconds ~roleAlias ())
           (Some Values.UpdateRoleAliasResponse.to_json)
           (Some Values.UpdateRoleAliasResponse.error_to_json)])
let update_scheduled_audit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and frequency =
         flag "frequency" (optional json_arg) ~doc:"JSON AuditFrequency"
       and dayOfMonth =
         flag "day-of-month" (optional string) ~doc:"STRING DayOfMonth"
       and dayOfWeek =
         flag "day-of-week" (optional json_arg) ~doc:"JSON DayOfWeek"
       and targetCheckNames =
         flag "target-check-names" (optional json_arg)
           ~doc:"JSON TargetAuditCheckNames"
       and scheduledAuditName =
         flag "scheduled-audit-name" (required string)
           ~doc:"STRING ScheduledAuditName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_scheduled_audit
           (Values.UpdateScheduledAuditRequest.make
              ?frequency:(Option.map ~f:Values.AuditFrequency.of_json
                            frequency) ?dayOfMonth
              ?dayOfWeek:(Option.map ~f:Values.DayOfWeek.of_json dayOfWeek)
              ?targetCheckNames:(Option.map
                                   ~f:Values.TargetAuditCheckNames.of_json
                                   targetCheckNames) ~scheduledAuditName ())
           (Some Values.UpdateScheduledAuditResponse.to_json)
           (Some Values.UpdateScheduledAuditResponse.error_to_json)])
let update_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityProfileDescription =
         flag "security-profile-description" (optional string)
           ~doc:"STRING SecurityProfileDescription"
       and behaviors =
         flag "behaviors" (optional json_arg) ~doc:"JSON Behaviors"
       and alertTargets =
         flag "alert-targets" (optional json_arg) ~doc:"JSON AlertTargets"
       and additionalMetricsToRetain =
         flag "additional-metrics-to-retain" (optional json_arg)
           ~doc:"JSON AdditionalMetricsToRetainList"
       and additionalMetricsToRetainV2 =
         flag "additional-metrics-to-retain-v2" (optional json_arg)
           ~doc:"JSON AdditionalMetricsToRetainV2List"
       and deleteBehaviors =
         flag "delete-behaviors" (optional bool) ~doc:"BOOL DeleteBehaviors"
       and deleteAlertTargets =
         flag "delete-alert-targets" (optional bool)
           ~doc:"BOOL DeleteAlertTargets"
       and deleteAdditionalMetricsToRetain =
         flag "delete-additional-metrics-to-retain" (optional bool)
           ~doc:"BOOL DeleteAdditionalMetricsToRetain"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and securityProfileName =
         flag "security-profile-name" (required string)
           ~doc:"STRING SecurityProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_security_profile
           (Values.UpdateSecurityProfileRequest.make
              ?securityProfileDescription
              ?behaviors:(Option.map ~f:Values.Behaviors.of_json behaviors)
              ?alertTargets:(Option.map ~f:Values.AlertTargets.of_json
                               alertTargets)
              ?additionalMetricsToRetain:(Option.map
                                            ~f:Values.AdditionalMetricsToRetainList.of_json
                                            additionalMetricsToRetain)
              ?additionalMetricsToRetainV2:(Option.map
                                              ~f:Values.AdditionalMetricsToRetainV2List.of_json
                                              additionalMetricsToRetainV2)
              ?deleteBehaviors ?deleteAlertTargets
              ?deleteAdditionalMetricsToRetain
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~securityProfileName ())
           (Some Values.UpdateSecurityProfileResponse.to_json)
           (Some Values.UpdateSecurityProfileResponse.error_to_json)])
let update_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING StreamDescription"
       and files = flag "files" (optional json_arg) ~doc:"JSON StreamFiles"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and streamId =
         flag "stream-id" (required string) ~doc:"STRING StreamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stream
           (Values.UpdateStreamRequest.make ?description
              ?files:(Option.map ~f:Values.StreamFiles.of_json files)
              ?roleArn ~streamId ())
           (Some Values.UpdateStreamResponse.to_json)
           (Some Values.UpdateStreamResponse.error_to_json)])
let update_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingTypeName =
         flag "thing-type-name" (optional string) ~doc:"STRING ThingTypeName"
       and attributePayload =
         flag "attribute-payload" (optional json_arg)
           ~doc:"JSON AttributePayload"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and removeThingType =
         flag "remove-thing-type" (optional bool) ~doc:"BOOL RemoveThingType"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_thing
           (Values.UpdateThingRequest.make ?thingTypeName
              ?attributePayload:(Option.map
                                   ~f:Values.AttributePayload.of_json
                                   attributePayload)
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ?removeThingType
              ~thingName ()) (Some Values.UpdateThingResponse.to_json)
           (Some Values.UpdateThingResponse.error_to_json)])
let update_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and thingGroupName =
         flag "thing-group-name" (required string)
           ~doc:"STRING ThingGroupName"
       and thingGroupProperties =
         flag "thing-group-properties" (required json_arg)
           ~doc:"JSON ThingGroupProperties" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_thing_group
           (Values.UpdateThingGroupRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~thingGroupName
              ~thingGroupProperties:(Values.ThingGroupProperties.of_json
                                       thingGroupProperties) ())
           (Some Values.UpdateThingGroupResponse.to_json)
           (Some Values.UpdateThingGroupResponse.error_to_json)])
let update_thing_groups_for_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (optional string) ~doc:"STRING ThingName"
       and thingGroupsToAdd =
         flag "thing-groups-to-add" (optional json_arg)
           ~doc:"JSON ThingGroupList"
       and thingGroupsToRemove =
         flag "thing-groups-to-remove" (optional json_arg)
           ~doc:"JSON ThingGroupList"
       and overrideDynamicGroups =
         flag "override-dynamic-groups" (optional bool)
           ~doc:"BOOL OverrideDynamicGroups" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_thing_groups_for_thing
           (Values.UpdateThingGroupsForThingRequest.make ?thingName
              ?thingGroupsToAdd:(Option.map ~f:Values.ThingGroupList.of_json
                                   thingGroupsToAdd)
              ?thingGroupsToRemove:(Option.map
                                      ~f:Values.ThingGroupList.of_json
                                      thingGroupsToRemove)
              ?overrideDynamicGroups ())
           (Some Values.UpdateThingGroupsForThingResponse.to_json)
           (Some Values.UpdateThingGroupsForThingResponse.error_to_json)])
let update_topic_rule_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING AwsArn"
       and status =
         flag "status" (required json_arg)
           ~doc:"JSON TopicRuleDestinationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_topic_rule_destination
           (Values.UpdateTopicRuleDestinationRequest.make ~arn
              ~status:(Values.TopicRuleDestinationStatus.of_json status) ())
           (Some Values.UpdateTopicRuleDestinationResponse.to_json)
           (Some Values.UpdateTopicRuleDestinationResponse.error_to_json)])
let validate_security_profile_behaviors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and behaviors =
         flag "behaviors" (required json_arg) ~doc:"JSON Behaviors" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_security_profile_behaviors
           (Values.ValidateSecurityProfileBehaviorsRequest.make
              ~behaviors:(Values.Behaviors.of_json behaviors) ())
           (Some Values.ValidateSecurityProfileBehaviorsResponse.to_json)
           (Some
              Values.ValidateSecurityProfileBehaviorsResponse.error_to_json)])
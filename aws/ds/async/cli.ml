(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_shared_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sharedDirectoryId =
         flag "shared-directory-id" (required string)
           ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_shared_directory
           (Values.AcceptSharedDirectoryRequest.make ~sharedDirectoryId ())
           (Some Values.AcceptSharedDirectoryResult.to_json)
           (Some Values.AcceptSharedDirectoryResult.error_to_json)])
let add_ip_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateSecurityGroupForDirectoryControllers =
         flag "update-security-group-for-directory-controllers"
           (optional bool)
           ~doc:"BOOL UpdateSecurityGroupForDirectoryControllers"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and ipRoutes =
         flag "ip-routes" (required json_arg) ~doc:"JSON IpRoutes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_ip_routes
           (Values.AddIpRoutesRequest.make
              ?updateSecurityGroupForDirectoryControllers ~directoryId
              ~ipRoutes:(Values.IpRoutes.of_json ipRoutes) ())
           (Some Values.AddIpRoutesResult.to_json)
           (Some Values.AddIpRoutesResult.error_to_json)])
let add_region =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and regionName =
         flag "region-name" (required string) ~doc:"STRING RegionName"
       and vPCSettings =
         flag "v-p-c-settings" (required json_arg)
           ~doc:"JSON DirectoryVpcSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_region
           (Values.AddRegionRequest.make ~directoryId ~regionName
              ~vPCSettings:(Values.DirectoryVpcSettings.of_json vPCSettings)
              ()) (Some Values.AddRegionResult.to_json)
           (Some Values.AddRegionResult.error_to_json)])
let add_tags_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_resource
           (Values.AddTagsToResourceRequest.make ~resourceId
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.AddTagsToResourceResult.to_json)
           (Some Values.AddTagsToResourceResult.error_to_json)])
let cancel_schema_extension =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and schemaExtensionId =
         flag "schema-extension-id" (required string)
           ~doc:"STRING SchemaExtensionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_schema_extension
           (Values.CancelSchemaExtensionRequest.make ~directoryId
              ~schemaExtensionId ())
           (Some Values.CancelSchemaExtensionResult.to_json)
           (Some Values.CancelSchemaExtensionResult.error_to_json)])
let connect_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shortName =
         flag "short-name" (optional string) ~doc:"STRING DirectoryShortName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING DirectoryName"
       and password =
         flag "password" (required string) ~doc:"STRING ConnectPassword"
       and size = flag "size" (required json_arg) ~doc:"JSON DirectorySize"
       and connectSettings =
         flag "connect-settings" (required json_arg)
           ~doc:"JSON DirectoryConnectSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.connect_directory
           (Values.ConnectDirectoryRequest.make ?shortName ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ~password
              ~size:(Values.DirectorySize.of_json size)
              ~connectSettings:(Values.DirectoryConnectSettings.of_json
                                  connectSettings) ())
           (Some Values.ConnectDirectoryResult.to_json)
           (Some Values.ConnectDirectoryResult.error_to_json)])
let create_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and alias = flag "alias" (required string) ~doc:"STRING AliasName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_alias
           (Values.CreateAliasRequest.make ~directoryId ~alias ())
           (Some Values.CreateAliasResult.to_json)
           (Some Values.CreateAliasResult.error_to_json)])
let create_computer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationalUnitDistinguishedName =
         flag "organizational-unit-distinguished-name" (optional string)
           ~doc:"STRING OrganizationalUnitDN"
       and computerAttributes =
         flag "computer-attributes" (optional json_arg)
           ~doc:"JSON Attributes"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and computerName =
         flag "computer-name" (required string) ~doc:"STRING ComputerName"
       and password =
         flag "password" (required string) ~doc:"STRING ComputerPassword" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_computer
           (Values.CreateComputerRequest.make
              ?organizationalUnitDistinguishedName
              ?computerAttributes:(Option.map ~f:Values.Attributes.of_json
                                     computerAttributes) ~directoryId
              ~computerName ~password ())
           (Some Values.CreateComputerResult.to_json)
           (Some Values.CreateComputerResult.error_to_json)])
let create_conditional_forwarder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and remoteDomainName =
         flag "remote-domain-name" (required string)
           ~doc:"STRING RemoteDomainName"
       and dnsIpAddrs =
         flag "dns-ip-addrs" (required json_arg) ~doc:"JSON DnsIpAddrs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_conditional_forwarder
           (Values.CreateConditionalForwarderRequest.make ~directoryId
              ~remoteDomainName
              ~dnsIpAddrs:(Values.DnsIpAddrs.of_json dnsIpAddrs) ())
           (Some Values.CreateConditionalForwarderResult.to_json)
           (Some Values.CreateConditionalForwarderResult.error_to_json)])
let create_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shortName =
         flag "short-name" (optional string) ~doc:"STRING DirectoryShortName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and vpcSettings =
         flag "vpc-settings" (optional json_arg)
           ~doc:"JSON DirectoryVpcSettings"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING DirectoryName"
       and password =
         flag "password" (required string) ~doc:"STRING Password"
       and size = flag "size" (required json_arg) ~doc:"JSON DirectorySize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_directory
           (Values.CreateDirectoryRequest.make ?shortName ?description
              ?vpcSettings:(Option.map ~f:Values.DirectoryVpcSettings.of_json
                              vpcSettings)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ~password
              ~size:(Values.DirectorySize.of_json size) ())
           (Some Values.CreateDirectoryResult.to_json)
           (Some Values.CreateDirectoryResult.error_to_json)])
let create_log_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and logGroupName =
         flag "log-group-name" (required string) ~doc:"STRING LogGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_log_subscription
           (Values.CreateLogSubscriptionRequest.make ~directoryId
              ~logGroupName ())
           (Some Values.CreateLogSubscriptionResult.to_json)
           (Some Values.CreateLogSubscriptionResult.error_to_json)])
let create_microsoft_a_d =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shortName =
         flag "short-name" (optional string) ~doc:"STRING DirectoryShortName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and edition =
         flag "edition" (optional json_arg) ~doc:"JSON DirectoryEdition"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING DirectoryName"
       and password =
         flag "password" (required string) ~doc:"STRING Password"
       and vpcSettings =
         flag "vpc-settings" (required json_arg)
           ~doc:"JSON DirectoryVpcSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_microsoft_a_d
           (Values.CreateMicrosoftADRequest.make ?shortName ?description
              ?edition:(Option.map ~f:Values.DirectoryEdition.of_json edition)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ~password
              ~vpcSettings:(Values.DirectoryVpcSettings.of_json vpcSettings)
              ()) (Some Values.CreateMicrosoftADResult.to_json)
           (Some Values.CreateMicrosoftADResult.error_to_json)])
let create_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING SnapshotName"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot
           (Values.CreateSnapshotRequest.make ?name ~directoryId ())
           (Some Values.CreateSnapshotResult.to_json)
           (Some Values.CreateSnapshotResult.error_to_json)])
let create_trust =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trustType =
         flag "trust-type" (optional json_arg) ~doc:"JSON TrustType"
       and conditionalForwarderIpAddrs =
         flag "conditional-forwarder-ip-addrs" (optional json_arg)
           ~doc:"JSON DnsIpAddrs"
       and selectiveAuth =
         flag "selective-auth" (optional json_arg) ~doc:"JSON SelectiveAuth"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and remoteDomainName =
         flag "remote-domain-name" (required string)
           ~doc:"STRING RemoteDomainName"
       and trustPassword =
         flag "trust-password" (required string) ~doc:"STRING TrustPassword"
       and trustDirection =
         flag "trust-direction" (required json_arg)
           ~doc:"JSON TrustDirection" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_trust
           (Values.CreateTrustRequest.make
              ?trustType:(Option.map ~f:Values.TrustType.of_json trustType)
              ?conditionalForwarderIpAddrs:(Option.map
                                              ~f:Values.DnsIpAddrs.of_json
                                              conditionalForwarderIpAddrs)
              ?selectiveAuth:(Option.map ~f:Values.SelectiveAuth.of_json
                                selectiveAuth) ~directoryId ~remoteDomainName
              ~trustPassword
              ~trustDirection:(Values.TrustDirection.of_json trustDirection)
              ()) (Some Values.CreateTrustResult.to_json)
           (Some Values.CreateTrustResult.error_to_json)])
let delete_conditional_forwarder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and remoteDomainName =
         flag "remote-domain-name" (required string)
           ~doc:"STRING RemoteDomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_conditional_forwarder
           (Values.DeleteConditionalForwarderRequest.make ~directoryId
              ~remoteDomainName ())
           (Some Values.DeleteConditionalForwarderResult.to_json)
           (Some Values.DeleteConditionalForwarderResult.error_to_json)])
let delete_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_directory
           (Values.DeleteDirectoryRequest.make ~directoryId ())
           (Some Values.DeleteDirectoryResult.to_json)
           (Some Values.DeleteDirectoryResult.error_to_json)])
let delete_log_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_log_subscription
           (Values.DeleteLogSubscriptionRequest.make ~directoryId ())
           (Some Values.DeleteLogSubscriptionResult.to_json)
           (Some Values.DeleteLogSubscriptionResult.error_to_json)])
let delete_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot
           (Values.DeleteSnapshotRequest.make ~snapshotId ())
           (Some Values.DeleteSnapshotResult.to_json)
           (Some Values.DeleteSnapshotResult.error_to_json)])
let delete_trust =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteAssociatedConditionalForwarder =
         flag "delete-associated-conditional-forwarder" (optional bool)
           ~doc:"BOOL DeleteAssociatedConditionalForwarder"
       and trustId = flag "trust-id" (required string) ~doc:"STRING TrustId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_trust
           (Values.DeleteTrustRequest.make
              ?deleteAssociatedConditionalForwarder ~trustId ())
           (Some Values.DeleteTrustResult.to_json)
           (Some Values.DeleteTrustResult.error_to_json)])
let deregister_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_certificate
           (Values.DeregisterCertificateRequest.make ~directoryId
              ~certificateId ())
           (Some Values.DeregisterCertificateResult.to_json)
           (Some Values.DeregisterCertificateResult.error_to_json)])
let deregister_event_topic =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and topicName =
         flag "topic-name" (required string) ~doc:"STRING TopicName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_event_topic
           (Values.DeregisterEventTopicRequest.make ~directoryId ~topicName
              ()) (Some Values.DeregisterEventTopicResult.to_json)
           (Some Values.DeregisterEventTopicResult.error_to_json)])
let describe_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_certificate
           (Values.DescribeCertificateRequest.make ~directoryId
              ~certificateId ())
           (Some Values.DescribeCertificateResult.to_json)
           (Some Values.DescribeCertificateResult.error_to_json)])
let describe_client_authentication_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg)
           ~doc:"JSON ClientAuthenticationType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_client_authentication_settings
           (Values.DescribeClientAuthenticationSettingsRequest.make
              ?type_:(Option.map ~f:Values.ClientAuthenticationType.of_json
                        type_) ?nextToken ?limit ~directoryId ())
           (Some Values.DescribeClientAuthenticationSettingsResult.to_json)
           (Some
              Values.DescribeClientAuthenticationSettingsResult.error_to_json)])
let describe_conditional_forwarders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and remoteDomainNames =
         flag "remote-domain-names" (optional json_arg)
           ~doc:"JSON RemoteDomainNames"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_conditional_forwarders
           (Values.DescribeConditionalForwardersRequest.make
              ?remoteDomainNames:(Option.map
                                    ~f:Values.RemoteDomainNames.of_json
                                    remoteDomainNames) ~directoryId ())
           (Some Values.DescribeConditionalForwardersResult.to_json)
           (Some Values.DescribeConditionalForwardersResult.error_to_json)])
let describe_directories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryIds =
         flag "directory-ids" (optional json_arg) ~doc:"JSON DirectoryIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_directories
           (Values.DescribeDirectoriesRequest.make
              ?directoryIds:(Option.map ~f:Values.DirectoryIds.of_json
                               directoryIds) ?nextToken ?limit ())
           (Some Values.DescribeDirectoriesResult.to_json)
           (Some Values.DescribeDirectoriesResult.error_to_json)])
let describe_domain_controllers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainControllerIds =
         flag "domain-controller-ids" (optional json_arg)
           ~doc:"JSON DomainControllerIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain_controllers
           (Values.DescribeDomainControllersRequest.make
              ?domainControllerIds:(Option.map
                                      ~f:Values.DomainControllerIds.of_json
                                      domainControllerIds) ?nextToken ?limit
              ~directoryId ())
           (Some Values.DescribeDomainControllersResult.to_json)
           (Some Values.DescribeDomainControllersResult.error_to_json)])
let describe_event_topics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (optional string) ~doc:"STRING DirectoryId"
       and topicNames =
         flag "topic-names" (optional json_arg) ~doc:"JSON TopicNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_topics
           (Values.DescribeEventTopicsRequest.make ?directoryId
              ?topicNames:(Option.map ~f:Values.TopicNames.of_json topicNames)
              ()) (Some Values.DescribeEventTopicsResult.to_json)
           (Some Values.DescribeEventTopicsResult.error_to_json)])
let describe_l_d_a_p_s_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON LDAPSType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_l_d_a_p_s_settings
           (Values.DescribeLDAPSSettingsRequest.make
              ?type_:(Option.map ~f:Values.LDAPSType.of_json type_)
              ?nextToken ?limit ~directoryId ())
           (Some Values.DescribeLDAPSSettingsResult.to_json)
           (Some Values.DescribeLDAPSSettingsResult.error_to_json)])
let describe_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regionName =
         flag "region-name" (optional string) ~doc:"STRING RegionName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_regions
           (Values.DescribeRegionsRequest.make ?regionName ?nextToken
              ~directoryId ()) (Some Values.DescribeRegionsResult.to_json)
           (Some Values.DescribeRegionsResult.error_to_json)])
let describe_shared_directories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sharedDirectoryIds =
         flag "shared-directory-ids" (optional json_arg)
           ~doc:"JSON DirectoryIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit"
       and ownerDirectoryId =
         flag "owner-directory-id" (required string)
           ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_shared_directories
           (Values.DescribeSharedDirectoriesRequest.make
              ?sharedDirectoryIds:(Option.map ~f:Values.DirectoryIds.of_json
                                     sharedDirectoryIds) ?nextToken ?limit
              ~ownerDirectoryId ())
           (Some Values.DescribeSharedDirectoriesResult.to_json)
           (Some Values.DescribeSharedDirectoriesResult.error_to_json)])
let describe_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (optional string) ~doc:"STRING DirectoryId"
       and snapshotIds =
         flag "snapshot-ids" (optional json_arg) ~doc:"JSON SnapshotIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_snapshots
           (Values.DescribeSnapshotsRequest.make ?directoryId
              ?snapshotIds:(Option.map ~f:Values.SnapshotIds.of_json
                              snapshotIds) ?nextToken ?limit ())
           (Some Values.DescribeSnapshotsResult.to_json)
           (Some Values.DescribeSnapshotsResult.error_to_json)])
let describe_trusts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (optional string) ~doc:"STRING DirectoryId"
       and trustIds =
         flag "trust-ids" (optional json_arg) ~doc:"JSON TrustIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_trusts
           (Values.DescribeTrustsRequest.make ?directoryId
              ?trustIds:(Option.map ~f:Values.TrustIds.of_json trustIds)
              ?nextToken ?limit ())
           (Some Values.DescribeTrustsResult.to_json)
           (Some Values.DescribeTrustsResult.error_to_json)])
let disable_client_authentication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and type_ =
         flag "type-" (required json_arg)
           ~doc:"JSON ClientAuthenticationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_client_authentication
           (Values.DisableClientAuthenticationRequest.make ~directoryId
              ~type_:(Values.ClientAuthenticationType.of_json type_) ())
           (Some Values.DisableClientAuthenticationResult.to_json)
           (Some Values.DisableClientAuthenticationResult.error_to_json)])
let disable_l_d_a_p_s =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON LDAPSType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_l_d_a_p_s
           (Values.DisableLDAPSRequest.make ~directoryId
              ~type_:(Values.LDAPSType.of_json type_) ())
           (Some Values.DisableLDAPSResult.to_json)
           (Some Values.DisableLDAPSResult.error_to_json)])
let disable_radius =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_radius
           (Values.DisableRadiusRequest.make ~directoryId ())
           (Some Values.DisableRadiusResult.to_json)
           (Some Values.DisableRadiusResult.error_to_json)])
let disable_sso =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING UserName"
       and password =
         flag "password" (optional string) ~doc:"STRING ConnectPassword"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_sso
           (Values.DisableSsoRequest.make ?userName ?password ~directoryId ())
           (Some Values.DisableSsoResult.to_json)
           (Some Values.DisableSsoResult.error_to_json)])
let enable_client_authentication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and type_ =
         flag "type-" (required json_arg)
           ~doc:"JSON ClientAuthenticationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_client_authentication
           (Values.EnableClientAuthenticationRequest.make ~directoryId
              ~type_:(Values.ClientAuthenticationType.of_json type_) ())
           (Some Values.EnableClientAuthenticationResult.to_json)
           (Some Values.EnableClientAuthenticationResult.error_to_json)])
let enable_l_d_a_p_s =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON LDAPSType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_l_d_a_p_s
           (Values.EnableLDAPSRequest.make ~directoryId
              ~type_:(Values.LDAPSType.of_json type_) ())
           (Some Values.EnableLDAPSResult.to_json)
           (Some Values.EnableLDAPSResult.error_to_json)])
let enable_radius =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and radiusSettings =
         flag "radius-settings" (required json_arg)
           ~doc:"JSON RadiusSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_radius
           (Values.EnableRadiusRequest.make ~directoryId
              ~radiusSettings:(Values.RadiusSettings.of_json radiusSettings)
              ()) (Some Values.EnableRadiusResult.to_json)
           (Some Values.EnableRadiusResult.error_to_json)])
let enable_sso =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING UserName"
       and password =
         flag "password" (optional string) ~doc:"STRING ConnectPassword"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_sso
           (Values.EnableSsoRequest.make ?userName ?password ~directoryId ())
           (Some Values.EnableSsoResult.to_json)
           (Some Values.EnableSsoResult.error_to_json)])
let get_directory_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_directory_limits (Values.GetDirectoryLimitsRequest.make ())
           (Some Values.GetDirectoryLimitsResult.to_json)
           (Some Values.GetDirectoryLimitsResult.error_to_json)])
let get_snapshot_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_snapshot_limits
           (Values.GetSnapshotLimitsRequest.make ~directoryId ())
           (Some Values.GetSnapshotLimitsResult.to_json)
           (Some Values.GetSnapshotLimitsResult.error_to_json)])
let list_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_certificates
           (Values.ListCertificatesRequest.make ?nextToken ?limit
              ~directoryId ()) (Some Values.ListCertificatesResult.to_json)
           (Some Values.ListCertificatesResult.error_to_json)])
let list_ip_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ip_routes
           (Values.ListIpRoutesRequest.make ?nextToken ?limit ~directoryId ())
           (Some Values.ListIpRoutesResult.to_json)
           (Some Values.ListIpRoutesResult.error_to_json)])
let list_log_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (optional string) ~doc:"STRING DirectoryId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_log_subscriptions
           (Values.ListLogSubscriptionsRequest.make ?directoryId ?nextToken
              ?limit ()) (Some Values.ListLogSubscriptionsResult.to_json)
           (Some Values.ListLogSubscriptionsResult.error_to_json)])
let list_schema_extensions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schema_extensions
           (Values.ListSchemaExtensionsRequest.make ?nextToken ?limit
              ~directoryId ())
           (Some Values.ListSchemaExtensionsResult.to_json)
           (Some Values.ListSchemaExtensionsResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and limit = flag "limit" (optional int) ~doc:"INT Limit"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ?limit
              ~resourceId ()) (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let register_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON CertificateType"
       and clientCertAuthSettings =
         flag "client-cert-auth-settings" (optional json_arg)
           ~doc:"JSON ClientCertAuthSettings"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and certificateData =
         flag "certificate-data" (required string)
           ~doc:"STRING CertificateData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_certificate
           (Values.RegisterCertificateRequest.make
              ?type_:(Option.map ~f:Values.CertificateType.of_json type_)
              ?clientCertAuthSettings:(Option.map
                                         ~f:Values.ClientCertAuthSettings.of_json
                                         clientCertAuthSettings) ~directoryId
              ~certificateData ())
           (Some Values.RegisterCertificateResult.to_json)
           (Some Values.RegisterCertificateResult.error_to_json)])
let register_event_topic =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and topicName =
         flag "topic-name" (required string) ~doc:"STRING TopicName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_event_topic
           (Values.RegisterEventTopicRequest.make ~directoryId ~topicName ())
           (Some Values.RegisterEventTopicResult.to_json)
           (Some Values.RegisterEventTopicResult.error_to_json)])
let reject_shared_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sharedDirectoryId =
         flag "shared-directory-id" (required string)
           ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_shared_directory
           (Values.RejectSharedDirectoryRequest.make ~sharedDirectoryId ())
           (Some Values.RejectSharedDirectoryResult.to_json)
           (Some Values.RejectSharedDirectoryResult.error_to_json)])
let remove_ip_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and cidrIps = flag "cidr-ips" (required json_arg) ~doc:"JSON CidrIps" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_ip_routes
           (Values.RemoveIpRoutesRequest.make ~directoryId
              ~cidrIps:(Values.CidrIps.of_json cidrIps) ())
           (Some Values.RemoveIpRoutesResult.to_json)
           (Some Values.RemoveIpRoutesResult.error_to_json)])
let remove_region =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_region (Values.RemoveRegionRequest.make ~directoryId ())
           (Some Values.RemoveRegionResult.to_json)
           (Some Values.RemoveRegionResult.error_to_json)])
let remove_tags_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_resource
           (Values.RemoveTagsFromResourceRequest.make ~resourceId
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.RemoveTagsFromResourceResult.to_json)
           (Some Values.RemoveTagsFromResourceResult.error_to_json)])
let reset_user_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING CustomerUserName"
       and newPassword =
         flag "new-password" (required string) ~doc:"STRING UserPassword" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_user_password
           (Values.ResetUserPasswordRequest.make ~directoryId ~userName
              ~newPassword ()) (Some Values.ResetUserPasswordResult.to_json)
           (Some Values.ResetUserPasswordResult.error_to_json)])
let restore_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_from_snapshot
           (Values.RestoreFromSnapshotRequest.make ~snapshotId ())
           (Some Values.RestoreFromSnapshotResult.to_json)
           (Some Values.RestoreFromSnapshotResult.error_to_json)])
let share_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareNotes =
         flag "share-notes" (optional string) ~doc:"STRING Notes"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and shareTarget =
         flag "share-target" (required json_arg) ~doc:"JSON ShareTarget"
       and shareMethod =
         flag "share-method" (required json_arg) ~doc:"JSON ShareMethod" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.share_directory
           (Values.ShareDirectoryRequest.make ?shareNotes ~directoryId
              ~shareTarget:(Values.ShareTarget.of_json shareTarget)
              ~shareMethod:(Values.ShareMethod.of_json shareMethod) ())
           (Some Values.ShareDirectoryResult.to_json)
           (Some Values.ShareDirectoryResult.error_to_json)])
let start_schema_extension =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and createSnapshotBeforeSchemaExtension =
         flag "create-snapshot-before-schema-extension" (required bool)
           ~doc:"BOOL CreateSnapshotBeforeSchemaExtension"
       and ldifContent =
         flag "ldif-content" (required string) ~doc:"STRING LdifContent"
       and description =
         flag "description" (required string) ~doc:"STRING Description" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_schema_extension
           (Values.StartSchemaExtensionRequest.make ~directoryId
              ~createSnapshotBeforeSchemaExtension ~ldifContent ~description
              ()) (Some Values.StartSchemaExtensionResult.to_json)
           (Some Values.StartSchemaExtensionResult.error_to_json)])
let unshare_directory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and unshareTarget =
         flag "unshare-target" (required json_arg) ~doc:"JSON UnshareTarget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unshare_directory
           (Values.UnshareDirectoryRequest.make ~directoryId
              ~unshareTarget:(Values.UnshareTarget.of_json unshareTarget) ())
           (Some Values.UnshareDirectoryResult.to_json)
           (Some Values.UnshareDirectoryResult.error_to_json)])
let update_conditional_forwarder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and remoteDomainName =
         flag "remote-domain-name" (required string)
           ~doc:"STRING RemoteDomainName"
       and dnsIpAddrs =
         flag "dns-ip-addrs" (required json_arg) ~doc:"JSON DnsIpAddrs" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_conditional_forwarder
           (Values.UpdateConditionalForwarderRequest.make ~directoryId
              ~remoteDomainName
              ~dnsIpAddrs:(Values.DnsIpAddrs.of_json dnsIpAddrs) ())
           (Some Values.UpdateConditionalForwarderResult.to_json)
           (Some Values.UpdateConditionalForwarderResult.error_to_json)])
let update_number_of_domain_controllers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and desiredNumber =
         flag "desired-number" (required int)
           ~doc:"INT DesiredNumberOfDomainControllers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_number_of_domain_controllers
           (Values.UpdateNumberOfDomainControllersRequest.make ~directoryId
              ~desiredNumber ())
           (Some Values.UpdateNumberOfDomainControllersResult.to_json)
           (Some Values.UpdateNumberOfDomainControllersResult.error_to_json)])
let update_radius =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (required string) ~doc:"STRING DirectoryId"
       and radiusSettings =
         flag "radius-settings" (required json_arg)
           ~doc:"JSON RadiusSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_radius
           (Values.UpdateRadiusRequest.make ~directoryId
              ~radiusSettings:(Values.RadiusSettings.of_json radiusSettings)
              ()) (Some Values.UpdateRadiusResult.to_json)
           (Some Values.UpdateRadiusResult.error_to_json)])
let update_trust =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and selectiveAuth =
         flag "selective-auth" (optional json_arg) ~doc:"JSON SelectiveAuth"
       and trustId = flag "trust-id" (required string) ~doc:"STRING TrustId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_trust
           (Values.UpdateTrustRequest.make
              ?selectiveAuth:(Option.map ~f:Values.SelectiveAuth.of_json
                                selectiveAuth) ~trustId ())
           (Some Values.UpdateTrustResult.to_json)
           (Some Values.UpdateTrustResult.error_to_json)])
let verify_trust =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trustId = flag "trust-id" (required string) ~doc:"STRING TrustId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_trust (Values.VerifyTrustRequest.make ~trustId ())
           (Some Values.VerifyTrustResult.to_json)
           (Some Values.VerifyTrustResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-shared-directory", accept_shared_directory);
    ("add-ip-routes", add_ip_routes);
    ("add-region", add_region);
    ("add-tags-to-resource", add_tags_to_resource);
    ("cancel-schema-extension", cancel_schema_extension);
    ("connect-directory", connect_directory);
    ("create-alias", create_alias);
    ("create-computer", create_computer);
    ("create-conditional-forwarder", create_conditional_forwarder);
    ("create-directory", create_directory);
    ("create-log-subscription", create_log_subscription);
    ("create-microsoft-a-d", create_microsoft_a_d);
    ("create-snapshot", create_snapshot);
    ("create-trust", create_trust);
    ("delete-conditional-forwarder", delete_conditional_forwarder);
    ("delete-directory", delete_directory);
    ("delete-log-subscription", delete_log_subscription);
    ("delete-snapshot", delete_snapshot);
    ("delete-trust", delete_trust);
    ("deregister-certificate", deregister_certificate);
    ("deregister-event-topic", deregister_event_topic);
    ("describe-certificate", describe_certificate);
    ("describe-client-authentication-settings",
      describe_client_authentication_settings);
    ("describe-conditional-forwarders", describe_conditional_forwarders);
    ("describe-directories", describe_directories);
    ("describe-domain-controllers", describe_domain_controllers);
    ("describe-event-topics", describe_event_topics);
    ("describe-l-d-a-p-s-settings", describe_l_d_a_p_s_settings);
    ("describe-regions", describe_regions);
    ("describe-shared-directories", describe_shared_directories);
    ("describe-snapshots", describe_snapshots);
    ("describe-trusts", describe_trusts);
    ("disable-client-authentication", disable_client_authentication);
    ("disable-l-d-a-p-s", disable_l_d_a_p_s);
    ("disable-radius", disable_radius);
    ("disable-sso", disable_sso);
    ("enable-client-authentication", enable_client_authentication);
    ("enable-l-d-a-p-s", enable_l_d_a_p_s);
    ("enable-radius", enable_radius);
    ("enable-sso", enable_sso);
    ("get-directory-limits", get_directory_limits);
    ("get-snapshot-limits", get_snapshot_limits);
    ("list-certificates", list_certificates);
    ("list-ip-routes", list_ip_routes);
    ("list-log-subscriptions", list_log_subscriptions);
    ("list-schema-extensions", list_schema_extensions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-certificate", register_certificate);
    ("register-event-topic", register_event_topic);
    ("reject-shared-directory", reject_shared_directory);
    ("remove-ip-routes", remove_ip_routes);
    ("remove-region", remove_region);
    ("remove-tags-from-resource", remove_tags_from_resource);
    ("reset-user-password", reset_user_password);
    ("restore-from-snapshot", restore_from_snapshot);
    ("share-directory", share_directory);
    ("start-schema-extension", start_schema_extension);
    ("unshare-directory", unshare_directory);
    ("update-conditional-forwarder", update_conditional_forwarder);
    ("update-number-of-domain-controllers",
      update_number_of_domain_controllers);
    ("update-radius", update_radius);
    ("update-trust", update_trust);
    ("verify-trust", verify_trust)]
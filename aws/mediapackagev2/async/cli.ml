(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_harvest_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eTag = flag "e-tag" (optional string) ~doc:"STRING EntityTag"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName"
       and harvestJobName =
         flag "harvest-job-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_harvest_job
           (Values.CancelHarvestJobRequest.make ?eTag ~channelGroupName
              ~channelName ~originEndpointName ~harvestJobName ())
           (Some Values.CancelHarvestJobResponse.to_json)
           (Some Values.CancelHarvestJobResponse.error_to_json)])
let create_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and inputType =
         flag "input-type" (optional json_arg) ~doc:"JSON InputType"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and inputSwitchConfiguration =
         flag "input-switch-configuration" (optional json_arg)
           ~doc:"JSON InputSwitchConfiguration"
       and outputHeaderConfiguration =
         flag "output-header-configuration" (optional json_arg)
           ~doc:"JSON OutputHeaderConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel
           (Values.CreateChannelRequest.make ?clientToken
              ?inputType:(Option.map ~f:Values.InputType.of_json inputType)
              ?description
              ?inputSwitchConfiguration:(Option.map
                                           ~f:Values.InputSwitchConfiguration.of_json
                                           inputSwitchConfiguration)
              ?outputHeaderConfiguration:(Option.map
                                            ~f:Values.OutputHeaderConfiguration.of_json
                                            outputHeaderConfiguration)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~channelGroupName ~channelName ())
           (Some Values.CreateChannelResponse.to_json)
           (Some Values.CreateChannelResponse.error_to_json)])
let create_channel_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_group
           (Values.CreateChannelGroupRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~channelGroupName ())
           (Some Values.CreateChannelGroupResponse.to_json)
           (Some Values.CreateChannelGroupResponse.error_to_json)])
let create_harvest_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and harvestJobName =
         flag "harvest-job-name" (optional string) ~doc:"STRING ResourceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName"
       and harvestedManifests =
         flag "harvested-manifests" (required json_arg)
           ~doc:"JSON HarvestedManifests"
       and scheduleConfiguration =
         flag "schedule-configuration" (required json_arg)
           ~doc:"JSON HarvesterScheduleConfiguration"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON Destination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_harvest_job
           (Values.CreateHarvestJobRequest.make ?description ?clientToken
              ?harvestJobName
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~channelGroupName ~channelName ~originEndpointName
              ~harvestedManifests:(Values.HarvestedManifests.of_json
                                     harvestedManifests)
              ~scheduleConfiguration:(Values.HarvesterScheduleConfiguration.of_json
                                        scheduleConfiguration)
              ~destination:(Values.Destination.of_json destination) ())
           (Some Values.CreateHarvestJobResponse.to_json)
           (Some Values.CreateHarvestJobResponse.error_to_json)])
let create_origin_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and segment = flag "segment" (optional json_arg) ~doc:"JSON Segment"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and startoverWindowSeconds =
         flag "startover-window-seconds" (optional int)
           ~doc:"INT CreateOriginEndpointRequestStartoverWindowSecondsInteger"
       and hlsManifests =
         flag "hls-manifests" (optional json_arg)
           ~doc:"JSON CreateHlsManifests"
       and lowLatencyHlsManifests =
         flag "low-latency-hls-manifests" (optional json_arg)
           ~doc:"JSON CreateLowLatencyHlsManifests"
       and dashManifests =
         flag "dash-manifests" (optional json_arg)
           ~doc:"JSON CreateDashManifests"
       and mssManifests =
         flag "mss-manifests" (optional json_arg)
           ~doc:"JSON CreateMssManifests"
       and forceEndpointErrorConfiguration =
         flag "force-endpoint-error-configuration" (optional json_arg)
           ~doc:"JSON ForceEndpointErrorConfiguration"
       and uriSeparator =
         flag "uri-separator" (optional json_arg) ~doc:"JSON UriSeparator"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName"
       and containerType =
         flag "container-type" (required json_arg) ~doc:"JSON ContainerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_origin_endpoint
           (Values.CreateOriginEndpointRequest.make
              ?segment:(Option.map ~f:Values.Segment.of_json segment)
              ?clientToken ?description ?startoverWindowSeconds
              ?hlsManifests:(Option.map ~f:Values.CreateHlsManifests.of_json
                               hlsManifests)
              ?lowLatencyHlsManifests:(Option.map
                                         ~f:Values.CreateLowLatencyHlsManifests.of_json
                                         lowLatencyHlsManifests)
              ?dashManifests:(Option.map
                                ~f:Values.CreateDashManifests.of_json
                                dashManifests)
              ?mssManifests:(Option.map ~f:Values.CreateMssManifests.of_json
                               mssManifests)
              ?forceEndpointErrorConfiguration:(Option.map
                                                  ~f:Values.ForceEndpointErrorConfiguration.of_json
                                                  forceEndpointErrorConfiguration)
              ?uriSeparator:(Option.map ~f:Values.UriSeparator.of_json
                               uriSeparator)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~channelGroupName ~channelName ~originEndpointName
              ~containerType:(Values.ContainerType.of_json containerType) ())
           (Some Values.CreateOriginEndpointResponse.to_json)
           (Some Values.CreateOriginEndpointResponse.error_to_json)])
let delete_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel
           (Values.DeleteChannelRequest.make ~channelGroupName ~channelName
              ()) (Some Values.DeleteChannelResponse.to_json)
           (Some Values.DeleteChannelResponse.error_to_json)])
let delete_channel_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_group
           (Values.DeleteChannelGroupRequest.make ~channelGroupName ())
           (Some Values.DeleteChannelGroupResponse.to_json)
           (Some Values.DeleteChannelGroupResponse.error_to_json)])
let delete_channel_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel_policy
           (Values.DeleteChannelPolicyRequest.make ~channelGroupName
              ~channelName ())
           (Some Values.DeleteChannelPolicyResponse.to_json)
           (Some Values.DeleteChannelPolicyResponse.error_to_json)])
let delete_origin_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_origin_endpoint
           (Values.DeleteOriginEndpointRequest.make ~channelGroupName
              ~channelName ~originEndpointName ())
           (Some Values.DeleteOriginEndpointResponse.to_json)
           (Some Values.DeleteOriginEndpointResponse.error_to_json)])
let delete_origin_endpoint_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_origin_endpoint_policy
           (Values.DeleteOriginEndpointPolicyRequest.make ~channelGroupName
              ~channelName ~originEndpointName ())
           (Some Values.DeleteOriginEndpointPolicyResponse.to_json)
           (Some Values.DeleteOriginEndpointPolicyResponse.error_to_json)])
let get_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_channel
           (Values.GetChannelRequest.make ~channelGroupName ~channelName ())
           (Some Values.GetChannelResponse.to_json)
           (Some Values.GetChannelResponse.error_to_json)])
let get_channel_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_channel_group
           (Values.GetChannelGroupRequest.make ~channelGroupName ())
           (Some Values.GetChannelGroupResponse.to_json)
           (Some Values.GetChannelGroupResponse.error_to_json)])
let get_channel_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_channel_policy
           (Values.GetChannelPolicyRequest.make ~channelGroupName
              ~channelName ()) (Some Values.GetChannelPolicyResponse.to_json)
           (Some Values.GetChannelPolicyResponse.error_to_json)])
let get_harvest_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName"
       and harvestJobName =
         flag "harvest-job-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_harvest_job
           (Values.GetHarvestJobRequest.make ~channelGroupName ~channelName
              ~originEndpointName ~harvestJobName ())
           (Some Values.GetHarvestJobResponse.to_json)
           (Some Values.GetHarvestJobResponse.error_to_json)])
let get_origin_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_origin_endpoint
           (Values.GetOriginEndpointRequest.make ~channelGroupName
              ~channelName ~originEndpointName ())
           (Some Values.GetOriginEndpointResponse.to_json)
           (Some Values.GetOriginEndpointResponse.error_to_json)])
let get_origin_endpoint_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_origin_endpoint_policy
           (Values.GetOriginEndpointPolicyRequest.make ~channelGroupName
              ~channelName ~originEndpointName ())
           (Some Values.GetOriginEndpointPolicyResponse.to_json)
           (Some Values.GetOriginEndpointPolicyResponse.error_to_json)])
let list_channel_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListResourceMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_groups
           (Values.ListChannelGroupsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListChannelGroupsResponse.to_json)
           (Some Values.ListChannelGroupsResponse.error_to_json)])
let list_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListResourceMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels
           (Values.ListChannelsRequest.make ?maxResults ?nextToken
              ~channelGroupName ())
           (Some Values.ListChannelsResponse.to_json)
           (Some Values.ListChannelsResponse.error_to_json)])
let list_harvest_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelName =
         flag "channel-name" (optional string)
           ~doc:"STRING ListHarvestJobsRequestChannelNameString"
       and originEndpointName =
         flag "origin-endpoint-name" (optional string)
           ~doc:"STRING ListHarvestJobsRequestOriginEndpointNameString"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON HarvestJobStatus"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListHarvestJobsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_harvest_jobs
           (Values.ListHarvestJobsRequest.make ?channelName
              ?originEndpointName
              ?status:(Option.map ~f:Values.HarvestJobStatus.of_json status)
              ?maxResults ?nextToken ~channelGroupName ())
           (Some Values.ListHarvestJobsResponse.to_json)
           (Some Values.ListHarvestJobsResponse.error_to_json)])
let list_origin_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListResourceMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_origin_endpoints
           (Values.ListOriginEndpointsRequest.make ?maxResults ?nextToken
              ~channelGroupName ~channelName ())
           (Some Values.ListOriginEndpointsResponse.to_json)
           (Some Values.ListOriginEndpointsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_channel_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and policy = flag "policy" (required string) ~doc:"STRING PolicyText" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_channel_policy
           (Values.PutChannelPolicyRequest.make ~channelGroupName
              ~channelName ~policy ())
           (Some Values.PutChannelPolicyResponse.to_json)
           (Some Values.PutChannelPolicyResponse.error_to_json)])
let put_origin_endpoint_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cdnAuthConfiguration =
         flag "cdn-auth-configuration" (optional json_arg)
           ~doc:"JSON CdnAuthConfiguration"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName"
       and policy = flag "policy" (required string) ~doc:"STRING PolicyText" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_origin_endpoint_policy
           (Values.PutOriginEndpointPolicyRequest.make
              ?cdnAuthConfiguration:(Option.map
                                       ~f:Values.CdnAuthConfiguration.of_json
                                       cdnAuthConfiguration)
              ~channelGroupName ~channelName ~originEndpointName ~policy ())
           (Some Values.PutOriginEndpointPolicyResponse.to_json)
           (Some Values.PutOriginEndpointPolicyResponse.error_to_json)])
let reset_channel_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_channel_state
           (Values.ResetChannelStateRequest.make ~channelGroupName
              ~channelName ())
           (Some Values.ResetChannelStateResponse.to_json)
           (Some Values.ResetChannelStateResponse.error_to_json)])
let reset_origin_endpoint_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_origin_endpoint_state
           (Values.ResetOriginEndpointStateRequest.make ~channelGroupName
              ~channelName ~originEndpointName ())
           (Some Values.ResetOriginEndpointStateResponse.to_json)
           (Some Values.ResetOriginEndpointStateResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eTag = flag "e-tag" (optional string) ~doc:"STRING EntityTag"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and inputSwitchConfiguration =
         flag "input-switch-configuration" (optional json_arg)
           ~doc:"JSON InputSwitchConfiguration"
       and outputHeaderConfiguration =
         flag "output-header-configuration" (optional json_arg)
           ~doc:"JSON OutputHeaderConfiguration"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel
           (Values.UpdateChannelRequest.make ?eTag ?description
              ?inputSwitchConfiguration:(Option.map
                                           ~f:Values.InputSwitchConfiguration.of_json
                                           inputSwitchConfiguration)
              ?outputHeaderConfiguration:(Option.map
                                            ~f:Values.OutputHeaderConfiguration.of_json
                                            outputHeaderConfiguration)
              ~channelGroupName ~channelName ())
           (Some Values.UpdateChannelResponse.to_json)
           (Some Values.UpdateChannelResponse.error_to_json)])
let update_channel_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eTag = flag "e-tag" (optional string) ~doc:"STRING EntityTag"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel_group
           (Values.UpdateChannelGroupRequest.make ?eTag ?description
              ~channelGroupName ())
           (Some Values.UpdateChannelGroupResponse.to_json)
           (Some Values.UpdateChannelGroupResponse.error_to_json)])
let update_origin_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and segment = flag "segment" (optional json_arg) ~doc:"JSON Segment"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and startoverWindowSeconds =
         flag "startover-window-seconds" (optional int)
           ~doc:"INT UpdateOriginEndpointRequestStartoverWindowSecondsInteger"
       and hlsManifests =
         flag "hls-manifests" (optional json_arg)
           ~doc:"JSON CreateHlsManifests"
       and lowLatencyHlsManifests =
         flag "low-latency-hls-manifests" (optional json_arg)
           ~doc:"JSON CreateLowLatencyHlsManifests"
       and dashManifests =
         flag "dash-manifests" (optional json_arg)
           ~doc:"JSON CreateDashManifests"
       and mssManifests =
         flag "mss-manifests" (optional json_arg)
           ~doc:"JSON CreateMssManifests"
       and forceEndpointErrorConfiguration =
         flag "force-endpoint-error-configuration" (optional json_arg)
           ~doc:"JSON ForceEndpointErrorConfiguration"
       and uriSeparator =
         flag "uri-separator" (optional json_arg) ~doc:"JSON UriSeparator"
       and eTag = flag "e-tag" (optional string) ~doc:"STRING EntityTag"
       and channelGroupName =
         flag "channel-group-name" (required string)
           ~doc:"STRING ResourceName"
       and channelName =
         flag "channel-name" (required string) ~doc:"STRING ResourceName"
       and originEndpointName =
         flag "origin-endpoint-name" (required string)
           ~doc:"STRING ResourceName"
       and containerType =
         flag "container-type" (required json_arg) ~doc:"JSON ContainerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_origin_endpoint
           (Values.UpdateOriginEndpointRequest.make
              ?segment:(Option.map ~f:Values.Segment.of_json segment)
              ?description ?startoverWindowSeconds
              ?hlsManifests:(Option.map ~f:Values.CreateHlsManifests.of_json
                               hlsManifests)
              ?lowLatencyHlsManifests:(Option.map
                                         ~f:Values.CreateLowLatencyHlsManifests.of_json
                                         lowLatencyHlsManifests)
              ?dashManifests:(Option.map
                                ~f:Values.CreateDashManifests.of_json
                                dashManifests)
              ?mssManifests:(Option.map ~f:Values.CreateMssManifests.of_json
                               mssManifests)
              ?forceEndpointErrorConfiguration:(Option.map
                                                  ~f:Values.ForceEndpointErrorConfiguration.of_json
                                                  forceEndpointErrorConfiguration)
              ?uriSeparator:(Option.map ~f:Values.UriSeparator.of_json
                               uriSeparator) ?eTag ~channelGroupName
              ~channelName ~originEndpointName
              ~containerType:(Values.ContainerType.of_json containerType) ())
           (Some Values.UpdateOriginEndpointResponse.to_json)
           (Some Values.UpdateOriginEndpointResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-harvest-job", cancel_harvest_job);
    ("create-channel", create_channel);
    ("create-channel-group", create_channel_group);
    ("create-harvest-job", create_harvest_job);
    ("create-origin-endpoint", create_origin_endpoint);
    ("delete-channel", delete_channel);
    ("delete-channel-group", delete_channel_group);
    ("delete-channel-policy", delete_channel_policy);
    ("delete-origin-endpoint", delete_origin_endpoint);
    ("delete-origin-endpoint-policy", delete_origin_endpoint_policy);
    ("get-channel", get_channel);
    ("get-channel-group", get_channel_group);
    ("get-channel-policy", get_channel_policy);
    ("get-harvest-job", get_harvest_job);
    ("get-origin-endpoint", get_origin_endpoint);
    ("get-origin-endpoint-policy", get_origin_endpoint_policy);
    ("list-channel-groups", list_channel_groups);
    ("list-channels", list_channels);
    ("list-harvest-jobs", list_harvest_jobs);
    ("list-origin-endpoints", list_origin_endpoints);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-channel-policy", put_channel_policy);
    ("put-origin-endpoint-policy", put_origin_endpoint_policy);
    ("reset-channel-state", reset_channel_state);
    ("reset-origin-endpoint-state", reset_origin_endpoint_state);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-channel", update_channel);
    ("update-channel-group", update_channel_group);
    ("update-origin-endpoint", update_origin_endpoint)]
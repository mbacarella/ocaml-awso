(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.mediapackagev2
let apiVersion = "2022-12-25"
let endpointPrefix = "mediapackagev2"
let serviceFullName = "AWS Elemental MediaPackage v2"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "mediapackagev2"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module DashDvbMetricsReportingProbabilityInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for DashDvbMetricsReportingProbabilityInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DashDvbMetricsReportingReportingUrlString =
  struct
    type nonrec t = string
    let context_ = "DashDvbMetricsReportingReportingUrlString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"DashDvbMetricsReportingReportingUrlString" j
    let to_json = simple_to_json to_value
  end
module ResourceName =
  struct
    type nonrec t = string
    let context_ = "ResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end
module DashBaseUrlDvbPriorityInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:15000) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for DashBaseUrlDvbPriorityInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DashBaseUrlDvbWeightInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:15000) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for DashBaseUrlDvbWeightInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DashBaseUrlServiceLocationString =
  struct
    type nonrec t = string
    let context_ = "DashBaseUrlServiceLocationString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DashBaseUrlServiceLocationString" j
    let to_json = simple_to_json to_value
  end
module DashBaseUrlUrlString =
  struct
    type nonrec t = string
    let context_ = "DashBaseUrlUrlString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DashBaseUrlUrlString" j
    let to_json = simple_to_json to_value
  end
module DashDvbMetricsReporting =
  struct
    type nonrec t =
      {
      reportingUrl: DashDvbMetricsReportingReportingUrlString.t
        [@ocaml.doc "The URL where playback devices send error reports."];
      probability: DashDvbMetricsReportingProbabilityInteger.t option
        [@ocaml.doc
          "The number of playback devices per 1000 that will send error reports to the reporting URL. This represents the probability that a playback device will be a reporting player for this session."]}
    let context_ = "DashDvbMetricsReporting"
    let make ?probability =
      fun ~reportingUrl -> fun () -> { probability; reportingUrl }
    let to_value x =
      structure_to_value
        [("ReportingUrl",
           (Some
              (DashDvbMetricsReportingReportingUrlString.to_value
                 x.reportingUrl)));
        ("Probability",
          (Option.map x.probability
             ~f:DashDvbMetricsReportingProbabilityInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let probability =
        (Option.map ~f:DashDvbMetricsReportingProbabilityInteger.of_xml)
          (Xml.child xml_arg0 "Probability") in
      let reportingUrl =
        DashDvbMetricsReportingReportingUrlString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReportingUrl") in
      make ?probability ~reportingUrl ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let probability =
        field_map json__ "Probability"
          DashDvbMetricsReportingProbabilityInteger.of_json in
      let reportingUrl =
        field_map_exn json__ "ReportingUrl"
          DashDvbMetricsReportingReportingUrlString.of_json in
      make ?probability ~reportingUrl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For use with DVB-DASH profiles only. The settings for error reporting from the playback device that you want Elemental MediaPackage to pass through to the manifest."]
module DashDvbFontDownloadFontFamilyString =
  struct
    type nonrec t = string
    let context_ = "DashDvbFontDownloadFontFamilyString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"DashDvbFontDownloadFontFamilyString" j
    let to_json = simple_to_json to_value
  end
module DashDvbFontDownloadMimeTypeString =
  struct
    type nonrec t = string
    let context_ = "DashDvbFontDownloadMimeTypeString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9][a-zA-Z0-9_/-]*[a-zA-Z0-9]")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"DashDvbFontDownloadMimeTypeString" j
    let to_json = simple_to_json to_value
  end
module DashDvbFontDownloadUrlString =
  struct
    type nonrec t = string
    let context_ = "DashDvbFontDownloadUrlString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DashDvbFontDownloadUrlString" j
    let to_json = simple_to_json to_value
  end
module DashTtmlProfile =
  struct
    type nonrec t =
      | IMSC_1 
      | EBU_TT_D_101 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IMSC_1 -> "IMSC_1"
      | EBU_TT_D_101 -> "EBU_TT_D_101"
      | Non_static_id s -> s
    let of_string =
      function
      | "IMSC_1" -> IMSC_1
      | "EBU_TT_D_101" -> EBU_TT_D_101
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DashTtmlProfile" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DashTtmlProfile" j)
    let to_json = simple_to_json to_value
  end
module PresetSpeke20Audio =
  struct
    type nonrec t =
      | PRESET_AUDIO_1 
      | PRESET_AUDIO_2 
      | PRESET_AUDIO_3 
      | SHARED 
      | UNENCRYPTED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PRESET_AUDIO_1 -> "PRESET_AUDIO_1"
      | PRESET_AUDIO_2 -> "PRESET_AUDIO_2"
      | PRESET_AUDIO_3 -> "PRESET_AUDIO_3"
      | SHARED -> "SHARED"
      | UNENCRYPTED -> "UNENCRYPTED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PRESET_AUDIO_1" -> PRESET_AUDIO_1
      | "PRESET_AUDIO_2" -> PRESET_AUDIO_2
      | "PRESET_AUDIO_3" -> PRESET_AUDIO_3
      | "SHARED" -> SHARED
      | "UNENCRYPTED" -> UNENCRYPTED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PresetSpeke20Audio" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PresetSpeke20Audio" j)
    let to_json = simple_to_json to_value
  end
module PresetSpeke20Video =
  struct
    type nonrec t =
      | PRESET_VIDEO_1 
      | PRESET_VIDEO_2 
      | PRESET_VIDEO_3 
      | PRESET_VIDEO_4 
      | PRESET_VIDEO_5 
      | PRESET_VIDEO_6 
      | PRESET_VIDEO_7 
      | PRESET_VIDEO_8 
      | SHARED 
      | UNENCRYPTED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PRESET_VIDEO_1 -> "PRESET_VIDEO_1"
      | PRESET_VIDEO_2 -> "PRESET_VIDEO_2"
      | PRESET_VIDEO_3 -> "PRESET_VIDEO_3"
      | PRESET_VIDEO_4 -> "PRESET_VIDEO_4"
      | PRESET_VIDEO_5 -> "PRESET_VIDEO_5"
      | PRESET_VIDEO_6 -> "PRESET_VIDEO_6"
      | PRESET_VIDEO_7 -> "PRESET_VIDEO_7"
      | PRESET_VIDEO_8 -> "PRESET_VIDEO_8"
      | SHARED -> "SHARED"
      | UNENCRYPTED -> "UNENCRYPTED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PRESET_VIDEO_1" -> PRESET_VIDEO_1
      | "PRESET_VIDEO_2" -> PRESET_VIDEO_2
      | "PRESET_VIDEO_3" -> PRESET_VIDEO_3
      | "PRESET_VIDEO_4" -> PRESET_VIDEO_4
      | "PRESET_VIDEO_5" -> PRESET_VIDEO_5
      | "PRESET_VIDEO_6" -> PRESET_VIDEO_6
      | "PRESET_VIDEO_7" -> PRESET_VIDEO_7
      | "PRESET_VIDEO_8" -> PRESET_VIDEO_8
      | "SHARED" -> SHARED
      | "UNENCRYPTED" -> UNENCRYPTED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PresetSpeke20Video" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PresetSpeke20Video" j)
    let to_json = simple_to_json to_value
  end
module DrmSystem =
  struct
    type nonrec t =
      | CLEAR_KEY_AES_128 
      | FAIRPLAY 
      | PLAYREADY 
      | WIDEVINE 
      | IRDETO 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CLEAR_KEY_AES_128 -> "CLEAR_KEY_AES_128"
      | FAIRPLAY -> "FAIRPLAY"
      | PLAYREADY -> "PLAYREADY"
      | WIDEVINE -> "WIDEVINE"
      | IRDETO -> "IRDETO"
      | Non_static_id s -> s
    let of_string =
      function
      | "CLEAR_KEY_AES_128" -> CLEAR_KEY_AES_128
      | "FAIRPLAY" -> FAIRPLAY
      | "PLAYREADY" -> PLAYREADY
      | "WIDEVINE" -> WIDEVINE
      | "IRDETO" -> IRDETO
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DrmSystem" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DrmSystem" j)
    let to_json = simple_to_json to_value
  end
module EndpointErrorCondition =
  struct
    type nonrec t =
      | STALE_MANIFEST 
      | INCOMPLETE_MANIFEST 
      | MISSING_DRM_KEY 
      | SLATE_INPUT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STALE_MANIFEST -> "STALE_MANIFEST"
      | INCOMPLETE_MANIFEST -> "INCOMPLETE_MANIFEST"
      | MISSING_DRM_KEY -> "MISSING_DRM_KEY"
      | SLATE_INPUT -> "SLATE_INPUT"
      | Non_static_id s -> s
    let of_string =
      function
      | "STALE_MANIFEST" -> STALE_MANIFEST
      | "INCOMPLETE_MANIFEST" -> INCOMPLETE_MANIFEST
      | "MISSING_DRM_KEY" -> MISSING_DRM_KEY
      | "SLATE_INPUT" -> SLATE_INPUT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EndpointErrorCondition" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"EndpointErrorCondition" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module S3BucketName =
  struct
    type nonrec t = string
    let context_ = "S3BucketName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:63) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3BucketName" j
    let to_json = simple_to_json to_value
  end
module S3DestinationPath =
  struct
    type nonrec t = string
    let context_ = "S3DestinationPath"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3DestinationPath" j
    let to_json = simple_to_json to_value
  end
module HarvestedDashManifest =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t
        [@ocaml.doc "The name of the harvested DASH manifest."]}
    let context_ = "HarvestedDashManifest"
    let make ~manifestName = fun () -> { manifestName }
    let to_value x =
      structure_to_value
        [("ManifestName", (Some (ResourceName.to_value x.manifestName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let manifestName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ManifestName") in
      make ~manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let manifestName =
        field_map_exn json__ "ManifestName" ResourceName.of_json in
      make ~manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a harvested DASH manifest."]
module HarvestedHlsManifest =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t
        [@ocaml.doc "The name of the harvested HLS manifest."]}
    let context_ = "HarvestedHlsManifest"
    let make ~manifestName = fun () -> { manifestName }
    let to_value x =
      structure_to_value
        [("ManifestName", (Some (ResourceName.to_value x.manifestName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let manifestName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ManifestName") in
      make ~manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let manifestName =
        field_map_exn json__ "ManifestName" ResourceName.of_json in
      make ~manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a harvested HLS manifest."]
module HarvestedLowLatencyHlsManifest =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t
        [@ocaml.doc "The name of the harvested Low-Latency HLS manifest."]}
    let context_ = "HarvestedLowLatencyHlsManifest"
    let make ~manifestName = fun () -> { manifestName }
    let to_value x =
      structure_to_value
        [("ManifestName", (Some (ResourceName.to_value x.manifestName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let manifestName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ManifestName") in
      make ~manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let manifestName =
        field_map_exn json__ "ManifestName" ResourceName.of_json in
      make ~manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a harvested Low-Latency HLS manifest."]
module SyntheticTimestamp_date_time =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module DashBaseUrl =
  struct
    type nonrec t =
      {
      url: DashBaseUrlUrlString.t
        [@ocaml.doc "A source location for segments."];
      serviceLocation: DashBaseUrlServiceLocationString.t option
        [@ocaml.doc "The name of the source location."];
      dvbPriority: DashBaseUrlDvbPriorityInteger.t option
        [@ocaml.doc
          "For use with DVB-DASH profiles only. The priority of this location for servings segments. The lower the number, the higher the priority."];
      dvbWeight: DashBaseUrlDvbWeightInteger.t option
        [@ocaml.doc
          "For use with DVB-DASH profiles only. The weighting for source locations that have the same priority."]}
    let context_ = "DashBaseUrl"
    let make ?serviceLocation =
      fun ?dvbPriority ->
        fun ?dvbWeight ->
          fun ~url ->
            fun () -> { serviceLocation; dvbPriority; dvbWeight; url }
    let to_value x =
      structure_to_value
        [("Url", (Some (DashBaseUrlUrlString.to_value x.url)));
        ("ServiceLocation",
          (Option.map x.serviceLocation
             ~f:DashBaseUrlServiceLocationString.to_value));
        ("DvbPriority",
          (Option.map x.dvbPriority ~f:DashBaseUrlDvbPriorityInteger.to_value));
        ("DvbWeight",
          (Option.map x.dvbWeight ~f:DashBaseUrlDvbWeightInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dvbWeight =
        (Option.map ~f:DashBaseUrlDvbWeightInteger.of_xml)
          (Xml.child xml_arg0 "DvbWeight") in
      let dvbPriority =
        (Option.map ~f:DashBaseUrlDvbPriorityInteger.of_xml)
          (Xml.child xml_arg0 "DvbPriority") in
      let serviceLocation =
        (Option.map ~f:DashBaseUrlServiceLocationString.of_xml)
          (Xml.child xml_arg0 "ServiceLocation") in
      let url =
        DashBaseUrlUrlString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Url") in
      make ?dvbWeight ?dvbPriority ?serviceLocation ~url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dvbWeight =
        field_map json__ "DvbWeight" DashBaseUrlDvbWeightInteger.of_json in
      let dvbPriority =
        field_map json__ "DvbPriority" DashBaseUrlDvbPriorityInteger.of_json in
      let serviceLocation =
        field_map json__ "ServiceLocation"
          DashBaseUrlServiceLocationString.of_json in
      let url = field_map_exn json__ "Url" DashBaseUrlUrlString.of_json in
      make ?dvbWeight ?dvbPriority ?serviceLocation ~url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The base URLs to use for retrieving segments. You can specify multiple locations and indicate the priority and weight for when each should be used, for use in mutli-CDN workflows."]
module DashDvbErrorMetrics =
  struct
    type nonrec t = DashDvbMetricsReporting.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DashDvbMetricsReporting.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DashDvbMetricsReporting.of_xml)
    let of_json j =
      list_of_json ~kind:"DashDvbErrorMetrics"
        ~of_json:DashDvbMetricsReporting.of_json j
    let to_json v = composed_to_json to_value v
  end
module DashDvbFontDownload =
  struct
    type nonrec t =
      {
      url: DashDvbFontDownloadUrlString.t option
        [@ocaml.doc "The URL for downloading fonts for subtitles."];
      mimeType: DashDvbFontDownloadMimeTypeString.t option
        [@ocaml.doc
          "The mimeType of the resource that's at the font download URL. For information about font MIME types, see the MPEG-DASH Profile for Transport of ISO BMFF Based DVB Services over IP Based Networks document."];
      fontFamily: DashDvbFontDownloadFontFamilyString.t option
        [@ocaml.doc
          "The fontFamily name for subtitles, as described in EBU-TT-D Subtitling Distribution Format."]}
    let make ?url =
      fun ?mimeType ->
        fun ?fontFamily -> fun () -> { url; mimeType; fontFamily }
    let to_value x =
      structure_to_value
        [("Url", (Option.map x.url ~f:DashDvbFontDownloadUrlString.to_value));
        ("MimeType",
          (Option.map x.mimeType
             ~f:DashDvbFontDownloadMimeTypeString.to_value));
        ("FontFamily",
          (Option.map x.fontFamily
             ~f:DashDvbFontDownloadFontFamilyString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fontFamily =
        (Option.map ~f:DashDvbFontDownloadFontFamilyString.of_xml)
          (Xml.child xml_arg0 "FontFamily") in
      let mimeType =
        (Option.map ~f:DashDvbFontDownloadMimeTypeString.of_xml)
          (Xml.child xml_arg0 "MimeType") in
      let url =
        (Option.map ~f:DashDvbFontDownloadUrlString.of_xml)
          (Xml.child xml_arg0 "Url") in
      make ?fontFamily ?mimeType ?url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fontFamily =
        field_map json__ "FontFamily"
          DashDvbFontDownloadFontFamilyString.of_json in
      let mimeType =
        field_map json__ "MimeType" DashDvbFontDownloadMimeTypeString.of_json in
      let url = field_map json__ "Url" DashDvbFontDownloadUrlString.of_json in
      make ?fontFamily ?mimeType ?url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For use with DVB-DASH profiles only. The settings for font downloads that you want Elemental MediaPackage to pass through to the manifest."]
module DashPeriodTrigger =
  struct
    type nonrec t =
      | AVAILS 
      | DRM_KEY_ROTATION 
      | SOURCE_CHANGES 
      | SOURCE_DISRUPTIONS 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AVAILS -> "AVAILS"
      | DRM_KEY_ROTATION -> "DRM_KEY_ROTATION"
      | SOURCE_CHANGES -> "SOURCE_CHANGES"
      | SOURCE_DISRUPTIONS -> "SOURCE_DISRUPTIONS"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "AVAILS" -> AVAILS
      | "DRM_KEY_ROTATION" -> DRM_KEY_ROTATION
      | "SOURCE_CHANGES" -> SOURCE_CHANGES
      | "SOURCE_DISRUPTIONS" -> SOURCE_DISRUPTIONS
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DashPeriodTrigger" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DashPeriodTrigger" j)
    let to_json = simple_to_json to_value
  end
module DashProfile =
  struct
    type nonrec t =
      | DVB_DASH 
      | Non_static_id of string 
    let make i = i
    let to_string = function | DVB_DASH -> "DVB_DASH" | Non_static_id s -> s
    let of_string = function | "DVB_DASH" -> DVB_DASH | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DashProfile" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DashProfile" j)
    let to_json = simple_to_json to_value
  end
module DashProgramInformationCopyrightString =
  struct
    type nonrec t = string
    let context_ = "DashProgramInformationCopyrightString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"DashProgramInformationCopyrightString" j
    let to_json = simple_to_json to_value
  end
module DashProgramInformationLanguageCodeString =
  struct
    type nonrec t = string
    let context_ = "DashProgramInformationLanguageCodeString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:5) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9][a-zA-Z0-9_-]*[a-zA-Z0-9]")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"DashProgramInformationLanguageCodeString" j
    let to_json = simple_to_json to_value
  end
module DashProgramInformationMoreInformationUrlString =
  struct
    type nonrec t = string
    let context_ = "DashProgramInformationMoreInformationUrlString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"DashProgramInformationMoreInformationUrlString" j
    let to_json = simple_to_json to_value
  end
module DashProgramInformationSourceString =
  struct
    type nonrec t = string
    let context_ = "DashProgramInformationSourceString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"DashProgramInformationSourceString" j
    let to_json = simple_to_json to_value
  end
module DashProgramInformationTitleString =
  struct
    type nonrec t = string
    let context_ = "DashProgramInformationTitleString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"DashProgramInformationTitleString" j
    let to_json = simple_to_json to_value
  end
module DashTtmlConfiguration =
  struct
    type nonrec t =
      {
      ttmlProfile: DashTtmlProfile.t
        [@ocaml.doc
          "The profile that MediaPackage uses when signaling subtitles in the manifest. IMSC is the default profile. EBU-TT-D produces subtitles that are compliant with the EBU-TT-D TTML profile. MediaPackage passes through subtitle styles to the manifest. For more information about EBU-TT-D subtitles, see EBU-TT-D Subtitling Distribution Format."]}
    let context_ = "DashTtmlConfiguration"
    let make ~ttmlProfile = fun () -> { ttmlProfile }
    let to_value x =
      structure_to_value
        [("TtmlProfile", (Some (DashTtmlProfile.to_value x.ttmlProfile)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ttmlProfile =
        DashTtmlProfile.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TtmlProfile") in
      make ~ttmlProfile ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ttmlProfile =
        field_map_exn json__ "TtmlProfile" DashTtmlProfile.of_json in
      make ~ttmlProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The settings for TTML subtitles."]
module DashUtcTimingMode =
  struct
    type nonrec t =
      | HTTP_HEAD 
      | HTTP_ISO 
      | HTTP_XSDATE 
      | UTC_DIRECT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HTTP_HEAD -> "HTTP_HEAD"
      | HTTP_ISO -> "HTTP_ISO"
      | HTTP_XSDATE -> "HTTP_XSDATE"
      | UTC_DIRECT -> "UTC_DIRECT"
      | Non_static_id s -> s
    let of_string =
      function
      | "HTTP_HEAD" -> HTTP_HEAD
      | "HTTP_ISO" -> HTTP_ISO
      | "HTTP_XSDATE" -> HTTP_XSDATE
      | "UTC_DIRECT" -> UTC_DIRECT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DashUtcTimingMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DashUtcTimingMode" j)
    let to_json = simple_to_json to_value
  end
module DashUtcTimingTimingSourceString =
  struct
    type nonrec t = string
    let context_ = "DashUtcTimingTimingSourceString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DashUtcTimingTimingSourceString" j
    let to_json = simple_to_json to_value
  end
module FilterConfigurationDrmSettingsString =
  struct
    type nonrec t = string
    let context_ = "FilterConfigurationDrmSettingsString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"FilterConfigurationDrmSettingsString" j
    let to_json = simple_to_json to_value
  end
module FilterConfigurationManifestFilterString =
  struct
    type nonrec t = string
    let context_ = "FilterConfigurationManifestFilterString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"FilterConfigurationManifestFilterString" j
    let to_json = simple_to_json to_value
  end
module FilterConfigurationTimeDelaySecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1209600) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for FilterConfigurationTimeDelaySecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module AdMarkerDash =
  struct
    type nonrec t =
      | BINARY 
      | XML 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | BINARY -> "BINARY" | XML -> "XML" | Non_static_id s -> s
    let of_string =
      function | "BINARY" -> BINARY | "XML" -> XML | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AdMarkerDash" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AdMarkerDash" j)
    let to_json = simple_to_json to_value
  end
module ScteInManifests =
  struct
    type nonrec t =
      | ALL 
      | MATCHES_FILTER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALL -> "ALL"
      | MATCHES_FILTER -> "MATCHES_FILTER"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALL" -> ALL
      | "MATCHES_FILTER" -> MATCHES_FILTER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ScteInManifests" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ScteInManifests" j)
    let to_json = simple_to_json to_value
  end
module AdMarkerHls =
  struct
    type nonrec t =
      | DATERANGE 
      | SCTE35_ENHANCED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DATERANGE -> "DATERANGE"
      | SCTE35_ENHANCED -> "SCTE35_ENHANCED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DATERANGE" -> DATERANGE
      | "SCTE35_ENHANCED" -> SCTE35_ENHANCED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AdMarkerHls" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AdMarkerHls" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Float_ =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Float x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a float" xml_arg0)
    let of_json j = float_of_json ~kind:"a float" j
    let to_json = simple_to_json to_value
  end
module CmafEncryptionMethod =
  struct
    type nonrec t =
      | CENC 
      | CBCS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | CENC -> "CENC" | CBCS -> "CBCS" | Non_static_id s -> s
    let of_string =
      function | "CENC" -> CENC | "CBCS" -> CBCS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CmafEncryptionMethod" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CmafEncryptionMethod" j)
    let to_json = simple_to_json to_value
  end
module IsmEncryptionMethod =
  struct
    type nonrec t =
      | CENC 
      | Non_static_id of string 
    let make i = i
    let to_string = function | CENC -> "CENC" | Non_static_id s -> s
    let of_string = function | "CENC" -> CENC | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration IsmEncryptionMethod" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IsmEncryptionMethod" j)
    let to_json = simple_to_json to_value
  end
module TsEncryptionMethod =
  struct
    type nonrec t =
      | AES_128 
      | SAMPLE_AES 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AES_128 -> "AES_128"
      | SAMPLE_AES -> "SAMPLE_AES"
      | Non_static_id s -> s
    let of_string =
      function
      | "AES_128" -> AES_128
      | "SAMPLE_AES" -> SAMPLE_AES
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TsEncryptionMethod" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TsEncryptionMethod" j)
    let to_json = simple_to_json to_value
  end
module EncryptionContractConfiguration =
  struct
    type nonrec t =
      {
      presetSpeke20Audio: PresetSpeke20Audio.t
        [@ocaml.doc
          "A collection of audio encryption presets. Value description: PRESET-AUDIO-1 - Use one content key to encrypt all of the audio tracks in your stream. PRESET-AUDIO-2 - Use one content key to encrypt all of the stereo audio tracks and one content key to encrypt all of the multichannel audio tracks. PRESET-AUDIO-3 - Use one content key to encrypt all of the stereo audio tracks, one content key to encrypt all of the multichannel audio tracks with 3 to 6 channels, and one content key to encrypt all of the multichannel audio tracks with more than 6 channels. SHARED - Use the same content key for all of the audio and video tracks in your stream. UNENCRYPTED - Don't encrypt any of the audio tracks in your stream."];
      presetSpeke20Video: PresetSpeke20Video.t
        [@ocaml.doc
          "A collection of video encryption presets. Value description: PRESET-VIDEO-1 - Use one content key to encrypt all of the video tracks in your stream. PRESET-VIDEO-2 - Use one content key to encrypt all of the SD video tracks and one content key for all HD and higher resolutions video tracks. PRESET-VIDEO-3 - Use one content key to encrypt all of the SD video tracks, one content key for HD video tracks and one content key for all UHD video tracks. PRESET-VIDEO-4 - Use one content key to encrypt all of the SD video tracks, one content key for HD video tracks, one content key for all UHD1 video tracks and one content key for all UHD2 video tracks. PRESET-VIDEO-5 - Use one content key to encrypt all of the SD video tracks, one content key for HD1 video tracks, one content key for HD2 video tracks, one content key for all UHD1 video tracks and one content key for all UHD2 video tracks. PRESET-VIDEO-6 - Use one content key to encrypt all of the SD video tracks, one content key for HD1 video tracks, one content key for HD2 video tracks and one content key for all UHD video tracks. PRESET-VIDEO-7 - Use one content key to encrypt all of the SD+HD1 video tracks, one content key for HD2 video tracks and one content key for all UHD video tracks. PRESET-VIDEO-8 - Use one content key to encrypt all of the SD+HD1 video tracks, one content key for HD2 video tracks, one content key for all UHD1 video tracks and one content key for all UHD2 video tracks. SHARED - Use the same content key for all of the video and audio tracks in your stream. UNENCRYPTED - Don't encrypt any of the video tracks in your stream."]}
    let context_ = "EncryptionContractConfiguration"
    let make ~presetSpeke20Audio =
      fun ~presetSpeke20Video ->
        fun () -> { presetSpeke20Audio; presetSpeke20Video }
    let to_value x =
      structure_to_value
        [("PresetSpeke20Audio",
           (Some (PresetSpeke20Audio.to_value x.presetSpeke20Audio)));
        ("PresetSpeke20Video",
          (Some (PresetSpeke20Video.to_value x.presetSpeke20Video)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let presetSpeke20Video =
        PresetSpeke20Video.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PresetSpeke20Video") in
      let presetSpeke20Audio =
        PresetSpeke20Audio.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PresetSpeke20Audio") in
      make ~presetSpeke20Video ~presetSpeke20Audio ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let presetSpeke20Video =
        field_map_exn json__ "PresetSpeke20Video" PresetSpeke20Video.of_json in
      let presetSpeke20Audio =
        field_map_exn json__ "PresetSpeke20Audio" PresetSpeke20Audio.of_json in
      make ~presetSpeke20Video ~presetSpeke20Audio ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configure one or more content encryption keys for your endpoints that use SPEKE Version 2.0. The encryption contract defines which content keys are used to encrypt the audio and video tracks in your stream. To configure the encryption contract, specify which audio and video encryption presets to use."]
module SpekeKeyProviderCertificateArnString =
  struct
    type nonrec t = string
    let context_ = "SpekeKeyProviderCertificateArnString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:([^:\\n]+):acm:([^:\\n]+):([0-9]+):certificate/[a-zA-Z0-9-_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"SpekeKeyProviderCertificateArnString" j
    let to_json = simple_to_json to_value
  end
module SpekeKeyProviderDrmSystemsList =
  struct
    type nonrec t = DrmSystem.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:4) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DrmSystem.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DrmSystem.of_xml)
    let of_json j =
      list_of_json ~kind:"SpekeKeyProviderDrmSystemsList"
        ~of_json:DrmSystem.of_json j
    let to_json v = composed_to_json to_value v
  end
module SpekeKeyProviderResourceIdString =
  struct
    type nonrec t = string
    let context_ = "SpekeKeyProviderResourceIdString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9a-zA-Z_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SpekeKeyProviderResourceIdString" j
    let to_json = simple_to_json to_value
  end
module SpekeKeyProviderRoleArnString =
  struct
    type nonrec t = string
    let context_ = "SpekeKeyProviderRoleArnString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SpekeKeyProviderRoleArnString" j
    let to_json = simple_to_json to_value
  end
module SpekeKeyProviderUrlString =
  struct
    type nonrec t = string
    let context_ = "SpekeKeyProviderUrlString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SpekeKeyProviderUrlString" j
    let to_json = simple_to_json to_value
  end
module CustomAdType =
  struct
    type nonrec t =
      | PROGRAM 
      | CHAPTER 
      | UNSCHEDULED_EVENT 
      | ALTERNATE_CONTENT_OPPORTUNITY 
      | NETWORK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PROGRAM -> "PROGRAM"
      | CHAPTER -> "CHAPTER"
      | UNSCHEDULED_EVENT -> "UNSCHEDULED_EVENT"
      | ALTERNATE_CONTENT_OPPORTUNITY -> "ALTERNATE_CONTENT_OPPORTUNITY"
      | NETWORK -> "NETWORK"
      | Non_static_id s -> s
    let of_string =
      function
      | "PROGRAM" -> PROGRAM
      | "CHAPTER" -> CHAPTER
      | "UNSCHEDULED_EVENT" -> UNSCHEDULED_EVENT
      | "ALTERNATE_CONTENT_OPPORTUNITY" -> ALTERNATE_CONTENT_OPPORTUNITY
      | "NETWORK" -> NETWORK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CustomAdType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CustomAdType" j)
    let to_json = simple_to_json to_value
  end
module ScteFilter =
  struct
    type nonrec t =
      | SPLICE_INSERT 
      | BREAK 
      | PROVIDER_ADVERTISEMENT 
      | DISTRIBUTOR_ADVERTISEMENT 
      | PROVIDER_PLACEMENT_OPPORTUNITY 
      | DISTRIBUTOR_PLACEMENT_OPPORTUNITY 
      | PROVIDER_OVERLAY_PLACEMENT_OPPORTUNITY 
      | DISTRIBUTOR_OVERLAY_PLACEMENT_OPPORTUNITY 
      | PROGRAM 
      | CHAPTER 
      | UNSCHEDULED_EVENT 
      | ALTERNATE_CONTENT_OPPORTUNITY 
      | NETWORK 
      | PROVIDER_PROMO 
      | DISTRIBUTOR_PROMO 
      | PROVIDER_AD_BLOCK 
      | DISTRIBUTOR_AD_BLOCK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SPLICE_INSERT -> "SPLICE_INSERT"
      | BREAK -> "BREAK"
      | PROVIDER_ADVERTISEMENT -> "PROVIDER_ADVERTISEMENT"
      | DISTRIBUTOR_ADVERTISEMENT -> "DISTRIBUTOR_ADVERTISEMENT"
      | PROVIDER_PLACEMENT_OPPORTUNITY -> "PROVIDER_PLACEMENT_OPPORTUNITY"
      | DISTRIBUTOR_PLACEMENT_OPPORTUNITY ->
          "DISTRIBUTOR_PLACEMENT_OPPORTUNITY"
      | PROVIDER_OVERLAY_PLACEMENT_OPPORTUNITY ->
          "PROVIDER_OVERLAY_PLACEMENT_OPPORTUNITY"
      | DISTRIBUTOR_OVERLAY_PLACEMENT_OPPORTUNITY ->
          "DISTRIBUTOR_OVERLAY_PLACEMENT_OPPORTUNITY"
      | PROGRAM -> "PROGRAM"
      | CHAPTER -> "CHAPTER"
      | UNSCHEDULED_EVENT -> "UNSCHEDULED_EVENT"
      | ALTERNATE_CONTENT_OPPORTUNITY -> "ALTERNATE_CONTENT_OPPORTUNITY"
      | NETWORK -> "NETWORK"
      | PROVIDER_PROMO -> "PROVIDER_PROMO"
      | DISTRIBUTOR_PROMO -> "DISTRIBUTOR_PROMO"
      | PROVIDER_AD_BLOCK -> "PROVIDER_AD_BLOCK"
      | DISTRIBUTOR_AD_BLOCK -> "DISTRIBUTOR_AD_BLOCK"
      | Non_static_id s -> s
    let of_string =
      function
      | "SPLICE_INSERT" -> SPLICE_INSERT
      | "BREAK" -> BREAK
      | "PROVIDER_ADVERTISEMENT" -> PROVIDER_ADVERTISEMENT
      | "DISTRIBUTOR_ADVERTISEMENT" -> DISTRIBUTOR_ADVERTISEMENT
      | "PROVIDER_PLACEMENT_OPPORTUNITY" -> PROVIDER_PLACEMENT_OPPORTUNITY
      | "DISTRIBUTOR_PLACEMENT_OPPORTUNITY" ->
          DISTRIBUTOR_PLACEMENT_OPPORTUNITY
      | "PROVIDER_OVERLAY_PLACEMENT_OPPORTUNITY" ->
          PROVIDER_OVERLAY_PLACEMENT_OPPORTUNITY
      | "DISTRIBUTOR_OVERLAY_PLACEMENT_OPPORTUNITY" ->
          DISTRIBUTOR_OVERLAY_PLACEMENT_OPPORTUNITY
      | "PROGRAM" -> PROGRAM
      | "CHAPTER" -> CHAPTER
      | "UNSCHEDULED_EVENT" -> UNSCHEDULED_EVENT
      | "ALTERNATE_CONTENT_OPPORTUNITY" -> ALTERNATE_CONTENT_OPPORTUNITY
      | "NETWORK" -> NETWORK
      | "PROVIDER_PROMO" -> PROVIDER_PROMO
      | "DISTRIBUTOR_PROMO" -> DISTRIBUTOR_PROMO
      | "PROVIDER_AD_BLOCK" -> PROVIDER_AD_BLOCK
      | "DISTRIBUTOR_AD_BLOCK" -> DISTRIBUTOR_AD_BLOCK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ScteFilter" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ScteFilter" j)
    let to_json = simple_to_json to_value
  end
module EndpointErrorConditions =
  struct
    type nonrec t = EndpointErrorCondition.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EndpointErrorCondition.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EndpointErrorCondition.of_xml)
    let of_json j =
      list_of_json ~kind:"EndpointErrorConditions"
        ~of_json:EndpointErrorCondition.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListDashManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t option
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index."];
      url: String_.t option
        [@ocaml.doc
          "The egress domain URL for stream delivery from MediaPackage."]}
    let make ?manifestName = fun ?url -> fun () -> { manifestName; url }
    let to_value x =
      structure_to_value
        [("ManifestName",
           (Option.map x.manifestName ~f:ResourceName.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let manifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ManifestName") in
      make ?url ?manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "Url" String_.of_json in
      let manifestName = field_map json__ "ManifestName" ResourceName.of_json in
      make ?url ?manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the DASH manifest configuration."]
module ListHlsManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t option
        [@ocaml.doc
          "A short short string that's appended to the endpoint URL. The manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index. MediaPackage automatically inserts the format extension, such as .m3u8. You can't use the same manifest name if you use HLS manifest and low-latency HLS manifest. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      childManifestName: ResourceName.t option
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The child manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default child manifest name, index_1. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      url: String_.t option
        [@ocaml.doc
          "The egress domain URL for stream delivery from MediaPackage."]}
    let make ?manifestName =
      fun ?childManifestName ->
        fun ?url -> fun () -> { manifestName; childManifestName; url }
    let to_value x =
      structure_to_value
        [("ManifestName",
           (Option.map x.manifestName ~f:ResourceName.to_value));
        ("ChildManifestName",
          (Option.map x.childManifestName ~f:ResourceName.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let childManifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChildManifestName") in
      let manifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ManifestName") in
      make ?url ?childManifestName ?manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "Url" String_.of_json in
      let childManifestName =
        field_map json__ "ChildManifestName" ResourceName.of_json in
      let manifestName = field_map json__ "ManifestName" ResourceName.of_json in
      make ?url ?childManifestName ?manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "List the HTTP live streaming (HLS) manifest configuration."]
module ListLowLatencyHlsManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t option
        [@ocaml.doc
          "A short short string that's appended to the endpoint URL. The manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index. MediaPackage automatically inserts the format extension, such as .m3u8. You can't use the same manifest name if you use HLS manifest and low-latency HLS manifest. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      childManifestName: ResourceName.t option
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The child manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default child manifest name, index_1. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      url: String_.t option
        [@ocaml.doc
          "The egress domain URL for stream delivery from MediaPackage."]}
    let make ?manifestName =
      fun ?childManifestName ->
        fun ?url -> fun () -> { manifestName; childManifestName; url }
    let to_value x =
      structure_to_value
        [("ManifestName",
           (Option.map x.manifestName ~f:ResourceName.to_value));
        ("ChildManifestName",
          (Option.map x.childManifestName ~f:ResourceName.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let childManifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChildManifestName") in
      let manifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ManifestName") in
      make ?url ?childManifestName ?manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "Url" String_.of_json in
      let childManifestName =
        field_map json__ "ChildManifestName" ResourceName.of_json in
      let manifestName = field_map json__ "ManifestName" ResourceName.of_json in
      make ?url ?childManifestName ?manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "List the low-latency HTTP live streaming (HLS) manifest configuration."]
module ListMssManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t option
        [@ocaml.doc "The name of the MSS manifest configuration."];
      url: String_.t option
        [@ocaml.doc "The URL for accessing the MSS manifest."]}
    let make ?manifestName = fun ?url -> fun () -> { manifestName; url }
    let to_value x =
      structure_to_value
        [("ManifestName",
           (Option.map x.manifestName ~f:ResourceName.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let manifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ManifestName") in
      make ?url ?manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "Url" String_.of_json in
      let manifestName = field_map json__ "ManifestName" ResourceName.of_json in
      make ?url ?manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Summary information about a Microsoft Smooth Streaming (MSS) manifest configuration. This provides key details about the MSS manifest without including all configuration parameters."]
module S3DestinationConfig =
  struct
    type nonrec t =
      {
      bucketName: S3BucketName.t
        [@ocaml.doc
          "The name of an S3 bucket within which harvested content will be exported."];
      destinationPath: S3DestinationPath.t
        [@ocaml.doc
          "The path within the specified S3 bucket where the harvested content will be placed."]}
    let context_ = "S3DestinationConfig"
    let make ~bucketName =
      fun ~destinationPath -> fun () -> { bucketName; destinationPath }
    let to_value x =
      structure_to_value
        [("BucketName", (Some (S3BucketName.to_value x.bucketName)));
        ("DestinationPath",
          (Some (S3DestinationPath.to_value x.destinationPath)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationPath =
        S3DestinationPath.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DestinationPath") in
      let bucketName =
        S3BucketName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BucketName") in
      make ~destinationPath ~bucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let destinationPath =
        field_map_exn json__ "DestinationPath" S3DestinationPath.of_json in
      let bucketName = field_map_exn json__ "BucketName" S3BucketName.of_json in
      make ~destinationPath ~bucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration parameters for where in an S3 bucket to place the harvested content."]
module HarvestedDashManifestsList =
  struct
    type nonrec t = HarvestedDashManifest.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:HarvestedDashManifest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HarvestedDashManifest.of_xml)
    let of_json j =
      list_of_json ~kind:"HarvestedDashManifestsList"
        ~of_json:HarvestedDashManifest.of_json j
    let to_json v = composed_to_json to_value v
  end
module HarvestedHlsManifestsList =
  struct
    type nonrec t = HarvestedHlsManifest.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:HarvestedHlsManifest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HarvestedHlsManifest.of_xml)
    let of_json j =
      list_of_json ~kind:"HarvestedHlsManifestsList"
        ~of_json:HarvestedHlsManifest.of_json j
    let to_json v = composed_to_json to_value v
  end
module HarvestedLowLatencyHlsManifestsList =
  struct
    type nonrec t = HarvestedLowLatencyHlsManifest.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:HarvestedLowLatencyHlsManifest.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HarvestedLowLatencyHlsManifest.of_xml)
    let of_json j =
      list_of_json ~kind:"HarvestedLowLatencyHlsManifestsList"
        ~of_json:HarvestedLowLatencyHlsManifest.of_json j
    let to_json v = composed_to_json to_value v
  end
module DashAvailabilityStartTimeConfiguration =
  struct
    type nonrec t =
      {
      fixedAvailabilityStartTime: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The fixed availability start time for the DASH manifest, in ISO 8601 date-time format. The value must have hourly granularity, meaning that the minutes, seconds, and fractional seconds must be zero. The value must be on or after 2024-01-01T00:00:00Z and must be at least 14 days before the current time."]}
    let make ?fixedAvailabilityStartTime =
      fun () -> { fixedAvailabilityStartTime }
    let to_value x =
      structure_to_value
        [("FixedAvailabilityStartTime",
           (Option.map x.fixedAvailabilityStartTime
              ~f:SyntheticTimestamp_date_time.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fixedAvailabilityStartTime =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "FixedAvailabilityStartTime") in
      make ?fixedAvailabilityStartTime ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fixedAvailabilityStartTime =
        field_map json__ "FixedAvailabilityStartTime"
          SyntheticTimestamp_date_time.of_json in
      make ?fixedAvailabilityStartTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration for the DASH availabilityStartTime attribute of the Media Presentation Description (MPD). Use this configuration to set a custom availability start time for your DASH manifest."]
module DashBaseUrls =
  struct
    type nonrec t = DashBaseUrl.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DashBaseUrl.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DashBaseUrl.of_xml)
    let of_json j =
      list_of_json ~kind:"DashBaseUrls" ~of_json:DashBaseUrl.of_json j
    let to_json v = composed_to_json to_value v
  end
module DashCompactness =
  struct
    type nonrec t =
      | STANDARD 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STANDARD -> "STANDARD"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "STANDARD" -> STANDARD
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DashCompactness" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DashCompactness" j)
    let to_json = simple_to_json to_value
  end
module DashDrmSignaling =
  struct
    type nonrec t =
      | INDIVIDUAL 
      | REFERENCED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INDIVIDUAL -> "INDIVIDUAL"
      | REFERENCED -> "REFERENCED"
      | Non_static_id s -> s
    let of_string =
      function
      | "INDIVIDUAL" -> INDIVIDUAL
      | "REFERENCED" -> REFERENCED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DashDrmSignaling" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DashDrmSignaling" j)
    let to_json = simple_to_json to_value
  end
module DashDvbSettings =
  struct
    type nonrec t =
      {
      fontDownload: DashDvbFontDownload.t option
        [@ocaml.doc "Subtitle font settings."];
      errorMetrics: DashDvbErrorMetrics.t option
        [@ocaml.doc "Playback device error reporting settings."]}
    let make ?fontDownload =
      fun ?errorMetrics -> fun () -> { fontDownload; errorMetrics }
    let to_value x =
      structure_to_value
        [("FontDownload",
           (Option.map x.fontDownload ~f:DashDvbFontDownload.to_value));
        ("ErrorMetrics",
          (Option.map x.errorMetrics ~f:DashDvbErrorMetrics.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMetrics =
        (Option.map ~f:DashDvbErrorMetrics.of_xml)
          (Xml.child xml_arg0 "ErrorMetrics") in
      let fontDownload =
        (Option.map ~f:DashDvbFontDownload.of_xml)
          (Xml.child xml_arg0 "FontDownload") in
      make ?errorMetrics ?fontDownload ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errorMetrics =
        field_map json__ "ErrorMetrics" DashDvbErrorMetrics.of_json in
      let fontDownload =
        field_map json__ "FontDownload" DashDvbFontDownload.of_json in
      make ?errorMetrics ?fontDownload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For endpoints that use the DVB-DASH profile only. The font download and error reporting information that you want MediaPackage to pass through to the manifest."]
module DashPeriodTriggers =
  struct
    type nonrec t = DashPeriodTrigger.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DashPeriodTrigger.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DashPeriodTrigger.of_xml)
    let of_json j =
      list_of_json ~kind:"DashPeriodTriggers"
        ~of_json:DashPeriodTrigger.of_json j
    let to_json v = composed_to_json to_value v
  end
module DashProfiles =
  struct
    type nonrec t = DashProfile.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DashProfile.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DashProfile.of_xml)
    let of_json j =
      list_of_json ~kind:"DashProfiles" ~of_json:DashProfile.of_json j
    let to_json v = composed_to_json to_value v
  end
module DashProgramInformation =
  struct
    type nonrec t =
      {
      title: DashProgramInformationTitleString.t option
        [@ocaml.doc "The title for the manifest."];
      source: DashProgramInformationSourceString.t option
        [@ocaml.doc "Information about the content provider."];
      copyright: DashProgramInformationCopyrightString.t option
        [@ocaml.doc "A copyright statement about the content."];
      languageCode: DashProgramInformationLanguageCodeString.t option
        [@ocaml.doc "The language code for this manifest."];
      moreInformationUrl:
        DashProgramInformationMoreInformationUrlString.t option
        [@ocaml.doc
          "An absolute URL that contains more information about this content."]}
    let make ?title =
      fun ?source ->
        fun ?copyright ->
          fun ?languageCode ->
            fun ?moreInformationUrl ->
              fun () ->
                { title; source; copyright; languageCode; moreInformationUrl
                }
    let to_value x =
      structure_to_value
        [("Title",
           (Option.map x.title ~f:DashProgramInformationTitleString.to_value));
        ("Source",
          (Option.map x.source ~f:DashProgramInformationSourceString.to_value));
        ("Copyright",
          (Option.map x.copyright
             ~f:DashProgramInformationCopyrightString.to_value));
        ("LanguageCode",
          (Option.map x.languageCode
             ~f:DashProgramInformationLanguageCodeString.to_value));
        ("MoreInformationUrl",
          (Option.map x.moreInformationUrl
             ~f:DashProgramInformationMoreInformationUrlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let moreInformationUrl =
        (Option.map ~f:DashProgramInformationMoreInformationUrlString.of_xml)
          (Xml.child xml_arg0 "MoreInformationUrl") in
      let languageCode =
        (Option.map ~f:DashProgramInformationLanguageCodeString.of_xml)
          (Xml.child xml_arg0 "LanguageCode") in
      let copyright =
        (Option.map ~f:DashProgramInformationCopyrightString.of_xml)
          (Xml.child xml_arg0 "Copyright") in
      let source =
        (Option.map ~f:DashProgramInformationSourceString.of_xml)
          (Xml.child xml_arg0 "Source") in
      let title =
        (Option.map ~f:DashProgramInformationTitleString.of_xml)
          (Xml.child xml_arg0 "Title") in
      make ?moreInformationUrl ?languageCode ?copyright ?source ?title ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let moreInformationUrl =
        field_map json__ "MoreInformationUrl"
          DashProgramInformationMoreInformationUrlString.of_json in
      let languageCode =
        field_map json__ "LanguageCode"
          DashProgramInformationLanguageCodeString.of_json in
      let copyright =
        field_map json__ "Copyright"
          DashProgramInformationCopyrightString.of_json in
      let source =
        field_map json__ "Source" DashProgramInformationSourceString.of_json in
      let title =
        field_map json__ "Title" DashProgramInformationTitleString.of_json in
      make ?moreInformationUrl ?languageCode ?copyright ?source ?title ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the content that you want MediaPackage to pass through in the manifest to the playback device."]
module DashSegmentTemplateFormat =
  struct
    type nonrec t =
      | NUMBER_WITH_TIMELINE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NUMBER_WITH_TIMELINE -> "NUMBER_WITH_TIMELINE"
      | Non_static_id s -> s
    let of_string =
      function
      | "NUMBER_WITH_TIMELINE" -> NUMBER_WITH_TIMELINE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DashSegmentTemplateFormat" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DashSegmentTemplateFormat" j)
    let to_json = simple_to_json to_value
  end
module DashSubtitleConfiguration =
  struct
    type nonrec t =
      {
      ttmlConfiguration: DashTtmlConfiguration.t option
        [@ocaml.doc "Settings for TTML subtitles."]}
    let make ?ttmlConfiguration = fun () -> { ttmlConfiguration }
    let to_value x =
      structure_to_value
        [("TtmlConfiguration",
           (Option.map x.ttmlConfiguration ~f:DashTtmlConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ttmlConfiguration =
        (Option.map ~f:DashTtmlConfiguration.of_xml)
          (Xml.child xml_arg0 "TtmlConfiguration") in
      make ?ttmlConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ttmlConfiguration =
        field_map json__ "TtmlConfiguration" DashTtmlConfiguration.of_json in
      make ?ttmlConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration for DASH subtitles."]
module DashUtcTiming =
  struct
    type nonrec t =
      {
      timingMode: DashUtcTimingMode.t option
        [@ocaml.doc "The UTC timing mode."];
      timingSource: DashUtcTimingTimingSourceString.t option
        [@ocaml.doc
          "The the method that the player uses to synchronize to coordinated universal time (UTC) wall clock time."]}
    let make ?timingMode =
      fun ?timingSource -> fun () -> { timingMode; timingSource }
    let to_value x =
      structure_to_value
        [("TimingMode",
           (Option.map x.timingMode ~f:DashUtcTimingMode.to_value));
        ("TimingSource",
          (Option.map x.timingSource
             ~f:DashUtcTimingTimingSourceString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timingSource =
        (Option.map ~f:DashUtcTimingTimingSourceString.of_xml)
          (Xml.child xml_arg0 "TimingSource") in
      let timingMode =
        (Option.map ~f:DashUtcTimingMode.of_xml)
          (Xml.child xml_arg0 "TimingMode") in
      make ?timingSource ?timingMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timingSource =
        field_map json__ "TimingSource"
          DashUtcTimingTimingSourceString.of_json in
      let timingMode =
        field_map json__ "TimingMode" DashUtcTimingMode.of_json in
      make ?timingSource ?timingMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Determines the type of UTC timing included in the DASH Media Presentation Description (MPD)."]
module FilterConfiguration =
  struct
    type nonrec t =
      {
      manifestFilter: FilterConfigurationManifestFilterString.t option
        [@ocaml.doc
          "Optionally specify one or more manifest filters for all of your manifest egress requests. When you include a manifest filter, note that you cannot use an identical manifest filter query parameter for this manifest's endpoint URL."];
      drmSettings: FilterConfigurationDrmSettingsString.t option
        [@ocaml.doc
          "Optionally specify one or more DRM settings for all of your manifest egress requests. When you include a DRM setting, note that you cannot use an identical DRM setting query parameter for this manifest's endpoint URL."];
      start: Timestamp.t option
        [@ocaml.doc
          "Optionally specify the start time for all of your manifest egress requests. When you include start time, note that you cannot use start time query parameters for this manifest's endpoint URL."];
      end_: Timestamp.t option
        [@ocaml.doc
          "Optionally specify the end time for all of your manifest egress requests. When you include end time, note that you cannot use end time query parameters for this manifest's endpoint URL."];
      timeDelaySeconds: FilterConfigurationTimeDelaySecondsInteger.t option
        [@ocaml.doc
          "Optionally specify the time delay for all of your manifest egress requests. Enter a value that is smaller than your endpoint's startover window. When you include time delay, note that you cannot use time delay query parameters for this manifest's endpoint URL."];
      clipStartTime: Timestamp.t option
        [@ocaml.doc
          "Optionally specify the clip start time for all of your manifest egress requests. When you include clip start time, note that you cannot use clip start time query parameters for this manifest's endpoint URL."]}
    let make ?manifestFilter =
      fun ?drmSettings ->
        fun ?start ->
          fun ?end_ ->
            fun ?timeDelaySeconds ->
              fun ?clipStartTime ->
                fun () ->
                  {
                    manifestFilter;
                    drmSettings;
                    start;
                    end_;
                    timeDelaySeconds;
                    clipStartTime
                  }
    let to_value x =
      structure_to_value
        [("ManifestFilter",
           (Option.map x.manifestFilter
              ~f:FilterConfigurationManifestFilterString.to_value));
        ("DrmSettings",
          (Option.map x.drmSettings
             ~f:FilterConfigurationDrmSettingsString.to_value));
        ("Start", (Option.map x.start ~f:Timestamp.to_value));
        ("End", (Option.map x.end_ ~f:Timestamp.to_value));
        ("TimeDelaySeconds",
          (Option.map x.timeDelaySeconds
             ~f:FilterConfigurationTimeDelaySecondsInteger.to_value));
        ("ClipStartTime", (Option.map x.clipStartTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clipStartTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ClipStartTime") in
      let timeDelaySeconds =
        (Option.map ~f:FilterConfigurationTimeDelaySecondsInteger.of_xml)
          (Xml.child xml_arg0 "TimeDelaySeconds") in
      let end_ = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "End") in
      let start =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "Start") in
      let drmSettings =
        (Option.map ~f:FilterConfigurationDrmSettingsString.of_xml)
          (Xml.child xml_arg0 "DrmSettings") in
      let manifestFilter =
        (Option.map ~f:FilterConfigurationManifestFilterString.of_xml)
          (Xml.child xml_arg0 "ManifestFilter") in
      make ?clipStartTime ?timeDelaySeconds ?end_ ?start ?drmSettings
        ?manifestFilter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clipStartTime = field_map json__ "ClipStartTime" Timestamp.of_json in
      let timeDelaySeconds =
        field_map json__ "TimeDelaySeconds"
          FilterConfigurationTimeDelaySecondsInteger.of_json in
      let end_ = field_map json__ "End" Timestamp.of_json in
      let start = field_map json__ "Start" Timestamp.of_json in
      let drmSettings =
        field_map json__ "DrmSettings"
          FilterConfigurationDrmSettingsString.of_json in
      let manifestFilter =
        field_map json__ "ManifestFilter"
          FilterConfigurationManifestFilterString.of_json in
      make ?clipStartTime ?timeDelaySeconds ?end_ ?start ?drmSettings
        ?manifestFilter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Filter configuration includes settings for manifest filtering, start and end times, and time delay that apply to all of your egress requests for this manifest."]
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ScteDash =
  struct
    type nonrec t =
      {
      adMarkerDash: AdMarkerDash.t option
        [@ocaml.doc
          "Choose how ad markers are included in the packaged content. If you include ad markers in the content stream in your upstream encoders, then you need to inform MediaPackage what to do with the ad markers in the output. Value description: Binary - The SCTE-35 marker is expressed as a hex-string (Base64 string) rather than full XML. XML - The SCTE marker is expressed fully in XML."];
      scteInManifests: ScteInManifests.t option
        [@ocaml.doc
          "Controls which SCTE-35 events appear in DASH manifests. ALL includes all non-implicit SCTE-35 events. MATCHES_FILTER includes only events whose type matches the configured ScteFilter. If you don't specify a value, the default is ALL."]}
    let make ?adMarkerDash =
      fun ?scteInManifests -> fun () -> { adMarkerDash; scteInManifests }
    let to_value x =
      structure_to_value
        [("AdMarkerDash",
           (Option.map x.adMarkerDash ~f:AdMarkerDash.to_value));
        ("ScteInManifests",
          (Option.map x.scteInManifests ~f:ScteInManifests.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scteInManifests =
        (Option.map ~f:ScteInManifests.of_xml)
          (Xml.child xml_arg0 "ScteInManifests") in
      let adMarkerDash =
        (Option.map ~f:AdMarkerDash.of_xml)
          (Xml.child xml_arg0 "AdMarkerDash") in
      make ?scteInManifests ?adMarkerDash ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scteInManifests =
        field_map json__ "ScteInManifests" ScteInManifests.of_json in
      let adMarkerDash = field_map json__ "AdMarkerDash" AdMarkerDash.of_json in
      make ?scteInManifests ?adMarkerDash ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The SCTE configuration."]
module UriPathType =
  struct
    type nonrec t =
      | LEAF 
      | ROOT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | LEAF -> "LEAF" | ROOT -> "ROOT" | Non_static_id s -> s
    let of_string =
      function | "LEAF" -> LEAF | "ROOT" -> ROOT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration UriPathType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"UriPathType" j)
    let to_json = simple_to_json to_value
  end
module ScteHls =
  struct
    type nonrec t =
      {
      adMarkerHls: AdMarkerHls.t option
        [@ocaml.doc
          "Ad markers indicate when ads should be inserted during playback. If you include ad markers in the content stream in your upstream encoders, then you need to inform MediaPackage what to do with the ad markers in the output. Choose what you want MediaPackage to do with the ad markers. Value description: SCTE35_ENHANCED - Generate industry-standard CUE tag ad markers in HLS manifests based on SCTE-35 input messages from the input stream. DATERANGE - Insert EXT-X-DATERANGE tags to signal ad and program transition events in TS and CMAF manifests. If you use DATERANGE, you must set a programDateTimeIntervalSeconds value of 1 or higher. To learn more about DATERANGE, see SCTE-35 Ad Marker EXT-X-DATERANGE."];
      scteInManifests: ScteInManifests.t option
        [@ocaml.doc
          "Controls which SCTE-35 events appear in HLS manifests. ALL includes all non-implicit SCTE-35 events. MATCHES_FILTER includes only events whose type matches the configured ScteFilter. If you don't specify a value, the default is ALL."]}
    let make ?adMarkerHls =
      fun ?scteInManifests -> fun () -> { adMarkerHls; scteInManifests }
    let to_value x =
      structure_to_value
        [("AdMarkerHls", (Option.map x.adMarkerHls ~f:AdMarkerHls.to_value));
        ("ScteInManifests",
          (Option.map x.scteInManifests ~f:ScteInManifests.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scteInManifests =
        (Option.map ~f:ScteInManifests.of_xml)
          (Xml.child xml_arg0 "ScteInManifests") in
      let adMarkerHls =
        (Option.map ~f:AdMarkerHls.of_xml) (Xml.child xml_arg0 "AdMarkerHls") in
      make ?scteInManifests ?adMarkerHls ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scteInManifests =
        field_map json__ "ScteInManifests" ScteInManifests.of_json in
      let adMarkerHls = field_map json__ "AdMarkerHls" AdMarkerHls.of_json in
      make ?scteInManifests ?adMarkerHls ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The SCTE configuration."]
module StartTag =
  struct
    type nonrec t =
      {
      timeOffset: Float_.t
        [@ocaml.doc
          "Specify the value for TIME-OFFSET within your EXT-X-START tag. Enter a signed floating point value which, if positive, must be less than the configured manifest duration minus three times the configured segment target duration. If negative, the absolute value must be larger than three times the configured segment target duration, and the absolute value must be smaller than the configured manifest duration."];
      precise: Boolean.t option
        [@ocaml.doc
          "Specify the value for PRECISE within your EXT-X-START tag. Leave blank, or choose false, to use the default value NO. Choose yes to use the value YES."]}
    let context_ = "StartTag"
    let make ?precise = fun ~timeOffset -> fun () -> { precise; timeOffset }
    let to_value x =
      structure_to_value
        [("TimeOffset", (Some (Float_.to_value x.timeOffset)));
        ("Precise", (Option.map x.precise ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let precise =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Precise") in
      let timeOffset =
        Float_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "TimeOffset") in
      make ?precise ~timeOffset ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let precise = field_map json__ "Precise" Boolean.of_json in
      let timeOffset = field_map_exn json__ "TimeOffset" Float_.of_json in
      make ?precise ~timeOffset ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "To insert an EXT-X-START tag in your HLS playlist, specify a StartTag configuration object with a valid TimeOffset. When you do, you can also optionally specify whether to include a PRECISE value in the EXT-X-START tag."]
module ManifestName =
  struct
    type nonrec t = string
    let context_ = "ManifestName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ManifestName" j
    let to_json = simple_to_json to_value
  end
module MssManifestLayout =
  struct
    type nonrec t =
      | FULL 
      | COMPACT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | FULL -> "FULL" | COMPACT -> "COMPACT" | Non_static_id s -> s
    let of_string =
      function | "FULL" -> FULL | "COMPACT" -> COMPACT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration MssManifestLayout" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MssManifestLayout" j)
    let to_json = simple_to_json to_value
  end
module EncryptionConstantInitializationVectorString =
  struct
    type nonrec t = string
    let context_ = "EncryptionConstantInitializationVectorString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:32) >>=
             (fun () ->
                (check_string_max i ~max:32) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9a-fA-F]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"EncryptionConstantInitializationVectorString" j
    let to_json = simple_to_json to_value
  end
module EncryptionKeyRotationIntervalSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:31536000) >>=
             (fun () -> check_int_min i ~min:300));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for EncryptionKeyRotationIntervalSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module EncryptionMethod =
  struct
    type nonrec t =
      {
      tsEncryptionMethod: TsEncryptionMethod.t option
        [@ocaml.doc "The encryption method to use."];
      cmafEncryptionMethod: CmafEncryptionMethod.t option
        [@ocaml.doc "The encryption method to use."];
      ismEncryptionMethod: IsmEncryptionMethod.t option
        [@ocaml.doc
          "The encryption method used for Microsoft Smooth Streaming (MSS) content. This specifies how the MSS segments are encrypted to protect the content during delivery to client players."]}
    let make ?tsEncryptionMethod =
      fun ?cmafEncryptionMethod ->
        fun ?ismEncryptionMethod ->
          fun () ->
            { tsEncryptionMethod; cmafEncryptionMethod; ismEncryptionMethod }
    let to_value x =
      structure_to_value
        [("TsEncryptionMethod",
           (Option.map x.tsEncryptionMethod ~f:TsEncryptionMethod.to_value));
        ("CmafEncryptionMethod",
          (Option.map x.cmafEncryptionMethod ~f:CmafEncryptionMethod.to_value));
        ("IsmEncryptionMethod",
          (Option.map x.ismEncryptionMethod ~f:IsmEncryptionMethod.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ismEncryptionMethod =
        (Option.map ~f:IsmEncryptionMethod.of_xml)
          (Xml.child xml_arg0 "IsmEncryptionMethod") in
      let cmafEncryptionMethod =
        (Option.map ~f:CmafEncryptionMethod.of_xml)
          (Xml.child xml_arg0 "CmafEncryptionMethod") in
      let tsEncryptionMethod =
        (Option.map ~f:TsEncryptionMethod.of_xml)
          (Xml.child xml_arg0 "TsEncryptionMethod") in
      make ?ismEncryptionMethod ?cmafEncryptionMethod ?tsEncryptionMethod ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ismEncryptionMethod =
        field_map json__ "IsmEncryptionMethod" IsmEncryptionMethod.of_json in
      let cmafEncryptionMethod =
        field_map json__ "CmafEncryptionMethod" CmafEncryptionMethod.of_json in
      let tsEncryptionMethod =
        field_map json__ "TsEncryptionMethod" TsEncryptionMethod.of_json in
      make ?ismEncryptionMethod ?cmafEncryptionMethod ?tsEncryptionMethod ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The encryption type."]
module SpekeKeyProvider =
  struct
    type nonrec t =
      {
      encryptionContractConfiguration: EncryptionContractConfiguration.t
        [@ocaml.doc
          "Configure one or more content encryption keys for your endpoints that use SPEKE Version 2.0. The encryption contract defines which content keys are used to encrypt the audio and video tracks in your stream. To configure the encryption contract, specify which audio and video encryption presets to use."];
      resourceId: SpekeKeyProviderResourceIdString.t
        [@ocaml.doc
          "The unique identifier for the content. The service sends this to the key server to identify the current endpoint. How unique you make this depends on how fine-grained you want access controls to be. The service does not permit you to use the same ID for two simultaneous encryption processes. The resource ID is also known as the content ID. The following example shows a resource ID: MovieNight20171126093045"];
      drmSystems: SpekeKeyProviderDrmSystemsList.t
        [@ocaml.doc
          "The DRM solution provider you're using to protect your content during distribution."];
      roleArn: SpekeKeyProviderRoleArnString.t
        [@ocaml.doc
          "The ARN for the IAM role granted by the key provider that provides access to the key provider API. This role must have a trust policy that allows MediaPackage to assume the role, and it must have a sufficient permissions policy to allow access to the specific key retrieval URL. Get this from your DRM solution provider. Valid format: arn:aws:iam::\\{accountID\\}:role/\\{name\\}. The following example shows a role ARN: arn:aws:iam::444455556666:role/SpekeAccess"];
      url: SpekeKeyProviderUrlString.t
        [@ocaml.doc
          "The URL of the API Gateway proxy that you set up to talk to your key server. The API Gateway proxy must reside in the same AWS Region as MediaPackage and must start with https://. The following example shows a URL: https://1wm2dx1f33.execute-api.us-west-2.amazonaws.com/SpekeSample/copyProtection"];
      certificateArn: SpekeKeyProviderCertificateArnString.t option
        [@ocaml.doc
          "The ARN for the certificate that you imported to Amazon Web Services Certificate Manager to add content key encryption to this endpoint. For this feature to work, your DRM key provider must support content key encryption."]}
    let context_ = "SpekeKeyProvider"
    let make ?certificateArn =
      fun ~encryptionContractConfiguration ->
        fun ~resourceId ->
          fun ~drmSystems ->
            fun ~roleArn ->
              fun ~url ->
                fun () ->
                  {
                    certificateArn;
                    encryptionContractConfiguration;
                    resourceId;
                    drmSystems;
                    roleArn;
                    url
                  }
    let to_value x =
      structure_to_value
        [("EncryptionContractConfiguration",
           (Some
              (EncryptionContractConfiguration.to_value
                 x.encryptionContractConfiguration)));
        ("ResourceId",
          (Some (SpekeKeyProviderResourceIdString.to_value x.resourceId)));
        ("DrmSystems",
          (Some (SpekeKeyProviderDrmSystemsList.to_value x.drmSystems)));
        ("RoleArn",
          (Some (SpekeKeyProviderRoleArnString.to_value x.roleArn)));
        ("Url", (Some (SpekeKeyProviderUrlString.to_value x.url)));
        ("CertificateArn",
          (Option.map x.certificateArn
             ~f:SpekeKeyProviderCertificateArnString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let certificateArn =
        (Option.map ~f:SpekeKeyProviderCertificateArnString.of_xml)
          (Xml.child xml_arg0 "CertificateArn") in
      let url =
        SpekeKeyProviderUrlString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Url") in
      let roleArn =
        SpekeKeyProviderRoleArnString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      let drmSystems =
        SpekeKeyProviderDrmSystemsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DrmSystems") in
      let resourceId =
        SpekeKeyProviderResourceIdString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let encryptionContractConfiguration =
        EncryptionContractConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "EncryptionContractConfiguration") in
      make ?certificateArn ~url ~roleArn ~drmSystems ~resourceId
        ~encryptionContractConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let certificateArn =
        field_map json__ "CertificateArn"
          SpekeKeyProviderCertificateArnString.of_json in
      let url = field_map_exn json__ "Url" SpekeKeyProviderUrlString.of_json in
      let roleArn =
        field_map_exn json__ "RoleArn" SpekeKeyProviderRoleArnString.of_json in
      let drmSystems =
        field_map_exn json__ "DrmSystems"
          SpekeKeyProviderDrmSystemsList.of_json in
      let resourceId =
        field_map_exn json__ "ResourceId"
          SpekeKeyProviderResourceIdString.of_json in
      let encryptionContractConfiguration =
        field_map_exn json__ "EncryptionContractConfiguration"
          EncryptionContractConfiguration.of_json in
      make ?certificateArn ~url ~roleArn ~drmSystems ~resourceId
        ~encryptionContractConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The parameters for the SPEKE key provider."]
module CustomAdTypeList =
  struct
    type nonrec t = CustomAdType.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CustomAdType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CustomAdType.of_xml)
    let of_json j =
      list_of_json ~kind:"CustomAdTypeList" ~of_json:CustomAdType.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScteFilterList =
  struct
    type nonrec t = ScteFilter.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ScteFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScteFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"ScteFilterList" ~of_json:ScteFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScteInSegments =
  struct
    type nonrec t =
      | NONE 
      | ALL 
      | MATCHES_FILTER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | ALL -> "ALL"
      | MATCHES_FILTER -> "MATCHES_FILTER"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "ALL" -> ALL
      | "MATCHES_FILTER" -> MATCHES_FILTER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ScteInSegments" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ScteInSegments" j)
    let to_json = simple_to_json to_value
  end
module CreateDashManifestConfigurationManifestWindowSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:30); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateDashManifestConfigurationManifestWindowSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateDashManifestConfigurationMinBufferTimeSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:3600) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateDashManifestConfigurationMinBufferTimeSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateDashManifestConfigurationMinUpdatePeriodSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:3600) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateDashManifestConfigurationMinUpdatePeriodSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateDashManifestConfigurationSuggestedPresentationDelaySecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:3600) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateDashManifestConfigurationSuggestedPresentationDelaySecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateHlsManifestConfigurationManifestWindowSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:30); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateHlsManifestConfigurationManifestWindowSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1209600) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateLowLatencyHlsManifestConfigurationManifestWindowSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:30); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateLowLatencyHlsManifestConfigurationManifestWindowSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateLowLatencyHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1209600) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateLowLatencyHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateMssManifestConfigurationManifestWindowSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:30); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateMssManifestConfigurationManifestWindowSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CdnIdentifierSecretArn =
  struct
    type nonrec t = string
    let context_ = "CdnIdentifierSecretArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:20));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CdnIdentifierSecretArn" j
    let to_json = simple_to_json to_value
  end
module ContainerType =
  struct
    type nonrec t =
      | TS 
      | CMAF 
      | ISM 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TS -> "TS"
      | CMAF -> "CMAF"
      | ISM -> "ISM"
      | Non_static_id s -> s
    let of_string =
      function
      | "TS" -> TS
      | "CMAF" -> CMAF
      | "ISM" -> ISM
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ContainerType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ContainerType" j)
    let to_json = simple_to_json to_value
  end
module ForceEndpointErrorConfiguration =
  struct
    type nonrec t =
      {
      endpointErrorConditions: EndpointErrorConditions.t option
        [@ocaml.doc
          "The failover conditions for the endpoint. The options are: STALE_MANIFEST - The manifest stalled and there are no new segments or parts. INCOMPLETE_MANIFEST - There is a gap in the manifest. MISSING_DRM_KEY - Key rotation is enabled but we're unable to fetch the key for the current key period. SLATE_INPUT - The segments which contain slate content are considered to be missing content."]}
    let make ?endpointErrorConditions = fun () -> { endpointErrorConditions }
    let to_value x =
      structure_to_value
        [("EndpointErrorConditions",
           (Option.map x.endpointErrorConditions
              ~f:EndpointErrorConditions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endpointErrorConditions =
        (Option.map ~f:EndpointErrorConditions.of_xml)
          (Xml.child xml_arg0 "EndpointErrorConditions") in
      make ?endpointErrorConditions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endpointErrorConditions =
        field_map json__ "EndpointErrorConditions"
          EndpointErrorConditions.of_json in
      make ?endpointErrorConditions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The failover settings for the endpoint."]
module ListDashManifests =
  struct
    type nonrec t = ListDashManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ListDashManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ListDashManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"ListDashManifests"
        ~of_json:ListDashManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListHlsManifests =
  struct
    type nonrec t = ListHlsManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ListHlsManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ListHlsManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"ListHlsManifests"
        ~of_json:ListHlsManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListLowLatencyHlsManifests =
  struct
    type nonrec t = ListLowLatencyHlsManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ListLowLatencyHlsManifestConfiguration.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:ListLowLatencyHlsManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"ListLowLatencyHlsManifests"
        ~of_json:ListLowLatencyHlsManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListMssManifests =
  struct
    type nonrec t = ListMssManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ListMssManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ListMssManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"ListMssManifests"
        ~of_json:ListMssManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceDescription =
  struct
    type nonrec t = string
    let context_ = "ResourceDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDescription" j
    let to_json = simple_to_json to_value
  end
module UriSeparator =
  struct
    type nonrec t =
      | UNDERSCORE 
      | HYPHEN 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UNDERSCORE -> "UNDERSCORE"
      | HYPHEN -> "HYPHEN"
      | Non_static_id s -> s
    let of_string =
      function
      | "UNDERSCORE" -> UNDERSCORE
      | "HYPHEN" -> HYPHEN
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration UriSeparator" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"UriSeparator" j)
    let to_json = simple_to_json to_value
  end
module Destination =
  struct
    type nonrec t =
      {
      s3Destination: S3DestinationConfig.t
        [@ocaml.doc
          "The configuration for exporting harvested content to an S3 bucket. This includes details such as the bucket name and destination path within the bucket."]}
    let context_ = "Destination"
    let make ~s3Destination = fun () -> { s3Destination }
    let to_value x =
      structure_to_value
        [("S3Destination",
           (Some (S3DestinationConfig.to_value x.s3Destination)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Destination =
        S3DestinationConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "S3Destination") in
      make ~s3Destination ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Destination =
        field_map_exn json__ "S3Destination" S3DestinationConfig.of_json in
      make ~s3Destination ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration for the destination where the harvested content will be exported."]
module EntityTag =
  struct
    type nonrec t = string
    let context_ = "EntityTag"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EntityTag" j
    let to_json = simple_to_json to_value
  end
module HarvestJobStatus =
  struct
    type nonrec t =
      | QUEUED 
      | IN_PROGRESS 
      | CANCELLED 
      | COMPLETED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | QUEUED -> "QUEUED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | CANCELLED -> "CANCELLED"
      | COMPLETED -> "COMPLETED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "QUEUED" -> QUEUED
      | "IN_PROGRESS" -> IN_PROGRESS
      | "CANCELLED" -> CANCELLED
      | "COMPLETED" -> COMPLETED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration HarvestJobStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"HarvestJobStatus" j)
    let to_json = simple_to_json to_value
  end
module HarvestedManifests =
  struct
    type nonrec t =
      {
      hlsManifests: HarvestedHlsManifestsList.t option
        [@ocaml.doc "A list of harvested HLS manifests."];
      dashManifests: HarvestedDashManifestsList.t option
        [@ocaml.doc "A list of harvested DASH manifests."];
      lowLatencyHlsManifests: HarvestedLowLatencyHlsManifestsList.t option
        [@ocaml.doc "A list of harvested Low-Latency HLS manifests."]}
    let make ?hlsManifests =
      fun ?dashManifests ->
        fun ?lowLatencyHlsManifests ->
          fun () -> { hlsManifests; dashManifests; lowLatencyHlsManifests }
    let to_value x =
      structure_to_value
        [("HlsManifests",
           (Option.map x.hlsManifests ~f:HarvestedHlsManifestsList.to_value));
        ("DashManifests",
          (Option.map x.dashManifests ~f:HarvestedDashManifestsList.to_value));
        ("LowLatencyHlsManifests",
          (Option.map x.lowLatencyHlsManifests
             ~f:HarvestedLowLatencyHlsManifestsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lowLatencyHlsManifests =
        (Option.map ~f:HarvestedLowLatencyHlsManifestsList.of_xml)
          (Xml.child xml_arg0 "LowLatencyHlsManifests") in
      let dashManifests =
        (Option.map ~f:HarvestedDashManifestsList.of_xml)
          (Xml.child xml_arg0 "DashManifests") in
      let hlsManifests =
        (Option.map ~f:HarvestedHlsManifestsList.of_xml)
          (Xml.child xml_arg0 "HlsManifests") in
      make ?lowLatencyHlsManifests ?dashManifests ?hlsManifests ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lowLatencyHlsManifests =
        field_map json__ "LowLatencyHlsManifests"
          HarvestedLowLatencyHlsManifestsList.of_json in
      let dashManifests =
        field_map json__ "DashManifests" HarvestedDashManifestsList.of_json in
      let hlsManifests =
        field_map json__ "HlsManifests" HarvestedHlsManifestsList.of_json in
      make ?lowLatencyHlsManifests ?dashManifests ?hlsManifests ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A collection of harvested manifests of different types."]
module HarvesterScheduleConfiguration =
  struct
    type nonrec t =
      {
      startTime: Timestamp.t
        [@ocaml.doc "The start time for the harvest job."];
      endTime: Timestamp.t [@ocaml.doc "The end time for the harvest job."]}
    let context_ = "HarvesterScheduleConfiguration"
    let make ~startTime = fun ~endTime -> fun () -> { startTime; endTime }
    let to_value x =
      structure_to_value
        [("StartTime", (Some (Timestamp.to_value x.startTime)));
        ("EndTime", (Some (Timestamp.to_value x.endTime)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endTime =
        Timestamp.of_xml (Xml.child_exn ~context:context_ xml_arg0 "EndTime") in
      let startTime =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StartTime") in
      make ~endTime ~startTime ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endTime = field_map_exn json__ "EndTime" Timestamp.of_json in
      let startTime = field_map_exn json__ "StartTime" Timestamp.of_json in
      make ~endTime ~startTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Defines the schedule configuration for a harvest job."]
module InputType =
  struct
    type nonrec t =
      | HLS 
      | CMAF 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | HLS -> "HLS" | CMAF -> "CMAF" | Non_static_id s -> s
    let of_string =
      function | "HLS" -> HLS | "CMAF" -> CMAF | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InputType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InputType" j)
    let to_json = simple_to_json to_value
  end
module ConflictExceptionType =
  struct
    type nonrec t =
      | RESOURCE_IN_USE 
      | RESOURCE_ALREADY_EXISTS 
      | IDEMPOTENT_PARAMETER_MISMATCH 
      | CONFLICTING_OPERATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RESOURCE_IN_USE -> "RESOURCE_IN_USE"
      | RESOURCE_ALREADY_EXISTS -> "RESOURCE_ALREADY_EXISTS"
      | IDEMPOTENT_PARAMETER_MISMATCH -> "IDEMPOTENT_PARAMETER_MISMATCH"
      | CONFLICTING_OPERATION -> "CONFLICTING_OPERATION"
      | Non_static_id s -> s
    let of_string =
      function
      | "RESOURCE_IN_USE" -> RESOURCE_IN_USE
      | "RESOURCE_ALREADY_EXISTS" -> RESOURCE_ALREADY_EXISTS
      | "IDEMPOTENT_PARAMETER_MISMATCH" -> IDEMPOTENT_PARAMETER_MISMATCH
      | "CONFLICTING_OPERATION" -> CONFLICTING_OPERATION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ConflictExceptionType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ConflictExceptionType" j)
    let to_json = simple_to_json to_value
  end
module GetDashManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t option
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index."];
      url: String_.t option
        [@ocaml.doc
          "The egress domain URL for stream delivery from MediaPackage."];
      manifestWindowSeconds: Integer.t option
        [@ocaml.doc
          "The total duration (in seconds) of the manifest's content."];
      filterConfiguration: FilterConfiguration.t option ;
      minUpdatePeriodSeconds: Integer.t option
        [@ocaml.doc
          "Minimum amount of time (in seconds) that the player should wait before requesting updates to the manifest."];
      minBufferTimeSeconds: Integer.t option
        [@ocaml.doc
          "Minimum amount of content (in seconds) that a player must keep available in the buffer."];
      suggestedPresentationDelaySeconds: Integer.t option
        [@ocaml.doc
          "The amount of time (in seconds) that the player should be from the end of the manifest."];
      segmentTemplateFormat: DashSegmentTemplateFormat.t option
        [@ocaml.doc
          "Determines the type of variable used in the media URL of the SegmentTemplate tag in the manifest. Also specifies if segment timeline information is included in SegmentTimeline or SegmentTemplate. Value description: NUMBER_WITH_TIMELINE - The $Number$ variable is used in the media URL. The value of this variable is the sequential number of the segment. A full SegmentTimeline object is presented in each SegmentTemplate."];
      periodTriggers: DashPeriodTriggers.t option
        [@ocaml.doc
          "A list of triggers that controls when AWS Elemental MediaPackage separates the MPEG-DASH manifest into multiple periods. Leave this value empty to indicate that the manifest is contained all in one period. For more information about periods in the DASH manifest, see Multi-period DASH in AWS Elemental MediaPackage."];
      scteDash: ScteDash.t option [@ocaml.doc "The SCTE configuration."];
      drmSignaling: DashDrmSignaling.t option
        [@ocaml.doc
          "Determines how the DASH manifest signals the DRM content."];
      utcTiming: DashUtcTiming.t option
        [@ocaml.doc
          "Determines the type of UTC timing included in the DASH Media Presentation Description (MPD)."];
      profiles: DashProfiles.t option
        [@ocaml.doc "The profile that the output is compliant with."];
      baseUrls: DashBaseUrls.t option
        [@ocaml.doc "The base URL to use for retrieving segments."];
      programInformation: DashProgramInformation.t option
        [@ocaml.doc
          "Details about the content that you want MediaPackage to pass through in the manifest to the playback device."];
      dvbSettings: DashDvbSettings.t option
        [@ocaml.doc
          "For endpoints that use the DVB-DASH profile only. The font download and error reporting information that you want MediaPackage to pass through to the manifest."];
      compactness: DashCompactness.t option
        [@ocaml.doc
          "The layout of the DASH manifest that MediaPackage produces. STANDARD indicates a default manifest, which is compacted. NONE indicates a full manifest."];
      subtitleConfiguration: DashSubtitleConfiguration.t option
        [@ocaml.doc "The configuration for DASH subtitles."];
      uriPathType: UriPathType.t option
        [@ocaml.doc
          "The type of path used in manifest URIs. LEAF indicates leaf-relative paths. ROOT indicates root-relative paths that include the full path from root."];
      availabilityStartTimeConfiguration:
        DashAvailabilityStartTimeConfiguration.t option
        [@ocaml.doc
          "The configuration for the DASH availabilityStartTime attribute of the Media Presentation Description (MPD)."]}
    let make ?manifestName =
      fun ?url ->
        fun ?manifestWindowSeconds ->
          fun ?filterConfiguration ->
            fun ?minUpdatePeriodSeconds ->
              fun ?minBufferTimeSeconds ->
                fun ?suggestedPresentationDelaySeconds ->
                  fun ?segmentTemplateFormat ->
                    fun ?periodTriggers ->
                      fun ?scteDash ->
                        fun ?drmSignaling ->
                          fun ?utcTiming ->
                            fun ?profiles ->
                              fun ?baseUrls ->
                                fun ?programInformation ->
                                  fun ?dvbSettings ->
                                    fun ?compactness ->
                                      fun ?subtitleConfiguration ->
                                        fun ?uriPathType ->
                                          fun
                                            ?availabilityStartTimeConfiguration
                                            ->
                                            fun () ->
                                              {
                                                manifestName;
                                                url;
                                                manifestWindowSeconds;
                                                filterConfiguration;
                                                minUpdatePeriodSeconds;
                                                minBufferTimeSeconds;
                                                suggestedPresentationDelaySeconds;
                                                segmentTemplateFormat;
                                                periodTriggers;
                                                scteDash;
                                                drmSignaling;
                                                utcTiming;
                                                profiles;
                                                baseUrls;
                                                programInformation;
                                                dvbSettings;
                                                compactness;
                                                subtitleConfiguration;
                                                uriPathType;
                                                availabilityStartTimeConfiguration
                                              }
    let to_value x =
      structure_to_value
        [("ManifestName",
           (Option.map x.manifestName ~f:ResourceName.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value));
        ("ManifestWindowSeconds",
          (Option.map x.manifestWindowSeconds ~f:Integer.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("MinUpdatePeriodSeconds",
          (Option.map x.minUpdatePeriodSeconds ~f:Integer.to_value));
        ("MinBufferTimeSeconds",
          (Option.map x.minBufferTimeSeconds ~f:Integer.to_value));
        ("SuggestedPresentationDelaySeconds",
          (Option.map x.suggestedPresentationDelaySeconds ~f:Integer.to_value));
        ("SegmentTemplateFormat",
          (Option.map x.segmentTemplateFormat
             ~f:DashSegmentTemplateFormat.to_value));
        ("PeriodTriggers",
          (Option.map x.periodTriggers ~f:DashPeriodTriggers.to_value));
        ("ScteDash", (Option.map x.scteDash ~f:ScteDash.to_value));
        ("DrmSignaling",
          (Option.map x.drmSignaling ~f:DashDrmSignaling.to_value));
        ("UtcTiming", (Option.map x.utcTiming ~f:DashUtcTiming.to_value));
        ("Profiles", (Option.map x.profiles ~f:DashProfiles.to_value));
        ("BaseUrls", (Option.map x.baseUrls ~f:DashBaseUrls.to_value));
        ("ProgramInformation",
          (Option.map x.programInformation ~f:DashProgramInformation.to_value));
        ("DvbSettings",
          (Option.map x.dvbSettings ~f:DashDvbSettings.to_value));
        ("Compactness",
          (Option.map x.compactness ~f:DashCompactness.to_value));
        ("SubtitleConfiguration",
          (Option.map x.subtitleConfiguration
             ~f:DashSubtitleConfiguration.to_value));
        ("UriPathType", (Option.map x.uriPathType ~f:UriPathType.to_value));
        ("AvailabilityStartTimeConfiguration",
          (Option.map x.availabilityStartTimeConfiguration
             ~f:DashAvailabilityStartTimeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityStartTimeConfiguration =
        (Option.map ~f:DashAvailabilityStartTimeConfiguration.of_xml)
          (Xml.child xml_arg0 "AvailabilityStartTimeConfiguration") in
      let uriPathType =
        (Option.map ~f:UriPathType.of_xml) (Xml.child xml_arg0 "UriPathType") in
      let subtitleConfiguration =
        (Option.map ~f:DashSubtitleConfiguration.of_xml)
          (Xml.child xml_arg0 "SubtitleConfiguration") in
      let compactness =
        (Option.map ~f:DashCompactness.of_xml)
          (Xml.child xml_arg0 "Compactness") in
      let dvbSettings =
        (Option.map ~f:DashDvbSettings.of_xml)
          (Xml.child xml_arg0 "DvbSettings") in
      let programInformation =
        (Option.map ~f:DashProgramInformation.of_xml)
          (Xml.child xml_arg0 "ProgramInformation") in
      let baseUrls =
        (Option.map ~f:DashBaseUrls.of_xml) (Xml.child xml_arg0 "BaseUrls") in
      let profiles =
        (Option.map ~f:DashProfiles.of_xml) (Xml.child xml_arg0 "Profiles") in
      let utcTiming =
        (Option.map ~f:DashUtcTiming.of_xml) (Xml.child xml_arg0 "UtcTiming") in
      let drmSignaling =
        (Option.map ~f:DashDrmSignaling.of_xml)
          (Xml.child xml_arg0 "DrmSignaling") in
      let scteDash =
        (Option.map ~f:ScteDash.of_xml) (Xml.child xml_arg0 "ScteDash") in
      let periodTriggers =
        (Option.map ~f:DashPeriodTriggers.of_xml)
          (Xml.child xml_arg0 "PeriodTriggers") in
      let segmentTemplateFormat =
        (Option.map ~f:DashSegmentTemplateFormat.of_xml)
          (Xml.child xml_arg0 "SegmentTemplateFormat") in
      let suggestedPresentationDelaySeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "SuggestedPresentationDelaySeconds") in
      let minBufferTimeSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MinBufferTimeSeconds") in
      let minUpdatePeriodSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MinUpdatePeriodSeconds") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let manifestWindowSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ManifestWindowSeconds") in
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let manifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ManifestName") in
      make ?availabilityStartTimeConfiguration ?uriPathType
        ?subtitleConfiguration ?compactness ?dvbSettings ?programInformation
        ?baseUrls ?profiles ?utcTiming ?drmSignaling ?scteDash
        ?periodTriggers ?segmentTemplateFormat
        ?suggestedPresentationDelaySeconds ?minBufferTimeSeconds
        ?minUpdatePeriodSeconds ?filterConfiguration ?manifestWindowSeconds
        ?url ?manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityStartTimeConfiguration =
        field_map json__ "AvailabilityStartTimeConfiguration"
          DashAvailabilityStartTimeConfiguration.of_json in
      let uriPathType = field_map json__ "UriPathType" UriPathType.of_json in
      let subtitleConfiguration =
        field_map json__ "SubtitleConfiguration"
          DashSubtitleConfiguration.of_json in
      let compactness =
        field_map json__ "Compactness" DashCompactness.of_json in
      let dvbSettings =
        field_map json__ "DvbSettings" DashDvbSettings.of_json in
      let programInformation =
        field_map json__ "ProgramInformation" DashProgramInformation.of_json in
      let baseUrls = field_map json__ "BaseUrls" DashBaseUrls.of_json in
      let profiles = field_map json__ "Profiles" DashProfiles.of_json in
      let utcTiming = field_map json__ "UtcTiming" DashUtcTiming.of_json in
      let drmSignaling =
        field_map json__ "DrmSignaling" DashDrmSignaling.of_json in
      let scteDash = field_map json__ "ScteDash" ScteDash.of_json in
      let periodTriggers =
        field_map json__ "PeriodTriggers" DashPeriodTriggers.of_json in
      let segmentTemplateFormat =
        field_map json__ "SegmentTemplateFormat"
          DashSegmentTemplateFormat.of_json in
      let suggestedPresentationDelaySeconds =
        field_map json__ "SuggestedPresentationDelaySeconds" Integer.of_json in
      let minBufferTimeSeconds =
        field_map json__ "MinBufferTimeSeconds" Integer.of_json in
      let minUpdatePeriodSeconds =
        field_map json__ "MinUpdatePeriodSeconds" Integer.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let manifestWindowSeconds =
        field_map json__ "ManifestWindowSeconds" Integer.of_json in
      let url = field_map json__ "Url" String_.of_json in
      let manifestName = field_map json__ "ManifestName" ResourceName.of_json in
      make ?availabilityStartTimeConfiguration ?uriPathType
        ?subtitleConfiguration ?compactness ?dvbSettings ?programInformation
        ?baseUrls ?profiles ?utcTiming ?drmSignaling ?scteDash
        ?periodTriggers ?segmentTemplateFormat
        ?suggestedPresentationDelaySeconds ?minBufferTimeSeconds
        ?minUpdatePeriodSeconds ?filterConfiguration ?manifestWindowSeconds
        ?url ?manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieve the DASH manifest configuration."]
module GetHlsManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t option
        [@ocaml.doc
          "A short short string that's appended to the endpoint URL. The manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index. MediaPackage automatically inserts the format extension, such as .m3u8. You can't use the same manifest name if you use HLS manifest and low-latency HLS manifest. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      url: String_.t option
        [@ocaml.doc
          "The egress domain URL for stream delivery from MediaPackage."];
      childManifestName: ResourceName.t option
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The child manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default child manifest name, index_1. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      manifestWindowSeconds: Integer.t option
        [@ocaml.doc
          "The total duration (in seconds) of the manifest's content."];
      programDateTimeIntervalSeconds: Integer.t option
        [@ocaml.doc
          "Inserts EXT-X-PROGRAM-DATE-TIME tags in the output manifest at the interval that you specify. If you don't enter an interval, EXT-X-PROGRAM-DATE-TIME tags aren't included in the manifest. The tags sync the stream to the wall clock so that viewers can seek to a specific time in the playback timeline on the player. Irrespective of this parameter, if any ID3Timed metadata is in the HLS input, it is passed through to the HLS output."];
      scteHls: ScteHls.t option ;
      filterConfiguration: FilterConfiguration.t option ;
      startTag: StartTag.t option ;
      urlEncodeChildManifest: Boolean.t option
        [@ocaml.doc
          "When enabled, MediaPackage URL-encodes the query string for API requests for HLS child manifests to comply with Amazon Web Services Signature Version 4 (SigV4) signature signing protocol. For more information, see Amazon Web Services Signature Version 4 for API requests in Identity and Access Management User Guide."];
      uriPathType: UriPathType.t option
        [@ocaml.doc
          "The type of path used in manifest URIs. LEAF indicates leaf-relative paths. ROOT indicates root-relative paths that include the full path from root."]}
    let make ?manifestName =
      fun ?url ->
        fun ?childManifestName ->
          fun ?manifestWindowSeconds ->
            fun ?programDateTimeIntervalSeconds ->
              fun ?scteHls ->
                fun ?filterConfiguration ->
                  fun ?startTag ->
                    fun ?urlEncodeChildManifest ->
                      fun ?uriPathType ->
                        fun () ->
                          {
                            manifestName;
                            url;
                            childManifestName;
                            manifestWindowSeconds;
                            programDateTimeIntervalSeconds;
                            scteHls;
                            filterConfiguration;
                            startTag;
                            urlEncodeChildManifest;
                            uriPathType
                          }
    let to_value x =
      structure_to_value
        [("ManifestName",
           (Option.map x.manifestName ~f:ResourceName.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value));
        ("ChildManifestName",
          (Option.map x.childManifestName ~f:ResourceName.to_value));
        ("ManifestWindowSeconds",
          (Option.map x.manifestWindowSeconds ~f:Integer.to_value));
        ("ProgramDateTimeIntervalSeconds",
          (Option.map x.programDateTimeIntervalSeconds ~f:Integer.to_value));
        ("ScteHls", (Option.map x.scteHls ~f:ScteHls.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("StartTag", (Option.map x.startTag ~f:StartTag.to_value));
        ("UrlEncodeChildManifest",
          (Option.map x.urlEncodeChildManifest ~f:Boolean.to_value));
        ("UriPathType", (Option.map x.uriPathType ~f:UriPathType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uriPathType =
        (Option.map ~f:UriPathType.of_xml) (Xml.child xml_arg0 "UriPathType") in
      let urlEncodeChildManifest =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "UrlEncodeChildManifest") in
      let startTag =
        (Option.map ~f:StartTag.of_xml) (Xml.child xml_arg0 "StartTag") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let scteHls =
        (Option.map ~f:ScteHls.of_xml) (Xml.child xml_arg0 "ScteHls") in
      let programDateTimeIntervalSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ProgramDateTimeIntervalSeconds") in
      let manifestWindowSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ManifestWindowSeconds") in
      let childManifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChildManifestName") in
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let manifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ManifestName") in
      make ?uriPathType ?urlEncodeChildManifest ?startTag
        ?filterConfiguration ?scteHls ?programDateTimeIntervalSeconds
        ?manifestWindowSeconds ?childManifestName ?url ?manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uriPathType = field_map json__ "UriPathType" UriPathType.of_json in
      let urlEncodeChildManifest =
        field_map json__ "UrlEncodeChildManifest" Boolean.of_json in
      let startTag = field_map json__ "StartTag" StartTag.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let scteHls = field_map json__ "ScteHls" ScteHls.of_json in
      let programDateTimeIntervalSeconds =
        field_map json__ "ProgramDateTimeIntervalSeconds" Integer.of_json in
      let manifestWindowSeconds =
        field_map json__ "ManifestWindowSeconds" Integer.of_json in
      let childManifestName =
        field_map json__ "ChildManifestName" ResourceName.of_json in
      let url = field_map json__ "Url" String_.of_json in
      let manifestName = field_map json__ "ManifestName" ResourceName.of_json in
      make ?uriPathType ?urlEncodeChildManifest ?startTag
        ?filterConfiguration ?scteHls ?programDateTimeIntervalSeconds
        ?manifestWindowSeconds ?childManifestName ?url ?manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieve the HTTP live streaming (HLS) manifest configuration."]
module GetLowLatencyHlsManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ResourceName.t option
        [@ocaml.doc
          "A short short string that's appended to the endpoint URL. The manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index. MediaPackage automatically inserts the format extension, such as .m3u8. You can't use the same manifest name if you use HLS manifest and low-latency HLS manifest. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      url: String_.t option
        [@ocaml.doc
          "The egress domain URL for stream delivery from MediaPackage."];
      childManifestName: ResourceName.t option
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The child manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default child manifest name, index_1. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      manifestWindowSeconds: Integer.t option
        [@ocaml.doc
          "The total duration (in seconds) of the manifest's content."];
      programDateTimeIntervalSeconds: Integer.t option
        [@ocaml.doc
          "Inserts EXT-X-PROGRAM-DATE-TIME tags in the output manifest at the interval that you specify. If you don't enter an interval, EXT-X-PROGRAM-DATE-TIME tags aren't included in the manifest. The tags sync the stream to the wall clock so that viewers can seek to a specific time in the playback timeline on the player. Irrespective of this parameter, if any ID3Timed metadata is in the HLS input, it is passed through to the HLS output."];
      scteHls: ScteHls.t option ;
      filterConfiguration: FilterConfiguration.t option ;
      startTag: StartTag.t option ;
      urlEncodeChildManifest: Boolean.t option
        [@ocaml.doc
          "When enabled, MediaPackage URL-encodes the query string for API requests for LL-HLS child manifests to comply with Amazon Web Services Signature Version 4 (SigV4) signature signing protocol. For more information, see Amazon Web Services Signature Version 4 for API requests in Identity and Access Management User Guide."];
      uriPathType: UriPathType.t option
        [@ocaml.doc
          "The type of path used in manifest URIs. LEAF indicates leaf-relative paths. ROOT indicates root-relative paths that include the full path from root."]}
    let make ?manifestName =
      fun ?url ->
        fun ?childManifestName ->
          fun ?manifestWindowSeconds ->
            fun ?programDateTimeIntervalSeconds ->
              fun ?scteHls ->
                fun ?filterConfiguration ->
                  fun ?startTag ->
                    fun ?urlEncodeChildManifest ->
                      fun ?uriPathType ->
                        fun () ->
                          {
                            manifestName;
                            url;
                            childManifestName;
                            manifestWindowSeconds;
                            programDateTimeIntervalSeconds;
                            scteHls;
                            filterConfiguration;
                            startTag;
                            urlEncodeChildManifest;
                            uriPathType
                          }
    let to_value x =
      structure_to_value
        [("ManifestName",
           (Option.map x.manifestName ~f:ResourceName.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value));
        ("ChildManifestName",
          (Option.map x.childManifestName ~f:ResourceName.to_value));
        ("ManifestWindowSeconds",
          (Option.map x.manifestWindowSeconds ~f:Integer.to_value));
        ("ProgramDateTimeIntervalSeconds",
          (Option.map x.programDateTimeIntervalSeconds ~f:Integer.to_value));
        ("ScteHls", (Option.map x.scteHls ~f:ScteHls.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("StartTag", (Option.map x.startTag ~f:StartTag.to_value));
        ("UrlEncodeChildManifest",
          (Option.map x.urlEncodeChildManifest ~f:Boolean.to_value));
        ("UriPathType", (Option.map x.uriPathType ~f:UriPathType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uriPathType =
        (Option.map ~f:UriPathType.of_xml) (Xml.child xml_arg0 "UriPathType") in
      let urlEncodeChildManifest =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "UrlEncodeChildManifest") in
      let startTag =
        (Option.map ~f:StartTag.of_xml) (Xml.child xml_arg0 "StartTag") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let scteHls =
        (Option.map ~f:ScteHls.of_xml) (Xml.child xml_arg0 "ScteHls") in
      let programDateTimeIntervalSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ProgramDateTimeIntervalSeconds") in
      let manifestWindowSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ManifestWindowSeconds") in
      let childManifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChildManifestName") in
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let manifestName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ManifestName") in
      make ?uriPathType ?urlEncodeChildManifest ?startTag
        ?filterConfiguration ?scteHls ?programDateTimeIntervalSeconds
        ?manifestWindowSeconds ?childManifestName ?url ?manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uriPathType = field_map json__ "UriPathType" UriPathType.of_json in
      let urlEncodeChildManifest =
        field_map json__ "UrlEncodeChildManifest" Boolean.of_json in
      let startTag = field_map json__ "StartTag" StartTag.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let scteHls = field_map json__ "ScteHls" ScteHls.of_json in
      let programDateTimeIntervalSeconds =
        field_map json__ "ProgramDateTimeIntervalSeconds" Integer.of_json in
      let manifestWindowSeconds =
        field_map json__ "ManifestWindowSeconds" Integer.of_json in
      let childManifestName =
        field_map json__ "ChildManifestName" ResourceName.of_json in
      let url = field_map json__ "Url" String_.of_json in
      let manifestName = field_map json__ "ManifestName" ResourceName.of_json in
      make ?uriPathType ?urlEncodeChildManifest ?startTag
        ?filterConfiguration ?scteHls ?programDateTimeIntervalSeconds
        ?manifestWindowSeconds ?childManifestName ?url ?manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieve the low-latency HTTP live streaming (HLS) manifest configuration."]
module GetMssManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ManifestName.t option
        [@ocaml.doc
          "The name of the MSS manifest. This name is appended to the origin endpoint URL to create the unique path for accessing this specific MSS manifest."];
      url: String_.t option
        [@ocaml.doc
          "The complete URL for accessing the MSS manifest. Client players use this URL to retrieve the manifest and begin streaming the Microsoft Smooth Streaming content."];
      filterConfiguration: FilterConfiguration.t option ;
      manifestWindowSeconds: Integer.t option
        [@ocaml.doc
          "The duration (in seconds) of the manifest window. This represents the total amount of content available in the manifest at any given time."];
      manifestLayout: MssManifestLayout.t option
        [@ocaml.doc
          "The layout format of the MSS manifest, which determines how the manifest is structured for client compatibility."]}
    let make ?manifestName =
      fun ?url ->
        fun ?filterConfiguration ->
          fun ?manifestWindowSeconds ->
            fun ?manifestLayout ->
              fun () ->
                {
                  manifestName;
                  url;
                  filterConfiguration;
                  manifestWindowSeconds;
                  manifestLayout
                }
    let to_value x =
      structure_to_value
        [("ManifestName",
           (Option.map x.manifestName ~f:ManifestName.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("ManifestWindowSeconds",
          (Option.map x.manifestWindowSeconds ~f:Integer.to_value));
        ("ManifestLayout",
          (Option.map x.manifestLayout ~f:MssManifestLayout.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let manifestLayout =
        (Option.map ~f:MssManifestLayout.of_xml)
          (Xml.child xml_arg0 "ManifestLayout") in
      let manifestWindowSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "ManifestWindowSeconds") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let manifestName =
        (Option.map ~f:ManifestName.of_xml)
          (Xml.child xml_arg0 "ManifestName") in
      make ?manifestLayout ?manifestWindowSeconds ?filterConfiguration ?url
        ?manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let manifestLayout =
        field_map json__ "ManifestLayout" MssManifestLayout.of_json in
      let manifestWindowSeconds =
        field_map json__ "ManifestWindowSeconds" Integer.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let url = field_map json__ "Url" String_.of_json in
      let manifestName = field_map json__ "ManifestName" ManifestName.of_json in
      make ?manifestLayout ?manifestWindowSeconds ?filterConfiguration ?url
        ?manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration details for a Microsoft Smooth Streaming (MSS) manifest associated with an origin endpoint. This includes all the settings and properties that define how the MSS content is packaged and delivered."]
module ResourceTypeNotFound =
  struct
    type nonrec t =
      | CHANNEL_GROUP 
      | CHANNEL 
      | ORIGIN_ENDPOINT 
      | HARVEST_JOB 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CHANNEL_GROUP -> "CHANNEL_GROUP"
      | CHANNEL -> "CHANNEL"
      | ORIGIN_ENDPOINT -> "ORIGIN_ENDPOINT"
      | HARVEST_JOB -> "HARVEST_JOB"
      | Non_static_id s -> s
    let of_string =
      function
      | "CHANNEL_GROUP" -> CHANNEL_GROUP
      | "CHANNEL" -> CHANNEL
      | "ORIGIN_ENDPOINT" -> ORIGIN_ENDPOINT
      | "HARVEST_JOB" -> HARVEST_JOB
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceTypeNotFound" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceTypeNotFound" j)
    let to_json = simple_to_json to_value
  end
module Encryption =
  struct
    type nonrec t =
      {
      constantInitializationVector:
        EncryptionConstantInitializationVectorString.t option
        [@ocaml.doc
          "A 128-bit, 16-byte hex value represented by a 32-character string, used in conjunction with the key for encrypting content. If you don't specify a value, then MediaPackage creates the constant initialization vector (IV)."];
      encryptionMethod: EncryptionMethod.t
        [@ocaml.doc "The encryption method to use."];
      keyRotationIntervalSeconds:
        EncryptionKeyRotationIntervalSecondsInteger.t option
        [@ocaml.doc
          "The frequency (in seconds) of key changes for live workflows, in which content is streamed real time. The service retrieves content keys before the live content begins streaming, and then retrieves them as needed over the lifetime of the workflow. By default, key rotation is set to 300 seconds (5 minutes), the minimum rotation interval, which is equivalent to setting it to 300. If you don't enter an interval, content keys aren't rotated. The following example setting causes the service to rotate keys every thirty minutes: 1800"];
      cmafExcludeSegmentDrmMetadata: Boolean.t option
        [@ocaml.doc
          "Excludes SEIG and SGPD boxes from segment metadata in CMAF containers. When set to true, MediaPackage omits these DRM metadata boxes from CMAF segments, which can improve compatibility with certain devices and players that don't support these boxes. Important considerations: This setting only affects CMAF container formats Key rotation can still be handled through media playlist signaling PSSH and TENC boxes remain unaffected Default behavior is preserved when this setting is disabled Valid values: true | false Default: false"];
      spekeKeyProvider: SpekeKeyProvider.t
        [@ocaml.doc "The parameters for the SPEKE key provider."]}
    let context_ = "Encryption"
    let make ?constantInitializationVector =
      fun ?keyRotationIntervalSeconds ->
        fun ?cmafExcludeSegmentDrmMetadata ->
          fun ~encryptionMethod ->
            fun ~spekeKeyProvider ->
              fun () ->
                {
                  constantInitializationVector;
                  keyRotationIntervalSeconds;
                  cmafExcludeSegmentDrmMetadata;
                  encryptionMethod;
                  spekeKeyProvider
                }
    let to_value x =
      structure_to_value
        [("ConstantInitializationVector",
           (Option.map x.constantInitializationVector
              ~f:EncryptionConstantInitializationVectorString.to_value));
        ("EncryptionMethod",
          (Some (EncryptionMethod.to_value x.encryptionMethod)));
        ("KeyRotationIntervalSeconds",
          (Option.map x.keyRotationIntervalSeconds
             ~f:EncryptionKeyRotationIntervalSecondsInteger.to_value));
        ("CmafExcludeSegmentDrmMetadata",
          (Option.map x.cmafExcludeSegmentDrmMetadata ~f:Boolean.to_value));
        ("SpekeKeyProvider",
          (Some (SpekeKeyProvider.to_value x.spekeKeyProvider)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let spekeKeyProvider =
        SpekeKeyProvider.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SpekeKeyProvider") in
      let cmafExcludeSegmentDrmMetadata =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "CmafExcludeSegmentDrmMetadata") in
      let keyRotationIntervalSeconds =
        (Option.map ~f:EncryptionKeyRotationIntervalSecondsInteger.of_xml)
          (Xml.child xml_arg0 "KeyRotationIntervalSeconds") in
      let encryptionMethod =
        EncryptionMethod.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EncryptionMethod") in
      let constantInitializationVector =
        (Option.map ~f:EncryptionConstantInitializationVectorString.of_xml)
          (Xml.child xml_arg0 "ConstantInitializationVector") in
      make ~spekeKeyProvider ?cmafExcludeSegmentDrmMetadata
        ?keyRotationIntervalSeconds ~encryptionMethod
        ?constantInitializationVector ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let spekeKeyProvider =
        field_map_exn json__ "SpekeKeyProvider" SpekeKeyProvider.of_json in
      let cmafExcludeSegmentDrmMetadata =
        field_map json__ "CmafExcludeSegmentDrmMetadata" Boolean.of_json in
      let keyRotationIntervalSeconds =
        field_map json__ "KeyRotationIntervalSeconds"
          EncryptionKeyRotationIntervalSecondsInteger.of_json in
      let encryptionMethod =
        field_map_exn json__ "EncryptionMethod" EncryptionMethod.of_json in
      let constantInitializationVector =
        field_map json__ "ConstantInitializationVector"
          EncryptionConstantInitializationVectorString.of_json in
      make ~spekeKeyProvider ?cmafExcludeSegmentDrmMetadata
        ?keyRotationIntervalSeconds ~encryptionMethod
        ?constantInitializationVector ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The parameters for encrypting content."]
module Scte =
  struct
    type nonrec t =
      {
      scteFilter: ScteFilterList.t option
        [@ocaml.doc
          "The SCTE-35 message types that you want to be treated as ad markers in the output."];
      scteInSegments: ScteInSegments.t option
        [@ocaml.doc
          "Controls whether SCTE-35 messages are included in segment files. None \226\128\147 SCTE-35 messages are not included in segments (default) All \226\128\147 SCTE-35 messages are embedded in segment data MatchesFilter \226\128\147 SCTE-35 messages which match the ScteFilter are embedded in segment data For DASH manifests, when set to All or MatchesFilter, an InbandEventStream tag signals that SCTE messages are present in segments. This setting works independently of manifest ad markers."];
      customAdTypes: CustomAdTypeList.t option
        [@ocaml.doc
          "A list of additional non-Ad SCTE-35 event types to treat as advertisements. When configured, events matching these types produce ad markers (such as SCTE35-OUT and SCTE35-IN in HLS DATERANGE tags) in manifests. Valid values: PROGRAM | CHAPTER | UNSCHEDULED_EVENT | ALTERNATE_CONTENT_OPPORTUNITY | NETWORK If you don't specify any values, the default is empty (only default ad types are used)."]}
    let make ?scteFilter =
      fun ?scteInSegments ->
        fun ?customAdTypes ->
          fun () -> { scteFilter; scteInSegments; customAdTypes }
    let to_value x =
      structure_to_value
        [("ScteFilter", (Option.map x.scteFilter ~f:ScteFilterList.to_value));
        ("ScteInSegments",
          (Option.map x.scteInSegments ~f:ScteInSegments.to_value));
        ("CustomAdTypes",
          (Option.map x.customAdTypes ~f:CustomAdTypeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customAdTypes =
        (Option.map ~f:CustomAdTypeList.of_xml)
          (Xml.child xml_arg0 "CustomAdTypes") in
      let scteInSegments =
        (Option.map ~f:ScteInSegments.of_xml)
          (Xml.child xml_arg0 "ScteInSegments") in
      let scteFilter =
        (Option.map ~f:ScteFilterList.of_xml)
          (Xml.child xml_arg0 "ScteFilter") in
      make ?customAdTypes ?scteInSegments ?scteFilter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let customAdTypes =
        field_map json__ "CustomAdTypes" CustomAdTypeList.of_json in
      let scteInSegments =
        field_map json__ "ScteInSegments" ScteInSegments.of_json in
      let scteFilter = field_map json__ "ScteFilter" ScteFilterList.of_json in
      make ?customAdTypes ?scteInSegments ?scteFilter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The SCTE configuration."]
module SegmentSegmentDurationSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:30) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for SegmentSegmentDurationSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SegmentSegmentNameString =
  struct
    type nonrec t = string
    let context_ = "SegmentSegmentNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SegmentSegmentNameString" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module ValidationExceptionType =
  struct
    type nonrec t =
      | CONTAINER_TYPE_IMMUTABLE 
      | INVALID_PAGINATION_TOKEN 
      | INVALID_PAGINATION_MAX_RESULTS 
      | INVALID_POLICY 
      | INVALID_ROLE_ARN 
      | MANIFEST_NAME_COLLISION 
      | ENCRYPTION_METHOD_CONTAINER_TYPE_MISMATCH 
      | CENC_IV_INCOMPATIBLE 
      | ENCRYPTION_CONTRACT_WITHOUT_AUDIO_RENDITION_INCOMPATIBLE 
      | ENCRYPTION_CONTRACT_WITH_ISM_CONTAINER_INCOMPATIBLE 
      | ENCRYPTION_CONTRACT_UNENCRYPTED 
      | ENCRYPTION_CONTRACT_SHARED 
      | NUM_MANIFESTS_LOW 
      | NUM_MANIFESTS_HIGH 
      | MANIFEST_DRM_SYSTEMS_INCOMPATIBLE 
      | DRM_SYSTEMS_ENCRYPTION_METHOD_INCOMPATIBLE 
      | ROLE_ARN_NOT_ASSUMABLE 
      | ROLE_ARN_LENGTH_OUT_OF_RANGE 
      | ROLE_ARN_INVALID_FORMAT 
      | URL_INVALID 
      | URL_SCHEME 
      | URL_USER_INFO 
      | URL_PORT 
      | URL_UNKNOWN_HOST 
      | URL_LOCAL_ADDRESS 
      | URL_LOOPBACK_ADDRESS 
      | URL_LINK_LOCAL_ADDRESS 
      | URL_MULTICAST_ADDRESS 
      | MEMBER_INVALID 
      | MEMBER_MISSING 
      | MEMBER_MIN_VALUE 
      | MEMBER_MAX_VALUE 
      | MEMBER_MIN_LENGTH 
      | MEMBER_MAX_LENGTH 
      | MEMBER_INVALID_ENUM_VALUE 
      | MEMBER_DOES_NOT_MATCH_PATTERN 
      | INVALID_MANIFEST_FILTER 
      | INVALID_DRM_SETTINGS 
      | INVALID_TIME_DELAY_SECONDS 
      | END_TIME_EARLIER_THAN_START_TIME 
      | TS_CONTAINER_TYPE_WITH_DASH_MANIFEST 
      | DIRECT_MODE_WITH_TIMING_SOURCE 
      | NONE_MODE_WITH_TIMING_SOURCE 
      | TIMING_SOURCE_MISSING 
      | UPDATE_PERIOD_SMALLER_THAN_SEGMENT_DURATION 
      | PERIOD_TRIGGERS_NONE_SPECIFIED_WITH_ADDITIONAL_VALUES 
      | DRM_SIGNALING_MISMATCH_SEGMENT_ENCRYPTION_STATUS 
      | ONLY_CMAF_INPUT_TYPE_ALLOW_FORCE_ENDPOINT_ERROR_CONFIGURATION 
      | SOURCE_DISRUPTIONS_ENABLED_INCORRECTLY 
      | HARVESTED_MANIFEST_HAS_START_END_FILTER_CONFIGURATION 
      | HARVESTED_MANIFEST_NOT_FOUND_ON_ENDPOINT 
      | TOO_MANY_IN_PROGRESS_HARVEST_JOBS 
      | HARVEST_JOB_INELIGIBLE_FOR_CANCELLATION 
      | INVALID_HARVEST_JOB_DURATION 
      | HARVEST_JOB_S3_DESTINATION_MISSING_OR_INCOMPLETE 
      | HARVEST_JOB_UNABLE_TO_WRITE_TO_S3_DESTINATION 
      | HARVEST_JOB_CUSTOMER_ENDPOINT_READ_ACCESS_DENIED 
      | CLIP_START_TIME_WITH_START_OR_END 
      | START_TAG_TIME_OFFSET_INVALID 
      | INCOMPATIBLE_DASH_PROFILE_DVB_DASH_CONFIGURATION 
      | DASH_DVB_ATTRIBUTES_WITHOUT_DVB_DASH_PROFILE 
      | INCOMPATIBLE_DASH_COMPACTNESS_CONFIGURATION 
      | INCOMPATIBLE_XML_ENCODING 
      | CMAF_EXCLUDE_SEGMENT_DRM_METADATA_INCOMPATIBLE_CONTAINER_TYPE 
      | ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_INPUT_SWITCHING 
      | ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_OUTPUT_CONFIGURATION 
      | ONLY_CMAF_INPUT_TYPE_ALLOW_PREFERRED_INPUT_CONFIGURATION 
      | TS_CONTAINER_TYPE_WITH_MSS_MANIFEST 
      | CMAF_CONTAINER_TYPE_WITH_MSS_MANIFEST 
      | ISM_CONTAINER_TYPE_WITH_HLS_MANIFEST 
      | ISM_CONTAINER_TYPE_WITH_LL_HLS_MANIFEST 
      | ISM_CONTAINER_TYPE_WITH_DASH_MANIFEST 
      | ISM_CONTAINER_TYPE_WITH_SCTE 
      | ISM_CONTAINER_WITH_KEY_ROTATION 
      | BATCH_GET_SECRET_VALUE_DENIED 
      | GET_SECRET_VALUE_DENIED 
      | DESCRIBE_SECRET_DENIED 
      | INVALID_SECRET_FORMAT 
      | SECRET_IS_NOT_ONE_KEY_VALUE_PAIR 
      | INVALID_SECRET_KEY 
      | INVALID_SECRET_VALUE 
      | SECRET_ARN_RESOURCE_NOT_FOUND 
      | DECRYPT_SECRET_FAILED 
      | TOO_MANY_SECRETS 
      | DUPLICATED_SECRET 
      | MALFORMED_SECRET_ARN 
      | SECRET_FROM_DIFFERENT_ACCOUNT 
      | SECRET_FROM_DIFFERENT_REGION 
      | INVALID_SECRET 
      | RESOURCE_NOT_IN_SAME_REGION 
      | CERTIFICATE_RESOURCE_NOT_FOUND 
      | CERTIFICATE_ACCESS_DENIED 
      | DESCRIBE_CERTIFICATE_FAILED 
      | INVALID_CERTIFICATE_STATUS 
      | INVALID_CERTIFICATE_KEY_ALGORITHM 
      | INVALID_CERTIFICATE_SIGNATURE_ALGORITHM 
      | MISSING_CERTIFICATE_DOMAIN_NAME 
      | INVALID_ARN 
      | SCTE_IN_MANIFESTS_INVALID_CONFIGURATION 
      | CUSTOM_AD_TYPES_INVALID_CONFIGURATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CONTAINER_TYPE_IMMUTABLE -> "CONTAINER_TYPE_IMMUTABLE"
      | INVALID_PAGINATION_TOKEN -> "INVALID_PAGINATION_TOKEN"
      | INVALID_PAGINATION_MAX_RESULTS -> "INVALID_PAGINATION_MAX_RESULTS"
      | INVALID_POLICY -> "INVALID_POLICY"
      | INVALID_ROLE_ARN -> "INVALID_ROLE_ARN"
      | MANIFEST_NAME_COLLISION -> "MANIFEST_NAME_COLLISION"
      | ENCRYPTION_METHOD_CONTAINER_TYPE_MISMATCH ->
          "ENCRYPTION_METHOD_CONTAINER_TYPE_MISMATCH"
      | CENC_IV_INCOMPATIBLE -> "CENC_IV_INCOMPATIBLE"
      | ENCRYPTION_CONTRACT_WITHOUT_AUDIO_RENDITION_INCOMPATIBLE ->
          "ENCRYPTION_CONTRACT_WITHOUT_AUDIO_RENDITION_INCOMPATIBLE"
      | ENCRYPTION_CONTRACT_WITH_ISM_CONTAINER_INCOMPATIBLE ->
          "ENCRYPTION_CONTRACT_WITH_ISM_CONTAINER_INCOMPATIBLE"
      | ENCRYPTION_CONTRACT_UNENCRYPTED -> "ENCRYPTION_CONTRACT_UNENCRYPTED"
      | ENCRYPTION_CONTRACT_SHARED -> "ENCRYPTION_CONTRACT_SHARED"
      | NUM_MANIFESTS_LOW -> "NUM_MANIFESTS_LOW"
      | NUM_MANIFESTS_HIGH -> "NUM_MANIFESTS_HIGH"
      | MANIFEST_DRM_SYSTEMS_INCOMPATIBLE ->
          "MANIFEST_DRM_SYSTEMS_INCOMPATIBLE"
      | DRM_SYSTEMS_ENCRYPTION_METHOD_INCOMPATIBLE ->
          "DRM_SYSTEMS_ENCRYPTION_METHOD_INCOMPATIBLE"
      | ROLE_ARN_NOT_ASSUMABLE -> "ROLE_ARN_NOT_ASSUMABLE"
      | ROLE_ARN_LENGTH_OUT_OF_RANGE -> "ROLE_ARN_LENGTH_OUT_OF_RANGE"
      | ROLE_ARN_INVALID_FORMAT -> "ROLE_ARN_INVALID_FORMAT"
      | URL_INVALID -> "URL_INVALID"
      | URL_SCHEME -> "URL_SCHEME"
      | URL_USER_INFO -> "URL_USER_INFO"
      | URL_PORT -> "URL_PORT"
      | URL_UNKNOWN_HOST -> "URL_UNKNOWN_HOST"
      | URL_LOCAL_ADDRESS -> "URL_LOCAL_ADDRESS"
      | URL_LOOPBACK_ADDRESS -> "URL_LOOPBACK_ADDRESS"
      | URL_LINK_LOCAL_ADDRESS -> "URL_LINK_LOCAL_ADDRESS"
      | URL_MULTICAST_ADDRESS -> "URL_MULTICAST_ADDRESS"
      | MEMBER_INVALID -> "MEMBER_INVALID"
      | MEMBER_MISSING -> "MEMBER_MISSING"
      | MEMBER_MIN_VALUE -> "MEMBER_MIN_VALUE"
      | MEMBER_MAX_VALUE -> "MEMBER_MAX_VALUE"
      | MEMBER_MIN_LENGTH -> "MEMBER_MIN_LENGTH"
      | MEMBER_MAX_LENGTH -> "MEMBER_MAX_LENGTH"
      | MEMBER_INVALID_ENUM_VALUE -> "MEMBER_INVALID_ENUM_VALUE"
      | MEMBER_DOES_NOT_MATCH_PATTERN -> "MEMBER_DOES_NOT_MATCH_PATTERN"
      | INVALID_MANIFEST_FILTER -> "INVALID_MANIFEST_FILTER"
      | INVALID_DRM_SETTINGS -> "INVALID_DRM_SETTINGS"
      | INVALID_TIME_DELAY_SECONDS -> "INVALID_TIME_DELAY_SECONDS"
      | END_TIME_EARLIER_THAN_START_TIME ->
          "END_TIME_EARLIER_THAN_START_TIME"
      | TS_CONTAINER_TYPE_WITH_DASH_MANIFEST ->
          "TS_CONTAINER_TYPE_WITH_DASH_MANIFEST"
      | DIRECT_MODE_WITH_TIMING_SOURCE -> "DIRECT_MODE_WITH_TIMING_SOURCE"
      | NONE_MODE_WITH_TIMING_SOURCE -> "NONE_MODE_WITH_TIMING_SOURCE"
      | TIMING_SOURCE_MISSING -> "TIMING_SOURCE_MISSING"
      | UPDATE_PERIOD_SMALLER_THAN_SEGMENT_DURATION ->
          "UPDATE_PERIOD_SMALLER_THAN_SEGMENT_DURATION"
      | PERIOD_TRIGGERS_NONE_SPECIFIED_WITH_ADDITIONAL_VALUES ->
          "PERIOD_TRIGGERS_NONE_SPECIFIED_WITH_ADDITIONAL_VALUES"
      | DRM_SIGNALING_MISMATCH_SEGMENT_ENCRYPTION_STATUS ->
          "DRM_SIGNALING_MISMATCH_SEGMENT_ENCRYPTION_STATUS"
      | ONLY_CMAF_INPUT_TYPE_ALLOW_FORCE_ENDPOINT_ERROR_CONFIGURATION ->
          "ONLY_CMAF_INPUT_TYPE_ALLOW_FORCE_ENDPOINT_ERROR_CONFIGURATION"
      | SOURCE_DISRUPTIONS_ENABLED_INCORRECTLY ->
          "SOURCE_DISRUPTIONS_ENABLED_INCORRECTLY"
      | HARVESTED_MANIFEST_HAS_START_END_FILTER_CONFIGURATION ->
          "HARVESTED_MANIFEST_HAS_START_END_FILTER_CONFIGURATION"
      | HARVESTED_MANIFEST_NOT_FOUND_ON_ENDPOINT ->
          "HARVESTED_MANIFEST_NOT_FOUND_ON_ENDPOINT"
      | TOO_MANY_IN_PROGRESS_HARVEST_JOBS ->
          "TOO_MANY_IN_PROGRESS_HARVEST_JOBS"
      | HARVEST_JOB_INELIGIBLE_FOR_CANCELLATION ->
          "HARVEST_JOB_INELIGIBLE_FOR_CANCELLATION"
      | INVALID_HARVEST_JOB_DURATION -> "INVALID_HARVEST_JOB_DURATION"
      | HARVEST_JOB_S3_DESTINATION_MISSING_OR_INCOMPLETE ->
          "HARVEST_JOB_S3_DESTINATION_MISSING_OR_INCOMPLETE"
      | HARVEST_JOB_UNABLE_TO_WRITE_TO_S3_DESTINATION ->
          "HARVEST_JOB_UNABLE_TO_WRITE_TO_S3_DESTINATION"
      | HARVEST_JOB_CUSTOMER_ENDPOINT_READ_ACCESS_DENIED ->
          "HARVEST_JOB_CUSTOMER_ENDPOINT_READ_ACCESS_DENIED"
      | CLIP_START_TIME_WITH_START_OR_END ->
          "CLIP_START_TIME_WITH_START_OR_END"
      | START_TAG_TIME_OFFSET_INVALID -> "START_TAG_TIME_OFFSET_INVALID"
      | INCOMPATIBLE_DASH_PROFILE_DVB_DASH_CONFIGURATION ->
          "INCOMPATIBLE_DASH_PROFILE_DVB_DASH_CONFIGURATION"
      | DASH_DVB_ATTRIBUTES_WITHOUT_DVB_DASH_PROFILE ->
          "DASH_DVB_ATTRIBUTES_WITHOUT_DVB_DASH_PROFILE"
      | INCOMPATIBLE_DASH_COMPACTNESS_CONFIGURATION ->
          "INCOMPATIBLE_DASH_COMPACTNESS_CONFIGURATION"
      | INCOMPATIBLE_XML_ENCODING -> "INCOMPATIBLE_XML_ENCODING"
      | CMAF_EXCLUDE_SEGMENT_DRM_METADATA_INCOMPATIBLE_CONTAINER_TYPE ->
          "CMAF_EXCLUDE_SEGMENT_DRM_METADATA_INCOMPATIBLE_CONTAINER_TYPE"
      | ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_INPUT_SWITCHING ->
          "ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_INPUT_SWITCHING"
      | ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_OUTPUT_CONFIGURATION ->
          "ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_OUTPUT_CONFIGURATION"
      | ONLY_CMAF_INPUT_TYPE_ALLOW_PREFERRED_INPUT_CONFIGURATION ->
          "ONLY_CMAF_INPUT_TYPE_ALLOW_PREFERRED_INPUT_CONFIGURATION"
      | TS_CONTAINER_TYPE_WITH_MSS_MANIFEST ->
          "TS_CONTAINER_TYPE_WITH_MSS_MANIFEST"
      | CMAF_CONTAINER_TYPE_WITH_MSS_MANIFEST ->
          "CMAF_CONTAINER_TYPE_WITH_MSS_MANIFEST"
      | ISM_CONTAINER_TYPE_WITH_HLS_MANIFEST ->
          "ISM_CONTAINER_TYPE_WITH_HLS_MANIFEST"
      | ISM_CONTAINER_TYPE_WITH_LL_HLS_MANIFEST ->
          "ISM_CONTAINER_TYPE_WITH_LL_HLS_MANIFEST"
      | ISM_CONTAINER_TYPE_WITH_DASH_MANIFEST ->
          "ISM_CONTAINER_TYPE_WITH_DASH_MANIFEST"
      | ISM_CONTAINER_TYPE_WITH_SCTE -> "ISM_CONTAINER_TYPE_WITH_SCTE"
      | ISM_CONTAINER_WITH_KEY_ROTATION -> "ISM_CONTAINER_WITH_KEY_ROTATION"
      | BATCH_GET_SECRET_VALUE_DENIED -> "BATCH_GET_SECRET_VALUE_DENIED"
      | GET_SECRET_VALUE_DENIED -> "GET_SECRET_VALUE_DENIED"
      | DESCRIBE_SECRET_DENIED -> "DESCRIBE_SECRET_DENIED"
      | INVALID_SECRET_FORMAT -> "INVALID_SECRET_FORMAT"
      | SECRET_IS_NOT_ONE_KEY_VALUE_PAIR ->
          "SECRET_IS_NOT_ONE_KEY_VALUE_PAIR"
      | INVALID_SECRET_KEY -> "INVALID_SECRET_KEY"
      | INVALID_SECRET_VALUE -> "INVALID_SECRET_VALUE"
      | SECRET_ARN_RESOURCE_NOT_FOUND -> "SECRET_ARN_RESOURCE_NOT_FOUND"
      | DECRYPT_SECRET_FAILED -> "DECRYPT_SECRET_FAILED"
      | TOO_MANY_SECRETS -> "TOO_MANY_SECRETS"
      | DUPLICATED_SECRET -> "DUPLICATED_SECRET"
      | MALFORMED_SECRET_ARN -> "MALFORMED_SECRET_ARN"
      | SECRET_FROM_DIFFERENT_ACCOUNT -> "SECRET_FROM_DIFFERENT_ACCOUNT"
      | SECRET_FROM_DIFFERENT_REGION -> "SECRET_FROM_DIFFERENT_REGION"
      | INVALID_SECRET -> "INVALID_SECRET"
      | RESOURCE_NOT_IN_SAME_REGION -> "RESOURCE_NOT_IN_SAME_REGION"
      | CERTIFICATE_RESOURCE_NOT_FOUND -> "CERTIFICATE_RESOURCE_NOT_FOUND"
      | CERTIFICATE_ACCESS_DENIED -> "CERTIFICATE_ACCESS_DENIED"
      | DESCRIBE_CERTIFICATE_FAILED -> "DESCRIBE_CERTIFICATE_FAILED"
      | INVALID_CERTIFICATE_STATUS -> "INVALID_CERTIFICATE_STATUS"
      | INVALID_CERTIFICATE_KEY_ALGORITHM ->
          "INVALID_CERTIFICATE_KEY_ALGORITHM"
      | INVALID_CERTIFICATE_SIGNATURE_ALGORITHM ->
          "INVALID_CERTIFICATE_SIGNATURE_ALGORITHM"
      | MISSING_CERTIFICATE_DOMAIN_NAME -> "MISSING_CERTIFICATE_DOMAIN_NAME"
      | INVALID_ARN -> "INVALID_ARN"
      | SCTE_IN_MANIFESTS_INVALID_CONFIGURATION ->
          "SCTE_IN_MANIFESTS_INVALID_CONFIGURATION"
      | CUSTOM_AD_TYPES_INVALID_CONFIGURATION ->
          "CUSTOM_AD_TYPES_INVALID_CONFIGURATION"
      | Non_static_id s -> s
    let of_string =
      function
      | "CONTAINER_TYPE_IMMUTABLE" -> CONTAINER_TYPE_IMMUTABLE
      | "INVALID_PAGINATION_TOKEN" -> INVALID_PAGINATION_TOKEN
      | "INVALID_PAGINATION_MAX_RESULTS" -> INVALID_PAGINATION_MAX_RESULTS
      | "INVALID_POLICY" -> INVALID_POLICY
      | "INVALID_ROLE_ARN" -> INVALID_ROLE_ARN
      | "MANIFEST_NAME_COLLISION" -> MANIFEST_NAME_COLLISION
      | "ENCRYPTION_METHOD_CONTAINER_TYPE_MISMATCH" ->
          ENCRYPTION_METHOD_CONTAINER_TYPE_MISMATCH
      | "CENC_IV_INCOMPATIBLE" -> CENC_IV_INCOMPATIBLE
      | "ENCRYPTION_CONTRACT_WITHOUT_AUDIO_RENDITION_INCOMPATIBLE" ->
          ENCRYPTION_CONTRACT_WITHOUT_AUDIO_RENDITION_INCOMPATIBLE
      | "ENCRYPTION_CONTRACT_WITH_ISM_CONTAINER_INCOMPATIBLE" ->
          ENCRYPTION_CONTRACT_WITH_ISM_CONTAINER_INCOMPATIBLE
      | "ENCRYPTION_CONTRACT_UNENCRYPTED" -> ENCRYPTION_CONTRACT_UNENCRYPTED
      | "ENCRYPTION_CONTRACT_SHARED" -> ENCRYPTION_CONTRACT_SHARED
      | "NUM_MANIFESTS_LOW" -> NUM_MANIFESTS_LOW
      | "NUM_MANIFESTS_HIGH" -> NUM_MANIFESTS_HIGH
      | "MANIFEST_DRM_SYSTEMS_INCOMPATIBLE" ->
          MANIFEST_DRM_SYSTEMS_INCOMPATIBLE
      | "DRM_SYSTEMS_ENCRYPTION_METHOD_INCOMPATIBLE" ->
          DRM_SYSTEMS_ENCRYPTION_METHOD_INCOMPATIBLE
      | "ROLE_ARN_NOT_ASSUMABLE" -> ROLE_ARN_NOT_ASSUMABLE
      | "ROLE_ARN_LENGTH_OUT_OF_RANGE" -> ROLE_ARN_LENGTH_OUT_OF_RANGE
      | "ROLE_ARN_INVALID_FORMAT" -> ROLE_ARN_INVALID_FORMAT
      | "URL_INVALID" -> URL_INVALID
      | "URL_SCHEME" -> URL_SCHEME
      | "URL_USER_INFO" -> URL_USER_INFO
      | "URL_PORT" -> URL_PORT
      | "URL_UNKNOWN_HOST" -> URL_UNKNOWN_HOST
      | "URL_LOCAL_ADDRESS" -> URL_LOCAL_ADDRESS
      | "URL_LOOPBACK_ADDRESS" -> URL_LOOPBACK_ADDRESS
      | "URL_LINK_LOCAL_ADDRESS" -> URL_LINK_LOCAL_ADDRESS
      | "URL_MULTICAST_ADDRESS" -> URL_MULTICAST_ADDRESS
      | "MEMBER_INVALID" -> MEMBER_INVALID
      | "MEMBER_MISSING" -> MEMBER_MISSING
      | "MEMBER_MIN_VALUE" -> MEMBER_MIN_VALUE
      | "MEMBER_MAX_VALUE" -> MEMBER_MAX_VALUE
      | "MEMBER_MIN_LENGTH" -> MEMBER_MIN_LENGTH
      | "MEMBER_MAX_LENGTH" -> MEMBER_MAX_LENGTH
      | "MEMBER_INVALID_ENUM_VALUE" -> MEMBER_INVALID_ENUM_VALUE
      | "MEMBER_DOES_NOT_MATCH_PATTERN" -> MEMBER_DOES_NOT_MATCH_PATTERN
      | "INVALID_MANIFEST_FILTER" -> INVALID_MANIFEST_FILTER
      | "INVALID_DRM_SETTINGS" -> INVALID_DRM_SETTINGS
      | "INVALID_TIME_DELAY_SECONDS" -> INVALID_TIME_DELAY_SECONDS
      | "END_TIME_EARLIER_THAN_START_TIME" ->
          END_TIME_EARLIER_THAN_START_TIME
      | "TS_CONTAINER_TYPE_WITH_DASH_MANIFEST" ->
          TS_CONTAINER_TYPE_WITH_DASH_MANIFEST
      | "DIRECT_MODE_WITH_TIMING_SOURCE" -> DIRECT_MODE_WITH_TIMING_SOURCE
      | "NONE_MODE_WITH_TIMING_SOURCE" -> NONE_MODE_WITH_TIMING_SOURCE
      | "TIMING_SOURCE_MISSING" -> TIMING_SOURCE_MISSING
      | "UPDATE_PERIOD_SMALLER_THAN_SEGMENT_DURATION" ->
          UPDATE_PERIOD_SMALLER_THAN_SEGMENT_DURATION
      | "PERIOD_TRIGGERS_NONE_SPECIFIED_WITH_ADDITIONAL_VALUES" ->
          PERIOD_TRIGGERS_NONE_SPECIFIED_WITH_ADDITIONAL_VALUES
      | "DRM_SIGNALING_MISMATCH_SEGMENT_ENCRYPTION_STATUS" ->
          DRM_SIGNALING_MISMATCH_SEGMENT_ENCRYPTION_STATUS
      | "ONLY_CMAF_INPUT_TYPE_ALLOW_FORCE_ENDPOINT_ERROR_CONFIGURATION" ->
          ONLY_CMAF_INPUT_TYPE_ALLOW_FORCE_ENDPOINT_ERROR_CONFIGURATION
      | "SOURCE_DISRUPTIONS_ENABLED_INCORRECTLY" ->
          SOURCE_DISRUPTIONS_ENABLED_INCORRECTLY
      | "HARVESTED_MANIFEST_HAS_START_END_FILTER_CONFIGURATION" ->
          HARVESTED_MANIFEST_HAS_START_END_FILTER_CONFIGURATION
      | "HARVESTED_MANIFEST_NOT_FOUND_ON_ENDPOINT" ->
          HARVESTED_MANIFEST_NOT_FOUND_ON_ENDPOINT
      | "TOO_MANY_IN_PROGRESS_HARVEST_JOBS" ->
          TOO_MANY_IN_PROGRESS_HARVEST_JOBS
      | "HARVEST_JOB_INELIGIBLE_FOR_CANCELLATION" ->
          HARVEST_JOB_INELIGIBLE_FOR_CANCELLATION
      | "INVALID_HARVEST_JOB_DURATION" -> INVALID_HARVEST_JOB_DURATION
      | "HARVEST_JOB_S3_DESTINATION_MISSING_OR_INCOMPLETE" ->
          HARVEST_JOB_S3_DESTINATION_MISSING_OR_INCOMPLETE
      | "HARVEST_JOB_UNABLE_TO_WRITE_TO_S3_DESTINATION" ->
          HARVEST_JOB_UNABLE_TO_WRITE_TO_S3_DESTINATION
      | "HARVEST_JOB_CUSTOMER_ENDPOINT_READ_ACCESS_DENIED" ->
          HARVEST_JOB_CUSTOMER_ENDPOINT_READ_ACCESS_DENIED
      | "CLIP_START_TIME_WITH_START_OR_END" ->
          CLIP_START_TIME_WITH_START_OR_END
      | "START_TAG_TIME_OFFSET_INVALID" -> START_TAG_TIME_OFFSET_INVALID
      | "INCOMPATIBLE_DASH_PROFILE_DVB_DASH_CONFIGURATION" ->
          INCOMPATIBLE_DASH_PROFILE_DVB_DASH_CONFIGURATION
      | "DASH_DVB_ATTRIBUTES_WITHOUT_DVB_DASH_PROFILE" ->
          DASH_DVB_ATTRIBUTES_WITHOUT_DVB_DASH_PROFILE
      | "INCOMPATIBLE_DASH_COMPACTNESS_CONFIGURATION" ->
          INCOMPATIBLE_DASH_COMPACTNESS_CONFIGURATION
      | "INCOMPATIBLE_XML_ENCODING" -> INCOMPATIBLE_XML_ENCODING
      | "CMAF_EXCLUDE_SEGMENT_DRM_METADATA_INCOMPATIBLE_CONTAINER_TYPE" ->
          CMAF_EXCLUDE_SEGMENT_DRM_METADATA_INCOMPATIBLE_CONTAINER_TYPE
      | "ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_INPUT_SWITCHING" ->
          ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_INPUT_SWITCHING
      | "ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_OUTPUT_CONFIGURATION" ->
          ONLY_CMAF_INPUT_TYPE_ALLOW_MQCS_OUTPUT_CONFIGURATION
      | "ONLY_CMAF_INPUT_TYPE_ALLOW_PREFERRED_INPUT_CONFIGURATION" ->
          ONLY_CMAF_INPUT_TYPE_ALLOW_PREFERRED_INPUT_CONFIGURATION
      | "TS_CONTAINER_TYPE_WITH_MSS_MANIFEST" ->
          TS_CONTAINER_TYPE_WITH_MSS_MANIFEST
      | "CMAF_CONTAINER_TYPE_WITH_MSS_MANIFEST" ->
          CMAF_CONTAINER_TYPE_WITH_MSS_MANIFEST
      | "ISM_CONTAINER_TYPE_WITH_HLS_MANIFEST" ->
          ISM_CONTAINER_TYPE_WITH_HLS_MANIFEST
      | "ISM_CONTAINER_TYPE_WITH_LL_HLS_MANIFEST" ->
          ISM_CONTAINER_TYPE_WITH_LL_HLS_MANIFEST
      | "ISM_CONTAINER_TYPE_WITH_DASH_MANIFEST" ->
          ISM_CONTAINER_TYPE_WITH_DASH_MANIFEST
      | "ISM_CONTAINER_TYPE_WITH_SCTE" -> ISM_CONTAINER_TYPE_WITH_SCTE
      | "ISM_CONTAINER_WITH_KEY_ROTATION" -> ISM_CONTAINER_WITH_KEY_ROTATION
      | "BATCH_GET_SECRET_VALUE_DENIED" -> BATCH_GET_SECRET_VALUE_DENIED
      | "GET_SECRET_VALUE_DENIED" -> GET_SECRET_VALUE_DENIED
      | "DESCRIBE_SECRET_DENIED" -> DESCRIBE_SECRET_DENIED
      | "INVALID_SECRET_FORMAT" -> INVALID_SECRET_FORMAT
      | "SECRET_IS_NOT_ONE_KEY_VALUE_PAIR" ->
          SECRET_IS_NOT_ONE_KEY_VALUE_PAIR
      | "INVALID_SECRET_KEY" -> INVALID_SECRET_KEY
      | "INVALID_SECRET_VALUE" -> INVALID_SECRET_VALUE
      | "SECRET_ARN_RESOURCE_NOT_FOUND" -> SECRET_ARN_RESOURCE_NOT_FOUND
      | "DECRYPT_SECRET_FAILED" -> DECRYPT_SECRET_FAILED
      | "TOO_MANY_SECRETS" -> TOO_MANY_SECRETS
      | "DUPLICATED_SECRET" -> DUPLICATED_SECRET
      | "MALFORMED_SECRET_ARN" -> MALFORMED_SECRET_ARN
      | "SECRET_FROM_DIFFERENT_ACCOUNT" -> SECRET_FROM_DIFFERENT_ACCOUNT
      | "SECRET_FROM_DIFFERENT_REGION" -> SECRET_FROM_DIFFERENT_REGION
      | "INVALID_SECRET" -> INVALID_SECRET
      | "RESOURCE_NOT_IN_SAME_REGION" -> RESOURCE_NOT_IN_SAME_REGION
      | "CERTIFICATE_RESOURCE_NOT_FOUND" -> CERTIFICATE_RESOURCE_NOT_FOUND
      | "CERTIFICATE_ACCESS_DENIED" -> CERTIFICATE_ACCESS_DENIED
      | "DESCRIBE_CERTIFICATE_FAILED" -> DESCRIBE_CERTIFICATE_FAILED
      | "INVALID_CERTIFICATE_STATUS" -> INVALID_CERTIFICATE_STATUS
      | "INVALID_CERTIFICATE_KEY_ALGORITHM" ->
          INVALID_CERTIFICATE_KEY_ALGORITHM
      | "INVALID_CERTIFICATE_SIGNATURE_ALGORITHM" ->
          INVALID_CERTIFICATE_SIGNATURE_ALGORITHM
      | "MISSING_CERTIFICATE_DOMAIN_NAME" -> MISSING_CERTIFICATE_DOMAIN_NAME
      | "INVALID_ARN" -> INVALID_ARN
      | "SCTE_IN_MANIFESTS_INVALID_CONFIGURATION" ->
          SCTE_IN_MANIFESTS_INVALID_CONFIGURATION
      | "CUSTOM_AD_TYPES_INVALID_CONFIGURATION" ->
          CUSTOM_AD_TYPES_INVALID_CONFIGURATION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionType" j)
    let to_json = simple_to_json to_value
  end
module CreateDashManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ManifestName.t
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The child manifest name creates a unique path to this endpoint."];
      manifestWindowSeconds:
        CreateDashManifestConfigurationManifestWindowSecondsInteger.t option
        [@ocaml.doc
          "The total duration (in seconds) of the manifest's content."];
      filterConfiguration: FilterConfiguration.t option ;
      minUpdatePeriodSeconds:
        CreateDashManifestConfigurationMinUpdatePeriodSecondsInteger.t option
        [@ocaml.doc
          "Minimum amount of time (in seconds) that the player should wait before requesting updates to the manifest."];
      minBufferTimeSeconds:
        CreateDashManifestConfigurationMinBufferTimeSecondsInteger.t option
        [@ocaml.doc
          "Minimum amount of content (in seconds) that a player must keep available in the buffer."];
      suggestedPresentationDelaySeconds:
        CreateDashManifestConfigurationSuggestedPresentationDelaySecondsInteger.t
          option
        [@ocaml.doc
          "The amount of time (in seconds) that the player should be from the end of the manifest."];
      segmentTemplateFormat: DashSegmentTemplateFormat.t option
        [@ocaml.doc
          "Determines the type of variable used in the media URL of the SegmentTemplate tag in the manifest. Also specifies if segment timeline information is included in SegmentTimeline or SegmentTemplate. Value description: NUMBER_WITH_TIMELINE - The $Number$ variable is used in the media URL. The value of this variable is the sequential number of the segment. A full SegmentTimeline object is presented in each SegmentTemplate."];
      periodTriggers: DashPeriodTriggers.t option
        [@ocaml.doc
          "A list of triggers that controls when AWS Elemental MediaPackage separates the MPEG-DASH manifest into multiple periods. Type ADS to indicate that AWS Elemental MediaPackage must create periods in the output manifest that correspond to SCTE-35 ad markers in the input source. Leave this value empty to indicate that the manifest is contained all in one period. For more information about periods in the DASH manifest, see Multi-period DASH in AWS Elemental MediaPackage."];
      scteDash: ScteDash.t option [@ocaml.doc "The SCTE configuration."];
      drmSignaling: DashDrmSignaling.t option
        [@ocaml.doc
          "Determines how the DASH manifest signals the DRM content."];
      utcTiming: DashUtcTiming.t option
        [@ocaml.doc
          "Determines the type of UTC timing included in the DASH Media Presentation Description (MPD)."];
      profiles: DashProfiles.t option
        [@ocaml.doc "The profile that the output is compliant with."];
      baseUrls: DashBaseUrls.t option
        [@ocaml.doc "The base URLs to use for retrieving segments."];
      programInformation: DashProgramInformation.t option
        [@ocaml.doc
          "Details about the content that you want MediaPackage to pass through in the manifest to the playback device."];
      dvbSettings: DashDvbSettings.t option
        [@ocaml.doc
          "For endpoints that use the DVB-DASH profile only. The font download and error reporting information that you want MediaPackage to pass through to the manifest."];
      compactness: DashCompactness.t option
        [@ocaml.doc
          "The layout of the DASH manifest that MediaPackage produces. STANDARD indicates a default manifest, which is compacted. NONE indicates a full manifest. For information about compactness, see DASH manifest compactness in the Elemental MediaPackage v2 User Guide."];
      subtitleConfiguration: DashSubtitleConfiguration.t option
        [@ocaml.doc "The configuration for DASH subtitles."];
      uriPathType: UriPathType.t option
        [@ocaml.doc
          "The type of path to use in manifest URIs. LEAF uses leaf-relative paths (for example, index_1.mpd). ROOT uses root-relative paths that include the full path from root (for example, /out/v1/channel-group/channel/endpoint/index_1.mpd). If you don't specify a value, the default is LEAF."];
      availabilityStartTimeConfiguration:
        DashAvailabilityStartTimeConfiguration.t option
        [@ocaml.doc
          "The configuration for the DASH availabilityStartTime attribute of the Media Presentation Description (MPD). If you don't specify a value, MediaPackage uses the default availability start time of 2024-01-01T00:00:00Z."]}
    let context_ = "CreateDashManifestConfiguration"
    let make ?manifestWindowSeconds =
      fun ?filterConfiguration ->
        fun ?minUpdatePeriodSeconds ->
          fun ?minBufferTimeSeconds ->
            fun ?suggestedPresentationDelaySeconds ->
              fun ?segmentTemplateFormat ->
                fun ?periodTriggers ->
                  fun ?scteDash ->
                    fun ?drmSignaling ->
                      fun ?utcTiming ->
                        fun ?profiles ->
                          fun ?baseUrls ->
                            fun ?programInformation ->
                              fun ?dvbSettings ->
                                fun ?compactness ->
                                  fun ?subtitleConfiguration ->
                                    fun ?uriPathType ->
                                      fun ?availabilityStartTimeConfiguration
                                        ->
                                        fun ~manifestName ->
                                          fun () ->
                                            {
                                              manifestWindowSeconds;
                                              filterConfiguration;
                                              minUpdatePeriodSeconds;
                                              minBufferTimeSeconds;
                                              suggestedPresentationDelaySeconds;
                                              segmentTemplateFormat;
                                              periodTriggers;
                                              scteDash;
                                              drmSignaling;
                                              utcTiming;
                                              profiles;
                                              baseUrls;
                                              programInformation;
                                              dvbSettings;
                                              compactness;
                                              subtitleConfiguration;
                                              uriPathType;
                                              availabilityStartTimeConfiguration;
                                              manifestName
                                            }
    let to_value x =
      structure_to_value
        [("ManifestName", (Some (ManifestName.to_value x.manifestName)));
        ("ManifestWindowSeconds",
          (Option.map x.manifestWindowSeconds
             ~f:CreateDashManifestConfigurationManifestWindowSecondsInteger.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("MinUpdatePeriodSeconds",
          (Option.map x.minUpdatePeriodSeconds
             ~f:CreateDashManifestConfigurationMinUpdatePeriodSecondsInteger.to_value));
        ("MinBufferTimeSeconds",
          (Option.map x.minBufferTimeSeconds
             ~f:CreateDashManifestConfigurationMinBufferTimeSecondsInteger.to_value));
        ("SuggestedPresentationDelaySeconds",
          (Option.map x.suggestedPresentationDelaySeconds
             ~f:CreateDashManifestConfigurationSuggestedPresentationDelaySecondsInteger.to_value));
        ("SegmentTemplateFormat",
          (Option.map x.segmentTemplateFormat
             ~f:DashSegmentTemplateFormat.to_value));
        ("PeriodTriggers",
          (Option.map x.periodTriggers ~f:DashPeriodTriggers.to_value));
        ("ScteDash", (Option.map x.scteDash ~f:ScteDash.to_value));
        ("DrmSignaling",
          (Option.map x.drmSignaling ~f:DashDrmSignaling.to_value));
        ("UtcTiming", (Option.map x.utcTiming ~f:DashUtcTiming.to_value));
        ("Profiles", (Option.map x.profiles ~f:DashProfiles.to_value));
        ("BaseUrls", (Option.map x.baseUrls ~f:DashBaseUrls.to_value));
        ("ProgramInformation",
          (Option.map x.programInformation ~f:DashProgramInformation.to_value));
        ("DvbSettings",
          (Option.map x.dvbSettings ~f:DashDvbSettings.to_value));
        ("Compactness",
          (Option.map x.compactness ~f:DashCompactness.to_value));
        ("SubtitleConfiguration",
          (Option.map x.subtitleConfiguration
             ~f:DashSubtitleConfiguration.to_value));
        ("UriPathType", (Option.map x.uriPathType ~f:UriPathType.to_value));
        ("AvailabilityStartTimeConfiguration",
          (Option.map x.availabilityStartTimeConfiguration
             ~f:DashAvailabilityStartTimeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityStartTimeConfiguration =
        (Option.map ~f:DashAvailabilityStartTimeConfiguration.of_xml)
          (Xml.child xml_arg0 "AvailabilityStartTimeConfiguration") in
      let uriPathType =
        (Option.map ~f:UriPathType.of_xml) (Xml.child xml_arg0 "UriPathType") in
      let subtitleConfiguration =
        (Option.map ~f:DashSubtitleConfiguration.of_xml)
          (Xml.child xml_arg0 "SubtitleConfiguration") in
      let compactness =
        (Option.map ~f:DashCompactness.of_xml)
          (Xml.child xml_arg0 "Compactness") in
      let dvbSettings =
        (Option.map ~f:DashDvbSettings.of_xml)
          (Xml.child xml_arg0 "DvbSettings") in
      let programInformation =
        (Option.map ~f:DashProgramInformation.of_xml)
          (Xml.child xml_arg0 "ProgramInformation") in
      let baseUrls =
        (Option.map ~f:DashBaseUrls.of_xml) (Xml.child xml_arg0 "BaseUrls") in
      let profiles =
        (Option.map ~f:DashProfiles.of_xml) (Xml.child xml_arg0 "Profiles") in
      let utcTiming =
        (Option.map ~f:DashUtcTiming.of_xml) (Xml.child xml_arg0 "UtcTiming") in
      let drmSignaling =
        (Option.map ~f:DashDrmSignaling.of_xml)
          (Xml.child xml_arg0 "DrmSignaling") in
      let scteDash =
        (Option.map ~f:ScteDash.of_xml) (Xml.child xml_arg0 "ScteDash") in
      let periodTriggers =
        (Option.map ~f:DashPeriodTriggers.of_xml)
          (Xml.child xml_arg0 "PeriodTriggers") in
      let segmentTemplateFormat =
        (Option.map ~f:DashSegmentTemplateFormat.of_xml)
          (Xml.child xml_arg0 "SegmentTemplateFormat") in
      let suggestedPresentationDelaySeconds =
        (Option.map
           ~f:CreateDashManifestConfigurationSuggestedPresentationDelaySecondsInteger.of_xml)
          (Xml.child xml_arg0 "SuggestedPresentationDelaySeconds") in
      let minBufferTimeSeconds =
        (Option.map
           ~f:CreateDashManifestConfigurationMinBufferTimeSecondsInteger.of_xml)
          (Xml.child xml_arg0 "MinBufferTimeSeconds") in
      let minUpdatePeriodSeconds =
        (Option.map
           ~f:CreateDashManifestConfigurationMinUpdatePeriodSecondsInteger.of_xml)
          (Xml.child xml_arg0 "MinUpdatePeriodSeconds") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let manifestWindowSeconds =
        (Option.map
           ~f:CreateDashManifestConfigurationManifestWindowSecondsInteger.of_xml)
          (Xml.child xml_arg0 "ManifestWindowSeconds") in
      let manifestName =
        ManifestName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ManifestName") in
      make ?availabilityStartTimeConfiguration ?uriPathType
        ?subtitleConfiguration ?compactness ?dvbSettings ?programInformation
        ?baseUrls ?profiles ?utcTiming ?drmSignaling ?scteDash
        ?periodTriggers ?segmentTemplateFormat
        ?suggestedPresentationDelaySeconds ?minBufferTimeSeconds
        ?minUpdatePeriodSeconds ?filterConfiguration ?manifestWindowSeconds
        ~manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityStartTimeConfiguration =
        field_map json__ "AvailabilityStartTimeConfiguration"
          DashAvailabilityStartTimeConfiguration.of_json in
      let uriPathType = field_map json__ "UriPathType" UriPathType.of_json in
      let subtitleConfiguration =
        field_map json__ "SubtitleConfiguration"
          DashSubtitleConfiguration.of_json in
      let compactness =
        field_map json__ "Compactness" DashCompactness.of_json in
      let dvbSettings =
        field_map json__ "DvbSettings" DashDvbSettings.of_json in
      let programInformation =
        field_map json__ "ProgramInformation" DashProgramInformation.of_json in
      let baseUrls = field_map json__ "BaseUrls" DashBaseUrls.of_json in
      let profiles = field_map json__ "Profiles" DashProfiles.of_json in
      let utcTiming = field_map json__ "UtcTiming" DashUtcTiming.of_json in
      let drmSignaling =
        field_map json__ "DrmSignaling" DashDrmSignaling.of_json in
      let scteDash = field_map json__ "ScteDash" ScteDash.of_json in
      let periodTriggers =
        field_map json__ "PeriodTriggers" DashPeriodTriggers.of_json in
      let segmentTemplateFormat =
        field_map json__ "SegmentTemplateFormat"
          DashSegmentTemplateFormat.of_json in
      let suggestedPresentationDelaySeconds =
        field_map json__ "SuggestedPresentationDelaySeconds"
          CreateDashManifestConfigurationSuggestedPresentationDelaySecondsInteger.of_json in
      let minBufferTimeSeconds =
        field_map json__ "MinBufferTimeSeconds"
          CreateDashManifestConfigurationMinBufferTimeSecondsInteger.of_json in
      let minUpdatePeriodSeconds =
        field_map json__ "MinUpdatePeriodSeconds"
          CreateDashManifestConfigurationMinUpdatePeriodSecondsInteger.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let manifestWindowSeconds =
        field_map json__ "ManifestWindowSeconds"
          CreateDashManifestConfigurationManifestWindowSecondsInteger.of_json in
      let manifestName =
        field_map_exn json__ "ManifestName" ManifestName.of_json in
      make ?availabilityStartTimeConfiguration ?uriPathType
        ?subtitleConfiguration ?compactness ?dvbSettings ?programInformation
        ?baseUrls ?profiles ?utcTiming ?drmSignaling ?scteDash
        ?periodTriggers ?segmentTemplateFormat
        ?suggestedPresentationDelaySeconds ?minBufferTimeSeconds
        ?minUpdatePeriodSeconds ?filterConfiguration ?manifestWindowSeconds
        ~manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a DASH manifest configuration."]
module CreateHlsManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ManifestName.t
        [@ocaml.doc
          "A short short string that's appended to the endpoint URL. The manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index. MediaPackage automatically inserts the format extension, such as .m3u8. You can't use the same manifest name if you use HLS manifest and low-latency HLS manifest. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      childManifestName: ManifestName.t option
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The child manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index, with an added suffix to distinguish it from the manifest name. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      scteHls: ScteHls.t option ;
      startTag: StartTag.t option ;
      manifestWindowSeconds:
        CreateHlsManifestConfigurationManifestWindowSecondsInteger.t option
        [@ocaml.doc
          "The total duration (in seconds) of the manifest's content."];
      programDateTimeIntervalSeconds:
        CreateHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger.t
          option
        [@ocaml.doc
          "Inserts EXT-X-PROGRAM-DATE-TIME tags in the output manifest at the interval that you specify. If you don't enter an interval, EXT-X-PROGRAM-DATE-TIME tags aren't included in the manifest. The tags sync the stream to the wall clock so that viewers can seek to a specific time in the playback timeline on the player. Irrespective of this parameter, if any ID3Timed metadata is in the HLS input, it is passed through to the HLS output."];
      filterConfiguration: FilterConfiguration.t option ;
      urlEncodeChildManifest: Boolean.t option
        [@ocaml.doc
          "When enabled, MediaPackage URL-encodes the query string for API requests for HLS child manifests to comply with Amazon Web Services Signature Version 4 (SigV4) signature signing protocol. For more information, see Amazon Web Services Signature Version 4 for API requests in Identity and Access Management User Guide."];
      uriPathType: UriPathType.t option
        [@ocaml.doc
          "The type of path to use in manifest URIs. LEAF uses leaf-relative paths (for example, index_1.m3u8). ROOT uses root-relative paths that include the full path from root (for example, /out/v1/channel-group/channel/endpoint/index_1.m3u8). If you don't specify a value, the default is LEAF."]}
    let context_ = "CreateHlsManifestConfiguration"
    let make ?childManifestName =
      fun ?scteHls ->
        fun ?startTag ->
          fun ?manifestWindowSeconds ->
            fun ?programDateTimeIntervalSeconds ->
              fun ?filterConfiguration ->
                fun ?urlEncodeChildManifest ->
                  fun ?uriPathType ->
                    fun ~manifestName ->
                      fun () ->
                        {
                          childManifestName;
                          scteHls;
                          startTag;
                          manifestWindowSeconds;
                          programDateTimeIntervalSeconds;
                          filterConfiguration;
                          urlEncodeChildManifest;
                          uriPathType;
                          manifestName
                        }
    let to_value x =
      structure_to_value
        [("ManifestName", (Some (ManifestName.to_value x.manifestName)));
        ("ChildManifestName",
          (Option.map x.childManifestName ~f:ManifestName.to_value));
        ("ScteHls", (Option.map x.scteHls ~f:ScteHls.to_value));
        ("StartTag", (Option.map x.startTag ~f:StartTag.to_value));
        ("ManifestWindowSeconds",
          (Option.map x.manifestWindowSeconds
             ~f:CreateHlsManifestConfigurationManifestWindowSecondsInteger.to_value));
        ("ProgramDateTimeIntervalSeconds",
          (Option.map x.programDateTimeIntervalSeconds
             ~f:CreateHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("UrlEncodeChildManifest",
          (Option.map x.urlEncodeChildManifest ~f:Boolean.to_value));
        ("UriPathType", (Option.map x.uriPathType ~f:UriPathType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uriPathType =
        (Option.map ~f:UriPathType.of_xml) (Xml.child xml_arg0 "UriPathType") in
      let urlEncodeChildManifest =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "UrlEncodeChildManifest") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let programDateTimeIntervalSeconds =
        (Option.map
           ~f:CreateHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger.of_xml)
          (Xml.child xml_arg0 "ProgramDateTimeIntervalSeconds") in
      let manifestWindowSeconds =
        (Option.map
           ~f:CreateHlsManifestConfigurationManifestWindowSecondsInteger.of_xml)
          (Xml.child xml_arg0 "ManifestWindowSeconds") in
      let startTag =
        (Option.map ~f:StartTag.of_xml) (Xml.child xml_arg0 "StartTag") in
      let scteHls =
        (Option.map ~f:ScteHls.of_xml) (Xml.child xml_arg0 "ScteHls") in
      let childManifestName =
        (Option.map ~f:ManifestName.of_xml)
          (Xml.child xml_arg0 "ChildManifestName") in
      let manifestName =
        ManifestName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ManifestName") in
      make ?uriPathType ?urlEncodeChildManifest ?filterConfiguration
        ?programDateTimeIntervalSeconds ?manifestWindowSeconds ?startTag
        ?scteHls ?childManifestName ~manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uriPathType = field_map json__ "UriPathType" UriPathType.of_json in
      let urlEncodeChildManifest =
        field_map json__ "UrlEncodeChildManifest" Boolean.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let programDateTimeIntervalSeconds =
        field_map json__ "ProgramDateTimeIntervalSeconds"
          CreateHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger.of_json in
      let manifestWindowSeconds =
        field_map json__ "ManifestWindowSeconds"
          CreateHlsManifestConfigurationManifestWindowSecondsInteger.of_json in
      let startTag = field_map json__ "StartTag" StartTag.of_json in
      let scteHls = field_map json__ "ScteHls" ScteHls.of_json in
      let childManifestName =
        field_map json__ "ChildManifestName" ManifestName.of_json in
      let manifestName =
        field_map_exn json__ "ManifestName" ManifestName.of_json in
      make ?uriPathType ?urlEncodeChildManifest ?filterConfiguration
        ?programDateTimeIntervalSeconds ?manifestWindowSeconds ?startTag
        ?scteHls ?childManifestName ~manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create an HTTP live streaming (HLS) manifest configuration."]
module CreateLowLatencyHlsManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ManifestName.t
        [@ocaml.doc
          "A short short string that's appended to the endpoint URL. The manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index. MediaPackage automatically inserts the format extension, such as .m3u8. You can't use the same manifest name if you use HLS manifest and low-latency HLS manifest. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      childManifestName: ManifestName.t option
        [@ocaml.doc
          "A short string that's appended to the endpoint URL. The child manifest name creates a unique path to this endpoint. If you don't enter a value, MediaPackage uses the default manifest name, index, with an added suffix to distinguish it from the manifest name. The manifestName on the HLSManifest object overrides the manifestName you provided on the originEndpoint object."];
      scteHls: ScteHls.t option ;
      startTag: StartTag.t option ;
      manifestWindowSeconds:
        CreateLowLatencyHlsManifestConfigurationManifestWindowSecondsInteger.t
          option
        [@ocaml.doc
          "The total duration (in seconds) of the manifest's content."];
      programDateTimeIntervalSeconds:
        CreateLowLatencyHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger.t
          option
        [@ocaml.doc
          "Inserts EXT-X-PROGRAM-DATE-TIME tags in the output manifest at the interval that you specify. If you don't enter an interval, EXT-X-PROGRAM-DATE-TIME tags aren't included in the manifest. The tags sync the stream to the wall clock so that viewers can seek to a specific time in the playback timeline on the player. Irrespective of this parameter, if any ID3Timed metadata is in the HLS input, it is passed through to the HLS output."];
      filterConfiguration: FilterConfiguration.t option ;
      urlEncodeChildManifest: Boolean.t option
        [@ocaml.doc
          "When enabled, MediaPackage URL-encodes the query string for API requests for LL-HLS child manifests to comply with Amazon Web Services Signature Version 4 (SigV4) signature signing protocol. For more information, see Amazon Web Services Signature Version 4 for API requests in Identity and Access Management User Guide."];
      uriPathType: UriPathType.t option
        [@ocaml.doc
          "The type of path to use in manifest URIs. LEAF uses leaf-relative paths (for example, index_1.m3u8). ROOT uses root-relative paths that include the full path from root (for example, /out/v1/channel-group/channel/endpoint/index_1.m3u8). If you don't specify a value, the default is LEAF."]}
    let context_ = "CreateLowLatencyHlsManifestConfiguration"
    let make ?childManifestName =
      fun ?scteHls ->
        fun ?startTag ->
          fun ?manifestWindowSeconds ->
            fun ?programDateTimeIntervalSeconds ->
              fun ?filterConfiguration ->
                fun ?urlEncodeChildManifest ->
                  fun ?uriPathType ->
                    fun ~manifestName ->
                      fun () ->
                        {
                          childManifestName;
                          scteHls;
                          startTag;
                          manifestWindowSeconds;
                          programDateTimeIntervalSeconds;
                          filterConfiguration;
                          urlEncodeChildManifest;
                          uriPathType;
                          manifestName
                        }
    let to_value x =
      structure_to_value
        [("ManifestName", (Some (ManifestName.to_value x.manifestName)));
        ("ChildManifestName",
          (Option.map x.childManifestName ~f:ManifestName.to_value));
        ("ScteHls", (Option.map x.scteHls ~f:ScteHls.to_value));
        ("StartTag", (Option.map x.startTag ~f:StartTag.to_value));
        ("ManifestWindowSeconds",
          (Option.map x.manifestWindowSeconds
             ~f:CreateLowLatencyHlsManifestConfigurationManifestWindowSecondsInteger.to_value));
        ("ProgramDateTimeIntervalSeconds",
          (Option.map x.programDateTimeIntervalSeconds
             ~f:CreateLowLatencyHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("UrlEncodeChildManifest",
          (Option.map x.urlEncodeChildManifest ~f:Boolean.to_value));
        ("UriPathType", (Option.map x.uriPathType ~f:UriPathType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uriPathType =
        (Option.map ~f:UriPathType.of_xml) (Xml.child xml_arg0 "UriPathType") in
      let urlEncodeChildManifest =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "UrlEncodeChildManifest") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let programDateTimeIntervalSeconds =
        (Option.map
           ~f:CreateLowLatencyHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger.of_xml)
          (Xml.child xml_arg0 "ProgramDateTimeIntervalSeconds") in
      let manifestWindowSeconds =
        (Option.map
           ~f:CreateLowLatencyHlsManifestConfigurationManifestWindowSecondsInteger.of_xml)
          (Xml.child xml_arg0 "ManifestWindowSeconds") in
      let startTag =
        (Option.map ~f:StartTag.of_xml) (Xml.child xml_arg0 "StartTag") in
      let scteHls =
        (Option.map ~f:ScteHls.of_xml) (Xml.child xml_arg0 "ScteHls") in
      let childManifestName =
        (Option.map ~f:ManifestName.of_xml)
          (Xml.child xml_arg0 "ChildManifestName") in
      let manifestName =
        ManifestName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ManifestName") in
      make ?uriPathType ?urlEncodeChildManifest ?filterConfiguration
        ?programDateTimeIntervalSeconds ?manifestWindowSeconds ?startTag
        ?scteHls ?childManifestName ~manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uriPathType = field_map json__ "UriPathType" UriPathType.of_json in
      let urlEncodeChildManifest =
        field_map json__ "UrlEncodeChildManifest" Boolean.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let programDateTimeIntervalSeconds =
        field_map json__ "ProgramDateTimeIntervalSeconds"
          CreateLowLatencyHlsManifestConfigurationProgramDateTimeIntervalSecondsInteger.of_json in
      let manifestWindowSeconds =
        field_map json__ "ManifestWindowSeconds"
          CreateLowLatencyHlsManifestConfigurationManifestWindowSecondsInteger.of_json in
      let startTag = field_map json__ "StartTag" StartTag.of_json in
      let scteHls = field_map json__ "ScteHls" ScteHls.of_json in
      let childManifestName =
        field_map json__ "ChildManifestName" ManifestName.of_json in
      let manifestName =
        field_map_exn json__ "ManifestName" ManifestName.of_json in
      make ?uriPathType ?urlEncodeChildManifest ?filterConfiguration
        ?programDateTimeIntervalSeconds ?manifestWindowSeconds ?startTag
        ?scteHls ?childManifestName ~manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a low-latency HTTP live streaming (HLS) manifest configuration."]
module CreateMssManifestConfiguration =
  struct
    type nonrec t =
      {
      manifestName: ManifestName.t
        [@ocaml.doc
          "A short string that's appended to the endpoint URL to create a unique path to this MSS manifest. The manifest name must be unique within the origin endpoint and can contain letters, numbers, hyphens, and underscores."];
      manifestWindowSeconds:
        CreateMssManifestConfigurationManifestWindowSecondsInteger.t option
        [@ocaml.doc
          "The total duration (in seconds) of the manifest window. This determines how much content is available in the manifest at any given time. The manifest window slides forward as new segments become available, maintaining a consistent duration of content. The minimum value is 30 seconds."];
      filterConfiguration: FilterConfiguration.t option ;
      manifestLayout: MssManifestLayout.t option
        [@ocaml.doc
          "Determines the layout format of the MSS manifest. This controls how the manifest is structured and presented to client players, affecting compatibility with different MSS-compatible devices and applications."]}
    let context_ = "CreateMssManifestConfiguration"
    let make ?manifestWindowSeconds =
      fun ?filterConfiguration ->
        fun ?manifestLayout ->
          fun ~manifestName ->
            fun () ->
              {
                manifestWindowSeconds;
                filterConfiguration;
                manifestLayout;
                manifestName
              }
    let to_value x =
      structure_to_value
        [("ManifestName", (Some (ManifestName.to_value x.manifestName)));
        ("ManifestWindowSeconds",
          (Option.map x.manifestWindowSeconds
             ~f:CreateMssManifestConfigurationManifestWindowSecondsInteger.to_value));
        ("FilterConfiguration",
          (Option.map x.filterConfiguration ~f:FilterConfiguration.to_value));
        ("ManifestLayout",
          (Option.map x.manifestLayout ~f:MssManifestLayout.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let manifestLayout =
        (Option.map ~f:MssManifestLayout.of_xml)
          (Xml.child xml_arg0 "ManifestLayout") in
      let filterConfiguration =
        (Option.map ~f:FilterConfiguration.of_xml)
          (Xml.child xml_arg0 "FilterConfiguration") in
      let manifestWindowSeconds =
        (Option.map
           ~f:CreateMssManifestConfigurationManifestWindowSecondsInteger.of_xml)
          (Xml.child xml_arg0 "ManifestWindowSeconds") in
      let manifestName =
        ManifestName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ManifestName") in
      make ?manifestLayout ?filterConfiguration ?manifestWindowSeconds
        ~manifestName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let manifestLayout =
        field_map json__ "ManifestLayout" MssManifestLayout.of_json in
      let filterConfiguration =
        field_map json__ "FilterConfiguration" FilterConfiguration.of_json in
      let manifestWindowSeconds =
        field_map json__ "ManifestWindowSeconds"
          CreateMssManifestConfigurationManifestWindowSecondsInteger.of_json in
      let manifestName =
        field_map_exn json__ "ManifestName" ManifestName.of_json in
      make ?manifestLayout ?filterConfiguration ?manifestWindowSeconds
        ~manifestName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration parameters for creating a Microsoft Smooth Streaming (MSS) manifest. MSS is a streaming media format developed by Microsoft that delivers adaptive bitrate streaming content to compatible players and devices."]
module IngestEndpoint =
  struct
    type nonrec t =
      {
      id: String_.t option
        [@ocaml.doc
          "The system-generated unique identifier for the IngestEndpoint."];
      url: String_.t option
        [@ocaml.doc
          "The ingest domain URL where the source stream should be sent."]}
    let make ?id = fun ?url -> fun () -> { id; url }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:String_.to_value));
        ("Url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Url") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Id") in
      make ?url ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "Url" String_.of_json in
      let id = field_map json__ "Id" String_.of_json in make ?url ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The ingest domain URL where the source stream should be sent."]
module InputSwitchConfigurationPreferredInputInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:2) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for InputSwitchConfigurationPreferredInputInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CdnAuthConfigurationCdnIdentifierSecretArnsList =
  struct
    type nonrec t = CdnIdentifierSecretArn.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CdnIdentifierSecretArn.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CdnIdentifierSecretArn.of_xml)
    let of_json j =
      list_of_json ~kind:"CdnAuthConfigurationCdnIdentifierSecretArnsList"
        ~of_json:CdnIdentifierSecretArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module CdnAuthConfigurationSecretsRoleArnString =
  struct
    type nonrec t = string
    let context_ = "CdnAuthConfigurationSecretsRoleArnString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:20));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"CdnAuthConfigurationSecretsRoleArnString" j
    let to_json = simple_to_json to_value
  end
module OriginEndpointListConfiguration =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."];
      containerType: ContainerType.t option
        [@ocaml.doc
          "The type of container attached to this origin endpoint. A container type is a file format that encapsulates one or more media streams, such as audio and video, into a single file."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Any descriptive information that you want to add to the origin endpoint for future identification purposes."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the origin endpoint was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the origin endpoint was modified."];
      hlsManifests: ListHlsManifests.t option
        [@ocaml.doc "An HTTP live streaming (HLS) manifest configuration."];
      lowLatencyHlsManifests: ListLowLatencyHlsManifests.t option
        [@ocaml.doc "A low-latency HLS manifest configuration."];
      dashManifests: ListDashManifests.t option
        [@ocaml.doc "A DASH manifest configuration."];
      mssManifests: ListMssManifests.t option
        [@ocaml.doc
          "A list of Microsoft Smooth Streaming (MSS) manifest configurations associated with the origin endpoint. Each configuration represents a different MSS streaming option available from this endpoint."];
      forceEndpointErrorConfiguration:
        ForceEndpointErrorConfiguration.t option
        [@ocaml.doc "The failover settings for the endpoint."];
      uriSeparator: UriSeparator.t option
        [@ocaml.doc
          "The separator character used in generated URIs for this origin endpoint."]}
    let make ?arn =
      fun ?channelGroupName ->
        fun ?channelName ->
          fun ?originEndpointName ->
            fun ?containerType ->
              fun ?description ->
                fun ?createdAt ->
                  fun ?modifiedAt ->
                    fun ?hlsManifests ->
                      fun ?lowLatencyHlsManifests ->
                        fun ?dashManifests ->
                          fun ?mssManifests ->
                            fun ?forceEndpointErrorConfiguration ->
                              fun ?uriSeparator ->
                                fun () ->
                                  {
                                    arn;
                                    channelGroupName;
                                    channelName;
                                    originEndpointName;
                                    containerType;
                                    description;
                                    createdAt;
                                    modifiedAt;
                                    hlsManifests;
                                    lowLatencyHlsManifests;
                                    dashManifests;
                                    mssManifests;
                                    forceEndpointErrorConfiguration;
                                    uriSeparator
                                  }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ChannelGroupName",
          (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("ContainerType",
          (Option.map x.containerType ~f:ContainerType.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("HlsManifests",
          (Option.map x.hlsManifests ~f:ListHlsManifests.to_value));
        ("LowLatencyHlsManifests",
          (Option.map x.lowLatencyHlsManifests
             ~f:ListLowLatencyHlsManifests.to_value));
        ("DashManifests",
          (Option.map x.dashManifests ~f:ListDashManifests.to_value));
        ("MssManifests",
          (Option.map x.mssManifests ~f:ListMssManifests.to_value));
        ("ForceEndpointErrorConfiguration",
          (Option.map x.forceEndpointErrorConfiguration
             ~f:ForceEndpointErrorConfiguration.to_value));
        ("UriSeparator",
          (Option.map x.uriSeparator ~f:UriSeparator.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uriSeparator =
        (Option.map ~f:UriSeparator.of_xml)
          (Xml.child xml_arg0 "UriSeparator") in
      let forceEndpointErrorConfiguration =
        (Option.map ~f:ForceEndpointErrorConfiguration.of_xml)
          (Xml.child xml_arg0 "ForceEndpointErrorConfiguration") in
      let mssManifests =
        (Option.map ~f:ListMssManifests.of_xml)
          (Xml.child xml_arg0 "MssManifests") in
      let dashManifests =
        (Option.map ~f:ListDashManifests.of_xml)
          (Xml.child xml_arg0 "DashManifests") in
      let lowLatencyHlsManifests =
        (Option.map ~f:ListLowLatencyHlsManifests.of_xml)
          (Xml.child xml_arg0 "LowLatencyHlsManifests") in
      let hlsManifests =
        (Option.map ~f:ListHlsManifests.of_xml)
          (Xml.child xml_arg0 "HlsManifests") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let containerType =
        (Option.map ~f:ContainerType.of_xml)
          (Xml.child xml_arg0 "ContainerType") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?uriSeparator ?forceEndpointErrorConfiguration ?mssManifests
        ?dashManifests ?lowLatencyHlsManifests ?hlsManifests ?modifiedAt
        ?createdAt ?description ?containerType ?originEndpointName
        ?channelName ?channelGroupName ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uriSeparator = field_map json__ "UriSeparator" UriSeparator.of_json in
      let forceEndpointErrorConfiguration =
        field_map json__ "ForceEndpointErrorConfiguration"
          ForceEndpointErrorConfiguration.of_json in
      let mssManifests =
        field_map json__ "MssManifests" ListMssManifests.of_json in
      let dashManifests =
        field_map json__ "DashManifests" ListDashManifests.of_json in
      let lowLatencyHlsManifests =
        field_map json__ "LowLatencyHlsManifests"
          ListLowLatencyHlsManifests.of_json in
      let hlsManifests =
        field_map json__ "HlsManifests" ListHlsManifests.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let containerType =
        field_map json__ "ContainerType" ContainerType.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      make ?uriSeparator ?forceEndpointErrorConfiguration ?mssManifests
        ?dashManifests ?lowLatencyHlsManifests ?hlsManifests ?modifiedAt
        ?createdAt ?description ?containerType ?originEndpointName
        ?channelName ?channelGroupName ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration of the origin endpoint."]
module HarvestJob =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name of the channel group containing the channel associated with this harvest job."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name of the channel associated with this harvest job."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc
          "The name of the origin endpoint associated with this harvest job."];
      destination: Destination.t option
        [@ocaml.doc
          "The S3 destination where the harvested content will be placed."];
      harvestJobName: ResourceName.t option
        [@ocaml.doc "The name of the harvest job."];
      harvestedManifests: HarvestedManifests.t option
        [@ocaml.doc
          "A list of manifests that are being or have been harvested."];
      description: ResourceDescription.t option
        [@ocaml.doc "An optional description of the harvest job."];
      scheduleConfiguration: HarvesterScheduleConfiguration.t option
        [@ocaml.doc
          "The configuration for when the harvest job is scheduled to run."];
      arn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the harvest job."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time when the harvest job was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the harvest job was last modified."];
      status: HarvestJobStatus.t option
        [@ocaml.doc
          "The current status of the harvest job (e.g., QUEUED, IN_PROGRESS, CANCELLED, COMPLETED, FAILED)."];
      errorMessage: String_.t option
        [@ocaml.doc
          "An error message if the harvest job encountered any issues."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current version of the harvest job. Used for concurrency control."]}
    let make ?channelGroupName =
      fun ?channelName ->
        fun ?originEndpointName ->
          fun ?destination ->
            fun ?harvestJobName ->
              fun ?harvestedManifests ->
                fun ?description ->
                  fun ?scheduleConfiguration ->
                    fun ?arn ->
                      fun ?createdAt ->
                        fun ?modifiedAt ->
                          fun ?status ->
                            fun ?errorMessage ->
                              fun ?eTag ->
                                fun () ->
                                  {
                                    channelGroupName;
                                    channelName;
                                    originEndpointName;
                                    destination;
                                    harvestJobName;
                                    harvestedManifests;
                                    description;
                                    scheduleConfiguration;
                                    arn;
                                    createdAt;
                                    modifiedAt;
                                    status;
                                    errorMessage;
                                    eTag
                                  }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("Destination", (Option.map x.destination ~f:Destination.to_value));
        ("HarvestJobName",
          (Option.map x.harvestJobName ~f:ResourceName.to_value));
        ("HarvestedManifests",
          (Option.map x.harvestedManifests ~f:HarvestedManifests.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("ScheduleConfiguration",
          (Option.map x.scheduleConfiguration
             ~f:HarvesterScheduleConfiguration.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:HarvestJobStatus.to_value));
        ("ErrorMessage", (Option.map x.errorMessage ~f:String_.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ErrorMessage") in
      let status =
        (Option.map ~f:HarvestJobStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let scheduleConfiguration =
        (Option.map ~f:HarvesterScheduleConfiguration.of_xml)
          (Xml.child xml_arg0 "ScheduleConfiguration") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let harvestedManifests =
        (Option.map ~f:HarvestedManifests.of_xml)
          (Xml.child xml_arg0 "HarvestedManifests") in
      let harvestJobName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "HarvestJobName") in
      let destination =
        (Option.map ~f:Destination.of_xml) (Xml.child xml_arg0 "Destination") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?eTag ?errorMessage ?status ?modifiedAt ?createdAt ?arn
        ?scheduleConfiguration ?description ?harvestedManifests
        ?harvestJobName ?destination ?originEndpointName ?channelName
        ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let errorMessage = field_map json__ "ErrorMessage" String_.of_json in
      let status = field_map json__ "Status" HarvestJobStatus.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let scheduleConfiguration =
        field_map json__ "ScheduleConfiguration"
          HarvesterScheduleConfiguration.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let harvestedManifests =
        field_map json__ "HarvestedManifests" HarvestedManifests.of_json in
      let harvestJobName =
        field_map json__ "HarvestJobName" ResourceName.of_json in
      let destination = field_map json__ "Destination" Destination.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      make ?eTag ?errorMessage ?status ?modifiedAt ?createdAt ?arn
        ?scheduleConfiguration ?description ?harvestedManifests
        ?harvestJobName ?destination ?originEndpointName ?channelName
        ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a harvest job resource in MediaPackage v2, which is used to export content from an origin endpoint to an S3 bucket."]
module ChannelListConfiguration =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      channelName: String_.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel was modified."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Any descriptive information that you want to add to the channel for future identification purposes."];
      inputType: InputType.t option
        [@ocaml.doc
          "The input type will be an immutable field which will be used to define whether the channel will allow CMAF ingest or HLS ingest. If unprovided, it will default to HLS to preserve current behavior. The allowed values are: HLS - The HLS streaming specification (which defines M3U8 manifests and TS segments). CMAF - The DASH-IF CMAF Ingest specification (which defines CMAF segments with optional DASH manifests)."]}
    let make ?arn =
      fun ?channelName ->
        fun ?channelGroupName ->
          fun ?createdAt ->
            fun ?modifiedAt ->
              fun ?description ->
                fun ?inputType ->
                  fun () ->
                    {
                      arn;
                      channelName;
                      channelGroupName;
                      createdAt;
                      modifiedAt;
                      description;
                      inputType
                    }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ChannelName", (Option.map x.channelName ~f:String_.to_value));
        ("ChannelGroupName",
          (Option.map x.channelGroupName ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("InputType", (Option.map x.inputType ~f:InputType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inputType =
        (Option.map ~f:InputType.of_xml) (Xml.child xml_arg0 "InputType") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      let channelName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ChannelName") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?inputType ?description ?modifiedAt ?createdAt ?channelGroupName
        ?channelName ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let inputType = field_map json__ "InputType" InputType.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      let channelName = field_map json__ "ChannelName" String_.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      make ?inputType ?description ?modifiedAt ?createdAt ?channelGroupName
        ?channelName ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration of the channel."]
module ChannelGroupListConfiguration =
  struct
    type nonrec t =
      {
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel group was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel group was modified."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Any descriptive information that you want to add to the channel group for future identification purposes."]}
    let make ?channelGroupName =
      fun ?arn ->
        fun ?createdAt ->
          fun ?modifiedAt ->
            fun ?description ->
              fun () ->
                { channelGroupName; arn; createdAt; modifiedAt; description }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:String_.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?description ?modifiedAt ?createdAt ?arn ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      make ?description ?modifiedAt ?createdAt ?arn ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration of the channel group."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Access is denied because either you don't have permissions to perform the requested operation or MediaPackage is getting throttling errors with CDN authorization. The user or role that is making the request must have at least one IAM permissions policy attached that grants the required permissions. For more information, see Access Management in the IAM User Guide. Or, if you're using CDN authorization, you will receive this exception if MediaPackage receives a throttling error from Secrets Manager."]
module ConflictException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      conflictExceptionType: ConflictExceptionType.t option
        [@ocaml.doc "The type of ConflictException."]}
    let make ?message =
      fun ?conflictExceptionType ->
        fun () -> { message; conflictExceptionType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ConflictExceptionType",
          (Option.map x.conflictExceptionType
             ~f:ConflictExceptionType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conflictExceptionType =
        (Option.map ~f:ConflictExceptionType.of_xml)
          (Xml.child xml_arg0 "ConflictExceptionType") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?conflictExceptionType ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let conflictExceptionType =
        field_map json__ "ConflictExceptionType"
          ConflictExceptionType.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?conflictExceptionType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updating or deleting this resource can cause an inconsistent state."]
module GetDashManifests =
  struct
    type nonrec t = GetDashManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GetDashManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GetDashManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"GetDashManifests"
        ~of_json:GetDashManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetHlsManifests =
  struct
    type nonrec t = GetHlsManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GetHlsManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GetHlsManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"GetHlsManifests"
        ~of_json:GetHlsManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetLowLatencyHlsManifests =
  struct
    type nonrec t = GetLowLatencyHlsManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GetLowLatencyHlsManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:GetLowLatencyHlsManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"GetLowLatencyHlsManifests"
        ~of_json:GetLowLatencyHlsManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetMssManifests =
  struct
    type nonrec t = GetMssManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GetMssManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GetMssManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"GetMssManifests"
        ~of_json:GetMssManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that an error from the service occurred while trying to process a request."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceTypeNotFound: ResourceTypeNotFound.t option
        [@ocaml.doc "The specified resource type wasn't found."]}
    let make ?message =
      fun ?resourceTypeNotFound ->
        fun () -> { message; resourceTypeNotFound }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceTypeNotFound",
          (Option.map x.resourceTypeNotFound ~f:ResourceTypeNotFound.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceTypeNotFound =
        (Option.map ~f:ResourceTypeNotFound.of_xml)
          (Xml.child xml_arg0 "ResourceTypeNotFound") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceTypeNotFound ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceTypeNotFound =
        field_map json__ "ResourceTypeNotFound" ResourceTypeNotFound.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?resourceTypeNotFound ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource doesn't exist."]
module Segment =
  struct
    type nonrec t =
      {
      segmentDurationSeconds: SegmentSegmentDurationSecondsInteger.t option
        [@ocaml.doc
          "The duration (in seconds) of each segment. Enter a value equal to, or a multiple of, the input segment duration. If the value that you enter is different from the input segment duration, MediaPackage rounds segments to the nearest multiple of the input segment duration."];
      segmentName: SegmentSegmentNameString.t option
        [@ocaml.doc
          "The name that describes the segment. The name is the base name of the segment used in all content manifests inside of the endpoint. You can't use spaces in the name."];
      tsUseAudioRenditionGroup: Boolean.t option
        [@ocaml.doc
          "When selected, MediaPackage bundles all audio tracks in a rendition group. All other tracks in the stream can be used with any audio rendition from the group."];
      includeIframeOnlyStreams: Boolean.t option
        [@ocaml.doc
          "When selected, the stream set includes an additional I-frame only stream, along with the other tracks. If false, this extra stream is not included. MediaPackage generates an I-frame only stream from the first rendition in the manifest. The service inserts EXT-I-FRAMES-ONLY tags in the output manifest, and then generates and includes an I-frames only playlist in the stream. This playlist permits player functionality like fast forward and rewind."];
      tsIncludeDvbSubtitles: Boolean.t option
        [@ocaml.doc
          "By default, MediaPackage excludes all digital video broadcasting (DVB) subtitles from the output. When selected, MediaPackage passes through DVB subtitles into the output."];
      scte: Scte.t option
        [@ocaml.doc
          "The SCTE configuration options in the segment settings."];
      encryption: Encryption.t option }
    let make ?segmentDurationSeconds =
      fun ?segmentName ->
        fun ?tsUseAudioRenditionGroup ->
          fun ?includeIframeOnlyStreams ->
            fun ?tsIncludeDvbSubtitles ->
              fun ?scte ->
                fun ?encryption ->
                  fun () ->
                    {
                      segmentDurationSeconds;
                      segmentName;
                      tsUseAudioRenditionGroup;
                      includeIframeOnlyStreams;
                      tsIncludeDvbSubtitles;
                      scte;
                      encryption
                    }
    let to_value x =
      structure_to_value
        [("SegmentDurationSeconds",
           (Option.map x.segmentDurationSeconds
              ~f:SegmentSegmentDurationSecondsInteger.to_value));
        ("SegmentName",
          (Option.map x.segmentName ~f:SegmentSegmentNameString.to_value));
        ("TsUseAudioRenditionGroup",
          (Option.map x.tsUseAudioRenditionGroup ~f:Boolean.to_value));
        ("IncludeIframeOnlyStreams",
          (Option.map x.includeIframeOnlyStreams ~f:Boolean.to_value));
        ("TsIncludeDvbSubtitles",
          (Option.map x.tsIncludeDvbSubtitles ~f:Boolean.to_value));
        ("Scte", (Option.map x.scte ~f:Scte.to_value));
        ("Encryption", (Option.map x.encryption ~f:Encryption.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryption =
        (Option.map ~f:Encryption.of_xml) (Xml.child xml_arg0 "Encryption") in
      let scte = (Option.map ~f:Scte.of_xml) (Xml.child xml_arg0 "Scte") in
      let tsIncludeDvbSubtitles =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "TsIncludeDvbSubtitles") in
      let includeIframeOnlyStreams =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "IncludeIframeOnlyStreams") in
      let tsUseAudioRenditionGroup =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "TsUseAudioRenditionGroup") in
      let segmentName =
        (Option.map ~f:SegmentSegmentNameString.of_xml)
          (Xml.child xml_arg0 "SegmentName") in
      let segmentDurationSeconds =
        (Option.map ~f:SegmentSegmentDurationSecondsInteger.of_xml)
          (Xml.child xml_arg0 "SegmentDurationSeconds") in
      make ?encryption ?scte ?tsIncludeDvbSubtitles ?includeIframeOnlyStreams
        ?tsUseAudioRenditionGroup ?segmentName ?segmentDurationSeconds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryption = field_map json__ "Encryption" Encryption.of_json in
      let scte = field_map json__ "Scte" Scte.of_json in
      let tsIncludeDvbSubtitles =
        field_map json__ "TsIncludeDvbSubtitles" Boolean.of_json in
      let includeIframeOnlyStreams =
        field_map json__ "IncludeIframeOnlyStreams" Boolean.of_json in
      let tsUseAudioRenditionGroup =
        field_map json__ "TsUseAudioRenditionGroup" Boolean.of_json in
      let segmentName =
        field_map json__ "SegmentName" SegmentSegmentNameString.of_json in
      let segmentDurationSeconds =
        field_map json__ "SegmentDurationSeconds"
          SegmentSegmentDurationSecondsInteger.of_json in
      make ?encryption ?scte ?tsIncludeDvbSubtitles ?includeIframeOnlyStreams
        ?tsUseAudioRenditionGroup ?segmentName ?segmentDurationSeconds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The segment configuration, including the segment name, duration, and other configuration values."]
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request would cause a service quota to be exceeded."]
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ThrottlingException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request throughput limit was exceeded."]
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      validationExceptionType: ValidationExceptionType.t option
        [@ocaml.doc "The type of ValidationException."]}
    let make ?message =
      fun ?validationExceptionType ->
        fun () -> { message; validationExceptionType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ValidationExceptionType",
          (Option.map x.validationExceptionType
             ~f:ValidationExceptionType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validationExceptionType =
        (Option.map ~f:ValidationExceptionType.of_xml)
          (Xml.child xml_arg0 "ValidationExceptionType") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?validationExceptionType ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let validationExceptionType =
        field_map json__ "ValidationExceptionType"
          ValidationExceptionType.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?validationExceptionType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input failed to meet the constraints specified by the AWS service."]
module CreateDashManifests =
  struct
    type nonrec t = CreateDashManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CreateDashManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CreateDashManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateDashManifests"
        ~of_json:CreateDashManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateHlsManifests =
  struct
    type nonrec t = CreateHlsManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CreateHlsManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CreateHlsManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateHlsManifests"
        ~of_json:CreateHlsManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateLowLatencyHlsManifests =
  struct
    type nonrec t = CreateLowLatencyHlsManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CreateLowLatencyHlsManifestConfiguration.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:CreateLowLatencyHlsManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateLowLatencyHlsManifests"
        ~of_json:CreateLowLatencyHlsManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateMssManifests =
  struct
    type nonrec t = CreateMssManifestConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CreateMssManifestConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CreateMssManifestConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateMssManifests"
        ~of_json:CreateMssManifestConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateOriginEndpointRequestStartoverWindowSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1209600) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for UpdateOriginEndpointRequestStartoverWindowSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module IngestEndpointList =
  struct
    type nonrec t = IngestEndpoint.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:IngestEndpoint.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:IngestEndpoint.of_xml)
    let of_json j =
      list_of_json ~kind:"IngestEndpointList" ~of_json:IngestEndpoint.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module InputSwitchConfiguration =
  struct
    type nonrec t =
      {
      mQCSInputSwitching: Boolean.t option
        [@ocaml.doc
          "When true, AWS Elemental MediaPackage performs input switching based on the MQCS. Default is false. This setting is valid only when InputType is CMAF."];
      preferredInput: InputSwitchConfigurationPreferredInputInteger.t option
        [@ocaml.doc
          "For CMAF inputs, indicates which input MediaPackage should prefer when both inputs have equal MQCS scores. Select 1 to prefer the first ingest endpoint, or 2 to prefer the second ingest endpoint. If you don't specify a preferred input, MediaPackage uses its default switching behavior when MQCS scores are equal."]}
    let make ?mQCSInputSwitching =
      fun ?preferredInput -> fun () -> { mQCSInputSwitching; preferredInput }
    let to_value x =
      structure_to_value
        [("MQCSInputSwitching",
           (Option.map x.mQCSInputSwitching ~f:Boolean.to_value));
        ("PreferredInput",
          (Option.map x.preferredInput
             ~f:InputSwitchConfigurationPreferredInputInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let preferredInput =
        (Option.map ~f:InputSwitchConfigurationPreferredInputInteger.of_xml)
          (Xml.child xml_arg0 "PreferredInput") in
      let mQCSInputSwitching =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "MQCSInputSwitching") in
      make ?preferredInput ?mQCSInputSwitching ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let preferredInput =
        field_map json__ "PreferredInput"
          InputSwitchConfigurationPreferredInputInteger.of_json in
      let mQCSInputSwitching =
        field_map json__ "MQCSInputSwitching" Boolean.of_json in
      make ?preferredInput ?mQCSInputSwitching ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration for input switching based on the media quality confidence score (MQCS) as provided from AWS Elemental MediaLive."]
module OutputHeaderConfiguration =
  struct
    type nonrec t =
      {
      publishMQCS: Boolean.t option
        [@ocaml.doc
          "When true, AWS Elemental MediaPackage includes the MQCS in responses to the CDN. This setting is valid only when InputType is CMAF."]}
    let make ?publishMQCS = fun () -> { publishMQCS }
    let to_value x =
      structure_to_value
        [("PublishMQCS", (Option.map x.publishMQCS ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let publishMQCS =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "PublishMQCS") in
      make ?publishMQCS ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let publishMQCS = field_map json__ "PublishMQCS" Boolean.of_json in
      make ?publishMQCS ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The settings for what common media server data (CMSD) headers AWS Elemental MediaPackage includes in responses to the CDN."]
module TagArn =
  struct
    type nonrec t = string
    let context_ = "TagArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagArn" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module CdnAuthConfiguration =
  struct
    type nonrec t =
      {
      cdnIdentifierSecretArns:
        CdnAuthConfigurationCdnIdentifierSecretArnsList.t
        [@ocaml.doc
          "The ARN for the secret in Secrets Manager that your CDN uses for authorization to access the endpoint."];
      secretsRoleArn: CdnAuthConfigurationSecretsRoleArnString.t
        [@ocaml.doc
          "The ARN for the IAM role that gives MediaPackage read access to Secrets Manager and KMS for CDN authorization."]}
    let context_ = "CdnAuthConfiguration"
    let make ~cdnIdentifierSecretArns =
      fun ~secretsRoleArn ->
        fun () -> { cdnIdentifierSecretArns; secretsRoleArn }
    let to_value x =
      structure_to_value
        [("CdnIdentifierSecretArns",
           (Some
              (CdnAuthConfigurationCdnIdentifierSecretArnsList.to_value
                 x.cdnIdentifierSecretArns)));
        ("SecretsRoleArn",
          (Some
             (CdnAuthConfigurationSecretsRoleArnString.to_value
                x.secretsRoleArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secretsRoleArn =
        CdnAuthConfigurationSecretsRoleArnString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecretsRoleArn") in
      let cdnIdentifierSecretArns =
        CdnAuthConfigurationCdnIdentifierSecretArnsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CdnIdentifierSecretArns") in
      make ~secretsRoleArn ~cdnIdentifierSecretArns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let secretsRoleArn =
        field_map_exn json__ "SecretsRoleArn"
          CdnAuthConfigurationSecretsRoleArnString.of_json in
      let cdnIdentifierSecretArns =
        field_map_exn json__ "CdnIdentifierSecretArns"
          CdnAuthConfigurationCdnIdentifierSecretArnsList.of_json in
      make ~secretsRoleArn ~cdnIdentifierSecretArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The settings to enable CDN authorization headers in MediaPackage."]
module PolicyText =
  struct
    type nonrec t = string
    let context_ = "PolicyText"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:6144) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyText" j
    let to_json = simple_to_json to_value
  end
module OriginEndpointsList =
  struct
    type nonrec t = OriginEndpointListConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OriginEndpointListConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OriginEndpointListConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"OriginEndpointsList"
        ~of_json:OriginEndpointListConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListResourceMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListResourceMaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module HarvestJobsList =
  struct
    type nonrec t = HarvestJob.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:HarvestJob.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HarvestJob.of_xml)
    let of_json j =
      list_of_json ~kind:"HarvestJobsList" ~of_json:HarvestJob.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListHarvestJobsRequestChannelNameString =
  struct
    type nonrec t = string
    let context_ = "ListHarvestJobsRequestChannelNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ListHarvestJobsRequestChannelNameString" j
    let to_json = simple_to_json to_value
  end
module ListHarvestJobsRequestMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListHarvestJobsRequestMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ListHarvestJobsRequestOriginEndpointNameString =
  struct
    type nonrec t = string
    let context_ = "ListHarvestJobsRequestOriginEndpointNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ListHarvestJobsRequestOriginEndpointNameString" j
    let to_json = simple_to_json to_value
  end
module ChannelList =
  struct
    type nonrec t = ChannelListConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ChannelListConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChannelListConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"ChannelList"
        ~of_json:ChannelListConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChannelGroupsList =
  struct
    type nonrec t = ChannelGroupListConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ChannelGroupListConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChannelGroupListConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"ChannelGroupsList"
        ~of_json:ChannelGroupListConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateOriginEndpointRequestStartoverWindowSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1209600) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateOriginEndpointRequestStartoverWindowSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module IdempotencyToken =
  struct
    type nonrec t = string
    let context_ = "IdempotencyToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IdempotencyToken" j
    let to_json = simple_to_json to_value
  end
module UpdateOriginEndpointResponse =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc "The ARN associated with the resource."];
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."];
      containerType: ContainerType.t option
        [@ocaml.doc
          "The type of container attached to this origin endpoint."];
      segment: Segment.t option
        [@ocaml.doc
          "The segment configuration, including the segment name, duration, and other configuration values."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the origin endpoint was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the origin endpoint was modified."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description of the origin endpoint."];
      startoverWindowSeconds: Integer.t option
        [@ocaml.doc
          "The size of the window (in seconds) to create a window of the live stream that's available for on-demand viewing. Viewers can start-over or catch-up on content that falls within the window."];
      hlsManifests: GetHlsManifests.t option
        [@ocaml.doc "An HTTP live streaming (HLS) manifest configuration."];
      lowLatencyHlsManifests: GetLowLatencyHlsManifests.t option
        [@ocaml.doc "A low-latency HLS manifest configuration."];
      mssManifests: GetMssManifests.t option
        [@ocaml.doc
          "The updated Microsoft Smooth Streaming (MSS) manifest configurations for this origin endpoint."];
      forceEndpointErrorConfiguration:
        ForceEndpointErrorConfiguration.t option
        [@ocaml.doc "The failover settings for the endpoint."];
      uriSeparator: UriSeparator.t option
        [@ocaml.doc
          "The separator character used in generated URIs for this origin endpoint."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the origin endpoint."];
      dashManifests: GetDashManifests.t option
        [@ocaml.doc "A DASH manifest configuration."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?channelGroupName ->
        fun ?channelName ->
          fun ?originEndpointName ->
            fun ?containerType ->
              fun ?segment ->
                fun ?createdAt ->
                  fun ?modifiedAt ->
                    fun ?description ->
                      fun ?startoverWindowSeconds ->
                        fun ?hlsManifests ->
                          fun ?lowLatencyHlsManifests ->
                            fun ?mssManifests ->
                              fun ?forceEndpointErrorConfiguration ->
                                fun ?uriSeparator ->
                                  fun ?eTag ->
                                    fun ?tags ->
                                      fun ?dashManifests ->
                                        fun () ->
                                          {
                                            arn;
                                            channelGroupName;
                                            channelName;
                                            originEndpointName;
                                            containerType;
                                            segment;
                                            createdAt;
                                            modifiedAt;
                                            description;
                                            startoverWindowSeconds;
                                            hlsManifests;
                                            lowLatencyHlsManifests;
                                            mssManifests;
                                            forceEndpointErrorConfiguration;
                                            uriSeparator;
                                            eTag;
                                            tags;
                                            dashManifests
                                          }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ChannelGroupName",
          (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("ContainerType",
          (Option.map x.containerType ~f:ContainerType.to_value));
        ("Segment", (Option.map x.segment ~f:Segment.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("StartoverWindowSeconds",
          (Option.map x.startoverWindowSeconds ~f:Integer.to_value));
        ("HlsManifests",
          (Option.map x.hlsManifests ~f:GetHlsManifests.to_value));
        ("LowLatencyHlsManifests",
          (Option.map x.lowLatencyHlsManifests
             ~f:GetLowLatencyHlsManifests.to_value));
        ("MssManifests",
          (Option.map x.mssManifests ~f:GetMssManifests.to_value));
        ("ForceEndpointErrorConfiguration",
          (Option.map x.forceEndpointErrorConfiguration
             ~f:ForceEndpointErrorConfiguration.to_value));
        ("UriSeparator",
          (Option.map x.uriSeparator ~f:UriSeparator.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("DashManifests",
          (Option.map x.dashManifests ~f:GetDashManifests.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dashManifests =
        (Option.map ~f:GetDashManifests.of_xml)
          (Xml.child xml_arg0 "DashManifests") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let uriSeparator =
        (Option.map ~f:UriSeparator.of_xml)
          (Xml.child xml_arg0 "UriSeparator") in
      let forceEndpointErrorConfiguration =
        (Option.map ~f:ForceEndpointErrorConfiguration.of_xml)
          (Xml.child xml_arg0 "ForceEndpointErrorConfiguration") in
      let mssManifests =
        (Option.map ~f:GetMssManifests.of_xml)
          (Xml.child xml_arg0 "MssManifests") in
      let lowLatencyHlsManifests =
        (Option.map ~f:GetLowLatencyHlsManifests.of_xml)
          (Xml.child xml_arg0 "LowLatencyHlsManifests") in
      let hlsManifests =
        (Option.map ~f:GetHlsManifests.of_xml)
          (Xml.child xml_arg0 "HlsManifests") in
      let startoverWindowSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "StartoverWindowSeconds") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let segment =
        (Option.map ~f:Segment.of_xml) (Xml.child xml_arg0 "Segment") in
      let containerType =
        (Option.map ~f:ContainerType.of_xml)
          (Xml.child xml_arg0 "ContainerType") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?dashManifests ?tags ?eTag ?uriSeparator
        ?forceEndpointErrorConfiguration ?mssManifests
        ?lowLatencyHlsManifests ?hlsManifests ?startoverWindowSeconds
        ?description ?modifiedAt ?createdAt ?segment ?containerType
        ?originEndpointName ?channelName ?channelGroupName ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dashManifests =
        field_map json__ "DashManifests" GetDashManifests.of_json in
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let uriSeparator = field_map json__ "UriSeparator" UriSeparator.of_json in
      let forceEndpointErrorConfiguration =
        field_map json__ "ForceEndpointErrorConfiguration"
          ForceEndpointErrorConfiguration.of_json in
      let mssManifests =
        field_map json__ "MssManifests" GetMssManifests.of_json in
      let lowLatencyHlsManifests =
        field_map json__ "LowLatencyHlsManifests"
          GetLowLatencyHlsManifests.of_json in
      let hlsManifests =
        field_map json__ "HlsManifests" GetHlsManifests.of_json in
      let startoverWindowSeconds =
        field_map json__ "StartoverWindowSeconds" Integer.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let segment = field_map json__ "Segment" Segment.of_json in
      let containerType =
        field_map json__ "ContainerType" ContainerType.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      make ?dashManifests ?tags ?eTag ?uriSeparator
        ?forceEndpointErrorConfiguration ?mssManifests
        ?lowLatencyHlsManifests ?hlsManifests ?startoverWindowSeconds
        ?description ?modifiedAt ?createdAt ?segment ?containerType
        ?originEndpointName ?channelName ?channelGroupName ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Update the specified origin endpoint. Edit the packaging preferences on an endpoint to optimize the viewing experience. You can't edit the name of the endpoint. Any edits you make that impact the video output may not be reflected for a few minutes."]
module UpdateOriginEndpointRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."];
      containerType: ContainerType.t
        [@ocaml.doc
          "The type of container attached to this origin endpoint. A container type is a file format that encapsulates one or more media streams, such as audio and video, into a single file."];
      segment: Segment.t option
        [@ocaml.doc
          "The segment configuration, including the segment name, duration, and other configuration values."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Any descriptive information that you want to add to the origin endpoint for future identification purposes."];
      startoverWindowSeconds:
        UpdateOriginEndpointRequestStartoverWindowSecondsInteger.t option
        [@ocaml.doc
          "The size of the window (in seconds) to create a window of the live stream that's available for on-demand viewing. Viewers can start-over or catch-up on content that falls within the window. The maximum startover window is 1,209,600 seconds (14 days)."];
      hlsManifests: CreateHlsManifests.t option
        [@ocaml.doc "An HTTP live streaming (HLS) manifest configuration."];
      lowLatencyHlsManifests: CreateLowLatencyHlsManifests.t option
        [@ocaml.doc "A low-latency HLS manifest configuration."];
      dashManifests: CreateDashManifests.t option
        [@ocaml.doc "A DASH manifest configuration."];
      mssManifests: CreateMssManifests.t option
        [@ocaml.doc
          "A list of Microsoft Smooth Streaming (MSS) manifest configurations to update for the origin endpoint. This replaces the existing MSS manifest configurations."];
      forceEndpointErrorConfiguration:
        ForceEndpointErrorConfiguration.t option
        [@ocaml.doc "The failover settings for the endpoint."];
      uriSeparator: UriSeparator.t option
        [@ocaml.doc
          "The separator character to use in generated URIs for this origin endpoint. This setting applies to all manifest types on the endpoint. If you don't specify a value in the update request, the current value is preserved."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The expected current Entity Tag (ETag) for the resource. If the specified ETag does not match the resource's current entity tag, the update request will be rejected."]}
    let context_ = "UpdateOriginEndpointRequest"
    let make ?segment =
      fun ?description ->
        fun ?startoverWindowSeconds ->
          fun ?hlsManifests ->
            fun ?lowLatencyHlsManifests ->
              fun ?dashManifests ->
                fun ?mssManifests ->
                  fun ?forceEndpointErrorConfiguration ->
                    fun ?uriSeparator ->
                      fun ?eTag ->
                        fun ~channelGroupName ->
                          fun ~channelName ->
                            fun ~originEndpointName ->
                              fun ~containerType ->
                                fun () ->
                                  {
                                    segment;
                                    description;
                                    startoverWindowSeconds;
                                    hlsManifests;
                                    lowLatencyHlsManifests;
                                    dashManifests;
                                    mssManifests;
                                    forceEndpointErrorConfiguration;
                                    uriSeparator;
                                    eTag;
                                    channelGroupName;
                                    channelName;
                                    originEndpointName;
                                    containerType
                                  }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)));
        ("ContainerType", (Some (ContainerType.to_value x.containerType)));
        ("Segment", (Option.map x.segment ~f:Segment.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("StartoverWindowSeconds",
          (Option.map x.startoverWindowSeconds
             ~f:UpdateOriginEndpointRequestStartoverWindowSecondsInteger.to_value));
        ("HlsManifests",
          (Option.map x.hlsManifests ~f:CreateHlsManifests.to_value));
        ("LowLatencyHlsManifests",
          (Option.map x.lowLatencyHlsManifests
             ~f:CreateLowLatencyHlsManifests.to_value));
        ("DashManifests",
          (Option.map x.dashManifests ~f:CreateDashManifests.to_value));
        ("MssManifests",
          (Option.map x.mssManifests ~f:CreateMssManifests.to_value));
        ("ForceEndpointErrorConfiguration",
          (Option.map x.forceEndpointErrorConfiguration
             ~f:ForceEndpointErrorConfiguration.to_value));
        ("UriSeparator",
          (Option.map x.uriSeparator ~f:UriSeparator.to_value));
        ("x-amzn-update-if-match", (Option.map x.eTag ~f:EntityTag.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eTag =
        (Option.map ~f:EntityTag.of_xml)
          (Xml.child xml_arg0 "x-amzn-update-if-match") in
      let uriSeparator =
        (Option.map ~f:UriSeparator.of_xml)
          (Xml.child xml_arg0 "UriSeparator") in
      let forceEndpointErrorConfiguration =
        (Option.map ~f:ForceEndpointErrorConfiguration.of_xml)
          (Xml.child xml_arg0 "ForceEndpointErrorConfiguration") in
      let mssManifests =
        (Option.map ~f:CreateMssManifests.of_xml)
          (Xml.child xml_arg0 "MssManifests") in
      let dashManifests =
        (Option.map ~f:CreateDashManifests.of_xml)
          (Xml.child xml_arg0 "DashManifests") in
      let lowLatencyHlsManifests =
        (Option.map ~f:CreateLowLatencyHlsManifests.of_xml)
          (Xml.child xml_arg0 "LowLatencyHlsManifests") in
      let hlsManifests =
        (Option.map ~f:CreateHlsManifests.of_xml)
          (Xml.child xml_arg0 "HlsManifests") in
      let startoverWindowSeconds =
        (Option.map
           ~f:UpdateOriginEndpointRequestStartoverWindowSecondsInteger.of_xml)
          (Xml.child xml_arg0 "StartoverWindowSeconds") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let segment =
        (Option.map ~f:Segment.of_xml) (Xml.child xml_arg0 "Segment") in
      let containerType =
        ContainerType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerType") in
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?eTag ?uriSeparator ?forceEndpointErrorConfiguration ?mssManifests
        ?dashManifests ?lowLatencyHlsManifests ?hlsManifests
        ?startoverWindowSeconds ?description ?segment ~containerType
        ~originEndpointName ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let uriSeparator = field_map json__ "UriSeparator" UriSeparator.of_json in
      let forceEndpointErrorConfiguration =
        field_map json__ "ForceEndpointErrorConfiguration"
          ForceEndpointErrorConfiguration.of_json in
      let mssManifests =
        field_map json__ "MssManifests" CreateMssManifests.of_json in
      let dashManifests =
        field_map json__ "DashManifests" CreateDashManifests.of_json in
      let lowLatencyHlsManifests =
        field_map json__ "LowLatencyHlsManifests"
          CreateLowLatencyHlsManifests.of_json in
      let hlsManifests =
        field_map json__ "HlsManifests" CreateHlsManifests.of_json in
      let startoverWindowSeconds =
        field_map json__ "StartoverWindowSeconds"
          UpdateOriginEndpointRequestStartoverWindowSecondsInteger.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let segment = field_map json__ "Segment" Segment.of_json in
      let containerType =
        field_map_exn json__ "ContainerType" ContainerType.of_json in
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?eTag ?uriSeparator ?forceEndpointErrorConfiguration ?mssManifests
        ?dashManifests ?lowLatencyHlsManifests ?hlsManifests
        ?startoverWindowSeconds ?description ?segment ~containerType
        ~originEndpointName ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Update the specified origin endpoint. Edit the packaging preferences on an endpoint to optimize the viewing experience. You can't edit the name of the endpoint. Any edits you make that impact the video output may not be reflected for a few minutes."]
module UpdateChannelResponse =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      channelName: String_.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel was modified."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description for your channel."];
      ingestEndpoints: IngestEndpointList.t option ;
      inputType: InputType.t option
        [@ocaml.doc
          "The input type will be an immutable field which will be used to define whether the channel will allow CMAF ingest or HLS ingest. If unprovided, it will default to HLS to preserve current behavior. The allowed values are: HLS - The HLS streaming specification (which defines M3U8 manifests and TS segments). CMAF - The DASH-IF CMAF Ingest specification (which defines CMAF segments with optional DASH manifests)."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the channel."];
      inputSwitchConfiguration: InputSwitchConfiguration.t option
        [@ocaml.doc
          "The configuration for input switching based on the media quality confidence score (MQCS) as provided from AWS Elemental MediaLive. This setting is valid only when InputType is CMAF."];
      outputHeaderConfiguration: OutputHeaderConfiguration.t option
        [@ocaml.doc
          "The settings for what common media server data (CMSD) headers AWS Elemental MediaPackage includes in responses to the CDN. This setting is valid only when InputType is CMAF."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?channelName ->
        fun ?channelGroupName ->
          fun ?createdAt ->
            fun ?modifiedAt ->
              fun ?description ->
                fun ?ingestEndpoints ->
                  fun ?inputType ->
                    fun ?eTag ->
                      fun ?tags ->
                        fun ?inputSwitchConfiguration ->
                          fun ?outputHeaderConfiguration ->
                            fun () ->
                              {
                                arn;
                                channelName;
                                channelGroupName;
                                createdAt;
                                modifiedAt;
                                description;
                                ingestEndpoints;
                                inputType;
                                eTag;
                                tags;
                                inputSwitchConfiguration;
                                outputHeaderConfiguration
                              }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ChannelName", (Option.map x.channelName ~f:String_.to_value));
        ("ChannelGroupName",
          (Option.map x.channelGroupName ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("IngestEndpoints",
          (Option.map x.ingestEndpoints ~f:IngestEndpointList.to_value));
        ("InputType", (Option.map x.inputType ~f:InputType.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("InputSwitchConfiguration",
          (Option.map x.inputSwitchConfiguration
             ~f:InputSwitchConfiguration.to_value));
        ("OutputHeaderConfiguration",
          (Option.map x.outputHeaderConfiguration
             ~f:OutputHeaderConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputHeaderConfiguration =
        (Option.map ~f:OutputHeaderConfiguration.of_xml)
          (Xml.child xml_arg0 "OutputHeaderConfiguration") in
      let inputSwitchConfiguration =
        (Option.map ~f:InputSwitchConfiguration.of_xml)
          (Xml.child xml_arg0 "InputSwitchConfiguration") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let inputType =
        (Option.map ~f:InputType.of_xml) (Xml.child xml_arg0 "InputType") in
      let ingestEndpoints =
        (Option.map ~f:IngestEndpointList.of_xml)
          (Xml.child xml_arg0 "IngestEndpoints") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      let channelName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ChannelName") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?outputHeaderConfiguration ?inputSwitchConfiguration ?tags ?eTag
        ?inputType ?ingestEndpoints ?description ?modifiedAt ?createdAt
        ?channelGroupName ?channelName ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputHeaderConfiguration =
        field_map json__ "OutputHeaderConfiguration"
          OutputHeaderConfiguration.of_json in
      let inputSwitchConfiguration =
        field_map json__ "InputSwitchConfiguration"
          InputSwitchConfiguration.of_json in
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let inputType = field_map json__ "InputType" InputType.of_json in
      let ingestEndpoints =
        field_map json__ "IngestEndpoints" IngestEndpointList.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      let channelName = field_map json__ "ChannelName" String_.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      make ?outputHeaderConfiguration ?inputSwitchConfiguration ?tags ?eTag
        ?inputType ?ingestEndpoints ?description ?modifiedAt ?createdAt
        ?channelGroupName ?channelName ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Update the specified channel. You can edit if MediaPackage sends ingest or egress access logs to the CloudWatch log group, if content will be encrypted, the description on a channel, and your channel's policy settings. You can't edit the name of the channel or CloudFront distribution details. Any edits you make that impact the video output may not be reflected for a few minutes."]
module UpdateChannelRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The expected current Entity Tag (ETag) for the resource. If the specified ETag does not match the resource's current entity tag, the update request will be rejected."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Any descriptive information that you want to add to the channel for future identification purposes."];
      inputSwitchConfiguration: InputSwitchConfiguration.t option
        [@ocaml.doc
          "The configuration for input switching based on the media quality confidence score (MQCS) as provided from AWS Elemental MediaLive. This setting is valid only when InputType is CMAF."];
      outputHeaderConfiguration: OutputHeaderConfiguration.t option
        [@ocaml.doc
          "The settings for what common media server data (CMSD) headers AWS Elemental MediaPackage includes in responses to the CDN. This setting is valid only when InputType is CMAF."]}
    let context_ = "UpdateChannelRequest"
    let make ?eTag =
      fun ?description ->
        fun ?inputSwitchConfiguration ->
          fun ?outputHeaderConfiguration ->
            fun ~channelGroupName ->
              fun ~channelName ->
                fun () ->
                  {
                    eTag;
                    description;
                    inputSwitchConfiguration;
                    outputHeaderConfiguration;
                    channelGroupName;
                    channelName
                  }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("x-amzn-update-if-match", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("InputSwitchConfiguration",
          (Option.map x.inputSwitchConfiguration
             ~f:InputSwitchConfiguration.to_value));
        ("OutputHeaderConfiguration",
          (Option.map x.outputHeaderConfiguration
             ~f:OutputHeaderConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputHeaderConfiguration =
        (Option.map ~f:OutputHeaderConfiguration.of_xml)
          (Xml.child xml_arg0 "OutputHeaderConfiguration") in
      let inputSwitchConfiguration =
        (Option.map ~f:InputSwitchConfiguration.of_xml)
          (Xml.child xml_arg0 "InputSwitchConfiguration") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let eTag =
        (Option.map ~f:EntityTag.of_xml)
          (Xml.child xml_arg0 "x-amzn-update-if-match") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?outputHeaderConfiguration ?inputSwitchConfiguration ?description
        ?eTag ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputHeaderConfiguration =
        field_map json__ "OutputHeaderConfiguration"
          OutputHeaderConfiguration.of_json in
      let inputSwitchConfiguration =
        field_map json__ "InputSwitchConfiguration"
          InputSwitchConfiguration.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?outputHeaderConfiguration ?inputSwitchConfiguration ?description
        ?eTag ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Update the specified channel. You can edit if MediaPackage sends ingest or egress access logs to the CloudWatch log group, if content will be encrypted, the description on a channel, and your channel's policy settings. You can't edit the name of the channel or CloudFront distribution details. Any edits you make that impact the video output may not be reflected for a few minutes."]
module UpdateChannelGroupResponse =
  struct
    type nonrec t =
      {
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      egressDomain: String_.t option
        [@ocaml.doc
          "The output domain where the source stream is sent. Integrate the domain with a downstream CDN (such as Amazon CloudFront) or playback device."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel group was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel group was modified."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description for your channel group."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the channel group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?arn ->
        fun ?egressDomain ->
          fun ?createdAt ->
            fun ?modifiedAt ->
              fun ?description ->
                fun ?eTag ->
                  fun ?tags ->
                    fun () ->
                      {
                        channelGroupName;
                        arn;
                        egressDomain;
                        createdAt;
                        modifiedAt;
                        description;
                        eTag;
                        tags
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:String_.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("EgressDomain", (Option.map x.egressDomain ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let egressDomain =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EgressDomain") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?tags ?eTag ?description ?modifiedAt ?createdAt ?egressDomain ?arn
        ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let egressDomain = field_map json__ "EgressDomain" String_.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      make ?tags ?eTag ?description ?modifiedAt ?createdAt ?egressDomain ?arn
        ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Update the specified channel group. You can edit the description on a channel group for easier identification later from the AWS Elemental MediaPackage console. You can't edit the name of the channel group. Any edits you make that impact the video output may not be reflected for a few minutes."]
module UpdateChannelGroupRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The expected current Entity Tag (ETag) for the resource. If the specified ETag does not match the resource's current entity tag, the update request will be rejected."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Any descriptive information that you want to add to the channel group for future identification purposes."]}
    let context_ = "UpdateChannelGroupRequest"
    let make ?eTag =
      fun ?description ->
        fun ~channelGroupName ->
          fun () -> { eTag; description; channelGroupName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("x-amzn-update-if-match", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let eTag =
        (Option.map ~f:EntityTag.of_xml)
          (Xml.child xml_arg0 "x-amzn-update-if-match") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?description ?eTag ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?description ?eTag ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Update the specified channel group. You can edit the description on a channel group for easier identification later from the AWS Elemental MediaPackage console. You can't edit the name of the channel group. Any edits you make that impact the video output may not be reflected for a few minutes."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: TagArn.t
        [@ocaml.doc
          "The ARN of the MediaPackage resource that you're removing tags from."];
      tagKeys: TagKeyList.t
        [@ocaml.doc "The list of tag keys to remove from the resource."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (TagArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        TagArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "TagKeys" TagKeyList.of_json in
      let resourceArn = field_map_exn json__ "ResourceArn" TagArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes one or more tags from the specified resource."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: TagArn.t
        [@ocaml.doc
          "The ARN of the MediaPackage resource that you're adding tags to."];
      tags: TagMap.t
        [@ocaml.doc
          "Contains a map of the key-value pairs for the resource tag or tags assigned to the resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (TagArn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        TagArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "Tags" TagMap.of_json in
      let resourceArn = field_map_exn json__ "ResourceArn" TagArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Assigns one of more tags (key-value pairs) to the specified MediaPackage resource. Tags can help you organize and categorize your resources. You can also use them to scope user permissions, by granting a user permission to access or change only resources with certain tag values. You can use the TagResource operation with a resource that already has tags. If you specify a new tag key for the resource, this tag is appended to the list of tags associated with the resource. If you specify a tag key that is already associated with the resource, the new tag value that you specify replaces the previous value for that tag."]
module ResetOriginEndpointStateResponse =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name of the channel group that contains the channel with the origin endpoint that you just reset."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name of the channel with the origin endpoint that you just reset."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc "The name of the origin endpoint that you just reset."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the endpoint that you just reset."];
      resetAt: Timestamp.t option
        [@ocaml.doc "The time that the origin endpoint was last reset."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?channelName ->
        fun ?originEndpointName ->
          fun ?arn ->
            fun ?resetAt ->
              fun () ->
                {
                  channelGroupName;
                  channelName;
                  originEndpointName;
                  arn;
                  resetAt
                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ResetAt", (Option.map x.resetAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resetAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ResetAt") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?resetAt ?arn ?originEndpointName ?channelName ?channelGroupName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resetAt = field_map json__ "ResetAt" Timestamp.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      make ?resetAt ?arn ?originEndpointName ?channelName ?channelGroupName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resetting the origin endpoint can help to resolve unexpected behavior and other content packaging issues. It also helps to preserve special events when you don't want the previous content to be available for viewing. A reset clears out all previous content from the origin endpoint. MediaPackage might return old content from this endpoint in the first 30 seconds after the endpoint reset. For best results, when possible, wait 30 seconds from endpoint reset to send playback requests to this endpoint."]
module ResetOriginEndpointStateRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name of the channel group that contains the channel with the origin endpoint that you are resetting."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name of the channel with the origin endpoint that you are resetting."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name of the origin endpoint that you are resetting."]}
    let context_ = "ResetOriginEndpointStateRequest"
    let make ~channelGroupName =
      fun ~channelName ->
        fun ~originEndpointName ->
          fun () -> { channelGroupName; channelName; originEndpointName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resetting the origin endpoint can help to resolve unexpected behavior and other content packaging issues. It also helps to preserve special events when you don't want the previous content to be available for viewing. A reset clears out all previous content from the origin endpoint. MediaPackage might return old content from this endpoint in the first 30 seconds after the endpoint reset. For best results, when possible, wait 30 seconds from endpoint reset to send playback requests to this endpoint."]
module ResetChannelStateResponse =
  struct
    type nonrec t =
      {
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name of the channel group that contains the channel that you just reset."];
      channelName: String_.t option
        [@ocaml.doc "The name of the channel that you just reset."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the channel that you just reset."];
      resetAt: Timestamp.t option
        [@ocaml.doc "The time that the channel was last reset."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?channelName ->
        fun ?arn ->
          fun ?resetAt ->
            fun () -> { channelGroupName; channelName; arn; resetAt }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:String_.to_value));
        ("ChannelName", (Option.map x.channelName ~f:String_.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ResetAt", (Option.map x.resetAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resetAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ResetAt") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let channelName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?resetAt ?arn ?channelName ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resetAt = field_map json__ "ResetAt" Timestamp.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let channelName = field_map json__ "ChannelName" String_.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      make ?resetAt ?arn ?channelName ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resetting the channel can help to clear errors from misconfigurations in the encoder. A reset refreshes the ingest stream and removes previous content. Be sure to stop the encoder before you reset the channel, and wait at least 30 seconds before you restart the encoder."]
module ResetChannelStateRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name of the channel group that contains the channel that you are resetting."];
      channelName: ResourceName.t
        [@ocaml.doc "The name of the channel that you are resetting."]}
    let context_ = "ResetChannelStateRequest"
    let make ~channelGroupName =
      fun ~channelName -> fun () -> { channelGroupName; channelName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resetting the channel can help to clear errors from misconfigurations in the encoder. A reset refreshes the ingest stream and removes previous content. Be sure to stop the encoder before you reset the channel, and wait at least 30 seconds before you restart the encoder."]
module PutOriginEndpointPolicyResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches an IAM policy to the specified origin endpoint. You can attach only one policy with each request."]
module PutOriginEndpointPolicyRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."];
      policy: PolicyText.t
        [@ocaml.doc "The policy to attach to the specified origin endpoint."];
      cdnAuthConfiguration: CdnAuthConfiguration.t option
        [@ocaml.doc
          "The settings for using authorization headers between the MediaPackage endpoint and your CDN. For information about CDN authorization, see CDN authorization in Elemental MediaPackage in the MediaPackage user guide."]}
    let context_ = "PutOriginEndpointPolicyRequest"
    let make ?cdnAuthConfiguration =
      fun ~channelGroupName ->
        fun ~channelName ->
          fun ~originEndpointName ->
            fun ~policy ->
              fun () ->
                {
                  cdnAuthConfiguration;
                  channelGroupName;
                  channelName;
                  originEndpointName;
                  policy
                }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)));
        ("Policy", (Some (PolicyText.to_value x.policy)));
        ("CdnAuthConfiguration",
          (Option.map x.cdnAuthConfiguration ~f:CdnAuthConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cdnAuthConfiguration =
        (Option.map ~f:CdnAuthConfiguration.of_xml)
          (Xml.child xml_arg0 "CdnAuthConfiguration") in
      let policy =
        PolicyText.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Policy") in
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?cdnAuthConfiguration ~policy ~originEndpointName ~channelName
        ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cdnAuthConfiguration =
        field_map json__ "CdnAuthConfiguration" CdnAuthConfiguration.of_json in
      let policy = field_map_exn json__ "Policy" PolicyText.of_json in
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?cdnAuthConfiguration ~policy ~originEndpointName ~channelName
        ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches an IAM policy to the specified origin endpoint. You can attach only one policy with each request."]
module PutChannelPolicyResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches an IAM policy to the specified channel. With policies, you can specify who has access to AWS resources and what actions they can perform on those resources. You can attach only one policy with each request."]
module PutChannelPolicyRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      policy: PolicyText.t
        [@ocaml.doc "The policy to attach to the specified channel."]}
    let context_ = "PutChannelPolicyRequest"
    let make ~channelGroupName =
      fun ~channelName ->
        fun ~policy -> fun () -> { channelGroupName; channelName; policy }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("Policy", (Some (PolicyText.to_value x.policy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        PolicyText.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Policy") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~policy ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policy = field_map_exn json__ "Policy" PolicyText.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~policy ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches an IAM policy to the specified channel. With policies, you can specify who has access to AWS resources and what actions they can perform on those resources. You can attach only one policy with each request."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagMap.t option
        [@ocaml.doc
          "Contains a map of the key-value pairs for the resource tag or tags assigned to the resource."]}
    type nonrec error =
      [ `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags assigned to a resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: TagArn.t
        [@ocaml.doc
          "The ARN of the CloudWatch resource that you want to view tags for."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (TagArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        TagArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn = field_map_exn json__ "ResourceArn" TagArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags assigned to a resource."]
module ListOriginEndpointsResponse =
  struct
    type nonrec t =
      {
      items: OriginEndpointsList.t option
        [@ocaml.doc "The objects being returned."];
      nextToken: String_.t option
        [@ocaml.doc
          "The pagination token from the GET list request. Use the token to fetch the next page of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?items = fun ?nextToken -> fun () -> { items; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Items", (Option.map x.items ~f:OriginEndpointsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let items =
        (Option.map ~f:OriginEndpointsList.of_xml)
          (Xml.child xml_arg0 "Items") in
      make ?nextToken ?items ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let items = field_map json__ "Items" OriginEndpointsList.of_json in
      make ?nextToken ?items ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves all origin endpoints in a specific channel that are configured in AWS Elemental MediaPackage."]
module ListOriginEndpointsRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      maxResults: ListResourceMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the response."];
      nextToken: String_.t option
        [@ocaml.doc
          "The pagination token from the GET list request. Use the token to fetch the next page of results."]}
    let context_ = "ListOriginEndpointsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~channelGroupName ->
          fun ~channelName ->
            fun () ->
              { maxResults; nextToken; channelGroupName; channelName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("maxResults",
          (Option.map x.maxResults ~f:ListResourceMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListResourceMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?nextToken ?maxResults ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" ListResourceMaxResults.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?nextToken ?maxResults ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves all origin endpoints in a specific channel that are configured in AWS Elemental MediaPackage."]
module ListHarvestJobsResponse =
  struct
    type nonrec t =
      {
      items: HarvestJobsList.t option
        [@ocaml.doc
          "An array of harvest job objects that match the specified criteria."];
      nextToken: String_.t option
        [@ocaml.doc
          "A token used for pagination. Include this value in subsequent requests to retrieve the next set of results. If null, there are no more results to retrieve."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?items = fun ?nextToken -> fun () -> { items; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Items", (Option.map x.items ~f:HarvestJobsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let items =
        (Option.map ~f:HarvestJobsList.of_xml) (Xml.child xml_arg0 "Items") in
      make ?nextToken ?items ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let items = field_map json__ "Items" HarvestJobsList.of_json in
      make ?nextToken ?items ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The response object containing the list of harvest jobs that match the specified criteria."]
module ListHarvestJobsRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name of the channel group to filter the harvest jobs by. If specified, only harvest jobs associated with channels in this group will be returned."];
      channelName: ListHarvestJobsRequestChannelNameString.t option
        [@ocaml.doc
          "The name of the channel to filter the harvest jobs by. If specified, only harvest jobs associated with this channel will be returned."];
      originEndpointName:
        ListHarvestJobsRequestOriginEndpointNameString.t option
        [@ocaml.doc
          "The name of the origin endpoint to filter the harvest jobs by. If specified, only harvest jobs associated with this origin endpoint will be returned."];
      status: HarvestJobStatus.t option
        [@ocaml.doc
          "The status to filter the harvest jobs by. If specified, only harvest jobs with this status will be returned."];
      maxResults: ListHarvestJobsRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of harvest jobs to return in a single request. If not specified, a default value will be used."];
      nextToken: String_.t option
        [@ocaml.doc
          "A token used for pagination. Provide this value in subsequent requests to retrieve the next set of results."]}
    let context_ = "ListHarvestJobsRequest"
    let make ?channelName =
      fun ?originEndpointName ->
        fun ?status ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun ~channelGroupName ->
                fun () ->
                  {
                    channelName;
                    originEndpointName;
                    status;
                    maxResults;
                    nextToken;
                    channelGroupName
                  }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("channelName",
          (Option.map x.channelName
             ~f:ListHarvestJobsRequestChannelNameString.to_value));
        ("originEndpointName",
          (Option.map x.originEndpointName
             ~f:ListHarvestJobsRequestOriginEndpointNameString.to_value));
        ("includeStatus", (Option.map x.status ~f:HarvestJobStatus.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListHarvestJobsRequestMaxResultsInteger.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListHarvestJobsRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let status =
        (Option.map ~f:HarvestJobStatus.of_xml)
          (Xml.child xml_arg0 "includeStatus") in
      let originEndpointName =
        (Option.map ~f:ListHarvestJobsRequestOriginEndpointNameString.of_xml)
          (Xml.child xml_arg0 "originEndpointName") in
      let channelName =
        (Option.map ~f:ListHarvestJobsRequestChannelNameString.of_xml)
          (Xml.child xml_arg0 "channelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?nextToken ?maxResults ?status ?originEndpointName ?channelName
        ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults"
          ListHarvestJobsRequestMaxResultsInteger.of_json in
      let status = field_map json__ "Status" HarvestJobStatus.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName"
          ListHarvestJobsRequestOriginEndpointNameString.of_json in
      let channelName =
        field_map json__ "ChannelName"
          ListHarvestJobsRequestChannelNameString.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?nextToken ?maxResults ?status ?originEndpointName ?channelName
        ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request object for listing harvest jobs."]
module ListChannelsResponse =
  struct
    type nonrec t =
      {
      items: ChannelList.t option [@ocaml.doc "The objects being returned."];
      nextToken: String_.t option
        [@ocaml.doc "The pagination token from the GET list request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?items = fun ?nextToken -> fun () -> { items; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Items", (Option.map x.items ~f:ChannelList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let items =
        (Option.map ~f:ChannelList.of_xml) (Xml.child xml_arg0 "Items") in
      make ?nextToken ?items ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let items = field_map json__ "Items" ChannelList.of_json in
      make ?nextToken ?items ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves all channels in a specific channel group that are configured in AWS Elemental MediaPackage."]
module ListChannelsRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      maxResults: ListResourceMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the response."];
      nextToken: String_.t option
        [@ocaml.doc
          "The pagination token from the GET list request. Use the token to fetch the next page of results."]}
    let context_ = "ListChannelsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~channelGroupName ->
          fun () -> { maxResults; nextToken; channelGroupName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("maxResults",
          (Option.map x.maxResults ~f:ListResourceMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListResourceMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?nextToken ?maxResults ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" ListResourceMaxResults.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?nextToken ?maxResults ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves all channels in a specific channel group that are configured in AWS Elemental MediaPackage."]
module ListChannelGroupsResponse =
  struct
    type nonrec t =
      {
      items: ChannelGroupsList.t option
        [@ocaml.doc "The objects being returned."];
      nextToken: String_.t option
        [@ocaml.doc
          "The pagination token from the GET list request. Use the token to fetch the next page of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?items = fun ?nextToken -> fun () -> { items; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Items", (Option.map x.items ~f:ChannelGroupsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let items =
        (Option.map ~f:ChannelGroupsList.of_xml) (Xml.child xml_arg0 "Items") in
      make ?nextToken ?items ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let items = field_map json__ "Items" ChannelGroupsList.of_json in
      make ?nextToken ?items ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves all channel groups that are configured in Elemental MediaPackage."]
module ListChannelGroupsRequest =
  struct
    type nonrec t =
      {
      maxResults: ListResourceMaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the response."];
      nextToken: String_.t option
        [@ocaml.doc
          "The pagination token from the GET list request. Use the token to fetch the next page of results."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults ~f:ListResourceMaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListResourceMaxResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" String_.of_json in
      let maxResults =
        field_map json__ "MaxResults" ListResourceMaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves all channel groups that are configured in Elemental MediaPackage."]
module GetOriginEndpointResponse =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."];
      containerType: ContainerType.t option
        [@ocaml.doc
          "The type of container attached to this origin endpoint."];
      segment: Segment.t option ;
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the origin endpoint was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the origin endpoint was modified."];
      resetAt: Timestamp.t option
        [@ocaml.doc "The time that the origin endpoint was last reset."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description for your origin endpoint."];
      startoverWindowSeconds: Integer.t option
        [@ocaml.doc
          "The size of the window (in seconds) to create a window of the live stream that's available for on-demand viewing. Viewers can start-over or catch-up on content that falls within the window."];
      hlsManifests: GetHlsManifests.t option
        [@ocaml.doc "An HTTP live streaming (HLS) manifest configuration."];
      lowLatencyHlsManifests: GetLowLatencyHlsManifests.t option
        [@ocaml.doc "A low-latency HLS manifest configuration."];
      dashManifests: GetDashManifests.t option
        [@ocaml.doc "A DASH manifest configuration."];
      mssManifests: GetMssManifests.t option
        [@ocaml.doc
          "The Microsoft Smooth Streaming (MSS) manifest configurations associated with this origin endpoint."];
      forceEndpointErrorConfiguration:
        ForceEndpointErrorConfiguration.t option
        [@ocaml.doc "The failover settings for the endpoint."];
      uriSeparator: UriSeparator.t option
        [@ocaml.doc
          "The separator character used in generated URIs for this origin endpoint."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the origin endpoint."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?channelGroupName ->
        fun ?channelName ->
          fun ?originEndpointName ->
            fun ?containerType ->
              fun ?segment ->
                fun ?createdAt ->
                  fun ?modifiedAt ->
                    fun ?resetAt ->
                      fun ?description ->
                        fun ?startoverWindowSeconds ->
                          fun ?hlsManifests ->
                            fun ?lowLatencyHlsManifests ->
                              fun ?dashManifests ->
                                fun ?mssManifests ->
                                  fun ?forceEndpointErrorConfiguration ->
                                    fun ?uriSeparator ->
                                      fun ?eTag ->
                                        fun ?tags ->
                                          fun () ->
                                            {
                                              arn;
                                              channelGroupName;
                                              channelName;
                                              originEndpointName;
                                              containerType;
                                              segment;
                                              createdAt;
                                              modifiedAt;
                                              resetAt;
                                              description;
                                              startoverWindowSeconds;
                                              hlsManifests;
                                              lowLatencyHlsManifests;
                                              dashManifests;
                                              mssManifests;
                                              forceEndpointErrorConfiguration;
                                              uriSeparator;
                                              eTag;
                                              tags
                                            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ChannelGroupName",
          (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("ContainerType",
          (Option.map x.containerType ~f:ContainerType.to_value));
        ("Segment", (Option.map x.segment ~f:Segment.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("ResetAt", (Option.map x.resetAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("StartoverWindowSeconds",
          (Option.map x.startoverWindowSeconds ~f:Integer.to_value));
        ("HlsManifests",
          (Option.map x.hlsManifests ~f:GetHlsManifests.to_value));
        ("LowLatencyHlsManifests",
          (Option.map x.lowLatencyHlsManifests
             ~f:GetLowLatencyHlsManifests.to_value));
        ("DashManifests",
          (Option.map x.dashManifests ~f:GetDashManifests.to_value));
        ("MssManifests",
          (Option.map x.mssManifests ~f:GetMssManifests.to_value));
        ("ForceEndpointErrorConfiguration",
          (Option.map x.forceEndpointErrorConfiguration
             ~f:ForceEndpointErrorConfiguration.to_value));
        ("UriSeparator",
          (Option.map x.uriSeparator ~f:UriSeparator.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let uriSeparator =
        (Option.map ~f:UriSeparator.of_xml)
          (Xml.child xml_arg0 "UriSeparator") in
      let forceEndpointErrorConfiguration =
        (Option.map ~f:ForceEndpointErrorConfiguration.of_xml)
          (Xml.child xml_arg0 "ForceEndpointErrorConfiguration") in
      let mssManifests =
        (Option.map ~f:GetMssManifests.of_xml)
          (Xml.child xml_arg0 "MssManifests") in
      let dashManifests =
        (Option.map ~f:GetDashManifests.of_xml)
          (Xml.child xml_arg0 "DashManifests") in
      let lowLatencyHlsManifests =
        (Option.map ~f:GetLowLatencyHlsManifests.of_xml)
          (Xml.child xml_arg0 "LowLatencyHlsManifests") in
      let hlsManifests =
        (Option.map ~f:GetHlsManifests.of_xml)
          (Xml.child xml_arg0 "HlsManifests") in
      let startoverWindowSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "StartoverWindowSeconds") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let resetAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ResetAt") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let segment =
        (Option.map ~f:Segment.of_xml) (Xml.child xml_arg0 "Segment") in
      let containerType =
        (Option.map ~f:ContainerType.of_xml)
          (Xml.child xml_arg0 "ContainerType") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?tags ?eTag ?uriSeparator ?forceEndpointErrorConfiguration
        ?mssManifests ?dashManifests ?lowLatencyHlsManifests ?hlsManifests
        ?startoverWindowSeconds ?description ?resetAt ?modifiedAt ?createdAt
        ?segment ?containerType ?originEndpointName ?channelName
        ?channelGroupName ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let uriSeparator = field_map json__ "UriSeparator" UriSeparator.of_json in
      let forceEndpointErrorConfiguration =
        field_map json__ "ForceEndpointErrorConfiguration"
          ForceEndpointErrorConfiguration.of_json in
      let mssManifests =
        field_map json__ "MssManifests" GetMssManifests.of_json in
      let dashManifests =
        field_map json__ "DashManifests" GetDashManifests.of_json in
      let lowLatencyHlsManifests =
        field_map json__ "LowLatencyHlsManifests"
          GetLowLatencyHlsManifests.of_json in
      let hlsManifests =
        field_map json__ "HlsManifests" GetHlsManifests.of_json in
      let startoverWindowSeconds =
        field_map json__ "StartoverWindowSeconds" Integer.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let resetAt = field_map json__ "ResetAt" Timestamp.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let segment = field_map json__ "Segment" Segment.of_json in
      let containerType =
        field_map json__ "ContainerType" ContainerType.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      make ?tags ?eTag ?uriSeparator ?forceEndpointErrorConfiguration
        ?mssManifests ?dashManifests ?lowLatencyHlsManifests ?hlsManifests
        ?startoverWindowSeconds ?description ?resetAt ?modifiedAt ?createdAt
        ?segment ?containerType ?originEndpointName ?channelName
        ?channelGroupName ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified origin endpoint that's configured in AWS Elemental MediaPackage to obtain its playback URL and to view the packaging settings that it's currently using."]
module GetOriginEndpointRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."]}
    let context_ = "GetOriginEndpointRequest"
    let make ~channelGroupName =
      fun ~channelName ->
        fun ~originEndpointName ->
          fun () -> { channelGroupName; channelName; originEndpointName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified origin endpoint that's configured in AWS Elemental MediaPackage to obtain its playback URL and to view the packaging settings that it's currently using."]
module GetOriginEndpointPolicyResponse =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."];
      policy: PolicyText.t option
        [@ocaml.doc "The policy assigned to the origin endpoint."];
      cdnAuthConfiguration: CdnAuthConfiguration.t option
        [@ocaml.doc
          "The settings for using authorization headers between the MediaPackage endpoint and your CDN. For information about CDN authorization, see CDN authorization in Elemental MediaPackage in the MediaPackage user guide."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?channelName ->
        fun ?originEndpointName ->
          fun ?policy ->
            fun ?cdnAuthConfiguration ->
              fun () ->
                {
                  channelGroupName;
                  channelName;
                  originEndpointName;
                  policy;
                  cdnAuthConfiguration
                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("Policy", (Option.map x.policy ~f:PolicyText.to_value));
        ("CdnAuthConfiguration",
          (Option.map x.cdnAuthConfiguration ~f:CdnAuthConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cdnAuthConfiguration =
        (Option.map ~f:CdnAuthConfiguration.of_xml)
          (Xml.child xml_arg0 "CdnAuthConfiguration") in
      let policy =
        (Option.map ~f:PolicyText.of_xml) (Xml.child xml_arg0 "Policy") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?cdnAuthConfiguration ?policy ?originEndpointName ?channelName
        ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cdnAuthConfiguration =
        field_map json__ "CdnAuthConfiguration" CdnAuthConfiguration.of_json in
      let policy = field_map json__ "Policy" PolicyText.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      make ?cdnAuthConfiguration ?policy ?originEndpointName ?channelName
        ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified origin endpoint policy that's configured in AWS Elemental MediaPackage."]
module GetOriginEndpointPolicyRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."]}
    let context_ = "GetOriginEndpointPolicyRequest"
    let make ~channelGroupName =
      fun ~channelName ->
        fun ~originEndpointName ->
          fun () -> { channelGroupName; channelName; originEndpointName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified origin endpoint policy that's configured in AWS Elemental MediaPackage."]
module GetHarvestJobResponse =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name of the channel group containing the channel associated with the harvest job."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name of the channel associated with the harvest job."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc
          "The name of the origin endpoint associated with the harvest job."];
      destination: Destination.t option
        [@ocaml.doc
          "The S3 destination where the harvested content is being placed."];
      harvestJobName: ResourceName.t option
        [@ocaml.doc "The name of the harvest job."];
      harvestedManifests: HarvestedManifests.t option
        [@ocaml.doc
          "A list of manifests that are being or have been harvested."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description of the harvest job, if provided."];
      scheduleConfiguration: HarvesterScheduleConfiguration.t option
        [@ocaml.doc
          "The configuration for when the harvest job is scheduled to run, including start and end times."];
      arn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the harvest job."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time when the harvest job was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the harvest job was last modified."];
      status: HarvestJobStatus.t option
        [@ocaml.doc
          "The current status of the harvest job (e.g., QUEUED, IN_PROGRESS, CANCELLED, COMPLETED, FAILED)."];
      errorMessage: String_.t option
        [@ocaml.doc
          "An error message if the harvest job encountered any issues."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current version of the harvest job. Used for concurrency control."];
      tags: TagMap.t option
        [@ocaml.doc "A collection of tags associated with the harvest job."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?channelName ->
        fun ?originEndpointName ->
          fun ?destination ->
            fun ?harvestJobName ->
              fun ?harvestedManifests ->
                fun ?description ->
                  fun ?scheduleConfiguration ->
                    fun ?arn ->
                      fun ?createdAt ->
                        fun ?modifiedAt ->
                          fun ?status ->
                            fun ?errorMessage ->
                              fun ?eTag ->
                                fun ?tags ->
                                  fun () ->
                                    {
                                      channelGroupName;
                                      channelName;
                                      originEndpointName;
                                      destination;
                                      harvestJobName;
                                      harvestedManifests;
                                      description;
                                      scheduleConfiguration;
                                      arn;
                                      createdAt;
                                      modifiedAt;
                                      status;
                                      errorMessage;
                                      eTag;
                                      tags
                                    }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("Destination", (Option.map x.destination ~f:Destination.to_value));
        ("HarvestJobName",
          (Option.map x.harvestJobName ~f:ResourceName.to_value));
        ("HarvestedManifests",
          (Option.map x.harvestedManifests ~f:HarvestedManifests.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("ScheduleConfiguration",
          (Option.map x.scheduleConfiguration
             ~f:HarvesterScheduleConfiguration.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:HarvestJobStatus.to_value));
        ("ErrorMessage", (Option.map x.errorMessage ~f:String_.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ErrorMessage") in
      let status =
        (Option.map ~f:HarvestJobStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let scheduleConfiguration =
        (Option.map ~f:HarvesterScheduleConfiguration.of_xml)
          (Xml.child xml_arg0 "ScheduleConfiguration") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let harvestedManifests =
        (Option.map ~f:HarvestedManifests.of_xml)
          (Xml.child xml_arg0 "HarvestedManifests") in
      let harvestJobName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "HarvestJobName") in
      let destination =
        (Option.map ~f:Destination.of_xml) (Xml.child xml_arg0 "Destination") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?tags ?eTag ?errorMessage ?status ?modifiedAt ?createdAt ?arn
        ?scheduleConfiguration ?description ?harvestedManifests
        ?harvestJobName ?destination ?originEndpointName ?channelName
        ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let errorMessage = field_map json__ "ErrorMessage" String_.of_json in
      let status = field_map json__ "Status" HarvestJobStatus.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let scheduleConfiguration =
        field_map json__ "ScheduleConfiguration"
          HarvesterScheduleConfiguration.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let harvestedManifests =
        field_map json__ "HarvestedManifests" HarvestedManifests.of_json in
      let harvestJobName =
        field_map json__ "HarvestJobName" ResourceName.of_json in
      let destination = field_map json__ "Destination" Destination.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      make ?tags ?eTag ?errorMessage ?status ?modifiedAt ?createdAt ?arn
        ?scheduleConfiguration ?description ?harvestedManifests
        ?harvestJobName ?destination ?originEndpointName ?channelName
        ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The response object containing the details of the requested harvest job."]
module GetHarvestJobRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name of the channel group containing the channel associated with the harvest job."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name of the channel associated with the harvest job."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name of the origin endpoint associated with the harvest job."];
      harvestJobName: ResourceName.t
        [@ocaml.doc "The name of the harvest job to retrieve."]}
    let context_ = "GetHarvestJobRequest"
    let make ~channelGroupName =
      fun ~channelName ->
        fun ~originEndpointName ->
          fun ~harvestJobName ->
            fun () ->
              {
                channelGroupName;
                channelName;
                originEndpointName;
                harvestJobName
              }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)));
        ("HarvestJobName", (Some (ResourceName.to_value x.harvestJobName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let harvestJobName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HarvestJobName") in
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~harvestJobName ~originEndpointName ~channelName ~channelGroupName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let harvestJobName =
        field_map_exn json__ "HarvestJobName" ResourceName.of_json in
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~harvestJobName ~originEndpointName ~channelName ~channelGroupName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request object for retrieving a specific harvest job."]
module GetChannelResponse =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      channelName: String_.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel was modified."];
      resetAt: Timestamp.t option
        [@ocaml.doc "The time that the channel was last reset."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description for your channel."];
      ingestEndpoints: IngestEndpointList.t option ;
      inputType: InputType.t option
        [@ocaml.doc
          "The input type will be an immutable field which will be used to define whether the channel will allow CMAF ingest or HLS ingest. If unprovided, it will default to HLS to preserve current behavior. The allowed values are: HLS - The HLS streaming specification (which defines M3U8 manifests and TS segments). CMAF - The DASH-IF CMAF Ingest specification (which defines CMAF segments with optional DASH manifests)."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the channel."];
      inputSwitchConfiguration: InputSwitchConfiguration.t option
        [@ocaml.doc
          "The configuration for input switching based on the media quality confidence score (MQCS) as provided from AWS Elemental MediaLive. This setting is valid only when InputType is CMAF."];
      outputHeaderConfiguration: OutputHeaderConfiguration.t option
        [@ocaml.doc
          "The settings for what common media server data (CMSD) headers AWS Elemental MediaPackage includes in responses to the CDN. This setting is valid only when InputType is CMAF."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?channelName ->
        fun ?channelGroupName ->
          fun ?createdAt ->
            fun ?modifiedAt ->
              fun ?resetAt ->
                fun ?description ->
                  fun ?ingestEndpoints ->
                    fun ?inputType ->
                      fun ?eTag ->
                        fun ?tags ->
                          fun ?inputSwitchConfiguration ->
                            fun ?outputHeaderConfiguration ->
                              fun () ->
                                {
                                  arn;
                                  channelName;
                                  channelGroupName;
                                  createdAt;
                                  modifiedAt;
                                  resetAt;
                                  description;
                                  ingestEndpoints;
                                  inputType;
                                  eTag;
                                  tags;
                                  inputSwitchConfiguration;
                                  outputHeaderConfiguration
                                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ChannelName", (Option.map x.channelName ~f:String_.to_value));
        ("ChannelGroupName",
          (Option.map x.channelGroupName ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("ResetAt", (Option.map x.resetAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("IngestEndpoints",
          (Option.map x.ingestEndpoints ~f:IngestEndpointList.to_value));
        ("InputType", (Option.map x.inputType ~f:InputType.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value));
        ("InputSwitchConfiguration",
          (Option.map x.inputSwitchConfiguration
             ~f:InputSwitchConfiguration.to_value));
        ("OutputHeaderConfiguration",
          (Option.map x.outputHeaderConfiguration
             ~f:OutputHeaderConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputHeaderConfiguration =
        (Option.map ~f:OutputHeaderConfiguration.of_xml)
          (Xml.child xml_arg0 "OutputHeaderConfiguration") in
      let inputSwitchConfiguration =
        (Option.map ~f:InputSwitchConfiguration.of_xml)
          (Xml.child xml_arg0 "InputSwitchConfiguration") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let inputType =
        (Option.map ~f:InputType.of_xml) (Xml.child xml_arg0 "InputType") in
      let ingestEndpoints =
        (Option.map ~f:IngestEndpointList.of_xml)
          (Xml.child xml_arg0 "IngestEndpoints") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let resetAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ResetAt") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      let channelName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ChannelName") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?outputHeaderConfiguration ?inputSwitchConfiguration ?tags ?eTag
        ?inputType ?ingestEndpoints ?description ?resetAt ?modifiedAt
        ?createdAt ?channelGroupName ?channelName ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputHeaderConfiguration =
        field_map json__ "OutputHeaderConfiguration"
          OutputHeaderConfiguration.of_json in
      let inputSwitchConfiguration =
        field_map json__ "InputSwitchConfiguration"
          InputSwitchConfiguration.of_json in
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let inputType = field_map json__ "InputType" InputType.of_json in
      let ingestEndpoints =
        field_map json__ "IngestEndpoints" IngestEndpointList.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let resetAt = field_map json__ "ResetAt" Timestamp.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      let channelName = field_map json__ "ChannelName" String_.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      make ?outputHeaderConfiguration ?inputSwitchConfiguration ?tags ?eTag
        ?inputType ?ingestEndpoints ?description ?resetAt ?modifiedAt
        ?createdAt ?channelGroupName ?channelName ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified channel that's configured in AWS Elemental MediaPackage."]
module GetChannelRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."]}
    let context_ = "GetChannelRequest"
    let make ~channelGroupName =
      fun ~channelName -> fun () -> { channelGroupName; channelName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified channel that's configured in AWS Elemental MediaPackage."]
module GetChannelPolicyResponse =
  struct
    type nonrec t =
      {
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: String_.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      policy: PolicyText.t option
        [@ocaml.doc "The policy assigned to the channel."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?channelName ->
        fun ?policy -> fun () -> { channelGroupName; channelName; policy }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:String_.to_value));
        ("ChannelName", (Option.map x.channelName ~f:String_.to_value));
        ("Policy", (Option.map x.policy ~f:PolicyText.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:PolicyText.of_xml) (Xml.child xml_arg0 "Policy") in
      let channelName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?policy ?channelName ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let policy = field_map json__ "Policy" PolicyText.of_json in
      let channelName = field_map json__ "ChannelName" String_.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      make ?policy ?channelName ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified channel policy that's configured in AWS Elemental MediaPackage. With policies, you can specify who has access to AWS resources and what actions they can perform on those resources."]
module GetChannelPolicyRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."]}
    let context_ = "GetChannelPolicyRequest"
    let make ~channelGroupName =
      fun ~channelName -> fun () -> { channelGroupName; channelName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified channel policy that's configured in AWS Elemental MediaPackage. With policies, you can specify who has access to AWS resources and what actions they can perform on those resources."]
module GetChannelGroupResponse =
  struct
    type nonrec t =
      {
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      egressDomain: String_.t option
        [@ocaml.doc
          "The output domain where the source stream should be sent. Integrate the domain with a downstream CDN (such as Amazon CloudFront) or playback device."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel group was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel group was modified."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description for your channel group."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the channel group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?arn ->
        fun ?egressDomain ->
          fun ?createdAt ->
            fun ?modifiedAt ->
              fun ?description ->
                fun ?eTag ->
                  fun ?tags ->
                    fun () ->
                      {
                        channelGroupName;
                        arn;
                        egressDomain;
                        createdAt;
                        modifiedAt;
                        description;
                        eTag;
                        tags
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:String_.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("EgressDomain", (Option.map x.egressDomain ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let egressDomain =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EgressDomain") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?tags ?eTag ?description ?modifiedAt ?createdAt ?egressDomain ?arn
        ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let egressDomain = field_map json__ "EgressDomain" String_.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      make ?tags ?eTag ?description ?modifiedAt ?createdAt ?egressDomain ?arn
        ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified channel group that's configured in AWS Elemental MediaPackage."]
module GetChannelGroupRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."]}
    let context_ = "GetChannelGroupRequest"
    let make ~channelGroupName = fun () -> { channelGroupName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the specified channel group that's configured in AWS Elemental MediaPackage."]
module DeleteOriginEndpointResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Origin endpoints can serve content until they're deleted. Delete the endpoint if it should no longer respond to playback requests. You must delete all endpoints from a channel before you can delete the channel."]
module DeleteOriginEndpointRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."]}
    let context_ = "DeleteOriginEndpointRequest"
    let make ~channelGroupName =
      fun ~channelName ->
        fun ~originEndpointName ->
          fun () -> { channelGroupName; channelName; originEndpointName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Origin endpoints can serve content until they're deleted. Delete the endpoint if it should no longer respond to playback requests. You must delete all endpoints from a channel before you can delete the channel."]
module DeleteOriginEndpointPolicyResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete an origin endpoint policy."]
module DeleteOriginEndpointPolicyRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."]}
    let context_ = "DeleteOriginEndpointPolicyRequest"
    let make ~channelGroupName =
      fun ~channelName ->
        fun ~originEndpointName ->
          fun () -> { channelGroupName; channelName; originEndpointName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~originEndpointName ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete an origin endpoint policy."]
module DeleteChannelResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a channel to stop AWS Elemental MediaPackage from receiving further content. You must delete the channel's origin endpoints before you can delete the channel."]
module DeleteChannelRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."]}
    let context_ = "DeleteChannelRequest"
    let make ~channelGroupName =
      fun ~channelName -> fun () -> { channelGroupName; channelName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a channel to stop AWS Elemental MediaPackage from receiving further content. You must delete the channel's origin endpoints before you can delete the channel."]
module DeleteChannelPolicyResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete a channel policy."]
module DeleteChannelPolicyRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."]}
    let context_ = "DeleteChannelPolicyRequest"
    let make ~channelGroupName =
      fun ~channelName -> fun () -> { channelGroupName; channelName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete a channel policy."]
module DeleteChannelGroupResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a channel group. You must delete the channel group's channels and origin endpoints before you can delete the channel group. If you delete a channel group, you'll lose access to the egress domain and will have to create a new channel group to replace it."]
module DeleteChannelGroupRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."]}
    let context_ = "DeleteChannelGroupRequest"
    let make ~channelGroupName = fun () -> { channelGroupName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a channel group. You must delete the channel group's channels and origin endpoints before you can delete the channel group. If you delete a channel group, you'll lose access to the egress domain and will have to create a new channel group to replace it."]
module CreateOriginEndpointResponse =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and and must be unique for your account in the AWS Region and channel."];
      containerType: ContainerType.t option
        [@ocaml.doc
          "The type of container attached to this origin endpoint."];
      segment: Segment.t option
        [@ocaml.doc
          "The segment configuration, including the segment name, duration, and other configuration values."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the origin endpoint was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the origin endpoint was modified."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description for your origin endpoint."];
      startoverWindowSeconds: Integer.t option
        [@ocaml.doc
          "The size of the window (in seconds) to create a window of the live stream that's available for on-demand viewing. Viewers can start-over or catch-up on content that falls within the window."];
      hlsManifests: GetHlsManifests.t option
        [@ocaml.doc "An HTTP live streaming (HLS) manifest configuration."];
      lowLatencyHlsManifests: GetLowLatencyHlsManifests.t option
        [@ocaml.doc "A low-latency HLS manifest configuration."];
      dashManifests: GetDashManifests.t option
        [@ocaml.doc "A DASH manifest configuration."];
      mssManifests: GetMssManifests.t option
        [@ocaml.doc
          "The Microsoft Smooth Streaming (MSS) manifest configurations that were created for this origin endpoint."];
      forceEndpointErrorConfiguration:
        ForceEndpointErrorConfiguration.t option
        [@ocaml.doc "The failover settings for the endpoint."];
      uriSeparator: UriSeparator.t option
        [@ocaml.doc
          "The separator character used in generated URIs for this origin endpoint."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the origin endpoint."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?channelGroupName ->
        fun ?channelName ->
          fun ?originEndpointName ->
            fun ?containerType ->
              fun ?segment ->
                fun ?createdAt ->
                  fun ?modifiedAt ->
                    fun ?description ->
                      fun ?startoverWindowSeconds ->
                        fun ?hlsManifests ->
                          fun ?lowLatencyHlsManifests ->
                            fun ?dashManifests ->
                              fun ?mssManifests ->
                                fun ?forceEndpointErrorConfiguration ->
                                  fun ?uriSeparator ->
                                    fun ?eTag ->
                                      fun ?tags ->
                                        fun () ->
                                          {
                                            arn;
                                            channelGroupName;
                                            channelName;
                                            originEndpointName;
                                            containerType;
                                            segment;
                                            createdAt;
                                            modifiedAt;
                                            description;
                                            startoverWindowSeconds;
                                            hlsManifests;
                                            lowLatencyHlsManifests;
                                            dashManifests;
                                            mssManifests;
                                            forceEndpointErrorConfiguration;
                                            uriSeparator;
                                            eTag;
                                            tags
                                          }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ChannelGroupName",
          (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("ContainerType",
          (Option.map x.containerType ~f:ContainerType.to_value));
        ("Segment", (Option.map x.segment ~f:Segment.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("StartoverWindowSeconds",
          (Option.map x.startoverWindowSeconds ~f:Integer.to_value));
        ("HlsManifests",
          (Option.map x.hlsManifests ~f:GetHlsManifests.to_value));
        ("LowLatencyHlsManifests",
          (Option.map x.lowLatencyHlsManifests
             ~f:GetLowLatencyHlsManifests.to_value));
        ("DashManifests",
          (Option.map x.dashManifests ~f:GetDashManifests.to_value));
        ("MssManifests",
          (Option.map x.mssManifests ~f:GetMssManifests.to_value));
        ("ForceEndpointErrorConfiguration",
          (Option.map x.forceEndpointErrorConfiguration
             ~f:ForceEndpointErrorConfiguration.to_value));
        ("UriSeparator",
          (Option.map x.uriSeparator ~f:UriSeparator.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let uriSeparator =
        (Option.map ~f:UriSeparator.of_xml)
          (Xml.child xml_arg0 "UriSeparator") in
      let forceEndpointErrorConfiguration =
        (Option.map ~f:ForceEndpointErrorConfiguration.of_xml)
          (Xml.child xml_arg0 "ForceEndpointErrorConfiguration") in
      let mssManifests =
        (Option.map ~f:GetMssManifests.of_xml)
          (Xml.child xml_arg0 "MssManifests") in
      let dashManifests =
        (Option.map ~f:GetDashManifests.of_xml)
          (Xml.child xml_arg0 "DashManifests") in
      let lowLatencyHlsManifests =
        (Option.map ~f:GetLowLatencyHlsManifests.of_xml)
          (Xml.child xml_arg0 "LowLatencyHlsManifests") in
      let hlsManifests =
        (Option.map ~f:GetHlsManifests.of_xml)
          (Xml.child xml_arg0 "HlsManifests") in
      let startoverWindowSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "StartoverWindowSeconds") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let segment =
        (Option.map ~f:Segment.of_xml) (Xml.child xml_arg0 "Segment") in
      let containerType =
        (Option.map ~f:ContainerType.of_xml)
          (Xml.child xml_arg0 "ContainerType") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?tags ?eTag ?uriSeparator ?forceEndpointErrorConfiguration
        ?mssManifests ?dashManifests ?lowLatencyHlsManifests ?hlsManifests
        ?startoverWindowSeconds ?description ?modifiedAt ?createdAt ?segment
        ?containerType ?originEndpointName ?channelName ?channelGroupName
        ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let uriSeparator = field_map json__ "UriSeparator" UriSeparator.of_json in
      let forceEndpointErrorConfiguration =
        field_map json__ "ForceEndpointErrorConfiguration"
          ForceEndpointErrorConfiguration.of_json in
      let mssManifests =
        field_map json__ "MssManifests" GetMssManifests.of_json in
      let dashManifests =
        field_map json__ "DashManifests" GetDashManifests.of_json in
      let lowLatencyHlsManifests =
        field_map json__ "LowLatencyHlsManifests"
          GetLowLatencyHlsManifests.of_json in
      let hlsManifests =
        field_map json__ "HlsManifests" GetHlsManifests.of_json in
      let startoverWindowSeconds =
        field_map json__ "StartoverWindowSeconds" Integer.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let segment = field_map json__ "Segment" Segment.of_json in
      let containerType =
        field_map json__ "ContainerType" ContainerType.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      make ?tags ?eTag ?uriSeparator ?forceEndpointErrorConfiguration
        ?mssManifests ?dashManifests ?lowLatencyHlsManifests ?hlsManifests
        ?startoverWindowSeconds ?description ?modifiedAt ?createdAt ?segment
        ?containerType ?originEndpointName ?channelName ?channelGroupName
        ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The endpoint is attached to a channel, and represents the output of the live content. You can associate multiple endpoints to a single channel. Each endpoint gives players and downstream CDNs (such as Amazon CloudFront) access to the content for playback. Content can't be served from a channel until it has an endpoint. You can create only one endpoint with each request."]
module CreateOriginEndpointRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name that describes the origin endpoint. The name is the primary identifier for the origin endpoint, and must be unique for your account in the AWS Region and channel. You can't use spaces in the name. You can't change the name after you create the endpoint."];
      containerType: ContainerType.t
        [@ocaml.doc
          "The type of container to attach to this origin endpoint. A container type is a file format that encapsulates one or more media streams, such as audio and video, into a single file. You can't change the container type after you create the endpoint."];
      segment: Segment.t option
        [@ocaml.doc
          "The segment configuration, including the segment name, duration, and other configuration values."];
      clientToken: IdempotencyToken.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure the idempotency of the request."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Enter any descriptive text that helps you to identify the origin endpoint."];
      startoverWindowSeconds:
        CreateOriginEndpointRequestStartoverWindowSecondsInteger.t option
        [@ocaml.doc
          "The size of the window (in seconds) to create a window of the live stream that's available for on-demand viewing. Viewers can start-over or catch-up on content that falls within the window. The maximum startover window is 1,209,600 seconds (14 days)."];
      hlsManifests: CreateHlsManifests.t option
        [@ocaml.doc "An HTTP live streaming (HLS) manifest configuration."];
      lowLatencyHlsManifests: CreateLowLatencyHlsManifests.t option
        [@ocaml.doc "A low-latency HLS manifest configuration."];
      dashManifests: CreateDashManifests.t option
        [@ocaml.doc "A DASH manifest configuration."];
      mssManifests: CreateMssManifests.t option
        [@ocaml.doc
          "A list of Microsoft Smooth Streaming (MSS) manifest configurations for the origin endpoint. You can configure multiple MSS manifests to provide different streaming experiences or to support different client requirements."];
      forceEndpointErrorConfiguration:
        ForceEndpointErrorConfiguration.t option
        [@ocaml.doc "The failover settings for the endpoint."];
      uriSeparator: UriSeparator.t option
        [@ocaml.doc
          "The separator character to use in generated URIs for this origin endpoint. This setting applies to all manifest types on the endpoint. If you don't specify a value, the default is UNDERSCORE."];
      tags: TagMap.t option
        [@ocaml.doc
          "A comma-separated list of tag key:value pairs that you define. For example: \"Key1\": \"Value1\", \"Key2\": \"Value2\""]}
    let context_ = "CreateOriginEndpointRequest"
    let make ?segment =
      fun ?clientToken ->
        fun ?description ->
          fun ?startoverWindowSeconds ->
            fun ?hlsManifests ->
              fun ?lowLatencyHlsManifests ->
                fun ?dashManifests ->
                  fun ?mssManifests ->
                    fun ?forceEndpointErrorConfiguration ->
                      fun ?uriSeparator ->
                        fun ?tags ->
                          fun ~channelGroupName ->
                            fun ~channelName ->
                              fun ~originEndpointName ->
                                fun ~containerType ->
                                  fun () ->
                                    {
                                      segment;
                                      clientToken;
                                      description;
                                      startoverWindowSeconds;
                                      hlsManifests;
                                      lowLatencyHlsManifests;
                                      dashManifests;
                                      mssManifests;
                                      forceEndpointErrorConfiguration;
                                      uriSeparator;
                                      tags;
                                      channelGroupName;
                                      channelName;
                                      originEndpointName;
                                      containerType
                                    }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)));
        ("ContainerType", (Some (ContainerType.to_value x.containerType)));
        ("Segment", (Option.map x.segment ~f:Segment.to_value));
        ("x-amzn-client-token",
          (Option.map x.clientToken ~f:IdempotencyToken.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("StartoverWindowSeconds",
          (Option.map x.startoverWindowSeconds
             ~f:CreateOriginEndpointRequestStartoverWindowSecondsInteger.to_value));
        ("HlsManifests",
          (Option.map x.hlsManifests ~f:CreateHlsManifests.to_value));
        ("LowLatencyHlsManifests",
          (Option.map x.lowLatencyHlsManifests
             ~f:CreateLowLatencyHlsManifests.to_value));
        ("DashManifests",
          (Option.map x.dashManifests ~f:CreateDashManifests.to_value));
        ("MssManifests",
          (Option.map x.mssManifests ~f:CreateMssManifests.to_value));
        ("ForceEndpointErrorConfiguration",
          (Option.map x.forceEndpointErrorConfiguration
             ~f:ForceEndpointErrorConfiguration.to_value));
        ("UriSeparator",
          (Option.map x.uriSeparator ~f:UriSeparator.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let uriSeparator =
        (Option.map ~f:UriSeparator.of_xml)
          (Xml.child xml_arg0 "UriSeparator") in
      let forceEndpointErrorConfiguration =
        (Option.map ~f:ForceEndpointErrorConfiguration.of_xml)
          (Xml.child xml_arg0 "ForceEndpointErrorConfiguration") in
      let mssManifests =
        (Option.map ~f:CreateMssManifests.of_xml)
          (Xml.child xml_arg0 "MssManifests") in
      let dashManifests =
        (Option.map ~f:CreateDashManifests.of_xml)
          (Xml.child xml_arg0 "DashManifests") in
      let lowLatencyHlsManifests =
        (Option.map ~f:CreateLowLatencyHlsManifests.of_xml)
          (Xml.child xml_arg0 "LowLatencyHlsManifests") in
      let hlsManifests =
        (Option.map ~f:CreateHlsManifests.of_xml)
          (Xml.child xml_arg0 "HlsManifests") in
      let startoverWindowSeconds =
        (Option.map
           ~f:CreateOriginEndpointRequestStartoverWindowSecondsInteger.of_xml)
          (Xml.child xml_arg0 "StartoverWindowSeconds") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "x-amzn-client-token") in
      let segment =
        (Option.map ~f:Segment.of_xml) (Xml.child xml_arg0 "Segment") in
      let containerType =
        ContainerType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ContainerType") in
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?tags ?uriSeparator ?forceEndpointErrorConfiguration ?mssManifests
        ?dashManifests ?lowLatencyHlsManifests ?hlsManifests
        ?startoverWindowSeconds ?description ?clientToken ?segment
        ~containerType ~originEndpointName ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let uriSeparator = field_map json__ "UriSeparator" UriSeparator.of_json in
      let forceEndpointErrorConfiguration =
        field_map json__ "ForceEndpointErrorConfiguration"
          ForceEndpointErrorConfiguration.of_json in
      let mssManifests =
        field_map json__ "MssManifests" CreateMssManifests.of_json in
      let dashManifests =
        field_map json__ "DashManifests" CreateDashManifests.of_json in
      let lowLatencyHlsManifests =
        field_map json__ "LowLatencyHlsManifests"
          CreateLowLatencyHlsManifests.of_json in
      let hlsManifests =
        field_map json__ "HlsManifests" CreateHlsManifests.of_json in
      let startoverWindowSeconds =
        field_map json__ "StartoverWindowSeconds"
          CreateOriginEndpointRequestStartoverWindowSecondsInteger.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let clientToken =
        field_map json__ "ClientToken" IdempotencyToken.of_json in
      let segment = field_map json__ "Segment" Segment.of_json in
      let containerType =
        field_map_exn json__ "ContainerType" ContainerType.of_json in
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?tags ?uriSeparator ?forceEndpointErrorConfiguration ?mssManifests
        ?dashManifests ?lowLatencyHlsManifests ?hlsManifests
        ?startoverWindowSeconds ?description ?clientToken ?segment
        ~containerType ~originEndpointName ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The endpoint is attached to a channel, and represents the output of the live content. You can associate multiple endpoints to a single channel. Each endpoint gives players and downstream CDNs (such as Amazon CloudFront) access to the content for playback. Content can't be served from a channel until it has an endpoint. You can create only one endpoint with each request."]
module CreateHarvestJobResponse =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t option
        [@ocaml.doc
          "The name of the channel group containing the channel from which content is being harvested."];
      channelName: ResourceName.t option
        [@ocaml.doc
          "The name of the channel from which content is being harvested."];
      originEndpointName: ResourceName.t option
        [@ocaml.doc
          "The name of the origin endpoint from which content is being harvested."];
      destination: Destination.t option
        [@ocaml.doc
          "The S3 destination where the harvested content will be placed."];
      harvestJobName: ResourceName.t option
        [@ocaml.doc "The name of the created harvest job."];
      harvestedManifests: HarvestedManifests.t option
        [@ocaml.doc "A list of manifests that will be harvested."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description of the harvest job, if provided."];
      scheduleConfiguration: HarvesterScheduleConfiguration.t option
        [@ocaml.doc
          "The configuration for when the harvest job will run, including start and end times."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the created harvest job."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the harvest job was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the harvest job was last modified."];
      status: HarvestJobStatus.t option
        [@ocaml.doc
          "The current status of the harvest job (e.g., CREATED, IN_PROGRESS, ABORTED, COMPLETED, FAILED)."];
      errorMessage: String_.t option
        [@ocaml.doc "An error message if the harvest job creation failed."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current version of the harvest job. Used for concurrency control."];
      tags: TagMap.t option
        [@ocaml.doc "A collection of tags associated with the harvest job."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?channelName ->
        fun ?originEndpointName ->
          fun ?destination ->
            fun ?harvestJobName ->
              fun ?harvestedManifests ->
                fun ?description ->
                  fun ?scheduleConfiguration ->
                    fun ?arn ->
                      fun ?createdAt ->
                        fun ?modifiedAt ->
                          fun ?status ->
                            fun ?errorMessage ->
                              fun ?eTag ->
                                fun ?tags ->
                                  fun () ->
                                    {
                                      channelGroupName;
                                      channelName;
                                      originEndpointName;
                                      destination;
                                      harvestJobName;
                                      harvestedManifests;
                                      description;
                                      scheduleConfiguration;
                                      arn;
                                      createdAt;
                                      modifiedAt;
                                      status;
                                      errorMessage;
                                      eTag;
                                      tags
                                    }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:ResourceName.to_value));
        ("ChannelName", (Option.map x.channelName ~f:ResourceName.to_value));
        ("OriginEndpointName",
          (Option.map x.originEndpointName ~f:ResourceName.to_value));
        ("Destination", (Option.map x.destination ~f:Destination.to_value));
        ("HarvestJobName",
          (Option.map x.harvestJobName ~f:ResourceName.to_value));
        ("HarvestedManifests",
          (Option.map x.harvestedManifests ~f:HarvestedManifests.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("ScheduleConfiguration",
          (Option.map x.scheduleConfiguration
             ~f:HarvesterScheduleConfiguration.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:HarvestJobStatus.to_value));
        ("ErrorMessage", (Option.map x.errorMessage ~f:String_.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ErrorMessage") in
      let status =
        (Option.map ~f:HarvestJobStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let scheduleConfiguration =
        (Option.map ~f:HarvesterScheduleConfiguration.of_xml)
          (Xml.child xml_arg0 "ScheduleConfiguration") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let harvestedManifests =
        (Option.map ~f:HarvestedManifests.of_xml)
          (Xml.child xml_arg0 "HarvestedManifests") in
      let harvestJobName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "HarvestJobName") in
      let destination =
        (Option.map ~f:Destination.of_xml) (Xml.child xml_arg0 "Destination") in
      let originEndpointName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "OriginEndpointName") in
      let channelName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelName") in
      let channelGroupName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?tags ?eTag ?errorMessage ?status ?modifiedAt ?createdAt ?arn
        ?scheduleConfiguration ?description ?harvestedManifests
        ?harvestJobName ?destination ?originEndpointName ?channelName
        ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let errorMessage = field_map json__ "ErrorMessage" String_.of_json in
      let status = field_map json__ "Status" HarvestJobStatus.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let scheduleConfiguration =
        field_map json__ "ScheduleConfiguration"
          HarvesterScheduleConfiguration.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let harvestedManifests =
        field_map json__ "HarvestedManifests" HarvestedManifests.of_json in
      let harvestJobName =
        field_map json__ "HarvestJobName" ResourceName.of_json in
      let destination = field_map json__ "Destination" Destination.of_json in
      let originEndpointName =
        field_map json__ "OriginEndpointName" ResourceName.of_json in
      let channelName = field_map json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" ResourceName.of_json in
      make ?tags ?eTag ?errorMessage ?status ?modifiedAt ?createdAt ?arn
        ?scheduleConfiguration ?description ?harvestedManifests
        ?harvestJobName ?destination ?originEndpointName ?channelName
        ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The response object returned after creating a harvest job."]
module CreateHarvestJobRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name of the channel group containing the channel from which to harvest content."];
      channelName: ResourceName.t
        [@ocaml.doc "The name of the channel from which to harvest content."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name of the origin endpoint from which to harvest content."];
      description: ResourceDescription.t option
        [@ocaml.doc "An optional description for the harvest job."];
      harvestedManifests: HarvestedManifests.t
        [@ocaml.doc "A list of manifests to be harvested."];
      scheduleConfiguration: HarvesterScheduleConfiguration.t
        [@ocaml.doc
          "The configuration for when the harvest job should run, including start and end times."];
      destination: Destination.t
        [@ocaml.doc
          "The S3 destination where the harvested content will be placed."];
      clientToken: IdempotencyToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      harvestJobName: ResourceName.t option
        [@ocaml.doc
          "A name for the harvest job. This name must be unique within the channel."];
      tags: TagMap.t option
        [@ocaml.doc "A collection of tags associated with the harvest job."]}
    let context_ = "CreateHarvestJobRequest"
    let make ?description =
      fun ?clientToken ->
        fun ?harvestJobName ->
          fun ?tags ->
            fun ~channelGroupName ->
              fun ~channelName ->
                fun ~originEndpointName ->
                  fun ~harvestedManifests ->
                    fun ~scheduleConfiguration ->
                      fun ~destination ->
                        fun () ->
                          {
                            description;
                            clientToken;
                            harvestJobName;
                            tags;
                            channelGroupName;
                            channelName;
                            originEndpointName;
                            harvestedManifests;
                            scheduleConfiguration;
                            destination
                          }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("HarvestedManifests",
          (Some (HarvestedManifests.to_value x.harvestedManifests)));
        ("ScheduleConfiguration",
          (Some
             (HarvesterScheduleConfiguration.to_value x.scheduleConfiguration)));
        ("Destination", (Some (Destination.to_value x.destination)));
        ("x-amzn-client-token",
          (Option.map x.clientToken ~f:IdempotencyToken.to_value));
        ("HarvestJobName",
          (Option.map x.harvestJobName ~f:ResourceName.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let harvestJobName =
        (Option.map ~f:ResourceName.of_xml)
          (Xml.child xml_arg0 "HarvestJobName") in
      let clientToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "x-amzn-client-token") in
      let destination =
        Destination.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Destination") in
      let scheduleConfiguration =
        HarvesterScheduleConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScheduleConfiguration") in
      let harvestedManifests =
        HarvestedManifests.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HarvestedManifests") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?tags ?harvestJobName ?clientToken ~destination
        ~scheduleConfiguration ~harvestedManifests ?description
        ~originEndpointName ~channelName ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let harvestJobName =
        field_map json__ "HarvestJobName" ResourceName.of_json in
      let clientToken =
        field_map json__ "ClientToken" IdempotencyToken.of_json in
      let destination =
        field_map_exn json__ "Destination" Destination.of_json in
      let scheduleConfiguration =
        field_map_exn json__ "ScheduleConfiguration"
          HarvesterScheduleConfiguration.of_json in
      let harvestedManifests =
        field_map_exn json__ "HarvestedManifests" HarvestedManifests.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?tags ?harvestJobName ?clientToken ~destination
        ~scheduleConfiguration ~harvestedManifests ?description
        ~originEndpointName ~channelName ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request object for creating a new harvest job."]
module CreateChannelResponse =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      channelName: String_.t option
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group."];
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel was modified."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description for your channel."];
      ingestEndpoints: IngestEndpointList.t option ;
      inputType: InputType.t option
        [@ocaml.doc
          "The input type will be an immutable field which will be used to define whether the channel will allow CMAF ingest or HLS ingest. If unprovided, it will default to HLS to preserve current behavior. The allowed values are: HLS - The HLS streaming specification (which defines M3U8 manifests and TS segments). CMAF - The DASH-IF CMAF Ingest specification (which defines CMAF segments with optional DASH manifests)."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the channel."];
      inputSwitchConfiguration: InputSwitchConfiguration.t option
        [@ocaml.doc
          "The configuration for input switching based on the media quality confidence score (MQCS) as provided from AWS Elemental MediaLive. This setting is valid only when InputType is CMAF."];
      outputHeaderConfiguration: OutputHeaderConfiguration.t option
        [@ocaml.doc
          "The settings for what common media server data (CMSD) headers AWS Elemental MediaPackage includes in responses to the CDN. This setting is valid only when InputType is CMAF."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?channelName ->
        fun ?channelGroupName ->
          fun ?createdAt ->
            fun ?modifiedAt ->
              fun ?description ->
                fun ?ingestEndpoints ->
                  fun ?inputType ->
                    fun ?eTag ->
                      fun ?tags ->
                        fun ?inputSwitchConfiguration ->
                          fun ?outputHeaderConfiguration ->
                            fun () ->
                              {
                                arn;
                                channelName;
                                channelGroupName;
                                createdAt;
                                modifiedAt;
                                description;
                                ingestEndpoints;
                                inputType;
                                eTag;
                                tags;
                                inputSwitchConfiguration;
                                outputHeaderConfiguration
                              }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:String_.to_value));
        ("ChannelName", (Option.map x.channelName ~f:String_.to_value));
        ("ChannelGroupName",
          (Option.map x.channelGroupName ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("IngestEndpoints",
          (Option.map x.ingestEndpoints ~f:IngestEndpointList.to_value));
        ("InputType", (Option.map x.inputType ~f:InputType.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value));
        ("InputSwitchConfiguration",
          (Option.map x.inputSwitchConfiguration
             ~f:InputSwitchConfiguration.to_value));
        ("OutputHeaderConfiguration",
          (Option.map x.outputHeaderConfiguration
             ~f:OutputHeaderConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputHeaderConfiguration =
        (Option.map ~f:OutputHeaderConfiguration.of_xml)
          (Xml.child xml_arg0 "OutputHeaderConfiguration") in
      let inputSwitchConfiguration =
        (Option.map ~f:InputSwitchConfiguration.of_xml)
          (Xml.child xml_arg0 "InputSwitchConfiguration") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let inputType =
        (Option.map ~f:InputType.of_xml) (Xml.child xml_arg0 "InputType") in
      let ingestEndpoints =
        (Option.map ~f:IngestEndpointList.of_xml)
          (Xml.child xml_arg0 "IngestEndpoints") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      let channelName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ChannelName") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?outputHeaderConfiguration ?inputSwitchConfiguration ?tags ?eTag
        ?inputType ?ingestEndpoints ?description ?modifiedAt ?createdAt
        ?channelGroupName ?channelName ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputHeaderConfiguration =
        field_map json__ "OutputHeaderConfiguration"
          OutputHeaderConfiguration.of_json in
      let inputSwitchConfiguration =
        field_map json__ "InputSwitchConfiguration"
          InputSwitchConfiguration.of_json in
      let tags = field_map json__ "Tags" TagMap.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let inputType = field_map json__ "InputType" InputType.of_json in
      let ingestEndpoints =
        field_map json__ "IngestEndpoints" IngestEndpointList.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      let channelName = field_map json__ "ChannelName" String_.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      make ?outputHeaderConfiguration ?inputSwitchConfiguration ?tags ?eTag
        ?inputType ?ingestEndpoints ?description ?modifiedAt ?createdAt
        ?channelGroupName ?channelName ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a channel to start receiving content streams. The channel represents the input to MediaPackage for incoming live content from an encoder such as AWS Elemental MediaLive. The channel receives content, and after packaging it, outputs it through an origin endpoint to downstream devices (such as video players or CDNs) that request the content. You can create only one channel with each request. We recommend that you spread out channels between channel groups, such as putting redundant channels in the same AWS Region in different channel groups."]
module CreateChannelRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel. The name is the primary identifier for the channel, and must be unique for your account in the AWS Region and channel group. You can't change the name after you create the channel."];
      clientToken: IdempotencyToken.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure the idempotency of the request."];
      inputType: InputType.t option
        [@ocaml.doc
          "The input type will be an immutable field which will be used to define whether the channel will allow CMAF ingest or HLS ingest. If unprovided, it will default to HLS to preserve current behavior. The allowed values are: HLS - The HLS streaming specification (which defines M3U8 manifests and TS segments). CMAF - The DASH-IF CMAF Ingest specification (which defines CMAF segments with optional DASH manifests)."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Enter any descriptive text that helps you to identify the channel."];
      inputSwitchConfiguration: InputSwitchConfiguration.t option
        [@ocaml.doc
          "The configuration for input switching based on the media quality confidence score (MQCS) as provided from AWS Elemental MediaLive. This setting is valid only when InputType is CMAF."];
      outputHeaderConfiguration: OutputHeaderConfiguration.t option
        [@ocaml.doc
          "The settings for what common media server data (CMSD) headers AWS Elemental MediaPackage includes in responses to the CDN. This setting is valid only when InputType is CMAF."];
      tags: TagMap.t option
        [@ocaml.doc
          "A comma-separated list of tag key:value pairs that you define. For example: \"Key1\": \"Value1\", \"Key2\": \"Value2\""]}
    let context_ = "CreateChannelRequest"
    let make ?clientToken =
      fun ?inputType ->
        fun ?description ->
          fun ?inputSwitchConfiguration ->
            fun ?outputHeaderConfiguration ->
              fun ?tags ->
                fun ~channelGroupName ->
                  fun ~channelName ->
                    fun () ->
                      {
                        clientToken;
                        inputType;
                        description;
                        inputSwitchConfiguration;
                        outputHeaderConfiguration;
                        tags;
                        channelGroupName;
                        channelName
                      }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("x-amzn-client-token",
          (Option.map x.clientToken ~f:IdempotencyToken.to_value));
        ("InputType", (Option.map x.inputType ~f:InputType.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("InputSwitchConfiguration",
          (Option.map x.inputSwitchConfiguration
             ~f:InputSwitchConfiguration.to_value));
        ("OutputHeaderConfiguration",
          (Option.map x.outputHeaderConfiguration
             ~f:OutputHeaderConfiguration.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let outputHeaderConfiguration =
        (Option.map ~f:OutputHeaderConfiguration.of_xml)
          (Xml.child xml_arg0 "OutputHeaderConfiguration") in
      let inputSwitchConfiguration =
        (Option.map ~f:InputSwitchConfiguration.of_xml)
          (Xml.child xml_arg0 "InputSwitchConfiguration") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let inputType =
        (Option.map ~f:InputType.of_xml) (Xml.child xml_arg0 "InputType") in
      let clientToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "x-amzn-client-token") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?tags ?outputHeaderConfiguration ?inputSwitchConfiguration
        ?description ?inputType ?clientToken ~channelName ~channelGroupName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let outputHeaderConfiguration =
        field_map json__ "OutputHeaderConfiguration"
          OutputHeaderConfiguration.of_json in
      let inputSwitchConfiguration =
        field_map json__ "InputSwitchConfiguration"
          InputSwitchConfiguration.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let inputType = field_map json__ "InputType" InputType.of_json in
      let clientToken =
        field_map json__ "ClientToken" IdempotencyToken.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?tags ?outputHeaderConfiguration ?inputSwitchConfiguration
        ?description ?inputType ?clientToken ~channelName ~channelGroupName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a channel to start receiving content streams. The channel represents the input to MediaPackage for incoming live content from an encoder such as AWS Elemental MediaLive. The channel receives content, and after packaging it, outputs it through an origin endpoint to downstream devices (such as video players or CDNs) that request the content. You can create only one channel with each request. We recommend that you spread out channels between channel groups, such as putting redundant channels in the same AWS Region in different channel groups."]
module CreateChannelGroupResponse =
  struct
    type nonrec t =
      {
      channelGroupName: String_.t option
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) associated with the resource."];
      egressDomain: String_.t option
        [@ocaml.doc
          "The output domain where the source stream should be sent. Integrate the egress domain with a downstream CDN (such as Amazon CloudFront) or playback device."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel group was created."];
      modifiedAt: Timestamp.t option
        [@ocaml.doc "The date and time the channel group was modified."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with this resource. The entity tag can be used to safely make concurrent updates to the resource."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description for your channel group."];
      tags: TagMap.t option
        [@ocaml.doc
          "The comma-separated list of tag key:value pairs assigned to the channel group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channelGroupName =
      fun ?arn ->
        fun ?egressDomain ->
          fun ?createdAt ->
            fun ?modifiedAt ->
              fun ?eTag ->
                fun ?description ->
                  fun ?tags ->
                    fun () ->
                      {
                        channelGroupName;
                        arn;
                        egressDomain;
                        createdAt;
                        modifiedAt;
                        eTag;
                        description;
                        tags
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Option.map x.channelGroupName ~f:String_.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("EgressDomain", (Option.map x.egressDomain ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ModifiedAt", (Option.map x.modifiedAt ~f:Timestamp.to_value));
        ("ETag", (Option.map x.eTag ~f:EntityTag.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let eTag = (Option.map ~f:EntityTag.of_xml) (Xml.child xml_arg0 "ETag") in
      let modifiedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ModifiedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let egressDomain =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EgressDomain") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let channelGroupName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ChannelGroupName") in
      make ?tags ?description ?eTag ?modifiedAt ?createdAt ?egressDomain ?arn
        ?channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let modifiedAt = field_map json__ "ModifiedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let egressDomain = field_map json__ "EgressDomain" String_.of_json in
      let arn = field_map json__ "Arn" String_.of_json in
      let channelGroupName =
        field_map json__ "ChannelGroupName" String_.of_json in
      make ?tags ?description ?eTag ?modifiedAt ?createdAt ?egressDomain ?arn
        ?channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a channel group to group your channels and origin endpoints. A channel group is the top-level resource that consists of channels and origin endpoints that are associated with it and that provides predictable URLs for stream delivery. All channels and origin endpoints within the channel group are guaranteed to share the DNS. You can create only one channel group with each request."]
module CreateChannelGroupRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name that describes the channel group. The name is the primary identifier for the channel group, and must be unique for your account in the AWS Region. You can't use spaces in the name. You can't change the name after you create the channel group."];
      clientToken: IdempotencyToken.t option
        [@ocaml.doc
          "A unique, case-sensitive token that you provide to ensure the idempotency of the request."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "Enter any descriptive text that helps you to identify the channel group."];
      tags: TagMap.t option
        [@ocaml.doc
          "A comma-separated list of tag key:value pairs that you define. For example: \"Key1\": \"Value1\", \"Key2\": \"Value2\""]}
    let context_ = "CreateChannelGroupRequest"
    let make ?clientToken =
      fun ?description ->
        fun ?tags ->
          fun ~channelGroupName ->
            fun () -> { clientToken; description; tags; channelGroupName }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("x-amzn-client-token",
          (Option.map x.clientToken ~f:IdempotencyToken.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let clientToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "x-amzn-client-token") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?tags ?description ?clientToken ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" TagMap.of_json in
      let description =
        field_map json__ "Description" ResourceDescription.of_json in
      let clientToken =
        field_map json__ "ClientToken" IdempotencyToken.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?tags ?description ?clientToken ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Create a channel group to group your channels and origin endpoints. A channel group is the top-level resource that consists of channels and origin endpoints that are associated with it and that provides predictable URLs for stream delivery. All channels and origin endpoints within the channel group are guaranteed to share the DNS. You can create only one channel group with each request."]
module CancelHarvestJobResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Cancels an in-progress harvest job."]
module CancelHarvestJobRequest =
  struct
    type nonrec t =
      {
      channelGroupName: ResourceName.t
        [@ocaml.doc
          "The name of the channel group containing the channel from which the harvest job is running."];
      channelName: ResourceName.t
        [@ocaml.doc
          "The name of the channel from which the harvest job is running."];
      originEndpointName: ResourceName.t
        [@ocaml.doc
          "The name of the origin endpoint that the harvest job is harvesting from. This cannot be changed after the harvest job is submitted."];
      harvestJobName: ResourceName.t
        [@ocaml.doc
          "The name of the harvest job to cancel. This name must be unique within the channel and cannot be changed after the harvest job is submitted."];
      eTag: EntityTag.t option
        [@ocaml.doc
          "The current Entity Tag (ETag) associated with the harvest job. Used for concurrency control."]}
    let context_ = "CancelHarvestJobRequest"
    let make ?eTag =
      fun ~channelGroupName ->
        fun ~channelName ->
          fun ~originEndpointName ->
            fun ~harvestJobName ->
              fun () ->
                {
                  eTag;
                  channelGroupName;
                  channelName;
                  originEndpointName;
                  harvestJobName
                }
    let to_value x =
      structure_to_value
        [("ChannelGroupName",
           (Some (ResourceName.to_value x.channelGroupName)));
        ("ChannelName", (Some (ResourceName.to_value x.channelName)));
        ("OriginEndpointName",
          (Some (ResourceName.to_value x.originEndpointName)));
        ("HarvestJobName", (Some (ResourceName.to_value x.harvestJobName)));
        ("x-amzn-update-if-match", (Option.map x.eTag ~f:EntityTag.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eTag =
        (Option.map ~f:EntityTag.of_xml)
          (Xml.child xml_arg0 "x-amzn-update-if-match") in
      let harvestJobName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HarvestJobName") in
      let originEndpointName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OriginEndpointName") in
      let channelName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelName") in
      let channelGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChannelGroupName") in
      make ?eTag ~harvestJobName ~originEndpointName ~channelName
        ~channelGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eTag = field_map json__ "ETag" EntityTag.of_json in
      let harvestJobName =
        field_map_exn json__ "HarvestJobName" ResourceName.of_json in
      let originEndpointName =
        field_map_exn json__ "OriginEndpointName" ResourceName.of_json in
      let channelName =
        field_map_exn json__ "ChannelName" ResourceName.of_json in
      let channelGroupName =
        field_map_exn json__ "ChannelGroupName" ResourceName.of_json in
      make ?eTag ~harvestJobName ~originEndpointName ~channelName
        ~channelGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Cancels an in-progress harvest job."]
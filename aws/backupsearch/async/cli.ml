(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_search_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchJobIdentifier =
         flag "search-job-identifier" (required string)
           ~doc:"STRING GenericId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_search_job
           (Values.GetSearchJobInput.make ~searchJobIdentifier ())
           (Some Values.GetSearchJobOutput.to_json)
           (Some Values.GetSearchJobOutput.error_to_json)])
let get_search_result_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportJobIdentifier =
         flag "export-job-identifier" (required string)
           ~doc:"STRING GenericId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_search_result_export_job
           (Values.GetSearchResultExportJobInput.make ~exportJobIdentifier ())
           (Some Values.GetSearchResultExportJobOutput.to_json)
           (Some Values.GetSearchResultExportJobOutput.error_to_json)])
let list_search_job_backups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSearchJobBackupsInputMaxResultsInteger"
       and searchJobIdentifier =
         flag "search-job-identifier" (required string)
           ~doc:"STRING GenericId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_search_job_backups
           (Values.ListSearchJobBackupsInput.make ?nextToken ?maxResults
              ~searchJobIdentifier ())
           (Some Values.ListSearchJobBackupsOutput.to_json)
           (Some Values.ListSearchJobBackupsOutput.error_to_json)])
let list_search_job_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSearchJobResultsInputMaxResultsInteger"
       and searchJobIdentifier =
         flag "search-job-identifier" (required string)
           ~doc:"STRING GenericId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_search_job_results
           (Values.ListSearchJobResultsInput.make ?nextToken ?maxResults
              ~searchJobIdentifier ())
           (Some Values.ListSearchJobResultsOutput.to_json)
           (Some Values.ListSearchJobResultsOutput.error_to_json)])
let list_search_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and byStatus =
         flag "by-status" (optional json_arg) ~doc:"JSON SearchJobState"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSearchJobsInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_search_jobs
           (Values.ListSearchJobsInput.make
              ?byStatus:(Option.map ~f:Values.SearchJobState.of_json byStatus)
              ?nextToken ?maxResults ())
           (Some Values.ListSearchJobsOutput.to_json)
           (Some Values.ListSearchJobsOutput.error_to_json)])
let list_search_result_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ExportJobStatus"
       and searchJobIdentifier =
         flag "search-job-identifier" (optional string)
           ~doc:"STRING GenericId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSearchResultExportJobsInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_search_result_export_jobs
           (Values.ListSearchResultExportJobsInput.make
              ?status:(Option.map ~f:Values.ExportJobStatus.of_json status)
              ?searchJobIdentifier ?nextToken ?maxResults ())
           (Some Values.ListSearchResultExportJobsOutput.to_json)
           (Some Values.ListSearchResultExportJobsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_search_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (optional string)
           ~doc:"STRING StartSearchJobInputNameString"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string)
           ~doc:"STRING EncryptionKeyArn"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and itemFilters =
         flag "item-filters" (optional json_arg) ~doc:"JSON ItemFilters"
       and searchScope =
         flag "search-scope" (required json_arg) ~doc:"JSON SearchScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_search_job
           (Values.StartSearchJobInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?name
              ?encryptionKeyArn ?clientToken
              ?itemFilters:(Option.map ~f:Values.ItemFilters.of_json
                              itemFilters)
              ~searchScope:(Values.SearchScope.of_json searchScope) ())
           (Some Values.StartSearchJobOutput.to_json)
           (Some Values.StartSearchJobOutput.error_to_json)])
let start_search_result_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and searchJobIdentifier =
         flag "search-job-identifier" (required string)
           ~doc:"STRING GenericId"
       and exportSpecification =
         flag "export-specification" (required json_arg)
           ~doc:"JSON ExportSpecification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_search_result_export_job
           (Values.StartSearchResultExportJobInput.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?roleArn
              ~searchJobIdentifier
              ~exportSpecification:(Values.ExportSpecification.of_json
                                      exportSpecification) ())
           (Some Values.StartSearchResultExportJobOutput.to_json)
           (Some Values.StartSearchResultExportJobOutput.error_to_json)])
let stop_search_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchJobIdentifier =
         flag "search-job-identifier" (required string)
           ~doc:"STRING GenericId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_search_job
           (Values.StopSearchJobInput.make ~searchJobIdentifier ())
           (Some Values.StopSearchJobOutput.to_json)
           (Some Values.StopSearchJobOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-search-job", get_search_job);
    ("get-search-result-export-job", get_search_result_export_job);
    ("list-search-job-backups", list_search_job_backups);
    ("list-search-job-results", list_search_job_results);
    ("list-search-jobs", list_search_jobs);
    ("list-search-result-export-jobs", list_search_result_export_jobs);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-search-job", start_search_job);
    ("start-search-result-export-job", start_search_result_export_job);
    ("stop-search-job", stop_search_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_batch_job_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authSecretsManagerArn =
         flag "auth-secrets-manager-arn" (optional string)
           ~doc:"STRING AuthSecretsManagerArn"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_batch_job_execution
           (Values.CancelBatchJobExecutionRequest.make ?authSecretsManagerArn
              ~applicationId ~executionId ())
           (Some Values.CancelBatchJobExecutionResponse.to_json)
           (Some Values.CancelBatchJobExecutionResponse.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING EntityDescription"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING Arn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON Definition"
       and engineType =
         flag "engine-type" (required json_arg) ~doc:"JSON EngineType"
       and name = flag "name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make ?clientToken ?description
              ?kmsKeyId ?roleArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~definition:(Values.Definition.of_json definition)
              ~engineType:(Values.EngineType.of_json engineType) ~name ())
           (Some Values.CreateApplicationResponse.to_json)
           (Some Values.CreateApplicationResponse.error_to_json)])
let create_data_set_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KMSKeyId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and exportConfig =
         flag "export-config" (required json_arg)
           ~doc:"JSON DataSetExportConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_set_export_task
           (Values.CreateDataSetExportTaskRequest.make ?clientToken ?kmsKeyId
              ~applicationId
              ~exportConfig:(Values.DataSetExportConfig.of_json exportConfig)
              ()) (Some Values.CreateDataSetExportTaskResponse.to_json)
           (Some Values.CreateDataSetExportTaskResponse.error_to_json)])
let create_data_set_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and importConfig =
         flag "import-config" (required json_arg)
           ~doc:"JSON DataSetImportConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_set_import_task
           (Values.CreateDataSetImportTaskRequest.make ?clientToken
              ~applicationId
              ~importConfig:(Values.DataSetImportConfig.of_json importConfig)
              ()) (Some Values.CreateDataSetImportTaskResponse.to_json)
           (Some Values.CreateDataSetImportTaskResponse.error_to_json)])
let create_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and applicationVersion =
         flag "application-version" (required int) ~doc:"INT Version"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment
           (Values.CreateDeploymentRequest.make ?clientToken ~applicationId
              ~applicationVersion ~environmentId ())
           (Some Values.CreateDeploymentResponse.to_json)
           (Some Values.CreateDeploymentResponse.error_to_json)])
let create_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING EntityDescription"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING EngineVersion"
       and highAvailabilityConfig =
         flag "high-availability-config" (optional json_arg)
           ~doc:"JSON HighAvailabilityConfig"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String50"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool) ~doc:"BOOL Boolean"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON String50List"
       and storageConfigurations =
         flag "storage-configurations" (optional json_arg)
           ~doc:"JSON StorageConfigurationList"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON String50List"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and engineType =
         flag "engine-type" (required json_arg) ~doc:"JSON EngineType"
       and instanceType =
         flag "instance-type" (required string) ~doc:"STRING String20"
       and name = flag "name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment
           (Values.CreateEnvironmentRequest.make ?clientToken ?description
              ?engineVersion
              ?highAvailabilityConfig:(Option.map
                                         ~f:Values.HighAvailabilityConfig.of_json
                                         highAvailabilityConfig) ?kmsKeyId
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType) ?preferredMaintenanceWindow
              ?publiclyAccessible
              ?securityGroupIds:(Option.map ~f:Values.String50List.of_json
                                   securityGroupIds)
              ?storageConfigurations:(Option.map
                                        ~f:Values.StorageConfigurationList.of_json
                                        storageConfigurations)
              ?subnetIds:(Option.map ~f:Values.String50List.of_json subnetIds)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~engineType:(Values.EngineType.of_json engineType)
              ~instanceType ~name ())
           (Some Values.CreateEnvironmentResponse.to_json)
           (Some Values.CreateEnvironmentResponse.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationRequest.make ~applicationId ())
           (Some Values.DeleteApplicationResponse.to_json)
           (Some Values.DeleteApplicationResponse.error_to_json)])
let delete_application_from_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application_from_environment
           (Values.DeleteApplicationFromEnvironmentRequest.make
              ~applicationId ~environmentId ())
           (Some Values.DeleteApplicationFromEnvironmentResponse.to_json)
           (Some
              Values.DeleteApplicationFromEnvironmentResponse.error_to_json)])
let delete_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment
           (Values.DeleteEnvironmentRequest.make ~environmentId ())
           (Some Values.DeleteEnvironmentResponse.to_json)
           (Some Values.DeleteEnvironmentResponse.error_to_json)])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationRequest.make ~applicationId ())
           (Some Values.GetApplicationResponse.to_json)
           (Some Values.GetApplicationResponse.error_to_json)])
let get_application_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and applicationVersion =
         flag "application-version" (required int) ~doc:"INT Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application_version
           (Values.GetApplicationVersionRequest.make ~applicationId
              ~applicationVersion ())
           (Some Values.GetApplicationVersionResponse.to_json)
           (Some Values.GetApplicationVersionResponse.error_to_json)])
let get_batch_job_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_batch_job_execution
           (Values.GetBatchJobExecutionRequest.make ~applicationId
              ~executionId ())
           (Some Values.GetBatchJobExecutionResponse.to_json)
           (Some Values.GetBatchJobExecutionResponse.error_to_json)])
let get_data_set_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and dataSetName =
         flag "data-set-name" (required string) ~doc:"STRING String200" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_set_details
           (Values.GetDataSetDetailsRequest.make ~applicationId ~dataSetName
              ()) (Some Values.GetDataSetDetailsResponse.to_json)
           (Some Values.GetDataSetDetailsResponse.error_to_json)])
let get_data_set_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and taskId = flag "task-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_set_export_task
           (Values.GetDataSetExportTaskRequest.make ~applicationId ~taskId ())
           (Some Values.GetDataSetExportTaskResponse.to_json)
           (Some Values.GetDataSetExportTaskResponse.error_to_json)])
let get_data_set_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and taskId = flag "task-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_set_import_task
           (Values.GetDataSetImportTaskRequest.make ~applicationId ~taskId ())
           (Some Values.GetDataSetImportTaskResponse.to_json)
           (Some Values.GetDataSetImportTaskResponse.error_to_json)])
let get_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployment
           (Values.GetDeploymentRequest.make ~applicationId ~deploymentId ())
           (Some Values.GetDeploymentResponse.to_json)
           (Some Values.GetDeploymentResponse.error_to_json)])
let get_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment
           (Values.GetEnvironmentRequest.make ~environmentId ())
           (Some Values.GetEnvironmentResponse.to_json)
           (Some Values.GetEnvironmentResponse.error_to_json)])
let get_signed_bluinsights_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_signed_bluinsights_url (Fn.id ())
           (Some Values.GetSignedBluinsightsUrlResponse.to_json)
           (Some Values.GetSignedBluinsightsUrlResponse.error_to_json)])
let list_application_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_application_versions
           (Values.ListApplicationVersionsRequest.make ?maxResults ?nextToken
              ~applicationId ())
           (Some Values.ListApplicationVersionsResponse.to_json)
           (Some Values.ListApplicationVersionsResponse.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING Identifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and names =
         flag "names" (optional json_arg) ~doc:"JSON EntityNameList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsRequest.make ?environmentId ?maxResults
              ?names:(Option.map ~f:Values.EntityNameList.of_json names)
              ?nextToken ()) (Some Values.ListApplicationsResponse.to_json)
           (Some Values.ListApplicationsResponse.error_to_json)])
let list_batch_job_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and prefix = flag "prefix" (optional string) ~doc:"STRING String"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_batch_job_definitions
           (Values.ListBatchJobDefinitionsRequest.make ?maxResults ?nextToken
              ?prefix ~applicationId ())
           (Some Values.ListBatchJobDefinitionsResponse.to_json)
           (Some Values.ListBatchJobDefinitionsResponse.error_to_json)])
let list_batch_job_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and executionIds =
         flag "execution-ids" (optional json_arg) ~doc:"JSON IdentifierList"
       and jobName =
         flag "job-name" (optional string) ~doc:"STRING String100"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and startedAfter =
         flag "started-after" (optional json_arg) ~doc:"JSON Timestamp"
       and startedBefore =
         flag "started-before" (optional json_arg) ~doc:"JSON Timestamp"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON BatchJobExecutionStatus"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_batch_job_executions
           (Values.ListBatchJobExecutionsRequest.make
              ?executionIds:(Option.map ~f:Values.IdentifierList.of_json
                               executionIds) ?jobName ?maxResults ?nextToken
              ?startedAfter:(Option.map ~f:Values.Timestamp.of_json
                               startedAfter)
              ?startedBefore:(Option.map ~f:Values.Timestamp.of_json
                                startedBefore)
              ?status:(Option.map ~f:Values.BatchJobExecutionStatus.of_json
                         status) ~applicationId ())
           (Some Values.ListBatchJobExecutionsResponse.to_json)
           (Some Values.ListBatchJobExecutionsResponse.error_to_json)])
let list_batch_job_restart_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authSecretsManagerArn =
         flag "auth-secrets-manager-arn" (optional string)
           ~doc:"STRING AuthSecretsManagerArn"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_batch_job_restart_points
           (Values.ListBatchJobRestartPointsRequest.make
              ?authSecretsManagerArn ~applicationId ~executionId ())
           (Some Values.ListBatchJobRestartPointsResponse.to_json)
           (Some Values.ListBatchJobRestartPointsResponse.error_to_json)])
let list_data_set_export_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_set_export_history
           (Values.ListDataSetExportHistoryRequest.make ?maxResults
              ?nextToken ~applicationId ())
           (Some Values.ListDataSetExportHistoryResponse.to_json)
           (Some Values.ListDataSetExportHistoryResponse.error_to_json)])
let list_data_set_import_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_set_import_history
           (Values.ListDataSetImportHistoryRequest.make ?maxResults
              ?nextToken ~applicationId ())
           (Some Values.ListDataSetImportHistoryResponse.to_json)
           (Some Values.ListDataSetImportHistoryResponse.error_to_json)])
let list_data_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nameFilter =
         flag "name-filter" (optional string) ~doc:"STRING String200"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and prefix = flag "prefix" (optional string) ~doc:"STRING String200"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_sets
           (Values.ListDataSetsRequest.make ?maxResults ?nameFilter
              ?nextToken ?prefix ~applicationId ())
           (Some Values.ListDataSetsResponse.to_json)
           (Some Values.ListDataSetsResponse.error_to_json)])
let list_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deployments
           (Values.ListDeploymentsRequest.make ?maxResults ?nextToken
              ~applicationId ())
           (Some Values.ListDeploymentsResponse.to_json)
           (Some Values.ListDeploymentsResponse.error_to_json)])
let list_engine_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineType =
         flag "engine-type" (optional json_arg) ~doc:"JSON EngineType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engine_versions
           (Values.ListEngineVersionsRequest.make
              ?engineType:(Option.map ~f:Values.EngineType.of_json engineType)
              ?maxResults ?nextToken ())
           (Some Values.ListEngineVersionsResponse.to_json)
           (Some Values.ListEngineVersionsResponse.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineType =
         flag "engine-type" (optional json_arg) ~doc:"JSON EngineType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and names =
         flag "names" (optional json_arg) ~doc:"JSON EntityNameList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsRequest.make
              ?engineType:(Option.map ~f:Values.EngineType.of_json engineType)
              ?maxResults
              ?names:(Option.map ~f:Values.EntityNameList.of_json names)
              ?nextToken ()) (Some Values.ListEnvironmentsResponse.to_json)
           (Some Values.ListEnvironmentsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_application
           (Values.StartApplicationRequest.make ~applicationId ())
           (Some Values.StartApplicationResponse.to_json)
           (Some Values.StartApplicationResponse.error_to_json)])
let start_batch_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authSecretsManagerArn =
         flag "auth-secrets-manager-arn" (optional string)
           ~doc:"STRING AuthSecretsManagerArn"
       and jobParams =
         flag "job-params" (optional json_arg)
           ~doc:"JSON BatchJobParametersMap"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and batchJobIdentifier =
         flag "batch-job-identifier" (required json_arg)
           ~doc:"JSON BatchJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_batch_job
           (Values.StartBatchJobRequest.make ?authSecretsManagerArn
              ?jobParams:(Option.map ~f:Values.BatchJobParametersMap.of_json
                            jobParams) ~applicationId
              ~batchJobIdentifier:(Values.BatchJobIdentifier.of_json
                                     batchJobIdentifier) ())
           (Some Values.StartBatchJobResponse.to_json)
           (Some Values.StartBatchJobResponse.error_to_json)])
let stop_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceStop = flag "force-stop" (optional bool) ~doc:"BOOL Boolean"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_application
           (Values.StopApplicationRequest.make ?forceStop ~applicationId ())
           (Some Values.StopApplicationResponse.to_json)
           (Some Values.StopApplicationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and definition =
         flag "definition" (optional json_arg) ~doc:"JSON Definition"
       and description =
         flag "description" (optional string) ~doc:"STRING EntityDescription"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING Identifier"
       and currentApplicationVersion =
         flag "current-application-version" (required int) ~doc:"INT Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationRequest.make
              ?definition:(Option.map ~f:Values.Definition.of_json definition)
              ?description ~applicationId ~currentApplicationVersion ())
           (Some Values.UpdateApplicationResponse.to_json)
           (Some Values.UpdateApplicationResponse.error_to_json)])
let update_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applyDuringMaintenanceWindow =
         flag "apply-during-maintenance-window" (optional bool)
           ~doc:"BOOL Boolean"
       and desiredCapacity =
         flag "desired-capacity" (optional int) ~doc:"INT CapacityValue"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING EngineVersion"
       and forceUpdate =
         flag "force-update" (optional bool) ~doc:"BOOL Boolean"
       and instanceType =
         flag "instance-type" (optional string) ~doc:"STRING String20"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment
           (Values.UpdateEnvironmentRequest.make
              ?applyDuringMaintenanceWindow ?desiredCapacity ?engineVersion
              ?forceUpdate ?instanceType ?preferredMaintenanceWindow
              ~environmentId ())
           (Some Values.UpdateEnvironmentResponse.to_json)
           (Some Values.UpdateEnvironmentResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-batch-job-execution", cancel_batch_job_execution);
    ("create-application", create_application);
    ("create-data-set-export-task", create_data_set_export_task);
    ("create-data-set-import-task", create_data_set_import_task);
    ("create-deployment", create_deployment);
    ("create-environment", create_environment);
    ("delete-application", delete_application);
    ("delete-application-from-environment",
      delete_application_from_environment);
    ("delete-environment", delete_environment);
    ("get-application", get_application);
    ("get-application-version", get_application_version);
    ("get-batch-job-execution", get_batch_job_execution);
    ("get-data-set-details", get_data_set_details);
    ("get-data-set-export-task", get_data_set_export_task);
    ("get-data-set-import-task", get_data_set_import_task);
    ("get-deployment", get_deployment);
    ("get-environment", get_environment);
    ("get-signed-bluinsights-url", get_signed_bluinsights_url);
    ("list-application-versions", list_application_versions);
    ("list-applications", list_applications);
    ("list-batch-job-definitions", list_batch_job_definitions);
    ("list-batch-job-executions", list_batch_job_executions);
    ("list-batch-job-restart-points", list_batch_job_restart_points);
    ("list-data-set-export-history", list_data_set_export_history);
    ("list-data-set-import-history", list_data_set_import_history);
    ("list-data-sets", list_data_sets);
    ("list-deployments", list_deployments);
    ("list-engine-versions", list_engine_versions);
    ("list-environments", list_environments);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-application", start_application);
    ("start-batch-job", start_batch_job);
    ("stop-application", stop_application);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-application", update_application);
    ("update-environment", update_environment)]
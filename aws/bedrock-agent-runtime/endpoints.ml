(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateInvocation: (CreateInvocationRequest.t, CreateInvocationResponse.t,
  CreateInvocationResponse.error) t 
  | CreateSession: (CreateSessionRequest.t, CreateSessionResponse.t,
  CreateSessionResponse.error) t 
  | DeleteAgentMemory: (DeleteAgentMemoryRequest.t,
  DeleteAgentMemoryResponse.t, DeleteAgentMemoryResponse.error) t 
  | DeleteSession: (DeleteSessionRequest.t, DeleteSessionResponse.t,
  DeleteSessionResponse.error) t 
  | EndSession: (EndSessionRequest.t, EndSessionResponse.t,
  EndSessionResponse.error) t 
  | GenerateQuery: (GenerateQueryRequest.t, GenerateQueryResponse.t,
  GenerateQueryResponse.error) t 
  | GetAgentMemory: (GetAgentMemoryRequest.t, GetAgentMemoryResponse.t,
  GetAgentMemoryResponse.error) t 
  | GetExecutionFlowSnapshot: (GetExecutionFlowSnapshotRequest.t,
  GetExecutionFlowSnapshotResponse.t, GetExecutionFlowSnapshotResponse.error)
  t 
  | GetFlowExecution: (GetFlowExecutionRequest.t, GetFlowExecutionResponse.t,
  GetFlowExecutionResponse.error) t 
  | GetInvocationStep: (GetInvocationStepRequest.t,
  GetInvocationStepResponse.t, GetInvocationStepResponse.error) t 
  | GetSession: (GetSessionRequest.t, GetSessionResponse.t,
  GetSessionResponse.error) t 
  | InvokeAgent: (InvokeAgentRequest.t, InvokeAgentResponse.t,
  InvokeAgentResponse.error) t 
  | InvokeFlow: (InvokeFlowRequest.t, InvokeFlowResponse.t,
  InvokeFlowResponse.error) t 
  | InvokeInlineAgent: (InvokeInlineAgentRequest.t,
  InvokeInlineAgentResponse.t, InvokeInlineAgentResponse.error) t 
  | ListFlowExecutionEvents: (ListFlowExecutionEventsRequest.t,
  ListFlowExecutionEventsResponse.t, ListFlowExecutionEventsResponse.error) t
  
  | ListFlowExecutions: (ListFlowExecutionsRequest.t,
  ListFlowExecutionsResponse.t, ListFlowExecutionsResponse.error) t 
  | ListInvocationSteps: (ListInvocationStepsRequest.t,
  ListInvocationStepsResponse.t, ListInvocationStepsResponse.error) t 
  | ListInvocations: (ListInvocationsRequest.t, ListInvocationsResponse.t,
  ListInvocationsResponse.error) t 
  | ListSessions: (ListSessionsRequest.t, ListSessionsResponse.t,
  ListSessionsResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | OptimizePrompt: (OptimizePromptRequest.t, OptimizePromptResponse.t,
  OptimizePromptResponse.error) t 
  | PutInvocationStep: (PutInvocationStepRequest.t,
  PutInvocationStepResponse.t, PutInvocationStepResponse.error) t 
  | Rerank: (RerankRequest.t, RerankResponse.t, RerankResponse.error) t 
  | Retrieve: (RetrieveRequest.t, RetrieveResponse.t, RetrieveResponse.error)
  t 
  | RetrieveAndGenerate: (RetrieveAndGenerateRequest.t,
  RetrieveAndGenerateResponse.t, RetrieveAndGenerateResponse.error) t 
  | RetrieveAndGenerateStream: (RetrieveAndGenerateStreamRequest.t,
  RetrieveAndGenerateStreamResponse.t,
  RetrieveAndGenerateStreamResponse.error) t 
  | StartFlowExecution: (StartFlowExecutionRequest.t,
  StartFlowExecutionResponse.t, StartFlowExecutionResponse.error) t 
  | StopFlowExecution: (StopFlowExecutionRequest.t,
  StopFlowExecutionResponse.t, StopFlowExecutionResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateSession: (UpdateSessionRequest.t, UpdateSessionResponse.t,
  UpdateSessionResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateInvocation -> `PUT
  | CreateSession -> `PUT
  | DeleteAgentMemory -> `DELETE
  | DeleteSession -> `DELETE
  | EndSession -> `PATCH
  | GenerateQuery -> `POST
  | GetAgentMemory -> `GET
  | GetExecutionFlowSnapshot -> `GET
  | GetFlowExecution -> `GET
  | GetInvocationStep -> `POST
  | GetSession -> `GET
  | InvokeAgent -> `POST
  | InvokeFlow -> `POST
  | InvokeInlineAgent -> `POST
  | ListFlowExecutionEvents -> `GET
  | ListFlowExecutions -> `GET
  | ListInvocationSteps -> `POST
  | ListInvocations -> `POST
  | ListSessions -> `POST
  | ListTagsForResource -> `GET
  | OptimizePrompt -> `POST
  | PutInvocationStep -> `PUT
  | Rerank -> `POST
  | Retrieve -> `POST
  | RetrieveAndGenerate -> `POST
  | RetrieveAndGenerateStream -> `POST
  | StartFlowExecution -> `POST
  | StopFlowExecution -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateSession -> `PUT
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateInvocation ->
          (Format.kasprintf Uri.of_string) "/sessions/%s/invocations/"
            (SessionIdentifier.to_header
               x.CreateInvocationRequest.sessionIdentifier)
      | CreateSession -> (Format.kasprintf Uri.of_string) "/sessions/"
      | DeleteAgentMemory ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/agents/%s/agentAliases/%s/memories"
               (AgentId.to_header x.DeleteAgentMemoryRequest.agentId)
               (AgentAliasId.to_header
                  x.DeleteAgentMemoryRequest.agentAliasId))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("memoryId", (MemoryId.to_header v)))
                  x.memoryId;
               Option.map
                 ~f:(fun v -> ("sessionId", (SessionId.to_header v)))
                 x.sessionId])
      | DeleteSession ->
          (Format.kasprintf Uri.of_string) "/sessions/%s/"
            (SessionIdentifier.to_header
               x.DeleteSessionRequest.sessionIdentifier)
      | EndSession ->
          (Format.kasprintf Uri.of_string) "/sessions/%s"
            (SessionIdentifier.to_header
               x.EndSessionRequest.sessionIdentifier)
      | GenerateQuery -> (Format.kasprintf Uri.of_string) "/generateQuery"
      | GetAgentMemory ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/agents/%s/agentAliases/%s/memories"
               (AgentId.to_header x.GetAgentMemoryRequest.agentId)
               (AgentAliasId.to_header x.GetAgentMemoryRequest.agentAliasId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxItems", (MaxResults.to_header v)))
                  x.maxItems;
               Some ("memoryId", (MemoryId.to_header x.memoryId));
               Some ("memoryType", (MemoryType.to_header x.memoryType));
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | GetExecutionFlowSnapshot ->
          (Format.kasprintf Uri.of_string)
            "/flows/%s/aliases/%s/executions/%s/flowsnapshot"
            (FlowIdentifier.to_header
               x.GetExecutionFlowSnapshotRequest.flowIdentifier)
            (FlowAliasIdentifier.to_header
               x.GetExecutionFlowSnapshotRequest.flowAliasIdentifier)
            (FlowExecutionIdentifier.to_header
               x.GetExecutionFlowSnapshotRequest.executionIdentifier)
      | GetFlowExecution ->
          (Format.kasprintf Uri.of_string)
            "/flows/%s/aliases/%s/executions/%s"
            (FlowIdentifier.to_header
               x.GetFlowExecutionRequest.flowIdentifier)
            (FlowAliasIdentifier.to_header
               x.GetFlowExecutionRequest.flowAliasIdentifier)
            (FlowExecutionIdentifier.to_header
               x.GetFlowExecutionRequest.executionIdentifier)
      | GetInvocationStep ->
          (Format.kasprintf Uri.of_string) "/sessions/%s/invocationSteps/%s"
            (SessionIdentifier.to_header
               x.GetInvocationStepRequest.sessionIdentifier)
            (Uuid.to_header x.GetInvocationStepRequest.invocationStepId)
      | GetSession ->
          (Format.kasprintf Uri.of_string) "/sessions/%s/"
            (SessionIdentifier.to_header
               x.GetSessionRequest.sessionIdentifier)
      | InvokeAgent ->
          (Format.kasprintf Uri.of_string)
            "/agents/%s/agentAliases/%s/sessions/%s/text"
            (AgentId.to_header x.InvokeAgentRequest.agentId)
            (AgentAliasId.to_header x.InvokeAgentRequest.agentAliasId)
            (SessionId.to_header x.InvokeAgentRequest.sessionId)
      | InvokeFlow ->
          (Format.kasprintf Uri.of_string) "/flows/%s/aliases/%s"
            (FlowIdentifier.to_header x.InvokeFlowRequest.flowIdentifier)
            (FlowAliasIdentifier.to_header
               x.InvokeFlowRequest.flowAliasIdentifier)
      | InvokeInlineAgent ->
          (Format.kasprintf Uri.of_string) "/agents/%s"
            (SessionId.to_header x.InvokeInlineAgentRequest.sessionId)
      | ListFlowExecutionEvents ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/flows/%s/aliases/%s/executions/%s/events"
               (FlowIdentifier.to_header
                  x.ListFlowExecutionEventsRequest.flowIdentifier)
               (FlowAliasIdentifier.to_header
                  x.ListFlowExecutionEventsRequest.flowAliasIdentifier)
               (FlowExecutionIdentifier.to_header
                  x.ListFlowExecutionEventsRequest.executionIdentifier))
            (List.filter_opt
               [Some
                  ("eventType",
                    (FlowExecutionEventType.to_header x.eventType));
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListFlowExecutions ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/flows/%s/executions"
               (FlowIdentifier.to_header
                  x.ListFlowExecutionsRequest.flowIdentifier))
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("flowAliasIdentifier",
                          (FlowAliasIdentifier.to_header v)))
                  x.flowAliasIdentifier;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListInvocationSteps ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/sessions/%s/invocationSteps/"
               (SessionIdentifier.to_header
                  x.ListInvocationStepsRequest.sessionIdentifier))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListInvocations ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/sessions/%s/invocations/"
               (SessionIdentifier.to_header
                  x.ListInvocationsRequest.sessionIdentifier))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListSessions ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/sessions/")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (TaggableResourcesArn.to_header
               x.ListTagsForResourceRequest.resourceArn)
      | OptimizePrompt -> (Format.kasprintf Uri.of_string) "/optimize-prompt"
      | PutInvocationStep ->
          (Format.kasprintf Uri.of_string) "/sessions/%s/invocationSteps/"
            (SessionIdentifier.to_header
               x.PutInvocationStepRequest.sessionIdentifier)
      | Rerank -> (Format.kasprintf Uri.of_string) "/rerank"
      | Retrieve ->
          (Format.kasprintf Uri.of_string) "/knowledgebases/%s/retrieve"
            (KnowledgeBaseId.to_header x.RetrieveRequest.knowledgeBaseId)
      | RetrieveAndGenerate ->
          (Format.kasprintf Uri.of_string) "/retrieveAndGenerate"
      | RetrieveAndGenerateStream ->
          (Format.kasprintf Uri.of_string) "/retrieveAndGenerateStream"
      | StartFlowExecution ->
          (Format.kasprintf Uri.of_string) "/flows/%s/aliases/%s/executions"
            (FlowIdentifier.to_header
               x.StartFlowExecutionRequest.flowIdentifier)
            (FlowAliasIdentifier.to_header
               x.StartFlowExecutionRequest.flowAliasIdentifier)
      | StopFlowExecution ->
          (Format.kasprintf Uri.of_string)
            "/flows/%s/aliases/%s/executions/%s/stop"
            (FlowIdentifier.to_header
               x.StopFlowExecutionRequest.flowIdentifier)
            (FlowAliasIdentifier.to_header
               x.StopFlowExecutionRequest.flowAliasIdentifier)
            (FlowExecutionIdentifier.to_header
               x.StopFlowExecutionRequest.executionIdentifier)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (TaggableResourcesArn.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (TaggableResourcesArn.to_header
                  x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateSession ->
          (Format.kasprintf Uri.of_string) "/sessions/%s/"
            (SessionIdentifier.to_header
               x.UpdateSessionRequest.sessionIdentifier))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateInvocation -> Awso.Http.Request.make (method_of_endpoint endp)
  | CreateSession -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteAgentMemory -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteSession -> Awso.Http.Request.make (method_of_endpoint endp)
  | EndSession -> Awso.Http.Request.make (method_of_endpoint endp)
  | GenerateQuery ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("queryGenerationInput",
                           (QueryGenerationInput.to_value
                              req.GenerateQueryRequest.queryGenerationInput));
                      Some
                        ("transformationConfiguration",
                          (TransformationConfiguration.to_value
                             req.GenerateQueryRequest.transformationConfiguration))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAgentMemory ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetExecutionFlowSnapshot ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetFlowExecution ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetInvocationStep ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("invocationIdentifier",
                           (InvocationIdentifier.to_value
                              req.GetInvocationStepRequest.invocationIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSession ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeAgent ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.InvokeAgentRequest.sourceArn
                   ~f:(fun x ->
                         ("x-amz-source-arn", (AWSResourceARN.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.InvokeAgentRequest.bedrockModelConfigurations
                         ~f:(fun x ->
                               ("bedrockModelConfigurations",
                                 (BedrockModelConfigurations.to_value x)));
                      Option.map req.InvokeAgentRequest.enableTrace
                        ~f:(fun x -> ("enableTrace", (Boolean.to_value x)));
                      Option.map req.InvokeAgentRequest.endSession
                        ~f:(fun x -> ("endSession", (Boolean.to_value x)));
                      Option.map req.InvokeAgentRequest.inputText
                        ~f:(fun x -> ("inputText", (InputText.to_value x)));
                      Option.map req.InvokeAgentRequest.memoryId
                        ~f:(fun x -> ("memoryId", (MemoryId.to_value x)));
                      Option.map
                        req.InvokeAgentRequest.promptCreationConfigurations
                        ~f:(fun x ->
                              ("promptCreationConfigurations",
                                (PromptCreationConfigurations.to_value x)));
                      Option.map req.InvokeAgentRequest.sessionState
                        ~f:(fun x ->
                              ("sessionState", (SessionState.to_value x)));
                      Option.map
                        req.InvokeAgentRequest.streamingConfigurations
                        ~f:(fun x ->
                              ("streamingConfigurations",
                                (StreamingConfigurations.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeFlow ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.InvokeFlowRequest.enableTrace
                         ~f:(fun x -> ("enableTrace", (Boolean.to_value x)));
                      Option.map req.InvokeFlowRequest.executionId
                        ~f:(fun x ->
                              ("executionId", (FlowExecutionId.to_value x)));
                      Some
                        ("inputs",
                          (FlowInputs.to_value req.InvokeFlowRequest.inputs));
                      Option.map
                        req.InvokeFlowRequest.modelPerformanceConfiguration
                        ~f:(fun x ->
                              ("modelPerformanceConfiguration",
                                (ModelPerformanceConfiguration.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeInlineAgent ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.InvokeInlineAgentRequest.actionGroups
                         ~f:(fun x ->
                               ("actionGroups",
                                 (AgentActionGroups.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.agentCollaboration
                        ~f:(fun x ->
                              ("agentCollaboration",
                                (AgentCollaboration.to_value x)));
                      Option.map req.InvokeInlineAgentRequest.agentName
                        ~f:(fun x -> ("agentName", (Name.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.bedrockModelConfigurations
                        ~f:(fun x ->
                              ("bedrockModelConfigurations",
                                (InlineBedrockModelConfigurations.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.collaboratorConfigurations
                        ~f:(fun x ->
                              ("collaboratorConfigurations",
                                (CollaboratorConfigurations.to_value x)));
                      Option.map req.InvokeInlineAgentRequest.collaborators
                        ~f:(fun x ->
                              ("collaborators", (Collaborators.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.customOrchestration
                        ~f:(fun x ->
                              ("customOrchestration",
                                (CustomOrchestration.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.customerEncryptionKeyArn
                        ~f:(fun x ->
                              ("customerEncryptionKeyArn",
                                (KmsKeyArn.to_value x)));
                      Option.map req.InvokeInlineAgentRequest.enableTrace
                        ~f:(fun x -> ("enableTrace", (Boolean.to_value x)));
                      Option.map req.InvokeInlineAgentRequest.endSession
                        ~f:(fun x -> ("endSession", (Boolean.to_value x)));
                      Some
                        ("foundationModel",
                          (ModelIdentifier.to_value
                             req.InvokeInlineAgentRequest.foundationModel));
                      Option.map
                        req.InvokeInlineAgentRequest.guardrailConfiguration
                        ~f:(fun x ->
                              ("guardrailConfiguration",
                                (GuardrailConfigurationWithArn.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.idleSessionTTLInSeconds
                        ~f:(fun x ->
                              ("idleSessionTTLInSeconds",
                                (SessionTTL.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.inlineSessionState
                        ~f:(fun x ->
                              ("inlineSessionState",
                                (InlineSessionState.to_value x)));
                      Option.map req.InvokeInlineAgentRequest.inputText
                        ~f:(fun x -> ("inputText", (InputText.to_value x)));
                      Some
                        ("instruction",
                          (Instruction.to_value
                             req.InvokeInlineAgentRequest.instruction));
                      Option.map req.InvokeInlineAgentRequest.knowledgeBases
                        ~f:(fun x ->
                              ("knowledgeBases", (KnowledgeBases.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.orchestrationType
                        ~f:(fun x ->
                              ("orchestrationType",
                                (OrchestrationType.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.promptCreationConfigurations
                        ~f:(fun x ->
                              ("promptCreationConfigurations",
                                (PromptCreationConfigurations.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.promptOverrideConfiguration
                        ~f:(fun x ->
                              ("promptOverrideConfiguration",
                                (PromptOverrideConfiguration.to_value x)));
                      Option.map
                        req.InvokeInlineAgentRequest.streamingConfigurations
                        ~f:(fun x ->
                              ("streamingConfigurations",
                                (StreamingConfigurations.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListFlowExecutionEvents ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListFlowExecutions ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListInvocationSteps ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListInvocationStepsRequest.invocationIdentifier
                         ~f:(fun x ->
                               ("invocationIdentifier",
                                 (InvocationIdentifier.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListInvocations ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSessions ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | OptimizePrompt ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("input",
                           (InputPrompt.to_value
                              req.OptimizePromptRequest.input));
                      Some
                        ("targetModelId",
                          (OptimizePromptRequestTargetModelIdString.to_value
                             req.OptimizePromptRequest.targetModelId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutInvocationStep -> Awso.Http.Request.make (method_of_endpoint endp)
  | Rerank ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.RerankRequest.nextToken
                         ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Some
                        ("queries",
                          (RerankQueriesList.to_value
                             req.RerankRequest.queries));
                      Some
                        ("rerankingConfiguration",
                          (RerankingConfiguration.to_value
                             req.RerankRequest.rerankingConfiguration));
                      Some
                        ("sources",
                          (RerankSourcesList.to_value
                             req.RerankRequest.sources))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | Retrieve ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.RetrieveRequest.guardrailConfiguration
                         ~f:(fun x ->
                               ("guardrailConfiguration",
                                 (GuardrailConfiguration.to_value x)));
                      Option.map req.RetrieveRequest.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.RetrieveRequest.retrievalConfiguration
                        ~f:(fun x ->
                              ("retrievalConfiguration",
                                (KnowledgeBaseRetrievalConfiguration.to_value
                                   x)));
                      Some
                        ("retrievalQuery",
                          (KnowledgeBaseQuery.to_value
                             req.RetrieveRequest.retrievalQuery))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RetrieveAndGenerate ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("input",
                           (RetrieveAndGenerateInput.to_value
                              req.RetrieveAndGenerateRequest.input));
                      Option.map
                        req.RetrieveAndGenerateRequest.retrieveAndGenerateConfiguration
                        ~f:(fun x ->
                              ("retrieveAndGenerateConfiguration",
                                (RetrieveAndGenerateConfiguration.to_value x)));
                      Option.map
                        req.RetrieveAndGenerateRequest.sessionConfiguration
                        ~f:(fun x ->
                              ("sessionConfiguration",
                                (RetrieveAndGenerateSessionConfiguration.to_value
                                   x)));
                      Option.map req.RetrieveAndGenerateRequest.sessionId
                        ~f:(fun x -> ("sessionId", (SessionId.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RetrieveAndGenerateStream ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("input",
                           (RetrieveAndGenerateInput.to_value
                              req.RetrieveAndGenerateStreamRequest.input));
                      Option.map
                        req.RetrieveAndGenerateStreamRequest.retrieveAndGenerateConfiguration
                        ~f:(fun x ->
                              ("retrieveAndGenerateConfiguration",
                                (RetrieveAndGenerateConfiguration.to_value x)));
                      Option.map
                        req.RetrieveAndGenerateStreamRequest.sessionConfiguration
                        ~f:(fun x ->
                              ("sessionConfiguration",
                                (RetrieveAndGenerateSessionConfiguration.to_value
                                   x)));
                      Option.map
                        req.RetrieveAndGenerateStreamRequest.sessionId
                        ~f:(fun x -> ("sessionId", (SessionId.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartFlowExecution ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.StartFlowExecutionRequest.flowExecutionName
                         ~f:(fun x ->
                               ("flowExecutionName",
                                 (FlowExecutionName.to_value x)));
                      Some
                        ("inputs",
                          (FlowInputs.to_value
                             req.StartFlowExecutionRequest.inputs));
                      Option.map
                        req.StartFlowExecutionRequest.modelPerformanceConfiguration
                        ~f:(fun x ->
                              ("modelPerformanceConfiguration",
                                (ModelPerformanceConfiguration.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopFlowExecution ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagsMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateSession -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateInvocation ->
      if is_success
      then Ok (CreateInvocationResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CreateInvocationResponse.error_of_json))
  | CreateSession ->
      if is_success
      then Ok (CreateSessionResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateSessionResponse.error_of_json))
  | DeleteAgentMemory ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteAgentMemoryResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some DeleteAgentMemoryResponse.error_of_json))
  | DeleteSession ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteSessionResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteSessionResponse.error_of_json))
  | EndSession ->
      if is_success
      then Ok (EndSessionResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some EndSessionResponse.error_of_json))
  | GenerateQuery ->
      if is_success
      then Ok (GenerateQueryResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GenerateQueryResponse.error_of_json))
  | GetAgentMemory ->
      if is_success
      then Ok (GetAgentMemoryResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetAgentMemoryResponse.error_of_json))
  | GetExecutionFlowSnapshot ->
      if is_success
      then
        Ok (GetExecutionFlowSnapshotResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetExecutionFlowSnapshotResponse.error_of_json))
  | GetFlowExecution ->
      if is_success
      then Ok (GetFlowExecutionResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetFlowExecutionResponse.error_of_json))
  | GetInvocationStep ->
      if is_success
      then Ok (GetInvocationStepResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetInvocationStepResponse.error_of_json))
  | GetSession ->
      if is_success
      then Ok (GetSessionResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetSessionResponse.error_of_json))
  | InvokeAgent ->
      if is_success
      then
        let body = ResponseStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (InvokeAgentResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some InvokeAgentResponse.error_of_json))
  | InvokeFlow ->
      if is_success
      then
        let body =
          FlowResponseStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (InvokeFlowResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some InvokeFlowResponse.error_of_json))
  | InvokeInlineAgent ->
      if is_success
      then
        let body =
          InlineAgentResponseStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (InvokeInlineAgentResponse.of_header_and_body (headers, body))
      else
        Error
          (parse_aws_error (Some InvokeInlineAgentResponse.error_of_json))
  | ListFlowExecutionEvents ->
      if is_success
      then
        Ok (ListFlowExecutionEventsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListFlowExecutionEventsResponse.error_of_json))
  | ListFlowExecutions ->
      if is_success
      then Ok (ListFlowExecutionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListFlowExecutionsResponse.error_of_json))
  | ListInvocationSteps ->
      if is_success
      then Ok (ListInvocationStepsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListInvocationStepsResponse.error_of_json))
  | ListInvocations ->
      if is_success
      then Ok (ListInvocationsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListInvocationsResponse.error_of_json))
  | ListSessions ->
      if is_success
      then Ok (ListSessionsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListSessionsResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | OptimizePrompt ->
      if is_success
      then
        let body =
          OptimizedPromptStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (OptimizePromptResponse.of_header_and_body (headers, body))
      else
        Error (parse_aws_error (Some OptimizePromptResponse.error_of_json))
  | PutInvocationStep ->
      if is_success
      then Ok (PutInvocationStepResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some PutInvocationStepResponse.error_of_json))
  | Rerank ->
      if is_success
      then Ok (RerankResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some RerankResponse.error_of_json))
  | Retrieve ->
      if is_success
      then Ok (RetrieveResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some RetrieveResponse.error_of_json))
  | RetrieveAndGenerate ->
      if is_success
      then Ok (RetrieveAndGenerateResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some RetrieveAndGenerateResponse.error_of_json))
  | RetrieveAndGenerateStream ->
      if is_success
      then
        let body =
          RetrieveAndGenerateStreamResponseOutput.of_string
            (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (RetrieveAndGenerateStreamResponse.of_header_and_body
             (headers, body))
      else
        Error
          (parse_aws_error
             (Some RetrieveAndGenerateStreamResponse.error_of_json))
  | StartFlowExecution ->
      if is_success
      then Ok (StartFlowExecutionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StartFlowExecutionResponse.error_of_json))
  | StopFlowExecution ->
      if is_success
      then Ok (StopFlowExecutionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StopFlowExecutionResponse.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateSession ->
      if is_success
      then Ok (UpdateSessionResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateSessionResponse.error_of_json))
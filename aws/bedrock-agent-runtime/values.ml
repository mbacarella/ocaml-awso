(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.bedrock_agent_runtime
let apiVersion = "2023-07-26"
let endpointPrefix = "bedrock-agent-runtime"
let serviceFullName = "Agents for Amazon Bedrock Runtime"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module S3ObjectKey =
  struct
    type nonrec t = string
    let context_ = "S3ObjectKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[\\.\\-\\!\\*\\_\\'\\(\\)a-zA-Z0-9][\\.\\-\\!\\*\\_\\'\\(\\)\\/a-zA-Z0-9]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3ObjectKey" j
    let to_json = simple_to_json to_value
  end
module S3BucketName =
  struct
    type nonrec t = string
    let context_ = "S3BucketName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:63) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[a-z0-9][\\.\\-a-z0-9]{1,61}[a-z0-9]$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3BucketName" j
    let to_json = simple_to_json to_value
  end
module S3Identifier =
  struct
    type nonrec t =
      {
      s3BucketName: S3BucketName.t option
        [@ocaml.doc "The name of the S3 bucket."];
      s3ObjectKey: S3ObjectKey.t option
        [@ocaml.doc "The S3 object key for the S3 resource."]}
    let make ?s3BucketName =
      fun ?s3ObjectKey -> fun () -> { s3BucketName; s3ObjectKey }
    let to_value x =
      structure_to_value
        [("s3BucketName",
           (Option.map x.s3BucketName ~f:S3BucketName.to_value));
        ("s3ObjectKey", (Option.map x.s3ObjectKey ~f:S3ObjectKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3ObjectKey =
        (Option.map ~f:S3ObjectKey.of_xml) (Xml.child xml_arg0 "s3ObjectKey") in
      let s3BucketName =
        (Option.map ~f:S3BucketName.of_xml)
          (Xml.child xml_arg0 "s3BucketName") in
      make ?s3ObjectKey ?s3BucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3ObjectKey = field_map json__ "s3ObjectKey" S3ObjectKey.of_json in
      let s3BucketName = field_map json__ "s3BucketName" S3BucketName.of_json in
      make ?s3ObjectKey ?s3BucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The identifier information for an Amazon S3 bucket."]
module Payload =
  struct
    type nonrec t = string
    let context_ = "Payload"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Payload" j
    let to_json = simple_to_json to_value
  end
module APISchema =
  struct
    type nonrec t =
      {
      payload: Payload.t option
        [@ocaml.doc
          "The JSON or YAML-formatted payload defining the OpenAPI schema for the action group."];
      s3: S3Identifier.t option
        [@ocaml.doc
          "Contains details about the S3 object containing the OpenAPI schema for the action group."]}
    let make ?payload = fun ?s3 -> fun () -> { payload; s3 }
    let to_value x =
      structure_to_value
        [("payload", (Option.map x.payload ~f:Payload.to_value));
        ("s3", (Option.map x.s3 ~f:S3Identifier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3 = (Option.map ~f:S3Identifier.of_xml) (Xml.child xml_arg0 "s3") in
      let payload =
        (Option.map ~f:Payload.of_xml) (Xml.child xml_arg0 "payload") in
      make ?s3 ?payload ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3 = field_map json__ "s3" S3Identifier.of_json in
      let payload = field_map json__ "payload" Payload.of_json in
      make ?s3 ?payload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the OpenAPI schema for the action group. For more information, see Action group OpenAPI schemas. You can either include the schema directly in the payload field or you can upload it to an S3 bucket and specify the S3 bucket location in the s3 field."]
module AWSResourceARN =
  struct
    type nonrec t = string
    let context_ = "AWSResourceARN"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:agent/[0-9a-zA-Z]{10}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AWSResourceARN" j
    let to_json = simple_to_json to_value
  end
module NonBlankString =
  struct
    type nonrec t = string
    let context_ = "NonBlankString"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^[\\s\\S]*$"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonBlankString" j
    let to_json = simple_to_json to_value
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request is denied because of missing access permissions. Check your permissions and retry your request."]
module LambdaResourceArn =
  struct
    type nonrec t = string
    let context_ = "LambdaResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:(aws[a-zA-Z-]*)?:lambda:[a-z]{2}(-gov)?-[a-z]+-\\d{1}:\\d{12}:function:[a-zA-Z0-9-_\\.]+(:(\\$LATEST|[a-zA-Z0-9-_]+))?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LambdaResourceArn" j
    let to_json = simple_to_json to_value
  end
module CustomControlMethod =
  struct
    type nonrec t =
      | RETURN_CONTROL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | RETURN_CONTROL -> "RETURN_CONTROL" | Non_static_id s -> s
    let of_string =
      function | "RETURN_CONTROL" -> RETURN_CONTROL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CustomControlMethod" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CustomControlMethod" j)
    let to_json = simple_to_json to_value
  end
module ActionGroupExecutor =
  struct
    type nonrec t =
      {
      customControl: CustomControlMethod.t option
        [@ocaml.doc
          "To return the action group invocation results directly in the InvokeInlineAgent response, specify RETURN_CONTROL."];
      lambda: LambdaResourceArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Lambda function containing the business logic that is carried out upon invoking the action."]}
    let make ?customControl =
      fun ?lambda -> fun () -> { customControl; lambda }
    let to_value x =
      structure_to_value
        [("customControl",
           (Option.map x.customControl ~f:CustomControlMethod.to_value));
        ("lambda", (Option.map x.lambda ~f:LambdaResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lambda =
        (Option.map ~f:LambdaResourceArn.of_xml)
          (Xml.child xml_arg0 "lambda") in
      let customControl =
        (Option.map ~f:CustomControlMethod.of_xml)
          (Xml.child xml_arg0 "customControl") in
      make ?lambda ?customControl ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lambda = field_map json__ "lambda" LambdaResourceArn.of_json in
      let customControl =
        field_map json__ "customControl" CustomControlMethod.of_json in
      make ?lambda ?customControl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the Lambda function containing the business logic that is carried out upon invoking the action or the custom control method for handling the information elicited from the user."]
module Verb =
  struct
    type nonrec t = string
    let context_ = "Verb"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Verb" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module Parameter =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the parameter."];
      type_: String_.t option [@ocaml.doc "The type of the parameter."];
      value: String_.t option [@ocaml.doc "The value of the parameter."]}
    let make ?name =
      fun ?type_ -> fun ?value -> fun () -> { name; type_; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?value ?type_ ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" String_.of_json in
      let type_ = field_map json__ "type" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?value ?type_ ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A parameter for the API request or function."]
module Parameters =
  struct
    type nonrec t = Parameter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Parameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Parameter.of_xml)
    let of_json j =
      list_of_json ~kind:"Parameters" ~of_json:Parameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContentMap =
  struct
    type nonrec t = (String_.t * Parameters.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String Parameters"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (Parameters.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:Parameters.of_json j
    let to_json v = composed_to_json to_value v
  end
module RequestBody =
  struct
    type nonrec t =
      {
      content: ContentMap.t option
        [@ocaml.doc "The content in the request body."]}
    let make ?content = fun () -> { content }
    let to_value x =
      structure_to_value
        [("content", (Option.map x.content ~f:ContentMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let content =
        (Option.map ~f:ContentMap.of_xml) (Xml.child xml_arg0 "content") in
      make ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let content = field_map json__ "content" ContentMap.of_json in
      make ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The parameters in the API request body."]
module Function =
  struct
    type nonrec t = string
    let context_ = "Function"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Function" j
    let to_json = simple_to_json to_value
  end
module ExecutionType =
  struct
    type nonrec t =
      | LAMBDA 
      | RETURN_CONTROL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LAMBDA -> "LAMBDA"
      | RETURN_CONTROL -> "RETURN_CONTROL"
      | Non_static_id s -> s
    let of_string =
      function
      | "LAMBDA" -> LAMBDA
      | "RETURN_CONTROL" -> RETURN_CONTROL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ExecutionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExecutionType" j)
    let to_json = simple_to_json to_value
  end
module ApiPath =
  struct
    type nonrec t = string
    let context_ = "ApiPath"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ApiPath" j
    let to_json = simple_to_json to_value
  end
module ActionGroupName =
  struct
    type nonrec t = string
    let context_ = "ActionGroupName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionGroupName" j
    let to_json = simple_to_json to_value
  end
module ActionGroupInvocationInput =
  struct
    type nonrec t =
      {
      actionGroupName: ActionGroupName.t option
        [@ocaml.doc "The name of the action group."];
      apiPath: ApiPath.t option
        [@ocaml.doc
          "The path to the API to call, based off the action group."];
      executionType: ExecutionType.t option
        [@ocaml.doc
          "How fulfillment of the action is handled. For more information, see Handling fulfillment of the action."];
      function_: Function.t option
        [@ocaml.doc "The function in the action group to call."];
      invocationId: String_.t option
        [@ocaml.doc
          "The unique identifier of the invocation. Only returned if the executionType is RETURN_CONTROL."];
      parameters: Parameters.t option
        [@ocaml.doc "The parameters in the Lambda input event."];
      requestBody: RequestBody.t option
        [@ocaml.doc
          "The parameters in the request body for the Lambda input event."];
      verb: Verb.t option
        [@ocaml.doc "The API method being used, based off the action group."]}
    let make ?actionGroupName =
      fun ?apiPath ->
        fun ?executionType ->
          fun ?function_ ->
            fun ?invocationId ->
              fun ?parameters ->
                fun ?requestBody ->
                  fun ?verb ->
                    fun () ->
                      {
                        actionGroupName;
                        apiPath;
                        executionType;
                        function_;
                        invocationId;
                        parameters;
                        requestBody;
                        verb
                      }
    let to_value x =
      structure_to_value
        [("actionGroupName",
           (Option.map x.actionGroupName ~f:ActionGroupName.to_value));
        ("apiPath", (Option.map x.apiPath ~f:ApiPath.to_value));
        ("executionType",
          (Option.map x.executionType ~f:ExecutionType.to_value));
        ("function", (Option.map x.function_ ~f:Function.to_value));
        ("invocationId", (Option.map x.invocationId ~f:String_.to_value));
        ("parameters", (Option.map x.parameters ~f:Parameters.to_value));
        ("requestBody", (Option.map x.requestBody ~f:RequestBody.to_value));
        ("verb", (Option.map x.verb ~f:Verb.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let verb = (Option.map ~f:Verb.of_xml) (Xml.child xml_arg0 "verb") in
      let requestBody =
        (Option.map ~f:RequestBody.of_xml) (Xml.child xml_arg0 "requestBody") in
      let parameters =
        (Option.map ~f:Parameters.of_xml) (Xml.child xml_arg0 "parameters") in
      let invocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "invocationId") in
      let function_ =
        (Option.map ~f:Function.of_xml) (Xml.child xml_arg0 "function") in
      let executionType =
        (Option.map ~f:ExecutionType.of_xml)
          (Xml.child xml_arg0 "executionType") in
      let apiPath =
        (Option.map ~f:ApiPath.of_xml) (Xml.child xml_arg0 "apiPath") in
      let actionGroupName =
        (Option.map ~f:ActionGroupName.of_xml)
          (Xml.child xml_arg0 "actionGroupName") in
      make ?verb ?requestBody ?parameters ?invocationId ?function_
        ?executionType ?apiPath ?actionGroupName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let verb = field_map json__ "verb" Verb.of_json in
      let requestBody = field_map json__ "requestBody" RequestBody.of_json in
      let parameters = field_map json__ "parameters" Parameters.of_json in
      let invocationId = field_map json__ "invocationId" String_.of_json in
      let function_ = field_map json__ "function" Function.of_json in
      let executionType =
        field_map json__ "executionType" ExecutionType.of_json in
      let apiPath = field_map json__ "apiPath" ApiPath.of_json in
      let actionGroupName =
        field_map json__ "actionGroupName" ActionGroupName.of_json in
      make ?verb ?requestBody ?parameters ?invocationId ?function_
        ?executionType ?apiPath ?actionGroupName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the action group being invoked. For more information about the possible structures, see the InvocationInput tab in OrchestrationTrace in the Amazon Bedrock User Guide."]
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Usage =
  struct
    type nonrec t =
      {
      inputTokens: Integer.t option
        [@ocaml.doc
          "Contains information about the input tokens from the foundation model usage."];
      outputTokens: Integer.t option
        [@ocaml.doc
          "Contains information about the output tokens from the foundation model usage."]}
    let make ?inputTokens =
      fun ?outputTokens -> fun () -> { inputTokens; outputTokens }
    let to_value x =
      structure_to_value
        [("inputTokens", (Option.map x.inputTokens ~f:Integer.to_value));
        ("outputTokens", (Option.map x.outputTokens ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputTokens =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "outputTokens") in
      let inputTokens =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "inputTokens") in
      make ?outputTokens ?inputTokens ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputTokens = field_map json__ "outputTokens" Integer.of_json in
      let inputTokens = field_map json__ "inputTokens" Integer.of_json in
      make ?outputTokens ?inputTokens ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information of the usage of the foundation model."]
module SyntheticTimestamp_date_time =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Metadata =
  struct
    type nonrec t =
      {
      clientRequestId: String_.t option
        [@ocaml.doc
          "A unique identifier associated with the downstream invocation. This ID can be used for tracing, debugging, and identifying specific invocations in customer logs or systems."];
      endTime: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "In the final response, endTime is the end time of the agent invocation operation."];
      operationTotalTimeMs: Long.t option
        [@ocaml.doc
          "The total time it took for the agent to complete execution. This field is only set for the final response."];
      startTime: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "In the final response, startTime is the start time of the agent invocation operation."];
      totalTimeMs: Long.t option
        [@ocaml.doc
          "The total execution time for the specific invocation being processed (model, knowledge base, guardrail, agent collaborator, or code interpreter). It represents how long the individual invocation took."];
      usage: Usage.t option
        [@ocaml.doc
          "Specific to model invocation and contains details about the usage of a foundation model."]}
    let make ?clientRequestId =
      fun ?endTime ->
        fun ?operationTotalTimeMs ->
          fun ?startTime ->
            fun ?totalTimeMs ->
              fun ?usage ->
                fun () ->
                  {
                    clientRequestId;
                    endTime;
                    operationTotalTimeMs;
                    startTime;
                    totalTimeMs;
                    usage
                  }
    let to_value x =
      structure_to_value
        [("clientRequestId",
           (Option.map x.clientRequestId ~f:String_.to_value));
        ("endTime",
          (Option.map x.endTime ~f:SyntheticTimestamp_date_time.to_value));
        ("operationTotalTimeMs",
          (Option.map x.operationTotalTimeMs ~f:Long.to_value));
        ("startTime",
          (Option.map x.startTime ~f:SyntheticTimestamp_date_time.to_value));
        ("totalTimeMs", (Option.map x.totalTimeMs ~f:Long.to_value));
        ("usage", (Option.map x.usage ~f:Usage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let usage = (Option.map ~f:Usage.of_xml) (Xml.child xml_arg0 "usage") in
      let totalTimeMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "totalTimeMs") in
      let startTime =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "startTime") in
      let operationTotalTimeMs =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "operationTotalTimeMs") in
      let endTime =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "endTime") in
      let clientRequestId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "clientRequestId") in
      make ?usage ?totalTimeMs ?startTime ?operationTotalTimeMs ?endTime
        ?clientRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let usage = field_map json__ "usage" Usage.of_json in
      let totalTimeMs = field_map json__ "totalTimeMs" Long.of_json in
      let startTime =
        field_map json__ "startTime" SyntheticTimestamp_date_time.of_json in
      let operationTotalTimeMs =
        field_map json__ "operationTotalTimeMs" Long.of_json in
      let endTime =
        field_map json__ "endTime" SyntheticTimestamp_date_time.of_json in
      let clientRequestId =
        field_map json__ "clientRequestId" String_.of_json in
      make ?usage ?totalTimeMs ?startTime ?operationTotalTimeMs ?endTime
        ?clientRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides information about the execution process for different types of invocations, such as model invocation, knowledge base invocation, agent collaborator invocation, guardrail invocation, and code interpreter Invocation."]
module ActionGroupOutputString =
  struct
    type nonrec t = string
    let context_ = "ActionGroupOutputString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionGroupOutputString" j
    let to_json = simple_to_json to_value
  end
module ActionGroupInvocationOutput =
  struct
    type nonrec t =
      {
      metadata: Metadata.t option
        [@ocaml.doc "Contains information about the action group output."];
      text: ActionGroupOutputString.t option
        [@ocaml.doc
          "The JSON-formatted string returned by the API invoked by the action group."]}
    let make ?metadata = fun ?text -> fun () -> { metadata; text }
    let to_value x =
      structure_to_value
        [("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("text", (Option.map x.text ~f:ActionGroupOutputString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        (Option.map ~f:ActionGroupOutputString.of_xml)
          (Xml.child xml_arg0 "text") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      make ?text ?metadata ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" ActionGroupOutputString.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      make ?text ?metadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the JSON-formatted string returned by the API invoked by the action group."]
module ActionGroupSignature =
  struct
    type nonrec t =
      | AMAZON_UserInput 
      | AMAZON_CodeInterpreter 
      | ANTHROPIC_Computer 
      | ANTHROPIC_Bash 
      | ANTHROPIC_TextEditor 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AMAZON_UserInput -> "AMAZON.UserInput"
      | AMAZON_CodeInterpreter -> "AMAZON.CodeInterpreter"
      | ANTHROPIC_Computer -> "ANTHROPIC.Computer"
      | ANTHROPIC_Bash -> "ANTHROPIC.Bash"
      | ANTHROPIC_TextEditor -> "ANTHROPIC.TextEditor"
      | Non_static_id s -> s
    let of_string =
      function
      | "AMAZON.UserInput" -> AMAZON_UserInput
      | "AMAZON.CodeInterpreter" -> AMAZON_CodeInterpreter
      | "ANTHROPIC.Computer" -> ANTHROPIC_Computer
      | "ANTHROPIC.Bash" -> ANTHROPIC_Bash
      | "ANTHROPIC.TextEditor" -> ANTHROPIC_TextEditor
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ActionGroupSignature" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ActionGroupSignature" j)
    let to_json = simple_to_json to_value
  end
module ActionGroupSignatureParamsValueString =
  struct
    type nonrec t = string
    let context_ = "ActionGroupSignatureParamsValueString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ActionGroupSignatureParamsValueString" j
    let to_json = simple_to_json to_value
  end
module ActionGroupSignatureParamsKeyString =
  struct
    type nonrec t = string
    let context_ = "ActionGroupSignatureParamsKeyString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ActionGroupSignatureParamsKeyString" j
    let to_json = simple_to_json to_value
  end
module ActionGroupSignatureParams =
  struct
    type nonrec t =
      (ActionGroupSignatureParamsKeyString.t *
        ActionGroupSignatureParamsValueString.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ActionGroupSignatureParamsKeyString.of_string
                                chopped),
                              (ActionGroupSignatureParamsValueString.of_string
                                 v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ActionGroupSignatureParamsKeyString.to_value x) |>
                    (fun x ->
                       (ActionGroupSignatureParamsValueString.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json
        ~key_of_string:ActionGroupSignatureParamsKeyString.of_string
        ~of_json:ActionGroupSignatureParamsValueString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ActionInvocationType =
  struct
    type nonrec t =
      | RESULT 
      | USER_CONFIRMATION 
      | USER_CONFIRMATION_AND_RESULT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RESULT -> "RESULT"
      | USER_CONFIRMATION -> "USER_CONFIRMATION"
      | USER_CONFIRMATION_AND_RESULT -> "USER_CONFIRMATION_AND_RESULT"
      | Non_static_id s -> s
    let of_string =
      function
      | "RESULT" -> RESULT
      | "USER_CONFIRMATION" -> USER_CONFIRMATION
      | "USER_CONFIRMATION_AND_RESULT" -> USER_CONFIRMATION_AND_RESULT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ActionInvocationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ActionInvocationType" j)
    let to_json = simple_to_json to_value
  end
module AdditionalModelRequestFieldsValue =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module AdditionalModelRequestFieldsKey =
  struct
    type nonrec t = string
    let context_ = "AdditionalModelRequestFieldsKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AdditionalModelRequestFieldsKey" j
    let to_json = simple_to_json to_value
  end
module AdditionalModelRequestFields =
  struct
    type nonrec t =
      (AdditionalModelRequestFieldsKey.t *
        AdditionalModelRequestFieldsValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types AdditionalModelRequestFieldsKey AdditionalModelRequestFieldsValue"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (AdditionalModelRequestFieldsKey.to_value x) |>
                    (fun x ->
                       (AdditionalModelRequestFieldsValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:AdditionalModelRequestFieldsKey.of_string
        ~of_json:AdditionalModelRequestFieldsValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceName =
  struct
    type nonrec t = string
    let context_ = "ResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^([0-9a-zA-Z][_-]?){1,100}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end
module ResourceDescription =
  struct
    type nonrec t = string[@@ocaml.doc
                            "/ \\@documentation(\"Description of the using the resource.\")"]
    let context_ = "ResourceDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDescription" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "/ \\@documentation(\"Description of the using the resource.\")"]
module RequireConfirmation =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RequireConfirmation" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RequireConfirmation" j)
    let to_json = simple_to_json to_value
  end
module ParameterName =
  struct
    type nonrec t = string
    let context_ = "ParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^([0-9a-zA-Z][_-]?){1,100}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterName" j
    let to_json = simple_to_json to_value
  end
module ParameterType =
  struct
    type nonrec t =
      | String_ 
      | Number 
      | Integer 
      | Boolean 
      | Array 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | String_ -> "string"
      | Number -> "number"
      | Integer -> "integer"
      | Boolean -> "boolean"
      | Array -> "array"
      | Non_static_id s -> s
    let of_string =
      function
      | "string" -> String_
      | "number" -> Number
      | "integer" -> Integer
      | "boolean" -> Boolean
      | "array" -> Array
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ParameterType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ParameterType" j)
    let to_json = simple_to_json to_value
  end
module ParameterDescription =
  struct
    type nonrec t = string
    let context_ = "ParameterDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:500) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterDescription" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ParameterDetail =
  struct
    type nonrec t =
      {
      description: ParameterDescription.t option
        [@ocaml.doc
          "A description of the parameter. Helps the foundation model determine how to elicit the parameters from the user."];
      required: Boolean.t option
        [@ocaml.doc
          "Whether the parameter is required for the agent to complete the function for action group invocation."];
      type_: ParameterType.t [@ocaml.doc "The data type of the parameter."]}
    let context_ = "ParameterDetail"
    let make ?description =
      fun ?required ->
        fun ~type_ -> fun () -> { description; required; type_ }
    let to_value x =
      structure_to_value
        [("description",
           (Option.map x.description ~f:ParameterDescription.to_value));
        ("required", (Option.map x.required ~f:Boolean.to_value));
        ("type", (Some (ParameterType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        ParameterType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let required =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "required") in
      let description =
        (Option.map ~f:ParameterDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      make ~type_ ?required ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" ParameterType.of_json in
      let required = field_map json__ "required" Boolean.of_json in
      let description =
        field_map json__ "description" ParameterDescription.of_json in
      make ~type_ ?required ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about a parameter in a function for an action group."]
module ParameterMap =
  struct
    type nonrec t = (ParameterName.t * ParameterDetail.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ParameterName ParameterDetail"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ParameterName.to_value x) |>
                    (fun x ->
                       (ParameterDetail.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ParameterName.of_string
        ~of_json:ParameterDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module FunctionDescription =
  struct
    type nonrec t = string
    let context_ = "FunctionDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1200) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FunctionDescription" j
    let to_json = simple_to_json to_value
  end
module FunctionDefinition =
  struct
    type nonrec t =
      {
      description: FunctionDescription.t option
        [@ocaml.doc "A description of the function and its purpose."];
      name: ResourceName.t [@ocaml.doc "A name for the function."];
      parameters: ParameterMap.t option
        [@ocaml.doc
          "The parameters that the agent elicits from the user to fulfill the function."];
      requireConfirmation: RequireConfirmation.t option
        [@ocaml.doc
          "Contains information if user confirmation is required to invoke the function."]}
    let context_ = "FunctionDefinition"
    let make ?description =
      fun ?parameters ->
        fun ?requireConfirmation ->
          fun ~name ->
            fun () -> { description; parameters; requireConfirmation; name }
    let to_value x =
      structure_to_value
        [("description",
           (Option.map x.description ~f:FunctionDescription.to_value));
        ("name", (Some (ResourceName.to_value x.name)));
        ("parameters", (Option.map x.parameters ~f:ParameterMap.to_value));
        ("requireConfirmation",
          (Option.map x.requireConfirmation ~f:RequireConfirmation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let requireConfirmation =
        (Option.map ~f:RequireConfirmation.of_xml)
          (Xml.child xml_arg0 "requireConfirmation") in
      let parameters =
        (Option.map ~f:ParameterMap.of_xml) (Xml.child xml_arg0 "parameters") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let description =
        (Option.map ~f:FunctionDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      make ?requireConfirmation ?parameters ~name ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let requireConfirmation =
        field_map json__ "requireConfirmation" RequireConfirmation.of_json in
      let parameters = field_map json__ "parameters" ParameterMap.of_json in
      let name = field_map_exn json__ "name" ResourceName.of_json in
      let description =
        field_map json__ "description" FunctionDescription.of_json in
      make ?requireConfirmation ?parameters ~name ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines parameters that the agent needs to invoke from the user to complete the function. Corresponds to an action in an action group."]
module Functions =
  struct
    type nonrec t = FunctionDefinition.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FunctionDefinition.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FunctionDefinition.of_xml)
    let of_json j =
      list_of_json ~kind:"Functions" ~of_json:FunctionDefinition.of_json j
    let to_json v = composed_to_json to_value v
  end
module FunctionSchema =
  struct
    type nonrec t =
      {
      functions: Functions.t option
        [@ocaml.doc
          "A list of functions that each define an action in the action group."]}
    let make ?functions = fun () -> { functions }
    let to_value x =
      structure_to_value
        [("functions", (Option.map x.functions ~f:Functions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let functions =
        (Option.map ~f:Functions.of_xml) (Xml.child xml_arg0 "functions") in
      make ?functions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let functions = field_map json__ "functions" Functions.of_json in
      make ?functions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the function schema for the action group or the JSON or YAML-formatted payload defining the schema."]
module AgentActionGroup =
  struct
    type nonrec t =
      {
      actionGroupExecutor: ActionGroupExecutor.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Lambda function containing the business logic that is carried out upon invoking the action or the custom control method for handling the information elicited from the user."];
      actionGroupName: ResourceName.t
        [@ocaml.doc "The name of the action group."];
      apiSchema: APISchema.t option
        [@ocaml.doc
          "Contains either details about the S3 object containing the OpenAPI schema for the action group or the JSON or YAML-formatted payload defining the schema. For more information, see Action group OpenAPI schemas."];
      description: ResourceDescription.t option
        [@ocaml.doc "A description of the action group."];
      functionSchema: FunctionSchema.t option
        [@ocaml.doc
          "Contains details about the function schema for the action group or the JSON or YAML-formatted payload defining the schema."];
      parentActionGroupSignature: ActionGroupSignature.t option
        [@ocaml.doc
          "Specify a built-in or computer use action for this action group. If you specify a value, you must leave the description, apiSchema, and actionGroupExecutor fields empty for this action group. To allow your agent to request the user for additional information when trying to complete a task, set this field to AMAZON.UserInput. To allow your agent to generate, run, and troubleshoot code when trying to complete a task, set this field to AMAZON.CodeInterpreter. To allow your agent to use an Anthropic computer use tool, specify one of the following values. Computer use is a new Anthropic Claude model capability (in beta) available with Anthropic Claude 3.7 Sonnet and Claude 3.5 Sonnet v2 only. When operating computer use functionality, we recommend taking additional security precautions, such as executing computer actions in virtual environments with restricted data access and limited internet connectivity. For more information, see Configure an Amazon Bedrock Agent to complete tasks with computer use tools. ANTHROPIC.Computer - Gives the agent permission to use the mouse and keyboard and take screenshots. ANTHROPIC.TextEditor - Gives the agent permission to view, create and edit files. ANTHROPIC.Bash - Gives the agent permission to run commands in a bash shell."];
      parentActionGroupSignatureParams: ActionGroupSignatureParams.t option
        [@ocaml.doc
          "The configuration settings for a computer use action. Computer use is a new Anthropic Claude model capability (in beta) available with Claude 3.7 Sonnet and Claude 3.5 Sonnet v2 only. For more information, see Configure an Amazon Bedrock Agent to complete tasks with computer use tools."]}
    let context_ = "AgentActionGroup"
    let make ?actionGroupExecutor =
      fun ?apiSchema ->
        fun ?description ->
          fun ?functionSchema ->
            fun ?parentActionGroupSignature ->
              fun ?parentActionGroupSignatureParams ->
                fun ~actionGroupName ->
                  fun () ->
                    {
                      actionGroupExecutor;
                      apiSchema;
                      description;
                      functionSchema;
                      parentActionGroupSignature;
                      parentActionGroupSignatureParams;
                      actionGroupName
                    }
    let to_value x =
      structure_to_value
        [("actionGroupExecutor",
           (Option.map x.actionGroupExecutor ~f:ActionGroupExecutor.to_value));
        ("actionGroupName", (Some (ResourceName.to_value x.actionGroupName)));
        ("apiSchema", (Option.map x.apiSchema ~f:APISchema.to_value));
        ("description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("functionSchema",
          (Option.map x.functionSchema ~f:FunctionSchema.to_value));
        ("parentActionGroupSignature",
          (Option.map x.parentActionGroupSignature
             ~f:ActionGroupSignature.to_value));
        ("parentActionGroupSignatureParams",
          (Option.map x.parentActionGroupSignatureParams
             ~f:ActionGroupSignatureParams.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parentActionGroupSignatureParams =
        (Option.map ~f:ActionGroupSignatureParams.of_xml)
          (Xml.child xml_arg0 "parentActionGroupSignatureParams") in
      let parentActionGroupSignature =
        (Option.map ~f:ActionGroupSignature.of_xml)
          (Xml.child xml_arg0 "parentActionGroupSignature") in
      let functionSchema =
        (Option.map ~f:FunctionSchema.of_xml)
          (Xml.child xml_arg0 "functionSchema") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let apiSchema =
        (Option.map ~f:APISchema.of_xml) (Xml.child xml_arg0 "apiSchema") in
      let actionGroupName =
        ResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "actionGroupName") in
      let actionGroupExecutor =
        (Option.map ~f:ActionGroupExecutor.of_xml)
          (Xml.child xml_arg0 "actionGroupExecutor") in
      make ?parentActionGroupSignatureParams ?parentActionGroupSignature
        ?functionSchema ?description ?apiSchema ~actionGroupName
        ?actionGroupExecutor ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parentActionGroupSignatureParams =
        field_map json__ "parentActionGroupSignatureParams"
          ActionGroupSignatureParams.of_json in
      let parentActionGroupSignature =
        field_map json__ "parentActionGroupSignature"
          ActionGroupSignature.of_json in
      let functionSchema =
        field_map json__ "functionSchema" FunctionSchema.of_json in
      let description =
        field_map json__ "description" ResourceDescription.of_json in
      let apiSchema = field_map json__ "apiSchema" APISchema.of_json in
      let actionGroupName =
        field_map_exn json__ "actionGroupName" ResourceName.of_json in
      let actionGroupExecutor =
        field_map json__ "actionGroupExecutor" ActionGroupExecutor.of_json in
      make ?parentActionGroupSignatureParams ?parentActionGroupSignature
        ?functionSchema ?description ?apiSchema ~actionGroupName
        ?actionGroupExecutor ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains details of the inline agent's action group."]
module AgentActionGroups =
  struct
    type nonrec t = AgentActionGroup.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AgentActionGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AgentActionGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"AgentActionGroups"
        ~of_json:AgentActionGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module AgentAliasArn =
  struct
    type nonrec t = string
    let context_ = "AgentAliasArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:agent-alias/[0-9a-zA-Z]{10}/[0-9a-zA-Z]{10}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgentAliasArn" j
    let to_json = simple_to_json to_value
  end
module AgentAliasId =
  struct
    type nonrec t = string
    let context_ = "AgentAliasId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:10) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9a-zA-Z]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgentAliasId" j
    let to_json = simple_to_json to_value
  end
module AgentCollaboration =
  struct
    type nonrec t =
      | SUPERVISOR 
      | SUPERVISOR_ROUTER 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUPERVISOR -> "SUPERVISOR"
      | SUPERVISOR_ROUTER -> "SUPERVISOR_ROUTER"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUPERVISOR" -> SUPERVISOR
      | "SUPERVISOR_ROUTER" -> SUPERVISOR_ROUTER
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AgentCollaboration" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AgentCollaboration" j)
    let to_json = simple_to_json to_value
  end
module ResponseState =
  struct
    type nonrec t =
      | FAILURE 
      | REPROMPT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FAILURE -> "FAILURE"
      | REPROMPT -> "REPROMPT"
      | Non_static_id s -> s
    let of_string =
      function
      | "FAILURE" -> FAILURE
      | "REPROMPT" -> REPROMPT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ResponseState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResponseState" j)
    let to_json = simple_to_json to_value
  end
module ImageInputSourceBytesBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module ImageInputSource =
  struct
    type nonrec t =
      {
      bytes: ImageInputSourceBytesBlob.t option
        [@ocaml.doc
          "The raw image bytes for the image. If you use an Amazon Web Services SDK, you don't need to encode the image bytes in base64."]}
    let make ?bytes = fun () -> { bytes }
    let of_header_and_body = ((fun (xs, pipe) -> make ?bytes:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("bytes",
           (Option.map x.bytes ~f:ImageInputSourceBytesBlob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytes =
        (Option.map ~f:ImageInputSourceBytesBlob.of_xml)
          (Xml.child xml_arg0 "bytes") in
      make ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bytes = field_map json__ "bytes" ImageInputSourceBytesBlob.of_json in
      make ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the source of an input image in the result from a function in the action group invocation."]
module ImageInputFormat =
  struct
    type nonrec t =
      | Png 
      | Jpeg 
      | Gif 
      | Webp 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Png -> "png"
      | Jpeg -> "jpeg"
      | Gif -> "gif"
      | Webp -> "webp"
      | Non_static_id s -> s
    let of_string =
      function
      | "png" -> Png
      | "jpeg" -> Jpeg
      | "gif" -> Gif
      | "webp" -> Webp
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ImageInputFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ImageInputFormat" j)
    let to_json = simple_to_json to_value
  end
module ImageInput =
  struct
    type nonrec t =
      {
      format: ImageInputFormat.t
        [@ocaml.doc "The type of image in the result."];
      source: ImageInputSource.t
        [@ocaml.doc "The source of the image in the result."]}
    let context_ = "ImageInput"
    let make ~format = fun ~source -> fun () -> { format; source }
    let to_value x =
      structure_to_value
        [("format", (Some (ImageInputFormat.to_value x.format)));
        ("source", (Some (ImageInputSource.to_value x.source)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let source =
        ImageInputSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let format =
        ImageInputFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      make ~source ~format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let source = field_map_exn json__ "source" ImageInputSource.of_json in
      let format = field_map_exn json__ "format" ImageInputFormat.of_json in
      make ~source ~format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about an image in the result from a function in the action group invocation. You can specify images only when the function is a computer use action. For more information, see Configure an Amazon Bedrock Agent to complete tasks with computer use tools."]
module ImageInputs =
  struct
    type nonrec t = ImageInput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ImageInput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ImageInput.of_xml)
    let of_json j =
      list_of_json ~kind:"ImageInputs" ~of_json:ImageInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContentBody =
  struct
    type nonrec t =
      {
      body: String_.t option [@ocaml.doc "The body of the API response."];
      images: ImageInputs.t option
        [@ocaml.doc
          "Lists details, including format and source, for the image in the response from the function call. You can specify only one image and the function in the returnControlInvocationResults must be a computer use action. For more information, see Configure an Amazon Bedrock Agent to complete tasks with computer use tools."]}
    let make ?body = fun ?images -> fun () -> { body; images }
    let to_value x =
      structure_to_value
        [("body", (Option.map x.body ~f:String_.to_value));
        ("images", (Option.map x.images ~f:ImageInputs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let images =
        (Option.map ~f:ImageInputs.of_xml) (Xml.child xml_arg0 "images") in
      let body = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "body") in
      make ?images ?body ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let images = field_map json__ "images" ImageInputs.of_json in
      let body = field_map json__ "body" String_.of_json in
      make ?images ?body ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the body of the API response. This data type is used in the following API operations: In the returnControlInvocationResults field of the InvokeAgent request"]
module ResponseBody =
  struct
    type nonrec t = (String_.t * ContentBody.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String ContentBody"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (ContentBody.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:ContentBody.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConfirmationState =
  struct
    type nonrec t =
      | CONFIRM 
      | DENY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | CONFIRM -> "CONFIRM" | DENY -> "DENY" | Non_static_id s -> s
    let of_string =
      function | "CONFIRM" -> CONFIRM | "DENY" -> DENY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ConfirmationState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConfirmationState" j)
    let to_json = simple_to_json to_value
  end
module FunctionResult =
  struct
    type nonrec t =
      {
      actionGroup: String_.t
        [@ocaml.doc "The action group that the function belongs to."];
      agentId: String_.t option [@ocaml.doc "The agent's ID."];
      confirmationState: ConfirmationState.t option
        [@ocaml.doc
          "Contains the user confirmation information about the function that was called."];
      function_: String_.t option
        [@ocaml.doc "The name of the function that was called."];
      responseBody: ResponseBody.t option
        [@ocaml.doc
          "The response from the function call using the parameters. The response might be returned directly or from the Lambda function. Specify TEXT or IMAGES. The key of the object is the content type. You can only specify one type. If you specify IMAGES, you can specify only one image. You can specify images only when the function in the returnControlInvocationResults is a computer use action. For more information, see Configure an Amazon Bedrock Agent to complete tasks with computer use tools."];
      responseState: ResponseState.t option
        [@ocaml.doc
          "Controls the final response state returned to end user when API/Function execution failed. When this state is FAILURE, the request would fail with dependency failure exception. When this state is REPROMPT, the API/function response will be sent to model for re-prompt"]}
    let context_ = "FunctionResult"
    let make ?agentId =
      fun ?confirmationState ->
        fun ?function_ ->
          fun ?responseBody ->
            fun ?responseState ->
              fun ~actionGroup ->
                fun () ->
                  {
                    agentId;
                    confirmationState;
                    function_;
                    responseBody;
                    responseState;
                    actionGroup
                  }
    let to_value x =
      structure_to_value
        [("actionGroup", (Some (String_.to_value x.actionGroup)));
        ("agentId", (Option.map x.agentId ~f:String_.to_value));
        ("confirmationState",
          (Option.map x.confirmationState ~f:ConfirmationState.to_value));
        ("function", (Option.map x.function_ ~f:String_.to_value));
        ("responseBody",
          (Option.map x.responseBody ~f:ResponseBody.to_value));
        ("responseState",
          (Option.map x.responseState ~f:ResponseState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let responseState =
        (Option.map ~f:ResponseState.of_xml)
          (Xml.child xml_arg0 "responseState") in
      let responseBody =
        (Option.map ~f:ResponseBody.of_xml)
          (Xml.child xml_arg0 "responseBody") in
      let function_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "function") in
      let confirmationState =
        (Option.map ~f:ConfirmationState.of_xml)
          (Xml.child xml_arg0 "confirmationState") in
      let agentId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "agentId") in
      let actionGroup =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "actionGroup") in
      make ?responseState ?responseBody ?function_ ?confirmationState
        ?agentId ~actionGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let responseState =
        field_map json__ "responseState" ResponseState.of_json in
      let responseBody = field_map json__ "responseBody" ResponseBody.of_json in
      let function_ = field_map json__ "function" String_.of_json in
      let confirmationState =
        field_map json__ "confirmationState" ConfirmationState.of_json in
      let agentId = field_map json__ "agentId" String_.of_json in
      let actionGroup = field_map_exn json__ "actionGroup" String_.of_json in
      make ?responseState ?responseBody ?function_ ?confirmationState
        ?agentId ~actionGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the function that was called from the action group and the response that was returned. This data type is used in the following API operations: In the returnControlInvocationResults of the InvokeAgent request"]
module ApiResult =
  struct
    type nonrec t =
      {
      actionGroup: String_.t
        [@ocaml.doc "The action group that the API operation belongs to."];
      agentId: String_.t option [@ocaml.doc "The agent's ID."];
      apiPath: ApiPath.t option [@ocaml.doc "The path to the API operation."];
      confirmationState: ConfirmationState.t option
        [@ocaml.doc
          "Controls the API operations or functions to invoke based on the user confirmation."];
      httpMethod: String_.t option
        [@ocaml.doc "The HTTP method for the API operation."];
      httpStatusCode: Integer.t option
        [@ocaml.doc
          "http status code from API execution response (for example: 200, 400, 500)."];
      responseBody: ResponseBody.t option
        [@ocaml.doc
          "The response body from the API operation. The key of the object is the content type (currently, only TEXT is supported). The response may be returned directly or from the Lambda function."];
      responseState: ResponseState.t option
        [@ocaml.doc
          "Controls the final response state returned to end user when API/Function execution failed. When this state is FAILURE, the request would fail with dependency failure exception. When this state is REPROMPT, the API/function response will be sent to model for re-prompt"]}
    let context_ = "ApiResult"
    let make ?agentId =
      fun ?apiPath ->
        fun ?confirmationState ->
          fun ?httpMethod ->
            fun ?httpStatusCode ->
              fun ?responseBody ->
                fun ?responseState ->
                  fun ~actionGroup ->
                    fun () ->
                      {
                        agentId;
                        apiPath;
                        confirmationState;
                        httpMethod;
                        httpStatusCode;
                        responseBody;
                        responseState;
                        actionGroup
                      }
    let to_value x =
      structure_to_value
        [("actionGroup", (Some (String_.to_value x.actionGroup)));
        ("agentId", (Option.map x.agentId ~f:String_.to_value));
        ("apiPath", (Option.map x.apiPath ~f:ApiPath.to_value));
        ("confirmationState",
          (Option.map x.confirmationState ~f:ConfirmationState.to_value));
        ("httpMethod", (Option.map x.httpMethod ~f:String_.to_value));
        ("httpStatusCode", (Option.map x.httpStatusCode ~f:Integer.to_value));
        ("responseBody",
          (Option.map x.responseBody ~f:ResponseBody.to_value));
        ("responseState",
          (Option.map x.responseState ~f:ResponseState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let responseState =
        (Option.map ~f:ResponseState.of_xml)
          (Xml.child xml_arg0 "responseState") in
      let responseBody =
        (Option.map ~f:ResponseBody.of_xml)
          (Xml.child xml_arg0 "responseBody") in
      let httpStatusCode =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "httpStatusCode") in
      let httpMethod =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "httpMethod") in
      let confirmationState =
        (Option.map ~f:ConfirmationState.of_xml)
          (Xml.child xml_arg0 "confirmationState") in
      let apiPath =
        (Option.map ~f:ApiPath.of_xml) (Xml.child xml_arg0 "apiPath") in
      let agentId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "agentId") in
      let actionGroup =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "actionGroup") in
      make ?responseState ?responseBody ?httpStatusCode ?httpMethod
        ?confirmationState ?apiPath ?agentId ~actionGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let responseState =
        field_map json__ "responseState" ResponseState.of_json in
      let responseBody = field_map json__ "responseBody" ResponseBody.of_json in
      let httpStatusCode = field_map json__ "httpStatusCode" Integer.of_json in
      let httpMethod = field_map json__ "httpMethod" String_.of_json in
      let confirmationState =
        field_map json__ "confirmationState" ConfirmationState.of_json in
      let apiPath = field_map json__ "apiPath" ApiPath.of_json in
      let agentId = field_map json__ "agentId" String_.of_json in
      let actionGroup = field_map_exn json__ "actionGroup" String_.of_json in
      make ?responseState ?responseBody ?httpStatusCode ?httpMethod
        ?confirmationState ?apiPath ?agentId ~actionGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the API operation that was called from the action group and the response body that was returned. This data type is used in the following API operations: In the returnControlInvocationResults of the InvokeAgent request"]
module InvocationResultMember =
  struct
    type nonrec t =
      {
      apiResult: ApiResult.t option
        [@ocaml.doc
          "The result from the API response from the action group invocation."];
      functionResult: FunctionResult.t option
        [@ocaml.doc
          "The result from the function from the action group invocation."]}
    let make ?apiResult =
      fun ?functionResult -> fun () -> { apiResult; functionResult }
    let to_value x =
      structure_to_value
        [("apiResult", (Option.map x.apiResult ~f:ApiResult.to_value));
        ("functionResult",
          (Option.map x.functionResult ~f:FunctionResult.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let functionResult =
        (Option.map ~f:FunctionResult.of_xml)
          (Xml.child xml_arg0 "functionResult") in
      let apiResult =
        (Option.map ~f:ApiResult.of_xml) (Xml.child xml_arg0 "apiResult") in
      make ?functionResult ?apiResult ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let functionResult =
        field_map json__ "functionResult" FunctionResult.of_json in
      let apiResult = field_map json__ "apiResult" ApiResult.of_json in
      make ?functionResult ?apiResult ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A result from the invocation of an action. For more information, see Return control to the agent developer and Control session context. This data type is used in the following API operations: InvokeAgent request"]
module ReturnControlInvocationResults =
  struct
    type nonrec t = InvocationResultMember.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:InvocationResultMember.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InvocationResultMember.of_xml)
    let of_json j =
      list_of_json ~kind:"ReturnControlInvocationResults"
        ~of_json:InvocationResultMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReturnControlResults =
  struct
    type nonrec t =
      {
      invocationId: String_.t option
        [@ocaml.doc "The action's invocation ID."];
      returnControlInvocationResults: ReturnControlInvocationResults.t option
        [@ocaml.doc "The action invocation result."]}
    let make ?invocationId =
      fun ?returnControlInvocationResults ->
        fun () -> { invocationId; returnControlInvocationResults }
    let to_value x =
      structure_to_value
        [("invocationId", (Option.map x.invocationId ~f:String_.to_value));
        ("returnControlInvocationResults",
          (Option.map x.returnControlInvocationResults
             ~f:ReturnControlInvocationResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnControlInvocationResults =
        (Option.map ~f:ReturnControlInvocationResults.of_xml)
          (Xml.child xml_arg0 "returnControlInvocationResults") in
      let invocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "invocationId") in
      make ?returnControlInvocationResults ?invocationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let returnControlInvocationResults =
        field_map json__ "returnControlInvocationResults"
          ReturnControlInvocationResults.of_json in
      let invocationId = field_map json__ "invocationId" String_.of_json in
      make ?returnControlInvocationResults ?invocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An action invocation result."]
module PayloadType =
  struct
    type nonrec t =
      | TEXT 
      | RETURN_CONTROL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TEXT -> "TEXT"
      | RETURN_CONTROL -> "RETURN_CONTROL"
      | Non_static_id s -> s
    let of_string =
      function
      | "TEXT" -> TEXT
      | "RETURN_CONTROL" -> RETURN_CONTROL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PayloadType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PayloadType" j)
    let to_json = simple_to_json to_value
  end
module AgentCollaboratorPayloadString =
  struct
    type nonrec t = string
    let context_ = "AgentCollaboratorPayloadString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgentCollaboratorPayloadString" j
    let to_json = simple_to_json to_value
  end
module AgentCollaboratorInputPayload =
  struct
    type nonrec t =
      {
      returnControlResults: ReturnControlResults.t option
        [@ocaml.doc "An action invocation result."];
      text: AgentCollaboratorPayloadString.t option
        [@ocaml.doc "Input text."];
      type_: PayloadType.t option [@ocaml.doc "The input type."]}
    let make ?returnControlResults =
      fun ?text ->
        fun ?type_ -> fun () -> { returnControlResults; text; type_ }
    let to_value x =
      structure_to_value
        [("returnControlResults",
           (Option.map x.returnControlResults
              ~f:ReturnControlResults.to_value));
        ("text",
          (Option.map x.text ~f:AgentCollaboratorPayloadString.to_value));
        ("type", (Option.map x.type_ ~f:PayloadType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:PayloadType.of_xml) (Xml.child xml_arg0 "type") in
      let text =
        (Option.map ~f:AgentCollaboratorPayloadString.of_xml)
          (Xml.child xml_arg0 "text") in
      let returnControlResults =
        (Option.map ~f:ReturnControlResults.of_xml)
          (Xml.child xml_arg0 "returnControlResults") in
      make ?type_ ?text ?returnControlResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" PayloadType.of_json in
      let text =
        field_map json__ "text" AgentCollaboratorPayloadString.of_json in
      let returnControlResults =
        field_map json__ "returnControlResults" ReturnControlResults.of_json in
      make ?type_ ?text ?returnControlResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Input for an agent collaborator. The input can be text or an action invocation result."]
module AgentCollaboratorInvocationInput =
  struct
    type nonrec t =
      {
      agentCollaboratorAliasArn: AgentAliasArn.t option
        [@ocaml.doc "The collaborator's alias ARN."];
      agentCollaboratorName: String_.t option
        [@ocaml.doc "The collaborator's name."];
      input: AgentCollaboratorInputPayload.t option
        [@ocaml.doc
          "Text or action invocation result input for the collaborator."]}
    let make ?agentCollaboratorAliasArn =
      fun ?agentCollaboratorName ->
        fun ?input ->
          fun () ->
            { agentCollaboratorAliasArn; agentCollaboratorName; input }
    let to_value x =
      structure_to_value
        [("agentCollaboratorAliasArn",
           (Option.map x.agentCollaboratorAliasArn ~f:AgentAliasArn.to_value));
        ("agentCollaboratorName",
          (Option.map x.agentCollaboratorName ~f:String_.to_value));
        ("input",
          (Option.map x.input ~f:AgentCollaboratorInputPayload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let input =
        (Option.map ~f:AgentCollaboratorInputPayload.of_xml)
          (Xml.child xml_arg0 "input") in
      let agentCollaboratorName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "agentCollaboratorName") in
      let agentCollaboratorAliasArn =
        (Option.map ~f:AgentAliasArn.of_xml)
          (Xml.child xml_arg0 "agentCollaboratorAliasArn") in
      make ?input ?agentCollaboratorName ?agentCollaboratorAliasArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let input =
        field_map json__ "input" AgentCollaboratorInputPayload.of_json in
      let agentCollaboratorName =
        field_map json__ "agentCollaboratorName" String_.of_json in
      let agentCollaboratorAliasArn =
        field_map json__ "agentCollaboratorAliasArn" AgentAliasArn.of_json in
      make ?input ?agentCollaboratorName ?agentCollaboratorAliasArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An agent collaborator invocation input."]
module Name =
  struct
    type nonrec t = string
    let context_ = "Name"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^([0-9a-zA-Z][_-]?){1,100}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Name" j
    let to_json = simple_to_json to_value
  end
module FunctionParameter =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the parameter."];
      type_: String_.t option [@ocaml.doc "The data type of the parameter."];
      value: String_.t option [@ocaml.doc "The value of the parameter."]}
    let make ?name =
      fun ?type_ -> fun ?value -> fun () -> { name; type_; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?value ?type_ ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" String_.of_json in
      let type_ = field_map json__ "type" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?value ?type_ ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a parameter of the function. This data type is used in the following API operations: In the returnControl field of the InvokeAgent response"]
module FunctionParameters =
  struct
    type nonrec t = FunctionParameter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FunctionParameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FunctionParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"FunctionParameters"
        ~of_json:FunctionParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module FunctionInvocationInput =
  struct
    type nonrec t =
      {
      actionGroup: String_.t option
        [@ocaml.doc "The action group that the function belongs to."];
      actionInvocationType: ActionInvocationType.t option
        [@ocaml.doc "Contains information about the function to invoke,"];
      agentId: String_.t option [@ocaml.doc "The agent's ID."];
      collaboratorName: Name.t option [@ocaml.doc "The collaborator's name."];
      function_: String_.t option [@ocaml.doc "The name of the function."];
      parameters: FunctionParameters.t option
        [@ocaml.doc "A list of parameters of the function."]}
    let make ?actionGroup =
      fun ?actionInvocationType ->
        fun ?agentId ->
          fun ?collaboratorName ->
            fun ?function_ ->
              fun ?parameters ->
                fun () ->
                  {
                    actionGroup;
                    actionInvocationType;
                    agentId;
                    collaboratorName;
                    function_;
                    parameters
                  }
    let to_value x =
      structure_to_value
        [("actionGroup", (Option.map x.actionGroup ~f:String_.to_value));
        ("actionInvocationType",
          (Option.map x.actionInvocationType ~f:ActionInvocationType.to_value));
        ("agentId", (Option.map x.agentId ~f:String_.to_value));
        ("collaboratorName",
          (Option.map x.collaboratorName ~f:Name.to_value));
        ("function", (Option.map x.function_ ~f:String_.to_value));
        ("parameters",
          (Option.map x.parameters ~f:FunctionParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:FunctionParameters.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let function_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "function") in
      let collaboratorName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "collaboratorName") in
      let agentId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "agentId") in
      let actionInvocationType =
        (Option.map ~f:ActionInvocationType.of_xml)
          (Xml.child xml_arg0 "actionInvocationType") in
      let actionGroup =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "actionGroup") in
      make ?parameters ?function_ ?collaboratorName ?agentId
        ?actionInvocationType ?actionGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parameters =
        field_map json__ "parameters" FunctionParameters.of_json in
      let function_ = field_map json__ "function" String_.of_json in
      let collaboratorName = field_map json__ "collaboratorName" Name.of_json in
      let agentId = field_map json__ "agentId" String_.of_json in
      let actionInvocationType =
        field_map json__ "actionInvocationType" ActionInvocationType.of_json in
      let actionGroup = field_map json__ "actionGroup" String_.of_json in
      make ?parameters ?function_ ?collaboratorName ?agentId
        ?actionInvocationType ?actionGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the function that the agent predicts should be called. This data type is used in the following API operations: In the returnControl field of the InvokeAgent response"]
module ParameterList =
  struct
    type nonrec t = Parameter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Parameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Parameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ParameterList" ~of_json:Parameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module PropertyParameters =
  struct
    type nonrec t =
      {
      properties: ParameterList.t option
        [@ocaml.doc "A list of parameters in the request body."]}
    let make ?properties = fun () -> { properties }
    let to_value x =
      structure_to_value
        [("properties", (Option.map x.properties ~f:ParameterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let properties =
        (Option.map ~f:ParameterList.of_xml)
          (Xml.child xml_arg0 "properties") in
      make ?properties ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let properties = field_map json__ "properties" ParameterList.of_json in
      make ?properties ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the parameters in the request body."]
module ApiContentMap =
  struct
    type nonrec t = (String_.t * PropertyParameters.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types String PropertyParameters"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x ->
                       (PropertyParameters.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:PropertyParameters.of_json j
    let to_json v = composed_to_json to_value v
  end
module ApiRequestBody =
  struct
    type nonrec t =
      {
      content: ApiContentMap.t option
        [@ocaml.doc
          "The content of the request body. The key of the object in this field is a media type defining the format of the request body."]}
    let make ?content = fun () -> { content }
    let to_value x =
      structure_to_value
        [("content", (Option.map x.content ~f:ApiContentMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let content =
        (Option.map ~f:ApiContentMap.of_xml) (Xml.child xml_arg0 "content") in
      make ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let content = field_map json__ "content" ApiContentMap.of_json in
      make ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request body to provide for the API request, as the agent elicited from the user. This data type is used in the following API operations: InvokeAgent response"]
module ApiParameter =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the parameter."];
      type_: String_.t option [@ocaml.doc "The data type for the parameter."];
      value: String_.t option [@ocaml.doc "The value of the parameter."]}
    let make ?name =
      fun ?type_ -> fun ?value -> fun () -> { name; type_; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?value ?type_ ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" String_.of_json in
      let type_ = field_map json__ "type" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?value ?type_ ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a parameter to provide to the API request. This data type is used in the following API operations: InvokeAgent response"]
module ApiParameters =
  struct
    type nonrec t = ApiParameter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ApiParameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ApiParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ApiParameters" ~of_json:ApiParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ApiInvocationInput =
  struct
    type nonrec t =
      {
      actionGroup: String_.t option
        [@ocaml.doc "The action group that the API operation belongs to."];
      actionInvocationType: ActionInvocationType.t option
        [@ocaml.doc
          "Contains information about the API operation to invoke."];
      agentId: String_.t option [@ocaml.doc "The agent's ID."];
      apiPath: ApiPath.t option [@ocaml.doc "The path to the API operation."];
      collaboratorName: Name.t option
        [@ocaml.doc "The agent collaborator's name."];
      httpMethod: String_.t option
        [@ocaml.doc "The HTTP method of the API operation."];
      parameters: ApiParameters.t option
        [@ocaml.doc
          "The parameters to provide for the API request, as the agent elicited from the user."];
      requestBody: ApiRequestBody.t option
        [@ocaml.doc
          "The request body to provide for the API request, as the agent elicited from the user."]}
    let make ?actionGroup =
      fun ?actionInvocationType ->
        fun ?agentId ->
          fun ?apiPath ->
            fun ?collaboratorName ->
              fun ?httpMethod ->
                fun ?parameters ->
                  fun ?requestBody ->
                    fun () ->
                      {
                        actionGroup;
                        actionInvocationType;
                        agentId;
                        apiPath;
                        collaboratorName;
                        httpMethod;
                        parameters;
                        requestBody
                      }
    let to_value x =
      structure_to_value
        [("actionGroup", (Option.map x.actionGroup ~f:String_.to_value));
        ("actionInvocationType",
          (Option.map x.actionInvocationType ~f:ActionInvocationType.to_value));
        ("agentId", (Option.map x.agentId ~f:String_.to_value));
        ("apiPath", (Option.map x.apiPath ~f:ApiPath.to_value));
        ("collaboratorName",
          (Option.map x.collaboratorName ~f:Name.to_value));
        ("httpMethod", (Option.map x.httpMethod ~f:String_.to_value));
        ("parameters", (Option.map x.parameters ~f:ApiParameters.to_value));
        ("requestBody",
          (Option.map x.requestBody ~f:ApiRequestBody.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let requestBody =
        (Option.map ~f:ApiRequestBody.of_xml)
          (Xml.child xml_arg0 "requestBody") in
      let parameters =
        (Option.map ~f:ApiParameters.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let httpMethod =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "httpMethod") in
      let collaboratorName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "collaboratorName") in
      let apiPath =
        (Option.map ~f:ApiPath.of_xml) (Xml.child xml_arg0 "apiPath") in
      let agentId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "agentId") in
      let actionInvocationType =
        (Option.map ~f:ActionInvocationType.of_xml)
          (Xml.child xml_arg0 "actionInvocationType") in
      let actionGroup =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "actionGroup") in
      make ?requestBody ?parameters ?httpMethod ?collaboratorName ?apiPath
        ?agentId ?actionInvocationType ?actionGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let requestBody = field_map json__ "requestBody" ApiRequestBody.of_json in
      let parameters = field_map json__ "parameters" ApiParameters.of_json in
      let httpMethod = field_map json__ "httpMethod" String_.of_json in
      let collaboratorName = field_map json__ "collaboratorName" Name.of_json in
      let apiPath = field_map json__ "apiPath" ApiPath.of_json in
      let agentId = field_map json__ "agentId" String_.of_json in
      let actionInvocationType =
        field_map json__ "actionInvocationType" ActionInvocationType.of_json in
      let actionGroup = field_map json__ "actionGroup" String_.of_json in
      make ?requestBody ?parameters ?httpMethod ?collaboratorName ?apiPath
        ?agentId ?actionInvocationType ?actionGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the API operation that the agent predicts should be called. This data type is used in the following API operations: In the returnControl field of the InvokeAgent response"]
module InvocationInputMember =
  struct
    type nonrec t =
      {
      apiInvocationInput: ApiInvocationInput.t option
        [@ocaml.doc
          "Contains information about the API operation that the agent predicts should be called."];
      functionInvocationInput: FunctionInvocationInput.t option
        [@ocaml.doc
          "Contains information about the function that the agent predicts should be called."]}
    let make ?apiInvocationInput =
      fun ?functionInvocationInput ->
        fun () -> { apiInvocationInput; functionInvocationInput }
    let to_value x =
      structure_to_value
        [("apiInvocationInput",
           (Option.map x.apiInvocationInput ~f:ApiInvocationInput.to_value));
        ("functionInvocationInput",
          (Option.map x.functionInvocationInput
             ~f:FunctionInvocationInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let functionInvocationInput =
        (Option.map ~f:FunctionInvocationInput.of_xml)
          (Xml.child xml_arg0 "functionInvocationInput") in
      let apiInvocationInput =
        (Option.map ~f:ApiInvocationInput.of_xml)
          (Xml.child xml_arg0 "apiInvocationInput") in
      make ?functionInvocationInput ?apiInvocationInput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let functionInvocationInput =
        field_map json__ "functionInvocationInput"
          FunctionInvocationInput.of_json in
      let apiInvocationInput =
        field_map json__ "apiInvocationInput" ApiInvocationInput.of_json in
      make ?functionInvocationInput ?apiInvocationInput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the API operation or function that the agent predicts should be called. This data type is used in the following API operations: In the returnControl field of the InvokeAgent response"]
module InvocationInputs =
  struct
    type nonrec t = InvocationInputMember.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:InvocationInputMember.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InvocationInputMember.of_xml)
    let of_json j =
      list_of_json ~kind:"InvocationInputs"
        ~of_json:InvocationInputMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReturnControlPayload =
  struct
    type nonrec t =
      {
      invocationId: String_.t option
        [@ocaml.doc "The identifier of the action group invocation."];
      invocationInputs: InvocationInputs.t option
        [@ocaml.doc
          "A list of objects that contain information about the parameters and inputs that need to be sent into the API operation or function, based on what the agent determines from its session with the user."]}
    let make ?invocationId =
      fun ?invocationInputs -> fun () -> { invocationId; invocationInputs }
    let to_value x =
      structure_to_value
        [("invocationId", (Option.map x.invocationId ~f:String_.to_value));
        ("invocationInputs",
          (Option.map x.invocationInputs ~f:InvocationInputs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationInputs =
        (Option.map ~f:InvocationInputs.of_xml)
          (Xml.child xml_arg0 "invocationInputs") in
      let invocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "invocationId") in
      make ?invocationInputs ?invocationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invocationInputs =
        field_map json__ "invocationInputs" InvocationInputs.of_json in
      let invocationId = field_map json__ "invocationId" String_.of_json in
      make ?invocationInputs ?invocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information to return from the action group that the agent has predicted to invoke. This data type is used in the following API operations: InvokeAgent response"]
module AgentCollaboratorOutputPayload =
  struct
    type nonrec t =
      {
      returnControlPayload: ReturnControlPayload.t option
        [@ocaml.doc "An action invocation result."];
      text: AgentCollaboratorPayloadString.t option
        [@ocaml.doc "Text output."];
      type_: PayloadType.t option [@ocaml.doc "The type of output."]}
    let make ?returnControlPayload =
      fun ?text ->
        fun ?type_ -> fun () -> { returnControlPayload; text; type_ }
    let to_value x =
      structure_to_value
        [("returnControlPayload",
           (Option.map x.returnControlPayload
              ~f:ReturnControlPayload.to_value));
        ("text",
          (Option.map x.text ~f:AgentCollaboratorPayloadString.to_value));
        ("type", (Option.map x.type_ ~f:PayloadType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:PayloadType.of_xml) (Xml.child xml_arg0 "type") in
      let text =
        (Option.map ~f:AgentCollaboratorPayloadString.of_xml)
          (Xml.child xml_arg0 "text") in
      let returnControlPayload =
        (Option.map ~f:ReturnControlPayload.of_xml)
          (Xml.child xml_arg0 "returnControlPayload") in
      make ?type_ ?text ?returnControlPayload ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" PayloadType.of_json in
      let text =
        field_map json__ "text" AgentCollaboratorPayloadString.of_json in
      let returnControlPayload =
        field_map json__ "returnControlPayload" ReturnControlPayload.of_json in
      make ?type_ ?text ?returnControlPayload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Output from an agent collaborator. The output can be text or an action invocation result."]
module AgentCollaboratorInvocationOutput =
  struct
    type nonrec t =
      {
      agentCollaboratorAliasArn: AgentAliasArn.t option
        [@ocaml.doc "The output's agent collaborator alias ARN."];
      agentCollaboratorName: String_.t option
        [@ocaml.doc "The output's agent collaborator name."];
      metadata: Metadata.t option
        [@ocaml.doc
          "Contains information about the output from the agent collaborator."];
      output: AgentCollaboratorOutputPayload.t option
        [@ocaml.doc "The output's output."]}
    let make ?agentCollaboratorAliasArn =
      fun ?agentCollaboratorName ->
        fun ?metadata ->
          fun ?output ->
            fun () ->
              {
                agentCollaboratorAliasArn;
                agentCollaboratorName;
                metadata;
                output
              }
    let to_value x =
      structure_to_value
        [("agentCollaboratorAliasArn",
           (Option.map x.agentCollaboratorAliasArn ~f:AgentAliasArn.to_value));
        ("agentCollaboratorName",
          (Option.map x.agentCollaboratorName ~f:String_.to_value));
        ("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("output",
          (Option.map x.output ~f:AgentCollaboratorOutputPayload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let output =
        (Option.map ~f:AgentCollaboratorOutputPayload.of_xml)
          (Xml.child xml_arg0 "output") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      let agentCollaboratorName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "agentCollaboratorName") in
      let agentCollaboratorAliasArn =
        (Option.map ~f:AgentAliasArn.of_xml)
          (Xml.child xml_arg0 "agentCollaboratorAliasArn") in
      make ?output ?metadata ?agentCollaboratorName
        ?agentCollaboratorAliasArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let output =
        field_map json__ "output" AgentCollaboratorOutputPayload.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      let agentCollaboratorName =
        field_map json__ "agentCollaboratorName" String_.of_json in
      let agentCollaboratorAliasArn =
        field_map json__ "agentCollaboratorAliasArn" AgentAliasArn.of_json in
      make ?output ?metadata ?agentCollaboratorName
        ?agentCollaboratorAliasArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Output from an agent collaborator."]
module AgentId =
  struct
    type nonrec t = string
    let context_ = "AgentId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:10) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9a-zA-Z]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgentId" j
    let to_json = simple_to_json to_value
  end
module TraceId =
  struct
    type nonrec t = string
    let context_ = "TraceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:16) >>=
             (fun () -> check_string_min i ~min:2));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TraceId" j
    let to_json = simple_to_json to_value
  end
module RawResponse =
  struct
    type nonrec t =
      {
      content: String_.t option
        [@ocaml.doc "The foundation model's raw output content."]}
    let make ?content = fun () -> { content }
    let to_value x =
      structure_to_value
        [("content", (Option.map x.content ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let content =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "content") in
      make ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let content = field_map json__ "content" String_.of_json in
      make ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the raw output from the foundation model."]
module RoutingClassifierModelInvocationOutput =
  struct
    type nonrec t =
      {
      metadata: Metadata.t option [@ocaml.doc "The invocation's metadata."];
      rawResponse: RawResponse.t option
        [@ocaml.doc "The invocation's raw response."];
      traceId: TraceId.t option [@ocaml.doc "The invocation's trace ID."]}
    let make ?metadata =
      fun ?rawResponse ->
        fun ?traceId -> fun () -> { metadata; rawResponse; traceId }
    let to_value x =
      structure_to_value
        [("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("rawResponse", (Option.map x.rawResponse ~f:RawResponse.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let rawResponse =
        (Option.map ~f:RawResponse.of_xml) (Xml.child xml_arg0 "rawResponse") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      make ?traceId ?rawResponse ?metadata ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let rawResponse = field_map json__ "rawResponse" RawResponse.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      make ?traceId ?rawResponse ?metadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Invocation output from a routing classifier model."]
module Type =
  struct
    type nonrec t =
      | ACTION_GROUP 
      | AGENT_COLLABORATOR 
      | KNOWLEDGE_BASE 
      | FINISH 
      | ASK_USER 
      | REPROMPT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTION_GROUP -> "ACTION_GROUP"
      | AGENT_COLLABORATOR -> "AGENT_COLLABORATOR"
      | KNOWLEDGE_BASE -> "KNOWLEDGE_BASE"
      | FINISH -> "FINISH"
      | ASK_USER -> "ASK_USER"
      | REPROMPT -> "REPROMPT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTION_GROUP" -> ACTION_GROUP
      | "AGENT_COLLABORATOR" -> AGENT_COLLABORATOR
      | "KNOWLEDGE_BASE" -> KNOWLEDGE_BASE
      | "FINISH" -> FINISH
      | "ASK_USER" -> ASK_USER
      | "REPROMPT" -> REPROMPT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Type" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Type" j)
    let to_json = simple_to_json to_value
  end
module Source =
  struct
    type nonrec t =
      | ACTION_GROUP 
      | KNOWLEDGE_BASE 
      | PARSER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTION_GROUP -> "ACTION_GROUP"
      | KNOWLEDGE_BASE -> "KNOWLEDGE_BASE"
      | PARSER -> "PARSER"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTION_GROUP" -> ACTION_GROUP
      | "KNOWLEDGE_BASE" -> KNOWLEDGE_BASE
      | "PARSER" -> PARSER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Source" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Source" j)
    let to_json = simple_to_json to_value
  end
module RepromptResponse =
  struct
    type nonrec t =
      {
      source: Source.t option
        [@ocaml.doc
          "Specifies what output is prompting the agent to reprompt the input."];
      text: String_.t option [@ocaml.doc "The text reprompting the input."]}
    let make ?source = fun ?text -> fun () -> { source; text }
    let to_value x =
      structure_to_value
        [("source", (Option.map x.source ~f:Source.to_value));
        ("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      let source =
        (Option.map ~f:Source.of_xml) (Xml.child xml_arg0 "source") in
      make ?text ?source ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in
      let source = field_map json__ "source" Source.of_json in
      make ?text ?source ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the agent's response to reprompt the input."]
module RetrievalResultMetadataValue =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module RetrievalResultMetadataKey =
  struct
    type nonrec t = string
    let context_ = "RetrievalResultMetadataKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RetrievalResultMetadataKey" j
    let to_json = simple_to_json to_value
  end
module RetrievalResultMetadata =
  struct
    type nonrec t =
      (RetrievalResultMetadataKey.t * RetrievalResultMetadataValue.t) list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types RetrievalResultMetadataKey RetrievalResultMetadataValue"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (RetrievalResultMetadataKey.to_value x) |>
                    (fun x ->
                       (RetrievalResultMetadataValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:RetrievalResultMetadataKey.of_string
        ~of_json:RetrievalResultMetadataValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module RetrievalResultWebLocation =
  struct
    type nonrec t =
      {
      url: String_.t option
        [@ocaml.doc "The web URL/URLs for the data source location."]}
    let make ?url = fun () -> { url }
    let to_value x =
      structure_to_value [("url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "url") in
      make ?url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "url" String_.of_json in make ?url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The web URL/URLs data source location."]
module RetrievalResultSqlLocation =
  struct
    type nonrec t =
      {
      query: String_.t option
        [@ocaml.doc "The SQL query used to retrieve the result."]}
    let make ?query = fun () -> { query }
    let to_value x =
      structure_to_value
        [("query", (Option.map x.query ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let query = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "query") in
      make ?query ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let query = field_map json__ "query" String_.of_json in make ?query ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the SQL query used to retrieve the result."]
module RetrievalResultSharePointLocation =
  struct
    type nonrec t =
      {
      url: String_.t option
        [@ocaml.doc "The SharePoint site URL for the data source location."]}
    let make ?url = fun () -> { url }
    let to_value x =
      structure_to_value [("url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "url") in
      make ?url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "url" String_.of_json in make ?url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The SharePoint data source location."]
module RetrievalResultSalesforceLocation =
  struct
    type nonrec t =
      {
      url: String_.t option
        [@ocaml.doc "The Salesforce host URL for the data source location."]}
    let make ?url = fun () -> { url }
    let to_value x =
      structure_to_value [("url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "url") in
      make ?url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "url" String_.of_json in make ?url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The Salesforce data source location."]
module RetrievalResultS3Location =
  struct
    type nonrec t =
      {
      uri: String_.t option
        [@ocaml.doc "The S3 URI for the data source location."]}
    let make ?uri = fun () -> { uri }
    let to_value x =
      structure_to_value [("uri", (Option.map x.uri ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uri = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "uri") in
      make ?uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uri = field_map json__ "uri" String_.of_json in make ?uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The S3 data source location. This data type is used in the following API operations: Retrieve response \226\128\147 in the s3Location field RetrieveAndGenerate response \226\128\147 in the s3Location field InvokeAgent response \226\128\147 in the s3Location field"]
module RetrievalResultLocationType =
  struct
    type nonrec t =
      | S3 
      | WEB 
      | CONFLUENCE 
      | SALESFORCE 
      | SHAREPOINT 
      | CUSTOM 
      | KENDRA 
      | SQL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | S3 -> "S3"
      | WEB -> "WEB"
      | CONFLUENCE -> "CONFLUENCE"
      | SALESFORCE -> "SALESFORCE"
      | SHAREPOINT -> "SHAREPOINT"
      | CUSTOM -> "CUSTOM"
      | KENDRA -> "KENDRA"
      | SQL -> "SQL"
      | Non_static_id s -> s
    let of_string =
      function
      | "S3" -> S3
      | "WEB" -> WEB
      | "CONFLUENCE" -> CONFLUENCE
      | "SALESFORCE" -> SALESFORCE
      | "SHAREPOINT" -> SHAREPOINT
      | "CUSTOM" -> CUSTOM
      | "KENDRA" -> KENDRA
      | "SQL" -> SQL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RetrievalResultLocationType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RetrievalResultLocationType" j)
    let to_json = simple_to_json to_value
  end
module RetrievalResultKendraDocumentLocation =
  struct
    type nonrec t =
      {
      uri: String_.t option [@ocaml.doc "The document's uri."]}
    let make ?uri = fun () -> { uri }
    let to_value x =
      structure_to_value [("uri", (Option.map x.uri ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uri = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "uri") in
      make ?uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uri = field_map json__ "uri" String_.of_json in make ?uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The location of a result in Amazon Kendra."]
module RetrievalResultCustomDocumentLocation =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the document."]}
    let make ?id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Option.map x.id ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map json__ "id" String_.of_json in make ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the location of a document in a custom data source."]
module RetrievalResultConfluenceLocation =
  struct
    type nonrec t =
      {
      url: String_.t option
        [@ocaml.doc "The Confluence host URL for the data source location."]}
    let make ?url = fun () -> { url }
    let to_value x =
      structure_to_value [("url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "url") in
      make ?url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "url" String_.of_json in make ?url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The Confluence data source location."]
module RetrievalResultLocation =
  struct
    type nonrec t =
      {
      confluenceLocation: RetrievalResultConfluenceLocation.t option
        [@ocaml.doc "The Confluence data source location."];
      customDocumentLocation: RetrievalResultCustomDocumentLocation.t option
        [@ocaml.doc
          "Specifies the location of a document in a custom data source."];
      kendraDocumentLocation: RetrievalResultKendraDocumentLocation.t option
        [@ocaml.doc "The location of a document in Amazon Kendra."];
      s3Location: RetrievalResultS3Location.t option
        [@ocaml.doc "The S3 data source location."];
      salesforceLocation: RetrievalResultSalesforceLocation.t option
        [@ocaml.doc "The Salesforce data source location."];
      sharePointLocation: RetrievalResultSharePointLocation.t option
        [@ocaml.doc "The SharePoint data source location."];
      sqlLocation: RetrievalResultSqlLocation.t option
        [@ocaml.doc
          "Specifies information about the SQL query used to retrieve the result."];
      type_: RetrievalResultLocationType.t option
        [@ocaml.doc "The type of data source location."];
      webLocation: RetrievalResultWebLocation.t option
        [@ocaml.doc "The web URL/URLs data source location."]}
    let make ?confluenceLocation =
      fun ?customDocumentLocation ->
        fun ?kendraDocumentLocation ->
          fun ?s3Location ->
            fun ?salesforceLocation ->
              fun ?sharePointLocation ->
                fun ?sqlLocation ->
                  fun ?type_ ->
                    fun ?webLocation ->
                      fun () ->
                        {
                          confluenceLocation;
                          customDocumentLocation;
                          kendraDocumentLocation;
                          s3Location;
                          salesforceLocation;
                          sharePointLocation;
                          sqlLocation;
                          type_;
                          webLocation
                        }
    let to_value x =
      structure_to_value
        [("confluenceLocation",
           (Option.map x.confluenceLocation
              ~f:RetrievalResultConfluenceLocation.to_value));
        ("customDocumentLocation",
          (Option.map x.customDocumentLocation
             ~f:RetrievalResultCustomDocumentLocation.to_value));
        ("kendraDocumentLocation",
          (Option.map x.kendraDocumentLocation
             ~f:RetrievalResultKendraDocumentLocation.to_value));
        ("s3Location",
          (Option.map x.s3Location ~f:RetrievalResultS3Location.to_value));
        ("salesforceLocation",
          (Option.map x.salesforceLocation
             ~f:RetrievalResultSalesforceLocation.to_value));
        ("sharePointLocation",
          (Option.map x.sharePointLocation
             ~f:RetrievalResultSharePointLocation.to_value));
        ("sqlLocation",
          (Option.map x.sqlLocation ~f:RetrievalResultSqlLocation.to_value));
        ("type",
          (Option.map x.type_ ~f:RetrievalResultLocationType.to_value));
        ("webLocation",
          (Option.map x.webLocation ~f:RetrievalResultWebLocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let webLocation =
        (Option.map ~f:RetrievalResultWebLocation.of_xml)
          (Xml.child xml_arg0 "webLocation") in
      let type_ =
        (Option.map ~f:RetrievalResultLocationType.of_xml)
          (Xml.child xml_arg0 "type") in
      let sqlLocation =
        (Option.map ~f:RetrievalResultSqlLocation.of_xml)
          (Xml.child xml_arg0 "sqlLocation") in
      let sharePointLocation =
        (Option.map ~f:RetrievalResultSharePointLocation.of_xml)
          (Xml.child xml_arg0 "sharePointLocation") in
      let salesforceLocation =
        (Option.map ~f:RetrievalResultSalesforceLocation.of_xml)
          (Xml.child xml_arg0 "salesforceLocation") in
      let s3Location =
        (Option.map ~f:RetrievalResultS3Location.of_xml)
          (Xml.child xml_arg0 "s3Location") in
      let kendraDocumentLocation =
        (Option.map ~f:RetrievalResultKendraDocumentLocation.of_xml)
          (Xml.child xml_arg0 "kendraDocumentLocation") in
      let customDocumentLocation =
        (Option.map ~f:RetrievalResultCustomDocumentLocation.of_xml)
          (Xml.child xml_arg0 "customDocumentLocation") in
      let confluenceLocation =
        (Option.map ~f:RetrievalResultConfluenceLocation.of_xml)
          (Xml.child xml_arg0 "confluenceLocation") in
      make ?webLocation ?type_ ?sqlLocation ?sharePointLocation
        ?salesforceLocation ?s3Location ?kendraDocumentLocation
        ?customDocumentLocation ?confluenceLocation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let webLocation =
        field_map json__ "webLocation" RetrievalResultWebLocation.of_json in
      let type_ = field_map json__ "type" RetrievalResultLocationType.of_json in
      let sqlLocation =
        field_map json__ "sqlLocation" RetrievalResultSqlLocation.of_json in
      let sharePointLocation =
        field_map json__ "sharePointLocation"
          RetrievalResultSharePointLocation.of_json in
      let salesforceLocation =
        field_map json__ "salesforceLocation"
          RetrievalResultSalesforceLocation.of_json in
      let s3Location =
        field_map json__ "s3Location" RetrievalResultS3Location.of_json in
      let kendraDocumentLocation =
        field_map json__ "kendraDocumentLocation"
          RetrievalResultKendraDocumentLocation.of_json in
      let customDocumentLocation =
        field_map json__ "customDocumentLocation"
          RetrievalResultCustomDocumentLocation.of_json in
      let confluenceLocation =
        field_map json__ "confluenceLocation"
          RetrievalResultConfluenceLocation.of_json in
      make ?webLocation ?type_ ?sqlLocation ?sharePointLocation
        ?salesforceLocation ?s3Location ?kendraDocumentLocation
        ?customDocumentLocation ?confluenceLocation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the data source location. This data type is used in the following API operations: Retrieve response \226\128\147 in the location field RetrieveAndGenerate response \226\128\147 in the location field InvokeAgent response \226\128\147 in the location field"]
module VideoSegmentS3UriString =
  struct
    type nonrec t = string
    let context_ = "VideoSegmentS3UriString"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VideoSegmentS3UriString" j
    let to_json = simple_to_json to_value
  end
module VideoSegment =
  struct
    type nonrec t =
      {
      s3Uri: VideoSegmentS3UriString.t option
        [@ocaml.doc
          "The S3 URI where this specific video segment is stored in the multimodal storage destination."];
      summary: String_.t option
        [@ocaml.doc
          "A text summary describing the content of the video segment."]}
    let make ?s3Uri = fun ?summary -> fun () -> { s3Uri; summary }
    let to_value x =
      structure_to_value
        [("s3Uri", (Option.map x.s3Uri ~f:VideoSegmentS3UriString.to_value));
        ("summary", (Option.map x.summary ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let summary =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "summary") in
      let s3Uri =
        (Option.map ~f:VideoSegmentS3UriString.of_xml)
          (Xml.child xml_arg0 "s3Uri") in
      make ?summary ?s3Uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let summary = field_map json__ "summary" String_.of_json in
      let s3Uri = field_map json__ "s3Uri" VideoSegmentS3UriString.of_json in
      make ?summary ?s3Uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a video segment retrieved from a knowledge base, including its location and summary. This data type is used in the following API operations: Retrieve response \226\128\147 in the video field"]
module RetrievalResultContentType =
  struct
    type nonrec t =
      | TEXT 
      | IMAGE 
      | ROW 
      | AUDIO 
      | VIDEO 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TEXT -> "TEXT"
      | IMAGE -> "IMAGE"
      | ROW -> "ROW"
      | AUDIO -> "AUDIO"
      | VIDEO -> "VIDEO"
      | Non_static_id s -> s
    let of_string =
      function
      | "TEXT" -> TEXT
      | "IMAGE" -> IMAGE
      | "ROW" -> ROW
      | "AUDIO" -> AUDIO
      | "VIDEO" -> VIDEO
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RetrievalResultContentType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RetrievalResultContentType" j)
    let to_json = simple_to_json to_value
  end
module RetrievalResultContentColumnType =
  struct
    type nonrec t =
      | BLOB 
      | BOOLEAN 
      | DOUBLE 
      | NULL 
      | LONG 
      | STRING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BLOB -> "BLOB"
      | BOOLEAN -> "BOOLEAN"
      | DOUBLE -> "DOUBLE"
      | NULL -> "NULL"
      | LONG -> "LONG"
      | STRING -> "STRING"
      | Non_static_id s -> s
    let of_string =
      function
      | "BLOB" -> BLOB
      | "BOOLEAN" -> BOOLEAN
      | "DOUBLE" -> DOUBLE
      | "NULL" -> NULL
      | "LONG" -> LONG
      | "STRING" -> STRING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RetrievalResultContentColumnType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RetrievalResultContentColumnType" j)
    let to_json = simple_to_json to_value
  end
module RetrievalResultContentColumn =
  struct
    type nonrec t =
      {
      columnName: String_.t option [@ocaml.doc "The name of the column."];
      columnValue: String_.t option [@ocaml.doc "The value in the column."];
      type_: RetrievalResultContentColumnType.t option
        [@ocaml.doc "The data type of the value."]}
    let make ?columnName =
      fun ?columnValue ->
        fun ?type_ -> fun () -> { columnName; columnValue; type_ }
    let to_value x =
      structure_to_value
        [("columnName", (Option.map x.columnName ~f:String_.to_value));
        ("columnValue", (Option.map x.columnValue ~f:String_.to_value));
        ("type",
          (Option.map x.type_ ~f:RetrievalResultContentColumnType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:RetrievalResultContentColumnType.of_xml)
          (Xml.child xml_arg0 "type") in
      let columnValue =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "columnValue") in
      let columnName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "columnName") in
      make ?type_ ?columnValue ?columnName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ =
        field_map json__ "type" RetrievalResultContentColumnType.of_json in
      let columnValue = field_map json__ "columnValue" String_.of_json in
      let columnName = field_map json__ "columnName" String_.of_json in
      make ?type_ ?columnValue ?columnName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a column with a cell to return in retrieval."]
module RetrievalResultContentRow =
  struct
    type nonrec t = RetrievalResultContentColumn.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RetrievalResultContentColumn.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RetrievalResultContentColumn.of_xml)
    let of_json j =
      list_of_json ~kind:"RetrievalResultContentRow"
        ~of_json:RetrievalResultContentColumn.of_json j
    let to_json v = composed_to_json to_value v
  end
module AudioSegmentS3UriString =
  struct
    type nonrec t = string
    let context_ = "AudioSegmentS3UriString"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AudioSegmentS3UriString" j
    let to_json = simple_to_json to_value
  end
module AudioSegment =
  struct
    type nonrec t =
      {
      s3Uri: AudioSegmentS3UriString.t option
        [@ocaml.doc
          "The S3 URI where this specific audio segment is stored in the multimodal storage destination."];
      transcription: String_.t option
        [@ocaml.doc "The text transcription of the audio segment content."]}
    let make ?s3Uri =
      fun ?transcription -> fun () -> { s3Uri; transcription }
    let to_value x =
      structure_to_value
        [("s3Uri", (Option.map x.s3Uri ~f:AudioSegmentS3UriString.to_value));
        ("transcription", (Option.map x.transcription ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transcription =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "transcription") in
      let s3Uri =
        (Option.map ~f:AudioSegmentS3UriString.of_xml)
          (Xml.child xml_arg0 "s3Uri") in
      make ?transcription ?s3Uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transcription = field_map json__ "transcription" String_.of_json in
      let s3Uri = field_map json__ "s3Uri" AudioSegmentS3UriString.of_json in
      make ?transcription ?s3Uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an audio segment retrieved from a knowledge base, including its location and transcription. This data type is used in the following API operations: Retrieve response \226\128\147 in the audio field"]
module RetrievalResultContent =
  struct
    type nonrec t =
      {
      audio: AudioSegment.t option
        [@ocaml.doc
          "Audio segment information when the retrieval result contains audio content."];
      byteContent: String_.t option
        [@ocaml.doc
          "A data URI with base64-encoded content from the data source. The URI is in the following format: returned in the following format: data:image/jpeg;base64,$\\{base64-encoded string\\}."];
      row: RetrievalResultContentRow.t option
        [@ocaml.doc
          "Specifies information about the rows with the cells to return in retrieval."];
      text: String_.t option
        [@ocaml.doc "The cited text from the data source."];
      type_: RetrievalResultContentType.t option
        [@ocaml.doc "The type of content in the retrieval result."];
      video: VideoSegment.t option
        [@ocaml.doc
          "Video segment information when the retrieval result contains video content."]}
    let make ?audio =
      fun ?byteContent ->
        fun ?row ->
          fun ?text ->
            fun ?type_ ->
              fun ?video ->
                fun () -> { audio; byteContent; row; text; type_; video }
    let to_value x =
      structure_to_value
        [("audio", (Option.map x.audio ~f:AudioSegment.to_value));
        ("byteContent", (Option.map x.byteContent ~f:String_.to_value));
        ("row", (Option.map x.row ~f:RetrievalResultContentRow.to_value));
        ("text", (Option.map x.text ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:RetrievalResultContentType.to_value));
        ("video", (Option.map x.video ~f:VideoSegment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let video =
        (Option.map ~f:VideoSegment.of_xml) (Xml.child xml_arg0 "video") in
      let type_ =
        (Option.map ~f:RetrievalResultContentType.of_xml)
          (Xml.child xml_arg0 "type") in
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      let row =
        (Option.map ~f:RetrievalResultContentRow.of_xml)
          (Xml.child xml_arg0 "row") in
      let byteContent =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "byteContent") in
      let audio =
        (Option.map ~f:AudioSegment.of_xml) (Xml.child xml_arg0 "audio") in
      make ?video ?type_ ?text ?row ?byteContent ?audio ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let video = field_map json__ "video" VideoSegment.of_json in
      let type_ = field_map json__ "type" RetrievalResultContentType.of_json in
      let text = field_map json__ "text" String_.of_json in
      let row = field_map json__ "row" RetrievalResultContentRow.of_json in
      let byteContent = field_map json__ "byteContent" String_.of_json in
      let audio = field_map json__ "audio" AudioSegment.of_json in
      make ?video ?type_ ?text ?row ?byteContent ?audio ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a chunk of text from a data source in the knowledge base. If the result is from a structured data source, the cell in the database and the type of the value is also identified. This data type is used in the following API operations: Retrieve response \226\128\147 in the content field RetrieveAndGenerate response \226\128\147 in the content field InvokeAgent response \226\128\147 in the content field"]
module RetrievedReference =
  struct
    type nonrec t =
      {
      content: RetrievalResultContent.t option
        [@ocaml.doc "Contains the cited text from the data source."];
      location: RetrievalResultLocation.t option
        [@ocaml.doc
          "Contains information about the location of the data source."];
      metadata: RetrievalResultMetadata.t option
        [@ocaml.doc
          "Contains metadata attributes and their values for the file in the data source. For more information, see Metadata and filtering."]}
    let make ?content =
      fun ?location ->
        fun ?metadata -> fun () -> { content; location; metadata }
    let to_value x =
      structure_to_value
        [("content",
           (Option.map x.content ~f:RetrievalResultContent.to_value));
        ("location",
          (Option.map x.location ~f:RetrievalResultLocation.to_value));
        ("metadata",
          (Option.map x.metadata ~f:RetrievalResultMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadata =
        (Option.map ~f:RetrievalResultMetadata.of_xml)
          (Xml.child xml_arg0 "metadata") in
      let location =
        (Option.map ~f:RetrievalResultLocation.of_xml)
          (Xml.child xml_arg0 "location") in
      let content =
        (Option.map ~f:RetrievalResultContent.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?metadata ?location ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let metadata =
        field_map json__ "metadata" RetrievalResultMetadata.of_json in
      let location =
        field_map json__ "location" RetrievalResultLocation.of_json in
      let content = field_map json__ "content" RetrievalResultContent.of_json in
      make ?metadata ?location ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains metadata about a source cited for the generated response. This data type is used in the following API operations: RetrieveAndGenerate response \226\128\147 in the retrievedReferences field InvokeAgent response \226\128\147 in the retrievedReferences field"]
module RetrievedReferences =
  struct
    type nonrec t = RetrievedReference.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RetrievedReference.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RetrievedReference.of_xml)
    let of_json j =
      list_of_json ~kind:"RetrievedReferences"
        ~of_json:RetrievedReference.of_json j
    let to_json v = composed_to_json to_value v
  end
module KnowledgeBaseLookupOutput =
  struct
    type nonrec t =
      {
      metadata: Metadata.t option
        [@ocaml.doc "Contains information about the knowledge base output."];
      retrievedReferences: RetrievedReferences.t option
        [@ocaml.doc
          "Contains metadata about the sources cited for the generated response."]}
    let make ?metadata =
      fun ?retrievedReferences -> fun () -> { metadata; retrievedReferences }
    let to_value x =
      structure_to_value
        [("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("retrievedReferences",
          (Option.map x.retrievedReferences ~f:RetrievedReferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retrievedReferences =
        (Option.map ~f:RetrievedReferences.of_xml)
          (Xml.child xml_arg0 "retrievedReferences") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      make ?retrievedReferences ?metadata ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retrievedReferences =
        field_map json__ "retrievedReferences" RetrievedReferences.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      make ?retrievedReferences ?metadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the results from looking up the knowledge base."]
module FinalResponseString =
  struct
    type nonrec t = string
    let context_ = "FinalResponseString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FinalResponseString" j
    let to_json = simple_to_json to_value
  end
module FinalResponse =
  struct
    type nonrec t =
      {
      metadata: Metadata.t option
        [@ocaml.doc "Contains information about the invoke agent operation."];
      text: FinalResponseString.t option
        [@ocaml.doc "The text in the response to the user."]}
    let make ?metadata = fun ?text -> fun () -> { metadata; text }
    let to_value x =
      structure_to_value
        [("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("text", (Option.map x.text ~f:FinalResponseString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        (Option.map ~f:FinalResponseString.of_xml)
          (Xml.child xml_arg0 "text") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      make ?text ?metadata ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" FinalResponseString.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      make ?text ?metadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains details about the response to the user."]
module Files =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j = list_of_json ~kind:"Files" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module CodeInterpreterInvocationOutput =
  struct
    type nonrec t =
      {
      executionError: String_.t option
        [@ocaml.doc "Contains the error returned from code execution."];
      executionOutput: String_.t option
        [@ocaml.doc
          "Contains the successful output returned from code execution"];
      executionTimeout: Boolean.t option
        [@ocaml.doc "Indicates if the execution of the code timed out."];
      files: Files.t option
        [@ocaml.doc "Contains output files, if generated by code execution."];
      metadata: Metadata.t option
        [@ocaml.doc
          "Contains information about the output from the code interpreter."]}
    let make ?executionError =
      fun ?executionOutput ->
        fun ?executionTimeout ->
          fun ?files ->
            fun ?metadata ->
              fun () ->
                {
                  executionError;
                  executionOutput;
                  executionTimeout;
                  files;
                  metadata
                }
    let to_value x =
      structure_to_value
        [("executionError",
           (Option.map x.executionError ~f:String_.to_value));
        ("executionOutput",
          (Option.map x.executionOutput ~f:String_.to_value));
        ("executionTimeout",
          (Option.map x.executionTimeout ~f:Boolean.to_value));
        ("files", (Option.map x.files ~f:Files.to_value));
        ("metadata", (Option.map x.metadata ~f:Metadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      let files = (Option.map ~f:Files.of_xml) (Xml.child xml_arg0 "files") in
      let executionTimeout =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "executionTimeout") in
      let executionOutput =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "executionOutput") in
      let executionError =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "executionError") in
      make ?metadata ?files ?executionTimeout ?executionOutput
        ?executionError ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let metadata = field_map json__ "metadata" Metadata.of_json in
      let files = field_map json__ "files" Files.of_json in
      let executionTimeout =
        field_map json__ "executionTimeout" Boolean.of_json in
      let executionOutput =
        field_map json__ "executionOutput" String_.of_json in
      let executionError = field_map json__ "executionError" String_.of_json in
      make ?metadata ?files ?executionTimeout ?executionOutput
        ?executionError ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the JSON-formatted string returned by the API invoked by the code interpreter."]
module Observation =
  struct
    type nonrec t =
      {
      actionGroupInvocationOutput: ActionGroupInvocationOutput.t option
        [@ocaml.doc
          "Contains the JSON-formatted string returned by the API invoked by the action group."];
      agentCollaboratorInvocationOutput:
        AgentCollaboratorInvocationOutput.t option
        [@ocaml.doc "A collaborator's invocation output."];
      codeInterpreterInvocationOutput:
        CodeInterpreterInvocationOutput.t option
        [@ocaml.doc
          "Contains the JSON-formatted string returned by the API invoked by the code interpreter."];
      finalResponse: FinalResponse.t option
        [@ocaml.doc "Contains details about the response to the user."];
      knowledgeBaseLookupOutput: KnowledgeBaseLookupOutput.t option
        [@ocaml.doc
          "Contains details about the results from looking up the knowledge base."];
      repromptResponse: RepromptResponse.t option
        [@ocaml.doc
          "Contains details about the response to reprompt the input."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace."];
      type_: Type.t option
        [@ocaml.doc
          "Specifies what kind of information the agent returns in the observation. The following values are possible. ACTION_GROUP \226\128\147 The agent returns the result of an action group. KNOWLEDGE_BASE \226\128\147 The agent returns information from a knowledge base. FINISH \226\128\147 The agent returns a final response to the user with no follow-up. ASK_USER \226\128\147 The agent asks the user a question. REPROMPT \226\128\147 The agent prompts the user again for the same information."]}
    let make ?actionGroupInvocationOutput =
      fun ?agentCollaboratorInvocationOutput ->
        fun ?codeInterpreterInvocationOutput ->
          fun ?finalResponse ->
            fun ?knowledgeBaseLookupOutput ->
              fun ?repromptResponse ->
                fun ?traceId ->
                  fun ?type_ ->
                    fun () ->
                      {
                        actionGroupInvocationOutput;
                        agentCollaboratorInvocationOutput;
                        codeInterpreterInvocationOutput;
                        finalResponse;
                        knowledgeBaseLookupOutput;
                        repromptResponse;
                        traceId;
                        type_
                      }
    let to_value x =
      structure_to_value
        [("actionGroupInvocationOutput",
           (Option.map x.actionGroupInvocationOutput
              ~f:ActionGroupInvocationOutput.to_value));
        ("agentCollaboratorInvocationOutput",
          (Option.map x.agentCollaboratorInvocationOutput
             ~f:AgentCollaboratorInvocationOutput.to_value));
        ("codeInterpreterInvocationOutput",
          (Option.map x.codeInterpreterInvocationOutput
             ~f:CodeInterpreterInvocationOutput.to_value));
        ("finalResponse",
          (Option.map x.finalResponse ~f:FinalResponse.to_value));
        ("knowledgeBaseLookupOutput",
          (Option.map x.knowledgeBaseLookupOutput
             ~f:KnowledgeBaseLookupOutput.to_value));
        ("repromptResponse",
          (Option.map x.repromptResponse ~f:RepromptResponse.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value));
        ("type", (Option.map x.type_ ~f:Type.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:Type.of_xml) (Xml.child xml_arg0 "type") in
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let repromptResponse =
        (Option.map ~f:RepromptResponse.of_xml)
          (Xml.child xml_arg0 "repromptResponse") in
      let knowledgeBaseLookupOutput =
        (Option.map ~f:KnowledgeBaseLookupOutput.of_xml)
          (Xml.child xml_arg0 "knowledgeBaseLookupOutput") in
      let finalResponse =
        (Option.map ~f:FinalResponse.of_xml)
          (Xml.child xml_arg0 "finalResponse") in
      let codeInterpreterInvocationOutput =
        (Option.map ~f:CodeInterpreterInvocationOutput.of_xml)
          (Xml.child xml_arg0 "codeInterpreterInvocationOutput") in
      let agentCollaboratorInvocationOutput =
        (Option.map ~f:AgentCollaboratorInvocationOutput.of_xml)
          (Xml.child xml_arg0 "agentCollaboratorInvocationOutput") in
      let actionGroupInvocationOutput =
        (Option.map ~f:ActionGroupInvocationOutput.of_xml)
          (Xml.child xml_arg0 "actionGroupInvocationOutput") in
      make ?type_ ?traceId ?repromptResponse ?knowledgeBaseLookupOutput
        ?finalResponse ?codeInterpreterInvocationOutput
        ?agentCollaboratorInvocationOutput ?actionGroupInvocationOutput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" Type.of_json in
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let repromptResponse =
        field_map json__ "repromptResponse" RepromptResponse.of_json in
      let knowledgeBaseLookupOutput =
        field_map json__ "knowledgeBaseLookupOutput"
          KnowledgeBaseLookupOutput.of_json in
      let finalResponse =
        field_map json__ "finalResponse" FinalResponse.of_json in
      let codeInterpreterInvocationOutput =
        field_map json__ "codeInterpreterInvocationOutput"
          CodeInterpreterInvocationOutput.of_json in
      let agentCollaboratorInvocationOutput =
        field_map json__ "agentCollaboratorInvocationOutput"
          AgentCollaboratorInvocationOutput.of_json in
      let actionGroupInvocationOutput =
        field_map json__ "actionGroupInvocationOutput"
          ActionGroupInvocationOutput.of_json in
      make ?type_ ?traceId ?repromptResponse ?knowledgeBaseLookupOutput
        ?finalResponse ?codeInterpreterInvocationOutput
        ?agentCollaboratorInvocationOutput ?actionGroupInvocationOutput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the result or output of an action group or knowledge base, or the response to the user."]
module PromptType =
  struct
    type nonrec t =
      | PRE_PROCESSING 
      | ORCHESTRATION 
      | KNOWLEDGE_BASE_RESPONSE_GENERATION 
      | POST_PROCESSING 
      | ROUTING_CLASSIFIER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PRE_PROCESSING -> "PRE_PROCESSING"
      | ORCHESTRATION -> "ORCHESTRATION"
      | KNOWLEDGE_BASE_RESPONSE_GENERATION ->
          "KNOWLEDGE_BASE_RESPONSE_GENERATION"
      | POST_PROCESSING -> "POST_PROCESSING"
      | ROUTING_CLASSIFIER -> "ROUTING_CLASSIFIER"
      | Non_static_id s -> s
    let of_string =
      function
      | "PRE_PROCESSING" -> PRE_PROCESSING
      | "ORCHESTRATION" -> ORCHESTRATION
      | "KNOWLEDGE_BASE_RESPONSE_GENERATION" ->
          KNOWLEDGE_BASE_RESPONSE_GENERATION
      | "POST_PROCESSING" -> POST_PROCESSING
      | "ROUTING_CLASSIFIER" -> ROUTING_CLASSIFIER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PromptType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PromptType" j)
    let to_json = simple_to_json to_value
  end
module PromptText =
  struct
    type nonrec t = string
    let context_ = "PromptText"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PromptText" j
    let to_json = simple_to_json to_value
  end
module ModelIdentifier =
  struct
    type nonrec t = string
    let context_ = "ModelIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(^arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:(([0-9]{12}:custom-model/[a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}(([:][a-z0-9-]{1,63}){0,2})?/[a-z0-9]{12})|(:foundation-model/([a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([.]?[a-z0-9-]{1,63})([:][a-z0-9-]{1,63}){0,2})))|(([a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([.]?[a-z0-9-]{1,63})([:][a-z0-9-]{1,63}){0,2}))|(([0-9a-zA-Z][_-]?)+))$|(^arn:aws(|-us-gov|-cn|-iso|-iso-b):bedrock:(|[0-9a-z-]{1,20}):(|[0-9]{12}):inference-profile/[a-zA-Z0-9-:.]+)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ModelIdentifier" j
    let to_json = simple_to_json to_value
  end
module LambdaArn =
  struct
    type nonrec t = string
    let context_ = "LambdaArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LambdaArn" j
    let to_json = simple_to_json to_value
  end
module TopP =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:1.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Float x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a float" xml_arg0)
    let of_json j = float_of_json ~kind:"a float" j
    let to_json = simple_to_json to_value
  end
module TopK =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:500) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for TopK" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Temperature =
  struct
    type nonrec t = float
    let make i =
      let open Result in
        ok_or_failwith
          ((check_float_min i ~min:1.) >>=
             (fun () -> check_float_min i ~min:0.));
        i
    let of_string = Float.of_string
    let to_value x = `Float x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a float" xml_arg0)
    let of_json j = float_of_json ~kind:"a float" j
    let to_json = simple_to_json to_value
  end
module StopSequences =
  struct
    type nonrec t = String_.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:4) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"StopSequences" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaximumLength =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:8192) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaximumLength" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module InferenceConfiguration =
  struct
    type nonrec t =
      {
      maximumLength: MaximumLength.t option
        [@ocaml.doc
          "The maximum number of tokens allowed in the generated response."];
      stopSequences: StopSequences.t option
        [@ocaml.doc
          "A list of stop sequences. A stop sequence is a sequence of characters that causes the model to stop generating the response."];
      temperature: Temperature.t option
        [@ocaml.doc
          "The likelihood of the model selecting higher-probability options while generating a response. A lower value makes the model more likely to choose higher-probability options, while a higher value makes the model more likely to choose lower-probability options."];
      topK: TopK.t option
        [@ocaml.doc
          "While generating a response, the model determines the probability of the following token at each point of generation. The value that you set for topK is the number of most-likely candidates from which the model chooses the next token in the sequence. For example, if you set topK to 50, the model selects the next token from among the top 50 most likely choices."];
      topP: TopP.t option
        [@ocaml.doc
          "While generating a response, the model determines the probability of the following token at each point of generation. The value that you set for Top P determines the number of most-likely candidates from which the model chooses the next token in the sequence. For example, if you set topP to 0.8, the model only selects the next token from the top 80% of the probability distribution of next tokens."]}
    let make ?maximumLength =
      fun ?stopSequences ->
        fun ?temperature ->
          fun ?topK ->
            fun ?topP ->
              fun () ->
                { maximumLength; stopSequences; temperature; topK; topP }
    let to_value x =
      structure_to_value
        [("maximumLength",
           (Option.map x.maximumLength ~f:MaximumLength.to_value));
        ("stopSequences",
          (Option.map x.stopSequences ~f:StopSequences.to_value));
        ("temperature", (Option.map x.temperature ~f:Temperature.to_value));
        ("topK", (Option.map x.topK ~f:TopK.to_value));
        ("topP", (Option.map x.topP ~f:TopP.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topP = (Option.map ~f:TopP.of_xml) (Xml.child xml_arg0 "topP") in
      let topK = (Option.map ~f:TopK.of_xml) (Xml.child xml_arg0 "topK") in
      let temperature =
        (Option.map ~f:Temperature.of_xml) (Xml.child xml_arg0 "temperature") in
      let stopSequences =
        (Option.map ~f:StopSequences.of_xml)
          (Xml.child xml_arg0 "stopSequences") in
      let maximumLength =
        (Option.map ~f:MaximumLength.of_xml)
          (Xml.child xml_arg0 "maximumLength") in
      make ?topP ?topK ?temperature ?stopSequences ?maximumLength ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let topP = field_map json__ "topP" TopP.of_json in
      let topK = field_map json__ "topK" TopK.of_json in
      let temperature = field_map json__ "temperature" Temperature.of_json in
      let stopSequences =
        field_map json__ "stopSequences" StopSequences.of_json in
      let maximumLength =
        field_map json__ "maximumLength" MaximumLength.of_json in
      make ?topP ?topK ?temperature ?stopSequences ?maximumLength ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifications about the inference parameters that were provided alongside the prompt. These are specified in the PromptOverrideConfiguration object that was set when the agent was created or updated. For more information, see Inference parameters for foundation models."]
module CreationMode =
  struct
    type nonrec t =
      | DEFAULT 
      | OVERRIDDEN 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DEFAULT -> "DEFAULT"
      | OVERRIDDEN -> "OVERRIDDEN"
      | Non_static_id s -> s
    let of_string =
      function
      | "DEFAULT" -> DEFAULT
      | "OVERRIDDEN" -> OVERRIDDEN
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CreationMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CreationMode" j)
    let to_json = simple_to_json to_value
  end
module ModelInvocationInput =
  struct
    type nonrec t =
      {
      foundationModel: ModelIdentifier.t option
        [@ocaml.doc "The identifier of a foundation model."];
      inferenceConfiguration: InferenceConfiguration.t option
        [@ocaml.doc
          "Specifications about the inference parameters that were provided alongside the prompt. These are specified in the PromptOverrideConfiguration object that was set when the agent was created or updated. For more information, see Inference parameters for foundation models."];
      overrideLambda: LambdaArn.t option
        [@ocaml.doc
          "The ARN of the Lambda function to use when parsing the raw foundation model output in parts of the agent sequence."];
      parserMode: CreationMode.t option
        [@ocaml.doc
          "Specifies whether to override the default parser Lambda function when parsing the raw foundation model output in the part of the agent sequence defined by the promptType."];
      promptCreationMode: CreationMode.t option
        [@ocaml.doc
          "Specifies whether the default prompt template was OVERRIDDEN. If it was, the basePromptTemplate that was set in the PromptOverrideConfiguration object when the agent was created or updated is used instead."];
      text: PromptText.t option
        [@ocaml.doc "The text that prompted the agent at this step."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace."];
      type_: PromptType.t option
        [@ocaml.doc "The step in the agent sequence."]}
    let make ?foundationModel =
      fun ?inferenceConfiguration ->
        fun ?overrideLambda ->
          fun ?parserMode ->
            fun ?promptCreationMode ->
              fun ?text ->
                fun ?traceId ->
                  fun ?type_ ->
                    fun () ->
                      {
                        foundationModel;
                        inferenceConfiguration;
                        overrideLambda;
                        parserMode;
                        promptCreationMode;
                        text;
                        traceId;
                        type_
                      }
    let to_value x =
      structure_to_value
        [("foundationModel",
           (Option.map x.foundationModel ~f:ModelIdentifier.to_value));
        ("inferenceConfiguration",
          (Option.map x.inferenceConfiguration
             ~f:InferenceConfiguration.to_value));
        ("overrideLambda",
          (Option.map x.overrideLambda ~f:LambdaArn.to_value));
        ("parserMode", (Option.map x.parserMode ~f:CreationMode.to_value));
        ("promptCreationMode",
          (Option.map x.promptCreationMode ~f:CreationMode.to_value));
        ("text", (Option.map x.text ~f:PromptText.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value));
        ("type", (Option.map x.type_ ~f:PromptType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:PromptType.of_xml) (Xml.child xml_arg0 "type") in
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let text =
        (Option.map ~f:PromptText.of_xml) (Xml.child xml_arg0 "text") in
      let promptCreationMode =
        (Option.map ~f:CreationMode.of_xml)
          (Xml.child xml_arg0 "promptCreationMode") in
      let parserMode =
        (Option.map ~f:CreationMode.of_xml) (Xml.child xml_arg0 "parserMode") in
      let overrideLambda =
        (Option.map ~f:LambdaArn.of_xml)
          (Xml.child xml_arg0 "overrideLambda") in
      let inferenceConfiguration =
        (Option.map ~f:InferenceConfiguration.of_xml)
          (Xml.child xml_arg0 "inferenceConfiguration") in
      let foundationModel =
        (Option.map ~f:ModelIdentifier.of_xml)
          (Xml.child xml_arg0 "foundationModel") in
      make ?type_ ?traceId ?text ?promptCreationMode ?parserMode
        ?overrideLambda ?inferenceConfiguration ?foundationModel ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" PromptType.of_json in
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let text = field_map json__ "text" PromptText.of_json in
      let promptCreationMode =
        field_map json__ "promptCreationMode" CreationMode.of_json in
      let parserMode = field_map json__ "parserMode" CreationMode.of_json in
      let overrideLambda =
        field_map json__ "overrideLambda" LambdaArn.of_json in
      let inferenceConfiguration =
        field_map json__ "inferenceConfiguration"
          InferenceConfiguration.of_json in
      let foundationModel =
        field_map json__ "foundationModel" ModelIdentifier.of_json in
      make ?type_ ?traceId ?text ?promptCreationMode ?parserMode
        ?overrideLambda ?inferenceConfiguration ?foundationModel ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input for the pre-processing step. The type matches the agent step. The text contains the prompt. The inferenceConfiguration, parserMode, and overrideLambda values are set in the PromptOverrideConfiguration object that was set when the agent was created or updated."]
module TraceKnowledgeBaseId =
  struct
    type nonrec t = string
    let context_ = "TraceKnowledgeBaseId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TraceKnowledgeBaseId" j
    let to_json = simple_to_json to_value
  end
module KnowledgeBaseLookupInputString =
  struct
    type nonrec t = string
    let context_ = "KnowledgeBaseLookupInputString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KnowledgeBaseLookupInputString" j
    let to_json = simple_to_json to_value
  end
module KnowledgeBaseLookupInput =
  struct
    type nonrec t =
      {
      knowledgeBaseId: TraceKnowledgeBaseId.t option
        [@ocaml.doc
          "The unique identifier of the knowledge base to look up."];
      text: KnowledgeBaseLookupInputString.t option
        [@ocaml.doc "The query made to the knowledge base."]}
    let make ?knowledgeBaseId =
      fun ?text -> fun () -> { knowledgeBaseId; text }
    let to_value x =
      structure_to_value
        [("knowledgeBaseId",
           (Option.map x.knowledgeBaseId ~f:TraceKnowledgeBaseId.to_value));
        ("text",
          (Option.map x.text ~f:KnowledgeBaseLookupInputString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        (Option.map ~f:KnowledgeBaseLookupInputString.of_xml)
          (Xml.child xml_arg0 "text") in
      let knowledgeBaseId =
        (Option.map ~f:TraceKnowledgeBaseId.of_xml)
          (Xml.child xml_arg0 "knowledgeBaseId") in
      make ?text ?knowledgeBaseId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text =
        field_map json__ "text" KnowledgeBaseLookupInputString.of_json in
      let knowledgeBaseId =
        field_map json__ "knowledgeBaseId" TraceKnowledgeBaseId.of_json in
      make ?text ?knowledgeBaseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the knowledge base to look up and the query to be made."]
module InvocationType =
  struct
    type nonrec t =
      | ACTION_GROUP 
      | KNOWLEDGE_BASE 
      | FINISH 
      | ACTION_GROUP_CODE_INTERPRETER 
      | AGENT_COLLABORATOR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTION_GROUP -> "ACTION_GROUP"
      | KNOWLEDGE_BASE -> "KNOWLEDGE_BASE"
      | FINISH -> "FINISH"
      | ACTION_GROUP_CODE_INTERPRETER -> "ACTION_GROUP_CODE_INTERPRETER"
      | AGENT_COLLABORATOR -> "AGENT_COLLABORATOR"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTION_GROUP" -> ACTION_GROUP
      | "KNOWLEDGE_BASE" -> KNOWLEDGE_BASE
      | "FINISH" -> FINISH
      | "ACTION_GROUP_CODE_INTERPRETER" -> ACTION_GROUP_CODE_INTERPRETER
      | "AGENT_COLLABORATOR" -> AGENT_COLLABORATOR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InvocationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InvocationType" j)
    let to_json = simple_to_json to_value
  end
module CodeInterpreterInvocationInput =
  struct
    type nonrec t =
      {
      code: String_.t option
        [@ocaml.doc "The code for the code interpreter to use."];
      files: Files.t option
        [@ocaml.doc "Files that are uploaded for code interpreter to use."]}
    let make ?code = fun ?files -> fun () -> { code; files }
    let to_value x =
      structure_to_value
        [("code", (Option.map x.code ~f:String_.to_value));
        ("files", (Option.map x.files ~f:Files.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let files = (Option.map ~f:Files.of_xml) (Xml.child xml_arg0 "files") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      make ?files ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let files = field_map json__ "files" Files.of_json in
      let code = field_map json__ "code" String_.of_json in
      make ?files ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the code interpreter being invoked."]
module InvocationInput =
  struct
    type nonrec t =
      {
      actionGroupInvocationInput: ActionGroupInvocationInput.t option
        [@ocaml.doc
          "Contains information about the action group to be invoked."];
      agentCollaboratorInvocationInput:
        AgentCollaboratorInvocationInput.t option
        [@ocaml.doc "The collaborator's invocation input."];
      codeInterpreterInvocationInput: CodeInterpreterInvocationInput.t option
        [@ocaml.doc
          "Contains information about the code interpreter to be invoked."];
      invocationType: InvocationType.t option
        [@ocaml.doc
          "Specifies whether the agent is invoking an action group or a knowledge base."];
      knowledgeBaseLookupInput: KnowledgeBaseLookupInput.t option
        [@ocaml.doc
          "Contains details about the knowledge base to look up and the query to be made."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace."]}
    let make ?actionGroupInvocationInput =
      fun ?agentCollaboratorInvocationInput ->
        fun ?codeInterpreterInvocationInput ->
          fun ?invocationType ->
            fun ?knowledgeBaseLookupInput ->
              fun ?traceId ->
                fun () ->
                  {
                    actionGroupInvocationInput;
                    agentCollaboratorInvocationInput;
                    codeInterpreterInvocationInput;
                    invocationType;
                    knowledgeBaseLookupInput;
                    traceId
                  }
    let to_value x =
      structure_to_value
        [("actionGroupInvocationInput",
           (Option.map x.actionGroupInvocationInput
              ~f:ActionGroupInvocationInput.to_value));
        ("agentCollaboratorInvocationInput",
          (Option.map x.agentCollaboratorInvocationInput
             ~f:AgentCollaboratorInvocationInput.to_value));
        ("codeInterpreterInvocationInput",
          (Option.map x.codeInterpreterInvocationInput
             ~f:CodeInterpreterInvocationInput.to_value));
        ("invocationType",
          (Option.map x.invocationType ~f:InvocationType.to_value));
        ("knowledgeBaseLookupInput",
          (Option.map x.knowledgeBaseLookupInput
             ~f:KnowledgeBaseLookupInput.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let knowledgeBaseLookupInput =
        (Option.map ~f:KnowledgeBaseLookupInput.of_xml)
          (Xml.child xml_arg0 "knowledgeBaseLookupInput") in
      let invocationType =
        (Option.map ~f:InvocationType.of_xml)
          (Xml.child xml_arg0 "invocationType") in
      let codeInterpreterInvocationInput =
        (Option.map ~f:CodeInterpreterInvocationInput.of_xml)
          (Xml.child xml_arg0 "codeInterpreterInvocationInput") in
      let agentCollaboratorInvocationInput =
        (Option.map ~f:AgentCollaboratorInvocationInput.of_xml)
          (Xml.child xml_arg0 "agentCollaboratorInvocationInput") in
      let actionGroupInvocationInput =
        (Option.map ~f:ActionGroupInvocationInput.of_xml)
          (Xml.child xml_arg0 "actionGroupInvocationInput") in
      make ?traceId ?knowledgeBaseLookupInput ?invocationType
        ?codeInterpreterInvocationInput ?agentCollaboratorInvocationInput
        ?actionGroupInvocationInput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let knowledgeBaseLookupInput =
        field_map json__ "knowledgeBaseLookupInput"
          KnowledgeBaseLookupInput.of_json in
      let invocationType =
        field_map json__ "invocationType" InvocationType.of_json in
      let codeInterpreterInvocationInput =
        field_map json__ "codeInterpreterInvocationInput"
          CodeInterpreterInvocationInput.of_json in
      let agentCollaboratorInvocationInput =
        field_map json__ "agentCollaboratorInvocationInput"
          AgentCollaboratorInvocationInput.of_json in
      let actionGroupInvocationInput =
        field_map json__ "actionGroupInvocationInput"
          ActionGroupInvocationInput.of_json in
      make ?traceId ?knowledgeBaseLookupInput ?invocationType
        ?codeInterpreterInvocationInput ?agentCollaboratorInvocationInput
        ?actionGroupInvocationInput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information pertaining to the action group or knowledge base that is being invoked."]
module RoutingClassifierTrace =
  struct
    type nonrec t =
      {
      invocationInput: InvocationInput.t option
        [@ocaml.doc "The classifier's invocation input."];
      modelInvocationInput: ModelInvocationInput.t option
        [@ocaml.doc "The classifier's model invocation input."];
      modelInvocationOutput: RoutingClassifierModelInvocationOutput.t option
        [@ocaml.doc "The classifier's model invocation output."];
      observation: Observation.t option
        [@ocaml.doc "The classifier's observation."]}
    let make ?invocationInput =
      fun ?modelInvocationInput ->
        fun ?modelInvocationOutput ->
          fun ?observation ->
            fun () ->
              {
                invocationInput;
                modelInvocationInput;
                modelInvocationOutput;
                observation
              }
    let to_value x =
      structure_to_value
        [("invocationInput",
           (Option.map x.invocationInput ~f:InvocationInput.to_value));
        ("modelInvocationInput",
          (Option.map x.modelInvocationInput ~f:ModelInvocationInput.to_value));
        ("modelInvocationOutput",
          (Option.map x.modelInvocationOutput
             ~f:RoutingClassifierModelInvocationOutput.to_value));
        ("observation", (Option.map x.observation ~f:Observation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let observation =
        (Option.map ~f:Observation.of_xml) (Xml.child xml_arg0 "observation") in
      let modelInvocationOutput =
        (Option.map ~f:RoutingClassifierModelInvocationOutput.of_xml)
          (Xml.child xml_arg0 "modelInvocationOutput") in
      let modelInvocationInput =
        (Option.map ~f:ModelInvocationInput.of_xml)
          (Xml.child xml_arg0 "modelInvocationInput") in
      let invocationInput =
        (Option.map ~f:InvocationInput.of_xml)
          (Xml.child xml_arg0 "invocationInput") in
      make ?observation ?modelInvocationOutput ?modelInvocationInput
        ?invocationInput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let observation = field_map json__ "observation" Observation.of_json in
      let modelInvocationOutput =
        field_map json__ "modelInvocationOutput"
          RoutingClassifierModelInvocationOutput.of_json in
      let modelInvocationInput =
        field_map json__ "modelInvocationInput" ModelInvocationInput.of_json in
      let invocationInput =
        field_map json__ "invocationInput" InvocationInput.of_json in
      make ?observation ?modelInvocationOutput ?modelInvocationInput
        ?invocationInput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A trace for a routing classifier."]
module ReasoningTextBlock =
  struct
    type nonrec t =
      {
      signature: String_.t option
        [@ocaml.doc
          "A hash of all the messages in the conversation to ensure that the content in the reasoning text block isn't tampered with. You must submit the signature in subsequent Converse requests, in addition to the previous messages. If the previous messages are tampered with, the response throws an error."];
      text: String_.t option
        [@ocaml.doc
          "Text describing the reasoning that the model used to return the content in the content block."]}
    let make ?signature = fun ?text -> fun () -> { signature; text }
    let to_value x =
      structure_to_value
        [("signature", (Option.map x.signature ~f:String_.to_value));
        ("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      let signature =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "signature") in
      make ?text ?signature ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in
      let signature = field_map json__ "signature" String_.of_json in
      make ?text ?signature ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the reasoning that the model used to return the content in the content block."]
module Blob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module ReasoningContentBlock =
  struct
    type nonrec t =
      {
      reasoningText: ReasoningTextBlock.t option
        [@ocaml.doc
          "Contains information about the reasoning that the model used to return the content in the content block."];
      redactedContent: Blob.t option
        [@ocaml.doc
          "The content in the reasoning that was encrypted by the model provider for trust and safety reasons."]}
    let make ?reasoningText =
      fun ?redactedContent -> fun () -> { reasoningText; redactedContent }
    let to_value x =
      structure_to_value
        [("reasoningText",
           (Option.map x.reasoningText ~f:ReasoningTextBlock.to_value));
        ("redactedContent", (Option.map x.redactedContent ~f:Blob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let redactedContent =
        (Option.map ~f:Blob.of_xml) (Xml.child xml_arg0 "redactedContent") in
      let reasoningText =
        (Option.map ~f:ReasoningTextBlock.of_xml)
          (Xml.child xml_arg0 "reasoningText") in
      make ?redactedContent ?reasoningText ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let redactedContent = field_map json__ "redactedContent" Blob.of_json in
      let reasoningText =
        field_map json__ "reasoningText" ReasoningTextBlock.of_json in
      make ?redactedContent ?reasoningText ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains content regarding the reasoning that the foundation model made with respect to the content in the content block. Reasoning refers to a Chain of Thought (CoT) that the model generates to enhance the accuracy of its final response."]
module RationaleString =
  struct
    type nonrec t = string
    let context_ = "RationaleString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RationaleString" j
    let to_json = simple_to_json to_value
  end
module PreProcessingParsedResponse =
  struct
    type nonrec t =
      {
      isValid: Boolean.t option
        [@ocaml.doc
          "Whether the user input is valid or not. If false, the agent doesn't proceed to orchestration."];
      rationale: RationaleString.t option
        [@ocaml.doc
          "The text returned by the parsing of the pre-processing step, explaining the steps that the agent plans to take in orchestration, if the user input is valid."]}
    let make ?isValid = fun ?rationale -> fun () -> { isValid; rationale }
    let to_value x =
      structure_to_value
        [("isValid", (Option.map x.isValid ~f:Boolean.to_value));
        ("rationale", (Option.map x.rationale ~f:RationaleString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rationale =
        (Option.map ~f:RationaleString.of_xml)
          (Xml.child xml_arg0 "rationale") in
      let isValid =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "isValid") in
      make ?rationale ?isValid ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rationale = field_map json__ "rationale" RationaleString.of_json in
      let isValid = field_map json__ "isValid" Boolean.of_json in
      make ?rationale ?isValid ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the response from the Lambda parsing of the output from the pre-processing step."]
module PreProcessingModelInvocationOutput =
  struct
    type nonrec t =
      {
      metadata: Metadata.t option
        [@ocaml.doc
          "Contains information about the foundation model output from the pre-processing step."];
      parsedResponse: PreProcessingParsedResponse.t option
        [@ocaml.doc
          "Details about the response from the Lambda parsing of the output of the pre-processing step."];
      rawResponse: RawResponse.t option
        [@ocaml.doc
          "Details of the raw response from the foundation model output."];
      reasoningContent: ReasoningContentBlock.t option
        [@ocaml.doc
          "Contains content about the reasoning that the model made during the pre-processing step."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace."]}
    let make ?metadata =
      fun ?parsedResponse ->
        fun ?rawResponse ->
          fun ?reasoningContent ->
            fun ?traceId ->
              fun () ->
                {
                  metadata;
                  parsedResponse;
                  rawResponse;
                  reasoningContent;
                  traceId
                }
    let to_value x =
      structure_to_value
        [("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("parsedResponse",
          (Option.map x.parsedResponse
             ~f:PreProcessingParsedResponse.to_value));
        ("rawResponse", (Option.map x.rawResponse ~f:RawResponse.to_value));
        ("reasoningContent",
          (Option.map x.reasoningContent ~f:ReasoningContentBlock.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let reasoningContent =
        (Option.map ~f:ReasoningContentBlock.of_xml)
          (Xml.child xml_arg0 "reasoningContent") in
      let rawResponse =
        (Option.map ~f:RawResponse.of_xml) (Xml.child xml_arg0 "rawResponse") in
      let parsedResponse =
        (Option.map ~f:PreProcessingParsedResponse.of_xml)
          (Xml.child xml_arg0 "parsedResponse") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      make ?traceId ?reasoningContent ?rawResponse ?parsedResponse ?metadata
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let reasoningContent =
        field_map json__ "reasoningContent" ReasoningContentBlock.of_json in
      let rawResponse = field_map json__ "rawResponse" RawResponse.of_json in
      let parsedResponse =
        field_map json__ "parsedResponse" PreProcessingParsedResponse.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      make ?traceId ?reasoningContent ?rawResponse ?parsedResponse ?metadata
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The foundation model output from the pre-processing step."]
module PreProcessingTrace =
  struct
    type nonrec t =
      {
      modelInvocationInput: ModelInvocationInput.t option
        [@ocaml.doc
          "The input for the pre-processing step. The type is PRE_PROCESSING. The text contains the prompt. The inferenceConfiguration, parserMode, and overrideLambda values are set in the PromptOverrideConfiguration object that was set when the agent was created or updated."];
      modelInvocationOutput: PreProcessingModelInvocationOutput.t option
        [@ocaml.doc
          "The foundation model output from the pre-processing step."]}
    let make ?modelInvocationInput =
      fun ?modelInvocationOutput ->
        fun () -> { modelInvocationInput; modelInvocationOutput }
    let to_value x =
      structure_to_value
        [("modelInvocationInput",
           (Option.map x.modelInvocationInput
              ~f:ModelInvocationInput.to_value));
        ("modelInvocationOutput",
          (Option.map x.modelInvocationOutput
             ~f:PreProcessingModelInvocationOutput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modelInvocationOutput =
        (Option.map ~f:PreProcessingModelInvocationOutput.of_xml)
          (Xml.child xml_arg0 "modelInvocationOutput") in
      let modelInvocationInput =
        (Option.map ~f:ModelInvocationInput.of_xml)
          (Xml.child xml_arg0 "modelInvocationInput") in
      make ?modelInvocationOutput ?modelInvocationInput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let modelInvocationOutput =
        field_map json__ "modelInvocationOutput"
          PreProcessingModelInvocationOutput.of_json in
      let modelInvocationInput =
        field_map json__ "modelInvocationInput" ModelInvocationInput.of_json in
      make ?modelInvocationOutput ?modelInvocationInput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the pre-processing step, in which the agent contextualizes and categorizes user inputs."]
module OutputString =
  struct
    type nonrec t = string
    let context_ = "OutputString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OutputString" j
    let to_json = simple_to_json to_value
  end
module PostProcessingParsedResponse =
  struct
    type nonrec t =
      {
      text: OutputString.t option
        [@ocaml.doc "The text returned by the parser."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:OutputString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        (Option.map ~f:OutputString.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" OutputString.of_json in
      make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the response from the Lambda parsing of the output from the post-processing step."]
module PostProcessingModelInvocationOutput =
  struct
    type nonrec t =
      {
      metadata: Metadata.t option
        [@ocaml.doc
          "Contains information about the foundation model output from the post-processing step."];
      parsedResponse: PostProcessingParsedResponse.t option
        [@ocaml.doc
          "Details about the response from the Lambda parsing of the output of the post-processing step."];
      rawResponse: RawResponse.t option
        [@ocaml.doc
          "Details of the raw response from the foundation model output."];
      reasoningContent: ReasoningContentBlock.t option
        [@ocaml.doc
          "Contains content about the reasoning that the model made during the post-processing step."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace."]}
    let make ?metadata =
      fun ?parsedResponse ->
        fun ?rawResponse ->
          fun ?reasoningContent ->
            fun ?traceId ->
              fun () ->
                {
                  metadata;
                  parsedResponse;
                  rawResponse;
                  reasoningContent;
                  traceId
                }
    let to_value x =
      structure_to_value
        [("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("parsedResponse",
          (Option.map x.parsedResponse
             ~f:PostProcessingParsedResponse.to_value));
        ("rawResponse", (Option.map x.rawResponse ~f:RawResponse.to_value));
        ("reasoningContent",
          (Option.map x.reasoningContent ~f:ReasoningContentBlock.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let reasoningContent =
        (Option.map ~f:ReasoningContentBlock.of_xml)
          (Xml.child xml_arg0 "reasoningContent") in
      let rawResponse =
        (Option.map ~f:RawResponse.of_xml) (Xml.child xml_arg0 "rawResponse") in
      let parsedResponse =
        (Option.map ~f:PostProcessingParsedResponse.of_xml)
          (Xml.child xml_arg0 "parsedResponse") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      make ?traceId ?reasoningContent ?rawResponse ?parsedResponse ?metadata
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let reasoningContent =
        field_map json__ "reasoningContent" ReasoningContentBlock.of_json in
      let rawResponse = field_map json__ "rawResponse" RawResponse.of_json in
      let parsedResponse =
        field_map json__ "parsedResponse"
          PostProcessingParsedResponse.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      make ?traceId ?reasoningContent ?rawResponse ?parsedResponse ?metadata
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The foundation model output from the post-processing step."]
module PostProcessingTrace =
  struct
    type nonrec t =
      {
      modelInvocationInput: ModelInvocationInput.t option
        [@ocaml.doc
          "The input for the post-processing step. The type is POST_PROCESSING. The text contains the prompt. The inferenceConfiguration, parserMode, and overrideLambda values are set in the PromptOverrideConfiguration object that was set when the agent was created or updated."];
      modelInvocationOutput: PostProcessingModelInvocationOutput.t option
        [@ocaml.doc
          "The foundation model output from the post-processing step."]}
    let make ?modelInvocationInput =
      fun ?modelInvocationOutput ->
        fun () -> { modelInvocationInput; modelInvocationOutput }
    let to_value x =
      structure_to_value
        [("modelInvocationInput",
           (Option.map x.modelInvocationInput
              ~f:ModelInvocationInput.to_value));
        ("modelInvocationOutput",
          (Option.map x.modelInvocationOutput
             ~f:PostProcessingModelInvocationOutput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modelInvocationOutput =
        (Option.map ~f:PostProcessingModelInvocationOutput.of_xml)
          (Xml.child xml_arg0 "modelInvocationOutput") in
      let modelInvocationInput =
        (Option.map ~f:ModelInvocationInput.of_xml)
          (Xml.child xml_arg0 "modelInvocationInput") in
      make ?modelInvocationOutput ?modelInvocationInput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let modelInvocationOutput =
        field_map json__ "modelInvocationOutput"
          PostProcessingModelInvocationOutput.of_json in
      let modelInvocationInput =
        field_map json__ "modelInvocationInput" ModelInvocationInput.of_json in
      make ?modelInvocationOutput ?modelInvocationInput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the post-processing step, in which the agent shapes the response."]
module Rationale =
  struct
    type nonrec t =
      {
      text: RationaleString.t option
        [@ocaml.doc
          "The reasoning or thought process of the agent, based on the input."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace step."]}
    let make ?text = fun ?traceId -> fun () -> { text; traceId }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:RationaleString.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let text =
        (Option.map ~f:RationaleString.of_xml) (Xml.child xml_arg0 "text") in
      make ?traceId ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let text = field_map json__ "text" RationaleString.of_json in
      make ?traceId ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the reasoning, based on the input, that the agent uses to justify carrying out an action group or getting information from a knowledge base."]
module OrchestrationModelInvocationOutput =
  struct
    type nonrec t =
      {
      metadata: Metadata.t option
        [@ocaml.doc
          "Contains information about the foundation model output from the orchestration step."];
      rawResponse: RawResponse.t option
        [@ocaml.doc
          "Contains details of the raw response from the foundation model output."];
      reasoningContent: ReasoningContentBlock.t option
        [@ocaml.doc
          "Contains content about the reasoning that the model made during the orchestration step."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace."]}
    let make ?metadata =
      fun ?rawResponse ->
        fun ?reasoningContent ->
          fun ?traceId ->
            fun () -> { metadata; rawResponse; reasoningContent; traceId }
    let to_value x =
      structure_to_value
        [("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("rawResponse", (Option.map x.rawResponse ~f:RawResponse.to_value));
        ("reasoningContent",
          (Option.map x.reasoningContent ~f:ReasoningContentBlock.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let reasoningContent =
        (Option.map ~f:ReasoningContentBlock.of_xml)
          (Xml.child xml_arg0 "reasoningContent") in
      let rawResponse =
        (Option.map ~f:RawResponse.of_xml) (Xml.child xml_arg0 "rawResponse") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      make ?traceId ?reasoningContent ?rawResponse ?metadata ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let reasoningContent =
        field_map json__ "reasoningContent" ReasoningContentBlock.of_json in
      let rawResponse = field_map json__ "rawResponse" RawResponse.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      make ?traceId ?reasoningContent ?rawResponse ?metadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The foundation model output from the orchestration step."]
module OrchestrationTrace =
  struct
    type nonrec t =
      {
      invocationInput: InvocationInput.t option
        [@ocaml.doc
          "Contains information pertaining to the action group or knowledge base that is being invoked."];
      modelInvocationInput: ModelInvocationInput.t option
        [@ocaml.doc
          "The input for the orchestration step. The type is ORCHESTRATION. The text contains the prompt. The inferenceConfiguration, parserMode, and overrideLambda values are set in the PromptOverrideConfiguration object that was set when the agent was created or updated."];
      modelInvocationOutput: OrchestrationModelInvocationOutput.t option
        [@ocaml.doc
          "Contains information pertaining to the output from the foundation model that is being invoked."];
      observation: Observation.t option
        [@ocaml.doc
          "Details about the observation (the output of the action group Lambda or knowledge base) made by the agent."];
      rationale: Rationale.t option
        [@ocaml.doc
          "Details about the reasoning, based on the input, that the agent uses to justify carrying out an action group or getting information from a knowledge base."]}
    let make ?invocationInput =
      fun ?modelInvocationInput ->
        fun ?modelInvocationOutput ->
          fun ?observation ->
            fun ?rationale ->
              fun () ->
                {
                  invocationInput;
                  modelInvocationInput;
                  modelInvocationOutput;
                  observation;
                  rationale
                }
    let to_value x =
      structure_to_value
        [("invocationInput",
           (Option.map x.invocationInput ~f:InvocationInput.to_value));
        ("modelInvocationInput",
          (Option.map x.modelInvocationInput ~f:ModelInvocationInput.to_value));
        ("modelInvocationOutput",
          (Option.map x.modelInvocationOutput
             ~f:OrchestrationModelInvocationOutput.to_value));
        ("observation", (Option.map x.observation ~f:Observation.to_value));
        ("rationale", (Option.map x.rationale ~f:Rationale.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rationale =
        (Option.map ~f:Rationale.of_xml) (Xml.child xml_arg0 "rationale") in
      let observation =
        (Option.map ~f:Observation.of_xml) (Xml.child xml_arg0 "observation") in
      let modelInvocationOutput =
        (Option.map ~f:OrchestrationModelInvocationOutput.of_xml)
          (Xml.child xml_arg0 "modelInvocationOutput") in
      let modelInvocationInput =
        (Option.map ~f:ModelInvocationInput.of_xml)
          (Xml.child xml_arg0 "modelInvocationInput") in
      let invocationInput =
        (Option.map ~f:InvocationInput.of_xml)
          (Xml.child xml_arg0 "invocationInput") in
      make ?rationale ?observation ?modelInvocationOutput
        ?modelInvocationInput ?invocationInput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rationale = field_map json__ "rationale" Rationale.of_json in
      let observation = field_map json__ "observation" Observation.of_json in
      let modelInvocationOutput =
        field_map json__ "modelInvocationOutput"
          OrchestrationModelInvocationOutput.of_json in
      let modelInvocationInput =
        field_map json__ "modelInvocationInput" ModelInvocationInput.of_json in
      let invocationInput =
        field_map json__ "invocationInput" InvocationInput.of_json in
      make ?rationale ?observation ?modelInvocationOutput
        ?modelInvocationInput ?invocationInput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the orchestration step, in which the agent determines the order in which actions are executed and which knowledge bases are retrieved."]
module GuardrailWordPolicyAction =
  struct
    type nonrec t =
      | BLOCKED 
      | Non_static_id of string 
    let make i = i
    let to_string = function | BLOCKED -> "BLOCKED" | Non_static_id s -> s
    let of_string = function | "BLOCKED" -> BLOCKED | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailWordPolicyAction" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailWordPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailManagedWordType =
  struct
    type nonrec t =
      | PROFANITY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | PROFANITY -> "PROFANITY" | Non_static_id s -> s
    let of_string =
      function | "PROFANITY" -> PROFANITY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailManagedWordType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailManagedWordType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailManagedWord =
  struct
    type nonrec t =
      {
      action: GuardrailWordPolicyAction.t option
        [@ocaml.doc
          "The action details for the managed word filter in the Guardrail."];
      match_: String_.t option
        [@ocaml.doc
          "The match details for the managed word filter in the Guardrail."];
      type_: GuardrailManagedWordType.t option
        [@ocaml.doc
          "The type details for the managed word filter in the Guardrail."]}
    let make ?action =
      fun ?match_ -> fun ?type_ -> fun () -> { action; match_; type_ }
    let to_value x =
      structure_to_value
        [("action",
           (Option.map x.action ~f:GuardrailWordPolicyAction.to_value));
        ("match", (Option.map x.match_ ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:GuardrailManagedWordType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:GuardrailManagedWordType.of_xml)
          (Xml.child xml_arg0 "type") in
      let match_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "match") in
      let action =
        (Option.map ~f:GuardrailWordPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      make ?type_ ?match_ ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" GuardrailManagedWordType.of_json in
      let match_ = field_map json__ "match" String_.of_json in
      let action =
        field_map json__ "action" GuardrailWordPolicyAction.of_json in
      make ?type_ ?match_ ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The managed word details for the filter in the Guardrail."]
module GuardrailManagedWordList =
  struct
    type nonrec t = GuardrailManagedWord.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailManagedWord.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailManagedWord.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailManagedWordList"
        ~of_json:GuardrailManagedWord.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailCustomWord =
  struct
    type nonrec t =
      {
      action: GuardrailWordPolicyAction.t option
        [@ocaml.doc
          "The action details for the custom word filter in the Guardrail."];
      match_: String_.t option
        [@ocaml.doc
          "The match details for the custom word filter in the Guardrail."]}
    let make ?action = fun ?match_ -> fun () -> { action; match_ }
    let to_value x =
      structure_to_value
        [("action",
           (Option.map x.action ~f:GuardrailWordPolicyAction.to_value));
        ("match", (Option.map x.match_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let match_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "match") in
      let action =
        (Option.map ~f:GuardrailWordPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      make ?match_ ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let match_ = field_map json__ "match" String_.of_json in
      let action =
        field_map json__ "action" GuardrailWordPolicyAction.of_json in
      make ?match_ ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The custom word details for the filter in the Guardrail."]
module GuardrailCustomWordList =
  struct
    type nonrec t = GuardrailCustomWord.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailCustomWord.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailCustomWord.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailCustomWordList"
        ~of_json:GuardrailCustomWord.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailWordPolicyAssessment =
  struct
    type nonrec t =
      {
      customWords: GuardrailCustomWordList.t option
        [@ocaml.doc
          "The custom word details for words defined in the Guardrail filter."];
      managedWordLists: GuardrailManagedWordList.t option
        [@ocaml.doc
          "The managed word lists for words defined in the Guardrail filter."]}
    let make ?customWords =
      fun ?managedWordLists -> fun () -> { customWords; managedWordLists }
    let to_value x =
      structure_to_value
        [("customWords",
           (Option.map x.customWords ~f:GuardrailCustomWordList.to_value));
        ("managedWordLists",
          (Option.map x.managedWordLists ~f:GuardrailManagedWordList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedWordLists =
        (Option.map ~f:GuardrailManagedWordList.of_xml)
          (Xml.child xml_arg0 "managedWordLists") in
      let customWords =
        (Option.map ~f:GuardrailCustomWordList.of_xml)
          (Xml.child xml_arg0 "customWords") in
      make ?managedWordLists ?customWords ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let managedWordLists =
        field_map json__ "managedWordLists" GuardrailManagedWordList.of_json in
      let customWords =
        field_map json__ "customWords" GuardrailCustomWordList.of_json in
      make ?managedWordLists ?customWords ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The assessment details for words defined in the Guardrail filter."]
module GuardrailTopicType =
  struct
    type nonrec t =
      | DENY 
      | Non_static_id of string 
    let make i = i
    let to_string = function | DENY -> "DENY" | Non_static_id s -> s
    let of_string = function | "DENY" -> DENY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailTopicType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailTopicType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailTopicPolicyAction =
  struct
    type nonrec t =
      | BLOCKED 
      | Non_static_id of string 
    let make i = i
    let to_string = function | BLOCKED -> "BLOCKED" | Non_static_id s -> s
    let of_string = function | "BLOCKED" -> BLOCKED | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailTopicPolicyAction"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailTopicPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailTopic =
  struct
    type nonrec t =
      {
      action: GuardrailTopicPolicyAction.t option
        [@ocaml.doc
          "The action details on a specific topic in the Guardrail."];
      name: String_.t option
        [@ocaml.doc "The name details on a specific topic in the Guardrail."];
      type_: GuardrailTopicType.t option
        [@ocaml.doc "The type details on a specific topic in the Guardrail."]}
    let make ?action =
      fun ?name -> fun ?type_ -> fun () -> { action; name; type_ }
    let to_value x =
      structure_to_value
        [("action",
           (Option.map x.action ~f:GuardrailTopicPolicyAction.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:GuardrailTopicType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:GuardrailTopicType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let action =
        (Option.map ~f:GuardrailTopicPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      make ?type_ ?name ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" GuardrailTopicType.of_json in
      let name = field_map json__ "name" String_.of_json in
      let action =
        field_map json__ "action" GuardrailTopicPolicyAction.of_json in
      make ?type_ ?name ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details for a specific topic defined in the Guardrail."]
module GuardrailTopicList =
  struct
    type nonrec t = GuardrailTopic.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailTopic.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailTopic.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailTopicList" ~of_json:GuardrailTopic.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module GuardrailTopicPolicyAssessment =
  struct
    type nonrec t =
      {
      topics: GuardrailTopicList.t option
        [@ocaml.doc
          "The topic details of the policy assessment used in the Guardrail."]}
    let make ?topics = fun () -> { topics }
    let to_value x =
      structure_to_value
        [("topics", (Option.map x.topics ~f:GuardrailTopicList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topics =
        (Option.map ~f:GuardrailTopicList.of_xml)
          (Xml.child xml_arg0 "topics") in
      make ?topics ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let topics = field_map json__ "topics" GuardrailTopicList.of_json in
      make ?topics ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of the policy assessment used in the Guardrail."]
module GuardrailSensitiveInformationPolicyAction =
  struct
    type nonrec t =
      | BLOCKED 
      | ANONYMIZED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BLOCKED -> "BLOCKED"
      | ANONYMIZED -> "ANONYMIZED"
      | Non_static_id s -> s
    let of_string =
      function
      | "BLOCKED" -> BLOCKED
      | "ANONYMIZED" -> ANONYMIZED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration GuardrailSensitiveInformationPolicyAction"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"GuardrailSensitiveInformationPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailRegexFilter =
  struct
    type nonrec t =
      {
      action: GuardrailSensitiveInformationPolicyAction.t option
        [@ocaml.doc
          "The action details for the regex filter used in the Guardrail."];
      match_: String_.t option
        [@ocaml.doc
          "The match details for the regex filter used in the Guardrail."];
      name: String_.t option
        [@ocaml.doc
          "The name details for the regex filter used in the Guardrail."];
      regex: String_.t option
        [@ocaml.doc
          "The regex details for the regex filter used in the Guardrail."]}
    let make ?action =
      fun ?match_ ->
        fun ?name -> fun ?regex -> fun () -> { action; match_; name; regex }
    let to_value x =
      structure_to_value
        [("action",
           (Option.map x.action
              ~f:GuardrailSensitiveInformationPolicyAction.to_value));
        ("match", (Option.map x.match_ ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("regex", (Option.map x.regex ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regex = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "regex") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let match_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "match") in
      let action =
        (Option.map ~f:GuardrailSensitiveInformationPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      make ?regex ?name ?match_ ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let regex = field_map json__ "regex" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let match_ = field_map json__ "match" String_.of_json in
      let action =
        field_map json__ "action"
          GuardrailSensitiveInformationPolicyAction.of_json in
      make ?regex ?name ?match_ ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details for the regex filter used in the Guardrail."]
module GuardrailRegexFilterList =
  struct
    type nonrec t = GuardrailRegexFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailRegexFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailRegexFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailRegexFilterList"
        ~of_json:GuardrailRegexFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailPiiEntityType =
  struct
    type nonrec t =
      | ADDRESS 
      | AGE 
      | AWS_ACCESS_KEY 
      | AWS_SECRET_KEY 
      | CA_HEALTH_NUMBER 
      | CA_SOCIAL_INSURANCE_NUMBER 
      | CREDIT_DEBIT_CARD_CVV 
      | CREDIT_DEBIT_CARD_EXPIRY 
      | CREDIT_DEBIT_CARD_NUMBER 
      | DRIVER_ID 
      | EMAIL 
      | INTERNATIONAL_BANK_ACCOUNT_NUMBER 
      | IP_ADDRESS 
      | LICENSE_PLATE 
      | MAC_ADDRESS 
      | NAME 
      | PASSWORD 
      | PHONE 
      | PIN 
      | SWIFT_CODE 
      | UK_NATIONAL_HEALTH_SERVICE_NUMBER 
      | UK_NATIONAL_INSURANCE_NUMBER 
      | UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER 
      | URL 
      | USERNAME 
      | US_BANK_ACCOUNT_NUMBER 
      | US_BANK_ROUTING_NUMBER 
      | US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER 
      | US_PASSPORT_NUMBER 
      | US_SOCIAL_SECURITY_NUMBER 
      | VEHICLE_IDENTIFICATION_NUMBER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ADDRESS -> "ADDRESS"
      | AGE -> "AGE"
      | AWS_ACCESS_KEY -> "AWS_ACCESS_KEY"
      | AWS_SECRET_KEY -> "AWS_SECRET_KEY"
      | CA_HEALTH_NUMBER -> "CA_HEALTH_NUMBER"
      | CA_SOCIAL_INSURANCE_NUMBER -> "CA_SOCIAL_INSURANCE_NUMBER"
      | CREDIT_DEBIT_CARD_CVV -> "CREDIT_DEBIT_CARD_CVV"
      | CREDIT_DEBIT_CARD_EXPIRY -> "CREDIT_DEBIT_CARD_EXPIRY"
      | CREDIT_DEBIT_CARD_NUMBER -> "CREDIT_DEBIT_CARD_NUMBER"
      | DRIVER_ID -> "DRIVER_ID"
      | EMAIL -> "EMAIL"
      | INTERNATIONAL_BANK_ACCOUNT_NUMBER ->
          "INTERNATIONAL_BANK_ACCOUNT_NUMBER"
      | IP_ADDRESS -> "IP_ADDRESS"
      | LICENSE_PLATE -> "LICENSE_PLATE"
      | MAC_ADDRESS -> "MAC_ADDRESS"
      | NAME -> "NAME"
      | PASSWORD -> "PASSWORD"
      | PHONE -> "PHONE"
      | PIN -> "PIN"
      | SWIFT_CODE -> "SWIFT_CODE"
      | UK_NATIONAL_HEALTH_SERVICE_NUMBER ->
          "UK_NATIONAL_HEALTH_SERVICE_NUMBER"
      | UK_NATIONAL_INSURANCE_NUMBER -> "UK_NATIONAL_INSURANCE_NUMBER"
      | UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER ->
          "UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER"
      | URL -> "URL"
      | USERNAME -> "USERNAME"
      | US_BANK_ACCOUNT_NUMBER -> "US_BANK_ACCOUNT_NUMBER"
      | US_BANK_ROUTING_NUMBER -> "US_BANK_ROUTING_NUMBER"
      | US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER ->
          "US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER"
      | US_PASSPORT_NUMBER -> "US_PASSPORT_NUMBER"
      | US_SOCIAL_SECURITY_NUMBER -> "US_SOCIAL_SECURITY_NUMBER"
      | VEHICLE_IDENTIFICATION_NUMBER -> "VEHICLE_IDENTIFICATION_NUMBER"
      | Non_static_id s -> s
    let of_string =
      function
      | "ADDRESS" -> ADDRESS
      | "AGE" -> AGE
      | "AWS_ACCESS_KEY" -> AWS_ACCESS_KEY
      | "AWS_SECRET_KEY" -> AWS_SECRET_KEY
      | "CA_HEALTH_NUMBER" -> CA_HEALTH_NUMBER
      | "CA_SOCIAL_INSURANCE_NUMBER" -> CA_SOCIAL_INSURANCE_NUMBER
      | "CREDIT_DEBIT_CARD_CVV" -> CREDIT_DEBIT_CARD_CVV
      | "CREDIT_DEBIT_CARD_EXPIRY" -> CREDIT_DEBIT_CARD_EXPIRY
      | "CREDIT_DEBIT_CARD_NUMBER" -> CREDIT_DEBIT_CARD_NUMBER
      | "DRIVER_ID" -> DRIVER_ID
      | "EMAIL" -> EMAIL
      | "INTERNATIONAL_BANK_ACCOUNT_NUMBER" ->
          INTERNATIONAL_BANK_ACCOUNT_NUMBER
      | "IP_ADDRESS" -> IP_ADDRESS
      | "LICENSE_PLATE" -> LICENSE_PLATE
      | "MAC_ADDRESS" -> MAC_ADDRESS
      | "NAME" -> NAME
      | "PASSWORD" -> PASSWORD
      | "PHONE" -> PHONE
      | "PIN" -> PIN
      | "SWIFT_CODE" -> SWIFT_CODE
      | "UK_NATIONAL_HEALTH_SERVICE_NUMBER" ->
          UK_NATIONAL_HEALTH_SERVICE_NUMBER
      | "UK_NATIONAL_INSURANCE_NUMBER" -> UK_NATIONAL_INSURANCE_NUMBER
      | "UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER" ->
          UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER
      | "URL" -> URL
      | "USERNAME" -> USERNAME
      | "US_BANK_ACCOUNT_NUMBER" -> US_BANK_ACCOUNT_NUMBER
      | "US_BANK_ROUTING_NUMBER" -> US_BANK_ROUTING_NUMBER
      | "US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER" ->
          US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER
      | "US_PASSPORT_NUMBER" -> US_PASSPORT_NUMBER
      | "US_SOCIAL_SECURITY_NUMBER" -> US_SOCIAL_SECURITY_NUMBER
      | "VEHICLE_IDENTIFICATION_NUMBER" -> VEHICLE_IDENTIFICATION_NUMBER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailPiiEntityType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailPiiEntityType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailPiiEntityFilter =
  struct
    type nonrec t =
      {
      action: GuardrailSensitiveInformationPolicyAction.t option
        [@ocaml.doc
          "The action of the Guardrail filter to identify and remove PII."];
      match_: String_.t option
        [@ocaml.doc
          "The match to settings in the Guardrail filter to identify and remove PII."];
      type_: GuardrailPiiEntityType.t option
        [@ocaml.doc
          "The type of PII the Guardrail filter has identified and removed."]}
    let make ?action =
      fun ?match_ -> fun ?type_ -> fun () -> { action; match_; type_ }
    let to_value x =
      structure_to_value
        [("action",
           (Option.map x.action
              ~f:GuardrailSensitiveInformationPolicyAction.to_value));
        ("match", (Option.map x.match_ ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:GuardrailPiiEntityType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:GuardrailPiiEntityType.of_xml)
          (Xml.child xml_arg0 "type") in
      let match_ =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "match") in
      let action =
        (Option.map ~f:GuardrailSensitiveInformationPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      make ?type_ ?match_ ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" GuardrailPiiEntityType.of_json in
      let match_ = field_map json__ "match" String_.of_json in
      let action =
        field_map json__ "action"
          GuardrailSensitiveInformationPolicyAction.of_json in
      make ?type_ ?match_ ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Guardrail filter to identify and remove personally identifiable information (PII)."]
module GuardrailPiiEntityFilterList =
  struct
    type nonrec t = GuardrailPiiEntityFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailPiiEntityFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailPiiEntityFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailPiiEntityFilterList"
        ~of_json:GuardrailPiiEntityFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailSensitiveInformationPolicyAssessment =
  struct
    type nonrec t =
      {
      piiEntities: GuardrailPiiEntityFilterList.t option
        [@ocaml.doc
          "The details of the PII entities used in the sensitive policy assessment for the Guardrail."];
      regexes: GuardrailRegexFilterList.t option
        [@ocaml.doc
          "The details of the regexes used in the sensitive policy assessment for the Guardrail."]}
    let make ?piiEntities =
      fun ?regexes -> fun () -> { piiEntities; regexes }
    let to_value x =
      structure_to_value
        [("piiEntities",
           (Option.map x.piiEntities ~f:GuardrailPiiEntityFilterList.to_value));
        ("regexes",
          (Option.map x.regexes ~f:GuardrailRegexFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let regexes =
        (Option.map ~f:GuardrailRegexFilterList.of_xml)
          (Xml.child xml_arg0 "regexes") in
      let piiEntities =
        (Option.map ~f:GuardrailPiiEntityFilterList.of_xml)
          (Xml.child xml_arg0 "piiEntities") in
      make ?regexes ?piiEntities ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let regexes =
        field_map json__ "regexes" GuardrailRegexFilterList.of_json in
      let piiEntities =
        field_map json__ "piiEntities" GuardrailPiiEntityFilterList.of_json in
      make ?regexes ?piiEntities ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of the sensitive policy assessment used in the Guardrail."]
module GuardrailContentPolicyAction =
  struct
    type nonrec t =
      | BLOCKED 
      | Non_static_id of string 
    let make i = i
    let to_string = function | BLOCKED -> "BLOCKED" | Non_static_id s -> s
    let of_string = function | "BLOCKED" -> BLOCKED | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentPolicyAction"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentPolicyAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContentFilterType =
  struct
    type nonrec t =
      | INSULTS 
      | HATE 
      | SEXUAL 
      | VIOLENCE 
      | MISCONDUCT 
      | PROMPT_ATTACK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INSULTS -> "INSULTS"
      | HATE -> "HATE"
      | SEXUAL -> "SEXUAL"
      | VIOLENCE -> "VIOLENCE"
      | MISCONDUCT -> "MISCONDUCT"
      | PROMPT_ATTACK -> "PROMPT_ATTACK"
      | Non_static_id s -> s
    let of_string =
      function
      | "INSULTS" -> INSULTS
      | "HATE" -> HATE
      | "SEXUAL" -> SEXUAL
      | "VIOLENCE" -> VIOLENCE
      | "MISCONDUCT" -> MISCONDUCT
      | "PROMPT_ATTACK" -> PROMPT_ATTACK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentFilterType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentFilterType" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContentFilterConfidence =
  struct
    type nonrec t =
      | NONE 
      | LOW 
      | MEDIUM 
      | HIGH 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | LOW -> "LOW"
      | MEDIUM -> "MEDIUM"
      | HIGH -> "HIGH"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "LOW" -> LOW
      | "MEDIUM" -> MEDIUM
      | "HIGH" -> HIGH
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GuardrailContentFilterConfidence"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GuardrailContentFilterConfidence" j)
    let to_json = simple_to_json to_value
  end
module GuardrailContentFilter =
  struct
    type nonrec t =
      {
      action: GuardrailContentPolicyAction.t option
        [@ocaml.doc
          "The action placed on the content by the Guardrail filter."];
      confidence: GuardrailContentFilterConfidence.t option
        [@ocaml.doc
          "The confidence level regarding the content detected in the filter by the Guardrail."];
      type_: GuardrailContentFilterType.t option
        [@ocaml.doc
          "The type of content detected in the filter by the Guardrail."]}
    let make ?action =
      fun ?confidence ->
        fun ?type_ -> fun () -> { action; confidence; type_ }
    let to_value x =
      structure_to_value
        [("action",
           (Option.map x.action ~f:GuardrailContentPolicyAction.to_value));
        ("confidence",
          (Option.map x.confidence
             ~f:GuardrailContentFilterConfidence.to_value));
        ("type", (Option.map x.type_ ~f:GuardrailContentFilterType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:GuardrailContentFilterType.of_xml)
          (Xml.child xml_arg0 "type") in
      let confidence =
        (Option.map ~f:GuardrailContentFilterConfidence.of_xml)
          (Xml.child xml_arg0 "confidence") in
      let action =
        (Option.map ~f:GuardrailContentPolicyAction.of_xml)
          (Xml.child xml_arg0 "action") in
      make ?type_ ?confidence ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" GuardrailContentFilterType.of_json in
      let confidence =
        field_map json__ "confidence"
          GuardrailContentFilterConfidence.of_json in
      let action =
        field_map json__ "action" GuardrailContentPolicyAction.of_json in
      make ?type_ ?confidence ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details of the content filter used in the Guardrail."]
module GuardrailContentFilterList =
  struct
    type nonrec t = GuardrailContentFilter.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailContentFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailContentFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailContentFilterList"
        ~of_json:GuardrailContentFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailContentPolicyAssessment =
  struct
    type nonrec t =
      {
      filters: GuardrailContentFilterList.t option
        [@ocaml.doc
          "The filter details of the policy assessment used in the Guardrails filter."]}
    let make ?filters = fun () -> { filters }
    let to_value x =
      structure_to_value
        [("filters",
           (Option.map x.filters ~f:GuardrailContentFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:GuardrailContentFilterList.of_xml)
          (Xml.child xml_arg0 "filters") in
      make ?filters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let filters =
        field_map json__ "filters" GuardrailContentFilterList.of_json in
      make ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of the policy assessment in the Guardrails filter."]
module GuardrailAssessment =
  struct
    type nonrec t =
      {
      contentPolicy: GuardrailContentPolicyAssessment.t option
        [@ocaml.doc "Content policy details of the Guardrail."];
      sensitiveInformationPolicy:
        GuardrailSensitiveInformationPolicyAssessment.t option
        [@ocaml.doc "Sensitive Information policy details of Guardrail."];
      topicPolicy: GuardrailTopicPolicyAssessment.t option
        [@ocaml.doc "Topic policy details of the Guardrail."];
      wordPolicy: GuardrailWordPolicyAssessment.t option
        [@ocaml.doc "Word policy details of the Guardrail."]}
    let make ?contentPolicy =
      fun ?sensitiveInformationPolicy ->
        fun ?topicPolicy ->
          fun ?wordPolicy ->
            fun () ->
              {
                contentPolicy;
                sensitiveInformationPolicy;
                topicPolicy;
                wordPolicy
              }
    let to_value x =
      structure_to_value
        [("contentPolicy",
           (Option.map x.contentPolicy
              ~f:GuardrailContentPolicyAssessment.to_value));
        ("sensitiveInformationPolicy",
          (Option.map x.sensitiveInformationPolicy
             ~f:GuardrailSensitiveInformationPolicyAssessment.to_value));
        ("topicPolicy",
          (Option.map x.topicPolicy
             ~f:GuardrailTopicPolicyAssessment.to_value));
        ("wordPolicy",
          (Option.map x.wordPolicy ~f:GuardrailWordPolicyAssessment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let wordPolicy =
        (Option.map ~f:GuardrailWordPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "wordPolicy") in
      let topicPolicy =
        (Option.map ~f:GuardrailTopicPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "topicPolicy") in
      let sensitiveInformationPolicy =
        (Option.map ~f:GuardrailSensitiveInformationPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "sensitiveInformationPolicy") in
      let contentPolicy =
        (Option.map ~f:GuardrailContentPolicyAssessment.of_xml)
          (Xml.child xml_arg0 "contentPolicy") in
      make ?wordPolicy ?topicPolicy ?sensitiveInformationPolicy
        ?contentPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let wordPolicy =
        field_map json__ "wordPolicy" GuardrailWordPolicyAssessment.of_json in
      let topicPolicy =
        field_map json__ "topicPolicy" GuardrailTopicPolicyAssessment.of_json in
      let sensitiveInformationPolicy =
        field_map json__ "sensitiveInformationPolicy"
          GuardrailSensitiveInformationPolicyAssessment.of_json in
      let contentPolicy =
        field_map json__ "contentPolicy"
          GuardrailContentPolicyAssessment.of_json in
      make ?wordPolicy ?topicPolicy ?sensitiveInformationPolicy
        ?contentPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Assessment details of the content analyzed by Guardrails."]
module GuardrailAssessmentList =
  struct
    type nonrec t = GuardrailAssessment.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GuardrailAssessment.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GuardrailAssessment.of_xml)
    let of_json j =
      list_of_json ~kind:"GuardrailAssessmentList"
        ~of_json:GuardrailAssessment.of_json j
    let to_json v = composed_to_json to_value v
  end
module GuardrailAction =
  struct
    type nonrec t =
      | INTERVENED 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERVENED -> "INTERVENED"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERVENED" -> INTERVENED
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GuardrailAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuardrailAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailTrace =
  struct
    type nonrec t =
      {
      action: GuardrailAction.t option
        [@ocaml.doc "The trace action details used with the Guardrail."];
      inputAssessments: GuardrailAssessmentList.t option
        [@ocaml.doc
          "The details of the input assessments used in the Guardrail Trace."];
      metadata: Metadata.t option
        [@ocaml.doc "Contains information about the Guardrail output."];
      outputAssessments: GuardrailAssessmentList.t option
        [@ocaml.doc
          "The details of the output assessments used in the Guardrail Trace."];
      traceId: TraceId.t option
        [@ocaml.doc
          "The details of the trace Id used in the Guardrail Trace."]}
    let make ?action =
      fun ?inputAssessments ->
        fun ?metadata ->
          fun ?outputAssessments ->
            fun ?traceId ->
              fun () ->
                {
                  action;
                  inputAssessments;
                  metadata;
                  outputAssessments;
                  traceId
                }
    let to_value x =
      structure_to_value
        [("action", (Option.map x.action ~f:GuardrailAction.to_value));
        ("inputAssessments",
          (Option.map x.inputAssessments ~f:GuardrailAssessmentList.to_value));
        ("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("outputAssessments",
          (Option.map x.outputAssessments ~f:GuardrailAssessmentList.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let outputAssessments =
        (Option.map ~f:GuardrailAssessmentList.of_xml)
          (Xml.child xml_arg0 "outputAssessments") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      let inputAssessments =
        (Option.map ~f:GuardrailAssessmentList.of_xml)
          (Xml.child xml_arg0 "inputAssessments") in
      let action =
        (Option.map ~f:GuardrailAction.of_xml) (Xml.child xml_arg0 "action") in
      make ?traceId ?outputAssessments ?metadata ?inputAssessments ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let outputAssessments =
        field_map json__ "outputAssessments" GuardrailAssessmentList.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      let inputAssessments =
        field_map json__ "inputAssessments" GuardrailAssessmentList.of_json in
      let action = field_map json__ "action" GuardrailAction.of_json in
      make ?traceId ?outputAssessments ?metadata ?inputAssessments ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The trace details used in the Guardrail."]
module FailureReasonString =
  struct
    type nonrec t = string
    let context_ = "FailureReasonString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FailureReasonString" j
    let to_json = simple_to_json to_value
  end
module FailureTrace =
  struct
    type nonrec t =
      {
      failureCode: Integer.t option
        [@ocaml.doc "The failure code for the trace."];
      failureReason: FailureReasonString.t option
        [@ocaml.doc "The reason the interaction failed."];
      metadata: Metadata.t option
        [@ocaml.doc "Information about the failure that occurred."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace."]}
    let make ?failureCode =
      fun ?failureReason ->
        fun ?metadata ->
          fun ?traceId ->
            fun () -> { failureCode; failureReason; metadata; traceId }
    let to_value x =
      structure_to_value
        [("failureCode", (Option.map x.failureCode ~f:Integer.to_value));
        ("failureReason",
          (Option.map x.failureReason ~f:FailureReasonString.to_value));
        ("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      let failureReason =
        (Option.map ~f:FailureReasonString.of_xml)
          (Xml.child xml_arg0 "failureReason") in
      let failureCode =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "failureCode") in
      make ?traceId ?metadata ?failureReason ?failureCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let metadata = field_map json__ "metadata" Metadata.of_json in
      let failureReason =
        field_map json__ "failureReason" FailureReasonString.of_json in
      let failureCode = field_map json__ "failureCode" Integer.of_json in
      make ?traceId ?metadata ?failureReason ?failureCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the failure of the interaction."]
module CustomOrchestrationTraceEvent =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc "The text that prompted the event at this step."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The event in the custom orchestration sequence. Events are the responses which the custom orchestration Lambda function sends as response to the agent."]
module CustomOrchestrationTrace =
  struct
    type nonrec t =
      {
      event: CustomOrchestrationTraceEvent.t option
        [@ocaml.doc "The event details used with the custom orchestration."];
      traceId: TraceId.t option
        [@ocaml.doc "The unique identifier of the trace."]}
    let make ?event = fun ?traceId -> fun () -> { event; traceId }
    let to_value x =
      structure_to_value
        [("event",
           (Option.map x.event ~f:CustomOrchestrationTraceEvent.to_value));
        ("traceId", (Option.map x.traceId ~f:TraceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceId =
        (Option.map ~f:TraceId.of_xml) (Xml.child xml_arg0 "traceId") in
      let event =
        (Option.map ~f:CustomOrchestrationTraceEvent.of_xml)
          (Xml.child xml_arg0 "event") in
      make ?traceId ?event ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceId = field_map json__ "traceId" TraceId.of_json in
      let event =
        field_map json__ "event" CustomOrchestrationTraceEvent.of_json in
      make ?traceId ?event ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The trace behavior for the custom orchestration."]
module Trace =
  struct
    type nonrec t =
      {
      customOrchestrationTrace: CustomOrchestrationTrace.t option
        [@ocaml.doc
          "Details about the custom orchestration step in which the agent determines the order in which actions are executed."];
      failureTrace: FailureTrace.t option
        [@ocaml.doc
          "Contains information about the failure of the interaction."];
      guardrailTrace: GuardrailTrace.t option
        [@ocaml.doc
          "The trace details for a trace defined in the Guardrail filter."];
      orchestrationTrace: OrchestrationTrace.t option
        [@ocaml.doc
          "Details about the orchestration step, in which the agent determines the order in which actions are executed and which knowledge bases are retrieved."];
      postProcessingTrace: PostProcessingTrace.t option
        [@ocaml.doc
          "Details about the post-processing step, in which the agent shapes the response.."];
      preProcessingTrace: PreProcessingTrace.t option
        [@ocaml.doc
          "Details about the pre-processing step, in which the agent contextualizes and categorizes user inputs."];
      routingClassifierTrace: RoutingClassifierTrace.t option
        [@ocaml.doc "A routing classifier's trace."]}
    let make ?customOrchestrationTrace =
      fun ?failureTrace ->
        fun ?guardrailTrace ->
          fun ?orchestrationTrace ->
            fun ?postProcessingTrace ->
              fun ?preProcessingTrace ->
                fun ?routingClassifierTrace ->
                  fun () ->
                    {
                      customOrchestrationTrace;
                      failureTrace;
                      guardrailTrace;
                      orchestrationTrace;
                      postProcessingTrace;
                      preProcessingTrace;
                      routingClassifierTrace
                    }
    let to_value x =
      structure_to_value
        [("customOrchestrationTrace",
           (Option.map x.customOrchestrationTrace
              ~f:CustomOrchestrationTrace.to_value));
        ("failureTrace",
          (Option.map x.failureTrace ~f:FailureTrace.to_value));
        ("guardrailTrace",
          (Option.map x.guardrailTrace ~f:GuardrailTrace.to_value));
        ("orchestrationTrace",
          (Option.map x.orchestrationTrace ~f:OrchestrationTrace.to_value));
        ("postProcessingTrace",
          (Option.map x.postProcessingTrace ~f:PostProcessingTrace.to_value));
        ("preProcessingTrace",
          (Option.map x.preProcessingTrace ~f:PreProcessingTrace.to_value));
        ("routingClassifierTrace",
          (Option.map x.routingClassifierTrace
             ~f:RoutingClassifierTrace.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routingClassifierTrace =
        (Option.map ~f:RoutingClassifierTrace.of_xml)
          (Xml.child xml_arg0 "routingClassifierTrace") in
      let preProcessingTrace =
        (Option.map ~f:PreProcessingTrace.of_xml)
          (Xml.child xml_arg0 "preProcessingTrace") in
      let postProcessingTrace =
        (Option.map ~f:PostProcessingTrace.of_xml)
          (Xml.child xml_arg0 "postProcessingTrace") in
      let orchestrationTrace =
        (Option.map ~f:OrchestrationTrace.of_xml)
          (Xml.child xml_arg0 "orchestrationTrace") in
      let guardrailTrace =
        (Option.map ~f:GuardrailTrace.of_xml)
          (Xml.child xml_arg0 "guardrailTrace") in
      let failureTrace =
        (Option.map ~f:FailureTrace.of_xml)
          (Xml.child xml_arg0 "failureTrace") in
      let customOrchestrationTrace =
        (Option.map ~f:CustomOrchestrationTrace.of_xml)
          (Xml.child xml_arg0 "customOrchestrationTrace") in
      make ?routingClassifierTrace ?preProcessingTrace ?postProcessingTrace
        ?orchestrationTrace ?guardrailTrace ?failureTrace
        ?customOrchestrationTrace ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let routingClassifierTrace =
        field_map json__ "routingClassifierTrace"
          RoutingClassifierTrace.of_json in
      let preProcessingTrace =
        field_map json__ "preProcessingTrace" PreProcessingTrace.of_json in
      let postProcessingTrace =
        field_map json__ "postProcessingTrace" PostProcessingTrace.of_json in
      let orchestrationTrace =
        field_map json__ "orchestrationTrace" OrchestrationTrace.of_json in
      let guardrailTrace =
        field_map json__ "guardrailTrace" GuardrailTrace.of_json in
      let failureTrace = field_map json__ "failureTrace" FailureTrace.of_json in
      let customOrchestrationTrace =
        field_map json__ "customOrchestrationTrace"
          CustomOrchestrationTrace.of_json in
      make ?routingClassifierTrace ?preProcessingTrace ?postProcessingTrace
        ?orchestrationTrace ?guardrailTrace ?failureTrace
        ?customOrchestrationTrace ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains one part of the agent's reasoning process and results from calling API actions and querying knowledge bases. You can use the trace to understand how the agent arrived at the response it provided the customer. For more information, see Trace enablement."]
module SessionId =
  struct
    type nonrec t = string
    let context_ = "SessionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9a-zA-Z._:-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionId" j
    let to_json = simple_to_json to_value
  end
module Caller =
  struct
    type nonrec t =
      {
      agentAliasArn: AgentAliasArn.t option
        [@ocaml.doc "The caller's agent alias ARN."]}
    let make ?agentAliasArn = fun () -> { agentAliasArn }
    let to_value x =
      structure_to_value
        [("agentAliasArn",
           (Option.map x.agentAliasArn ~f:AgentAliasArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agentAliasArn =
        (Option.map ~f:AgentAliasArn.of_xml)
          (Xml.child xml_arg0 "agentAliasArn") in
      make ?agentAliasArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agentAliasArn =
        field_map json__ "agentAliasArn" AgentAliasArn.of_json in
      make ?agentAliasArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details about a caller."]
module CallerChain =
  struct
    type nonrec t = Caller.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Caller.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Caller.of_xml)
    let of_json j =
      list_of_json ~kind:"CallerChain" ~of_json:Caller.of_json j
    let to_json v = composed_to_json to_value v
  end
module AgentVersion =
  struct
    type nonrec t = string
    let context_ = "AgentVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:5) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(DRAFT|[0-9]{0,4}[1-9][0-9]{0,4})$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgentVersion" j
    let to_json = simple_to_json to_value
  end
module TracePart =
  struct
    type nonrec t =
      {
      agentAliasId: AgentAliasId.t option
        [@ocaml.doc "The unique identifier of the alias of the agent."];
      agentId: AgentId.t option
        [@ocaml.doc "The unique identifier of the agent."];
      agentVersion: AgentVersion.t option
        [@ocaml.doc "The version of the agent."];
      callerChain: CallerChain.t option
        [@ocaml.doc "The part's caller chain."];
      collaboratorName: Name.t option
        [@ocaml.doc "The part's collaborator name."];
      eventTime: SyntheticTimestamp_date_time.t option
        [@ocaml.doc "The time of the trace."];
      sessionId: SessionId.t option
        [@ocaml.doc "The unique identifier of the session with the agent."];
      trace: Trace.t option
        [@ocaml.doc
          "Contains one part of the agent's reasoning process and results from calling API actions and querying knowledge bases. You can use the trace to understand how the agent arrived at the response it provided the customer. For more information, see Trace enablement."]}
    let make ?agentAliasId =
      fun ?agentId ->
        fun ?agentVersion ->
          fun ?callerChain ->
            fun ?collaboratorName ->
              fun ?eventTime ->
                fun ?sessionId ->
                  fun ?trace ->
                    fun () ->
                      {
                        agentAliasId;
                        agentId;
                        agentVersion;
                        callerChain;
                        collaboratorName;
                        eventTime;
                        sessionId;
                        trace
                      }
    let to_value x =
      structure_to_value
        [("agentAliasId",
           (Option.map x.agentAliasId ~f:AgentAliasId.to_value));
        ("agentId", (Option.map x.agentId ~f:AgentId.to_value));
        ("agentVersion",
          (Option.map x.agentVersion ~f:AgentVersion.to_value));
        ("callerChain", (Option.map x.callerChain ~f:CallerChain.to_value));
        ("collaboratorName",
          (Option.map x.collaboratorName ~f:Name.to_value));
        ("eventTime",
          (Option.map x.eventTime ~f:SyntheticTimestamp_date_time.to_value));
        ("sessionId", (Option.map x.sessionId ~f:SessionId.to_value));
        ("trace", (Option.map x.trace ~f:Trace.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trace = (Option.map ~f:Trace.of_xml) (Xml.child xml_arg0 "trace") in
      let sessionId =
        (Option.map ~f:SessionId.of_xml) (Xml.child xml_arg0 "sessionId") in
      let eventTime =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "eventTime") in
      let collaboratorName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "collaboratorName") in
      let callerChain =
        (Option.map ~f:CallerChain.of_xml) (Xml.child xml_arg0 "callerChain") in
      let agentVersion =
        (Option.map ~f:AgentVersion.of_xml)
          (Xml.child xml_arg0 "agentVersion") in
      let agentId =
        (Option.map ~f:AgentId.of_xml) (Xml.child xml_arg0 "agentId") in
      let agentAliasId =
        (Option.map ~f:AgentAliasId.of_xml)
          (Xml.child xml_arg0 "agentAliasId") in
      make ?trace ?sessionId ?eventTime ?collaboratorName ?callerChain
        ?agentVersion ?agentId ?agentAliasId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trace = field_map json__ "trace" Trace.of_json in
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let eventTime =
        field_map json__ "eventTime" SyntheticTimestamp_date_time.of_json in
      let collaboratorName = field_map json__ "collaboratorName" Name.of_json in
      let callerChain = field_map json__ "callerChain" CallerChain.of_json in
      let agentVersion = field_map json__ "agentVersion" AgentVersion.of_json in
      let agentId = field_map json__ "agentId" AgentId.of_json in
      let agentAliasId = field_map json__ "agentAliasId" AgentAliasId.of_json in
      make ?trace ?sessionId ?eventTime ?collaboratorName ?callerChain
        ?agentVersion ?agentId ?agentAliasId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the agent and session, alongside the agent's reasoning process and results from calling API actions and querying knowledge bases and metadata about the trace. You can use the trace to understand how the agent arrived at the response it provided the customer. For more information, see Trace enablement."]
module AgentTraces =
  struct
    type nonrec t = TracePart.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TracePart.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TracePart.of_xml)
    let of_json j =
      list_of_json ~kind:"AgentTraces" ~of_json:TracePart.of_json j
    let to_json v = composed_to_json to_value v
  end
module AnalyzePromptEvent =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc "A message describing the analysis of the prompt."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An event in which the prompt was analyzed in preparation for optimization."]
module AttributeType =
  struct
    type nonrec t =
      | STRING 
      | NUMBER 
      | BOOLEAN 
      | STRING_LIST 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STRING -> "STRING"
      | NUMBER -> "NUMBER"
      | BOOLEAN -> "BOOLEAN"
      | STRING_LIST -> "STRING_LIST"
      | Non_static_id s -> s
    let of_string =
      function
      | "STRING" -> STRING
      | "NUMBER" -> NUMBER
      | "BOOLEAN" -> BOOLEAN
      | "STRING_LIST" -> STRING_LIST
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AttributeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AttributeType" j)
    let to_json = simple_to_json to_value
  end
module SpanStartInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SpanStartInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SpanEndInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SpanEndInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Span =
  struct
    type nonrec t =
      {
      end_: SpanEndInteger.t option
        [@ocaml.doc
          "Where the text with a citation ends in the generated output."];
      start: SpanStartInteger.t option
        [@ocaml.doc
          "Where the text with a citation starts in the generated output."]}
    let make ?end_ = fun ?start -> fun () -> { end_; start }
    let to_value x =
      structure_to_value
        [("end", (Option.map x.end_ ~f:SpanEndInteger.to_value));
        ("start", (Option.map x.start ~f:SpanStartInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let start =
        (Option.map ~f:SpanStartInteger.of_xml) (Xml.child xml_arg0 "start") in
      let end_ =
        (Option.map ~f:SpanEndInteger.of_xml) (Xml.child xml_arg0 "end") in
      make ?start ?end_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let start = field_map json__ "start" SpanStartInteger.of_json in
      let end_ = field_map json__ "end" SpanEndInteger.of_json in
      make ?start ?end_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about where the text with a citation begins and ends in the generated output. This data type is used in the following API operations: RetrieveAndGenerate response \226\128\147 in the span field InvokeAgent response \226\128\147 in the span field"]
module TextResponsePart =
  struct
    type nonrec t =
      {
      span: Span.t option
        [@ocaml.doc
          "Contains information about where the text with a citation begins and ends in the generated output."];
      text: String_.t option
        [@ocaml.doc
          "The part of the generated text that contains a citation."]}
    let make ?span = fun ?text -> fun () -> { span; text }
    let to_value x =
      structure_to_value
        [("span", (Option.map x.span ~f:Span.to_value));
        ("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      let span = (Option.map ~f:Span.of_xml) (Xml.child xml_arg0 "span") in
      make ?text ?span ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in
      let span = field_map json__ "span" Span.of_json in make ?text ?span ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the part of the generated text that contains a citation, alongside where it begins and ends. This data type is used in the following API operations: RetrieveAndGenerate response \226\128\147 in the textResponsePart field InvokeAgent response \226\128\147 in the textResponsePart field"]
module GeneratedResponsePart =
  struct
    type nonrec t =
      {
      textResponsePart: TextResponsePart.t option
        [@ocaml.doc
          "Contains metadata about a textual part of the generated response that is accompanied by a citation."]}
    let make ?textResponsePart = fun () -> { textResponsePart }
    let to_value x =
      structure_to_value
        [("textResponsePart",
           (Option.map x.textResponsePart ~f:TextResponsePart.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let textResponsePart =
        (Option.map ~f:TextResponsePart.of_xml)
          (Xml.child xml_arg0 "textResponsePart") in
      make ?textResponsePart ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let textResponsePart =
        field_map json__ "textResponsePart" TextResponsePart.of_json in
      make ?textResponsePart ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains metadata about a part of the generated response that is accompanied by a citation. This data type is used in the following API operations: InvokeAgent response \226\128\147 in the generatedResponsePart field RetrieveAndGenerate response \226\128\147 in the generatedResponsePart field"]
module Citation =
  struct
    type nonrec t =
      {
      generatedResponsePart: GeneratedResponsePart.t option
        [@ocaml.doc "Contains the generated response and metadata"];
      retrievedReferences: RetrievedReferences.t option
        [@ocaml.doc
          "Contains metadata about the sources cited for the generated response."]}
    let make ?generatedResponsePart =
      fun ?retrievedReferences ->
        fun () -> { generatedResponsePart; retrievedReferences }
    let to_value x =
      structure_to_value
        [("generatedResponsePart",
           (Option.map x.generatedResponsePart
              ~f:GeneratedResponsePart.to_value));
        ("retrievedReferences",
          (Option.map x.retrievedReferences ~f:RetrievedReferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retrievedReferences =
        (Option.map ~f:RetrievedReferences.of_xml)
          (Xml.child xml_arg0 "retrievedReferences") in
      let generatedResponsePart =
        (Option.map ~f:GeneratedResponsePart.of_xml)
          (Xml.child xml_arg0 "generatedResponsePart") in
      make ?retrievedReferences ?generatedResponsePart ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retrievedReferences =
        field_map json__ "retrievedReferences" RetrievedReferences.of_json in
      let generatedResponsePart =
        field_map json__ "generatedResponsePart"
          GeneratedResponsePart.of_json in
      make ?retrievedReferences ?generatedResponsePart ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object containing a segment of the generated response that is based on a source in the knowledge base, alongside information about the source. This data type is used in the following API operations: InvokeAgent response \226\128\147 in the citations field RetrieveAndGenerate response \226\128\147 in the citations field"]
module Citations =
  struct
    type nonrec t = Citation.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Citation.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Citation.of_xml)
    let of_json j =
      list_of_json ~kind:"Citations" ~of_json:Citation.of_json j
    let to_json v = composed_to_json to_value v
  end
module Attribution =
  struct
    type nonrec t =
      {
      citations: Citations.t option
        [@ocaml.doc
          "A list of citations and related information for a part of an agent response."]}
    let make ?citations = fun () -> { citations }
    let to_value x =
      structure_to_value
        [("citations", (Option.map x.citations ~f:Citations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let citations =
        (Option.map ~f:Citations.of_xml) (Xml.child xml_arg0 "citations") in
      make ?citations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let citations = field_map json__ "citations" Citations.of_json in
      make ?citations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains citations for a part of an agent response."]
module BadGatewayException =
  struct
    type nonrec t =
      {
      message: NonBlankString.t option ;
      resourceName: NonBlankString.t option
        [@ocaml.doc
          "The name of the dependency that caused the issue, such as Amazon Bedrock, Lambda, or STS."]}
    let make ?message =
      fun ?resourceName -> fun () -> { message; resourceName }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value));
        ("resourceName",
          (Option.map x.resourceName ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:NonBlankString.of_xml)
          (Xml.child xml_arg0 "resourceName") in
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?resourceName ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceName =
        field_map json__ "resourceName" NonBlankString.of_json in
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?resourceName ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There was an issue with a dependency due to a server issue. Retry your request."]
module BasePromptTemplate =
  struct
    type nonrec t = string
    let context_ = "BasePromptTemplate"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BasePromptTemplate" j
    let to_json = simple_to_json to_value
  end
module BedrockModelArn =
  struct
    type nonrec t = string
    let context_ = "BedrockModelArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(arn:aws(-[^:]+)?:(bedrock|sagemaker):[a-z0-9-]{1,20}:([0-9]{12})?:([a-z-]+/)?)?([a-z0-9.-]{1,63}){0,2}(([:][a-z0-9-]{1,63}){0,2})?(/[a-z0-9]{1,12})?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BedrockModelArn" j
    let to_json = simple_to_json to_value
  end
module PerformanceConfigLatency =
  struct
    type nonrec t =
      | Standard 
      | Optimized 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Standard -> "standard"
      | Optimized -> "optimized"
      | Non_static_id s -> s
    let of_string =
      function
      | "standard" -> Standard
      | "optimized" -> Optimized
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PerformanceConfigLatency" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PerformanceConfigLatency" j)
    let to_json = simple_to_json to_value
  end
module PerformanceConfiguration =
  struct
    type nonrec t =
      {
      latency: PerformanceConfigLatency.t option
        [@ocaml.doc
          "To use a latency-optimized version of the model, set to optimized."]}
    let make ?latency = fun () -> { latency }
    let to_value x =
      structure_to_value
        [("latency",
           (Option.map x.latency ~f:PerformanceConfigLatency.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latency =
        (Option.map ~f:PerformanceConfigLatency.of_xml)
          (Xml.child xml_arg0 "latency") in
      make ?latency ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let latency =
        field_map json__ "latency" PerformanceConfigLatency.of_json in
      make ?latency ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Performance settings for a model."]
module BedrockModelConfigurations =
  struct
    type nonrec t =
      {
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "The performance configuration for the model."]}
    let make ?performanceConfig = fun () -> { performanceConfig }
    let to_value x =
      structure_to_value
        [("performanceConfig",
           (Option.map x.performanceConfig
              ~f:PerformanceConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      make ?performanceConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      make ?performanceConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Settings for a model called with InvokeAgent."]
module BedrockRerankingModelConfiguration =
  struct
    type nonrec t =
      {
      additionalModelRequestFields: AdditionalModelRequestFields.t option
        [@ocaml.doc
          "A JSON object whose keys are request fields for the model and whose values are values for those fields."];
      modelArn: BedrockModelArn.t
        [@ocaml.doc "The ARN of the reranker model."]}
    let context_ = "BedrockRerankingModelConfiguration"
    let make ?additionalModelRequestFields =
      fun ~modelArn -> fun () -> { additionalModelRequestFields; modelArn }
    let to_value x =
      structure_to_value
        [("additionalModelRequestFields",
           (Option.map x.additionalModelRequestFields
              ~f:AdditionalModelRequestFields.to_value));
        ("modelArn", (Some (BedrockModelArn.to_value x.modelArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modelArn =
        BedrockModelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelArn") in
      let additionalModelRequestFields =
        (Option.map ~f:AdditionalModelRequestFields.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      make ~modelArn ?additionalModelRequestFields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let modelArn = field_map_exn json__ "modelArn" BedrockModelArn.of_json in
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields"
          AdditionalModelRequestFields.of_json in
      make ~modelArn ?additionalModelRequestFields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains configurations for a reranker model."]
module BedrockRerankingConfigurationNumberOfResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for BedrockRerankingConfigurationNumberOfResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module BedrockRerankingConfiguration =
  struct
    type nonrec t =
      {
      modelConfiguration: BedrockRerankingModelConfiguration.t
        [@ocaml.doc "Contains configurations for a reranker model."];
      numberOfResults:
        BedrockRerankingConfigurationNumberOfResultsInteger.t option
        [@ocaml.doc "The number of results to return after reranking."]}
    let context_ = "BedrockRerankingConfiguration"
    let make ?numberOfResults =
      fun ~modelConfiguration ->
        fun () -> { numberOfResults; modelConfiguration }
    let to_value x =
      structure_to_value
        [("modelConfiguration",
           (Some
              (BedrockRerankingModelConfiguration.to_value
                 x.modelConfiguration)));
        ("numberOfResults",
          (Option.map x.numberOfResults
             ~f:BedrockRerankingConfigurationNumberOfResultsInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let numberOfResults =
        (Option.map
           ~f:BedrockRerankingConfigurationNumberOfResultsInteger.of_xml)
          (Xml.child xml_arg0 "numberOfResults") in
      let modelConfiguration =
        BedrockRerankingModelConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelConfiguration") in
      make ?numberOfResults ~modelConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let numberOfResults =
        field_map json__ "numberOfResults"
          BedrockRerankingConfigurationNumberOfResultsInteger.of_json in
      let modelConfiguration =
        field_map_exn json__ "modelConfiguration"
          BedrockRerankingModelConfiguration.of_json in
      make ?numberOfResults ~modelConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for an Amazon Bedrock reranker model."]
module BedrockRerankingModelArn =
  struct
    type nonrec t = string
    let context_ = "BedrockRerankingModelArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}::foundation-model/(.*))?$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BedrockRerankingModelArn" j
    let to_json = simple_to_json to_value
  end
module S3Uri =
  struct
    type nonrec t = string
    let context_ = "S3Uri"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^s3://[a-z0-9][a-z0-9.-]{1,61}[a-z0-9]/.{1,1024}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Uri" j
    let to_json = simple_to_json to_value
  end
module S3Location =
  struct
    type nonrec t =
      {
      uri: S3Uri.t
        [@ocaml.doc
          "The path to the Amazon S3 bucket where the image is stored."]}
    let context_ = "S3Location"
    let make ~uri = fun () -> { uri }
    let to_value x =
      structure_to_value [("uri", (Some (S3Uri.to_value x.uri)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uri = S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "uri") in
      make ~uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uri = field_map_exn json__ "uri" S3Uri.of_json in make ~uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the Amazon S3 bucket where the image is stored."]
module ImageSourceBytesBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module ImageSource =
  struct
    type nonrec t =
      {
      bytes: ImageSourceBytesBlob.t option
        [@ocaml.doc
          "The raw image bytes for the image. If you use an Amazon Web Services SDK, you don't need to encode the image bytes in base64."];
      s3Location: S3Location.t option
        [@ocaml.doc
          "The path to the Amazon S3 bucket where the image is stored."]}
    let make ?bytes = fun ?s3Location -> fun () -> { bytes; s3Location }
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:ImageSourceBytesBlob.to_value));
        ("s3Location", (Option.map x.s3Location ~f:S3Location.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Location =
        (Option.map ~f:S3Location.of_xml) (Xml.child xml_arg0 "s3Location") in
      let bytes =
        (Option.map ~f:ImageSourceBytesBlob.of_xml)
          (Xml.child xml_arg0 "bytes") in
      make ?s3Location ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Location = field_map json__ "s3Location" S3Location.of_json in
      let bytes = field_map json__ "bytes" ImageSourceBytesBlob.of_json in
      make ?s3Location ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The source for an image."]
module ImageFormat =
  struct
    type nonrec t =
      | Png 
      | Jpeg 
      | Gif 
      | Webp 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Png -> "png"
      | Jpeg -> "jpeg"
      | Gif -> "gif"
      | Webp -> "webp"
      | Non_static_id s -> s
    let of_string =
      function
      | "png" -> Png
      | "jpeg" -> Jpeg
      | "gif" -> Gif
      | "webp" -> Webp
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ImageFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ImageFormat" j)
    let to_json = simple_to_json to_value
  end
module ImageBlock =
  struct
    type nonrec t =
      {
      format: ImageFormat.t [@ocaml.doc "The format of the image."];
      source: ImageSource.t [@ocaml.doc "The source for the image."]}
    let context_ = "ImageBlock"
    let make ~format = fun ~source -> fun () -> { format; source }
    let to_value x =
      structure_to_value
        [("format", (Some (ImageFormat.to_value x.format)));
        ("source", (Some (ImageSource.to_value x.source)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let source =
        ImageSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let format =
        ImageFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      make ~source ~format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let source = field_map_exn json__ "source" ImageSource.of_json in
      let format = field_map_exn json__ "format" ImageFormat.of_json in
      make ~source ~format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Image content for an invocation step."]
module BedrockSessionContentBlockTextString =
  struct
    type nonrec t = string
    let context_ = "BedrockSessionContentBlockTextString"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"BedrockSessionContentBlockTextString" j
    let to_json = simple_to_json to_value
  end
module BedrockSessionContentBlock =
  struct
    type nonrec t =
      {
      image: ImageBlock.t option
        [@ocaml.doc "The image in the invocation step."];
      text: BedrockSessionContentBlockTextString.t option
        [@ocaml.doc "The text in the invocation step."]}
    let make ?image = fun ?text -> fun () -> { image; text }
    let to_value x =
      structure_to_value
        [("image", (Option.map x.image ~f:ImageBlock.to_value));
        ("text",
          (Option.map x.text ~f:BedrockSessionContentBlockTextString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        (Option.map ~f:BedrockSessionContentBlockTextString.of_xml)
          (Xml.child xml_arg0 "text") in
      let image =
        (Option.map ~f:ImageBlock.of_xml) (Xml.child xml_arg0 "image") in
      make ?text ?image ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text =
        field_map json__ "text" BedrockSessionContentBlockTextString.of_json in
      let image = field_map json__ "image" ImageBlock.of_json in
      make ?text ?image ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A block of content that you pass to, or receive from, a Amazon Bedrock session in an invocation step. You pass the content to a session in the payLoad of the PutInvocationStep API operation. You retrieve the content with the GetInvocationStep API operation. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module BedrockSessionContentBlocks =
  struct
    type nonrec t = BedrockSessionContentBlock.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BedrockSessionContentBlock.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BedrockSessionContentBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"BedrockSessionContentBlocks"
        ~of_json:BedrockSessionContentBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module ByteContentBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module Identifier =
  struct
    type nonrec t = string
    let context_ = "Identifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Identifier" j
    let to_json = simple_to_json to_value
  end
module ContentType =
  struct
    type nonrec t = string
    let context_ = "ContentType"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-z]{1,20}/.{1,20}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContentType" j
    let to_json = simple_to_json to_value
  end
module ByteContentDoc =
  struct
    type nonrec t =
      {
      contentType: ContentType.t
        [@ocaml.doc
          "The MIME type of the document contained in the wrapper object."];
      data: ByteContentBlob.t
        [@ocaml.doc
          "The byte value of the file to upload, encoded as a Base-64 string."];
      identifier: Identifier.t
        [@ocaml.doc
          "The file name of the document contained in the wrapper object."]}
    let context_ = "ByteContentDoc"
    let make ~contentType =
      fun ~data ->
        fun ~identifier -> fun () -> { contentType; data; identifier }
    let to_value x =
      structure_to_value
        [("contentType", (Some (ContentType.to_value x.contentType)));
        ("data", (Some (ByteContentBlob.to_value x.data)));
        ("identifier", (Some (Identifier.to_value x.identifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identifier =
        Identifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "identifier") in
      let data =
        ByteContentBlob.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "data") in
      let contentType =
        ContentType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "contentType") in
      make ~identifier ~data ~contentType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let identifier = field_map_exn json__ "identifier" Identifier.of_json in
      let data = field_map_exn json__ "data" ByteContentBlob.of_json in
      let contentType =
        field_map_exn json__ "contentType" ContentType.of_json in
      make ~identifier ~data ~contentType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This property contains the document to chat with, along with its attributes."]
module MimeType =
  struct
    type nonrec t = string
    let context_ = "MimeType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MimeType" j
    let to_json = simple_to_json to_value
  end
module ByteContentFile =
  struct
    type nonrec t =
      {
      data: ByteContentBlob.t
        [@ocaml.doc
          "The raw bytes of the file to attach. The maximum size of all files that is attached is 10MB. You can attach a maximum of 5 files."];
      mediaType: MimeType.t
        [@ocaml.doc
          "The MIME type of data contained in the file used for chat."]}
    let context_ = "ByteContentFile"
    let make ~data = fun ~mediaType -> fun () -> { data; mediaType }
    let to_value x =
      structure_to_value
        [("data", (Some (ByteContentBlob.to_value x.data)));
        ("mediaType", (Some (MimeType.to_value x.mediaType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mediaType =
        MimeType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "mediaType") in
      let data =
        ByteContentBlob.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "data") in
      make ~mediaType ~data ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let mediaType = field_map_exn json__ "mediaType" MimeType.of_json in
      let data = field_map_exn json__ "data" ByteContentBlob.of_json in
      make ~mediaType ~data ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The property contains the file to chat with, along with its attributes."]
module CitationEvent =
  struct
    type nonrec t =
      {
      citation: Citation.t option [@ocaml.doc "The citation."];
      generatedResponsePart: GeneratedResponsePart.t option
        [@ocaml.doc "The generated response to the citation event."];
      retrievedReferences: RetrievedReferences.t option
        [@ocaml.doc "The retrieved references of the citation event."]}
    let make ?citation =
      fun ?generatedResponsePart ->
        fun ?retrievedReferences ->
          fun () -> { citation; generatedResponsePart; retrievedReferences }
    let to_value x =
      structure_to_value
        [("citation", (Option.map x.citation ~f:Citation.to_value));
        ("generatedResponsePart",
          (Option.map x.generatedResponsePart
             ~f:GeneratedResponsePart.to_value));
        ("retrievedReferences",
          (Option.map x.retrievedReferences ~f:RetrievedReferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retrievedReferences =
        (Option.map ~f:RetrievedReferences.of_xml)
          (Xml.child xml_arg0 "retrievedReferences") in
      let generatedResponsePart =
        (Option.map ~f:GeneratedResponsePart.of_xml)
          (Xml.child xml_arg0 "generatedResponsePart") in
      let citation =
        (Option.map ~f:Citation.of_xml) (Xml.child xml_arg0 "citation") in
      make ?retrievedReferences ?generatedResponsePart ?citation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retrievedReferences =
        field_map json__ "retrievedReferences" RetrievedReferences.of_json in
      let generatedResponsePart =
        field_map json__ "generatedResponsePart"
          GeneratedResponsePart.of_json in
      let citation = field_map json__ "citation" Citation.of_json in
      make ?retrievedReferences ?generatedResponsePart ?citation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A citation event."]
module CollaborationInstruction =
  struct
    type nonrec t = string
    let context_ = "CollaborationInstruction"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:4000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CollaborationInstruction" j
    let to_json = simple_to_json to_value
  end
module SessionTTL =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:3600) >>=
             (fun () -> check_int_min i ~min:60));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SessionTTL" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PromptState =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PromptState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PromptState" j)
    let to_json = simple_to_json to_value
  end
module Document =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PromptConfiguration =
  struct
    type nonrec t =
      {
      additionalModelRequestFields: Document.t option
        [@ocaml.doc
          "If the Converse or ConverseStream operations support the model, additionalModelRequestFields contains additional inference parameters, beyond the base set of inference parameters in the inferenceConfiguration field. For more information, see Inference request parameters and response fields for foundation models in the Amazon Bedrock user guide."];
      basePromptTemplate: BasePromptTemplate.t option
        [@ocaml.doc
          "Defines the prompt template with which to replace the default prompt template. You can use placeholder variables in the base prompt template to customize the prompt. For more information, see Prompt template placeholder variables. For more information, see Configure the prompt templates."];
      foundationModel: ModelIdentifier.t option
        [@ocaml.doc "The foundation model to use."];
      inferenceConfiguration: InferenceConfiguration.t option
        [@ocaml.doc
          "Contains inference parameters to use when the agent invokes a foundation model in the part of the agent sequence defined by the promptType. For more information, see Inference parameters for foundation models."];
      parserMode: CreationMode.t option
        [@ocaml.doc
          "Specifies whether to override the default parser Lambda function when parsing the raw foundation model output in the part of the agent sequence defined by the promptType. If you set the field as OVERRIDDEN, the overrideLambda field in the PromptOverrideConfiguration must be specified with the ARN of a Lambda function."];
      promptCreationMode: CreationMode.t option
        [@ocaml.doc
          "Specifies whether to override the default prompt template for this promptType. Set this value to OVERRIDDEN to use the prompt that you provide in the basePromptTemplate. If you leave it as DEFAULT, the agent uses a default prompt template."];
      promptState: PromptState.t option
        [@ocaml.doc
          "Specifies whether to allow the inline agent to carry out the step specified in the promptType. If you set this value to DISABLED, the agent skips that step. The default state for each promptType is as follows. PRE_PROCESSING \226\128\147 ENABLED ORCHESTRATION \226\128\147 ENABLED KNOWLEDGE_BASE_RESPONSE_GENERATION \226\128\147 ENABLED POST_PROCESSING \226\128\147 DISABLED"];
      promptType: PromptType.t option
        [@ocaml.doc
          "The step in the agent sequence that this prompt configuration applies to."]}
    let make ?additionalModelRequestFields =
      fun ?basePromptTemplate ->
        fun ?foundationModel ->
          fun ?inferenceConfiguration ->
            fun ?parserMode ->
              fun ?promptCreationMode ->
                fun ?promptState ->
                  fun ?promptType ->
                    fun () ->
                      {
                        additionalModelRequestFields;
                        basePromptTemplate;
                        foundationModel;
                        inferenceConfiguration;
                        parserMode;
                        promptCreationMode;
                        promptState;
                        promptType
                      }
    let to_value x =
      structure_to_value
        [("additionalModelRequestFields",
           (Option.map x.additionalModelRequestFields ~f:Document.to_value));
        ("basePromptTemplate",
          (Option.map x.basePromptTemplate ~f:BasePromptTemplate.to_value));
        ("foundationModel",
          (Option.map x.foundationModel ~f:ModelIdentifier.to_value));
        ("inferenceConfiguration",
          (Option.map x.inferenceConfiguration
             ~f:InferenceConfiguration.to_value));
        ("parserMode", (Option.map x.parserMode ~f:CreationMode.to_value));
        ("promptCreationMode",
          (Option.map x.promptCreationMode ~f:CreationMode.to_value));
        ("promptState", (Option.map x.promptState ~f:PromptState.to_value));
        ("promptType", (Option.map x.promptType ~f:PromptType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let promptType =
        (Option.map ~f:PromptType.of_xml) (Xml.child xml_arg0 "promptType") in
      let promptState =
        (Option.map ~f:PromptState.of_xml) (Xml.child xml_arg0 "promptState") in
      let promptCreationMode =
        (Option.map ~f:CreationMode.of_xml)
          (Xml.child xml_arg0 "promptCreationMode") in
      let parserMode =
        (Option.map ~f:CreationMode.of_xml) (Xml.child xml_arg0 "parserMode") in
      let inferenceConfiguration =
        (Option.map ~f:InferenceConfiguration.of_xml)
          (Xml.child xml_arg0 "inferenceConfiguration") in
      let foundationModel =
        (Option.map ~f:ModelIdentifier.of_xml)
          (Xml.child xml_arg0 "foundationModel") in
      let basePromptTemplate =
        (Option.map ~f:BasePromptTemplate.of_xml)
          (Xml.child xml_arg0 "basePromptTemplate") in
      let additionalModelRequestFields =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      make ?promptType ?promptState ?promptCreationMode ?parserMode
        ?inferenceConfiguration ?foundationModel ?basePromptTemplate
        ?additionalModelRequestFields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let promptType = field_map json__ "promptType" PromptType.of_json in
      let promptState = field_map json__ "promptState" PromptState.of_json in
      let promptCreationMode =
        field_map json__ "promptCreationMode" CreationMode.of_json in
      let parserMode = field_map json__ "parserMode" CreationMode.of_json in
      let inferenceConfiguration =
        field_map json__ "inferenceConfiguration"
          InferenceConfiguration.of_json in
      let foundationModel =
        field_map json__ "foundationModel" ModelIdentifier.of_json in
      let basePromptTemplate =
        field_map json__ "basePromptTemplate" BasePromptTemplate.of_json in
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields" Document.of_json in
      make ?promptType ?promptState ?promptCreationMode ?parserMode
        ?inferenceConfiguration ?foundationModel ?basePromptTemplate
        ?additionalModelRequestFields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations to override a prompt template in one part of an agent sequence. For more information, see Advanced prompts."]
module PromptConfigurations =
  struct
    type nonrec t = PromptConfiguration.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PromptConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PromptConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"PromptConfigurations"
        ~of_json:PromptConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module PromptOverrideConfiguration =
  struct
    type nonrec t =
      {
      overrideLambda: LambdaResourceArn.t option
        [@ocaml.doc
          "The ARN of the Lambda function to use when parsing the raw foundation model output in parts of the agent sequence. If you specify this field, at least one of the promptConfigurations must contain a parserMode value that is set to OVERRIDDEN. For more information, see Parser Lambda function in Amazon Bedrock Agents."];
      promptConfigurations: PromptConfigurations.t
        [@ocaml.doc
          "Contains configurations to override a prompt template in one part of an agent sequence. For more information, see Advanced prompts."]}
    let context_ = "PromptOverrideConfiguration"
    let make ?overrideLambda =
      fun ~promptConfigurations ->
        fun () -> { overrideLambda; promptConfigurations }
    let to_value x =
      structure_to_value
        [("overrideLambda",
           (Option.map x.overrideLambda ~f:LambdaResourceArn.to_value));
        ("promptConfigurations",
          (Some (PromptConfigurations.to_value x.promptConfigurations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let promptConfigurations =
        PromptConfigurations.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "promptConfigurations") in
      let overrideLambda =
        (Option.map ~f:LambdaResourceArn.of_xml)
          (Xml.child xml_arg0 "overrideLambda") in
      make ~promptConfigurations ?overrideLambda ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let promptConfigurations =
        field_map_exn json__ "promptConfigurations"
          PromptConfigurations.of_json in
      let overrideLambda =
        field_map json__ "overrideLambda" LambdaResourceArn.of_json in
      make ~promptConfigurations ?overrideLambda ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations to override prompts in different parts of an agent sequence. For more information, see Advanced prompts."]
module VectorSearchRerankingConfigurationType =
  struct
    type nonrec t =
      | BEDROCK_RERANKING_MODEL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BEDROCK_RERANKING_MODEL -> "BEDROCK_RERANKING_MODEL"
      | Non_static_id s -> s
    let of_string =
      function
      | "BEDROCK_RERANKING_MODEL" -> BEDROCK_RERANKING_MODEL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration VectorSearchRerankingConfigurationType"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"VectorSearchRerankingConfigurationType" j)
    let to_json = simple_to_json to_value
  end
module VectorSearchBedrockRerankingModelConfiguration =
  struct
    type nonrec t =
      {
      additionalModelRequestFields: AdditionalModelRequestFields.t option
        [@ocaml.doc
          "A JSON object whose keys are request fields for the model and whose values are values for those fields."];
      modelArn: BedrockRerankingModelArn.t
        [@ocaml.doc "The ARN of the reranker model to use."]}
    let context_ = "VectorSearchBedrockRerankingModelConfiguration"
    let make ?additionalModelRequestFields =
      fun ~modelArn -> fun () -> { additionalModelRequestFields; modelArn }
    let to_value x =
      structure_to_value
        [("additionalModelRequestFields",
           (Option.map x.additionalModelRequestFields
              ~f:AdditionalModelRequestFields.to_value));
        ("modelArn", (Some (BedrockRerankingModelArn.to_value x.modelArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modelArn =
        BedrockRerankingModelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelArn") in
      let additionalModelRequestFields =
        (Option.map ~f:AdditionalModelRequestFields.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      make ~modelArn ?additionalModelRequestFields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let modelArn =
        field_map_exn json__ "modelArn" BedrockRerankingModelArn.of_json in
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields"
          AdditionalModelRequestFields.of_json in
      make ~modelArn ?additionalModelRequestFields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for an Amazon Bedrock reranker model."]
module VectorSearchBedrockRerankingConfigurationNumberOfRerankedResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for VectorSearchBedrockRerankingConfigurationNumberOfRerankedResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module FieldForRerankingFieldNameString =
  struct
    type nonrec t = string
    let context_ = "FieldForRerankingFieldNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FieldForRerankingFieldNameString" j
    let to_json = simple_to_json to_value
  end
module FieldForReranking =
  struct
    type nonrec t =
      {
      fieldName: FieldForRerankingFieldNameString.t
        [@ocaml.doc
          "The name of a metadata field to include in or exclude from consideration when reranking."]}
    let context_ = "FieldForReranking"
    let make ~fieldName = fun () -> { fieldName }
    let to_value x =
      structure_to_value
        [("fieldName",
           (Some (FieldForRerankingFieldNameString.to_value x.fieldName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldName =
        FieldForRerankingFieldNameString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "fieldName") in
      make ~fieldName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldName =
        field_map_exn json__ "fieldName"
          FieldForRerankingFieldNameString.of_json in
      make ~fieldName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information for a metadata field to include in or exclude from consideration when reranking."]
module FieldsForReranking =
  struct
    type nonrec t = FieldForReranking.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FieldForReranking.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FieldForReranking.of_xml)
    let of_json j =
      list_of_json ~kind:"FieldsForReranking"
        ~of_json:FieldForReranking.of_json j
    let to_json v = composed_to_json to_value v
  end
module RerankingMetadataSelectiveModeConfiguration =
  struct
    type nonrec t =
      {
      fieldsToExclude: FieldsForReranking.t option
        [@ocaml.doc
          "An array of objects, each of which specifies a metadata field to exclude from consideration when reranking."];
      fieldsToInclude: FieldsForReranking.t option
        [@ocaml.doc
          "An array of objects, each of which specifies a metadata field to include in consideration when reranking. The remaining metadata fields are ignored."]}
    let make ?fieldsToExclude =
      fun ?fieldsToInclude -> fun () -> { fieldsToExclude; fieldsToInclude }
    let to_value x =
      structure_to_value
        [("fieldsToExclude",
           (Option.map x.fieldsToExclude ~f:FieldsForReranking.to_value));
        ("fieldsToInclude",
          (Option.map x.fieldsToInclude ~f:FieldsForReranking.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldsToInclude =
        (Option.map ~f:FieldsForReranking.of_xml)
          (Xml.child xml_arg0 "fieldsToInclude") in
      let fieldsToExclude =
        (Option.map ~f:FieldsForReranking.of_xml)
          (Xml.child xml_arg0 "fieldsToExclude") in
      make ?fieldsToInclude ?fieldsToExclude ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldsToInclude =
        field_map json__ "fieldsToInclude" FieldsForReranking.of_json in
      let fieldsToExclude =
        field_map json__ "fieldsToExclude" FieldsForReranking.of_json in
      make ?fieldsToInclude ?fieldsToExclude ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for the metadata fields to include or exclude when considering reranking. If you include the fieldsToExclude field, the reranker ignores all the metadata fields that you specify. If you include the fieldsToInclude field, the reranker uses only the metadata fields that you specify and ignores all others. You can include only one of these fields."]
module RerankingMetadataSelectionMode =
  struct
    type nonrec t =
      | SELECTIVE 
      | ALL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SELECTIVE -> "SELECTIVE"
      | ALL -> "ALL"
      | Non_static_id s -> s
    let of_string =
      function
      | "SELECTIVE" -> SELECTIVE
      | "ALL" -> ALL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RerankingMetadataSelectionMode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RerankingMetadataSelectionMode" j)
    let to_json = simple_to_json to_value
  end
module MetadataConfigurationForReranking =
  struct
    type nonrec t =
      {
      selectionMode: RerankingMetadataSelectionMode.t
        [@ocaml.doc
          "Specifies whether to consider all metadata when reranking, or only the metadata that you select. If you specify SELECTIVE, include the selectiveModeConfiguration field."];
      selectiveModeConfiguration:
        RerankingMetadataSelectiveModeConfiguration.t option
        [@ocaml.doc
          "Contains configurations for the metadata fields to include or exclude when considering reranking."]}
    let context_ = "MetadataConfigurationForReranking"
    let make ?selectiveModeConfiguration =
      fun ~selectionMode ->
        fun () -> { selectiveModeConfiguration; selectionMode }
    let to_value x =
      structure_to_value
        [("selectionMode",
           (Some (RerankingMetadataSelectionMode.to_value x.selectionMode)));
        ("selectiveModeConfiguration",
          (Option.map x.selectiveModeConfiguration
             ~f:RerankingMetadataSelectiveModeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let selectiveModeConfiguration =
        (Option.map ~f:RerankingMetadataSelectiveModeConfiguration.of_xml)
          (Xml.child xml_arg0 "selectiveModeConfiguration") in
      let selectionMode =
        RerankingMetadataSelectionMode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "selectionMode") in
      make ?selectiveModeConfiguration ~selectionMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let selectiveModeConfiguration =
        field_map json__ "selectiveModeConfiguration"
          RerankingMetadataSelectiveModeConfiguration.of_json in
      let selectionMode =
        field_map_exn json__ "selectionMode"
          RerankingMetadataSelectionMode.of_json in
      make ?selectiveModeConfiguration ~selectionMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for the metadata to use in reranking."]
module VectorSearchBedrockRerankingConfiguration =
  struct
    type nonrec t =
      {
      metadataConfiguration: MetadataConfigurationForReranking.t option
        [@ocaml.doc
          "Contains configurations for the metadata to use in reranking."];
      modelConfiguration: VectorSearchBedrockRerankingModelConfiguration.t
        [@ocaml.doc "Contains configurations for the reranker model."];
      numberOfRerankedResults:
        VectorSearchBedrockRerankingConfigurationNumberOfRerankedResultsInteger.t
          option
        [@ocaml.doc "The number of results to return after reranking."]}
    let context_ = "VectorSearchBedrockRerankingConfiguration"
    let make ?metadataConfiguration =
      fun ?numberOfRerankedResults ->
        fun ~modelConfiguration ->
          fun () ->
            {
              metadataConfiguration;
              numberOfRerankedResults;
              modelConfiguration
            }
    let to_value x =
      structure_to_value
        [("metadataConfiguration",
           (Option.map x.metadataConfiguration
              ~f:MetadataConfigurationForReranking.to_value));
        ("modelConfiguration",
          (Some
             (VectorSearchBedrockRerankingModelConfiguration.to_value
                x.modelConfiguration)));
        ("numberOfRerankedResults",
          (Option.map x.numberOfRerankedResults
             ~f:VectorSearchBedrockRerankingConfigurationNumberOfRerankedResultsInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let numberOfRerankedResults =
        (Option.map
           ~f:VectorSearchBedrockRerankingConfigurationNumberOfRerankedResultsInteger.of_xml)
          (Xml.child xml_arg0 "numberOfRerankedResults") in
      let modelConfiguration =
        VectorSearchBedrockRerankingModelConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelConfiguration") in
      let metadataConfiguration =
        (Option.map ~f:MetadataConfigurationForReranking.of_xml)
          (Xml.child xml_arg0 "metadataConfiguration") in
      make ?numberOfRerankedResults ~modelConfiguration
        ?metadataConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let numberOfRerankedResults =
        field_map json__ "numberOfRerankedResults"
          VectorSearchBedrockRerankingConfigurationNumberOfRerankedResultsInteger.of_json in
      let modelConfiguration =
        field_map_exn json__ "modelConfiguration"
          VectorSearchBedrockRerankingModelConfiguration.of_json in
      let metadataConfiguration =
        field_map json__ "metadataConfiguration"
          MetadataConfigurationForReranking.of_json in
      make ?numberOfRerankedResults ~modelConfiguration
        ?metadataConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for reranking with an Amazon Bedrock reranker model."]
module VectorSearchRerankingConfiguration =
  struct
    type nonrec t =
      {
      bedrockRerankingConfiguration:
        VectorSearchBedrockRerankingConfiguration.t option
        [@ocaml.doc
          "Contains configurations for an Amazon Bedrock reranker model."];
      type_: VectorSearchRerankingConfigurationType.t
        [@ocaml.doc "The type of reranker model."]}
    let context_ = "VectorSearchRerankingConfiguration"
    let make ?bedrockRerankingConfiguration =
      fun ~type_ -> fun () -> { bedrockRerankingConfiguration; type_ }
    let to_value x =
      structure_to_value
        [("bedrockRerankingConfiguration",
           (Option.map x.bedrockRerankingConfiguration
              ~f:VectorSearchBedrockRerankingConfiguration.to_value));
        ("type",
          (Some (VectorSearchRerankingConfigurationType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        VectorSearchRerankingConfigurationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let bedrockRerankingConfiguration =
        (Option.map ~f:VectorSearchBedrockRerankingConfiguration.of_xml)
          (Xml.child xml_arg0 "bedrockRerankingConfiguration") in
      make ~type_ ?bedrockRerankingConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ =
        field_map_exn json__ "type"
          VectorSearchRerankingConfigurationType.of_json in
      let bedrockRerankingConfiguration =
        field_map json__ "bedrockRerankingConfiguration"
          VectorSearchBedrockRerankingConfiguration.of_json in
      make ~type_ ?bedrockRerankingConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for reranking the retrieved results."]
module SearchType =
  struct
    type nonrec t =
      | HYBRID 
      | SEMANTIC 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HYBRID -> "HYBRID"
      | SEMANTIC -> "SEMANTIC"
      | Non_static_id s -> s
    let of_string =
      function
      | "HYBRID" -> HYBRID
      | "SEMANTIC" -> SEMANTIC
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SearchType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SearchType" j)
    let to_json = simple_to_json to_value
  end
module FilterValue =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module FilterKey =
  struct
    type nonrec t = string
    let context_ = "FilterKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FilterKey" j
    let to_json = simple_to_json to_value
  end
module FilterAttribute =
  struct
    type nonrec t =
      {
      key: FilterKey.t
        [@ocaml.doc "The name that the metadata attribute must match."];
      value: FilterValue.t
        [@ocaml.doc
          "The value to whcih to compare the value of the metadata attribute."]}
    let context_ = "FilterAttribute"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("key", (Some (FilterKey.to_value x.key)));
        ("value", (Some (FilterValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        FilterValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let key =
        FilterKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "key") in
      make ~value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map_exn json__ "value" FilterValue.of_json in
      let key = field_map_exn json__ "key" FilterKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the name that the metadata attribute must match and the value to which to compare the value of the metadata attribute. For more information, see Query configurations. This data type is used in the following API operations: RetrieveAndGenerate request"]
module rec
  RetrievalFilter:sig
                    type nonrec t =
                      {
                      andAll: RetrievalFilterList.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if their metadata attributes fulfill all the filter conditions inside this list."];
                      equals: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value matches the value in this object. The following example would return data sources with an animal attribute whose value is cat: \"equals\": \\{ \"key\": \"animal\", \"value\": \"cat\" \\}"];
                      greaterThan: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is greater than the value in this object. The following example would return data sources with an year attribute whose value is greater than 1989: \"greaterThan\": \\{ \"key\": \"year\", \"value\": 1989 \\}"];
                      greaterThanOrEquals: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is greater than or equal to the value in this object. The following example would return data sources with an year attribute whose value is greater than or equal to 1989: \"greaterThanOrEquals\": \\{ \"key\": \"year\", \"value\": 1989 \\}"];
                      in_: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is in the list specified in the value in this object. The following example would return data sources with an animal attribute that is either cat or dog: \"in\": \\{ \"key\": \"animal\", \"value\": \\[\"cat\", \"dog\"\\] \\}"];
                      lessThan: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is less than the value in this object. The following example would return data sources with an year attribute whose value is less than to 1989. \"lessThan\": \\{ \"key\": \"year\", \"value\": 1989 \\}"];
                      lessThanOrEquals: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is less than or equal to the value in this object. The following example would return data sources with an year attribute whose value is less than or equal to 1989. \"lessThanOrEquals\": \\{ \"key\": \"year\", \"value\": 1989 \\}"];
                      listContains: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is a list that contains the value as one of its members. The following example would return data sources with an animals attribute that is a list containing a cat member (for example \\[\"dog\", \"cat\"\\]). \"listContains\": \\{ \"key\": \"animals\", \"value\": \"cat\" \\}"];
                      notEquals: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned when: It contains a metadata attribute whose name matches the key and whose value doesn't match the value in this object. The key is not present in the document. The following example would return data sources that don't contain an animal attribute whose value is cat. \"notEquals\": \\{ \"key\": \"animal\", \"value\": \"cat\" \\}"];
                      notIn: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value isn't in the list specified in the value in this object. The following example would return data sources whose animal attribute is neither cat nor dog. \"notIn\": \\{ \"key\": \"animal\", \"value\": \\[\"cat\", \"dog\"\\] \\}"];
                      orAll: RetrievalFilterList.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if their metadata attributes fulfill at least one of the filter conditions inside this list."];
                      startsWith: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value starts with the value in this object. This filter is currently only supported for Amazon OpenSearch Serverless vector stores. The following example would return data sources with an animal attribute starts with ca (for example, cat or camel). \"startsWith\": \\{ \"key\": \"animal\", \"value\": \"ca\" \\}"];
                      stringContains: FilterAttribute.t option
                        [@ocaml.doc
                          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is one of the following: A string that contains the value as a substring. The following example would return data sources with an animal attribute that contains the substring at (for example cat). \"stringContains\": \\{ \"key\": \"animal\", \"value\": \"at\" \\} A list with a member that contains the value as a substring. The following example would return data sources with an animals attribute that is a list containing a member that contains the substring at (for example \\[\"dog\", \"cat\"\\]). \"stringContains\": \\{ \"key\": \"animals\", \"value\": \"at\" \\}"]}
                    val make :
                      ?andAll:RetrievalFilterList.t ->
                        ?equals:FilterAttribute.t ->
                          ?greaterThan:FilterAttribute.t ->
                            ?greaterThanOrEquals:FilterAttribute.t ->
                              ?in_:FilterAttribute.t ->
                                ?lessThan:FilterAttribute.t ->
                                  ?lessThanOrEquals:FilterAttribute.t ->
                                    ?listContains:FilterAttribute.t ->
                                      ?notEquals:FilterAttribute.t ->
                                        ?notIn:FilterAttribute.t ->
                                          ?orAll:RetrievalFilterList.t ->
                                            ?startsWith:FilterAttribute.t ->
                                              ?stringContains:FilterAttribute.t
                                                -> unit -> t
                    val to_value : t -> Botodata.value
                    val to_query : t -> Client.Query.t
                    val of_xml : Xml.t -> t
                    val of_json : Yojson.Safe.t -> t
                    val to_json : t -> Yojson.Safe.t
                  end =
  struct
    type nonrec t =
      {
      andAll: RetrievalFilterList.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if their metadata attributes fulfill all the filter conditions inside this list."];
      equals: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value matches the value in this object. The following example would return data sources with an animal attribute whose value is cat: \"equals\": \\{ \"key\": \"animal\", \"value\": \"cat\" \\}"];
      greaterThan: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is greater than the value in this object. The following example would return data sources with an year attribute whose value is greater than 1989: \"greaterThan\": \\{ \"key\": \"year\", \"value\": 1989 \\}"];
      greaterThanOrEquals: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is greater than or equal to the value in this object. The following example would return data sources with an year attribute whose value is greater than or equal to 1989: \"greaterThanOrEquals\": \\{ \"key\": \"year\", \"value\": 1989 \\}"];
      in_: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is in the list specified in the value in this object. The following example would return data sources with an animal attribute that is either cat or dog: \"in\": \\{ \"key\": \"animal\", \"value\": \\[\"cat\", \"dog\"\\] \\}"];
      lessThan: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is less than the value in this object. The following example would return data sources with an year attribute whose value is less than to 1989. \"lessThan\": \\{ \"key\": \"year\", \"value\": 1989 \\}"];
      lessThanOrEquals: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is less than or equal to the value in this object. The following example would return data sources with an year attribute whose value is less than or equal to 1989. \"lessThanOrEquals\": \\{ \"key\": \"year\", \"value\": 1989 \\}"];
      listContains: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is a list that contains the value as one of its members. The following example would return data sources with an animals attribute that is a list containing a cat member (for example \\[\"dog\", \"cat\"\\]). \"listContains\": \\{ \"key\": \"animals\", \"value\": \"cat\" \\}"];
      notEquals: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned when: It contains a metadata attribute whose name matches the key and whose value doesn't match the value in this object. The key is not present in the document. The following example would return data sources that don't contain an animal attribute whose value is cat. \"notEquals\": \\{ \"key\": \"animal\", \"value\": \"cat\" \\}"];
      notIn: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value isn't in the list specified in the value in this object. The following example would return data sources whose animal attribute is neither cat nor dog. \"notIn\": \\{ \"key\": \"animal\", \"value\": \\[\"cat\", \"dog\"\\] \\}"];
      orAll: RetrievalFilterList.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if their metadata attributes fulfill at least one of the filter conditions inside this list."];
      startsWith: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value starts with the value in this object. This filter is currently only supported for Amazon OpenSearch Serverless vector stores. The following example would return data sources with an animal attribute starts with ca (for example, cat or camel). \"startsWith\": \\{ \"key\": \"animal\", \"value\": \"ca\" \\}"];
      stringContains: FilterAttribute.t option
        [@ocaml.doc
          "Knowledge base data sources are returned if they contain a metadata attribute whose name matches the key and whose value is one of the following: A string that contains the value as a substring. The following example would return data sources with an animal attribute that contains the substring at (for example cat). \"stringContains\": \\{ \"key\": \"animal\", \"value\": \"at\" \\} A list with a member that contains the value as a substring. The following example would return data sources with an animals attribute that is a list containing a member that contains the substring at (for example \\[\"dog\", \"cat\"\\]). \"stringContains\": \\{ \"key\": \"animals\", \"value\": \"at\" \\}"]}
    let make ?andAll =
      fun ?equals ->
        fun ?greaterThan ->
          fun ?greaterThanOrEquals ->
            fun ?in_ ->
              fun ?lessThan ->
                fun ?lessThanOrEquals ->
                  fun ?listContains ->
                    fun ?notEquals ->
                      fun ?notIn ->
                        fun ?orAll ->
                          fun ?startsWith ->
                            fun ?stringContains ->
                              fun () ->
                                {
                                  andAll;
                                  equals;
                                  greaterThan;
                                  greaterThanOrEquals;
                                  in_;
                                  lessThan;
                                  lessThanOrEquals;
                                  listContains;
                                  notEquals;
                                  notIn;
                                  orAll;
                                  startsWith;
                                  stringContains
                                }
    let to_value x =
      structure_to_value
        [("andAll", (Option.map x.andAll ~f:RetrievalFilterList.to_value));
        ("equals", (Option.map x.equals ~f:FilterAttribute.to_value));
        ("greaterThan",
          (Option.map x.greaterThan ~f:FilterAttribute.to_value));
        ("greaterThanOrEquals",
          (Option.map x.greaterThanOrEquals ~f:FilterAttribute.to_value));
        ("in", (Option.map x.in_ ~f:FilterAttribute.to_value));
        ("lessThan", (Option.map x.lessThan ~f:FilterAttribute.to_value));
        ("lessThanOrEquals",
          (Option.map x.lessThanOrEquals ~f:FilterAttribute.to_value));
        ("listContains",
          (Option.map x.listContains ~f:FilterAttribute.to_value));
        ("notEquals", (Option.map x.notEquals ~f:FilterAttribute.to_value));
        ("notIn", (Option.map x.notIn ~f:FilterAttribute.to_value));
        ("orAll", (Option.map x.orAll ~f:RetrievalFilterList.to_value));
        ("startsWith", (Option.map x.startsWith ~f:FilterAttribute.to_value));
        ("stringContains",
          (Option.map x.stringContains ~f:FilterAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stringContains =
        (Option.map ~f:FilterAttribute.of_xml)
          (Xml.child xml_arg0 "stringContains") in
      let startsWith =
        (Option.map ~f:FilterAttribute.of_xml)
          (Xml.child xml_arg0 "startsWith") in
      let orAll =
        (Option.map ~f:RetrievalFilterList.of_xml)
          (Xml.child xml_arg0 "orAll") in
      let notIn =
        (Option.map ~f:FilterAttribute.of_xml) (Xml.child xml_arg0 "notIn") in
      let notEquals =
        (Option.map ~f:FilterAttribute.of_xml)
          (Xml.child xml_arg0 "notEquals") in
      let listContains =
        (Option.map ~f:FilterAttribute.of_xml)
          (Xml.child xml_arg0 "listContains") in
      let lessThanOrEquals =
        (Option.map ~f:FilterAttribute.of_xml)
          (Xml.child xml_arg0 "lessThanOrEquals") in
      let lessThan =
        (Option.map ~f:FilterAttribute.of_xml)
          (Xml.child xml_arg0 "lessThan") in
      let in_ =
        (Option.map ~f:FilterAttribute.of_xml) (Xml.child xml_arg0 "in") in
      let greaterThanOrEquals =
        (Option.map ~f:FilterAttribute.of_xml)
          (Xml.child xml_arg0 "greaterThanOrEquals") in
      let greaterThan =
        (Option.map ~f:FilterAttribute.of_xml)
          (Xml.child xml_arg0 "greaterThan") in
      let equals =
        (Option.map ~f:FilterAttribute.of_xml) (Xml.child xml_arg0 "equals") in
      let andAll =
        (Option.map ~f:RetrievalFilterList.of_xml)
          (Xml.child xml_arg0 "andAll") in
      make ?stringContains ?startsWith ?orAll ?notIn ?notEquals ?listContains
        ?lessThanOrEquals ?lessThan ?in_ ?greaterThanOrEquals ?greaterThan
        ?equals ?andAll ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stringContains =
        field_map json__ "stringContains" FilterAttribute.of_json in
      let startsWith = field_map json__ "startsWith" FilterAttribute.of_json in
      let orAll = field_map json__ "orAll" RetrievalFilterList.of_json in
      let notIn = field_map json__ "notIn" FilterAttribute.of_json in
      let notEquals = field_map json__ "notEquals" FilterAttribute.of_json in
      let listContains =
        field_map json__ "listContains" FilterAttribute.of_json in
      let lessThanOrEquals =
        field_map json__ "lessThanOrEquals" FilterAttribute.of_json in
      let lessThan = field_map json__ "lessThan" FilterAttribute.of_json in
      let in_ = field_map json__ "in" FilterAttribute.of_json in
      let greaterThanOrEquals =
        field_map json__ "greaterThanOrEquals" FilterAttribute.of_json in
      let greaterThan =
        field_map json__ "greaterThan" FilterAttribute.of_json in
      let equals = field_map json__ "equals" FilterAttribute.of_json in
      let andAll = field_map json__ "andAll" RetrievalFilterList.of_json in
      make ?stringContains ?startsWith ?orAll ?notIn ?notEquals ?listContains
        ?lessThanOrEquals ?lessThan ?in_ ?greaterThanOrEquals ?greaterThan
        ?equals ?andAll ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the filters to use on the metadata attributes in the knowledge base data sources before returning results. For more information, see Query configurations. See the examples below to see how to use these filters. This data type is used in the following API operations: Retrieve request \226\128\147 in the filter field RetrieveAndGenerate request \226\128\147 in the filter field"]
 and
  RetrievalFilterList:sig
                        type nonrec t = RetrievalFilter.t list
                        val make : RetrievalFilter.t list -> t
                        val to_value : t -> Botodata.value
                        val to_query : t -> Client.Query.t
                        val of_xml : Xml.t -> RetrievalFilter.t list
                        val of_json : Yojson.Safe.t -> t
                        val to_json : t -> Yojson.Safe.t
                        val to_header : t -> string
                      end =
  struct
    type nonrec t = RetrievalFilter.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:2); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RetrievalFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RetrievalFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"RetrievalFilterList"
        ~of_json:RetrievalFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module KnowledgeBaseVectorSearchConfigurationNumberOfResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for KnowledgeBaseVectorSearchConfigurationNumberOfResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MetadataAttributeSchemaKeyString =
  struct
    type nonrec t = string
    let context_ = "MetadataAttributeSchemaKeyString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\s\\S]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetadataAttributeSchemaKeyString" j
    let to_json = simple_to_json to_value
  end
module MetadataAttributeSchemaDescriptionString =
  struct
    type nonrec t = string
    let context_ = "MetadataAttributeSchemaDescriptionString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\s\\S]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"MetadataAttributeSchemaDescriptionString" j
    let to_json = simple_to_json to_value
  end
module MetadataAttributeSchema =
  struct
    type nonrec t =
      {
      description: MetadataAttributeSchemaDescriptionString.t
        [@ocaml.doc "The attribute's description."];
      key: MetadataAttributeSchemaKeyString.t
        [@ocaml.doc "The attribute's key."];
      type_: AttributeType.t [@ocaml.doc "The attribute's type."]}
    let context_ = "MetadataAttributeSchema"
    let make ~description =
      fun ~key -> fun ~type_ -> fun () -> { description; key; type_ }
    let to_value x =
      structure_to_value
        [("description",
           (Some
              (MetadataAttributeSchemaDescriptionString.to_value
                 x.description)));
        ("key", (Some (MetadataAttributeSchemaKeyString.to_value x.key)));
        ("type", (Some (AttributeType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        AttributeType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let key =
        MetadataAttributeSchemaKeyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "key") in
      let description =
        MetadataAttributeSchemaDescriptionString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "description") in
      make ~type_ ~key ~description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" AttributeType.of_json in
      let key =
        field_map_exn json__ "key" MetadataAttributeSchemaKeyString.of_json in
      let description =
        field_map_exn json__ "description"
          MetadataAttributeSchemaDescriptionString.of_json in
      make ~type_ ~key ~description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details about a metadata attribute."]
module MetadataAttributeSchemaList =
  struct
    type nonrec t = MetadataAttributeSchema.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MetadataAttributeSchema.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetadataAttributeSchema.of_xml)
    let of_json j =
      list_of_json ~kind:"MetadataAttributeSchemaList"
        ~of_json:MetadataAttributeSchema.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImplicitFilterConfiguration =
  struct
    type nonrec t =
      {
      metadataAttributes: MetadataAttributeSchemaList.t
        [@ocaml.doc "Metadata that can be used in a filter."];
      modelArn: BedrockModelArn.t
        [@ocaml.doc "The model that generates the filter."]}
    let context_ = "ImplicitFilterConfiguration"
    let make ~metadataAttributes =
      fun ~modelArn -> fun () -> { metadataAttributes; modelArn }
    let to_value x =
      structure_to_value
        [("metadataAttributes",
           (Some (MetadataAttributeSchemaList.to_value x.metadataAttributes)));
        ("modelArn", (Some (BedrockModelArn.to_value x.modelArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modelArn =
        BedrockModelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelArn") in
      let metadataAttributes =
        MetadataAttributeSchemaList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "metadataAttributes") in
      make ~modelArn ~metadataAttributes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let modelArn = field_map_exn json__ "modelArn" BedrockModelArn.of_json in
      let metadataAttributes =
        field_map_exn json__ "metadataAttributes"
          MetadataAttributeSchemaList.of_json in
      make ~modelArn ~metadataAttributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Settings for implicit filtering, where a model generates a metadata filter based on the prompt."]
module KnowledgeBaseVectorSearchConfiguration =
  struct
    type nonrec t =
      {
      filter: RetrievalFilter.t option
        [@ocaml.doc
          "Specifies the filters to use on the metadata in the knowledge base data sources before returning results. For more information, see Query configurations."];
      implicitFilterConfiguration: ImplicitFilterConfiguration.t option
        [@ocaml.doc "Settings for implicit filtering."];
      numberOfResults:
        KnowledgeBaseVectorSearchConfigurationNumberOfResultsInteger.t option
        [@ocaml.doc "The number of source chunks to retrieve."];
      overrideSearchType: SearchType.t option
        [@ocaml.doc
          "By default, Amazon Bedrock decides a search strategy for you. If you're using an Amazon OpenSearch Serverless vector store that contains a filterable text field, you can specify whether to query the knowledge base with a HYBRID search using both vector embeddings and raw text, or SEMANTIC search using only vector embeddings. For other vector store configurations, only SEMANTIC search is available. For more information, see Test a knowledge base."];
      rerankingConfiguration: VectorSearchRerankingConfiguration.t option
        [@ocaml.doc
          "Contains configurations for reranking the retrieved results. For more information, see Improve the relevance of query responses with a reranker model."]}
    let make ?filter =
      fun ?implicitFilterConfiguration ->
        fun ?numberOfResults ->
          fun ?overrideSearchType ->
            fun ?rerankingConfiguration ->
              fun () ->
                {
                  filter;
                  implicitFilterConfiguration;
                  numberOfResults;
                  overrideSearchType;
                  rerankingConfiguration
                }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:RetrievalFilter.to_value));
        ("implicitFilterConfiguration",
          (Option.map x.implicitFilterConfiguration
             ~f:ImplicitFilterConfiguration.to_value));
        ("numberOfResults",
          (Option.map x.numberOfResults
             ~f:KnowledgeBaseVectorSearchConfigurationNumberOfResultsInteger.to_value));
        ("overrideSearchType",
          (Option.map x.overrideSearchType ~f:SearchType.to_value));
        ("rerankingConfiguration",
          (Option.map x.rerankingConfiguration
             ~f:VectorSearchRerankingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rerankingConfiguration =
        (Option.map ~f:VectorSearchRerankingConfiguration.of_xml)
          (Xml.child xml_arg0 "rerankingConfiguration") in
      let overrideSearchType =
        (Option.map ~f:SearchType.of_xml)
          (Xml.child xml_arg0 "overrideSearchType") in
      let numberOfResults =
        (Option.map
           ~f:KnowledgeBaseVectorSearchConfigurationNumberOfResultsInteger.of_xml)
          (Xml.child xml_arg0 "numberOfResults") in
      let implicitFilterConfiguration =
        (Option.map ~f:ImplicitFilterConfiguration.of_xml)
          (Xml.child xml_arg0 "implicitFilterConfiguration") in
      let filter =
        (Option.map ~f:RetrievalFilter.of_xml) (Xml.child xml_arg0 "filter") in
      make ?rerankingConfiguration ?overrideSearchType ?numberOfResults
        ?implicitFilterConfiguration ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rerankingConfiguration =
        field_map json__ "rerankingConfiguration"
          VectorSearchRerankingConfiguration.of_json in
      let overrideSearchType =
        field_map json__ "overrideSearchType" SearchType.of_json in
      let numberOfResults =
        field_map json__ "numberOfResults"
          KnowledgeBaseVectorSearchConfigurationNumberOfResultsInteger.of_json in
      let implicitFilterConfiguration =
        field_map json__ "implicitFilterConfiguration"
          ImplicitFilterConfiguration.of_json in
      let filter = field_map json__ "filter" RetrievalFilter.of_json in
      make ?rerankingConfiguration ?overrideSearchType ?numberOfResults
        ?implicitFilterConfiguration ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configurations for how to perform the search query and return results. For more information, see Query configurations. This data type is used in the following API operations: Retrieve request \226\128\147 in the vectorSearchConfiguration field RetrieveAndGenerate request \226\128\147 in the vectorSearchConfiguration field"]
module KnowledgeBaseRetrievalConfiguration =
  struct
    type nonrec t =
      {
      vectorSearchConfiguration: KnowledgeBaseVectorSearchConfiguration.t
        [@ocaml.doc
          "Contains details about how the results from the vector search should be returned. For more information, see Query configurations."]}
    let context_ = "KnowledgeBaseRetrievalConfiguration"
    let make ~vectorSearchConfiguration =
      fun () -> { vectorSearchConfiguration }
    let to_value x =
      structure_to_value
        [("vectorSearchConfiguration",
           (Some
              (KnowledgeBaseVectorSearchConfiguration.to_value
                 x.vectorSearchConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vectorSearchConfiguration =
        KnowledgeBaseVectorSearchConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "vectorSearchConfiguration") in
      make ~vectorSearchConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let vectorSearchConfiguration =
        field_map_exn json__ "vectorSearchConfiguration"
          KnowledgeBaseVectorSearchConfiguration.of_json in
      make ~vectorSearchConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for knowledge base query. For more information, see Query configurations. This data type is used in the following API operations: Retrieve request \226\128\147 in the retrievalConfiguration field RetrieveAndGenerate request \226\128\147 in the retrievalConfiguration field"]
module KnowledgeBaseId =
  struct
    type nonrec t = string
    let context_ = "KnowledgeBaseId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:10) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9a-zA-Z]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KnowledgeBaseId" j
    let to_json = simple_to_json to_value
  end
module KnowledgeBase =
  struct
    type nonrec t =
      {
      description: ResourceDescription.t
        [@ocaml.doc
          "The description of the knowledge base associated with the inline agent."];
      knowledgeBaseId: KnowledgeBaseId.t
        [@ocaml.doc
          "The unique identifier for a knowledge base associated with the inline agent."];
      retrievalConfiguration: KnowledgeBaseRetrievalConfiguration.t option
        [@ocaml.doc
          "The configurations to apply to the knowledge base during query. For more information, see Query configurations."]}
    let context_ = "KnowledgeBase"
    let make ?retrievalConfiguration =
      fun ~description ->
        fun ~knowledgeBaseId ->
          fun () -> { retrievalConfiguration; description; knowledgeBaseId }
    let to_value x =
      structure_to_value
        [("description", (Some (ResourceDescription.to_value x.description)));
        ("knowledgeBaseId",
          (Some (KnowledgeBaseId.to_value x.knowledgeBaseId)));
        ("retrievalConfiguration",
          (Option.map x.retrievalConfiguration
             ~f:KnowledgeBaseRetrievalConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retrievalConfiguration =
        (Option.map ~f:KnowledgeBaseRetrievalConfiguration.of_xml)
          (Xml.child xml_arg0 "retrievalConfiguration") in
      let knowledgeBaseId =
        KnowledgeBaseId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "knowledgeBaseId") in
      let description =
        ResourceDescription.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "description") in
      make ?retrievalConfiguration ~knowledgeBaseId ~description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retrievalConfiguration =
        field_map json__ "retrievalConfiguration"
          KnowledgeBaseRetrievalConfiguration.of_json in
      let knowledgeBaseId =
        field_map_exn json__ "knowledgeBaseId" KnowledgeBaseId.of_json in
      let description =
        field_map_exn json__ "description" ResourceDescription.of_json in
      make ?retrievalConfiguration ~knowledgeBaseId ~description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details of the knowledge base associated withe inline agent."]
module KnowledgeBases =
  struct
    type nonrec t = KnowledgeBase.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:KnowledgeBase.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KnowledgeBase.of_xml)
    let of_json j =
      list_of_json ~kind:"KnowledgeBases" ~of_json:KnowledgeBase.of_json j
    let to_json v = composed_to_json to_value v
  end
module KmsKeyArn =
  struct
    type nonrec t = string
    let context_ = "KmsKeyArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws(|-cn|-us-gov):kms:[a-zA-Z0-9-]*:[0-9]{12}:key/[a-zA-Z0-9-]{36}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKeyArn" j
    let to_json = simple_to_json to_value
  end
module Instruction =
  struct
    type nonrec t = string
    let context_ = "Instruction"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:40); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Instruction" j
    let to_json = simple_to_json to_value
  end
module GuardrailVersion =
  struct
    type nonrec t = string
    let context_ = "GuardrailVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:5) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^(([1-9][0-9]{0,7})|(DRAFT))$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GuardrailVersion" j
    let to_json = simple_to_json to_value
  end
module GuardrailIdentifierWithArn =
  struct
    type nonrec t = string
    let context_ = "GuardrailIdentifierWithArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(([a-z0-9]+)|(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:guardrail/[a-z0-9]+))$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GuardrailIdentifierWithArn" j
    let to_json = simple_to_json to_value
  end
module GuardrailConfigurationWithArn =
  struct
    type nonrec t =
      {
      guardrailIdentifier: GuardrailIdentifierWithArn.t
        [@ocaml.doc "The unique identifier for the guardrail."];
      guardrailVersion: GuardrailVersion.t
        [@ocaml.doc "The version of the guardrail."]}
    let context_ = "GuardrailConfigurationWithArn"
    let make ~guardrailIdentifier =
      fun ~guardrailVersion ->
        fun () -> { guardrailIdentifier; guardrailVersion }
    let to_value x =
      structure_to_value
        [("guardrailIdentifier",
           (Some (GuardrailIdentifierWithArn.to_value x.guardrailIdentifier)));
        ("guardrailVersion",
          (Some (GuardrailVersion.to_value x.guardrailVersion)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guardrailVersion =
        GuardrailVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "guardrailVersion") in
      let guardrailIdentifier =
        GuardrailIdentifierWithArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "guardrailIdentifier") in
      make ~guardrailVersion ~guardrailIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let guardrailVersion =
        field_map_exn json__ "guardrailVersion" GuardrailVersion.of_json in
      let guardrailIdentifier =
        field_map_exn json__ "guardrailIdentifier"
          GuardrailIdentifierWithArn.of_json in
      make ~guardrailVersion ~guardrailIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration details for the guardrail."]
module RelayConversationHistory =
  struct
    type nonrec t =
      | TO_COLLABORATOR 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TO_COLLABORATOR -> "TO_COLLABORATOR"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "TO_COLLABORATOR" -> TO_COLLABORATOR
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RelayConversationHistory" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RelayConversationHistory" j)
    let to_json = simple_to_json to_value
  end
module CollaboratorConfiguration =
  struct
    type nonrec t =
      {
      agentAliasArn: AgentAliasArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the inline collaborator agent."];
      collaboratorInstruction: CollaborationInstruction.t
        [@ocaml.doc
          "Instructions that tell the inline collaborator agent what it should do and how it should interact with users."];
      collaboratorName: Name.t
        [@ocaml.doc
          "Name of the inline collaborator agent which must be the same name as specified for agentName."];
      relayConversationHistory: RelayConversationHistory.t option
        [@ocaml.doc
          "A relay conversation history for the inline collaborator agent."]}
    let context_ = "CollaboratorConfiguration"
    let make ?agentAliasArn =
      fun ?relayConversationHistory ->
        fun ~collaboratorInstruction ->
          fun ~collaboratorName ->
            fun () ->
              {
                agentAliasArn;
                relayConversationHistory;
                collaboratorInstruction;
                collaboratorName
              }
    let to_value x =
      structure_to_value
        [("agentAliasArn",
           (Option.map x.agentAliasArn ~f:AgentAliasArn.to_value));
        ("collaboratorInstruction",
          (Some (CollaborationInstruction.to_value x.collaboratorInstruction)));
        ("collaboratorName", (Some (Name.to_value x.collaboratorName)));
        ("relayConversationHistory",
          (Option.map x.relayConversationHistory
             ~f:RelayConversationHistory.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let relayConversationHistory =
        (Option.map ~f:RelayConversationHistory.of_xml)
          (Xml.child xml_arg0 "relayConversationHistory") in
      let collaboratorName =
        Name.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "collaboratorName") in
      let collaboratorInstruction =
        CollaborationInstruction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "collaboratorInstruction") in
      let agentAliasArn =
        (Option.map ~f:AgentAliasArn.of_xml)
          (Xml.child xml_arg0 "agentAliasArn") in
      make ?relayConversationHistory ~collaboratorName
        ~collaboratorInstruction ?agentAliasArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let relayConversationHistory =
        field_map json__ "relayConversationHistory"
          RelayConversationHistory.of_json in
      let collaboratorName =
        field_map_exn json__ "collaboratorName" Name.of_json in
      let collaboratorInstruction =
        field_map_exn json__ "collaboratorInstruction"
          CollaborationInstruction.of_json in
      let agentAliasArn =
        field_map json__ "agentAliasArn" AgentAliasArn.of_json in
      make ?relayConversationHistory ~collaboratorName
        ~collaboratorInstruction ?agentAliasArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Settings of an inline collaborator agent."]
module CollaboratorConfigurations =
  struct
    type nonrec t = CollaboratorConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CollaboratorConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CollaboratorConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"CollaboratorConfigurations"
        ~of_json:CollaboratorConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module Collaborator =
  struct
    type nonrec t =
      {
      actionGroups: AgentActionGroups.t option
        [@ocaml.doc
          "List of action groups with each action group defining tasks the inline collaborator agent needs to carry out."];
      agentCollaboration: AgentCollaboration.t option
        [@ocaml.doc
          "Defines how the inline supervisor agent handles information across multiple collaborator agents to coordinate a final response."];
      agentName: Name.t option
        [@ocaml.doc
          "Name of the inline collaborator agent which must be the same name as specified for collaboratorName."];
      collaboratorConfigurations: CollaboratorConfigurations.t option
        [@ocaml.doc "Settings of the collaborator agent."];
      customerEncryptionKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the AWS KMS key that encrypts the inline collaborator."];
      foundationModel: ModelIdentifier.t
        [@ocaml.doc
          "The foundation model used by the inline collaborator agent."];
      guardrailConfiguration: GuardrailConfigurationWithArn.t option
        [@ocaml.doc
          "Details of the guardwrail associated with the inline collaborator."];
      idleSessionTTLInSeconds: SessionTTL.t option
        [@ocaml.doc
          "The number of seconds for which the Amazon Bedrock keeps information about the user's conversation with the inline collaborator agent. A user interaction remains active for the amount of time specified. If no conversation occurs during this time, the session expires and Amazon Bedrock deletes any data provided before the timeout."];
      instruction: Instruction.t
        [@ocaml.doc
          "Instruction that tell the inline collaborator agent what it should do and how it should interact with users."];
      knowledgeBases: KnowledgeBases.t option
        [@ocaml.doc
          "Knowledge base associated with the inline collaborator agent."];
      promptOverrideConfiguration: PromptOverrideConfiguration.t option
        [@ocaml.doc
          "Contains configurations to override prompt templates in different parts of an inline collaborator sequence. For more information, see Advanced prompts."]}
    let context_ = "Collaborator"
    let make ?actionGroups =
      fun ?agentCollaboration ->
        fun ?agentName ->
          fun ?collaboratorConfigurations ->
            fun ?customerEncryptionKeyArn ->
              fun ?guardrailConfiguration ->
                fun ?idleSessionTTLInSeconds ->
                  fun ?knowledgeBases ->
                    fun ?promptOverrideConfiguration ->
                      fun ~foundationModel ->
                        fun ~instruction ->
                          fun () ->
                            {
                              actionGroups;
                              agentCollaboration;
                              agentName;
                              collaboratorConfigurations;
                              customerEncryptionKeyArn;
                              guardrailConfiguration;
                              idleSessionTTLInSeconds;
                              knowledgeBases;
                              promptOverrideConfiguration;
                              foundationModel;
                              instruction
                            }
    let to_value x =
      structure_to_value
        [("actionGroups",
           (Option.map x.actionGroups ~f:AgentActionGroups.to_value));
        ("agentCollaboration",
          (Option.map x.agentCollaboration ~f:AgentCollaboration.to_value));
        ("agentName", (Option.map x.agentName ~f:Name.to_value));
        ("collaboratorConfigurations",
          (Option.map x.collaboratorConfigurations
             ~f:CollaboratorConfigurations.to_value));
        ("customerEncryptionKeyArn",
          (Option.map x.customerEncryptionKeyArn ~f:KmsKeyArn.to_value));
        ("foundationModel",
          (Some (ModelIdentifier.to_value x.foundationModel)));
        ("guardrailConfiguration",
          (Option.map x.guardrailConfiguration
             ~f:GuardrailConfigurationWithArn.to_value));
        ("idleSessionTTLInSeconds",
          (Option.map x.idleSessionTTLInSeconds ~f:SessionTTL.to_value));
        ("instruction", (Some (Instruction.to_value x.instruction)));
        ("knowledgeBases",
          (Option.map x.knowledgeBases ~f:KnowledgeBases.to_value));
        ("promptOverrideConfiguration",
          (Option.map x.promptOverrideConfiguration
             ~f:PromptOverrideConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let promptOverrideConfiguration =
        (Option.map ~f:PromptOverrideConfiguration.of_xml)
          (Xml.child xml_arg0 "promptOverrideConfiguration") in
      let knowledgeBases =
        (Option.map ~f:KnowledgeBases.of_xml)
          (Xml.child xml_arg0 "knowledgeBases") in
      let instruction =
        Instruction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instruction") in
      let idleSessionTTLInSeconds =
        (Option.map ~f:SessionTTL.of_xml)
          (Xml.child xml_arg0 "idleSessionTTLInSeconds") in
      let guardrailConfiguration =
        (Option.map ~f:GuardrailConfigurationWithArn.of_xml)
          (Xml.child xml_arg0 "guardrailConfiguration") in
      let foundationModel =
        ModelIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "foundationModel") in
      let customerEncryptionKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml)
          (Xml.child xml_arg0 "customerEncryptionKeyArn") in
      let collaboratorConfigurations =
        (Option.map ~f:CollaboratorConfigurations.of_xml)
          (Xml.child xml_arg0 "collaboratorConfigurations") in
      let agentName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "agentName") in
      let agentCollaboration =
        (Option.map ~f:AgentCollaboration.of_xml)
          (Xml.child xml_arg0 "agentCollaboration") in
      let actionGroups =
        (Option.map ~f:AgentActionGroups.of_xml)
          (Xml.child xml_arg0 "actionGroups") in
      make ?promptOverrideConfiguration ?knowledgeBases ~instruction
        ?idleSessionTTLInSeconds ?guardrailConfiguration ~foundationModel
        ?customerEncryptionKeyArn ?collaboratorConfigurations ?agentName
        ?agentCollaboration ?actionGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let promptOverrideConfiguration =
        field_map json__ "promptOverrideConfiguration"
          PromptOverrideConfiguration.of_json in
      let knowledgeBases =
        field_map json__ "knowledgeBases" KnowledgeBases.of_json in
      let instruction =
        field_map_exn json__ "instruction" Instruction.of_json in
      let idleSessionTTLInSeconds =
        field_map json__ "idleSessionTTLInSeconds" SessionTTL.of_json in
      let guardrailConfiguration =
        field_map json__ "guardrailConfiguration"
          GuardrailConfigurationWithArn.of_json in
      let foundationModel =
        field_map_exn json__ "foundationModel" ModelIdentifier.of_json in
      let customerEncryptionKeyArn =
        field_map json__ "customerEncryptionKeyArn" KmsKeyArn.of_json in
      let collaboratorConfigurations =
        field_map json__ "collaboratorConfigurations"
          CollaboratorConfigurations.of_json in
      let agentName = field_map json__ "agentName" Name.of_json in
      let agentCollaboration =
        field_map json__ "agentCollaboration" AgentCollaboration.of_json in
      let actionGroups =
        field_map json__ "actionGroups" AgentActionGroups.of_json in
      make ?promptOverrideConfiguration ?knowledgeBases ~instruction
        ?idleSessionTTLInSeconds ?guardrailConfiguration ~foundationModel
        ?customerEncryptionKeyArn ?collaboratorConfigurations ?agentName
        ?agentCollaboration ?actionGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List of inline collaborators."]
module Collaborators =
  struct
    type nonrec t = Collaborator.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Collaborator.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Collaborator.of_xml)
    let of_json j =
      list_of_json ~kind:"Collaborators" ~of_json:Collaborator.of_json j
    let to_json v = composed_to_json to_value v
  end
module SatisfiedCondition =
  struct
    type nonrec t =
      {
      conditionName: String_.t option
        [@ocaml.doc "The name of the condition that was satisfied."]}
    let make ?conditionName = fun () -> { conditionName }
    let to_value x =
      structure_to_value
        [("conditionName", (Option.map x.conditionName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conditionName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "conditionName") in
      make ?conditionName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let conditionName = field_map json__ "conditionName" String_.of_json in
      make ?conditionName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a condition that was satisfied during a condition node evaluation in a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module SatisfiedConditions =
  struct
    type nonrec t = SatisfiedCondition.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SatisfiedCondition.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SatisfiedCondition.of_xml)
    let of_json j =
      list_of_json ~kind:"SatisfiedConditions"
        ~of_json:SatisfiedCondition.of_json j
    let to_json v = composed_to_json to_value v
  end
module NodeName =
  struct
    type nonrec t = string
    let context_ = "NodeName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z]([_]?[0-9a-zA-Z]){0,99}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NodeName" j
    let to_json = simple_to_json to_value
  end
module DateTimestamp =
  struct
    type nonrec t = string[@@ocaml.doc "Time Stamp."]
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Time Stamp."]
module ConditionResultEvent =
  struct
    type nonrec t =
      {
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the condition node that evaluated the conditions."];
      satisfiedConditions: SatisfiedConditions.t option
        [@ocaml.doc
          "A list of conditions that were satisfied during the evaluation."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The timestamp when the condition evaluation occurred."]}
    let make ?nodeName =
      fun ?satisfiedConditions ->
        fun ?timestamp ->
          fun () -> { nodeName; satisfiedConditions; timestamp }
    let to_value x =
      structure_to_value
        [("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("satisfiedConditions",
          (Option.map x.satisfiedConditions ~f:SatisfiedConditions.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let satisfiedConditions =
        (Option.map ~f:SatisfiedConditions.of_xml)
          (Xml.child xml_arg0 "satisfiedConditions") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      make ?timestamp ?satisfiedConditions ?nodeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let satisfiedConditions =
        field_map json__ "satisfiedConditions" SatisfiedConditions.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      make ?timestamp ?satisfiedConditions ?nodeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a condition evaluation result during a flow execution. This event is generated when a condition node in the flow evaluates its conditions. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module ConflictException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There was a conflict performing an operation. Resolve the conflict and retry your request."]
module ContentBlock =
  struct
    type nonrec t = {
      text: String_.t option [@ocaml.doc "The block's text."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A content block."]
module ContentBlocks =
  struct
    type nonrec t = ContentBlock.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ContentBlock.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ContentBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"ContentBlocks" ~of_json:ContentBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConversationRole =
  struct
    type nonrec t =
      | User 
      | Assistant 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | User -> "user"
      | Assistant -> "assistant"
      | Non_static_id s -> s
    let of_string =
      function
      | "user" -> User
      | "assistant" -> Assistant
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ConversationRole" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConversationRole" j)
    let to_json = simple_to_json to_value
  end
module Message =
  struct
    type nonrec t =
      {
      content: ContentBlocks.t [@ocaml.doc "The message's content."];
      role: ConversationRole.t [@ocaml.doc "The message's role."]}
    let context_ = "Message"
    let make ~content = fun ~role -> fun () -> { content; role }
    let to_value x =
      structure_to_value
        [("content", (Some (ContentBlocks.to_value x.content)));
        ("role", (Some (ConversationRole.to_value x.role)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let role =
        ConversationRole.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "role") in
      let content =
        ContentBlocks.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "content") in
      make ~role ~content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let role = field_map_exn json__ "role" ConversationRole.of_json in
      let content = field_map_exn json__ "content" ContentBlocks.of_json in
      make ~role ~content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details about a message."]
module Messages =
  struct
    type nonrec t = Message.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Message.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Message.of_xml)
    let of_json j = list_of_json ~kind:"Messages" ~of_json:Message.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConversationHistory =
  struct
    type nonrec t =
      {
      messages: Messages.t option [@ocaml.doc "The conversation's messages."]}
    let make ?messages = fun () -> { messages }
    let to_value x =
      structure_to_value
        [("messages", (Option.map x.messages ~f:Messages.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let messages =
        (Option.map ~f:Messages.of_xml) (Xml.child xml_arg0 "messages") in
      make ?messages ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let messages = field_map json__ "messages" Messages.of_json in
      make ?messages ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A conversation history."]
module Uuid =
  struct
    type nonrec t = string
    let context_ = "Uuid"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Uuid" j
    let to_json = simple_to_json to_value
  end
module SessionIdentifier =
  struct
    type nonrec t = string
    let context_ = "SessionIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]+:[0-9]{12}:session/[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12})|([a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12})$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionIdentifier" j
    let to_json = simple_to_json to_value
  end
module InvocationDescription =
  struct
    type nonrec t = string
    let context_ = "InvocationDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InvocationDescription" j
    let to_json = simple_to_json to_value
  end
module CreateInvocationRequest =
  struct
    type nonrec t =
      {
      description: InvocationDescription.t option
        [@ocaml.doc
          "A description for the interactions in the invocation. For example, \"User asking about weather in Seattle\"."];
      invocationId: Uuid.t option
        [@ocaml.doc "A unique identifier for the invocation in UUID format."];
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "The unique identifier for the associated session for the invocation. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."]}
    let context_ = "CreateInvocationRequest"
    let make ?description =
      fun ?invocationId ->
        fun ~sessionIdentifier ->
          fun () -> { description; invocationId; sessionIdentifier }
    let to_value x =
      structure_to_value
        [("description",
           (Option.map x.description ~f:InvocationDescription.to_value));
        ("invocationId", (Option.map x.invocationId ~f:Uuid.to_value));
        ("sessionIdentifier",
          (Some (SessionIdentifier.to_value x.sessionIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      let invocationId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationId") in
      let description =
        (Option.map ~f:InvocationDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      make ~sessionIdentifier ?invocationId ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      let invocationId = field_map json__ "invocationId" Uuid.of_json in
      let description =
        field_map json__ "description" InvocationDescription.of_json in
      make ~sessionIdentifier ?invocationId ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new invocation within a session. An invocation groups the related invocation steps that store the content from a conversation. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions. Related APIs ListInvocations ListSessions GetSession"]
module ValidationException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Input validation failed. Check your request parameters and retry the request."]
module ThrottlingException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The number of requests exceeds the limit. Resubmit your request later."]
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The number of requests exceeds the service quota. Resubmit your request later."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified resource Amazon Resource Name (ARN) was not found. Check the Amazon Resource Name (ARN) and try your request again."]
module InternalServerException =
  struct
    type nonrec t =
      {
      message: NonBlankString.t option ;
      reason: String_.t option
        [@ocaml.doc
          "The reason for the exception. If the reason is BEDROCK_MODEL_INVOCATION_SERVICE_UNAVAILABLE, the model invocation service is unavailable. Retry your request."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value));
        ("reason", (Option.map x.reason ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "reason") in
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason = field_map json__ "reason" String_.of_json in
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An internal server error occurred. Retry your request."]
module CreateInvocationResponse =
  struct
    type nonrec t =
      {
      createdAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the invocation was created."];
      invocationId: Uuid.t option
        [@ocaml.doc "The unique identifier for the invocation."];
      sessionId: Uuid.t option
        [@ocaml.doc
          "The unique identifier for the session associated with the invocation."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?createdAt =
      fun ?invocationId ->
        fun ?sessionId -> fun () -> { createdAt; invocationId; sessionId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("createdAt", (Option.map x.createdAt ~f:DateTimestamp.to_value));
        ("invocationId", (Option.map x.invocationId ~f:Uuid.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let invocationId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationId") in
      let createdAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      make ?sessionId ?invocationId ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let invocationId = field_map json__ "invocationId" Uuid.of_json in
      let createdAt = field_map json__ "createdAt" DateTimestamp.of_json in
      make ?sessionId ?invocationId ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new invocation within a session. An invocation groups the related invocation steps that store the content from a conversation. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions. Related APIs ListInvocations ListSessions GetSession"]
module TagValue =
  struct
    type nonrec t = string[@@ocaml.doc "Value of a tag"]
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9\\s._:/=+@-]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Value of a tag"]
module TagKey =
  struct
    type nonrec t = string[@@ocaml.doc "Key of a tag"]
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[a-zA-Z0-9\\s._:/=+@-]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Key of a tag"]
module TagsMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module SessionMetadataValue =
  struct
    type nonrec t = string
    let context_ = "SessionMetadataValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:5000) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionMetadataValue" j
    let to_json = simple_to_json to_value
  end
module SessionMetadataKey =
  struct
    type nonrec t = string
    let context_ = "SessionMetadataKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionMetadataKey" j
    let to_json = simple_to_json to_value
  end
module SessionMetadataMap =
  struct
    type nonrec t = (SessionMetadataKey.t * SessionMetadataValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((SessionMetadataKey.of_string chopped),
                              (SessionMetadataValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (SessionMetadataKey.to_value x) |>
                    (fun x ->
                       (SessionMetadataValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:SessionMetadataKey.of_string
        ~of_json:SessionMetadataValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateSessionRequest =
  struct
    type nonrec t =
      {
      encryptionKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the KMS key to use to encrypt the session data. The user or role creating the session must have permission to use the key. For more information, see Amazon Bedrock session encryption."];
      sessionMetadata: SessionMetadataMap.t option
        [@ocaml.doc
          "A map of key-value pairs containing attributes to be persisted across the session. For example, the user's ID, their language preference, and the type of device they are using."];
      tags: TagsMap.t option
        [@ocaml.doc
          "Specify the key-value pairs for the tags that you want to attach to the session."]}
    let make ?encryptionKeyArn =
      fun ?sessionMetadata ->
        fun ?tags -> fun () -> { encryptionKeyArn; sessionMetadata; tags }
    let to_value x =
      structure_to_value
        [("encryptionKeyArn",
           (Option.map x.encryptionKeyArn ~f:KmsKeyArn.to_value));
        ("sessionMetadata",
          (Option.map x.sessionMetadata ~f:SessionMetadataMap.to_value));
        ("tags", (Option.map x.tags ~f:TagsMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagsMap.of_xml) (Xml.child xml_arg0 "tags") in
      let sessionMetadata =
        (Option.map ~f:SessionMetadataMap.of_xml)
          (Xml.child xml_arg0 "sessionMetadata") in
      let encryptionKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml)
          (Xml.child xml_arg0 "encryptionKeyArn") in
      make ?tags ?sessionMetadata ?encryptionKeyArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagsMap.of_json in
      let sessionMetadata =
        field_map json__ "sessionMetadata" SessionMetadataMap.of_json in
      let encryptionKeyArn =
        field_map json__ "encryptionKeyArn" KmsKeyArn.of_json in
      make ?tags ?sessionMetadata ?encryptionKeyArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a session to temporarily store conversations for generative AI (GenAI) applications built with open-source frameworks such as LangGraph and LlamaIndex. Sessions enable you to save the state of conversations at checkpoints, with the added security and infrastructure of Amazon Web Services. For more information, see Store and retrieve conversation history and context with Amazon Bedrock sessions. By default, Amazon Bedrock uses Amazon Web Services-managed keys for session encryption, including session metadata, or you can use your own KMS key. For more information, see Amazon Bedrock session encryption. You use a session to store state and conversation history for generative AI applications built with open-source frameworks. For Amazon Bedrock Agents, the service automatically manages conversation context and associates them with the agent-specific sessionId you specify in the InvokeAgent API operation. Related APIs: ListSessions GetSession EndSession DeleteSession"]
module SessionStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | EXPIRED 
      | ENDED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | EXPIRED -> "EXPIRED"
      | ENDED -> "ENDED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "EXPIRED" -> EXPIRED
      | "ENDED" -> ENDED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SessionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SessionStatus" j)
    let to_json = simple_to_json to_value
  end
module SessionArn =
  struct
    type nonrec t = string
    let context_ = "SessionArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^arn:aws(-[^:]+)?:bedrock:[a-z0-9-]+:[0-9]{12}:session/[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SessionArn" j
    let to_json = simple_to_json to_value
  end
module CreateSessionResponse =
  struct
    type nonrec t =
      {
      createdAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the session was created."];
      sessionArn: SessionArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the created session."];
      sessionId: Uuid.t option
        [@ocaml.doc "The unique identifier for the session."];
      sessionStatus: SessionStatus.t option
        [@ocaml.doc "The current status of the session."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?createdAt =
      fun ?sessionArn ->
        fun ?sessionId ->
          fun ?sessionStatus ->
            fun () -> { createdAt; sessionArn; sessionId; sessionStatus }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("createdAt", (Option.map x.createdAt ~f:DateTimestamp.to_value));
        ("sessionArn", (Option.map x.sessionArn ~f:SessionArn.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value));
        ("sessionStatus",
          (Option.map x.sessionStatus ~f:SessionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionStatus =
        (Option.map ~f:SessionStatus.of_xml)
          (Xml.child xml_arg0 "sessionStatus") in
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionArn =
        (Option.map ~f:SessionArn.of_xml) (Xml.child xml_arg0 "sessionArn") in
      let createdAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      make ?sessionStatus ?sessionId ?sessionArn ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionStatus =
        field_map json__ "sessionStatus" SessionStatus.of_json in
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let sessionArn = field_map json__ "sessionArn" SessionArn.of_json in
      let createdAt = field_map json__ "createdAt" DateTimestamp.of_json in
      make ?sessionStatus ?sessionId ?sessionArn ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a session to temporarily store conversations for generative AI (GenAI) applications built with open-source frameworks such as LangGraph and LlamaIndex. Sessions enable you to save the state of conversations at checkpoints, with the added security and infrastructure of Amazon Web Services. For more information, see Store and retrieve conversation history and context with Amazon Bedrock sessions. By default, Amazon Bedrock uses Amazon Web Services-managed keys for session encryption, including session metadata, or you can use your own KMS key. For more information, see Amazon Bedrock session encryption. You use a session to store state and conversation history for generative AI applications built with open-source frameworks. For Amazon Bedrock Agents, the service automatically manages conversation context and associates them with the agent-specific sessionId you specify in the InvokeAgent API operation. Related APIs: ListSessions GetSession EndSession DeleteSession"]
module OrchestrationExecutor =
  struct
    type nonrec t =
      {
      lambda: LambdaArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Lambda function containing the business logic that is carried out upon invoking the action."]}
    let make ?lambda = fun () -> { lambda }
    let to_value x =
      structure_to_value
        [("lambda", (Option.map x.lambda ~f:LambdaArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lambda =
        (Option.map ~f:LambdaArn.of_xml) (Xml.child xml_arg0 "lambda") in
      make ?lambda ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lambda = field_map json__ "lambda" LambdaArn.of_json in
      make ?lambda ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The structure of the executor invoking the actions in custom orchestration."]
module CustomOrchestration =
  struct
    type nonrec t =
      {
      executor: OrchestrationExecutor.t option
        [@ocaml.doc
          "The structure of the executor invoking the actions in custom orchestration."]}
    let make ?executor = fun () -> { executor }
    let to_value x =
      structure_to_value
        [("executor",
           (Option.map x.executor ~f:OrchestrationExecutor.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let executor =
        (Option.map ~f:OrchestrationExecutor.of_xml)
          (Xml.child xml_arg0 "executor") in
      make ?executor ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let executor =
        field_map json__ "executor" OrchestrationExecutor.of_json in
      make ?executor ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details of the custom orchestration configured for the agent."]
module MemoryId =
  struct
    type nonrec t = string
    let context_ = "MemoryId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9a-zA-Z._:-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MemoryId" j
    let to_json = simple_to_json to_value
  end
module DeleteAgentMemoryRequest =
  struct
    type nonrec t =
      {
      agentAliasId: AgentAliasId.t
        [@ocaml.doc "The unique identifier of an alias of an agent."];
      agentId: AgentId.t
        [@ocaml.doc
          "The unique identifier of the agent to which the alias belongs."];
      memoryId: MemoryId.t option
        [@ocaml.doc "The unique identifier of the memory."];
      sessionId: SessionId.t option
        [@ocaml.doc "The unique session identifier of the memory."]}
    let context_ = "DeleteAgentMemoryRequest"
    let make ?memoryId =
      fun ?sessionId ->
        fun ~agentAliasId ->
          fun ~agentId ->
            fun () -> { memoryId; sessionId; agentAliasId; agentId }
    let to_value x =
      structure_to_value
        [("agentAliasId", (Some (AgentAliasId.to_value x.agentAliasId)));
        ("agentId", (Some (AgentId.to_value x.agentId)));
        ("memoryId", (Option.map x.memoryId ~f:MemoryId.to_value));
        ("sessionId", (Option.map x.sessionId ~f:SessionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:SessionId.of_xml) (Xml.child xml_arg0 "sessionId") in
      let memoryId =
        (Option.map ~f:MemoryId.of_xml) (Xml.child xml_arg0 "memoryId") in
      let agentId =
        AgentId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "agentId") in
      let agentAliasId =
        AgentAliasId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agentAliasId") in
      make ?sessionId ?memoryId ~agentId ~agentAliasId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let memoryId = field_map json__ "memoryId" MemoryId.of_json in
      let agentId = field_map_exn json__ "agentId" AgentId.of_json in
      let agentAliasId =
        field_map_exn json__ "agentAliasId" AgentAliasId.of_json in
      make ?sessionId ?memoryId ~agentId ~agentAliasId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes memory from the specified memory identifier."]
module DependencyFailedException =
  struct
    type nonrec t =
      {
      message: NonBlankString.t option ;
      resourceName: NonBlankString.t option
        [@ocaml.doc
          "The name of the dependency that caused the issue, such as Amazon Bedrock, Lambda, or STS."]}
    let make ?message =
      fun ?resourceName -> fun () -> { message; resourceName }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value));
        ("resourceName",
          (Option.map x.resourceName ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:NonBlankString.of_xml)
          (Xml.child xml_arg0 "resourceName") in
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?resourceName ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceName =
        field_map json__ "resourceName" NonBlankString.of_json in
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?resourceName ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There was an issue with a dependency. Check the resource configurations and retry the request."]
module DeleteAgentMemoryResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes memory from the specified memory identifier."]
module DeleteSessionRequest =
  struct
    type nonrec t =
      {
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "The unique identifier for the session to be deleted. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."]}
    let context_ = "DeleteSessionRequest"
    let make ~sessionIdentifier = fun () -> { sessionIdentifier }
    let to_value x =
      structure_to_value
        [("sessionIdentifier",
           (Some (SessionIdentifier.to_value x.sessionIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      make ~sessionIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      make ~sessionIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a session that you ended. You can't delete a session with an ACTIVE status. To delete an active session, you must first end it with the EndSession API operation. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module DeleteSessionResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a session that you ended. You can't delete a session with an ACTIVE status. To delete an active session, you must first end it with the EndSession API operation. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module EndSessionRequest =
  struct
    type nonrec t =
      {
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "The unique identifier for the session to end. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."]}
    let context_ = "EndSessionRequest"
    let make ~sessionIdentifier = fun () -> { sessionIdentifier }
    let to_value x =
      structure_to_value
        [("sessionIdentifier",
           (Some (SessionIdentifier.to_value x.sessionIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      make ~sessionIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      make ~sessionIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Ends the session. After you end a session, you can still access its content but you can\226\128\153t add to it. To delete the session and it's content, you use the DeleteSession API operation. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module EndSessionResponse =
  struct
    type nonrec t =
      {
      sessionArn: SessionArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the session you ended."];
      sessionId: Uuid.t option
        [@ocaml.doc "The unique identifier of the session you ended."];
      sessionStatus: SessionStatus.t option
        [@ocaml.doc "The current status of the session you ended."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sessionArn =
      fun ?sessionId ->
        fun ?sessionStatus ->
          fun () -> { sessionArn; sessionId; sessionStatus }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("sessionArn", (Option.map x.sessionArn ~f:SessionArn.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value));
        ("sessionStatus",
          (Option.map x.sessionStatus ~f:SessionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionStatus =
        (Option.map ~f:SessionStatus.of_xml)
          (Xml.child xml_arg0 "sessionStatus") in
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionArn =
        (Option.map ~f:SessionArn.of_xml) (Xml.child xml_arg0 "sessionArn") in
      make ?sessionStatus ?sessionId ?sessionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionStatus =
        field_map json__ "sessionStatus" SessionStatus.of_json in
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let sessionArn = field_map json__ "sessionArn" SessionArn.of_json in
      make ?sessionStatus ?sessionId ?sessionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Ends the session. After you end a session, you can still access its content but you can\226\128\153t add to it. To delete the session and it's content, you use the DeleteSession API operation. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module S3ObjectDoc =
  struct
    type nonrec t =
      {
      uri: S3Uri.t [@ocaml.doc "The file location of the S3 wrapper object."]}
    let context_ = "S3ObjectDoc"
    let make ~uri = fun () -> { uri }
    let to_value x =
      structure_to_value [("uri", (Some (S3Uri.to_value x.uri)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uri = S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "uri") in
      make ~uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uri = field_map_exn json__ "uri" S3Uri.of_json in make ~uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The unique wrapper object of the document from the S3 location."]
module ExternalSourceType =
  struct
    type nonrec t =
      | S3 
      | BYTE_CONTENT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | S3 -> "S3"
      | BYTE_CONTENT -> "BYTE_CONTENT"
      | Non_static_id s -> s
    let of_string =
      function
      | "S3" -> S3
      | "BYTE_CONTENT" -> BYTE_CONTENT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ExternalSourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExternalSourceType" j)
    let to_json = simple_to_json to_value
  end
module ExternalSource =
  struct
    type nonrec t =
      {
      byteContent: ByteContentDoc.t option
        [@ocaml.doc
          "The identifier, contentType, and data of the external source wrapper object."];
      s3Location: S3ObjectDoc.t option
        [@ocaml.doc "The S3 location of the external source wrapper object."];
      sourceType: ExternalSourceType.t
        [@ocaml.doc "The source type of the external source wrapper object."]}
    let context_ = "ExternalSource"
    let make ?byteContent =
      fun ?s3Location ->
        fun ~sourceType -> fun () -> { byteContent; s3Location; sourceType }
    let to_value x =
      structure_to_value
        [("byteContent",
           (Option.map x.byteContent ~f:ByteContentDoc.to_value));
        ("s3Location", (Option.map x.s3Location ~f:S3ObjectDoc.to_value));
        ("sourceType", (Some (ExternalSourceType.to_value x.sourceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceType =
        ExternalSourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sourceType") in
      let s3Location =
        (Option.map ~f:S3ObjectDoc.of_xml) (Xml.child xml_arg0 "s3Location") in
      let byteContent =
        (Option.map ~f:ByteContentDoc.of_xml)
          (Xml.child xml_arg0 "byteContent") in
      make ~sourceType ?s3Location ?byteContent ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sourceType =
        field_map_exn json__ "sourceType" ExternalSourceType.of_json in
      let s3Location = field_map json__ "s3Location" S3ObjectDoc.of_json in
      let byteContent = field_map json__ "byteContent" ByteContentDoc.of_json in
      make ~sourceType ?s3Location ?byteContent ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The unique external source of the content contained in the wrapper object."]
module ExternalSources =
  struct
    type nonrec t = ExternalSource.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExternalSource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExternalSource.of_xml)
    let of_json j =
      list_of_json ~kind:"ExternalSources" ~of_json:ExternalSource.of_json j
    let to_json v = composed_to_json to_value v
  end
module TextPromptTemplate =
  struct
    type nonrec t = string
    let context_ = "TextPromptTemplate"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:4000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TextPromptTemplate" j
    let to_json = simple_to_json to_value
  end
module PromptTemplate =
  struct
    type nonrec t =
      {
      textPromptTemplate: TextPromptTemplate.t option
        [@ocaml.doc
          "The template for the prompt that's sent to the model for response generation. You can include prompt placeholders, which become replaced before the prompt is sent to the model to provide instructions and context to the model. In addition, you can include XML tags to delineate meaningful sections of the prompt template. For more information, see the following resources: Knowledge base prompt templates Use XML tags with Anthropic Claude models"]}
    let make ?textPromptTemplate = fun () -> { textPromptTemplate }
    let to_value x =
      structure_to_value
        [("textPromptTemplate",
           (Option.map x.textPromptTemplate ~f:TextPromptTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let textPromptTemplate =
        (Option.map ~f:TextPromptTemplate.of_xml)
          (Xml.child xml_arg0 "textPromptTemplate") in
      make ?textPromptTemplate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let textPromptTemplate =
        field_map json__ "textPromptTemplate" TextPromptTemplate.of_json in
      make ?textPromptTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the template for the prompt that's sent to the model for response generation. For more information, see Knowledge base prompt templates. This data type is used in the following API operations: RetrieveAndGenerate request \226\128\147 in the filter field"]
module RAGStopSequencesMemberString =
  struct
    type nonrec t = string
    let context_ = "RAGStopSequencesMemberString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RAGStopSequencesMemberString" j
    let to_json = simple_to_json to_value
  end
module RAGStopSequences =
  struct
    type nonrec t = RAGStopSequencesMemberString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:4) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RAGStopSequencesMemberString.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RAGStopSequencesMemberString.of_xml)
    let of_json j =
      list_of_json ~kind:"RAGStopSequences"
        ~of_json:RAGStopSequencesMemberString.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxTokens =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:65536) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for MaxTokens" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TextInferenceConfig =
  struct
    type nonrec t =
      {
      maxTokens: MaxTokens.t option
        [@ocaml.doc
          "The maximum number of tokens to generate in the output text. Do not use the minimum of 0 or the maximum of 65536. The limit values described here are arbitary values, for actual values consult the limits defined by your specific model."];
      stopSequences: RAGStopSequences.t option
        [@ocaml.doc
          "A list of sequences of characters that, if generated, will cause the model to stop generating further tokens. Do not use a minimum length of 1 or a maximum length of 1000. The limit values described here are arbitary values, for actual values consult the limits defined by your specific model."];
      temperature: Temperature.t option
        [@ocaml.doc
          "Controls the random-ness of text generated by the language model, influencing how much the model sticks to the most predictable next words versus exploring more surprising options. A lower temperature value (e.g. 0.2 or 0.3) makes model outputs more deterministic or predictable, while a higher temperature (e.g. 0.8 or 0.9) makes the outputs more creative or unpredictable."];
      topP: TopP.t option
        [@ocaml.doc
          "A probability distribution threshold which controls what the model considers for the set of possible next tokens. The model will only consider the top p% of the probability distribution when generating the next token."]}
    let make ?maxTokens =
      fun ?stopSequences ->
        fun ?temperature ->
          fun ?topP ->
            fun () -> { maxTokens; stopSequences; temperature; topP }
    let to_value x =
      structure_to_value
        [("maxTokens", (Option.map x.maxTokens ~f:MaxTokens.to_value));
        ("stopSequences",
          (Option.map x.stopSequences ~f:RAGStopSequences.to_value));
        ("temperature", (Option.map x.temperature ~f:Temperature.to_value));
        ("topP", (Option.map x.topP ~f:TopP.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let topP = (Option.map ~f:TopP.of_xml) (Xml.child xml_arg0 "topP") in
      let temperature =
        (Option.map ~f:Temperature.of_xml) (Xml.child xml_arg0 "temperature") in
      let stopSequences =
        (Option.map ~f:RAGStopSequences.of_xml)
          (Xml.child xml_arg0 "stopSequences") in
      let maxTokens =
        (Option.map ~f:MaxTokens.of_xml) (Xml.child xml_arg0 "maxTokens") in
      make ?topP ?temperature ?stopSequences ?maxTokens ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let topP = field_map json__ "topP" TopP.of_json in
      let temperature = field_map json__ "temperature" Temperature.of_json in
      let stopSequences =
        field_map json__ "stopSequences" RAGStopSequences.of_json in
      let maxTokens = field_map json__ "maxTokens" MaxTokens.of_json in
      make ?topP ?temperature ?stopSequences ?maxTokens ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration settings for text generation using a language model via the RetrieveAndGenerate operation. Includes parameters like temperature, top-p, maximum token count, and stop sequences. The valid range of maxTokens depends on the accepted values for your chosen model's inference parameters. To see the inference parameters for your model, see Inference parameters for foundation models."]
module InferenceConfig =
  struct
    type nonrec t =
      {
      textInferenceConfig: TextInferenceConfig.t option
        [@ocaml.doc
          "Configuration settings specific to text generation while generating responses using RetrieveAndGenerate."]}
    let make ?textInferenceConfig = fun () -> { textInferenceConfig }
    let to_value x =
      structure_to_value
        [("textInferenceConfig",
           (Option.map x.textInferenceConfig ~f:TextInferenceConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let textInferenceConfig =
        (Option.map ~f:TextInferenceConfig.of_xml)
          (Xml.child xml_arg0 "textInferenceConfig") in
      make ?textInferenceConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let textInferenceConfig =
        field_map json__ "textInferenceConfig" TextInferenceConfig.of_json in
      make ?textInferenceConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration for inference settings when generating responses using RetrieveAndGenerate."]
module GuardrailConfigurationGuardrailVersionString =
  struct
    type nonrec t = string
    let context_ = "GuardrailConfigurationGuardrailVersionString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:5) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^(([1-9][0-9]{0,7})|(DRAFT))$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"GuardrailConfigurationGuardrailVersionString" j
    let to_json = simple_to_json to_value
  end
module GuardrailConfigurationGuardrailIdString =
  struct
    type nonrec t = string
    let context_ = "GuardrailConfigurationGuardrailIdString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-z0-9]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"GuardrailConfigurationGuardrailIdString" j
    let to_json = simple_to_json to_value
  end
module GuardrailConfiguration =
  struct
    type nonrec t =
      {
      guardrailId: GuardrailConfigurationGuardrailIdString.t
        [@ocaml.doc "The unique identifier for the guardrail."];
      guardrailVersion: GuardrailConfigurationGuardrailVersionString.t
        [@ocaml.doc "The version of the guardrail."]}
    let context_ = "GuardrailConfiguration"
    let make ~guardrailId =
      fun ~guardrailVersion -> fun () -> { guardrailId; guardrailVersion }
    let to_value x =
      structure_to_value
        [("guardrailId",
           (Some
              (GuardrailConfigurationGuardrailIdString.to_value x.guardrailId)));
        ("guardrailVersion",
          (Some
             (GuardrailConfigurationGuardrailVersionString.to_value
                x.guardrailVersion)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let guardrailVersion =
        GuardrailConfigurationGuardrailVersionString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "guardrailVersion") in
      let guardrailId =
        GuardrailConfigurationGuardrailIdString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "guardrailId") in
      make ~guardrailVersion ~guardrailId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let guardrailVersion =
        field_map_exn json__ "guardrailVersion"
          GuardrailConfigurationGuardrailVersionString.of_json in
      let guardrailId =
        field_map_exn json__ "guardrailId"
          GuardrailConfigurationGuardrailIdString.of_json in
      make ~guardrailVersion ~guardrailId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The configuration details for the guardrail."]
module ExternalSourcesGenerationConfiguration =
  struct
    type nonrec t =
      {
      additionalModelRequestFields: AdditionalModelRequestFields.t option
        [@ocaml.doc
          "Additional model parameters and their corresponding values not included in the textInferenceConfig structure for an external source. Takes in custom model parameters specific to the language model being used."];
      guardrailConfiguration: GuardrailConfiguration.t option
        [@ocaml.doc "The configuration details for the guardrail."];
      inferenceConfig: InferenceConfig.t option
        [@ocaml.doc
          "Configuration settings for inference when using RetrieveAndGenerate to generate responses while using an external source."];
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "The latency configuration for the model."];
      promptTemplate: PromptTemplate.t option
        [@ocaml.doc
          "Contain the textPromptTemplate string for the external source wrapper object."]}
    let make ?additionalModelRequestFields =
      fun ?guardrailConfiguration ->
        fun ?inferenceConfig ->
          fun ?performanceConfig ->
            fun ?promptTemplate ->
              fun () ->
                {
                  additionalModelRequestFields;
                  guardrailConfiguration;
                  inferenceConfig;
                  performanceConfig;
                  promptTemplate
                }
    let to_value x =
      structure_to_value
        [("additionalModelRequestFields",
           (Option.map x.additionalModelRequestFields
              ~f:AdditionalModelRequestFields.to_value));
        ("guardrailConfiguration",
          (Option.map x.guardrailConfiguration
             ~f:GuardrailConfiguration.to_value));
        ("inferenceConfig",
          (Option.map x.inferenceConfig ~f:InferenceConfig.to_value));
        ("performanceConfig",
          (Option.map x.performanceConfig
             ~f:PerformanceConfiguration.to_value));
        ("promptTemplate",
          (Option.map x.promptTemplate ~f:PromptTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let promptTemplate =
        (Option.map ~f:PromptTemplate.of_xml)
          (Xml.child xml_arg0 "promptTemplate") in
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      let inferenceConfig =
        (Option.map ~f:InferenceConfig.of_xml)
          (Xml.child xml_arg0 "inferenceConfig") in
      let guardrailConfiguration =
        (Option.map ~f:GuardrailConfiguration.of_xml)
          (Xml.child xml_arg0 "guardrailConfiguration") in
      let additionalModelRequestFields =
        (Option.map ~f:AdditionalModelRequestFields.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      make ?promptTemplate ?performanceConfig ?inferenceConfig
        ?guardrailConfiguration ?additionalModelRequestFields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let promptTemplate =
        field_map json__ "promptTemplate" PromptTemplate.of_json in
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      let inferenceConfig =
        field_map json__ "inferenceConfig" InferenceConfig.of_json in
      let guardrailConfiguration =
        field_map json__ "guardrailConfiguration"
          GuardrailConfiguration.of_json in
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields"
          AdditionalModelRequestFields.of_json in
      make ?promptTemplate ?performanceConfig ?inferenceConfig
        ?guardrailConfiguration ?additionalModelRequestFields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the generation configuration of the external source wrapper object."]
module ExternalSourcesRetrieveAndGenerateConfiguration =
  struct
    type nonrec t =
      {
      generationConfiguration:
        ExternalSourcesGenerationConfiguration.t option
        [@ocaml.doc
          "The prompt used with the external source wrapper object with the retrieveAndGenerate function."];
      modelArn: BedrockModelArn.t
        [@ocaml.doc
          "The model Amazon Resource Name (ARN) for the external source wrapper object in the retrieveAndGenerate function."];
      sources: ExternalSources.t
        [@ocaml.doc
          "The document for the external source wrapper object in the retrieveAndGenerate function."]}
    let context_ = "ExternalSourcesRetrieveAndGenerateConfiguration"
    let make ?generationConfiguration =
      fun ~modelArn ->
        fun ~sources ->
          fun () -> { generationConfiguration; modelArn; sources }
    let to_value x =
      structure_to_value
        [("generationConfiguration",
           (Option.map x.generationConfiguration
              ~f:ExternalSourcesGenerationConfiguration.to_value));
        ("modelArn", (Some (BedrockModelArn.to_value x.modelArn)));
        ("sources", (Some (ExternalSources.to_value x.sources)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sources =
        ExternalSources.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sources") in
      let modelArn =
        BedrockModelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelArn") in
      let generationConfiguration =
        (Option.map ~f:ExternalSourcesGenerationConfiguration.of_xml)
          (Xml.child xml_arg0 "generationConfiguration") in
      make ~sources ~modelArn ?generationConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sources = field_map_exn json__ "sources" ExternalSources.of_json in
      let modelArn = field_map_exn json__ "modelArn" BedrockModelArn.of_json in
      let generationConfiguration =
        field_map json__ "generationConfiguration"
          ExternalSourcesGenerationConfiguration.of_json in
      make ~sources ~modelArn ?generationConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configurations of the external source wrapper object in the retrieveAndGenerate function."]
module FileBody =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module OutputFile =
  struct
    type nonrec t =
      {
      bytes: FileBody.t option
        [@ocaml.doc
          "The byte count of files that contains response from code interpreter."];
      name: String_.t option
        [@ocaml.doc
          "The name of the file containing response from code interpreter."];
      type_: MimeType.t option
        [@ocaml.doc
          "The type of file that contains response from the code interpreter."]}
    let make ?bytes =
      fun ?name -> fun ?type_ -> fun () -> { bytes; name; type_ }
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:FileBody.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:MimeType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:MimeType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let bytes =
        (Option.map ~f:FileBody.of_xml) (Xml.child xml_arg0 "bytes") in
      make ?type_ ?name ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" MimeType.of_json in
      let name = field_map json__ "name" String_.of_json in
      let bytes = field_map json__ "bytes" FileBody.of_json in
      make ?type_ ?name ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains details of the response from code interpreter."]
module OutputFiles =
  struct
    type nonrec t = OutputFile.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OutputFile.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OutputFile.of_xml)
    let of_json j =
      list_of_json ~kind:"OutputFiles" ~of_json:OutputFile.of_json j
    let to_json v = composed_to_json to_value v
  end
module FilePart =
  struct
    type nonrec t =
      {
      files: OutputFiles.t option
        [@ocaml.doc "Files containing intermediate response for the user."]}
    let make ?files = fun () -> { files }
    let to_value x =
      structure_to_value
        [("files", (Option.map x.files ~f:OutputFiles.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let files =
        (Option.map ~f:OutputFiles.of_xml) (Xml.child xml_arg0 "files") in
      make ?files ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let files = field_map json__ "files" OutputFiles.of_json in
      make ?files ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains intermediate response for code interpreter if any files have been generated."]
module S3ObjectFile =
  struct
    type nonrec t = {
      uri: S3Uri.t [@ocaml.doc "The uri of the s3 object."]}
    let context_ = "S3ObjectFile"
    let make ~uri = fun () -> { uri }
    let to_value x =
      structure_to_value [("uri", (Some (S3Uri.to_value x.uri)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uri = S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "uri") in
      make ~uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let uri = field_map_exn json__ "uri" S3Uri.of_json in make ~uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details of the s3 object where the source file is located."]
module FileSourceType =
  struct
    type nonrec t =
      | S3 
      | BYTE_CONTENT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | S3 -> "S3"
      | BYTE_CONTENT -> "BYTE_CONTENT"
      | Non_static_id s -> s
    let of_string =
      function
      | "S3" -> S3
      | "BYTE_CONTENT" -> BYTE_CONTENT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FileSourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FileSourceType" j)
    let to_json = simple_to_json to_value
  end
module FileSource =
  struct
    type nonrec t =
      {
      byteContent: ByteContentFile.t option
        [@ocaml.doc "The data and the text of the attached files."];
      s3Location: S3ObjectFile.t option
        [@ocaml.doc "The s3 location of the files to attach."];
      sourceType: FileSourceType.t
        [@ocaml.doc "The source type of the files to attach."]}
    let context_ = "FileSource"
    let make ?byteContent =
      fun ?s3Location ->
        fun ~sourceType -> fun () -> { byteContent; s3Location; sourceType }
    let to_value x =
      structure_to_value
        [("byteContent",
           (Option.map x.byteContent ~f:ByteContentFile.to_value));
        ("s3Location", (Option.map x.s3Location ~f:S3ObjectFile.to_value));
        ("sourceType", (Some (FileSourceType.to_value x.sourceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceType =
        FileSourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sourceType") in
      let s3Location =
        (Option.map ~f:S3ObjectFile.of_xml) (Xml.child xml_arg0 "s3Location") in
      let byteContent =
        (Option.map ~f:ByteContentFile.of_xml)
          (Xml.child xml_arg0 "byteContent") in
      make ~sourceType ?s3Location ?byteContent ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sourceType =
        field_map_exn json__ "sourceType" FileSourceType.of_json in
      let s3Location = field_map json__ "s3Location" S3ObjectFile.of_json in
      let byteContent =
        field_map json__ "byteContent" ByteContentFile.of_json in
      make ~sourceType ?s3Location ?byteContent ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The source file of the content contained in the wrapper object."]
module FileUseCase =
  struct
    type nonrec t =
      | CODE_INTERPRETER 
      | CHAT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CODE_INTERPRETER -> "CODE_INTERPRETER"
      | CHAT -> "CHAT"
      | Non_static_id s -> s
    let of_string =
      function
      | "CODE_INTERPRETER" -> CODE_INTERPRETER
      | "CHAT" -> CHAT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FileUseCase" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FileUseCase" j)
    let to_json = simple_to_json to_value
  end
module Float_ =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Float x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a float" xml_arg0)
    let of_json j = float_of_json ~kind:"a float" j
    let to_json = simple_to_json to_value
  end
module FlowAliasIdentifier =
  struct
    type nonrec t = string
    let context_ = "FlowAliasIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(arn:aws:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:flow/[0-9a-zA-Z]{10}/alias/[0-9a-zA-Z]{10})|(\\bTSTALIASID\\b|[0-9a-zA-Z]+)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowAliasIdentifier" j
    let to_json = simple_to_json to_value
  end
module FlowCompletionReason =
  struct
    type nonrec t =
      | SUCCESS 
      | INPUT_REQUIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUCCESS -> "SUCCESS"
      | INPUT_REQUIRED -> "INPUT_REQUIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUCCESS" -> SUCCESS
      | "INPUT_REQUIRED" -> INPUT_REQUIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FlowCompletionReason" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FlowCompletionReason" j)
    let to_json = simple_to_json to_value
  end
module FlowCompletionEvent =
  struct
    type nonrec t =
      {
      completionReason: FlowCompletionReason.t option
        [@ocaml.doc "The reason that the flow completed."]}
    let make ?completionReason = fun () -> { completionReason }
    let to_value x =
      structure_to_value
        [("completionReason",
           (Option.map x.completionReason ~f:FlowCompletionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let completionReason =
        (Option.map ~f:FlowCompletionReason.of_xml)
          (Xml.child xml_arg0 "completionReason") in
      make ?completionReason ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let completionReason =
        field_map json__ "completionReason" FlowCompletionReason.of_json in
      make ?completionReason ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about why a flow completed."]
module FlowControlNodeType =
  struct
    type nonrec t =
      | Iterator 
      | Loop 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Iterator -> "Iterator"
      | Loop -> "Loop"
      | Non_static_id s -> s
    let of_string =
      function
      | "Iterator" -> Iterator
      | "Loop" -> Loop
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FlowControlNodeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FlowControlNodeType" j)
    let to_json = simple_to_json to_value
  end
module FlowErrorCode =
  struct
    type nonrec t =
      | VALIDATION 
      | INTERNAL_SERVER 
      | NODE_EXECUTION_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | VALIDATION -> "VALIDATION"
      | INTERNAL_SERVER -> "INTERNAL_SERVER"
      | NODE_EXECUTION_FAILED -> "NODE_EXECUTION_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "VALIDATION" -> VALIDATION
      | "INTERNAL_SERVER" -> INTERNAL_SERVER
      | "NODE_EXECUTION_FAILED" -> NODE_EXECUTION_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FlowErrorCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FlowErrorCode" j)
    let to_json = simple_to_json to_value
  end
module FlowExecutionContent =
  struct
    type nonrec t =
      {
      document: Document.t option
        [@ocaml.doc
          "The document content of the field, which can contain text or structured data."]}
    let make ?document = fun () -> { document }
    let to_value x =
      structure_to_value
        [("document", (Option.map x.document ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let document =
        (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "document") in
      make ?document ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let document = field_map json__ "document" Document.of_json in
      make ?document ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the content of an flow execution input or output field. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowExecutionErrorType =
  struct
    type nonrec t =
      | ExecutionTimedOut 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ExecutionTimedOut -> "ExecutionTimedOut"
      | Non_static_id s -> s
    let of_string =
      function
      | "ExecutionTimedOut" -> ExecutionTimedOut
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FlowExecutionErrorType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FlowExecutionErrorType" j)
    let to_json = simple_to_json to_value
  end
module FlowExecutionError =
  struct
    type nonrec t =
      {
      error: FlowExecutionErrorType.t option
        [@ocaml.doc "The error code for the type of error that occurred."];
      message: String_.t option
        [@ocaml.doc
          "A descriptive message that provides details about the error."];
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the node in the flow where the error occurred (if applicable)."]}
    let make ?error =
      fun ?message -> fun ?nodeName -> fun () -> { error; message; nodeName }
    let to_value x =
      structure_to_value
        [("error", (Option.map x.error ~f:FlowExecutionErrorType.to_value));
        ("message", (Option.map x.message ~f:String_.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let error =
        (Option.map ~f:FlowExecutionErrorType.of_xml)
          (Xml.child xml_arg0 "error") in
      make ?nodeName ?message ?error ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let message = field_map json__ "message" String_.of_json in
      let error = field_map json__ "error" FlowExecutionErrorType.of_json in
      make ?nodeName ?message ?error ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an error that occurred during an flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowExecutionErrors =
  struct
    type nonrec t = FlowExecutionError.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowExecutionError.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowExecutionError.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowExecutionErrors"
        ~of_json:FlowExecutionError.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowNodeInputName =
  struct
    type nonrec t = string
    let context_ = "FlowNodeInputName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z]([_]?[0-9a-zA-Z]){1,50}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowNodeInputName" j
    let to_json = simple_to_json to_value
  end
module NodeOutputNext =
  struct
    type nonrec t =
      {
      inputFieldName: FlowNodeInputName.t option
        [@ocaml.doc
          "The name of the input field in the next node that receives the data."];
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the next node that receives the output data."]}
    let make ?inputFieldName =
      fun ?nodeName -> fun () -> { inputFieldName; nodeName }
    let to_value x =
      structure_to_value
        [("inputFieldName",
           (Option.map x.inputFieldName ~f:FlowNodeInputName.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let inputFieldName =
        (Option.map ~f:FlowNodeInputName.of_xml)
          (Xml.child xml_arg0 "inputFieldName") in
      make ?nodeName ?inputFieldName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let inputFieldName =
        field_map json__ "inputFieldName" FlowNodeInputName.of_json in
      make ?nodeName ?inputFieldName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the next node that receives output data."]
module NodeOutputNextList =
  struct
    type nonrec t = NodeOutputNext.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NodeOutputNext.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NodeOutputNext.of_xml)
    let of_json j =
      list_of_json ~kind:"NodeOutputNextList" ~of_json:NodeOutputNext.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module NodeOutputName =
  struct
    type nonrec t = string
    let context_ = "NodeOutputName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z]([_]?[0-9a-zA-Z]){0,99}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NodeOutputName" j
    let to_json = simple_to_json to_value
  end
module NodeExecutionContent =
  struct
    type nonrec t =
      {
      document: Document.t option
        [@ocaml.doc
          "The document content of the field, which can contain text or structured data."]}
    let make ?document = fun () -> { document }
    let to_value x =
      structure_to_value
        [("document", (Option.map x.document ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let document =
        (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "document") in
      make ?document ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let document = field_map json__ "document" Document.of_json in
      make ?document ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the content of a flow node's input or output field for a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowNodeIODataType =
  struct
    type nonrec t =
      | String_ 
      | Number 
      | Boolean 
      | Object 
      | Array 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | String_ -> "String"
      | Number -> "Number"
      | Boolean -> "Boolean"
      | Object -> "Object"
      | Array -> "Array"
      | Non_static_id s -> s
    let of_string =
      function
      | "String" -> String_
      | "Number" -> Number
      | "Boolean" -> Boolean
      | "Object" -> Object
      | "Array" -> Array
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FlowNodeIODataType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FlowNodeIODataType" j)
    let to_json = simple_to_json to_value
  end
module NodeOutputField =
  struct
    type nonrec t =
      {
      content: NodeExecutionContent.t option
        [@ocaml.doc
          "The content of the output field, which can contain text or structured data."];
      name: NodeOutputName.t option
        [@ocaml.doc
          "The name of the output field as defined in the node's output schema."];
      next: NodeOutputNextList.t option
        [@ocaml.doc
          "The next node that receives output data from this field."];
      type_: FlowNodeIODataType.t option
        [@ocaml.doc
          "The data type of the output field for compatibility validation."]}
    let make ?content =
      fun ?name ->
        fun ?next -> fun ?type_ -> fun () -> { content; name; next; type_ }
    let to_value x =
      structure_to_value
        [("content", (Option.map x.content ~f:NodeExecutionContent.to_value));
        ("name", (Option.map x.name ~f:NodeOutputName.to_value));
        ("next", (Option.map x.next ~f:NodeOutputNextList.to_value));
        ("type", (Option.map x.type_ ~f:FlowNodeIODataType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:FlowNodeIODataType.of_xml) (Xml.child xml_arg0 "type") in
      let next =
        (Option.map ~f:NodeOutputNextList.of_xml) (Xml.child xml_arg0 "next") in
      let name =
        (Option.map ~f:NodeOutputName.of_xml) (Xml.child xml_arg0 "name") in
      let content =
        (Option.map ~f:NodeExecutionContent.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?type_ ?next ?name ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" FlowNodeIODataType.of_json in
      let next = field_map json__ "next" NodeOutputNextList.of_json in
      let name = field_map json__ "name" NodeOutputName.of_json in
      let content = field_map json__ "content" NodeExecutionContent.of_json in
      make ?type_ ?next ?name ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an output field produced by a node during a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module NodeOutputFields =
  struct
    type nonrec t = NodeOutputField.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:2) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NodeOutputField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NodeOutputField.of_xml)
    let of_json j =
      list_of_json ~kind:"NodeOutputFields" ~of_json:NodeOutputField.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module NodeOutputEvent =
  struct
    type nonrec t =
      {
      fields: NodeOutputFields.t option
        [@ocaml.doc "A list of output fields produced by the node."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node that produced the outputs."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc
          "The timestamp when the outputs were produced by the node."]}
    let make ?fields =
      fun ?nodeName ->
        fun ?timestamp -> fun () -> { fields; nodeName; timestamp }
    let to_value x =
      structure_to_value
        [("fields", (Option.map x.fields ~f:NodeOutputFields.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let fields =
        (Option.map ~f:NodeOutputFields.of_xml) (Xml.child xml_arg0 "fields") in
      make ?timestamp ?nodeName ?fields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let fields = field_map json__ "fields" NodeOutputFields.of_json in
      make ?timestamp ?nodeName ?fields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the outputs produced by a specific node during a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowNodeOutputName =
  struct
    type nonrec t = string
    let context_ = "FlowNodeOutputName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z]([_]?[0-9a-zA-Z]){1,50}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowNodeOutputName" j
    let to_json = simple_to_json to_value
  end
module FlowNodeInputExpression =
  struct
    type nonrec t = string
    let context_ = "FlowNodeInputExpression"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowNodeInputExpression" j
    let to_json = simple_to_json to_value
  end
module NodeInputSource =
  struct
    type nonrec t =
      {
      expression: FlowNodeInputExpression.t option
        [@ocaml.doc "The expression used to extract data from the source."];
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the source node that provides the input data."];
      outputFieldName: FlowNodeOutputName.t option
        [@ocaml.doc "The name of the output field from the source node."]}
    let make ?expression =
      fun ?nodeName ->
        fun ?outputFieldName ->
          fun () -> { expression; nodeName; outputFieldName }
    let to_value x =
      structure_to_value
        [("expression",
           (Option.map x.expression ~f:FlowNodeInputExpression.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("outputFieldName",
          (Option.map x.outputFieldName ~f:FlowNodeOutputName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputFieldName =
        (Option.map ~f:FlowNodeOutputName.of_xml)
          (Xml.child xml_arg0 "outputFieldName") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let expression =
        (Option.map ~f:FlowNodeInputExpression.of_xml)
          (Xml.child xml_arg0 "expression") in
      make ?outputFieldName ?nodeName ?expression ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputFieldName =
        field_map json__ "outputFieldName" FlowNodeOutputName.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let expression =
        field_map json__ "expression" FlowNodeInputExpression.of_json in
      make ?outputFieldName ?nodeName ?expression ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the source of input data for a node field."]
module NodeInputName =
  struct
    type nonrec t = string
    let context_ = "NodeInputName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z]([_]?[0-9a-zA-Z]){0,99}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NodeInputName" j
    let to_json = simple_to_json to_value
  end
module NodeInputExecutionChainItem =
  struct
    type nonrec t =
      {
      index: Integer.t option
        [@ocaml.doc
          "The index position of this item in the execution chain."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node in the execution chain."];
      type_: FlowControlNodeType.t option
        [@ocaml.doc
          "The type of execution chain item. Supported values are Iterator and Loop."]}
    let make ?index =
      fun ?nodeName -> fun ?type_ -> fun () -> { index; nodeName; type_ }
    let to_value x =
      structure_to_value
        [("index", (Option.map x.index ~f:Integer.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("type", (Option.map x.type_ ~f:FlowControlNodeType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:FlowControlNodeType.of_xml)
          (Xml.child xml_arg0 "type") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let index = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "index") in
      make ?type_ ?nodeName ?index ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" FlowControlNodeType.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let index = field_map json__ "index" Integer.of_json in
      make ?type_ ?nodeName ?index ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an item in the execution chain for node input tracking."]
module NodeInputExecutionChain =
  struct
    type nonrec t = NodeInputExecutionChainItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NodeInputExecutionChainItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NodeInputExecutionChainItem.of_xml)
    let of_json j =
      list_of_json ~kind:"NodeInputExecutionChain"
        ~of_json:NodeInputExecutionChainItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowNodeInputCategory =
  struct
    type nonrec t =
      | LoopCondition 
      | ReturnValueToLoopStart 
      | ExitLoop 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LoopCondition -> "LoopCondition"
      | ReturnValueToLoopStart -> "ReturnValueToLoopStart"
      | ExitLoop -> "ExitLoop"
      | Non_static_id s -> s
    let of_string =
      function
      | "LoopCondition" -> LoopCondition
      | "ReturnValueToLoopStart" -> ReturnValueToLoopStart
      | "ExitLoop" -> ExitLoop
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FlowNodeInputCategory" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FlowNodeInputCategory" j)
    let to_json = simple_to_json to_value
  end
module NodeInputField =
  struct
    type nonrec t =
      {
      category: FlowNodeInputCategory.t option
        [@ocaml.doc "The category of the input field."];
      content: NodeExecutionContent.t option
        [@ocaml.doc
          "The content of the input field, which can contain text or structured data."];
      executionChain: NodeInputExecutionChain.t option
        [@ocaml.doc
          "The execution path through nested nodes like iterators and loops."];
      name: NodeInputName.t option
        [@ocaml.doc
          "The name of the input field as defined in the node's input schema."];
      source: NodeInputSource.t option
        [@ocaml.doc
          "The source node that provides input data to this field."];
      type_: FlowNodeIODataType.t option
        [@ocaml.doc
          "The data type of the input field for compatibility validation."]}
    let make ?category =
      fun ?content ->
        fun ?executionChain ->
          fun ?name ->
            fun ?source ->
              fun ?type_ ->
                fun () ->
                  { category; content; executionChain; name; source; type_ }
    let to_value x =
      structure_to_value
        [("category",
           (Option.map x.category ~f:FlowNodeInputCategory.to_value));
        ("content", (Option.map x.content ~f:NodeExecutionContent.to_value));
        ("executionChain",
          (Option.map x.executionChain ~f:NodeInputExecutionChain.to_value));
        ("name", (Option.map x.name ~f:NodeInputName.to_value));
        ("source", (Option.map x.source ~f:NodeInputSource.to_value));
        ("type", (Option.map x.type_ ~f:FlowNodeIODataType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:FlowNodeIODataType.of_xml) (Xml.child xml_arg0 "type") in
      let source =
        (Option.map ~f:NodeInputSource.of_xml) (Xml.child xml_arg0 "source") in
      let name =
        (Option.map ~f:NodeInputName.of_xml) (Xml.child xml_arg0 "name") in
      let executionChain =
        (Option.map ~f:NodeInputExecutionChain.of_xml)
          (Xml.child xml_arg0 "executionChain") in
      let content =
        (Option.map ~f:NodeExecutionContent.of_xml)
          (Xml.child xml_arg0 "content") in
      let category =
        (Option.map ~f:FlowNodeInputCategory.of_xml)
          (Xml.child xml_arg0 "category") in
      make ?type_ ?source ?name ?executionChain ?content ?category ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" FlowNodeIODataType.of_json in
      let source = field_map json__ "source" NodeInputSource.of_json in
      let name = field_map json__ "name" NodeInputName.of_json in
      let executionChain =
        field_map json__ "executionChain" NodeInputExecutionChain.of_json in
      let content = field_map json__ "content" NodeExecutionContent.of_json in
      let category =
        field_map json__ "category" FlowNodeInputCategory.of_json in
      make ?type_ ?source ?name ?executionChain ?content ?category ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an input field provided to a node during a flow execution."]
module NodeInputFields =
  struct
    type nonrec t = NodeInputField.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NodeInputField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NodeInputField.of_xml)
    let of_json j =
      list_of_json ~kind:"NodeInputFields" ~of_json:NodeInputField.of_json j
    let to_json v = composed_to_json to_value v
  end
module NodeInputEvent =
  struct
    type nonrec t =
      {
      fields: NodeInputFields.t option
        [@ocaml.doc "A list of input fields provided to the node."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node that received the inputs."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc
          "The timestamp when the inputs were provided to the node."]}
    let make ?fields =
      fun ?nodeName ->
        fun ?timestamp -> fun () -> { fields; nodeName; timestamp }
    let to_value x =
      structure_to_value
        [("fields", (Option.map x.fields ~f:NodeInputFields.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let fields =
        (Option.map ~f:NodeInputFields.of_xml) (Xml.child xml_arg0 "fields") in
      make ?timestamp ?nodeName ?fields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let fields = field_map json__ "fields" NodeInputFields.of_json in
      make ?timestamp ?nodeName ?fields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the inputs provided to a specific node during a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module NodeErrorCode =
  struct
    type nonrec t =
      | VALIDATION 
      | DEPENDENCY_FAILED 
      | BAD_GATEWAY 
      | INTERNAL_SERVER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | VALIDATION -> "VALIDATION"
      | DEPENDENCY_FAILED -> "DEPENDENCY_FAILED"
      | BAD_GATEWAY -> "BAD_GATEWAY"
      | INTERNAL_SERVER -> "INTERNAL_SERVER"
      | Non_static_id s -> s
    let of_string =
      function
      | "VALIDATION" -> VALIDATION
      | "DEPENDENCY_FAILED" -> DEPENDENCY_FAILED
      | "BAD_GATEWAY" -> BAD_GATEWAY
      | "INTERNAL_SERVER" -> INTERNAL_SERVER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration NodeErrorCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"NodeErrorCode" j)
    let to_json = simple_to_json to_value
  end
module NodeFailureEvent =
  struct
    type nonrec t =
      {
      errorCode: NodeErrorCode.t option
        [@ocaml.doc
          "The error code that identifies the type of failure that occurred at the node."];
      errorMessage: String_.t option
        [@ocaml.doc
          "A descriptive message that provides details about the node failure."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node where the failure occurred."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The timestamp when the node failure occurred."]}
    let make ?errorCode =
      fun ?errorMessage ->
        fun ?nodeName ->
          fun ?timestamp ->
            fun () -> { errorCode; errorMessage; nodeName; timestamp }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:NodeErrorCode.to_value));
        ("errorMessage", (Option.map x.errorMessage ~f:String_.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorMessage") in
      let errorCode =
        (Option.map ~f:NodeErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?timestamp ?nodeName ?errorMessage ?errorCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let errorMessage = field_map json__ "errorMessage" String_.of_json in
      let errorCode = field_map json__ "errorCode" NodeErrorCode.of_json in
      make ?timestamp ?nodeName ?errorMessage ?errorCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a failure that occurred at a specific node during a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module NodeTraceElements =
  struct
    type nonrec t =
      {
      agentTraces: AgentTraces.t option
        [@ocaml.doc "Agent trace information for the node execution."]}
    let make ?agentTraces = fun () -> { agentTraces }
    let to_value x =
      structure_to_value
        [("agentTraces", (Option.map x.agentTraces ~f:AgentTraces.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agentTraces =
        (Option.map ~f:AgentTraces.of_xml) (Xml.child xml_arg0 "agentTraces") in
      make ?agentTraces ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agentTraces = field_map json__ "agentTraces" AgentTraces.of_json in
      make ?agentTraces ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains trace elements for node execution tracking."]
module NodeDependencyEvent =
  struct
    type nonrec t =
      {
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the node that generated the dependency trace."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc
          "The date and time that the dependency trace was generated."];
      traceElements: NodeTraceElements.t option
        [@ocaml.doc
          "The trace elements containing detailed information about the node execution."]}
    let make ?nodeName =
      fun ?timestamp ->
        fun ?traceElements ->
          fun () -> { nodeName; timestamp; traceElements }
    let to_value x =
      structure_to_value
        [("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value));
        ("traceElements",
          (Option.map x.traceElements ~f:NodeTraceElements.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceElements =
        (Option.map ~f:NodeTraceElements.of_xml)
          (Xml.child xml_arg0 "traceElements") in
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      make ?traceElements ?timestamp ?nodeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceElements =
        field_map json__ "traceElements" NodeTraceElements.of_json in
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      make ?traceElements ?timestamp ?nodeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an internal trace of a specific node during execution."]
module NodeActionEvent =
  struct
    type nonrec t =
      {
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node that called the operation."];
      operationName: String_.t option
        [@ocaml.doc "The name of the operation that the node called."];
      operationRequest: Document.t option
        [@ocaml.doc "The request payload sent to the downstream service."];
      operationResponse: Document.t option
        [@ocaml.doc
          "The response payload received from the downstream service."];
      requestId: String_.t option
        [@ocaml.doc
          "The ID of the request that the node made to the operation."];
      serviceName: String_.t option
        [@ocaml.doc "The name of the service that the node called."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The date and time that the operation was called."]}
    let make ?nodeName =
      fun ?operationName ->
        fun ?operationRequest ->
          fun ?operationResponse ->
            fun ?requestId ->
              fun ?serviceName ->
                fun ?timestamp ->
                  fun () ->
                    {
                      nodeName;
                      operationName;
                      operationRequest;
                      operationResponse;
                      requestId;
                      serviceName;
                      timestamp
                    }
    let to_value x =
      structure_to_value
        [("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("operationName", (Option.map x.operationName ~f:String_.to_value));
        ("operationRequest",
          (Option.map x.operationRequest ~f:Document.to_value));
        ("operationResponse",
          (Option.map x.operationResponse ~f:Document.to_value));
        ("requestId", (Option.map x.requestId ~f:String_.to_value));
        ("serviceName", (Option.map x.serviceName ~f:String_.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let serviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceName") in
      let requestId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "requestId") in
      let operationResponse =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "operationResponse") in
      let operationRequest =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "operationRequest") in
      let operationName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "operationName") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      make ?timestamp ?serviceName ?requestId ?operationResponse
        ?operationRequest ?operationName ?nodeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let serviceName = field_map json__ "serviceName" String_.of_json in
      let requestId = field_map json__ "requestId" String_.of_json in
      let operationResponse =
        field_map json__ "operationResponse" Document.of_json in
      let operationRequest =
        field_map json__ "operationRequest" Document.of_json in
      let operationName = field_map json__ "operationName" String_.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      make ?timestamp ?serviceName ?requestId ?operationResponse
        ?operationRequest ?operationName ?nodeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an action (operation) called by a node during execution."]
module FlowFailureEvent =
  struct
    type nonrec t =
      {
      errorCode: FlowErrorCode.t option
        [@ocaml.doc
          "The error code that identifies the type of failure that occurred."];
      errorMessage: String_.t option
        [@ocaml.doc
          "A descriptive message that provides details about the failure."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The timestamp when the failure occurred."]}
    let make ?errorCode =
      fun ?errorMessage ->
        fun ?timestamp -> fun () -> { errorCode; errorMessage; timestamp }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:FlowErrorCode.to_value));
        ("errorMessage", (Option.map x.errorMessage ~f:String_.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorMessage") in
      let errorCode =
        (Option.map ~f:FlowErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?timestamp ?errorMessage ?errorCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let errorMessage = field_map json__ "errorMessage" String_.of_json in
      let errorCode = field_map json__ "errorCode" FlowErrorCode.of_json in
      make ?timestamp ?errorMessage ?errorCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a failure that occurred at the flow level during a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowOutputField =
  struct
    type nonrec t =
      {
      content: FlowExecutionContent.t option
        [@ocaml.doc
          "The content of the output field, which can contain text or structured data."];
      name: NodeOutputName.t option
        [@ocaml.doc
          "The name of the output field as defined in the flow's output schema."]}
    let make ?content = fun ?name -> fun () -> { content; name }
    let to_value x =
      structure_to_value
        [("content", (Option.map x.content ~f:FlowExecutionContent.to_value));
        ("name", (Option.map x.name ~f:NodeOutputName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:NodeOutputName.of_xml) (Xml.child xml_arg0 "name") in
      let content =
        (Option.map ~f:FlowExecutionContent.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?name ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "name" NodeOutputName.of_json in
      let content = field_map json__ "content" FlowExecutionContent.of_json in
      make ?name ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an output field produced by a flow during a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowOutputFields =
  struct
    type nonrec t = FlowOutputField.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowOutputField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowOutputField.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowOutputFields" ~of_json:FlowOutputField.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module FlowExecutionOutputEvent =
  struct
    type nonrec t =
      {
      fields: FlowOutputFields.t option
        [@ocaml.doc "A list of output fields produced by the flow."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node that produces the outputs."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The timestamp when the outputs are produced."]}
    let make ?fields =
      fun ?nodeName ->
        fun ?timestamp -> fun () -> { fields; nodeName; timestamp }
    let to_value x =
      structure_to_value
        [("fields", (Option.map x.fields ~f:FlowOutputFields.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let fields =
        (Option.map ~f:FlowOutputFields.of_xml) (Xml.child xml_arg0 "fields") in
      make ?timestamp ?nodeName ?fields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let fields = field_map json__ "fields" FlowOutputFields.of_json in
      make ?timestamp ?nodeName ?fields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the outputs produced by the flow during a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowInputField =
  struct
    type nonrec t =
      {
      content: FlowExecutionContent.t option
        [@ocaml.doc
          "The content of the input field, which can contain text or structured data."];
      name: NodeInputName.t option
        [@ocaml.doc
          "The name of the input field as defined in the flow's input schema."]}
    let make ?content = fun ?name -> fun () -> { content; name }
    let to_value x =
      structure_to_value
        [("content", (Option.map x.content ~f:FlowExecutionContent.to_value));
        ("name", (Option.map x.name ~f:NodeInputName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:NodeInputName.of_xml) (Xml.child xml_arg0 "name") in
      let content =
        (Option.map ~f:FlowExecutionContent.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?name ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "name" NodeInputName.of_json in
      let content = field_map json__ "content" FlowExecutionContent.of_json in
      make ?name ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an input field provided to a flow during a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowInputFields =
  struct
    type nonrec t = FlowInputField.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowInputField.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowInputField.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowInputFields" ~of_json:FlowInputField.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowExecutionInputEvent =
  struct
    type nonrec t =
      {
      fields: FlowInputFields.t option
        [@ocaml.doc "A list of input fields provided to the flow."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node that receives the inputs."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The timestamp when the inputs are provided."]}
    let make ?fields =
      fun ?nodeName ->
        fun ?timestamp -> fun () -> { fields; nodeName; timestamp }
    let to_value x =
      structure_to_value
        [("fields", (Option.map x.fields ~f:FlowInputFields.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let fields =
        (Option.map ~f:FlowInputFields.of_xml) (Xml.child xml_arg0 "fields") in
      make ?timestamp ?nodeName ?fields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let fields = field_map json__ "fields" FlowInputFields.of_json in
      make ?timestamp ?nodeName ?fields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the inputs provided to the flow at the start of a flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowExecutionEvent =
  struct
    type nonrec t =
      {
      conditionResultEvent: ConditionResultEvent.t option
        [@ocaml.doc
          "Contains information about a condition evaluation result during the flow execution. This event is generated when a condition node in the flow evaluates its conditions."];
      flowFailureEvent: FlowFailureEvent.t option
        [@ocaml.doc
          "Contains information about a failure that occurred at the flow level during execution."];
      flowInputEvent: FlowExecutionInputEvent.t option
        [@ocaml.doc
          "Contains information about the inputs provided to the flow at the start of execution."];
      flowOutputEvent: FlowExecutionOutputEvent.t option
        [@ocaml.doc
          "Contains information about the outputs produced by the flow at the end of execution."];
      nodeActionEvent: NodeActionEvent.t option
        [@ocaml.doc
          "Contains information about an action (operation) called by a node during execution."];
      nodeDependencyEvent: NodeDependencyEvent.t option
        [@ocaml.doc
          "Contains information about an internal trace of a specific node during execution."];
      nodeFailureEvent: NodeFailureEvent.t option
        [@ocaml.doc
          "Contains information about a failure that occurred at a specific node during execution."];
      nodeInputEvent: NodeInputEvent.t option
        [@ocaml.doc
          "Contains information about the inputs provided to a specific node during execution."];
      nodeOutputEvent: NodeOutputEvent.t option
        [@ocaml.doc
          "Contains information about the outputs produced by a specific node during execution."]}
    let make ?conditionResultEvent =
      fun ?flowFailureEvent ->
        fun ?flowInputEvent ->
          fun ?flowOutputEvent ->
            fun ?nodeActionEvent ->
              fun ?nodeDependencyEvent ->
                fun ?nodeFailureEvent ->
                  fun ?nodeInputEvent ->
                    fun ?nodeOutputEvent ->
                      fun () ->
                        {
                          conditionResultEvent;
                          flowFailureEvent;
                          flowInputEvent;
                          flowOutputEvent;
                          nodeActionEvent;
                          nodeDependencyEvent;
                          nodeFailureEvent;
                          nodeInputEvent;
                          nodeOutputEvent
                        }
    let to_value x =
      structure_to_value
        [("conditionResultEvent",
           (Option.map x.conditionResultEvent
              ~f:ConditionResultEvent.to_value));
        ("flowFailureEvent",
          (Option.map x.flowFailureEvent ~f:FlowFailureEvent.to_value));
        ("flowInputEvent",
          (Option.map x.flowInputEvent ~f:FlowExecutionInputEvent.to_value));
        ("flowOutputEvent",
          (Option.map x.flowOutputEvent ~f:FlowExecutionOutputEvent.to_value));
        ("nodeActionEvent",
          (Option.map x.nodeActionEvent ~f:NodeActionEvent.to_value));
        ("nodeDependencyEvent",
          (Option.map x.nodeDependencyEvent ~f:NodeDependencyEvent.to_value));
        ("nodeFailureEvent",
          (Option.map x.nodeFailureEvent ~f:NodeFailureEvent.to_value));
        ("nodeInputEvent",
          (Option.map x.nodeInputEvent ~f:NodeInputEvent.to_value));
        ("nodeOutputEvent",
          (Option.map x.nodeOutputEvent ~f:NodeOutputEvent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeOutputEvent =
        (Option.map ~f:NodeOutputEvent.of_xml)
          (Xml.child xml_arg0 "nodeOutputEvent") in
      let nodeInputEvent =
        (Option.map ~f:NodeInputEvent.of_xml)
          (Xml.child xml_arg0 "nodeInputEvent") in
      let nodeFailureEvent =
        (Option.map ~f:NodeFailureEvent.of_xml)
          (Xml.child xml_arg0 "nodeFailureEvent") in
      let nodeDependencyEvent =
        (Option.map ~f:NodeDependencyEvent.of_xml)
          (Xml.child xml_arg0 "nodeDependencyEvent") in
      let nodeActionEvent =
        (Option.map ~f:NodeActionEvent.of_xml)
          (Xml.child xml_arg0 "nodeActionEvent") in
      let flowOutputEvent =
        (Option.map ~f:FlowExecutionOutputEvent.of_xml)
          (Xml.child xml_arg0 "flowOutputEvent") in
      let flowInputEvent =
        (Option.map ~f:FlowExecutionInputEvent.of_xml)
          (Xml.child xml_arg0 "flowInputEvent") in
      let flowFailureEvent =
        (Option.map ~f:FlowFailureEvent.of_xml)
          (Xml.child xml_arg0 "flowFailureEvent") in
      let conditionResultEvent =
        (Option.map ~f:ConditionResultEvent.of_xml)
          (Xml.child xml_arg0 "conditionResultEvent") in
      make ?nodeOutputEvent ?nodeInputEvent ?nodeFailureEvent
        ?nodeDependencyEvent ?nodeActionEvent ?flowOutputEvent
        ?flowInputEvent ?flowFailureEvent ?conditionResultEvent ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeOutputEvent =
        field_map json__ "nodeOutputEvent" NodeOutputEvent.of_json in
      let nodeInputEvent =
        field_map json__ "nodeInputEvent" NodeInputEvent.of_json in
      let nodeFailureEvent =
        field_map json__ "nodeFailureEvent" NodeFailureEvent.of_json in
      let nodeDependencyEvent =
        field_map json__ "nodeDependencyEvent" NodeDependencyEvent.of_json in
      let nodeActionEvent =
        field_map json__ "nodeActionEvent" NodeActionEvent.of_json in
      let flowOutputEvent =
        field_map json__ "flowOutputEvent" FlowExecutionOutputEvent.of_json in
      let flowInputEvent =
        field_map json__ "flowInputEvent" FlowExecutionInputEvent.of_json in
      let flowFailureEvent =
        field_map json__ "flowFailureEvent" FlowFailureEvent.of_json in
      let conditionResultEvent =
        field_map json__ "conditionResultEvent" ConditionResultEvent.of_json in
      make ?nodeOutputEvent ?nodeInputEvent ?nodeFailureEvent
        ?nodeDependencyEvent ?nodeActionEvent ?flowOutputEvent
        ?flowInputEvent ?flowFailureEvent ?conditionResultEvent ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an event that occurred during an flow execution. This is a union type that can contain one of several event types, such as node input and output events; flow input and output events; condition node result events, or failure events. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowExecutionEventType =
  struct
    type nonrec t =
      | Node 
      | Flow 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Node -> "Node" | Flow -> "Flow" | Non_static_id s -> s
    let of_string =
      function | "Node" -> Node | "Flow" -> Flow | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FlowExecutionEventType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"FlowExecutionEventType" j)
    let to_json = simple_to_json to_value
  end
module FlowExecutionEvents =
  struct
    type nonrec t = FlowExecutionEvent.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowExecutionEvent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowExecutionEvent.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowExecutionEvents"
        ~of_json:FlowExecutionEvent.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowExecutionId =
  struct
    type nonrec t = string
    let context_ = "FlowExecutionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9a-zA-Z._:-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowExecutionId" j
    let to_json = simple_to_json to_value
  end
module FlowExecutionIdentifier =
  struct
    type nonrec t = string
    let context_ = "FlowExecutionIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[a-zA-Z0-9-]{1,36}$|^(arn:aws:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:flow/[0-9a-zA-Z]{10}/alias/[0-9a-zA-Z]{10}/execution/[a-zA-Z0-9-]{1,36})$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowExecutionIdentifier" j
    let to_json = simple_to_json to_value
  end
module FlowExecutionName =
  struct
    type nonrec t = string
    let context_ = "FlowExecutionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:36) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9-]{1,36}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowExecutionName" j
    let to_json = simple_to_json to_value
  end
module FlowExecutionRoleArn =
  struct
    type nonrec t = string
    let context_ = "FlowExecutionRoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws(-[^:]+)?:iam::([0-9]{12})?:role/(service-role/)?.+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowExecutionRoleArn" j
    let to_json = simple_to_json to_value
  end
module FlowExecutionStatus =
  struct
    type nonrec t =
      | Running 
      | Succeeded 
      | Failed 
      | TimedOut 
      | Aborted 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Running -> "Running"
      | Succeeded -> "Succeeded"
      | Failed -> "Failed"
      | TimedOut -> "TimedOut"
      | Aborted -> "Aborted"
      | Non_static_id s -> s
    let of_string =
      function
      | "Running" -> Running
      | "Succeeded" -> Succeeded
      | "Failed" -> Failed
      | "TimedOut" -> TimedOut
      | "Aborted" -> Aborted
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration FlowExecutionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FlowExecutionStatus" j)
    let to_json = simple_to_json to_value
  end
module Version =
  struct
    type nonrec t = string
    let context_ = "Version"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:5) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(DRAFT|[0-9]{0,4}[1-9][0-9]{0,4})$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Version" j
    let to_json = simple_to_json to_value
  end
module FlowIdentifier =
  struct
    type nonrec t = string
    let context_ = "FlowIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(arn:aws:bedrock:[a-z0-9-]{1,20}:[0-9]{12}:flow/[0-9a-zA-Z]{10})|([0-9a-zA-Z]{10})$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FlowIdentifier" j
    let to_json = simple_to_json to_value
  end
module FlowExecutionSummary =
  struct
    type nonrec t =
      {
      createdAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp when the flow execution was created."];
      endedAt: DateTimestamp.t option
        [@ocaml.doc
          "The timestamp when the flow execution ended. This field is only populated when the execution has completed, failed, timed out, or been aborted."];
      executionArn: FlowExecutionIdentifier.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies the flow execution."];
      flowAliasIdentifier: FlowAliasIdentifier.t option
        [@ocaml.doc
          "The unique identifier of the flow alias used for the execution."];
      flowIdentifier: FlowIdentifier.t option
        [@ocaml.doc "The unique identifier of the flow."];
      flowVersion: Version.t option
        [@ocaml.doc "The version of the flow used for the execution."];
      status: FlowExecutionStatus.t option
        [@ocaml.doc
          "The current status of the flow execution. Flow executions time out after 24 hours."]}
    let make ?createdAt =
      fun ?endedAt ->
        fun ?executionArn ->
          fun ?flowAliasIdentifier ->
            fun ?flowIdentifier ->
              fun ?flowVersion ->
                fun ?status ->
                  fun () ->
                    {
                      createdAt;
                      endedAt;
                      executionArn;
                      flowAliasIdentifier;
                      flowIdentifier;
                      flowVersion;
                      status
                    }
    let to_value x =
      structure_to_value
        [("createdAt", (Option.map x.createdAt ~f:DateTimestamp.to_value));
        ("endedAt", (Option.map x.endedAt ~f:DateTimestamp.to_value));
        ("executionArn",
          (Option.map x.executionArn ~f:FlowExecutionIdentifier.to_value));
        ("flowAliasIdentifier",
          (Option.map x.flowAliasIdentifier ~f:FlowAliasIdentifier.to_value));
        ("flowIdentifier",
          (Option.map x.flowIdentifier ~f:FlowIdentifier.to_value));
        ("flowVersion", (Option.map x.flowVersion ~f:Version.to_value));
        ("status", (Option.map x.status ~f:FlowExecutionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:FlowExecutionStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let flowVersion =
        (Option.map ~f:Version.of_xml) (Xml.child xml_arg0 "flowVersion") in
      let flowIdentifier =
        (Option.map ~f:FlowIdentifier.of_xml)
          (Xml.child xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        (Option.map ~f:FlowAliasIdentifier.of_xml)
          (Xml.child xml_arg0 "flowAliasIdentifier") in
      let executionArn =
        (Option.map ~f:FlowExecutionIdentifier.of_xml)
          (Xml.child xml_arg0 "executionArn") in
      let endedAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "endedAt") in
      let createdAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      make ?status ?flowVersion ?flowIdentifier ?flowAliasIdentifier
        ?executionArn ?endedAt ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" FlowExecutionStatus.of_json in
      let flowVersion = field_map json__ "flowVersion" Version.of_json in
      let flowIdentifier =
        field_map json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map json__ "flowAliasIdentifier" FlowAliasIdentifier.of_json in
      let executionArn =
        field_map json__ "executionArn" FlowExecutionIdentifier.of_json in
      let endedAt = field_map json__ "endedAt" DateTimestamp.of_json in
      let createdAt = field_map json__ "createdAt" DateTimestamp.of_json in
      make ?status ?flowVersion ?flowIdentifier ?flowAliasIdentifier
        ?executionArn ?endedAt ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains summary information about a flow execution, including its status, timestamps, and identifiers. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module FlowExecutionSummaries =
  struct
    type nonrec t = FlowExecutionSummary.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowExecutionSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowExecutionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowExecutionSummaries"
        ~of_json:FlowExecutionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowInputContent =
  struct
    type nonrec t =
      {
      document: Document.t option
        [@ocaml.doc "The input to send to the prompt flow input node."]}
    let make ?document = fun () -> { document }
    let to_value x =
      structure_to_value
        [("document", (Option.map x.document ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let document =
        (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "document") in
      make ?document ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let document = field_map json__ "document" Document.of_json in
      make ?document ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about an input into the flow."]
module FlowInput =
  struct
    type nonrec t =
      {
      content: FlowInputContent.t
        [@ocaml.doc
          "Contains information about an input into the prompt flow."];
      nodeInputName: NodeInputName.t option
        [@ocaml.doc "The name of the input from the flow input node."];
      nodeName: NodeName.t
        [@ocaml.doc
          "The name of the flow input node that begins the prompt flow."];
      nodeOutputName: NodeOutputName.t option
        [@ocaml.doc
          "The name of the output from the flow input node that begins the prompt flow."]}
    let context_ = "FlowInput"
    let make ?nodeInputName =
      fun ?nodeOutputName ->
        fun ~content ->
          fun ~nodeName ->
            fun () -> { nodeInputName; nodeOutputName; content; nodeName }
    let to_value x =
      structure_to_value
        [("content", (Some (FlowInputContent.to_value x.content)));
        ("nodeInputName",
          (Option.map x.nodeInputName ~f:NodeInputName.to_value));
        ("nodeName", (Some (NodeName.to_value x.nodeName)));
        ("nodeOutputName",
          (Option.map x.nodeOutputName ~f:NodeOutputName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeOutputName =
        (Option.map ~f:NodeOutputName.of_xml)
          (Xml.child xml_arg0 "nodeOutputName") in
      let nodeName =
        NodeName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "nodeName") in
      let nodeInputName =
        (Option.map ~f:NodeInputName.of_xml)
          (Xml.child xml_arg0 "nodeInputName") in
      let content =
        FlowInputContent.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "content") in
      make ?nodeOutputName ~nodeName ?nodeInputName ~content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeOutputName =
        field_map json__ "nodeOutputName" NodeOutputName.of_json in
      let nodeName = field_map_exn json__ "nodeName" NodeName.of_json in
      let nodeInputName =
        field_map json__ "nodeInputName" NodeInputName.of_json in
      let content = field_map_exn json__ "content" FlowInputContent.of_json in
      make ?nodeOutputName ~nodeName ?nodeInputName ~content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an input into the prompt flow and where to send it."]
module FlowInputs =
  struct
    type nonrec t = FlowInput.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowInput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowInput.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowInputs" ~of_json:FlowInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowMultiTurnInputContent =
  struct
    type nonrec t =
      {
      document: Document.t option
        [@ocaml.doc
          "The requested additional input to send back to the multi-turn flow node."]}
    let make ?document = fun () -> { document }
    let to_value x =
      structure_to_value
        [("document", (Option.map x.document ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let document =
        (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "document") in
      make ?document ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let document = field_map json__ "document" Document.of_json in
      make ?document ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The content structure containing input information for multi-turn flow interactions."]
module NodeType =
  struct
    type nonrec t =
      | FlowInputNode 
      | FlowOutputNode 
      | LambdaFunctionNode 
      | KnowledgeBaseNode 
      | PromptNode 
      | ConditionNode 
      | LexNode 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FlowInputNode -> "FlowInputNode"
      | FlowOutputNode -> "FlowOutputNode"
      | LambdaFunctionNode -> "LambdaFunctionNode"
      | KnowledgeBaseNode -> "KnowledgeBaseNode"
      | PromptNode -> "PromptNode"
      | ConditionNode -> "ConditionNode"
      | LexNode -> "LexNode"
      | Non_static_id s -> s
    let of_string =
      function
      | "FlowInputNode" -> FlowInputNode
      | "FlowOutputNode" -> FlowOutputNode
      | "LambdaFunctionNode" -> LambdaFunctionNode
      | "KnowledgeBaseNode" -> KnowledgeBaseNode
      | "PromptNode" -> PromptNode
      | "ConditionNode" -> ConditionNode
      | "LexNode" -> LexNode
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration NodeType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"NodeType" j)
    let to_json = simple_to_json to_value
  end
module FlowMultiTurnInputRequestEvent =
  struct
    type nonrec t =
      {
      content: FlowMultiTurnInputContent.t option
        [@ocaml.doc
          "The content payload containing the input request details for the multi-turn interaction."];
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the node in the flow that is requesting the input."];
      nodeType: NodeType.t option
        [@ocaml.doc
          "The type of the node in the flow that is requesting the input."]}
    let make ?content =
      fun ?nodeName ->
        fun ?nodeType -> fun () -> { content; nodeName; nodeType }
    let to_value x =
      structure_to_value
        [("content",
           (Option.map x.content ~f:FlowMultiTurnInputContent.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("nodeType", (Option.map x.nodeType ~f:NodeType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeType =
        (Option.map ~f:NodeType.of_xml) (Xml.child xml_arg0 "nodeType") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let content =
        (Option.map ~f:FlowMultiTurnInputContent.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?nodeType ?nodeName ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeType = field_map json__ "nodeType" NodeType.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let content =
        field_map json__ "content" FlowMultiTurnInputContent.of_json in
      make ?nodeType ?nodeName ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Response object from the flow multi-turn node requesting additional information."]
module FlowOutputContent =
  struct
    type nonrec t =
      {
      document: Document.t option [@ocaml.doc "The content in the output."]}
    let make ?document = fun () -> { document }
    let to_value x =
      structure_to_value
        [("document", (Option.map x.document ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let document =
        (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "document") in
      make ?document ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let document = field_map json__ "document" Document.of_json in
      make ?document ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the content in an output from prompt flow invocation."]
module FlowOutputEvent =
  struct
    type nonrec t =
      {
      content: FlowOutputContent.t option
        [@ocaml.doc "The content in the output."];
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the flow output node that the output is from."];
      nodeType: NodeType.t option
        [@ocaml.doc "The type of the node that the output is from."]}
    let make ?content =
      fun ?nodeName ->
        fun ?nodeType -> fun () -> { content; nodeName; nodeType }
    let to_value x =
      structure_to_value
        [("content", (Option.map x.content ~f:FlowOutputContent.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("nodeType", (Option.map x.nodeType ~f:NodeType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeType =
        (Option.map ~f:NodeType.of_xml) (Xml.child xml_arg0 "nodeType") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let content =
        (Option.map ~f:FlowOutputContent.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?nodeType ?nodeName ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeType = field_map json__ "nodeType" NodeType.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let content = field_map json__ "content" FlowOutputContent.of_json in
      make ?nodeType ?nodeName ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an output from prompt flow invoction."]
module FlowTraceNodeOutputNext =
  struct
    type nonrec t =
      {
      inputFieldName: FlowNodeInputName.t option
        [@ocaml.doc
          "The name of the input field in the next node that receives the data."];
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the next node that receives the output data."]}
    let make ?inputFieldName =
      fun ?nodeName -> fun () -> { inputFieldName; nodeName }
    let to_value x =
      structure_to_value
        [("inputFieldName",
           (Option.map x.inputFieldName ~f:FlowNodeInputName.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let inputFieldName =
        (Option.map ~f:FlowNodeInputName.of_xml)
          (Xml.child xml_arg0 "inputFieldName") in
      make ?nodeName ?inputFieldName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let inputFieldName =
        field_map json__ "inputFieldName" FlowNodeInputName.of_json in
      make ?nodeName ?inputFieldName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the next node that receives output data from a flow trace."]
module FlowTraceNodeOutputNextList =
  struct
    type nonrec t = FlowTraceNodeOutputNext.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowTraceNodeOutputNext.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowTraceNodeOutputNext.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowTraceNodeOutputNextList"
        ~of_json:FlowTraceNodeOutputNext.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowTraceNodeOutputContent =
  struct
    type nonrec t =
      {
      document: Document.t option
        [@ocaml.doc "The content of the node output."]}
    let make ?document = fun () -> { document }
    let to_value x =
      structure_to_value
        [("document", (Option.map x.document ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let document =
        (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "document") in
      make ?document ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let document = field_map json__ "document" Document.of_json in
      make ?document ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the content of the node output. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTraceNodeOutputField =
  struct
    type nonrec t =
      {
      content: FlowTraceNodeOutputContent.t option
        [@ocaml.doc "The content of the node output."];
      next: FlowTraceNodeOutputNextList.t option
        [@ocaml.doc
          "The next node that receives output data from this field."];
      nodeOutputName: NodeOutputName.t option
        [@ocaml.doc "The name of the node output."];
      type_: FlowNodeIODataType.t option
        [@ocaml.doc
          "The data type of the output field for compatibility validation."]}
    let make ?content =
      fun ?next ->
        fun ?nodeOutputName ->
          fun ?type_ -> fun () -> { content; next; nodeOutputName; type_ }
    let to_value x =
      structure_to_value
        [("content",
           (Option.map x.content ~f:FlowTraceNodeOutputContent.to_value));
        ("next", (Option.map x.next ~f:FlowTraceNodeOutputNextList.to_value));
        ("nodeOutputName",
          (Option.map x.nodeOutputName ~f:NodeOutputName.to_value));
        ("type", (Option.map x.type_ ~f:FlowNodeIODataType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:FlowNodeIODataType.of_xml) (Xml.child xml_arg0 "type") in
      let nodeOutputName =
        (Option.map ~f:NodeOutputName.of_xml)
          (Xml.child xml_arg0 "nodeOutputName") in
      let next =
        (Option.map ~f:FlowTraceNodeOutputNextList.of_xml)
          (Xml.child xml_arg0 "next") in
      let content =
        (Option.map ~f:FlowTraceNodeOutputContent.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?type_ ?nodeOutputName ?next ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" FlowNodeIODataType.of_json in
      let nodeOutputName =
        field_map json__ "nodeOutputName" NodeOutputName.of_json in
      let next = field_map json__ "next" FlowTraceNodeOutputNextList.of_json in
      let content =
        field_map json__ "content" FlowTraceNodeOutputContent.of_json in
      make ?type_ ?nodeOutputName ?next ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a field in the output from a node. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTraceNodeOutputFields =
  struct
    type nonrec t = FlowTraceNodeOutputField.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:2) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowTraceNodeOutputField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowTraceNodeOutputField.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowTraceNodeOutputFields"
        ~of_json:FlowTraceNodeOutputField.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowTraceNodeOutputEvent =
  struct
    type nonrec t =
      {
      fields: FlowTraceNodeOutputFields.t option
        [@ocaml.doc
          "An array of objects containing information about each field in the output."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node that yielded the output."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The date and time that the trace was returned."]}
    let make ?fields =
      fun ?nodeName ->
        fun ?timestamp -> fun () -> { fields; nodeName; timestamp }
    let to_value x =
      structure_to_value
        [("fields",
           (Option.map x.fields ~f:FlowTraceNodeOutputFields.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let fields =
        (Option.map ~f:FlowTraceNodeOutputFields.of_xml)
          (Xml.child xml_arg0 "fields") in
      make ?timestamp ?nodeName ?fields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let fields =
        field_map json__ "fields" FlowTraceNodeOutputFields.of_json in
      make ?timestamp ?nodeName ?fields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the output from a node. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTraceNodeInputSource =
  struct
    type nonrec t =
      {
      expression: FlowNodeInputExpression.t option
        [@ocaml.doc "The expression used to extract data from the source."];
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the source node that provides the input data."];
      outputFieldName: FlowNodeOutputName.t option
        [@ocaml.doc "The name of the output field from the source node."]}
    let make ?expression =
      fun ?nodeName ->
        fun ?outputFieldName ->
          fun () -> { expression; nodeName; outputFieldName }
    let to_value x =
      structure_to_value
        [("expression",
           (Option.map x.expression ~f:FlowNodeInputExpression.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("outputFieldName",
          (Option.map x.outputFieldName ~f:FlowNodeOutputName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputFieldName =
        (Option.map ~f:FlowNodeOutputName.of_xml)
          (Xml.child xml_arg0 "outputFieldName") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let expression =
        (Option.map ~f:FlowNodeInputExpression.of_xml)
          (Xml.child xml_arg0 "expression") in
      make ?outputFieldName ?nodeName ?expression ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputFieldName =
        field_map json__ "outputFieldName" FlowNodeOutputName.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let expression =
        field_map json__ "expression" FlowNodeInputExpression.of_json in
      make ?outputFieldName ?nodeName ?expression ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the source of input data for a flow trace node field."]
module FlowTraceNodeInputExecutionChainItem =
  struct
    type nonrec t =
      {
      index: Integer.t option
        [@ocaml.doc
          "The index position of this item in the execution chain."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node in the execution chain."];
      type_: FlowControlNodeType.t option
        [@ocaml.doc
          "The type of execution chain item. Supported values are Iterator and Loop."]}
    let make ?index =
      fun ?nodeName -> fun ?type_ -> fun () -> { index; nodeName; type_ }
    let to_value x =
      structure_to_value
        [("index", (Option.map x.index ~f:Integer.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("type", (Option.map x.type_ ~f:FlowControlNodeType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:FlowControlNodeType.of_xml)
          (Xml.child xml_arg0 "type") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let index = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "index") in
      make ?type_ ?nodeName ?index ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" FlowControlNodeType.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let index = field_map json__ "index" Integer.of_json in
      make ?type_ ?nodeName ?index ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an item in the execution chain for flow trace node input tracking."]
module FlowTraceNodeInputExecutionChain =
  struct
    type nonrec t = FlowTraceNodeInputExecutionChainItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowTraceNodeInputExecutionChainItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:FlowTraceNodeInputExecutionChainItem.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowTraceNodeInputExecutionChain"
        ~of_json:FlowTraceNodeInputExecutionChainItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowTraceNodeInputContent =
  struct
    type nonrec t =
      {
      document: Document.t option
        [@ocaml.doc "The content of the node input."]}
    let make ?document = fun () -> { document }
    let to_value x =
      structure_to_value
        [("document", (Option.map x.document ~f:Document.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let document =
        (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "document") in
      make ?document ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let document = field_map json__ "document" Document.of_json in
      make ?document ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the content of the node input. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTraceNodeInputField =
  struct
    type nonrec t =
      {
      category: FlowNodeInputCategory.t option
        [@ocaml.doc "The category of the input field."];
      content: FlowTraceNodeInputContent.t option
        [@ocaml.doc "The content of the node input."];
      executionChain: FlowTraceNodeInputExecutionChain.t option
        [@ocaml.doc
          "The execution path through nested nodes like iterators and loops."];
      nodeInputName: NodeInputName.t option
        [@ocaml.doc "The name of the node input."];
      source: FlowTraceNodeInputSource.t option
        [@ocaml.doc
          "The source node that provides input data to this field."];
      type_: FlowNodeIODataType.t option
        [@ocaml.doc
          "The data type of the input field for compatibility validation."]}
    let make ?category =
      fun ?content ->
        fun ?executionChain ->
          fun ?nodeInputName ->
            fun ?source ->
              fun ?type_ ->
                fun () ->
                  {
                    category;
                    content;
                    executionChain;
                    nodeInputName;
                    source;
                    type_
                  }
    let to_value x =
      structure_to_value
        [("category",
           (Option.map x.category ~f:FlowNodeInputCategory.to_value));
        ("content",
          (Option.map x.content ~f:FlowTraceNodeInputContent.to_value));
        ("executionChain",
          (Option.map x.executionChain
             ~f:FlowTraceNodeInputExecutionChain.to_value));
        ("nodeInputName",
          (Option.map x.nodeInputName ~f:NodeInputName.to_value));
        ("source",
          (Option.map x.source ~f:FlowTraceNodeInputSource.to_value));
        ("type", (Option.map x.type_ ~f:FlowNodeIODataType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:FlowNodeIODataType.of_xml) (Xml.child xml_arg0 "type") in
      let source =
        (Option.map ~f:FlowTraceNodeInputSource.of_xml)
          (Xml.child xml_arg0 "source") in
      let nodeInputName =
        (Option.map ~f:NodeInputName.of_xml)
          (Xml.child xml_arg0 "nodeInputName") in
      let executionChain =
        (Option.map ~f:FlowTraceNodeInputExecutionChain.of_xml)
          (Xml.child xml_arg0 "executionChain") in
      let content =
        (Option.map ~f:FlowTraceNodeInputContent.of_xml)
          (Xml.child xml_arg0 "content") in
      let category =
        (Option.map ~f:FlowNodeInputCategory.of_xml)
          (Xml.child xml_arg0 "category") in
      make ?type_ ?source ?nodeInputName ?executionChain ?content ?category
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" FlowNodeIODataType.of_json in
      let source = field_map json__ "source" FlowTraceNodeInputSource.of_json in
      let nodeInputName =
        field_map json__ "nodeInputName" NodeInputName.of_json in
      let executionChain =
        field_map json__ "executionChain"
          FlowTraceNodeInputExecutionChain.of_json in
      let content =
        field_map json__ "content" FlowTraceNodeInputContent.of_json in
      let category =
        field_map json__ "category" FlowNodeInputCategory.of_json in
      make ?type_ ?source ?nodeInputName ?executionChain ?content ?category
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a field in the input into a node. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTraceNodeInputFields =
  struct
    type nonrec t = FlowTraceNodeInputField.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowTraceNodeInputField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowTraceNodeInputField.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowTraceNodeInputFields"
        ~of_json:FlowTraceNodeInputField.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowTraceNodeInputEvent =
  struct
    type nonrec t =
      {
      fields: FlowTraceNodeInputFields.t option
        [@ocaml.doc
          "An array of objects containing information about each field in the input."];
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node that received the input."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The date and time that the trace was returned."]}
    let make ?fields =
      fun ?nodeName ->
        fun ?timestamp -> fun () -> { fields; nodeName; timestamp }
    let to_value x =
      structure_to_value
        [("fields",
           (Option.map x.fields ~f:FlowTraceNodeInputFields.to_value));
        ("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      let fields =
        (Option.map ~f:FlowTraceNodeInputFields.of_xml)
          (Xml.child xml_arg0 "fields") in
      make ?timestamp ?nodeName ?fields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      let fields = field_map json__ "fields" FlowTraceNodeInputFields.of_json in
      make ?timestamp ?nodeName ?fields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the input into a node. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTraceNodeActionEvent =
  struct
    type nonrec t =
      {
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the node that called the operation."];
      operationName: String_.t option
        [@ocaml.doc "The name of the operation that the node called."];
      operationRequest: Document.t option
        [@ocaml.doc "The request payload sent to the downstream service."];
      operationResponse: Document.t option
        [@ocaml.doc
          "The response payload received from the downstream service."];
      requestId: String_.t option
        [@ocaml.doc
          "The ID of the request that the node made to the operation."];
      serviceName: String_.t option
        [@ocaml.doc "The name of the service that the node called."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The date and time that the operation was called."]}
    let make ?nodeName =
      fun ?operationName ->
        fun ?operationRequest ->
          fun ?operationResponse ->
            fun ?requestId ->
              fun ?serviceName ->
                fun ?timestamp ->
                  fun () ->
                    {
                      nodeName;
                      operationName;
                      operationRequest;
                      operationResponse;
                      requestId;
                      serviceName;
                      timestamp
                    }
    let to_value x =
      structure_to_value
        [("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("operationName", (Option.map x.operationName ~f:String_.to_value));
        ("operationRequest",
          (Option.map x.operationRequest ~f:Document.to_value));
        ("operationResponse",
          (Option.map x.operationResponse ~f:Document.to_value));
        ("requestId", (Option.map x.requestId ~f:String_.to_value));
        ("serviceName", (Option.map x.serviceName ~f:String_.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let serviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceName") in
      let requestId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "requestId") in
      let operationResponse =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "operationResponse") in
      let operationRequest =
        (Option.map ~f:Document.of_xml)
          (Xml.child xml_arg0 "operationRequest") in
      let operationName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "operationName") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      make ?timestamp ?serviceName ?requestId ?operationResponse
        ?operationRequest ?operationName ?nodeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let serviceName = field_map json__ "serviceName" String_.of_json in
      let requestId = field_map json__ "requestId" String_.of_json in
      let operationResponse =
        field_map json__ "operationResponse" Document.of_json in
      let operationRequest =
        field_map json__ "operationRequest" Document.of_json in
      let operationName = field_map json__ "operationName" String_.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      make ?timestamp ?serviceName ?requestId ?operationResponse
        ?operationRequest ?operationName ?nodeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an action (operation) called by a node in an Amazon Bedrock flow. The service generates action events for calls made by prompt nodes, agent nodes, and Amazon Web Services Lambda nodes."]
module TraceElements =
  struct
    type nonrec t =
      {
      agentTraces: AgentTraces.t option
        [@ocaml.doc "Agent trace information for the flow execution."]}
    let make ?agentTraces = fun () -> { agentTraces }
    let to_value x =
      structure_to_value
        [("agentTraces", (Option.map x.agentTraces ~f:AgentTraces.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agentTraces =
        (Option.map ~f:AgentTraces.of_xml) (Xml.child xml_arg0 "agentTraces") in
      make ?agentTraces ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agentTraces = field_map json__ "agentTraces" AgentTraces.of_json in
      make ?agentTraces ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains trace elements for flow execution tracking."]
module FlowTraceDependencyEvent =
  struct
    type nonrec t =
      {
      nodeName: NodeName.t option
        [@ocaml.doc
          "The name of the node that generated the dependency trace."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc
          "The date and time that the dependency trace was generated."];
      traceElements: TraceElements.t option
        [@ocaml.doc
          "The trace elements containing detailed information about the dependency."]}
    let make ?nodeName =
      fun ?timestamp ->
        fun ?traceElements ->
          fun () -> { nodeName; timestamp; traceElements }
    let to_value x =
      structure_to_value
        [("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value));
        ("traceElements",
          (Option.map x.traceElements ~f:TraceElements.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let traceElements =
        (Option.map ~f:TraceElements.of_xml)
          (Xml.child xml_arg0 "traceElements") in
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      make ?traceElements ?timestamp ?nodeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let traceElements =
        field_map json__ "traceElements" TraceElements.of_json in
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      make ?traceElements ?timestamp ?nodeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a dependency trace event in the flow."]
module FlowTraceCondition =
  struct
    type nonrec t =
      {
      conditionName: String_.t option
        [@ocaml.doc "The name of the condition."]}
    let make ?conditionName = fun () -> { conditionName }
    let to_value x =
      structure_to_value
        [("conditionName", (Option.map x.conditionName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conditionName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "conditionName") in
      make ?conditionName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let conditionName = field_map json__ "conditionName" String_.of_json in
      make ?conditionName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a condition that was satisfied. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTraceConditions =
  struct
    type nonrec t = FlowTraceCondition.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FlowTraceCondition.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FlowTraceCondition.of_xml)
    let of_json j =
      list_of_json ~kind:"FlowTraceConditions"
        ~of_json:FlowTraceCondition.of_json j
    let to_json v = composed_to_json to_value v
  end
module FlowTraceConditionNodeResultEvent =
  struct
    type nonrec t =
      {
      nodeName: NodeName.t option
        [@ocaml.doc "The name of the condition node."];
      satisfiedConditions: FlowTraceConditions.t option
        [@ocaml.doc
          "An array of objects containing information about the conditions that were satisfied."];
      timestamp: DateTimestamp.t option
        [@ocaml.doc "The date and time that the trace was returned."]}
    let make ?nodeName =
      fun ?satisfiedConditions ->
        fun ?timestamp ->
          fun () -> { nodeName; satisfiedConditions; timestamp }
    let to_value x =
      structure_to_value
        [("nodeName", (Option.map x.nodeName ~f:NodeName.to_value));
        ("satisfiedConditions",
          (Option.map x.satisfiedConditions ~f:FlowTraceConditions.to_value));
        ("timestamp", (Option.map x.timestamp ~f:DateTimestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "timestamp") in
      let satisfiedConditions =
        (Option.map ~f:FlowTraceConditions.of_xml)
          (Xml.child xml_arg0 "satisfiedConditions") in
      let nodeName =
        (Option.map ~f:NodeName.of_xml) (Xml.child xml_arg0 "nodeName") in
      make ?timestamp ?satisfiedConditions ?nodeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" DateTimestamp.of_json in
      let satisfiedConditions =
        field_map json__ "satisfiedConditions" FlowTraceConditions.of_json in
      let nodeName = field_map json__ "nodeName" NodeName.of_json in
      make ?timestamp ?satisfiedConditions ?nodeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an output from a condition node. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTrace =
  struct
    type nonrec t =
      {
      conditionNodeResultTrace: FlowTraceConditionNodeResultEvent.t option
        [@ocaml.doc
          "Contains information about an output from a condition node."];
      nodeActionTrace: FlowTraceNodeActionEvent.t option
        [@ocaml.doc
          "Contains information about an action (operation) called by a node."];
      nodeDependencyTrace: FlowTraceDependencyEvent.t option
        [@ocaml.doc
          "Contains information about an internal trace of a node."];
      nodeInputTrace: FlowTraceNodeInputEvent.t option
        [@ocaml.doc "Contains information about the input into a node."];
      nodeOutputTrace: FlowTraceNodeOutputEvent.t option
        [@ocaml.doc "Contains information about the output from a node."]}
    let make ?conditionNodeResultTrace =
      fun ?nodeActionTrace ->
        fun ?nodeDependencyTrace ->
          fun ?nodeInputTrace ->
            fun ?nodeOutputTrace ->
              fun () ->
                {
                  conditionNodeResultTrace;
                  nodeActionTrace;
                  nodeDependencyTrace;
                  nodeInputTrace;
                  nodeOutputTrace
                }
    let to_value x =
      structure_to_value
        [("conditionNodeResultTrace",
           (Option.map x.conditionNodeResultTrace
              ~f:FlowTraceConditionNodeResultEvent.to_value));
        ("nodeActionTrace",
          (Option.map x.nodeActionTrace ~f:FlowTraceNodeActionEvent.to_value));
        ("nodeDependencyTrace",
          (Option.map x.nodeDependencyTrace
             ~f:FlowTraceDependencyEvent.to_value));
        ("nodeInputTrace",
          (Option.map x.nodeInputTrace ~f:FlowTraceNodeInputEvent.to_value));
        ("nodeOutputTrace",
          (Option.map x.nodeOutputTrace ~f:FlowTraceNodeOutputEvent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeOutputTrace =
        (Option.map ~f:FlowTraceNodeOutputEvent.of_xml)
          (Xml.child xml_arg0 "nodeOutputTrace") in
      let nodeInputTrace =
        (Option.map ~f:FlowTraceNodeInputEvent.of_xml)
          (Xml.child xml_arg0 "nodeInputTrace") in
      let nodeDependencyTrace =
        (Option.map ~f:FlowTraceDependencyEvent.of_xml)
          (Xml.child xml_arg0 "nodeDependencyTrace") in
      let nodeActionTrace =
        (Option.map ~f:FlowTraceNodeActionEvent.of_xml)
          (Xml.child xml_arg0 "nodeActionTrace") in
      let conditionNodeResultTrace =
        (Option.map ~f:FlowTraceConditionNodeResultEvent.of_xml)
          (Xml.child xml_arg0 "conditionNodeResultTrace") in
      make ?nodeOutputTrace ?nodeInputTrace ?nodeDependencyTrace
        ?nodeActionTrace ?conditionNodeResultTrace ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nodeOutputTrace =
        field_map json__ "nodeOutputTrace" FlowTraceNodeOutputEvent.of_json in
      let nodeInputTrace =
        field_map json__ "nodeInputTrace" FlowTraceNodeInputEvent.of_json in
      let nodeDependencyTrace =
        field_map json__ "nodeDependencyTrace"
          FlowTraceDependencyEvent.of_json in
      let nodeActionTrace =
        field_map json__ "nodeActionTrace" FlowTraceNodeActionEvent.of_json in
      let conditionNodeResultTrace =
        field_map json__ "conditionNodeResultTrace"
          FlowTraceConditionNodeResultEvent.of_json in
      make ?nodeOutputTrace ?nodeInputTrace ?nodeDependencyTrace
        ?nodeActionTrace ?conditionNodeResultTrace ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about an input or output for a node in the flow. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowTraceEvent =
  struct
    type nonrec t =
      {
      trace: FlowTrace.t option
        [@ocaml.doc
          "The trace object containing information about an input or output for a node in the flow."]}
    let make ?trace = fun () -> { trace }
    let to_value x =
      structure_to_value
        [("trace", (Option.map x.trace ~f:FlowTrace.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trace =
        (Option.map ~f:FlowTrace.of_xml) (Xml.child xml_arg0 "trace") in
      make ?trace ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trace = field_map json__ "trace" FlowTrace.of_json in
      make ?trace ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a trace, which tracks an input or output for a node in the flow. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."]
module FlowResponseStream =
  struct
    type nonrec t =
      {
      accessDeniedException: AccessDeniedException.t option
        [@ocaml.doc
          "The request is denied because of missing access permissions. Check your permissions and retry your request."];
      badGatewayException: BadGatewayException.t option
        [@ocaml.doc
          "There was an issue with a dependency due to a server issue. Retry your request."];
      conflictException: ConflictException.t option
        [@ocaml.doc
          "There was a conflict performing an operation. Resolve the conflict and retry your request."];
      dependencyFailedException: DependencyFailedException.t option
        [@ocaml.doc
          "There was an issue with a dependency. Check the resource configurations and retry the request."];
      flowCompletionEvent: FlowCompletionEvent.t option
        [@ocaml.doc "Contains information about why the flow completed."];
      flowMultiTurnInputRequestEvent: FlowMultiTurnInputRequestEvent.t option
        [@ocaml.doc
          "The event stream containing the multi-turn input request information from the flow."];
      flowOutputEvent: FlowOutputEvent.t option
        [@ocaml.doc
          "Contains information about an output from flow invocation."];
      flowTraceEvent: FlowTraceEvent.t option
        [@ocaml.doc
          "Contains information about a trace, which tracks an input or output for a node in the flow."];
      internalServerException: InternalServerException.t option
        [@ocaml.doc "An internal server error occurred. Retry your request."];
      resourceNotFoundException: ResourceNotFoundException.t option
        [@ocaml.doc
          "The specified resource Amazon Resource Name (ARN) was not found. Check the Amazon Resource Name (ARN) and try your request again."];
      serviceQuotaExceededException: ServiceQuotaExceededException.t option
        [@ocaml.doc
          "The number of requests exceeds the service quota. Resubmit your request later."];
      throttlingException: ThrottlingException.t option
        [@ocaml.doc
          "The number of requests exceeds the limit. Resubmit your request later."];
      validationException: ValidationException.t option
        [@ocaml.doc
          "Input validation failed. Check your request parameters and retry the request."]}
    let make ?accessDeniedException =
      fun ?badGatewayException ->
        fun ?conflictException ->
          fun ?dependencyFailedException ->
            fun ?flowCompletionEvent ->
              fun ?flowMultiTurnInputRequestEvent ->
                fun ?flowOutputEvent ->
                  fun ?flowTraceEvent ->
                    fun ?internalServerException ->
                      fun ?resourceNotFoundException ->
                        fun ?serviceQuotaExceededException ->
                          fun ?throttlingException ->
                            fun ?validationException ->
                              fun () ->
                                {
                                  accessDeniedException;
                                  badGatewayException;
                                  conflictException;
                                  dependencyFailedException;
                                  flowCompletionEvent;
                                  flowMultiTurnInputRequestEvent;
                                  flowOutputEvent;
                                  flowTraceEvent;
                                  internalServerException;
                                  resourceNotFoundException;
                                  serviceQuotaExceededException;
                                  throttlingException;
                                  validationException
                                }
    let to_value x =
      structure_to_value
        [("accessDeniedException",
           (Option.map x.accessDeniedException
              ~f:AccessDeniedException.to_value));
        ("badGatewayException",
          (Option.map x.badGatewayException ~f:BadGatewayException.to_value));
        ("conflictException",
          (Option.map x.conflictException ~f:ConflictException.to_value));
        ("dependencyFailedException",
          (Option.map x.dependencyFailedException
             ~f:DependencyFailedException.to_value));
        ("flowCompletionEvent",
          (Option.map x.flowCompletionEvent ~f:FlowCompletionEvent.to_value));
        ("flowMultiTurnInputRequestEvent",
          (Option.map x.flowMultiTurnInputRequestEvent
             ~f:FlowMultiTurnInputRequestEvent.to_value));
        ("flowOutputEvent",
          (Option.map x.flowOutputEvent ~f:FlowOutputEvent.to_value));
        ("flowTraceEvent",
          (Option.map x.flowTraceEvent ~f:FlowTraceEvent.to_value));
        ("internalServerException",
          (Option.map x.internalServerException
             ~f:InternalServerException.to_value));
        ("resourceNotFoundException",
          (Option.map x.resourceNotFoundException
             ~f:ResourceNotFoundException.to_value));
        ("serviceQuotaExceededException",
          (Option.map x.serviceQuotaExceededException
             ~f:ServiceQuotaExceededException.to_value));
        ("throttlingException",
          (Option.map x.throttlingException ~f:ThrottlingException.to_value));
        ("validationException",
          (Option.map x.validationException ~f:ValidationException.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validationException =
        (Option.map ~f:ValidationException.of_xml)
          (Xml.child xml_arg0 "validationException") in
      let throttlingException =
        (Option.map ~f:ThrottlingException.of_xml)
          (Xml.child xml_arg0 "throttlingException") in
      let serviceQuotaExceededException =
        (Option.map ~f:ServiceQuotaExceededException.of_xml)
          (Xml.child xml_arg0 "serviceQuotaExceededException") in
      let resourceNotFoundException =
        (Option.map ~f:ResourceNotFoundException.of_xml)
          (Xml.child xml_arg0 "resourceNotFoundException") in
      let internalServerException =
        (Option.map ~f:InternalServerException.of_xml)
          (Xml.child xml_arg0 "internalServerException") in
      let flowTraceEvent =
        (Option.map ~f:FlowTraceEvent.of_xml)
          (Xml.child xml_arg0 "flowTraceEvent") in
      let flowOutputEvent =
        (Option.map ~f:FlowOutputEvent.of_xml)
          (Xml.child xml_arg0 "flowOutputEvent") in
      let flowMultiTurnInputRequestEvent =
        (Option.map ~f:FlowMultiTurnInputRequestEvent.of_xml)
          (Xml.child xml_arg0 "flowMultiTurnInputRequestEvent") in
      let flowCompletionEvent =
        (Option.map ~f:FlowCompletionEvent.of_xml)
          (Xml.child xml_arg0 "flowCompletionEvent") in
      let dependencyFailedException =
        (Option.map ~f:DependencyFailedException.of_xml)
          (Xml.child xml_arg0 "dependencyFailedException") in
      let conflictException =
        (Option.map ~f:ConflictException.of_xml)
          (Xml.child xml_arg0 "conflictException") in
      let badGatewayException =
        (Option.map ~f:BadGatewayException.of_xml)
          (Xml.child xml_arg0 "badGatewayException") in
      let accessDeniedException =
        (Option.map ~f:AccessDeniedException.of_xml)
          (Xml.child xml_arg0 "accessDeniedException") in
      make ?validationException ?throttlingException
        ?serviceQuotaExceededException ?resourceNotFoundException
        ?internalServerException ?flowTraceEvent ?flowOutputEvent
        ?flowMultiTurnInputRequestEvent ?flowCompletionEvent
        ?dependencyFailedException ?conflictException ?badGatewayException
        ?accessDeniedException ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let validationException =
        field_map json__ "validationException" ValidationException.of_json in
      let throttlingException =
        field_map json__ "throttlingException" ThrottlingException.of_json in
      let serviceQuotaExceededException =
        field_map json__ "serviceQuotaExceededException"
          ServiceQuotaExceededException.of_json in
      let resourceNotFoundException =
        field_map json__ "resourceNotFoundException"
          ResourceNotFoundException.of_json in
      let internalServerException =
        field_map json__ "internalServerException"
          InternalServerException.of_json in
      let flowTraceEvent =
        field_map json__ "flowTraceEvent" FlowTraceEvent.of_json in
      let flowOutputEvent =
        field_map json__ "flowOutputEvent" FlowOutputEvent.of_json in
      let flowMultiTurnInputRequestEvent =
        field_map json__ "flowMultiTurnInputRequestEvent"
          FlowMultiTurnInputRequestEvent.of_json in
      let flowCompletionEvent =
        field_map json__ "flowCompletionEvent" FlowCompletionEvent.of_json in
      let dependencyFailedException =
        field_map json__ "dependencyFailedException"
          DependencyFailedException.of_json in
      let conflictException =
        field_map json__ "conflictException" ConflictException.of_json in
      let badGatewayException =
        field_map json__ "badGatewayException" BadGatewayException.of_json in
      let accessDeniedException =
        field_map json__ "accessDeniedException"
          AccessDeniedException.of_json in
      make ?validationException ?throttlingException
        ?serviceQuotaExceededException ?resourceNotFoundException
        ?internalServerException ?flowTraceEvent ?flowOutputEvent
        ?flowMultiTurnInputRequestEvent ?flowCompletionEvent
        ?dependencyFailedException ?conflictException ?badGatewayException
        ?accessDeniedException ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output of the flow."]
module KnowledgeBaseArn =
  struct
    type nonrec t = string
    let context_ = "KnowledgeBaseArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws(|-cn|-us-gov):bedrock:[a-zA-Z0-9-]*:[0-9]{12}:knowledge-base/[0-9a-zA-Z]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KnowledgeBaseArn" j
    let to_json = simple_to_json to_value
  end
module TextToSqlKnowledgeBaseConfiguration =
  struct
    type nonrec t =
      {
      knowledgeBaseArn: KnowledgeBaseArn.t
        [@ocaml.doc "The ARN of the knowledge base"]}
    let context_ = "TextToSqlKnowledgeBaseConfiguration"
    let make ~knowledgeBaseArn = fun () -> { knowledgeBaseArn }
    let to_value x =
      structure_to_value
        [("knowledgeBaseArn",
           (Some (KnowledgeBaseArn.to_value x.knowledgeBaseArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let knowledgeBaseArn =
        KnowledgeBaseArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "knowledgeBaseArn") in
      make ~knowledgeBaseArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let knowledgeBaseArn =
        field_map_exn json__ "knowledgeBaseArn" KnowledgeBaseArn.of_json in
      make ~knowledgeBaseArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for a knowledge base to use in transformation."]
module TextToSqlConfigurationType =
  struct
    type nonrec t =
      | KNOWLEDGE_BASE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | KNOWLEDGE_BASE -> "KNOWLEDGE_BASE" | Non_static_id s -> s
    let of_string =
      function | "KNOWLEDGE_BASE" -> KNOWLEDGE_BASE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TextToSqlConfigurationType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"TextToSqlConfigurationType" j)
    let to_json = simple_to_json to_value
  end
module TextToSqlConfiguration =
  struct
    type nonrec t =
      {
      knowledgeBaseConfiguration:
        TextToSqlKnowledgeBaseConfiguration.t option
        [@ocaml.doc
          "Specifies configurations for a knowledge base to use in transformation."];
      type_: TextToSqlConfigurationType.t
        [@ocaml.doc "The type of resource to use in transformation."]}
    let context_ = "TextToSqlConfiguration"
    let make ?knowledgeBaseConfiguration =
      fun ~type_ -> fun () -> { knowledgeBaseConfiguration; type_ }
    let to_value x =
      structure_to_value
        [("knowledgeBaseConfiguration",
           (Option.map x.knowledgeBaseConfiguration
              ~f:TextToSqlKnowledgeBaseConfiguration.to_value));
        ("type", (Some (TextToSqlConfigurationType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        TextToSqlConfigurationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let knowledgeBaseConfiguration =
        (Option.map ~f:TextToSqlKnowledgeBaseConfiguration.of_xml)
          (Xml.child xml_arg0 "knowledgeBaseConfiguration") in
      make ~type_ ?knowledgeBaseConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ =
        field_map_exn json__ "type" TextToSqlConfigurationType.of_json in
      let knowledgeBaseConfiguration =
        field_map json__ "knowledgeBaseConfiguration"
          TextToSqlKnowledgeBaseConfiguration.of_json in
      make ~type_ ?knowledgeBaseConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains configurations for transforming text to SQL."]
module QueryTransformationMode =
  struct
    type nonrec t =
      | TEXT_TO_SQL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | TEXT_TO_SQL -> "TEXT_TO_SQL" | Non_static_id s -> s
    let of_string =
      function | "TEXT_TO_SQL" -> TEXT_TO_SQL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration QueryTransformationMode" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"QueryTransformationMode" j)
    let to_json = simple_to_json to_value
  end
module TransformationConfiguration =
  struct
    type nonrec t =
      {
      mode: QueryTransformationMode.t
        [@ocaml.doc "The mode of the transformation."];
      textToSqlConfiguration: TextToSqlConfiguration.t option
        [@ocaml.doc "Specifies configurations for transforming text to SQL."]}
    let context_ = "TransformationConfiguration"
    let make ?textToSqlConfiguration =
      fun ~mode -> fun () -> { textToSqlConfiguration; mode }
    let to_value x =
      structure_to_value
        [("mode", (Some (QueryTransformationMode.to_value x.mode)));
        ("textToSqlConfiguration",
          (Option.map x.textToSqlConfiguration
             ~f:TextToSqlConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let textToSqlConfiguration =
        (Option.map ~f:TextToSqlConfiguration.of_xml)
          (Xml.child xml_arg0 "textToSqlConfiguration") in
      let mode =
        QueryTransformationMode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "mode") in
      make ?textToSqlConfiguration ~mode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let textToSqlConfiguration =
        field_map json__ "textToSqlConfiguration"
          TextToSqlConfiguration.of_json in
      let mode = field_map_exn json__ "mode" QueryTransformationMode.of_json in
      make ?textToSqlConfiguration ~mode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for transforming the natural language query into SQL."]
module QueryGenerationInputTextString =
  struct
    type nonrec t = string
    let context_ = "QueryGenerationInputTextString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:20000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QueryGenerationInputTextString" j
    let to_json = simple_to_json to_value
  end
module InputQueryType =
  struct
    type nonrec t =
      | TEXT 
      | Non_static_id of string 
    let make i = i
    let to_string = function | TEXT -> "TEXT" | Non_static_id s -> s
    let of_string = function | "TEXT" -> TEXT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InputQueryType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InputQueryType" j)
    let to_json = simple_to_json to_value
  end
module QueryGenerationInput =
  struct
    type nonrec t =
      {
      text: QueryGenerationInputTextString.t
        [@ocaml.doc "The text of the query."];
      type_: InputQueryType.t [@ocaml.doc "The type of the query."]}
    let context_ = "QueryGenerationInput"
    let make ~text = fun ~type_ -> fun () -> { text; type_ }
    let to_value x =
      structure_to_value
        [("text", (Some (QueryGenerationInputTextString.to_value x.text)));
        ("type", (Some (InputQueryType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        InputQueryType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let text =
        QueryGenerationInputTextString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "text") in
      make ~type_ ~text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" InputQueryType.of_json in
      let text =
        field_map_exn json__ "text" QueryGenerationInputTextString.of_json in
      make ~type_ ~text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a natural language query to transform into SQL."]
module GenerateQueryRequest =
  struct
    type nonrec t =
      {
      queryGenerationInput: QueryGenerationInput.t
        [@ocaml.doc
          "Specifies information about a natural language query to transform into SQL."];
      transformationConfiguration: TransformationConfiguration.t
        [@ocaml.doc
          "Specifies configurations for transforming the natural language query into SQL."]}
    let context_ = "GenerateQueryRequest"
    let make ~queryGenerationInput =
      fun ~transformationConfiguration ->
        fun () -> { queryGenerationInput; transformationConfiguration }
    let to_value x =
      structure_to_value
        [("queryGenerationInput",
           (Some (QueryGenerationInput.to_value x.queryGenerationInput)));
        ("transformationConfiguration",
          (Some
             (TransformationConfiguration.to_value
                x.transformationConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transformationConfiguration =
        TransformationConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "transformationConfiguration") in
      let queryGenerationInput =
        QueryGenerationInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "queryGenerationInput") in
      make ~transformationConfiguration ~queryGenerationInput ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transformationConfiguration =
        field_map_exn json__ "transformationConfiguration"
          TransformationConfiguration.of_json in
      let queryGenerationInput =
        field_map_exn json__ "queryGenerationInput"
          QueryGenerationInput.of_json in
      make ~transformationConfiguration ~queryGenerationInput ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Generates an SQL query from a natural language query. For more information, see Generate a query for structured data in the Amazon Bedrock User Guide."]
module GeneratedQueryType =
  struct
    type nonrec t =
      | REDSHIFT_SQL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | REDSHIFT_SQL -> "REDSHIFT_SQL" | Non_static_id s -> s
    let of_string =
      function | "REDSHIFT_SQL" -> REDSHIFT_SQL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GeneratedQueryType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GeneratedQueryType" j)
    let to_json = simple_to_json to_value
  end
module GeneratedQuery =
  struct
    type nonrec t =
      {
      sql: String_.t option
        [@ocaml.doc
          "An SQL query that corresponds to the natural language query."];
      type_: GeneratedQueryType.t option
        [@ocaml.doc "The type of transformed query."]}
    let make ?sql = fun ?type_ -> fun () -> { sql; type_ }
    let to_value x =
      structure_to_value
        [("sql", (Option.map x.sql ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:GeneratedQueryType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:GeneratedQueryType.of_xml) (Xml.child xml_arg0 "type") in
      let sql = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sql") in
      make ?type_ ?sql ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" GeneratedQueryType.of_json in
      let sql = field_map json__ "sql" String_.of_json in make ?type_ ?sql ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a query generated for a natural language query."]
module GeneratedQueries =
  struct
    type nonrec t = GeneratedQuery.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:0); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GeneratedQuery.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GeneratedQuery.of_xml)
    let of_json j =
      list_of_json ~kind:"GeneratedQueries" ~of_json:GeneratedQuery.of_json j
    let to_json v = composed_to_json to_value v
  end
module GenerateQueryResponse =
  struct
    type nonrec t =
      {
      queries: GeneratedQueries.t option
        [@ocaml.doc
          "A list of objects, each of which defines a generated query that can correspond to the natural language queries."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?queries = fun () -> { queries }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("queries", (Option.map x.queries ~f:GeneratedQueries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let queries =
        (Option.map ~f:GeneratedQueries.of_xml)
          (Xml.child xml_arg0 "queries") in
      make ?queries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let queries = field_map json__ "queries" GeneratedQueries.of_json in
      make ?queries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Generates an SQL query from a natural language query. For more information, see Generate a query for structured data in the Amazon Bedrock User Guide."]
module GenerationConfiguration =
  struct
    type nonrec t =
      {
      additionalModelRequestFields: AdditionalModelRequestFields.t option
        [@ocaml.doc
          "Additional model parameters and corresponding values not included in the textInferenceConfig structure for a knowledge base. This allows users to provide custom model parameters specific to the language model being used."];
      guardrailConfiguration: GuardrailConfiguration.t option
        [@ocaml.doc "The configuration details for the guardrail."];
      inferenceConfig: InferenceConfig.t option
        [@ocaml.doc
          "Configuration settings for inference when using RetrieveAndGenerate to generate responses while using a knowledge base as a source."];
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "The latency configuration for the model."];
      promptTemplate: PromptTemplate.t option
        [@ocaml.doc
          "Contains the template for the prompt that's sent to the model for response generation. Generation prompts must include the $search_results$ variable. For more information, see Use placeholder variables in the user guide."]}
    let make ?additionalModelRequestFields =
      fun ?guardrailConfiguration ->
        fun ?inferenceConfig ->
          fun ?performanceConfig ->
            fun ?promptTemplate ->
              fun () ->
                {
                  additionalModelRequestFields;
                  guardrailConfiguration;
                  inferenceConfig;
                  performanceConfig;
                  promptTemplate
                }
    let to_value x =
      structure_to_value
        [("additionalModelRequestFields",
           (Option.map x.additionalModelRequestFields
              ~f:AdditionalModelRequestFields.to_value));
        ("guardrailConfiguration",
          (Option.map x.guardrailConfiguration
             ~f:GuardrailConfiguration.to_value));
        ("inferenceConfig",
          (Option.map x.inferenceConfig ~f:InferenceConfig.to_value));
        ("performanceConfig",
          (Option.map x.performanceConfig
             ~f:PerformanceConfiguration.to_value));
        ("promptTemplate",
          (Option.map x.promptTemplate ~f:PromptTemplate.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let promptTemplate =
        (Option.map ~f:PromptTemplate.of_xml)
          (Xml.child xml_arg0 "promptTemplate") in
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      let inferenceConfig =
        (Option.map ~f:InferenceConfig.of_xml)
          (Xml.child xml_arg0 "inferenceConfig") in
      let guardrailConfiguration =
        (Option.map ~f:GuardrailConfiguration.of_xml)
          (Xml.child xml_arg0 "guardrailConfiguration") in
      let additionalModelRequestFields =
        (Option.map ~f:AdditionalModelRequestFields.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      make ?promptTemplate ?performanceConfig ?inferenceConfig
        ?guardrailConfiguration ?additionalModelRequestFields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let promptTemplate =
        field_map json__ "promptTemplate" PromptTemplate.of_json in
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      let inferenceConfig =
        field_map json__ "inferenceConfig" InferenceConfig.of_json in
      let guardrailConfiguration =
        field_map json__ "guardrailConfiguration"
          GuardrailConfiguration.of_json in
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields"
          AdditionalModelRequestFields.of_json in
      make ?promptTemplate ?performanceConfig ?inferenceConfig
        ?guardrailConfiguration ?additionalModelRequestFields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configurations for response generation based on the knowledge base query results. This data type is used in the following API operations: RetrieveAndGenerate request"]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"^\\S*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module MemoryType =
  struct
    type nonrec t =
      | SESSION_SUMMARY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | SESSION_SUMMARY -> "SESSION_SUMMARY" | Non_static_id s -> s
    let of_string =
      function | "SESSION_SUMMARY" -> SESSION_SUMMARY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MemoryType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MemoryType" j)
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int[@@ocaml.doc "Max Results."]
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Max Results."]
module GetAgentMemoryRequest =
  struct
    type nonrec t =
      {
      agentAliasId: AgentAliasId.t
        [@ocaml.doc "The unique identifier of an alias of an agent."];
      agentId: AgentId.t
        [@ocaml.doc
          "The unique identifier of the agent to which the alias belongs."];
      maxItems: MaxResults.t option
        [@ocaml.doc
          "The maximum number of items to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results."];
      memoryId: MemoryId.t
        [@ocaml.doc "The unique identifier of the memory."];
      memoryType: MemoryType.t [@ocaml.doc "The type of memory."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than the maxItems value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results."]}
    let context_ = "GetAgentMemoryRequest"
    let make ?maxItems =
      fun ?nextToken ->
        fun ~agentAliasId ->
          fun ~agentId ->
            fun ~memoryId ->
              fun ~memoryType ->
                fun () ->
                  {
                    maxItems;
                    nextToken;
                    agentAliasId;
                    agentId;
                    memoryId;
                    memoryType
                  }
    let to_value x =
      structure_to_value
        [("agentAliasId", (Some (AgentAliasId.to_value x.agentAliasId)));
        ("agentId", (Some (AgentId.to_value x.agentId)));
        ("maxItems", (Option.map x.maxItems ~f:MaxResults.to_value));
        ("memoryId", (Some (MemoryId.to_value x.memoryId)));
        ("memoryType", (Some (MemoryType.to_value x.memoryType)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let memoryType =
        MemoryType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "memoryType") in
      let memoryId =
        MemoryId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "memoryId") in
      let maxItems =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxItems") in
      let agentId =
        AgentId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "agentId") in
      let agentAliasId =
        AgentAliasId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agentAliasId") in
      make ?nextToken ~memoryType ~memoryId ?maxItems ~agentId ~agentAliasId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let memoryType = field_map_exn json__ "memoryType" MemoryType.of_json in
      let memoryId = field_map_exn json__ "memoryId" MemoryId.of_json in
      let maxItems = field_map json__ "maxItems" MaxResults.of_json in
      let agentId = field_map_exn json__ "agentId" AgentId.of_json in
      let agentAliasId =
        field_map_exn json__ "agentAliasId" AgentAliasId.of_json in
      make ?nextToken ~memoryType ~memoryId ?maxItems ~agentId ~agentAliasId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets the sessions stored in the memory of the agent."]
module SummaryText =
  struct
    type nonrec t = string
    let context_ = "SummaryText"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:25000000) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SummaryText" j
    let to_json = simple_to_json to_value
  end
module MemorySessionSummary =
  struct
    type nonrec t =
      {
      memoryId: MemoryId.t option
        [@ocaml.doc
          "The unique identifier of the memory where the session summary is stored."];
      sessionExpiryTime: DateTimestamp.t option
        [@ocaml.doc
          "The time when the memory duration for the session is set to end."];
      sessionId: SessionId.t option
        [@ocaml.doc "The identifier for this session."];
      sessionStartTime: DateTimestamp.t option
        [@ocaml.doc "The start time for this session."];
      summaryText: SummaryText.t option
        [@ocaml.doc "The summarized text for this session."]}
    let make ?memoryId =
      fun ?sessionExpiryTime ->
        fun ?sessionId ->
          fun ?sessionStartTime ->
            fun ?summaryText ->
              fun () ->
                {
                  memoryId;
                  sessionExpiryTime;
                  sessionId;
                  sessionStartTime;
                  summaryText
                }
    let to_value x =
      structure_to_value
        [("memoryId", (Option.map x.memoryId ~f:MemoryId.to_value));
        ("sessionExpiryTime",
          (Option.map x.sessionExpiryTime ~f:DateTimestamp.to_value));
        ("sessionId", (Option.map x.sessionId ~f:SessionId.to_value));
        ("sessionStartTime",
          (Option.map x.sessionStartTime ~f:DateTimestamp.to_value));
        ("summaryText", (Option.map x.summaryText ~f:SummaryText.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let summaryText =
        (Option.map ~f:SummaryText.of_xml) (Xml.child xml_arg0 "summaryText") in
      let sessionStartTime =
        (Option.map ~f:DateTimestamp.of_xml)
          (Xml.child xml_arg0 "sessionStartTime") in
      let sessionId =
        (Option.map ~f:SessionId.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionExpiryTime =
        (Option.map ~f:DateTimestamp.of_xml)
          (Xml.child xml_arg0 "sessionExpiryTime") in
      let memoryId =
        (Option.map ~f:MemoryId.of_xml) (Xml.child xml_arg0 "memoryId") in
      make ?summaryText ?sessionStartTime ?sessionId ?sessionExpiryTime
        ?memoryId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let summaryText = field_map json__ "summaryText" SummaryText.of_json in
      let sessionStartTime =
        field_map json__ "sessionStartTime" DateTimestamp.of_json in
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let sessionExpiryTime =
        field_map json__ "sessionExpiryTime" DateTimestamp.of_json in
      let memoryId = field_map json__ "memoryId" MemoryId.of_json in
      make ?summaryText ?sessionStartTime ?sessionId ?sessionExpiryTime
        ?memoryId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains details of a session summary."]
module Memory =
  struct
    type nonrec t =
      {
      sessionSummary: MemorySessionSummary.t option
        [@ocaml.doc "Contains summary of a session."]}
    let make ?sessionSummary = fun () -> { sessionSummary }
    let to_value x =
      structure_to_value
        [("sessionSummary",
           (Option.map x.sessionSummary ~f:MemorySessionSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionSummary =
        (Option.map ~f:MemorySessionSummary.of_xml)
          (Xml.child xml_arg0 "sessionSummary") in
      make ?sessionSummary ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionSummary =
        field_map json__ "sessionSummary" MemorySessionSummary.of_json in
      make ?sessionSummary ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains sessions summaries."]
module Memories =
  struct
    type nonrec t = Memory.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Memory.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Memory.of_xml)
    let of_json j = list_of_json ~kind:"Memories" ~of_json:Memory.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetAgentMemoryResponse =
  struct
    type nonrec t =
      {
      memoryContents: Memories.t option
        [@ocaml.doc "Contains details of the sessions stored in the memory"];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than the maxItems value provided in the request, use this token when making another request in the nextToken field to return the next batch of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?memoryContents =
      fun ?nextToken -> fun () -> { memoryContents; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("memoryContents",
           (Option.map x.memoryContents ~f:Memories.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let memoryContents =
        (Option.map ~f:Memories.of_xml) (Xml.child xml_arg0 "memoryContents") in
      make ?nextToken ?memoryContents ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let memoryContents = field_map json__ "memoryContents" Memories.of_json in
      make ?nextToken ?memoryContents ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets the sessions stored in the memory of the agent."]
module GetExecutionFlowSnapshotRequest =
  struct
    type nonrec t =
      {
      executionIdentifier: FlowExecutionIdentifier.t
        [@ocaml.doc "The unique identifier of the flow execution."];
      flowAliasIdentifier: FlowAliasIdentifier.t
        [@ocaml.doc
          "The unique identifier of the flow alias used for the flow execution."];
      flowIdentifier: FlowIdentifier.t
        [@ocaml.doc "The unique identifier of the flow."]}
    let context_ = "GetExecutionFlowSnapshotRequest"
    let make ~executionIdentifier =
      fun ~flowAliasIdentifier ->
        fun ~flowIdentifier ->
          fun () ->
            { executionIdentifier; flowAliasIdentifier; flowIdentifier }
    let to_value x =
      structure_to_value
        [("executionIdentifier",
           (Some (FlowExecutionIdentifier.to_value x.executionIdentifier)));
        ("flowAliasIdentifier",
          (Some (FlowAliasIdentifier.to_value x.flowAliasIdentifier)));
        ("flowIdentifier", (Some (FlowIdentifier.to_value x.flowIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flowIdentifier =
        FlowIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        FlowAliasIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowAliasIdentifier") in
      let executionIdentifier =
        FlowExecutionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "executionIdentifier") in
      make ~flowIdentifier ~flowAliasIdentifier ~executionIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let flowIdentifier =
        field_map_exn json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map_exn json__ "flowAliasIdentifier"
          FlowAliasIdentifier.of_json in
      let executionIdentifier =
        field_map_exn json__ "executionIdentifier"
          FlowExecutionIdentifier.of_json in
      make ~flowIdentifier ~flowAliasIdentifier ~executionIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the flow definition snapshot used for a flow execution. The snapshot represents the flow metadata and definition as it existed at the time the execution was started. Note that even if the flow is edited after an execution starts, the snapshot connected to the execution remains unchanged. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module GetExecutionFlowSnapshotResponse =
  struct
    type nonrec t =
      {
      customerEncryptionKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the customer managed KMS key that's used to encrypt the flow snapshot."];
      definition: String_.t option
        [@ocaml.doc
          "The flow definition used for the flow execution, including the nodes, connections, and configuration at the time when the execution started. The definition returns as a string that follows the structure of a FlowDefinition object."];
      executionRoleArn: FlowExecutionRoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM service role that's used by the flow execution."];
      flowAliasIdentifier: FlowAliasIdentifier.t option
        [@ocaml.doc
          "The unique identifier of the flow alias used for the flow execution."];
      flowIdentifier: FlowIdentifier.t option
        [@ocaml.doc "The unique identifier of the flow."];
      flowVersion: Version.t option
        [@ocaml.doc "The version of the flow used for the flow execution."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?customerEncryptionKeyArn =
      fun ?definition ->
        fun ?executionRoleArn ->
          fun ?flowAliasIdentifier ->
            fun ?flowIdentifier ->
              fun ?flowVersion ->
                fun () ->
                  {
                    customerEncryptionKeyArn;
                    definition;
                    executionRoleArn;
                    flowAliasIdentifier;
                    flowIdentifier;
                    flowVersion
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("customerEncryptionKeyArn",
           (Option.map x.customerEncryptionKeyArn ~f:KmsKeyArn.to_value));
        ("definition", (Option.map x.definition ~f:String_.to_value));
        ("executionRoleArn",
          (Option.map x.executionRoleArn ~f:FlowExecutionRoleArn.to_value));
        ("flowAliasIdentifier",
          (Option.map x.flowAliasIdentifier ~f:FlowAliasIdentifier.to_value));
        ("flowIdentifier",
          (Option.map x.flowIdentifier ~f:FlowIdentifier.to_value));
        ("flowVersion", (Option.map x.flowVersion ~f:Version.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flowVersion =
        (Option.map ~f:Version.of_xml) (Xml.child xml_arg0 "flowVersion") in
      let flowIdentifier =
        (Option.map ~f:FlowIdentifier.of_xml)
          (Xml.child xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        (Option.map ~f:FlowAliasIdentifier.of_xml)
          (Xml.child xml_arg0 "flowAliasIdentifier") in
      let executionRoleArn =
        (Option.map ~f:FlowExecutionRoleArn.of_xml)
          (Xml.child xml_arg0 "executionRoleArn") in
      let definition =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "definition") in
      let customerEncryptionKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml)
          (Xml.child xml_arg0 "customerEncryptionKeyArn") in
      make ?flowVersion ?flowIdentifier ?flowAliasIdentifier
        ?executionRoleArn ?definition ?customerEncryptionKeyArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let flowVersion = field_map json__ "flowVersion" Version.of_json in
      let flowIdentifier =
        field_map json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map json__ "flowAliasIdentifier" FlowAliasIdentifier.of_json in
      let executionRoleArn =
        field_map json__ "executionRoleArn" FlowExecutionRoleArn.of_json in
      let definition = field_map json__ "definition" String_.of_json in
      let customerEncryptionKeyArn =
        field_map json__ "customerEncryptionKeyArn" KmsKeyArn.of_json in
      make ?flowVersion ?flowIdentifier ?flowAliasIdentifier
        ?executionRoleArn ?definition ?customerEncryptionKeyArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the flow definition snapshot used for a flow execution. The snapshot represents the flow metadata and definition as it existed at the time the execution was started. Note that even if the flow is edited after an execution starts, the snapshot connected to the execution remains unchanged. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module GetFlowExecutionRequest =
  struct
    type nonrec t =
      {
      executionIdentifier: FlowExecutionIdentifier.t
        [@ocaml.doc
          "The unique identifier of the flow execution to retrieve."];
      flowAliasIdentifier: FlowAliasIdentifier.t
        [@ocaml.doc
          "The unique identifier of the flow alias used for the execution."];
      flowIdentifier: FlowIdentifier.t
        [@ocaml.doc "The unique identifier of the flow."]}
    let context_ = "GetFlowExecutionRequest"
    let make ~executionIdentifier =
      fun ~flowAliasIdentifier ->
        fun ~flowIdentifier ->
          fun () ->
            { executionIdentifier; flowAliasIdentifier; flowIdentifier }
    let to_value x =
      structure_to_value
        [("executionIdentifier",
           (Some (FlowExecutionIdentifier.to_value x.executionIdentifier)));
        ("flowAliasIdentifier",
          (Some (FlowAliasIdentifier.to_value x.flowAliasIdentifier)));
        ("flowIdentifier", (Some (FlowIdentifier.to_value x.flowIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flowIdentifier =
        FlowIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        FlowAliasIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowAliasIdentifier") in
      let executionIdentifier =
        FlowExecutionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "executionIdentifier") in
      make ~flowIdentifier ~flowAliasIdentifier ~executionIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let flowIdentifier =
        field_map_exn json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map_exn json__ "flowAliasIdentifier"
          FlowAliasIdentifier.of_json in
      let executionIdentifier =
        field_map_exn json__ "executionIdentifier"
          FlowExecutionIdentifier.of_json in
      make ~flowIdentifier ~flowAliasIdentifier ~executionIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves details about a specific flow execution, including its status, start and end times, and any errors that occurred during execution."]
module GetFlowExecutionResponse =
  struct
    type nonrec t =
      {
      endedAt: DateTimestamp.t option
        [@ocaml.doc
          "The timestamp when the flow execution ended. This field is only populated when the execution has completed, failed, timed out, or been aborted."];
      errors: FlowExecutionErrors.t option
        [@ocaml.doc
          "A list of errors that occurred during the flow execution. Each error includes an error code, message, and the node where the error occurred, if applicable."];
      executionArn: FlowExecutionIdentifier.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies the flow execution."];
      flowAliasIdentifier: FlowAliasIdentifier.t option
        [@ocaml.doc
          "The unique identifier of the flow alias used for the execution."];
      flowIdentifier: FlowIdentifier.t option
        [@ocaml.doc "The unique identifier of the flow."];
      flowVersion: Version.t option
        [@ocaml.doc "The version of the flow used for the execution."];
      startedAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp when the flow execution started."];
      status: FlowExecutionStatus.t option
        [@ocaml.doc
          "The current status of the flow execution. Flow executions time out after 24 hours."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?endedAt =
      fun ?errors ->
        fun ?executionArn ->
          fun ?flowAliasIdentifier ->
            fun ?flowIdentifier ->
              fun ?flowVersion ->
                fun ?startedAt ->
                  fun ?status ->
                    fun () ->
                      {
                        endedAt;
                        errors;
                        executionArn;
                        flowAliasIdentifier;
                        flowIdentifier;
                        flowVersion;
                        startedAt;
                        status
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("endedAt", (Option.map x.endedAt ~f:DateTimestamp.to_value));
        ("errors", (Option.map x.errors ~f:FlowExecutionErrors.to_value));
        ("executionArn",
          (Option.map x.executionArn ~f:FlowExecutionIdentifier.to_value));
        ("flowAliasIdentifier",
          (Option.map x.flowAliasIdentifier ~f:FlowAliasIdentifier.to_value));
        ("flowIdentifier",
          (Option.map x.flowIdentifier ~f:FlowIdentifier.to_value));
        ("flowVersion", (Option.map x.flowVersion ~f:Version.to_value));
        ("startedAt", (Option.map x.startedAt ~f:DateTimestamp.to_value));
        ("status", (Option.map x.status ~f:FlowExecutionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:FlowExecutionStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let startedAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "startedAt") in
      let flowVersion =
        (Option.map ~f:Version.of_xml) (Xml.child xml_arg0 "flowVersion") in
      let flowIdentifier =
        (Option.map ~f:FlowIdentifier.of_xml)
          (Xml.child xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        (Option.map ~f:FlowAliasIdentifier.of_xml)
          (Xml.child xml_arg0 "flowAliasIdentifier") in
      let executionArn =
        (Option.map ~f:FlowExecutionIdentifier.of_xml)
          (Xml.child xml_arg0 "executionArn") in
      let errors =
        (Option.map ~f:FlowExecutionErrors.of_xml)
          (Xml.child xml_arg0 "errors") in
      let endedAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "endedAt") in
      make ?status ?startedAt ?flowVersion ?flowIdentifier
        ?flowAliasIdentifier ?executionArn ?errors ?endedAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" FlowExecutionStatus.of_json in
      let startedAt = field_map json__ "startedAt" DateTimestamp.of_json in
      let flowVersion = field_map json__ "flowVersion" Version.of_json in
      let flowIdentifier =
        field_map json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map json__ "flowAliasIdentifier" FlowAliasIdentifier.of_json in
      let executionArn =
        field_map json__ "executionArn" FlowExecutionIdentifier.of_json in
      let errors = field_map json__ "errors" FlowExecutionErrors.of_json in
      let endedAt = field_map json__ "endedAt" DateTimestamp.of_json in
      make ?status ?startedAt ?flowVersion ?flowIdentifier
        ?flowAliasIdentifier ?executionArn ?errors ?endedAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves details about a specific flow execution, including its status, start and end times, and any errors that occurred during execution."]
module InvocationIdentifier =
  struct
    type nonrec t = string
    let context_ = "InvocationIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InvocationIdentifier" j
    let to_json = simple_to_json to_value
  end
module GetInvocationStepRequest =
  struct
    type nonrec t =
      {
      invocationIdentifier: InvocationIdentifier.t
        [@ocaml.doc
          "The unique identifier for the invocation in UUID format."];
      invocationStepId: Uuid.t
        [@ocaml.doc
          "The unique identifier (in UUID format) for the specific invocation step to retrieve."];
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "The unique identifier for the invocation step's associated session. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."]}
    let context_ = "GetInvocationStepRequest"
    let make ~invocationIdentifier =
      fun ~invocationStepId ->
        fun ~sessionIdentifier ->
          fun () ->
            { invocationIdentifier; invocationStepId; sessionIdentifier }
    let to_value x =
      structure_to_value
        [("invocationIdentifier",
           (Some (InvocationIdentifier.to_value x.invocationIdentifier)));
        ("invocationStepId", (Some (Uuid.to_value x.invocationStepId)));
        ("sessionIdentifier",
          (Some (SessionIdentifier.to_value x.sessionIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      let invocationStepId =
        Uuid.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "invocationStepId") in
      let invocationIdentifier =
        InvocationIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "invocationIdentifier") in
      make ~sessionIdentifier ~invocationStepId ~invocationIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      let invocationStepId =
        field_map_exn json__ "invocationStepId" Uuid.of_json in
      let invocationIdentifier =
        field_map_exn json__ "invocationIdentifier"
          InvocationIdentifier.of_json in
      make ~sessionIdentifier ~invocationStepId ~invocationIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the details of a specific invocation step within an invocation in a session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module InvocationStepPayload =
  struct
    type nonrec t =
      {
      contentBlocks: BedrockSessionContentBlocks.t option
        [@ocaml.doc "The content for the invocation step."]}
    let make ?contentBlocks = fun () -> { contentBlocks }
    let to_value x =
      structure_to_value
        [("contentBlocks",
           (Option.map x.contentBlocks
              ~f:BedrockSessionContentBlocks.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contentBlocks =
        (Option.map ~f:BedrockSessionContentBlocks.of_xml)
          (Xml.child xml_arg0 "contentBlocks") in
      make ?contentBlocks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contentBlocks =
        field_map json__ "contentBlocks" BedrockSessionContentBlocks.of_json in
      make ?contentBlocks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Payload content, such as text and images, for the invocation step."]
module InvocationStep =
  struct
    type nonrec t =
      {
      invocationId: Uuid.t option
        [@ocaml.doc
          "The unique identifier (in UUID format) for the invocation that includes the invocation step."];
      invocationStepId: Uuid.t option
        [@ocaml.doc
          "The unique identifier (in UUID format) for the invocation step."];
      invocationStepTime: DateTimestamp.t option
        [@ocaml.doc
          "The timestamp for when the invocation step was created."];
      payload: InvocationStepPayload.t option
        [@ocaml.doc
          "Payload content, such as text and images, for the invocation step."];
      sessionId: Uuid.t option
        [@ocaml.doc
          "The unique identifier of the session containing the invocation step."]}
    let make ?invocationId =
      fun ?invocationStepId ->
        fun ?invocationStepTime ->
          fun ?payload ->
            fun ?sessionId ->
              fun () ->
                {
                  invocationId;
                  invocationStepId;
                  invocationStepTime;
                  payload;
                  sessionId
                }
    let to_value x =
      structure_to_value
        [("invocationId", (Option.map x.invocationId ~f:Uuid.to_value));
        ("invocationStepId",
          (Option.map x.invocationStepId ~f:Uuid.to_value));
        ("invocationStepTime",
          (Option.map x.invocationStepTime ~f:DateTimestamp.to_value));
        ("payload", (Option.map x.payload ~f:InvocationStepPayload.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let payload =
        (Option.map ~f:InvocationStepPayload.of_xml)
          (Xml.child xml_arg0 "payload") in
      let invocationStepTime =
        (Option.map ~f:DateTimestamp.of_xml)
          (Xml.child xml_arg0 "invocationStepTime") in
      let invocationStepId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationStepId") in
      let invocationId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationId") in
      make ?sessionId ?payload ?invocationStepTime ?invocationStepId
        ?invocationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let payload = field_map json__ "payload" InvocationStepPayload.of_json in
      let invocationStepTime =
        field_map json__ "invocationStepTime" DateTimestamp.of_json in
      let invocationStepId = field_map json__ "invocationStepId" Uuid.of_json in
      let invocationId = field_map json__ "invocationId" Uuid.of_json in
      make ?sessionId ?payload ?invocationStepTime ?invocationStepId
        ?invocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stores fine-grained state checkpoints, including text and images, for each interaction in an invocation in a session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module GetInvocationStepResponse =
  struct
    type nonrec t =
      {
      invocationStep: InvocationStep.t option
        [@ocaml.doc "The complete details of the requested invocation step."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?invocationStep = fun () -> { invocationStep }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("invocationStep",
           (Option.map x.invocationStep ~f:InvocationStep.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationStep =
        (Option.map ~f:InvocationStep.of_xml)
          (Xml.child xml_arg0 "invocationStep") in
      make ?invocationStep ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invocationStep =
        field_map json__ "invocationStep" InvocationStep.of_json in
      make ?invocationStep ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the details of a specific invocation step within an invocation in a session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module GetSessionRequest =
  struct
    type nonrec t =
      {
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "A unique identifier for the session to retrieve. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."]}
    let context_ = "GetSessionRequest"
    let make ~sessionIdentifier = fun () -> { sessionIdentifier }
    let to_value x =
      structure_to_value
        [("sessionIdentifier",
           (Some (SessionIdentifier.to_value x.sessionIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      make ~sessionIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      make ~sessionIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves details about a specific session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module GetSessionResponse =
  struct
    type nonrec t =
      {
      createdAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the session was created."];
      encryptionKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Key Management Service key used to encrypt the session data. For more information, see Amazon Bedrock session encryption."];
      lastUpdatedAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the session was last modified."];
      sessionArn: SessionArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the session."];
      sessionId: Uuid.t option
        [@ocaml.doc "The unique identifier for the session in UUID format."];
      sessionMetadata: SessionMetadataMap.t option
        [@ocaml.doc
          "A map of key-value pairs containing attributes persisted across the session."];
      sessionStatus: SessionStatus.t option
        [@ocaml.doc "The current status of the session."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?createdAt =
      fun ?encryptionKeyArn ->
        fun ?lastUpdatedAt ->
          fun ?sessionArn ->
            fun ?sessionId ->
              fun ?sessionMetadata ->
                fun ?sessionStatus ->
                  fun () ->
                    {
                      createdAt;
                      encryptionKeyArn;
                      lastUpdatedAt;
                      sessionArn;
                      sessionId;
                      sessionMetadata;
                      sessionStatus
                    }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("createdAt", (Option.map x.createdAt ~f:DateTimestamp.to_value));
        ("encryptionKeyArn",
          (Option.map x.encryptionKeyArn ~f:KmsKeyArn.to_value));
        ("lastUpdatedAt",
          (Option.map x.lastUpdatedAt ~f:DateTimestamp.to_value));
        ("sessionArn", (Option.map x.sessionArn ~f:SessionArn.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value));
        ("sessionMetadata",
          (Option.map x.sessionMetadata ~f:SessionMetadataMap.to_value));
        ("sessionStatus",
          (Option.map x.sessionStatus ~f:SessionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionStatus =
        (Option.map ~f:SessionStatus.of_xml)
          (Xml.child xml_arg0 "sessionStatus") in
      let sessionMetadata =
        (Option.map ~f:SessionMetadataMap.of_xml)
          (Xml.child xml_arg0 "sessionMetadata") in
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionArn =
        (Option.map ~f:SessionArn.of_xml) (Xml.child xml_arg0 "sessionArn") in
      let lastUpdatedAt =
        (Option.map ~f:DateTimestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdatedAt") in
      let encryptionKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml)
          (Xml.child xml_arg0 "encryptionKeyArn") in
      let createdAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      make ?sessionStatus ?sessionMetadata ?sessionId ?sessionArn
        ?lastUpdatedAt ?encryptionKeyArn ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionStatus =
        field_map json__ "sessionStatus" SessionStatus.of_json in
      let sessionMetadata =
        field_map json__ "sessionMetadata" SessionMetadataMap.of_json in
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let sessionArn = field_map json__ "sessionArn" SessionArn.of_json in
      let lastUpdatedAt =
        field_map json__ "lastUpdatedAt" DateTimestamp.of_json in
      let encryptionKeyArn =
        field_map json__ "encryptionKeyArn" KmsKeyArn.of_json in
      let createdAt = field_map json__ "createdAt" DateTimestamp.of_json in
      make ?sessionStatus ?sessionMetadata ?sessionId ?sessionArn
        ?lastUpdatedAt ?encryptionKeyArn ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves details about a specific session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module GuadrailAction =
  struct
    type nonrec t =
      | INTERVENED 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERVENED -> "INTERVENED"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERVENED" -> INTERVENED
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GuadrailAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GuadrailAction" j)
    let to_json = simple_to_json to_value
  end
module GuardrailEvent =
  struct
    type nonrec t =
      {
      action: GuadrailAction.t option [@ocaml.doc "The guardrail action."]}
    let make ?action = fun () -> { action }
    let to_value x =
      structure_to_value
        [("action", (Option.map x.action ~f:GuadrailAction.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let action =
        (Option.map ~f:GuadrailAction.of_xml) (Xml.child xml_arg0 "action") in
      make ?action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let action = field_map json__ "action" GuadrailAction.of_json in
      make ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A guardrail event."]
module InlineAgentFilePart =
  struct
    type nonrec t =
      {
      files: OutputFiles.t option
        [@ocaml.doc "Files containing intermediate response for the user."]}
    let make ?files = fun () -> { files }
    let to_value x =
      structure_to_value
        [("files", (Option.map x.files ~f:OutputFiles.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let files =
        (Option.map ~f:OutputFiles.of_xml) (Xml.child xml_arg0 "files") in
      make ?files ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let files = field_map json__ "files" OutputFiles.of_json in
      make ?files ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains intermediate response for code interpreter if any files have been generated."]
module PartBody =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module InlineAgentPayloadPart =
  struct
    type nonrec t =
      {
      attribution: Attribution.t option
        [@ocaml.doc "Contains citations for a part of an agent response."];
      bytes: PartBody.t option
        [@ocaml.doc "A part of the agent response in bytes."]}
    let make ?attribution = fun ?bytes -> fun () -> { attribution; bytes }
    let to_value x =
      structure_to_value
        [("attribution", (Option.map x.attribution ~f:Attribution.to_value));
        ("bytes", (Option.map x.bytes ~f:PartBody.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytes =
        (Option.map ~f:PartBody.of_xml) (Xml.child xml_arg0 "bytes") in
      let attribution =
        (Option.map ~f:Attribution.of_xml) (Xml.child xml_arg0 "attribution") in
      make ?bytes ?attribution ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bytes = field_map json__ "bytes" PartBody.of_json in
      let attribution = field_map json__ "attribution" Attribution.of_json in
      make ?bytes ?attribution ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains a part of an agent response and citations for it."]
module InlineAgentTracePart =
  struct
    type nonrec t =
      {
      callerChain: CallerChain.t option
        [@ocaml.doc "The caller chain for the trace part."];
      collaboratorName: Name.t option
        [@ocaml.doc "The collaborator name for the trace part."];
      eventTime: SyntheticTimestamp_date_time.t option
        [@ocaml.doc "The time that trace occurred."];
      sessionId: SessionId.t option
        [@ocaml.doc "The unique identifier of the session with the agent."];
      trace: Trace.t option
        [@ocaml.doc
          "Contains one part of the agent's reasoning process and results from calling API actions and querying knowledge bases. You can use the trace to understand how the agent arrived at the response it provided the customer. For more information, see Trace enablement."]}
    let make ?callerChain =
      fun ?collaboratorName ->
        fun ?eventTime ->
          fun ?sessionId ->
            fun ?trace ->
              fun () ->
                { callerChain; collaboratorName; eventTime; sessionId; trace
                }
    let to_value x =
      structure_to_value
        [("callerChain", (Option.map x.callerChain ~f:CallerChain.to_value));
        ("collaboratorName",
          (Option.map x.collaboratorName ~f:Name.to_value));
        ("eventTime",
          (Option.map x.eventTime ~f:SyntheticTimestamp_date_time.to_value));
        ("sessionId", (Option.map x.sessionId ~f:SessionId.to_value));
        ("trace", (Option.map x.trace ~f:Trace.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trace = (Option.map ~f:Trace.of_xml) (Xml.child xml_arg0 "trace") in
      let sessionId =
        (Option.map ~f:SessionId.of_xml) (Xml.child xml_arg0 "sessionId") in
      let eventTime =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "eventTime") in
      let collaboratorName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "collaboratorName") in
      let callerChain =
        (Option.map ~f:CallerChain.of_xml) (Xml.child xml_arg0 "callerChain") in
      make ?trace ?sessionId ?eventTime ?collaboratorName ?callerChain ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trace = field_map json__ "trace" Trace.of_json in
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let eventTime =
        field_map json__ "eventTime" SyntheticTimestamp_date_time.of_json in
      let collaboratorName = field_map json__ "collaboratorName" Name.of_json in
      let callerChain = field_map json__ "callerChain" CallerChain.of_json in
      make ?trace ?sessionId ?eventTime ?collaboratorName ?callerChain ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the agent and session, alongside the agent's reasoning process and results from calling API actions and querying knowledge bases and metadata about the trace. You can use the trace to understand how the agent arrived at the response it provided the customer. For more information, see Trace enablement."]
module InlineAgentReturnControlPayload =
  struct
    type nonrec t =
      {
      invocationId: String_.t option
        [@ocaml.doc "The identifier of the action group invocation."];
      invocationInputs: InvocationInputs.t option
        [@ocaml.doc
          "A list of objects that contain information about the parameters and inputs that need to be sent into the API operation or function, based on what the agent determines from its session with the user."]}
    let make ?invocationId =
      fun ?invocationInputs -> fun () -> { invocationId; invocationInputs }
    let to_value x =
      structure_to_value
        [("invocationId", (Option.map x.invocationId ~f:String_.to_value));
        ("invocationInputs",
          (Option.map x.invocationInputs ~f:InvocationInputs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationInputs =
        (Option.map ~f:InvocationInputs.of_xml)
          (Xml.child xml_arg0 "invocationInputs") in
      let invocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "invocationId") in
      make ?invocationInputs ?invocationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invocationInputs =
        field_map json__ "invocationInputs" InvocationInputs.of_json in
      let invocationId = field_map json__ "invocationId" String_.of_json in
      make ?invocationInputs ?invocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information to return from the action group that the agent has predicted to invoke. This data type is used in the InvokeAgent response API operation."]
module InlineAgentResponseStream =
  struct
    type nonrec t =
      {
      accessDeniedException: AccessDeniedException.t option
        [@ocaml.doc
          "The request is denied because of missing access permissions. Check your permissions and retry your request."];
      badGatewayException: BadGatewayException.t option
        [@ocaml.doc
          "There was an issue with a dependency due to a server issue. Retry your request."];
      chunk: InlineAgentPayloadPart.t option
        [@ocaml.doc
          "Contains a part of an agent response and citations for it."];
      conflictException: ConflictException.t option
        [@ocaml.doc
          "There was a conflict performing an operation. Resolve the conflict and retry your request."];
      dependencyFailedException: DependencyFailedException.t option
        [@ocaml.doc
          "There was an issue with a dependency. Check the resource configurations and retry the request."];
      files: InlineAgentFilePart.t option
        [@ocaml.doc
          "Contains intermediate response for code interpreter if any files have been generated."];
      internalServerException: InternalServerException.t option
        [@ocaml.doc "An internal server error occurred. Retry your request."];
      resourceNotFoundException: ResourceNotFoundException.t option
        [@ocaml.doc
          "The specified resource Amazon Resource Name (ARN) was not found. Check the Amazon Resource Name (ARN) and try your request again."];
      returnControl: InlineAgentReturnControlPayload.t option
        [@ocaml.doc
          "Contains the parameters and information that the agent elicited from the customer to carry out an action. This information is returned to the system and can be used in your own setup for fulfilling the action."];
      serviceQuotaExceededException: ServiceQuotaExceededException.t option
        [@ocaml.doc
          "The number of requests exceeds the service quota. Resubmit your request later."];
      throttlingException: ThrottlingException.t option
        [@ocaml.doc
          "The number of requests exceeds the limit. Resubmit your request later."];
      trace: InlineAgentTracePart.t option
        [@ocaml.doc
          "Contains information about the agent and session, alongside the agent's reasoning process and results from calling actions and querying knowledge bases and metadata about the trace. You can use the trace to understand how the agent arrived at the response it provided the customer. For more information, see Trace events."];
      validationException: ValidationException.t option
        [@ocaml.doc
          "Input validation failed. Check your request parameters and retry the request."]}
    let make ?accessDeniedException =
      fun ?badGatewayException ->
        fun ?chunk ->
          fun ?conflictException ->
            fun ?dependencyFailedException ->
              fun ?files ->
                fun ?internalServerException ->
                  fun ?resourceNotFoundException ->
                    fun ?returnControl ->
                      fun ?serviceQuotaExceededException ->
                        fun ?throttlingException ->
                          fun ?trace ->
                            fun ?validationException ->
                              fun () ->
                                {
                                  accessDeniedException;
                                  badGatewayException;
                                  chunk;
                                  conflictException;
                                  dependencyFailedException;
                                  files;
                                  internalServerException;
                                  resourceNotFoundException;
                                  returnControl;
                                  serviceQuotaExceededException;
                                  throttlingException;
                                  trace;
                                  validationException
                                }
    let to_value x =
      structure_to_value
        [("accessDeniedException",
           (Option.map x.accessDeniedException
              ~f:AccessDeniedException.to_value));
        ("badGatewayException",
          (Option.map x.badGatewayException ~f:BadGatewayException.to_value));
        ("chunk", (Option.map x.chunk ~f:InlineAgentPayloadPart.to_value));
        ("conflictException",
          (Option.map x.conflictException ~f:ConflictException.to_value));
        ("dependencyFailedException",
          (Option.map x.dependencyFailedException
             ~f:DependencyFailedException.to_value));
        ("files", (Option.map x.files ~f:InlineAgentFilePart.to_value));
        ("internalServerException",
          (Option.map x.internalServerException
             ~f:InternalServerException.to_value));
        ("resourceNotFoundException",
          (Option.map x.resourceNotFoundException
             ~f:ResourceNotFoundException.to_value));
        ("returnControl",
          (Option.map x.returnControl
             ~f:InlineAgentReturnControlPayload.to_value));
        ("serviceQuotaExceededException",
          (Option.map x.serviceQuotaExceededException
             ~f:ServiceQuotaExceededException.to_value));
        ("throttlingException",
          (Option.map x.throttlingException ~f:ThrottlingException.to_value));
        ("trace", (Option.map x.trace ~f:InlineAgentTracePart.to_value));
        ("validationException",
          (Option.map x.validationException ~f:ValidationException.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validationException =
        (Option.map ~f:ValidationException.of_xml)
          (Xml.child xml_arg0 "validationException") in
      let trace =
        (Option.map ~f:InlineAgentTracePart.of_xml)
          (Xml.child xml_arg0 "trace") in
      let throttlingException =
        (Option.map ~f:ThrottlingException.of_xml)
          (Xml.child xml_arg0 "throttlingException") in
      let serviceQuotaExceededException =
        (Option.map ~f:ServiceQuotaExceededException.of_xml)
          (Xml.child xml_arg0 "serviceQuotaExceededException") in
      let returnControl =
        (Option.map ~f:InlineAgentReturnControlPayload.of_xml)
          (Xml.child xml_arg0 "returnControl") in
      let resourceNotFoundException =
        (Option.map ~f:ResourceNotFoundException.of_xml)
          (Xml.child xml_arg0 "resourceNotFoundException") in
      let internalServerException =
        (Option.map ~f:InternalServerException.of_xml)
          (Xml.child xml_arg0 "internalServerException") in
      let files =
        (Option.map ~f:InlineAgentFilePart.of_xml)
          (Xml.child xml_arg0 "files") in
      let dependencyFailedException =
        (Option.map ~f:DependencyFailedException.of_xml)
          (Xml.child xml_arg0 "dependencyFailedException") in
      let conflictException =
        (Option.map ~f:ConflictException.of_xml)
          (Xml.child xml_arg0 "conflictException") in
      let chunk =
        (Option.map ~f:InlineAgentPayloadPart.of_xml)
          (Xml.child xml_arg0 "chunk") in
      let badGatewayException =
        (Option.map ~f:BadGatewayException.of_xml)
          (Xml.child xml_arg0 "badGatewayException") in
      let accessDeniedException =
        (Option.map ~f:AccessDeniedException.of_xml)
          (Xml.child xml_arg0 "accessDeniedException") in
      make ?validationException ?trace ?throttlingException
        ?serviceQuotaExceededException ?returnControl
        ?resourceNotFoundException ?internalServerException ?files
        ?dependencyFailedException ?conflictException ?chunk
        ?badGatewayException ?accessDeniedException ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let validationException =
        field_map json__ "validationException" ValidationException.of_json in
      let trace = field_map json__ "trace" InlineAgentTracePart.of_json in
      let throttlingException =
        field_map json__ "throttlingException" ThrottlingException.of_json in
      let serviceQuotaExceededException =
        field_map json__ "serviceQuotaExceededException"
          ServiceQuotaExceededException.of_json in
      let returnControl =
        field_map json__ "returnControl"
          InlineAgentReturnControlPayload.of_json in
      let resourceNotFoundException =
        field_map json__ "resourceNotFoundException"
          ResourceNotFoundException.of_json in
      let internalServerException =
        field_map json__ "internalServerException"
          InternalServerException.of_json in
      let files = field_map json__ "files" InlineAgentFilePart.of_json in
      let dependencyFailedException =
        field_map json__ "dependencyFailedException"
          DependencyFailedException.of_json in
      let conflictException =
        field_map json__ "conflictException" ConflictException.of_json in
      let chunk = field_map json__ "chunk" InlineAgentPayloadPart.of_json in
      let badGatewayException =
        field_map json__ "badGatewayException" BadGatewayException.of_json in
      let accessDeniedException =
        field_map json__ "accessDeniedException"
          AccessDeniedException.of_json in
      make ?validationException ?trace ?throttlingException
        ?serviceQuotaExceededException ?returnControl
        ?resourceNotFoundException ?internalServerException ?files
        ?dependencyFailedException ?conflictException ?chunk
        ?badGatewayException ?accessDeniedException ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The response from invoking the agent and associated citations and trace information."]
module InlineBedrockModelConfigurations =
  struct
    type nonrec t =
      {
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "The latency configuration for the model."]}
    let make ?performanceConfig = fun () -> { performanceConfig }
    let to_value x =
      structure_to_value
        [("performanceConfig",
           (Option.map x.performanceConfig
              ~f:PerformanceConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      make ?performanceConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      make ?performanceConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Settings for a model called with InvokeInlineAgent."]
module SessionAttributesMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module PromptSessionAttributesMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module InputFile =
  struct
    type nonrec t =
      {
      name: String_.t [@ocaml.doc "The name of the source file."];
      source: FileSource.t
        [@ocaml.doc "Specifies where the files are located."];
      useCase: FileUseCase.t
        [@ocaml.doc
          "Specifies how the source files will be used by the code interpreter."]}
    let context_ = "InputFile"
    let make ~name =
      fun ~source -> fun ~useCase -> fun () -> { name; source; useCase }
    let to_value x =
      structure_to_value
        [("name", (Some (String_.to_value x.name)));
        ("source", (Some (FileSource.to_value x.source)));
        ("useCase", (Some (FileUseCase.to_value x.useCase)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let useCase =
        FileUseCase.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "useCase") in
      let source =
        FileSource.of_xml (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~useCase ~source ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let useCase = field_map_exn json__ "useCase" FileUseCase.of_json in
      let source = field_map_exn json__ "source" FileSource.of_json in
      let name = field_map_exn json__ "name" String_.of_json in
      make ~useCase ~source ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains details of the source files."]
module InputFiles =
  struct
    type nonrec t = InputFile.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:InputFile.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InputFile.of_xml)
    let of_json j =
      list_of_json ~kind:"InputFiles" ~of_json:InputFile.of_json j
    let to_json v = composed_to_json to_value v
  end
module InlineSessionState =
  struct
    type nonrec t =
      {
      conversationHistory: ConversationHistory.t option
        [@ocaml.doc
          "Contains the conversation history that persist across sessions."];
      files: InputFiles.t option
        [@ocaml.doc
          "Contains information about the files used by code interpreter."];
      invocationId: String_.t option
        [@ocaml.doc
          "The identifier of the invocation of an action. This value must match the invocationId returned in the InvokeInlineAgent response for the action whose results are provided in the returnControlInvocationResults field. For more information, see Return control to the agent developer."];
      promptSessionAttributes: PromptSessionAttributesMap.t option
        [@ocaml.doc
          "Contains attributes that persist across a session and the values of those attributes."];
      returnControlInvocationResults: ReturnControlInvocationResults.t option
        [@ocaml.doc
          "Contains information about the results from the action group invocation. For more information, see Return control to the agent developer. If you include this field in the sessionState field, the inputText field will be ignored."];
      sessionAttributes: SessionAttributesMap.t option
        [@ocaml.doc
          "Contains attributes that persist across a session and the values of those attributes."]}
    let make ?conversationHistory =
      fun ?files ->
        fun ?invocationId ->
          fun ?promptSessionAttributes ->
            fun ?returnControlInvocationResults ->
              fun ?sessionAttributes ->
                fun () ->
                  {
                    conversationHistory;
                    files;
                    invocationId;
                    promptSessionAttributes;
                    returnControlInvocationResults;
                    sessionAttributes
                  }
    let to_value x =
      structure_to_value
        [("conversationHistory",
           (Option.map x.conversationHistory ~f:ConversationHistory.to_value));
        ("files", (Option.map x.files ~f:InputFiles.to_value));
        ("invocationId", (Option.map x.invocationId ~f:String_.to_value));
        ("promptSessionAttributes",
          (Option.map x.promptSessionAttributes
             ~f:PromptSessionAttributesMap.to_value));
        ("returnControlInvocationResults",
          (Option.map x.returnControlInvocationResults
             ~f:ReturnControlInvocationResults.to_value));
        ("sessionAttributes",
          (Option.map x.sessionAttributes ~f:SessionAttributesMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionAttributes =
        (Option.map ~f:SessionAttributesMap.of_xml)
          (Xml.child xml_arg0 "sessionAttributes") in
      let returnControlInvocationResults =
        (Option.map ~f:ReturnControlInvocationResults.of_xml)
          (Xml.child xml_arg0 "returnControlInvocationResults") in
      let promptSessionAttributes =
        (Option.map ~f:PromptSessionAttributesMap.of_xml)
          (Xml.child xml_arg0 "promptSessionAttributes") in
      let invocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "invocationId") in
      let files =
        (Option.map ~f:InputFiles.of_xml) (Xml.child xml_arg0 "files") in
      let conversationHistory =
        (Option.map ~f:ConversationHistory.of_xml)
          (Xml.child xml_arg0 "conversationHistory") in
      make ?sessionAttributes ?returnControlInvocationResults
        ?promptSessionAttributes ?invocationId ?files ?conversationHistory ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionAttributes =
        field_map json__ "sessionAttributes" SessionAttributesMap.of_json in
      let returnControlInvocationResults =
        field_map json__ "returnControlInvocationResults"
          ReturnControlInvocationResults.of_json in
      let promptSessionAttributes =
        field_map json__ "promptSessionAttributes"
          PromptSessionAttributesMap.of_json in
      let invocationId = field_map json__ "invocationId" String_.of_json in
      let files = field_map json__ "files" InputFiles.of_json in
      let conversationHistory =
        field_map json__ "conversationHistory" ConversationHistory.of_json in
      make ?sessionAttributes ?returnControlInvocationResults
        ?promptSessionAttributes ?invocationId ?files ?conversationHistory ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains parameters that specify various attributes that persist across a session or prompt. You can define session state attributes as key-value pairs when writing a Lambda function for an action group or pass them when making an InvokeInlineAgent request. Use session state attributes to control and provide conversational context for your inline agent and to help customize your agent's behavior. For more information, see Control session context"]
module InputImageInlineContentBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module InputImageFormat =
  struct
    type nonrec t =
      | Png 
      | Jpeg 
      | Gif 
      | Webp 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Png -> "png"
      | Jpeg -> "jpeg"
      | Gif -> "gif"
      | Webp -> "webp"
      | Non_static_id s -> s
    let of_string =
      function
      | "png" -> Png
      | "jpeg" -> Jpeg
      | "gif" -> Gif
      | "webp" -> Webp
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InputImageFormat" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InputImageFormat" j)
    let to_json = simple_to_json to_value
  end
module InputImage =
  struct
    type nonrec t =
      {
      format: InputImageFormat.t
        [@ocaml.doc
          "The format of the input image. Supported formats include png, gif, jpeg, and webp."];
      inlineContent: InputImageInlineContentBlob.t
        [@ocaml.doc
          "The base64-encoded image data for inline image content. Maximum size is 5MB."]}
    let context_ = "InputImage"
    let make ~format =
      fun ~inlineContent -> fun () -> { format; inlineContent }
    let to_value x =
      structure_to_value
        [("format", (Some (InputImageFormat.to_value x.format)));
        ("inlineContent",
          (Some (InputImageInlineContentBlob.to_value x.inlineContent)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inlineContent =
        InputImageInlineContentBlob.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "inlineContent") in
      let format =
        InputImageFormat.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "format") in
      make ~inlineContent ~format ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let inlineContent =
        field_map_exn json__ "inlineContent"
          InputImageInlineContentBlob.of_json in
      let format = field_map_exn json__ "format" InputImageFormat.of_json in
      make ~inlineContent ~format ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the image data for multimodal knowledge base queries, including format and content. This data type is used in the following API operations: Retrieve request \226\128\147 in the image field"]
module TextPromptTextString =
  struct
    type nonrec t = string
    let context_ = "TextPromptTextString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TextPromptTextString" j
    let to_json = simple_to_json to_value
  end
module TextPrompt =
  struct
    type nonrec t =
      {
      text: TextPromptTextString.t
        [@ocaml.doc "The text in the text prompt to optimize."]}
    let context_ = "TextPrompt"
    let make ~text = fun () -> { text }
    let to_value x =
      structure_to_value
        [("text", (Some (TextPromptTextString.to_value x.text)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        TextPromptTextString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "text") in
      make ~text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map_exn json__ "text" TextPromptTextString.of_json in
      make ~text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about the text prompt to optimize."]
module InputPrompt =
  struct
    type nonrec t =
      {
      textPrompt: TextPrompt.t option
        [@ocaml.doc
          "Contains information about the text prompt to optimize."]}
    let make ?textPrompt = fun () -> { textPrompt }
    let to_value x =
      structure_to_value
        [("textPrompt", (Option.map x.textPrompt ~f:TextPrompt.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let textPrompt =
        (Option.map ~f:TextPrompt.of_xml) (Xml.child xml_arg0 "textPrompt") in
      make ?textPrompt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let textPrompt = field_map json__ "textPrompt" TextPrompt.of_json in
      make ?textPrompt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about the prompt to optimize."]
module InputText =
  struct
    type nonrec t = string
    let context_ = "InputText"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:25000000) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InputText" j
    let to_json = simple_to_json to_value
  end
module InvocationStepSummary =
  struct
    type nonrec t =
      {
      invocationId: Uuid.t option
        [@ocaml.doc "A unique identifier for the invocation in UUID format."];
      invocationStepId: Uuid.t option
        [@ocaml.doc
          "The unique identifier (in UUID format) for the invocation step."];
      invocationStepTime: DateTimestamp.t option
        [@ocaml.doc
          "The timestamp for when the invocation step was created."];
      sessionId: Uuid.t option
        [@ocaml.doc
          "The unique identifier for the session associated with the invocation step."]}
    let make ?invocationId =
      fun ?invocationStepId ->
        fun ?invocationStepTime ->
          fun ?sessionId ->
            fun () ->
              { invocationId; invocationStepId; invocationStepTime; sessionId
              }
    let to_value x =
      structure_to_value
        [("invocationId", (Option.map x.invocationId ~f:Uuid.to_value));
        ("invocationStepId",
          (Option.map x.invocationStepId ~f:Uuid.to_value));
        ("invocationStepTime",
          (Option.map x.invocationStepTime ~f:DateTimestamp.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let invocationStepTime =
        (Option.map ~f:DateTimestamp.of_xml)
          (Xml.child xml_arg0 "invocationStepTime") in
      let invocationStepId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationStepId") in
      let invocationId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationId") in
      make ?sessionId ?invocationStepTime ?invocationStepId ?invocationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let invocationStepTime =
        field_map json__ "invocationStepTime" DateTimestamp.of_json in
      let invocationStepId = field_map json__ "invocationStepId" Uuid.of_json in
      let invocationId = field_map json__ "invocationId" Uuid.of_json in
      make ?sessionId ?invocationStepTime ?invocationStepId ?invocationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about an invocation step within an invocation in a session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module InvocationStepSummaries =
  struct
    type nonrec t = InvocationStepSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:InvocationStepSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InvocationStepSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"InvocationStepSummaries"
        ~of_json:InvocationStepSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvocationSummary =
  struct
    type nonrec t =
      {
      createdAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the invocation was created."];
      invocationId: Uuid.t option
        [@ocaml.doc "A unique identifier for the invocation in UUID format."];
      sessionId: Uuid.t option
        [@ocaml.doc
          "The unique identifier for the session associated with the invocation."]}
    let make ?createdAt =
      fun ?invocationId ->
        fun ?sessionId -> fun () -> { createdAt; invocationId; sessionId }
    let to_value x =
      structure_to_value
        [("createdAt", (Option.map x.createdAt ~f:DateTimestamp.to_value));
        ("invocationId", (Option.map x.invocationId ~f:Uuid.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let invocationId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationId") in
      let createdAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      make ?sessionId ?invocationId ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let invocationId = field_map json__ "invocationId" Uuid.of_json in
      let createdAt = field_map json__ "createdAt" DateTimestamp.of_json in
      make ?sessionId ?invocationId ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about an invocation in a session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module InvocationSummaries =
  struct
    type nonrec t = InvocationSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:InvocationSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InvocationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"InvocationSummaries"
        ~of_json:InvocationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module StreamingConfigurationsApplyGuardrailIntervalInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for StreamingConfigurationsApplyGuardrailIntervalInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StreamingConfigurations =
  struct
    type nonrec t =
      {
      applyGuardrailInterval:
        StreamingConfigurationsApplyGuardrailIntervalInteger.t option
        [@ocaml.doc
          "The guardrail interval to apply as response is generated. By default, the guardrail interval is set to 50 characters. If a larger interval is specified, the response will be generated in larger chunks with fewer ApplyGuardrail calls. The following examples show the response generated for Hello, I am an agent input string. Example response in chunks: Interval set to 3 characters 'Hel', 'lo, ','I am', ' an', ' Age', 'nt' Each chunk has at least 3 characters except for the last chunk Example response in chunks: Interval set to 20 or more characters Hello, I am an Agent"];
      streamFinalResponse: Boolean.t option
        [@ocaml.doc
          "Specifies whether to enable streaming for the final response. This is set to false by default."]}
    let make ?applyGuardrailInterval =
      fun ?streamFinalResponse ->
        fun () -> { applyGuardrailInterval; streamFinalResponse }
    let to_value x =
      structure_to_value
        [("applyGuardrailInterval",
           (Option.map x.applyGuardrailInterval
              ~f:StreamingConfigurationsApplyGuardrailIntervalInteger.to_value));
        ("streamFinalResponse",
          (Option.map x.streamFinalResponse ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamFinalResponse =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "streamFinalResponse") in
      let applyGuardrailInterval =
        (Option.map
           ~f:StreamingConfigurationsApplyGuardrailIntervalInteger.of_xml)
          (Xml.child xml_arg0 "applyGuardrailInterval") in
      make ?streamFinalResponse ?applyGuardrailInterval ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streamFinalResponse =
        field_map json__ "streamFinalResponse" Boolean.of_json in
      let applyGuardrailInterval =
        field_map json__ "applyGuardrailInterval"
          StreamingConfigurationsApplyGuardrailIntervalInteger.of_json in
      make ?streamFinalResponse ?applyGuardrailInterval ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Configurations for streaming."]
module KnowledgeBaseConfiguration =
  struct
    type nonrec t =
      {
      knowledgeBaseId: KnowledgeBaseId.t
        [@ocaml.doc
          "The unique identifier for a knowledge base attached to the agent."];
      retrievalConfiguration: KnowledgeBaseRetrievalConfiguration.t
        [@ocaml.doc
          "The configurations to apply to the knowledge base during query. For more information, see Query configurations."]}
    let context_ = "KnowledgeBaseConfiguration"
    let make ~knowledgeBaseId =
      fun ~retrievalConfiguration ->
        fun () -> { knowledgeBaseId; retrievalConfiguration }
    let to_value x =
      structure_to_value
        [("knowledgeBaseId",
           (Some (KnowledgeBaseId.to_value x.knowledgeBaseId)));
        ("retrievalConfiguration",
          (Some
             (KnowledgeBaseRetrievalConfiguration.to_value
                x.retrievalConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retrievalConfiguration =
        KnowledgeBaseRetrievalConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "retrievalConfiguration") in
      let knowledgeBaseId =
        KnowledgeBaseId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "knowledgeBaseId") in
      make ~retrievalConfiguration ~knowledgeBaseId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retrievalConfiguration =
        field_map_exn json__ "retrievalConfiguration"
          KnowledgeBaseRetrievalConfiguration.of_json in
      let knowledgeBaseId =
        field_map_exn json__ "knowledgeBaseId" KnowledgeBaseId.of_json in
      make ~retrievalConfiguration ~knowledgeBaseId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configurations to apply to a knowledge base attached to the agent during query. For more information, see Knowledge base retrieval configurations."]
module KnowledgeBaseConfigurations =
  struct
    type nonrec t = KnowledgeBaseConfiguration.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:KnowledgeBaseConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KnowledgeBaseConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"KnowledgeBaseConfigurations"
        ~of_json:KnowledgeBaseConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module SessionState =
  struct
    type nonrec t =
      {
      conversationHistory: ConversationHistory.t option
        [@ocaml.doc "The state's conversation history."];
      files: InputFiles.t option
        [@ocaml.doc
          "Contains information about the files used by code interpreter."];
      invocationId: String_.t option
        [@ocaml.doc
          "The identifier of the invocation of an action. This value must match the invocationId returned in the InvokeAgent response for the action whose results are provided in the returnControlInvocationResults field. For more information, see Return control to the agent developer and Control session context."];
      knowledgeBaseConfigurations: KnowledgeBaseConfigurations.t option
        [@ocaml.doc
          "An array of configurations, each of which applies to a knowledge base attached to the agent."];
      promptSessionAttributes: PromptSessionAttributesMap.t option
        [@ocaml.doc
          "Contains attributes that persist across a prompt and the values of those attributes. In orchestration prompt template, these attributes replace the $prompt_session_attributes$ placeholder variable. For more information, see Prompt template placeholder variables. In multi-agent collaboration, the promptSessionAttributes will only be used by supervisor agent when $prompt_session_attributes$ is present in prompt template."];
      returnControlInvocationResults: ReturnControlInvocationResults.t option
        [@ocaml.doc
          "Contains information about the results from the action group invocation. For more information, see Return control to the agent developer and Control session context. If you include this field, the inputText field will be ignored."];
      sessionAttributes: SessionAttributesMap.t option
        [@ocaml.doc
          "Contains attributes that persist across a session and the values of those attributes. If sessionAttributes are passed to a supervisor agent in multi-agent collaboration, it will be forwarded to all agent collaborators."]}
    let make ?conversationHistory =
      fun ?files ->
        fun ?invocationId ->
          fun ?knowledgeBaseConfigurations ->
            fun ?promptSessionAttributes ->
              fun ?returnControlInvocationResults ->
                fun ?sessionAttributes ->
                  fun () ->
                    {
                      conversationHistory;
                      files;
                      invocationId;
                      knowledgeBaseConfigurations;
                      promptSessionAttributes;
                      returnControlInvocationResults;
                      sessionAttributes
                    }
    let to_value x =
      structure_to_value
        [("conversationHistory",
           (Option.map x.conversationHistory ~f:ConversationHistory.to_value));
        ("files", (Option.map x.files ~f:InputFiles.to_value));
        ("invocationId", (Option.map x.invocationId ~f:String_.to_value));
        ("knowledgeBaseConfigurations",
          (Option.map x.knowledgeBaseConfigurations
             ~f:KnowledgeBaseConfigurations.to_value));
        ("promptSessionAttributes",
          (Option.map x.promptSessionAttributes
             ~f:PromptSessionAttributesMap.to_value));
        ("returnControlInvocationResults",
          (Option.map x.returnControlInvocationResults
             ~f:ReturnControlInvocationResults.to_value));
        ("sessionAttributes",
          (Option.map x.sessionAttributes ~f:SessionAttributesMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionAttributes =
        (Option.map ~f:SessionAttributesMap.of_xml)
          (Xml.child xml_arg0 "sessionAttributes") in
      let returnControlInvocationResults =
        (Option.map ~f:ReturnControlInvocationResults.of_xml)
          (Xml.child xml_arg0 "returnControlInvocationResults") in
      let promptSessionAttributes =
        (Option.map ~f:PromptSessionAttributesMap.of_xml)
          (Xml.child xml_arg0 "promptSessionAttributes") in
      let knowledgeBaseConfigurations =
        (Option.map ~f:KnowledgeBaseConfigurations.of_xml)
          (Xml.child xml_arg0 "knowledgeBaseConfigurations") in
      let invocationId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "invocationId") in
      let files =
        (Option.map ~f:InputFiles.of_xml) (Xml.child xml_arg0 "files") in
      let conversationHistory =
        (Option.map ~f:ConversationHistory.of_xml)
          (Xml.child xml_arg0 "conversationHistory") in
      make ?sessionAttributes ?returnControlInvocationResults
        ?promptSessionAttributes ?knowledgeBaseConfigurations ?invocationId
        ?files ?conversationHistory ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionAttributes =
        field_map json__ "sessionAttributes" SessionAttributesMap.of_json in
      let returnControlInvocationResults =
        field_map json__ "returnControlInvocationResults"
          ReturnControlInvocationResults.of_json in
      let promptSessionAttributes =
        field_map json__ "promptSessionAttributes"
          PromptSessionAttributesMap.of_json in
      let knowledgeBaseConfigurations =
        field_map json__ "knowledgeBaseConfigurations"
          KnowledgeBaseConfigurations.of_json in
      let invocationId = field_map json__ "invocationId" String_.of_json in
      let files = field_map json__ "files" InputFiles.of_json in
      let conversationHistory =
        field_map json__ "conversationHistory" ConversationHistory.of_json in
      make ?sessionAttributes ?returnControlInvocationResults
        ?promptSessionAttributes ?knowledgeBaseConfigurations ?invocationId
        ?files ?conversationHistory ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains parameters that specify various attributes that persist across a session or prompt. You can define session state attributes as key-value pairs when writing a Lambda function for an action group or pass them when making an InvokeAgent request. Use session state attributes to control and provide conversational context for your agent and to help customize your agent's behavior. For more information, see Control session context."]
module PromptCreationConfigurationsPreviousConversationTurnsToIncludeInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for PromptCreationConfigurationsPreviousConversationTurnsToIncludeInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PromptCreationConfigurations =
  struct
    type nonrec t =
      {
      excludePreviousThinkingSteps: Boolean.t option
        [@ocaml.doc
          "If true, the service removes any content between <thinking> tags from previous conversations in an agent session. The service will only remove content from already processed turns. This helps you remove content which might not be useful for current and subsequent invocations. This can reduce the input token count and potentially save costs. The default value is false."];
      previousConversationTurnsToInclude:
        PromptCreationConfigurationsPreviousConversationTurnsToIncludeInteger.t
          option
        [@ocaml.doc
          "The number of previous conversations from the ongoing agent session to include in the conversation history of the agent prompt, during the current invocation. This gives you more granular control over the context that the model is made aware of, and helps the model remove older context which is no longer useful during the ongoing agent session."]}
    let make ?excludePreviousThinkingSteps =
      fun ?previousConversationTurnsToInclude ->
        fun () ->
          { excludePreviousThinkingSteps; previousConversationTurnsToInclude
          }
    let to_value x =
      structure_to_value
        [("excludePreviousThinkingSteps",
           (Option.map x.excludePreviousThinkingSteps ~f:Boolean.to_value));
        ("previousConversationTurnsToInclude",
          (Option.map x.previousConversationTurnsToInclude
             ~f:PromptCreationConfigurationsPreviousConversationTurnsToIncludeInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let previousConversationTurnsToInclude =
        (Option.map
           ~f:PromptCreationConfigurationsPreviousConversationTurnsToIncludeInteger.of_xml)
          (Xml.child xml_arg0 "previousConversationTurnsToInclude") in
      let excludePreviousThinkingSteps =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "excludePreviousThinkingSteps") in
      make ?previousConversationTurnsToInclude ?excludePreviousThinkingSteps
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let previousConversationTurnsToInclude =
        field_map json__ "previousConversationTurnsToInclude"
          PromptCreationConfigurationsPreviousConversationTurnsToIncludeInteger.of_json in
      let excludePreviousThinkingSteps =
        field_map json__ "excludePreviousThinkingSteps" Boolean.of_json in
      make ?previousConversationTurnsToInclude ?excludePreviousThinkingSteps
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies parameters that control how the service populates the agent prompt for an InvokeAgent or InvokeInlineAgent request. You can control which aspects of previous invocations in the same agent session the service uses to populate the agent prompt. This gives you more granular control over the contextual history that is used to process the current request."]
module InvokeAgentRequest =
  struct
    type nonrec t =
      {
      agentAliasId: AgentAliasId.t
        [@ocaml.doc "The alias of the agent to use."];
      agentId: AgentId.t
        [@ocaml.doc "The unique identifier of the agent to use."];
      bedrockModelConfigurations: BedrockModelConfigurations.t option
        [@ocaml.doc "Model performance settings for the request."];
      enableTrace: Boolean.t option
        [@ocaml.doc
          "Specifies whether to turn on the trace or not to track the agent's reasoning process. For more information, see Trace enablement."];
      endSession: Boolean.t option
        [@ocaml.doc
          "Specifies whether to end the session with the agent or not."];
      inputText: InputText.t option
        [@ocaml.doc
          "The prompt text to send the agent. If you include returnControlInvocationResults in the sessionState field, the inputText field will be ignored."];
      memoryId: MemoryId.t option
        [@ocaml.doc "The unique identifier of the agent memory."];
      promptCreationConfigurations: PromptCreationConfigurations.t option
        [@ocaml.doc
          "Specifies parameters that control how the service populates the agent prompt for an InvokeAgent request. You can control which aspects of previous invocations in the same agent session the service uses to populate the agent prompt. This gives you more granular control over the contextual history that is used to process the current request."];
      sessionId: SessionId.t
        [@ocaml.doc
          "The unique identifier of the session. Use the same value across requests to continue the same conversation."];
      sessionState: SessionState.t option
        [@ocaml.doc
          "Contains parameters that specify various attributes of the session. For more information, see Control session context. If you include returnControlInvocationResults in the sessionState field, the inputText field will be ignored."];
      sourceArn: AWSResourceARN.t option
        [@ocaml.doc "The ARN of the resource making the request."];
      streamingConfigurations: StreamingConfigurations.t option
        [@ocaml.doc
          "Specifies the configurations for streaming. To use agent streaming, you need permissions to perform the bedrock:InvokeModelWithResponseStream action."]}
    let context_ = "InvokeAgentRequest"
    let make ?bedrockModelConfigurations =
      fun ?enableTrace ->
        fun ?endSession ->
          fun ?inputText ->
            fun ?memoryId ->
              fun ?promptCreationConfigurations ->
                fun ?sessionState ->
                  fun ?sourceArn ->
                    fun ?streamingConfigurations ->
                      fun ~agentAliasId ->
                        fun ~agentId ->
                          fun ~sessionId ->
                            fun () ->
                              {
                                bedrockModelConfigurations;
                                enableTrace;
                                endSession;
                                inputText;
                                memoryId;
                                promptCreationConfigurations;
                                sessionState;
                                sourceArn;
                                streamingConfigurations;
                                agentAliasId;
                                agentId;
                                sessionId
                              }
    let to_value x =
      structure_to_value
        [("agentAliasId", (Some (AgentAliasId.to_value x.agentAliasId)));
        ("agentId", (Some (AgentId.to_value x.agentId)));
        ("bedrockModelConfigurations",
          (Option.map x.bedrockModelConfigurations
             ~f:BedrockModelConfigurations.to_value));
        ("enableTrace", (Option.map x.enableTrace ~f:Boolean.to_value));
        ("endSession", (Option.map x.endSession ~f:Boolean.to_value));
        ("inputText", (Option.map x.inputText ~f:InputText.to_value));
        ("memoryId", (Option.map x.memoryId ~f:MemoryId.to_value));
        ("promptCreationConfigurations",
          (Option.map x.promptCreationConfigurations
             ~f:PromptCreationConfigurations.to_value));
        ("sessionId", (Some (SessionId.to_value x.sessionId)));
        ("sessionState",
          (Option.map x.sessionState ~f:SessionState.to_value));
        ("x-amz-source-arn",
          (Option.map x.sourceArn ~f:AWSResourceARN.to_value));
        ("streamingConfigurations",
          (Option.map x.streamingConfigurations
             ~f:StreamingConfigurations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamingConfigurations =
        (Option.map ~f:StreamingConfigurations.of_xml)
          (Xml.child xml_arg0 "streamingConfigurations") in
      let sourceArn =
        (Option.map ~f:AWSResourceARN.of_xml)
          (Xml.child xml_arg0 "x-amz-source-arn") in
      let sessionState =
        (Option.map ~f:SessionState.of_xml)
          (Xml.child xml_arg0 "sessionState") in
      let sessionId =
        SessionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionId") in
      let promptCreationConfigurations =
        (Option.map ~f:PromptCreationConfigurations.of_xml)
          (Xml.child xml_arg0 "promptCreationConfigurations") in
      let memoryId =
        (Option.map ~f:MemoryId.of_xml) (Xml.child xml_arg0 "memoryId") in
      let inputText =
        (Option.map ~f:InputText.of_xml) (Xml.child xml_arg0 "inputText") in
      let endSession =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "endSession") in
      let enableTrace =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enableTrace") in
      let bedrockModelConfigurations =
        (Option.map ~f:BedrockModelConfigurations.of_xml)
          (Xml.child xml_arg0 "bedrockModelConfigurations") in
      let agentId =
        AgentId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "agentId") in
      let agentAliasId =
        AgentAliasId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agentAliasId") in
      make ?streamingConfigurations ?sourceArn ?sessionState ~sessionId
        ?promptCreationConfigurations ?memoryId ?inputText ?endSession
        ?enableTrace ?bedrockModelConfigurations ~agentId ~agentAliasId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streamingConfigurations =
        field_map json__ "streamingConfigurations"
          StreamingConfigurations.of_json in
      let sourceArn = field_map json__ "sourceArn" AWSResourceARN.of_json in
      let sessionState = field_map json__ "sessionState" SessionState.of_json in
      let sessionId = field_map_exn json__ "sessionId" SessionId.of_json in
      let promptCreationConfigurations =
        field_map json__ "promptCreationConfigurations"
          PromptCreationConfigurations.of_json in
      let memoryId = field_map json__ "memoryId" MemoryId.of_json in
      let inputText = field_map json__ "inputText" InputText.of_json in
      let endSession = field_map json__ "endSession" Boolean.of_json in
      let enableTrace = field_map json__ "enableTrace" Boolean.of_json in
      let bedrockModelConfigurations =
        field_map json__ "bedrockModelConfigurations"
          BedrockModelConfigurations.of_json in
      let agentId = field_map_exn json__ "agentId" AgentId.of_json in
      let agentAliasId =
        field_map_exn json__ "agentAliasId" AgentAliasId.of_json in
      make ?streamingConfigurations ?sourceArn ?sessionState ~sessionId
        ?promptCreationConfigurations ?memoryId ?inputText ?endSession
        ?enableTrace ?bedrockModelConfigurations ~agentId ~agentAliasId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends a prompt for the agent to process and respond to. Note the following fields for the request: To continue the same conversation with an agent, use the same sessionId value in the request. To activate trace enablement, turn enableTrace to true. Trace enablement helps you follow the agent's reasoning process that led it to the information it processed, the actions it took, and the final result it yielded. For more information, see Trace enablement. End a conversation by setting endSession to true. In the sessionState object, you can include attributes for the session or prompt or, if you configured an action group to return control, results from invocation of the action group. The response contains both chunk and trace attributes. The final response is returned in the bytes field of the chunk object. The InvokeAgent returns one chunk for the entire interaction. The attribution object contains citations for parts of the response. If you set enableTrace to true in the request, you can trace the agent's steps and reasoning process that led it to the response. If the action predicted was configured to return control, the response returns parameters for the action, elicited from the user, in the returnControl field. Errors are also surfaced in the response."]
module PayloadPart =
  struct
    type nonrec t =
      {
      attribution: Attribution.t option
        [@ocaml.doc "Contains citations for a part of an agent response."];
      bytes: PartBody.t option
        [@ocaml.doc "A part of the agent response in bytes."]}
    let make ?attribution = fun ?bytes -> fun () -> { attribution; bytes }
    let to_value x =
      structure_to_value
        [("attribution", (Option.map x.attribution ~f:Attribution.to_value));
        ("bytes", (Option.map x.bytes ~f:PartBody.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytes =
        (Option.map ~f:PartBody.of_xml) (Xml.child xml_arg0 "bytes") in
      let attribution =
        (Option.map ~f:Attribution.of_xml) (Xml.child xml_arg0 "attribution") in
      make ?bytes ?attribution ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let bytes = field_map json__ "bytes" PartBody.of_json in
      let attribution = field_map json__ "attribution" Attribution.of_json in
      make ?bytes ?attribution ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains a part of an agent response and citations for it."]
module ModelNotReadyException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The model specified in the request is not ready to serve inference requests. The AWS SDK will automatically retry the operation up to 5 times. For information about configuring automatic retries, see Retry behavior in the AWS SDKs and Tools reference guide."]
module ResponseStream =
  struct
    type nonrec t =
      {
      accessDeniedException: AccessDeniedException.t option
        [@ocaml.doc
          "The request is denied because of missing access permissions. Check your permissions and retry your request."];
      badGatewayException: BadGatewayException.t option
        [@ocaml.doc
          "There was an issue with a dependency due to a server issue. Retry your request."];
      chunk: PayloadPart.t option
        [@ocaml.doc
          "Contains a part of an agent response and citations for it."];
      conflictException: ConflictException.t option
        [@ocaml.doc
          "There was a conflict performing an operation. Resolve the conflict and retry your request."];
      dependencyFailedException: DependencyFailedException.t option
        [@ocaml.doc
          "There was an issue with a dependency. Check the resource configurations and retry the request."];
      files: FilePart.t option
        [@ocaml.doc
          "Contains intermediate response for code interpreter if any files have been generated."];
      internalServerException: InternalServerException.t option
        [@ocaml.doc "An internal server error occurred. Retry your request."];
      modelNotReadyException: ModelNotReadyException.t option
        [@ocaml.doc
          "The model specified in the request is not ready to serve Inference requests. The AWS SDK will automatically retry the operation up to 5 times. For information about configuring automatic retries, see Retry behavior in the AWS SDKs and Tools reference guide."];
      resourceNotFoundException: ResourceNotFoundException.t option
        [@ocaml.doc
          "The specified resource Amazon Resource Name (ARN) was not found. Check the Amazon Resource Name (ARN) and try your request again."];
      returnControl: ReturnControlPayload.t option
        [@ocaml.doc
          "Contains the parameters and information that the agent elicited from the customer to carry out an action. This information is returned to the system and can be used in your own setup for fulfilling the action."];
      serviceQuotaExceededException: ServiceQuotaExceededException.t option
        [@ocaml.doc
          "The number of requests exceeds the service quota. Resubmit your request later."];
      throttlingException: ThrottlingException.t option
        [@ocaml.doc
          "The number of requests exceeds the limit. Resubmit your request later."];
      trace: TracePart.t option
        [@ocaml.doc
          "Contains information about the agent and session, alongside the agent's reasoning process and results from calling actions and querying knowledge bases and metadata about the trace. You can use the trace to understand how the agent arrived at the response it provided the customer. For more information, see Trace events."];
      validationException: ValidationException.t option
        [@ocaml.doc
          "Input validation failed. Check your request parameters and retry the request."]}
    let make ?accessDeniedException =
      fun ?badGatewayException ->
        fun ?chunk ->
          fun ?conflictException ->
            fun ?dependencyFailedException ->
              fun ?files ->
                fun ?internalServerException ->
                  fun ?modelNotReadyException ->
                    fun ?resourceNotFoundException ->
                      fun ?returnControl ->
                        fun ?serviceQuotaExceededException ->
                          fun ?throttlingException ->
                            fun ?trace ->
                              fun ?validationException ->
                                fun () ->
                                  {
                                    accessDeniedException;
                                    badGatewayException;
                                    chunk;
                                    conflictException;
                                    dependencyFailedException;
                                    files;
                                    internalServerException;
                                    modelNotReadyException;
                                    resourceNotFoundException;
                                    returnControl;
                                    serviceQuotaExceededException;
                                    throttlingException;
                                    trace;
                                    validationException
                                  }
    let to_value x =
      structure_to_value
        [("accessDeniedException",
           (Option.map x.accessDeniedException
              ~f:AccessDeniedException.to_value));
        ("badGatewayException",
          (Option.map x.badGatewayException ~f:BadGatewayException.to_value));
        ("chunk", (Option.map x.chunk ~f:PayloadPart.to_value));
        ("conflictException",
          (Option.map x.conflictException ~f:ConflictException.to_value));
        ("dependencyFailedException",
          (Option.map x.dependencyFailedException
             ~f:DependencyFailedException.to_value));
        ("files", (Option.map x.files ~f:FilePart.to_value));
        ("internalServerException",
          (Option.map x.internalServerException
             ~f:InternalServerException.to_value));
        ("modelNotReadyException",
          (Option.map x.modelNotReadyException
             ~f:ModelNotReadyException.to_value));
        ("resourceNotFoundException",
          (Option.map x.resourceNotFoundException
             ~f:ResourceNotFoundException.to_value));
        ("returnControl",
          (Option.map x.returnControl ~f:ReturnControlPayload.to_value));
        ("serviceQuotaExceededException",
          (Option.map x.serviceQuotaExceededException
             ~f:ServiceQuotaExceededException.to_value));
        ("throttlingException",
          (Option.map x.throttlingException ~f:ThrottlingException.to_value));
        ("trace", (Option.map x.trace ~f:TracePart.to_value));
        ("validationException",
          (Option.map x.validationException ~f:ValidationException.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validationException =
        (Option.map ~f:ValidationException.of_xml)
          (Xml.child xml_arg0 "validationException") in
      let trace =
        (Option.map ~f:TracePart.of_xml) (Xml.child xml_arg0 "trace") in
      let throttlingException =
        (Option.map ~f:ThrottlingException.of_xml)
          (Xml.child xml_arg0 "throttlingException") in
      let serviceQuotaExceededException =
        (Option.map ~f:ServiceQuotaExceededException.of_xml)
          (Xml.child xml_arg0 "serviceQuotaExceededException") in
      let returnControl =
        (Option.map ~f:ReturnControlPayload.of_xml)
          (Xml.child xml_arg0 "returnControl") in
      let resourceNotFoundException =
        (Option.map ~f:ResourceNotFoundException.of_xml)
          (Xml.child xml_arg0 "resourceNotFoundException") in
      let modelNotReadyException =
        (Option.map ~f:ModelNotReadyException.of_xml)
          (Xml.child xml_arg0 "modelNotReadyException") in
      let internalServerException =
        (Option.map ~f:InternalServerException.of_xml)
          (Xml.child xml_arg0 "internalServerException") in
      let files =
        (Option.map ~f:FilePart.of_xml) (Xml.child xml_arg0 "files") in
      let dependencyFailedException =
        (Option.map ~f:DependencyFailedException.of_xml)
          (Xml.child xml_arg0 "dependencyFailedException") in
      let conflictException =
        (Option.map ~f:ConflictException.of_xml)
          (Xml.child xml_arg0 "conflictException") in
      let chunk =
        (Option.map ~f:PayloadPart.of_xml) (Xml.child xml_arg0 "chunk") in
      let badGatewayException =
        (Option.map ~f:BadGatewayException.of_xml)
          (Xml.child xml_arg0 "badGatewayException") in
      let accessDeniedException =
        (Option.map ~f:AccessDeniedException.of_xml)
          (Xml.child xml_arg0 "accessDeniedException") in
      make ?validationException ?trace ?throttlingException
        ?serviceQuotaExceededException ?returnControl
        ?resourceNotFoundException ?modelNotReadyException
        ?internalServerException ?files ?dependencyFailedException
        ?conflictException ?chunk ?badGatewayException ?accessDeniedException
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let validationException =
        field_map json__ "validationException" ValidationException.of_json in
      let trace = field_map json__ "trace" TracePart.of_json in
      let throttlingException =
        field_map json__ "throttlingException" ThrottlingException.of_json in
      let serviceQuotaExceededException =
        field_map json__ "serviceQuotaExceededException"
          ServiceQuotaExceededException.of_json in
      let returnControl =
        field_map json__ "returnControl" ReturnControlPayload.of_json in
      let resourceNotFoundException =
        field_map json__ "resourceNotFoundException"
          ResourceNotFoundException.of_json in
      let modelNotReadyException =
        field_map json__ "modelNotReadyException"
          ModelNotReadyException.of_json in
      let internalServerException =
        field_map json__ "internalServerException"
          InternalServerException.of_json in
      let files = field_map json__ "files" FilePart.of_json in
      let dependencyFailedException =
        field_map json__ "dependencyFailedException"
          DependencyFailedException.of_json in
      let conflictException =
        field_map json__ "conflictException" ConflictException.of_json in
      let chunk = field_map json__ "chunk" PayloadPart.of_json in
      let badGatewayException =
        field_map json__ "badGatewayException" BadGatewayException.of_json in
      let accessDeniedException =
        field_map json__ "accessDeniedException"
          AccessDeniedException.of_json in
      make ?validationException ?trace ?throttlingException
        ?serviceQuotaExceededException ?returnControl
        ?resourceNotFoundException ?modelNotReadyException
        ?internalServerException ?files ?dependencyFailedException
        ?conflictException ?chunk ?badGatewayException ?accessDeniedException
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The response from invoking the agent and associated citations and trace information."]
module InvokeAgentResponse =
  struct
    type nonrec t =
      {
      completion: ResponseStream.t option
        [@ocaml.doc "The agent's response to the user prompt."];
      contentType: MimeType.t option
        [@ocaml.doc
          "The MIME type of the input data in the request. The default value is application/json."];
      memoryId: MemoryId.t option
        [@ocaml.doc "The unique identifier of the agent memory."];
      sessionId: SessionId.t option
        [@ocaml.doc "The unique identifier of the session with the agent."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ModelNotReadyException of ModelNotReadyException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?completion =
      fun ?contentType ->
        fun ?memoryId ->
          fun ?sessionId ->
            fun () -> { completion; contentType; memoryId; sessionId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ModelNotReadyException" ->
          `ModelNotReadyException (ModelNotReadyException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ModelNotReadyException e ->
          `Assoc
            [("error", (`String "ModelNotReadyException"));
            ("details", (ModelNotReadyException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make ?completion:(Some pipe)
            ?contentType:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "x-amzn-bedrock-agent-content-type")
                            ~f:MimeType.of_string)
            ?memoryId:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "x-amz-bedrock-agent-memory-id")
                         ~f:MemoryId.of_string)
            ?sessionId:(Option.map
                          ((List.Assoc.find ~equal:String.Caseless.equal) xs
                             "x-amz-bedrock-agent-session-id")
                          ~f:SessionId.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("completion", (Option.map x.completion ~f:ResponseStream.to_value));
        ("x-amzn-bedrock-agent-content-type",
          (Option.map x.contentType ~f:MimeType.to_value));
        ("x-amz-bedrock-agent-memory-id",
          (Option.map x.memoryId ~f:MemoryId.to_value));
        ("x-amz-bedrock-agent-session-id",
          (Option.map x.sessionId ~f:SessionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:SessionId.of_xml)
          (Xml.child xml_arg0 "x-amz-bedrock-agent-session-id") in
      let memoryId =
        (Option.map ~f:MemoryId.of_xml)
          (Xml.child xml_arg0 "x-amz-bedrock-agent-memory-id") in
      let contentType =
        (Option.map ~f:MimeType.of_xml)
          (Xml.child xml_arg0 "x-amzn-bedrock-agent-content-type") in
      let completion =
        (Option.map ~f:ResponseStream.of_xml)
          (Xml.child xml_arg0 "completion") in
      make ?sessionId ?memoryId ?contentType ?completion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let memoryId = field_map json__ "memoryId" MemoryId.of_json in
      let contentType = field_map json__ "contentType" MimeType.of_json in
      let completion = field_map json__ "completion" ResponseStream.of_json in
      make ?sessionId ?memoryId ?contentType ?completion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends a prompt for the agent to process and respond to. Note the following fields for the request: To continue the same conversation with an agent, use the same sessionId value in the request. To activate trace enablement, turn enableTrace to true. Trace enablement helps you follow the agent's reasoning process that led it to the information it processed, the actions it took, and the final result it yielded. For more information, see Trace enablement. End a conversation by setting endSession to true. In the sessionState object, you can include attributes for the session or prompt or, if you configured an action group to return control, results from invocation of the action group. The response contains both chunk and trace attributes. The final response is returned in the bytes field of the chunk object. The InvokeAgent returns one chunk for the entire interaction. The attribution object contains citations for parts of the response. If you set enableTrace to true in the request, you can trace the agent's steps and reasoning process that led it to the response. If the action predicted was configured to return control, the response returns parameters for the action, elicited from the user, in the returnControl field. Errors are also surfaced in the response."]
module ModelPerformanceConfiguration =
  struct
    type nonrec t =
      {
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "The latency configuration for the model."]}
    let make ?performanceConfig = fun () -> { performanceConfig }
    let to_value x =
      structure_to_value
        [("performanceConfig",
           (Option.map x.performanceConfig
              ~f:PerformanceConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      make ?performanceConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      make ?performanceConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The performance configuration for a model called with InvokeFlow."]
module InvokeFlowRequest =
  struct
    type nonrec t =
      {
      enableTrace: Boolean.t option
        [@ocaml.doc
          "Specifies whether to return the trace for the flow or not. Traces track inputs and outputs for nodes in the flow. For more information, see Track each step in your prompt flow by viewing its trace in Amazon Bedrock."];
      executionId: FlowExecutionId.t option
        [@ocaml.doc
          "The unique identifier for the current flow execution. If you don't provide a value, Amazon Bedrock creates the identifier for you."];
      flowAliasIdentifier: FlowAliasIdentifier.t
        [@ocaml.doc "The unique identifier of the flow alias."];
      flowIdentifier: FlowIdentifier.t
        [@ocaml.doc "The unique identifier of the flow."];
      inputs: FlowInputs.t
        [@ocaml.doc
          "A list of objects, each containing information about an input into the flow."];
      modelPerformanceConfiguration: ModelPerformanceConfiguration.t option
        [@ocaml.doc "Model performance settings for the request."]}
    let context_ = "InvokeFlowRequest"
    let make ?enableTrace =
      fun ?executionId ->
        fun ?modelPerformanceConfiguration ->
          fun ~flowAliasIdentifier ->
            fun ~flowIdentifier ->
              fun ~inputs ->
                fun () ->
                  {
                    enableTrace;
                    executionId;
                    modelPerformanceConfiguration;
                    flowAliasIdentifier;
                    flowIdentifier;
                    inputs
                  }
    let to_value x =
      structure_to_value
        [("enableTrace", (Option.map x.enableTrace ~f:Boolean.to_value));
        ("executionId",
          (Option.map x.executionId ~f:FlowExecutionId.to_value));
        ("flowAliasIdentifier",
          (Some (FlowAliasIdentifier.to_value x.flowAliasIdentifier)));
        ("flowIdentifier", (Some (FlowIdentifier.to_value x.flowIdentifier)));
        ("inputs", (Some (FlowInputs.to_value x.inputs)));
        ("modelPerformanceConfiguration",
          (Option.map x.modelPerformanceConfiguration
             ~f:ModelPerformanceConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modelPerformanceConfiguration =
        (Option.map ~f:ModelPerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "modelPerformanceConfiguration") in
      let inputs =
        FlowInputs.of_xml (Xml.child_exn ~context:context_ xml_arg0 "inputs") in
      let flowIdentifier =
        FlowIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        FlowAliasIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowAliasIdentifier") in
      let executionId =
        (Option.map ~f:FlowExecutionId.of_xml)
          (Xml.child xml_arg0 "executionId") in
      let enableTrace =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enableTrace") in
      make ?modelPerformanceConfiguration ~inputs ~flowIdentifier
        ~flowAliasIdentifier ?executionId ?enableTrace ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let modelPerformanceConfiguration =
        field_map json__ "modelPerformanceConfiguration"
          ModelPerformanceConfiguration.of_json in
      let inputs = field_map_exn json__ "inputs" FlowInputs.of_json in
      let flowIdentifier =
        field_map_exn json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map_exn json__ "flowAliasIdentifier"
          FlowAliasIdentifier.of_json in
      let executionId =
        field_map json__ "executionId" FlowExecutionId.of_json in
      let enableTrace = field_map json__ "enableTrace" Boolean.of_json in
      make ?modelPerformanceConfiguration ~inputs ~flowIdentifier
        ~flowAliasIdentifier ?executionId ?enableTrace ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invokes an alias of a flow to run the inputs that you specify and return the output of each node as a stream. If there's an error, the error is returned. For more information, see Test a flow in Amazon Bedrock in the Amazon Bedrock User Guide. The CLI doesn't support streaming operations in Amazon Bedrock, including InvokeFlow."]
module InvokeFlowResponse =
  struct
    type nonrec t =
      {
      executionId: FlowExecutionId.t option
        [@ocaml.doc "The unique identifier for the current flow execution."];
      responseStream: FlowResponseStream.t option
        [@ocaml.doc
          "The output of the flow, returned as a stream. If there's an error, the error is returned."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?executionId =
      fun ?responseStream -> fun () -> { executionId; responseStream }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?executionId:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "x-amz-bedrock-flow-execution-id")
                            ~f:FlowExecutionId.of_string)
            ?responseStream:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("x-amz-bedrock-flow-execution-id",
           (Option.map x.executionId ~f:FlowExecutionId.to_value));
        ("responseStream",
          (Option.map x.responseStream ~f:FlowResponseStream.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let responseStream =
        (Option.map ~f:FlowResponseStream.of_xml)
          (Xml.child xml_arg0 "responseStream") in
      let executionId =
        (Option.map ~f:FlowExecutionId.of_xml)
          (Xml.child xml_arg0 "x-amz-bedrock-flow-execution-id") in
      make ?responseStream ?executionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let responseStream =
        field_map json__ "responseStream" FlowResponseStream.of_json in
      let executionId =
        field_map json__ "executionId" FlowExecutionId.of_json in
      make ?responseStream ?executionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invokes an alias of a flow to run the inputs that you specify and return the output of each node as a stream. If there's an error, the error is returned. For more information, see Test a flow in Amazon Bedrock in the Amazon Bedrock User Guide. The CLI doesn't support streaming operations in Amazon Bedrock, including InvokeFlow."]
module OrchestrationType =
  struct
    type nonrec t =
      | DEFAULT 
      | CUSTOM_ORCHESTRATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DEFAULT -> "DEFAULT"
      | CUSTOM_ORCHESTRATION -> "CUSTOM_ORCHESTRATION"
      | Non_static_id s -> s
    let of_string =
      function
      | "DEFAULT" -> DEFAULT
      | "CUSTOM_ORCHESTRATION" -> CUSTOM_ORCHESTRATION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration OrchestrationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OrchestrationType" j)
    let to_json = simple_to_json to_value
  end
module InvokeInlineAgentRequest =
  struct
    type nonrec t =
      {
      actionGroups: AgentActionGroups.t option
        [@ocaml.doc
          "A list of action groups with each action group defining the action the inline agent needs to carry out."];
      agentCollaboration: AgentCollaboration.t option
        [@ocaml.doc
          "Defines how the inline collaborator agent handles information across multiple collaborator agents to coordinate a final response. The inline collaborator agent can also be the supervisor."];
      agentName: Name.t option [@ocaml.doc "The name for the agent."];
      bedrockModelConfigurations: InlineBedrockModelConfigurations.t option
        [@ocaml.doc "Model settings for the request."];
      collaboratorConfigurations: CollaboratorConfigurations.t option
        [@ocaml.doc
          "Settings for an inline agent collaborator called with InvokeInlineAgent."];
      collaborators: Collaborators.t option
        [@ocaml.doc "List of collaborator inline agents."];
      customOrchestration: CustomOrchestration.t option
        [@ocaml.doc
          "Contains details of the custom orchestration configured for the agent."];
      customerEncryptionKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon Web Services KMS key to use to encrypt your inline agent."];
      enableTrace: Boolean.t option
        [@ocaml.doc
          "Specifies whether to turn on the trace or not to track the agent's reasoning process. For more information, see Using trace."];
      endSession: Boolean.t option
        [@ocaml.doc
          "Specifies whether to end the session with the inline agent or not."];
      foundationModel: ModelIdentifier.t
        [@ocaml.doc
          "The model identifier (ID) of the model to use for orchestration by the inline agent. For example, meta.llama3-1-70b-instruct-v1:0."];
      guardrailConfiguration: GuardrailConfigurationWithArn.t option
        [@ocaml.doc "The guardrails to assign to the inline agent."];
      idleSessionTTLInSeconds: SessionTTL.t option
        [@ocaml.doc
          "The number of seconds for which the inline agent should maintain session information. After this time expires, the subsequent InvokeInlineAgent request begins a new session. A user interaction remains active for the amount of time specified. If no conversation occurs during this time, the session expires and the data provided before the timeout is deleted."];
      inlineSessionState: InlineSessionState.t option
        [@ocaml.doc
          "Parameters that specify the various attributes of a sessions. You can include attributes for the session or prompt or, if you configured an action group to return control, results from invocation of the action group. For more information, see Control session context. If you include returnControlInvocationResults in the sessionState field, the inputText field will be ignored."];
      inputText: InputText.t option
        [@ocaml.doc
          "The prompt text to send to the agent. If you include returnControlInvocationResults in the sessionState field, the inputText field will be ignored."];
      instruction: Instruction.t
        [@ocaml.doc
          "The instructions that tell the inline agent what it should do and how it should interact with users."];
      knowledgeBases: KnowledgeBases.t option
        [@ocaml.doc
          "Contains information of the knowledge bases to associate with."];
      orchestrationType: OrchestrationType.t option
        [@ocaml.doc
          "Specifies the type of orchestration strategy for the agent. This is set to DEFAULT orchestration type, by default."];
      promptCreationConfigurations: PromptCreationConfigurations.t option
        [@ocaml.doc
          "Specifies parameters that control how the service populates the agent prompt for an InvokeInlineAgent request. You can control which aspects of previous invocations in the same agent session the service uses to populate the agent prompt. This gives you more granular control over the contextual history that is used to process the current request."];
      promptOverrideConfiguration: PromptOverrideConfiguration.t option
        [@ocaml.doc
          "Configurations for advanced prompts used to override the default prompts to enhance the accuracy of the inline agent."];
      sessionId: SessionId.t
        [@ocaml.doc
          "The unique identifier of the session. Use the same value across requests to continue the same conversation."];
      streamingConfigurations: StreamingConfigurations.t option
        [@ocaml.doc
          "Specifies the configurations for streaming. To use agent streaming, you need permissions to perform the bedrock:InvokeModelWithResponseStream action."]}
    let context_ = "InvokeInlineAgentRequest"
    let make ?actionGroups =
      fun ?agentCollaboration ->
        fun ?agentName ->
          fun ?bedrockModelConfigurations ->
            fun ?collaboratorConfigurations ->
              fun ?collaborators ->
                fun ?customOrchestration ->
                  fun ?customerEncryptionKeyArn ->
                    fun ?enableTrace ->
                      fun ?endSession ->
                        fun ?guardrailConfiguration ->
                          fun ?idleSessionTTLInSeconds ->
                            fun ?inlineSessionState ->
                              fun ?inputText ->
                                fun ?knowledgeBases ->
                                  fun ?orchestrationType ->
                                    fun ?promptCreationConfigurations ->
                                      fun ?promptOverrideConfiguration ->
                                        fun ?streamingConfigurations ->
                                          fun ~foundationModel ->
                                            fun ~instruction ->
                                              fun ~sessionId ->
                                                fun () ->
                                                  {
                                                    actionGroups;
                                                    agentCollaboration;
                                                    agentName;
                                                    bedrockModelConfigurations;
                                                    collaboratorConfigurations;
                                                    collaborators;
                                                    customOrchestration;
                                                    customerEncryptionKeyArn;
                                                    enableTrace;
                                                    endSession;
                                                    guardrailConfiguration;
                                                    idleSessionTTLInSeconds;
                                                    inlineSessionState;
                                                    inputText;
                                                    knowledgeBases;
                                                    orchestrationType;
                                                    promptCreationConfigurations;
                                                    promptOverrideConfiguration;
                                                    streamingConfigurations;
                                                    foundationModel;
                                                    instruction;
                                                    sessionId
                                                  }
    let to_value x =
      structure_to_value
        [("actionGroups",
           (Option.map x.actionGroups ~f:AgentActionGroups.to_value));
        ("agentCollaboration",
          (Option.map x.agentCollaboration ~f:AgentCollaboration.to_value));
        ("agentName", (Option.map x.agentName ~f:Name.to_value));
        ("bedrockModelConfigurations",
          (Option.map x.bedrockModelConfigurations
             ~f:InlineBedrockModelConfigurations.to_value));
        ("collaboratorConfigurations",
          (Option.map x.collaboratorConfigurations
             ~f:CollaboratorConfigurations.to_value));
        ("collaborators",
          (Option.map x.collaborators ~f:Collaborators.to_value));
        ("customOrchestration",
          (Option.map x.customOrchestration ~f:CustomOrchestration.to_value));
        ("customerEncryptionKeyArn",
          (Option.map x.customerEncryptionKeyArn ~f:KmsKeyArn.to_value));
        ("enableTrace", (Option.map x.enableTrace ~f:Boolean.to_value));
        ("endSession", (Option.map x.endSession ~f:Boolean.to_value));
        ("foundationModel",
          (Some (ModelIdentifier.to_value x.foundationModel)));
        ("guardrailConfiguration",
          (Option.map x.guardrailConfiguration
             ~f:GuardrailConfigurationWithArn.to_value));
        ("idleSessionTTLInSeconds",
          (Option.map x.idleSessionTTLInSeconds ~f:SessionTTL.to_value));
        ("inlineSessionState",
          (Option.map x.inlineSessionState ~f:InlineSessionState.to_value));
        ("inputText", (Option.map x.inputText ~f:InputText.to_value));
        ("instruction", (Some (Instruction.to_value x.instruction)));
        ("knowledgeBases",
          (Option.map x.knowledgeBases ~f:KnowledgeBases.to_value));
        ("orchestrationType",
          (Option.map x.orchestrationType ~f:OrchestrationType.to_value));
        ("promptCreationConfigurations",
          (Option.map x.promptCreationConfigurations
             ~f:PromptCreationConfigurations.to_value));
        ("promptOverrideConfiguration",
          (Option.map x.promptOverrideConfiguration
             ~f:PromptOverrideConfiguration.to_value));
        ("sessionId", (Some (SessionId.to_value x.sessionId)));
        ("streamingConfigurations",
          (Option.map x.streamingConfigurations
             ~f:StreamingConfigurations.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamingConfigurations =
        (Option.map ~f:StreamingConfigurations.of_xml)
          (Xml.child xml_arg0 "streamingConfigurations") in
      let sessionId =
        SessionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionId") in
      let promptOverrideConfiguration =
        (Option.map ~f:PromptOverrideConfiguration.of_xml)
          (Xml.child xml_arg0 "promptOverrideConfiguration") in
      let promptCreationConfigurations =
        (Option.map ~f:PromptCreationConfigurations.of_xml)
          (Xml.child xml_arg0 "promptCreationConfigurations") in
      let orchestrationType =
        (Option.map ~f:OrchestrationType.of_xml)
          (Xml.child xml_arg0 "orchestrationType") in
      let knowledgeBases =
        (Option.map ~f:KnowledgeBases.of_xml)
          (Xml.child xml_arg0 "knowledgeBases") in
      let instruction =
        Instruction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instruction") in
      let inputText =
        (Option.map ~f:InputText.of_xml) (Xml.child xml_arg0 "inputText") in
      let inlineSessionState =
        (Option.map ~f:InlineSessionState.of_xml)
          (Xml.child xml_arg0 "inlineSessionState") in
      let idleSessionTTLInSeconds =
        (Option.map ~f:SessionTTL.of_xml)
          (Xml.child xml_arg0 "idleSessionTTLInSeconds") in
      let guardrailConfiguration =
        (Option.map ~f:GuardrailConfigurationWithArn.of_xml)
          (Xml.child xml_arg0 "guardrailConfiguration") in
      let foundationModel =
        ModelIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "foundationModel") in
      let endSession =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "endSession") in
      let enableTrace =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enableTrace") in
      let customerEncryptionKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml)
          (Xml.child xml_arg0 "customerEncryptionKeyArn") in
      let customOrchestration =
        (Option.map ~f:CustomOrchestration.of_xml)
          (Xml.child xml_arg0 "customOrchestration") in
      let collaborators =
        (Option.map ~f:Collaborators.of_xml)
          (Xml.child xml_arg0 "collaborators") in
      let collaboratorConfigurations =
        (Option.map ~f:CollaboratorConfigurations.of_xml)
          (Xml.child xml_arg0 "collaboratorConfigurations") in
      let bedrockModelConfigurations =
        (Option.map ~f:InlineBedrockModelConfigurations.of_xml)
          (Xml.child xml_arg0 "bedrockModelConfigurations") in
      let agentName =
        (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "agentName") in
      let agentCollaboration =
        (Option.map ~f:AgentCollaboration.of_xml)
          (Xml.child xml_arg0 "agentCollaboration") in
      let actionGroups =
        (Option.map ~f:AgentActionGroups.of_xml)
          (Xml.child xml_arg0 "actionGroups") in
      make ?streamingConfigurations ~sessionId ?promptOverrideConfiguration
        ?promptCreationConfigurations ?orchestrationType ?knowledgeBases
        ~instruction ?inputText ?inlineSessionState ?idleSessionTTLInSeconds
        ?guardrailConfiguration ~foundationModel ?endSession ?enableTrace
        ?customerEncryptionKeyArn ?customOrchestration ?collaborators
        ?collaboratorConfigurations ?bedrockModelConfigurations ?agentName
        ?agentCollaboration ?actionGroups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let streamingConfigurations =
        field_map json__ "streamingConfigurations"
          StreamingConfigurations.of_json in
      let sessionId = field_map_exn json__ "sessionId" SessionId.of_json in
      let promptOverrideConfiguration =
        field_map json__ "promptOverrideConfiguration"
          PromptOverrideConfiguration.of_json in
      let promptCreationConfigurations =
        field_map json__ "promptCreationConfigurations"
          PromptCreationConfigurations.of_json in
      let orchestrationType =
        field_map json__ "orchestrationType" OrchestrationType.of_json in
      let knowledgeBases =
        field_map json__ "knowledgeBases" KnowledgeBases.of_json in
      let instruction =
        field_map_exn json__ "instruction" Instruction.of_json in
      let inputText = field_map json__ "inputText" InputText.of_json in
      let inlineSessionState =
        field_map json__ "inlineSessionState" InlineSessionState.of_json in
      let idleSessionTTLInSeconds =
        field_map json__ "idleSessionTTLInSeconds" SessionTTL.of_json in
      let guardrailConfiguration =
        field_map json__ "guardrailConfiguration"
          GuardrailConfigurationWithArn.of_json in
      let foundationModel =
        field_map_exn json__ "foundationModel" ModelIdentifier.of_json in
      let endSession = field_map json__ "endSession" Boolean.of_json in
      let enableTrace = field_map json__ "enableTrace" Boolean.of_json in
      let customerEncryptionKeyArn =
        field_map json__ "customerEncryptionKeyArn" KmsKeyArn.of_json in
      let customOrchestration =
        field_map json__ "customOrchestration" CustomOrchestration.of_json in
      let collaborators =
        field_map json__ "collaborators" Collaborators.of_json in
      let collaboratorConfigurations =
        field_map json__ "collaboratorConfigurations"
          CollaboratorConfigurations.of_json in
      let bedrockModelConfigurations =
        field_map json__ "bedrockModelConfigurations"
          InlineBedrockModelConfigurations.of_json in
      let agentName = field_map json__ "agentName" Name.of_json in
      let agentCollaboration =
        field_map json__ "agentCollaboration" AgentCollaboration.of_json in
      let actionGroups =
        field_map json__ "actionGroups" AgentActionGroups.of_json in
      make ?streamingConfigurations ~sessionId ?promptOverrideConfiguration
        ?promptCreationConfigurations ?orchestrationType ?knowledgeBases
        ~instruction ?inputText ?inlineSessionState ?idleSessionTTLInSeconds
        ?guardrailConfiguration ~foundationModel ?endSession ?enableTrace
        ?customerEncryptionKeyArn ?customOrchestration ?collaborators
        ?collaboratorConfigurations ?bedrockModelConfigurations ?agentName
        ?agentCollaboration ?actionGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invokes an inline Amazon Bedrock agent using the configurations you provide with the request. Specify the following fields for security purposes. (Optional) customerEncryptionKeyArn \226\128\147 The Amazon Resource Name (ARN) of a KMS key to encrypt the creation of the agent. (Optional) idleSessionTTLinSeconds \226\128\147 Specify the number of seconds for which the agent should maintain session information. After this time expires, the subsequent InvokeInlineAgent request begins a new session. To override the default prompt behavior for agent orchestration and to use advanced prompts, include a promptOverrideConfiguration object. For more information, see Advanced prompts. The agent instructions will not be honored if your agent has only one knowledge base, uses default prompts, has no action group, and user input is disabled."]
module InvokeInlineAgentResponse =
  struct
    type nonrec t =
      {
      completion: InlineAgentResponseStream.t option
        [@ocaml.doc "The inline agent's response to the user prompt."];
      contentType: MimeType.t option
        [@ocaml.doc
          "The MIME type of the input data in the request. The default value is application/json."];
      sessionId: SessionId.t option
        [@ocaml.doc "The unique identifier of the session with the agent."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?completion =
      fun ?contentType ->
        fun ?sessionId -> fun () -> { completion; contentType; sessionId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make ?completion:(Some pipe)
            ?contentType:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "x-amzn-bedrock-agent-content-type")
                            ~f:MimeType.of_string)
            ?sessionId:(Option.map
                          ((List.Assoc.find ~equal:String.Caseless.equal) xs
                             "x-amz-bedrock-agent-session-id")
                          ~f:SessionId.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("completion",
           (Option.map x.completion ~f:InlineAgentResponseStream.to_value));
        ("x-amzn-bedrock-agent-content-type",
          (Option.map x.contentType ~f:MimeType.to_value));
        ("x-amz-bedrock-agent-session-id",
          (Option.map x.sessionId ~f:SessionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:SessionId.of_xml)
          (Xml.child xml_arg0 "x-amz-bedrock-agent-session-id") in
      let contentType =
        (Option.map ~f:MimeType.of_xml)
          (Xml.child xml_arg0 "x-amzn-bedrock-agent-content-type") in
      let completion =
        (Option.map ~f:InlineAgentResponseStream.of_xml)
          (Xml.child xml_arg0 "completion") in
      make ?sessionId ?contentType ?completion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let contentType = field_map json__ "contentType" MimeType.of_json in
      let completion =
        field_map json__ "completion" InlineAgentResponseStream.of_json in
      make ?sessionId ?contentType ?completion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Invokes an inline Amazon Bedrock agent using the configurations you provide with the request. Specify the following fields for security purposes. (Optional) customerEncryptionKeyArn \226\128\147 The Amazon Resource Name (ARN) of a KMS key to encrypt the creation of the agent. (Optional) idleSessionTTLinSeconds \226\128\147 Specify the number of seconds for which the agent should maintain session information. After this time expires, the subsequent InvokeInlineAgent request begins a new session. To override the default prompt behavior for agent orchestration and to use advanced prompts, include a promptOverrideConfiguration object. For more information, see Advanced prompts. The agent instructions will not be honored if your agent has only one knowledge base, uses default prompts, has no action group, and user input is disabled."]
module KnowledgeBaseQueryType =
  struct
    type nonrec t =
      | TEXT 
      | IMAGE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | TEXT -> "TEXT" | IMAGE -> "IMAGE" | Non_static_id s -> s
    let of_string =
      function | "TEXT" -> TEXT | "IMAGE" -> IMAGE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration KnowledgeBaseQueryType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"KnowledgeBaseQueryType" j)
    let to_json = simple_to_json to_value
  end
module KnowledgeBaseQueryTextString =
  struct
    type nonrec t = string
    let context_ = "KnowledgeBaseQueryTextString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:20000) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KnowledgeBaseQueryTextString" j
    let to_json = simple_to_json to_value
  end
module KnowledgeBaseQuery =
  struct
    type nonrec t =
      {
      image: InputImage.t option
        [@ocaml.doc
          "An image to include in the knowledge base query for multimodal retrieval."];
      text: KnowledgeBaseQueryTextString.t option
        [@ocaml.doc "The text of the query made to the knowledge base."];
      type_: KnowledgeBaseQueryType.t option
        [@ocaml.doc "The type of query being performed."]}
    let make ?image =
      fun ?text -> fun ?type_ -> fun () -> { image; text; type_ }
    let to_value x =
      structure_to_value
        [("image", (Option.map x.image ~f:InputImage.to_value));
        ("text",
          (Option.map x.text ~f:KnowledgeBaseQueryTextString.to_value));
        ("type", (Option.map x.type_ ~f:KnowledgeBaseQueryType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:KnowledgeBaseQueryType.of_xml)
          (Xml.child xml_arg0 "type") in
      let text =
        (Option.map ~f:KnowledgeBaseQueryTextString.of_xml)
          (Xml.child xml_arg0 "text") in
      let image =
        (Option.map ~f:InputImage.of_xml) (Xml.child xml_arg0 "image") in
      make ?type_ ?text ?image ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" KnowledgeBaseQueryType.of_json in
      let text = field_map json__ "text" KnowledgeBaseQueryTextString.of_json in
      let image = field_map json__ "image" InputImage.of_json in
      make ?type_ ?text ?image ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the query made to the knowledge base. This data type is used in the following API operations: Retrieve request \226\128\147 in the retrievalQuery field"]
module KnowledgeBaseRetrievalResult =
  struct
    type nonrec t =
      {
      content: RetrievalResultContent.t option
        [@ocaml.doc "Contains information about the content of the chunk."];
      location: RetrievalResultLocation.t option
        [@ocaml.doc
          "Contains information about the location of the data source."];
      metadata: RetrievalResultMetadata.t option
        [@ocaml.doc
          "Contains metadata attributes and their values for the file in the data source. For more information, see Metadata and filtering."];
      score: Double.t option
        [@ocaml.doc "The level of relevance of the result to the query."]}
    let make ?content =
      fun ?location ->
        fun ?metadata ->
          fun ?score -> fun () -> { content; location; metadata; score }
    let to_value x =
      structure_to_value
        [("content",
           (Option.map x.content ~f:RetrievalResultContent.to_value));
        ("location",
          (Option.map x.location ~f:RetrievalResultLocation.to_value));
        ("metadata",
          (Option.map x.metadata ~f:RetrievalResultMetadata.to_value));
        ("score", (Option.map x.score ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let score = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "score") in
      let metadata =
        (Option.map ~f:RetrievalResultMetadata.of_xml)
          (Xml.child xml_arg0 "metadata") in
      let location =
        (Option.map ~f:RetrievalResultLocation.of_xml)
          (Xml.child xml_arg0 "location") in
      let content =
        (Option.map ~f:RetrievalResultContent.of_xml)
          (Xml.child xml_arg0 "content") in
      make ?score ?metadata ?location ?content ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let score = field_map json__ "score" Double.of_json in
      let metadata =
        field_map json__ "metadata" RetrievalResultMetadata.of_json in
      let location =
        field_map json__ "location" RetrievalResultLocation.of_json in
      let content = field_map json__ "content" RetrievalResultContent.of_json in
      make ?score ?metadata ?location ?content ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about a result from querying the knowledge base. This data type is used in the following API operations: Retrieve response \226\128\147 in the retrievalResults field"]
module KnowledgeBaseRetrievalResults =
  struct
    type nonrec t = KnowledgeBaseRetrievalResult.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:KnowledgeBaseRetrievalResult.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KnowledgeBaseRetrievalResult.of_xml)
    let of_json j =
      list_of_json ~kind:"KnowledgeBaseRetrievalResults"
        ~of_json:KnowledgeBaseRetrievalResult.of_json j
    let to_json v = composed_to_json to_value v
  end
module QueryTransformationType =
  struct
    type nonrec t =
      | QUERY_DECOMPOSITION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | QUERY_DECOMPOSITION -> "QUERY_DECOMPOSITION"
      | Non_static_id s -> s
    let of_string =
      function
      | "QUERY_DECOMPOSITION" -> QUERY_DECOMPOSITION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration QueryTransformationType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"QueryTransformationType" j)
    let to_json = simple_to_json to_value
  end
module QueryTransformationConfiguration =
  struct
    type nonrec t =
      {
      type_: QueryTransformationType.t
        [@ocaml.doc "The type of transformation to apply to the prompt."]}
    let context_ = "QueryTransformationConfiguration"
    let make ~type_ = fun () -> { type_ }
    let to_value x =
      structure_to_value
        [("type", (Some (QueryTransformationType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        QueryTransformationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" QueryTransformationType.of_json in
      make ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "To split up the prompt and retrieve multiple sources, set the transformation type to QUERY_DECOMPOSITION."]
module OrchestrationConfiguration =
  struct
    type nonrec t =
      {
      additionalModelRequestFields: AdditionalModelRequestFields.t option
        [@ocaml.doc
          "Additional model parameters and corresponding values not included in the textInferenceConfig structure for a knowledge base. This allows users to provide custom model parameters specific to the language model being used."];
      inferenceConfig: InferenceConfig.t option
        [@ocaml.doc
          "Configuration settings for inference when using RetrieveAndGenerate to generate responses while using a knowledge base as a source."];
      performanceConfig: PerformanceConfiguration.t option
        [@ocaml.doc "The latency configuration for the model."];
      promptTemplate: PromptTemplate.t option
        [@ocaml.doc
          "Contains the template for the prompt that's sent to the model. Orchestration prompts must include the $conversation_history$ and $output_format_instructions$ variables. For more information, see Use placeholder variables in the user guide."];
      queryTransformationConfiguration:
        QueryTransformationConfiguration.t option
        [@ocaml.doc
          "To split up the prompt and retrieve multiple sources, set the transformation type to QUERY_DECOMPOSITION."]}
    let make ?additionalModelRequestFields =
      fun ?inferenceConfig ->
        fun ?performanceConfig ->
          fun ?promptTemplate ->
            fun ?queryTransformationConfiguration ->
              fun () ->
                {
                  additionalModelRequestFields;
                  inferenceConfig;
                  performanceConfig;
                  promptTemplate;
                  queryTransformationConfiguration
                }
    let to_value x =
      structure_to_value
        [("additionalModelRequestFields",
           (Option.map x.additionalModelRequestFields
              ~f:AdditionalModelRequestFields.to_value));
        ("inferenceConfig",
          (Option.map x.inferenceConfig ~f:InferenceConfig.to_value));
        ("performanceConfig",
          (Option.map x.performanceConfig
             ~f:PerformanceConfiguration.to_value));
        ("promptTemplate",
          (Option.map x.promptTemplate ~f:PromptTemplate.to_value));
        ("queryTransformationConfiguration",
          (Option.map x.queryTransformationConfiguration
             ~f:QueryTransformationConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let queryTransformationConfiguration =
        (Option.map ~f:QueryTransformationConfiguration.of_xml)
          (Xml.child xml_arg0 "queryTransformationConfiguration") in
      let promptTemplate =
        (Option.map ~f:PromptTemplate.of_xml)
          (Xml.child xml_arg0 "promptTemplate") in
      let performanceConfig =
        (Option.map ~f:PerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performanceConfig") in
      let inferenceConfig =
        (Option.map ~f:InferenceConfig.of_xml)
          (Xml.child xml_arg0 "inferenceConfig") in
      let additionalModelRequestFields =
        (Option.map ~f:AdditionalModelRequestFields.of_xml)
          (Xml.child xml_arg0 "additionalModelRequestFields") in
      make ?queryTransformationConfiguration ?promptTemplate
        ?performanceConfig ?inferenceConfig ?additionalModelRequestFields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let queryTransformationConfiguration =
        field_map json__ "queryTransformationConfiguration"
          QueryTransformationConfiguration.of_json in
      let promptTemplate =
        field_map json__ "promptTemplate" PromptTemplate.of_json in
      let performanceConfig =
        field_map json__ "performanceConfig" PerformanceConfiguration.of_json in
      let inferenceConfig =
        field_map json__ "inferenceConfig" InferenceConfig.of_json in
      let additionalModelRequestFields =
        field_map json__ "additionalModelRequestFields"
          AdditionalModelRequestFields.of_json in
      make ?queryTransformationConfiguration ?promptTemplate
        ?performanceConfig ?inferenceConfig ?additionalModelRequestFields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Settings for how the model processes the prompt prior to retrieval and generation."]
module KnowledgeBaseRetrieveAndGenerateConfiguration =
  struct
    type nonrec t =
      {
      generationConfiguration: GenerationConfiguration.t option
        [@ocaml.doc
          "Contains configurations for response generation based on the knowledge base query results."];
      knowledgeBaseId: KnowledgeBaseId.t
        [@ocaml.doc
          "The unique identifier of the knowledge base that is queried."];
      modelArn: BedrockModelArn.t
        [@ocaml.doc
          "The ARN of the foundation model or inference profile used to generate a response."];
      orchestrationConfiguration: OrchestrationConfiguration.t option
        [@ocaml.doc
          "Settings for how the model processes the prompt prior to retrieval and generation."];
      retrievalConfiguration: KnowledgeBaseRetrievalConfiguration.t option
        [@ocaml.doc
          "Contains configurations for how to retrieve and return the knowledge base query."]}
    let context_ = "KnowledgeBaseRetrieveAndGenerateConfiguration"
    let make ?generationConfiguration =
      fun ?orchestrationConfiguration ->
        fun ?retrievalConfiguration ->
          fun ~knowledgeBaseId ->
            fun ~modelArn ->
              fun () ->
                {
                  generationConfiguration;
                  orchestrationConfiguration;
                  retrievalConfiguration;
                  knowledgeBaseId;
                  modelArn
                }
    let to_value x =
      structure_to_value
        [("generationConfiguration",
           (Option.map x.generationConfiguration
              ~f:GenerationConfiguration.to_value));
        ("knowledgeBaseId",
          (Some (KnowledgeBaseId.to_value x.knowledgeBaseId)));
        ("modelArn", (Some (BedrockModelArn.to_value x.modelArn)));
        ("orchestrationConfiguration",
          (Option.map x.orchestrationConfiguration
             ~f:OrchestrationConfiguration.to_value));
        ("retrievalConfiguration",
          (Option.map x.retrievalConfiguration
             ~f:KnowledgeBaseRetrievalConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retrievalConfiguration =
        (Option.map ~f:KnowledgeBaseRetrievalConfiguration.of_xml)
          (Xml.child xml_arg0 "retrievalConfiguration") in
      let orchestrationConfiguration =
        (Option.map ~f:OrchestrationConfiguration.of_xml)
          (Xml.child xml_arg0 "orchestrationConfiguration") in
      let modelArn =
        BedrockModelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "modelArn") in
      let knowledgeBaseId =
        KnowledgeBaseId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "knowledgeBaseId") in
      let generationConfiguration =
        (Option.map ~f:GenerationConfiguration.of_xml)
          (Xml.child xml_arg0 "generationConfiguration") in
      make ?retrievalConfiguration ?orchestrationConfiguration ~modelArn
        ~knowledgeBaseId ?generationConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retrievalConfiguration =
        field_map json__ "retrievalConfiguration"
          KnowledgeBaseRetrievalConfiguration.of_json in
      let orchestrationConfiguration =
        field_map json__ "orchestrationConfiguration"
          OrchestrationConfiguration.of_json in
      let modelArn = field_map_exn json__ "modelArn" BedrockModelArn.of_json in
      let knowledgeBaseId =
        field_map_exn json__ "knowledgeBaseId" KnowledgeBaseId.of_json in
      let generationConfiguration =
        field_map json__ "generationConfiguration"
          GenerationConfiguration.of_json in
      make ?retrievalConfiguration ?orchestrationConfiguration ~modelArn
        ~knowledgeBaseId ?generationConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the resource being queried. This data type is used in the following API operations: Retrieve request \226\128\147 in the knowledgeBaseConfiguration field RetrieveAndGenerate request \226\128\147 in the knowledgeBaseConfiguration field"]
module ListFlowExecutionEventsRequest =
  struct
    type nonrec t =
      {
      eventType: FlowExecutionEventType.t
        [@ocaml.doc
          "The type of events to retrieve. Specify Node for node-level events or Flow for flow-level events."];
      executionIdentifier: FlowExecutionIdentifier.t
        [@ocaml.doc "The unique identifier of the flow execution."];
      flowAliasIdentifier: FlowAliasIdentifier.t
        [@ocaml.doc
          "The unique identifier of the flow alias used for the execution."];
      flowIdentifier: FlowIdentifier.t
        [@ocaml.doc "The unique identifier of the flow."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of events to return in a single response. If more events exist than the specified maxResults value, a token is included in the response so that the remaining results can be retrieved."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token to retrieve the next set of results. This value is returned in the response if more results are available."]}
    let context_ = "ListFlowExecutionEventsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~eventType ->
          fun ~executionIdentifier ->
            fun ~flowAliasIdentifier ->
              fun ~flowIdentifier ->
                fun () ->
                  {
                    maxResults;
                    nextToken;
                    eventType;
                    executionIdentifier;
                    flowAliasIdentifier;
                    flowIdentifier
                  }
    let to_value x =
      structure_to_value
        [("eventType", (Some (FlowExecutionEventType.to_value x.eventType)));
        ("executionIdentifier",
          (Some (FlowExecutionIdentifier.to_value x.executionIdentifier)));
        ("flowAliasIdentifier",
          (Some (FlowAliasIdentifier.to_value x.flowAliasIdentifier)));
        ("flowIdentifier", (Some (FlowIdentifier.to_value x.flowIdentifier)));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let flowIdentifier =
        FlowIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        FlowAliasIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowAliasIdentifier") in
      let executionIdentifier =
        FlowExecutionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "executionIdentifier") in
      let eventType =
        FlowExecutionEventType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "eventType") in
      make ?nextToken ?maxResults ~flowIdentifier ~flowAliasIdentifier
        ~executionIdentifier ~eventType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let flowIdentifier =
        field_map_exn json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map_exn json__ "flowAliasIdentifier"
          FlowAliasIdentifier.of_json in
      let executionIdentifier =
        field_map_exn json__ "executionIdentifier"
          FlowExecutionIdentifier.of_json in
      let eventType =
        field_map_exn json__ "eventType" FlowExecutionEventType.of_json in
      make ?nextToken ?maxResults ~flowIdentifier ~flowAliasIdentifier
        ~executionIdentifier ~eventType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists events that occurred during a flow execution. Events provide detailed information about the execution progress, including node inputs and outputs, flow inputs and outputs, condition results, and failure events. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module ListFlowExecutionEventsResponse =
  struct
    type nonrec t =
      {
      flowExecutionEvents: FlowExecutionEvents.t option
        [@ocaml.doc
          "A list of events that occurred during the flow execution. Events can include node inputs and outputs, flow inputs and outputs, condition results, and failure events."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token to retrieve the next set of results. This value is returned if more results are available."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?flowExecutionEvents =
      fun ?nextToken -> fun () -> { flowExecutionEvents; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("flowExecutionEvents",
           (Option.map x.flowExecutionEvents ~f:FlowExecutionEvents.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let flowExecutionEvents =
        (Option.map ~f:FlowExecutionEvents.of_xml)
          (Xml.child xml_arg0 "flowExecutionEvents") in
      make ?nextToken ?flowExecutionEvents ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let flowExecutionEvents =
        field_map json__ "flowExecutionEvents" FlowExecutionEvents.of_json in
      make ?nextToken ?flowExecutionEvents ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists events that occurred during a flow execution. Events provide detailed information about the execution progress, including node inputs and outputs, flow inputs and outputs, condition results, and failure events. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module ListFlowExecutionsRequest =
  struct
    type nonrec t =
      {
      flowAliasIdentifier: FlowAliasIdentifier.t option
        [@ocaml.doc
          "The unique identifier of the flow alias to list executions for."];
      flowIdentifier: FlowIdentifier.t
        [@ocaml.doc
          "The unique identifier of the flow to list executions for."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of flow executions to return in a single response. If more executions exist than the specified maxResults value, a token is included in the response so that the remaining results can be retrieved."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token to retrieve the next set of results. This value is returned in the response if more results are available."]}
    let context_ = "ListFlowExecutionsRequest"
    let make ?flowAliasIdentifier =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~flowIdentifier ->
            fun () ->
              { flowAliasIdentifier; maxResults; nextToken; flowIdentifier }
    let to_value x =
      structure_to_value
        [("flowAliasIdentifier",
           (Option.map x.flowAliasIdentifier ~f:FlowAliasIdentifier.to_value));
        ("flowIdentifier", (Some (FlowIdentifier.to_value x.flowIdentifier)));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let flowIdentifier =
        FlowIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        (Option.map ~f:FlowAliasIdentifier.of_xml)
          (Xml.child xml_arg0 "flowAliasIdentifier") in
      make ?nextToken ?maxResults ~flowIdentifier ?flowAliasIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let flowIdentifier =
        field_map_exn json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map json__ "flowAliasIdentifier" FlowAliasIdentifier.of_json in
      make ?nextToken ?maxResults ~flowIdentifier ?flowAliasIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all executions of a flow. Results can be paginated and include summary information about each execution, such as status, start and end times, and the execution's Amazon Resource Name (ARN). Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module ListFlowExecutionsResponse =
  struct
    type nonrec t =
      {
      flowExecutionSummaries: FlowExecutionSummaries.t option
        [@ocaml.doc
          "A list of flow execution summaries. Each summary includes the execution ARN, flow identifier, flow alias identifier, flow version, status, and timestamps."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token to retrieve the next set of results. This value is returned if more results are available."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?flowExecutionSummaries =
      fun ?nextToken -> fun () -> { flowExecutionSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("flowExecutionSummaries",
           (Option.map x.flowExecutionSummaries
              ~f:FlowExecutionSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let flowExecutionSummaries =
        (Option.map ~f:FlowExecutionSummaries.of_xml)
          (Xml.child xml_arg0 "flowExecutionSummaries") in
      make ?nextToken ?flowExecutionSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let flowExecutionSummaries =
        field_map json__ "flowExecutionSummaries"
          FlowExecutionSummaries.of_json in
      make ?nextToken ?flowExecutionSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all executions of a flow. Results can be paginated and include summary information about each execution, such as status, start and end times, and the execution's Amazon Resource Name (ARN). Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module ListInvocationStepsRequest =
  struct
    type nonrec t =
      {
      invocationIdentifier: InvocationIdentifier.t option
        [@ocaml.doc
          "The unique identifier (in UUID format) for the invocation to list invocation steps for."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results."];
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "The unique identifier for the session associated with the invocation steps. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."]}
    let context_ = "ListInvocationStepsRequest"
    let make ?invocationIdentifier =
      fun ?maxResults ->
        fun ?nextToken ->
          fun ~sessionIdentifier ->
            fun () ->
              {
                invocationIdentifier;
                maxResults;
                nextToken;
                sessionIdentifier
              }
    let to_value x =
      structure_to_value
        [("invocationIdentifier",
           (Option.map x.invocationIdentifier
              ~f:InvocationIdentifier.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("sessionIdentifier",
          (Some (SessionIdentifier.to_value x.sessionIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let invocationIdentifier =
        (Option.map ~f:InvocationIdentifier.of_xml)
          (Xml.child xml_arg0 "invocationIdentifier") in
      make ~sessionIdentifier ?nextToken ?maxResults ?invocationIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let invocationIdentifier =
        field_map json__ "invocationIdentifier" InvocationIdentifier.of_json in
      make ~sessionIdentifier ?nextToken ?maxResults ?invocationIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all invocation steps associated with a session and optionally, an invocation within the session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module ListInvocationStepsResponse =
  struct
    type nonrec t =
      {
      invocationStepSummaries: InvocationStepSummaries.t option
        [@ocaml.doc
          "A list of summaries for each invocation step associated with a session and if you specified it, an invocation within the session."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than the maxResults value provided in the request, use this token when making another request in the nextToken field to return the next batch of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?invocationStepSummaries =
      fun ?nextToken -> fun () -> { invocationStepSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("invocationStepSummaries",
           (Option.map x.invocationStepSummaries
              ~f:InvocationStepSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let invocationStepSummaries =
        (Option.map ~f:InvocationStepSummaries.of_xml)
          (Xml.child xml_arg0 "invocationStepSummaries") in
      make ?nextToken ?invocationStepSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let invocationStepSummaries =
        field_map json__ "invocationStepSummaries"
          InvocationStepSummaries.of_json in
      make ?nextToken ?invocationStepSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all invocation steps associated with a session and optionally, an invocation within the session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module ListInvocationsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results."];
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "The unique identifier for the session to list invocations for. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."]}
    let context_ = "ListInvocationsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~sessionIdentifier ->
          fun () -> { maxResults; nextToken; sessionIdentifier }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("sessionIdentifier",
          (Some (SessionIdentifier.to_value x.sessionIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ~sessionIdentifier ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      make ~sessionIdentifier ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all invocations associated with a specific session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module ListInvocationsResponse =
  struct
    type nonrec t =
      {
      invocationSummaries: InvocationSummaries.t option
        [@ocaml.doc
          "A list of invocation summaries associated with the session."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than the maxResults value provided in the request, use this token when making another request in the nextToken field to return the next batch of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?invocationSummaries =
      fun ?nextToken -> fun () -> { invocationSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("invocationSummaries",
           (Option.map x.invocationSummaries ~f:InvocationSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let invocationSummaries =
        (Option.map ~f:InvocationSummaries.of_xml)
          (Xml.child xml_arg0 "invocationSummaries") in
      make ?nextToken ?invocationSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let invocationSummaries =
        field_map json__ "invocationSummaries" InvocationSummaries.of_json in
      make ?nextToken ?invocationSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all invocations associated with a specific session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module ListSessionsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all sessions in your Amazon Web Services account. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module SessionSummary =
  struct
    type nonrec t =
      {
      createdAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the session was created."];
      lastUpdatedAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the session was last modified."];
      sessionArn: SessionArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the session."];
      sessionId: Uuid.t option
        [@ocaml.doc "The unique identifier for the session."];
      sessionStatus: SessionStatus.t option
        [@ocaml.doc "The current status of the session."]}
    let make ?createdAt =
      fun ?lastUpdatedAt ->
        fun ?sessionArn ->
          fun ?sessionId ->
            fun ?sessionStatus ->
              fun () ->
                {
                  createdAt;
                  lastUpdatedAt;
                  sessionArn;
                  sessionId;
                  sessionStatus
                }
    let to_value x =
      structure_to_value
        [("createdAt", (Option.map x.createdAt ~f:DateTimestamp.to_value));
        ("lastUpdatedAt",
          (Option.map x.lastUpdatedAt ~f:DateTimestamp.to_value));
        ("sessionArn", (Option.map x.sessionArn ~f:SessionArn.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value));
        ("sessionStatus",
          (Option.map x.sessionStatus ~f:SessionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionStatus =
        (Option.map ~f:SessionStatus.of_xml)
          (Xml.child xml_arg0 "sessionStatus") in
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionArn =
        (Option.map ~f:SessionArn.of_xml) (Xml.child xml_arg0 "sessionArn") in
      let lastUpdatedAt =
        (Option.map ~f:DateTimestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdatedAt") in
      let createdAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      make ?sessionStatus ?sessionId ?sessionArn ?lastUpdatedAt ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionStatus =
        field_map json__ "sessionStatus" SessionStatus.of_json in
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let sessionArn = field_map json__ "sessionArn" SessionArn.of_json in
      let lastUpdatedAt =
        field_map json__ "lastUpdatedAt" DateTimestamp.of_json in
      let createdAt = field_map json__ "createdAt" DateTimestamp.of_json in
      make ?sessionStatus ?sessionId ?sessionArn ?lastUpdatedAt ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about a session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module SessionSummaries =
  struct
    type nonrec t = SessionSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SessionSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SessionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SessionSummaries" ~of_json:SessionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListSessionsResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than the maxResults value provided in the request, use this token when making another request in the nextToken field to return the next batch of results."];
      sessionSummaries: SessionSummaries.t option
        [@ocaml.doc
          "A list of summaries for each session in your Amazon Web Services account."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?sessionSummaries -> fun () -> { nextToken; sessionSummaries }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("sessionSummaries",
          (Option.map x.sessionSummaries ~f:SessionSummaries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionSummaries =
        (Option.map ~f:SessionSummaries.of_xml)
          (Xml.child xml_arg0 "sessionSummaries") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?sessionSummaries ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionSummaries =
        field_map json__ "sessionSummaries" SessionSummaries.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?sessionSummaries ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all sessions in your Amazon Web Services account. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module TaggableResourcesArn =
  struct
    type nonrec t = string[@@ocaml.doc
                            "ARN of Taggable resources: \\[session\\]"]
    let context_ = "TaggableResourcesArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(^arn:aws(-[^:]+)?:bedrock:[a-zA-Z0-9-]+:[0-9]{12}:(session)/[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TaggableResourcesArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "ARN of Taggable resources: \\[session\\]"]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: TaggableResourcesArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource for which to list tags."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn",
           (Some (TaggableResourcesArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        TaggableResourcesArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" TaggableResourcesArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List all the tags for the resource you specify."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagsMap.t option
        [@ocaml.doc
          "The key-value pairs for the tags associated with the resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagsMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagsMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagsMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List all the tags for the resource you specify."]
module OptimizePromptRequestTargetModelIdString =
  struct
    type nonrec t = string
    let context_ = "OptimizePromptRequestTargetModelIdString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(arn:aws(-[^:]+)?:bedrock:[a-z0-9-]{1,20}:(([0-9]{12}:custom-model/[a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}/[a-z0-9]{12})|(:foundation-model/[a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([.:]?[a-z0-9-]{1,63}))|([0-9]{12}:provisioned-model/[a-z0-9]{12})))|([a-z0-9-]{1,63}[.]{1}[a-z0-9-]{1,63}([.:]?[a-z0-9-]{1,63}))|(([0-9a-zA-Z][_-]?)+)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"OptimizePromptRequestTargetModelIdString" j
    let to_json = simple_to_json to_value
  end
module OptimizePromptRequest =
  struct
    type nonrec t =
      {
      input: InputPrompt.t [@ocaml.doc "Contains the prompt to optimize."];
      targetModelId: OptimizePromptRequestTargetModelIdString.t
        [@ocaml.doc
          "The unique identifier of the model that you want to optimize the prompt for."]}
    let context_ = "OptimizePromptRequest"
    let make ~input =
      fun ~targetModelId -> fun () -> { input; targetModelId }
    let to_value x =
      structure_to_value
        [("input", (Some (InputPrompt.to_value x.input)));
        ("targetModelId",
          (Some
             (OptimizePromptRequestTargetModelIdString.to_value
                x.targetModelId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetModelId =
        OptimizePromptRequestTargetModelIdString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "targetModelId") in
      let input =
        InputPrompt.of_xml (Xml.child_exn ~context:context_ xml_arg0 "input") in
      make ~targetModelId ~input ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetModelId =
        field_map_exn json__ "targetModelId"
          OptimizePromptRequestTargetModelIdString.of_json in
      let input = field_map_exn json__ "input" InputPrompt.of_json in
      make ~targetModelId ~input ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Optimizes a prompt for the task that you specify. For more information, see Optimize a prompt in the Amazon Bedrock User Guide."]
module OptimizedPrompt =
  struct
    type nonrec t =
      {
      textPrompt: TextPrompt.t option
        [@ocaml.doc
          "Contains information about the text in the prompt that was optimized."]}
    let make ?textPrompt = fun () -> { textPrompt }
    let to_value x =
      structure_to_value
        [("textPrompt", (Option.map x.textPrompt ~f:TextPrompt.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let textPrompt =
        (Option.map ~f:TextPrompt.of_xml) (Xml.child xml_arg0 "textPrompt") in
      make ?textPrompt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let textPrompt = field_map json__ "textPrompt" TextPrompt.of_json in
      make ?textPrompt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about the optimized prompt."]
module OptimizedPromptEvent =
  struct
    type nonrec t =
      {
      optimizedPrompt: OptimizedPrompt.t option
        [@ocaml.doc "Contains information about the optimized prompt."]}
    let make ?optimizedPrompt = fun () -> { optimizedPrompt }
    let to_value x =
      structure_to_value
        [("optimizedPrompt",
           (Option.map x.optimizedPrompt ~f:OptimizedPrompt.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let optimizedPrompt =
        (Option.map ~f:OptimizedPrompt.of_xml)
          (Xml.child xml_arg0 "optimizedPrompt") in
      make ?optimizedPrompt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let optimizedPrompt =
        field_map json__ "optimizedPrompt" OptimizedPrompt.of_json in
      make ?optimizedPrompt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An event in which the prompt was optimized."]
module OptimizedPromptStream =
  struct
    type nonrec t =
      {
      accessDeniedException: AccessDeniedException.t option
        [@ocaml.doc
          "The request is denied because of missing access permissions. Check your permissions and retry your request."];
      analyzePromptEvent: AnalyzePromptEvent.t option
        [@ocaml.doc
          "An event in which the prompt was analyzed in preparation for optimization."];
      badGatewayException: BadGatewayException.t option
        [@ocaml.doc
          "There was an issue with a dependency due to a server issue. Retry your request."];
      dependencyFailedException: DependencyFailedException.t option
        [@ocaml.doc
          "There was an issue with a dependency. Check the resource configurations and retry the request."];
      internalServerException: InternalServerException.t option
        [@ocaml.doc "An internal server error occurred. Retry your request."];
      optimizedPromptEvent: OptimizedPromptEvent.t option
        [@ocaml.doc "An event in which the prompt was optimized."];
      throttlingException: ThrottlingException.t option
        [@ocaml.doc
          "Your request was throttled because of service-wide limitations. Resubmit your request later or in a different region. You can also purchase Provisioned Throughput to increase the rate or number of tokens you can process."];
      validationException: ValidationException.t option
        [@ocaml.doc
          "Input validation failed. Check your request parameters and retry the request."]}
    let make ?accessDeniedException =
      fun ?analyzePromptEvent ->
        fun ?badGatewayException ->
          fun ?dependencyFailedException ->
            fun ?internalServerException ->
              fun ?optimizedPromptEvent ->
                fun ?throttlingException ->
                  fun ?validationException ->
                    fun () ->
                      {
                        accessDeniedException;
                        analyzePromptEvent;
                        badGatewayException;
                        dependencyFailedException;
                        internalServerException;
                        optimizedPromptEvent;
                        throttlingException;
                        validationException
                      }
    let to_value x =
      structure_to_value
        [("accessDeniedException",
           (Option.map x.accessDeniedException
              ~f:AccessDeniedException.to_value));
        ("analyzePromptEvent",
          (Option.map x.analyzePromptEvent ~f:AnalyzePromptEvent.to_value));
        ("badGatewayException",
          (Option.map x.badGatewayException ~f:BadGatewayException.to_value));
        ("dependencyFailedException",
          (Option.map x.dependencyFailedException
             ~f:DependencyFailedException.to_value));
        ("internalServerException",
          (Option.map x.internalServerException
             ~f:InternalServerException.to_value));
        ("optimizedPromptEvent",
          (Option.map x.optimizedPromptEvent ~f:OptimizedPromptEvent.to_value));
        ("throttlingException",
          (Option.map x.throttlingException ~f:ThrottlingException.to_value));
        ("validationException",
          (Option.map x.validationException ~f:ValidationException.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validationException =
        (Option.map ~f:ValidationException.of_xml)
          (Xml.child xml_arg0 "validationException") in
      let throttlingException =
        (Option.map ~f:ThrottlingException.of_xml)
          (Xml.child xml_arg0 "throttlingException") in
      let optimizedPromptEvent =
        (Option.map ~f:OptimizedPromptEvent.of_xml)
          (Xml.child xml_arg0 "optimizedPromptEvent") in
      let internalServerException =
        (Option.map ~f:InternalServerException.of_xml)
          (Xml.child xml_arg0 "internalServerException") in
      let dependencyFailedException =
        (Option.map ~f:DependencyFailedException.of_xml)
          (Xml.child xml_arg0 "dependencyFailedException") in
      let badGatewayException =
        (Option.map ~f:BadGatewayException.of_xml)
          (Xml.child xml_arg0 "badGatewayException") in
      let analyzePromptEvent =
        (Option.map ~f:AnalyzePromptEvent.of_xml)
          (Xml.child xml_arg0 "analyzePromptEvent") in
      let accessDeniedException =
        (Option.map ~f:AccessDeniedException.of_xml)
          (Xml.child xml_arg0 "accessDeniedException") in
      make ?validationException ?throttlingException ?optimizedPromptEvent
        ?internalServerException ?dependencyFailedException
        ?badGatewayException ?analyzePromptEvent ?accessDeniedException ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let validationException =
        field_map json__ "validationException" ValidationException.of_json in
      let throttlingException =
        field_map json__ "throttlingException" ThrottlingException.of_json in
      let optimizedPromptEvent =
        field_map json__ "optimizedPromptEvent" OptimizedPromptEvent.of_json in
      let internalServerException =
        field_map json__ "internalServerException"
          InternalServerException.of_json in
      let dependencyFailedException =
        field_map json__ "dependencyFailedException"
          DependencyFailedException.of_json in
      let badGatewayException =
        field_map json__ "badGatewayException" BadGatewayException.of_json in
      let analyzePromptEvent =
        field_map json__ "analyzePromptEvent" AnalyzePromptEvent.of_json in
      let accessDeniedException =
        field_map json__ "accessDeniedException"
          AccessDeniedException.of_json in
      make ?validationException ?throttlingException ?optimizedPromptEvent
        ?internalServerException ?dependencyFailedException
        ?badGatewayException ?analyzePromptEvent ?accessDeniedException ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The stream containing events in the prompt optimization process."]
module OptimizePromptResponse =
  struct
    type nonrec t =
      {
      optimizedPrompt: OptimizedPromptStream.t option
        [@ocaml.doc "The prompt after being optimized for the task."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?optimizedPrompt = fun () -> { optimizedPrompt }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) -> make ?optimizedPrompt:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("optimizedPrompt",
           (Option.map x.optimizedPrompt ~f:OptimizedPromptStream.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let optimizedPrompt =
        (Option.map ~f:OptimizedPromptStream.of_xml)
          (Xml.child xml_arg0 "optimizedPrompt") in
      make ?optimizedPrompt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let optimizedPrompt =
        field_map json__ "optimizedPrompt" OptimizedPromptStream.of_json in
      make ?optimizedPrompt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Optimizes a prompt for the task that you specify. For more information, see Optimize a prompt in the Amazon Bedrock User Guide."]
module PutInvocationStepRequest =
  struct
    type nonrec t =
      {
      invocationIdentifier: InvocationIdentifier.t
        [@ocaml.doc
          "The unique identifier (in UUID format) of the invocation to add the invocation step to."];
      invocationStepId: Uuid.t option
        [@ocaml.doc
          "The unique identifier of the invocation step in UUID format."];
      invocationStepTime: DateTimestamp.t
        [@ocaml.doc "The timestamp for when the invocation step occurred."];
      payload: InvocationStepPayload.t
        [@ocaml.doc
          "The payload for the invocation step, including text and images for the interaction."];
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "The unique identifier for the session to add the invocation step to. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."]}
    let context_ = "PutInvocationStepRequest"
    let make ?invocationStepId =
      fun ~invocationIdentifier ->
        fun ~invocationStepTime ->
          fun ~payload ->
            fun ~sessionIdentifier ->
              fun () ->
                {
                  invocationStepId;
                  invocationIdentifier;
                  invocationStepTime;
                  payload;
                  sessionIdentifier
                }
    let to_value x =
      structure_to_value
        [("invocationIdentifier",
           (Some (InvocationIdentifier.to_value x.invocationIdentifier)));
        ("invocationStepId",
          (Option.map x.invocationStepId ~f:Uuid.to_value));
        ("invocationStepTime",
          (Some (DateTimestamp.to_value x.invocationStepTime)));
        ("payload", (Some (InvocationStepPayload.to_value x.payload)));
        ("sessionIdentifier",
          (Some (SessionIdentifier.to_value x.sessionIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      let payload =
        InvocationStepPayload.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "payload") in
      let invocationStepTime =
        DateTimestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "invocationStepTime") in
      let invocationStepId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationStepId") in
      let invocationIdentifier =
        InvocationIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "invocationIdentifier") in
      make ~sessionIdentifier ~payload ~invocationStepTime ?invocationStepId
        ~invocationIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      let payload =
        field_map_exn json__ "payload" InvocationStepPayload.of_json in
      let invocationStepTime =
        field_map_exn json__ "invocationStepTime" DateTimestamp.of_json in
      let invocationStepId = field_map json__ "invocationStepId" Uuid.of_json in
      let invocationIdentifier =
        field_map_exn json__ "invocationIdentifier"
          InvocationIdentifier.of_json in
      make ~sessionIdentifier ~payload ~invocationStepTime ?invocationStepId
        ~invocationIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Add an invocation step to an invocation in a session. An invocation step stores fine-grained state checkpoints, including text and images, for each interaction. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions. Related APIs: GetInvocationStep ListInvocationSteps ListInvocations ListSessions"]
module PutInvocationStepResponse =
  struct
    type nonrec t =
      {
      invocationStepId: Uuid.t option
        [@ocaml.doc
          "The unique identifier of the invocation step in UUID format."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?invocationStepId = fun () -> { invocationStepId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("invocationStepId",
           (Option.map x.invocationStepId ~f:Uuid.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationStepId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "invocationStepId") in
      make ?invocationStepId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invocationStepId = field_map json__ "invocationStepId" Uuid.of_json in
      make ?invocationStepId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Add an invocation step to an invocation in a session. An invocation step stores fine-grained state checkpoints, including text and images, for each interaction. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions. Related APIs: GetInvocationStep ListInvocationSteps ListInvocations ListSessions"]
module RerankTextDocumentTextString =
  struct
    type nonrec t = string
    let context_ = "RerankTextDocumentTextString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RerankTextDocumentTextString" j
    let to_json = simple_to_json to_value
  end
module RerankTextDocument =
  struct
    type nonrec t =
      {
      text: RerankTextDocumentTextString.t option
        [@ocaml.doc "The text of the document."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value
        [("text",
           (Option.map x.text ~f:RerankTextDocumentTextString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        (Option.map ~f:RerankTextDocumentTextString.of_xml)
          (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" RerankTextDocumentTextString.of_json in
      make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about a text document to rerank."]
module RerankDocumentType =
  struct
    type nonrec t =
      | TEXT 
      | JSON 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | TEXT -> "TEXT" | JSON -> "JSON" | Non_static_id s -> s
    let of_string =
      function | "TEXT" -> TEXT | "JSON" -> JSON | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RerankDocumentType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RerankDocumentType" j)
    let to_json = simple_to_json to_value
  end
module RerankDocument =
  struct
    type nonrec t =
      {
      jsonDocument: Document.t option
        [@ocaml.doc "Contains a JSON document to rerank."];
      textDocument: RerankTextDocument.t option
        [@ocaml.doc "Contains information about a text document to rerank."];
      type_: RerankDocumentType.t
        [@ocaml.doc "The type of document to rerank."]}
    let context_ = "RerankDocument"
    let make ?jsonDocument =
      fun ?textDocument ->
        fun ~type_ -> fun () -> { jsonDocument; textDocument; type_ }
    let to_value x =
      structure_to_value
        [("jsonDocument", (Option.map x.jsonDocument ~f:Document.to_value));
        ("textDocument",
          (Option.map x.textDocument ~f:RerankTextDocument.to_value));
        ("type", (Some (RerankDocumentType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        RerankDocumentType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let textDocument =
        (Option.map ~f:RerankTextDocument.of_xml)
          (Xml.child xml_arg0 "textDocument") in
      let jsonDocument =
        (Option.map ~f:Document.of_xml) (Xml.child xml_arg0 "jsonDocument") in
      make ~type_ ?textDocument ?jsonDocument ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" RerankDocumentType.of_json in
      let textDocument =
        field_map json__ "textDocument" RerankTextDocument.of_json in
      let jsonDocument = field_map json__ "jsonDocument" Document.of_json in
      make ~type_ ?textDocument ?jsonDocument ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a document to rerank. Choose the type to define and include the field that corresponds to the type."]
module RerankQueryContentType =
  struct
    type nonrec t =
      | TEXT 
      | Non_static_id of string 
    let make i = i
    let to_string = function | TEXT -> "TEXT" | Non_static_id s -> s
    let of_string = function | "TEXT" -> TEXT | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RerankQueryContentType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RerankQueryContentType" j)
    let to_json = simple_to_json to_value
  end
module RerankQuery =
  struct
    type nonrec t =
      {
      textQuery: RerankTextDocument.t
        [@ocaml.doc "Contains information about a text query."];
      type_: RerankQueryContentType.t [@ocaml.doc "The type of the query."]}
    let context_ = "RerankQuery"
    let make ~textQuery = fun ~type_ -> fun () -> { textQuery; type_ }
    let to_value x =
      structure_to_value
        [("textQuery", (Some (RerankTextDocument.to_value x.textQuery)));
        ("type", (Some (RerankQueryContentType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        RerankQueryContentType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let textQuery =
        RerankTextDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "textQuery") in
      make ~type_ ~textQuery ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" RerankQueryContentType.of_json in
      let textQuery =
        field_map_exn json__ "textQuery" RerankTextDocument.of_json in
      make ~type_ ~textQuery ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a query to submit to the reranker model."]
module RerankQueriesList =
  struct
    type nonrec t = RerankQuery.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RerankQuery.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RerankQuery.of_xml)
    let of_json j =
      list_of_json ~kind:"RerankQueriesList" ~of_json:RerankQuery.of_json j
    let to_json v = composed_to_json to_value v
  end
module RerankingConfigurationType =
  struct
    type nonrec t =
      | BEDROCK_RERANKING_MODEL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BEDROCK_RERANKING_MODEL -> "BEDROCK_RERANKING_MODEL"
      | Non_static_id s -> s
    let of_string =
      function
      | "BEDROCK_RERANKING_MODEL" -> BEDROCK_RERANKING_MODEL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RerankingConfigurationType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RerankingConfigurationType" j)
    let to_json = simple_to_json to_value
  end
module RerankingConfiguration =
  struct
    type nonrec t =
      {
      bedrockRerankingConfiguration: BedrockRerankingConfiguration.t
        [@ocaml.doc
          "Contains configurations for an Amazon Bedrock reranker."];
      type_: RerankingConfigurationType.t
        [@ocaml.doc "The type of reranker that the configurations apply to."]}
    let context_ = "RerankingConfiguration"
    let make ~bedrockRerankingConfiguration =
      fun ~type_ -> fun () -> { bedrockRerankingConfiguration; type_ }
    let to_value x =
      structure_to_value
        [("bedrockRerankingConfiguration",
           (Some
              (BedrockRerankingConfiguration.to_value
                 x.bedrockRerankingConfiguration)));
        ("type", (Some (RerankingConfigurationType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        RerankingConfigurationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let bedrockRerankingConfiguration =
        BedrockRerankingConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "bedrockRerankingConfiguration") in
      make ~type_ ~bedrockRerankingConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ =
        field_map_exn json__ "type" RerankingConfigurationType.of_json in
      let bedrockRerankingConfiguration =
        field_map_exn json__ "bedrockRerankingConfiguration"
          BedrockRerankingConfiguration.of_json in
      make ~type_ ~bedrockRerankingConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains configurations for reranking."]
module RerankSourceType =
  struct
    type nonrec t =
      | INLINE 
      | Non_static_id of string 
    let make i = i
    let to_string = function | INLINE -> "INLINE" | Non_static_id s -> s
    let of_string = function | "INLINE" -> INLINE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RerankSourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RerankSourceType" j)
    let to_json = simple_to_json to_value
  end
module RerankSource =
  struct
    type nonrec t =
      {
      inlineDocumentSource: RerankDocument.t
        [@ocaml.doc
          "Contains an inline definition of a source for reranking."];
      type_: RerankSourceType.t [@ocaml.doc "The type of the source."]}
    let context_ = "RerankSource"
    let make ~inlineDocumentSource =
      fun ~type_ -> fun () -> { inlineDocumentSource; type_ }
    let to_value x =
      structure_to_value
        [("inlineDocumentSource",
           (Some (RerankDocument.to_value x.inlineDocumentSource)));
        ("type", (Some (RerankSourceType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        RerankSourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let inlineDocumentSource =
        RerankDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "inlineDocumentSource") in
      make ~type_ ~inlineDocumentSource ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" RerankSourceType.of_json in
      let inlineDocumentSource =
        field_map_exn json__ "inlineDocumentSource" RerankDocument.of_json in
      make ~type_ ~inlineDocumentSource ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about a source for reranking."]
module RerankSourcesList =
  struct
    type nonrec t = RerankSource.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1000) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RerankSource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RerankSource.of_xml)
    let of_json j =
      list_of_json ~kind:"RerankSourcesList" ~of_json:RerankSource.of_json j
    let to_json v = composed_to_json to_value v
  end
module RerankRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results was greater than could fit in a response, a token is returned in the nextToken field. You can enter that token in this field to return the next batch of results."];
      queries: RerankQueriesList.t
        [@ocaml.doc
          "An array of objects, each of which contains information about a query to submit to the reranker model."];
      rerankingConfiguration: RerankingConfiguration.t
        [@ocaml.doc "Contains configurations for reranking."];
      sources: RerankSourcesList.t
        [@ocaml.doc
          "An array of objects, each of which contains information about the sources to rerank."]}
    let context_ = "RerankRequest"
    let make ?nextToken =
      fun ~queries ->
        fun ~rerankingConfiguration ->
          fun ~sources ->
            fun () -> { nextToken; queries; rerankingConfiguration; sources }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("queries", (Some (RerankQueriesList.to_value x.queries)));
        ("rerankingConfiguration",
          (Some (RerankingConfiguration.to_value x.rerankingConfiguration)));
        ("sources", (Some (RerankSourcesList.to_value x.sources)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sources =
        RerankSourcesList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sources") in
      let rerankingConfiguration =
        RerankingConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "rerankingConfiguration") in
      let queries =
        RerankQueriesList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "queries") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ~sources ~rerankingConfiguration ~queries ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sources = field_map_exn json__ "sources" RerankSourcesList.of_json in
      let rerankingConfiguration =
        field_map_exn json__ "rerankingConfiguration"
          RerankingConfiguration.of_json in
      let queries = field_map_exn json__ "queries" RerankQueriesList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ~sources ~rerankingConfiguration ~queries ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Reranks the relevance of sources based on queries. For more information, see Improve the relevance of query responses with a reranker model."]
module RerankResultIndexInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for RerankResultIndexInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module RerankResult =
  struct
    type nonrec t =
      {
      document: RerankDocument.t option
        [@ocaml.doc "Contains information about the document."];
      index: RerankResultIndexInteger.t option
        [@ocaml.doc
          "The original index of the document from the input sources array."];
      relevanceScore: Float_.t option
        [@ocaml.doc "The relevance score of the document."]}
    let make ?document =
      fun ?index ->
        fun ?relevanceScore -> fun () -> { document; index; relevanceScore }
    let to_value x =
      structure_to_value
        [("document", (Option.map x.document ~f:RerankDocument.to_value));
        ("index", (Option.map x.index ~f:RerankResultIndexInteger.to_value));
        ("relevanceScore", (Option.map x.relevanceScore ~f:Float_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let relevanceScore =
        (Option.map ~f:Float_.of_xml) (Xml.child xml_arg0 "relevanceScore") in
      let index =
        (Option.map ~f:RerankResultIndexInteger.of_xml)
          (Xml.child xml_arg0 "index") in
      let document =
        (Option.map ~f:RerankDocument.of_xml) (Xml.child xml_arg0 "document") in
      make ?relevanceScore ?index ?document ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let relevanceScore = field_map json__ "relevanceScore" Float_.of_json in
      let index = field_map json__ "index" RerankResultIndexInteger.of_json in
      let document = field_map json__ "document" RerankDocument.of_json in
      make ?relevanceScore ?index ?document ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains information about a document that was reranked."]
module RerankResultsList =
  struct
    type nonrec t = RerankResult.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RerankResult.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RerankResult.of_xml)
    let of_json j =
      list_of_json ~kind:"RerankResultsList" ~of_json:RerankResult.of_json j
    let to_json v = composed_to_json to_value v
  end
module RerankResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the total number of results is greater than can fit in the response, use this token in the nextToken field when making another request to return the next batch of results."];
      results: RerankResultsList.t option
        [@ocaml.doc
          "An array of objects, each of which contains information about the results of reranking."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?results -> fun () -> { nextToken; results }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("results", (Option.map x.results ~f:RerankResultsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let results =
        (Option.map ~f:RerankResultsList.of_xml)
          (Xml.child xml_arg0 "results") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?results ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let results = field_map json__ "results" RerankResultsList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?results ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Reranks the relevance of sources based on queries. For more information, see Improve the relevance of query responses with a reranker model."]
module RetrieveAndGenerateType =
  struct
    type nonrec t =
      | KNOWLEDGE_BASE 
      | EXTERNAL_SOURCES 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | KNOWLEDGE_BASE -> "KNOWLEDGE_BASE"
      | EXTERNAL_SOURCES -> "EXTERNAL_SOURCES"
      | Non_static_id s -> s
    let of_string =
      function
      | "KNOWLEDGE_BASE" -> KNOWLEDGE_BASE
      | "EXTERNAL_SOURCES" -> EXTERNAL_SOURCES
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RetrieveAndGenerateType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RetrieveAndGenerateType" j)
    let to_json = simple_to_json to_value
  end
module RetrieveAndGenerateConfiguration =
  struct
    type nonrec t =
      {
      externalSourcesConfiguration:
        ExternalSourcesRetrieveAndGenerateConfiguration.t option
        [@ocaml.doc
          "The configuration for the external source wrapper object in the retrieveAndGenerate function."];
      knowledgeBaseConfiguration:
        KnowledgeBaseRetrieveAndGenerateConfiguration.t option
        [@ocaml.doc
          "Contains details about the knowledge base for retrieving information and generating responses."];
      type_: RetrieveAndGenerateType.t
        [@ocaml.doc
          "The type of resource that contains your data for retrieving information and generating responses. If you choose to use EXTERNAL_SOURCES, then currently only Anthropic Claude 3 Sonnet models for knowledge bases are supported."]}
    let context_ = "RetrieveAndGenerateConfiguration"
    let make ?externalSourcesConfiguration =
      fun ?knowledgeBaseConfiguration ->
        fun ~type_ ->
          fun () ->
            { externalSourcesConfiguration; knowledgeBaseConfiguration; type_
            }
    let to_value x =
      structure_to_value
        [("externalSourcesConfiguration",
           (Option.map x.externalSourcesConfiguration
              ~f:ExternalSourcesRetrieveAndGenerateConfiguration.to_value));
        ("knowledgeBaseConfiguration",
          (Option.map x.knowledgeBaseConfiguration
             ~f:KnowledgeBaseRetrieveAndGenerateConfiguration.to_value));
        ("type", (Some (RetrieveAndGenerateType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        RetrieveAndGenerateType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let knowledgeBaseConfiguration =
        (Option.map ~f:KnowledgeBaseRetrieveAndGenerateConfiguration.of_xml)
          (Xml.child xml_arg0 "knowledgeBaseConfiguration") in
      let externalSourcesConfiguration =
        (Option.map ~f:ExternalSourcesRetrieveAndGenerateConfiguration.of_xml)
          (Xml.child xml_arg0 "externalSourcesConfiguration") in
      make ~type_ ?knowledgeBaseConfiguration ?externalSourcesConfiguration
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map_exn json__ "type" RetrieveAndGenerateType.of_json in
      let knowledgeBaseConfiguration =
        field_map json__ "knowledgeBaseConfiguration"
          KnowledgeBaseRetrieveAndGenerateConfiguration.of_json in
      let externalSourcesConfiguration =
        field_map json__ "externalSourcesConfiguration"
          ExternalSourcesRetrieveAndGenerateConfiguration.of_json in
      make ~type_ ?knowledgeBaseConfiguration ?externalSourcesConfiguration
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains details about the resource being queried. This data type is used in the following API operations: RetrieveAndGenerate request \226\128\147 in the retrieveAndGenerateConfiguration field"]
module RetrieveAndGenerateInputTextString =
  struct
    type nonrec t = string
    let context_ = "RetrieveAndGenerateInputTextString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"RetrieveAndGenerateInputTextString" j
    let to_json = simple_to_json to_value
  end
module RetrieveAndGenerateInput =
  struct
    type nonrec t =
      {
      text: RetrieveAndGenerateInputTextString.t
        [@ocaml.doc "The query made to the knowledge base, in characters."]}
    let context_ = "RetrieveAndGenerateInput"
    let make ~text = fun () -> { text }
    let to_value x =
      structure_to_value
        [("text",
           (Some (RetrieveAndGenerateInputTextString.to_value x.text)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text =
        RetrieveAndGenerateInputTextString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "text") in
      make ~text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text =
        field_map_exn json__ "text"
          RetrieveAndGenerateInputTextString.of_json in
      make ~text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the query made to the knowledge base. This data type is used in the following API operations: RetrieveAndGenerate request \226\128\147 in the input field"]
module RetrieveAndGenerateOutput =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc
          "The response generated from querying the knowledge base."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the response generated from querying the knowledge base. This data type is used in the following API operations: RetrieveAndGenerate response \226\128\147 in the output field"]
module RetrieveAndGenerateOutputEvent =
  struct
    type nonrec t = {
      text: String_.t option [@ocaml.doc "A text response."]}
    let make ?text = fun () -> { text }
    let to_value x =
      structure_to_value [("text", (Option.map x.text ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let text = field_map json__ "text" String_.of_json in make ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A retrieve and generate output event."]
module RetrieveAndGenerateSessionConfiguration =
  struct
    type nonrec t =
      {
      kmsKeyArn: KmsKeyArn.t
        [@ocaml.doc "The ARN of the KMS key encrypting the session."]}
    let context_ = "RetrieveAndGenerateSessionConfiguration"
    let make ~kmsKeyArn = fun () -> { kmsKeyArn }
    let to_value x =
      structure_to_value
        [("kmsKeyArn", (Some (KmsKeyArn.to_value x.kmsKeyArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        KmsKeyArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "kmsKeyArn") in
      make ~kmsKeyArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let kmsKeyArn = field_map_exn json__ "kmsKeyArn" KmsKeyArn.of_json in
      make ~kmsKeyArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configuration about the session with the knowledge base. This data type is used in the following API operations: RetrieveAndGenerate request \226\128\147 in the sessionConfiguration field"]
module RetrieveAndGenerateRequest =
  struct
    type nonrec t =
      {
      input: RetrieveAndGenerateInput.t
        [@ocaml.doc "Contains the query to be made to the knowledge base."];
      retrieveAndGenerateConfiguration:
        RetrieveAndGenerateConfiguration.t option
        [@ocaml.doc
          "Contains configurations for the knowledge base query and retrieval process. For more information, see Query configurations."];
      sessionConfiguration: RetrieveAndGenerateSessionConfiguration.t option
        [@ocaml.doc
          "Contains details about the session with the knowledge base."];
      sessionId: SessionId.t option
        [@ocaml.doc
          "The unique identifier of the session. When you first make a RetrieveAndGenerate request, Amazon Bedrock automatically generates this value. You must reuse this value for all subsequent requests in the same conversational session. This value allows Amazon Bedrock to maintain context and knowledge from previous interactions. You can't explicitly set the sessionId yourself."]}
    let context_ = "RetrieveAndGenerateRequest"
    let make ?retrieveAndGenerateConfiguration =
      fun ?sessionConfiguration ->
        fun ?sessionId ->
          fun ~input ->
            fun () ->
              {
                retrieveAndGenerateConfiguration;
                sessionConfiguration;
                sessionId;
                input
              }
    let to_value x =
      structure_to_value
        [("input", (Some (RetrieveAndGenerateInput.to_value x.input)));
        ("retrieveAndGenerateConfiguration",
          (Option.map x.retrieveAndGenerateConfiguration
             ~f:RetrieveAndGenerateConfiguration.to_value));
        ("sessionConfiguration",
          (Option.map x.sessionConfiguration
             ~f:RetrieveAndGenerateSessionConfiguration.to_value));
        ("sessionId", (Option.map x.sessionId ~f:SessionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:SessionId.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionConfiguration =
        (Option.map ~f:RetrieveAndGenerateSessionConfiguration.of_xml)
          (Xml.child xml_arg0 "sessionConfiguration") in
      let retrieveAndGenerateConfiguration =
        (Option.map ~f:RetrieveAndGenerateConfiguration.of_xml)
          (Xml.child xml_arg0 "retrieveAndGenerateConfiguration") in
      let input =
        RetrieveAndGenerateInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "input") in
      make ?sessionId ?sessionConfiguration ?retrieveAndGenerateConfiguration
        ~input ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let sessionConfiguration =
        field_map json__ "sessionConfiguration"
          RetrieveAndGenerateSessionConfiguration.of_json in
      let retrieveAndGenerateConfiguration =
        field_map json__ "retrieveAndGenerateConfiguration"
          RetrieveAndGenerateConfiguration.of_json in
      let input =
        field_map_exn json__ "input" RetrieveAndGenerateInput.of_json in
      make ?sessionId ?sessionConfiguration ?retrieveAndGenerateConfiguration
        ~input ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Queries a knowledge base and generates responses based on the retrieved results and using the specified foundation model or inference profile. The response only cites sources that are relevant to the query."]
module RetrieveAndGenerateResponse =
  struct
    type nonrec t =
      {
      citations: Citations.t option
        [@ocaml.doc
          "A list of segments of the generated response that are based on sources in the knowledge base, alongside information about the sources."];
      guardrailAction: GuadrailAction.t option
        [@ocaml.doc
          "Specifies if there is a guardrail intervention in the response."];
      output: RetrieveAndGenerateOutput.t option
        [@ocaml.doc
          "Contains the response generated from querying the knowledge base."];
      sessionId: SessionId.t option
        [@ocaml.doc
          "The unique identifier of the session. When you first make a RetrieveAndGenerate request, Amazon Bedrock automatically generates this value. You must reuse this value for all subsequent requests in the same conversational session. This value allows Amazon Bedrock to maintain context and knowledge from previous interactions. You can't explicitly set the sessionId yourself."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?citations =
      fun ?guardrailAction ->
        fun ?output ->
          fun ?sessionId ->
            fun () -> { citations; guardrailAction; output; sessionId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("citations", (Option.map x.citations ~f:Citations.to_value));
        ("guardrailAction",
          (Option.map x.guardrailAction ~f:GuadrailAction.to_value));
        ("output",
          (Option.map x.output ~f:RetrieveAndGenerateOutput.to_value));
        ("sessionId", (Option.map x.sessionId ~f:SessionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:SessionId.of_xml) (Xml.child xml_arg0 "sessionId") in
      let output =
        (Option.map ~f:RetrieveAndGenerateOutput.of_xml)
          (Xml.child xml_arg0 "output") in
      let guardrailAction =
        (Option.map ~f:GuadrailAction.of_xml)
          (Xml.child xml_arg0 "guardrailAction") in
      let citations =
        (Option.map ~f:Citations.of_xml) (Xml.child xml_arg0 "citations") in
      make ?sessionId ?output ?guardrailAction ?citations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let output =
        field_map json__ "output" RetrieveAndGenerateOutput.of_json in
      let guardrailAction =
        field_map json__ "guardrailAction" GuadrailAction.of_json in
      let citations = field_map json__ "citations" Citations.of_json in
      make ?sessionId ?output ?guardrailAction ?citations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Queries a knowledge base and generates responses based on the retrieved results and using the specified foundation model or inference profile. The response only cites sources that are relevant to the query."]
module RetrieveAndGenerateStreamRequest =
  struct
    type nonrec t =
      {
      input: RetrieveAndGenerateInput.t
        [@ocaml.doc "Contains the query to be made to the knowledge base."];
      retrieveAndGenerateConfiguration:
        RetrieveAndGenerateConfiguration.t option
        [@ocaml.doc
          "Contains configurations for the knowledge base query and retrieval process. For more information, see Query configurations."];
      sessionConfiguration: RetrieveAndGenerateSessionConfiguration.t option
        [@ocaml.doc
          "Contains details about the session with the knowledge base."];
      sessionId: SessionId.t option
        [@ocaml.doc
          "The unique identifier of the session. When you first make a RetrieveAndGenerate request, Amazon Bedrock automatically generates this value. You must reuse this value for all subsequent requests in the same conversational session. This value allows Amazon Bedrock to maintain context and knowledge from previous interactions. You can't explicitly set the sessionId yourself."]}
    let context_ = "RetrieveAndGenerateStreamRequest"
    let make ?retrieveAndGenerateConfiguration =
      fun ?sessionConfiguration ->
        fun ?sessionId ->
          fun ~input ->
            fun () ->
              {
                retrieveAndGenerateConfiguration;
                sessionConfiguration;
                sessionId;
                input
              }
    let to_value x =
      structure_to_value
        [("input", (Some (RetrieveAndGenerateInput.to_value x.input)));
        ("retrieveAndGenerateConfiguration",
          (Option.map x.retrieveAndGenerateConfiguration
             ~f:RetrieveAndGenerateConfiguration.to_value));
        ("sessionConfiguration",
          (Option.map x.sessionConfiguration
             ~f:RetrieveAndGenerateSessionConfiguration.to_value));
        ("sessionId", (Option.map x.sessionId ~f:SessionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:SessionId.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionConfiguration =
        (Option.map ~f:RetrieveAndGenerateSessionConfiguration.of_xml)
          (Xml.child xml_arg0 "sessionConfiguration") in
      let retrieveAndGenerateConfiguration =
        (Option.map ~f:RetrieveAndGenerateConfiguration.of_xml)
          (Xml.child xml_arg0 "retrieveAndGenerateConfiguration") in
      let input =
        RetrieveAndGenerateInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "input") in
      make ?sessionId ?sessionConfiguration ?retrieveAndGenerateConfiguration
        ~input ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      let sessionConfiguration =
        field_map json__ "sessionConfiguration"
          RetrieveAndGenerateSessionConfiguration.of_json in
      let retrieveAndGenerateConfiguration =
        field_map json__ "retrieveAndGenerateConfiguration"
          RetrieveAndGenerateConfiguration.of_json in
      let input =
        field_map_exn json__ "input" RetrieveAndGenerateInput.of_json in
      make ?sessionId ?sessionConfiguration ?retrieveAndGenerateConfiguration
        ~input ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Queries a knowledge base and generates responses based on the retrieved results, with output in streaming format. The CLI doesn't support streaming operations in Amazon Bedrock, including InvokeModelWithResponseStream. This operation requires permission for the bedrock:RetrieveAndGenerate action."]
module RetrieveAndGenerateStreamResponseOutput =
  struct
    type nonrec t =
      {
      accessDeniedException: AccessDeniedException.t option
        [@ocaml.doc
          "The request is denied because you do not have sufficient permissions to perform the requested action. For troubleshooting this error, see AccessDeniedException in the Amazon Bedrock User Guide."];
      badGatewayException: BadGatewayException.t option
        [@ocaml.doc "The request failed due to a bad gateway error."];
      citation: CitationEvent.t option [@ocaml.doc "A citation event."];
      conflictException: ConflictException.t option
        [@ocaml.doc
          "Error occurred because of a conflict while performing an operation."];
      dependencyFailedException: DependencyFailedException.t option
        [@ocaml.doc "The request failed due to a dependency error."];
      guardrail: GuardrailEvent.t option [@ocaml.doc "A guardrail event."];
      internalServerException: InternalServerException.t option
        [@ocaml.doc "An internal server error occurred. Retry your request."];
      output: RetrieveAndGenerateOutputEvent.t option
        [@ocaml.doc "An output event."];
      resourceNotFoundException: ResourceNotFoundException.t option
        [@ocaml.doc
          "The specified resource ARN was not found. For troubleshooting this error, see ResourceNotFound in the Amazon Bedrock User Guide."];
      serviceQuotaExceededException: ServiceQuotaExceededException.t option
        [@ocaml.doc
          "Your request exceeds the service quota for your account. You can view your quotas at Viewing service quotas. You can resubmit your request later."];
      throttlingException: ThrottlingException.t option
        [@ocaml.doc
          "Your request was denied due to exceeding the account quotas for Amazon Bedrock. For troubleshooting this error, see ThrottlingException in the Amazon Bedrock User Guide."];
      validationException: ValidationException.t option
        [@ocaml.doc
          "The input fails to satisfy the constraints specified by Amazon Bedrock. For troubleshooting this error, see ValidationError in the Amazon Bedrock User Guide."]}
    let make ?accessDeniedException =
      fun ?badGatewayException ->
        fun ?citation ->
          fun ?conflictException ->
            fun ?dependencyFailedException ->
              fun ?guardrail ->
                fun ?internalServerException ->
                  fun ?output ->
                    fun ?resourceNotFoundException ->
                      fun ?serviceQuotaExceededException ->
                        fun ?throttlingException ->
                          fun ?validationException ->
                            fun () ->
                              {
                                accessDeniedException;
                                badGatewayException;
                                citation;
                                conflictException;
                                dependencyFailedException;
                                guardrail;
                                internalServerException;
                                output;
                                resourceNotFoundException;
                                serviceQuotaExceededException;
                                throttlingException;
                                validationException
                              }
    let to_value x =
      structure_to_value
        [("accessDeniedException",
           (Option.map x.accessDeniedException
              ~f:AccessDeniedException.to_value));
        ("badGatewayException",
          (Option.map x.badGatewayException ~f:BadGatewayException.to_value));
        ("citation", (Option.map x.citation ~f:CitationEvent.to_value));
        ("conflictException",
          (Option.map x.conflictException ~f:ConflictException.to_value));
        ("dependencyFailedException",
          (Option.map x.dependencyFailedException
             ~f:DependencyFailedException.to_value));
        ("guardrail", (Option.map x.guardrail ~f:GuardrailEvent.to_value));
        ("internalServerException",
          (Option.map x.internalServerException
             ~f:InternalServerException.to_value));
        ("output",
          (Option.map x.output ~f:RetrieveAndGenerateOutputEvent.to_value));
        ("resourceNotFoundException",
          (Option.map x.resourceNotFoundException
             ~f:ResourceNotFoundException.to_value));
        ("serviceQuotaExceededException",
          (Option.map x.serviceQuotaExceededException
             ~f:ServiceQuotaExceededException.to_value));
        ("throttlingException",
          (Option.map x.throttlingException ~f:ThrottlingException.to_value));
        ("validationException",
          (Option.map x.validationException ~f:ValidationException.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let validationException =
        (Option.map ~f:ValidationException.of_xml)
          (Xml.child xml_arg0 "validationException") in
      let throttlingException =
        (Option.map ~f:ThrottlingException.of_xml)
          (Xml.child xml_arg0 "throttlingException") in
      let serviceQuotaExceededException =
        (Option.map ~f:ServiceQuotaExceededException.of_xml)
          (Xml.child xml_arg0 "serviceQuotaExceededException") in
      let resourceNotFoundException =
        (Option.map ~f:ResourceNotFoundException.of_xml)
          (Xml.child xml_arg0 "resourceNotFoundException") in
      let output =
        (Option.map ~f:RetrieveAndGenerateOutputEvent.of_xml)
          (Xml.child xml_arg0 "output") in
      let internalServerException =
        (Option.map ~f:InternalServerException.of_xml)
          (Xml.child xml_arg0 "internalServerException") in
      let guardrail =
        (Option.map ~f:GuardrailEvent.of_xml)
          (Xml.child xml_arg0 "guardrail") in
      let dependencyFailedException =
        (Option.map ~f:DependencyFailedException.of_xml)
          (Xml.child xml_arg0 "dependencyFailedException") in
      let conflictException =
        (Option.map ~f:ConflictException.of_xml)
          (Xml.child xml_arg0 "conflictException") in
      let citation =
        (Option.map ~f:CitationEvent.of_xml) (Xml.child xml_arg0 "citation") in
      let badGatewayException =
        (Option.map ~f:BadGatewayException.of_xml)
          (Xml.child xml_arg0 "badGatewayException") in
      let accessDeniedException =
        (Option.map ~f:AccessDeniedException.of_xml)
          (Xml.child xml_arg0 "accessDeniedException") in
      make ?validationException ?throttlingException
        ?serviceQuotaExceededException ?resourceNotFoundException ?output
        ?internalServerException ?guardrail ?dependencyFailedException
        ?conflictException ?citation ?badGatewayException
        ?accessDeniedException ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let validationException =
        field_map json__ "validationException" ValidationException.of_json in
      let throttlingException =
        field_map json__ "throttlingException" ThrottlingException.of_json in
      let serviceQuotaExceededException =
        field_map json__ "serviceQuotaExceededException"
          ServiceQuotaExceededException.of_json in
      let resourceNotFoundException =
        field_map json__ "resourceNotFoundException"
          ResourceNotFoundException.of_json in
      let output =
        field_map json__ "output" RetrieveAndGenerateOutputEvent.of_json in
      let internalServerException =
        field_map json__ "internalServerException"
          InternalServerException.of_json in
      let guardrail = field_map json__ "guardrail" GuardrailEvent.of_json in
      let dependencyFailedException =
        field_map json__ "dependencyFailedException"
          DependencyFailedException.of_json in
      let conflictException =
        field_map json__ "conflictException" ConflictException.of_json in
      let citation = field_map json__ "citation" CitationEvent.of_json in
      let badGatewayException =
        field_map json__ "badGatewayException" BadGatewayException.of_json in
      let accessDeniedException =
        field_map json__ "accessDeniedException"
          AccessDeniedException.of_json in
      make ?validationException ?throttlingException
        ?serviceQuotaExceededException ?resourceNotFoundException ?output
        ?internalServerException ?guardrail ?dependencyFailedException
        ?conflictException ?citation ?badGatewayException
        ?accessDeniedException ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A retrieve and generate stream response output."]
module RetrieveAndGenerateStreamResponse =
  struct
    type nonrec t =
      {
      sessionId: SessionId.t option [@ocaml.doc "The session ID."];
      stream: RetrieveAndGenerateStreamResponseOutput.t option
        [@ocaml.doc "A stream of events from the model."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sessionId = fun ?stream -> fun () -> { sessionId; stream }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?sessionId:(Option.map
                          ((List.Assoc.find ~equal:String.Caseless.equal) xs
                             "x-amzn-bedrock-knowledge-base-session-id")
                          ~f:SessionId.of_string) ?stream:(Some pipe) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("x-amzn-bedrock-knowledge-base-session-id",
           (Option.map x.sessionId ~f:SessionId.to_value));
        ("stream",
          (Option.map x.stream
             ~f:RetrieveAndGenerateStreamResponseOutput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stream =
        (Option.map ~f:RetrieveAndGenerateStreamResponseOutput.of_xml)
          (Xml.child xml_arg0 "stream") in
      let sessionId =
        (Option.map ~f:SessionId.of_xml)
          (Xml.child xml_arg0 "x-amzn-bedrock-knowledge-base-session-id") in
      make ?stream ?sessionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stream =
        field_map json__ "stream"
          RetrieveAndGenerateStreamResponseOutput.of_json in
      let sessionId = field_map json__ "sessionId" SessionId.of_json in
      make ?stream ?sessionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Queries a knowledge base and generates responses based on the retrieved results, with output in streaming format. The CLI doesn't support streaming operations in Amazon Bedrock, including InvokeModelWithResponseStream. This operation requires permission for the bedrock:RetrieveAndGenerate action."]
module RetrieveRequest =
  struct
    type nonrec t =
      {
      guardrailConfiguration: GuardrailConfiguration.t option
        [@ocaml.doc "Guardrail settings."];
      knowledgeBaseId: KnowledgeBaseId.t
        [@ocaml.doc "The unique identifier of the knowledge base to query."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If there are more results than can fit in the response, the response returns a nextToken. Use this token in the nextToken field of another request to retrieve the next batch of results."];
      retrievalConfiguration: KnowledgeBaseRetrievalConfiguration.t option
        [@ocaml.doc
          "Contains configurations for the knowledge base query and retrieval process. For more information, see Query configurations."];
      retrievalQuery: KnowledgeBaseQuery.t
        [@ocaml.doc "Contains the query to send the knowledge base."]}
    let context_ = "RetrieveRequest"
    let make ?guardrailConfiguration =
      fun ?nextToken ->
        fun ?retrievalConfiguration ->
          fun ~knowledgeBaseId ->
            fun ~retrievalQuery ->
              fun () ->
                {
                  guardrailConfiguration;
                  nextToken;
                  retrievalConfiguration;
                  knowledgeBaseId;
                  retrievalQuery
                }
    let to_value x =
      structure_to_value
        [("guardrailConfiguration",
           (Option.map x.guardrailConfiguration
              ~f:GuardrailConfiguration.to_value));
        ("knowledgeBaseId",
          (Some (KnowledgeBaseId.to_value x.knowledgeBaseId)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("retrievalConfiguration",
          (Option.map x.retrievalConfiguration
             ~f:KnowledgeBaseRetrievalConfiguration.to_value));
        ("retrievalQuery",
          (Some (KnowledgeBaseQuery.to_value x.retrievalQuery)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retrievalQuery =
        KnowledgeBaseQuery.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "retrievalQuery") in
      let retrievalConfiguration =
        (Option.map ~f:KnowledgeBaseRetrievalConfiguration.of_xml)
          (Xml.child xml_arg0 "retrievalConfiguration") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let knowledgeBaseId =
        KnowledgeBaseId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "knowledgeBaseId") in
      let guardrailConfiguration =
        (Option.map ~f:GuardrailConfiguration.of_xml)
          (Xml.child xml_arg0 "guardrailConfiguration") in
      make ~retrievalQuery ?retrievalConfiguration ?nextToken
        ~knowledgeBaseId ?guardrailConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retrievalQuery =
        field_map_exn json__ "retrievalQuery" KnowledgeBaseQuery.of_json in
      let retrievalConfiguration =
        field_map json__ "retrievalConfiguration"
          KnowledgeBaseRetrievalConfiguration.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let knowledgeBaseId =
        field_map_exn json__ "knowledgeBaseId" KnowledgeBaseId.of_json in
      let guardrailConfiguration =
        field_map json__ "guardrailConfiguration"
          GuardrailConfiguration.of_json in
      make ~retrievalQuery ?retrievalConfiguration ?nextToken
        ~knowledgeBaseId ?guardrailConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Queries a knowledge base and retrieves information from it."]
module RetrieveResponse =
  struct
    type nonrec t =
      {
      guardrailAction: GuadrailAction.t option
        [@ocaml.doc
          "Specifies if there is a guardrail intervention in the response."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If there are more results than can fit in the response, the response returns a nextToken. Use this token in the nextToken field of another request to retrieve the next batch of results."];
      retrievalResults: KnowledgeBaseRetrievalResults.t option
        [@ocaml.doc "A list of results from querying the knowledge base."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?guardrailAction =
      fun ?nextToken ->
        fun ?retrievalResults ->
          fun () -> { guardrailAction; nextToken; retrievalResults }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("guardrailAction",
           (Option.map x.guardrailAction ~f:GuadrailAction.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("retrievalResults",
          (Option.map x.retrievalResults
             ~f:KnowledgeBaseRetrievalResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retrievalResults =
        (Option.map ~f:KnowledgeBaseRetrievalResults.of_xml)
          (Xml.child xml_arg0 "retrievalResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let guardrailAction =
        (Option.map ~f:GuadrailAction.of_xml)
          (Xml.child xml_arg0 "guardrailAction") in
      make ?retrievalResults ?nextToken ?guardrailAction ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retrievalResults =
        field_map json__ "retrievalResults"
          KnowledgeBaseRetrievalResults.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let guardrailAction =
        field_map json__ "guardrailAction" GuadrailAction.of_json in
      make ?retrievalResults ?nextToken ?guardrailAction ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Queries a knowledge base and retrieves information from it."]
module StartFlowExecutionRequest =
  struct
    type nonrec t =
      {
      flowAliasIdentifier: FlowAliasIdentifier.t
        [@ocaml.doc
          "The unique identifier of the flow alias to use for the flow execution."];
      flowExecutionName: FlowExecutionName.t option
        [@ocaml.doc
          "The unique name for the flow execution. If you don't provide one, a system-generated name is used."];
      flowIdentifier: FlowIdentifier.t
        [@ocaml.doc "The unique identifier of the flow to execute."];
      inputs: FlowInputs.t
        [@ocaml.doc
          "The input data required for the flow execution. This must match the input schema defined in the flow."];
      modelPerformanceConfiguration: ModelPerformanceConfiguration.t option
        [@ocaml.doc
          "The performance settings for the foundation model used in the flow execution."]}
    let context_ = "StartFlowExecutionRequest"
    let make ?flowExecutionName =
      fun ?modelPerformanceConfiguration ->
        fun ~flowAliasIdentifier ->
          fun ~flowIdentifier ->
            fun ~inputs ->
              fun () ->
                {
                  flowExecutionName;
                  modelPerformanceConfiguration;
                  flowAliasIdentifier;
                  flowIdentifier;
                  inputs
                }
    let to_value x =
      structure_to_value
        [("flowAliasIdentifier",
           (Some (FlowAliasIdentifier.to_value x.flowAliasIdentifier)));
        ("flowExecutionName",
          (Option.map x.flowExecutionName ~f:FlowExecutionName.to_value));
        ("flowIdentifier", (Some (FlowIdentifier.to_value x.flowIdentifier)));
        ("inputs", (Some (FlowInputs.to_value x.inputs)));
        ("modelPerformanceConfiguration",
          (Option.map x.modelPerformanceConfiguration
             ~f:ModelPerformanceConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let modelPerformanceConfiguration =
        (Option.map ~f:ModelPerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "modelPerformanceConfiguration") in
      let inputs =
        FlowInputs.of_xml (Xml.child_exn ~context:context_ xml_arg0 "inputs") in
      let flowIdentifier =
        FlowIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowIdentifier") in
      let flowExecutionName =
        (Option.map ~f:FlowExecutionName.of_xml)
          (Xml.child xml_arg0 "flowExecutionName") in
      let flowAliasIdentifier =
        FlowAliasIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowAliasIdentifier") in
      make ?modelPerformanceConfiguration ~inputs ~flowIdentifier
        ?flowExecutionName ~flowAliasIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let modelPerformanceConfiguration =
        field_map json__ "modelPerformanceConfiguration"
          ModelPerformanceConfiguration.of_json in
      let inputs = field_map_exn json__ "inputs" FlowInputs.of_json in
      let flowIdentifier =
        field_map_exn json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowExecutionName =
        field_map json__ "flowExecutionName" FlowExecutionName.of_json in
      let flowAliasIdentifier =
        field_map_exn json__ "flowAliasIdentifier"
          FlowAliasIdentifier.of_json in
      make ?modelPerformanceConfiguration ~inputs ~flowIdentifier
        ?flowExecutionName ~flowAliasIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an execution of an Amazon Bedrock flow. Unlike flows that run until completion or time out after five minutes, flow executions let you run flows asynchronously for longer durations. Flow executions also yield control so that your application can perform other tasks. This operation returns an Amazon Resource Name (ARN) that you can use to track and manage your flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module StartFlowExecutionResponse =
  struct
    type nonrec t =
      {
      executionArn: FlowExecutionIdentifier.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies the flow execution."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?executionArn = fun () -> { executionArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("executionArn",
           (Option.map x.executionArn ~f:FlowExecutionIdentifier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let executionArn =
        (Option.map ~f:FlowExecutionIdentifier.of_xml)
          (Xml.child xml_arg0 "executionArn") in
      make ?executionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let executionArn =
        field_map json__ "executionArn" FlowExecutionIdentifier.of_json in
      make ?executionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an execution of an Amazon Bedrock flow. Unlike flows that run until completion or time out after five minutes, flow executions let you run flows asynchronously for longer durations. Flow executions also yield control so that your application can perform other tasks. This operation returns an Amazon Resource Name (ARN) that you can use to track and manage your flow execution. Flow executions is in preview release for Amazon Bedrock and is subject to change."]
module StopFlowExecutionRequest =
  struct
    type nonrec t =
      {
      executionIdentifier: FlowExecutionIdentifier.t
        [@ocaml.doc "The unique identifier of the flow execution to stop."];
      flowAliasIdentifier: FlowAliasIdentifier.t
        [@ocaml.doc
          "The unique identifier of the flow alias used for the execution."];
      flowIdentifier: FlowIdentifier.t
        [@ocaml.doc "The unique identifier of the flow."]}
    let context_ = "StopFlowExecutionRequest"
    let make ~executionIdentifier =
      fun ~flowAliasIdentifier ->
        fun ~flowIdentifier ->
          fun () ->
            { executionIdentifier; flowAliasIdentifier; flowIdentifier }
    let to_value x =
      structure_to_value
        [("executionIdentifier",
           (Some (FlowExecutionIdentifier.to_value x.executionIdentifier)));
        ("flowAliasIdentifier",
          (Some (FlowAliasIdentifier.to_value x.flowAliasIdentifier)));
        ("flowIdentifier", (Some (FlowIdentifier.to_value x.flowIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let flowIdentifier =
        FlowIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowIdentifier") in
      let flowAliasIdentifier =
        FlowAliasIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "flowAliasIdentifier") in
      let executionIdentifier =
        FlowExecutionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "executionIdentifier") in
      make ~flowIdentifier ~flowAliasIdentifier ~executionIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let flowIdentifier =
        field_map_exn json__ "flowIdentifier" FlowIdentifier.of_json in
      let flowAliasIdentifier =
        field_map_exn json__ "flowAliasIdentifier"
          FlowAliasIdentifier.of_json in
      let executionIdentifier =
        field_map_exn json__ "executionIdentifier"
          FlowExecutionIdentifier.of_json in
      make ~flowIdentifier ~flowAliasIdentifier ~executionIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops an Amazon Bedrock flow's execution. This operation prevents further processing of the flow and changes the execution status to Aborted."]
module StopFlowExecutionResponse =
  struct
    type nonrec t =
      {
      executionArn: FlowExecutionIdentifier.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that uniquely identifies the flow execution that was stopped."];
      status: FlowExecutionStatus.t option
        [@ocaml.doc
          "The updated status of the flow execution after the stop request. This will typically be ABORTED if the execution was successfully stopped."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadGatewayException of BadGatewayException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?executionArn =
      fun ?status -> fun () -> { executionArn; status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("executionArn",
           (Option.map x.executionArn ~f:FlowExecutionIdentifier.to_value));
        ("status", (Option.map x.status ~f:FlowExecutionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:FlowExecutionStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let executionArn =
        (Option.map ~f:FlowExecutionIdentifier.of_xml)
          (Xml.child xml_arg0 "executionArn") in
      make ?status ?executionArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" FlowExecutionStatus.of_json in
      let executionArn =
        field_map json__ "executionArn" FlowExecutionIdentifier.of_json in
      make ?status ?executionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Stops an Amazon Bedrock flow's execution. This operation prevents further processing of the flow and changes the execution status to Aborted."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: TaggableResourcesArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource to tag."];
      tags: TagsMap.t
        [@ocaml.doc
          "An object containing key-value pairs that define the tags to attach to the resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn",
           (Some (TaggableResourcesArn.to_value x.resourceArn)));
        ("tags", (Some (TagsMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagsMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        TaggableResourcesArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagsMap.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" TaggableResourcesArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associate tags with a resource. For more information, see Tagging resources in the Amazon Bedrock User Guide."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associate tags with a resource. For more information, see Tagging resources in the Amazon Bedrock User Guide."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: TaggableResourcesArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource from which to remove tags."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "A list of keys of the tags to remove from the resource."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn",
           (Some (TaggableResourcesArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        TaggableResourcesArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "tagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" TaggableResourcesArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Remove tags from a resource."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Remove tags from a resource."]
module UpdateSessionRequest =
  struct
    type nonrec t =
      {
      sessionIdentifier: SessionIdentifier.t
        [@ocaml.doc
          "The unique identifier of the session to modify. You can specify either the session's sessionId or its Amazon Resource Name (ARN)."];
      sessionMetadata: SessionMetadataMap.t option
        [@ocaml.doc
          "A map of key-value pairs containing attributes to be persisted across the session. For example the user's ID, their language preference, and the type of device they are using."]}
    let context_ = "UpdateSessionRequest"
    let make ?sessionMetadata =
      fun ~sessionIdentifier ->
        fun () -> { sessionMetadata; sessionIdentifier }
    let to_value x =
      structure_to_value
        [("sessionIdentifier",
           (Some (SessionIdentifier.to_value x.sessionIdentifier)));
        ("sessionMetadata",
          (Option.map x.sessionMetadata ~f:SessionMetadataMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionMetadata =
        (Option.map ~f:SessionMetadataMap.of_xml)
          (Xml.child xml_arg0 "sessionMetadata") in
      let sessionIdentifier =
        SessionIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionIdentifier") in
      make ?sessionMetadata ~sessionIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionMetadata =
        field_map json__ "sessionMetadata" SessionMetadataMap.of_json in
      let sessionIdentifier =
        field_map_exn json__ "sessionIdentifier" SessionIdentifier.of_json in
      make ?sessionMetadata ~sessionIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the metadata or encryption settings of a session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
module UpdateSessionResponse =
  struct
    type nonrec t =
      {
      createdAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the session was created."];
      lastUpdatedAt: DateTimestamp.t option
        [@ocaml.doc "The timestamp for when the session was last modified."];
      sessionArn: SessionArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the session that was updated."];
      sessionId: Uuid.t option
        [@ocaml.doc "The unique identifier of the session you updated."];
      sessionStatus: SessionStatus.t option
        [@ocaml.doc "The status of the session you updated."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?createdAt =
      fun ?lastUpdatedAt ->
        fun ?sessionArn ->
          fun ?sessionId ->
            fun ?sessionStatus ->
              fun () ->
                {
                  createdAt;
                  lastUpdatedAt;
                  sessionArn;
                  sessionId;
                  sessionStatus
                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("createdAt", (Option.map x.createdAt ~f:DateTimestamp.to_value));
        ("lastUpdatedAt",
          (Option.map x.lastUpdatedAt ~f:DateTimestamp.to_value));
        ("sessionArn", (Option.map x.sessionArn ~f:SessionArn.to_value));
        ("sessionId", (Option.map x.sessionId ~f:Uuid.to_value));
        ("sessionStatus",
          (Option.map x.sessionStatus ~f:SessionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionStatus =
        (Option.map ~f:SessionStatus.of_xml)
          (Xml.child xml_arg0 "sessionStatus") in
      let sessionId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionArn =
        (Option.map ~f:SessionArn.of_xml) (Xml.child xml_arg0 "sessionArn") in
      let lastUpdatedAt =
        (Option.map ~f:DateTimestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdatedAt") in
      let createdAt =
        (Option.map ~f:DateTimestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      make ?sessionStatus ?sessionId ?sessionArn ?lastUpdatedAt ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionStatus =
        field_map json__ "sessionStatus" SessionStatus.of_json in
      let sessionId = field_map json__ "sessionId" Uuid.of_json in
      let sessionArn = field_map json__ "sessionArn" SessionArn.of_json in
      let lastUpdatedAt =
        field_map json__ "lastUpdatedAt" DateTimestamp.of_json in
      let createdAt = field_map json__ "createdAt" DateTimestamp.of_json in
      make ?sessionStatus ?sessionId ?sessionArn ?lastUpdatedAt ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the metadata or encryption settings of a session. For more information about sessions, see Store and retrieve conversation history and context with Amazon Bedrock sessions."]
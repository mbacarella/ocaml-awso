(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_resource_share_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and resourceShareInvitationArn =
         flag "resource-share-invitation-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_resource_share_invitation
           (Values.AcceptResourceShareInvitationRequest.make ?clientToken
              ~resourceShareInvitationArn ())
           (Some Values.AcceptResourceShareInvitationResponse.to_json)
           (Some Values.AcceptResourceShareInvitationResponse.error_to_json)])
let associate_resource_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArns =
         flag "resource-arns" (optional json_arg) ~doc:"JSON ResourceArnList"
       and principals =
         flag "principals" (optional json_arg)
           ~doc:"JSON PrincipalArnOrIdList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_resource_share
           (Values.AssociateResourceShareRequest.make
              ?resourceArns:(Option.map ~f:Values.ResourceArnList.of_json
                               resourceArns)
              ?principals:(Option.map ~f:Values.PrincipalArnOrIdList.of_json
                             principals) ?clientToken ~resourceShareArn ())
           (Some Values.AssociateResourceShareResponse.to_json)
           (Some Values.AssociateResourceShareResponse.error_to_json)])
let associate_resource_share_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replace = flag "replace" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and permissionVersion =
         flag "permission-version" (optional int) ~doc:"INT Integer"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String"
       and permissionArn =
         flag "permission-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_resource_share_permission
           (Values.AssociateResourceSharePermissionRequest.make ?replace
              ?clientToken ?permissionVersion ~resourceShareArn
              ~permissionArn ())
           (Some Values.AssociateResourceSharePermissionResponse.to_json)
           (Some
              Values.AssociateResourceSharePermissionResponse.error_to_json)])
let create_resource_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArns =
         flag "resource-arns" (optional json_arg) ~doc:"JSON ResourceArnList"
       and principals =
         flag "principals" (optional json_arg)
           ~doc:"JSON PrincipalArnOrIdList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and allowExternalPrincipals =
         flag "allow-external-principals" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and permissionArns =
         flag "permission-arns" (optional json_arg)
           ~doc:"JSON PermissionArnList"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_share
           (Values.CreateResourceShareRequest.make
              ?resourceArns:(Option.map ~f:Values.ResourceArnList.of_json
                               resourceArns)
              ?principals:(Option.map ~f:Values.PrincipalArnOrIdList.of_json
                             principals)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?allowExternalPrincipals ?clientToken
              ?permissionArns:(Option.map ~f:Values.PermissionArnList.of_json
                                 permissionArns) ~name ())
           (Some Values.CreateResourceShareResponse.to_json)
           (Some Values.CreateResourceShareResponse.error_to_json)])
let delete_resource_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_share
           (Values.DeleteResourceShareRequest.make ?clientToken
              ~resourceShareArn ())
           (Some Values.DeleteResourceShareResponse.to_json)
           (Some Values.DeleteResourceShareResponse.error_to_json)])
let disassociate_resource_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArns =
         flag "resource-arns" (optional json_arg) ~doc:"JSON ResourceArnList"
       and principals =
         flag "principals" (optional json_arg)
           ~doc:"JSON PrincipalArnOrIdList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_resource_share
           (Values.DisassociateResourceShareRequest.make
              ?resourceArns:(Option.map ~f:Values.ResourceArnList.of_json
                               resourceArns)
              ?principals:(Option.map ~f:Values.PrincipalArnOrIdList.of_json
                             principals) ?clientToken ~resourceShareArn ())
           (Some Values.DisassociateResourceShareResponse.to_json)
           (Some Values.DisassociateResourceShareResponse.error_to_json)])
let disassociate_resource_share_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String"
       and permissionArn =
         flag "permission-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_resource_share_permission
           (Values.DisassociateResourceSharePermissionRequest.make
              ?clientToken ~resourceShareArn ~permissionArn ())
           (Some Values.DisassociateResourceSharePermissionResponse.to_json)
           (Some
              Values.DisassociateResourceSharePermissionResponse.error_to_json)])
let enable_sharing_with_aws_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_sharing_with_aws_organization
           (Values.EnableSharingWithAwsOrganizationRequest.make ())
           (Some Values.EnableSharingWithAwsOrganizationResponse.to_json)
           (Some
              Values.EnableSharingWithAwsOrganizationResponse.error_to_json)])
let get_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and permissionVersion =
         flag "permission-version" (optional int) ~doc:"INT Integer"
       and permissionArn =
         flag "permission-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_permission
           (Values.GetPermissionRequest.make ?permissionVersion
              ~permissionArn ()) (Some Values.GetPermissionResponse.to_json)
           (Some Values.GetPermissionResponse.error_to_json)])
let get_resource_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principal =
         flag "principal" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceArns =
         flag "resource-arns" (required json_arg) ~doc:"JSON ResourceArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policies
           (Values.GetResourcePoliciesRequest.make ?principal ?nextToken
              ?maxResults
              ~resourceArns:(Values.ResourceArnList.of_json resourceArns) ())
           (Some Values.GetResourcePoliciesResponse.to_json)
           (Some Values.GetResourcePoliciesResponse.error_to_json)])
let get_resource_share_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceShareArns =
         flag "resource-share-arns" (optional json_arg)
           ~doc:"JSON ResourceShareArnList"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING String"
       and principal =
         flag "principal" (optional string) ~doc:"STRING String"
       and associationStatus =
         flag "association-status" (optional json_arg)
           ~doc:"JSON ResourceShareAssociationStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and associationType =
         flag "association-type" (required json_arg)
           ~doc:"JSON ResourceShareAssociationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_share_associations
           (Values.GetResourceShareAssociationsRequest.make
              ?resourceShareArns:(Option.map
                                    ~f:Values.ResourceShareArnList.of_json
                                    resourceShareArns) ?resourceArn
              ?principal
              ?associationStatus:(Option.map
                                    ~f:Values.ResourceShareAssociationStatus.of_json
                                    associationStatus) ?nextToken ?maxResults
              ~associationType:(Values.ResourceShareAssociationType.of_json
                                  associationType) ())
           (Some Values.GetResourceShareAssociationsResponse.to_json)
           (Some Values.GetResourceShareAssociationsResponse.error_to_json)])
let get_resource_share_invitations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceShareInvitationArns =
         flag "resource-share-invitation-arns" (optional json_arg)
           ~doc:"JSON ResourceShareInvitationArnList"
       and resourceShareArns =
         flag "resource-share-arns" (optional json_arg)
           ~doc:"JSON ResourceShareArnList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_share_invitations
           (Values.GetResourceShareInvitationsRequest.make
              ?resourceShareInvitationArns:(Option.map
                                              ~f:Values.ResourceShareInvitationArnList.of_json
                                              resourceShareInvitationArns)
              ?resourceShareArns:(Option.map
                                    ~f:Values.ResourceShareArnList.of_json
                                    resourceShareArns) ?nextToken ?maxResults
              ()) (Some Values.GetResourceShareInvitationsResponse.to_json)
           (Some Values.GetResourceShareInvitationsResponse.error_to_json)])
let get_resource_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceShareArns =
         flag "resource-share-arns" (optional json_arg)
           ~doc:"JSON ResourceShareArnList"
       and resourceShareStatus =
         flag "resource-share-status" (optional json_arg)
           ~doc:"JSON ResourceShareStatus"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and tagFilters =
         flag "tag-filters" (optional json_arg) ~doc:"JSON TagFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and permissionArn =
         flag "permission-arn" (optional string) ~doc:"STRING String"
       and resourceOwner =
         flag "resource-owner" (required json_arg) ~doc:"JSON ResourceOwner" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_shares
           (Values.GetResourceSharesRequest.make
              ?resourceShareArns:(Option.map
                                    ~f:Values.ResourceShareArnList.of_json
                                    resourceShareArns)
              ?resourceShareStatus:(Option.map
                                      ~f:Values.ResourceShareStatus.of_json
                                      resourceShareStatus) ?name
              ?tagFilters:(Option.map ~f:Values.TagFilters.of_json tagFilters)
              ?nextToken ?maxResults ?permissionArn
              ~resourceOwner:(Values.ResourceOwner.of_json resourceOwner) ())
           (Some Values.GetResourceSharesResponse.to_json)
           (Some Values.GetResourceSharesResponse.error_to_json)])
let list_pending_invitation_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceRegionScope =
         flag "resource-region-scope" (optional json_arg)
           ~doc:"JSON ResourceRegionScopeFilter"
       and resourceShareInvitationArn =
         flag "resource-share-invitation-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pending_invitation_resources
           (Values.ListPendingInvitationResourcesRequest.make ?nextToken
              ?maxResults
              ?resourceRegionScope:(Option.map
                                      ~f:Values.ResourceRegionScopeFilter.of_json
                                      resourceRegionScope)
              ~resourceShareInvitationArn ())
           (Some Values.ListPendingInvitationResourcesResponse.to_json)
           (Some Values.ListPendingInvitationResourcesResponse.error_to_json)])
let list_permission_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and permissionArn =
         flag "permission-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permission_versions
           (Values.ListPermissionVersionsRequest.make ?nextToken ?maxResults
              ~permissionArn ())
           (Some Values.ListPermissionVersionsResponse.to_json)
           (Some Values.ListPermissionVersionsResponse.error_to_json)])
let list_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permissions
           (Values.ListPermissionsRequest.make ?resourceType ?nextToken
              ?maxResults ()) (Some Values.ListPermissionsResponse.to_json)
           (Some Values.ListPermissionsResponse.error_to_json)])
let list_principals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING String"
       and principals =
         flag "principals" (optional json_arg)
           ~doc:"JSON PrincipalArnOrIdList"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING String"
       and resourceShareArns =
         flag "resource-share-arns" (optional json_arg)
           ~doc:"JSON ResourceShareArnList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceOwner =
         flag "resource-owner" (required json_arg) ~doc:"JSON ResourceOwner" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_principals
           (Values.ListPrincipalsRequest.make ?resourceArn
              ?principals:(Option.map ~f:Values.PrincipalArnOrIdList.of_json
                             principals) ?resourceType
              ?resourceShareArns:(Option.map
                                    ~f:Values.ResourceShareArnList.of_json
                                    resourceShareArns) ?nextToken ?maxResults
              ~resourceOwner:(Values.ResourceOwner.of_json resourceOwner) ())
           (Some Values.ListPrincipalsResponse.to_json)
           (Some Values.ListPrincipalsResponse.error_to_json)])
let list_resource_share_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_share_permissions
           (Values.ListResourceSharePermissionsRequest.make ?nextToken
              ?maxResults ~resourceShareArn ())
           (Some Values.ListResourceSharePermissionsResponse.to_json)
           (Some Values.ListResourceSharePermissionsResponse.error_to_json)])
let list_resource_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceRegionScope =
         flag "resource-region-scope" (optional json_arg)
           ~doc:"JSON ResourceRegionScopeFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_types
           (Values.ListResourceTypesRequest.make ?nextToken ?maxResults
              ?resourceRegionScope:(Option.map
                                      ~f:Values.ResourceRegionScopeFilter.of_json
                                      resourceRegionScope) ())
           (Some Values.ListResourceTypesResponse.to_json)
           (Some Values.ListResourceTypesResponse.error_to_json)])
let list_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principal =
         flag "principal" (optional string) ~doc:"STRING String"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING String"
       and resourceArns =
         flag "resource-arns" (optional json_arg) ~doc:"JSON ResourceArnList"
       and resourceShareArns =
         flag "resource-share-arns" (optional json_arg)
           ~doc:"JSON ResourceShareArnList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceRegionScope =
         flag "resource-region-scope" (optional json_arg)
           ~doc:"JSON ResourceRegionScopeFilter"
       and resourceOwner =
         flag "resource-owner" (required json_arg) ~doc:"JSON ResourceOwner" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources
           (Values.ListResourcesRequest.make ?principal ?resourceType
              ?resourceArns:(Option.map ~f:Values.ResourceArnList.of_json
                               resourceArns)
              ?resourceShareArns:(Option.map
                                    ~f:Values.ResourceShareArnList.of_json
                                    resourceShareArns) ?nextToken ?maxResults
              ?resourceRegionScope:(Option.map
                                      ~f:Values.ResourceRegionScopeFilter.of_json
                                      resourceRegionScope)
              ~resourceOwner:(Values.ResourceOwner.of_json resourceOwner) ())
           (Some Values.ListResourcesResponse.to_json)
           (Some Values.ListResourcesResponse.error_to_json)])
let promote_resource_share_created_from_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.promote_resource_share_created_from_policy
           (Values.PromoteResourceShareCreatedFromPolicyRequest.make
              ~resourceShareArn ())
           (Some Values.PromoteResourceShareCreatedFromPolicyResponse.to_json)
           (Some
              Values.PromoteResourceShareCreatedFromPolicyResponse.error_to_json)])
let reject_resource_share_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and resourceShareInvitationArn =
         flag "resource-share-invitation-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_resource_share_invitation
           (Values.RejectResourceShareInvitationRequest.make ?clientToken
              ~resourceShareInvitationArn ())
           (Some Values.RejectResourceShareInvitationResponse.to_json)
           (Some Values.RejectResourceShareInvitationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceShareArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceShareArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_resource_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and allowExternalPrincipals =
         flag "allow-external-principals" (optional bool) ~doc:"BOOL Boolean"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and resourceShareArn =
         flag "resource-share-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_share
           (Values.UpdateResourceShareRequest.make ?name
              ?allowExternalPrincipals ?clientToken ~resourceShareArn ())
           (Some Values.UpdateResourceShareResponse.to_json)
           (Some Values.UpdateResourceShareResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-resource-share-invitation", accept_resource_share_invitation);
    ("associate-resource-share", associate_resource_share);
    ("associate-resource-share-permission",
      associate_resource_share_permission);
    ("create-resource-share", create_resource_share);
    ("delete-resource-share", delete_resource_share);
    ("disassociate-resource-share", disassociate_resource_share);
    ("disassociate-resource-share-permission",
      disassociate_resource_share_permission);
    ("enable-sharing-with-aws-organization",
      enable_sharing_with_aws_organization);
    ("get-permission", get_permission);
    ("get-resource-policies", get_resource_policies);
    ("get-resource-share-associations", get_resource_share_associations);
    ("get-resource-share-invitations", get_resource_share_invitations);
    ("get-resource-shares", get_resource_shares);
    ("list-pending-invitation-resources", list_pending_invitation_resources);
    ("list-permission-versions", list_permission_versions);
    ("list-permissions", list_permissions);
    ("list-principals", list_principals);
    ("list-resource-share-permissions", list_resource_share_permissions);
    ("list-resource-types", list_resource_types);
    ("list-resources", list_resources);
    ("promote-resource-share-created-from-policy",
      promote_resource_share_created_from_policy);
    ("reject-resource-share-invitation", reject_resource_share_invitation);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-resource-share", update_resource_share)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_addon_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and addonSubscriptionId =
         flag "addon-subscription-id" (required string)
           ~doc:"STRING AddonSubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_addon_instance
           (Values.CreateAddonInstanceRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~addonSubscriptionId ())
           (Some Values.CreateAddonInstanceResponse.to_json)
           (Some Values.CreateAddonInstanceResponse.error_to_json)])
let create_addon_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and addonName =
         flag "addon-name" (required string) ~doc:"STRING AddonName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_addon_subscription
           (Values.CreateAddonSubscriptionRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~addonName ())
           (Some Values.CreateAddonSubscriptionResponse.to_json)
           (Some Values.CreateAddonSubscriptionResponse.error_to_json)])
let create_address_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and addressListName =
         flag "address-list-name" (required string)
           ~doc:"STRING AddressListName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_address_list
           (Values.CreateAddressListRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~addressListName ())
           (Some Values.CreateAddressListResponse.to_json)
           (Some Values.CreateAddressListResponse.error_to_json)])
let create_address_list_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and addressListId =
         flag "address-list-id" (required string) ~doc:"STRING AddressListId"
       and name = flag "name" (required string) ~doc:"STRING JobName"
       and importDataFormat =
         flag "import-data-format" (required json_arg)
           ~doc:"JSON ImportDataFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_address_list_import_job
           (Values.CreateAddressListImportJobRequest.make ?clientToken
              ~addressListId ~name
              ~importDataFormat:(Values.ImportDataFormat.of_json
                                   importDataFormat) ())
           (Some Values.CreateAddressListImportJobResponse.to_json)
           (Some Values.CreateAddressListImportJobResponse.error_to_json)])
let create_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and retention =
         flag "retention" (optional json_arg) ~doc:"JSON ArchiveRetention"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and archiveName =
         flag "archive-name" (required string)
           ~doc:"STRING ArchiveNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_archive
           (Values.CreateArchiveRequest.make ?clientToken
              ?retention:(Option.map ~f:Values.ArchiveRetention.of_json
                            retention) ?kmsKeyArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~archiveName
              ()) (Some Values.CreateArchiveResponse.to_json)
           (Some Values.CreateArchiveResponse.error_to_json)])
let create_ingress_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and ingressPointConfiguration =
         flag "ingress-point-configuration" (optional json_arg)
           ~doc:"JSON IngressPointConfiguration"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and tlsPolicy =
         flag "tls-policy" (optional json_arg) ~doc:"JSON TlsPolicy"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and ingressPointName =
         flag "ingress-point-name" (required string)
           ~doc:"STRING IngressPointName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON IngressPointType"
       and ruleSetId =
         flag "rule-set-id" (required string) ~doc:"STRING RuleSetId"
       and trafficPolicyId =
         flag "traffic-policy-id" (required string)
           ~doc:"STRING TrafficPolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ingress_point
           (Values.CreateIngressPointRequest.make ?clientToken
              ?ingressPointConfiguration:(Option.map
                                            ~f:Values.IngressPointConfiguration.of_json
                                            ingressPointConfiguration)
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?tlsPolicy:(Option.map ~f:Values.TlsPolicy.of_json tlsPolicy)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~ingressPointName
              ~type_:(Values.IngressPointType.of_json type_) ~ruleSetId
              ~trafficPolicyId ())
           (Some Values.CreateIngressPointResponse.to_json)
           (Some Values.CreateIngressPointResponse.error_to_json)])
let create_relay =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and relayName =
         flag "relay-name" (required string) ~doc:"STRING RelayName"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING RelayServerName"
       and serverPort =
         flag "server-port" (required int) ~doc:"INT RelayServerPort"
       and authentication =
         flag "authentication" (required json_arg)
           ~doc:"JSON RelayAuthentication" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_relay
           (Values.CreateRelayRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~relayName
              ~serverName ~serverPort
              ~authentication:(Values.RelayAuthentication.of_json
                                 authentication) ())
           (Some Values.CreateRelayResponse.to_json)
           (Some Values.CreateRelayResponse.error_to_json)])
let create_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and ruleSetName =
         flag "rule-set-name" (required string) ~doc:"STRING RuleSetName"
       and rules = flag "rules" (required json_arg) ~doc:"JSON Rules" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rule_set
           (Values.CreateRuleSetRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~ruleSetName
              ~rules:(Values.Rules.of_json rules) ())
           (Some Values.CreateRuleSetResponse.to_json)
           (Some Values.CreateRuleSetResponse.error_to_json)])
let create_traffic_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and maxMessageSizeBytes =
         flag "max-message-size-bytes" (optional int)
           ~doc:"INT MaxMessageSizeBytes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and trafficPolicyName =
         flag "traffic-policy-name" (required string)
           ~doc:"STRING TrafficPolicyName"
       and policyStatements =
         flag "policy-statements" (required json_arg)
           ~doc:"JSON PolicyStatementList"
       and defaultAction =
         flag "default-action" (required json_arg) ~doc:"JSON AcceptAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_traffic_policy
           (Values.CreateTrafficPolicyRequest.make ?clientToken
              ?maxMessageSizeBytes
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~trafficPolicyName
              ~policyStatements:(Values.PolicyStatementList.of_json
                                   policyStatements)
              ~defaultAction:(Values.AcceptAction.of_json defaultAction) ())
           (Some Values.CreateTrafficPolicyResponse.to_json)
           (Some Values.CreateTrafficPolicyResponse.error_to_json)])
let delete_addon_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addonInstanceId =
         flag "addon-instance-id" (required string)
           ~doc:"STRING AddonInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_addon_instance
           (Values.DeleteAddonInstanceRequest.make ~addonInstanceId ())
           (Some Values.DeleteAddonInstanceResponse.to_json)
           (Some Values.DeleteAddonInstanceResponse.error_to_json)])
let delete_addon_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addonSubscriptionId =
         flag "addon-subscription-id" (required string)
           ~doc:"STRING AddonSubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_addon_subscription
           (Values.DeleteAddonSubscriptionRequest.make ~addonSubscriptionId
              ()) (Some Values.DeleteAddonSubscriptionResponse.to_json)
           (Some Values.DeleteAddonSubscriptionResponse.error_to_json)])
let delete_address_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addressListId =
         flag "address-list-id" (required string) ~doc:"STRING AddressListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_address_list
           (Values.DeleteAddressListRequest.make ~addressListId ())
           (Some Values.DeleteAddressListResponse.to_json)
           (Some Values.DeleteAddressListResponse.error_to_json)])
let delete_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveId =
         flag "archive-id" (required string) ~doc:"STRING ArchiveIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_archive (Values.DeleteArchiveRequest.make ~archiveId ())
           (Some Values.DeleteArchiveResponse.to_json)
           (Some Values.DeleteArchiveResponse.error_to_json)])
let delete_ingress_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ingressPointId =
         flag "ingress-point-id" (required string)
           ~doc:"STRING IngressPointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ingress_point
           (Values.DeleteIngressPointRequest.make ~ingressPointId ())
           (Some Values.DeleteIngressPointResponse.to_json)
           (Some Values.DeleteIngressPointResponse.error_to_json)])
let delete_relay =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relayId = flag "relay-id" (required string) ~doc:"STRING RelayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_relay (Values.DeleteRelayRequest.make ~relayId ())
           (Some Values.DeleteRelayResponse.to_json)
           (Some Values.DeleteRelayResponse.error_to_json)])
let delete_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetId =
         flag "rule-set-id" (required string) ~doc:"STRING RuleSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule_set
           (Values.DeleteRuleSetRequest.make ~ruleSetId ())
           (Some Values.DeleteRuleSetResponse.to_json)
           (Some Values.DeleteRuleSetResponse.error_to_json)])
let delete_traffic_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trafficPolicyId =
         flag "traffic-policy-id" (required string)
           ~doc:"STRING TrafficPolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_policy
           (Values.DeleteTrafficPolicyRequest.make ~trafficPolicyId ())
           (Some Values.DeleteTrafficPolicyResponse.to_json)
           (Some Values.DeleteTrafficPolicyResponse.error_to_json)])
let deregister_member_from_address_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addressListId =
         flag "address-list-id" (required string) ~doc:"STRING AddressListId"
       and address = flag "address" (required string) ~doc:"STRING Address" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_member_from_address_list
           (Values.DeregisterMemberFromAddressListRequest.make ~addressListId
              ~address ())
           (Some Values.DeregisterMemberFromAddressListResponse.to_json)
           (Some Values.DeregisterMemberFromAddressListResponse.error_to_json)])
let get_addon_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addonInstanceId =
         flag "addon-instance-id" (required string)
           ~doc:"STRING AddonInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_addon_instance
           (Values.GetAddonInstanceRequest.make ~addonInstanceId ())
           (Some Values.GetAddonInstanceResponse.to_json)
           (Some Values.GetAddonInstanceResponse.error_to_json)])
let get_addon_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addonSubscriptionId =
         flag "addon-subscription-id" (required string)
           ~doc:"STRING AddonSubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_addon_subscription
           (Values.GetAddonSubscriptionRequest.make ~addonSubscriptionId ())
           (Some Values.GetAddonSubscriptionResponse.to_json)
           (Some Values.GetAddonSubscriptionResponse.error_to_json)])
let get_address_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addressListId =
         flag "address-list-id" (required string) ~doc:"STRING AddressListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_address_list
           (Values.GetAddressListRequest.make ~addressListId ())
           (Some Values.GetAddressListResponse.to_json)
           (Some Values.GetAddressListResponse.error_to_json)])
let get_address_list_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_address_list_import_job
           (Values.GetAddressListImportJobRequest.make ~jobId ())
           (Some Values.GetAddressListImportJobResponse.to_json)
           (Some Values.GetAddressListImportJobResponse.error_to_json)])
let get_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveId =
         flag "archive-id" (required string) ~doc:"STRING ArchiveIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_archive (Values.GetArchiveRequest.make ~archiveId ())
           (Some Values.GetArchiveResponse.to_json)
           (Some Values.GetArchiveResponse.error_to_json)])
let get_archive_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportId =
         flag "export-id" (required string) ~doc:"STRING ExportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_archive_export
           (Values.GetArchiveExportRequest.make ~exportId ())
           (Some Values.GetArchiveExportResponse.to_json)
           (Some Values.GetArchiveExportResponse.error_to_json)])
let get_archive_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archivedMessageId =
         flag "archived-message-id" (required string)
           ~doc:"STRING ArchivedMessageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_archive_message
           (Values.GetArchiveMessageRequest.make ~archivedMessageId ())
           (Some Values.GetArchiveMessageResponse.to_json)
           (Some Values.GetArchiveMessageResponse.error_to_json)])
let get_archive_message_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archivedMessageId =
         flag "archived-message-id" (required string)
           ~doc:"STRING ArchivedMessageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_archive_message_content
           (Values.GetArchiveMessageContentRequest.make ~archivedMessageId ())
           (Some Values.GetArchiveMessageContentResponse.to_json)
           (Some Values.GetArchiveMessageContentResponse.error_to_json)])
let get_archive_search =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchId =
         flag "search-id" (required string) ~doc:"STRING SearchId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_archive_search
           (Values.GetArchiveSearchRequest.make ~searchId ())
           (Some Values.GetArchiveSearchResponse.to_json)
           (Some Values.GetArchiveSearchResponse.error_to_json)])
let get_archive_search_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchId =
         flag "search-id" (required string) ~doc:"STRING SearchId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_archive_search_results
           (Values.GetArchiveSearchResultsRequest.make ~searchId ())
           (Some Values.GetArchiveSearchResultsResponse.to_json)
           (Some Values.GetArchiveSearchResultsResponse.error_to_json)])
let get_ingress_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeTrustStoreContents =
         flag "include-trust-store-contents" (optional json_arg)
           ~doc:"JSON TrustStoreResponseOption"
       and ingressPointId =
         flag "ingress-point-id" (required string)
           ~doc:"STRING IngressPointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ingress_point
           (Values.GetIngressPointRequest.make
              ?includeTrustStoreContents:(Option.map
                                            ~f:Values.TrustStoreResponseOption.of_json
                                            includeTrustStoreContents)
              ~ingressPointId ())
           (Some Values.GetIngressPointResponse.to_json)
           (Some Values.GetIngressPointResponse.error_to_json)])
let get_member_of_address_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addressListId =
         flag "address-list-id" (required string) ~doc:"STRING AddressListId"
       and address = flag "address" (required string) ~doc:"STRING Address" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_member_of_address_list
           (Values.GetMemberOfAddressListRequest.make ~addressListId ~address
              ()) (Some Values.GetMemberOfAddressListResponse.to_json)
           (Some Values.GetMemberOfAddressListResponse.error_to_json)])
let get_relay =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relayId = flag "relay-id" (required string) ~doc:"STRING RelayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relay (Values.GetRelayRequest.make ~relayId ())
           (Some Values.GetRelayResponse.to_json)
           (Some Values.GetRelayResponse.error_to_json)])
let get_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetId =
         flag "rule-set-id" (required string) ~doc:"STRING RuleSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rule_set (Values.GetRuleSetRequest.make ~ruleSetId ())
           (Some Values.GetRuleSetResponse.to_json)
           (Some Values.GetRuleSetResponse.error_to_json)])
let get_traffic_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trafficPolicyId =
         flag "traffic-policy-id" (required string)
           ~doc:"STRING TrafficPolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_traffic_policy
           (Values.GetTrafficPolicyRequest.make ~trafficPolicyId ())
           (Some Values.GetTrafficPolicyResponse.to_json)
           (Some Values.GetTrafficPolicyResponse.error_to_json)])
let list_addon_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_addon_instances
           (Values.ListAddonInstancesRequest.make ?nextToken ?pageSize ())
           (Some Values.ListAddonInstancesResponse.to_json)
           (Some Values.ListAddonInstancesResponse.error_to_json)])
let list_addon_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_addon_subscriptions
           (Values.ListAddonSubscriptionsRequest.make ?nextToken ?pageSize ())
           (Some Values.ListAddonSubscriptionsResponse.to_json)
           (Some Values.ListAddonSubscriptionsResponse.error_to_json)])
let list_address_list_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and addressListId =
         flag "address-list-id" (required string) ~doc:"STRING AddressListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_address_list_import_jobs
           (Values.ListAddressListImportJobsRequest.make ?nextToken ?pageSize
              ~addressListId ())
           (Some Values.ListAddressListImportJobsResponse.to_json)
           (Some Values.ListAddressListImportJobsResponse.error_to_json)])
let list_address_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_address_lists
           (Values.ListAddressListsRequest.make ?nextToken ?pageSize ())
           (Some Values.ListAddressListsResponse.to_json)
           (Some Values.ListAddressListsResponse.error_to_json)])
let list_archive_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and archiveId =
         flag "archive-id" (required string) ~doc:"STRING ArchiveId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_archive_exports
           (Values.ListArchiveExportsRequest.make ?nextToken ?pageSize
              ~archiveId ()) (Some Values.ListArchiveExportsResponse.to_json)
           (Some Values.ListArchiveExportsResponse.error_to_json)])
let list_archive_searches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and archiveId =
         flag "archive-id" (required string) ~doc:"STRING ArchiveId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_archive_searches
           (Values.ListArchiveSearchesRequest.make ?nextToken ?pageSize
              ~archiveId ())
           (Some Values.ListArchiveSearchesResponse.to_json)
           (Some Values.ListArchiveSearchesResponse.error_to_json)])
let list_archives =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_archives
           (Values.ListArchivesRequest.make ?nextToken ?pageSize ())
           (Some Values.ListArchivesResponse.to_json)
           (Some Values.ListArchivesResponse.error_to_json)])
let list_ingress_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ingress_points
           (Values.ListIngressPointsRequest.make ?pageSize ?nextToken ())
           (Some Values.ListIngressPointsResponse.to_json)
           (Some Values.ListIngressPointsResponse.error_to_json)])
let list_members_of_address_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON AddressFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT AddressPageSize"
       and addressListId =
         flag "address-list-id" (required string) ~doc:"STRING AddressListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_members_of_address_list
           (Values.ListMembersOfAddressListRequest.make
              ?filter:(Option.map ~f:Values.AddressFilter.of_json filter)
              ?nextToken ?pageSize ~addressListId ())
           (Some Values.ListMembersOfAddressListResponse.to_json)
           (Some Values.ListMembersOfAddressListResponse.error_to_json)])
let list_relays =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_relays
           (Values.ListRelaysRequest.make ?pageSize ?nextToken ())
           (Some Values.ListRelaysResponse.to_json)
           (Some Values.ListRelaysResponse.error_to_json)])
let list_rule_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rule_sets
           (Values.ListRuleSetsRequest.make ?nextToken ?pageSize ())
           (Some Values.ListRuleSetsResponse.to_json)
           (Some Values.ListRuleSetsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_traffic_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_traffic_policies
           (Values.ListTrafficPoliciesRequest.make ?pageSize ?nextToken ())
           (Some Values.ListTrafficPoliciesResponse.to_json)
           (Some Values.ListTrafficPoliciesResponse.error_to_json)])
let register_member_to_address_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addressListId =
         flag "address-list-id" (required string) ~doc:"STRING AddressListId"
       and address = flag "address" (required string) ~doc:"STRING Address" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_member_to_address_list
           (Values.RegisterMemberToAddressListRequest.make ~addressListId
              ~address ())
           (Some Values.RegisterMemberToAddressListResponse.to_json)
           (Some Values.RegisterMemberToAddressListResponse.error_to_json)])
let start_address_list_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_address_list_import_job
           (Values.StartAddressListImportJobRequest.make ~jobId ())
           (Some Values.StartAddressListImportJobResponse.to_json)
           (Some Values.StartAddressListImportJobResponse.error_to_json)])
let start_archive_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ArchiveFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ExportMaxResults"
       and includeMetadata =
         flag "include-metadata" (optional bool) ~doc:"BOOL Boolean"
       and archiveId =
         flag "archive-id" (required string) ~doc:"STRING ArchiveId"
       and fromTimestamp =
         flag "from-timestamp" (required json_arg) ~doc:"JSON Timestamp"
       and toTimestamp =
         flag "to-timestamp" (required json_arg) ~doc:"JSON Timestamp"
       and exportDestinationConfiguration =
         flag "export-destination-configuration" (required json_arg)
           ~doc:"JSON ExportDestinationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_archive_export
           (Values.StartArchiveExportRequest.make
              ?filters:(Option.map ~f:Values.ArchiveFilters.of_json filters)
              ?maxResults ?includeMetadata ~archiveId
              ~fromTimestamp:(Values.Timestamp.of_json fromTimestamp)
              ~toTimestamp:(Values.Timestamp.of_json toTimestamp)
              ~exportDestinationConfiguration:(Values.ExportDestinationConfiguration.of_json
                                                 exportDestinationConfiguration)
              ()) (Some Values.StartArchiveExportResponse.to_json)
           (Some Values.StartArchiveExportResponse.error_to_json)])
let start_archive_search =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ArchiveFilters"
       and archiveId =
         flag "archive-id" (required string) ~doc:"STRING ArchiveId"
       and fromTimestamp =
         flag "from-timestamp" (required json_arg) ~doc:"JSON Timestamp"
       and toTimestamp =
         flag "to-timestamp" (required json_arg) ~doc:"JSON Timestamp"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT SearchMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_archive_search
           (Values.StartArchiveSearchRequest.make
              ?filters:(Option.map ~f:Values.ArchiveFilters.of_json filters)
              ~archiveId
              ~fromTimestamp:(Values.Timestamp.of_json fromTimestamp)
              ~toTimestamp:(Values.Timestamp.of_json toTimestamp) ~maxResults
              ()) (Some Values.StartArchiveSearchResponse.to_json)
           (Some Values.StartArchiveSearchResponse.error_to_json)])
let stop_address_list_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_address_list_import_job
           (Values.StopAddressListImportJobRequest.make ~jobId ())
           (Some Values.StopAddressListImportJobResponse.to_json)
           (Some Values.StopAddressListImportJobResponse.error_to_json)])
let stop_archive_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportId =
         flag "export-id" (required string) ~doc:"STRING ExportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_archive_export
           (Values.StopArchiveExportRequest.make ~exportId ())
           (Some Values.StopArchiveExportResponse.to_json)
           (Some Values.StopArchiveExportResponse.error_to_json)])
let stop_archive_search =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchId =
         flag "search-id" (required string) ~doc:"STRING SearchId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_archive_search
           (Values.StopArchiveSearchRequest.make ~searchId ())
           (Some Values.StopArchiveSearchResponse.to_json)
           (Some Values.StopArchiveSearchResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveName =
         flag "archive-name" (optional string)
           ~doc:"STRING ArchiveNameString"
       and retention =
         flag "retention" (optional json_arg) ~doc:"JSON ArchiveRetention"
       and archiveId =
         flag "archive-id" (required string) ~doc:"STRING ArchiveIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_archive
           (Values.UpdateArchiveRequest.make ?archiveName
              ?retention:(Option.map ~f:Values.ArchiveRetention.of_json
                            retention) ~archiveId ())
           (Some Values.UpdateArchiveResponse.to_json)
           (Some Values.UpdateArchiveResponse.error_to_json)])
let update_ingress_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ingressPointName =
         flag "ingress-point-name" (optional string)
           ~doc:"STRING IngressPointName"
       and statusToUpdate =
         flag "status-to-update" (optional json_arg)
           ~doc:"JSON IngressPointStatusToUpdate"
       and ruleSetId =
         flag "rule-set-id" (optional string) ~doc:"STRING RuleSetId"
       and trafficPolicyId =
         flag "traffic-policy-id" (optional string)
           ~doc:"STRING TrafficPolicyId"
       and ingressPointConfiguration =
         flag "ingress-point-configuration" (optional json_arg)
           ~doc:"JSON IngressPointConfiguration"
       and tlsPolicy =
         flag "tls-policy" (optional json_arg) ~doc:"JSON TlsPolicy"
       and ingressPointId =
         flag "ingress-point-id" (required string)
           ~doc:"STRING IngressPointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ingress_point
           (Values.UpdateIngressPointRequest.make ?ingressPointName
              ?statusToUpdate:(Option.map
                                 ~f:Values.IngressPointStatusToUpdate.of_json
                                 statusToUpdate) ?ruleSetId ?trafficPolicyId
              ?ingressPointConfiguration:(Option.map
                                            ~f:Values.IngressPointConfiguration.of_json
                                            ingressPointConfiguration)
              ?tlsPolicy:(Option.map ~f:Values.TlsPolicy.of_json tlsPolicy)
              ~ingressPointId ())
           (Some Values.UpdateIngressPointResponse.to_json)
           (Some Values.UpdateIngressPointResponse.error_to_json)])
let update_relay =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and relayName =
         flag "relay-name" (optional string) ~doc:"STRING RelayName"
       and serverName =
         flag "server-name" (optional string) ~doc:"STRING RelayServerName"
       and serverPort =
         flag "server-port" (optional int) ~doc:"INT RelayServerPort"
       and authentication =
         flag "authentication" (optional json_arg)
           ~doc:"JSON RelayAuthentication"
       and relayId = flag "relay-id" (required string) ~doc:"STRING RelayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_relay
           (Values.UpdateRelayRequest.make ?relayName ?serverName ?serverPort
              ?authentication:(Option.map
                                 ~f:Values.RelayAuthentication.of_json
                                 authentication) ~relayId ())
           (Some Values.UpdateRelayResponse.to_json)
           (Some Values.UpdateRelayResponse.error_to_json)])
let update_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetName =
         flag "rule-set-name" (optional string) ~doc:"STRING RuleSetName"
       and rules = flag "rules" (optional json_arg) ~doc:"JSON Rules"
       and ruleSetId =
         flag "rule-set-id" (required string) ~doc:"STRING RuleSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rule_set
           (Values.UpdateRuleSetRequest.make ?ruleSetName
              ?rules:(Option.map ~f:Values.Rules.of_json rules) ~ruleSetId ())
           (Some Values.UpdateRuleSetResponse.to_json)
           (Some Values.UpdateRuleSetResponse.error_to_json)])
let update_traffic_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trafficPolicyName =
         flag "traffic-policy-name" (optional string)
           ~doc:"STRING TrafficPolicyName"
       and policyStatements =
         flag "policy-statements" (optional json_arg)
           ~doc:"JSON PolicyStatementList"
       and defaultAction =
         flag "default-action" (optional json_arg) ~doc:"JSON AcceptAction"
       and maxMessageSizeBytes =
         flag "max-message-size-bytes" (optional int)
           ~doc:"INT MaxMessageSizeBytes"
       and trafficPolicyId =
         flag "traffic-policy-id" (required string)
           ~doc:"STRING TrafficPolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_traffic_policy
           (Values.UpdateTrafficPolicyRequest.make ?trafficPolicyName
              ?policyStatements:(Option.map
                                   ~f:Values.PolicyStatementList.of_json
                                   policyStatements)
              ?defaultAction:(Option.map ~f:Values.AcceptAction.of_json
                                defaultAction) ?maxMessageSizeBytes
              ~trafficPolicyId ())
           (Some Values.UpdateTrafficPolicyResponse.to_json)
           (Some Values.UpdateTrafficPolicyResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-addon-instance", create_addon_instance);
    ("create-addon-subscription", create_addon_subscription);
    ("create-address-list", create_address_list);
    ("create-address-list-import-job", create_address_list_import_job);
    ("create-archive", create_archive);
    ("create-ingress-point", create_ingress_point);
    ("create-relay", create_relay);
    ("create-rule-set", create_rule_set);
    ("create-traffic-policy", create_traffic_policy);
    ("delete-addon-instance", delete_addon_instance);
    ("delete-addon-subscription", delete_addon_subscription);
    ("delete-address-list", delete_address_list);
    ("delete-archive", delete_archive);
    ("delete-ingress-point", delete_ingress_point);
    ("delete-relay", delete_relay);
    ("delete-rule-set", delete_rule_set);
    ("delete-traffic-policy", delete_traffic_policy);
    ("deregister-member-from-address-list",
      deregister_member_from_address_list);
    ("get-addon-instance", get_addon_instance);
    ("get-addon-subscription", get_addon_subscription);
    ("get-address-list", get_address_list);
    ("get-address-list-import-job", get_address_list_import_job);
    ("get-archive", get_archive);
    ("get-archive-export", get_archive_export);
    ("get-archive-message", get_archive_message);
    ("get-archive-message-content", get_archive_message_content);
    ("get-archive-search", get_archive_search);
    ("get-archive-search-results", get_archive_search_results);
    ("get-ingress-point", get_ingress_point);
    ("get-member-of-address-list", get_member_of_address_list);
    ("get-relay", get_relay);
    ("get-rule-set", get_rule_set);
    ("get-traffic-policy", get_traffic_policy);
    ("list-addon-instances", list_addon_instances);
    ("list-addon-subscriptions", list_addon_subscriptions);
    ("list-address-list-import-jobs", list_address_list_import_jobs);
    ("list-address-lists", list_address_lists);
    ("list-archive-exports", list_archive_exports);
    ("list-archive-searches", list_archive_searches);
    ("list-archives", list_archives);
    ("list-ingress-points", list_ingress_points);
    ("list-members-of-address-list", list_members_of_address_list);
    ("list-relays", list_relays);
    ("list-rule-sets", list_rule_sets);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-traffic-policies", list_traffic_policies);
    ("register-member-to-address-list", register_member_to_address_list);
    ("start-address-list-import-job", start_address_list_import_job);
    ("start-archive-export", start_archive_export);
    ("start-archive-search", start_archive_search);
    ("stop-address-list-import-job", stop_address_list_import_job);
    ("stop-archive-export", stop_archive_export);
    ("stop-archive-search", stop_archive_search);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-archive", update_archive);
    ("update-ingress-point", update_ingress_point);
    ("update-relay", update_relay);
    ("update-rule-set", update_rule_set);
    ("update-traffic-policy", update_traffic_policy)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_role_to_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_role_to_group
           (Values.AssociateRoleToGroupRequest.make ~groupId ~roleArn ())
           (Some Values.AssociateRoleToGroupResponse.to_json)
           (Some Values.AssociateRoleToGroupResponse.error_to_json)])
let associate_service_role_to_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_service_role_to_account
           (Values.AssociateServiceRoleToAccountRequest.make ~roleArn ())
           (Some Values.AssociateServiceRoleToAccountResponse.to_json)
           (Some Values.AssociateServiceRoleToAccountResponse.error_to_json)])
let create_connector_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and initialVersion =
         flag "initial-version" (optional json_arg)
           ~doc:"JSON ConnectorDefinitionVersion"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connector_definition
           (Values.CreateConnectorDefinitionRequest.make ?amznClientToken
              ?initialVersion:(Option.map
                                 ~f:Values.ConnectorDefinitionVersion.of_json
                                 initialVersion) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateConnectorDefinitionResponse.to_json)
           (Some Values.CreateConnectorDefinitionResponse.error_to_json)])
let create_connector_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and connectors =
         flag "connectors" (optional json_arg) ~doc:"JSON __listOfConnector"
       and connectorDefinitionId =
         flag "connector-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connector_definition_version
           (Values.CreateConnectorDefinitionVersionRequest.make
              ?amznClientToken
              ?connectors:(Option.map ~f:Values.Zz__listOfConnector.of_json
                             connectors) ~connectorDefinitionId ())
           (Some Values.CreateConnectorDefinitionVersionResponse.to_json)
           (Some
              Values.CreateConnectorDefinitionVersionResponse.error_to_json)])
let create_core_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and initialVersion =
         flag "initial-version" (optional json_arg)
           ~doc:"JSON CoreDefinitionVersion"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_core_definition
           (Values.CreateCoreDefinitionRequest.make ?amznClientToken
              ?initialVersion:(Option.map
                                 ~f:Values.CoreDefinitionVersion.of_json
                                 initialVersion) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateCoreDefinitionResponse.to_json)
           (Some Values.CreateCoreDefinitionResponse.error_to_json)])
let create_core_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and cores = flag "cores" (optional json_arg) ~doc:"JSON __listOfCore"
       and coreDefinitionId =
         flag "core-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_core_definition_version
           (Values.CreateCoreDefinitionVersionRequest.make ?amznClientToken
              ?cores:(Option.map ~f:Values.Zz__listOfCore.of_json cores)
              ~coreDefinitionId ())
           (Some Values.CreateCoreDefinitionVersionResponse.to_json)
           (Some Values.CreateCoreDefinitionVersionResponse.error_to_json)])
let create_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and deploymentId =
         flag "deployment-id" (optional string) ~doc:"STRING __string"
       and groupVersionId =
         flag "group-version-id" (optional string) ~doc:"STRING __string"
       and deploymentType =
         flag "deployment-type" (required json_arg)
           ~doc:"JSON DeploymentType"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment
           (Values.CreateDeploymentRequest.make ?amznClientToken
              ?deploymentId ?groupVersionId
              ~deploymentType:(Values.DeploymentType.of_json deploymentType)
              ~groupId ()) (Some Values.CreateDeploymentResponse.to_json)
           (Some Values.CreateDeploymentResponse.error_to_json)])
let create_device_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and initialVersion =
         flag "initial-version" (optional json_arg)
           ~doc:"JSON DeviceDefinitionVersion"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_device_definition
           (Values.CreateDeviceDefinitionRequest.make ?amznClientToken
              ?initialVersion:(Option.map
                                 ~f:Values.DeviceDefinitionVersion.of_json
                                 initialVersion) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateDeviceDefinitionResponse.to_json)
           (Some Values.CreateDeviceDefinitionResponse.error_to_json)])
let create_device_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and devices =
         flag "devices" (optional json_arg) ~doc:"JSON __listOfDevice"
       and deviceDefinitionId =
         flag "device-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_device_definition_version
           (Values.CreateDeviceDefinitionVersionRequest.make ?amznClientToken
              ?devices:(Option.map ~f:Values.Zz__listOfDevice.of_json devices)
              ~deviceDefinitionId ())
           (Some Values.CreateDeviceDefinitionVersionResponse.to_json)
           (Some Values.CreateDeviceDefinitionVersionResponse.error_to_json)])
let create_function_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and initialVersion =
         flag "initial-version" (optional json_arg)
           ~doc:"JSON FunctionDefinitionVersion"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_function_definition
           (Values.CreateFunctionDefinitionRequest.make ?amznClientToken
              ?initialVersion:(Option.map
                                 ~f:Values.FunctionDefinitionVersion.of_json
                                 initialVersion) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateFunctionDefinitionResponse.to_json)
           (Some Values.CreateFunctionDefinitionResponse.error_to_json)])
let create_function_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and defaultConfig =
         flag "default-config" (optional json_arg)
           ~doc:"JSON FunctionDefaultConfig"
       and functions =
         flag "functions" (optional json_arg) ~doc:"JSON __listOfFunction"
       and functionDefinitionId =
         flag "function-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_function_definition_version
           (Values.CreateFunctionDefinitionVersionRequest.make
              ?amznClientToken
              ?defaultConfig:(Option.map
                                ~f:Values.FunctionDefaultConfig.of_json
                                defaultConfig)
              ?functions:(Option.map ~f:Values.Zz__listOfFunction.of_json
                            functions) ~functionDefinitionId ())
           (Some Values.CreateFunctionDefinitionVersionResponse.to_json)
           (Some Values.CreateFunctionDefinitionVersionResponse.error_to_json)])
let create_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and initialVersion =
         flag "initial-version" (optional json_arg) ~doc:"JSON GroupVersion"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group
           (Values.CreateGroupRequest.make ?amznClientToken
              ?initialVersion:(Option.map ~f:Values.GroupVersion.of_json
                                 initialVersion)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ())
           (Some Values.CreateGroupResponse.to_json)
           (Some Values.CreateGroupResponse.error_to_json)])
let create_group_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group_certificate_authority
           (Values.CreateGroupCertificateAuthorityRequest.make
              ?amznClientToken ~groupId ())
           (Some Values.CreateGroupCertificateAuthorityResponse.to_json)
           (Some Values.CreateGroupCertificateAuthorityResponse.error_to_json)])
let create_group_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and connectorDefinitionVersionArn =
         flag "connector-definition-version-arn" (optional string)
           ~doc:"STRING __string"
       and coreDefinitionVersionArn =
         flag "core-definition-version-arn" (optional string)
           ~doc:"STRING __string"
       and deviceDefinitionVersionArn =
         flag "device-definition-version-arn" (optional string)
           ~doc:"STRING __string"
       and functionDefinitionVersionArn =
         flag "function-definition-version-arn" (optional string)
           ~doc:"STRING __string"
       and loggerDefinitionVersionArn =
         flag "logger-definition-version-arn" (optional string)
           ~doc:"STRING __string"
       and resourceDefinitionVersionArn =
         flag "resource-definition-version-arn" (optional string)
           ~doc:"STRING __string"
       and subscriptionDefinitionVersionArn =
         flag "subscription-definition-version-arn" (optional string)
           ~doc:"STRING __string"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group_version
           (Values.CreateGroupVersionRequest.make ?amznClientToken
              ?connectorDefinitionVersionArn ?coreDefinitionVersionArn
              ?deviceDefinitionVersionArn ?functionDefinitionVersionArn
              ?loggerDefinitionVersionArn ?resourceDefinitionVersionArn
              ?subscriptionDefinitionVersionArn ~groupId ())
           (Some Values.CreateGroupVersionResponse.to_json)
           (Some Values.CreateGroupVersionResponse.error_to_json)])
let create_logger_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and initialVersion =
         flag "initial-version" (optional json_arg)
           ~doc:"JSON LoggerDefinitionVersion"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_logger_definition
           (Values.CreateLoggerDefinitionRequest.make ?amznClientToken
              ?initialVersion:(Option.map
                                 ~f:Values.LoggerDefinitionVersion.of_json
                                 initialVersion) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateLoggerDefinitionResponse.to_json)
           (Some Values.CreateLoggerDefinitionResponse.error_to_json)])
let create_logger_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and loggers =
         flag "loggers" (optional json_arg) ~doc:"JSON __listOfLogger"
       and loggerDefinitionId =
         flag "logger-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_logger_definition_version
           (Values.CreateLoggerDefinitionVersionRequest.make ?amznClientToken
              ?loggers:(Option.map ~f:Values.Zz__listOfLogger.of_json loggers)
              ~loggerDefinitionId ())
           (Some Values.CreateLoggerDefinitionVersionResponse.to_json)
           (Some Values.CreateLoggerDefinitionVersionResponse.error_to_json)])
let create_resource_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and initialVersion =
         flag "initial-version" (optional json_arg)
           ~doc:"JSON ResourceDefinitionVersion"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_definition
           (Values.CreateResourceDefinitionRequest.make ?amznClientToken
              ?initialVersion:(Option.map
                                 ~f:Values.ResourceDefinitionVersion.of_json
                                 initialVersion) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateResourceDefinitionResponse.to_json)
           (Some Values.CreateResourceDefinitionResponse.error_to_json)])
let create_resource_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and resources =
         flag "resources" (optional json_arg) ~doc:"JSON __listOfResource"
       and resourceDefinitionId =
         flag "resource-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_definition_version
           (Values.CreateResourceDefinitionVersionRequest.make
              ?amznClientToken
              ?resources:(Option.map ~f:Values.Zz__listOfResource.of_json
                            resources) ~resourceDefinitionId ())
           (Some Values.CreateResourceDefinitionVersionResponse.to_json)
           (Some Values.CreateResourceDefinitionVersionResponse.error_to_json)])
let create_software_update_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and updateAgentLogLevel =
         flag "update-agent-log-level" (optional json_arg)
           ~doc:"JSON UpdateAgentLogLevel"
       and s3UrlSignerRole =
         flag "s3-url-signer-role" (required string)
           ~doc:"STRING S3UrlSignerRole"
       and softwareToUpdate =
         flag "software-to-update" (required json_arg)
           ~doc:"JSON SoftwareToUpdate"
       and updateTargets =
         flag "update-targets" (required json_arg) ~doc:"JSON UpdateTargets"
       and updateTargetsArchitecture =
         flag "update-targets-architecture" (required json_arg)
           ~doc:"JSON UpdateTargetsArchitecture"
       and updateTargetsOperatingSystem =
         flag "update-targets-operating-system" (required json_arg)
           ~doc:"JSON UpdateTargetsOperatingSystem" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_software_update_job
           (Values.CreateSoftwareUpdateJobRequest.make ?amznClientToken
              ?updateAgentLogLevel:(Option.map
                                      ~f:Values.UpdateAgentLogLevel.of_json
                                      updateAgentLogLevel) ~s3UrlSignerRole
              ~softwareToUpdate:(Values.SoftwareToUpdate.of_json
                                   softwareToUpdate)
              ~updateTargets:(Values.UpdateTargets.of_json updateTargets)
              ~updateTargetsArchitecture:(Values.UpdateTargetsArchitecture.of_json
                                            updateTargetsArchitecture)
              ~updateTargetsOperatingSystem:(Values.UpdateTargetsOperatingSystem.of_json
                                               updateTargetsOperatingSystem)
              ()) (Some Values.CreateSoftwareUpdateJobResponse.to_json)
           (Some Values.CreateSoftwareUpdateJobResponse.error_to_json)])
let create_subscription_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and initialVersion =
         flag "initial-version" (optional json_arg)
           ~doc:"JSON SubscriptionDefinitionVersion"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscription_definition
           (Values.CreateSubscriptionDefinitionRequest.make ?amznClientToken
              ?initialVersion:(Option.map
                                 ~f:Values.SubscriptionDefinitionVersion.of_json
                                 initialVersion) ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ())
           (Some Values.CreateSubscriptionDefinitionResponse.to_json)
           (Some Values.CreateSubscriptionDefinitionResponse.error_to_json)])
let create_subscription_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and subscriptions =
         flag "subscriptions" (optional json_arg)
           ~doc:"JSON __listOfSubscription"
       and subscriptionDefinitionId =
         flag "subscription-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscription_definition_version
           (Values.CreateSubscriptionDefinitionVersionRequest.make
              ?amznClientToken
              ?subscriptions:(Option.map
                                ~f:Values.Zz__listOfSubscription.of_json
                                subscriptions) ~subscriptionDefinitionId ())
           (Some Values.CreateSubscriptionDefinitionVersionResponse.to_json)
           (Some
              Values.CreateSubscriptionDefinitionVersionResponse.error_to_json)])
let delete_connector_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorDefinitionId =
         flag "connector-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connector_definition
           (Values.DeleteConnectorDefinitionRequest.make
              ~connectorDefinitionId ())
           (Some Values.DeleteConnectorDefinitionResponse.to_json)
           (Some Values.DeleteConnectorDefinitionResponse.error_to_json)])
let delete_core_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreDefinitionId =
         flag "core-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_core_definition
           (Values.DeleteCoreDefinitionRequest.make ~coreDefinitionId ())
           (Some Values.DeleteCoreDefinitionResponse.to_json)
           (Some Values.DeleteCoreDefinitionResponse.error_to_json)])
let delete_device_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceDefinitionId =
         flag "device-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device_definition
           (Values.DeleteDeviceDefinitionRequest.make ~deviceDefinitionId ())
           (Some Values.DeleteDeviceDefinitionResponse.to_json)
           (Some Values.DeleteDeviceDefinitionResponse.error_to_json)])
let delete_function_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionDefinitionId =
         flag "function-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_function_definition
           (Values.DeleteFunctionDefinitionRequest.make ~functionDefinitionId
              ()) (Some Values.DeleteFunctionDefinitionResponse.to_json)
           (Some Values.DeleteFunctionDefinitionResponse.error_to_json)])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group (Values.DeleteGroupRequest.make ~groupId ())
           (Some Values.DeleteGroupResponse.to_json)
           (Some Values.DeleteGroupResponse.error_to_json)])
let delete_logger_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loggerDefinitionId =
         flag "logger-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_logger_definition
           (Values.DeleteLoggerDefinitionRequest.make ~loggerDefinitionId ())
           (Some Values.DeleteLoggerDefinitionResponse.to_json)
           (Some Values.DeleteLoggerDefinitionResponse.error_to_json)])
let delete_resource_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceDefinitionId =
         flag "resource-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_definition
           (Values.DeleteResourceDefinitionRequest.make ~resourceDefinitionId
              ()) (Some Values.DeleteResourceDefinitionResponse.to_json)
           (Some Values.DeleteResourceDefinitionResponse.error_to_json)])
let delete_subscription_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionDefinitionId =
         flag "subscription-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subscription_definition
           (Values.DeleteSubscriptionDefinitionRequest.make
              ~subscriptionDefinitionId ())
           (Some Values.DeleteSubscriptionDefinitionResponse.to_json)
           (Some Values.DeleteSubscriptionDefinitionResponse.error_to_json)])
let disassociate_role_from_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_role_from_group
           (Values.DisassociateRoleFromGroupRequest.make ~groupId ())
           (Some Values.DisassociateRoleFromGroupResponse.to_json)
           (Some Values.DisassociateRoleFromGroupResponse.error_to_json)])
let disassociate_service_role_from_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_service_role_from_account
           (Values.DisassociateServiceRoleFromAccountRequest.make ())
           (Some Values.DisassociateServiceRoleFromAccountResponse.to_json)
           (Some
              Values.DisassociateServiceRoleFromAccountResponse.error_to_json)])
let get_associated_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_associated_role
           (Values.GetAssociatedRoleRequest.make ~groupId ())
           (Some Values.GetAssociatedRoleResponse.to_json)
           (Some Values.GetAssociatedRoleResponse.error_to_json)])
let get_bulk_deployment_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bulkDeploymentId =
         flag "bulk-deployment-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bulk_deployment_status
           (Values.GetBulkDeploymentStatusRequest.make ~bulkDeploymentId ())
           (Some Values.GetBulkDeploymentStatusResponse.to_json)
           (Some Values.GetBulkDeploymentStatusResponse.error_to_json)])
let get_connectivity_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connectivity_info
           (Values.GetConnectivityInfoRequest.make ~thingName ())
           (Some Values.GetConnectivityInfoResponse.to_json)
           (Some Values.GetConnectivityInfoResponse.error_to_json)])
let get_connector_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorDefinitionId =
         flag "connector-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connector_definition
           (Values.GetConnectorDefinitionRequest.make ~connectorDefinitionId
              ()) (Some Values.GetConnectorDefinitionResponse.to_json)
           (Some Values.GetConnectorDefinitionResponse.error_to_json)])
let get_connector_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and connectorDefinitionId =
         flag "connector-definition-id" (required string)
           ~doc:"STRING __string"
       and connectorDefinitionVersionId =
         flag "connector-definition-version-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connector_definition_version
           (Values.GetConnectorDefinitionVersionRequest.make ?nextToken
              ~connectorDefinitionId ~connectorDefinitionVersionId ())
           (Some Values.GetConnectorDefinitionVersionResponse.to_json)
           (Some Values.GetConnectorDefinitionVersionResponse.error_to_json)])
let get_core_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreDefinitionId =
         flag "core-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_core_definition
           (Values.GetCoreDefinitionRequest.make ~coreDefinitionId ())
           (Some Values.GetCoreDefinitionResponse.to_json)
           (Some Values.GetCoreDefinitionResponse.error_to_json)])
let get_core_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreDefinitionId =
         flag "core-definition-id" (required string) ~doc:"STRING __string"
       and coreDefinitionVersionId =
         flag "core-definition-version-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_core_definition_version
           (Values.GetCoreDefinitionVersionRequest.make ~coreDefinitionId
              ~coreDefinitionVersionId ())
           (Some Values.GetCoreDefinitionVersionResponse.to_json)
           (Some Values.GetCoreDefinitionVersionResponse.error_to_json)])
let get_deployment_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING __string"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployment_status
           (Values.GetDeploymentStatusRequest.make ~deploymentId ~groupId ())
           (Some Values.GetDeploymentStatusResponse.to_json)
           (Some Values.GetDeploymentStatusResponse.error_to_json)])
let get_device_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceDefinitionId =
         flag "device-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_definition
           (Values.GetDeviceDefinitionRequest.make ~deviceDefinitionId ())
           (Some Values.GetDeviceDefinitionResponse.to_json)
           (Some Values.GetDeviceDefinitionResponse.error_to_json)])
let get_device_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and deviceDefinitionId =
         flag "device-definition-id" (required string) ~doc:"STRING __string"
       and deviceDefinitionVersionId =
         flag "device-definition-version-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_definition_version
           (Values.GetDeviceDefinitionVersionRequest.make ?nextToken
              ~deviceDefinitionId ~deviceDefinitionVersionId ())
           (Some Values.GetDeviceDefinitionVersionResponse.to_json)
           (Some Values.GetDeviceDefinitionVersionResponse.error_to_json)])
let get_function_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionDefinitionId =
         flag "function-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_definition
           (Values.GetFunctionDefinitionRequest.make ~functionDefinitionId ())
           (Some Values.GetFunctionDefinitionResponse.to_json)
           (Some Values.GetFunctionDefinitionResponse.error_to_json)])
let get_function_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and functionDefinitionId =
         flag "function-definition-id" (required string)
           ~doc:"STRING __string"
       and functionDefinitionVersionId =
         flag "function-definition-version-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_definition_version
           (Values.GetFunctionDefinitionVersionRequest.make ?nextToken
              ~functionDefinitionId ~functionDefinitionVersionId ())
           (Some Values.GetFunctionDefinitionVersionResponse.to_json)
           (Some Values.GetFunctionDefinitionVersionResponse.error_to_json)])
let get_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group (Values.GetGroupRequest.make ~groupId ())
           (Some Values.GetGroupResponse.to_json)
           (Some Values.GetGroupResponse.error_to_json)])
let get_group_certificate_authority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateAuthorityId =
         flag "certificate-authority-id" (required string)
           ~doc:"STRING __string"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group_certificate_authority
           (Values.GetGroupCertificateAuthorityRequest.make
              ~certificateAuthorityId ~groupId ())
           (Some Values.GetGroupCertificateAuthorityResponse.to_json)
           (Some Values.GetGroupCertificateAuthorityResponse.error_to_json)])
let get_group_certificate_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group_certificate_configuration
           (Values.GetGroupCertificateConfigurationRequest.make ~groupId ())
           (Some Values.GetGroupCertificateConfigurationResponse.to_json)
           (Some
              Values.GetGroupCertificateConfigurationResponse.error_to_json)])
let get_group_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string"
       and groupVersionId =
         flag "group-version-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group_version
           (Values.GetGroupVersionRequest.make ~groupId ~groupVersionId ())
           (Some Values.GetGroupVersionResponse.to_json)
           (Some Values.GetGroupVersionResponse.error_to_json)])
let get_logger_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loggerDefinitionId =
         flag "logger-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_logger_definition
           (Values.GetLoggerDefinitionRequest.make ~loggerDefinitionId ())
           (Some Values.GetLoggerDefinitionResponse.to_json)
           (Some Values.GetLoggerDefinitionResponse.error_to_json)])
let get_logger_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and loggerDefinitionId =
         flag "logger-definition-id" (required string) ~doc:"STRING __string"
       and loggerDefinitionVersionId =
         flag "logger-definition-version-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_logger_definition_version
           (Values.GetLoggerDefinitionVersionRequest.make ?nextToken
              ~loggerDefinitionId ~loggerDefinitionVersionId ())
           (Some Values.GetLoggerDefinitionVersionResponse.to_json)
           (Some Values.GetLoggerDefinitionVersionResponse.error_to_json)])
let get_resource_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceDefinitionId =
         flag "resource-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_definition
           (Values.GetResourceDefinitionRequest.make ~resourceDefinitionId ())
           (Some Values.GetResourceDefinitionResponse.to_json)
           (Some Values.GetResourceDefinitionResponse.error_to_json)])
let get_resource_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceDefinitionId =
         flag "resource-definition-id" (required string)
           ~doc:"STRING __string"
       and resourceDefinitionVersionId =
         flag "resource-definition-version-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_definition_version
           (Values.GetResourceDefinitionVersionRequest.make
              ~resourceDefinitionId ~resourceDefinitionVersionId ())
           (Some Values.GetResourceDefinitionVersionResponse.to_json)
           (Some Values.GetResourceDefinitionVersionResponse.error_to_json)])
let get_service_role_for_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_role_for_account
           (Values.GetServiceRoleForAccountRequest.make ())
           (Some Values.GetServiceRoleForAccountResponse.to_json)
           (Some Values.GetServiceRoleForAccountResponse.error_to_json)])
let get_subscription_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionDefinitionId =
         flag "subscription-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription_definition
           (Values.GetSubscriptionDefinitionRequest.make
              ~subscriptionDefinitionId ())
           (Some Values.GetSubscriptionDefinitionResponse.to_json)
           (Some Values.GetSubscriptionDefinitionResponse.error_to_json)])
let get_subscription_definition_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and subscriptionDefinitionId =
         flag "subscription-definition-id" (required string)
           ~doc:"STRING __string"
       and subscriptionDefinitionVersionId =
         flag "subscription-definition-version-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription_definition_version
           (Values.GetSubscriptionDefinitionVersionRequest.make ?nextToken
              ~subscriptionDefinitionId ~subscriptionDefinitionVersionId ())
           (Some Values.GetSubscriptionDefinitionVersionResponse.to_json)
           (Some
              Values.GetSubscriptionDefinitionVersionResponse.error_to_json)])
let get_thing_runtime_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_thing_runtime_configuration
           (Values.GetThingRuntimeConfigurationRequest.make ~thingName ())
           (Some Values.GetThingRuntimeConfigurationResponse.to_json)
           (Some Values.GetThingRuntimeConfigurationResponse.error_to_json)])
let list_bulk_deployment_detailed_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and bulkDeploymentId =
         flag "bulk-deployment-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bulk_deployment_detailed_reports
           (Values.ListBulkDeploymentDetailedReportsRequest.make ?maxResults
              ?nextToken ~bulkDeploymentId ())
           (Some Values.ListBulkDeploymentDetailedReportsResponse.to_json)
           (Some
              Values.ListBulkDeploymentDetailedReportsResponse.error_to_json)])
let list_bulk_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bulk_deployments
           (Values.ListBulkDeploymentsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListBulkDeploymentsResponse.to_json)
           (Some Values.ListBulkDeploymentsResponse.error_to_json)])
let list_connector_definition_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and connectorDefinitionId =
         flag "connector-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connector_definition_versions
           (Values.ListConnectorDefinitionVersionsRequest.make ?maxResults
              ?nextToken ~connectorDefinitionId ())
           (Some Values.ListConnectorDefinitionVersionsResponse.to_json)
           (Some Values.ListConnectorDefinitionVersionsResponse.error_to_json)])
let list_connector_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connector_definitions
           (Values.ListConnectorDefinitionsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListConnectorDefinitionsResponse.to_json)
           (Some Values.ListConnectorDefinitionsResponse.error_to_json)])
let list_core_definition_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and coreDefinitionId =
         flag "core-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_core_definition_versions
           (Values.ListCoreDefinitionVersionsRequest.make ?maxResults
              ?nextToken ~coreDefinitionId ())
           (Some Values.ListCoreDefinitionVersionsResponse.to_json)
           (Some Values.ListCoreDefinitionVersionsResponse.error_to_json)])
let list_core_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_core_definitions
           (Values.ListCoreDefinitionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListCoreDefinitionsResponse.to_json)
           (Some Values.ListCoreDefinitionsResponse.error_to_json)])
let list_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deployments
           (Values.ListDeploymentsRequest.make ?maxResults ?nextToken
              ~groupId ()) (Some Values.ListDeploymentsResponse.to_json)
           (Some Values.ListDeploymentsResponse.error_to_json)])
let list_device_definition_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and deviceDefinitionId =
         flag "device-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_definition_versions
           (Values.ListDeviceDefinitionVersionsRequest.make ?maxResults
              ?nextToken ~deviceDefinitionId ())
           (Some Values.ListDeviceDefinitionVersionsResponse.to_json)
           (Some Values.ListDeviceDefinitionVersionsResponse.error_to_json)])
let list_device_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_definitions
           (Values.ListDeviceDefinitionsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListDeviceDefinitionsResponse.to_json)
           (Some Values.ListDeviceDefinitionsResponse.error_to_json)])
let list_function_definition_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and functionDefinitionId =
         flag "function-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_function_definition_versions
           (Values.ListFunctionDefinitionVersionsRequest.make ?maxResults
              ?nextToken ~functionDefinitionId ())
           (Some Values.ListFunctionDefinitionVersionsResponse.to_json)
           (Some Values.ListFunctionDefinitionVersionsResponse.error_to_json)])
let list_function_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_function_definitions
           (Values.ListFunctionDefinitionsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListFunctionDefinitionsResponse.to_json)
           (Some Values.ListFunctionDefinitionsResponse.error_to_json)])
let list_group_certificate_authorities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_group_certificate_authorities
           (Values.ListGroupCertificateAuthoritiesRequest.make ~groupId ())
           (Some Values.ListGroupCertificateAuthoritiesResponse.to_json)
           (Some Values.ListGroupCertificateAuthoritiesResponse.error_to_json)])
let list_group_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_group_versions
           (Values.ListGroupVersionsRequest.make ?maxResults ?nextToken
              ~groupId ()) (Some Values.ListGroupVersionsResponse.to_json)
           (Some Values.ListGroupVersionsResponse.error_to_json)])
let list_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups
           (Values.ListGroupsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListGroupsResponse.to_json)
           (Some Values.ListGroupsResponse.error_to_json)])
let list_logger_definition_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and loggerDefinitionId =
         flag "logger-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_logger_definition_versions
           (Values.ListLoggerDefinitionVersionsRequest.make ?maxResults
              ?nextToken ~loggerDefinitionId ())
           (Some Values.ListLoggerDefinitionVersionsResponse.to_json)
           (Some Values.ListLoggerDefinitionVersionsResponse.error_to_json)])
let list_logger_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_logger_definitions
           (Values.ListLoggerDefinitionsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListLoggerDefinitionsResponse.to_json)
           (Some Values.ListLoggerDefinitionsResponse.error_to_json)])
let list_resource_definition_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and resourceDefinitionId =
         flag "resource-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_definition_versions
           (Values.ListResourceDefinitionVersionsRequest.make ?maxResults
              ?nextToken ~resourceDefinitionId ())
           (Some Values.ListResourceDefinitionVersionsResponse.to_json)
           (Some Values.ListResourceDefinitionVersionsResponse.error_to_json)])
let list_resource_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_definitions
           (Values.ListResourceDefinitionsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListResourceDefinitionsResponse.to_json)
           (Some Values.ListResourceDefinitionsResponse.error_to_json)])
let list_subscription_definition_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and subscriptionDefinitionId =
         flag "subscription-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscription_definition_versions
           (Values.ListSubscriptionDefinitionVersionsRequest.make ?maxResults
              ?nextToken ~subscriptionDefinitionId ())
           (Some Values.ListSubscriptionDefinitionVersionsResponse.to_json)
           (Some
              Values.ListSubscriptionDefinitionVersionsResponse.error_to_json)])
let list_subscription_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscription_definitions
           (Values.ListSubscriptionDefinitionsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListSubscriptionDefinitionsResponse.to_json)
           (Some Values.ListSubscriptionDefinitionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let reset_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and force = flag "force" (optional bool) ~doc:"BOOL __boolean"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_deployments
           (Values.ResetDeploymentsRequest.make ?amznClientToken ?force
              ~groupId ()) (Some Values.ResetDeploymentsResponse.to_json)
           (Some Values.ResetDeploymentsResponse.error_to_json)])
let start_bulk_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amznClientToken =
         flag "amzn-client-token" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING __string"
       and inputFileUri =
         flag "input-file-uri" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_bulk_deployment
           (Values.StartBulkDeploymentRequest.make ?amznClientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~executionRoleArn ~inputFileUri ())
           (Some Values.StartBulkDeploymentResponse.to_json)
           (Some Values.StartBulkDeploymentResponse.error_to_json)])
let stop_bulk_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bulkDeploymentId =
         flag "bulk-deployment-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_bulk_deployment
           (Values.StopBulkDeploymentRequest.make ~bulkDeploymentId ())
           (Some Values.StopBulkDeploymentResponse.to_json)
           (Some Values.StopBulkDeploymentResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~resourceArn ())
           None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let update_connectivity_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectivityInfo =
         flag "connectivity-info" (optional json_arg)
           ~doc:"JSON __listOfConnectivityInfo"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connectivity_info
           (Values.UpdateConnectivityInfoRequest.make
              ?connectivityInfo:(Option.map
                                   ~f:Values.Zz__listOfConnectivityInfo.of_json
                                   connectivityInfo) ~thingName ())
           (Some Values.UpdateConnectivityInfoResponse.to_json)
           (Some Values.UpdateConnectivityInfoResponse.error_to_json)])
let update_connector_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and connectorDefinitionId =
         flag "connector-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connector_definition
           (Values.UpdateConnectorDefinitionRequest.make ?name
              ~connectorDefinitionId ())
           (Some Values.UpdateConnectorDefinitionResponse.to_json)
           (Some Values.UpdateConnectorDefinitionResponse.error_to_json)])
let update_core_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and coreDefinitionId =
         flag "core-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_core_definition
           (Values.UpdateCoreDefinitionRequest.make ?name ~coreDefinitionId
              ()) (Some Values.UpdateCoreDefinitionResponse.to_json)
           (Some Values.UpdateCoreDefinitionResponse.error_to_json)])
let update_device_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and deviceDefinitionId =
         flag "device-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device_definition
           (Values.UpdateDeviceDefinitionRequest.make ?name
              ~deviceDefinitionId ())
           (Some Values.UpdateDeviceDefinitionResponse.to_json)
           (Some Values.UpdateDeviceDefinitionResponse.error_to_json)])
let update_function_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and functionDefinitionId =
         flag "function-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_function_definition
           (Values.UpdateFunctionDefinitionRequest.make ?name
              ~functionDefinitionId ())
           (Some Values.UpdateFunctionDefinitionResponse.to_json)
           (Some Values.UpdateFunctionDefinitionResponse.error_to_json)])
let update_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group (Values.UpdateGroupRequest.make ?name ~groupId ())
           (Some Values.UpdateGroupResponse.to_json)
           (Some Values.UpdateGroupResponse.error_to_json)])
let update_group_certificate_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateExpiryInMilliseconds =
         flag "certificate-expiry-in-milliseconds" (optional string)
           ~doc:"STRING __string"
       and groupId = flag "group-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group_certificate_configuration
           (Values.UpdateGroupCertificateConfigurationRequest.make
              ?certificateExpiryInMilliseconds ~groupId ())
           (Some Values.UpdateGroupCertificateConfigurationResponse.to_json)
           (Some
              Values.UpdateGroupCertificateConfigurationResponse.error_to_json)])
let update_logger_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and loggerDefinitionId =
         flag "logger-definition-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_logger_definition
           (Values.UpdateLoggerDefinitionRequest.make ?name
              ~loggerDefinitionId ())
           (Some Values.UpdateLoggerDefinitionResponse.to_json)
           (Some Values.UpdateLoggerDefinitionResponse.error_to_json)])
let update_resource_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and resourceDefinitionId =
         flag "resource-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_definition
           (Values.UpdateResourceDefinitionRequest.make ?name
              ~resourceDefinitionId ())
           (Some Values.UpdateResourceDefinitionResponse.to_json)
           (Some Values.UpdateResourceDefinitionResponse.error_to_json)])
let update_subscription_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING __string"
       and subscriptionDefinitionId =
         flag "subscription-definition-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscription_definition
           (Values.UpdateSubscriptionDefinitionRequest.make ?name
              ~subscriptionDefinitionId ())
           (Some Values.UpdateSubscriptionDefinitionResponse.to_json)
           (Some Values.UpdateSubscriptionDefinitionResponse.error_to_json)])
let update_thing_runtime_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and telemetryConfiguration =
         flag "telemetry-configuration" (optional json_arg)
           ~doc:"JSON TelemetryConfigurationUpdate"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_thing_runtime_configuration
           (Values.UpdateThingRuntimeConfigurationRequest.make
              ?telemetryConfiguration:(Option.map
                                         ~f:Values.TelemetryConfigurationUpdate.of_json
                                         telemetryConfiguration) ~thingName
              ())
           (Some Values.UpdateThingRuntimeConfigurationResponse.to_json)
           (Some Values.UpdateThingRuntimeConfigurationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-role-to-group", associate_role_to_group);
    ("associate-service-role-to-account", associate_service_role_to_account);
    ("create-connector-definition", create_connector_definition);
    ("create-connector-definition-version",
      create_connector_definition_version);
    ("create-core-definition", create_core_definition);
    ("create-core-definition-version", create_core_definition_version);
    ("create-deployment", create_deployment);
    ("create-device-definition", create_device_definition);
    ("create-device-definition-version", create_device_definition_version);
    ("create-function-definition", create_function_definition);
    ("create-function-definition-version",
      create_function_definition_version);
    ("create-group", create_group);
    ("create-group-certificate-authority",
      create_group_certificate_authority);
    ("create-group-version", create_group_version);
    ("create-logger-definition", create_logger_definition);
    ("create-logger-definition-version", create_logger_definition_version);
    ("create-resource-definition", create_resource_definition);
    ("create-resource-definition-version",
      create_resource_definition_version);
    ("create-software-update-job", create_software_update_job);
    ("create-subscription-definition", create_subscription_definition);
    ("create-subscription-definition-version",
      create_subscription_definition_version);
    ("delete-connector-definition", delete_connector_definition);
    ("delete-core-definition", delete_core_definition);
    ("delete-device-definition", delete_device_definition);
    ("delete-function-definition", delete_function_definition);
    ("delete-group", delete_group);
    ("delete-logger-definition", delete_logger_definition);
    ("delete-resource-definition", delete_resource_definition);
    ("delete-subscription-definition", delete_subscription_definition);
    ("disassociate-role-from-group", disassociate_role_from_group);
    ("disassociate-service-role-from-account",
      disassociate_service_role_from_account);
    ("get-associated-role", get_associated_role);
    ("get-bulk-deployment-status", get_bulk_deployment_status);
    ("get-connectivity-info", get_connectivity_info);
    ("get-connector-definition", get_connector_definition);
    ("get-connector-definition-version", get_connector_definition_version);
    ("get-core-definition", get_core_definition);
    ("get-core-definition-version", get_core_definition_version);
    ("get-deployment-status", get_deployment_status);
    ("get-device-definition", get_device_definition);
    ("get-device-definition-version", get_device_definition_version);
    ("get-function-definition", get_function_definition);
    ("get-function-definition-version", get_function_definition_version);
    ("get-group", get_group);
    ("get-group-certificate-authority", get_group_certificate_authority);
    ("get-group-certificate-configuration",
      get_group_certificate_configuration);
    ("get-group-version", get_group_version);
    ("get-logger-definition", get_logger_definition);
    ("get-logger-definition-version", get_logger_definition_version);
    ("get-resource-definition", get_resource_definition);
    ("get-resource-definition-version", get_resource_definition_version);
    ("get-service-role-for-account", get_service_role_for_account);
    ("get-subscription-definition", get_subscription_definition);
    ("get-subscription-definition-version",
      get_subscription_definition_version);
    ("get-thing-runtime-configuration", get_thing_runtime_configuration);
    ("list-bulk-deployment-detailed-reports",
      list_bulk_deployment_detailed_reports);
    ("list-bulk-deployments", list_bulk_deployments);
    ("list-connector-definition-versions",
      list_connector_definition_versions);
    ("list-connector-definitions", list_connector_definitions);
    ("list-core-definition-versions", list_core_definition_versions);
    ("list-core-definitions", list_core_definitions);
    ("list-deployments", list_deployments);
    ("list-device-definition-versions", list_device_definition_versions);
    ("list-device-definitions", list_device_definitions);
    ("list-function-definition-versions", list_function_definition_versions);
    ("list-function-definitions", list_function_definitions);
    ("list-group-certificate-authorities",
      list_group_certificate_authorities);
    ("list-group-versions", list_group_versions);
    ("list-groups", list_groups);
    ("list-logger-definition-versions", list_logger_definition_versions);
    ("list-logger-definitions", list_logger_definitions);
    ("list-resource-definition-versions", list_resource_definition_versions);
    ("list-resource-definitions", list_resource_definitions);
    ("list-subscription-definition-versions",
      list_subscription_definition_versions);
    ("list-subscription-definitions", list_subscription_definitions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("reset-deployments", reset_deployments);
    ("start-bulk-deployment", start_bulk_deployment);
    ("stop-bulk-deployment", stop_bulk_deployment);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-connectivity-info", update_connectivity_info);
    ("update-connector-definition", update_connector_definition);
    ("update-core-definition", update_core_definition);
    ("update-device-definition", update_device_definition);
    ("update-function-definition", update_function_definition);
    ("update-group", update_group);
    ("update-group-certificate-configuration",
      update_group_certificate_configuration);
    ("update-logger-definition", update_logger_definition);
    ("update-resource-definition", update_resource_definition);
    ("update-subscription-definition", update_subscription_definition);
    ("update-thing-runtime-configuration",
      update_thing_runtime_configuration)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required json_arg)
           ~doc:"JSON CreateNamespaceRequestNamespaceList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_namespace
           (Values.CreateNamespaceRequest.make ~tableBucketARN
              ~namespace:(Values.CreateNamespaceRequestNamespaceList.of_json
                            namespace) ())
           (Some Values.CreateNamespaceResponse.to_json)
           (Some Values.CreateNamespaceResponse.error_to_json)])
let create_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON TableMetadata"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and storageClassConfiguration =
         flag "storage-class-configuration" (optional json_arg)
           ~doc:"JSON StorageClassConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName"
       and format =
         flag "format" (required json_arg) ~doc:"JSON OpenTableFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_table
           (Values.CreateTableRequest.make
              ?metadata:(Option.map ~f:Values.TableMetadata.of_json metadata)
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?storageClassConfiguration:(Option.map
                                            ~f:Values.StorageClassConfiguration.of_json
                                            storageClassConfiguration)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~tableBucketARN
              ~namespace ~name
              ~format:(Values.OpenTableFormat.of_json format) ())
           (Some Values.CreateTableResponse.to_json)
           (Some Values.CreateTableResponse.error_to_json)])
let create_table_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and storageClassConfiguration =
         flag "storage-class-configuration" (optional json_arg)
           ~doc:"JSON StorageClassConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING TableBucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_table_bucket
           (Values.CreateTableBucketRequest.make
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?storageClassConfiguration:(Option.map
                                            ~f:Values.StorageClassConfiguration.of_json
                                            storageClassConfiguration)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ())
           (Some Values.CreateTableBucketResponse.to_json)
           (Some Values.CreateTableBucketResponse.error_to_json)])
let delete_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_namespace
           (Values.DeleteNamespaceRequest.make ~tableBucketARN ~namespace ())
           None None])
let delete_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionToken =
         flag "version-token" (optional string) ~doc:"STRING VersionToken"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table
           (Values.DeleteTableRequest.make ?versionToken ~tableBucketARN
              ~namespace ~name ()) None None])
let delete_table_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_bucket
           (Values.DeleteTableBucketRequest.make ~tableBucketARN ()) None
           None])
let delete_table_bucket_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_bucket_encryption
           (Values.DeleteTableBucketEncryptionRequest.make ~tableBucketARN ())
           None None])
let delete_table_bucket_metrics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_bucket_metrics_configuration
           (Values.DeleteTableBucketMetricsConfigurationRequest.make
              ~tableBucketARN ()) None None])
let delete_table_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_bucket_policy
           (Values.DeleteTableBucketPolicyRequest.make ~tableBucketARN ())
           None None])
let delete_table_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionToken =
         flag "version-token" (optional string) ~doc:"STRING VersionToken"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_bucket_replication
           (Values.DeleteTableBucketReplicationRequest.make ?versionToken
              ~tableBucketARN ()) None None])
let delete_table_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_policy
           (Values.DeleteTablePolicyRequest.make ~tableBucketARN ~namespace
              ~name ()) None None])
let delete_table_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING TableARN"
       and versionToken =
         flag "version-token" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table_replication
           (Values.DeleteTableReplicationRequest.make ~tableArn ~versionToken
              ()) None None])
let get_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_namespace
           (Values.GetNamespaceRequest.make ~tableBucketARN ~namespace ())
           (Some Values.GetNamespaceResponse.to_json)
           (Some Values.GetNamespaceResponse.error_to_json)])
let get_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (optional string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING NamespaceName"
       and name = flag "name" (optional string) ~doc:"STRING TableName"
       and tableArn =
         flag "table-arn" (optional string) ~doc:"STRING TableARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table
           (Values.GetTableRequest.make ?tableBucketARN ?namespace ?name
              ?tableArn ()) (Some Values.GetTableResponse.to_json)
           (Some Values.GetTableResponse.error_to_json)])
let get_table_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_bucket
           (Values.GetTableBucketRequest.make ~tableBucketARN ())
           (Some Values.GetTableBucketResponse.to_json)
           (Some Values.GetTableBucketResponse.error_to_json)])
let get_table_bucket_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_bucket_encryption
           (Values.GetTableBucketEncryptionRequest.make ~tableBucketARN ())
           (Some Values.GetTableBucketEncryptionResponse.to_json)
           (Some Values.GetTableBucketEncryptionResponse.error_to_json)])
let get_table_bucket_maintenance_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_bucket_maintenance_configuration
           (Values.GetTableBucketMaintenanceConfigurationRequest.make
              ~tableBucketARN ())
           (Some
              Values.GetTableBucketMaintenanceConfigurationResponse.to_json)
           (Some
              Values.GetTableBucketMaintenanceConfigurationResponse.error_to_json)])
let get_table_bucket_metrics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_bucket_metrics_configuration
           (Values.GetTableBucketMetricsConfigurationRequest.make
              ~tableBucketARN ())
           (Some Values.GetTableBucketMetricsConfigurationResponse.to_json)
           (Some
              Values.GetTableBucketMetricsConfigurationResponse.error_to_json)])
let get_table_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_bucket_policy
           (Values.GetTableBucketPolicyRequest.make ~tableBucketARN ())
           (Some Values.GetTableBucketPolicyResponse.to_json)
           (Some Values.GetTableBucketPolicyResponse.error_to_json)])
let get_table_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_bucket_replication
           (Values.GetTableBucketReplicationRequest.make ~tableBucketARN ())
           (Some Values.GetTableBucketReplicationResponse.to_json)
           (Some Values.GetTableBucketReplicationResponse.error_to_json)])
let get_table_bucket_storage_class =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_bucket_storage_class
           (Values.GetTableBucketStorageClassRequest.make ~tableBucketARN ())
           (Some Values.GetTableBucketStorageClassResponse.to_json)
           (Some Values.GetTableBucketStorageClassResponse.error_to_json)])
let get_table_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_encryption
           (Values.GetTableEncryptionRequest.make ~tableBucketARN ~namespace
              ~name ()) (Some Values.GetTableEncryptionResponse.to_json)
           (Some Values.GetTableEncryptionResponse.error_to_json)])
let get_table_maintenance_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_maintenance_configuration
           (Values.GetTableMaintenanceConfigurationRequest.make
              ~tableBucketARN ~namespace ~name ())
           (Some Values.GetTableMaintenanceConfigurationResponse.to_json)
           (Some
              Values.GetTableMaintenanceConfigurationResponse.error_to_json)])
let get_table_maintenance_job_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_maintenance_job_status
           (Values.GetTableMaintenanceJobStatusRequest.make ~tableBucketARN
              ~namespace ~name ())
           (Some Values.GetTableMaintenanceJobStatusResponse.to_json)
           (Some Values.GetTableMaintenanceJobStatusResponse.error_to_json)])
let get_table_metadata_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_metadata_location
           (Values.GetTableMetadataLocationRequest.make ~tableBucketARN
              ~namespace ~name ())
           (Some Values.GetTableMetadataLocationResponse.to_json)
           (Some Values.GetTableMetadataLocationResponse.error_to_json)])
let get_table_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_policy
           (Values.GetTablePolicyRequest.make ~tableBucketARN ~namespace
              ~name ()) (Some Values.GetTablePolicyResponse.to_json)
           (Some Values.GetTablePolicyResponse.error_to_json)])
let get_table_record_expiration_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING TableARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_record_expiration_configuration
           (Values.GetTableRecordExpirationConfigurationRequest.make
              ~tableArn ())
           (Some Values.GetTableRecordExpirationConfigurationResponse.to_json)
           (Some
              Values.GetTableRecordExpirationConfigurationResponse.error_to_json)])
let get_table_record_expiration_job_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING TableARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_record_expiration_job_status
           (Values.GetTableRecordExpirationJobStatusRequest.make ~tableArn ())
           (Some Values.GetTableRecordExpirationJobStatusResponse.to_json)
           (Some
              Values.GetTableRecordExpirationJobStatusResponse.error_to_json)])
let get_table_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING TableARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_replication
           (Values.GetTableReplicationRequest.make ~tableArn ())
           (Some Values.GetTableReplicationResponse.to_json)
           (Some Values.GetTableReplicationResponse.error_to_json)])
let get_table_replication_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING TableARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_replication_status
           (Values.GetTableReplicationStatusRequest.make ~tableArn ())
           (Some Values.GetTableReplicationStatusResponse.to_json)
           (Some Values.GetTableReplicationStatusResponse.error_to_json)])
let get_table_storage_class =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_storage_class
           (Values.GetTableStorageClassRequest.make ~tableBucketARN
              ~namespace ~name ())
           (Some Values.GetTableStorageClassResponse.to_json)
           (Some Values.GetTableStorageClassResponse.error_to_json)])
let list_namespaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and prefix =
         flag "prefix" (optional string)
           ~doc:"STRING ListNamespacesRequestPrefixString"
       and continuationToken =
         flag "continuation-token" (optional string) ~doc:"STRING NextToken"
       and maxNamespaces =
         flag "max-namespaces" (optional int) ~doc:"INT ListNamespacesLimit"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_namespaces
           (Values.ListNamespacesRequest.make ?prefix ?continuationToken
              ?maxNamespaces ~tableBucketARN ())
           (Some Values.ListNamespacesResponse.to_json)
           (Some Values.ListNamespacesResponse.error_to_json)])
let list_table_buckets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and prefix =
         flag "prefix" (optional string)
           ~doc:"STRING ListTableBucketsRequestPrefixString"
       and continuationToken =
         flag "continuation-token" (optional string) ~doc:"STRING NextToken"
       and maxBuckets =
         flag "max-buckets" (optional int) ~doc:"INT ListTableBucketsLimit"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON TableBucketType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_table_buckets
           (Values.ListTableBucketsRequest.make ?prefix ?continuationToken
              ?maxBuckets
              ?type_:(Option.map ~f:Values.TableBucketType.of_json type_) ())
           (Some Values.ListTableBucketsResponse.to_json)
           (Some Values.ListTableBucketsResponse.error_to_json)])
let list_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING NamespaceName"
       and prefix =
         flag "prefix" (optional string)
           ~doc:"STRING ListTablesRequestPrefixString"
       and continuationToken =
         flag "continuation-token" (optional string) ~doc:"STRING NextToken"
       and maxTables =
         flag "max-tables" (optional int) ~doc:"INT ListTablesLimit"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tables
           (Values.ListTablesRequest.make ?namespace ?prefix
              ?continuationToken ?maxTables ~tableBucketARN ())
           (Some Values.ListTablesResponse.to_json)
           (Some Values.ListTablesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_table_bucket_encryption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and encryptionConfiguration =
         flag "encryption-configuration" (required json_arg)
           ~doc:"JSON EncryptionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_bucket_encryption
           (Values.PutTableBucketEncryptionRequest.make ~tableBucketARN
              ~encryptionConfiguration:(Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration) ()) None
           None])
let put_table_bucket_maintenance_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and type_ =
         flag "type-" (required json_arg)
           ~doc:"JSON TableBucketMaintenanceType"
       and value =
         flag "value" (required json_arg)
           ~doc:"JSON TableBucketMaintenanceConfigurationValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_bucket_maintenance_configuration
           (Values.PutTableBucketMaintenanceConfigurationRequest.make
              ~tableBucketARN
              ~type_:(Values.TableBucketMaintenanceType.of_json type_)
              ~value:(Values.TableBucketMaintenanceConfigurationValue.of_json
                        value) ()) None None])
let put_table_bucket_metrics_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_bucket_metrics_configuration
           (Values.PutTableBucketMetricsConfigurationRequest.make
              ~tableBucketARN ()) None None])
let put_table_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and resourcePolicy =
         flag "resource-policy" (required string)
           ~doc:"STRING ResourcePolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_bucket_policy
           (Values.PutTableBucketPolicyRequest.make ~tableBucketARN
              ~resourcePolicy ()) None None])
let put_table_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionToken =
         flag "version-token" (optional string) ~doc:"STRING VersionToken"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON TableBucketReplicationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_bucket_replication
           (Values.PutTableBucketReplicationRequest.make ?versionToken
              ~tableBucketARN
              ~configuration:(Values.TableBucketReplicationConfiguration.of_json
                                configuration) ())
           (Some Values.PutTableBucketReplicationResponse.to_json)
           (Some Values.PutTableBucketReplicationResponse.error_to_json)])
let put_table_bucket_storage_class =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and storageClassConfiguration =
         flag "storage-class-configuration" (required json_arg)
           ~doc:"JSON StorageClassConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_bucket_storage_class
           (Values.PutTableBucketStorageClassRequest.make ~tableBucketARN
              ~storageClassConfiguration:(Values.StorageClassConfiguration.of_json
                                            storageClassConfiguration) ())
           None None])
let put_table_maintenance_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON TableMaintenanceType"
       and value =
         flag "value" (required json_arg)
           ~doc:"JSON TableMaintenanceConfigurationValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_maintenance_configuration
           (Values.PutTableMaintenanceConfigurationRequest.make
              ~tableBucketARN ~namespace ~name
              ~type_:(Values.TableMaintenanceType.of_json type_)
              ~value:(Values.TableMaintenanceConfigurationValue.of_json value)
              ()) None None])
let put_table_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName"
       and resourcePolicy =
         flag "resource-policy" (required string)
           ~doc:"STRING ResourcePolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_policy
           (Values.PutTablePolicyRequest.make ~tableBucketARN ~namespace
              ~name ~resourcePolicy ()) None None])
let put_table_record_expiration_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING TableARN"
       and value =
         flag "value" (required json_arg)
           ~doc:"JSON TableRecordExpirationConfigurationValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_record_expiration_configuration
           (Values.PutTableRecordExpirationConfigurationRequest.make
              ~tableArn
              ~value:(Values.TableRecordExpirationConfigurationValue.of_json
                        value) ()) None None])
let put_table_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionToken =
         flag "version-token" (optional string) ~doc:"STRING String"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING TableARN"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON TableReplicationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_table_replication
           (Values.PutTableReplicationRequest.make ?versionToken ~tableArn
              ~configuration:(Values.TableReplicationConfiguration.of_json
                                configuration) ())
           (Some Values.PutTableReplicationResponse.to_json)
           (Some Values.PutTableReplicationResponse.error_to_json)])
let rename_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newNamespaceName =
         flag "new-namespace-name" (optional string)
           ~doc:"STRING NamespaceName"
       and newName =
         flag "new-name" (optional string) ~doc:"STRING TableName"
       and versionToken =
         flag "version-token" (optional string) ~doc:"STRING VersionToken"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rename_table
           (Values.RenameTableRequest.make ?newNamespaceName ?newName
              ?versionToken ~tableBucketARN ~namespace ~name ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_table_metadata_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableBucketARN =
         flag "table-bucket-a-r-n" (required string)
           ~doc:"STRING TableBucketARN"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING NamespaceName"
       and name = flag "name" (required string) ~doc:"STRING TableName"
       and versionToken =
         flag "version-token" (required string) ~doc:"STRING VersionToken"
       and metadataLocation =
         flag "metadata-location" (required string)
           ~doc:"STRING MetadataLocation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table_metadata_location
           (Values.UpdateTableMetadataLocationRequest.make ~tableBucketARN
              ~namespace ~name ~versionToken ~metadataLocation ())
           (Some Values.UpdateTableMetadataLocationResponse.to_json)
           (Some Values.UpdateTableMetadataLocationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-namespace", create_namespace);
    ("create-table", create_table);
    ("create-table-bucket", create_table_bucket);
    ("delete-namespace", delete_namespace);
    ("delete-table", delete_table);
    ("delete-table-bucket", delete_table_bucket);
    ("delete-table-bucket-encryption", delete_table_bucket_encryption);
    ("delete-table-bucket-metrics-configuration",
      delete_table_bucket_metrics_configuration);
    ("delete-table-bucket-policy", delete_table_bucket_policy);
    ("delete-table-bucket-replication", delete_table_bucket_replication);
    ("delete-table-policy", delete_table_policy);
    ("delete-table-replication", delete_table_replication);
    ("get-namespace", get_namespace);
    ("get-table", get_table);
    ("get-table-bucket", get_table_bucket);
    ("get-table-bucket-encryption", get_table_bucket_encryption);
    ("get-table-bucket-maintenance-configuration",
      get_table_bucket_maintenance_configuration);
    ("get-table-bucket-metrics-configuration",
      get_table_bucket_metrics_configuration);
    ("get-table-bucket-policy", get_table_bucket_policy);
    ("get-table-bucket-replication", get_table_bucket_replication);
    ("get-table-bucket-storage-class", get_table_bucket_storage_class);
    ("get-table-encryption", get_table_encryption);
    ("get-table-maintenance-configuration",
      get_table_maintenance_configuration);
    ("get-table-maintenance-job-status", get_table_maintenance_job_status);
    ("get-table-metadata-location", get_table_metadata_location);
    ("get-table-policy", get_table_policy);
    ("get-table-record-expiration-configuration",
      get_table_record_expiration_configuration);
    ("get-table-record-expiration-job-status",
      get_table_record_expiration_job_status);
    ("get-table-replication", get_table_replication);
    ("get-table-replication-status", get_table_replication_status);
    ("get-table-storage-class", get_table_storage_class);
    ("list-namespaces", list_namespaces);
    ("list-table-buckets", list_table_buckets);
    ("list-tables", list_tables);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-table-bucket-encryption", put_table_bucket_encryption);
    ("put-table-bucket-maintenance-configuration",
      put_table_bucket_maintenance_configuration);
    ("put-table-bucket-metrics-configuration",
      put_table_bucket_metrics_configuration);
    ("put-table-bucket-policy", put_table_bucket_policy);
    ("put-table-bucket-replication", put_table_bucket_replication);
    ("put-table-bucket-storage-class", put_table_bucket_storage_class);
    ("put-table-maintenance-configuration",
      put_table_maintenance_configuration);
    ("put-table-policy", put_table_policy);
    ("put-table-record-expiration-configuration",
      put_table_record_expiration_configuration);
    ("put-table-replication", put_table_replication);
    ("rename-table", rename_table);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-table-metadata-location", update_table_metadata_location)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ServiceId"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON ServiceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_service
           (Values.AssociateServiceInput.make ~agentSpaceId ~serviceId
              ~configuration:(Values.ServiceConfiguration.of_json
                                configuration) ())
           (Some Values.AssociateServiceOutput.to_json)
           (Some Values.AssociateServiceOutput.error_to_json)])
let create_agent_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and locale = flag "locale" (optional string) ~doc:"STRING Locale"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING CreateAgentSpaceInputClientTokenString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING AgentSpaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent_space
           (Values.CreateAgentSpaceInput.make ?description ?locale ?kmsKeyArn
              ?clientToken ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~name ()) (Some Values.CreateAgentSpaceOutput.to_json)
           (Some Values.CreateAgentSpaceOutput.error_to_json)])
let create_backlog_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reference =
         flag "reference" (optional json_arg) ~doc:"JSON ReferenceInput"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BacklogTaskDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and taskType =
         flag "task-type" (required json_arg) ~doc:"JSON TaskType"
       and title =
         flag "title" (required string) ~doc:"STRING BacklogTaskTitle"
       and priority =
         flag "priority" (required json_arg) ~doc:"JSON Priority" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backlog_task
           (Values.CreateBacklogTaskRequest.make
              ?reference:(Option.map ~f:Values.ReferenceInput.of_json
                            reference) ?description ?clientToken
              ~agentSpaceId ~taskType:(Values.TaskType.of_json taskType)
              ~title ~priority:(Values.Priority.of_json priority) ())
           (Some Values.CreateBacklogTaskResponse.to_json)
           (Some Values.CreateBacklogTaskResponse.error_to_json)])
let create_chat =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING ResourceId"
       and userType =
         flag "user-type" (optional json_arg) ~doc:"JSON UserType"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_chat
           (Values.CreateChatRequest.make ?userId
              ?userType:(Option.map ~f:Values.UserType.of_json userType)
              ~agentSpaceId ()) (Some Values.CreateChatResponse.to_json)
           (Some Values.CreateChatResponse.error_to_json)])
let create_private_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name =
         flag "name" (required string) ~doc:"STRING PrivateConnectionName"
       and mode =
         flag "mode" (required json_arg) ~doc:"JSON PrivateConnectionMode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_private_connection
           (Values.CreatePrivateConnectionInput.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~mode:(Values.PrivateConnectionMode.of_json mode) ())
           (Some Values.CreatePrivateConnectionOutput.to_json)
           (Some Values.CreatePrivateConnectionOutput.error_to_json)])
let delete_agent_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent_space
           (Values.DeleteAgentSpaceInput.make ~agentSpaceId ())
           (Some Values.DeleteAgentSpaceOutput.to_json)
           (Some Values.DeleteAgentSpaceOutput.error_to_json)])
let delete_private_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING PrivateConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_private_connection
           (Values.DeletePrivateConnectionInput.make ~name ())
           (Some Values.DeletePrivateConnectionOutput.to_json)
           (Some Values.DeletePrivateConnectionOutput.error_to_json)])
let deregister_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ServiceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_service
           (Values.DeregisterServiceInput.make ~serviceId ())
           (Some Values.DeregisterServiceOutput.to_json)
           (Some Values.DeregisterServiceOutput.error_to_json)])
let describe_private_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING PrivateConnectionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_private_connection
           (Values.DescribePrivateConnectionInput.make ~name ())
           (Some Values.DescribePrivateConnectionOutput.to_json)
           (Some Values.DescribePrivateConnectionOutput.error_to_json)])
let disable_operator_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authFlow =
         flag "auth-flow" (optional json_arg) ~doc:"JSON AuthFlow"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_operator_app
           (Values.DisableOperatorAppInput.make
              ?authFlow:(Option.map ~f:Values.AuthFlow.of_json authFlow)
              ~agentSpaceId ()) None None])
let disassociate_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_service
           (Values.DisassociateServiceInput.make ~agentSpaceId ~associationId
              ()) (Some Values.DisassociateServiceOutput.to_json)
           (Some Values.DisassociateServiceOutput.error_to_json)])
let enable_operator_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idcInstanceArn =
         flag "idc-instance-arn" (optional string) ~doc:"STRING String"
       and issuerUrl =
         flag "issuer-url" (optional string) ~doc:"STRING String"
       and idpClientId =
         flag "idp-client-id" (optional string) ~doc:"STRING IdpClientId"
       and idpClientSecret =
         flag "idp-client-secret" (optional string)
           ~doc:"STRING IdpClientSecret"
       and provider = flag "provider" (optional string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and authFlow =
         flag "auth-flow" (required json_arg) ~doc:"JSON AuthFlow"
       and operatorAppRoleArn =
         flag "operator-app-role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_operator_app
           (Values.EnableOperatorAppInput.make ?idcInstanceArn ?issuerUrl
              ?idpClientId ?idpClientSecret ?provider ~agentSpaceId
              ~authFlow:(Values.AuthFlow.of_json authFlow)
              ~operatorAppRoleArn ())
           (Some Values.EnableOperatorAppOutput.to_json)
           (Some Values.EnableOperatorAppOutput.error_to_json)])
let get_account_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_usage (Values.GetAccountUsageInput.make ())
           (Some Values.GetAccountUsageOutput.to_json)
           (Some Values.GetAccountUsageOutput.error_to_json)])
let get_agent_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent_space
           (Values.GetAgentSpaceInput.make ~agentSpaceId ())
           (Some Values.GetAgentSpaceOutput.to_json)
           (Some Values.GetAgentSpaceOutput.error_to_json)])
let get_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_association
           (Values.GetAssociationInput.make ~agentSpaceId ~associationId ())
           (Some Values.GetAssociationOutput.to_json)
           (Some Values.GetAssociationOutput.error_to_json)])
let get_backlog_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and taskId = flag "task-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_backlog_task
           (Values.GetBacklogTaskRequest.make ~agentSpaceId ~taskId ())
           (Some Values.GetBacklogTaskResponse.to_json)
           (Some Values.GetBacklogTaskResponse.error_to_json)])
let get_operator_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_operator_app
           (Values.GetOperatorAppInput.make ~agentSpaceId ())
           (Some Values.GetOperatorAppOutput.to_json)
           (Some Values.GetOperatorAppOutput.error_to_json)])
let get_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recommendationVersion =
         flag "recommendation-version" (optional json_arg) ~doc:"JSON Long"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and recommendationId =
         flag "recommendation-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recommendation
           (Values.GetRecommendationRequest.make
              ?recommendationVersion:(Option.map ~f:Values.Long.of_json
                                        recommendationVersion) ~agentSpaceId
              ~recommendationId ())
           (Some Values.GetRecommendationResponse.to_json)
           (Some Values.GetRecommendationResponse.error_to_json)])
let get_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceId =
         flag "service-id" (required string) ~doc:"STRING ServiceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service (Values.GetServiceInput.make ~serviceId ())
           (Some Values.GetServiceOutput.to_json)
           (Some Values.GetServiceOutput.error_to_json)])
let list_agent_spaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAgentSpacesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_spaces
           (Values.ListAgentSpacesInput.make ?maxResults ?nextToken ())
           (Some Values.ListAgentSpacesOutput.to_json)
           (Some Values.ListAgentSpacesOutput.error_to_json)])
let list_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAssociationsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filterServiceTypes =
         flag "filter-service-types" (optional string)
           ~doc:"STRING ListAssociationsInputFilterServiceTypesString"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associations
           (Values.ListAssociationsInput.make ?maxResults ?nextToken
              ?filterServiceTypes ~agentSpaceId ())
           (Some Values.ListAssociationsOutput.to_json)
           (Some Values.ListAssociationsOutput.error_to_json)])
let list_backlog_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON TaskFilter"
       and limit =
         flag "limit" (optional int)
           ~doc:"INT ListBacklogTasksRequestLimitInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sortField =
         flag "sort-field" (optional json_arg) ~doc:"JSON TaskSortField"
       and order = flag "order" (optional json_arg) ~doc:"JSON TaskSortOrder"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_backlog_tasks
           (Values.ListBacklogTasksRequest.make
              ?filter:(Option.map ~f:Values.TaskFilter.of_json filter) ?limit
              ?nextToken
              ?sortField:(Option.map ~f:Values.TaskSortField.of_json
                            sortField)
              ?order:(Option.map ~f:Values.TaskSortOrder.of_json order)
              ~agentSpaceId ())
           (Some Values.ListBacklogTasksResponse.to_json)
           (Some Values.ListBacklogTasksResponse.error_to_json)])
let list_chats =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING ResourceId"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListChatsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_chats
           (Values.ListChatsRequest.make ?userId ?maxResults ?nextToken
              ~agentSpaceId ()) (Some Values.ListChatsResponse.to_json)
           (Some Values.ListChatsResponse.error_to_json)])
let list_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and taskId = flag "task-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_executions
           (Values.ListExecutionsRequest.make ?limit ?nextToken ~agentSpaceId
              ~taskId ()) (Some Values.ListExecutionsResponse.to_json)
           (Some Values.ListExecutionsResponse.error_to_json)])
let list_goals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status = flag "status" (optional json_arg) ~doc:"JSON GoalStatus"
       and goalType =
         flag "goal-type" (optional json_arg) ~doc:"JSON GoalType"
       and limit =
         flag "limit" (optional int) ~doc:"INT ListGoalsRequestLimitInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_goals
           (Values.ListGoalsRequest.make
              ?status:(Option.map ~f:Values.GoalStatus.of_json status)
              ?goalType:(Option.map ~f:Values.GoalType.of_json goalType)
              ?limit ?nextToken ~agentSpaceId ())
           (Some Values.ListGoalsResponse.to_json)
           (Some Values.ListGoalsResponse.error_to_json)])
let list_journal_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit =
         flag "limit" (optional int)
           ~doc:"INT ListJournalRecordsRequestLimitInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and recordType =
         flag "record-type" (optional string) ~doc:"STRING String"
       and order = flag "order" (optional json_arg) ~doc:"JSON OrderType"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_journal_records
           (Values.ListJournalRecordsRequest.make ?limit ?nextToken
              ?recordType
              ?order:(Option.map ~f:Values.OrderType.of_json order)
              ~agentSpaceId ~executionId ())
           (Some Values.ListJournalRecordsResponse.to_json)
           (Some Values.ListJournalRecordsResponse.error_to_json)])
let list_pending_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pending_messages
           (Values.ListPendingMessagesRequest.make ~agentSpaceId ~executionId
              ()) (Some Values.ListPendingMessagesResponse.to_json)
           (Some Values.ListPendingMessagesResponse.error_to_json)])
let list_private_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_private_connections
           (Values.ListPrivateConnectionsInput.make ())
           (Some Values.ListPrivateConnectionsOutput.to_json)
           (Some Values.ListPrivateConnectionsOutput.error_to_json)])
let list_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId = flag "task-id" (optional string) ~doc:"STRING ResourceId"
       and goalId = flag "goal-id" (optional string) ~doc:"STRING ResourceId"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON RecommendationStatus"
       and priority =
         flag "priority" (optional json_arg)
           ~doc:"JSON RecommendationPriority"
       and limit =
         flag "limit" (optional int)
           ~doc:"INT ListRecommendationsRequestLimitInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendations
           (Values.ListRecommendationsRequest.make ?taskId ?goalId
              ?status:(Option.map ~f:Values.RecommendationStatus.of_json
                         status)
              ?priority:(Option.map ~f:Values.RecommendationPriority.of_json
                           priority) ?limit ?nextToken ~agentSpaceId ())
           (Some Values.ListRecommendationsResponse.to_json)
           (Some Values.ListRecommendationsResponse.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServicesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filterServiceType =
         flag "filter-service-type" (optional json_arg) ~doc:"JSON Service" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesInput.make ?maxResults ?nextToken
              ?filterServiceType:(Option.map ~f:Values.Service.of_json
                                    filterServiceType) ())
           (Some Values.ListServicesOutput.to_json)
           (Some Values.ListServicesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ListTagsForResourceRequestResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_webhooks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_webhooks
           (Values.ListWebhooksInput.make ~agentSpaceId ~associationId ())
           (Some Values.ListWebhooksOutput.to_json)
           (Some Values.ListWebhooksOutput.error_to_json)])
let register_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and privateConnectionName =
         flag "private-connection-name" (optional string)
           ~doc:"STRING PrivateConnectionName"
       and name = flag "name" (optional string) ~doc:"STRING ServiceName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and service =
         flag "service" (required json_arg)
           ~doc:"JSON PostRegisterServiceSupportedService"
       and serviceDetails =
         flag "service-details" (required json_arg)
           ~doc:"JSON ServiceDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_service
           (Values.RegisterServiceInput.make ?kmsKeyArn
              ?privateConnectionName ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~service:(Values.PostRegisterServiceSupportedService.of_json
                          service)
              ~serviceDetails:(Values.ServiceDetails.of_json serviceDetails)
              ()) (Some Values.RegisterServiceOutput.to_json)
           (Some Values.RegisterServiceOutput.error_to_json)])
let send_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and context =
         flag "context" (optional json_arg) ~doc:"JSON SendMessageContext"
       and userId = flag "user-id" (optional string) ~doc:"STRING ResourceId"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ResourceId"
       and content =
         flag "content" (required string) ~doc:"STRING MessageContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_message
           (Values.SendMessageRequest.make
              ?context:(Option.map ~f:Values.SendMessageContext.of_json
                          context) ?userId ~agentSpaceId ~executionId
              ~content ()) (Some Values.SendMessageResponse.to_json)
           (Some Values.SendMessageResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TagResourceRequestResourceArnString"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING UntagResourceRequestResourceArnString"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_agent_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AgentSpaceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and locale = flag "locale" (optional string) ~doc:"STRING Locale"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_space
           (Values.UpdateAgentSpaceInput.make ?name ?description ?locale
              ~agentSpaceId ()) (Some Values.UpdateAgentSpaceOutput.to_json)
           (Some Values.UpdateAgentSpaceOutput.error_to_json)])
let update_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON ServiceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_association
           (Values.UpdateAssociationInput.make ~agentSpaceId ~associationId
              ~configuration:(Values.ServiceConfiguration.of_json
                                configuration) ())
           (Some Values.UpdateAssociationOutput.to_json)
           (Some Values.UpdateAssociationOutput.error_to_json)])
let update_backlog_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskStatus =
         flag "task-status" (optional json_arg) ~doc:"JSON TaskStatus"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and taskId = flag "task-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_backlog_task
           (Values.UpdateBacklogTaskRequest.make
              ?taskStatus:(Option.map ~f:Values.TaskStatus.of_json taskStatus)
              ?clientToken ~agentSpaceId ~taskId ())
           (Some Values.UpdateBacklogTaskResponse.to_json)
           (Some Values.UpdateBacklogTaskResponse.error_to_json)])
let update_goal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and evaluationSchedule =
         flag "evaluation-schedule" (optional json_arg)
           ~doc:"JSON GoalScheduleInput"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and goalId = flag "goal-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_goal
           (Values.UpdateGoalRequest.make
              ?evaluationSchedule:(Option.map
                                     ~f:Values.GoalScheduleInput.of_json
                                     evaluationSchedule) ?clientToken
              ~agentSpaceId ~goalId ())
           (Some Values.UpdateGoalResponse.to_json)
           (Some Values.UpdateGoalResponse.error_to_json)])
let update_operator_app_idp_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idpClientSecret =
         flag "idp-client-secret" (optional string)
           ~doc:"STRING IdpClientSecret"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_operator_app_idp_config
           (Values.UpdateOperatorAppIdpConfigInput.make ?idpClientSecret
              ~agentSpaceId ())
           (Some Values.UpdateOperatorAppIdpConfigOutput.to_json)
           (Some Values.UpdateOperatorAppIdpConfigOutput.error_to_json)])
let update_private_connection_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING PrivateConnectionName"
       and certificate =
         flag "certificate" (required string) ~doc:"STRING CertificateString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_private_connection_certificate
           (Values.UpdatePrivateConnectionCertificateInput.make ~name
              ~certificate ())
           (Some Values.UpdatePrivateConnectionCertificateOutput.to_json)
           (Some
              Values.UpdatePrivateConnectionCertificateOutput.error_to_json)])
let update_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON RecommendationStatus"
       and additionalContext =
         flag "additional-context" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and recommendationId =
         flag "recommendation-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_recommendation
           (Values.UpdateRecommendationRequest.make
              ?status:(Option.map ~f:Values.RecommendationStatus.of_json
                         status) ?additionalContext ?clientToken
              ~agentSpaceId ~recommendationId ())
           (Some Values.UpdateRecommendationResponse.to_json)
           (Some Values.UpdateRecommendationResponse.error_to_json)])
let validate_aws_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_aws_associations
           (Values.ValidateAwsAssociationsInput.make ~agentSpaceId ())
           (Some Values.ValidateAwsAssociationsOutput.to_json)
           (Some Values.ValidateAwsAssociationsOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-service", associate_service);
    ("create-agent-space", create_agent_space);
    ("create-backlog-task", create_backlog_task);
    ("create-chat", create_chat);
    ("create-private-connection", create_private_connection);
    ("delete-agent-space", delete_agent_space);
    ("delete-private-connection", delete_private_connection);
    ("deregister-service", deregister_service);
    ("describe-private-connection", describe_private_connection);
    ("disable-operator-app", disable_operator_app);
    ("disassociate-service", disassociate_service);
    ("enable-operator-app", enable_operator_app);
    ("get-account-usage", get_account_usage);
    ("get-agent-space", get_agent_space);
    ("get-association", get_association);
    ("get-backlog-task", get_backlog_task);
    ("get-operator-app", get_operator_app);
    ("get-recommendation", get_recommendation);
    ("get-service", get_service);
    ("list-agent-spaces", list_agent_spaces);
    ("list-associations", list_associations);
    ("list-backlog-tasks", list_backlog_tasks);
    ("list-chats", list_chats);
    ("list-executions", list_executions);
    ("list-goals", list_goals);
    ("list-journal-records", list_journal_records);
    ("list-pending-messages", list_pending_messages);
    ("list-private-connections", list_private_connections);
    ("list-recommendations", list_recommendations);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-webhooks", list_webhooks);
    ("register-service", register_service);
    ("send-message", send_message);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-agent-space", update_agent_space);
    ("update-association", update_association);
    ("update-backlog-task", update_backlog_task);
    ("update-goal", update_goal);
    ("update-operator-app-idp-config", update_operator_app_idp_config);
    ("update-private-connection-certificate",
      update_private_connection_certificate);
    ("update-recommendation", update_recommendation);
    ("validate-aws-associations", validate_aws_associations)]
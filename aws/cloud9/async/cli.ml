(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_environment_e_c2 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING EnvironmentDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and subnetId =
         flag "subnet-id" (optional string) ~doc:"STRING SubnetId"
       and imageId = flag "image-id" (optional string) ~doc:"STRING ImageId"
       and automaticStopTimeMinutes =
         flag "automatic-stop-time-minutes" (optional int)
           ~doc:"INT AutomaticStopTimeMinutes"
       and ownerArn =
         flag "owner-arn" (optional string) ~doc:"STRING UserArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and connectionType =
         flag "connection-type" (optional json_arg)
           ~doc:"JSON ConnectionType"
       and dryRun =
         flag "dry-run" (optional bool) ~doc:"BOOL NullableBoolean"
       and name = flag "name" (required string) ~doc:"STRING EnvironmentName"
       and instanceType =
         flag "instance-type" (required string) ~doc:"STRING InstanceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_e_c2
           (Values.CreateEnvironmentEC2Request.make ?description
              ?clientRequestToken ?subnetId ?imageId
              ?automaticStopTimeMinutes ?ownerArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?connectionType:(Option.map ~f:Values.ConnectionType.of_json
                                 connectionType) ?dryRun ~name ~instanceType
              ()) (Some Values.CreateEnvironmentEC2Result.to_json)
           (Some Values.CreateEnvironmentEC2Result.error_to_json)])
let create_environment_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn"
       and permissions =
         flag "permissions" (required json_arg) ~doc:"JSON MemberPermissions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment_membership
           (Values.CreateEnvironmentMembershipRequest.make ~environmentId
              ~userArn
              ~permissions:(Values.MemberPermissions.of_json permissions) ())
           (Some Values.CreateEnvironmentMembershipResult.to_json)
           (Some Values.CreateEnvironmentMembershipResult.error_to_json)])
let delete_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment
           (Values.DeleteEnvironmentRequest.make ~environmentId ())
           (Some Values.DeleteEnvironmentResult.to_json)
           (Some Values.DeleteEnvironmentResult.error_to_json)])
let delete_environment_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment_membership
           (Values.DeleteEnvironmentMembershipRequest.make ~environmentId
              ~userArn ())
           (Some Values.DeleteEnvironmentMembershipResult.to_json)
           (Some Values.DeleteEnvironmentMembershipResult.error_to_json)])
let describe_environment_memberships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userArn = flag "user-arn" (optional string) ~doc:"STRING UserArn"
       and environmentId =
         flag "environment-id" (optional string) ~doc:"STRING EnvironmentId"
       and permissions =
         flag "permissions" (optional json_arg) ~doc:"JSON PermissionsList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_environment_memberships
           (Values.DescribeEnvironmentMembershipsRequest.make ?userArn
              ?environmentId
              ?permissions:(Option.map ~f:Values.PermissionsList.of_json
                              permissions) ?nextToken ?maxResults ())
           (Some Values.DescribeEnvironmentMembershipsResult.to_json)
           (Some Values.DescribeEnvironmentMembershipsResult.error_to_json)])
let describe_environment_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_environment_status
           (Values.DescribeEnvironmentStatusRequest.make ~environmentId ())
           (Some Values.DescribeEnvironmentStatusResult.to_json)
           (Some Values.DescribeEnvironmentStatusResult.error_to_json)])
let describe_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentIds =
         flag "environment-ids" (required json_arg)
           ~doc:"JSON BoundedEnvironmentIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_environments
           (Values.DescribeEnvironmentsRequest.make
              ~environmentIds:(Values.BoundedEnvironmentIdList.of_json
                                 environmentIds) ())
           (Some Values.DescribeEnvironmentsResult.to_json)
           (Some Values.DescribeEnvironmentsResult.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListEnvironmentsResult.to_json)
           (Some Values.ListEnvironmentsResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING EnvironmentArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING EnvironmentArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING EnvironmentArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING EnvironmentName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING EnvironmentDescription"
       and managedCredentialsAction =
         flag "managed-credentials-action" (optional json_arg)
           ~doc:"JSON ManagedCredentialsAction"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment
           (Values.UpdateEnvironmentRequest.make ?name ?description
              ?managedCredentialsAction:(Option.map
                                           ~f:Values.ManagedCredentialsAction.of_json
                                           managedCredentialsAction)
              ~environmentId ())
           (Some Values.UpdateEnvironmentResult.to_json)
           (Some Values.UpdateEnvironmentResult.error_to_json)])
let update_environment_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING EnvironmentId"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn"
       and permissions =
         flag "permissions" (required json_arg) ~doc:"JSON MemberPermissions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment_membership
           (Values.UpdateEnvironmentMembershipRequest.make ~environmentId
              ~userArn
              ~permissions:(Values.MemberPermissions.of_json permissions) ())
           (Some Values.UpdateEnvironmentMembershipResult.to_json)
           (Some Values.UpdateEnvironmentMembershipResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-environment-e-c2", create_environment_e_c2);
    ("create-environment-membership", create_environment_membership);
    ("delete-environment", delete_environment);
    ("delete-environment-membership", delete_environment_membership);
    ("describe-environment-memberships", describe_environment_memberships);
    ("describe-environment-status", describe_environment_status);
    ("describe-environments", describe_environments);
    ("list-environments", list_environments);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-environment", update_environment);
    ("update-environment-membership", update_environment_membership)]
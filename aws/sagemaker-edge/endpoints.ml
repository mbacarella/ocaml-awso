(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetDeviceRegistration: (GetDeviceRegistrationRequest.t,
  GetDeviceRegistrationResult.t, GetDeviceRegistrationResult.error) t 
  | SendHeartbeat: (SendHeartbeatRequest.t, unit, unit) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function | GetDeviceRegistration -> `POST | SendHeartbeat -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetDeviceRegistration ->
          (Format.kasprintf Uri.of_string) "/GetDeviceRegistration"
      | SendHeartbeat -> (Format.kasprintf Uri.of_string) "/SendHeartbeat")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetDeviceRegistration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("DeviceName",
                           (DeviceName.to_value
                              req.GetDeviceRegistrationRequest.deviceName));
                      Some
                        ("DeviceFleetName",
                          (DeviceFleetName.to_value
                             req.GetDeviceRegistrationRequest.deviceFleetName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SendHeartbeat ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.SendHeartbeatRequest.agentMetrics
                         ~f:(fun x ->
                               ("AgentMetrics", (EdgeMetrics.to_value x)));
                      Option.map req.SendHeartbeatRequest.models
                        ~f:(fun x -> ("Models", (Models.to_value x)));
                      Some
                        ("AgentVersion",
                          (Version.to_value
                             req.SendHeartbeatRequest.agentVersion));
                      Some
                        ("DeviceName",
                          (DeviceName.to_value
                             req.SendHeartbeatRequest.deviceName));
                      Some
                        ("DeviceFleetName",
                          (DeviceFleetName.to_value
                             req.SendHeartbeatRequest.deviceFleetName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | GetDeviceRegistration ->
      (match resp with
       | Error err ->
           handle_error err (Some GetDeviceRegistrationResult.error_of_json)
       | Ok resp ->
           Ok (GetDeviceRegistrationResult.of_json (response_to_json resp)))
  | SendHeartbeat -> Ok ()
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_device_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceName =
         flag "device-name" (required string) ~doc:"STRING DeviceName"
       and deviceFleetName =
         flag "device-fleet-name" (required string)
           ~doc:"STRING DeviceFleetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_registration
           (Values.GetDeviceRegistrationRequest.make ~deviceName
              ~deviceFleetName ())
           (Some Values.GetDeviceRegistrationResult.to_json)
           (Some Values.GetDeviceRegistrationResult.error_to_json)])
let send_heartbeat =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentMetrics =
         flag "agent-metrics" (optional json_arg) ~doc:"JSON EdgeMetrics"
       and models = flag "models" (optional json_arg) ~doc:"JSON Models"
       and agentVersion =
         flag "agent-version" (required string) ~doc:"STRING Version"
       and deviceName =
         flag "device-name" (required string) ~doc:"STRING DeviceName"
       and deviceFleetName =
         flag "device-fleet-name" (required string)
           ~doc:"STRING DeviceFleetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_heartbeat
           (Values.SendHeartbeatRequest.make
              ?agentMetrics:(Option.map ~f:Values.EdgeMetrics.of_json
                               agentMetrics)
              ?models:(Option.map ~f:Values.Models.of_json models)
              ~agentVersion ~deviceName ~deviceFleetName ()) None None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("get-device-registration", get_device_registration);
    ("send-heartbeat", send_heartbeat)]
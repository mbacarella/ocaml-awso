(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyTokenString"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and loggingConfiguration =
         flag "logging-configuration" (optional json_arg)
           ~doc:"JSON LoggingConfiguration"
       and engineVersion =
         flag "engine-version" (optional int) ~doc:"INT EngineVersion"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and triggerMode =
         flag "trigger-mode" (optional string) ~doc:"STRING GenericString"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and definitionS3Location =
         flag "definition-s3-location" (required json_arg)
           ~doc:"JSON DefinitionS3Location"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workflow
           (Values.CreateWorkflowRequest.make ?clientToken ?description
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?loggingConfiguration:(Option.map
                                       ~f:Values.LoggingConfiguration.of_json
                                       loggingConfiguration) ?engineVersion
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?triggerMode
              ~name
              ~definitionS3Location:(Values.DefinitionS3Location.of_json
                                       definitionS3Location) ~roleArn ())
           (Some Values.CreateWorkflowResponse.to_json)
           (Some Values.CreateWorkflowResponse.error_to_json)])
let delete_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowVersion =
         flag "workflow-version" (optional string)
           ~doc:"STRING WorkflowVersion"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow
           (Values.DeleteWorkflowRequest.make ?workflowVersion ~workflowArn
              ()) (Some Values.DeleteWorkflowResponse.to_json)
           (Some Values.DeleteWorkflowResponse.error_to_json)])
let get_task_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn"
       and taskInstanceId =
         flag "task-instance-id" (required string) ~doc:"STRING IdString"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_task_instance
           (Values.GetTaskInstanceRequest.make ~workflowArn ~taskInstanceId
              ~runId ()) (Some Values.GetTaskInstanceResponse.to_json)
           (Some Values.GetTaskInstanceResponse.error_to_json)])
let get_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowVersion =
         flag "workflow-version" (optional string)
           ~doc:"STRING WorkflowVersion"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow
           (Values.GetWorkflowRequest.make ?workflowVersion ~workflowArn ())
           (Some Values.GetWorkflowResponse.to_json)
           (Some Values.GetWorkflowResponse.error_to_json)])
let get_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_run
           (Values.GetWorkflowRunRequest.make ~workflowArn ~runId ())
           (Some Values.GetWorkflowRunResponse.to_json)
           (Some Values.GetWorkflowRunResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_task_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTaskInstancesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_task_instances
           (Values.ListTaskInstancesRequest.make ?maxResults ?nextToken
              ~workflowArn ~runId ())
           (Some Values.ListTaskInstancesResponse.to_json)
           (Some Values.ListTaskInstancesResponse.error_to_json)])
let list_workflow_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkflowRunsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and workflowVersion =
         flag "workflow-version" (optional string) ~doc:"STRING VersionId"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflow_runs
           (Values.ListWorkflowRunsRequest.make ?maxResults ?nextToken
              ?workflowVersion ~workflowArn ())
           (Some Values.ListWorkflowRunsResponse.to_json)
           (Some Values.ListWorkflowRunsResponse.error_to_json)])
let list_workflow_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkflowVersionsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflow_versions
           (Values.ListWorkflowVersionsRequest.make ?maxResults ?nextToken
              ~workflowArn ())
           (Some Values.ListWorkflowVersionsResponse.to_json)
           (Some Values.ListWorkflowVersionsResponse.error_to_json)])
let list_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkflowsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflows
           (Values.ListWorkflowsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListWorkflowsResponse.to_json)
           (Some Values.ListWorkflowsResponse.error_to_json)])
let start_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyTokenString"
       and overrideParameters =
         flag "override-parameters" (optional json_arg) ~doc:"JSON ObjectMap"
       and workflowVersion =
         flag "workflow-version" (optional string) ~doc:"STRING VersionId"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_workflow_run
           (Values.StartWorkflowRunRequest.make ?clientToken
              ?overrideParameters:(Option.map ~f:Values.ObjectMap.of_json
                                     overrideParameters) ?workflowVersion
              ~workflowArn ()) (Some Values.StartWorkflowRunResponse.to_json)
           (Some Values.StartWorkflowRunResponse.error_to_json)])
let stop_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn"
       and runId = flag "run-id" (required string) ~doc:"STRING IdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_workflow_run
           (Values.StopWorkflowRunRequest.make ~workflowArn ~runId ())
           (Some Values.StopWorkflowRunResponse.to_json)
           (Some Values.StopWorkflowRunResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and loggingConfiguration =
         flag "logging-configuration" (optional json_arg)
           ~doc:"JSON LoggingConfiguration"
       and engineVersion =
         flag "engine-version" (optional int) ~doc:"INT EngineVersion"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and triggerMode =
         flag "trigger-mode" (optional string) ~doc:"STRING GenericString"
       and workflowArn =
         flag "workflow-arn" (required string) ~doc:"STRING WorkflowArn"
       and definitionS3Location =
         flag "definition-s3-location" (required json_arg)
           ~doc:"JSON DefinitionS3Location"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workflow
           (Values.UpdateWorkflowRequest.make ?description
              ?loggingConfiguration:(Option.map
                                       ~f:Values.LoggingConfiguration.of_json
                                       loggingConfiguration) ?engineVersion
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration) ?triggerMode
              ~workflowArn
              ~definitionS3Location:(Values.DefinitionS3Location.of_json
                                       definitionS3Location) ~roleArn ())
           (Some Values.UpdateWorkflowResponse.to_json)
           (Some Values.UpdateWorkflowResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-workflow", create_workflow);
    ("delete-workflow", delete_workflow);
    ("get-task-instance", get_task_instance);
    ("get-workflow", get_workflow);
    ("get-workflow-run", get_workflow_run);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-task-instances", list_task_instances);
    ("list-workflow-runs", list_workflow_runs);
    ("list-workflow-versions", list_workflow_versions);
    ("list-workflows", list_workflows);
    ("start-workflow-run", start_workflow_run);
    ("stop-workflow-run", stop_workflow_run);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-workflow", update_workflow)]
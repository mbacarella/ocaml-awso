(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_bridge_outputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING AddBridgeOutputsRequestBridgeArnString"
       and outputs =
         flag "outputs" (required json_arg)
           ~doc:"JSON __listOfAddBridgeOutputRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_bridge_outputs
           (Values.AddBridgeOutputsRequest.make ~bridgeArn
              ~outputs:(Values.Zz__listOfAddBridgeOutputRequest.of_json
                          outputs) ())
           (Some Values.AddBridgeOutputsResponse.to_json)
           (Some Values.AddBridgeOutputsResponse.error_to_json)])
let add_bridge_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bridgeArn =
         flag "bridge-arn" (required string) ~doc:"STRING BridgeArn"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON __listOfAddBridgeSourceRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_bridge_sources
           (Values.AddBridgeSourcesRequest.make ~bridgeArn
              ~sources:(Values.Zz__listOfAddBridgeSourceRequest.of_json
                          sources) ())
           (Some Values.AddBridgeSourcesResponse.to_json)
           (Some Values.AddBridgeSourcesResponse.error_to_json)])
let add_flow_media_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING AddFlowMediaStreamsRequestFlowArnString"
       and mediaStreams =
         flag "media-streams" (required json_arg)
           ~doc:"JSON __listOfAddMediaStreamRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_flow_media_streams
           (Values.AddFlowMediaStreamsRequest.make ~flowArn
              ~mediaStreams:(Values.Zz__listOfAddMediaStreamRequest.of_json
                               mediaStreams) ())
           (Some Values.AddFlowMediaStreamsResponse.to_json)
           (Some Values.AddFlowMediaStreamsResponse.error_to_json)])
let add_flow_outputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING AddFlowOutputsRequestFlowArnString"
       and outputs =
         flag "outputs" (required json_arg)
           ~doc:"JSON __listOfAddOutputRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_flow_outputs
           (Values.AddFlowOutputsRequest.make ~flowArn
              ~outputs:(Values.Zz__listOfAddOutputRequest.of_json outputs) ())
           (Some Values.AddFlowOutputsResponse.to_json)
           (Some Values.AddFlowOutputsResponse.error_to_json)])
let add_flow_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING AddFlowSourcesRequestFlowArnString"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON __listOfSetSourceRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_flow_sources
           (Values.AddFlowSourcesRequest.make ~flowArn
              ~sources:(Values.Zz__listOfSetSourceRequest.of_json sources) ())
           (Some Values.AddFlowSourcesResponse.to_json)
           (Some Values.AddFlowSourcesResponse.error_to_json)])
let add_flow_vpc_interfaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING AddFlowVpcInterfacesRequestFlowArnString"
       and vpcInterfaces =
         flag "vpc-interfaces" (required json_arg)
           ~doc:"JSON __listOfVpcInterfaceRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_flow_vpc_interfaces
           (Values.AddFlowVpcInterfacesRequest.make ~flowArn
              ~vpcInterfaces:(Values.Zz__listOfVpcInterfaceRequest.of_json
                                vpcInterfaces) ())
           (Some Values.AddFlowVpcInterfacesResponse.to_json)
           (Some Values.AddFlowVpcInterfacesResponse.error_to_json)])
let batch_get_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arns =
         flag "arns" (required json_arg)
           ~doc:"JSON BatchGetRouterInputRequestArnsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_router_input
           (Values.BatchGetRouterInputRequest.make
              ~arns:(Values.BatchGetRouterInputRequestArnsList.of_json arns)
              ()) (Some Values.BatchGetRouterInputResponse.to_json)
           (Some Values.BatchGetRouterInputResponse.error_to_json)])
let batch_get_router_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arns =
         flag "arns" (required json_arg)
           ~doc:"JSON BatchGetRouterNetworkInterfaceRequestArnsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_router_network_interface
           (Values.BatchGetRouterNetworkInterfaceRequest.make
              ~arns:(Values.BatchGetRouterNetworkInterfaceRequestArnsList.of_json
                       arns) ())
           (Some Values.BatchGetRouterNetworkInterfaceResponse.to_json)
           (Some Values.BatchGetRouterNetworkInterfaceResponse.error_to_json)])
let batch_get_router_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arns =
         flag "arns" (required json_arg)
           ~doc:"JSON BatchGetRouterOutputRequestArnsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_router_output
           (Values.BatchGetRouterOutputRequest.make
              ~arns:(Values.BatchGetRouterOutputRequestArnsList.of_json arns)
              ()) (Some Values.BatchGetRouterOutputResponse.to_json)
           (Some Values.BatchGetRouterOutputResponse.error_to_json)])
let create_bridge =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and egressGatewayBridge =
         flag "egress-gateway-bridge" (optional json_arg)
           ~doc:"JSON AddEgressGatewayBridgeRequest"
       and ingressGatewayBridge =
         flag "ingress-gateway-bridge" (optional json_arg)
           ~doc:"JSON AddIngressGatewayBridgeRequest"
       and outputs =
         flag "outputs" (optional json_arg)
           ~doc:"JSON __listOfAddBridgeOutputRequest"
       and sourceFailoverConfig =
         flag "source-failover-config" (optional json_arg)
           ~doc:"JSON FailoverConfig"
       and name = flag "name" (required string) ~doc:"STRING String"
       and placementArn =
         flag "placement-arn" (required string) ~doc:"STRING String"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON __listOfAddBridgeSourceRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bridge
           (Values.CreateBridgeRequest.make
              ?egressGatewayBridge:(Option.map
                                      ~f:Values.AddEgressGatewayBridgeRequest.of_json
                                      egressGatewayBridge)
              ?ingressGatewayBridge:(Option.map
                                       ~f:Values.AddIngressGatewayBridgeRequest.of_json
                                       ingressGatewayBridge)
              ?outputs:(Option.map
                          ~f:Values.Zz__listOfAddBridgeOutputRequest.of_json
                          outputs)
              ?sourceFailoverConfig:(Option.map
                                       ~f:Values.FailoverConfig.of_json
                                       sourceFailoverConfig) ~name
              ~placementArn
              ~sources:(Values.Zz__listOfAddBridgeSourceRequest.of_json
                          sources) ())
           (Some Values.CreateBridgeResponse.to_json)
           (Some Values.CreateBridgeResponse.error_to_json)])
let create_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and entitlements =
         flag "entitlements" (optional json_arg)
           ~doc:"JSON __listOfGrantEntitlementRequest"
       and mediaStreams =
         flag "media-streams" (optional json_arg)
           ~doc:"JSON __listOfAddMediaStreamRequest"
       and outputs =
         flag "outputs" (optional json_arg)
           ~doc:"JSON __listOfAddOutputRequest"
       and source =
         flag "source" (optional json_arg) ~doc:"JSON SetSourceRequest"
       and sourceFailoverConfig =
         flag "source-failover-config" (optional json_arg)
           ~doc:"JSON FailoverConfig"
       and sources =
         flag "sources" (optional json_arg)
           ~doc:"JSON __listOfSetSourceRequest"
       and vpcInterfaces =
         flag "vpc-interfaces" (optional json_arg)
           ~doc:"JSON __listOfVpcInterfaceRequest"
       and maintenance =
         flag "maintenance" (optional json_arg) ~doc:"JSON AddMaintenance"
       and sourceMonitoringConfig =
         flag "source-monitoring-config" (optional json_arg)
           ~doc:"JSON MonitoringConfig"
       and flowSize =
         flag "flow-size" (optional json_arg) ~doc:"JSON FlowSize"
       and ndiConfig =
         flag "ndi-config" (optional json_arg) ~doc:"JSON NdiConfig"
       and encodingConfig =
         flag "encoding-config" (optional json_arg)
           ~doc:"JSON EncodingConfig"
       and flowTags =
         flag "flow-tags" (optional json_arg) ~doc:"JSON __mapOfString"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flow
           (Values.CreateFlowRequest.make ?availabilityZone
              ?entitlements:(Option.map
                               ~f:Values.Zz__listOfGrantEntitlementRequest.of_json
                               entitlements)
              ?mediaStreams:(Option.map
                               ~f:Values.Zz__listOfAddMediaStreamRequest.of_json
                               mediaStreams)
              ?outputs:(Option.map
                          ~f:Values.Zz__listOfAddOutputRequest.of_json
                          outputs)
              ?source:(Option.map ~f:Values.SetSourceRequest.of_json source)
              ?sourceFailoverConfig:(Option.map
                                       ~f:Values.FailoverConfig.of_json
                                       sourceFailoverConfig)
              ?sources:(Option.map
                          ~f:Values.Zz__listOfSetSourceRequest.of_json
                          sources)
              ?vpcInterfaces:(Option.map
                                ~f:Values.Zz__listOfVpcInterfaceRequest.of_json
                                vpcInterfaces)
              ?maintenance:(Option.map ~f:Values.AddMaintenance.of_json
                              maintenance)
              ?sourceMonitoringConfig:(Option.map
                                         ~f:Values.MonitoringConfig.of_json
                                         sourceMonitoringConfig)
              ?flowSize:(Option.map ~f:Values.FlowSize.of_json flowSize)
              ?ndiConfig:(Option.map ~f:Values.NdiConfig.of_json ndiConfig)
              ?encodingConfig:(Option.map ~f:Values.EncodingConfig.of_json
                                 encodingConfig)
              ?flowTags:(Option.map ~f:Values.Zz__mapOfString.of_json
                           flowTags) ~name ())
           (Some Values.CreateFlowResponse.to_json)
           (Some Values.CreateFlowResponse.error_to_json)])
let create_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and egressCidrBlocks =
         flag "egress-cidr-blocks" (required json_arg)
           ~doc:"JSON __listOfString"
       and name = flag "name" (required string) ~doc:"STRING String"
       and networks =
         flag "networks" (required json_arg)
           ~doc:"JSON __listOfGatewayNetwork" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gateway
           (Values.CreateGatewayRequest.make
              ~egressCidrBlocks:(Values.Zz__listOfString.of_json
                                   egressCidrBlocks) ~name
              ~networks:(Values.Zz__listOfGatewayNetwork.of_json networks) ())
           (Some Values.CreateGatewayResponse.to_json)
           (Some Values.CreateGatewayResponse.error_to_json)])
let create_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regionName =
         flag "region-name" (optional string) ~doc:"STRING String"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and transitEncryption =
         flag "transit-encryption" (optional json_arg)
           ~doc:"JSON RouterInputTransitEncryption"
       and maintenanceConfiguration =
         flag "maintenance-configuration" (optional json_arg)
           ~doc:"JSON MaintenanceConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOfString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and name =
         flag "name" (required string)
           ~doc:"STRING CreateRouterInputRequestNameString"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON RouterInputConfiguration"
       and maximumBitrate =
         flag "maximum-bitrate" (required json_arg) ~doc:"JSON Long"
       and routingScope =
         flag "routing-scope" (required json_arg) ~doc:"JSON RoutingScope"
       and tier = flag "tier" (required json_arg) ~doc:"JSON RouterInputTier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_router_input
           (Values.CreateRouterInputRequest.make ?regionName
              ?availabilityZone
              ?transitEncryption:(Option.map
                                    ~f:Values.RouterInputTransitEncryption.of_json
                                    transitEncryption)
              ?maintenanceConfiguration:(Option.map
                                           ~f:Values.MaintenanceConfiguration.of_json
                                           maintenanceConfiguration)
              ?tags:(Option.map ~f:Values.Zz__mapOfString.of_json tags)
              ?clientToken ~name
              ~configuration:(Values.RouterInputConfiguration.of_json
                                configuration)
              ~maximumBitrate:(Values.Long.of_json maximumBitrate)
              ~routingScope:(Values.RoutingScope.of_json routingScope)
              ~tier:(Values.RouterInputTier.of_json tier) ())
           (Some Values.CreateRouterInputResponse.to_json)
           (Some Values.CreateRouterInputResponse.error_to_json)])
let create_router_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regionName =
         flag "region-name" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOfString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and name =
         flag "name" (required string)
           ~doc:"STRING CreateRouterNetworkInterfaceRequestNameString"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON RouterNetworkInterfaceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_router_network_interface
           (Values.CreateRouterNetworkInterfaceRequest.make ?regionName
              ?tags:(Option.map ~f:Values.Zz__mapOfString.of_json tags)
              ?clientToken ~name
              ~configuration:(Values.RouterNetworkInterfaceConfiguration.of_json
                                configuration) ())
           (Some Values.CreateRouterNetworkInterfaceResponse.to_json)
           (Some Values.CreateRouterNetworkInterfaceResponse.error_to_json)])
let create_router_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regionName =
         flag "region-name" (optional string) ~doc:"STRING String"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and maintenanceConfiguration =
         flag "maintenance-configuration" (optional json_arg)
           ~doc:"JSON MaintenanceConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOfString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and name =
         flag "name" (required string)
           ~doc:"STRING CreateRouterOutputRequestNameString"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON RouterOutputConfiguration"
       and maximumBitrate =
         flag "maximum-bitrate" (required json_arg) ~doc:"JSON Long"
       and routingScope =
         flag "routing-scope" (required json_arg) ~doc:"JSON RoutingScope"
       and tier =
         flag "tier" (required json_arg) ~doc:"JSON RouterOutputTier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_router_output
           (Values.CreateRouterOutputRequest.make ?regionName
              ?availabilityZone
              ?maintenanceConfiguration:(Option.map
                                           ~f:Values.MaintenanceConfiguration.of_json
                                           maintenanceConfiguration)
              ?tags:(Option.map ~f:Values.Zz__mapOfString.of_json tags)
              ?clientToken ~name
              ~configuration:(Values.RouterOutputConfiguration.of_json
                                configuration)
              ~maximumBitrate:(Values.Long.of_json maximumBitrate)
              ~routingScope:(Values.RoutingScope.of_json routingScope)
              ~tier:(Values.RouterOutputTier.of_json tier) ())
           (Some Values.CreateRouterOutputResponse.to_json)
           (Some Values.CreateRouterOutputResponse.error_to_json)])
let delete_bridge =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING DeleteBridgeRequestBridgeArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bridge (Values.DeleteBridgeRequest.make ~bridgeArn ())
           (Some Values.DeleteBridgeResponse.to_json)
           (Some Values.DeleteBridgeResponse.error_to_json)])
let delete_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING DeleteFlowRequestFlowArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flow (Values.DeleteFlowRequest.make ~flowArn ())
           (Some Values.DeleteFlowResponse.to_json)
           (Some Values.DeleteFlowResponse.error_to_json)])
let delete_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayArn =
         flag "gateway-arn" (required string)
           ~doc:"STRING DeleteGatewayRequestGatewayArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway
           (Values.DeleteGatewayRequest.make ~gatewayArn ())
           (Some Values.DeleteGatewayResponse.to_json)
           (Some Values.DeleteGatewayResponse.error_to_json)])
let delete_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterInputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_router_input
           (Values.DeleteRouterInputRequest.make ~arn ())
           (Some Values.DeleteRouterInputResponse.to_json)
           (Some Values.DeleteRouterInputResponse.error_to_json)])
let delete_router_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING RouterNetworkInterfaceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_router_network_interface
           (Values.DeleteRouterNetworkInterfaceRequest.make ~arn ())
           (Some Values.DeleteRouterNetworkInterfaceResponse.to_json)
           (Some Values.DeleteRouterNetworkInterfaceResponse.error_to_json)])
let delete_router_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterOutputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_router_output
           (Values.DeleteRouterOutputRequest.make ~arn ())
           (Some Values.DeleteRouterOutputResponse.to_json)
           (Some Values.DeleteRouterOutputResponse.error_to_json)])
let deregister_gateway_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and gatewayInstanceArn =
         flag "gateway-instance-arn" (required string)
           ~doc:"STRING DeregisterGatewayInstanceRequestGatewayInstanceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_gateway_instance
           (Values.DeregisterGatewayInstanceRequest.make ?force
              ~gatewayInstanceArn ())
           (Some Values.DeregisterGatewayInstanceResponse.to_json)
           (Some Values.DeregisterGatewayInstanceResponse.error_to_json)])
let describe_bridge =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING DescribeBridgeRequestBridgeArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bridge
           (Values.DescribeBridgeRequest.make ~bridgeArn ())
           (Some Values.DescribeBridgeResponse.to_json)
           (Some Values.DescribeBridgeResponse.error_to_json)])
let describe_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING DescribeFlowRequestFlowArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flow (Values.DescribeFlowRequest.make ~flowArn ())
           (Some Values.DescribeFlowResponse.to_json)
           (Some Values.DescribeFlowResponse.error_to_json)])
let describe_flow_source_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn = flag "flow-arn" (required string) ~doc:"STRING FlowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flow_source_metadata
           (Values.DescribeFlowSourceMetadataRequest.make ~flowArn ())
           (Some Values.DescribeFlowSourceMetadataResponse.to_json)
           (Some Values.DescribeFlowSourceMetadataResponse.error_to_json)])
let describe_flow_source_thumbnail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn = flag "flow-arn" (required string) ~doc:"STRING FlowArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flow_source_thumbnail
           (Values.DescribeFlowSourceThumbnailRequest.make ~flowArn ())
           (Some Values.DescribeFlowSourceThumbnailResponse.to_json)
           (Some Values.DescribeFlowSourceThumbnailResponse.error_to_json)])
let describe_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayArn =
         flag "gateway-arn" (required string)
           ~doc:"STRING DescribeGatewayRequestGatewayArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_gateway
           (Values.DescribeGatewayRequest.make ~gatewayArn ())
           (Some Values.DescribeGatewayResponse.to_json)
           (Some Values.DescribeGatewayResponse.error_to_json)])
let describe_gateway_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayInstanceArn =
         flag "gateway-instance-arn" (required string)
           ~doc:"STRING DescribeGatewayInstanceRequestGatewayInstanceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_gateway_instance
           (Values.DescribeGatewayInstanceRequest.make ~gatewayInstanceArn ())
           (Some Values.DescribeGatewayInstanceResponse.to_json)
           (Some Values.DescribeGatewayInstanceResponse.error_to_json)])
let describe_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offeringArn =
         flag "offering-arn" (required string) ~doc:"STRING OfferingArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_offering
           (Values.DescribeOfferingRequest.make ~offeringArn ())
           (Some Values.DescribeOfferingResponse.to_json)
           (Some Values.DescribeOfferingResponse.error_to_json)])
let describe_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservationArn =
         flag "reservation-arn" (required string)
           ~doc:"STRING ReservationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reservation
           (Values.DescribeReservationRequest.make ~reservationArn ())
           (Some Values.DescribeReservationResponse.to_json)
           (Some Values.DescribeReservationResponse.error_to_json)])
let get_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterInputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_router_input (Values.GetRouterInputRequest.make ~arn ())
           (Some Values.GetRouterInputResponse.to_json)
           (Some Values.GetRouterInputResponse.error_to_json)])
let get_router_input_source_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterInputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_router_input_source_metadata
           (Values.GetRouterInputSourceMetadataRequest.make ~arn ())
           (Some Values.GetRouterInputSourceMetadataResponse.to_json)
           (Some Values.GetRouterInputSourceMetadataResponse.error_to_json)])
let get_router_input_thumbnail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterInputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_router_input_thumbnail
           (Values.GetRouterInputThumbnailRequest.make ~arn ())
           (Some Values.GetRouterInputThumbnailResponse.to_json)
           (Some Values.GetRouterInputThumbnailResponse.error_to_json)])
let get_router_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING RouterNetworkInterfaceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_router_network_interface
           (Values.GetRouterNetworkInterfaceRequest.make ~arn ())
           (Some Values.GetRouterNetworkInterfaceResponse.to_json)
           (Some Values.GetRouterNetworkInterfaceResponse.error_to_json)])
let get_router_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterOutputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_router_output (Values.GetRouterOutputRequest.make ~arn ())
           (Some Values.GetRouterOutputResponse.to_json)
           (Some Values.GetRouterOutputResponse.error_to_json)])
let grant_flow_entitlements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entitlements =
         flag "entitlements" (required json_arg)
           ~doc:"JSON __listOfGrantEntitlementRequest"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING GrantFlowEntitlementsRequestFlowArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.grant_flow_entitlements
           (Values.GrantFlowEntitlementsRequest.make
              ~entitlements:(Values.Zz__listOfGrantEntitlementRequest.of_json
                               entitlements) ~flowArn ())
           (Some Values.GrantFlowEntitlementsResponse.to_json)
           (Some Values.GrantFlowEntitlementsResponse.error_to_json)])
let list_bridges =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterArn =
         flag "filter-arn" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bridges
           (Values.ListBridgesRequest.make ?filterArn ?maxResults ?nextToken
              ()) (Some Values.ListBridgesResponse.to_json)
           (Some Values.ListBridgesResponse.error_to_json)])
let list_entitlements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entitlements
           (Values.ListEntitlementsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListEntitlementsResponse.to_json)
           (Some Values.ListEntitlementsResponse.error_to_json)])
let list_flows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_flows
           (Values.ListFlowsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListFlowsResponse.to_json)
           (Some Values.ListFlowsResponse.error_to_json)])
let list_gateway_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterArn =
         flag "filter-arn" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateway_instances
           (Values.ListGatewayInstancesRequest.make ?filterArn ?maxResults
              ?nextToken ())
           (Some Values.ListGatewayInstancesResponse.to_json)
           (Some Values.ListGatewayInstancesResponse.error_to_json)])
let list_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateways
           (Values.ListGatewaysRequest.make ?maxResults ?nextToken ())
           (Some Values.ListGatewaysResponse.to_json)
           (Some Values.ListGatewaysResponse.error_to_json)])
let list_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_offerings
           (Values.ListOfferingsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListOfferingsResponse.to_json)
           (Some Values.ListOfferingsResponse.error_to_json)])
let list_reservations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reservations
           (Values.ListReservationsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListReservationsResponse.to_json)
           (Some Values.ListReservationsResponse.error_to_json)])
let list_router_inputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRouterInputsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON RouterInputFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_router_inputs
           (Values.ListRouterInputsRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.RouterInputFilterList.of_json
                          filters) ())
           (Some Values.ListRouterInputsResponse.to_json)
           (Some Values.ListRouterInputsResponse.error_to_json)])
let list_router_network_interfaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRouterNetworkInterfacesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON RouterNetworkInterfaceFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_router_network_interfaces
           (Values.ListRouterNetworkInterfacesRequest.make ?maxResults
              ?nextToken
              ?filters:(Option.map
                          ~f:Values.RouterNetworkInterfaceFilterList.of_json
                          filters) ())
           (Some Values.ListRouterNetworkInterfacesResponse.to_json)
           (Some Values.ListRouterNetworkInterfacesResponse.error_to_json)])
let list_router_outputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRouterOutputsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON RouterOutputFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_router_outputs
           (Values.ListRouterOutputsRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.RouterOutputFilterList.of_json
                          filters) ())
           (Some Values.ListRouterOutputsResponse.to_json)
           (Some Values.ListRouterOutputsResponse.error_to_json)])
let list_tags_for_global_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_global_resource
           (Values.ListTagsForGlobalResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForGlobalResourceResponse.to_json)
           (Some Values.ListTagsForGlobalResourceResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let purchase_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offeringArn =
         flag "offering-arn" (required string) ~doc:"STRING String"
       and reservationName =
         flag "reservation-name" (required string) ~doc:"STRING String"
       and start = flag "start" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_offering
           (Values.PurchaseOfferingRequest.make ~offeringArn ~reservationName
              ~start ()) (Some Values.PurchaseOfferingResponse.to_json)
           (Some Values.PurchaseOfferingResponse.error_to_json)])
let remove_bridge_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING RemoveBridgeOutputRequestBridgeArnString"
       and outputName =
         flag "output-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_bridge_output
           (Values.RemoveBridgeOutputRequest.make ~bridgeArn ~outputName ())
           (Some Values.RemoveBridgeOutputResponse.to_json)
           (Some Values.RemoveBridgeOutputResponse.error_to_json)])
let remove_bridge_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING RemoveBridgeSourceRequestBridgeArnString"
       and sourceName =
         flag "source-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_bridge_source
           (Values.RemoveBridgeSourceRequest.make ~bridgeArn ~sourceName ())
           (Some Values.RemoveBridgeSourceResponse.to_json)
           (Some Values.RemoveBridgeSourceResponse.error_to_json)])
let remove_flow_media_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING RemoveFlowMediaStreamRequestFlowArnString"
       and mediaStreamName =
         flag "media-stream-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_flow_media_stream
           (Values.RemoveFlowMediaStreamRequest.make ~flowArn
              ~mediaStreamName ())
           (Some Values.RemoveFlowMediaStreamResponse.to_json)
           (Some Values.RemoveFlowMediaStreamResponse.error_to_json)])
let remove_flow_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING RemoveFlowOutputRequestFlowArnString"
       and outputArn =
         flag "output-arn" (required string)
           ~doc:"STRING RemoveFlowOutputRequestOutputArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_flow_output
           (Values.RemoveFlowOutputRequest.make ~flowArn ~outputArn ())
           (Some Values.RemoveFlowOutputResponse.to_json)
           (Some Values.RemoveFlowOutputResponse.error_to_json)])
let remove_flow_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING RemoveFlowSourceRequestFlowArnString"
       and sourceArn =
         flag "source-arn" (required string)
           ~doc:"STRING RemoveFlowSourceRequestSourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_flow_source
           (Values.RemoveFlowSourceRequest.make ~flowArn ~sourceArn ())
           (Some Values.RemoveFlowSourceResponse.to_json)
           (Some Values.RemoveFlowSourceResponse.error_to_json)])
let remove_flow_vpc_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING RemoveFlowVpcInterfaceRequestFlowArnString"
       and vpcInterfaceName =
         flag "vpc-interface-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_flow_vpc_interface
           (Values.RemoveFlowVpcInterfaceRequest.make ~flowArn
              ~vpcInterfaceName ())
           (Some Values.RemoveFlowVpcInterfaceResponse.to_json)
           (Some Values.RemoveFlowVpcInterfaceResponse.error_to_json)])
let restart_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterInputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restart_router_input
           (Values.RestartRouterInputRequest.make ~arn ())
           (Some Values.RestartRouterInputResponse.to_json)
           (Some Values.RestartRouterInputResponse.error_to_json)])
let restart_router_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterOutputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restart_router_output
           (Values.RestartRouterOutputRequest.make ~arn ())
           (Some Values.RestartRouterOutputResponse.to_json)
           (Some Values.RestartRouterOutputResponse.error_to_json)])
let revoke_flow_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entitlementArn =
         flag "entitlement-arn" (required string)
           ~doc:"STRING RevokeFlowEntitlementRequestEntitlementArnString"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING RevokeFlowEntitlementRequestFlowArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_flow_entitlement
           (Values.RevokeFlowEntitlementRequest.make ~entitlementArn ~flowArn
              ()) (Some Values.RevokeFlowEntitlementResponse.to_json)
           (Some Values.RevokeFlowEntitlementResponse.error_to_json)])
let start_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING StartFlowRequestFlowArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_flow (Values.StartFlowRequest.make ~flowArn ())
           (Some Values.StartFlowResponse.to_json)
           (Some Values.StartFlowResponse.error_to_json)])
let start_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterInputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_router_input
           (Values.StartRouterInputRequest.make ~arn ())
           (Some Values.StartRouterInputResponse.to_json)
           (Some Values.StartRouterInputResponse.error_to_json)])
let start_router_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterOutputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_router_output
           (Values.StartRouterOutputRequest.make ~arn ())
           (Some Values.StartRouterOutputResponse.to_json)
           (Some Values.StartRouterOutputResponse.error_to_json)])
let stop_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING StopFlowRequestFlowArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_flow (Values.StopFlowRequest.make ~flowArn ())
           (Some Values.StopFlowResponse.to_json)
           (Some Values.StopFlowResponse.error_to_json)])
let stop_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterInputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_router_input (Values.StopRouterInputRequest.make ~arn ())
           (Some Values.StopRouterInputResponse.to_json)
           (Some Values.StopRouterInputResponse.error_to_json)])
let stop_router_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING RouterOutputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_router_output
           (Values.StopRouterOutputRequest.make ~arn ())
           (Some Values.StopRouterOutputResponse.to_json)
           (Some Values.StopRouterOutputResponse.error_to_json)])
let tag_global_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON __mapOfString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_global_resource
           (Values.TagGlobalResourceRequest.make ~resourceArn
              ~tags:(Values.Zz__mapOfString.of_json tags) ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON __mapOfString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Zz__mapOfString.of_json tags) ()) None None])
let take_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routerInputArn =
         flag "router-input-arn" (optional string)
           ~doc:"STRING RouterInputArn"
       and routerOutputArn =
         flag "router-output-arn" (required string)
           ~doc:"STRING RouterOutputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.take_router_input
           (Values.TakeRouterInputRequest.make ?routerInputArn
              ~routerOutputArn ())
           (Some Values.TakeRouterInputResponse.to_json)
           (Some Values.TakeRouterInputResponse.error_to_json)])
let untag_global_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOfString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_global_resource
           (Values.UntagGlobalResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOfString.of_json tagKeys) ()) None
           None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOfString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOfString.of_json tagKeys) ()) None
           None])
let update_bridge =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and egressGatewayBridge =
         flag "egress-gateway-bridge" (optional json_arg)
           ~doc:"JSON UpdateEgressGatewayBridgeRequest"
       and ingressGatewayBridge =
         flag "ingress-gateway-bridge" (optional json_arg)
           ~doc:"JSON UpdateIngressGatewayBridgeRequest"
       and sourceFailoverConfig =
         flag "source-failover-config" (optional json_arg)
           ~doc:"JSON UpdateFailoverConfig"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING UpdateBridgeRequestBridgeArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bridge
           (Values.UpdateBridgeRequest.make
              ?egressGatewayBridge:(Option.map
                                      ~f:Values.UpdateEgressGatewayBridgeRequest.of_json
                                      egressGatewayBridge)
              ?ingressGatewayBridge:(Option.map
                                       ~f:Values.UpdateIngressGatewayBridgeRequest.of_json
                                       ingressGatewayBridge)
              ?sourceFailoverConfig:(Option.map
                                       ~f:Values.UpdateFailoverConfig.of_json
                                       sourceFailoverConfig) ~bridgeArn ())
           (Some Values.UpdateBridgeResponse.to_json)
           (Some Values.UpdateBridgeResponse.error_to_json)])
let update_bridge_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkOutput =
         flag "network-output" (optional json_arg)
           ~doc:"JSON UpdateBridgeNetworkOutputRequest"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING UpdateBridgeOutputRequestBridgeArnString"
       and outputName =
         flag "output-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bridge_output
           (Values.UpdateBridgeOutputRequest.make
              ?networkOutput:(Option.map
                                ~f:Values.UpdateBridgeNetworkOutputRequest.of_json
                                networkOutput) ~bridgeArn ~outputName ())
           (Some Values.UpdateBridgeOutputResponse.to_json)
           (Some Values.UpdateBridgeOutputResponse.error_to_json)])
let update_bridge_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowSource =
         flag "flow-source" (optional json_arg)
           ~doc:"JSON UpdateBridgeFlowSourceRequest"
       and networkSource =
         flag "network-source" (optional json_arg)
           ~doc:"JSON UpdateBridgeNetworkSourceRequest"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING UpdateBridgeSourceRequestBridgeArnString"
       and sourceName =
         flag "source-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bridge_source
           (Values.UpdateBridgeSourceRequest.make
              ?flowSource:(Option.map
                             ~f:Values.UpdateBridgeFlowSourceRequest.of_json
                             flowSource)
              ?networkSource:(Option.map
                                ~f:Values.UpdateBridgeNetworkSourceRequest.of_json
                                networkSource) ~bridgeArn ~sourceName ())
           (Some Values.UpdateBridgeSourceResponse.to_json)
           (Some Values.UpdateBridgeSourceResponse.error_to_json)])
let update_bridge_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bridgeArn =
         flag "bridge-arn" (required string)
           ~doc:"STRING UpdateBridgeStateRequestBridgeArnString"
       and desiredState =
         flag "desired-state" (required json_arg) ~doc:"JSON DesiredState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_bridge_state
           (Values.UpdateBridgeStateRequest.make ~bridgeArn
              ~desiredState:(Values.DesiredState.of_json desiredState) ())
           (Some Values.UpdateBridgeStateResponse.to_json)
           (Some Values.UpdateBridgeStateResponse.error_to_json)])
let update_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceFailoverConfig =
         flag "source-failover-config" (optional json_arg)
           ~doc:"JSON UpdateFailoverConfig"
       and maintenance =
         flag "maintenance" (optional json_arg) ~doc:"JSON UpdateMaintenance"
       and sourceMonitoringConfig =
         flag "source-monitoring-config" (optional json_arg)
           ~doc:"JSON MonitoringConfig"
       and ndiConfig =
         flag "ndi-config" (optional json_arg) ~doc:"JSON NdiConfig"
       and flowSize =
         flag "flow-size" (optional json_arg) ~doc:"JSON FlowSize"
       and encodingConfig =
         flag "encoding-config" (optional json_arg)
           ~doc:"JSON EncodingConfig"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING UpdateFlowRequestFlowArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow
           (Values.UpdateFlowRequest.make
              ?sourceFailoverConfig:(Option.map
                                       ~f:Values.UpdateFailoverConfig.of_json
                                       sourceFailoverConfig)
              ?maintenance:(Option.map ~f:Values.UpdateMaintenance.of_json
                              maintenance)
              ?sourceMonitoringConfig:(Option.map
                                         ~f:Values.MonitoringConfig.of_json
                                         sourceMonitoringConfig)
              ?ndiConfig:(Option.map ~f:Values.NdiConfig.of_json ndiConfig)
              ?flowSize:(Option.map ~f:Values.FlowSize.of_json flowSize)
              ?encodingConfig:(Option.map ~f:Values.EncodingConfig.of_json
                                 encodingConfig) ~flowArn ())
           (Some Values.UpdateFlowResponse.to_json)
           (Some Values.UpdateFlowResponse.error_to_json)])
let update_flow_entitlement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and encryption =
         flag "encryption" (optional json_arg) ~doc:"JSON UpdateEncryption"
       and entitlementStatus =
         flag "entitlement-status" (optional json_arg)
           ~doc:"JSON EntitlementStatus"
       and subscribers =
         flag "subscribers" (optional json_arg) ~doc:"JSON __listOfString"
       and entitlementArn =
         flag "entitlement-arn" (required string)
           ~doc:"STRING UpdateFlowEntitlementRequestEntitlementArnString"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING UpdateFlowEntitlementRequestFlowArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow_entitlement
           (Values.UpdateFlowEntitlementRequest.make ?description
              ?encryption:(Option.map ~f:Values.UpdateEncryption.of_json
                             encryption)
              ?entitlementStatus:(Option.map
                                    ~f:Values.EntitlementStatus.of_json
                                    entitlementStatus)
              ?subscribers:(Option.map ~f:Values.Zz__listOfString.of_json
                              subscribers) ~entitlementArn ~flowArn ())
           (Some Values.UpdateFlowEntitlementResponse.to_json)
           (Some Values.UpdateFlowEntitlementResponse.error_to_json)])
let update_flow_media_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON MediaStreamAttributesRequest"
       and clockRate = flag "clock-rate" (optional int) ~doc:"INT Integer"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and mediaStreamType =
         flag "media-stream-type" (optional json_arg)
           ~doc:"JSON MediaStreamType"
       and videoFormat =
         flag "video-format" (optional string) ~doc:"STRING String"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING UpdateFlowMediaStreamRequestFlowArnString"
       and mediaStreamName =
         flag "media-stream-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow_media_stream
           (Values.UpdateFlowMediaStreamRequest.make
              ?attributes:(Option.map
                             ~f:Values.MediaStreamAttributesRequest.of_json
                             attributes) ?clockRate ?description
              ?mediaStreamType:(Option.map ~f:Values.MediaStreamType.of_json
                                  mediaStreamType) ?videoFormat ~flowArn
              ~mediaStreamName ())
           (Some Values.UpdateFlowMediaStreamResponse.to_json)
           (Some Values.UpdateFlowMediaStreamResponse.error_to_json)])
let update_flow_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cidrAllowList =
         flag "cidr-allow-list" (optional json_arg)
           ~doc:"JSON __listOfString"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and destination =
         flag "destination" (optional string) ~doc:"STRING String"
       and encryption =
         flag "encryption" (optional json_arg) ~doc:"JSON UpdateEncryption"
       and maxLatency = flag "max-latency" (optional int) ~doc:"INT Integer"
       and mediaStreamOutputConfigurations =
         flag "media-stream-output-configurations" (optional json_arg)
           ~doc:"JSON __listOfMediaStreamOutputConfigurationRequest"
       and minLatency = flag "min-latency" (optional int) ~doc:"INT Integer"
       and port = flag "port" (optional int) ~doc:"INT Integer"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON Protocol"
       and remoteId = flag "remote-id" (optional string) ~doc:"STRING String"
       and senderControlPort =
         flag "sender-control-port" (optional int) ~doc:"INT Integer"
       and senderIpAddress =
         flag "sender-ip-address" (optional string) ~doc:"STRING String"
       and smoothingLatency =
         flag "smoothing-latency" (optional int) ~doc:"INT Integer"
       and streamId = flag "stream-id" (optional string) ~doc:"STRING String"
       and vpcInterfaceAttachment =
         flag "vpc-interface-attachment" (optional json_arg)
           ~doc:"JSON VpcInterfaceAttachment"
       and outputStatus =
         flag "output-status" (optional json_arg) ~doc:"JSON OutputStatus"
       and ndiProgramName =
         flag "ndi-program-name" (optional string) ~doc:"STRING String"
       and ndiSpeedHqQuality =
         flag "ndi-speed-hq-quality" (optional int) ~doc:"INT Integer"
       and routerIntegrationState =
         flag "router-integration-state" (optional json_arg)
           ~doc:"JSON State"
       and routerIntegrationTransitEncryption =
         flag "router-integration-transit-encryption" (optional json_arg)
           ~doc:"JSON FlowTransitEncryption"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING UpdateFlowOutputRequestFlowArnString"
       and outputArn =
         flag "output-arn" (required string)
           ~doc:"STRING UpdateFlowOutputRequestOutputArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow_output
           (Values.UpdateFlowOutputRequest.make
              ?cidrAllowList:(Option.map ~f:Values.Zz__listOfString.of_json
                                cidrAllowList) ?description ?destination
              ?encryption:(Option.map ~f:Values.UpdateEncryption.of_json
                             encryption) ?maxLatency
              ?mediaStreamOutputConfigurations:(Option.map
                                                  ~f:Values.Zz__listOfMediaStreamOutputConfigurationRequest.of_json
                                                  mediaStreamOutputConfigurations)
              ?minLatency ?port
              ?protocol:(Option.map ~f:Values.Protocol.of_json protocol)
              ?remoteId ?senderControlPort ?senderIpAddress ?smoothingLatency
              ?streamId
              ?vpcInterfaceAttachment:(Option.map
                                         ~f:Values.VpcInterfaceAttachment.of_json
                                         vpcInterfaceAttachment)
              ?outputStatus:(Option.map ~f:Values.OutputStatus.of_json
                               outputStatus) ?ndiProgramName
              ?ndiSpeedHqQuality
              ?routerIntegrationState:(Option.map ~f:Values.State.of_json
                                         routerIntegrationState)
              ?routerIntegrationTransitEncryption:(Option.map
                                                     ~f:Values.FlowTransitEncryption.of_json
                                                     routerIntegrationTransitEncryption)
              ~flowArn ~outputArn ())
           (Some Values.UpdateFlowOutputResponse.to_json)
           (Some Values.UpdateFlowOutputResponse.error_to_json)])
let update_flow_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and decryption =
         flag "decryption" (optional json_arg) ~doc:"JSON UpdateEncryption"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and entitlementArn =
         flag "entitlement-arn" (optional string)
           ~doc:"STRING UpdateFlowSourceRequestEntitlementArnString"
       and ingestPort = flag "ingest-port" (optional int) ~doc:"INT Integer"
       and maxBitrate = flag "max-bitrate" (optional int) ~doc:"INT Integer"
       and maxLatency = flag "max-latency" (optional int) ~doc:"INT Integer"
       and maxSyncBuffer =
         flag "max-sync-buffer" (optional int) ~doc:"INT Integer"
       and mediaStreamSourceConfigurations =
         flag "media-stream-source-configurations" (optional json_arg)
           ~doc:"JSON __listOfMediaStreamSourceConfigurationRequest"
       and minLatency = flag "min-latency" (optional int) ~doc:"INT Integer"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON Protocol"
       and senderControlPort =
         flag "sender-control-port" (optional int) ~doc:"INT Integer"
       and senderIpAddress =
         flag "sender-ip-address" (optional string) ~doc:"STRING String"
       and sourceListenerAddress =
         flag "source-listener-address" (optional string)
           ~doc:"STRING String"
       and sourceListenerPort =
         flag "source-listener-port" (optional int) ~doc:"INT Integer"
       and streamId = flag "stream-id" (optional string) ~doc:"STRING String"
       and vpcInterfaceName =
         flag "vpc-interface-name" (optional string) ~doc:"STRING String"
       and whitelistCidr =
         flag "whitelist-cidr" (optional string) ~doc:"STRING String"
       and gatewayBridgeSource =
         flag "gateway-bridge-source" (optional json_arg)
           ~doc:"JSON UpdateGatewayBridgeSourceRequest"
       and ndiSourceSettings =
         flag "ndi-source-settings" (optional json_arg)
           ~doc:"JSON NdiSourceSettings"
       and routerIntegrationState =
         flag "router-integration-state" (optional json_arg)
           ~doc:"JSON State"
       and routerIntegrationTransitDecryption =
         flag "router-integration-transit-decryption" (optional json_arg)
           ~doc:"JSON FlowTransitEncryption"
       and flowArn =
         flag "flow-arn" (required string)
           ~doc:"STRING UpdateFlowSourceRequestFlowArnString"
       and sourceArn =
         flag "source-arn" (required string)
           ~doc:"STRING UpdateFlowSourceRequestSourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow_source
           (Values.UpdateFlowSourceRequest.make
              ?decryption:(Option.map ~f:Values.UpdateEncryption.of_json
                             decryption) ?description ?entitlementArn
              ?ingestPort ?maxBitrate ?maxLatency ?maxSyncBuffer
              ?mediaStreamSourceConfigurations:(Option.map
                                                  ~f:Values.Zz__listOfMediaStreamSourceConfigurationRequest.of_json
                                                  mediaStreamSourceConfigurations)
              ?minLatency
              ?protocol:(Option.map ~f:Values.Protocol.of_json protocol)
              ?senderControlPort ?senderIpAddress ?sourceListenerAddress
              ?sourceListenerPort ?streamId ?vpcInterfaceName ?whitelistCidr
              ?gatewayBridgeSource:(Option.map
                                      ~f:Values.UpdateGatewayBridgeSourceRequest.of_json
                                      gatewayBridgeSource)
              ?ndiSourceSettings:(Option.map
                                    ~f:Values.NdiSourceSettings.of_json
                                    ndiSourceSettings)
              ?routerIntegrationState:(Option.map ~f:Values.State.of_json
                                         routerIntegrationState)
              ?routerIntegrationTransitDecryption:(Option.map
                                                     ~f:Values.FlowTransitEncryption.of_json
                                                     routerIntegrationTransitDecryption)
              ~flowArn ~sourceArn ())
           (Some Values.UpdateFlowSourceResponse.to_json)
           (Some Values.UpdateFlowSourceResponse.error_to_json)])
let update_gateway_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bridgePlacement =
         flag "bridge-placement" (optional json_arg)
           ~doc:"JSON BridgePlacement"
       and gatewayInstanceArn =
         flag "gateway-instance-arn" (required string)
           ~doc:"STRING UpdateGatewayInstanceRequestGatewayInstanceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_instance
           (Values.UpdateGatewayInstanceRequest.make
              ?bridgePlacement:(Option.map ~f:Values.BridgePlacement.of_json
                                  bridgePlacement) ~gatewayInstanceArn ())
           (Some Values.UpdateGatewayInstanceResponse.to_json)
           (Some Values.UpdateGatewayInstanceResponse.error_to_json)])
let update_router_input =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING UpdateRouterInputRequestNameString"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON RouterInputConfiguration"
       and maximumBitrate =
         flag "maximum-bitrate" (optional json_arg) ~doc:"JSON Long"
       and routingScope =
         flag "routing-scope" (optional json_arg) ~doc:"JSON RoutingScope"
       and tier = flag "tier" (optional json_arg) ~doc:"JSON RouterInputTier"
       and transitEncryption =
         flag "transit-encryption" (optional json_arg)
           ~doc:"JSON RouterInputTransitEncryption"
       and maintenanceConfiguration =
         flag "maintenance-configuration" (optional json_arg)
           ~doc:"JSON MaintenanceConfiguration"
       and arn = flag "arn" (required string) ~doc:"STRING RouterInputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_router_input
           (Values.UpdateRouterInputRequest.make ?name
              ?configuration:(Option.map
                                ~f:Values.RouterInputConfiguration.of_json
                                configuration)
              ?maximumBitrate:(Option.map ~f:Values.Long.of_json
                                 maximumBitrate)
              ?routingScope:(Option.map ~f:Values.RoutingScope.of_json
                               routingScope)
              ?tier:(Option.map ~f:Values.RouterInputTier.of_json tier)
              ?transitEncryption:(Option.map
                                    ~f:Values.RouterInputTransitEncryption.of_json
                                    transitEncryption)
              ?maintenanceConfiguration:(Option.map
                                           ~f:Values.MaintenanceConfiguration.of_json
                                           maintenanceConfiguration) ~arn ())
           (Some Values.UpdateRouterInputResponse.to_json)
           (Some Values.UpdateRouterInputResponse.error_to_json)])
let update_router_network_interface =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING UpdateRouterNetworkInterfaceRequestNameString"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON RouterNetworkInterfaceConfiguration"
       and arn =
         flag "arn" (required string) ~doc:"STRING RouterNetworkInterfaceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_router_network_interface
           (Values.UpdateRouterNetworkInterfaceRequest.make ?name
              ?configuration:(Option.map
                                ~f:Values.RouterNetworkInterfaceConfiguration.of_json
                                configuration) ~arn ())
           (Some Values.UpdateRouterNetworkInterfaceResponse.to_json)
           (Some Values.UpdateRouterNetworkInterfaceResponse.error_to_json)])
let update_router_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING UpdateRouterOutputRequestNameString"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON RouterOutputConfiguration"
       and maximumBitrate =
         flag "maximum-bitrate" (optional json_arg) ~doc:"JSON Long"
       and routingScope =
         flag "routing-scope" (optional json_arg) ~doc:"JSON RoutingScope"
       and tier =
         flag "tier" (optional json_arg) ~doc:"JSON RouterOutputTier"
       and maintenanceConfiguration =
         flag "maintenance-configuration" (optional json_arg)
           ~doc:"JSON MaintenanceConfiguration"
       and arn = flag "arn" (required string) ~doc:"STRING RouterOutputArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_router_output
           (Values.UpdateRouterOutputRequest.make ?name
              ?configuration:(Option.map
                                ~f:Values.RouterOutputConfiguration.of_json
                                configuration)
              ?maximumBitrate:(Option.map ~f:Values.Long.of_json
                                 maximumBitrate)
              ?routingScope:(Option.map ~f:Values.RoutingScope.of_json
                               routingScope)
              ?tier:(Option.map ~f:Values.RouterOutputTier.of_json tier)
              ?maintenanceConfiguration:(Option.map
                                           ~f:Values.MaintenanceConfiguration.of_json
                                           maintenanceConfiguration) ~arn ())
           (Some Values.UpdateRouterOutputResponse.to_json)
           (Some Values.UpdateRouterOutputResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-bridge-outputs", add_bridge_outputs);
    ("add-bridge-sources", add_bridge_sources);
    ("add-flow-media-streams", add_flow_media_streams);
    ("add-flow-outputs", add_flow_outputs);
    ("add-flow-sources", add_flow_sources);
    ("add-flow-vpc-interfaces", add_flow_vpc_interfaces);
    ("batch-get-router-input", batch_get_router_input);
    ("batch-get-router-network-interface",
      batch_get_router_network_interface);
    ("batch-get-router-output", batch_get_router_output);
    ("create-bridge", create_bridge);
    ("create-flow", create_flow);
    ("create-gateway", create_gateway);
    ("create-router-input", create_router_input);
    ("create-router-network-interface", create_router_network_interface);
    ("create-router-output", create_router_output);
    ("delete-bridge", delete_bridge);
    ("delete-flow", delete_flow);
    ("delete-gateway", delete_gateway);
    ("delete-router-input", delete_router_input);
    ("delete-router-network-interface", delete_router_network_interface);
    ("delete-router-output", delete_router_output);
    ("deregister-gateway-instance", deregister_gateway_instance);
    ("describe-bridge", describe_bridge);
    ("describe-flow", describe_flow);
    ("describe-flow-source-metadata", describe_flow_source_metadata);
    ("describe-flow-source-thumbnail", describe_flow_source_thumbnail);
    ("describe-gateway", describe_gateway);
    ("describe-gateway-instance", describe_gateway_instance);
    ("describe-offering", describe_offering);
    ("describe-reservation", describe_reservation);
    ("get-router-input", get_router_input);
    ("get-router-input-source-metadata", get_router_input_source_metadata);
    ("get-router-input-thumbnail", get_router_input_thumbnail);
    ("get-router-network-interface", get_router_network_interface);
    ("get-router-output", get_router_output);
    ("grant-flow-entitlements", grant_flow_entitlements);
    ("list-bridges", list_bridges);
    ("list-entitlements", list_entitlements);
    ("list-flows", list_flows);
    ("list-gateway-instances", list_gateway_instances);
    ("list-gateways", list_gateways);
    ("list-offerings", list_offerings);
    ("list-reservations", list_reservations);
    ("list-router-inputs", list_router_inputs);
    ("list-router-network-interfaces", list_router_network_interfaces);
    ("list-router-outputs", list_router_outputs);
    ("list-tags-for-global-resource", list_tags_for_global_resource);
    ("list-tags-for-resource", list_tags_for_resource);
    ("purchase-offering", purchase_offering);
    ("remove-bridge-output", remove_bridge_output);
    ("remove-bridge-source", remove_bridge_source);
    ("remove-flow-media-stream", remove_flow_media_stream);
    ("remove-flow-output", remove_flow_output);
    ("remove-flow-source", remove_flow_source);
    ("remove-flow-vpc-interface", remove_flow_vpc_interface);
    ("restart-router-input", restart_router_input);
    ("restart-router-output", restart_router_output);
    ("revoke-flow-entitlement", revoke_flow_entitlement);
    ("start-flow", start_flow);
    ("start-router-input", start_router_input);
    ("start-router-output", start_router_output);
    ("stop-flow", stop_flow);
    ("stop-router-input", stop_router_input);
    ("stop-router-output", stop_router_output);
    ("tag-global-resource", tag_global_resource);
    ("tag-resource", tag_resource);
    ("take-router-input", take_router_input);
    ("untag-global-resource", untag_global_resource);
    ("untag-resource", untag_resource);
    ("update-bridge", update_bridge);
    ("update-bridge-output", update_bridge_output);
    ("update-bridge-source", update_bridge_source);
    ("update-bridge-state", update_bridge_state);
    ("update-flow", update_flow);
    ("update-flow-entitlement", update_flow_entitlement);
    ("update-flow-media-stream", update_flow_media_stream);
    ("update-flow-output", update_flow_output);
    ("update-flow-source", update_flow_source);
    ("update-gateway-instance", update_gateway_instance);
    ("update-router-input", update_router_input);
    ("update-router-network-interface", update_router_network_interface);
    ("update-router-output", update_router_output)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.s3outposts
let apiVersion = "2017-07-25"
let endpointPrefix = "s3-outposts"
let serviceFullName = "Amazon S3 on Outposts"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Amazon S3 Outposts"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module NetworkInterfaceId =
  struct
    type nonrec t = string
    let context_ = "NetworkInterfaceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NetworkInterfaceId" j
    let to_json = simple_to_json to_value
  end
module NetworkInterface =
  struct
    type nonrec t =
      {
      networkInterfaceId: NetworkInterfaceId.t option
        [@ocaml.doc "The ID for the network interface."]}
    let make ?networkInterfaceId = fun () -> { networkInterfaceId }
    let to_value x =
      structure_to_value
        [("NetworkInterfaceId",
           (Option.map x.networkInterfaceId ~f:NetworkInterfaceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkInterfaceId =
        (Option.map ~f:NetworkInterfaceId.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaceId") in
      make ?networkInterfaceId ()
    let of_json json =
      let networkInterfaceId =
        field_map json "NetworkInterfaceId" NetworkInterfaceId.of_json in
      make ?networkInterfaceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The container for the network interface."]
module CidrBlock =
  struct
    type nonrec t = string
    let context_ = "CidrBlock"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CidrBlock" j
    let to_json = simple_to_json to_value
  end
module CreationTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module CustomerOwnedIpv4Pool =
  struct
    type nonrec t = string
    let context_ = "CustomerOwnedIpv4Pool"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^ipv4pool-coip-([0-9a-f]{17})$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomerOwnedIpv4Pool" j
    let to_json = simple_to_json to_value
  end
module EndpointAccessType =
  struct
    type nonrec t =
      | Private 
      | CustomerOwnedIp 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Private -> "Private"
      | CustomerOwnedIp -> "CustomerOwnedIp"
      | Non_static_id s -> s
    let of_string =
      function
      | "Private" -> Private
      | "CustomerOwnedIp" -> CustomerOwnedIp
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EndpointAccessType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EndpointAccessType" j)
    let to_json = simple_to_json to_value
  end
module EndpointArn =
  struct
    type nonrec t = string
    let context_ = "EndpointArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"^arn:(aws|aws-cn|aws-us-gov|aws-iso|aws-iso-b):s3-outposts:[a-z\\-0-9]*:[0-9]{12}:outpost/(op-[a-f0-9]{17}|ec2)/endpoint/[a-zA-Z0-9]{19}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EndpointArn" j
    let to_json = simple_to_json to_value
  end
module EndpointStatus =
  struct
    type nonrec t =
      | Pending 
      | Available 
      | Deleting 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Pending -> "Pending"
      | Available -> "Available"
      | Deleting -> "Deleting"
      | Non_static_id s -> s
    let of_string =
      function
      | "Pending" -> Pending
      | "Available" -> Available
      | "Deleting" -> Deleting
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EndpointStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EndpointStatus" j)
    let to_json = simple_to_json to_value
  end
module NetworkInterfaces =
  struct
    type nonrec t = NetworkInterface.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NetworkInterface.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NetworkInterface.of_xml)
    let of_json j =
      list_of_json ~kind:"NetworkInterfaces"
        ~of_json:NetworkInterface.of_json j
    let to_json v = composed_to_json to_value v
  end
module OutpostId =
  struct
    type nonrec t = string
    let context_ = "OutpostId"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^(op-[a-f0-9]{17}|\\d{12}|ec2)$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OutpostId" j
    let to_json = simple_to_json to_value
  end
module SecurityGroupId =
  struct
    type nonrec t = string
    let context_ = "SecurityGroupId"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^sg-([0-9a-f]{8}|[0-9a-f]{17})$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SecurityGroupId" j
    let to_json = simple_to_json to_value
  end
module SubnetId =
  struct
    type nonrec t = string
    let context_ = "SubnetId"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^subnet-([0-9a-f]{8}|[0-9a-f]{17})$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SubnetId" j
    let to_json = simple_to_json to_value
  end
module VpcId =
  struct
    type nonrec t = string
    let context_ = "VpcId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VpcId" j
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module Endpoint =
  struct
    type nonrec t =
      {
      endpointArn: EndpointArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the endpoint."];
      outpostsId: OutpostId.t option [@ocaml.doc "The ID of the Outposts."];
      cidrBlock: CidrBlock.t option
        [@ocaml.doc "The VPC CIDR committed by this endpoint."];
      status: EndpointStatus.t option
        [@ocaml.doc "The status of the endpoint."];
      creationTime: CreationTime.t option
        [@ocaml.doc "The time the endpoint was created."];
      networkInterfaces: NetworkInterfaces.t option
        [@ocaml.doc "The network interface of the endpoint."];
      vpcId: VpcId.t option
        [@ocaml.doc "The ID of the VPC used for the endpoint."];
      subnetId: SubnetId.t option
        [@ocaml.doc "The ID of the subnet used for the endpoint."];
      securityGroupId: SecurityGroupId.t option
        [@ocaml.doc "The ID of the security group used for the endpoint."];
      accessType: EndpointAccessType.t option
        [@ocaml.doc
          "The type of connectivity used to access the Amazon S3 on Outposts endpoint."];
      customerOwnedIpv4Pool: CustomerOwnedIpv4Pool.t option
        [@ocaml.doc
          "The ID of the customer-owned IPv4 address pool used for the endpoint."]}
    let make ?endpointArn =
      fun ?outpostsId ->
        fun ?cidrBlock ->
          fun ?status ->
            fun ?creationTime ->
              fun ?networkInterfaces ->
                fun ?vpcId ->
                  fun ?subnetId ->
                    fun ?securityGroupId ->
                      fun ?accessType ->
                        fun ?customerOwnedIpv4Pool ->
                          fun () ->
                            {
                              endpointArn;
                              outpostsId;
                              cidrBlock;
                              status;
                              creationTime;
                              networkInterfaces;
                              vpcId;
                              subnetId;
                              securityGroupId;
                              accessType;
                              customerOwnedIpv4Pool
                            }
    let to_value x =
      structure_to_value
        [("EndpointArn", (Option.map x.endpointArn ~f:EndpointArn.to_value));
        ("OutpostsId", (Option.map x.outpostsId ~f:OutpostId.to_value));
        ("CidrBlock", (Option.map x.cidrBlock ~f:CidrBlock.to_value));
        ("Status", (Option.map x.status ~f:EndpointStatus.to_value));
        ("CreationTime",
          (Option.map x.creationTime ~f:CreationTime.to_value));
        ("NetworkInterfaces",
          (Option.map x.networkInterfaces ~f:NetworkInterfaces.to_value));
        ("VpcId", (Option.map x.vpcId ~f:VpcId.to_value));
        ("SubnetId", (Option.map x.subnetId ~f:SubnetId.to_value));
        ("SecurityGroupId",
          (Option.map x.securityGroupId ~f:SecurityGroupId.to_value));
        ("AccessType",
          (Option.map x.accessType ~f:EndpointAccessType.to_value));
        ("CustomerOwnedIpv4Pool",
          (Option.map x.customerOwnedIpv4Pool
             ~f:CustomerOwnedIpv4Pool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customerOwnedIpv4Pool =
        (Option.map ~f:CustomerOwnedIpv4Pool.of_xml)
          (Xml.child xml_arg0 "CustomerOwnedIpv4Pool") in
      let accessType =
        (Option.map ~f:EndpointAccessType.of_xml)
          (Xml.child xml_arg0 "AccessType") in
      let securityGroupId =
        (Option.map ~f:SecurityGroupId.of_xml)
          (Xml.child xml_arg0 "SecurityGroupId") in
      let subnetId =
        (Option.map ~f:SubnetId.of_xml) (Xml.child xml_arg0 "SubnetId") in
      let vpcId = (Option.map ~f:VpcId.of_xml) (Xml.child xml_arg0 "VpcId") in
      let networkInterfaces =
        (Option.map ~f:NetworkInterfaces.of_xml)
          (Xml.child xml_arg0 "NetworkInterfaces") in
      let creationTime =
        (Option.map ~f:CreationTime.of_xml)
          (Xml.child xml_arg0 "CreationTime") in
      let status =
        (Option.map ~f:EndpointStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let cidrBlock =
        (Option.map ~f:CidrBlock.of_xml) (Xml.child xml_arg0 "CidrBlock") in
      let outpostsId =
        (Option.map ~f:OutpostId.of_xml) (Xml.child xml_arg0 "OutpostsId") in
      let endpointArn =
        (Option.map ~f:EndpointArn.of_xml) (Xml.child xml_arg0 "EndpointArn") in
      make ?customerOwnedIpv4Pool ?accessType ?securityGroupId ?subnetId
        ?vpcId ?networkInterfaces ?creationTime ?status ?cidrBlock
        ?outpostsId ?endpointArn ()
    let of_json json =
      let customerOwnedIpv4Pool =
        field_map json "CustomerOwnedIpv4Pool" CustomerOwnedIpv4Pool.of_json in
      let accessType = field_map json "AccessType" EndpointAccessType.of_json in
      let securityGroupId =
        field_map json "SecurityGroupId" SecurityGroupId.of_json in
      let subnetId = field_map json "SubnetId" SubnetId.of_json in
      let vpcId = field_map json "VpcId" VpcId.of_json in
      let networkInterfaces =
        field_map json "NetworkInterfaces" NetworkInterfaces.of_json in
      let creationTime = field_map json "CreationTime" CreationTime.of_json in
      let status = field_map json "Status" EndpointStatus.of_json in
      let cidrBlock = field_map json "CidrBlock" CidrBlock.of_json in
      let outpostsId = field_map json "OutpostsId" OutpostId.of_json in
      let endpointArn = field_map json "EndpointArn" EndpointArn.of_json in
      make ?customerOwnedIpv4Pool ?accessType ?securityGroupId ?subnetId
        ?vpcId ?networkInterfaces ?creationTime ?status ?cidrBlock
        ?outpostsId ?endpointArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon S3 on Outposts Access Points simplify managing data access at scale for shared datasets in S3 on Outposts. S3 on Outposts uses endpoints to connect to Outposts buckets so that you can perform actions within your virtual private cloud (VPC). For more information, see Accessing S3 on Outposts using VPC-only access points in the Amazon Simple Storage Service User Guide."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Access was denied for this action."]
module Endpoints =
  struct
    type nonrec t = Endpoint.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Endpoint.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Endpoint.of_xml)
    let of_json j =
      list_of_json ~kind:"Endpoints" ~of_json:Endpoint.of_json j
    let to_json v = composed_to_json to_value v
  end
module InternalServerException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "There was an exception with the internal server."]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^[A-Za-z0-9\\+\\:\\/\\=\\?\\#-_]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The requested resource was not found."]
module ValidationException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "There was an exception validating this data."]
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module EndpointId =
  struct
    type nonrec t = string
    let context_ = "EndpointId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^[a-zA-Z0-9]{19}$"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EndpointId" j
    let to_json = simple_to_json to_value
  end
module ConflictException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There was a conflict with this action, and it could not be completed."]
module ListSharedEndpointsResult =
  struct
    type nonrec t =
      {
      endpoints: Endpoints.t option
        [@ocaml.doc
          "The list of endpoints associated with the specified Outpost that have been shared by Amazon Web Services Resource Access Manager (RAM)."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the number of endpoints associated with the specified Outpost exceeds MaxResults, you can include this value in subsequent calls to this operation to retrieve more results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?endpoints =
      fun ?nextToken -> fun () -> { endpoints; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Endpoints", (Option.map x.endpoints ~f:Endpoints.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let endpoints =
        (Option.map ~f:Endpoints.of_xml) (Xml.child xml_arg0 "Endpoints") in
      make ?nextToken ?endpoints ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let endpoints = field_map json "Endpoints" Endpoints.of_json in
      make ?nextToken ?endpoints ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all endpoints associated with an Outpost that has been shared by Amazon Web Services Resource Access Manager (RAM). Related actions include: CreateEndpoint DeleteEndpoint"]
module ListSharedEndpointsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "If a previous response from this operation included a NextToken value, you can provide that value here to retrieve the next page of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of endpoints that will be returned in the response."];
      outpostId: OutpostId.t
        [@ocaml.doc "The ID of the Amazon Web Services Outpost."]}
    let context_ = "ListSharedEndpointsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~outpostId -> fun () -> { nextToken; maxResults; outpostId }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("outpostId", (Some (OutpostId.to_value x.outpostId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outpostId =
        OutpostId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outpostId") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ~outpostId ?maxResults ?nextToken ()
    let of_json json =
      let outpostId = field_map_exn json "OutpostId" OutpostId.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ~outpostId ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all endpoints associated with an Outpost that has been shared by Amazon Web Services Resource Access Manager (RAM). Related actions include: CreateEndpoint DeleteEndpoint"]
module ListEndpointsResult =
  struct
    type nonrec t =
      {
      endpoints: Endpoints.t option
        [@ocaml.doc
          "The list of endpoints associated with the specified Outpost."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "If the number of endpoints associated with the specified Outpost exceeds MaxResults, you can include this value in subsequent calls to this operation to retrieve more results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?endpoints =
      fun ?nextToken -> fun () -> { endpoints; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Endpoints", (Option.map x.endpoints ~f:Endpoints.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let endpoints =
        (Option.map ~f:Endpoints.of_xml) (Xml.child xml_arg0 "Endpoints") in
      make ?nextToken ?endpoints ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let endpoints = field_map json "Endpoints" Endpoints.of_json in
      make ?nextToken ?endpoints ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists endpoints associated with the specified Outpost. Related actions include: CreateEndpoint DeleteEndpoint"]
module ListEndpointsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "If a previous response from this operation included a NextToken value, provide that value here to retrieve the next page of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of endpoints that will be returned in the response."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists endpoints associated with the specified Outpost. Related actions include: CreateEndpoint DeleteEndpoint"]
module DeleteEndpointRequest =
  struct
    type nonrec t =
      {
      endpointId: EndpointId.t [@ocaml.doc "The ID of the endpoint."];
      outpostId: OutpostId.t [@ocaml.doc "The ID of the Outposts."]}
    let context_ = "DeleteEndpointRequest"
    let make ~endpointId =
      fun ~outpostId -> fun () -> { endpointId; outpostId }
    let to_value x =
      structure_to_value
        [("endpointId", (Some (EndpointId.to_value x.endpointId)));
        ("outpostId", (Some (OutpostId.to_value x.outpostId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outpostId =
        OutpostId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outpostId") in
      let endpointId =
        EndpointId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "endpointId") in
      make ~outpostId ~endpointId ()
    let of_json json =
      let outpostId = field_map_exn json "OutpostId" OutpostId.of_json in
      let endpointId = field_map_exn json "EndpointId" EndpointId.of_json in
      make ~outpostId ~endpointId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes an endpoint. It can take up to 5 minutes for this action to finish. Related actions include: CreateEndpoint ListEndpoints"]
module CreateEndpointResult =
  struct
    type nonrec t =
      {
      endpointArn: EndpointArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the endpoint."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?endpointArn = fun () -> { endpointArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("EndpointArn", (Option.map x.endpointArn ~f:EndpointArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endpointArn =
        (Option.map ~f:EndpointArn.of_xml) (Xml.child xml_arg0 "EndpointArn") in
      make ?endpointArn ()
    let of_json json =
      let endpointArn = field_map json "EndpointArn" EndpointArn.of_json in
      make ?endpointArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an endpoint and associates it with the specified Outpost. It can take up to 5 minutes for this action to finish. Related actions include: DeleteEndpoint ListEndpoints"]
module CreateEndpointRequest =
  struct
    type nonrec t =
      {
      outpostId: OutpostId.t [@ocaml.doc "The ID of the Outposts."];
      subnetId: SubnetId.t
        [@ocaml.doc
          "The ID of the subnet in the selected VPC. The endpoint subnet must belong to the Outpost that has Amazon S3 on Outposts provisioned."];
      securityGroupId: SecurityGroupId.t
        [@ocaml.doc "The ID of the security group to use with the endpoint."];
      accessType: EndpointAccessType.t option
        [@ocaml.doc
          "The type of access for the network connectivity for the Amazon S3 on Outposts endpoint. To use the Amazon Web Services VPC, choose Private. To use the endpoint with an on-premises network, choose CustomerOwnedIp. If you choose CustomerOwnedIp, you must also provide the customer-owned IP address pool (CoIP pool). Private is the default access type value."];
      customerOwnedIpv4Pool: CustomerOwnedIpv4Pool.t option
        [@ocaml.doc
          "The ID of the customer-owned IPv4 address pool (CoIP pool) for the endpoint. IP addresses are allocated from this pool for the endpoint."]}
    let context_ = "CreateEndpointRequest"
    let make ?accessType =
      fun ?customerOwnedIpv4Pool ->
        fun ~outpostId ->
          fun ~subnetId ->
            fun ~securityGroupId ->
              fun () ->
                {
                  accessType;
                  customerOwnedIpv4Pool;
                  outpostId;
                  subnetId;
                  securityGroupId
                }
    let to_value x =
      structure_to_value
        [("OutpostId", (Some (OutpostId.to_value x.outpostId)));
        ("SubnetId", (Some (SubnetId.to_value x.subnetId)));
        ("SecurityGroupId",
          (Some (SecurityGroupId.to_value x.securityGroupId)));
        ("AccessType",
          (Option.map x.accessType ~f:EndpointAccessType.to_value));
        ("CustomerOwnedIpv4Pool",
          (Option.map x.customerOwnedIpv4Pool
             ~f:CustomerOwnedIpv4Pool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customerOwnedIpv4Pool =
        (Option.map ~f:CustomerOwnedIpv4Pool.of_xml)
          (Xml.child xml_arg0 "CustomerOwnedIpv4Pool") in
      let accessType =
        (Option.map ~f:EndpointAccessType.of_xml)
          (Xml.child xml_arg0 "AccessType") in
      let securityGroupId =
        SecurityGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityGroupId") in
      let subnetId =
        SubnetId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SubnetId") in
      let outpostId =
        OutpostId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "OutpostId") in
      make ?customerOwnedIpv4Pool ?accessType ~securityGroupId ~subnetId
        ~outpostId ()
    let of_json json =
      let customerOwnedIpv4Pool =
        field_map json "CustomerOwnedIpv4Pool" CustomerOwnedIpv4Pool.of_json in
      let accessType = field_map json "AccessType" EndpointAccessType.of_json in
      let securityGroupId =
        field_map_exn json "SecurityGroupId" SecurityGroupId.of_json in
      let subnetId = field_map_exn json "SubnetId" SubnetId.of_json in
      let outpostId = field_map_exn json "OutpostId" OutpostId.of_json in
      make ?customerOwnedIpv4Pool ?accessType ~securityGroupId ~subnetId
        ~outpostId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an endpoint and associates it with the specified Outpost. It can take up to 5 minutes for this action to finish. Related actions include: DeleteEndpoint ListEndpoints"]
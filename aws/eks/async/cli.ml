(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_encryption_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and encryptionConfig =
         flag "encryption-config" (required json_arg)
           ~doc:"JSON EncryptionConfigList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_encryption_config
           (Values.AssociateEncryptionConfigRequest.make ?clientRequestToken
              ~clusterName
              ~encryptionConfig:(Values.EncryptionConfigList.of_json
                                   encryptionConfig) ())
           (Some Values.AssociateEncryptionConfigResponse.to_json)
           (Some Values.AssociateEncryptionConfigResponse.error_to_json)])
let associate_identity_provider_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and oidc =
         flag "oidc" (required json_arg)
           ~doc:"JSON OidcIdentityProviderConfigRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_identity_provider_config
           (Values.AssociateIdentityProviderConfigRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?clientRequestToken ~clusterName
              ~oidc:(Values.OidcIdentityProviderConfigRequest.of_json oidc)
              ())
           (Some Values.AssociateIdentityProviderConfigResponse.to_json)
           (Some Values.AssociateIdentityProviderConfigResponse.error_to_json)])
let create_addon =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addonVersion =
         flag "addon-version" (optional string) ~doc:"STRING String"
       and serviceAccountRoleArn =
         flag "service-account-role-arn" (optional string)
           ~doc:"STRING RoleArn"
       and resolveConflicts =
         flag "resolve-conflicts" (optional json_arg)
           ~doc:"JSON ResolveConflicts"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING ClusterName"
       and addonName =
         flag "addon-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_addon
           (Values.CreateAddonRequest.make ?addonVersion
              ?serviceAccountRoleArn
              ?resolveConflicts:(Option.map
                                   ~f:Values.ResolveConflicts.of_json
                                   resolveConflicts) ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~clusterName
              ~addonName ()) (Some Values.CreateAddonResponse.to_json)
           (Some Values.CreateAddonResponse.error_to_json)])
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and version = flag "version" (optional string) ~doc:"STRING String"
       and kubernetesNetworkConfig =
         flag "kubernetes-network-config" (optional json_arg)
           ~doc:"JSON KubernetesNetworkConfigRequest"
       and logging = flag "logging" (optional json_arg) ~doc:"JSON Logging"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and encryptionConfig =
         flag "encryption-config" (optional json_arg)
           ~doc:"JSON EncryptionConfigList"
       and name = flag "name" (required string) ~doc:"STRING ClusterName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String"
       and resourcesVpcConfig =
         flag "resources-vpc-config" (required json_arg)
           ~doc:"JSON VpcConfigRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make ?version
              ?kubernetesNetworkConfig:(Option.map
                                          ~f:Values.KubernetesNetworkConfigRequest.of_json
                                          kubernetesNetworkConfig)
              ?logging:(Option.map ~f:Values.Logging.of_json logging)
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?encryptionConfig:(Option.map
                                   ~f:Values.EncryptionConfigList.of_json
                                   encryptionConfig) ~name ~roleArn
              ~resourcesVpcConfig:(Values.VpcConfigRequest.of_json
                                     resourcesVpcConfig) ())
           (Some Values.CreateClusterResponse.to_json)
           (Some Values.CreateClusterResponse.error_to_json)])
let create_fargate_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subnets =
         flag "subnets" (optional json_arg) ~doc:"JSON StringList"
       and selectors =
         flag "selectors" (optional json_arg)
           ~doc:"JSON FargateProfileSelectors"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and fargateProfileName =
         flag "fargate-profile-name" (required string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and podExecutionRoleArn =
         flag "pod-execution-role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fargate_profile
           (Values.CreateFargateProfileRequest.make
              ?subnets:(Option.map ~f:Values.StringList.of_json subnets)
              ?selectors:(Option.map
                            ~f:Values.FargateProfileSelectors.of_json
                            selectors) ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~fargateProfileName ~clusterName ~podExecutionRoleArn ())
           (Some Values.CreateFargateProfileResponse.to_json)
           (Some Values.CreateFargateProfileResponse.error_to_json)])
let create_nodegroup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scalingConfig =
         flag "scaling-config" (optional json_arg)
           ~doc:"JSON NodegroupScalingConfig"
       and diskSize = flag "disk-size" (optional int) ~doc:"INT BoxedInteger"
       and instanceTypes =
         flag "instance-types" (optional json_arg) ~doc:"JSON StringList"
       and amiType = flag "ami-type" (optional json_arg) ~doc:"JSON AMITypes"
       and remoteAccess =
         flag "remote-access" (optional json_arg)
           ~doc:"JSON RemoteAccessConfig"
       and labels = flag "labels" (optional json_arg) ~doc:"JSON labelsMap"
       and taints = flag "taints" (optional json_arg) ~doc:"JSON taintsList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and launchTemplate =
         flag "launch-template" (optional json_arg)
           ~doc:"JSON LaunchTemplateSpecification"
       and updateConfig =
         flag "update-config" (optional json_arg)
           ~doc:"JSON NodegroupUpdateConfig"
       and capacityType =
         flag "capacity-type" (optional json_arg) ~doc:"JSON CapacityTypes"
       and version = flag "version" (optional string) ~doc:"STRING String"
       and releaseVersion =
         flag "release-version" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and nodegroupName =
         flag "nodegroup-name" (required string) ~doc:"STRING String"
       and subnets =
         flag "subnets" (required json_arg) ~doc:"JSON StringList"
       and nodeRole = flag "node-role" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_nodegroup
           (Values.CreateNodegroupRequest.make
              ?scalingConfig:(Option.map
                                ~f:Values.NodegroupScalingConfig.of_json
                                scalingConfig) ?diskSize
              ?instanceTypes:(Option.map ~f:Values.StringList.of_json
                                instanceTypes)
              ?amiType:(Option.map ~f:Values.AMITypes.of_json amiType)
              ?remoteAccess:(Option.map ~f:Values.RemoteAccessConfig.of_json
                               remoteAccess)
              ?labels:(Option.map ~f:Values.LabelsMap.of_json labels)
              ?taints:(Option.map ~f:Values.TaintsList.of_json taints)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?clientRequestToken
              ?launchTemplate:(Option.map
                                 ~f:Values.LaunchTemplateSpecification.of_json
                                 launchTemplate)
              ?updateConfig:(Option.map
                               ~f:Values.NodegroupUpdateConfig.of_json
                               updateConfig)
              ?capacityType:(Option.map ~f:Values.CapacityTypes.of_json
                               capacityType) ?version ?releaseVersion
              ~clusterName ~nodegroupName
              ~subnets:(Values.StringList.of_json subnets) ~nodeRole ())
           (Some Values.CreateNodegroupResponse.to_json)
           (Some Values.CreateNodegroupResponse.error_to_json)])
let delete_addon =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and preserve = flag "preserve" (optional bool) ~doc:"BOOL Boolean"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING ClusterName"
       and addonName =
         flag "addon-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_addon
           (Values.DeleteAddonRequest.make ?preserve ~clusterName ~addonName
              ()) (Some Values.DeleteAddonResponse.to_json)
           (Some Values.DeleteAddonResponse.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster (Values.DeleteClusterRequest.make ~name ())
           (Some Values.DeleteClusterResponse.to_json)
           (Some Values.DeleteClusterResponse.error_to_json)])
let delete_fargate_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and fargateProfileName =
         flag "fargate-profile-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fargate_profile
           (Values.DeleteFargateProfileRequest.make ~clusterName
              ~fargateProfileName ())
           (Some Values.DeleteFargateProfileResponse.to_json)
           (Some Values.DeleteFargateProfileResponse.error_to_json)])
let delete_nodegroup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and nodegroupName =
         flag "nodegroup-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_nodegroup
           (Values.DeleteNodegroupRequest.make ~clusterName ~nodegroupName ())
           (Some Values.DeleteNodegroupResponse.to_json)
           (Some Values.DeleteNodegroupResponse.error_to_json)])
let deregister_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_cluster
           (Values.DeregisterClusterRequest.make ~name ())
           (Some Values.DeregisterClusterResponse.to_json)
           (Some Values.DeregisterClusterResponse.error_to_json)])
let describe_addon =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING ClusterName"
       and addonName =
         flag "addon-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_addon
           (Values.DescribeAddonRequest.make ~clusterName ~addonName ())
           (Some Values.DescribeAddonResponse.to_json)
           (Some Values.DescribeAddonResponse.error_to_json)])
let describe_addon_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kubernetesVersion =
         flag "kubernetes-version" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeAddonVersionsRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and addonName =
         flag "addon-name" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_addon_versions
           (Values.DescribeAddonVersionsRequest.make ?kubernetesVersion
              ?maxResults ?nextToken ?addonName ())
           (Some Values.DescribeAddonVersionsResponse.to_json)
           (Some Values.DescribeAddonVersionsResponse.error_to_json)])
let describe_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster (Values.DescribeClusterRequest.make ~name ())
           (Some Values.DescribeClusterResponse.to_json)
           (Some Values.DescribeClusterResponse.error_to_json)])
let describe_fargate_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and fargateProfileName =
         flag "fargate-profile-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fargate_profile
           (Values.DescribeFargateProfileRequest.make ~clusterName
              ~fargateProfileName ())
           (Some Values.DescribeFargateProfileResponse.to_json)
           (Some Values.DescribeFargateProfileResponse.error_to_json)])
let describe_identity_provider_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and identityProviderConfig =
         flag "identity-provider-config" (required json_arg)
           ~doc:"JSON IdentityProviderConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity_provider_config
           (Values.DescribeIdentityProviderConfigRequest.make ~clusterName
              ~identityProviderConfig:(Values.IdentityProviderConfig.of_json
                                         identityProviderConfig) ())
           (Some Values.DescribeIdentityProviderConfigResponse.to_json)
           (Some Values.DescribeIdentityProviderConfigResponse.error_to_json)])
let describe_nodegroup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and nodegroupName =
         flag "nodegroup-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_nodegroup
           (Values.DescribeNodegroupRequest.make ~clusterName ~nodegroupName
              ()) (Some Values.DescribeNodegroupResponse.to_json)
           (Some Values.DescribeNodegroupResponse.error_to_json)])
let describe_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nodegroupName =
         flag "nodegroup-name" (optional string) ~doc:"STRING String"
       and addonName =
         flag "addon-name" (optional string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String"
       and updateId = flag "update-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_update
           (Values.DescribeUpdateRequest.make ?nodegroupName ?addonName ~name
              ~updateId ()) (Some Values.DescribeUpdateResponse.to_json)
           (Some Values.DescribeUpdateResponse.error_to_json)])
let disassociate_identity_provider_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and identityProviderConfig =
         flag "identity-provider-config" (required json_arg)
           ~doc:"JSON IdentityProviderConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_identity_provider_config
           (Values.DisassociateIdentityProviderConfigRequest.make
              ?clientRequestToken ~clusterName
              ~identityProviderConfig:(Values.IdentityProviderConfig.of_json
                                         identityProviderConfig) ())
           (Some Values.DisassociateIdentityProviderConfigResponse.to_json)
           (Some
              Values.DisassociateIdentityProviderConfigResponse.error_to_json)])
let list_addons =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAddonsRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING ClusterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_addons
           (Values.ListAddonsRequest.make ?maxResults ?nextToken ~clusterName
              ()) (Some Values.ListAddonsResponse.to_json)
           (Some Values.ListAddonsResponse.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListClustersRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and include_ =
         flag "include-" (optional json_arg) ~doc:"JSON IncludeClustersList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersRequest.make ?maxResults ?nextToken
              ?include_:(Option.map ~f:Values.IncludeClustersList.of_json
                           include_) ())
           (Some Values.ListClustersResponse.to_json)
           (Some Values.ListClustersResponse.error_to_json)])
let list_fargate_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT FargateProfilesRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fargate_profiles
           (Values.ListFargateProfilesRequest.make ?maxResults ?nextToken
              ~clusterName ())
           (Some Values.ListFargateProfilesResponse.to_json)
           (Some Values.ListFargateProfilesResponse.error_to_json)])
let list_identity_provider_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListIdentityProviderConfigsRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_provider_configs
           (Values.ListIdentityProviderConfigsRequest.make ?maxResults
              ?nextToken ~clusterName ())
           (Some Values.ListIdentityProviderConfigsResponse.to_json)
           (Some Values.ListIdentityProviderConfigsResponse.error_to_json)])
let list_nodegroups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListNodegroupsRequestMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_nodegroups
           (Values.ListNodegroupsRequest.make ?maxResults ?nextToken
              ~clusterName ()) (Some Values.ListNodegroupsResponse.to_json)
           (Some Values.ListNodegroupsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_updates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nodegroupName =
         flag "nodegroup-name" (optional string) ~doc:"STRING String"
       and addonName =
         flag "addon-name" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListUpdatesRequestMaxResults"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_updates
           (Values.ListUpdatesRequest.make ?nodegroupName ?addonName
              ?nextToken ?maxResults ~name ())
           (Some Values.ListUpdatesResponse.to_json)
           (Some Values.ListUpdatesResponse.error_to_json)])
let register_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING ClusterName"
       and connectorConfig =
         flag "connector-config" (required json_arg)
           ~doc:"JSON ConnectorConfigRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_cluster
           (Values.RegisterClusterRequest.make ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~connectorConfig:(Values.ConnectorConfigRequest.of_json
                                  connectorConfig) ())
           (Some Values.RegisterClusterResponse.to_json)
           (Some Values.RegisterClusterResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_addon =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addonVersion =
         flag "addon-version" (optional string) ~doc:"STRING String"
       and serviceAccountRoleArn =
         flag "service-account-role-arn" (optional string)
           ~doc:"STRING RoleArn"
       and resolveConflicts =
         flag "resolve-conflicts" (optional json_arg)
           ~doc:"JSON ResolveConflicts"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING ClusterName"
       and addonName =
         flag "addon-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_addon
           (Values.UpdateAddonRequest.make ?addonVersion
              ?serviceAccountRoleArn
              ?resolveConflicts:(Option.map
                                   ~f:Values.ResolveConflicts.of_json
                                   resolveConflicts) ?clientRequestToken
              ~clusterName ~addonName ())
           (Some Values.UpdateAddonResponse.to_json)
           (Some Values.UpdateAddonResponse.error_to_json)])
let update_cluster_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourcesVpcConfig =
         flag "resources-vpc-config" (optional json_arg)
           ~doc:"JSON VpcConfigRequest"
       and logging = flag "logging" (optional json_arg) ~doc:"JSON Logging"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster_config
           (Values.UpdateClusterConfigRequest.make
              ?resourcesVpcConfig:(Option.map
                                     ~f:Values.VpcConfigRequest.of_json
                                     resourcesVpcConfig)
              ?logging:(Option.map ~f:Values.Logging.of_json logging)
              ?clientRequestToken ~name ())
           (Some Values.UpdateClusterConfigResponse.to_json)
           (Some Values.UpdateClusterConfigResponse.error_to_json)])
let update_cluster_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and name = flag "name" (required string) ~doc:"STRING String"
       and version = flag "version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster_version
           (Values.UpdateClusterVersionRequest.make ?clientRequestToken ~name
              ~version ()) (Some Values.UpdateClusterVersionResponse.to_json)
           (Some Values.UpdateClusterVersionResponse.error_to_json)])
let update_nodegroup_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and labels =
         flag "labels" (optional json_arg) ~doc:"JSON UpdateLabelsPayload"
       and taints =
         flag "taints" (optional json_arg) ~doc:"JSON UpdateTaintsPayload"
       and scalingConfig =
         flag "scaling-config" (optional json_arg)
           ~doc:"JSON NodegroupScalingConfig"
       and updateConfig =
         flag "update-config" (optional json_arg)
           ~doc:"JSON NodegroupUpdateConfig"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and nodegroupName =
         flag "nodegroup-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_nodegroup_config
           (Values.UpdateNodegroupConfigRequest.make
              ?labels:(Option.map ~f:Values.UpdateLabelsPayload.of_json
                         labels)
              ?taints:(Option.map ~f:Values.UpdateTaintsPayload.of_json
                         taints)
              ?scalingConfig:(Option.map
                                ~f:Values.NodegroupScalingConfig.of_json
                                scalingConfig)
              ?updateConfig:(Option.map
                               ~f:Values.NodegroupUpdateConfig.of_json
                               updateConfig) ?clientRequestToken ~clusterName
              ~nodegroupName ())
           (Some Values.UpdateNodegroupConfigResponse.to_json)
           (Some Values.UpdateNodegroupConfigResponse.error_to_json)])
let update_nodegroup_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and version = flag "version" (optional string) ~doc:"STRING String"
       and releaseVersion =
         flag "release-version" (optional string) ~doc:"STRING String"
       and launchTemplate =
         flag "launch-template" (optional json_arg)
           ~doc:"JSON LaunchTemplateSpecification"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING String"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and nodegroupName =
         flag "nodegroup-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_nodegroup_version
           (Values.UpdateNodegroupVersionRequest.make ?version
              ?releaseVersion
              ?launchTemplate:(Option.map
                                 ~f:Values.LaunchTemplateSpecification.of_json
                                 launchTemplate) ?force ?clientRequestToken
              ~clusterName ~nodegroupName ())
           (Some Values.UpdateNodegroupVersionResponse.to_json)
           (Some Values.UpdateNodegroupVersionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-encryption-config", associate_encryption_config);
    ("associate-identity-provider-config",
      associate_identity_provider_config);
    ("create-addon", create_addon);
    ("create-cluster", create_cluster);
    ("create-fargate-profile", create_fargate_profile);
    ("create-nodegroup", create_nodegroup);
    ("delete-addon", delete_addon);
    ("delete-cluster", delete_cluster);
    ("delete-fargate-profile", delete_fargate_profile);
    ("delete-nodegroup", delete_nodegroup);
    ("deregister-cluster", deregister_cluster);
    ("describe-addon", describe_addon);
    ("describe-addon-versions", describe_addon_versions);
    ("describe-cluster", describe_cluster);
    ("describe-fargate-profile", describe_fargate_profile);
    ("describe-identity-provider-config", describe_identity_provider_config);
    ("describe-nodegroup", describe_nodegroup);
    ("describe-update", describe_update);
    ("disassociate-identity-provider-config",
      disassociate_identity_provider_config);
    ("list-addons", list_addons);
    ("list-clusters", list_clusters);
    ("list-fargate-profiles", list_fargate_profiles);
    ("list-identity-provider-configs", list_identity_provider_configs);
    ("list-nodegroups", list_nodegroups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-updates", list_updates);
    ("register-cluster", register_cluster);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-addon", update_addon);
    ("update-cluster-config", update_cluster_config);
    ("update-cluster-version", update_cluster_version);
    ("update-nodegroup-config", update_nodegroup_config);
    ("update-nodegroup-version", update_nodegroup_version)]
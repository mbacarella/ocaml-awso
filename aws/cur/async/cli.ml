(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_report_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportName =
         flag "report-name" (optional string) ~doc:"STRING ReportName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_report_definition
           (Values.DeleteReportDefinitionRequest.make ?reportName ())
           (Some Values.DeleteReportDefinitionResponse.to_json)
           (Some Values.DeleteReportDefinitionResponse.error_to_json)])
let describe_report_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_report_definitions
           (Values.DescribeReportDefinitionsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.DescribeReportDefinitionsResponse.to_json)
           (Some Values.DescribeReportDefinitionsResponse.error_to_json)])
let modify_report_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportName =
         flag "report-name" (required string) ~doc:"STRING ReportName"
       and reportDefinition =
         flag "report-definition" (required json_arg)
           ~doc:"JSON ReportDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_report_definition
           (Values.ModifyReportDefinitionRequest.make ~reportName
              ~reportDefinition:(Values.ReportDefinition.of_json
                                   reportDefinition) ())
           (Some Values.ModifyReportDefinitionResponse.to_json)
           (Some Values.ModifyReportDefinitionResponse.error_to_json)])
let put_report_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportDefinition =
         flag "report-definition" (required json_arg)
           ~doc:"JSON ReportDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_report_definition
           (Values.PutReportDefinitionRequest.make
              ~reportDefinition:(Values.ReportDefinition.of_json
                                   reportDefinition) ())
           (Some Values.PutReportDefinitionResponse.to_json)
           (Some Values.PutReportDefinitionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-report-definition", delete_report_definition);
    ("describe-report-definitions", describe_report_definitions);
    ("modify-report-definition", modify_report_definition);
    ("put-report-definition", put_report_definition)]
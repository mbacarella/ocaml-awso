(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logPublishingOptions =
         flag "log-publishing-options" (optional json_arg)
           ~doc:"JSON LogPublishingOptions"
       and vpcOptions =
         flag "vpc-options" (optional json_arg) ~doc:"JSON VpcOptions"
       and bufferOptions =
         flag "buffer-options" (optional json_arg) ~doc:"JSON BufferOptions"
       and encryptionAtRestOptions =
         flag "encryption-at-rest-options" (optional json_arg)
           ~doc:"JSON EncryptionAtRestOptions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and pipelineRoleArn =
         flag "pipeline-role-arn" (optional string)
           ~doc:"STRING PipelineRoleArn"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and minUnits =
         flag "min-units" (required int) ~doc:"INT PipelineUnits"
       and maxUnits =
         flag "max-units" (required int) ~doc:"INT PipelineUnits"
       and pipelineConfigurationBody =
         flag "pipeline-configuration-body" (required string)
           ~doc:"STRING PipelineConfigurationBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pipeline
           (Values.CreatePipelineRequest.make
              ?logPublishingOptions:(Option.map
                                       ~f:Values.LogPublishingOptions.of_json
                                       logPublishingOptions)
              ?vpcOptions:(Option.map ~f:Values.VpcOptions.of_json vpcOptions)
              ?bufferOptions:(Option.map ~f:Values.BufferOptions.of_json
                                bufferOptions)
              ?encryptionAtRestOptions:(Option.map
                                          ~f:Values.EncryptionAtRestOptions.of_json
                                          encryptionAtRestOptions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?pipelineRoleArn ~pipelineName ~minUnits ~maxUnits
              ~pipelineConfigurationBody ())
           (Some Values.CreatePipelineResponse.to_json)
           (Some Values.CreatePipelineResponse.error_to_json)])
let create_pipeline_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineArn =
         flag "pipeline-arn" (required string) ~doc:"STRING PipelineArn"
       and vpcOptions =
         flag "vpc-options" (required json_arg)
           ~doc:"JSON PipelineEndpointVpcOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pipeline_endpoint
           (Values.CreatePipelineEndpointRequest.make ~pipelineArn
              ~vpcOptions:(Values.PipelineEndpointVpcOptions.of_json
                             vpcOptions) ())
           (Some Values.CreatePipelineEndpointResponse.to_json)
           (Some Values.CreatePipelineEndpointResponse.error_to_json)])
let delete_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pipeline
           (Values.DeletePipelineRequest.make ~pipelineName ())
           (Some Values.DeletePipelineResponse.to_json)
           (Some Values.DeletePipelineResponse.error_to_json)])
let delete_pipeline_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointId =
         flag "endpoint-id" (required string)
           ~doc:"STRING PipelineEndpointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pipeline_endpoint
           (Values.DeletePipelineEndpointRequest.make ~endpointId ())
           (Some Values.DeletePipelineEndpointResponse.to_json)
           (Some Values.DeletePipelineEndpointResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PipelineArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let get_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pipeline (Values.GetPipelineRequest.make ~pipelineName ())
           (Some Values.GetPipelineResponse.to_json)
           (Some Values.GetPipelineResponse.error_to_json)])
let get_pipeline_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and format =
         flag "format" (optional string) ~doc:"STRING BlueprintFormat"
       and blueprintName =
         flag "blueprint-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pipeline_blueprint
           (Values.GetPipelineBlueprintRequest.make ?format ~blueprintName ())
           (Some Values.GetPipelineBlueprintResponse.to_json)
           (Some Values.GetPipelineBlueprintResponse.error_to_json)])
let get_pipeline_change_progress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pipeline_change_progress
           (Values.GetPipelineChangeProgressRequest.make ~pipelineName ())
           (Some Values.GetPipelineChangeProgressResponse.to_json)
           (Some Values.GetPipelineChangeProgressResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PipelineArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let list_pipeline_blueprints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipeline_blueprints
           (Values.ListPipelineBlueprintsRequest.make ())
           (Some Values.ListPipelineBlueprintsResponse.to_json)
           (Some Values.ListPipelineBlueprintsResponse.error_to_json)])
let list_pipeline_endpoint_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipeline_endpoint_connections
           (Values.ListPipelineEndpointConnectionsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListPipelineEndpointConnectionsResponse.to_json)
           (Some Values.ListPipelineEndpointConnectionsResponse.error_to_json)])
let list_pipeline_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipeline_endpoints
           (Values.ListPipelineEndpointsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListPipelineEndpointsResponse.to_json)
           (Some Values.ListPipelineEndpointsResponse.error_to_json)])
let list_pipelines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipelines
           (Values.ListPipelinesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPipelinesResponse.to_json)
           (Some Values.ListPipelinesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PipelineArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PipelineArn"
       and policy =
         flag "policy" (required string) ~doc:"STRING ResourcePolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ~resourceArn ~policy ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let revoke_pipeline_endpoint_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineArn =
         flag "pipeline-arn" (required string) ~doc:"STRING PipelineArn"
       and endpointIds =
         flag "endpoint-ids" (required json_arg)
           ~doc:"JSON PipelineEndpointIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_pipeline_endpoint_connections
           (Values.RevokePipelineEndpointConnectionsRequest.make ~pipelineArn
              ~endpointIds:(Values.PipelineEndpointIdsList.of_json
                              endpointIds) ())
           (Some Values.RevokePipelineEndpointConnectionsResponse.to_json)
           (Some
              Values.RevokePipelineEndpointConnectionsResponse.error_to_json)])
let start_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_pipeline
           (Values.StartPipelineRequest.make ~pipelineName ())
           (Some Values.StartPipelineResponse.to_json)
           (Some Values.StartPipelineResponse.error_to_json)])
let stop_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_pipeline
           (Values.StopPipelineRequest.make ~pipelineName ())
           (Some Values.StopPipelineResponse.to_json)
           (Some Values.StopPipelineResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PipelineArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING PipelineArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.StringList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and minUnits =
         flag "min-units" (optional int) ~doc:"INT PipelineUnits"
       and maxUnits =
         flag "max-units" (optional int) ~doc:"INT PipelineUnits"
       and pipelineConfigurationBody =
         flag "pipeline-configuration-body" (optional string)
           ~doc:"STRING PipelineConfigurationBody"
       and logPublishingOptions =
         flag "log-publishing-options" (optional json_arg)
           ~doc:"JSON LogPublishingOptions"
       and bufferOptions =
         flag "buffer-options" (optional json_arg) ~doc:"JSON BufferOptions"
       and encryptionAtRestOptions =
         flag "encryption-at-rest-options" (optional json_arg)
           ~doc:"JSON EncryptionAtRestOptions"
       and pipelineRoleArn =
         flag "pipeline-role-arn" (optional string)
           ~doc:"STRING PipelineRoleArn"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pipeline
           (Values.UpdatePipelineRequest.make ?minUnits ?maxUnits
              ?pipelineConfigurationBody
              ?logPublishingOptions:(Option.map
                                       ~f:Values.LogPublishingOptions.of_json
                                       logPublishingOptions)
              ?bufferOptions:(Option.map ~f:Values.BufferOptions.of_json
                                bufferOptions)
              ?encryptionAtRestOptions:(Option.map
                                          ~f:Values.EncryptionAtRestOptions.of_json
                                          encryptionAtRestOptions)
              ?pipelineRoleArn ~pipelineName ())
           (Some Values.UpdatePipelineResponse.to_json)
           (Some Values.UpdatePipelineResponse.error_to_json)])
let validate_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineConfigurationBody =
         flag "pipeline-configuration-body" (required string)
           ~doc:"STRING PipelineConfigurationBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_pipeline
           (Values.ValidatePipelineRequest.make ~pipelineConfigurationBody ())
           (Some Values.ValidatePipelineResponse.to_json)
           (Some Values.ValidatePipelineResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-pipeline", create_pipeline);
    ("create-pipeline-endpoint", create_pipeline_endpoint);
    ("delete-pipeline", delete_pipeline);
    ("delete-pipeline-endpoint", delete_pipeline_endpoint);
    ("delete-resource-policy", delete_resource_policy);
    ("get-pipeline", get_pipeline);
    ("get-pipeline-blueprint", get_pipeline_blueprint);
    ("get-pipeline-change-progress", get_pipeline_change_progress);
    ("get-resource-policy", get_resource_policy);
    ("list-pipeline-blueprints", list_pipeline_blueprints);
    ("list-pipeline-endpoint-connections",
      list_pipeline_endpoint_connections);
    ("list-pipeline-endpoints", list_pipeline_endpoints);
    ("list-pipelines", list_pipelines);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-resource-policy", put_resource_policy);
    ("revoke-pipeline-endpoint-connections",
      revoke_pipeline_endpoint_connections);
    ("start-pipeline", start_pipeline);
    ("stop-pipeline", stop_pipeline);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-pipeline", update_pipeline);
    ("validate-pipeline", validate_pipeline)]
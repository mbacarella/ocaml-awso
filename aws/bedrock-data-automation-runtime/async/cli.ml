(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_data_automation_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and invocationArn =
         flag "invocation-arn" (required string) ~doc:"STRING InvocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_automation_status
           (Values.GetDataAutomationStatusRequest.make ~invocationArn ())
           (Some Values.GetDataAutomationStatusResponse.to_json)
           (Some Values.GetDataAutomationStatusResponse.error_to_json)])
let invoke_data_automation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataAutomationConfiguration =
         flag "data-automation-configuration" (optional json_arg)
           ~doc:"JSON DataAutomationConfiguration"
       and blueprints =
         flag "blueprints" (optional json_arg) ~doc:"JSON BlueprintList"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and outputConfiguration =
         flag "output-configuration" (optional json_arg)
           ~doc:"JSON OutputConfiguration"
       and inputConfiguration =
         flag "input-configuration" (required json_arg)
           ~doc:"JSON SyncInputConfiguration"
       and dataAutomationProfileArn =
         flag "data-automation-profile-arn" (required string)
           ~doc:"STRING DataAutomationProfileArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_data_automation
           (Values.InvokeDataAutomationRequest.make
              ?dataAutomationConfiguration:(Option.map
                                              ~f:Values.DataAutomationConfiguration.of_json
                                              dataAutomationConfiguration)
              ?blueprints:(Option.map ~f:Values.BlueprintList.of_json
                             blueprints)
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?outputConfiguration:(Option.map
                                      ~f:Values.OutputConfiguration.of_json
                                      outputConfiguration)
              ~inputConfiguration:(Values.SyncInputConfiguration.of_json
                                     inputConfiguration)
              ~dataAutomationProfileArn ())
           (Some Values.InvokeDataAutomationResponse.to_json)
           (Some Values.InvokeDataAutomationResponse.error_to_json)])
let invoke_data_automation_async =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and dataAutomationConfiguration =
         flag "data-automation-configuration" (optional json_arg)
           ~doc:"JSON DataAutomationConfiguration"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and notificationConfiguration =
         flag "notification-configuration" (optional json_arg)
           ~doc:"JSON NotificationConfiguration"
       and blueprints =
         flag "blueprints" (optional json_arg) ~doc:"JSON BlueprintList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inputConfiguration =
         flag "input-configuration" (required json_arg)
           ~doc:"JSON InputConfiguration"
       and outputConfiguration =
         flag "output-configuration" (required json_arg)
           ~doc:"JSON OutputConfiguration"
       and dataAutomationProfileArn =
         flag "data-automation-profile-arn" (required string)
           ~doc:"STRING DataAutomationProfileArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_data_automation_async
           (Values.InvokeDataAutomationAsyncRequest.make ?clientToken
              ?dataAutomationConfiguration:(Option.map
                                              ~f:Values.DataAutomationConfiguration.of_json
                                              dataAutomationConfiguration)
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?notificationConfiguration:(Option.map
                                            ~f:Values.NotificationConfiguration.of_json
                                            notificationConfiguration)
              ?blueprints:(Option.map ~f:Values.BlueprintList.of_json
                             blueprints)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inputConfiguration:(Values.InputConfiguration.of_json
                                     inputConfiguration)
              ~outputConfiguration:(Values.OutputConfiguration.of_json
                                      outputConfiguration)
              ~dataAutomationProfileArn ())
           (Some Values.InvokeDataAutomationAsyncResponse.to_json)
           (Some Values.InvokeDataAutomationAsyncResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-data-automation-status", get_data_automation_status);
    ("invoke-data-automation", invoke_data_automation);
    ("invoke-data-automation-async", invoke_data_automation_async);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
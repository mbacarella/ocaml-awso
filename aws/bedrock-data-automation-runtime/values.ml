(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.bedrock_data_automation_runtime
let apiVersion = "2024-06-13"
let endpointPrefix = "bedrock-data-automation-runtime"
let serviceFullName = "Runtime for Amazon Bedrock Data Automation"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "AmazonBedrockKeystoneRuntimeService"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module TimestampSegmentEndTimeMillisLong =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:300000L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module TimestampSegmentStartTimeMillisLong =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:0L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module TimestampSegment =
  struct
    type nonrec t =
      {
      startTimeMillis: TimestampSegmentStartTimeMillisLong.t
        [@ocaml.doc "Start timestamp in milliseconds"];
      endTimeMillis: TimestampSegmentEndTimeMillisLong.t
        [@ocaml.doc "End timestamp in milliseconds"]}
    let context_ = "TimestampSegment"
    let make ~startTimeMillis =
      fun ~endTimeMillis -> fun () -> { startTimeMillis; endTimeMillis }
    let to_value x =
      structure_to_value
        [("startTimeMillis",
           (Some
              (TimestampSegmentStartTimeMillisLong.to_value x.startTimeMillis)));
        ("endTimeMillis",
          (Some (TimestampSegmentEndTimeMillisLong.to_value x.endTimeMillis)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endTimeMillis =
        TimestampSegmentEndTimeMillisLong.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "endTimeMillis") in
      let startTimeMillis =
        TimestampSegmentStartTimeMillisLong.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "startTimeMillis") in
      make ~endTimeMillis ~startTimeMillis ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endTimeMillis =
        field_map_exn json__ "endTimeMillis"
          TimestampSegmentEndTimeMillisLong.of_json in
      let startTimeMillis =
        field_map_exn json__ "startTimeMillis"
          TimestampSegmentStartTimeMillisLong.of_json in
      make ~endTimeMillis ~startTimeMillis ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Timestamp segment"]
module VideoSegmentConfiguration =
  struct
    type nonrec t =
      {
      timestampSegment: TimestampSegment.t option
        [@ocaml.doc "Timestamp segment"]}
    let make ?timestampSegment = fun () -> { timestampSegment }
    let to_value x =
      structure_to_value
        [("timestampSegment",
           (Option.map x.timestampSegment ~f:TimestampSegment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestampSegment =
        (Option.map ~f:TimestampSegment.of_xml)
          (Xml.child xml_arg0 "timestampSegment") in
      make ?timestampSegment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestampSegment =
        field_map json__ "timestampSegment" TimestampSegment.of_json in
      make ?timestampSegment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delimits the segment of the input that will be processed"]
module TagKey =
  struct
    type nonrec t = string[@@ocaml.doc "Defines the context of the tag."]
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(?!aws:)[\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Defines the context of the tag."]
module TagValue =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Defines the value within the context. e.g. <key=reason, value=training>."]
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Defines the value within the context. e.g. <key=reason, value=training>."]
module CustomOutputStatus =
  struct
    type nonrec t =
      | MATCH 
      | NO_MATCH 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MATCH -> "MATCH"
      | NO_MATCH -> "NO_MATCH"
      | Non_static_id s -> s
    let of_string =
      function
      | "MATCH" -> MATCH
      | "NO_MATCH" -> NO_MATCH
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CustomOutputStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CustomOutputStatus" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module BlueprintArn =
  struct
    type nonrec t = string[@@ocaml.doc "ARN of a Blueprint"]
    let context_ = "BlueprintArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(|-cn|-iso|-iso-[a-z]|-us-gov):bedrock:[a-zA-Z0-9-]*:(aws|[0-9]{12}):blueprint/(bedrock-data-insights-public-[a-zA-Z0-9-_]{1,30}|bedrock-data-automation-public-[a-zA-Z0-9-_]{1,30}|[a-zA-Z0-9-]{12,36})")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BlueprintArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "ARN of a Blueprint"]
module BlueprintStage =
  struct
    type nonrec t =
      | DEVELOPMENT 
      | LIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DEVELOPMENT -> "DEVELOPMENT"
      | LIVE -> "LIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "DEVELOPMENT" -> DEVELOPMENT
      | "LIVE" -> LIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BlueprintStage" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BlueprintStage" j)
    let to_json = simple_to_json to_value
  end
module BlueprintVersion =
  struct
    type nonrec t = string[@@ocaml.doc "Blueprint version."]
    let context_ = "BlueprintVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BlueprintVersion" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Blueprint version."]
module EncryptionContextKey =
  struct
    type nonrec t = string[@@ocaml.doc "Excryption context key."]
    let context_ = "EncryptionContextKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2000) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EncryptionContextKey" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Excryption context key."]
module EncryptionContextValue =
  struct
    type nonrec t = string[@@ocaml.doc "Encryption context value."]
    let context_ = "EncryptionContextValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2000) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EncryptionContextValue" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Encryption context value."]
module VideoAssetProcessingConfiguration =
  struct
    type nonrec t =
      {
      segmentConfiguration: VideoSegmentConfiguration.t option
        [@ocaml.doc
          "Delimits the segment of the input that will be processed"]}
    let make ?segmentConfiguration = fun () -> { segmentConfiguration }
    let to_value x =
      structure_to_value
        [("segmentConfiguration",
           (Option.map x.segmentConfiguration
              ~f:VideoSegmentConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let segmentConfiguration =
        (Option.map ~f:VideoSegmentConfiguration.of_xml)
          (Xml.child xml_arg0 "segmentConfiguration") in
      make ?segmentConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let segmentConfiguration =
        field_map json__ "segmentConfiguration"
          VideoSegmentConfiguration.of_json in
      make ?segmentConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Video asset processing configuration"]
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module NonBlankString =
  struct
    type nonrec t = string
    let context_ = "NonBlankString"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[\\s\\S]*"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonBlankString" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t = {
      key: TagKey.t ;
      value: TagValue.t }
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("key", (Some (TagKey.to_value x.key)));
        ("value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "key") in
      make ~value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map_exn json__ "value" TagValue.of_json in
      let key = field_map_exn json__ "key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Key value pair of a tag"]
module S3Uri =
  struct
    type nonrec t = string[@@ocaml.doc "A path in S3"]
    let context_ = "S3Uri"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"s3://[a-z0-9][\\.\\-a-z0-9]{1,61}[a-z0-9](/[^\\x00-\\x1F\\x7F\\{^}%`\\]\">\\[~<#|]*)?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Uri" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "A path in S3"]
module OutputSegment =
  struct
    type nonrec t =
      {
      customOutputStatus: CustomOutputStatus.t option
        [@ocaml.doc "Status of blueprint match"];
      customOutput: String_.t option [@ocaml.doc "Custom output response"];
      standardOutput: String_.t option
        [@ocaml.doc "Standard output response"]}
    let make ?customOutputStatus =
      fun ?customOutput ->
        fun ?standardOutput ->
          fun () -> { customOutputStatus; customOutput; standardOutput }
    let to_value x =
      structure_to_value
        [("customOutputStatus",
           (Option.map x.customOutputStatus ~f:CustomOutputStatus.to_value));
        ("customOutput", (Option.map x.customOutput ~f:String_.to_value));
        ("standardOutput", (Option.map x.standardOutput ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let standardOutput =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "standardOutput") in
      let customOutput =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "customOutput") in
      let customOutputStatus =
        (Option.map ~f:CustomOutputStatus.of_xml)
          (Xml.child xml_arg0 "customOutputStatus") in
      make ?standardOutput ?customOutput ?customOutputStatus ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let standardOutput = field_map json__ "standardOutput" String_.of_json in
      let customOutput = field_map json__ "customOutput" String_.of_json in
      let customOutputStatus =
        field_map json__ "customOutputStatus" CustomOutputStatus.of_json in
      make ?standardOutput ?customOutput ?customOutputStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Results for an output segment"]
module Blueprint =
  struct
    type nonrec t =
      {
      blueprintArn: BlueprintArn.t [@ocaml.doc "Arn of blueprint."];
      version: BlueprintVersion.t option [@ocaml.doc "Version of blueprint."];
      stage: BlueprintStage.t option [@ocaml.doc "Stage of blueprint."]}
    let context_ = "Blueprint"
    let make ?version =
      fun ?stage ->
        fun ~blueprintArn -> fun () -> { version; stage; blueprintArn }
    let to_value x =
      structure_to_value
        [("blueprintArn", (Some (BlueprintArn.to_value x.blueprintArn)));
        ("version", (Option.map x.version ~f:BlueprintVersion.to_value));
        ("stage", (Option.map x.stage ~f:BlueprintStage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stage =
        (Option.map ~f:BlueprintStage.of_xml) (Xml.child xml_arg0 "stage") in
      let version =
        (Option.map ~f:BlueprintVersion.of_xml)
          (Xml.child xml_arg0 "version") in
      let blueprintArn =
        BlueprintArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "blueprintArn") in
      make ?stage ?version ~blueprintArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stage = field_map json__ "stage" BlueprintStage.of_json in
      let version = field_map json__ "version" BlueprintVersion.of_json in
      let blueprintArn =
        field_map_exn json__ "blueprintArn" BlueprintArn.of_json in
      make ?stage ?version ~blueprintArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Structure for single blueprint entity."]
module DataAutomationArn =
  struct
    type nonrec t = string[@@ocaml.doc "Data automation arn."]
    let context_ = "DataAutomationArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(|-cn|-iso|-iso-[a-z]|-us-gov):bedrock:[a-zA-Z0-9-]*:(aws|[0-9]{12}):data-automation-project/[a-zA-Z0-9-_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DataAutomationArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Data automation arn."]
module DataAutomationStage =
  struct
    type nonrec t =
      | LIVE 
      | DEVELOPMENT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LIVE -> "LIVE"
      | DEVELOPMENT -> "DEVELOPMENT"
      | Non_static_id s -> s
    let of_string =
      function
      | "LIVE" -> LIVE
      | "DEVELOPMENT" -> DEVELOPMENT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DataAutomationStage" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DataAutomationStage" j)
    let to_json = simple_to_json to_value
  end
module EncryptionContextMap =
  struct
    type nonrec t = (EncryptionContextKey.t * EncryptionContextValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((EncryptionContextKey.of_string chopped),
                              (EncryptionContextValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (EncryptionContextKey.to_value x) |>
                    (fun x ->
                       (EncryptionContextValue.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:EncryptionContextKey.of_string
        ~of_json:EncryptionContextValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module KMSKeyId =
  struct
    type nonrec t = string
    let context_ = "KMSKeyId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[A-Za-z0-9][A-Za-z0-9:_/+=,@.-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KMSKeyId" j
    let to_json = simple_to_json to_value
  end
module Blob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module AssetProcessingConfiguration =
  struct
    type nonrec t =
      {
      video: VideoAssetProcessingConfiguration.t option
        [@ocaml.doc "Video asset processing configuration"]}
    let make ?video = fun () -> { video }
    let to_value x =
      structure_to_value
        [("video",
           (Option.map x.video ~f:VideoAssetProcessingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let video =
        (Option.map ~f:VideoAssetProcessingConfiguration.of_xml)
          (Xml.child xml_arg0 "video") in
      make ?video ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let video =
        field_map json__ "video" VideoAssetProcessingConfiguration.of_json in
      make ?video ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Config containing asset processing related knobs for all modalities"]
module EventBridgeConfiguration =
  struct
    type nonrec t =
      {
      eventBridgeEnabled: Boolean.t [@ocaml.doc "Event bridge flag."]}
    let context_ = "EventBridgeConfiguration"
    let make ~eventBridgeEnabled = fun () -> { eventBridgeEnabled }
    let to_value x =
      structure_to_value
        [("eventBridgeEnabled",
           (Some (Boolean.to_value x.eventBridgeEnabled)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventBridgeEnabled =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "eventBridgeEnabled") in
      make ~eventBridgeEnabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eventBridgeEnabled =
        field_map_exn json__ "eventBridgeEnabled" Boolean.of_json in
      make ~eventBridgeEnabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Event bridge configuration."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception will be thrown when customer does not have access to API."]
module InternalServerException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception is for any internal un-expected service errors."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception will be thrown when resource provided from customer not found."]
module ThrottlingException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception will be thrown when customer reached API TPS limit."]
module ValidationException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception will be thrown when customer provided invalid parameters."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaggableResourceArn =
  struct
    type nonrec t = string[@@ocaml.doc "ARN of a taggable resource"]
    let context_ = "TaggableResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:20) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(|-cn|-iso|-iso-[a-z]|-us-gov):bedrock:[a-zA-Z0-9-]*:[0-9]{12}:data-automation-invocation/[a-zA-Z0-9-_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TaggableResourceArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "ARN of a taggable resource"]
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception will be thrown when service quota is exceeded."]
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module OutputConfiguration =
  struct
    type nonrec t = {
      s3Uri: S3Uri.t [@ocaml.doc "S3 uri."]}
    let context_ = "OutputConfiguration"
    let make ~s3Uri = fun () -> { s3Uri }
    let to_value x =
      structure_to_value [("s3Uri", (Some (S3Uri.to_value x.s3Uri)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Uri =
        S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "s3Uri") in
      make ~s3Uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Uri = field_map_exn json__ "s3Uri" S3Uri.of_json in
      make ~s3Uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Output configuration."]
module OutputSegmentList =
  struct
    type nonrec t = OutputSegment.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:OutputSegment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OutputSegment.of_xml)
    let of_json j =
      list_of_json ~kind:"OutputSegmentList" ~of_json:OutputSegment.of_json j
    let to_json v = composed_to_json to_value v
  end
module SemanticModality =
  struct
    type nonrec t =
      | DOCUMENT 
      | IMAGE 
      | AUDIO 
      | VIDEO 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DOCUMENT -> "DOCUMENT"
      | IMAGE -> "IMAGE"
      | AUDIO -> "AUDIO"
      | VIDEO -> "VIDEO"
      | Non_static_id s -> s
    let of_string =
      function
      | "DOCUMENT" -> DOCUMENT
      | "IMAGE" -> IMAGE
      | "AUDIO" -> AUDIO
      | "VIDEO" -> VIDEO
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SemanticModality" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SemanticModality" j)
    let to_json = simple_to_json to_value
  end
module ServiceUnavailableException =
  struct
    type nonrec t = {
      message: NonBlankString.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:NonBlankString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:NonBlankString.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" NonBlankString.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception will be thrown when service is temporarily unavailable."]
module BlueprintList =
  struct
    type nonrec t = Blueprint.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:40) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Blueprint.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Blueprint.of_xml)
    let of_json j =
      list_of_json ~kind:"BlueprintList" ~of_json:Blueprint.of_json j
    let to_json v = composed_to_json to_value v
  end
module DataAutomationConfiguration =
  struct
    type nonrec t =
      {
      dataAutomationProjectArn: DataAutomationArn.t
        [@ocaml.doc "Data automation project arn."];
      stage: DataAutomationStage.t option
        [@ocaml.doc "Data automation stage."]}
    let context_ = "DataAutomationConfiguration"
    let make ?stage =
      fun ~dataAutomationProjectArn ->
        fun () -> { stage; dataAutomationProjectArn }
    let to_value x =
      structure_to_value
        [("dataAutomationProjectArn",
           (Some (DataAutomationArn.to_value x.dataAutomationProjectArn)));
        ("stage", (Option.map x.stage ~f:DataAutomationStage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stage =
        (Option.map ~f:DataAutomationStage.of_xml)
          (Xml.child xml_arg0 "stage") in
      let dataAutomationProjectArn =
        DataAutomationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "dataAutomationProjectArn") in
      make ?stage ~dataAutomationProjectArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stage = field_map json__ "stage" DataAutomationStage.of_json in
      let dataAutomationProjectArn =
        field_map_exn json__ "dataAutomationProjectArn"
          DataAutomationArn.of_json in
      make ?stage ~dataAutomationProjectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Data automation configuration."]
module DataAutomationProfileArn =
  struct
    type nonrec t = string[@@ocaml.doc "Data automation profile arn."]
    let context_ = "DataAutomationProfileArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(|-cn|-iso|-iso-[a-z]|-us-gov):bedrock:[a-zA-Z0-9-]*:(aws|[0-9]{12}):data-automation-profile/[a-zA-Z0-9-_.]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DataAutomationProfileArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Data automation profile arn."]
module EncryptionConfiguration =
  struct
    type nonrec t =
      {
      kmsKeyId: KMSKeyId.t
        [@ocaml.doc "Customer KMS key used for encryption"];
      kmsEncryptionContext: EncryptionContextMap.t option
        [@ocaml.doc "KMS encryption context."]}
    let context_ = "EncryptionConfiguration"
    let make ?kmsEncryptionContext =
      fun ~kmsKeyId -> fun () -> { kmsEncryptionContext; kmsKeyId }
    let to_value x =
      structure_to_value
        [("kmsKeyId", (Some (KMSKeyId.to_value x.kmsKeyId)));
        ("kmsEncryptionContext",
          (Option.map x.kmsEncryptionContext ~f:EncryptionContextMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsEncryptionContext =
        (Option.map ~f:EncryptionContextMap.of_xml)
          (Xml.child xml_arg0 "kmsEncryptionContext") in
      let kmsKeyId =
        KMSKeyId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "kmsKeyId") in
      make ?kmsEncryptionContext ~kmsKeyId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let kmsEncryptionContext =
        field_map json__ "kmsEncryptionContext" EncryptionContextMap.of_json in
      let kmsKeyId = field_map_exn json__ "kmsKeyId" KMSKeyId.of_json in
      make ?kmsEncryptionContext ~kmsKeyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Encryption configuration."]
module SyncInputConfiguration =
  struct
    type nonrec t =
      {
      bytes: Blob.t option [@ocaml.doc "Input data as bytes"];
      s3Uri: S3Uri.t option [@ocaml.doc "S3 URI of the input data"]}
    let make ?bytes = fun ?s3Uri -> fun () -> { bytes; s3Uri }
    let to_value x =
      structure_to_value
        [("bytes", (Option.map x.bytes ~f:Blob.to_value));
        ("s3Uri", (Option.map x.s3Uri ~f:S3Uri.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Uri = (Option.map ~f:S3Uri.of_xml) (Xml.child xml_arg0 "s3Uri") in
      let bytes = (Option.map ~f:Blob.of_xml) (Xml.child xml_arg0 "bytes") in
      make ?s3Uri ?bytes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let s3Uri = field_map json__ "s3Uri" S3Uri.of_json in
      let bytes = field_map json__ "bytes" Blob.of_json in
      make ?s3Uri ?bytes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Input configuration for synchronous API"]
module InvocationArn =
  struct
    type nonrec t = string[@@ocaml.doc "Invocation arn."]
    let context_ = "InvocationArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(|-cn|-iso|-iso-[a-z]|-us-gov):bedrock:[a-zA-Z0-9-]*:[0-9]{12}:data-automation-invocation/[a-zA-Z0-9-_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InvocationArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Invocation arn."]
module IdempotencyToken =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Client specified token used for idempotency checks"]
    let context_ = "IdempotencyToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9](-*[a-zA-Z0-9]){1,256}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IdempotencyToken" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Client specified token used for idempotency checks"]
module InputConfiguration =
  struct
    type nonrec t =
      {
      s3Uri: S3Uri.t [@ocaml.doc "S3 uri."];
      assetProcessingConfiguration: AssetProcessingConfiguration.t option
        [@ocaml.doc "Asset processing configuration"]}
    let context_ = "InputConfiguration"
    let make ?assetProcessingConfiguration =
      fun ~s3Uri -> fun () -> { assetProcessingConfiguration; s3Uri }
    let to_value x =
      structure_to_value
        [("s3Uri", (Some (S3Uri.to_value x.s3Uri)));
        ("assetProcessingConfiguration",
          (Option.map x.assetProcessingConfiguration
             ~f:AssetProcessingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let assetProcessingConfiguration =
        (Option.map ~f:AssetProcessingConfiguration.of_xml)
          (Xml.child xml_arg0 "assetProcessingConfiguration") in
      let s3Uri =
        S3Uri.of_xml (Xml.child_exn ~context:context_ xml_arg0 "s3Uri") in
      make ?assetProcessingConfiguration ~s3Uri ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let assetProcessingConfiguration =
        field_map json__ "assetProcessingConfiguration"
          AssetProcessingConfiguration.of_json in
      let s3Uri = field_map_exn json__ "s3Uri" S3Uri.of_json in
      make ?assetProcessingConfiguration ~s3Uri ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Input configuration."]
module NotificationConfiguration =
  struct
    type nonrec t =
      {
      eventBridgeConfiguration: EventBridgeConfiguration.t
        [@ocaml.doc "Event bridge configuration."]}
    let context_ = "NotificationConfiguration"
    let make ~eventBridgeConfiguration =
      fun () -> { eventBridgeConfiguration }
    let to_value x =
      structure_to_value
        [("eventBridgeConfiguration",
           (Some
              (EventBridgeConfiguration.to_value x.eventBridgeConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eventBridgeConfiguration =
        EventBridgeConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "eventBridgeConfiguration") in
      make ~eventBridgeConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let eventBridgeConfiguration =
        field_map_exn json__ "eventBridgeConfiguration"
          EventBridgeConfiguration.of_json in
      make ~eventBridgeConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Notification configuration."]
module AutomationJobStatus =
  struct
    type nonrec t =
      | Created 
      | InProgress 
      | Success 
      | ServiceError 
      | ClientError 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Created -> "Created"
      | InProgress -> "InProgress"
      | Success -> "Success"
      | ServiceError -> "ServiceError"
      | ClientError -> "ClientError"
      | Non_static_id s -> s
    let of_string =
      function
      | "Created" -> Created
      | "InProgress" -> InProgress
      | "Success" -> Success
      | "ServiceError" -> ServiceError
      | "ClientError" -> ClientError
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AutomationJobStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AutomationJobStatus" j)
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SyntheticTimestamp_date_time =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Untag an Amazon Bedrock Data Automation resource"]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: TaggableResourceArn.t ;
      tagKeys: TagKeyList.t }
    let context_ = "UntagResourceRequest"
    let make ~resourceARN =
      fun ~tagKeys -> fun () -> { resourceARN; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceARN", (Some (TaggableResourceArn.to_value x.resourceARN)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceARN =
        TaggableResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceARN") in
      make ~tagKeys ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "tagKeys" TagKeyList.of_json in
      let resourceARN =
        field_map_exn json__ "resourceARN" TaggableResourceArn.of_json in
      make ~tagKeys ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Untag an Amazon Bedrock Data Automation resource"]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Tag an Amazon Bedrock Data Automation resource"]
module TagResourceRequest =
  struct
    type nonrec t = {
      resourceARN: TaggableResourceArn.t ;
      tags: TagList.t }
    let context_ = "TagResourceRequest"
    let make ~resourceARN = fun ~tags -> fun () -> { resourceARN; tags }
    let to_value x =
      structure_to_value
        [("resourceARN", (Some (TaggableResourceArn.to_value x.resourceARN)));
        ("tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceARN =
        TaggableResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceARN") in
      make ~tags ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagList.of_json in
      let resourceARN =
        field_map_exn json__ "resourceARN" TaggableResourceArn.of_json in
      make ~tags ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Tag an Amazon Bedrock Data Automation resource"]
module ListTagsForResourceResponse =
  struct
    type nonrec t = {
      tags: TagList.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List tags for an Amazon Bedrock Data Automation resource"]
module ListTagsForResourceRequest =
  struct
    type nonrec t = {
      resourceARN: TaggableResourceArn.t }
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceARN = fun () -> { resourceARN }
    let to_value x =
      structure_to_value
        [("resourceARN", (Some (TaggableResourceArn.to_value x.resourceARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceARN =
        TaggableResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceARN") in
      make ~resourceARN ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceARN =
        field_map_exn json__ "resourceARN" TaggableResourceArn.of_json in
      make ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List tags for an Amazon Bedrock Data Automation resource"]
module InvokeDataAutomationResponse =
  struct
    type nonrec t =
      {
      outputConfiguration: OutputConfiguration.t option
        [@ocaml.doc "Output configuration"];
      semanticModality: SemanticModality.t option
        [@ocaml.doc "Detected semantic modality"];
      outputSegments: OutputSegmentList.t option
        [@ocaml.doc "List of outputs for each logical sub-doc"]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?outputConfiguration =
      fun ?semanticModality ->
        fun ?outputSegments ->
          fun () -> { outputConfiguration; semanticModality; outputSegments }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("outputConfiguration",
           (Option.map x.outputConfiguration ~f:OutputConfiguration.to_value));
        ("semanticModality",
          (Option.map x.semanticModality ~f:SemanticModality.to_value));
        ("outputSegments",
          (Option.map x.outputSegments ~f:OutputSegmentList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputSegments =
        (Option.map ~f:OutputSegmentList.of_xml)
          (Xml.child xml_arg0 "outputSegments") in
      let semanticModality =
        (Option.map ~f:SemanticModality.of_xml)
          (Xml.child xml_arg0 "semanticModality") in
      let outputConfiguration =
        (Option.map ~f:OutputConfiguration.of_xml)
          (Xml.child xml_arg0 "outputConfiguration") in
      make ?outputSegments ?semanticModality ?outputConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputSegments =
        field_map json__ "outputSegments" OutputSegmentList.of_json in
      let semanticModality =
        field_map json__ "semanticModality" SemanticModality.of_json in
      let outputConfiguration =
        field_map json__ "outputConfiguration" OutputConfiguration.of_json in
      make ?outputSegments ?semanticModality ?outputConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Invoke Data Automation Response"]
module InvokeDataAutomationRequest =
  struct
    type nonrec t =
      {
      inputConfiguration: SyncInputConfiguration.t
        [@ocaml.doc "Input configuration."];
      dataAutomationConfiguration: DataAutomationConfiguration.t option
        [@ocaml.doc "Data automation configuration."];
      blueprints: BlueprintList.t option [@ocaml.doc "Blueprint list."];
      dataAutomationProfileArn: DataAutomationProfileArn.t
        [@ocaml.doc "Data automation profile ARN"];
      encryptionConfiguration: EncryptionConfiguration.t option
        [@ocaml.doc "Encryption configuration."];
      outputConfiguration: OutputConfiguration.t option
        [@ocaml.doc "Output configuration."]}
    let context_ = "InvokeDataAutomationRequest"
    let make ?dataAutomationConfiguration =
      fun ?blueprints ->
        fun ?encryptionConfiguration ->
          fun ?outputConfiguration ->
            fun ~inputConfiguration ->
              fun ~dataAutomationProfileArn ->
                fun () ->
                  {
                    dataAutomationConfiguration;
                    blueprints;
                    encryptionConfiguration;
                    outputConfiguration;
                    inputConfiguration;
                    dataAutomationProfileArn
                  }
    let to_value x =
      structure_to_value
        [("inputConfiguration",
           (Some (SyncInputConfiguration.to_value x.inputConfiguration)));
        ("dataAutomationConfiguration",
          (Option.map x.dataAutomationConfiguration
             ~f:DataAutomationConfiguration.to_value));
        ("blueprints", (Option.map x.blueprints ~f:BlueprintList.to_value));
        ("dataAutomationProfileArn",
          (Some
             (DataAutomationProfileArn.to_value x.dataAutomationProfileArn)));
        ("encryptionConfiguration",
          (Option.map x.encryptionConfiguration
             ~f:EncryptionConfiguration.to_value));
        ("outputConfiguration",
          (Option.map x.outputConfiguration ~f:OutputConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputConfiguration =
        (Option.map ~f:OutputConfiguration.of_xml)
          (Xml.child xml_arg0 "outputConfiguration") in
      let encryptionConfiguration =
        (Option.map ~f:EncryptionConfiguration.of_xml)
          (Xml.child xml_arg0 "encryptionConfiguration") in
      let dataAutomationProfileArn =
        DataAutomationProfileArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "dataAutomationProfileArn") in
      let blueprints =
        (Option.map ~f:BlueprintList.of_xml)
          (Xml.child xml_arg0 "blueprints") in
      let dataAutomationConfiguration =
        (Option.map ~f:DataAutomationConfiguration.of_xml)
          (Xml.child xml_arg0 "dataAutomationConfiguration") in
      let inputConfiguration =
        SyncInputConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "inputConfiguration") in
      make ?outputConfiguration ?encryptionConfiguration
        ~dataAutomationProfileArn ?blueprints ?dataAutomationConfiguration
        ~inputConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputConfiguration =
        field_map json__ "outputConfiguration" OutputConfiguration.of_json in
      let encryptionConfiguration =
        field_map json__ "encryptionConfiguration"
          EncryptionConfiguration.of_json in
      let dataAutomationProfileArn =
        field_map_exn json__ "dataAutomationProfileArn"
          DataAutomationProfileArn.of_json in
      let blueprints = field_map json__ "blueprints" BlueprintList.of_json in
      let dataAutomationConfiguration =
        field_map json__ "dataAutomationConfiguration"
          DataAutomationConfiguration.of_json in
      let inputConfiguration =
        field_map_exn json__ "inputConfiguration"
          SyncInputConfiguration.of_json in
      make ?outputConfiguration ?encryptionConfiguration
        ~dataAutomationProfileArn ?blueprints ?dataAutomationConfiguration
        ~inputConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Invoke Data Automation Request"]
module InvokeDataAutomationAsyncResponse =
  struct
    type nonrec t =
      {
      invocationArn: InvocationArn.t option
        [@ocaml.doc "ARN of the automation job"]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?invocationArn = fun () -> { invocationArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("invocationArn",
           (Option.map x.invocationArn ~f:InvocationArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationArn =
        (Option.map ~f:InvocationArn.of_xml)
          (Xml.child xml_arg0 "invocationArn") in
      make ?invocationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invocationArn =
        field_map json__ "invocationArn" InvocationArn.of_json in
      make ?invocationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Invoke Data Automation Async Response"]
module InvokeDataAutomationAsyncRequest =
  struct
    type nonrec t =
      {
      clientToken: IdempotencyToken.t option
        [@ocaml.doc "Idempotency token."];
      inputConfiguration: InputConfiguration.t
        [@ocaml.doc "Input configuration."];
      outputConfiguration: OutputConfiguration.t
        [@ocaml.doc "Output configuration."];
      dataAutomationConfiguration: DataAutomationConfiguration.t option
        [@ocaml.doc "Data automation configuration."];
      encryptionConfiguration: EncryptionConfiguration.t option
        [@ocaml.doc "Encryption configuration."];
      notificationConfiguration: NotificationConfiguration.t option
        [@ocaml.doc "Notification configuration."];
      blueprints: BlueprintList.t option [@ocaml.doc "Blueprint list."];
      dataAutomationProfileArn: DataAutomationProfileArn.t
        [@ocaml.doc "Data automation profile ARN"];
      tags: TagList.t option [@ocaml.doc "List of tags."]}
    let context_ = "InvokeDataAutomationAsyncRequest"
    let make ?clientToken =
      fun ?dataAutomationConfiguration ->
        fun ?encryptionConfiguration ->
          fun ?notificationConfiguration ->
            fun ?blueprints ->
              fun ?tags ->
                fun ~inputConfiguration ->
                  fun ~outputConfiguration ->
                    fun ~dataAutomationProfileArn ->
                      fun () ->
                        {
                          clientToken;
                          dataAutomationConfiguration;
                          encryptionConfiguration;
                          notificationConfiguration;
                          blueprints;
                          tags;
                          inputConfiguration;
                          outputConfiguration;
                          dataAutomationProfileArn
                        }
    let to_value x =
      structure_to_value
        [("clientToken",
           (Option.map x.clientToken ~f:IdempotencyToken.to_value));
        ("inputConfiguration",
          (Some (InputConfiguration.to_value x.inputConfiguration)));
        ("outputConfiguration",
          (Some (OutputConfiguration.to_value x.outputConfiguration)));
        ("dataAutomationConfiguration",
          (Option.map x.dataAutomationConfiguration
             ~f:DataAutomationConfiguration.to_value));
        ("encryptionConfiguration",
          (Option.map x.encryptionConfiguration
             ~f:EncryptionConfiguration.to_value));
        ("notificationConfiguration",
          (Option.map x.notificationConfiguration
             ~f:NotificationConfiguration.to_value));
        ("blueprints", (Option.map x.blueprints ~f:BlueprintList.to_value));
        ("dataAutomationProfileArn",
          (Some
             (DataAutomationProfileArn.to_value x.dataAutomationProfileArn)));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let dataAutomationProfileArn =
        DataAutomationProfileArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "dataAutomationProfileArn") in
      let blueprints =
        (Option.map ~f:BlueprintList.of_xml)
          (Xml.child xml_arg0 "blueprints") in
      let notificationConfiguration =
        (Option.map ~f:NotificationConfiguration.of_xml)
          (Xml.child xml_arg0 "notificationConfiguration") in
      let encryptionConfiguration =
        (Option.map ~f:EncryptionConfiguration.of_xml)
          (Xml.child xml_arg0 "encryptionConfiguration") in
      let dataAutomationConfiguration =
        (Option.map ~f:DataAutomationConfiguration.of_xml)
          (Xml.child xml_arg0 "dataAutomationConfiguration") in
      let outputConfiguration =
        OutputConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outputConfiguration") in
      let inputConfiguration =
        InputConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "inputConfiguration") in
      let clientToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "clientToken") in
      make ?tags ~dataAutomationProfileArn ?blueprints
        ?notificationConfiguration ?encryptionConfiguration
        ?dataAutomationConfiguration ~outputConfiguration ~inputConfiguration
        ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let dataAutomationProfileArn =
        field_map_exn json__ "dataAutomationProfileArn"
          DataAutomationProfileArn.of_json in
      let blueprints = field_map json__ "blueprints" BlueprintList.of_json in
      let notificationConfiguration =
        field_map json__ "notificationConfiguration"
          NotificationConfiguration.of_json in
      let encryptionConfiguration =
        field_map json__ "encryptionConfiguration"
          EncryptionConfiguration.of_json in
      let dataAutomationConfiguration =
        field_map json__ "dataAutomationConfiguration"
          DataAutomationConfiguration.of_json in
      let outputConfiguration =
        field_map_exn json__ "outputConfiguration"
          OutputConfiguration.of_json in
      let inputConfiguration =
        field_map_exn json__ "inputConfiguration" InputConfiguration.of_json in
      let clientToken =
        field_map json__ "clientToken" IdempotencyToken.of_json in
      make ?tags ~dataAutomationProfileArn ?blueprints
        ?notificationConfiguration ?encryptionConfiguration
        ?dataAutomationConfiguration ~outputConfiguration ~inputConfiguration
        ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Invoke Data Automation Async Request"]
module GetDataAutomationStatusResponse =
  struct
    type nonrec t =
      {
      status: AutomationJobStatus.t option [@ocaml.doc "Job Status."];
      errorType: String_.t option [@ocaml.doc "Error Type."];
      errorMessage: String_.t option [@ocaml.doc "Error Message."];
      outputConfiguration: OutputConfiguration.t option
        [@ocaml.doc "Output configuration."];
      jobSubmissionTime: SyntheticTimestamp_date_time.t option
        [@ocaml.doc "Job Submission time."];
      jobCompletionTime: SyntheticTimestamp_date_time.t option
        [@ocaml.doc "Job completion time."];
      jobDurationInSeconds: Integer.t option
        [@ocaml.doc "Job duration in seconds."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status =
      fun ?errorType ->
        fun ?errorMessage ->
          fun ?outputConfiguration ->
            fun ?jobSubmissionTime ->
              fun ?jobCompletionTime ->
                fun ?jobDurationInSeconds ->
                  fun () ->
                    {
                      status;
                      errorType;
                      errorMessage;
                      outputConfiguration;
                      jobSubmissionTime;
                      jobCompletionTime;
                      jobDurationInSeconds
                    }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:AutomationJobStatus.to_value));
        ("errorType", (Option.map x.errorType ~f:String_.to_value));
        ("errorMessage", (Option.map x.errorMessage ~f:String_.to_value));
        ("outputConfiguration",
          (Option.map x.outputConfiguration ~f:OutputConfiguration.to_value));
        ("jobSubmissionTime",
          (Option.map x.jobSubmissionTime
             ~f:SyntheticTimestamp_date_time.to_value));
        ("jobCompletionTime",
          (Option.map x.jobCompletionTime
             ~f:SyntheticTimestamp_date_time.to_value));
        ("jobDurationInSeconds",
          (Option.map x.jobDurationInSeconds ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobDurationInSeconds =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "jobDurationInSeconds") in
      let jobCompletionTime =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "jobCompletionTime") in
      let jobSubmissionTime =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "jobSubmissionTime") in
      let outputConfiguration =
        (Option.map ~f:OutputConfiguration.of_xml)
          (Xml.child xml_arg0 "outputConfiguration") in
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorMessage") in
      let errorType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorType") in
      let status =
        (Option.map ~f:AutomationJobStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      make ?jobDurationInSeconds ?jobCompletionTime ?jobSubmissionTime
        ?outputConfiguration ?errorMessage ?errorType ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let jobDurationInSeconds =
        field_map json__ "jobDurationInSeconds" Integer.of_json in
      let jobCompletionTime =
        field_map json__ "jobCompletionTime"
          SyntheticTimestamp_date_time.of_json in
      let jobSubmissionTime =
        field_map json__ "jobSubmissionTime"
          SyntheticTimestamp_date_time.of_json in
      let outputConfiguration =
        field_map json__ "outputConfiguration" OutputConfiguration.of_json in
      let errorMessage = field_map json__ "errorMessage" String_.of_json in
      let errorType = field_map json__ "errorType" String_.of_json in
      let status = field_map json__ "status" AutomationJobStatus.of_json in
      make ?jobDurationInSeconds ?jobCompletionTime ?jobSubmissionTime
        ?outputConfiguration ?errorMessage ?errorType ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Response of GetDataAutomationStatus API."]
module GetDataAutomationStatusRequest =
  struct
    type nonrec t =
      {
      invocationArn: InvocationArn.t [@ocaml.doc "Invocation arn."]}
    let context_ = "GetDataAutomationStatusRequest"
    let make ~invocationArn = fun () -> { invocationArn }
    let to_value x =
      structure_to_value
        [("invocationArn", (Some (InvocationArn.to_value x.invocationArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invocationArn =
        InvocationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "invocationArn") in
      make ~invocationArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invocationArn =
        field_map_exn json__ "invocationArn" InvocationArn.of_json in
      make ~invocationArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Structure for request of GetDataAutomationStatus API."]
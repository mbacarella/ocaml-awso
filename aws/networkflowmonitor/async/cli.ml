(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and remoteResources =
         flag "remote-resources" (optional json_arg)
           ~doc:"JSON MonitorRemoteResources"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING UuidString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and localResources =
         flag "local-resources" (required json_arg)
           ~doc:"JSON CreateMonitorInputLocalResourcesList"
       and scopeArn = flag "scope-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_monitor
           (Values.CreateMonitorInput.make
              ?remoteResources:(Option.map
                                  ~f:Values.MonitorRemoteResources.of_json
                                  remoteResources) ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~monitorName
              ~localResources:(Values.CreateMonitorInputLocalResourcesList.of_json
                                 localResources) ~scopeArn ())
           (Some Values.CreateMonitorOutput.to_json)
           (Some Values.CreateMonitorOutput.error_to_json)])
let create_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING UuidString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and targets =
         flag "targets" (required json_arg)
           ~doc:"JSON CreateScopeInputTargetsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_scope
           (Values.CreateScopeInput.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~targets:(Values.CreateScopeInputTargetsList.of_json targets)
              ()) (Some Values.CreateScopeOutput.to_json)
           (Some Values.CreateScopeOutput.error_to_json)])
let delete_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_monitor (Values.DeleteMonitorInput.make ~monitorName ())
           (Some Values.DeleteMonitorOutput.to_json)
           (Some Values.DeleteMonitorOutput.error_to_json)])
let delete_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scope (Values.DeleteScopeInput.make ~scopeId ())
           (Some Values.DeleteScopeOutput.to_json)
           (Some Values.DeleteScopeOutput.error_to_json)])
let get_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_monitor (Values.GetMonitorInput.make ~monitorName ())
           (Some Values.GetMonitorOutput.to_json)
           (Some Values.GetMonitorOutput.error_to_json)])
let get_query_results_monitor_top_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_results_monitor_top_contributors
           (Values.GetQueryResultsMonitorTopContributorsInput.make ?nextToken
              ?maxResults ~monitorName ~queryId ())
           (Some Values.GetQueryResultsMonitorTopContributorsOutput.to_json)
           (Some
              Values.GetQueryResultsMonitorTopContributorsOutput.error_to_json)])
let get_query_results_workload_insights_top_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_results_workload_insights_top_contributors
           (Values.GetQueryResultsWorkloadInsightsTopContributorsInput.make
              ?nextToken ?maxResults ~scopeId ~queryId ())
           (Some
              Values.GetQueryResultsWorkloadInsightsTopContributorsOutput.to_json)
           (Some
              Values.GetQueryResultsWorkloadInsightsTopContributorsOutput.error_to_json)])
let get_query_results_workload_insights_top_contributors_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_results_workload_insights_top_contributors_data
           (Values.GetQueryResultsWorkloadInsightsTopContributorsDataInput.make
              ?nextToken ?maxResults ~scopeId ~queryId ())
           (Some
              Values.GetQueryResultsWorkloadInsightsTopContributorsDataOutput.to_json)
           (Some
              Values.GetQueryResultsWorkloadInsightsTopContributorsDataOutput.error_to_json)])
let get_query_status_monitor_top_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_status_monitor_top_contributors
           (Values.GetQueryStatusMonitorTopContributorsInput.make
              ~monitorName ~queryId ())
           (Some Values.GetQueryStatusMonitorTopContributorsOutput.to_json)
           (Some
              Values.GetQueryStatusMonitorTopContributorsOutput.error_to_json)])
let get_query_status_workload_insights_top_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_status_workload_insights_top_contributors
           (Values.GetQueryStatusWorkloadInsightsTopContributorsInput.make
              ~scopeId ~queryId ())
           (Some
              Values.GetQueryStatusWorkloadInsightsTopContributorsOutput.to_json)
           (Some
              Values.GetQueryStatusWorkloadInsightsTopContributorsOutput.error_to_json)])
let get_query_status_workload_insights_top_contributors_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_status_workload_insights_top_contributors_data
           (Values.GetQueryStatusWorkloadInsightsTopContributorsDataInput.make
              ~scopeId ~queryId ())
           (Some
              Values.GetQueryStatusWorkloadInsightsTopContributorsDataOutput.to_json)
           (Some
              Values.GetQueryStatusWorkloadInsightsTopContributorsDataOutput.error_to_json)])
let get_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopeId =
         flag "scope-id" (required string)
           ~doc:"STRING GetScopeInputScopeIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_scope (Values.GetScopeInput.make ~scopeId ())
           (Some Values.GetScopeOutput.to_json)
           (Some Values.GetScopeOutput.error_to_json)])
let list_monitors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and monitorStatus =
         flag "monitor-status" (optional json_arg) ~doc:"JSON MonitorStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_monitors
           (Values.ListMonitorsInput.make ?nextToken ?maxResults
              ?monitorStatus:(Option.map ~f:Values.MonitorStatus.of_json
                                monitorStatus) ())
           (Some Values.ListMonitorsOutput.to_json)
           (Some Values.ListMonitorsOutput.error_to_json)])
let list_scopes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_scopes
           (Values.ListScopesInput.make ?nextToken ?maxResults ())
           (Some Values.ListScopesOutput.to_json)
           (Some Values.ListScopesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let start_query_monitor_top_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT Limit"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and startTime =
         flag "start-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and metricName =
         flag "metric-name" (required json_arg) ~doc:"JSON MonitorMetric"
       and destinationCategory =
         flag "destination-category" (required json_arg)
           ~doc:"JSON DestinationCategory" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_query_monitor_top_contributors
           (Values.StartQueryMonitorTopContributorsInput.make ?limit
              ~monitorName
              ~startTime:(Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ~endTime:(Values.SyntheticTimestamp_date_time.of_json endTime)
              ~metricName:(Values.MonitorMetric.of_json metricName)
              ~destinationCategory:(Values.DestinationCategory.of_json
                                      destinationCategory) ())
           (Some Values.StartQueryMonitorTopContributorsOutput.to_json)
           (Some Values.StartQueryMonitorTopContributorsOutput.error_to_json)])
let start_query_workload_insights_top_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT Limit"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId"
       and startTime =
         flag "start-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and metricName =
         flag "metric-name" (required json_arg)
           ~doc:"JSON WorkloadInsightsMetric"
       and destinationCategory =
         flag "destination-category" (required json_arg)
           ~doc:"JSON DestinationCategory" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_query_workload_insights_top_contributors
           (Values.StartQueryWorkloadInsightsTopContributorsInput.make ?limit
              ~scopeId
              ~startTime:(Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ~endTime:(Values.SyntheticTimestamp_date_time.of_json endTime)
              ~metricName:(Values.WorkloadInsightsMetric.of_json metricName)
              ~destinationCategory:(Values.DestinationCategory.of_json
                                      destinationCategory) ())
           (Some
              Values.StartQueryWorkloadInsightsTopContributorsOutput.to_json)
           (Some
              Values.StartQueryWorkloadInsightsTopContributorsOutput.error_to_json)])
let start_query_workload_insights_top_contributors_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId"
       and startTime =
         flag "start-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and metricName =
         flag "metric-name" (required json_arg)
           ~doc:"JSON WorkloadInsightsMetric"
       and destinationCategory =
         flag "destination-category" (required json_arg)
           ~doc:"JSON DestinationCategory" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_query_workload_insights_top_contributors_data
           (Values.StartQueryWorkloadInsightsTopContributorsDataInput.make
              ~scopeId
              ~startTime:(Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ~endTime:(Values.SyntheticTimestamp_date_time.of_json endTime)
              ~metricName:(Values.WorkloadInsightsMetric.of_json metricName)
              ~destinationCategory:(Values.DestinationCategory.of_json
                                      destinationCategory) ())
           (Some
              Values.StartQueryWorkloadInsightsTopContributorsDataOutput.to_json)
           (Some
              Values.StartQueryWorkloadInsightsTopContributorsDataOutput.error_to_json)])
let stop_query_monitor_top_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_query_monitor_top_contributors
           (Values.StopQueryMonitorTopContributorsInput.make ~monitorName
              ~queryId ())
           (Some Values.StopQueryMonitorTopContributorsOutput.to_json)
           (Some Values.StopQueryMonitorTopContributorsOutput.error_to_json)])
let stop_query_workload_insights_top_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_query_workload_insights_top_contributors
           (Values.StopQueryWorkloadInsightsTopContributorsInput.make
              ~scopeId ~queryId ())
           (Some
              Values.StopQueryWorkloadInsightsTopContributorsOutput.to_json)
           (Some
              Values.StopQueryWorkloadInsightsTopContributorsOutput.error_to_json)])
let stop_query_workload_insights_top_contributors_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_query_workload_insights_top_contributors_data
           (Values.StopQueryWorkloadInsightsTopContributorsDataInput.make
              ~scopeId ~queryId ())
           (Some
              Values.StopQueryWorkloadInsightsTopContributorsDataOutput.to_json)
           (Some
              Values.StopQueryWorkloadInsightsTopContributorsDataOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localResourcesToAdd =
         flag "local-resources-to-add" (optional json_arg)
           ~doc:"JSON MonitorLocalResources"
       and localResourcesToRemove =
         flag "local-resources-to-remove" (optional json_arg)
           ~doc:"JSON MonitorLocalResources"
       and remoteResourcesToAdd =
         flag "remote-resources-to-add" (optional json_arg)
           ~doc:"JSON MonitorRemoteResources"
       and remoteResourcesToRemove =
         flag "remote-resources-to-remove" (optional json_arg)
           ~doc:"JSON MonitorRemoteResources"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING UuidString"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_monitor
           (Values.UpdateMonitorInput.make
              ?localResourcesToAdd:(Option.map
                                      ~f:Values.MonitorLocalResources.of_json
                                      localResourcesToAdd)
              ?localResourcesToRemove:(Option.map
                                         ~f:Values.MonitorLocalResources.of_json
                                         localResourcesToRemove)
              ?remoteResourcesToAdd:(Option.map
                                       ~f:Values.MonitorRemoteResources.of_json
                                       remoteResourcesToAdd)
              ?remoteResourcesToRemove:(Option.map
                                          ~f:Values.MonitorRemoteResources.of_json
                                          remoteResourcesToRemove)
              ?clientToken ~monitorName ())
           (Some Values.UpdateMonitorOutput.to_json)
           (Some Values.UpdateMonitorOutput.error_to_json)])
let update_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourcesToAdd =
         flag "resources-to-add" (optional json_arg)
           ~doc:"JSON UpdateScopeInputResourcesToAddList"
       and resourcesToDelete =
         flag "resources-to-delete" (optional json_arg)
           ~doc:"JSON UpdateScopeInputResourcesToDeleteList"
       and scopeId = flag "scope-id" (required string) ~doc:"STRING ScopeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_scope
           (Values.UpdateScopeInput.make
              ?resourcesToAdd:(Option.map
                                 ~f:Values.UpdateScopeInputResourcesToAddList.of_json
                                 resourcesToAdd)
              ?resourcesToDelete:(Option.map
                                    ~f:Values.UpdateScopeInputResourcesToDeleteList.of_json
                                    resourcesToDelete) ~scopeId ())
           (Some Values.UpdateScopeOutput.to_json)
           (Some Values.UpdateScopeOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-monitor", create_monitor);
    ("create-scope", create_scope);
    ("delete-monitor", delete_monitor);
    ("delete-scope", delete_scope);
    ("get-monitor", get_monitor);
    ("get-query-results-monitor-top-contributors",
      get_query_results_monitor_top_contributors);
    ("get-query-results-workload-insights-top-contributors",
      get_query_results_workload_insights_top_contributors);
    ("get-query-results-workload-insights-top-contributors-data",
      get_query_results_workload_insights_top_contributors_data);
    ("get-query-status-monitor-top-contributors",
      get_query_status_monitor_top_contributors);
    ("get-query-status-workload-insights-top-contributors",
      get_query_status_workload_insights_top_contributors);
    ("get-query-status-workload-insights-top-contributors-data",
      get_query_status_workload_insights_top_contributors_data);
    ("get-scope", get_scope);
    ("list-monitors", list_monitors);
    ("list-scopes", list_scopes);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-query-monitor-top-contributors",
      start_query_monitor_top_contributors);
    ("start-query-workload-insights-top-contributors",
      start_query_workload_insights_top_contributors);
    ("start-query-workload-insights-top-contributors-data",
      start_query_workload_insights_top_contributors_data);
    ("stop-query-monitor-top-contributors",
      stop_query_monitor_top_contributors);
    ("stop-query-workload-insights-top-contributors",
      stop_query_workload_insights_top_contributors);
    ("stop-query-workload-insights-top-contributors-data",
      stop_query_workload_insights_top_contributors_data);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-monitor", update_monitor);
    ("update-scope", update_scope)]
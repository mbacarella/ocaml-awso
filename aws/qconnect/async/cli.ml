(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let activate_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and versionNumber =
         flag "version-number" (required json_arg) ~doc:"JSON Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_message_template
           (Values.ActivateMessageTemplateRequest.make ~knowledgeBaseId
              ~messageTemplateId
              ~versionNumber:(Values.Version.of_json versionNumber) ())
           (Some Values.ActivateMessageTemplateResponse.to_json)
           (Some Values.ActivateMessageTemplateResponse.error_to_json)])
let create_a_i_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON AIAgentType"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON AIAgentConfiguration"
       and visibilityStatus =
         flag "visibility-status" (required json_arg)
           ~doc:"JSON VisibilityStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_a_i_agent
           (Values.CreateAIAgentRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?description
              ~assistantId ~name ~type_:(Values.AIAgentType.of_json type_)
              ~configuration:(Values.AIAgentConfiguration.of_json
                                configuration)
              ~visibilityStatus:(Values.VisibilityStatus.of_json
                                   visibilityStatus) ())
           (Some Values.CreateAIAgentResponse.to_json)
           (Some Values.CreateAIAgentResponse.error_to_json)])
let create_a_i_agent_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modifiedTime =
         flag "modified-time" (optional json_arg) ~doc:"JSON Timestamp"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiAgentId =
         flag "ai-agent-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_a_i_agent_version
           (Values.CreateAIAgentVersionRequest.make
              ?modifiedTime:(Option.map ~f:Values.Timestamp.of_json
                               modifiedTime) ?clientToken ~assistantId
              ~aiAgentId ())
           (Some Values.CreateAIAgentVersionResponse.to_json)
           (Some Values.CreateAIAgentVersionResponse.error_to_json)])
let create_a_i_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AIGuardrailDescription"
       and topicPolicyConfig =
         flag "topic-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailTopicPolicyConfig"
       and contentPolicyConfig =
         flag "content-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailContentPolicyConfig"
       and wordPolicyConfig =
         flag "word-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailWordPolicyConfig"
       and sensitiveInformationPolicyConfig =
         flag "sensitive-information-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailSensitiveInformationPolicyConfig"
       and contextualGroundingPolicyConfig =
         flag "contextual-grounding-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailContextualGroundingPolicyConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and blockedInputMessaging =
         flag "blocked-input-messaging" (required string)
           ~doc:"STRING AIGuardrailBlockedMessaging"
       and blockedOutputsMessaging =
         flag "blocked-outputs-messaging" (required string)
           ~doc:"STRING AIGuardrailBlockedMessaging"
       and visibilityStatus =
         flag "visibility-status" (required json_arg)
           ~doc:"JSON VisibilityStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_a_i_guardrail
           (Values.CreateAIGuardrailRequest.make ?clientToken ?description
              ?topicPolicyConfig:(Option.map
                                    ~f:Values.AIGuardrailTopicPolicyConfig.of_json
                                    topicPolicyConfig)
              ?contentPolicyConfig:(Option.map
                                      ~f:Values.AIGuardrailContentPolicyConfig.of_json
                                      contentPolicyConfig)
              ?wordPolicyConfig:(Option.map
                                   ~f:Values.AIGuardrailWordPolicyConfig.of_json
                                   wordPolicyConfig)
              ?sensitiveInformationPolicyConfig:(Option.map
                                                   ~f:Values.AIGuardrailSensitiveInformationPolicyConfig.of_json
                                                   sensitiveInformationPolicyConfig)
              ?contextualGroundingPolicyConfig:(Option.map
                                                  ~f:Values.AIGuardrailContextualGroundingPolicyConfig.of_json
                                                  contextualGroundingPolicyConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~assistantId
              ~name ~blockedInputMessaging ~blockedOutputsMessaging
              ~visibilityStatus:(Values.VisibilityStatus.of_json
                                   visibilityStatus) ())
           (Some Values.CreateAIGuardrailResponse.to_json)
           (Some Values.CreateAIGuardrailResponse.error_to_json)])
let create_a_i_guardrail_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modifiedTime =
         flag "modified-time" (optional json_arg) ~doc:"JSON Timestamp"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiGuardrailId =
         flag "ai-guardrail-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_a_i_guardrail_version
           (Values.CreateAIGuardrailVersionRequest.make
              ?modifiedTime:(Option.map ~f:Values.Timestamp.of_json
                               modifiedTime) ?clientToken ~assistantId
              ~aiGuardrailId ())
           (Some Values.CreateAIGuardrailVersionResponse.to_json)
           (Some Values.CreateAIGuardrailVersionResponse.error_to_json)])
let create_a_i_prompt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and inferenceConfiguration =
         flag "inference-configuration" (optional json_arg)
           ~doc:"JSON AIPromptInferenceConfiguration"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON AIPromptType"
       and templateConfiguration =
         flag "template-configuration" (required json_arg)
           ~doc:"JSON AIPromptTemplateConfiguration"
       and visibilityStatus =
         flag "visibility-status" (required json_arg)
           ~doc:"JSON VisibilityStatus"
       and templateType =
         flag "template-type" (required json_arg)
           ~doc:"JSON AIPromptTemplateType"
       and modelId =
         flag "model-id" (required string)
           ~doc:"STRING AIPromptModelIdentifier"
       and apiFormat =
         flag "api-format" (required json_arg) ~doc:"JSON AIPromptAPIFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_a_i_prompt
           (Values.CreateAIPromptRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?description
              ?inferenceConfiguration:(Option.map
                                         ~f:Values.AIPromptInferenceConfiguration.of_json
                                         inferenceConfiguration) ~assistantId
              ~name ~type_:(Values.AIPromptType.of_json type_)
              ~templateConfiguration:(Values.AIPromptTemplateConfiguration.of_json
                                        templateConfiguration)
              ~visibilityStatus:(Values.VisibilityStatus.of_json
                                   visibilityStatus)
              ~templateType:(Values.AIPromptTemplateType.of_json templateType)
              ~modelId
              ~apiFormat:(Values.AIPromptAPIFormat.of_json apiFormat) ())
           (Some Values.CreateAIPromptResponse.to_json)
           (Some Values.CreateAIPromptResponse.error_to_json)])
let create_a_i_prompt_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modifiedTime =
         flag "modified-time" (optional json_arg) ~doc:"JSON Timestamp"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiPromptId =
         flag "ai-prompt-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_a_i_prompt_version
           (Values.CreateAIPromptVersionRequest.make
              ?modifiedTime:(Option.map ~f:Values.Timestamp.of_json
                               modifiedTime) ?clientToken ~assistantId
              ~aiPromptId ())
           (Some Values.CreateAIPromptVersionResponse.to_json)
           (Some Values.CreateAIPromptVersionResponse.error_to_json)])
let create_assistant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (optional json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON AssistantType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_assistant
           (Values.CreateAssistantRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?serverSideEncryptionConfiguration:(Option.map
                                                    ~f:Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ~name ~type_:(Values.AssistantType.of_json type_) ())
           (Some Values.CreateAssistantResponse.to_json)
           (Some Values.CreateAssistantResponse.error_to_json)])
let create_assistant_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and associationType =
         flag "association-type" (required json_arg)
           ~doc:"JSON AssociationType"
       and association =
         flag "association" (required json_arg)
           ~doc:"JSON AssistantAssociationInputData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_assistant_association
           (Values.CreateAssistantAssociationRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~assistantId
              ~associationType:(Values.AssociationType.of_json
                                  associationType)
              ~association:(Values.AssistantAssociationInputData.of_json
                              association) ())
           (Some Values.CreateAssistantAssociationResponse.to_json)
           (Some Values.CreateAssistantAssociationResponse.error_to_json)])
let create_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and title = flag "title" (optional string) ~doc:"STRING ContentTitle"
       and overrideLinkOutUri =
         flag "override-link-out-uri" (optional string) ~doc:"STRING Uri"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON ContentMetadata"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING NonEmptyString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_content
           (Values.CreateContentRequest.make ?title ?overrideLinkOutUri
              ?metadata:(Option.map ~f:Values.ContentMetadata.of_json
                           metadata) ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~knowledgeBaseId
              ~name ~uploadId ()) (Some Values.CreateContentResponse.to_json)
           (Some Values.CreateContentResponse.error_to_json)])
let create_content_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and contentId =
         flag "content-id" (required string) ~doc:"STRING UuidOrArn"
       and associationType =
         flag "association-type" (required json_arg)
           ~doc:"JSON ContentAssociationType"
       and association =
         flag "association" (required json_arg)
           ~doc:"JSON ContentAssociationContents" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_content_association
           (Values.CreateContentAssociationRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~knowledgeBaseId
              ~contentId
              ~associationType:(Values.ContentAssociationType.of_json
                                  associationType)
              ~association:(Values.ContentAssociationContents.of_json
                              association) ())
           (Some Values.CreateContentAssociationResponse.to_json)
           (Some Values.CreateContentAssociationResponse.error_to_json)])
let create_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING NonEmptyString"
       and sourceConfiguration =
         flag "source-configuration" (optional json_arg)
           ~doc:"JSON SourceConfiguration"
       and renderingConfiguration =
         flag "rendering-configuration" (optional json_arg)
           ~doc:"JSON RenderingConfiguration"
       and vectorIngestionConfiguration =
         flag "vector-ingestion-configuration" (optional json_arg)
           ~doc:"JSON VectorIngestionConfiguration"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (optional json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and knowledgeBaseType =
         flag "knowledge-base-type" (required json_arg)
           ~doc:"JSON KnowledgeBaseType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_knowledge_base
           (Values.CreateKnowledgeBaseRequest.make ?clientToken
              ?sourceConfiguration:(Option.map
                                      ~f:Values.SourceConfiguration.of_json
                                      sourceConfiguration)
              ?renderingConfiguration:(Option.map
                                         ~f:Values.RenderingConfiguration.of_json
                                         renderingConfiguration)
              ?vectorIngestionConfiguration:(Option.map
                                               ~f:Values.VectorIngestionConfiguration.of_json
                                               vectorIngestionConfiguration)
              ?serverSideEncryptionConfiguration:(Option.map
                                                    ~f:Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ?description ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~name
              ~knowledgeBaseType:(Values.KnowledgeBaseType.of_json
                                    knowledgeBaseType) ())
           (Some Values.CreateKnowledgeBaseResponse.to_json)
           (Some Values.CreateKnowledgeBaseResponse.error_to_json)])
let create_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and content =
         flag "content" (optional json_arg)
           ~doc:"JSON MessageTemplateContentProvider"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and language =
         flag "language" (optional string) ~doc:"STRING LanguageCode"
       and sourceConfiguration =
         flag "source-configuration" (optional json_arg)
           ~doc:"JSON MessageTemplateSourceConfiguration"
       and defaultAttributes =
         flag "default-attributes" (optional json_arg)
           ~doc:"JSON MessageTemplateAttributes"
       and groupingConfiguration =
         flag "grouping-configuration" (optional json_arg)
           ~doc:"JSON GroupingConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and channelSubtype =
         flag "channel-subtype" (required json_arg)
           ~doc:"JSON ChannelSubtype" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_message_template
           (Values.CreateMessageTemplateRequest.make ?name
              ?content:(Option.map
                          ~f:Values.MessageTemplateContentProvider.of_json
                          content) ?description ?language
              ?sourceConfiguration:(Option.map
                                      ~f:Values.MessageTemplateSourceConfiguration.of_json
                                      sourceConfiguration)
              ?defaultAttributes:(Option.map
                                    ~f:Values.MessageTemplateAttributes.of_json
                                    defaultAttributes)
              ?groupingConfiguration:(Option.map
                                        ~f:Values.GroupingConfiguration.of_json
                                        groupingConfiguration) ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~knowledgeBaseId
              ~channelSubtype:(Values.ChannelSubtype.of_json channelSubtype)
              ()) (Some Values.CreateMessageTemplateResponse.to_json)
           (Some Values.CreateMessageTemplateResponse.error_to_json)])
let create_message_template_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and contentDisposition =
         flag "content-disposition" (required json_arg)
           ~doc:"JSON ContentDisposition"
       and name =
         flag "name" (required string) ~doc:"STRING AttachmentFileName"
       and body =
         flag "body" (required string) ~doc:"STRING NonEmptyUnlimitedString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_message_template_attachment
           (Values.CreateMessageTemplateAttachmentRequest.make ?clientToken
              ~knowledgeBaseId ~messageTemplateId
              ~contentDisposition:(Values.ContentDisposition.of_json
                                     contentDisposition) ~name ~body ())
           (Some Values.CreateMessageTemplateAttachmentResponse.to_json)
           (Some Values.CreateMessageTemplateAttachmentResponse.error_to_json)])
let create_message_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messageTemplateContentSha256 =
         flag "message-template-content-sha256" (optional string)
           ~doc:"STRING MessageTemplateContentSha256"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_message_template_version
           (Values.CreateMessageTemplateVersionRequest.make
              ?messageTemplateContentSha256 ~knowledgeBaseId
              ~messageTemplateId ())
           (Some Values.CreateMessageTemplateVersionResponse.to_json)
           (Some Values.CreateMessageTemplateVersionResponse.error_to_json)])
let create_quick_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional string)
           ~doc:"STRING QuickResponseType"
       and groupingConfiguration =
         flag "grouping-configuration" (optional json_arg)
           ~doc:"JSON GroupingConfiguration"
       and description =
         flag "description" (optional string)
           ~doc:"STRING QuickResponseDescription"
       and shortcutKey =
         flag "shortcut-key" (optional string) ~doc:"STRING ShortCutKey"
       and isActive = flag "is-active" (optional bool) ~doc:"BOOL Boolean"
       and channels =
         flag "channels" (optional json_arg) ~doc:"JSON Channels"
       and language =
         flag "language" (optional string) ~doc:"STRING LanguageCode"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING NonEmptyString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and name =
         flag "name" (required string) ~doc:"STRING QuickResponseName"
       and content =
         flag "content" (required json_arg)
           ~doc:"JSON QuickResponseDataProvider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_quick_response
           (Values.CreateQuickResponseRequest.make ?contentType
              ?groupingConfiguration:(Option.map
                                        ~f:Values.GroupingConfiguration.of_json
                                        groupingConfiguration) ?description
              ?shortcutKey ?isActive
              ?channels:(Option.map ~f:Values.Channels.of_json channels)
              ?language ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~knowledgeBaseId
              ~name
              ~content:(Values.QuickResponseDataProvider.of_json content) ())
           (Some Values.CreateQuickResponseResponse.to_json)
           (Some Values.CreateQuickResponseResponse.error_to_json)])
let create_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and tagFilter =
         flag "tag-filter" (optional json_arg) ~doc:"JSON TagFilter"
       and aiAgentConfiguration =
         flag "ai-agent-configuration" (optional json_arg)
           ~doc:"JSON AIAgentConfigurationMap"
       and contactArn =
         flag "contact-arn" (optional string) ~doc:"STRING GenericArn"
       and orchestratorConfigurationList =
         flag "orchestrator-configuration-list" (optional json_arg)
           ~doc:"JSON OrchestratorConfigurationList"
       and removeOrchestratorConfigurationList =
         flag "remove-orchestrator-configuration-list" (optional bool)
           ~doc:"BOOL Boolean"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_session
           (Values.CreateSessionRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?tagFilter:(Option.map ~f:Values.TagFilter.of_json tagFilter)
              ?aiAgentConfiguration:(Option.map
                                       ~f:Values.AIAgentConfigurationMap.of_json
                                       aiAgentConfiguration) ?contactArn
              ?orchestratorConfigurationList:(Option.map
                                                ~f:Values.OrchestratorConfigurationList.of_json
                                                orchestratorConfigurationList)
              ?removeOrchestratorConfigurationList ~assistantId ~name ())
           (Some Values.CreateSessionResponse.to_json)
           (Some Values.CreateSessionResponse.error_to_json)])
let deactivate_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and versionNumber =
         flag "version-number" (required json_arg) ~doc:"JSON Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_message_template
           (Values.DeactivateMessageTemplateRequest.make ~knowledgeBaseId
              ~messageTemplateId
              ~versionNumber:(Values.Version.of_json versionNumber) ())
           (Some Values.DeactivateMessageTemplateResponse.to_json)
           (Some Values.DeactivateMessageTemplateResponse.error_to_json)])
let delete_a_i_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiAgentId =
         flag "ai-agent-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_a_i_agent
           (Values.DeleteAIAgentRequest.make ~assistantId ~aiAgentId ())
           (Some Values.DeleteAIAgentResponse.to_json)
           (Some Values.DeleteAIAgentResponse.error_to_json)])
let delete_a_i_agent_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiAgentId =
         flag "ai-agent-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and versionNumber =
         flag "version-number" (required json_arg) ~doc:"JSON Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_a_i_agent_version
           (Values.DeleteAIAgentVersionRequest.make ~assistantId ~aiAgentId
              ~versionNumber:(Values.Version.of_json versionNumber) ())
           (Some Values.DeleteAIAgentVersionResponse.to_json)
           (Some Values.DeleteAIAgentVersionResponse.error_to_json)])
let delete_a_i_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiGuardrailId =
         flag "ai-guardrail-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_a_i_guardrail
           (Values.DeleteAIGuardrailRequest.make ~assistantId ~aiGuardrailId
              ()) (Some Values.DeleteAIGuardrailResponse.to_json)
           (Some Values.DeleteAIGuardrailResponse.error_to_json)])
let delete_a_i_guardrail_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiGuardrailId =
         flag "ai-guardrail-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and versionNumber =
         flag "version-number" (required json_arg) ~doc:"JSON Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_a_i_guardrail_version
           (Values.DeleteAIGuardrailVersionRequest.make ~assistantId
              ~aiGuardrailId
              ~versionNumber:(Values.Version.of_json versionNumber) ())
           (Some Values.DeleteAIGuardrailVersionResponse.to_json)
           (Some Values.DeleteAIGuardrailVersionResponse.error_to_json)])
let delete_a_i_prompt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiPromptId =
         flag "ai-prompt-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_a_i_prompt
           (Values.DeleteAIPromptRequest.make ~assistantId ~aiPromptId ())
           (Some Values.DeleteAIPromptResponse.to_json)
           (Some Values.DeleteAIPromptResponse.error_to_json)])
let delete_a_i_prompt_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiPromptId =
         flag "ai-prompt-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and versionNumber =
         flag "version-number" (required json_arg) ~doc:"JSON Version" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_a_i_prompt_version
           (Values.DeleteAIPromptVersionRequest.make ~assistantId ~aiPromptId
              ~versionNumber:(Values.Version.of_json versionNumber) ())
           (Some Values.DeleteAIPromptVersionResponse.to_json)
           (Some Values.DeleteAIPromptVersionResponse.error_to_json)])
let delete_assistant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_assistant
           (Values.DeleteAssistantRequest.make ~assistantId ())
           (Some Values.DeleteAssistantResponse.to_json)
           (Some Values.DeleteAssistantResponse.error_to_json)])
let delete_assistant_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantAssociationId =
         flag "assistant-association-id" (required string)
           ~doc:"STRING UuidOrArn"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_assistant_association
           (Values.DeleteAssistantAssociationRequest.make
              ~assistantAssociationId ~assistantId ())
           (Some Values.DeleteAssistantAssociationResponse.to_json)
           (Some Values.DeleteAssistantAssociationResponse.error_to_json)])
let delete_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and contentId =
         flag "content-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_content
           (Values.DeleteContentRequest.make ~knowledgeBaseId ~contentId ())
           (Some Values.DeleteContentResponse.to_json)
           (Some Values.DeleteContentResponse.error_to_json)])
let delete_content_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and contentId =
         flag "content-id" (required string) ~doc:"STRING UuidOrArn"
       and contentAssociationId =
         flag "content-association-id" (required string)
           ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_content_association
           (Values.DeleteContentAssociationRequest.make ~knowledgeBaseId
              ~contentId ~contentAssociationId ())
           (Some Values.DeleteContentAssociationResponse.to_json)
           (Some Values.DeleteContentAssociationResponse.error_to_json)])
let delete_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and importJobId =
         flag "import-job-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_import_job
           (Values.DeleteImportJobRequest.make ~knowledgeBaseId ~importJobId
              ()) (Some Values.DeleteImportJobResponse.to_json)
           (Some Values.DeleteImportJobResponse.error_to_json)])
let delete_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_knowledge_base
           (Values.DeleteKnowledgeBaseRequest.make ~knowledgeBaseId ())
           (Some Values.DeleteKnowledgeBaseResponse.to_json)
           (Some Values.DeleteKnowledgeBaseResponse.error_to_json)])
let delete_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_message_template
           (Values.DeleteMessageTemplateRequest.make ~knowledgeBaseId
              ~messageTemplateId ())
           (Some Values.DeleteMessageTemplateResponse.to_json)
           (Some Values.DeleteMessageTemplateResponse.error_to_json)])
let delete_message_template_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_message_template_attachment
           (Values.DeleteMessageTemplateAttachmentRequest.make
              ~knowledgeBaseId ~messageTemplateId ~attachmentId ())
           (Some Values.DeleteMessageTemplateAttachmentResponse.to_json)
           (Some Values.DeleteMessageTemplateAttachmentResponse.error_to_json)])
let delete_quick_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and quickResponseId =
         flag "quick-response-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_quick_response
           (Values.DeleteQuickResponseRequest.make ~knowledgeBaseId
              ~quickResponseId ())
           (Some Values.DeleteQuickResponseResponse.to_json)
           (Some Values.DeleteQuickResponseResponse.error_to_json)])
let get_a_i_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiAgentId =
         flag "ai-agent-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_a_i_agent
           (Values.GetAIAgentRequest.make ~assistantId ~aiAgentId ())
           (Some Values.GetAIAgentResponse.to_json)
           (Some Values.GetAIAgentResponse.error_to_json)])
let get_a_i_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiGuardrailId =
         flag "ai-guardrail-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_a_i_guardrail
           (Values.GetAIGuardrailRequest.make ~assistantId ~aiGuardrailId ())
           (Some Values.GetAIGuardrailResponse.to_json)
           (Some Values.GetAIGuardrailResponse.error_to_json)])
let get_a_i_prompt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiPromptId =
         flag "ai-prompt-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_a_i_prompt
           (Values.GetAIPromptRequest.make ~assistantId ~aiPromptId ())
           (Some Values.GetAIPromptResponse.to_json)
           (Some Values.GetAIPromptResponse.error_to_json)])
let get_assistant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_assistant (Values.GetAssistantRequest.make ~assistantId ())
           (Some Values.GetAssistantResponse.to_json)
           (Some Values.GetAssistantResponse.error_to_json)])
let get_assistant_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantAssociationId =
         flag "assistant-association-id" (required string)
           ~doc:"STRING UuidOrArn"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_assistant_association
           (Values.GetAssistantAssociationRequest.make
              ~assistantAssociationId ~assistantId ())
           (Some Values.GetAssistantAssociationResponse.to_json)
           (Some Values.GetAssistantAssociationResponse.error_to_json)])
let get_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentId =
         flag "content-id" (required string) ~doc:"STRING UuidOrArn"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_content
           (Values.GetContentRequest.make ~contentId ~knowledgeBaseId ())
           (Some Values.GetContentResponse.to_json)
           (Some Values.GetContentResponse.error_to_json)])
let get_content_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and contentId =
         flag "content-id" (required string) ~doc:"STRING UuidOrArn"
       and contentAssociationId =
         flag "content-association-id" (required string)
           ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_content_association
           (Values.GetContentAssociationRequest.make ~knowledgeBaseId
              ~contentId ~contentAssociationId ())
           (Some Values.GetContentAssociationResponse.to_json)
           (Some Values.GetContentAssociationResponse.error_to_json)])
let get_content_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentId =
         flag "content-id" (required string) ~doc:"STRING UuidOrArn"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_content_summary
           (Values.GetContentSummaryRequest.make ~contentId ~knowledgeBaseId
              ()) (Some Values.GetContentSummaryResponse.to_json)
           (Some Values.GetContentSummaryResponse.error_to_json)])
let get_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and importJobId =
         flag "import-job-id" (required string) ~doc:"STRING Uuid"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_import_job
           (Values.GetImportJobRequest.make ~importJobId ~knowledgeBaseId ())
           (Some Values.GetImportJobResponse.to_json)
           (Some Values.GetImportJobResponse.error_to_json)])
let get_knowledge_base =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_knowledge_base
           (Values.GetKnowledgeBaseRequest.make ~knowledgeBaseId ())
           (Some Values.GetKnowledgeBaseResponse.to_json)
           (Some Values.GetKnowledgeBaseResponse.error_to_json)])
let get_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_message_template
           (Values.GetMessageTemplateRequest.make ~messageTemplateId
              ~knowledgeBaseId ())
           (Some Values.GetMessageTemplateResponse.to_json)
           (Some Values.GetMessageTemplateResponse.error_to_json)])
let get_next_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn"
       and nextMessageToken =
         flag "next-message-token" (required string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_next_message
           (Values.GetNextMessageRequest.make ~assistantId ~sessionId
              ~nextMessageToken ())
           (Some Values.GetNextMessageResponse.to_json)
           (Some Values.GetNextMessageResponse.error_to_json)])
let get_quick_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and quickResponseId =
         flag "quick-response-id" (required string) ~doc:"STRING UuidOrArn"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_quick_response
           (Values.GetQuickResponseRequest.make ~quickResponseId
              ~knowledgeBaseId ())
           (Some Values.GetQuickResponseResponse.to_json)
           (Some Values.GetQuickResponseResponse.error_to_json)])
let get_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and waitTimeSeconds =
         flag "wait-time-seconds" (optional int) ~doc:"INT WaitTimeSeconds"
       and nextChunkToken =
         flag "next-chunk-token" (optional string) ~doc:"STRING NextToken"
       and recommendationType =
         flag "recommendation-type" (optional json_arg)
           ~doc:"JSON RecommendationType"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recommendations
           (Values.GetRecommendationsRequest.make ?maxResults
              ?waitTimeSeconds ?nextChunkToken
              ?recommendationType:(Option.map
                                     ~f:Values.RecommendationType.of_json
                                     recommendationType) ~assistantId
              ~sessionId ()) (Some Values.GetRecommendationsResponse.to_json)
           (Some Values.GetRecommendationsResponse.error_to_json)])
let get_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session
           (Values.GetSessionRequest.make ~assistantId ~sessionId ())
           (Some Values.GetSessionResponse.to_json)
           (Some Values.GetSessionResponse.error_to_json)])
let list_a_i_agent_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and origin = flag "origin" (optional json_arg) ~doc:"JSON Origin"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiAgentId =
         flag "ai-agent-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_a_i_agent_versions
           (Values.ListAIAgentVersionsRequest.make ?nextToken ?maxResults
              ?origin:(Option.map ~f:Values.Origin.of_json origin)
              ~assistantId ~aiAgentId ())
           (Some Values.ListAIAgentVersionsResponse.to_json)
           (Some Values.ListAIAgentVersionsResponse.error_to_json)])
let list_a_i_agents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and origin = flag "origin" (optional json_arg) ~doc:"JSON Origin"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_a_i_agents
           (Values.ListAIAgentsRequest.make ?nextToken ?maxResults
              ?origin:(Option.map ~f:Values.Origin.of_json origin)
              ~assistantId ()) (Some Values.ListAIAgentsResponse.to_json)
           (Some Values.ListAIAgentsResponse.error_to_json)])
let list_a_i_guardrail_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiGuardrailId =
         flag "ai-guardrail-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_a_i_guardrail_versions
           (Values.ListAIGuardrailVersionsRequest.make ?nextToken ?maxResults
              ~assistantId ~aiGuardrailId ())
           (Some Values.ListAIGuardrailVersionsResponse.to_json)
           (Some Values.ListAIGuardrailVersionsResponse.error_to_json)])
let list_a_i_guardrails =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_a_i_guardrails
           (Values.ListAIGuardrailsRequest.make ?nextToken ?maxResults
              ~assistantId ()) (Some Values.ListAIGuardrailsResponse.to_json)
           (Some Values.ListAIGuardrailsResponse.error_to_json)])
let list_a_i_prompt_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and origin = flag "origin" (optional json_arg) ~doc:"JSON Origin"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiPromptId =
         flag "ai-prompt-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_a_i_prompt_versions
           (Values.ListAIPromptVersionsRequest.make ?nextToken ?maxResults
              ?origin:(Option.map ~f:Values.Origin.of_json origin)
              ~assistantId ~aiPromptId ())
           (Some Values.ListAIPromptVersionsResponse.to_json)
           (Some Values.ListAIPromptVersionsResponse.error_to_json)])
let list_a_i_prompts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and origin = flag "origin" (optional json_arg) ~doc:"JSON Origin"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_a_i_prompts
           (Values.ListAIPromptsRequest.make ?nextToken ?maxResults
              ?origin:(Option.map ~f:Values.Origin.of_json origin)
              ~assistantId ()) (Some Values.ListAIPromptsResponse.to_json)
           (Some Values.ListAIPromptsResponse.error_to_json)])
let list_assistant_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assistant_associations
           (Values.ListAssistantAssociationsRequest.make ?nextToken
              ?maxResults ~assistantId ())
           (Some Values.ListAssistantAssociationsResponse.to_json)
           (Some Values.ListAssistantAssociationsResponse.error_to_json)])
let list_assistants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assistants
           (Values.ListAssistantsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListAssistantsResponse.to_json)
           (Some Values.ListAssistantsResponse.error_to_json)])
let list_content_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and contentId =
         flag "content-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_content_associations
           (Values.ListContentAssociationsRequest.make ?nextToken ?maxResults
              ~knowledgeBaseId ~contentId ())
           (Some Values.ListContentAssociationsResponse.to_json)
           (Some Values.ListContentAssociationsResponse.error_to_json)])
let list_contents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contents
           (Values.ListContentsRequest.make ?nextToken ?maxResults
              ~knowledgeBaseId ()) (Some Values.ListContentsResponse.to_json)
           (Some Values.ListContentsResponse.error_to_json)])
let list_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_import_jobs
           (Values.ListImportJobsRequest.make ?nextToken ?maxResults
              ~knowledgeBaseId ())
           (Some Values.ListImportJobsResponse.to_json)
           (Some Values.ListImportJobsResponse.error_to_json)])
let list_knowledge_bases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_knowledge_bases
           (Values.ListKnowledgeBasesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListKnowledgeBasesResponse.to_json)
           (Some Values.ListKnowledgeBasesResponse.error_to_json)])
let list_message_template_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_message_template_versions
           (Values.ListMessageTemplateVersionsRequest.make ?nextToken
              ?maxResults ~knowledgeBaseId ~messageTemplateId ())
           (Some Values.ListMessageTemplateVersionsResponse.to_json)
           (Some Values.ListMessageTemplateVersionsResponse.error_to_json)])
let list_message_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_message_templates
           (Values.ListMessageTemplatesRequest.make ?nextToken ?maxResults
              ~knowledgeBaseId ())
           (Some Values.ListMessageTemplatesResponse.to_json)
           (Some Values.ListMessageTemplatesResponse.error_to_json)])
let list_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON MessageFilterType"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_messages
           (Values.ListMessagesRequest.make ?nextToken ?maxResults
              ?filter:(Option.map ~f:Values.MessageFilterType.of_json filter)
              ~assistantId ~sessionId ())
           (Some Values.ListMessagesResponse.to_json)
           (Some Values.ListMessagesResponse.error_to_json)])
let list_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aiPromptType =
         flag "ai-prompt-type" (optional json_arg) ~doc:"JSON AIPromptType"
       and modelLifecycle =
         flag "model-lifecycle" (optional json_arg)
           ~doc:"JSON ModelLifecycle"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_models
           (Values.ListModelsRequest.make
              ?aiPromptType:(Option.map ~f:Values.AIPromptType.of_json
                               aiPromptType)
              ?modelLifecycle:(Option.map ~f:Values.ModelLifecycle.of_json
                                 modelLifecycle) ?nextToken ?maxResults
              ~assistantId ()) (Some Values.ListModelsResponse.to_json)
           (Some Values.ListModelsResponse.error_to_json)])
let list_quick_responses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_quick_responses
           (Values.ListQuickResponsesRequest.make ?nextToken ?maxResults
              ~knowledgeBaseId ())
           (Some Values.ListQuickResponsesResponse.to_json)
           (Some Values.ListQuickResponsesResponse.error_to_json)])
let list_spans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_spans
           (Values.ListSpansRequest.make ?nextToken ?maxResults ~assistantId
              ~sessionId ()) (Some Values.ListSpansResponse.to_json)
           (Some Values.ListSpansResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let notify_recommendations_received =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn"
       and recommendationIds =
         flag "recommendation-ids" (required json_arg)
           ~doc:"JSON RecommendationIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.notify_recommendations_received
           (Values.NotifyRecommendationsReceivedRequest.make ~assistantId
              ~sessionId
              ~recommendationIds:(Values.RecommendationIdList.of_json
                                    recommendationIds) ())
           (Some Values.NotifyRecommendationsReceivedResponse.to_json)
           (Some Values.NotifyRecommendationsReceivedResponse.error_to_json)])
let put_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and targetId = flag "target-id" (required string) ~doc:"STRING Uuid"
       and targetType =
         flag "target-type" (required json_arg) ~doc:"JSON TargetType"
       and contentFeedback =
         flag "content-feedback" (required json_arg)
           ~doc:"JSON ContentFeedbackData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_feedback
           (Values.PutFeedbackRequest.make ~assistantId ~targetId
              ~targetType:(Values.TargetType.of_json targetType)
              ~contentFeedback:(Values.ContentFeedbackData.of_json
                                  contentFeedback) ())
           (Some Values.PutFeedbackResponse.to_json)
           (Some Values.PutFeedbackResponse.error_to_json)])
let query_assistant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryText =
         flag "query-text" (optional string) ~doc:"STRING QueryText"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and sessionId =
         flag "session-id" (optional string) ~doc:"STRING UuidOrArn"
       and queryCondition =
         flag "query-condition" (optional json_arg)
           ~doc:"JSON QueryConditionExpression"
       and queryInputData =
         flag "query-input-data" (optional json_arg)
           ~doc:"JSON QueryInputData"
       and overrideKnowledgeBaseSearchType =
         flag "override-knowledge-base-search-type" (optional json_arg)
           ~doc:"JSON KnowledgeBaseSearchType"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.query_assistant
           (Values.QueryAssistantRequest.make ?queryText ?nextToken
              ?maxResults ?sessionId
              ?queryCondition:(Option.map
                                 ~f:Values.QueryConditionExpression.of_json
                                 queryCondition)
              ?queryInputData:(Option.map ~f:Values.QueryInputData.of_json
                                 queryInputData)
              ?overrideKnowledgeBaseSearchType:(Option.map
                                                  ~f:Values.KnowledgeBaseSearchType.of_json
                                                  overrideKnowledgeBaseSearchType)
              ~assistantId ()) (Some Values.QueryAssistantResponse.to_json)
           (Some Values.QueryAssistantResponse.error_to_json)])
let remove_assistant_a_i_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and orchestratorUseCase =
         flag "orchestrator-use-case" (optional string)
           ~doc:"STRING NonEmptyString"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiAgentType =
         flag "ai-agent-type" (required json_arg) ~doc:"JSON AIAgentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_assistant_a_i_agent
           (Values.RemoveAssistantAIAgentRequest.make ?orchestratorUseCase
              ~assistantId
              ~aiAgentType:(Values.AIAgentType.of_json aiAgentType) ())
           (Some Values.RemoveAssistantAIAgentResponse.to_json)
           (Some Values.RemoveAssistantAIAgentResponse.error_to_json)])
let remove_knowledge_base_template_uri =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_knowledge_base_template_uri
           (Values.RemoveKnowledgeBaseTemplateUriRequest.make
              ~knowledgeBaseId ())
           (Some Values.RemoveKnowledgeBaseTemplateUriResponse.to_json)
           (Some Values.RemoveKnowledgeBaseTemplateUriResponse.error_to_json)])
let render_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and attributes =
         flag "attributes" (required json_arg)
           ~doc:"JSON MessageTemplateAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.render_message_template
           (Values.RenderMessageTemplateRequest.make ~knowledgeBaseId
              ~messageTemplateId
              ~attributes:(Values.MessageTemplateAttributes.of_json
                             attributes) ())
           (Some Values.RenderMessageTemplateResponse.to_json)
           (Some Values.RenderMessageTemplateResponse.error_to_json)])
let retrieve =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and retrievalConfiguration =
         flag "retrieval-configuration" (required json_arg)
           ~doc:"JSON RetrievalConfiguration"
       and retrievalQuery =
         flag "retrieval-query" (required string)
           ~doc:"STRING NonEmptySensitiveString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retrieve
           (Values.RetrieveRequest.make ~assistantId
              ~retrievalConfiguration:(Values.RetrievalConfiguration.of_json
                                         retrievalConfiguration)
              ~retrievalQuery ()) (Some Values.RetrieveResponse.to_json)
           (Some Values.RetrieveResponse.error_to_json)])
let search_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and searchExpression =
         flag "search-expression" (required json_arg)
           ~doc:"JSON SearchExpression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_content
           (Values.SearchContentRequest.make ?nextToken ?maxResults
              ~knowledgeBaseId
              ~searchExpression:(Values.SearchExpression.of_json
                                   searchExpression) ())
           (Some Values.SearchContentResponse.to_json)
           (Some Values.SearchContentResponse.error_to_json)])
let search_message_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and searchExpression =
         flag "search-expression" (required json_arg)
           ~doc:"JSON MessageTemplateSearchExpression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_message_templates
           (Values.SearchMessageTemplatesRequest.make ?nextToken ?maxResults
              ~knowledgeBaseId
              ~searchExpression:(Values.MessageTemplateSearchExpression.of_json
                                   searchExpression) ())
           (Some Values.SearchMessageTemplatesResponse.to_json)
           (Some Values.SearchMessageTemplatesResponse.error_to_json)])
let search_quick_responses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON ContactAttributes"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and searchExpression =
         flag "search-expression" (required json_arg)
           ~doc:"JSON QuickResponseSearchExpression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_quick_responses
           (Values.SearchQuickResponsesRequest.make ?nextToken ?maxResults
              ?attributes:(Option.map ~f:Values.ContactAttributes.of_json
                             attributes) ~knowledgeBaseId
              ~searchExpression:(Values.QuickResponseSearchExpression.of_json
                                   searchExpression) ())
           (Some Values.SearchQuickResponsesResponse.to_json)
           (Some Values.SearchQuickResponsesResponse.error_to_json)])
let search_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and searchExpression =
         flag "search-expression" (required json_arg)
           ~doc:"JSON SearchExpression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_sessions
           (Values.SearchSessionsRequest.make ?nextToken ?maxResults
              ~assistantId
              ~searchExpression:(Values.SearchExpression.of_json
                                   searchExpression) ())
           (Some Values.SearchSessionsResponse.to_json)
           (Some Values.SearchSessionsResponse.error_to_json)])
let send_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aiAgentId =
         flag "ai-agent-id" (optional string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and conversationContext =
         flag "conversation-context" (optional json_arg)
           ~doc:"JSON ConversationContext"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON MessageConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and orchestratorUseCase =
         flag "orchestrator-use-case" (optional string)
           ~doc:"STRING NonEmptyString"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON MessageMetadata"
       and originRequestId =
         flag "origin-request-id" (optional string)
           ~doc:"STRING SendMessageRequestOriginRequestIdString"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON MessageType"
       and message =
         flag "message" (required json_arg) ~doc:"JSON MessageInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_message
           (Values.SendMessageRequest.make ?aiAgentId
              ?conversationContext:(Option.map
                                      ~f:Values.ConversationContext.of_json
                                      conversationContext)
              ?configuration:(Option.map
                                ~f:Values.MessageConfiguration.of_json
                                configuration) ?clientToken
              ?orchestratorUseCase
              ?metadata:(Option.map ~f:Values.MessageMetadata.of_json
                           metadata) ?originRequestId ~assistantId ~sessionId
              ~type_:(Values.MessageType.of_json type_)
              ~message:(Values.MessageInput.of_json message) ())
           (Some Values.SendMessageResponse.to_json)
           (Some Values.SendMessageResponse.error_to_json)])
let start_content_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and presignedUrlTimeToLive =
         flag "presigned-url-time-to-live" (optional int)
           ~doc:"INT TimeToLive"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and contentType =
         flag "content-type" (required string) ~doc:"STRING ContentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_content_upload
           (Values.StartContentUploadRequest.make ?presignedUrlTimeToLive
              ~knowledgeBaseId ~contentType ())
           (Some Values.StartContentUploadResponse.to_json)
           (Some Values.StartContentUploadResponse.error_to_json)])
let start_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING NonEmptyString"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON ContentMetadata"
       and externalSourceConfiguration =
         flag "external-source-configuration" (optional json_arg)
           ~doc:"JSON ExternalSourceConfiguration"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and importJobType =
         flag "import-job-type" (required json_arg) ~doc:"JSON ImportJobType"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_import_job
           (Values.StartImportJobRequest.make ?clientToken
              ?metadata:(Option.map ~f:Values.ContentMetadata.of_json
                           metadata)
              ?externalSourceConfiguration:(Option.map
                                              ~f:Values.ExternalSourceConfiguration.of_json
                                              externalSourceConfiguration)
              ~knowledgeBaseId
              ~importJobType:(Values.ImportJobType.of_json importJobType)
              ~uploadId ()) (Some Values.StartImportJobResponse.to_json)
           (Some Values.StartImportJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_a_i_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON AIAgentConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiAgentId =
         flag "ai-agent-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and visibilityStatus =
         flag "visibility-status" (required json_arg)
           ~doc:"JSON VisibilityStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_a_i_agent
           (Values.UpdateAIAgentRequest.make ?clientToken
              ?configuration:(Option.map
                                ~f:Values.AIAgentConfiguration.of_json
                                configuration) ?description ~assistantId
              ~aiAgentId
              ~visibilityStatus:(Values.VisibilityStatus.of_json
                                   visibilityStatus) ())
           (Some Values.UpdateAIAgentResponse.to_json)
           (Some Values.UpdateAIAgentResponse.error_to_json)])
let update_a_i_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AIGuardrailDescription"
       and topicPolicyConfig =
         flag "topic-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailTopicPolicyConfig"
       and contentPolicyConfig =
         flag "content-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailContentPolicyConfig"
       and wordPolicyConfig =
         flag "word-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailWordPolicyConfig"
       and sensitiveInformationPolicyConfig =
         flag "sensitive-information-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailSensitiveInformationPolicyConfig"
       and contextualGroundingPolicyConfig =
         flag "contextual-grounding-policy-config" (optional json_arg)
           ~doc:"JSON AIGuardrailContextualGroundingPolicyConfig"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiGuardrailId =
         flag "ai-guardrail-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and visibilityStatus =
         flag "visibility-status" (required json_arg)
           ~doc:"JSON VisibilityStatus"
       and blockedInputMessaging =
         flag "blocked-input-messaging" (required string)
           ~doc:"STRING AIGuardrailBlockedMessaging"
       and blockedOutputsMessaging =
         flag "blocked-outputs-messaging" (required string)
           ~doc:"STRING AIGuardrailBlockedMessaging" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_a_i_guardrail
           (Values.UpdateAIGuardrailRequest.make ?clientToken ?description
              ?topicPolicyConfig:(Option.map
                                    ~f:Values.AIGuardrailTopicPolicyConfig.of_json
                                    topicPolicyConfig)
              ?contentPolicyConfig:(Option.map
                                      ~f:Values.AIGuardrailContentPolicyConfig.of_json
                                      contentPolicyConfig)
              ?wordPolicyConfig:(Option.map
                                   ~f:Values.AIGuardrailWordPolicyConfig.of_json
                                   wordPolicyConfig)
              ?sensitiveInformationPolicyConfig:(Option.map
                                                   ~f:Values.AIGuardrailSensitiveInformationPolicyConfig.of_json
                                                   sensitiveInformationPolicyConfig)
              ?contextualGroundingPolicyConfig:(Option.map
                                                  ~f:Values.AIGuardrailContextualGroundingPolicyConfig.of_json
                                                  contextualGroundingPolicyConfig)
              ~assistantId ~aiGuardrailId
              ~visibilityStatus:(Values.VisibilityStatus.of_json
                                   visibilityStatus) ~blockedInputMessaging
              ~blockedOutputsMessaging ())
           (Some Values.UpdateAIGuardrailResponse.to_json)
           (Some Values.UpdateAIGuardrailResponse.error_to_json)])
let update_a_i_prompt =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and templateConfiguration =
         flag "template-configuration" (optional json_arg)
           ~doc:"JSON AIPromptTemplateConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and modelId =
         flag "model-id" (optional string)
           ~doc:"STRING AIPromptModelIdentifier"
       and inferenceConfiguration =
         flag "inference-configuration" (optional json_arg)
           ~doc:"JSON AIPromptInferenceConfiguration"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiPromptId =
         flag "ai-prompt-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier"
       and visibilityStatus =
         flag "visibility-status" (required json_arg)
           ~doc:"JSON VisibilityStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_a_i_prompt
           (Values.UpdateAIPromptRequest.make ?clientToken
              ?templateConfiguration:(Option.map
                                        ~f:Values.AIPromptTemplateConfiguration.of_json
                                        templateConfiguration) ?description
              ?modelId
              ?inferenceConfiguration:(Option.map
                                         ~f:Values.AIPromptInferenceConfiguration.of_json
                                         inferenceConfiguration) ~assistantId
              ~aiPromptId
              ~visibilityStatus:(Values.VisibilityStatus.of_json
                                   visibilityStatus) ())
           (Some Values.UpdateAIPromptResponse.to_json)
           (Some Values.UpdateAIPromptResponse.error_to_json)])
let update_assistant_a_i_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and orchestratorUseCase =
         flag "orchestrator-use-case" (optional string)
           ~doc:"STRING NonEmptyString"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and aiAgentType =
         flag "ai-agent-type" (required json_arg) ~doc:"JSON AIAgentType"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON AIAgentConfigurationData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_assistant_a_i_agent
           (Values.UpdateAssistantAIAgentRequest.make ?orchestratorUseCase
              ~assistantId
              ~aiAgentType:(Values.AIAgentType.of_json aiAgentType)
              ~configuration:(Values.AIAgentConfigurationData.of_json
                                configuration) ())
           (Some Values.UpdateAssistantAIAgentResponse.to_json)
           (Some Values.UpdateAssistantAIAgentResponse.error_to_json)])
let update_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING NonEmptyString"
       and title = flag "title" (optional string) ~doc:"STRING ContentTitle"
       and overrideLinkOutUri =
         flag "override-link-out-uri" (optional string) ~doc:"STRING Uri"
       and removeOverrideLinkOutUri =
         flag "remove-override-link-out-uri" (optional bool)
           ~doc:"BOOL Boolean"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON ContentMetadata"
       and uploadId =
         flag "upload-id" (optional string) ~doc:"STRING UploadId"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and contentId =
         flag "content-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_content
           (Values.UpdateContentRequest.make ?revisionId ?title
              ?overrideLinkOutUri ?removeOverrideLinkOutUri
              ?metadata:(Option.map ~f:Values.ContentMetadata.of_json
                           metadata) ?uploadId ~knowledgeBaseId ~contentId ())
           (Some Values.UpdateContentResponse.to_json)
           (Some Values.UpdateContentResponse.error_to_json)])
let update_knowledge_base_template_uri =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and templateUri =
         flag "template-uri" (required string) ~doc:"STRING Uri" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_knowledge_base_template_uri
           (Values.UpdateKnowledgeBaseTemplateUriRequest.make
              ~knowledgeBaseId ~templateUri ())
           (Some Values.UpdateKnowledgeBaseTemplateUriResponse.to_json)
           (Some Values.UpdateKnowledgeBaseTemplateUriResponse.error_to_json)])
let update_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and content =
         flag "content" (optional json_arg)
           ~doc:"JSON MessageTemplateContentProvider"
       and language =
         flag "language" (optional string) ~doc:"STRING LanguageCode"
       and sourceConfiguration =
         flag "source-configuration" (optional json_arg)
           ~doc:"JSON MessageTemplateSourceConfiguration"
       and defaultAttributes =
         flag "default-attributes" (optional json_arg)
           ~doc:"JSON MessageTemplateAttributes"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_message_template
           (Values.UpdateMessageTemplateRequest.make
              ?content:(Option.map
                          ~f:Values.MessageTemplateContentProvider.of_json
                          content) ?language
              ?sourceConfiguration:(Option.map
                                      ~f:Values.MessageTemplateSourceConfiguration.of_json
                                      sourceConfiguration)
              ?defaultAttributes:(Option.map
                                    ~f:Values.MessageTemplateAttributes.of_json
                                    defaultAttributes) ~knowledgeBaseId
              ~messageTemplateId ())
           (Some Values.UpdateMessageTemplateResponse.to_json)
           (Some Values.UpdateMessageTemplateResponse.error_to_json)])
let update_message_template_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and groupingConfiguration =
         flag "grouping-configuration" (optional json_arg)
           ~doc:"JSON GroupingConfiguration"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and messageTemplateId =
         flag "message-template-id" (required string)
           ~doc:"STRING UuidOrArnOrEitherWithQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_message_template_metadata
           (Values.UpdateMessageTemplateMetadataRequest.make ?name
              ?description
              ?groupingConfiguration:(Option.map
                                        ~f:Values.GroupingConfiguration.of_json
                                        groupingConfiguration)
              ~knowledgeBaseId ~messageTemplateId ())
           (Some Values.UpdateMessageTemplateMetadataResponse.to_json)
           (Some Values.UpdateMessageTemplateMetadataResponse.error_to_json)])
let update_quick_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING QuickResponseName"
       and content =
         flag "content" (optional json_arg)
           ~doc:"JSON QuickResponseDataProvider"
       and contentType =
         flag "content-type" (optional string)
           ~doc:"STRING QuickResponseType"
       and groupingConfiguration =
         flag "grouping-configuration" (optional json_arg)
           ~doc:"JSON GroupingConfiguration"
       and removeGroupingConfiguration =
         flag "remove-grouping-configuration" (optional bool)
           ~doc:"BOOL Boolean"
       and description =
         flag "description" (optional string)
           ~doc:"STRING QuickResponseDescription"
       and removeDescription =
         flag "remove-description" (optional bool) ~doc:"BOOL Boolean"
       and shortcutKey =
         flag "shortcut-key" (optional string) ~doc:"STRING ShortCutKey"
       and removeShortcutKey =
         flag "remove-shortcut-key" (optional bool) ~doc:"BOOL Boolean"
       and isActive = flag "is-active" (optional bool) ~doc:"BOOL Boolean"
       and channels =
         flag "channels" (optional json_arg) ~doc:"JSON Channels"
       and language =
         flag "language" (optional string) ~doc:"STRING LanguageCode"
       and knowledgeBaseId =
         flag "knowledge-base-id" (required string) ~doc:"STRING UuidOrArn"
       and quickResponseId =
         flag "quick-response-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_quick_response
           (Values.UpdateQuickResponseRequest.make ?name
              ?content:(Option.map
                          ~f:Values.QuickResponseDataProvider.of_json content)
              ?contentType
              ?groupingConfiguration:(Option.map
                                        ~f:Values.GroupingConfiguration.of_json
                                        groupingConfiguration)
              ?removeGroupingConfiguration ?description ?removeDescription
              ?shortcutKey ?removeShortcutKey ?isActive
              ?channels:(Option.map ~f:Values.Channels.of_json channels)
              ?language ~knowledgeBaseId ~quickResponseId ())
           (Some Values.UpdateQuickResponseResponse.to_json)
           (Some Values.UpdateQuickResponseResponse.error_to_json)])
let update_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tagFilter =
         flag "tag-filter" (optional json_arg) ~doc:"JSON TagFilter"
       and aiAgentConfiguration =
         flag "ai-agent-configuration" (optional json_arg)
           ~doc:"JSON AIAgentConfigurationMap"
       and orchestratorConfigurationList =
         flag "orchestrator-configuration-list" (optional json_arg)
           ~doc:"JSON OrchestratorConfigurationList"
       and removeOrchestratorConfigurationList =
         flag "remove-orchestrator-configuration-list" (optional bool)
           ~doc:"BOOL Boolean"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_session
           (Values.UpdateSessionRequest.make ?description
              ?tagFilter:(Option.map ~f:Values.TagFilter.of_json tagFilter)
              ?aiAgentConfiguration:(Option.map
                                       ~f:Values.AIAgentConfigurationMap.of_json
                                       aiAgentConfiguration)
              ?orchestratorConfigurationList:(Option.map
                                                ~f:Values.OrchestratorConfigurationList.of_json
                                                orchestratorConfigurationList)
              ?removeOrchestratorConfigurationList ~assistantId ~sessionId ())
           (Some Values.UpdateSessionResponse.to_json)
           (Some Values.UpdateSessionResponse.error_to_json)])
let update_session_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional json_arg)
           ~doc:"JSON SessionDataNamespace"
       and assistantId =
         flag "assistant-id" (required string) ~doc:"STRING UuidOrArn"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING UuidOrArn"
       and data =
         flag "data" (required json_arg) ~doc:"JSON RuntimeSessionDataList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_session_data
           (Values.UpdateSessionDataRequest.make
              ?namespace:(Option.map ~f:Values.SessionDataNamespace.of_json
                            namespace) ~assistantId ~sessionId
              ~data:(Values.RuntimeSessionDataList.of_json data) ())
           (Some Values.UpdateSessionDataResponse.to_json)
           (Some Values.UpdateSessionDataResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("activate-message-template", activate_message_template);
    ("create-a-i-agent", create_a_i_agent);
    ("create-a-i-agent-version", create_a_i_agent_version);
    ("create-a-i-guardrail", create_a_i_guardrail);
    ("create-a-i-guardrail-version", create_a_i_guardrail_version);
    ("create-a-i-prompt", create_a_i_prompt);
    ("create-a-i-prompt-version", create_a_i_prompt_version);
    ("create-assistant", create_assistant);
    ("create-assistant-association", create_assistant_association);
    ("create-content", create_content);
    ("create-content-association", create_content_association);
    ("create-knowledge-base", create_knowledge_base);
    ("create-message-template", create_message_template);
    ("create-message-template-attachment",
      create_message_template_attachment);
    ("create-message-template-version", create_message_template_version);
    ("create-quick-response", create_quick_response);
    ("create-session", create_session);
    ("deactivate-message-template", deactivate_message_template);
    ("delete-a-i-agent", delete_a_i_agent);
    ("delete-a-i-agent-version", delete_a_i_agent_version);
    ("delete-a-i-guardrail", delete_a_i_guardrail);
    ("delete-a-i-guardrail-version", delete_a_i_guardrail_version);
    ("delete-a-i-prompt", delete_a_i_prompt);
    ("delete-a-i-prompt-version", delete_a_i_prompt_version);
    ("delete-assistant", delete_assistant);
    ("delete-assistant-association", delete_assistant_association);
    ("delete-content", delete_content);
    ("delete-content-association", delete_content_association);
    ("delete-import-job", delete_import_job);
    ("delete-knowledge-base", delete_knowledge_base);
    ("delete-message-template", delete_message_template);
    ("delete-message-template-attachment",
      delete_message_template_attachment);
    ("delete-quick-response", delete_quick_response);
    ("get-a-i-agent", get_a_i_agent);
    ("get-a-i-guardrail", get_a_i_guardrail);
    ("get-a-i-prompt", get_a_i_prompt);
    ("get-assistant", get_assistant);
    ("get-assistant-association", get_assistant_association);
    ("get-content", get_content);
    ("get-content-association", get_content_association);
    ("get-content-summary", get_content_summary);
    ("get-import-job", get_import_job);
    ("get-knowledge-base", get_knowledge_base);
    ("get-message-template", get_message_template);
    ("get-next-message", get_next_message);
    ("get-quick-response", get_quick_response);
    ("get-recommendations", get_recommendations);
    ("get-session", get_session);
    ("list-a-i-agent-versions", list_a_i_agent_versions);
    ("list-a-i-agents", list_a_i_agents);
    ("list-a-i-guardrail-versions", list_a_i_guardrail_versions);
    ("list-a-i-guardrails", list_a_i_guardrails);
    ("list-a-i-prompt-versions", list_a_i_prompt_versions);
    ("list-a-i-prompts", list_a_i_prompts);
    ("list-assistant-associations", list_assistant_associations);
    ("list-assistants", list_assistants);
    ("list-content-associations", list_content_associations);
    ("list-contents", list_contents);
    ("list-import-jobs", list_import_jobs);
    ("list-knowledge-bases", list_knowledge_bases);
    ("list-message-template-versions", list_message_template_versions);
    ("list-message-templates", list_message_templates);
    ("list-messages", list_messages);
    ("list-models", list_models);
    ("list-quick-responses", list_quick_responses);
    ("list-spans", list_spans);
    ("list-tags-for-resource", list_tags_for_resource);
    ("notify-recommendations-received", notify_recommendations_received);
    ("put-feedback", put_feedback);
    ("query-assistant", query_assistant);
    ("remove-assistant-a-i-agent", remove_assistant_a_i_agent);
    ("remove-knowledge-base-template-uri",
      remove_knowledge_base_template_uri);
    ("render-message-template", render_message_template);
    ("retrieve", retrieve);
    ("search-content", search_content);
    ("search-message-templates", search_message_templates);
    ("search-quick-responses", search_quick_responses);
    ("search-sessions", search_sessions);
    ("send-message", send_message);
    ("start-content-upload", start_content_upload);
    ("start-import-job", start_import_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-a-i-agent", update_a_i_agent);
    ("update-a-i-guardrail", update_a_i_guardrail);
    ("update-a-i-prompt", update_a_i_prompt);
    ("update-assistant-a-i-agent", update_assistant_a_i_agent);
    ("update-content", update_content);
    ("update-knowledge-base-template-uri",
      update_knowledge_base_template_uri);
    ("update-message-template", update_message_template);
    ("update-message-template-metadata", update_message_template_metadata);
    ("update-quick-response", update_quick_response);
    ("update-session", update_session);
    ("update-session-data", update_session_data)]
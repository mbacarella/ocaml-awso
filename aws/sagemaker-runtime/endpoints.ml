(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | InvokeEndpoint: (InvokeEndpointInput.t, InvokeEndpointOutput.t,
  InvokeEndpointOutput.error) t 
  | InvokeEndpointAsync: (InvokeEndpointAsyncInput.t,
  InvokeEndpointAsyncOutput.t, InvokeEndpointAsyncOutput.error) t 
  | InvokeEndpointWithResponseStream:
  (InvokeEndpointWithResponseStreamInput.t,
  InvokeEndpointWithResponseStreamOutput.t,
  InvokeEndpointWithResponseStreamOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | InvokeEndpoint -> `POST
  | InvokeEndpointAsync -> `POST
  | InvokeEndpointWithResponseStream -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | InvokeEndpoint ->
          (Format.kasprintf Uri.of_string) "/endpoints/%s/invocations"
            (EndpointName.to_header x.InvokeEndpointInput.endpointName)
      | InvokeEndpointAsync ->
          (Format.kasprintf Uri.of_string) "/endpoints/%s/async-invocations"
            (EndpointName.to_header x.InvokeEndpointAsyncInput.endpointName)
      | InvokeEndpointWithResponseStream ->
          (Format.kasprintf Uri.of_string)
            "/endpoints/%s/invocations-response-stream"
            (EndpointName.to_header
               x.InvokeEndpointWithResponseStreamInput.endpointName))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | InvokeEndpoint ->
      let headers =
        Some
          ((List.filter_opt
              [Option.map req.InvokeEndpointInput.contentType
                 ~f:(fun x -> ("Content-Type", (Header.to_header x)));
              Option.map req.InvokeEndpointInput.accept
                ~f:(fun x -> ("Accept", (Header.to_header x)));
              Option.map req.InvokeEndpointInput.customAttributes
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Custom-Attributes",
                        (CustomAttributesHeader.to_header x)));
              Option.map req.InvokeEndpointInput.targetModel
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Target-Model",
                        (TargetModelHeader.to_header x)));
              Option.map req.InvokeEndpointInput.targetVariant
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Target-Variant",
                        (TargetVariantHeader.to_header x)));
              Option.map req.InvokeEndpointInput.targetContainerHostname
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Target-Container-Hostname",
                        (TargetContainerHostnameHeader.to_header x)));
              Option.map req.InvokeEndpointInput.inferenceId
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Inference-Id",
                        (InferenceId.to_header x)));
              Option.map req.InvokeEndpointInput.enableExplanations
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Enable-Explanations",
                        (EnableExplanationsHeader.to_header x)));
              Option.map req.InvokeEndpointInput.inferenceComponentName
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Inference-Component",
                        (InferenceComponentHeader.to_header x)));
              Option.map req.InvokeEndpointInput.sessionId
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Session-Id",
                        (SessionIdOrNewSessionConstantHeader.to_header x)))])
             |> Awso.Http.Headers.of_list) in
      let body = BodyBlob.to_header req.body in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
  | InvokeEndpointAsync ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.InvokeEndpointAsyncInput.contentType
                   ~f:(fun x ->
                         ("X-Amzn-SageMaker-Content-Type",
                           (Header.to_header x)));
                Option.map req.InvokeEndpointAsyncInput.accept
                  ~f:(fun x ->
                        ("X-Amzn-SageMaker-Accept", (Header.to_header x)));
                Option.map req.InvokeEndpointAsyncInput.customAttributes
                  ~f:(fun x ->
                        ("X-Amzn-SageMaker-Custom-Attributes",
                          (CustomAttributesHeader.to_header x)));
                Option.map req.InvokeEndpointAsyncInput.inferenceId
                  ~f:(fun x ->
                        ("X-Amzn-SageMaker-Inference-Id",
                          (InferenceId.to_header x)));
                Some
                  ("X-Amzn-SageMaker-InputLocation",
                    (InputLocationHeader.to_header
                       req.InvokeEndpointAsyncInput.inputLocation));
                Option.map req.InvokeEndpointAsyncInput.s3OutputPathExtension
                  ~f:(fun x ->
                        ("X-Amzn-SageMaker-S3OutputPathExtension",
                          (S3OutputPathExtensionHeader.to_header x)));
                Option.map req.InvokeEndpointAsyncInput.filename
                  ~f:(fun x ->
                        ("X-Amzn-SageMaker-Filename",
                          (FilenameHeader.to_header x)));
                Option.map req.InvokeEndpointAsyncInput.requestTTLSeconds
                  ~f:(fun x ->
                        ("X-Amzn-SageMaker-RequestTTLSeconds",
                          (RequestTTLSecondsHeader.to_header x)));
                Option.map
                  req.InvokeEndpointAsyncInput.invocationTimeoutSeconds
                  ~f:(fun x ->
                        ("X-Amzn-SageMaker-InvocationTimeoutSeconds",
                          (InvocationTimeoutSecondsHeader.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map (List.filter_opt [])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeEndpointWithResponseStream ->
      let headers =
        Some
          ((List.filter_opt
              [Option.map
                 req.InvokeEndpointWithResponseStreamInput.contentType
                 ~f:(fun x -> ("Content-Type", (Header.to_header x)));
              Option.map req.InvokeEndpointWithResponseStreamInput.accept
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Accept", (Header.to_header x)));
              Option.map
                req.InvokeEndpointWithResponseStreamInput.customAttributes
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Custom-Attributes",
                        (CustomAttributesHeader.to_header x)));
              Option.map
                req.InvokeEndpointWithResponseStreamInput.targetVariant
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Target-Variant",
                        (TargetVariantHeader.to_header x)));
              Option.map
                req.InvokeEndpointWithResponseStreamInput.targetContainerHostname
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Target-Container-Hostname",
                        (TargetContainerHostnameHeader.to_header x)));
              Option.map
                req.InvokeEndpointWithResponseStreamInput.inferenceId
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Inference-Id",
                        (InferenceId.to_header x)));
              Option.map
                req.InvokeEndpointWithResponseStreamInput.inferenceComponentName
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Inference-Component",
                        (InferenceComponentHeader.to_header x)));
              Option.map req.InvokeEndpointWithResponseStreamInput.sessionId
                ~f:(fun x ->
                      ("X-Amzn-SageMaker-Session-Id",
                        (SessionIdHeader.to_header x)))])
             |> Awso.Http.Headers.of_list) in
      let body = BodyBlob.to_header req.body in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | InvokeEndpoint ->
      if is_success
      then
        let body = BodyBlob.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (InvokeEndpointOutput.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some InvokeEndpointOutput.error_of_json))
  | InvokeEndpointAsync ->
      if is_success
      then Ok (InvokeEndpointAsyncOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some InvokeEndpointAsyncOutput.error_of_json))
  | InvokeEndpointWithResponseStream ->
      if is_success
      then
        let body = ResponseStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (InvokeEndpointWithResponseStreamOutput.of_header_and_body
             (headers, body))
      else
        Error
          (parse_aws_error
             (Some InvokeEndpointWithResponseStreamOutput.error_of_json))
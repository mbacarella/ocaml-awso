(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | AssociateFeed: (AssociateFeedRequest.t, AssociateFeedResponse.t,
  AssociateFeedResponse.error) t 
  | CreateFeed: (CreateFeedRequest.t, CreateFeedResponse.t,
  CreateFeedResponse.error) t 
  | DeleteFeed: (DeleteFeedRequest.t, DeleteFeedResponse.t,
  DeleteFeedResponse.error) t 
  | DisassociateFeed: (DisassociateFeedRequest.t, DisassociateFeedResponse.t,
  DisassociateFeedResponse.error) t 
  | GetFeed: (GetFeedRequest.t, GetFeedResponse.t, GetFeedResponse.error) t 
  | ListFeeds: (ListFeedsRequest.t, ListFeedsResponse.t,
  ListFeedsResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | TagResource: (TagResourceRequest.t, unit, unit) t 
  | UntagResource: (UntagResourceRequest.t, unit, unit) t 
  | UpdateFeed: (UpdateFeedRequest.t, UpdateFeedResponse.t,
  UpdateFeedResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | AssociateFeed -> `POST
  | CreateFeed -> `POST
  | DeleteFeed -> `DELETE
  | DisassociateFeed -> `POST
  | GetFeed -> `GET
  | ListFeeds -> `GET
  | ListTagsForResource -> `GET
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateFeed -> `PUT
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | AssociateFeed ->
          (Format.kasprintf Uri.of_string) "/v1/feed/%s/associate"
            (FeedId.to_header x.AssociateFeedRequest.id)
      | CreateFeed -> (Format.kasprintf Uri.of_string) "/v1/feed"
      | DeleteFeed ->
          (Format.kasprintf Uri.of_string) "/v1/feed/%s"
            (FeedId.to_header x.DeleteFeedRequest.id)
      | DisassociateFeed ->
          (Format.kasprintf Uri.of_string) "/v1/feed/%s/disassociate"
            (FeedId.to_header x.DisassociateFeedRequest.id)
      | GetFeed ->
          (Format.kasprintf Uri.of_string) "/v1/feed/%s"
            (FeedId.to_header x.GetFeedRequest.id)
      | ListFeeds ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v1/feeds")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("maxResults",
                          (ListFeedsRequestMaxResultsInteger.to_header v)))
                  x.maxResults;
               Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                 x.nextToken])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/v1/tags/%s"
            (ResourceArn.to_header x.ListTagsForResourceRequest.resourceArn)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/v1/tags/%s"
            (ResourceArn.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/v1/tags/%s"
               (ResourceArn.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateFeed ->
          (Format.kasprintf Uri.of_string) "/v1/feed/%s"
            (FeedId.to_header x.UpdateFeedRequest.id))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | AssociateFeed ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("associatedResourceName",
                           (AssociatedResourceName.to_value
                              req.AssociateFeedRequest.associatedResourceName));
                      Some
                        ("outputs",
                          (CreateOutputList.to_value
                             req.AssociateFeedRequest.outputs));
                      Option.map req.AssociateFeedRequest.dryRun
                        ~f:(fun x -> ("dryRun", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateFeed ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("name",
                           (ResourceName.to_value req.CreateFeedRequest.name));
                      Some
                        ("outputs",
                          (CreateOutputList.to_value
                             req.CreateFeedRequest.outputs));
                      Option.map req.CreateFeedRequest.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteFeed -> Awso.Http.Request.make (method_of_endpoint endp)
  | DisassociateFeed ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("associatedResourceName",
                           (AssociatedResourceName.to_value
                              req.DisassociateFeedRequest.associatedResourceName));
                      Option.map req.DisassociateFeedRequest.dryRun
                        ~f:(fun x -> ("dryRun", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetFeed ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListFeeds ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateFeed -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | AssociateFeed ->
      if is_success
      then Ok (AssociateFeedResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some AssociateFeedResponse.error_of_json))
  | CreateFeed ->
      if is_success
      then Ok (CreateFeedResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateFeedResponse.error_of_json))
  | DeleteFeed ->
      if is_success
      then Ok (DeleteFeedResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DeleteFeedResponse.error_of_json))
  | DisassociateFeed ->
      if is_success
      then Ok (DisassociateFeedResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some DisassociateFeedResponse.error_of_json))
  | GetFeed ->
      if is_success
      then Ok (GetFeedResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetFeedResponse.error_of_json))
  | ListFeeds ->
      if is_success
      then Ok (ListFeedsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListFeedsResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | TagResource -> if is_success then Ok () else Error (parse_aws_error None)
  | UntagResource ->
      if is_success then Ok () else Error (parse_aws_error None)
  | UpdateFeed ->
      if is_success
      then Ok (UpdateFeedResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateFeedResponse.error_of_json))
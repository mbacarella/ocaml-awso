(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.elementalinference
let apiVersion = "2018-11-14"
let endpointPrefix = "elemental-inference"
let serviceFullName = "AWS Elemental Inference"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ResourceDescription =
  struct
    type nonrec t = string
    let context_ = "ResourceDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"[\\w \\-\\.',@:;]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDescription" j
    let to_json = simple_to_json to_value
  end
module ClippingConfig =
  struct
    type nonrec t =
      {
      callbackMetadata: ResourceDescription.t option
        [@ocaml.doc
          "The metadata that is the result of the clip request to Elemental Inference."]}
    let make ?callbackMetadata = fun () -> { callbackMetadata }
    let to_value x =
      structure_to_value
        [("callbackMetadata",
           (Option.map x.callbackMetadata ~f:ResourceDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let callbackMetadata =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "callbackMetadata") in
      make ?callbackMetadata ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let callbackMetadata =
        field_map json__ "callbackMetadata" ResourceDescription.of_json in
      make ?callbackMetadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A type of OutputConfig, used when the output in a feed is for the clip feature."]
module CroppingConfig =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A type of OutputConfig, used when the output in a feed is for the crop feature."]
module AssociatedResourceName =
  struct
    type nonrec t = string
    let context_ = "AssociatedResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"[\\w \\-\\.',@:;/]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AssociatedResourceName" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module OutputConfig =
  struct
    type nonrec t =
      {
      cropping: CroppingConfig.t option
        [@ocaml.doc
          "The output config type that applies to the cropping feature."];
      clipping: ClippingConfig.t option
        [@ocaml.doc
          "The output config type that applies to the clipping feature."]}
    let make ?cropping = fun ?clipping -> fun () -> { cropping; clipping }
    let to_value x =
      structure_to_value
        [("cropping", (Option.map x.cropping ~f:CroppingConfig.to_value));
        ("clipping", (Option.map x.clipping ~f:ClippingConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clipping =
        (Option.map ~f:ClippingConfig.of_xml) (Xml.child xml_arg0 "clipping") in
      let cropping =
        (Option.map ~f:CroppingConfig.of_xml) (Xml.child xml_arg0 "cropping") in
      make ?clipping ?cropping ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clipping = field_map json__ "clipping" ClippingConfig.of_json in
      let cropping = field_map json__ "cropping" CroppingConfig.of_json in
      make ?clipping ?cropping ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains one typed output. It is used in the CreateOutput, GetOutput, and Update Output structures."]
module OutputStatus =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OutputStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OutputStatus" j)
    let to_json = simple_to_json to_value
  end
module ResourceName =
  struct
    type nonrec t = string
    let context_ = "ResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"[a-zA-Z0-9]([a-zA-Z0-9-_]{0,126}[a-zA-Z0-9])?");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end
module FeedArn =
  struct
    type nonrec t = string
    let context_ = "FeedArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FeedArn" j
    let to_json = simple_to_json to_value
  end
module FeedAssociation =
  struct
    type nonrec t =
      {
      associatedResourceName: AssociatedResourceName.t option
        [@ocaml.doc "The name of the associated resource."]}
    let make ?associatedResourceName = fun () -> { associatedResourceName }
    let to_value x =
      structure_to_value
        [("associatedResourceName",
           (Option.map x.associatedResourceName
              ~f:AssociatedResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associatedResourceName =
        (Option.map ~f:AssociatedResourceName.of_xml)
          (Xml.child xml_arg0 "associatedResourceName") in
      make ?associatedResourceName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let associatedResourceName =
        field_map json__ "associatedResourceName"
          AssociatedResourceName.of_json in
      make ?associatedResourceName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the resource that is associated with a feed. It is used in the FeedSummary that is used in the response of a ListFeeds action."]
module FeedId =
  struct
    type nonrec t = string
    let context_ = "FeedId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-z0-9]{19}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FeedId" j
    let to_json = simple_to_json to_value
  end
module FeedStatus =
  struct
    type nonrec t =
      | CREATING 
      | AVAILABLE 
      | ACTIVE 
      | UPDATING 
      | DELETING 
      | DELETED 
      | ARCHIVED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | AVAILABLE -> "AVAILABLE"
      | ACTIVE -> "ACTIVE"
      | UPDATING -> "UPDATING"
      | DELETING -> "DELETING"
      | DELETED -> "DELETED"
      | ARCHIVED -> "ARCHIVED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "AVAILABLE" -> AVAILABLE
      | "ACTIVE" -> ACTIVE
      | "UPDATING" -> UPDATING
      | "DELETING" -> DELETING
      | "DELETED" -> DELETED
      | "ARCHIVED" -> ARCHIVED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FeedStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FeedStatus" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module GetOutput =
  struct
    type nonrec t =
      {
      name: ResourceName.t option [@ocaml.doc "The ARN of the output."];
      outputConfig: OutputConfig.t option
        [@ocaml.doc
          "A typed property for an output in a feed. It is used in the GetFeed action. It identifies the action for Elemental Inference to perform. It also provides a repository for the results of that action. For example, CroppingConfig output will contain the metadata for the crop feature."];
      status: OutputStatus.t option [@ocaml.doc "The status of the output."];
      description: ResourceDescription.t option
        [@ocaml.doc "The description of the output."];
      fromAssociation: Boolean.t option
        [@ocaml.doc
          "True means that the output was originally created in the feed by the AssociateFeed operation. False means it was created using CreateFeed or UpdateFeed. You will need this value if you use the UpdateFeed operation to modify the list of outputs in the feed."]}
    let make ?name =
      fun ?outputConfig ->
        fun ?status ->
          fun ?description ->
            fun ?fromAssociation ->
              fun () ->
                { name; outputConfig; status; description; fromAssociation }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ResourceName.to_value));
        ("outputConfig",
          (Option.map x.outputConfig ~f:OutputConfig.to_value));
        ("status", (Option.map x.status ~f:OutputStatus.to_value));
        ("description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("fromAssociation",
          (Option.map x.fromAssociation ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fromAssociation =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "fromAssociation") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let status =
        (Option.map ~f:OutputStatus.of_xml) (Xml.child xml_arg0 "status") in
      let outputConfig =
        (Option.map ~f:OutputConfig.of_xml)
          (Xml.child xml_arg0 "outputConfig") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      make ?fromAssociation ?description ?status ?outputConfig ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fromAssociation =
        field_map json__ "fromAssociation" Boolean.of_json in
      let description =
        field_map json__ "description" ResourceDescription.of_json in
      let status = field_map json__ "status" OutputStatus.of_json in
      let outputConfig = field_map json__ "outputConfig" OutputConfig.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      make ?fromAssociation ?description ?status ?outputConfig ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configuration information about one output in a feed. It is used in the GetFeed action."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module UpdateOutput =
  struct
    type nonrec t =
      {
      name: ResourceName.t [@ocaml.doc "The name start here"];
      outputConfig: OutputConfig.t
        [@ocaml.doc
          "A typed property for an output in a feed. It is used in the UpdateFeed action. It identifies the action for Elemental Inference to perform. It also provides a repository for the results of that action. For example, CroppingConfig output will contain the metadata for the crop feature."];
      status: OutputStatus.t [@ocaml.doc "The status of the output."];
      description: ResourceDescription.t option
        [@ocaml.doc "A description of the output."];
      fromAssociation: Boolean.t option
        [@ocaml.doc
          "This property is set by the service when you add the output to the feed, and indicates how you added the output. True means that you used the AssociateFeed operation. False means that you used the CreateFeed or UpdateFeed operation. Use GetFeed to obtain the value. If the value is True, include this field here with a value of True. If the value is False, omit the field here."]}
    let context_ = "UpdateOutput"
    let make ?description =
      fun ?fromAssociation ->
        fun ~name ->
          fun ~outputConfig ->
            fun ~status ->
              fun () ->
                { description; fromAssociation; name; outputConfig; status }
    let to_value x =
      structure_to_value
        [("name", (Some (ResourceName.to_value x.name)));
        ("outputConfig", (Some (OutputConfig.to_value x.outputConfig)));
        ("status", (Some (OutputStatus.to_value x.status)));
        ("description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("fromAssociation",
          (Option.map x.fromAssociation ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fromAssociation =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "fromAssociation") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let status =
        OutputStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let outputConfig =
        OutputConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outputConfig") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?fromAssociation ?description ~status ~outputConfig ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fromAssociation =
        field_map json__ "fromAssociation" Boolean.of_json in
      let description =
        field_map json__ "description" ResourceDescription.of_json in
      let status = field_map_exn json__ "status" OutputStatus.of_json in
      let outputConfig =
        field_map_exn json__ "outputConfig" OutputConfig.of_json in
      let name = field_map_exn json__ "name" ResourceName.of_json in
      make ?fromAssociation ?description ~status ~outputConfig ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configuration information about one output in a feed. It is used in the UpdateFeed action."]
module FeedSummary =
  struct
    type nonrec t =
      {
      arn: FeedArn.t option [@ocaml.doc "The ARN of the feed."];
      id: FeedId.t option [@ocaml.doc "The ID of the feed."];
      name: ResourceName.t option [@ocaml.doc "The name of the feed"];
      association: FeedAssociation.t option
        [@ocaml.doc "The resource, if any, associated with the feed."];
      status: FeedStatus.t option [@ocaml.doc "The status of the feed."]}
    let make ?arn =
      fun ?id ->
        fun ?name ->
          fun ?association ->
            fun ?status -> fun () -> { arn; id; name; association; status }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:FeedArn.to_value));
        ("id", (Option.map x.id ~f:FeedId.to_value));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("association",
          (Option.map x.association ~f:FeedAssociation.to_value));
        ("status", (Option.map x.status ~f:FeedStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:FeedStatus.of_xml) (Xml.child xml_arg0 "status") in
      let association =
        (Option.map ~f:FeedAssociation.of_xml)
          (Xml.child xml_arg0 "association") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:FeedId.of_xml) (Xml.child xml_arg0 "id") in
      let arn = (Option.map ~f:FeedArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?status ?association ?name ?id ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" FeedStatus.of_json in
      let association =
        field_map json__ "association" FeedAssociation.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let id = field_map json__ "id" FeedId.of_json in
      let arn = field_map json__ "arn" FeedArn.of_json in
      make ?status ?association ?name ?id ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configuration information about a feed. It is used in the ListFeeds action."]
module CreateOutput =
  struct
    type nonrec t =
      {
      name: ResourceName.t [@ocaml.doc "A name for the output."];
      outputConfig: OutputConfig.t
        [@ocaml.doc
          "A typed property for an output in a feed. It is used in the CreateFeed and AssociateFeed actions. It identifies the action for Elemental Inference to perform. It also provides a repository for the results of that action. For example, CroppingConfig output will contain the metadata for the crop feature."];
      status: OutputStatus.t
        [@ocaml.doc "The status to assign to the output."];
      description: ResourceDescription.t option
        [@ocaml.doc "A description for the output."]}
    let context_ = "CreateOutput"
    let make ?description =
      fun ~name ->
        fun ~outputConfig ->
          fun ~status ->
            fun () -> { description; name; outputConfig; status }
    let to_value x =
      structure_to_value
        [("name", (Some (ResourceName.to_value x.name)));
        ("outputConfig", (Some (OutputConfig.to_value x.outputConfig)));
        ("status", (Some (OutputStatus.to_value x.status)));
        ("description",
          (Option.map x.description ~f:ResourceDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let status =
        OutputStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      let outputConfig =
        OutputConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outputConfig") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?description ~status ~outputConfig ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description" ResourceDescription.of_json in
      let status = field_map_exn json__ "status" OutputStatus.of_json in
      let outputConfig =
        field_map_exn json__ "outputConfig" OutputConfig.of_json in
      let name = field_map_exn json__ "name" ResourceName.of_json in
      make ?description ~status ~outputConfig ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configuration information about one output in a feed. It is used in the AssociateFeed and the CreateFeed actions."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient access to perform this action."]
module ConflictException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request could not be completed due to a conflict."]
module GetOutputList =
  struct
    type nonrec t = GetOutput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GetOutput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GetOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"GetOutputList" ~of_json:GetOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module InternalServerErrorException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An internal server error occurred. This is a temporary condition and the request can be retried. If the problem persists, contact AWS Support."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource specified in the action doesn't exist."]
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was rejected because it would exceed one or more service quotas for your account. Review your service quotas and either delete unused resources or request a quota increase."]
module StringList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"StringList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module TooManyRequestException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was denied due to request throttling. Too many requests have been made within a given time period. Reduce the frequency of requests and use exponential backoff when retrying."]
module ValidationException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input fails to satisfy the constraints specified by the service. Check the error message for details about which parameter or field is invalid and correct the request before retrying."]
module UpdateOutputList =
  struct
    type nonrec t = UpdateOutput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:UpdateOutput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UpdateOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"UpdateOutputList" ~of_json:UpdateOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceArn =
  struct
    type nonrec t = string
    let context_ = "ResourceArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceArn" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module FeedSummaryList =
  struct
    type nonrec t = FeedSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FeedSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FeedSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"FeedSummaryList" ~of_json:FeedSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListFeedsRequestMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListFeedsRequestMaxResultsInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateOutputList =
  struct
    type nonrec t = CreateOutput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CreateOutput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CreateOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"CreateOutputList" ~of_json:CreateOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateFeedResponse =
  struct
    type nonrec t =
      {
      arn: FeedArn.t option [@ocaml.doc "The ARN of the feed."];
      name: ResourceName.t option
        [@ocaml.doc "The updated or original name of the feed."];
      id: FeedId.t option [@ocaml.doc "The ID of the feed."];
      dataEndpoints: StringList.t option
        [@ocaml.doc "The data endpoints of the feed."];
      outputs: GetOutputList.t option
        [@ocaml.doc
          "The array of outputs in the feed. You might have left this array unchanged, or you might have changed it."];
      status: FeedStatus.t option [@ocaml.doc "The status of the output."];
      association: FeedAssociation.t option
        [@ocaml.doc
          "True means that the output was originally created in the feed by the AssociateFeed operation. False means it was created using CreateFeed or UpdateFeed. You will need this value if you use the UpdateFeed operation to modify the list of outputs in the feed."];
      tags: TagMap.t option
        [@ocaml.doc
          "The name of the resource currently associated with the feed, if any."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestException of TooManyRequestException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?name ->
        fun ?id ->
          fun ?dataEndpoints ->
            fun ?outputs ->
              fun ?status ->
                fun ?association ->
                  fun ?tags ->
                    fun () ->
                      {
                        arn;
                        name;
                        id;
                        dataEndpoints;
                        outputs;
                        status;
                        association;
                        tags
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestException e ->
          `Assoc
            [("error", (`String "TooManyRequestException"));
            ("details", (TooManyRequestException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:FeedArn.to_value));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("id", (Option.map x.id ~f:FeedId.to_value));
        ("dataEndpoints",
          (Option.map x.dataEndpoints ~f:StringList.to_value));
        ("outputs", (Option.map x.outputs ~f:GetOutputList.to_value));
        ("status", (Option.map x.status ~f:FeedStatus.to_value));
        ("association",
          (Option.map x.association ~f:FeedAssociation.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let association =
        (Option.map ~f:FeedAssociation.of_xml)
          (Xml.child xml_arg0 "association") in
      let status =
        (Option.map ~f:FeedStatus.of_xml) (Xml.child xml_arg0 "status") in
      let outputs =
        (Option.map ~f:GetOutputList.of_xml) (Xml.child xml_arg0 "outputs") in
      let dataEndpoints =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "dataEndpoints") in
      let id = (Option.map ~f:FeedId.of_xml) (Xml.child xml_arg0 "id") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let arn = (Option.map ~f:FeedArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?association ?status ?outputs ?dataEndpoints ?id ?name ?arn
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let association =
        field_map json__ "association" FeedAssociation.of_json in
      let status = field_map json__ "status" FeedStatus.of_json in
      let outputs = field_map json__ "outputs" GetOutputList.of_json in
      let dataEndpoints = field_map json__ "dataEndpoints" StringList.of_json in
      let id = field_map json__ "id" FeedId.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let arn = field_map json__ "arn" FeedArn.of_json in
      make ?tags ?association ?status ?outputs ?dataEndpoints ?id ?name ?arn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the name and/or outputs in a feed."]
module UpdateFeedRequest =
  struct
    type nonrec t =
      {
      name: ResourceName.t
        [@ocaml.doc
          "Required. You can specify the existing name (to leave it unchanged) or a new name."];
      id: FeedId.t [@ocaml.doc "The ID of the feed to update."];
      outputs: UpdateOutputList.t
        [@ocaml.doc
          "Required. You can specify the existing array of outputs (to leave outputs unchanged) or you can specify a new array."]}
    let context_ = "UpdateFeedRequest"
    let make ~name =
      fun ~id -> fun ~outputs -> fun () -> { name; id; outputs }
    let to_value x =
      structure_to_value
        [("name", (Some (ResourceName.to_value x.name)));
        ("id", (Some (FeedId.to_value x.id)));
        ("outputs", (Some (UpdateOutputList.to_value x.outputs)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputs =
        UpdateOutputList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outputs") in
      let id = FeedId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~outputs ~id ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let outputs = field_map_exn json__ "outputs" UpdateOutputList.of_json in
      let id = field_map_exn json__ "id" FeedId.of_json in
      let name = field_map_exn json__ "name" ResourceName.of_json in
      make ~outputs ~id ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the name and/or outputs in a feed."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc
          "The ARN of the resource where you want to delete one or more tags."];
      tagKeys: TagKeyList.t [@ocaml.doc "The keys of the tags to delete."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "tagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes specified tags from the specified resource in the current region."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The ARN of the resource where you want to add tags."];
      tags: TagMap.t [@ocaml.doc "A list of tags to add to the resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagMap.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified tags to the resource identified by the specified resourceArn in the current region. If existing tags on a resource are not specified in the request parameters, they are not changed. When a resource is deleted, the tags associated with that resource are also deleted."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagMap.t option
        [@ocaml.doc "A list of the tags that belong to this resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestException of TooManyRequestException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestException e ->
          `Assoc
            [("error", (`String "TooManyRequestException"));
            ("details", (TooManyRequestException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "List all tags that are on an Elemental Inference resource in the current region."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The ARN of the resource whose tags you want to query."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "List all tags that are on an Elemental Inference resource in the current region."]
module ListFeedsResponse =
  struct
    type nonrec t =
      {
      feeds: FeedSummaryList.t option
        [@ocaml.doc "A list of feed summaries."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token that identifies the batch of results that you want to see. For example, you submit a list request with MaxResults set at 5. The service returns the first batch of results (up to 5) and a NextToken value. To see the next batch of results, you can submit the list request a second time and specify the NextToken value."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestException of TooManyRequestException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?feeds = fun ?nextToken -> fun () -> { feeds; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestException e ->
          `Assoc
            [("error", (`String "TooManyRequestException"));
            ("details", (TooManyRequestException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("feeds", (Option.map x.feeds ~f:FeedSummaryList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let feeds =
        (Option.map ~f:FeedSummaryList.of_xml) (Xml.child xml_arg0 "feeds") in
      make ?nextToken ?feeds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let feeds = field_map json__ "feeds" FeedSummaryList.of_json in
      make ?nextToken ?feeds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Displays a list of feeds that belong to this AWS account."]
module ListFeedsRequest =
  struct
    type nonrec t =
      {
      maxResults: ListFeedsRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of results to return per API request. For example, you submit a list request with MaxResults set at 5. Although 20 items match your request, the service returns no more than the first 5 items. (The service also returns a NextToken value that you can use to fetch the next batch of results.) The service might return fewer results than the MaxResults value. If MaxResults is not included in the request, the service defaults to pagination with a maximum of 10 results per page. Valid Range: Minimum value of 1. Maximum value of 1000."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token that identifies the batch of results that you want to see. For example, you submit a ListBridges request with MaxResults set at 5. The service returns the first batch of results (up to 5) and a NextToken value. To see the next batch of results, you can submit the ListBridges request a second time and specify the NextToken value."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults
              ~f:ListFeedsRequestMaxResultsInteger.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListFeedsRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListFeedsRequestMaxResultsInteger.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Displays a list of feeds that belong to this AWS account."]
module GetFeedResponse =
  struct
    type nonrec t =
      {
      arn: FeedArn.t option [@ocaml.doc "The ARN of the feed."];
      name: ResourceName.t option
        [@ocaml.doc "The name of the feed being queried."];
      id: FeedId.t option [@ocaml.doc "The ID of the feed being queried."];
      dataEndpoints: StringList.t option
        [@ocaml.doc "The dataEndpoints of the feed being queried."];
      outputs: GetOutputList.t option
        [@ocaml.doc "An array of the outputs in the feed being queried."];
      status: FeedStatus.t option
        [@ocaml.doc "The status of the feed being queried."];
      association: FeedAssociation.t option
        [@ocaml.doc
          "Information about the resource, if any, associated with the feed being queried."];
      tags: TagMap.t option
        [@ocaml.doc
          "A list of the tags, if any, for the feed being queried."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestException of TooManyRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?name ->
        fun ?id ->
          fun ?dataEndpoints ->
            fun ?outputs ->
              fun ?status ->
                fun ?association ->
                  fun ?tags ->
                    fun () ->
                      {
                        arn;
                        name;
                        id;
                        dataEndpoints;
                        outputs;
                        status;
                        association;
                        tags
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestException e ->
          `Assoc
            [("error", (`String "TooManyRequestException"));
            ("details", (TooManyRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:FeedArn.to_value));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("id", (Option.map x.id ~f:FeedId.to_value));
        ("dataEndpoints",
          (Option.map x.dataEndpoints ~f:StringList.to_value));
        ("outputs", (Option.map x.outputs ~f:GetOutputList.to_value));
        ("status", (Option.map x.status ~f:FeedStatus.to_value));
        ("association",
          (Option.map x.association ~f:FeedAssociation.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let association =
        (Option.map ~f:FeedAssociation.of_xml)
          (Xml.child xml_arg0 "association") in
      let status =
        (Option.map ~f:FeedStatus.of_xml) (Xml.child xml_arg0 "status") in
      let outputs =
        (Option.map ~f:GetOutputList.of_xml) (Xml.child xml_arg0 "outputs") in
      let dataEndpoints =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "dataEndpoints") in
      let id = (Option.map ~f:FeedId.of_xml) (Xml.child xml_arg0 "id") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let arn = (Option.map ~f:FeedArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?association ?status ?outputs ?dataEndpoints ?id ?name ?arn
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let association =
        field_map json__ "association" FeedAssociation.of_json in
      let status = field_map json__ "status" FeedStatus.of_json in
      let outputs = field_map json__ "outputs" GetOutputList.of_json in
      let dataEndpoints = field_map json__ "dataEndpoints" StringList.of_json in
      let id = field_map json__ "id" FeedId.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let arn = field_map json__ "arn" FeedArn.of_json in
      make ?tags ?association ?status ?outputs ?dataEndpoints ?id ?name ?arn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about the specified feed."]
module GetFeedRequest =
  struct
    type nonrec t =
      {
      id: FeedId.t [@ocaml.doc "The ID of the feed to query."]}
    let context_ = "GetFeedRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (FeedId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = FeedId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" FeedId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about the specified feed."]
module DisassociateFeedResponse =
  struct
    type nonrec t =
      {
      arn: FeedArn.t option
        [@ocaml.doc
          "The ID of the feed where you deleted the associated resource."];
      id: FeedId.t option
        [@ocaml.doc "The ARN of the resource that you deleted."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestException of TooManyRequestException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun ?id -> fun () -> { arn; id }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestException e ->
          `Assoc
            [("error", (`String "TooManyRequestException"));
            ("details", (TooManyRequestException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:FeedArn.to_value));
        ("id", (Option.map x.id ~f:FeedId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = (Option.map ~f:FeedId.of_xml) (Xml.child xml_arg0 "id") in
      let arn = (Option.map ~f:FeedArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?id ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map json__ "id" FeedId.of_json in
      let arn = field_map json__ "arn" FeedArn.of_json in make ?id ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Releases the resource (for example, an MediaLive channel) that is associated with this feed. The outputs in the feed become disabled."]
module DisassociateFeedRequest =
  struct
    type nonrec t =
      {
      id: FeedId.t
        [@ocaml.doc
          "The ID of the feed where you want to release the resource."];
      associatedResourceName: AssociatedResourceName.t
        [@ocaml.doc
          "The name of the resource currently associated with the feed'."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Set to true if you want to do a dry run of the disassociate action."]}
    let context_ = "DisassociateFeedRequest"
    let make ?dryRun =
      fun ~id ->
        fun ~associatedResourceName ->
          fun () -> { dryRun; id; associatedResourceName }
    let to_value x =
      structure_to_value
        [("id", (Some (FeedId.to_value x.id)));
        ("associatedResourceName",
          (Some (AssociatedResourceName.to_value x.associatedResourceName)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let associatedResourceName =
        AssociatedResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "associatedResourceName") in
      let id = FeedId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?dryRun ~associatedResourceName ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "dryRun" Boolean.of_json in
      let associatedResourceName =
        field_map_exn json__ "associatedResourceName"
          AssociatedResourceName.of_json in
      let id = field_map_exn json__ "id" FeedId.of_json in
      make ?dryRun ~associatedResourceName ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Releases the resource (for example, an MediaLive channel) that is associated with this feed. The outputs in the feed become disabled."]
module DeleteFeedResponse =
  struct
    type nonrec t =
      {
      arn: FeedArn.t option [@ocaml.doc "The ARN of the deleted feed."];
      id: FeedId.t option [@ocaml.doc "The ID of the deleted feed."];
      status: FeedStatus.t option
        [@ocaml.doc
          "The current status of the feed. When deletion of the feed has succeeded, the status will be DELETED."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyRequestException of TooManyRequestException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun ?id -> fun ?status -> fun () -> { arn; id; status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyRequestException e ->
          `Assoc
            [("error", (`String "TooManyRequestException"));
            ("details", (TooManyRequestException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:FeedArn.to_value));
        ("id", (Option.map x.id ~f:FeedId.to_value));
        ("status", (Option.map x.status ~f:FeedStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:FeedStatus.of_xml) (Xml.child xml_arg0 "status") in
      let id = (Option.map ~f:FeedId.of_xml) (Xml.child xml_arg0 "id") in
      let arn = (Option.map ~f:FeedArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?status ?id ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" FeedStatus.of_json in
      let id = field_map json__ "id" FeedId.of_json in
      let arn = field_map json__ "arn" FeedArn.of_json in
      make ?status ?id ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified feed. The feed can be deleted at any time."]
module DeleteFeedRequest =
  struct
    type nonrec t = {
      id: FeedId.t [@ocaml.doc "The ID of the feed."]}
    let context_ = "DeleteFeedRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (FeedId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = FeedId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" FeedId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified feed. The feed can be deleted at any time."]
module CreateFeedResponse =
  struct
    type nonrec t =
      {
      arn: FeedArn.t option
        [@ocaml.doc
          "A unique ARN that Elemental Inference assigns to the feed."];
      name: ResourceName.t option [@ocaml.doc "The name that you specified."];
      id: FeedId.t option
        [@ocaml.doc
          "A unique ID that Elemental Inference assigns to the feed."];
      dataEndpoints: StringList.t option
        [@ocaml.doc
          "A unique ARN that Elemental Inference assigns to the feed."];
      outputs: GetOutputList.t option
        [@ocaml.doc
          "Data endpoints that Elemental Inference assigns to the feed."];
      status: FeedStatus.t option
        [@ocaml.doc
          "The current status of the feed. After creation of the feed has succeeded, the status will be AVAILABLE."];
      association: FeedAssociation.t option
        [@ocaml.doc
          "The association for this feed. When you create the feed, this property is empty. You must associate a resource with the feed using AssociateFeed."];
      tags: TagMap.t option
        [@ocaml.doc "Any tags that you included when you created the feed."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestException of TooManyRequestException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn =
      fun ?name ->
        fun ?id ->
          fun ?dataEndpoints ->
            fun ?outputs ->
              fun ?status ->
                fun ?association ->
                  fun ?tags ->
                    fun () ->
                      {
                        arn;
                        name;
                        id;
                        dataEndpoints;
                        outputs;
                        status;
                        association;
                        tags
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestException e ->
          `Assoc
            [("error", (`String "TooManyRequestException"));
            ("details", (TooManyRequestException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:FeedArn.to_value));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("id", (Option.map x.id ~f:FeedId.to_value));
        ("dataEndpoints",
          (Option.map x.dataEndpoints ~f:StringList.to_value));
        ("outputs", (Option.map x.outputs ~f:GetOutputList.to_value));
        ("status", (Option.map x.status ~f:FeedStatus.to_value));
        ("association",
          (Option.map x.association ~f:FeedAssociation.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let association =
        (Option.map ~f:FeedAssociation.of_xml)
          (Xml.child xml_arg0 "association") in
      let status =
        (Option.map ~f:FeedStatus.of_xml) (Xml.child xml_arg0 "status") in
      let outputs =
        (Option.map ~f:GetOutputList.of_xml) (Xml.child xml_arg0 "outputs") in
      let dataEndpoints =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "dataEndpoints") in
      let id = (Option.map ~f:FeedId.of_xml) (Xml.child xml_arg0 "id") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let arn = (Option.map ~f:FeedArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?association ?status ?outputs ?dataEndpoints ?id ?name ?arn
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let association =
        field_map json__ "association" FeedAssociation.of_json in
      let status = field_map json__ "status" FeedStatus.of_json in
      let outputs = field_map json__ "outputs" GetOutputList.of_json in
      let dataEndpoints = field_map json__ "dataEndpoints" StringList.of_json in
      let id = field_map json__ "id" FeedId.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let arn = field_map json__ "arn" FeedArn.of_json in
      make ?tags ?association ?status ?outputs ?dataEndpoints ?id ?name ?arn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a feed. The feed is the target for live streams being sent by the calling application. An example of a calling application is AWS Elemental MediaLive. After you create the feed, you can associate a resource with the feed."]
module CreateFeedRequest =
  struct
    type nonrec t =
      {
      name: ResourceName.t [@ocaml.doc "A name for this feed."];
      outputs: CreateOutputList.t
        [@ocaml.doc
          "An array of outputs for this feed. Each output represents a specific Elemental Inference feature. For example, an output might represent the crop feature."];
      tags: TagMap.t option
        [@ocaml.doc
          "If you want to include tags, add them now. You won't be able to add them later."]}
    let context_ = "CreateFeedRequest"
    let make ?tags =
      fun ~name -> fun ~outputs -> fun () -> { tags; name; outputs }
    let to_value x =
      structure_to_value
        [("name", (Some (ResourceName.to_value x.name)));
        ("outputs", (Some (CreateOutputList.to_value x.outputs)));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let outputs =
        CreateOutputList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outputs") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ~outputs ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let outputs = field_map_exn json__ "outputs" CreateOutputList.of_json in
      let name = field_map_exn json__ "name" ResourceName.of_json in
      make ?tags ~outputs ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a feed. The feed is the target for live streams being sent by the calling application. An example of a calling application is AWS Elemental MediaLive. After you create the feed, you can associate a resource with the feed."]
module AssociateFeedResponse =
  struct
    type nonrec t =
      {
      arn: FeedArn.t option [@ocaml.doc "The AWS ARN for this association."];
      id: FeedId.t option
        [@ocaml.doc
          "An ID for this response. It is unique in Elemental Inference for this AWS account."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerErrorException of InternalServerErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `TooManyRequestException of TooManyRequestException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?arn = fun ?id -> fun () -> { arn; id }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerErrorException" ->
          `InternalServerErrorException
            (InternalServerErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "TooManyRequestException" ->
          `TooManyRequestException (TooManyRequestException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerErrorException e ->
          `Assoc
            [("error", (`String "InternalServerErrorException"));
            ("details", (InternalServerErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `TooManyRequestException e ->
          `Assoc
            [("error", (`String "TooManyRequestException"));
            ("details", (TooManyRequestException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:FeedArn.to_value));
        ("id", (Option.map x.id ~f:FeedId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = (Option.map ~f:FeedId.of_xml) (Xml.child xml_arg0 "id") in
      let arn = (Option.map ~f:FeedArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?id ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map json__ "id" FeedId.of_json in
      let arn = field_map json__ "arn" FeedArn.of_json in make ?id ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a resource with the feed. The resource provides the input that Elemental Inference needs needs in order to perform an Elemental Inference feature, such as cropping video. You always provide the resource by associating it with a feed. You can associate only one resource with each feed."]
module AssociateFeedRequest =
  struct
    type nonrec t =
      {
      id: FeedId.t [@ocaml.doc "The ID of the feed."];
      associatedResourceName: AssociatedResourceName.t
        [@ocaml.doc
          "An identifier for the resource. If the resource is from an AWS service, this identifier must be the full ARN of that resource. Otherwise, the identifier is a name that you assign and that is appropriate for the application that owns the resource. This name must not resemble an ARN."];
      outputs: CreateOutputList.t
        [@ocaml.doc
          "The outputs to add to this feed. You must specify at least one output. You can later use the UpdateFeed action to change the list of outputs."];
      dryRun: Boolean.t option
        [@ocaml.doc
          "Set to true if you want to do a dry run of the associate action."]}
    let context_ = "AssociateFeedRequest"
    let make ?dryRun =
      fun ~id ->
        fun ~associatedResourceName ->
          fun ~outputs ->
            fun () -> { dryRun; id; associatedResourceName; outputs }
    let to_value x =
      structure_to_value
        [("id", (Some (FeedId.to_value x.id)));
        ("associatedResourceName",
          (Some (AssociatedResourceName.to_value x.associatedResourceName)));
        ("outputs", (Some (CreateOutputList.to_value x.outputs)));
        ("dryRun", (Option.map x.dryRun ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dryRun =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "dryRun") in
      let outputs =
        CreateOutputList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "outputs") in
      let associatedResourceName =
        AssociatedResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "associatedResourceName") in
      let id = FeedId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?dryRun ~outputs ~associatedResourceName ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dryRun = field_map json__ "dryRun" Boolean.of_json in
      let outputs = field_map_exn json__ "outputs" CreateOutputList.of_json in
      let associatedResourceName =
        field_map_exn json__ "associatedResourceName"
          AssociatedResourceName.of_json in
      let id = field_map_exn json__ "id" FeedId.of_json in
      make ?dryRun ~outputs ~associatedResourceName ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a resource with the feed. The resource provides the input that Elemental Inference needs needs in order to perform an Elemental Inference feature, such as cropping video. You always provide the resource by associating it with a feed. You can associate only one resource with each feed."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trackingOptions =
         flag "tracking-options" (optional json_arg)
           ~doc:"JSON TrackingOptions"
       and deliveryOptions =
         flag "delivery-options" (optional json_arg)
           ~doc:"JSON DeliveryOptions"
       and reputationOptions =
         flag "reputation-options" (optional json_arg)
           ~doc:"JSON ReputationOptions"
       and sendingOptions =
         flag "sending-options" (optional json_arg)
           ~doc:"JSON SendingOptions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_set
           (Values.CreateConfigurationSetRequest.make
              ?trackingOptions:(Option.map ~f:Values.TrackingOptions.of_json
                                  trackingOptions)
              ?deliveryOptions:(Option.map ~f:Values.DeliveryOptions.of_json
                                  deliveryOptions)
              ?reputationOptions:(Option.map
                                    ~f:Values.ReputationOptions.of_json
                                    reputationOptions)
              ?sendingOptions:(Option.map ~f:Values.SendingOptions.of_json
                                 sendingOptions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~configurationSetName ())
           (Some Values.CreateConfigurationSetResponse.to_json)
           (Some Values.CreateConfigurationSetResponse.error_to_json)])
let create_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName"
       and eventDestination =
         flag "event-destination" (required json_arg)
           ~doc:"JSON EventDestinationDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_set_event_destination
           (Values.CreateConfigurationSetEventDestinationRequest.make
              ~configurationSetName ~eventDestinationName
              ~eventDestination:(Values.EventDestinationDefinition.of_json
                                   eventDestination) ())
           (Some
              Values.CreateConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.CreateConfigurationSetEventDestinationResponse.error_to_json)])
let create_dedicated_ip_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and poolName =
         flag "pool-name" (required string) ~doc:"STRING PoolName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dedicated_ip_pool
           (Values.CreateDedicatedIpPoolRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~poolName ())
           (Some Values.CreateDedicatedIpPoolResponse.to_json)
           (Some Values.CreateDedicatedIpPoolResponse.error_to_json)])
let create_deliverability_test_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportName =
         flag "report-name" (optional string) ~doc:"STRING ReportName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and fromEmailAddress =
         flag "from-email-address" (required string)
           ~doc:"STRING EmailAddress"
       and content =
         flag "content" (required json_arg) ~doc:"JSON EmailContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deliverability_test_report
           (Values.CreateDeliverabilityTestReportRequest.make ?reportName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~fromEmailAddress
              ~content:(Values.EmailContent.of_json content) ())
           (Some Values.CreateDeliverabilityTestReportResponse.to_json)
           (Some Values.CreateDeliverabilityTestReportResponse.error_to_json)])
let create_email_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_email_identity
           (Values.CreateEmailIdentityRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~emailIdentity ())
           (Some Values.CreateEmailIdentityResponse.to_json)
           (Some Values.CreateEmailIdentityResponse.error_to_json)])
let delete_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_set
           (Values.DeleteConfigurationSetRequest.make ~configurationSetName
              ()) (Some Values.DeleteConfigurationSetResponse.to_json)
           (Some Values.DeleteConfigurationSetResponse.error_to_json)])
let delete_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_set_event_destination
           (Values.DeleteConfigurationSetEventDestinationRequest.make
              ~configurationSetName ~eventDestinationName ())
           (Some
              Values.DeleteConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.DeleteConfigurationSetEventDestinationResponse.error_to_json)])
let delete_dedicated_ip_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolName =
         flag "pool-name" (required string) ~doc:"STRING PoolName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dedicated_ip_pool
           (Values.DeleteDedicatedIpPoolRequest.make ~poolName ())
           (Some Values.DeleteDedicatedIpPoolResponse.to_json)
           (Some Values.DeleteDedicatedIpPoolResponse.error_to_json)])
let delete_email_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_email_identity
           (Values.DeleteEmailIdentityRequest.make ~emailIdentity ())
           (Some Values.DeleteEmailIdentityResponse.to_json)
           (Some Values.DeleteEmailIdentityResponse.error_to_json)])
let get_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account (Values.GetAccountRequest.make ())
           (Some Values.GetAccountResponse.to_json)
           (Some Values.GetAccountResponse.error_to_json)])
let get_blacklist_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blacklistItemNames =
         flag "blacklist-item-names" (required json_arg)
           ~doc:"JSON BlacklistItemNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blacklist_reports
           (Values.GetBlacklistReportsRequest.make
              ~blacklistItemNames:(Values.BlacklistItemNames.of_json
                                     blacklistItemNames) ())
           (Some Values.GetBlacklistReportsResponse.to_json)
           (Some Values.GetBlacklistReportsResponse.error_to_json)])
let get_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration_set
           (Values.GetConfigurationSetRequest.make ~configurationSetName ())
           (Some Values.GetConfigurationSetResponse.to_json)
           (Some Values.GetConfigurationSetResponse.error_to_json)])
let get_configuration_set_event_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration_set_event_destinations
           (Values.GetConfigurationSetEventDestinationsRequest.make
              ~configurationSetName ())
           (Some Values.GetConfigurationSetEventDestinationsResponse.to_json)
           (Some
              Values.GetConfigurationSetEventDestinationsResponse.error_to_json)])
let get_dedicated_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ip = flag "ip" (required string) ~doc:"STRING Ip" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dedicated_ip (Values.GetDedicatedIpRequest.make ~ip ())
           (Some Values.GetDedicatedIpResponse.to_json)
           (Some Values.GetDedicatedIpResponse.error_to_json)])
let get_dedicated_ips =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolName =
         flag "pool-name" (optional string) ~doc:"STRING PoolName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dedicated_ips
           (Values.GetDedicatedIpsRequest.make ?poolName ?nextToken ?pageSize
              ()) (Some Values.GetDedicatedIpsResponse.to_json)
           (Some Values.GetDedicatedIpsResponse.error_to_json)])
let get_deliverability_dashboard_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deliverability_dashboard_options
           (Values.GetDeliverabilityDashboardOptionsRequest.make ())
           (Some Values.GetDeliverabilityDashboardOptionsResponse.to_json)
           (Some
              Values.GetDeliverabilityDashboardOptionsResponse.error_to_json)])
let get_deliverability_test_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deliverability_test_report
           (Values.GetDeliverabilityTestReportRequest.make ~reportId ())
           (Some Values.GetDeliverabilityTestReportResponse.to_json)
           (Some Values.GetDeliverabilityTestReportResponse.error_to_json)])
let get_domain_deliverability_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and campaignId =
         flag "campaign-id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_deliverability_campaign
           (Values.GetDomainDeliverabilityCampaignRequest.make ~campaignId ())
           (Some Values.GetDomainDeliverabilityCampaignResponse.to_json)
           (Some Values.GetDomainDeliverabilityCampaignResponse.error_to_json)])
let get_domain_statistics_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING Identity"
       and startDate =
         flag "start-date" (required json_arg) ~doc:"JSON Timestamp"
       and endDate =
         flag "end-date" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_statistics_report
           (Values.GetDomainStatisticsReportRequest.make ~domain
              ~startDate:(Values.Timestamp.of_json startDate)
              ~endDate:(Values.Timestamp.of_json endDate) ())
           (Some Values.GetDomainStatisticsReportResponse.to_json)
           (Some Values.GetDomainStatisticsReportResponse.error_to_json)])
let get_email_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_email_identity
           (Values.GetEmailIdentityRequest.make ~emailIdentity ())
           (Some Values.GetEmailIdentityResponse.to_json)
           (Some Values.GetEmailIdentityResponse.error_to_json)])
let list_configuration_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_sets
           (Values.ListConfigurationSetsRequest.make ?nextToken ?pageSize ())
           (Some Values.ListConfigurationSetsResponse.to_json)
           (Some Values.ListConfigurationSetsResponse.error_to_json)])
let list_dedicated_ip_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dedicated_ip_pools
           (Values.ListDedicatedIpPoolsRequest.make ?nextToken ?pageSize ())
           (Some Values.ListDedicatedIpPoolsResponse.to_json)
           (Some Values.ListDedicatedIpPoolsResponse.error_to_json)])
let list_deliverability_test_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deliverability_test_reports
           (Values.ListDeliverabilityTestReportsRequest.make ?nextToken
              ?pageSize ())
           (Some Values.ListDeliverabilityTestReportsResponse.to_json)
           (Some Values.ListDeliverabilityTestReportsResponse.error_to_json)])
let list_domain_deliverability_campaigns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems"
       and startDate =
         flag "start-date" (required json_arg) ~doc:"JSON Timestamp"
       and endDate =
         flag "end-date" (required json_arg) ~doc:"JSON Timestamp"
       and subscribedDomain =
         flag "subscribed-domain" (required string) ~doc:"STRING Domain" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_deliverability_campaigns
           (Values.ListDomainDeliverabilityCampaignsRequest.make ?nextToken
              ?pageSize ~startDate:(Values.Timestamp.of_json startDate)
              ~endDate:(Values.Timestamp.of_json endDate) ~subscribedDomain
              ())
           (Some Values.ListDomainDeliverabilityCampaignsResponse.to_json)
           (Some
              Values.ListDomainDeliverabilityCampaignsResponse.error_to_json)])
let list_email_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_email_identities
           (Values.ListEmailIdentitiesRequest.make ?nextToken ?pageSize ())
           (Some Values.ListEmailIdentitiesResponse.to_json)
           (Some Values.ListEmailIdentitiesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_account_dedicated_ip_warmup_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoWarmupEnabled =
         flag "auto-warmup-enabled" (optional bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_dedicated_ip_warmup_attributes
           (Values.PutAccountDedicatedIpWarmupAttributesRequest.make
              ?autoWarmupEnabled ())
           (Some Values.PutAccountDedicatedIpWarmupAttributesResponse.to_json)
           (Some
              Values.PutAccountDedicatedIpWarmupAttributesResponse.error_to_json)])
let put_account_sending_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sendingEnabled =
         flag "sending-enabled" (optional bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_sending_attributes
           (Values.PutAccountSendingAttributesRequest.make ?sendingEnabled ())
           (Some Values.PutAccountSendingAttributesResponse.to_json)
           (Some Values.PutAccountSendingAttributesResponse.error_to_json)])
let put_configuration_set_delivery_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tlsPolicy =
         flag "tls-policy" (optional json_arg) ~doc:"JSON TlsPolicy"
       and sendingPoolName =
         flag "sending-pool-name" (optional string)
           ~doc:"STRING SendingPoolName"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_delivery_options
           (Values.PutConfigurationSetDeliveryOptionsRequest.make
              ?tlsPolicy:(Option.map ~f:Values.TlsPolicy.of_json tlsPolicy)
              ?sendingPoolName ~configurationSetName ())
           (Some Values.PutConfigurationSetDeliveryOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetDeliveryOptionsResponse.error_to_json)])
let put_configuration_set_reputation_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reputationMetricsEnabled =
         flag "reputation-metrics-enabled" (optional bool)
           ~doc:"BOOL Enabled"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_reputation_options
           (Values.PutConfigurationSetReputationOptionsRequest.make
              ?reputationMetricsEnabled ~configurationSetName ())
           (Some Values.PutConfigurationSetReputationOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetReputationOptionsResponse.error_to_json)])
let put_configuration_set_sending_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sendingEnabled =
         flag "sending-enabled" (optional bool) ~doc:"BOOL Enabled"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_sending_options
           (Values.PutConfigurationSetSendingOptionsRequest.make
              ?sendingEnabled ~configurationSetName ())
           (Some Values.PutConfigurationSetSendingOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetSendingOptionsResponse.error_to_json)])
let put_configuration_set_tracking_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customRedirectDomain =
         flag "custom-redirect-domain" (optional string)
           ~doc:"STRING CustomRedirectDomain"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_tracking_options
           (Values.PutConfigurationSetTrackingOptionsRequest.make
              ?customRedirectDomain ~configurationSetName ())
           (Some Values.PutConfigurationSetTrackingOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetTrackingOptionsResponse.error_to_json)])
let put_dedicated_ip_in_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ip = flag "ip" (required string) ~doc:"STRING Ip"
       and destinationPoolName =
         flag "destination-pool-name" (required string)
           ~doc:"STRING PoolName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_dedicated_ip_in_pool
           (Values.PutDedicatedIpInPoolRequest.make ~ip ~destinationPoolName
              ()) (Some Values.PutDedicatedIpInPoolResponse.to_json)
           (Some Values.PutDedicatedIpInPoolResponse.error_to_json)])
let put_dedicated_ip_warmup_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ip = flag "ip" (required string) ~doc:"STRING Ip"
       and warmupPercentage =
         flag "warmup-percentage" (required int)
           ~doc:"INT Percentage100Wrapper" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_dedicated_ip_warmup_attributes
           (Values.PutDedicatedIpWarmupAttributesRequest.make ~ip
              ~warmupPercentage ())
           (Some Values.PutDedicatedIpWarmupAttributesResponse.to_json)
           (Some Values.PutDedicatedIpWarmupAttributesResponse.error_to_json)])
let put_deliverability_dashboard_option =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscribedDomains =
         flag "subscribed-domains" (optional json_arg)
           ~doc:"JSON DomainDeliverabilityTrackingOptions"
       and dashboardEnabled =
         flag "dashboard-enabled" (required bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_deliverability_dashboard_option
           (Values.PutDeliverabilityDashboardOptionRequest.make
              ?subscribedDomains:(Option.map
                                    ~f:Values.DomainDeliverabilityTrackingOptions.of_json
                                    subscribedDomains) ~dashboardEnabled ())
           (Some Values.PutDeliverabilityDashboardOptionResponse.to_json)
           (Some
              Values.PutDeliverabilityDashboardOptionResponse.error_to_json)])
let put_email_identity_dkim_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and signingEnabled =
         flag "signing-enabled" (optional bool) ~doc:"BOOL Enabled"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_identity_dkim_attributes
           (Values.PutEmailIdentityDkimAttributesRequest.make ?signingEnabled
              ~emailIdentity ())
           (Some Values.PutEmailIdentityDkimAttributesResponse.to_json)
           (Some Values.PutEmailIdentityDkimAttributesResponse.error_to_json)])
let put_email_identity_feedback_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailForwardingEnabled =
         flag "email-forwarding-enabled" (optional bool) ~doc:"BOOL Enabled"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_identity_feedback_attributes
           (Values.PutEmailIdentityFeedbackAttributesRequest.make
              ?emailForwardingEnabled ~emailIdentity ())
           (Some Values.PutEmailIdentityFeedbackAttributesResponse.to_json)
           (Some
              Values.PutEmailIdentityFeedbackAttributesResponse.error_to_json)])
let put_email_identity_mail_from_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mailFromDomain =
         flag "mail-from-domain" (optional string)
           ~doc:"STRING MailFromDomainName"
       and behaviorOnMxFailure =
         flag "behavior-on-mx-failure" (optional json_arg)
           ~doc:"JSON BehaviorOnMxFailure"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_identity_mail_from_attributes
           (Values.PutEmailIdentityMailFromAttributesRequest.make
              ?mailFromDomain
              ?behaviorOnMxFailure:(Option.map
                                      ~f:Values.BehaviorOnMxFailure.of_json
                                      behaviorOnMxFailure) ~emailIdentity ())
           (Some Values.PutEmailIdentityMailFromAttributesResponse.to_json)
           (Some
              Values.PutEmailIdentityMailFromAttributesResponse.error_to_json)])
let send_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fromEmailAddress =
         flag "from-email-address" (optional string)
           ~doc:"STRING EmailAddress"
       and replyToAddresses =
         flag "reply-to-addresses" (optional json_arg)
           ~doc:"JSON EmailAddressList"
       and feedbackForwardingEmailAddress =
         flag "feedback-forwarding-email-address" (optional string)
           ~doc:"STRING EmailAddress"
       and emailTags =
         flag "email-tags" (optional json_arg) ~doc:"JSON MessageTagList"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON Destination"
       and content =
         flag "content" (required json_arg) ~doc:"JSON EmailContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_email
           (Values.SendEmailRequest.make ?fromEmailAddress
              ?replyToAddresses:(Option.map
                                   ~f:Values.EmailAddressList.of_json
                                   replyToAddresses)
              ?feedbackForwardingEmailAddress
              ?emailTags:(Option.map ~f:Values.MessageTagList.of_json
                            emailTags) ?configurationSetName
              ~destination:(Values.Destination.of_json destination)
              ~content:(Values.EmailContent.of_json content) ())
           (Some Values.SendEmailResponse.to_json)
           (Some Values.SendEmailResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName"
       and eventDestination =
         flag "event-destination" (required json_arg)
           ~doc:"JSON EventDestinationDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_set_event_destination
           (Values.UpdateConfigurationSetEventDestinationRequest.make
              ~configurationSetName ~eventDestinationName
              ~eventDestination:(Values.EventDestinationDefinition.of_json
                                   eventDestination) ())
           (Some
              Values.UpdateConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.UpdateConfigurationSetEventDestinationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-configuration-set", create_configuration_set);
    ("create-configuration-set-event-destination",
      create_configuration_set_event_destination);
    ("create-dedicated-ip-pool", create_dedicated_ip_pool);
    ("create-deliverability-test-report", create_deliverability_test_report);
    ("create-email-identity", create_email_identity);
    ("delete-configuration-set", delete_configuration_set);
    ("delete-configuration-set-event-destination",
      delete_configuration_set_event_destination);
    ("delete-dedicated-ip-pool", delete_dedicated_ip_pool);
    ("delete-email-identity", delete_email_identity);
    ("get-account", get_account);
    ("get-blacklist-reports", get_blacklist_reports);
    ("get-configuration-set", get_configuration_set);
    ("get-configuration-set-event-destinations",
      get_configuration_set_event_destinations);
    ("get-dedicated-ip", get_dedicated_ip);
    ("get-dedicated-ips", get_dedicated_ips);
    ("get-deliverability-dashboard-options",
      get_deliverability_dashboard_options);
    ("get-deliverability-test-report", get_deliverability_test_report);
    ("get-domain-deliverability-campaign",
      get_domain_deliverability_campaign);
    ("get-domain-statistics-report", get_domain_statistics_report);
    ("get-email-identity", get_email_identity);
    ("list-configuration-sets", list_configuration_sets);
    ("list-dedicated-ip-pools", list_dedicated_ip_pools);
    ("list-deliverability-test-reports", list_deliverability_test_reports);
    ("list-domain-deliverability-campaigns",
      list_domain_deliverability_campaigns);
    ("list-email-identities", list_email_identities);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-account-dedicated-ip-warmup-attributes",
      put_account_dedicated_ip_warmup_attributes);
    ("put-account-sending-attributes", put_account_sending_attributes);
    ("put-configuration-set-delivery-options",
      put_configuration_set_delivery_options);
    ("put-configuration-set-reputation-options",
      put_configuration_set_reputation_options);
    ("put-configuration-set-sending-options",
      put_configuration_set_sending_options);
    ("put-configuration-set-tracking-options",
      put_configuration_set_tracking_options);
    ("put-dedicated-ip-in-pool", put_dedicated_ip_in_pool);
    ("put-dedicated-ip-warmup-attributes",
      put_dedicated_ip_warmup_attributes);
    ("put-deliverability-dashboard-option",
      put_deliverability_dashboard_option);
    ("put-email-identity-dkim-attributes",
      put_email_identity_dkim_attributes);
    ("put-email-identity-feedback-attributes",
      put_email_identity_feedback_attributes);
    ("put-email-identity-mail-from-attributes",
      put_email_identity_mail_from_attributes);
    ("send-email", send_email);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-configuration-set-event-destination",
      update_configuration_set_event_destination)]
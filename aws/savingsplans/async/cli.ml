(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_savings_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and upfrontPaymentAmount =
         flag "upfront-payment-amount" (optional string) ~doc:"STRING Amount"
       and purchaseTime =
         flag "purchase-time" (optional json_arg) ~doc:"JSON DateTime"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and savingsPlanOfferingId =
         flag "savings-plan-offering-id" (required string)
           ~doc:"STRING SavingsPlanOfferingId"
       and commitment =
         flag "commitment" (required string) ~doc:"STRING Amount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_savings_plan
           (Values.CreateSavingsPlanRequest.make ?upfrontPaymentAmount
              ?purchaseTime:(Option.map ~f:Values.DateTime.of_json
                               purchaseTime) ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~savingsPlanOfferingId ~commitment ())
           (Some Values.CreateSavingsPlanResponse.to_json)
           (Some Values.CreateSavingsPlanResponse.error_to_json)])
let delete_queued_savings_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and savingsPlanId =
         flag "savings-plan-id" (required string) ~doc:"STRING SavingsPlanId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queued_savings_plan
           (Values.DeleteQueuedSavingsPlanRequest.make ~savingsPlanId ())
           (Some Values.DeleteQueuedSavingsPlanResponse.to_json)
           (Some Values.DeleteQueuedSavingsPlanResponse.error_to_json)])
let describe_savings_plan_rates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON SavingsPlanRateFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and savingsPlanId =
         flag "savings-plan-id" (required string) ~doc:"STRING SavingsPlanId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_savings_plan_rates
           (Values.DescribeSavingsPlanRatesRequest.make
              ?filters:(Option.map
                          ~f:Values.SavingsPlanRateFilterList.of_json filters)
              ?nextToken ?maxResults ~savingsPlanId ())
           (Some Values.DescribeSavingsPlanRatesResponse.to_json)
           (Some Values.DescribeSavingsPlanRatesResponse.error_to_json)])
let describe_savings_plans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and savingsPlanArns =
         flag "savings-plan-arns" (optional json_arg)
           ~doc:"JSON SavingsPlanArnList"
       and savingsPlanIds =
         flag "savings-plan-ids" (optional json_arg)
           ~doc:"JSON SavingsPlanIdList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON SavingsPlanStateList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SavingsPlanFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_savings_plans
           (Values.DescribeSavingsPlansRequest.make
              ?savingsPlanArns:(Option.map
                                  ~f:Values.SavingsPlanArnList.of_json
                                  savingsPlanArns)
              ?savingsPlanIds:(Option.map ~f:Values.SavingsPlanIdList.of_json
                                 savingsPlanIds) ?nextToken ?maxResults
              ?states:(Option.map ~f:Values.SavingsPlanStateList.of_json
                         states)
              ?filters:(Option.map ~f:Values.SavingsPlanFilterList.of_json
                          filters) ())
           (Some Values.DescribeSavingsPlansResponse.to_json)
           (Some Values.DescribeSavingsPlansResponse.error_to_json)])
let describe_savings_plans_offering_rates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and savingsPlanOfferingIds =
         flag "savings-plan-offering-ids" (optional json_arg)
           ~doc:"JSON UUIDs"
       and savingsPlanPaymentOptions =
         flag "savings-plan-payment-options" (optional json_arg)
           ~doc:"JSON SavingsPlanPaymentOptionList"
       and savingsPlanTypes =
         flag "savings-plan-types" (optional json_arg)
           ~doc:"JSON SavingsPlanTypeList"
       and products =
         flag "products" (optional json_arg)
           ~doc:"JSON SavingsPlanProductTypeList"
       and serviceCodes =
         flag "service-codes" (optional json_arg)
           ~doc:"JSON SavingsPlanRateServiceCodeList"
       and usageTypes =
         flag "usage-types" (optional json_arg)
           ~doc:"JSON SavingsPlanRateUsageTypeList"
       and operations =
         flag "operations" (optional json_arg)
           ~doc:"JSON SavingsPlanRateOperationList"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON SavingsPlanOfferingRateFiltersList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_savings_plans_offering_rates
           (Values.DescribeSavingsPlansOfferingRatesRequest.make
              ?savingsPlanOfferingIds:(Option.map ~f:Values.UUIDs.of_json
                                         savingsPlanOfferingIds)
              ?savingsPlanPaymentOptions:(Option.map
                                            ~f:Values.SavingsPlanPaymentOptionList.of_json
                                            savingsPlanPaymentOptions)
              ?savingsPlanTypes:(Option.map
                                   ~f:Values.SavingsPlanTypeList.of_json
                                   savingsPlanTypes)
              ?products:(Option.map
                           ~f:Values.SavingsPlanProductTypeList.of_json
                           products)
              ?serviceCodes:(Option.map
                               ~f:Values.SavingsPlanRateServiceCodeList.of_json
                               serviceCodes)
              ?usageTypes:(Option.map
                             ~f:Values.SavingsPlanRateUsageTypeList.of_json
                             usageTypes)
              ?operations:(Option.map
                             ~f:Values.SavingsPlanRateOperationList.of_json
                             operations)
              ?filters:(Option.map
                          ~f:Values.SavingsPlanOfferingRateFiltersList.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.DescribeSavingsPlansOfferingRatesResponse.to_json)
           (Some
              Values.DescribeSavingsPlansOfferingRatesResponse.error_to_json)])
let describe_savings_plans_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offeringIds =
         flag "offering-ids" (optional json_arg) ~doc:"JSON UUIDs"
       and paymentOptions =
         flag "payment-options" (optional json_arg)
           ~doc:"JSON SavingsPlanPaymentOptionList"
       and productType =
         flag "product-type" (optional json_arg)
           ~doc:"JSON SavingsPlanProductType"
       and planTypes =
         flag "plan-types" (optional json_arg)
           ~doc:"JSON SavingsPlanTypeList"
       and durations =
         flag "durations" (optional json_arg) ~doc:"JSON DurationsList"
       and currencies =
         flag "currencies" (optional json_arg) ~doc:"JSON CurrencyList"
       and descriptions =
         flag "descriptions" (optional json_arg)
           ~doc:"JSON SavingsPlanDescriptionsList"
       and serviceCodes =
         flag "service-codes" (optional json_arg)
           ~doc:"JSON SavingsPlanServiceCodeList"
       and usageTypes =
         flag "usage-types" (optional json_arg)
           ~doc:"JSON SavingsPlanUsageTypeList"
       and operations =
         flag "operations" (optional json_arg)
           ~doc:"JSON SavingsPlanOperationList"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON SavingsPlanOfferingFiltersList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_savings_plans_offerings
           (Values.DescribeSavingsPlansOfferingsRequest.make
              ?offeringIds:(Option.map ~f:Values.UUIDs.of_json offeringIds)
              ?paymentOptions:(Option.map
                                 ~f:Values.SavingsPlanPaymentOptionList.of_json
                                 paymentOptions)
              ?productType:(Option.map
                              ~f:Values.SavingsPlanProductType.of_json
                              productType)
              ?planTypes:(Option.map ~f:Values.SavingsPlanTypeList.of_json
                            planTypes)
              ?durations:(Option.map ~f:Values.DurationsList.of_json
                            durations)
              ?currencies:(Option.map ~f:Values.CurrencyList.of_json
                             currencies)
              ?descriptions:(Option.map
                               ~f:Values.SavingsPlanDescriptionsList.of_json
                               descriptions)
              ?serviceCodes:(Option.map
                               ~f:Values.SavingsPlanServiceCodeList.of_json
                               serviceCodes)
              ?usageTypes:(Option.map
                             ~f:Values.SavingsPlanUsageTypeList.of_json
                             usageTypes)
              ?operations:(Option.map
                             ~f:Values.SavingsPlanOperationList.of_json
                             operations)
              ?filters:(Option.map
                          ~f:Values.SavingsPlanOfferingFiltersList.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.DescribeSavingsPlansOfferingsResponse.to_json)
           (Some Values.DescribeSavingsPlansOfferingsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING SavingsPlanArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING SavingsPlanArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING SavingsPlanArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-savings-plan", create_savings_plan);
    ("delete-queued-savings-plan", delete_queued_savings_plan);
    ("describe-savings-plan-rates", describe_savings_plan_rates);
    ("describe-savings-plans", describe_savings_plans);
    ("describe-savings-plans-offering-rates",
      describe_savings_plans_offering_rates);
    ("describe-savings-plans-offerings", describe_savings_plans_offerings);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.savingsplans
let apiVersion = "2019-06-28"
let endpointPrefix = "savingsplans"
let serviceFullName = "AWS Savings Plans"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "AWSSavingsPlans"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module JsonSafeFilterValueString =
  struct
    type nonrec t = string
    let context_ = "JsonSafeFilterValueString"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z0-9_ \\/.\\:\\-\\(\\)]+$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"JsonSafeFilterValueString" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanOfferingPropertyKey =
  struct
    type nonrec t =
      | Region 
      | InstanceFamily 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Region -> "region"
      | InstanceFamily -> "instanceFamily"
      | Non_static_id s -> s
    let of_string =
      function
      | "region" -> Region
      | "instanceFamily" -> InstanceFamily
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanOfferingPropertyKey"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlanOfferingPropertyKey" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanRatePropertyKey =
  struct
    type nonrec t =
      | Region 
      | InstanceType 
      | InstanceFamily 
      | ProductDescription 
      | Tenancy 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Region -> "region"
      | InstanceType -> "instanceType"
      | InstanceFamily -> "instanceFamily"
      | ProductDescription -> "productDescription"
      | Tenancy -> "tenancy"
      | Non_static_id s -> s
    let of_string =
      function
      | "region" -> Region
      | "instanceType" -> InstanceType
      | "instanceFamily" -> InstanceFamily
      | "productDescription" -> ProductDescription
      | "tenancy" -> Tenancy
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanRatePropertyKey"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlanRatePropertyKey" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanProductType =
  struct
    type nonrec t =
      | EC2 
      | Fargate 
      | Lambda 
      | SageMaker 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EC2 -> "EC2"
      | Fargate -> "Fargate"
      | Lambda -> "Lambda"
      | SageMaker -> "SageMaker"
      | Non_static_id s -> s
    let of_string =
      function
      | "EC2" -> EC2
      | "Fargate" -> Fargate
      | "Lambda" -> Lambda
      | "SageMaker" -> SageMaker
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanProductType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlanProductType" j)
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanOfferingProperty =
  struct
    type nonrec t =
      {
      name: SavingsPlanOfferingPropertyKey.t option
        [@ocaml.doc "The property name."];
      value: JsonSafeFilterValueString.t option
        [@ocaml.doc "The property value."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name",
           (Option.map x.name ~f:SavingsPlanOfferingPropertyKey.to_value));
        ("value", (Option.map x.value ~f:JsonSafeFilterValueString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:JsonSafeFilterValueString.of_xml)
          (Xml.child xml_arg0 "value") in
      let name =
        (Option.map ~f:SavingsPlanOfferingPropertyKey.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?value ?name ()
    let of_json json =
      let value = field_map json "value" JsonSafeFilterValueString.of_json in
      let name = field_map json "name" SavingsPlanOfferingPropertyKey.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a property."]
module CurrencyCode =
  struct
    type nonrec t =
      | CNY 
      | USD 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | CNY -> "CNY" | USD -> "USD" | Non_static_id s -> s
    let of_string =
      function | "CNY" -> CNY | "USD" -> USD | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CurrencyCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CurrencyCode" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanDescription =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanDescription"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^[a-zA-Z0-9_\\- ]+$"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanDescription" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanPaymentOption =
  struct
    type nonrec t =
      | All_Upfront 
      | Partial_Upfront 
      | No_Upfront 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | All_Upfront -> "All Upfront"
      | Partial_Upfront -> "Partial Upfront"
      | No_Upfront -> "No Upfront"
      | Non_static_id s -> s
    let of_string =
      function
      | "All Upfront" -> All_Upfront
      | "Partial Upfront" -> Partial_Upfront
      | "No Upfront" -> No_Upfront
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanPaymentOption" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlanPaymentOption" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanType =
  struct
    type nonrec t =
      | Compute 
      | EC2Instance 
      | SageMaker 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Compute -> "Compute"
      | EC2Instance -> "EC2Instance"
      | SageMaker -> "SageMaker"
      | Non_static_id s -> s
    let of_string =
      function
      | "Compute" -> Compute
      | "EC2Instance" -> EC2Instance
      | "SageMaker" -> SageMaker
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SavingsPlanType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SavingsPlanType" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlansDuration =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:0L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module UUID =
  struct
    type nonrec t = string
    let context_ = "UUID"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-f0-9]+(-[a-f0-9]+)*"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UUID" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanOfferingRateProperty =
  struct
    type nonrec t =
      {
      name: JsonSafeFilterValueString.t option
        [@ocaml.doc "The property name."];
      value: JsonSafeFilterValueString.t option
        [@ocaml.doc "The property value."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:JsonSafeFilterValueString.to_value));
        ("value", (Option.map x.value ~f:JsonSafeFilterValueString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:JsonSafeFilterValueString.of_xml)
          (Xml.child xml_arg0 "value") in
      let name =
        (Option.map ~f:JsonSafeFilterValueString.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?value ?name ()
    let of_json json =
      let value = field_map json "value" JsonSafeFilterValueString.of_json in
      let name = field_map json "name" JsonSafeFilterValueString.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a property."]
module SavingsPlanRateProperty =
  struct
    type nonrec t =
      {
      name: SavingsPlanRatePropertyKey.t option
        [@ocaml.doc "The property name."];
      value: JsonSafeFilterValueString.t option
        [@ocaml.doc "The property value."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:SavingsPlanRatePropertyKey.to_value));
        ("value", (Option.map x.value ~f:JsonSafeFilterValueString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:JsonSafeFilterValueString.of_xml)
          (Xml.child xml_arg0 "value") in
      let name =
        (Option.map ~f:SavingsPlanRatePropertyKey.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?value ?name ()
    let of_json json =
      let value = field_map json "value" JsonSafeFilterValueString.of_json in
      let name = field_map json "name" SavingsPlanRatePropertyKey.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a property."]
module Amount =
  struct
    type nonrec t = string
    let context_ = "Amount"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Amount" j
    let to_json = simple_to_json to_value
  end
module EC2InstanceFamily =
  struct
    type nonrec t = string
    let context_ = "EC2InstanceFamily"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EC2InstanceFamily" j
    let to_json = simple_to_json to_value
  end
module Region =
  struct
    type nonrec t = string
    let context_ = "Region"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Region" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanArn =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"arn:aws:[a-z]+:([a-z]{2}-[a-z]+-\\d{1}|):(\\d{12}):savingsplan\\/([0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12})$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanArn" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanId =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanId" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanOfferingId =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanOfferingId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanOfferingId" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanProductTypeList =
  struct
    type nonrec t = SavingsPlanProductType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanProductType.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanProductType.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanProductTypeList"
        ~of_json:SavingsPlanProductType.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanState =
  struct
    type nonrec t =
      | Payment_pending 
      | Payment_failed 
      | Active 
      | Retired 
      | Queued 
      | Queued_deleted 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Payment_pending -> "payment-pending"
      | Payment_failed -> "payment-failed"
      | Active -> "active"
      | Retired -> "retired"
      | Queued -> "queued"
      | Queued_deleted -> "queued-deleted"
      | Non_static_id s -> s
    let of_string =
      function
      | "payment-pending" -> Payment_pending
      | "payment-failed" -> Payment_failed
      | "active" -> Active
      | "retired" -> Retired
      | "queued" -> Queued
      | "queued-deleted" -> Queued_deleted
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SavingsPlanState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SavingsPlanState" j)
    let to_json = simple_to_json to_value
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module TermDurationInSeconds =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ListOfStrings =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"ListOfStrings" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlansFilterName =
  struct
    type nonrec t =
      | Region 
      | Ec2_instance_family 
      | Commitment 
      | Upfront 
      | Term 
      | Savings_plan_type 
      | Payment_option 
      | Start 
      | End 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Region -> "region"
      | Ec2_instance_family -> "ec2-instance-family"
      | Commitment -> "commitment"
      | Upfront -> "upfront"
      | Term -> "term"
      | Savings_plan_type -> "savings-plan-type"
      | Payment_option -> "payment-option"
      | Start -> "start"
      | End -> "end"
      | Non_static_id s -> s
    let of_string =
      function
      | "region" -> Region
      | "ec2-instance-family" -> Ec2_instance_family
      | "commitment" -> Commitment
      | "upfront" -> Upfront
      | "term" -> Term
      | "savings-plan-type" -> Savings_plan_type
      | "payment-option" -> Payment_option
      | "start" -> Start
      | "end" -> End
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlansFilterName" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlansFilterName" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanOfferingPropertyList =
  struct
    type nonrec t = SavingsPlanOfferingProperty.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanOfferingProperty.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanOfferingProperty.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanOfferingPropertyList"
        ~of_json:SavingsPlanOfferingProperty.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanOperation =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanOperation"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_ \\/.:-]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanOperation" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanServiceCode =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanServiceCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:"^[a-zA-Z]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanServiceCode" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanUsageType =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanUsageType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_ \\/.:-]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanUsageType" j
    let to_json = simple_to_json to_value
  end
module FilterValuesList =
  struct
    type nonrec t = JsonSafeFilterValueString.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:JsonSafeFilterValueString.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:JsonSafeFilterValueString.of_xml)
    let of_json j =
      list_of_json ~kind:"FilterValuesList"
        ~of_json:JsonSafeFilterValueString.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanOfferingFilterAttribute =
  struct
    type nonrec t =
      | Region 
      | InstanceFamily 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Region -> "region"
      | InstanceFamily -> "instanceFamily"
      | Non_static_id s -> s
    let of_string =
      function
      | "region" -> Region
      | "instanceFamily" -> InstanceFamily
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanOfferingFilterAttribute"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlanOfferingFilterAttribute" j)
    let to_json = simple_to_json to_value
  end
module ParentSavingsPlanOffering =
  struct
    type nonrec t =
      {
      offeringId: UUID.t option [@ocaml.doc "The ID of the offering."];
      paymentOption: SavingsPlanPaymentOption.t option
        [@ocaml.doc "The payment option."];
      planType: SavingsPlanType.t option [@ocaml.doc "The plan type."];
      durationSeconds: SavingsPlansDuration.t option
        [@ocaml.doc "The duration, in seconds."];
      currency: CurrencyCode.t option [@ocaml.doc "The currency."];
      planDescription: SavingsPlanDescription.t option
        [@ocaml.doc "The description."]}
    let make ?offeringId =
      fun ?paymentOption ->
        fun ?planType ->
          fun ?durationSeconds ->
            fun ?currency ->
              fun ?planDescription ->
                fun () ->
                  {
                    offeringId;
                    paymentOption;
                    planType;
                    durationSeconds;
                    currency;
                    planDescription
                  }
    let to_value x =
      structure_to_value
        [("offeringId", (Option.map x.offeringId ~f:UUID.to_value));
        ("paymentOption",
          (Option.map x.paymentOption ~f:SavingsPlanPaymentOption.to_value));
        ("planType", (Option.map x.planType ~f:SavingsPlanType.to_value));
        ("durationSeconds",
          (Option.map x.durationSeconds ~f:SavingsPlansDuration.to_value));
        ("currency", (Option.map x.currency ~f:CurrencyCode.to_value));
        ("planDescription",
          (Option.map x.planDescription ~f:SavingsPlanDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let planDescription =
        (Option.map ~f:SavingsPlanDescription.of_xml)
          (Xml.child xml_arg0 "planDescription") in
      let currency =
        (Option.map ~f:CurrencyCode.of_xml) (Xml.child xml_arg0 "currency") in
      let durationSeconds =
        (Option.map ~f:SavingsPlansDuration.of_xml)
          (Xml.child xml_arg0 "durationSeconds") in
      let planType =
        (Option.map ~f:SavingsPlanType.of_xml)
          (Xml.child xml_arg0 "planType") in
      let paymentOption =
        (Option.map ~f:SavingsPlanPaymentOption.of_xml)
          (Xml.child xml_arg0 "paymentOption") in
      let offeringId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "offeringId") in
      make ?planDescription ?currency ?durationSeconds ?planType
        ?paymentOption ?offeringId ()
    let of_json json =
      let planDescription =
        field_map json "planDescription" SavingsPlanDescription.of_json in
      let currency = field_map json "currency" CurrencyCode.of_json in
      let durationSeconds =
        field_map json "durationSeconds" SavingsPlansDuration.of_json in
      let planType = field_map json "planType" SavingsPlanType.of_json in
      let paymentOption =
        field_map json "paymentOption" SavingsPlanPaymentOption.of_json in
      let offeringId = field_map json "offeringId" UUID.of_json in
      make ?planDescription ?currency ?durationSeconds ?planType
        ?paymentOption ?offeringId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a Savings Plan offering."]
module SavingsPlanOfferingRatePropertyList =
  struct
    type nonrec t = SavingsPlanOfferingRateProperty.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanOfferingRateProperty.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanOfferingRateProperty.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanOfferingRatePropertyList"
        ~of_json:SavingsPlanOfferingRateProperty.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanRateOperation =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanRateOperation"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_ \\/.:-]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanRateOperation" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanRatePricePerUnit =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanRatePricePerUnit"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanRatePricePerUnit" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanRateServiceCode =
  struct
    type nonrec t =
      | AmazonEC2 
      | AmazonECS 
      | AmazonEKS 
      | AWSLambda 
      | AmazonSageMaker 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AmazonEC2 -> "AmazonEC2"
      | AmazonECS -> "AmazonECS"
      | AmazonEKS -> "AmazonEKS"
      | AWSLambda -> "AWSLambda"
      | AmazonSageMaker -> "AmazonSageMaker"
      | Non_static_id s -> s
    let of_string =
      function
      | "AmazonEC2" -> AmazonEC2
      | "AmazonECS" -> AmazonECS
      | "AmazonEKS" -> AmazonEKS
      | "AWSLambda" -> AWSLambda
      | "AmazonSageMaker" -> AmazonSageMaker
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanRateServiceCode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlanRateServiceCode" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanRateUnit =
  struct
    type nonrec t =
      | Hrs 
      | Lambda_GB_Second 
      | Request 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Hrs -> "Hrs"
      | Lambda_GB_Second -> "Lambda-GB-Second"
      | Request -> "Request"
      | Non_static_id s -> s
    let of_string =
      function
      | "Hrs" -> Hrs
      | "Lambda-GB-Second" -> Lambda_GB_Second
      | "Request" -> Request
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanRateUnit" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SavingsPlanRateUnit" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanRateUsageType =
  struct
    type nonrec t = string
    let context_ = "SavingsPlanRateUsageType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9_ \\/.:-]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SavingsPlanRateUsageType" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanRateFilterAttribute =
  struct
    type nonrec t =
      | Region 
      | InstanceFamily 
      | InstanceType 
      | ProductDescription 
      | Tenancy 
      | ProductId 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Region -> "region"
      | InstanceFamily -> "instanceFamily"
      | InstanceType -> "instanceType"
      | ProductDescription -> "productDescription"
      | Tenancy -> "tenancy"
      | ProductId -> "productId"
      | Non_static_id s -> s
    let of_string =
      function
      | "region" -> Region
      | "instanceFamily" -> InstanceFamily
      | "instanceType" -> InstanceType
      | "productDescription" -> ProductDescription
      | "tenancy" -> Tenancy
      | "productId" -> ProductId
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanRateFilterAttribute"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlanRateFilterAttribute" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanRatePropertyList =
  struct
    type nonrec t = SavingsPlanRateProperty.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanRateProperty.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanRateProperty.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanRatePropertyList"
        ~of_json:SavingsPlanRateProperty.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanRateFilterName =
  struct
    type nonrec t =
      | Region 
      | InstanceType 
      | ProductDescription 
      | Tenancy 
      | ProductType 
      | ServiceCode 
      | UsageType 
      | Operation 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Region -> "region"
      | InstanceType -> "instanceType"
      | ProductDescription -> "productDescription"
      | Tenancy -> "tenancy"
      | ProductType -> "productType"
      | ServiceCode -> "serviceCode"
      | UsageType -> "usageType"
      | Operation -> "operation"
      | Non_static_id s -> s
    let of_string =
      function
      | "region" -> Region
      | "instanceType" -> InstanceType
      | "productDescription" -> ProductDescription
      | "tenancy" -> Tenancy
      | "productType" -> ProductType
      | "serviceCode" -> ServiceCode
      | "usageType" -> UsageType
      | "operation" -> Operation
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsPlanRateFilterName" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsPlanRateFilterName" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlan =
  struct
    type nonrec t =
      {
      offeringId: SavingsPlanOfferingId.t option
        [@ocaml.doc "The ID of the offering."];
      savingsPlanId: SavingsPlanId.t option
        [@ocaml.doc "The ID of the Savings Plan."];
      savingsPlanArn: SavingsPlanArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the Savings Plan."];
      description: String_.t option [@ocaml.doc "The description."];
      start: String_.t option [@ocaml.doc "The start time."];
      end_: String_.t option [@ocaml.doc "The end time."];
      state: SavingsPlanState.t option [@ocaml.doc "The state."];
      region: Region.t option [@ocaml.doc "The AWS Region."];
      ec2InstanceFamily: EC2InstanceFamily.t option
        [@ocaml.doc "The EC2 instance family."];
      savingsPlanType: SavingsPlanType.t option [@ocaml.doc "The plan type."];
      paymentOption: SavingsPlanPaymentOption.t option
        [@ocaml.doc "The payment option."];
      productTypes: SavingsPlanProductTypeList.t option
        [@ocaml.doc "The product types."];
      currency: CurrencyCode.t option [@ocaml.doc "The currency."];
      commitment: Amount.t option
        [@ocaml.doc "The hourly commitment, in USD."];
      upfrontPaymentAmount: Amount.t option
        [@ocaml.doc "The up-front payment amount."];
      recurringPaymentAmount: Amount.t option
        [@ocaml.doc "The recurring payment amount."];
      termDurationInSeconds: TermDurationInSeconds.t option
        [@ocaml.doc "The duration of the term, in seconds."];
      tags: TagMap.t option [@ocaml.doc "One or more tags."]}
    let make ?offeringId =
      fun ?savingsPlanId ->
        fun ?savingsPlanArn ->
          fun ?description ->
            fun ?start ->
              fun ?end_ ->
                fun ?state ->
                  fun ?region ->
                    fun ?ec2InstanceFamily ->
                      fun ?savingsPlanType ->
                        fun ?paymentOption ->
                          fun ?productTypes ->
                            fun ?currency ->
                              fun ?commitment ->
                                fun ?upfrontPaymentAmount ->
                                  fun ?recurringPaymentAmount ->
                                    fun ?termDurationInSeconds ->
                                      fun ?tags ->
                                        fun () ->
                                          {
                                            offeringId;
                                            savingsPlanId;
                                            savingsPlanArn;
                                            description;
                                            start;
                                            end_;
                                            state;
                                            region;
                                            ec2InstanceFamily;
                                            savingsPlanType;
                                            paymentOption;
                                            productTypes;
                                            currency;
                                            commitment;
                                            upfrontPaymentAmount;
                                            recurringPaymentAmount;
                                            termDurationInSeconds;
                                            tags
                                          }
    let to_value x =
      structure_to_value
        [("offeringId",
           (Option.map x.offeringId ~f:SavingsPlanOfferingId.to_value));
        ("savingsPlanId",
          (Option.map x.savingsPlanId ~f:SavingsPlanId.to_value));
        ("savingsPlanArn",
          (Option.map x.savingsPlanArn ~f:SavingsPlanArn.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("start", (Option.map x.start ~f:String_.to_value));
        ("end", (Option.map x.end_ ~f:String_.to_value));
        ("state", (Option.map x.state ~f:SavingsPlanState.to_value));
        ("region", (Option.map x.region ~f:Region.to_value));
        ("ec2InstanceFamily",
          (Option.map x.ec2InstanceFamily ~f:EC2InstanceFamily.to_value));
        ("savingsPlanType",
          (Option.map x.savingsPlanType ~f:SavingsPlanType.to_value));
        ("paymentOption",
          (Option.map x.paymentOption ~f:SavingsPlanPaymentOption.to_value));
        ("productTypes",
          (Option.map x.productTypes ~f:SavingsPlanProductTypeList.to_value));
        ("currency", (Option.map x.currency ~f:CurrencyCode.to_value));
        ("commitment", (Option.map x.commitment ~f:Amount.to_value));
        ("upfrontPaymentAmount",
          (Option.map x.upfrontPaymentAmount ~f:Amount.to_value));
        ("recurringPaymentAmount",
          (Option.map x.recurringPaymentAmount ~f:Amount.to_value));
        ("termDurationInSeconds",
          (Option.map x.termDurationInSeconds
             ~f:TermDurationInSeconds.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let termDurationInSeconds =
        (Option.map ~f:TermDurationInSeconds.of_xml)
          (Xml.child xml_arg0 "termDurationInSeconds") in
      let recurringPaymentAmount =
        (Option.map ~f:Amount.of_xml)
          (Xml.child xml_arg0 "recurringPaymentAmount") in
      let upfrontPaymentAmount =
        (Option.map ~f:Amount.of_xml)
          (Xml.child xml_arg0 "upfrontPaymentAmount") in
      let commitment =
        (Option.map ~f:Amount.of_xml) (Xml.child xml_arg0 "commitment") in
      let currency =
        (Option.map ~f:CurrencyCode.of_xml) (Xml.child xml_arg0 "currency") in
      let productTypes =
        (Option.map ~f:SavingsPlanProductTypeList.of_xml)
          (Xml.child xml_arg0 "productTypes") in
      let paymentOption =
        (Option.map ~f:SavingsPlanPaymentOption.of_xml)
          (Xml.child xml_arg0 "paymentOption") in
      let savingsPlanType =
        (Option.map ~f:SavingsPlanType.of_xml)
          (Xml.child xml_arg0 "savingsPlanType") in
      let ec2InstanceFamily =
        (Option.map ~f:EC2InstanceFamily.of_xml)
          (Xml.child xml_arg0 "ec2InstanceFamily") in
      let region =
        (Option.map ~f:Region.of_xml) (Xml.child xml_arg0 "region") in
      let state =
        (Option.map ~f:SavingsPlanState.of_xml) (Xml.child xml_arg0 "state") in
      let end_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "end") in
      let start = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "start") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let savingsPlanArn =
        (Option.map ~f:SavingsPlanArn.of_xml)
          (Xml.child xml_arg0 "savingsPlanArn") in
      let savingsPlanId =
        (Option.map ~f:SavingsPlanId.of_xml)
          (Xml.child xml_arg0 "savingsPlanId") in
      let offeringId =
        (Option.map ~f:SavingsPlanOfferingId.of_xml)
          (Xml.child xml_arg0 "offeringId") in
      make ?tags ?termDurationInSeconds ?recurringPaymentAmount
        ?upfrontPaymentAmount ?commitment ?currency ?productTypes
        ?paymentOption ?savingsPlanType ?ec2InstanceFamily ?region ?state
        ?end_ ?start ?description ?savingsPlanArn ?savingsPlanId ?offeringId
        ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let termDurationInSeconds =
        field_map json "termDurationInSeconds" TermDurationInSeconds.of_json in
      let recurringPaymentAmount =
        field_map json "recurringPaymentAmount" Amount.of_json in
      let upfrontPaymentAmount =
        field_map json "upfrontPaymentAmount" Amount.of_json in
      let commitment = field_map json "commitment" Amount.of_json in
      let currency = field_map json "currency" CurrencyCode.of_json in
      let productTypes =
        field_map json "productTypes" SavingsPlanProductTypeList.of_json in
      let paymentOption =
        field_map json "paymentOption" SavingsPlanPaymentOption.of_json in
      let savingsPlanType =
        field_map json "savingsPlanType" SavingsPlanType.of_json in
      let ec2InstanceFamily =
        field_map json "ec2InstanceFamily" EC2InstanceFamily.of_json in
      let region = field_map json "region" Region.of_json in
      let state = field_map json "state" SavingsPlanState.of_json in
      let end_ = field_map json "end" String_.of_json in
      let start = field_map json "start" String_.of_json in
      let description = field_map json "description" String_.of_json in
      let savingsPlanArn =
        field_map json "savingsPlanArn" SavingsPlanArn.of_json in
      let savingsPlanId =
        field_map json "savingsPlanId" SavingsPlanId.of_json in
      let offeringId =
        field_map json "offeringId" SavingsPlanOfferingId.of_json in
      make ?tags ?termDurationInSeconds ?recurringPaymentAmount
        ?upfrontPaymentAmount ?commitment ?currency ?productTypes
        ?paymentOption ?savingsPlanType ?ec2InstanceFamily ?region ?state
        ?end_ ?start ?description ?savingsPlanArn ?savingsPlanId ?offeringId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a Savings Plan."]
module SavingsPlanFilter =
  struct
    type nonrec t =
      {
      name: SavingsPlansFilterName.t option [@ocaml.doc "The filter name."];
      values: ListOfStrings.t option [@ocaml.doc "The filter value."]}
    let make ?name = fun ?values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:SavingsPlansFilterName.to_value));
        ("values", (Option.map x.values ~f:ListOfStrings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:ListOfStrings.of_xml) (Xml.child xml_arg0 "values") in
      let name =
        (Option.map ~f:SavingsPlansFilterName.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?values ?name ()
    let of_json json =
      let values = field_map json "values" ListOfStrings.of_json in
      let name = field_map json "name" SavingsPlansFilterName.of_json in
      make ?values ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a filter."]
module SavingsPlanOffering =
  struct
    type nonrec t =
      {
      offeringId: UUID.t option [@ocaml.doc "The ID of the offering."];
      productTypes: SavingsPlanProductTypeList.t option
        [@ocaml.doc "The product type."];
      planType: SavingsPlanType.t option [@ocaml.doc "The plan type."];
      description: SavingsPlanDescription.t option
        [@ocaml.doc "The description."];
      paymentOption: SavingsPlanPaymentOption.t option
        [@ocaml.doc "The payment option."];
      durationSeconds: SavingsPlansDuration.t option
        [@ocaml.doc "The duration, in seconds."];
      currency: CurrencyCode.t option [@ocaml.doc "The currency."];
      serviceCode: SavingsPlanServiceCode.t option
        [@ocaml.doc "The service."];
      usageType: SavingsPlanUsageType.t option
        [@ocaml.doc
          "The usage details of the line item in the billing report."];
      operation: SavingsPlanOperation.t option
        [@ocaml.doc
          "The specific AWS operation for the line item in the billing report."];
      properties: SavingsPlanOfferingPropertyList.t option
        [@ocaml.doc "The properties."]}
    let make ?offeringId =
      fun ?productTypes ->
        fun ?planType ->
          fun ?description ->
            fun ?paymentOption ->
              fun ?durationSeconds ->
                fun ?currency ->
                  fun ?serviceCode ->
                    fun ?usageType ->
                      fun ?operation ->
                        fun ?properties ->
                          fun () ->
                            {
                              offeringId;
                              productTypes;
                              planType;
                              description;
                              paymentOption;
                              durationSeconds;
                              currency;
                              serviceCode;
                              usageType;
                              operation;
                              properties
                            }
    let to_value x =
      structure_to_value
        [("offeringId", (Option.map x.offeringId ~f:UUID.to_value));
        ("productTypes",
          (Option.map x.productTypes ~f:SavingsPlanProductTypeList.to_value));
        ("planType", (Option.map x.planType ~f:SavingsPlanType.to_value));
        ("description",
          (Option.map x.description ~f:SavingsPlanDescription.to_value));
        ("paymentOption",
          (Option.map x.paymentOption ~f:SavingsPlanPaymentOption.to_value));
        ("durationSeconds",
          (Option.map x.durationSeconds ~f:SavingsPlansDuration.to_value));
        ("currency", (Option.map x.currency ~f:CurrencyCode.to_value));
        ("serviceCode",
          (Option.map x.serviceCode ~f:SavingsPlanServiceCode.to_value));
        ("usageType",
          (Option.map x.usageType ~f:SavingsPlanUsageType.to_value));
        ("operation",
          (Option.map x.operation ~f:SavingsPlanOperation.to_value));
        ("properties",
          (Option.map x.properties
             ~f:SavingsPlanOfferingPropertyList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let properties =
        (Option.map ~f:SavingsPlanOfferingPropertyList.of_xml)
          (Xml.child xml_arg0 "properties") in
      let operation =
        (Option.map ~f:SavingsPlanOperation.of_xml)
          (Xml.child xml_arg0 "operation") in
      let usageType =
        (Option.map ~f:SavingsPlanUsageType.of_xml)
          (Xml.child xml_arg0 "usageType") in
      let serviceCode =
        (Option.map ~f:SavingsPlanServiceCode.of_xml)
          (Xml.child xml_arg0 "serviceCode") in
      let currency =
        (Option.map ~f:CurrencyCode.of_xml) (Xml.child xml_arg0 "currency") in
      let durationSeconds =
        (Option.map ~f:SavingsPlansDuration.of_xml)
          (Xml.child xml_arg0 "durationSeconds") in
      let paymentOption =
        (Option.map ~f:SavingsPlanPaymentOption.of_xml)
          (Xml.child xml_arg0 "paymentOption") in
      let description =
        (Option.map ~f:SavingsPlanDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let planType =
        (Option.map ~f:SavingsPlanType.of_xml)
          (Xml.child xml_arg0 "planType") in
      let productTypes =
        (Option.map ~f:SavingsPlanProductTypeList.of_xml)
          (Xml.child xml_arg0 "productTypes") in
      let offeringId =
        (Option.map ~f:UUID.of_xml) (Xml.child xml_arg0 "offeringId") in
      make ?properties ?operation ?usageType ?serviceCode ?currency
        ?durationSeconds ?paymentOption ?description ?planType ?productTypes
        ?offeringId ()
    let of_json json =
      let properties =
        field_map json "properties" SavingsPlanOfferingPropertyList.of_json in
      let operation = field_map json "operation" SavingsPlanOperation.of_json in
      let usageType = field_map json "usageType" SavingsPlanUsageType.of_json in
      let serviceCode =
        field_map json "serviceCode" SavingsPlanServiceCode.of_json in
      let currency = field_map json "currency" CurrencyCode.of_json in
      let durationSeconds =
        field_map json "durationSeconds" SavingsPlansDuration.of_json in
      let paymentOption =
        field_map json "paymentOption" SavingsPlanPaymentOption.of_json in
      let description =
        field_map json "description" SavingsPlanDescription.of_json in
      let planType = field_map json "planType" SavingsPlanType.of_json in
      let productTypes =
        field_map json "productTypes" SavingsPlanProductTypeList.of_json in
      let offeringId = field_map json "offeringId" UUID.of_json in
      make ?properties ?operation ?usageType ?serviceCode ?currency
        ?durationSeconds ?paymentOption ?description ?planType ?productTypes
        ?offeringId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a Savings Plan offering."]
module SavingsPlanOfferingFilterElement =
  struct
    type nonrec t =
      {
      name: SavingsPlanOfferingFilterAttribute.t option
        [@ocaml.doc "The filter name."];
      values: FilterValuesList.t option [@ocaml.doc "The filter values."]}
    let make ?name = fun ?values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("name",
           (Option.map x.name ~f:SavingsPlanOfferingFilterAttribute.to_value));
        ("values", (Option.map x.values ~f:FilterValuesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:FilterValuesList.of_xml) (Xml.child xml_arg0 "values") in
      let name =
        (Option.map ~f:SavingsPlanOfferingFilterAttribute.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?values ?name ()
    let of_json json =
      let values = field_map json "values" FilterValuesList.of_json in
      let name =
        field_map json "name" SavingsPlanOfferingFilterAttribute.of_json in
      make ?values ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a filter."]
module SavingsPlanOfferingRate =
  struct
    type nonrec t =
      {
      savingsPlanOffering: ParentSavingsPlanOffering.t option
        [@ocaml.doc "The Savings Plan offering."];
      rate: SavingsPlanRatePricePerUnit.t option
        [@ocaml.doc "The Savings Plan rate."];
      unit: SavingsPlanRateUnit.t option [@ocaml.doc "The unit."];
      productType: SavingsPlanProductType.t option
        [@ocaml.doc "The product type."];
      serviceCode: SavingsPlanRateServiceCode.t option
        [@ocaml.doc "The service."];
      usageType: SavingsPlanRateUsageType.t option
        [@ocaml.doc
          "The usage details of the line item in the billing report."];
      operation: SavingsPlanRateOperation.t option
        [@ocaml.doc
          "The specific AWS operation for the line item in the billing report."];
      properties: SavingsPlanOfferingRatePropertyList.t option
        [@ocaml.doc "The properties."]}
    let make ?savingsPlanOffering =
      fun ?rate ->
        fun ?unit ->
          fun ?productType ->
            fun ?serviceCode ->
              fun ?usageType ->
                fun ?operation ->
                  fun ?properties ->
                    fun () ->
                      {
                        savingsPlanOffering;
                        rate;
                        unit;
                        productType;
                        serviceCode;
                        usageType;
                        operation;
                        properties
                      }
    let to_value x =
      structure_to_value
        [("savingsPlanOffering",
           (Option.map x.savingsPlanOffering
              ~f:ParentSavingsPlanOffering.to_value));
        ("rate", (Option.map x.rate ~f:SavingsPlanRatePricePerUnit.to_value));
        ("unit", (Option.map x.unit ~f:SavingsPlanRateUnit.to_value));
        ("productType",
          (Option.map x.productType ~f:SavingsPlanProductType.to_value));
        ("serviceCode",
          (Option.map x.serviceCode ~f:SavingsPlanRateServiceCode.to_value));
        ("usageType",
          (Option.map x.usageType ~f:SavingsPlanRateUsageType.to_value));
        ("operation",
          (Option.map x.operation ~f:SavingsPlanRateOperation.to_value));
        ("properties",
          (Option.map x.properties
             ~f:SavingsPlanOfferingRatePropertyList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let properties =
        (Option.map ~f:SavingsPlanOfferingRatePropertyList.of_xml)
          (Xml.child xml_arg0 "properties") in
      let operation =
        (Option.map ~f:SavingsPlanRateOperation.of_xml)
          (Xml.child xml_arg0 "operation") in
      let usageType =
        (Option.map ~f:SavingsPlanRateUsageType.of_xml)
          (Xml.child xml_arg0 "usageType") in
      let serviceCode =
        (Option.map ~f:SavingsPlanRateServiceCode.of_xml)
          (Xml.child xml_arg0 "serviceCode") in
      let productType =
        (Option.map ~f:SavingsPlanProductType.of_xml)
          (Xml.child xml_arg0 "productType") in
      let unit =
        (Option.map ~f:SavingsPlanRateUnit.of_xml)
          (Xml.child xml_arg0 "unit") in
      let rate =
        (Option.map ~f:SavingsPlanRatePricePerUnit.of_xml)
          (Xml.child xml_arg0 "rate") in
      let savingsPlanOffering =
        (Option.map ~f:ParentSavingsPlanOffering.of_xml)
          (Xml.child xml_arg0 "savingsPlanOffering") in
      make ?properties ?operation ?usageType ?serviceCode ?productType ?unit
        ?rate ?savingsPlanOffering ()
    let of_json json =
      let properties =
        field_map json "properties"
          SavingsPlanOfferingRatePropertyList.of_json in
      let operation =
        field_map json "operation" SavingsPlanRateOperation.of_json in
      let usageType =
        field_map json "usageType" SavingsPlanRateUsageType.of_json in
      let serviceCode =
        field_map json "serviceCode" SavingsPlanRateServiceCode.of_json in
      let productType =
        field_map json "productType" SavingsPlanProductType.of_json in
      let unit = field_map json "unit" SavingsPlanRateUnit.of_json in
      let rate = field_map json "rate" SavingsPlanRatePricePerUnit.of_json in
      let savingsPlanOffering =
        field_map json "savingsPlanOffering"
          ParentSavingsPlanOffering.of_json in
      make ?properties ?operation ?usageType ?serviceCode ?productType ?unit
        ?rate ?savingsPlanOffering ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a Savings Plan offering rate."]
module SavingsPlanOfferingRateFilterElement =
  struct
    type nonrec t =
      {
      name: SavingsPlanRateFilterAttribute.t option
        [@ocaml.doc "The filter name."];
      values: FilterValuesList.t option [@ocaml.doc "The filter values."]}
    let make ?name = fun ?values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("name",
           (Option.map x.name ~f:SavingsPlanRateFilterAttribute.to_value));
        ("values", (Option.map x.values ~f:FilterValuesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:FilterValuesList.of_xml) (Xml.child xml_arg0 "values") in
      let name =
        (Option.map ~f:SavingsPlanRateFilterAttribute.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?values ?name ()
    let of_json json =
      let values = field_map json "values" FilterValuesList.of_json in
      let name = field_map json "name" SavingsPlanRateFilterAttribute.of_json in
      make ?values ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a filter."]
module SavingsPlanRate =
  struct
    type nonrec t =
      {
      rate: Amount.t option [@ocaml.doc "The rate."];
      currency: CurrencyCode.t option [@ocaml.doc "The currency."];
      unit: SavingsPlanRateUnit.t option [@ocaml.doc "The unit."];
      productType: SavingsPlanProductType.t option
        [@ocaml.doc "The product type."];
      serviceCode: SavingsPlanRateServiceCode.t option
        [@ocaml.doc "The service."];
      usageType: SavingsPlanRateUsageType.t option
        [@ocaml.doc
          "The usage details of the line item in the billing report."];
      operation: SavingsPlanRateOperation.t option
        [@ocaml.doc
          "The specific AWS operation for the line item in the billing report."];
      properties: SavingsPlanRatePropertyList.t option
        [@ocaml.doc "The properties."]}
    let make ?rate =
      fun ?currency ->
        fun ?unit ->
          fun ?productType ->
            fun ?serviceCode ->
              fun ?usageType ->
                fun ?operation ->
                  fun ?properties ->
                    fun () ->
                      {
                        rate;
                        currency;
                        unit;
                        productType;
                        serviceCode;
                        usageType;
                        operation;
                        properties
                      }
    let to_value x =
      structure_to_value
        [("rate", (Option.map x.rate ~f:Amount.to_value));
        ("currency", (Option.map x.currency ~f:CurrencyCode.to_value));
        ("unit", (Option.map x.unit ~f:SavingsPlanRateUnit.to_value));
        ("productType",
          (Option.map x.productType ~f:SavingsPlanProductType.to_value));
        ("serviceCode",
          (Option.map x.serviceCode ~f:SavingsPlanRateServiceCode.to_value));
        ("usageType",
          (Option.map x.usageType ~f:SavingsPlanRateUsageType.to_value));
        ("operation",
          (Option.map x.operation ~f:SavingsPlanRateOperation.to_value));
        ("properties",
          (Option.map x.properties ~f:SavingsPlanRatePropertyList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let properties =
        (Option.map ~f:SavingsPlanRatePropertyList.of_xml)
          (Xml.child xml_arg0 "properties") in
      let operation =
        (Option.map ~f:SavingsPlanRateOperation.of_xml)
          (Xml.child xml_arg0 "operation") in
      let usageType =
        (Option.map ~f:SavingsPlanRateUsageType.of_xml)
          (Xml.child xml_arg0 "usageType") in
      let serviceCode =
        (Option.map ~f:SavingsPlanRateServiceCode.of_xml)
          (Xml.child xml_arg0 "serviceCode") in
      let productType =
        (Option.map ~f:SavingsPlanProductType.of_xml)
          (Xml.child xml_arg0 "productType") in
      let unit =
        (Option.map ~f:SavingsPlanRateUnit.of_xml)
          (Xml.child xml_arg0 "unit") in
      let currency =
        (Option.map ~f:CurrencyCode.of_xml) (Xml.child xml_arg0 "currency") in
      let rate = (Option.map ~f:Amount.of_xml) (Xml.child xml_arg0 "rate") in
      make ?properties ?operation ?usageType ?serviceCode ?productType ?unit
        ?currency ?rate ()
    let of_json json =
      let properties =
        field_map json "properties" SavingsPlanRatePropertyList.of_json in
      let operation =
        field_map json "operation" SavingsPlanRateOperation.of_json in
      let usageType =
        field_map json "usageType" SavingsPlanRateUsageType.of_json in
      let serviceCode =
        field_map json "serviceCode" SavingsPlanRateServiceCode.of_json in
      let productType =
        field_map json "productType" SavingsPlanProductType.of_json in
      let unit = field_map json "unit" SavingsPlanRateUnit.of_json in
      let currency = field_map json "currency" CurrencyCode.of_json in
      let rate = field_map json "rate" Amount.of_json in
      make ?properties ?operation ?usageType ?serviceCode ?productType ?unit
        ?currency ?rate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a Savings Plan rate."]
module SavingsPlanRateFilter =
  struct
    type nonrec t =
      {
      name: SavingsPlanRateFilterName.t option
        [@ocaml.doc "The filter name."];
      values: ListOfStrings.t option [@ocaml.doc "The filter values."]}
    let make ?name = fun ?values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:SavingsPlanRateFilterName.to_value));
        ("values", (Option.map x.values ~f:ListOfStrings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:ListOfStrings.of_xml) (Xml.child xml_arg0 "values") in
      let name =
        (Option.map ~f:SavingsPlanRateFilterName.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?values ?name ()
    let of_json json =
      let values = field_map json "values" ListOfStrings.of_json in
      let name = field_map json "name" SavingsPlanRateFilterName.of_json in
      make ?values ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a filter."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "InternalServerException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An unexpected error occurred."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "ResourceNotFoundException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource was not found."]
module ValidationException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "ValidationException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "One of the input parameters is not valid."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "ServiceQuotaExceededException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A service quota has been exceeded."]
module PaginationToken =
  struct
    type nonrec t = string
    let context_ = "PaginationToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_pattern i ~pattern:"^[A-Za-z0-9/=\\+]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaginationToken" j
    let to_json = simple_to_json to_value
  end
module SavingsPlanList =
  struct
    type nonrec t = SavingsPlan.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlan.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlan.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanList" ~of_json:SavingsPlan.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanArnList =
  struct
    type nonrec t = SavingsPlanArn.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanArn.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanArnList" ~of_json:SavingsPlanArn.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanFilterList =
  struct
    type nonrec t = SavingsPlanFilter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanFilterList"
        ~of_json:SavingsPlanFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanIdList =
  struct
    type nonrec t = SavingsPlanId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanId.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanIdList" ~of_json:SavingsPlanId.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanStateList =
  struct
    type nonrec t = SavingsPlanState.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanState.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanState.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanStateList"
        ~of_json:SavingsPlanState.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanOfferingsList =
  struct
    type nonrec t = SavingsPlanOffering.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanOffering.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanOffering.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanOfferingsList"
        ~of_json:SavingsPlanOffering.of_json j
    let to_json v = composed_to_json to_value v
  end
module CurrencyList =
  struct
    type nonrec t = CurrencyCode.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CurrencyCode.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CurrencyCode.of_xml)
    let of_json j =
      list_of_json ~kind:"CurrencyList" ~of_json:CurrencyCode.of_json j
    let to_json v = composed_to_json to_value v
  end
module DurationsList =
  struct
    type nonrec t = SavingsPlansDuration.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlansDuration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlansDuration.of_xml)
    let of_json j =
      list_of_json ~kind:"DurationsList"
        ~of_json:SavingsPlansDuration.of_json j
    let to_json v = composed_to_json to_value v
  end
module PageSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for PageSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SavingsPlanDescriptionsList =
  struct
    type nonrec t = SavingsPlanDescription.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanDescriptionsList"
        ~of_json:SavingsPlanDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanOfferingFiltersList =
  struct
    type nonrec t = SavingsPlanOfferingFilterElement.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanOfferingFilterElement.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:SavingsPlanOfferingFilterElement.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanOfferingFiltersList"
        ~of_json:SavingsPlanOfferingFilterElement.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanOperationList =
  struct
    type nonrec t = SavingsPlanOperation.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanOperation.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanOperation.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanOperationList"
        ~of_json:SavingsPlanOperation.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanPaymentOptionList =
  struct
    type nonrec t = SavingsPlanPaymentOption.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanPaymentOption.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanPaymentOption.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanPaymentOptionList"
        ~of_json:SavingsPlanPaymentOption.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanServiceCodeList =
  struct
    type nonrec t = SavingsPlanServiceCode.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanServiceCode.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanServiceCode.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanServiceCodeList"
        ~of_json:SavingsPlanServiceCode.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanTypeList =
  struct
    type nonrec t = SavingsPlanType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanType.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanTypeList"
        ~of_json:SavingsPlanType.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanUsageTypeList =
  struct
    type nonrec t = SavingsPlanUsageType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanUsageType.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanUsageType.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanUsageTypeList"
        ~of_json:SavingsPlanUsageType.of_json j
    let to_json v = composed_to_json to_value v
  end
module UUIDs =
  struct
    type nonrec t = UUID.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:UUID.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UUID.of_xml)
    let of_json j = list_of_json ~kind:"UUIDs" ~of_json:UUID.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanOfferingRatesList =
  struct
    type nonrec t = SavingsPlanOfferingRate.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanOfferingRate.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanOfferingRate.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanOfferingRatesList"
        ~of_json:SavingsPlanOfferingRate.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanOfferingRateFiltersList =
  struct
    type nonrec t = SavingsPlanOfferingRateFilterElement.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanOfferingRateFilterElement.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:SavingsPlanOfferingRateFilterElement.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanOfferingRateFiltersList"
        ~of_json:SavingsPlanOfferingRateFilterElement.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanRateOperationList =
  struct
    type nonrec t = SavingsPlanRateOperation.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanRateOperation.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanRateOperation.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanRateOperationList"
        ~of_json:SavingsPlanRateOperation.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanRateServiceCodeList =
  struct
    type nonrec t = SavingsPlanRateServiceCode.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanRateServiceCode.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanRateServiceCode.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanRateServiceCodeList"
        ~of_json:SavingsPlanRateServiceCode.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanRateUsageTypeList =
  struct
    type nonrec t = SavingsPlanRateUsageType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanRateUsageType.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanRateUsageType.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanRateUsageTypeList"
        ~of_json:SavingsPlanRateUsageType.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanRateList =
  struct
    type nonrec t = SavingsPlanRate.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanRate.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanRate.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanRateList"
        ~of_json:SavingsPlanRate.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlanRateFilterList =
  struct
    type nonrec t = SavingsPlanRateFilter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SavingsPlanRateFilter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SavingsPlanRateFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"SavingsPlanRateFilterList"
        ~of_json:SavingsPlanRateFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module DateTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes the specified tags from the specified resource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: SavingsPlanArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      tagKeys: TagKeyList.t [@ocaml.doc "The tag keys."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (SavingsPlanArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        SavingsPlanArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_json json =
      let tagKeys = field_map_exn json "tagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json "resourceArn" SavingsPlanArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes the specified tags from the specified resource."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds the specified tags to the specified resource."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: SavingsPlanArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      tags: TagMap.t
        [@ocaml.doc
          "One or more tags. For example, { \"tags\": {\"key1\":\"value1\", \"key2\":\"value2\"} }."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (SavingsPlanArn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        SavingsPlanArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_json json =
      let tags = field_map_exn json "tags" TagMap.of_json in
      let resourceArn =
        field_map_exn json "resourceArn" SavingsPlanArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds the specified tags to the specified resource."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagMap.t option [@ocaml.doc "Information about the tags."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags for the specified resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: SavingsPlanArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (SavingsPlanArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        SavingsPlanArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_json json =
      let resourceArn =
        field_map_exn json "resourceArn" SavingsPlanArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags for the specified resource."]
module DescribeSavingsPlansResponse =
  struct
    type nonrec t =
      {
      savingsPlans: SavingsPlanList.t option
        [@ocaml.doc "Information about the Savings Plans."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?savingsPlans =
      fun ?nextToken -> fun () -> { savingsPlans; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("savingsPlans",
           (Option.map x.savingsPlans ~f:SavingsPlanList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let savingsPlans =
        (Option.map ~f:SavingsPlanList.of_xml)
          (Xml.child xml_arg0 "savingsPlans") in
      make ?nextToken ?savingsPlans ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let savingsPlans =
        field_map json "savingsPlans" SavingsPlanList.of_json in
      make ?nextToken ?savingsPlans ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Savings Plans."]
module DescribeSavingsPlansRequest =
  struct
    type nonrec t =
      {
      savingsPlanArns: SavingsPlanArnList.t option
        [@ocaml.doc "The Amazon Resource Names (ARN) of the Savings Plans."];
      savingsPlanIds: SavingsPlanIdList.t option
        [@ocaml.doc "The IDs of the Savings Plans."];
      nextToken: PaginationToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve additional results, make another call with the returned token value."];
      states: SavingsPlanStateList.t option [@ocaml.doc "The states."];
      filters: SavingsPlanFilterList.t option [@ocaml.doc "The filters."]}
    let make ?savingsPlanArns =
      fun ?savingsPlanIds ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ?states ->
              fun ?filters ->
                fun () ->
                  {
                    savingsPlanArns;
                    savingsPlanIds;
                    nextToken;
                    maxResults;
                    states;
                    filters
                  }
    let to_value x =
      structure_to_value
        [("savingsPlanArns",
           (Option.map x.savingsPlanArns ~f:SavingsPlanArnList.to_value));
        ("savingsPlanIds",
          (Option.map x.savingsPlanIds ~f:SavingsPlanIdList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("states", (Option.map x.states ~f:SavingsPlanStateList.to_value));
        ("filters", (Option.map x.filters ~f:SavingsPlanFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:SavingsPlanFilterList.of_xml)
          (Xml.child xml_arg0 "filters") in
      let states =
        (Option.map ~f:SavingsPlanStateList.of_xml)
          (Xml.child xml_arg0 "states") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let savingsPlanIds =
        (Option.map ~f:SavingsPlanIdList.of_xml)
          (Xml.child xml_arg0 "savingsPlanIds") in
      let savingsPlanArns =
        (Option.map ~f:SavingsPlanArnList.of_xml)
          (Xml.child xml_arg0 "savingsPlanArns") in
      make ?filters ?states ?maxResults ?nextToken ?savingsPlanIds
        ?savingsPlanArns ()
    let of_json json =
      let filters = field_map json "filters" SavingsPlanFilterList.of_json in
      let states = field_map json "states" SavingsPlanStateList.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let savingsPlanIds =
        field_map json "savingsPlanIds" SavingsPlanIdList.of_json in
      let savingsPlanArns =
        field_map json "savingsPlanArns" SavingsPlanArnList.of_json in
      make ?filters ?states ?maxResults ?nextToken ?savingsPlanIds
        ?savingsPlanArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Savings Plans."]
module DescribeSavingsPlansOfferingsResponse =
  struct
    type nonrec t =
      {
      searchResults: SavingsPlanOfferingsList.t option
        [@ocaml.doc "Information about the Savings Plans offerings."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?searchResults =
      fun ?nextToken -> fun () -> { searchResults; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("searchResults",
           (Option.map x.searchResults ~f:SavingsPlanOfferingsList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let searchResults =
        (Option.map ~f:SavingsPlanOfferingsList.of_xml)
          (Xml.child xml_arg0 "searchResults") in
      make ?nextToken ?searchResults ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let searchResults =
        field_map json "searchResults" SavingsPlanOfferingsList.of_json in
      make ?nextToken ?searchResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Savings Plans offerings."]
module DescribeSavingsPlansOfferingsRequest =
  struct
    type nonrec t =
      {
      offeringIds: UUIDs.t option [@ocaml.doc "The IDs of the offerings."];
      paymentOptions: SavingsPlanPaymentOptionList.t option
        [@ocaml.doc "The payment options."];
      productType: SavingsPlanProductType.t option
        [@ocaml.doc "The product type."];
      planTypes: SavingsPlanTypeList.t option [@ocaml.doc "The plan type."];
      durations: DurationsList.t option
        [@ocaml.doc "The durations, in seconds."];
      currencies: CurrencyList.t option [@ocaml.doc "The currencies."];
      descriptions: SavingsPlanDescriptionsList.t option
        [@ocaml.doc "The descriptions."];
      serviceCodes: SavingsPlanServiceCodeList.t option
        [@ocaml.doc "The services."];
      usageTypes: SavingsPlanUsageTypeList.t option
        [@ocaml.doc
          "The usage details of the line item in the billing report."];
      operations: SavingsPlanOperationList.t option
        [@ocaml.doc
          "The specific AWS operation for the line item in the billing report."];
      filters: SavingsPlanOfferingFiltersList.t option
        [@ocaml.doc "The filters."];
      nextToken: PaginationToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve additional results, make another call with the returned token value."]}
    let make ?offeringIds =
      fun ?paymentOptions ->
        fun ?productType ->
          fun ?planTypes ->
            fun ?durations ->
              fun ?currencies ->
                fun ?descriptions ->
                  fun ?serviceCodes ->
                    fun ?usageTypes ->
                      fun ?operations ->
                        fun ?filters ->
                          fun ?nextToken ->
                            fun ?maxResults ->
                              fun () ->
                                {
                                  offeringIds;
                                  paymentOptions;
                                  productType;
                                  planTypes;
                                  durations;
                                  currencies;
                                  descriptions;
                                  serviceCodes;
                                  usageTypes;
                                  operations;
                                  filters;
                                  nextToken;
                                  maxResults
                                }
    let to_value x =
      structure_to_value
        [("offeringIds", (Option.map x.offeringIds ~f:UUIDs.to_value));
        ("paymentOptions",
          (Option.map x.paymentOptions
             ~f:SavingsPlanPaymentOptionList.to_value));
        ("productType",
          (Option.map x.productType ~f:SavingsPlanProductType.to_value));
        ("planTypes",
          (Option.map x.planTypes ~f:SavingsPlanTypeList.to_value));
        ("durations", (Option.map x.durations ~f:DurationsList.to_value));
        ("currencies", (Option.map x.currencies ~f:CurrencyList.to_value));
        ("descriptions",
          (Option.map x.descriptions ~f:SavingsPlanDescriptionsList.to_value));
        ("serviceCodes",
          (Option.map x.serviceCodes ~f:SavingsPlanServiceCodeList.to_value));
        ("usageTypes",
          (Option.map x.usageTypes ~f:SavingsPlanUsageTypeList.to_value));
        ("operations",
          (Option.map x.operations ~f:SavingsPlanOperationList.to_value));
        ("filters",
          (Option.map x.filters ~f:SavingsPlanOfferingFiltersList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let filters =
        (Option.map ~f:SavingsPlanOfferingFiltersList.of_xml)
          (Xml.child xml_arg0 "filters") in
      let operations =
        (Option.map ~f:SavingsPlanOperationList.of_xml)
          (Xml.child xml_arg0 "operations") in
      let usageTypes =
        (Option.map ~f:SavingsPlanUsageTypeList.of_xml)
          (Xml.child xml_arg0 "usageTypes") in
      let serviceCodes =
        (Option.map ~f:SavingsPlanServiceCodeList.of_xml)
          (Xml.child xml_arg0 "serviceCodes") in
      let descriptions =
        (Option.map ~f:SavingsPlanDescriptionsList.of_xml)
          (Xml.child xml_arg0 "descriptions") in
      let currencies =
        (Option.map ~f:CurrencyList.of_xml) (Xml.child xml_arg0 "currencies") in
      let durations =
        (Option.map ~f:DurationsList.of_xml) (Xml.child xml_arg0 "durations") in
      let planTypes =
        (Option.map ~f:SavingsPlanTypeList.of_xml)
          (Xml.child xml_arg0 "planTypes") in
      let productType =
        (Option.map ~f:SavingsPlanProductType.of_xml)
          (Xml.child xml_arg0 "productType") in
      let paymentOptions =
        (Option.map ~f:SavingsPlanPaymentOptionList.of_xml)
          (Xml.child xml_arg0 "paymentOptions") in
      let offeringIds =
        (Option.map ~f:UUIDs.of_xml) (Xml.child xml_arg0 "offeringIds") in
      make ?maxResults ?nextToken ?filters ?operations ?usageTypes
        ?serviceCodes ?descriptions ?currencies ?durations ?planTypes
        ?productType ?paymentOptions ?offeringIds ()
    let of_json json =
      let maxResults = field_map json "maxResults" PageSize.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let filters =
        field_map json "filters" SavingsPlanOfferingFiltersList.of_json in
      let operations =
        field_map json "operations" SavingsPlanOperationList.of_json in
      let usageTypes =
        field_map json "usageTypes" SavingsPlanUsageTypeList.of_json in
      let serviceCodes =
        field_map json "serviceCodes" SavingsPlanServiceCodeList.of_json in
      let descriptions =
        field_map json "descriptions" SavingsPlanDescriptionsList.of_json in
      let currencies = field_map json "currencies" CurrencyList.of_json in
      let durations = field_map json "durations" DurationsList.of_json in
      let planTypes = field_map json "planTypes" SavingsPlanTypeList.of_json in
      let productType =
        field_map json "productType" SavingsPlanProductType.of_json in
      let paymentOptions =
        field_map json "paymentOptions" SavingsPlanPaymentOptionList.of_json in
      let offeringIds = field_map json "offeringIds" UUIDs.of_json in
      make ?maxResults ?nextToken ?filters ?operations ?usageTypes
        ?serviceCodes ?descriptions ?currencies ?durations ?planTypes
        ?productType ?paymentOptions ?offeringIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Savings Plans offerings."]
module DescribeSavingsPlansOfferingRatesResponse =
  struct
    type nonrec t =
      {
      searchResults: SavingsPlanOfferingRatesList.t option
        [@ocaml.doc "Information about the Savings Plans offering rates."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?searchResults =
      fun ?nextToken -> fun () -> { searchResults; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("searchResults",
           (Option.map x.searchResults
              ~f:SavingsPlanOfferingRatesList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let searchResults =
        (Option.map ~f:SavingsPlanOfferingRatesList.of_xml)
          (Xml.child xml_arg0 "searchResults") in
      make ?nextToken ?searchResults ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let searchResults =
        field_map json "searchResults" SavingsPlanOfferingRatesList.of_json in
      make ?nextToken ?searchResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Savings Plans offering rates."]
module DescribeSavingsPlansOfferingRatesRequest =
  struct
    type nonrec t =
      {
      savingsPlanOfferingIds: UUIDs.t option
        [@ocaml.doc "The IDs of the offerings."];
      savingsPlanPaymentOptions: SavingsPlanPaymentOptionList.t option
        [@ocaml.doc "The payment options."];
      savingsPlanTypes: SavingsPlanTypeList.t option
        [@ocaml.doc "The plan types."];
      products: SavingsPlanProductTypeList.t option
        [@ocaml.doc "The AWS products."];
      serviceCodes: SavingsPlanRateServiceCodeList.t option
        [@ocaml.doc "The services."];
      usageTypes: SavingsPlanRateUsageTypeList.t option
        [@ocaml.doc
          "The usage details of the line item in the billing report."];
      operations: SavingsPlanRateOperationList.t option
        [@ocaml.doc
          "The specific AWS operation for the line item in the billing report."];
      filters: SavingsPlanOfferingRateFiltersList.t option
        [@ocaml.doc "The filters."];
      nextToken: PaginationToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: PageSize.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve additional results, make another call with the returned token value."]}
    let make ?savingsPlanOfferingIds =
      fun ?savingsPlanPaymentOptions ->
        fun ?savingsPlanTypes ->
          fun ?products ->
            fun ?serviceCodes ->
              fun ?usageTypes ->
                fun ?operations ->
                  fun ?filters ->
                    fun ?nextToken ->
                      fun ?maxResults ->
                        fun () ->
                          {
                            savingsPlanOfferingIds;
                            savingsPlanPaymentOptions;
                            savingsPlanTypes;
                            products;
                            serviceCodes;
                            usageTypes;
                            operations;
                            filters;
                            nextToken;
                            maxResults
                          }
    let to_value x =
      structure_to_value
        [("savingsPlanOfferingIds",
           (Option.map x.savingsPlanOfferingIds ~f:UUIDs.to_value));
        ("savingsPlanPaymentOptions",
          (Option.map x.savingsPlanPaymentOptions
             ~f:SavingsPlanPaymentOptionList.to_value));
        ("savingsPlanTypes",
          (Option.map x.savingsPlanTypes ~f:SavingsPlanTypeList.to_value));
        ("products",
          (Option.map x.products ~f:SavingsPlanProductTypeList.to_value));
        ("serviceCodes",
          (Option.map x.serviceCodes
             ~f:SavingsPlanRateServiceCodeList.to_value));
        ("usageTypes",
          (Option.map x.usageTypes ~f:SavingsPlanRateUsageTypeList.to_value));
        ("operations",
          (Option.map x.operations ~f:SavingsPlanRateOperationList.to_value));
        ("filters",
          (Option.map x.filters
             ~f:SavingsPlanOfferingRateFiltersList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:PageSize.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:PageSize.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let filters =
        (Option.map ~f:SavingsPlanOfferingRateFiltersList.of_xml)
          (Xml.child xml_arg0 "filters") in
      let operations =
        (Option.map ~f:SavingsPlanRateOperationList.of_xml)
          (Xml.child xml_arg0 "operations") in
      let usageTypes =
        (Option.map ~f:SavingsPlanRateUsageTypeList.of_xml)
          (Xml.child xml_arg0 "usageTypes") in
      let serviceCodes =
        (Option.map ~f:SavingsPlanRateServiceCodeList.of_xml)
          (Xml.child xml_arg0 "serviceCodes") in
      let products =
        (Option.map ~f:SavingsPlanProductTypeList.of_xml)
          (Xml.child xml_arg0 "products") in
      let savingsPlanTypes =
        (Option.map ~f:SavingsPlanTypeList.of_xml)
          (Xml.child xml_arg0 "savingsPlanTypes") in
      let savingsPlanPaymentOptions =
        (Option.map ~f:SavingsPlanPaymentOptionList.of_xml)
          (Xml.child xml_arg0 "savingsPlanPaymentOptions") in
      let savingsPlanOfferingIds =
        (Option.map ~f:UUIDs.of_xml)
          (Xml.child xml_arg0 "savingsPlanOfferingIds") in
      make ?maxResults ?nextToken ?filters ?operations ?usageTypes
        ?serviceCodes ?products ?savingsPlanTypes ?savingsPlanPaymentOptions
        ?savingsPlanOfferingIds ()
    let of_json json =
      let maxResults = field_map json "maxResults" PageSize.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let filters =
        field_map json "filters" SavingsPlanOfferingRateFiltersList.of_json in
      let operations =
        field_map json "operations" SavingsPlanRateOperationList.of_json in
      let usageTypes =
        field_map json "usageTypes" SavingsPlanRateUsageTypeList.of_json in
      let serviceCodes =
        field_map json "serviceCodes" SavingsPlanRateServiceCodeList.of_json in
      let products =
        field_map json "products" SavingsPlanProductTypeList.of_json in
      let savingsPlanTypes =
        field_map json "savingsPlanTypes" SavingsPlanTypeList.of_json in
      let savingsPlanPaymentOptions =
        field_map json "savingsPlanPaymentOptions"
          SavingsPlanPaymentOptionList.of_json in
      let savingsPlanOfferingIds =
        field_map json "savingsPlanOfferingIds" UUIDs.of_json in
      make ?maxResults ?nextToken ?filters ?operations ?usageTypes
        ?serviceCodes ?products ?savingsPlanTypes ?savingsPlanPaymentOptions
        ?savingsPlanOfferingIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Savings Plans offering rates."]
module DescribeSavingsPlanRatesResponse =
  struct
    type nonrec t =
      {
      savingsPlanId: SavingsPlanId.t option
        [@ocaml.doc "The ID of the Savings Plan."];
      searchResults: SavingsPlanRateList.t option
        [@ocaml.doc "Information about the Savings Plans rates."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?savingsPlanId =
      fun ?searchResults ->
        fun ?nextToken ->
          fun () -> { savingsPlanId; searchResults; nextToken }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("savingsPlanId",
           (Option.map x.savingsPlanId ~f:SavingsPlanId.to_value));
        ("searchResults",
          (Option.map x.searchResults ~f:SavingsPlanRateList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let searchResults =
        (Option.map ~f:SavingsPlanRateList.of_xml)
          (Xml.child xml_arg0 "searchResults") in
      let savingsPlanId =
        (Option.map ~f:SavingsPlanId.of_xml)
          (Xml.child xml_arg0 "savingsPlanId") in
      make ?nextToken ?searchResults ?savingsPlanId ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let searchResults =
        field_map json "searchResults" SavingsPlanRateList.of_json in
      let savingsPlanId =
        field_map json "savingsPlanId" SavingsPlanId.of_json in
      make ?nextToken ?searchResults ?savingsPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Savings Plans rates."]
module DescribeSavingsPlanRatesRequest =
  struct
    type nonrec t =
      {
      savingsPlanId: SavingsPlanId.t
        [@ocaml.doc "The ID of the Savings Plan."];
      filters: SavingsPlanRateFilterList.t option [@ocaml.doc "The filters."];
      nextToken: PaginationToken.t option
        [@ocaml.doc "The token for the next page of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return with a single call. To retrieve additional results, make another call with the returned token value."]}
    let context_ = "DescribeSavingsPlanRatesRequest"
    let make ?filters =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ~savingsPlanId ->
            fun () -> { filters; nextToken; maxResults; savingsPlanId }
    let to_value x =
      structure_to_value
        [("savingsPlanId", (Some (SavingsPlanId.to_value x.savingsPlanId)));
        ("filters",
          (Option.map x.filters ~f:SavingsPlanRateFilterList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let filters =
        (Option.map ~f:SavingsPlanRateFilterList.of_xml)
          (Xml.child xml_arg0 "filters") in
      let savingsPlanId =
        SavingsPlanId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "savingsPlanId") in
      make ?maxResults ?nextToken ?filters ~savingsPlanId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let filters =
        field_map json "filters" SavingsPlanRateFilterList.of_json in
      let savingsPlanId =
        field_map_exn json "savingsPlanId" SavingsPlanId.of_json in
      make ?maxResults ?nextToken ?filters ~savingsPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the specified Savings Plans rates."]
module DeleteQueuedSavingsPlanResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the queued purchase for the specified Savings Plan."]
module DeleteQueuedSavingsPlanRequest =
  struct
    type nonrec t =
      {
      savingsPlanId: SavingsPlanId.t
        [@ocaml.doc "The ID of the Savings Plan."]}
    let context_ = "DeleteQueuedSavingsPlanRequest"
    let make ~savingsPlanId = fun () -> { savingsPlanId }
    let to_value x =
      structure_to_value
        [("savingsPlanId", (Some (SavingsPlanId.to_value x.savingsPlanId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let savingsPlanId =
        SavingsPlanId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "savingsPlanId") in
      make ~savingsPlanId ()
    let of_json json =
      let savingsPlanId =
        field_map_exn json "savingsPlanId" SavingsPlanId.of_json in
      make ~savingsPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the queued purchase for the specified Savings Plan."]
module CreateSavingsPlanResponse =
  struct
    type nonrec t =
      {
      savingsPlanId: SavingsPlanId.t option
        [@ocaml.doc "The ID of the Savings Plan."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?savingsPlanId = fun () -> { savingsPlanId }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("savingsPlanId",
           (Option.map x.savingsPlanId ~f:SavingsPlanId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let savingsPlanId =
        (Option.map ~f:SavingsPlanId.of_xml)
          (Xml.child xml_arg0 "savingsPlanId") in
      make ?savingsPlanId ()
    let of_json json =
      let savingsPlanId =
        field_map json "savingsPlanId" SavingsPlanId.of_json in
      make ?savingsPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a Savings Plan."]
module CreateSavingsPlanRequest =
  struct
    type nonrec t =
      {
      savingsPlanOfferingId: SavingsPlanOfferingId.t
        [@ocaml.doc "The ID of the offering."];
      commitment: Amount.t
        [@ocaml.doc
          "The hourly commitment, in USD. This is a value between 0.001 and 1 million. You cannot specify more than five digits after the decimal point."];
      upfrontPaymentAmount: Amount.t option
        [@ocaml.doc
          "The up-front payment amount. This is a whole number between 50 and 99 percent of the total value of the Savings Plan. This parameter is supported only if the payment option is Partial Upfront."];
      purchaseTime: DateTime.t option
        [@ocaml.doc
          "The time at which to purchase the Savings Plan, in UTC format (YYYY-MM-DDTHH:MM:SSZ)."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "Unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      tags: TagMap.t option [@ocaml.doc "One or more tags."]}
    let context_ = "CreateSavingsPlanRequest"
    let make ?upfrontPaymentAmount =
      fun ?purchaseTime ->
        fun ?clientToken ->
          fun ?tags ->
            fun ~savingsPlanOfferingId ->
              fun ~commitment ->
                fun () ->
                  {
                    upfrontPaymentAmount;
                    purchaseTime;
                    clientToken;
                    tags;
                    savingsPlanOfferingId;
                    commitment
                  }
    let to_value x =
      structure_to_value
        [("savingsPlanOfferingId",
           (Some (SavingsPlanOfferingId.to_value x.savingsPlanOfferingId)));
        ("commitment", (Some (Amount.to_value x.commitment)));
        ("upfrontPaymentAmount",
          (Option.map x.upfrontPaymentAmount ~f:Amount.to_value));
        ("purchaseTime", (Option.map x.purchaseTime ~f:DateTime.to_value));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let purchaseTime =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "purchaseTime") in
      let upfrontPaymentAmount =
        (Option.map ~f:Amount.of_xml)
          (Xml.child xml_arg0 "upfrontPaymentAmount") in
      let commitment =
        Amount.of_xml (Xml.child_exn ~context:context_ xml_arg0 "commitment") in
      let savingsPlanOfferingId =
        SavingsPlanOfferingId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "savingsPlanOfferingId") in
      make ?tags ?clientToken ?purchaseTime ?upfrontPaymentAmount ~commitment
        ~savingsPlanOfferingId ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let clientToken = field_map json "clientToken" ClientToken.of_json in
      let purchaseTime = field_map json "purchaseTime" DateTime.of_json in
      let upfrontPaymentAmount =
        field_map json "upfrontPaymentAmount" Amount.of_json in
      let commitment = field_map_exn json "commitment" Amount.of_json in
      let savingsPlanOfferingId =
        field_map_exn json "savingsPlanOfferingId"
          SavingsPlanOfferingId.of_json in
      make ?tags ?clientToken ?purchaseTime ?upfrontPaymentAmount ~commitment
        ~savingsPlanOfferingId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a Savings Plan."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateSavingsPlan: (CreateSavingsPlanRequest.t,
  CreateSavingsPlanResponse.t, CreateSavingsPlanResponse.error) t 
  | DeleteQueuedSavingsPlan: (DeleteQueuedSavingsPlanRequest.t,
  DeleteQueuedSavingsPlanResponse.t, DeleteQueuedSavingsPlanResponse.error) t
  
  | DescribeSavingsPlanRates: (DescribeSavingsPlanRatesRequest.t,
  DescribeSavingsPlanRatesResponse.t, DescribeSavingsPlanRatesResponse.error)
  t 
  | DescribeSavingsPlans: (DescribeSavingsPlansRequest.t,
  DescribeSavingsPlansResponse.t, DescribeSavingsPlansResponse.error) t 
  | DescribeSavingsPlansOfferingRates:
  (DescribeSavingsPlansOfferingRatesRequest.t,
  DescribeSavingsPlansOfferingRatesResponse.t,
  DescribeSavingsPlansOfferingRatesResponse.error) t 
  | DescribeSavingsPlansOfferings: (DescribeSavingsPlansOfferingsRequest.t,
  DescribeSavingsPlansOfferingsResponse.t,
  DescribeSavingsPlansOfferingsResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateSavingsPlan -> `POST
  | DeleteQueuedSavingsPlan -> `POST
  | DescribeSavingsPlanRates -> `POST
  | DescribeSavingsPlans -> `POST
  | DescribeSavingsPlansOfferingRates -> `POST
  | DescribeSavingsPlansOfferings -> `POST
  | ListTagsForResource -> `POST
  | TagResource -> `POST
  | UntagResource -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateSavingsPlan ->
          (Format.kasprintf Uri.of_string) "/CreateSavingsPlan"
      | DeleteQueuedSavingsPlan ->
          (Format.kasprintf Uri.of_string) "/DeleteQueuedSavingsPlan"
      | DescribeSavingsPlanRates ->
          (Format.kasprintf Uri.of_string) "/DescribeSavingsPlanRates"
      | DescribeSavingsPlans ->
          (Format.kasprintf Uri.of_string) "/DescribeSavingsPlans"
      | DescribeSavingsPlansOfferingRates ->
          (Format.kasprintf Uri.of_string)
            "/DescribeSavingsPlansOfferingRates"
      | DescribeSavingsPlansOfferings ->
          (Format.kasprintf Uri.of_string) "/DescribeSavingsPlansOfferings"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/ListTagsForResource"
      | TagResource -> (Format.kasprintf Uri.of_string) "/TagResource"
      | UntagResource -> (Format.kasprintf Uri.of_string) "/UntagResource")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateSavingsPlan ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("savingsPlanOfferingId",
                           (SavingsPlanOfferingId.to_value
                              req.CreateSavingsPlanRequest.savingsPlanOfferingId));
                      Some
                        ("commitment",
                          (Amount.to_value
                             req.CreateSavingsPlanRequest.commitment));
                      Option.map
                        req.CreateSavingsPlanRequest.upfrontPaymentAmount
                        ~f:(fun x ->
                              ("upfrontPaymentAmount", (Amount.to_value x)));
                      Option.map req.CreateSavingsPlanRequest.purchaseTime
                        ~f:(fun x -> ("purchaseTime", (DateTime.to_value x)));
                      Option.map req.CreateSavingsPlanRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)));
                      Option.map req.CreateSavingsPlanRequest.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteQueuedSavingsPlan ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("savingsPlanId",
                           (SavingsPlanId.to_value
                              req.DeleteQueuedSavingsPlanRequest.savingsPlanId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeSavingsPlanRates ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("savingsPlanId",
                           (SavingsPlanId.to_value
                              req.DescribeSavingsPlanRatesRequest.savingsPlanId));
                      Option.map req.DescribeSavingsPlanRatesRequest.filters
                        ~f:(fun x ->
                              ("filters",
                                (SavingsPlanRateFilterList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlanRatesRequest.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)));
                      Option.map
                        req.DescribeSavingsPlanRatesRequest.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeSavingsPlans ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DescribeSavingsPlansRequest.savingsPlanArns
                         ~f:(fun x ->
                               ("savingsPlanArns",
                                 (SavingsPlanArnList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansRequest.savingsPlanIds
                        ~f:(fun x ->
                              ("savingsPlanIds",
                                (SavingsPlanIdList.to_value x)));
                      Option.map req.DescribeSavingsPlansRequest.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)));
                      Option.map req.DescribeSavingsPlansRequest.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.DescribeSavingsPlansRequest.states
                        ~f:(fun x ->
                              ("states", (SavingsPlanStateList.to_value x)));
                      Option.map req.DescribeSavingsPlansRequest.filters
                        ~f:(fun x ->
                              ("filters", (SavingsPlanFilterList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeSavingsPlansOfferingRates ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DescribeSavingsPlansOfferingRatesRequest.savingsPlanOfferingIds
                         ~f:(fun x ->
                               ("savingsPlanOfferingIds", (UUIDs.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.savingsPlanPaymentOptions
                        ~f:(fun x ->
                              ("savingsPlanPaymentOptions",
                                (SavingsPlanPaymentOptionList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.savingsPlanTypes
                        ~f:(fun x ->
                              ("savingsPlanTypes",
                                (SavingsPlanTypeList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.products
                        ~f:(fun x ->
                              ("products",
                                (SavingsPlanProductTypeList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.serviceCodes
                        ~f:(fun x ->
                              ("serviceCodes",
                                (SavingsPlanRateServiceCodeList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.usageTypes
                        ~f:(fun x ->
                              ("usageTypes",
                                (SavingsPlanRateUsageTypeList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.operations
                        ~f:(fun x ->
                              ("operations",
                                (SavingsPlanRateOperationList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.filters
                        ~f:(fun x ->
                              ("filters",
                                (SavingsPlanOfferingRateFiltersList.to_value
                                   x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingRatesRequest.maxResults
                        ~f:(fun x -> ("maxResults", (PageSize.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeSavingsPlansOfferings ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DescribeSavingsPlansOfferingsRequest.offeringIds
                         ~f:(fun x -> ("offeringIds", (UUIDs.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.paymentOptions
                        ~f:(fun x ->
                              ("paymentOptions",
                                (SavingsPlanPaymentOptionList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.productType
                        ~f:(fun x ->
                              ("productType",
                                (SavingsPlanProductType.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.planTypes
                        ~f:(fun x ->
                              ("planTypes", (SavingsPlanTypeList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.durations
                        ~f:(fun x ->
                              ("durations", (DurationsList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.currencies
                        ~f:(fun x ->
                              ("currencies", (CurrencyList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.descriptions
                        ~f:(fun x ->
                              ("descriptions",
                                (SavingsPlanDescriptionsList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.serviceCodes
                        ~f:(fun x ->
                              ("serviceCodes",
                                (SavingsPlanServiceCodeList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.usageTypes
                        ~f:(fun x ->
                              ("usageTypes",
                                (SavingsPlanUsageTypeList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.operations
                        ~f:(fun x ->
                              ("operations",
                                (SavingsPlanOperationList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.filters
                        ~f:(fun x ->
                              ("filters",
                                (SavingsPlanOfferingFiltersList.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)));
                      Option.map
                        req.DescribeSavingsPlansOfferingsRequest.maxResults
                        ~f:(fun x -> ("maxResults", (PageSize.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("resourceArn",
                           (SavingsPlanArn.to_value
                              req.ListTagsForResourceRequest.resourceArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("resourceArn",
                           (SavingsPlanArn.to_value
                              req.TagResourceRequest.resourceArn));
                      Some
                        ("tags",
                          (TagMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("resourceArn",
                           (SavingsPlanArn.to_value
                              req.UntagResourceRequest.resourceArn));
                      Some
                        ("tagKeys",
                          (TagKeyList.to_value
                             req.UntagResourceRequest.tagKeys))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | CreateSavingsPlan ->
      (match resp with
       | Error err ->
           handle_error err (Some CreateSavingsPlanResponse.error_of_json)
       | Ok resp ->
           Ok (CreateSavingsPlanResponse.of_json (response_to_json resp)))
  | DeleteQueuedSavingsPlan ->
      (match resp with
       | Error err ->
           handle_error err
             (Some DeleteQueuedSavingsPlanResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok
             (DeleteQueuedSavingsPlanResponse.of_header_and_body
                (headers, ())))
  | DescribeSavingsPlanRates ->
      (match resp with
       | Error err ->
           handle_error err
             (Some DescribeSavingsPlanRatesResponse.error_of_json)
       | Ok resp ->
           Ok
             (DescribeSavingsPlanRatesResponse.of_json
                (response_to_json resp)))
  | DescribeSavingsPlans ->
      (match resp with
       | Error err ->
           handle_error err (Some DescribeSavingsPlansResponse.error_of_json)
       | Ok resp ->
           Ok (DescribeSavingsPlansResponse.of_json (response_to_json resp)))
  | DescribeSavingsPlansOfferingRates ->
      (match resp with
       | Error err ->
           handle_error err
             (Some DescribeSavingsPlansOfferingRatesResponse.error_of_json)
       | Ok resp ->
           Ok
             (DescribeSavingsPlansOfferingRatesResponse.of_json
                (response_to_json resp)))
  | DescribeSavingsPlansOfferings ->
      (match resp with
       | Error err ->
           handle_error err
             (Some DescribeSavingsPlansOfferingsResponse.error_of_json)
       | Ok resp ->
           Ok
             (DescribeSavingsPlansOfferingsResponse.of_json
                (response_to_json resp)))
  | ListTagsForResource ->
      (match resp with
       | Error err ->
           handle_error err (Some ListTagsForResourceResponse.error_of_json)
       | Ok resp ->
           Ok (ListTagsForResourceResponse.of_json (response_to_json resp)))
  | TagResource ->
      (match resp with
       | Error err ->
           handle_error err (Some TagResourceResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (TagResourceResponse.of_header_and_body (headers, ())))
  | UntagResource ->
      (match resp with
       | Error err ->
           handle_error err (Some UntagResourceResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (UntagResourceResponse.of_header_and_body (headers, ())))
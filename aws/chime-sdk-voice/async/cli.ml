(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_phone_numbers_with_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceAssociate =
         flag "force-associate" (optional bool) ~doc:"BOOL NullableBoolean"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_phone_numbers_with_voice_connector
           (Values.AssociatePhoneNumbersWithVoiceConnectorRequest.make
              ?forceAssociate ~voiceConnectorId
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some
              Values.AssociatePhoneNumbersWithVoiceConnectorResponse.to_json)
           (Some
              Values.AssociatePhoneNumbersWithVoiceConnectorResponse.error_to_json)])
let associate_phone_numbers_with_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceAssociate =
         flag "force-associate" (optional bool) ~doc:"BOOL NullableBoolean"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_phone_numbers_with_voice_connector_group
           (Values.AssociatePhoneNumbersWithVoiceConnectorGroupRequest.make
              ?forceAssociate ~voiceConnectorGroupId
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some
              Values.AssociatePhoneNumbersWithVoiceConnectorGroupResponse.to_json)
           (Some
              Values.AssociatePhoneNumbersWithVoiceConnectorGroupResponse.error_to_json)])
let batch_delete_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberIds =
         flag "phone-number-ids" (required json_arg)
           ~doc:"JSON NonEmptyStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_phone_number
           (Values.BatchDeletePhoneNumberRequest.make
              ~phoneNumberIds:(Values.NonEmptyStringList.of_json
                                 phoneNumberIds) ())
           (Some Values.BatchDeletePhoneNumberResponse.to_json)
           (Some Values.BatchDeletePhoneNumberResponse.error_to_json)])
let batch_update_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updatePhoneNumberRequestItems =
         flag "update-phone-number-request-items" (required json_arg)
           ~doc:"JSON UpdatePhoneNumberRequestItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_phone_number
           (Values.BatchUpdatePhoneNumberRequest.make
              ~updatePhoneNumberRequestItems:(Values.UpdatePhoneNumberRequestItemList.of_json
                                                updatePhoneNumberRequestItems)
              ()) (Some Values.BatchUpdatePhoneNumberResponse.to_json)
           (Some Values.BatchUpdatePhoneNumberResponse.error_to_json)])
let create_phone_number_order =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING PhoneNumberName"
       and productType =
         flag "product-type" (required json_arg)
           ~doc:"JSON PhoneNumberProductType"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_phone_number_order
           (Values.CreatePhoneNumberOrderRequest.make ?name
              ~productType:(Values.PhoneNumberProductType.of_json productType)
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some Values.CreatePhoneNumberOrderResponse.to_json)
           (Some Values.CreatePhoneNumberOrderResponse.error_to_json)])
let create_proxy_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ProxySessionNameString"
       and expiryMinutes =
         flag "expiry-minutes" (optional int) ~doc:"INT PositiveInteger"
       and numberSelectionBehavior =
         flag "number-selection-behavior" (optional json_arg)
           ~doc:"JSON NumberSelectionBehavior"
       and geoMatchLevel =
         flag "geo-match-level" (optional json_arg) ~doc:"JSON GeoMatchLevel"
       and geoMatchParams =
         flag "geo-match-params" (optional json_arg)
           ~doc:"JSON GeoMatchParams"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and participantPhoneNumbers =
         flag "participant-phone-numbers" (required json_arg)
           ~doc:"JSON ParticipantPhoneNumberList"
       and capabilities =
         flag "capabilities" (required json_arg) ~doc:"JSON CapabilityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_proxy_session
           (Values.CreateProxySessionRequest.make ?name ?expiryMinutes
              ?numberSelectionBehavior:(Option.map
                                          ~f:Values.NumberSelectionBehavior.of_json
                                          numberSelectionBehavior)
              ?geoMatchLevel:(Option.map ~f:Values.GeoMatchLevel.of_json
                                geoMatchLevel)
              ?geoMatchParams:(Option.map ~f:Values.GeoMatchParams.of_json
                                 geoMatchParams) ~voiceConnectorId
              ~participantPhoneNumbers:(Values.ParticipantPhoneNumberList.of_json
                                          participantPhoneNumbers)
              ~capabilities:(Values.CapabilityList.of_json capabilities) ())
           (Some Values.CreateProxySessionResponse.to_json)
           (Some Values.CreateProxySessionResponse.error_to_json)])
let create_sip_media_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and awsRegion =
         flag "aws-region" (required string) ~doc:"STRING String"
       and name =
         flag "name" (required string) ~doc:"STRING SipMediaApplicationName"
       and endpoints =
         flag "endpoints" (required json_arg)
           ~doc:"JSON SipMediaApplicationEndpointList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sip_media_application
           (Values.CreateSipMediaApplicationRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~awsRegion
              ~name
              ~endpoints:(Values.SipMediaApplicationEndpointList.of_json
                            endpoints) ())
           (Some Values.CreateSipMediaApplicationResponse.to_json)
           (Some Values.CreateSipMediaApplicationResponse.error_to_json)])
let create_sip_media_application_call =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipHeaders =
         flag "sip-headers" (optional json_arg) ~doc:"JSON SipHeadersMap"
       and argumentsMap =
         flag "arguments-map" (optional json_arg)
           ~doc:"JSON SMACreateCallArgumentsMap"
       and fromPhoneNumber =
         flag "from-phone-number" (required string)
           ~doc:"STRING E164PhoneNumber"
       and toPhoneNumber =
         flag "to-phone-number" (required string)
           ~doc:"STRING E164PhoneNumber"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sip_media_application_call
           (Values.CreateSipMediaApplicationCallRequest.make
              ?sipHeaders:(Option.map ~f:Values.SipHeadersMap.of_json
                             sipHeaders)
              ?argumentsMap:(Option.map
                               ~f:Values.SMACreateCallArgumentsMap.of_json
                               argumentsMap) ~fromPhoneNumber ~toPhoneNumber
              ~sipMediaApplicationId ())
           (Some Values.CreateSipMediaApplicationCallResponse.to_json)
           (Some Values.CreateSipMediaApplicationCallResponse.error_to_json)])
let create_sip_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and disabled =
         flag "disabled" (optional bool) ~doc:"BOOL NullableBoolean"
       and targetApplications =
         flag "target-applications" (optional json_arg)
           ~doc:"JSON SipRuleTargetApplicationList"
       and name = flag "name" (required string) ~doc:"STRING SipRuleName"
       and triggerType =
         flag "trigger-type" (required json_arg)
           ~doc:"JSON SipRuleTriggerType"
       and triggerValue =
         flag "trigger-value" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sip_rule
           (Values.CreateSipRuleRequest.make ?disabled
              ?targetApplications:(Option.map
                                     ~f:Values.SipRuleTargetApplicationList.of_json
                                     targetApplications) ~name
              ~triggerType:(Values.SipRuleTriggerType.of_json triggerType)
              ~triggerValue ()) (Some Values.CreateSipRuleResponse.to_json)
           (Some Values.CreateSipRuleResponse.error_to_json)])
let create_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsRegion =
         flag "aws-region" (optional json_arg)
           ~doc:"JSON VoiceConnectorAwsRegion"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and integrationType =
         flag "integration-type" (optional json_arg)
           ~doc:"JSON VoiceConnectorIntegrationType"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceConnectorName"
       and requireEncryption =
         flag "require-encryption" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_voice_connector
           (Values.CreateVoiceConnectorRequest.make
              ?awsRegion:(Option.map
                            ~f:Values.VoiceConnectorAwsRegion.of_json
                            awsRegion)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?integrationType:(Option.map
                                  ~f:Values.VoiceConnectorIntegrationType.of_json
                                  integrationType)
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType) ~name ~requireEncryption ())
           (Some Values.CreateVoiceConnectorResponse.to_json)
           (Some Values.CreateVoiceConnectorResponse.error_to_json)])
let create_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorItems =
         flag "voice-connector-items" (optional json_arg)
           ~doc:"JSON VoiceConnectorItemList"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceConnectorGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_voice_connector_group
           (Values.CreateVoiceConnectorGroupRequest.make
              ?voiceConnectorItems:(Option.map
                                      ~f:Values.VoiceConnectorItemList.of_json
                                      voiceConnectorItems) ~name ())
           (Some Values.CreateVoiceConnectorGroupResponse.to_json)
           (Some Values.CreateVoiceConnectorGroupResponse.error_to_json)])
let create_voice_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and speakerSearchTaskId =
         flag "speaker-search-task-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_voice_profile
           (Values.CreateVoiceProfileRequest.make ~speakerSearchTaskId ())
           (Some Values.CreateVoiceProfileResponse.to_json)
           (Some Values.CreateVoiceProfileResponse.error_to_json)])
let create_voice_profile_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING VoiceProfileDomainDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceProfileDomainName"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (required json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_voice_profile_domain
           (Values.CreateVoiceProfileDomainRequest.make ?description
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~serverSideEncryptionConfiguration:(Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ()) (Some Values.CreateVoiceProfileDomainResponse.to_json)
           (Some Values.CreateVoiceProfileDomainResponse.error_to_json)])
let delete_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberId =
         flag "phone-number-id" (required string)
           ~doc:"STRING SensitiveNonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_phone_number
           (Values.DeletePhoneNumberRequest.make ~phoneNumberId ()) None None])
let delete_proxy_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and proxySessionId =
         flag "proxy-session-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_proxy_session
           (Values.DeleteProxySessionRequest.make ~voiceConnectorId
              ~proxySessionId ()) None None])
let delete_sip_media_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sip_media_application
           (Values.DeleteSipMediaApplicationRequest.make
              ~sipMediaApplicationId ()) None None])
let delete_sip_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipRuleId =
         flag "sip-rule-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sip_rule
           (Values.DeleteSipRuleRequest.make ~sipRuleId ()) None None])
let delete_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector
           (Values.DeleteVoiceConnectorRequest.make ~voiceConnectorId ())
           None None])
let delete_voice_connector_emergency_calling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_emergency_calling_configuration
           (Values.DeleteVoiceConnectorEmergencyCallingConfigurationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_external_systems_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_external_systems_configuration
           (Values.DeleteVoiceConnectorExternalSystemsConfigurationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_group
           (Values.DeleteVoiceConnectorGroupRequest.make
              ~voiceConnectorGroupId ()) None None])
let delete_voice_connector_origination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_origination
           (Values.DeleteVoiceConnectorOriginationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_proxy
           (Values.DeleteVoiceConnectorProxyRequest.make ~voiceConnectorId ())
           None None])
let delete_voice_connector_streaming_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_streaming_configuration
           (Values.DeleteVoiceConnectorStreamingConfigurationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_termination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_termination
           (Values.DeleteVoiceConnectorTerminationRequest.make
              ~voiceConnectorId ()) None None])
let delete_voice_connector_termination_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and usernames =
         flag "usernames" (required json_arg) ~doc:"JSON SensitiveStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_connector_termination_credentials
           (Values.DeleteVoiceConnectorTerminationCredentialsRequest.make
              ~voiceConnectorId
              ~usernames:(Values.SensitiveStringList.of_json usernames) ())
           None None])
let delete_voice_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceProfileId =
         flag "voice-profile-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_profile
           (Values.DeleteVoiceProfileRequest.make ~voiceProfileId ()) None
           None])
let delete_voice_profile_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceProfileDomainId =
         flag "voice-profile-domain-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_profile_domain
           (Values.DeleteVoiceProfileDomainRequest.make ~voiceProfileDomainId
              ()) None None])
let disassociate_phone_numbers_from_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_phone_numbers_from_voice_connector
           (Values.DisassociatePhoneNumbersFromVoiceConnectorRequest.make
              ~voiceConnectorId
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some
              Values.DisassociatePhoneNumbersFromVoiceConnectorResponse.to_json)
           (Some
              Values.DisassociatePhoneNumbersFromVoiceConnectorResponse.error_to_json)])
let disassociate_phone_numbers_from_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString"
       and e164PhoneNumbers =
         flag "e164-phone-numbers" (required json_arg)
           ~doc:"JSON E164PhoneNumberList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_phone_numbers_from_voice_connector_group
           (Values.DisassociatePhoneNumbersFromVoiceConnectorGroupRequest.make
              ~voiceConnectorGroupId
              ~e164PhoneNumbers:(Values.E164PhoneNumberList.of_json
                                   e164PhoneNumbers) ())
           (Some
              Values.DisassociatePhoneNumbersFromVoiceConnectorGroupResponse.to_json)
           (Some
              Values.DisassociatePhoneNumbersFromVoiceConnectorGroupResponse.error_to_json)])
let get_global_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_global_settings (Fn.id ())
           (Some Values.GetGlobalSettingsResponse.to_json)
           (Some Values.GetGlobalSettingsResponse.error_to_json)])
let get_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberId =
         flag "phone-number-id" (required string)
           ~doc:"STRING SensitiveNonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_phone_number
           (Values.GetPhoneNumberRequest.make ~phoneNumberId ())
           (Some Values.GetPhoneNumberResponse.to_json)
           (Some Values.GetPhoneNumberResponse.error_to_json)])
let get_phone_number_order =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberOrderId =
         flag "phone-number-order-id" (required string)
           ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_phone_number_order
           (Values.GetPhoneNumberOrderRequest.make ~phoneNumberOrderId ())
           (Some Values.GetPhoneNumberOrderResponse.to_json)
           (Some Values.GetPhoneNumberOrderResponse.error_to_json)])
let get_phone_number_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_phone_number_settings (Fn.id ())
           (Some Values.GetPhoneNumberSettingsResponse.to_json)
           (Some Values.GetPhoneNumberSettingsResponse.error_to_json)])
let get_proxy_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and proxySessionId =
         flag "proxy-session-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_proxy_session
           (Values.GetProxySessionRequest.make ~voiceConnectorId
              ~proxySessionId ())
           (Some Values.GetProxySessionResponse.to_json)
           (Some Values.GetProxySessionResponse.error_to_json)])
let get_sip_media_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sip_media_application
           (Values.GetSipMediaApplicationRequest.make ~sipMediaApplicationId
              ()) (Some Values.GetSipMediaApplicationResponse.to_json)
           (Some Values.GetSipMediaApplicationResponse.error_to_json)])
let get_sip_media_application_alexa_skill_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sip_media_application_alexa_skill_configuration
           (Values.GetSipMediaApplicationAlexaSkillConfigurationRequest.make
              ~sipMediaApplicationId ())
           (Some
              Values.GetSipMediaApplicationAlexaSkillConfigurationResponse.to_json)
           (Some
              Values.GetSipMediaApplicationAlexaSkillConfigurationResponse.error_to_json)])
let get_sip_media_application_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sip_media_application_logging_configuration
           (Values.GetSipMediaApplicationLoggingConfigurationRequest.make
              ~sipMediaApplicationId ())
           (Some
              Values.GetSipMediaApplicationLoggingConfigurationResponse.to_json)
           (Some
              Values.GetSipMediaApplicationLoggingConfigurationResponse.error_to_json)])
let get_sip_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipRuleId =
         flag "sip-rule-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sip_rule (Values.GetSipRuleRequest.make ~sipRuleId ())
           (Some Values.GetSipRuleResponse.to_json)
           (Some Values.GetSipRuleResponse.error_to_json)])
let get_speaker_search_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and speakerSearchTaskId =
         flag "speaker-search-task-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_speaker_search_task
           (Values.GetSpeakerSearchTaskRequest.make ~voiceConnectorId
              ~speakerSearchTaskId ())
           (Some Values.GetSpeakerSearchTaskResponse.to_json)
           (Some Values.GetSpeakerSearchTaskResponse.error_to_json)])
let get_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector
           (Values.GetVoiceConnectorRequest.make ~voiceConnectorId ())
           (Some Values.GetVoiceConnectorResponse.to_json)
           (Some Values.GetVoiceConnectorResponse.error_to_json)])
let get_voice_connector_emergency_calling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_emergency_calling_configuration
           (Values.GetVoiceConnectorEmergencyCallingConfigurationRequest.make
              ~voiceConnectorId ())
           (Some
              Values.GetVoiceConnectorEmergencyCallingConfigurationResponse.to_json)
           (Some
              Values.GetVoiceConnectorEmergencyCallingConfigurationResponse.error_to_json)])
let get_voice_connector_external_systems_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_external_systems_configuration
           (Values.GetVoiceConnectorExternalSystemsConfigurationRequest.make
              ~voiceConnectorId ())
           (Some
              Values.GetVoiceConnectorExternalSystemsConfigurationResponse.to_json)
           (Some
              Values.GetVoiceConnectorExternalSystemsConfigurationResponse.error_to_json)])
let get_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_group
           (Values.GetVoiceConnectorGroupRequest.make ~voiceConnectorGroupId
              ()) (Some Values.GetVoiceConnectorGroupResponse.to_json)
           (Some Values.GetVoiceConnectorGroupResponse.error_to_json)])
let get_voice_connector_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_logging_configuration
           (Values.GetVoiceConnectorLoggingConfigurationRequest.make
              ~voiceConnectorId ())
           (Some Values.GetVoiceConnectorLoggingConfigurationResponse.to_json)
           (Some
              Values.GetVoiceConnectorLoggingConfigurationResponse.error_to_json)])
let get_voice_connector_origination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_origination
           (Values.GetVoiceConnectorOriginationRequest.make ~voiceConnectorId
              ()) (Some Values.GetVoiceConnectorOriginationResponse.to_json)
           (Some Values.GetVoiceConnectorOriginationResponse.error_to_json)])
let get_voice_connector_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_proxy
           (Values.GetVoiceConnectorProxyRequest.make ~voiceConnectorId ())
           (Some Values.GetVoiceConnectorProxyResponse.to_json)
           (Some Values.GetVoiceConnectorProxyResponse.error_to_json)])
let get_voice_connector_streaming_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_streaming_configuration
           (Values.GetVoiceConnectorStreamingConfigurationRequest.make
              ~voiceConnectorId ())
           (Some
              Values.GetVoiceConnectorStreamingConfigurationResponse.to_json)
           (Some
              Values.GetVoiceConnectorStreamingConfigurationResponse.error_to_json)])
let get_voice_connector_termination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_termination
           (Values.GetVoiceConnectorTerminationRequest.make ~voiceConnectorId
              ()) (Some Values.GetVoiceConnectorTerminationResponse.to_json)
           (Some Values.GetVoiceConnectorTerminationResponse.error_to_json)])
let get_voice_connector_termination_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_connector_termination_health
           (Values.GetVoiceConnectorTerminationHealthRequest.make
              ~voiceConnectorId ())
           (Some Values.GetVoiceConnectorTerminationHealthResponse.to_json)
           (Some
              Values.GetVoiceConnectorTerminationHealthResponse.error_to_json)])
let get_voice_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceProfileId =
         flag "voice-profile-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_profile
           (Values.GetVoiceProfileRequest.make ~voiceProfileId ())
           (Some Values.GetVoiceProfileResponse.to_json)
           (Some Values.GetVoiceProfileResponse.error_to_json)])
let get_voice_profile_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceProfileDomainId =
         flag "voice-profile-domain-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_profile_domain
           (Values.GetVoiceProfileDomainRequest.make ~voiceProfileDomainId ())
           (Some Values.GetVoiceProfileDomainResponse.to_json)
           (Some Values.GetVoiceProfileDomainResponse.error_to_json)])
let get_voice_tone_analysis_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and voiceToneAnalysisTaskId =
         flag "voice-tone-analysis-task-id" (required string)
           ~doc:"STRING NonEmptyString256"
       and isCaller = flag "is-caller" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_voice_tone_analysis_task
           (Values.GetVoiceToneAnalysisTaskRequest.make ~voiceConnectorId
              ~voiceToneAnalysisTaskId ~isCaller ())
           (Some Values.GetVoiceToneAnalysisTaskResponse.to_json)
           (Some Values.GetVoiceToneAnalysisTaskResponse.error_to_json)])
let list_available_voice_connector_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_available_voice_connector_regions (Fn.id ())
           (Some Values.ListAvailableVoiceConnectorRegionsResponse.to_json)
           (Some
              Values.ListAvailableVoiceConnectorRegionsResponse.error_to_json)])
let list_phone_number_orders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_phone_number_orders
           (Values.ListPhoneNumberOrdersRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListPhoneNumberOrdersResponse.to_json)
           (Some Values.ListPhoneNumberOrdersResponse.error_to_json)])
let list_phone_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status = flag "status" (optional string) ~doc:"STRING String"
       and productType =
         flag "product-type" (optional json_arg)
           ~doc:"JSON PhoneNumberProductType"
       and filterName =
         flag "filter-name" (optional json_arg)
           ~doc:"JSON PhoneNumberAssociationName"
       and filterValue =
         flag "filter-value" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_phone_numbers
           (Values.ListPhoneNumbersRequest.make ?status
              ?productType:(Option.map
                              ~f:Values.PhoneNumberProductType.of_json
                              productType)
              ?filterName:(Option.map
                             ~f:Values.PhoneNumberAssociationName.of_json
                             filterName) ?filterValue ?maxResults ?nextToken
              ()) (Some Values.ListPhoneNumbersResponse.to_json)
           (Some Values.ListPhoneNumbersResponse.error_to_json)])
let list_proxy_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ProxySessionStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_proxy_sessions
           (Values.ListProxySessionsRequest.make
              ?status:(Option.map ~f:Values.ProxySessionStatus.of_json status)
              ?nextToken ?maxResults ~voiceConnectorId ())
           (Some Values.ListProxySessionsResponse.to_json)
           (Some Values.ListProxySessionsResponse.error_to_json)])
let list_sip_media_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sip_media_applications
           (Values.ListSipMediaApplicationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListSipMediaApplicationsResponse.to_json)
           (Some Values.ListSipMediaApplicationsResponse.error_to_json)])
let list_sip_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (optional string)
           ~doc:"STRING NonEmptyString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sip_rules
           (Values.ListSipRulesRequest.make ?sipMediaApplicationId
              ?maxResults ?nextToken ())
           (Some Values.ListSipRulesResponse.to_json)
           (Some Values.ListSipRulesResponse.error_to_json)])
let list_supported_phone_number_countries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and productType =
         flag "product-type" (required json_arg)
           ~doc:"JSON PhoneNumberProductType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_supported_phone_number_countries
           (Values.ListSupportedPhoneNumberCountriesRequest.make
              ~productType:(Values.PhoneNumberProductType.of_json productType)
              ())
           (Some Values.ListSupportedPhoneNumberCountriesResponse.to_json)
           (Some
              Values.ListSupportedPhoneNumberCountriesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_voice_connector_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_voice_connector_groups
           (Values.ListVoiceConnectorGroupsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListVoiceConnectorGroupsResponse.to_json)
           (Some Values.ListVoiceConnectorGroupsResponse.error_to_json)])
let list_voice_connector_termination_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_voice_connector_termination_credentials
           (Values.ListVoiceConnectorTerminationCredentialsRequest.make
              ~voiceConnectorId ())
           (Some
              Values.ListVoiceConnectorTerminationCredentialsResponse.to_json)
           (Some
              Values.ListVoiceConnectorTerminationCredentialsResponse.error_to_json)])
let list_voice_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_voice_connectors
           (Values.ListVoiceConnectorsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListVoiceConnectorsResponse.to_json)
           (Some Values.ListVoiceConnectorsResponse.error_to_json)])
let list_voice_profile_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_voice_profile_domains
           (Values.ListVoiceProfileDomainsRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListVoiceProfileDomainsResponse.to_json)
           (Some Values.ListVoiceProfileDomainsResponse.error_to_json)])
let list_voice_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and voiceProfileDomainId =
         flag "voice-profile-domain-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_voice_profiles
           (Values.ListVoiceProfilesRequest.make ?nextToken ?maxResults
              ~voiceProfileDomainId ())
           (Some Values.ListVoiceProfilesResponse.to_json)
           (Some Values.ListVoiceProfilesResponse.error_to_json)])
let put_sip_media_application_alexa_skill_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationAlexaSkillConfiguration =
         flag "sip-media-application-alexa-skill-configuration"
           (optional json_arg)
           ~doc:"JSON SipMediaApplicationAlexaSkillConfiguration"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_sip_media_application_alexa_skill_configuration
           (Values.PutSipMediaApplicationAlexaSkillConfigurationRequest.make
              ?sipMediaApplicationAlexaSkillConfiguration:(Option.map
                                                             ~f:Values.SipMediaApplicationAlexaSkillConfiguration.of_json
                                                             sipMediaApplicationAlexaSkillConfiguration)
              ~sipMediaApplicationId ())
           (Some
              Values.PutSipMediaApplicationAlexaSkillConfigurationResponse.to_json)
           (Some
              Values.PutSipMediaApplicationAlexaSkillConfigurationResponse.error_to_json)])
let put_sip_media_application_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationLoggingConfiguration =
         flag "sip-media-application-logging-configuration"
           (optional json_arg)
           ~doc:"JSON SipMediaApplicationLoggingConfiguration"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_sip_media_application_logging_configuration
           (Values.PutSipMediaApplicationLoggingConfigurationRequest.make
              ?sipMediaApplicationLoggingConfiguration:(Option.map
                                                          ~f:Values.SipMediaApplicationLoggingConfiguration.of_json
                                                          sipMediaApplicationLoggingConfiguration)
              ~sipMediaApplicationId ())
           (Some
              Values.PutSipMediaApplicationLoggingConfigurationResponse.to_json)
           (Some
              Values.PutSipMediaApplicationLoggingConfigurationResponse.error_to_json)])
let put_voice_connector_emergency_calling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and emergencyCallingConfiguration =
         flag "emergency-calling-configuration" (required json_arg)
           ~doc:"JSON EmergencyCallingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_emergency_calling_configuration
           (Values.PutVoiceConnectorEmergencyCallingConfigurationRequest.make
              ~voiceConnectorId
              ~emergencyCallingConfiguration:(Values.EmergencyCallingConfiguration.of_json
                                                emergencyCallingConfiguration)
              ())
           (Some
              Values.PutVoiceConnectorEmergencyCallingConfigurationResponse.to_json)
           (Some
              Values.PutVoiceConnectorEmergencyCallingConfigurationResponse.error_to_json)])
let put_voice_connector_external_systems_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionBorderControllerTypes =
         flag "session-border-controller-types" (optional json_arg)
           ~doc:"JSON SessionBorderControllerTypeList"
       and contactCenterSystemTypes =
         flag "contact-center-system-types" (optional json_arg)
           ~doc:"JSON ContactCenterSystemTypeList"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_external_systems_configuration
           (Values.PutVoiceConnectorExternalSystemsConfigurationRequest.make
              ?sessionBorderControllerTypes:(Option.map
                                               ~f:Values.SessionBorderControllerTypeList.of_json
                                               sessionBorderControllerTypes)
              ?contactCenterSystemTypes:(Option.map
                                           ~f:Values.ContactCenterSystemTypeList.of_json
                                           contactCenterSystemTypes)
              ~voiceConnectorId ())
           (Some
              Values.PutVoiceConnectorExternalSystemsConfigurationResponse.to_json)
           (Some
              Values.PutVoiceConnectorExternalSystemsConfigurationResponse.error_to_json)])
let put_voice_connector_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and loggingConfiguration =
         flag "logging-configuration" (required json_arg)
           ~doc:"JSON LoggingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_logging_configuration
           (Values.PutVoiceConnectorLoggingConfigurationRequest.make
              ~voiceConnectorId
              ~loggingConfiguration:(Values.LoggingConfiguration.of_json
                                       loggingConfiguration) ())
           (Some Values.PutVoiceConnectorLoggingConfigurationResponse.to_json)
           (Some
              Values.PutVoiceConnectorLoggingConfigurationResponse.error_to_json)])
let put_voice_connector_origination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and origination =
         flag "origination" (required json_arg) ~doc:"JSON Origination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_origination
           (Values.PutVoiceConnectorOriginationRequest.make ~voiceConnectorId
              ~origination:(Values.Origination.of_json origination) ())
           (Some Values.PutVoiceConnectorOriginationResponse.to_json)
           (Some Values.PutVoiceConnectorOriginationResponse.error_to_json)])
let put_voice_connector_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fallBackPhoneNumber =
         flag "fall-back-phone-number" (optional string)
           ~doc:"STRING E164PhoneNumber"
       and disabled = flag "disabled" (optional bool) ~doc:"BOOL Boolean"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and defaultSessionExpiryMinutes =
         flag "default-session-expiry-minutes" (required int)
           ~doc:"INT Integer"
       and phoneNumberPoolCountries =
         flag "phone-number-pool-countries" (required json_arg)
           ~doc:"JSON CountryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_proxy
           (Values.PutVoiceConnectorProxyRequest.make ?fallBackPhoneNumber
              ?disabled ~voiceConnectorId ~defaultSessionExpiryMinutes
              ~phoneNumberPoolCountries:(Values.CountryList.of_json
                                           phoneNumberPoolCountries) ())
           (Some Values.PutVoiceConnectorProxyResponse.to_json)
           (Some Values.PutVoiceConnectorProxyResponse.error_to_json)])
let put_voice_connector_streaming_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and streamingConfiguration =
         flag "streaming-configuration" (required json_arg)
           ~doc:"JSON StreamingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_streaming_configuration
           (Values.PutVoiceConnectorStreamingConfigurationRequest.make
              ~voiceConnectorId
              ~streamingConfiguration:(Values.StreamingConfiguration.of_json
                                         streamingConfiguration) ())
           (Some
              Values.PutVoiceConnectorStreamingConfigurationResponse.to_json)
           (Some
              Values.PutVoiceConnectorStreamingConfigurationResponse.error_to_json)])
let put_voice_connector_termination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and termination =
         flag "termination" (required json_arg) ~doc:"JSON Termination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_termination
           (Values.PutVoiceConnectorTerminationRequest.make ~voiceConnectorId
              ~termination:(Values.Termination.of_json termination) ())
           (Some Values.PutVoiceConnectorTerminationResponse.to_json)
           (Some Values.PutVoiceConnectorTerminationResponse.error_to_json)])
let put_voice_connector_termination_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and credentials =
         flag "credentials" (optional json_arg) ~doc:"JSON CredentialList"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_voice_connector_termination_credentials
           (Values.PutVoiceConnectorTerminationCredentialsRequest.make
              ?credentials:(Option.map ~f:Values.CredentialList.of_json
                              credentials) ~voiceConnectorId ()) None None])
let restore_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberId =
         flag "phone-number-id" (required string)
           ~doc:"STRING SensitiveNonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_phone_number
           (Values.RestorePhoneNumberRequest.make ~phoneNumberId ())
           (Some Values.RestorePhoneNumberResponse.to_json)
           (Some Values.RestorePhoneNumberResponse.error_to_json)])
let search_available_phone_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and areaCode = flag "area-code" (optional string) ~doc:"STRING String"
       and city = flag "city" (optional string) ~doc:"STRING String"
       and country =
         flag "country" (optional string) ~doc:"STRING Alpha2CountryCode"
       and state = flag "state" (optional string) ~doc:"STRING String"
       and tollFreePrefix =
         flag "toll-free-prefix" (optional string)
           ~doc:"STRING TollFreePrefix"
       and phoneNumberType =
         flag "phone-number-type" (optional json_arg)
           ~doc:"JSON PhoneNumberType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PhoneNumberMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_available_phone_numbers
           (Values.SearchAvailablePhoneNumbersRequest.make ?areaCode ?city
              ?country ?state ?tollFreePrefix
              ?phoneNumberType:(Option.map ~f:Values.PhoneNumberType.of_json
                                  phoneNumberType) ?maxResults ?nextToken ())
           (Some Values.SearchAvailablePhoneNumbersResponse.to_json)
           (Some Values.SearchAvailablePhoneNumbersResponse.error_to_json)])
let start_speaker_search_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestId"
       and callLeg =
         flag "call-leg" (optional json_arg) ~doc:"JSON CallLegType"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and transactionId =
         flag "transaction-id" (required string)
           ~doc:"STRING NonEmptyString256"
       and voiceProfileDomainId =
         flag "voice-profile-domain-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_speaker_search_task
           (Values.StartSpeakerSearchTaskRequest.make ?clientRequestToken
              ?callLeg:(Option.map ~f:Values.CallLegType.of_json callLeg)
              ~voiceConnectorId ~transactionId ~voiceProfileDomainId ())
           (Some Values.StartSpeakerSearchTaskResponse.to_json)
           (Some Values.StartSpeakerSearchTaskResponse.error_to_json)])
let start_voice_tone_analysis_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestId"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and transactionId =
         flag "transaction-id" (required string)
           ~doc:"STRING NonEmptyString256"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_voice_tone_analysis_task
           (Values.StartVoiceToneAnalysisTaskRequest.make ?clientRequestToken
              ~voiceConnectorId ~transactionId
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartVoiceToneAnalysisTaskResponse.to_json)
           (Some Values.StartVoiceToneAnalysisTaskResponse.error_to_json)])
let stop_speaker_search_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and speakerSearchTaskId =
         flag "speaker-search-task-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_speaker_search_task
           (Values.StopSpeakerSearchTaskRequest.make ~voiceConnectorId
              ~speakerSearchTaskId ()) None None])
let stop_voice_tone_analysis_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and voiceToneAnalysisTaskId =
         flag "voice-tone-analysis-task-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_voice_tone_analysis_task
           (Values.StopVoiceToneAnalysisTaskRequest.make ~voiceConnectorId
              ~voiceToneAnalysisTaskId ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_global_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnector =
         flag "voice-connector" (optional json_arg)
           ~doc:"JSON VoiceConnectorSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_global_settings
           (Values.UpdateGlobalSettingsRequest.make
              ?voiceConnector:(Option.map
                                 ~f:Values.VoiceConnectorSettings.of_json
                                 voiceConnector) ()) None None])
let update_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and productType =
         flag "product-type" (optional json_arg)
           ~doc:"JSON PhoneNumberProductType"
       and callingName =
         flag "calling-name" (optional string) ~doc:"STRING CallingName"
       and name = flag "name" (optional string) ~doc:"STRING PhoneNumberName"
       and phoneNumberId =
         flag "phone-number-id" (required string)
           ~doc:"STRING SensitiveNonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_phone_number
           (Values.UpdatePhoneNumberRequest.make
              ?productType:(Option.map
                              ~f:Values.PhoneNumberProductType.of_json
                              productType) ?callingName ?name ~phoneNumberId
              ()) (Some Values.UpdatePhoneNumberResponse.to_json)
           (Some Values.UpdatePhoneNumberResponse.error_to_json)])
let update_phone_number_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callingName =
         flag "calling-name" (required string) ~doc:"STRING CallingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_phone_number_settings
           (Values.UpdatePhoneNumberSettingsRequest.make ~callingName ())
           None None])
let update_proxy_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expiryMinutes =
         flag "expiry-minutes" (optional int) ~doc:"INT PositiveInteger"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and proxySessionId =
         flag "proxy-session-id" (required string)
           ~doc:"STRING NonEmptyString128"
       and capabilities =
         flag "capabilities" (required json_arg) ~doc:"JSON CapabilityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_proxy_session
           (Values.UpdateProxySessionRequest.make ?expiryMinutes
              ~voiceConnectorId ~proxySessionId
              ~capabilities:(Values.CapabilityList.of_json capabilities) ())
           (Some Values.UpdateProxySessionResponse.to_json)
           (Some Values.UpdateProxySessionResponse.error_to_json)])
let update_sip_media_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING SipMediaApplicationName"
       and endpoints =
         flag "endpoints" (optional json_arg)
           ~doc:"JSON SipMediaApplicationEndpointList"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sip_media_application
           (Values.UpdateSipMediaApplicationRequest.make ?name
              ?endpoints:(Option.map
                            ~f:Values.SipMediaApplicationEndpointList.of_json
                            endpoints) ~sipMediaApplicationId ())
           (Some Values.UpdateSipMediaApplicationResponse.to_json)
           (Some Values.UpdateSipMediaApplicationResponse.error_to_json)])
let update_sip_media_application_call =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sipMediaApplicationId =
         flag "sip-media-application-id" (required string)
           ~doc:"STRING NonEmptyString"
       and transactionId =
         flag "transaction-id" (required string) ~doc:"STRING NonEmptyString"
       and arguments =
         flag "arguments" (required json_arg)
           ~doc:"JSON SMAUpdateCallArgumentsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sip_media_application_call
           (Values.UpdateSipMediaApplicationCallRequest.make
              ~sipMediaApplicationId ~transactionId
              ~arguments:(Values.SMAUpdateCallArgumentsMap.of_json arguments)
              ()) (Some Values.UpdateSipMediaApplicationCallResponse.to_json)
           (Some Values.UpdateSipMediaApplicationCallResponse.error_to_json)])
let update_sip_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and disabled =
         flag "disabled" (optional bool) ~doc:"BOOL NullableBoolean"
       and targetApplications =
         flag "target-applications" (optional json_arg)
           ~doc:"JSON SipRuleTargetApplicationList"
       and sipRuleId =
         flag "sip-rule-id" (required string) ~doc:"STRING NonEmptyString"
       and name = flag "name" (required string) ~doc:"STRING SipRuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sip_rule
           (Values.UpdateSipRuleRequest.make ?disabled
              ?targetApplications:(Option.map
                                     ~f:Values.SipRuleTargetApplicationList.of_json
                                     targetApplications) ~sipRuleId ~name ())
           (Some Values.UpdateSipRuleResponse.to_json)
           (Some Values.UpdateSipRuleResponse.error_to_json)])
let update_voice_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorId =
         flag "voice-connector-id" (required string)
           ~doc:"STRING NonEmptyString"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceConnectorName"
       and requireEncryption =
         flag "require-encryption" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_voice_connector
           (Values.UpdateVoiceConnectorRequest.make ~voiceConnectorId ~name
              ~requireEncryption ())
           (Some Values.UpdateVoiceConnectorResponse.to_json)
           (Some Values.UpdateVoiceConnectorResponse.error_to_json)])
let update_voice_connector_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceConnectorGroupId =
         flag "voice-connector-group-id" (required string)
           ~doc:"STRING NonEmptyString"
       and name =
         flag "name" (required string) ~doc:"STRING VoiceConnectorGroupName"
       and voiceConnectorItems =
         flag "voice-connector-items" (required json_arg)
           ~doc:"JSON VoiceConnectorItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_voice_connector_group
           (Values.UpdateVoiceConnectorGroupRequest.make
              ~voiceConnectorGroupId ~name
              ~voiceConnectorItems:(Values.VoiceConnectorItemList.of_json
                                      voiceConnectorItems) ())
           (Some Values.UpdateVoiceConnectorGroupResponse.to_json)
           (Some Values.UpdateVoiceConnectorGroupResponse.error_to_json)])
let update_voice_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and voiceProfileId =
         flag "voice-profile-id" (required string)
           ~doc:"STRING NonEmptyString256"
       and speakerSearchTaskId =
         flag "speaker-search-task-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_voice_profile
           (Values.UpdateVoiceProfileRequest.make ~voiceProfileId
              ~speakerSearchTaskId ())
           (Some Values.UpdateVoiceProfileResponse.to_json)
           (Some Values.UpdateVoiceProfileResponse.error_to_json)])
let update_voice_profile_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING VoiceProfileDomainName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING VoiceProfileDomainDescription"
       and voiceProfileDomainId =
         flag "voice-profile-domain-id" (required string)
           ~doc:"STRING NonEmptyString256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_voice_profile_domain
           (Values.UpdateVoiceProfileDomainRequest.make ?name ?description
              ~voiceProfileDomainId ())
           (Some Values.UpdateVoiceProfileDomainResponse.to_json)
           (Some Values.UpdateVoiceProfileDomainResponse.error_to_json)])
let validate_e911_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and awsAccountId =
         flag "aws-account-id" (required string) ~doc:"STRING NonEmptyString"
       and streetNumber =
         flag "street-number" (required string)
           ~doc:"STRING SensitiveNonEmptyString"
       and streetInfo =
         flag "street-info" (required string)
           ~doc:"STRING SensitiveNonEmptyString"
       and city =
         flag "city" (required string) ~doc:"STRING SensitiveNonEmptyString"
       and state =
         flag "state" (required string) ~doc:"STRING SensitiveNonEmptyString"
       and country =
         flag "country" (required string)
           ~doc:"STRING SensitiveNonEmptyString"
       and postalCode =
         flag "postal-code" (required string)
           ~doc:"STRING SensitiveNonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_e911_address
           (Values.ValidateE911AddressRequest.make ~awsAccountId
              ~streetNumber ~streetInfo ~city ~state ~country ~postalCode ())
           (Some Values.ValidateE911AddressResponse.to_json)
           (Some Values.ValidateE911AddressResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-phone-numbers-with-voice-connector",
       associate_phone_numbers_with_voice_connector);
    ("associate-phone-numbers-with-voice-connector-group",
      associate_phone_numbers_with_voice_connector_group);
    ("batch-delete-phone-number", batch_delete_phone_number);
    ("batch-update-phone-number", batch_update_phone_number);
    ("create-phone-number-order", create_phone_number_order);
    ("create-proxy-session", create_proxy_session);
    ("create-sip-media-application", create_sip_media_application);
    ("create-sip-media-application-call", create_sip_media_application_call);
    ("create-sip-rule", create_sip_rule);
    ("create-voice-connector", create_voice_connector);
    ("create-voice-connector-group", create_voice_connector_group);
    ("create-voice-profile", create_voice_profile);
    ("create-voice-profile-domain", create_voice_profile_domain);
    ("delete-phone-number", delete_phone_number);
    ("delete-proxy-session", delete_proxy_session);
    ("delete-sip-media-application", delete_sip_media_application);
    ("delete-sip-rule", delete_sip_rule);
    ("delete-voice-connector", delete_voice_connector);
    ("delete-voice-connector-emergency-calling-configuration",
      delete_voice_connector_emergency_calling_configuration);
    ("delete-voice-connector-external-systems-configuration",
      delete_voice_connector_external_systems_configuration);
    ("delete-voice-connector-group", delete_voice_connector_group);
    ("delete-voice-connector-origination",
      delete_voice_connector_origination);
    ("delete-voice-connector-proxy", delete_voice_connector_proxy);
    ("delete-voice-connector-streaming-configuration",
      delete_voice_connector_streaming_configuration);
    ("delete-voice-connector-termination",
      delete_voice_connector_termination);
    ("delete-voice-connector-termination-credentials",
      delete_voice_connector_termination_credentials);
    ("delete-voice-profile", delete_voice_profile);
    ("delete-voice-profile-domain", delete_voice_profile_domain);
    ("disassociate-phone-numbers-from-voice-connector",
      disassociate_phone_numbers_from_voice_connector);
    ("disassociate-phone-numbers-from-voice-connector-group",
      disassociate_phone_numbers_from_voice_connector_group);
    ("get-global-settings", get_global_settings);
    ("get-phone-number", get_phone_number);
    ("get-phone-number-order", get_phone_number_order);
    ("get-phone-number-settings", get_phone_number_settings);
    ("get-proxy-session", get_proxy_session);
    ("get-sip-media-application", get_sip_media_application);
    ("get-sip-media-application-alexa-skill-configuration",
      get_sip_media_application_alexa_skill_configuration);
    ("get-sip-media-application-logging-configuration",
      get_sip_media_application_logging_configuration);
    ("get-sip-rule", get_sip_rule);
    ("get-speaker-search-task", get_speaker_search_task);
    ("get-voice-connector", get_voice_connector);
    ("get-voice-connector-emergency-calling-configuration",
      get_voice_connector_emergency_calling_configuration);
    ("get-voice-connector-external-systems-configuration",
      get_voice_connector_external_systems_configuration);
    ("get-voice-connector-group", get_voice_connector_group);
    ("get-voice-connector-logging-configuration",
      get_voice_connector_logging_configuration);
    ("get-voice-connector-origination", get_voice_connector_origination);
    ("get-voice-connector-proxy", get_voice_connector_proxy);
    ("get-voice-connector-streaming-configuration",
      get_voice_connector_streaming_configuration);
    ("get-voice-connector-termination", get_voice_connector_termination);
    ("get-voice-connector-termination-health",
      get_voice_connector_termination_health);
    ("get-voice-profile", get_voice_profile);
    ("get-voice-profile-domain", get_voice_profile_domain);
    ("get-voice-tone-analysis-task", get_voice_tone_analysis_task);
    ("list-available-voice-connector-regions",
      list_available_voice_connector_regions);
    ("list-phone-number-orders", list_phone_number_orders);
    ("list-phone-numbers", list_phone_numbers);
    ("list-proxy-sessions", list_proxy_sessions);
    ("list-sip-media-applications", list_sip_media_applications);
    ("list-sip-rules", list_sip_rules);
    ("list-supported-phone-number-countries",
      list_supported_phone_number_countries);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-voice-connector-groups", list_voice_connector_groups);
    ("list-voice-connector-termination-credentials",
      list_voice_connector_termination_credentials);
    ("list-voice-connectors", list_voice_connectors);
    ("list-voice-profile-domains", list_voice_profile_domains);
    ("list-voice-profiles", list_voice_profiles);
    ("put-sip-media-application-alexa-skill-configuration",
      put_sip_media_application_alexa_skill_configuration);
    ("put-sip-media-application-logging-configuration",
      put_sip_media_application_logging_configuration);
    ("put-voice-connector-emergency-calling-configuration",
      put_voice_connector_emergency_calling_configuration);
    ("put-voice-connector-external-systems-configuration",
      put_voice_connector_external_systems_configuration);
    ("put-voice-connector-logging-configuration",
      put_voice_connector_logging_configuration);
    ("put-voice-connector-origination", put_voice_connector_origination);
    ("put-voice-connector-proxy", put_voice_connector_proxy);
    ("put-voice-connector-streaming-configuration",
      put_voice_connector_streaming_configuration);
    ("put-voice-connector-termination", put_voice_connector_termination);
    ("put-voice-connector-termination-credentials",
      put_voice_connector_termination_credentials);
    ("restore-phone-number", restore_phone_number);
    ("search-available-phone-numbers", search_available_phone_numbers);
    ("start-speaker-search-task", start_speaker_search_task);
    ("start-voice-tone-analysis-task", start_voice_tone_analysis_task);
    ("stop-speaker-search-task", stop_speaker_search_task);
    ("stop-voice-tone-analysis-task", stop_voice_tone_analysis_task);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-global-settings", update_global_settings);
    ("update-phone-number", update_phone_number);
    ("update-phone-number-settings", update_phone_number_settings);
    ("update-proxy-session", update_proxy_session);
    ("update-sip-media-application", update_sip_media_application);
    ("update-sip-media-application-call", update_sip_media_application_call);
    ("update-sip-rule", update_sip_rule);
    ("update-voice-connector", update_voice_connector);
    ("update-voice-connector-group", update_voice_connector_group);
    ("update-voice-profile", update_voice_profile);
    ("update-voice-profile-domain", update_voice_profile_domain);
    ("validate-e911-address", validate_e911_address)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_origination_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and poolId =
         flag "pool-id" (required string) ~doc:"STRING PoolIdOrArn"
       and originationIdentity =
         flag "origination-identity" (required string)
           ~doc:"STRING PhoneOrSenderIdOrArn"
       and isoCountryCode =
         flag "iso-country-code" (required string)
           ~doc:"STRING IsoCountryCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_origination_identity
           (Values.AssociateOriginationIdentityRequest.make ?clientToken
              ~poolId ~originationIdentity ~isoCountryCode ())
           (Some Values.AssociateOriginationIdentityResult.to_json)
           (Some Values.AssociateOriginationIdentityResult.error_to_json)])
let create_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_set
           (Values.CreateConfigurationSetRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~configurationSetName ())
           (Some Values.CreateConfigurationSetResult.to_json)
           (Some Values.CreateConfigurationSetResult.error_to_json)])
let create_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cloudWatchLogsDestination =
         flag "cloud-watch-logs-destination" (optional json_arg)
           ~doc:"JSON CloudWatchLogsDestination"
       and kinesisFirehoseDestination =
         flag "kinesis-firehose-destination" (optional json_arg)
           ~doc:"JSON KinesisFirehoseDestination"
       and snsDestination =
         flag "sns-destination" (optional json_arg)
           ~doc:"JSON SnsDestination"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetNameOrArn"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName"
       and matchingEventTypes =
         flag "matching-event-types" (required json_arg)
           ~doc:"JSON EventTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_destination
           (Values.CreateEventDestinationRequest.make
              ?cloudWatchLogsDestination:(Option.map
                                            ~f:Values.CloudWatchLogsDestination.of_json
                                            cloudWatchLogsDestination)
              ?kinesisFirehoseDestination:(Option.map
                                             ~f:Values.KinesisFirehoseDestination.of_json
                                             kinesisFirehoseDestination)
              ?snsDestination:(Option.map ~f:Values.SnsDestination.of_json
                                 snsDestination) ?clientToken
              ~configurationSetName ~eventDestinationName
              ~matchingEventTypes:(Values.EventTypeList.of_json
                                     matchingEventTypes) ())
           (Some Values.CreateEventDestinationResult.to_json)
           (Some Values.CreateEventDestinationResult.error_to_json)])
let create_opt_out_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and optOutListName =
         flag "opt-out-list-name" (required string)
           ~doc:"STRING OptOutListName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_opt_out_list
           (Values.CreateOptOutListRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~optOutListName ())
           (Some Values.CreateOptOutListResult.to_json)
           (Some Values.CreateOptOutListResult.error_to_json)])
let create_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deletionProtectionEnabled =
         flag "deletion-protection-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and originationIdentity =
         flag "origination-identity" (required string)
           ~doc:"STRING PhoneOrSenderIdOrArn"
       and isoCountryCode =
         flag "iso-country-code" (required string)
           ~doc:"STRING IsoCountryCode"
       and messageType =
         flag "message-type" (required json_arg) ~doc:"JSON MessageType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pool
           (Values.CreatePoolRequest.make ?deletionProtectionEnabled
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~originationIdentity ~isoCountryCode
              ~messageType:(Values.MessageType.of_json messageType) ())
           (Some Values.CreatePoolResult.to_json)
           (Some Values.CreatePoolResult.error_to_json)])
let delete_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_set
           (Values.DeleteConfigurationSetRequest.make ~configurationSetName
              ()) (Some Values.DeleteConfigurationSetResult.to_json)
           (Some Values.DeleteConfigurationSetResult.error_to_json)])
let delete_default_message_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_default_message_type
           (Values.DeleteDefaultMessageTypeRequest.make ~configurationSetName
              ()) (Some Values.DeleteDefaultMessageTypeResult.to_json)
           (Some Values.DeleteDefaultMessageTypeResult.error_to_json)])
let delete_default_sender_id =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_default_sender_id
           (Values.DeleteDefaultSenderIdRequest.make ~configurationSetName ())
           (Some Values.DeleteDefaultSenderIdResult.to_json)
           (Some Values.DeleteDefaultSenderIdResult.error_to_json)])
let delete_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetNameOrArn"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_destination
           (Values.DeleteEventDestinationRequest.make ~configurationSetName
              ~eventDestinationName ())
           (Some Values.DeleteEventDestinationResult.to_json)
           (Some Values.DeleteEventDestinationResult.error_to_json)])
let delete_keyword =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and originationIdentity =
         flag "origination-identity" (required string)
           ~doc:"STRING PhoneOrPoolIdOrArn"
       and keyword = flag "keyword" (required string) ~doc:"STRING Keyword" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_keyword
           (Values.DeleteKeywordRequest.make ~originationIdentity ~keyword ())
           (Some Values.DeleteKeywordResult.to_json)
           (Some Values.DeleteKeywordResult.error_to_json)])
let delete_opt_out_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optOutListName =
         flag "opt-out-list-name" (required string)
           ~doc:"STRING OptOutListNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_opt_out_list
           (Values.DeleteOptOutListRequest.make ~optOutListName ())
           (Some Values.DeleteOptOutListResult.to_json)
           (Some Values.DeleteOptOutListResult.error_to_json)])
let delete_opted_out_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optOutListName =
         flag "opt-out-list-name" (required string)
           ~doc:"STRING OptOutListNameOrArn"
       and optedOutNumber =
         flag "opted-out-number" (required string) ~doc:"STRING PhoneNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_opted_out_number
           (Values.DeleteOptedOutNumberRequest.make ~optOutListName
              ~optedOutNumber ())
           (Some Values.DeleteOptedOutNumberResult.to_json)
           (Some Values.DeleteOptedOutNumberResult.error_to_json)])
let delete_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolId =
         flag "pool-id" (required string) ~doc:"STRING PoolIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pool (Values.DeletePoolRequest.make ~poolId ())
           (Some Values.DeletePoolResult.to_json)
           (Some Values.DeletePoolResult.error_to_json)])
let delete_text_message_spend_limit_override =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_text_message_spend_limit_override
           (Values.DeleteTextMessageSpendLimitOverrideRequest.make ())
           (Some Values.DeleteTextMessageSpendLimitOverrideResult.to_json)
           (Some
              Values.DeleteTextMessageSpendLimitOverrideResult.error_to_json)])
let delete_voice_message_spend_limit_override =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_voice_message_spend_limit_override
           (Values.DeleteVoiceMessageSpendLimitOverrideRequest.make ())
           (Some Values.DeleteVoiceMessageSpendLimitOverrideResult.to_json)
           (Some
              Values.DeleteVoiceMessageSpendLimitOverrideResult.error_to_json)])
let describe_account_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_attributes
           (Values.DescribeAccountAttributesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.DescribeAccountAttributesResult.to_json)
           (Some Values.DescribeAccountAttributesResult.error_to_json)])
let describe_account_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_limits
           (Values.DescribeAccountLimitsRequest.make ?nextToken ?maxResults
              ()) (Some Values.DescribeAccountLimitsResult.to_json)
           (Some Values.DescribeAccountLimitsResult.error_to_json)])
let describe_configuration_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetNames =
         flag "configuration-set-names" (optional json_arg)
           ~doc:"JSON ConfigurationSetNameList"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ConfigurationSetFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configuration_sets
           (Values.DescribeConfigurationSetsRequest.make
              ?configurationSetNames:(Option.map
                                        ~f:Values.ConfigurationSetNameList.of_json
                                        configurationSetNames)
              ?filters:(Option.map
                          ~f:Values.ConfigurationSetFilterList.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.DescribeConfigurationSetsResult.to_json)
           (Some Values.DescribeConfigurationSetsResult.error_to_json)])
let describe_keywords =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keywords =
         flag "keywords" (optional json_arg) ~doc:"JSON KeywordList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON KeywordFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and originationIdentity =
         flag "origination-identity" (required string)
           ~doc:"STRING PhoneOrPoolIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_keywords
           (Values.DescribeKeywordsRequest.make
              ?keywords:(Option.map ~f:Values.KeywordList.of_json keywords)
              ?filters:(Option.map ~f:Values.KeywordFilterList.of_json
                          filters) ?nextToken ?maxResults
              ~originationIdentity ())
           (Some Values.DescribeKeywordsResult.to_json)
           (Some Values.DescribeKeywordsResult.error_to_json)])
let describe_opt_out_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optOutListNames =
         flag "opt-out-list-names" (optional json_arg)
           ~doc:"JSON OptOutListNameList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_opt_out_lists
           (Values.DescribeOptOutListsRequest.make
              ?optOutListNames:(Option.map
                                  ~f:Values.OptOutListNameList.of_json
                                  optOutListNames) ?nextToken ?maxResults ())
           (Some Values.DescribeOptOutListsResult.to_json)
           (Some Values.DescribeOptOutListsResult.error_to_json)])
let describe_opted_out_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optedOutNumbers =
         flag "opted-out-numbers" (optional json_arg)
           ~doc:"JSON OptedOutNumberList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON OptedOutFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and optOutListName =
         flag "opt-out-list-name" (required string)
           ~doc:"STRING OptOutListNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_opted_out_numbers
           (Values.DescribeOptedOutNumbersRequest.make
              ?optedOutNumbers:(Option.map
                                  ~f:Values.OptedOutNumberList.of_json
                                  optedOutNumbers)
              ?filters:(Option.map ~f:Values.OptedOutFilterList.of_json
                          filters) ?nextToken ?maxResults ~optOutListName ())
           (Some Values.DescribeOptedOutNumbersResult.to_json)
           (Some Values.DescribeOptedOutNumbersResult.error_to_json)])
let describe_phone_numbers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberIds =
         flag "phone-number-ids" (optional json_arg)
           ~doc:"JSON PhoneNumberIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON PhoneNumberFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_phone_numbers
           (Values.DescribePhoneNumbersRequest.make
              ?phoneNumberIds:(Option.map ~f:Values.PhoneNumberIdList.of_json
                                 phoneNumberIds)
              ?filters:(Option.map ~f:Values.PhoneNumberFilterList.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.DescribePhoneNumbersResult.to_json)
           (Some Values.DescribePhoneNumbersResult.error_to_json)])
let describe_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolIds =
         flag "pool-ids" (optional json_arg) ~doc:"JSON PoolIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON PoolFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pools
           (Values.DescribePoolsRequest.make
              ?poolIds:(Option.map ~f:Values.PoolIdList.of_json poolIds)
              ?filters:(Option.map ~f:Values.PoolFilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.DescribePoolsResult.to_json)
           (Some Values.DescribePoolsResult.error_to_json)])
let describe_sender_ids =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and senderIds =
         flag "sender-ids" (optional json_arg) ~doc:"JSON SenderIdList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SenderIdFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_sender_ids
           (Values.DescribeSenderIdsRequest.make
              ?senderIds:(Option.map ~f:Values.SenderIdList.of_json senderIds)
              ?filters:(Option.map ~f:Values.SenderIdFilterList.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.DescribeSenderIdsResult.to_json)
           (Some Values.DescribeSenderIdsResult.error_to_json)])
let describe_spend_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_spend_limits
           (Values.DescribeSpendLimitsRequest.make ?nextToken ?maxResults ())
           (Some Values.DescribeSpendLimitsResult.to_json)
           (Some Values.DescribeSpendLimitsResult.error_to_json)])
let disassociate_origination_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and poolId =
         flag "pool-id" (required string) ~doc:"STRING PoolIdOrArn"
       and originationIdentity =
         flag "origination-identity" (required string)
           ~doc:"STRING PhoneOrSenderIdOrArn"
       and isoCountryCode =
         flag "iso-country-code" (required string)
           ~doc:"STRING IsoCountryCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_origination_identity
           (Values.DisassociateOriginationIdentityRequest.make ?clientToken
              ~poolId ~originationIdentity ~isoCountryCode ())
           (Some Values.DisassociateOriginationIdentityResult.to_json)
           (Some Values.DisassociateOriginationIdentityResult.error_to_json)])
let list_pool_origination_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON PoolOriginationIdentitiesFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and poolId =
         flag "pool-id" (required string) ~doc:"STRING PoolIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pool_origination_identities
           (Values.ListPoolOriginationIdentitiesRequest.make
              ?filters:(Option.map
                          ~f:Values.PoolOriginationIdentitiesFilterList.of_json
                          filters) ?nextToken ?maxResults ~poolId ())
           (Some Values.ListPoolOriginationIdentitiesResult.to_json)
           (Some Values.ListPoolOriginationIdentitiesResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let put_keyword =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keywordAction =
         flag "keyword-action" (optional json_arg) ~doc:"JSON KeywordAction"
       and originationIdentity =
         flag "origination-identity" (required string)
           ~doc:"STRING PhoneOrPoolIdOrArn"
       and keyword = flag "keyword" (required string) ~doc:"STRING Keyword"
       and keywordMessage =
         flag "keyword-message" (required string)
           ~doc:"STRING KeywordMessage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_keyword
           (Values.PutKeywordRequest.make
              ?keywordAction:(Option.map ~f:Values.KeywordAction.of_json
                                keywordAction) ~originationIdentity ~keyword
              ~keywordMessage ()) (Some Values.PutKeywordResult.to_json)
           (Some Values.PutKeywordResult.error_to_json)])
let put_opted_out_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optOutListName =
         flag "opt-out-list-name" (required string)
           ~doc:"STRING OptOutListNameOrArn"
       and optedOutNumber =
         flag "opted-out-number" (required string) ~doc:"STRING PhoneNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_opted_out_number
           (Values.PutOptedOutNumberRequest.make ~optOutListName
              ~optedOutNumber ())
           (Some Values.PutOptedOutNumberResult.to_json)
           (Some Values.PutOptedOutNumberResult.error_to_json)])
let release_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phoneNumberId =
         flag "phone-number-id" (required string)
           ~doc:"STRING PhoneNumberIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.release_phone_number
           (Values.ReleasePhoneNumberRequest.make ~phoneNumberId ())
           (Some Values.ReleasePhoneNumberResult.to_json)
           (Some Values.ReleasePhoneNumberResult.error_to_json)])
let request_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optOutListName =
         flag "opt-out-list-name" (optional string)
           ~doc:"STRING OptOutListNameOrArn"
       and poolId =
         flag "pool-id" (optional string) ~doc:"STRING PoolIdOrArn"
       and registrationId =
         flag "registration-id" (optional string)
           ~doc:"STRING RegistrationId"
       and deletionProtectionEnabled =
         flag "deletion-protection-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and isoCountryCode =
         flag "iso-country-code" (required string)
           ~doc:"STRING IsoCountryCode"
       and messageType =
         flag "message-type" (required json_arg) ~doc:"JSON MessageType"
       and numberCapabilities =
         flag "number-capabilities" (required json_arg)
           ~doc:"JSON NumberCapabilityList"
       and numberType =
         flag "number-type" (required json_arg)
           ~doc:"JSON RequestableNumberType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.request_phone_number
           (Values.RequestPhoneNumberRequest.make ?optOutListName ?poolId
              ?registrationId ?deletionProtectionEnabled
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~isoCountryCode
              ~messageType:(Values.MessageType.of_json messageType)
              ~numberCapabilities:(Values.NumberCapabilityList.of_json
                                     numberCapabilities)
              ~numberType:(Values.RequestableNumberType.of_json numberType)
              ()) (Some Values.RequestPhoneNumberResult.to_json)
           (Some Values.RequestPhoneNumberResult.error_to_json)])
let send_text_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and originationIdentity =
         flag "origination-identity" (optional string)
           ~doc:"STRING TextMessageOriginationIdentity"
       and messageBody =
         flag "message-body" (optional string) ~doc:"STRING TextMessageBody"
       and messageType =
         flag "message-type" (optional json_arg) ~doc:"JSON MessageType"
       and keyword = flag "keyword" (optional string) ~doc:"STRING Keyword"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetNameOrArn"
       and maxPrice =
         flag "max-price" (optional string) ~doc:"STRING MaxPrice"
       and timeToLive =
         flag "time-to-live" (optional int) ~doc:"INT TimeToLive"
       and context =
         flag "context" (optional json_arg) ~doc:"JSON ContextMap"
       and destinationCountryParameters =
         flag "destination-country-parameters" (optional json_arg)
           ~doc:"JSON DestinationCountryParameters"
       and dryRun =
         flag "dry-run" (optional bool) ~doc:"BOOL PrimitiveBoolean"
       and destinationPhoneNumber =
         flag "destination-phone-number" (required string)
           ~doc:"STRING PhoneNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_text_message
           (Values.SendTextMessageRequest.make ?originationIdentity
              ?messageBody
              ?messageType:(Option.map ~f:Values.MessageType.of_json
                              messageType) ?keyword ?configurationSetName
              ?maxPrice ?timeToLive
              ?context:(Option.map ~f:Values.ContextMap.of_json context)
              ?destinationCountryParameters:(Option.map
                                               ~f:Values.DestinationCountryParameters.of_json
                                               destinationCountryParameters)
              ?dryRun ~destinationPhoneNumber ())
           (Some Values.SendTextMessageResult.to_json)
           (Some Values.SendTextMessageResult.error_to_json)])
let send_voice_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and messageBody =
         flag "message-body" (optional string) ~doc:"STRING VoiceMessageBody"
       and messageBodyTextType =
         flag "message-body-text-type" (optional json_arg)
           ~doc:"JSON VoiceMessageBodyTextType"
       and voiceId = flag "voice-id" (optional json_arg) ~doc:"JSON VoiceId"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetNameOrArn"
       and maxPricePerMinute =
         flag "max-price-per-minute" (optional string) ~doc:"STRING MaxPrice"
       and timeToLive =
         flag "time-to-live" (optional int) ~doc:"INT TimeToLive"
       and context =
         flag "context" (optional json_arg) ~doc:"JSON ContextMap"
       and dryRun =
         flag "dry-run" (optional bool) ~doc:"BOOL PrimitiveBoolean"
       and destinationPhoneNumber =
         flag "destination-phone-number" (required string)
           ~doc:"STRING PhoneNumber"
       and originationIdentity =
         flag "origination-identity" (required string)
           ~doc:"STRING VoiceMessageOriginationIdentity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_voice_message
           (Values.SendVoiceMessageRequest.make ?messageBody
              ?messageBodyTextType:(Option.map
                                      ~f:Values.VoiceMessageBodyTextType.of_json
                                      messageBodyTextType)
              ?voiceId:(Option.map ~f:Values.VoiceId.of_json voiceId)
              ?configurationSetName ?maxPricePerMinute ?timeToLive
              ?context:(Option.map ~f:Values.ContextMap.of_json context)
              ?dryRun ~destinationPhoneNumber ~originationIdentity ())
           (Some Values.SendVoiceMessageResult.to_json)
           (Some Values.SendVoiceMessageResult.error_to_json)])
let set_default_message_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetNameOrArn"
       and messageType =
         flag "message-type" (required json_arg) ~doc:"JSON MessageType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_default_message_type
           (Values.SetDefaultMessageTypeRequest.make ~configurationSetName
              ~messageType:(Values.MessageType.of_json messageType) ())
           (Some Values.SetDefaultMessageTypeResult.to_json)
           (Some Values.SetDefaultMessageTypeResult.error_to_json)])
let set_default_sender_id =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetNameOrArn"
       and senderId =
         flag "sender-id" (required string) ~doc:"STRING SenderId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_default_sender_id
           (Values.SetDefaultSenderIdRequest.make ~configurationSetName
              ~senderId ()) (Some Values.SetDefaultSenderIdResult.to_json)
           (Some Values.SetDefaultSenderIdResult.error_to_json)])
let set_text_message_spend_limit_override =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monthlyLimit =
         flag "monthly-limit" (required json_arg) ~doc:"JSON MonthlyLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_text_message_spend_limit_override
           (Values.SetTextMessageSpendLimitOverrideRequest.make
              ~monthlyLimit:(Values.MonthlyLimit.of_json monthlyLimit) ())
           (Some Values.SetTextMessageSpendLimitOverrideResult.to_json)
           (Some Values.SetTextMessageSpendLimitOverrideResult.error_to_json)])
let set_voice_message_spend_limit_override =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monthlyLimit =
         flag "monthly-limit" (required json_arg) ~doc:"JSON MonthlyLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_voice_message_spend_limit_override
           (Values.SetVoiceMessageSpendLimitOverrideRequest.make
              ~monthlyLimit:(Values.MonthlyLimit.of_json monthlyLimit) ())
           (Some Values.SetVoiceMessageSpendLimitOverrideResult.to_json)
           (Some Values.SetVoiceMessageSpendLimitOverrideResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON NonEmptyTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.NonEmptyTagList.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let update_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and matchingEventTypes =
         flag "matching-event-types" (optional json_arg)
           ~doc:"JSON EventTypeList"
       and cloudWatchLogsDestination =
         flag "cloud-watch-logs-destination" (optional json_arg)
           ~doc:"JSON CloudWatchLogsDestination"
       and kinesisFirehoseDestination =
         flag "kinesis-firehose-destination" (optional json_arg)
           ~doc:"JSON KinesisFirehoseDestination"
       and snsDestination =
         flag "sns-destination" (optional json_arg)
           ~doc:"JSON SnsDestination"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetNameOrArn"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_destination
           (Values.UpdateEventDestinationRequest.make ?enabled
              ?matchingEventTypes:(Option.map ~f:Values.EventTypeList.of_json
                                     matchingEventTypes)
              ?cloudWatchLogsDestination:(Option.map
                                            ~f:Values.CloudWatchLogsDestination.of_json
                                            cloudWatchLogsDestination)
              ?kinesisFirehoseDestination:(Option.map
                                             ~f:Values.KinesisFirehoseDestination.of_json
                                             kinesisFirehoseDestination)
              ?snsDestination:(Option.map ~f:Values.SnsDestination.of_json
                                 snsDestination) ~configurationSetName
              ~eventDestinationName ())
           (Some Values.UpdateEventDestinationResult.to_json)
           (Some Values.UpdateEventDestinationResult.error_to_json)])
let update_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and twoWayEnabled =
         flag "two-way-enabled" (optional bool) ~doc:"BOOL Boolean"
       and twoWayChannelArn =
         flag "two-way-channel-arn" (optional string)
           ~doc:"STRING TwoWayChannelArn"
       and selfManagedOptOutsEnabled =
         flag "self-managed-opt-outs-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and optOutListName =
         flag "opt-out-list-name" (optional string)
           ~doc:"STRING OptOutListNameOrArn"
       and deletionProtectionEnabled =
         flag "deletion-protection-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and phoneNumberId =
         flag "phone-number-id" (required string)
           ~doc:"STRING PhoneNumberIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_phone_number
           (Values.UpdatePhoneNumberRequest.make ?twoWayEnabled
              ?twoWayChannelArn ?selfManagedOptOutsEnabled ?optOutListName
              ?deletionProtectionEnabled ~phoneNumberId ())
           (Some Values.UpdatePhoneNumberResult.to_json)
           (Some Values.UpdatePhoneNumberResult.error_to_json)])
let update_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and twoWayEnabled =
         flag "two-way-enabled" (optional bool) ~doc:"BOOL Boolean"
       and twoWayChannelArn =
         flag "two-way-channel-arn" (optional string)
           ~doc:"STRING TwoWayChannelArn"
       and selfManagedOptOutsEnabled =
         flag "self-managed-opt-outs-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and optOutListName =
         flag "opt-out-list-name" (optional string)
           ~doc:"STRING OptOutListNameOrArn"
       and sharedRoutesEnabled =
         flag "shared-routes-enabled" (optional bool) ~doc:"BOOL Boolean"
       and deletionProtectionEnabled =
         flag "deletion-protection-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and poolId =
         flag "pool-id" (required string) ~doc:"STRING PoolIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pool
           (Values.UpdatePoolRequest.make ?twoWayEnabled ?twoWayChannelArn
              ?selfManagedOptOutsEnabled ?optOutListName ?sharedRoutesEnabled
              ?deletionProtectionEnabled ~poolId ())
           (Some Values.UpdatePoolResult.to_json)
           (Some Values.UpdatePoolResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-origination-identity", associate_origination_identity);
    ("create-configuration-set", create_configuration_set);
    ("create-event-destination", create_event_destination);
    ("create-opt-out-list", create_opt_out_list);
    ("create-pool", create_pool);
    ("delete-configuration-set", delete_configuration_set);
    ("delete-default-message-type", delete_default_message_type);
    ("delete-default-sender-id", delete_default_sender_id);
    ("delete-event-destination", delete_event_destination);
    ("delete-keyword", delete_keyword);
    ("delete-opt-out-list", delete_opt_out_list);
    ("delete-opted-out-number", delete_opted_out_number);
    ("delete-pool", delete_pool);
    ("delete-text-message-spend-limit-override",
      delete_text_message_spend_limit_override);
    ("delete-voice-message-spend-limit-override",
      delete_voice_message_spend_limit_override);
    ("describe-account-attributes", describe_account_attributes);
    ("describe-account-limits", describe_account_limits);
    ("describe-configuration-sets", describe_configuration_sets);
    ("describe-keywords", describe_keywords);
    ("describe-opt-out-lists", describe_opt_out_lists);
    ("describe-opted-out-numbers", describe_opted_out_numbers);
    ("describe-phone-numbers", describe_phone_numbers);
    ("describe-pools", describe_pools);
    ("describe-sender-ids", describe_sender_ids);
    ("describe-spend-limits", describe_spend_limits);
    ("disassociate-origination-identity", disassociate_origination_identity);
    ("list-pool-origination-identities", list_pool_origination_identities);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-keyword", put_keyword);
    ("put-opted-out-number", put_opted_out_number);
    ("release-phone-number", release_phone_number);
    ("request-phone-number", request_phone_number);
    ("send-text-message", send_text_message);
    ("send-voice-message", send_voice_message);
    ("set-default-message-type", set_default_message_type);
    ("set-default-sender-id", set_default_sender_id);
    ("set-text-message-spend-limit-override",
      set_text_message_spend_limit_override);
    ("set-voice-message-spend-limit-override",
      set_voice_message_spend_limit_override);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-event-destination", update_event_destination);
    ("update-phone-number", update_phone_number);
    ("update-pool", update_pool)]
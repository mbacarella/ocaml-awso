(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and componentToCreate =
         flag "component-to-create" (required json_arg)
           ~doc:"JSON CreateComponentData"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_component
           (Values.CreateComponentRequest.make ?clientToken ~appId
              ~componentToCreate:(Values.CreateComponentData.of_json
                                    componentToCreate) ~environmentName ())
           (Some Values.CreateComponentResponse.to_json)
           (Some Values.CreateComponentResponse.error_to_json)])
let create_theme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String"
       and themeToCreate =
         flag "theme-to-create" (required json_arg)
           ~doc:"JSON CreateThemeData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_theme
           (Values.CreateThemeRequest.make ?clientToken ~appId
              ~environmentName
              ~themeToCreate:(Values.CreateThemeData.of_json themeToCreate)
              ()) (Some Values.CreateThemeResponse.to_json)
           (Some Values.CreateThemeResponse.error_to_json)])
let delete_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_component
           (Values.DeleteComponentRequest.make ~appId ~environmentName ~id ())
           None None])
let delete_theme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_theme
           (Values.DeleteThemeRequest.make ~appId ~environmentName ~id ())
           None None])
let exchange_code_for_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provider =
         flag "provider" (required json_arg) ~doc:"JSON TokenProviders"
       and request =
         flag "request" (required json_arg)
           ~doc:"JSON ExchangeCodeForTokenRequestBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.exchange_code_for_token
           (Values.ExchangeCodeForTokenRequest.make
              ~provider:(Values.TokenProviders.of_json provider)
              ~request:(Values.ExchangeCodeForTokenRequestBody.of_json
                          request) ())
           (Some Values.ExchangeCodeForTokenResponse.to_json)
           (Some Values.ExchangeCodeForTokenResponse.error_to_json)])
let export_components =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_components
           (Values.ExportComponentsRequest.make ?nextToken ~appId
              ~environmentName ())
           (Some Values.ExportComponentsResponse.to_json)
           (Some Values.ExportComponentsResponse.error_to_json)])
let export_themes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_themes
           (Values.ExportThemesRequest.make ?nextToken ~appId
              ~environmentName ()) (Some Values.ExportThemesResponse.to_json)
           (Some Values.ExportThemesResponse.error_to_json)])
let get_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_component
           (Values.GetComponentRequest.make ~appId ~environmentName ~id ())
           (Some Values.GetComponentResponse.to_json)
           (Some Values.GetComponentResponse.error_to_json)])
let get_theme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_theme
           (Values.GetThemeRequest.make ~appId ~environmentName ~id ())
           (Some Values.GetThemeResponse.to_json)
           (Some Values.GetThemeResponse.error_to_json)])
let list_components =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListComponentsLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_components
           (Values.ListComponentsRequest.make ?maxResults ?nextToken ~appId
              ~environmentName ())
           (Some Values.ListComponentsResponse.to_json)
           (Some Values.ListComponentsResponse.error_to_json)])
let list_themes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListThemesLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_themes
           (Values.ListThemesRequest.make ?maxResults ?nextToken ~appId
              ~environmentName ()) (Some Values.ListThemesResponse.to_json)
           (Some Values.ListThemesResponse.error_to_json)])
let refresh_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provider =
         flag "provider" (required json_arg) ~doc:"JSON TokenProviders"
       and refreshTokenBody =
         flag "refresh-token-body" (required json_arg)
           ~doc:"JSON RefreshTokenRequestBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.refresh_token
           (Values.RefreshTokenRequest.make
              ~provider:(Values.TokenProviders.of_json provider)
              ~refreshTokenBody:(Values.RefreshTokenRequestBody.of_json
                                   refreshTokenBody) ())
           (Some Values.RefreshTokenResponse.to_json)
           (Some Values.RefreshTokenResponse.error_to_json)])
let update_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String"
       and id = flag "id" (required string) ~doc:"STRING Uuid"
       and updatedComponent =
         flag "updated-component" (required json_arg)
           ~doc:"JSON UpdateComponentData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_component
           (Values.UpdateComponentRequest.make ?clientToken ~appId
              ~environmentName ~id
              ~updatedComponent:(Values.UpdateComponentData.of_json
                                   updatedComponent) ())
           (Some Values.UpdateComponentResponse.to_json)
           (Some Values.UpdateComponentResponse.error_to_json)])
let update_theme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and appId = flag "app-id" (required string) ~doc:"STRING String"
       and environmentName =
         flag "environment-name" (required string) ~doc:"STRING String"
       and id = flag "id" (required string) ~doc:"STRING Uuid"
       and updatedTheme =
         flag "updated-theme" (required json_arg) ~doc:"JSON UpdateThemeData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_theme
           (Values.UpdateThemeRequest.make ?clientToken ~appId
              ~environmentName ~id
              ~updatedTheme:(Values.UpdateThemeData.of_json updatedTheme) ())
           (Some Values.UpdateThemeResponse.to_json)
           (Some Values.UpdateThemeResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-component", create_component);
    ("create-theme", create_theme);
    ("delete-component", delete_component);
    ("delete-theme", delete_theme);
    ("exchange-code-for-token", exchange_code_for_token);
    ("export-components", export_components);
    ("export-themes", export_themes);
    ("get-component", get_component);
    ("get-theme", get_theme);
    ("list-components", list_components);
    ("list-themes", list_themes);
    ("refresh-token", refresh_token);
    ("update-component", update_component);
    ("update-theme", update_theme)]
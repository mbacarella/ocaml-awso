(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DescribeJobExecution: (DescribeJobExecutionRequest.t,
  DescribeJobExecutionResponse.t, DescribeJobExecutionResponse.error) t 
  | GetPendingJobExecutions: (GetPendingJobExecutionsRequest.t,
  GetPendingJobExecutionsResponse.t, GetPendingJobExecutionsResponse.error) t
  
  | StartCommandExecution: (StartCommandExecutionRequest.t,
  StartCommandExecutionResponse.t, StartCommandExecutionResponse.error) t 
  | StartNextPendingJobExecution: (StartNextPendingJobExecutionRequest.t,
  StartNextPendingJobExecutionResponse.t,
  StartNextPendingJobExecutionResponse.error) t 
  | UpdateJobExecution: (UpdateJobExecutionRequest.t,
  UpdateJobExecutionResponse.t, UpdateJobExecutionResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DescribeJobExecution -> `GET
  | GetPendingJobExecutions -> `GET
  | StartCommandExecution -> `POST
  | StartNextPendingJobExecution -> `PUT
  | UpdateJobExecution -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DescribeJobExecution ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/things/%s/jobs/%s"
               (ThingName.to_header x.DescribeJobExecutionRequest.thingName)
               (DescribeJobExecutionJobId.to_header
                  x.DescribeJobExecutionRequest.jobId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("includeJobDocument",
                          (IncludeJobDocument.to_header v)))
                  x.includeJobDocument;
               Option.map
                 ~f:(fun v ->
                       ("executionNumber", (ExecutionNumber.to_header v)))
                 x.executionNumber])
      | GetPendingJobExecutions ->
          (Format.kasprintf Uri.of_string) "/things/%s/jobs"
            (ThingName.to_header x.GetPendingJobExecutionsRequest.thingName)
      | StartCommandExecution ->
          (Format.kasprintf Uri.of_string) "/command-executions"
      | StartNextPendingJobExecution ->
          (Format.kasprintf Uri.of_string) "/things/%s/jobs/$next"
            (ThingName.to_header
               x.StartNextPendingJobExecutionRequest.thingName)
      | UpdateJobExecution ->
          (Format.kasprintf Uri.of_string) "/things/%s/jobs/%s"
            (ThingName.to_header x.UpdateJobExecutionRequest.thingName)
            (JobId.to_header x.UpdateJobExecutionRequest.jobId))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DescribeJobExecution ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetPendingJobExecutions ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartCommandExecution ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("targetArn",
                           (TargetArn.to_value
                              req.StartCommandExecutionRequest.targetArn));
                      Some
                        ("commandArn",
                          (CommandArn.to_value
                             req.StartCommandExecutionRequest.commandArn));
                      Option.map req.StartCommandExecutionRequest.parameters
                        ~f:(fun x ->
                              ("parameters",
                                (CommandExecutionParameterMap.to_value x)));
                      Option.map
                        req.StartCommandExecutionRequest.executionTimeoutSeconds
                        ~f:(fun x ->
                              ("executionTimeoutSeconds",
                                (CommandExecutionTimeoutInSeconds.to_value x)));
                      Option.map req.StartCommandExecutionRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken",
                                (ClientRequestTokenV2.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartNextPendingJobExecution ->
      Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateJobExecution ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("status",
                           (JobExecutionStatus.to_value
                              req.UpdateJobExecutionRequest.status));
                      Option.map req.UpdateJobExecutionRequest.statusDetails
                        ~f:(fun x ->
                              ("statusDetails", (DetailsMap.to_value x)));
                      Option.map
                        req.UpdateJobExecutionRequest.stepTimeoutInMinutes
                        ~f:(fun x ->
                              ("stepTimeoutInMinutes",
                                (StepTimeoutInMinutes.to_value x)));
                      Option.map
                        req.UpdateJobExecutionRequest.expectedVersion
                        ~f:(fun x ->
                              ("expectedVersion",
                                (ExpectedVersion.to_value x)));
                      Option.map
                        req.UpdateJobExecutionRequest.includeJobExecutionState
                        ~f:(fun x ->
                              ("includeJobExecutionState",
                                (IncludeExecutionState.to_value x)));
                      Option.map
                        req.UpdateJobExecutionRequest.includeJobDocument
                        ~f:(fun x ->
                              ("includeJobDocument",
                                (IncludeJobDocument.to_value x)));
                      Option.map
                        req.UpdateJobExecutionRequest.executionNumber
                        ~f:(fun x ->
                              ("executionNumber",
                                (ExecutionNumber.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | DescribeJobExecution ->
      if is_success
      then Ok (DescribeJobExecutionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some DescribeJobExecutionResponse.error_of_json))
  | GetPendingJobExecutions ->
      if is_success
      then
        Ok (GetPendingJobExecutionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetPendingJobExecutionsResponse.error_of_json))
  | StartCommandExecution ->
      if is_success
      then Ok (StartCommandExecutionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StartCommandExecutionResponse.error_of_json))
  | StartNextPendingJobExecution ->
      if is_success
      then
        Ok
          (StartNextPendingJobExecutionResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some StartNextPendingJobExecutionResponse.error_of_json))
  | UpdateJobExecution ->
      if is_success
      then Ok (UpdateJobExecutionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some UpdateJobExecutionResponse.error_of_json))
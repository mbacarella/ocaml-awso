(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let describe_job_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeJobDocument =
         flag "include-job-document" (optional bool)
           ~doc:"BOOL IncludeJobDocument"
       and executionNumber =
         flag "execution-number" (optional json_arg)
           ~doc:"JSON ExecutionNumber"
       and jobId =
         flag "job-id" (required string)
           ~doc:"STRING DescribeJobExecutionJobId"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job_execution
           (Values.DescribeJobExecutionRequest.make ?includeJobDocument
              ?executionNumber:(Option.map ~f:Values.ExecutionNumber.of_json
                                  executionNumber) ~jobId ~thingName ())
           (Some Values.DescribeJobExecutionResponse.to_json)
           (Some Values.DescribeJobExecutionResponse.error_to_json)])
let get_pending_job_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pending_job_executions
           (Values.GetPendingJobExecutionsRequest.make ~thingName ())
           (Some Values.GetPendingJobExecutionsResponse.to_json)
           (Some Values.GetPendingJobExecutionsResponse.error_to_json)])
let start_command_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON CommandExecutionParameterMap"
       and executionTimeoutSeconds =
         flag "execution-timeout-seconds" (optional json_arg)
           ~doc:"JSON CommandExecutionTimeoutInSeconds"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientRequestTokenV2"
       and targetArn =
         flag "target-arn" (required string) ~doc:"STRING TargetArn"
       and commandArn =
         flag "command-arn" (required string) ~doc:"STRING CommandArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_command_execution
           (Values.StartCommandExecutionRequest.make
              ?parameters:(Option.map
                             ~f:Values.CommandExecutionParameterMap.of_json
                             parameters)
              ?executionTimeoutSeconds:(Option.map
                                          ~f:Values.CommandExecutionTimeoutInSeconds.of_json
                                          executionTimeoutSeconds)
              ?clientToken ~targetArn ~commandArn ())
           (Some Values.StartCommandExecutionResponse.to_json)
           (Some Values.StartCommandExecutionResponse.error_to_json)])
let start_next_pending_job_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statusDetails =
         flag "status-details" (optional json_arg) ~doc:"JSON DetailsMap"
       and stepTimeoutInMinutes =
         flag "step-timeout-in-minutes" (optional json_arg)
           ~doc:"JSON StepTimeoutInMinutes"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_next_pending_job_execution
           (Values.StartNextPendingJobExecutionRequest.make
              ?statusDetails:(Option.map ~f:Values.DetailsMap.of_json
                                statusDetails)
              ?stepTimeoutInMinutes:(Option.map
                                       ~f:Values.StepTimeoutInMinutes.of_json
                                       stepTimeoutInMinutes) ~thingName ())
           (Some Values.StartNextPendingJobExecutionResponse.to_json)
           (Some Values.StartNextPendingJobExecutionResponse.error_to_json)])
let update_job_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statusDetails =
         flag "status-details" (optional json_arg) ~doc:"JSON DetailsMap"
       and stepTimeoutInMinutes =
         flag "step-timeout-in-minutes" (optional json_arg)
           ~doc:"JSON StepTimeoutInMinutes"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON ExpectedVersion"
       and includeJobExecutionState =
         flag "include-job-execution-state" (optional bool)
           ~doc:"BOOL IncludeExecutionState"
       and includeJobDocument =
         flag "include-job-document" (optional bool)
           ~doc:"BOOL IncludeJobDocument"
       and executionNumber =
         flag "execution-number" (optional json_arg)
           ~doc:"JSON ExecutionNumber"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName"
       and status =
         flag "status" (required json_arg) ~doc:"JSON JobExecutionStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job_execution
           (Values.UpdateJobExecutionRequest.make
              ?statusDetails:(Option.map ~f:Values.DetailsMap.of_json
                                statusDetails)
              ?stepTimeoutInMinutes:(Option.map
                                       ~f:Values.StepTimeoutInMinutes.of_json
                                       stepTimeoutInMinutes)
              ?expectedVersion:(Option.map ~f:Values.ExpectedVersion.of_json
                                  expectedVersion) ?includeJobExecutionState
              ?includeJobDocument
              ?executionNumber:(Option.map ~f:Values.ExecutionNumber.of_json
                                  executionNumber) ~jobId ~thingName
              ~status:(Values.JobExecutionStatus.of_json status) ())
           (Some Values.UpdateJobExecutionResponse.to_json)
           (Some Values.UpdateJobExecutionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("describe-job-execution", describe_job_execution);
    ("get-pending-job-executions", get_pending_job_executions);
    ("start-command-execution", start_command_execution);
    ("start-next-pending-job-execution", start_next_pending_job_execution);
    ("update-job-execution", update_job_execution)]
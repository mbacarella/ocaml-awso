(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_connection_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ConnectionInvitationId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_connection_invitation
           (Values.AcceptConnectionInvitationRequest.make ~catalog
              ~identifier ~clientToken ())
           (Some Values.AcceptConnectionInvitationResponse.to_json)
           (Some Values.AcceptConnectionInvitationResponse.error_to_json)])
let associate_aws_training_certification_email_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier"
       and email = flag "email" (required string) ~doc:"STRING Email"
       and emailVerificationCode =
         flag "email-verification-code" (required string)
           ~doc:"STRING EmailVerificationCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_aws_training_certification_email_domain
           (Values.AssociateAwsTrainingCertificationEmailDomainRequest.make
              ?clientToken ~catalog ~identifier ~email ~emailVerificationCode
              ())
           (Some
              Values.AssociateAwsTrainingCertificationEmailDomainResponse.to_json)
           (Some
              Values.AssociateAwsTrainingCertificationEmailDomainResponse.error_to_json)])
let cancel_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ConnectionId"
       and connectionType =
         flag "connection-type" (required json_arg)
           ~doc:"JSON ConnectionType"
       and reason =
         flag "reason" (required string)
           ~doc:"STRING CancelConnectionRequestReasonString"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_connection
           (Values.CancelConnectionRequest.make ~catalog ~identifier
              ~connectionType:(Values.ConnectionType.of_json connectionType)
              ~reason ~clientToken ())
           (Some Values.CancelConnectionResponse.to_json)
           (Some Values.CancelConnectionResponse.error_to_json)])
let cancel_connection_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ConnectionInvitationId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_connection_invitation
           (Values.CancelConnectionInvitationRequest.make ~catalog
              ~identifier ~clientToken ())
           (Some Values.CancelConnectionInvitationResponse.to_json)
           (Some Values.CancelConnectionInvitationResponse.error_to_json)])
let cancel_profile_update_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier"
       and taskId =
         flag "task-id" (required string) ~doc:"STRING ProfileTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_profile_update_task
           (Values.CancelProfileUpdateTaskRequest.make ?clientToken ~catalog
              ~identifier ~taskId ())
           (Some Values.CancelProfileUpdateTaskResponse.to_json)
           (Some Values.CancelProfileUpdateTaskResponse.error_to_json)])
let create_connection_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and connectionType =
         flag "connection-type" (required json_arg)
           ~doc:"JSON ConnectionType"
       and email = flag "email" (required string) ~doc:"STRING Email"
       and message =
         flag "message" (required string)
           ~doc:"STRING CreateConnectionInvitationRequestMessageString"
       and name =
         flag "name" (required string) ~doc:"STRING SensitiveUnicodeString"
       and receiverIdentifier =
         flag "receiver-identifier" (required string)
           ~doc:"STRING ParticipantIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connection_invitation
           (Values.CreateConnectionInvitationRequest.make ~catalog
              ~clientToken
              ~connectionType:(Values.ConnectionType.of_json connectionType)
              ~email ~message ~name ~receiverIdentifier ())
           (Some Values.CreateConnectionInvitationResponse.to_json)
           (Some Values.CreateConnectionInvitationResponse.error_to_json)])
let create_partner =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and legalName =
         flag "legal-name" (required string)
           ~doc:"STRING SensitiveUnicodeString"
       and primarySolutionType =
         flag "primary-solution-type" (required json_arg)
           ~doc:"JSON PrimarySolutionType"
       and allianceLeadContact =
         flag "alliance-lead-contact" (required json_arg)
           ~doc:"JSON AllianceLeadContact"
       and emailVerificationCode =
         flag "email-verification-code" (required string)
           ~doc:"STRING EmailVerificationCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_partner
           (Values.CreatePartnerRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~catalog
              ~legalName
              ~primarySolutionType:(Values.PrimarySolutionType.of_json
                                      primarySolutionType)
              ~allianceLeadContact:(Values.AllianceLeadContact.of_json
                                      allianceLeadContact)
              ~emailVerificationCode ())
           (Some Values.CreatePartnerResponse.to_json)
           (Some Values.CreatePartnerResponse.error_to_json)])
let disassociate_aws_training_certification_email_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_aws_training_certification_email_domain
           (Values.DisassociateAwsTrainingCertificationEmailDomainRequest.make
              ?clientToken ~catalog ~identifier ~domainName ())
           (Some
              Values.DisassociateAwsTrainingCertificationEmailDomainResponse.to_json)
           (Some
              Values.DisassociateAwsTrainingCertificationEmailDomainResponse.error_to_json)])
let get_alliance_lead_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_alliance_lead_contact
           (Values.GetAllianceLeadContactRequest.make ~catalog ~identifier ())
           (Some Values.GetAllianceLeadContactResponse.to_json)
           (Some Values.GetAllianceLeadContactResponse.error_to_json)])
let get_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ConnectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connection
           (Values.GetConnectionRequest.make ~catalog ~identifier ())
           (Some Values.GetConnectionResponse.to_json)
           (Some Values.GetConnectionResponse.error_to_json)])
let get_connection_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ConnectionInvitationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connection_invitation
           (Values.GetConnectionInvitationRequest.make ~catalog ~identifier
              ()) (Some Values.GetConnectionInvitationResponse.to_json)
           (Some Values.GetConnectionInvitationResponse.error_to_json)])
let get_connection_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connection_preferences
           (Values.GetConnectionPreferencesRequest.make ~catalog ())
           (Some Values.GetConnectionPreferencesResponse.to_json)
           (Some Values.GetConnectionPreferencesResponse.error_to_json)])
let get_partner =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_partner
           (Values.GetPartnerRequest.make ~catalog ~identifier ())
           (Some Values.GetPartnerResponse.to_json)
           (Some Values.GetPartnerResponse.error_to_json)])
let get_profile_update_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile_update_task
           (Values.GetProfileUpdateTaskRequest.make ~catalog ~identifier ())
           (Some Values.GetProfileUpdateTaskResponse.to_json)
           (Some Values.GetProfileUpdateTaskResponse.error_to_json)])
let get_profile_visibility =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile_visibility
           (Values.GetProfileVisibilityRequest.make ~catalog ~identifier ())
           (Some Values.GetProfileVisibilityResponse.to_json)
           (Some Values.GetProfileVisibilityResponse.error_to_json)])
let get_verification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and verificationType =
         flag "verification-type" (required json_arg)
           ~doc:"JSON VerificationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_verification
           (Values.GetVerificationRequest.make
              ~verificationType:(Values.VerificationType.of_json
                                   verificationType) ())
           (Some Values.GetVerificationResponse.to_json)
           (Some Values.GetVerificationResponse.error_to_json)])
let list_connection_invitations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and connectionType =
         flag "connection-type" (optional json_arg)
           ~doc:"JSON ConnectionType"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and otherParticipantIdentifiers =
         flag "other-participant-identifiers" (optional json_arg)
           ~doc:"JSON ListConnectionInvitationsRequestOtherParticipantIdentifiersList"
       and participantType =
         flag "participant-type" (optional json_arg)
           ~doc:"JSON ParticipantType"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON InvitationStatus"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connection_invitations
           (Values.ListConnectionInvitationsRequest.make ?nextToken
              ?connectionType:(Option.map ~f:Values.ConnectionType.of_json
                                 connectionType) ?maxResults
              ?otherParticipantIdentifiers:(Option.map
                                              ~f:Values.ListConnectionInvitationsRequestOtherParticipantIdentifiersList.of_json
                                              otherParticipantIdentifiers)
              ?participantType:(Option.map ~f:Values.ParticipantType.of_json
                                  participantType)
              ?status:(Option.map ~f:Values.InvitationStatus.of_json status)
              ~catalog ())
           (Some Values.ListConnectionInvitationsResponse.to_json)
           (Some Values.ListConnectionInvitationsResponse.error_to_json)])
let list_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and connectionType =
         flag "connection-type" (optional string)
           ~doc:"STRING ConnectionTypeFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and otherParticipantIdentifiers =
         flag "other-participant-identifiers" (optional json_arg)
           ~doc:"JSON ListConnectionsRequestOtherParticipantIdentifiersList"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connections
           (Values.ListConnectionsRequest.make ?nextToken ?connectionType
              ?maxResults
              ?otherParticipantIdentifiers:(Option.map
                                              ~f:Values.ListConnectionsRequestOtherParticipantIdentifiersList.of_json
                                              otherParticipantIdentifiers)
              ~catalog ()) (Some Values.ListConnectionsResponse.to_json)
           (Some Values.ListConnectionsResponse.error_to_json)])
let list_partners =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_partners
           (Values.ListPartnersRequest.make ?nextToken ~catalog ())
           (Some Values.ListPartnersResponse.to_json)
           (Some Values.ListPartnersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_alliance_lead_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailVerificationCode =
         flag "email-verification-code" (optional string)
           ~doc:"STRING EmailVerificationCode"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier"
       and allianceLeadContact =
         flag "alliance-lead-contact" (required json_arg)
           ~doc:"JSON AllianceLeadContact" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_alliance_lead_contact
           (Values.PutAllianceLeadContactRequest.make ?emailVerificationCode
              ~catalog ~identifier
              ~allianceLeadContact:(Values.AllianceLeadContact.of_json
                                      allianceLeadContact) ())
           (Some Values.PutAllianceLeadContactResponse.to_json)
           (Some Values.PutAllianceLeadContactResponse.error_to_json)])
let put_profile_visibility =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier"
       and visibility =
         flag "visibility" (required json_arg) ~doc:"JSON ProfileVisibility" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_profile_visibility
           (Values.PutProfileVisibilityRequest.make ~catalog ~identifier
              ~visibility:(Values.ProfileVisibility.of_json visibility) ())
           (Some Values.PutProfileVisibilityResponse.to_json)
           (Some Values.PutProfileVisibilityResponse.error_to_json)])
let reject_connection_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason =
         flag "reason" (optional string)
           ~doc:"STRING RejectConnectionInvitationRequestReasonString"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ConnectionInvitationId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_connection_invitation
           (Values.RejectConnectionInvitationRequest.make ?reason ~catalog
              ~identifier ~clientToken ())
           (Some Values.RejectConnectionInvitationResponse.to_json)
           (Some Values.RejectConnectionInvitationResponse.error_to_json)])
let send_email_verification_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and email = flag "email" (required string) ~doc:"STRING Email" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_email_verification_code
           (Values.SendEmailVerificationCodeRequest.make ~catalog ~email ())
           (Some Values.SendEmailVerificationCodeResponse.to_json)
           (Some Values.SendEmailVerificationCodeResponse.error_to_json)])
let start_profile_update_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING PartnerIdentifier"
       and taskDetails =
         flag "task-details" (required json_arg) ~doc:"JSON TaskDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_profile_update_task
           (Values.StartProfileUpdateTaskRequest.make ?clientToken ~catalog
              ~identifier
              ~taskDetails:(Values.TaskDetails.of_json taskDetails) ())
           (Some Values.StartProfileUpdateTaskResponse.to_json)
           (Some Values.StartProfileUpdateTaskResponse.error_to_json)])
let start_verification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and verificationDetails =
         flag "verification-details" (optional json_arg)
           ~doc:"JSON VerificationDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_verification
           (Values.StartVerificationRequest.make ?clientToken
              ?verificationDetails:(Option.map
                                      ~f:Values.VerificationDetails.of_json
                                      verificationDetails) ())
           (Some Values.StartVerificationResponse.to_json)
           (Some Values.StartVerificationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_connection_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and excludedParticipantIdentifiers =
         flag "excluded-participant-identifiers" (optional json_arg)
           ~doc:"JSON UpdateConnectionPreferencesRequestExcludedParticipantIdentifiersList"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and revision =
         flag "revision" (required json_arg) ~doc:"JSON Revision"
       and accessType =
         flag "access-type" (required json_arg) ~doc:"JSON AccessType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connection_preferences
           (Values.UpdateConnectionPreferencesRequest.make
              ?excludedParticipantIdentifiers:(Option.map
                                                 ~f:Values.UpdateConnectionPreferencesRequestExcludedParticipantIdentifiersList.of_json
                                                 excludedParticipantIdentifiers)
              ~catalog ~revision:(Values.Revision.of_json revision)
              ~accessType:(Values.AccessType.of_json accessType) ())
           (Some Values.UpdateConnectionPreferencesResponse.to_json)
           (Some Values.UpdateConnectionPreferencesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-connection-invitation", accept_connection_invitation);
    ("associate-aws-training-certification-email-domain",
      associate_aws_training_certification_email_domain);
    ("cancel-connection", cancel_connection);
    ("cancel-connection-invitation", cancel_connection_invitation);
    ("cancel-profile-update-task", cancel_profile_update_task);
    ("create-connection-invitation", create_connection_invitation);
    ("create-partner", create_partner);
    ("disassociate-aws-training-certification-email-domain",
      disassociate_aws_training_certification_email_domain);
    ("get-alliance-lead-contact", get_alliance_lead_contact);
    ("get-connection", get_connection);
    ("get-connection-invitation", get_connection_invitation);
    ("get-connection-preferences", get_connection_preferences);
    ("get-partner", get_partner);
    ("get-profile-update-task", get_profile_update_task);
    ("get-profile-visibility", get_profile_visibility);
    ("get-verification", get_verification);
    ("list-connection-invitations", list_connection_invitations);
    ("list-connections", list_connections);
    ("list-partners", list_partners);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-alliance-lead-contact", put_alliance_lead_contact);
    ("put-profile-visibility", put_profile_visibility);
    ("reject-connection-invitation", reject_connection_invitation);
    ("send-email-verification-code", send_email_verification_code);
    ("start-profile-update-task", start_profile_update_task);
    ("start-verification", start_verification);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-connection-preferences", update_connection_preferences)]
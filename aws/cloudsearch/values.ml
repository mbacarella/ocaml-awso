(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.cloudsearch
let apiVersion = "2013-01-01"
let endpointPrefix = "cloudsearch"
let serviceFullName = "Amazon CloudSearch"
let signatureVersion = "v4"
let protocol = "query"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let xmlNamespace = "http://cloudsearch.amazonaws.com/doc/2013-01-01/"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module FieldName =
  struct
    type nonrec t = string[@@ocaml.doc
                            "A string that represents the name of an index field. CloudSearch supports regular index fields as well as dynamic fields. A dynamic field's name defines a pattern that begins or ends with a wildcard. Any document fields that don't map to a regular index field but do match a dynamic field's pattern are configured with the dynamic field's indexing options. Regular field names begin with a letter and can contain the following characters: a-z (lowercase), 0-9, and _ (underscore). Dynamic field names must begin or end with a wildcard (*). The wildcard can also be the only character in a dynamic field name. Multiple wildcards, and wildcards embedded within a string are not supported. The name score is reserved and cannot be used as a field name. To reference a document's ID, you can use the name _id."]
    let context_ = "FieldName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-z][a-z0-9_]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FieldName" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "A string that represents the name of an index field. CloudSearch supports regular index fields as well as dynamic fields. A dynamic field's name defines a pattern that begins or ends with a wildcard. Any document fields that don't map to a regular index field but do match a dynamic field's pattern are configured with the dynamic field's indexing options. Regular field names begin with a letter and can contain the following characters: a-z (lowercase), 0-9, and _ (underscore). Dynamic field names must begin or end with a wildcard (*). The wildcard can also be the only character in a dynamic field name. Multiple wildcards, and wildcards embedded within a string are not supported. The name score is reserved and cannot be used as a field name. To reference a document's ID, you can use the name _id."]
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module SuggesterFuzzyMatching =
  struct
    type nonrec t =
      | None 
      | Low 
      | High 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | None -> "none"
      | Low -> "low"
      | High -> "high"
      | Non_static_id s -> s
    let of_string =
      function
      | "none" -> None
      | "low" -> Low
      | "high" -> High
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SuggesterFuzzyMatching" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SuggesterFuzzyMatching" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module FieldNameCommaList =
  struct
    type nonrec t = string
    let context_ = "FieldNameCommaList"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"\\s*[a-z*][a-z0-9_]*\\*?\\s*(,\\s*[a-z*][a-z0-9_]*\\*?\\s*)*");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FieldNameCommaList" j
    let to_json = simple_to_json to_value
  end
module FieldValue =
  struct
    type nonrec t = string[@@ocaml.doc "The value of a field attribute."]
    let context_ = "FieldValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FieldValue" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The value of a field attribute."]
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Word =
  struct
    type nonrec t = string
    let context_ = "Word"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[\\S]+"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Word" j
    let to_json = simple_to_json to_value
  end
module AlgorithmicStemming =
  struct
    type nonrec t =
      | None 
      | Minimal 
      | Light 
      | Full 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | None -> "none"
      | Minimal -> "minimal"
      | Light -> "light"
      | Full -> "full"
      | Non_static_id s -> s
    let of_string =
      function
      | "none" -> None
      | "minimal" -> Minimal
      | "light" -> Light
      | "full" -> Full
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AlgorithmicStemming" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AlgorithmicStemming" j)
    let to_json = simple_to_json to_value
  end
module OptionState =
  struct
    type nonrec t =
      | RequiresIndexDocuments 
      | Processing 
      | Active 
      | FailedToValidate 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RequiresIndexDocuments -> "RequiresIndexDocuments"
      | Processing -> "Processing"
      | Active -> "Active"
      | FailedToValidate -> "FailedToValidate"
      | Non_static_id s -> s
    let of_string =
      function
      | "RequiresIndexDocuments" -> RequiresIndexDocuments
      | "Processing" -> Processing
      | "Active" -> Active
      | "FailedToValidate" -> FailedToValidate
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OptionState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OptionState" j)
    let to_json = simple_to_json to_value
  end
module UIntValue =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for UIntValue" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module UpdateTimestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module DocumentSuggesterOptions =
  struct
    type nonrec t =
      {
      sourceField: FieldName.t
        [@ocaml.doc
          "The name of the index field you want to use for suggestions."];
      fuzzyMatching: SuggesterFuzzyMatching.t option
        [@ocaml.doc
          "The level of fuzziness allowed when suggesting matches for a string: none, low, or high. With none, the specified string is treated as an exact prefix. With low, suggestions must differ from the specified string by no more than one character. With high, suggestions can differ by up to two characters. The default is none."];
      sortExpression: String_.t option
        [@ocaml.doc
          "An expression that computes a score for each suggestion to control how they are sorted. The scores are rounded to the nearest integer, with a floor of 0 and a ceiling of 2^31-1. A document's relevance score is not computed for suggestions, so sort expressions cannot reference the _score value. To sort suggestions using a numeric field or existing expression, simply specify the name of the field or expression. If no expression is configured for the suggester, the suggestions are sorted with the closest matches listed first."]}
    let context_ = "DocumentSuggesterOptions"
    let make ?fuzzyMatching =
      fun ?sortExpression ->
        fun ~sourceField ->
          fun () -> { fuzzyMatching; sortExpression; sourceField }
    let to_value x =
      structure_to_value
        [("SourceField", (Some (FieldName.to_value x.sourceField)));
        ("FuzzyMatching",
          (Option.map x.fuzzyMatching ~f:SuggesterFuzzyMatching.to_value));
        ("SortExpression", (Option.map x.sortExpression ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortExpression =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SortExpression") in
      let fuzzyMatching =
        (Option.map ~f:SuggesterFuzzyMatching.of_xml)
          (Xml.child xml_arg0 "FuzzyMatching") in
      let sourceField =
        FieldName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceField") in
      make ?sortExpression ?fuzzyMatching ~sourceField ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortExpression = field_map json__ "SortExpression" String_.of_json in
      let fuzzyMatching =
        field_map json__ "FuzzyMatching" SuggesterFuzzyMatching.of_json in
      let sourceField = field_map_exn json__ "SourceField" FieldName.of_json in
      make ?sortExpression ?fuzzyMatching ~sourceField ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Options for a search suggester."]
module StandardName =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Names must begin with a letter and can contain the following characters: a-z (lowercase), 0-9, and _ (underscore)."]
    let context_ = "StandardName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-z][a-z0-9_]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StandardName" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Names must begin with a letter and can contain the following characters: a-z (lowercase), 0-9, and _ (underscore)."]
module DateArrayOptions =
  struct
    type nonrec t =
      {
      defaultValue: FieldValue.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceFields: FieldNameCommaList.t option
        [@ocaml.doc "A list of source fields to map to the field."];
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."]}
    let make ?defaultValue =
      fun ?sourceFields ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun () ->
                {
                  defaultValue;
                  sourceFields;
                  facetEnabled;
                  searchEnabled;
                  returnEnabled
                }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:FieldValue.to_value));
        ("SourceFields",
          (Option.map x.sourceFields ~f:FieldNameCommaList.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceFields =
        (Option.map ~f:FieldNameCommaList.of_xml)
          (Xml.child xml_arg0 "SourceFields") in
      let defaultValue =
        (Option.map ~f:FieldValue.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?returnEnabled ?searchEnabled ?facetEnabled ?sourceFields
        ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceFields =
        field_map json__ "SourceFields" FieldNameCommaList.of_json in
      let defaultValue = field_map json__ "DefaultValue" FieldValue.of_json in
      make ?returnEnabled ?searchEnabled ?facetEnabled ?sourceFields
        ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a field that contains an array of dates. Present if IndexFieldType specifies the field is of type date-array. All options are enabled by default."]
module DateOptions =
  struct
    type nonrec t =
      {
      defaultValue: FieldValue.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceField: FieldName.t option ;
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."];
      sortEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the field can be used to sort the search results."]}
    let make ?defaultValue =
      fun ?sourceField ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun ?sortEnabled ->
                fun () ->
                  {
                    defaultValue;
                    sourceField;
                    facetEnabled;
                    searchEnabled;
                    returnEnabled;
                    sortEnabled
                  }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:FieldValue.to_value));
        ("SourceField", (Option.map x.sourceField ~f:FieldName.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value));
        ("SortEnabled", (Option.map x.sortEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SortEnabled") in
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceField =
        (Option.map ~f:FieldName.of_xml) (Xml.child xml_arg0 "SourceField") in
      let defaultValue =
        (Option.map ~f:FieldValue.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortEnabled = field_map json__ "SortEnabled" Boolean.of_json in
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceField = field_map json__ "SourceField" FieldName.of_json in
      let defaultValue = field_map json__ "DefaultValue" FieldValue.of_json in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a date field. Dates and times are specified in UTC (Coordinated Universal Time) according to IETF RFC3339: yyyy-mm-ddT00:00:00Z. Present if IndexFieldType specifies the field is of type date. All options are enabled by default."]
module DoubleArrayOptions =
  struct
    type nonrec t =
      {
      defaultValue: Double.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceFields: FieldNameCommaList.t option
        [@ocaml.doc "A list of source fields to map to the field."];
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."]}
    let make ?defaultValue =
      fun ?sourceFields ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun () ->
                {
                  defaultValue;
                  sourceFields;
                  facetEnabled;
                  searchEnabled;
                  returnEnabled
                }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:Double.to_value));
        ("SourceFields",
          (Option.map x.sourceFields ~f:FieldNameCommaList.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceFields =
        (Option.map ~f:FieldNameCommaList.of_xml)
          (Xml.child xml_arg0 "SourceFields") in
      let defaultValue =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?returnEnabled ?searchEnabled ?facetEnabled ?sourceFields
        ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceFields =
        field_map json__ "SourceFields" FieldNameCommaList.of_json in
      let defaultValue = field_map json__ "DefaultValue" Double.of_json in
      make ?returnEnabled ?searchEnabled ?facetEnabled ?sourceFields
        ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a field that contains an array of double-precision 64-bit floating point values. Present if IndexFieldType specifies the field is of type double-array. All options are enabled by default."]
module DoubleOptions =
  struct
    type nonrec t =
      {
      defaultValue: Double.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document. This can be important if you are using the field in an expression and that field is not present in every document."];
      sourceField: FieldName.t option
        [@ocaml.doc "The name of the source field to map to the field."];
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."];
      sortEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the field can be used to sort the search results."]}
    let make ?defaultValue =
      fun ?sourceField ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun ?sortEnabled ->
                fun () ->
                  {
                    defaultValue;
                    sourceField;
                    facetEnabled;
                    searchEnabled;
                    returnEnabled;
                    sortEnabled
                  }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:Double.to_value));
        ("SourceField", (Option.map x.sourceField ~f:FieldName.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value));
        ("SortEnabled", (Option.map x.sortEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SortEnabled") in
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceField =
        (Option.map ~f:FieldName.of_xml) (Xml.child xml_arg0 "SourceField") in
      let defaultValue =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortEnabled = field_map json__ "SortEnabled" Boolean.of_json in
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceField = field_map json__ "SourceField" FieldName.of_json in
      let defaultValue = field_map json__ "DefaultValue" Double.of_json in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a double-precision 64-bit floating point field. Present if IndexFieldType specifies the field is of type double. All options are enabled by default."]
module DynamicFieldName =
  struct
    type nonrec t = string
    let context_ = "DynamicFieldName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"([a-z][a-z0-9_]*\\*?|\\*[a-z0-9_]*)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DynamicFieldName" j
    let to_json = simple_to_json to_value
  end
module IndexFieldType =
  struct
    type nonrec t =
      | Int_ 
      | Double 
      | Literal 
      | Text 
      | Date 
      | Latlon 
      | Int_array 
      | Double_array 
      | Literal_array 
      | Text_array 
      | Date_array 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Int_ -> "int"
      | Double -> "double"
      | Literal -> "literal"
      | Text -> "text"
      | Date -> "date"
      | Latlon -> "latlon"
      | Int_array -> "int-array"
      | Double_array -> "double-array"
      | Literal_array -> "literal-array"
      | Text_array -> "text-array"
      | Date_array -> "date-array"
      | Non_static_id s -> s
    let of_string =
      function
      | "int" -> Int_
      | "double" -> Double
      | "literal" -> Literal
      | "text" -> Text
      | "date" -> Date
      | "latlon" -> Latlon
      | "int-array" -> Int_array
      | "double-array" -> Double_array
      | "literal-array" -> Literal_array
      | "text-array" -> Text_array
      | "date-array" -> Date_array
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration IndexFieldType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IndexFieldType" j)
    let to_json = simple_to_json to_value
  end
module IntArrayOptions =
  struct
    type nonrec t =
      {
      defaultValue: Long.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceFields: FieldNameCommaList.t option
        [@ocaml.doc "A list of source fields to map to the field."];
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."]}
    let make ?defaultValue =
      fun ?sourceFields ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun () ->
                {
                  defaultValue;
                  sourceFields;
                  facetEnabled;
                  searchEnabled;
                  returnEnabled
                }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:Long.to_value));
        ("SourceFields",
          (Option.map x.sourceFields ~f:FieldNameCommaList.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceFields =
        (Option.map ~f:FieldNameCommaList.of_xml)
          (Xml.child xml_arg0 "SourceFields") in
      let defaultValue =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?returnEnabled ?searchEnabled ?facetEnabled ?sourceFields
        ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceFields =
        field_map json__ "SourceFields" FieldNameCommaList.of_json in
      let defaultValue = field_map json__ "DefaultValue" Long.of_json in
      make ?returnEnabled ?searchEnabled ?facetEnabled ?sourceFields
        ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a field that contains an array of 64-bit signed integers. Present if IndexFieldType specifies the field is of type int-array. All options are enabled by default."]
module IntOptions =
  struct
    type nonrec t =
      {
      defaultValue: Long.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document. This can be important if you are using the field in an expression and that field is not present in every document."];
      sourceField: FieldName.t option
        [@ocaml.doc "The name of the source field to map to the field."];
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."];
      sortEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the field can be used to sort the search results."]}
    let make ?defaultValue =
      fun ?sourceField ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun ?sortEnabled ->
                fun () ->
                  {
                    defaultValue;
                    sourceField;
                    facetEnabled;
                    searchEnabled;
                    returnEnabled;
                    sortEnabled
                  }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:Long.to_value));
        ("SourceField", (Option.map x.sourceField ~f:FieldName.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value));
        ("SortEnabled", (Option.map x.sortEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SortEnabled") in
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceField =
        (Option.map ~f:FieldName.of_xml) (Xml.child xml_arg0 "SourceField") in
      let defaultValue =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortEnabled = field_map json__ "SortEnabled" Boolean.of_json in
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceField = field_map json__ "SourceField" FieldName.of_json in
      let defaultValue = field_map json__ "DefaultValue" Long.of_json in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a 64-bit signed integer field. Present if IndexFieldType specifies the field is of type int. All options are enabled by default."]
module LatLonOptions =
  struct
    type nonrec t =
      {
      defaultValue: FieldValue.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceField: FieldName.t option ;
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."];
      sortEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the field can be used to sort the search results."]}
    let make ?defaultValue =
      fun ?sourceField ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun ?sortEnabled ->
                fun () ->
                  {
                    defaultValue;
                    sourceField;
                    facetEnabled;
                    searchEnabled;
                    returnEnabled;
                    sortEnabled
                  }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:FieldValue.to_value));
        ("SourceField", (Option.map x.sourceField ~f:FieldName.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value));
        ("SortEnabled", (Option.map x.sortEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SortEnabled") in
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceField =
        (Option.map ~f:FieldName.of_xml) (Xml.child xml_arg0 "SourceField") in
      let defaultValue =
        (Option.map ~f:FieldValue.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortEnabled = field_map json__ "SortEnabled" Boolean.of_json in
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceField = field_map json__ "SourceField" FieldName.of_json in
      let defaultValue = field_map json__ "DefaultValue" FieldValue.of_json in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a latlon field. A latlon field contains a location stored as a latitude and longitude value pair. Present if IndexFieldType specifies the field is of type latlon. All options are enabled by default."]
module LiteralArrayOptions =
  struct
    type nonrec t =
      {
      defaultValue: FieldValue.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceFields: FieldNameCommaList.t option
        [@ocaml.doc "A list of source fields to map to the field."];
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."]}
    let make ?defaultValue =
      fun ?sourceFields ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun () ->
                {
                  defaultValue;
                  sourceFields;
                  facetEnabled;
                  searchEnabled;
                  returnEnabled
                }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:FieldValue.to_value));
        ("SourceFields",
          (Option.map x.sourceFields ~f:FieldNameCommaList.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceFields =
        (Option.map ~f:FieldNameCommaList.of_xml)
          (Xml.child xml_arg0 "SourceFields") in
      let defaultValue =
        (Option.map ~f:FieldValue.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?returnEnabled ?searchEnabled ?facetEnabled ?sourceFields
        ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceFields =
        field_map json__ "SourceFields" FieldNameCommaList.of_json in
      let defaultValue = field_map json__ "DefaultValue" FieldValue.of_json in
      make ?returnEnabled ?searchEnabled ?facetEnabled ?sourceFields
        ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a field that contains an array of literal strings. Present if IndexFieldType specifies the field is of type literal-array. All options are enabled by default."]
module LiteralOptions =
  struct
    type nonrec t =
      {
      defaultValue: FieldValue.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceField: FieldName.t option ;
      facetEnabled: Boolean.t option
        [@ocaml.doc
          "Whether facet information can be returned for the field."];
      searchEnabled: Boolean.t option
        [@ocaml.doc "Whether the contents of the field are searchable."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."];
      sortEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the field can be used to sort the search results."]}
    let make ?defaultValue =
      fun ?sourceField ->
        fun ?facetEnabled ->
          fun ?searchEnabled ->
            fun ?returnEnabled ->
              fun ?sortEnabled ->
                fun () ->
                  {
                    defaultValue;
                    sourceField;
                    facetEnabled;
                    searchEnabled;
                    returnEnabled;
                    sortEnabled
                  }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:FieldValue.to_value));
        ("SourceField", (Option.map x.sourceField ~f:FieldName.to_value));
        ("FacetEnabled", (Option.map x.facetEnabled ~f:Boolean.to_value));
        ("SearchEnabled", (Option.map x.searchEnabled ~f:Boolean.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value));
        ("SortEnabled", (Option.map x.sortEnabled ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SortEnabled") in
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let searchEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SearchEnabled") in
      let facetEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "FacetEnabled") in
      let sourceField =
        (Option.map ~f:FieldName.of_xml) (Xml.child xml_arg0 "SourceField") in
      let defaultValue =
        (Option.map ~f:FieldValue.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortEnabled = field_map json__ "SortEnabled" Boolean.of_json in
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let searchEnabled = field_map json__ "SearchEnabled" Boolean.of_json in
      let facetEnabled = field_map json__ "FacetEnabled" Boolean.of_json in
      let sourceField = field_map json__ "SourceField" FieldName.of_json in
      let defaultValue = field_map json__ "DefaultValue" FieldValue.of_json in
      make ?sortEnabled ?returnEnabled ?searchEnabled ?facetEnabled
        ?sourceField ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for literal field. Present if IndexFieldType specifies the field is of type literal. All options are enabled by default."]
module TextArrayOptions =
  struct
    type nonrec t =
      {
      defaultValue: FieldValue.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceFields: FieldNameCommaList.t option
        [@ocaml.doc "A list of source fields to map to the field."];
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."];
      highlightEnabled: Boolean.t option
        [@ocaml.doc "Whether highlights can be returned for the field."];
      analysisScheme: Word.t option
        [@ocaml.doc "The name of an analysis scheme for a text-array field."]}
    let make ?defaultValue =
      fun ?sourceFields ->
        fun ?returnEnabled ->
          fun ?highlightEnabled ->
            fun ?analysisScheme ->
              fun () ->
                {
                  defaultValue;
                  sourceFields;
                  returnEnabled;
                  highlightEnabled;
                  analysisScheme
                }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:FieldValue.to_value));
        ("SourceFields",
          (Option.map x.sourceFields ~f:FieldNameCommaList.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value));
        ("HighlightEnabled",
          (Option.map x.highlightEnabled ~f:Boolean.to_value));
        ("AnalysisScheme", (Option.map x.analysisScheme ~f:Word.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let analysisScheme =
        (Option.map ~f:Word.of_xml) (Xml.child xml_arg0 "AnalysisScheme") in
      let highlightEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "HighlightEnabled") in
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let sourceFields =
        (Option.map ~f:FieldNameCommaList.of_xml)
          (Xml.child xml_arg0 "SourceFields") in
      let defaultValue =
        (Option.map ~f:FieldValue.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?analysisScheme ?highlightEnabled ?returnEnabled ?sourceFields
        ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let analysisScheme = field_map json__ "AnalysisScheme" Word.of_json in
      let highlightEnabled =
        field_map json__ "HighlightEnabled" Boolean.of_json in
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let sourceFields =
        field_map json__ "SourceFields" FieldNameCommaList.of_json in
      let defaultValue = field_map json__ "DefaultValue" FieldValue.of_json in
      make ?analysisScheme ?highlightEnabled ?returnEnabled ?sourceFields
        ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for a field that contains an array of text strings. Present if IndexFieldType specifies the field is of type text-array. A text-array field is always searchable. All options are enabled by default."]
module TextOptions =
  struct
    type nonrec t =
      {
      defaultValue: FieldValue.t option
        [@ocaml.doc
          "A value to use for the field if the field isn't specified for a document."];
      sourceField: FieldName.t option ;
      returnEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the contents of the field can be returned in the search results."];
      sortEnabled: Boolean.t option
        [@ocaml.doc
          "Whether the field can be used to sort the search results."];
      highlightEnabled: Boolean.t option
        [@ocaml.doc "Whether highlights can be returned for the field."];
      analysisScheme: Word.t option
        [@ocaml.doc "The name of an analysis scheme for a text field."]}
    let make ?defaultValue =
      fun ?sourceField ->
        fun ?returnEnabled ->
          fun ?sortEnabled ->
            fun ?highlightEnabled ->
              fun ?analysisScheme ->
                fun () ->
                  {
                    defaultValue;
                    sourceField;
                    returnEnabled;
                    sortEnabled;
                    highlightEnabled;
                    analysisScheme
                  }
    let to_value x =
      structure_to_value
        [("DefaultValue", (Option.map x.defaultValue ~f:FieldValue.to_value));
        ("SourceField", (Option.map x.sourceField ~f:FieldName.to_value));
        ("ReturnEnabled", (Option.map x.returnEnabled ~f:Boolean.to_value));
        ("SortEnabled", (Option.map x.sortEnabled ~f:Boolean.to_value));
        ("HighlightEnabled",
          (Option.map x.highlightEnabled ~f:Boolean.to_value));
        ("AnalysisScheme", (Option.map x.analysisScheme ~f:Word.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let analysisScheme =
        (Option.map ~f:Word.of_xml) (Xml.child xml_arg0 "AnalysisScheme") in
      let highlightEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "HighlightEnabled") in
      let sortEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "SortEnabled") in
      let returnEnabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "ReturnEnabled") in
      let sourceField =
        (Option.map ~f:FieldName.of_xml) (Xml.child xml_arg0 "SourceField") in
      let defaultValue =
        (Option.map ~f:FieldValue.of_xml) (Xml.child xml_arg0 "DefaultValue") in
      make ?analysisScheme ?highlightEnabled ?sortEnabled ?returnEnabled
        ?sourceField ?defaultValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let analysisScheme = field_map json__ "AnalysisScheme" Word.of_json in
      let highlightEnabled =
        field_map json__ "HighlightEnabled" Boolean.of_json in
      let sortEnabled = field_map json__ "SortEnabled" Boolean.of_json in
      let returnEnabled = field_map json__ "ReturnEnabled" Boolean.of_json in
      let sourceField = field_map json__ "SourceField" FieldName.of_json in
      let defaultValue = field_map json__ "DefaultValue" FieldValue.of_json in
      make ?analysisScheme ?highlightEnabled ?sortEnabled ?returnEnabled
        ?sourceField ?defaultValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Options for text field. Present if IndexFieldType specifies the field is of type text. A text field is always searchable. All options are enabled by default."]
module ExpressionValue =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The expression to evaluate for sorting while processing a search request. The Expression syntax is based on JavaScript expressions. For more information, see Configuring Expressions in the Amazon CloudSearch Developer Guide."]
    let context_ = "ExpressionValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10240) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExpressionValue" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The expression to evaluate for sorting while processing a search request. The Expression syntax is based on JavaScript expressions. For more information, see Configuring Expressions in the Amazon CloudSearch Developer Guide."]
module MaximumPartitionCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaximumPartitionCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MaximumReplicationCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaximumReplicationCount"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ServiceUrl =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The endpoint to which service requests can be submitted. For example, search-imdb-movies-oopcnjfn6ugofer3zx5iadxxca.eu-west-1.cloudsearch.amazonaws.com or doc-imdb-movies-oopcnjfn6ugofer3zx5iadxxca.eu-west-1.cloudsearch.amazonaws.com."]
    let context_ = "ServiceUrl"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceUrl" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The endpoint to which service requests can be submitted. For example, search-imdb-movies-oopcnjfn6ugofer3zx5iadxxca.eu-west-1.cloudsearch.amazonaws.com or doc-imdb-movies-oopcnjfn6ugofer3zx5iadxxca.eu-west-1.cloudsearch.amazonaws.com."]
module AnalysisOptions =
  struct
    type nonrec t =
      {
      synonyms: String_.t option
        [@ocaml.doc
          "A JSON object that defines synonym groups and aliases. A synonym group is an array of arrays, where each sub-array is a group of terms where each term in the group is considered a synonym of every other term in the group. The aliases value is an object that contains a collection of string:value pairs where the string specifies a term and the array of values specifies each of the aliases for that term. An alias is considered a synonym of the specified term, but the term is not considered a synonym of the alias. For more information about specifying synonyms, see Synonyms in the Amazon CloudSearch Developer Guide."];
      stopwords: String_.t option
        [@ocaml.doc
          "A JSON array of terms to ignore during indexing and searching. For example, \\[\"a\", \"an\", \"the\", \"of\"\\]. The stopwords dictionary must explicitly list each word you want to ignore. Wildcards and regular expressions are not supported."];
      stemmingDictionary: String_.t option
        [@ocaml.doc
          "A JSON object that contains a collection of string:value pairs that each map a term to its stem. For example, \\{\"term1\": \"stem1\", \"term2\": \"stem2\", \"term3\": \"stem3\"\\}. The stemming dictionary is applied in addition to any algorithmic stemming. This enables you to override the results of the algorithmic stemming to correct specific cases of overstemming or understemming. The maximum size of a stemming dictionary is 500 KB."];
      japaneseTokenizationDictionary: String_.t option
        [@ocaml.doc
          "A JSON array that contains a collection of terms, tokens, readings and part of speech for Japanese Tokenizaiton. The Japanese tokenization dictionary enables you to override the default tokenization for selected terms. This is only valid for Japanese language fields."];
      algorithmicStemming: AlgorithmicStemming.t option
        [@ocaml.doc
          "The level of algorithmic stemming to perform: none, minimal, light, or full. The available levels vary depending on the language. For more information, see Language Specific Text Processing Settings in the Amazon CloudSearch Developer Guide"]}
    let make ?synonyms =
      fun ?stopwords ->
        fun ?stemmingDictionary ->
          fun ?japaneseTokenizationDictionary ->
            fun ?algorithmicStemming ->
              fun () ->
                {
                  synonyms;
                  stopwords;
                  stemmingDictionary;
                  japaneseTokenizationDictionary;
                  algorithmicStemming
                }
    let to_value x =
      structure_to_value
        [("Synonyms", (Option.map x.synonyms ~f:String_.to_value));
        ("Stopwords", (Option.map x.stopwords ~f:String_.to_value));
        ("StemmingDictionary",
          (Option.map x.stemmingDictionary ~f:String_.to_value));
        ("JapaneseTokenizationDictionary",
          (Option.map x.japaneseTokenizationDictionary ~f:String_.to_value));
        ("AlgorithmicStemming",
          (Option.map x.algorithmicStemming ~f:AlgorithmicStemming.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let algorithmicStemming =
        (Option.map ~f:AlgorithmicStemming.of_xml)
          (Xml.child xml_arg0 "AlgorithmicStemming") in
      let japaneseTokenizationDictionary =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "JapaneseTokenizationDictionary") in
      let stemmingDictionary =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "StemmingDictionary") in
      let stopwords =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Stopwords") in
      let synonyms =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Synonyms") in
      make ?algorithmicStemming ?japaneseTokenizationDictionary
        ?stemmingDictionary ?stopwords ?synonyms ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let algorithmicStemming =
        field_map json__ "AlgorithmicStemming" AlgorithmicStemming.of_json in
      let japaneseTokenizationDictionary =
        field_map json__ "JapaneseTokenizationDictionary" String_.of_json in
      let stemmingDictionary =
        field_map json__ "StemmingDictionary" String_.of_json in
      let stopwords = field_map json__ "Stopwords" String_.of_json in
      let synonyms = field_map json__ "Synonyms" String_.of_json in
      make ?algorithmicStemming ?japaneseTokenizationDictionary
        ?stemmingDictionary ?stopwords ?synonyms ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Synonyms, stopwords, and stemming options for an analysis scheme. Includes tokenization dictionary for Japanese."]
module AnalysisSchemeLanguage =
  struct
    type nonrec t =
      | Ar 
      | Bg 
      | Ca 
      | Cs 
      | Da 
      | De 
      | El 
      | En 
      | Es 
      | Eu 
      | Fa 
      | Fi 
      | Fr 
      | Ga 
      | Gl 
      | He 
      | Hi 
      | Hu 
      | Hy 
      | Id 
      | It 
      | Ja 
      | Ko 
      | Lv 
      | Mul 
      | Nl 
      | No 
      | Pt 
      | Ro 
      | Ru 
      | Sv 
      | Th 
      | Tr 
      | Zh_Hans 
      | Zh_Hant 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Ar -> "ar"
      | Bg -> "bg"
      | Ca -> "ca"
      | Cs -> "cs"
      | Da -> "da"
      | De -> "de"
      | El -> "el"
      | En -> "en"
      | Es -> "es"
      | Eu -> "eu"
      | Fa -> "fa"
      | Fi -> "fi"
      | Fr -> "fr"
      | Ga -> "ga"
      | Gl -> "gl"
      | He -> "he"
      | Hi -> "hi"
      | Hu -> "hu"
      | Hy -> "hy"
      | Id -> "id"
      | It -> "it"
      | Ja -> "ja"
      | Ko -> "ko"
      | Lv -> "lv"
      | Mul -> "mul"
      | Nl -> "nl"
      | No -> "no"
      | Pt -> "pt"
      | Ro -> "ro"
      | Ru -> "ru"
      | Sv -> "sv"
      | Th -> "th"
      | Tr -> "tr"
      | Zh_Hans -> "zh-Hans"
      | Zh_Hant -> "zh-Hant"
      | Non_static_id s -> s
    let of_string =
      function
      | "ar" -> Ar
      | "bg" -> Bg
      | "ca" -> Ca
      | "cs" -> Cs
      | "da" -> Da
      | "de" -> De
      | "el" -> El
      | "en" -> En
      | "es" -> Es
      | "eu" -> Eu
      | "fa" -> Fa
      | "fi" -> Fi
      | "fr" -> Fr
      | "ga" -> Ga
      | "gl" -> Gl
      | "he" -> He
      | "hi" -> Hi
      | "hu" -> Hu
      | "hy" -> Hy
      | "id" -> Id
      | "it" -> It
      | "ja" -> Ja
      | "ko" -> Ko
      | "lv" -> Lv
      | "mul" -> Mul
      | "nl" -> Nl
      | "no" -> No
      | "pt" -> Pt
      | "ro" -> Ro
      | "ru" -> Ru
      | "sv" -> Sv
      | "th" -> Th
      | "tr" -> Tr
      | "zh-Hans" -> Zh_Hans
      | "zh-Hant" -> Zh_Hant
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AnalysisSchemeLanguage" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AnalysisSchemeLanguage" j)
    let to_json = simple_to_json to_value
  end
module PartitionInstanceType =
  struct
    type nonrec t =
      | Search_m1_small 
      | Search_m1_large 
      | Search_m2_xlarge 
      | Search_m2_2xlarge 
      | Search_m3_medium 
      | Search_m3_large 
      | Search_m3_xlarge 
      | Search_m3_2xlarge 
      | Search_small 
      | Search_medium 
      | Search_large 
      | Search_xlarge 
      | Search_2xlarge 
      | Search_previousgeneration_small 
      | Search_previousgeneration_large 
      | Search_previousgeneration_xlarge 
      | Search_previousgeneration_2xlarge 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Search_m1_small -> "search.m1.small"
      | Search_m1_large -> "search.m1.large"
      | Search_m2_xlarge -> "search.m2.xlarge"
      | Search_m2_2xlarge -> "search.m2.2xlarge"
      | Search_m3_medium -> "search.m3.medium"
      | Search_m3_large -> "search.m3.large"
      | Search_m3_xlarge -> "search.m3.xlarge"
      | Search_m3_2xlarge -> "search.m3.2xlarge"
      | Search_small -> "search.small"
      | Search_medium -> "search.medium"
      | Search_large -> "search.large"
      | Search_xlarge -> "search.xlarge"
      | Search_2xlarge -> "search.2xlarge"
      | Search_previousgeneration_small -> "search.previousgeneration.small"
      | Search_previousgeneration_large -> "search.previousgeneration.large"
      | Search_previousgeneration_xlarge ->
          "search.previousgeneration.xlarge"
      | Search_previousgeneration_2xlarge ->
          "search.previousgeneration.2xlarge"
      | Non_static_id s -> s
    let of_string =
      function
      | "search.m1.small" -> Search_m1_small
      | "search.m1.large" -> Search_m1_large
      | "search.m2.xlarge" -> Search_m2_xlarge
      | "search.m2.2xlarge" -> Search_m2_2xlarge
      | "search.m3.medium" -> Search_m3_medium
      | "search.m3.large" -> Search_m3_large
      | "search.m3.xlarge" -> Search_m3_xlarge
      | "search.m3.2xlarge" -> Search_m3_2xlarge
      | "search.small" -> Search_small
      | "search.medium" -> Search_medium
      | "search.large" -> Search_large
      | "search.xlarge" -> Search_xlarge
      | "search.2xlarge" -> Search_2xlarge
      | "search.previousgeneration.small" -> Search_previousgeneration_small
      | "search.previousgeneration.large" -> Search_previousgeneration_large
      | "search.previousgeneration.xlarge" ->
          Search_previousgeneration_xlarge
      | "search.previousgeneration.2xlarge" ->
          Search_previousgeneration_2xlarge
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PartitionInstanceType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PartitionInstanceType" j)
    let to_json = simple_to_json to_value
  end
module TLSSecurityPolicy =
  struct
    type nonrec t =
      | Policy_Min_TLS_1_0_2019_07 
      | Policy_Min_TLS_1_2_2019_07 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Policy_Min_TLS_1_0_2019_07 -> "Policy-Min-TLS-1-0-2019-07"
      | Policy_Min_TLS_1_2_2019_07 -> "Policy-Min-TLS-1-2-2019-07"
      | Non_static_id s -> s
    let of_string =
      function
      | "Policy-Min-TLS-1-0-2019-07" -> Policy_Min_TLS_1_0_2019_07
      | "Policy-Min-TLS-1-2-2019-07" -> Policy_Min_TLS_1_2_2019_07
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TLSSecurityPolicy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TLSSecurityPolicy" j)
    let to_json = simple_to_json to_value
  end
module OptionStatus =
  struct
    type nonrec t =
      {
      creationDate: UpdateTimestamp.t option
        [@ocaml.doc "A timestamp for when this option was created."];
      updateDate: UpdateTimestamp.t option
        [@ocaml.doc "A timestamp for when this option was last updated."];
      updateVersion: UIntValue.t option
        [@ocaml.doc
          "A unique integer that indicates when this option was last updated."];
      state: OptionState.t option
        [@ocaml.doc
          "The state of processing a change to an option. Possible values: RequiresIndexDocuments: the option's latest value will not be deployed until IndexDocuments has been called and indexing is complete. Processing: the option's latest value is in the process of being activated. Active: the option's latest value is completely deployed. FailedToValidate: the option value is not compatible with the domain's data and cannot be used to index the data. You must either modify the option value or update or remove the incompatible documents."];
      pendingDeletion: Boolean.t option
        [@ocaml.doc
          "Indicates that the option will be deleted once processing is complete."]}
    let make ?creationDate =
      fun ?updateDate ->
        fun ?updateVersion ->
          fun ?state ->
            fun ?pendingDeletion ->
              fun () ->
                {
                  creationDate;
                  updateDate;
                  updateVersion;
                  state;
                  pendingDeletion
                }
    let to_value x =
      structure_to_value
        [("CreationDate",
           (Option.map x.creationDate ~f:UpdateTimestamp.to_value));
        ("UpdateDate", (Option.map x.updateDate ~f:UpdateTimestamp.to_value));
        ("UpdateVersion", (Option.map x.updateVersion ~f:UIntValue.to_value));
        ("State", (Option.map x.state ~f:OptionState.to_value));
        ("PendingDeletion",
          (Option.map x.pendingDeletion ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pendingDeletion =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "PendingDeletion") in
      let state =
        (Option.map ~f:OptionState.of_xml) (Xml.child xml_arg0 "State") in
      let updateVersion =
        (Option.map ~f:UIntValue.of_xml) (Xml.child xml_arg0 "UpdateVersion") in
      let updateDate =
        (Option.map ~f:UpdateTimestamp.of_xml)
          (Xml.child xml_arg0 "UpdateDate") in
      let creationDate =
        (Option.map ~f:UpdateTimestamp.of_xml)
          (Xml.child xml_arg0 "CreationDate") in
      make ?pendingDeletion ?state ?updateVersion ?updateDate ?creationDate
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pendingDeletion =
        field_map json__ "PendingDeletion" Boolean.of_json in
      let state = field_map json__ "State" OptionState.of_json in
      let updateVersion = field_map json__ "UpdateVersion" UIntValue.of_json in
      let updateDate = field_map json__ "UpdateDate" UpdateTimestamp.of_json in
      let creationDate =
        field_map json__ "CreationDate" UpdateTimestamp.of_json in
      make ?pendingDeletion ?state ?updateVersion ?updateDate ?creationDate
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The status of domain configuration option."]
module Suggester =
  struct
    type nonrec t =
      {
      suggesterName: StandardName.t ;
      documentSuggesterOptions: DocumentSuggesterOptions.t }
    let context_ = "Suggester"
    let make ~suggesterName =
      fun ~documentSuggesterOptions ->
        fun () -> { suggesterName; documentSuggesterOptions }
    let to_value x =
      structure_to_value
        [("SuggesterName", (Some (StandardName.to_value x.suggesterName)));
        ("DocumentSuggesterOptions",
          (Some
             (DocumentSuggesterOptions.to_value x.documentSuggesterOptions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let documentSuggesterOptions =
        DocumentSuggesterOptions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DocumentSuggesterOptions") in
      let suggesterName =
        StandardName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SuggesterName") in
      make ~documentSuggesterOptions ~suggesterName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let documentSuggesterOptions =
        field_map_exn json__ "DocumentSuggesterOptions"
          DocumentSuggesterOptions.of_json in
      let suggesterName =
        field_map_exn json__ "SuggesterName" StandardName.of_json in
      make ~documentSuggesterOptions ~suggesterName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration information for a search suggester. Each suggester has a unique name and specifies the text field you want to use for suggestions. The following options can be configured for a suggester: FuzzyMatching, SortExpression."]
module IndexField =
  struct
    type nonrec t =
      {
      indexFieldName: DynamicFieldName.t
        [@ocaml.doc
          "A string that represents the name of an index field. CloudSearch supports regular index fields as well as dynamic fields. A dynamic field's name defines a pattern that begins or ends with a wildcard. Any document fields that don't map to a regular index field but do match a dynamic field's pattern are configured with the dynamic field's indexing options. Regular field names begin with a letter and can contain the following characters: a-z (lowercase), 0-9, and _ (underscore). Dynamic field names must begin or end with a wildcard (*). The wildcard can also be the only character in a dynamic field name. Multiple wildcards, and wildcards embedded within a string are not supported. The name score is reserved and cannot be used as a field name. To reference a document's ID, you can use the name _id."];
      indexFieldType: IndexFieldType.t ;
      intOptions: IntOptions.t option ;
      doubleOptions: DoubleOptions.t option ;
      literalOptions: LiteralOptions.t option ;
      textOptions: TextOptions.t option ;
      dateOptions: DateOptions.t option ;
      latLonOptions: LatLonOptions.t option ;
      intArrayOptions: IntArrayOptions.t option ;
      doubleArrayOptions: DoubleArrayOptions.t option ;
      literalArrayOptions: LiteralArrayOptions.t option ;
      textArrayOptions: TextArrayOptions.t option ;
      dateArrayOptions: DateArrayOptions.t option }
    let context_ = "IndexField"
    let make ?intOptions =
      fun ?doubleOptions ->
        fun ?literalOptions ->
          fun ?textOptions ->
            fun ?dateOptions ->
              fun ?latLonOptions ->
                fun ?intArrayOptions ->
                  fun ?doubleArrayOptions ->
                    fun ?literalArrayOptions ->
                      fun ?textArrayOptions ->
                        fun ?dateArrayOptions ->
                          fun ~indexFieldName ->
                            fun ~indexFieldType ->
                              fun () ->
                                {
                                  intOptions;
                                  doubleOptions;
                                  literalOptions;
                                  textOptions;
                                  dateOptions;
                                  latLonOptions;
                                  intArrayOptions;
                                  doubleArrayOptions;
                                  literalArrayOptions;
                                  textArrayOptions;
                                  dateArrayOptions;
                                  indexFieldName;
                                  indexFieldType
                                }
    let to_value x =
      structure_to_value
        [("IndexFieldName",
           (Some (DynamicFieldName.to_value x.indexFieldName)));
        ("IndexFieldType", (Some (IndexFieldType.to_value x.indexFieldType)));
        ("IntOptions", (Option.map x.intOptions ~f:IntOptions.to_value));
        ("DoubleOptions",
          (Option.map x.doubleOptions ~f:DoubleOptions.to_value));
        ("LiteralOptions",
          (Option.map x.literalOptions ~f:LiteralOptions.to_value));
        ("TextOptions", (Option.map x.textOptions ~f:TextOptions.to_value));
        ("DateOptions", (Option.map x.dateOptions ~f:DateOptions.to_value));
        ("LatLonOptions",
          (Option.map x.latLonOptions ~f:LatLonOptions.to_value));
        ("IntArrayOptions",
          (Option.map x.intArrayOptions ~f:IntArrayOptions.to_value));
        ("DoubleArrayOptions",
          (Option.map x.doubleArrayOptions ~f:DoubleArrayOptions.to_value));
        ("LiteralArrayOptions",
          (Option.map x.literalArrayOptions ~f:LiteralArrayOptions.to_value));
        ("TextArrayOptions",
          (Option.map x.textArrayOptions ~f:TextArrayOptions.to_value));
        ("DateArrayOptions",
          (Option.map x.dateArrayOptions ~f:DateArrayOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dateArrayOptions =
        (Option.map ~f:DateArrayOptions.of_xml)
          (Xml.child xml_arg0 "DateArrayOptions") in
      let textArrayOptions =
        (Option.map ~f:TextArrayOptions.of_xml)
          (Xml.child xml_arg0 "TextArrayOptions") in
      let literalArrayOptions =
        (Option.map ~f:LiteralArrayOptions.of_xml)
          (Xml.child xml_arg0 "LiteralArrayOptions") in
      let doubleArrayOptions =
        (Option.map ~f:DoubleArrayOptions.of_xml)
          (Xml.child xml_arg0 "DoubleArrayOptions") in
      let intArrayOptions =
        (Option.map ~f:IntArrayOptions.of_xml)
          (Xml.child xml_arg0 "IntArrayOptions") in
      let latLonOptions =
        (Option.map ~f:LatLonOptions.of_xml)
          (Xml.child xml_arg0 "LatLonOptions") in
      let dateOptions =
        (Option.map ~f:DateOptions.of_xml) (Xml.child xml_arg0 "DateOptions") in
      let textOptions =
        (Option.map ~f:TextOptions.of_xml) (Xml.child xml_arg0 "TextOptions") in
      let literalOptions =
        (Option.map ~f:LiteralOptions.of_xml)
          (Xml.child xml_arg0 "LiteralOptions") in
      let doubleOptions =
        (Option.map ~f:DoubleOptions.of_xml)
          (Xml.child xml_arg0 "DoubleOptions") in
      let intOptions =
        (Option.map ~f:IntOptions.of_xml) (Xml.child xml_arg0 "IntOptions") in
      let indexFieldType =
        IndexFieldType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IndexFieldType") in
      let indexFieldName =
        DynamicFieldName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IndexFieldName") in
      make ?dateArrayOptions ?textArrayOptions ?literalArrayOptions
        ?doubleArrayOptions ?intArrayOptions ?latLonOptions ?dateOptions
        ?textOptions ?literalOptions ?doubleOptions ?intOptions
        ~indexFieldType ~indexFieldName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dateArrayOptions =
        field_map json__ "DateArrayOptions" DateArrayOptions.of_json in
      let textArrayOptions =
        field_map json__ "TextArrayOptions" TextArrayOptions.of_json in
      let literalArrayOptions =
        field_map json__ "LiteralArrayOptions" LiteralArrayOptions.of_json in
      let doubleArrayOptions =
        field_map json__ "DoubleArrayOptions" DoubleArrayOptions.of_json in
      let intArrayOptions =
        field_map json__ "IntArrayOptions" IntArrayOptions.of_json in
      let latLonOptions =
        field_map json__ "LatLonOptions" LatLonOptions.of_json in
      let dateOptions = field_map json__ "DateOptions" DateOptions.of_json in
      let textOptions = field_map json__ "TextOptions" TextOptions.of_json in
      let literalOptions =
        field_map json__ "LiteralOptions" LiteralOptions.of_json in
      let doubleOptions =
        field_map json__ "DoubleOptions" DoubleOptions.of_json in
      let intOptions = field_map json__ "IntOptions" IntOptions.of_json in
      let indexFieldType =
        field_map_exn json__ "IndexFieldType" IndexFieldType.of_json in
      let indexFieldName =
        field_map_exn json__ "IndexFieldName" DynamicFieldName.of_json in
      make ?dateArrayOptions ?textArrayOptions ?literalArrayOptions
        ?doubleArrayOptions ?intArrayOptions ?latLonOptions ?dateOptions
        ?textOptions ?literalOptions ?doubleOptions ?intOptions
        ~indexFieldType ~indexFieldName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration information for a field in the index, including its name, type, and options. The supported options depend on the IndexFieldType."]
module Expression =
  struct
    type nonrec t =
      {
      expressionName: StandardName.t ;
      expressionValue: ExpressionValue.t }
    let context_ = "Expression"
    let make ~expressionName =
      fun ~expressionValue -> fun () -> { expressionName; expressionValue }
    let to_value x =
      structure_to_value
        [("ExpressionName", (Some (StandardName.to_value x.expressionName)));
        ("ExpressionValue",
          (Some (ExpressionValue.to_value x.expressionValue)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expressionValue =
        ExpressionValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExpressionValue") in
      let expressionName =
        StandardName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExpressionName") in
      make ~expressionValue ~expressionName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expressionValue =
        field_map_exn json__ "ExpressionValue" ExpressionValue.of_json in
      let expressionName =
        field_map_exn json__ "ExpressionName" StandardName.of_json in
      make ~expressionValue ~expressionName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A named expression that can be evaluated at search time. Can be used to sort the search results, define other expressions, or return computed information in the search results."]
module ARN =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The Amazon Resource Name (ARN) of the search domain. See Identifiers for IAM Entities in Using AWS Identity and Access Management for more information."]
    let context_ = "ARN"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ARN" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The Amazon Resource Name (ARN) of the search domain. See Identifiers for IAM Entities in Using AWS Identity and Access Management for more information."]
module DomainId =
  struct
    type nonrec t = string[@@ocaml.doc
                            "An internally generated unique identifier for a domain."]
    let context_ = "DomainId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DomainId" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "An internally generated unique identifier for a domain."]
module DomainName =
  struct
    type nonrec t = string[@@ocaml.doc
                            "A string that represents the name of a domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen)."]
    let context_ = "DomainName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:28) >>=
                  (fun () -> check_pattern i ~pattern:"[a-z][a-z0-9\\-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DomainName" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "A string that represents the name of a domain. Domain names are unique across the domains owned by an account within an AWS region. Domain names start with a letter or number and can contain the following characters: a-z (lowercase), 0-9, and - (hyphen)."]
module InstanceCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for InstanceCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Limits =
  struct
    type nonrec t =
      {
      maximumReplicationCount: MaximumReplicationCount.t option ;
      maximumPartitionCount: MaximumPartitionCount.t option }
    let make ?maximumReplicationCount =
      fun ?maximumPartitionCount ->
        fun () -> { maximumReplicationCount; maximumPartitionCount }
    let to_value x =
      structure_to_value
        [("MaximumReplicationCount",
           (Option.map x.maximumReplicationCount
              ~f:MaximumReplicationCount.to_value));
        ("MaximumPartitionCount",
          (Option.map x.maximumPartitionCount
             ~f:MaximumPartitionCount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maximumPartitionCount =
        (Option.map ~f:MaximumPartitionCount.of_xml)
          (Xml.child xml_arg0 "MaximumPartitionCount") in
      let maximumReplicationCount =
        (Option.map ~f:MaximumReplicationCount.of_xml)
          (Xml.child xml_arg0 "MaximumReplicationCount") in
      make ?maximumPartitionCount ?maximumReplicationCount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maximumPartitionCount =
        field_map json__ "MaximumPartitionCount"
          MaximumPartitionCount.of_json in
      let maximumReplicationCount =
        field_map json__ "MaximumReplicationCount"
          MaximumReplicationCount.of_json in
      make ?maximumPartitionCount ?maximumReplicationCount ()
    let to_json v = composed_to_json to_value v
  end
module PartitionCount =
  struct
    type nonrec t = int[@@ocaml.doc
                         "The number of partitions used to hold the domain's index."]
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for PartitionCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The number of partitions used to hold the domain's index."]
module SearchInstanceType =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The instance type (such as search.m1.small) that is being used to process search requests."]
    let context_ = "SearchInstanceType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SearchInstanceType" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The instance type (such as search.m1.small) that is being used to process search requests."]
module ServiceEndpoint =
  struct
    type nonrec t = {
      endpoint: ServiceUrl.t option }
    let make ?endpoint = fun () -> { endpoint }
    let to_value x =
      structure_to_value
        [("Endpoint", (Option.map x.endpoint ~f:ServiceUrl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endpoint =
        (Option.map ~f:ServiceUrl.of_xml) (Xml.child xml_arg0 "Endpoint") in
      make ?endpoint ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endpoint = field_map json__ "Endpoint" ServiceUrl.of_json in
      make ?endpoint ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The endpoint to which service requests can be submitted."]
module AnalysisScheme =
  struct
    type nonrec t =
      {
      analysisSchemeName: StandardName.t ;
      analysisSchemeLanguage: AnalysisSchemeLanguage.t ;
      analysisOptions: AnalysisOptions.t option }
    let context_ = "AnalysisScheme"
    let make ?analysisOptions =
      fun ~analysisSchemeName ->
        fun ~analysisSchemeLanguage ->
          fun () ->
            { analysisOptions; analysisSchemeName; analysisSchemeLanguage }
    let to_value x =
      structure_to_value
        [("AnalysisSchemeName",
           (Some (StandardName.to_value x.analysisSchemeName)));
        ("AnalysisSchemeLanguage",
          (Some (AnalysisSchemeLanguage.to_value x.analysisSchemeLanguage)));
        ("AnalysisOptions",
          (Option.map x.analysisOptions ~f:AnalysisOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let analysisOptions =
        (Option.map ~f:AnalysisOptions.of_xml)
          (Xml.child xml_arg0 "AnalysisOptions") in
      let analysisSchemeLanguage =
        AnalysisSchemeLanguage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AnalysisSchemeLanguage") in
      let analysisSchemeName =
        StandardName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AnalysisSchemeName") in
      make ?analysisOptions ~analysisSchemeLanguage ~analysisSchemeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let analysisOptions =
        field_map json__ "AnalysisOptions" AnalysisOptions.of_json in
      let analysisSchemeLanguage =
        field_map_exn json__ "AnalysisSchemeLanguage"
          AnalysisSchemeLanguage.of_json in
      let analysisSchemeName =
        field_map_exn json__ "AnalysisSchemeName" StandardName.of_json in
      make ?analysisOptions ~analysisSchemeLanguage ~analysisSchemeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration information for an analysis scheme. Each analysis scheme has a unique name and specifies the language of the text to be processed. The following options can be configured for an analysis scheme: Synonyms, Stopwords, StemmingDictionary, JapaneseTokenizationDictionary and AlgorithmicStemming."]
module PolicyDocument =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Access rules for a domain's document or search service endpoints. For more information, see Configuring Access for a Search Domain in the Amazon CloudSearch Developer Guide. The maximum size of a policy document is 100 KB."]
    let context_ = "PolicyDocument"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyDocument" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Access rules for a domain's document or search service endpoints. For more information, see Configuring Access for a Search Domain in the Amazon CloudSearch Developer Guide. The maximum size of a policy document is 100 KB."]
module ErrorCode =
  struct
    type nonrec t = string[@@ocaml.doc
                            "A machine-parsable string error or warning code."]
    let context_ = "ErrorCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorCode" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "A machine-parsable string error or warning code."]
module ErrorMessage =
  struct
    type nonrec t = string[@@ocaml.doc
                            "A human-readable string error or warning message."]
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "A human-readable string error or warning message."]
module ScalingParameters =
  struct
    type nonrec t =
      {
      desiredInstanceType: PartitionInstanceType.t option
        [@ocaml.doc
          "The instance type that you want to preconfigure for your domain. For example, search.m1.small."];
      desiredReplicationCount: UIntValue.t option
        [@ocaml.doc
          "The number of replicas you want to preconfigure for each index partition."];
      desiredPartitionCount: UIntValue.t option
        [@ocaml.doc
          "The number of partitions you want to preconfigure for your domain. Only valid when you select m2.2xlarge as the desired instance type."]}
    let make ?desiredInstanceType =
      fun ?desiredReplicationCount ->
        fun ?desiredPartitionCount ->
          fun () ->
            {
              desiredInstanceType;
              desiredReplicationCount;
              desiredPartitionCount
            }
    let to_value x =
      structure_to_value
        [("DesiredInstanceType",
           (Option.map x.desiredInstanceType
              ~f:PartitionInstanceType.to_value));
        ("DesiredReplicationCount",
          (Option.map x.desiredReplicationCount ~f:UIntValue.to_value));
        ("DesiredPartitionCount",
          (Option.map x.desiredPartitionCount ~f:UIntValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let desiredPartitionCount =
        (Option.map ~f:UIntValue.of_xml)
          (Xml.child xml_arg0 "DesiredPartitionCount") in
      let desiredReplicationCount =
        (Option.map ~f:UIntValue.of_xml)
          (Xml.child xml_arg0 "DesiredReplicationCount") in
      let desiredInstanceType =
        (Option.map ~f:PartitionInstanceType.of_xml)
          (Xml.child xml_arg0 "DesiredInstanceType") in
      make ?desiredPartitionCount ?desiredReplicationCount
        ?desiredInstanceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let desiredPartitionCount =
        field_map json__ "DesiredPartitionCount" UIntValue.of_json in
      let desiredReplicationCount =
        field_map json__ "DesiredReplicationCount" UIntValue.of_json in
      let desiredInstanceType =
        field_map json__ "DesiredInstanceType" PartitionInstanceType.of_json in
      make ?desiredPartitionCount ?desiredReplicationCount
        ?desiredInstanceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The desired instance type and desired number of replicas of each index partition."]
module DomainEndpointOptions =
  struct
    type nonrec t =
      {
      enforceHTTPS: Boolean.t option
        [@ocaml.doc "Whether the domain is HTTPS only enabled."];
      tLSSecurityPolicy: TLSSecurityPolicy.t option
        [@ocaml.doc "The minimum required TLS version"]}
    let make ?enforceHTTPS =
      fun ?tLSSecurityPolicy -> fun () -> { enforceHTTPS; tLSSecurityPolicy }
    let to_value x =
      structure_to_value
        [("EnforceHTTPS", (Option.map x.enforceHTTPS ~f:Boolean.to_value));
        ("TLSSecurityPolicy",
          (Option.map x.tLSSecurityPolicy ~f:TLSSecurityPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tLSSecurityPolicy =
        (Option.map ~f:TLSSecurityPolicy.of_xml)
          (Xml.child xml_arg0 "TLSSecurityPolicy") in
      let enforceHTTPS =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "EnforceHTTPS") in
      make ?tLSSecurityPolicy ?enforceHTTPS ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tLSSecurityPolicy =
        field_map json__ "TLSSecurityPolicy" TLSSecurityPolicy.of_json in
      let enforceHTTPS = field_map json__ "EnforceHTTPS" Boolean.of_json in
      make ?tLSSecurityPolicy ?enforceHTTPS ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The domain's endpoint options."]
module MultiAZ =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module APIVersion =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The Amazon CloudSearch API version for a domain: 2011-02-01 or 2013-01-01."]
    let context_ = "APIVersion"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"APIVersion" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The Amazon CloudSearch API version for a domain: 2011-02-01 or 2013-01-01."]
module SuggesterStatus =
  struct
    type nonrec t =
      {
      options: Suggester.t option ;
      status: OptionStatus.t option }
    let make ?options = fun ?status -> fun () -> { options; status }
    let to_value x =
      structure_to_value
        [("Options", (Option.map x.options ~f:Suggester.to_value));
        ("Status", (Option.map x.status ~f:OptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:OptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let options =
        (Option.map ~f:Suggester.of_xml) (Xml.child xml_arg0 "Options") in
      make ?status ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" OptionStatus.of_json in
      let options = field_map json__ "Options" Suggester.of_json in
      make ?status ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The value of a Suggester and its current status."]
module IndexFieldStatus =
  struct
    type nonrec t =
      {
      options: IndexField.t option ;
      status: OptionStatus.t option }
    let make ?options = fun ?status -> fun () -> { options; status }
    let to_value x =
      structure_to_value
        [("Options", (Option.map x.options ~f:IndexField.to_value));
        ("Status", (Option.map x.status ~f:OptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:OptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let options =
        (Option.map ~f:IndexField.of_xml) (Xml.child xml_arg0 "Options") in
      make ?status ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" OptionStatus.of_json in
      let options = field_map json__ "Options" IndexField.of_json in
      make ?status ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The value of an IndexField and its current status."]
module ExpressionStatus =
  struct
    type nonrec t =
      {
      options: Expression.t option
        [@ocaml.doc
          "The expression that is evaluated for sorting while processing a search request."];
      status: OptionStatus.t option }
    let make ?options = fun ?status -> fun () -> { options; status }
    let to_value x =
      structure_to_value
        [("Options", (Option.map x.options ~f:Expression.to_value));
        ("Status", (Option.map x.status ~f:OptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:OptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let options =
        (Option.map ~f:Expression.of_xml) (Xml.child xml_arg0 "Options") in
      make ?status ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" OptionStatus.of_json in
      let options = field_map json__ "Options" Expression.of_json in
      make ?status ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The value of an Expression and its current status."]
module DomainStatus =
  struct
    type nonrec t =
      {
      domainId: DomainId.t option ;
      domainName: DomainName.t option ;
      aRN: ARN.t option ;
      created: Boolean.t option
        [@ocaml.doc
          "True if the search domain is created. It can take several minutes to initialize a domain when CreateDomain is called. Newly created search domains are returned from DescribeDomains with a false value for Created until domain creation is complete."];
      deleted: Boolean.t option
        [@ocaml.doc
          "True if the search domain has been deleted. The system must clean up resources dedicated to the search domain when DeleteDomain is called. Newly deleted search domains are returned from DescribeDomains with a true value for IsDeleted for several minutes until resource cleanup is complete."];
      docService: ServiceEndpoint.t option
        [@ocaml.doc
          "The service endpoint for updating documents in a search domain."];
      searchService: ServiceEndpoint.t option
        [@ocaml.doc
          "The service endpoint for requesting search results from a search domain."];
      requiresIndexDocuments: Boolean.t option
        [@ocaml.doc
          "True if IndexDocuments needs to be called to activate the current domain configuration."];
      processing: Boolean.t option
        [@ocaml.doc
          "True if processing is being done to activate the current domain configuration."];
      searchInstanceType: SearchInstanceType.t option
        [@ocaml.doc
          "The instance type that is being used to process search requests."];
      searchPartitionCount: PartitionCount.t option
        [@ocaml.doc
          "The number of partitions across which the search index is spread."];
      searchInstanceCount: InstanceCount.t option
        [@ocaml.doc
          "The number of search instances that are available to process search requests."];
      limits: Limits.t option }
    let make ?domainId =
      fun ?domainName ->
        fun ?aRN ->
          fun ?created ->
            fun ?deleted ->
              fun ?docService ->
                fun ?searchService ->
                  fun ?requiresIndexDocuments ->
                    fun ?processing ->
                      fun ?searchInstanceType ->
                        fun ?searchPartitionCount ->
                          fun ?searchInstanceCount ->
                            fun ?limits ->
                              fun () ->
                                {
                                  domainId;
                                  domainName;
                                  aRN;
                                  created;
                                  deleted;
                                  docService;
                                  searchService;
                                  requiresIndexDocuments;
                                  processing;
                                  searchInstanceType;
                                  searchPartitionCount;
                                  searchInstanceCount;
                                  limits
                                }
    let to_value x =
      structure_to_value
        [("DomainId", (Option.map x.domainId ~f:DomainId.to_value));
        ("DomainName", (Option.map x.domainName ~f:DomainName.to_value));
        ("ARN", (Option.map x.aRN ~f:ARN.to_value));
        ("Created", (Option.map x.created ~f:Boolean.to_value));
        ("Deleted", (Option.map x.deleted ~f:Boolean.to_value));
        ("DocService", (Option.map x.docService ~f:ServiceEndpoint.to_value));
        ("SearchService",
          (Option.map x.searchService ~f:ServiceEndpoint.to_value));
        ("RequiresIndexDocuments",
          (Option.map x.requiresIndexDocuments ~f:Boolean.to_value));
        ("Processing", (Option.map x.processing ~f:Boolean.to_value));
        ("SearchInstanceType",
          (Option.map x.searchInstanceType ~f:SearchInstanceType.to_value));
        ("SearchPartitionCount",
          (Option.map x.searchPartitionCount ~f:PartitionCount.to_value));
        ("SearchInstanceCount",
          (Option.map x.searchInstanceCount ~f:InstanceCount.to_value));
        ("Limits", (Option.map x.limits ~f:Limits.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let limits =
        (Option.map ~f:Limits.of_xml) (Xml.child xml_arg0 "Limits") in
      let searchInstanceCount =
        (Option.map ~f:InstanceCount.of_xml)
          (Xml.child xml_arg0 "SearchInstanceCount") in
      let searchPartitionCount =
        (Option.map ~f:PartitionCount.of_xml)
          (Xml.child xml_arg0 "SearchPartitionCount") in
      let searchInstanceType =
        (Option.map ~f:SearchInstanceType.of_xml)
          (Xml.child xml_arg0 "SearchInstanceType") in
      let processing =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Processing") in
      let requiresIndexDocuments =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "RequiresIndexDocuments") in
      let searchService =
        (Option.map ~f:ServiceEndpoint.of_xml)
          (Xml.child xml_arg0 "SearchService") in
      let docService =
        (Option.map ~f:ServiceEndpoint.of_xml)
          (Xml.child xml_arg0 "DocService") in
      let deleted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Deleted") in
      let created =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Created") in
      let aRN = (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ARN") in
      let domainName =
        (Option.map ~f:DomainName.of_xml) (Xml.child xml_arg0 "DomainName") in
      let domainId =
        (Option.map ~f:DomainId.of_xml) (Xml.child xml_arg0 "DomainId") in
      make ?limits ?searchInstanceCount ?searchPartitionCount
        ?searchInstanceType ?processing ?requiresIndexDocuments
        ?searchService ?docService ?deleted ?created ?aRN ?domainName
        ?domainId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let limits = field_map json__ "Limits" Limits.of_json in
      let searchInstanceCount =
        field_map json__ "SearchInstanceCount" InstanceCount.of_json in
      let searchPartitionCount =
        field_map json__ "SearchPartitionCount" PartitionCount.of_json in
      let searchInstanceType =
        field_map json__ "SearchInstanceType" SearchInstanceType.of_json in
      let processing = field_map json__ "Processing" Boolean.of_json in
      let requiresIndexDocuments =
        field_map json__ "RequiresIndexDocuments" Boolean.of_json in
      let searchService =
        field_map json__ "SearchService" ServiceEndpoint.of_json in
      let docService = field_map json__ "DocService" ServiceEndpoint.of_json in
      let deleted = field_map json__ "Deleted" Boolean.of_json in
      let created = field_map json__ "Created" Boolean.of_json in
      let aRN = field_map json__ "ARN" ARN.of_json in
      let domainName = field_map json__ "DomainName" DomainName.of_json in
      let domainId = field_map json__ "DomainId" DomainId.of_json in
      make ?limits ?searchInstanceCount ?searchPartitionCount
        ?searchInstanceType ?processing ?requiresIndexDocuments
        ?searchService ?docService ?deleted ?created ?aRN ?domainName
        ?domainId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The current status of the search domain."]
module AnalysisSchemeStatus =
  struct
    type nonrec t =
      {
      options: AnalysisScheme.t option ;
      status: OptionStatus.t option }
    let make ?options = fun ?status -> fun () -> { options; status }
    let to_value x =
      structure_to_value
        [("Options", (Option.map x.options ~f:AnalysisScheme.to_value));
        ("Status", (Option.map x.status ~f:OptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:OptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let options =
        (Option.map ~f:AnalysisScheme.of_xml) (Xml.child xml_arg0 "Options") in
      make ?status ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" OptionStatus.of_json in
      let options = field_map json__ "Options" AnalysisScheme.of_json in
      make ?status ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The status and configuration of an AnalysisScheme."]
module AccessPoliciesStatus =
  struct
    type nonrec t =
      {
      options: PolicyDocument.t option ;
      status: OptionStatus.t option }
    let make ?options = fun ?status -> fun () -> { options; status }
    let to_value x =
      structure_to_value
        [("Options", (Option.map x.options ~f:PolicyDocument.to_value));
        ("Status", (Option.map x.status ~f:OptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:OptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let options =
        (Option.map ~f:PolicyDocument.of_xml) (Xml.child xml_arg0 "Options") in
      make ?status ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" OptionStatus.of_json in
      let options = field_map json__ "Options" PolicyDocument.of_json in
      make ?status ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configured access rules for the domain's document and search endpoints, and the current status of those rules."]
module BaseException =
  struct
    type nonrec t =
      {
      code: ErrorCode.t option ;
      message: ErrorMessage.t option }
    let make ?code = fun ?message -> fun () -> { code; message }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:ErrorCode.to_value));
        ("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "Code") in
      make ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" ErrorMessage.of_json in
      let code = field_map json__ "Code" ErrorCode.of_json in
      make ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An error occurred while processing the request."]
module InternalException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An internal error occurred while processing the request. If this problem persists, report an issue from the Service Health Dashboard."]
module InvalidTypeException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was rejected because it specified an invalid type definition."]
module LimitExceededException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was rejected because a resource limit has already been met."]
module ResourceNotFoundException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was rejected because it attempted to reference a resource that does not exist."]
module ValidationException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was rejected because it has invalid parameters."]
module ScalingParametersStatus =
  struct
    type nonrec t =
      {
      options: ScalingParameters.t option ;
      status: OptionStatus.t option }
    let make ?options = fun ?status -> fun () -> { options; status }
    let to_value x =
      structure_to_value
        [("Options", (Option.map x.options ~f:ScalingParameters.to_value));
        ("Status", (Option.map x.status ~f:OptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:OptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let options =
        (Option.map ~f:ScalingParameters.of_xml)
          (Xml.child xml_arg0 "Options") in
      make ?status ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" OptionStatus.of_json in
      let options = field_map json__ "Options" ScalingParameters.of_json in
      make ?status ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The status and configuration of a search domain's scaling parameters."]
module DisabledOperationException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was rejected because it attempted an operation which is not enabled."]
module DomainEndpointOptionsStatus =
  struct
    type nonrec t =
      {
      options: DomainEndpointOptions.t option
        [@ocaml.doc "The domain endpoint options configured for the domain."];
      status: OptionStatus.t option
        [@ocaml.doc "The status of the configured domain endpoint options."]}
    let make ?options = fun ?status -> fun () -> { options; status }
    let to_value x =
      structure_to_value
        [("Options",
           (Option.map x.options ~f:DomainEndpointOptions.to_value));
        ("Status", (Option.map x.status ~f:OptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:OptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let options =
        (Option.map ~f:DomainEndpointOptions.of_xml)
          (Xml.child xml_arg0 "Options") in
      make ?status ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" OptionStatus.of_json in
      let options = field_map json__ "Options" DomainEndpointOptions.of_json in
      make ?status ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration and status of the domain's endpoint options."]
module AvailabilityOptionsStatus =
  struct
    type nonrec t =
      {
      options: MultiAZ.t option
        [@ocaml.doc "The availability options configured for the domain."];
      status: OptionStatus.t option }
    let make ?options = fun ?status -> fun () -> { options; status }
    let to_value x =
      structure_to_value
        [("Options", (Option.map x.options ~f:MultiAZ.to_value));
        ("Status", (Option.map x.status ~f:OptionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:OptionStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let options =
        (Option.map ~f:MultiAZ.of_xml) (Xml.child xml_arg0 "Options") in
      make ?status ?options ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "Status" OptionStatus.of_json in
      let options = field_map json__ "Options" MultiAZ.of_json in
      make ?status ?options ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The status and configuration of the domain's availability options."]
module DomainNameMap =
  struct
    type nonrec t = (DomainName.t * APIVersion.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((DomainName.of_string chopped),
                              (APIVersion.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (DomainName.to_value x) |>
                    (fun x -> (APIVersion.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:DomainName.of_string
        ~of_json:APIVersion.of_json j
    let to_json v = composed_to_json to_value v
  end
module FieldNameList =
  struct
    type nonrec t = FieldName.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FieldName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FieldName.of_xml)
    let of_json j =
      list_of_json ~kind:"FieldNameList" ~of_json:FieldName.of_json j
    let to_json v = composed_to_json to_value v
  end
module SuggesterStatusList =
  struct
    type nonrec t = SuggesterStatus.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SuggesterStatus.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SuggesterStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"SuggesterStatusList"
        ~of_json:SuggesterStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module StandardNameList =
  struct
    type nonrec t = StandardName.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:StandardName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StandardName.of_xml)
    let of_json j =
      list_of_json ~kind:"StandardNameList" ~of_json:StandardName.of_json j
    let to_json v = composed_to_json to_value v
  end
module IndexFieldStatusList =
  struct
    type nonrec t = IndexFieldStatus.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:IndexFieldStatus.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:IndexFieldStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"IndexFieldStatusList"
        ~of_json:IndexFieldStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module DynamicFieldNameList =
  struct
    type nonrec t = DynamicFieldName.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DynamicFieldName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DynamicFieldName.of_xml)
    let of_json j =
      list_of_json ~kind:"DynamicFieldNameList"
        ~of_json:DynamicFieldName.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExpressionStatusList =
  struct
    type nonrec t = ExpressionStatus.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExpressionStatus.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExpressionStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"ExpressionStatusList"
        ~of_json:ExpressionStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module DomainStatusList =
  struct
    type nonrec t = DomainStatus.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DomainStatus.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DomainStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"DomainStatusList" ~of_json:DomainStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module DomainNameList =
  struct
    type nonrec t = DomainName.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DomainName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DomainName.of_xml)
    let of_json j =
      list_of_json ~kind:"DomainNameList" ~of_json:DomainName.of_json j
    let to_json v = composed_to_json to_value v
  end
module AnalysisSchemeStatusList =
  struct
    type nonrec t = AnalysisSchemeStatus.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AnalysisSchemeStatus.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AnalysisSchemeStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"AnalysisSchemeStatusList"
        ~of_json:AnalysisSchemeStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceAlreadyExistsException =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request was rejected because it attempted to create a resource that already exists."]
module UpdateServiceAccessPoliciesResponse =
  struct
    type updateServiceAccessPoliciesResult =
      {
      accessPolicies: AccessPoliciesStatus.t option
        [@ocaml.doc "The access rules configured for the domain."]}
    and responseMetaData = unit
    and t =
      {
      updateServiceAccessPoliciesResult: updateServiceAccessPoliciesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "UpdateServiceAccessPoliciesResponse"
    let make ?accessPolicies =
      fun () ->
        {
          updateServiceAccessPoliciesResult = { accessPolicies };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.updateServiceAccessPoliciesResult in
      structure_to_wrapped_value
        [("AccessPolicies",
           (Option.map x.accessPolicies ~f:AccessPoliciesStatus.to_value))]
        ~wrapper:"UpdateServiceAccessPoliciesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "UpdateServiceAccessPoliciesResult" in
      let accessPolicies =
        (Option.map ~f:AccessPoliciesStatus.of_xml)
          (Xml.child xml_arg0 "AccessPolicies") in
      make ?accessPolicies ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accessPolicies =
        field_map json__ "AccessPolicies" AccessPoliciesStatus.of_json in
      make ?accessPolicies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of an UpdateServiceAccessPolicies request. Contains the new access policies."]
module UpdateServiceAccessPoliciesRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      accessPolicies: PolicyDocument.t
        [@ocaml.doc
          "The access rules you want to configure. These rules replace any existing rules."]}
    let context_ = "UpdateServiceAccessPoliciesRequest"
    let make ~domainName =
      fun ~accessPolicies -> fun () -> { domainName; accessPolicies }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("AccessPolicies", (Some (PolicyDocument.to_value x.accessPolicies)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accessPolicies =
        PolicyDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AccessPolicies") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~accessPolicies ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accessPolicies =
        field_map_exn json__ "AccessPolicies" PolicyDocument.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~accessPolicies ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the UpdateServiceAccessPolicies operation. Specifies the name of the domain you want to update and the access rules you want to configure."]
module UpdateScalingParametersResponse =
  struct
    type updateScalingParametersResult =
      {
      scalingParameters: ScalingParametersStatus.t option }
    and responseMetaData = unit
    and t =
      {
      updateScalingParametersResult: updateScalingParametersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "UpdateScalingParametersResponse"
    let make ?scalingParameters =
      fun () ->
        {
          updateScalingParametersResult = { scalingParameters };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.updateScalingParametersResult in
      structure_to_wrapped_value
        [("ScalingParameters",
           (Option.map x.scalingParameters
              ~f:ScalingParametersStatus.to_value))]
        ~wrapper:"UpdateScalingParametersResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "UpdateScalingParametersResult" in
      let scalingParameters =
        (Option.map ~f:ScalingParametersStatus.of_xml)
          (Xml.child xml_arg0 "ScalingParameters") in
      make ?scalingParameters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scalingParameters =
        field_map json__ "ScalingParameters" ScalingParametersStatus.of_json in
      make ?scalingParameters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a UpdateScalingParameters request. Contains the status of the newly-configured scaling parameters."]
module UpdateScalingParametersRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      scalingParameters: ScalingParameters.t }
    let context_ = "UpdateScalingParametersRequest"
    let make ~domainName =
      fun ~scalingParameters -> fun () -> { domainName; scalingParameters }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("ScalingParameters",
          (Some (ScalingParameters.to_value x.scalingParameters)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scalingParameters =
        ScalingParameters.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScalingParameters") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~scalingParameters ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scalingParameters =
        field_map_exn json__ "ScalingParameters" ScalingParameters.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~scalingParameters ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the UpdateScalingParameters operation. Specifies the name of the domain you want to update and the scaling parameters you want to configure."]
module UpdateDomainEndpointOptionsResponse =
  struct
    type updateDomainEndpointOptionsResult =
      {
      domainEndpointOptions: DomainEndpointOptionsStatus.t option
        [@ocaml.doc "The newly-configured domain endpoint options."]}
    and responseMetaData = unit
    and t =
      {
      updateDomainEndpointOptionsResult: updateDomainEndpointOptionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `DisabledOperationException of DisabledOperationException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "UpdateDomainEndpointOptionsResponse"
    let make ?domainEndpointOptions =
      fun () ->
        {
          updateDomainEndpointOptionsResult = { domainEndpointOptions };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "DisabledOperationException" ->
          `DisabledOperationException
            (DisabledOperationException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "DisabledOperationException" ->
          `DisabledOperationException (DisabledOperationException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `DisabledOperationException e ->
          `Assoc
            [("error", (`String "DisabledOperationException"));
            ("details", (DisabledOperationException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.updateDomainEndpointOptionsResult in
      structure_to_wrapped_value
        [("DomainEndpointOptions",
           (Option.map x.domainEndpointOptions
              ~f:DomainEndpointOptionsStatus.to_value))]
        ~wrapper:"UpdateDomainEndpointOptionsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "UpdateDomainEndpointOptionsResult" in
      let domainEndpointOptions =
        (Option.map ~f:DomainEndpointOptionsStatus.of_xml)
          (Xml.child xml_arg0 "DomainEndpointOptions") in
      make ?domainEndpointOptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainEndpointOptions =
        field_map json__ "DomainEndpointOptions"
          DomainEndpointOptionsStatus.of_json in
      make ?domainEndpointOptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a UpdateDomainEndpointOptions request. Contains the configuration and status of the domain's endpoint options."]
module UpdateDomainEndpointOptionsRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "A string that represents the name of a domain."];
      domainEndpointOptions: DomainEndpointOptions.t
        [@ocaml.doc
          "Whether to require that all requests to the domain arrive over HTTPS. We recommend Policy-Min-TLS-1-2-2019-07 for TLSSecurityPolicy. For compatibility with older clients, the default is Policy-Min-TLS-1-0-2019-07."]}
    let context_ = "UpdateDomainEndpointOptionsRequest"
    let make ~domainName =
      fun ~domainEndpointOptions ->
        fun () -> { domainName; domainEndpointOptions }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("DomainEndpointOptions",
          (Some (DomainEndpointOptions.to_value x.domainEndpointOptions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainEndpointOptions =
        DomainEndpointOptions.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainEndpointOptions") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~domainEndpointOptions ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainEndpointOptions =
        field_map_exn json__ "DomainEndpointOptions"
          DomainEndpointOptions.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~domainEndpointOptions ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the UpdateDomainEndpointOptions operation. Specifies the name of the domain you want to update and the domain endpoint options."]
module UpdateAvailabilityOptionsResponse =
  struct
    type updateAvailabilityOptionsResult =
      {
      availabilityOptions: AvailabilityOptionsStatus.t option
        [@ocaml.doc
          "The newly-configured availability options. Indicates whether Multi-AZ is enabled for the domain."]}
    and responseMetaData = unit
    and t =
      {
      updateAvailabilityOptionsResult: updateAvailabilityOptionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `DisabledOperationException of DisabledOperationException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "UpdateAvailabilityOptionsResponse"
    let make ?availabilityOptions =
      fun () ->
        {
          updateAvailabilityOptionsResult = { availabilityOptions };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "DisabledOperationException" ->
          `DisabledOperationException
            (DisabledOperationException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "DisabledOperationException" ->
          `DisabledOperationException (DisabledOperationException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `DisabledOperationException e ->
          `Assoc
            [("error", (`String "DisabledOperationException"));
            ("details", (DisabledOperationException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.updateAvailabilityOptionsResult in
      structure_to_wrapped_value
        [("AvailabilityOptions",
           (Option.map x.availabilityOptions
              ~f:AvailabilityOptionsStatus.to_value))]
        ~wrapper:"UpdateAvailabilityOptionsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "UpdateAvailabilityOptionsResult" in
      let availabilityOptions =
        (Option.map ~f:AvailabilityOptionsStatus.of_xml)
          (Xml.child xml_arg0 "AvailabilityOptions") in
      make ?availabilityOptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityOptions =
        field_map json__ "AvailabilityOptions"
          AvailabilityOptionsStatus.of_json in
      make ?availabilityOptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a UpdateAvailabilityOptions request. Contains the status of the domain's availability options."]
module UpdateAvailabilityOptionsRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      multiAZ: Boolean.t
        [@ocaml.doc
          "You expand an existing search domain to a second Availability Zone by setting the Multi-AZ option to true. Similarly, you can turn off the Multi-AZ option to downgrade the domain to a single Availability Zone by setting the Multi-AZ option to false."]}
    let context_ = "UpdateAvailabilityOptionsRequest"
    let make ~domainName = fun ~multiAZ -> fun () -> { domainName; multiAZ }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("MultiAZ", (Some (Boolean.to_value x.multiAZ)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let multiAZ =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "MultiAZ") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~multiAZ ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let multiAZ = field_map_exn json__ "MultiAZ" Boolean.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~multiAZ ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the UpdateAvailabilityOptions operation. Specifies the name of the domain you want to update and the Multi-AZ availability option."]
module ListDomainNamesResponse =
  struct
    type listDomainNamesResult =
      {
      domainNames: DomainNameMap.t option
        [@ocaml.doc "The names of the search domains owned by an account."]}
    and responseMetaData = unit
    and t =
      {
      listDomainNamesResult: listDomainNamesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListDomainNamesResponse"
    let make ?domainNames =
      fun () ->
        { listDomainNamesResult = { domainNames }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.listDomainNamesResult in
      structure_to_wrapped_value
        [("DomainNames",
           (Option.map x.domainNames ~f:DomainNameMap.to_value))]
        ~wrapper:"ListDomainNamesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "ListDomainNamesResult" in
      let domainNames =
        (Option.map ~f:DomainNameMap.of_xml)
          (Xml.child xml_arg0 "DomainNames") in
      make ?domainNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainNames = field_map json__ "DomainNames" DomainNameMap.of_json in
      make ?domainNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a ListDomainNames request. Contains a list of the domains owned by an account."]
module IndexDocumentsResponse =
  struct
    type indexDocumentsResult =
      {
      fieldNames: FieldNameList.t option
        [@ocaml.doc
          "The names of the fields that are currently being indexed."]}
    and responseMetaData = unit
    and t =
      {
      indexDocumentsResult: indexDocumentsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "IndexDocumentsResponse"
    let make ?fieldNames =
      fun () ->
        { indexDocumentsResult = { fieldNames }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.indexDocumentsResult in
      structure_to_wrapped_value
        [("FieldNames", (Option.map x.fieldNames ~f:FieldNameList.to_value))]
        ~wrapper:"IndexDocumentsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "IndexDocumentsResult" in
      let fieldNames =
        (Option.map ~f:FieldNameList.of_xml)
          (Xml.child xml_arg0 "FieldNames") in
      make ?fieldNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldNames = field_map json__ "FieldNames" FieldNameList.of_json in
      make ?fieldNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of an IndexDocuments request. Contains the status of the indexing operation, including the fields being indexed."]
module IndexDocumentsRequest =
  struct
    type nonrec t = {
      domainName: DomainName.t }
    let context_ = "IndexDocumentsRequest"
    let make ~domainName = fun () -> { domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the IndexDocuments operation. Specifies the name of the domain you want to re-index."]
module DescribeSuggestersResponse =
  struct
    type describeSuggestersResult =
      {
      suggesters: SuggesterStatusList.t option
        [@ocaml.doc
          "The suggesters configured for the domain specified in the request."]}
    and responseMetaData = unit
    and t =
      {
      describeSuggestersResult: describeSuggestersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeSuggestersResponse"
    let make ?suggesters =
      fun () ->
        { describeSuggestersResult = { suggesters }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeSuggestersResult in
      structure_to_wrapped_value
        [("Suggesters",
           (Option.map x.suggesters ~f:SuggesterStatusList.to_value))]
        ~wrapper:"DescribeSuggestersResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeSuggestersResult" in
      let suggesters =
        (Option.map ~f:SuggesterStatusList.of_xml)
          (Xml.child xml_arg0 "Suggesters") in
      make ?suggesters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let suggesters =
        field_map json__ "Suggesters" SuggesterStatusList.of_json in
      make ?suggesters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The result of a DescribeSuggesters request."]
module DescribeSuggestersRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "The name of the domain you want to describe."];
      suggesterNames: StandardNameList.t option
        [@ocaml.doc "The suggesters you want to describe."];
      deployed: Boolean.t option
        [@ocaml.doc
          "Whether to display the deployed configuration (true) or include any pending changes (false). Defaults to false."]}
    let context_ = "DescribeSuggestersRequest"
    let make ?suggesterNames =
      fun ?deployed ->
        fun ~domainName -> fun () -> { suggesterNames; deployed; domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("SuggesterNames",
          (Option.map x.suggesterNames ~f:StandardNameList.to_value));
        ("Deployed", (Option.map x.deployed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Deployed") in
      let suggesterNames =
        (Option.map ~f:StandardNameList.of_xml)
          (Xml.child xml_arg0 "SuggesterNames") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ?deployed ?suggesterNames ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployed = field_map json__ "Deployed" Boolean.of_json in
      let suggesterNames =
        field_map json__ "SuggesterNames" StandardNameList.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ?deployed ?suggesterNames ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeSuggester operation. Specifies the name of the domain you want to describe. To restrict the response to particular suggesters, specify the names of the suggesters you want to describe. To show the active configuration and exclude any pending changes, set the Deployed option to true."]
module DescribeServiceAccessPoliciesResponse =
  struct
    type describeServiceAccessPoliciesResult =
      {
      accessPolicies: AccessPoliciesStatus.t option
        [@ocaml.doc
          "The access rules configured for the domain specified in the request."]}
    and responseMetaData = unit
    and t =
      {
      describeServiceAccessPoliciesResult:
        describeServiceAccessPoliciesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeServiceAccessPoliciesResponse"
    let make ?accessPolicies =
      fun () ->
        {
          describeServiceAccessPoliciesResult = { accessPolicies };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeServiceAccessPoliciesResult in
      structure_to_wrapped_value
        [("AccessPolicies",
           (Option.map x.accessPolicies ~f:AccessPoliciesStatus.to_value))]
        ~wrapper:"DescribeServiceAccessPoliciesResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeServiceAccessPoliciesResult" in
      let accessPolicies =
        (Option.map ~f:AccessPoliciesStatus.of_xml)
          (Xml.child xml_arg0 "AccessPolicies") in
      make ?accessPolicies ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accessPolicies =
        field_map json__ "AccessPolicies" AccessPoliciesStatus.of_json in
      make ?accessPolicies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The result of a DescribeServiceAccessPolicies request."]
module DescribeServiceAccessPoliciesRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "The name of the domain you want to describe."];
      deployed: Boolean.t option
        [@ocaml.doc
          "Whether to display the deployed configuration (true) or include any pending changes (false). Defaults to false."]}
    let context_ = "DescribeServiceAccessPoliciesRequest"
    let make ?deployed =
      fun ~domainName -> fun () -> { deployed; domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("Deployed", (Option.map x.deployed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Deployed") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ?deployed ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployed = field_map json__ "Deployed" Boolean.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ?deployed ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeServiceAccessPolicies operation. Specifies the name of the domain you want to describe. To show the active configuration and exclude any pending changes, set the Deployed option to true."]
module DescribeScalingParametersResponse =
  struct
    type describeScalingParametersResult =
      {
      scalingParameters: ScalingParametersStatus.t option }
    and responseMetaData = unit
    and t =
      {
      describeScalingParametersResult: describeScalingParametersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeScalingParametersResponse"
    let make ?scalingParameters =
      fun () ->
        {
          describeScalingParametersResult = { scalingParameters };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeScalingParametersResult in
      structure_to_wrapped_value
        [("ScalingParameters",
           (Option.map x.scalingParameters
              ~f:ScalingParametersStatus.to_value))]
        ~wrapper:"DescribeScalingParametersResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeScalingParametersResult" in
      let scalingParameters =
        (Option.map ~f:ScalingParametersStatus.of_xml)
          (Xml.child xml_arg0 "ScalingParameters") in
      make ?scalingParameters ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scalingParameters =
        field_map json__ "ScalingParameters" ScalingParametersStatus.of_json in
      make ?scalingParameters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DescribeScalingParameters request. Contains the scaling parameters configured for the domain specified in the request."]
module DescribeScalingParametersRequest =
  struct
    type nonrec t = {
      domainName: DomainName.t }
    let context_ = "DescribeScalingParametersRequest"
    let make ~domainName = fun () -> { domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeScalingParameters operation. Specifies the name of the domain you want to describe."]
module DescribeIndexFieldsResponse =
  struct
    type describeIndexFieldsResult =
      {
      indexFields: IndexFieldStatusList.t option
        [@ocaml.doc "The index fields configured for the domain."]}
    and responseMetaData = unit
    and t =
      {
      describeIndexFieldsResult: describeIndexFieldsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeIndexFieldsResponse"
    let make ?indexFields =
      fun () ->
        { describeIndexFieldsResult = { indexFields }; responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeIndexFieldsResult in
      structure_to_wrapped_value
        [("IndexFields",
           (Option.map x.indexFields ~f:IndexFieldStatusList.to_value))]
        ~wrapper:"DescribeIndexFieldsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeIndexFieldsResult" in
      let indexFields =
        (Option.map ~f:IndexFieldStatusList.of_xml)
          (Xml.child xml_arg0 "IndexFields") in
      make ?indexFields ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let indexFields =
        field_map json__ "IndexFields" IndexFieldStatusList.of_json in
      make ?indexFields ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DescribeIndexFields request. Contains the index fields configured for the domain specified in the request."]
module DescribeIndexFieldsRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "The name of the domain you want to describe."];
      fieldNames: DynamicFieldNameList.t option
        [@ocaml.doc
          "A list of the index fields you want to describe. If not specified, information is returned for all configured index fields."];
      deployed: Boolean.t option
        [@ocaml.doc
          "Whether to display the deployed configuration (true) or include any pending changes (false). Defaults to false."]}
    let context_ = "DescribeIndexFieldsRequest"
    let make ?fieldNames =
      fun ?deployed ->
        fun ~domainName -> fun () -> { fieldNames; deployed; domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("FieldNames",
          (Option.map x.fieldNames ~f:DynamicFieldNameList.to_value));
        ("Deployed", (Option.map x.deployed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Deployed") in
      let fieldNames =
        (Option.map ~f:DynamicFieldNameList.of_xml)
          (Xml.child xml_arg0 "FieldNames") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ?deployed ?fieldNames ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployed = field_map json__ "Deployed" Boolean.of_json in
      let fieldNames =
        field_map json__ "FieldNames" DynamicFieldNameList.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ?deployed ?fieldNames ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeIndexFields operation. Specifies the name of the domain you want to describe. To restrict the response to particular index fields, specify the names of the index fields you want to describe. To show the active configuration and exclude any pending changes, set the Deployed option to true."]
module DescribeExpressionsResponse =
  struct
    type describeExpressionsResult =
      {
      expressions: ExpressionStatusList.t option
        [@ocaml.doc "The expressions configured for the domain."]}
    and responseMetaData = unit
    and t =
      {
      describeExpressionsResult: describeExpressionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeExpressionsResponse"
    let make ?expressions =
      fun () ->
        { describeExpressionsResult = { expressions }; responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeExpressionsResult in
      structure_to_wrapped_value
        [("Expressions",
           (Option.map x.expressions ~f:ExpressionStatusList.to_value))]
        ~wrapper:"DescribeExpressionsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeExpressionsResult" in
      let expressions =
        (Option.map ~f:ExpressionStatusList.of_xml)
          (Xml.child xml_arg0 "Expressions") in
      make ?expressions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expressions =
        field_map json__ "Expressions" ExpressionStatusList.of_json in
      make ?expressions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DescribeExpressions request. Contains the expressions configured for the domain specified in the request."]
module DescribeExpressionsRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "The name of the domain you want to describe."];
      expressionNames: StandardNameList.t option
        [@ocaml.doc
          "Limits the DescribeExpressions response to the specified expressions. If not specified, all expressions are shown."];
      deployed: Boolean.t option
        [@ocaml.doc
          "Whether to display the deployed configuration (true) or include any pending changes (false). Defaults to false."]}
    let context_ = "DescribeExpressionsRequest"
    let make ?expressionNames =
      fun ?deployed ->
        fun ~domainName ->
          fun () -> { expressionNames; deployed; domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("ExpressionNames",
          (Option.map x.expressionNames ~f:StandardNameList.to_value));
        ("Deployed", (Option.map x.deployed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Deployed") in
      let expressionNames =
        (Option.map ~f:StandardNameList.of_xml)
          (Xml.child xml_arg0 "ExpressionNames") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ?deployed ?expressionNames ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployed = field_map json__ "Deployed" Boolean.of_json in
      let expressionNames =
        field_map json__ "ExpressionNames" StandardNameList.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ?deployed ?expressionNames ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeDomains operation. Specifies the name of the domain you want to describe. To restrict the response to particular expressions, specify the names of the expressions you want to describe. To show the active configuration and exclude any pending changes, set the Deployed option to true."]
module DescribeDomainsResponse =
  struct
    type describeDomainsResult =
      {
      domainStatusList: DomainStatusList.t option }
    and responseMetaData = unit
    and t =
      {
      describeDomainsResult: describeDomainsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeDomainsResponse"
    let make ?domainStatusList =
      fun () ->
        { describeDomainsResult = { domainStatusList }; responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeDomainsResult in
      structure_to_wrapped_value
        [("DomainStatusList",
           (Option.map x.domainStatusList ~f:DomainStatusList.to_value))]
        ~wrapper:"DescribeDomainsResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeDomainsResult" in
      let domainStatusList =
        (Option.map ~f:DomainStatusList.of_xml)
          (Xml.child xml_arg0 "DomainStatusList") in
      make ?domainStatusList ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainStatusList =
        field_map json__ "DomainStatusList" DomainStatusList.of_json in
      make ?domainStatusList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DescribeDomains request. Contains the status of the domains specified in the request or all domains owned by the account."]
module DescribeDomainsRequest =
  struct
    type nonrec t =
      {
      domainNames: DomainNameList.t option
        [@ocaml.doc
          "The names of the domains you want to include in the response."]}
    let make ?domainNames = fun () -> { domainNames }
    let to_value x =
      structure_to_value
        [("DomainNames",
           (Option.map x.domainNames ~f:DomainNameList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainNames =
        (Option.map ~f:DomainNameList.of_xml)
          (Xml.child xml_arg0 "DomainNames") in
      make ?domainNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainNames = field_map json__ "DomainNames" DomainNameList.of_json in
      make ?domainNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeDomains operation. By default shows the status of all domains. To restrict the response to particular domains, specify the names of the domains you want to describe."]
module DescribeDomainEndpointOptionsResponse =
  struct
    type describeDomainEndpointOptionsResult =
      {
      domainEndpointOptions: DomainEndpointOptionsStatus.t option
        [@ocaml.doc
          "The status and configuration of a search domain's endpoint options."]}
    and responseMetaData = unit
    and t =
      {
      describeDomainEndpointOptionsResult:
        describeDomainEndpointOptionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `DisabledOperationException of DisabledOperationException.t 
      | `InternalException of InternalException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeDomainEndpointOptionsResponse"
    let make ?domainEndpointOptions =
      fun () ->
        {
          describeDomainEndpointOptionsResult = { domainEndpointOptions };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "DisabledOperationException" ->
          `DisabledOperationException
            (DisabledOperationException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "DisabledOperationException" ->
          `DisabledOperationException (DisabledOperationException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `DisabledOperationException e ->
          `Assoc
            [("error", (`String "DisabledOperationException"));
            ("details", (DisabledOperationException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeDomainEndpointOptionsResult in
      structure_to_wrapped_value
        [("DomainEndpointOptions",
           (Option.map x.domainEndpointOptions
              ~f:DomainEndpointOptionsStatus.to_value))]
        ~wrapper:"DescribeDomainEndpointOptionsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t
          "DescribeDomainEndpointOptionsResult" in
      let domainEndpointOptions =
        (Option.map ~f:DomainEndpointOptionsStatus.of_xml)
          (Xml.child xml_arg0 "DomainEndpointOptions") in
      make ?domainEndpointOptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainEndpointOptions =
        field_map json__ "DomainEndpointOptions"
          DomainEndpointOptionsStatus.of_json in
      make ?domainEndpointOptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DescribeDomainEndpointOptions request. Contains the status and configuration of a search domain's endpoint options."]
module DescribeDomainEndpointOptionsRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "A string that represents the name of a domain."];
      deployed: Boolean.t option
        [@ocaml.doc
          "Whether to retrieve the latest configuration (which might be in a Processing state) or the current, active configuration. Defaults to false."]}
    let context_ = "DescribeDomainEndpointOptionsRequest"
    let make ?deployed =
      fun ~domainName -> fun () -> { deployed; domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("Deployed", (Option.map x.deployed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Deployed") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ?deployed ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployed = field_map json__ "Deployed" Boolean.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ?deployed ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeDomainEndpointOptions operation. Specify the name of the domain you want to describe. To show the active configuration and exclude any pending changes, set the Deployed option to true."]
module DescribeAvailabilityOptionsResponse =
  struct
    type describeAvailabilityOptionsResult =
      {
      availabilityOptions: AvailabilityOptionsStatus.t option
        [@ocaml.doc
          "The availability options configured for the domain. Indicates whether Multi-AZ is enabled for the domain."]}
    and responseMetaData = unit
    and t =
      {
      describeAvailabilityOptionsResult: describeAvailabilityOptionsResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `DisabledOperationException of DisabledOperationException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeAvailabilityOptionsResponse"
    let make ?availabilityOptions =
      fun () ->
        {
          describeAvailabilityOptionsResult = { availabilityOptions };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "DisabledOperationException" ->
          `DisabledOperationException
            (DisabledOperationException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "DisabledOperationException" ->
          `DisabledOperationException (DisabledOperationException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `DisabledOperationException e ->
          `Assoc
            [("error", (`String "DisabledOperationException"));
            ("details", (DisabledOperationException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeAvailabilityOptionsResult in
      structure_to_wrapped_value
        [("AvailabilityOptions",
           (Option.map x.availabilityOptions
              ~f:AvailabilityOptionsStatus.to_value))]
        ~wrapper:"DescribeAvailabilityOptionsResult"
        ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeAvailabilityOptionsResult" in
      let availabilityOptions =
        (Option.map ~f:AvailabilityOptionsStatus.of_xml)
          (Xml.child xml_arg0 "AvailabilityOptions") in
      make ?availabilityOptions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let availabilityOptions =
        field_map json__ "AvailabilityOptions"
          AvailabilityOptionsStatus.of_json in
      make ?availabilityOptions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DescribeAvailabilityOptions request. Indicates whether or not the Multi-AZ option is enabled for the domain specified in the request."]
module DescribeAvailabilityOptionsRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "The name of the domain you want to describe."];
      deployed: Boolean.t option
        [@ocaml.doc
          "Whether to display the deployed configuration (true) or include any pending changes (false). Defaults to false."]}
    let context_ = "DescribeAvailabilityOptionsRequest"
    let make ?deployed =
      fun ~domainName -> fun () -> { deployed; domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("Deployed", (Option.map x.deployed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Deployed") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ?deployed ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployed = field_map json__ "Deployed" Boolean.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ?deployed ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeAvailabilityOptions operation. Specifies the name of the domain you want to describe. To show the active configuration and exclude any pending changes, set the Deployed option to true."]
module DescribeAnalysisSchemesResponse =
  struct
    type describeAnalysisSchemesResult =
      {
      analysisSchemes: AnalysisSchemeStatusList.t option
        [@ocaml.doc "The analysis scheme descriptions."]}
    and responseMetaData = unit
    and t =
      {
      describeAnalysisSchemesResult: describeAnalysisSchemesResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeAnalysisSchemesResponse"
    let make ?analysisSchemes =
      fun () ->
        {
          describeAnalysisSchemesResult = { analysisSchemes };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.describeAnalysisSchemesResult in
      structure_to_wrapped_value
        [("AnalysisSchemes",
           (Option.map x.analysisSchemes ~f:AnalysisSchemeStatusList.to_value))]
        ~wrapper:"DescribeAnalysisSchemesResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DescribeAnalysisSchemesResult" in
      let analysisSchemes =
        (Option.map ~f:AnalysisSchemeStatusList.of_xml)
          (Xml.child xml_arg0 "AnalysisSchemes") in
      make ?analysisSchemes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let analysisSchemes =
        field_map json__ "AnalysisSchemes" AnalysisSchemeStatusList.of_json in
      make ?analysisSchemes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DescribeAnalysisSchemes request. Contains the analysis schemes configured for the domain specified in the request."]
module DescribeAnalysisSchemesRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "The name of the domain you want to describe."];
      analysisSchemeNames: StandardNameList.t option
        [@ocaml.doc "The analysis schemes you want to describe."];
      deployed: Boolean.t option
        [@ocaml.doc
          "Whether to display the deployed configuration (true) or include any pending changes (false). Defaults to false."]}
    let context_ = "DescribeAnalysisSchemesRequest"
    let make ?analysisSchemeNames =
      fun ?deployed ->
        fun ~domainName ->
          fun () -> { analysisSchemeNames; deployed; domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("AnalysisSchemeNames",
          (Option.map x.analysisSchemeNames ~f:StandardNameList.to_value));
        ("Deployed", (Option.map x.deployed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Deployed") in
      let analysisSchemeNames =
        (Option.map ~f:StandardNameList.of_xml)
          (Xml.child xml_arg0 "AnalysisSchemeNames") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ?deployed ?analysisSchemeNames ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployed = field_map json__ "Deployed" Boolean.of_json in
      let analysisSchemeNames =
        field_map json__ "AnalysisSchemeNames" StandardNameList.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ?deployed ?analysisSchemeNames ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DescribeAnalysisSchemes operation. Specifies the name of the domain you want to describe. To limit the response to particular analysis schemes, specify the names of the analysis schemes you want to describe. To show the active configuration and exclude any pending changes, set the Deployed option to true."]
module DeleteSuggesterResponse =
  struct
    type deleteSuggesterResult =
      {
      suggester: SuggesterStatus.t option
        [@ocaml.doc "The status of the suggester being deleted."]}
    and responseMetaData = unit
    and t =
      {
      deleteSuggesterResult: deleteSuggesterResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeleteSuggesterResponse"
    let make ?suggester =
      fun () ->
        { deleteSuggesterResult = { suggester }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.deleteSuggesterResult in
      structure_to_wrapped_value
        [("Suggester", (Option.map x.suggester ~f:SuggesterStatus.to_value))]
        ~wrapper:"DeleteSuggesterResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DeleteSuggesterResult" in
      let suggester =
        (Option.map ~f:SuggesterStatus.of_xml)
          (Xml.child xml_arg0 "Suggester") in
      make ?suggester ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let suggester = field_map json__ "Suggester" SuggesterStatus.of_json in
      make ?suggester ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DeleteSuggester request. Contains the status of the deleted suggester."]
module DeleteSuggesterRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      suggesterName: StandardName.t
        [@ocaml.doc
          "Specifies the name of the suggester you want to delete."]}
    let context_ = "DeleteSuggesterRequest"
    let make ~domainName =
      fun ~suggesterName -> fun () -> { domainName; suggesterName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("SuggesterName", (Some (StandardName.to_value x.suggesterName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let suggesterName =
        StandardName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SuggesterName") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~suggesterName ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let suggesterName =
        field_map_exn json__ "SuggesterName" StandardName.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~suggesterName ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DeleteSuggester operation. Specifies the name of the domain you want to update and name of the suggester you want to delete."]
module DeleteIndexFieldResponse =
  struct
    type deleteIndexFieldResult =
      {
      indexField: IndexFieldStatus.t option
        [@ocaml.doc "The status of the index field being deleted."]}
    and responseMetaData = unit
    and t =
      {
      deleteIndexFieldResult: deleteIndexFieldResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeleteIndexFieldResponse"
    let make ?indexField =
      fun () ->
        { deleteIndexFieldResult = { indexField }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.deleteIndexFieldResult in
      structure_to_wrapped_value
        [("IndexField",
           (Option.map x.indexField ~f:IndexFieldStatus.to_value))]
        ~wrapper:"DeleteIndexFieldResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DeleteIndexFieldResult" in
      let indexField =
        (Option.map ~f:IndexFieldStatus.of_xml)
          (Xml.child xml_arg0 "IndexField") in
      make ?indexField ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let indexField = field_map json__ "IndexField" IndexFieldStatus.of_json in
      make ?indexField ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The result of a DeleteIndexField request."]
module DeleteIndexFieldRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      indexFieldName: DynamicFieldName.t
        [@ocaml.doc
          "The name of the index field your want to remove from the domain's indexing options."]}
    let context_ = "DeleteIndexFieldRequest"
    let make ~domainName =
      fun ~indexFieldName -> fun () -> { domainName; indexFieldName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("IndexFieldName",
          (Some (DynamicFieldName.to_value x.indexFieldName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let indexFieldName =
        DynamicFieldName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IndexFieldName") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~indexFieldName ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let indexFieldName =
        field_map_exn json__ "IndexFieldName" DynamicFieldName.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~indexFieldName ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DeleteIndexField operation. Specifies the name of the domain you want to update and the name of the index field you want to delete."]
module DeleteExpressionResponse =
  struct
    type deleteExpressionResult =
      {
      expression: ExpressionStatus.t option
        [@ocaml.doc "The status of the expression being deleted."]}
    and responseMetaData = unit
    and t =
      {
      deleteExpressionResult: deleteExpressionResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeleteExpressionResponse"
    let make ?expression =
      fun () ->
        { deleteExpressionResult = { expression }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.deleteExpressionResult in
      structure_to_wrapped_value
        [("Expression",
           (Option.map x.expression ~f:ExpressionStatus.to_value))]
        ~wrapper:"DeleteExpressionResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DeleteExpressionResult" in
      let expression =
        (Option.map ~f:ExpressionStatus.of_xml)
          (Xml.child xml_arg0 "Expression") in
      make ?expression ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expression = field_map json__ "Expression" ExpressionStatus.of_json in
      make ?expression ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DeleteExpression request. Specifies the expression being deleted."]
module DeleteExpressionRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      expressionName: StandardName.t
        [@ocaml.doc "The name of the Expression to delete."]}
    let context_ = "DeleteExpressionRequest"
    let make ~domainName =
      fun ~expressionName -> fun () -> { domainName; expressionName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("ExpressionName", (Some (StandardName.to_value x.expressionName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expressionName =
        StandardName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExpressionName") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~expressionName ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expressionName =
        field_map_exn json__ "ExpressionName" StandardName.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~expressionName ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DeleteExpression operation. Specifies the name of the domain you want to update and the name of the expression you want to delete."]
module DeleteDomainResponse =
  struct
    type deleteDomainResult = {
      domainStatus: DomainStatus.t option }
    and responseMetaData = unit
    and t =
      {
      deleteDomainResult: deleteDomainResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeleteDomainResponse"
    let make ?domainStatus =
      fun () ->
        { deleteDomainResult = { domainStatus }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.deleteDomainResult in
      structure_to_wrapped_value
        [("DomainStatus",
           (Option.map x.domainStatus ~f:DomainStatus.to_value))]
        ~wrapper:"DeleteDomainResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "DeleteDomainResult" in
      let domainStatus =
        (Option.map ~f:DomainStatus.of_xml)
          (Xml.child xml_arg0 "DomainStatus") in
      make ?domainStatus ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainStatus = field_map json__ "DomainStatus" DomainStatus.of_json in
      make ?domainStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DeleteDomain request. Contains the status of a newly deleted domain, or no status if the domain has already been completely deleted."]
module DeleteDomainRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc "The name of the domain you want to permanently delete."]}
    let context_ = "DeleteDomainRequest"
    let make ~domainName = fun () -> { domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DeleteDomain operation. Specifies the name of the domain you want to delete."]
module DeleteAnalysisSchemeResponse =
  struct
    type deleteAnalysisSchemeResult =
      {
      analysisScheme: AnalysisSchemeStatus.t option
        [@ocaml.doc "The status of the analysis scheme being deleted."]}
    and responseMetaData = unit
    and t =
      {
      deleteAnalysisSchemeResult: deleteAnalysisSchemeResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeleteAnalysisSchemeResponse"
    let make ?analysisScheme =
      fun () ->
        {
          deleteAnalysisSchemeResult = { analysisScheme };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.deleteAnalysisSchemeResult in
      structure_to_wrapped_value
        [("AnalysisScheme",
           (Option.map x.analysisScheme ~f:AnalysisSchemeStatus.to_value))]
        ~wrapper:"DeleteAnalysisSchemeResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DeleteAnalysisSchemeResult" in
      let analysisScheme =
        (Option.map ~f:AnalysisSchemeStatus.of_xml)
          (Xml.child xml_arg0 "AnalysisScheme") in
      make ?analysisScheme ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let analysisScheme =
        field_map json__ "AnalysisScheme" AnalysisSchemeStatus.of_json in
      make ?analysisScheme ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DeleteAnalysisScheme request. Contains the status of the deleted analysis scheme."]
module DeleteAnalysisSchemeRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      analysisSchemeName: StandardName.t
        [@ocaml.doc "The name of the analysis scheme you want to delete."]}
    let context_ = "DeleteAnalysisSchemeRequest"
    let make ~domainName =
      fun ~analysisSchemeName -> fun () -> { domainName; analysisSchemeName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("AnalysisSchemeName",
          (Some (StandardName.to_value x.analysisSchemeName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let analysisSchemeName =
        StandardName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AnalysisSchemeName") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~analysisSchemeName ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let analysisSchemeName =
        field_map_exn json__ "AnalysisSchemeName" StandardName.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~analysisSchemeName ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DeleteAnalysisScheme operation. Specifies the name of the domain you want to update and the analysis scheme you want to delete."]
module DefineSuggesterResponse =
  struct
    type defineSuggesterResult = {
      suggester: SuggesterStatus.t option }
    and responseMetaData = unit
    and t =
      {
      defineSuggesterResult: defineSuggesterResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DefineSuggesterResponse"
    let make ?suggester =
      fun () ->
        { defineSuggesterResult = { suggester }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.defineSuggesterResult in
      structure_to_wrapped_value
        [("Suggester", (Option.map x.suggester ~f:SuggesterStatus.to_value))]
        ~wrapper:"DefineSuggesterResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DefineSuggesterResult" in
      let suggester =
        (Option.map ~f:SuggesterStatus.of_xml)
          (Xml.child xml_arg0 "Suggester") in
      make ?suggester ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let suggester = field_map json__ "Suggester" SuggesterStatus.of_json in
      make ?suggester ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DefineSuggester request. Contains the status of the newly-configured suggester."]
module DefineSuggesterRequest =
  struct
    type nonrec t = {
      domainName: DomainName.t ;
      suggester: Suggester.t }
    let context_ = "DefineSuggesterRequest"
    let make ~domainName =
      fun ~suggester -> fun () -> { domainName; suggester }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("Suggester", (Some (Suggester.to_value x.suggester)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let suggester =
        Suggester.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Suggester") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~suggester ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let suggester = field_map_exn json__ "Suggester" Suggester.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~suggester ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DefineSuggester operation. Specifies the name of the domain you want to update and the suggester configuration."]
module DefineIndexFieldResponse =
  struct
    type defineIndexFieldResult = {
      indexField: IndexFieldStatus.t option }
    and responseMetaData = unit
    and t =
      {
      defineIndexFieldResult: defineIndexFieldResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DefineIndexFieldResponse"
    let make ?indexField =
      fun () ->
        { defineIndexFieldResult = { indexField }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.defineIndexFieldResult in
      structure_to_wrapped_value
        [("IndexField",
           (Option.map x.indexField ~f:IndexFieldStatus.to_value))]
        ~wrapper:"DefineIndexFieldResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DefineIndexFieldResult" in
      let indexField =
        (Option.map ~f:IndexFieldStatus.of_xml)
          (Xml.child xml_arg0 "IndexField") in
      make ?indexField ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let indexField = field_map json__ "IndexField" IndexFieldStatus.of_json in
      make ?indexField ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DefineIndexField request. Contains the status of the newly-configured index field."]
module DefineIndexFieldRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      indexField: IndexField.t
        [@ocaml.doc
          "The index field and field options you want to configure."]}
    let context_ = "DefineIndexFieldRequest"
    let make ~domainName =
      fun ~indexField -> fun () -> { domainName; indexField }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("IndexField", (Some (IndexField.to_value x.indexField)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let indexField =
        IndexField.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IndexField") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~indexField ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let indexField = field_map_exn json__ "IndexField" IndexField.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~indexField ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DefineIndexField operation. Specifies the name of the domain you want to update and the index field configuration."]
module DefineExpressionResponse =
  struct
    type defineExpressionResult = {
      expression: ExpressionStatus.t option }
    and responseMetaData = unit
    and t =
      {
      defineExpressionResult: defineExpressionResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DefineExpressionResponse"
    let make ?expression =
      fun () ->
        { defineExpressionResult = { expression }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.defineExpressionResult in
      structure_to_wrapped_value
        [("Expression",
           (Option.map x.expression ~f:ExpressionStatus.to_value))]
        ~wrapper:"DefineExpressionResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DefineExpressionResult" in
      let expression =
        (Option.map ~f:ExpressionStatus.of_xml)
          (Xml.child xml_arg0 "Expression") in
      make ?expression ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expression = field_map json__ "Expression" ExpressionStatus.of_json in
      make ?expression ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DefineExpression request. Contains the status of the newly-configured expression."]
module DefineExpressionRequest =
  struct
    type nonrec t = {
      domainName: DomainName.t ;
      expression: Expression.t }
    let context_ = "DefineExpressionRequest"
    let make ~domainName =
      fun ~expression -> fun () -> { domainName; expression }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("Expression", (Some (Expression.to_value x.expression)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expression =
        Expression.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Expression") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~expression ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expression = field_map_exn json__ "Expression" Expression.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~expression ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DefineExpression operation. Specifies the name of the domain you want to update and the expression you want to configure."]
module DefineAnalysisSchemeResponse =
  struct
    type defineAnalysisSchemeResult =
      {
      analysisScheme: AnalysisSchemeStatus.t option }
    and responseMetaData = unit
    and t =
      {
      defineAnalysisSchemeResult: defineAnalysisSchemeResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `InvalidTypeException of InvalidTypeException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DefineAnalysisSchemeResponse"
    let make ?analysisScheme =
      fun () ->
        {
          defineAnalysisSchemeResult = { analysisScheme };
          responseMetaData = ()
        }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidTypeException" ->
          `InvalidTypeException (InvalidTypeException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidTypeException e ->
          `Assoc
            [("error", (`String "InvalidTypeException"));
            ("details", (InvalidTypeException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.defineAnalysisSchemeResult in
      structure_to_wrapped_value
        [("AnalysisScheme",
           (Option.map x.analysisScheme ~f:AnalysisSchemeStatus.to_value))]
        ~wrapper:"DefineAnalysisSchemeResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "DefineAnalysisSchemeResult" in
      let analysisScheme =
        (Option.map ~f:AnalysisSchemeStatus.of_xml)
          (Xml.child xml_arg0 "AnalysisScheme") in
      make ?analysisScheme ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let analysisScheme =
        field_map json__ "AnalysisScheme" AnalysisSchemeStatus.of_json in
      make ?analysisScheme ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a DefineAnalysisScheme request. Contains the status of the newly-configured analysis scheme."]
module DefineAnalysisSchemeRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t ;
      analysisScheme: AnalysisScheme.t }
    let context_ = "DefineAnalysisSchemeRequest"
    let make ~domainName =
      fun ~analysisScheme -> fun () -> { domainName; analysisScheme }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)));
        ("AnalysisScheme", (Some (AnalysisScheme.to_value x.analysisScheme)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let analysisScheme =
        AnalysisScheme.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AnalysisScheme") in
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~analysisScheme ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let analysisScheme =
        field_map_exn json__ "AnalysisScheme" AnalysisScheme.of_json in
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~analysisScheme ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the DefineAnalysisScheme operation. Specifies the name of the domain you want to update and the analysis scheme configuration."]
module CreateDomainResponse =
  struct
    type createDomainResult = {
      domainStatus: DomainStatus.t option }
    and responseMetaData = unit
    and t =
      {
      createDomainResult: createDomainResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateDomainResponse"
    let make ?domainStatus =
      fun () ->
        { createDomainResult = { domainStatus }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.createDomainResult in
      structure_to_wrapped_value
        [("DomainStatus",
           (Option.map x.domainStatus ~f:DomainStatus.to_value))]
        ~wrapper:"CreateDomainResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 = Xml.child_exn ~context:context_ t "CreateDomainResult" in
      let domainStatus =
        (Option.map ~f:DomainStatus.of_xml)
          (Xml.child xml_arg0 "DomainStatus") in
      make ?domainStatus ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainStatus = field_map json__ "DomainStatus" DomainStatus.of_json in
      make ?domainStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a CreateDomainRequest. Contains the status of a newly created domain."]
module CreateDomainRequest =
  struct
    type nonrec t =
      {
      domainName: DomainName.t
        [@ocaml.doc
          "A name for the domain you are creating. Allowed characters are a-z (lower-case letters), 0-9, and hyphen (-). Domain names must start with a letter or number and be at least 3 and no more than 28 characters long."]}
    let context_ = "CreateDomainRequest"
    let make ~domainName = fun () -> { domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the CreateDomain operation. Specifies a name for the new search domain."]
module BuildSuggestersResponse =
  struct
    type buildSuggestersResult = {
      fieldNames: FieldNameList.t option }
    and responseMetaData = unit
    and t =
      {
      buildSuggestersResult: buildSuggestersResult ;
      responseMetaData: responseMetaData }
    type error =
      [ `BaseException of BaseException.t 
      | `InternalException of InternalException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "BuildSuggestersResponse"
    let make ?fieldNames =
      fun () ->
        { buildSuggestersResult = { fieldNames }; responseMetaData = () }
    let error_of_json name json =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BaseException" -> `BaseException (BaseException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BaseException e ->
          `Assoc
            [("error", (`String "BaseException"));
            ("details", (BaseException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value t =
      let x = t.buildSuggestersResult in
      structure_to_wrapped_value
        [("FieldNames", (Option.map x.fieldNames ~f:FieldNameList.to_value))]
        ~wrapper:"BuildSuggestersResult" ~response:"ResponseMetaData"
    let to_query v = to_query to_value v
    let of_xml t =
      let xml_arg0 =
        Xml.child_exn ~context:context_ t "BuildSuggestersResult" in
      let fieldNames =
        (Option.map ~f:FieldNameList.of_xml)
          (Xml.child xml_arg0 "FieldNames") in
      make ?fieldNames ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldNames = field_map json__ "FieldNames" FieldNameList.of_json in
      make ?fieldNames ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of a BuildSuggester request. Contains a list of the fields used for suggestions."]
module BuildSuggestersRequest =
  struct
    type nonrec t = {
      domainName: DomainName.t }
    let context_ = "BuildSuggestersRequest"
    let make ~domainName = fun () -> { domainName }
    let to_value x =
      structure_to_value
        [("DomainName", (Some (DomainName.to_value x.domainName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domainName =
        DomainName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DomainName") in
      make ~domainName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domainName = field_map_exn json__ "DomainName" DomainName.of_json in
      make ~domainName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the parameters to the BuildSuggester operation. Specifies the name of the domain you want to update."]
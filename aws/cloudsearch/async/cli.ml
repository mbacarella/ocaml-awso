(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let build_suggesters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.build_suggesters
           (Values.BuildSuggestersRequest.make ~domainName ())
           (Some Values.BuildSuggestersResponse.to_json)
           (Some Values.BuildSuggestersResponse.error_to_json)])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain (Values.CreateDomainRequest.make ~domainName ())
           (Some Values.CreateDomainResponse.to_json)
           (Some Values.CreateDomainResponse.error_to_json)])
let define_analysis_scheme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and analysisScheme =
         flag "analysis-scheme" (required json_arg)
           ~doc:"JSON AnalysisScheme" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.define_analysis_scheme
           (Values.DefineAnalysisSchemeRequest.make ~domainName
              ~analysisScheme:(Values.AnalysisScheme.of_json analysisScheme)
              ()) (Some Values.DefineAnalysisSchemeResponse.to_json)
           (Some Values.DefineAnalysisSchemeResponse.error_to_json)])
let define_expression =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and expression =
         flag "expression" (required json_arg) ~doc:"JSON Expression" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.define_expression
           (Values.DefineExpressionRequest.make ~domainName
              ~expression:(Values.Expression.of_json expression) ())
           (Some Values.DefineExpressionResponse.to_json)
           (Some Values.DefineExpressionResponse.error_to_json)])
let define_index_field =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and indexField =
         flag "index-field" (required json_arg) ~doc:"JSON IndexField" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.define_index_field
           (Values.DefineIndexFieldRequest.make ~domainName
              ~indexField:(Values.IndexField.of_json indexField) ())
           (Some Values.DefineIndexFieldResponse.to_json)
           (Some Values.DefineIndexFieldResponse.error_to_json)])
let define_suggester =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and suggester =
         flag "suggester" (required json_arg) ~doc:"JSON Suggester" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.define_suggester
           (Values.DefineSuggesterRequest.make ~domainName
              ~suggester:(Values.Suggester.of_json suggester) ())
           (Some Values.DefineSuggesterResponse.to_json)
           (Some Values.DefineSuggesterResponse.error_to_json)])
let delete_analysis_scheme =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and analysisSchemeName =
         flag "analysis-scheme-name" (required string)
           ~doc:"STRING StandardName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_analysis_scheme
           (Values.DeleteAnalysisSchemeRequest.make ~domainName
              ~analysisSchemeName ())
           (Some Values.DeleteAnalysisSchemeResponse.to_json)
           (Some Values.DeleteAnalysisSchemeResponse.error_to_json)])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain (Values.DeleteDomainRequest.make ~domainName ())
           (Some Values.DeleteDomainResponse.to_json)
           (Some Values.DeleteDomainResponse.error_to_json)])
let delete_expression =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and expressionName =
         flag "expression-name" (required string) ~doc:"STRING StandardName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_expression
           (Values.DeleteExpressionRequest.make ~domainName ~expressionName
              ()) (Some Values.DeleteExpressionResponse.to_json)
           (Some Values.DeleteExpressionResponse.error_to_json)])
let delete_index_field =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and indexFieldName =
         flag "index-field-name" (required string)
           ~doc:"STRING DynamicFieldName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_index_field
           (Values.DeleteIndexFieldRequest.make ~domainName ~indexFieldName
              ()) (Some Values.DeleteIndexFieldResponse.to_json)
           (Some Values.DeleteIndexFieldResponse.error_to_json)])
let delete_suggester =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and suggesterName =
         flag "suggester-name" (required string) ~doc:"STRING StandardName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_suggester
           (Values.DeleteSuggesterRequest.make ~domainName ~suggesterName ())
           (Some Values.DeleteSuggesterResponse.to_json)
           (Some Values.DeleteSuggesterResponse.error_to_json)])
let describe_analysis_schemes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and analysisSchemeNames =
         flag "analysis-scheme-names" (optional json_arg)
           ~doc:"JSON StandardNameList"
       and deployed = flag "deployed" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_analysis_schemes
           (Values.DescribeAnalysisSchemesRequest.make
              ?analysisSchemeNames:(Option.map
                                      ~f:Values.StandardNameList.of_json
                                      analysisSchemeNames) ?deployed
              ~domainName ())
           (Some Values.DescribeAnalysisSchemesResponse.to_json)
           (Some Values.DescribeAnalysisSchemesResponse.error_to_json)])
let describe_availability_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deployed = flag "deployed" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_availability_options
           (Values.DescribeAvailabilityOptionsRequest.make ?deployed
              ~domainName ())
           (Some Values.DescribeAvailabilityOptionsResponse.to_json)
           (Some Values.DescribeAvailabilityOptionsResponse.error_to_json)])
let describe_domain_endpoint_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deployed = flag "deployed" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain_endpoint_options
           (Values.DescribeDomainEndpointOptionsRequest.make ?deployed
              ~domainName ())
           (Some Values.DescribeDomainEndpointOptionsResponse.to_json)
           (Some Values.DescribeDomainEndpointOptionsResponse.error_to_json)])
let describe_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNames =
         flag "domain-names" (optional json_arg) ~doc:"JSON DomainNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domains
           (Values.DescribeDomainsRequest.make
              ?domainNames:(Option.map ~f:Values.DomainNameList.of_json
                              domainNames) ())
           (Some Values.DescribeDomainsResponse.to_json)
           (Some Values.DescribeDomainsResponse.error_to_json)])
let describe_expressions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expressionNames =
         flag "expression-names" (optional json_arg)
           ~doc:"JSON StandardNameList"
       and deployed = flag "deployed" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_expressions
           (Values.DescribeExpressionsRequest.make
              ?expressionNames:(Option.map ~f:Values.StandardNameList.of_json
                                  expressionNames) ?deployed ~domainName ())
           (Some Values.DescribeExpressionsResponse.to_json)
           (Some Values.DescribeExpressionsResponse.error_to_json)])
let describe_index_fields =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fieldNames =
         flag "field-names" (optional json_arg)
           ~doc:"JSON DynamicFieldNameList"
       and deployed = flag "deployed" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_index_fields
           (Values.DescribeIndexFieldsRequest.make
              ?fieldNames:(Option.map ~f:Values.DynamicFieldNameList.of_json
                             fieldNames) ?deployed ~domainName ())
           (Some Values.DescribeIndexFieldsResponse.to_json)
           (Some Values.DescribeIndexFieldsResponse.error_to_json)])
let describe_scaling_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scaling_parameters
           (Values.DescribeScalingParametersRequest.make ~domainName ())
           (Some Values.DescribeScalingParametersResponse.to_json)
           (Some Values.DescribeScalingParametersResponse.error_to_json)])
let describe_service_access_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deployed = flag "deployed" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_service_access_policies
           (Values.DescribeServiceAccessPoliciesRequest.make ?deployed
              ~domainName ())
           (Some Values.DescribeServiceAccessPoliciesResponse.to_json)
           (Some Values.DescribeServiceAccessPoliciesResponse.error_to_json)])
let describe_suggesters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suggesterNames =
         flag "suggester-names" (optional json_arg)
           ~doc:"JSON StandardNameList"
       and deployed = flag "deployed" (optional bool) ~doc:"BOOL Boolean"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_suggesters
           (Values.DescribeSuggestersRequest.make
              ?suggesterNames:(Option.map ~f:Values.StandardNameList.of_json
                                 suggesterNames) ?deployed ~domainName ())
           (Some Values.DescribeSuggestersResponse.to_json)
           (Some Values.DescribeSuggestersResponse.error_to_json)])
let index_documents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.index_documents
           (Values.IndexDocumentsRequest.make ~domainName ())
           (Some Values.IndexDocumentsResponse.to_json)
           (Some Values.IndexDocumentsResponse.error_to_json)])
let list_domain_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_names (Fn.id ())
           (Some Values.ListDomainNamesResponse.to_json)
           (Some Values.ListDomainNamesResponse.error_to_json)])
let update_availability_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and multiAZ = flag "multi-a-z" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_availability_options
           (Values.UpdateAvailabilityOptionsRequest.make ~domainName ~multiAZ
              ()) (Some Values.UpdateAvailabilityOptionsResponse.to_json)
           (Some Values.UpdateAvailabilityOptionsResponse.error_to_json)])
let update_domain_endpoint_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and domainEndpointOptions =
         flag "domain-endpoint-options" (required json_arg)
           ~doc:"JSON DomainEndpointOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_endpoint_options
           (Values.UpdateDomainEndpointOptionsRequest.make ~domainName
              ~domainEndpointOptions:(Values.DomainEndpointOptions.of_json
                                        domainEndpointOptions) ())
           (Some Values.UpdateDomainEndpointOptionsResponse.to_json)
           (Some Values.UpdateDomainEndpointOptionsResponse.error_to_json)])
let update_scaling_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and scalingParameters =
         flag "scaling-parameters" (required json_arg)
           ~doc:"JSON ScalingParameters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_scaling_parameters
           (Values.UpdateScalingParametersRequest.make ~domainName
              ~scalingParameters:(Values.ScalingParameters.of_json
                                    scalingParameters) ())
           (Some Values.UpdateScalingParametersResponse.to_json)
           (Some Values.UpdateScalingParametersResponse.error_to_json)])
let update_service_access_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and accessPolicies =
         flag "access-policies" (required string)
           ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_access_policies
           (Values.UpdateServiceAccessPoliciesRequest.make ~domainName
              ~accessPolicies ())
           (Some Values.UpdateServiceAccessPoliciesResponse.to_json)
           (Some Values.UpdateServiceAccessPoliciesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("build-suggesters", build_suggesters);
    ("create-domain", create_domain);
    ("define-analysis-scheme", define_analysis_scheme);
    ("define-expression", define_expression);
    ("define-index-field", define_index_field);
    ("define-suggester", define_suggester);
    ("delete-analysis-scheme", delete_analysis_scheme);
    ("delete-domain", delete_domain);
    ("delete-expression", delete_expression);
    ("delete-index-field", delete_index_field);
    ("delete-suggester", delete_suggester);
    ("describe-analysis-schemes", describe_analysis_schemes);
    ("describe-availability-options", describe_availability_options);
    ("describe-domain-endpoint-options", describe_domain_endpoint_options);
    ("describe-domains", describe_domains);
    ("describe-expressions", describe_expressions);
    ("describe-index-fields", describe_index_fields);
    ("describe-scaling-parameters", describe_scaling_parameters);
    ("describe-service-access-policies", describe_service_access_policies);
    ("describe-suggesters", describe_suggesters);
    ("index-documents", index_documents);
    ("list-domain-names", list_domain_names);
    ("update-availability-options", update_availability_options);
    ("update-domain-endpoint-options", update_domain_endpoint_options);
    ("update-scaling-parameters", update_scaling_parameters);
    ("update-service-access-policies", update_service_access_policies)]
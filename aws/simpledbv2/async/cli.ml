(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportArn =
         flag "export-arn" (required string) ~doc:"STRING ExportArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_export (Values.GetExportRequest.make ~exportArn ())
           (Some Values.GetExportResponse.to_json)
           (Some Values.GetExportResponse.error_to_json)])
let list_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_exports
           (Values.ListExportsRequest.make ?domainName ?maxResults ?nextToken
              ()) (Some Values.ListExportsResponse.to_json)
           (Some Values.ListExportsResponse.error_to_json)])
let start_domain_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and s3KeyPrefix =
         flag "s3-key-prefix" (optional string) ~doc:"STRING S3KeyPrefix"
       and s3SseAlgorithm =
         flag "s3-sse-algorithm" (optional json_arg)
           ~doc:"JSON S3SseAlgorithm"
       and s3SseKmsKeyId =
         flag "s3-sse-kms-key-id" (optional string)
           ~doc:"STRING S3SseKmsKeyId"
       and s3BucketOwner =
         flag "s3-bucket-owner" (optional string) ~doc:"STRING AwsAccountId"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and s3Bucket =
         flag "s3-bucket" (required string) ~doc:"STRING S3BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_domain_export
           (Values.StartDomainExportRequest.make ?clientToken ?s3KeyPrefix
              ?s3SseAlgorithm:(Option.map ~f:Values.S3SseAlgorithm.of_json
                                 s3SseAlgorithm) ?s3SseKmsKeyId
              ?s3BucketOwner ~domainName ~s3Bucket ())
           (Some Values.StartDomainExportResponse.to_json)
           (Some Values.StartDomainExportResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-export", get_export);
    ("list-exports", list_exports);
    ("start-domain-export", start_domain_export)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.backup
let apiVersion = "2018-11-15"
let endpointPrefix = "backup"
let serviceFullName = "AWS Backup"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ARN =
  struct
    type nonrec t = string
    let context_ = "ARN"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ARN" j
    let to_json = simple_to_json to_value
  end
module Lifecycle =
  struct
    type nonrec t =
      {
      moveToColdStorageAfterDays: Long.t option
        [@ocaml.doc
          "Specifies the number of days after creation that a recovery point is moved to cold storage."];
      deleteAfterDays: Long.t option
        [@ocaml.doc
          "Specifies the number of days after creation that a recovery point is deleted. Must be greater than 90 days plus MoveToColdStorageAfterDays."]}
    let make ?moveToColdStorageAfterDays =
      fun ?deleteAfterDays ->
        fun () -> { moveToColdStorageAfterDays; deleteAfterDays }
    let to_value x =
      structure_to_value
        [("MoveToColdStorageAfterDays",
           (Option.map x.moveToColdStorageAfterDays ~f:Long.to_value));
        ("DeleteAfterDays", (Option.map x.deleteAfterDays ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deleteAfterDays =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "DeleteAfterDays") in
      let moveToColdStorageAfterDays =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "MoveToColdStorageAfterDays") in
      make ?deleteAfterDays ?moveToColdStorageAfterDays ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let deleteAfterDays = field_map json "DeleteAfterDays" Long.of_json in
      let moveToColdStorageAfterDays =
        field_map json "MoveToColdStorageAfterDays" Long.of_json in
      make ?deleteAfterDays ?moveToColdStorageAfterDays ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains an array of Transition objects specifying how long in days before a recovery point transitions to cold storage or is deleted. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, on the console, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types."]
module BackupOptionKey =
  struct
    type nonrec t = string
    let context_ = "BackupOptionKey"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z0-9\\-\\_\\.]{1,50}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BackupOptionKey" j
    let to_json = simple_to_json to_value
  end
module BackupOptionValue =
  struct
    type nonrec t = string
    let context_ = "BackupOptionValue"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z0-9\\-\\_\\.]{1,50}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BackupOptionValue" j
    let to_json = simple_to_json to_value
  end
module ParameterName =
  struct
    type nonrec t = string
    let context_ = "ParameterName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterName" j
    let to_json = simple_to_json to_value
  end
module ParameterValue =
  struct
    type nonrec t = string
    let context_ = "ParameterValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterValue" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "string"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"string" j
    let to_json = simple_to_json to_value
  end
module CopyAction =
  struct
    type nonrec t =
      {
      lifecycle: Lifecycle.t option ;
      destinationBackupVaultArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies the destination backup vault for the copied backup. For example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."]}
    let context_ = "CopyAction"
    let make ?lifecycle =
      fun ~destinationBackupVaultArn ->
        fun () -> { lifecycle; destinationBackupVaultArn }
    let to_value x =
      structure_to_value
        [("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value));
        ("DestinationBackupVaultArn",
          (Some (ARN.to_value x.destinationBackupVaultArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let destinationBackupVaultArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DestinationBackupVaultArn") in
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      make ~destinationBackupVaultArn ?lifecycle ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let destinationBackupVaultArn =
        field_map_exn json "DestinationBackupVaultArn" ARN.of_json in
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      make ~destinationBackupVaultArn ?lifecycle ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details of the copy operation."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module BackupOptions =
  struct
    type nonrec t = (BackupOptionKey.t * BackupOptionValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((BackupOptionKey.of_string chopped),
                              (BackupOptionValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (BackupOptionKey.to_value x) |>
                    (fun x ->
                       (BackupOptionValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:BackupOptionKey.of_string
        ~of_json:BackupOptionValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceType =
  struct
    type nonrec t = string
    let context_ = "ResourceType"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z0-9\\-\\_\\.]{1,50}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceType" j
    let to_json = simple_to_json to_value
  end
module ConditionKey =
  struct
    type nonrec t = string
    let context_ = "ConditionKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConditionKey" j
    let to_json = simple_to_json to_value
  end
module ConditionValue =
  struct
    type nonrec t = string
    let context_ = "ConditionValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConditionValue" j
    let to_json = simple_to_json to_value
  end
module ControlInputParameter =
  struct
    type nonrec t =
      {
      parameterName: ParameterName.t option
        [@ocaml.doc
          "The name of a parameter, for example, BackupPlanFrequency."];
      parameterValue: ParameterValue.t option
        [@ocaml.doc "The value of parameter, for example, hourly."]}
    let make ?parameterName =
      fun ?parameterValue -> fun () -> { parameterName; parameterValue }
    let to_value x =
      structure_to_value
        [("ParameterName",
           (Option.map x.parameterName ~f:ParameterName.to_value));
        ("ParameterValue",
          (Option.map x.parameterValue ~f:ParameterValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameterValue =
        (Option.map ~f:ParameterValue.of_xml)
          (Xml.child xml_arg0 "ParameterValue") in
      let parameterName =
        (Option.map ~f:ParameterName.of_xml)
          (Xml.child xml_arg0 "ParameterName") in
      make ?parameterValue ?parameterName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let parameterValue =
        field_map json "ParameterValue" ParameterValue.of_json in
      let parameterName =
        field_map json "ParameterName" ParameterName.of_json in
      make ?parameterValue ?parameterName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of parameters for a control. A control can have zero, one, or more than one parameter. An example of a control with two parameters is: \"backup plan frequency is at least daily and the retention period is at least 1 year\". The first parameter is daily. The second parameter is 1 year."]
module ComplianceResourceIdList =
  struct
    type nonrec t = String_.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"ComplianceResourceIdList" ~of_json:String_.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ResourceTypeList =
  struct
    type nonrec t = ARN.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ARN.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ARN.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceTypeList" ~of_json:ARN.of_json j
    let to_json v = composed_to_json to_value v
  end
module StringMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupRuleName =
  struct
    type nonrec t = string
    let context_ = "BackupRuleName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z0-9\\-\\_\\.]{1,50}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BackupRuleName" j
    let to_json = simple_to_json to_value
  end
module BackupVaultName =
  struct
    type nonrec t = string
    let context_ = "BackupVaultName"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^[a-zA-Z0-9\\-\\_]{2,50}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BackupVaultName" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module CopyActions =
  struct
    type nonrec t = CopyAction.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CopyAction.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CopyAction.of_xml)
    let of_json j =
      list_of_json ~kind:"CopyActions" ~of_json:CopyAction.of_json j
    let to_json v = composed_to_json to_value v
  end
module CronExpression =
  struct
    type nonrec t = string
    let context_ = "CronExpression"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CronExpression" j
    let to_json = simple_to_json to_value
  end
module Tags =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module WindowMinutes =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module FormatList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"FormatList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StringList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"stringList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module AdvancedBackupSetting =
  struct
    type nonrec t =
      {
      resourceType: ResourceType.t option
        [@ocaml.doc
          "Specifies an object containing resource type and backup options. The only supported resource type is Amazon EC2 instances with Windows Volume Shadow Copy Service (VSS). For a CloudFormation example, see the sample CloudFormation template to enable Windows VSS in the Backup User Guide. Valid values: EC2."];
      backupOptions: BackupOptions.t option
        [@ocaml.doc
          "Specifies the backup option for a selected resource. This option is only available for Windows VSS backup jobs. Valid values: Set to \"WindowsVSS\":\"enabled\" to enable the WindowsVSS backup option and create a Windows VSS backup. Set to \"WindowsVSS\":\"disabled\" to create a regular backup. The WindowsVSS option is not enabled by default. If you specify an invalid option, you get an InvalidParameterValueException exception. For more information about Windows VSS backups, see Creating a VSS-Enabled Windows Backup."]}
    let make ?resourceType =
      fun ?backupOptions -> fun () -> { resourceType; backupOptions }
    let to_value x =
      structure_to_value
        [("ResourceType",
           (Option.map x.resourceType ~f:ResourceType.to_value));
        ("BackupOptions",
          (Option.map x.backupOptions ~f:BackupOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupOptions =
        (Option.map ~f:BackupOptions.of_xml)
          (Xml.child xml_arg0 "BackupOptions") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      make ?backupOptions ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupOptions =
        field_map json "BackupOptions" BackupOptions.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      make ?backupOptions ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A list of backup options for each resource type."]
module ConditionParameter =
  struct
    type nonrec t =
      {
      conditionKey: ConditionKey.t option
        [@ocaml.doc
          "The key in a key-value pair. For example, in the tag Department: Accounting, Department is the key."];
      conditionValue: ConditionValue.t option
        [@ocaml.doc
          "The value in a key-value pair. For example, in the tag Department: Accounting, Accounting is the value."]}
    let make ?conditionKey =
      fun ?conditionValue -> fun () -> { conditionKey; conditionValue }
    let to_value x =
      structure_to_value
        [("ConditionKey",
           (Option.map x.conditionKey ~f:ConditionKey.to_value));
        ("ConditionValue",
          (Option.map x.conditionValue ~f:ConditionValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conditionValue =
        (Option.map ~f:ConditionValue.of_xml)
          (Xml.child xml_arg0 "ConditionValue") in
      let conditionKey =
        (Option.map ~f:ConditionKey.of_xml)
          (Xml.child xml_arg0 "ConditionKey") in
      make ?conditionValue ?conditionKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let conditionValue =
        field_map json "ConditionValue" ConditionValue.of_json in
      let conditionKey = field_map json "ConditionKey" ConditionKey.of_json in
      make ?conditionValue ?conditionKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Includes information about tags you define to assign tagged resources to a backup plan."]
module ConditionType =
  struct
    type nonrec t =
      | STRINGEQUALS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | STRINGEQUALS -> "STRINGEQUALS" | Non_static_id s -> s
    let of_string =
      function | "STRINGEQUALS" -> STRINGEQUALS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ConditionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConditionType" j)
    let to_json = simple_to_json to_value
  end
module ControlInputParameters =
  struct
    type nonrec t = ControlInputParameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ControlInputParameter.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ControlInputParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ControlInputParameters"
        ~of_json:ControlInputParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module ControlName =
  struct
    type nonrec t = string
    let context_ = "ControlName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ControlName" j
    let to_json = simple_to_json to_value
  end
module ControlScope =
  struct
    type nonrec t =
      {
      complianceResourceIds: ComplianceResourceIdList.t option
        [@ocaml.doc
          "The ID of the only Amazon Web Services resource that you want your control scope to contain."];
      complianceResourceTypes: ResourceTypeList.t option
        [@ocaml.doc
          "Describes whether the control scope includes one or more types of resources, such as EFS or RDS."];
      tags: StringMap.t option
        [@ocaml.doc
          "The tag key-value pair applied to those Amazon Web Services resources that you want to trigger an evaluation for a rule. A maximum of one key-value pair can be provided. The tag value is optional, but it cannot be an empty string. The structure to assign a tag is: \\[\\{\"Key\":\"string\",\"Value\":\"string\"\\}\\]."]}
    let make ?complianceResourceIds =
      fun ?complianceResourceTypes ->
        fun ?tags ->
          fun () -> { complianceResourceIds; complianceResourceTypes; tags }
    let to_value x =
      structure_to_value
        [("ComplianceResourceIds",
           (Option.map x.complianceResourceIds
              ~f:ComplianceResourceIdList.to_value));
        ("ComplianceResourceTypes",
          (Option.map x.complianceResourceTypes ~f:ResourceTypeList.to_value));
        ("Tags", (Option.map x.tags ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let complianceResourceTypes =
        (Option.map ~f:ResourceTypeList.of_xml)
          (Xml.child xml_arg0 "ComplianceResourceTypes") in
      let complianceResourceIds =
        (Option.map ~f:ComplianceResourceIdList.of_xml)
          (Xml.child xml_arg0 "ComplianceResourceIds") in
      make ?tags ?complianceResourceTypes ?complianceResourceIds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "Tags" StringMap.of_json in
      let complianceResourceTypes =
        field_map json "ComplianceResourceTypes" ResourceTypeList.of_json in
      let complianceResourceIds =
        field_map json "ComplianceResourceIds"
          ComplianceResourceIdList.of_json in
      make ?tags ?complianceResourceTypes ?complianceResourceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A framework consists of one or more controls. Each control has its own control scope. The control scope can include one or more resource types, a combination of a tag key and value, or a combination of one resource type and one resource ID. If no scope is specified, evaluations for the rule are triggered when any resource in your recording group changes in configuration. To set a control scope that includes all of a particular resource, leave the ControlScope empty or do not pass it when calling CreateFramework."]
module BackupRuleInput =
  struct
    type nonrec t =
      {
      ruleName: BackupRuleName.t
        [@ocaml.doc
          "A display name for a backup rule. Must contain 1 to 50 alphanumeric or '-_.' characters."];
      targetBackupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      scheduleExpression: CronExpression.t option
        [@ocaml.doc
          "A CRON expression in UTC specifying when Backup initiates a backup job."];
      startWindowMinutes: WindowMinutes.t option
        [@ocaml.doc
          "A value in minutes after a backup is scheduled before a job will be canceled if it doesn't start successfully. This value is optional."];
      completionWindowMinutes: WindowMinutes.t option
        [@ocaml.doc
          "A value in minutes after a backup job is successfully started before it must be completed or it will be canceled by Backup. This value is optional."];
      lifecycle: Lifecycle.t option
        [@ocaml.doc
          "The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup will transition and expire backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types."];
      recoveryPointTags: Tags.t option
        [@ocaml.doc
          "To help organize your resources, you can assign your own metadata to the resources that you create. Each tag is a key-value pair."];
      copyActions: CopyActions.t option
        [@ocaml.doc
          "An array of CopyAction objects, which contains the details of the copy operation."];
      enableContinuousBackup: Boolean.t option
        [@ocaml.doc
          "Specifies whether Backup creates continuous backups. True causes Backup to create continuous backups capable of point-in-time restore (PITR). False (or not specified) causes Backup to create snapshot backups."]}
    let context_ = "BackupRuleInput"
    let make ?scheduleExpression =
      fun ?startWindowMinutes ->
        fun ?completionWindowMinutes ->
          fun ?lifecycle ->
            fun ?recoveryPointTags ->
              fun ?copyActions ->
                fun ?enableContinuousBackup ->
                  fun ~ruleName ->
                    fun ~targetBackupVaultName ->
                      fun () ->
                        {
                          scheduleExpression;
                          startWindowMinutes;
                          completionWindowMinutes;
                          lifecycle;
                          recoveryPointTags;
                          copyActions;
                          enableContinuousBackup;
                          ruleName;
                          targetBackupVaultName
                        }
    let to_value x =
      structure_to_value
        [("RuleName", (Some (BackupRuleName.to_value x.ruleName)));
        ("TargetBackupVaultName",
          (Some (BackupVaultName.to_value x.targetBackupVaultName)));
        ("ScheduleExpression",
          (Option.map x.scheduleExpression ~f:CronExpression.to_value));
        ("StartWindowMinutes",
          (Option.map x.startWindowMinutes ~f:WindowMinutes.to_value));
        ("CompletionWindowMinutes",
          (Option.map x.completionWindowMinutes ~f:WindowMinutes.to_value));
        ("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value));
        ("RecoveryPointTags",
          (Option.map x.recoveryPointTags ~f:Tags.to_value));
        ("CopyActions", (Option.map x.copyActions ~f:CopyActions.to_value));
        ("EnableContinuousBackup",
          (Option.map x.enableContinuousBackup ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableContinuousBackup =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnableContinuousBackup") in
      let copyActions =
        (Option.map ~f:CopyActions.of_xml) (Xml.child xml_arg0 "CopyActions") in
      let recoveryPointTags =
        (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "RecoveryPointTags") in
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      let completionWindowMinutes =
        (Option.map ~f:WindowMinutes.of_xml)
          (Xml.child xml_arg0 "CompletionWindowMinutes") in
      let startWindowMinutes =
        (Option.map ~f:WindowMinutes.of_xml)
          (Xml.child xml_arg0 "StartWindowMinutes") in
      let scheduleExpression =
        (Option.map ~f:CronExpression.of_xml)
          (Xml.child xml_arg0 "ScheduleExpression") in
      let targetBackupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetBackupVaultName") in
      let ruleName =
        BackupRuleName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      make ?enableContinuousBackup ?copyActions ?recoveryPointTags ?lifecycle
        ?completionWindowMinutes ?startWindowMinutes ?scheduleExpression
        ~targetBackupVaultName ~ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let enableContinuousBackup =
        field_map json "EnableContinuousBackup" Boolean.of_json in
      let copyActions = field_map json "CopyActions" CopyActions.of_json in
      let recoveryPointTags = field_map json "RecoveryPointTags" Tags.of_json in
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      let completionWindowMinutes =
        field_map json "CompletionWindowMinutes" WindowMinutes.of_json in
      let startWindowMinutes =
        field_map json "StartWindowMinutes" WindowMinutes.of_json in
      let scheduleExpression =
        field_map json "ScheduleExpression" CronExpression.of_json in
      let targetBackupVaultName =
        field_map_exn json "TargetBackupVaultName" BackupVaultName.of_json in
      let ruleName = field_map_exn json "RuleName" BackupRuleName.of_json in
      make ?enableContinuousBackup ?copyActions ?recoveryPointTags ?lifecycle
        ?completionWindowMinutes ?startWindowMinutes ?scheduleExpression
        ~targetBackupVaultName ~ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a scheduled task used to back up a selection of resources."]
module AccountId =
  struct
    type nonrec t = string
    let context_ = "AccountId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"^[0-9]{12}$"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccountId" j
    let to_json = simple_to_json to_value
  end
module IAMRoleArn =
  struct
    type nonrec t = string
    let context_ = "IAMRoleArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IAMRoleArn" j
    let to_json = simple_to_json to_value
  end
module RestoreJobStatus =
  struct
    type nonrec t =
      | PENDING 
      | RUNNING 
      | COMPLETED 
      | ABORTED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | RUNNING -> "RUNNING"
      | COMPLETED -> "COMPLETED"
      | ABORTED -> "ABORTED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "RUNNING" -> RUNNING
      | "COMPLETED" -> COMPLETED
      | "ABORTED" -> ABORTED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RestoreJobStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RestoreJobStatus" j)
    let to_json = simple_to_json to_value
  end
module ReportDeliveryChannel =
  struct
    type nonrec t =
      {
      s3BucketName: String_.t
        [@ocaml.doc
          "The unique name of the S3 bucket that receives your reports."];
      s3KeyPrefix: String_.t option
        [@ocaml.doc
          "The prefix for where Backup Audit Manager delivers your reports to Amazon S3. The prefix is this part of the following path: s3://your-bucket-name/prefix/Backup/us-west-2/year/month/day/report-name. If not specified, there is no prefix."];
      formats: FormatList.t option
        [@ocaml.doc
          "A list of the format of your reports: CSV, JSON, or both. If not specified, the default format is CSV."]}
    let context_ = "ReportDeliveryChannel"
    let make ?s3KeyPrefix =
      fun ?formats ->
        fun ~s3BucketName -> fun () -> { s3KeyPrefix; formats; s3BucketName }
    let to_value x =
      structure_to_value
        [("S3BucketName", (Some (String_.to_value x.s3BucketName)));
        ("S3KeyPrefix", (Option.map x.s3KeyPrefix ~f:String_.to_value));
        ("Formats", (Option.map x.formats ~f:FormatList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let formats =
        (Option.map ~f:FormatList.of_xml) (Xml.child xml_arg0 "Formats") in
      let s3KeyPrefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "S3KeyPrefix") in
      let s3BucketName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "S3BucketName") in
      make ?formats ?s3KeyPrefix ~s3BucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let formats = field_map json "Formats" FormatList.of_json in
      let s3KeyPrefix = field_map json "S3KeyPrefix" String_.of_json in
      let s3BucketName = field_map_exn json "S3BucketName" String_.of_json in
      make ?formats ?s3KeyPrefix ~s3BucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information from your report plan about where to deliver your reports, specifically your Amazon S3 bucket name, S3 key prefix, and the formats of your reports."]
module ReportPlanDescription =
  struct
    type nonrec t = string
    let context_ = "ReportPlanDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReportPlanDescription" j
    let to_json = simple_to_json to_value
  end
module ReportPlanName =
  struct
    type nonrec t = string
    let context_ = "ReportPlanName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z][_a-zA-Z0-9]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReportPlanName" j
    let to_json = simple_to_json to_value
  end
module ReportSetting =
  struct
    type nonrec t =
      {
      reportTemplate: String_.t
        [@ocaml.doc
          "Identifies the report template for the report. Reports are built using a report template. The report templates are: RESOURCE_COMPLIANCE_REPORT | CONTROL_COMPLIANCE_REPORT | BACKUP_JOB_REPORT | COPY_JOB_REPORT | RESTORE_JOB_REPORT"];
      frameworkArns: StringList.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of the frameworks a report covers."];
      numberOfFrameworks: Integer.t option
        [@ocaml.doc "The number of frameworks a report covers."]}
    let context_ = "ReportSetting"
    let make ?frameworkArns =
      fun ?numberOfFrameworks ->
        fun ~reportTemplate ->
          fun () -> { frameworkArns; numberOfFrameworks; reportTemplate }
    let to_value x =
      structure_to_value
        [("ReportTemplate", (Some (String_.to_value x.reportTemplate)));
        ("FrameworkArns",
          (Option.map x.frameworkArns ~f:StringList.to_value));
        ("NumberOfFrameworks",
          (Option.map x.numberOfFrameworks ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let numberOfFrameworks =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "NumberOfFrameworks") in
      let frameworkArns =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "FrameworkArns") in
      let reportTemplate =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReportTemplate") in
      make ?numberOfFrameworks ?frameworkArns ~reportTemplate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let numberOfFrameworks =
        field_map json "NumberOfFrameworks" Integer.of_json in
      let frameworkArns = field_map json "FrameworkArns" StringList.of_json in
      let reportTemplate =
        field_map_exn json "ReportTemplate" String_.of_json in
      make ?numberOfFrameworks ?frameworkArns ~reportTemplate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains detailed information about a report setting."]
module ReportDestination =
  struct
    type nonrec t =
      {
      s3BucketName: String_.t option
        [@ocaml.doc
          "The unique name of the Amazon S3 bucket that receives your reports."];
      s3Keys: StringList.t option
        [@ocaml.doc
          "The object key that uniquely identifies your reports in your S3 bucket."]}
    let make ?s3BucketName =
      fun ?s3Keys -> fun () -> { s3BucketName; s3Keys }
    let to_value x =
      structure_to_value
        [("S3BucketName", (Option.map x.s3BucketName ~f:String_.to_value));
        ("S3Keys", (Option.map x.s3Keys ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Keys =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "S3Keys") in
      let s3BucketName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "S3BucketName") in
      make ?s3Keys ?s3BucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3Keys = field_map json "S3Keys" StringList.of_json in
      let s3BucketName = field_map json "S3BucketName" String_.of_json in
      make ?s3Keys ?s3BucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information from your report job about your report destination."]
module ReportJobId =
  struct
    type nonrec t = string
    let context_ = "ReportJobId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ReportJobId" j
    let to_json = simple_to_json to_value
  end
module RecoveryPointStatus =
  struct
    type nonrec t =
      | COMPLETED 
      | PARTIAL 
      | DELETING 
      | EXPIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | COMPLETED -> "COMPLETED"
      | PARTIAL -> "PARTIAL"
      | DELETING -> "DELETING"
      | EXPIRED -> "EXPIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "COMPLETED" -> COMPLETED
      | "PARTIAL" -> PARTIAL
      | "DELETING" -> DELETING
      | "EXPIRED" -> EXPIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RecoveryPointStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RecoveryPointStatus" j)
    let to_json = simple_to_json to_value
  end
module Boolean__lc1 =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module CalculatedLifecycle =
  struct
    type nonrec t =
      {
      moveToColdStorageAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp that specifies when to transition a recovery point to cold storage."];
      deleteAt: Timestamp.t option
        [@ocaml.doc
          "A timestamp that specifies when to delete a recovery point."]}
    let make ?moveToColdStorageAt =
      fun ?deleteAt -> fun () -> { moveToColdStorageAt; deleteAt }
    let to_value x =
      structure_to_value
        [("MoveToColdStorageAt",
           (Option.map x.moveToColdStorageAt ~f:Timestamp.to_value));
        ("DeleteAt", (Option.map x.deleteAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deleteAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "DeleteAt") in
      let moveToColdStorageAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "MoveToColdStorageAt") in
      make ?deleteAt ?moveToColdStorageAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let deleteAt = field_map json "DeleteAt" Timestamp.of_json in
      let moveToColdStorageAt =
        field_map json "MoveToColdStorageAt" Timestamp.of_json in
      make ?deleteAt ?moveToColdStorageAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains DeleteAt and MoveToColdStorageAt timestamps, which are used to specify a lifecycle for a recovery point. The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup transitions and expires backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types."]
module RecoveryPointCreator =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      backupPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup plan; for example, arn:aws:backup:us-east-1:123456789012:plan:8F81F553-3A74-4A3F-B93D-B3360DC80C50."];
      backupPlanVersion: String_.t option
        [@ocaml.doc
          "Version IDs are unique, randomly generated, Unicode, UTF-8 encoded strings that are at most 1,024 bytes long. They cannot be edited."];
      backupRuleId: String_.t option
        [@ocaml.doc
          "Uniquely identifies a rule used to schedule the backup of a selection of resources."]}
    let make ?backupPlanId =
      fun ?backupPlanArn ->
        fun ?backupPlanVersion ->
          fun ?backupRuleId ->
            fun () ->
              { backupPlanId; backupPlanArn; backupPlanVersion; backupRuleId
              }
    let to_value x =
      structure_to_value
        [("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("BackupPlanArn", (Option.map x.backupPlanArn ~f:ARN.to_value));
        ("BackupPlanVersion",
          (Option.map x.backupPlanVersion ~f:String_.to_value));
        ("BackupRuleId", (Option.map x.backupRuleId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupRuleId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupRuleId") in
      let backupPlanVersion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "BackupPlanVersion") in
      let backupPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupPlanArn") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      make ?backupRuleId ?backupPlanVersion ?backupPlanArn ?backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupRuleId = field_map json "BackupRuleId" String_.of_json in
      let backupPlanVersion =
        field_map json "BackupPlanVersion" String_.of_json in
      let backupPlanArn = field_map json "BackupPlanArn" ARN.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      make ?backupRuleId ?backupPlanVersion ?backupPlanArn ?backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the backup plan and rule that Backup used to initiate the recovery point backup."]
module FrameworkDescription =
  struct
    type nonrec t = string
    let context_ = "FrameworkDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FrameworkDescription" j
    let to_json = simple_to_json to_value
  end
module FrameworkName =
  struct
    type nonrec t = string
    let context_ = "FrameworkName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z][_a-zA-Z0-9]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FrameworkName" j
    let to_json = simple_to_json to_value
  end
module CopyJobState =
  struct
    type nonrec t =
      | CREATED 
      | RUNNING 
      | COMPLETED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATED -> "CREATED"
      | RUNNING -> "RUNNING"
      | COMPLETED -> "COMPLETED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATED" -> CREATED
      | "RUNNING" -> RUNNING
      | "COMPLETED" -> COMPLETED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CopyJobState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CopyJobState" j)
    let to_json = simple_to_json to_value
  end
module Long__lc1 =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module BackupSelectionName =
  struct
    type nonrec t = string
    let context_ = "BackupSelectionName"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"^[a-zA-Z0-9\\-\\_\\.]{1,50}$");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BackupSelectionName" j
    let to_json = simple_to_json to_value
  end
module AdvancedBackupSettings =
  struct
    type nonrec t = AdvancedBackupSetting.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AdvancedBackupSetting.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AdvancedBackupSetting.of_xml)
    let of_json j =
      list_of_json ~kind:"AdvancedBackupSettings"
        ~of_json:AdvancedBackupSetting.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupPlanName =
  struct
    type nonrec t = string
    let context_ = "BackupPlanName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BackupPlanName" j
    let to_json = simple_to_json to_value
  end
module BackupJobState =
  struct
    type nonrec t =
      | CREATED 
      | PENDING 
      | RUNNING 
      | ABORTING 
      | ABORTED 
      | COMPLETED 
      | FAILED 
      | EXPIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATED -> "CREATED"
      | PENDING -> "PENDING"
      | RUNNING -> "RUNNING"
      | ABORTING -> "ABORTING"
      | ABORTED -> "ABORTED"
      | COMPLETED -> "COMPLETED"
      | FAILED -> "FAILED"
      | EXPIRED -> "EXPIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATED" -> CREATED
      | "PENDING" -> PENDING
      | "RUNNING" -> RUNNING
      | "ABORTING" -> ABORTING
      | "ABORTED" -> ABORTED
      | "COMPLETED" -> COMPLETED
      | "FAILED" -> FAILED
      | "EXPIRED" -> EXPIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BackupJobState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BackupJobState" j)
    let to_json = simple_to_json to_value
  end
module ConditionParameters =
  struct
    type nonrec t = ConditionParameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ConditionParameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConditionParameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ConditionParameters"
        ~of_json:ConditionParameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module Condition =
  struct
    type nonrec t =
      {
      conditionType: ConditionType.t
        [@ocaml.doc
          "An operation applied to a key-value pair used to assign resources to your backup plan. Condition only supports StringEquals. For more flexible assignment options, including StringLike and the ability to exclude resources from your backup plan, use Conditions (with an \"s\" on the end) for your BackupSelection ."];
      conditionKey: ConditionKey.t
        [@ocaml.doc
          "The key in a key-value pair. For example, in the tag Department: Accounting, Department is the key."];
      conditionValue: ConditionValue.t
        [@ocaml.doc
          "The value in a key-value pair. For example, in the tag Department: Accounting, Accounting is the value."]}
    let context_ = "Condition"
    let make ~conditionType =
      fun ~conditionKey ->
        fun ~conditionValue ->
          fun () -> { conditionType; conditionKey; conditionValue }
    let to_value x =
      structure_to_value
        [("ConditionType", (Some (ConditionType.to_value x.conditionType)));
        ("ConditionKey", (Some (ConditionKey.to_value x.conditionKey)));
        ("ConditionValue", (Some (ConditionValue.to_value x.conditionValue)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conditionValue =
        ConditionValue.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConditionValue") in
      let conditionKey =
        ConditionKey.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConditionKey") in
      let conditionType =
        ConditionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConditionType") in
      make ~conditionValue ~conditionKey ~conditionType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let conditionValue =
        field_map_exn json "ConditionValue" ConditionValue.of_json in
      let conditionKey =
        field_map_exn json "ConditionKey" ConditionKey.of_json in
      let conditionType =
        field_map_exn json "ConditionType" ConditionType.of_json in
      make ~conditionValue ~conditionKey ~conditionType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains an array of triplets made up of a condition type (such as StringEquals), a key, and a value. Used to filter resources using their tags and assign them to a backup plan. Case sensitive."]
module BackupRule =
  struct
    type nonrec t =
      {
      ruleName: BackupRuleName.t
        [@ocaml.doc
          "A display name for a backup rule. Must contain 1 to 50 alphanumeric or '-_.' characters."];
      targetBackupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      scheduleExpression: CronExpression.t option
        [@ocaml.doc
          "A cron expression in UTC specifying when Backup initiates a backup job. For more information about Amazon Web Services cron expressions, see Schedule Expressions for Rules in the Amazon CloudWatch Events User Guide.. Two examples of Amazon Web Services cron expressions are 15 * ? * * * (take a backup every hour at 15 minutes past the hour) and 0 12 * * ? * (take a backup every day at 12 noon UTC). For a table of examples, click the preceding link and scroll down the page."];
      startWindowMinutes: WindowMinutes.t option
        [@ocaml.doc
          "A value in minutes after a backup is scheduled before a job will be canceled if it doesn't start successfully. This value is optional."];
      completionWindowMinutes: WindowMinutes.t option
        [@ocaml.doc
          "A value in minutes after a backup job is successfully started before it must be completed or it will be canceled by Backup. This value is optional."];
      lifecycle: Lifecycle.t option
        [@ocaml.doc
          "The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup transitions and expires backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types."];
      recoveryPointTags: Tags.t option
        [@ocaml.doc
          "An array of key-value pair strings that are assigned to resources that are associated with this rule when restored from backup."];
      ruleId: String_.t option
        [@ocaml.doc
          "Uniquely identifies a rule that is used to schedule the backup of a selection of resources."];
      copyActions: CopyActions.t option
        [@ocaml.doc
          "An array of CopyAction objects, which contains the details of the copy operation."];
      enableContinuousBackup: Boolean.t option
        [@ocaml.doc
          "Specifies whether Backup creates continuous backups. True causes Backup to create continuous backups capable of point-in-time restore (PITR). False (or not specified) causes Backup to create snapshot backups."]}
    let context_ = "BackupRule"
    let make ?scheduleExpression =
      fun ?startWindowMinutes ->
        fun ?completionWindowMinutes ->
          fun ?lifecycle ->
            fun ?recoveryPointTags ->
              fun ?ruleId ->
                fun ?copyActions ->
                  fun ?enableContinuousBackup ->
                    fun ~ruleName ->
                      fun ~targetBackupVaultName ->
                        fun () ->
                          {
                            scheduleExpression;
                            startWindowMinutes;
                            completionWindowMinutes;
                            lifecycle;
                            recoveryPointTags;
                            ruleId;
                            copyActions;
                            enableContinuousBackup;
                            ruleName;
                            targetBackupVaultName
                          }
    let to_value x =
      structure_to_value
        [("RuleName", (Some (BackupRuleName.to_value x.ruleName)));
        ("TargetBackupVaultName",
          (Some (BackupVaultName.to_value x.targetBackupVaultName)));
        ("ScheduleExpression",
          (Option.map x.scheduleExpression ~f:CronExpression.to_value));
        ("StartWindowMinutes",
          (Option.map x.startWindowMinutes ~f:WindowMinutes.to_value));
        ("CompletionWindowMinutes",
          (Option.map x.completionWindowMinutes ~f:WindowMinutes.to_value));
        ("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value));
        ("RecoveryPointTags",
          (Option.map x.recoveryPointTags ~f:Tags.to_value));
        ("RuleId", (Option.map x.ruleId ~f:String_.to_value));
        ("CopyActions", (Option.map x.copyActions ~f:CopyActions.to_value));
        ("EnableContinuousBackup",
          (Option.map x.enableContinuousBackup ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableContinuousBackup =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "EnableContinuousBackup") in
      let copyActions =
        (Option.map ~f:CopyActions.of_xml) (Xml.child xml_arg0 "CopyActions") in
      let ruleId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RuleId") in
      let recoveryPointTags =
        (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "RecoveryPointTags") in
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      let completionWindowMinutes =
        (Option.map ~f:WindowMinutes.of_xml)
          (Xml.child xml_arg0 "CompletionWindowMinutes") in
      let startWindowMinutes =
        (Option.map ~f:WindowMinutes.of_xml)
          (Xml.child xml_arg0 "StartWindowMinutes") in
      let scheduleExpression =
        (Option.map ~f:CronExpression.of_xml)
          (Xml.child xml_arg0 "ScheduleExpression") in
      let targetBackupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetBackupVaultName") in
      let ruleName =
        BackupRuleName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RuleName") in
      make ?enableContinuousBackup ?copyActions ?ruleId ?recoveryPointTags
        ?lifecycle ?completionWindowMinutes ?startWindowMinutes
        ?scheduleExpression ~targetBackupVaultName ~ruleName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let enableContinuousBackup =
        field_map json "EnableContinuousBackup" Boolean.of_json in
      let copyActions = field_map json "CopyActions" CopyActions.of_json in
      let ruleId = field_map json "RuleId" String_.of_json in
      let recoveryPointTags = field_map json "RecoveryPointTags" Tags.of_json in
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      let completionWindowMinutes =
        field_map json "CompletionWindowMinutes" WindowMinutes.of_json in
      let startWindowMinutes =
        field_map json "StartWindowMinutes" WindowMinutes.of_json in
      let scheduleExpression =
        field_map json "ScheduleExpression" CronExpression.of_json in
      let targetBackupVaultName =
        field_map_exn json "TargetBackupVaultName" BackupVaultName.of_json in
      let ruleName = field_map_exn json "RuleName" BackupRuleName.of_json in
      make ?enableContinuousBackup ?copyActions ?ruleId ?recoveryPointTags
        ?lifecycle ?completionWindowMinutes ?startWindowMinutes
        ?scheduleExpression ~targetBackupVaultName ~ruleName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a scheduled task used to back up a selection of resources."]
module IsEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module GlobalSettingsName =
  struct
    type nonrec t = string
    let context_ = "GlobalSettingsName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GlobalSettingsName" j
    let to_json = simple_to_json to_value
  end
module GlobalSettingsValue =
  struct
    type nonrec t = string
    let context_ = "GlobalSettingsValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GlobalSettingsValue" j
    let to_json = simple_to_json to_value
  end
module FrameworkControl =
  struct
    type nonrec t =
      {
      controlName: ControlName.t
        [@ocaml.doc
          "The name of a control. This name is between 1 and 256 characters."];
      controlInputParameters: ControlInputParameters.t option
        [@ocaml.doc "A list of ParameterName and ParameterValue pairs."];
      controlScope: ControlScope.t option
        [@ocaml.doc
          "The scope of a control. The control scope defines what the control will evaluate. Three examples of control scopes are: a specific backup plan, all backup plans with a specific tag, or all backup plans. For more information, see ControlScope."]}
    let context_ = "FrameworkControl"
    let make ?controlInputParameters =
      fun ?controlScope ->
        fun ~controlName ->
          fun () -> { controlInputParameters; controlScope; controlName }
    let to_value x =
      structure_to_value
        [("ControlName", (Some (ControlName.to_value x.controlName)));
        ("ControlInputParameters",
          (Option.map x.controlInputParameters
             ~f:ControlInputParameters.to_value));
        ("ControlScope",
          (Option.map x.controlScope ~f:ControlScope.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let controlScope =
        (Option.map ~f:ControlScope.of_xml)
          (Xml.child xml_arg0 "ControlScope") in
      let controlInputParameters =
        (Option.map ~f:ControlInputParameters.of_xml)
          (Xml.child xml_arg0 "ControlInputParameters") in
      let controlName =
        ControlName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ControlName") in
      make ?controlScope ?controlInputParameters ~controlName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let controlScope = field_map json "ControlScope" ControlScope.of_json in
      let controlInputParameters =
        field_map json "ControlInputParameters"
          ControlInputParameters.of_json in
      let controlName = field_map_exn json "ControlName" ControlName.of_json in
      make ?controlScope ?controlInputParameters ~controlName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains detailed information about all of the controls of a framework. Each framework must contain at least one control."]
module BackupRulesInput =
  struct
    type nonrec t = BackupRuleInput.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupRuleInput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupRuleInput.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupRulesInput" ~of_json:BackupRuleInput.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module MetadataKey =
  struct
    type nonrec t = string
    let context_ = "MetadataKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetadataKey" j
    let to_json = simple_to_json to_value
  end
module MetadataValue =
  struct
    type nonrec t = string
    let context_ = "MetadataValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetadataValue" j
    let to_json = simple_to_json to_value
  end
module BackupVaultEvent =
  struct
    type nonrec t =
      | BACKUP_JOB_STARTED 
      | BACKUP_JOB_COMPLETED 
      | BACKUP_JOB_SUCCESSFUL 
      | BACKUP_JOB_FAILED 
      | BACKUP_JOB_EXPIRED 
      | RESTORE_JOB_STARTED 
      | RESTORE_JOB_COMPLETED 
      | RESTORE_JOB_SUCCESSFUL 
      | RESTORE_JOB_FAILED 
      | COPY_JOB_STARTED 
      | COPY_JOB_SUCCESSFUL 
      | COPY_JOB_FAILED 
      | RECOVERY_POINT_MODIFIED 
      | BACKUP_PLAN_CREATED 
      | BACKUP_PLAN_MODIFIED 
      | S3_BACKUP_OBJECT_FAILED 
      | S3_RESTORE_OBJECT_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BACKUP_JOB_STARTED -> "BACKUP_JOB_STARTED"
      | BACKUP_JOB_COMPLETED -> "BACKUP_JOB_COMPLETED"
      | BACKUP_JOB_SUCCESSFUL -> "BACKUP_JOB_SUCCESSFUL"
      | BACKUP_JOB_FAILED -> "BACKUP_JOB_FAILED"
      | BACKUP_JOB_EXPIRED -> "BACKUP_JOB_EXPIRED"
      | RESTORE_JOB_STARTED -> "RESTORE_JOB_STARTED"
      | RESTORE_JOB_COMPLETED -> "RESTORE_JOB_COMPLETED"
      | RESTORE_JOB_SUCCESSFUL -> "RESTORE_JOB_SUCCESSFUL"
      | RESTORE_JOB_FAILED -> "RESTORE_JOB_FAILED"
      | COPY_JOB_STARTED -> "COPY_JOB_STARTED"
      | COPY_JOB_SUCCESSFUL -> "COPY_JOB_SUCCESSFUL"
      | COPY_JOB_FAILED -> "COPY_JOB_FAILED"
      | RECOVERY_POINT_MODIFIED -> "RECOVERY_POINT_MODIFIED"
      | BACKUP_PLAN_CREATED -> "BACKUP_PLAN_CREATED"
      | BACKUP_PLAN_MODIFIED -> "BACKUP_PLAN_MODIFIED"
      | S3_BACKUP_OBJECT_FAILED -> "S3_BACKUP_OBJECT_FAILED"
      | S3_RESTORE_OBJECT_FAILED -> "S3_RESTORE_OBJECT_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "BACKUP_JOB_STARTED" -> BACKUP_JOB_STARTED
      | "BACKUP_JOB_COMPLETED" -> BACKUP_JOB_COMPLETED
      | "BACKUP_JOB_SUCCESSFUL" -> BACKUP_JOB_SUCCESSFUL
      | "BACKUP_JOB_FAILED" -> BACKUP_JOB_FAILED
      | "BACKUP_JOB_EXPIRED" -> BACKUP_JOB_EXPIRED
      | "RESTORE_JOB_STARTED" -> RESTORE_JOB_STARTED
      | "RESTORE_JOB_COMPLETED" -> RESTORE_JOB_COMPLETED
      | "RESTORE_JOB_SUCCESSFUL" -> RESTORE_JOB_SUCCESSFUL
      | "RESTORE_JOB_FAILED" -> RESTORE_JOB_FAILED
      | "COPY_JOB_STARTED" -> COPY_JOB_STARTED
      | "COPY_JOB_SUCCESSFUL" -> COPY_JOB_SUCCESSFUL
      | "COPY_JOB_FAILED" -> COPY_JOB_FAILED
      | "RECOVERY_POINT_MODIFIED" -> RECOVERY_POINT_MODIFIED
      | "BACKUP_PLAN_CREATED" -> BACKUP_PLAN_CREATED
      | "BACKUP_PLAN_MODIFIED" -> BACKUP_PLAN_MODIFIED
      | "S3_BACKUP_OBJECT_FAILED" -> S3_BACKUP_OBJECT_FAILED
      | "S3_RESTORE_OBJECT_FAILED" -> S3_RESTORE_OBJECT_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BackupVaultEvent" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BackupVaultEvent" j)
    let to_json = simple_to_json to_value
  end
module RestoreJobsListMember =
  struct
    type nonrec t =
      {
      accountId: AccountId.t option
        [@ocaml.doc "The account ID that owns the restore job."];
      restoreJobId: String_.t option
        [@ocaml.doc
          "Uniquely identifies the job that restores a recovery point."];
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a restore job is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      completionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a job to restore a recovery point is completed, in Unix format and Coordinated Universal Time (UTC). The value of CompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      status: RestoreJobStatus.t option
        [@ocaml.doc
          "A status code specifying the state of the job initiated by Backup to restore a recovery point."];
      statusMessage: String_.t option
        [@ocaml.doc
          "A detailed message explaining the status of the job to restore a recovery point."];
      percentDone: String_.t option
        [@ocaml.doc
          "Contains an estimated percentage complete of a job at the time the job status was queried."];
      backupSizeInBytes: Long.t option
        [@ocaml.doc "The size, in bytes, of the restored resource."];
      iamRoleArn: IAMRoleArn.t option
        [@ocaml.doc
          "Specifies the IAM role ARN used to create the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      expectedCompletionTimeMinutes: Long.t option
        [@ocaml.doc
          "The amount of time in minutes that a job restoring a recovery point is expected to take."];
      createdResourceArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The resource type of the listed restore jobs; for example, an Amazon Elastic Block Store (Amazon EBS) volume or an Amazon Relational Database Service (Amazon RDS) database. For Windows Volume Shadow Copy Service (VSS) backups, the only supported resource type is Amazon EC2."]}
    let make ?accountId =
      fun ?restoreJobId ->
        fun ?recoveryPointArn ->
          fun ?creationDate ->
            fun ?completionDate ->
              fun ?status ->
                fun ?statusMessage ->
                  fun ?percentDone ->
                    fun ?backupSizeInBytes ->
                      fun ?iamRoleArn ->
                        fun ?expectedCompletionTimeMinutes ->
                          fun ?createdResourceArn ->
                            fun ?resourceType ->
                              fun () ->
                                {
                                  accountId;
                                  restoreJobId;
                                  recoveryPointArn;
                                  creationDate;
                                  completionDate;
                                  status;
                                  statusMessage;
                                  percentDone;
                                  backupSizeInBytes;
                                  iamRoleArn;
                                  expectedCompletionTimeMinutes;
                                  createdResourceArn;
                                  resourceType
                                }
    let to_value x =
      structure_to_value
        [("AccountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("RestoreJobId", (Option.map x.restoreJobId ~f:String_.to_value));
        ("RecoveryPointArn", (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CompletionDate",
          (Option.map x.completionDate ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:RestoreJobStatus.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("PercentDone", (Option.map x.percentDone ~f:String_.to_value));
        ("BackupSizeInBytes",
          (Option.map x.backupSizeInBytes ~f:Long.to_value));
        ("IamRoleArn", (Option.map x.iamRoleArn ~f:IAMRoleArn.to_value));
        ("ExpectedCompletionTimeMinutes",
          (Option.map x.expectedCompletionTimeMinutes ~f:Long.to_value));
        ("CreatedResourceArn",
          (Option.map x.createdResourceArn ~f:ARN.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let createdResourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "CreatedResourceArn") in
      let expectedCompletionTimeMinutes =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "ExpectedCompletionTimeMinutes") in
      let iamRoleArn =
        (Option.map ~f:IAMRoleArn.of_xml) (Xml.child xml_arg0 "IamRoleArn") in
      let backupSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BackupSizeInBytes") in
      let percentDone =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PercentDone") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:RestoreJobStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let completionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CompletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      let restoreJobId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RestoreJobId") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "AccountId") in
      make ?resourceType ?createdResourceArn ?expectedCompletionTimeMinutes
        ?iamRoleArn ?backupSizeInBytes ?percentDone ?statusMessage ?status
        ?completionDate ?creationDate ?recoveryPointArn ?restoreJobId
        ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let createdResourceArn =
        field_map json "CreatedResourceArn" ARN.of_json in
      let expectedCompletionTimeMinutes =
        field_map json "ExpectedCompletionTimeMinutes" Long.of_json in
      let iamRoleArn = field_map json "IamRoleArn" IAMRoleArn.of_json in
      let backupSizeInBytes = field_map json "BackupSizeInBytes" Long.of_json in
      let percentDone = field_map json "PercentDone" String_.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let status = field_map json "Status" RestoreJobStatus.of_json in
      let completionDate = field_map json "CompletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      let restoreJobId = field_map json "RestoreJobId" String_.of_json in
      let accountId = field_map json "AccountId" AccountId.of_json in
      make ?resourceType ?createdResourceArn ?expectedCompletionTimeMinutes
        ?iamRoleArn ?backupSizeInBytes ?percentDone ?statusMessage ?status
        ?completionDate ?creationDate ?recoveryPointArn ?restoreJobId
        ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains metadata about a restore job."]
module ReportPlan =
  struct
    type nonrec t =
      {
      reportPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      reportPlanName: ReportPlanName.t option
        [@ocaml.doc
          "The unique name of the report plan. This name is between 1 and 256 characters starting with a letter, and consisting of letters (a-z, A-Z), numbers (0-9), and underscores (_)."];
      reportPlanDescription: ReportPlanDescription.t option
        [@ocaml.doc
          "An optional description of the report plan with a maximum 1,024 characters."];
      reportSetting: ReportSetting.t option
        [@ocaml.doc
          "Identifies the report template for the report. Reports are built using a report template. The report templates are: RESOURCE_COMPLIANCE_REPORT | CONTROL_COMPLIANCE_REPORT | BACKUP_JOB_REPORT | COPY_JOB_REPORT | RESTORE_JOB_REPORT If the report template is RESOURCE_COMPLIANCE_REPORT or CONTROL_COMPLIANCE_REPORT, this API resource also describes the report coverage by Amazon Web Services Regions and frameworks."];
      reportDeliveryChannel: ReportDeliveryChannel.t option
        [@ocaml.doc
          "Contains information about where and how to deliver your reports, specifically your Amazon S3 bucket name, S3 key prefix, and the formats of your reports."];
      deploymentStatus: String_.t option
        [@ocaml.doc
          "The deployment status of a report plan. The statuses are: CREATE_IN_PROGRESS | UPDATE_IN_PROGRESS | DELETE_IN_PROGRESS | COMPLETED"];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a report plan is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      lastAttemptedExecutionTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a report job associated with this report plan last attempted to run, in Unix format and Coordinated Universal Time (UTC). The value of LastAttemptedExecutionTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      lastSuccessfulExecutionTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a report job associated with this report plan last successfully ran, in Unix format and Coordinated Universal Time (UTC). The value of LastSuccessfulExecutionTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    let make ?reportPlanArn =
      fun ?reportPlanName ->
        fun ?reportPlanDescription ->
          fun ?reportSetting ->
            fun ?reportDeliveryChannel ->
              fun ?deploymentStatus ->
                fun ?creationTime ->
                  fun ?lastAttemptedExecutionTime ->
                    fun ?lastSuccessfulExecutionTime ->
                      fun () ->
                        {
                          reportPlanArn;
                          reportPlanName;
                          reportPlanDescription;
                          reportSetting;
                          reportDeliveryChannel;
                          deploymentStatus;
                          creationTime;
                          lastAttemptedExecutionTime;
                          lastSuccessfulExecutionTime
                        }
    let to_value x =
      structure_to_value
        [("ReportPlanArn", (Option.map x.reportPlanArn ~f:ARN.to_value));
        ("ReportPlanName",
          (Option.map x.reportPlanName ~f:ReportPlanName.to_value));
        ("ReportPlanDescription",
          (Option.map x.reportPlanDescription
             ~f:ReportPlanDescription.to_value));
        ("ReportSetting",
          (Option.map x.reportSetting ~f:ReportSetting.to_value));
        ("ReportDeliveryChannel",
          (Option.map x.reportDeliveryChannel
             ~f:ReportDeliveryChannel.to_value));
        ("DeploymentStatus",
          (Option.map x.deploymentStatus ~f:String_.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("LastAttemptedExecutionTime",
          (Option.map x.lastAttemptedExecutionTime ~f:Timestamp.to_value));
        ("LastSuccessfulExecutionTime",
          (Option.map x.lastSuccessfulExecutionTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastSuccessfulExecutionTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastSuccessfulExecutionTime") in
      let lastAttemptedExecutionTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastAttemptedExecutionTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let deploymentStatus =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DeploymentStatus") in
      let reportDeliveryChannel =
        (Option.map ~f:ReportDeliveryChannel.of_xml)
          (Xml.child xml_arg0 "ReportDeliveryChannel") in
      let reportSetting =
        (Option.map ~f:ReportSetting.of_xml)
          (Xml.child xml_arg0 "ReportSetting") in
      let reportPlanDescription =
        (Option.map ~f:ReportPlanDescription.of_xml)
          (Xml.child xml_arg0 "ReportPlanDescription") in
      let reportPlanName =
        (Option.map ~f:ReportPlanName.of_xml)
          (Xml.child xml_arg0 "ReportPlanName") in
      let reportPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ReportPlanArn") in
      make ?lastSuccessfulExecutionTime ?lastAttemptedExecutionTime
        ?creationTime ?deploymentStatus ?reportDeliveryChannel ?reportSetting
        ?reportPlanDescription ?reportPlanName ?reportPlanArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastSuccessfulExecutionTime =
        field_map json "LastSuccessfulExecutionTime" Timestamp.of_json in
      let lastAttemptedExecutionTime =
        field_map json "LastAttemptedExecutionTime" Timestamp.of_json in
      let creationTime = field_map json "CreationTime" Timestamp.of_json in
      let deploymentStatus =
        field_map json "DeploymentStatus" String_.of_json in
      let reportDeliveryChannel =
        field_map json "ReportDeliveryChannel" ReportDeliveryChannel.of_json in
      let reportSetting =
        field_map json "ReportSetting" ReportSetting.of_json in
      let reportPlanDescription =
        field_map json "ReportPlanDescription" ReportPlanDescription.of_json in
      let reportPlanName =
        field_map json "ReportPlanName" ReportPlanName.of_json in
      let reportPlanArn = field_map json "ReportPlanArn" ARN.of_json in
      make ?lastSuccessfulExecutionTime ?lastAttemptedExecutionTime
        ?creationTime ?deploymentStatus ?reportDeliveryChannel ?reportSetting
        ?reportPlanDescription ?reportPlanName ?reportPlanArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains detailed information about a report plan."]
module ReportJob =
  struct
    type nonrec t =
      {
      reportJobId: ReportJobId.t option
        [@ocaml.doc
          "The identifier for a report job. A unique, randomly generated, Unicode, UTF-8 encoded string that is at most 1,024 bytes long. Report job IDs cannot be edited."];
      reportPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      reportTemplate: String_.t option
        [@ocaml.doc
          "Identifies the report template for the report. Reports are built using a report template. The report templates are: RESOURCE_COMPLIANCE_REPORT | CONTROL_COMPLIANCE_REPORT | BACKUP_JOB_REPORT | COPY_JOB_REPORT | RESTORE_JOB_REPORT"];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a report job is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      completionTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a report job is completed, in Unix format and Coordinated Universal Time (UTC). The value of CompletionTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      status: String_.t option
        [@ocaml.doc
          "The status of a report job. The statuses are: CREATED | RUNNING | COMPLETED | FAILED COMPLETED means that the report is available for your review at your designated destination. If the status is FAILED, review the StatusMessage for the reason."];
      statusMessage: String_.t option
        [@ocaml.doc "A message explaining the status of the report job."];
      reportDestination: ReportDestination.t option
        [@ocaml.doc
          "The S3 bucket name and S3 keys for the destination where the report job publishes the report."]}
    let make ?reportJobId =
      fun ?reportPlanArn ->
        fun ?reportTemplate ->
          fun ?creationTime ->
            fun ?completionTime ->
              fun ?status ->
                fun ?statusMessage ->
                  fun ?reportDestination ->
                    fun () ->
                      {
                        reportJobId;
                        reportPlanArn;
                        reportTemplate;
                        creationTime;
                        completionTime;
                        status;
                        statusMessage;
                        reportDestination
                      }
    let to_value x =
      structure_to_value
        [("ReportJobId", (Option.map x.reportJobId ~f:ReportJobId.to_value));
        ("ReportPlanArn", (Option.map x.reportPlanArn ~f:ARN.to_value));
        ("ReportTemplate", (Option.map x.reportTemplate ~f:String_.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("CompletionTime",
          (Option.map x.completionTime ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:String_.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("ReportDestination",
          (Option.map x.reportDestination ~f:ReportDestination.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportDestination =
        (Option.map ~f:ReportDestination.of_xml)
          (Xml.child xml_arg0 "ReportDestination") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Status") in
      let completionTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CompletionTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let reportTemplate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ReportTemplate") in
      let reportPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ReportPlanArn") in
      let reportJobId =
        (Option.map ~f:ReportJobId.of_xml) (Xml.child xml_arg0 "ReportJobId") in
      make ?reportDestination ?statusMessage ?status ?completionTime
        ?creationTime ?reportTemplate ?reportPlanArn ?reportJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reportDestination =
        field_map json "ReportDestination" ReportDestination.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let status = field_map json "Status" String_.of_json in
      let completionTime = field_map json "CompletionTime" Timestamp.of_json in
      let creationTime = field_map json "CreationTime" Timestamp.of_json in
      let reportTemplate = field_map json "ReportTemplate" String_.of_json in
      let reportPlanArn = field_map json "ReportPlanArn" ARN.of_json in
      let reportJobId = field_map json "ReportJobId" ReportJobId.of_json in
      make ?reportDestination ?statusMessage ?status ?completionTime
        ?creationTime ?reportTemplate ?reportPlanArn ?reportJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains detailed information about a report job. A report job compiles a report based on a report plan and publishes it to Amazon S3."]
module RecoveryPointByResource =
  struct
    type nonrec t =
      {
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a recovery point is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      status: RecoveryPointStatus.t option
        [@ocaml.doc
          "A status code specifying the state of the recovery point."];
      statusMessage: String_.t option
        [@ocaml.doc
          "A message explaining the reason of the recovery point deletion failure."];
      encryptionKeyArn: ARN.t option
        [@ocaml.doc
          "The server-side encryption key that is used to protect your backups; for example, arn:aws:kms:us-west-2:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab."];
      backupSizeBytes: Long.t option
        [@ocaml.doc "The size, in bytes, of a backup."];
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."]}
    let make ?recoveryPointArn =
      fun ?creationDate ->
        fun ?status ->
          fun ?statusMessage ->
            fun ?encryptionKeyArn ->
              fun ?backupSizeBytes ->
                fun ?backupVaultName ->
                  fun () ->
                    {
                      recoveryPointArn;
                      creationDate;
                      status;
                      statusMessage;
                      encryptionKeyArn;
                      backupSizeBytes;
                      backupVaultName
                    }
    let to_value x =
      structure_to_value
        [("RecoveryPointArn",
           (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:RecoveryPointStatus.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("EncryptionKeyArn", (Option.map x.encryptionKeyArn ~f:ARN.to_value));
        ("BackupSizeBytes", (Option.map x.backupSizeBytes ~f:Long.to_value));
        ("BackupVaultName",
          (Option.map x.backupVaultName ~f:BackupVaultName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      let backupSizeBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BackupSizeBytes") in
      let encryptionKeyArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "EncryptionKeyArn") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:RecoveryPointStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      make ?backupVaultName ?backupSizeBytes ?encryptionKeyArn ?statusMessage
        ?status ?creationDate ?recoveryPointArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      let backupSizeBytes = field_map json "BackupSizeBytes" Long.of_json in
      let encryptionKeyArn = field_map json "EncryptionKeyArn" ARN.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let status = field_map json "Status" RecoveryPointStatus.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      make ?backupVaultName ?backupSizeBytes ?encryptionKeyArn ?statusMessage
        ?status ?creationDate ?recoveryPointArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains detailed information about a saved recovery point."]
module RecoveryPointByBackupVault =
  struct
    type nonrec t =
      {
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      sourceBackupVaultArn: ARN.t option
        [@ocaml.doc
          "The backup vault where the recovery point was originally copied from. If the recovery point is restored to the same account this value will be null."];
      resourceArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource saved as a recovery point; for example, an Amazon Elastic Block Store (Amazon EBS) volume or an Amazon Relational Database Service (Amazon RDS) database. For Windows Volume Shadow Copy Service (VSS) backups, the only supported resource type is Amazon EC2."];
      createdBy: RecoveryPointCreator.t option
        [@ocaml.doc
          "Contains identifying information about the creation of a recovery point, including the BackupPlanArn, BackupPlanId, BackupPlanVersion, and BackupRuleId of the backup plan that is used to create it."];
      iamRoleArn: IAMRoleArn.t option
        [@ocaml.doc
          "Specifies the IAM role ARN used to create the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      status: RecoveryPointStatus.t option
        [@ocaml.doc
          "A status code specifying the state of the recovery point."];
      statusMessage: String_.t option
        [@ocaml.doc
          "A message explaining the reason of the recovery point deletion failure."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a recovery point is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      completionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a job to restore a recovery point is completed, in Unix format and Coordinated Universal Time (UTC). The value of CompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      backupSizeInBytes: Long.t option
        [@ocaml.doc "The size, in bytes, of a backup."];
      calculatedLifecycle: CalculatedLifecycle.t option
        [@ocaml.doc
          "A CalculatedLifecycle object containing DeleteAt and MoveToColdStorageAt timestamps."];
      lifecycle: Lifecycle.t option
        [@ocaml.doc
          "The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup transitions and expires backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types."];
      encryptionKeyArn: ARN.t option
        [@ocaml.doc
          "The server-side encryption key that is used to protect your backups; for example, arn:aws:kms:us-west-2:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab."];
      isEncrypted: Boolean__lc1.t option
        [@ocaml.doc
          "A Boolean value that is returned as TRUE if the specified recovery point is encrypted, or FALSE if the recovery point is not encrypted."];
      lastRestoreTime: Timestamp.t option
        [@ocaml.doc
          "The date and time a recovery point was last restored, in Unix format and Coordinated Universal Time (UTC). The value of LastRestoreTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    let make ?recoveryPointArn =
      fun ?backupVaultName ->
        fun ?backupVaultArn ->
          fun ?sourceBackupVaultArn ->
            fun ?resourceArn ->
              fun ?resourceType ->
                fun ?createdBy ->
                  fun ?iamRoleArn ->
                    fun ?status ->
                      fun ?statusMessage ->
                        fun ?creationDate ->
                          fun ?completionDate ->
                            fun ?backupSizeInBytes ->
                              fun ?calculatedLifecycle ->
                                fun ?lifecycle ->
                                  fun ?encryptionKeyArn ->
                                    fun ?isEncrypted ->
                                      fun ?lastRestoreTime ->
                                        fun () ->
                                          {
                                            recoveryPointArn;
                                            backupVaultName;
                                            backupVaultArn;
                                            sourceBackupVaultArn;
                                            resourceArn;
                                            resourceType;
                                            createdBy;
                                            iamRoleArn;
                                            status;
                                            statusMessage;
                                            creationDate;
                                            completionDate;
                                            backupSizeInBytes;
                                            calculatedLifecycle;
                                            lifecycle;
                                            encryptionKeyArn;
                                            isEncrypted;
                                            lastRestoreTime
                                          }
    let to_value x =
      structure_to_value
        [("RecoveryPointArn",
           (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("BackupVaultName",
          (Option.map x.backupVaultName ~f:BackupVaultName.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("SourceBackupVaultArn",
          (Option.map x.sourceBackupVaultArn ~f:ARN.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:ARN.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("CreatedBy",
          (Option.map x.createdBy ~f:RecoveryPointCreator.to_value));
        ("IamRoleArn", (Option.map x.iamRoleArn ~f:IAMRoleArn.to_value));
        ("Status", (Option.map x.status ~f:RecoveryPointStatus.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CompletionDate",
          (Option.map x.completionDate ~f:Timestamp.to_value));
        ("BackupSizeInBytes",
          (Option.map x.backupSizeInBytes ~f:Long.to_value));
        ("CalculatedLifecycle",
          (Option.map x.calculatedLifecycle ~f:CalculatedLifecycle.to_value));
        ("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value));
        ("EncryptionKeyArn", (Option.map x.encryptionKeyArn ~f:ARN.to_value));
        ("IsEncrypted", (Option.map x.isEncrypted ~f:Boolean__lc1.to_value));
        ("LastRestoreTime",
          (Option.map x.lastRestoreTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastRestoreTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastRestoreTime") in
      let isEncrypted =
        (Option.map ~f:Boolean__lc1.of_xml)
          (Xml.child xml_arg0 "IsEncrypted") in
      let encryptionKeyArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "EncryptionKeyArn") in
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      let calculatedLifecycle =
        (Option.map ~f:CalculatedLifecycle.of_xml)
          (Xml.child xml_arg0 "CalculatedLifecycle") in
      let backupSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BackupSizeInBytes") in
      let completionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CompletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:RecoveryPointStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let iamRoleArn =
        (Option.map ~f:IAMRoleArn.of_xml) (Xml.child xml_arg0 "IamRoleArn") in
      let createdBy =
        (Option.map ~f:RecoveryPointCreator.of_xml)
          (Xml.child xml_arg0 "CreatedBy") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let resourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let sourceBackupVaultArn =
        (Option.map ~f:ARN.of_xml)
          (Xml.child xml_arg0 "SourceBackupVaultArn") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      make ?lastRestoreTime ?isEncrypted ?encryptionKeyArn ?lifecycle
        ?calculatedLifecycle ?backupSizeInBytes ?completionDate ?creationDate
        ?statusMessage ?status ?iamRoleArn ?createdBy ?resourceType
        ?resourceArn ?sourceBackupVaultArn ?backupVaultArn ?backupVaultName
        ?recoveryPointArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastRestoreTime =
        field_map json "LastRestoreTime" Timestamp.of_json in
      let isEncrypted = field_map json "IsEncrypted" Boolean__lc1.of_json in
      let encryptionKeyArn = field_map json "EncryptionKeyArn" ARN.of_json in
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      let calculatedLifecycle =
        field_map json "CalculatedLifecycle" CalculatedLifecycle.of_json in
      let backupSizeInBytes = field_map json "BackupSizeInBytes" Long.of_json in
      let completionDate = field_map json "CompletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let status = field_map json "Status" RecoveryPointStatus.of_json in
      let iamRoleArn = field_map json "IamRoleArn" IAMRoleArn.of_json in
      let createdBy = field_map json "CreatedBy" RecoveryPointCreator.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let resourceArn = field_map json "ResourceArn" ARN.of_json in
      let sourceBackupVaultArn =
        field_map json "SourceBackupVaultArn" ARN.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      make ?lastRestoreTime ?isEncrypted ?encryptionKeyArn ?lifecycle
        ?calculatedLifecycle ?backupSizeInBytes ?completionDate ?creationDate
        ?statusMessage ?status ?iamRoleArn ?createdBy ?resourceType
        ?resourceArn ?sourceBackupVaultArn ?backupVaultArn ?backupVaultName
        ?recoveryPointArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains detailed information about the recovery points stored in a backup vault."]
module ProtectedResource =
  struct
    type nonrec t =
      {
      resourceArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource; for example, an Amazon Elastic Block Store (Amazon EBS) volume or an Amazon Relational Database Service (Amazon RDS) database. For Windows Volume Shadow Copy Service (VSS) backups, the only supported resource type is Amazon EC2."];
      lastBackupTime: Timestamp.t option
        [@ocaml.doc
          "The date and time a resource was last backed up, in Unix format and Coordinated Universal Time (UTC). The value of LastBackupTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    let make ?resourceArn =
      fun ?resourceType ->
        fun ?lastBackupTime ->
          fun () -> { resourceArn; resourceType; lastBackupTime }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Option.map x.resourceArn ~f:ARN.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("LastBackupTime",
          (Option.map x.lastBackupTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastBackupTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastBackupTime") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let resourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      make ?lastBackupTime ?resourceType ?resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastBackupTime = field_map json "LastBackupTime" Timestamp.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let resourceArn = field_map json "ResourceArn" ARN.of_json in
      make ?lastBackupTime ?resourceType ?resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A structure that contains information about a backed-up resource."]
module Framework =
  struct
    type nonrec t =
      {
      frameworkName: FrameworkName.t option
        [@ocaml.doc
          "The unique name of a framework. This name is between 1 and 256 characters, starting with a letter, and consisting of letters (a-z, A-Z), numbers (0-9), and underscores (_)."];
      frameworkArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      frameworkDescription: FrameworkDescription.t option
        [@ocaml.doc
          "An optional description of the framework with a maximum 1,024 characters."];
      numberOfControls: Integer.t option
        [@ocaml.doc "The number of controls contained by the framework."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a framework is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      deploymentStatus: String_.t option
        [@ocaml.doc
          "The deployment status of a framework. The statuses are: CREATE_IN_PROGRESS | UPDATE_IN_PROGRESS | DELETE_IN_PROGRESS | COMPLETED | FAILED"]}
    let make ?frameworkName =
      fun ?frameworkArn ->
        fun ?frameworkDescription ->
          fun ?numberOfControls ->
            fun ?creationTime ->
              fun ?deploymentStatus ->
                fun () ->
                  {
                    frameworkName;
                    frameworkArn;
                    frameworkDescription;
                    numberOfControls;
                    creationTime;
                    deploymentStatus
                  }
    let to_value x =
      structure_to_value
        [("FrameworkName",
           (Option.map x.frameworkName ~f:FrameworkName.to_value));
        ("FrameworkArn", (Option.map x.frameworkArn ~f:ARN.to_value));
        ("FrameworkDescription",
          (Option.map x.frameworkDescription ~f:FrameworkDescription.to_value));
        ("NumberOfControls",
          (Option.map x.numberOfControls ~f:Integer.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("DeploymentStatus",
          (Option.map x.deploymentStatus ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deploymentStatus =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DeploymentStatus") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let numberOfControls =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "NumberOfControls") in
      let frameworkDescription =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "FrameworkDescription") in
      let frameworkArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "FrameworkArn") in
      let frameworkName =
        (Option.map ~f:FrameworkName.of_xml)
          (Xml.child xml_arg0 "FrameworkName") in
      make ?deploymentStatus ?creationTime ?numberOfControls
        ?frameworkDescription ?frameworkArn ?frameworkName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let deploymentStatus =
        field_map json "DeploymentStatus" String_.of_json in
      let creationTime = field_map json "CreationTime" Timestamp.of_json in
      let numberOfControls =
        field_map json "NumberOfControls" Integer.of_json in
      let frameworkDescription =
        field_map json "FrameworkDescription" FrameworkDescription.of_json in
      let frameworkArn = field_map json "FrameworkArn" ARN.of_json in
      let frameworkName =
        field_map json "FrameworkName" FrameworkName.of_json in
      make ?deploymentStatus ?creationTime ?numberOfControls
        ?frameworkDescription ?frameworkArn ?frameworkName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains detailed information about a framework. Frameworks contain controls, which evaluate and report on your backup events and resources. Frameworks generate daily compliance results."]
module CopyJob =
  struct
    type nonrec t =
      {
      accountId: AccountId.t option
        [@ocaml.doc "The account ID that owns the copy job."];
      copyJobId: String_.t option
        [@ocaml.doc "Uniquely identifies a copy job."];
      sourceBackupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a source copy vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      sourceRecoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a source recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      destinationBackupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a destination copy vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      destinationRecoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a destination recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      resourceArn: ARN.t option
        [@ocaml.doc
          "The Amazon Web Services resource to be copied; for example, an Amazon Elastic Block Store (Amazon EBS) volume or an Amazon Relational Database Service (Amazon RDS) database."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a copy job is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      completionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a copy job is completed, in Unix format and Coordinated Universal Time (UTC). The value of CompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      state: CopyJobState.t option
        [@ocaml.doc "The current state of a copy job."];
      statusMessage: String_.t option
        [@ocaml.doc
          "A detailed message explaining the status of the job to copy a resource."];
      backupSizeInBytes: Long.t option
        [@ocaml.doc "The size, in bytes, of a copy job."];
      iamRoleArn: IAMRoleArn.t option
        [@ocaml.doc
          "Specifies the IAM role ARN used to copy the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      createdBy: RecoveryPointCreator.t option ;
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource to be copied; for example, an Amazon Elastic Block Store (Amazon EBS) volume or an Amazon Relational Database Service (Amazon RDS) database."]}
    let make ?accountId =
      fun ?copyJobId ->
        fun ?sourceBackupVaultArn ->
          fun ?sourceRecoveryPointArn ->
            fun ?destinationBackupVaultArn ->
              fun ?destinationRecoveryPointArn ->
                fun ?resourceArn ->
                  fun ?creationDate ->
                    fun ?completionDate ->
                      fun ?state ->
                        fun ?statusMessage ->
                          fun ?backupSizeInBytes ->
                            fun ?iamRoleArn ->
                              fun ?createdBy ->
                                fun ?resourceType ->
                                  fun () ->
                                    {
                                      accountId;
                                      copyJobId;
                                      sourceBackupVaultArn;
                                      sourceRecoveryPointArn;
                                      destinationBackupVaultArn;
                                      destinationRecoveryPointArn;
                                      resourceArn;
                                      creationDate;
                                      completionDate;
                                      state;
                                      statusMessage;
                                      backupSizeInBytes;
                                      iamRoleArn;
                                      createdBy;
                                      resourceType
                                    }
    let to_value x =
      structure_to_value
        [("AccountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("CopyJobId", (Option.map x.copyJobId ~f:String_.to_value));
        ("SourceBackupVaultArn",
          (Option.map x.sourceBackupVaultArn ~f:ARN.to_value));
        ("SourceRecoveryPointArn",
          (Option.map x.sourceRecoveryPointArn ~f:ARN.to_value));
        ("DestinationBackupVaultArn",
          (Option.map x.destinationBackupVaultArn ~f:ARN.to_value));
        ("DestinationRecoveryPointArn",
          (Option.map x.destinationRecoveryPointArn ~f:ARN.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CompletionDate",
          (Option.map x.completionDate ~f:Timestamp.to_value));
        ("State", (Option.map x.state ~f:CopyJobState.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("BackupSizeInBytes",
          (Option.map x.backupSizeInBytes ~f:Long.to_value));
        ("IamRoleArn", (Option.map x.iamRoleArn ~f:IAMRoleArn.to_value));
        ("CreatedBy",
          (Option.map x.createdBy ~f:RecoveryPointCreator.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let createdBy =
        (Option.map ~f:RecoveryPointCreator.of_xml)
          (Xml.child xml_arg0 "CreatedBy") in
      let iamRoleArn =
        (Option.map ~f:IAMRoleArn.of_xml) (Xml.child xml_arg0 "IamRoleArn") in
      let backupSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BackupSizeInBytes") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let state =
        (Option.map ~f:CopyJobState.of_xml) (Xml.child xml_arg0 "State") in
      let completionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CompletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let resourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let destinationRecoveryPointArn =
        (Option.map ~f:ARN.of_xml)
          (Xml.child xml_arg0 "DestinationRecoveryPointArn") in
      let destinationBackupVaultArn =
        (Option.map ~f:ARN.of_xml)
          (Xml.child xml_arg0 "DestinationBackupVaultArn") in
      let sourceRecoveryPointArn =
        (Option.map ~f:ARN.of_xml)
          (Xml.child xml_arg0 "SourceRecoveryPointArn") in
      let sourceBackupVaultArn =
        (Option.map ~f:ARN.of_xml)
          (Xml.child xml_arg0 "SourceBackupVaultArn") in
      let copyJobId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CopyJobId") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "AccountId") in
      make ?resourceType ?createdBy ?iamRoleArn ?backupSizeInBytes
        ?statusMessage ?state ?completionDate ?creationDate ?resourceArn
        ?destinationRecoveryPointArn ?destinationBackupVaultArn
        ?sourceRecoveryPointArn ?sourceBackupVaultArn ?copyJobId ?accountId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let createdBy = field_map json "CreatedBy" RecoveryPointCreator.of_json in
      let iamRoleArn = field_map json "IamRoleArn" IAMRoleArn.of_json in
      let backupSizeInBytes = field_map json "BackupSizeInBytes" Long.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let state = field_map json "State" CopyJobState.of_json in
      let completionDate = field_map json "CompletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let resourceArn = field_map json "ResourceArn" ARN.of_json in
      let destinationRecoveryPointArn =
        field_map json "DestinationRecoveryPointArn" ARN.of_json in
      let destinationBackupVaultArn =
        field_map json "DestinationBackupVaultArn" ARN.of_json in
      let sourceRecoveryPointArn =
        field_map json "SourceRecoveryPointArn" ARN.of_json in
      let sourceBackupVaultArn =
        field_map json "SourceBackupVaultArn" ARN.of_json in
      let copyJobId = field_map json "CopyJobId" String_.of_json in
      let accountId = field_map json "AccountId" AccountId.of_json in
      make ?resourceType ?createdBy ?iamRoleArn ?backupSizeInBytes
        ?statusMessage ?state ?completionDate ?creationDate ?resourceArn
        ?destinationRecoveryPointArn ?destinationBackupVaultArn
        ?sourceRecoveryPointArn ?sourceBackupVaultArn ?copyJobId ?accountId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains detailed information about a copy job."]
module BackupVaultListMember =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a resource backup is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      encryptionKeyArn: ARN.t option
        [@ocaml.doc
          "A server-side encryption key you can specify to encrypt your backups from services that support full Backup management; for example, arn:aws:kms:us-west-2:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab. If you specify a key, you must specify its ARN, not its alias. If you do not specify a key, Backup creates a KMS key for you by default. To learn which Backup services support full Backup management and how Backup handles encryption for backups from services that do not yet support full Backup, see Encryption for backups in Backup"];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "A unique string that identifies the request and allows failed requests to be retried without the risk of running the operation twice. This parameter is optional. If used, this parameter must contain 1 to 50 alphanumeric or '-_.' characters."];
      numberOfRecoveryPoints: Long__lc1.t option
        [@ocaml.doc
          "The number of recovery points that are stored in a backup vault."];
      locked: Boolean.t option
        [@ocaml.doc
          "A Boolean value that indicates whether Backup Vault Lock applies to the selected backup vault. If true, Vault Lock prevents delete and update operations on the recovery points in the selected vault."];
      minRetentionDays: Long.t option
        [@ocaml.doc
          "The Backup Vault Lock setting that specifies the minimum retention period that the vault retains its recovery points. If this parameter is not specified, Vault Lock does not enforce a minimum retention period. If specified, any backup or copy job to the vault must have a lifecycle policy with a retention period equal to or longer than the minimum retention period. If the job's retention period is shorter than that minimum retention period, then the vault fails the backup or copy job, and you should either modify your lifecycle settings or use a different vault. Recovery points already stored in the vault prior to Vault Lock are not affected."];
      maxRetentionDays: Long.t option
        [@ocaml.doc
          "The Backup Vault Lock setting that specifies the maximum retention period that the vault retains its recovery points. If this parameter is not specified, Vault Lock does not enforce a maximum retention period on the recovery points in the vault (allowing indefinite storage). If specified, any backup or copy job to the vault must have a lifecycle policy with a retention period equal to or shorter than the maximum retention period. If the job's retention period is longer than that maximum retention period, then the vault fails the backup or copy job, and you should either modify your lifecycle settings or use a different vault. Recovery points already stored in the vault prior to Vault Lock are not affected."];
      lockDate: Timestamp.t option
        [@ocaml.doc
          "The date and time when Backup Vault Lock configuration becomes immutable, meaning it cannot be changed or deleted. If you applied Vault Lock to your vault without specifying a lock date, you can change your Vault Lock settings, or delete Vault Lock from the vault entirely, at any time. This value is in Unix format, Coordinated Universal Time (UTC), and accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    let make ?backupVaultName =
      fun ?backupVaultArn ->
        fun ?creationDate ->
          fun ?encryptionKeyArn ->
            fun ?creatorRequestId ->
              fun ?numberOfRecoveryPoints ->
                fun ?locked ->
                  fun ?minRetentionDays ->
                    fun ?maxRetentionDays ->
                      fun ?lockDate ->
                        fun () ->
                          {
                            backupVaultName;
                            backupVaultArn;
                            creationDate;
                            encryptionKeyArn;
                            creatorRequestId;
                            numberOfRecoveryPoints;
                            locked;
                            minRetentionDays;
                            maxRetentionDays;
                            lockDate
                          }
    let to_value x =
      structure_to_value
        [("BackupVaultName",
           (Option.map x.backupVaultName ~f:BackupVaultName.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("EncryptionKeyArn", (Option.map x.encryptionKeyArn ~f:ARN.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value));
        ("NumberOfRecoveryPoints",
          (Option.map x.numberOfRecoveryPoints ~f:Long__lc1.to_value));
        ("Locked", (Option.map x.locked ~f:Boolean.to_value));
        ("MinRetentionDays",
          (Option.map x.minRetentionDays ~f:Long.to_value));
        ("MaxRetentionDays",
          (Option.map x.maxRetentionDays ~f:Long.to_value));
        ("LockDate", (Option.map x.lockDate ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lockDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "LockDate") in
      let maxRetentionDays =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MaxRetentionDays") in
      let minRetentionDays =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MinRetentionDays") in
      let locked =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Locked") in
      let numberOfRecoveryPoints =
        (Option.map ~f:Long__lc1.of_xml)
          (Xml.child xml_arg0 "NumberOfRecoveryPoints") in
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let encryptionKeyArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "EncryptionKeyArn") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      make ?lockDate ?maxRetentionDays ?minRetentionDays ?locked
        ?numberOfRecoveryPoints ?creatorRequestId ?encryptionKeyArn
        ?creationDate ?backupVaultArn ?backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lockDate = field_map json "LockDate" Timestamp.of_json in
      let maxRetentionDays = field_map json "MaxRetentionDays" Long.of_json in
      let minRetentionDays = field_map json "MinRetentionDays" Long.of_json in
      let locked = field_map json "Locked" Boolean.of_json in
      let numberOfRecoveryPoints =
        field_map json "NumberOfRecoveryPoints" Long__lc1.of_json in
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let encryptionKeyArn = field_map json "EncryptionKeyArn" ARN.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      make ?lockDate ?maxRetentionDays ?minRetentionDays ?locked
        ?numberOfRecoveryPoints ?creatorRequestId ?encryptionKeyArn
        ?creationDate ?backupVaultArn ?backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains metadata about a backup vault."]
module BackupSelectionsListMember =
  struct
    type nonrec t =
      {
      selectionId: String_.t option
        [@ocaml.doc
          "Uniquely identifies a request to assign a set of resources to a backup plan."];
      selectionName: BackupSelectionName.t option
        [@ocaml.doc "The display name of a resource selection document."];
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup plan is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "A unique string that identifies the request and allows failed requests to be retried without the risk of running the operation twice. This parameter is optional. If used, this parameter must contain 1 to 50 alphanumeric or '-_.' characters."];
      iamRoleArn: IAMRoleArn.t option
        [@ocaml.doc
          "Specifies the IAM role Amazon Resource Name (ARN) to create the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."]}
    let make ?selectionId =
      fun ?selectionName ->
        fun ?backupPlanId ->
          fun ?creationDate ->
            fun ?creatorRequestId ->
              fun ?iamRoleArn ->
                fun () ->
                  {
                    selectionId;
                    selectionName;
                    backupPlanId;
                    creationDate;
                    creatorRequestId;
                    iamRoleArn
                  }
    let to_value x =
      structure_to_value
        [("SelectionId", (Option.map x.selectionId ~f:String_.to_value));
        ("SelectionName",
          (Option.map x.selectionName ~f:BackupSelectionName.to_value));
        ("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value));
        ("IamRoleArn", (Option.map x.iamRoleArn ~f:IAMRoleArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let iamRoleArn =
        (Option.map ~f:IAMRoleArn.of_xml) (Xml.child xml_arg0 "IamRoleArn") in
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      let selectionName =
        (Option.map ~f:BackupSelectionName.of_xml)
          (Xml.child xml_arg0 "SelectionName") in
      let selectionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SelectionId") in
      make ?iamRoleArn ?creatorRequestId ?creationDate ?backupPlanId
        ?selectionName ?selectionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let iamRoleArn = field_map json "IamRoleArn" IAMRoleArn.of_json in
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      let selectionName =
        field_map json "SelectionName" BackupSelectionName.of_json in
      let selectionId = field_map json "SelectionId" String_.of_json in
      make ?iamRoleArn ?creatorRequestId ?creationDate ?backupPlanId
        ?selectionName ?selectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains metadata about a BackupSelection object."]
module BackupPlansListMember =
  struct
    type nonrec t =
      {
      backupPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup plan; for example, arn:aws:backup:us-east-1:123456789012:plan:8F81F553-3A74-4A3F-B93D-B3360DC80C50."];
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a resource backup plan is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      deletionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup plan is deleted, in Unix format and Coordinated Universal Time (UTC). The value of DeletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      versionId: String_.t option
        [@ocaml.doc
          "Unique, randomly generated, Unicode, UTF-8 encoded strings that are at most 1,024 bytes long. Version IDs cannot be edited."];
      backupPlanName: BackupPlanName.t option
        [@ocaml.doc "The display name of a saved backup plan."];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "A unique string that identifies the request and allows failed requests to be retried without the risk of running the operation twice. This parameter is optional. If used, this parameter must contain 1 to 50 alphanumeric or '-_.' characters."];
      lastExecutionDate: Timestamp.t option
        [@ocaml.doc
          "The last time a job to back up resources was run with this rule. A date and time, in Unix format and Coordinated Universal Time (UTC). The value of LastExecutionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      advancedBackupSettings: AdvancedBackupSettings.t option
        [@ocaml.doc "Contains a list of BackupOptions for a resource type."]}
    let make ?backupPlanArn =
      fun ?backupPlanId ->
        fun ?creationDate ->
          fun ?deletionDate ->
            fun ?versionId ->
              fun ?backupPlanName ->
                fun ?creatorRequestId ->
                  fun ?lastExecutionDate ->
                    fun ?advancedBackupSettings ->
                      fun () ->
                        {
                          backupPlanArn;
                          backupPlanId;
                          creationDate;
                          deletionDate;
                          versionId;
                          backupPlanName;
                          creatorRequestId;
                          lastExecutionDate;
                          advancedBackupSettings
                        }
    let to_value x =
      structure_to_value
        [("BackupPlanArn", (Option.map x.backupPlanArn ~f:ARN.to_value));
        ("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("DeletionDate", (Option.map x.deletionDate ~f:Timestamp.to_value));
        ("VersionId", (Option.map x.versionId ~f:String_.to_value));
        ("BackupPlanName",
          (Option.map x.backupPlanName ~f:BackupPlanName.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value));
        ("LastExecutionDate",
          (Option.map x.lastExecutionDate ~f:Timestamp.to_value));
        ("AdvancedBackupSettings",
          (Option.map x.advancedBackupSettings
             ~f:AdvancedBackupSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let advancedBackupSettings =
        (Option.map ~f:AdvancedBackupSettings.of_xml)
          (Xml.child xml_arg0 "AdvancedBackupSettings") in
      let lastExecutionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastExecutionDate") in
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let backupPlanName =
        (Option.map ~f:BackupPlanName.of_xml)
          (Xml.child xml_arg0 "BackupPlanName") in
      let versionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VersionId") in
      let deletionDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "DeletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      let backupPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupPlanArn") in
      make ?advancedBackupSettings ?lastExecutionDate ?creatorRequestId
        ?backupPlanName ?versionId ?deletionDate ?creationDate ?backupPlanId
        ?backupPlanArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let advancedBackupSettings =
        field_map json "AdvancedBackupSettings"
          AdvancedBackupSettings.of_json in
      let lastExecutionDate =
        field_map json "LastExecutionDate" Timestamp.of_json in
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let backupPlanName =
        field_map json "BackupPlanName" BackupPlanName.of_json in
      let versionId = field_map json "VersionId" String_.of_json in
      let deletionDate = field_map json "DeletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      let backupPlanArn = field_map json "BackupPlanArn" ARN.of_json in
      make ?advancedBackupSettings ?lastExecutionDate ?creatorRequestId
        ?backupPlanName ?versionId ?deletionDate ?creationDate ?backupPlanId
        ?backupPlanArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains metadata about a backup plan."]
module BackupPlanTemplatesListMember =
  struct
    type nonrec t =
      {
      backupPlanTemplateId: String_.t option
        [@ocaml.doc "Uniquely identifies a stored backup plan template."];
      backupPlanTemplateName: String_.t option
        [@ocaml.doc "The optional display name of a backup plan template."]}
    let make ?backupPlanTemplateId =
      fun ?backupPlanTemplateName ->
        fun () -> { backupPlanTemplateId; backupPlanTemplateName }
    let to_value x =
      structure_to_value
        [("BackupPlanTemplateId",
           (Option.map x.backupPlanTemplateId ~f:String_.to_value));
        ("BackupPlanTemplateName",
          (Option.map x.backupPlanTemplateName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanTemplateName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "BackupPlanTemplateName") in
      let backupPlanTemplateId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "BackupPlanTemplateId") in
      make ?backupPlanTemplateName ?backupPlanTemplateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanTemplateName =
        field_map json "BackupPlanTemplateName" String_.of_json in
      let backupPlanTemplateId =
        field_map json "BackupPlanTemplateId" String_.of_json in
      make ?backupPlanTemplateName ?backupPlanTemplateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object specifying metadata associated with a backup plan template."]
module BackupJob =
  struct
    type nonrec t =
      {
      accountId: AccountId.t option
        [@ocaml.doc "The account ID that owns the backup job."];
      backupJobId: String_.t option
        [@ocaml.doc
          "Uniquely identifies a request to Backup to back up a resource."];
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      resourceArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup job is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      completionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a job to create a backup job is completed, in Unix format and Coordinated Universal Time (UTC). The value of CompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      state: BackupJobState.t option
        [@ocaml.doc "The current state of a resource recovery point."];
      statusMessage: String_.t option
        [@ocaml.doc
          "A detailed message explaining the status of the job to back up a resource."];
      percentDone: String_.t option
        [@ocaml.doc
          "Contains an estimated percentage complete of a job at the time the job status was queried."];
      backupSizeInBytes: Long.t option
        [@ocaml.doc "The size, in bytes, of a backup."];
      iamRoleArn: IAMRoleArn.t option
        [@ocaml.doc
          "Specifies the IAM role ARN used to create the target recovery point. IAM roles other than the default role must include either AWSBackup or AwsBackup in the role name. For example, arn:aws:iam::123456789012:role/AWSBackupRDSAccess. Role names without those strings lack permissions to perform backup jobs."];
      createdBy: RecoveryPointCreator.t option
        [@ocaml.doc
          "Contains identifying information about the creation of a backup job, including the BackupPlanArn, BackupPlanId, BackupPlanVersion, and BackupRuleId of the backup plan used to create it."];
      expectedCompletionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a job to back up resources is expected to be completed, in Unix format and Coordinated Universal Time (UTC). The value of ExpectedCompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      startBy: Timestamp.t option
        [@ocaml.doc
          "Specifies the time in Unix format and Coordinated Universal Time (UTC) when a backup job must be started before it is canceled. The value is calculated by adding the start window to the scheduled time. So if the scheduled time were 6:00 PM and the start window is 2 hours, the StartBy time would be 8:00 PM on the date specified. The value of StartBy is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource to be backed up; for example, an Amazon Elastic Block Store (Amazon EBS) volume or an Amazon Relational Database Service (Amazon RDS) database. For Windows Volume Shadow Copy Service (VSS) backups, the only supported resource type is Amazon EC2."];
      bytesTransferred: Long.t option
        [@ocaml.doc
          "The size in bytes transferred to a backup vault at the time that the job status was queried."];
      backupOptions: BackupOptions.t option
        [@ocaml.doc
          "Specifies the backup option for a selected resource. This option is only available for Windows Volume Shadow Copy Service (VSS) backup jobs. Valid values: Set to \"WindowsVSS\":\"enabled\" to enable the WindowsVSS backup option and create a Windows VSS backup. Set to \"WindowsVSS\":\"disabled\" to create a regular backup. If you specify an invalid option, you get an InvalidParameterValueException exception."];
      backupType: String_.t option
        [@ocaml.doc "Represents the type of backup for a backup job."]}
    let make ?accountId =
      fun ?backupJobId ->
        fun ?backupVaultName ->
          fun ?backupVaultArn ->
            fun ?recoveryPointArn ->
              fun ?resourceArn ->
                fun ?creationDate ->
                  fun ?completionDate ->
                    fun ?state ->
                      fun ?statusMessage ->
                        fun ?percentDone ->
                          fun ?backupSizeInBytes ->
                            fun ?iamRoleArn ->
                              fun ?createdBy ->
                                fun ?expectedCompletionDate ->
                                  fun ?startBy ->
                                    fun ?resourceType ->
                                      fun ?bytesTransferred ->
                                        fun ?backupOptions ->
                                          fun ?backupType ->
                                            fun () ->
                                              {
                                                accountId;
                                                backupJobId;
                                                backupVaultName;
                                                backupVaultArn;
                                                recoveryPointArn;
                                                resourceArn;
                                                creationDate;
                                                completionDate;
                                                state;
                                                statusMessage;
                                                percentDone;
                                                backupSizeInBytes;
                                                iamRoleArn;
                                                createdBy;
                                                expectedCompletionDate;
                                                startBy;
                                                resourceType;
                                                bytesTransferred;
                                                backupOptions;
                                                backupType
                                              }
    let to_value x =
      structure_to_value
        [("AccountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("BackupJobId", (Option.map x.backupJobId ~f:String_.to_value));
        ("BackupVaultName",
          (Option.map x.backupVaultName ~f:BackupVaultName.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("RecoveryPointArn", (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CompletionDate",
          (Option.map x.completionDate ~f:Timestamp.to_value));
        ("State", (Option.map x.state ~f:BackupJobState.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("PercentDone", (Option.map x.percentDone ~f:String_.to_value));
        ("BackupSizeInBytes",
          (Option.map x.backupSizeInBytes ~f:Long.to_value));
        ("IamRoleArn", (Option.map x.iamRoleArn ~f:IAMRoleArn.to_value));
        ("CreatedBy",
          (Option.map x.createdBy ~f:RecoveryPointCreator.to_value));
        ("ExpectedCompletionDate",
          (Option.map x.expectedCompletionDate ~f:Timestamp.to_value));
        ("StartBy", (Option.map x.startBy ~f:Timestamp.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("BytesTransferred",
          (Option.map x.bytesTransferred ~f:Long.to_value));
        ("BackupOptions",
          (Option.map x.backupOptions ~f:BackupOptions.to_value));
        ("BackupType", (Option.map x.backupType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupType") in
      let backupOptions =
        (Option.map ~f:BackupOptions.of_xml)
          (Xml.child xml_arg0 "BackupOptions") in
      let bytesTransferred =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BytesTransferred") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let startBy =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartBy") in
      let expectedCompletionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ExpectedCompletionDate") in
      let createdBy =
        (Option.map ~f:RecoveryPointCreator.of_xml)
          (Xml.child xml_arg0 "CreatedBy") in
      let iamRoleArn =
        (Option.map ~f:IAMRoleArn.of_xml) (Xml.child xml_arg0 "IamRoleArn") in
      let backupSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BackupSizeInBytes") in
      let percentDone =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PercentDone") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let state =
        (Option.map ~f:BackupJobState.of_xml) (Xml.child xml_arg0 "State") in
      let completionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CompletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let resourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      let backupJobId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupJobId") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "AccountId") in
      make ?backupType ?backupOptions ?bytesTransferred ?resourceType
        ?startBy ?expectedCompletionDate ?createdBy ?iamRoleArn
        ?backupSizeInBytes ?percentDone ?statusMessage ?state ?completionDate
        ?creationDate ?resourceArn ?recoveryPointArn ?backupVaultArn
        ?backupVaultName ?backupJobId ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupType = field_map json "BackupType" String_.of_json in
      let backupOptions =
        field_map json "BackupOptions" BackupOptions.of_json in
      let bytesTransferred = field_map json "BytesTransferred" Long.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let startBy = field_map json "StartBy" Timestamp.of_json in
      let expectedCompletionDate =
        field_map json "ExpectedCompletionDate" Timestamp.of_json in
      let createdBy = field_map json "CreatedBy" RecoveryPointCreator.of_json in
      let iamRoleArn = field_map json "IamRoleArn" IAMRoleArn.of_json in
      let backupSizeInBytes = field_map json "BackupSizeInBytes" Long.of_json in
      let percentDone = field_map json "PercentDone" String_.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let state = field_map json "State" BackupJobState.of_json in
      let completionDate = field_map json "CompletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let resourceArn = field_map json "ResourceArn" ARN.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      let backupJobId = field_map json "BackupJobId" String_.of_json in
      let accountId = field_map json "AccountId" AccountId.of_json in
      make ?backupType ?backupOptions ?bytesTransferred ?resourceType
        ?startBy ?expectedCompletionDate ?createdBy ?iamRoleArn
        ?backupSizeInBytes ?percentDone ?statusMessage ?state ?completionDate
        ?creationDate ?resourceArn ?recoveryPointArn ?backupVaultArn
        ?backupVaultName ?backupJobId ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains detailed information about a backup job."]
module Conditions =
  struct
    type nonrec t =
      {
      stringEquals: ConditionParameters.t option
        [@ocaml.doc
          "Filters the values of your tagged resources for only those resources that you tagged with the same value. Also called \"exact matching.\""];
      stringNotEquals: ConditionParameters.t option
        [@ocaml.doc
          "Filters the values of your tagged resources for only those resources that you tagged that do not have the same value. Also called \"negated matching.\""];
      stringLike: ConditionParameters.t option
        [@ocaml.doc
          "Filters the values of your tagged resources for matching tag values with the use of a wildcard character (*) anywhere in the string. For example, \"prod*\" or \"*rod*\" matches the tag value \"production\"."];
      stringNotLike: ConditionParameters.t option
        [@ocaml.doc
          "Filters the values of your tagged resources for non-matching tag values with the use of a wildcard character (*) anywhere in the string."]}
    let make ?stringEquals =
      fun ?stringNotEquals ->
        fun ?stringLike ->
          fun ?stringNotLike ->
            fun () ->
              { stringEquals; stringNotEquals; stringLike; stringNotLike }
    let to_value x =
      structure_to_value
        [("StringEquals",
           (Option.map x.stringEquals ~f:ConditionParameters.to_value));
        ("StringNotEquals",
          (Option.map x.stringNotEquals ~f:ConditionParameters.to_value));
        ("StringLike",
          (Option.map x.stringLike ~f:ConditionParameters.to_value));
        ("StringNotLike",
          (Option.map x.stringNotLike ~f:ConditionParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stringNotLike =
        (Option.map ~f:ConditionParameters.of_xml)
          (Xml.child xml_arg0 "StringNotLike") in
      let stringLike =
        (Option.map ~f:ConditionParameters.of_xml)
          (Xml.child xml_arg0 "StringLike") in
      let stringNotEquals =
        (Option.map ~f:ConditionParameters.of_xml)
          (Xml.child xml_arg0 "StringNotEquals") in
      let stringEquals =
        (Option.map ~f:ConditionParameters.of_xml)
          (Xml.child xml_arg0 "StringEquals") in
      make ?stringNotLike ?stringLike ?stringNotEquals ?stringEquals ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let stringNotLike =
        field_map json "StringNotLike" ConditionParameters.of_json in
      let stringLike =
        field_map json "StringLike" ConditionParameters.of_json in
      let stringNotEquals =
        field_map json "StringNotEquals" ConditionParameters.of_json in
      let stringEquals =
        field_map json "StringEquals" ConditionParameters.of_json in
      make ?stringNotLike ?stringLike ?stringNotEquals ?stringEquals ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about which resources to include or exclude from a backup plan using their tags. Conditions are case sensitive."]
module ListOfTags =
  struct
    type nonrec t = Condition.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Condition.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Condition.of_xml)
    let of_json j =
      list_of_json ~kind:"ListOfTags" ~of_json:Condition.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceArns =
  struct
    type nonrec t = ARN.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ARN.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ARN.of_xml)
    let of_json j = list_of_json ~kind:"ResourceArns" ~of_json:ARN.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupRules =
  struct
    type nonrec t = BackupRule.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupRule.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupRules" ~of_json:BackupRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConflictException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Backup can't perform the action that you requested until it finishes performing a previous action. Try again later."]
module InvalidParameterValueException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that something is wrong with a parameter's value. For example, the value is out of range."]
module MissingParameterValueException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Indicates that a required parameter is missing."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A resource that is required for the action doesn't exist."]
module ServiceUnavailableException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request failed due to a temporary failure of the server."]
module ResourceTypeManagementPreference =
  struct
    type nonrec t = (ResourceType.t * IsEnabled.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ResourceType.of_string chopped),
                              (IsEnabled.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ResourceType.to_value x) |>
                    (fun x -> (IsEnabled.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ResourceType.of_string
        ~of_json:IsEnabled.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceTypeOptInPreference =
  struct
    type nonrec t = (ResourceType.t * IsEnabled.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ResourceType.of_string chopped),
                              (IsEnabled.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ResourceType.to_value x) |>
                    (fun x -> (IsEnabled.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ResourceType.of_string
        ~of_json:IsEnabled.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidRequestException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Indicates that something is wrong with the input to the request. For example, a parameter is of the wrong type."]
module GlobalSettings =
  struct
    type nonrec t = (GlobalSettingsName.t * GlobalSettingsValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((GlobalSettingsName.of_string chopped),
                              (GlobalSettingsValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (GlobalSettingsName.to_value x) |>
                    (fun x ->
                       (GlobalSettingsValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:GlobalSettingsName.of_string
        ~of_json:GlobalSettingsValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module AlreadyExistsException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      creatorRequestId: String_.t option ;
      arn: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?creatorRequestId ->
          fun ?arn ->
            fun ?type_ ->
              fun ?context ->
                fun () ->
                  { code; message; creatorRequestId; arn; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value));
        ("Arn", (Option.map x.arn ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Arn") in
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?arn ?creatorRequestId ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let arn = field_map json "Arn" String_.of_json in
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?arn ?creatorRequestId ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The required resource already exists."]
module LimitExceededException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A limit in the request has been exceeded; for example, a maximum number of items allowed in a request."]
module FrameworkControls =
  struct
    type nonrec t = FrameworkControl.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FrameworkControl.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FrameworkControl.of_xml)
    let of_json j =
      list_of_json ~kind:"FrameworkControls"
        ~of_json:FrameworkControl.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupPlanInput =
  struct
    type nonrec t =
      {
      backupPlanName: BackupPlanName.t
        [@ocaml.doc
          "The display name of a backup plan. Must contain 1 to 50 alphanumeric or '-_.' characters."];
      rules: BackupRulesInput.t
        [@ocaml.doc
          "An array of BackupRule objects, each of which specifies a scheduled task that is used to back up a selection of resources."];
      advancedBackupSettings: AdvancedBackupSettings.t option
        [@ocaml.doc
          "Specifies a list of BackupOptions for each resource type. These settings are only available for Windows Volume Shadow Copy Service (VSS) backup jobs."]}
    let context_ = "BackupPlanInput"
    let make ?advancedBackupSettings =
      fun ~backupPlanName ->
        fun ~rules ->
          fun () -> { advancedBackupSettings; backupPlanName; rules }
    let to_value x =
      structure_to_value
        [("BackupPlanName",
           (Some (BackupPlanName.to_value x.backupPlanName)));
        ("Rules", (Some (BackupRulesInput.to_value x.rules)));
        ("AdvancedBackupSettings",
          (Option.map x.advancedBackupSettings
             ~f:AdvancedBackupSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let advancedBackupSettings =
        (Option.map ~f:AdvancedBackupSettings.of_xml)
          (Xml.child xml_arg0 "AdvancedBackupSettings") in
      let rules =
        BackupRulesInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Rules") in
      let backupPlanName =
        BackupPlanName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BackupPlanName") in
      make ?advancedBackupSettings ~rules ~backupPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let advancedBackupSettings =
        field_map json "AdvancedBackupSettings"
          AdvancedBackupSettings.of_json in
      let rules = field_map_exn json "Rules" BackupRulesInput.of_json in
      let backupPlanName =
        field_map_exn json "BackupPlanName" BackupPlanName.of_json in
      make ?advancedBackupSettings ~rules ~backupPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains an optional backup plan display name and an array of BackupRule objects, each of which specifies a backup rule. Each rule in a backup plan is a separate scheduled task."]
module TagKeyList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"TagKeyList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module RestoreJobId =
  struct
    type nonrec t = string
    let context_ = "RestoreJobId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RestoreJobId" j
    let to_json = simple_to_json to_value
  end
module Metadata =
  struct
    type nonrec t = (MetadataKey.t * MetadataValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((MetadataKey.of_string chopped),
                              (MetadataValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (MetadataKey.to_value x) |>
                    (fun x -> (MetadataValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:MetadataKey.of_string
        ~of_json:MetadataValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupVaultEvents =
  struct
    type nonrec t = BackupVaultEvent.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupVaultEvent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupVaultEvent.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupVaultEvents"
        ~of_json:BackupVaultEvent.of_json j
    let to_json v = composed_to_json to_value v
  end
module IAMPolicy =
  struct
    type nonrec t = string
    let context_ = "IAMPolicy"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IAMPolicy" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module RestoreJobsList =
  struct
    type nonrec t = RestoreJobsListMember.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RestoreJobsListMember.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RestoreJobsListMember.of_xml)
    let of_json j =
      list_of_json ~kind:"RestoreJobsList"
        ~of_json:RestoreJobsListMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportPlanList =
  struct
    type nonrec t = ReportPlan.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ReportPlan.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReportPlan.of_xml)
    let of_json j =
      list_of_json ~kind:"ReportPlanList" ~of_json:ReportPlan.of_json j
    let to_json v = composed_to_json to_value v
  end
module ReportJobList =
  struct
    type nonrec t = ReportJob.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ReportJob.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ReportJob.of_xml)
    let of_json j =
      list_of_json ~kind:"ReportJobList" ~of_json:ReportJob.of_json j
    let to_json v = composed_to_json to_value v
  end
module RecoveryPointByResourceList =
  struct
    type nonrec t = RecoveryPointByResource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RecoveryPointByResource.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecoveryPointByResource.of_xml)
    let of_json j =
      list_of_json ~kind:"RecoveryPointByResourceList"
        ~of_json:RecoveryPointByResource.of_json j
    let to_json v = composed_to_json to_value v
  end
module RecoveryPointByBackupVaultList =
  struct
    type nonrec t = RecoveryPointByBackupVault.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RecoveryPointByBackupVault.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecoveryPointByBackupVault.of_xml)
    let of_json j =
      list_of_json ~kind:"RecoveryPointByBackupVaultList"
        ~of_json:RecoveryPointByBackupVault.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProtectedResourcesList =
  struct
    type nonrec t = ProtectedResource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProtectedResource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProtectedResource.of_xml)
    let of_json j =
      list_of_json ~kind:"ProtectedResourcesList"
        ~of_json:ProtectedResource.of_json j
    let to_json v = composed_to_json to_value v
  end
module FrameworkList =
  struct
    type nonrec t = Framework.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Framework.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Framework.of_xml)
    let of_json j =
      list_of_json ~kind:"FrameworkList" ~of_json:Framework.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxFrameworkInputs =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxFrameworkInputs" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CopyJobsList =
  struct
    type nonrec t = CopyJob.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CopyJob.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CopyJob.of_xml)
    let of_json j =
      list_of_json ~kind:"CopyJobsList" ~of_json:CopyJob.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupVaultList =
  struct
    type nonrec t = BackupVaultListMember.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupVaultListMember.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupVaultListMember.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupVaultList"
        ~of_json:BackupVaultListMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupSelectionsList =
  struct
    type nonrec t = BackupSelectionsListMember.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupSelectionsListMember.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupSelectionsListMember.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupSelectionsList"
        ~of_json:BackupSelectionsListMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupPlansList =
  struct
    type nonrec t = BackupPlansListMember.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupPlansListMember.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupPlansListMember.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupPlansList"
        ~of_json:BackupPlansListMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupPlanVersionsList =
  struct
    type nonrec t = BackupPlansListMember.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupPlansListMember.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupPlansListMember.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupPlanVersionsList"
        ~of_json:BackupPlansListMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupPlanTemplatesList =
  struct
    type nonrec t = BackupPlanTemplatesListMember.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupPlanTemplatesListMember.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupPlanTemplatesListMember.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupPlanTemplatesList"
        ~of_json:BackupPlanTemplatesListMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupJobsList =
  struct
    type nonrec t = BackupJob.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BackupJob.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BackupJob.of_xml)
    let of_json j =
      list_of_json ~kind:"BackupJobsList" ~of_json:BackupJob.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceTypes =
  struct
    type nonrec t = ResourceType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceType.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceTypes" ~of_json:ResourceType.of_json j
    let to_json v = composed_to_json to_value v
  end
module BackupSelection =
  struct
    type nonrec t =
      {
      selectionName: BackupSelectionName.t
        [@ocaml.doc
          "The display name of a resource selection document. Must contain 1 to 50 alphanumeric or '-_.' characters."];
      iamRoleArn: IAMRoleArn.t
        [@ocaml.doc
          "The ARN of the IAM role that Backup uses to authenticate when backing up the target resource; for example, arn:aws:iam::123456789012:role/S3Access."];
      resources: ResourceArns.t option
        [@ocaml.doc
          "A list of Amazon Resource Names (ARNs) to assign to a backup plan. The maximum number of ARNs is 500 without wildcards, or 30 ARNs with wildcards. If you need to assign many resources to a backup plan, consider a different resource selection strategy, such as assigning all resources of a resource type or refining your resource selection using tags."];
      listOfTags: ListOfTags.t option
        [@ocaml.doc
          "A list of conditions that you define to assign resources to your backup plans using tags. For example, \"StringEquals\": \\{\"Department\": \"accounting\". Condition operators are case sensitive. ListOfTags differs from Conditions as follows: When you specify more than one condition, you assign all resources that match AT LEAST ONE condition (using OR logic). ListOfTags only supports StringEquals. Conditions supports StringEquals, StringLike, StringNotEquals, and StringNotLike."];
      notResources: ResourceArns.t option
        [@ocaml.doc
          "A list of Amazon Resource Names (ARNs) to exclude from a backup plan. The maximum number of ARNs is 500 without wildcards, or 30 ARNs with wildcards. If you need to exclude many resources from a backup plan, consider a different resource selection strategy, such as assigning only one or a few resource types or refining your resource selection using tags."];
      conditions: Conditions.t option
        [@ocaml.doc
          "A list of conditions that you define to assign resources to your backup plans using tags. For example, \"StringEquals\": \\{\"Department\": \"accounting\". Condition operators are case sensitive. Conditions differs from ListOfTags as follows: When you specify more than one condition, you only assign the resources that match ALL conditions (using AND logic). Conditions supports StringEquals, StringLike, StringNotEquals, and StringNotLike. ListOfTags only supports StringEquals."]}
    let context_ = "BackupSelection"
    let make ?resources =
      fun ?listOfTags ->
        fun ?notResources ->
          fun ?conditions ->
            fun ~selectionName ->
              fun ~iamRoleArn ->
                fun () ->
                  {
                    resources;
                    listOfTags;
                    notResources;
                    conditions;
                    selectionName;
                    iamRoleArn
                  }
    let to_value x =
      structure_to_value
        [("SelectionName",
           (Some (BackupSelectionName.to_value x.selectionName)));
        ("IamRoleArn", (Some (IAMRoleArn.to_value x.iamRoleArn)));
        ("Resources", (Option.map x.resources ~f:ResourceArns.to_value));
        ("ListOfTags", (Option.map x.listOfTags ~f:ListOfTags.to_value));
        ("NotResources",
          (Option.map x.notResources ~f:ResourceArns.to_value));
        ("Conditions", (Option.map x.conditions ~f:Conditions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let conditions =
        (Option.map ~f:Conditions.of_xml) (Xml.child xml_arg0 "Conditions") in
      let notResources =
        (Option.map ~f:ResourceArns.of_xml)
          (Xml.child xml_arg0 "NotResources") in
      let listOfTags =
        (Option.map ~f:ListOfTags.of_xml) (Xml.child xml_arg0 "ListOfTags") in
      let resources =
        (Option.map ~f:ResourceArns.of_xml) (Xml.child xml_arg0 "Resources") in
      let iamRoleArn =
        IAMRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IamRoleArn") in
      let selectionName =
        BackupSelectionName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SelectionName") in
      make ?conditions ?notResources ?listOfTags ?resources ~iamRoleArn
        ~selectionName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let conditions = field_map json "Conditions" Conditions.of_json in
      let notResources = field_map json "NotResources" ResourceArns.of_json in
      let listOfTags = field_map json "ListOfTags" ListOfTags.of_json in
      let resources = field_map json "Resources" ResourceArns.of_json in
      let iamRoleArn = field_map_exn json "IamRoleArn" IAMRoleArn.of_json in
      let selectionName =
        field_map_exn json "SelectionName" BackupSelectionName.of_json in
      make ?conditions ?notResources ?listOfTags ?resources ~iamRoleArn
        ~selectionName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Used to specify a set of resources to a backup plan."]
module BackupPlan =
  struct
    type nonrec t =
      {
      backupPlanName: BackupPlanName.t
        [@ocaml.doc
          "The display name of a backup plan. Must contain 1 to 50 alphanumeric or '-_.' characters."];
      rules: BackupRules.t
        [@ocaml.doc
          "An array of BackupRule objects, each of which specifies a scheduled task that is used to back up a selection of resources."];
      advancedBackupSettings: AdvancedBackupSettings.t option
        [@ocaml.doc
          "Contains a list of BackupOptions for each resource type."]}
    let context_ = "BackupPlan"
    let make ?advancedBackupSettings =
      fun ~backupPlanName ->
        fun ~rules ->
          fun () -> { advancedBackupSettings; backupPlanName; rules }
    let to_value x =
      structure_to_value
        [("BackupPlanName",
           (Some (BackupPlanName.to_value x.backupPlanName)));
        ("Rules", (Some (BackupRules.to_value x.rules)));
        ("AdvancedBackupSettings",
          (Option.map x.advancedBackupSettings
             ~f:AdvancedBackupSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let advancedBackupSettings =
        (Option.map ~f:AdvancedBackupSettings.of_xml)
          (Xml.child xml_arg0 "AdvancedBackupSettings") in
      let rules =
        BackupRules.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Rules") in
      let backupPlanName =
        BackupPlanName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BackupPlanName") in
      make ?advancedBackupSettings ~rules ~backupPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let advancedBackupSettings =
        field_map json "AdvancedBackupSettings"
          AdvancedBackupSettings.of_json in
      let rules = field_map_exn json "Rules" BackupRules.of_json in
      let backupPlanName =
        field_map_exn json "BackupPlanName" BackupPlanName.of_json in
      make ?advancedBackupSettings ~rules ~backupPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains an optional backup plan display name and an array of BackupRule objects, each of which specifies a backup rule. Each rule in a backup plan is a separate scheduled task and can back up a different selection of Amazon Web Services resources."]
module DependencyFailureException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A dependent Amazon Web Services service or resource returned an error to the Backup service, and the action cannot be completed."]
module StorageClass =
  struct
    type nonrec t =
      | WARM 
      | COLD 
      | DELETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WARM -> "WARM"
      | COLD -> "COLD"
      | DELETED -> "DELETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "WARM" -> WARM
      | "COLD" -> COLD
      | "DELETED" -> DELETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StorageClass" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StorageClass" j)
    let to_json = simple_to_json to_value
  end
module UpdateReportPlanOutput =
  struct
    type nonrec t =
      {
      reportPlanName: ReportPlanName.t option
        [@ocaml.doc "The unique name of the report plan."];
      reportPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a report plan is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `ConflictException of ConflictException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportPlanName =
      fun ?reportPlanArn ->
        fun ?creationTime ->
          fun () -> { reportPlanName; reportPlanArn; creationTime }
    let error_of_json name json =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReportPlanName",
           (Option.map x.reportPlanName ~f:ReportPlanName.to_value));
        ("ReportPlanArn", (Option.map x.reportPlanArn ~f:ARN.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let reportPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ReportPlanArn") in
      let reportPlanName =
        (Option.map ~f:ReportPlanName.of_xml)
          (Xml.child xml_arg0 "ReportPlanName") in
      make ?creationTime ?reportPlanArn ?reportPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creationTime = field_map json "CreationTime" Timestamp.of_json in
      let reportPlanArn = field_map json "ReportPlanArn" ARN.of_json in
      let reportPlanName =
        field_map json "ReportPlanName" ReportPlanName.of_json in
      make ?creationTime ?reportPlanArn ?reportPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing report plan identified by its ReportPlanName with the input document in JSON format."]
module UpdateReportPlanInput =
  struct
    type nonrec t =
      {
      reportPlanName: ReportPlanName.t
        [@ocaml.doc
          "The unique name of the report plan. This name is between 1 and 256 characters, starting with a letter, and consisting of letters (a-z, A-Z), numbers (0-9), and underscores (_)."];
      reportPlanDescription: ReportPlanDescription.t option
        [@ocaml.doc
          "An optional description of the report plan with a maximum 1,024 characters."];
      reportDeliveryChannel: ReportDeliveryChannel.t option
        [@ocaml.doc
          "A structure that contains information about where to deliver your reports, specifically your Amazon S3 bucket name, S3 key prefix, and the formats of your reports."];
      reportSetting: ReportSetting.t option
        [@ocaml.doc
          "Identifies the report template for the report. Reports are built using a report template. The report templates are: RESOURCE_COMPLIANCE_REPORT | CONTROL_COMPLIANCE_REPORT | BACKUP_JOB_REPORT | COPY_JOB_REPORT | RESTORE_JOB_REPORT If the report template is RESOURCE_COMPLIANCE_REPORT or CONTROL_COMPLIANCE_REPORT, this API resource also describes the report coverage by Amazon Web Services Regions and frameworks."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to UpdateReportPlanInput. Retrying a successful request with the same idempotency token results in a success message with no action taken."]}
    let context_ = "UpdateReportPlanInput"
    let make ?reportPlanDescription =
      fun ?reportDeliveryChannel ->
        fun ?reportSetting ->
          fun ?idempotencyToken ->
            fun ~reportPlanName ->
              fun () ->
                {
                  reportPlanDescription;
                  reportDeliveryChannel;
                  reportSetting;
                  idempotencyToken;
                  reportPlanName
                }
    let to_value x =
      structure_to_value
        [("reportPlanName",
           (Some (ReportPlanName.to_value x.reportPlanName)));
        ("ReportPlanDescription",
          (Option.map x.reportPlanDescription
             ~f:ReportPlanDescription.to_value));
        ("ReportDeliveryChannel",
          (Option.map x.reportDeliveryChannel
             ~f:ReportDeliveryChannel.to_value));
        ("ReportSetting",
          (Option.map x.reportSetting ~f:ReportSetting.to_value));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let reportSetting =
        (Option.map ~f:ReportSetting.of_xml)
          (Xml.child xml_arg0 "ReportSetting") in
      let reportDeliveryChannel =
        (Option.map ~f:ReportDeliveryChannel.of_xml)
          (Xml.child xml_arg0 "ReportDeliveryChannel") in
      let reportPlanDescription =
        (Option.map ~f:ReportPlanDescription.of_xml)
          (Xml.child xml_arg0 "ReportPlanDescription") in
      let reportPlanName =
        ReportPlanName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportPlanName") in
      make ?idempotencyToken ?reportSetting ?reportDeliveryChannel
        ?reportPlanDescription ~reportPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let reportSetting =
        field_map json "ReportSetting" ReportSetting.of_json in
      let reportDeliveryChannel =
        field_map json "ReportDeliveryChannel" ReportDeliveryChannel.of_json in
      let reportPlanDescription =
        field_map json "ReportPlanDescription" ReportPlanDescription.of_json in
      let reportPlanName =
        field_map_exn json "ReportPlanName" ReportPlanName.of_json in
      make ?idempotencyToken ?reportSetting ?reportDeliveryChannel
        ?reportPlanDescription ~reportPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing report plan identified by its ReportPlanName with the input document in JSON format."]
module UpdateRegionSettingsInput =
  struct
    type nonrec t =
      {
      resourceTypeOptInPreference: ResourceTypeOptInPreference.t option
        [@ocaml.doc
          "Updates the list of services along with the opt-in preferences for the Region."];
      resourceTypeManagementPreference:
        ResourceTypeManagementPreference.t option
        [@ocaml.doc
          "Enables or disables full Backup management of backups for a resource type. To enable full Backup management for DynamoDB along with Backup's advanced DynamoDB backup features, follow the procedure to enable advanced DynamoDB backup programmatically."]}
    let make ?resourceTypeOptInPreference =
      fun ?resourceTypeManagementPreference ->
        fun () ->
          { resourceTypeOptInPreference; resourceTypeManagementPreference }
    let to_value x =
      structure_to_value
        [("ResourceTypeOptInPreference",
           (Option.map x.resourceTypeOptInPreference
              ~f:ResourceTypeOptInPreference.to_value));
        ("ResourceTypeManagementPreference",
          (Option.map x.resourceTypeManagementPreference
             ~f:ResourceTypeManagementPreference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceTypeManagementPreference =
        (Option.map ~f:ResourceTypeManagementPreference.of_xml)
          (Xml.child xml_arg0 "ResourceTypeManagementPreference") in
      let resourceTypeOptInPreference =
        (Option.map ~f:ResourceTypeOptInPreference.of_xml)
          (Xml.child xml_arg0 "ResourceTypeOptInPreference") in
      make ?resourceTypeManagementPreference ?resourceTypeOptInPreference ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceTypeManagementPreference =
        field_map json "ResourceTypeManagementPreference"
          ResourceTypeManagementPreference.of_json in
      let resourceTypeOptInPreference =
        field_map json "ResourceTypeOptInPreference"
          ResourceTypeOptInPreference.of_json in
      make ?resourceTypeManagementPreference ?resourceTypeOptInPreference ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the current service opt-in settings for the Region. If service-opt-in is enabled for a service, Backup tries to protect that service's resources in this Region, when the resource is included in an on-demand backup or scheduled backup plan. Otherwise, Backup does not try to protect that service's resources in this Region. Use the DescribeRegionSettings API to determine the resource types that are supported."]
module UpdateRecoveryPointLifecycleOutput =
  struct
    type nonrec t =
      {
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      lifecycle: Lifecycle.t option
        [@ocaml.doc
          "The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup transitions and expires backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types."];
      calculatedLifecycle: CalculatedLifecycle.t option
        [@ocaml.doc
          "A CalculatedLifecycle object containing DeleteAt and MoveToColdStorageAt timestamps."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupVaultArn =
      fun ?recoveryPointArn ->
        fun ?lifecycle ->
          fun ?calculatedLifecycle ->
            fun () ->
              {
                backupVaultArn;
                recoveryPointArn;
                lifecycle;
                calculatedLifecycle
              }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("RecoveryPointArn", (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value));
        ("CalculatedLifecycle",
          (Option.map x.calculatedLifecycle ~f:CalculatedLifecycle.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let calculatedLifecycle =
        (Option.map ~f:CalculatedLifecycle.of_xml)
          (Xml.child xml_arg0 "CalculatedLifecycle") in
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      make ?calculatedLifecycle ?lifecycle ?recoveryPointArn ?backupVaultArn
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let calculatedLifecycle =
        field_map json "CalculatedLifecycle" CalculatedLifecycle.of_json in
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      make ?calculatedLifecycle ?lifecycle ?recoveryPointArn ?backupVaultArn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sets the transition lifecycle of a recovery point. The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup transitions and expires backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types. This operation does not support continuous backups."]
module UpdateRecoveryPointLifecycleInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      recoveryPointArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      lifecycle: Lifecycle.t option
        [@ocaml.doc
          "The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup transitions and expires backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold."]}
    let context_ = "UpdateRecoveryPointLifecycleInput"
    let make ?lifecycle =
      fun ~backupVaultName ->
        fun ~recoveryPointArn ->
          fun () -> { lifecycle; backupVaultName; recoveryPointArn }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("recoveryPointArn", (Some (ARN.to_value x.recoveryPointArn)));
        ("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      let recoveryPointArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recoveryPointArn") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ?lifecycle ~recoveryPointArn ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      let recoveryPointArn =
        field_map_exn json "RecoveryPointArn" ARN.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ?lifecycle ~recoveryPointArn ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sets the transition lifecycle of a recovery point. The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup transitions and expires backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types. This operation does not support continuous backups."]
module UpdateGlobalSettingsInput =
  struct
    type nonrec t =
      {
      globalSettings: GlobalSettings.t option
        [@ocaml.doc
          "A value for isCrossAccountBackupEnabled and a Region. Example: update-global-settings --global-settings isCrossAccountBackupEnabled=false --region us-west-2."]}
    let make ?globalSettings = fun () -> { globalSettings }
    let to_value x =
      structure_to_value
        [("GlobalSettings",
           (Option.map x.globalSettings ~f:GlobalSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let globalSettings =
        (Option.map ~f:GlobalSettings.of_xml)
          (Xml.child xml_arg0 "GlobalSettings") in
      make ?globalSettings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let globalSettings =
        field_map json "GlobalSettings" GlobalSettings.of_json in
      make ?globalSettings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates whether the Amazon Web Services account is opted in to cross-account backup. Returns an error if the account is not an Organizations management account. Use the DescribeGlobalSettings API to determine the current settings."]
module UpdateFrameworkOutput =
  struct
    type nonrec t =
      {
      frameworkName: FrameworkName.t option
        [@ocaml.doc
          "The unique name of a framework. This name is between 1 and 256 characters, starting with a letter, and consisting of letters (a-z, A-Z), numbers (0-9), and underscores (_)."];
      frameworkArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a framework is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `ConflictException of ConflictException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?frameworkName =
      fun ?frameworkArn ->
        fun ?creationTime ->
          fun () -> { frameworkName; frameworkArn; creationTime }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FrameworkName",
           (Option.map x.frameworkName ~f:FrameworkName.to_value));
        ("FrameworkArn", (Option.map x.frameworkArn ~f:ARN.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let frameworkArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "FrameworkArn") in
      let frameworkName =
        (Option.map ~f:FrameworkName.of_xml)
          (Xml.child xml_arg0 "FrameworkName") in
      make ?creationTime ?frameworkArn ?frameworkName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creationTime = field_map json "CreationTime" Timestamp.of_json in
      let frameworkArn = field_map json "FrameworkArn" ARN.of_json in
      let frameworkName =
        field_map json "FrameworkName" FrameworkName.of_json in
      make ?creationTime ?frameworkArn ?frameworkName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing framework identified by its FrameworkName with the input document in JSON format."]
module UpdateFrameworkInput =
  struct
    type nonrec t =
      {
      frameworkName: FrameworkName.t
        [@ocaml.doc
          "The unique name of a framework. This name is between 1 and 256 characters, starting with a letter, and consisting of letters (a-z, A-Z), numbers (0-9), and underscores (_)."];
      frameworkDescription: FrameworkDescription.t option
        [@ocaml.doc
          "An optional description of the framework with a maximum 1,024 characters."];
      frameworkControls: FrameworkControls.t option
        [@ocaml.doc
          "A list of the controls that make up the framework. Each control in the list has a name, input parameters, and scope."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to UpdateFrameworkInput. Retrying a successful request with the same idempotency token results in a success message with no action taken."]}
    let context_ = "UpdateFrameworkInput"
    let make ?frameworkDescription =
      fun ?frameworkControls ->
        fun ?idempotencyToken ->
          fun ~frameworkName ->
            fun () ->
              {
                frameworkDescription;
                frameworkControls;
                idempotencyToken;
                frameworkName
              }
    let to_value x =
      structure_to_value
        [("frameworkName", (Some (FrameworkName.to_value x.frameworkName)));
        ("FrameworkDescription",
          (Option.map x.frameworkDescription ~f:FrameworkDescription.to_value));
        ("FrameworkControls",
          (Option.map x.frameworkControls ~f:FrameworkControls.to_value));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let frameworkControls =
        (Option.map ~f:FrameworkControls.of_xml)
          (Xml.child xml_arg0 "FrameworkControls") in
      let frameworkDescription =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "FrameworkDescription") in
      let frameworkName =
        FrameworkName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "frameworkName") in
      make ?idempotencyToken ?frameworkControls ?frameworkDescription
        ~frameworkName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let frameworkControls =
        field_map json "FrameworkControls" FrameworkControls.of_json in
      let frameworkDescription =
        field_map json "FrameworkDescription" FrameworkDescription.of_json in
      let frameworkName =
        field_map_exn json "FrameworkName" FrameworkName.of_json in
      make ?idempotencyToken ?frameworkControls ?frameworkDescription
        ~frameworkName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing framework identified by its FrameworkName with the input document in JSON format."]
module UpdateBackupPlanOutput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      backupPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup plan; for example, arn:aws:backup:us-east-1:123456789012:plan:8F81F553-3A74-4A3F-B93D-B3360DC80C50."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup plan is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      versionId: String_.t option
        [@ocaml.doc
          "Unique, randomly generated, Unicode, UTF-8 encoded strings that are at most 1,024 bytes long. Version Ids cannot be edited."];
      advancedBackupSettings: AdvancedBackupSettings.t option
        [@ocaml.doc
          "Contains a list of BackupOptions for each resource type."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupPlanId =
      fun ?backupPlanArn ->
        fun ?creationDate ->
          fun ?versionId ->
            fun ?advancedBackupSettings ->
              fun () ->
                {
                  backupPlanId;
                  backupPlanArn;
                  creationDate;
                  versionId;
                  advancedBackupSettings
                }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("BackupPlanArn", (Option.map x.backupPlanArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("VersionId", (Option.map x.versionId ~f:String_.to_value));
        ("AdvancedBackupSettings",
          (Option.map x.advancedBackupSettings
             ~f:AdvancedBackupSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let advancedBackupSettings =
        (Option.map ~f:AdvancedBackupSettings.of_xml)
          (Xml.child xml_arg0 "AdvancedBackupSettings") in
      let versionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VersionId") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let backupPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupPlanArn") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      make ?advancedBackupSettings ?versionId ?creationDate ?backupPlanArn
        ?backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let advancedBackupSettings =
        field_map json "AdvancedBackupSettings"
          AdvancedBackupSettings.of_json in
      let versionId = field_map json "VersionId" String_.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let backupPlanArn = field_map json "BackupPlanArn" ARN.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      make ?advancedBackupSettings ?versionId ?creationDate ?backupPlanArn
        ?backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing backup plan identified by its backupPlanId with the input document in JSON format. The new version is uniquely identified by a VersionId."]
module UpdateBackupPlanInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc "Uniquely identifies a backup plan."];
      backupPlan: BackupPlanInput.t
        [@ocaml.doc
          "Specifies the body of a backup plan. Includes a BackupPlanName and one or more sets of Rules."]}
    let context_ = "UpdateBackupPlanInput"
    let make ~backupPlanId =
      fun ~backupPlan -> fun () -> { backupPlanId; backupPlan }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)));
        ("BackupPlan", (Some (BackupPlanInput.to_value x.backupPlan)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlan =
        BackupPlanInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BackupPlan") in
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ~backupPlan ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlan =
        field_map_exn json "BackupPlan" BackupPlanInput.of_json in
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ~backupPlan ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing backup plan identified by its backupPlanId with the input document in JSON format. The new version is uniquely identified by a VersionId."]
module UntagResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: ARN.t
        [@ocaml.doc
          "An ARN that uniquely identifies a resource. The format of the ARN depends on the type of the tagged resource."];
      tagKeyList: TagKeyList.t
        [@ocaml.doc
          "A list of keys to identify which key-value tags to remove from a resource."]}
    let context_ = "UntagResourceInput"
    let make ~resourceArn =
      fun ~tagKeyList -> fun () -> { resourceArn; tagKeyList }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ARN.to_value x.resourceArn)));
        ("TagKeyList", (Some (TagKeyList.to_value x.tagKeyList)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeyList =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeyList") in
      let resourceArn =
        ARN.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeyList ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tagKeyList = field_map_exn json "TagKeyList" TagKeyList.of_json in
      let resourceArn = field_map_exn json "ResourceArn" ARN.of_json in
      make ~tagKeyList ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes a set of key-value pairs from a recovery point, backup plan, or backup vault identified by an Amazon Resource Name (ARN)"]
module TagResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: ARN.t
        [@ocaml.doc
          "An ARN that uniquely identifies a resource. The format of the ARN depends on the type of the tagged resource."];
      tags: Tags.t
        [@ocaml.doc
          "Key-value pairs that are used to help organize your resources. You can assign your own metadata to the resources you create. For clarity, this is the structure to assign tags: \\[\\{\"Key\":\"string\",\"Value\":\"string\"\\}\\]."]}
    let context_ = "TagResourceInput"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ARN.to_value x.resourceArn)));
        ("Tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceArn =
        ARN.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map_exn json "Tags" Tags.of_json in
      let resourceArn = field_map_exn json "ResourceArn" ARN.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Assigns a set of key-value pairs to a recovery point, backup plan, or backup vault identified by an Amazon Resource Name (ARN)."]
module StopBackupJobInput =
  struct
    type nonrec t =
      {
      backupJobId: String_.t
        [@ocaml.doc
          "Uniquely identifies a request to Backup to back up a resource."]}
    let context_ = "StopBackupJobInput"
    let make ~backupJobId = fun () -> { backupJobId }
    let to_value x =
      structure_to_value
        [("backupJobId", (Some (String_.to_value x.backupJobId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupJobId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupJobId") in
      make ~backupJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupJobId = field_map_exn json "BackupJobId" String_.of_json in
      make ~backupJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attempts to cancel a job to create a one-time backup of a resource."]
module StartRestoreJobOutput =
  struct
    type nonrec t =
      {
      restoreJobId: RestoreJobId.t option
        [@ocaml.doc
          "Uniquely identifies the job that restores a recovery point."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?restoreJobId = fun () -> { restoreJobId }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RestoreJobId",
           (Option.map x.restoreJobId ~f:RestoreJobId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let restoreJobId =
        (Option.map ~f:RestoreJobId.of_xml)
          (Xml.child xml_arg0 "RestoreJobId") in
      make ?restoreJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let restoreJobId = field_map json "RestoreJobId" RestoreJobId.of_json in
      make ?restoreJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Recovers the saved resource identified by an Amazon Resource Name (ARN)."]
module StartRestoreJobInput =
  struct
    type nonrec t =
      {
      recoveryPointArn: ARN.t
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      metadata: Metadata.t
        [@ocaml.doc
          "A set of metadata key-value pairs. Contains information, such as a resource name, required to restore a recovery point. You can get configuration metadata about a resource at the time it was backed up by calling GetRecoveryPointRestoreMetadata. However, values in addition to those provided by GetRecoveryPointRestoreMetadata might be required to restore a resource. For example, you might need to provide a new resource name if the original already exists. You need to specify specific metadata to restore an Amazon Elastic File System (Amazon EFS) instance: file-system-id: The ID of the Amazon EFS file system that is backed up by Backup. Returned in GetRecoveryPointRestoreMetadata. Encrypted: A Boolean value that, if true, specifies that the file system is encrypted. If KmsKeyId is specified, Encrypted must be set to true. KmsKeyId: Specifies the Amazon Web Services KMS key that is used to encrypt the restored file system. You can specify a key from another Amazon Web Services account provided that key it is properly shared with your account via Amazon Web Services KMS. PerformanceMode: Specifies the throughput mode of the file system. CreationToken: A user-supplied value that ensures the uniqueness (idempotency) of the request. newFileSystem: A Boolean value that, if true, specifies that the recovery point is restored to a new Amazon EFS file system. ItemsToRestore: An array of one to five strings where each string is a file path. Use ItemsToRestore to restore specific files or directories rather than the entire file system. This parameter is optional. For example, \"itemsToRestore\":\"\\[\\\"/my.test\\\"\\]\"."];
      iamRoleArn: IAMRoleArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role that Backup uses to create the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to StartRestoreJob. Retrying a successful request with the same idempotency token results in a success message with no action taken."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "Starts a job to restore a recovery point for one of the following resources: Aurora for Amazon Aurora DocumentDB for Amazon DocumentDB (with MongoDB compatibility) DynamoDB for Amazon DynamoDB EBS for Amazon Elastic Block Store EC2 for Amazon Elastic Compute Cloud EFS for Amazon Elastic File System FSx for Amazon FSx Neptune for Amazon Neptune RDS for Amazon Relational Database Service Storage Gateway for Storage Gateway S3 for Amazon S3 VirtualMachine for virtual machines"]}
    let context_ = "StartRestoreJobInput"
    let make ?idempotencyToken =
      fun ?resourceType ->
        fun ~recoveryPointArn ->
          fun ~metadata ->
            fun ~iamRoleArn ->
              fun () ->
                {
                  idempotencyToken;
                  resourceType;
                  recoveryPointArn;
                  metadata;
                  iamRoleArn
                }
    let to_value x =
      structure_to_value
        [("RecoveryPointArn", (Some (ARN.to_value x.recoveryPointArn)));
        ("Metadata", (Some (Metadata.to_value x.metadata)));
        ("IamRoleArn", (Some (IAMRoleArn.to_value x.iamRoleArn)));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let iamRoleArn =
        IAMRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IamRoleArn") in
      let metadata =
        Metadata.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Metadata") in
      let recoveryPointArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RecoveryPointArn") in
      make ?resourceType ?idempotencyToken ~iamRoleArn ~metadata
        ~recoveryPointArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let iamRoleArn = field_map_exn json "IamRoleArn" IAMRoleArn.of_json in
      let metadata = field_map_exn json "Metadata" Metadata.of_json in
      let recoveryPointArn =
        field_map_exn json "RecoveryPointArn" ARN.of_json in
      make ?resourceType ?idempotencyToken ~iamRoleArn ~metadata
        ~recoveryPointArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Recovers the saved resource identified by an Amazon Resource Name (ARN)."]
module StartReportJobOutput =
  struct
    type nonrec t =
      {
      reportJobId: ReportJobId.t option
        [@ocaml.doc
          "The identifier of the report job. A unique, randomly generated, Unicode, UTF-8 encoded string that is at most 1,024 bytes long. The report job ID cannot be edited."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportJobId = fun () -> { reportJobId }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReportJobId", (Option.map x.reportJobId ~f:ReportJobId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportJobId =
        (Option.map ~f:ReportJobId.of_xml) (Xml.child xml_arg0 "ReportJobId") in
      make ?reportJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reportJobId = field_map json "ReportJobId" ReportJobId.of_json in
      make ?reportJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an on-demand report job for the specified report plan."]
module StartReportJobInput =
  struct
    type nonrec t =
      {
      reportPlanName: ReportPlanName.t
        [@ocaml.doc "The unique name of a report plan."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to StartReportJobInput. Retrying a successful request with the same idempotency token results in a success message with no action taken."]}
    let context_ = "StartReportJobInput"
    let make ?idempotencyToken =
      fun ~reportPlanName -> fun () -> { idempotencyToken; reportPlanName }
    let to_value x =
      structure_to_value
        [("reportPlanName",
           (Some (ReportPlanName.to_value x.reportPlanName)));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let reportPlanName =
        ReportPlanName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportPlanName") in
      make ?idempotencyToken ~reportPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let reportPlanName =
        field_map_exn json "ReportPlanName" ReportPlanName.of_json in
      make ?idempotencyToken ~reportPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an on-demand report job for the specified report plan."]
module StartCopyJobOutput =
  struct
    type nonrec t =
      {
      copyJobId: String_.t option
        [@ocaml.doc "Uniquely identifies a copy job."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a copy job is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?copyJobId =
      fun ?creationDate -> fun () -> { copyJobId; creationDate }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CopyJobId", (Option.map x.copyJobId ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let copyJobId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CopyJobId") in
      make ?creationDate ?copyJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let copyJobId = field_map json "CopyJobId" String_.of_json in
      make ?creationDate ?copyJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a job to create a one-time copy of the specified resource. Does not support continuous backups."]
module StartCopyJobInput =
  struct
    type nonrec t =
      {
      recoveryPointArn: ARN.t
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point to use for the copy job; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      sourceBackupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical source container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      destinationBackupVaultArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a destination backup vault to copy to; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      iamRoleArn: IAMRoleArn.t
        [@ocaml.doc
          "Specifies the IAM role ARN used to copy the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to StartCopyJob. Retrying a successful request with the same idempotency token results in a success message with no action taken."];
      lifecycle: Lifecycle.t option }
    let context_ = "StartCopyJobInput"
    let make ?idempotencyToken =
      fun ?lifecycle ->
        fun ~recoveryPointArn ->
          fun ~sourceBackupVaultName ->
            fun ~destinationBackupVaultArn ->
              fun ~iamRoleArn ->
                fun () ->
                  {
                    idempotencyToken;
                    lifecycle;
                    recoveryPointArn;
                    sourceBackupVaultName;
                    destinationBackupVaultArn;
                    iamRoleArn
                  }
    let to_value x =
      structure_to_value
        [("RecoveryPointArn", (Some (ARN.to_value x.recoveryPointArn)));
        ("SourceBackupVaultName",
          (Some (BackupVaultName.to_value x.sourceBackupVaultName)));
        ("DestinationBackupVaultArn",
          (Some (ARN.to_value x.destinationBackupVaultArn)));
        ("IamRoleArn", (Some (IAMRoleArn.to_value x.iamRoleArn)));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value));
        ("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let iamRoleArn =
        IAMRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IamRoleArn") in
      let destinationBackupVaultArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "DestinationBackupVaultArn") in
      let sourceBackupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SourceBackupVaultName") in
      let recoveryPointArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RecoveryPointArn") in
      make ?lifecycle ?idempotencyToken ~iamRoleArn
        ~destinationBackupVaultArn ~sourceBackupVaultName ~recoveryPointArn
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let iamRoleArn = field_map_exn json "IamRoleArn" IAMRoleArn.of_json in
      let destinationBackupVaultArn =
        field_map_exn json "DestinationBackupVaultArn" ARN.of_json in
      let sourceBackupVaultName =
        field_map_exn json "SourceBackupVaultName" BackupVaultName.of_json in
      let recoveryPointArn =
        field_map_exn json "RecoveryPointArn" ARN.of_json in
      make ?lifecycle ?idempotencyToken ~iamRoleArn
        ~destinationBackupVaultArn ~sourceBackupVaultName ~recoveryPointArn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts a job to create a one-time copy of the specified resource. Does not support continuous backups."]
module StartBackupJobOutput =
  struct
    type nonrec t =
      {
      backupJobId: String_.t option
        [@ocaml.doc
          "Uniquely identifies a request to Backup to back up a resource."];
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a backup job is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupJobId =
      fun ?recoveryPointArn ->
        fun ?creationDate ->
          fun () -> { backupJobId; recoveryPointArn; creationDate }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupJobId", (Option.map x.backupJobId ~f:String_.to_value));
        ("RecoveryPointArn", (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      let backupJobId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupJobId") in
      make ?creationDate ?recoveryPointArn ?backupJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      let backupJobId = field_map json "BackupJobId" String_.of_json in
      make ?creationDate ?recoveryPointArn ?backupJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an on-demand backup job for the specified resource."]
module StartBackupJobInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      resourceArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      iamRoleArn: IAMRoleArn.t
        [@ocaml.doc
          "Specifies the IAM role ARN used to create the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to StartBackupJob. Retrying a successful request with the same idempotency token results in a success message with no action taken."];
      startWindowMinutes: WindowMinutes.t option
        [@ocaml.doc
          "A value in minutes after a backup is scheduled before a job will be canceled if it doesn't start successfully. This value is optional, and the default is 8 hours."];
      completeWindowMinutes: WindowMinutes.t option
        [@ocaml.doc
          "A value in minutes during which a successfully started backup must complete, or else Backup will cancel the job. This value is optional. This value begins counting down from when the backup was scheduled. It does not add additional time for StartWindowMinutes, or if the backup started later than scheduled."];
      lifecycle: Lifecycle.t option
        [@ocaml.doc
          "The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup will transition and expire backups automatically according to the lifecycle that you define. Backups transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types."];
      recoveryPointTags: Tags.t option
        [@ocaml.doc
          "To help organize your resources, you can assign your own metadata to the resources that you create. Each tag is a key-value pair."];
      backupOptions: BackupOptions.t option
        [@ocaml.doc
          "Specifies the backup option for a selected resource. This option is only available for Windows Volume Shadow Copy Service (VSS) backup jobs. Valid values: Set to \"WindowsVSS\":\"enabled\" to enable the WindowsVSS backup option and create a Windows VSS backup. Set to \"WindowsVSS\"\"disabled\" to create a regular backup. The WindowsVSS option is not enabled by default."]}
    let context_ = "StartBackupJobInput"
    let make ?idempotencyToken =
      fun ?startWindowMinutes ->
        fun ?completeWindowMinutes ->
          fun ?lifecycle ->
            fun ?recoveryPointTags ->
              fun ?backupOptions ->
                fun ~backupVaultName ->
                  fun ~resourceArn ->
                    fun ~iamRoleArn ->
                      fun () ->
                        {
                          idempotencyToken;
                          startWindowMinutes;
                          completeWindowMinutes;
                          lifecycle;
                          recoveryPointTags;
                          backupOptions;
                          backupVaultName;
                          resourceArn;
                          iamRoleArn
                        }
    let to_value x =
      structure_to_value
        [("BackupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("ResourceArn", (Some (ARN.to_value x.resourceArn)));
        ("IamRoleArn", (Some (IAMRoleArn.to_value x.iamRoleArn)));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value));
        ("StartWindowMinutes",
          (Option.map x.startWindowMinutes ~f:WindowMinutes.to_value));
        ("CompleteWindowMinutes",
          (Option.map x.completeWindowMinutes ~f:WindowMinutes.to_value));
        ("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value));
        ("RecoveryPointTags",
          (Option.map x.recoveryPointTags ~f:Tags.to_value));
        ("BackupOptions",
          (Option.map x.backupOptions ~f:BackupOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupOptions =
        (Option.map ~f:BackupOptions.of_xml)
          (Xml.child xml_arg0 "BackupOptions") in
      let recoveryPointTags =
        (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "RecoveryPointTags") in
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      let completeWindowMinutes =
        (Option.map ~f:WindowMinutes.of_xml)
          (Xml.child xml_arg0 "CompleteWindowMinutes") in
      let startWindowMinutes =
        (Option.map ~f:WindowMinutes.of_xml)
          (Xml.child xml_arg0 "StartWindowMinutes") in
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let iamRoleArn =
        IAMRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IamRoleArn") in
      let resourceArn =
        ARN.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BackupVaultName") in
      make ?backupOptions ?recoveryPointTags ?lifecycle
        ?completeWindowMinutes ?startWindowMinutes ?idempotencyToken
        ~iamRoleArn ~resourceArn ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupOptions =
        field_map json "BackupOptions" BackupOptions.of_json in
      let recoveryPointTags = field_map json "RecoveryPointTags" Tags.of_json in
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      let completeWindowMinutes =
        field_map json "CompleteWindowMinutes" WindowMinutes.of_json in
      let startWindowMinutes =
        field_map json "StartWindowMinutes" WindowMinutes.of_json in
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let iamRoleArn = field_map_exn json "IamRoleArn" IAMRoleArn.of_json in
      let resourceArn = field_map_exn json "ResourceArn" ARN.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ?backupOptions ?recoveryPointTags ?lifecycle
        ?completeWindowMinutes ?startWindowMinutes ?idempotencyToken
        ~iamRoleArn ~resourceArn ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Starts an on-demand backup job for the specified resource."]
module PutBackupVaultNotificationsInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      sNSTopicArn: ARN.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) that specifies the topic for a backup vault\226\128\153s events; for example, arn:aws:sns:us-west-2:111122223333:MyVaultTopic."];
      backupVaultEvents: BackupVaultEvents.t
        [@ocaml.doc
          "An array of events that indicate the status of jobs to back up resources to the backup vault. For common use cases and code samples, see Using Amazon SNS to track Backup events. The following events are supported: BACKUP_JOB_STARTED | BACKUP_JOB_COMPLETED COPY_JOB_STARTED | COPY_JOB_SUCCESSFUL | COPY_JOB_FAILED RESTORE_JOB_STARTED | RESTORE_JOB_COMPLETED | RECOVERY_POINT_MODIFIED S3_BACKUP_OBJECT_FAILED | S3_RESTORE_OBJECT_FAILED Ignore the list below because it includes deprecated events. Refer to the list above."]}
    let context_ = "PutBackupVaultNotificationsInput"
    let make ~backupVaultName =
      fun ~sNSTopicArn ->
        fun ~backupVaultEvents ->
          fun () -> { backupVaultName; sNSTopicArn; backupVaultEvents }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("SNSTopicArn", (Some (ARN.to_value x.sNSTopicArn)));
        ("BackupVaultEvents",
          (Some (BackupVaultEvents.to_value x.backupVaultEvents)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultEvents =
        BackupVaultEvents.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BackupVaultEvents") in
      let sNSTopicArn =
        ARN.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SNSTopicArn") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~backupVaultEvents ~sNSTopicArn ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultEvents =
        field_map_exn json "BackupVaultEvents" BackupVaultEvents.of_json in
      let sNSTopicArn = field_map_exn json "SNSTopicArn" ARN.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~backupVaultEvents ~sNSTopicArn ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Turns on notifications on a backup vault for the specified topic and events."]
module PutBackupVaultLockConfigurationInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The Backup Vault Lock configuration that specifies the name of the backup vault it protects."];
      minRetentionDays: Long.t option
        [@ocaml.doc
          "The Backup Vault Lock configuration that specifies the minimum retention period that the vault retains its recovery points. This setting can be useful if, for example, your organization's policies require you to retain certain data for at least seven years (2555 days). If this parameter is not specified, Vault Lock will not enforce a minimum retention period. If this parameter is specified, any backup or copy job to the vault must have a lifecycle policy with a retention period equal to or longer than the minimum retention period. If the job's retention period is shorter than that minimum retention period, then the vault fails that backup or copy job, and you should either modify your lifecycle settings or use a different vault. The shortest minimum retention period you can specify is 1 day. Recovery points already saved in the vault prior to Vault Lock are not affected."];
      maxRetentionDays: Long.t option
        [@ocaml.doc
          "The Backup Vault Lock configuration that specifies the maximum retention period that the vault retains its recovery points. This setting can be useful if, for example, your organization's policies require you to destroy certain data after retaining it for four years (1460 days). If this parameter is not included, Vault Lock does not enforce a maximum retention period on the recovery points in the vault. If this parameter is included without a value, Vault Lock will not enforce a maximum retention period. If this parameter is specified, any backup or copy job to the vault must have a lifecycle policy with a retention period equal to or shorter than the maximum retention period. If the job's retention period is longer than that maximum retention period, then the vault fails the backup or copy job, and you should either modify your lifecycle settings or use a different vault. The longest maximum retention period you can specify is 36500 days (approximately 100 years). Recovery points already saved in the vault prior to Vault Lock are not affected."];
      changeableForDays: Long.t option
        [@ocaml.doc
          "The Backup Vault Lock configuration that specifies the number of days before the lock date. For example, setting ChangeableForDays to 30 on Jan. 1, 2022 at 8pm UTC will set the lock date to Jan. 31, 2022 at 8pm UTC. Backup enforces a 72-hour cooling-off period before Vault Lock takes effect and becomes immutable. Therefore, you must set ChangeableForDays to 3 or greater. Before the lock date, you can delete Vault Lock from the vault using DeleteBackupVaultLockConfiguration or change the Vault Lock configuration using PutBackupVaultLockConfiguration. On and after the lock date, the Vault Lock becomes immutable and cannot be changed or deleted. If this parameter is not specified, you can delete Vault Lock from the vault using DeleteBackupVaultLockConfiguration or change the Vault Lock configuration using PutBackupVaultLockConfiguration at any time."]}
    let context_ = "PutBackupVaultLockConfigurationInput"
    let make ?minRetentionDays =
      fun ?maxRetentionDays ->
        fun ?changeableForDays ->
          fun ~backupVaultName ->
            fun () ->
              {
                minRetentionDays;
                maxRetentionDays;
                changeableForDays;
                backupVaultName
              }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("MinRetentionDays",
          (Option.map x.minRetentionDays ~f:Long.to_value));
        ("MaxRetentionDays",
          (Option.map x.maxRetentionDays ~f:Long.to_value));
        ("ChangeableForDays",
          (Option.map x.changeableForDays ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeableForDays =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "ChangeableForDays") in
      let maxRetentionDays =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MaxRetentionDays") in
      let minRetentionDays =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MinRetentionDays") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ?changeableForDays ?maxRetentionDays ?minRetentionDays
        ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeableForDays = field_map json "ChangeableForDays" Long.of_json in
      let maxRetentionDays = field_map json "MaxRetentionDays" Long.of_json in
      let minRetentionDays = field_map json "MinRetentionDays" Long.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ?changeableForDays ?maxRetentionDays ?minRetentionDays
        ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Applies Backup Vault Lock to a backup vault, preventing attempts to delete any recovery point stored in or created in a backup vault. Vault Lock also prevents attempts to update the lifecycle policy that controls the retention period of any recovery point currently stored in a backup vault. If specified, Vault Lock enforces a minimum and maximum retention period for future backup and copy jobs that target a backup vault. Backup Vault Lock has yet to receive a third-party assessment for SEC 17a-4(f) and CFTC."]
module PutBackupVaultAccessPolicyInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      policy: IAMPolicy.t option
        [@ocaml.doc
          "The backup vault access policy document in JSON format."]}
    let context_ = "PutBackupVaultAccessPolicyInput"
    let make ?policy =
      fun ~backupVaultName -> fun () -> { policy; backupVaultName }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("Policy", (Option.map x.policy ~f:IAMPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:IAMPolicy.of_xml) (Xml.child xml_arg0 "Policy") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ?policy ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let policy = field_map json "Policy" IAMPolicy.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ?policy ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sets a resource-based policy that is used to manage access permissions on the target backup vault. Requires a backup vault name and an access policy document in JSON format."]
module ListTagsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      tags: Tags.t option
        [@ocaml.doc
          "To help organize your resources, you can assign your own metadata to the resources you create. Each tag is a key-value pair."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?tags -> fun () -> { nextToken; tags }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?tags ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "Tags" Tags.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?tags ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of key-value pairs assigned to a target recovery point, backup plan, or backup vault. ListTags only works for resource types that support full Backup management of their backups. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table."]
module ListTagsInput =
  struct
    type nonrec t =
      {
      resourceArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the type of resource. Valid targets for ListTags are recovery points, backup plans, and backup vaults."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."]}
    let context_ = "ListTagsInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~resourceArn -> fun () -> { nextToken; maxResults; resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ARN.to_value x.resourceArn)));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let resourceArn =
        ARN.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ?maxResults ?nextToken ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let resourceArn = field_map_exn json "ResourceArn" ARN.of_json in
      make ?maxResults ?nextToken ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of key-value pairs assigned to a target recovery point, backup plan, or backup vault. ListTags only works for resource types that support full Backup management of their backups. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table."]
module ListRestoreJobsOutput =
  struct
    type nonrec t =
      {
      restoreJobs: RestoreJobsList.t option
        [@ocaml.doc
          "An array of objects that contain detailed information about jobs to restore saved resources."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?restoreJobs =
      fun ?nextToken -> fun () -> { restoreJobs; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RestoreJobs",
           (Option.map x.restoreJobs ~f:RestoreJobsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let restoreJobs =
        (Option.map ~f:RestoreJobsList.of_xml)
          (Xml.child xml_arg0 "RestoreJobs") in
      make ?nextToken ?restoreJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let restoreJobs = field_map json "RestoreJobs" RestoreJobsList.of_json in
      make ?nextToken ?restoreJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of jobs that Backup initiated to restore a saved resource, including details about the recovery process."]
module ListRestoreJobsInput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."];
      byAccountId: AccountId.t option
        [@ocaml.doc
          "The account ID to list the jobs from. Returns only restore jobs associated with the specified account ID."];
      byCreatedBefore: Timestamp.t option
        [@ocaml.doc
          "Returns only restore jobs that were created before the specified date."];
      byCreatedAfter: Timestamp.t option
        [@ocaml.doc
          "Returns only restore jobs that were created after the specified date."];
      byStatus: RestoreJobStatus.t option
        [@ocaml.doc
          "Returns only restore jobs associated with the specified job status."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?byAccountId ->
          fun ?byCreatedBefore ->
            fun ?byCreatedAfter ->
              fun ?byStatus ->
                fun () ->
                  {
                    nextToken;
                    maxResults;
                    byAccountId;
                    byCreatedBefore;
                    byCreatedAfter;
                    byStatus
                  }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("accountId", (Option.map x.byAccountId ~f:AccountId.to_value));
        ("createdBefore",
          (Option.map x.byCreatedBefore ~f:Timestamp.to_value));
        ("createdAfter", (Option.map x.byCreatedAfter ~f:Timestamp.to_value));
        ("status", (Option.map x.byStatus ~f:RestoreJobStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byStatus =
        (Option.map ~f:RestoreJobStatus.of_xml) (Xml.child xml_arg0 "status") in
      let byCreatedAfter =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAfter") in
      let byCreatedBefore =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdBefore") in
      let byAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?byStatus ?byCreatedAfter ?byCreatedBefore ?byAccountId
        ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let byStatus = field_map json "ByStatus" RestoreJobStatus.of_json in
      let byCreatedAfter = field_map json "ByCreatedAfter" Timestamp.of_json in
      let byCreatedBefore =
        field_map json "ByCreatedBefore" Timestamp.of_json in
      let byAccountId = field_map json "ByAccountId" AccountId.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?byStatus ?byCreatedAfter ?byCreatedBefore ?byAccountId
        ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of jobs that Backup initiated to restore a saved resource, including details about the recovery process."]
module ListReportPlansOutput =
  struct
    type nonrec t =
      {
      reportPlans: ReportPlanList.t option
        [@ocaml.doc
          "A list of your report plans with detailed information for each plan. This information includes the Amazon Resource Name (ARN), report plan name, description, settings, delivery channel, deployment status, creation time, and last times the report plan attempted to and successfully ran."];
      nextToken: String_.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportPlans =
      fun ?nextToken -> fun () -> { reportPlans; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReportPlans",
           (Option.map x.reportPlans ~f:ReportPlanList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let reportPlans =
        (Option.map ~f:ReportPlanList.of_xml)
          (Xml.child xml_arg0 "ReportPlans") in
      make ?nextToken ?reportPlans ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let reportPlans = field_map json "ReportPlans" ReportPlanList.of_json in
      make ?nextToken ?reportPlans ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of your report plans. For detailed information about a single report plan, use DescribeReportPlan."]
module ListReportPlansInput =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The number of desired results from 1 to 1000. Optional. If unspecified, the query will return 1 MB of data."];
      nextToken: String_.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of your report plans. For detailed information about a single report plan, use DescribeReportPlan."]
module ListReportJobsOutput =
  struct
    type nonrec t =
      {
      reportJobs: ReportJobList.t option
        [@ocaml.doc "Details about your report jobs in JSON format."];
      nextToken: String_.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportJobs =
      fun ?nextToken -> fun () -> { reportJobs; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReportJobs", (Option.map x.reportJobs ~f:ReportJobList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let reportJobs =
        (Option.map ~f:ReportJobList.of_xml)
          (Xml.child xml_arg0 "ReportJobs") in
      make ?nextToken ?reportJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let reportJobs = field_map json "ReportJobs" ReportJobList.of_json in
      make ?nextToken ?reportJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns details about your report jobs."]
module ListReportJobsInput =
  struct
    type nonrec t =
      {
      byReportPlanName: ReportPlanName.t option
        [@ocaml.doc
          "Returns only report jobs with the specified report plan name."];
      byCreationBefore: Timestamp.t option
        [@ocaml.doc
          "Returns only report jobs that were created before the date and time specified in Unix format and Coordinated Universal Time (UTC). For example, the value 1516925490 represents Friday, January 26, 2018 12:11:30 AM."];
      byCreationAfter: Timestamp.t option
        [@ocaml.doc
          "Returns only report jobs that were created after the date and time specified in Unix format and Coordinated Universal Time (UTC). For example, the value 1516925490 represents Friday, January 26, 2018 12:11:30 AM."];
      byStatus: String_.t option
        [@ocaml.doc
          "Returns only report jobs that are in the specified status. The statuses are: CREATED | RUNNING | COMPLETED | FAILED"];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The number of desired results from 1 to 1000. Optional. If unspecified, the query will return 1 MB of data."];
      nextToken: String_.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?byReportPlanName =
      fun ?byCreationBefore ->
        fun ?byCreationAfter ->
          fun ?byStatus ->
            fun ?maxResults ->
              fun ?nextToken ->
                fun () ->
                  {
                    byReportPlanName;
                    byCreationBefore;
                    byCreationAfter;
                    byStatus;
                    maxResults;
                    nextToken
                  }
    let to_value x =
      structure_to_value
        [("ReportPlanName",
           (Option.map x.byReportPlanName ~f:ReportPlanName.to_value));
        ("CreationBefore",
          (Option.map x.byCreationBefore ~f:Timestamp.to_value));
        ("CreationAfter",
          (Option.map x.byCreationAfter ~f:Timestamp.to_value));
        ("Status", (Option.map x.byStatus ~f:String_.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let byStatus =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Status") in
      let byCreationAfter =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationAfter") in
      let byCreationBefore =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CreationBefore") in
      let byReportPlanName =
        (Option.map ~f:ReportPlanName.of_xml)
          (Xml.child xml_arg0 "ReportPlanName") in
      make ?nextToken ?maxResults ?byStatus ?byCreationAfter
        ?byCreationBefore ?byReportPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let byStatus = field_map json "ByStatus" String_.of_json in
      let byCreationAfter =
        field_map json "ByCreationAfter" Timestamp.of_json in
      let byCreationBefore =
        field_map json "ByCreationBefore" Timestamp.of_json in
      let byReportPlanName =
        field_map json "ByReportPlanName" ReportPlanName.of_json in
      make ?nextToken ?maxResults ?byStatus ?byCreationAfter
        ?byCreationBefore ?byReportPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns details about your report jobs."]
module ListRecoveryPointsByResourceOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      recoveryPoints: RecoveryPointByResourceList.t option
        [@ocaml.doc
          "An array of objects that contain detailed information about recovery points of the specified resource type. Only Amazon EFS and Amazon EC2 recovery points return BackupVaultName."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?recoveryPoints -> fun () -> { nextToken; recoveryPoints }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("RecoveryPoints",
          (Option.map x.recoveryPoints
             ~f:RecoveryPointByResourceList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recoveryPoints =
        (Option.map ~f:RecoveryPointByResourceList.of_xml)
          (Xml.child xml_arg0 "RecoveryPoints") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?recoveryPoints ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recoveryPoints =
        field_map json "RecoveryPoints" RecoveryPointByResourceList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?recoveryPoints ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about all the recovery points of the type specified by a resource Amazon Resource Name (ARN). For Amazon EFS and Amazon EC2, this action only lists recovery points created by Backup."]
module ListRecoveryPointsByResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: ARN.t
        [@ocaml.doc
          "An ARN that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of items to be returned. Amazon RDS requires a value of at least 20."]}
    let context_ = "ListRecoveryPointsByResourceInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~resourceArn -> fun () -> { nextToken; maxResults; resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ARN.to_value x.resourceArn)));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let resourceArn =
        ARN.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ?maxResults ?nextToken ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let resourceArn = field_map_exn json "ResourceArn" ARN.of_json in
      make ?maxResults ?nextToken ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about all the recovery points of the type specified by a resource Amazon Resource Name (ARN). For Amazon EFS and Amazon EC2, this action only lists recovery points created by Backup."]
module ListRecoveryPointsByBackupVaultOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      recoveryPoints: RecoveryPointByBackupVaultList.t option
        [@ocaml.doc
          "An array of objects that contain detailed information about recovery points saved in a backup vault."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?recoveryPoints -> fun () -> { nextToken; recoveryPoints }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("RecoveryPoints",
          (Option.map x.recoveryPoints
             ~f:RecoveryPointByBackupVaultList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recoveryPoints =
        (Option.map ~f:RecoveryPointByBackupVaultList.of_xml)
          (Xml.child xml_arg0 "RecoveryPoints") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?recoveryPoints ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recoveryPoints =
        field_map json "RecoveryPoints"
          RecoveryPointByBackupVaultList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?recoveryPoints ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about the recovery points stored in a backup vault."]
module ListRecoveryPointsByBackupVaultInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens. Backup vault name might not be available when a supported service creates the backup."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."];
      byResourceArn: ARN.t option
        [@ocaml.doc
          "Returns only recovery points that match the specified resource Amazon Resource Name (ARN)."];
      byResourceType: ResourceType.t option
        [@ocaml.doc
          "Returns only recovery points that match the specified resource type."];
      byBackupPlanId: String_.t option
        [@ocaml.doc
          "Returns only recovery points that match the specified backup plan ID."];
      byCreatedBefore: Timestamp.t option
        [@ocaml.doc
          "Returns only recovery points that were created before the specified timestamp."];
      byCreatedAfter: Timestamp.t option
        [@ocaml.doc
          "Returns only recovery points that were created after the specified timestamp."]}
    let context_ = "ListRecoveryPointsByBackupVaultInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?byResourceArn ->
          fun ?byResourceType ->
            fun ?byBackupPlanId ->
              fun ?byCreatedBefore ->
                fun ?byCreatedAfter ->
                  fun ~backupVaultName ->
                    fun () ->
                      {
                        nextToken;
                        maxResults;
                        byResourceArn;
                        byResourceType;
                        byBackupPlanId;
                        byCreatedBefore;
                        byCreatedAfter;
                        backupVaultName
                      }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("resourceArn", (Option.map x.byResourceArn ~f:ARN.to_value));
        ("resourceType",
          (Option.map x.byResourceType ~f:ResourceType.to_value));
        ("backupPlanId", (Option.map x.byBackupPlanId ~f:String_.to_value));
        ("createdBefore",
          (Option.map x.byCreatedBefore ~f:Timestamp.to_value));
        ("createdAfter", (Option.map x.byCreatedAfter ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byCreatedAfter =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAfter") in
      let byCreatedBefore =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdBefore") in
      let byBackupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "backupPlanId") in
      let byResourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let byResourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "resourceArn") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ?byCreatedAfter ?byCreatedBefore ?byBackupPlanId ?byResourceType
        ?byResourceArn ?maxResults ?nextToken ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let byCreatedAfter = field_map json "ByCreatedAfter" Timestamp.of_json in
      let byCreatedBefore =
        field_map json "ByCreatedBefore" Timestamp.of_json in
      let byBackupPlanId = field_map json "ByBackupPlanId" String_.of_json in
      let byResourceType =
        field_map json "ByResourceType" ResourceType.of_json in
      let byResourceArn = field_map json "ByResourceArn" ARN.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ?byCreatedAfter ?byCreatedBefore ?byBackupPlanId ?byResourceType
        ?byResourceArn ?maxResults ?nextToken ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns detailed information about the recovery points stored in a backup vault."]
module ListProtectedResourcesOutput =
  struct
    type nonrec t =
      {
      results: ProtectedResourcesList.t option
        [@ocaml.doc
          "An array of resources successfully backed up by Backup including the time the resource was saved, an Amazon Resource Name (ARN) of the resource, and a resource type."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?results = fun ?nextToken -> fun () -> { results; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Results",
           (Option.map x.results ~f:ProtectedResourcesList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let results =
        (Option.map ~f:ProtectedResourcesList.of_xml)
          (Xml.child xml_arg0 "Results") in
      make ?nextToken ?results ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let results = field_map json "Results" ProtectedResourcesList.of_json in
      make ?nextToken ?results ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of resources successfully backed up by Backup, including the time the resource was saved, an Amazon Resource Name (ARN) of the resource, and a resource type."]
module ListProtectedResourcesInput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array of resources successfully backed up by Backup, including the time the resource was saved, an Amazon Resource Name (ARN) of the resource, and a resource type."]
module ListFrameworksOutput =
  struct
    type nonrec t =
      {
      frameworks: FrameworkList.t option
        [@ocaml.doc
          "A list of frameworks with details for each framework, including the framework name, Amazon Resource Name (ARN), description, number of controls, creation time, and deployment status."];
      nextToken: String_.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?frameworks =
      fun ?nextToken -> fun () -> { frameworks; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Frameworks", (Option.map x.frameworks ~f:FrameworkList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let frameworks =
        (Option.map ~f:FrameworkList.of_xml)
          (Xml.child xml_arg0 "Frameworks") in
      make ?nextToken ?frameworks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let frameworks = field_map json "Frameworks" FrameworkList.of_json in
      make ?nextToken ?frameworks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all frameworks for an Amazon Web Services account and Amazon Web Services Region."]
module ListFrameworksInput =
  struct
    type nonrec t =
      {
      maxResults: MaxFrameworkInputs.t option
        [@ocaml.doc
          "The number of desired results from 1 to 1000. Optional. If unspecified, the query will return 1 MB of data."];
      nextToken: String_.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("MaxResults",
           (Option.map x.maxResults ~f:MaxFrameworkInputs.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxFrameworkInputs.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let maxResults = field_map json "MaxResults" MaxFrameworkInputs.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all frameworks for an Amazon Web Services account and Amazon Web Services Region."]
module ListCopyJobsOutput =
  struct
    type nonrec t =
      {
      copyJobs: CopyJobsList.t option
        [@ocaml.doc
          "An array of structures containing metadata about your copy jobs returned in JSON format."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?copyJobs = fun ?nextToken -> fun () -> { copyJobs; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CopyJobs", (Option.map x.copyJobs ~f:CopyJobsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let copyJobs =
        (Option.map ~f:CopyJobsList.of_xml) (Xml.child xml_arg0 "CopyJobs") in
      make ?nextToken ?copyJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let copyJobs = field_map json "CopyJobs" CopyJobsList.of_json in
      make ?nextToken ?copyJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns metadata about your copy jobs."]
module ListCopyJobsInput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."];
      byResourceArn: ARN.t option
        [@ocaml.doc
          "Returns only copy jobs that match the specified resource Amazon Resource Name (ARN)."];
      byState: CopyJobState.t option
        [@ocaml.doc
          "Returns only copy jobs that are in the specified state."];
      byCreatedBefore: Timestamp.t option
        [@ocaml.doc
          "Returns only copy jobs that were created before the specified date."];
      byCreatedAfter: Timestamp.t option
        [@ocaml.doc
          "Returns only copy jobs that were created after the specified date."];
      byResourceType: ResourceType.t option
        [@ocaml.doc
          "Returns only backup jobs for the specified resources: Aurora for Amazon Aurora DocumentDB for Amazon DocumentDB (with MongoDB compatibility) DynamoDB for Amazon DynamoDB EBS for Amazon Elastic Block Store EC2 for Amazon Elastic Compute Cloud EFS for Amazon Elastic File System FSx for Amazon FSx Neptune for Amazon Neptune RDS for Amazon Relational Database Service Storage Gateway for Storage Gateway S3 for Amazon S3 VirtualMachine for virtual machines"];
      byDestinationVaultArn: String_.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a source backup vault to copy from; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      byAccountId: AccountId.t option
        [@ocaml.doc
          "The account ID to list the jobs from. Returns only copy jobs associated with the specified account ID."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?byResourceArn ->
          fun ?byState ->
            fun ?byCreatedBefore ->
              fun ?byCreatedAfter ->
                fun ?byResourceType ->
                  fun ?byDestinationVaultArn ->
                    fun ?byAccountId ->
                      fun () ->
                        {
                          nextToken;
                          maxResults;
                          byResourceArn;
                          byState;
                          byCreatedBefore;
                          byCreatedAfter;
                          byResourceType;
                          byDestinationVaultArn;
                          byAccountId
                        }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("resourceArn", (Option.map x.byResourceArn ~f:ARN.to_value));
        ("state", (Option.map x.byState ~f:CopyJobState.to_value));
        ("createdBefore",
          (Option.map x.byCreatedBefore ~f:Timestamp.to_value));
        ("createdAfter", (Option.map x.byCreatedAfter ~f:Timestamp.to_value));
        ("resourceType",
          (Option.map x.byResourceType ~f:ResourceType.to_value));
        ("destinationVaultArn",
          (Option.map x.byDestinationVaultArn ~f:String_.to_value));
        ("accountId", (Option.map x.byAccountId ~f:AccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      let byDestinationVaultArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "destinationVaultArn") in
      let byResourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let byCreatedAfter =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAfter") in
      let byCreatedBefore =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdBefore") in
      let byState =
        (Option.map ~f:CopyJobState.of_xml) (Xml.child xml_arg0 "state") in
      let byResourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "resourceArn") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?byAccountId ?byDestinationVaultArn ?byResourceType
        ?byCreatedAfter ?byCreatedBefore ?byState ?byResourceArn ?maxResults
        ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let byAccountId = field_map json "ByAccountId" AccountId.of_json in
      let byDestinationVaultArn =
        field_map json "ByDestinationVaultArn" String_.of_json in
      let byResourceType =
        field_map json "ByResourceType" ResourceType.of_json in
      let byCreatedAfter = field_map json "ByCreatedAfter" Timestamp.of_json in
      let byCreatedBefore =
        field_map json "ByCreatedBefore" Timestamp.of_json in
      let byState = field_map json "ByState" CopyJobState.of_json in
      let byResourceArn = field_map json "ByResourceArn" ARN.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?byAccountId ?byDestinationVaultArn ?byResourceType
        ?byCreatedAfter ?byCreatedBefore ?byState ?byResourceArn ?maxResults
        ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns metadata about your copy jobs."]
module ListBackupVaultsOutput =
  struct
    type nonrec t =
      {
      backupVaultList: BackupVaultList.t option
        [@ocaml.doc
          "An array of backup vault list members containing vault metadata, including Amazon Resource Name (ARN), display name, creation date, number of saved recovery points, and encryption information if the resources saved in the backup vault are encrypted."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupVaultList =
      fun ?nextToken -> fun () -> { backupVaultList; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupVaultList",
           (Option.map x.backupVaultList ~f:BackupVaultList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let backupVaultList =
        (Option.map ~f:BackupVaultList.of_xml)
          (Xml.child xml_arg0 "BackupVaultList") in
      make ?nextToken ?backupVaultList ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let backupVaultList =
        field_map json "BackupVaultList" BackupVaultList.of_json in
      make ?nextToken ?backupVaultList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of recovery point storage containers along with information about them."]
module ListBackupVaultsInput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of recovery point storage containers along with information about them."]
module ListBackupSelectionsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      backupSelectionsList: BackupSelectionsList.t option
        [@ocaml.doc
          "An array of backup selection list items containing metadata about each resource in the list."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?backupSelectionsList ->
        fun () -> { nextToken; backupSelectionsList }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("BackupSelectionsList",
          (Option.map x.backupSelectionsList ~f:BackupSelectionsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupSelectionsList =
        (Option.map ~f:BackupSelectionsList.of_xml)
          (Xml.child xml_arg0 "BackupSelectionsList") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?backupSelectionsList ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupSelectionsList =
        field_map json "BackupSelectionsList" BackupSelectionsList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?backupSelectionsList ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array containing metadata of the resources associated with the target backup plan."]
module ListBackupSelectionsInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc "Uniquely identifies a backup plan."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."]}
    let context_ = "ListBackupSelectionsInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~backupPlanId ->
          fun () -> { nextToken; maxResults; backupPlanId }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ?maxResults ?nextToken ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ?maxResults ?nextToken ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns an array containing metadata of the resources associated with the target backup plan."]
module ListBackupPlansOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      backupPlansList: BackupPlansList.t option
        [@ocaml.doc
          "An array of backup plan list items containing metadata about your saved backup plans."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?backupPlansList -> fun () -> { nextToken; backupPlansList }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("BackupPlansList",
          (Option.map x.backupPlansList ~f:BackupPlansList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlansList =
        (Option.map ~f:BackupPlansList.of_xml)
          (Xml.child xml_arg0 "BackupPlansList") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?backupPlansList ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlansList =
        field_map json "BackupPlansList" BackupPlansList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?backupPlansList ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all active backup plans for an authenticated account. The list contains information such as Amazon Resource Names (ARNs), plan IDs, creation and deletion dates, version IDs, plan names, and creator request IDs."]
module ListBackupPlansInput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."];
      includeDeleted: Boolean.t option
        [@ocaml.doc
          "A Boolean value with a default value of FALSE that returns deleted backup plans when set to TRUE."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?includeDeleted ->
          fun () -> { nextToken; maxResults; includeDeleted }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("includeDeleted", (Option.map x.includeDeleted ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let includeDeleted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "includeDeleted") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?includeDeleted ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let includeDeleted = field_map json "IncludeDeleted" Boolean.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?includeDeleted ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all active backup plans for an authenticated account. The list contains information such as Amazon Resource Names (ARNs), plan IDs, creation and deletion dates, version IDs, plan names, and creator request IDs."]
module ListBackupPlanVersionsOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      backupPlanVersionsList: BackupPlanVersionsList.t option
        [@ocaml.doc
          "An array of version list items containing metadata about your backup plans."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?backupPlanVersionsList ->
        fun () -> { nextToken; backupPlanVersionsList }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("BackupPlanVersionsList",
          (Option.map x.backupPlanVersionsList
             ~f:BackupPlanVersionsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanVersionsList =
        (Option.map ~f:BackupPlanVersionsList.of_xml)
          (Xml.child xml_arg0 "BackupPlanVersionsList") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?backupPlanVersionsList ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanVersionsList =
        field_map json "BackupPlanVersionsList"
          BackupPlanVersionsList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?backupPlanVersionsList ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns version metadata of your backup plans, including Amazon Resource Names (ARNs), backup plan IDs, creation and deletion dates, plan names, and version IDs."]
module ListBackupPlanVersionsInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc "Uniquely identifies a backup plan."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."]}
    let context_ = "ListBackupPlanVersionsInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~backupPlanId ->
          fun () -> { nextToken; maxResults; backupPlanId }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ?maxResults ?nextToken ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ?maxResults ?nextToken ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns version metadata of your backup plans, including Amazon Resource Names (ARNs), backup plan IDs, creation and deletion dates, plan names, and version IDs."]
module ListBackupPlanTemplatesOutput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      backupPlanTemplatesList: BackupPlanTemplatesList.t option
        [@ocaml.doc
          "An array of template list items containing metadata about your saved templates."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?backupPlanTemplatesList ->
        fun () -> { nextToken; backupPlanTemplatesList }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("BackupPlanTemplatesList",
          (Option.map x.backupPlanTemplatesList
             ~f:BackupPlanTemplatesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanTemplatesList =
        (Option.map ~f:BackupPlanTemplatesList.of_xml)
          (Xml.child xml_arg0 "BackupPlanTemplatesList") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?backupPlanTemplatesList ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanTemplatesList =
        field_map json "BackupPlanTemplatesList"
          BackupPlanTemplatesList.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?backupPlanTemplatesList ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata of your saved backup plan templates, including the template ID, name, and the creation and deletion dates."]
module ListBackupPlanTemplatesInput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata of your saved backup plan templates, including the template ID, name, and the creation and deletion dates."]
module ListBackupJobsOutput =
  struct
    type nonrec t =
      {
      backupJobs: BackupJobsList.t option
        [@ocaml.doc
          "An array of structures containing metadata about your backup jobs returned in JSON format."];
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupJobs =
      fun ?nextToken -> fun () -> { backupJobs; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupJobs", (Option.map x.backupJobs ~f:BackupJobsList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let backupJobs =
        (Option.map ~f:BackupJobsList.of_xml)
          (Xml.child xml_arg0 "BackupJobs") in
      make ?nextToken ?backupJobs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let backupJobs = field_map json "BackupJobs" BackupJobsList.of_json in
      make ?nextToken ?backupJobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of existing backup jobs for an authenticated account for the last 30 days. For a longer period of time, consider using these monitoring tools."]
module ListBackupJobsInput =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "The next item following a partial list of returned items. For example, if a request is made to return maxResults number of items, NextToken allows you to return more items in your list starting at the location pointed to by the next token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of items to be returned."];
      byResourceArn: ARN.t option
        [@ocaml.doc
          "Returns only backup jobs that match the specified resource Amazon Resource Name (ARN)."];
      byState: BackupJobState.t option
        [@ocaml.doc
          "Returns only backup jobs that are in the specified state."];
      byBackupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "Returns only backup jobs that will be stored in the specified backup vault. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      byCreatedBefore: Timestamp.t option
        [@ocaml.doc
          "Returns only backup jobs that were created before the specified date."];
      byCreatedAfter: Timestamp.t option
        [@ocaml.doc
          "Returns only backup jobs that were created after the specified date."];
      byResourceType: ResourceType.t option
        [@ocaml.doc
          "Returns only backup jobs for the specified resources: Aurora for Amazon Aurora DocumentDB for Amazon DocumentDB (with MongoDB compatibility) DynamoDB for Amazon DynamoDB EBS for Amazon Elastic Block Store EC2 for Amazon Elastic Compute Cloud EFS for Amazon Elastic File System FSx for Amazon FSx Neptune for Amazon Neptune RDS for Amazon Relational Database Service Storage Gateway for Storage Gateway S3 for Amazon S3 VirtualMachine for virtual machines"];
      byAccountId: AccountId.t option
        [@ocaml.doc
          "The account ID to list the jobs from. Returns only backup jobs associated with the specified account ID. If used from an Organizations management account, passing * returns all jobs across the organization."]}
    let make ?nextToken =
      fun ?maxResults ->
        fun ?byResourceArn ->
          fun ?byState ->
            fun ?byBackupVaultName ->
              fun ?byCreatedBefore ->
                fun ?byCreatedAfter ->
                  fun ?byResourceType ->
                    fun ?byAccountId ->
                      fun () ->
                        {
                          nextToken;
                          maxResults;
                          byResourceArn;
                          byState;
                          byBackupVaultName;
                          byCreatedBefore;
                          byCreatedAfter;
                          byResourceType;
                          byAccountId
                        }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("resourceArn", (Option.map x.byResourceArn ~f:ARN.to_value));
        ("state", (Option.map x.byState ~f:BackupJobState.to_value));
        ("backupVaultName",
          (Option.map x.byBackupVaultName ~f:BackupVaultName.to_value));
        ("createdBefore",
          (Option.map x.byCreatedBefore ~f:Timestamp.to_value));
        ("createdAfter", (Option.map x.byCreatedAfter ~f:Timestamp.to_value));
        ("resourceType",
          (Option.map x.byResourceType ~f:ResourceType.to_value));
        ("accountId", (Option.map x.byAccountId ~f:AccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let byAccountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      let byResourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let byCreatedAfter =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAfter") in
      let byCreatedBefore =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdBefore") in
      let byBackupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "backupVaultName") in
      let byState =
        (Option.map ~f:BackupJobState.of_xml) (Xml.child xml_arg0 "state") in
      let byResourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "resourceArn") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?byAccountId ?byResourceType ?byCreatedAfter ?byCreatedBefore
        ?byBackupVaultName ?byState ?byResourceArn ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let byAccountId = field_map json "ByAccountId" AccountId.of_json in
      let byResourceType =
        field_map json "ByResourceType" ResourceType.of_json in
      let byCreatedAfter = field_map json "ByCreatedAfter" Timestamp.of_json in
      let byCreatedBefore =
        field_map json "ByCreatedBefore" Timestamp.of_json in
      let byBackupVaultName =
        field_map json "ByBackupVaultName" BackupVaultName.of_json in
      let byState = field_map json "ByState" BackupJobState.of_json in
      let byResourceArn = field_map json "ByResourceArn" ARN.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      make ?byAccountId ?byResourceType ?byCreatedAfter ?byCreatedBefore
        ?byBackupVaultName ?byState ?byResourceArn ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of existing backup jobs for an authenticated account for the last 30 days. For a longer period of time, consider using these monitoring tools."]
module InvalidResourceStateException =
  struct
    type nonrec t =
      {
      code: String_.t option ;
      message: String_.t option ;
      type_: String_.t option ;
      context: String_.t option }
    let make ?code =
      fun ?message ->
        fun ?type_ ->
          fun ?context -> fun () -> { code; message; type_; context }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Context", (Option.map x.context ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let context =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Context") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Code") in
      make ?context ?type_ ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let context = field_map json "Context" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" String_.of_json in
      make ?context ?type_ ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Backup is already performing an action on this recovery point. It can't perform the action you requested until the first action finishes. Try again later."]
module GetSupportedResourceTypesOutput =
  struct
    type nonrec t =
      {
      resourceTypes: ResourceTypes.t option
        [@ocaml.doc
          "Contains a string with the supported Amazon Web Services resource types: Aurora for Amazon Aurora DynamoDB for Amazon DynamoDB EBS for Amazon Elastic Block Store EC2 for Amazon Elastic Compute Cloud EFS for Amazon Elastic File System FSX for Amazon FSx RDS for Amazon Relational Database Service Storage Gateway for Storage Gateway DocDB for Amazon DocumentDB (with MongoDB compatibility) Neptune for Amazon Neptune"]}
    type nonrec error =
      [ `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?resourceTypes = fun () -> { resourceTypes }
    let error_of_json name json =
      match name with
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceTypes",
           (Option.map x.resourceTypes ~f:ResourceTypes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceTypes =
        (Option.map ~f:ResourceTypes.of_xml)
          (Xml.child xml_arg0 "ResourceTypes") in
      make ?resourceTypes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceTypes =
        field_map json "ResourceTypes" ResourceTypes.of_json in
      make ?resourceTypes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the Amazon Web Services resource types supported by Backup."]
module GetRecoveryPointRestoreMetadataOutput =
  struct
    type nonrec t =
      {
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      restoreMetadata: Metadata.t option
        [@ocaml.doc
          "The set of metadata key-value pairs that describe the original configuration of the backed-up resource. These values vary depending on the service that is being restored."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupVaultArn =
      fun ?recoveryPointArn ->
        fun ?restoreMetadata ->
          fun () -> { backupVaultArn; recoveryPointArn; restoreMetadata }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("RecoveryPointArn", (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("RestoreMetadata",
          (Option.map x.restoreMetadata ~f:Metadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let restoreMetadata =
        (Option.map ~f:Metadata.of_xml)
          (Xml.child xml_arg0 "RestoreMetadata") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      make ?restoreMetadata ?recoveryPointArn ?backupVaultArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let restoreMetadata = field_map json "RestoreMetadata" Metadata.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      make ?restoreMetadata ?recoveryPointArn ?backupVaultArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a set of metadata key-value pairs that were used to create the backup."]
module GetRecoveryPointRestoreMetadataInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      recoveryPointArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."]}
    let context_ = "GetRecoveryPointRestoreMetadataInput"
    let make ~backupVaultName =
      fun ~recoveryPointArn ->
        fun () -> { backupVaultName; recoveryPointArn }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("recoveryPointArn", (Some (ARN.to_value x.recoveryPointArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recoveryPointArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recoveryPointArn") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~recoveryPointArn ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recoveryPointArn =
        field_map_exn json "RecoveryPointArn" ARN.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~recoveryPointArn ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a set of metadata key-value pairs that were used to create the backup."]
module GetBackupVaultNotificationsOutput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      sNSTopicArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies an Amazon Simple Notification Service (Amazon SNS) topic; for example, arn:aws:sns:us-west-2:111122223333:MyTopic."];
      backupVaultEvents: BackupVaultEvents.t option
        [@ocaml.doc
          "An array of events that indicate the status of jobs to back up resources to the backup vault."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupVaultName =
      fun ?backupVaultArn ->
        fun ?sNSTopicArn ->
          fun ?backupVaultEvents ->
            fun () ->
              {
                backupVaultName;
                backupVaultArn;
                sNSTopicArn;
                backupVaultEvents
              }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupVaultName",
           (Option.map x.backupVaultName ~f:BackupVaultName.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("SNSTopicArn", (Option.map x.sNSTopicArn ~f:ARN.to_value));
        ("BackupVaultEvents",
          (Option.map x.backupVaultEvents ~f:BackupVaultEvents.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultEvents =
        (Option.map ~f:BackupVaultEvents.of_xml)
          (Xml.child xml_arg0 "BackupVaultEvents") in
      let sNSTopicArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "SNSTopicArn") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      make ?backupVaultEvents ?sNSTopicArn ?backupVaultArn ?backupVaultName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultEvents =
        field_map json "BackupVaultEvents" BackupVaultEvents.of_json in
      let sNSTopicArn = field_map json "SNSTopicArn" ARN.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      make ?backupVaultEvents ?sNSTopicArn ?backupVaultArn ?backupVaultName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns event notifications for the specified backup vault."]
module GetBackupVaultNotificationsInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."]}
    let context_ = "GetBackupVaultNotificationsInput"
    let make ~backupVaultName = fun () -> { backupVaultName }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns event notifications for the specified backup vault."]
module GetBackupVaultAccessPolicyOutput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      policy: IAMPolicy.t option
        [@ocaml.doc
          "The backup vault access policy document in JSON format."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupVaultName =
      fun ?backupVaultArn ->
        fun ?policy -> fun () -> { backupVaultName; backupVaultArn; policy }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupVaultName",
           (Option.map x.backupVaultName ~f:BackupVaultName.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("Policy", (Option.map x.policy ~f:IAMPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:IAMPolicy.of_xml) (Xml.child xml_arg0 "Policy") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      make ?policy ?backupVaultArn ?backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let policy = field_map json "Policy" IAMPolicy.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      make ?policy ?backupVaultArn ?backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the access policy document that is associated with the named backup vault."]
module GetBackupVaultAccessPolicyInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."]}
    let context_ = "GetBackupVaultAccessPolicyInput"
    let make ~backupVaultName = fun () -> { backupVaultName }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the access policy document that is associated with the named backup vault."]
module GetBackupSelectionOutput =
  struct
    type nonrec t =
      {
      backupSelection: BackupSelection.t option
        [@ocaml.doc
          "Specifies the body of a request to assign a set of resources to a backup plan."];
      selectionId: String_.t option
        [@ocaml.doc
          "Uniquely identifies the body of a request to assign a set of resources to a backup plan."];
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup selection is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "A unique string that identifies the request and allows failed requests to be retried without the risk of running the operation twice."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupSelection =
      fun ?selectionId ->
        fun ?backupPlanId ->
          fun ?creationDate ->
            fun ?creatorRequestId ->
              fun () ->
                {
                  backupSelection;
                  selectionId;
                  backupPlanId;
                  creationDate;
                  creatorRequestId
                }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupSelection",
           (Option.map x.backupSelection ~f:BackupSelection.to_value));
        ("SelectionId", (Option.map x.selectionId ~f:String_.to_value));
        ("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      let selectionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SelectionId") in
      let backupSelection =
        (Option.map ~f:BackupSelection.of_xml)
          (Xml.child xml_arg0 "BackupSelection") in
      make ?creatorRequestId ?creationDate ?backupPlanId ?selectionId
        ?backupSelection ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      let selectionId = field_map json "SelectionId" String_.of_json in
      let backupSelection =
        field_map json "BackupSelection" BackupSelection.of_json in
      make ?creatorRequestId ?creationDate ?backupPlanId ?selectionId
        ?backupSelection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns selection metadata and a document in JSON format that specifies a list of resources that are associated with a backup plan."]
module GetBackupSelectionInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc "Uniquely identifies a backup plan."];
      selectionId: String_.t
        [@ocaml.doc
          "Uniquely identifies the body of a request to assign a set of resources to a backup plan."]}
    let context_ = "GetBackupSelectionInput"
    let make ~backupPlanId =
      fun ~selectionId -> fun () -> { backupPlanId; selectionId }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)));
        ("selectionId", (Some (String_.to_value x.selectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let selectionId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "selectionId") in
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ~selectionId ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let selectionId = field_map_exn json "SelectionId" String_.of_json in
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ~selectionId ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns selection metadata and a document in JSON format that specifies a list of resources that are associated with a backup plan."]
module GetBackupPlanOutput =
  struct
    type nonrec t =
      {
      backupPlan: BackupPlan.t option
        [@ocaml.doc
          "Specifies the body of a backup plan. Includes a BackupPlanName and one or more sets of Rules."];
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      backupPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup plan; for example, arn:aws:backup:us-east-1:123456789012:plan:8F81F553-3A74-4A3F-B93D-B3360DC80C50."];
      versionId: String_.t option
        [@ocaml.doc
          "Unique, randomly generated, Unicode, UTF-8 encoded strings that are at most 1,024 bytes long. Version IDs cannot be edited."];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "A unique string that identifies the request and allows failed requests to be retried without the risk of running the operation twice."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a backup plan is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      deletionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a backup plan is deleted, in Unix format and Coordinated Universal Time (UTC). The value of DeletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      lastExecutionDate: Timestamp.t option
        [@ocaml.doc
          "The last time a job to back up resources was run with this backup plan. A date and time, in Unix format and Coordinated Universal Time (UTC). The value of LastExecutionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      advancedBackupSettings: AdvancedBackupSettings.t option
        [@ocaml.doc
          "Contains a list of BackupOptions for each resource type. The list is populated only if the advanced option is set for the backup plan."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupPlan =
      fun ?backupPlanId ->
        fun ?backupPlanArn ->
          fun ?versionId ->
            fun ?creatorRequestId ->
              fun ?creationDate ->
                fun ?deletionDate ->
                  fun ?lastExecutionDate ->
                    fun ?advancedBackupSettings ->
                      fun () ->
                        {
                          backupPlan;
                          backupPlanId;
                          backupPlanArn;
                          versionId;
                          creatorRequestId;
                          creationDate;
                          deletionDate;
                          lastExecutionDate;
                          advancedBackupSettings
                        }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupPlan", (Option.map x.backupPlan ~f:BackupPlan.to_value));
        ("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("BackupPlanArn", (Option.map x.backupPlanArn ~f:ARN.to_value));
        ("VersionId", (Option.map x.versionId ~f:String_.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("DeletionDate", (Option.map x.deletionDate ~f:Timestamp.to_value));
        ("LastExecutionDate",
          (Option.map x.lastExecutionDate ~f:Timestamp.to_value));
        ("AdvancedBackupSettings",
          (Option.map x.advancedBackupSettings
             ~f:AdvancedBackupSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let advancedBackupSettings =
        (Option.map ~f:AdvancedBackupSettings.of_xml)
          (Xml.child xml_arg0 "AdvancedBackupSettings") in
      let lastExecutionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastExecutionDate") in
      let deletionDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "DeletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let versionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VersionId") in
      let backupPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupPlanArn") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      let backupPlan =
        (Option.map ~f:BackupPlan.of_xml) (Xml.child xml_arg0 "BackupPlan") in
      make ?advancedBackupSettings ?lastExecutionDate ?deletionDate
        ?creationDate ?creatorRequestId ?versionId ?backupPlanArn
        ?backupPlanId ?backupPlan ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let advancedBackupSettings =
        field_map json "AdvancedBackupSettings"
          AdvancedBackupSettings.of_json in
      let lastExecutionDate =
        field_map json "LastExecutionDate" Timestamp.of_json in
      let deletionDate = field_map json "DeletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let versionId = field_map json "VersionId" String_.of_json in
      let backupPlanArn = field_map json "BackupPlanArn" ARN.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      let backupPlan = field_map json "BackupPlan" BackupPlan.of_json in
      make ?advancedBackupSettings ?lastExecutionDate ?deletionDate
        ?creationDate ?creatorRequestId ?versionId ?backupPlanArn
        ?backupPlanId ?backupPlan ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns BackupPlan details for the specified BackupPlanId. The details are the body of a backup plan in JSON format, in addition to plan metadata."]
module GetBackupPlanInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc "Uniquely identifies a backup plan."];
      versionId: String_.t option
        [@ocaml.doc
          "Unique, randomly generated, Unicode, UTF-8 encoded strings that are at most 1,024 bytes long. Version IDs cannot be edited."]}
    let context_ = "GetBackupPlanInput"
    let make ?versionId =
      fun ~backupPlanId -> fun () -> { versionId; backupPlanId }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)));
        ("versionId", (Option.map x.versionId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let versionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "versionId") in
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ?versionId ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let versionId = field_map json "VersionId" String_.of_json in
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ?versionId ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns BackupPlan details for the specified BackupPlanId. The details are the body of a backup plan in JSON format, in addition to plan metadata."]
module GetBackupPlanFromTemplateOutput =
  struct
    type nonrec t =
      {
      backupPlanDocument: BackupPlan.t option
        [@ocaml.doc
          "Returns the body of a backup plan based on the target template, including the name, rules, and backup vault of the plan."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupPlanDocument = fun () -> { backupPlanDocument }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupPlanDocument",
           (Option.map x.backupPlanDocument ~f:BackupPlan.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanDocument =
        (Option.map ~f:BackupPlan.of_xml)
          (Xml.child xml_arg0 "BackupPlanDocument") in
      make ?backupPlanDocument ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanDocument =
        field_map json "BackupPlanDocument" BackupPlan.of_json in
      make ?backupPlanDocument ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the template specified by its templateId as a backup plan."]
module GetBackupPlanFromTemplateInput =
  struct
    type nonrec t =
      {
      backupPlanTemplateId: String_.t
        [@ocaml.doc "Uniquely identifies a stored backup plan template."]}
    let context_ = "GetBackupPlanFromTemplateInput"
    let make ~backupPlanTemplateId = fun () -> { backupPlanTemplateId }
    let to_value x =
      structure_to_value
        [("templateId", (Some (String_.to_value x.backupPlanTemplateId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanTemplateId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "templateId") in
      make ~backupPlanTemplateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanTemplateId =
        field_map_exn json "BackupPlanTemplateId" String_.of_json in
      make ~backupPlanTemplateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the template specified by its templateId as a backup plan."]
module GetBackupPlanFromJSONOutput =
  struct
    type nonrec t =
      {
      backupPlan: BackupPlan.t option
        [@ocaml.doc
          "Specifies the body of a backup plan. Includes a BackupPlanName and one or more sets of Rules."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupPlan = fun () -> { backupPlan }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupPlan", (Option.map x.backupPlan ~f:BackupPlan.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlan =
        (Option.map ~f:BackupPlan.of_xml) (Xml.child xml_arg0 "BackupPlan") in
      make ?backupPlan ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlan = field_map json "BackupPlan" BackupPlan.of_json in
      make ?backupPlan ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a valid JSON document specifying a backup plan or an error."]
module GetBackupPlanFromJSONInput =
  struct
    type nonrec t =
      {
      backupPlanTemplateJson: String_.t
        [@ocaml.doc
          "A customer-supplied backup plan document in JSON format."]}
    let context_ = "GetBackupPlanFromJSONInput"
    let make ~backupPlanTemplateJson = fun () -> { backupPlanTemplateJson }
    let to_value x =
      structure_to_value
        [("BackupPlanTemplateJson",
           (Some (String_.to_value x.backupPlanTemplateJson)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanTemplateJson =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BackupPlanTemplateJson") in
      make ~backupPlanTemplateJson ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanTemplateJson =
        field_map_exn json "BackupPlanTemplateJson" String_.of_json in
      make ~backupPlanTemplateJson ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a valid JSON document specifying a backup plan or an error."]
module ExportBackupPlanTemplateOutput =
  struct
    type nonrec t =
      {
      backupPlanTemplateJson: String_.t option
        [@ocaml.doc
          "The body of a backup plan template in JSON format. This is a signed JSON document that cannot be modified before being passed to GetBackupPlanFromJSON."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupPlanTemplateJson = fun () -> { backupPlanTemplateJson }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupPlanTemplateJson",
           (Option.map x.backupPlanTemplateJson ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanTemplateJson =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "BackupPlanTemplateJson") in
      make ?backupPlanTemplateJson ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanTemplateJson =
        field_map json "BackupPlanTemplateJson" String_.of_json in
      make ?backupPlanTemplateJson ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the backup plan that is specified by the plan ID as a backup template."]
module ExportBackupPlanTemplateInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc "Uniquely identifies a backup plan."]}
    let context_ = "ExportBackupPlanTemplateInput"
    let make ~backupPlanId = fun () -> { backupPlanId }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the backup plan that is specified by the plan ID as a backup template."]
module DisassociateRecoveryPointInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc "The unique name of an Backup vault."];
      recoveryPointArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies an Backup recovery point."]}
    let context_ = "DisassociateRecoveryPointInput"
    let make ~backupVaultName =
      fun ~recoveryPointArn ->
        fun () -> { backupVaultName; recoveryPointArn }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("recoveryPointArn", (Some (ARN.to_value x.recoveryPointArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recoveryPointArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recoveryPointArn") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~recoveryPointArn ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recoveryPointArn =
        field_map_exn json "RecoveryPointArn" ARN.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~recoveryPointArn ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified continuous backup recovery point from Backup and releases control of that continuous backup to the source service, such as Amazon RDS. The source service will continue to create and retain continuous backups using the lifecycle that you specified in your original backup plan. Does not support snapshot backup recovery points."]
module DescribeRestoreJobOutput =
  struct
    type nonrec t =
      {
      accountId: AccountId.t option
        [@ocaml.doc "Returns the account ID that owns the restore job."];
      restoreJobId: String_.t option
        [@ocaml.doc
          "Uniquely identifies the job that restores a recovery point."];
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a restore job is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      completionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a job to restore a recovery point is completed, in Unix format and Coordinated Universal Time (UTC). The value of CompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      status: RestoreJobStatus.t option
        [@ocaml.doc
          "Status code specifying the state of the job that is initiated by Backup to restore a recovery point."];
      statusMessage: String_.t option
        [@ocaml.doc
          "A message showing the status of a job to restore a recovery point."];
      percentDone: String_.t option
        [@ocaml.doc
          "Contains an estimated percentage that is complete of a job at the time the job status was queried."];
      backupSizeInBytes: Long.t option
        [@ocaml.doc "The size, in bytes, of the restored resource."];
      iamRoleArn: IAMRoleArn.t option
        [@ocaml.doc
          "Specifies the IAM role ARN used to create the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      expectedCompletionTimeMinutes: Long.t option
        [@ocaml.doc
          "The amount of time in minutes that a job restoring a recovery point is expected to take."];
      createdResourceArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource whose recovery point is being restored. The format of the ARN depends on the resource type of the backed-up resource."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "Returns metadata associated with a restore job listed by resource type."]}
    type nonrec error =
      [ `DependencyFailureException of DependencyFailureException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?accountId =
      fun ?restoreJobId ->
        fun ?recoveryPointArn ->
          fun ?creationDate ->
            fun ?completionDate ->
              fun ?status ->
                fun ?statusMessage ->
                  fun ?percentDone ->
                    fun ?backupSizeInBytes ->
                      fun ?iamRoleArn ->
                        fun ?expectedCompletionTimeMinutes ->
                          fun ?createdResourceArn ->
                            fun ?resourceType ->
                              fun () ->
                                {
                                  accountId;
                                  restoreJobId;
                                  recoveryPointArn;
                                  creationDate;
                                  completionDate;
                                  status;
                                  statusMessage;
                                  percentDone;
                                  backupSizeInBytes;
                                  iamRoleArn;
                                  expectedCompletionTimeMinutes;
                                  createdResourceArn;
                                  resourceType
                                }
    let error_of_json name json =
      match name with
      | "DependencyFailureException" ->
          `DependencyFailureException
            (DependencyFailureException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DependencyFailureException" ->
          `DependencyFailureException (DependencyFailureException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DependencyFailureException e ->
          `Assoc
            [("error", (`String "DependencyFailureException"));
            ("details", (DependencyFailureException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AccountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("RestoreJobId", (Option.map x.restoreJobId ~f:String_.to_value));
        ("RecoveryPointArn", (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CompletionDate",
          (Option.map x.completionDate ~f:Timestamp.to_value));
        ("Status", (Option.map x.status ~f:RestoreJobStatus.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("PercentDone", (Option.map x.percentDone ~f:String_.to_value));
        ("BackupSizeInBytes",
          (Option.map x.backupSizeInBytes ~f:Long.to_value));
        ("IamRoleArn", (Option.map x.iamRoleArn ~f:IAMRoleArn.to_value));
        ("ExpectedCompletionTimeMinutes",
          (Option.map x.expectedCompletionTimeMinutes ~f:Long.to_value));
        ("CreatedResourceArn",
          (Option.map x.createdResourceArn ~f:ARN.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let createdResourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "CreatedResourceArn") in
      let expectedCompletionTimeMinutes =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "ExpectedCompletionTimeMinutes") in
      let iamRoleArn =
        (Option.map ~f:IAMRoleArn.of_xml) (Xml.child xml_arg0 "IamRoleArn") in
      let backupSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BackupSizeInBytes") in
      let percentDone =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PercentDone") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:RestoreJobStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let completionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CompletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      let restoreJobId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RestoreJobId") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "AccountId") in
      make ?resourceType ?createdResourceArn ?expectedCompletionTimeMinutes
        ?iamRoleArn ?backupSizeInBytes ?percentDone ?statusMessage ?status
        ?completionDate ?creationDate ?recoveryPointArn ?restoreJobId
        ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let createdResourceArn =
        field_map json "CreatedResourceArn" ARN.of_json in
      let expectedCompletionTimeMinutes =
        field_map json "ExpectedCompletionTimeMinutes" Long.of_json in
      let iamRoleArn = field_map json "IamRoleArn" IAMRoleArn.of_json in
      let backupSizeInBytes = field_map json "BackupSizeInBytes" Long.of_json in
      let percentDone = field_map json "PercentDone" String_.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let status = field_map json "Status" RestoreJobStatus.of_json in
      let completionDate = field_map json "CompletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      let restoreJobId = field_map json "RestoreJobId" String_.of_json in
      let accountId = field_map json "AccountId" AccountId.of_json in
      make ?resourceType ?createdResourceArn ?expectedCompletionTimeMinutes
        ?iamRoleArn ?backupSizeInBytes ?percentDone ?statusMessage ?status
        ?completionDate ?creationDate ?recoveryPointArn ?restoreJobId
        ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata associated with a restore job that is specified by a job ID."]
module DescribeRestoreJobInput =
  struct
    type nonrec t =
      {
      restoreJobId: RestoreJobId.t
        [@ocaml.doc
          "Uniquely identifies the job that restores a recovery point."]}
    let context_ = "DescribeRestoreJobInput"
    let make ~restoreJobId = fun () -> { restoreJobId }
    let to_value x =
      structure_to_value
        [("restoreJobId", (Some (RestoreJobId.to_value x.restoreJobId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let restoreJobId =
        RestoreJobId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "restoreJobId") in
      make ~restoreJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let restoreJobId =
        field_map_exn json "RestoreJobId" RestoreJobId.of_json in
      make ~restoreJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata associated with a restore job that is specified by a job ID."]
module DescribeReportPlanOutput =
  struct
    type nonrec t =
      {
      reportPlan: ReportPlan.t option
        [@ocaml.doc
          "Returns details about the report plan that is specified by its name. These details include the report plan's Amazon Resource Name (ARN), description, settings, delivery channel, deployment status, creation time, and last attempted and successful run times."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportPlan = fun () -> { reportPlan }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReportPlan", (Option.map x.reportPlan ~f:ReportPlan.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportPlan =
        (Option.map ~f:ReportPlan.of_xml) (Xml.child xml_arg0 "ReportPlan") in
      make ?reportPlan ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reportPlan = field_map json "ReportPlan" ReportPlan.of_json in
      make ?reportPlan ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all report plans for an Amazon Web Services account and Amazon Web Services Region."]
module DescribeReportPlanInput =
  struct
    type nonrec t =
      {
      reportPlanName: ReportPlanName.t
        [@ocaml.doc "The unique name of a report plan."]}
    let context_ = "DescribeReportPlanInput"
    let make ~reportPlanName = fun () -> { reportPlanName }
    let to_value x =
      structure_to_value
        [("reportPlanName",
           (Some (ReportPlanName.to_value x.reportPlanName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportPlanName =
        ReportPlanName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportPlanName") in
      make ~reportPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reportPlanName =
        field_map_exn json "ReportPlanName" ReportPlanName.of_json in
      make ~reportPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all report plans for an Amazon Web Services account and Amazon Web Services Region."]
module DescribeReportJobOutput =
  struct
    type nonrec t =
      {
      reportJob: ReportJob.t option
        [@ocaml.doc
          "A list of information about a report job, including its completion and creation times, report destination, unique report job ID, Amazon Resource Name (ARN), report template, status, and status message."]}
    type nonrec error =
      [ `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportJob = fun () -> { reportJob }
    let error_of_json name json =
      match name with
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReportJob", (Option.map x.reportJob ~f:ReportJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportJob =
        (Option.map ~f:ReportJob.of_xml) (Xml.child xml_arg0 "ReportJob") in
      make ?reportJob ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reportJob = field_map json "ReportJob" ReportJob.of_json in
      make ?reportJob ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the details associated with creating a report as specified by its ReportJobId."]
module DescribeReportJobInput =
  struct
    type nonrec t =
      {
      reportJobId: ReportJobId.t
        [@ocaml.doc
          "The identifier of the report job. A unique, randomly generated, Unicode, UTF-8 encoded string that is at most 1,024 bytes long. The report job ID cannot be edited."]}
    let context_ = "DescribeReportJobInput"
    let make ~reportJobId = fun () -> { reportJobId }
    let to_value x =
      structure_to_value
        [("reportJobId", (Some (ReportJobId.to_value x.reportJobId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportJobId =
        ReportJobId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportJobId") in
      make ~reportJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reportJobId = field_map_exn json "ReportJobId" ReportJobId.of_json in
      make ~reportJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the details associated with creating a report as specified by its ReportJobId."]
module DescribeRegionSettingsOutput =
  struct
    type nonrec t =
      {
      resourceTypeOptInPreference: ResourceTypeOptInPreference.t option
        [@ocaml.doc
          "Returns a list of all services along with the opt-in preferences in the Region."];
      resourceTypeManagementPreference:
        ResourceTypeManagementPreference.t option
        [@ocaml.doc
          "Returns whether Backup fully manages the backups for a resource type. For the benefits of full Backup management, see Full Backup management. For a list of resource types and whether each supports full Backup management, see the Feature availability by resource table. If \"DynamoDB\":false, you can enable full Backup management for DynamoDB backup by enabling Backup's advanced DynamoDB backup features."]}
    type nonrec error =
      [ `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?resourceTypeOptInPreference =
      fun ?resourceTypeManagementPreference ->
        fun () ->
          { resourceTypeOptInPreference; resourceTypeManagementPreference }
    let error_of_json name json =
      match name with
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceTypeOptInPreference",
           (Option.map x.resourceTypeOptInPreference
              ~f:ResourceTypeOptInPreference.to_value));
        ("ResourceTypeManagementPreference",
          (Option.map x.resourceTypeManagementPreference
             ~f:ResourceTypeManagementPreference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceTypeManagementPreference =
        (Option.map ~f:ResourceTypeManagementPreference.of_xml)
          (Xml.child xml_arg0 "ResourceTypeManagementPreference") in
      let resourceTypeOptInPreference =
        (Option.map ~f:ResourceTypeOptInPreference.of_xml)
          (Xml.child xml_arg0 "ResourceTypeOptInPreference") in
      make ?resourceTypeManagementPreference ?resourceTypeOptInPreference ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceTypeManagementPreference =
        field_map json "ResourceTypeManagementPreference"
          ResourceTypeManagementPreference.of_json in
      let resourceTypeOptInPreference =
        field_map json "ResourceTypeOptInPreference"
          ResourceTypeOptInPreference.of_json in
      make ?resourceTypeManagementPreference ?resourceTypeOptInPreference ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current service opt-in settings for the Region. If service opt-in is enabled for a service, Backup tries to protect that service's resources in this Region, when the resource is included in an on-demand backup or scheduled backup plan. Otherwise, Backup does not try to protect that service's resources in this Region."]
module DescribeRegionSettingsInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current service opt-in settings for the Region. If service opt-in is enabled for a service, Backup tries to protect that service's resources in this Region, when the resource is included in an on-demand backup or scheduled backup plan. Otherwise, Backup does not try to protect that service's resources in this Region."]
module DescribeRecoveryPointOutput =
  struct
    type nonrec t =
      {
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      sourceBackupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies the source vault where the resource was originally backed up in; for example, arn:aws:backup:us-east-1:123456789012:vault:BackupVault. If the recovery is restored to the same Amazon Web Services account or Region, this value will be null."];
      resourceArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a saved resource. The format of the ARN depends on the resource type."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource to save as a recovery point; for example, an Amazon Elastic Block Store (Amazon EBS) volume or an Amazon Relational Database Service (Amazon RDS) database."];
      createdBy: RecoveryPointCreator.t option
        [@ocaml.doc
          "Contains identifying information about the creation of a recovery point, including the BackupPlanArn, BackupPlanId, BackupPlanVersion, and BackupRuleId of the backup plan used to create it."];
      iamRoleArn: IAMRoleArn.t option
        [@ocaml.doc
          "Specifies the IAM role ARN used to create the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      status: RecoveryPointStatus.t option
        [@ocaml.doc
          "A status code specifying the state of the recovery point. PARTIAL status indicates Backup could not create the recovery point before the backup window closed. To increase your backup plan window using the API, see UpdateBackupPlan. You can also increase your backup plan window using the Console by choosing and editing your backup plan. EXPIRED status indicates that the recovery point has exceeded its retention period, but Backup lacks permission or is otherwise unable to delete it. To manually delete these recovery points, see Step 3: Delete the recovery points in the Clean up resources section of Getting started."];
      statusMessage: String_.t option
        [@ocaml.doc
          "A status message explaining the reason for the recovery point deletion failure."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a recovery point is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      completionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a job to create a recovery point is completed, in Unix format and Coordinated Universal Time (UTC). The value of CompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      backupSizeInBytes: Long.t option
        [@ocaml.doc "The size, in bytes, of a backup."];
      calculatedLifecycle: CalculatedLifecycle.t option
        [@ocaml.doc
          "A CalculatedLifecycle object containing DeleteAt and MoveToColdStorageAt timestamps."];
      lifecycle: Lifecycle.t option
        [@ocaml.doc
          "The lifecycle defines when a protected resource is transitioned to cold storage and when it expires. Backup transitions and expires backups automatically according to the lifecycle that you define. Backups that are transitioned to cold storage must be stored in cold storage for a minimum of 90 days. Therefore, the \226\128\156retention\226\128\157 setting must be 90 days greater than the \226\128\156transition to cold after days\226\128\157 setting. The \226\128\156transition to cold after days\226\128\157 setting cannot be changed after a backup has been transitioned to cold. Only resource types that support full Backup management can transition their backups to cold storage. Those resource types are listed in the \"Full Backup management\" section of the Feature availability by resource table. Backup ignores this expression for other resource types."];
      encryptionKeyArn: ARN.t option
        [@ocaml.doc
          "The server-side encryption key used to protect your backups; for example, arn:aws:kms:us-west-2:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab."];
      isEncrypted: Boolean__lc1.t option
        [@ocaml.doc
          "A Boolean value that is returned as TRUE if the specified recovery point is encrypted, or FALSE if the recovery point is not encrypted."];
      storageClass: StorageClass.t option
        [@ocaml.doc
          "Specifies the storage class of the recovery point. Valid values are WARM or COLD."];
      lastRestoreTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a recovery point was last restored, in Unix format and Coordinated Universal Time (UTC). The value of LastRestoreTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recoveryPointArn =
      fun ?backupVaultName ->
        fun ?backupVaultArn ->
          fun ?sourceBackupVaultArn ->
            fun ?resourceArn ->
              fun ?resourceType ->
                fun ?createdBy ->
                  fun ?iamRoleArn ->
                    fun ?status ->
                      fun ?statusMessage ->
                        fun ?creationDate ->
                          fun ?completionDate ->
                            fun ?backupSizeInBytes ->
                              fun ?calculatedLifecycle ->
                                fun ?lifecycle ->
                                  fun ?encryptionKeyArn ->
                                    fun ?isEncrypted ->
                                      fun ?storageClass ->
                                        fun ?lastRestoreTime ->
                                          fun () ->
                                            {
                                              recoveryPointArn;
                                              backupVaultName;
                                              backupVaultArn;
                                              sourceBackupVaultArn;
                                              resourceArn;
                                              resourceType;
                                              createdBy;
                                              iamRoleArn;
                                              status;
                                              statusMessage;
                                              creationDate;
                                              completionDate;
                                              backupSizeInBytes;
                                              calculatedLifecycle;
                                              lifecycle;
                                              encryptionKeyArn;
                                              isEncrypted;
                                              storageClass;
                                              lastRestoreTime
                                            }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RecoveryPointArn",
           (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("BackupVaultName",
          (Option.map x.backupVaultName ~f:BackupVaultName.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("SourceBackupVaultArn",
          (Option.map x.sourceBackupVaultArn ~f:ARN.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:ARN.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("CreatedBy",
          (Option.map x.createdBy ~f:RecoveryPointCreator.to_value));
        ("IamRoleArn", (Option.map x.iamRoleArn ~f:IAMRoleArn.to_value));
        ("Status", (Option.map x.status ~f:RecoveryPointStatus.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CompletionDate",
          (Option.map x.completionDate ~f:Timestamp.to_value));
        ("BackupSizeInBytes",
          (Option.map x.backupSizeInBytes ~f:Long.to_value));
        ("CalculatedLifecycle",
          (Option.map x.calculatedLifecycle ~f:CalculatedLifecycle.to_value));
        ("Lifecycle", (Option.map x.lifecycle ~f:Lifecycle.to_value));
        ("EncryptionKeyArn", (Option.map x.encryptionKeyArn ~f:ARN.to_value));
        ("IsEncrypted", (Option.map x.isEncrypted ~f:Boolean__lc1.to_value));
        ("StorageClass",
          (Option.map x.storageClass ~f:StorageClass.to_value));
        ("LastRestoreTime",
          (Option.map x.lastRestoreTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastRestoreTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastRestoreTime") in
      let storageClass =
        (Option.map ~f:StorageClass.of_xml)
          (Xml.child xml_arg0 "StorageClass") in
      let isEncrypted =
        (Option.map ~f:Boolean__lc1.of_xml)
          (Xml.child xml_arg0 "IsEncrypted") in
      let encryptionKeyArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "EncryptionKeyArn") in
      let lifecycle =
        (Option.map ~f:Lifecycle.of_xml) (Xml.child xml_arg0 "Lifecycle") in
      let calculatedLifecycle =
        (Option.map ~f:CalculatedLifecycle.of_xml)
          (Xml.child xml_arg0 "CalculatedLifecycle") in
      let backupSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BackupSizeInBytes") in
      let completionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CompletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:RecoveryPointStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let iamRoleArn =
        (Option.map ~f:IAMRoleArn.of_xml) (Xml.child xml_arg0 "IamRoleArn") in
      let createdBy =
        (Option.map ~f:RecoveryPointCreator.of_xml)
          (Xml.child xml_arg0 "CreatedBy") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let resourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let sourceBackupVaultArn =
        (Option.map ~f:ARN.of_xml)
          (Xml.child xml_arg0 "SourceBackupVaultArn") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      make ?lastRestoreTime ?storageClass ?isEncrypted ?encryptionKeyArn
        ?lifecycle ?calculatedLifecycle ?backupSizeInBytes ?completionDate
        ?creationDate ?statusMessage ?status ?iamRoleArn ?createdBy
        ?resourceType ?resourceArn ?sourceBackupVaultArn ?backupVaultArn
        ?backupVaultName ?recoveryPointArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastRestoreTime =
        field_map json "LastRestoreTime" Timestamp.of_json in
      let storageClass = field_map json "StorageClass" StorageClass.of_json in
      let isEncrypted = field_map json "IsEncrypted" Boolean__lc1.of_json in
      let encryptionKeyArn = field_map json "EncryptionKeyArn" ARN.of_json in
      let lifecycle = field_map json "Lifecycle" Lifecycle.of_json in
      let calculatedLifecycle =
        field_map json "CalculatedLifecycle" CalculatedLifecycle.of_json in
      let backupSizeInBytes = field_map json "BackupSizeInBytes" Long.of_json in
      let completionDate = field_map json "CompletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let status = field_map json "Status" RecoveryPointStatus.of_json in
      let iamRoleArn = field_map json "IamRoleArn" IAMRoleArn.of_json in
      let createdBy = field_map json "CreatedBy" RecoveryPointCreator.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let resourceArn = field_map json "ResourceArn" ARN.of_json in
      let sourceBackupVaultArn =
        field_map json "SourceBackupVaultArn" ARN.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      make ?lastRestoreTime ?storageClass ?isEncrypted ?encryptionKeyArn
        ?lifecycle ?calculatedLifecycle ?backupSizeInBytes ?completionDate
        ?creationDate ?statusMessage ?status ?iamRoleArn ?createdBy
        ?resourceType ?resourceArn ?sourceBackupVaultArn ?backupVaultArn
        ?backupVaultName ?recoveryPointArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata associated with a recovery point, including ID, status, encryption, and lifecycle."]
module DescribeRecoveryPointInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      recoveryPointArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."]}
    let context_ = "DescribeRecoveryPointInput"
    let make ~backupVaultName =
      fun ~recoveryPointArn ->
        fun () -> { backupVaultName; recoveryPointArn }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("recoveryPointArn", (Some (ARN.to_value x.recoveryPointArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recoveryPointArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recoveryPointArn") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~recoveryPointArn ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recoveryPointArn =
        field_map_exn json "RecoveryPointArn" ARN.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~recoveryPointArn ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata associated with a recovery point, including ID, status, encryption, and lifecycle."]
module DescribeProtectedResourceOutput =
  struct
    type nonrec t =
      {
      resourceArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource saved as a recovery point; for example, an Amazon EBS volume or an Amazon RDS database."];
      lastBackupTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a resource was last backed up, in Unix format and Coordinated Universal Time (UTC). The value of LastBackupTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?resourceArn =
      fun ?resourceType ->
        fun ?lastBackupTime ->
          fun () -> { resourceArn; resourceType; lastBackupTime }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceArn", (Option.map x.resourceArn ~f:ARN.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("LastBackupTime",
          (Option.map x.lastBackupTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastBackupTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastBackupTime") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let resourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      make ?lastBackupTime ?resourceType ?resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastBackupTime = field_map json "LastBackupTime" Timestamp.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let resourceArn = field_map json "ResourceArn" ARN.of_json in
      make ?lastBackupTime ?resourceType ?resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a saved resource, including the last time it was backed up, its Amazon Resource Name (ARN), and the Amazon Web Services service type of the saved resource."]
module DescribeProtectedResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."]}
    let context_ = "DescribeProtectedResourceInput"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ARN.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        ARN.of_xml (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceArn = field_map_exn json "ResourceArn" ARN.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a saved resource, including the last time it was backed up, its Amazon Resource Name (ARN), and the Amazon Web Services service type of the saved resource."]
module DescribeGlobalSettingsOutput =
  struct
    type nonrec t =
      {
      globalSettings: GlobalSettings.t option
        [@ocaml.doc "The status of the flag isCrossAccountBackupEnabled."];
      lastUpdateTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that the flag isCrossAccountBackupEnabled was last updated. This update is in Unix format and Coordinated Universal Time (UTC). The value of LastUpdateTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `InvalidRequestException of InvalidRequestException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?globalSettings =
      fun ?lastUpdateTime -> fun () -> { globalSettings; lastUpdateTime }
    let error_of_json name json =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GlobalSettings",
           (Option.map x.globalSettings ~f:GlobalSettings.to_value));
        ("LastUpdateTime",
          (Option.map x.lastUpdateTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdateTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "LastUpdateTime") in
      let globalSettings =
        (Option.map ~f:GlobalSettings.of_xml)
          (Xml.child xml_arg0 "GlobalSettings") in
      make ?lastUpdateTime ?globalSettings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastUpdateTime = field_map json "LastUpdateTime" Timestamp.of_json in
      let globalSettings =
        field_map json "GlobalSettings" GlobalSettings.of_json in
      make ?lastUpdateTime ?globalSettings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes whether the Amazon Web Services account is opted in to cross-account backup. Returns an error if the account is not a member of an Organizations organization. Example: describe-global-settings --region us-west-2"]
module DescribeGlobalSettingsInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes whether the Amazon Web Services account is opted in to cross-account backup. Returns an error if the account is not a member of an Organizations organization. Example: describe-global-settings --region us-west-2"]
module DescribeFrameworkOutput =
  struct
    type nonrec t =
      {
      frameworkName: FrameworkName.t option
        [@ocaml.doc "The unique name of a framework."];
      frameworkArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      frameworkDescription: FrameworkDescription.t option
        [@ocaml.doc "An optional description of the framework."];
      frameworkControls: FrameworkControls.t option
        [@ocaml.doc
          "A list of the controls that make up the framework. Each control in the list has a name, input parameters, and scope."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "The date and time that a framework is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      deploymentStatus: String_.t option
        [@ocaml.doc
          "The deployment status of a framework. The statuses are: CREATE_IN_PROGRESS | UPDATE_IN_PROGRESS | DELETE_IN_PROGRESS | COMPLETED | FAILED"];
      frameworkStatus: String_.t option
        [@ocaml.doc
          "A framework consists of one or more controls. Each control governs a resource, such as backup plans, backup selections, backup vaults, or recovery points. You can also turn Config recording on or off for each resource. The statuses are: ACTIVE when recording is turned on for all resources governed by the framework. PARTIALLY_ACTIVE when recording is turned off for at least one resource governed by the framework. INACTIVE when recording is turned off for all resources governed by the framework. UNAVAILABLE when Backup is unable to validate recording status at this time."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to DescribeFrameworkOutput. Retrying a successful request with the same idempotency token results in a success message with no action taken."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?frameworkName =
      fun ?frameworkArn ->
        fun ?frameworkDescription ->
          fun ?frameworkControls ->
            fun ?creationTime ->
              fun ?deploymentStatus ->
                fun ?frameworkStatus ->
                  fun ?idempotencyToken ->
                    fun () ->
                      {
                        frameworkName;
                        frameworkArn;
                        frameworkDescription;
                        frameworkControls;
                        creationTime;
                        deploymentStatus;
                        frameworkStatus;
                        idempotencyToken
                      }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FrameworkName",
           (Option.map x.frameworkName ~f:FrameworkName.to_value));
        ("FrameworkArn", (Option.map x.frameworkArn ~f:ARN.to_value));
        ("FrameworkDescription",
          (Option.map x.frameworkDescription ~f:FrameworkDescription.to_value));
        ("FrameworkControls",
          (Option.map x.frameworkControls ~f:FrameworkControls.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("DeploymentStatus",
          (Option.map x.deploymentStatus ~f:String_.to_value));
        ("FrameworkStatus",
          (Option.map x.frameworkStatus ~f:String_.to_value));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let frameworkStatus =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "FrameworkStatus") in
      let deploymentStatus =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "DeploymentStatus") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let frameworkControls =
        (Option.map ~f:FrameworkControls.of_xml)
          (Xml.child xml_arg0 "FrameworkControls") in
      let frameworkDescription =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "FrameworkDescription") in
      let frameworkArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "FrameworkArn") in
      let frameworkName =
        (Option.map ~f:FrameworkName.of_xml)
          (Xml.child xml_arg0 "FrameworkName") in
      make ?idempotencyToken ?frameworkStatus ?deploymentStatus ?creationTime
        ?frameworkControls ?frameworkDescription ?frameworkArn ?frameworkName
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let frameworkStatus = field_map json "FrameworkStatus" String_.of_json in
      let deploymentStatus =
        field_map json "DeploymentStatus" String_.of_json in
      let creationTime = field_map json "CreationTime" Timestamp.of_json in
      let frameworkControls =
        field_map json "FrameworkControls" FrameworkControls.of_json in
      let frameworkDescription =
        field_map json "FrameworkDescription" FrameworkDescription.of_json in
      let frameworkArn = field_map json "FrameworkArn" ARN.of_json in
      let frameworkName =
        field_map json "FrameworkName" FrameworkName.of_json in
      make ?idempotencyToken ?frameworkStatus ?deploymentStatus ?creationTime
        ?frameworkControls ?frameworkDescription ?frameworkArn ?frameworkName
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the framework details for the specified FrameworkName."]
module DescribeFrameworkInput =
  struct
    type nonrec t =
      {
      frameworkName: FrameworkName.t
        [@ocaml.doc "The unique name of a framework."]}
    let context_ = "DescribeFrameworkInput"
    let make ~frameworkName = fun () -> { frameworkName }
    let to_value x =
      structure_to_value
        [("frameworkName", (Some (FrameworkName.to_value x.frameworkName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let frameworkName =
        FrameworkName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "frameworkName") in
      make ~frameworkName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let frameworkName =
        field_map_exn json "FrameworkName" FrameworkName.of_json in
      make ~frameworkName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the framework details for the specified FrameworkName."]
module DescribeCopyJobOutput =
  struct
    type nonrec t =
      {
      copyJob: CopyJob.t option
        [@ocaml.doc "Contains detailed information about a copy job."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?copyJob = fun () -> { copyJob }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CopyJob", (Option.map x.copyJob ~f:CopyJob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let copyJob =
        (Option.map ~f:CopyJob.of_xml) (Xml.child xml_arg0 "CopyJob") in
      make ?copyJob ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let copyJob = field_map json "CopyJob" CopyJob.of_json in
      make ?copyJob ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata associated with creating a copy of a resource."]
module DescribeCopyJobInput =
  struct
    type nonrec t =
      {
      copyJobId: String_.t [@ocaml.doc "Uniquely identifies a copy job."]}
    let context_ = "DescribeCopyJobInput"
    let make ~copyJobId = fun () -> { copyJobId }
    let to_value x =
      structure_to_value
        [("copyJobId", (Some (String_.to_value x.copyJobId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let copyJobId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "copyJobId") in
      make ~copyJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let copyJobId = field_map_exn json "CopyJobId" String_.of_json in
      make ~copyJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata associated with creating a copy of a resource."]
module DescribeBackupVaultOutput =
  struct
    type nonrec t =
      {
      backupVaultName: String_.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      encryptionKeyArn: ARN.t option
        [@ocaml.doc
          "The server-side encryption key that is used to protect your backups; for example, arn:aws:kms:us-west-2:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a backup vault is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "A unique string that identifies the request and allows failed requests to be retried without the risk of running the operation twice."];
      numberOfRecoveryPoints: Long__lc1.t option
        [@ocaml.doc
          "The number of recovery points that are stored in a backup vault."];
      locked: Boolean.t option
        [@ocaml.doc
          "A Boolean that indicates whether Backup Vault Lock is currently protecting the backup vault. True means that Vault Lock causes delete or update operations on the recovery points stored in the vault to fail."];
      minRetentionDays: Long.t option
        [@ocaml.doc
          "The Backup Vault Lock setting that specifies the minimum retention period that the vault retains its recovery points. If this parameter is not specified, Vault Lock does not enforce a minimum retention period. If specified, any backup or copy job to the vault must have a lifecycle policy with a retention period equal to or longer than the minimum retention period. If the job's retention period is shorter than that minimum retention period, then the vault fails the backup or copy job, and you should either modify your lifecycle settings or use a different vault. Recovery points already stored in the vault prior to Vault Lock are not affected."];
      maxRetentionDays: Long.t option
        [@ocaml.doc
          "The Backup Vault Lock setting that specifies the maximum retention period that the vault retains its recovery points. If this parameter is not specified, Vault Lock does not enforce a maximum retention period on the recovery points in the vault (allowing indefinite storage). If specified, any backup or copy job to the vault must have a lifecycle policy with a retention period equal to or shorter than the maximum retention period. If the job's retention period is longer than that maximum retention period, then the vault fails the backup or copy job, and you should either modify your lifecycle settings or use a different vault. Recovery points already stored in the vault prior to Vault Lock are not affected."];
      lockDate: Timestamp.t option
        [@ocaml.doc
          "The date and time when Backup Vault Lock configuration cannot be changed or deleted. If you applied Vault Lock to your vault without specifying a lock date, you can change any of your Vault Lock settings, or delete Vault Lock from the vault entirely, at any time. This value is in Unix format, Coordinated Universal Time (UTC), and accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupVaultName =
      fun ?backupVaultArn ->
        fun ?encryptionKeyArn ->
          fun ?creationDate ->
            fun ?creatorRequestId ->
              fun ?numberOfRecoveryPoints ->
                fun ?locked ->
                  fun ?minRetentionDays ->
                    fun ?maxRetentionDays ->
                      fun ?lockDate ->
                        fun () ->
                          {
                            backupVaultName;
                            backupVaultArn;
                            encryptionKeyArn;
                            creationDate;
                            creatorRequestId;
                            numberOfRecoveryPoints;
                            locked;
                            minRetentionDays;
                            maxRetentionDays;
                            lockDate
                          }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupVaultName",
           (Option.map x.backupVaultName ~f:String_.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("EncryptionKeyArn", (Option.map x.encryptionKeyArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value));
        ("NumberOfRecoveryPoints",
          (Option.map x.numberOfRecoveryPoints ~f:Long__lc1.to_value));
        ("Locked", (Option.map x.locked ~f:Boolean.to_value));
        ("MinRetentionDays",
          (Option.map x.minRetentionDays ~f:Long.to_value));
        ("MaxRetentionDays",
          (Option.map x.maxRetentionDays ~f:Long.to_value));
        ("LockDate", (Option.map x.lockDate ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lockDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "LockDate") in
      let maxRetentionDays =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MaxRetentionDays") in
      let minRetentionDays =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "MinRetentionDays") in
      let locked =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Locked") in
      let numberOfRecoveryPoints =
        (Option.map ~f:Long__lc1.of_xml)
          (Xml.child xml_arg0 "NumberOfRecoveryPoints") in
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let encryptionKeyArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "EncryptionKeyArn") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupVaultName") in
      make ?lockDate ?maxRetentionDays ?minRetentionDays ?locked
        ?numberOfRecoveryPoints ?creatorRequestId ?creationDate
        ?encryptionKeyArn ?backupVaultArn ?backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lockDate = field_map json "LockDate" Timestamp.of_json in
      let maxRetentionDays = field_map json "MaxRetentionDays" Long.of_json in
      let minRetentionDays = field_map json "MinRetentionDays" Long.of_json in
      let locked = field_map json "Locked" Boolean.of_json in
      let numberOfRecoveryPoints =
        field_map json "NumberOfRecoveryPoints" Long__lc1.of_json in
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let encryptionKeyArn = field_map json "EncryptionKeyArn" ARN.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName = field_map json "BackupVaultName" String_.of_json in
      make ?lockDate ?maxRetentionDays ?minRetentionDays ?locked
        ?numberOfRecoveryPoints ?creatorRequestId ?creationDate
        ?encryptionKeyArn ?backupVaultArn ?backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata about a backup vault specified by its name."]
module DescribeBackupVaultInput =
  struct
    type nonrec t =
      {
      backupVaultName: String_.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."]}
    let context_ = "DescribeBackupVaultInput"
    let make ~backupVaultName = fun () -> { backupVaultName }
    let to_value x =
      structure_to_value
        [("backupVaultName", (Some (String_.to_value x.backupVaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultName =
        field_map_exn json "BackupVaultName" String_.of_json in
      make ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metadata about a backup vault specified by its name."]
module DescribeBackupJobOutput =
  struct
    type nonrec t =
      {
      accountId: AccountId.t option
        [@ocaml.doc "Returns the account ID that owns the backup job."];
      backupJobId: String_.t option
        [@ocaml.doc
          "Uniquely identifies a request to Backup to back up a resource."];
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      recoveryPointArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."];
      resourceArn: ARN.t option
        [@ocaml.doc
          "An ARN that uniquely identifies a saved resource. The format of the ARN depends on the resource type."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a backup job is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      completionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a job to create a backup job is completed, in Unix format and Coordinated Universal Time (UTC). The value of CompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      state: BackupJobState.t option
        [@ocaml.doc "The current state of a resource recovery point."];
      statusMessage: String_.t option
        [@ocaml.doc
          "A detailed message explaining the status of the job to back up a resource."];
      percentDone: String_.t option
        [@ocaml.doc
          "Contains an estimated percentage that is complete of a job at the time the job status was queried."];
      backupSizeInBytes: Long.t option
        [@ocaml.doc "The size, in bytes, of a backup."];
      iamRoleArn: IAMRoleArn.t option
        [@ocaml.doc
          "Specifies the IAM role ARN used to create the target recovery point; for example, arn:aws:iam::123456789012:role/S3Access."];
      createdBy: RecoveryPointCreator.t option
        [@ocaml.doc
          "Contains identifying information about the creation of a backup job, including the BackupPlanArn, BackupPlanId, BackupPlanVersion, and BackupRuleId of the backup plan that is used to create it."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The type of Amazon Web Services resource to be backed up; for example, an Amazon Elastic Block Store (Amazon EBS) volume or an Amazon Relational Database Service (Amazon RDS) database."];
      bytesTransferred: Long.t option
        [@ocaml.doc
          "The size in bytes transferred to a backup vault at the time that the job status was queried."];
      expectedCompletionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a job to back up resources is expected to be completed, in Unix format and Coordinated Universal Time (UTC). The value of ExpectedCompletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      startBy: Timestamp.t option
        [@ocaml.doc
          "Specifies the time in Unix format and Coordinated Universal Time (UTC) when a backup job must be started before it is canceled. The value is calculated by adding the start window to the scheduled time. So if the scheduled time were 6:00 PM and the start window is 2 hours, the StartBy time would be 8:00 PM on the date specified. The value of StartBy is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      backupOptions: BackupOptions.t option
        [@ocaml.doc
          "Represents the options specified as part of backup plan or on-demand backup job."];
      backupType: String_.t option
        [@ocaml.doc
          "Represents the actual backup type selected for a backup job. For example, if a successful Windows Volume Shadow Copy Service (VSS) backup was taken, BackupType returns \"WindowsVSS\". If BackupType is empty, then the backup type was a regular backup."]}
    type nonrec error =
      [ `DependencyFailureException of DependencyFailureException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?accountId =
      fun ?backupJobId ->
        fun ?backupVaultName ->
          fun ?backupVaultArn ->
            fun ?recoveryPointArn ->
              fun ?resourceArn ->
                fun ?creationDate ->
                  fun ?completionDate ->
                    fun ?state ->
                      fun ?statusMessage ->
                        fun ?percentDone ->
                          fun ?backupSizeInBytes ->
                            fun ?iamRoleArn ->
                              fun ?createdBy ->
                                fun ?resourceType ->
                                  fun ?bytesTransferred ->
                                    fun ?expectedCompletionDate ->
                                      fun ?startBy ->
                                        fun ?backupOptions ->
                                          fun ?backupType ->
                                            fun () ->
                                              {
                                                accountId;
                                                backupJobId;
                                                backupVaultName;
                                                backupVaultArn;
                                                recoveryPointArn;
                                                resourceArn;
                                                creationDate;
                                                completionDate;
                                                state;
                                                statusMessage;
                                                percentDone;
                                                backupSizeInBytes;
                                                iamRoleArn;
                                                createdBy;
                                                resourceType;
                                                bytesTransferred;
                                                expectedCompletionDate;
                                                startBy;
                                                backupOptions;
                                                backupType
                                              }
    let error_of_json name json =
      match name with
      | "DependencyFailureException" ->
          `DependencyFailureException
            (DependencyFailureException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DependencyFailureException" ->
          `DependencyFailureException (DependencyFailureException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DependencyFailureException e ->
          `Assoc
            [("error", (`String "DependencyFailureException"));
            ("details", (DependencyFailureException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AccountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("BackupJobId", (Option.map x.backupJobId ~f:String_.to_value));
        ("BackupVaultName",
          (Option.map x.backupVaultName ~f:BackupVaultName.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("RecoveryPointArn", (Option.map x.recoveryPointArn ~f:ARN.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("CompletionDate",
          (Option.map x.completionDate ~f:Timestamp.to_value));
        ("State", (Option.map x.state ~f:BackupJobState.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("PercentDone", (Option.map x.percentDone ~f:String_.to_value));
        ("BackupSizeInBytes",
          (Option.map x.backupSizeInBytes ~f:Long.to_value));
        ("IamRoleArn", (Option.map x.iamRoleArn ~f:IAMRoleArn.to_value));
        ("CreatedBy",
          (Option.map x.createdBy ~f:RecoveryPointCreator.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("BytesTransferred",
          (Option.map x.bytesTransferred ~f:Long.to_value));
        ("ExpectedCompletionDate",
          (Option.map x.expectedCompletionDate ~f:Timestamp.to_value));
        ("StartBy", (Option.map x.startBy ~f:Timestamp.to_value));
        ("BackupOptions",
          (Option.map x.backupOptions ~f:BackupOptions.to_value));
        ("BackupType", (Option.map x.backupType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupType") in
      let backupOptions =
        (Option.map ~f:BackupOptions.of_xml)
          (Xml.child xml_arg0 "BackupOptions") in
      let startBy =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartBy") in
      let expectedCompletionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ExpectedCompletionDate") in
      let bytesTransferred =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BytesTransferred") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let createdBy =
        (Option.map ~f:RecoveryPointCreator.of_xml)
          (Xml.child xml_arg0 "CreatedBy") in
      let iamRoleArn =
        (Option.map ~f:IAMRoleArn.of_xml) (Xml.child xml_arg0 "IamRoleArn") in
      let backupSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "BackupSizeInBytes") in
      let percentDone =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PercentDone") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let state =
        (Option.map ~f:BackupJobState.of_xml) (Xml.child xml_arg0 "State") in
      let completionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "CompletionDate") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let resourceArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let recoveryPointArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "RecoveryPointArn") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      let backupJobId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupJobId") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "AccountId") in
      make ?backupType ?backupOptions ?startBy ?expectedCompletionDate
        ?bytesTransferred ?resourceType ?createdBy ?iamRoleArn
        ?backupSizeInBytes ?percentDone ?statusMessage ?state ?completionDate
        ?creationDate ?resourceArn ?recoveryPointArn ?backupVaultArn
        ?backupVaultName ?backupJobId ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupType = field_map json "BackupType" String_.of_json in
      let backupOptions =
        field_map json "BackupOptions" BackupOptions.of_json in
      let startBy = field_map json "StartBy" Timestamp.of_json in
      let expectedCompletionDate =
        field_map json "ExpectedCompletionDate" Timestamp.of_json in
      let bytesTransferred = field_map json "BytesTransferred" Long.of_json in
      let resourceType = field_map json "ResourceType" ResourceType.of_json in
      let createdBy = field_map json "CreatedBy" RecoveryPointCreator.of_json in
      let iamRoleArn = field_map json "IamRoleArn" IAMRoleArn.of_json in
      let backupSizeInBytes = field_map json "BackupSizeInBytes" Long.of_json in
      let percentDone = field_map json "PercentDone" String_.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let state = field_map json "State" BackupJobState.of_json in
      let completionDate = field_map json "CompletionDate" Timestamp.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let resourceArn = field_map json "ResourceArn" ARN.of_json in
      let recoveryPointArn = field_map json "RecoveryPointArn" ARN.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      let backupJobId = field_map json "BackupJobId" String_.of_json in
      let accountId = field_map json "AccountId" AccountId.of_json in
      make ?backupType ?backupOptions ?startBy ?expectedCompletionDate
        ?bytesTransferred ?resourceType ?createdBy ?iamRoleArn
        ?backupSizeInBytes ?percentDone ?statusMessage ?state ?completionDate
        ?creationDate ?resourceArn ?recoveryPointArn ?backupVaultArn
        ?backupVaultName ?backupJobId ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns backup job details for the specified BackupJobId."]
module DescribeBackupJobInput =
  struct
    type nonrec t =
      {
      backupJobId: String_.t
        [@ocaml.doc
          "Uniquely identifies a request to Backup to back up a resource."]}
    let context_ = "DescribeBackupJobInput"
    let make ~backupJobId = fun () -> { backupJobId }
    let to_value x =
      structure_to_value
        [("backupJobId", (Some (String_.to_value x.backupJobId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupJobId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupJobId") in
      make ~backupJobId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupJobId = field_map_exn json "BackupJobId" String_.of_json in
      make ~backupJobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns backup job details for the specified BackupJobId."]
module DeleteReportPlanInput =
  struct
    type nonrec t =
      {
      reportPlanName: ReportPlanName.t
        [@ocaml.doc "The unique name of a report plan."]}
    let context_ = "DeleteReportPlanInput"
    let make ~reportPlanName = fun () -> { reportPlanName }
    let to_value x =
      structure_to_value
        [("reportPlanName",
           (Some (ReportPlanName.to_value x.reportPlanName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reportPlanName =
        ReportPlanName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reportPlanName") in
      make ~reportPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reportPlanName =
        field_map_exn json "ReportPlanName" ReportPlanName.of_json in
      make ~reportPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the report plan specified by a report plan name."]
module DeleteRecoveryPointInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      recoveryPointArn: ARN.t
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a recovery point; for example, arn:aws:backup:us-east-1:123456789012:recovery-point:1EB3B5E7-9EB0-435A-A80B-108B488B0D45."]}
    let context_ = "DeleteRecoveryPointInput"
    let make ~backupVaultName =
      fun ~recoveryPointArn ->
        fun () -> { backupVaultName; recoveryPointArn }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("recoveryPointArn", (Some (ARN.to_value x.recoveryPointArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recoveryPointArn =
        ARN.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recoveryPointArn") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~recoveryPointArn ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recoveryPointArn =
        field_map_exn json "RecoveryPointArn" ARN.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~recoveryPointArn ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the recovery point specified by a recovery point ID. If the recovery point ID belongs to a continuous backup, calling this endpoint deletes the existing continuous backup and stops future continuous backup."]
module DeleteFrameworkInput =
  struct
    type nonrec t =
      {
      frameworkName: FrameworkName.t
        [@ocaml.doc "The unique name of a framework."]}
    let context_ = "DeleteFrameworkInput"
    let make ~frameworkName = fun () -> { frameworkName }
    let to_value x =
      structure_to_value
        [("frameworkName", (Some (FrameworkName.to_value x.frameworkName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let frameworkName =
        FrameworkName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "frameworkName") in
      make ~frameworkName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let frameworkName =
        field_map_exn json "FrameworkName" FrameworkName.of_json in
      make ~frameworkName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the framework specified by a framework name."]
module DeleteBackupVaultNotificationsInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Region where they are created. They consist of lowercase letters, numbers, and hyphens."]}
    let context_ = "DeleteBackupVaultNotificationsInput"
    let make ~backupVaultName = fun () -> { backupVaultName }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes event notifications for the specified backup vault."]
module DeleteBackupVaultLockConfigurationInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of the backup vault from which to delete Backup Vault Lock."]}
    let context_ = "DeleteBackupVaultLockConfigurationInput"
    let make ~backupVaultName = fun () -> { backupVaultName }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes Backup Vault Lock from a backup vault specified by a backup vault name. If the Vault Lock configuration is immutable, then you cannot delete Vault Lock using API operations, and you will receive an InvalidRequestException if you attempt to do so. For more information, see Vault Lock in the Backup Developer Guide."]
module DeleteBackupVaultInput =
  struct
    type nonrec t =
      {
      backupVaultName: String_.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."]}
    let context_ = "DeleteBackupVaultInput"
    let make ~backupVaultName = fun () -> { backupVaultName }
    let to_value x =
      structure_to_value
        [("backupVaultName", (Some (String_.to_value x.backupVaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultName =
        field_map_exn json "BackupVaultName" String_.of_json in
      make ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the backup vault identified by its name. A vault can be deleted only if it is empty."]
module DeleteBackupVaultAccessPolicyInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of lowercase letters, numbers, and hyphens."]}
    let context_ = "DeleteBackupVaultAccessPolicyInput"
    let make ~backupVaultName = fun () -> { backupVaultName }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the policy document that manages permissions on a backup vault."]
module DeleteBackupSelectionInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc "Uniquely identifies a backup plan."];
      selectionId: String_.t
        [@ocaml.doc
          "Uniquely identifies the body of a request to assign a set of resources to a backup plan."]}
    let context_ = "DeleteBackupSelectionInput"
    let make ~backupPlanId =
      fun ~selectionId -> fun () -> { backupPlanId; selectionId }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)));
        ("selectionId", (Some (String_.to_value x.selectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let selectionId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "selectionId") in
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ~selectionId ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let selectionId = field_map_exn json "SelectionId" String_.of_json in
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ~selectionId ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the resource selection associated with a backup plan that is specified by the SelectionId."]
module DeleteBackupPlanOutput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      backupPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup plan; for example, arn:aws:backup:us-east-1:123456789012:plan:8F81F553-3A74-4A3F-B93D-B3360DC80C50."];
      deletionDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup plan is deleted, in Unix format and Coordinated Universal Time (UTC). The value of DeletionDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      versionId: String_.t option
        [@ocaml.doc
          "Unique, randomly generated, Unicode, UTF-8 encoded strings that are at most 1,024 bytes long. Version IDs cannot be edited."]}
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupPlanId =
      fun ?backupPlanArn ->
        fun ?deletionDate ->
          fun ?versionId ->
            fun () ->
              { backupPlanId; backupPlanArn; deletionDate; versionId }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("BackupPlanArn", (Option.map x.backupPlanArn ~f:ARN.to_value));
        ("DeletionDate", (Option.map x.deletionDate ~f:Timestamp.to_value));
        ("VersionId", (Option.map x.versionId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let versionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VersionId") in
      let deletionDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "DeletionDate") in
      let backupPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupPlanArn") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      make ?versionId ?deletionDate ?backupPlanArn ?backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let versionId = field_map json "VersionId" String_.of_json in
      let deletionDate = field_map json "DeletionDate" Timestamp.of_json in
      let backupPlanArn = field_map json "BackupPlanArn" ARN.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      make ?versionId ?deletionDate ?backupPlanArn ?backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a backup plan. A backup plan can only be deleted after all associated selections of resources have been deleted. Deleting a backup plan deletes the current version of a backup plan. Previous versions, if any, will still exist."]
module DeleteBackupPlanInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc "Uniquely identifies a backup plan."]}
    let context_ = "DeleteBackupPlanInput"
    let make ~backupPlanId = fun () -> { backupPlanId }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a backup plan. A backup plan can only be deleted after all associated selections of resources have been deleted. Deleting a backup plan deletes the current version of a backup plan. Previous versions, if any, will still exist."]
module CreateReportPlanOutput =
  struct
    type nonrec t =
      {
      reportPlanName: ReportPlanName.t option
        [@ocaml.doc "The unique name of the report plan."];
      reportPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."];
      creationTime: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup vault is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationTime is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?reportPlanName =
      fun ?reportPlanArn ->
        fun ?creationTime ->
          fun () -> { reportPlanName; reportPlanArn; creationTime }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReportPlanName",
           (Option.map x.reportPlanName ~f:ReportPlanName.to_value));
        ("ReportPlanArn", (Option.map x.reportPlanArn ~f:ARN.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let reportPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "ReportPlanArn") in
      let reportPlanName =
        (Option.map ~f:ReportPlanName.of_xml)
          (Xml.child xml_arg0 "ReportPlanName") in
      make ?creationTime ?reportPlanArn ?reportPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creationTime = field_map json "CreationTime" Timestamp.of_json in
      let reportPlanArn = field_map json "ReportPlanArn" ARN.of_json in
      let reportPlanName =
        field_map json "ReportPlanName" ReportPlanName.of_json in
      make ?creationTime ?reportPlanArn ?reportPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a report plan. A report plan is a document that contains information about the contents of the report and where Backup will deliver it. If you call CreateReportPlan with a plan that already exists, you receive an AlreadyExistsException exception."]
module CreateReportPlanInput =
  struct
    type nonrec t =
      {
      reportPlanName: ReportPlanName.t
        [@ocaml.doc
          "The unique name of the report plan. The name must be between 1 and 256 characters, starting with a letter, and consisting of letters (a-z, A-Z), numbers (0-9), and underscores (_)."];
      reportPlanDescription: ReportPlanDescription.t option
        [@ocaml.doc
          "An optional description of the report plan with a maximum of 1,024 characters."];
      reportDeliveryChannel: ReportDeliveryChannel.t
        [@ocaml.doc
          "A structure that contains information about where and how to deliver your reports, specifically your Amazon S3 bucket name, S3 key prefix, and the formats of your reports."];
      reportSetting: ReportSetting.t
        [@ocaml.doc
          "Identifies the report template for the report. Reports are built using a report template. The report templates are: RESOURCE_COMPLIANCE_REPORT | CONTROL_COMPLIANCE_REPORT | BACKUP_JOB_REPORT | COPY_JOB_REPORT | RESTORE_JOB_REPORT If the report template is RESOURCE_COMPLIANCE_REPORT or CONTROL_COMPLIANCE_REPORT, this API resource also describes the report coverage by Amazon Web Services Regions and frameworks."];
      reportPlanTags: StringMap.t option
        [@ocaml.doc
          "Metadata that you can assign to help organize the report plans that you create. Each tag is a key-value pair."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to CreateReportPlanInput. Retrying a successful request with the same idempotency token results in a success message with no action taken."]}
    let context_ = "CreateReportPlanInput"
    let make ?reportPlanDescription =
      fun ?reportPlanTags ->
        fun ?idempotencyToken ->
          fun ~reportPlanName ->
            fun ~reportDeliveryChannel ->
              fun ~reportSetting ->
                fun () ->
                  {
                    reportPlanDescription;
                    reportPlanTags;
                    idempotencyToken;
                    reportPlanName;
                    reportDeliveryChannel;
                    reportSetting
                  }
    let to_value x =
      structure_to_value
        [("ReportPlanName",
           (Some (ReportPlanName.to_value x.reportPlanName)));
        ("ReportPlanDescription",
          (Option.map x.reportPlanDescription
             ~f:ReportPlanDescription.to_value));
        ("ReportDeliveryChannel",
          (Some (ReportDeliveryChannel.to_value x.reportDeliveryChannel)));
        ("ReportSetting", (Some (ReportSetting.to_value x.reportSetting)));
        ("ReportPlanTags",
          (Option.map x.reportPlanTags ~f:StringMap.to_value));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let reportPlanTags =
        (Option.map ~f:StringMap.of_xml)
          (Xml.child xml_arg0 "ReportPlanTags") in
      let reportSetting =
        ReportSetting.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReportSetting") in
      let reportDeliveryChannel =
        ReportDeliveryChannel.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReportDeliveryChannel") in
      let reportPlanDescription =
        (Option.map ~f:ReportPlanDescription.of_xml)
          (Xml.child xml_arg0 "ReportPlanDescription") in
      let reportPlanName =
        ReportPlanName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReportPlanName") in
      make ?idempotencyToken ?reportPlanTags ~reportSetting
        ~reportDeliveryChannel ?reportPlanDescription ~reportPlanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let reportPlanTags = field_map json "ReportPlanTags" StringMap.of_json in
      let reportSetting =
        field_map_exn json "ReportSetting" ReportSetting.of_json in
      let reportDeliveryChannel =
        field_map_exn json "ReportDeliveryChannel"
          ReportDeliveryChannel.of_json in
      let reportPlanDescription =
        field_map json "ReportPlanDescription" ReportPlanDescription.of_json in
      let reportPlanName =
        field_map_exn json "ReportPlanName" ReportPlanName.of_json in
      make ?idempotencyToken ?reportPlanTags ~reportSetting
        ~reportDeliveryChannel ?reportPlanDescription ~reportPlanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a report plan. A report plan is a document that contains information about the contents of the report and where Backup will deliver it. If you call CreateReportPlan with a plan that already exists, you receive an AlreadyExistsException exception."]
module CreateFrameworkOutput =
  struct
    type nonrec t =
      {
      frameworkName: FrameworkName.t option
        [@ocaml.doc
          "The unique name of the framework. The name must be between 1 and 256 characters, starting with a letter, and consisting of letters (a-z, A-Z), numbers (0-9), and underscores (_)."];
      frameworkArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a resource. The format of the ARN depends on the resource type."]}
    type nonrec error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?frameworkName =
      fun ?frameworkArn -> fun () -> { frameworkName; frameworkArn }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("FrameworkName",
           (Option.map x.frameworkName ~f:FrameworkName.to_value));
        ("FrameworkArn", (Option.map x.frameworkArn ~f:ARN.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let frameworkArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "FrameworkArn") in
      let frameworkName =
        (Option.map ~f:FrameworkName.of_xml)
          (Xml.child xml_arg0 "FrameworkName") in
      make ?frameworkArn ?frameworkName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let frameworkArn = field_map json "FrameworkArn" ARN.of_json in
      let frameworkName =
        field_map json "FrameworkName" FrameworkName.of_json in
      make ?frameworkArn ?frameworkName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a framework with one or more controls. A framework is a collection of controls that you can use to evaluate your backup practices. By using pre-built customizable controls to define your policies, you can evaluate whether your backup practices comply with your policies and which resources are not yet in compliance."]
module CreateFrameworkInput =
  struct
    type nonrec t =
      {
      frameworkName: FrameworkName.t
        [@ocaml.doc
          "The unique name of the framework. The name must be between 1 and 256 characters, starting with a letter, and consisting of letters (a-z, A-Z), numbers (0-9), and underscores (_)."];
      frameworkDescription: FrameworkDescription.t option
        [@ocaml.doc
          "An optional description of the framework with a maximum of 1,024 characters."];
      frameworkControls: FrameworkControls.t
        [@ocaml.doc
          "A list of the controls that make up the framework. Each control in the list has a name, input parameters, and scope."];
      idempotencyToken: String_.t option
        [@ocaml.doc
          "A customer-chosen string that you can use to distinguish between otherwise identical calls to CreateFrameworkInput. Retrying a successful request with the same idempotency token results in a success message with no action taken."];
      frameworkTags: StringMap.t option
        [@ocaml.doc
          "Metadata that you can assign to help organize the frameworks that you create. Each tag is a key-value pair."]}
    let context_ = "CreateFrameworkInput"
    let make ?frameworkDescription =
      fun ?idempotencyToken ->
        fun ?frameworkTags ->
          fun ~frameworkName ->
            fun ~frameworkControls ->
              fun () ->
                {
                  frameworkDescription;
                  idempotencyToken;
                  frameworkTags;
                  frameworkName;
                  frameworkControls
                }
    let to_value x =
      structure_to_value
        [("FrameworkName", (Some (FrameworkName.to_value x.frameworkName)));
        ("FrameworkDescription",
          (Option.map x.frameworkDescription ~f:FrameworkDescription.to_value));
        ("FrameworkControls",
          (Some (FrameworkControls.to_value x.frameworkControls)));
        ("IdempotencyToken",
          (Option.map x.idempotencyToken ~f:String_.to_value));
        ("FrameworkTags", (Option.map x.frameworkTags ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let frameworkTags =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "FrameworkTags") in
      let idempotencyToken =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IdempotencyToken") in
      let frameworkControls =
        FrameworkControls.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FrameworkControls") in
      let frameworkDescription =
        (Option.map ~f:FrameworkDescription.of_xml)
          (Xml.child xml_arg0 "FrameworkDescription") in
      let frameworkName =
        FrameworkName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "FrameworkName") in
      make ?frameworkTags ?idempotencyToken ~frameworkControls
        ?frameworkDescription ~frameworkName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let frameworkTags = field_map json "FrameworkTags" StringMap.of_json in
      let idempotencyToken =
        field_map json "IdempotencyToken" String_.of_json in
      let frameworkControls =
        field_map_exn json "FrameworkControls" FrameworkControls.of_json in
      let frameworkDescription =
        field_map json "FrameworkDescription" FrameworkDescription.of_json in
      let frameworkName =
        field_map_exn json "FrameworkName" FrameworkName.of_json in
      make ?frameworkTags ?idempotencyToken ~frameworkControls
        ?frameworkDescription ~frameworkName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a framework with one or more controls. A framework is a collection of controls that you can use to evaluate your backup practices. By using pre-built customizable controls to define your policies, you can evaluate whether your backup practices comply with your policies and which resources are not yet in compliance."]
module CreateBackupVaultOutput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t option
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Region where they are created. They consist of lowercase letters, numbers, and hyphens."];
      backupVaultArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup vault; for example, arn:aws:backup:us-east-1:123456789012:vault:aBackupVault."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup vault is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupVaultName =
      fun ?backupVaultArn ->
        fun ?creationDate ->
          fun () -> { backupVaultName; backupVaultArn; creationDate }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupVaultName",
           (Option.map x.backupVaultName ~f:BackupVaultName.to_value));
        ("BackupVaultArn", (Option.map x.backupVaultArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let backupVaultArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupVaultArn") in
      let backupVaultName =
        (Option.map ~f:BackupVaultName.of_xml)
          (Xml.child xml_arg0 "BackupVaultName") in
      make ?creationDate ?backupVaultArn ?backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let backupVaultArn = field_map json "BackupVaultArn" ARN.of_json in
      let backupVaultName =
        field_map json "BackupVaultName" BackupVaultName.of_json in
      make ?creationDate ?backupVaultArn ?backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a logical container where backups are stored. A CreateBackupVault request includes a name, optionally one or more resource tags, an encryption key, and a request ID. Do not include sensitive data, such as passport numbers, in the name of a backup vault."]
module CreateBackupVaultInput =
  struct
    type nonrec t =
      {
      backupVaultName: BackupVaultName.t
        [@ocaml.doc
          "The name of a logical container where backups are stored. Backup vaults are identified by names that are unique to the account used to create them and the Amazon Web Services Region where they are created. They consist of letters, numbers, and hyphens."];
      backupVaultTags: Tags.t option
        [@ocaml.doc
          "Metadata that you can assign to help organize the resources that you create. Each tag is a key-value pair."];
      encryptionKeyArn: ARN.t option
        [@ocaml.doc
          "The server-side encryption key that is used to protect your backups; for example, arn:aws:kms:us-west-2:111122223333:key/1234abcd-12ab-34cd-56ef-1234567890ab."];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "A unique string that identifies the request and allows failed requests to be retried without the risk of running the operation twice. This parameter is optional. If used, this parameter must contain 1 to 50 alphanumeric or '-_.' characters."]}
    let context_ = "CreateBackupVaultInput"
    let make ?backupVaultTags =
      fun ?encryptionKeyArn ->
        fun ?creatorRequestId ->
          fun ~backupVaultName ->
            fun () ->
              {
                backupVaultTags;
                encryptionKeyArn;
                creatorRequestId;
                backupVaultName
              }
    let to_value x =
      structure_to_value
        [("backupVaultName",
           (Some (BackupVaultName.to_value x.backupVaultName)));
        ("BackupVaultTags", (Option.map x.backupVaultTags ~f:Tags.to_value));
        ("EncryptionKeyArn", (Option.map x.encryptionKeyArn ~f:ARN.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let encryptionKeyArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "EncryptionKeyArn") in
      let backupVaultTags =
        (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "BackupVaultTags") in
      let backupVaultName =
        BackupVaultName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupVaultName") in
      make ?creatorRequestId ?encryptionKeyArn ?backupVaultTags
        ~backupVaultName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let encryptionKeyArn = field_map json "EncryptionKeyArn" ARN.of_json in
      let backupVaultTags = field_map json "BackupVaultTags" Tags.of_json in
      let backupVaultName =
        field_map_exn json "BackupVaultName" BackupVaultName.of_json in
      make ?creatorRequestId ?encryptionKeyArn ?backupVaultTags
        ~backupVaultName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a logical container where backups are stored. A CreateBackupVault request includes a name, optionally one or more resource tags, an encryption key, and a request ID. Do not include sensitive data, such as passport numbers, in the name of a backup vault."]
module CreateBackupSelectionOutput =
  struct
    type nonrec t =
      {
      selectionId: String_.t option
        [@ocaml.doc
          "Uniquely identifies the body of a request to assign a set of resources to a backup plan."];
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time a backup selection is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."]}
    type nonrec error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?selectionId =
      fun ?backupPlanId ->
        fun ?creationDate ->
          fun () -> { selectionId; backupPlanId; creationDate }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SelectionId", (Option.map x.selectionId ~f:String_.to_value));
        ("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      let selectionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SelectionId") in
      make ?creationDate ?backupPlanId ?selectionId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      let selectionId = field_map json "SelectionId" String_.of_json in
      make ?creationDate ?backupPlanId ?selectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a JSON document that specifies a set of resources to assign to a backup plan. For examples, see Assigning resources programmatically."]
module CreateBackupSelectionInput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t
        [@ocaml.doc
          "Uniquely identifies the backup plan to be associated with the selection of resources."];
      backupSelection: BackupSelection.t
        [@ocaml.doc
          "Specifies the body of a request to assign a set of resources to a backup plan."];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "A unique string that identifies the request and allows failed requests to be retried without the risk of running the operation twice. This parameter is optional. If used, this parameter must contain 1 to 50 alphanumeric or '-_.' characters."]}
    let context_ = "CreateBackupSelectionInput"
    let make ?creatorRequestId =
      fun ~backupPlanId ->
        fun ~backupSelection ->
          fun () -> { creatorRequestId; backupPlanId; backupSelection }
    let to_value x =
      structure_to_value
        [("backupPlanId", (Some (String_.to_value x.backupPlanId)));
        ("BackupSelection",
          (Some (BackupSelection.to_value x.backupSelection)));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let backupSelection =
        BackupSelection.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BackupSelection") in
      let backupPlanId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "backupPlanId") in
      make ?creatorRequestId ~backupSelection ~backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let backupSelection =
        field_map_exn json "BackupSelection" BackupSelection.of_json in
      let backupPlanId = field_map_exn json "BackupPlanId" String_.of_json in
      make ?creatorRequestId ~backupSelection ~backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a JSON document that specifies a set of resources to assign to a backup plan. For examples, see Assigning resources programmatically."]
module CreateBackupPlanOutput =
  struct
    type nonrec t =
      {
      backupPlanId: String_.t option
        [@ocaml.doc "Uniquely identifies a backup plan."];
      backupPlanArn: ARN.t option
        [@ocaml.doc
          "An Amazon Resource Name (ARN) that uniquely identifies a backup plan; for example, arn:aws:backup:us-east-1:123456789012:plan:8F81F553-3A74-4A3F-B93D-B3360DC80C50."];
      creationDate: Timestamp.t option
        [@ocaml.doc
          "The date and time that a backup plan is created, in Unix format and Coordinated Universal Time (UTC). The value of CreationDate is accurate to milliseconds. For example, the value 1516925490.087 represents Friday, January 26, 2018 12:11:30.087 AM."];
      versionId: String_.t option
        [@ocaml.doc
          "Unique, randomly generated, Unicode, UTF-8 encoded strings that are at most 1,024 bytes long. They cannot be edited."];
      advancedBackupSettings: AdvancedBackupSettings.t option
        [@ocaml.doc
          "A list of BackupOptions settings for a resource type. This option is only available for Windows Volume Shadow Copy Service (VSS) backup jobs."]}
    type nonrec error =
      [ `AlreadyExistsException of AlreadyExistsException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backupPlanId =
      fun ?backupPlanArn ->
        fun ?creationDate ->
          fun ?versionId ->
            fun ?advancedBackupSettings ->
              fun () ->
                {
                  backupPlanId;
                  backupPlanArn;
                  creationDate;
                  versionId;
                  advancedBackupSettings
                }
    let error_of_json name json =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AlreadyExistsException" ->
          `AlreadyExistsException (AlreadyExistsException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AlreadyExistsException e ->
          `Assoc
            [("error", (`String "AlreadyExistsException"));
            ("details", (AlreadyExistsException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BackupPlanId", (Option.map x.backupPlanId ~f:String_.to_value));
        ("BackupPlanArn", (Option.map x.backupPlanArn ~f:ARN.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:Timestamp.to_value));
        ("VersionId", (Option.map x.versionId ~f:String_.to_value));
        ("AdvancedBackupSettings",
          (Option.map x.advancedBackupSettings
             ~f:AdvancedBackupSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let advancedBackupSettings =
        (Option.map ~f:AdvancedBackupSettings.of_xml)
          (Xml.child xml_arg0 "AdvancedBackupSettings") in
      let versionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VersionId") in
      let creationDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let backupPlanArn =
        (Option.map ~f:ARN.of_xml) (Xml.child xml_arg0 "BackupPlanArn") in
      let backupPlanId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupPlanId") in
      make ?advancedBackupSettings ?versionId ?creationDate ?backupPlanArn
        ?backupPlanId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let advancedBackupSettings =
        field_map json "AdvancedBackupSettings"
          AdvancedBackupSettings.of_json in
      let versionId = field_map json "VersionId" String_.of_json in
      let creationDate = field_map json "CreationDate" Timestamp.of_json in
      let backupPlanArn = field_map json "BackupPlanArn" ARN.of_json in
      let backupPlanId = field_map json "BackupPlanId" String_.of_json in
      make ?advancedBackupSettings ?versionId ?creationDate ?backupPlanArn
        ?backupPlanId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a backup plan using a backup plan name and backup rules. A backup plan is a document that contains information that Backup uses to schedule tasks that create recovery points for resources. If you call CreateBackupPlan with a plan that already exists, you receive an AlreadyExistsException exception."]
module CreateBackupPlanInput =
  struct
    type nonrec t =
      {
      backupPlan: BackupPlanInput.t
        [@ocaml.doc
          "Specifies the body of a backup plan. Includes a BackupPlanName and one or more sets of Rules."];
      backupPlanTags: Tags.t option
        [@ocaml.doc
          "To help organize your resources, you can assign your own metadata to the resources that you create. Each tag is a key-value pair. The specified tags are assigned to all backups created with this plan."];
      creatorRequestId: String_.t option
        [@ocaml.doc
          "Identifies the request and allows failed requests to be retried without the risk of running the operation twice. If the request includes a CreatorRequestId that matches an existing backup plan, that plan is returned. This parameter is optional. If used, this parameter must contain 1 to 50 alphanumeric or '-_.' characters."]}
    let context_ = "CreateBackupPlanInput"
    let make ?backupPlanTags =
      fun ?creatorRequestId ->
        fun ~backupPlan ->
          fun () -> { backupPlanTags; creatorRequestId; backupPlan }
    let to_value x =
      structure_to_value
        [("BackupPlan", (Some (BackupPlanInput.to_value x.backupPlan)));
        ("BackupPlanTags", (Option.map x.backupPlanTags ~f:Tags.to_value));
        ("CreatorRequestId",
          (Option.map x.creatorRequestId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creatorRequestId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CreatorRequestId") in
      let backupPlanTags =
        (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "BackupPlanTags") in
      let backupPlan =
        BackupPlanInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BackupPlan") in
      make ?creatorRequestId ?backupPlanTags ~backupPlan ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let creatorRequestId =
        field_map json "CreatorRequestId" String_.of_json in
      let backupPlanTags = field_map json "BackupPlanTags" Tags.of_json in
      let backupPlan =
        field_map_exn json "BackupPlan" BackupPlanInput.of_json in
      make ?creatorRequestId ?backupPlanTags ~backupPlan ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a backup plan using a backup plan name and backup rules. A backup plan is a document that contains information that Backup uses to schedule tasks that create recovery points for resources. If you call CreateBackupPlan with a plan that already exists, you receive an AlreadyExistsException exception."]
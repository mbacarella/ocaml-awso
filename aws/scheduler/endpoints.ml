(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateSchedule: (CreateScheduleInput.t, CreateScheduleOutput.t,
  CreateScheduleOutput.error) t 
  | CreateScheduleGroup: (CreateScheduleGroupInput.t,
  CreateScheduleGroupOutput.t, CreateScheduleGroupOutput.error) t 
  | DeleteSchedule: (DeleteScheduleInput.t, DeleteScheduleOutput.t,
  DeleteScheduleOutput.error) t 
  | DeleteScheduleGroup: (DeleteScheduleGroupInput.t,
  DeleteScheduleGroupOutput.t, DeleteScheduleGroupOutput.error) t 
  | GetSchedule: (GetScheduleInput.t, GetScheduleOutput.t,
  GetScheduleOutput.error) t 
  | GetScheduleGroup: (GetScheduleGroupInput.t, GetScheduleGroupOutput.t,
  GetScheduleGroupOutput.error) t 
  | ListScheduleGroups: (ListScheduleGroupsInput.t,
  ListScheduleGroupsOutput.t, ListScheduleGroupsOutput.error) t 
  | ListSchedules: (ListSchedulesInput.t, ListSchedulesOutput.t,
  ListSchedulesOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
  | UpdateSchedule: (UpdateScheduleInput.t, UpdateScheduleOutput.t,
  UpdateScheduleOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateSchedule -> `POST
  | CreateScheduleGroup -> `POST
  | DeleteSchedule -> `DELETE
  | DeleteScheduleGroup -> `DELETE
  | GetSchedule -> `GET
  | GetScheduleGroup -> `GET
  | ListScheduleGroups -> `GET
  | ListSchedules -> `GET
  | ListTagsForResource -> `GET
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateSchedule -> `PUT
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateSchedule ->
          (Format.kasprintf Uri.of_string) "/schedules/%s"
            (Name.to_header x.CreateScheduleInput.name)
      | CreateScheduleGroup ->
          (Format.kasprintf Uri.of_string) "/schedule-groups/%s"
            (ScheduleGroupName.to_header x.CreateScheduleGroupInput.name)
      | DeleteSchedule ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/schedules/%s"
               (Name.to_header x.DeleteScheduleInput.name))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("clientToken", (ClientToken.to_header v)))
                  x.clientToken;
               Option.map
                 ~f:(fun v -> ("groupName", (ScheduleGroupName.to_header v)))
                 x.groupName])
      | DeleteScheduleGroup ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/schedule-groups/%s"
               (ScheduleGroupName.to_header x.DeleteScheduleGroupInput.name))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("clientToken", (ClientToken.to_header v)))
                  x.clientToken])
      | GetSchedule ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/schedules/%s"
               (Name.to_header x.GetScheduleInput.name))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("groupName", (ScheduleGroupName.to_header v)))
                  x.groupName])
      | GetScheduleGroup ->
          (Format.kasprintf Uri.of_string) "/schedule-groups/%s"
            (ScheduleGroupName.to_header x.GetScheduleGroupInput.name)
      | ListScheduleGroups ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/schedule-groups")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("MaxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v ->
                       ("NamePrefix", (ScheduleGroupNamePrefix.to_header v)))
                 x.namePrefix;
               Option.map
                 ~f:(fun v -> ("NextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListSchedules ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/schedules")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("ScheduleGroup", (ScheduleGroupName.to_header v)))
                  x.groupName;
               Option.map
                 ~f:(fun v -> ("MaxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v -> ("NamePrefix", (NamePrefix.to_header v)))
                 x.namePrefix;
               Option.map
                 ~f:(fun v -> ("NextToken", (NextToken.to_header v)))
                 x.nextToken;
               Option.map
                 ~f:(fun v -> ("State", (ScheduleState.to_header v))) 
                 x.state])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (TagResourceArn.to_header x.ListTagsForResourceInput.resourceArn)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (TagResourceArn.to_header x.TagResourceInput.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (TagResourceArn.to_header x.UntagResourceInput.resourceArn))
            (List.filter_opt
               [Some ("TagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateSchedule ->
          (Format.kasprintf Uri.of_string) "/schedules/%s"
            (Name.to_header x.UpdateScheduleInput.name))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateSchedule ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.CreateScheduleInput.actionAfterCompletion
                         ~f:(fun x ->
                               ("ActionAfterCompletion",
                                 (ActionAfterCompletion.to_value x)));
                      Option.map req.CreateScheduleInput.clientToken
                        ~f:(fun x ->
                              ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.CreateScheduleInput.description
                        ~f:(fun x ->
                              ("Description", (Description.to_value x)));
                      Option.map req.CreateScheduleInput.endDate
                        ~f:(fun x -> ("EndDate", (EndDate.to_value x)));
                      Some
                        ("FlexibleTimeWindow",
                          (FlexibleTimeWindow.to_value
                             req.CreateScheduleInput.flexibleTimeWindow));
                      Option.map req.CreateScheduleInput.groupName
                        ~f:(fun x ->
                              ("GroupName", (ScheduleGroupName.to_value x)));
                      Option.map req.CreateScheduleInput.kmsKeyArn
                        ~f:(fun x -> ("KmsKeyArn", (KmsKeyArn.to_value x)));
                      Some
                        ("ScheduleExpression",
                          (ScheduleExpression.to_value
                             req.CreateScheduleInput.scheduleExpression));
                      Option.map
                        req.CreateScheduleInput.scheduleExpressionTimezone
                        ~f:(fun x ->
                              ("ScheduleExpressionTimezone",
                                (ScheduleExpressionTimezone.to_value x)));
                      Option.map req.CreateScheduleInput.startDate
                        ~f:(fun x -> ("StartDate", (StartDate.to_value x)));
                      Option.map req.CreateScheduleInput.state
                        ~f:(fun x -> ("State", (ScheduleState.to_value x)));
                      Some
                        ("Target",
                          (Target.to_value req.CreateScheduleInput.target))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateScheduleGroup ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateScheduleGroupInput.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.CreateScheduleGroupInput.tags
                        ~f:(fun x -> ("Tags", (TagList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteSchedule -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteScheduleGroup -> Awso.Http.Request.make (method_of_endpoint endp)
  | GetSchedule ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetScheduleGroup ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListScheduleGroups ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSchedules ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Tags",
                           (TagList.to_value req.TagResourceInput.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateSchedule -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateSchedule ->
      if is_success
      then Ok (CreateScheduleOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateScheduleOutput.error_of_json))
  | CreateScheduleGroup ->
      if is_success
      then Ok (CreateScheduleGroupOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some CreateScheduleGroupOutput.error_of_json))
  | DeleteSchedule ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteScheduleOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteScheduleOutput.error_of_json))
  | DeleteScheduleGroup ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteScheduleGroupOutput.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some DeleteScheduleGroupOutput.error_of_json))
  | GetSchedule ->
      if is_success
      then Ok (GetScheduleOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetScheduleOutput.error_of_json))
  | GetScheduleGroup ->
      if is_success
      then Ok (GetScheduleGroupOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetScheduleGroupOutput.error_of_json))
  | ListScheduleGroups ->
      if is_success
      then Ok (ListScheduleGroupsOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListScheduleGroupsOutput.error_of_json))
  | ListSchedules ->
      if is_success
      then Ok (ListSchedulesOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListSchedulesOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
  | UpdateSchedule ->
      if is_success
      then Ok (UpdateScheduleOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateScheduleOutput.error_of_json))
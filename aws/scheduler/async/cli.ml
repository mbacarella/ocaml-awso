(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionAfterCompletion =
         flag "action-after-completion" (optional json_arg)
           ~doc:"JSON ActionAfterCompletion"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and endDate = flag "end-date" (optional json_arg) ~doc:"JSON EndDate"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING ScheduleGroupName"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and scheduleExpressionTimezone =
         flag "schedule-expression-timezone" (optional string)
           ~doc:"STRING ScheduleExpressionTimezone"
       and startDate =
         flag "start-date" (optional json_arg) ~doc:"JSON StartDate"
       and state = flag "state" (optional json_arg) ~doc:"JSON ScheduleState"
       and flexibleTimeWindow =
         flag "flexible-time-window" (required json_arg)
           ~doc:"JSON FlexibleTimeWindow"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and scheduleExpression =
         flag "schedule-expression" (required string)
           ~doc:"STRING ScheduleExpression"
       and target = flag "target" (required json_arg) ~doc:"JSON Target" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_schedule
           (Values.CreateScheduleInput.make
              ?actionAfterCompletion:(Option.map
                                        ~f:Values.ActionAfterCompletion.of_json
                                        actionAfterCompletion) ?clientToken
              ?description
              ?endDate:(Option.map ~f:Values.EndDate.of_json endDate)
              ?groupName ?kmsKeyArn ?scheduleExpressionTimezone
              ?startDate:(Option.map ~f:Values.StartDate.of_json startDate)
              ?state:(Option.map ~f:Values.ScheduleState.of_json state)
              ~flexibleTimeWindow:(Values.FlexibleTimeWindow.of_json
                                     flexibleTimeWindow) ~name
              ~scheduleExpression ~target:(Values.Target.of_json target) ())
           (Some Values.CreateScheduleOutput.to_json)
           (Some Values.CreateScheduleOutput.error_to_json)])
let create_schedule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING ScheduleGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_schedule_group
           (Values.CreateScheduleGroupInput.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateScheduleGroupOutput.to_json)
           (Some Values.CreateScheduleGroupOutput.error_to_json)])
let delete_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING ScheduleGroupName"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schedule
           (Values.DeleteScheduleInput.make ?clientToken ?groupName ~name ())
           (Some Values.DeleteScheduleOutput.to_json)
           (Some Values.DeleteScheduleOutput.error_to_json)])
let delete_schedule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name =
         flag "name" (required string) ~doc:"STRING ScheduleGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_schedule_group
           (Values.DeleteScheduleGroupInput.make ?clientToken ~name ())
           (Some Values.DeleteScheduleGroupOutput.to_json)
           (Some Values.DeleteScheduleGroupOutput.error_to_json)])
let get_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING ScheduleGroupName"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schedule (Values.GetScheduleInput.make ?groupName ~name ())
           (Some Values.GetScheduleOutput.to_json)
           (Some Values.GetScheduleOutput.error_to_json)])
let get_schedule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING ScheduleGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_schedule_group (Values.GetScheduleGroupInput.make ~name ())
           (Some Values.GetScheduleGroupOutput.to_json)
           (Some Values.GetScheduleGroupOutput.error_to_json)])
let list_schedule_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and namePrefix =
         flag "name-prefix" (optional string)
           ~doc:"STRING ScheduleGroupNamePrefix"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schedule_groups
           (Values.ListScheduleGroupsInput.make ?maxResults ?namePrefix
              ?nextToken ()) (Some Values.ListScheduleGroupsOutput.to_json)
           (Some Values.ListScheduleGroupsOutput.error_to_json)])
let list_schedules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING ScheduleGroupName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and namePrefix =
         flag "name-prefix" (optional string) ~doc:"STRING NamePrefix"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and state = flag "state" (optional json_arg) ~doc:"JSON ScheduleState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_schedules
           (Values.ListSchedulesInput.make ?groupName ?maxResults ?namePrefix
              ?nextToken
              ?state:(Option.map ~f:Values.ScheduleState.of_json state) ())
           (Some Values.ListSchedulesOutput.to_json)
           (Some Values.ListSchedulesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionAfterCompletion =
         flag "action-after-completion" (optional json_arg)
           ~doc:"JSON ActionAfterCompletion"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and endDate = flag "end-date" (optional json_arg) ~doc:"JSON EndDate"
       and groupName =
         flag "group-name" (optional string) ~doc:"STRING ScheduleGroupName"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and scheduleExpressionTimezone =
         flag "schedule-expression-timezone" (optional string)
           ~doc:"STRING ScheduleExpressionTimezone"
       and startDate =
         flag "start-date" (optional json_arg) ~doc:"JSON StartDate"
       and state = flag "state" (optional json_arg) ~doc:"JSON ScheduleState"
       and flexibleTimeWindow =
         flag "flexible-time-window" (required json_arg)
           ~doc:"JSON FlexibleTimeWindow"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and scheduleExpression =
         flag "schedule-expression" (required string)
           ~doc:"STRING ScheduleExpression"
       and target = flag "target" (required json_arg) ~doc:"JSON Target" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_schedule
           (Values.UpdateScheduleInput.make
              ?actionAfterCompletion:(Option.map
                                        ~f:Values.ActionAfterCompletion.of_json
                                        actionAfterCompletion) ?clientToken
              ?description
              ?endDate:(Option.map ~f:Values.EndDate.of_json endDate)
              ?groupName ?kmsKeyArn ?scheduleExpressionTimezone
              ?startDate:(Option.map ~f:Values.StartDate.of_json startDate)
              ?state:(Option.map ~f:Values.ScheduleState.of_json state)
              ~flexibleTimeWindow:(Values.FlexibleTimeWindow.of_json
                                     flexibleTimeWindow) ~name
              ~scheduleExpression ~target:(Values.Target.of_json target) ())
           (Some Values.UpdateScheduleOutput.to_json)
           (Some Values.UpdateScheduleOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-schedule", create_schedule);
    ("create-schedule-group", create_schedule_group);
    ("delete-schedule", delete_schedule);
    ("delete-schedule-group", delete_schedule_group);
    ("get-schedule", get_schedule);
    ("get-schedule-group", get_schedule_group);
    ("list-schedule-groups", list_schedule_groups);
    ("list-schedules", list_schedules);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-schedule", update_schedule)]
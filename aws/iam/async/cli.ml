(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_client_i_d_to_open_i_d_connect_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and openIDConnectProviderArn =
         flag "open-i-d-connect-provider-arn" (required string)
           ~doc:"STRING arnType"
       and clientID =
         flag "client-i-d" (required string) ~doc:"STRING clientIDType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_client_i_d_to_open_i_d_connect_provider
           (Values.AddClientIDToOpenIDConnectProviderRequest.make
              ~openIDConnectProviderArn ~clientID ()) None None])
let add_role_to_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceProfileName =
         flag "instance-profile-name" (required string)
           ~doc:"STRING instanceProfileNameType"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_role_to_instance_profile
           (Values.AddRoleToInstanceProfileRequest.make ~instanceProfileName
              ~roleName ()) None None])
let add_user_to_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_user_to_group
           (Values.AddUserToGroupRequest.make ~groupName ~userName ()) None
           None])
let attach_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_group_policy
           (Values.AttachGroupPolicyRequest.make ~groupName ~policyArn ())
           None None])
let attach_role_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_role_policy
           (Values.AttachRolePolicyRequest.make ~roleName ~policyArn ()) None
           None])
let attach_user_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_user_policy
           (Values.AttachUserPolicyRequest.make ~userName ~policyArn ()) None
           None])
let change_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and oldPassword =
         flag "old-password" (required string) ~doc:"STRING passwordType"
       and newPassword =
         flag "new-password" (required string) ~doc:"STRING passwordType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.change_password
           (Values.ChangePasswordRequest.make ~oldPassword ~newPassword ())
           None None])
let create_access_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_key
           (Values.CreateAccessKeyRequest.make ?userName ())
           (Some Values.CreateAccessKeyResponse.to_json)
           (Some Values.CreateAccessKeyResponse.error_to_json)])
let create_account_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountAlias =
         flag "account-alias" (required string)
           ~doc:"STRING accountAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_account_alias
           (Values.CreateAccountAliasRequest.make ~accountAlias ()) None None])
let create_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (optional string) ~doc:"STRING pathType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group
           (Values.CreateGroupRequest.make ?path ~groupName ())
           (Some Values.CreateGroupResponse.to_json)
           (Some Values.CreateGroupResponse.error_to_json)])
let create_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (optional string) ~doc:"STRING pathType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and instanceProfileName =
         flag "instance-profile-name" (required string)
           ~doc:"STRING instanceProfileNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance_profile
           (Values.CreateInstanceProfileRequest.make ?path
              ?tags:(Option.map ~f:Values.TagListType.of_json tags)
              ~instanceProfileName ())
           (Some Values.CreateInstanceProfileResponse.to_json)
           (Some Values.CreateInstanceProfileResponse.error_to_json)])
let create_login_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and passwordResetRequired =
         flag "password-reset-required" (optional bool)
           ~doc:"BOOL booleanType"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and password =
         flag "password" (required string) ~doc:"STRING passwordType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_login_profile
           (Values.CreateLoginProfileRequest.make ?passwordResetRequired
              ~userName ~password ())
           (Some Values.CreateLoginProfileResponse.to_json)
           (Some Values.CreateLoginProfileResponse.error_to_json)])
let create_open_i_d_connect_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientIDList =
         flag "client-i-d-list" (optional json_arg)
           ~doc:"JSON clientIDListType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and url =
         flag "url" (required string)
           ~doc:"STRING OpenIDConnectProviderUrlType"
       and thumbprintList =
         flag "thumbprint-list" (required json_arg)
           ~doc:"JSON thumbprintListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_open_i_d_connect_provider
           (Values.CreateOpenIDConnectProviderRequest.make
              ?clientIDList:(Option.map ~f:Values.ClientIDListType.of_json
                               clientIDList)
              ?tags:(Option.map ~f:Values.TagListType.of_json tags) ~url
              ~thumbprintList:(Values.ThumbprintListType.of_json
                                 thumbprintList) ())
           (Some Values.CreateOpenIDConnectProviderResponse.to_json)
           (Some Values.CreateOpenIDConnectProviderResponse.error_to_json)])
let create_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (optional string) ~doc:"STRING policyPathType"
       and description =
         flag "description" (optional string)
           ~doc:"STRING policyDescriptionType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING policyDocumentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy
           (Values.CreatePolicyRequest.make ?path ?description
              ?tags:(Option.map ~f:Values.TagListType.of_json tags)
              ~policyName ~policyDocument ())
           (Some Values.CreatePolicyResponse.to_json)
           (Some Values.CreatePolicyResponse.error_to_json)])
let create_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and setAsDefault =
         flag "set-as-default" (optional bool) ~doc:"BOOL booleanType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING policyDocumentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy_version
           (Values.CreatePolicyVersionRequest.make ?setAsDefault ~policyArn
              ~policyDocument ())
           (Some Values.CreatePolicyVersionResponse.to_json)
           (Some Values.CreatePolicyVersionResponse.error_to_json)])
let create_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (optional string) ~doc:"STRING pathType"
       and description =
         flag "description" (optional string)
           ~doc:"STRING roleDescriptionType"
       and maxSessionDuration =
         flag "max-session-duration" (optional int)
           ~doc:"INT roleMaxSessionDurationType"
       and permissionsBoundary =
         flag "permissions-boundary" (optional string) ~doc:"STRING arnType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and assumeRolePolicyDocument =
         flag "assume-role-policy-document" (required string)
           ~doc:"STRING policyDocumentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_role
           (Values.CreateRoleRequest.make ?path ?description
              ?maxSessionDuration ?permissionsBoundary
              ?tags:(Option.map ~f:Values.TagListType.of_json tags) ~roleName
              ~assumeRolePolicyDocument ())
           (Some Values.CreateRoleResponse.to_json)
           (Some Values.CreateRoleResponse.error_to_json)])
let create_s_a_m_l_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and sAMLMetadataDocument =
         flag "s-a-m-l-metadata-document" (required string)
           ~doc:"STRING SAMLMetadataDocumentType"
       and name =
         flag "name" (required string) ~doc:"STRING SAMLProviderNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_s_a_m_l_provider
           (Values.CreateSAMLProviderRequest.make
              ?tags:(Option.map ~f:Values.TagListType.of_json tags)
              ~sAMLMetadataDocument ~name ())
           (Some Values.CreateSAMLProviderResponse.to_json)
           (Some Values.CreateSAMLProviderResponse.error_to_json)])
let create_service_linked_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING roleDescriptionType"
       and customSuffix =
         flag "custom-suffix" (optional string)
           ~doc:"STRING customSuffixType"
       and aWSServiceName =
         flag "a-w-s-service-name" (required string)
           ~doc:"STRING groupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_linked_role
           (Values.CreateServiceLinkedRoleRequest.make ?description
              ?customSuffix ~aWSServiceName ())
           (Some Values.CreateServiceLinkedRoleResponse.to_json)
           (Some Values.CreateServiceLinkedRoleResponse.error_to_json)])
let create_service_specific_credential =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING serviceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_specific_credential
           (Values.CreateServiceSpecificCredentialRequest.make ~userName
              ~serviceName ())
           (Some Values.CreateServiceSpecificCredentialResponse.to_json)
           (Some Values.CreateServiceSpecificCredentialResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (optional string) ~doc:"STRING pathType"
       and permissionsBoundary =
         flag "permissions-boundary" (optional string) ~doc:"STRING arnType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?path ?permissionsBoundary
              ?tags:(Option.map ~f:Values.TagListType.of_json tags) ~userName
              ()) (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let create_virtual_m_f_a_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (optional string) ~doc:"STRING pathType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and virtualMFADeviceName =
         flag "virtual-m-f-a-device-name" (required string)
           ~doc:"STRING virtualMFADeviceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_virtual_m_f_a_device
           (Values.CreateVirtualMFADeviceRequest.make ?path
              ?tags:(Option.map ~f:Values.TagListType.of_json tags)
              ~virtualMFADeviceName ())
           (Some Values.CreateVirtualMFADeviceResponse.to_json)
           (Some Values.CreateVirtualMFADeviceResponse.error_to_json)])
let deactivate_m_f_a_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType"
       and serialNumber =
         flag "serial-number" (required string)
           ~doc:"STRING serialNumberType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_m_f_a_device
           (Values.DeactivateMFADeviceRequest.make ~userName ~serialNumber ())
           None None])
let delete_access_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType"
       and accessKeyId =
         flag "access-key-id" (required string) ~doc:"STRING accessKeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_key
           (Values.DeleteAccessKeyRequest.make ?userName ~accessKeyId ())
           None None])
let delete_account_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountAlias =
         flag "account-alias" (required string)
           ~doc:"STRING accountAliasType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_alias
           (Values.DeleteAccountAliasRequest.make ~accountAlias ()) None None])
let delete_account_password_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_password_policy (Fn.id ()) None None])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group (Values.DeleteGroupRequest.make ~groupName ())
           None None])
let delete_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group_policy
           (Values.DeleteGroupPolicyRequest.make ~groupName ~policyName ())
           None None])
let delete_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceProfileName =
         flag "instance-profile-name" (required string)
           ~doc:"STRING instanceProfileNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance_profile
           (Values.DeleteInstanceProfileRequest.make ~instanceProfileName ())
           None None])
let delete_login_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_login_profile
           (Values.DeleteLoginProfileRequest.make ~userName ()) None None])
let delete_open_i_d_connect_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and openIDConnectProviderArn =
         flag "open-i-d-connect-provider-arn" (required string)
           ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_open_i_d_connect_provider
           (Values.DeleteOpenIDConnectProviderRequest.make
              ~openIDConnectProviderArn ()) None None])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy (Values.DeletePolicyRequest.make ~policyArn ())
           None None])
let delete_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING policyVersionIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy_version
           (Values.DeletePolicyVersionRequest.make ~policyArn ~versionId ())
           None None])
let delete_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_role (Values.DeleteRoleRequest.make ~roleName ()) None
           None])
let delete_role_permissions_boundary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_role_permissions_boundary
           (Values.DeleteRolePermissionsBoundaryRequest.make ~roleName ())
           None None])
let delete_role_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_role_policy
           (Values.DeleteRolePolicyRequest.make ~roleName ~policyName ())
           None None])
let delete_s_a_m_l_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sAMLProviderArn =
         flag "s-a-m-l-provider-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_s_a_m_l_provider
           (Values.DeleteSAMLProviderRequest.make ~sAMLProviderArn ()) None
           None])
let delete_s_s_h_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and sSHPublicKeyId =
         flag "s-s-h-public-key-id" (required string)
           ~doc:"STRING publicKeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_s_s_h_public_key
           (Values.DeleteSSHPublicKeyRequest.make ~userName ~sSHPublicKeyId
              ()) None None])
let delete_server_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverCertificateName =
         flag "server-certificate-name" (required string)
           ~doc:"STRING serverCertificateNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_server_certificate
           (Values.DeleteServerCertificateRequest.make ~serverCertificateName
              ()) None None])
let delete_service_linked_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_linked_role
           (Values.DeleteServiceLinkedRoleRequest.make ~roleName ())
           (Some Values.DeleteServiceLinkedRoleResponse.to_json)
           (Some Values.DeleteServiceLinkedRoleResponse.error_to_json)])
let delete_service_specific_credential =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING userNameType"
       and serviceSpecificCredentialId =
         flag "service-specific-credential-id" (required string)
           ~doc:"STRING serviceSpecificCredentialId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_specific_credential
           (Values.DeleteServiceSpecificCredentialRequest.make ?userName
              ~serviceSpecificCredentialId ()) None None])
let delete_signing_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType"
       and certificateId =
         flag "certificate-id" (required string)
           ~doc:"STRING certificateIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_signing_certificate
           (Values.DeleteSigningCertificateRequest.make ?userName
              ~certificateId ()) None None])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user (Values.DeleteUserRequest.make ~userName ()) None
           None])
let delete_user_permissions_boundary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_permissions_boundary
           (Values.DeleteUserPermissionsBoundaryRequest.make ~userName ())
           None None])
let delete_user_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_policy
           (Values.DeleteUserPolicyRequest.make ~userName ~policyName ())
           None None])
let delete_virtual_m_f_a_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serialNumber =
         flag "serial-number" (required string)
           ~doc:"STRING serialNumberType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_virtual_m_f_a_device
           (Values.DeleteVirtualMFADeviceRequest.make ~serialNumber ()) None
           None])
let detach_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_group_policy
           (Values.DetachGroupPolicyRequest.make ~groupName ~policyArn ())
           None None])
let detach_role_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_role_policy
           (Values.DetachRolePolicyRequest.make ~roleName ~policyArn ()) None
           None])
let detach_user_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_user_policy
           (Values.DetachUserPolicyRequest.make ~userName ~policyArn ()) None
           None])
let enable_m_f_a_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType"
       and serialNumber =
         flag "serial-number" (required string)
           ~doc:"STRING serialNumberType"
       and authenticationCode1 =
         flag "authentication-code1" (required string)
           ~doc:"STRING authenticationCodeType"
       and authenticationCode2 =
         flag "authentication-code2" (required string)
           ~doc:"STRING authenticationCodeType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_m_f_a_device
           (Values.EnableMFADeviceRequest.make ~userName ~serialNumber
              ~authenticationCode1 ~authenticationCode2 ()) None None])
let generate_credential_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_credential_report (Fn.id ())
           (Some Values.GenerateCredentialReportResponse.to_json)
           (Some Values.GenerateCredentialReportResponse.error_to_json)])
let generate_organizations_access_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationsPolicyId =
         flag "organizations-policy-id" (optional string)
           ~doc:"STRING organizationsPolicyIdType"
       and entityPath =
         flag "entity-path" (required string)
           ~doc:"STRING organizationsEntityPathType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_organizations_access_report
           (Values.GenerateOrganizationsAccessReportRequest.make
              ?organizationsPolicyId ~entityPath ())
           (Some Values.GenerateOrganizationsAccessReportResponse.to_json)
           (Some
              Values.GenerateOrganizationsAccessReportResponse.error_to_json)])
let generate_service_last_accessed_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and granularity =
         flag "granularity" (optional json_arg)
           ~doc:"JSON AccessAdvisorUsageGranularityType"
       and arn = flag "arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_service_last_accessed_details
           (Values.GenerateServiceLastAccessedDetailsRequest.make
              ?granularity:(Option.map
                              ~f:Values.AccessAdvisorUsageGranularityType.of_json
                              granularity) ~arn ())
           (Some Values.GenerateServiceLastAccessedDetailsResponse.to_json)
           (Some
              Values.GenerateServiceLastAccessedDetailsResponse.error_to_json)])
let get_access_key_last_used =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessKeyId =
         flag "access-key-id" (required string) ~doc:"STRING accessKeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_key_last_used
           (Values.GetAccessKeyLastUsedRequest.make ~accessKeyId ())
           (Some Values.GetAccessKeyLastUsedResponse.to_json)
           (Some Values.GetAccessKeyLastUsedResponse.error_to_json)])
let get_account_authorization_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON entityListType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_authorization_details
           (Values.GetAccountAuthorizationDetailsRequest.make
              ?filter:(Option.map ~f:Values.EntityListType.of_json filter)
              ?maxItems ?marker ())
           (Some Values.GetAccountAuthorizationDetailsResponse.to_json)
           (Some Values.GetAccountAuthorizationDetailsResponse.error_to_json)])
let get_account_password_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_password_policy (Fn.id ())
           (Some Values.GetAccountPasswordPolicyResponse.to_json)
           (Some Values.GetAccountPasswordPolicyResponse.error_to_json)])
let get_account_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_summary (Fn.id ())
           (Some Values.GetAccountSummaryResponse.to_json)
           (Some Values.GetAccountSummaryResponse.error_to_json)])
let get_context_keys_for_custom_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyInputList =
         flag "policy-input-list" (required json_arg)
           ~doc:"JSON SimulationPolicyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_context_keys_for_custom_policy
           (Values.GetContextKeysForCustomPolicyRequest.make
              ~policyInputList:(Values.SimulationPolicyListType.of_json
                                  policyInputList) ())
           (Some Values.GetContextKeysForPolicyResponse.to_json)
           (Some Values.GetContextKeysForPolicyResponse.error_to_json)])
let get_context_keys_for_principal_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyInputList =
         flag "policy-input-list" (optional json_arg)
           ~doc:"JSON SimulationPolicyListType"
       and policySourceArn =
         flag "policy-source-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_context_keys_for_principal_policy
           (Values.GetContextKeysForPrincipalPolicyRequest.make
              ?policyInputList:(Option.map
                                  ~f:Values.SimulationPolicyListType.of_json
                                  policyInputList) ~policySourceArn ())
           (Some Values.GetContextKeysForPolicyResponse.to_json)
           (Some Values.GetContextKeysForPolicyResponse.error_to_json)])
let get_credential_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_credential_report (Fn.id ())
           (Some Values.GetCredentialReportResponse.to_json)
           (Some Values.GetCredentialReportResponse.error_to_json)])
let get_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group
           (Values.GetGroupRequest.make ?marker ?maxItems ~groupName ())
           (Some Values.GetGroupResponse.to_json)
           (Some Values.GetGroupResponse.error_to_json)])
let get_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group_policy
           (Values.GetGroupPolicyRequest.make ~groupName ~policyName ())
           (Some Values.GetGroupPolicyResponse.to_json)
           (Some Values.GetGroupPolicyResponse.error_to_json)])
let get_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceProfileName =
         flag "instance-profile-name" (required string)
           ~doc:"STRING instanceProfileNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_profile
           (Values.GetInstanceProfileRequest.make ~instanceProfileName ())
           (Some Values.GetInstanceProfileResponse.to_json)
           (Some Values.GetInstanceProfileResponse.error_to_json)])
let get_login_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_login_profile
           (Values.GetLoginProfileRequest.make ~userName ())
           (Some Values.GetLoginProfileResponse.to_json)
           (Some Values.GetLoginProfileResponse.error_to_json)])
let get_open_i_d_connect_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and openIDConnectProviderArn =
         flag "open-i-d-connect-provider-arn" (required string)
           ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_open_i_d_connect_provider
           (Values.GetOpenIDConnectProviderRequest.make
              ~openIDConnectProviderArn ())
           (Some Values.GetOpenIDConnectProviderResponse.to_json)
           (Some Values.GetOpenIDConnectProviderResponse.error_to_json)])
let get_organizations_access_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and sortKey =
         flag "sort-key" (optional json_arg) ~doc:"JSON sortKeyType"
       and jobId = flag "job-id" (required string) ~doc:"STRING jobIDType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_organizations_access_report
           (Values.GetOrganizationsAccessReportRequest.make ?maxItems ?marker
              ?sortKey:(Option.map ~f:Values.SortKeyType.of_json sortKey)
              ~jobId ())
           (Some Values.GetOrganizationsAccessReportResponse.to_json)
           (Some Values.GetOrganizationsAccessReportResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy (Values.GetPolicyRequest.make ~policyArn ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let get_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING policyVersionIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_version
           (Values.GetPolicyVersionRequest.make ~policyArn ~versionId ())
           (Some Values.GetPolicyVersionResponse.to_json)
           (Some Values.GetPolicyVersionResponse.error_to_json)])
let get_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_role (Values.GetRoleRequest.make ~roleName ())
           (Some Values.GetRoleResponse.to_json)
           (Some Values.GetRoleResponse.error_to_json)])
let get_role_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_role_policy
           (Values.GetRolePolicyRequest.make ~roleName ~policyName ())
           (Some Values.GetRolePolicyResponse.to_json)
           (Some Values.GetRolePolicyResponse.error_to_json)])
let get_s_a_m_l_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sAMLProviderArn =
         flag "s-a-m-l-provider-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_s_a_m_l_provider
           (Values.GetSAMLProviderRequest.make ~sAMLProviderArn ())
           (Some Values.GetSAMLProviderResponse.to_json)
           (Some Values.GetSAMLProviderResponse.error_to_json)])
let get_s_s_h_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and sSHPublicKeyId =
         flag "s-s-h-public-key-id" (required string)
           ~doc:"STRING publicKeyIdType"
       and encoding =
         flag "encoding" (required json_arg) ~doc:"JSON encodingType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_s_s_h_public_key
           (Values.GetSSHPublicKeyRequest.make ~userName ~sSHPublicKeyId
              ~encoding:(Values.EncodingType.of_json encoding) ())
           (Some Values.GetSSHPublicKeyResponse.to_json)
           (Some Values.GetSSHPublicKeyResponse.error_to_json)])
let get_server_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverCertificateName =
         flag "server-certificate-name" (required string)
           ~doc:"STRING serverCertificateNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_server_certificate
           (Values.GetServerCertificateRequest.make ~serverCertificateName ())
           (Some Values.GetServerCertificateResponse.to_json)
           (Some Values.GetServerCertificateResponse.error_to_json)])
let get_service_last_accessed_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and jobId = flag "job-id" (required string) ~doc:"STRING jobIDType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_last_accessed_details
           (Values.GetServiceLastAccessedDetailsRequest.make ?maxItems
              ?marker ~jobId ())
           (Some Values.GetServiceLastAccessedDetailsResponse.to_json)
           (Some Values.GetServiceLastAccessedDetailsResponse.error_to_json)])
let get_service_last_accessed_details_with_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and jobId = flag "job-id" (required string) ~doc:"STRING jobIDType"
       and serviceNamespace =
         flag "service-namespace" (required string)
           ~doc:"STRING serviceNamespaceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_last_accessed_details_with_entities
           (Values.GetServiceLastAccessedDetailsWithEntitiesRequest.make
              ?maxItems ?marker ~jobId ~serviceNamespace ())
           (Some
              Values.GetServiceLastAccessedDetailsWithEntitiesResponse.to_json)
           (Some
              Values.GetServiceLastAccessedDetailsWithEntitiesResponse.error_to_json)])
let get_service_linked_role_deletion_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deletionTaskId =
         flag "deletion-task-id" (required string)
           ~doc:"STRING DeletionTaskIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_linked_role_deletion_status
           (Values.GetServiceLinkedRoleDeletionStatusRequest.make
              ~deletionTaskId ())
           (Some Values.GetServiceLinkedRoleDeletionStatusResponse.to_json)
           (Some
              Values.GetServiceLinkedRoleDeletionStatusResponse.error_to_json)])
let get_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user (Values.GetUserRequest.make ?userName ())
           (Some Values.GetUserResponse.to_json)
           (Some Values.GetUserResponse.error_to_json)])
let get_user_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user_policy
           (Values.GetUserPolicyRequest.make ~userName ~policyName ())
           (Some Values.GetUserPolicyResponse.to_json)
           (Some Values.GetUserPolicyResponse.error_to_json)])
let list_access_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_keys
           (Values.ListAccessKeysRequest.make ?userName ?marker ?maxItems ())
           (Some Values.ListAccessKeysResponse.to_json)
           (Some Values.ListAccessKeysResponse.error_to_json)])
let list_account_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_aliases
           (Values.ListAccountAliasesRequest.make ?marker ?maxItems ())
           (Some Values.ListAccountAliasesResponse.to_json)
           (Some Values.ListAccountAliasesResponse.error_to_json)])
let list_attached_group_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING policyPathType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attached_group_policies
           (Values.ListAttachedGroupPoliciesRequest.make ?pathPrefix ?marker
              ?maxItems ~groupName ())
           (Some Values.ListAttachedGroupPoliciesResponse.to_json)
           (Some Values.ListAttachedGroupPoliciesResponse.error_to_json)])
let list_attached_role_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING policyPathType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attached_role_policies
           (Values.ListAttachedRolePoliciesRequest.make ?pathPrefix ?marker
              ?maxItems ~roleName ())
           (Some Values.ListAttachedRolePoliciesResponse.to_json)
           (Some Values.ListAttachedRolePoliciesResponse.error_to_json)])
let list_attached_user_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING policyPathType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attached_user_policies
           (Values.ListAttachedUserPoliciesRequest.make ?pathPrefix ?marker
              ?maxItems ~userName ())
           (Some Values.ListAttachedUserPoliciesResponse.to_json)
           (Some Values.ListAttachedUserPoliciesResponse.error_to_json)])
let list_entities_for_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entityFilter =
         flag "entity-filter" (optional json_arg) ~doc:"JSON EntityType"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING pathType"
       and policyUsageFilter =
         flag "policy-usage-filter" (optional json_arg)
           ~doc:"JSON PolicyUsageType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entities_for_policy
           (Values.ListEntitiesForPolicyRequest.make
              ?entityFilter:(Option.map ~f:Values.EntityType.of_json
                               entityFilter) ?pathPrefix
              ?policyUsageFilter:(Option.map
                                    ~f:Values.PolicyUsageType.of_json
                                    policyUsageFilter) ?marker ?maxItems
              ~policyArn ())
           (Some Values.ListEntitiesForPolicyResponse.to_json)
           (Some Values.ListEntitiesForPolicyResponse.error_to_json)])
let list_group_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_group_policies
           (Values.ListGroupPoliciesRequest.make ?marker ?maxItems ~groupName
              ()) (Some Values.ListGroupPoliciesResponse.to_json)
           (Some Values.ListGroupPoliciesResponse.error_to_json)])
let list_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING pathPrefixType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups
           (Values.ListGroupsRequest.make ?pathPrefix ?marker ?maxItems ())
           (Some Values.ListGroupsResponse.to_json)
           (Some Values.ListGroupsResponse.error_to_json)])
let list_groups_for_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups_for_user
           (Values.ListGroupsForUserRequest.make ?marker ?maxItems ~userName
              ()) (Some Values.ListGroupsForUserResponse.to_json)
           (Some Values.ListGroupsForUserResponse.error_to_json)])
let list_instance_profile_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and instanceProfileName =
         flag "instance-profile-name" (required string)
           ~doc:"STRING instanceProfileNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instance_profile_tags
           (Values.ListInstanceProfileTagsRequest.make ?marker ?maxItems
              ~instanceProfileName ())
           (Some Values.ListInstanceProfileTagsResponse.to_json)
           (Some Values.ListInstanceProfileTagsResponse.error_to_json)])
let list_instance_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING pathPrefixType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instance_profiles
           (Values.ListInstanceProfilesRequest.make ?pathPrefix ?marker
              ?maxItems ())
           (Some Values.ListInstanceProfilesResponse.to_json)
           (Some Values.ListInstanceProfilesResponse.error_to_json)])
let list_instance_profiles_for_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instance_profiles_for_role
           (Values.ListInstanceProfilesForRoleRequest.make ?marker ?maxItems
              ~roleName ())
           (Some Values.ListInstanceProfilesForRoleResponse.to_json)
           (Some Values.ListInstanceProfilesForRoleResponse.error_to_json)])
let list_m_f_a_device_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and serialNumber =
         flag "serial-number" (required string)
           ~doc:"STRING serialNumberType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_m_f_a_device_tags
           (Values.ListMFADeviceTagsRequest.make ?marker ?maxItems
              ~serialNumber ())
           (Some Values.ListMFADeviceTagsResponse.to_json)
           (Some Values.ListMFADeviceTagsResponse.error_to_json)])
let list_m_f_a_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_m_f_a_devices
           (Values.ListMFADevicesRequest.make ?userName ?marker ?maxItems ())
           (Some Values.ListMFADevicesResponse.to_json)
           (Some Values.ListMFADevicesResponse.error_to_json)])
let list_open_i_d_connect_provider_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and openIDConnectProviderArn =
         flag "open-i-d-connect-provider-arn" (required string)
           ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_open_i_d_connect_provider_tags
           (Values.ListOpenIDConnectProviderTagsRequest.make ?marker
              ?maxItems ~openIDConnectProviderArn ())
           (Some Values.ListOpenIDConnectProviderTagsResponse.to_json)
           (Some Values.ListOpenIDConnectProviderTagsResponse.error_to_json)])
let list_open_i_d_connect_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_open_i_d_connect_providers
           (Values.ListOpenIDConnectProvidersRequest.make ())
           (Some Values.ListOpenIDConnectProvidersResponse.to_json)
           (Some Values.ListOpenIDConnectProvidersResponse.error_to_json)])
let list_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scope =
         flag "scope" (optional json_arg) ~doc:"JSON policyScopeType"
       and onlyAttached =
         flag "only-attached" (optional bool) ~doc:"BOOL booleanType"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING policyPathType"
       and policyUsageFilter =
         flag "policy-usage-filter" (optional json_arg)
           ~doc:"JSON PolicyUsageType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies
           (Values.ListPoliciesRequest.make
              ?scope:(Option.map ~f:Values.PolicyScopeType.of_json scope)
              ?onlyAttached ?pathPrefix
              ?policyUsageFilter:(Option.map
                                    ~f:Values.PolicyUsageType.of_json
                                    policyUsageFilter) ?marker ?maxItems ())
           (Some Values.ListPoliciesResponse.to_json)
           (Some Values.ListPoliciesResponse.error_to_json)])
let list_policies_granting_service_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and arn = flag "arn" (required string) ~doc:"STRING arnType"
       and serviceNamespaces =
         flag "service-namespaces" (required json_arg)
           ~doc:"JSON serviceNamespaceListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies_granting_service_access
           (Values.ListPoliciesGrantingServiceAccessRequest.make ?marker ~arn
              ~serviceNamespaces:(Values.ServiceNamespaceListType.of_json
                                    serviceNamespaces) ())
           (Some Values.ListPoliciesGrantingServiceAccessResponse.to_json)
           (Some
              Values.ListPoliciesGrantingServiceAccessResponse.error_to_json)])
let list_policy_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_tags
           (Values.ListPolicyTagsRequest.make ?marker ?maxItems ~policyArn ())
           (Some Values.ListPolicyTagsResponse.to_json)
           (Some Values.ListPolicyTagsResponse.error_to_json)])
let list_policy_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_versions
           (Values.ListPolicyVersionsRequest.make ?marker ?maxItems
              ~policyArn ()) (Some Values.ListPolicyVersionsResponse.to_json)
           (Some Values.ListPolicyVersionsResponse.error_to_json)])
let list_role_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_role_policies
           (Values.ListRolePoliciesRequest.make ?marker ?maxItems ~roleName
              ()) (Some Values.ListRolePoliciesResponse.to_json)
           (Some Values.ListRolePoliciesResponse.error_to_json)])
let list_role_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_role_tags
           (Values.ListRoleTagsRequest.make ?marker ?maxItems ~roleName ())
           (Some Values.ListRoleTagsResponse.to_json)
           (Some Values.ListRoleTagsResponse.error_to_json)])
let list_roles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING pathPrefixType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_roles
           (Values.ListRolesRequest.make ?pathPrefix ?marker ?maxItems ())
           (Some Values.ListRolesResponse.to_json)
           (Some Values.ListRolesResponse.error_to_json)])
let list_s_a_m_l_provider_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and sAMLProviderArn =
         flag "s-a-m-l-provider-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_s_a_m_l_provider_tags
           (Values.ListSAMLProviderTagsRequest.make ?marker ?maxItems
              ~sAMLProviderArn ())
           (Some Values.ListSAMLProviderTagsResponse.to_json)
           (Some Values.ListSAMLProviderTagsResponse.error_to_json)])
let list_s_a_m_l_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_s_a_m_l_providers
           (Values.ListSAMLProvidersRequest.make ())
           (Some Values.ListSAMLProvidersResponse.to_json)
           (Some Values.ListSAMLProvidersResponse.error_to_json)])
let list_s_s_h_public_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING userNameType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_s_s_h_public_keys
           (Values.ListSSHPublicKeysRequest.make ?userName ?marker ?maxItems
              ()) (Some Values.ListSSHPublicKeysResponse.to_json)
           (Some Values.ListSSHPublicKeysResponse.error_to_json)])
let list_server_certificate_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and serverCertificateName =
         flag "server-certificate-name" (required string)
           ~doc:"STRING serverCertificateNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_server_certificate_tags
           (Values.ListServerCertificateTagsRequest.make ?marker ?maxItems
              ~serverCertificateName ())
           (Some Values.ListServerCertificateTagsResponse.to_json)
           (Some Values.ListServerCertificateTagsResponse.error_to_json)])
let list_server_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING pathPrefixType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_server_certificates
           (Values.ListServerCertificatesRequest.make ?pathPrefix ?marker
              ?maxItems ())
           (Some Values.ListServerCertificatesResponse.to_json)
           (Some Values.ListServerCertificatesResponse.error_to_json)])
let list_service_specific_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING userNameType"
       and serviceName =
         flag "service-name" (optional string) ~doc:"STRING serviceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_specific_credentials
           (Values.ListServiceSpecificCredentialsRequest.make ?userName
              ?serviceName ())
           (Some Values.ListServiceSpecificCredentialsResponse.to_json)
           (Some Values.ListServiceSpecificCredentialsResponse.error_to_json)])
let list_signing_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_signing_certificates
           (Values.ListSigningCertificatesRequest.make ?userName ?marker
              ?maxItems ())
           (Some Values.ListSigningCertificatesResponse.to_json)
           (Some Values.ListSigningCertificatesResponse.error_to_json)])
let list_user_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_policies
           (Values.ListUserPoliciesRequest.make ?marker ?maxItems ~userName
              ()) (Some Values.ListUserPoliciesResponse.to_json)
           (Some Values.ListUserPoliciesResponse.error_to_json)])
let list_user_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_tags
           (Values.ListUserTagsRequest.make ?marker ?maxItems ~userName ())
           (Some Values.ListUserTagsResponse.to_json)
           (Some Values.ListUserTagsResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pathPrefix =
         flag "path-prefix" (optional string) ~doc:"STRING pathPrefixType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?pathPrefix ?marker ?maxItems ())
           (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let list_virtual_m_f_a_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assignmentStatus =
         flag "assignment-status" (optional json_arg)
           ~doc:"JSON assignmentStatusType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_virtual_m_f_a_devices
           (Values.ListVirtualMFADevicesRequest.make
              ?assignmentStatus:(Option.map
                                   ~f:Values.AssignmentStatusType.of_json
                                   assignmentStatus) ?marker ?maxItems ())
           (Some Values.ListVirtualMFADevicesResponse.to_json)
           (Some Values.ListVirtualMFADevicesResponse.error_to_json)])
let put_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING policyDocumentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_group_policy
           (Values.PutGroupPolicyRequest.make ~groupName ~policyName
              ~policyDocument ()) None None])
let put_role_permissions_boundary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and permissionsBoundary =
         flag "permissions-boundary" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_role_permissions_boundary
           (Values.PutRolePermissionsBoundaryRequest.make ~roleName
              ~permissionsBoundary ()) None None])
let put_role_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING policyDocumentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_role_policy
           (Values.PutRolePolicyRequest.make ~roleName ~policyName
              ~policyDocument ()) None None])
let put_user_permissions_boundary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and permissionsBoundary =
         flag "permissions-boundary" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_user_permissions_boundary
           (Values.PutUserPermissionsBoundaryRequest.make ~userName
              ~permissionsBoundary ()) None None])
let put_user_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING policyNameType"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING policyDocumentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_user_policy
           (Values.PutUserPolicyRequest.make ~userName ~policyName
              ~policyDocument ()) None None])
let remove_client_i_d_from_open_i_d_connect_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and openIDConnectProviderArn =
         flag "open-i-d-connect-provider-arn" (required string)
           ~doc:"STRING arnType"
       and clientID =
         flag "client-i-d" (required string) ~doc:"STRING clientIDType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_client_i_d_from_open_i_d_connect_provider
           (Values.RemoveClientIDFromOpenIDConnectProviderRequest.make
              ~openIDConnectProviderArn ~clientID ()) None None])
let remove_role_from_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceProfileName =
         flag "instance-profile-name" (required string)
           ~doc:"STRING instanceProfileNameType"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_role_from_instance_profile
           (Values.RemoveRoleFromInstanceProfileRequest.make
              ~instanceProfileName ~roleName ()) None None])
let remove_user_from_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_user_from_group
           (Values.RemoveUserFromGroupRequest.make ~groupName ~userName ())
           None None])
let reset_service_specific_credential =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING userNameType"
       and serviceSpecificCredentialId =
         flag "service-specific-credential-id" (required string)
           ~doc:"STRING serviceSpecificCredentialId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_service_specific_credential
           (Values.ResetServiceSpecificCredentialRequest.make ?userName
              ~serviceSpecificCredentialId ())
           (Some Values.ResetServiceSpecificCredentialResponse.to_json)
           (Some Values.ResetServiceSpecificCredentialResponse.error_to_json)])
let resync_m_f_a_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType"
       and serialNumber =
         flag "serial-number" (required string)
           ~doc:"STRING serialNumberType"
       and authenticationCode1 =
         flag "authentication-code1" (required string)
           ~doc:"STRING authenticationCodeType"
       and authenticationCode2 =
         flag "authentication-code2" (required string)
           ~doc:"STRING authenticationCodeType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resync_m_f_a_device
           (Values.ResyncMFADeviceRequest.make ~userName ~serialNumber
              ~authenticationCode1 ~authenticationCode2 ()) None None])
let set_default_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING policyVersionIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_default_policy_version
           (Values.SetDefaultPolicyVersionRequest.make ~policyArn ~versionId
              ()) None None])
let set_security_token_service_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalEndpointTokenVersion =
         flag "global-endpoint-token-version" (required json_arg)
           ~doc:"JSON globalEndpointTokenVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_security_token_service_preferences
           (Values.SetSecurityTokenServicePreferencesRequest.make
              ~globalEndpointTokenVersion:(Values.GlobalEndpointTokenVersion.of_json
                                             globalEndpointTokenVersion) ())
           None None])
let simulate_custom_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and permissionsBoundaryPolicyInputList =
         flag "permissions-boundary-policy-input-list" (optional json_arg)
           ~doc:"JSON SimulationPolicyListType"
       and resourceArns =
         flag "resource-arns" (optional json_arg)
           ~doc:"JSON ResourceNameListType"
       and resourcePolicy =
         flag "resource-policy" (optional string)
           ~doc:"STRING policyDocumentType"
       and resourceOwner =
         flag "resource-owner" (optional string)
           ~doc:"STRING ResourceNameType"
       and callerArn =
         flag "caller-arn" (optional string) ~doc:"STRING ResourceNameType"
       and contextEntries =
         flag "context-entries" (optional json_arg)
           ~doc:"JSON ContextEntryListType"
       and resourceHandlingOption =
         flag "resource-handling-option" (optional string)
           ~doc:"STRING ResourceHandlingOptionType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and policyInputList =
         flag "policy-input-list" (required json_arg)
           ~doc:"JSON SimulationPolicyListType"
       and actionNames =
         flag "action-names" (required json_arg)
           ~doc:"JSON ActionNameListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.simulate_custom_policy
           (Values.SimulateCustomPolicyRequest.make
              ?permissionsBoundaryPolicyInputList:(Option.map
                                                     ~f:Values.SimulationPolicyListType.of_json
                                                     permissionsBoundaryPolicyInputList)
              ?resourceArns:(Option.map
                               ~f:Values.ResourceNameListType.of_json
                               resourceArns) ?resourcePolicy ?resourceOwner
              ?callerArn
              ?contextEntries:(Option.map
                                 ~f:Values.ContextEntryListType.of_json
                                 contextEntries) ?resourceHandlingOption
              ?maxItems ?marker
              ~policyInputList:(Values.SimulationPolicyListType.of_json
                                  policyInputList)
              ~actionNames:(Values.ActionNameListType.of_json actionNames) ())
           (Some Values.SimulatePolicyResponse.to_json)
           (Some Values.SimulatePolicyResponse.error_to_json)])
let simulate_principal_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyInputList =
         flag "policy-input-list" (optional json_arg)
           ~doc:"JSON SimulationPolicyListType"
       and permissionsBoundaryPolicyInputList =
         flag "permissions-boundary-policy-input-list" (optional json_arg)
           ~doc:"JSON SimulationPolicyListType"
       and resourceArns =
         flag "resource-arns" (optional json_arg)
           ~doc:"JSON ResourceNameListType"
       and resourcePolicy =
         flag "resource-policy" (optional string)
           ~doc:"STRING policyDocumentType"
       and resourceOwner =
         flag "resource-owner" (optional string)
           ~doc:"STRING ResourceNameType"
       and callerArn =
         flag "caller-arn" (optional string) ~doc:"STRING ResourceNameType"
       and contextEntries =
         flag "context-entries" (optional json_arg)
           ~doc:"JSON ContextEntryListType"
       and resourceHandlingOption =
         flag "resource-handling-option" (optional string)
           ~doc:"STRING ResourceHandlingOptionType"
       and maxItems = flag "max-items" (optional int) ~doc:"INT maxItemsType"
       and marker = flag "marker" (optional string) ~doc:"STRING markerType"
       and policySourceArn =
         flag "policy-source-arn" (required string) ~doc:"STRING arnType"
       and actionNames =
         flag "action-names" (required json_arg)
           ~doc:"JSON ActionNameListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.simulate_principal_policy
           (Values.SimulatePrincipalPolicyRequest.make
              ?policyInputList:(Option.map
                                  ~f:Values.SimulationPolicyListType.of_json
                                  policyInputList)
              ?permissionsBoundaryPolicyInputList:(Option.map
                                                     ~f:Values.SimulationPolicyListType.of_json
                                                     permissionsBoundaryPolicyInputList)
              ?resourceArns:(Option.map
                               ~f:Values.ResourceNameListType.of_json
                               resourceArns) ?resourcePolicy ?resourceOwner
              ?callerArn
              ?contextEntries:(Option.map
                                 ~f:Values.ContextEntryListType.of_json
                                 contextEntries) ?resourceHandlingOption
              ?maxItems ?marker ~policySourceArn
              ~actionNames:(Values.ActionNameListType.of_json actionNames) ())
           (Some Values.SimulatePolicyResponse.to_json)
           (Some Values.SimulatePolicyResponse.error_to_json)])
let tag_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceProfileName =
         flag "instance-profile-name" (required string)
           ~doc:"STRING instanceProfileNameType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_instance_profile
           (Values.TagInstanceProfileRequest.make ~instanceProfileName
              ~tags:(Values.TagListType.of_json tags) ()) None None])
let tag_m_f_a_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serialNumber =
         flag "serial-number" (required string)
           ~doc:"STRING serialNumberType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_m_f_a_device
           (Values.TagMFADeviceRequest.make ~serialNumber
              ~tags:(Values.TagListType.of_json tags) ()) None None])
let tag_open_i_d_connect_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and openIDConnectProviderArn =
         flag "open-i-d-connect-provider-arn" (required string)
           ~doc:"STRING arnType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_open_i_d_connect_provider
           (Values.TagOpenIDConnectProviderRequest.make
              ~openIDConnectProviderArn
              ~tags:(Values.TagListType.of_json tags) ()) None None])
let tag_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_policy
           (Values.TagPolicyRequest.make ~policyArn
              ~tags:(Values.TagListType.of_json tags) ()) None None])
let tag_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_role
           (Values.TagRoleRequest.make ~roleName
              ~tags:(Values.TagListType.of_json tags) ()) None None])
let tag_s_a_m_l_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sAMLProviderArn =
         flag "s-a-m-l-provider-arn" (required string) ~doc:"STRING arnType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_s_a_m_l_provider
           (Values.TagSAMLProviderRequest.make ~sAMLProviderArn
              ~tags:(Values.TagListType.of_json tags) ()) None None])
let tag_server_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverCertificateName =
         flag "server-certificate-name" (required string)
           ~doc:"STRING serverCertificateNameType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_server_certificate
           (Values.TagServerCertificateRequest.make ~serverCertificateName
              ~tags:(Values.TagListType.of_json tags) ()) None None])
let tag_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_user
           (Values.TagUserRequest.make ~userName
              ~tags:(Values.TagListType.of_json tags) ()) None None])
let untag_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceProfileName =
         flag "instance-profile-name" (required string)
           ~doc:"STRING instanceProfileNameType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_instance_profile
           (Values.UntagInstanceProfileRequest.make ~instanceProfileName
              ~tagKeys:(Values.TagKeyListType.of_json tagKeys) ()) None None])
let untag_m_f_a_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serialNumber =
         flag "serial-number" (required string)
           ~doc:"STRING serialNumberType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_m_f_a_device
           (Values.UntagMFADeviceRequest.make ~serialNumber
              ~tagKeys:(Values.TagKeyListType.of_json tagKeys) ()) None None])
let untag_open_i_d_connect_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and openIDConnectProviderArn =
         flag "open-i-d-connect-provider-arn" (required string)
           ~doc:"STRING arnType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_open_i_d_connect_provider
           (Values.UntagOpenIDConnectProviderRequest.make
              ~openIDConnectProviderArn
              ~tagKeys:(Values.TagKeyListType.of_json tagKeys) ()) None None])
let untag_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string) ~doc:"STRING arnType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_policy
           (Values.UntagPolicyRequest.make ~policyArn
              ~tagKeys:(Values.TagKeyListType.of_json tagKeys) ()) None None])
let untag_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_role
           (Values.UntagRoleRequest.make ~roleName
              ~tagKeys:(Values.TagKeyListType.of_json tagKeys) ()) None None])
let untag_s_a_m_l_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sAMLProviderArn =
         flag "s-a-m-l-provider-arn" (required string) ~doc:"STRING arnType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_s_a_m_l_provider
           (Values.UntagSAMLProviderRequest.make ~sAMLProviderArn
              ~tagKeys:(Values.TagKeyListType.of_json tagKeys) ()) None None])
let untag_server_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverCertificateName =
         flag "server-certificate-name" (required string)
           ~doc:"STRING serverCertificateNameType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_server_certificate
           (Values.UntagServerCertificateRequest.make ~serverCertificateName
              ~tagKeys:(Values.TagKeyListType.of_json tagKeys) ()) None None])
let untag_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_user
           (Values.UntagUserRequest.make ~userName
              ~tagKeys:(Values.TagKeyListType.of_json tagKeys) ()) None None])
let update_access_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType"
       and accessKeyId =
         flag "access-key-id" (required string) ~doc:"STRING accessKeyIdType"
       and status = flag "status" (required json_arg) ~doc:"JSON statusType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_access_key
           (Values.UpdateAccessKeyRequest.make ?userName ~accessKeyId
              ~status:(Values.StatusType.of_json status) ()) None None])
let update_account_password_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and minimumPasswordLength =
         flag "minimum-password-length" (optional int)
           ~doc:"INT minimumPasswordLengthType"
       and requireSymbols =
         flag "require-symbols" (optional bool) ~doc:"BOOL booleanType"
       and requireNumbers =
         flag "require-numbers" (optional bool) ~doc:"BOOL booleanType"
       and requireUppercaseCharacters =
         flag "require-uppercase-characters" (optional bool)
           ~doc:"BOOL booleanType"
       and requireLowercaseCharacters =
         flag "require-lowercase-characters" (optional bool)
           ~doc:"BOOL booleanType"
       and allowUsersToChangePassword =
         flag "allow-users-to-change-password" (optional bool)
           ~doc:"BOOL booleanType"
       and maxPasswordAge =
         flag "max-password-age" (optional int) ~doc:"INT maxPasswordAgeType"
       and passwordReusePrevention =
         flag "password-reuse-prevention" (optional int)
           ~doc:"INT passwordReusePreventionType"
       and hardExpiry =
         flag "hard-expiry" (optional bool) ~doc:"BOOL booleanObjectType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_password_policy
           (Values.UpdateAccountPasswordPolicyRequest.make
              ?minimumPasswordLength ?requireSymbols ?requireNumbers
              ?requireUppercaseCharacters ?requireLowercaseCharacters
              ?allowUsersToChangePassword ?maxPasswordAge
              ?passwordReusePrevention ?hardExpiry ()) None None])
let update_assume_role_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING policyDocumentType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_assume_role_policy
           (Values.UpdateAssumeRolePolicyRequest.make ~roleName
              ~policyDocument ()) None None])
let update_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newPath = flag "new-path" (optional string) ~doc:"STRING pathType"
       and newGroupName =
         flag "new-group-name" (optional string) ~doc:"STRING groupNameType"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING groupNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group
           (Values.UpdateGroupRequest.make ?newPath ?newGroupName ~groupName
              ()) None None])
let update_login_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and password =
         flag "password" (optional string) ~doc:"STRING passwordType"
       and passwordResetRequired =
         flag "password-reset-required" (optional bool)
           ~doc:"BOOL booleanObjectType"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_login_profile
           (Values.UpdateLoginProfileRequest.make ?password
              ?passwordResetRequired ~userName ()) None None])
let update_open_i_d_connect_provider_thumbprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and openIDConnectProviderArn =
         flag "open-i-d-connect-provider-arn" (required string)
           ~doc:"STRING arnType"
       and thumbprintList =
         flag "thumbprint-list" (required json_arg)
           ~doc:"JSON thumbprintListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_open_i_d_connect_provider_thumbprint
           (Values.UpdateOpenIDConnectProviderThumbprintRequest.make
              ~openIDConnectProviderArn
              ~thumbprintList:(Values.ThumbprintListType.of_json
                                 thumbprintList) ()) None None])
let update_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING roleDescriptionType"
       and maxSessionDuration =
         flag "max-session-duration" (optional int)
           ~doc:"INT roleMaxSessionDurationType"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_role
           (Values.UpdateRoleRequest.make ?description ?maxSessionDuration
              ~roleName ()) (Some Values.UpdateRoleResponse.to_json)
           (Some Values.UpdateRoleResponse.error_to_json)])
let update_role_description =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleName =
         flag "role-name" (required string) ~doc:"STRING roleNameType"
       and description =
         flag "description" (required string)
           ~doc:"STRING roleDescriptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_role_description
           (Values.UpdateRoleDescriptionRequest.make ~roleName ~description
              ()) (Some Values.UpdateRoleDescriptionResponse.to_json)
           (Some Values.UpdateRoleDescriptionResponse.error_to_json)])
let update_s_a_m_l_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sAMLMetadataDocument =
         flag "s-a-m-l-metadata-document" (required string)
           ~doc:"STRING SAMLMetadataDocumentType"
       and sAMLProviderArn =
         flag "s-a-m-l-provider-arn" (required string) ~doc:"STRING arnType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_s_a_m_l_provider
           (Values.UpdateSAMLProviderRequest.make ~sAMLMetadataDocument
              ~sAMLProviderArn ())
           (Some Values.UpdateSAMLProviderResponse.to_json)
           (Some Values.UpdateSAMLProviderResponse.error_to_json)])
let update_s_s_h_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and sSHPublicKeyId =
         flag "s-s-h-public-key-id" (required string)
           ~doc:"STRING publicKeyIdType"
       and status = flag "status" (required json_arg) ~doc:"JSON statusType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_s_s_h_public_key
           (Values.UpdateSSHPublicKeyRequest.make ~userName ~sSHPublicKeyId
              ~status:(Values.StatusType.of_json status) ()) None None])
let update_server_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newPath = flag "new-path" (optional string) ~doc:"STRING pathType"
       and newServerCertificateName =
         flag "new-server-certificate-name" (optional string)
           ~doc:"STRING serverCertificateNameType"
       and serverCertificateName =
         flag "server-certificate-name" (required string)
           ~doc:"STRING serverCertificateNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_server_certificate
           (Values.UpdateServerCertificateRequest.make ?newPath
              ?newServerCertificateName ~serverCertificateName ()) None None])
let update_service_specific_credential =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string) ~doc:"STRING userNameType"
       and serviceSpecificCredentialId =
         flag "service-specific-credential-id" (required string)
           ~doc:"STRING serviceSpecificCredentialId"
       and status = flag "status" (required json_arg) ~doc:"JSON statusType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_specific_credential
           (Values.UpdateServiceSpecificCredentialRequest.make ?userName
              ~serviceSpecificCredentialId
              ~status:(Values.StatusType.of_json status) ()) None None])
let update_signing_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType"
       and certificateId =
         flag "certificate-id" (required string)
           ~doc:"STRING certificateIdType"
       and status = flag "status" (required json_arg) ~doc:"JSON statusType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_signing_certificate
           (Values.UpdateSigningCertificateRequest.make ?userName
              ~certificateId ~status:(Values.StatusType.of_json status) ())
           None None])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newPath = flag "new-path" (optional string) ~doc:"STRING pathType"
       and newUserName =
         flag "new-user-name" (optional string) ~doc:"STRING userNameType"
       and userName =
         flag "user-name" (required string)
           ~doc:"STRING existingUserNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make ?newPath ?newUserName ~userName ())
           None None])
let upload_s_s_h_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (required string) ~doc:"STRING userNameType"
       and sSHPublicKeyBody =
         flag "s-s-h-public-key-body" (required string)
           ~doc:"STRING publicKeyMaterialType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_s_s_h_public_key
           (Values.UploadSSHPublicKeyRequest.make ~userName ~sSHPublicKeyBody
              ()) (Some Values.UploadSSHPublicKeyResponse.to_json)
           (Some Values.UploadSSHPublicKeyResponse.error_to_json)])
let upload_server_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (optional string) ~doc:"STRING pathType"
       and certificateChain =
         flag "certificate-chain" (optional string)
           ~doc:"STRING certificateChainType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and serverCertificateName =
         flag "server-certificate-name" (required string)
           ~doc:"STRING serverCertificateNameType"
       and certificateBody =
         flag "certificate-body" (required string)
           ~doc:"STRING certificateBodyType"
       and privateKey =
         flag "private-key" (required string) ~doc:"STRING privateKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_server_certificate
           (Values.UploadServerCertificateRequest.make ?path
              ?certificateChain
              ?tags:(Option.map ~f:Values.TagListType.of_json tags)
              ~serverCertificateName ~certificateBody ~privateKey ())
           (Some Values.UploadServerCertificateResponse.to_json)
           (Some Values.UploadServerCertificateResponse.error_to_json)])
let upload_signing_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING existingUserNameType"
       and certificateBody =
         flag "certificate-body" (required string)
           ~doc:"STRING certificateBodyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_signing_certificate
           (Values.UploadSigningCertificateRequest.make ?userName
              ~certificateBody ())
           (Some Values.UploadSigningCertificateResponse.to_json)
           (Some Values.UploadSigningCertificateResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-client-i-d-to-open-i-d-connect-provider",
       add_client_i_d_to_open_i_d_connect_provider);
    ("add-role-to-instance-profile", add_role_to_instance_profile);
    ("add-user-to-group", add_user_to_group);
    ("attach-group-policy", attach_group_policy);
    ("attach-role-policy", attach_role_policy);
    ("attach-user-policy", attach_user_policy);
    ("change-password", change_password);
    ("create-access-key", create_access_key);
    ("create-account-alias", create_account_alias);
    ("create-group", create_group);
    ("create-instance-profile", create_instance_profile);
    ("create-login-profile", create_login_profile);
    ("create-open-i-d-connect-provider", create_open_i_d_connect_provider);
    ("create-policy", create_policy);
    ("create-policy-version", create_policy_version);
    ("create-role", create_role);
    ("create-s-a-m-l-provider", create_s_a_m_l_provider);
    ("create-service-linked-role", create_service_linked_role);
    ("create-service-specific-credential",
      create_service_specific_credential);
    ("create-user", create_user);
    ("create-virtual-m-f-a-device", create_virtual_m_f_a_device);
    ("deactivate-m-f-a-device", deactivate_m_f_a_device);
    ("delete-access-key", delete_access_key);
    ("delete-account-alias", delete_account_alias);
    ("delete-account-password-policy", delete_account_password_policy);
    ("delete-group", delete_group);
    ("delete-group-policy", delete_group_policy);
    ("delete-instance-profile", delete_instance_profile);
    ("delete-login-profile", delete_login_profile);
    ("delete-open-i-d-connect-provider", delete_open_i_d_connect_provider);
    ("delete-policy", delete_policy);
    ("delete-policy-version", delete_policy_version);
    ("delete-role", delete_role);
    ("delete-role-permissions-boundary", delete_role_permissions_boundary);
    ("delete-role-policy", delete_role_policy);
    ("delete-s-a-m-l-provider", delete_s_a_m_l_provider);
    ("delete-s-s-h-public-key", delete_s_s_h_public_key);
    ("delete-server-certificate", delete_server_certificate);
    ("delete-service-linked-role", delete_service_linked_role);
    ("delete-service-specific-credential",
      delete_service_specific_credential);
    ("delete-signing-certificate", delete_signing_certificate);
    ("delete-user", delete_user);
    ("delete-user-permissions-boundary", delete_user_permissions_boundary);
    ("delete-user-policy", delete_user_policy);
    ("delete-virtual-m-f-a-device", delete_virtual_m_f_a_device);
    ("detach-group-policy", detach_group_policy);
    ("detach-role-policy", detach_role_policy);
    ("detach-user-policy", detach_user_policy);
    ("enable-m-f-a-device", enable_m_f_a_device);
    ("generate-credential-report", generate_credential_report);
    ("generate-organizations-access-report",
      generate_organizations_access_report);
    ("generate-service-last-accessed-details",
      generate_service_last_accessed_details);
    ("get-access-key-last-used", get_access_key_last_used);
    ("get-account-authorization-details", get_account_authorization_details);
    ("get-account-password-policy", get_account_password_policy);
    ("get-account-summary", get_account_summary);
    ("get-context-keys-for-custom-policy",
      get_context_keys_for_custom_policy);
    ("get-context-keys-for-principal-policy",
      get_context_keys_for_principal_policy);
    ("get-credential-report", get_credential_report);
    ("get-group", get_group);
    ("get-group-policy", get_group_policy);
    ("get-instance-profile", get_instance_profile);
    ("get-login-profile", get_login_profile);
    ("get-open-i-d-connect-provider", get_open_i_d_connect_provider);
    ("get-organizations-access-report", get_organizations_access_report);
    ("get-policy", get_policy);
    ("get-policy-version", get_policy_version);
    ("get-role", get_role);
    ("get-role-policy", get_role_policy);
    ("get-s-a-m-l-provider", get_s_a_m_l_provider);
    ("get-s-s-h-public-key", get_s_s_h_public_key);
    ("get-server-certificate", get_server_certificate);
    ("get-service-last-accessed-details", get_service_last_accessed_details);
    ("get-service-last-accessed-details-with-entities",
      get_service_last_accessed_details_with_entities);
    ("get-service-linked-role-deletion-status",
      get_service_linked_role_deletion_status);
    ("get-user", get_user);
    ("get-user-policy", get_user_policy);
    ("list-access-keys", list_access_keys);
    ("list-account-aliases", list_account_aliases);
    ("list-attached-group-policies", list_attached_group_policies);
    ("list-attached-role-policies", list_attached_role_policies);
    ("list-attached-user-policies", list_attached_user_policies);
    ("list-entities-for-policy", list_entities_for_policy);
    ("list-group-policies", list_group_policies);
    ("list-groups", list_groups);
    ("list-groups-for-user", list_groups_for_user);
    ("list-instance-profile-tags", list_instance_profile_tags);
    ("list-instance-profiles", list_instance_profiles);
    ("list-instance-profiles-for-role", list_instance_profiles_for_role);
    ("list-m-f-a-device-tags", list_m_f_a_device_tags);
    ("list-m-f-a-devices", list_m_f_a_devices);
    ("list-open-i-d-connect-provider-tags",
      list_open_i_d_connect_provider_tags);
    ("list-open-i-d-connect-providers", list_open_i_d_connect_providers);
    ("list-policies", list_policies);
    ("list-policies-granting-service-access",
      list_policies_granting_service_access);
    ("list-policy-tags", list_policy_tags);
    ("list-policy-versions", list_policy_versions);
    ("list-role-policies", list_role_policies);
    ("list-role-tags", list_role_tags);
    ("list-roles", list_roles);
    ("list-s-a-m-l-provider-tags", list_s_a_m_l_provider_tags);
    ("list-s-a-m-l-providers", list_s_a_m_l_providers);
    ("list-s-s-h-public-keys", list_s_s_h_public_keys);
    ("list-server-certificate-tags", list_server_certificate_tags);
    ("list-server-certificates", list_server_certificates);
    ("list-service-specific-credentials", list_service_specific_credentials);
    ("list-signing-certificates", list_signing_certificates);
    ("list-user-policies", list_user_policies);
    ("list-user-tags", list_user_tags);
    ("list-users", list_users);
    ("list-virtual-m-f-a-devices", list_virtual_m_f_a_devices);
    ("put-group-policy", put_group_policy);
    ("put-role-permissions-boundary", put_role_permissions_boundary);
    ("put-role-policy", put_role_policy);
    ("put-user-permissions-boundary", put_user_permissions_boundary);
    ("put-user-policy", put_user_policy);
    ("remove-client-i-d-from-open-i-d-connect-provider",
      remove_client_i_d_from_open_i_d_connect_provider);
    ("remove-role-from-instance-profile", remove_role_from_instance_profile);
    ("remove-user-from-group", remove_user_from_group);
    ("reset-service-specific-credential", reset_service_specific_credential);
    ("resync-m-f-a-device", resync_m_f_a_device);
    ("set-default-policy-version", set_default_policy_version);
    ("set-security-token-service-preferences",
      set_security_token_service_preferences);
    ("simulate-custom-policy", simulate_custom_policy);
    ("simulate-principal-policy", simulate_principal_policy);
    ("tag-instance-profile", tag_instance_profile);
    ("tag-m-f-a-device", tag_m_f_a_device);
    ("tag-open-i-d-connect-provider", tag_open_i_d_connect_provider);
    ("tag-policy", tag_policy);
    ("tag-role", tag_role);
    ("tag-s-a-m-l-provider", tag_s_a_m_l_provider);
    ("tag-server-certificate", tag_server_certificate);
    ("tag-user", tag_user);
    ("untag-instance-profile", untag_instance_profile);
    ("untag-m-f-a-device", untag_m_f_a_device);
    ("untag-open-i-d-connect-provider", untag_open_i_d_connect_provider);
    ("untag-policy", untag_policy);
    ("untag-role", untag_role);
    ("untag-s-a-m-l-provider", untag_s_a_m_l_provider);
    ("untag-server-certificate", untag_server_certificate);
    ("untag-user", untag_user);
    ("update-access-key", update_access_key);
    ("update-account-password-policy", update_account_password_policy);
    ("update-assume-role-policy", update_assume_role_policy);
    ("update-group", update_group);
    ("update-login-profile", update_login_profile);
    ("update-open-i-d-connect-provider-thumbprint",
      update_open_i_d_connect_provider_thumbprint);
    ("update-role", update_role);
    ("update-role-description", update_role_description);
    ("update-s-a-m-l-provider", update_s_a_m_l_provider);
    ("update-s-s-h-public-key", update_s_s_h_public_key);
    ("update-server-certificate", update_server_certificate);
    ("update-service-specific-credential",
      update_service_specific_credential);
    ("update-signing-certificate", update_signing_certificate);
    ("update-user", update_user);
    ("upload-s-s-h-public-key", upload_s_s_h_public_key);
    ("upload-server-certificate", upload_server_certificate);
    ("upload-signing-certificate", upload_signing_certificate)]
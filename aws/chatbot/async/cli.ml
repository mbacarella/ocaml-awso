(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_to_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (required string) ~doc:"STRING ResourceIdentifier"
       and chatConfiguration =
         flag "chat-configuration" (required string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_to_configuration
           (Values.AssociateToConfigurationRequest.make ~resource
              ~chatConfiguration ())
           (Some Values.AssociateToConfigurationResult.to_json)
           (Some Values.AssociateToConfigurationResult.error_to_json)])
let create_chime_webhook_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loggingLevel =
         flag "logging-level" (optional string)
           ~doc:"STRING CustomerCwLogLevel"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and webhookDescription =
         flag "webhook-description" (required string)
           ~doc:"STRING ChimeWebhookDescription"
       and webhookUrl =
         flag "webhook-url" (required string) ~doc:"STRING ChimeWebhookUrl"
       and snsTopicArns =
         flag "sns-topic-arns" (required json_arg)
           ~doc:"JSON SnsTopicArnList"
       and iamRoleArn =
         flag "iam-role-arn" (required string) ~doc:"STRING Arn"
       and configurationName =
         flag "configuration-name" (required string)
           ~doc:"STRING ConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_chime_webhook_configuration
           (Values.CreateChimeWebhookConfigurationRequest.make ?loggingLevel
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~webhookDescription ~webhookUrl
              ~snsTopicArns:(Values.SnsTopicArnList.of_json snsTopicArns)
              ~iamRoleArn ~configurationName ())
           (Some Values.CreateChimeWebhookConfigurationResult.to_json)
           (Some Values.CreateChimeWebhookConfigurationResult.error_to_json)])
let create_custom_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (optional string)
           ~doc:"STRING CustomActionAliasName"
       and attachments =
         flag "attachments" (optional json_arg)
           ~doc:"JSON CustomActionAttachmentList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and definition =
         flag "definition" (required json_arg)
           ~doc:"JSON CustomActionDefinition"
       and actionName =
         flag "action-name" (required string) ~doc:"STRING CustomActionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_action
           (Values.CreateCustomActionRequest.make ?aliasName
              ?attachments:(Option.map
                              ~f:Values.CustomActionAttachmentList.of_json
                              attachments)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~definition:(Values.CustomActionDefinition.of_json definition)
              ~actionName ()) (Some Values.CreateCustomActionResult.to_json)
           (Some Values.CreateCustomActionResult.error_to_json)])
let create_microsoft_teams_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelName =
         flag "channel-name" (optional string) ~doc:"STRING TeamsChannelName"
       and teamName =
         flag "team-name" (optional string) ~doc:"STRING TeamName"
       and snsTopicArns =
         flag "sns-topic-arns" (optional json_arg)
           ~doc:"JSON SnsTopicArnList"
       and loggingLevel =
         flag "logging-level" (optional string)
           ~doc:"STRING CustomerCwLogLevel"
       and guardrailPolicyArns =
         flag "guardrail-policy-arns" (optional json_arg)
           ~doc:"JSON GuardrailPolicyArnList"
       and userAuthorizationRequired =
         flag "user-authorization-required" (optional bool)
           ~doc:"BOOL BooleanAccountPreference"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING TeamsChannelId"
       and teamId = flag "team-id" (required string) ~doc:"STRING UUID"
       and tenantId = flag "tenant-id" (required string) ~doc:"STRING UUID"
       and iamRoleArn =
         flag "iam-role-arn" (required string) ~doc:"STRING Arn"
       and configurationName =
         flag "configuration-name" (required string)
           ~doc:"STRING ConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_microsoft_teams_channel_configuration
           (Values.CreateTeamsChannelConfigurationRequest.make ?channelName
              ?teamName
              ?snsTopicArns:(Option.map ~f:Values.SnsTopicArnList.of_json
                               snsTopicArns) ?loggingLevel
              ?guardrailPolicyArns:(Option.map
                                      ~f:Values.GuardrailPolicyArnList.of_json
                                      guardrailPolicyArns)
              ?userAuthorizationRequired
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~channelId
              ~teamId ~tenantId ~iamRoleArn ~configurationName ())
           (Some Values.CreateTeamsChannelConfigurationResult.to_json)
           (Some Values.CreateTeamsChannelConfigurationResult.error_to_json)])
let create_slack_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and slackChannelName =
         flag "slack-channel-name" (optional string)
           ~doc:"STRING SlackChannelDisplayName"
       and snsTopicArns =
         flag "sns-topic-arns" (optional json_arg)
           ~doc:"JSON SnsTopicArnList"
       and loggingLevel =
         flag "logging-level" (optional string)
           ~doc:"STRING CustomerCwLogLevel"
       and guardrailPolicyArns =
         flag "guardrail-policy-arns" (optional json_arg)
           ~doc:"JSON GuardrailPolicyArnList"
       and userAuthorizationRequired =
         flag "user-authorization-required" (optional bool)
           ~doc:"BOOL BooleanAccountPreference"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and slackTeamId =
         flag "slack-team-id" (required string) ~doc:"STRING SlackTeamId"
       and slackChannelId =
         flag "slack-channel-id" (required string)
           ~doc:"STRING SlackChannelId"
       and iamRoleArn =
         flag "iam-role-arn" (required string) ~doc:"STRING Arn"
       and configurationName =
         flag "configuration-name" (required string)
           ~doc:"STRING ConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_slack_channel_configuration
           (Values.CreateSlackChannelConfigurationRequest.make
              ?slackChannelName
              ?snsTopicArns:(Option.map ~f:Values.SnsTopicArnList.of_json
                               snsTopicArns) ?loggingLevel
              ?guardrailPolicyArns:(Option.map
                                      ~f:Values.GuardrailPolicyArnList.of_json
                                      guardrailPolicyArns)
              ?userAuthorizationRequired
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~slackTeamId
              ~slackChannelId ~iamRoleArn ~configurationName ())
           (Some Values.CreateSlackChannelConfigurationResult.to_json)
           (Some Values.CreateSlackChannelConfigurationResult.error_to_json)])
let delete_chime_webhook_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_chime_webhook_configuration
           (Values.DeleteChimeWebhookConfigurationRequest.make
              ~chatConfigurationArn ())
           (Some Values.DeleteChimeWebhookConfigurationResult.to_json)
           (Some Values.DeleteChimeWebhookConfigurationResult.error_to_json)])
let delete_custom_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customActionArn =
         flag "custom-action-arn" (required string)
           ~doc:"STRING CustomActionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_action
           (Values.DeleteCustomActionRequest.make ~customActionArn ())
           (Some Values.DeleteCustomActionResult.to_json)
           (Some Values.DeleteCustomActionResult.error_to_json)])
let delete_microsoft_teams_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_microsoft_teams_channel_configuration
           (Values.DeleteTeamsChannelConfigurationRequest.make
              ~chatConfigurationArn ())
           (Some Values.DeleteTeamsChannelConfigurationResult.to_json)
           (Some Values.DeleteTeamsChannelConfigurationResult.error_to_json)])
let delete_microsoft_teams_configured_team =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and teamId = flag "team-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_microsoft_teams_configured_team
           (Values.DeleteTeamsConfiguredTeamRequest.make ~teamId ())
           (Some Values.DeleteTeamsConfiguredTeamResult.to_json)
           (Some Values.DeleteTeamsConfiguredTeamResult.error_to_json)])
let delete_microsoft_teams_user_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn"
       and userId = flag "user-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_microsoft_teams_user_identity
           (Values.DeleteMicrosoftTeamsUserIdentityRequest.make
              ~chatConfigurationArn ~userId ())
           (Some Values.DeleteMicrosoftTeamsUserIdentityResult.to_json)
           (Some Values.DeleteMicrosoftTeamsUserIdentityResult.error_to_json)])
let delete_slack_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_slack_channel_configuration
           (Values.DeleteSlackChannelConfigurationRequest.make
              ~chatConfigurationArn ())
           (Some Values.DeleteSlackChannelConfigurationResult.to_json)
           (Some Values.DeleteSlackChannelConfigurationResult.error_to_json)])
let delete_slack_user_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn"
       and slackTeamId =
         flag "slack-team-id" (required string) ~doc:"STRING SlackTeamId"
       and slackUserId =
         flag "slack-user-id" (required string) ~doc:"STRING SlackUserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_slack_user_identity
           (Values.DeleteSlackUserIdentityRequest.make ~chatConfigurationArn
              ~slackTeamId ~slackUserId ())
           (Some Values.DeleteSlackUserIdentityResult.to_json)
           (Some Values.DeleteSlackUserIdentityResult.error_to_json)])
let delete_slack_workspace_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and slackTeamId =
         flag "slack-team-id" (required string) ~doc:"STRING SlackTeamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_slack_workspace_authorization
           (Values.DeleteSlackWorkspaceAuthorizationRequest.make ~slackTeamId
              ())
           (Some Values.DeleteSlackWorkspaceAuthorizationResult.to_json)
           (Some Values.DeleteSlackWorkspaceAuthorizationResult.error_to_json)])
let describe_chime_webhook_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (optional string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_chime_webhook_configurations
           (Values.DescribeChimeWebhookConfigurationsRequest.make ?maxResults
              ?nextToken ?chatConfigurationArn ())
           (Some Values.DescribeChimeWebhookConfigurationsResult.to_json)
           (Some
              Values.DescribeChimeWebhookConfigurationsResult.error_to_json)])
let describe_slack_channel_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (optional string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_slack_channel_configurations
           (Values.DescribeSlackChannelConfigurationsRequest.make ?maxResults
              ?nextToken ?chatConfigurationArn ())
           (Some Values.DescribeSlackChannelConfigurationsResult.to_json)
           (Some
              Values.DescribeSlackChannelConfigurationsResult.error_to_json)])
let describe_slack_user_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (optional string)
           ~doc:"STRING ChatConfigurationArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_slack_user_identities
           (Values.DescribeSlackUserIdentitiesRequest.make
              ?chatConfigurationArn ?nextToken ?maxResults ())
           (Some Values.DescribeSlackUserIdentitiesResult.to_json)
           (Some Values.DescribeSlackUserIdentitiesResult.error_to_json)])
let describe_slack_workspaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_slack_workspaces
           (Values.DescribeSlackWorkspacesRequest.make ?maxResults ?nextToken
              ()) (Some Values.DescribeSlackWorkspacesResult.to_json)
           (Some Values.DescribeSlackWorkspacesResult.error_to_json)])
let disassociate_from_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (required string) ~doc:"STRING ResourceIdentifier"
       and chatConfiguration =
         flag "chat-configuration" (required string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_from_configuration
           (Values.DisassociateFromConfigurationRequest.make ~resource
              ~chatConfiguration ())
           (Some Values.DisassociateFromConfigurationResult.to_json)
           (Some Values.DisassociateFromConfigurationResult.error_to_json)])
let get_account_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_preferences
           (Values.GetAccountPreferencesRequest.make ())
           (Some Values.GetAccountPreferencesResult.to_json)
           (Some Values.GetAccountPreferencesResult.error_to_json)])
let get_custom_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customActionArn =
         flag "custom-action-arn" (required string)
           ~doc:"STRING CustomActionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_custom_action
           (Values.GetCustomActionRequest.make ~customActionArn ())
           (Some Values.GetCustomActionResult.to_json)
           (Some Values.GetCustomActionResult.error_to_json)])
let get_microsoft_teams_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_microsoft_teams_channel_configuration
           (Values.GetTeamsChannelConfigurationRequest.make
              ~chatConfigurationArn ())
           (Some Values.GetTeamsChannelConfigurationResult.to_json)
           (Some Values.GetTeamsChannelConfigurationResult.error_to_json)])
let list_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAssociationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListAssociationsRequestNextTokenString"
       and chatConfiguration =
         flag "chat-configuration" (required string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associations
           (Values.ListAssociationsRequest.make ?maxResults ?nextToken
              ~chatConfiguration ())
           (Some Values.ListAssociationsResult.to_json)
           (Some Values.ListAssociationsResult.error_to_json)])
let list_custom_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCustomActionsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListCustomActionsRequestNextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_actions
           (Values.ListCustomActionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListCustomActionsResult.to_json)
           (Some Values.ListCustomActionsResult.error_to_json)])
let list_microsoft_teams_channel_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and teamId = flag "team-id" (optional string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_microsoft_teams_channel_configurations
           (Values.ListTeamsChannelConfigurationsRequest.make ?maxResults
              ?nextToken ?teamId ())
           (Some Values.ListTeamsChannelConfigurationsResult.to_json)
           (Some Values.ListTeamsChannelConfigurationsResult.error_to_json)])
let list_microsoft_teams_configured_teams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_microsoft_teams_configured_teams
           (Values.ListMicrosoftTeamsConfiguredTeamsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListMicrosoftTeamsConfiguredTeamsResult.to_json)
           (Some Values.ListMicrosoftTeamsConfiguredTeamsResult.error_to_json)])
let list_microsoft_teams_user_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (optional string)
           ~doc:"STRING ChatConfigurationArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_microsoft_teams_user_identities
           (Values.ListMicrosoftTeamsUserIdentitiesRequest.make
              ?chatConfigurationArn ?nextToken ?maxResults ())
           (Some Values.ListMicrosoftTeamsUserIdentitiesResult.to_json)
           (Some Values.ListMicrosoftTeamsUserIdentitiesResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_account_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userAuthorizationRequired =
         flag "user-authorization-required" (optional bool)
           ~doc:"BOOL BooleanAccountPreference"
       and trainingDataCollectionEnabled =
         flag "training-data-collection-enabled" (optional bool)
           ~doc:"BOOL BooleanAccountPreference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_account_preferences
           (Values.UpdateAccountPreferencesRequest.make
              ?userAuthorizationRequired ?trainingDataCollectionEnabled ())
           (Some Values.UpdateAccountPreferencesResult.to_json)
           (Some Values.UpdateAccountPreferencesResult.error_to_json)])
let update_chime_webhook_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webhookDescription =
         flag "webhook-description" (optional string)
           ~doc:"STRING ChimeWebhookDescription"
       and webhookUrl =
         flag "webhook-url" (optional string) ~doc:"STRING ChimeWebhookUrl"
       and snsTopicArns =
         flag "sns-topic-arns" (optional json_arg)
           ~doc:"JSON SnsTopicArnList"
       and iamRoleArn =
         flag "iam-role-arn" (optional string) ~doc:"STRING Arn"
       and loggingLevel =
         flag "logging-level" (optional string)
           ~doc:"STRING CustomerCwLogLevel"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_chime_webhook_configuration
           (Values.UpdateChimeWebhookConfigurationRequest.make
              ?webhookDescription ?webhookUrl
              ?snsTopicArns:(Option.map ~f:Values.SnsTopicArnList.of_json
                               snsTopicArns) ?iamRoleArn ?loggingLevel
              ~chatConfigurationArn ())
           (Some Values.UpdateChimeWebhookConfigurationResult.to_json)
           (Some Values.UpdateChimeWebhookConfigurationResult.error_to_json)])
let update_custom_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasName =
         flag "alias-name" (optional string)
           ~doc:"STRING CustomActionAliasName"
       and attachments =
         flag "attachments" (optional json_arg)
           ~doc:"JSON CustomActionAttachmentList"
       and customActionArn =
         flag "custom-action-arn" (required string)
           ~doc:"STRING CustomActionArn"
       and definition =
         flag "definition" (required json_arg)
           ~doc:"JSON CustomActionDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_action
           (Values.UpdateCustomActionRequest.make ?aliasName
              ?attachments:(Option.map
                              ~f:Values.CustomActionAttachmentList.of_json
                              attachments) ~customActionArn
              ~definition:(Values.CustomActionDefinition.of_json definition)
              ()) (Some Values.UpdateCustomActionResult.to_json)
           (Some Values.UpdateCustomActionResult.error_to_json)])
let update_microsoft_teams_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelName =
         flag "channel-name" (optional string) ~doc:"STRING TeamsChannelName"
       and snsTopicArns =
         flag "sns-topic-arns" (optional json_arg)
           ~doc:"JSON SnsTopicArnList"
       and iamRoleArn =
         flag "iam-role-arn" (optional string) ~doc:"STRING Arn"
       and loggingLevel =
         flag "logging-level" (optional string)
           ~doc:"STRING CustomerCwLogLevel"
       and guardrailPolicyArns =
         flag "guardrail-policy-arns" (optional json_arg)
           ~doc:"JSON GuardrailPolicyArnList"
       and userAuthorizationRequired =
         flag "user-authorization-required" (optional bool)
           ~doc:"BOOL BooleanAccountPreference"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING TeamsChannelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_microsoft_teams_channel_configuration
           (Values.UpdateTeamsChannelConfigurationRequest.make ?channelName
              ?snsTopicArns:(Option.map ~f:Values.SnsTopicArnList.of_json
                               snsTopicArns) ?iamRoleArn ?loggingLevel
              ?guardrailPolicyArns:(Option.map
                                      ~f:Values.GuardrailPolicyArnList.of_json
                                      guardrailPolicyArns)
              ?userAuthorizationRequired ~chatConfigurationArn ~channelId ())
           (Some Values.UpdateTeamsChannelConfigurationResult.to_json)
           (Some Values.UpdateTeamsChannelConfigurationResult.error_to_json)])
let update_slack_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and slackChannelName =
         flag "slack-channel-name" (optional string)
           ~doc:"STRING SlackChannelDisplayName"
       and snsTopicArns =
         flag "sns-topic-arns" (optional json_arg)
           ~doc:"JSON SnsTopicArnList"
       and iamRoleArn =
         flag "iam-role-arn" (optional string) ~doc:"STRING Arn"
       and loggingLevel =
         flag "logging-level" (optional string)
           ~doc:"STRING CustomerCwLogLevel"
       and guardrailPolicyArns =
         flag "guardrail-policy-arns" (optional json_arg)
           ~doc:"JSON GuardrailPolicyArnList"
       and userAuthorizationRequired =
         flag "user-authorization-required" (optional bool)
           ~doc:"BOOL BooleanAccountPreference"
       and chatConfigurationArn =
         flag "chat-configuration-arn" (required string)
           ~doc:"STRING ChatConfigurationArn"
       and slackChannelId =
         flag "slack-channel-id" (required string)
           ~doc:"STRING SlackChannelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_slack_channel_configuration
           (Values.UpdateSlackChannelConfigurationRequest.make
              ?slackChannelName
              ?snsTopicArns:(Option.map ~f:Values.SnsTopicArnList.of_json
                               snsTopicArns) ?iamRoleArn ?loggingLevel
              ?guardrailPolicyArns:(Option.map
                                      ~f:Values.GuardrailPolicyArnList.of_json
                                      guardrailPolicyArns)
              ?userAuthorizationRequired ~chatConfigurationArn
              ~slackChannelId ())
           (Some Values.UpdateSlackChannelConfigurationResult.to_json)
           (Some Values.UpdateSlackChannelConfigurationResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-to-configuration", associate_to_configuration);
    ("create-chime-webhook-configuration",
      create_chime_webhook_configuration);
    ("create-custom-action", create_custom_action);
    ("create-microsoft-teams-channel-configuration",
      create_microsoft_teams_channel_configuration);
    ("create-slack-channel-configuration",
      create_slack_channel_configuration);
    ("delete-chime-webhook-configuration",
      delete_chime_webhook_configuration);
    ("delete-custom-action", delete_custom_action);
    ("delete-microsoft-teams-channel-configuration",
      delete_microsoft_teams_channel_configuration);
    ("delete-microsoft-teams-configured-team",
      delete_microsoft_teams_configured_team);
    ("delete-microsoft-teams-user-identity",
      delete_microsoft_teams_user_identity);
    ("delete-slack-channel-configuration",
      delete_slack_channel_configuration);
    ("delete-slack-user-identity", delete_slack_user_identity);
    ("delete-slack-workspace-authorization",
      delete_slack_workspace_authorization);
    ("describe-chime-webhook-configurations",
      describe_chime_webhook_configurations);
    ("describe-slack-channel-configurations",
      describe_slack_channel_configurations);
    ("describe-slack-user-identities", describe_slack_user_identities);
    ("describe-slack-workspaces", describe_slack_workspaces);
    ("disassociate-from-configuration", disassociate_from_configuration);
    ("get-account-preferences", get_account_preferences);
    ("get-custom-action", get_custom_action);
    ("get-microsoft-teams-channel-configuration",
      get_microsoft_teams_channel_configuration);
    ("list-associations", list_associations);
    ("list-custom-actions", list_custom_actions);
    ("list-microsoft-teams-channel-configurations",
      list_microsoft_teams_channel_configurations);
    ("list-microsoft-teams-configured-teams",
      list_microsoft_teams_configured_teams);
    ("list-microsoft-teams-user-identities",
      list_microsoft_teams_user_identities);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-account-preferences", update_account_preferences);
    ("update-chime-webhook-configuration",
      update_chime_webhook_configuration);
    ("update-custom-action", update_custom_action);
    ("update-microsoft-teams-channel-configuration",
      update_microsoft_teams_channel_configuration);
    ("update-slack-channel-configuration",
      update_slack_channel_configuration)]
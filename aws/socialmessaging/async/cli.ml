(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_whats_app_business_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and signupCallback =
         flag "signup-callback" (optional json_arg)
           ~doc:"JSON WhatsAppSignupCallback"
       and setupFinalization =
         flag "setup-finalization" (optional json_arg)
           ~doc:"JSON WhatsAppSetupFinalization" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_whats_app_business_account
           (Values.AssociateWhatsAppBusinessAccountInput.make
              ?signupCallback:(Option.map
                                 ~f:Values.WhatsAppSignupCallback.of_json
                                 signupCallback)
              ?setupFinalization:(Option.map
                                    ~f:Values.WhatsAppSetupFinalization.of_json
                                    setupFinalization) ())
           (Some Values.AssociateWhatsAppBusinessAccountOutput.to_json)
           (Some Values.AssociateWhatsAppBusinessAccountOutput.error_to_json)])
let create_whats_app_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateDefinition =
         flag "template-definition" (required json_arg)
           ~doc:"JSON MetaTemplateDefinition"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_whats_app_message_template
           (Values.CreateWhatsAppMessageTemplateInput.make
              ~templateDefinition:(Values.MetaTemplateDefinition.of_json
                                     templateDefinition) ~id ())
           (Some Values.CreateWhatsAppMessageTemplateOutput.to_json)
           (Some Values.CreateWhatsAppMessageTemplateOutput.error_to_json)])
let create_whats_app_message_template_from_library =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metaLibraryTemplate =
         flag "meta-library-template" (required json_arg)
           ~doc:"JSON MetaLibraryTemplate"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_whats_app_message_template_from_library
           (Values.CreateWhatsAppMessageTemplateFromLibraryInput.make
              ~metaLibraryTemplate:(Values.MetaLibraryTemplate.of_json
                                      metaLibraryTemplate) ~id ())
           (Some
              Values.CreateWhatsAppMessageTemplateFromLibraryOutput.to_json)
           (Some
              Values.CreateWhatsAppMessageTemplateFromLibraryOutput.error_to_json)])
let create_whats_app_message_template_media =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceS3File =
         flag "source-s3-file" (optional json_arg) ~doc:"JSON S3File"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_whats_app_message_template_media
           (Values.CreateWhatsAppMessageTemplateMediaInput.make
              ?sourceS3File:(Option.map ~f:Values.S3File.of_json sourceS3File)
              ~id ())
           (Some Values.CreateWhatsAppMessageTemplateMediaOutput.to_json)
           (Some
              Values.CreateWhatsAppMessageTemplateMediaOutput.error_to_json)])
let delete_whats_app_message_media =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mediaId =
         flag "media-id" (required string) ~doc:"STRING WhatsAppMediaId"
       and originationPhoneNumberId =
         flag "origination-phone-number-id" (required string)
           ~doc:"STRING WhatsAppPhoneNumberId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_whats_app_message_media
           (Values.DeleteWhatsAppMessageMediaInput.make ~mediaId
              ~originationPhoneNumberId ())
           (Some Values.DeleteWhatsAppMessageMediaOutput.to_json)
           (Some Values.DeleteWhatsAppMessageMediaOutput.error_to_json)])
let delete_whats_app_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metaTemplateId =
         flag "meta-template-id" (optional string)
           ~doc:"STRING MetaTemplateId"
       and deleteAllLanguages =
         flag "delete-all-languages" (optional bool)
           ~doc:"BOOL DeleteAllLanguages"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING MetaTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_whats_app_message_template
           (Values.DeleteWhatsAppMessageTemplateInput.make ?metaTemplateId
              ?deleteAllLanguages ~id ~templateName ())
           (Some Values.DeleteWhatsAppMessageTemplateOutput.to_json)
           (Some Values.DeleteWhatsAppMessageTemplateOutput.error_to_json)])
let disassociate_whats_app_business_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_whats_app_business_account
           (Values.DisassociateWhatsAppBusinessAccountInput.make ~id ())
           (Some Values.DisassociateWhatsAppBusinessAccountOutput.to_json)
           (Some
              Values.DisassociateWhatsAppBusinessAccountOutput.error_to_json)])
let get_linked_whats_app_business_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_linked_whats_app_business_account
           (Values.GetLinkedWhatsAppBusinessAccountInput.make ~id ())
           (Some Values.GetLinkedWhatsAppBusinessAccountOutput.to_json)
           (Some Values.GetLinkedWhatsAppBusinessAccountOutput.error_to_json)])
let get_linked_whats_app_business_account_phone_number =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING WhatsAppPhoneNumberId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_linked_whats_app_business_account_phone_number
           (Values.GetLinkedWhatsAppBusinessAccountPhoneNumberInput.make ~id
              ())
           (Some
              Values.GetLinkedWhatsAppBusinessAccountPhoneNumberOutput.to_json)
           (Some
              Values.GetLinkedWhatsAppBusinessAccountPhoneNumberOutput.error_to_json)])
let get_whats_app_message_media =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadataOnly =
         flag "metadata-only" (optional bool) ~doc:"BOOL Boolean"
       and destinationS3PresignedUrl =
         flag "destination-s3-presigned-url" (optional json_arg)
           ~doc:"JSON S3PresignedUrl"
       and destinationS3File =
         flag "destination-s3-file" (optional json_arg) ~doc:"JSON S3File"
       and mediaId =
         flag "media-id" (required string) ~doc:"STRING WhatsAppMediaId"
       and originationPhoneNumberId =
         flag "origination-phone-number-id" (required string)
           ~doc:"STRING WhatsAppPhoneNumberId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_whats_app_message_media
           (Values.GetWhatsAppMessageMediaInput.make ?metadataOnly
              ?destinationS3PresignedUrl:(Option.map
                                            ~f:Values.S3PresignedUrl.of_json
                                            destinationS3PresignedUrl)
              ?destinationS3File:(Option.map ~f:Values.S3File.of_json
                                    destinationS3File) ~mediaId
              ~originationPhoneNumberId ())
           (Some Values.GetWhatsAppMessageMediaOutput.to_json)
           (Some Values.GetWhatsAppMessageMediaOutput.error_to_json)])
let get_whats_app_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metaTemplateId =
         flag "meta-template-id" (optional string)
           ~doc:"STRING MetaTemplateId"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING MetaTemplateName"
       and templateLanguageCode =
         flag "template-language-code" (optional string)
           ~doc:"STRING MetaTemplateLanguage"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_whats_app_message_template
           (Values.GetWhatsAppMessageTemplateInput.make ?metaTemplateId
              ?templateName ?templateLanguageCode ~id ())
           (Some Values.GetWhatsAppMessageTemplateOutput.to_json)
           (Some Values.GetWhatsAppMessageTemplateOutput.error_to_json)])
let list_linked_whats_app_business_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_linked_whats_app_business_accounts
           (Values.ListLinkedWhatsAppBusinessAccountsInput.make ?nextToken
              ?maxResults ())
           (Some Values.ListLinkedWhatsAppBusinessAccountsOutput.to_json)
           (Some
              Values.ListLinkedWhatsAppBusinessAccountsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_whats_app_message_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_whats_app_message_templates
           (Values.ListWhatsAppMessageTemplatesInput.make ?nextToken
              ?maxResults ~id ())
           (Some Values.ListWhatsAppMessageTemplatesOutput.to_json)
           (Some Values.ListWhatsAppMessageTemplatesOutput.error_to_json)])
let list_whats_app_template_library =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filter"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_whats_app_template_library
           (Values.ListWhatsAppTemplateLibraryInput.make ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.Filter.of_json filters) ~id ())
           (Some Values.ListWhatsAppTemplateLibraryOutput.to_json)
           (Some Values.ListWhatsAppTemplateLibraryOutput.error_to_json)])
let post_whats_app_message_media =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceS3PresignedUrl =
         flag "source-s3-presigned-url" (optional json_arg)
           ~doc:"JSON S3PresignedUrl"
       and sourceS3File =
         flag "source-s3-file" (optional json_arg) ~doc:"JSON S3File"
       and originationPhoneNumberId =
         flag "origination-phone-number-id" (required string)
           ~doc:"STRING WhatsAppPhoneNumberId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_whats_app_message_media
           (Values.PostWhatsAppMessageMediaInput.make
              ?sourceS3PresignedUrl:(Option.map
                                       ~f:Values.S3PresignedUrl.of_json
                                       sourceS3PresignedUrl)
              ?sourceS3File:(Option.map ~f:Values.S3File.of_json sourceS3File)
              ~originationPhoneNumberId ())
           (Some Values.PostWhatsAppMessageMediaOutput.to_json)
           (Some Values.PostWhatsAppMessageMediaOutput.error_to_json)])
let put_whats_app_business_account_event_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId"
       and eventDestinations =
         flag "event-destinations" (required json_arg)
           ~doc:"JSON WhatsAppBusinessAccountEventDestinations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_whats_app_business_account_event_destinations
           (Values.PutWhatsAppBusinessAccountEventDestinationsInput.make ~id
              ~eventDestinations:(Values.WhatsAppBusinessAccountEventDestinations.of_json
                                    eventDestinations) ())
           (Some
              Values.PutWhatsAppBusinessAccountEventDestinationsOutput.to_json)
           (Some
              Values.PutWhatsAppBusinessAccountEventDestinationsOutput.error_to_json)])
let send_whats_app_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and originationPhoneNumberId =
         flag "origination-phone-number-id" (required string)
           ~doc:"STRING WhatsAppPhoneNumberId"
       and message =
         flag "message" (required json_arg) ~doc:"JSON WhatsAppMessageBlob"
       and metaApiVersion =
         flag "meta-api-version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_whats_app_message
           (Values.SendWhatsAppMessageInput.make ~originationPhoneNumberId
              ~message:(Values.WhatsAppMessageBlob.of_json message)
              ~metaApiVersion ())
           (Some Values.SendWhatsAppMessageOutput.to_json)
           (Some Values.SendWhatsAppMessageOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.StringList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_whats_app_message_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metaTemplateId =
         flag "meta-template-id" (optional string)
           ~doc:"STRING MetaTemplateId"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING MetaTemplateName"
       and templateLanguageCode =
         flag "template-language-code" (optional string)
           ~doc:"STRING MetaTemplateLanguage"
       and parameterFormat =
         flag "parameter-format" (optional string)
           ~doc:"STRING MetaParameterFormat"
       and templateCategory =
         flag "template-category" (optional string)
           ~doc:"STRING MetaTemplateCategory"
       and templateComponents =
         flag "template-components" (optional json_arg)
           ~doc:"JSON MetaTemplateComponents"
       and ctaUrlLinkTrackingOptedOut =
         flag "cta-url-link-tracking-opted-out" (optional bool)
           ~doc:"BOOL MetaTemplateCtaLinkTrackingOptedOut"
       and id =
         flag "id" (required string)
           ~doc:"STRING LinkedWhatsAppBusinessAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_whats_app_message_template
           (Values.UpdateWhatsAppMessageTemplateInput.make ?metaTemplateId
              ?templateName ?templateLanguageCode ?parameterFormat
              ?templateCategory
              ?templateComponents:(Option.map
                                     ~f:Values.MetaTemplateComponents.of_json
                                     templateComponents)
              ?ctaUrlLinkTrackingOptedOut ~id ())
           (Some Values.UpdateWhatsAppMessageTemplateOutput.to_json)
           (Some Values.UpdateWhatsAppMessageTemplateOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-whats-app-business-account",
       associate_whats_app_business_account);
    ("create-whats-app-message-template", create_whats_app_message_template);
    ("create-whats-app-message-template-from-library",
      create_whats_app_message_template_from_library);
    ("create-whats-app-message-template-media",
      create_whats_app_message_template_media);
    ("delete-whats-app-message-media", delete_whats_app_message_media);
    ("delete-whats-app-message-template", delete_whats_app_message_template);
    ("disassociate-whats-app-business-account",
      disassociate_whats_app_business_account);
    ("get-linked-whats-app-business-account",
      get_linked_whats_app_business_account);
    ("get-linked-whats-app-business-account-phone-number",
      get_linked_whats_app_business_account_phone_number);
    ("get-whats-app-message-media", get_whats_app_message_media);
    ("get-whats-app-message-template", get_whats_app_message_template);
    ("list-linked-whats-app-business-accounts",
      list_linked_whats_app_business_accounts);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-whats-app-message-templates", list_whats_app_message_templates);
    ("list-whats-app-template-library", list_whats_app_template_library);
    ("post-whats-app-message-media", post_whats_app_message_media);
    ("put-whats-app-business-account-event-destinations",
      put_whats_app_business_account_event_destinations);
    ("send-whats-app-message", send_whats_app_message);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-whats-app-message-template", update_whats_app_message_template)]
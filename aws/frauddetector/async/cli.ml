(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_create_variable =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and variableEntries =
         flag "variable-entries" (required json_arg)
           ~doc:"JSON VariableEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_variable
           (Values.BatchCreateVariableRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~variableEntries:(Values.VariableEntryList.of_json
                                  variableEntries) ())
           (Some Values.BatchCreateVariableResult.to_json)
           (Some Values.BatchCreateVariableResult.error_to_json)])
let batch_get_variable =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names = flag "names" (required json_arg) ~doc:"JSON NameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_variable
           (Values.BatchGetVariableRequest.make
              ~names:(Values.NameList.of_json names) ())
           (Some Values.BatchGetVariableResult.to_json)
           (Some Values.BatchGetVariableResult.error_to_json)])
let cancel_batch_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_batch_import_job
           (Values.CancelBatchImportJobRequest.make ~jobId ())
           (Some Values.CancelBatchImportJobResult.to_json)
           (Some Values.CancelBatchImportJobResult.error_to_json)])
let cancel_batch_prediction_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_batch_prediction_job
           (Values.CancelBatchPredictionJobRequest.make ~jobId ())
           (Some Values.CancelBatchPredictionJobResult.to_json)
           (Some Values.CancelBatchPredictionJobResult.error_to_json)])
let create_batch_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and jobId = flag "job-id" (required string) ~doc:"STRING identifier"
       and inputPath =
         flag "input-path" (required string) ~doc:"STRING s3BucketLocation"
       and outputPath =
         flag "output-path" (required string) ~doc:"STRING s3BucketLocation"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier"
       and iamRoleArn =
         flag "iam-role-arn" (required string) ~doc:"STRING iamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_batch_import_job
           (Values.CreateBatchImportJobRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~jobId
              ~inputPath ~outputPath ~eventTypeName ~iamRoleArn ())
           (Some Values.CreateBatchImportJobResult.to_json)
           (Some Values.CreateBatchImportJobResult.error_to_json)])
let create_batch_prediction_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectorVersion =
         flag "detector-version" (optional string)
           ~doc:"STRING wholeNumberVersionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and jobId = flag "job-id" (required string) ~doc:"STRING identifier"
       and inputPath =
         flag "input-path" (required string) ~doc:"STRING s3BucketLocation"
       and outputPath =
         flag "output-path" (required string) ~doc:"STRING s3BucketLocation"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier"
       and detectorName =
         flag "detector-name" (required string) ~doc:"STRING identifier"
       and iamRoleArn =
         flag "iam-role-arn" (required string) ~doc:"STRING iamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_batch_prediction_job
           (Values.CreateBatchPredictionJobRequest.make ?detectorVersion
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~jobId
              ~inputPath ~outputPath ~eventTypeName ~detectorName ~iamRoleArn
              ()) (Some Values.CreateBatchPredictionJobResult.to_json)
           (Some Values.CreateBatchPredictionJobResult.error_to_json)])
let create_detector_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and externalModelEndpoints =
         flag "external-model-endpoints" (optional json_arg)
           ~doc:"JSON ListOfStrings"
       and modelVersions =
         flag "model-versions" (optional json_arg)
           ~doc:"JSON ListOfModelVersions"
       and ruleExecutionMode =
         flag "rule-execution-mode" (optional json_arg)
           ~doc:"JSON RuleExecutionMode"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and rules = flag "rules" (required json_arg) ~doc:"JSON RuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_detector_version
           (Values.CreateDetectorVersionRequest.make ?description
              ?externalModelEndpoints:(Option.map
                                         ~f:Values.ListOfStrings.of_json
                                         externalModelEndpoints)
              ?modelVersions:(Option.map
                                ~f:Values.ListOfModelVersions.of_json
                                modelVersions)
              ?ruleExecutionMode:(Option.map
                                    ~f:Values.RuleExecutionMode.of_json
                                    ruleExecutionMode)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~detectorId
              ~rules:(Values.RuleList.of_json rules) ())
           (Some Values.CreateDetectorVersionResult.to_json)
           (Some Values.CreateDetectorVersionResult.error_to_json)])
let create_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (required json_arg) ~doc:"JSON ModelTypeEnum"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model
           (Values.CreateModelRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~modelId
              ~modelType:(Values.ModelTypeEnum.of_json modelType)
              ~eventTypeName ()) (Some Values.CreateModelResult.to_json)
           (Some Values.CreateModelResult.error_to_json)])
let create_model_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and externalEventsDetail =
         flag "external-events-detail" (optional json_arg)
           ~doc:"JSON ExternalEventsDetail"
       and ingestedEventsDetail =
         flag "ingested-events-detail" (optional json_arg)
           ~doc:"JSON IngestedEventsDetail"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (required json_arg) ~doc:"JSON ModelTypeEnum"
       and trainingDataSource =
         flag "training-data-source" (required json_arg)
           ~doc:"JSON TrainingDataSourceEnum"
       and trainingDataSchema =
         flag "training-data-schema" (required json_arg)
           ~doc:"JSON TrainingDataSchema" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_version
           (Values.CreateModelVersionRequest.make
              ?externalEventsDetail:(Option.map
                                       ~f:Values.ExternalEventsDetail.of_json
                                       externalEventsDetail)
              ?ingestedEventsDetail:(Option.map
                                       ~f:Values.IngestedEventsDetail.of_json
                                       ingestedEventsDetail)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~modelId
              ~modelType:(Values.ModelTypeEnum.of_json modelType)
              ~trainingDataSource:(Values.TrainingDataSourceEnum.of_json
                                     trainingDataSource)
              ~trainingDataSchema:(Values.TrainingDataSchema.of_json
                                     trainingDataSchema) ())
           (Some Values.CreateModelVersionResult.to_json)
           (Some Values.CreateModelVersionResult.error_to_json)])
let create_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING identifier"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and expression =
         flag "expression" (required string) ~doc:"STRING ruleExpression"
       and language =
         flag "language" (required json_arg) ~doc:"JSON Language"
       and outcomes =
         flag "outcomes" (required json_arg)
           ~doc:"JSON NonEmptyListOfStrings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rule
           (Values.CreateRuleRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~ruleId
              ~detectorId ~expression
              ~language:(Values.Language.of_json language)
              ~outcomes:(Values.NonEmptyListOfStrings.of_json outcomes) ())
           (Some Values.CreateRuleResult.to_json)
           (Some Values.CreateRuleResult.error_to_json)])
let create_variable =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING string"
       and variableType =
         flag "variable-type" (optional string) ~doc:"STRING string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and name = flag "name" (required string) ~doc:"STRING string"
       and dataType =
         flag "data-type" (required json_arg) ~doc:"JSON DataType"
       and dataSource =
         flag "data-source" (required json_arg) ~doc:"JSON DataSource"
       and defaultValue =
         flag "default-value" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_variable
           (Values.CreateVariableRequest.make ?description ?variableType
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~dataType:(Values.DataType.of_json dataType)
              ~dataSource:(Values.DataSource.of_json dataSource)
              ~defaultValue ()) (Some Values.CreateVariableResult.to_json)
           (Some Values.CreateVariableResult.error_to_json)])
let delete_batch_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_batch_import_job
           (Values.DeleteBatchImportJobRequest.make ~jobId ())
           (Some Values.DeleteBatchImportJobResult.to_json)
           (Some Values.DeleteBatchImportJobResult.error_to_json)])
let delete_batch_prediction_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_batch_prediction_job
           (Values.DeleteBatchPredictionJobRequest.make ~jobId ())
           (Some Values.DeleteBatchPredictionJobResult.to_json)
           (Some Values.DeleteBatchPredictionJobResult.error_to_json)])
let delete_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_detector
           (Values.DeleteDetectorRequest.make ~detectorId ())
           (Some Values.DeleteDetectorResult.to_json)
           (Some Values.DeleteDetectorResult.error_to_json)])
let delete_detector_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and detectorVersionId =
         flag "detector-version-id" (required string)
           ~doc:"STRING wholeNumberVersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_detector_version
           (Values.DeleteDetectorVersionRequest.make ~detectorId
              ~detectorVersionId ())
           (Some Values.DeleteDetectorVersionResult.to_json)
           (Some Values.DeleteDetectorVersionResult.error_to_json)])
let delete_entity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_entity_type
           (Values.DeleteEntityTypeRequest.make ~name ())
           (Some Values.DeleteEntityTypeResult.to_json)
           (Some Values.DeleteEntityTypeResult.error_to_json)])
let delete_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteAuditHistory =
         flag "delete-audit-history" (optional bool)
           ~doc:"BOOL DeleteAuditHistory"
       and eventId =
         flag "event-id" (required string) ~doc:"STRING identifier"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event
           (Values.DeleteEventRequest.make ?deleteAuditHistory ~eventId
              ~eventTypeName ()) (Some Values.DeleteEventResult.to_json)
           (Some Values.DeleteEventResult.error_to_json)])
let delete_event_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_type (Values.DeleteEventTypeRequest.make ~name ())
           (Some Values.DeleteEventTypeResult.to_json)
           (Some Values.DeleteEventTypeResult.error_to_json)])
let delete_events_by_event_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_events_by_event_type
           (Values.DeleteEventsByEventTypeRequest.make ~eventTypeName ())
           (Some Values.DeleteEventsByEventTypeResult.to_json)
           (Some Values.DeleteEventsByEventTypeResult.error_to_json)])
let delete_external_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelEndpoint =
         flag "model-endpoint" (required string)
           ~doc:"STRING sageMakerEndpointIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_external_model
           (Values.DeleteExternalModelRequest.make ~modelEndpoint ())
           (Some Values.DeleteExternalModelResult.to_json)
           (Some Values.DeleteExternalModelResult.error_to_json)])
let delete_label =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_label (Values.DeleteLabelRequest.make ~name ())
           (Some Values.DeleteLabelResult.to_json)
           (Some Values.DeleteLabelResult.error_to_json)])
let delete_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (required json_arg) ~doc:"JSON ModelTypeEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model
           (Values.DeleteModelRequest.make ~modelId
              ~modelType:(Values.ModelTypeEnum.of_json modelType) ())
           (Some Values.DeleteModelResult.to_json)
           (Some Values.DeleteModelResult.error_to_json)])
let delete_model_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (required json_arg) ~doc:"JSON ModelTypeEnum"
       and modelVersionNumber =
         flag "model-version-number" (required string)
           ~doc:"STRING floatVersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_version
           (Values.DeleteModelVersionRequest.make ~modelId
              ~modelType:(Values.ModelTypeEnum.of_json modelType)
              ~modelVersionNumber ())
           (Some Values.DeleteModelVersionResult.to_json)
           (Some Values.DeleteModelVersionResult.error_to_json)])
let delete_outcome =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_outcome (Values.DeleteOutcomeRequest.make ~name ())
           (Some Values.DeleteOutcomeResult.to_json)
           (Some Values.DeleteOutcomeResult.error_to_json)])
let delete_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rule = flag "rule" (required json_arg) ~doc:"JSON Rule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule
           (Values.DeleteRuleRequest.make ~rule:(Values.Rule.of_json rule) ())
           (Some Values.DeleteRuleResult.to_json)
           (Some Values.DeleteRuleResult.error_to_json)])
let delete_variable =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_variable (Values.DeleteVariableRequest.make ~name ())
           (Some Values.DeleteVariableResult.to_json)
           (Some Values.DeleteVariableResult.error_to_json)])
let describe_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DetectorVersionMaxResults"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_detector
           (Values.DescribeDetectorRequest.make ?nextToken ?maxResults
              ~detectorId ()) (Some Values.DescribeDetectorResult.to_json)
           (Some Values.DescribeDetectorResult.error_to_json)])
let describe_model_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (optional string) ~doc:"STRING modelIdentifier"
       and modelVersionNumber =
         flag "model-version-number" (optional string)
           ~doc:"STRING floatVersionString"
       and modelType =
         flag "model-type" (optional json_arg) ~doc:"JSON ModelTypeEnum"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT modelsMaxPageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_model_versions
           (Values.DescribeModelVersionsRequest.make ?modelId
              ?modelVersionNumber
              ?modelType:(Option.map ~f:Values.ModelTypeEnum.of_json
                            modelType) ?nextToken ?maxResults ())
           (Some Values.DescribeModelVersionsResult.to_json)
           (Some Values.DescribeModelVersionsResult.error_to_json)])
let get_batch_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (optional string) ~doc:"STRING identifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT batchImportsMaxPageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_batch_import_jobs
           (Values.GetBatchImportJobsRequest.make ?jobId ?maxResults
              ?nextToken ()) (Some Values.GetBatchImportJobsResult.to_json)
           (Some Values.GetBatchImportJobsResult.error_to_json)])
let get_batch_prediction_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (optional string) ~doc:"STRING identifier"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT batchPredictionsMaxPageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_batch_prediction_jobs
           (Values.GetBatchPredictionJobsRequest.make ?jobId ?maxResults
              ?nextToken ())
           (Some Values.GetBatchPredictionJobsResult.to_json)
           (Some Values.GetBatchPredictionJobsResult.error_to_json)])
let get_delete_events_by_event_type_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_delete_events_by_event_type_status
           (Values.GetDeleteEventsByEventTypeStatusRequest.make
              ~eventTypeName ())
           (Some Values.GetDeleteEventsByEventTypeStatusResult.to_json)
           (Some Values.GetDeleteEventsByEventTypeStatusResult.error_to_json)])
let get_detector_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and detectorVersionId =
         flag "detector-version-id" (required string)
           ~doc:"STRING wholeNumberVersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_detector_version
           (Values.GetDetectorVersionRequest.make ~detectorId
              ~detectorVersionId ())
           (Some Values.GetDetectorVersionResult.to_json)
           (Some Values.GetDetectorVersionResult.error_to_json)])
let get_detectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectorId =
         flag "detector-id" (optional string) ~doc:"STRING identifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DetectorsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_detectors
           (Values.GetDetectorsRequest.make ?detectorId ?nextToken
              ?maxResults ()) (Some Values.GetDetectorsResult.to_json)
           (Some Values.GetDetectorsResult.error_to_json)])
let get_entity_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING identifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT entityTypesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_entity_types
           (Values.GetEntityTypesRequest.make ?name ?nextToken ?maxResults ())
           (Some Values.GetEntityTypesResult.to_json)
           (Some Values.GetEntityTypesResult.error_to_json)])
let get_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventId = flag "event-id" (required string) ~doc:"STRING string"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event
           (Values.GetEventRequest.make ~eventId ~eventTypeName ())
           (Some Values.GetEventResult.to_json)
           (Some Values.GetEventResult.error_to_json)])
let get_event_prediction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectorVersionId =
         flag "detector-version-id" (optional string)
           ~doc:"STRING wholeNumberVersionString"
       and externalModelEndpointDataBlobs =
         flag "external-model-endpoint-data-blobs" (optional json_arg)
           ~doc:"JSON ExternalModelEndpointDataBlobMap"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING string"
       and eventId = flag "event-id" (required string) ~doc:"STRING string"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING string"
       and entities =
         flag "entities" (required json_arg) ~doc:"JSON listOfEntities"
       and eventTimestamp =
         flag "event-timestamp" (required string)
           ~doc:"STRING utcTimestampISO8601"
       and eventVariables =
         flag "event-variables" (required json_arg)
           ~doc:"JSON EventVariableMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_prediction
           (Values.GetEventPredictionRequest.make ?detectorVersionId
              ?externalModelEndpointDataBlobs:(Option.map
                                                 ~f:Values.ExternalModelEndpointDataBlobMap.of_json
                                                 externalModelEndpointDataBlobs)
              ~detectorId ~eventId ~eventTypeName
              ~entities:(Values.ListOfEntities.of_json entities)
              ~eventTimestamp
              ~eventVariables:(Values.EventVariableMap.of_json eventVariables)
              ()) (Some Values.GetEventPredictionResult.to_json)
           (Some Values.GetEventPredictionResult.error_to_json)])
let get_event_prediction_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventId =
         flag "event-id" (required string) ~doc:"STRING identifier"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and detectorVersionId =
         flag "detector-version-id" (required string)
           ~doc:"STRING wholeNumberVersionString"
       and predictionTimestamp =
         flag "prediction-timestamp" (required string) ~doc:"STRING time" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_prediction_metadata
           (Values.GetEventPredictionMetadataRequest.make ~eventId
              ~eventTypeName ~detectorId ~detectorVersionId
              ~predictionTimestamp ())
           (Some Values.GetEventPredictionMetadataResult.to_json)
           (Some Values.GetEventPredictionMetadataResult.error_to_json)])
let get_event_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING identifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT eventTypesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_types
           (Values.GetEventTypesRequest.make ?name ?nextToken ?maxResults ())
           (Some Values.GetEventTypesResult.to_json)
           (Some Values.GetEventTypesResult.error_to_json)])
let get_external_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelEndpoint =
         flag "model-endpoint" (optional string) ~doc:"STRING string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ExternalModelsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_external_models
           (Values.GetExternalModelsRequest.make ?modelEndpoint ?nextToken
              ?maxResults ()) (Some Values.GetExternalModelsResult.to_json)
           (Some Values.GetExternalModelsResult.error_to_json)])
let get_k_m_s_encryption_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_k_m_s_encryption_key (Fn.id ())
           (Some Values.GetKMSEncryptionKeyResult.to_json)
           (Some Values.GetKMSEncryptionKeyResult.error_to_json)])
let get_labels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING identifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT labelsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_labels
           (Values.GetLabelsRequest.make ?name ?nextToken ?maxResults ())
           (Some Values.GetLabelsResult.to_json)
           (Some Values.GetLabelsResult.error_to_json)])
let get_model_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (required json_arg) ~doc:"JSON ModelTypeEnum"
       and modelVersionNumber =
         flag "model-version-number" (required string)
           ~doc:"STRING floatVersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_version
           (Values.GetModelVersionRequest.make ~modelId
              ~modelType:(Values.ModelTypeEnum.of_json modelType)
              ~modelVersionNumber ())
           (Some Values.GetModelVersionResult.to_json)
           (Some Values.GetModelVersionResult.error_to_json)])
let get_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (optional string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (optional json_arg) ~doc:"JSON ModelTypeEnum"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT modelsMaxPageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_models
           (Values.GetModelsRequest.make ?modelId
              ?modelType:(Option.map ~f:Values.ModelTypeEnum.of_json
                            modelType) ?nextToken ?maxResults ())
           (Some Values.GetModelsResult.to_json)
           (Some Values.GetModelsResult.error_to_json)])
let get_outcomes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING identifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT OutcomesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_outcomes
           (Values.GetOutcomesRequest.make ?name ?nextToken ?maxResults ())
           (Some Values.GetOutcomesResult.to_json)
           (Some Values.GetOutcomesResult.error_to_json)])
let get_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleId = flag "rule-id" (optional string) ~doc:"STRING identifier"
       and ruleVersion =
         flag "rule-version" (optional string)
           ~doc:"STRING wholeNumberVersionString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT RulesMaxResults"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rules
           (Values.GetRulesRequest.make ?ruleId ?ruleVersion ?nextToken
              ?maxResults ~detectorId ())
           (Some Values.GetRulesResult.to_json)
           (Some Values.GetRulesResult.error_to_json)])
let get_variables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT VariablesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_variables
           (Values.GetVariablesRequest.make ?name ?nextToken ?maxResults ())
           (Some Values.GetVariablesResult.to_json)
           (Some Values.GetVariablesResult.error_to_json)])
let list_event_predictions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventId =
         flag "event-id" (optional json_arg) ~doc:"JSON FilterCondition"
       and eventType =
         flag "event-type" (optional json_arg) ~doc:"JSON FilterCondition"
       and detectorId =
         flag "detector-id" (optional json_arg) ~doc:"JSON FilterCondition"
       and detectorVersionId =
         flag "detector-version-id" (optional json_arg)
           ~doc:"JSON FilterCondition"
       and predictionTimeRange =
         flag "prediction-time-range" (optional json_arg)
           ~doc:"JSON PredictionTimeRange"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT EventPredictionsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_predictions
           (Values.ListEventPredictionsRequest.make
              ?eventId:(Option.map ~f:Values.FilterCondition.of_json eventId)
              ?eventType:(Option.map ~f:Values.FilterCondition.of_json
                            eventType)
              ?detectorId:(Option.map ~f:Values.FilterCondition.of_json
                             detectorId)
              ?detectorVersionId:(Option.map
                                    ~f:Values.FilterCondition.of_json
                                    detectorVersionId)
              ?predictionTimeRange:(Option.map
                                      ~f:Values.PredictionTimeRange.of_json
                                      predictionTimeRange) ?nextToken
              ?maxResults ())
           (Some Values.ListEventPredictionsResult.to_json)
           (Some Values.ListEventPredictionsResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT TagsMaxResults"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING fraudDetectorArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ?maxResults
              ~resourceARN ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let put_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_detector
           (Values.PutDetectorRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~detectorId
              ~eventTypeName ()) (Some Values.PutDetectorResult.to_json)
           (Some Values.PutDetectorResult.error_to_json)])
let put_entity_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and name = flag "name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_entity_type
           (Values.PutEntityTypeRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.PutEntityTypeResult.to_json)
           (Some Values.PutEntityTypeResult.error_to_json)])
let put_event_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and labels =
         flag "labels" (optional json_arg) ~doc:"JSON ListOfStrings"
       and eventIngestion =
         flag "event-ingestion" (optional json_arg)
           ~doc:"JSON EventIngestion"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and name = flag "name" (required string) ~doc:"STRING identifier"
       and eventVariables =
         flag "event-variables" (required json_arg)
           ~doc:"JSON NonEmptyListOfStrings"
       and entityTypes =
         flag "entity-types" (required json_arg)
           ~doc:"JSON NonEmptyListOfStrings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_event_type
           (Values.PutEventTypeRequest.make ?description
              ?labels:(Option.map ~f:Values.ListOfStrings.of_json labels)
              ?eventIngestion:(Option.map ~f:Values.EventIngestion.of_json
                                 eventIngestion)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~eventVariables:(Values.NonEmptyListOfStrings.of_json
                                 eventVariables)
              ~entityTypes:(Values.NonEmptyListOfStrings.of_json entityTypes)
              ()) (Some Values.PutEventTypeResult.to_json)
           (Some Values.PutEventTypeResult.error_to_json)])
let put_external_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and modelEndpoint =
         flag "model-endpoint" (required string)
           ~doc:"STRING sageMakerEndpointIdentifier"
       and modelSource =
         flag "model-source" (required json_arg) ~doc:"JSON ModelSource"
       and invokeModelEndpointRoleArn =
         flag "invoke-model-endpoint-role-arn" (required string)
           ~doc:"STRING string"
       and inputConfiguration =
         flag "input-configuration" (required json_arg)
           ~doc:"JSON ModelInputConfiguration"
       and outputConfiguration =
         flag "output-configuration" (required json_arg)
           ~doc:"JSON ModelOutputConfiguration"
       and modelEndpointStatus =
         flag "model-endpoint-status" (required json_arg)
           ~doc:"JSON ModelEndpointStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_external_model
           (Values.PutExternalModelRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~modelEndpoint
              ~modelSource:(Values.ModelSource.of_json modelSource)
              ~invokeModelEndpointRoleArn
              ~inputConfiguration:(Values.ModelInputConfiguration.of_json
                                     inputConfiguration)
              ~outputConfiguration:(Values.ModelOutputConfiguration.of_json
                                      outputConfiguration)
              ~modelEndpointStatus:(Values.ModelEndpointStatus.of_json
                                      modelEndpointStatus) ())
           (Some Values.PutExternalModelResult.to_json)
           (Some Values.PutExternalModelResult.error_to_json)])
let put_k_m_s_encryption_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsEncryptionKeyArn =
         flag "kms-encryption-key-arn" (required string)
           ~doc:"STRING KmsEncryptionKeyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_k_m_s_encryption_key
           (Values.PutKMSEncryptionKeyRequest.make ~kmsEncryptionKeyArn ())
           (Some Values.PutKMSEncryptionKeyResult.to_json)
           (Some Values.PutKMSEncryptionKeyResult.error_to_json)])
let put_label =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and name = flag "name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_label
           (Values.PutLabelRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.PutLabelResult.to_json)
           (Some Values.PutLabelResult.error_to_json)])
let put_outcome =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and name = flag "name" (required string) ~doc:"STRING identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_outcome
           (Values.PutOutcomeRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.PutOutcomeResult.to_json)
           (Some Values.PutOutcomeResult.error_to_json)])
let send_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assignedLabel =
         flag "assigned-label" (optional string) ~doc:"STRING identifier"
       and labelTimestamp =
         flag "label-timestamp" (optional string)
           ~doc:"STRING utcTimestampISO8601"
       and eventId =
         flag "event-id" (required string) ~doc:"STRING identifier"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier"
       and eventTimestamp =
         flag "event-timestamp" (required string)
           ~doc:"STRING utcTimestampISO8601"
       and eventVariables =
         flag "event-variables" (required json_arg)
           ~doc:"JSON EventVariableMap"
       and entities =
         flag "entities" (required json_arg) ~doc:"JSON listOfEntities" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_event
           (Values.SendEventRequest.make ?assignedLabel ?labelTimestamp
              ~eventId ~eventTypeName ~eventTimestamp
              ~eventVariables:(Values.EventVariableMap.of_json eventVariables)
              ~entities:(Values.ListOfEntities.of_json entities) ())
           (Some Values.SendEventResult.to_json)
           (Some Values.SendEventResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING fraudDetectorArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON tagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING fraudDetectorArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON tagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let update_detector_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and modelVersions =
         flag "model-versions" (optional json_arg)
           ~doc:"JSON ListOfModelVersions"
       and ruleExecutionMode =
         flag "rule-execution-mode" (optional json_arg)
           ~doc:"JSON RuleExecutionMode"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and detectorVersionId =
         flag "detector-version-id" (required string)
           ~doc:"STRING wholeNumberVersionString"
       and externalModelEndpoints =
         flag "external-model-endpoints" (required json_arg)
           ~doc:"JSON ListOfStrings"
       and rules = flag "rules" (required json_arg) ~doc:"JSON RuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_detector_version
           (Values.UpdateDetectorVersionRequest.make ?description
              ?modelVersions:(Option.map
                                ~f:Values.ListOfModelVersions.of_json
                                modelVersions)
              ?ruleExecutionMode:(Option.map
                                    ~f:Values.RuleExecutionMode.of_json
                                    ruleExecutionMode) ~detectorId
              ~detectorVersionId
              ~externalModelEndpoints:(Values.ListOfStrings.of_json
                                         externalModelEndpoints)
              ~rules:(Values.RuleList.of_json rules) ())
           (Some Values.UpdateDetectorVersionResult.to_json)
           (Some Values.UpdateDetectorVersionResult.error_to_json)])
let update_detector_version_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and detectorVersionId =
         flag "detector-version-id" (required string)
           ~doc:"STRING wholeNumberVersionString"
       and description =
         flag "description" (required string) ~doc:"STRING description" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_detector_version_metadata
           (Values.UpdateDetectorVersionMetadataRequest.make ~detectorId
              ~detectorVersionId ~description ())
           (Some Values.UpdateDetectorVersionMetadataResult.to_json)
           (Some Values.UpdateDetectorVersionMetadataResult.error_to_json)])
let update_detector_version_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and detectorId =
         flag "detector-id" (required string) ~doc:"STRING identifier"
       and detectorVersionId =
         flag "detector-version-id" (required string)
           ~doc:"STRING wholeNumberVersionString"
       and status =
         flag "status" (required json_arg) ~doc:"JSON DetectorVersionStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_detector_version_status
           (Values.UpdateDetectorVersionStatusRequest.make ~detectorId
              ~detectorVersionId
              ~status:(Values.DetectorVersionStatus.of_json status) ())
           (Some Values.UpdateDetectorVersionStatusResult.to_json)
           (Some Values.UpdateDetectorVersionStatusResult.error_to_json)])
let update_event_label =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventId =
         flag "event-id" (required string) ~doc:"STRING identifier"
       and eventTypeName =
         flag "event-type-name" (required string) ~doc:"STRING identifier"
       and assignedLabel =
         flag "assigned-label" (required string) ~doc:"STRING identifier"
       and labelTimestamp =
         flag "label-timestamp" (required string)
           ~doc:"STRING utcTimestampISO8601" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_label
           (Values.UpdateEventLabelRequest.make ~eventId ~eventTypeName
              ~assignedLabel ~labelTimestamp ())
           (Some Values.UpdateEventLabelResult.to_json)
           (Some Values.UpdateEventLabelResult.error_to_json)])
let update_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (required json_arg) ~doc:"JSON ModelTypeEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_model
           (Values.UpdateModelRequest.make ?description ~modelId
              ~modelType:(Values.ModelTypeEnum.of_json modelType) ())
           (Some Values.UpdateModelResult.to_json)
           (Some Values.UpdateModelResult.error_to_json)])
let update_model_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and externalEventsDetail =
         flag "external-events-detail" (optional json_arg)
           ~doc:"JSON ExternalEventsDetail"
       and ingestedEventsDetail =
         flag "ingested-events-detail" (optional json_arg)
           ~doc:"JSON IngestedEventsDetail"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (required json_arg) ~doc:"JSON ModelTypeEnum"
       and majorVersionNumber =
         flag "major-version-number" (required string)
           ~doc:"STRING wholeNumberVersionString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_model_version
           (Values.UpdateModelVersionRequest.make
              ?externalEventsDetail:(Option.map
                                       ~f:Values.ExternalEventsDetail.of_json
                                       externalEventsDetail)
              ?ingestedEventsDetail:(Option.map
                                       ~f:Values.IngestedEventsDetail.of_json
                                       ingestedEventsDetail)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~modelId
              ~modelType:(Values.ModelTypeEnum.of_json modelType)
              ~majorVersionNumber ())
           (Some Values.UpdateModelVersionResult.to_json)
           (Some Values.UpdateModelVersionResult.error_to_json)])
let update_model_version_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING modelIdentifier"
       and modelType =
         flag "model-type" (required json_arg) ~doc:"JSON ModelTypeEnum"
       and modelVersionNumber =
         flag "model-version-number" (required string)
           ~doc:"STRING floatVersionString"
       and status =
         flag "status" (required json_arg) ~doc:"JSON ModelVersionStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_model_version_status
           (Values.UpdateModelVersionStatusRequest.make ~modelId
              ~modelType:(Values.ModelTypeEnum.of_json modelType)
              ~modelVersionNumber
              ~status:(Values.ModelVersionStatus.of_json status) ())
           (Some Values.UpdateModelVersionStatusResult.to_json)
           (Some Values.UpdateModelVersionStatusResult.error_to_json)])
let update_rule_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rule = flag "rule" (required json_arg) ~doc:"JSON Rule"
       and description =
         flag "description" (required string) ~doc:"STRING description" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rule_metadata
           (Values.UpdateRuleMetadataRequest.make
              ~rule:(Values.Rule.of_json rule) ~description ())
           (Some Values.UpdateRuleMetadataResult.to_json)
           (Some Values.UpdateRuleMetadataResult.error_to_json)])
let update_rule_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagList"
       and rule = flag "rule" (required json_arg) ~doc:"JSON Rule"
       and expression =
         flag "expression" (required string) ~doc:"STRING ruleExpression"
       and language =
         flag "language" (required json_arg) ~doc:"JSON Language"
       and outcomes =
         flag "outcomes" (required json_arg)
           ~doc:"JSON NonEmptyListOfStrings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rule_version
           (Values.UpdateRuleVersionRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~rule:(Values.Rule.of_json rule) ~expression
              ~language:(Values.Language.of_json language)
              ~outcomes:(Values.NonEmptyListOfStrings.of_json outcomes) ())
           (Some Values.UpdateRuleVersionResult.to_json)
           (Some Values.UpdateRuleVersionResult.error_to_json)])
let update_variable =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultValue =
         flag "default-value" (optional string) ~doc:"STRING string"
       and description =
         flag "description" (optional string) ~doc:"STRING string"
       and variableType =
         flag "variable-type" (optional string) ~doc:"STRING string"
       and name = flag "name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_variable
           (Values.UpdateVariableRequest.make ?defaultValue ?description
              ?variableType ~name ())
           (Some Values.UpdateVariableResult.to_json)
           (Some Values.UpdateVariableResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-create-variable", batch_create_variable);
    ("batch-get-variable", batch_get_variable);
    ("cancel-batch-import-job", cancel_batch_import_job);
    ("cancel-batch-prediction-job", cancel_batch_prediction_job);
    ("create-batch-import-job", create_batch_import_job);
    ("create-batch-prediction-job", create_batch_prediction_job);
    ("create-detector-version", create_detector_version);
    ("create-model", create_model);
    ("create-model-version", create_model_version);
    ("create-rule", create_rule);
    ("create-variable", create_variable);
    ("delete-batch-import-job", delete_batch_import_job);
    ("delete-batch-prediction-job", delete_batch_prediction_job);
    ("delete-detector", delete_detector);
    ("delete-detector-version", delete_detector_version);
    ("delete-entity-type", delete_entity_type);
    ("delete-event", delete_event);
    ("delete-event-type", delete_event_type);
    ("delete-events-by-event-type", delete_events_by_event_type);
    ("delete-external-model", delete_external_model);
    ("delete-label", delete_label);
    ("delete-model", delete_model);
    ("delete-model-version", delete_model_version);
    ("delete-outcome", delete_outcome);
    ("delete-rule", delete_rule);
    ("delete-variable", delete_variable);
    ("describe-detector", describe_detector);
    ("describe-model-versions", describe_model_versions);
    ("get-batch-import-jobs", get_batch_import_jobs);
    ("get-batch-prediction-jobs", get_batch_prediction_jobs);
    ("get-delete-events-by-event-type-status",
      get_delete_events_by_event_type_status);
    ("get-detector-version", get_detector_version);
    ("get-detectors", get_detectors);
    ("get-entity-types", get_entity_types);
    ("get-event", get_event);
    ("get-event-prediction", get_event_prediction);
    ("get-event-prediction-metadata", get_event_prediction_metadata);
    ("get-event-types", get_event_types);
    ("get-external-models", get_external_models);
    ("get-k-m-s-encryption-key", get_k_m_s_encryption_key);
    ("get-labels", get_labels);
    ("get-model-version", get_model_version);
    ("get-models", get_models);
    ("get-outcomes", get_outcomes);
    ("get-rules", get_rules);
    ("get-variables", get_variables);
    ("list-event-predictions", list_event_predictions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-detector", put_detector);
    ("put-entity-type", put_entity_type);
    ("put-event-type", put_event_type);
    ("put-external-model", put_external_model);
    ("put-k-m-s-encryption-key", put_k_m_s_encryption_key);
    ("put-label", put_label);
    ("put-outcome", put_outcome);
    ("send-event", send_event);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-detector-version", update_detector_version);
    ("update-detector-version-metadata", update_detector_version_metadata);
    ("update-detector-version-status", update_detector_version_status);
    ("update-event-label", update_event_label);
    ("update-model", update_model);
    ("update-model-version", update_model_version);
    ("update-model-version-status", update_model_version_status);
    ("update-rule-metadata", update_rule_metadata);
    ("update-rule-version", update_rule_version);
    ("update-variable", update_variable)]
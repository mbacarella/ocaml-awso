(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_earth_observation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING EarthObservationJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_earth_observation_job
           (Values.DeleteEarthObservationJobInput.make ~arn ())
           (Some Values.DeleteEarthObservationJobOutput.to_json)
           (Some Values.DeleteEarthObservationJobOutput.error_to_json)])
let delete_vector_enrichment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING VectorEnrichmentJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vector_enrichment_job
           (Values.DeleteVectorEnrichmentJobInput.make ~arn ())
           (Some Values.DeleteVectorEnrichmentJobOutput.to_json)
           (Some Values.DeleteVectorEnrichmentJobOutput.error_to_json)])
let export_earth_observation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ExportEarthObservationJobInputClientTokenString"
       and exportSourceImages =
         flag "export-source-images" (optional bool) ~doc:"BOOL Boolean"
       and arn =
         flag "arn" (required string) ~doc:"STRING EarthObservationJobArn"
       and executionRoleArn =
         flag "execution-role-arn" (required string)
           ~doc:"STRING ExecutionRoleArn"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON OutputConfigInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_earth_observation_job
           (Values.ExportEarthObservationJobInput.make ?clientToken
              ?exportSourceImages ~arn ~executionRoleArn
              ~outputConfig:(Values.OutputConfigInput.of_json outputConfig)
              ()) (Some Values.ExportEarthObservationJobOutput.to_json)
           (Some Values.ExportEarthObservationJobOutput.error_to_json)])
let export_vector_enrichment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ExportVectorEnrichmentJobInputClientTokenString"
       and arn =
         flag "arn" (required string) ~doc:"STRING VectorEnrichmentJobArn"
       and executionRoleArn =
         flag "execution-role-arn" (required string)
           ~doc:"STRING ExecutionRoleArn"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON ExportVectorEnrichmentJobOutputConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_vector_enrichment_job
           (Values.ExportVectorEnrichmentJobInput.make ?clientToken ~arn
              ~executionRoleArn
              ~outputConfig:(Values.ExportVectorEnrichmentJobOutputConfig.of_json
                               outputConfig) ())
           (Some Values.ExportVectorEnrichmentJobOutput.to_json)
           (Some Values.ExportVectorEnrichmentJobOutput.error_to_json)])
let get_earth_observation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING EarthObservationJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_earth_observation_job
           (Values.GetEarthObservationJobInput.make ~arn ())
           (Some Values.GetEarthObservationJobOutput.to_json)
           (Some Values.GetEarthObservationJobOutput.error_to_json)])
let get_raster_data_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING DataCollectionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_raster_data_collection
           (Values.GetRasterDataCollectionInput.make ~arn ())
           (Some Values.GetRasterDataCollectionOutput.to_json)
           (Some Values.GetRasterDataCollectionOutput.error_to_json)])
let get_tile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and executionRoleArn =
         flag "execution-role-arn" (optional string)
           ~doc:"STRING ExecutionRoleArn"
       and imageMask = flag "image-mask" (optional bool) ~doc:"BOOL Boolean"
       and outputDataType =
         flag "output-data-type" (optional json_arg) ~doc:"JSON OutputType"
       and outputFormat =
         flag "output-format" (optional string) ~doc:"STRING String"
       and propertyFilters =
         flag "property-filters" (optional string) ~doc:"STRING String"
       and timeRangeFilter =
         flag "time-range-filter" (optional string) ~doc:"STRING String"
       and arn =
         flag "arn" (required string) ~doc:"STRING EarthObservationJobArn"
       and imageAssets =
         flag "image-assets" (required json_arg) ~doc:"JSON StringListInput"
       and target =
         flag "target" (required json_arg) ~doc:"JSON TargetOptions"
       and x = flag "x" (required int) ~doc:"INT Integer"
       and y = flag "y" (required int) ~doc:"INT Integer"
       and z = flag "z" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tile
           (Values.GetTileInput.make ?executionRoleArn ?imageMask
              ?outputDataType:(Option.map ~f:Values.OutputType.of_json
                                 outputDataType) ?outputFormat
              ?propertyFilters ?timeRangeFilter ~arn
              ~imageAssets:(Values.StringListInput.of_json imageAssets)
              ~target:(Values.TargetOptions.of_json target) ~x ~y ~z ())
           (Some Values.GetTileOutput.to_json)
           (Some Values.GetTileOutput.error_to_json)])
let get_vector_enrichment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING VectorEnrichmentJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vector_enrichment_job
           (Values.GetVectorEnrichmentJobInput.make ~arn ())
           (Some Values.GetVectorEnrichmentJobOutput.to_json)
           (Some Values.GetVectorEnrichmentJobOutput.error_to_json)])
let list_earth_observation_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEarthObservationJobInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sortBy = flag "sort-by" (optional string) ~doc:"STRING String"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON EarthObservationJobStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_earth_observation_jobs
           (Values.ListEarthObservationJobInput.make ?maxResults ?nextToken
              ?sortBy
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?statusEquals:(Option.map
                               ~f:Values.EarthObservationJobStatus.of_json
                               statusEquals) ())
           (Some Values.ListEarthObservationJobOutput.to_json)
           (Some Values.ListEarthObservationJobOutput.error_to_json)])
let list_raster_data_collections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRasterDataCollectionsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_raster_data_collections
           (Values.ListRasterDataCollectionsInput.make ?maxResults ?nextToken
              ()) (Some Values.ListRasterDataCollectionsOutput.to_json)
           (Some Values.ListRasterDataCollectionsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_vector_enrichment_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListVectorEnrichmentJobInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sortBy = flag "sort-by" (optional string) ~doc:"STRING String"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and statusEquals =
         flag "status-equals" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vector_enrichment_jobs
           (Values.ListVectorEnrichmentJobInput.make ?maxResults ?nextToken
              ?sortBy
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?statusEquals ())
           (Some Values.ListVectorEnrichmentJobOutput.to_json)
           (Some Values.ListVectorEnrichmentJobOutput.error_to_json)])
let search_raster_data_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and arn = flag "arn" (required string) ~doc:"STRING DataCollectionArn"
       and rasterDataCollectionQuery =
         flag "raster-data-collection-query" (required json_arg)
           ~doc:"JSON RasterDataCollectionQueryWithBandFilterInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_raster_data_collection
           (Values.SearchRasterDataCollectionInput.make ?nextToken ~arn
              ~rasterDataCollectionQuery:(Values.RasterDataCollectionQueryWithBandFilterInput.of_json
                                            rasterDataCollectionQuery) ())
           (Some Values.SearchRasterDataCollectionOutput.to_json)
           (Some Values.SearchRasterDataCollectionOutput.error_to_json)])
let start_earth_observation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING StartEarthObservationJobInputClientTokenString"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKey"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and executionRoleArn =
         flag "execution-role-arn" (required string)
           ~doc:"STRING ExecutionRoleArn"
       and inputConfig =
         flag "input-config" (required json_arg) ~doc:"JSON InputConfigInput"
       and jobConfig =
         flag "job-config" (required json_arg) ~doc:"JSON JobConfigInput"
       and name =
         flag "name" (required string)
           ~doc:"STRING StartEarthObservationJobInputNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_earth_observation_job
           (Values.StartEarthObservationJobInput.make ?clientToken ?kmsKeyId
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~executionRoleArn
              ~inputConfig:(Values.InputConfigInput.of_json inputConfig)
              ~jobConfig:(Values.JobConfigInput.of_json jobConfig) ~name ())
           (Some Values.StartEarthObservationJobOutput.to_json)
           (Some Values.StartEarthObservationJobOutput.error_to_json)])
let start_vector_enrichment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING StartVectorEnrichmentJobInputClientTokenString"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKey"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and executionRoleArn =
         flag "execution-role-arn" (required string)
           ~doc:"STRING ExecutionRoleArn"
       and inputConfig =
         flag "input-config" (required json_arg)
           ~doc:"JSON VectorEnrichmentJobInputConfig"
       and jobConfig =
         flag "job-config" (required json_arg)
           ~doc:"JSON VectorEnrichmentJobConfig"
       and name =
         flag "name" (required string)
           ~doc:"STRING StartVectorEnrichmentJobInputNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_vector_enrichment_job
           (Values.StartVectorEnrichmentJobInput.make ?clientToken ?kmsKeyId
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~executionRoleArn
              ~inputConfig:(Values.VectorEnrichmentJobInputConfig.of_json
                              inputConfig)
              ~jobConfig:(Values.VectorEnrichmentJobConfig.of_json jobConfig)
              ~name ()) (Some Values.StartVectorEnrichmentJobOutput.to_json)
           (Some Values.StartVectorEnrichmentJobOutput.error_to_json)])
let stop_earth_observation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING EarthObservationJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_earth_observation_job
           (Values.StopEarthObservationJobInput.make ~arn ())
           (Some Values.StopEarthObservationJobOutput.to_json)
           (Some Values.StopEarthObservationJobOutput.error_to_json)])
let stop_vector_enrichment_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING VectorEnrichmentJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_vector_enrichment_job
           (Values.StopVectorEnrichmentJobInput.make ~arn ())
           (Some Values.StopVectorEnrichmentJobOutput.to_json)
           (Some Values.StopVectorEnrichmentJobOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-earth-observation-job", delete_earth_observation_job);
    ("delete-vector-enrichment-job", delete_vector_enrichment_job);
    ("export-earth-observation-job", export_earth_observation_job);
    ("export-vector-enrichment-job", export_vector_enrichment_job);
    ("get-earth-observation-job", get_earth_observation_job);
    ("get-raster-data-collection", get_raster_data_collection);
    ("get-tile", get_tile);
    ("get-vector-enrichment-job", get_vector_enrichment_job);
    ("list-earth-observation-jobs", list_earth_observation_jobs);
    ("list-raster-data-collections", list_raster_data_collections);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-vector-enrichment-jobs", list_vector_enrichment_jobs);
    ("search-raster-data-collection", search_raster_data_collection);
    ("start-earth-observation-job", start_earth_observation_job);
    ("start-vector-enrichment-job", start_vector_enrichment_job);
    ("stop-earth-observation-job", stop_earth_observation_job);
    ("stop-vector-enrichment-job", stop_vector_enrichment_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
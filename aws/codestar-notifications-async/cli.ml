(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_notification_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON NotificationRuleStatus"
       and name =
         flag "name" (required string) ~doc:"STRING NotificationRuleName"
       and eventTypeIds =
         flag "event-type-ids" (required json_arg) ~doc:"JSON EventTypeIds"
       and resource =
         flag "resource" (required string)
           ~doc:"STRING NotificationRuleResource"
       and targets = flag "targets" (required json_arg) ~doc:"JSON Targets"
       and detailType =
         flag "detail-type" (required json_arg) ~doc:"JSON DetailType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_notification_rule
           (Values.CreateNotificationRuleRequest.make ?clientRequestToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?status:(Option.map ~f:Values.NotificationRuleStatus.of_json
                         status) ~name
              ~eventTypeIds:(Values.EventTypeIds.of_json eventTypeIds)
              ~resource ~targets:(Values.Targets.of_json targets)
              ~detailType:(Values.DetailType.of_json detailType) ())
           (Some Values.CreateNotificationRuleResult.to_json)
           (Some Values.CreateNotificationRuleResult.error_to_json)])
let delete_notification_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notification_rule
           (Values.DeleteNotificationRuleRequest.make ~arn ())
           (Some Values.DeleteNotificationRuleResult.to_json)
           (Some Values.DeleteNotificationRuleResult.error_to_json)])
let delete_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceUnsubscribeAll =
         flag "force-unsubscribe-all" (optional bool)
           ~doc:"BOOL ForceUnsubscribeAll"
       and targetAddress =
         flag "target-address" (required string) ~doc:"STRING TargetAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_target
           (Values.DeleteTargetRequest.make ?forceUnsubscribeAll
              ~targetAddress ()) (Some Values.DeleteTargetResult.to_json)
           (Some Values.DeleteTargetResult.error_to_json)])
let describe_notification_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_notification_rule
           (Values.DescribeNotificationRuleRequest.make ~arn ())
           (Some Values.DescribeNotificationRuleResult.to_json)
           (Some Values.DescribeNotificationRuleResult.error_to_json)])
let list_event_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListEventTypesFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_types
           (Values.ListEventTypesRequest.make
              ?filters:(Option.map ~f:Values.ListEventTypesFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.ListEventTypesResult.to_json)
           (Some Values.ListEventTypesResult.error_to_json)])
let list_notification_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListNotificationRulesFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notification_rules
           (Values.ListNotificationRulesRequest.make
              ?filters:(Option.map
                          ~f:Values.ListNotificationRulesFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.ListNotificationRulesResult.to_json)
           (Some Values.ListNotificationRulesResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let list_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListTargetsFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_targets
           (Values.ListTargetsRequest.make
              ?filters:(Option.map ~f:Values.ListTargetsFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.ListTargetsResult.to_json)
           (Some Values.ListTargetsResult.error_to_json)])
let subscribe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationRuleArn"
       and target = flag "target" (required json_arg) ~doc:"JSON Target" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.subscribe
           (Values.SubscribeRequest.make ?clientRequestToken ~arn
              ~target:(Values.Target.of_json target) ())
           (Some Values.SubscribeResult.to_json)
           (Some Values.SubscribeResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationRuleArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let unsubscribe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationRuleArn"
       and targetAddress =
         flag "target-address" (required string) ~doc:"STRING TargetAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unsubscribe
           (Values.UnsubscribeRequest.make ~arn ~targetAddress ())
           (Some Values.UnsubscribeResult.to_json)
           (Some Values.UnsubscribeResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationRuleArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let update_notification_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING NotificationRuleName"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON NotificationRuleStatus"
       and eventTypeIds =
         flag "event-type-ids" (optional json_arg) ~doc:"JSON EventTypeIds"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and detailType =
         flag "detail-type" (optional json_arg) ~doc:"JSON DetailType"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_notification_rule
           (Values.UpdateNotificationRuleRequest.make ?name
              ?status:(Option.map ~f:Values.NotificationRuleStatus.of_json
                         status)
              ?eventTypeIds:(Option.map ~f:Values.EventTypeIds.of_json
                               eventTypeIds)
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?detailType:(Option.map ~f:Values.DetailType.of_json detailType)
              ~arn ()) (Some Values.UpdateNotificationRuleResult.to_json)
           (Some Values.UpdateNotificationRuleResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-notification-rule", create_notification_rule);
    ("delete-notification-rule", delete_notification_rule);
    ("delete-target", delete_target);
    ("describe-notification-rule", describe_notification_rule);
    ("list-event-types", list_event_types);
    ("list-notification-rules", list_notification_rules);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-targets", list_targets);
    ("subscribe", subscribe);
    ("tag-resource", tag_resource);
    ("unsubscribe", unsubscribe);
    ("untag-resource", untag_resource);
    ("update-notification-rule", update_notification_rule)]
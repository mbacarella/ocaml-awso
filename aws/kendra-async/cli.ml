(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_entities_to_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and entityList =
         flag "entity-list" (required json_arg)
           ~doc:"JSON AssociateEntityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_entities_to_experience
           (Values.AssociateEntitiesToExperienceRequest.make ~id ~indexId
              ~entityList:(Values.AssociateEntityList.of_json entityList) ())
           (Some Values.AssociateEntitiesToExperienceResponse.to_json)
           (Some Values.AssociateEntitiesToExperienceResponse.error_to_json)])
let associate_personas_to_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and personas =
         flag "personas" (required json_arg)
           ~doc:"JSON EntityPersonaConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_personas_to_entities
           (Values.AssociatePersonasToEntitiesRequest.make ~id ~indexId
              ~personas:(Values.EntityPersonaConfigurationList.of_json
                           personas) ())
           (Some Values.AssociatePersonasToEntitiesResponse.to_json)
           (Some Values.AssociatePersonasToEntitiesResponse.error_to_json)])
let batch_delete_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceSyncJobMetricTarget =
         flag "data-source-sync-job-metric-target" (optional json_arg)
           ~doc:"JSON DataSourceSyncJobMetricTarget"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and documentIdList =
         flag "document-id-list" (required json_arg)
           ~doc:"JSON DocumentIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_document
           (Values.BatchDeleteDocumentRequest.make
              ?dataSourceSyncJobMetricTarget:(Option.map
                                                ~f:Values.DataSourceSyncJobMetricTarget.of_json
                                                dataSourceSyncJobMetricTarget)
              ~indexId
              ~documentIdList:(Values.DocumentIdList.of_json documentIdList)
              ()) (Some Values.BatchDeleteDocumentResponse.to_json)
           (Some Values.BatchDeleteDocumentResponse.error_to_json)])
let batch_get_document_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and documentInfoList =
         flag "document-info-list" (required json_arg)
           ~doc:"JSON DocumentInfoList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_document_status
           (Values.BatchGetDocumentStatusRequest.make ~indexId
              ~documentInfoList:(Values.DocumentInfoList.of_json
                                   documentInfoList) ())
           (Some Values.BatchGetDocumentStatusResponse.to_json)
           (Some Values.BatchGetDocumentStatusResponse.error_to_json)])
let batch_put_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and customDocumentEnrichmentConfiguration =
         flag "custom-document-enrichment-configuration" (optional json_arg)
           ~doc:"JSON CustomDocumentEnrichmentConfiguration"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and documents =
         flag "documents" (required json_arg) ~doc:"JSON DocumentList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_document
           (Values.BatchPutDocumentRequest.make ?roleArn
              ?customDocumentEnrichmentConfiguration:(Option.map
                                                        ~f:Values.CustomDocumentEnrichmentConfiguration.of_json
                                                        customDocumentEnrichmentConfiguration)
              ~indexId ~documents:(Values.DocumentList.of_json documents) ())
           (Some Values.BatchPutDocumentResponse.to_json)
           (Some Values.BatchPutDocumentResponse.error_to_json)])
let clear_query_suggestions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.clear_query_suggestions
           (Values.ClearQuerySuggestionsRequest.make ~indexId ()) None None])
let create_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON DataSourceConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING ScanSchedule"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientTokenName"
       and languageCode =
         flag "language-code" (optional string) ~doc:"STRING LanguageCode"
       and customDocumentEnrichmentConfiguration =
         flag "custom-document-enrichment-configuration" (optional json_arg)
           ~doc:"JSON CustomDocumentEnrichmentConfiguration"
       and name = flag "name" (required string) ~doc:"STRING DataSourceName"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON DataSourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_source
           (Values.CreateDataSourceRequest.make
              ?configuration:(Option.map
                                ~f:Values.DataSourceConfiguration.of_json
                                configuration) ?description ?schedule
              ?roleArn ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientToken ?languageCode
              ?customDocumentEnrichmentConfiguration:(Option.map
                                                        ~f:Values.CustomDocumentEnrichmentConfiguration.of_json
                                                        customDocumentEnrichmentConfiguration)
              ~name ~indexId ~type_:(Values.DataSourceType.of_json type_) ())
           (Some Values.CreateDataSourceResponse.to_json)
           (Some Values.CreateDataSourceResponse.error_to_json)])
let create_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ExperienceConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientTokenName"
       and name = flag "name" (required string) ~doc:"STRING ExperienceName"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_experience
           (Values.CreateExperienceRequest.make ?roleArn
              ?configuration:(Option.map
                                ~f:Values.ExperienceConfiguration.of_json
                                configuration) ?description ?clientToken
              ~name ~indexId ())
           (Some Values.CreateExperienceResponse.to_json)
           (Some Values.CreateExperienceResponse.error_to_json)])
let create_faq =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and fileFormat =
         flag "file-format" (optional json_arg) ~doc:"JSON FaqFileFormat"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientTokenName"
       and languageCode =
         flag "language-code" (optional string) ~doc:"STRING LanguageCode"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and name = flag "name" (required string) ~doc:"STRING FaqName"
       and s3Path = flag "s3-path" (required json_arg) ~doc:"JSON S3Path"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_faq
           (Values.CreateFaqRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?fileFormat:(Option.map ~f:Values.FaqFileFormat.of_json
                             fileFormat) ?clientToken ?languageCode ~indexId
              ~name ~s3Path:(Values.S3Path.of_json s3Path) ~roleArn ())
           (Some Values.CreateFaqResponse.to_json)
           (Some Values.CreateFaqResponse.error_to_json)])
let create_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and edition =
         flag "edition" (optional json_arg) ~doc:"JSON IndexEdition"
       and serverSideEncryptionConfiguration =
         flag "server-side-encryption-configuration" (optional json_arg)
           ~doc:"JSON ServerSideEncryptionConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientTokenName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and userTokenConfigurations =
         flag "user-token-configurations" (optional json_arg)
           ~doc:"JSON UserTokenConfigurationList"
       and userContextPolicy =
         flag "user-context-policy" (optional json_arg)
           ~doc:"JSON UserContextPolicy"
       and userGroupResolutionConfiguration =
         flag "user-group-resolution-configuration" (optional json_arg)
           ~doc:"JSON UserGroupResolutionConfiguration"
       and name = flag "name" (required string) ~doc:"STRING IndexName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_index
           (Values.CreateIndexRequest.make
              ?edition:(Option.map ~f:Values.IndexEdition.of_json edition)
              ?serverSideEncryptionConfiguration:(Option.map
                                                    ~f:Values.ServerSideEncryptionConfiguration.of_json
                                                    serverSideEncryptionConfiguration)
              ?description ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?userTokenConfigurations:(Option.map
                                          ~f:Values.UserTokenConfigurationList.of_json
                                          userTokenConfigurations)
              ?userContextPolicy:(Option.map
                                    ~f:Values.UserContextPolicy.of_json
                                    userContextPolicy)
              ?userGroupResolutionConfiguration:(Option.map
                                                   ~f:Values.UserGroupResolutionConfiguration.of_json
                                                   userGroupResolutionConfiguration)
              ~name ~roleArn ()) (Some Values.CreateIndexResponse.to_json)
           (Some Values.CreateIndexResponse.error_to_json)])
let create_query_suggestions_block_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientTokenName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and name =
         flag "name" (required string)
           ~doc:"STRING QuerySuggestionsBlockListName"
       and sourceS3Path =
         flag "source-s3-path" (required json_arg) ~doc:"JSON S3Path"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_query_suggestions_block_list
           (Values.CreateQuerySuggestionsBlockListRequest.make ?description
              ?clientToken ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~indexId ~name
              ~sourceS3Path:(Values.S3Path.of_json sourceS3Path) ~roleArn ())
           (Some Values.CreateQuerySuggestionsBlockListResponse.to_json)
           (Some Values.CreateQuerySuggestionsBlockListResponse.error_to_json)])
let create_thesaurus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientTokenName"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and name = flag "name" (required string) ~doc:"STRING ThesaurusName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and sourceS3Path =
         flag "source-s3-path" (required json_arg) ~doc:"JSON S3Path" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_thesaurus
           (Values.CreateThesaurusRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~indexId ~name ~roleArn
              ~sourceS3Path:(Values.S3Path.of_json sourceS3Path) ())
           (Some Values.CreateThesaurusResponse.to_json)
           (Some Values.CreateThesaurusResponse.error_to_json)])
let delete_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING DataSourceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_source
           (Values.DeleteDataSourceRequest.make ~id ~indexId ()) None None])
let delete_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_experience
           (Values.DeleteExperienceRequest.make ~id ~indexId ())
           (Some Values.DeleteExperienceResponse.to_json)
           (Some Values.DeleteExperienceResponse.error_to_json)])
let delete_faq =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING FaqId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_faq (Values.DeleteFaqRequest.make ~id ~indexId ()) None
           None])
let delete_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_index (Values.DeleteIndexRequest.make ~id ()) None None])
let delete_principal_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and orderingId =
         flag "ordering-id" (optional json_arg)
           ~doc:"JSON PrincipalOrderingId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and groupId = flag "group-id" (required string) ~doc:"STRING GroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_principal_mapping
           (Values.DeletePrincipalMappingRequest.make ?dataSourceId
              ?orderingId:(Option.map ~f:Values.PrincipalOrderingId.of_json
                             orderingId) ~indexId ~groupId ()) None None])
let delete_query_suggestions_block_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and id =
         flag "id" (required string)
           ~doc:"STRING QuerySuggestionsBlockListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_query_suggestions_block_list
           (Values.DeleteQuerySuggestionsBlockListRequest.make ~indexId ~id
              ()) None None])
let delete_thesaurus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ThesaurusId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_thesaurus
           (Values.DeleteThesaurusRequest.make ~id ~indexId ()) None None])
let describe_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING DataSourceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_source
           (Values.DescribeDataSourceRequest.make ~id ~indexId ())
           (Some Values.DescribeDataSourceResponse.to_json)
           (Some Values.DescribeDataSourceResponse.error_to_json)])
let describe_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_experience
           (Values.DescribeExperienceRequest.make ~id ~indexId ())
           (Some Values.DescribeExperienceResponse.to_json)
           (Some Values.DescribeExperienceResponse.error_to_json)])
let describe_faq =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING FaqId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_faq (Values.DescribeFaqRequest.make ~id ~indexId ())
           (Some Values.DescribeFaqResponse.to_json)
           (Some Values.DescribeFaqResponse.error_to_json)])
let describe_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_index (Values.DescribeIndexRequest.make ~id ())
           (Some Values.DescribeIndexResponse.to_json)
           (Some Values.DescribeIndexResponse.error_to_json)])
let describe_principal_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and groupId = flag "group-id" (required string) ~doc:"STRING GroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_principal_mapping
           (Values.DescribePrincipalMappingRequest.make ?dataSourceId
              ~indexId ~groupId ())
           (Some Values.DescribePrincipalMappingResponse.to_json)
           (Some Values.DescribePrincipalMappingResponse.error_to_json)])
let describe_query_suggestions_block_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and id =
         flag "id" (required string)
           ~doc:"STRING QuerySuggestionsBlockListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_query_suggestions_block_list
           (Values.DescribeQuerySuggestionsBlockListRequest.make ~indexId ~id
              ())
           (Some Values.DescribeQuerySuggestionsBlockListResponse.to_json)
           (Some
              Values.DescribeQuerySuggestionsBlockListResponse.error_to_json)])
let describe_query_suggestions_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_query_suggestions_config
           (Values.DescribeQuerySuggestionsConfigRequest.make ~indexId ())
           (Some Values.DescribeQuerySuggestionsConfigResponse.to_json)
           (Some Values.DescribeQuerySuggestionsConfigResponse.error_to_json)])
let describe_thesaurus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ThesaurusId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_thesaurus
           (Values.DescribeThesaurusRequest.make ~id ~indexId ())
           (Some Values.DescribeThesaurusResponse.to_json)
           (Some Values.DescribeThesaurusResponse.error_to_json)])
let disassociate_entities_from_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and entityList =
         flag "entity-list" (required json_arg)
           ~doc:"JSON DisassociateEntityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_entities_from_experience
           (Values.DisassociateEntitiesFromExperienceRequest.make ~id
              ~indexId
              ~entityList:(Values.DisassociateEntityList.of_json entityList)
              ())
           (Some Values.DisassociateEntitiesFromExperienceResponse.to_json)
           (Some
              Values.DisassociateEntitiesFromExperienceResponse.error_to_json)])
let disassociate_personas_from_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and entityIds =
         flag "entity-ids" (required json_arg) ~doc:"JSON EntityIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_personas_from_entities
           (Values.DisassociatePersonasFromEntitiesRequest.make ~id ~indexId
              ~entityIds:(Values.EntityIdsList.of_json entityIds) ())
           (Some Values.DisassociatePersonasFromEntitiesResponse.to_json)
           (Some
              Values.DisassociatePersonasFromEntitiesResponse.error_to_json)])
let get_query_suggestions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxSuggestionsCount =
         flag "max-suggestions-count" (optional int) ~doc:"INT Integer"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and queryText =
         flag "query-text" (required string)
           ~doc:"STRING SuggestionQueryText" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_suggestions
           (Values.GetQuerySuggestionsRequest.make ?maxSuggestionsCount
              ~indexId ~queryText ())
           (Some Values.GetQuerySuggestionsResponse.to_json)
           (Some Values.GetQuerySuggestionsResponse.error_to_json)])
let get_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and interval =
         flag "interval" (required json_arg) ~doc:"JSON Interval"
       and metricType =
         flag "metric-type" (required json_arg) ~doc:"JSON MetricType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_snapshots
           (Values.GetSnapshotsRequest.make ?nextToken ?maxResults ~indexId
              ~interval:(Values.Interval.of_json interval)
              ~metricType:(Values.MetricType.of_json metricType) ())
           (Some Values.GetSnapshotsResponse.to_json)
           (Some Values.GetSnapshotsResponse.error_to_json)])
let list_data_source_sync_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListDataSourceSyncJobsRequest"
       and startTimeFilter =
         flag "start-time-filter" (optional json_arg) ~doc:"JSON TimeRange"
       and statusFilter =
         flag "status-filter" (optional json_arg)
           ~doc:"JSON DataSourceSyncJobStatus"
       and id = flag "id" (required string) ~doc:"STRING DataSourceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_source_sync_jobs
           (Values.ListDataSourceSyncJobsRequest.make ?nextToken ?maxResults
              ?startTimeFilter:(Option.map ~f:Values.TimeRange.of_json
                                  startTimeFilter)
              ?statusFilter:(Option.map
                               ~f:Values.DataSourceSyncJobStatus.of_json
                               statusFilter) ~id ~indexId ())
           (Some Values.ListDataSourceSyncJobsResponse.to_json)
           (Some Values.ListDataSourceSyncJobsResponse.error_to_json)])
let list_data_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListDataSourcesRequest"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_sources
           (Values.ListDataSourcesRequest.make ?nextToken ?maxResults
              ~indexId ()) (Some Values.ListDataSourcesResponse.to_json)
           (Some Values.ListDataSourcesResponse.error_to_json)])
let list_entity_personas =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListEntityPersonasRequest"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entity_personas
           (Values.ListEntityPersonasRequest.make ?nextToken ?maxResults ~id
              ~indexId ()) (Some Values.ListEntityPersonasResponse.to_json)
           (Some Values.ListEntityPersonasResponse.error_to_json)])
let list_experience_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_experience_entities
           (Values.ListExperienceEntitiesRequest.make ?nextToken ~id ~indexId
              ()) (Some Values.ListExperienceEntitiesResponse.to_json)
           (Some Values.ListExperienceEntitiesResponse.error_to_json)])
let list_experiences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListExperiencesRequest"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_experiences
           (Values.ListExperiencesRequest.make ?nextToken ?maxResults
              ~indexId ()) (Some Values.ListExperiencesResponse.to_json)
           (Some Values.ListExperiencesResponse.error_to_json)])
let list_faqs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListFaqsRequest"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_faqs
           (Values.ListFaqsRequest.make ?nextToken ?maxResults ~indexId ())
           (Some Values.ListFaqsResponse.to_json)
           (Some Values.ListFaqsResponse.error_to_json)])
let list_groups_older_than_ordering_id =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListPrincipalsRequest"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and orderingId =
         flag "ordering-id" (required json_arg)
           ~doc:"JSON PrincipalOrderingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups_older_than_ordering_id
           (Values.ListGroupsOlderThanOrderingIdRequest.make ?dataSourceId
              ?nextToken ?maxResults ~indexId
              ~orderingId:(Values.PrincipalOrderingId.of_json orderingId) ())
           (Some Values.ListGroupsOlderThanOrderingIdResponse.to_json)
           (Some Values.ListGroupsOlderThanOrderingIdResponse.error_to_json)])
let list_indices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListIndicesRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_indices
           (Values.ListIndicesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListIndicesResponse.to_json)
           (Some Values.ListIndicesResponse.error_to_json)])
let list_query_suggestions_block_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListQuerySuggestionsBlockLists"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_query_suggestions_block_lists
           (Values.ListQuerySuggestionsBlockListsRequest.make ?nextToken
              ?maxResults ~indexId ())
           (Some Values.ListQuerySuggestionsBlockListsResponse.to_json)
           (Some Values.ListQuerySuggestionsBlockListsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_thesauri =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListThesauriRequest"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_thesauri
           (Values.ListThesauriRequest.make ?nextToken ?maxResults ~indexId
              ()) (Some Values.ListThesauriResponse.to_json)
           (Some Values.ListThesauriResponse.error_to_json)])
let put_principal_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and orderingId =
         flag "ordering-id" (optional json_arg)
           ~doc:"JSON PrincipalOrderingId"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and groupId = flag "group-id" (required string) ~doc:"STRING GroupId"
       and groupMembers =
         flag "group-members" (required json_arg) ~doc:"JSON GroupMembers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_principal_mapping
           (Values.PutPrincipalMappingRequest.make ?dataSourceId
              ?orderingId:(Option.map ~f:Values.PrincipalOrderingId.of_json
                             orderingId) ?roleArn ~indexId ~groupId
              ~groupMembers:(Values.GroupMembers.of_json groupMembers) ())
           None None])
let query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryText =
         flag "query-text" (optional string) ~doc:"STRING QueryText"
       and attributeFilter =
         flag "attribute-filter" (optional json_arg)
           ~doc:"JSON AttributeFilter"
       and facets = flag "facets" (optional json_arg) ~doc:"JSON FacetList"
       and requestedDocumentAttributes =
         flag "requested-document-attributes" (optional json_arg)
           ~doc:"JSON DocumentAttributeKeyList"
       and queryResultTypeFilter =
         flag "query-result-type-filter" (optional json_arg)
           ~doc:"JSON QueryResultType"
       and documentRelevanceOverrideConfigurations =
         flag "document-relevance-override-configurations"
           (optional json_arg)
           ~doc:"JSON DocumentRelevanceOverrideConfigurationList"
       and pageNumber = flag "page-number" (optional int) ~doc:"INT Integer"
       and pageSize = flag "page-size" (optional int) ~doc:"INT Integer"
       and sortingConfiguration =
         flag "sorting-configuration" (optional json_arg)
           ~doc:"JSON SortingConfiguration"
       and userContext =
         flag "user-context" (optional json_arg) ~doc:"JSON UserContext"
       and visitorId =
         flag "visitor-id" (optional string) ~doc:"STRING VisitorId"
       and spellCorrectionConfiguration =
         flag "spell-correction-configuration" (optional json_arg)
           ~doc:"JSON SpellCorrectionConfiguration"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.query
           (Values.QueryRequest.make ?queryText
              ?attributeFilter:(Option.map ~f:Values.AttributeFilter.of_json
                                  attributeFilter)
              ?facets:(Option.map ~f:Values.FacetList.of_json facets)
              ?requestedDocumentAttributes:(Option.map
                                              ~f:Values.DocumentAttributeKeyList.of_json
                                              requestedDocumentAttributes)
              ?queryResultTypeFilter:(Option.map
                                        ~f:Values.QueryResultType.of_json
                                        queryResultTypeFilter)
              ?documentRelevanceOverrideConfigurations:(Option.map
                                                          ~f:Values.DocumentRelevanceOverrideConfigurationList.of_json
                                                          documentRelevanceOverrideConfigurations)
              ?pageNumber ?pageSize
              ?sortingConfiguration:(Option.map
                                       ~f:Values.SortingConfiguration.of_json
                                       sortingConfiguration)
              ?userContext:(Option.map ~f:Values.UserContext.of_json
                              userContext) ?visitorId
              ?spellCorrectionConfiguration:(Option.map
                                               ~f:Values.SpellCorrectionConfiguration.of_json
                                               spellCorrectionConfiguration)
              ~indexId ()) (Some Values.QueryResult.to_json)
           (Some Values.QueryResult.error_to_json)])
let start_data_source_sync_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING DataSourceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_data_source_sync_job
           (Values.StartDataSourceSyncJobRequest.make ~id ~indexId ())
           (Some Values.StartDataSourceSyncJobResponse.to_json)
           (Some Values.StartDataSourceSyncJobResponse.error_to_json)])
let stop_data_source_sync_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING DataSourceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_data_source_sync_job
           (Values.StopDataSourceSyncJobRequest.make ~id ~indexId ()) None
           None])
let submit_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clickFeedbackItems =
         flag "click-feedback-items" (optional json_arg)
           ~doc:"JSON ClickFeedbackList"
       and relevanceFeedbackItems =
         flag "relevance-feedback-items" (optional json_arg)
           ~doc:"JSON RelevanceFeedbackList"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and queryId = flag "query-id" (required string) ~doc:"STRING QueryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_feedback
           (Values.SubmitFeedbackRequest.make
              ?clickFeedbackItems:(Option.map
                                     ~f:Values.ClickFeedbackList.of_json
                                     clickFeedbackItems)
              ?relevanceFeedbackItems:(Option.map
                                         ~f:Values.RelevanceFeedbackList.of_json
                                         relevanceFeedbackItems) ~indexId
              ~queryId ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING DataSourceName"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON DataSourceConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and schedule =
         flag "schedule" (optional string) ~doc:"STRING ScanSchedule"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and languageCode =
         flag "language-code" (optional string) ~doc:"STRING LanguageCode"
       and customDocumentEnrichmentConfiguration =
         flag "custom-document-enrichment-configuration" (optional json_arg)
           ~doc:"JSON CustomDocumentEnrichmentConfiguration"
       and id = flag "id" (required string) ~doc:"STRING DataSourceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_source
           (Values.UpdateDataSourceRequest.make ?name
              ?configuration:(Option.map
                                ~f:Values.DataSourceConfiguration.of_json
                                configuration) ?description ?schedule
              ?roleArn ?languageCode
              ?customDocumentEnrichmentConfiguration:(Option.map
                                                        ~f:Values.CustomDocumentEnrichmentConfiguration.of_json
                                                        customDocumentEnrichmentConfiguration)
              ~id ~indexId ()) None None])
let update_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ExperienceName"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ExperienceConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and id = flag "id" (required string) ~doc:"STRING ExperienceId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_experience
           (Values.UpdateExperienceRequest.make ?name ?roleArn
              ?configuration:(Option.map
                                ~f:Values.ExperienceConfiguration.of_json
                                configuration) ?description ~id ~indexId ())
           None None])
let update_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING IndexName"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and documentMetadataConfigurationUpdates =
         flag "document-metadata-configuration-updates" (optional json_arg)
           ~doc:"JSON DocumentMetadataConfigurationList"
       and capacityUnits =
         flag "capacity-units" (optional json_arg)
           ~doc:"JSON CapacityUnitsConfiguration"
       and userTokenConfigurations =
         flag "user-token-configurations" (optional json_arg)
           ~doc:"JSON UserTokenConfigurationList"
       and userContextPolicy =
         flag "user-context-policy" (optional json_arg)
           ~doc:"JSON UserContextPolicy"
       and userGroupResolutionConfiguration =
         flag "user-group-resolution-configuration" (optional json_arg)
           ~doc:"JSON UserGroupResolutionConfiguration"
       and id = flag "id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_index
           (Values.UpdateIndexRequest.make ?name ?roleArn ?description
              ?documentMetadataConfigurationUpdates:(Option.map
                                                       ~f:Values.DocumentMetadataConfigurationList.of_json
                                                       documentMetadataConfigurationUpdates)
              ?capacityUnits:(Option.map
                                ~f:Values.CapacityUnitsConfiguration.of_json
                                capacityUnits)
              ?userTokenConfigurations:(Option.map
                                          ~f:Values.UserTokenConfigurationList.of_json
                                          userTokenConfigurations)
              ?userContextPolicy:(Option.map
                                    ~f:Values.UserContextPolicy.of_json
                                    userContextPolicy)
              ?userGroupResolutionConfiguration:(Option.map
                                                   ~f:Values.UserGroupResolutionConfiguration.of_json
                                                   userGroupResolutionConfiguration)
              ~id ()) None None])
let update_query_suggestions_block_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING QuerySuggestionsBlockListName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and sourceS3Path =
         flag "source-s3-path" (optional json_arg) ~doc:"JSON S3Path"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and id =
         flag "id" (required string)
           ~doc:"STRING QuerySuggestionsBlockListId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_query_suggestions_block_list
           (Values.UpdateQuerySuggestionsBlockListRequest.make ?name
              ?description
              ?sourceS3Path:(Option.map ~f:Values.S3Path.of_json sourceS3Path)
              ?roleArn ~indexId ~id ()) None None])
let update_query_suggestions_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON Mode"
       and queryLogLookBackWindowInDays =
         flag "query-log-look-back-window-in-days" (optional int)
           ~doc:"INT Integer"
       and includeQueriesWithoutUserInformation =
         flag "include-queries-without-user-information" (optional bool)
           ~doc:"BOOL ObjectBoolean"
       and minimumNumberOfQueryingUsers =
         flag "minimum-number-of-querying-users" (optional int)
           ~doc:"INT MinimumNumberOfQueryingUsers"
       and minimumQueryCount =
         flag "minimum-query-count" (optional int)
           ~doc:"INT MinimumQueryCount"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_query_suggestions_config
           (Values.UpdateQuerySuggestionsConfigRequest.make
              ?mode:(Option.map ~f:Values.Mode.of_json mode)
              ?queryLogLookBackWindowInDays
              ?includeQueriesWithoutUserInformation
              ?minimumNumberOfQueryingUsers ?minimumQueryCount ~indexId ())
           None None])
let update_thesaurus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ThesaurusName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and sourceS3Path =
         flag "source-s3-path" (optional json_arg) ~doc:"JSON S3Path"
       and id = flag "id" (required string) ~doc:"STRING ThesaurusId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_thesaurus
           (Values.UpdateThesaurusRequest.make ?name ?description ?roleArn
              ?sourceS3Path:(Option.map ~f:Values.S3Path.of_json sourceS3Path)
              ~id ~indexId ()) None None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-entities-to-experience", associate_entities_to_experience);
    ("associate-personas-to-entities", associate_personas_to_entities);
    ("batch-delete-document", batch_delete_document);
    ("batch-get-document-status", batch_get_document_status);
    ("batch-put-document", batch_put_document);
    ("clear-query-suggestions", clear_query_suggestions);
    ("create-data-source", create_data_source);
    ("create-experience", create_experience);
    ("create-faq", create_faq);
    ("create-index", create_index);
    ("create-query-suggestions-block-list",
      create_query_suggestions_block_list);
    ("create-thesaurus", create_thesaurus);
    ("delete-data-source", delete_data_source);
    ("delete-experience", delete_experience);
    ("delete-faq", delete_faq);
    ("delete-index", delete_index);
    ("delete-principal-mapping", delete_principal_mapping);
    ("delete-query-suggestions-block-list",
      delete_query_suggestions_block_list);
    ("delete-thesaurus", delete_thesaurus);
    ("describe-data-source", describe_data_source);
    ("describe-experience", describe_experience);
    ("describe-faq", describe_faq);
    ("describe-index", describe_index);
    ("describe-principal-mapping", describe_principal_mapping);
    ("describe-query-suggestions-block-list",
      describe_query_suggestions_block_list);
    ("describe-query-suggestions-config", describe_query_suggestions_config);
    ("describe-thesaurus", describe_thesaurus);
    ("disassociate-entities-from-experience",
      disassociate_entities_from_experience);
    ("disassociate-personas-from-entities",
      disassociate_personas_from_entities);
    ("get-query-suggestions", get_query_suggestions);
    ("get-snapshots", get_snapshots);
    ("list-data-source-sync-jobs", list_data_source_sync_jobs);
    ("list-data-sources", list_data_sources);
    ("list-entity-personas", list_entity_personas);
    ("list-experience-entities", list_experience_entities);
    ("list-experiences", list_experiences);
    ("list-faqs", list_faqs);
    ("list-groups-older-than-ordering-id",
      list_groups_older_than_ordering_id);
    ("list-indices", list_indices);
    ("list-query-suggestions-block-lists",
      list_query_suggestions_block_lists);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-thesauri", list_thesauri);
    ("put-principal-mapping", put_principal_mapping);
    ("query", query);
    ("start-data-source-sync-job", start_data_source_sync_job);
    ("stop-data-source-sync-job", stop_data_source_sync_job);
    ("submit-feedback", submit_feedback);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-data-source", update_data_source);
    ("update-experience", update_experience);
    ("update-index", update_index);
    ("update-query-suggestions-block-list",
      update_query_suggestions_block_list);
    ("update-query-suggestions-config", update_query_suggestions_config);
    ("update-thesaurus", update_thesaurus)]
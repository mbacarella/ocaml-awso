(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_channel_handshake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ChannelHandshakeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_channel_handshake
           (Values.AcceptChannelHandshakeRequest.make ~catalog ~identifier ())
           (Some Values.AcceptChannelHandshakeResponse.to_json)
           (Some Values.AcceptChannelHandshakeResponse.error_to_json)])
let cancel_channel_handshake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ChannelHandshakeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_channel_handshake
           (Values.CancelChannelHandshakeRequest.make ~catalog ~identifier ())
           (Some Values.CancelChannelHandshakeResponse.to_json)
           (Some Values.CancelChannelHandshakeResponse.error_to_json)])
let create_channel_handshake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and payload =
         flag "payload" (optional json_arg)
           ~doc:"JSON ChannelHandshakePayload"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and handshakeType =
         flag "handshake-type" (required json_arg) ~doc:"JSON HandshakeType"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and associatedResourceIdentifier =
         flag "associated-resource-identifier" (required string)
           ~doc:"STRING AssociatedResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel_handshake
           (Values.CreateChannelHandshakeRequest.make
              ?payload:(Option.map ~f:Values.ChannelHandshakePayload.of_json
                          payload) ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~handshakeType:(Values.HandshakeType.of_json handshakeType)
              ~catalog ~associatedResourceIdentifier ())
           (Some Values.CreateChannelHandshakeResponse.to_json)
           (Some Values.CreateChannelHandshakeResponse.error_to_json)])
let create_program_management_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and program = flag "program" (required json_arg) ~doc:"JSON Program"
       and displayName =
         flag "display-name" (required string)
           ~doc:"STRING ProgramManagementAccountDisplayName"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_program_management_account
           (Values.CreateProgramManagementAccountRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~catalog
              ~program:(Values.Program.of_json program) ~displayName
              ~accountId ())
           (Some Values.CreateProgramManagementAccountResponse.to_json)
           (Some Values.CreateProgramManagementAccountResponse.error_to_json)])
let create_relationship =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resaleAccountModel =
         flag "resale-account-model" (optional json_arg)
           ~doc:"JSON ResaleAccountModel"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and requestedSupportPlan =
         flag "requested-support-plan" (optional json_arg)
           ~doc:"JSON SupportPlan"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and associationType =
         flag "association-type" (required json_arg)
           ~doc:"JSON AssociationType"
       and programManagementAccountIdentifier =
         flag "program-management-account-identifier" (required string)
           ~doc:"STRING ProgramManagementAccountIdentifier"
       and associatedAccountId =
         flag "associated-account-id" (required string)
           ~doc:"STRING AccountId"
       and displayName =
         flag "display-name" (required string)
           ~doc:"STRING RelationshipDisplayName"
       and sector = flag "sector" (required json_arg) ~doc:"JSON Sector" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_relationship
           (Values.CreateRelationshipRequest.make
              ?resaleAccountModel:(Option.map
                                     ~f:Values.ResaleAccountModel.of_json
                                     resaleAccountModel) ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?requestedSupportPlan:(Option.map ~f:Values.SupportPlan.of_json
                                       requestedSupportPlan) ~catalog
              ~associationType:(Values.AssociationType.of_json
                                  associationType)
              ~programManagementAccountIdentifier ~associatedAccountId
              ~displayName ~sector:(Values.Sector.of_json sector) ())
           (Some Values.CreateRelationshipResponse.to_json)
           (Some Values.CreateRelationshipResponse.error_to_json)])
let delete_program_management_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ProgramManagementAccountIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_program_management_account
           (Values.DeleteProgramManagementAccountRequest.make ?clientToken
              ~catalog ~identifier ())
           (Some Values.DeleteProgramManagementAccountResponse.to_json)
           (Some Values.DeleteProgramManagementAccountResponse.error_to_json)])
let delete_relationship =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING RelationshipIdentifier"
       and programManagementAccountIdentifier =
         flag "program-management-account-identifier" (required string)
           ~doc:"STRING ProgramManagementAccountIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_relationship
           (Values.DeleteRelationshipRequest.make ?clientToken ~catalog
              ~identifier ~programManagementAccountIdentifier ())
           (Some Values.DeleteRelationshipResponse.to_json)
           (Some Values.DeleteRelationshipResponse.error_to_json)])
let get_relationship =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and programManagementAccountIdentifier =
         flag "program-management-account-identifier" (required string)
           ~doc:"STRING ProgramManagementAccountIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING RelationshipIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_relationship
           (Values.GetRelationshipRequest.make ~catalog
              ~programManagementAccountIdentifier ~identifier ())
           (Some Values.GetRelationshipResponse.to_json)
           (Some Values.GetRelationshipResponse.error_to_json)])
let list_channel_handshakes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListChannelHandshakesRequestMaxResultsInteger"
       and statuses =
         flag "statuses" (optional json_arg) ~doc:"JSON HandshakeStatusList"
       and associatedResourceIdentifiers =
         flag "associated-resource-identifiers" (optional json_arg)
           ~doc:"JSON AssociatedResourceIdentifierList"
       and handshakeTypeFilters =
         flag "handshake-type-filters" (optional json_arg)
           ~doc:"JSON ListChannelHandshakesTypeFilters"
       and handshakeTypeSort =
         flag "handshake-type-sort" (optional json_arg)
           ~doc:"JSON ListChannelHandshakesTypeSort"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and handshakeType =
         flag "handshake-type" (required json_arg) ~doc:"JSON HandshakeType"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and participantType =
         flag "participant-type" (required json_arg)
           ~doc:"JSON ParticipantType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channel_handshakes
           (Values.ListChannelHandshakesRequest.make ?maxResults
              ?statuses:(Option.map ~f:Values.HandshakeStatusList.of_json
                           statuses)
              ?associatedResourceIdentifiers:(Option.map
                                                ~f:Values.AssociatedResourceIdentifierList.of_json
                                                associatedResourceIdentifiers)
              ?handshakeTypeFilters:(Option.map
                                       ~f:Values.ListChannelHandshakesTypeFilters.of_json
                                       handshakeTypeFilters)
              ?handshakeTypeSort:(Option.map
                                    ~f:Values.ListChannelHandshakesTypeSort.of_json
                                    handshakeTypeSort) ?nextToken
              ~handshakeType:(Values.HandshakeType.of_json handshakeType)
              ~catalog
              ~participantType:(Values.ParticipantType.of_json
                                  participantType) ())
           (Some Values.ListChannelHandshakesResponse.to_json)
           (Some Values.ListChannelHandshakesResponse.error_to_json)])
let list_program_management_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListProgramManagementAccountsRequestMaxResultsInteger"
       and displayNames =
         flag "display-names" (optional json_arg)
           ~doc:"JSON ProgramManagementAccountDisplayNameList"
       and programs =
         flag "programs" (optional json_arg) ~doc:"JSON ProgramList"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIdList"
       and statuses =
         flag "statuses" (optional json_arg)
           ~doc:"JSON ProgramManagementAccountStatusList"
       and sort =
         flag "sort" (optional json_arg)
           ~doc:"JSON ListProgramManagementAccountsSortBase"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_program_management_accounts
           (Values.ListProgramManagementAccountsRequest.make ?maxResults
              ?displayNames:(Option.map
                               ~f:Values.ProgramManagementAccountDisplayNameList.of_json
                               displayNames)
              ?programs:(Option.map ~f:Values.ProgramList.of_json programs)
              ?accountIds:(Option.map ~f:Values.AccountIdList.of_json
                             accountIds)
              ?statuses:(Option.map
                           ~f:Values.ProgramManagementAccountStatusList.of_json
                           statuses)
              ?sort:(Option.map
                       ~f:Values.ListProgramManagementAccountsSortBase.of_json
                       sort) ?nextToken ~catalog ())
           (Some Values.ListProgramManagementAccountsResponse.to_json)
           (Some Values.ListProgramManagementAccountsResponse.error_to_json)])
let list_relationships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRelationshipsRequestMaxResultsInteger"
       and associatedAccountIds =
         flag "associated-account-ids" (optional json_arg)
           ~doc:"JSON AccountIdList"
       and associationTypes =
         flag "association-types" (optional json_arg)
           ~doc:"JSON AssociationTypeList"
       and displayNames =
         flag "display-names" (optional json_arg)
           ~doc:"JSON RelationshipDisplayNameList"
       and programManagementAccountIdentifiers =
         flag "program-management-account-identifiers" (optional json_arg)
           ~doc:"JSON ProgramManagementAccountIdentifierList"
       and sort =
         flag "sort" (optional json_arg)
           ~doc:"JSON ListRelationshipsSortBase"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_relationships
           (Values.ListRelationshipsRequest.make ?maxResults
              ?associatedAccountIds:(Option.map
                                       ~f:Values.AccountIdList.of_json
                                       associatedAccountIds)
              ?associationTypes:(Option.map
                                   ~f:Values.AssociationTypeList.of_json
                                   associationTypes)
              ?displayNames:(Option.map
                               ~f:Values.RelationshipDisplayNameList.of_json
                               displayNames)
              ?programManagementAccountIdentifiers:(Option.map
                                                      ~f:Values.ProgramManagementAccountIdentifierList.of_json
                                                      programManagementAccountIdentifiers)
              ?sort:(Option.map ~f:Values.ListRelationshipsSortBase.of_json
                       sort) ?nextToken ~catalog ())
           (Some Values.ListRelationshipsResponse.to_json)
           (Some Values.ListRelationshipsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TaggableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let reject_channel_handshake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ChannelHandshakeIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_channel_handshake
           (Values.RejectChannelHandshakeRequest.make ~catalog ~identifier ())
           (Some Values.RejectChannelHandshakeResponse.to_json)
           (Some Values.RejectChannelHandshakeResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TaggableArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TaggableArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_program_management_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING ProgramManagementAccountDisplayName"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ProgramManagementAccountIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_program_management_account
           (Values.UpdateProgramManagementAccountRequest.make ?revision
              ?displayName ~catalog ~identifier ())
           (Some Values.UpdateProgramManagementAccountResponse.to_json)
           (Some Values.UpdateProgramManagementAccountResponse.error_to_json)])
let update_relationship =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional string) ~doc:"STRING Revision"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING RelationshipDisplayName"
       and requestedSupportPlan =
         flag "requested-support-plan" (optional json_arg)
           ~doc:"JSON SupportPlan"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING RelationshipIdentifier"
       and programManagementAccountIdentifier =
         flag "program-management-account-identifier" (required string)
           ~doc:"STRING ProgramManagementAccountIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_relationship
           (Values.UpdateRelationshipRequest.make ?revision ?displayName
              ?requestedSupportPlan:(Option.map ~f:Values.SupportPlan.of_json
                                       requestedSupportPlan) ~catalog
              ~identifier ~programManagementAccountIdentifier ())
           (Some Values.UpdateRelationshipResponse.to_json)
           (Some Values.UpdateRelationshipResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-channel-handshake", accept_channel_handshake);
    ("cancel-channel-handshake", cancel_channel_handshake);
    ("create-channel-handshake", create_channel_handshake);
    ("create-program-management-account", create_program_management_account);
    ("create-relationship", create_relationship);
    ("delete-program-management-account", delete_program_management_account);
    ("delete-relationship", delete_relationship);
    ("get-relationship", get_relationship);
    ("list-channel-handshakes", list_channel_handshakes);
    ("list-program-management-accounts", list_program_management_accounts);
    ("list-relationships", list_relationships);
    ("list-tags-for-resource", list_tags_for_resource);
    ("reject-channel-handshake", reject_channel_handshake);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-program-management-account", update_program_management_account);
    ("update-relationship", update_relationship)]
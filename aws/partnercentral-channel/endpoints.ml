(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | AcceptChannelHandshake: (AcceptChannelHandshakeRequest.t,
  AcceptChannelHandshakeResponse.t, AcceptChannelHandshakeResponse.error) t 
  | CancelChannelHandshake: (CancelChannelHandshakeRequest.t,
  CancelChannelHandshakeResponse.t, CancelChannelHandshakeResponse.error) t 
  | CreateChannelHandshake: (CreateChannelHandshakeRequest.t,
  CreateChannelHandshakeResponse.t, CreateChannelHandshakeResponse.error) t 
  | CreateProgramManagementAccount: (CreateProgramManagementAccountRequest.t,
  CreateProgramManagementAccountResponse.t,
  CreateProgramManagementAccountResponse.error) t 
  | CreateRelationship: (CreateRelationshipRequest.t,
  CreateRelationshipResponse.t, CreateRelationshipResponse.error) t 
  | DeleteProgramManagementAccount: (DeleteProgramManagementAccountRequest.t,
  DeleteProgramManagementAccountResponse.t,
  DeleteProgramManagementAccountResponse.error) t 
  | DeleteRelationship: (DeleteRelationshipRequest.t,
  DeleteRelationshipResponse.t, DeleteRelationshipResponse.error) t 
  | GetRelationship: (GetRelationshipRequest.t, GetRelationshipResponse.t,
  GetRelationshipResponse.error) t 
  | ListChannelHandshakes: (ListChannelHandshakesRequest.t,
  ListChannelHandshakesResponse.t, ListChannelHandshakesResponse.error) t 
  | ListProgramManagementAccounts: (ListProgramManagementAccountsRequest.t,
  ListProgramManagementAccountsResponse.t,
  ListProgramManagementAccountsResponse.error) t 
  | ListRelationships: (ListRelationshipsRequest.t,
  ListRelationshipsResponse.t, ListRelationshipsResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | RejectChannelHandshake: (RejectChannelHandshakeRequest.t,
  RejectChannelHandshakeResponse.t, RejectChannelHandshakeResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateProgramManagementAccount: (UpdateProgramManagementAccountRequest.t,
  UpdateProgramManagementAccountResponse.t,
  UpdateProgramManagementAccountResponse.error) t 
  | UpdateRelationship: (UpdateRelationshipRequest.t,
  UpdateRelationshipResponse.t, UpdateRelationshipResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | AcceptChannelHandshake -> `POST
  | CancelChannelHandshake -> `POST
  | CreateChannelHandshake -> `POST
  | CreateProgramManagementAccount -> `POST
  | CreateRelationship -> `POST
  | DeleteProgramManagementAccount -> `POST
  | DeleteRelationship -> `POST
  | GetRelationship -> `POST
  | ListChannelHandshakes -> `POST
  | ListProgramManagementAccounts -> `POST
  | ListRelationships -> `POST
  | ListTagsForResource -> `POST
  | RejectChannelHandshake -> `POST
  | TagResource -> `POST
  | UntagResource -> `POST
  | UpdateProgramManagementAccount -> `POST
  | UpdateRelationship -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | AcceptChannelHandshake -> (Format.kasprintf Uri.of_string) "/"
      | CancelChannelHandshake -> (Format.kasprintf Uri.of_string) "/"
      | CreateChannelHandshake -> (Format.kasprintf Uri.of_string) "/"
      | CreateProgramManagementAccount ->
          (Format.kasprintf Uri.of_string) "/"
      | CreateRelationship -> (Format.kasprintf Uri.of_string) "/"
      | DeleteProgramManagementAccount ->
          (Format.kasprintf Uri.of_string) "/"
      | DeleteRelationship -> (Format.kasprintf Uri.of_string) "/"
      | GetRelationship -> (Format.kasprintf Uri.of_string) "/"
      | ListChannelHandshakes -> (Format.kasprintf Uri.of_string) "/"
      | ListProgramManagementAccounts -> (Format.kasprintf Uri.of_string) "/"
      | ListRelationships -> (Format.kasprintf Uri.of_string) "/"
      | ListTagsForResource -> (Format.kasprintf Uri.of_string) "/"
      | RejectChannelHandshake -> (Format.kasprintf Uri.of_string) "/"
      | TagResource -> (Format.kasprintf Uri.of_string) "/"
      | UntagResource -> (Format.kasprintf Uri.of_string) "/"
      | UpdateProgramManagementAccount ->
          (Format.kasprintf Uri.of_string) "/"
      | UpdateRelationship -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | AcceptChannelHandshake ->
      let json = AcceptChannelHandshakeRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.AcceptChannelHandshake")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CancelChannelHandshake ->
      let json = CancelChannelHandshakeRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.CancelChannelHandshake")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreateChannelHandshake ->
      let json = CreateChannelHandshakeRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.CreateChannelHandshake")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreateProgramManagementAccount ->
      let json = CreateProgramManagementAccountRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "PartnerCentralChannel.CreateProgramManagementAccount")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreateRelationship ->
      let json = CreateRelationshipRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.CreateRelationship")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteProgramManagementAccount ->
      let json = DeleteProgramManagementAccountRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "PartnerCentralChannel.DeleteProgramManagementAccount")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteRelationship ->
      let json = DeleteRelationshipRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.DeleteRelationship")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetRelationship ->
      let json = GetRelationshipRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.GetRelationship")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListChannelHandshakes ->
      let json = ListChannelHandshakesRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.ListChannelHandshakes")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListProgramManagementAccounts ->
      let json = ListProgramManagementAccountsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "PartnerCentralChannel.ListProgramManagementAccounts")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListRelationships ->
      let json = ListRelationshipsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.ListRelationships")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListTagsForResource ->
      let json = ListTagsForResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.ListTagsForResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RejectChannelHandshake ->
      let json = RejectChannelHandshakeRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.RejectChannelHandshake")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | TagResource ->
      let json = TagResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.TagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UntagResource ->
      let json = UntagResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.UntagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateProgramManagementAccount ->
      let json = UpdateProgramManagementAccountRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "PartnerCentralChannel.UpdateProgramManagementAccount")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateRelationship ->
      let json = UpdateRelationshipRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "PartnerCentralChannel.UpdateRelationship")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_json, ((code >= 400) && (code <= 599))) with
    | (Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        (match json |> (Yojson.Safe.Util.member "__type") with
         | `String error_type -> error_of_json error_type json
         | `Null -> bail ()
         | _ ->
             failwith
               (sprintf "Error '__type' did not have string type: %s" body))
    | (None, _) | (_, false) -> bail () in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | AcceptChannelHandshake ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (AcceptChannelHandshakeResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some AcceptChannelHandshakeResponse.error_of_json))
  | CancelChannelHandshake ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CancelChannelHandshakeResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some CancelChannelHandshakeResponse.error_of_json))
  | CreateChannelHandshake ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateChannelHandshakeResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some CreateChannelHandshakeResponse.error_of_json))
  | CreateProgramManagementAccount ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateProgramManagementAccountResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some CreateProgramManagementAccountResponse.error_of_json))
  | CreateRelationship ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateRelationshipResponse.of_json json)
      else
        Error
          (parse_aws_error (Some CreateRelationshipResponse.error_of_json))
  | DeleteProgramManagementAccount ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeleteProgramManagementAccountResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some DeleteProgramManagementAccountResponse.error_of_json))
  | DeleteRelationship ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeleteRelationshipResponse.of_json json)
      else
        Error
          (parse_aws_error (Some DeleteRelationshipResponse.error_of_json))
  | GetRelationship ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetRelationshipResponse.of_json json)
      else
        Error (parse_aws_error (Some GetRelationshipResponse.error_of_json))
  | ListChannelHandshakes ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListChannelHandshakesResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ListChannelHandshakesResponse.error_of_json))
  | ListProgramManagementAccounts ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListProgramManagementAccountsResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some ListProgramManagementAccountsResponse.error_of_json))
  | ListRelationships ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListRelationshipsResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ListRelationshipsResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListTagsForResourceResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | RejectChannelHandshake ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (RejectChannelHandshakeResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some RejectChannelHandshakeResponse.error_of_json))
  | TagResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (TagResourceResponse.of_json json)
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UntagResourceResponse.of_json json)
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateProgramManagementAccount ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateProgramManagementAccountResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some UpdateProgramManagementAccountResponse.error_of_json))
  | UpdateRelationship ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateRelationshipResponse.of_json json)
      else
        Error
          (parse_aws_error (Some UpdateRelationshipResponse.error_of_json))
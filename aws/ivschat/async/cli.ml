(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_chat_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capabilities =
         flag "capabilities" (optional json_arg)
           ~doc:"JSON ChatTokenCapabilities"
       and sessionDurationInMinutes =
         flag "session-duration-in-minutes" (optional int)
           ~doc:"INT SessionDurationInMinutes"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON ChatTokenAttributes"
       and roomIdentifier =
         flag "room-identifier" (required string)
           ~doc:"STRING RoomIdentifier"
       and userId = flag "user-id" (required string) ~doc:"STRING UserID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_chat_token
           (Values.CreateChatTokenRequest.make
              ?capabilities:(Option.map
                               ~f:Values.ChatTokenCapabilities.of_json
                               capabilities) ?sessionDurationInMinutes
              ?attributes:(Option.map ~f:Values.ChatTokenAttributes.of_json
                             attributes) ~roomIdentifier ~userId ())
           (Some Values.CreateChatTokenResponse.to_json)
           (Some Values.CreateChatTokenResponse.error_to_json)])
let create_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING LoggingConfigurationName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and destinationConfiguration =
         flag "destination-configuration" (required json_arg)
           ~doc:"JSON DestinationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_logging_configuration
           (Values.CreateLoggingConfigurationRequest.make ?name
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~destinationConfiguration:(Values.DestinationConfiguration.of_json
                                           destinationConfiguration) ())
           (Some Values.CreateLoggingConfigurationResponse.to_json)
           (Some Values.CreateLoggingConfigurationResponse.error_to_json)])
let create_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RoomName"
       and maximumMessageRatePerSecond =
         flag "maximum-message-rate-per-second" (optional int)
           ~doc:"INT RoomMaxMessageRatePerSecond"
       and maximumMessageLength =
         flag "maximum-message-length" (optional int)
           ~doc:"INT RoomMaxMessageLength"
       and messageReviewHandler =
         flag "message-review-handler" (optional json_arg)
           ~doc:"JSON MessageReviewHandler"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and loggingConfigurationIdentifiers =
         flag "logging-configuration-identifiers" (optional json_arg)
           ~doc:"JSON LoggingConfigurationIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_room
           (Values.CreateRoomRequest.make ?name ?maximumMessageRatePerSecond
              ?maximumMessageLength
              ?messageReviewHandler:(Option.map
                                       ~f:Values.MessageReviewHandler.of_json
                                       messageReviewHandler)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?loggingConfigurationIdentifiers:(Option.map
                                                  ~f:Values.LoggingConfigurationIdentifierList.of_json
                                                  loggingConfigurationIdentifiers)
              ()) (Some Values.CreateRoomResponse.to_json)
           (Some Values.CreateRoomResponse.error_to_json)])
let delete_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING LoggingConfigurationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_logging_configuration
           (Values.DeleteLoggingConfigurationRequest.make ~identifier ())
           None None])
let delete_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason = flag "reason" (optional string) ~doc:"STRING Reason"
       and roomIdentifier =
         flag "room-identifier" (required string)
           ~doc:"STRING RoomIdentifier"
       and id = flag "id" (required string) ~doc:"STRING MessageID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_message
           (Values.DeleteMessageRequest.make ?reason ~roomIdentifier ~id ())
           (Some Values.DeleteMessageResponse.to_json)
           (Some Values.DeleteMessageResponse.error_to_json)])
let delete_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RoomIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_room (Values.DeleteRoomRequest.make ~identifier ()) None
           None])
let disconnect_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason = flag "reason" (optional string) ~doc:"STRING Reason"
       and roomIdentifier =
         flag "room-identifier" (required string)
           ~doc:"STRING RoomIdentifier"
       and userId = flag "user-id" (required string) ~doc:"STRING UserID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disconnect_user
           (Values.DisconnectUserRequest.make ?reason ~roomIdentifier ~userId
              ()) (Some Values.DisconnectUserResponse.to_json)
           (Some Values.DisconnectUserResponse.error_to_json)])
let get_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING LoggingConfigurationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_logging_configuration
           (Values.GetLoggingConfigurationRequest.make ~identifier ())
           (Some Values.GetLoggingConfigurationResponse.to_json)
           (Some Values.GetLoggingConfigurationResponse.error_to_json)])
let get_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RoomIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_room (Values.GetRoomRequest.make ~identifier ())
           (Some Values.GetRoomResponse.to_json)
           (Some Values.GetRoomResponse.error_to_json)])
let list_logging_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxLoggingConfigurationResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_logging_configurations
           (Values.ListLoggingConfigurationsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListLoggingConfigurationsResponse.to_json)
           (Some Values.ListLoggingConfigurationsResponse.error_to_json)])
let list_rooms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RoomName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxRoomResults"
       and messageReviewHandlerUri =
         flag "message-review-handler-uri" (optional string)
           ~doc:"STRING LambdaArn"
       and loggingConfigurationIdentifier =
         flag "logging-configuration-identifier" (optional string)
           ~doc:"STRING LoggingConfigurationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rooms
           (Values.ListRoomsRequest.make ?name ?nextToken ?maxResults
              ?messageReviewHandlerUri ?loggingConfigurationIdentifier ())
           (Some Values.ListRoomsResponse.to_json)
           (Some Values.ListRoomsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let send_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON EventAttributes"
       and roomIdentifier =
         flag "room-identifier" (required string)
           ~doc:"STRING RoomIdentifier"
       and eventName =
         flag "event-name" (required string) ~doc:"STRING EventName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_event
           (Values.SendEventRequest.make
              ?attributes:(Option.map ~f:Values.EventAttributes.of_json
                             attributes) ~roomIdentifier ~eventName ())
           (Some Values.SendEventResponse.to_json)
           (Some Values.SendEventResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING LoggingConfigurationName"
       and destinationConfiguration =
         flag "destination-configuration" (optional json_arg)
           ~doc:"JSON DestinationConfiguration"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING LoggingConfigurationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_logging_configuration
           (Values.UpdateLoggingConfigurationRequest.make ?name
              ?destinationConfiguration:(Option.map
                                           ~f:Values.DestinationConfiguration.of_json
                                           destinationConfiguration)
              ~identifier ())
           (Some Values.UpdateLoggingConfigurationResponse.to_json)
           (Some Values.UpdateLoggingConfigurationResponse.error_to_json)])
let update_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RoomName"
       and maximumMessageRatePerSecond =
         flag "maximum-message-rate-per-second" (optional int)
           ~doc:"INT RoomMaxMessageRatePerSecond"
       and maximumMessageLength =
         flag "maximum-message-length" (optional int)
           ~doc:"INT RoomMaxMessageLength"
       and messageReviewHandler =
         flag "message-review-handler" (optional json_arg)
           ~doc:"JSON MessageReviewHandler"
       and loggingConfigurationIdentifiers =
         flag "logging-configuration-identifiers" (optional json_arg)
           ~doc:"JSON LoggingConfigurationIdentifierList"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING RoomIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_room
           (Values.UpdateRoomRequest.make ?name ?maximumMessageRatePerSecond
              ?maximumMessageLength
              ?messageReviewHandler:(Option.map
                                       ~f:Values.MessageReviewHandler.of_json
                                       messageReviewHandler)
              ?loggingConfigurationIdentifiers:(Option.map
                                                  ~f:Values.LoggingConfigurationIdentifierList.of_json
                                                  loggingConfigurationIdentifiers)
              ~identifier ()) (Some Values.UpdateRoomResponse.to_json)
           (Some Values.UpdateRoomResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-chat-token", create_chat_token);
    ("create-logging-configuration", create_logging_configuration);
    ("create-room", create_room);
    ("delete-logging-configuration", delete_logging_configuration);
    ("delete-message", delete_message);
    ("delete-room", delete_room);
    ("disconnect-user", disconnect_user);
    ("get-logging-configuration", get_logging_configuration);
    ("get-room", get_room);
    ("list-logging-configurations", list_logging_configurations);
    ("list-rooms", list_rooms);
    ("list-tags-for-resource", list_tags_for_resource);
    ("send-event", send_event);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-logging-configuration", update_logging_configuration);
    ("update-room", update_room)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expiresTime =
         flag "expires-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and name = flag "name" (required string) ~doc:"STRING AccessTokenName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_token
           (Values.CreateAccessTokenRequest.make
              ?expiresTime:(Option.map
                              ~f:Values.SyntheticTimestamp_date_time.of_json
                              expiresTime) ~name ())
           (Some Values.CreateAccessTokenResponse.to_json)
           (Some Values.CreateAccessTokenResponse.error_to_json)])
let create_dev_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and repositories =
         flag "repositories" (optional json_arg)
           ~doc:"JSON RepositoriesInput"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and alias =
         flag "alias" (optional string)
           ~doc:"STRING CreateDevEnvironmentRequestAliasString"
       and ides =
         flag "ides" (optional json_arg) ~doc:"JSON IdeConfigurationList"
       and inactivityTimeoutMinutes =
         flag "inactivity-timeout-minutes" (optional int)
           ~doc:"INT InactivityTimeoutMinutes"
       and vpcConnectionName =
         flag "vpc-connection-name" (optional string)
           ~doc:"STRING NameString"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and instanceType =
         flag "instance-type" (required json_arg) ~doc:"JSON InstanceType"
       and persistentStorage =
         flag "persistent-storage" (required json_arg)
           ~doc:"JSON PersistentStorageConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dev_environment
           (Values.CreateDevEnvironmentRequest.make
              ?repositories:(Option.map ~f:Values.RepositoriesInput.of_json
                               repositories) ?clientToken ?alias
              ?ides:(Option.map ~f:Values.IdeConfigurationList.of_json ides)
              ?inactivityTimeoutMinutes ?vpcConnectionName ~spaceName
              ~projectName
              ~instanceType:(Values.InstanceType.of_json instanceType)
              ~persistentStorage:(Values.PersistentStorageConfiguration.of_json
                                    persistentStorage) ())
           (Some Values.CreateDevEnvironmentResponse.to_json)
           (Some Values.CreateDevEnvironmentResponse.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProjectDescription"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and displayName =
         flag "display-name" (required string)
           ~doc:"STRING ProjectDisplayName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectRequest.make ?description ~spaceName
              ~displayName ()) (Some Values.CreateProjectResponse.to_json)
           (Some Values.CreateProjectResponse.error_to_json)])
let create_source_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SourceRepositoryDescriptionString"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and name =
         flag "name" (required string)
           ~doc:"STRING SourceRepositoryNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_source_repository
           (Values.CreateSourceRepositoryRequest.make ?description ~spaceName
              ~projectName ~name ())
           (Some Values.CreateSourceRepositoryResponse.to_json)
           (Some Values.CreateSourceRepositoryResponse.error_to_json)])
let create_source_repository_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and headCommitId =
         flag "head-commit-id" (optional string) ~doc:"STRING String"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and sourceRepositoryName =
         flag "source-repository-name" (required string)
           ~doc:"STRING SourceRepositoryNameString"
       and name =
         flag "name" (required string)
           ~doc:"STRING SourceRepositoryBranchString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_source_repository_branch
           (Values.CreateSourceRepositoryBranchRequest.make ?headCommitId
              ~spaceName ~projectName ~sourceRepositoryName ~name ())
           (Some Values.CreateSourceRepositoryBranchResponse.to_json)
           (Some Values.CreateSourceRepositoryBranchResponse.error_to_json)])
let delete_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING AccessTokenId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_token
           (Values.DeleteAccessTokenRequest.make ~id ())
           (Some Values.DeleteAccessTokenResponse.to_json)
           (Some Values.DeleteAccessTokenResponse.error_to_json)])
let delete_dev_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dev_environment
           (Values.DeleteDevEnvironmentRequest.make ~spaceName ~projectName
              ~id ()) (Some Values.DeleteDevEnvironmentResponse.to_json)
           (Some Values.DeleteDevEnvironmentResponse.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project
           (Values.DeleteProjectRequest.make ~spaceName ~name ())
           (Some Values.DeleteProjectResponse.to_json)
           (Some Values.DeleteProjectResponse.error_to_json)])
let delete_source_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and name =
         flag "name" (required string)
           ~doc:"STRING SourceRepositoryNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_source_repository
           (Values.DeleteSourceRepositoryRequest.make ~spaceName ~projectName
              ~name ()) (Some Values.DeleteSourceRepositoryResponse.to_json)
           (Some Values.DeleteSourceRepositoryResponse.error_to_json)])
let delete_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_space (Values.DeleteSpaceRequest.make ~name ())
           (Some Values.DeleteSpaceResponse.to_json)
           (Some Values.DeleteSpaceResponse.error_to_json)])
let get_dev_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dev_environment
           (Values.GetDevEnvironmentRequest.make ~spaceName ~projectName ~id
              ()) (Some Values.GetDevEnvironmentResponse.to_json)
           (Some Values.GetDevEnvironmentResponse.error_to_json)])
let get_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_project (Values.GetProjectRequest.make ~spaceName ~name ())
           (Some Values.GetProjectResponse.to_json)
           (Some Values.GetProjectResponse.error_to_json)])
let get_source_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and name =
         flag "name" (required string)
           ~doc:"STRING SourceRepositoryNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_source_repository
           (Values.GetSourceRepositoryRequest.make ~spaceName ~projectName
              ~name ()) (Some Values.GetSourceRepositoryResponse.to_json)
           (Some Values.GetSourceRepositoryResponse.error_to_json)])
let get_source_repository_clone_urls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and sourceRepositoryName =
         flag "source-repository-name" (required string)
           ~doc:"STRING SourceRepositoryNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_source_repository_clone_urls
           (Values.GetSourceRepositoryCloneUrlsRequest.make ~spaceName
              ~projectName ~sourceRepositoryName ())
           (Some Values.GetSourceRepositoryCloneUrlsResponse.to_json)
           (Some Values.GetSourceRepositoryCloneUrlsResponse.error_to_json)])
let get_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_space (Values.GetSpaceRequest.make ~name ())
           (Some Values.GetSpaceResponse.to_json)
           (Some Values.GetSpaceResponse.error_to_json)])
let get_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription
           (Values.GetSubscriptionRequest.make ~spaceName ())
           (Some Values.GetSubscriptionResponse.to_json)
           (Some Values.GetSubscriptionResponse.error_to_json)])
let get_user_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (optional string)
           ~doc:"STRING GetUserDetailsRequestIdString"
       and userName =
         flag "user-name" (optional string)
           ~doc:"STRING GetUserDetailsRequestUserNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user_details
           (Values.GetUserDetailsRequest.make ?id ?userName ())
           (Some Values.GetUserDetailsResponse.to_json)
           (Some Values.GetUserDetailsResponse.error_to_json)])
let get_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid"
       and projectName =
         flag "project-name" (required string)
           ~doc:"STRING GetWorkflowRequestProjectNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow
           (Values.GetWorkflowRequest.make ~spaceName ~id ~projectName ())
           (Some Values.GetWorkflowResponse.to_json)
           (Some Values.GetWorkflowResponse.error_to_json)])
let get_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid"
       and projectName =
         flag "project-name" (required string)
           ~doc:"STRING GetWorkflowRunRequestProjectNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_run
           (Values.GetWorkflowRunRequest.make ~spaceName ~id ~projectName ())
           (Some Values.GetWorkflowRunResponse.to_json)
           (Some Values.GetWorkflowRunResponse.error_to_json)])
let list_access_tokens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAccessTokensRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListAccessTokensRequestNextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_tokens
           (Values.ListAccessTokensRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAccessTokensResponse.to_json)
           (Some Values.ListAccessTokensResponse.error_to_json)])
let list_dev_environment_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListDevEnvironmentSessionsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListDevEnvironmentSessionsRequestMaxResultsInteger"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and devEnvironmentId =
         flag "dev-environment-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dev_environment_sessions
           (Values.ListDevEnvironmentSessionsRequest.make ?nextToken
              ?maxResults ~spaceName ~projectName ~devEnvironmentId ())
           (Some Values.ListDevEnvironmentSessionsResponse.to_json)
           (Some Values.ListDevEnvironmentSessionsResponse.error_to_json)])
let list_dev_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (optional string) ~doc:"STRING NameString"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListDevEnvironmentsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListDevEnvironmentsRequestMaxResultsInteger"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dev_environments
           (Values.ListDevEnvironmentsRequest.make ?projectName
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?nextToken ?maxResults ~spaceName ())
           (Some Values.ListDevEnvironmentsResponse.to_json)
           (Some Values.ListDevEnvironmentsResponse.error_to_json)])
let list_event_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventName =
         flag "event-name" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListEventLogsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEventLogsRequestMaxResultsInteger"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and startTime =
         flag "start-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (required json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_logs
           (Values.ListEventLogsRequest.make ?eventName ?nextToken
              ?maxResults ~spaceName
              ~startTime:(Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ~endTime:(Values.SyntheticTimestamp_date_time.of_json endTime)
              ()) (Some Values.ListEventLogsResponse.to_json)
           (Some Values.ListEventLogsResponse.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListProjectsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListProjectsRequestMaxResultsInteger"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ProjectListFilters"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.ProjectListFilters.of_json
                          filters) ~spaceName ())
           (Some Values.ListProjectsResponse.to_json)
           (Some Values.ListProjectsResponse.error_to_json)])
let list_source_repositories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListSourceRepositoriesRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSourceRepositoriesRequestMaxResultsInteger"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_source_repositories
           (Values.ListSourceRepositoriesRequest.make ?nextToken ?maxResults
              ~spaceName ~projectName ())
           (Some Values.ListSourceRepositoriesResponse.to_json)
           (Some Values.ListSourceRepositoriesResponse.error_to_json)])
let list_source_repository_branches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListSourceRepositoryBranchesRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSourceRepositoryBranchesRequestMaxResultsInteger"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and sourceRepositoryName =
         flag "source-repository-name" (required string)
           ~doc:"STRING SourceRepositoryNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_source_repository_branches
           (Values.ListSourceRepositoryBranchesRequest.make ?nextToken
              ?maxResults ~spaceName ~projectName ~sourceRepositoryName ())
           (Some Values.ListSourceRepositoryBranchesResponse.to_json)
           (Some Values.ListSourceRepositoryBranchesResponse.error_to_json)])
let list_spaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListSpacesRequestNextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_spaces (Values.ListSpacesRequest.make ?nextToken ())
           (Some Values.ListSpacesResponse.to_json)
           (Some Values.ListSpacesResponse.error_to_json)])
let list_workflow_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (optional string) ~doc:"STRING Uuid"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListWorkflowRunsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkflowRunsRequestMaxResultsInteger"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON WorkflowRunSortCriteriaList"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string)
           ~doc:"STRING ListWorkflowRunsRequestProjectNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflow_runs
           (Values.ListWorkflowRunsRequest.make ?workflowId ?nextToken
              ?maxResults
              ?sortBy:(Option.map
                         ~f:Values.WorkflowRunSortCriteriaList.of_json sortBy)
              ~spaceName ~projectName ())
           (Some Values.ListWorkflowRunsResponse.to_json)
           (Some Values.ListWorkflowRunsResponse.error_to_json)])
let list_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListWorkflowsRequestNextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkflowsRequestMaxResultsInteger"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON WorkflowSortCriteriaList"
       and spaceName =
         flag "space-name" (required string)
           ~doc:"STRING ListWorkflowsRequestSpaceNameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflows
           (Values.ListWorkflowsRequest.make ?nextToken ?maxResults
              ?sortBy:(Option.map ~f:Values.WorkflowSortCriteriaList.of_json
                         sortBy) ~spaceName ~projectName ())
           (Some Values.ListWorkflowsResponse.to_json)
           (Some Values.ListWorkflowsResponse.error_to_json)])
let start_dev_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ides =
         flag "ides" (optional json_arg) ~doc:"JSON IdeConfigurationList"
       and instanceType =
         flag "instance-type" (optional json_arg) ~doc:"JSON InstanceType"
       and inactivityTimeoutMinutes =
         flag "inactivity-timeout-minutes" (optional int)
           ~doc:"INT InactivityTimeoutMinutes"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_dev_environment
           (Values.StartDevEnvironmentRequest.make
              ?ides:(Option.map ~f:Values.IdeConfigurationList.of_json ides)
              ?instanceType:(Option.map ~f:Values.InstanceType.of_json
                               instanceType) ?inactivityTimeoutMinutes
              ~spaceName ~projectName ~id ())
           (Some Values.StartDevEnvironmentResponse.to_json)
           (Some Values.StartDevEnvironmentResponse.error_to_json)])
let start_dev_environment_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid"
       and sessionConfiguration =
         flag "session-configuration" (required json_arg)
           ~doc:"JSON DevEnvironmentSessionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_dev_environment_session
           (Values.StartDevEnvironmentSessionRequest.make ~spaceName
              ~projectName ~id
              ~sessionConfiguration:(Values.DevEnvironmentSessionConfiguration.of_json
                                       sessionConfiguration) ())
           (Some Values.StartDevEnvironmentSessionResponse.to_json)
           (Some Values.StartDevEnvironmentSessionResponse.error_to_json)])
let start_workflow_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING StartWorkflowRunRequestClientTokenString"
       and spaceName =
         flag "space-name" (required string)
           ~doc:"STRING StartWorkflowRunRequestSpaceNameString"
       and projectName =
         flag "project-name" (required string)
           ~doc:"STRING StartWorkflowRunRequestProjectNameString"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_workflow_run
           (Values.StartWorkflowRunRequest.make ?clientToken ~spaceName
              ~projectName ~workflowId ())
           (Some Values.StartWorkflowRunResponse.to_json)
           (Some Values.StartWorkflowRunResponse.error_to_json)])
let stop_dev_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_dev_environment
           (Values.StopDevEnvironmentRequest.make ~spaceName ~projectName ~id
              ()) (Some Values.StopDevEnvironmentResponse.to_json)
           (Some Values.StopDevEnvironmentResponse.error_to_json)])
let stop_dev_environment_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid"
       and sessionId =
         flag "session-id" (required string)
           ~doc:"STRING StopDevEnvironmentSessionRequestSessionIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_dev_environment_session
           (Values.StopDevEnvironmentSessionRequest.make ~spaceName
              ~projectName ~id ~sessionId ())
           (Some Values.StopDevEnvironmentSessionResponse.to_json)
           (Some Values.StopDevEnvironmentSessionResponse.error_to_json)])
let update_dev_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alias =
         flag "alias" (optional string)
           ~doc:"STRING UpdateDevEnvironmentRequestAliasString"
       and ides =
         flag "ides" (optional json_arg) ~doc:"JSON IdeConfigurationList"
       and instanceType =
         flag "instance-type" (optional json_arg) ~doc:"JSON InstanceType"
       and inactivityTimeoutMinutes =
         flag "inactivity-timeout-minutes" (optional int)
           ~doc:"INT InactivityTimeoutMinutes"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING NameString"
       and id = flag "id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dev_environment
           (Values.UpdateDevEnvironmentRequest.make ?alias
              ?ides:(Option.map ~f:Values.IdeConfigurationList.of_json ides)
              ?instanceType:(Option.map ~f:Values.InstanceType.of_json
                               instanceType) ?inactivityTimeoutMinutes
              ?clientToken ~spaceName ~projectName ~id ())
           (Some Values.UpdateDevEnvironmentResponse.to_json)
           (Some Values.UpdateDevEnvironmentResponse.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProjectDescription"
       and spaceName =
         flag "space-name" (required string) ~doc:"STRING NameString"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectRequest.make ?description ~spaceName ~name ())
           (Some Values.UpdateProjectResponse.to_json)
           (Some Values.UpdateProjectResponse.error_to_json)])
let update_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING SpaceDescription"
       and name = flag "name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_space
           (Values.UpdateSpaceRequest.make ?description ~name ())
           (Some Values.UpdateSpaceResponse.to_json)
           (Some Values.UpdateSpaceResponse.error_to_json)])
let verify_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_session (Fn.id ())
           (Some Values.VerifySessionResponse.to_json)
           (Some Values.VerifySessionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-access-token", create_access_token);
    ("create-dev-environment", create_dev_environment);
    ("create-project", create_project);
    ("create-source-repository", create_source_repository);
    ("create-source-repository-branch", create_source_repository_branch);
    ("delete-access-token", delete_access_token);
    ("delete-dev-environment", delete_dev_environment);
    ("delete-project", delete_project);
    ("delete-source-repository", delete_source_repository);
    ("delete-space", delete_space);
    ("get-dev-environment", get_dev_environment);
    ("get-project", get_project);
    ("get-source-repository", get_source_repository);
    ("get-source-repository-clone-urls", get_source_repository_clone_urls);
    ("get-space", get_space);
    ("get-subscription", get_subscription);
    ("get-user-details", get_user_details);
    ("get-workflow", get_workflow);
    ("get-workflow-run", get_workflow_run);
    ("list-access-tokens", list_access_tokens);
    ("list-dev-environment-sessions", list_dev_environment_sessions);
    ("list-dev-environments", list_dev_environments);
    ("list-event-logs", list_event_logs);
    ("list-projects", list_projects);
    ("list-source-repositories", list_source_repositories);
    ("list-source-repository-branches", list_source_repository_branches);
    ("list-spaces", list_spaces);
    ("list-workflow-runs", list_workflow_runs);
    ("list-workflows", list_workflows);
    ("start-dev-environment", start_dev_environment);
    ("start-dev-environment-session", start_dev_environment_session);
    ("start-workflow-run", start_workflow_run);
    ("stop-dev-environment", stop_dev_environment);
    ("stop-dev-environment-session", stop_dev_environment_session);
    ("update-dev-environment", update_dev_environment);
    ("update-project", update_project);
    ("update-space", update_space);
    ("verify-session", verify_session)]
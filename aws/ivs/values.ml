(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.ivs
let apiVersion = "2020-07-14"
let endpointPrefix = "ivs"
let serviceFullName = "Amazon Interactive Video Service"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Amazon IVS"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module S3DestinationBucketName =
  struct
    type nonrec t = string
    let context_ = "S3DestinationBucketName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:63) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-z0-9-.]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3DestinationBucketName" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module S3DestinationConfiguration =
  struct
    type nonrec t =
      {
      bucketName: S3DestinationBucketName.t
        [@ocaml.doc
          "Location (S3 bucket name) where recorded videos will be stored."]}
    let context_ = "S3DestinationConfiguration"
    let make ~bucketName = fun () -> { bucketName }
    let to_value x =
      structure_to_value
        [("bucketName",
           (Some (S3DestinationBucketName.to_value x.bucketName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bucketName =
        S3DestinationBucketName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "bucketName") in
      make ~bucketName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let bucketName =
        field_map_exn json "bucketName" S3DestinationBucketName.of_json in
      make ~bucketName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that describes an S3 location where recorded videos will be stored."]
module Integer =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module RecordingMode =
  struct
    type nonrec t =
      | DISABLED 
      | INTERVAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DISABLED -> "DISABLED"
      | INTERVAL -> "INTERVAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "DISABLED" -> DISABLED
      | "INTERVAL" -> INTERVAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RecordingMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RecordingMode" j)
    let to_json = simple_to_json to_value
  end
module TargetIntervalSeconds =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int64_max i ~max:60L) >>=
             (fun () -> check_int64_min i ~min:5L));
        i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Time =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ChannelArn =
  struct
    type nonrec t = string
    let context_ = "ChannelArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws:[is]vs:[a-z0-9-]+:[0-9]+:channel/[a-zA-Z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChannelArn" j
    let to_json = simple_to_json to_value
  end
module StreamHealth =
  struct
    type nonrec t =
      | HEALTHY 
      | STARVING 
      | UNKNOWN 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HEALTHY -> "HEALTHY"
      | STARVING -> "STARVING"
      | UNKNOWN -> "UNKNOWN"
      | Non_static_id s -> s
    let of_string =
      function
      | "HEALTHY" -> HEALTHY
      | "STARVING" -> STARVING
      | "UNKNOWN" -> UNKNOWN
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamHealth" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamHealth" j)
    let to_json = simple_to_json to_value
  end
module StreamId =
  struct
    type nonrec t = string
    let context_ = "StreamId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:26) >>=
             (fun () ->
                (check_string_max i ~max:26) >>=
                  (fun () -> check_pattern i ~pattern:"^st-[a-zA-Z0-9]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamId" j
    let to_json = simple_to_json to_value
  end
module StreamStartTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module StreamState =
  struct
    type nonrec t =
      | LIVE 
      | OFFLINE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | LIVE -> "LIVE" | OFFLINE -> "OFFLINE" | Non_static_id s -> s
    let of_string =
      function | "LIVE" -> LIVE | "OFFLINE" -> OFFLINE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StreamState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StreamState" j)
    let to_json = simple_to_json to_value
  end
module StreamViewerCount =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module StreamKeyArn =
  struct
    type nonrec t = string
    let context_ = "StreamKeyArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws:[is]vs:[a-z0-9-]+:[0-9]+:stream-key/[a-zA-Z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamKeyArn" j
    let to_json = simple_to_json to_value
  end
module Tags =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module DestinationConfiguration =
  struct
    type nonrec t =
      {
      s3: S3DestinationConfiguration.t option
        [@ocaml.doc
          "An S3 destination configuration where recorded videos will be stored."]}
    let make ?s3 = fun () -> { s3 }
    let to_value x =
      structure_to_value
        [("s3", (Option.map x.s3 ~f:S3DestinationConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3 =
        (Option.map ~f:S3DestinationConfiguration.of_xml)
          (Xml.child xml_arg0 "s3") in
      make ?s3 ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let s3 = field_map json "s3" S3DestinationConfiguration.of_json in
      make ?s3 ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that describes a location where recorded videos will be stored. Each member represents a type of destination configuration. For recording, you define one and only one type of destination configuration."]
module RecordingConfigurationArn =
  struct
    type nonrec t = string
    let context_ = "RecordingConfigurationArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws:ivs:[a-z0-9-]+:[0-9]+:recording-configuration/[a-zA-Z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RecordingConfigurationArn" j
    let to_json = simple_to_json to_value
  end
module RecordingConfigurationName =
  struct
    type nonrec t = string
    let context_ = "RecordingConfigurationName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9-_]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RecordingConfigurationName" j
    let to_json = simple_to_json to_value
  end
module RecordingConfigurationState =
  struct
    type nonrec t =
      | CREATING 
      | CREATE_FAILED 
      | ACTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | CREATE_FAILED -> "CREATE_FAILED"
      | ACTIVE -> "ACTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "CREATE_FAILED" -> CREATE_FAILED
      | "ACTIVE" -> ACTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RecordingConfigurationState"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RecordingConfigurationState" j)
    let to_json = simple_to_json to_value
  end
module PlaybackKeyPairArn =
  struct
    type nonrec t = string
    let context_ = "PlaybackKeyPairArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws:[is]vs:[a-z0-9-]+:[0-9]+:playback-key/[a-zA-Z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlaybackKeyPairArn" j
    let to_json = simple_to_json to_value
  end
module PlaybackKeyPairName =
  struct
    type nonrec t = string
    let context_ = "PlaybackKeyPairName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9-_]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlaybackKeyPairName" j
    let to_json = simple_to_json to_value
  end
module ChannelLatencyMode =
  struct
    type nonrec t =
      | NORMAL 
      | LOW 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | NORMAL -> "NORMAL" | LOW -> "LOW" | Non_static_id s -> s
    let of_string =
      function | "NORMAL" -> NORMAL | "LOW" -> LOW | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ChannelLatencyMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ChannelLatencyMode" j)
    let to_json = simple_to_json to_value
  end
module ChannelName =
  struct
    type nonrec t = string
    let context_ = "ChannelName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"^[a-zA-Z0-9-_]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChannelName" j
    let to_json = simple_to_json to_value
  end
module ChannelRecordingConfigurationArn =
  struct
    type nonrec t = string
    let context_ = "ChannelRecordingConfigurationArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^$|^arn:aws:ivs:[a-z0-9-]+:[0-9]+:recording-configuration/[a-zA-Z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChannelRecordingConfigurationArn" j
    let to_json = simple_to_json to_value
  end
module IsAuthorized =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ChannelType =
  struct
    type nonrec t =
      | BASIC 
      | STANDARD 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BASIC -> "BASIC"
      | STANDARD -> "STANDARD"
      | Non_static_id s -> s
    let of_string =
      function
      | "BASIC" -> BASIC
      | "STANDARD" -> STANDARD
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ChannelType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ChannelType" j)
    let to_json = simple_to_json to_value
  end
module IngestEndpoint =
  struct
    type nonrec t = string
    let context_ = "IngestEndpoint"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IngestEndpoint" j
    let to_json = simple_to_json to_value
  end
module PlaybackURL =
  struct
    type nonrec t = string
    let context_ = "PlaybackURL"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlaybackURL" j
    let to_json = simple_to_json to_value
  end
module AudioConfiguration =
  struct
    type nonrec t =
      {
      channels: Integer.t option [@ocaml.doc "Number of audio channels."];
      codec: String_.t option
        [@ocaml.doc "Codec used for the audio encoding."];
      sampleRate: Integer.t option
        [@ocaml.doc "Number of audio samples recorded per second."];
      targetBitrate: Integer.t option
        [@ocaml.doc
          "The expected ingest bitrate (bits per second). This is configured in the encoder."]}
    let make ?channels =
      fun ?codec ->
        fun ?sampleRate ->
          fun ?targetBitrate ->
            fun () -> { channels; codec; sampleRate; targetBitrate }
    let to_value x =
      structure_to_value
        [("channels", (Option.map x.channels ~f:Integer.to_value));
        ("codec", (Option.map x.codec ~f:String_.to_value));
        ("sampleRate", (Option.map x.sampleRate ~f:Integer.to_value));
        ("targetBitrate", (Option.map x.targetBitrate ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetBitrate =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "targetBitrate") in
      let sampleRate =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "sampleRate") in
      let codec = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "codec") in
      let channels =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "channels") in
      make ?targetBitrate ?sampleRate ?codec ?channels ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let targetBitrate = field_map json "targetBitrate" Integer.of_json in
      let sampleRate = field_map json "sampleRate" Integer.of_json in
      let codec = field_map json "codec" String_.of_json in
      let channels = field_map json "channels" Integer.of_json in
      make ?targetBitrate ?sampleRate ?codec ?channels ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Object specifying a stream\226\128\153s audio configuration."]
module VideoConfiguration =
  struct
    type nonrec t =
      {
      avcLevel: String_.t option
        [@ocaml.doc
          "Indicates the degree of required decoder performance for a profile. Normally this is set automatically by the encoder. For details, see the H.264 specification."];
      avcProfile: String_.t option
        [@ocaml.doc
          "Indicates to the decoder the requirements for decoding the stream. For definitions of the valid values, see the H.264 specification."];
      codec: String_.t option
        [@ocaml.doc "Codec used for the video encoding."];
      encoder: String_.t option
        [@ocaml.doc "Software or hardware used to encode the video."];
      targetBitrate: Integer.t option
        [@ocaml.doc
          "The expected ingest bitrate (bits per second). This is configured in the encoder."];
      targetFramerate: Integer.t option
        [@ocaml.doc
          "The expected ingest framerate. This is configured in the encoder."];
      videoHeight: Integer.t option
        [@ocaml.doc "Video-resolution height in pixels."];
      videoWidth: Integer.t option
        [@ocaml.doc "Video-resolution width in pixels."]}
    let make ?avcLevel =
      fun ?avcProfile ->
        fun ?codec ->
          fun ?encoder ->
            fun ?targetBitrate ->
              fun ?targetFramerate ->
                fun ?videoHeight ->
                  fun ?videoWidth ->
                    fun () ->
                      {
                        avcLevel;
                        avcProfile;
                        codec;
                        encoder;
                        targetBitrate;
                        targetFramerate;
                        videoHeight;
                        videoWidth
                      }
    let to_value x =
      structure_to_value
        [("avcLevel", (Option.map x.avcLevel ~f:String_.to_value));
        ("avcProfile", (Option.map x.avcProfile ~f:String_.to_value));
        ("codec", (Option.map x.codec ~f:String_.to_value));
        ("encoder", (Option.map x.encoder ~f:String_.to_value));
        ("targetBitrate", (Option.map x.targetBitrate ~f:Integer.to_value));
        ("targetFramerate",
          (Option.map x.targetFramerate ~f:Integer.to_value));
        ("videoHeight", (Option.map x.videoHeight ~f:Integer.to_value));
        ("videoWidth", (Option.map x.videoWidth ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let videoWidth =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "videoWidth") in
      let videoHeight =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "videoHeight") in
      let targetFramerate =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "targetFramerate") in
      let targetBitrate =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "targetBitrate") in
      let encoder =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "encoder") in
      let codec = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "codec") in
      let avcProfile =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "avcProfile") in
      let avcLevel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "avcLevel") in
      make ?videoWidth ?videoHeight ?targetFramerate ?targetBitrate ?encoder
        ?codec ?avcProfile ?avcLevel ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let videoWidth = field_map json "videoWidth" Integer.of_json in
      let videoHeight = field_map json "videoHeight" Integer.of_json in
      let targetFramerate = field_map json "targetFramerate" Integer.of_json in
      let targetBitrate = field_map json "targetBitrate" Integer.of_json in
      let encoder = field_map json "encoder" String_.of_json in
      let codec = field_map json "codec" String_.of_json in
      let avcProfile = field_map json "avcProfile" String_.of_json in
      let avcLevel = field_map json "avcLevel" String_.of_json in
      make ?videoWidth ?videoHeight ?targetFramerate ?targetBitrate ?encoder
        ?codec ?avcProfile ?avcLevel ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Object specifying a stream\226\128\153s video configuration."]
module ThumbnailConfiguration =
  struct
    type nonrec t =
      {
      recordingMode: RecordingMode.t option
        [@ocaml.doc "Thumbnail recording mode. Default: INTERVAL."];
      targetIntervalSeconds: TargetIntervalSeconds.t option
        [@ocaml.doc
          "The targeted thumbnail-generation interval in seconds. This is configurable (and required) only if recordingMode is INTERVAL. Default: 60. Important: Setting a value for targetIntervalSeconds does not guarantee that thumbnails are generated at the specified interval. For thumbnails to be generated at the targetIntervalSeconds interval, the IDR/Keyframe value for the input video must be less than the targetIntervalSeconds value. See Amazon IVS Streaming Configuration for information on setting IDR/Keyframe to the recommended value in video-encoder settings."]}
    let make ?recordingMode =
      fun ?targetIntervalSeconds ->
        fun () -> { recordingMode; targetIntervalSeconds }
    let to_value x =
      structure_to_value
        [("recordingMode",
           (Option.map x.recordingMode ~f:RecordingMode.to_value));
        ("targetIntervalSeconds",
          (Option.map x.targetIntervalSeconds
             ~f:TargetIntervalSeconds.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetIntervalSeconds =
        (Option.map ~f:TargetIntervalSeconds.of_xml)
          (Xml.child xml_arg0 "targetIntervalSeconds") in
      let recordingMode =
        (Option.map ~f:RecordingMode.of_xml)
          (Xml.child xml_arg0 "recordingMode") in
      make ?targetIntervalSeconds ?recordingMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let targetIntervalSeconds =
        field_map json "targetIntervalSeconds" TargetIntervalSeconds.of_json in
      let recordingMode =
        field_map json "recordingMode" RecordingMode.of_json in
      make ?targetIntervalSeconds ?recordingMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing a configuration of thumbnails for recorded video."]
module StreamEvent =
  struct
    type nonrec t =
      {
      eventTime: Time.t option
        [@ocaml.doc
          "UTC ISO-8601 formatted timestamp of when the event occurred."];
      name: String_.t option
        [@ocaml.doc "Name that identifies the stream event within a type."];
      type_: String_.t option
        [@ocaml.doc "Logical group for certain events."]}
    let make ?eventTime =
      fun ?name -> fun ?type_ -> fun () -> { eventTime; name; type_ }
    let to_value x =
      structure_to_value
        [("eventTime", (Option.map x.eventTime ~f:Time.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let eventTime =
        (Option.map ~f:Time.of_xml) (Xml.child xml_arg0 "eventTime") in
      make ?type_ ?name ?eventTime ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let type_ = field_map json "type" String_.of_json in
      let name = field_map json "name" String_.of_json in
      let eventTime = field_map json "eventTime" Time.of_json in
      make ?type_ ?name ?eventTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Object specifying a stream\226\128\153s events. For a list of events, see Using Amazon EventBridge with Amazon IVS."]
module ResourceArn =
  struct
    type nonrec t = string
    let context_ = "ResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws:[is]vs:[a-z0-9-]+:[0-9]+:[a-z-]/[a-zA-Z0-9-]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceArn" j
    let to_json = simple_to_json to_value
  end
module ErrorCode =
  struct
    type nonrec t = string
    let context_ = "errorCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"errorCode" j
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "errorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"errorMessage" j
    let to_json = simple_to_json to_value
  end
module StreamKeyValue =
  struct
    type nonrec t = string
    let context_ = "StreamKeyValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamKeyValue" j
    let to_json = simple_to_json to_value
  end
module StreamSummary =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t option
        [@ocaml.doc "Channel ARN for the stream."];
      health: StreamHealth.t option
        [@ocaml.doc "The stream\226\128\153s health."];
      startTime: StreamStartTime.t option
        [@ocaml.doc
          "Time of the stream\226\128\153s start. This is an ISO 8601 timestamp returned as a string."];
      state: StreamState.t option
        [@ocaml.doc "The stream\226\128\153s state."];
      streamId: StreamId.t option
        [@ocaml.doc
          "Unique identifier for a live or previously live stream in the specified channel."];
      viewerCount: StreamViewerCount.t option
        [@ocaml.doc
          "A count of concurrent views of the stream. Typically, a new view appears in viewerCount within 15 seconds of when video playback starts and a view is removed from viewerCount within 1 minute of when video playback ends. A value of -1 indicates that the request timed out; in this case, retry."]}
    let make ?channelArn =
      fun ?health ->
        fun ?startTime ->
          fun ?state ->
            fun ?streamId ->
              fun ?viewerCount ->
                fun () ->
                  {
                    channelArn;
                    health;
                    startTime;
                    state;
                    streamId;
                    viewerCount
                  }
    let to_value x =
      structure_to_value
        [("channelArn", (Option.map x.channelArn ~f:ChannelArn.to_value));
        ("health", (Option.map x.health ~f:StreamHealth.to_value));
        ("startTime", (Option.map x.startTime ~f:StreamStartTime.to_value));
        ("state", (Option.map x.state ~f:StreamState.to_value));
        ("streamId", (Option.map x.streamId ~f:StreamId.to_value));
        ("viewerCount",
          (Option.map x.viewerCount ~f:StreamViewerCount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let viewerCount =
        (Option.map ~f:StreamViewerCount.of_xml)
          (Xml.child xml_arg0 "viewerCount") in
      let streamId =
        (Option.map ~f:StreamId.of_xml) (Xml.child xml_arg0 "streamId") in
      let state =
        (Option.map ~f:StreamState.of_xml) (Xml.child xml_arg0 "state") in
      let startTime =
        (Option.map ~f:StreamStartTime.of_xml)
          (Xml.child xml_arg0 "startTime") in
      let health =
        (Option.map ~f:StreamHealth.of_xml) (Xml.child xml_arg0 "health") in
      let channelArn =
        (Option.map ~f:ChannelArn.of_xml) (Xml.child xml_arg0 "channelArn") in
      make ?viewerCount ?streamId ?state ?startTime ?health ?channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let viewerCount =
        field_map json "viewerCount" StreamViewerCount.of_json in
      let streamId = field_map json "streamId" StreamId.of_json in
      let state = field_map json "state" StreamState.of_json in
      let startTime = field_map json "startTime" StreamStartTime.of_json in
      let health = field_map json "health" StreamHealth.of_json in
      let channelArn = field_map json "channelArn" ChannelArn.of_json in
      make ?viewerCount ?streamId ?state ?startTime ?health ?channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about a stream."]
module StreamSessionSummary =
  struct
    type nonrec t =
      {
      endTime: Time.t option
        [@ocaml.doc
          "UTC ISO-8601 formatted timestamp of when the channel went offline. For live streams, this is NULL."];
      hasErrorEvent: Boolean.t option
        [@ocaml.doc
          "If true, this stream encountered a quota breach or failure."];
      startTime: Time.t option
        [@ocaml.doc
          "UTC ISO-8601 formatted timestamp of when the channel went live."];
      streamId: StreamId.t option
        [@ocaml.doc
          "Unique identifier for a live or previously live stream in the specified channel."]}
    let make ?endTime =
      fun ?hasErrorEvent ->
        fun ?startTime ->
          fun ?streamId ->
            fun () -> { endTime; hasErrorEvent; startTime; streamId }
    let to_value x =
      structure_to_value
        [("endTime", (Option.map x.endTime ~f:Time.to_value));
        ("hasErrorEvent", (Option.map x.hasErrorEvent ~f:Boolean.to_value));
        ("startTime", (Option.map x.startTime ~f:Time.to_value));
        ("streamId", (Option.map x.streamId ~f:StreamId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamId =
        (Option.map ~f:StreamId.of_xml) (Xml.child xml_arg0 "streamId") in
      let startTime =
        (Option.map ~f:Time.of_xml) (Xml.child xml_arg0 "startTime") in
      let hasErrorEvent =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "hasErrorEvent") in
      let endTime =
        (Option.map ~f:Time.of_xml) (Xml.child xml_arg0 "endTime") in
      make ?streamId ?startTime ?hasErrorEvent ?endTime ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamId = field_map json "streamId" StreamId.of_json in
      let startTime = field_map json "startTime" Time.of_json in
      let hasErrorEvent = field_map json "hasErrorEvent" Boolean.of_json in
      let endTime = field_map json "endTime" Time.of_json in
      make ?streamId ?startTime ?hasErrorEvent ?endTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about a stream session."]
module StreamKeySummary =
  struct
    type nonrec t =
      {
      arn: StreamKeyArn.t option [@ocaml.doc "Stream-key ARN."];
      channelArn: ChannelArn.t option
        [@ocaml.doc "Channel ARN for the stream."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."]}
    let make ?arn =
      fun ?channelArn -> fun ?tags -> fun () -> { arn; channelArn; tags }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:StreamKeyArn.to_value));
        ("channelArn", (Option.map x.channelArn ~f:ChannelArn.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let channelArn =
        (Option.map ~f:ChannelArn.of_xml) (Xml.child xml_arg0 "channelArn") in
      let arn =
        (Option.map ~f:StreamKeyArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?channelArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let channelArn = field_map json "channelArn" ChannelArn.of_json in
      let arn = field_map json "arn" StreamKeyArn.of_json in
      make ?tags ?channelArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about a stream key."]
module RecordingConfigurationSummary =
  struct
    type nonrec t =
      {
      arn: RecordingConfigurationArn.t
        [@ocaml.doc "Recording-configuration ARN."];
      destinationConfiguration: DestinationConfiguration.t
        [@ocaml.doc
          "A complex type that contains information about where recorded video will be stored."];
      name: RecordingConfigurationName.t option
        [@ocaml.doc
          "Recording-configuration name. The value does not need to be unique."];
      state: RecordingConfigurationState.t
        [@ocaml.doc
          "Indicates the current state of the recording configuration. When the state is ACTIVE, the configuration is ready for recording a channel stream."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."]}
    let context_ = "RecordingConfigurationSummary"
    let make ?name =
      fun ?tags ->
        fun ~arn ->
          fun ~destinationConfiguration ->
            fun ~state ->
              fun () -> { name; tags; arn; destinationConfiguration; state }
    let to_value x =
      structure_to_value
        [("arn", (Some (RecordingConfigurationArn.to_value x.arn)));
        ("destinationConfiguration",
          (Some
             (DestinationConfiguration.to_value x.destinationConfiguration)));
        ("name", (Option.map x.name ~f:RecordingConfigurationName.to_value));
        ("state", (Some (RecordingConfigurationState.to_value x.state)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let state =
        RecordingConfigurationState.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "state") in
      let name =
        (Option.map ~f:RecordingConfigurationName.of_xml)
          (Xml.child xml_arg0 "name") in
      let destinationConfiguration =
        DestinationConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "destinationConfiguration") in
      let arn =
        RecordingConfigurationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?tags ~state ?name ~destinationConfiguration ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let state =
        field_map_exn json "state" RecordingConfigurationState.of_json in
      let name = field_map json "name" RecordingConfigurationName.of_json in
      let destinationConfiguration =
        field_map_exn json "destinationConfiguration"
          DestinationConfiguration.of_json in
      let arn = field_map_exn json "arn" RecordingConfigurationArn.of_json in
      make ?tags ~state ?name ~destinationConfiguration ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about a RecordingConfiguration."]
module PlaybackKeyPairSummary =
  struct
    type nonrec t =
      {
      arn: PlaybackKeyPairArn.t option [@ocaml.doc "Key-pair ARN."];
      name: PlaybackKeyPairName.t option
        [@ocaml.doc
          "Playback-key-pair name. The value does not need to be unique."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."]}
    let make ?arn = fun ?name -> fun ?tags -> fun () -> { arn; name; tags }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:PlaybackKeyPairArn.to_value));
        ("name", (Option.map x.name ~f:PlaybackKeyPairName.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let name =
        (Option.map ~f:PlaybackKeyPairName.of_xml)
          (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:PlaybackKeyPairArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?name ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let name = field_map json "name" PlaybackKeyPairName.of_json in
      let arn = field_map json "arn" PlaybackKeyPairArn.of_json in
      make ?tags ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about a playback key pair."]
module ChannelSummary =
  struct
    type nonrec t =
      {
      arn: ChannelArn.t option [@ocaml.doc "Channel ARN."];
      authorized: IsAuthorized.t option
        [@ocaml.doc
          "Whether the channel is private (enabled for playback authorization). Default: false."];
      latencyMode: ChannelLatencyMode.t option
        [@ocaml.doc
          "Channel latency mode. Use NORMAL to broadcast and deliver live video up to Full HD. Use LOW for near-real-time interaction with viewers. Default: LOW. (Note: In the Amazon IVS console, LOW and NORMAL correspond to Ultra-low and Standard, respectively.)"];
      name: ChannelName.t option [@ocaml.doc "Channel name."];
      recordingConfigurationArn: ChannelRecordingConfigurationArn.t option
        [@ocaml.doc
          "Recording-configuration ARN. A value other than an empty string indicates that recording is enabled. Default: \"\" (empty string, recording is disabled)."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."]}
    let make ?arn =
      fun ?authorized ->
        fun ?latencyMode ->
          fun ?name ->
            fun ?recordingConfigurationArn ->
              fun ?tags ->
                fun () ->
                  {
                    arn;
                    authorized;
                    latencyMode;
                    name;
                    recordingConfigurationArn;
                    tags
                  }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:ChannelArn.to_value));
        ("authorized", (Option.map x.authorized ~f:IsAuthorized.to_value));
        ("latencyMode",
          (Option.map x.latencyMode ~f:ChannelLatencyMode.to_value));
        ("name", (Option.map x.name ~f:ChannelName.to_value));
        ("recordingConfigurationArn",
          (Option.map x.recordingConfigurationArn
             ~f:ChannelRecordingConfigurationArn.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let recordingConfigurationArn =
        (Option.map ~f:ChannelRecordingConfigurationArn.of_xml)
          (Xml.child xml_arg0 "recordingConfigurationArn") in
      let name =
        (Option.map ~f:ChannelName.of_xml) (Xml.child xml_arg0 "name") in
      let latencyMode =
        (Option.map ~f:ChannelLatencyMode.of_xml)
          (Xml.child xml_arg0 "latencyMode") in
      let authorized =
        (Option.map ~f:IsAuthorized.of_xml) (Xml.child xml_arg0 "authorized") in
      let arn = (Option.map ~f:ChannelArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?recordingConfigurationArn ?name ?latencyMode ?authorized
        ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let recordingConfigurationArn =
        field_map json "recordingConfigurationArn"
          ChannelRecordingConfigurationArn.of_json in
      let name = field_map json "name" ChannelName.of_json in
      let latencyMode =
        field_map json "latencyMode" ChannelLatencyMode.of_json in
      let authorized = field_map json "authorized" IsAuthorized.of_json in
      let arn = field_map json "arn" ChannelArn.of_json in
      make ?tags ?recordingConfigurationArn ?name ?latencyMode ?authorized
        ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about a channel."]
module PlaybackKeyPairFingerprint =
  struct
    type nonrec t = string
    let context_ = "PlaybackKeyPairFingerprint"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlaybackKeyPairFingerprint" j
    let to_json = simple_to_json to_value
  end
module Channel =
  struct
    type nonrec t =
      {
      arn: ChannelArn.t option [@ocaml.doc "Channel ARN."];
      authorized: IsAuthorized.t option
        [@ocaml.doc
          "Whether the channel is private (enabled for playback authorization). Default: false."];
      ingestEndpoint: IngestEndpoint.t option
        [@ocaml.doc
          "Channel ingest endpoint, part of the definition of an ingest server, used when you set up streaming software."];
      latencyMode: ChannelLatencyMode.t option
        [@ocaml.doc
          "Channel latency mode. Use NORMAL to broadcast and deliver live video up to Full HD. Use LOW for near-real-time interaction with viewers. Default: LOW. (Note: In the Amazon IVS console, LOW and NORMAL correspond to Ultra-low and Standard, respectively.)"];
      name: ChannelName.t option [@ocaml.doc "Channel name."];
      playbackUrl: PlaybackURL.t option [@ocaml.doc "Channel playback URL."];
      recordingConfigurationArn: ChannelRecordingConfigurationArn.t option
        [@ocaml.doc
          "Recording-configuration ARN. A value other than an empty string indicates that recording is enabled. Default: \"\" (empty string, recording is disabled)."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."];
      type_: ChannelType.t option
        [@ocaml.doc
          "Channel type, which determines the allowable resolution and bitrate. If you exceed the allowable resolution or bitrate, the stream probably will disconnect immediately. Default: STANDARD. Valid values: STANDARD: Multiple qualities are generated from the original input, to automatically give viewers the best experience for their devices and network conditions. Resolution can be up to 1080p and bitrate can be up to 8.5 Mbps. Audio is transcoded only for renditions 360p and below; above that, audio is passed through. BASIC: Amazon IVS delivers the original input to viewers. The viewer\226\128\153s video-quality choice is limited to the original input. Resolution can be up to 480p and bitrate can be up to 1.5 Mbps."]}
    let make ?arn =
      fun ?authorized ->
        fun ?ingestEndpoint ->
          fun ?latencyMode ->
            fun ?name ->
              fun ?playbackUrl ->
                fun ?recordingConfigurationArn ->
                  fun ?tags ->
                    fun ?type_ ->
                      fun () ->
                        {
                          arn;
                          authorized;
                          ingestEndpoint;
                          latencyMode;
                          name;
                          playbackUrl;
                          recordingConfigurationArn;
                          tags;
                          type_
                        }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:ChannelArn.to_value));
        ("authorized", (Option.map x.authorized ~f:IsAuthorized.to_value));
        ("ingestEndpoint",
          (Option.map x.ingestEndpoint ~f:IngestEndpoint.to_value));
        ("latencyMode",
          (Option.map x.latencyMode ~f:ChannelLatencyMode.to_value));
        ("name", (Option.map x.name ~f:ChannelName.to_value));
        ("playbackUrl", (Option.map x.playbackUrl ~f:PlaybackURL.to_value));
        ("recordingConfigurationArn",
          (Option.map x.recordingConfigurationArn
             ~f:ChannelRecordingConfigurationArn.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("type", (Option.map x.type_ ~f:ChannelType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:ChannelType.of_xml) (Xml.child xml_arg0 "type") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let recordingConfigurationArn =
        (Option.map ~f:ChannelRecordingConfigurationArn.of_xml)
          (Xml.child xml_arg0 "recordingConfigurationArn") in
      let playbackUrl =
        (Option.map ~f:PlaybackURL.of_xml) (Xml.child xml_arg0 "playbackUrl") in
      let name =
        (Option.map ~f:ChannelName.of_xml) (Xml.child xml_arg0 "name") in
      let latencyMode =
        (Option.map ~f:ChannelLatencyMode.of_xml)
          (Xml.child xml_arg0 "latencyMode") in
      let ingestEndpoint =
        (Option.map ~f:IngestEndpoint.of_xml)
          (Xml.child xml_arg0 "ingestEndpoint") in
      let authorized =
        (Option.map ~f:IsAuthorized.of_xml) (Xml.child xml_arg0 "authorized") in
      let arn = (Option.map ~f:ChannelArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?type_ ?tags ?recordingConfigurationArn ?playbackUrl ?name
        ?latencyMode ?ingestEndpoint ?authorized ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let type_ = field_map json "type" ChannelType.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let recordingConfigurationArn =
        field_map json "recordingConfigurationArn"
          ChannelRecordingConfigurationArn.of_json in
      let playbackUrl = field_map json "playbackUrl" PlaybackURL.of_json in
      let name = field_map json "name" ChannelName.of_json in
      let latencyMode =
        field_map json "latencyMode" ChannelLatencyMode.of_json in
      let ingestEndpoint =
        field_map json "ingestEndpoint" IngestEndpoint.of_json in
      let authorized = field_map json "authorized" IsAuthorized.of_json in
      let arn = field_map json "arn" ChannelArn.of_json in
      make ?type_ ?tags ?recordingConfigurationArn ?playbackUrl ?name
        ?latencyMode ?ingestEndpoint ?authorized ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Object specifying a channel."]
module IngestConfiguration =
  struct
    type nonrec t =
      {
      audio: AudioConfiguration.t option
        [@ocaml.doc "Encoder settings for audio."];
      video: VideoConfiguration.t option
        [@ocaml.doc "Encoder settings for video."]}
    let make ?audio = fun ?video -> fun () -> { audio; video }
    let to_value x =
      structure_to_value
        [("audio", (Option.map x.audio ~f:AudioConfiguration.to_value));
        ("video", (Option.map x.video ~f:VideoConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let video =
        (Option.map ~f:VideoConfiguration.of_xml)
          (Xml.child xml_arg0 "video") in
      let audio =
        (Option.map ~f:AudioConfiguration.of_xml)
          (Xml.child xml_arg0 "audio") in
      make ?video ?audio ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let video = field_map json "video" VideoConfiguration.of_json in
      let audio = field_map json "audio" AudioConfiguration.of_json in
      make ?video ?audio ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Object specifying the ingest configuration set up by the broadcaster, usually in an encoder."]
module RecordingConfiguration =
  struct
    type nonrec t =
      {
      arn: RecordingConfigurationArn.t
        [@ocaml.doc "Recording-configuration ARN."];
      destinationConfiguration: DestinationConfiguration.t
        [@ocaml.doc
          "A complex type that contains information about where recorded video will be stored."];
      name: RecordingConfigurationName.t option
        [@ocaml.doc
          "Recording-configuration name. The value does not need to be unique."];
      state: RecordingConfigurationState.t
        [@ocaml.doc
          "Indicates the current state of the recording configuration. When the state is ACTIVE, the configuration is ready for recording a channel stream."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."];
      thumbnailConfiguration: ThumbnailConfiguration.t option
        [@ocaml.doc
          "A complex type that allows you to enable/disable the recording of thumbnails for a live session and modify the interval at which thumbnails are generated for the live session."]}
    let context_ = "RecordingConfiguration"
    let make ?name =
      fun ?tags ->
        fun ?thumbnailConfiguration ->
          fun ~arn ->
            fun ~destinationConfiguration ->
              fun ~state ->
                fun () ->
                  {
                    name;
                    tags;
                    thumbnailConfiguration;
                    arn;
                    destinationConfiguration;
                    state
                  }
    let to_value x =
      structure_to_value
        [("arn", (Some (RecordingConfigurationArn.to_value x.arn)));
        ("destinationConfiguration",
          (Some
             (DestinationConfiguration.to_value x.destinationConfiguration)));
        ("name", (Option.map x.name ~f:RecordingConfigurationName.to_value));
        ("state", (Some (RecordingConfigurationState.to_value x.state)));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("thumbnailConfiguration",
          (Option.map x.thumbnailConfiguration
             ~f:ThumbnailConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let thumbnailConfiguration =
        (Option.map ~f:ThumbnailConfiguration.of_xml)
          (Xml.child xml_arg0 "thumbnailConfiguration") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let state =
        RecordingConfigurationState.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "state") in
      let name =
        (Option.map ~f:RecordingConfigurationName.of_xml)
          (Xml.child xml_arg0 "name") in
      let destinationConfiguration =
        DestinationConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "destinationConfiguration") in
      let arn =
        RecordingConfigurationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?thumbnailConfiguration ?tags ~state ?name
        ~destinationConfiguration ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let thumbnailConfiguration =
        field_map json "thumbnailConfiguration"
          ThumbnailConfiguration.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let state =
        field_map_exn json "state" RecordingConfigurationState.of_json in
      let name = field_map json "name" RecordingConfigurationName.of_json in
      let destinationConfiguration =
        field_map_exn json "destinationConfiguration"
          DestinationConfiguration.of_json in
      let arn = field_map_exn json "arn" RecordingConfigurationArn.of_json in
      make ?thumbnailConfiguration ?tags ~state ?name
        ~destinationConfiguration ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object representing a configuration to record a channel stream."]
module StreamEvents =
  struct
    type nonrec t = StreamEvent.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:500) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:StreamEvent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StreamEvent.of_xml)
    let of_json j =
      list_of_json ~kind:"StreamEvents" ~of_json:StreamEvent.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchError =
  struct
    type nonrec t =
      {
      arn: ResourceArn.t option [@ocaml.doc "Channel ARN."];
      code: ErrorCode.t option [@ocaml.doc "Error code."];
      message: ErrorMessage.t option
        [@ocaml.doc "Error message, determined by the application."]}
    let make ?arn =
      fun ?code -> fun ?message -> fun () -> { arn; code; message }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("code", (Option.map x.code ~f:ErrorCode.to_value));
        ("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "code") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?message ?code ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      let code = field_map json "code" ErrorCode.of_json in
      let arn = field_map json "arn" ResourceArn.of_json in
      make ?message ?code ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Error related to a specific channel, specified by its ARN."]
module StreamKey =
  struct
    type nonrec t =
      {
      arn: StreamKeyArn.t option [@ocaml.doc "Stream-key ARN."];
      channelArn: ChannelArn.t option
        [@ocaml.doc "Channel ARN for the stream."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."];
      value: StreamKeyValue.t option [@ocaml.doc "Stream-key value."]}
    let make ?arn =
      fun ?channelArn ->
        fun ?tags -> fun ?value -> fun () -> { arn; channelArn; tags; value }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:StreamKeyArn.to_value));
        ("channelArn", (Option.map x.channelArn ~f:ChannelArn.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("value", (Option.map x.value ~f:StreamKeyValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:StreamKeyValue.of_xml) (Xml.child xml_arg0 "value") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let channelArn =
        (Option.map ~f:ChannelArn.of_xml) (Xml.child xml_arg0 "channelArn") in
      let arn =
        (Option.map ~f:StreamKeyArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?value ?tags ?channelArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map json "value" StreamKeyValue.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let channelArn = field_map json "channelArn" ChannelArn.of_json in
      let arn = field_map json "arn" StreamKeyArn.of_json in
      make ?value ?tags ?channelArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Object specifying a stream key."]
module AccessDeniedException =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc
          "User does not have sufficient access to perform this action."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module ConflictException =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc
          "Updating or deleting a resource can cause an inconsistent state."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module PendingVerification =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc "Your account is pending verification."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc "Request references a resource which does not exist."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module ValidationException =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc
          "The input fails to satisfy the constraints specified by an Amazon Web Services service."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module InternalServerException =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc "Unexpected error during processing of request."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChannelNotBroadcasting =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc "The stream is offline for the given channel ARN."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module StreamUnavailable =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc "The stream is temporarily unavailable."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module StreamMetadata =
  struct
    type nonrec t = string
    let context_ = "StreamMetadata"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StreamMetadata" j
    let to_json = simple_to_json to_value
  end
module PaginationToken =
  struct
    type nonrec t = string
    let context_ = "PaginationToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaginationToken" j
    let to_json = simple_to_json to_value
  end
module StreamList =
  struct
    type nonrec t = StreamSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StreamSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StreamSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"StreamList" ~of_json:StreamSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxStreamResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxStreamResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module StreamFilters =
  struct
    type nonrec t =
      {
      health: StreamHealth.t option
        [@ocaml.doc "The stream\226\128\153s health."]}
    let make ?health = fun () -> { health }
    let to_value x =
      structure_to_value
        [("health", (Option.map x.health ~f:StreamHealth.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let health =
        (Option.map ~f:StreamHealth.of_xml) (Xml.child xml_arg0 "health") in
      make ?health ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let health = field_map json "health" StreamHealth.of_json in
      make ?health ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Object specifying the stream attribute on which to filter."]
module StreamSessionList =
  struct
    type nonrec t = StreamSessionSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StreamSessionSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StreamSessionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"StreamSessionList"
        ~of_json:StreamSessionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module StreamKeyList =
  struct
    type nonrec t = StreamKeySummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StreamKeySummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StreamKeySummary.of_xml)
    let of_json j =
      list_of_json ~kind:"StreamKeyList" ~of_json:StreamKeySummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxStreamKeyResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxStreamKeyResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module RecordingConfigurationList =
  struct
    type nonrec t = RecordingConfigurationSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RecordingConfigurationSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecordingConfigurationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"RecordingConfigurationList"
        ~of_json:RecordingConfigurationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxRecordingConfigurationResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for MaxRecordingConfigurationResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PlaybackKeyPairList =
  struct
    type nonrec t = PlaybackKeyPairSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PlaybackKeyPairSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlaybackKeyPairSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"PlaybackKeyPairList"
        ~of_json:PlaybackKeyPairSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxPlaybackKeyPairResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxPlaybackKeyPairResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ChannelList =
  struct
    type nonrec t = ChannelSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ChannelSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChannelSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ChannelList" ~of_json:ChannelSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxChannelResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxChannelResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PlaybackKeyPair =
  struct
    type nonrec t =
      {
      arn: PlaybackKeyPairArn.t option [@ocaml.doc "Key-pair ARN."];
      fingerprint: PlaybackKeyPairFingerprint.t option
        [@ocaml.doc "Key-pair identifier."];
      name: PlaybackKeyPairName.t option
        [@ocaml.doc
          "Playback-key-pair name. The value does not need to be unique."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."]}
    let make ?arn =
      fun ?fingerprint ->
        fun ?name -> fun ?tags -> fun () -> { arn; fingerprint; name; tags }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:PlaybackKeyPairArn.to_value));
        ("fingerprint",
          (Option.map x.fingerprint ~f:PlaybackKeyPairFingerprint.to_value));
        ("name", (Option.map x.name ~f:PlaybackKeyPairName.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let name =
        (Option.map ~f:PlaybackKeyPairName.of_xml)
          (Xml.child xml_arg0 "name") in
      let fingerprint =
        (Option.map ~f:PlaybackKeyPairFingerprint.of_xml)
          (Xml.child xml_arg0 "fingerprint") in
      let arn =
        (Option.map ~f:PlaybackKeyPairArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?tags ?name ?fingerprint ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let name = field_map json "name" PlaybackKeyPairName.of_json in
      let fingerprint =
        field_map json "fingerprint" PlaybackKeyPairFingerprint.of_json in
      let arn = field_map json "arn" PlaybackKeyPairArn.of_json in
      make ?tags ?name ?fingerprint ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A key pair used to sign and validate a playback authorization token."]
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc "Request would cause a service quota to be exceeded."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module PlaybackPublicKeyMaterial =
  struct
    type nonrec t = string
    let context_ = "PlaybackPublicKeyMaterial"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PlaybackPublicKeyMaterial" j
    let to_json = simple_to_json to_value
  end
module StreamSession =
  struct
    type nonrec t =
      {
      channel: Channel.t option
        [@ocaml.doc
          "The properties of the channel at the time of going live."];
      endTime: Time.t option
        [@ocaml.doc
          "UTC ISO-8601 formatted timestamp of when the channel went offline. For live streams, this is NULL."];
      ingestConfiguration: IngestConfiguration.t option
        [@ocaml.doc
          "The properties of the incoming RTMP stream for the stream."];
      recordingConfiguration: RecordingConfiguration.t option
        [@ocaml.doc "The properties of recording the live stream."];
      startTime: Time.t option
        [@ocaml.doc
          "UTC ISO-8601 formatted timestamp of when the channel went live."];
      streamId: StreamId.t option
        [@ocaml.doc
          "Unique identifier for a live or previously live stream in the specified channel."];
      truncatedEvents: StreamEvents.t option
        [@ocaml.doc
          "List of Amazon IVS events that the stream encountered. The list is sorted by most recent events and contains up to 500 events. For Amazon IVS events, see Using Amazon EventBridge with Amazon IVS."]}
    let make ?channel =
      fun ?endTime ->
        fun ?ingestConfiguration ->
          fun ?recordingConfiguration ->
            fun ?startTime ->
              fun ?streamId ->
                fun ?truncatedEvents ->
                  fun () ->
                    {
                      channel;
                      endTime;
                      ingestConfiguration;
                      recordingConfiguration;
                      startTime;
                      streamId;
                      truncatedEvents
                    }
    let to_value x =
      structure_to_value
        [("channel", (Option.map x.channel ~f:Channel.to_value));
        ("endTime", (Option.map x.endTime ~f:Time.to_value));
        ("ingestConfiguration",
          (Option.map x.ingestConfiguration ~f:IngestConfiguration.to_value));
        ("recordingConfiguration",
          (Option.map x.recordingConfiguration
             ~f:RecordingConfiguration.to_value));
        ("startTime", (Option.map x.startTime ~f:Time.to_value));
        ("streamId", (Option.map x.streamId ~f:StreamId.to_value));
        ("truncatedEvents",
          (Option.map x.truncatedEvents ~f:StreamEvents.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let truncatedEvents =
        (Option.map ~f:StreamEvents.of_xml)
          (Xml.child xml_arg0 "truncatedEvents") in
      let streamId =
        (Option.map ~f:StreamId.of_xml) (Xml.child xml_arg0 "streamId") in
      let startTime =
        (Option.map ~f:Time.of_xml) (Xml.child xml_arg0 "startTime") in
      let recordingConfiguration =
        (Option.map ~f:RecordingConfiguration.of_xml)
          (Xml.child xml_arg0 "recordingConfiguration") in
      let ingestConfiguration =
        (Option.map ~f:IngestConfiguration.of_xml)
          (Xml.child xml_arg0 "ingestConfiguration") in
      let endTime =
        (Option.map ~f:Time.of_xml) (Xml.child xml_arg0 "endTime") in
      let channel =
        (Option.map ~f:Channel.of_xml) (Xml.child xml_arg0 "channel") in
      make ?truncatedEvents ?streamId ?startTime ?recordingConfiguration
        ?ingestConfiguration ?endTime ?channel ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let truncatedEvents =
        field_map json "truncatedEvents" StreamEvents.of_json in
      let streamId = field_map json "streamId" StreamId.of_json in
      let startTime = field_map json "startTime" Time.of_json in
      let recordingConfiguration =
        field_map json "recordingConfiguration"
          RecordingConfiguration.of_json in
      let ingestConfiguration =
        field_map json "ingestConfiguration" IngestConfiguration.of_json in
      let endTime = field_map json "endTime" Time.of_json in
      let channel = field_map json "channel" Channel.of_json in
      make ?truncatedEvents ?streamId ?startTime ?recordingConfiguration
        ?ingestConfiguration ?endTime ?channel ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Object that captures the Amazon IVS configuration that the customer provisioned, the ingest configurations that the broadcaster used, and the most recent Amazon IVS stream events it encountered."]
module Stream =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t option
        [@ocaml.doc "Channel ARN for the stream."];
      health: StreamHealth.t option
        [@ocaml.doc "The stream\226\128\153s health."];
      playbackUrl: PlaybackURL.t option
        [@ocaml.doc
          "URL of the master playlist, required by the video player to play the HLS stream."];
      startTime: StreamStartTime.t option
        [@ocaml.doc
          "Time of the stream\226\128\153s start. This is an ISO 8601 timestamp returned as a string."];
      state: StreamState.t option
        [@ocaml.doc "The stream\226\128\153s state."];
      streamId: StreamId.t option
        [@ocaml.doc
          "Unique identifier for a live or previously live stream in the specified channel."];
      viewerCount: StreamViewerCount.t option
        [@ocaml.doc
          "A count of concurrent views of the stream. Typically, a new view appears in viewerCount within 15 seconds of when video playback starts and a view is removed from viewerCount within 1 minute of when video playback ends. A value of -1 indicates that the request timed out; in this case, retry."]}
    let make ?channelArn =
      fun ?health ->
        fun ?playbackUrl ->
          fun ?startTime ->
            fun ?state ->
              fun ?streamId ->
                fun ?viewerCount ->
                  fun () ->
                    {
                      channelArn;
                      health;
                      playbackUrl;
                      startTime;
                      state;
                      streamId;
                      viewerCount
                    }
    let to_value x =
      structure_to_value
        [("channelArn", (Option.map x.channelArn ~f:ChannelArn.to_value));
        ("health", (Option.map x.health ~f:StreamHealth.to_value));
        ("playbackUrl", (Option.map x.playbackUrl ~f:PlaybackURL.to_value));
        ("startTime", (Option.map x.startTime ~f:StreamStartTime.to_value));
        ("state", (Option.map x.state ~f:StreamState.to_value));
        ("streamId", (Option.map x.streamId ~f:StreamId.to_value));
        ("viewerCount",
          (Option.map x.viewerCount ~f:StreamViewerCount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let viewerCount =
        (Option.map ~f:StreamViewerCount.of_xml)
          (Xml.child xml_arg0 "viewerCount") in
      let streamId =
        (Option.map ~f:StreamId.of_xml) (Xml.child xml_arg0 "streamId") in
      let state =
        (Option.map ~f:StreamState.of_xml) (Xml.child xml_arg0 "state") in
      let startTime =
        (Option.map ~f:StreamStartTime.of_xml)
          (Xml.child xml_arg0 "startTime") in
      let playbackUrl =
        (Option.map ~f:PlaybackURL.of_xml) (Xml.child xml_arg0 "playbackUrl") in
      let health =
        (Option.map ~f:StreamHealth.of_xml) (Xml.child xml_arg0 "health") in
      let channelArn =
        (Option.map ~f:ChannelArn.of_xml) (Xml.child xml_arg0 "channelArn") in
      make ?viewerCount ?streamId ?state ?startTime ?playbackUrl ?health
        ?channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let viewerCount =
        field_map json "viewerCount" StreamViewerCount.of_json in
      let streamId = field_map json "streamId" StreamId.of_json in
      let state = field_map json "state" StreamState.of_json in
      let startTime = field_map json "startTime" StreamStartTime.of_json in
      let playbackUrl = field_map json "playbackUrl" PlaybackURL.of_json in
      let health = field_map json "health" StreamHealth.of_json in
      let channelArn = field_map json "channelArn" ChannelArn.of_json in
      make ?viewerCount ?streamId ?state ?startTime ?playbackUrl ?health
        ?channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a live video stream that has been ingested and distributed."]
module BatchErrors =
  struct
    type nonrec t = BatchError.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BatchError.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchError.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchErrors" ~of_json:BatchError.of_json j
    let to_json v = composed_to_json to_value v
  end
module StreamKeys =
  struct
    type nonrec t = StreamKey.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StreamKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StreamKey.of_xml)
    let of_json j =
      list_of_json ~kind:"StreamKeys" ~of_json:StreamKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module StreamKeyArnList =
  struct
    type nonrec t = StreamKeyArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:StreamKeyArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StreamKeyArn.of_xml)
    let of_json j =
      list_of_json ~kind:"StreamKeyArnList" ~of_json:StreamKeyArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module Channels =
  struct
    type nonrec t = Channel.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Channel.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Channel.of_xml)
    let of_json j = list_of_json ~kind:"Channels" ~of_json:Channel.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChannelArnList =
  struct
    type nonrec t = ChannelArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:ChannelArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChannelArn.of_xml)
    let of_json j =
      list_of_json ~kind:"ChannelArnList" ~of_json:ChannelArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateChannelResponse =
  struct
    type nonrec t = {
      channel: Channel.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `PendingVerification of PendingVerification.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channel = fun () -> { channel }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `PendingVerification e ->
          `Assoc
            [("error", (`String "PendingVerification"));
            ("details", (PendingVerification.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("channel", (Option.map x.channel ~f:Channel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channel =
        (Option.map ~f:Channel.of_xml) (Xml.child xml_arg0 "channel") in
      make ?channel ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let channel = field_map json "channel" Channel.of_json in
      make ?channel ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a channel's configuration. This does not affect an ongoing stream of this channel. You must stop and restart the stream for the changes to take effect."]
module UpdateChannelRequest =
  struct
    type nonrec t =
      {
      arn: ChannelArn.t [@ocaml.doc "ARN of the channel to be updated."];
      authorized: Boolean.t option
        [@ocaml.doc
          "Whether the channel is private (enabled for playback authorization)."];
      latencyMode: ChannelLatencyMode.t option
        [@ocaml.doc
          "Channel latency mode. Use NORMAL to broadcast and deliver live video up to Full HD. Use LOW for near-real-time interaction with viewers. (Note: In the Amazon IVS console, LOW and NORMAL correspond to Ultra-low and Standard, respectively.)"];
      name: ChannelName.t option [@ocaml.doc "Channel name."];
      recordingConfigurationArn: ChannelRecordingConfigurationArn.t option
        [@ocaml.doc
          "Recording-configuration ARN. If this is set to an empty string, recording is disabled. A value other than an empty string indicates that recording is enabled"];
      type_: ChannelType.t option
        [@ocaml.doc
          "Channel type, which determines the allowable resolution and bitrate. If you exceed the allowable resolution or bitrate, the stream probably will disconnect immediately. Valid values: STANDARD: Multiple qualities are generated from the original input, to automatically give viewers the best experience for their devices and network conditions. Resolution can be up to 1080p and bitrate can be up to 8.5 Mbps. Audio is transcoded only for renditions 360p and below; above that, audio is passed through. BASIC: Amazon IVS delivers the original input to viewers. The viewer\226\128\153s video-quality choice is limited to the original input. Resolution can be up to 480p and bitrate can be up to 1.5 Mbps."]}
    let context_ = "UpdateChannelRequest"
    let make ?authorized =
      fun ?latencyMode ->
        fun ?name ->
          fun ?recordingConfigurationArn ->
            fun ?type_ ->
              fun ~arn ->
                fun () ->
                  {
                    authorized;
                    latencyMode;
                    name;
                    recordingConfigurationArn;
                    type_;
                    arn
                  }
    let to_value x =
      structure_to_value
        [("arn", (Some (ChannelArn.to_value x.arn)));
        ("authorized", (Option.map x.authorized ~f:Boolean.to_value));
        ("latencyMode",
          (Option.map x.latencyMode ~f:ChannelLatencyMode.to_value));
        ("name", (Option.map x.name ~f:ChannelName.to_value));
        ("recordingConfigurationArn",
          (Option.map x.recordingConfigurationArn
             ~f:ChannelRecordingConfigurationArn.to_value));
        ("type", (Option.map x.type_ ~f:ChannelType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:ChannelType.of_xml) (Xml.child xml_arg0 "type") in
      let recordingConfigurationArn =
        (Option.map ~f:ChannelRecordingConfigurationArn.of_xml)
          (Xml.child xml_arg0 "recordingConfigurationArn") in
      let name =
        (Option.map ~f:ChannelName.of_xml) (Xml.child xml_arg0 "name") in
      let latencyMode =
        (Option.map ~f:ChannelLatencyMode.of_xml)
          (Xml.child xml_arg0 "latencyMode") in
      let authorized =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "authorized") in
      let arn =
        ChannelArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?type_ ?recordingConfigurationArn ?name ?latencyMode ?authorized
        ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let type_ = field_map json "type" ChannelType.of_json in
      let recordingConfigurationArn =
        field_map json "recordingConfigurationArn"
          ChannelRecordingConfigurationArn.of_json in
      let name = field_map json "name" ChannelName.of_json in
      let latencyMode =
        field_map json "latencyMode" ChannelLatencyMode.of_json in
      let authorized = field_map json "authorized" Boolean.of_json in
      let arn = field_map_exn json "arn" ChannelArn.of_json in
      make ?type_ ?recordingConfigurationArn ?name ?latencyMode ?authorized
        ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a channel's configuration. This does not affect an ongoing stream of this channel. You must stop and restart the stream for the changes to take effect."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes tags from the resource with the specified ARN."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "ARN of the resource for which tags are to be removed."];
      tagKeys: TagKeyList.t [@ocaml.doc "Array of tags to be removed."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tagKeys = field_map_exn json "tagKeys" TagKeyList.of_json in
      let resourceArn = field_map_exn json "resourceArn" ResourceArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes tags from the resource with the specified ARN."]
module ThrottlingException =
  struct
    type nonrec t =
      {
      exceptionMessage: ErrorMessage.t option
        [@ocaml.doc "Request was denied due to request throttling."]}
    let make ?exceptionMessage = fun () -> { exceptionMessage }
    let to_value x =
      structure_to_value
        [("exceptionMessage",
           (Option.map x.exceptionMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let exceptionMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "exceptionMessage") in
      make ?exceptionMessage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let exceptionMessage =
        field_map json "exceptionMessage" ErrorMessage.of_json in
      make ?exceptionMessage ()
    let to_json v = composed_to_json to_value v
  end
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds or updates tags for the Amazon Web Services resource with the specified ARN."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc
          "ARN of the resource for which tags are to be added or updated."];
      tags: Tags.t [@ocaml.doc "Array of tags to be added or updated."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map_exn json "tags" Tags.of_json in
      let resourceArn = field_map_exn json "resourceArn" ResourceArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds or updates tags for the Amazon Web Services resource with the specified ARN."]
module StopStreamResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ChannelNotBroadcasting of ChannelNotBroadcasting.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `StreamUnavailable of StreamUnavailable.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ChannelNotBroadcasting" ->
          `ChannelNotBroadcasting (ChannelNotBroadcasting.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "StreamUnavailable" ->
          `StreamUnavailable (StreamUnavailable.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ChannelNotBroadcasting" ->
          `ChannelNotBroadcasting (ChannelNotBroadcasting.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "StreamUnavailable" ->
          `StreamUnavailable (StreamUnavailable.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ChannelNotBroadcasting e ->
          `Assoc
            [("error", (`String "ChannelNotBroadcasting"));
            ("details", (ChannelNotBroadcasting.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `StreamUnavailable e ->
          `Assoc
            [("error", (`String "StreamUnavailable"));
            ("details", (StreamUnavailable.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disconnects the incoming RTMPS stream for the specified channel. Can be used in conjunction with DeleteStreamKey to prevent further streaming to a channel. Many streaming client-software libraries automatically reconnect a dropped RTMPS session, so to stop the stream permanently, you may want to first revoke the streamKey attached to the channel."]
module StopStreamRequest =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t
        [@ocaml.doc
          "ARN of the channel for which the stream is to be stopped."]}
    let context_ = "StopStreamRequest"
    let make ~channelArn = fun () -> { channelArn }
    let to_value x =
      structure_to_value
        [("channelArn", (Some (ChannelArn.to_value x.channelArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelArn =
        ChannelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "channelArn") in
      make ~channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let channelArn = field_map_exn json "channelArn" ChannelArn.of_json in
      make ~channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disconnects the incoming RTMPS stream for the specified channel. Can be used in conjunction with DeleteStreamKey to prevent further streaming to a channel. Many streaming client-software libraries automatically reconnect a dropped RTMPS session, so to stop the stream permanently, you may want to first revoke the streamKey attached to the channel."]
module PutMetadataRequest =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t
        [@ocaml.doc
          "ARN of the channel into which metadata is inserted. This channel must have an active stream."];
      metadata: StreamMetadata.t
        [@ocaml.doc
          "Metadata to insert into the stream. Maximum: 1 KB per request."]}
    let context_ = "PutMetadataRequest"
    let make ~channelArn =
      fun ~metadata -> fun () -> { channelArn; metadata }
    let to_value x =
      structure_to_value
        [("channelArn", (Some (ChannelArn.to_value x.channelArn)));
        ("metadata", (Some (StreamMetadata.to_value x.metadata)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadata =
        StreamMetadata.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "metadata") in
      let channelArn =
        ChannelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "channelArn") in
      make ~metadata ~channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let metadata = field_map_exn json "metadata" StreamMetadata.of_json in
      let channelArn = field_map_exn json "channelArn" ChannelArn.of_json in
      make ~metadata ~channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Inserts metadata into the active stream of the specified channel. At most 5 requests per second per channel are allowed, each with a maximum 1 KB payload. (If 5 TPS is not sufficient for your needs, we recommend batching your data into a single PutMetadata call.) At most 155 requests per second per account are allowed. Also see Embedding Metadata within a Video Stream in the Amazon IVS User Guide."]
module ListTagsForResourceResponse =
  struct
    type nonrec t = {
      tags: Tags.t }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTagsForResourceResponse"
    let make ~tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      make ~tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map_exn json "tags" Tags.of_json in make ~tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about Amazon Web Services tags for the specified ARN."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The ARN of the resource to be retrieved."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceArn = field_map_exn json "resourceArn" ResourceArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about Amazon Web Services tags for the specified ARN."]
module ListStreamsResponse =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If there are more streams than maxResults, use nextToken in the request to get the next set."];
      streams: StreamList.t [@ocaml.doc "List of streams."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListStreamsResponse"
    let make ?nextToken = fun ~streams -> fun () -> { nextToken; streams }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("streams", (Some (StreamList.to_value x.streams)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streams =
        StreamList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "streams") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ~streams ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streams = field_map_exn json "streams" StreamList.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      make ~streams ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about live streams in your account, in the Amazon Web Services region where the API request is processed."]
module ListStreamsRequest =
  struct
    type nonrec t =
      {
      filterBy: StreamFilters.t option
        [@ocaml.doc
          "Filters the stream list to match the specified criterion."];
      maxResults: MaxStreamResults.t option
        [@ocaml.doc "Maximum number of streams to return. Default: 50."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The first stream to retrieve. This is used for pagination; see the nextToken response field."]}
    let make ?filterBy =
      fun ?maxResults ->
        fun ?nextToken -> fun () -> { filterBy; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("filterBy", (Option.map x.filterBy ~f:StreamFilters.to_value));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxStreamResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxStreamResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let filterBy =
        (Option.map ~f:StreamFilters.of_xml) (Xml.child xml_arg0 "filterBy") in
      make ?nextToken ?maxResults ?filterBy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults = field_map json "maxResults" MaxStreamResults.of_json in
      let filterBy = field_map json "filterBy" StreamFilters.of_json in
      make ?nextToken ?maxResults ?filterBy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about live streams in your account, in the Amazon Web Services region where the API request is processed."]
module ListStreamSessionsResponse =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If there are more streams than maxResults, use nextToken in the request to get the next set."];
      streamSessions: StreamSessionList.t
        [@ocaml.doc "List of stream sessions."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListStreamSessionsResponse"
    let make ?nextToken =
      fun ~streamSessions -> fun () -> { nextToken; streamSessions }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("streamSessions",
          (Some (StreamSessionList.to_value x.streamSessions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamSessions =
        StreamSessionList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "streamSessions") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ~streamSessions ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamSessions =
        field_map_exn json "streamSessions" StreamSessionList.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      make ~streamSessions ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a summary of current and previous streams for a specified channel in your account, in the AWS region where the API request is processed."]
module ListStreamSessionsRequest =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t
        [@ocaml.doc "Channel ARN used to filter the list."];
      maxResults: MaxStreamResults.t option
        [@ocaml.doc "Maximum number of streams to return. Default: 50."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The first stream to retrieve. This is used for pagination; see the nextToken response field."]}
    let context_ = "ListStreamSessionsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~channelArn -> fun () -> { maxResults; nextToken; channelArn }
    let to_value x =
      structure_to_value
        [("channelArn", (Some (ChannelArn.to_value x.channelArn)));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxStreamResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxStreamResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let channelArn =
        ChannelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "channelArn") in
      make ?nextToken ?maxResults ~channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults = field_map json "maxResults" MaxStreamResults.of_json in
      let channelArn = field_map_exn json "channelArn" ChannelArn.of_json in
      make ?nextToken ?maxResults ~channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a summary of current and previous streams for a specified channel in your account, in the AWS region where the API request is processed."]
module ListStreamKeysResponse =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If there are more stream keys than maxResults, use nextToken in the request to get the next set."];
      streamKeys: StreamKeyList.t [@ocaml.doc "List of stream keys."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListStreamKeysResponse"
    let make ?nextToken =
      fun ~streamKeys -> fun () -> { nextToken; streamKeys }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("streamKeys", (Some (StreamKeyList.to_value x.streamKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamKeys =
        StreamKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "streamKeys") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ~streamKeys ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamKeys = field_map_exn json "streamKeys" StreamKeyList.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      make ~streamKeys ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about stream keys for the specified channel."]
module ListStreamKeysRequest =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t
        [@ocaml.doc "Channel ARN used to filter the list."];
      maxResults: MaxStreamKeyResults.t option
        [@ocaml.doc "Maximum number of streamKeys to return. Default: 50."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The first stream key to retrieve. This is used for pagination; see the nextToken response field."]}
    let context_ = "ListStreamKeysRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~channelArn -> fun () -> { maxResults; nextToken; channelArn }
    let to_value x =
      structure_to_value
        [("channelArn", (Some (ChannelArn.to_value x.channelArn)));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxStreamKeyResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxStreamKeyResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let channelArn =
        ChannelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "channelArn") in
      make ?nextToken ?maxResults ~channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults =
        field_map json "maxResults" MaxStreamKeyResults.of_json in
      let channelArn = field_map_exn json "channelArn" ChannelArn.of_json in
      make ?nextToken ?maxResults ~channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about stream keys for the specified channel."]
module ListRecordingConfigurationsResponse =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If there are more recording configurations than maxResults, use nextToken in the request to get the next set."];
      recordingConfigurations: RecordingConfigurationList.t
        [@ocaml.doc "List of the matching recording configurations."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListRecordingConfigurationsResponse"
    let make ?nextToken =
      fun ~recordingConfigurations ->
        fun () -> { nextToken; recordingConfigurations }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("recordingConfigurations",
          (Some
             (RecordingConfigurationList.to_value x.recordingConfigurations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordingConfigurations =
        RecordingConfigurationList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recordingConfigurations") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ~recordingConfigurations ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recordingConfigurations =
        field_map_exn json "recordingConfigurations"
          RecordingConfigurationList.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      make ~recordingConfigurations ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about all recording configurations in your account, in the Amazon Web Services region where the API request is processed."]
module ListRecordingConfigurationsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxRecordingConfigurationResults.t option
        [@ocaml.doc
          "Maximum number of recording configurations to return. Default: 50."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The first recording configuration to retrieve. This is used for pagination; see the nextToken response field."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults
              ~f:MaxRecordingConfigurationResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxRecordingConfigurationResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults =
        field_map json "maxResults" MaxRecordingConfigurationResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about all recording configurations in your account, in the Amazon Web Services region where the API request is processed."]
module ListPlaybackKeyPairsResponse =
  struct
    type nonrec t =
      {
      keyPairs: PlaybackKeyPairList.t [@ocaml.doc "List of key pairs."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If there are more key pairs than maxResults, use nextToken in the request to get the next set."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListPlaybackKeyPairsResponse"
    let make ?nextToken = fun ~keyPairs -> fun () -> { nextToken; keyPairs }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("keyPairs", (Some (PlaybackKeyPairList.to_value x.keyPairs)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let keyPairs =
        PlaybackKeyPairList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "keyPairs") in
      make ?nextToken ~keyPairs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let keyPairs =
        field_map_exn json "keyPairs" PlaybackKeyPairList.of_json in
      make ?nextToken ~keyPairs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about playback key pairs. For more information, see Setting Up Private Channels in the Amazon IVS User Guide."]
module ListPlaybackKeyPairsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxPlaybackKeyPairResults.t option
        [@ocaml.doc
          "The first key pair to retrieve. This is used for pagination; see the nextToken response field. Default: 50."];
      nextToken: PaginationToken.t option
        [@ocaml.doc "Maximum number of key pairs to return."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults",
           (Option.map x.maxResults ~f:MaxPlaybackKeyPairResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxPlaybackKeyPairResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults =
        field_map json "maxResults" MaxPlaybackKeyPairResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about playback key pairs. For more information, see Setting Up Private Channels in the Amazon IVS User Guide."]
module ListChannelsResponse =
  struct
    type nonrec t =
      {
      channels: ChannelList.t [@ocaml.doc "List of the matching channels."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If there are more channels than maxResults, use nextToken in the request to get the next set."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListChannelsResponse"
    let make ?nextToken = fun ~channels -> fun () -> { nextToken; channels }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("channels", (Some (ChannelList.to_value x.channels)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let channels =
        ChannelList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "channels") in
      make ?nextToken ~channels ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let channels = field_map_exn json "channels" ChannelList.of_json in
      make ?nextToken ~channels ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about all channels in your account, in the Amazon Web Services region where the API request is processed. This list can be filtered to match a specified name or recording-configuration ARN. Filters are mutually exclusive and cannot be used together. If you try to use both filters, you will get an error (409 ConflictException)."]
module ListChannelsRequest =
  struct
    type nonrec t =
      {
      filterByName: ChannelName.t option
        [@ocaml.doc "Filters the channel list to match the specified name."];
      filterByRecordingConfigurationArn:
        ChannelRecordingConfigurationArn.t option
        [@ocaml.doc
          "Filters the channel list to match the specified recording-configuration ARN."];
      maxResults: MaxChannelResults.t option
        [@ocaml.doc "Maximum number of channels to return. Default: 50."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The first channel to retrieve. This is used for pagination; see the nextToken response field."]}
    let make ?filterByName =
      fun ?filterByRecordingConfigurationArn ->
        fun ?maxResults ->
          fun ?nextToken ->
            fun () ->
              {
                filterByName;
                filterByRecordingConfigurationArn;
                maxResults;
                nextToken
              }
    let to_value x =
      structure_to_value
        [("filterByName",
           (Option.map x.filterByName ~f:ChannelName.to_value));
        ("filterByRecordingConfigurationArn",
          (Option.map x.filterByRecordingConfigurationArn
             ~f:ChannelRecordingConfigurationArn.to_value));
        ("maxResults",
          (Option.map x.maxResults ~f:MaxChannelResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxChannelResults.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let filterByRecordingConfigurationArn =
        (Option.map ~f:ChannelRecordingConfigurationArn.of_xml)
          (Xml.child xml_arg0 "filterByRecordingConfigurationArn") in
      let filterByName =
        (Option.map ~f:ChannelName.of_xml)
          (Xml.child xml_arg0 "filterByName") in
      make ?nextToken ?maxResults ?filterByRecordingConfigurationArn
        ?filterByName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults = field_map json "maxResults" MaxChannelResults.of_json in
      let filterByRecordingConfigurationArn =
        field_map json "filterByRecordingConfigurationArn"
          ChannelRecordingConfigurationArn.of_json in
      let filterByName = field_map json "filterByName" ChannelName.of_json in
      make ?nextToken ?maxResults ?filterByRecordingConfigurationArn
        ?filterByName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about all channels in your account, in the Amazon Web Services region where the API request is processed. This list can be filtered to match a specified name or recording-configuration ARN. Filters are mutually exclusive and cannot be used together. If you try to use both filters, you will get an error (409 ConflictException)."]
module ImportPlaybackKeyPairResponse =
  struct
    type nonrec t = {
      keyPair: PlaybackKeyPair.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `PendingVerification of PendingVerification.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?keyPair = fun () -> { keyPair }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `PendingVerification e ->
          `Assoc
            [("error", (`String "PendingVerification"));
            ("details", (PendingVerification.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("keyPair", (Option.map x.keyPair ~f:PlaybackKeyPair.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyPair =
        (Option.map ~f:PlaybackKeyPair.of_xml) (Xml.child xml_arg0 "keyPair") in
      make ?keyPair ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let keyPair = field_map json "keyPair" PlaybackKeyPair.of_json in
      make ?keyPair ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports the public portion of a new key pair and returns its arn and fingerprint. The privateKey can then be used to generate viewer authorization tokens, to grant viewers access to private channels. For more information, see Setting Up Private Channels in the Amazon IVS User Guide."]
module ImportPlaybackKeyPairRequest =
  struct
    type nonrec t =
      {
      name: PlaybackKeyPairName.t option
        [@ocaml.doc
          "Playback-key-pair name. The value does not need to be unique."];
      publicKeyMaterial: PlaybackPublicKeyMaterial.t
        [@ocaml.doc "The public portion of a customer-generated key pair."];
      tags: Tags.t option
        [@ocaml.doc
          "Any tags provided with the request are added to the playback key pair tags."]}
    let context_ = "ImportPlaybackKeyPairRequest"
    let make ?name =
      fun ?tags ->
        fun ~publicKeyMaterial -> fun () -> { name; tags; publicKeyMaterial }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:PlaybackKeyPairName.to_value));
        ("publicKeyMaterial",
          (Some (PlaybackPublicKeyMaterial.to_value x.publicKeyMaterial)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let publicKeyMaterial =
        PlaybackPublicKeyMaterial.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "publicKeyMaterial") in
      let name =
        (Option.map ~f:PlaybackKeyPairName.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?tags ~publicKeyMaterial ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let publicKeyMaterial =
        field_map_exn json "publicKeyMaterial"
          PlaybackPublicKeyMaterial.of_json in
      let name = field_map json "name" PlaybackKeyPairName.of_json in
      make ?tags ~publicKeyMaterial ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports the public portion of a new key pair and returns its arn and fingerprint. The privateKey can then be used to generate viewer authorization tokens, to grant viewers access to private channels. For more information, see Setting Up Private Channels in the Amazon IVS User Guide."]
module GetStreamSessionResponse =
  struct
    type nonrec t =
      {
      streamSession: StreamSession.t option
        [@ocaml.doc "List of stream details."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?streamSession = fun () -> { streamSession }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("streamSession",
           (Option.map x.streamSession ~f:StreamSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamSession =
        (Option.map ~f:StreamSession.of_xml)
          (Xml.child xml_arg0 "streamSession") in
      make ?streamSession ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamSession =
        field_map json "streamSession" StreamSession.of_json in
      make ?streamSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets metadata on a specified stream."]
module GetStreamSessionRequest =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t [@ocaml.doc "ARN of the channel resource"];
      streamId: StreamId.t option
        [@ocaml.doc
          "Unique identifier for a live or previously live stream in the specified channel. If no streamId is provided, this returns the most recent stream session for the channel, if it exists."]}
    let context_ = "GetStreamSessionRequest"
    let make ?streamId =
      fun ~channelArn -> fun () -> { streamId; channelArn }
    let to_value x =
      structure_to_value
        [("channelArn", (Some (ChannelArn.to_value x.channelArn)));
        ("streamId", (Option.map x.streamId ~f:StreamId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamId =
        (Option.map ~f:StreamId.of_xml) (Xml.child xml_arg0 "streamId") in
      let channelArn =
        ChannelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "channelArn") in
      make ?streamId ~channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamId = field_map json "streamId" StreamId.of_json in
      let channelArn = field_map_exn json "channelArn" ChannelArn.of_json in
      make ?streamId ~channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets metadata on a specified stream."]
module GetStreamResponse =
  struct
    type nonrec t = {
      stream: Stream.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ChannelNotBroadcasting of ChannelNotBroadcasting.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?stream = fun () -> { stream }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ChannelNotBroadcasting" ->
          `ChannelNotBroadcasting (ChannelNotBroadcasting.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ChannelNotBroadcasting" ->
          `ChannelNotBroadcasting (ChannelNotBroadcasting.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ChannelNotBroadcasting e ->
          `Assoc
            [("error", (`String "ChannelNotBroadcasting"));
            ("details", (ChannelNotBroadcasting.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("stream", (Option.map x.stream ~f:Stream.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stream =
        (Option.map ~f:Stream.of_xml) (Xml.child xml_arg0 "stream") in
      make ?stream ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let stream = field_map json "stream" Stream.of_json in make ?stream ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the active (live) stream on a specified channel."]
module GetStreamRequest =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t
        [@ocaml.doc "Channel ARN for stream to be accessed."]}
    let context_ = "GetStreamRequest"
    let make ~channelArn = fun () -> { channelArn }
    let to_value x =
      structure_to_value
        [("channelArn", (Some (ChannelArn.to_value x.channelArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channelArn =
        ChannelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "channelArn") in
      make ~channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let channelArn = field_map_exn json "channelArn" ChannelArn.of_json in
      make ~channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about the active (live) stream on a specified channel."]
module GetStreamKeyResponse =
  struct
    type nonrec t = {
      streamKey: StreamKey.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?streamKey = fun () -> { streamKey }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("streamKey", (Option.map x.streamKey ~f:StreamKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamKey =
        (Option.map ~f:StreamKey.of_xml) (Xml.child xml_arg0 "streamKey") in
      make ?streamKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamKey = field_map json "streamKey" StreamKey.of_json in
      make ?streamKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets stream-key information for a specified ARN."]
module GetStreamKeyRequest =
  struct
    type nonrec t =
      {
      arn: StreamKeyArn.t
        [@ocaml.doc "ARN for the stream key to be retrieved."]}
    let context_ = "GetStreamKeyRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("arn", (Some (StreamKeyArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        StreamKeyArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arn = field_map_exn json "arn" StreamKeyArn.of_json in make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets stream-key information for a specified ARN."]
module GetRecordingConfigurationResponse =
  struct
    type nonrec t =
      {
      recordingConfiguration: RecordingConfiguration.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordingConfiguration = fun () -> { recordingConfiguration }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recordingConfiguration",
           (Option.map x.recordingConfiguration
              ~f:RecordingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordingConfiguration =
        (Option.map ~f:RecordingConfiguration.of_xml)
          (Xml.child xml_arg0 "recordingConfiguration") in
      make ?recordingConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recordingConfiguration =
        field_map json "recordingConfiguration"
          RecordingConfiguration.of_json in
      make ?recordingConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets the recording configuration for the specified ARN."]
module GetRecordingConfigurationRequest =
  struct
    type nonrec t =
      {
      arn: RecordingConfigurationArn.t
        [@ocaml.doc "ARN of the recording configuration to be retrieved."]}
    let context_ = "GetRecordingConfigurationRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (RecordingConfigurationArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        RecordingConfigurationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arn = field_map_exn json "arn" RecordingConfigurationArn.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Gets the recording configuration for the specified ARN."]
module GetPlaybackKeyPairResponse =
  struct
    type nonrec t = {
      keyPair: PlaybackKeyPair.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?keyPair = fun () -> { keyPair }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("keyPair", (Option.map x.keyPair ~f:PlaybackKeyPair.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyPair =
        (Option.map ~f:PlaybackKeyPair.of_xml) (Xml.child xml_arg0 "keyPair") in
      make ?keyPair ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let keyPair = field_map json "keyPair" PlaybackKeyPair.of_json in
      make ?keyPair ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a specified playback authorization key pair and returns the arn and fingerprint. The privateKey held by the caller can be used to generate viewer authorization tokens, to grant viewers access to private channels. For more information, see Setting Up Private Channels in the Amazon IVS User Guide."]
module GetPlaybackKeyPairRequest =
  struct
    type nonrec t =
      {
      arn: PlaybackKeyPairArn.t
        [@ocaml.doc "ARN of the key pair to be returned."]}
    let context_ = "GetPlaybackKeyPairRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (PlaybackKeyPairArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        PlaybackKeyPairArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arn = field_map_exn json "arn" PlaybackKeyPairArn.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a specified playback authorization key pair and returns the arn and fingerprint. The privateKey held by the caller can be used to generate viewer authorization tokens, to grant viewers access to private channels. For more information, see Setting Up Private Channels in the Amazon IVS User Guide."]
module GetChannelResponse =
  struct
    type nonrec t = {
      channel: Channel.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channel = fun () -> { channel }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("channel", (Option.map x.channel ~f:Channel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channel =
        (Option.map ~f:Channel.of_xml) (Xml.child xml_arg0 "channel") in
      make ?channel ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let channel = field_map json "channel" Channel.of_json in
      make ?channel ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the channel configuration for the specified channel ARN. See also BatchGetChannel."]
module GetChannelRequest =
  struct
    type nonrec t =
      {
      arn: ChannelArn.t
        [@ocaml.doc
          "ARN of the channel for which the configuration is to be retrieved."]}
    let context_ = "GetChannelRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("arn", (Some (ChannelArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        ChannelArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arn = field_map_exn json "arn" ChannelArn.of_json in make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the channel configuration for the specified channel ARN. See also BatchGetChannel."]
module DeleteStreamKeyRequest =
  struct
    type nonrec t =
      {
      arn: StreamKeyArn.t [@ocaml.doc "ARN of the stream key to be deleted."]}
    let context_ = "DeleteStreamKeyRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("arn", (Some (StreamKeyArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        StreamKeyArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arn = field_map_exn json "arn" StreamKeyArn.of_json in make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the stream key for the specified ARN, so it can no longer be used to stream."]
module DeleteRecordingConfigurationRequest =
  struct
    type nonrec t =
      {
      arn: RecordingConfigurationArn.t
        [@ocaml.doc "ARN of the recording configuration to be deleted."]}
    let context_ = "DeleteRecordingConfigurationRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (RecordingConfigurationArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        RecordingConfigurationArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arn = field_map_exn json "arn" RecordingConfigurationArn.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the recording configuration for the specified ARN. If you try to delete a recording configuration that is associated with a channel, you will get an error (409 ConflictException). To avoid this, for all channels that reference the recording configuration, first use UpdateChannel to set the recordingConfigurationArn field to an empty string, then use DeleteRecordingConfiguration."]
module DeletePlaybackKeyPairResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `PendingVerification of PendingVerification.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `PendingVerification e ->
          `Assoc
            [("error", (`String "PendingVerification"));
            ("details", (PendingVerification.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a specified authorization key pair. This invalidates future viewer tokens generated using the key pair\226\128\153s privateKey. For more information, see Setting Up Private Channels in the Amazon IVS User Guide."]
module DeletePlaybackKeyPairRequest =
  struct
    type nonrec t =
      {
      arn: PlaybackKeyPairArn.t
        [@ocaml.doc "ARN of the key pair to be deleted."]}
    let context_ = "DeletePlaybackKeyPairRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (PlaybackKeyPairArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        PlaybackKeyPairArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arn = field_map_exn json "arn" PlaybackKeyPairArn.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a specified authorization key pair. This invalidates future viewer tokens generated using the key pair\226\128\153s privateKey. For more information, see Setting Up Private Channels in the Amazon IVS User Guide."]
module DeleteChannelRequest =
  struct
    type nonrec t =
      {
      arn: ChannelArn.t [@ocaml.doc "ARN of the channel to be deleted."]}
    let context_ = "DeleteChannelRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value [("arn", (Some (ChannelArn.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        ChannelArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arn = field_map_exn json "arn" ChannelArn.of_json in make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified channel and its associated stream keys. If you try to delete a live channel, you will get an error (409 ConflictException). To delete a channel that is live, call StopStream, wait for the Amazon EventBridge \"Stream End\" event (to verify that the stream's state was changed from Live to Offline), then call DeleteChannel. (See Using EventBridge with Amazon IVS.)"]
module CreateStreamKeyResponse =
  struct
    type nonrec t =
      {
      streamKey: StreamKey.t option
        [@ocaml.doc
          "Stream key used to authenticate an RTMPS stream for ingestion."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `PendingVerification of PendingVerification.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?streamKey = fun () -> { streamKey }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `PendingVerification e ->
          `Assoc
            [("error", (`String "PendingVerification"));
            ("details", (PendingVerification.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("streamKey", (Option.map x.streamKey ~f:StreamKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamKey =
        (Option.map ~f:StreamKey.of_xml) (Xml.child xml_arg0 "streamKey") in
      make ?streamKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamKey = field_map json "streamKey" StreamKey.of_json in
      make ?streamKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a stream key, used to initiate a stream, for the specified channel ARN. Note that CreateChannel creates a stream key. If you subsequently use CreateStreamKey on the same channel, it will fail because a stream key already exists and there is a limit of 1 stream key per channel. To reset the stream key on a channel, use DeleteStreamKey and then CreateStreamKey."]
module CreateStreamKeyRequest =
  struct
    type nonrec t =
      {
      channelArn: ChannelArn.t
        [@ocaml.doc "ARN of the channel for which to create the stream key."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."]}
    let context_ = "CreateStreamKeyRequest"
    let make ?tags = fun ~channelArn -> fun () -> { tags; channelArn }
    let to_value x =
      structure_to_value
        [("channelArn", (Some (ChannelArn.to_value x.channelArn)));
        ("tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let channelArn =
        ChannelArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "channelArn") in
      make ?tags ~channelArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "tags" Tags.of_json in
      let channelArn = field_map_exn json "channelArn" ChannelArn.of_json in
      make ?tags ~channelArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a stream key, used to initiate a stream, for the specified channel ARN. Note that CreateChannel creates a stream key. If you subsequently use CreateStreamKey on the same channel, it will fail because a stream key already exists and there is a limit of 1 stream key per channel. To reset the stream key on a channel, use DeleteStreamKey and then CreateStreamKey."]
module CreateRecordingConfigurationResponse =
  struct
    type nonrec t =
      {
      recordingConfiguration: RecordingConfiguration.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `PendingVerification of PendingVerification.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recordingConfiguration = fun () -> { recordingConfiguration }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `PendingVerification e ->
          `Assoc
            [("error", (`String "PendingVerification"));
            ("details", (PendingVerification.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recordingConfiguration",
           (Option.map x.recordingConfiguration
              ~f:RecordingConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recordingConfiguration =
        (Option.map ~f:RecordingConfiguration.of_xml)
          (Xml.child xml_arg0 "recordingConfiguration") in
      make ?recordingConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let recordingConfiguration =
        field_map json "recordingConfiguration"
          RecordingConfiguration.of_json in
      make ?recordingConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new recording configuration, used to enable recording to Amazon S3. Known issue: In the us-east-1 region, if you use the Amazon Web Services CLI to create a recording configuration, it returns success even if the S3 bucket is in a different region. In this case, the state of the recording configuration is CREATE_FAILED (instead of ACTIVE). (In other regions, the CLI correctly returns failure if the bucket is in a different region.) Workaround: Ensure that your S3 bucket is in the same region as the recording configuration. If you create a recording configuration in a different region as your S3 bucket, delete that recording configuration and create a new one with an S3 bucket from the correct region."]
module CreateRecordingConfigurationRequest =
  struct
    type nonrec t =
      {
      destinationConfiguration: DestinationConfiguration.t
        [@ocaml.doc
          "A complex type that contains a destination configuration for where recorded video will be stored."];
      name: RecordingConfigurationName.t option
        [@ocaml.doc
          "Recording-configuration name. The value does not need to be unique."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."];
      thumbnailConfiguration: ThumbnailConfiguration.t option
        [@ocaml.doc
          "A complex type that allows you to enable/disable the recording of thumbnails for a live session and modify the interval at which thumbnails are generated for the live session."]}
    let context_ = "CreateRecordingConfigurationRequest"
    let make ?name =
      fun ?tags ->
        fun ?thumbnailConfiguration ->
          fun ~destinationConfiguration ->
            fun () ->
              { name; tags; thumbnailConfiguration; destinationConfiguration
              }
    let to_value x =
      structure_to_value
        [("destinationConfiguration",
           (Some
              (DestinationConfiguration.to_value x.destinationConfiguration)));
        ("name", (Option.map x.name ~f:RecordingConfigurationName.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("thumbnailConfiguration",
          (Option.map x.thumbnailConfiguration
             ~f:ThumbnailConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let thumbnailConfiguration =
        (Option.map ~f:ThumbnailConfiguration.of_xml)
          (Xml.child xml_arg0 "thumbnailConfiguration") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let name =
        (Option.map ~f:RecordingConfigurationName.of_xml)
          (Xml.child xml_arg0 "name") in
      let destinationConfiguration =
        DestinationConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "destinationConfiguration") in
      make ?thumbnailConfiguration ?tags ?name ~destinationConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let thumbnailConfiguration =
        field_map json "thumbnailConfiguration"
          ThumbnailConfiguration.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let name = field_map json "name" RecordingConfigurationName.of_json in
      let destinationConfiguration =
        field_map_exn json "destinationConfiguration"
          DestinationConfiguration.of_json in
      make ?thumbnailConfiguration ?tags ?name ~destinationConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new recording configuration, used to enable recording to Amazon S3. Known issue: In the us-east-1 region, if you use the Amazon Web Services CLI to create a recording configuration, it returns success even if the S3 bucket is in a different region. In this case, the state of the recording configuration is CREATE_FAILED (instead of ACTIVE). (In other regions, the CLI correctly returns failure if the bucket is in a different region.) Workaround: Ensure that your S3 bucket is in the same region as the recording configuration. If you create a recording configuration in a different region as your S3 bucket, delete that recording configuration and create a new one with an S3 bucket from the correct region."]
module CreateChannelResponse =
  struct
    type nonrec t =
      {
      channel: Channel.t option ;
      streamKey: StreamKey.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `PendingVerification of PendingVerification.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?channel = fun ?streamKey -> fun () -> { channel; streamKey }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "PendingVerification" ->
          `PendingVerification (PendingVerification.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `PendingVerification e ->
          `Assoc
            [("error", (`String "PendingVerification"));
            ("details", (PendingVerification.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("channel", (Option.map x.channel ~f:Channel.to_value));
        ("streamKey", (Option.map x.streamKey ~f:StreamKey.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamKey =
        (Option.map ~f:StreamKey.of_xml) (Xml.child xml_arg0 "streamKey") in
      let channel =
        (Option.map ~f:Channel.of_xml) (Xml.child xml_arg0 "channel") in
      make ?streamKey ?channel ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamKey = field_map json "streamKey" StreamKey.of_json in
      let channel = field_map json "channel" Channel.of_json in
      make ?streamKey ?channel ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new channel and an associated stream key to start streaming."]
module CreateChannelRequest =
  struct
    type nonrec t =
      {
      authorized: Boolean.t option
        [@ocaml.doc
          "Whether the channel is private (enabled for playback authorization). Default: false."];
      latencyMode: ChannelLatencyMode.t option
        [@ocaml.doc
          "Channel latency mode. Use NORMAL to broadcast and deliver live video up to Full HD. Use LOW for near-real-time interaction with viewers. (Note: In the Amazon IVS console, LOW and NORMAL correspond to Ultra-low and Standard, respectively.) Default: LOW."];
      name: ChannelName.t option [@ocaml.doc "Channel name."];
      recordingConfigurationArn: ChannelRecordingConfigurationArn.t option
        [@ocaml.doc
          "Recording-configuration ARN. Default: \"\" (empty string, recording is disabled)."];
      tags: Tags.t option
        [@ocaml.doc
          "Array of 1-50 maps, each of the form string:string (key:value)."];
      type_: ChannelType.t option
        [@ocaml.doc
          "Channel type, which determines the allowable resolution and bitrate. If you exceed the allowable resolution or bitrate, the stream probably will disconnect immediately. Default: STANDARD. Valid values: STANDARD: Multiple qualities are generated from the original input, to automatically give viewers the best experience for their devices and network conditions. Resolution can be up to 1080p and bitrate can be up to 8.5 Mbps. Audio is transcoded only for renditions 360p and below; above that, audio is passed through. BASIC: Amazon IVS delivers the original input to viewers. The viewer\226\128\153s video-quality choice is limited to the original input. Resolution can be up to 480p and bitrate can be up to 1.5 Mbps."]}
    let make ?authorized =
      fun ?latencyMode ->
        fun ?name ->
          fun ?recordingConfigurationArn ->
            fun ?tags ->
              fun ?type_ ->
                fun () ->
                  {
                    authorized;
                    latencyMode;
                    name;
                    recordingConfigurationArn;
                    tags;
                    type_
                  }
    let to_value x =
      structure_to_value
        [("authorized", (Option.map x.authorized ~f:Boolean.to_value));
        ("latencyMode",
          (Option.map x.latencyMode ~f:ChannelLatencyMode.to_value));
        ("name", (Option.map x.name ~f:ChannelName.to_value));
        ("recordingConfigurationArn",
          (Option.map x.recordingConfigurationArn
             ~f:ChannelRecordingConfigurationArn.to_value));
        ("tags", (Option.map x.tags ~f:Tags.to_value));
        ("type", (Option.map x.type_ ~f:ChannelType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:ChannelType.of_xml) (Xml.child xml_arg0 "type") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "tags") in
      let recordingConfigurationArn =
        (Option.map ~f:ChannelRecordingConfigurationArn.of_xml)
          (Xml.child xml_arg0 "recordingConfigurationArn") in
      let name =
        (Option.map ~f:ChannelName.of_xml) (Xml.child xml_arg0 "name") in
      let latencyMode =
        (Option.map ~f:ChannelLatencyMode.of_xml)
          (Xml.child xml_arg0 "latencyMode") in
      let authorized =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "authorized") in
      make ?type_ ?tags ?recordingConfigurationArn ?name ?latencyMode
        ?authorized ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let type_ = field_map json "type" ChannelType.of_json in
      let tags = field_map json "tags" Tags.of_json in
      let recordingConfigurationArn =
        field_map json "recordingConfigurationArn"
          ChannelRecordingConfigurationArn.of_json in
      let name = field_map json "name" ChannelName.of_json in
      let latencyMode =
        field_map json "latencyMode" ChannelLatencyMode.of_json in
      let authorized = field_map json "authorized" Boolean.of_json in
      make ?type_ ?tags ?recordingConfigurationArn ?name ?latencyMode
        ?authorized ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new channel and an associated stream key to start streaming."]
module BatchGetStreamKeyResponse =
  struct
    type nonrec t =
      {
      errors: BatchErrors.t option ;
      streamKeys: StreamKeys.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?errors = fun ?streamKeys -> fun () -> { errors; streamKeys }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("errors", (Option.map x.errors ~f:BatchErrors.to_value));
        ("streamKeys", (Option.map x.streamKeys ~f:StreamKeys.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let streamKeys =
        (Option.map ~f:StreamKeys.of_xml) (Xml.child xml_arg0 "streamKeys") in
      let errors =
        (Option.map ~f:BatchErrors.of_xml) (Xml.child xml_arg0 "errors") in
      make ?streamKeys ?errors ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let streamKeys = field_map json "streamKeys" StreamKeys.of_json in
      let errors = field_map json "errors" BatchErrors.of_json in
      make ?streamKeys ?errors ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Performs GetStreamKey on multiple ARNs simultaneously."]
module BatchGetStreamKeyRequest =
  struct
    type nonrec t =
      {
      arns: StreamKeyArnList.t [@ocaml.doc "Array of ARNs, one per channel."]}
    let context_ = "BatchGetStreamKeyRequest"
    let make ~arns = fun () -> { arns }
    let to_value x =
      structure_to_value
        [("arns", (Some (StreamKeyArnList.to_value x.arns)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arns =
        StreamKeyArnList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arns") in
      make ~arns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arns = field_map_exn json "arns" StreamKeyArnList.of_json in
      make ~arns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Performs GetStreamKey on multiple ARNs simultaneously."]
module BatchGetChannelResponse =
  struct
    type nonrec t =
      {
      channels: Channels.t option ;
      errors: BatchErrors.t option
        [@ocaml.doc
          "Each error object is related to a specific ARN in the request."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?channels = fun ?errors -> fun () -> { channels; errors }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("channels", (Option.map x.channels ~f:Channels.to_value));
        ("errors", (Option.map x.errors ~f:BatchErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:BatchErrors.of_xml) (Xml.child xml_arg0 "errors") in
      let channels =
        (Option.map ~f:Channels.of_xml) (Xml.child xml_arg0 "channels") in
      make ?errors ?channels ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let errors = field_map json "errors" BatchErrors.of_json in
      let channels = field_map json "channels" Channels.of_json in
      make ?errors ?channels ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Performs GetChannel on multiple ARNs simultaneously."]
module BatchGetChannelRequest =
  struct
    type nonrec t =
      {
      arns: ChannelArnList.t [@ocaml.doc "Array of ARNs, one per channel."]}
    let context_ = "BatchGetChannelRequest"
    let make ~arns = fun () -> { arns }
    let to_value x =
      structure_to_value [("arns", (Some (ChannelArnList.to_value x.arns)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arns =
        ChannelArnList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arns") in
      make ~arns ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let arns = field_map_exn json "arns" ChannelArnList.of_json in
      make ~arns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Performs GetChannel on multiple ARNs simultaneously."]
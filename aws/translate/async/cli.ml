(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_parallel_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and encryptionKey =
         flag "encryption-key" (optional json_arg) ~doc:"JSON EncryptionKey"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and parallelDataConfig =
         flag "parallel-data-config" (required json_arg)
           ~doc:"JSON ParallelDataConfig"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING ClientTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_parallel_data
           (Values.CreateParallelDataRequest.make ?description
              ?encryptionKey:(Option.map ~f:Values.EncryptionKey.of_json
                                encryptionKey) ~name
              ~parallelDataConfig:(Values.ParallelDataConfig.of_json
                                     parallelDataConfig) ~clientToken ())
           (Some Values.CreateParallelDataResponse.to_json)
           (Some Values.CreateParallelDataResponse.error_to_json)])
let delete_parallel_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_parallel_data
           (Values.DeleteParallelDataRequest.make ~name ())
           (Some Values.DeleteParallelDataResponse.to_json)
           (Some Values.DeleteParallelDataResponse.error_to_json)])
let delete_terminology =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_terminology
           (Values.DeleteTerminologyRequest.make ~name ()) None None])
let describe_text_translation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_text_translation_job
           (Values.DescribeTextTranslationJobRequest.make ~jobId ())
           (Some Values.DescribeTextTranslationJobResponse.to_json)
           (Some Values.DescribeTextTranslationJobResponse.error_to_json)])
let get_parallel_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_parallel_data (Values.GetParallelDataRequest.make ~name ())
           (Some Values.GetParallelDataResponse.to_json)
           (Some Values.GetParallelDataResponse.error_to_json)])
let get_terminology =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and terminologyDataFormat =
         flag "terminology-data-format" (optional json_arg)
           ~doc:"JSON TerminologyDataFormat"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_terminology
           (Values.GetTerminologyRequest.make
              ?terminologyDataFormat:(Option.map
                                        ~f:Values.TerminologyDataFormat.of_json
                                        terminologyDataFormat) ~name ())
           (Some Values.GetTerminologyResponse.to_json)
           (Some Values.GetTerminologyResponse.error_to_json)])
let import_terminology =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and encryptionKey =
         flag "encryption-key" (optional json_arg) ~doc:"JSON EncryptionKey"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and mergeStrategy =
         flag "merge-strategy" (required json_arg) ~doc:"JSON MergeStrategy"
       and terminologyData =
         flag "terminology-data" (required json_arg)
           ~doc:"JSON TerminologyData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_terminology
           (Values.ImportTerminologyRequest.make ?description
              ?encryptionKey:(Option.map ~f:Values.EncryptionKey.of_json
                                encryptionKey) ~name
              ~mergeStrategy:(Values.MergeStrategy.of_json mergeStrategy)
              ~terminologyData:(Values.TerminologyData.of_json
                                  terminologyData) ())
           (Some Values.ImportTerminologyResponse.to_json)
           (Some Values.ImportTerminologyResponse.error_to_json)])
let list_parallel_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_parallel_data
           (Values.ListParallelDataRequest.make ?nextToken ?maxResults ())
           (Some Values.ListParallelDataResponse.to_json)
           (Some Values.ListParallelDataResponse.error_to_json)])
let list_terminologies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_terminologies
           (Values.ListTerminologiesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListTerminologiesResponse.to_json)
           (Some Values.ListTerminologiesResponse.error_to_json)])
let list_text_translation_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON TextTranslationJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_text_translation_jobs
           (Values.ListTextTranslationJobsRequest.make
              ?filter:(Option.map ~f:Values.TextTranslationJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListTextTranslationJobsResponse.to_json)
           (Some Values.ListTextTranslationJobsResponse.error_to_json)])
let start_text_translation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and terminologyNames =
         flag "terminology-names" (optional json_arg)
           ~doc:"JSON ResourceNameList"
       and parallelDataNames =
         flag "parallel-data-names" (optional json_arg)
           ~doc:"JSON ResourceNameList"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON TranslationSettings"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and sourceLanguageCode =
         flag "source-language-code" (required string)
           ~doc:"STRING LanguageCodeString"
       and targetLanguageCodes =
         flag "target-language-codes" (required json_arg)
           ~doc:"JSON TargetLanguageCodeStringList"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING ClientTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_text_translation_job
           (Values.StartTextTranslationJobRequest.make ?jobName
              ?terminologyNames:(Option.map
                                   ~f:Values.ResourceNameList.of_json
                                   terminologyNames)
              ?parallelDataNames:(Option.map
                                    ~f:Values.ResourceNameList.of_json
                                    parallelDataNames)
              ?settings:(Option.map ~f:Values.TranslationSettings.of_json
                           settings)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~sourceLanguageCode
              ~targetLanguageCodes:(Values.TargetLanguageCodeStringList.of_json
                                      targetLanguageCodes) ~clientToken ())
           (Some Values.StartTextTranslationJobResponse.to_json)
           (Some Values.StartTextTranslationJobResponse.error_to_json)])
let stop_text_translation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_text_translation_job
           (Values.StopTextTranslationJobRequest.make ~jobId ())
           (Some Values.StopTextTranslationJobResponse.to_json)
           (Some Values.StopTextTranslationJobResponse.error_to_json)])
let translate_text =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and terminologyNames =
         flag "terminology-names" (optional json_arg)
           ~doc:"JSON ResourceNameList"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON TranslationSettings"
       and text =
         flag "text" (required string) ~doc:"STRING BoundedLengthString"
       and sourceLanguageCode =
         flag "source-language-code" (required string)
           ~doc:"STRING LanguageCodeString"
       and targetLanguageCode =
         flag "target-language-code" (required string)
           ~doc:"STRING LanguageCodeString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.translate_text
           (Values.TranslateTextRequest.make
              ?terminologyNames:(Option.map
                                   ~f:Values.ResourceNameList.of_json
                                   terminologyNames)
              ?settings:(Option.map ~f:Values.TranslationSettings.of_json
                           settings) ~text ~sourceLanguageCode
              ~targetLanguageCode ())
           (Some Values.TranslateTextResponse.to_json)
           (Some Values.TranslateTextResponse.error_to_json)])
let update_parallel_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and parallelDataConfig =
         flag "parallel-data-config" (required json_arg)
           ~doc:"JSON ParallelDataConfig"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING ClientTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_parallel_data
           (Values.UpdateParallelDataRequest.make ?description ~name
              ~parallelDataConfig:(Values.ParallelDataConfig.of_json
                                     parallelDataConfig) ~clientToken ())
           (Some Values.UpdateParallelDataResponse.to_json)
           (Some Values.UpdateParallelDataResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-parallel-data", create_parallel_data);
    ("delete-parallel-data", delete_parallel_data);
    ("delete-terminology", delete_terminology);
    ("describe-text-translation-job", describe_text_translation_job);
    ("get-parallel-data", get_parallel_data);
    ("get-terminology", get_terminology);
    ("import-terminology", import_terminology);
    ("list-parallel-data", list_parallel_data);
    ("list-terminologies", list_terminologies);
    ("list-text-translation-jobs", list_text_translation_jobs);
    ("start-text-translation-job", start_text_translation_job);
    ("stop-text-translation-job", stop_text_translation_job);
    ("translate-text", translate_text);
    ("update-parallel-data", update_parallel_data)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_detect_dominant_language =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and textList =
         flag "text-list" (required json_arg)
           ~doc:"JSON CustomerInputStringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_detect_dominant_language
           (Values.BatchDetectDominantLanguageRequest.make
              ~textList:(Values.CustomerInputStringList.of_json textList) ())
           (Some Values.BatchDetectDominantLanguageResponse.to_json)
           (Some Values.BatchDetectDominantLanguageResponse.error_to_json)])
let batch_detect_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and textList =
         flag "text-list" (required json_arg)
           ~doc:"JSON CustomerInputStringList"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_detect_entities
           (Values.BatchDetectEntitiesRequest.make
              ~textList:(Values.CustomerInputStringList.of_json textList)
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.BatchDetectEntitiesResponse.to_json)
           (Some Values.BatchDetectEntitiesResponse.error_to_json)])
let batch_detect_key_phrases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and textList =
         flag "text-list" (required json_arg)
           ~doc:"JSON CustomerInputStringList"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_detect_key_phrases
           (Values.BatchDetectKeyPhrasesRequest.make
              ~textList:(Values.CustomerInputStringList.of_json textList)
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.BatchDetectKeyPhrasesResponse.to_json)
           (Some Values.BatchDetectKeyPhrasesResponse.error_to_json)])
let batch_detect_sentiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and textList =
         flag "text-list" (required json_arg)
           ~doc:"JSON CustomerInputStringList"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_detect_sentiment
           (Values.BatchDetectSentimentRequest.make
              ~textList:(Values.CustomerInputStringList.of_json textList)
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.BatchDetectSentimentResponse.to_json)
           (Some Values.BatchDetectSentimentResponse.error_to_json)])
let batch_detect_syntax =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and textList =
         flag "text-list" (required json_arg)
           ~doc:"JSON CustomerInputStringList"
       and languageCode =
         flag "language-code" (required json_arg)
           ~doc:"JSON SyntaxLanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_detect_syntax
           (Values.BatchDetectSyntaxRequest.make
              ~textList:(Values.CustomerInputStringList.of_json textList)
              ~languageCode:(Values.SyntaxLanguageCode.of_json languageCode)
              ()) (Some Values.BatchDetectSyntaxResponse.to_json)
           (Some Values.BatchDetectSyntaxResponse.error_to_json)])
let batch_detect_targeted_sentiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and textList =
         flag "text-list" (required json_arg)
           ~doc:"JSON CustomerInputStringList"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_detect_targeted_sentiment
           (Values.BatchDetectTargetedSentimentRequest.make
              ~textList:(Values.CustomerInputStringList.of_json textList)
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.BatchDetectTargetedSentimentResponse.to_json)
           (Some Values.BatchDetectTargetedSentimentResponse.error_to_json)])
let classify_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (optional string) ~doc:"STRING CustomerInputString"
       and bytes =
         flag "bytes" (optional json_arg)
           ~doc:"JSON SemiStructuredDocumentBlob"
       and documentReaderConfig =
         flag "document-reader-config" (optional json_arg)
           ~doc:"JSON DocumentReaderConfig"
       and endpointArn =
         flag "endpoint-arn" (required string)
           ~doc:"STRING DocumentClassifierEndpointArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.classify_document
           (Values.ClassifyDocumentRequest.make ?text
              ?bytes:(Option.map ~f:Values.SemiStructuredDocumentBlob.of_json
                        bytes)
              ?documentReaderConfig:(Option.map
                                       ~f:Values.DocumentReaderConfig.of_json
                                       documentReaderConfig) ~endpointArn ())
           (Some Values.ClassifyDocumentResponse.to_json)
           (Some Values.ClassifyDocumentResponse.error_to_json)])
let contains_pii_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text = flag "text" (required string) ~doc:"STRING String"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.contains_pii_entities
           (Values.ContainsPiiEntitiesRequest.make ~text
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.ContainsPiiEntitiesResponse.to_json)
           (Some Values.ContainsPiiEntitiesResponse.error_to_json)])
let create_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetType =
         flag "dataset-type" (optional json_arg) ~doc:"JSON DatasetType"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and flywheelArn =
         flag "flywheel-arn" (required string)
           ~doc:"STRING ComprehendFlywheelArn"
       and datasetName =
         flag "dataset-name" (required string)
           ~doc:"STRING ComprehendArnName"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON DatasetInputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset
           (Values.CreateDatasetRequest.make
              ?datasetType:(Option.map ~f:Values.DatasetType.of_json
                              datasetType) ?description ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~flywheelArn
              ~datasetName
              ~inputDataConfig:(Values.DatasetInputDataConfig.of_json
                                  inputDataConfig) ())
           (Some Values.CreateDatasetResponse.to_json)
           (Some Values.CreateDatasetResponse.error_to_json)])
let create_document_classifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionName =
         flag "version-name" (optional string) ~doc:"STRING VersionName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and outputDataConfig =
         flag "output-data-config" (optional json_arg)
           ~doc:"JSON DocumentClassifierOutputDataConfig"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and mode =
         flag "mode" (optional json_arg) ~doc:"JSON DocumentClassifierMode"
       and modelKmsKeyId =
         flag "model-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and modelPolicy =
         flag "model-policy" (optional string) ~doc:"STRING Policy"
       and documentClassifierName =
         flag "document-classifier-name" (required string)
           ~doc:"STRING ComprehendArnName"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON DocumentClassifierInputDataConfig"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_document_classifier
           (Values.CreateDocumentClassifierRequest.make ?versionName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?outputDataConfig:(Option.map
                                   ~f:Values.DocumentClassifierOutputDataConfig.of_json
                                   outputDataConfig) ?clientRequestToken
              ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?mode:(Option.map ~f:Values.DocumentClassifierMode.of_json mode)
              ?modelKmsKeyId ?modelPolicy ~documentClassifierName
              ~dataAccessRoleArn
              ~inputDataConfig:(Values.DocumentClassifierInputDataConfig.of_json
                                  inputDataConfig)
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.CreateDocumentClassifierResponse.to_json)
           (Some Values.CreateDocumentClassifierResponse.error_to_json)])
let create_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelArn =
         flag "model-arn" (optional string) ~doc:"STRING ComprehendModelArn"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (optional string)
           ~doc:"STRING IamRoleArn"
       and flywheelArn =
         flag "flywheel-arn" (optional string)
           ~doc:"STRING ComprehendFlywheelArn"
       and endpointName =
         flag "endpoint-name" (required string)
           ~doc:"STRING ComprehendEndpointName"
       and desiredInferenceUnits =
         flag "desired-inference-units" (required int)
           ~doc:"INT InferenceUnitsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_endpoint
           (Values.CreateEndpointRequest.make ?modelArn ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?dataAccessRoleArn ?flywheelArn ~endpointName
              ~desiredInferenceUnits ())
           (Some Values.CreateEndpointResponse.to_json)
           (Some Values.CreateEndpointResponse.error_to_json)])
let create_entity_recognizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionName =
         flag "version-name" (optional string) ~doc:"STRING VersionName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and modelKmsKeyId =
         flag "model-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and modelPolicy =
         flag "model-policy" (optional string) ~doc:"STRING Policy"
       and recognizerName =
         flag "recognizer-name" (required string)
           ~doc:"STRING ComprehendArnName"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON EntityRecognizerInputDataConfig"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_entity_recognizer
           (Values.CreateEntityRecognizerRequest.make ?versionName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientRequestToken ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?modelKmsKeyId ?modelPolicy ~recognizerName ~dataAccessRoleArn
              ~inputDataConfig:(Values.EntityRecognizerInputDataConfig.of_json
                                  inputDataConfig)
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.CreateEntityRecognizerResponse.to_json)
           (Some Values.CreateEntityRecognizerResponse.error_to_json)])
let create_flywheel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activeModelArn =
         flag "active-model-arn" (optional string)
           ~doc:"STRING ComprehendModelArn"
       and taskConfig =
         flag "task-config" (optional json_arg) ~doc:"JSON TaskConfig"
       and modelType =
         flag "model-type" (optional json_arg) ~doc:"JSON ModelType"
       and dataSecurityConfig =
         flag "data-security-config" (optional json_arg)
           ~doc:"JSON DataSecurityConfig"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and flywheelName =
         flag "flywheel-name" (required string)
           ~doc:"STRING ComprehendArnName"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and dataLakeS3Uri =
         flag "data-lake-s3-uri" (required string)
           ~doc:"STRING FlywheelS3Uri" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flywheel
           (Values.CreateFlywheelRequest.make ?activeModelArn
              ?taskConfig:(Option.map ~f:Values.TaskConfig.of_json taskConfig)
              ?modelType:(Option.map ~f:Values.ModelType.of_json modelType)
              ?dataSecurityConfig:(Option.map
                                     ~f:Values.DataSecurityConfig.of_json
                                     dataSecurityConfig) ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~flywheelName
              ~dataAccessRoleArn ~dataLakeS3Uri ())
           (Some Values.CreateFlywheelResponse.to_json)
           (Some Values.CreateFlywheelResponse.error_to_json)])
let delete_document_classifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentClassifierArn =
         flag "document-classifier-arn" (required string)
           ~doc:"STRING DocumentClassifierArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_document_classifier
           (Values.DeleteDocumentClassifierRequest.make
              ~documentClassifierArn ())
           (Some Values.DeleteDocumentClassifierResponse.to_json)
           (Some Values.DeleteDocumentClassifierResponse.error_to_json)])
let delete_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string)
           ~doc:"STRING ComprehendEndpointArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint
           (Values.DeleteEndpointRequest.make ~endpointArn ())
           (Some Values.DeleteEndpointResponse.to_json)
           (Some Values.DeleteEndpointResponse.error_to_json)])
let delete_entity_recognizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entityRecognizerArn =
         flag "entity-recognizer-arn" (required string)
           ~doc:"STRING EntityRecognizerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_entity_recognizer
           (Values.DeleteEntityRecognizerRequest.make ~entityRecognizerArn ())
           (Some Values.DeleteEntityRecognizerResponse.to_json)
           (Some Values.DeleteEntityRecognizerResponse.error_to_json)])
let delete_flywheel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flywheelArn =
         flag "flywheel-arn" (required string)
           ~doc:"STRING ComprehendFlywheelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flywheel
           (Values.DeleteFlywheelRequest.make ~flywheelArn ())
           (Some Values.DeleteFlywheelResponse.to_json)
           (Some Values.DeleteFlywheelResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyRevisionId =
         flag "policy-revision-id" (optional string)
           ~doc:"STRING PolicyRevisionId"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ComprehendModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ?policyRevisionId
              ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let describe_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetArn =
         flag "dataset-arn" (required string)
           ~doc:"STRING ComprehendDatasetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset
           (Values.DescribeDatasetRequest.make ~datasetArn ())
           (Some Values.DescribeDatasetResponse.to_json)
           (Some Values.DescribeDatasetResponse.error_to_json)])
let describe_document_classification_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_document_classification_job
           (Values.DescribeDocumentClassificationJobRequest.make ~jobId ())
           (Some Values.DescribeDocumentClassificationJobResponse.to_json)
           (Some
              Values.DescribeDocumentClassificationJobResponse.error_to_json)])
let describe_document_classifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentClassifierArn =
         flag "document-classifier-arn" (required string)
           ~doc:"STRING DocumentClassifierArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_document_classifier
           (Values.DescribeDocumentClassifierRequest.make
              ~documentClassifierArn ())
           (Some Values.DescribeDocumentClassifierResponse.to_json)
           (Some Values.DescribeDocumentClassifierResponse.error_to_json)])
let describe_dominant_language_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dominant_language_detection_job
           (Values.DescribeDominantLanguageDetectionJobRequest.make ~jobId ())
           (Some Values.DescribeDominantLanguageDetectionJobResponse.to_json)
           (Some
              Values.DescribeDominantLanguageDetectionJobResponse.error_to_json)])
let describe_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string)
           ~doc:"STRING ComprehendEndpointArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint
           (Values.DescribeEndpointRequest.make ~endpointArn ())
           (Some Values.DescribeEndpointResponse.to_json)
           (Some Values.DescribeEndpointResponse.error_to_json)])
let describe_entities_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entities_detection_job
           (Values.DescribeEntitiesDetectionJobRequest.make ~jobId ())
           (Some Values.DescribeEntitiesDetectionJobResponse.to_json)
           (Some Values.DescribeEntitiesDetectionJobResponse.error_to_json)])
let describe_entity_recognizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entityRecognizerArn =
         flag "entity-recognizer-arn" (required string)
           ~doc:"STRING EntityRecognizerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entity_recognizer
           (Values.DescribeEntityRecognizerRequest.make ~entityRecognizerArn
              ()) (Some Values.DescribeEntityRecognizerResponse.to_json)
           (Some Values.DescribeEntityRecognizerResponse.error_to_json)])
let describe_events_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events_detection_job
           (Values.DescribeEventsDetectionJobRequest.make ~jobId ())
           (Some Values.DescribeEventsDetectionJobResponse.to_json)
           (Some Values.DescribeEventsDetectionJobResponse.error_to_json)])
let describe_flywheel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flywheelArn =
         flag "flywheel-arn" (required string)
           ~doc:"STRING ComprehendFlywheelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flywheel
           (Values.DescribeFlywheelRequest.make ~flywheelArn ())
           (Some Values.DescribeFlywheelResponse.to_json)
           (Some Values.DescribeFlywheelResponse.error_to_json)])
let describe_flywheel_iteration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flywheelArn =
         flag "flywheel-arn" (required string)
           ~doc:"STRING ComprehendFlywheelArn"
       and flywheelIterationId =
         flag "flywheel-iteration-id" (required string)
           ~doc:"STRING FlywheelIterationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flywheel_iteration
           (Values.DescribeFlywheelIterationRequest.make ~flywheelArn
              ~flywheelIterationId ())
           (Some Values.DescribeFlywheelIterationResponse.to_json)
           (Some Values.DescribeFlywheelIterationResponse.error_to_json)])
let describe_key_phrases_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_key_phrases_detection_job
           (Values.DescribeKeyPhrasesDetectionJobRequest.make ~jobId ())
           (Some Values.DescribeKeyPhrasesDetectionJobResponse.to_json)
           (Some Values.DescribeKeyPhrasesDetectionJobResponse.error_to_json)])
let describe_pii_entities_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pii_entities_detection_job
           (Values.DescribePiiEntitiesDetectionJobRequest.make ~jobId ())
           (Some Values.DescribePiiEntitiesDetectionJobResponse.to_json)
           (Some Values.DescribePiiEntitiesDetectionJobResponse.error_to_json)])
let describe_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ComprehendModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resource_policy
           (Values.DescribeResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DescribeResourcePolicyResponse.to_json)
           (Some Values.DescribeResourcePolicyResponse.error_to_json)])
let describe_sentiment_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_sentiment_detection_job
           (Values.DescribeSentimentDetectionJobRequest.make ~jobId ())
           (Some Values.DescribeSentimentDetectionJobResponse.to_json)
           (Some Values.DescribeSentimentDetectionJobResponse.error_to_json)])
let describe_targeted_sentiment_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_targeted_sentiment_detection_job
           (Values.DescribeTargetedSentimentDetectionJobRequest.make ~jobId
              ())
           (Some Values.DescribeTargetedSentimentDetectionJobResponse.to_json)
           (Some
              Values.DescribeTargetedSentimentDetectionJobResponse.error_to_json)])
let describe_topics_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_topics_detection_job
           (Values.DescribeTopicsDetectionJobRequest.make ~jobId ())
           (Some Values.DescribeTopicsDetectionJobResponse.to_json)
           (Some Values.DescribeTopicsDetectionJobResponse.error_to_json)])
let detect_dominant_language =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string) ~doc:"STRING CustomerInputString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_dominant_language
           (Values.DetectDominantLanguageRequest.make ~text ())
           (Some Values.DetectDominantLanguageResponse.to_json)
           (Some Values.DetectDominantLanguageResponse.error_to_json)])
let detect_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (optional string) ~doc:"STRING CustomerInputString"
       and languageCode =
         flag "language-code" (optional json_arg) ~doc:"JSON LanguageCode"
       and endpointArn =
         flag "endpoint-arn" (optional string)
           ~doc:"STRING EntityRecognizerEndpointArn"
       and bytes =
         flag "bytes" (optional json_arg)
           ~doc:"JSON SemiStructuredDocumentBlob"
       and documentReaderConfig =
         flag "document-reader-config" (optional json_arg)
           ~doc:"JSON DocumentReaderConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_entities
           (Values.DetectEntitiesRequest.make ?text
              ?languageCode:(Option.map ~f:Values.LanguageCode.of_json
                               languageCode) ?endpointArn
              ?bytes:(Option.map ~f:Values.SemiStructuredDocumentBlob.of_json
                        bytes)
              ?documentReaderConfig:(Option.map
                                       ~f:Values.DocumentReaderConfig.of_json
                                       documentReaderConfig) ())
           (Some Values.DetectEntitiesResponse.to_json)
           (Some Values.DetectEntitiesResponse.error_to_json)])
let detect_key_phrases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string) ~doc:"STRING CustomerInputString"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_key_phrases
           (Values.DetectKeyPhrasesRequest.make ~text
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.DetectKeyPhrasesResponse.to_json)
           (Some Values.DetectKeyPhrasesResponse.error_to_json)])
let detect_pii_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text = flag "text" (required string) ~doc:"STRING String"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_pii_entities
           (Values.DetectPiiEntitiesRequest.make ~text
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.DetectPiiEntitiesResponse.to_json)
           (Some Values.DetectPiiEntitiesResponse.error_to_json)])
let detect_sentiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string) ~doc:"STRING CustomerInputString"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_sentiment
           (Values.DetectSentimentRequest.make ~text
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.DetectSentimentResponse.to_json)
           (Some Values.DetectSentimentResponse.error_to_json)])
let detect_syntax =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string) ~doc:"STRING CustomerInputString"
       and languageCode =
         flag "language-code" (required json_arg)
           ~doc:"JSON SyntaxLanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_syntax
           (Values.DetectSyntaxRequest.make ~text
              ~languageCode:(Values.SyntaxLanguageCode.of_json languageCode)
              ()) (Some Values.DetectSyntaxResponse.to_json)
           (Some Values.DetectSyntaxResponse.error_to_json)])
let detect_targeted_sentiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and text =
         flag "text" (required string) ~doc:"STRING CustomerInputString"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_targeted_sentiment
           (Values.DetectTargetedSentimentRequest.make ~text
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.DetectTargetedSentimentResponse.to_json)
           (Some Values.DetectTargetedSentimentResponse.error_to_json)])
let detect_toxic_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and textSegments =
         flag "text-segments" (required json_arg)
           ~doc:"JSON ListOfTextSegments"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detect_toxic_content
           (Values.DetectToxicContentRequest.make
              ~textSegments:(Values.ListOfTextSegments.of_json textSegments)
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.DetectToxicContentResponse.to_json)
           (Some Values.DetectToxicContentResponse.error_to_json)])
let import_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelName =
         flag "model-name" (optional string) ~doc:"STRING ComprehendArnName"
       and versionName =
         flag "version-name" (optional string) ~doc:"STRING VersionName"
       and modelKmsKeyId =
         flag "model-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (optional string)
           ~doc:"STRING IamRoleArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sourceModelArn =
         flag "source-model-arn" (required string)
           ~doc:"STRING ComprehendModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_model
           (Values.ImportModelRequest.make ?modelName ?versionName
              ?modelKmsKeyId ?dataAccessRoleArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sourceModelArn ()) (Some Values.ImportModelResponse.to_json)
           (Some Values.ImportModelResponse.error_to_json)])
let list_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flywheelArn =
         flag "flywheel-arn" (optional string)
           ~doc:"STRING ComprehendFlywheelArn"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON DatasetFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datasets
           (Values.ListDatasetsRequest.make ?flywheelArn
              ?filter:(Option.map ~f:Values.DatasetFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListDatasetsResponse.to_json)
           (Some Values.ListDatasetsResponse.error_to_json)])
let list_document_classification_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON DocumentClassificationJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_document_classification_jobs
           (Values.ListDocumentClassificationJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.DocumentClassificationJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListDocumentClassificationJobsResponse.to_json)
           (Some Values.ListDocumentClassificationJobsResponse.error_to_json)])
let list_document_classifier_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_document_classifier_summaries
           (Values.ListDocumentClassifierSummariesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListDocumentClassifierSummariesResponse.to_json)
           (Some Values.ListDocumentClassifierSummariesResponse.error_to_json)])
let list_document_classifiers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON DocumentClassifierFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_document_classifiers
           (Values.ListDocumentClassifiersRequest.make
              ?filter:(Option.map ~f:Values.DocumentClassifierFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListDocumentClassifiersResponse.to_json)
           (Some Values.ListDocumentClassifiersResponse.error_to_json)])
let list_dominant_language_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON DominantLanguageDetectionJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dominant_language_detection_jobs
           (Values.ListDominantLanguageDetectionJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.DominantLanguageDetectionJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListDominantLanguageDetectionJobsResponse.to_json)
           (Some
              Values.ListDominantLanguageDetectionJobsResponse.error_to_json)])
let list_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON EndpointFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_endpoints
           (Values.ListEndpointsRequest.make
              ?filter:(Option.map ~f:Values.EndpointFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListEndpointsResponse.to_json)
           (Some Values.ListEndpointsResponse.error_to_json)])
let list_entities_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON EntitiesDetectionJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entities_detection_jobs
           (Values.ListEntitiesDetectionJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.EntitiesDetectionJobFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListEntitiesDetectionJobsResponse.to_json)
           (Some Values.ListEntitiesDetectionJobsResponse.error_to_json)])
let list_entity_recognizer_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entity_recognizer_summaries
           (Values.ListEntityRecognizerSummariesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListEntityRecognizerSummariesResponse.to_json)
           (Some Values.ListEntityRecognizerSummariesResponse.error_to_json)])
let list_entity_recognizers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON EntityRecognizerFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entity_recognizers
           (Values.ListEntityRecognizersRequest.make
              ?filter:(Option.map ~f:Values.EntityRecognizerFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListEntityRecognizersResponse.to_json)
           (Some Values.ListEntityRecognizersResponse.error_to_json)])
let list_events_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON EventsDetectionJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_events_detection_jobs
           (Values.ListEventsDetectionJobsRequest.make
              ?filter:(Option.map ~f:Values.EventsDetectionJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListEventsDetectionJobsResponse.to_json)
           (Some Values.ListEventsDetectionJobsResponse.error_to_json)])
let list_flywheel_iteration_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON FlywheelIterationFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger"
       and flywheelArn =
         flag "flywheel-arn" (required string)
           ~doc:"STRING ComprehendFlywheelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_flywheel_iteration_history
           (Values.ListFlywheelIterationHistoryRequest.make
              ?filter:(Option.map ~f:Values.FlywheelIterationFilter.of_json
                         filter) ?nextToken ?maxResults ~flywheelArn ())
           (Some Values.ListFlywheelIterationHistoryResponse.to_json)
           (Some Values.ListFlywheelIterationHistoryResponse.error_to_json)])
let list_flywheels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON FlywheelFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_flywheels
           (Values.ListFlywheelsRequest.make
              ?filter:(Option.map ~f:Values.FlywheelFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListFlywheelsResponse.to_json)
           (Some Values.ListFlywheelsResponse.error_to_json)])
let list_key_phrases_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON KeyPhrasesDetectionJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_key_phrases_detection_jobs
           (Values.ListKeyPhrasesDetectionJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.KeyPhrasesDetectionJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListKeyPhrasesDetectionJobsResponse.to_json)
           (Some Values.ListKeyPhrasesDetectionJobsResponse.error_to_json)])
let list_pii_entities_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON PiiEntitiesDetectionJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pii_entities_detection_jobs
           (Values.ListPiiEntitiesDetectionJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.PiiEntitiesDetectionJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListPiiEntitiesDetectionJobsResponse.to_json)
           (Some Values.ListPiiEntitiesDetectionJobsResponse.error_to_json)])
let list_sentiment_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON SentimentDetectionJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sentiment_detection_jobs
           (Values.ListSentimentDetectionJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.SentimentDetectionJobFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListSentimentDetectionJobsResponse.to_json)
           (Some Values.ListSentimentDetectionJobsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ComprehendArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_targeted_sentiment_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON TargetedSentimentDetectionJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_targeted_sentiment_detection_jobs
           (Values.ListTargetedSentimentDetectionJobsRequest.make
              ?filter:(Option.map
                         ~f:Values.TargetedSentimentDetectionJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListTargetedSentimentDetectionJobsResponse.to_json)
           (Some
              Values.ListTargetedSentimentDetectionJobsResponse.error_to_json)])
let list_topics_detection_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON TopicsDetectionJobFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_topics_detection_jobs
           (Values.ListTopicsDetectionJobsRequest.make
              ?filter:(Option.map ~f:Values.TopicsDetectionJobFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListTopicsDetectionJobsResponse.to_json)
           (Some Values.ListTopicsDetectionJobsResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyRevisionId =
         flag "policy-revision-id" (optional string)
           ~doc:"STRING PolicyRevisionId"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ComprehendModelArn"
       and resourcePolicy =
         flag "resource-policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ?policyRevisionId
              ~resourceArn ~resourcePolicy ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let start_document_classification_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and documentClassifierArn =
         flag "document-classifier-arn" (optional string)
           ~doc:"STRING DocumentClassifierArn"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and flywheelArn =
         flag "flywheel-arn" (optional string)
           ~doc:"STRING ComprehendFlywheelArn"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_document_classification_job
           (Values.StartDocumentClassificationJobRequest.make ?jobName
              ?documentClassifierArn ?clientRequestToken ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?flywheelArn
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn ())
           (Some Values.StartDocumentClassificationJobResponse.to_json)
           (Some Values.StartDocumentClassificationJobResponse.error_to_json)])
let start_dominant_language_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_dominant_language_detection_job
           (Values.StartDominantLanguageDetectionJobRequest.make ?jobName
              ?clientRequestToken ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn ())
           (Some Values.StartDominantLanguageDetectionJobResponse.to_json)
           (Some
              Values.StartDominantLanguageDetectionJobResponse.error_to_json)])
let start_entities_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and entityRecognizerArn =
         flag "entity-recognizer-arn" (optional string)
           ~doc:"STRING EntityRecognizerArn"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and flywheelArn =
         flag "flywheel-arn" (optional string)
           ~doc:"STRING ComprehendFlywheelArn"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_entities_detection_job
           (Values.StartEntitiesDetectionJobRequest.make ?jobName
              ?entityRecognizerArn ?clientRequestToken ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?flywheelArn
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartEntitiesDetectionJobResponse.to_json)
           (Some Values.StartEntitiesDetectionJobResponse.error_to_json)])
let start_events_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode"
       and targetEventTypes =
         flag "target-event-types" (required json_arg)
           ~doc:"JSON TargetEventTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_events_detection_job
           (Values.StartEventsDetectionJobRequest.make ?jobName
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode)
              ~targetEventTypes:(Values.TargetEventTypes.of_json
                                   targetEventTypes) ())
           (Some Values.StartEventsDetectionJobResponse.to_json)
           (Some Values.StartEventsDetectionJobResponse.error_to_json)])
let start_flywheel_iteration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and flywheelArn =
         flag "flywheel-arn" (required string)
           ~doc:"STRING ComprehendFlywheelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_flywheel_iteration
           (Values.StartFlywheelIterationRequest.make ?clientRequestToken
              ~flywheelArn ())
           (Some Values.StartFlywheelIterationResponse.to_json)
           (Some Values.StartFlywheelIterationResponse.error_to_json)])
let start_key_phrases_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_key_phrases_detection_job
           (Values.StartKeyPhrasesDetectionJobRequest.make ?jobName
              ?clientRequestToken ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartKeyPhrasesDetectionJobResponse.to_json)
           (Some Values.StartKeyPhrasesDetectionJobResponse.error_to_json)])
let start_pii_entities_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and redactionConfig =
         flag "redaction-config" (optional json_arg)
           ~doc:"JSON RedactionConfig"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and mode =
         flag "mode" (required json_arg) ~doc:"JSON PiiEntitiesDetectionMode"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_pii_entities_detection_job
           (Values.StartPiiEntitiesDetectionJobRequest.make
              ?redactionConfig:(Option.map ~f:Values.RedactionConfig.of_json
                                  redactionConfig) ?jobName
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig)
              ~mode:(Values.PiiEntitiesDetectionMode.of_json mode)
              ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartPiiEntitiesDetectionJobResponse.to_json)
           (Some Values.StartPiiEntitiesDetectionJobResponse.error_to_json)])
let start_sentiment_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_sentiment_detection_job
           (Values.StartSentimentDetectionJobRequest.make ?jobName
              ?clientRequestToken ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartSentimentDetectionJobResponse.to_json)
           (Some Values.StartSentimentDetectionJobResponse.error_to_json)])
let start_targeted_sentiment_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_targeted_sentiment_detection_job
           (Values.StartTargetedSentimentDetectionJobRequest.make ?jobName
              ?clientRequestToken ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.StartTargetedSentimentDetectionJobResponse.to_json)
           (Some
              Values.StartTargetedSentimentDetectionJobResponse.error_to_json)])
let start_topics_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and numberOfTopics =
         flag "number-of-topics" (optional int)
           ~doc:"INT NumberOfTopicsInteger"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestTokenString"
       and volumeKmsKeyId =
         flag "volume-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_topics_detection_job
           (Values.StartTopicsDetectionJobRequest.make ?jobName
              ?numberOfTopics ?clientRequestToken ?volumeKmsKeyId
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~dataAccessRoleArn ())
           (Some Values.StartTopicsDetectionJobResponse.to_json)
           (Some Values.StartTopicsDetectionJobResponse.error_to_json)])
let stop_dominant_language_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_dominant_language_detection_job
           (Values.StopDominantLanguageDetectionJobRequest.make ~jobId ())
           (Some Values.StopDominantLanguageDetectionJobResponse.to_json)
           (Some
              Values.StopDominantLanguageDetectionJobResponse.error_to_json)])
let stop_entities_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_entities_detection_job
           (Values.StopEntitiesDetectionJobRequest.make ~jobId ())
           (Some Values.StopEntitiesDetectionJobResponse.to_json)
           (Some Values.StopEntitiesDetectionJobResponse.error_to_json)])
let stop_events_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_events_detection_job
           (Values.StopEventsDetectionJobRequest.make ~jobId ())
           (Some Values.StopEventsDetectionJobResponse.to_json)
           (Some Values.StopEventsDetectionJobResponse.error_to_json)])
let stop_key_phrases_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_key_phrases_detection_job
           (Values.StopKeyPhrasesDetectionJobRequest.make ~jobId ())
           (Some Values.StopKeyPhrasesDetectionJobResponse.to_json)
           (Some Values.StopKeyPhrasesDetectionJobResponse.error_to_json)])
let stop_pii_entities_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_pii_entities_detection_job
           (Values.StopPiiEntitiesDetectionJobRequest.make ~jobId ())
           (Some Values.StopPiiEntitiesDetectionJobResponse.to_json)
           (Some Values.StopPiiEntitiesDetectionJobResponse.error_to_json)])
let stop_sentiment_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_sentiment_detection_job
           (Values.StopSentimentDetectionJobRequest.make ~jobId ())
           (Some Values.StopSentimentDetectionJobResponse.to_json)
           (Some Values.StopSentimentDetectionJobResponse.error_to_json)])
let stop_targeted_sentiment_detection_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_targeted_sentiment_detection_job
           (Values.StopTargetedSentimentDetectionJobRequest.make ~jobId ())
           (Some Values.StopTargetedSentimentDetectionJobResponse.to_json)
           (Some
              Values.StopTargetedSentimentDetectionJobResponse.error_to_json)])
let stop_training_document_classifier =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentClassifierArn =
         flag "document-classifier-arn" (required string)
           ~doc:"STRING DocumentClassifierArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_training_document_classifier
           (Values.StopTrainingDocumentClassifierRequest.make
              ~documentClassifierArn ())
           (Some Values.StopTrainingDocumentClassifierResponse.to_json)
           (Some Values.StopTrainingDocumentClassifierResponse.error_to_json)])
let stop_training_entity_recognizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entityRecognizerArn =
         flag "entity-recognizer-arn" (required string)
           ~doc:"STRING EntityRecognizerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_training_entity_recognizer
           (Values.StopTrainingEntityRecognizerRequest.make
              ~entityRecognizerArn ())
           (Some Values.StopTrainingEntityRecognizerResponse.to_json)
           (Some Values.StopTrainingEntityRecognizerResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ComprehendArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ComprehendArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and desiredModelArn =
         flag "desired-model-arn" (optional string)
           ~doc:"STRING ComprehendModelArn"
       and desiredInferenceUnits =
         flag "desired-inference-units" (optional int)
           ~doc:"INT InferenceUnitsInteger"
       and desiredDataAccessRoleArn =
         flag "desired-data-access-role-arn" (optional string)
           ~doc:"STRING IamRoleArn"
       and flywheelArn =
         flag "flywheel-arn" (optional string)
           ~doc:"STRING ComprehendFlywheelArn"
       and endpointArn =
         flag "endpoint-arn" (required string)
           ~doc:"STRING ComprehendEndpointArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_endpoint
           (Values.UpdateEndpointRequest.make ?desiredModelArn
              ?desiredInferenceUnits ?desiredDataAccessRoleArn ?flywheelArn
              ~endpointArn ()) (Some Values.UpdateEndpointResponse.to_json)
           (Some Values.UpdateEndpointResponse.error_to_json)])
let update_flywheel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activeModelArn =
         flag "active-model-arn" (optional string)
           ~doc:"STRING ComprehendModelArn"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (optional string)
           ~doc:"STRING IamRoleArn"
       and dataSecurityConfig =
         flag "data-security-config" (optional json_arg)
           ~doc:"JSON UpdateDataSecurityConfig"
       and flywheelArn =
         flag "flywheel-arn" (required string)
           ~doc:"STRING ComprehendFlywheelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flywheel
           (Values.UpdateFlywheelRequest.make ?activeModelArn
              ?dataAccessRoleArn
              ?dataSecurityConfig:(Option.map
                                     ~f:Values.UpdateDataSecurityConfig.of_json
                                     dataSecurityConfig) ~flywheelArn ())
           (Some Values.UpdateFlywheelResponse.to_json)
           (Some Values.UpdateFlywheelResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-detect-dominant-language", batch_detect_dominant_language);
    ("batch-detect-entities", batch_detect_entities);
    ("batch-detect-key-phrases", batch_detect_key_phrases);
    ("batch-detect-sentiment", batch_detect_sentiment);
    ("batch-detect-syntax", batch_detect_syntax);
    ("batch-detect-targeted-sentiment", batch_detect_targeted_sentiment);
    ("classify-document", classify_document);
    ("contains-pii-entities", contains_pii_entities);
    ("create-dataset", create_dataset);
    ("create-document-classifier", create_document_classifier);
    ("create-endpoint", create_endpoint);
    ("create-entity-recognizer", create_entity_recognizer);
    ("create-flywheel", create_flywheel);
    ("delete-document-classifier", delete_document_classifier);
    ("delete-endpoint", delete_endpoint);
    ("delete-entity-recognizer", delete_entity_recognizer);
    ("delete-flywheel", delete_flywheel);
    ("delete-resource-policy", delete_resource_policy);
    ("describe-dataset", describe_dataset);
    ("describe-document-classification-job",
      describe_document_classification_job);
    ("describe-document-classifier", describe_document_classifier);
    ("describe-dominant-language-detection-job",
      describe_dominant_language_detection_job);
    ("describe-endpoint", describe_endpoint);
    ("describe-entities-detection-job", describe_entities_detection_job);
    ("describe-entity-recognizer", describe_entity_recognizer);
    ("describe-events-detection-job", describe_events_detection_job);
    ("describe-flywheel", describe_flywheel);
    ("describe-flywheel-iteration", describe_flywheel_iteration);
    ("describe-key-phrases-detection-job",
      describe_key_phrases_detection_job);
    ("describe-pii-entities-detection-job",
      describe_pii_entities_detection_job);
    ("describe-resource-policy", describe_resource_policy);
    ("describe-sentiment-detection-job", describe_sentiment_detection_job);
    ("describe-targeted-sentiment-detection-job",
      describe_targeted_sentiment_detection_job);
    ("describe-topics-detection-job", describe_topics_detection_job);
    ("detect-dominant-language", detect_dominant_language);
    ("detect-entities", detect_entities);
    ("detect-key-phrases", detect_key_phrases);
    ("detect-pii-entities", detect_pii_entities);
    ("detect-sentiment", detect_sentiment);
    ("detect-syntax", detect_syntax);
    ("detect-targeted-sentiment", detect_targeted_sentiment);
    ("detect-toxic-content", detect_toxic_content);
    ("import-model", import_model);
    ("list-datasets", list_datasets);
    ("list-document-classification-jobs", list_document_classification_jobs);
    ("list-document-classifier-summaries",
      list_document_classifier_summaries);
    ("list-document-classifiers", list_document_classifiers);
    ("list-dominant-language-detection-jobs",
      list_dominant_language_detection_jobs);
    ("list-endpoints", list_endpoints);
    ("list-entities-detection-jobs", list_entities_detection_jobs);
    ("list-entity-recognizer-summaries", list_entity_recognizer_summaries);
    ("list-entity-recognizers", list_entity_recognizers);
    ("list-events-detection-jobs", list_events_detection_jobs);
    ("list-flywheel-iteration-history", list_flywheel_iteration_history);
    ("list-flywheels", list_flywheels);
    ("list-key-phrases-detection-jobs", list_key_phrases_detection_jobs);
    ("list-pii-entities-detection-jobs", list_pii_entities_detection_jobs);
    ("list-sentiment-detection-jobs", list_sentiment_detection_jobs);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-targeted-sentiment-detection-jobs",
      list_targeted_sentiment_detection_jobs);
    ("list-topics-detection-jobs", list_topics_detection_jobs);
    ("put-resource-policy", put_resource_policy);
    ("start-document-classification-job", start_document_classification_job);
    ("start-dominant-language-detection-job",
      start_dominant_language_detection_job);
    ("start-entities-detection-job", start_entities_detection_job);
    ("start-events-detection-job", start_events_detection_job);
    ("start-flywheel-iteration", start_flywheel_iteration);
    ("start-key-phrases-detection-job", start_key_phrases_detection_job);
    ("start-pii-entities-detection-job", start_pii_entities_detection_job);
    ("start-sentiment-detection-job", start_sentiment_detection_job);
    ("start-targeted-sentiment-detection-job",
      start_targeted_sentiment_detection_job);
    ("start-topics-detection-job", start_topics_detection_job);
    ("stop-dominant-language-detection-job",
      stop_dominant_language_detection_job);
    ("stop-entities-detection-job", stop_entities_detection_job);
    ("stop-events-detection-job", stop_events_detection_job);
    ("stop-key-phrases-detection-job", stop_key_phrases_detection_job);
    ("stop-pii-entities-detection-job", stop_pii_entities_detection_job);
    ("stop-sentiment-detection-job", stop_sentiment_detection_job);
    ("stop-targeted-sentiment-detection-job",
      stop_targeted_sentiment_detection_job);
    ("stop-training-document-classifier", stop_training_document_classifier);
    ("stop-training-entity-recognizer", stop_training_entity_recognizer);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-endpoint", update_endpoint);
    ("update-flywheel", update_flywheel)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_notification_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and config =
         flag "config" (required json_arg)
           ~doc:"JSON NotificationChannelConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_notification_channel
           (Values.AddNotificationChannelRequest.make
              ~config:(Values.NotificationChannelConfig.of_json config) ())
           (Some Values.AddNotificationChannelResponse.to_json)
           (Some Values.AddNotificationChannelResponse.error_to_json)])
let delete_insight =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING InsightId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_insight (Values.DeleteInsightRequest.make ~id ())
           (Some Values.DeleteInsightResponse.to_json)
           (Some Values.DeleteInsightResponse.error_to_json)])
let describe_account_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_health
           (Values.DescribeAccountHealthRequest.make ())
           (Some Values.DescribeAccountHealthResponse.to_json)
           (Some Values.DescribeAccountHealthResponse.error_to_json)])
let describe_account_overview =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and toTime = flag "to-time" (optional json_arg) ~doc:"JSON Timestamp"
       and fromTime =
         flag "from-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_overview
           (Values.DescribeAccountOverviewRequest.make
              ?toTime:(Option.map ~f:Values.Timestamp.of_json toTime)
              ~fromTime:(Values.Timestamp.of_json fromTime) ())
           (Some Values.DescribeAccountOverviewResponse.to_json)
           (Some Values.DescribeAccountOverviewResponse.error_to_json)])
let describe_anomaly =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AwsAccountId"
       and id = flag "id" (required string) ~doc:"STRING AnomalyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_anomaly
           (Values.DescribeAnomalyRequest.make ?accountId ~id ())
           (Some Values.DescribeAnomalyResponse.to_json)
           (Some Values.DescribeAnomalyResponse.error_to_json)])
let describe_event_sources_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_sources_config
           (Values.DescribeEventSourcesConfigRequest.make ())
           (Some Values.DescribeEventSourcesConfigResponse.to_json)
           (Some Values.DescribeEventSourcesConfigResponse.error_to_json)])
let describe_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and insightId =
         flag "insight-id" (optional string) ~doc:"STRING InsightId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_feedback
           (Values.DescribeFeedbackRequest.make ?insightId ())
           (Some Values.DescribeFeedbackResponse.to_json)
           (Some Values.DescribeFeedbackResponse.error_to_json)])
let describe_insight =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AwsAccountId"
       and id = flag "id" (required string) ~doc:"STRING InsightId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_insight
           (Values.DescribeInsightRequest.make ?accountId ~id ())
           (Some Values.DescribeInsightResponse.to_json)
           (Some Values.DescribeInsightResponse.error_to_json)])
let describe_organization_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIdList"
       and organizationalUnitIds =
         flag "organizational-unit-ids" (optional json_arg)
           ~doc:"JSON OrganizationalUnitIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_organization_health
           (Values.DescribeOrganizationHealthRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIdList.of_json
                             accountIds)
              ?organizationalUnitIds:(Option.map
                                        ~f:Values.OrganizationalUnitIdList.of_json
                                        organizationalUnitIds) ())
           (Some Values.DescribeOrganizationHealthResponse.to_json)
           (Some Values.DescribeOrganizationHealthResponse.error_to_json)])
let describe_organization_overview =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and toTime = flag "to-time" (optional json_arg) ~doc:"JSON Timestamp"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIdList"
       and organizationalUnitIds =
         flag "organizational-unit-ids" (optional json_arg)
           ~doc:"JSON OrganizationalUnitIdList"
       and fromTime =
         flag "from-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_organization_overview
           (Values.DescribeOrganizationOverviewRequest.make
              ?toTime:(Option.map ~f:Values.Timestamp.of_json toTime)
              ?accountIds:(Option.map ~f:Values.AccountIdList.of_json
                             accountIds)
              ?organizationalUnitIds:(Option.map
                                        ~f:Values.OrganizationalUnitIdList.of_json
                                        organizationalUnitIds)
              ~fromTime:(Values.Timestamp.of_json fromTime) ())
           (Some Values.DescribeOrganizationOverviewResponse.to_json)
           (Some Values.DescribeOrganizationOverviewResponse.error_to_json)])
let describe_organization_resource_collection_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIdList"
       and organizationalUnitIds =
         flag "organizational-unit-ids" (optional json_arg)
           ~doc:"JSON OrganizationalUnitIdList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT OrganizationResourceCollectionMaxResults"
       and organizationResourceCollectionType =
         flag "organization-resource-collection-type" (required json_arg)
           ~doc:"JSON OrganizationResourceCollectionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_organization_resource_collection_health
           (Values.DescribeOrganizationResourceCollectionHealthRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIdList.of_json
                             accountIds)
              ?organizationalUnitIds:(Option.map
                                        ~f:Values.OrganizationalUnitIdList.of_json
                                        organizationalUnitIds) ?nextToken
              ?maxResults
              ~organizationResourceCollectionType:(Values.OrganizationResourceCollectionType.of_json
                                                     organizationResourceCollectionType)
              ())
           (Some
              Values.DescribeOrganizationResourceCollectionHealthResponse.to_json)
           (Some
              Values.DescribeOrganizationResourceCollectionHealthResponse.error_to_json)])
let describe_resource_collection_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and resourceCollectionType =
         flag "resource-collection-type" (required json_arg)
           ~doc:"JSON ResourceCollectionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resource_collection_health
           (Values.DescribeResourceCollectionHealthRequest.make ?nextToken
              ~resourceCollectionType:(Values.ResourceCollectionType.of_json
                                         resourceCollectionType) ())
           (Some Values.DescribeResourceCollectionHealthResponse.to_json)
           (Some
              Values.DescribeResourceCollectionHealthResponse.error_to_json)])
let describe_service_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_service_integration
           (Values.DescribeServiceIntegrationRequest.make ())
           (Some Values.DescribeServiceIntegrationResponse.to_json)
           (Some Values.DescribeServiceIntegrationResponse.error_to_json)])
let get_cost_estimation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cost_estimation
           (Values.GetCostEstimationRequest.make ?nextToken ())
           (Some Values.GetCostEstimationResponse.to_json)
           (Some Values.GetCostEstimationResponse.error_to_json)])
let get_resource_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and resourceCollectionType =
         flag "resource-collection-type" (required json_arg)
           ~doc:"JSON ResourceCollectionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_collection
           (Values.GetResourceCollectionRequest.make ?nextToken
              ~resourceCollectionType:(Values.ResourceCollectionType.of_json
                                         resourceCollectionType) ())
           (Some Values.GetResourceCollectionResponse.to_json)
           (Some Values.GetResourceCollectionResponse.error_to_json)])
let list_anomalies_for_insight =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTimeRange =
         flag "start-time-range" (optional json_arg)
           ~doc:"JSON StartTimeRange"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAnomaliesForInsightMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AwsAccountId"
       and insightId =
         flag "insight-id" (required string) ~doc:"STRING InsightId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_anomalies_for_insight
           (Values.ListAnomaliesForInsightRequest.make
              ?startTimeRange:(Option.map ~f:Values.StartTimeRange.of_json
                                 startTimeRange) ?maxResults ?nextToken
              ?accountId ~insightId ())
           (Some Values.ListAnomaliesForInsightResponse.to_json)
           (Some Values.ListAnomaliesForInsightResponse.error_to_json)])
let list_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListEventsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AwsAccountId"
       and filters =
         flag "filters" (required json_arg) ~doc:"JSON ListEventsFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_events
           (Values.ListEventsRequest.make ?maxResults ?nextToken ?accountId
              ~filters:(Values.ListEventsFilters.of_json filters) ())
           (Some Values.ListEventsResponse.to_json)
           (Some Values.ListEventsResponse.error_to_json)])
let list_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListInsightsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and statusFilter =
         flag "status-filter" (required json_arg)
           ~doc:"JSON ListInsightsStatusFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_insights
           (Values.ListInsightsRequest.make ?maxResults ?nextToken
              ~statusFilter:(Values.ListInsightsStatusFilter.of_json
                               statusFilter) ())
           (Some Values.ListInsightsResponse.to_json)
           (Some Values.ListInsightsResponse.error_to_json)])
let list_notification_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notification_channels
           (Values.ListNotificationChannelsRequest.make ?nextToken ())
           (Some Values.ListNotificationChannelsResponse.to_json)
           (Some Values.ListNotificationChannelsResponse.error_to_json)])
let list_organization_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListInsightsMaxResults"
       and accountIds =
         flag "account-ids" (optional json_arg)
           ~doc:"JSON ListInsightsAccountIdList"
       and organizationalUnitIds =
         flag "organizational-unit-ids" (optional json_arg)
           ~doc:"JSON ListInsightsOrganizationalUnitIdList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and statusFilter =
         flag "status-filter" (required json_arg)
           ~doc:"JSON ListInsightsStatusFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organization_insights
           (Values.ListOrganizationInsightsRequest.make ?maxResults
              ?accountIds:(Option.map
                             ~f:Values.ListInsightsAccountIdList.of_json
                             accountIds)
              ?organizationalUnitIds:(Option.map
                                        ~f:Values.ListInsightsOrganizationalUnitIdList.of_json
                                        organizationalUnitIds) ?nextToken
              ~statusFilter:(Values.ListInsightsStatusFilter.of_json
                               statusFilter) ())
           (Some Values.ListOrganizationInsightsResponse.to_json)
           (Some Values.ListOrganizationInsightsResponse.error_to_json)])
let list_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON Locale"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AwsAccountId"
       and insightId =
         flag "insight-id" (required string) ~doc:"STRING InsightId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendations
           (Values.ListRecommendationsRequest.make ?nextToken
              ?locale:(Option.map ~f:Values.Locale.of_json locale) ?accountId
              ~insightId ())
           (Some Values.ListRecommendationsResponse.to_json)
           (Some Values.ListRecommendationsResponse.error_to_json)])
let put_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and insightFeedback =
         flag "insight-feedback" (optional json_arg)
           ~doc:"JSON InsightFeedback" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_feedback
           (Values.PutFeedbackRequest.make
              ?insightFeedback:(Option.map ~f:Values.InsightFeedback.of_json
                                  insightFeedback) ())
           (Some Values.PutFeedbackResponse.to_json)
           (Some Values.PutFeedbackResponse.error_to_json)])
let remove_notification_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING NotificationChannelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_notification_channel
           (Values.RemoveNotificationChannelRequest.make ~id ())
           (Some Values.RemoveNotificationChannelResponse.to_json)
           (Some Values.RemoveNotificationChannelResponse.error_to_json)])
let search_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SearchInsightsFilters"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT SearchInsightsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and startTimeRange =
         flag "start-time-range" (required json_arg)
           ~doc:"JSON StartTimeRange"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON InsightType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_insights
           (Values.SearchInsightsRequest.make
              ?filters:(Option.map ~f:Values.SearchInsightsFilters.of_json
                          filters) ?maxResults ?nextToken
              ~startTimeRange:(Values.StartTimeRange.of_json startTimeRange)
              ~type_:(Values.InsightType.of_json type_) ())
           (Some Values.SearchInsightsResponse.to_json)
           (Some Values.SearchInsightsResponse.error_to_json)])
let search_organization_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON SearchOrganizationInsightsFilters"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT SearchOrganizationInsightsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING UuidNextToken"
       and accountIds =
         flag "account-ids" (required json_arg)
           ~doc:"JSON SearchInsightsAccountIdList"
       and startTimeRange =
         flag "start-time-range" (required json_arg)
           ~doc:"JSON StartTimeRange"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON InsightType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_organization_insights
           (Values.SearchOrganizationInsightsRequest.make
              ?filters:(Option.map
                          ~f:Values.SearchOrganizationInsightsFilters.of_json
                          filters) ?maxResults ?nextToken
              ~accountIds:(Values.SearchInsightsAccountIdList.of_json
                             accountIds)
              ~startTimeRange:(Values.StartTimeRange.of_json startTimeRange)
              ~type_:(Values.InsightType.of_json type_) ())
           (Some Values.SearchOrganizationInsightsResponse.to_json)
           (Some Values.SearchOrganizationInsightsResponse.error_to_json)])
let start_cost_estimation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and resourceCollection =
         flag "resource-collection" (required json_arg)
           ~doc:"JSON CostEstimationResourceCollectionFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_cost_estimation
           (Values.StartCostEstimationRequest.make ?clientToken
              ~resourceCollection:(Values.CostEstimationResourceCollectionFilter.of_json
                                     resourceCollection) ())
           (Some Values.StartCostEstimationResponse.to_json)
           (Some Values.StartCostEstimationResponse.error_to_json)])
let update_event_sources_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventSources =
         flag "event-sources" (optional json_arg)
           ~doc:"JSON EventSourcesConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_sources_config
           (Values.UpdateEventSourcesConfigRequest.make
              ?eventSources:(Option.map ~f:Values.EventSourcesConfig.of_json
                               eventSources) ())
           (Some Values.UpdateEventSourcesConfigResponse.to_json)
           (Some Values.UpdateEventSourcesConfigResponse.error_to_json)])
let update_resource_collection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and action =
         flag "action" (required json_arg)
           ~doc:"JSON UpdateResourceCollectionAction"
       and resourceCollection =
         flag "resource-collection" (required json_arg)
           ~doc:"JSON UpdateResourceCollectionFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_collection
           (Values.UpdateResourceCollectionRequest.make
              ~action:(Values.UpdateResourceCollectionAction.of_json action)
              ~resourceCollection:(Values.UpdateResourceCollectionFilter.of_json
                                     resourceCollection) ())
           (Some Values.UpdateResourceCollectionResponse.to_json)
           (Some Values.UpdateResourceCollectionResponse.error_to_json)])
let update_service_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceIntegration =
         flag "service-integration" (required json_arg)
           ~doc:"JSON UpdateServiceIntegrationConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_integration
           (Values.UpdateServiceIntegrationRequest.make
              ~serviceIntegration:(Values.UpdateServiceIntegrationConfig.of_json
                                     serviceIntegration) ())
           (Some Values.UpdateServiceIntegrationResponse.to_json)
           (Some Values.UpdateServiceIntegrationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-notification-channel", add_notification_channel);
    ("delete-insight", delete_insight);
    ("describe-account-health", describe_account_health);
    ("describe-account-overview", describe_account_overview);
    ("describe-anomaly", describe_anomaly);
    ("describe-event-sources-config", describe_event_sources_config);
    ("describe-feedback", describe_feedback);
    ("describe-insight", describe_insight);
    ("describe-organization-health", describe_organization_health);
    ("describe-organization-overview", describe_organization_overview);
    ("describe-organization-resource-collection-health",
      describe_organization_resource_collection_health);
    ("describe-resource-collection-health",
      describe_resource_collection_health);
    ("describe-service-integration", describe_service_integration);
    ("get-cost-estimation", get_cost_estimation);
    ("get-resource-collection", get_resource_collection);
    ("list-anomalies-for-insight", list_anomalies_for_insight);
    ("list-events", list_events);
    ("list-insights", list_insights);
    ("list-notification-channels", list_notification_channels);
    ("list-organization-insights", list_organization_insights);
    ("list-recommendations", list_recommendations);
    ("put-feedback", put_feedback);
    ("remove-notification-channel", remove_notification_channel);
    ("search-insights", search_insights);
    ("search-organization-insights", search_organization_insights);
    ("start-cost-estimation", start_cost_estimation);
    ("update-event-sources-config", update_event_sources_config);
    ("update-resource-collection", update_resource_collection);
    ("update-service-integration", update_service_integration)]
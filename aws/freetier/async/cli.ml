(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_account_activity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and languageCode =
         flag "language-code" (optional json_arg) ~doc:"JSON LanguageCode"
       and activityId =
         flag "activity-id" (required string) ~doc:"STRING ActivityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_activity
           (Values.GetAccountActivityRequest.make
              ?languageCode:(Option.map ~f:Values.LanguageCode.of_json
                               languageCode) ~activityId ())
           (Some Values.GetAccountActivityResponse.to_json)
           (Some Values.GetAccountActivityResponse.error_to_json)])
let get_account_plan_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_plan_state
           (Values.GetAccountPlanStateRequest.make ())
           (Some Values.GetAccountPlanStateResponse.to_json)
           (Some Values.GetAccountPlanStateResponse.error_to_json)])
let get_free_tier_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Expression"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_free_tier_usage
           (Values.GetFreeTierUsageRequest.make
              ?filter:(Option.map ~f:Values.Expression.of_json filter)
              ?maxResults ?nextToken ())
           (Some Values.GetFreeTierUsageResponse.to_json)
           (Some Values.GetFreeTierUsageResponse.error_to_json)])
let list_account_activities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterActivityStatuses =
         flag "filter-activity-statuses" (optional json_arg)
           ~doc:"JSON FilterActivityStatuses"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and languageCode =
         flag "language-code" (optional json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_activities
           (Values.ListAccountActivitiesRequest.make
              ?filterActivityStatuses:(Option.map
                                         ~f:Values.FilterActivityStatuses.of_json
                                         filterActivityStatuses) ?nextToken
              ?maxResults
              ?languageCode:(Option.map ~f:Values.LanguageCode.of_json
                               languageCode) ())
           (Some Values.ListAccountActivitiesResponse.to_json)
           (Some Values.ListAccountActivitiesResponse.error_to_json)])
let upgrade_account_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountPlanType =
         flag "account-plan-type" (required json_arg)
           ~doc:"JSON AccountPlanType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upgrade_account_plan
           (Values.UpgradeAccountPlanRequest.make
              ~accountPlanType:(Values.AccountPlanType.of_json
                                  accountPlanType) ())
           (Some Values.UpgradeAccountPlanResponse.to_json)
           (Some Values.UpgradeAccountPlanResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-account-activity", get_account_activity);
    ("get-account-plan-state", get_account_plan_state);
    ("get-free-tier-usage", get_free_tier_usage);
    ("list-account-activities", list_account_activities);
    ("upgrade-account-plan", upgrade_account_plan)]
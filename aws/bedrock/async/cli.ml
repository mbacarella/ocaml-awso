(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_delete_advanced_prompt_optimization_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifiers =
         flag "job-identifiers" (required json_arg)
           ~doc:"JSON AdvancedPromptOptimizationJobIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_advanced_prompt_optimization_job
           (Values.BatchDeleteAdvancedPromptOptimizationJobRequest.make
              ~jobIdentifiers:(Values.AdvancedPromptOptimizationJobIdentifiers.of_json
                                 jobIdentifiers) ())
           (Some
              Values.BatchDeleteAdvancedPromptOptimizationJobResponse.to_json)
           (Some
              Values.BatchDeleteAdvancedPromptOptimizationJobResponse.error_to_json)])
let batch_delete_evaluation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifiers =
         flag "job-identifiers" (required json_arg)
           ~doc:"JSON EvaluationJobIdentifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_evaluation_job
           (Values.BatchDeleteEvaluationJobRequest.make
              ~jobIdentifiers:(Values.EvaluationJobIdentifiers.of_json
                                 jobIdentifiers) ())
           (Some Values.BatchDeleteEvaluationJobResponse.to_json)
           (Some Values.BatchDeleteEvaluationJobResponse.error_to_json)])
let cancel_automated_reasoning_policy_build_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_automated_reasoning_policy_build_workflow
           (Values.CancelAutomatedReasoningPolicyBuildWorkflowRequest.make
              ~policyArn ~buildWorkflowId ())
           (Some
              Values.CancelAutomatedReasoningPolicyBuildWorkflowResponse.to_json)
           (Some
              Values.CancelAutomatedReasoningPolicyBuildWorkflowResponse.error_to_json)])
let create_advanced_prompt_optimization_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDescription =
         flag "job-description" (optional string)
           ~doc:"STRING AdvancedPromptOptimizationJobDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and jobName =
         flag "job-name" (required string)
           ~doc:"STRING AdvancedPromptOptimizationJobName"
       and inputConfig =
         flag "input-config" (required json_arg)
           ~doc:"JSON AdvancedPromptOptimizationInputConfig"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON AdvancedPromptOptimizationOutputConfig"
       and modelConfigurations =
         flag "model-configurations" (required json_arg)
           ~doc:"JSON ModelConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_advanced_prompt_optimization_job
           (Values.CreateAdvancedPromptOptimizationJobRequest.make
              ?jobDescription ?clientToken ?encryptionKeyArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~jobName
              ~inputConfig:(Values.AdvancedPromptOptimizationInputConfig.of_json
                              inputConfig)
              ~outputConfig:(Values.AdvancedPromptOptimizationOutputConfig.of_json
                               outputConfig)
              ~modelConfigurations:(Values.ModelConfigurations.of_json
                                      modelConfigurations) ())
           (Some Values.CreateAdvancedPromptOptimizationJobResponse.to_json)
           (Some
              Values.CreateAdvancedPromptOptimizationJobResponse.error_to_json)])
let create_automated_reasoning_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AutomatedReasoningPolicyDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and policyDefinition =
         flag "policy-definition" (optional json_arg)
           ~doc:"JSON AutomatedReasoningPolicyDefinition"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string)
           ~doc:"STRING AutomatedReasoningPolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_automated_reasoning_policy
           (Values.CreateAutomatedReasoningPolicyRequest.make ?description
              ?clientRequestToken
              ?policyDefinition:(Option.map
                                   ~f:Values.AutomatedReasoningPolicyDefinition.of_json
                                   policyDefinition) ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateAutomatedReasoningPolicyResponse.to_json)
           (Some Values.CreateAutomatedReasoningPolicyResponse.error_to_json)])
let create_automated_reasoning_policy_test_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryContent =
         flag "query-content" (optional string)
           ~doc:"STRING AutomatedReasoningPolicyTestQueryContent"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and confidenceThreshold =
         flag "confidence-threshold" (optional float)
           ~doc:"FLOAT AutomatedReasoningCheckTranslationConfidence"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and guardContent =
         flag "guard-content" (required string)
           ~doc:"STRING AutomatedReasoningPolicyTestGuardContent"
       and expectedAggregatedFindingsResult =
         flag "expected-aggregated-findings-result" (required json_arg)
           ~doc:"JSON AutomatedReasoningCheckResult" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_automated_reasoning_policy_test_case
           (Values.CreateAutomatedReasoningPolicyTestCaseRequest.make
              ?queryContent ?clientRequestToken ?confidenceThreshold
              ~policyArn ~guardContent
              ~expectedAggregatedFindingsResult:(Values.AutomatedReasoningCheckResult.of_json
                                                   expectedAggregatedFindingsResult)
              ())
           (Some
              Values.CreateAutomatedReasoningPolicyTestCaseResponse.to_json)
           (Some
              Values.CreateAutomatedReasoningPolicyTestCaseResponse.error_to_json)])
let create_automated_reasoning_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and lastUpdatedDefinitionHash =
         flag "last-updated-definition-hash" (required string)
           ~doc:"STRING AutomatedReasoningPolicyHash" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_automated_reasoning_policy_version
           (Values.CreateAutomatedReasoningPolicyVersionRequest.make
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~policyArn
              ~lastUpdatedDefinitionHash ())
           (Some Values.CreateAutomatedReasoningPolicyVersionResponse.to_json)
           (Some
              Values.CreateAutomatedReasoningPolicyVersionResponse.error_to_json)])
let create_custom_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelKmsKeyArn =
         flag "model-kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and modelTags =
         flag "model-tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING CustomModelName"
       and modelSourceConfig =
         flag "model-source-config" (required json_arg)
           ~doc:"JSON ModelDataSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_model
           (Values.CreateCustomModelRequest.make ?modelKmsKeyArn ?roleArn
              ?modelTags:(Option.map ~f:Values.TagList.of_json modelTags)
              ?clientRequestToken ~modelName
              ~modelSourceConfig:(Values.ModelDataSource.of_json
                                    modelSourceConfig) ())
           (Some Values.CreateCustomModelResponse.to_json)
           (Some Values.CreateCustomModelResponse.error_to_json)])
let create_custom_model_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CustomModelDeploymentDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and modelDeploymentName =
         flag "model-deployment-name" (required string)
           ~doc:"STRING ModelDeploymentName"
       and modelArn =
         flag "model-arn" (required string) ~doc:"STRING CustomModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_model_deployment
           (Values.CreateCustomModelDeploymentRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientRequestToken ~modelDeploymentName ~modelArn ())
           (Some Values.CreateCustomModelDeploymentResponse.to_json)
           (Some Values.CreateCustomModelDeploymentResponse.error_to_json)])
let create_evaluation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDescription =
         flag "job-description" (optional string)
           ~doc:"STRING EvaluationJobDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and customerEncryptionKeyId =
         flag "customer-encryption-key-id" (optional string)
           ~doc:"STRING KmsKeyId"
       and jobTags = flag "job-tags" (optional json_arg) ~doc:"JSON TagList"
       and applicationType =
         flag "application-type" (optional json_arg)
           ~doc:"JSON ApplicationType"
       and jobName =
         flag "job-name" (required string) ~doc:"STRING EvaluationJobName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and evaluationConfig =
         flag "evaluation-config" (required json_arg)
           ~doc:"JSON EvaluationConfig"
       and inferenceConfig =
         flag "inference-config" (required json_arg)
           ~doc:"JSON EvaluationInferenceConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON EvaluationOutputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_evaluation_job
           (Values.CreateEvaluationJobRequest.make ?jobDescription
              ?clientRequestToken ?customerEncryptionKeyId
              ?jobTags:(Option.map ~f:Values.TagList.of_json jobTags)
              ?applicationType:(Option.map ~f:Values.ApplicationType.of_json
                                  applicationType) ~jobName ~roleArn
              ~evaluationConfig:(Values.EvaluationConfig.of_json
                                   evaluationConfig)
              ~inferenceConfig:(Values.EvaluationInferenceConfig.of_json
                                  inferenceConfig)
              ~outputDataConfig:(Values.EvaluationOutputDataConfig.of_json
                                   outputDataConfig) ())
           (Some Values.CreateEvaluationJobResponse.to_json)
           (Some Values.CreateEvaluationJobResponse.error_to_json)])
let create_foundation_model_agreement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offerToken =
         flag "offer-token" (required string) ~doc:"STRING OfferToken"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING BedrockModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_foundation_model_agreement
           (Values.CreateFoundationModelAgreementRequest.make ~offerToken
              ~modelId ())
           (Some Values.CreateFoundationModelAgreementResponse.to_json)
           (Some Values.CreateFoundationModelAgreementResponse.error_to_json)])
let create_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GuardrailDescription"
       and topicPolicyConfig =
         flag "topic-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailTopicPolicyConfig"
       and contentPolicyConfig =
         flag "content-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailContentPolicyConfig"
       and wordPolicyConfig =
         flag "word-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailWordPolicyConfig"
       and sensitiveInformationPolicyConfig =
         flag "sensitive-information-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailSensitiveInformationPolicyConfig"
       and contextualGroundingPolicyConfig =
         flag "contextual-grounding-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailContextualGroundingPolicyConfig"
       and automatedReasoningPolicyConfig =
         flag "automated-reasoning-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailAutomatedReasoningPolicyConfig"
       and crossRegionConfig =
         flag "cross-region-config" (optional json_arg)
           ~doc:"JSON GuardrailCrossRegionConfig"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and name = flag "name" (required string) ~doc:"STRING GuardrailName"
       and blockedInputMessaging =
         flag "blocked-input-messaging" (required string)
           ~doc:"STRING GuardrailBlockedMessaging"
       and blockedOutputsMessaging =
         flag "blocked-outputs-messaging" (required string)
           ~doc:"STRING GuardrailBlockedMessaging" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_guardrail
           (Values.CreateGuardrailRequest.make ?description
              ?topicPolicyConfig:(Option.map
                                    ~f:Values.GuardrailTopicPolicyConfig.of_json
                                    topicPolicyConfig)
              ?contentPolicyConfig:(Option.map
                                      ~f:Values.GuardrailContentPolicyConfig.of_json
                                      contentPolicyConfig)
              ?wordPolicyConfig:(Option.map
                                   ~f:Values.GuardrailWordPolicyConfig.of_json
                                   wordPolicyConfig)
              ?sensitiveInformationPolicyConfig:(Option.map
                                                   ~f:Values.GuardrailSensitiveInformationPolicyConfig.of_json
                                                   sensitiveInformationPolicyConfig)
              ?contextualGroundingPolicyConfig:(Option.map
                                                  ~f:Values.GuardrailContextualGroundingPolicyConfig.of_json
                                                  contextualGroundingPolicyConfig)
              ?automatedReasoningPolicyConfig:(Option.map
                                                 ~f:Values.GuardrailAutomatedReasoningPolicyConfig.of_json
                                                 automatedReasoningPolicyConfig)
              ?crossRegionConfig:(Option.map
                                    ~f:Values.GuardrailCrossRegionConfig.of_json
                                    crossRegionConfig) ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?clientRequestToken ~name ~blockedInputMessaging
              ~blockedOutputsMessaging ())
           (Some Values.CreateGuardrailResponse.to_json)
           (Some Values.CreateGuardrailResponse.error_to_json)])
let create_guardrail_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GuardrailDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and guardrailIdentifier =
         flag "guardrail-identifier" (required string)
           ~doc:"STRING GuardrailIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_guardrail_version
           (Values.CreateGuardrailVersionRequest.make ?description
              ?clientRequestToken ~guardrailIdentifier ())
           (Some Values.CreateGuardrailVersionResponse.to_json)
           (Some Values.CreateGuardrailVersionResponse.error_to_json)])
let create_inference_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING InferenceProfileDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and inferenceProfileName =
         flag "inference-profile-name" (required string)
           ~doc:"STRING InferenceProfileName"
       and modelSource =
         flag "model-source" (required json_arg)
           ~doc:"JSON InferenceProfileModelSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_inference_profile
           (Values.CreateInferenceProfileRequest.make ?description
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~inferenceProfileName
              ~modelSource:(Values.InferenceProfileModelSource.of_json
                              modelSource) ())
           (Some Values.CreateInferenceProfileResponse.to_json)
           (Some Values.CreateInferenceProfileResponse.error_to_json)])
let create_marketplace_model_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and acceptEula =
         flag "accept-eula" (optional bool) ~doc:"BOOL AcceptEula"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and modelSourceIdentifier =
         flag "model-source-identifier" (required string)
           ~doc:"STRING ModelSourceIdentifier"
       and endpointConfig =
         flag "endpoint-config" (required json_arg)
           ~doc:"JSON EndpointConfig"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_marketplace_model_endpoint
           (Values.CreateMarketplaceModelEndpointRequest.make ?acceptEula
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~modelSourceIdentifier
              ~endpointConfig:(Values.EndpointConfig.of_json endpointConfig)
              ~endpointName ())
           (Some Values.CreateMarketplaceModelEndpointResponse.to_json)
           (Some Values.CreateMarketplaceModelEndpointResponse.error_to_json)])
let create_model_copy_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelKmsKeyId =
         flag "model-kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and targetModelTags =
         flag "target-model-tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and sourceModelArn =
         flag "source-model-arn" (required string) ~doc:"STRING ModelArn"
       and targetModelName =
         flag "target-model-name" (required string)
           ~doc:"STRING CustomModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_copy_job
           (Values.CreateModelCopyJobRequest.make ?modelKmsKeyId
              ?targetModelTags:(Option.map ~f:Values.TagList.of_json
                                  targetModelTags) ?clientRequestToken
              ~sourceModelArn ~targetModelName ())
           (Some Values.CreateModelCopyJobResponse.to_json)
           (Some Values.CreateModelCopyJobResponse.error_to_json)])
let create_model_customization_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and customizationType =
         flag "customization-type" (optional json_arg)
           ~doc:"JSON CustomizationType"
       and customModelKmsKeyId =
         flag "custom-model-kms-key-id" (optional string)
           ~doc:"STRING KmsKeyId"
       and jobTags = flag "job-tags" (optional json_arg) ~doc:"JSON TagList"
       and customModelTags =
         flag "custom-model-tags" (optional json_arg) ~doc:"JSON TagList"
       and validationDataConfig =
         flag "validation-data-config" (optional json_arg)
           ~doc:"JSON ValidationDataConfig"
       and hyperParameters =
         flag "hyper-parameters" (optional json_arg)
           ~doc:"JSON ModelCustomizationHyperParameters"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and customizationConfig =
         flag "customization-config" (optional json_arg)
           ~doc:"JSON CustomizationConfig"
       and jobName = flag "job-name" (required string) ~doc:"STRING JobName"
       and customModelName =
         flag "custom-model-name" (required string)
           ~doc:"STRING CustomModelName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and baseModelIdentifier =
         flag "base-model-identifier" (required string)
           ~doc:"STRING BaseModelIdentifier"
       and trainingDataConfig =
         flag "training-data-config" (required json_arg)
           ~doc:"JSON TrainingDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_customization_job
           (Values.CreateModelCustomizationJobRequest.make
              ?clientRequestToken
              ?customizationType:(Option.map
                                    ~f:Values.CustomizationType.of_json
                                    customizationType) ?customModelKmsKeyId
              ?jobTags:(Option.map ~f:Values.TagList.of_json jobTags)
              ?customModelTags:(Option.map ~f:Values.TagList.of_json
                                  customModelTags)
              ?validationDataConfig:(Option.map
                                       ~f:Values.ValidationDataConfig.of_json
                                       validationDataConfig)
              ?hyperParameters:(Option.map
                                  ~f:Values.ModelCustomizationHyperParameters.of_json
                                  hyperParameters)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?customizationConfig:(Option.map
                                      ~f:Values.CustomizationConfig.of_json
                                      customizationConfig) ~jobName
              ~customModelName ~roleArn ~baseModelIdentifier
              ~trainingDataConfig:(Values.TrainingDataConfig.of_json
                                     trainingDataConfig)
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ())
           (Some Values.CreateModelCustomizationJobResponse.to_json)
           (Some Values.CreateModelCustomizationJobResponse.error_to_json)])
let create_model_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobTags = flag "job-tags" (optional json_arg) ~doc:"JSON TagList"
       and importedModelTags =
         flag "imported-model-tags" (optional json_arg) ~doc:"JSON TagList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and importedModelKmsKeyId =
         flag "imported-model-kms-key-id" (optional string)
           ~doc:"STRING KmsKeyId"
       and jobName = flag "job-name" (required string) ~doc:"STRING JobName"
       and importedModelName =
         flag "imported-model-name" (required string)
           ~doc:"STRING ImportedModelName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and modelDataSource =
         flag "model-data-source" (required json_arg)
           ~doc:"JSON ModelDataSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_import_job
           (Values.CreateModelImportJobRequest.make
              ?jobTags:(Option.map ~f:Values.TagList.of_json jobTags)
              ?importedModelTags:(Option.map ~f:Values.TagList.of_json
                                    importedModelTags) ?clientRequestToken
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?importedModelKmsKeyId ~jobName ~importedModelName ~roleArn
              ~modelDataSource:(Values.ModelDataSource.of_json
                                  modelDataSource) ())
           (Some Values.CreateModelImportJobResponse.to_json)
           (Some Values.CreateModelImportJobResponse.error_to_json)])
let create_model_invocation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ModelInvocationIdempotencyToken"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and timeoutDurationInHours =
         flag "timeout-duration-in-hours" (optional int)
           ~doc:"INT ModelInvocationJobTimeoutDurationInHours"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and modelInvocationType =
         flag "model-invocation-type" (optional json_arg)
           ~doc:"JSON ModelInvocationType"
       and jobName =
         flag "job-name" (required string)
           ~doc:"STRING ModelInvocationJobName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and modelId = flag "model-id" (required string) ~doc:"STRING ModelId"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON ModelInvocationJobInputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON ModelInvocationJobOutputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_invocation_job
           (Values.CreateModelInvocationJobRequest.make ?clientRequestToken
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?timeoutDurationInHours
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?modelInvocationType:(Option.map
                                      ~f:Values.ModelInvocationType.of_json
                                      modelInvocationType) ~jobName ~roleArn
              ~modelId
              ~inputDataConfig:(Values.ModelInvocationJobInputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.ModelInvocationJobOutputDataConfig.of_json
                                   outputDataConfig) ())
           (Some Values.CreateModelInvocationJobResponse.to_json)
           (Some Values.CreateModelInvocationJobResponse.error_to_json)])
let create_prompt_router =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PromptRouterDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and promptRouterName =
         flag "prompt-router-name" (required string)
           ~doc:"STRING PromptRouterName"
       and models =
         flag "models" (required json_arg)
           ~doc:"JSON PromptRouterTargetModels"
       and routingCriteria =
         flag "routing-criteria" (required json_arg)
           ~doc:"JSON RoutingCriteria"
       and fallbackModel =
         flag "fallback-model" (required json_arg)
           ~doc:"JSON PromptRouterTargetModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_prompt_router
           (Values.CreatePromptRouterRequest.make ?clientRequestToken
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~promptRouterName
              ~models:(Values.PromptRouterTargetModels.of_json models)
              ~routingCriteria:(Values.RoutingCriteria.of_json
                                  routingCriteria)
              ~fallbackModel:(Values.PromptRouterTargetModel.of_json
                                fallbackModel) ())
           (Some Values.CreatePromptRouterResponse.to_json)
           (Some Values.CreatePromptRouterResponse.error_to_json)])
let create_provisioned_model_throughput =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and commitmentDuration =
         flag "commitment-duration" (optional json_arg)
           ~doc:"JSON CommitmentDuration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and modelUnits =
         flag "model-units" (required int) ~doc:"INT PositiveInteger"
       and provisionedModelName =
         flag "provisioned-model-name" (required string)
           ~doc:"STRING ProvisionedModelName"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING ModelIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_provisioned_model_throughput
           (Values.CreateProvisionedModelThroughputRequest.make
              ?clientRequestToken
              ?commitmentDuration:(Option.map
                                     ~f:Values.CommitmentDuration.of_json
                                     commitmentDuration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~modelUnits
              ~provisionedModelName ~modelId ())
           (Some Values.CreateProvisionedModelThroughputResponse.to_json)
           (Some
              Values.CreateProvisionedModelThroughputResponse.error_to_json)])
let delete_automated_reasoning_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_automated_reasoning_policy
           (Values.DeleteAutomatedReasoningPolicyRequest.make ?force
              ~policyArn ())
           (Some Values.DeleteAutomatedReasoningPolicyResponse.to_json)
           (Some Values.DeleteAutomatedReasoningPolicyResponse.error_to_json)])
let delete_automated_reasoning_policy_build_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId"
       and lastUpdatedAt =
         flag "last-updated-at" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_automated_reasoning_policy_build_workflow
           (Values.DeleteAutomatedReasoningPolicyBuildWorkflowRequest.make
              ~policyArn ~buildWorkflowId
              ~lastUpdatedAt:(Values.Timestamp.of_json lastUpdatedAt) ())
           (Some
              Values.DeleteAutomatedReasoningPolicyBuildWorkflowResponse.to_json)
           (Some
              Values.DeleteAutomatedReasoningPolicyBuildWorkflowResponse.error_to_json)])
let delete_automated_reasoning_policy_test_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and testCaseId =
         flag "test-case-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyTestCaseId"
       and lastUpdatedAt =
         flag "last-updated-at" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_automated_reasoning_policy_test_case
           (Values.DeleteAutomatedReasoningPolicyTestCaseRequest.make
              ~policyArn ~testCaseId
              ~lastUpdatedAt:(Values.Timestamp.of_json lastUpdatedAt) ())
           (Some
              Values.DeleteAutomatedReasoningPolicyTestCaseResponse.to_json)
           (Some
              Values.DeleteAutomatedReasoningPolicyTestCaseResponse.error_to_json)])
let delete_custom_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelIdentifier =
         flag "model-identifier" (required string)
           ~doc:"STRING ModelIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_model
           (Values.DeleteCustomModelRequest.make ~modelIdentifier ())
           (Some Values.DeleteCustomModelResponse.to_json)
           (Some Values.DeleteCustomModelResponse.error_to_json)])
let delete_custom_model_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customModelDeploymentIdentifier =
         flag "custom-model-deployment-identifier" (required string)
           ~doc:"STRING CustomModelDeploymentIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_model_deployment
           (Values.DeleteCustomModelDeploymentRequest.make
              ~customModelDeploymentIdentifier ())
           (Some Values.DeleteCustomModelDeploymentResponse.to_json)
           (Some Values.DeleteCustomModelDeploymentResponse.error_to_json)])
let delete_enforced_guardrail_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configId =
         flag "config-id" (required string)
           ~doc:"STRING AccountEnforcedGuardrailConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_enforced_guardrail_configuration
           (Values.DeleteEnforcedGuardrailConfigurationRequest.make ~configId
              ())
           (Some Values.DeleteEnforcedGuardrailConfigurationResponse.to_json)
           (Some
              Values.DeleteEnforcedGuardrailConfigurationResponse.error_to_json)])
let delete_foundation_model_agreement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING BedrockModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_foundation_model_agreement
           (Values.DeleteFoundationModelAgreementRequest.make ~modelId ())
           (Some Values.DeleteFoundationModelAgreementResponse.to_json)
           (Some Values.DeleteFoundationModelAgreementResponse.error_to_json)])
let delete_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and guardrailVersion =
         flag "guardrail-version" (optional string)
           ~doc:"STRING GuardrailNumericalVersion"
       and guardrailIdentifier =
         flag "guardrail-identifier" (required string)
           ~doc:"STRING GuardrailIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_guardrail
           (Values.DeleteGuardrailRequest.make ?guardrailVersion
              ~guardrailIdentifier ())
           (Some Values.DeleteGuardrailResponse.to_json)
           (Some Values.DeleteGuardrailResponse.error_to_json)])
let delete_imported_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelIdentifier =
         flag "model-identifier" (required string)
           ~doc:"STRING ImportedModelIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_imported_model
           (Values.DeleteImportedModelRequest.make ~modelIdentifier ())
           (Some Values.DeleteImportedModelResponse.to_json)
           (Some Values.DeleteImportedModelResponse.error_to_json)])
let delete_inference_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inferenceProfileIdentifier =
         flag "inference-profile-identifier" (required string)
           ~doc:"STRING InferenceProfileIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_inference_profile
           (Values.DeleteInferenceProfileRequest.make
              ~inferenceProfileIdentifier ())
           (Some Values.DeleteInferenceProfileResponse.to_json)
           (Some Values.DeleteInferenceProfileResponse.error_to_json)])
let delete_marketplace_model_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_marketplace_model_endpoint
           (Values.DeleteMarketplaceModelEndpointRequest.make ~endpointArn ())
           (Some Values.DeleteMarketplaceModelEndpointResponse.to_json)
           (Some Values.DeleteMarketplaceModelEndpointResponse.error_to_json)])
let delete_model_invocation_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_invocation_logging_configuration
           (Values.DeleteModelInvocationLoggingConfigurationRequest.make ())
           (Some
              Values.DeleteModelInvocationLoggingConfigurationResponse.to_json)
           (Some
              Values.DeleteModelInvocationLoggingConfigurationResponse.error_to_json)])
let delete_prompt_router =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and promptRouterArn =
         flag "prompt-router-arn" (required string)
           ~doc:"STRING PromptRouterArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_prompt_router
           (Values.DeletePromptRouterRequest.make ~promptRouterArn ())
           (Some Values.DeletePromptRouterResponse.to_json)
           (Some Values.DeletePromptRouterResponse.error_to_json)])
let delete_provisioned_model_throughput =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provisionedModelId =
         flag "provisioned-model-id" (required string)
           ~doc:"STRING ProvisionedModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_provisioned_model_throughput
           (Values.DeleteProvisionedModelThroughputRequest.make
              ~provisionedModelId ())
           (Some Values.DeleteProvisionedModelThroughputResponse.to_json)
           (Some
              Values.DeleteProvisionedModelThroughputResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourcePolicyResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let deregister_marketplace_model_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_marketplace_model_endpoint
           (Values.DeregisterMarketplaceModelEndpointRequest.make
              ~endpointArn ())
           (Some Values.DeregisterMarketplaceModelEndpointResponse.to_json)
           (Some
              Values.DeregisterMarketplaceModelEndpointResponse.error_to_json)])
let export_automated_reasoning_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_automated_reasoning_policy_version
           (Values.ExportAutomatedReasoningPolicyVersionRequest.make
              ~policyArn ())
           (Some Values.ExportAutomatedReasoningPolicyVersionResponse.to_json)
           (Some
              Values.ExportAutomatedReasoningPolicyVersionResponse.error_to_json)])
let get_advanced_prompt_optimization_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING AdvancedPromptOptimizationJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_advanced_prompt_optimization_job
           (Values.GetAdvancedPromptOptimizationJobRequest.make
              ~jobIdentifier ())
           (Some Values.GetAdvancedPromptOptimizationJobResponse.to_json)
           (Some
              Values.GetAdvancedPromptOptimizationJobResponse.error_to_json)])
let get_automated_reasoning_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automated_reasoning_policy
           (Values.GetAutomatedReasoningPolicyRequest.make ~policyArn ())
           (Some Values.GetAutomatedReasoningPolicyResponse.to_json)
           (Some Values.GetAutomatedReasoningPolicyResponse.error_to_json)])
let get_automated_reasoning_policy_annotations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automated_reasoning_policy_annotations
           (Values.GetAutomatedReasoningPolicyAnnotationsRequest.make
              ~policyArn ~buildWorkflowId ())
           (Some
              Values.GetAutomatedReasoningPolicyAnnotationsResponse.to_json)
           (Some
              Values.GetAutomatedReasoningPolicyAnnotationsResponse.error_to_json)])
let get_automated_reasoning_policy_build_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automated_reasoning_policy_build_workflow
           (Values.GetAutomatedReasoningPolicyBuildWorkflowRequest.make
              ~policyArn ~buildWorkflowId ())
           (Some
              Values.GetAutomatedReasoningPolicyBuildWorkflowResponse.to_json)
           (Some
              Values.GetAutomatedReasoningPolicyBuildWorkflowResponse.error_to_json)])
let get_automated_reasoning_policy_build_workflow_result_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assetId =
         flag "asset-id" (optional string)
           ~doc:"STRING AutomatedReasoningPolicyBuildResultAssetId"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId"
       and assetType =
         flag "asset-type" (required json_arg)
           ~doc:"JSON AutomatedReasoningPolicyBuildResultAssetType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automated_reasoning_policy_build_workflow_result_assets
           (Values.GetAutomatedReasoningPolicyBuildWorkflowResultAssetsRequest.make
              ?assetId ~policyArn ~buildWorkflowId
              ~assetType:(Values.AutomatedReasoningPolicyBuildResultAssetType.of_json
                            assetType) ())
           (Some
              Values.GetAutomatedReasoningPolicyBuildWorkflowResultAssetsResponse.to_json)
           (Some
              Values.GetAutomatedReasoningPolicyBuildWorkflowResultAssetsResponse.error_to_json)])
let get_automated_reasoning_policy_next_scenario =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automated_reasoning_policy_next_scenario
           (Values.GetAutomatedReasoningPolicyNextScenarioRequest.make
              ~policyArn ~buildWorkflowId ())
           (Some
              Values.GetAutomatedReasoningPolicyNextScenarioResponse.to_json)
           (Some
              Values.GetAutomatedReasoningPolicyNextScenarioResponse.error_to_json)])
let get_automated_reasoning_policy_test_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and testCaseId =
         flag "test-case-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyTestCaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automated_reasoning_policy_test_case
           (Values.GetAutomatedReasoningPolicyTestCaseRequest.make ~policyArn
              ~testCaseId ())
           (Some Values.GetAutomatedReasoningPolicyTestCaseResponse.to_json)
           (Some
              Values.GetAutomatedReasoningPolicyTestCaseResponse.error_to_json)])
let get_automated_reasoning_policy_test_result =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId"
       and testCaseId =
         flag "test-case-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyTestCaseId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automated_reasoning_policy_test_result
           (Values.GetAutomatedReasoningPolicyTestResultRequest.make
              ~policyArn ~buildWorkflowId ~testCaseId ())
           (Some Values.GetAutomatedReasoningPolicyTestResultResponse.to_json)
           (Some
              Values.GetAutomatedReasoningPolicyTestResultResponse.error_to_json)])
let get_custom_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelIdentifier =
         flag "model-identifier" (required string)
           ~doc:"STRING ModelIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_custom_model
           (Values.GetCustomModelRequest.make ~modelIdentifier ())
           (Some Values.GetCustomModelResponse.to_json)
           (Some Values.GetCustomModelResponse.error_to_json)])
let get_custom_model_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customModelDeploymentIdentifier =
         flag "custom-model-deployment-identifier" (required string)
           ~doc:"STRING CustomModelDeploymentIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_custom_model_deployment
           (Values.GetCustomModelDeploymentRequest.make
              ~customModelDeploymentIdentifier ())
           (Some Values.GetCustomModelDeploymentResponse.to_json)
           (Some Values.GetCustomModelDeploymentResponse.error_to_json)])
let get_evaluation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING EvaluationJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_evaluation_job
           (Values.GetEvaluationJobRequest.make ~jobIdentifier ())
           (Some Values.GetEvaluationJobResponse.to_json)
           (Some Values.GetEvaluationJobResponse.error_to_json)])
let get_foundation_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelIdentifier =
         flag "model-identifier" (required string)
           ~doc:"STRING GetFoundationModelIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_foundation_model
           (Values.GetFoundationModelRequest.make ~modelIdentifier ())
           (Some Values.GetFoundationModelResponse.to_json)
           (Some Values.GetFoundationModelResponse.error_to_json)])
let get_foundation_model_availability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING BedrockModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_foundation_model_availability
           (Values.GetFoundationModelAvailabilityRequest.make ~modelId ())
           (Some Values.GetFoundationModelAvailabilityResponse.to_json)
           (Some Values.GetFoundationModelAvailabilityResponse.error_to_json)])
let get_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and guardrailVersion =
         flag "guardrail-version" (optional string)
           ~doc:"STRING GuardrailVersion"
       and guardrailIdentifier =
         flag "guardrail-identifier" (required string)
           ~doc:"STRING GuardrailIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_guardrail
           (Values.GetGuardrailRequest.make ?guardrailVersion
              ~guardrailIdentifier ())
           (Some Values.GetGuardrailResponse.to_json)
           (Some Values.GetGuardrailResponse.error_to_json)])
let get_imported_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelIdentifier =
         flag "model-identifier" (required string)
           ~doc:"STRING ImportedModelIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_imported_model
           (Values.GetImportedModelRequest.make ~modelIdentifier ())
           (Some Values.GetImportedModelResponse.to_json)
           (Some Values.GetImportedModelResponse.error_to_json)])
let get_inference_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inferenceProfileIdentifier =
         flag "inference-profile-identifier" (required string)
           ~doc:"STRING InferenceProfileIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_inference_profile
           (Values.GetInferenceProfileRequest.make
              ~inferenceProfileIdentifier ())
           (Some Values.GetInferenceProfileResponse.to_json)
           (Some Values.GetInferenceProfileResponse.error_to_json)])
let get_marketplace_model_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_marketplace_model_endpoint
           (Values.GetMarketplaceModelEndpointRequest.make ~endpointArn ())
           (Some Values.GetMarketplaceModelEndpointResponse.to_json)
           (Some Values.GetMarketplaceModelEndpointResponse.error_to_json)])
let get_model_copy_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobArn =
         flag "job-arn" (required string) ~doc:"STRING ModelCopyJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_copy_job
           (Values.GetModelCopyJobRequest.make ~jobArn ())
           (Some Values.GetModelCopyJobResponse.to_json)
           (Some Values.GetModelCopyJobResponse.error_to_json)])
let get_model_customization_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING ModelCustomizationJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_customization_job
           (Values.GetModelCustomizationJobRequest.make ~jobIdentifier ())
           (Some Values.GetModelCustomizationJobResponse.to_json)
           (Some Values.GetModelCustomizationJobResponse.error_to_json)])
let get_model_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING ModelImportJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_import_job
           (Values.GetModelImportJobRequest.make ~jobIdentifier ())
           (Some Values.GetModelImportJobResponse.to_json)
           (Some Values.GetModelImportJobResponse.error_to_json)])
let get_model_invocation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING ModelInvocationJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_invocation_job
           (Values.GetModelInvocationJobRequest.make ~jobIdentifier ())
           (Some Values.GetModelInvocationJobResponse.to_json)
           (Some Values.GetModelInvocationJobResponse.error_to_json)])
let get_model_invocation_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_invocation_logging_configuration
           (Values.GetModelInvocationLoggingConfigurationRequest.make ())
           (Some
              Values.GetModelInvocationLoggingConfigurationResponse.to_json)
           (Some
              Values.GetModelInvocationLoggingConfigurationResponse.error_to_json)])
let get_prompt_router =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and promptRouterArn =
         flag "prompt-router-arn" (required string)
           ~doc:"STRING PromptRouterArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_prompt_router
           (Values.GetPromptRouterRequest.make ~promptRouterArn ())
           (Some Values.GetPromptRouterResponse.to_json)
           (Some Values.GetPromptRouterResponse.error_to_json)])
let get_provisioned_model_throughput =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provisionedModelId =
         flag "provisioned-model-id" (required string)
           ~doc:"STRING ProvisionedModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_provisioned_model_throughput
           (Values.GetProvisionedModelThroughputRequest.make
              ~provisionedModelId ())
           (Some Values.GetProvisionedModelThroughputResponse.to_json)
           (Some Values.GetProvisionedModelThroughputResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourcePolicyResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_use_case_for_model_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_use_case_for_model_access
           (Values.GetUseCaseForModelAccessRequest.make ())
           (Some Values.GetUseCaseForModelAccessResponse.to_json)
           (Some Values.GetUseCaseForModelAccessResponse.error_to_json)])
let list_advanced_prompt_optimization_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortJobsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_advanced_prompt_optimization_jobs
           (Values.ListAdvancedPromptOptimizationJobsRequest.make ?maxResults
              ?nextToken
              ?sortBy:(Option.map ~f:Values.SortJobsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ())
           (Some Values.ListAdvancedPromptOptimizationJobsResponse.to_json)
           (Some
              Values.ListAdvancedPromptOptimizationJobsResponse.error_to_json)])
let list_automated_reasoning_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (optional string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automated_reasoning_policies
           (Values.ListAutomatedReasoningPoliciesRequest.make ?policyArn
              ?nextToken ?maxResults ())
           (Some Values.ListAutomatedReasoningPoliciesResponse.to_json)
           (Some Values.ListAutomatedReasoningPoliciesResponse.error_to_json)])
let list_automated_reasoning_policy_build_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automated_reasoning_policy_build_workflows
           (Values.ListAutomatedReasoningPolicyBuildWorkflowsRequest.make
              ?nextToken ?maxResults ~policyArn ())
           (Some
              Values.ListAutomatedReasoningPolicyBuildWorkflowsResponse.to_json)
           (Some
              Values.ListAutomatedReasoningPolicyBuildWorkflowsResponse.error_to_json)])
let list_automated_reasoning_policy_test_cases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automated_reasoning_policy_test_cases
           (Values.ListAutomatedReasoningPolicyTestCasesRequest.make
              ?nextToken ?maxResults ~policyArn ())
           (Some Values.ListAutomatedReasoningPolicyTestCasesResponse.to_json)
           (Some
              Values.ListAutomatedReasoningPolicyTestCasesResponse.error_to_json)])
let list_automated_reasoning_policy_test_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_automated_reasoning_policy_test_results
           (Values.ListAutomatedReasoningPolicyTestResultsRequest.make
              ?nextToken ?maxResults ~policyArn ~buildWorkflowId ())
           (Some
              Values.ListAutomatedReasoningPolicyTestResultsResponse.to_json)
           (Some
              Values.ListAutomatedReasoningPolicyTestResultsResponse.error_to_json)])
let list_custom_model_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and createdBefore =
         flag "created-before" (optional json_arg) ~doc:"JSON Timestamp"
       and createdAfter =
         flag "created-after" (optional json_arg) ~doc:"JSON Timestamp"
       and nameContains =
         flag "name-contains" (optional string)
           ~doc:"STRING ModelDeploymentName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortModelsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON CustomModelDeploymentStatus"
       and modelArnEquals =
         flag "model-arn-equals" (optional string)
           ~doc:"STRING CustomModelArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_model_deployments
           (Values.ListCustomModelDeploymentsRequest.make
              ?createdBefore:(Option.map ~f:Values.Timestamp.of_json
                                createdBefore)
              ?createdAfter:(Option.map ~f:Values.Timestamp.of_json
                               createdAfter) ?nameContains ?maxResults
              ?nextToken
              ?sortBy:(Option.map ~f:Values.SortModelsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?statusEquals:(Option.map
                               ~f:Values.CustomModelDeploymentStatus.of_json
                               statusEquals) ?modelArnEquals ())
           (Some Values.ListCustomModelDeploymentsResponse.to_json)
           (Some Values.ListCustomModelDeploymentsResponse.error_to_json)])
let list_custom_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and nameContains =
         flag "name-contains" (optional string) ~doc:"STRING CustomModelName"
       and baseModelArnEquals =
         flag "base-model-arn-equals" (optional string)
           ~doc:"STRING ModelArn"
       and foundationModelArnEquals =
         flag "foundation-model-arn-equals" (optional string)
           ~doc:"STRING FoundationModelArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortModelsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and isOwned = flag "is-owned" (optional bool) ~doc:"BOOL Boolean"
       and modelStatus =
         flag "model-status" (optional json_arg) ~doc:"JSON ModelStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_models
           (Values.ListCustomModelsRequest.make
              ?creationTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                     creationTimeBefore)
              ?creationTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                    creationTimeAfter) ?nameContains
              ?baseModelArnEquals ?foundationModelArnEquals ?maxResults
              ?nextToken
              ?sortBy:(Option.map ~f:Values.SortModelsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?isOwned
              ?modelStatus:(Option.map ~f:Values.ModelStatus.of_json
                              modelStatus) ())
           (Some Values.ListCustomModelsResponse.to_json)
           (Some Values.ListCustomModelsResponse.error_to_json)])
let list_enforced_guardrails_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_enforced_guardrails_configuration
           (Values.ListEnforcedGuardrailsConfigurationRequest.make ?nextToken
              ())
           (Some Values.ListEnforcedGuardrailsConfigurationResponse.to_json)
           (Some
              Values.ListEnforcedGuardrailsConfigurationResponse.error_to_json)])
let list_evaluation_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON EvaluationJobStatus"
       and applicationTypeEquals =
         flag "application-type-equals" (optional json_arg)
           ~doc:"JSON ApplicationType"
       and nameContains =
         flag "name-contains" (optional string)
           ~doc:"STRING EvaluationJobName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortJobsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_evaluation_jobs
           (Values.ListEvaluationJobsRequest.make
              ?creationTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                    creationTimeAfter)
              ?creationTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                     creationTimeBefore)
              ?statusEquals:(Option.map ~f:Values.EvaluationJobStatus.of_json
                               statusEquals)
              ?applicationTypeEquals:(Option.map
                                        ~f:Values.ApplicationType.of_json
                                        applicationTypeEquals) ?nameContains
              ?maxResults ?nextToken
              ?sortBy:(Option.map ~f:Values.SortJobsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ()) (Some Values.ListEvaluationJobsResponse.to_json)
           (Some Values.ListEvaluationJobsResponse.error_to_json)])
let list_foundation_model_agreement_offers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offerType =
         flag "offer-type" (optional json_arg) ~doc:"JSON OfferType"
       and modelId =
         flag "model-id" (required string) ~doc:"STRING BedrockModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_foundation_model_agreement_offers
           (Values.ListFoundationModelAgreementOffersRequest.make
              ?offerType:(Option.map ~f:Values.OfferType.of_json offerType)
              ~modelId ())
           (Some Values.ListFoundationModelAgreementOffersResponse.to_json)
           (Some
              Values.ListFoundationModelAgreementOffersResponse.error_to_json)])
let list_foundation_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and byProvider =
         flag "by-provider" (optional string) ~doc:"STRING Provider"
       and byCustomizationType =
         flag "by-customization-type" (optional json_arg)
           ~doc:"JSON ModelCustomization"
       and byOutputModality =
         flag "by-output-modality" (optional json_arg)
           ~doc:"JSON ModelModality"
       and byInferenceType =
         flag "by-inference-type" (optional json_arg)
           ~doc:"JSON InferenceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_foundation_models
           (Values.ListFoundationModelsRequest.make ?byProvider
              ?byCustomizationType:(Option.map
                                      ~f:Values.ModelCustomization.of_json
                                      byCustomizationType)
              ?byOutputModality:(Option.map ~f:Values.ModelModality.of_json
                                   byOutputModality)
              ?byInferenceType:(Option.map ~f:Values.InferenceType.of_json
                                  byInferenceType) ())
           (Some Values.ListFoundationModelsResponse.to_json)
           (Some Values.ListFoundationModelsResponse.error_to_json)])
let list_guardrails =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and guardrailIdentifier =
         flag "guardrail-identifier" (optional string)
           ~doc:"STRING GuardrailIdentifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_guardrails
           (Values.ListGuardrailsRequest.make ?guardrailIdentifier
              ?maxResults ?nextToken ())
           (Some Values.ListGuardrailsResponse.to_json)
           (Some Values.ListGuardrailsResponse.error_to_json)])
let list_imported_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and nameContains =
         flag "name-contains" (optional string)
           ~doc:"STRING ImportedModelName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy =
         flag "sort-by" (optional json_arg) ~doc:"JSON SortModelsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_imported_models
           (Values.ListImportedModelsRequest.make
              ?creationTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                     creationTimeBefore)
              ?creationTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                    creationTimeAfter) ?nameContains
              ?maxResults ?nextToken
              ?sortBy:(Option.map ~f:Values.SortModelsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ()) (Some Values.ListImportedModelsResponse.to_json)
           (Some Values.ListImportedModelsResponse.error_to_json)])
let list_inference_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and typeEquals =
         flag "type-equals" (optional json_arg)
           ~doc:"JSON InferenceProfileType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_inference_profiles
           (Values.ListInferenceProfilesRequest.make ?maxResults ?nextToken
              ?typeEquals:(Option.map ~f:Values.InferenceProfileType.of_json
                             typeEquals) ())
           (Some Values.ListInferenceProfilesResponse.to_json)
           (Some Values.ListInferenceProfilesResponse.error_to_json)])
let list_marketplace_model_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and modelSourceEquals =
         flag "model-source-equals" (optional string)
           ~doc:"STRING ModelSourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_marketplace_model_endpoints
           (Values.ListMarketplaceModelEndpointsRequest.make ?maxResults
              ?nextToken ?modelSourceEquals ())
           (Some Values.ListMarketplaceModelEndpointsResponse.to_json)
           (Some Values.ListMarketplaceModelEndpointsResponse.error_to_json)])
let list_model_copy_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON ModelCopyJobStatus"
       and sourceAccountEquals =
         flag "source-account-equals" (optional string)
           ~doc:"STRING AccountId"
       and sourceModelArnEquals =
         flag "source-model-arn-equals" (optional string)
           ~doc:"STRING ModelArn"
       and targetModelNameContains =
         flag "target-model-name-contains" (optional string)
           ~doc:"STRING CustomModelName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortJobsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_model_copy_jobs
           (Values.ListModelCopyJobsRequest.make
              ?creationTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                    creationTimeAfter)
              ?creationTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                     creationTimeBefore)
              ?statusEquals:(Option.map ~f:Values.ModelCopyJobStatus.of_json
                               statusEquals) ?sourceAccountEquals
              ?sourceModelArnEquals ?targetModelNameContains ?maxResults
              ?nextToken
              ?sortBy:(Option.map ~f:Values.SortJobsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ()) (Some Values.ListModelCopyJobsResponse.to_json)
           (Some Values.ListModelCopyJobsResponse.error_to_json)])
let list_model_customization_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON FineTuningJobStatus"
       and nameContains =
         flag "name-contains" (optional string) ~doc:"STRING JobName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortJobsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_model_customization_jobs
           (Values.ListModelCustomizationJobsRequest.make
              ?creationTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                    creationTimeAfter)
              ?creationTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                     creationTimeBefore)
              ?statusEquals:(Option.map ~f:Values.FineTuningJobStatus.of_json
                               statusEquals) ?nameContains ?maxResults
              ?nextToken
              ?sortBy:(Option.map ~f:Values.SortJobsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ()) (Some Values.ListModelCustomizationJobsResponse.to_json)
           (Some Values.ListModelCustomizationJobsResponse.error_to_json)])
let list_model_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON ModelImportJobStatus"
       and nameContains =
         flag "name-contains" (optional string) ~doc:"STRING JobName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortJobsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_model_import_jobs
           (Values.ListModelImportJobsRequest.make
              ?creationTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                    creationTimeAfter)
              ?creationTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                     creationTimeBefore)
              ?statusEquals:(Option.map
                               ~f:Values.ModelImportJobStatus.of_json
                               statusEquals) ?nameContains ?maxResults
              ?nextToken
              ?sortBy:(Option.map ~f:Values.SortJobsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ()) (Some Values.ListModelImportJobsResponse.to_json)
           (Some Values.ListModelImportJobsResponse.error_to_json)])
let list_model_invocation_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and submitTimeAfter =
         flag "submit-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and submitTimeBefore =
         flag "submit-time-before" (optional json_arg) ~doc:"JSON Timestamp"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON ModelInvocationJobStatus"
       and nameContains =
         flag "name-contains" (optional string)
           ~doc:"STRING ModelInvocationJobName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy = flag "sort-by" (optional json_arg) ~doc:"JSON SortJobsBy"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_model_invocation_jobs
           (Values.ListModelInvocationJobsRequest.make
              ?submitTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                  submitTimeAfter)
              ?submitTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                   submitTimeBefore)
              ?statusEquals:(Option.map
                               ~f:Values.ModelInvocationJobStatus.of_json
                               statusEquals) ?nameContains ?maxResults
              ?nextToken
              ?sortBy:(Option.map ~f:Values.SortJobsBy.of_json sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ()) (Some Values.ListModelInvocationJobsResponse.to_json)
           (Some Values.ListModelInvocationJobsResponse.error_to_json)])
let list_prompt_routers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON PromptRouterType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_prompt_routers
           (Values.ListPromptRoutersRequest.make ?maxResults ?nextToken
              ?type_:(Option.map ~f:Values.PromptRouterType.of_json type_) ())
           (Some Values.ListPromptRoutersResponse.to_json)
           (Some Values.ListPromptRoutersResponse.error_to_json)])
let list_provisioned_model_throughputs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and statusEquals =
         flag "status-equals" (optional json_arg)
           ~doc:"JSON ProvisionedModelStatus"
       and modelArnEquals =
         flag "model-arn-equals" (optional string) ~doc:"STRING ModelArn"
       and nameContains =
         flag "name-contains" (optional string)
           ~doc:"STRING ProvisionedModelName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sortBy =
         flag "sort-by" (optional json_arg)
           ~doc:"JSON SortByProvisionedModels"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioned_model_throughputs
           (Values.ListProvisionedModelThroughputsRequest.make
              ?creationTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                    creationTimeAfter)
              ?creationTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                     creationTimeBefore)
              ?statusEquals:(Option.map
                               ~f:Values.ProvisionedModelStatus.of_json
                               statusEquals) ?modelArnEquals ?nameContains
              ?maxResults ?nextToken
              ?sortBy:(Option.map ~f:Values.SortByProvisionedModels.of_json
                         sortBy)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ())
           (Some Values.ListProvisionedModelThroughputsResponse.to_json)
           (Some Values.ListProvisionedModelThroughputsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourcesArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_enforced_guardrail_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configId =
         flag "config-id" (optional string)
           ~doc:"STRING AccountEnforcedGuardrailConfigurationId"
       and guardrailInferenceConfig =
         flag "guardrail-inference-config" (required json_arg)
           ~doc:"JSON AccountEnforcedGuardrailInferenceInputConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_enforced_guardrail_configuration
           (Values.PutEnforcedGuardrailConfigurationRequest.make ?configId
              ~guardrailInferenceConfig:(Values.AccountEnforcedGuardrailInferenceInputConfiguration.of_json
                                           guardrailInferenceConfig) ())
           (Some Values.PutEnforcedGuardrailConfigurationResponse.to_json)
           (Some
              Values.PutEnforcedGuardrailConfigurationResponse.error_to_json)])
let put_model_invocation_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loggingConfig =
         flag "logging-config" (required json_arg) ~doc:"JSON LoggingConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_model_invocation_logging_configuration
           (Values.PutModelInvocationLoggingConfigurationRequest.make
              ~loggingConfig:(Values.LoggingConfig.of_json loggingConfig) ())
           (Some
              Values.PutModelInvocationLoggingConfigurationResponse.to_json)
           (Some
              Values.PutModelInvocationLoggingConfigurationResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourcePolicyResourceArn"
       and resourcePolicy =
         flag "resource-policy" (required string)
           ~doc:"STRING ResourcePolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ~resourceArn ~resourcePolicy
              ()) (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let put_use_case_for_model_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and formData =
         flag "form-data" (required json_arg)
           ~doc:"JSON AcknowledgementFormDataBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_use_case_for_model_access
           (Values.PutUseCaseForModelAccessRequest.make
              ~formData:(Values.AcknowledgementFormDataBody.of_json formData)
              ()) (Some Values.PutUseCaseForModelAccessResponse.to_json)
           (Some Values.PutUseCaseForModelAccessResponse.error_to_json)])
let register_marketplace_model_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointIdentifier =
         flag "endpoint-identifier" (required string) ~doc:"STRING Arn"
       and modelSourceIdentifier =
         flag "model-source-identifier" (required string)
           ~doc:"STRING ModelSourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_marketplace_model_endpoint
           (Values.RegisterMarketplaceModelEndpointRequest.make
              ~endpointIdentifier ~modelSourceIdentifier ())
           (Some Values.RegisterMarketplaceModelEndpointResponse.to_json)
           (Some
              Values.RegisterMarketplaceModelEndpointResponse.error_to_json)])
let start_automated_reasoning_policy_build_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowType =
         flag "build-workflow-type" (required json_arg)
           ~doc:"JSON AutomatedReasoningPolicyBuildWorkflowType"
       and sourceContent =
         flag "source-content" (required json_arg)
           ~doc:"JSON AutomatedReasoningPolicyBuildWorkflowSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_automated_reasoning_policy_build_workflow
           (Values.StartAutomatedReasoningPolicyBuildWorkflowRequest.make
              ?clientRequestToken ~policyArn
              ~buildWorkflowType:(Values.AutomatedReasoningPolicyBuildWorkflowType.of_json
                                    buildWorkflowType)
              ~sourceContent:(Values.AutomatedReasoningPolicyBuildWorkflowSource.of_json
                                sourceContent) ())
           (Some
              Values.StartAutomatedReasoningPolicyBuildWorkflowResponse.to_json)
           (Some
              Values.StartAutomatedReasoningPolicyBuildWorkflowResponse.error_to_json)])
let start_automated_reasoning_policy_test_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and testCaseIds =
         flag "test-case-ids" (optional json_arg)
           ~doc:"JSON AutomatedReasoningPolicyTestCaseIdList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_automated_reasoning_policy_test_workflow
           (Values.StartAutomatedReasoningPolicyTestWorkflowRequest.make
              ?testCaseIds:(Option.map
                              ~f:Values.AutomatedReasoningPolicyTestCaseIdList.of_json
                              testCaseIds) ?clientRequestToken ~policyArn
              ~buildWorkflowId ())
           (Some
              Values.StartAutomatedReasoningPolicyTestWorkflowResponse.to_json)
           (Some
              Values.StartAutomatedReasoningPolicyTestWorkflowResponse.error_to_json)])
let stop_advanced_prompt_optimization_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING AdvancedPromptOptimizationJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_advanced_prompt_optimization_job
           (Values.StopAdvancedPromptOptimizationJobRequest.make
              ~jobIdentifier ())
           (Some Values.StopAdvancedPromptOptimizationJobResponse.to_json)
           (Some
              Values.StopAdvancedPromptOptimizationJobResponse.error_to_json)])
let stop_evaluation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING EvaluationJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_evaluation_job
           (Values.StopEvaluationJobRequest.make ~jobIdentifier ())
           (Some Values.StopEvaluationJobResponse.to_json)
           (Some Values.StopEvaluationJobResponse.error_to_json)])
let stop_model_customization_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING ModelCustomizationJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_model_customization_job
           (Values.StopModelCustomizationJobRequest.make ~jobIdentifier ())
           (Some Values.StopModelCustomizationJobResponse.to_json)
           (Some Values.StopModelCustomizationJobResponse.error_to_json)])
let stop_model_invocation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIdentifier =
         flag "job-identifier" (required string)
           ~doc:"STRING ModelInvocationJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_model_invocation_job
           (Values.StopModelInvocationJobRequest.make ~jobIdentifier ())
           (Some Values.StopModelInvocationJobResponse.to_json)
           (Some Values.StopModelInvocationJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourcesArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourcesArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_automated_reasoning_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING AutomatedReasoningPolicyName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AutomatedReasoningPolicyDescription"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and policyDefinition =
         flag "policy-definition" (required json_arg)
           ~doc:"JSON AutomatedReasoningPolicyDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_automated_reasoning_policy
           (Values.UpdateAutomatedReasoningPolicyRequest.make ?name
              ?description ~policyArn
              ~policyDefinition:(Values.AutomatedReasoningPolicyDefinition.of_json
                                   policyDefinition) ())
           (Some Values.UpdateAutomatedReasoningPolicyResponse.to_json)
           (Some Values.UpdateAutomatedReasoningPolicyResponse.error_to_json)])
let update_automated_reasoning_policy_annotations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and buildWorkflowId =
         flag "build-workflow-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyBuildWorkflowId"
       and annotations =
         flag "annotations" (required json_arg)
           ~doc:"JSON AutomatedReasoningPolicyAnnotationList"
       and lastUpdatedAnnotationSetHash =
         flag "last-updated-annotation-set-hash" (required string)
           ~doc:"STRING AutomatedReasoningPolicyHash" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_automated_reasoning_policy_annotations
           (Values.UpdateAutomatedReasoningPolicyAnnotationsRequest.make
              ~policyArn ~buildWorkflowId
              ~annotations:(Values.AutomatedReasoningPolicyAnnotationList.of_json
                              annotations) ~lastUpdatedAnnotationSetHash ())
           (Some
              Values.UpdateAutomatedReasoningPolicyAnnotationsResponse.to_json)
           (Some
              Values.UpdateAutomatedReasoningPolicyAnnotationsResponse.error_to_json)])
let update_automated_reasoning_policy_test_case =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryContent =
         flag "query-content" (optional string)
           ~doc:"STRING AutomatedReasoningPolicyTestQueryContent"
       and confidenceThreshold =
         flag "confidence-threshold" (optional float)
           ~doc:"FLOAT AutomatedReasoningCheckTranslationConfidence"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING AutomatedReasoningPolicyArn"
       and testCaseId =
         flag "test-case-id" (required string)
           ~doc:"STRING AutomatedReasoningPolicyTestCaseId"
       and guardContent =
         flag "guard-content" (required string)
           ~doc:"STRING AutomatedReasoningPolicyTestGuardContent"
       and lastUpdatedAt =
         flag "last-updated-at" (required json_arg) ~doc:"JSON Timestamp"
       and expectedAggregatedFindingsResult =
         flag "expected-aggregated-findings-result" (required json_arg)
           ~doc:"JSON AutomatedReasoningCheckResult" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_automated_reasoning_policy_test_case
           (Values.UpdateAutomatedReasoningPolicyTestCaseRequest.make
              ?queryContent ?confidenceThreshold ?clientRequestToken
              ~policyArn ~testCaseId ~guardContent
              ~lastUpdatedAt:(Values.Timestamp.of_json lastUpdatedAt)
              ~expectedAggregatedFindingsResult:(Values.AutomatedReasoningCheckResult.of_json
                                                   expectedAggregatedFindingsResult)
              ())
           (Some
              Values.UpdateAutomatedReasoningPolicyTestCaseResponse.to_json)
           (Some
              Values.UpdateAutomatedReasoningPolicyTestCaseResponse.error_to_json)])
let update_custom_model_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelArn =
         flag "model-arn" (required string) ~doc:"STRING CustomModelArn"
       and customModelDeploymentIdentifier =
         flag "custom-model-deployment-identifier" (required string)
           ~doc:"STRING CustomModelDeploymentIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_model_deployment
           (Values.UpdateCustomModelDeploymentRequest.make ~modelArn
              ~customModelDeploymentIdentifier ())
           (Some Values.UpdateCustomModelDeploymentResponse.to_json)
           (Some Values.UpdateCustomModelDeploymentResponse.error_to_json)])
let update_guardrail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GuardrailDescription"
       and topicPolicyConfig =
         flag "topic-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailTopicPolicyConfig"
       and contentPolicyConfig =
         flag "content-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailContentPolicyConfig"
       and wordPolicyConfig =
         flag "word-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailWordPolicyConfig"
       and sensitiveInformationPolicyConfig =
         flag "sensitive-information-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailSensitiveInformationPolicyConfig"
       and contextualGroundingPolicyConfig =
         flag "contextual-grounding-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailContextualGroundingPolicyConfig"
       and automatedReasoningPolicyConfig =
         flag "automated-reasoning-policy-config" (optional json_arg)
           ~doc:"JSON GuardrailAutomatedReasoningPolicyConfig"
       and crossRegionConfig =
         flag "cross-region-config" (optional json_arg)
           ~doc:"JSON GuardrailCrossRegionConfig"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and guardrailIdentifier =
         flag "guardrail-identifier" (required string)
           ~doc:"STRING GuardrailIdentifier"
       and name = flag "name" (required string) ~doc:"STRING GuardrailName"
       and blockedInputMessaging =
         flag "blocked-input-messaging" (required string)
           ~doc:"STRING GuardrailBlockedMessaging"
       and blockedOutputsMessaging =
         flag "blocked-outputs-messaging" (required string)
           ~doc:"STRING GuardrailBlockedMessaging" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_guardrail
           (Values.UpdateGuardrailRequest.make ?description
              ?topicPolicyConfig:(Option.map
                                    ~f:Values.GuardrailTopicPolicyConfig.of_json
                                    topicPolicyConfig)
              ?contentPolicyConfig:(Option.map
                                      ~f:Values.GuardrailContentPolicyConfig.of_json
                                      contentPolicyConfig)
              ?wordPolicyConfig:(Option.map
                                   ~f:Values.GuardrailWordPolicyConfig.of_json
                                   wordPolicyConfig)
              ?sensitiveInformationPolicyConfig:(Option.map
                                                   ~f:Values.GuardrailSensitiveInformationPolicyConfig.of_json
                                                   sensitiveInformationPolicyConfig)
              ?contextualGroundingPolicyConfig:(Option.map
                                                  ~f:Values.GuardrailContextualGroundingPolicyConfig.of_json
                                                  contextualGroundingPolicyConfig)
              ?automatedReasoningPolicyConfig:(Option.map
                                                 ~f:Values.GuardrailAutomatedReasoningPolicyConfig.of_json
                                                 automatedReasoningPolicyConfig)
              ?crossRegionConfig:(Option.map
                                    ~f:Values.GuardrailCrossRegionConfig.of_json
                                    crossRegionConfig) ?kmsKeyId
              ~guardrailIdentifier ~name ~blockedInputMessaging
              ~blockedOutputsMessaging ())
           (Some Values.UpdateGuardrailResponse.to_json)
           (Some Values.UpdateGuardrailResponse.error_to_json)])
let update_marketplace_model_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and endpointArn =
         flag "endpoint-arn" (required string) ~doc:"STRING Arn"
       and endpointConfig =
         flag "endpoint-config" (required json_arg)
           ~doc:"JSON EndpointConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_marketplace_model_endpoint
           (Values.UpdateMarketplaceModelEndpointRequest.make
              ?clientRequestToken ~endpointArn
              ~endpointConfig:(Values.EndpointConfig.of_json endpointConfig)
              ())
           (Some Values.UpdateMarketplaceModelEndpointResponse.to_json)
           (Some Values.UpdateMarketplaceModelEndpointResponse.error_to_json)])
let update_provisioned_model_throughput =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and desiredProvisionedModelName =
         flag "desired-provisioned-model-name" (optional string)
           ~doc:"STRING ProvisionedModelName"
       and desiredModelId =
         flag "desired-model-id" (optional string)
           ~doc:"STRING ModelIdentifier"
       and provisionedModelId =
         flag "provisioned-model-id" (required string)
           ~doc:"STRING ProvisionedModelId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_provisioned_model_throughput
           (Values.UpdateProvisionedModelThroughputRequest.make
              ?desiredProvisionedModelName ?desiredModelId
              ~provisionedModelId ())
           (Some Values.UpdateProvisionedModelThroughputResponse.to_json)
           (Some
              Values.UpdateProvisionedModelThroughputResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-delete-advanced-prompt-optimization-job",
       batch_delete_advanced_prompt_optimization_job);
    ("batch-delete-evaluation-job", batch_delete_evaluation_job);
    ("cancel-automated-reasoning-policy-build-workflow",
      cancel_automated_reasoning_policy_build_workflow);
    ("create-advanced-prompt-optimization-job",
      create_advanced_prompt_optimization_job);
    ("create-automated-reasoning-policy", create_automated_reasoning_policy);
    ("create-automated-reasoning-policy-test-case",
      create_automated_reasoning_policy_test_case);
    ("create-automated-reasoning-policy-version",
      create_automated_reasoning_policy_version);
    ("create-custom-model", create_custom_model);
    ("create-custom-model-deployment", create_custom_model_deployment);
    ("create-evaluation-job", create_evaluation_job);
    ("create-foundation-model-agreement", create_foundation_model_agreement);
    ("create-guardrail", create_guardrail);
    ("create-guardrail-version", create_guardrail_version);
    ("create-inference-profile", create_inference_profile);
    ("create-marketplace-model-endpoint", create_marketplace_model_endpoint);
    ("create-model-copy-job", create_model_copy_job);
    ("create-model-customization-job", create_model_customization_job);
    ("create-model-import-job", create_model_import_job);
    ("create-model-invocation-job", create_model_invocation_job);
    ("create-prompt-router", create_prompt_router);
    ("create-provisioned-model-throughput",
      create_provisioned_model_throughput);
    ("delete-automated-reasoning-policy", delete_automated_reasoning_policy);
    ("delete-automated-reasoning-policy-build-workflow",
      delete_automated_reasoning_policy_build_workflow);
    ("delete-automated-reasoning-policy-test-case",
      delete_automated_reasoning_policy_test_case);
    ("delete-custom-model", delete_custom_model);
    ("delete-custom-model-deployment", delete_custom_model_deployment);
    ("delete-enforced-guardrail-configuration",
      delete_enforced_guardrail_configuration);
    ("delete-foundation-model-agreement", delete_foundation_model_agreement);
    ("delete-guardrail", delete_guardrail);
    ("delete-imported-model", delete_imported_model);
    ("delete-inference-profile", delete_inference_profile);
    ("delete-marketplace-model-endpoint", delete_marketplace_model_endpoint);
    ("delete-model-invocation-logging-configuration",
      delete_model_invocation_logging_configuration);
    ("delete-prompt-router", delete_prompt_router);
    ("delete-provisioned-model-throughput",
      delete_provisioned_model_throughput);
    ("delete-resource-policy", delete_resource_policy);
    ("deregister-marketplace-model-endpoint",
      deregister_marketplace_model_endpoint);
    ("export-automated-reasoning-policy-version",
      export_automated_reasoning_policy_version);
    ("get-advanced-prompt-optimization-job",
      get_advanced_prompt_optimization_job);
    ("get-automated-reasoning-policy", get_automated_reasoning_policy);
    ("get-automated-reasoning-policy-annotations",
      get_automated_reasoning_policy_annotations);
    ("get-automated-reasoning-policy-build-workflow",
      get_automated_reasoning_policy_build_workflow);
    ("get-automated-reasoning-policy-build-workflow-result-assets",
      get_automated_reasoning_policy_build_workflow_result_assets);
    ("get-automated-reasoning-policy-next-scenario",
      get_automated_reasoning_policy_next_scenario);
    ("get-automated-reasoning-policy-test-case",
      get_automated_reasoning_policy_test_case);
    ("get-automated-reasoning-policy-test-result",
      get_automated_reasoning_policy_test_result);
    ("get-custom-model", get_custom_model);
    ("get-custom-model-deployment", get_custom_model_deployment);
    ("get-evaluation-job", get_evaluation_job);
    ("get-foundation-model", get_foundation_model);
    ("get-foundation-model-availability", get_foundation_model_availability);
    ("get-guardrail", get_guardrail);
    ("get-imported-model", get_imported_model);
    ("get-inference-profile", get_inference_profile);
    ("get-marketplace-model-endpoint", get_marketplace_model_endpoint);
    ("get-model-copy-job", get_model_copy_job);
    ("get-model-customization-job", get_model_customization_job);
    ("get-model-import-job", get_model_import_job);
    ("get-model-invocation-job", get_model_invocation_job);
    ("get-model-invocation-logging-configuration",
      get_model_invocation_logging_configuration);
    ("get-prompt-router", get_prompt_router);
    ("get-provisioned-model-throughput", get_provisioned_model_throughput);
    ("get-resource-policy", get_resource_policy);
    ("get-use-case-for-model-access", get_use_case_for_model_access);
    ("list-advanced-prompt-optimization-jobs",
      list_advanced_prompt_optimization_jobs);
    ("list-automated-reasoning-policies", list_automated_reasoning_policies);
    ("list-automated-reasoning-policy-build-workflows",
      list_automated_reasoning_policy_build_workflows);
    ("list-automated-reasoning-policy-test-cases",
      list_automated_reasoning_policy_test_cases);
    ("list-automated-reasoning-policy-test-results",
      list_automated_reasoning_policy_test_results);
    ("list-custom-model-deployments", list_custom_model_deployments);
    ("list-custom-models", list_custom_models);
    ("list-enforced-guardrails-configuration",
      list_enforced_guardrails_configuration);
    ("list-evaluation-jobs", list_evaluation_jobs);
    ("list-foundation-model-agreement-offers",
      list_foundation_model_agreement_offers);
    ("list-foundation-models", list_foundation_models);
    ("list-guardrails", list_guardrails);
    ("list-imported-models", list_imported_models);
    ("list-inference-profiles", list_inference_profiles);
    ("list-marketplace-model-endpoints", list_marketplace_model_endpoints);
    ("list-model-copy-jobs", list_model_copy_jobs);
    ("list-model-customization-jobs", list_model_customization_jobs);
    ("list-model-import-jobs", list_model_import_jobs);
    ("list-model-invocation-jobs", list_model_invocation_jobs);
    ("list-prompt-routers", list_prompt_routers);
    ("list-provisioned-model-throughputs",
      list_provisioned_model_throughputs);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-enforced-guardrail-configuration",
      put_enforced_guardrail_configuration);
    ("put-model-invocation-logging-configuration",
      put_model_invocation_logging_configuration);
    ("put-resource-policy", put_resource_policy);
    ("put-use-case-for-model-access", put_use_case_for_model_access);
    ("register-marketplace-model-endpoint",
      register_marketplace_model_endpoint);
    ("start-automated-reasoning-policy-build-workflow",
      start_automated_reasoning_policy_build_workflow);
    ("start-automated-reasoning-policy-test-workflow",
      start_automated_reasoning_policy_test_workflow);
    ("stop-advanced-prompt-optimization-job",
      stop_advanced_prompt_optimization_job);
    ("stop-evaluation-job", stop_evaluation_job);
    ("stop-model-customization-job", stop_model_customization_job);
    ("stop-model-invocation-job", stop_model_invocation_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-automated-reasoning-policy", update_automated_reasoning_policy);
    ("update-automated-reasoning-policy-annotations",
      update_automated_reasoning_policy_annotations);
    ("update-automated-reasoning-policy-test-case",
      update_automated_reasoning_policy_test_case);
    ("update-custom-model-deployment", update_custom_model_deployment);
    ("update-guardrail", update_guardrail);
    ("update-marketplace-model-endpoint", update_marketplace_model_endpoint);
    ("update-provisioned-model-throughput",
      update_provisioned_model_throughput)]
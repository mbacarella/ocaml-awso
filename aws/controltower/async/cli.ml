(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_landing_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and remediationTypes =
         flag "remediation-types" (optional json_arg)
           ~doc:"JSON RemediationTypes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and manifest =
         flag "manifest" (optional json_arg) ~doc:"JSON Manifest"
       and version =
         flag "version" (required string) ~doc:"STRING LandingZoneVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_landing_zone
           (Values.CreateLandingZoneInput.make
              ?remediationTypes:(Option.map
                                   ~f:Values.RemediationTypes.of_json
                                   remediationTypes)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?manifest:(Option.map ~f:Values.Manifest.of_json manifest)
              ~version ()) (Some Values.CreateLandingZoneOutput.to_json)
           (Some Values.CreateLandingZoneOutput.error_to_json)])
let delete_landing_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and landingZoneIdentifier =
         flag "landing-zone-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_landing_zone
           (Values.DeleteLandingZoneInput.make ~landingZoneIdentifier ())
           (Some Values.DeleteLandingZoneOutput.to_json)
           (Some Values.DeleteLandingZoneOutput.error_to_json)])
let disable_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabledBaselineIdentifier =
         flag "enabled-baseline-identifier" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_baseline
           (Values.DisableBaselineInput.make ~enabledBaselineIdentifier ())
           (Some Values.DisableBaselineOutput.to_json)
           (Some Values.DisableBaselineOutput.error_to_json)])
let disable_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlIdentifier =
         flag "control-identifier" (optional string)
           ~doc:"STRING ControlIdentifier"
       and targetIdentifier =
         flag "target-identifier" (optional string)
           ~doc:"STRING TargetIdentifier"
       and enabledControlIdentifier =
         flag "enabled-control-identifier" (optional string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_control
           (Values.DisableControlInput.make ?controlIdentifier
              ?targetIdentifier ?enabledControlIdentifier ())
           (Some Values.DisableControlOutput.to_json)
           (Some Values.DisableControlOutput.error_to_json)])
let enable_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON EnabledBaselineParameters"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and baselineVersion =
         flag "baseline-version" (required string)
           ~doc:"STRING BaselineVersion"
       and baselineIdentifier =
         flag "baseline-identifier" (required string) ~doc:"STRING Arn"
       and targetIdentifier =
         flag "target-identifier" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_baseline
           (Values.EnableBaselineInput.make
              ?parameters:(Option.map
                             ~f:Values.EnabledBaselineParameters.of_json
                             parameters)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~baselineVersion ~baselineIdentifier ~targetIdentifier ())
           (Some Values.EnableBaselineOutput.to_json)
           (Some Values.EnableBaselineOutput.error_to_json)])
let enable_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON EnabledControlParameters"
       and controlIdentifier =
         flag "control-identifier" (required string)
           ~doc:"STRING ControlIdentifier"
       and targetIdentifier =
         flag "target-identifier" (required string)
           ~doc:"STRING TargetIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_control
           (Values.EnableControlInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?parameters:(Option.map
                             ~f:Values.EnabledControlParameters.of_json
                             parameters) ~controlIdentifier ~targetIdentifier
              ()) (Some Values.EnableControlOutput.to_json)
           (Some Values.EnableControlOutput.error_to_json)])
let get_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baselineIdentifier =
         flag "baseline-identifier" (required string)
           ~doc:"STRING BaselineArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_baseline
           (Values.GetBaselineInput.make ~baselineIdentifier ())
           (Some Values.GetBaselineOutput.to_json)
           (Some Values.GetBaselineOutput.error_to_json)])
let get_baseline_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationIdentifier =
         flag "operation-identifier" (required string)
           ~doc:"STRING OperationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_baseline_operation
           (Values.GetBaselineOperationInput.make ~operationIdentifier ())
           (Some Values.GetBaselineOperationOutput.to_json)
           (Some Values.GetBaselineOperationOutput.error_to_json)])
let get_control_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationIdentifier =
         flag "operation-identifier" (required string)
           ~doc:"STRING OperationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_control_operation
           (Values.GetControlOperationInput.make ~operationIdentifier ())
           (Some Values.GetControlOperationOutput.to_json)
           (Some Values.GetControlOperationOutput.error_to_json)])
let get_enabled_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabledBaselineIdentifier =
         flag "enabled-baseline-identifier" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_enabled_baseline
           (Values.GetEnabledBaselineInput.make ~enabledBaselineIdentifier ())
           (Some Values.GetEnabledBaselineOutput.to_json)
           (Some Values.GetEnabledBaselineOutput.error_to_json)])
let get_enabled_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabledControlIdentifier =
         flag "enabled-control-identifier" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_enabled_control
           (Values.GetEnabledControlInput.make ~enabledControlIdentifier ())
           (Some Values.GetEnabledControlOutput.to_json)
           (Some Values.GetEnabledControlOutput.error_to_json)])
let get_landing_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and landingZoneIdentifier =
         flag "landing-zone-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_landing_zone
           (Values.GetLandingZoneInput.make ~landingZoneIdentifier ())
           (Some Values.GetLandingZoneOutput.to_json)
           (Some Values.GetLandingZoneOutput.error_to_json)])
let get_landing_zone_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationIdentifier =
         flag "operation-identifier" (required string)
           ~doc:"STRING OperationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_landing_zone_operation
           (Values.GetLandingZoneOperationInput.make ~operationIdentifier ())
           (Some Values.GetLandingZoneOperationOutput.to_json)
           (Some Values.GetLandingZoneOperationOutput.error_to_json)])
let list_baselines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListBaselinesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_baselines
           (Values.ListBaselinesInput.make ?nextToken ?maxResults ())
           (Some Values.ListBaselinesOutput.to_json)
           (Some Values.ListBaselinesOutput.error_to_json)])
let list_control_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ControlOperationFilter"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListControlOperationsNextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListControlOperationsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_control_operations
           (Values.ListControlOperationsInput.make
              ?filter:(Option.map ~f:Values.ControlOperationFilter.of_json
                         filter) ?nextToken ?maxResults ())
           (Some Values.ListControlOperationsOutput.to_json)
           (Some Values.ListControlOperationsOutput.error_to_json)])
let list_enabled_baselines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON EnabledBaselineFilter"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListEnabledBaselinesNextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEnabledBaselinesMaxResults"
       and includeChildren =
         flag "include-children" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_enabled_baselines
           (Values.ListEnabledBaselinesInput.make
              ?filter:(Option.map ~f:Values.EnabledBaselineFilter.of_json
                         filter) ?nextToken ?maxResults ?includeChildren ())
           (Some Values.ListEnabledBaselinesOutput.to_json)
           (Some Values.ListEnabledBaselinesOutput.error_to_json)])
let list_enabled_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetIdentifier =
         flag "target-identifier" (optional string)
           ~doc:"STRING TargetIdentifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON EnabledControlFilter"
       and includeChildren =
         flag "include-children" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_enabled_controls
           (Values.ListEnabledControlsInput.make ?targetIdentifier ?nextToken
              ?maxResults
              ?filter:(Option.map ~f:Values.EnabledControlFilter.of_json
                         filter) ?includeChildren ())
           (Some Values.ListEnabledControlsOutput.to_json)
           (Some Values.ListEnabledControlsOutput.error_to_json)])
let list_landing_zone_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON LandingZoneOperationFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListLandingZoneOperationsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_landing_zone_operations
           (Values.ListLandingZoneOperationsInput.make
              ?filter:(Option.map
                         ~f:Values.LandingZoneOperationFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListLandingZoneOperationsOutput.to_json)
           (Some Values.ListLandingZoneOperationsOutput.error_to_json)])
let list_landing_zones =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListLandingZonesMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_landing_zones
           (Values.ListLandingZonesInput.make ?nextToken ?maxResults ())
           (Some Values.ListLandingZonesOutput.to_json)
           (Some Values.ListLandingZonesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let reset_enabled_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabledBaselineIdentifier =
         flag "enabled-baseline-identifier" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_enabled_baseline
           (Values.ResetEnabledBaselineInput.make ~enabledBaselineIdentifier
              ()) (Some Values.ResetEnabledBaselineOutput.to_json)
           (Some Values.ResetEnabledBaselineOutput.error_to_json)])
let reset_enabled_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabledControlIdentifier =
         flag "enabled-control-identifier" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_enabled_control
           (Values.ResetEnabledControlInput.make ~enabledControlIdentifier ())
           (Some Values.ResetEnabledControlOutput.to_json)
           (Some Values.ResetEnabledControlOutput.error_to_json)])
let reset_landing_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and landingZoneIdentifier =
         flag "landing-zone-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_landing_zone
           (Values.ResetLandingZoneInput.make ~landingZoneIdentifier ())
           (Some Values.ResetLandingZoneOutput.to_json)
           (Some Values.ResetLandingZoneOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_enabled_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON EnabledBaselineParameters"
       and baselineVersion =
         flag "baseline-version" (required string)
           ~doc:"STRING BaselineVersion"
       and enabledBaselineIdentifier =
         flag "enabled-baseline-identifier" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_enabled_baseline
           (Values.UpdateEnabledBaselineInput.make
              ?parameters:(Option.map
                             ~f:Values.EnabledBaselineParameters.of_json
                             parameters) ~baselineVersion
              ~enabledBaselineIdentifier ())
           (Some Values.UpdateEnabledBaselineOutput.to_json)
           (Some Values.UpdateEnabledBaselineOutput.error_to_json)])
let update_enabled_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (required json_arg)
           ~doc:"JSON EnabledControlParameters"
       and enabledControlIdentifier =
         flag "enabled-control-identifier" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_enabled_control
           (Values.UpdateEnabledControlInput.make
              ~parameters:(Values.EnabledControlParameters.of_json parameters)
              ~enabledControlIdentifier ())
           (Some Values.UpdateEnabledControlOutput.to_json)
           (Some Values.UpdateEnabledControlOutput.error_to_json)])
let update_landing_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and remediationTypes =
         flag "remediation-types" (optional json_arg)
           ~doc:"JSON RemediationTypes"
       and manifest =
         flag "manifest" (optional json_arg) ~doc:"JSON Manifest"
       and version =
         flag "version" (required string) ~doc:"STRING LandingZoneVersion"
       and landingZoneIdentifier =
         flag "landing-zone-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_landing_zone
           (Values.UpdateLandingZoneInput.make
              ?remediationTypes:(Option.map
                                   ~f:Values.RemediationTypes.of_json
                                   remediationTypes)
              ?manifest:(Option.map ~f:Values.Manifest.of_json manifest)
              ~version ~landingZoneIdentifier ())
           (Some Values.UpdateLandingZoneOutput.to_json)
           (Some Values.UpdateLandingZoneOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-landing-zone", create_landing_zone);
    ("delete-landing-zone", delete_landing_zone);
    ("disable-baseline", disable_baseline);
    ("disable-control", disable_control);
    ("enable-baseline", enable_baseline);
    ("enable-control", enable_control);
    ("get-baseline", get_baseline);
    ("get-baseline-operation", get_baseline_operation);
    ("get-control-operation", get_control_operation);
    ("get-enabled-baseline", get_enabled_baseline);
    ("get-enabled-control", get_enabled_control);
    ("get-landing-zone", get_landing_zone);
    ("get-landing-zone-operation", get_landing_zone_operation);
    ("list-baselines", list_baselines);
    ("list-control-operations", list_control_operations);
    ("list-enabled-baselines", list_enabled_baselines);
    ("list-enabled-controls", list_enabled_controls);
    ("list-landing-zone-operations", list_landing_zone_operations);
    ("list-landing-zones", list_landing_zones);
    ("list-tags-for-resource", list_tags_for_resource);
    ("reset-enabled-baseline", reset_enabled_baseline);
    ("reset-enabled-control", reset_enabled_control);
    ("reset-landing-zone", reset_landing_zone);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-enabled-baseline", update_enabled_baseline);
    ("update-enabled-control", update_enabled_control);
    ("update-landing-zone", update_landing_zone)]
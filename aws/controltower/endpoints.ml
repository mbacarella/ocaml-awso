(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateLandingZone: (CreateLandingZoneInput.t, CreateLandingZoneOutput.t,
  CreateLandingZoneOutput.error) t 
  | DeleteLandingZone: (DeleteLandingZoneInput.t, DeleteLandingZoneOutput.t,
  DeleteLandingZoneOutput.error) t 
  | DisableBaseline: (DisableBaselineInput.t, DisableBaselineOutput.t,
  DisableBaselineOutput.error) t 
  | DisableControl: (DisableControlInput.t, DisableControlOutput.t,
  DisableControlOutput.error) t 
  | EnableBaseline: (EnableBaselineInput.t, EnableBaselineOutput.t,
  EnableBaselineOutput.error) t 
  | EnableControl: (EnableControlInput.t, EnableControlOutput.t,
  EnableControlOutput.error) t 
  | GetBaseline: (GetBaselineInput.t, GetBaselineOutput.t,
  GetBaselineOutput.error) t 
  | GetBaselineOperation: (GetBaselineOperationInput.t,
  GetBaselineOperationOutput.t, GetBaselineOperationOutput.error) t 
  | GetControlOperation: (GetControlOperationInput.t,
  GetControlOperationOutput.t, GetControlOperationOutput.error) t 
  | GetEnabledBaseline: (GetEnabledBaselineInput.t,
  GetEnabledBaselineOutput.t, GetEnabledBaselineOutput.error) t 
  | GetEnabledControl: (GetEnabledControlInput.t, GetEnabledControlOutput.t,
  GetEnabledControlOutput.error) t 
  | GetLandingZone: (GetLandingZoneInput.t, GetLandingZoneOutput.t,
  GetLandingZoneOutput.error) t 
  | GetLandingZoneOperation: (GetLandingZoneOperationInput.t,
  GetLandingZoneOperationOutput.t, GetLandingZoneOperationOutput.error) t 
  | ListBaselines: (ListBaselinesInput.t, ListBaselinesOutput.t,
  ListBaselinesOutput.error) t 
  | ListControlOperations: (ListControlOperationsInput.t,
  ListControlOperationsOutput.t, ListControlOperationsOutput.error) t 
  | ListEnabledBaselines: (ListEnabledBaselinesInput.t,
  ListEnabledBaselinesOutput.t, ListEnabledBaselinesOutput.error) t 
  | ListEnabledControls: (ListEnabledControlsInput.t,
  ListEnabledControlsOutput.t, ListEnabledControlsOutput.error) t 
  | ListLandingZoneOperations: (ListLandingZoneOperationsInput.t,
  ListLandingZoneOperationsOutput.t, ListLandingZoneOperationsOutput.error) t
  
  | ListLandingZones: (ListLandingZonesInput.t, ListLandingZonesOutput.t,
  ListLandingZonesOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | ResetEnabledBaseline: (ResetEnabledBaselineInput.t,
  ResetEnabledBaselineOutput.t, ResetEnabledBaselineOutput.error) t 
  | ResetEnabledControl: (ResetEnabledControlInput.t,
  ResetEnabledControlOutput.t, ResetEnabledControlOutput.error) t 
  | ResetLandingZone: (ResetLandingZoneInput.t, ResetLandingZoneOutput.t,
  ResetLandingZoneOutput.error) t 
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
  | UpdateEnabledBaseline: (UpdateEnabledBaselineInput.t,
  UpdateEnabledBaselineOutput.t, UpdateEnabledBaselineOutput.error) t 
  | UpdateEnabledControl: (UpdateEnabledControlInput.t,
  UpdateEnabledControlOutput.t, UpdateEnabledControlOutput.error) t 
  | UpdateLandingZone: (UpdateLandingZoneInput.t, UpdateLandingZoneOutput.t,
  UpdateLandingZoneOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateLandingZone -> `POST
  | DeleteLandingZone -> `POST
  | DisableBaseline -> `POST
  | DisableControl -> `POST
  | EnableBaseline -> `POST
  | EnableControl -> `POST
  | GetBaseline -> `POST
  | GetBaselineOperation -> `POST
  | GetControlOperation -> `POST
  | GetEnabledBaseline -> `POST
  | GetEnabledControl -> `POST
  | GetLandingZone -> `POST
  | GetLandingZoneOperation -> `POST
  | ListBaselines -> `POST
  | ListControlOperations -> `POST
  | ListEnabledBaselines -> `POST
  | ListEnabledControls -> `POST
  | ListLandingZoneOperations -> `POST
  | ListLandingZones -> `POST
  | ListTagsForResource -> `GET
  | ResetEnabledBaseline -> `POST
  | ResetEnabledControl -> `POST
  | ResetLandingZone -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateEnabledBaseline -> `POST
  | UpdateEnabledControl -> `POST
  | UpdateLandingZone -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateLandingZone ->
          (Format.kasprintf Uri.of_string) "/create-landingzone"
      | DeleteLandingZone ->
          (Format.kasprintf Uri.of_string) "/delete-landingzone"
      | DisableBaseline ->
          (Format.kasprintf Uri.of_string) "/disable-baseline"
      | DisableControl -> (Format.kasprintf Uri.of_string) "/disable-control"
      | EnableBaseline -> (Format.kasprintf Uri.of_string) "/enable-baseline"
      | EnableControl -> (Format.kasprintf Uri.of_string) "/enable-control"
      | GetBaseline -> (Format.kasprintf Uri.of_string) "/get-baseline"
      | GetBaselineOperation ->
          (Format.kasprintf Uri.of_string) "/get-baseline-operation"
      | GetControlOperation ->
          (Format.kasprintf Uri.of_string) "/get-control-operation"
      | GetEnabledBaseline ->
          (Format.kasprintf Uri.of_string) "/get-enabled-baseline"
      | GetEnabledControl ->
          (Format.kasprintf Uri.of_string) "/get-enabled-control"
      | GetLandingZone -> (Format.kasprintf Uri.of_string) "/get-landingzone"
      | GetLandingZoneOperation ->
          (Format.kasprintf Uri.of_string) "/get-landingzone-operation"
      | ListBaselines -> (Format.kasprintf Uri.of_string) "/list-baselines"
      | ListControlOperations ->
          (Format.kasprintf Uri.of_string) "/list-control-operations"
      | ListEnabledBaselines ->
          (Format.kasprintf Uri.of_string) "/list-enabled-baselines"
      | ListEnabledControls ->
          (Format.kasprintf Uri.of_string) "/list-enabled-controls"
      | ListLandingZoneOperations ->
          (Format.kasprintf Uri.of_string) "/list-landingzone-operations"
      | ListLandingZones ->
          (Format.kasprintf Uri.of_string) "/list-landingzones"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (Arn.to_header x.ListTagsForResourceInput.resourceArn)
      | ResetEnabledBaseline ->
          (Format.kasprintf Uri.of_string) "/reset-enabled-baseline"
      | ResetEnabledControl ->
          (Format.kasprintf Uri.of_string) "/reset-enabled-control"
      | ResetLandingZone ->
          (Format.kasprintf Uri.of_string) "/reset-landingzone"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (Arn.to_header x.TagResourceInput.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (Arn.to_header x.UntagResourceInput.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeys.to_header x.tagKeys))])
      | UpdateEnabledBaseline ->
          (Format.kasprintf Uri.of_string) "/update-enabled-baseline"
      | UpdateEnabledControl ->
          (Format.kasprintf Uri.of_string) "/update-enabled-control"
      | UpdateLandingZone ->
          (Format.kasprintf Uri.of_string) "/update-landingzone")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateLandingZone ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("version",
                           (LandingZoneVersion.to_value
                              req.CreateLandingZoneInput.version));
                      Option.map req.CreateLandingZoneInput.remediationTypes
                        ~f:(fun x ->
                              ("remediationTypes",
                                (RemediationTypes.to_value x)));
                      Option.map req.CreateLandingZoneInput.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)));
                      Option.map req.CreateLandingZoneInput.manifest
                        ~f:(fun x -> ("manifest", (Manifest.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteLandingZone ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("landingZoneIdentifier",
                           (String_.to_value
                              req.DeleteLandingZoneInput.landingZoneIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DisableBaseline ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("enabledBaselineIdentifier",
                           (Arn.to_value
                              req.DisableBaselineInput.enabledBaselineIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DisableControl ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DisableControlInput.controlIdentifier
                         ~f:(fun x ->
                               ("controlIdentifier",
                                 (ControlIdentifier.to_value x)));
                      Option.map req.DisableControlInput.targetIdentifier
                        ~f:(fun x ->
                              ("targetIdentifier",
                                (TargetIdentifier.to_value x)));
                      Option.map
                        req.DisableControlInput.enabledControlIdentifier
                        ~f:(fun x ->
                              ("enabledControlIdentifier", (Arn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | EnableBaseline ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("baselineVersion",
                           (BaselineVersion.to_value
                              req.EnableBaselineInput.baselineVersion));
                      Option.map req.EnableBaselineInput.parameters
                        ~f:(fun x ->
                              ("parameters",
                                (EnabledBaselineParameters.to_value x)));
                      Some
                        ("baselineIdentifier",
                          (Arn.to_value
                             req.EnableBaselineInput.baselineIdentifier));
                      Some
                        ("targetIdentifier",
                          (Arn.to_value
                             req.EnableBaselineInput.targetIdentifier));
                      Option.map req.EnableBaselineInput.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | EnableControl ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("controlIdentifier",
                           (ControlIdentifier.to_value
                              req.EnableControlInput.controlIdentifier));
                      Some
                        ("targetIdentifier",
                          (TargetIdentifier.to_value
                             req.EnableControlInput.targetIdentifier));
                      Option.map req.EnableControlInput.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)));
                      Option.map req.EnableControlInput.parameters
                        ~f:(fun x ->
                              ("parameters",
                                (EnabledControlParameters.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetBaseline ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("baselineIdentifier",
                           (BaselineArn.to_value
                              req.GetBaselineInput.baselineIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetBaselineOperation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("operationIdentifier",
                           (OperationIdentifier.to_value
                              req.GetBaselineOperationInput.operationIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetControlOperation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("operationIdentifier",
                           (OperationIdentifier.to_value
                              req.GetControlOperationInput.operationIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetEnabledBaseline ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("enabledBaselineIdentifier",
                           (Arn.to_value
                              req.GetEnabledBaselineInput.enabledBaselineIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetEnabledControl ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("enabledControlIdentifier",
                           (Arn.to_value
                              req.GetEnabledControlInput.enabledControlIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetLandingZone ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("landingZoneIdentifier",
                           (String_.to_value
                              req.GetLandingZoneInput.landingZoneIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetLandingZoneOperation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("operationIdentifier",
                           (OperationIdentifier.to_value
                              req.GetLandingZoneOperationInput.operationIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListBaselines ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListBaselinesInput.nextToken
                         ~f:(fun x -> ("nextToken", (String_.to_value x)));
                      Option.map req.ListBaselinesInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListBaselinesMaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListControlOperations ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListControlOperationsInput.filter
                         ~f:(fun x ->
                               ("filter",
                                 (ControlOperationFilter.to_value x)));
                      Option.map req.ListControlOperationsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken",
                                (ListControlOperationsNextToken.to_value x)));
                      Option.map req.ListControlOperationsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListControlOperationsMaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListEnabledBaselines ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListEnabledBaselinesInput.filter
                         ~f:(fun x ->
                               ("filter", (EnabledBaselineFilter.to_value x)));
                      Option.map req.ListEnabledBaselinesInput.nextToken
                        ~f:(fun x ->
                              ("nextToken",
                                (ListEnabledBaselinesNextToken.to_value x)));
                      Option.map req.ListEnabledBaselinesInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListEnabledBaselinesMaxResults.to_value x)));
                      Option.map
                        req.ListEnabledBaselinesInput.includeChildren
                        ~f:(fun x ->
                              ("includeChildren", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListEnabledControls ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListEnabledControlsInput.targetIdentifier
                         ~f:(fun x ->
                               ("targetIdentifier",
                                 (TargetIdentifier.to_value x)));
                      Option.map req.ListEnabledControlsInput.nextToken
                        ~f:(fun x -> ("nextToken", (String_.to_value x)));
                      Option.map req.ListEnabledControlsInput.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListEnabledControlsInput.filter
                        ~f:(fun x ->
                              ("filter", (EnabledControlFilter.to_value x)));
                      Option.map req.ListEnabledControlsInput.includeChildren
                        ~f:(fun x ->
                              ("includeChildren", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListLandingZoneOperations ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListLandingZoneOperationsInput.filter
                         ~f:(fun x ->
                               ("filter",
                                 (LandingZoneOperationFilter.to_value x)));
                      Option.map req.ListLandingZoneOperationsInput.nextToken
                        ~f:(fun x -> ("nextToken", (String_.to_value x)));
                      Option.map
                        req.ListLandingZoneOperationsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListLandingZoneOperationsMaxResults.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListLandingZones ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListLandingZonesInput.nextToken
                         ~f:(fun x -> ("nextToken", (String_.to_value x)));
                      Option.map req.ListLandingZonesInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListLandingZonesMaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ResetEnabledBaseline ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("enabledBaselineIdentifier",
                           (Arn.to_value
                              req.ResetEnabledBaselineInput.enabledBaselineIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ResetEnabledControl ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("enabledControlIdentifier",
                           (Arn.to_value
                              req.ResetEnabledControlInput.enabledControlIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ResetLandingZone ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("landingZoneIdentifier",
                           (String_.to_value
                              req.ResetLandingZoneInput.landingZoneIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagMap.to_value req.TagResourceInput.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateEnabledBaseline ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("baselineVersion",
                           (BaselineVersion.to_value
                              req.UpdateEnabledBaselineInput.baselineVersion));
                      Option.map req.UpdateEnabledBaselineInput.parameters
                        ~f:(fun x ->
                              ("parameters",
                                (EnabledBaselineParameters.to_value x)));
                      Some
                        ("enabledBaselineIdentifier",
                          (Arn.to_value
                             req.UpdateEnabledBaselineInput.enabledBaselineIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateEnabledControl ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("parameters",
                           (EnabledControlParameters.to_value
                              req.UpdateEnabledControlInput.parameters));
                      Some
                        ("enabledControlIdentifier",
                          (Arn.to_value
                             req.UpdateEnabledControlInput.enabledControlIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateLandingZone ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("version",
                           (LandingZoneVersion.to_value
                              req.UpdateLandingZoneInput.version));
                      Option.map req.UpdateLandingZoneInput.remediationTypes
                        ~f:(fun x ->
                              ("remediationTypes",
                                (RemediationTypes.to_value x)));
                      Some
                        ("landingZoneIdentifier",
                          (String_.to_value
                             req.UpdateLandingZoneInput.landingZoneIdentifier));
                      Option.map req.UpdateLandingZoneInput.manifest
                        ~f:(fun x -> ("manifest", (Manifest.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateLandingZone ->
      if is_success
      then Ok (CreateLandingZoneOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CreateLandingZoneOutput.error_of_json))
  | DeleteLandingZone ->
      if is_success
      then Ok (DeleteLandingZoneOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some DeleteLandingZoneOutput.error_of_json))
  | DisableBaseline ->
      if is_success
      then Ok (DisableBaselineOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DisableBaselineOutput.error_of_json))
  | DisableControl ->
      if is_success
      then Ok (DisableControlOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DisableControlOutput.error_of_json))
  | EnableBaseline ->
      if is_success
      then Ok (EnableBaselineOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some EnableBaselineOutput.error_of_json))
  | EnableControl ->
      if is_success
      then Ok (EnableControlOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some EnableControlOutput.error_of_json))
  | GetBaseline ->
      if is_success
      then Ok (GetBaselineOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetBaselineOutput.error_of_json))
  | GetBaselineOperation ->
      if is_success
      then Ok (GetBaselineOperationOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetBaselineOperationOutput.error_of_json))
  | GetControlOperation ->
      if is_success
      then Ok (GetControlOperationOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetControlOperationOutput.error_of_json))
  | GetEnabledBaseline ->
      if is_success
      then Ok (GetEnabledBaselineOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetEnabledBaselineOutput.error_of_json))
  | GetEnabledControl ->
      if is_success
      then Ok (GetEnabledControlOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetEnabledControlOutput.error_of_json))
  | GetLandingZone ->
      if is_success
      then Ok (GetLandingZoneOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetLandingZoneOutput.error_of_json))
  | GetLandingZoneOperation ->
      if is_success
      then Ok (GetLandingZoneOperationOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetLandingZoneOperationOutput.error_of_json))
  | ListBaselines ->
      if is_success
      then Ok (ListBaselinesOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListBaselinesOutput.error_of_json))
  | ListControlOperations ->
      if is_success
      then Ok (ListControlOperationsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListControlOperationsOutput.error_of_json))
  | ListEnabledBaselines ->
      if is_success
      then Ok (ListEnabledBaselinesOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListEnabledBaselinesOutput.error_of_json))
  | ListEnabledControls ->
      if is_success
      then Ok (ListEnabledControlsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListEnabledControlsOutput.error_of_json))
  | ListLandingZoneOperations ->
      if is_success
      then
        Ok (ListLandingZoneOperationsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListLandingZoneOperationsOutput.error_of_json))
  | ListLandingZones ->
      if is_success
      then Ok (ListLandingZonesOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListLandingZonesOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | ResetEnabledBaseline ->
      if is_success
      then Ok (ResetEnabledBaselineOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ResetEnabledBaselineOutput.error_of_json))
  | ResetEnabledControl ->
      if is_success
      then Ok (ResetEnabledControlOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ResetEnabledControlOutput.error_of_json))
  | ResetLandingZone ->
      if is_success
      then Ok (ResetLandingZoneOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ResetLandingZoneOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
  | UpdateEnabledBaseline ->
      if is_success
      then Ok (UpdateEnabledBaselineOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some UpdateEnabledBaselineOutput.error_of_json))
  | UpdateEnabledControl ->
      if is_success
      then Ok (UpdateEnabledControlOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some UpdateEnabledControlOutput.error_of_json))
  | UpdateLandingZone ->
      if is_success
      then Ok (UpdateLandingZoneOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some UpdateLandingZoneOutput.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let assume_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArns =
         flag "policy-arns" (optional json_arg)
           ~doc:"JSON policyDescriptorListType"
       and policy =
         flag "policy" (optional string)
           ~doc:"STRING unrestrictedSessionPolicyDocumentType"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT roleDurationSecondsType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and transitiveTagKeys =
         flag "transitive-tag-keys" (optional json_arg)
           ~doc:"JSON tagKeyListType"
       and externalId =
         flag "external-id" (optional string) ~doc:"STRING externalIdType"
       and serialNumber =
         flag "serial-number" (optional string)
           ~doc:"STRING serialNumberType"
       and tokenCode =
         flag "token-code" (optional string) ~doc:"STRING tokenCodeType"
       and sourceIdentity =
         flag "source-identity" (optional string)
           ~doc:"STRING sourceIdentityType"
       and providedContexts =
         flag "provided-contexts" (optional json_arg)
           ~doc:"JSON ProvidedContextsListType"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING arnType"
       and roleSessionName =
         flag "role-session-name" (required string)
           ~doc:"STRING roleSessionNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_role
           (Values.AssumeRoleRequest.make
              ?policyArns:(Option.map
                             ~f:Values.PolicyDescriptorListType.of_json
                             policyArns) ?policy ?durationSeconds
              ?tags:(Option.map ~f:Values.TagListType.of_json tags)
              ?transitiveTagKeys:(Option.map ~f:Values.TagKeyListType.of_json
                                    transitiveTagKeys) ?externalId
              ?serialNumber ?tokenCode ?sourceIdentity
              ?providedContexts:(Option.map
                                   ~f:Values.ProvidedContextsListType.of_json
                                   providedContexts) ~roleArn
              ~roleSessionName ()) (Some Values.AssumeRoleResponse.to_json)
           (Some Values.AssumeRoleResponse.error_to_json)])
let assume_role_with_s_a_m_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyArns =
         flag "policy-arns" (optional json_arg)
           ~doc:"JSON policyDescriptorListType"
       and policy =
         flag "policy" (optional string)
           ~doc:"STRING sessionPolicyDocumentType"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT roleDurationSecondsType"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING arnType"
       and principalArn =
         flag "principal-arn" (required string) ~doc:"STRING arnType"
       and sAMLAssertion =
         flag "s-a-m-l-assertion" (required string)
           ~doc:"STRING SAMLAssertionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_role_with_s_a_m_l
           (Values.AssumeRoleWithSAMLRequest.make
              ?policyArns:(Option.map
                             ~f:Values.PolicyDescriptorListType.of_json
                             policyArns) ?policy ?durationSeconds ~roleArn
              ~principalArn ~sAMLAssertion ())
           (Some Values.AssumeRoleWithSAMLResponse.to_json)
           (Some Values.AssumeRoleWithSAMLResponse.error_to_json)])
let assume_role_with_web_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and providerId =
         flag "provider-id" (optional string) ~doc:"STRING urlType"
       and policyArns =
         flag "policy-arns" (optional json_arg)
           ~doc:"JSON policyDescriptorListType"
       and policy =
         flag "policy" (optional string)
           ~doc:"STRING sessionPolicyDocumentType"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT roleDurationSecondsType"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING arnType"
       and roleSessionName =
         flag "role-session-name" (required string)
           ~doc:"STRING roleSessionNameType"
       and webIdentityToken =
         flag "web-identity-token" (required string)
           ~doc:"STRING clientTokenType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_role_with_web_identity
           (Values.AssumeRoleWithWebIdentityRequest.make ?providerId
              ?policyArns:(Option.map
                             ~f:Values.PolicyDescriptorListType.of_json
                             policyArns) ?policy ?durationSeconds ~roleArn
              ~roleSessionName ~webIdentityToken ())
           (Some Values.AssumeRoleWithWebIdentityResponse.to_json)
           (Some Values.AssumeRoleWithWebIdentityResponse.error_to_json)])
let assume_root =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT RootDurationSecondsType"
       and targetPrincipal =
         flag "target-principal" (required string)
           ~doc:"STRING TargetPrincipalType"
       and taskPolicyArn =
         flag "task-policy-arn" (required json_arg)
           ~doc:"JSON PolicyDescriptorType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_root
           (Values.AssumeRootRequest.make ?durationSeconds ~targetPrincipal
              ~taskPolicyArn:(Values.PolicyDescriptorType.of_json
                                taskPolicyArn) ())
           (Some Values.AssumeRootResponse.to_json)
           (Some Values.AssumeRootResponse.error_to_json)])
let decode_authorization_message =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encodedMessage =
         flag "encoded-message" (required string)
           ~doc:"STRING encodedMessageType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.decode_authorization_message
           (Values.DecodeAuthorizationMessageRequest.make ~encodedMessage ())
           (Some Values.DecodeAuthorizationMessageResponse.to_json)
           (Some Values.DecodeAuthorizationMessageResponse.error_to_json)])
let get_access_key_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessKeyId =
         flag "access-key-id" (required string) ~doc:"STRING accessKeyIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_key_info
           (Values.GetAccessKeyInfoRequest.make ~accessKeyId ())
           (Some Values.GetAccessKeyInfoResponse.to_json)
           (Some Values.GetAccessKeyInfoResponse.error_to_json)])
let get_caller_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_caller_identity (Values.GetCallerIdentityRequest.make ())
           (Some Values.GetCallerIdentityResponse.to_json)
           (Some Values.GetCallerIdentityResponse.error_to_json)])
let get_delegated_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tradeInToken =
         flag "trade-in-token" (required string)
           ~doc:"STRING tradeInTokenType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_delegated_access_token
           (Values.GetDelegatedAccessTokenRequest.make ~tradeInToken ())
           (Some Values.GetDelegatedAccessTokenResponse.to_json)
           (Some Values.GetDelegatedAccessTokenResponse.error_to_json)])
let get_federation_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy =
         flag "policy" (optional string)
           ~doc:"STRING sessionPolicyDocumentType"
       and policyArns =
         flag "policy-arns" (optional json_arg)
           ~doc:"JSON policyDescriptorListType"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT durationSecondsType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and name = flag "name" (required string) ~doc:"STRING userNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_federation_token
           (Values.GetFederationTokenRequest.make ?policy
              ?policyArns:(Option.map
                             ~f:Values.PolicyDescriptorListType.of_json
                             policyArns) ?durationSeconds
              ?tags:(Option.map ~f:Values.TagListType.of_json tags) ~name ())
           (Some Values.GetFederationTokenResponse.to_json)
           (Some Values.GetFederationTokenResponse.error_to_json)])
let get_session_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT durationSecondsType"
       and serialNumber =
         flag "serial-number" (optional string)
           ~doc:"STRING serialNumberType"
       and tokenCode =
         flag "token-code" (optional string) ~doc:"STRING tokenCodeType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session_token
           (Values.GetSessionTokenRequest.make ?durationSeconds ?serialNumber
              ?tokenCode ()) (Some Values.GetSessionTokenResponse.to_json)
           (Some Values.GetSessionTokenResponse.error_to_json)])
let get_web_identity_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT webIdentityTokenDurationSecondsType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON tagListType"
       and audience =
         flag "audience" (required json_arg)
           ~doc:"JSON webIdentityTokenAudienceListType"
       and signingAlgorithm =
         flag "signing-algorithm" (required string)
           ~doc:"STRING jwtAlgorithmType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_web_identity_token
           (Values.GetWebIdentityTokenRequest.make ?durationSeconds
              ?tags:(Option.map ~f:Values.TagListType.of_json tags)
              ~audience:(Values.WebIdentityTokenAudienceListType.of_json
                           audience) ~signingAlgorithm ())
           (Some Values.GetWebIdentityTokenResponse.to_json)
           (Some Values.GetWebIdentityTokenResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("assume-role", assume_role);
    ("assume-role-with-s-a-m-l", assume_role_with_s_a_m_l);
    ("assume-role-with-web-identity", assume_role_with_web_identity);
    ("assume-root", assume_root);
    ("decode-authorization-message", decode_authorization_message);
    ("get-access-key-info", get_access_key_info);
    ("get-caller-identity", get_caller_identity);
    ("get-delegated-access-token", get_delegated_access_token);
    ("get-federation-token", get_federation_token);
    ("get-session-token", get_session_token);
    ("get-web-identity-token", get_web_identity_token)]
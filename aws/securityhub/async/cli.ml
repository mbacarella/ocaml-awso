(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_administrator_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and administratorId =
         flag "administrator-id" (required string)
           ~doc:"STRING NonEmptyString"
       and invitationId =
         flag "invitation-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_administrator_invitation
           (Values.AcceptAdministratorInvitationRequest.make ~administratorId
              ~invitationId ())
           (Some Values.AcceptAdministratorInvitationResponse.to_json)
           (Some Values.AcceptAdministratorInvitationResponse.error_to_json)])
let accept_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and masterId =
         flag "master-id" (required string) ~doc:"STRING NonEmptyString"
       and invitationId =
         flag "invitation-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_invitation
           (Values.AcceptInvitationRequest.make ~masterId ~invitationId ())
           (Some Values.AcceptInvitationResponse.to_json)
           (Some Values.AcceptInvitationResponse.error_to_json)])
let batch_disable_standards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and standardsSubscriptionArns =
         flag "standards-subscription-arns" (required json_arg)
           ~doc:"JSON StandardsSubscriptionArns" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disable_standards
           (Values.BatchDisableStandardsRequest.make
              ~standardsSubscriptionArns:(Values.StandardsSubscriptionArns.of_json
                                            standardsSubscriptionArns) ())
           (Some Values.BatchDisableStandardsResponse.to_json)
           (Some Values.BatchDisableStandardsResponse.error_to_json)])
let batch_enable_standards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and standardsSubscriptionRequests =
         flag "standards-subscription-requests" (required json_arg)
           ~doc:"JSON StandardsSubscriptionRequests" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_enable_standards
           (Values.BatchEnableStandardsRequest.make
              ~standardsSubscriptionRequests:(Values.StandardsSubscriptionRequests.of_json
                                                standardsSubscriptionRequests)
              ()) (Some Values.BatchEnableStandardsResponse.to_json)
           (Some Values.BatchEnableStandardsResponse.error_to_json)])
let batch_import_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and findings =
         flag "findings" (required json_arg)
           ~doc:"JSON BatchImportFindingsRequestFindingList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_import_findings
           (Values.BatchImportFindingsRequest.make
              ~findings:(Values.BatchImportFindingsRequestFindingList.of_json
                           findings) ())
           (Some Values.BatchImportFindingsResponse.to_json)
           (Some Values.BatchImportFindingsResponse.error_to_json)])
let batch_update_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and note = flag "note" (optional json_arg) ~doc:"JSON NoteUpdate"
       and severity =
         flag "severity" (optional json_arg) ~doc:"JSON SeverityUpdate"
       and verificationState =
         flag "verification-state" (optional json_arg)
           ~doc:"JSON VerificationState"
       and confidence =
         flag "confidence" (optional int) ~doc:"INT RatioScale"
       and criticality =
         flag "criticality" (optional int) ~doc:"INT RatioScale"
       and types = flag "types" (optional json_arg) ~doc:"JSON TypeList"
       and userDefinedFields =
         flag "user-defined-fields" (optional json_arg) ~doc:"JSON FieldMap"
       and workflow =
         flag "workflow" (optional json_arg) ~doc:"JSON WorkflowUpdate"
       and relatedFindings =
         flag "related-findings" (optional json_arg)
           ~doc:"JSON RelatedFindingList"
       and findingIdentifiers =
         flag "finding-identifiers" (required json_arg)
           ~doc:"JSON AwsSecurityFindingIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_findings
           (Values.BatchUpdateFindingsRequest.make
              ?note:(Option.map ~f:Values.NoteUpdate.of_json note)
              ?severity:(Option.map ~f:Values.SeverityUpdate.of_json severity)
              ?verificationState:(Option.map
                                    ~f:Values.VerificationState.of_json
                                    verificationState) ?confidence
              ?criticality
              ?types:(Option.map ~f:Values.TypeList.of_json types)
              ?userDefinedFields:(Option.map ~f:Values.FieldMap.of_json
                                    userDefinedFields)
              ?workflow:(Option.map ~f:Values.WorkflowUpdate.of_json workflow)
              ?relatedFindings:(Option.map
                                  ~f:Values.RelatedFindingList.of_json
                                  relatedFindings)
              ~findingIdentifiers:(Values.AwsSecurityFindingIdentifierList.of_json
                                     findingIdentifiers) ())
           (Some Values.BatchUpdateFindingsResponse.to_json)
           (Some Values.BatchUpdateFindingsResponse.error_to_json)])
let create_action_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NonEmptyString"
       and description =
         flag "description" (required string) ~doc:"STRING NonEmptyString"
       and id = flag "id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_action_target
           (Values.CreateActionTargetRequest.make ~name ~description ~id ())
           (Some Values.CreateActionTargetResponse.to_json)
           (Some Values.CreateActionTargetResponse.error_to_json)])
let create_finding_aggregator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regions =
         flag "regions" (optional json_arg) ~doc:"JSON StringList"
       and regionLinkingMode =
         flag "region-linking-mode" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_finding_aggregator
           (Values.CreateFindingAggregatorRequest.make
              ?regions:(Option.map ~f:Values.StringList.of_json regions)
              ~regionLinkingMode ())
           (Some Values.CreateFindingAggregatorResponse.to_json)
           (Some Values.CreateFindingAggregatorResponse.error_to_json)])
let create_insight =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING NonEmptyString"
       and filters =
         flag "filters" (required json_arg)
           ~doc:"JSON AwsSecurityFindingFilters"
       and groupByAttribute =
         flag "group-by-attribute" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_insight
           (Values.CreateInsightRequest.make ~name
              ~filters:(Values.AwsSecurityFindingFilters.of_json filters)
              ~groupByAttribute ())
           (Some Values.CreateInsightResponse.to_json)
           (Some Values.CreateInsightResponse.error_to_json)])
let create_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountDetails =
         flag "account-details" (required json_arg)
           ~doc:"JSON AccountDetailsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_members
           (Values.CreateMembersRequest.make
              ~accountDetails:(Values.AccountDetailsList.of_json
                                 accountDetails) ())
           (Some Values.CreateMembersResponse.to_json)
           (Some Values.CreateMembersResponse.error_to_json)])
let decline_invitations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.decline_invitations
           (Values.DeclineInvitationsRequest.make
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.DeclineInvitationsResponse.to_json)
           (Some Values.DeclineInvitationsResponse.error_to_json)])
let delete_action_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionTargetArn =
         flag "action-target-arn" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_action_target
           (Values.DeleteActionTargetRequest.make ~actionTargetArn ())
           (Some Values.DeleteActionTargetResponse.to_json)
           (Some Values.DeleteActionTargetResponse.error_to_json)])
let delete_finding_aggregator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and findingAggregatorArn =
         flag "finding-aggregator-arn" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_finding_aggregator
           (Values.DeleteFindingAggregatorRequest.make ~findingAggregatorArn
              ()) (Some Values.DeleteFindingAggregatorResponse.to_json)
           (Some Values.DeleteFindingAggregatorResponse.error_to_json)])
let delete_insight =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and insightArn =
         flag "insight-arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_insight
           (Values.DeleteInsightRequest.make ~insightArn ())
           (Some Values.DeleteInsightResponse.to_json)
           (Some Values.DeleteInsightResponse.error_to_json)])
let delete_invitations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_invitations
           (Values.DeleteInvitationsRequest.make
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.DeleteInvitationsResponse.to_json)
           (Some Values.DeleteInvitationsResponse.error_to_json)])
let delete_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_members
           (Values.DeleteMembersRequest.make
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.DeleteMembersResponse.to_json)
           (Some Values.DeleteMembersResponse.error_to_json)])
let describe_action_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionTargetArns =
         flag "action-target-arns" (optional json_arg) ~doc:"JSON ArnList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_action_targets
           (Values.DescribeActionTargetsRequest.make
              ?actionTargetArns:(Option.map ~f:Values.ArnList.of_json
                                   actionTargetArns) ?nextToken ?maxResults
              ()) (Some Values.DescribeActionTargetsResponse.to_json)
           (Some Values.DescribeActionTargetsResponse.error_to_json)])
let describe_hub =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hubArn =
         flag "hub-arn" (optional string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_hub (Values.DescribeHubRequest.make ?hubArn ())
           (Some Values.DescribeHubResponse.to_json)
           (Some Values.DescribeHubResponse.error_to_json)])
let describe_organization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_organization_configuration
           (Values.DescribeOrganizationConfigurationRequest.make ())
           (Some Values.DescribeOrganizationConfigurationResponse.to_json)
           (Some
              Values.DescribeOrganizationConfigurationResponse.error_to_json)])
let describe_products =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and productArn =
         flag "product-arn" (optional string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_products
           (Values.DescribeProductsRequest.make ?nextToken ?maxResults
              ?productArn ()) (Some Values.DescribeProductsResponse.to_json)
           (Some Values.DescribeProductsResponse.error_to_json)])
let describe_standards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_standards
           (Values.DescribeStandardsRequest.make ?nextToken ?maxResults ())
           (Some Values.DescribeStandardsResponse.to_json)
           (Some Values.DescribeStandardsResponse.error_to_json)])
let describe_standards_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and standardsSubscriptionArn =
         flag "standards-subscription-arn" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_standards_controls
           (Values.DescribeStandardsControlsRequest.make ?nextToken
              ?maxResults ~standardsSubscriptionArn ())
           (Some Values.DescribeStandardsControlsResponse.to_json)
           (Some Values.DescribeStandardsControlsResponse.error_to_json)])
let disable_import_findings_for_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and productSubscriptionArn =
         flag "product-subscription-arn" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_import_findings_for_product
           (Values.DisableImportFindingsForProductRequest.make
              ~productSubscriptionArn ())
           (Some Values.DisableImportFindingsForProductResponse.to_json)
           (Some Values.DisableImportFindingsForProductResponse.error_to_json)])
let disable_organization_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminAccountId =
         flag "admin-account-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_organization_admin_account
           (Values.DisableOrganizationAdminAccountRequest.make
              ~adminAccountId ())
           (Some Values.DisableOrganizationAdminAccountResponse.to_json)
           (Some Values.DisableOrganizationAdminAccountResponse.error_to_json)])
let disable_security_hub =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_security_hub (Values.DisableSecurityHubRequest.make ())
           (Some Values.DisableSecurityHubResponse.to_json)
           (Some Values.DisableSecurityHubResponse.error_to_json)])
let disassociate_from_administrator_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_from_administrator_account
           (Values.DisassociateFromAdministratorAccountRequest.make ())
           (Some Values.DisassociateFromAdministratorAccountResponse.to_json)
           (Some
              Values.DisassociateFromAdministratorAccountResponse.error_to_json)])
let disassociate_from_master_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_from_master_account
           (Values.DisassociateFromMasterAccountRequest.make ())
           (Some Values.DisassociateFromMasterAccountResponse.to_json)
           (Some Values.DisassociateFromMasterAccountResponse.error_to_json)])
let disassociate_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_members
           (Values.DisassociateMembersRequest.make
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.DisassociateMembersResponse.to_json)
           (Some Values.DisassociateMembersResponse.error_to_json)])
let enable_import_findings_for_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and productArn =
         flag "product-arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_import_findings_for_product
           (Values.EnableImportFindingsForProductRequest.make ~productArn ())
           (Some Values.EnableImportFindingsForProductResponse.to_json)
           (Some Values.EnableImportFindingsForProductResponse.error_to_json)])
let enable_organization_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminAccountId =
         flag "admin-account-id" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_organization_admin_account
           (Values.EnableOrganizationAdminAccountRequest.make ~adminAccountId
              ())
           (Some Values.EnableOrganizationAdminAccountResponse.to_json)
           (Some Values.EnableOrganizationAdminAccountResponse.error_to_json)])
let enable_security_hub =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and enableDefaultStandards =
         flag "enable-default-standards" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_security_hub
           (Values.EnableSecurityHubRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?enableDefaultStandards ())
           (Some Values.EnableSecurityHubResponse.to_json)
           (Some Values.EnableSecurityHubResponse.error_to_json)])
let get_administrator_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_administrator_account
           (Values.GetAdministratorAccountRequest.make ())
           (Some Values.GetAdministratorAccountResponse.to_json)
           (Some Values.GetAdministratorAccountResponse.error_to_json)])
let get_enabled_standards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and standardsSubscriptionArns =
         flag "standards-subscription-arns" (optional json_arg)
           ~doc:"JSON StandardsSubscriptionArns"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_enabled_standards
           (Values.GetEnabledStandardsRequest.make
              ?standardsSubscriptionArns:(Option.map
                                            ~f:Values.StandardsSubscriptionArns.of_json
                                            standardsSubscriptionArns)
              ?nextToken ?maxResults ())
           (Some Values.GetEnabledStandardsResponse.to_json)
           (Some Values.GetEnabledStandardsResponse.error_to_json)])
let get_finding_aggregator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and findingAggregatorArn =
         flag "finding-aggregator-arn" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_finding_aggregator
           (Values.GetFindingAggregatorRequest.make ~findingAggregatorArn ())
           (Some Values.GetFindingAggregatorResponse.to_json)
           (Some Values.GetFindingAggregatorResponse.error_to_json)])
let get_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON AwsSecurityFindingFilters"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortCriteria"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_findings
           (Values.GetFindingsRequest.make
              ?filters:(Option.map
                          ~f:Values.AwsSecurityFindingFilters.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortCriteria.of_json
                               sortCriteria) ?nextToken ?maxResults ())
           (Some Values.GetFindingsResponse.to_json)
           (Some Values.GetFindingsResponse.error_to_json)])
let get_insight_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and insightArn =
         flag "insight-arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insight_results
           (Values.GetInsightResultsRequest.make ~insightArn ())
           (Some Values.GetInsightResultsResponse.to_json)
           (Some Values.GetInsightResultsResponse.error_to_json)])
let get_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and insightArns =
         flag "insight-arns" (optional json_arg) ~doc:"JSON ArnList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insights
           (Values.GetInsightsRequest.make
              ?insightArns:(Option.map ~f:Values.ArnList.of_json insightArns)
              ?nextToken ?maxResults ())
           (Some Values.GetInsightsResponse.to_json)
           (Some Values.GetInsightsResponse.error_to_json)])
let get_invitations_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_invitations_count
           (Values.GetInvitationsCountRequest.make ())
           (Some Values.GetInvitationsCountResponse.to_json)
           (Some Values.GetInvitationsCountResponse.error_to_json)])
let get_master_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_master_account (Values.GetMasterAccountRequest.make ())
           (Some Values.GetMasterAccountResponse.to_json)
           (Some Values.GetMasterAccountResponse.error_to_json)])
let get_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_members
           (Values.GetMembersRequest.make
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.GetMembersResponse.to_json)
           (Some Values.GetMembersResponse.error_to_json)])
let invite_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invite_members
           (Values.InviteMembersRequest.make
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.InviteMembersResponse.to_json)
           (Some Values.InviteMembersResponse.error_to_json)])
let list_enabled_products_for_import =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_enabled_products_for_import
           (Values.ListEnabledProductsForImportRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListEnabledProductsForImportResponse.to_json)
           (Some Values.ListEnabledProductsForImportResponse.error_to_json)])
let list_finding_aggregators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_finding_aggregators
           (Values.ListFindingAggregatorsRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListFindingAggregatorsResponse.to_json)
           (Some Values.ListFindingAggregatorsResponse.error_to_json)])
let list_invitations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CrossAccountMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_invitations
           (Values.ListInvitationsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListInvitationsResponse.to_json)
           (Some Values.ListInvitationsResponse.error_to_json)])
let list_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and onlyAssociated =
         flag "only-associated" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CrossAccountMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_members
           (Values.ListMembersRequest.make ?onlyAssociated ?maxResults
              ?nextToken ()) (Some Values.ListMembersResponse.to_json)
           (Some Values.ListMembersResponse.error_to_json)])
let list_organization_admin_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT AdminsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organization_admin_accounts
           (Values.ListOrganizationAdminAccountsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListOrganizationAdminAccountsResponse.to_json)
           (Some Values.ListOrganizationAdminAccountsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_action_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING NonEmptyString"
       and description =
         flag "description" (optional string) ~doc:"STRING NonEmptyString"
       and actionTargetArn =
         flag "action-target-arn" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_action_target
           (Values.UpdateActionTargetRequest.make ?name ?description
              ~actionTargetArn ())
           (Some Values.UpdateActionTargetResponse.to_json)
           (Some Values.UpdateActionTargetResponse.error_to_json)])
let update_finding_aggregator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regions =
         flag "regions" (optional json_arg) ~doc:"JSON StringList"
       and findingAggregatorArn =
         flag "finding-aggregator-arn" (required string)
           ~doc:"STRING NonEmptyString"
       and regionLinkingMode =
         flag "region-linking-mode" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_finding_aggregator
           (Values.UpdateFindingAggregatorRequest.make
              ?regions:(Option.map ~f:Values.StringList.of_json regions)
              ~findingAggregatorArn ~regionLinkingMode ())
           (Some Values.UpdateFindingAggregatorResponse.to_json)
           (Some Values.UpdateFindingAggregatorResponse.error_to_json)])
let update_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and note = flag "note" (optional json_arg) ~doc:"JSON NoteUpdate"
       and recordState =
         flag "record-state" (optional json_arg) ~doc:"JSON RecordState"
       and filters =
         flag "filters" (required json_arg)
           ~doc:"JSON AwsSecurityFindingFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_findings
           (Values.UpdateFindingsRequest.make
              ?note:(Option.map ~f:Values.NoteUpdate.of_json note)
              ?recordState:(Option.map ~f:Values.RecordState.of_json
                              recordState)
              ~filters:(Values.AwsSecurityFindingFilters.of_json filters) ())
           (Some Values.UpdateFindingsResponse.to_json)
           (Some Values.UpdateFindingsResponse.error_to_json)])
let update_insight =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING NonEmptyString"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON AwsSecurityFindingFilters"
       and groupByAttribute =
         flag "group-by-attribute" (optional string)
           ~doc:"STRING NonEmptyString"
       and insightArn =
         flag "insight-arn" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_insight
           (Values.UpdateInsightRequest.make ?name
              ?filters:(Option.map
                          ~f:Values.AwsSecurityFindingFilters.of_json filters)
              ?groupByAttribute ~insightArn ())
           (Some Values.UpdateInsightResponse.to_json)
           (Some Values.UpdateInsightResponse.error_to_json)])
let update_organization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoEnable =
         flag "auto-enable" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_organization_configuration
           (Values.UpdateOrganizationConfigurationRequest.make ~autoEnable ())
           (Some Values.UpdateOrganizationConfigurationResponse.to_json)
           (Some Values.UpdateOrganizationConfigurationResponse.error_to_json)])
let update_security_hub_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoEnableControls =
         flag "auto-enable-controls" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_security_hub_configuration
           (Values.UpdateSecurityHubConfigurationRequest.make
              ?autoEnableControls ())
           (Some Values.UpdateSecurityHubConfigurationResponse.to_json)
           (Some Values.UpdateSecurityHubConfigurationResponse.error_to_json)])
let update_standards_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlStatus =
         flag "control-status" (optional json_arg) ~doc:"JSON ControlStatus"
       and disabledReason =
         flag "disabled-reason" (optional string)
           ~doc:"STRING NonEmptyString"
       and standardsControlArn =
         flag "standards-control-arn" (required string)
           ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_standards_control
           (Values.UpdateStandardsControlRequest.make
              ?controlStatus:(Option.map ~f:Values.ControlStatus.of_json
                                controlStatus) ?disabledReason
              ~standardsControlArn ())
           (Some Values.UpdateStandardsControlResponse.to_json)
           (Some Values.UpdateStandardsControlResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-administrator-invitation", accept_administrator_invitation);
    ("accept-invitation", accept_invitation);
    ("batch-disable-standards", batch_disable_standards);
    ("batch-enable-standards", batch_enable_standards);
    ("batch-import-findings", batch_import_findings);
    ("batch-update-findings", batch_update_findings);
    ("create-action-target", create_action_target);
    ("create-finding-aggregator", create_finding_aggregator);
    ("create-insight", create_insight);
    ("create-members", create_members);
    ("decline-invitations", decline_invitations);
    ("delete-action-target", delete_action_target);
    ("delete-finding-aggregator", delete_finding_aggregator);
    ("delete-insight", delete_insight);
    ("delete-invitations", delete_invitations);
    ("delete-members", delete_members);
    ("describe-action-targets", describe_action_targets);
    ("describe-hub", describe_hub);
    ("describe-organization-configuration",
      describe_organization_configuration);
    ("describe-products", describe_products);
    ("describe-standards", describe_standards);
    ("describe-standards-controls", describe_standards_controls);
    ("disable-import-findings-for-product",
      disable_import_findings_for_product);
    ("disable-organization-admin-account",
      disable_organization_admin_account);
    ("disable-security-hub", disable_security_hub);
    ("disassociate-from-administrator-account",
      disassociate_from_administrator_account);
    ("disassociate-from-master-account", disassociate_from_master_account);
    ("disassociate-members", disassociate_members);
    ("enable-import-findings-for-product",
      enable_import_findings_for_product);
    ("enable-organization-admin-account", enable_organization_admin_account);
    ("enable-security-hub", enable_security_hub);
    ("get-administrator-account", get_administrator_account);
    ("get-enabled-standards", get_enabled_standards);
    ("get-finding-aggregator", get_finding_aggregator);
    ("get-findings", get_findings);
    ("get-insight-results", get_insight_results);
    ("get-insights", get_insights);
    ("get-invitations-count", get_invitations_count);
    ("get-master-account", get_master_account);
    ("get-members", get_members);
    ("invite-members", invite_members);
    ("list-enabled-products-for-import", list_enabled_products_for_import);
    ("list-finding-aggregators", list_finding_aggregators);
    ("list-invitations", list_invitations);
    ("list-members", list_members);
    ("list-organization-admin-accounts", list_organization_admin_accounts);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-action-target", update_action_target);
    ("update-finding-aggregator", update_finding_aggregator);
    ("update-findings", update_findings);
    ("update-insight", update_insight);
    ("update-organization-configuration", update_organization_configuration);
    ("update-security-hub-configuration", update_security_hub_configuration);
    ("update-standards-control", update_standards_control)]
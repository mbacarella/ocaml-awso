(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_stream_group_locations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and locationConfigurations =
         flag "location-configurations" (required json_arg)
           ~doc:"JSON LocationConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_stream_group_locations
           (Values.AddStreamGroupLocationsInput.make ~identifier
              ~locationConfigurations:(Values.LocationConfigurations.of_json
                                         locationConfigurations) ())
           (Some Values.AddStreamGroupLocationsOutput.to_json)
           (Some Values.AddStreamGroupLocationsOutput.error_to_json)])
let associate_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and applicationIdentifiers =
         flag "application-identifiers" (required json_arg)
           ~doc:"JSON Identifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_applications
           (Values.AssociateApplicationsInput.make ~identifier
              ~applicationIdentifiers:(Values.Identifiers.of_json
                                         applicationIdentifiers) ())
           (Some Values.AssociateApplicationsOutput.to_json)
           (Some Values.AssociateApplicationsOutput.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationLogPaths =
         flag "application-log-paths" (optional json_arg)
           ~doc:"JSON FilePaths"
       and applicationLogOutputUri =
         flag "application-log-output-uri" (optional string)
           ~doc:"STRING ApplicationLogOutputUri"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (required string) ~doc:"STRING Description"
       and runtimeEnvironment =
         flag "runtime-environment" (required json_arg)
           ~doc:"JSON RuntimeEnvironment"
       and executablePath =
         flag "executable-path" (required string)
           ~doc:"STRING ExecutablePath"
       and applicationSourceUri =
         flag "application-source-uri" (required string)
           ~doc:"STRING ApplicationSourceUri" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationInput.make
              ?applicationLogPaths:(Option.map ~f:Values.FilePaths.of_json
                                      applicationLogPaths)
              ?applicationLogOutputUri
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?clientToken
              ~description
              ~runtimeEnvironment:(Values.RuntimeEnvironment.of_json
                                     runtimeEnvironment) ~executablePath
              ~applicationSourceUri ())
           (Some Values.CreateApplicationOutput.to_json)
           (Some Values.CreateApplicationOutput.error_to_json)])
let create_stream_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultApplicationIdentifier =
         flag "default-application-identifier" (optional string)
           ~doc:"STRING Identifier"
       and locationConfigurations =
         flag "location-configurations" (optional json_arg)
           ~doc:"JSON LocationConfigurations"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (required string) ~doc:"STRING Description"
       and streamClass =
         flag "stream-class" (required json_arg) ~doc:"JSON StreamClass" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stream_group
           (Values.CreateStreamGroupInput.make ?defaultApplicationIdentifier
              ?locationConfigurations:(Option.map
                                         ~f:Values.LocationConfigurations.of_json
                                         locationConfigurations)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?clientToken
              ~description
              ~streamClass:(Values.StreamClass.of_json streamClass) ())
           (Some Values.CreateStreamGroupOutput.to_json)
           (Some Values.CreateStreamGroupOutput.error_to_json)])
let create_stream_session_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and streamSessionIdentifier =
         flag "stream-session-identifier" (required string)
           ~doc:"STRING Identifier"
       and signalRequest =
         flag "signal-request" (required string) ~doc:"STRING SignalRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stream_session_connection
           (Values.CreateStreamSessionConnectionInput.make ?clientToken
              ~identifier ~streamSessionIdentifier ~signalRequest ())
           (Some Values.CreateStreamSessionConnectionOutput.to_json)
           (Some Values.CreateStreamSessionConnectionOutput.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationInput.make ~identifier ()) None None])
let delete_stream_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stream_group
           (Values.DeleteStreamGroupInput.make ~identifier ()) None None])
let disassociate_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and applicationIdentifiers =
         flag "application-identifiers" (required json_arg)
           ~doc:"JSON Identifiers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_applications
           (Values.DisassociateApplicationsInput.make ~identifier
              ~applicationIdentifiers:(Values.Identifiers.of_json
                                         applicationIdentifiers) ())
           (Some Values.DisassociateApplicationsOutput.to_json)
           (Some Values.DisassociateApplicationsOutput.error_to_json)])
let export_stream_session_files =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and streamSessionIdentifier =
         flag "stream-session-identifier" (required string)
           ~doc:"STRING Identifier"
       and outputUri =
         flag "output-uri" (required string) ~doc:"STRING OutputUri" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_stream_session_files
           (Values.ExportStreamSessionFilesInput.make ~identifier
              ~streamSessionIdentifier ~outputUri ())
           (Some Values.ExportStreamSessionFilesOutput.to_json)
           (Some Values.ExportStreamSessionFilesOutput.error_to_json)])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationInput.make ~identifier ())
           (Some Values.GetApplicationOutput.to_json)
           (Some Values.GetApplicationOutput.error_to_json)])
let get_stream_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stream_group
           (Values.GetStreamGroupInput.make ~identifier ())
           (Some Values.GetStreamGroupOutput.to_json)
           (Some Values.GetStreamGroupOutput.error_to_json)])
let get_stream_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and streamSessionIdentifier =
         flag "stream-session-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stream_session
           (Values.GetStreamSessionInput.make ~identifier
              ~streamSessionIdentifier ())
           (Some Values.GetStreamSessionOutput.to_json)
           (Some Values.GetStreamSessionOutput.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsInput.make ?nextToken ?maxResults ())
           (Some Values.ListApplicationsOutput.to_json)
           (Some Values.ListApplicationsOutput.error_to_json)])
let list_stream_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stream_groups
           (Values.ListStreamGroupsInput.make ?nextToken ?maxResults ())
           (Some Values.ListStreamGroupsOutput.to_json)
           (Some Values.ListStreamGroupsOutput.error_to_json)])
let list_stream_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON StreamSessionStatus"
       and exportFilesStatus =
         flag "export-files-status" (optional json_arg)
           ~doc:"JSON ExportFilesStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stream_sessions
           (Values.ListStreamSessionsInput.make
              ?status:(Option.map ~f:Values.StreamSessionStatus.of_json
                         status)
              ?exportFilesStatus:(Option.map
                                    ~f:Values.ExportFilesStatus.of_json
                                    exportFilesStatus) ?nextToken ?maxResults
              ~identifier ()) (Some Values.ListStreamSessionsOutput.to_json)
           (Some Values.ListStreamSessionsOutput.error_to_json)])
let list_stream_sessions_by_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON StreamSessionStatus"
       and exportFilesStatus =
         flag "export-files-status" (optional json_arg)
           ~doc:"JSON ExportFilesStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_stream_sessions_by_account
           (Values.ListStreamSessionsByAccountInput.make
              ?status:(Option.map ~f:Values.StreamSessionStatus.of_json
                         status)
              ?exportFilesStatus:(Option.map
                                    ~f:Values.ExportFilesStatus.of_json
                                    exportFilesStatus) ?nextToken ?maxResults
              ()) (Some Values.ListStreamSessionsByAccountOutput.to_json)
           (Some Values.ListStreamSessionsByAccountOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let remove_stream_group_locations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and locations =
         flag "locations" (required json_arg) ~doc:"JSON LocationsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_stream_group_locations
           (Values.RemoveStreamGroupLocationsInput.make ~identifier
              ~locations:(Values.LocationsList.of_json locations) ()) None
           None])
let start_stream_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and locations =
         flag "locations" (optional json_arg) ~doc:"JSON LocationList"
       and connectionTimeoutSeconds =
         flag "connection-timeout-seconds" (optional int)
           ~doc:"INT ConnectionTimeoutSeconds"
       and sessionLengthSeconds =
         flag "session-length-seconds" (optional int)
           ~doc:"INT SessionLengthSeconds"
       and additionalLaunchArgs =
         flag "additional-launch-args" (optional json_arg)
           ~doc:"JSON GameLaunchArgList"
       and additionalEnvironmentVariables =
         flag "additional-environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and performanceStatsConfiguration =
         flag "performance-stats-configuration" (optional json_arg)
           ~doc:"JSON PerformanceStatsConfiguration"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON Protocol"
       and signalRequest =
         flag "signal-request" (required string) ~doc:"STRING SignalRequest"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_stream_session
           (Values.StartStreamSessionInput.make ?clientToken ?description
              ?userId
              ?locations:(Option.map ~f:Values.LocationList.of_json locations)
              ?connectionTimeoutSeconds ?sessionLengthSeconds
              ?additionalLaunchArgs:(Option.map
                                       ~f:Values.GameLaunchArgList.of_json
                                       additionalLaunchArgs)
              ?additionalEnvironmentVariables:(Option.map
                                                 ~f:Values.EnvironmentVariables.of_json
                                                 additionalEnvironmentVariables)
              ?performanceStatsConfiguration:(Option.map
                                                ~f:Values.PerformanceStatsConfiguration.of_json
                                                performanceStatsConfiguration)
              ~identifier ~protocol:(Values.Protocol.of_json protocol)
              ~signalRequest ~applicationIdentifier ())
           (Some Values.StartStreamSessionOutput.to_json)
           (Some Values.StartStreamSessionOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let terminate_stream_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier"
       and streamSessionIdentifier =
         flag "stream-session-identifier" (required string)
           ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_stream_session
           (Values.TerminateStreamSessionInput.make ~identifier
              ~streamSessionIdentifier ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and applicationLogPaths =
         flag "application-log-paths" (optional json_arg)
           ~doc:"JSON FilePaths"
       and applicationLogOutputUri =
         flag "application-log-output-uri" (optional string)
           ~doc:"STRING ApplicationLogOutputUri"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationInput.make ?description
              ?applicationLogPaths:(Option.map ~f:Values.FilePaths.of_json
                                      applicationLogPaths)
              ?applicationLogOutputUri ~identifier ())
           (Some Values.UpdateApplicationOutput.to_json)
           (Some Values.UpdateApplicationOutput.error_to_json)])
let update_stream_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationConfigurations =
         flag "location-configurations" (optional json_arg)
           ~doc:"JSON LocationConfigurations"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and defaultApplicationIdentifier =
         flag "default-application-identifier" (optional string)
           ~doc:"STRING Identifier"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING Identifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stream_group
           (Values.UpdateStreamGroupInput.make
              ?locationConfigurations:(Option.map
                                         ~f:Values.LocationConfigurations.of_json
                                         locationConfigurations) ?description
              ?defaultApplicationIdentifier ~identifier ())
           (Some Values.UpdateStreamGroupOutput.to_json)
           (Some Values.UpdateStreamGroupOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-stream-group-locations", add_stream_group_locations);
    ("associate-applications", associate_applications);
    ("create-application", create_application);
    ("create-stream-group", create_stream_group);
    ("create-stream-session-connection", create_stream_session_connection);
    ("delete-application", delete_application);
    ("delete-stream-group", delete_stream_group);
    ("disassociate-applications", disassociate_applications);
    ("export-stream-session-files", export_stream_session_files);
    ("get-application", get_application);
    ("get-stream-group", get_stream_group);
    ("get-stream-session", get_stream_session);
    ("list-applications", list_applications);
    ("list-stream-groups", list_stream_groups);
    ("list-stream-sessions", list_stream_sessions);
    ("list-stream-sessions-by-account", list_stream_sessions_by_account);
    ("list-tags-for-resource", list_tags_for_resource);
    ("remove-stream-group-locations", remove_stream_group_locations);
    ("start-stream-session", start_stream_session);
    ("tag-resource", tag_resource);
    ("terminate-stream-session", terminate_stream_session);
    ("untag-resource", untag_resource);
    ("update-application", update_application);
    ("update-stream-group", update_stream_group)]
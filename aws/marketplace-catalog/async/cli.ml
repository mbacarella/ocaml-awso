(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and changeSetId =
         flag "change-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_change_set
           (Values.CancelChangeSetRequest.make ~catalog ~changeSetId ())
           (Some Values.CancelChangeSetResponse.to_json)
           (Some Values.CancelChangeSetResponse.error_to_json)])
let describe_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and changeSetId =
         flag "change-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_change_set
           (Values.DescribeChangeSetRequest.make ~catalog ~changeSetId ())
           (Some Values.DescribeChangeSetResponse.to_json)
           (Some Values.DescribeChangeSetResponse.error_to_json)])
let describe_entity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entity
           (Values.DescribeEntityRequest.make ~catalog ~entityId ())
           (Some Values.DescribeEntityResponse.to_json)
           (Some Values.DescribeEntityResponse.error_to_json)])
let list_change_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterList =
         flag "filter-list" (optional json_arg) ~doc:"JSON FilterList"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON Sort"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_change_sets
           (Values.ListChangeSetsRequest.make
              ?filterList:(Option.map ~f:Values.FilterList.of_json filterList)
              ?sort:(Option.map ~f:Values.Sort.of_json sort) ?maxResults
              ?nextToken ~catalog ())
           (Some Values.ListChangeSetsResponse.to_json)
           (Some Values.ListChangeSetsResponse.error_to_json)])
let list_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterList =
         flag "filter-list" (optional json_arg) ~doc:"JSON FilterList"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON Sort"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultInteger"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and entityType =
         flag "entity-type" (required string) ~doc:"STRING EntityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entities
           (Values.ListEntitiesRequest.make
              ?filterList:(Option.map ~f:Values.FilterList.of_json filterList)
              ?sort:(Option.map ~f:Values.Sort.of_json sort) ?nextToken
              ?maxResults ~catalog ~entityType ())
           (Some Values.ListEntitiesResponse.to_json)
           (Some Values.ListEntitiesResponse.error_to_json)])
let start_change_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and changeSetName =
         flag "change-set-name" (optional string) ~doc:"STRING ChangeSetName"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and catalog = flag "catalog" (required string) ~doc:"STRING Catalog"
       and changeSet =
         flag "change-set" (required json_arg)
           ~doc:"JSON RequestedChangeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_change_set
           (Values.StartChangeSetRequest.make ?changeSetName
              ?clientRequestToken ~catalog
              ~changeSet:(Values.RequestedChangeList.of_json changeSet) ())
           (Some Values.StartChangeSetResponse.to_json)
           (Some Values.StartChangeSetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-change-set", cancel_change_set);
    ("describe-change-set", describe_change_set);
    ("describe-entity", describe_entity);
    ("list-change-sets", list_change_sets);
    ("list-entities", list_entities);
    ("start-change-set", start_change_set)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_service_role_to_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_service_role_to_account
           (Values.AssociateServiceRoleToAccountRequest.make ~roleArn ())
           (Some Values.AssociateServiceRoleToAccountResponse.to_json)
           (Some Values.AssociateServiceRoleToAccountResponse.error_to_json)])
let batch_associate_client_device_with_core_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entries =
         flag "entries" (optional json_arg)
           ~doc:"JSON AssociateClientDeviceWithCoreDeviceEntryList"
       and coreDeviceThingName =
         flag "core-device-thing-name" (required string)
           ~doc:"STRING IoTThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_associate_client_device_with_core_device
           (Values.BatchAssociateClientDeviceWithCoreDeviceRequest.make
              ?entries:(Option.map
                          ~f:Values.AssociateClientDeviceWithCoreDeviceEntryList.of_json
                          entries) ~coreDeviceThingName ())
           (Some
              Values.BatchAssociateClientDeviceWithCoreDeviceResponse.to_json)
           (Some
              Values.BatchAssociateClientDeviceWithCoreDeviceResponse.error_to_json)])
let batch_disassociate_client_device_from_core_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entries =
         flag "entries" (optional json_arg)
           ~doc:"JSON DisassociateClientDeviceFromCoreDeviceEntryList"
       and coreDeviceThingName =
         flag "core-device-thing-name" (required string)
           ~doc:"STRING IoTThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_disassociate_client_device_from_core_device
           (Values.BatchDisassociateClientDeviceFromCoreDeviceRequest.make
              ?entries:(Option.map
                          ~f:Values.DisassociateClientDeviceFromCoreDeviceEntryList.of_json
                          entries) ~coreDeviceThingName ())
           (Some
              Values.BatchDisassociateClientDeviceFromCoreDeviceResponse.to_json)
           (Some
              Values.BatchDisassociateClientDeviceFromCoreDeviceResponse.error_to_json)])
let cancel_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_deployment
           (Values.CancelDeploymentRequest.make ~deploymentId ())
           (Some Values.CancelDeploymentResponse.to_json)
           (Some Values.CancelDeploymentResponse.error_to_json)])
let create_component_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inlineRecipe =
         flag "inline-recipe" (optional json_arg) ~doc:"JSON RecipeBlob"
       and lambdaFunction =
         flag "lambda-function" (optional json_arg)
           ~doc:"JSON LambdaFunctionRecipeSource"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_component_version
           (Values.CreateComponentVersionRequest.make
              ?inlineRecipe:(Option.map ~f:Values.RecipeBlob.of_json
                               inlineRecipe)
              ?lambdaFunction:(Option.map
                                 ~f:Values.LambdaFunctionRecipeSource.of_json
                                 lambdaFunction)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ()) (Some Values.CreateComponentVersionResponse.to_json)
           (Some Values.CreateComponentVersionResponse.error_to_json)])
let create_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentName =
         flag "deployment-name" (optional string)
           ~doc:"STRING DeploymentNameString"
       and components =
         flag "components" (optional json_arg)
           ~doc:"JSON ComponentDeploymentSpecifications"
       and iotJobConfiguration =
         flag "iot-job-configuration" (optional json_arg)
           ~doc:"JSON DeploymentIoTJobConfiguration"
       and deploymentPolicies =
         flag "deployment-policies" (optional json_arg)
           ~doc:"JSON DeploymentPolicies"
       and parentTargetArn =
         flag "parent-target-arn" (optional string)
           ~doc:"STRING ThingGroupARN"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientTokenString"
       and targetArn =
         flag "target-arn" (required string) ~doc:"STRING TargetARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment
           (Values.CreateDeploymentRequest.make ?deploymentName
              ?components:(Option.map
                             ~f:Values.ComponentDeploymentSpecifications.of_json
                             components)
              ?iotJobConfiguration:(Option.map
                                      ~f:Values.DeploymentIoTJobConfiguration.of_json
                                      iotJobConfiguration)
              ?deploymentPolicies:(Option.map
                                     ~f:Values.DeploymentPolicies.of_json
                                     deploymentPolicies) ?parentTargetArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ~targetArn ()) (Some Values.CreateDeploymentResponse.to_json)
           (Some Values.CreateDeploymentResponse.error_to_json)])
let delete_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING ComponentVersionARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_component (Values.DeleteComponentRequest.make ~arn ())
           None None])
let delete_core_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreDeviceThingName =
         flag "core-device-thing-name" (required string)
           ~doc:"STRING CoreDeviceThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_core_device
           (Values.DeleteCoreDeviceRequest.make ~coreDeviceThingName ()) None
           None])
let delete_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_deployment
           (Values.DeleteDeploymentRequest.make ~deploymentId ()) None None])
let describe_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING ComponentVersionARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_component
           (Values.DescribeComponentRequest.make ~arn ())
           (Some Values.DescribeComponentResponse.to_json)
           (Some Values.DescribeComponentResponse.error_to_json)])
let disassociate_service_role_from_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_service_role_from_account
           (Values.DisassociateServiceRoleFromAccountRequest.make ())
           (Some Values.DisassociateServiceRoleFromAccountResponse.to_json)
           (Some
              Values.DisassociateServiceRoleFromAccountResponse.error_to_json)])
let get_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recipeOutputFormat =
         flag "recipe-output-format" (optional json_arg)
           ~doc:"JSON RecipeOutputFormat"
       and arn =
         flag "arn" (required string) ~doc:"STRING ComponentVersionARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_component
           (Values.GetComponentRequest.make
              ?recipeOutputFormat:(Option.map
                                     ~f:Values.RecipeOutputFormat.of_json
                                     recipeOutputFormat) ~arn ())
           (Some Values.GetComponentResponse.to_json)
           (Some Values.GetComponentResponse.error_to_json)])
let get_component_version_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3EndpointType =
         flag "s3-endpoint-type" (optional json_arg)
           ~doc:"JSON S3EndpointType"
       and iotEndpointType =
         flag "iot-endpoint-type" (optional json_arg)
           ~doc:"JSON IotEndpointType"
       and arn =
         flag "arn" (required string) ~doc:"STRING ComponentVersionARN"
       and artifactName =
         flag "artifact-name" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_component_version_artifact
           (Values.GetComponentVersionArtifactRequest.make
              ?s3EndpointType:(Option.map ~f:Values.S3EndpointType.of_json
                                 s3EndpointType)
              ?iotEndpointType:(Option.map ~f:Values.IotEndpointType.of_json
                                  iotEndpointType) ~arn ~artifactName ())
           (Some Values.GetComponentVersionArtifactResponse.to_json)
           (Some Values.GetComponentVersionArtifactResponse.error_to_json)])
let get_connectivity_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string)
           ~doc:"STRING CoreDeviceThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connectivity_info
           (Values.GetConnectivityInfoRequest.make ~thingName ())
           (Some Values.GetConnectivityInfoResponse.to_json)
           (Some Values.GetConnectivityInfoResponse.error_to_json)])
let get_core_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and coreDeviceThingName =
         flag "core-device-thing-name" (required string)
           ~doc:"STRING CoreDeviceThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_core_device
           (Values.GetCoreDeviceRequest.make ~coreDeviceThingName ())
           (Some Values.GetCoreDeviceResponse.to_json)
           (Some Values.GetCoreDeviceResponse.error_to_json)])
let get_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployment
           (Values.GetDeploymentRequest.make ~deploymentId ())
           (Some Values.GetDeploymentResponse.to_json)
           (Some Values.GetDeploymentResponse.error_to_json)])
let get_service_role_for_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_role_for_account
           (Values.GetServiceRoleForAccountRequest.make ())
           (Some Values.GetServiceRoleForAccountResponse.to_json)
           (Some Values.GetServiceRoleForAccountResponse.error_to_json)])
let list_client_devices_associated_with_core_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DefaultMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and coreDeviceThingName =
         flag "core-device-thing-name" (required string)
           ~doc:"STRING IoTThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_client_devices_associated_with_core_device
           (Values.ListClientDevicesAssociatedWithCoreDeviceRequest.make
              ?maxResults ?nextToken ~coreDeviceThingName ())
           (Some
              Values.ListClientDevicesAssociatedWithCoreDeviceResponse.to_json)
           (Some
              Values.ListClientDevicesAssociatedWithCoreDeviceResponse.error_to_json)])
let list_component_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DefaultMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and arn = flag "arn" (required string) ~doc:"STRING ComponentARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_component_versions
           (Values.ListComponentVersionsRequest.make ?maxResults ?nextToken
              ~arn ()) (Some Values.ListComponentVersionsResponse.to_json)
           (Some Values.ListComponentVersionsResponse.error_to_json)])
let list_components =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scope =
         flag "scope" (optional json_arg)
           ~doc:"JSON ComponentVisibilityScope"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DefaultMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_components
           (Values.ListComponentsRequest.make
              ?scope:(Option.map ~f:Values.ComponentVisibilityScope.of_json
                        scope) ?maxResults ?nextToken ())
           (Some Values.ListComponentsResponse.to_json)
           (Some Values.ListComponentsResponse.error_to_json)])
let list_core_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingGroupArn =
         flag "thing-group-arn" (optional string) ~doc:"STRING ThingGroupARN"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON CoreDeviceStatus"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DefaultMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and runtime =
         flag "runtime" (optional string)
           ~doc:"STRING CoreDeviceRuntimeString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_core_devices
           (Values.ListCoreDevicesRequest.make ?thingGroupArn
              ?status:(Option.map ~f:Values.CoreDeviceStatus.of_json status)
              ?maxResults ?nextToken ?runtime ())
           (Some Values.ListCoreDevicesResponse.to_json)
           (Some Values.ListCoreDevicesResponse.error_to_json)])
let list_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetArn =
         flag "target-arn" (optional string) ~doc:"STRING TargetARN"
       and historyFilter =
         flag "history-filter" (optional json_arg)
           ~doc:"JSON DeploymentHistoryFilter"
       and parentTargetArn =
         flag "parent-target-arn" (optional string)
           ~doc:"STRING ThingGroupARN"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DefaultMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deployments
           (Values.ListDeploymentsRequest.make ?targetArn
              ?historyFilter:(Option.map
                                ~f:Values.DeploymentHistoryFilter.of_json
                                historyFilter) ?parentTargetArn ?maxResults
              ?nextToken ()) (Some Values.ListDeploymentsResponse.to_json)
           (Some Values.ListDeploymentsResponse.error_to_json)])
let list_effective_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DefaultMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and coreDeviceThingName =
         flag "core-device-thing-name" (required string)
           ~doc:"STRING CoreDeviceThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_effective_deployments
           (Values.ListEffectiveDeploymentsRequest.make ?maxResults
              ?nextToken ~coreDeviceThingName ())
           (Some Values.ListEffectiveDeploymentsResponse.to_json)
           (Some Values.ListEffectiveDeploymentsResponse.error_to_json)])
let list_installed_components =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT DefaultMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and topologyFilter =
         flag "topology-filter" (optional json_arg)
           ~doc:"JSON InstalledComponentTopologyFilter"
       and coreDeviceThingName =
         flag "core-device-thing-name" (required string)
           ~doc:"STRING CoreDeviceThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_installed_components
           (Values.ListInstalledComponentsRequest.make ?maxResults ?nextToken
              ?topologyFilter:(Option.map
                                 ~f:Values.InstalledComponentTopologyFilter.of_json
                                 topologyFilter) ~coreDeviceThingName ())
           (Some Values.ListInstalledComponentsResponse.to_json)
           (Some Values.ListInstalledComponentsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GenericV2ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let resolve_component_candidates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and platform =
         flag "platform" (optional json_arg) ~doc:"JSON ComponentPlatform"
       and componentCandidates =
         flag "component-candidates" (optional json_arg)
           ~doc:"JSON ComponentCandidateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resolve_component_candidates
           (Values.ResolveComponentCandidatesRequest.make
              ?platform:(Option.map ~f:Values.ComponentPlatform.of_json
                           platform)
              ?componentCandidates:(Option.map
                                      ~f:Values.ComponentCandidateList.of_json
                                      componentCandidates) ())
           (Some Values.ResolveComponentCandidatesResponse.to_json)
           (Some Values.ResolveComponentCandidatesResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GenericV2ARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING GenericV2ARN"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_connectivity_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string)
           ~doc:"STRING CoreDeviceThingName"
       and connectivityInfo =
         flag "connectivity-info" (required json_arg)
           ~doc:"JSON connectivityInfoList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connectivity_info
           (Values.UpdateConnectivityInfoRequest.make ~thingName
              ~connectivityInfo:(Values.ConnectivityInfoList.of_json
                                   connectivityInfo) ())
           (Some Values.UpdateConnectivityInfoResponse.to_json)
           (Some Values.UpdateConnectivityInfoResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-service-role-to-account", associate_service_role_to_account);
    ("batch-associate-client-device-with-core-device",
      batch_associate_client_device_with_core_device);
    ("batch-disassociate-client-device-from-core-device",
      batch_disassociate_client_device_from_core_device);
    ("cancel-deployment", cancel_deployment);
    ("create-component-version", create_component_version);
    ("create-deployment", create_deployment);
    ("delete-component", delete_component);
    ("delete-core-device", delete_core_device);
    ("delete-deployment", delete_deployment);
    ("describe-component", describe_component);
    ("disassociate-service-role-from-account",
      disassociate_service_role_from_account);
    ("get-component", get_component);
    ("get-component-version-artifact", get_component_version_artifact);
    ("get-connectivity-info", get_connectivity_info);
    ("get-core-device", get_core_device);
    ("get-deployment", get_deployment);
    ("get-service-role-for-account", get_service_role_for_account);
    ("list-client-devices-associated-with-core-device",
      list_client_devices_associated_with_core_device);
    ("list-component-versions", list_component_versions);
    ("list-components", list_components);
    ("list-core-devices", list_core_devices);
    ("list-deployments", list_deployments);
    ("list-effective-deployments", list_effective_deployments);
    ("list-installed-components", list_installed_components);
    ("list-tags-for-resource", list_tags_for_resource);
    ("resolve-component-candidates", resolve_component_candidates);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-connectivity-info", update_connectivity_info)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let describe_report_creation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_report_creation
           (Values.DescribeReportCreationInput.make ())
           (Some Values.DescribeReportCreationOutput.to_json)
           (Some Values.DescribeReportCreationOutput.error_to_json)])
let get_compliance_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetIdFilters =
         flag "target-id-filters" (optional json_arg)
           ~doc:"JSON TargetIdFilterList"
       and regionFilters =
         flag "region-filters" (optional json_arg)
           ~doc:"JSON RegionFilterList"
       and resourceTypeFilters =
         flag "resource-type-filters" (optional json_arg)
           ~doc:"JSON ResourceTypeFilterList"
       and tagKeyFilters =
         flag "tag-key-filters" (optional json_arg)
           ~doc:"JSON TagKeyFilterList"
       and groupBy = flag "group-by" (optional json_arg) ~doc:"JSON GroupBy"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsGetComplianceSummary"
       and paginationToken =
         flag "pagination-token" (optional string)
           ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_compliance_summary
           (Values.GetComplianceSummaryInput.make
              ?targetIdFilters:(Option.map
                                  ~f:Values.TargetIdFilterList.of_json
                                  targetIdFilters)
              ?regionFilters:(Option.map ~f:Values.RegionFilterList.of_json
                                regionFilters)
              ?resourceTypeFilters:(Option.map
                                      ~f:Values.ResourceTypeFilterList.of_json
                                      resourceTypeFilters)
              ?tagKeyFilters:(Option.map ~f:Values.TagKeyFilterList.of_json
                                tagKeyFilters)
              ?groupBy:(Option.map ~f:Values.GroupBy.of_json groupBy)
              ?maxResults ?paginationToken ())
           (Some Values.GetComplianceSummaryOutput.to_json)
           (Some Values.GetComplianceSummaryOutput.error_to_json)])
let get_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and paginationToken =
         flag "pagination-token" (optional string)
           ~doc:"STRING PaginationToken"
       and tagFilters =
         flag "tag-filters" (optional json_arg) ~doc:"JSON TagFilterList"
       and resourcesPerPage =
         flag "resources-per-page" (optional int) ~doc:"INT ResourcesPerPage"
       and tagsPerPage =
         flag "tags-per-page" (optional int) ~doc:"INT TagsPerPage"
       and resourceTypeFilters =
         flag "resource-type-filters" (optional json_arg)
           ~doc:"JSON ResourceTypeFilterList"
       and includeComplianceDetails =
         flag "include-compliance-details" (optional bool)
           ~doc:"BOOL IncludeComplianceDetails"
       and excludeCompliantResources =
         flag "exclude-compliant-resources" (optional bool)
           ~doc:"BOOL ExcludeCompliantResources"
       and resourceARNList =
         flag "resource-a-r-n-list" (optional json_arg)
           ~doc:"JSON ResourceARNListForGet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resources
           (Values.GetResourcesInput.make ?paginationToken
              ?tagFilters:(Option.map ~f:Values.TagFilterList.of_json
                             tagFilters) ?resourcesPerPage ?tagsPerPage
              ?resourceTypeFilters:(Option.map
                                      ~f:Values.ResourceTypeFilterList.of_json
                                      resourceTypeFilters)
              ?includeComplianceDetails ?excludeCompliantResources
              ?resourceARNList:(Option.map
                                  ~f:Values.ResourceARNListForGet.of_json
                                  resourceARNList) ())
           (Some Values.GetResourcesOutput.to_json)
           (Some Values.GetResourcesOutput.error_to_json)])
let get_tag_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and paginationToken =
         flag "pagination-token" (optional string)
           ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tag_keys (Values.GetTagKeysInput.make ?paginationToken ())
           (Some Values.GetTagKeysOutput.to_json)
           (Some Values.GetTagKeysOutput.error_to_json)])
let get_tag_values =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and paginationToken =
         flag "pagination-token" (optional string)
           ~doc:"STRING PaginationToken"
       and key = flag "key" (required string) ~doc:"STRING TagKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tag_values
           (Values.GetTagValuesInput.make ?paginationToken ~key ())
           (Some Values.GetTagValuesOutput.to_json)
           (Some Values.GetTagValuesOutput.error_to_json)])
let list_required_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsForListRequiredTags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_required_tags
           (Values.ListRequiredTagsInput.make ?nextToken ?maxResults ())
           (Some Values.ListRequiredTagsOutput.to_json)
           (Some Values.ListRequiredTagsOutput.error_to_json)])
let start_report_creation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3Bucket =
         flag "s3-bucket" (required string) ~doc:"STRING S3Bucket" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_report_creation
           (Values.StartReportCreationInput.make ~s3Bucket ())
           (Some Values.StartReportCreationOutput.to_json)
           (Some Values.StartReportCreationOutput.error_to_json)])
let tag_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARNList =
         flag "resource-a-r-n-list" (required json_arg)
           ~doc:"JSON ResourceARNListForTagUntag"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resources
           (Values.TagResourcesInput.make
              ~resourceARNList:(Values.ResourceARNListForTagUntag.of_json
                                  resourceARNList)
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourcesOutput.to_json)
           (Some Values.TagResourcesOutput.error_to_json)])
let untag_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARNList =
         flag "resource-a-r-n-list" (required json_arg)
           ~doc:"JSON ResourceARNListForTagUntag"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyListForUntag" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resources
           (Values.UntagResourcesInput.make
              ~resourceARNList:(Values.ResourceARNListForTagUntag.of_json
                                  resourceARNList)
              ~tagKeys:(Values.TagKeyListForUntag.of_json tagKeys) ())
           (Some Values.UntagResourcesOutput.to_json)
           (Some Values.UntagResourcesOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("describe-report-creation", describe_report_creation);
    ("get-compliance-summary", get_compliance_summary);
    ("get-resources", get_resources);
    ("get-tag-keys", get_tag_keys);
    ("get-tag-values", get_tag_values);
    ("list-required-tags", list_required_tags);
    ("start-report-creation", start_report_creation);
    ("tag-resources", tag_resources);
    ("untag-resources", untag_resources)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_vehicle_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vehicleName =
         flag "vehicle-name" (required string) ~doc:"STRING vehicleName"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING fleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_vehicle_fleet
           (Values.AssociateVehicleFleetRequest.make ~vehicleName ~fleetId ())
           (Some Values.AssociateVehicleFleetResponse.to_json)
           (Some Values.AssociateVehicleFleetResponse.error_to_json)])
let batch_create_vehicle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vehicles =
         flag "vehicles" (required json_arg)
           ~doc:"JSON createVehicleRequestItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_vehicle
           (Values.BatchCreateVehicleRequest.make
              ~vehicles:(Values.CreateVehicleRequestItems.of_json vehicles)
              ()) (Some Values.BatchCreateVehicleResponse.to_json)
           (Some Values.BatchCreateVehicleResponse.error_to_json)])
let batch_update_vehicle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vehicles =
         flag "vehicles" (required json_arg)
           ~doc:"JSON updateVehicleRequestItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_vehicle
           (Values.BatchUpdateVehicleRequest.make
              ~vehicles:(Values.UpdateVehicleRequestItems.of_json vehicles)
              ()) (Some Values.BatchUpdateVehicleResponse.to_json)
           (Some Values.BatchUpdateVehicleResponse.error_to_json)])
let create_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON timestamp"
       and expiryTime =
         flag "expiry-time" (optional json_arg) ~doc:"JSON timestamp"
       and postTriggerCollectionDuration =
         flag "post-trigger-collection-duration" (optional json_arg)
           ~doc:"JSON uint32"
       and diagnosticsMode =
         flag "diagnostics-mode" (optional json_arg)
           ~doc:"JSON DiagnosticsMode"
       and spoolingMode =
         flag "spooling-mode" (optional json_arg) ~doc:"JSON SpoolingMode"
       and compression =
         flag "compression" (optional json_arg) ~doc:"JSON Compression"
       and priority = flag "priority" (optional int) ~doc:"INT priority"
       and signalsToCollect =
         flag "signals-to-collect" (optional json_arg)
           ~doc:"JSON SignalInformationList"
       and dataExtraDimensions =
         flag "data-extra-dimensions" (optional json_arg)
           ~doc:"JSON DataExtraDimensionNodePathList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dataDestinationConfigs =
         flag "data-destination-configs" (optional json_arg)
           ~doc:"JSON DataDestinationConfigs"
       and dataPartitions =
         flag "data-partitions" (optional json_arg)
           ~doc:"JSON DataPartitions"
       and signalsToFetch =
         flag "signals-to-fetch" (optional json_arg)
           ~doc:"JSON SignalFetchInformationList"
       and name = flag "name" (required string) ~doc:"STRING campaignName"
       and signalCatalogArn =
         flag "signal-catalog-arn" (required string) ~doc:"STRING arn"
       and targetArn = flag "target-arn" (required string) ~doc:"STRING arn"
       and collectionScheme =
         flag "collection-scheme" (required json_arg)
           ~doc:"JSON CollectionScheme" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_campaign
           (Values.CreateCampaignRequest.make ?description
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?expiryTime:(Option.map ~f:Values.Timestamp.of_json expiryTime)
              ?postTriggerCollectionDuration:(Option.map
                                                ~f:Values.Uint32.of_json
                                                postTriggerCollectionDuration)
              ?diagnosticsMode:(Option.map ~f:Values.DiagnosticsMode.of_json
                                  diagnosticsMode)
              ?spoolingMode:(Option.map ~f:Values.SpoolingMode.of_json
                               spoolingMode)
              ?compression:(Option.map ~f:Values.Compression.of_json
                              compression) ?priority
              ?signalsToCollect:(Option.map
                                   ~f:Values.SignalInformationList.of_json
                                   signalsToCollect)
              ?dataExtraDimensions:(Option.map
                                      ~f:Values.DataExtraDimensionNodePathList.of_json
                                      dataExtraDimensions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?dataDestinationConfigs:(Option.map
                                         ~f:Values.DataDestinationConfigs.of_json
                                         dataDestinationConfigs)
              ?dataPartitions:(Option.map ~f:Values.DataPartitions.of_json
                                 dataPartitions)
              ?signalsToFetch:(Option.map
                                 ~f:Values.SignalFetchInformationList.of_json
                                 signalsToFetch) ~name ~signalCatalogArn
              ~targetArn
              ~collectionScheme:(Values.CollectionScheme.of_json
                                   collectionScheme) ())
           (Some Values.CreateCampaignResponse.to_json)
           (Some Values.CreateCampaignResponse.error_to_json)])
let create_decoder_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and signalDecoders =
         flag "signal-decoders" (optional json_arg)
           ~doc:"JSON SignalDecoders"
       and networkInterfaces =
         flag "network-interfaces" (optional json_arg)
           ~doc:"JSON NetworkInterfaces"
       and defaultForUnmappedSignals =
         flag "default-for-unmapped-signals" (optional json_arg)
           ~doc:"JSON DefaultForUnmappedSignalsType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING resourceName"
       and modelManifestArn =
         flag "model-manifest-arn" (required string)
           ~doc:"STRING CreateDecoderManifestRequestModelManifestArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_decoder_manifest
           (Values.CreateDecoderManifestRequest.make ?description
              ?signalDecoders:(Option.map ~f:Values.SignalDecoders.of_json
                                 signalDecoders)
              ?networkInterfaces:(Option.map
                                    ~f:Values.NetworkInterfaces.of_json
                                    networkInterfaces)
              ?defaultForUnmappedSignals:(Option.map
                                            ~f:Values.DefaultForUnmappedSignalsType.of_json
                                            defaultForUnmappedSignals)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~modelManifestArn ())
           (Some Values.CreateDecoderManifestResponse.to_json)
           (Some Values.CreateDecoderManifestResponse.error_to_json)])
let create_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING fleetId"
       and signalCatalogArn =
         flag "signal-catalog-arn" (required string) ~doc:"STRING arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet
           (Values.CreateFleetRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~fleetId
              ~signalCatalogArn ()) (Some Values.CreateFleetResponse.to_json)
           (Some Values.CreateFleetResponse.error_to_json)])
let create_model_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING resourceName"
       and nodes =
         flag "nodes" (required json_arg)
           ~doc:"JSON CreateModelManifestRequestNodesList"
       and signalCatalogArn =
         flag "signal-catalog-arn" (required string) ~doc:"STRING arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_manifest
           (Values.CreateModelManifestRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~nodes:(Values.CreateModelManifestRequestNodesList.of_json
                        nodes) ~signalCatalogArn ())
           (Some Values.CreateModelManifestResponse.to_json)
           (Some Values.CreateModelManifestResponse.error_to_json)])
let create_signal_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and nodes = flag "nodes" (optional json_arg) ~doc:"JSON Nodes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_signal_catalog
           (Values.CreateSignalCatalogRequest.make ?description
              ?nodes:(Option.map ~f:Values.Nodes.of_json nodes)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateSignalCatalogResponse.to_json)
           (Some Values.CreateSignalCatalogResponse.error_to_json)])
let create_state_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and dataExtraDimensions =
         flag "data-extra-dimensions" (optional json_arg)
           ~doc:"JSON StateTemplateDataExtraDimensionNodePathList"
       and metadataExtraDimensions =
         flag "metadata-extra-dimensions" (optional json_arg)
           ~doc:"JSON StateTemplateMetadataExtraDimensionNodePathList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING resourceName"
       and signalCatalogArn =
         flag "signal-catalog-arn" (required string) ~doc:"STRING arn"
       and stateTemplateProperties =
         flag "state-template-properties" (required json_arg)
           ~doc:"JSON StateTemplateProperties" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_state_template
           (Values.CreateStateTemplateRequest.make ?description
              ?dataExtraDimensions:(Option.map
                                      ~f:Values.StateTemplateDataExtraDimensionNodePathList.of_json
                                      dataExtraDimensions)
              ?metadataExtraDimensions:(Option.map
                                          ~f:Values.StateTemplateMetadataExtraDimensionNodePathList.of_json
                                          metadataExtraDimensions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~signalCatalogArn
              ~stateTemplateProperties:(Values.StateTemplateProperties.of_json
                                          stateTemplateProperties) ())
           (Some Values.CreateStateTemplateResponse.to_json)
           (Some Values.CreateStateTemplateResponse.error_to_json)])
let create_vehicle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON attributesMap"
       and associationBehavior =
         flag "association-behavior" (optional json_arg)
           ~doc:"JSON VehicleAssociationBehavior"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and stateTemplates =
         flag "state-templates" (optional json_arg)
           ~doc:"JSON StateTemplateAssociations"
       and vehicleName =
         flag "vehicle-name" (required string) ~doc:"STRING vehicleName"
       and modelManifestArn =
         flag "model-manifest-arn" (required string) ~doc:"STRING arn"
       and decoderManifestArn =
         flag "decoder-manifest-arn" (required string) ~doc:"STRING arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vehicle
           (Values.CreateVehicleRequest.make
              ?attributes:(Option.map ~f:Values.AttributesMap.of_json
                             attributes)
              ?associationBehavior:(Option.map
                                      ~f:Values.VehicleAssociationBehavior.of_json
                                      associationBehavior)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?stateTemplates:(Option.map
                                 ~f:Values.StateTemplateAssociations.of_json
                                 stateTemplates) ~vehicleName
              ~modelManifestArn ~decoderManifestArn ())
           (Some Values.CreateVehicleResponse.to_json)
           (Some Values.CreateVehicleResponse.error_to_json)])
let delete_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING campaignName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_campaign (Values.DeleteCampaignRequest.make ~name ())
           (Some Values.DeleteCampaignResponse.to_json)
           (Some Values.DeleteCampaignResponse.error_to_json)])
let delete_decoder_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_decoder_manifest
           (Values.DeleteDecoderManifestRequest.make ~name ())
           (Some Values.DeleteDecoderManifestResponse.to_json)
           (Some Values.DeleteDecoderManifestResponse.error_to_json)])
let delete_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING fleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleet (Values.DeleteFleetRequest.make ~fleetId ())
           (Some Values.DeleteFleetResponse.to_json)
           (Some Values.DeleteFleetResponse.error_to_json)])
let delete_model_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_manifest
           (Values.DeleteModelManifestRequest.make ~name ())
           (Some Values.DeleteModelManifestResponse.to_json)
           (Some Values.DeleteModelManifestResponse.error_to_json)])
let delete_signal_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_signal_catalog
           (Values.DeleteSignalCatalogRequest.make ~name ())
           (Some Values.DeleteSignalCatalogResponse.to_json)
           (Some Values.DeleteSignalCatalogResponse.error_to_json)])
let delete_state_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_state_template
           (Values.DeleteStateTemplateRequest.make ~identifier ())
           (Some Values.DeleteStateTemplateResponse.to_json)
           (Some Values.DeleteStateTemplateResponse.error_to_json)])
let delete_vehicle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vehicleName =
         flag "vehicle-name" (required string) ~doc:"STRING vehicleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vehicle
           (Values.DeleteVehicleRequest.make ~vehicleName ())
           (Some Values.DeleteVehicleResponse.to_json)
           (Some Values.DeleteVehicleResponse.error_to_json)])
let disassociate_vehicle_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vehicleName =
         flag "vehicle-name" (required string) ~doc:"STRING vehicleName"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING fleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_vehicle_fleet
           (Values.DisassociateVehicleFleetRequest.make ~vehicleName ~fleetId
              ()) (Some Values.DisassociateVehicleFleetResponse.to_json)
           (Some Values.DisassociateVehicleFleetResponse.error_to_json)])
let get_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING campaignName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_campaign (Values.GetCampaignRequest.make ~name ())
           (Some Values.GetCampaignResponse.to_json)
           (Some Values.GetCampaignResponse.error_to_json)])
let get_decoder_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_decoder_manifest
           (Values.GetDecoderManifestRequest.make ~name ())
           (Some Values.GetDecoderManifestResponse.to_json)
           (Some Values.GetDecoderManifestResponse.error_to_json)])
let get_encryption_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_encryption_configuration
           (Values.GetEncryptionConfigurationRequest.make ())
           (Some Values.GetEncryptionConfigurationResponse.to_json)
           (Some Values.GetEncryptionConfigurationResponse.error_to_json)])
let get_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING fleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_fleet (Values.GetFleetRequest.make ~fleetId ())
           (Some Values.GetFleetResponse.to_json)
           (Some Values.GetFleetResponse.error_to_json)])
let get_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_logging_options (Values.GetLoggingOptionsRequest.make ())
           (Some Values.GetLoggingOptionsResponse.to_json)
           (Some Values.GetLoggingOptionsResponse.error_to_json)])
let get_model_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_manifest
           (Values.GetModelManifestRequest.make ~name ())
           (Some Values.GetModelManifestResponse.to_json)
           (Some Values.GetModelManifestResponse.error_to_json)])
let get_register_account_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_register_account_status
           (Values.GetRegisterAccountStatusRequest.make ())
           (Some Values.GetRegisterAccountStatusResponse.to_json)
           (Some Values.GetRegisterAccountStatusResponse.error_to_json)])
let get_signal_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_signal_catalog
           (Values.GetSignalCatalogRequest.make ~name ())
           (Some Values.GetSignalCatalogResponse.to_json)
           (Some Values.GetSignalCatalogResponse.error_to_json)])
let get_state_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_state_template
           (Values.GetStateTemplateRequest.make ~identifier ())
           (Some Values.GetStateTemplateResponse.to_json)
           (Some Values.GetStateTemplateResponse.error_to_json)])
let get_vehicle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vehicleName =
         flag "vehicle-name" (required string) ~doc:"STRING vehicleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vehicle (Values.GetVehicleRequest.make ~vehicleName ())
           (Some Values.GetVehicleResponse.to_json)
           (Some Values.GetVehicleResponse.error_to_json)])
let get_vehicle_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and vehicleName =
         flag "vehicle-name" (required string) ~doc:"STRING vehicleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vehicle_status
           (Values.GetVehicleStatusRequest.make ?nextToken ?maxResults
              ~vehicleName ()) (Some Values.GetVehicleStatusResponse.to_json)
           (Some Values.GetVehicleStatusResponse.error_to_json)])
let import_decoder_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING resourceName"
       and networkFileDefinitions =
         flag "network-file-definitions" (required json_arg)
           ~doc:"JSON NetworkFileDefinitions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_decoder_manifest
           (Values.ImportDecoderManifestRequest.make ~name
              ~networkFileDefinitions:(Values.NetworkFileDefinitions.of_json
                                         networkFileDefinitions) ())
           (Some Values.ImportDecoderManifestResponse.to_json)
           (Some Values.ImportDecoderManifestResponse.error_to_json)])
let import_signal_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and vss = flag "vss" (optional json_arg) ~doc:"JSON FormattedVss"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_signal_catalog
           (Values.ImportSignalCatalogRequest.make ?description
              ?vss:(Option.map ~f:Values.FormattedVss.of_json vss)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.ImportSignalCatalogResponse.to_json)
           (Some Values.ImportSignalCatalogResponse.error_to_json)])
let list_campaigns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and status = flag "status" (optional string) ~doc:"STRING statusStr"
       and listResponseScope =
         flag "list-response-scope" (optional json_arg)
           ~doc:"JSON ListResponseScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_campaigns
           (Values.ListCampaignsRequest.make ?nextToken ?maxResults ?status
              ?listResponseScope:(Option.map
                                    ~f:Values.ListResponseScope.of_json
                                    listResponseScope) ())
           (Some Values.ListCampaignsResponse.to_json)
           (Some Values.ListCampaignsResponse.error_to_json)])
let list_decoder_manifest_network_interfaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_decoder_manifest_network_interfaces
           (Values.ListDecoderManifestNetworkInterfacesRequest.make
              ?nextToken ?maxResults ~name ())
           (Some Values.ListDecoderManifestNetworkInterfacesResponse.to_json)
           (Some
              Values.ListDecoderManifestNetworkInterfacesResponse.error_to_json)])
let list_decoder_manifest_signals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_decoder_manifest_signals
           (Values.ListDecoderManifestSignalsRequest.make ?nextToken
              ?maxResults ~name ())
           (Some Values.ListDecoderManifestSignalsResponse.to_json)
           (Some Values.ListDecoderManifestSignalsResponse.error_to_json)])
let list_decoder_manifests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelManifestArn =
         flag "model-manifest-arn" (optional string) ~doc:"STRING arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and listResponseScope =
         flag "list-response-scope" (optional json_arg)
           ~doc:"JSON ListResponseScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_decoder_manifests
           (Values.ListDecoderManifestsRequest.make ?modelManifestArn
              ?nextToken ?maxResults
              ?listResponseScope:(Option.map
                                    ~f:Values.ListResponseScope.of_json
                                    listResponseScope) ())
           (Some Values.ListDecoderManifestsResponse.to_json)
           (Some Values.ListDecoderManifestsResponse.error_to_json)])
let list_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and listResponseScope =
         flag "list-response-scope" (optional json_arg)
           ~doc:"JSON ListResponseScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fleets
           (Values.ListFleetsRequest.make ?nextToken ?maxResults
              ?listResponseScope:(Option.map
                                    ~f:Values.ListResponseScope.of_json
                                    listResponseScope) ())
           (Some Values.ListFleetsResponse.to_json)
           (Some Values.ListFleetsResponse.error_to_json)])
let list_fleets_for_vehicle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and vehicleName =
         flag "vehicle-name" (required string) ~doc:"STRING vehicleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fleets_for_vehicle
           (Values.ListFleetsForVehicleRequest.make ?nextToken ?maxResults
              ~vehicleName ())
           (Some Values.ListFleetsForVehicleResponse.to_json)
           (Some Values.ListFleetsForVehicleResponse.error_to_json)])
let list_model_manifest_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_model_manifest_nodes
           (Values.ListModelManifestNodesRequest.make ?nextToken ?maxResults
              ~name ()) (Some Values.ListModelManifestNodesResponse.to_json)
           (Some Values.ListModelManifestNodesResponse.error_to_json)])
let list_model_manifests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and signalCatalogArn =
         flag "signal-catalog-arn" (optional string) ~doc:"STRING arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and listResponseScope =
         flag "list-response-scope" (optional json_arg)
           ~doc:"JSON ListResponseScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_model_manifests
           (Values.ListModelManifestsRequest.make ?signalCatalogArn
              ?nextToken ?maxResults
              ?listResponseScope:(Option.map
                                    ~f:Values.ListResponseScope.of_json
                                    listResponseScope) ())
           (Some Values.ListModelManifestsResponse.to_json)
           (Some Values.ListModelManifestsResponse.error_to_json)])
let list_signal_catalog_nodes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and signalNodeType =
         flag "signal-node-type" (optional json_arg)
           ~doc:"JSON SignalNodeType"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_signal_catalog_nodes
           (Values.ListSignalCatalogNodesRequest.make ?nextToken ?maxResults
              ?signalNodeType:(Option.map ~f:Values.SignalNodeType.of_json
                                 signalNodeType) ~name ())
           (Some Values.ListSignalCatalogNodesResponse.to_json)
           (Some Values.ListSignalCatalogNodesResponse.error_to_json)])
let list_signal_catalogs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_signal_catalogs
           (Values.ListSignalCatalogsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListSignalCatalogsResponse.to_json)
           (Some Values.ListSignalCatalogsResponse.error_to_json)])
let list_state_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and listResponseScope =
         flag "list-response-scope" (optional json_arg)
           ~doc:"JSON ListResponseScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_state_templates
           (Values.ListStateTemplatesRequest.make ?nextToken ?maxResults
              ?listResponseScope:(Option.map
                                    ~f:Values.ListResponseScope.of_json
                                    listResponseScope) ())
           (Some Values.ListStateTemplatesResponse.to_json)
           (Some Values.ListStateTemplatesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_vehicles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelManifestArn =
         flag "model-manifest-arn" (optional string) ~doc:"STRING arn"
       and attributeNames =
         flag "attribute-names" (optional json_arg)
           ~doc:"JSON attributeNamesList"
       and attributeValues =
         flag "attribute-values" (optional json_arg)
           ~doc:"JSON attributeValuesList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT listVehiclesMaxResults"
       and listResponseScope =
         flag "list-response-scope" (optional json_arg)
           ~doc:"JSON ListResponseScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vehicles
           (Values.ListVehiclesRequest.make ?modelManifestArn
              ?attributeNames:(Option.map
                                 ~f:Values.AttributeNamesList.of_json
                                 attributeNames)
              ?attributeValues:(Option.map
                                  ~f:Values.AttributeValuesList.of_json
                                  attributeValues) ?nextToken ?maxResults
              ?listResponseScope:(Option.map
                                    ~f:Values.ListResponseScope.of_json
                                    listResponseScope) ())
           (Some Values.ListVehiclesResponse.to_json)
           (Some Values.ListVehiclesResponse.error_to_json)])
let list_vehicles_in_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING nextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT maxResults"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING fleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vehicles_in_fleet
           (Values.ListVehiclesInFleetRequest.make ?nextToken ?maxResults
              ~fleetId ()) (Some Values.ListVehiclesInFleetResponse.to_json)
           (Some Values.ListVehiclesInFleetResponse.error_to_json)])
let put_encryption_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string)
           ~doc:"STRING PutEncryptionConfigurationRequestKmsKeyIdString"
       and encryptionType =
         flag "encryption-type" (required json_arg)
           ~doc:"JSON EncryptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_encryption_configuration
           (Values.PutEncryptionConfigurationRequest.make ?kmsKeyId
              ~encryptionType:(Values.EncryptionType.of_json encryptionType)
              ()) (Some Values.PutEncryptionConfigurationResponse.to_json)
           (Some Values.PutEncryptionConfigurationResponse.error_to_json)])
let put_logging_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cloudWatchLogDelivery =
         flag "cloud-watch-log-delivery" (required json_arg)
           ~doc:"JSON CloudWatchLogDeliveryOptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_logging_options
           (Values.PutLoggingOptionsRequest.make
              ~cloudWatchLogDelivery:(Values.CloudWatchLogDeliveryOptions.of_json
                                        cloudWatchLogDelivery) ())
           (Some Values.PutLoggingOptionsResponse.to_json)
           (Some Values.PutLoggingOptionsResponse.error_to_json)])
let register_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timestreamResources =
         flag "timestream-resources" (optional json_arg)
           ~doc:"JSON TimestreamResources"
       and iamResources =
         flag "iam-resources" (optional json_arg) ~doc:"JSON IamResources" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_account
           (Values.RegisterAccountRequest.make
              ?timestreamResources:(Option.map
                                      ~f:Values.TimestreamResources.of_json
                                      timestreamResources)
              ?iamResources:(Option.map ~f:Values.IamResources.of_json
                               iamResources) ())
           (Some Values.RegisterAccountResponse.to_json)
           (Some Values.RegisterAccountResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and dataExtraDimensions =
         flag "data-extra-dimensions" (optional json_arg)
           ~doc:"JSON DataExtraDimensionNodePathList"
       and name = flag "name" (required string) ~doc:"STRING campaignName"
       and action =
         flag "action" (required json_arg) ~doc:"JSON UpdateCampaignAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign
           (Values.UpdateCampaignRequest.make ?description
              ?dataExtraDimensions:(Option.map
                                      ~f:Values.DataExtraDimensionNodePathList.of_json
                                      dataExtraDimensions) ~name
              ~action:(Values.UpdateCampaignAction.of_json action) ())
           (Some Values.UpdateCampaignResponse.to_json)
           (Some Values.UpdateCampaignResponse.error_to_json)])
let update_decoder_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and signalDecodersToAdd =
         flag "signal-decoders-to-add" (optional json_arg)
           ~doc:"JSON SignalDecoders"
       and signalDecodersToUpdate =
         flag "signal-decoders-to-update" (optional json_arg)
           ~doc:"JSON SignalDecoders"
       and signalDecodersToRemove =
         flag "signal-decoders-to-remove" (optional json_arg)
           ~doc:"JSON Fqns"
       and networkInterfacesToAdd =
         flag "network-interfaces-to-add" (optional json_arg)
           ~doc:"JSON NetworkInterfaces"
       and networkInterfacesToUpdate =
         flag "network-interfaces-to-update" (optional json_arg)
           ~doc:"JSON NetworkInterfaces"
       and networkInterfacesToRemove =
         flag "network-interfaces-to-remove" (optional json_arg)
           ~doc:"JSON InterfaceIds"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ManifestStatus"
       and defaultForUnmappedSignals =
         flag "default-for-unmapped-signals" (optional json_arg)
           ~doc:"JSON DefaultForUnmappedSignalsType"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_decoder_manifest
           (Values.UpdateDecoderManifestRequest.make ?description
              ?signalDecodersToAdd:(Option.map
                                      ~f:Values.SignalDecoders.of_json
                                      signalDecodersToAdd)
              ?signalDecodersToUpdate:(Option.map
                                         ~f:Values.SignalDecoders.of_json
                                         signalDecodersToUpdate)
              ?signalDecodersToRemove:(Option.map ~f:Values.Fqns.of_json
                                         signalDecodersToRemove)
              ?networkInterfacesToAdd:(Option.map
                                         ~f:Values.NetworkInterfaces.of_json
                                         networkInterfacesToAdd)
              ?networkInterfacesToUpdate:(Option.map
                                            ~f:Values.NetworkInterfaces.of_json
                                            networkInterfacesToUpdate)
              ?networkInterfacesToRemove:(Option.map
                                            ~f:Values.InterfaceIds.of_json
                                            networkInterfacesToRemove)
              ?status:(Option.map ~f:Values.ManifestStatus.of_json status)
              ?defaultForUnmappedSignals:(Option.map
                                            ~f:Values.DefaultForUnmappedSignalsType.of_json
                                            defaultForUnmappedSignals) ~name
              ()) (Some Values.UpdateDecoderManifestResponse.to_json)
           (Some Values.UpdateDecoderManifestResponse.error_to_json)])
let update_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING fleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fleet
           (Values.UpdateFleetRequest.make ?description ~fleetId ())
           (Some Values.UpdateFleetResponse.to_json)
           (Some Values.UpdateFleetResponse.error_to_json)])
let update_model_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and nodesToAdd =
         flag "nodes-to-add" (optional json_arg) ~doc:"JSON NodePaths"
       and nodesToRemove =
         flag "nodes-to-remove" (optional json_arg) ~doc:"JSON NodePaths"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ManifestStatus"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_model_manifest
           (Values.UpdateModelManifestRequest.make ?description
              ?nodesToAdd:(Option.map ~f:Values.NodePaths.of_json nodesToAdd)
              ?nodesToRemove:(Option.map ~f:Values.NodePaths.of_json
                                nodesToRemove)
              ?status:(Option.map ~f:Values.ManifestStatus.of_json status)
              ~name ()) (Some Values.UpdateModelManifestResponse.to_json)
           (Some Values.UpdateModelManifestResponse.error_to_json)])
let update_signal_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and nodesToAdd =
         flag "nodes-to-add" (optional json_arg) ~doc:"JSON Nodes"
       and nodesToUpdate =
         flag "nodes-to-update" (optional json_arg) ~doc:"JSON Nodes"
       and nodesToRemove =
         flag "nodes-to-remove" (optional json_arg) ~doc:"JSON NodePaths"
       and name = flag "name" (required string) ~doc:"STRING resourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_signal_catalog
           (Values.UpdateSignalCatalogRequest.make ?description
              ?nodesToAdd:(Option.map ~f:Values.Nodes.of_json nodesToAdd)
              ?nodesToUpdate:(Option.map ~f:Values.Nodes.of_json
                                nodesToUpdate)
              ?nodesToRemove:(Option.map ~f:Values.NodePaths.of_json
                                nodesToRemove) ~name ())
           (Some Values.UpdateSignalCatalogResponse.to_json)
           (Some Values.UpdateSignalCatalogResponse.error_to_json)])
let update_state_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING description"
       and stateTemplatePropertiesToAdd =
         flag "state-template-properties-to-add" (optional json_arg)
           ~doc:"JSON StateTemplateProperties"
       and stateTemplatePropertiesToRemove =
         flag "state-template-properties-to-remove" (optional json_arg)
           ~doc:"JSON StateTemplateProperties"
       and dataExtraDimensions =
         flag "data-extra-dimensions" (optional json_arg)
           ~doc:"JSON StateTemplateDataExtraDimensionNodePathList"
       and metadataExtraDimensions =
         flag "metadata-extra-dimensions" (optional json_arg)
           ~doc:"JSON StateTemplateMetadataExtraDimensionNodePathList"
       and identifier =
         flag "identifier" (required string) ~doc:"STRING ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_state_template
           (Values.UpdateStateTemplateRequest.make ?description
              ?stateTemplatePropertiesToAdd:(Option.map
                                               ~f:Values.StateTemplateProperties.of_json
                                               stateTemplatePropertiesToAdd)
              ?stateTemplatePropertiesToRemove:(Option.map
                                                  ~f:Values.StateTemplateProperties.of_json
                                                  stateTemplatePropertiesToRemove)
              ?dataExtraDimensions:(Option.map
                                      ~f:Values.StateTemplateDataExtraDimensionNodePathList.of_json
                                      dataExtraDimensions)
              ?metadataExtraDimensions:(Option.map
                                          ~f:Values.StateTemplateMetadataExtraDimensionNodePathList.of_json
                                          metadataExtraDimensions)
              ~identifier ())
           (Some Values.UpdateStateTemplateResponse.to_json)
           (Some Values.UpdateStateTemplateResponse.error_to_json)])
let update_vehicle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelManifestArn =
         flag "model-manifest-arn" (optional string) ~doc:"STRING arn"
       and decoderManifestArn =
         flag "decoder-manifest-arn" (optional string) ~doc:"STRING arn"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON attributesMap"
       and attributeUpdateMode =
         flag "attribute-update-mode" (optional json_arg)
           ~doc:"JSON UpdateMode"
       and stateTemplatesToAdd =
         flag "state-templates-to-add" (optional json_arg)
           ~doc:"JSON StateTemplateAssociations"
       and stateTemplatesToRemove =
         flag "state-templates-to-remove" (optional json_arg)
           ~doc:"JSON StateTemplateAssociationIdentifiers"
       and stateTemplatesToUpdate =
         flag "state-templates-to-update" (optional json_arg)
           ~doc:"JSON StateTemplateAssociations"
       and vehicleName =
         flag "vehicle-name" (required string) ~doc:"STRING vehicleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_vehicle
           (Values.UpdateVehicleRequest.make ?modelManifestArn
              ?decoderManifestArn
              ?attributes:(Option.map ~f:Values.AttributesMap.of_json
                             attributes)
              ?attributeUpdateMode:(Option.map ~f:Values.UpdateMode.of_json
                                      attributeUpdateMode)
              ?stateTemplatesToAdd:(Option.map
                                      ~f:Values.StateTemplateAssociations.of_json
                                      stateTemplatesToAdd)
              ?stateTemplatesToRemove:(Option.map
                                         ~f:Values.StateTemplateAssociationIdentifiers.of_json
                                         stateTemplatesToRemove)
              ?stateTemplatesToUpdate:(Option.map
                                         ~f:Values.StateTemplateAssociations.of_json
                                         stateTemplatesToUpdate) ~vehicleName
              ()) (Some Values.UpdateVehicleResponse.to_json)
           (Some Values.UpdateVehicleResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-vehicle-fleet", associate_vehicle_fleet);
    ("batch-create-vehicle", batch_create_vehicle);
    ("batch-update-vehicle", batch_update_vehicle);
    ("create-campaign", create_campaign);
    ("create-decoder-manifest", create_decoder_manifest);
    ("create-fleet", create_fleet);
    ("create-model-manifest", create_model_manifest);
    ("create-signal-catalog", create_signal_catalog);
    ("create-state-template", create_state_template);
    ("create-vehicle", create_vehicle);
    ("delete-campaign", delete_campaign);
    ("delete-decoder-manifest", delete_decoder_manifest);
    ("delete-fleet", delete_fleet);
    ("delete-model-manifest", delete_model_manifest);
    ("delete-signal-catalog", delete_signal_catalog);
    ("delete-state-template", delete_state_template);
    ("delete-vehicle", delete_vehicle);
    ("disassociate-vehicle-fleet", disassociate_vehicle_fleet);
    ("get-campaign", get_campaign);
    ("get-decoder-manifest", get_decoder_manifest);
    ("get-encryption-configuration", get_encryption_configuration);
    ("get-fleet", get_fleet);
    ("get-logging-options", get_logging_options);
    ("get-model-manifest", get_model_manifest);
    ("get-register-account-status", get_register_account_status);
    ("get-signal-catalog", get_signal_catalog);
    ("get-state-template", get_state_template);
    ("get-vehicle", get_vehicle);
    ("get-vehicle-status", get_vehicle_status);
    ("import-decoder-manifest", import_decoder_manifest);
    ("import-signal-catalog", import_signal_catalog);
    ("list-campaigns", list_campaigns);
    ("list-decoder-manifest-network-interfaces",
      list_decoder_manifest_network_interfaces);
    ("list-decoder-manifest-signals", list_decoder_manifest_signals);
    ("list-decoder-manifests", list_decoder_manifests);
    ("list-fleets", list_fleets);
    ("list-fleets-for-vehicle", list_fleets_for_vehicle);
    ("list-model-manifest-nodes", list_model_manifest_nodes);
    ("list-model-manifests", list_model_manifests);
    ("list-signal-catalog-nodes", list_signal_catalog_nodes);
    ("list-signal-catalogs", list_signal_catalogs);
    ("list-state-templates", list_state_templates);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-vehicles", list_vehicles);
    ("list-vehicles-in-fleet", list_vehicles_in_fleet);
    ("put-encryption-configuration", put_encryption_configuration);
    ("put-logging-options", put_logging_options);
    ("register-account", register_account);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-campaign", update_campaign);
    ("update-decoder-manifest", update_decoder_manifest);
    ("update-fleet", update_fleet);
    ("update-model-manifest", update_model_manifest);
    ("update-signal-catalog", update_signal_catalog);
    ("update-state-template", update_state_template);
    ("update-vehicle", update_vehicle)]
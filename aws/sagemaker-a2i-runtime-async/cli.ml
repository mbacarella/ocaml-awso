(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_human_loop =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and humanLoopName =
         flag "human-loop-name" (required string) ~doc:"STRING HumanLoopName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_human_loop
           (Values.DeleteHumanLoopRequest.make ~humanLoopName ())
           (Some Values.DeleteHumanLoopResponse.to_json)
           (Some Values.DeleteHumanLoopResponse.error_to_json)])
let describe_human_loop =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and humanLoopName =
         flag "human-loop-name" (required string) ~doc:"STRING HumanLoopName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_human_loop
           (Values.DescribeHumanLoopRequest.make ~humanLoopName ())
           (Some Values.DescribeHumanLoopResponse.to_json)
           (Some Values.DescribeHumanLoopResponse.error_to_json)])
let list_human_loops =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON Timestamp"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and flowDefinitionArn =
         flag "flow-definition-arn" (required string)
           ~doc:"STRING FlowDefinitionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_human_loops
           (Values.ListHumanLoopsRequest.make
              ?creationTimeAfter:(Option.map ~f:Values.Timestamp.of_json
                                    creationTimeAfter)
              ?creationTimeBefore:(Option.map ~f:Values.Timestamp.of_json
                                     creationTimeBefore)
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?maxResults ~flowDefinitionArn ())
           (Some Values.ListHumanLoopsResponse.to_json)
           (Some Values.ListHumanLoopsResponse.error_to_json)])
let start_human_loop =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataAttributes =
         flag "data-attributes" (optional json_arg)
           ~doc:"JSON HumanLoopDataAttributes"
       and humanLoopName =
         flag "human-loop-name" (required string) ~doc:"STRING HumanLoopName"
       and flowDefinitionArn =
         flag "flow-definition-arn" (required string)
           ~doc:"STRING FlowDefinitionArn"
       and humanLoopInput =
         flag "human-loop-input" (required json_arg)
           ~doc:"JSON HumanLoopInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_human_loop
           (Values.StartHumanLoopRequest.make
              ?dataAttributes:(Option.map
                                 ~f:Values.HumanLoopDataAttributes.of_json
                                 dataAttributes) ~humanLoopName
              ~flowDefinitionArn
              ~humanLoopInput:(Values.HumanLoopInput.of_json humanLoopInput)
              ()) (Some Values.StartHumanLoopResponse.to_json)
           (Some Values.StartHumanLoopResponse.error_to_json)])
let stop_human_loop =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and humanLoopName =
         flag "human-loop-name" (required string) ~doc:"STRING HumanLoopName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_human_loop
           (Values.StopHumanLoopRequest.make ~humanLoopName ())
           (Some Values.StopHumanLoopResponse.to_json)
           (Some Values.StopHumanLoopResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("delete-human-loop", delete_human_loop);
    ("describe-human-loop", describe_human_loop);
    ("list-human-loops", list_human_loops);
    ("start-human-loop", start_human_loop);
    ("stop-human-loop", stop_human_loop)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | ActivateEmailContact: (ActivateEmailContactRequest.t,
  ActivateEmailContactResponse.t, ActivateEmailContactResponse.error) t 
  | CreateEmailContact: (CreateEmailContactRequest.t,
  CreateEmailContactResponse.t, CreateEmailContactResponse.error) t 
  | DeleteEmailContact: (DeleteEmailContactRequest.t,
  DeleteEmailContactResponse.t, DeleteEmailContactResponse.error) t 
  | GetEmailContact: (GetEmailContactRequest.t, GetEmailContactResponse.t,
  GetEmailContactResponse.error) t 
  | ListEmailContacts: (ListEmailContactsRequest.t,
  ListEmailContactsResponse.t, ListEmailContactsResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | SendActivationCode: (SendActivationCodeRequest.t,
  SendActivationCodeResponse.t, SendActivationCodeResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | ActivateEmailContact -> `PUT
  | CreateEmailContact -> `POST
  | DeleteEmailContact -> `DELETE
  | GetEmailContact -> `GET
  | ListEmailContacts -> `GET
  | ListTagsForResource -> `GET
  | SendActivationCode -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | ActivateEmailContact ->
          (Format.kasprintf Uri.of_string) "/emailcontacts/%s/activate/%s"
            (EmailContactArn.to_header x.ActivateEmailContactRequest.arn)
            (Token.to_header x.ActivateEmailContactRequest.code)
      | CreateEmailContact ->
          (Format.kasprintf Uri.of_string) "/2022-09-19/emailcontacts"
      | DeleteEmailContact ->
          (Format.kasprintf Uri.of_string) "/emailcontacts/%s"
            (EmailContactArn.to_header x.DeleteEmailContactRequest.arn)
      | GetEmailContact ->
          (Format.kasprintf Uri.of_string) "/emailcontacts/%s"
            (EmailContactArn.to_header x.GetEmailContactRequest.arn)
      | ListEmailContacts ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/emailcontacts")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("maxResults",
                          (ListEmailContactsRequestMaxResultsInteger.to_header
                             v))) x.maxResults;
               Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                 x.nextToken])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (EmailContactArn.to_header x.ListTagsForResourceRequest.arn)
      | SendActivationCode ->
          (Format.kasprintf Uri.of_string)
            "/2022-10-31/emailcontacts/%s/activate/send"
            (EmailContactArn.to_header x.SendActivationCodeRequest.arn)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (EmailContactArn.to_header x.TagResourceRequest.arn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (EmailContactArn.to_header x.UntagResourceRequest.arn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeys.to_header x.tagKeys))]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | ActivateEmailContact -> Awso.Http.Request.make (method_of_endpoint endp)
  | CreateEmailContact ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("name",
                           (EmailContactName.to_value
                              req.CreateEmailContactRequest.name));
                      Some
                        ("emailAddress",
                          (EmailContactAddress.to_value
                             req.CreateEmailContactRequest.emailAddress));
                      Option.map req.CreateEmailContactRequest.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteEmailContact -> Awso.Http.Request.make (method_of_endpoint endp)
  | GetEmailContact ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListEmailContacts ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SendActivationCode ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | ActivateEmailContact ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (ActivateEmailContactResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some ActivateEmailContactResponse.error_of_json))
  | CreateEmailContact ->
      if is_success
      then Ok (CreateEmailContactResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some CreateEmailContactResponse.error_of_json))
  | DeleteEmailContact ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteEmailContactResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some DeleteEmailContactResponse.error_of_json))
  | GetEmailContact ->
      if is_success
      then Ok (GetEmailContactResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetEmailContactResponse.error_of_json))
  | ListEmailContacts ->
      if is_success
      then Ok (ListEmailContactsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListEmailContactsResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | SendActivationCode ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (SendActivationCodeResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some SendActivationCodeResponse.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
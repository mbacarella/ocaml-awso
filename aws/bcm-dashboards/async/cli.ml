(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and name = flag "name" (required string) ~doc:"STRING DashboardName"
       and widgets =
         flag "widgets" (required json_arg) ~doc:"JSON WidgetList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dashboard
           (Values.CreateDashboardRequest.make ?description
              ?resourceTags:(Option.map ~f:Values.ResourceTagList.of_json
                               resourceTags) ~name
              ~widgets:(Values.WidgetList.of_json widgets) ())
           (Some Values.CreateDashboardResponse.to_json)
           (Some Values.CreateDashboardResponse.error_to_json)])
let create_scheduled_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and scheduledReport =
         flag "scheduled-report" (required json_arg)
           ~doc:"JSON ScheduledReportInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_scheduled_report
           (Values.CreateScheduledReportRequest.make
              ?resourceTags:(Option.map ~f:Values.ResourceTagList.of_json
                               resourceTags) ?clientToken
              ~scheduledReport:(Values.ScheduledReportInput.of_json
                                  scheduledReport) ())
           (Some Values.CreateScheduledReportResponse.to_json)
           (Some Values.CreateScheduledReportResponse.error_to_json)])
let delete_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING DashboardArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dashboard (Values.DeleteDashboardRequest.make ~arn ())
           (Some Values.DeleteDashboardResponse.to_json)
           (Some Values.DeleteDashboardResponse.error_to_json)])
let delete_scheduled_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING ScheduledReportArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scheduled_report
           (Values.DeleteScheduledReportRequest.make ~arn ())
           (Some Values.DeleteScheduledReportResponse.to_json)
           (Some Values.DeleteScheduledReportResponse.error_to_json)])
let execute_scheduled_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and arn =
         flag "arn" (required string) ~doc:"STRING ScheduledReportArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_scheduled_report
           (Values.ExecuteScheduledReportRequest.make ?clientToken ?dryRun
              ~arn ()) (Some Values.ExecuteScheduledReportResponse.to_json)
           (Some Values.ExecuteScheduledReportResponse.error_to_json)])
let get_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING DashboardArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dashboard (Values.GetDashboardRequest.make ~arn ())
           (Some Values.GetDashboardResponse.to_json)
           (Some Values.GetDashboardResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING DashboardArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_scheduled_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING ScheduledReportArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_scheduled_report
           (Values.GetScheduledReportRequest.make ~arn ())
           (Some Values.GetScheduledReportResponse.to_json)
           (Some Values.GetScheduledReportResponse.error_to_json)])
let list_dashboards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dashboards
           (Values.ListDashboardsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDashboardsResponse.to_json)
           (Some Values.ListDashboardsResponse.error_to_json)])
let list_scheduled_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextPageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_scheduled_reports
           (Values.ListScheduledReportsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListScheduledReportsResponse.to_json)
           (Some Values.ListScheduledReportsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and resourceTags =
         flag "resource-tags" (required json_arg) ~doc:"JSON ResourceTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~resourceTags:(Values.ResourceTagList.of_json resourceTags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and resourceTagKeys =
         flag "resource-tag-keys" (required json_arg)
           ~doc:"JSON ResourceTagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~resourceTagKeys:(Values.ResourceTagKeyList.of_json
                                  resourceTagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and widgets =
         flag "widgets" (optional json_arg) ~doc:"JSON WidgetList"
       and arn = flag "arn" (required string) ~doc:"STRING DashboardArn"
       and name = flag "name" (required string) ~doc:"STRING DashboardName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dashboard
           (Values.UpdateDashboardRequest.make ?description
              ?widgets:(Option.map ~f:Values.WidgetList.of_json widgets) ~arn
              ~name ()) (Some Values.UpdateDashboardResponse.to_json)
           (Some Values.UpdateDashboardResponse.error_to_json)])
let update_scheduled_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ScheduledReportName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and dashboardArn =
         flag "dashboard-arn" (optional string) ~doc:"STRING DashboardArn"
       and scheduledReportExecutionRoleArn =
         flag "scheduled-report-execution-role-arn" (optional string)
           ~doc:"STRING ServiceRoleArn"
       and scheduleConfig =
         flag "schedule-config" (optional json_arg)
           ~doc:"JSON ScheduleConfig"
       and widgetIds =
         flag "widget-ids" (optional json_arg) ~doc:"JSON WidgetIdList"
       and widgetDateRangeOverride =
         flag "widget-date-range-override" (optional json_arg)
           ~doc:"JSON DateTimeRange"
       and clearWidgetIds =
         flag "clear-widget-ids" (optional bool) ~doc:"BOOL Boolean"
       and clearWidgetDateRangeOverride =
         flag "clear-widget-date-range-override" (optional bool)
           ~doc:"BOOL Boolean"
       and arn =
         flag "arn" (required string) ~doc:"STRING ScheduledReportArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_scheduled_report
           (Values.UpdateScheduledReportRequest.make ?name ?description
              ?dashboardArn ?scheduledReportExecutionRoleArn
              ?scheduleConfig:(Option.map ~f:Values.ScheduleConfig.of_json
                                 scheduleConfig)
              ?widgetIds:(Option.map ~f:Values.WidgetIdList.of_json widgetIds)
              ?widgetDateRangeOverride:(Option.map
                                          ~f:Values.DateTimeRange.of_json
                                          widgetDateRangeOverride)
              ?clearWidgetIds ?clearWidgetDateRangeOverride ~arn ())
           (Some Values.UpdateScheduledReportResponse.to_json)
           (Some Values.UpdateScheduledReportResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-dashboard", create_dashboard);
    ("create-scheduled-report", create_scheduled_report);
    ("delete-dashboard", delete_dashboard);
    ("delete-scheduled-report", delete_scheduled_report);
    ("execute-scheduled-report", execute_scheduled_report);
    ("get-dashboard", get_dashboard);
    ("get-resource-policy", get_resource_policy);
    ("get-scheduled-report", get_scheduled_report);
    ("list-dashboards", list_dashboards);
    ("list-scheduled-reports", list_scheduled_reports);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-dashboard", update_dashboard);
    ("update-scheduled-report", update_scheduled_report)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_device_with_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName"
       and placementName =
         flag "placement-name" (required string) ~doc:"STRING PlacementName"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId"
       and deviceTemplateName =
         flag "device-template-name" (required string)
           ~doc:"STRING DeviceTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_device_with_placement
           (Values.AssociateDeviceWithPlacementRequest.make ~projectName
              ~placementName ~deviceId ~deviceTemplateName ())
           (Some Values.AssociateDeviceWithPlacementResponse.to_json)
           (Some Values.AssociateDeviceWithPlacementResponse.error_to_json)])
let create_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON PlacementAttributeMap"
       and placementName =
         flag "placement-name" (required string) ~doc:"STRING PlacementName"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_placement
           (Values.CreatePlacementRequest.make
              ?attributes:(Option.map ~f:Values.PlacementAttributeMap.of_json
                             attributes) ~placementName ~projectName ())
           (Some Values.CreatePlacementResponse.to_json)
           (Some Values.CreatePlacementResponse.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and placementTemplate =
         flag "placement-template" (optional json_arg)
           ~doc:"JSON PlacementTemplate"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectRequest.make ?description
              ?placementTemplate:(Option.map
                                    ~f:Values.PlacementTemplate.of_json
                                    placementTemplate)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~projectName
              ()) (Some Values.CreateProjectResponse.to_json)
           (Some Values.CreateProjectResponse.error_to_json)])
let delete_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and placementName =
         flag "placement-name" (required string) ~doc:"STRING PlacementName"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_placement
           (Values.DeletePlacementRequest.make ~placementName ~projectName ())
           (Some Values.DeletePlacementResponse.to_json)
           (Some Values.DeletePlacementResponse.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project
           (Values.DeleteProjectRequest.make ~projectName ())
           (Some Values.DeleteProjectResponse.to_json)
           (Some Values.DeleteProjectResponse.error_to_json)])
let describe_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and placementName =
         flag "placement-name" (required string) ~doc:"STRING PlacementName"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_placement
           (Values.DescribePlacementRequest.make ~placementName ~projectName
              ()) (Some Values.DescribePlacementResponse.to_json)
           (Some Values.DescribePlacementResponse.error_to_json)])
let describe_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_project
           (Values.DescribeProjectRequest.make ~projectName ())
           (Some Values.DescribeProjectResponse.to_json)
           (Some Values.DescribeProjectResponse.error_to_json)])
let disassociate_device_from_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName"
       and placementName =
         flag "placement-name" (required string) ~doc:"STRING PlacementName"
       and deviceTemplateName =
         flag "device-template-name" (required string)
           ~doc:"STRING DeviceTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_device_from_placement
           (Values.DisassociateDeviceFromPlacementRequest.make ~projectName
              ~placementName ~deviceTemplateName ())
           (Some Values.DisassociateDeviceFromPlacementResponse.to_json)
           (Some Values.DisassociateDeviceFromPlacementResponse.error_to_json)])
let get_devices_in_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName"
       and placementName =
         flag "placement-name" (required string) ~doc:"STRING PlacementName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_devices_in_placement
           (Values.GetDevicesInPlacementRequest.make ~projectName
              ~placementName ())
           (Some Values.GetDevicesInPlacementResponse.to_json)
           (Some Values.GetDevicesInPlacementResponse.error_to_json)])
let list_placements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_placements
           (Values.ListPlacementsRequest.make ?nextToken ?maxResults
              ~projectName ()) (Some Values.ListPlacementsResponse.to_json)
           (Some Values.ListPlacementsResponse.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListProjectsResponse.to_json)
           (Some Values.ListProjectsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ProjectArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ProjectArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ProjectArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg)
           ~doc:"JSON PlacementAttributeMap"
       and placementName =
         flag "placement-name" (required string) ~doc:"STRING PlacementName"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_placement
           (Values.UpdatePlacementRequest.make
              ?attributes:(Option.map ~f:Values.PlacementAttributeMap.of_json
                             attributes) ~placementName ~projectName ())
           (Some Values.UpdatePlacementResponse.to_json)
           (Some Values.UpdatePlacementResponse.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and placementTemplate =
         flag "placement-template" (optional json_arg)
           ~doc:"JSON PlacementTemplate"
       and projectName =
         flag "project-name" (required string) ~doc:"STRING ProjectName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectRequest.make ?description
              ?placementTemplate:(Option.map
                                    ~f:Values.PlacementTemplate.of_json
                                    placementTemplate) ~projectName ())
           (Some Values.UpdateProjectResponse.to_json)
           (Some Values.UpdateProjectResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-device-with-placement", associate_device_with_placement);
    ("create-placement", create_placement);
    ("create-project", create_project);
    ("delete-placement", delete_placement);
    ("delete-project", delete_project);
    ("describe-placement", describe_placement);
    ("describe-project", describe_project);
    ("disassociate-device-from-placement",
      disassociate_device_from_placement);
    ("get-devices-in-placement", get_devices_in_placement);
    ("list-placements", list_placements);
    ("list-projects", list_projects);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-placement", update_placement);
    ("update-project", update_project)]
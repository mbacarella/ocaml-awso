(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.wellarchitected
let apiVersion = "2020-03-31"
let endpointPrefix = "wellarchitected"
let serviceFullName = "AWS Well-Architected Tool"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Well-Architected"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ChoiceContentDisplayText =
  struct
    type nonrec t = string
    let context_ = "ChoiceContentDisplayText"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChoiceContentDisplayText" j
    let to_json = simple_to_json to_value
  end
module ChoiceContentUrl =
  struct
    type nonrec t = string
    let context_ = "ChoiceContentUrl"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChoiceContentUrl" j
    let to_json = simple_to_json to_value
  end
module DifferenceStatus =
  struct
    type nonrec t =
      | UPDATED 
      | NEW 
      | DELETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UPDATED -> "UPDATED"
      | NEW -> "NEW"
      | DELETED -> "DELETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "UPDATED" -> UPDATED
      | "NEW" -> NEW
      | "DELETED" -> DELETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DifferenceStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DifferenceStatus" j)
    let to_json = simple_to_json to_value
  end
module QuestionId =
  struct
    type nonrec t = string
    let context_ = "QuestionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QuestionId" j
    let to_json = simple_to_json to_value
  end
module QuestionTitle =
  struct
    type nonrec t = string
    let context_ = "QuestionTitle"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QuestionTitle" j
    let to_json = simple_to_json to_value
  end
module Count =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Count" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Risk =
  struct
    type nonrec t =
      | UNANSWERED 
      | HIGH 
      | MEDIUM 
      | NONE 
      | NOT_APPLICABLE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UNANSWERED -> "UNANSWERED"
      | HIGH -> "HIGH"
      | MEDIUM -> "MEDIUM"
      | NONE -> "NONE"
      | NOT_APPLICABLE -> "NOT_APPLICABLE"
      | Non_static_id s -> s
    let of_string =
      function
      | "UNANSWERED" -> UNANSWERED
      | "HIGH" -> HIGH
      | "MEDIUM" -> MEDIUM
      | "NONE" -> NONE
      | "NOT_APPLICABLE" -> NOT_APPLICABLE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Risk" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Risk" j)
    let to_json = simple_to_json to_value
  end
module LensAlias =
  struct
    type nonrec t = string
    let context_ = "LensAlias"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LensAlias" j
    let to_json = simple_to_json to_value
  end
module ChoiceId =
  struct
    type nonrec t = string
    let context_ = "ChoiceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChoiceId" j
    let to_json = simple_to_json to_value
  end
module DisplayText =
  struct
    type nonrec t = string
    let context_ = "DisplayText"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DisplayText" j
    let to_json = simple_to_json to_value
  end
module ImprovementPlanUrl =
  struct
    type nonrec t = string
    let context_ = "ImprovementPlanUrl"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ImprovementPlanUrl" j
    let to_json = simple_to_json to_value
  end
module ChoiceReason =
  struct
    type nonrec t =
      | OUT_OF_SCOPE 
      | BUSINESS_PRIORITIES 
      | ARCHITECTURE_CONSTRAINTS 
      | OTHER 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OUT_OF_SCOPE -> "OUT_OF_SCOPE"
      | BUSINESS_PRIORITIES -> "BUSINESS_PRIORITIES"
      | ARCHITECTURE_CONSTRAINTS -> "ARCHITECTURE_CONSTRAINTS"
      | OTHER -> "OTHER"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "OUT_OF_SCOPE" -> OUT_OF_SCOPE
      | "BUSINESS_PRIORITIES" -> BUSINESS_PRIORITIES
      | "ARCHITECTURE_CONSTRAINTS" -> ARCHITECTURE_CONSTRAINTS
      | "OTHER" -> OTHER
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ChoiceReason" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ChoiceReason" j)
    let to_json = simple_to_json to_value
  end
module ChoiceStatus =
  struct
    type nonrec t =
      | SELECTED 
      | NOT_APPLICABLE 
      | UNSELECTED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SELECTED -> "SELECTED"
      | NOT_APPLICABLE -> "NOT_APPLICABLE"
      | UNSELECTED -> "UNSELECTED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SELECTED" -> SELECTED
      | "NOT_APPLICABLE" -> NOT_APPLICABLE
      | "UNSELECTED" -> UNSELECTED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ChoiceStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ChoiceStatus" j)
    let to_json = simple_to_json to_value
  end
module ChoiceContent =
  struct
    type nonrec t =
      {
      displayText: ChoiceContentDisplayText.t option ;
      url: ChoiceContentUrl.t option }
    let make ?displayText = fun ?url -> fun () -> { displayText; url }
    let to_value x =
      structure_to_value
        [("DisplayText",
           (Option.map x.displayText ~f:ChoiceContentDisplayText.to_value));
        ("Url", (Option.map x.url ~f:ChoiceContentUrl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url =
        (Option.map ~f:ChoiceContentUrl.of_xml) (Xml.child xml_arg0 "Url") in
      let displayText =
        (Option.map ~f:ChoiceContentDisplayText.of_xml)
          (Xml.child xml_arg0 "DisplayText") in
      make ?url ?displayText ()
    let of_json json =
      let url = field_map json "Url" ChoiceContentUrl.of_json in
      let displayText =
        field_map json "DisplayText" ChoiceContentDisplayText.of_json in
      make ?url ?displayText ()
    let to_json v = composed_to_json to_value v
  end
module ChoiceDescription =
  struct
    type nonrec t = string
    let context_ = "ChoiceDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChoiceDescription" j
    let to_json = simple_to_json to_value
  end
module ChoiceTitle =
  struct
    type nonrec t = string
    let context_ = "ChoiceTitle"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChoiceTitle" j
    let to_json = simple_to_json to_value
  end
module QuestionDifference =
  struct
    type nonrec t =
      {
      questionId: QuestionId.t option ;
      questionTitle: QuestionTitle.t option ;
      differenceStatus: DifferenceStatus.t option }
    let make ?questionId =
      fun ?questionTitle ->
        fun ?differenceStatus ->
          fun () -> { questionId; questionTitle; differenceStatus }
    let to_value x =
      structure_to_value
        [("QuestionId", (Option.map x.questionId ~f:QuestionId.to_value));
        ("QuestionTitle",
          (Option.map x.questionTitle ~f:QuestionTitle.to_value));
        ("DifferenceStatus",
          (Option.map x.differenceStatus ~f:DifferenceStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let differenceStatus =
        (Option.map ~f:DifferenceStatus.of_xml)
          (Xml.child xml_arg0 "DifferenceStatus") in
      let questionTitle =
        (Option.map ~f:QuestionTitle.of_xml)
          (Xml.child xml_arg0 "QuestionTitle") in
      let questionId =
        (Option.map ~f:QuestionId.of_xml) (Xml.child xml_arg0 "QuestionId") in
      make ?differenceStatus ?questionTitle ?questionId ()
    let of_json json =
      let differenceStatus =
        field_map json "DifferenceStatus" DifferenceStatus.of_json in
      let questionTitle =
        field_map json "QuestionTitle" QuestionTitle.of_json in
      let questionId = field_map json "QuestionId" QuestionId.of_json in
      make ?differenceStatus ?questionTitle ?questionId ()
    let to_json v = composed_to_json to_value v
  end
module ExceptionMessage =
  struct
    type nonrec t = string
    let context_ = "ExceptionMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionMessage" j
    let to_json = simple_to_json to_value
  end
module ValidationExceptionFieldName =
  struct
    type nonrec t = string
    let context_ = "ValidationExceptionFieldName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ValidationExceptionFieldName" j
    let to_json = simple_to_json to_value
  end
module Notes =
  struct
    type nonrec t = string
    let context_ = "Notes"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:2084); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Notes" j
    let to_json = simple_to_json to_value
  end
module PillarId =
  struct
    type nonrec t = string
    let context_ = "PillarId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PillarId" j
    let to_json = simple_to_json to_value
  end
module PillarName =
  struct
    type nonrec t = string
    let context_ = "PillarName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PillarName" j
    let to_json = simple_to_json to_value
  end
module RiskCounts =
  struct
    type nonrec t = (Risk.t * Count.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((Risk.of_string chopped), (Count.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (Risk.to_value x) |>
                    (fun x -> (Count.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:Risk.of_string ~of_json:Count.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChoiceNotes =
  struct
    type nonrec t = string
    let context_ = "ChoiceNotes"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:250); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChoiceNotes" j
    let to_json = simple_to_json to_value
  end
module LensArn =
  struct
    type nonrec t = string
    let context_ = "LensArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LensArn" j
    let to_json = simple_to_json to_value
  end
module LensVersion =
  struct
    type nonrec t = string
    let context_ = "LensVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LensVersion" j
    let to_json = simple_to_json to_value
  end
module WorkloadId =
  struct
    type nonrec t = string
    let context_ = "WorkloadId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[0-9a-f]{32}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadId" j
    let to_json = simple_to_json to_value
  end
module WorkloadName =
  struct
    type nonrec t = string
    let context_ = "WorkloadName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadName" j
    let to_json = simple_to_json to_value
  end
module AwsAccountId =
  struct
    type nonrec t = string
    let context_ = "AwsAccountId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[0-9]{12}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AwsAccountId" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module WorkloadArn =
  struct
    type nonrec t = string
    let context_ = "WorkloadArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadArn" j
    let to_json = simple_to_json to_value
  end
module WorkloadImprovementStatus =
  struct
    type nonrec t =
      | NOT_APPLICABLE 
      | NOT_STARTED 
      | IN_PROGRESS 
      | COMPLETE 
      | RISK_ACKNOWLEDGED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NOT_APPLICABLE -> "NOT_APPLICABLE"
      | NOT_STARTED -> "NOT_STARTED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMPLETE -> "COMPLETE"
      | RISK_ACKNOWLEDGED -> "RISK_ACKNOWLEDGED"
      | Non_static_id s -> s
    let of_string =
      function
      | "NOT_APPLICABLE" -> NOT_APPLICABLE
      | "NOT_STARTED" -> NOT_STARTED
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMPLETE" -> COMPLETE
      | "RISK_ACKNOWLEDGED" -> RISK_ACKNOWLEDGED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration WorkloadImprovementStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"WorkloadImprovementStatus" j)
    let to_json = simple_to_json to_value
  end
module WorkloadLenses =
  struct
    type nonrec t = LensAlias.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LensAlias.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LensAlias.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkloadLenses" ~of_json:LensAlias.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChoiceImprovementPlan =
  struct
    type nonrec t =
      {
      choiceId: ChoiceId.t option ;
      displayText: DisplayText.t option ;
      improvementPlanUrl: ImprovementPlanUrl.t option }
    let make ?choiceId =
      fun ?displayText ->
        fun ?improvementPlanUrl ->
          fun () -> { choiceId; displayText; improvementPlanUrl }
    let to_value x =
      structure_to_value
        [("ChoiceId", (Option.map x.choiceId ~f:ChoiceId.to_value));
        ("DisplayText", (Option.map x.displayText ~f:DisplayText.to_value));
        ("ImprovementPlanUrl",
          (Option.map x.improvementPlanUrl ~f:ImprovementPlanUrl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let improvementPlanUrl =
        (Option.map ~f:ImprovementPlanUrl.of_xml)
          (Xml.child xml_arg0 "ImprovementPlanUrl") in
      let displayText =
        (Option.map ~f:DisplayText.of_xml) (Xml.child xml_arg0 "DisplayText") in
      let choiceId =
        (Option.map ~f:ChoiceId.of_xml) (Xml.child xml_arg0 "ChoiceId") in
      make ?improvementPlanUrl ?displayText ?choiceId ()
    let of_json json =
      let improvementPlanUrl =
        field_map json "ImprovementPlanUrl" ImprovementPlanUrl.of_json in
      let displayText = field_map json "DisplayText" DisplayText.of_json in
      let choiceId = field_map json "ChoiceId" ChoiceId.of_json in
      make ?improvementPlanUrl ?displayText ?choiceId ()
    let to_json v = composed_to_json to_value v
  end
module ChoiceAnswerSummary =
  struct
    type nonrec t =
      {
      choiceId: ChoiceId.t option ;
      status: ChoiceStatus.t option ;
      reason: ChoiceReason.t option }
    let make ?choiceId =
      fun ?status -> fun ?reason -> fun () -> { choiceId; status; reason }
    let to_value x =
      structure_to_value
        [("ChoiceId", (Option.map x.choiceId ~f:ChoiceId.to_value));
        ("Status", (Option.map x.status ~f:ChoiceStatus.to_value));
        ("Reason", (Option.map x.reason ~f:ChoiceReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:ChoiceReason.of_xml) (Xml.child xml_arg0 "Reason") in
      let status =
        (Option.map ~f:ChoiceStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let choiceId =
        (Option.map ~f:ChoiceId.of_xml) (Xml.child xml_arg0 "ChoiceId") in
      make ?reason ?status ?choiceId ()
    let of_json json =
      let reason = field_map json "Reason" ChoiceReason.of_json in
      let status = field_map json "Status" ChoiceStatus.of_json in
      let choiceId = field_map json "ChoiceId" ChoiceId.of_json in
      make ?reason ?status ?choiceId ()
    let to_json v = composed_to_json to_value v
  end
module Choice =
  struct
    type nonrec t =
      {
      choiceId: ChoiceId.t option ;
      title: ChoiceTitle.t option ;
      description: ChoiceDescription.t option ;
      helpfulResource: ChoiceContent.t option ;
      improvementPlan: ChoiceContent.t option }
    let make ?choiceId =
      fun ?title ->
        fun ?description ->
          fun ?helpfulResource ->
            fun ?improvementPlan ->
              fun () ->
                {
                  choiceId;
                  title;
                  description;
                  helpfulResource;
                  improvementPlan
                }
    let to_value x =
      structure_to_value
        [("ChoiceId", (Option.map x.choiceId ~f:ChoiceId.to_value));
        ("Title", (Option.map x.title ~f:ChoiceTitle.to_value));
        ("Description",
          (Option.map x.description ~f:ChoiceDescription.to_value));
        ("HelpfulResource",
          (Option.map x.helpfulResource ~f:ChoiceContent.to_value));
        ("ImprovementPlan",
          (Option.map x.improvementPlan ~f:ChoiceContent.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let improvementPlan =
        (Option.map ~f:ChoiceContent.of_xml)
          (Xml.child xml_arg0 "ImprovementPlan") in
      let helpfulResource =
        (Option.map ~f:ChoiceContent.of_xml)
          (Xml.child xml_arg0 "HelpfulResource") in
      let description =
        (Option.map ~f:ChoiceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let title =
        (Option.map ~f:ChoiceTitle.of_xml) (Xml.child xml_arg0 "Title") in
      let choiceId =
        (Option.map ~f:ChoiceId.of_xml) (Xml.child xml_arg0 "ChoiceId") in
      make ?improvementPlan ?helpfulResource ?description ?title ?choiceId ()
    let of_json json =
      let improvementPlan =
        field_map json "ImprovementPlan" ChoiceContent.of_json in
      let helpfulResource =
        field_map json "HelpfulResource" ChoiceContent.of_json in
      let description =
        field_map json "Description" ChoiceDescription.of_json in
      let title = field_map json "Title" ChoiceTitle.of_json in
      let choiceId = field_map json "ChoiceId" ChoiceId.of_json in
      make ?improvementPlan ?helpfulResource ?description ?title ?choiceId ()
    let to_json v = composed_to_json to_value v
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module AwsRegion =
  struct
    type nonrec t = string
    let context_ = "AwsRegion"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:100); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AwsRegion" j
    let to_json = simple_to_json to_value
  end
module WorkloadNonAwsRegion =
  struct
    type nonrec t = string
    let context_ = "WorkloadNonAwsRegion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:25) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadNonAwsRegion" j
    let to_json = simple_to_json to_value
  end
module QuestionDifferences =
  struct
    type nonrec t = QuestionDifference.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:QuestionDifference.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:QuestionDifference.of_xml)
    let of_json j =
      list_of_json ~kind:"QuestionDifferences"
        ~of_json:QuestionDifference.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionField =
  struct
    type nonrec t =
      {
      name: ValidationExceptionFieldName.t ;
      message: ExceptionMessage.t }
    let context_ = "ValidationExceptionField"
    let make ~name = fun ~message -> fun () -> { name; message }
    let to_value x =
      structure_to_value
        [("Name", (Some (ValidationExceptionFieldName.to_value x.name)));
        ("Message", (Some (ExceptionMessage.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        ExceptionMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      let name =
        ValidationExceptionFieldName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~message ~name ()
    let of_json json =
      let message = field_map_exn json "Message" ExceptionMessage.of_json in
      let name =
        field_map_exn json "Name" ValidationExceptionFieldName.of_json in
      make ~message ~name ()
    let to_json v = composed_to_json to_value v
  end
module PillarReviewSummary =
  struct
    type nonrec t =
      {
      pillarId: PillarId.t option ;
      pillarName: PillarName.t option ;
      notes: Notes.t option ;
      riskCounts: RiskCounts.t option }
    let make ?pillarId =
      fun ?pillarName ->
        fun ?notes ->
          fun ?riskCounts ->
            fun () -> { pillarId; pillarName; notes; riskCounts }
    let to_value x =
      structure_to_value
        [("PillarId", (Option.map x.pillarId ~f:PillarId.to_value));
        ("PillarName", (Option.map x.pillarName ~f:PillarName.to_value));
        ("Notes", (Option.map x.notes ~f:Notes.to_value));
        ("RiskCounts", (Option.map x.riskCounts ~f:RiskCounts.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let riskCounts =
        (Option.map ~f:RiskCounts.of_xml) (Xml.child xml_arg0 "RiskCounts") in
      let notes = (Option.map ~f:Notes.of_xml) (Xml.child xml_arg0 "Notes") in
      let pillarName =
        (Option.map ~f:PillarName.of_xml) (Xml.child xml_arg0 "PillarName") in
      let pillarId =
        (Option.map ~f:PillarId.of_xml) (Xml.child xml_arg0 "PillarId") in
      make ?riskCounts ?notes ?pillarName ?pillarId ()
    let of_json json =
      let riskCounts = field_map json "RiskCounts" RiskCounts.of_json in
      let notes = field_map json "Notes" Notes.of_json in
      let pillarName = field_map json "PillarName" PillarName.of_json in
      let pillarId = field_map json "PillarId" PillarId.of_json in
      make ?riskCounts ?notes ?pillarName ?pillarId ()
    let to_json v = composed_to_json to_value v
  end
module ChoiceAnswer =
  struct
    type nonrec t =
      {
      choiceId: ChoiceId.t option ;
      status: ChoiceStatus.t option ;
      reason: ChoiceReason.t option ;
      notes: ChoiceNotes.t option }
    let make ?choiceId =
      fun ?status ->
        fun ?reason ->
          fun ?notes -> fun () -> { choiceId; status; reason; notes }
    let to_value x =
      structure_to_value
        [("ChoiceId", (Option.map x.choiceId ~f:ChoiceId.to_value));
        ("Status", (Option.map x.status ~f:ChoiceStatus.to_value));
        ("Reason", (Option.map x.reason ~f:ChoiceReason.to_value));
        ("Notes", (Option.map x.notes ~f:ChoiceNotes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notes =
        (Option.map ~f:ChoiceNotes.of_xml) (Xml.child xml_arg0 "Notes") in
      let reason =
        (Option.map ~f:ChoiceReason.of_xml) (Xml.child xml_arg0 "Reason") in
      let status =
        (Option.map ~f:ChoiceStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let choiceId =
        (Option.map ~f:ChoiceId.of_xml) (Xml.child xml_arg0 "ChoiceId") in
      make ?notes ?reason ?status ?choiceId ()
    let of_json json =
      let notes = field_map json "Notes" ChoiceNotes.of_json in
      let reason = field_map json "Reason" ChoiceReason.of_json in
      let status = field_map json "Status" ChoiceStatus.of_json in
      let choiceId = field_map json "ChoiceId" ChoiceId.of_json in
      make ?notes ?reason ?status ?choiceId ()
    let to_json v = composed_to_json to_value v
  end
module PermissionType =
  struct
    type nonrec t =
      | READONLY 
      | CONTRIBUTOR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | READONLY -> "READONLY"
      | CONTRIBUTOR -> "CONTRIBUTOR"
      | Non_static_id s -> s
    let of_string =
      function
      | "READONLY" -> READONLY
      | "CONTRIBUTOR" -> CONTRIBUTOR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PermissionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PermissionType" j)
    let to_json = simple_to_json to_value
  end
module ShareId =
  struct
    type nonrec t = string
    let context_ = "ShareId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[0-9a-f]{32}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ShareId" j
    let to_json = simple_to_json to_value
  end
module ShareStatus =
  struct
    type nonrec t =
      | ACCEPTED 
      | REJECTED 
      | PENDING 
      | REVOKED 
      | EXPIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACCEPTED -> "ACCEPTED"
      | REJECTED -> "REJECTED"
      | PENDING -> "PENDING"
      | REVOKED -> "REVOKED"
      | EXPIRED -> "EXPIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACCEPTED" -> ACCEPTED
      | "REJECTED" -> REJECTED
      | "PENDING" -> PENDING
      | "REVOKED" -> REVOKED
      | "EXPIRED" -> EXPIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ShareStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ShareStatus" j)
    let to_json = simple_to_json to_value
  end
module SharedWith =
  struct
    type nonrec t = string
    let context_ = "SharedWith"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:12));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SharedWith" j
    let to_json = simple_to_json to_value
  end
module LensName =
  struct
    type nonrec t = string
    let context_ = "LensName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LensName" j
    let to_json = simple_to_json to_value
  end
module ShareInvitationId =
  struct
    type nonrec t = string
    let context_ = "ShareInvitationId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[0-9a-f]{32}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ShareInvitationId" j
    let to_json = simple_to_json to_value
  end
module ShareResourceType =
  struct
    type nonrec t =
      | WORKLOAD 
      | LENS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | WORKLOAD -> "WORKLOAD"
      | LENS -> "LENS"
      | Non_static_id s -> s
    let of_string =
      function
      | "WORKLOAD" -> WORKLOAD
      | "LENS" -> LENS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ShareResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ShareResourceType" j)
    let to_json = simple_to_json to_value
  end
module LensUpgradeSummary =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      workloadName: WorkloadName.t option ;
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      currentLensVersion: LensVersion.t option ;
      latestLensVersion: LensVersion.t option }
    let make ?workloadId =
      fun ?workloadName ->
        fun ?lensAlias ->
          fun ?lensArn ->
            fun ?currentLensVersion ->
              fun ?latestLensVersion ->
                fun () ->
                  {
                    workloadId;
                    workloadName;
                    lensAlias;
                    lensArn;
                    currentLensVersion;
                    latestLensVersion
                  }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("WorkloadName",
          (Option.map x.workloadName ~f:WorkloadName.to_value));
        ("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("CurrentLensVersion",
          (Option.map x.currentLensVersion ~f:LensVersion.to_value));
        ("LatestLensVersion",
          (Option.map x.latestLensVersion ~f:LensVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let latestLensVersion =
        (Option.map ~f:LensVersion.of_xml)
          (Xml.child xml_arg0 "LatestLensVersion") in
      let currentLensVersion =
        (Option.map ~f:LensVersion.of_xml)
          (Xml.child xml_arg0 "CurrentLensVersion") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      let workloadName =
        (Option.map ~f:WorkloadName.of_xml)
          (Xml.child xml_arg0 "WorkloadName") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?latestLensVersion ?currentLensVersion ?lensArn ?lensAlias
        ?workloadName ?workloadId ()
    let of_json json =
      let latestLensVersion =
        field_map json "LatestLensVersion" LensVersion.of_json in
      let currentLensVersion =
        field_map json "CurrentLensVersion" LensVersion.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      let workloadName = field_map json "WorkloadName" WorkloadName.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?latestLensVersion ?currentLensVersion ?lensArn ?lensAlias
        ?workloadName ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module NotificationType =
  struct
    type nonrec t =
      | LENS_VERSION_UPGRADED 
      | LENS_VERSION_DEPRECATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | LENS_VERSION_UPGRADED -> "LENS_VERSION_UPGRADED"
      | LENS_VERSION_DEPRECATED -> "LENS_VERSION_DEPRECATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "LENS_VERSION_UPGRADED" -> LENS_VERSION_UPGRADED
      | "LENS_VERSION_DEPRECATED" -> LENS_VERSION_DEPRECATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration NotificationType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"NotificationType" j)
    let to_json = simple_to_json to_value
  end
module MilestoneName =
  struct
    type nonrec t = string
    let context_ = "MilestoneName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MilestoneName" j
    let to_json = simple_to_json to_value
  end
module MilestoneNumber =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MilestoneNumber" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module WorkloadSummary =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      workloadArn: WorkloadArn.t option ;
      workloadName: WorkloadName.t option ;
      owner: AwsAccountId.t option ;
      updatedAt: Timestamp.t option ;
      lenses: WorkloadLenses.t option ;
      riskCounts: RiskCounts.t option ;
      improvementStatus: WorkloadImprovementStatus.t option }
    let make ?workloadId =
      fun ?workloadArn ->
        fun ?workloadName ->
          fun ?owner ->
            fun ?updatedAt ->
              fun ?lenses ->
                fun ?riskCounts ->
                  fun ?improvementStatus ->
                    fun () ->
                      {
                        workloadId;
                        workloadArn;
                        workloadName;
                        owner;
                        updatedAt;
                        lenses;
                        riskCounts;
                        improvementStatus
                      }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("WorkloadArn", (Option.map x.workloadArn ~f:WorkloadArn.to_value));
        ("WorkloadName",
          (Option.map x.workloadName ~f:WorkloadName.to_value));
        ("Owner", (Option.map x.owner ~f:AwsAccountId.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("Lenses", (Option.map x.lenses ~f:WorkloadLenses.to_value));
        ("RiskCounts", (Option.map x.riskCounts ~f:RiskCounts.to_value));
        ("ImprovementStatus",
          (Option.map x.improvementStatus
             ~f:WorkloadImprovementStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let improvementStatus =
        (Option.map ~f:WorkloadImprovementStatus.of_xml)
          (Xml.child xml_arg0 "ImprovementStatus") in
      let riskCounts =
        (Option.map ~f:RiskCounts.of_xml) (Xml.child xml_arg0 "RiskCounts") in
      let lenses =
        (Option.map ~f:WorkloadLenses.of_xml) (Xml.child xml_arg0 "Lenses") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let owner =
        (Option.map ~f:AwsAccountId.of_xml) (Xml.child xml_arg0 "Owner") in
      let workloadName =
        (Option.map ~f:WorkloadName.of_xml)
          (Xml.child xml_arg0 "WorkloadName") in
      let workloadArn =
        (Option.map ~f:WorkloadArn.of_xml) (Xml.child xml_arg0 "WorkloadArn") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?improvementStatus ?riskCounts ?lenses ?updatedAt ?owner
        ?workloadName ?workloadArn ?workloadId ()
    let of_json json =
      let improvementStatus =
        field_map json "ImprovementStatus" WorkloadImprovementStatus.of_json in
      let riskCounts = field_map json "RiskCounts" RiskCounts.of_json in
      let lenses = field_map json "Lenses" WorkloadLenses.of_json in
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let owner = field_map json "Owner" AwsAccountId.of_json in
      let workloadName = field_map json "WorkloadName" WorkloadName.of_json in
      let workloadArn = field_map json "WorkloadArn" WorkloadArn.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?improvementStatus ?riskCounts ?lenses ?updatedAt ?owner
        ?workloadName ?workloadArn ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module LensDescription =
  struct
    type nonrec t = string
    let context_ = "LensDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LensDescription" j
    let to_json = simple_to_json to_value
  end
module LensStatus =
  struct
    type nonrec t =
      | CURRENT 
      | NOT_CURRENT 
      | DEPRECATED 
      | DELETED 
      | UNSHARED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CURRENT -> "CURRENT"
      | NOT_CURRENT -> "NOT_CURRENT"
      | DEPRECATED -> "DEPRECATED"
      | DELETED -> "DELETED"
      | UNSHARED -> "UNSHARED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CURRENT" -> CURRENT
      | "NOT_CURRENT" -> NOT_CURRENT
      | "DEPRECATED" -> DEPRECATED
      | "DELETED" -> DELETED
      | "UNSHARED" -> UNSHARED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LensStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LensStatus" j)
    let to_json = simple_to_json to_value
  end
module LensType =
  struct
    type nonrec t =
      | AWS_OFFICIAL 
      | CUSTOM_SHARED 
      | CUSTOM_SELF 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWS_OFFICIAL -> "AWS_OFFICIAL"
      | CUSTOM_SHARED -> "CUSTOM_SHARED"
      | CUSTOM_SELF -> "CUSTOM_SELF"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWS_OFFICIAL" -> AWS_OFFICIAL
      | "CUSTOM_SHARED" -> CUSTOM_SHARED
      | "CUSTOM_SELF" -> CUSTOM_SELF
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LensType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LensType" j)
    let to_json = simple_to_json to_value
  end
module ChoiceImprovementPlans =
  struct
    type nonrec t = ChoiceImprovementPlan.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ChoiceImprovementPlan.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChoiceImprovementPlan.of_xml)
    let of_json j =
      list_of_json ~kind:"ChoiceImprovementPlans"
        ~of_json:ChoiceImprovementPlan.of_json j
    let to_json v = composed_to_json to_value v
  end
module AnswerReason =
  struct
    type nonrec t =
      | OUT_OF_SCOPE 
      | BUSINESS_PRIORITIES 
      | ARCHITECTURE_CONSTRAINTS 
      | OTHER 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OUT_OF_SCOPE -> "OUT_OF_SCOPE"
      | BUSINESS_PRIORITIES -> "BUSINESS_PRIORITIES"
      | ARCHITECTURE_CONSTRAINTS -> "ARCHITECTURE_CONSTRAINTS"
      | OTHER -> "OTHER"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "OUT_OF_SCOPE" -> OUT_OF_SCOPE
      | "BUSINESS_PRIORITIES" -> BUSINESS_PRIORITIES
      | "ARCHITECTURE_CONSTRAINTS" -> ARCHITECTURE_CONSTRAINTS
      | "OTHER" -> OTHER
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AnswerReason" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AnswerReason" j)
    let to_json = simple_to_json to_value
  end
module ChoiceAnswerSummaries =
  struct
    type nonrec t = ChoiceAnswerSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ChoiceAnswerSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChoiceAnswerSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ChoiceAnswerSummaries"
        ~of_json:ChoiceAnswerSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Choices =
  struct
    type nonrec t = Choice.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Choice.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Choice.of_xml)
    let of_json j = list_of_json ~kind:"Choices" ~of_json:Choice.of_json j
    let to_json v = composed_to_json to_value v
  end
module IsApplicable =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module SelectedChoices =
  struct
    type nonrec t = ChoiceId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ChoiceId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChoiceId.of_xml)
    let of_json j =
      list_of_json ~kind:"SelectedChoices" ~of_json:ChoiceId.of_json j
    let to_json v = composed_to_json to_value v
  end
module IsReviewOwnerUpdateAcknowledged =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module WorkloadAccountIds =
  struct
    type nonrec t = AwsAccountId.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:100); i
    let to_value xs =
      (xs |> (List.map ~f:AwsAccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AwsAccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkloadAccountIds" ~of_json:AwsAccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module WorkloadArchitecturalDesign =
  struct
    type nonrec t = string
    let context_ = "WorkloadArchitecturalDesign"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:2048); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadArchitecturalDesign" j
    let to_json = simple_to_json to_value
  end
module WorkloadAwsRegions =
  struct
    type nonrec t = AwsRegion.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:50); i
    let to_value xs =
      (xs |> (List.map ~f:AwsRegion.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AwsRegion.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkloadAwsRegions" ~of_json:AwsRegion.of_json j
    let to_json v = composed_to_json to_value v
  end
module WorkloadDescription =
  struct
    type nonrec t = string
    let context_ = "WorkloadDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:250) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadDescription" j
    let to_json = simple_to_json to_value
  end
module WorkloadEnvironment =
  struct
    type nonrec t =
      | PRODUCTION 
      | PREPRODUCTION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PRODUCTION -> "PRODUCTION"
      | PREPRODUCTION -> "PREPRODUCTION"
      | Non_static_id s -> s
    let of_string =
      function
      | "PRODUCTION" -> PRODUCTION
      | "PREPRODUCTION" -> PREPRODUCTION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration WorkloadEnvironment" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"WorkloadEnvironment" j)
    let to_json = simple_to_json to_value
  end
module WorkloadIndustry =
  struct
    type nonrec t = string
    let context_ = "WorkloadIndustry"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:100); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadIndustry" j
    let to_json = simple_to_json to_value
  end
module WorkloadIndustryType =
  struct
    type nonrec t = string
    let context_ = "WorkloadIndustryType"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:100); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadIndustryType" j
    let to_json = simple_to_json to_value
  end
module WorkloadNonAwsRegions =
  struct
    type nonrec t = WorkloadNonAwsRegion.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:5); i
    let to_value xs =
      (xs |> (List.map ~f:WorkloadNonAwsRegion.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WorkloadNonAwsRegion.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkloadNonAwsRegions"
        ~of_json:WorkloadNonAwsRegion.of_json j
    let to_json v = composed_to_json to_value v
  end
module WorkloadPillarPriorities =
  struct
    type nonrec t = PillarId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PillarId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PillarId.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkloadPillarPriorities" ~of_json:PillarId.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module WorkloadReviewOwner =
  struct
    type nonrec t = string
    let context_ = "WorkloadReviewOwner"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadReviewOwner" j
    let to_json = simple_to_json to_value
  end
module PillarDifference =
  struct
    type nonrec t =
      {
      pillarId: PillarId.t option ;
      pillarName: PillarName.t option ;
      differenceStatus: DifferenceStatus.t option ;
      questionDifferences: QuestionDifferences.t option }
    let make ?pillarId =
      fun ?pillarName ->
        fun ?differenceStatus ->
          fun ?questionDifferences ->
            fun () ->
              { pillarId; pillarName; differenceStatus; questionDifferences }
    let to_value x =
      structure_to_value
        [("PillarId", (Option.map x.pillarId ~f:PillarId.to_value));
        ("PillarName", (Option.map x.pillarName ~f:PillarName.to_value));
        ("DifferenceStatus",
          (Option.map x.differenceStatus ~f:DifferenceStatus.to_value));
        ("QuestionDifferences",
          (Option.map x.questionDifferences ~f:QuestionDifferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let questionDifferences =
        (Option.map ~f:QuestionDifferences.of_xml)
          (Xml.child xml_arg0 "QuestionDifferences") in
      let differenceStatus =
        (Option.map ~f:DifferenceStatus.of_xml)
          (Xml.child xml_arg0 "DifferenceStatus") in
      let pillarName =
        (Option.map ~f:PillarName.of_xml) (Xml.child xml_arg0 "PillarName") in
      let pillarId =
        (Option.map ~f:PillarId.of_xml) (Xml.child xml_arg0 "PillarId") in
      make ?questionDifferences ?differenceStatus ?pillarName ?pillarId ()
    let of_json json =
      let questionDifferences =
        field_map json "QuestionDifferences" QuestionDifferences.of_json in
      let differenceStatus =
        field_map json "DifferenceStatus" DifferenceStatus.of_json in
      let pillarName = field_map json "PillarName" PillarName.of_json in
      let pillarId = field_map json "PillarId" PillarId.of_json in
      make ?questionDifferences ?differenceStatus ?pillarName ?pillarId ()
    let to_json v = composed_to_json to_value v
  end
module ExceptionResourceId =
  struct
    type nonrec t = string
    let context_ = "ExceptionResourceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionResourceId" j
    let to_json = simple_to_json to_value
  end
module ExceptionResourceType =
  struct
    type nonrec t = string
    let context_ = "ExceptionResourceType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionResourceType" j
    let to_json = simple_to_json to_value
  end
module QuotaCode =
  struct
    type nonrec t = string
    let context_ = "QuotaCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QuotaCode" j
    let to_json = simple_to_json to_value
  end
module ServiceCode =
  struct
    type nonrec t = string
    let context_ = "ServiceCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceCode" j
    let to_json = simple_to_json to_value
  end
module ValidationExceptionFieldList =
  struct
    type nonrec t = ValidationExceptionField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ValidationExceptionField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationExceptionField.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationExceptionFieldList"
        ~of_json:ValidationExceptionField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | UNKNOWN_OPERATION 
      | CANNOT_PARSE 
      | FIELD_VALIDATION_FAILED 
      | OTHER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UNKNOWN_OPERATION -> "UNKNOWN_OPERATION"
      | CANNOT_PARSE -> "CANNOT_PARSE"
      | FIELD_VALIDATION_FAILED -> "FIELD_VALIDATION_FAILED"
      | OTHER -> "OTHER"
      | Non_static_id s -> s
    let of_string =
      function
      | "UNKNOWN_OPERATION" -> UNKNOWN_OPERATION
      | "CANNOT_PARSE" -> CANNOT_PARSE
      | "FIELD_VALIDATION_FAILED" -> FIELD_VALIDATION_FAILED
      | "OTHER" -> OTHER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module PillarReviewSummaries =
  struct
    type nonrec t = PillarReviewSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PillarReviewSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PillarReviewSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"PillarReviewSummaries"
        ~of_json:PillarReviewSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChoiceAnswers =
  struct
    type nonrec t = ChoiceAnswer.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ChoiceAnswer.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChoiceAnswer.of_xml)
    let of_json j =
      list_of_json ~kind:"ChoiceAnswers" ~of_json:ChoiceAnswer.of_json j
    let to_json v = composed_to_json to_value v
  end
module HelpfulResourceUrl =
  struct
    type nonrec t = string
    let context_ = "HelpfulResourceUrl"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HelpfulResourceUrl" j
    let to_json = simple_to_json to_value
  end
module QuestionDescription =
  struct
    type nonrec t = string
    let context_ = "QuestionDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QuestionDescription" j
    let to_json = simple_to_json to_value
  end
module ChoiceUpdate =
  struct
    type nonrec t =
      {
      status: ChoiceStatus.t ;
      reason: ChoiceReason.t option ;
      notes: ChoiceNotes.t option }
    let context_ = "ChoiceUpdate"
    let make ?reason =
      fun ?notes -> fun ~status -> fun () -> { reason; notes; status }
    let to_value x =
      structure_to_value
        [("Status", (Some (ChoiceStatus.to_value x.status)));
        ("Reason", (Option.map x.reason ~f:ChoiceReason.to_value));
        ("Notes", (Option.map x.notes ~f:ChoiceNotes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notes =
        (Option.map ~f:ChoiceNotes.of_xml) (Xml.child xml_arg0 "Notes") in
      let reason =
        (Option.map ~f:ChoiceReason.of_xml) (Xml.child xml_arg0 "Reason") in
      let status =
        ChoiceStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      make ?notes ?reason ~status ()
    let of_json json =
      let notes = field_map json "Notes" ChoiceNotes.of_json in
      let reason = field_map json "Reason" ChoiceReason.of_json in
      let status = field_map_exn json "Status" ChoiceStatus.of_json in
      make ?notes ?reason ~status ()
    let to_json v = composed_to_json to_value v
  end
module WorkloadShareSummary =
  struct
    type nonrec t =
      {
      shareId: ShareId.t option ;
      sharedWith: SharedWith.t option ;
      permissionType: PermissionType.t option ;
      status: ShareStatus.t option }
    let make ?shareId =
      fun ?sharedWith ->
        fun ?permissionType ->
          fun ?status ->
            fun () -> { shareId; sharedWith; permissionType; status }
    let to_value x =
      structure_to_value
        [("ShareId", (Option.map x.shareId ~f:ShareId.to_value));
        ("SharedWith", (Option.map x.sharedWith ~f:SharedWith.to_value));
        ("PermissionType",
          (Option.map x.permissionType ~f:PermissionType.to_value));
        ("Status", (Option.map x.status ~f:ShareStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ShareStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let permissionType =
        (Option.map ~f:PermissionType.of_xml)
          (Xml.child xml_arg0 "PermissionType") in
      let sharedWith =
        (Option.map ~f:SharedWith.of_xml) (Xml.child xml_arg0 "SharedWith") in
      let shareId =
        (Option.map ~f:ShareId.of_xml) (Xml.child xml_arg0 "ShareId") in
      make ?status ?permissionType ?sharedWith ?shareId ()
    let of_json json =
      let status = field_map json "Status" ShareStatus.of_json in
      let permissionType =
        field_map json "PermissionType" PermissionType.of_json in
      let sharedWith = field_map json "SharedWith" SharedWith.of_json in
      let shareId = field_map json "ShareId" ShareId.of_json in
      make ?status ?permissionType ?sharedWith ?shareId ()
    let to_json v = composed_to_json to_value v
  end
module ShareInvitationSummary =
  struct
    type nonrec t =
      {
      shareInvitationId: ShareInvitationId.t option ;
      sharedBy: AwsAccountId.t option ;
      sharedWith: SharedWith.t option ;
      permissionType: PermissionType.t option ;
      shareResourceType: ShareResourceType.t option ;
      workloadName: WorkloadName.t option ;
      workloadId: WorkloadId.t option ;
      lensName: LensName.t option ;
      lensArn: LensArn.t option }
    let make ?shareInvitationId =
      fun ?sharedBy ->
        fun ?sharedWith ->
          fun ?permissionType ->
            fun ?shareResourceType ->
              fun ?workloadName ->
                fun ?workloadId ->
                  fun ?lensName ->
                    fun ?lensArn ->
                      fun () ->
                        {
                          shareInvitationId;
                          sharedBy;
                          sharedWith;
                          permissionType;
                          shareResourceType;
                          workloadName;
                          workloadId;
                          lensName;
                          lensArn
                        }
    let to_value x =
      structure_to_value
        [("ShareInvitationId",
           (Option.map x.shareInvitationId ~f:ShareInvitationId.to_value));
        ("SharedBy", (Option.map x.sharedBy ~f:AwsAccountId.to_value));
        ("SharedWith", (Option.map x.sharedWith ~f:SharedWith.to_value));
        ("PermissionType",
          (Option.map x.permissionType ~f:PermissionType.to_value));
        ("ShareResourceType",
          (Option.map x.shareResourceType ~f:ShareResourceType.to_value));
        ("WorkloadName",
          (Option.map x.workloadName ~f:WorkloadName.to_value));
        ("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("LensName", (Option.map x.lensName ~f:LensName.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensName =
        (Option.map ~f:LensName.of_xml) (Xml.child xml_arg0 "LensName") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      let workloadName =
        (Option.map ~f:WorkloadName.of_xml)
          (Xml.child xml_arg0 "WorkloadName") in
      let shareResourceType =
        (Option.map ~f:ShareResourceType.of_xml)
          (Xml.child xml_arg0 "ShareResourceType") in
      let permissionType =
        (Option.map ~f:PermissionType.of_xml)
          (Xml.child xml_arg0 "PermissionType") in
      let sharedWith =
        (Option.map ~f:SharedWith.of_xml) (Xml.child xml_arg0 "SharedWith") in
      let sharedBy =
        (Option.map ~f:AwsAccountId.of_xml) (Xml.child xml_arg0 "SharedBy") in
      let shareInvitationId =
        (Option.map ~f:ShareInvitationId.of_xml)
          (Xml.child xml_arg0 "ShareInvitationId") in
      make ?lensArn ?lensName ?workloadId ?workloadName ?shareResourceType
        ?permissionType ?sharedWith ?sharedBy ?shareInvitationId ()
    let of_json json =
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensName = field_map json "LensName" LensName.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      let workloadName = field_map json "WorkloadName" WorkloadName.of_json in
      let shareResourceType =
        field_map json "ShareResourceType" ShareResourceType.of_json in
      let permissionType =
        field_map json "PermissionType" PermissionType.of_json in
      let sharedWith = field_map json "SharedWith" SharedWith.of_json in
      let sharedBy = field_map json "SharedBy" AwsAccountId.of_json in
      let shareInvitationId =
        field_map json "ShareInvitationId" ShareInvitationId.of_json in
      make ?lensArn ?lensName ?workloadId ?workloadName ?shareResourceType
        ?permissionType ?sharedWith ?sharedBy ?shareInvitationId ()
    let to_json v = composed_to_json to_value v
  end
module NotificationSummary =
  struct
    type nonrec t =
      {
      type_: NotificationType.t option ;
      lensUpgradeSummary: LensUpgradeSummary.t option }
    let make ?type_ =
      fun ?lensUpgradeSummary -> fun () -> { type_; lensUpgradeSummary }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:NotificationType.to_value));
        ("LensUpgradeSummary",
          (Option.map x.lensUpgradeSummary ~f:LensUpgradeSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensUpgradeSummary =
        (Option.map ~f:LensUpgradeSummary.of_xml)
          (Xml.child xml_arg0 "LensUpgradeSummary") in
      let type_ =
        (Option.map ~f:NotificationType.of_xml) (Xml.child xml_arg0 "Type") in
      make ?lensUpgradeSummary ?type_ ()
    let of_json json =
      let lensUpgradeSummary =
        field_map json "LensUpgradeSummary" LensUpgradeSummary.of_json in
      let type_ = field_map json "Type" NotificationType.of_json in
      make ?lensUpgradeSummary ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module MilestoneSummary =
  struct
    type nonrec t =
      {
      milestoneNumber: MilestoneNumber.t option ;
      milestoneName: MilestoneName.t option ;
      recordedAt: Timestamp.t option ;
      workloadSummary: WorkloadSummary.t option }
    let make ?milestoneNumber =
      fun ?milestoneName ->
        fun ?recordedAt ->
          fun ?workloadSummary ->
            fun () ->
              { milestoneNumber; milestoneName; recordedAt; workloadSummary }
    let to_value x =
      structure_to_value
        [("MilestoneNumber",
           (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("MilestoneName",
          (Option.map x.milestoneName ~f:MilestoneName.to_value));
        ("RecordedAt", (Option.map x.recordedAt ~f:Timestamp.to_value));
        ("WorkloadSummary",
          (Option.map x.workloadSummary ~f:WorkloadSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workloadSummary =
        (Option.map ~f:WorkloadSummary.of_xml)
          (Xml.child xml_arg0 "WorkloadSummary") in
      let recordedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "RecordedAt") in
      let milestoneName =
        (Option.map ~f:MilestoneName.of_xml)
          (Xml.child xml_arg0 "MilestoneName") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      make ?workloadSummary ?recordedAt ?milestoneName ?milestoneNumber ()
    let of_json json =
      let workloadSummary =
        field_map json "WorkloadSummary" WorkloadSummary.of_json in
      let recordedAt = field_map json "RecordedAt" Timestamp.of_json in
      let milestoneName =
        field_map json "MilestoneName" MilestoneName.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      make ?workloadSummary ?recordedAt ?milestoneName ?milestoneNumber ()
    let to_json v = composed_to_json to_value v
  end
module LensSummary =
  struct
    type nonrec t =
      {
      lensArn: LensArn.t option ;
      lensAlias: LensAlias.t option ;
      lensName: LensName.t option ;
      lensType: LensType.t option ;
      description: LensDescription.t option ;
      createdAt: Timestamp.t option ;
      updatedAt: Timestamp.t option ;
      lensVersion: LensVersion.t option ;
      owner: AwsAccountId.t option ;
      lensStatus: LensStatus.t option }
    let make ?lensArn =
      fun ?lensAlias ->
        fun ?lensName ->
          fun ?lensType ->
            fun ?description ->
              fun ?createdAt ->
                fun ?updatedAt ->
                  fun ?lensVersion ->
                    fun ?owner ->
                      fun ?lensStatus ->
                        fun () ->
                          {
                            lensArn;
                            lensAlias;
                            lensName;
                            lensType;
                            description;
                            createdAt;
                            updatedAt;
                            lensVersion;
                            owner;
                            lensStatus
                          }
    let to_value x =
      structure_to_value
        [("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensName", (Option.map x.lensName ~f:LensName.to_value));
        ("LensType", (Option.map x.lensType ~f:LensType.to_value));
        ("Description",
          (Option.map x.description ~f:LensDescription.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("LensVersion", (Option.map x.lensVersion ~f:LensVersion.to_value));
        ("Owner", (Option.map x.owner ~f:AwsAccountId.to_value));
        ("LensStatus", (Option.map x.lensStatus ~f:LensStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensStatus =
        (Option.map ~f:LensStatus.of_xml) (Xml.child xml_arg0 "LensStatus") in
      let owner =
        (Option.map ~f:AwsAccountId.of_xml) (Xml.child xml_arg0 "Owner") in
      let lensVersion =
        (Option.map ~f:LensVersion.of_xml) (Xml.child xml_arg0 "LensVersion") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let description =
        (Option.map ~f:LensDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let lensType =
        (Option.map ~f:LensType.of_xml) (Xml.child xml_arg0 "LensType") in
      let lensName =
        (Option.map ~f:LensName.of_xml) (Xml.child xml_arg0 "LensName") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      make ?lensStatus ?owner ?lensVersion ?updatedAt ?createdAt ?description
        ?lensType ?lensName ?lensAlias ?lensArn ()
    let of_json json =
      let lensStatus = field_map json "LensStatus" LensStatus.of_json in
      let owner = field_map json "Owner" AwsAccountId.of_json in
      let lensVersion = field_map json "LensVersion" LensVersion.of_json in
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      let description = field_map json "Description" LensDescription.of_json in
      let lensType = field_map json "LensType" LensType.of_json in
      let lensName = field_map json "LensName" LensName.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      make ?lensStatus ?owner ?lensVersion ?updatedAt ?createdAt ?description
        ?lensType ?lensName ?lensAlias ?lensArn ()
    let to_json v = composed_to_json to_value v
  end
module LensShareSummary =
  struct
    type nonrec t =
      {
      shareId: ShareId.t option ;
      sharedWith: SharedWith.t option ;
      status: ShareStatus.t option }
    let make ?shareId =
      fun ?sharedWith ->
        fun ?status -> fun () -> { shareId; sharedWith; status }
    let to_value x =
      structure_to_value
        [("ShareId", (Option.map x.shareId ~f:ShareId.to_value));
        ("SharedWith", (Option.map x.sharedWith ~f:SharedWith.to_value));
        ("Status", (Option.map x.status ~f:ShareStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ShareStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let sharedWith =
        (Option.map ~f:SharedWith.of_xml) (Xml.child xml_arg0 "SharedWith") in
      let shareId =
        (Option.map ~f:ShareId.of_xml) (Xml.child xml_arg0 "ShareId") in
      make ?status ?sharedWith ?shareId ()
    let of_json json =
      let status = field_map json "Status" ShareStatus.of_json in
      let sharedWith = field_map json "SharedWith" SharedWith.of_json in
      let shareId = field_map json "ShareId" ShareId.of_json in
      make ?status ?sharedWith ?shareId ()
    let to_json v = composed_to_json to_value v
  end
module LensReviewSummary =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      lensVersion: LensVersion.t option ;
      lensName: LensName.t option ;
      lensStatus: LensStatus.t option ;
      updatedAt: Timestamp.t option ;
      riskCounts: RiskCounts.t option }
    let make ?lensAlias =
      fun ?lensArn ->
        fun ?lensVersion ->
          fun ?lensName ->
            fun ?lensStatus ->
              fun ?updatedAt ->
                fun ?riskCounts ->
                  fun () ->
                    {
                      lensAlias;
                      lensArn;
                      lensVersion;
                      lensName;
                      lensStatus;
                      updatedAt;
                      riskCounts
                    }
    let to_value x =
      structure_to_value
        [("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("LensVersion", (Option.map x.lensVersion ~f:LensVersion.to_value));
        ("LensName", (Option.map x.lensName ~f:LensName.to_value));
        ("LensStatus", (Option.map x.lensStatus ~f:LensStatus.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("RiskCounts", (Option.map x.riskCounts ~f:RiskCounts.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let riskCounts =
        (Option.map ~f:RiskCounts.of_xml) (Xml.child xml_arg0 "RiskCounts") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let lensStatus =
        (Option.map ~f:LensStatus.of_xml) (Xml.child xml_arg0 "LensStatus") in
      let lensName =
        (Option.map ~f:LensName.of_xml) (Xml.child xml_arg0 "LensName") in
      let lensVersion =
        (Option.map ~f:LensVersion.of_xml) (Xml.child xml_arg0 "LensVersion") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      make ?riskCounts ?updatedAt ?lensStatus ?lensName ?lensVersion ?lensArn
        ?lensAlias ()
    let of_json json =
      let riskCounts = field_map json "RiskCounts" RiskCounts.of_json in
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let lensStatus = field_map json "LensStatus" LensStatus.of_json in
      let lensName = field_map json "LensName" LensName.of_json in
      let lensVersion = field_map json "LensVersion" LensVersion.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      make ?riskCounts ?updatedAt ?lensStatus ?lensName ?lensVersion ?lensArn
        ?lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module ImprovementSummary =
  struct
    type nonrec t =
      {
      questionId: QuestionId.t option ;
      pillarId: PillarId.t option ;
      questionTitle: QuestionTitle.t option ;
      risk: Risk.t option ;
      improvementPlanUrl: ImprovementPlanUrl.t option ;
      improvementPlans: ChoiceImprovementPlans.t option }
    let make ?questionId =
      fun ?pillarId ->
        fun ?questionTitle ->
          fun ?risk ->
            fun ?improvementPlanUrl ->
              fun ?improvementPlans ->
                fun () ->
                  {
                    questionId;
                    pillarId;
                    questionTitle;
                    risk;
                    improvementPlanUrl;
                    improvementPlans
                  }
    let to_value x =
      structure_to_value
        [("QuestionId", (Option.map x.questionId ~f:QuestionId.to_value));
        ("PillarId", (Option.map x.pillarId ~f:PillarId.to_value));
        ("QuestionTitle",
          (Option.map x.questionTitle ~f:QuestionTitle.to_value));
        ("Risk", (Option.map x.risk ~f:Risk.to_value));
        ("ImprovementPlanUrl",
          (Option.map x.improvementPlanUrl ~f:ImprovementPlanUrl.to_value));
        ("ImprovementPlans",
          (Option.map x.improvementPlans ~f:ChoiceImprovementPlans.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let improvementPlans =
        (Option.map ~f:ChoiceImprovementPlans.of_xml)
          (Xml.child xml_arg0 "ImprovementPlans") in
      let improvementPlanUrl =
        (Option.map ~f:ImprovementPlanUrl.of_xml)
          (Xml.child xml_arg0 "ImprovementPlanUrl") in
      let risk = (Option.map ~f:Risk.of_xml) (Xml.child xml_arg0 "Risk") in
      let questionTitle =
        (Option.map ~f:QuestionTitle.of_xml)
          (Xml.child xml_arg0 "QuestionTitle") in
      let pillarId =
        (Option.map ~f:PillarId.of_xml) (Xml.child xml_arg0 "PillarId") in
      let questionId =
        (Option.map ~f:QuestionId.of_xml) (Xml.child xml_arg0 "QuestionId") in
      make ?improvementPlans ?improvementPlanUrl ?risk ?questionTitle
        ?pillarId ?questionId ()
    let of_json json =
      let improvementPlans =
        field_map json "ImprovementPlans" ChoiceImprovementPlans.of_json in
      let improvementPlanUrl =
        field_map json "ImprovementPlanUrl" ImprovementPlanUrl.of_json in
      let risk = field_map json "Risk" Risk.of_json in
      let questionTitle =
        field_map json "QuestionTitle" QuestionTitle.of_json in
      let pillarId = field_map json "PillarId" PillarId.of_json in
      let questionId = field_map json "QuestionId" QuestionId.of_json in
      make ?improvementPlans ?improvementPlanUrl ?risk ?questionTitle
        ?pillarId ?questionId ()
    let to_json v = composed_to_json to_value v
  end
module AnswerSummary =
  struct
    type nonrec t =
      {
      questionId: QuestionId.t option ;
      pillarId: PillarId.t option ;
      questionTitle: QuestionTitle.t option ;
      choices: Choices.t option ;
      selectedChoices: SelectedChoices.t option ;
      choiceAnswerSummaries: ChoiceAnswerSummaries.t option ;
      isApplicable: IsApplicable.t option ;
      risk: Risk.t option ;
      reason: AnswerReason.t option }
    let make ?questionId =
      fun ?pillarId ->
        fun ?questionTitle ->
          fun ?choices ->
            fun ?selectedChoices ->
              fun ?choiceAnswerSummaries ->
                fun ?isApplicable ->
                  fun ?risk ->
                    fun ?reason ->
                      fun () ->
                        {
                          questionId;
                          pillarId;
                          questionTitle;
                          choices;
                          selectedChoices;
                          choiceAnswerSummaries;
                          isApplicable;
                          risk;
                          reason
                        }
    let to_value x =
      structure_to_value
        [("QuestionId", (Option.map x.questionId ~f:QuestionId.to_value));
        ("PillarId", (Option.map x.pillarId ~f:PillarId.to_value));
        ("QuestionTitle",
          (Option.map x.questionTitle ~f:QuestionTitle.to_value));
        ("Choices", (Option.map x.choices ~f:Choices.to_value));
        ("SelectedChoices",
          (Option.map x.selectedChoices ~f:SelectedChoices.to_value));
        ("ChoiceAnswerSummaries",
          (Option.map x.choiceAnswerSummaries
             ~f:ChoiceAnswerSummaries.to_value));
        ("IsApplicable",
          (Option.map x.isApplicable ~f:IsApplicable.to_value));
        ("Risk", (Option.map x.risk ~f:Risk.to_value));
        ("Reason", (Option.map x.reason ~f:AnswerReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:AnswerReason.of_xml) (Xml.child xml_arg0 "Reason") in
      let risk = (Option.map ~f:Risk.of_xml) (Xml.child xml_arg0 "Risk") in
      let isApplicable =
        (Option.map ~f:IsApplicable.of_xml)
          (Xml.child xml_arg0 "IsApplicable") in
      let choiceAnswerSummaries =
        (Option.map ~f:ChoiceAnswerSummaries.of_xml)
          (Xml.child xml_arg0 "ChoiceAnswerSummaries") in
      let selectedChoices =
        (Option.map ~f:SelectedChoices.of_xml)
          (Xml.child xml_arg0 "SelectedChoices") in
      let choices =
        (Option.map ~f:Choices.of_xml) (Xml.child xml_arg0 "Choices") in
      let questionTitle =
        (Option.map ~f:QuestionTitle.of_xml)
          (Xml.child xml_arg0 "QuestionTitle") in
      let pillarId =
        (Option.map ~f:PillarId.of_xml) (Xml.child xml_arg0 "PillarId") in
      let questionId =
        (Option.map ~f:QuestionId.of_xml) (Xml.child xml_arg0 "QuestionId") in
      make ?reason ?risk ?isApplicable ?choiceAnswerSummaries
        ?selectedChoices ?choices ?questionTitle ?pillarId ?questionId ()
    let of_json json =
      let reason = field_map json "Reason" AnswerReason.of_json in
      let risk = field_map json "Risk" Risk.of_json in
      let isApplicable = field_map json "IsApplicable" IsApplicable.of_json in
      let choiceAnswerSummaries =
        field_map json "ChoiceAnswerSummaries" ChoiceAnswerSummaries.of_json in
      let selectedChoices =
        field_map json "SelectedChoices" SelectedChoices.of_json in
      let choices = field_map json "Choices" Choices.of_json in
      let questionTitle =
        field_map json "QuestionTitle" QuestionTitle.of_json in
      let pillarId = field_map json "PillarId" PillarId.of_json in
      let questionId = field_map json "QuestionId" QuestionId.of_json in
      make ?reason ?risk ?isApplicable ?choiceAnswerSummaries
        ?selectedChoices ?choices ?questionTitle ?pillarId ?questionId ()
    let to_json v = composed_to_json to_value v
  end
module Workload =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      workloadArn: WorkloadArn.t option ;
      workloadName: WorkloadName.t option ;
      description: WorkloadDescription.t option ;
      environment: WorkloadEnvironment.t option ;
      updatedAt: Timestamp.t option ;
      accountIds: WorkloadAccountIds.t option ;
      awsRegions: WorkloadAwsRegions.t option ;
      nonAwsRegions: WorkloadNonAwsRegions.t option ;
      architecturalDesign: WorkloadArchitecturalDesign.t option ;
      reviewOwner: WorkloadReviewOwner.t option ;
      reviewRestrictionDate: Timestamp.t option ;
      isReviewOwnerUpdateAcknowledged:
        IsReviewOwnerUpdateAcknowledged.t option ;
      industryType: WorkloadIndustryType.t option ;
      industry: WorkloadIndustry.t option ;
      notes: Notes.t option ;
      improvementStatus: WorkloadImprovementStatus.t option ;
      riskCounts: RiskCounts.t option ;
      pillarPriorities: WorkloadPillarPriorities.t option ;
      lenses: WorkloadLenses.t option ;
      owner: AwsAccountId.t option ;
      shareInvitationId: ShareInvitationId.t option ;
      tags: TagMap.t option }
    let make ?workloadId =
      fun ?workloadArn ->
        fun ?workloadName ->
          fun ?description ->
            fun ?environment ->
              fun ?updatedAt ->
                fun ?accountIds ->
                  fun ?awsRegions ->
                    fun ?nonAwsRegions ->
                      fun ?architecturalDesign ->
                        fun ?reviewOwner ->
                          fun ?reviewRestrictionDate ->
                            fun ?isReviewOwnerUpdateAcknowledged ->
                              fun ?industryType ->
                                fun ?industry ->
                                  fun ?notes ->
                                    fun ?improvementStatus ->
                                      fun ?riskCounts ->
                                        fun ?pillarPriorities ->
                                          fun ?lenses ->
                                            fun ?owner ->
                                              fun ?shareInvitationId ->
                                                fun ?tags ->
                                                  fun () ->
                                                    {
                                                      workloadId;
                                                      workloadArn;
                                                      workloadName;
                                                      description;
                                                      environment;
                                                      updatedAt;
                                                      accountIds;
                                                      awsRegions;
                                                      nonAwsRegions;
                                                      architecturalDesign;
                                                      reviewOwner;
                                                      reviewRestrictionDate;
                                                      isReviewOwnerUpdateAcknowledged;
                                                      industryType;
                                                      industry;
                                                      notes;
                                                      improvementStatus;
                                                      riskCounts;
                                                      pillarPriorities;
                                                      lenses;
                                                      owner;
                                                      shareInvitationId;
                                                      tags
                                                    }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("WorkloadArn", (Option.map x.workloadArn ~f:WorkloadArn.to_value));
        ("WorkloadName",
          (Option.map x.workloadName ~f:WorkloadName.to_value));
        ("Description",
          (Option.map x.description ~f:WorkloadDescription.to_value));
        ("Environment",
          (Option.map x.environment ~f:WorkloadEnvironment.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("AccountIds",
          (Option.map x.accountIds ~f:WorkloadAccountIds.to_value));
        ("AwsRegions",
          (Option.map x.awsRegions ~f:WorkloadAwsRegions.to_value));
        ("NonAwsRegions",
          (Option.map x.nonAwsRegions ~f:WorkloadNonAwsRegions.to_value));
        ("ArchitecturalDesign",
          (Option.map x.architecturalDesign
             ~f:WorkloadArchitecturalDesign.to_value));
        ("ReviewOwner",
          (Option.map x.reviewOwner ~f:WorkloadReviewOwner.to_value));
        ("ReviewRestrictionDate",
          (Option.map x.reviewRestrictionDate ~f:Timestamp.to_value));
        ("IsReviewOwnerUpdateAcknowledged",
          (Option.map x.isReviewOwnerUpdateAcknowledged
             ~f:IsReviewOwnerUpdateAcknowledged.to_value));
        ("IndustryType",
          (Option.map x.industryType ~f:WorkloadIndustryType.to_value));
        ("Industry", (Option.map x.industry ~f:WorkloadIndustry.to_value));
        ("Notes", (Option.map x.notes ~f:Notes.to_value));
        ("ImprovementStatus",
          (Option.map x.improvementStatus
             ~f:WorkloadImprovementStatus.to_value));
        ("RiskCounts", (Option.map x.riskCounts ~f:RiskCounts.to_value));
        ("PillarPriorities",
          (Option.map x.pillarPriorities ~f:WorkloadPillarPriorities.to_value));
        ("Lenses", (Option.map x.lenses ~f:WorkloadLenses.to_value));
        ("Owner", (Option.map x.owner ~f:AwsAccountId.to_value));
        ("ShareInvitationId",
          (Option.map x.shareInvitationId ~f:ShareInvitationId.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let shareInvitationId =
        (Option.map ~f:ShareInvitationId.of_xml)
          (Xml.child xml_arg0 "ShareInvitationId") in
      let owner =
        (Option.map ~f:AwsAccountId.of_xml) (Xml.child xml_arg0 "Owner") in
      let lenses =
        (Option.map ~f:WorkloadLenses.of_xml) (Xml.child xml_arg0 "Lenses") in
      let pillarPriorities =
        (Option.map ~f:WorkloadPillarPriorities.of_xml)
          (Xml.child xml_arg0 "PillarPriorities") in
      let riskCounts =
        (Option.map ~f:RiskCounts.of_xml) (Xml.child xml_arg0 "RiskCounts") in
      let improvementStatus =
        (Option.map ~f:WorkloadImprovementStatus.of_xml)
          (Xml.child xml_arg0 "ImprovementStatus") in
      let notes = (Option.map ~f:Notes.of_xml) (Xml.child xml_arg0 "Notes") in
      let industry =
        (Option.map ~f:WorkloadIndustry.of_xml)
          (Xml.child xml_arg0 "Industry") in
      let industryType =
        (Option.map ~f:WorkloadIndustryType.of_xml)
          (Xml.child xml_arg0 "IndustryType") in
      let isReviewOwnerUpdateAcknowledged =
        (Option.map ~f:IsReviewOwnerUpdateAcknowledged.of_xml)
          (Xml.child xml_arg0 "IsReviewOwnerUpdateAcknowledged") in
      let reviewRestrictionDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "ReviewRestrictionDate") in
      let reviewOwner =
        (Option.map ~f:WorkloadReviewOwner.of_xml)
          (Xml.child xml_arg0 "ReviewOwner") in
      let architecturalDesign =
        (Option.map ~f:WorkloadArchitecturalDesign.of_xml)
          (Xml.child xml_arg0 "ArchitecturalDesign") in
      let nonAwsRegions =
        (Option.map ~f:WorkloadNonAwsRegions.of_xml)
          (Xml.child xml_arg0 "NonAwsRegions") in
      let awsRegions =
        (Option.map ~f:WorkloadAwsRegions.of_xml)
          (Xml.child xml_arg0 "AwsRegions") in
      let accountIds =
        (Option.map ~f:WorkloadAccountIds.of_xml)
          (Xml.child xml_arg0 "AccountIds") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let environment =
        (Option.map ~f:WorkloadEnvironment.of_xml)
          (Xml.child xml_arg0 "Environment") in
      let description =
        (Option.map ~f:WorkloadDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let workloadName =
        (Option.map ~f:WorkloadName.of_xml)
          (Xml.child xml_arg0 "WorkloadName") in
      let workloadArn =
        (Option.map ~f:WorkloadArn.of_xml) (Xml.child xml_arg0 "WorkloadArn") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?tags ?shareInvitationId ?owner ?lenses ?pillarPriorities
        ?riskCounts ?improvementStatus ?notes ?industry ?industryType
        ?isReviewOwnerUpdateAcknowledged ?reviewRestrictionDate ?reviewOwner
        ?architecturalDesign ?nonAwsRegions ?awsRegions ?accountIds
        ?updatedAt ?environment ?description ?workloadName ?workloadArn
        ?workloadId ()
    let of_json json =
      let tags = field_map json "Tags" TagMap.of_json in
      let shareInvitationId =
        field_map json "ShareInvitationId" ShareInvitationId.of_json in
      let owner = field_map json "Owner" AwsAccountId.of_json in
      let lenses = field_map json "Lenses" WorkloadLenses.of_json in
      let pillarPriorities =
        field_map json "PillarPriorities" WorkloadPillarPriorities.of_json in
      let riskCounts = field_map json "RiskCounts" RiskCounts.of_json in
      let improvementStatus =
        field_map json "ImprovementStatus" WorkloadImprovementStatus.of_json in
      let notes = field_map json "Notes" Notes.of_json in
      let industry = field_map json "Industry" WorkloadIndustry.of_json in
      let industryType =
        field_map json "IndustryType" WorkloadIndustryType.of_json in
      let isReviewOwnerUpdateAcknowledged =
        field_map json "IsReviewOwnerUpdateAcknowledged"
          IsReviewOwnerUpdateAcknowledged.of_json in
      let reviewRestrictionDate =
        field_map json "ReviewRestrictionDate" Timestamp.of_json in
      let reviewOwner =
        field_map json "ReviewOwner" WorkloadReviewOwner.of_json in
      let architecturalDesign =
        field_map json "ArchitecturalDesign"
          WorkloadArchitecturalDesign.of_json in
      let nonAwsRegions =
        field_map json "NonAwsRegions" WorkloadNonAwsRegions.of_json in
      let awsRegions = field_map json "AwsRegions" WorkloadAwsRegions.of_json in
      let accountIds = field_map json "AccountIds" WorkloadAccountIds.of_json in
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let environment =
        field_map json "Environment" WorkloadEnvironment.of_json in
      let description =
        field_map json "Description" WorkloadDescription.of_json in
      let workloadName = field_map json "WorkloadName" WorkloadName.of_json in
      let workloadArn = field_map json "WorkloadArn" WorkloadArn.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?tags ?shareInvitationId ?owner ?lenses ?pillarPriorities
        ?riskCounts ?improvementStatus ?notes ?industry ?industryType
        ?isReviewOwnerUpdateAcknowledged ?reviewRestrictionDate ?reviewOwner
        ?architecturalDesign ?nonAwsRegions ?awsRegions ?accountIds
        ?updatedAt ?environment ?description ?workloadName ?workloadArn
        ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module PillarDifferences =
  struct
    type nonrec t = PillarDifference.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PillarDifference.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PillarDifference.of_xml)
    let of_json j =
      list_of_json ~kind:"PillarDifferences"
        ~of_json:PillarDifference.of_json j
    let to_json v = composed_to_json to_value v
  end
module Base64String =
  struct
    type nonrec t = string
    let context_ = "Base64String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Base64String" j
    let to_json = simple_to_json to_value
  end
module LensOwner =
  struct
    type nonrec t = string
    let context_ = "LensOwner"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LensOwner" j
    let to_json = simple_to_json to_value
  end
module ClientRequestToken =
  struct
    type nonrec t = string
    let context_ = "ClientRequestToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientRequestToken" j
    let to_json = simple_to_json to_value
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t }
    let context_ = "AccessDeniedException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Some (ExceptionMessage.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        ExceptionMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "Message" ExceptionMessage.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end
module ConflictException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t ;
      resourceId: ExceptionResourceId.t ;
      resourceType: ExceptionResourceType.t }
    let context_ = "ConflictException"
    let make ~message =
      fun ~resourceId ->
        fun ~resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Some (ExceptionMessage.to_value x.message)));
        ("ResourceId", (Some (ExceptionResourceId.to_value x.resourceId)));
        ("ResourceType",
          (Some (ExceptionResourceType.to_value x.resourceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        ExceptionResourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceType") in
      let resourceId =
        ExceptionResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let message =
        ExceptionMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      make ~resourceType ~resourceId ~message ()
    let of_json json =
      let resourceType =
        field_map_exn json "ResourceType" ExceptionResourceType.of_json in
      let resourceId =
        field_map_exn json "ResourceId" ExceptionResourceId.of_json in
      let message = field_map_exn json "Message" ExceptionMessage.of_json in
      make ~resourceType ~resourceId ~message ()
    let to_json v = composed_to_json to_value v
  end
module InternalServerException =
  struct
    type nonrec t = {
      message: ExceptionMessage.t }
    let context_ = "InternalServerException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Some (ExceptionMessage.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        ExceptionMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "Message" ExceptionMessage.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t ;
      resourceId: ExceptionResourceId.t ;
      resourceType: ExceptionResourceType.t }
    let context_ = "ResourceNotFoundException"
    let make ~message =
      fun ~resourceId ->
        fun ~resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Some (ExceptionMessage.to_value x.message)));
        ("ResourceId", (Some (ExceptionResourceId.to_value x.resourceId)));
        ("ResourceType",
          (Some (ExceptionResourceType.to_value x.resourceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        ExceptionResourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceType") in
      let resourceId =
        ExceptionResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let message =
        ExceptionMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      make ~resourceType ~resourceId ~message ()
    let of_json json =
      let resourceType =
        field_map_exn json "ResourceType" ExceptionResourceType.of_json in
      let resourceId =
        field_map_exn json "ResourceId" ExceptionResourceId.of_json in
      let message = field_map_exn json "Message" ExceptionMessage.of_json in
      make ~resourceType ~resourceId ~message ()
    let to_json v = composed_to_json to_value v
  end
module ThrottlingException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t ;
      quotaCode: QuotaCode.t option ;
      serviceCode: ServiceCode.t option }
    let context_ = "ThrottlingException"
    let make ?quotaCode =
      fun ?serviceCode ->
        fun ~message -> fun () -> { quotaCode; serviceCode; message }
    let to_value x =
      structure_to_value
        [("Message", (Some (ExceptionMessage.to_value x.message)));
        ("QuotaCode", (Option.map x.quotaCode ~f:QuotaCode.to_value));
        ("ServiceCode", (Option.map x.serviceCode ~f:ServiceCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceCode =
        (Option.map ~f:ServiceCode.of_xml) (Xml.child xml_arg0 "ServiceCode") in
      let quotaCode =
        (Option.map ~f:QuotaCode.of_xml) (Xml.child xml_arg0 "QuotaCode") in
      let message =
        ExceptionMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      make ?serviceCode ?quotaCode ~message ()
    let of_json json =
      let serviceCode = field_map json "ServiceCode" ServiceCode.of_json in
      let quotaCode = field_map json "QuotaCode" QuotaCode.of_json in
      let message = field_map_exn json "Message" ExceptionMessage.of_json in
      make ?serviceCode ?quotaCode ~message ()
    let to_json v = composed_to_json to_value v
  end
module ValidationException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t ;
      reason: ValidationExceptionReason.t option ;
      fields: ValidationExceptionFieldList.t option }
    let context_ = "ValidationException"
    let make ?reason =
      fun ?fields -> fun ~message -> fun () -> { reason; fields; message }
    let to_value x =
      structure_to_value
        [("Message", (Some (ExceptionMessage.to_value x.message)));
        ("Reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value));
        ("Fields",
          (Option.map x.fields ~f:ValidationExceptionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fields =
        (Option.map ~f:ValidationExceptionFieldList.of_xml)
          (Xml.child xml_arg0 "Fields") in
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        ExceptionMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      make ?fields ?reason ~message ()
    let of_json json =
      let fields =
        field_map json "Fields" ValidationExceptionFieldList.of_json in
      let reason = field_map json "Reason" ValidationExceptionReason.of_json in
      let message = field_map_exn json "Message" ExceptionMessage.of_json in
      make ?fields ?reason ~message ()
    let to_json v = composed_to_json to_value v
  end
module WorkloadShare =
  struct
    type nonrec t =
      {
      shareId: ShareId.t option ;
      sharedBy: AwsAccountId.t option ;
      sharedWith: SharedWith.t option ;
      permissionType: PermissionType.t option ;
      status: ShareStatus.t option ;
      workloadName: WorkloadName.t option ;
      workloadId: WorkloadId.t option }
    let make ?shareId =
      fun ?sharedBy ->
        fun ?sharedWith ->
          fun ?permissionType ->
            fun ?status ->
              fun ?workloadName ->
                fun ?workloadId ->
                  fun () ->
                    {
                      shareId;
                      sharedBy;
                      sharedWith;
                      permissionType;
                      status;
                      workloadName;
                      workloadId
                    }
    let to_value x =
      structure_to_value
        [("ShareId", (Option.map x.shareId ~f:ShareId.to_value));
        ("SharedBy", (Option.map x.sharedBy ~f:AwsAccountId.to_value));
        ("SharedWith", (Option.map x.sharedWith ~f:SharedWith.to_value));
        ("PermissionType",
          (Option.map x.permissionType ~f:PermissionType.to_value));
        ("Status", (Option.map x.status ~f:ShareStatus.to_value));
        ("WorkloadName",
          (Option.map x.workloadName ~f:WorkloadName.to_value));
        ("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      let workloadName =
        (Option.map ~f:WorkloadName.of_xml)
          (Xml.child xml_arg0 "WorkloadName") in
      let status =
        (Option.map ~f:ShareStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let permissionType =
        (Option.map ~f:PermissionType.of_xml)
          (Xml.child xml_arg0 "PermissionType") in
      let sharedWith =
        (Option.map ~f:SharedWith.of_xml) (Xml.child xml_arg0 "SharedWith") in
      let sharedBy =
        (Option.map ~f:AwsAccountId.of_xml) (Xml.child xml_arg0 "SharedBy") in
      let shareId =
        (Option.map ~f:ShareId.of_xml) (Xml.child xml_arg0 "ShareId") in
      make ?workloadId ?workloadName ?status ?permissionType ?sharedWith
        ?sharedBy ?shareId ()
    let of_json json =
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      let workloadName = field_map json "WorkloadName" WorkloadName.of_json in
      let status = field_map json "Status" ShareStatus.of_json in
      let permissionType =
        field_map json "PermissionType" PermissionType.of_json in
      let sharedWith = field_map json "SharedWith" SharedWith.of_json in
      let sharedBy = field_map json "SharedBy" AwsAccountId.of_json in
      let shareId = field_map json "ShareId" ShareId.of_json in
      make ?workloadId ?workloadName ?status ?permissionType ?sharedWith
        ?sharedBy ?shareId ()
    let to_json v = composed_to_json to_value v
  end
module ShareInvitation =
  struct
    type nonrec t =
      {
      shareInvitationId: ShareInvitationId.t option ;
      shareResourceType: ShareResourceType.t option ;
      workloadId: WorkloadId.t option ;
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option }
    let make ?shareInvitationId =
      fun ?shareResourceType ->
        fun ?workloadId ->
          fun ?lensAlias ->
            fun ?lensArn ->
              fun () ->
                {
                  shareInvitationId;
                  shareResourceType;
                  workloadId;
                  lensAlias;
                  lensArn
                }
    let to_value x =
      structure_to_value
        [("ShareInvitationId",
           (Option.map x.shareInvitationId ~f:ShareInvitationId.to_value));
        ("ShareResourceType",
          (Option.map x.shareResourceType ~f:ShareResourceType.to_value));
        ("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      let shareResourceType =
        (Option.map ~f:ShareResourceType.of_xml)
          (Xml.child xml_arg0 "ShareResourceType") in
      let shareInvitationId =
        (Option.map ~f:ShareInvitationId.of_xml)
          (Xml.child xml_arg0 "ShareInvitationId") in
      make ?lensArn ?lensAlias ?workloadId ?shareResourceType
        ?shareInvitationId ()
    let of_json json =
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      let shareResourceType =
        field_map json "ShareResourceType" ShareResourceType.of_json in
      let shareInvitationId =
        field_map json "ShareInvitationId" ShareInvitationId.of_json in
      make ?lensArn ?lensAlias ?workloadId ?shareResourceType
        ?shareInvitationId ()
    let to_json v = composed_to_json to_value v
  end
module ShareInvitationAction =
  struct
    type nonrec t =
      | ACCEPT 
      | REJECT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACCEPT -> "ACCEPT"
      | REJECT -> "REJECT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACCEPT" -> ACCEPT
      | "REJECT" -> REJECT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ShareInvitationAction" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ShareInvitationAction" j)
    let to_json = simple_to_json to_value
  end
module LensReview =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      lensVersion: LensVersion.t option ;
      lensName: LensName.t option ;
      lensStatus: LensStatus.t option ;
      pillarReviewSummaries: PillarReviewSummaries.t option ;
      updatedAt: Timestamp.t option ;
      notes: Notes.t option ;
      riskCounts: RiskCounts.t option ;
      nextToken: NextToken.t option }
    let make ?lensAlias =
      fun ?lensArn ->
        fun ?lensVersion ->
          fun ?lensName ->
            fun ?lensStatus ->
              fun ?pillarReviewSummaries ->
                fun ?updatedAt ->
                  fun ?notes ->
                    fun ?riskCounts ->
                      fun ?nextToken ->
                        fun () ->
                          {
                            lensAlias;
                            lensArn;
                            lensVersion;
                            lensName;
                            lensStatus;
                            pillarReviewSummaries;
                            updatedAt;
                            notes;
                            riskCounts;
                            nextToken
                          }
    let to_value x =
      structure_to_value
        [("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("LensVersion", (Option.map x.lensVersion ~f:LensVersion.to_value));
        ("LensName", (Option.map x.lensName ~f:LensName.to_value));
        ("LensStatus", (Option.map x.lensStatus ~f:LensStatus.to_value));
        ("PillarReviewSummaries",
          (Option.map x.pillarReviewSummaries
             ~f:PillarReviewSummaries.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("Notes", (Option.map x.notes ~f:Notes.to_value));
        ("RiskCounts", (Option.map x.riskCounts ~f:RiskCounts.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let riskCounts =
        (Option.map ~f:RiskCounts.of_xml) (Xml.child xml_arg0 "RiskCounts") in
      let notes = (Option.map ~f:Notes.of_xml) (Xml.child xml_arg0 "Notes") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let pillarReviewSummaries =
        (Option.map ~f:PillarReviewSummaries.of_xml)
          (Xml.child xml_arg0 "PillarReviewSummaries") in
      let lensStatus =
        (Option.map ~f:LensStatus.of_xml) (Xml.child xml_arg0 "LensStatus") in
      let lensName =
        (Option.map ~f:LensName.of_xml) (Xml.child xml_arg0 "LensName") in
      let lensVersion =
        (Option.map ~f:LensVersion.of_xml) (Xml.child xml_arg0 "LensVersion") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      make ?nextToken ?riskCounts ?notes ?updatedAt ?pillarReviewSummaries
        ?lensStatus ?lensName ?lensVersion ?lensArn ?lensAlias ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let riskCounts = field_map json "RiskCounts" RiskCounts.of_json in
      let notes = field_map json "Notes" Notes.of_json in
      let updatedAt = field_map json "UpdatedAt" Timestamp.of_json in
      let pillarReviewSummaries =
        field_map json "PillarReviewSummaries" PillarReviewSummaries.of_json in
      let lensStatus = field_map json "LensStatus" LensStatus.of_json in
      let lensName = field_map json "LensName" LensName.of_json in
      let lensVersion = field_map json "LensVersion" LensVersion.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      make ?nextToken ?riskCounts ?notes ?updatedAt ?pillarReviewSummaries
        ?lensStatus ?lensName ?lensVersion ?lensArn ?lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module PillarNotes =
  struct
    type nonrec t = (PillarId.t * Notes.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((PillarId.of_string chopped),
                              (Notes.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (PillarId.to_value x) |>
                    (fun x -> (Notes.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:PillarId.of_string ~of_json:Notes.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Answer =
  struct
    type nonrec t =
      {
      questionId: QuestionId.t option ;
      pillarId: PillarId.t option ;
      questionTitle: QuestionTitle.t option ;
      questionDescription: QuestionDescription.t option ;
      improvementPlanUrl: ImprovementPlanUrl.t option ;
      helpfulResourceUrl: HelpfulResourceUrl.t option ;
      helpfulResourceDisplayText: DisplayText.t option ;
      choices: Choices.t option ;
      selectedChoices: SelectedChoices.t option ;
      choiceAnswers: ChoiceAnswers.t option ;
      isApplicable: IsApplicable.t option ;
      risk: Risk.t option ;
      notes: Notes.t option ;
      reason: AnswerReason.t option }
    let make ?questionId =
      fun ?pillarId ->
        fun ?questionTitle ->
          fun ?questionDescription ->
            fun ?improvementPlanUrl ->
              fun ?helpfulResourceUrl ->
                fun ?helpfulResourceDisplayText ->
                  fun ?choices ->
                    fun ?selectedChoices ->
                      fun ?choiceAnswers ->
                        fun ?isApplicable ->
                          fun ?risk ->
                            fun ?notes ->
                              fun ?reason ->
                                fun () ->
                                  {
                                    questionId;
                                    pillarId;
                                    questionTitle;
                                    questionDescription;
                                    improvementPlanUrl;
                                    helpfulResourceUrl;
                                    helpfulResourceDisplayText;
                                    choices;
                                    selectedChoices;
                                    choiceAnswers;
                                    isApplicable;
                                    risk;
                                    notes;
                                    reason
                                  }
    let to_value x =
      structure_to_value
        [("QuestionId", (Option.map x.questionId ~f:QuestionId.to_value));
        ("PillarId", (Option.map x.pillarId ~f:PillarId.to_value));
        ("QuestionTitle",
          (Option.map x.questionTitle ~f:QuestionTitle.to_value));
        ("QuestionDescription",
          (Option.map x.questionDescription ~f:QuestionDescription.to_value));
        ("ImprovementPlanUrl",
          (Option.map x.improvementPlanUrl ~f:ImprovementPlanUrl.to_value));
        ("HelpfulResourceUrl",
          (Option.map x.helpfulResourceUrl ~f:HelpfulResourceUrl.to_value));
        ("HelpfulResourceDisplayText",
          (Option.map x.helpfulResourceDisplayText ~f:DisplayText.to_value));
        ("Choices", (Option.map x.choices ~f:Choices.to_value));
        ("SelectedChoices",
          (Option.map x.selectedChoices ~f:SelectedChoices.to_value));
        ("ChoiceAnswers",
          (Option.map x.choiceAnswers ~f:ChoiceAnswers.to_value));
        ("IsApplicable",
          (Option.map x.isApplicable ~f:IsApplicable.to_value));
        ("Risk", (Option.map x.risk ~f:Risk.to_value));
        ("Notes", (Option.map x.notes ~f:Notes.to_value));
        ("Reason", (Option.map x.reason ~f:AnswerReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:AnswerReason.of_xml) (Xml.child xml_arg0 "Reason") in
      let notes = (Option.map ~f:Notes.of_xml) (Xml.child xml_arg0 "Notes") in
      let risk = (Option.map ~f:Risk.of_xml) (Xml.child xml_arg0 "Risk") in
      let isApplicable =
        (Option.map ~f:IsApplicable.of_xml)
          (Xml.child xml_arg0 "IsApplicable") in
      let choiceAnswers =
        (Option.map ~f:ChoiceAnswers.of_xml)
          (Xml.child xml_arg0 "ChoiceAnswers") in
      let selectedChoices =
        (Option.map ~f:SelectedChoices.of_xml)
          (Xml.child xml_arg0 "SelectedChoices") in
      let choices =
        (Option.map ~f:Choices.of_xml) (Xml.child xml_arg0 "Choices") in
      let helpfulResourceDisplayText =
        (Option.map ~f:DisplayText.of_xml)
          (Xml.child xml_arg0 "HelpfulResourceDisplayText") in
      let helpfulResourceUrl =
        (Option.map ~f:HelpfulResourceUrl.of_xml)
          (Xml.child xml_arg0 "HelpfulResourceUrl") in
      let improvementPlanUrl =
        (Option.map ~f:ImprovementPlanUrl.of_xml)
          (Xml.child xml_arg0 "ImprovementPlanUrl") in
      let questionDescription =
        (Option.map ~f:QuestionDescription.of_xml)
          (Xml.child xml_arg0 "QuestionDescription") in
      let questionTitle =
        (Option.map ~f:QuestionTitle.of_xml)
          (Xml.child xml_arg0 "QuestionTitle") in
      let pillarId =
        (Option.map ~f:PillarId.of_xml) (Xml.child xml_arg0 "PillarId") in
      let questionId =
        (Option.map ~f:QuestionId.of_xml) (Xml.child xml_arg0 "QuestionId") in
      make ?reason ?notes ?risk ?isApplicable ?choiceAnswers ?selectedChoices
        ?choices ?helpfulResourceDisplayText ?helpfulResourceUrl
        ?improvementPlanUrl ?questionDescription ?questionTitle ?pillarId
        ?questionId ()
    let of_json json =
      let reason = field_map json "Reason" AnswerReason.of_json in
      let notes = field_map json "Notes" Notes.of_json in
      let risk = field_map json "Risk" Risk.of_json in
      let isApplicable = field_map json "IsApplicable" IsApplicable.of_json in
      let choiceAnswers =
        field_map json "ChoiceAnswers" ChoiceAnswers.of_json in
      let selectedChoices =
        field_map json "SelectedChoices" SelectedChoices.of_json in
      let choices = field_map json "Choices" Choices.of_json in
      let helpfulResourceDisplayText =
        field_map json "HelpfulResourceDisplayText" DisplayText.of_json in
      let helpfulResourceUrl =
        field_map json "HelpfulResourceUrl" HelpfulResourceUrl.of_json in
      let improvementPlanUrl =
        field_map json "ImprovementPlanUrl" ImprovementPlanUrl.of_json in
      let questionDescription =
        field_map json "QuestionDescription" QuestionDescription.of_json in
      let questionTitle =
        field_map json "QuestionTitle" QuestionTitle.of_json in
      let pillarId = field_map json "PillarId" PillarId.of_json in
      let questionId = field_map json "QuestionId" QuestionId.of_json in
      make ?reason ?notes ?risk ?isApplicable ?choiceAnswers ?selectedChoices
        ?choices ?helpfulResourceDisplayText ?helpfulResourceUrl
        ?improvementPlanUrl ?questionDescription ?questionTitle ?pillarId
        ?questionId ()
    let to_json v = composed_to_json to_value v
  end
module ChoiceUpdates =
  struct
    type nonrec t = (ChoiceId.t * ChoiceUpdate.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ChoiceId ChoiceUpdate"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ChoiceId.to_value x) |>
                    (fun x -> (ChoiceUpdate.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ChoiceId.of_string
        ~of_json:ChoiceUpdate.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module WorkloadSummaries =
  struct
    type nonrec t = WorkloadSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:WorkloadSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WorkloadSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkloadSummaries" ~of_json:WorkloadSummary.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ListWorkloadsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListWorkloadsMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module WorkloadNamePrefix =
  struct
    type nonrec t = string
    let context_ = "WorkloadNamePrefix"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:100); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkloadNamePrefix" j
    let to_json = simple_to_json to_value
  end
module WorkloadShareSummaries =
  struct
    type nonrec t = WorkloadShareSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:WorkloadShareSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WorkloadShareSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkloadShareSummaries"
        ~of_json:WorkloadShareSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListWorkloadSharesMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListWorkloadSharesMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SharedWithPrefix =
  struct
    type nonrec t = string
    let context_ = "SharedWithPrefix"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:100); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SharedWithPrefix" j
    let to_json = simple_to_json to_value
  end
module ShareInvitationSummaries =
  struct
    type nonrec t = ShareInvitationSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ShareInvitationSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ShareInvitationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ShareInvitationSummaries"
        ~of_json:ShareInvitationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module LensNamePrefix =
  struct
    type nonrec t = string
    let context_ = "LensNamePrefix"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:100); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LensNamePrefix" j
    let to_json = simple_to_json to_value
  end
module ListShareInvitationsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListShareInvitationsMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NotificationSummaries =
  struct
    type nonrec t = NotificationSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NotificationSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NotificationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"NotificationSummaries"
        ~of_json:NotificationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListNotificationsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListNotificationsMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MilestoneSummaries =
  struct
    type nonrec t = MilestoneSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MilestoneSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MilestoneSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"MilestoneSummaries"
        ~of_json:MilestoneSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module LensSummaries =
  struct
    type nonrec t = LensSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LensSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LensSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"LensSummaries" ~of_json:LensSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module LensStatusType =
  struct
    type nonrec t =
      | ALL 
      | DRAFT 
      | PUBLISHED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALL -> "ALL"
      | DRAFT -> "DRAFT"
      | PUBLISHED -> "PUBLISHED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALL" -> ALL
      | "DRAFT" -> DRAFT
      | "PUBLISHED" -> PUBLISHED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LensStatusType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LensStatusType" j)
    let to_json = simple_to_json to_value
  end
module LensShareSummaries =
  struct
    type nonrec t = LensShareSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LensShareSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LensShareSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"LensShareSummaries"
        ~of_json:LensShareSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module LensReviewSummaries =
  struct
    type nonrec t = LensReviewSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LensReviewSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LensReviewSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"LensReviewSummaries"
        ~of_json:LensReviewSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImprovementSummaries =
  struct
    type nonrec t = ImprovementSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ImprovementSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ImprovementSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ImprovementSummaries"
        ~of_json:ImprovementSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListLensReviewImprovementsMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListLensReviewImprovementsMaxResults"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AnswerSummaries =
  struct
    type nonrec t = AnswerSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AnswerSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AnswerSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"AnswerSummaries" ~of_json:AnswerSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListAnswersMaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ListAnswersMaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ImportLensStatus =
  struct
    type nonrec t =
      | IN_PROGRESS 
      | COMPLETE 
      | ERROR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMPLETE -> "COMPLETE"
      | ERROR -> "ERROR"
      | Non_static_id s -> s
    let of_string =
      function
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMPLETE" -> COMPLETE
      | "ERROR" -> ERROR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ImportLensStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ImportLensStatus" j)
    let to_json = simple_to_json to_value
  end
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t ;
      resourceId: ExceptionResourceId.t option ;
      resourceType: ExceptionResourceType.t option ;
      quotaCode: QuotaCode.t ;
      serviceCode: ServiceCode.t }
    let context_ = "ServiceQuotaExceededException"
    let make ?resourceId =
      fun ?resourceType ->
        fun ~message ->
          fun ~quotaCode ->
            fun ~serviceCode ->
              fun () ->
                { resourceId; resourceType; message; quotaCode; serviceCode }
    let to_value x =
      structure_to_value
        [("Message", (Some (ExceptionMessage.to_value x.message)));
        ("ResourceId",
          (Option.map x.resourceId ~f:ExceptionResourceId.to_value));
        ("ResourceType",
          (Option.map x.resourceType ~f:ExceptionResourceType.to_value));
        ("QuotaCode", (Some (QuotaCode.to_value x.quotaCode)));
        ("ServiceCode", (Some (ServiceCode.to_value x.serviceCode)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serviceCode =
        ServiceCode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceCode") in
      let quotaCode =
        QuotaCode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "QuotaCode") in
      let resourceType =
        (Option.map ~f:ExceptionResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:ExceptionResourceId.of_xml)
          (Xml.child xml_arg0 "ResourceId") in
      let message =
        ExceptionMessage.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      make ~serviceCode ~quotaCode ?resourceType ?resourceId ~message ()
    let of_json json =
      let serviceCode = field_map_exn json "ServiceCode" ServiceCode.of_json in
      let quotaCode = field_map_exn json "QuotaCode" QuotaCode.of_json in
      let resourceType =
        field_map json "ResourceType" ExceptionResourceType.of_json in
      let resourceId =
        field_map json "ResourceId" ExceptionResourceId.of_json in
      let message = field_map_exn json "Message" ExceptionMessage.of_json in
      make ~serviceCode ~quotaCode ?resourceType ?resourceId ~message ()
    let to_json v = composed_to_json to_value v
  end
module LensJSON =
  struct
    type nonrec t = string
    let context_ = "LensJSON"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:500000) >>=
             (fun () -> check_string_min i ~min:2));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LensJSON" j
    let to_json = simple_to_json to_value
  end
module Milestone =
  struct
    type nonrec t =
      {
      milestoneNumber: MilestoneNumber.t option ;
      milestoneName: MilestoneName.t option ;
      recordedAt: Timestamp.t option ;
      workload: Workload.t option }
    let make ?milestoneNumber =
      fun ?milestoneName ->
        fun ?recordedAt ->
          fun ?workload ->
            fun () ->
              { milestoneNumber; milestoneName; recordedAt; workload }
    let to_value x =
      structure_to_value
        [("MilestoneNumber",
           (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("MilestoneName",
          (Option.map x.milestoneName ~f:MilestoneName.to_value));
        ("RecordedAt", (Option.map x.recordedAt ~f:Timestamp.to_value));
        ("Workload", (Option.map x.workload ~f:Workload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workload =
        (Option.map ~f:Workload.of_xml) (Xml.child xml_arg0 "Workload") in
      let recordedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "RecordedAt") in
      let milestoneName =
        (Option.map ~f:MilestoneName.of_xml)
          (Xml.child xml_arg0 "MilestoneName") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      make ?workload ?recordedAt ?milestoneName ?milestoneNumber ()
    let of_json json =
      let workload = field_map json "Workload" Workload.of_json in
      let recordedAt = field_map json "RecordedAt" Timestamp.of_json in
      let milestoneName =
        field_map json "MilestoneName" MilestoneName.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      make ?workload ?recordedAt ?milestoneName ?milestoneNumber ()
    let to_json v = composed_to_json to_value v
  end
module VersionDifferences =
  struct
    type nonrec t = {
      pillarDifferences: PillarDifferences.t option }
    let make ?pillarDifferences = fun () -> { pillarDifferences }
    let to_value x =
      structure_to_value
        [("PillarDifferences",
           (Option.map x.pillarDifferences ~f:PillarDifferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pillarDifferences =
        (Option.map ~f:PillarDifferences.of_xml)
          (Xml.child xml_arg0 "PillarDifferences") in
      make ?pillarDifferences ()
    let of_json json =
      let pillarDifferences =
        field_map json "PillarDifferences" PillarDifferences.of_json in
      make ?pillarDifferences ()
    let to_json v = composed_to_json to_value v
  end
module LensReviewReport =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      base64String: Base64String.t option }
    let make ?lensAlias =
      fun ?lensArn ->
        fun ?base64String -> fun () -> { lensAlias; lensArn; base64String }
    let to_value x =
      structure_to_value
        [("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("Base64String",
          (Option.map x.base64String ~f:Base64String.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let base64String =
        (Option.map ~f:Base64String.of_xml)
          (Xml.child xml_arg0 "Base64String") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      make ?base64String ?lensArn ?lensAlias ()
    let of_json json =
      let base64String = field_map json "Base64String" Base64String.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      make ?base64String ?lensArn ?lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module Lens =
  struct
    type nonrec t =
      {
      lensArn: LensArn.t option ;
      lensVersion: LensVersion.t option ;
      name: LensName.t option ;
      description: LensDescription.t option ;
      owner: LensOwner.t option ;
      shareInvitationId: ShareInvitationId.t option }
    let make ?lensArn =
      fun ?lensVersion ->
        fun ?name ->
          fun ?description ->
            fun ?owner ->
              fun ?shareInvitationId ->
                fun () ->
                  {
                    lensArn;
                    lensVersion;
                    name;
                    description;
                    owner;
                    shareInvitationId
                  }
    let to_value x =
      structure_to_value
        [("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("LensVersion", (Option.map x.lensVersion ~f:LensVersion.to_value));
        ("Name", (Option.map x.name ~f:LensName.to_value));
        ("Description",
          (Option.map x.description ~f:LensDescription.to_value));
        ("Owner", (Option.map x.owner ~f:LensOwner.to_value));
        ("ShareInvitationId",
          (Option.map x.shareInvitationId ~f:ShareInvitationId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareInvitationId =
        (Option.map ~f:ShareInvitationId.of_xml)
          (Xml.child xml_arg0 "ShareInvitationId") in
      let owner =
        (Option.map ~f:LensOwner.of_xml) (Xml.child xml_arg0 "Owner") in
      let description =
        (Option.map ~f:LensDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name = (Option.map ~f:LensName.of_xml) (Xml.child xml_arg0 "Name") in
      let lensVersion =
        (Option.map ~f:LensVersion.of_xml) (Xml.child xml_arg0 "LensVersion") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      make ?shareInvitationId ?owner ?description ?name ?lensVersion ?lensArn
        ()
    let of_json json =
      let shareInvitationId =
        field_map json "ShareInvitationId" ShareInvitationId.of_json in
      let owner = field_map json "Owner" LensOwner.of_json in
      let description = field_map json "Description" LensDescription.of_json in
      let name = field_map json "Name" LensName.of_json in
      let lensVersion = field_map json "LensVersion" LensVersion.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      make ?shareInvitationId ?owner ?description ?name ?lensVersion ?lensArn
        ()
    let to_json v = composed_to_json to_value v
  end
module LensAliases =
  struct
    type nonrec t = LensAlias.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:LensAlias.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LensAlias.of_xml)
    let of_json j =
      list_of_json ~kind:"LensAliases" ~of_json:LensAlias.of_json j
    let to_json v = composed_to_json to_value v
  end
module IsMajorVersion =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module UpgradeLensReviewInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      lensAlias: LensAlias.t ;
      milestoneName: MilestoneName.t ;
      clientRequestToken: ClientRequestToken.t option }
    let context_ = "UpgradeLensReviewInput"
    let make ?clientRequestToken =
      fun ~workloadId ->
        fun ~lensAlias ->
          fun ~milestoneName ->
            fun () ->
              { clientRequestToken; workloadId; lensAlias; milestoneName }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("MilestoneName", (Some (MilestoneName.to_value x.milestoneName)));
        ("ClientRequestToken",
          (Option.map x.clientRequestToken ~f:ClientRequestToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientRequestToken =
        (Option.map ~f:ClientRequestToken.of_xml)
          (Xml.child xml_arg0 "ClientRequestToken") in
      let milestoneName =
        MilestoneName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MilestoneName") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?clientRequestToken ~milestoneName ~lensAlias ~workloadId ()
    let of_json json =
      let clientRequestToken =
        field_map json "ClientRequestToken" ClientRequestToken.of_json in
      let milestoneName =
        field_map_exn json "MilestoneName" MilestoneName.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?clientRequestToken ~milestoneName ~lensAlias ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateWorkloadShareOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      workloadShare: WorkloadShare.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?workloadShare -> fun () -> { workloadId; workloadShare }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("WorkloadShare",
          (Option.map x.workloadShare ~f:WorkloadShare.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workloadShare =
        (Option.map ~f:WorkloadShare.of_xml)
          (Xml.child xml_arg0 "WorkloadShare") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?workloadShare ?workloadId ()
    let of_json json =
      let workloadShare =
        field_map json "WorkloadShare" WorkloadShare.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?workloadShare ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateWorkloadShareInput =
  struct
    type nonrec t =
      {
      shareId: ShareId.t ;
      workloadId: WorkloadId.t ;
      permissionType: PermissionType.t }
    let context_ = "UpdateWorkloadShareInput"
    let make ~shareId =
      fun ~workloadId ->
        fun ~permissionType ->
          fun () -> { shareId; workloadId; permissionType }
    let to_value x =
      structure_to_value
        [("ShareId", (Some (ShareId.to_value x.shareId)));
        ("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("PermissionType", (Some (PermissionType.to_value x.permissionType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let permissionType =
        PermissionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PermissionType") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      let shareId =
        ShareId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ShareId") in
      make ~permissionType ~workloadId ~shareId ()
    let of_json json =
      let permissionType =
        field_map_exn json "PermissionType" PermissionType.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      let shareId = field_map_exn json "ShareId" ShareId.of_json in
      make ~permissionType ~workloadId ~shareId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateWorkloadOutput =
  struct
    type nonrec t = {
      workload: Workload.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workload = fun () -> { workload }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Workload", (Option.map x.workload ~f:Workload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workload =
        (Option.map ~f:Workload.of_xml) (Xml.child xml_arg0 "Workload") in
      make ?workload ()
    let of_json json =
      let workload = field_map json "Workload" Workload.of_json in
      make ?workload ()
    let to_json v = composed_to_json to_value v
  end
module UpdateWorkloadInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      workloadName: WorkloadName.t option ;
      description: WorkloadDescription.t option ;
      environment: WorkloadEnvironment.t option ;
      accountIds: WorkloadAccountIds.t option ;
      awsRegions: WorkloadAwsRegions.t option ;
      nonAwsRegions: WorkloadNonAwsRegions.t option ;
      pillarPriorities: WorkloadPillarPriorities.t option ;
      architecturalDesign: WorkloadArchitecturalDesign.t option ;
      reviewOwner: WorkloadReviewOwner.t option ;
      isReviewOwnerUpdateAcknowledged:
        IsReviewOwnerUpdateAcknowledged.t option ;
      industryType: WorkloadIndustryType.t option ;
      industry: WorkloadIndustry.t option ;
      notes: Notes.t option ;
      improvementStatus: WorkloadImprovementStatus.t option }
    let context_ = "UpdateWorkloadInput"
    let make ?workloadName =
      fun ?description ->
        fun ?environment ->
          fun ?accountIds ->
            fun ?awsRegions ->
              fun ?nonAwsRegions ->
                fun ?pillarPriorities ->
                  fun ?architecturalDesign ->
                    fun ?reviewOwner ->
                      fun ?isReviewOwnerUpdateAcknowledged ->
                        fun ?industryType ->
                          fun ?industry ->
                            fun ?notes ->
                              fun ?improvementStatus ->
                                fun ~workloadId ->
                                  fun () ->
                                    {
                                      workloadName;
                                      description;
                                      environment;
                                      accountIds;
                                      awsRegions;
                                      nonAwsRegions;
                                      pillarPriorities;
                                      architecturalDesign;
                                      reviewOwner;
                                      isReviewOwnerUpdateAcknowledged;
                                      industryType;
                                      industry;
                                      notes;
                                      improvementStatus;
                                      workloadId
                                    }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("WorkloadName",
          (Option.map x.workloadName ~f:WorkloadName.to_value));
        ("Description",
          (Option.map x.description ~f:WorkloadDescription.to_value));
        ("Environment",
          (Option.map x.environment ~f:WorkloadEnvironment.to_value));
        ("AccountIds",
          (Option.map x.accountIds ~f:WorkloadAccountIds.to_value));
        ("AwsRegions",
          (Option.map x.awsRegions ~f:WorkloadAwsRegions.to_value));
        ("NonAwsRegions",
          (Option.map x.nonAwsRegions ~f:WorkloadNonAwsRegions.to_value));
        ("PillarPriorities",
          (Option.map x.pillarPriorities ~f:WorkloadPillarPriorities.to_value));
        ("ArchitecturalDesign",
          (Option.map x.architecturalDesign
             ~f:WorkloadArchitecturalDesign.to_value));
        ("ReviewOwner",
          (Option.map x.reviewOwner ~f:WorkloadReviewOwner.to_value));
        ("IsReviewOwnerUpdateAcknowledged",
          (Option.map x.isReviewOwnerUpdateAcknowledged
             ~f:IsReviewOwnerUpdateAcknowledged.to_value));
        ("IndustryType",
          (Option.map x.industryType ~f:WorkloadIndustryType.to_value));
        ("Industry", (Option.map x.industry ~f:WorkloadIndustry.to_value));
        ("Notes", (Option.map x.notes ~f:Notes.to_value));
        ("ImprovementStatus",
          (Option.map x.improvementStatus
             ~f:WorkloadImprovementStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let improvementStatus =
        (Option.map ~f:WorkloadImprovementStatus.of_xml)
          (Xml.child xml_arg0 "ImprovementStatus") in
      let notes = (Option.map ~f:Notes.of_xml) (Xml.child xml_arg0 "Notes") in
      let industry =
        (Option.map ~f:WorkloadIndustry.of_xml)
          (Xml.child xml_arg0 "Industry") in
      let industryType =
        (Option.map ~f:WorkloadIndustryType.of_xml)
          (Xml.child xml_arg0 "IndustryType") in
      let isReviewOwnerUpdateAcknowledged =
        (Option.map ~f:IsReviewOwnerUpdateAcknowledged.of_xml)
          (Xml.child xml_arg0 "IsReviewOwnerUpdateAcknowledged") in
      let reviewOwner =
        (Option.map ~f:WorkloadReviewOwner.of_xml)
          (Xml.child xml_arg0 "ReviewOwner") in
      let architecturalDesign =
        (Option.map ~f:WorkloadArchitecturalDesign.of_xml)
          (Xml.child xml_arg0 "ArchitecturalDesign") in
      let pillarPriorities =
        (Option.map ~f:WorkloadPillarPriorities.of_xml)
          (Xml.child xml_arg0 "PillarPriorities") in
      let nonAwsRegions =
        (Option.map ~f:WorkloadNonAwsRegions.of_xml)
          (Xml.child xml_arg0 "NonAwsRegions") in
      let awsRegions =
        (Option.map ~f:WorkloadAwsRegions.of_xml)
          (Xml.child xml_arg0 "AwsRegions") in
      let accountIds =
        (Option.map ~f:WorkloadAccountIds.of_xml)
          (Xml.child xml_arg0 "AccountIds") in
      let environment =
        (Option.map ~f:WorkloadEnvironment.of_xml)
          (Xml.child xml_arg0 "Environment") in
      let description =
        (Option.map ~f:WorkloadDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let workloadName =
        (Option.map ~f:WorkloadName.of_xml)
          (Xml.child xml_arg0 "WorkloadName") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?improvementStatus ?notes ?industry ?industryType
        ?isReviewOwnerUpdateAcknowledged ?reviewOwner ?architecturalDesign
        ?pillarPriorities ?nonAwsRegions ?awsRegions ?accountIds ?environment
        ?description ?workloadName ~workloadId ()
    let of_json json =
      let improvementStatus =
        field_map json "ImprovementStatus" WorkloadImprovementStatus.of_json in
      let notes = field_map json "Notes" Notes.of_json in
      let industry = field_map json "Industry" WorkloadIndustry.of_json in
      let industryType =
        field_map json "IndustryType" WorkloadIndustryType.of_json in
      let isReviewOwnerUpdateAcknowledged =
        field_map json "IsReviewOwnerUpdateAcknowledged"
          IsReviewOwnerUpdateAcknowledged.of_json in
      let reviewOwner =
        field_map json "ReviewOwner" WorkloadReviewOwner.of_json in
      let architecturalDesign =
        field_map json "ArchitecturalDesign"
          WorkloadArchitecturalDesign.of_json in
      let pillarPriorities =
        field_map json "PillarPriorities" WorkloadPillarPriorities.of_json in
      let nonAwsRegions =
        field_map json "NonAwsRegions" WorkloadNonAwsRegions.of_json in
      let awsRegions = field_map json "AwsRegions" WorkloadAwsRegions.of_json in
      let accountIds = field_map json "AccountIds" WorkloadAccountIds.of_json in
      let environment =
        field_map json "Environment" WorkloadEnvironment.of_json in
      let description =
        field_map json "Description" WorkloadDescription.of_json in
      let workloadName = field_map json "WorkloadName" WorkloadName.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?improvementStatus ?notes ?industry ?industryType
        ?isReviewOwnerUpdateAcknowledged ?reviewOwner ?architecturalDesign
        ?pillarPriorities ?nonAwsRegions ?awsRegions ?accountIds ?environment
        ?description ?workloadName ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateShareInvitationOutput =
  struct
    type nonrec t = {
      shareInvitation: ShareInvitation.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?shareInvitation = fun () -> { shareInvitation }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ShareInvitation",
           (Option.map x.shareInvitation ~f:ShareInvitation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareInvitation =
        (Option.map ~f:ShareInvitation.of_xml)
          (Xml.child xml_arg0 "ShareInvitation") in
      make ?shareInvitation ()
    let of_json json =
      let shareInvitation =
        field_map json "ShareInvitation" ShareInvitation.of_json in
      make ?shareInvitation ()
    let to_json v = composed_to_json to_value v
  end
module UpdateShareInvitationInput =
  struct
    type nonrec t =
      {
      shareInvitationId: ShareInvitationId.t ;
      shareInvitationAction: ShareInvitationAction.t }
    let context_ = "UpdateShareInvitationInput"
    let make ~shareInvitationId =
      fun ~shareInvitationAction ->
        fun () -> { shareInvitationId; shareInvitationAction }
    let to_value x =
      structure_to_value
        [("ShareInvitationId",
           (Some (ShareInvitationId.to_value x.shareInvitationId)));
        ("ShareInvitationAction",
          (Some (ShareInvitationAction.to_value x.shareInvitationAction)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareInvitationAction =
        ShareInvitationAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ShareInvitationAction") in
      let shareInvitationId =
        ShareInvitationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ShareInvitationId") in
      make ~shareInvitationAction ~shareInvitationId ()
    let of_json json =
      let shareInvitationAction =
        field_map_exn json "ShareInvitationAction"
          ShareInvitationAction.of_json in
      let shareInvitationId =
        field_map_exn json "ShareInvitationId" ShareInvitationId.of_json in
      make ~shareInvitationAction ~shareInvitationId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateLensReviewOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      lensReview: LensReview.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?lensReview -> fun () -> { workloadId; lensReview }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("LensReview", (Option.map x.lensReview ~f:LensReview.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensReview =
        (Option.map ~f:LensReview.of_xml) (Xml.child xml_arg0 "LensReview") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?lensReview ?workloadId ()
    let of_json json =
      let lensReview = field_map json "LensReview" LensReview.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?lensReview ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateLensReviewInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      lensAlias: LensAlias.t ;
      lensNotes: Notes.t option ;
      pillarNotes: PillarNotes.t option }
    let context_ = "UpdateLensReviewInput"
    let make ?lensNotes =
      fun ?pillarNotes ->
        fun ~workloadId ->
          fun ~lensAlias ->
            fun () -> { lensNotes; pillarNotes; workloadId; lensAlias }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("LensNotes", (Option.map x.lensNotes ~f:Notes.to_value));
        ("PillarNotes", (Option.map x.pillarNotes ~f:PillarNotes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pillarNotes =
        (Option.map ~f:PillarNotes.of_xml) (Xml.child xml_arg0 "PillarNotes") in
      let lensNotes =
        (Option.map ~f:Notes.of_xml) (Xml.child xml_arg0 "LensNotes") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?pillarNotes ?lensNotes ~lensAlias ~workloadId ()
    let of_json json =
      let pillarNotes = field_map json "PillarNotes" PillarNotes.of_json in
      let lensNotes = field_map json "LensNotes" Notes.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?pillarNotes ?lensNotes ~lensAlias ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAnswerOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      answer: Answer.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?lensAlias ->
        fun ?lensArn ->
          fun ?answer -> fun () -> { workloadId; lensAlias; lensArn; answer }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("Answer", (Option.map x.answer ~f:Answer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let answer =
        (Option.map ~f:Answer.of_xml) (Xml.child xml_arg0 "Answer") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?answer ?lensArn ?lensAlias ?workloadId ()
    let of_json json =
      let answer = field_map json "Answer" Answer.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?answer ?lensArn ?lensAlias ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateAnswerInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      lensAlias: LensAlias.t ;
      questionId: QuestionId.t ;
      selectedChoices: SelectedChoices.t option ;
      choiceUpdates: ChoiceUpdates.t option ;
      notes: Notes.t option ;
      isApplicable: IsApplicable.t option ;
      reason: AnswerReason.t option }
    let context_ = "UpdateAnswerInput"
    let make ?selectedChoices =
      fun ?choiceUpdates ->
        fun ?notes ->
          fun ?isApplicable ->
            fun ?reason ->
              fun ~workloadId ->
                fun ~lensAlias ->
                  fun ~questionId ->
                    fun () ->
                      {
                        selectedChoices;
                        choiceUpdates;
                        notes;
                        isApplicable;
                        reason;
                        workloadId;
                        lensAlias;
                        questionId
                      }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("QuestionId", (Some (QuestionId.to_value x.questionId)));
        ("SelectedChoices",
          (Option.map x.selectedChoices ~f:SelectedChoices.to_value));
        ("ChoiceUpdates",
          (Option.map x.choiceUpdates ~f:ChoiceUpdates.to_value));
        ("Notes", (Option.map x.notes ~f:Notes.to_value));
        ("IsApplicable",
          (Option.map x.isApplicable ~f:IsApplicable.to_value));
        ("Reason", (Option.map x.reason ~f:AnswerReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:AnswerReason.of_xml) (Xml.child xml_arg0 "Reason") in
      let isApplicable =
        (Option.map ~f:IsApplicable.of_xml)
          (Xml.child xml_arg0 "IsApplicable") in
      let notes = (Option.map ~f:Notes.of_xml) (Xml.child xml_arg0 "Notes") in
      let choiceUpdates =
        (Option.map ~f:ChoiceUpdates.of_xml)
          (Xml.child xml_arg0 "ChoiceUpdates") in
      let selectedChoices =
        (Option.map ~f:SelectedChoices.of_xml)
          (Xml.child xml_arg0 "SelectedChoices") in
      let questionId =
        QuestionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "QuestionId") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?reason ?isApplicable ?notes ?choiceUpdates ?selectedChoices
        ~questionId ~lensAlias ~workloadId ()
    let of_json json =
      let reason = field_map json "Reason" AnswerReason.of_json in
      let isApplicable = field_map json "IsApplicable" IsApplicable.of_json in
      let notes = field_map json "Notes" Notes.of_json in
      let choiceUpdates =
        field_map json "ChoiceUpdates" ChoiceUpdates.of_json in
      let selectedChoices =
        field_map json "SelectedChoices" SelectedChoices.of_json in
      let questionId = field_map_exn json "QuestionId" QuestionId.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?reason ?isApplicable ?notes ?choiceUpdates ?selectedChoices
        ~questionId ~lensAlias ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module UntagResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module UntagResourceInput =
  struct
    type nonrec t = {
      workloadArn: WorkloadArn.t ;
      tagKeys: TagKeyList.t }
    let context_ = "UntagResourceInput"
    let make ~workloadArn =
      fun ~tagKeys -> fun () -> { workloadArn; tagKeys }
    let to_value x =
      structure_to_value
        [("WorkloadArn", (Some (WorkloadArn.to_value x.workloadArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let workloadArn =
        WorkloadArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadArn") in
      make ~tagKeys ~workloadArn ()
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let workloadArn = field_map_exn json "WorkloadArn" WorkloadArn.of_json in
      make ~tagKeys ~workloadArn ()
    let to_json v = composed_to_json to_value v
  end
module TagResourceOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module TagResourceInput =
  struct
    type nonrec t = {
      workloadArn: WorkloadArn.t ;
      tags: TagMap.t }
    let context_ = "TagResourceInput"
    let make ~workloadArn = fun ~tags -> fun () -> { workloadArn; tags }
    let to_value x =
      structure_to_value
        [("WorkloadArn", (Some (WorkloadArn.to_value x.workloadArn)));
        ("Tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let workloadArn =
        WorkloadArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadArn") in
      make ~tags ~workloadArn ()
    let of_json json =
      let tags = field_map_exn json "Tags" TagMap.of_json in
      let workloadArn = field_map_exn json "WorkloadArn" WorkloadArn.of_json in
      make ~tags ~workloadArn ()
    let to_json v = composed_to_json to_value v
  end
module ListWorkloadsOutput =
  struct
    type nonrec t =
      {
      workloadSummaries: WorkloadSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadSummaries =
      fun ?nextToken -> fun () -> { workloadSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadSummaries",
           (Option.map x.workloadSummaries ~f:WorkloadSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let workloadSummaries =
        (Option.map ~f:WorkloadSummaries.of_xml)
          (Xml.child xml_arg0 "WorkloadSummaries") in
      make ?nextToken ?workloadSummaries ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let workloadSummaries =
        field_map json "WorkloadSummaries" WorkloadSummaries.of_json in
      make ?nextToken ?workloadSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListWorkloadsInput =
  struct
    type nonrec t =
      {
      workloadNamePrefix: WorkloadNamePrefix.t option ;
      nextToken: NextToken.t option ;
      maxResults: ListWorkloadsMaxResults.t option }
    let make ?workloadNamePrefix =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { workloadNamePrefix; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("WorkloadNamePrefix",
           (Option.map x.workloadNamePrefix ~f:WorkloadNamePrefix.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:ListWorkloadsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListWorkloadsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let workloadNamePrefix =
        (Option.map ~f:WorkloadNamePrefix.of_xml)
          (Xml.child xml_arg0 "WorkloadNamePrefix") in
      make ?maxResults ?nextToken ?workloadNamePrefix ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" ListWorkloadsMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let workloadNamePrefix =
        field_map json "WorkloadNamePrefix" WorkloadNamePrefix.of_json in
      make ?maxResults ?nextToken ?workloadNamePrefix ()
    let to_json v = composed_to_json to_value v
  end
module ListWorkloadSharesOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      workloadShareSummaries: WorkloadShareSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?workloadShareSummaries ->
        fun ?nextToken ->
          fun () -> { workloadId; workloadShareSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("WorkloadShareSummaries",
          (Option.map x.workloadShareSummaries
             ~f:WorkloadShareSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let workloadShareSummaries =
        (Option.map ~f:WorkloadShareSummaries.of_xml)
          (Xml.child xml_arg0 "WorkloadShareSummaries") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?nextToken ?workloadShareSummaries ?workloadId ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let workloadShareSummaries =
        field_map json "WorkloadShareSummaries"
          WorkloadShareSummaries.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?nextToken ?workloadShareSummaries ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListWorkloadSharesInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      sharedWithPrefix: SharedWithPrefix.t option ;
      nextToken: NextToken.t option ;
      maxResults: ListWorkloadSharesMaxResults.t option }
    let context_ = "ListWorkloadSharesInput"
    let make ?sharedWithPrefix =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ~workloadId ->
            fun () -> { sharedWithPrefix; nextToken; maxResults; workloadId }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("SharedWithPrefix",
          (Option.map x.sharedWithPrefix ~f:SharedWithPrefix.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:ListWorkloadSharesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListWorkloadSharesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let sharedWithPrefix =
        (Option.map ~f:SharedWithPrefix.of_xml)
          (Xml.child xml_arg0 "SharedWithPrefix") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?maxResults ?nextToken ?sharedWithPrefix ~workloadId ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" ListWorkloadSharesMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let sharedWithPrefix =
        field_map json "SharedWithPrefix" SharedWithPrefix.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?maxResults ?nextToken ?sharedWithPrefix ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListTagsForResourceOutput =
  struct
    type nonrec t = {
      tags: TagMap.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "Tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end
module ListTagsForResourceInput =
  struct
    type nonrec t = {
      workloadArn: WorkloadArn.t }
    let context_ = "ListTagsForResourceInput"
    let make ~workloadArn = fun () -> { workloadArn }
    let to_value x =
      structure_to_value
        [("WorkloadArn", (Some (WorkloadArn.to_value x.workloadArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workloadArn =
        WorkloadArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadArn") in
      make ~workloadArn ()
    let of_json json =
      let workloadArn = field_map_exn json "WorkloadArn" WorkloadArn.of_json in
      make ~workloadArn ()
    let to_json v = composed_to_json to_value v
  end
module ListShareInvitationsOutput =
  struct
    type nonrec t =
      {
      shareInvitationSummaries: ShareInvitationSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?shareInvitationSummaries =
      fun ?nextToken -> fun () -> { shareInvitationSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ShareInvitationSummaries",
           (Option.map x.shareInvitationSummaries
              ~f:ShareInvitationSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let shareInvitationSummaries =
        (Option.map ~f:ShareInvitationSummaries.of_xml)
          (Xml.child xml_arg0 "ShareInvitationSummaries") in
      make ?nextToken ?shareInvitationSummaries ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let shareInvitationSummaries =
        field_map json "ShareInvitationSummaries"
          ShareInvitationSummaries.of_json in
      make ?nextToken ?shareInvitationSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListShareInvitationsInput =
  struct
    type nonrec t =
      {
      workloadNamePrefix: WorkloadNamePrefix.t option ;
      lensNamePrefix: LensNamePrefix.t option ;
      shareResourceType: ShareResourceType.t option ;
      nextToken: NextToken.t option ;
      maxResults: ListShareInvitationsMaxResults.t option }
    let make ?workloadNamePrefix =
      fun ?lensNamePrefix ->
        fun ?shareResourceType ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                {
                  workloadNamePrefix;
                  lensNamePrefix;
                  shareResourceType;
                  nextToken;
                  maxResults
                }
    let to_value x =
      structure_to_value
        [("WorkloadNamePrefix",
           (Option.map x.workloadNamePrefix ~f:WorkloadNamePrefix.to_value));
        ("LensNamePrefix",
          (Option.map x.lensNamePrefix ~f:LensNamePrefix.to_value));
        ("ShareResourceType",
          (Option.map x.shareResourceType ~f:ShareResourceType.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:ListShareInvitationsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListShareInvitationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let shareResourceType =
        (Option.map ~f:ShareResourceType.of_xml)
          (Xml.child xml_arg0 "ShareResourceType") in
      let lensNamePrefix =
        (Option.map ~f:LensNamePrefix.of_xml)
          (Xml.child xml_arg0 "LensNamePrefix") in
      let workloadNamePrefix =
        (Option.map ~f:WorkloadNamePrefix.of_xml)
          (Xml.child xml_arg0 "WorkloadNamePrefix") in
      make ?maxResults ?nextToken ?shareResourceType ?lensNamePrefix
        ?workloadNamePrefix ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" ListShareInvitationsMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let shareResourceType =
        field_map json "ShareResourceType" ShareResourceType.of_json in
      let lensNamePrefix =
        field_map json "LensNamePrefix" LensNamePrefix.of_json in
      let workloadNamePrefix =
        field_map json "WorkloadNamePrefix" WorkloadNamePrefix.of_json in
      make ?maxResults ?nextToken ?shareResourceType ?lensNamePrefix
        ?workloadNamePrefix ()
    let to_json v = composed_to_json to_value v
  end
module ListNotificationsOutput =
  struct
    type nonrec t =
      {
      notificationSummaries: NotificationSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?notificationSummaries =
      fun ?nextToken -> fun () -> { notificationSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NotificationSummaries",
           (Option.map x.notificationSummaries
              ~f:NotificationSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let notificationSummaries =
        (Option.map ~f:NotificationSummaries.of_xml)
          (Xml.child xml_arg0 "NotificationSummaries") in
      make ?nextToken ?notificationSummaries ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let notificationSummaries =
        field_map json "NotificationSummaries" NotificationSummaries.of_json in
      make ?nextToken ?notificationSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListNotificationsInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      nextToken: NextToken.t option ;
      maxResults: ListNotificationsMaxResults.t option }
    let make ?workloadId =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { workloadId; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:ListNotificationsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListNotificationsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?maxResults ?nextToken ?workloadId ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" ListNotificationsMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?maxResults ?nextToken ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListMilestonesOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestoneSummaries: MilestoneSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestoneSummaries ->
        fun ?nextToken ->
          fun () -> { workloadId; milestoneSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("MilestoneSummaries",
          (Option.map x.milestoneSummaries ~f:MilestoneSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let milestoneSummaries =
        (Option.map ~f:MilestoneSummaries.of_xml)
          (Xml.child xml_arg0 "MilestoneSummaries") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?nextToken ?milestoneSummaries ?workloadId ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let milestoneSummaries =
        field_map json "MilestoneSummaries" MilestoneSummaries.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?nextToken ?milestoneSummaries ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListMilestonesInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      nextToken: NextToken.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListMilestonesInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~workloadId -> fun () -> { nextToken; maxResults; workloadId }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?maxResults ?nextToken ~workloadId ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?maxResults ?nextToken ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListLensesOutput =
  struct
    type nonrec t =
      {
      lensSummaries: LensSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?lensSummaries =
      fun ?nextToken -> fun () -> { lensSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("LensSummaries",
           (Option.map x.lensSummaries ~f:LensSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let lensSummaries =
        (Option.map ~f:LensSummaries.of_xml)
          (Xml.child xml_arg0 "LensSummaries") in
      make ?nextToken ?lensSummaries ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let lensSummaries =
        field_map json "LensSummaries" LensSummaries.of_json in
      make ?nextToken ?lensSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListLensesInput =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option ;
      maxResults: MaxResults.t option ;
      lensType: LensType.t option ;
      lensStatus: LensStatusType.t option ;
      lensName: LensName.t option }
    let make ?nextToken =
      fun ?maxResults ->
        fun ?lensType ->
          fun ?lensStatus ->
            fun ?lensName ->
              fun () ->
                { nextToken; maxResults; lensType; lensStatus; lensName }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("LensType", (Option.map x.lensType ~f:LensType.to_value));
        ("LensStatus", (Option.map x.lensStatus ~f:LensStatusType.to_value));
        ("LensName", (Option.map x.lensName ~f:LensName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensName =
        (Option.map ~f:LensName.of_xml) (Xml.child xml_arg0 "LensName") in
      let lensStatus =
        (Option.map ~f:LensStatusType.of_xml)
          (Xml.child xml_arg0 "LensStatus") in
      let lensType =
        (Option.map ~f:LensType.of_xml) (Xml.child xml_arg0 "LensType") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?lensName ?lensStatus ?lensType ?maxResults ?nextToken ()
    let of_json json =
      let lensName = field_map json "LensName" LensName.of_json in
      let lensStatus = field_map json "LensStatus" LensStatusType.of_json in
      let lensType = field_map json "LensType" LensType.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      make ?lensName ?lensStatus ?lensType ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module ListLensSharesOutput =
  struct
    type nonrec t =
      {
      lensShareSummaries: LensShareSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?lensShareSummaries =
      fun ?nextToken -> fun () -> { lensShareSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("LensShareSummaries",
           (Option.map x.lensShareSummaries ~f:LensShareSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let lensShareSummaries =
        (Option.map ~f:LensShareSummaries.of_xml)
          (Xml.child xml_arg0 "LensShareSummaries") in
      make ?nextToken ?lensShareSummaries ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let lensShareSummaries =
        field_map json "LensShareSummaries" LensShareSummaries.of_json in
      make ?nextToken ?lensShareSummaries ()
    let to_json v = composed_to_json to_value v
  end
module ListLensSharesInput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t ;
      sharedWithPrefix: SharedWithPrefix.t option ;
      nextToken: NextToken.t option ;
      maxResults: ListWorkloadSharesMaxResults.t option }
    let context_ = "ListLensSharesInput"
    let make ?sharedWithPrefix =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ~lensAlias ->
            fun () -> { sharedWithPrefix; nextToken; maxResults; lensAlias }
    let to_value x =
      structure_to_value
        [("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("SharedWithPrefix",
          (Option.map x.sharedWithPrefix ~f:SharedWithPrefix.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:ListWorkloadSharesMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListWorkloadSharesMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let sharedWithPrefix =
        (Option.map ~f:SharedWithPrefix.of_xml)
          (Xml.child xml_arg0 "SharedWithPrefix") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      make ?maxResults ?nextToken ?sharedWithPrefix ~lensAlias ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" ListWorkloadSharesMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let sharedWithPrefix =
        field_map json "SharedWithPrefix" SharedWithPrefix.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      make ?maxResults ?nextToken ?sharedWithPrefix ~lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module ListLensReviewsOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestoneNumber: MilestoneNumber.t option ;
      lensReviewSummaries: LensReviewSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestoneNumber ->
        fun ?lensReviewSummaries ->
          fun ?nextToken ->
            fun () ->
              { workloadId; milestoneNumber; lensReviewSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("LensReviewSummaries",
          (Option.map x.lensReviewSummaries ~f:LensReviewSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let lensReviewSummaries =
        (Option.map ~f:LensReviewSummaries.of_xml)
          (Xml.child xml_arg0 "LensReviewSummaries") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?nextToken ?lensReviewSummaries ?milestoneNumber ?workloadId ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let lensReviewSummaries =
        field_map json "LensReviewSummaries" LensReviewSummaries.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?nextToken ?lensReviewSummaries ?milestoneNumber ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListLensReviewsInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      milestoneNumber: MilestoneNumber.t option ;
      nextToken: NextToken.t option ;
      maxResults: MaxResults.t option }
    let context_ = "ListLensReviewsInput"
    let make ?milestoneNumber =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ~workloadId ->
            fun () -> { milestoneNumber; nextToken; maxResults; workloadId }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?maxResults ?nextToken ?milestoneNumber ~workloadId ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?maxResults ?nextToken ?milestoneNumber ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListLensReviewImprovementsOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestoneNumber: MilestoneNumber.t option ;
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      improvementSummaries: ImprovementSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestoneNumber ->
        fun ?lensAlias ->
          fun ?lensArn ->
            fun ?improvementSummaries ->
              fun ?nextToken ->
                fun () ->
                  {
                    workloadId;
                    milestoneNumber;
                    lensAlias;
                    lensArn;
                    improvementSummaries;
                    nextToken
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("ImprovementSummaries",
          (Option.map x.improvementSummaries ~f:ImprovementSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let improvementSummaries =
        (Option.map ~f:ImprovementSummaries.of_xml)
          (Xml.child xml_arg0 "ImprovementSummaries") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?nextToken ?improvementSummaries ?lensArn ?lensAlias
        ?milestoneNumber ?workloadId ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let improvementSummaries =
        field_map json "ImprovementSummaries" ImprovementSummaries.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?nextToken ?improvementSummaries ?lensArn ?lensAlias
        ?milestoneNumber ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListLensReviewImprovementsInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      lensAlias: LensAlias.t ;
      pillarId: PillarId.t option ;
      milestoneNumber: MilestoneNumber.t option ;
      nextToken: NextToken.t option ;
      maxResults: ListLensReviewImprovementsMaxResults.t option }
    let context_ = "ListLensReviewImprovementsInput"
    let make ?pillarId =
      fun ?milestoneNumber ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ~workloadId ->
              fun ~lensAlias ->
                fun () ->
                  {
                    pillarId;
                    milestoneNumber;
                    nextToken;
                    maxResults;
                    workloadId;
                    lensAlias
                  }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("PillarId", (Option.map x.pillarId ~f:PillarId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults
             ~f:ListLensReviewImprovementsMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListLensReviewImprovementsMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let pillarId =
        (Option.map ~f:PillarId.of_xml) (Xml.child xml_arg0 "PillarId") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?maxResults ?nextToken ?milestoneNumber ?pillarId ~lensAlias
        ~workloadId ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults"
          ListLensReviewImprovementsMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let pillarId = field_map json "PillarId" PillarId.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?maxResults ?nextToken ?milestoneNumber ?pillarId ~lensAlias
        ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListAnswersOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestoneNumber: MilestoneNumber.t option ;
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      answerSummaries: AnswerSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestoneNumber ->
        fun ?lensAlias ->
          fun ?lensArn ->
            fun ?answerSummaries ->
              fun ?nextToken ->
                fun () ->
                  {
                    workloadId;
                    milestoneNumber;
                    lensAlias;
                    lensArn;
                    answerSummaries;
                    nextToken
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("AnswerSummaries",
          (Option.map x.answerSummaries ~f:AnswerSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let answerSummaries =
        (Option.map ~f:AnswerSummaries.of_xml)
          (Xml.child xml_arg0 "AnswerSummaries") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?nextToken ?answerSummaries ?lensArn ?lensAlias ?milestoneNumber
        ?workloadId ()
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let answerSummaries =
        field_map json "AnswerSummaries" AnswerSummaries.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?nextToken ?answerSummaries ?lensArn ?lensAlias ?milestoneNumber
        ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ListAnswersInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      lensAlias: LensAlias.t ;
      pillarId: PillarId.t option ;
      milestoneNumber: MilestoneNumber.t option ;
      nextToken: NextToken.t option ;
      maxResults: ListAnswersMaxResults.t option }
    let context_ = "ListAnswersInput"
    let make ?pillarId =
      fun ?milestoneNumber ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ~workloadId ->
              fun ~lensAlias ->
                fun () ->
                  {
                    pillarId;
                    milestoneNumber;
                    nextToken;
                    maxResults;
                    workloadId;
                    lensAlias
                  }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("PillarId", (Option.map x.pillarId ~f:PillarId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:ListAnswersMaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListAnswersMaxResults.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let pillarId =
        (Option.map ~f:PillarId.of_xml) (Xml.child xml_arg0 "PillarId") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?maxResults ?nextToken ?milestoneNumber ?pillarId ~lensAlias
        ~workloadId ()
    let of_json json =
      let maxResults =
        field_map json "MaxResults" ListAnswersMaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let pillarId = field_map json "PillarId" PillarId.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?maxResults ?nextToken ?milestoneNumber ?pillarId ~lensAlias
        ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ImportLensOutput =
  struct
    type nonrec t =
      {
      lensArn: LensArn.t option ;
      status: ImportLensStatus.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?lensArn = fun ?status -> fun () -> { lensArn; status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("Status", (Option.map x.status ~f:ImportLensStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:ImportLensStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      make ?status ?lensArn ()
    let of_json json =
      let status = field_map json "Status" ImportLensStatus.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      make ?status ?lensArn ()
    let to_json v = composed_to_json to_value v
  end
module ImportLensInput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t option ;
      jSONString: LensJSON.t ;
      clientRequestToken: ClientRequestToken.t ;
      tags: TagMap.t option }
    let context_ = "ImportLensInput"
    let make ?lensAlias =
      fun ?tags ->
        fun ~jSONString ->
          fun ~clientRequestToken ->
            fun () -> { lensAlias; tags; jSONString; clientRequestToken }
    let to_value x =
      structure_to_value
        [("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("JSONString", (Some (LensJSON.to_value x.jSONString)));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let jSONString =
        LensJSON.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "JSONString") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      make ?tags ~clientRequestToken ~jSONString ?lensAlias ()
    let of_json json =
      let tags = field_map json "Tags" TagMap.of_json in
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let jSONString = field_map_exn json "JSONString" LensJSON.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      make ?tags ~clientRequestToken ~jSONString ?lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module GetWorkloadOutput =
  struct
    type nonrec t = {
      workload: Workload.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workload = fun () -> { workload }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Workload", (Option.map x.workload ~f:Workload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workload =
        (Option.map ~f:Workload.of_xml) (Xml.child xml_arg0 "Workload") in
      make ?workload ()
    let of_json json =
      let workload = field_map json "Workload" Workload.of_json in
      make ?workload ()
    let to_json v = composed_to_json to_value v
  end
module GetWorkloadInput =
  struct
    type nonrec t = {
      workloadId: WorkloadId.t }
    let context_ = "GetWorkloadInput"
    let make ~workloadId = fun () -> { workloadId }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ~workloadId ()
    let of_json json =
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module GetMilestoneOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestone: Milestone.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestone -> fun () -> { workloadId; milestone }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("Milestone", (Option.map x.milestone ~f:Milestone.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let milestone =
        (Option.map ~f:Milestone.of_xml) (Xml.child xml_arg0 "Milestone") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?milestone ?workloadId ()
    let of_json json =
      let milestone = field_map json "Milestone" Milestone.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?milestone ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module GetMilestoneInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      milestoneNumber: MilestoneNumber.t }
    let context_ = "GetMilestoneInput"
    let make ~workloadId =
      fun ~milestoneNumber -> fun () -> { workloadId; milestoneNumber }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("MilestoneNumber",
          (Some (MilestoneNumber.to_value x.milestoneNumber)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let milestoneNumber =
        MilestoneNumber.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MilestoneNumber") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ~milestoneNumber ~workloadId ()
    let of_json json =
      let milestoneNumber =
        field_map_exn json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ~milestoneNumber ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module GetLensVersionDifferenceOutput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      baseLensVersion: LensVersion.t option ;
      targetLensVersion: LensVersion.t option ;
      latestLensVersion: LensVersion.t option ;
      versionDifferences: VersionDifferences.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?lensAlias =
      fun ?lensArn ->
        fun ?baseLensVersion ->
          fun ?targetLensVersion ->
            fun ?latestLensVersion ->
              fun ?versionDifferences ->
                fun () ->
                  {
                    lensAlias;
                    lensArn;
                    baseLensVersion;
                    targetLensVersion;
                    latestLensVersion;
                    versionDifferences
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("BaseLensVersion",
          (Option.map x.baseLensVersion ~f:LensVersion.to_value));
        ("TargetLensVersion",
          (Option.map x.targetLensVersion ~f:LensVersion.to_value));
        ("LatestLensVersion",
          (Option.map x.latestLensVersion ~f:LensVersion.to_value));
        ("VersionDifferences",
          (Option.map x.versionDifferences ~f:VersionDifferences.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let versionDifferences =
        (Option.map ~f:VersionDifferences.of_xml)
          (Xml.child xml_arg0 "VersionDifferences") in
      let latestLensVersion =
        (Option.map ~f:LensVersion.of_xml)
          (Xml.child xml_arg0 "LatestLensVersion") in
      let targetLensVersion =
        (Option.map ~f:LensVersion.of_xml)
          (Xml.child xml_arg0 "TargetLensVersion") in
      let baseLensVersion =
        (Option.map ~f:LensVersion.of_xml)
          (Xml.child xml_arg0 "BaseLensVersion") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      make ?versionDifferences ?latestLensVersion ?targetLensVersion
        ?baseLensVersion ?lensArn ?lensAlias ()
    let of_json json =
      let versionDifferences =
        field_map json "VersionDifferences" VersionDifferences.of_json in
      let latestLensVersion =
        field_map json "LatestLensVersion" LensVersion.of_json in
      let targetLensVersion =
        field_map json "TargetLensVersion" LensVersion.of_json in
      let baseLensVersion =
        field_map json "BaseLensVersion" LensVersion.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      make ?versionDifferences ?latestLensVersion ?targetLensVersion
        ?baseLensVersion ?lensArn ?lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module GetLensVersionDifferenceInput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t ;
      baseLensVersion: LensVersion.t option ;
      targetLensVersion: LensVersion.t option }
    let context_ = "GetLensVersionDifferenceInput"
    let make ?baseLensVersion =
      fun ?targetLensVersion ->
        fun ~lensAlias ->
          fun () -> { baseLensVersion; targetLensVersion; lensAlias }
    let to_value x =
      structure_to_value
        [("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("BaseLensVersion",
          (Option.map x.baseLensVersion ~f:LensVersion.to_value));
        ("TargetLensVersion",
          (Option.map x.targetLensVersion ~f:LensVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetLensVersion =
        (Option.map ~f:LensVersion.of_xml)
          (Xml.child xml_arg0 "TargetLensVersion") in
      let baseLensVersion =
        (Option.map ~f:LensVersion.of_xml)
          (Xml.child xml_arg0 "BaseLensVersion") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      make ?targetLensVersion ?baseLensVersion ~lensAlias ()
    let of_json json =
      let targetLensVersion =
        field_map json "TargetLensVersion" LensVersion.of_json in
      let baseLensVersion =
        field_map json "BaseLensVersion" LensVersion.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      make ?targetLensVersion ?baseLensVersion ~lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module GetLensReviewReportOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestoneNumber: MilestoneNumber.t option ;
      lensReviewReport: LensReviewReport.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestoneNumber ->
        fun ?lensReviewReport ->
          fun () -> { workloadId; milestoneNumber; lensReviewReport }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("LensReviewReport",
          (Option.map x.lensReviewReport ~f:LensReviewReport.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensReviewReport =
        (Option.map ~f:LensReviewReport.of_xml)
          (Xml.child xml_arg0 "LensReviewReport") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?lensReviewReport ?milestoneNumber ?workloadId ()
    let of_json json =
      let lensReviewReport =
        field_map json "LensReviewReport" LensReviewReport.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?lensReviewReport ?milestoneNumber ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module GetLensReviewReportInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      lensAlias: LensAlias.t ;
      milestoneNumber: MilestoneNumber.t option }
    let context_ = "GetLensReviewReportInput"
    let make ?milestoneNumber =
      fun ~workloadId ->
        fun ~lensAlias ->
          fun () -> { milestoneNumber; workloadId; lensAlias }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?milestoneNumber ~lensAlias ~workloadId ()
    let of_json json =
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?milestoneNumber ~lensAlias ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module GetLensReviewOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestoneNumber: MilestoneNumber.t option ;
      lensReview: LensReview.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestoneNumber ->
        fun ?lensReview ->
          fun () -> { workloadId; milestoneNumber; lensReview }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("LensReview", (Option.map x.lensReview ~f:LensReview.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensReview =
        (Option.map ~f:LensReview.of_xml) (Xml.child xml_arg0 "LensReview") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?lensReview ?milestoneNumber ?workloadId ()
    let of_json json =
      let lensReview = field_map json "LensReview" LensReview.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?lensReview ?milestoneNumber ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module GetLensReviewInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      lensAlias: LensAlias.t ;
      milestoneNumber: MilestoneNumber.t option }
    let context_ = "GetLensReviewInput"
    let make ?milestoneNumber =
      fun ~workloadId ->
        fun ~lensAlias ->
          fun () -> { milestoneNumber; workloadId; lensAlias }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?milestoneNumber ~lensAlias ~workloadId ()
    let of_json json =
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?milestoneNumber ~lensAlias ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module GetLensOutput =
  struct
    type nonrec t = {
      lens: Lens.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?lens = fun () -> { lens }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Lens", (Option.map x.lens ~f:Lens.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lens = (Option.map ~f:Lens.of_xml) (Xml.child xml_arg0 "Lens") in
      make ?lens ()
    let of_json json =
      let lens = field_map json "Lens" Lens.of_json in make ?lens ()
    let to_json v = composed_to_json to_value v
  end
module GetLensInput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t ;
      lensVersion: LensVersion.t option }
    let context_ = "GetLensInput"
    let make ?lensVersion =
      fun ~lensAlias -> fun () -> { lensVersion; lensAlias }
    let to_value x =
      structure_to_value
        [("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("LensVersion", (Option.map x.lensVersion ~f:LensVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensVersion =
        (Option.map ~f:LensVersion.of_xml) (Xml.child xml_arg0 "LensVersion") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      make ?lensVersion ~lensAlias ()
    let of_json json =
      let lensVersion = field_map json "LensVersion" LensVersion.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      make ?lensVersion ~lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module GetAnswerOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestoneNumber: MilestoneNumber.t option ;
      lensAlias: LensAlias.t option ;
      lensArn: LensArn.t option ;
      answer: Answer.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestoneNumber ->
        fun ?lensAlias ->
          fun ?lensArn ->
            fun ?answer ->
              fun () ->
                { workloadId; milestoneNumber; lensAlias; lensArn; answer }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value));
        ("LensAlias", (Option.map x.lensAlias ~f:LensAlias.to_value));
        ("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("Answer", (Option.map x.answer ~f:Answer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let answer =
        (Option.map ~f:Answer.of_xml) (Xml.child xml_arg0 "Answer") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      let lensAlias =
        (Option.map ~f:LensAlias.of_xml) (Xml.child xml_arg0 "LensAlias") in
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?answer ?lensArn ?lensAlias ?milestoneNumber ?workloadId ()
    let of_json json =
      let answer = field_map json "Answer" Answer.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      let lensAlias = field_map json "LensAlias" LensAlias.of_json in
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?answer ?lensArn ?lensAlias ?milestoneNumber ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module GetAnswerInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      lensAlias: LensAlias.t ;
      questionId: QuestionId.t ;
      milestoneNumber: MilestoneNumber.t option }
    let context_ = "GetAnswerInput"
    let make ?milestoneNumber =
      fun ~workloadId ->
        fun ~lensAlias ->
          fun ~questionId ->
            fun () -> { milestoneNumber; workloadId; lensAlias; questionId }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("QuestionId", (Some (QuestionId.to_value x.questionId)));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let questionId =
        QuestionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "QuestionId") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ?milestoneNumber ~questionId ~lensAlias ~workloadId ()
    let of_json json =
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let questionId = field_map_exn json "QuestionId" QuestionId.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ?milestoneNumber ~questionId ~lensAlias ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module ExportLensOutput =
  struct
    type nonrec t = {
      lensJSON: LensJSON.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?lensJSON = fun () -> { lensJSON }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("LensJSON", (Option.map x.lensJSON ~f:LensJSON.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensJSON =
        (Option.map ~f:LensJSON.of_xml) (Xml.child xml_arg0 "LensJSON") in
      make ?lensJSON ()
    let of_json json =
      let lensJSON = field_map json "LensJSON" LensJSON.of_json in
      make ?lensJSON ()
    let to_json v = composed_to_json to_value v
  end
module ExportLensInput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t ;
      lensVersion: LensVersion.t option }
    let context_ = "ExportLensInput"
    let make ?lensVersion =
      fun ~lensAlias -> fun () -> { lensVersion; lensAlias }
    let to_value x =
      structure_to_value
        [("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("LensVersion", (Option.map x.lensVersion ~f:LensVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensVersion =
        (Option.map ~f:LensVersion.of_xml) (Xml.child xml_arg0 "LensVersion") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      make ?lensVersion ~lensAlias ()
    let of_json json =
      let lensVersion = field_map json "LensVersion" LensVersion.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      make ?lensVersion ~lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateLensesInput =
  struct
    type nonrec t = {
      workloadId: WorkloadId.t ;
      lensAliases: LensAliases.t }
    let context_ = "DisassociateLensesInput"
    let make ~workloadId =
      fun ~lensAliases -> fun () -> { workloadId; lensAliases }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAliases", (Some (LensAliases.to_value x.lensAliases)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensAliases =
        LensAliases.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAliases") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ~lensAliases ~workloadId ()
    let of_json json =
      let lensAliases = field_map_exn json "LensAliases" LensAliases.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ~lensAliases ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteWorkloadShareInput =
  struct
    type nonrec t =
      {
      shareId: ShareId.t ;
      workloadId: WorkloadId.t ;
      clientRequestToken: ClientRequestToken.t }
    let context_ = "DeleteWorkloadShareInput"
    let make ~shareId =
      fun ~workloadId ->
        fun ~clientRequestToken ->
          fun () -> { shareId; workloadId; clientRequestToken }
    let to_value x =
      structure_to_value
        [("ShareId", (Some (ShareId.to_value x.shareId)));
        ("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      let shareId =
        ShareId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ShareId") in
      make ~clientRequestToken ~workloadId ~shareId ()
    let of_json json =
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      let shareId = field_map_exn json "ShareId" ShareId.of_json in
      make ~clientRequestToken ~workloadId ~shareId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteWorkloadInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      clientRequestToken: ClientRequestToken.t }
    let context_ = "DeleteWorkloadInput"
    let make ~workloadId =
      fun ~clientRequestToken -> fun () -> { workloadId; clientRequestToken }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ~clientRequestToken ~workloadId ()
    let of_json json =
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ~clientRequestToken ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLensShareInput =
  struct
    type nonrec t =
      {
      shareId: ShareId.t ;
      lensAlias: LensAlias.t ;
      clientRequestToken: ClientRequestToken.t }
    let context_ = "DeleteLensShareInput"
    let make ~shareId =
      fun ~lensAlias ->
        fun ~clientRequestToken ->
          fun () -> { shareId; lensAlias; clientRequestToken }
    let to_value x =
      structure_to_value
        [("ShareId", (Some (ShareId.to_value x.shareId)));
        ("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      let shareId =
        ShareId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ShareId") in
      make ~clientRequestToken ~lensAlias ~shareId ()
    let of_json json =
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      let shareId = field_map_exn json "ShareId" ShareId.of_json in
      make ~clientRequestToken ~lensAlias ~shareId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteLensInput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t ;
      clientRequestToken: ClientRequestToken.t ;
      lensStatus: LensStatusType.t }
    let context_ = "DeleteLensInput"
    let make ~lensAlias =
      fun ~clientRequestToken ->
        fun ~lensStatus ->
          fun () -> { lensAlias; clientRequestToken; lensStatus }
    let to_value x =
      structure_to_value
        [("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)));
        ("LensStatus", (Some (LensStatusType.to_value x.lensStatus)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensStatus =
        LensStatusType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensStatus") in
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      make ~lensStatus ~clientRequestToken ~lensAlias ()
    let of_json json =
      let lensStatus = field_map_exn json "LensStatus" LensStatusType.of_json in
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      make ~lensStatus ~clientRequestToken ~lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module CreateWorkloadShareOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      shareId: ShareId.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId = fun ?shareId -> fun () -> { workloadId; shareId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("ShareId", (Option.map x.shareId ~f:ShareId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareId =
        (Option.map ~f:ShareId.of_xml) (Xml.child xml_arg0 "ShareId") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?shareId ?workloadId ()
    let of_json json =
      let shareId = field_map json "ShareId" ShareId.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?shareId ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module CreateWorkloadShareInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      sharedWith: SharedWith.t ;
      permissionType: PermissionType.t ;
      clientRequestToken: ClientRequestToken.t }
    let context_ = "CreateWorkloadShareInput"
    let make ~workloadId =
      fun ~sharedWith ->
        fun ~permissionType ->
          fun ~clientRequestToken ->
            fun () ->
              { workloadId; sharedWith; permissionType; clientRequestToken }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("SharedWith", (Some (SharedWith.to_value x.sharedWith)));
        ("PermissionType", (Some (PermissionType.to_value x.permissionType)));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let permissionType =
        PermissionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PermissionType") in
      let sharedWith =
        SharedWith.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SharedWith") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ~clientRequestToken ~permissionType ~sharedWith ~workloadId ()
    let of_json json =
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let permissionType =
        field_map_exn json "PermissionType" PermissionType.of_json in
      let sharedWith = field_map_exn json "SharedWith" SharedWith.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ~clientRequestToken ~permissionType ~sharedWith ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module CreateWorkloadOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      workloadArn: WorkloadArn.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?workloadArn -> fun () -> { workloadId; workloadArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("WorkloadArn", (Option.map x.workloadArn ~f:WorkloadArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workloadArn =
        (Option.map ~f:WorkloadArn.of_xml) (Xml.child xml_arg0 "WorkloadArn") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?workloadArn ?workloadId ()
    let of_json json =
      let workloadArn = field_map json "WorkloadArn" WorkloadArn.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?workloadArn ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module CreateWorkloadInput =
  struct
    type nonrec t =
      {
      workloadName: WorkloadName.t ;
      description: WorkloadDescription.t ;
      environment: WorkloadEnvironment.t ;
      accountIds: WorkloadAccountIds.t option ;
      awsRegions: WorkloadAwsRegions.t option ;
      nonAwsRegions: WorkloadNonAwsRegions.t option ;
      pillarPriorities: WorkloadPillarPriorities.t option ;
      architecturalDesign: WorkloadArchitecturalDesign.t option ;
      reviewOwner: WorkloadReviewOwner.t ;
      industryType: WorkloadIndustryType.t option ;
      industry: WorkloadIndustry.t option ;
      lenses: WorkloadLenses.t ;
      notes: Notes.t option ;
      clientRequestToken: ClientRequestToken.t ;
      tags: TagMap.t option }
    let context_ = "CreateWorkloadInput"
    let make ?accountIds =
      fun ?awsRegions ->
        fun ?nonAwsRegions ->
          fun ?pillarPriorities ->
            fun ?architecturalDesign ->
              fun ?industryType ->
                fun ?industry ->
                  fun ?notes ->
                    fun ?tags ->
                      fun ~workloadName ->
                        fun ~description ->
                          fun ~environment ->
                            fun ~reviewOwner ->
                              fun ~lenses ->
                                fun ~clientRequestToken ->
                                  fun () ->
                                    {
                                      accountIds;
                                      awsRegions;
                                      nonAwsRegions;
                                      pillarPriorities;
                                      architecturalDesign;
                                      industryType;
                                      industry;
                                      notes;
                                      tags;
                                      workloadName;
                                      description;
                                      environment;
                                      reviewOwner;
                                      lenses;
                                      clientRequestToken
                                    }
    let to_value x =
      structure_to_value
        [("WorkloadName", (Some (WorkloadName.to_value x.workloadName)));
        ("Description", (Some (WorkloadDescription.to_value x.description)));
        ("Environment", (Some (WorkloadEnvironment.to_value x.environment)));
        ("AccountIds",
          (Option.map x.accountIds ~f:WorkloadAccountIds.to_value));
        ("AwsRegions",
          (Option.map x.awsRegions ~f:WorkloadAwsRegions.to_value));
        ("NonAwsRegions",
          (Option.map x.nonAwsRegions ~f:WorkloadNonAwsRegions.to_value));
        ("PillarPriorities",
          (Option.map x.pillarPriorities ~f:WorkloadPillarPriorities.to_value));
        ("ArchitecturalDesign",
          (Option.map x.architecturalDesign
             ~f:WorkloadArchitecturalDesign.to_value));
        ("ReviewOwner", (Some (WorkloadReviewOwner.to_value x.reviewOwner)));
        ("IndustryType",
          (Option.map x.industryType ~f:WorkloadIndustryType.to_value));
        ("Industry", (Option.map x.industry ~f:WorkloadIndustry.to_value));
        ("Lenses", (Some (WorkloadLenses.to_value x.lenses)));
        ("Notes", (Option.map x.notes ~f:Notes.to_value));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let notes = (Option.map ~f:Notes.of_xml) (Xml.child xml_arg0 "Notes") in
      let lenses =
        WorkloadLenses.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Lenses") in
      let industry =
        (Option.map ~f:WorkloadIndustry.of_xml)
          (Xml.child xml_arg0 "Industry") in
      let industryType =
        (Option.map ~f:WorkloadIndustryType.of_xml)
          (Xml.child xml_arg0 "IndustryType") in
      let reviewOwner =
        WorkloadReviewOwner.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ReviewOwner") in
      let architecturalDesign =
        (Option.map ~f:WorkloadArchitecturalDesign.of_xml)
          (Xml.child xml_arg0 "ArchitecturalDesign") in
      let pillarPriorities =
        (Option.map ~f:WorkloadPillarPriorities.of_xml)
          (Xml.child xml_arg0 "PillarPriorities") in
      let nonAwsRegions =
        (Option.map ~f:WorkloadNonAwsRegions.of_xml)
          (Xml.child xml_arg0 "NonAwsRegions") in
      let awsRegions =
        (Option.map ~f:WorkloadAwsRegions.of_xml)
          (Xml.child xml_arg0 "AwsRegions") in
      let accountIds =
        (Option.map ~f:WorkloadAccountIds.of_xml)
          (Xml.child xml_arg0 "AccountIds") in
      let environment =
        WorkloadEnvironment.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Environment") in
      let description =
        WorkloadDescription.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Description") in
      let workloadName =
        WorkloadName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadName") in
      make ?tags ~clientRequestToken ?notes ~lenses ?industry ?industryType
        ~reviewOwner ?architecturalDesign ?pillarPriorities ?nonAwsRegions
        ?awsRegions ?accountIds ~environment ~description ~workloadName ()
    let of_json json =
      let tags = field_map json "Tags" TagMap.of_json in
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let notes = field_map json "Notes" Notes.of_json in
      let lenses = field_map_exn json "Lenses" WorkloadLenses.of_json in
      let industry = field_map json "Industry" WorkloadIndustry.of_json in
      let industryType =
        field_map json "IndustryType" WorkloadIndustryType.of_json in
      let reviewOwner =
        field_map_exn json "ReviewOwner" WorkloadReviewOwner.of_json in
      let architecturalDesign =
        field_map json "ArchitecturalDesign"
          WorkloadArchitecturalDesign.of_json in
      let pillarPriorities =
        field_map json "PillarPriorities" WorkloadPillarPriorities.of_json in
      let nonAwsRegions =
        field_map json "NonAwsRegions" WorkloadNonAwsRegions.of_json in
      let awsRegions = field_map json "AwsRegions" WorkloadAwsRegions.of_json in
      let accountIds = field_map json "AccountIds" WorkloadAccountIds.of_json in
      let environment =
        field_map_exn json "Environment" WorkloadEnvironment.of_json in
      let description =
        field_map_exn json "Description" WorkloadDescription.of_json in
      let workloadName =
        field_map_exn json "WorkloadName" WorkloadName.of_json in
      make ?tags ~clientRequestToken ?notes ~lenses ?industry ?industryType
        ~reviewOwner ?architecturalDesign ?pillarPriorities ?nonAwsRegions
        ?awsRegions ?accountIds ~environment ~description ~workloadName ()
    let to_json v = composed_to_json to_value v
  end
module CreateMilestoneOutput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t option ;
      milestoneNumber: MilestoneNumber.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workloadId =
      fun ?milestoneNumber -> fun () -> { workloadId; milestoneNumber }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("WorkloadId", (Option.map x.workloadId ~f:WorkloadId.to_value));
        ("MilestoneNumber",
          (Option.map x.milestoneNumber ~f:MilestoneNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let milestoneNumber =
        (Option.map ~f:MilestoneNumber.of_xml)
          (Xml.child xml_arg0 "MilestoneNumber") in
      let workloadId =
        (Option.map ~f:WorkloadId.of_xml) (Xml.child xml_arg0 "WorkloadId") in
      make ?milestoneNumber ?workloadId ()
    let of_json json =
      let milestoneNumber =
        field_map json "MilestoneNumber" MilestoneNumber.of_json in
      let workloadId = field_map json "WorkloadId" WorkloadId.of_json in
      make ?milestoneNumber ?workloadId ()
    let to_json v = composed_to_json to_value v
  end
module CreateMilestoneInput =
  struct
    type nonrec t =
      {
      workloadId: WorkloadId.t ;
      milestoneName: MilestoneName.t ;
      clientRequestToken: ClientRequestToken.t }
    let context_ = "CreateMilestoneInput"
    let make ~workloadId =
      fun ~milestoneName ->
        fun ~clientRequestToken ->
          fun () -> { workloadId; milestoneName; clientRequestToken }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("MilestoneName", (Some (MilestoneName.to_value x.milestoneName)));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let milestoneName =
        MilestoneName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MilestoneName") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ~clientRequestToken ~milestoneName ~workloadId ()
    let of_json json =
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let milestoneName =
        field_map_exn json "MilestoneName" MilestoneName.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ~clientRequestToken ~milestoneName ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
module CreateLensVersionOutput =
  struct
    type nonrec t =
      {
      lensArn: LensArn.t option ;
      lensVersion: LensVersion.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?lensArn =
      fun ?lensVersion -> fun () -> { lensArn; lensVersion }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("LensArn", (Option.map x.lensArn ~f:LensArn.to_value));
        ("LensVersion", (Option.map x.lensVersion ~f:LensVersion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensVersion =
        (Option.map ~f:LensVersion.of_xml) (Xml.child xml_arg0 "LensVersion") in
      let lensArn =
        (Option.map ~f:LensArn.of_xml) (Xml.child xml_arg0 "LensArn") in
      make ?lensVersion ?lensArn ()
    let of_json json =
      let lensVersion = field_map json "LensVersion" LensVersion.of_json in
      let lensArn = field_map json "LensArn" LensArn.of_json in
      make ?lensVersion ?lensArn ()
    let to_json v = composed_to_json to_value v
  end
module CreateLensVersionInput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t ;
      lensVersion: LensVersion.t ;
      isMajorVersion: IsMajorVersion.t option ;
      clientRequestToken: ClientRequestToken.t }
    let context_ = "CreateLensVersionInput"
    let make ?isMajorVersion =
      fun ~lensAlias ->
        fun ~lensVersion ->
          fun ~clientRequestToken ->
            fun () ->
              { isMajorVersion; lensAlias; lensVersion; clientRequestToken }
    let to_value x =
      structure_to_value
        [("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("LensVersion", (Some (LensVersion.to_value x.lensVersion)));
        ("IsMajorVersion",
          (Option.map x.isMajorVersion ~f:IsMajorVersion.to_value));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let isMajorVersion =
        (Option.map ~f:IsMajorVersion.of_xml)
          (Xml.child xml_arg0 "IsMajorVersion") in
      let lensVersion =
        LensVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensVersion") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      make ~clientRequestToken ?isMajorVersion ~lensVersion ~lensAlias ()
    let of_json json =
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let isMajorVersion =
        field_map json "IsMajorVersion" IsMajorVersion.of_json in
      let lensVersion = field_map_exn json "LensVersion" LensVersion.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      make ~clientRequestToken ?isMajorVersion ~lensVersion ~lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module CreateLensShareOutput =
  struct
    type nonrec t = {
      shareId: ShareId.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?shareId = fun () -> { shareId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ShareId", (Option.map x.shareId ~f:ShareId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let shareId =
        (Option.map ~f:ShareId.of_xml) (Xml.child xml_arg0 "ShareId") in
      make ?shareId ()
    let of_json json =
      let shareId = field_map json "ShareId" ShareId.of_json in
      make ?shareId ()
    let to_json v = composed_to_json to_value v
  end
module CreateLensShareInput =
  struct
    type nonrec t =
      {
      lensAlias: LensAlias.t ;
      sharedWith: SharedWith.t ;
      clientRequestToken: ClientRequestToken.t }
    let context_ = "CreateLensShareInput"
    let make ~lensAlias =
      fun ~sharedWith ->
        fun ~clientRequestToken ->
          fun () -> { lensAlias; sharedWith; clientRequestToken }
    let to_value x =
      structure_to_value
        [("LensAlias", (Some (LensAlias.to_value x.lensAlias)));
        ("SharedWith", (Some (SharedWith.to_value x.sharedWith)));
        ("ClientRequestToken",
          (Some (ClientRequestToken.to_value x.clientRequestToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientRequestToken =
        ClientRequestToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClientRequestToken") in
      let sharedWith =
        SharedWith.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SharedWith") in
      let lensAlias =
        LensAlias.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAlias") in
      make ~clientRequestToken ~sharedWith ~lensAlias ()
    let of_json json =
      let clientRequestToken =
        field_map_exn json "ClientRequestToken" ClientRequestToken.of_json in
      let sharedWith = field_map_exn json "SharedWith" SharedWith.of_json in
      let lensAlias = field_map_exn json "LensAlias" LensAlias.of_json in
      make ~clientRequestToken ~sharedWith ~lensAlias ()
    let to_json v = composed_to_json to_value v
  end
module AssociateLensesInput =
  struct
    type nonrec t = {
      workloadId: WorkloadId.t ;
      lensAliases: LensAliases.t }
    let context_ = "AssociateLensesInput"
    let make ~workloadId =
      fun ~lensAliases -> fun () -> { workloadId; lensAliases }
    let to_value x =
      structure_to_value
        [("WorkloadId", (Some (WorkloadId.to_value x.workloadId)));
        ("LensAliases", (Some (LensAliases.to_value x.lensAliases)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lensAliases =
        LensAliases.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LensAliases") in
      let workloadId =
        WorkloadId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "WorkloadId") in
      make ~lensAliases ~workloadId ()
    let of_json json =
      let lensAliases = field_map_exn json "LensAliases" LensAliases.of_json in
      let workloadId = field_map_exn json "WorkloadId" WorkloadId.of_json in
      make ~lensAliases ~workloadId ()
    let to_json v = composed_to_json to_value v
  end
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and homeDirectory =
         flag "home-directory" (optional string) ~doc:"STRING HomeDirectory"
       and homeDirectoryType =
         flag "home-directory-type" (optional json_arg)
           ~doc:"JSON HomeDirectoryType"
       and homeDirectoryMappings =
         flag "home-directory-mappings" (optional json_arg)
           ~doc:"JSON HomeDirectoryMappings"
       and policy = flag "policy" (optional string) ~doc:"STRING Policy"
       and posixProfile =
         flag "posix-profile" (optional json_arg) ~doc:"JSON PosixProfile"
       and role = flag "role" (required string) ~doc:"STRING Role"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and externalId =
         flag "external-id" (required string) ~doc:"STRING ExternalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access
           (Values.CreateAccessRequest.make ?homeDirectory
              ?homeDirectoryType:(Option.map
                                    ~f:Values.HomeDirectoryType.of_json
                                    homeDirectoryType)
              ?homeDirectoryMappings:(Option.map
                                        ~f:Values.HomeDirectoryMappings.of_json
                                        homeDirectoryMappings) ?policy
              ?posixProfile:(Option.map ~f:Values.PosixProfile.of_json
                               posixProfile) ~role ~serverId ~externalId ())
           (Some Values.CreateAccessResponse.to_json)
           (Some Values.CreateAccessResponse.error_to_json)])
let create_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificate =
         flag "certificate" (optional string) ~doc:"STRING Certificate"
       and domain = flag "domain" (optional json_arg) ~doc:"JSON Domain"
       and endpointDetails =
         flag "endpoint-details" (optional json_arg)
           ~doc:"JSON EndpointDetails"
       and endpointType =
         flag "endpoint-type" (optional json_arg) ~doc:"JSON EndpointType"
       and hostKey = flag "host-key" (optional string) ~doc:"STRING HostKey"
       and identityProviderDetails =
         flag "identity-provider-details" (optional json_arg)
           ~doc:"JSON IdentityProviderDetails"
       and identityProviderType =
         flag "identity-provider-type" (optional json_arg)
           ~doc:"JSON IdentityProviderType"
       and loggingRole =
         flag "logging-role" (optional string) ~doc:"STRING Role"
       and postAuthenticationLoginBanner =
         flag "post-authentication-login-banner" (optional string)
           ~doc:"STRING PostAuthenticationLoginBanner"
       and preAuthenticationLoginBanner =
         flag "pre-authentication-login-banner" (optional string)
           ~doc:"STRING PreAuthenticationLoginBanner"
       and protocols =
         flag "protocols" (optional json_arg) ~doc:"JSON Protocols"
       and protocolDetails =
         flag "protocol-details" (optional json_arg)
           ~doc:"JSON ProtocolDetails"
       and securityPolicyName =
         flag "security-policy-name" (optional string)
           ~doc:"STRING SecurityPolicyName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and workflowDetails =
         flag "workflow-details" (optional json_arg)
           ~doc:"JSON WorkflowDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_server
           (Values.CreateServerRequest.make ?certificate
              ?domain:(Option.map ~f:Values.Domain.of_json domain)
              ?endpointDetails:(Option.map ~f:Values.EndpointDetails.of_json
                                  endpointDetails)
              ?endpointType:(Option.map ~f:Values.EndpointType.of_json
                               endpointType) ?hostKey
              ?identityProviderDetails:(Option.map
                                          ~f:Values.IdentityProviderDetails.of_json
                                          identityProviderDetails)
              ?identityProviderType:(Option.map
                                       ~f:Values.IdentityProviderType.of_json
                                       identityProviderType) ?loggingRole
              ?postAuthenticationLoginBanner ?preAuthenticationLoginBanner
              ?protocols:(Option.map ~f:Values.Protocols.of_json protocols)
              ?protocolDetails:(Option.map ~f:Values.ProtocolDetails.of_json
                                  protocolDetails) ?securityPolicyName
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?workflowDetails:(Option.map ~f:Values.WorkflowDetails.of_json
                                  workflowDetails) ())
           (Some Values.CreateServerResponse.to_json)
           (Some Values.CreateServerResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and homeDirectory =
         flag "home-directory" (optional string) ~doc:"STRING HomeDirectory"
       and homeDirectoryType =
         flag "home-directory-type" (optional json_arg)
           ~doc:"JSON HomeDirectoryType"
       and homeDirectoryMappings =
         flag "home-directory-mappings" (optional json_arg)
           ~doc:"JSON HomeDirectoryMappings"
       and policy = flag "policy" (optional string) ~doc:"STRING Policy"
       and posixProfile =
         flag "posix-profile" (optional json_arg) ~doc:"JSON PosixProfile"
       and sshPublicKeyBody =
         flag "ssh-public-key-body" (optional string)
           ~doc:"STRING SshPublicKeyBody"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and role = flag "role" (required string) ~doc:"STRING Role"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?homeDirectory
              ?homeDirectoryType:(Option.map
                                    ~f:Values.HomeDirectoryType.of_json
                                    homeDirectoryType)
              ?homeDirectoryMappings:(Option.map
                                        ~f:Values.HomeDirectoryMappings.of_json
                                        homeDirectoryMappings) ?policy
              ?posixProfile:(Option.map ~f:Values.PosixProfile.of_json
                               posixProfile) ?sshPublicKeyBody
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~role ~serverId
              ~userName ()) (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let create_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkflowDescription"
       and onExceptionSteps =
         flag "on-exception-steps" (optional json_arg)
           ~doc:"JSON WorkflowSteps"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and steps = flag "steps" (required json_arg) ~doc:"JSON WorkflowSteps" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workflow
           (Values.CreateWorkflowRequest.make ?description
              ?onExceptionSteps:(Option.map ~f:Values.WorkflowSteps.of_json
                                   onExceptionSteps)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~steps:(Values.WorkflowSteps.of_json steps) ())
           (Some Values.CreateWorkflowResponse.to_json)
           (Some Values.CreateWorkflowResponse.error_to_json)])
let delete_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and externalId =
         flag "external-id" (required string) ~doc:"STRING ExternalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access
           (Values.DeleteAccessRequest.make ~serverId ~externalId ()) None
           None])
let delete_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_server (Values.DeleteServerRequest.make ~serverId ())
           None None])
let delete_ssh_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and sshPublicKeyId =
         flag "ssh-public-key-id" (required string)
           ~doc:"STRING SshPublicKeyId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ssh_public_key
           (Values.DeleteSshPublicKeyRequest.make ~serverId ~sshPublicKeyId
              ~userName ()) None None])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ~serverId ~userName ()) None None])
let delete_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow
           (Values.DeleteWorkflowRequest.make ~workflowId ()) None None])
let describe_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and externalId =
         flag "external-id" (required string) ~doc:"STRING ExternalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_access
           (Values.DescribeAccessRequest.make ~serverId ~externalId ())
           (Some Values.DescribeAccessResponse.to_json)
           (Some Values.DescribeAccessResponse.error_to_json)])
let describe_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ExecutionId"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_execution
           (Values.DescribeExecutionRequest.make ~executionId ~workflowId ())
           (Some Values.DescribeExecutionResponse.to_json)
           (Some Values.DescribeExecutionResponse.error_to_json)])
let describe_security_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityPolicyName =
         flag "security-policy-name" (required string)
           ~doc:"STRING SecurityPolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_security_policy
           (Values.DescribeSecurityPolicyRequest.make ~securityPolicyName ())
           (Some Values.DescribeSecurityPolicyResponse.to_json)
           (Some Values.DescribeSecurityPolicyResponse.error_to_json)])
let describe_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_server
           (Values.DescribeServerRequest.make ~serverId ())
           (Some Values.DescribeServerResponse.to_json)
           (Some Values.DescribeServerResponse.error_to_json)])
let describe_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user
           (Values.DescribeUserRequest.make ~serverId ~userName ())
           (Some Values.DescribeUserResponse.to_json)
           (Some Values.DescribeUserResponse.error_to_json)])
let describe_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_workflow
           (Values.DescribeWorkflowRequest.make ~workflowId ())
           (Some Values.DescribeWorkflowResponse.to_json)
           (Some Values.DescribeWorkflowResponse.error_to_json)])
let import_ssh_public_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and sshPublicKeyBody =
         flag "ssh-public-key-body" (required string)
           ~doc:"STRING SshPublicKeyBody"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_ssh_public_key
           (Values.ImportSshPublicKeyRequest.make ~serverId ~sshPublicKeyBody
              ~userName ()) (Some Values.ImportSshPublicKeyResponse.to_json)
           (Some Values.ImportSshPublicKeyResponse.error_to_json)])
let list_accesses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_accesses
           (Values.ListAccessesRequest.make ?maxResults ?nextToken ~serverId
              ()) (Some Values.ListAccessesResponse.to_json)
           (Some Values.ListAccessesResponse.error_to_json)])
let list_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_executions
           (Values.ListExecutionsRequest.make ?maxResults ?nextToken
              ~workflowId ()) (Some Values.ListExecutionsResponse.to_json)
           (Some Values.ListExecutionsResponse.error_to_json)])
let list_security_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_security_policies
           (Values.ListSecurityPoliciesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListSecurityPoliciesResponse.to_json)
           (Some Values.ListSecurityPoliciesResponse.error_to_json)])
let list_servers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_servers
           (Values.ListServersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListServersResponse.to_json)
           (Some Values.ListServersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?maxResults ?nextToken
              ~arn ()) (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?maxResults ?nextToken ~serverId ())
           (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let list_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflows
           (Values.ListWorkflowsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListWorkflowsResponse.to_json)
           (Some Values.ListWorkflowsResponse.error_to_json)])
let send_workflow_step_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId"
       and executionId =
         flag "execution-id" (required string) ~doc:"STRING ExecutionId"
       and token = flag "token" (required string) ~doc:"STRING CallbackToken"
       and status =
         flag "status" (required json_arg) ~doc:"JSON CustomStepStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_workflow_step_state
           (Values.SendWorkflowStepStateRequest.make ~workflowId ~executionId
              ~token ~status:(Values.CustomStepStatus.of_json status) ())
           (Some Values.SendWorkflowStepStateResponse.to_json)
           (Some Values.SendWorkflowStepStateResponse.error_to_json)])
let start_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_server (Values.StartServerRequest.make ~serverId ()) None
           None])
let stop_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_server (Values.StopServerRequest.make ~serverId ()) None
           None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.Tags.of_json tags) ()) None None])
let test_identity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverProtocol =
         flag "server-protocol" (optional json_arg) ~doc:"JSON Protocol"
       and sourceIp =
         flag "source-ip" (optional string) ~doc:"STRING SourceIp"
       and userPassword =
         flag "user-password" (optional string) ~doc:"STRING UserPassword"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_identity_provider
           (Values.TestIdentityProviderRequest.make
              ?serverProtocol:(Option.map ~f:Values.Protocol.of_json
                                 serverProtocol) ?sourceIp ?userPassword
              ~serverId ~userName ())
           (Some Values.TestIdentityProviderResponse.to_json)
           (Some Values.TestIdentityProviderResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let update_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and homeDirectory =
         flag "home-directory" (optional string) ~doc:"STRING HomeDirectory"
       and homeDirectoryType =
         flag "home-directory-type" (optional json_arg)
           ~doc:"JSON HomeDirectoryType"
       and homeDirectoryMappings =
         flag "home-directory-mappings" (optional json_arg)
           ~doc:"JSON HomeDirectoryMappings"
       and policy = flag "policy" (optional string) ~doc:"STRING Policy"
       and posixProfile =
         flag "posix-profile" (optional json_arg) ~doc:"JSON PosixProfile"
       and role = flag "role" (optional string) ~doc:"STRING Role"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and externalId =
         flag "external-id" (required string) ~doc:"STRING ExternalId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_access
           (Values.UpdateAccessRequest.make ?homeDirectory
              ?homeDirectoryType:(Option.map
                                    ~f:Values.HomeDirectoryType.of_json
                                    homeDirectoryType)
              ?homeDirectoryMappings:(Option.map
                                        ~f:Values.HomeDirectoryMappings.of_json
                                        homeDirectoryMappings) ?policy
              ?posixProfile:(Option.map ~f:Values.PosixProfile.of_json
                               posixProfile) ?role ~serverId ~externalId ())
           (Some Values.UpdateAccessResponse.to_json)
           (Some Values.UpdateAccessResponse.error_to_json)])
let update_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificate =
         flag "certificate" (optional string) ~doc:"STRING Certificate"
       and protocolDetails =
         flag "protocol-details" (optional json_arg)
           ~doc:"JSON ProtocolDetails"
       and endpointDetails =
         flag "endpoint-details" (optional json_arg)
           ~doc:"JSON EndpointDetails"
       and endpointType =
         flag "endpoint-type" (optional json_arg) ~doc:"JSON EndpointType"
       and hostKey = flag "host-key" (optional string) ~doc:"STRING HostKey"
       and identityProviderDetails =
         flag "identity-provider-details" (optional json_arg)
           ~doc:"JSON IdentityProviderDetails"
       and loggingRole =
         flag "logging-role" (optional string) ~doc:"STRING NullableRole"
       and postAuthenticationLoginBanner =
         flag "post-authentication-login-banner" (optional string)
           ~doc:"STRING PostAuthenticationLoginBanner"
       and preAuthenticationLoginBanner =
         flag "pre-authentication-login-banner" (optional string)
           ~doc:"STRING PreAuthenticationLoginBanner"
       and protocols =
         flag "protocols" (optional json_arg) ~doc:"JSON Protocols"
       and securityPolicyName =
         flag "security-policy-name" (optional string)
           ~doc:"STRING SecurityPolicyName"
       and workflowDetails =
         flag "workflow-details" (optional json_arg)
           ~doc:"JSON WorkflowDetails"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_server
           (Values.UpdateServerRequest.make ?certificate
              ?protocolDetails:(Option.map ~f:Values.ProtocolDetails.of_json
                                  protocolDetails)
              ?endpointDetails:(Option.map ~f:Values.EndpointDetails.of_json
                                  endpointDetails)
              ?endpointType:(Option.map ~f:Values.EndpointType.of_json
                               endpointType) ?hostKey
              ?identityProviderDetails:(Option.map
                                          ~f:Values.IdentityProviderDetails.of_json
                                          identityProviderDetails)
              ?loggingRole ?postAuthenticationLoginBanner
              ?preAuthenticationLoginBanner
              ?protocols:(Option.map ~f:Values.Protocols.of_json protocols)
              ?securityPolicyName
              ?workflowDetails:(Option.map ~f:Values.WorkflowDetails.of_json
                                  workflowDetails) ~serverId ())
           (Some Values.UpdateServerResponse.to_json)
           (Some Values.UpdateServerResponse.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and homeDirectory =
         flag "home-directory" (optional string) ~doc:"STRING HomeDirectory"
       and homeDirectoryType =
         flag "home-directory-type" (optional json_arg)
           ~doc:"JSON HomeDirectoryType"
       and homeDirectoryMappings =
         flag "home-directory-mappings" (optional json_arg)
           ~doc:"JSON HomeDirectoryMappings"
       and policy = flag "policy" (optional string) ~doc:"STRING Policy"
       and posixProfile =
         flag "posix-profile" (optional json_arg) ~doc:"JSON PosixProfile"
       and role = flag "role" (optional string) ~doc:"STRING Role"
       and serverId =
         flag "server-id" (required string) ~doc:"STRING ServerId"
       and userName =
         flag "user-name" (required string) ~doc:"STRING UserName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make ?homeDirectory
              ?homeDirectoryType:(Option.map
                                    ~f:Values.HomeDirectoryType.of_json
                                    homeDirectoryType)
              ?homeDirectoryMappings:(Option.map
                                        ~f:Values.HomeDirectoryMappings.of_json
                                        homeDirectoryMappings) ?policy
              ?posixProfile:(Option.map ~f:Values.PosixProfile.of_json
                               posixProfile) ?role ~serverId ~userName ())
           (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-access", create_access);
    ("create-server", create_server);
    ("create-user", create_user);
    ("create-workflow", create_workflow);
    ("delete-access", delete_access);
    ("delete-server", delete_server);
    ("delete-ssh-public-key", delete_ssh_public_key);
    ("delete-user", delete_user);
    ("delete-workflow", delete_workflow);
    ("describe-access", describe_access);
    ("describe-execution", describe_execution);
    ("describe-security-policy", describe_security_policy);
    ("describe-server", describe_server);
    ("describe-user", describe_user);
    ("describe-workflow", describe_workflow);
    ("import-ssh-public-key", import_ssh_public_key);
    ("list-accesses", list_accesses);
    ("list-executions", list_executions);
    ("list-security-policies", list_security_policies);
    ("list-servers", list_servers);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-users", list_users);
    ("list-workflows", list_workflows);
    ("send-workflow-step-state", send_workflow_step_state);
    ("start-server", start_server);
    ("stop-server", stop_server);
    ("tag-resource", tag_resource);
    ("test-identity-provider", test_identity_provider);
    ("untag-resource", untag_resource);
    ("update-access", update_access);
    ("update-server", update_server);
    ("update-user", update_user)]
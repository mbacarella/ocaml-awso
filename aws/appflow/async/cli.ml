(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_flow_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and executionIds =
         flag "execution-ids" (optional json_arg) ~doc:"JSON ExecutionIds"
       and flowName =
         flag "flow-name" (required string) ~doc:"STRING FlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_flow_executions
           (Values.CancelFlowExecutionsRequest.make
              ?executionIds:(Option.map ~f:Values.ExecutionIds.of_json
                               executionIds) ~flowName ())
           (Some Values.CancelFlowExecutionsResponse.to_json)
           (Some Values.CancelFlowExecutionsResponse.error_to_json)])
let create_connector_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsArn = flag "kms-arn" (optional string) ~doc:"STRING KMSArn"
       and connectorLabel =
         flag "connector-label" (optional string)
           ~doc:"STRING ConnectorLabel"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and connectorProfileName =
         flag "connector-profile-name" (required string)
           ~doc:"STRING ConnectorProfileName"
       and connectorType =
         flag "connector-type" (required json_arg) ~doc:"JSON ConnectorType"
       and connectionMode =
         flag "connection-mode" (required json_arg)
           ~doc:"JSON ConnectionMode"
       and connectorProfileConfig =
         flag "connector-profile-config" (required json_arg)
           ~doc:"JSON ConnectorProfileConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connector_profile
           (Values.CreateConnectorProfileRequest.make ?kmsArn ?connectorLabel
              ?clientToken ~connectorProfileName
              ~connectorType:(Values.ConnectorType.of_json connectorType)
              ~connectionMode:(Values.ConnectionMode.of_json connectionMode)
              ~connectorProfileConfig:(Values.ConnectorProfileConfig.of_json
                                         connectorProfileConfig) ())
           (Some Values.CreateConnectorProfileResponse.to_json)
           (Some Values.CreateConnectorProfileResponse.error_to_json)])
let create_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING FlowDescription"
       and kmsArn = flag "kms-arn" (optional string) ~doc:"STRING KMSArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and metadataCatalogConfig =
         flag "metadata-catalog-config" (optional json_arg)
           ~doc:"JSON MetadataCatalogConfig"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and flowName =
         flag "flow-name" (required string) ~doc:"STRING FlowName"
       and triggerConfig =
         flag "trigger-config" (required json_arg) ~doc:"JSON TriggerConfig"
       and sourceFlowConfig =
         flag "source-flow-config" (required json_arg)
           ~doc:"JSON SourceFlowConfig"
       and destinationFlowConfigList =
         flag "destination-flow-config-list" (required json_arg)
           ~doc:"JSON DestinationFlowConfigList"
       and tasks = flag "tasks" (required json_arg) ~doc:"JSON Tasks" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flow
           (Values.CreateFlowRequest.make ?description ?kmsArn
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?metadataCatalogConfig:(Option.map
                                        ~f:Values.MetadataCatalogConfig.of_json
                                        metadataCatalogConfig) ?clientToken
              ~flowName
              ~triggerConfig:(Values.TriggerConfig.of_json triggerConfig)
              ~sourceFlowConfig:(Values.SourceFlowConfig.of_json
                                   sourceFlowConfig)
              ~destinationFlowConfigList:(Values.DestinationFlowConfigList.of_json
                                            destinationFlowConfigList)
              ~tasks:(Values.Tasks.of_json tasks) ())
           (Some Values.CreateFlowResponse.to_json)
           (Some Values.CreateFlowResponse.error_to_json)])
let delete_connector_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean"
       and connectorProfileName =
         flag "connector-profile-name" (required string)
           ~doc:"STRING ConnectorProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connector_profile
           (Values.DeleteConnectorProfileRequest.make ?forceDelete
              ~connectorProfileName ())
           (Some Values.DeleteConnectorProfileResponse.to_json)
           (Some Values.DeleteConnectorProfileResponse.error_to_json)])
let delete_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean"
       and flowName =
         flag "flow-name" (required string) ~doc:"STRING FlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flow
           (Values.DeleteFlowRequest.make ?forceDelete ~flowName ())
           (Some Values.DeleteFlowResponse.to_json)
           (Some Values.DeleteFlowResponse.error_to_json)])
let describe_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorLabel =
         flag "connector-label" (optional string)
           ~doc:"STRING ConnectorLabel"
       and connectorType =
         flag "connector-type" (required json_arg) ~doc:"JSON ConnectorType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_connector
           (Values.DescribeConnectorRequest.make ?connectorLabel
              ~connectorType:(Values.ConnectorType.of_json connectorType) ())
           (Some Values.DescribeConnectorResponse.to_json)
           (Some Values.DescribeConnectorResponse.error_to_json)])
let describe_connector_entity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorType =
         flag "connector-type" (optional json_arg) ~doc:"JSON ConnectorType"
       and connectorProfileName =
         flag "connector-profile-name" (optional string)
           ~doc:"STRING ConnectorProfileName"
       and apiVersion =
         flag "api-version" (optional string) ~doc:"STRING ApiVersion"
       and connectorEntityName =
         flag "connector-entity-name" (required string)
           ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_connector_entity
           (Values.DescribeConnectorEntityRequest.make
              ?connectorType:(Option.map ~f:Values.ConnectorType.of_json
                                connectorType) ?connectorProfileName
              ?apiVersion ~connectorEntityName ())
           (Some Values.DescribeConnectorEntityResponse.to_json)
           (Some Values.DescribeConnectorEntityResponse.error_to_json)])
let describe_connector_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorProfileNames =
         flag "connector-profile-names" (optional json_arg)
           ~doc:"JSON ConnectorProfileNameList"
       and connectorType =
         flag "connector-type" (optional json_arg) ~doc:"JSON ConnectorType"
       and connectorLabel =
         flag "connector-label" (optional string)
           ~doc:"STRING ConnectorLabel"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_connector_profiles
           (Values.DescribeConnectorProfilesRequest.make
              ?connectorProfileNames:(Option.map
                                        ~f:Values.ConnectorProfileNameList.of_json
                                        connectorProfileNames)
              ?connectorType:(Option.map ~f:Values.ConnectorType.of_json
                                connectorType) ?connectorLabel ?maxResults
              ?nextToken ())
           (Some Values.DescribeConnectorProfilesResponse.to_json)
           (Some Values.DescribeConnectorProfilesResponse.error_to_json)])
let describe_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorTypes =
         flag "connector-types" (optional json_arg)
           ~doc:"JSON ConnectorTypeList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_connectors
           (Values.DescribeConnectorsRequest.make
              ?connectorTypes:(Option.map ~f:Values.ConnectorTypeList.of_json
                                 connectorTypes) ?maxResults ?nextToken ())
           (Some Values.DescribeConnectorsResponse.to_json)
           (Some Values.DescribeConnectorsResponse.error_to_json)])
let describe_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowName =
         flag "flow-name" (required string) ~doc:"STRING FlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flow (Values.DescribeFlowRequest.make ~flowName ())
           (Some Values.DescribeFlowResponse.to_json)
           (Some Values.DescribeFlowResponse.error_to_json)])
let describe_flow_execution_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and flowName =
         flag "flow-name" (required string) ~doc:"STRING FlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flow_execution_records
           (Values.DescribeFlowExecutionRecordsRequest.make ?maxResults
              ?nextToken ~flowName ())
           (Some Values.DescribeFlowExecutionRecordsResponse.to_json)
           (Some Values.DescribeFlowExecutionRecordsResponse.error_to_json)])
let list_connector_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorProfileName =
         flag "connector-profile-name" (optional string)
           ~doc:"STRING ConnectorProfileName"
       and connectorType =
         flag "connector-type" (optional json_arg) ~doc:"JSON ConnectorType"
       and entitiesPath =
         flag "entities-path" (optional string) ~doc:"STRING EntitiesPath"
       and apiVersion =
         flag "api-version" (optional string) ~doc:"STRING ApiVersion"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListEntitiesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connector_entities
           (Values.ListConnectorEntitiesRequest.make ?connectorProfileName
              ?connectorType:(Option.map ~f:Values.ConnectorType.of_json
                                connectorType) ?entitiesPath ?apiVersion
              ?maxResults ?nextToken ())
           (Some Values.ListConnectorEntitiesResponse.to_json)
           (Some Values.ListConnectorEntitiesResponse.error_to_json)])
let list_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connectors
           (Values.ListConnectorsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListConnectorsResponse.to_json)
           (Some Values.ListConnectorsResponse.error_to_json)])
let list_flows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_flows
           (Values.ListFlowsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListFlowsResponse.to_json)
           (Some Values.ListFlowsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let register_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorLabel =
         flag "connector-label" (optional string)
           ~doc:"STRING ConnectorLabel"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and connectorProvisioningType =
         flag "connector-provisioning-type" (optional json_arg)
           ~doc:"JSON ConnectorProvisioningType"
       and connectorProvisioningConfig =
         flag "connector-provisioning-config" (optional json_arg)
           ~doc:"JSON ConnectorProvisioningConfig"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_connector
           (Values.RegisterConnectorRequest.make ?connectorLabel ?description
              ?connectorProvisioningType:(Option.map
                                            ~f:Values.ConnectorProvisioningType.of_json
                                            connectorProvisioningType)
              ?connectorProvisioningConfig:(Option.map
                                              ~f:Values.ConnectorProvisioningConfig.of_json
                                              connectorProvisioningConfig)
              ?clientToken ())
           (Some Values.RegisterConnectorResponse.to_json)
           (Some Values.RegisterConnectorResponse.error_to_json)])
let reset_connector_metadata_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectorProfileName =
         flag "connector-profile-name" (optional string)
           ~doc:"STRING ConnectorProfileName"
       and connectorType =
         flag "connector-type" (optional json_arg) ~doc:"JSON ConnectorType"
       and connectorEntityName =
         flag "connector-entity-name" (optional string)
           ~doc:"STRING EntityName"
       and entitiesPath =
         flag "entities-path" (optional string) ~doc:"STRING EntitiesPath"
       and apiVersion =
         flag "api-version" (optional string) ~doc:"STRING ApiVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_connector_metadata_cache
           (Values.ResetConnectorMetadataCacheRequest.make
              ?connectorProfileName
              ?connectorType:(Option.map ~f:Values.ConnectorType.of_json
                                connectorType) ?connectorEntityName
              ?entitiesPath ?apiVersion ())
           (Some Values.ResetConnectorMetadataCacheResponse.to_json)
           (Some Values.ResetConnectorMetadataCacheResponse.error_to_json)])
let start_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and flowName =
         flag "flow-name" (required string) ~doc:"STRING FlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_flow
           (Values.StartFlowRequest.make ?clientToken ~flowName ())
           (Some Values.StartFlowResponse.to_json)
           (Some Values.StartFlowResponse.error_to_json)])
let stop_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowName =
         flag "flow-name" (required string) ~doc:"STRING FlowName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_flow (Values.StopFlowRequest.make ~flowName ())
           (Some Values.StopFlowResponse.to_json)
           (Some Values.StopFlowResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let unregister_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean"
       and connectorLabel =
         flag "connector-label" (required string)
           ~doc:"STRING ConnectorLabel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unregister_connector
           (Values.UnregisterConnectorRequest.make ?forceDelete
              ~connectorLabel ())
           (Some Values.UnregisterConnectorResponse.to_json)
           (Some Values.UnregisterConnectorResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_connector_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and connectorProfileName =
         flag "connector-profile-name" (required string)
           ~doc:"STRING ConnectorProfileName"
       and connectionMode =
         flag "connection-mode" (required json_arg)
           ~doc:"JSON ConnectionMode"
       and connectorProfileConfig =
         flag "connector-profile-config" (required json_arg)
           ~doc:"JSON ConnectorProfileConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connector_profile
           (Values.UpdateConnectorProfileRequest.make ?clientToken
              ~connectorProfileName
              ~connectionMode:(Values.ConnectionMode.of_json connectionMode)
              ~connectorProfileConfig:(Values.ConnectorProfileConfig.of_json
                                         connectorProfileConfig) ())
           (Some Values.UpdateConnectorProfileResponse.to_json)
           (Some Values.UpdateConnectorProfileResponse.error_to_json)])
let update_connector_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and connectorProvisioningConfig =
         flag "connector-provisioning-config" (optional json_arg)
           ~doc:"JSON ConnectorProvisioningConfig"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and connectorLabel =
         flag "connector-label" (required string)
           ~doc:"STRING ConnectorLabel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_connector_registration
           (Values.UpdateConnectorRegistrationRequest.make ?description
              ?connectorProvisioningConfig:(Option.map
                                              ~f:Values.ConnectorProvisioningConfig.of_json
                                              connectorProvisioningConfig)
              ?clientToken ~connectorLabel ())
           (Some Values.UpdateConnectorRegistrationResponse.to_json)
           (Some Values.UpdateConnectorRegistrationResponse.error_to_json)])
let update_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING FlowDescription"
       and metadataCatalogConfig =
         flag "metadata-catalog-config" (optional json_arg)
           ~doc:"JSON MetadataCatalogConfig"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and flowName =
         flag "flow-name" (required string) ~doc:"STRING FlowName"
       and triggerConfig =
         flag "trigger-config" (required json_arg) ~doc:"JSON TriggerConfig"
       and sourceFlowConfig =
         flag "source-flow-config" (required json_arg)
           ~doc:"JSON SourceFlowConfig"
       and destinationFlowConfigList =
         flag "destination-flow-config-list" (required json_arg)
           ~doc:"JSON DestinationFlowConfigList"
       and tasks = flag "tasks" (required json_arg) ~doc:"JSON Tasks" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow
           (Values.UpdateFlowRequest.make ?description
              ?metadataCatalogConfig:(Option.map
                                        ~f:Values.MetadataCatalogConfig.of_json
                                        metadataCatalogConfig) ?clientToken
              ~flowName
              ~triggerConfig:(Values.TriggerConfig.of_json triggerConfig)
              ~sourceFlowConfig:(Values.SourceFlowConfig.of_json
                                   sourceFlowConfig)
              ~destinationFlowConfigList:(Values.DestinationFlowConfigList.of_json
                                            destinationFlowConfigList)
              ~tasks:(Values.Tasks.of_json tasks) ())
           (Some Values.UpdateFlowResponse.to_json)
           (Some Values.UpdateFlowResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-flow-executions", cancel_flow_executions);
    ("create-connector-profile", create_connector_profile);
    ("create-flow", create_flow);
    ("delete-connector-profile", delete_connector_profile);
    ("delete-flow", delete_flow);
    ("describe-connector", describe_connector);
    ("describe-connector-entity", describe_connector_entity);
    ("describe-connector-profiles", describe_connector_profiles);
    ("describe-connectors", describe_connectors);
    ("describe-flow", describe_flow);
    ("describe-flow-execution-records", describe_flow_execution_records);
    ("list-connector-entities", list_connector_entities);
    ("list-connectors", list_connectors);
    ("list-flows", list_flows);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-connector", register_connector);
    ("reset-connector-metadata-cache", reset_connector_metadata_cache);
    ("start-flow", start_flow);
    ("stop-flow", stop_flow);
    ("tag-resource", tag_resource);
    ("unregister-connector", unregister_connector);
    ("untag-resource", untag_resource);
    ("update-connector-profile", update_connector_profile);
    ("update-connector-registration", update_connector_registration);
    ("update-flow", update_flow)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_identity_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allowClassicFlow =
         flag "allow-classic-flow" (optional bool) ~doc:"BOOL ClassicFlow"
       and supportedLoginProviders =
         flag "supported-login-providers" (optional json_arg)
           ~doc:"JSON IdentityProviders"
       and developerProviderName =
         flag "developer-provider-name" (optional string)
           ~doc:"STRING DeveloperProviderName"
       and openIdConnectProviderARNs =
         flag "open-id-connect-provider-a-r-ns" (optional json_arg)
           ~doc:"JSON OIDCProviderList"
       and cognitoIdentityProviders =
         flag "cognito-identity-providers" (optional json_arg)
           ~doc:"JSON CognitoIdentityProviderList"
       and samlProviderARNs =
         flag "saml-provider-a-r-ns" (optional json_arg)
           ~doc:"JSON SAMLProviderList"
       and identityPoolTags =
         flag "identity-pool-tags" (optional json_arg)
           ~doc:"JSON IdentityPoolTagsType"
       and identityPoolName =
         flag "identity-pool-name" (required string)
           ~doc:"STRING IdentityPoolName"
       and allowUnauthenticatedIdentities =
         flag "allow-unauthenticated-identities" (required bool)
           ~doc:"BOOL IdentityPoolUnauthenticated" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_identity_pool
           (Values.CreateIdentityPoolInput.make ?allowClassicFlow
              ?supportedLoginProviders:(Option.map
                                          ~f:Values.IdentityProviders.of_json
                                          supportedLoginProviders)
              ?developerProviderName
              ?openIdConnectProviderARNs:(Option.map
                                            ~f:Values.OIDCProviderList.of_json
                                            openIdConnectProviderARNs)
              ?cognitoIdentityProviders:(Option.map
                                           ~f:Values.CognitoIdentityProviderList.of_json
                                           cognitoIdentityProviders)
              ?samlProviderARNs:(Option.map
                                   ~f:Values.SAMLProviderList.of_json
                                   samlProviderARNs)
              ?identityPoolTags:(Option.map
                                   ~f:Values.IdentityPoolTagsType.of_json
                                   identityPoolTags) ~identityPoolName
              ~allowUnauthenticatedIdentities ())
           (Some Values.IdentityPool.to_json)
           (Some Values.IdentityPool.error_to_json)])
let delete_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityIdsToDelete =
         flag "identity-ids-to-delete" (required json_arg)
           ~doc:"JSON IdentityIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identities
           (Values.DeleteIdentitiesInput.make
              ~identityIdsToDelete:(Values.IdentityIdList.of_json
                                      identityIdsToDelete) ())
           (Some Values.DeleteIdentitiesResponse.to_json)
           (Some Values.DeleteIdentitiesResponse.error_to_json)])
let delete_identity_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identity_pool
           (Values.DeleteIdentityPoolInput.make ~identityPoolId ()) None None])
let describe_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity
           (Values.DescribeIdentityInput.make ~identityId ())
           (Some Values.IdentityDescription.to_json)
           (Some Values.IdentityDescription.error_to_json)])
let describe_identity_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity_pool
           (Values.DescribeIdentityPoolInput.make ~identityPoolId ())
           (Some Values.IdentityPool.to_json)
           (Some Values.IdentityPool.error_to_json)])
let get_credentials_for_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logins = flag "logins" (optional json_arg) ~doc:"JSON LoginsMap"
       and customRoleArn =
         flag "custom-role-arn" (optional string) ~doc:"STRING ARNString"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_credentials_for_identity
           (Values.GetCredentialsForIdentityInput.make
              ?logins:(Option.map ~f:Values.LoginsMap.of_json logins)
              ?customRoleArn ~identityId ())
           (Some Values.GetCredentialsForIdentityResponse.to_json)
           (Some Values.GetCredentialsForIdentityResponse.error_to_json)])
let get_id =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AccountId"
       and logins = flag "logins" (optional json_arg) ~doc:"JSON LoginsMap"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.get_id
           (Values.GetIdInput.make ?accountId
              ?logins:(Option.map ~f:Values.LoginsMap.of_json logins)
              ~identityPoolId ()) (Some Values.GetIdResponse.to_json)
           (Some Values.GetIdResponse.error_to_json)])
let get_identity_pool_roles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_pool_roles
           (Values.GetIdentityPoolRolesInput.make ~identityPoolId ())
           (Some Values.GetIdentityPoolRolesResponse.to_json)
           (Some Values.GetIdentityPoolRolesResponse.error_to_json)])
let get_open_id_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logins = flag "logins" (optional json_arg) ~doc:"JSON LoginsMap"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_open_id_token
           (Values.GetOpenIdTokenInput.make
              ?logins:(Option.map ~f:Values.LoginsMap.of_json logins)
              ~identityId ()) (Some Values.GetOpenIdTokenResponse.to_json)
           (Some Values.GetOpenIdTokenResponse.error_to_json)])
let get_open_id_token_for_developer_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityId =
         flag "identity-id" (optional string) ~doc:"STRING IdentityId"
       and principalTags =
         flag "principal-tags" (optional json_arg) ~doc:"JSON PrincipalTags"
       and tokenDuration =
         flag "token-duration" (optional json_arg) ~doc:"JSON TokenDuration"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and logins = flag "logins" (required json_arg) ~doc:"JSON LoginsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_open_id_token_for_developer_identity
           (Values.GetOpenIdTokenForDeveloperIdentityInput.make ?identityId
              ?principalTags:(Option.map ~f:Values.PrincipalTags.of_json
                                principalTags)
              ?tokenDuration:(Option.map ~f:Values.TokenDuration.of_json
                                tokenDuration) ~identityPoolId
              ~logins:(Values.LoginsMap.of_json logins) ())
           (Some Values.GetOpenIdTokenForDeveloperIdentityResponse.to_json)
           (Some
              Values.GetOpenIdTokenForDeveloperIdentityResponse.error_to_json)])
let get_principal_tag_attribute_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityProviderName =
         flag "identity-provider-name" (required string)
           ~doc:"STRING IdentityProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_principal_tag_attribute_map
           (Values.GetPrincipalTagAttributeMapInput.make ~identityPoolId
              ~identityProviderName ())
           (Some Values.GetPrincipalTagAttributeMapResponse.to_json)
           (Some Values.GetPrincipalTagAttributeMapResponse.error_to_json)])
let list_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKey"
       and hideDisabled =
         flag "hide-disabled" (optional bool) ~doc:"BOOL HideDisabled"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT QueryLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identities
           (Values.ListIdentitiesInput.make ?nextToken ?hideDisabled
              ~identityPoolId ~maxResults ())
           (Some Values.ListIdentitiesResponse.to_json)
           (Some Values.ListIdentitiesResponse.error_to_json)])
let list_identity_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKey"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT QueryLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_pools
           (Values.ListIdentityPoolsInput.make ?nextToken ~maxResults ())
           (Some Values.ListIdentityPoolsResponse.to_json)
           (Some Values.ListIdentityPoolsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARNString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let lookup_developer_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityId =
         flag "identity-id" (optional string) ~doc:"STRING IdentityId"
       and developerUserIdentifier =
         flag "developer-user-identifier" (optional string)
           ~doc:"STRING DeveloperUserIdentifier"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT QueryLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationKey"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.lookup_developer_identity
           (Values.LookupDeveloperIdentityInput.make ?identityId
              ?developerUserIdentifier ?maxResults ?nextToken ~identityPoolId
              ()) (Some Values.LookupDeveloperIdentityResponse.to_json)
           (Some Values.LookupDeveloperIdentityResponse.error_to_json)])
let merge_developer_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceUserIdentifier =
         flag "source-user-identifier" (required string)
           ~doc:"STRING DeveloperUserIdentifier"
       and destinationUserIdentifier =
         flag "destination-user-identifier" (required string)
           ~doc:"STRING DeveloperUserIdentifier"
       and developerProviderName =
         flag "developer-provider-name" (required string)
           ~doc:"STRING DeveloperProviderName"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.merge_developer_identities
           (Values.MergeDeveloperIdentitiesInput.make ~sourceUserIdentifier
              ~destinationUserIdentifier ~developerProviderName
              ~identityPoolId ())
           (Some Values.MergeDeveloperIdentitiesResponse.to_json)
           (Some Values.MergeDeveloperIdentitiesResponse.error_to_json)])
let set_identity_pool_roles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleMappings =
         flag "role-mappings" (optional json_arg) ~doc:"JSON RoleMappingMap"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and roles = flag "roles" (required json_arg) ~doc:"JSON RolesMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_identity_pool_roles
           (Values.SetIdentityPoolRolesInput.make
              ?roleMappings:(Option.map ~f:Values.RoleMappingMap.of_json
                               roleMappings) ~identityPoolId
              ~roles:(Values.RolesMap.of_json roles) ()) None None])
let set_principal_tag_attribute_map =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and useDefaults =
         flag "use-defaults" (optional bool) ~doc:"BOOL UseDefaults"
       and principalTags =
         flag "principal-tags" (optional json_arg) ~doc:"JSON PrincipalTags"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityProviderName =
         flag "identity-provider-name" (required string)
           ~doc:"STRING IdentityProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_principal_tag_attribute_map
           (Values.SetPrincipalTagAttributeMapInput.make ?useDefaults
              ?principalTags:(Option.map ~f:Values.PrincipalTags.of_json
                                principalTags) ~identityPoolId
              ~identityProviderName ())
           (Some Values.SetPrincipalTagAttributeMapResponse.to_json)
           (Some Values.SetPrincipalTagAttributeMapResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARNString"
       and tags =
         flag "tags" (required json_arg) ~doc:"JSON IdentityPoolTagsType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.IdentityPoolTagsType.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let unlink_developer_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and developerProviderName =
         flag "developer-provider-name" (required string)
           ~doc:"STRING DeveloperProviderName"
       and developerUserIdentifier =
         flag "developer-user-identifier" (required string)
           ~doc:"STRING DeveloperUserIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unlink_developer_identity
           (Values.UnlinkDeveloperIdentityInput.make ~identityId
              ~identityPoolId ~developerProviderName ~developerUserIdentifier
              ()) None None])
let unlink_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityId =
         flag "identity-id" (required string) ~doc:"STRING IdentityId"
       and logins = flag "logins" (required json_arg) ~doc:"JSON LoginsMap"
       and loginsToRemove =
         flag "logins-to-remove" (required json_arg) ~doc:"JSON LoginsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unlink_identity
           (Values.UnlinkIdentityInput.make ~identityId
              ~logins:(Values.LoginsMap.of_json logins)
              ~loginsToRemove:(Values.LoginsList.of_json loginsToRemove) ())
           None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARNString"
       and tagKeys =
         flag "tag-keys" (required json_arg)
           ~doc:"JSON IdentityPoolTagsListType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.IdentityPoolTagsListType.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_identity_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allowClassicFlow =
         flag "allow-classic-flow" (optional bool) ~doc:"BOOL ClassicFlow"
       and supportedLoginProviders =
         flag "supported-login-providers" (optional json_arg)
           ~doc:"JSON IdentityProviders"
       and developerProviderName =
         flag "developer-provider-name" (optional string)
           ~doc:"STRING DeveloperProviderName"
       and openIdConnectProviderARNs =
         flag "open-id-connect-provider-a-r-ns" (optional json_arg)
           ~doc:"JSON OIDCProviderList"
       and cognitoIdentityProviders =
         flag "cognito-identity-providers" (optional json_arg)
           ~doc:"JSON CognitoIdentityProviderList"
       and samlProviderARNs =
         flag "saml-provider-a-r-ns" (optional json_arg)
           ~doc:"JSON SAMLProviderList"
       and identityPoolTags =
         flag "identity-pool-tags" (optional json_arg)
           ~doc:"JSON IdentityPoolTagsType"
       and identityPoolId =
         flag "identity-pool-id" (required string)
           ~doc:"STRING IdentityPoolId"
       and identityPoolName =
         flag "identity-pool-name" (required string)
           ~doc:"STRING IdentityPoolName"
       and allowUnauthenticatedIdentities =
         flag "allow-unauthenticated-identities" (required bool)
           ~doc:"BOOL IdentityPoolUnauthenticated" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_identity_pool
           (Values.IdentityPool.make ?allowClassicFlow
              ?supportedLoginProviders:(Option.map
                                          ~f:Values.IdentityProviders.of_json
                                          supportedLoginProviders)
              ?developerProviderName
              ?openIdConnectProviderARNs:(Option.map
                                            ~f:Values.OIDCProviderList.of_json
                                            openIdConnectProviderARNs)
              ?cognitoIdentityProviders:(Option.map
                                           ~f:Values.CognitoIdentityProviderList.of_json
                                           cognitoIdentityProviders)
              ?samlProviderARNs:(Option.map
                                   ~f:Values.SAMLProviderList.of_json
                                   samlProviderARNs)
              ?identityPoolTags:(Option.map
                                   ~f:Values.IdentityPoolTagsType.of_json
                                   identityPoolTags) ~identityPoolId
              ~identityPoolName ~allowUnauthenticatedIdentities ())
           (Some Values.IdentityPool.to_json)
           (Some Values.IdentityPool.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-identity-pool", create_identity_pool);
    ("delete-identities", delete_identities);
    ("delete-identity-pool", delete_identity_pool);
    ("describe-identity", describe_identity);
    ("describe-identity-pool", describe_identity_pool);
    ("get-credentials-for-identity", get_credentials_for_identity);
    ("get-id", get_id);
    ("get-identity-pool-roles", get_identity_pool_roles);
    ("get-open-id-token", get_open_id_token);
    ("get-open-id-token-for-developer-identity",
      get_open_id_token_for_developer_identity);
    ("get-principal-tag-attribute-map", get_principal_tag_attribute_map);
    ("list-identities", list_identities);
    ("list-identity-pools", list_identity_pools);
    ("list-tags-for-resource", list_tags_for_resource);
    ("lookup-developer-identity", lookup_developer_identity);
    ("merge-developer-identities", merge_developer_identities);
    ("set-identity-pool-roles", set_identity_pool_roles);
    ("set-principal-tag-attribute-map", set_principal_tag_attribute_map);
    ("tag-resource", tag_resource);
    ("unlink-developer-identity", unlink_developer_identity);
    ("unlink-identity", unlink_identity);
    ("untag-resource", untag_resource);
    ("update-identity-pool", update_identity_pool)]
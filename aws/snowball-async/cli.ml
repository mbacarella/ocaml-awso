(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_cluster (Values.CancelClusterRequest.make ~clusterId ())
           (Some Values.CancelClusterResult.to_json)
           (Some Values.CancelClusterResult.error_to_json)])
let cancel_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_job (Values.CancelJobRequest.make ~jobId ())
           (Some Values.CancelJobResult.to_json)
           (Some Values.CancelJobResult.error_to_json)])
let create_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and address = flag "address" (required json_arg) ~doc:"JSON Address" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_address
           (Values.CreateAddressRequest.make
              ~address:(Values.Address.of_json address) ())
           (Some Values.CreateAddressResult.to_json)
           (Some Values.CreateAddressResult.error_to_json)])
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and onDeviceServiceConfiguration =
         flag "on-device-service-configuration" (optional json_arg)
           ~doc:"JSON OnDeviceServiceConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and kmsKeyARN =
         flag "kms-key-a-r-n" (optional string) ~doc:"STRING KmsKeyARN"
       and notification =
         flag "notification" (optional json_arg) ~doc:"JSON Notification"
       and forwardingAddressId =
         flag "forwarding-address-id" (optional string)
           ~doc:"STRING AddressId"
       and taxDocuments =
         flag "tax-documents" (optional json_arg) ~doc:"JSON TaxDocuments"
       and remoteManagement =
         flag "remote-management" (optional json_arg)
           ~doc:"JSON RemoteManagement"
       and jobType = flag "job-type" (required json_arg) ~doc:"JSON JobType"
       and resources =
         flag "resources" (required json_arg) ~doc:"JSON JobResource"
       and addressId =
         flag "address-id" (required string) ~doc:"STRING AddressId"
       and roleARN =
         flag "role-a-r-n" (required string) ~doc:"STRING RoleARN"
       and snowballType =
         flag "snowball-type" (required json_arg) ~doc:"JSON SnowballType"
       and shippingOption =
         flag "shipping-option" (required json_arg)
           ~doc:"JSON ShippingOption" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make
              ?onDeviceServiceConfiguration:(Option.map
                                               ~f:Values.OnDeviceServiceConfiguration.of_json
                                               onDeviceServiceConfiguration)
              ?description ?kmsKeyARN
              ?notification:(Option.map ~f:Values.Notification.of_json
                               notification) ?forwardingAddressId
              ?taxDocuments:(Option.map ~f:Values.TaxDocuments.of_json
                               taxDocuments)
              ?remoteManagement:(Option.map
                                   ~f:Values.RemoteManagement.of_json
                                   remoteManagement)
              ~jobType:(Values.JobType.of_json jobType)
              ~resources:(Values.JobResource.of_json resources) ~addressId
              ~roleARN
              ~snowballType:(Values.SnowballType.of_json snowballType)
              ~shippingOption:(Values.ShippingOption.of_json shippingOption)
              ()) (Some Values.CreateClusterResult.to_json)
           (Some Values.CreateClusterResult.error_to_json)])
let create_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobType = flag "job-type" (optional json_arg) ~doc:"JSON JobType"
       and resources =
         flag "resources" (optional json_arg) ~doc:"JSON JobResource"
       and onDeviceServiceConfiguration =
         flag "on-device-service-configuration" (optional json_arg)
           ~doc:"JSON OnDeviceServiceConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and addressId =
         flag "address-id" (optional string) ~doc:"STRING AddressId"
       and kmsKeyARN =
         flag "kms-key-a-r-n" (optional string) ~doc:"STRING KmsKeyARN"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and snowballCapacityPreference =
         flag "snowball-capacity-preference" (optional json_arg)
           ~doc:"JSON SnowballCapacity"
       and shippingOption =
         flag "shipping-option" (optional json_arg)
           ~doc:"JSON ShippingOption"
       and notification =
         flag "notification" (optional json_arg) ~doc:"JSON Notification"
       and clusterId =
         flag "cluster-id" (optional string) ~doc:"STRING ClusterId"
       and snowballType =
         flag "snowball-type" (optional json_arg) ~doc:"JSON SnowballType"
       and forwardingAddressId =
         flag "forwarding-address-id" (optional string)
           ~doc:"STRING AddressId"
       and taxDocuments =
         flag "tax-documents" (optional json_arg) ~doc:"JSON TaxDocuments"
       and deviceConfiguration =
         flag "device-configuration" (optional json_arg)
           ~doc:"JSON DeviceConfiguration"
       and remoteManagement =
         flag "remote-management" (optional json_arg)
           ~doc:"JSON RemoteManagement"
       and longTermPricingId =
         flag "long-term-pricing-id" (optional string)
           ~doc:"STRING LongTermPricingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job
           (Values.CreateJobRequest.make
              ?jobType:(Option.map ~f:Values.JobType.of_json jobType)
              ?resources:(Option.map ~f:Values.JobResource.of_json resources)
              ?onDeviceServiceConfiguration:(Option.map
                                               ~f:Values.OnDeviceServiceConfiguration.of_json
                                               onDeviceServiceConfiguration)
              ?description ?addressId ?kmsKeyARN ?roleARN
              ?snowballCapacityPreference:(Option.map
                                             ~f:Values.SnowballCapacity.of_json
                                             snowballCapacityPreference)
              ?shippingOption:(Option.map ~f:Values.ShippingOption.of_json
                                 shippingOption)
              ?notification:(Option.map ~f:Values.Notification.of_json
                               notification) ?clusterId
              ?snowballType:(Option.map ~f:Values.SnowballType.of_json
                               snowballType) ?forwardingAddressId
              ?taxDocuments:(Option.map ~f:Values.TaxDocuments.of_json
                               taxDocuments)
              ?deviceConfiguration:(Option.map
                                      ~f:Values.DeviceConfiguration.of_json
                                      deviceConfiguration)
              ?remoteManagement:(Option.map
                                   ~f:Values.RemoteManagement.of_json
                                   remoteManagement) ?longTermPricingId ())
           (Some Values.CreateJobResult.to_json)
           (Some Values.CreateJobResult.error_to_json)])
let create_long_term_pricing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and isLongTermPricingAutoRenew =
         flag "is-long-term-pricing-auto-renew" (optional bool)
           ~doc:"BOOL JavaBoolean"
       and snowballType =
         flag "snowball-type" (optional json_arg) ~doc:"JSON SnowballType"
       and longTermPricingType =
         flag "long-term-pricing-type" (required json_arg)
           ~doc:"JSON LongTermPricingType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_long_term_pricing
           (Values.CreateLongTermPricingRequest.make
              ?isLongTermPricingAutoRenew
              ?snowballType:(Option.map ~f:Values.SnowballType.of_json
                               snowballType)
              ~longTermPricingType:(Values.LongTermPricingType.of_json
                                      longTermPricingType) ())
           (Some Values.CreateLongTermPricingResult.to_json)
           (Some Values.CreateLongTermPricingResult.error_to_json)])
let create_return_shipping_label =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shippingOption =
         flag "shipping-option" (optional json_arg)
           ~doc:"JSON ShippingOption"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_return_shipping_label
           (Values.CreateReturnShippingLabelRequest.make
              ?shippingOption:(Option.map ~f:Values.ShippingOption.of_json
                                 shippingOption) ~jobId ())
           (Some Values.CreateReturnShippingLabelResult.to_json)
           (Some Values.CreateReturnShippingLabelResult.error_to_json)])
let describe_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addressId =
         flag "address-id" (required string) ~doc:"STRING AddressId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_address
           (Values.DescribeAddressRequest.make ~addressId ())
           (Some Values.DescribeAddressResult.to_json)
           (Some Values.DescribeAddressResult.error_to_json)])
let describe_addresses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_addresses
           (Values.DescribeAddressesRequest.make ?maxResults ?nextToken ())
           (Some Values.DescribeAddressesResult.to_json)
           (Some Values.DescribeAddressesResult.error_to_json)])
let describe_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster
           (Values.DescribeClusterRequest.make ~clusterId ())
           (Some Values.DescribeClusterResult.to_json)
           (Some Values.DescribeClusterResult.error_to_json)])
let describe_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job (Values.DescribeJobRequest.make ~jobId ())
           (Some Values.DescribeJobResult.to_json)
           (Some Values.DescribeJobResult.error_to_json)])
let describe_return_shipping_label =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_return_shipping_label
           (Values.DescribeReturnShippingLabelRequest.make ~jobId ())
           (Some Values.DescribeReturnShippingLabelResult.to_json)
           (Some Values.DescribeReturnShippingLabelResult.error_to_json)])
let get_job_manifest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_manifest (Values.GetJobManifestRequest.make ~jobId ())
           (Some Values.GetJobManifestResult.to_json)
           (Some Values.GetJobManifestResult.error_to_json)])
let get_job_unlock_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_unlock_code
           (Values.GetJobUnlockCodeRequest.make ~jobId ())
           (Some Values.GetJobUnlockCodeResult.to_json)
           (Some Values.GetJobUnlockCodeResult.error_to_json)])
let get_snowball_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_snowball_usage (Values.GetSnowballUsageRequest.make ())
           (Some Values.GetSnowballUsageResult.to_json)
           (Some Values.GetSnowballUsageResult.error_to_json)])
let get_software_updates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_software_updates
           (Values.GetSoftwareUpdatesRequest.make ~jobId ())
           (Some Values.GetSoftwareUpdatesResult.to_json)
           (Some Values.GetSoftwareUpdatesResult.error_to_json)])
let list_cluster_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cluster_jobs
           (Values.ListClusterJobsRequest.make ?maxResults ?nextToken
              ~clusterId ()) (Some Values.ListClusterJobsResult.to_json)
           (Some Values.ListClusterJobsResult.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListClustersResult.to_json)
           (Some Values.ListClustersResult.error_to_json)])
let list_compatible_images =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_compatible_images
           (Values.ListCompatibleImagesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListCompatibleImagesResult.to_json)
           (Some Values.ListCompatibleImagesResult.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListJobsResult.to_json)
           (Some Values.ListJobsResult.error_to_json)])
let list_long_term_pricing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_long_term_pricing
           (Values.ListLongTermPricingRequest.make ?maxResults ?nextToken ())
           (Some Values.ListLongTermPricingResult.to_json)
           (Some Values.ListLongTermPricingResult.error_to_json)])
let update_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and resources =
         flag "resources" (optional json_arg) ~doc:"JSON JobResource"
       and onDeviceServiceConfiguration =
         flag "on-device-service-configuration" (optional json_arg)
           ~doc:"JSON OnDeviceServiceConfiguration"
       and addressId =
         flag "address-id" (optional string) ~doc:"STRING AddressId"
       and shippingOption =
         flag "shipping-option" (optional json_arg)
           ~doc:"JSON ShippingOption"
       and notification =
         flag "notification" (optional json_arg) ~doc:"JSON Notification"
       and forwardingAddressId =
         flag "forwarding-address-id" (optional string)
           ~doc:"STRING AddressId"
       and clusterId =
         flag "cluster-id" (required string) ~doc:"STRING ClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster
           (Values.UpdateClusterRequest.make ?roleARN ?description
              ?resources:(Option.map ~f:Values.JobResource.of_json resources)
              ?onDeviceServiceConfiguration:(Option.map
                                               ~f:Values.OnDeviceServiceConfiguration.of_json
                                               onDeviceServiceConfiguration)
              ?addressId
              ?shippingOption:(Option.map ~f:Values.ShippingOption.of_json
                                 shippingOption)
              ?notification:(Option.map ~f:Values.Notification.of_json
                               notification) ?forwardingAddressId ~clusterId
              ()) (Some Values.UpdateClusterResult.to_json)
           (Some Values.UpdateClusterResult.error_to_json)])
let update_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleARN =
         flag "role-a-r-n" (optional string) ~doc:"STRING RoleARN"
       and notification =
         flag "notification" (optional json_arg) ~doc:"JSON Notification"
       and resources =
         flag "resources" (optional json_arg) ~doc:"JSON JobResource"
       and onDeviceServiceConfiguration =
         flag "on-device-service-configuration" (optional json_arg)
           ~doc:"JSON OnDeviceServiceConfiguration"
       and addressId =
         flag "address-id" (optional string) ~doc:"STRING AddressId"
       and shippingOption =
         flag "shipping-option" (optional json_arg)
           ~doc:"JSON ShippingOption"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and snowballCapacityPreference =
         flag "snowball-capacity-preference" (optional json_arg)
           ~doc:"JSON SnowballCapacity"
       and forwardingAddressId =
         flag "forwarding-address-id" (optional string)
           ~doc:"STRING AddressId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job
           (Values.UpdateJobRequest.make ?roleARN
              ?notification:(Option.map ~f:Values.Notification.of_json
                               notification)
              ?resources:(Option.map ~f:Values.JobResource.of_json resources)
              ?onDeviceServiceConfiguration:(Option.map
                                               ~f:Values.OnDeviceServiceConfiguration.of_json
                                               onDeviceServiceConfiguration)
              ?addressId
              ?shippingOption:(Option.map ~f:Values.ShippingOption.of_json
                                 shippingOption) ?description
              ?snowballCapacityPreference:(Option.map
                                             ~f:Values.SnowballCapacity.of_json
                                             snowballCapacityPreference)
              ?forwardingAddressId ~jobId ())
           (Some Values.UpdateJobResult.to_json)
           (Some Values.UpdateJobResult.error_to_json)])
let update_job_shipment_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and shipmentState =
         flag "shipment-state" (required json_arg) ~doc:"JSON ShipmentState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job_shipment_state
           (Values.UpdateJobShipmentStateRequest.make ~jobId
              ~shipmentState:(Values.ShipmentState.of_json shipmentState) ())
           (Some Values.UpdateJobShipmentStateResult.to_json)
           (Some Values.UpdateJobShipmentStateResult.error_to_json)])
let update_long_term_pricing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and replacementJob =
         flag "replacement-job" (optional string) ~doc:"STRING JobId"
       and isLongTermPricingAutoRenew =
         flag "is-long-term-pricing-auto-renew" (optional bool)
           ~doc:"BOOL JavaBoolean"
       and longTermPricingId =
         flag "long-term-pricing-id" (required string)
           ~doc:"STRING LongTermPricingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_long_term_pricing
           (Values.UpdateLongTermPricingRequest.make ?replacementJob
              ?isLongTermPricingAutoRenew ~longTermPricingId ())
           (Some Values.UpdateLongTermPricingResult.to_json)
           (Some Values.UpdateLongTermPricingResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("cancel-cluster", cancel_cluster);
    ("cancel-job", cancel_job);
    ("create-address", create_address);
    ("create-cluster", create_cluster);
    ("create-job", create_job);
    ("create-long-term-pricing", create_long_term_pricing);
    ("create-return-shipping-label", create_return_shipping_label);
    ("describe-address", describe_address);
    ("describe-addresses", describe_addresses);
    ("describe-cluster", describe_cluster);
    ("describe-job", describe_job);
    ("describe-return-shipping-label", describe_return_shipping_label);
    ("get-job-manifest", get_job_manifest);
    ("get-job-unlock-code", get_job_unlock_code);
    ("get-snowball-usage", get_snowball_usage);
    ("get-software-updates", get_software_updates);
    ("list-cluster-jobs", list_cluster_jobs);
    ("list-clusters", list_clusters);
    ("list-compatible-images", list_compatible_images);
    ("list-jobs", list_jobs);
    ("list-long-term-pricing", list_long_term_pricing);
    ("update-cluster", update_cluster);
    ("update-job", update_job);
    ("update-job-shipment-state", update_job_shipment_state);
    ("update-long-term-pricing", update_long_term_pricing)]
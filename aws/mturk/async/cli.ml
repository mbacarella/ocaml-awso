(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_qualification_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integerValue =
         flag "integer-value" (optional int) ~doc:"INT Integer"
       and qualificationRequestId =
         flag "qualification-request-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_qualification_request
           (Values.AcceptQualificationRequestRequest.make ?integerValue
              ~qualificationRequestId ())
           (Some Values.AcceptQualificationRequestResponse.to_json)
           (Some Values.AcceptQualificationRequestResponse.error_to_json)])
let approve_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requesterFeedback =
         flag "requester-feedback" (optional string) ~doc:"STRING String"
       and overrideRejection =
         flag "override-rejection" (optional bool) ~doc:"BOOL Boolean"
       and assignmentId =
         flag "assignment-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.approve_assignment
           (Values.ApproveAssignmentRequest.make ?requesterFeedback
              ?overrideRejection ~assignmentId ())
           (Some Values.ApproveAssignmentResponse.to_json)
           (Some Values.ApproveAssignmentResponse.error_to_json)])
let associate_qualification_with_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integerValue =
         flag "integer-value" (optional int) ~doc:"INT Integer"
       and sendNotification =
         flag "send-notification" (optional bool) ~doc:"BOOL Boolean"
       and qualificationTypeId =
         flag "qualification-type-id" (required string)
           ~doc:"STRING EntityId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING CustomerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_qualification_with_worker
           (Values.AssociateQualificationWithWorkerRequest.make ?integerValue
              ?sendNotification ~qualificationTypeId ~workerId ())
           (Some Values.AssociateQualificationWithWorkerResponse.to_json)
           (Some
              Values.AssociateQualificationWithWorkerResponse.error_to_json)])
let create_additional_assignments_for_h_i_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and uniqueRequestToken =
         flag "unique-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and hITId = flag "h-i-t-id" (required string) ~doc:"STRING EntityId"
       and numberOfAdditionalAssignments =
         flag "number-of-additional-assignments" (required int)
           ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_additional_assignments_for_h_i_t
           (Values.CreateAdditionalAssignmentsForHITRequest.make
              ?uniqueRequestToken ~hITId ~numberOfAdditionalAssignments ())
           (Some Values.CreateAdditionalAssignmentsForHITResponse.to_json)
           (Some
              Values.CreateAdditionalAssignmentsForHITResponse.error_to_json)])
let create_h_i_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxAssignments =
         flag "max-assignments" (optional int) ~doc:"INT Integer"
       and autoApprovalDelayInSeconds =
         flag "auto-approval-delay-in-seconds" (optional json_arg)
           ~doc:"JSON Long"
       and keywords = flag "keywords" (optional string) ~doc:"STRING String"
       and question = flag "question" (optional string) ~doc:"STRING String"
       and requesterAnnotation =
         flag "requester-annotation" (optional string) ~doc:"STRING String"
       and qualificationRequirements =
         flag "qualification-requirements" (optional json_arg)
           ~doc:"JSON QualificationRequirementList"
       and uniqueRequestToken =
         flag "unique-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and assignmentReviewPolicy =
         flag "assignment-review-policy" (optional json_arg)
           ~doc:"JSON ReviewPolicy"
       and hITReviewPolicy =
         flag "h-i-t-review-policy" (optional json_arg)
           ~doc:"JSON ReviewPolicy"
       and hITLayoutId =
         flag "h-i-t-layout-id" (optional string) ~doc:"STRING EntityId"
       and hITLayoutParameters =
         flag "h-i-t-layout-parameters" (optional json_arg)
           ~doc:"JSON HITLayoutParameterList"
       and lifetimeInSeconds =
         flag "lifetime-in-seconds" (required json_arg) ~doc:"JSON Long"
       and assignmentDurationInSeconds =
         flag "assignment-duration-in-seconds" (required json_arg)
           ~doc:"JSON Long"
       and reward =
         flag "reward" (required string) ~doc:"STRING CurrencyAmount"
       and title = flag "title" (required string) ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_h_i_t
           (Values.CreateHITRequest.make ?maxAssignments
              ?autoApprovalDelayInSeconds:(Option.map ~f:Values.Long.of_json
                                             autoApprovalDelayInSeconds)
              ?keywords ?question ?requesterAnnotation
              ?qualificationRequirements:(Option.map
                                            ~f:Values.QualificationRequirementList.of_json
                                            qualificationRequirements)
              ?uniqueRequestToken
              ?assignmentReviewPolicy:(Option.map
                                         ~f:Values.ReviewPolicy.of_json
                                         assignmentReviewPolicy)
              ?hITReviewPolicy:(Option.map ~f:Values.ReviewPolicy.of_json
                                  hITReviewPolicy) ?hITLayoutId
              ?hITLayoutParameters:(Option.map
                                      ~f:Values.HITLayoutParameterList.of_json
                                      hITLayoutParameters)
              ~lifetimeInSeconds:(Values.Long.of_json lifetimeInSeconds)
              ~assignmentDurationInSeconds:(Values.Long.of_json
                                              assignmentDurationInSeconds)
              ~reward ~title ~description ())
           (Some Values.CreateHITResponse.to_json)
           (Some Values.CreateHITResponse.error_to_json)])
let create_h_i_t_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoApprovalDelayInSeconds =
         flag "auto-approval-delay-in-seconds" (optional json_arg)
           ~doc:"JSON Long"
       and keywords = flag "keywords" (optional string) ~doc:"STRING String"
       and qualificationRequirements =
         flag "qualification-requirements" (optional json_arg)
           ~doc:"JSON QualificationRequirementList"
       and assignmentDurationInSeconds =
         flag "assignment-duration-in-seconds" (required json_arg)
           ~doc:"JSON Long"
       and reward =
         flag "reward" (required string) ~doc:"STRING CurrencyAmount"
       and title = flag "title" (required string) ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_h_i_t_type
           (Values.CreateHITTypeRequest.make
              ?autoApprovalDelayInSeconds:(Option.map ~f:Values.Long.of_json
                                             autoApprovalDelayInSeconds)
              ?keywords
              ?qualificationRequirements:(Option.map
                                            ~f:Values.QualificationRequirementList.of_json
                                            qualificationRequirements)
              ~assignmentDurationInSeconds:(Values.Long.of_json
                                              assignmentDurationInSeconds)
              ~reward ~title ~description ())
           (Some Values.CreateHITTypeResponse.to_json)
           (Some Values.CreateHITTypeResponse.error_to_json)])
let create_h_i_t_with_h_i_t_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxAssignments =
         flag "max-assignments" (optional int) ~doc:"INT Integer"
       and question = flag "question" (optional string) ~doc:"STRING String"
       and requesterAnnotation =
         flag "requester-annotation" (optional string) ~doc:"STRING String"
       and uniqueRequestToken =
         flag "unique-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and assignmentReviewPolicy =
         flag "assignment-review-policy" (optional json_arg)
           ~doc:"JSON ReviewPolicy"
       and hITReviewPolicy =
         flag "h-i-t-review-policy" (optional json_arg)
           ~doc:"JSON ReviewPolicy"
       and hITLayoutId =
         flag "h-i-t-layout-id" (optional string) ~doc:"STRING EntityId"
       and hITLayoutParameters =
         flag "h-i-t-layout-parameters" (optional json_arg)
           ~doc:"JSON HITLayoutParameterList"
       and hITTypeId =
         flag "h-i-t-type-id" (required string) ~doc:"STRING EntityId"
       and lifetimeInSeconds =
         flag "lifetime-in-seconds" (required json_arg) ~doc:"JSON Long" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_h_i_t_with_h_i_t_type
           (Values.CreateHITWithHITTypeRequest.make ?maxAssignments ?question
              ?requesterAnnotation ?uniqueRequestToken
              ?assignmentReviewPolicy:(Option.map
                                         ~f:Values.ReviewPolicy.of_json
                                         assignmentReviewPolicy)
              ?hITReviewPolicy:(Option.map ~f:Values.ReviewPolicy.of_json
                                  hITReviewPolicy) ?hITLayoutId
              ?hITLayoutParameters:(Option.map
                                      ~f:Values.HITLayoutParameterList.of_json
                                      hITLayoutParameters) ~hITTypeId
              ~lifetimeInSeconds:(Values.Long.of_json lifetimeInSeconds) ())
           (Some Values.CreateHITWithHITTypeResponse.to_json)
           (Some Values.CreateHITWithHITTypeResponse.error_to_json)])
let create_qualification_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keywords = flag "keywords" (optional string) ~doc:"STRING String"
       and retryDelayInSeconds =
         flag "retry-delay-in-seconds" (optional json_arg) ~doc:"JSON Long"
       and test = flag "test" (optional string) ~doc:"STRING String"
       and answerKey =
         flag "answer-key" (optional string) ~doc:"STRING String"
       and testDurationInSeconds =
         flag "test-duration-in-seconds" (optional json_arg) ~doc:"JSON Long"
       and autoGranted =
         flag "auto-granted" (optional bool) ~doc:"BOOL Boolean"
       and autoGrantedValue =
         flag "auto-granted-value" (optional int) ~doc:"INT Integer"
       and name = flag "name" (required string) ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String"
       and qualificationTypeStatus =
         flag "qualification-type-status" (required json_arg)
           ~doc:"JSON QualificationTypeStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_qualification_type
           (Values.CreateQualificationTypeRequest.make ?keywords
              ?retryDelayInSeconds:(Option.map ~f:Values.Long.of_json
                                      retryDelayInSeconds) ?test ?answerKey
              ?testDurationInSeconds:(Option.map ~f:Values.Long.of_json
                                        testDurationInSeconds) ?autoGranted
              ?autoGrantedValue ~name ~description
              ~qualificationTypeStatus:(Values.QualificationTypeStatus.of_json
                                          qualificationTypeStatus) ())
           (Some Values.CreateQualificationTypeResponse.to_json)
           (Some Values.CreateQualificationTypeResponse.error_to_json)])
let create_worker_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING CustomerId"
       and reason = flag "reason" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_worker_block
           (Values.CreateWorkerBlockRequest.make ~workerId ~reason ())
           (Some Values.CreateWorkerBlockResponse.to_json)
           (Some Values.CreateWorkerBlockResponse.error_to_json)])
let delete_h_i_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hITId = flag "h-i-t-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_h_i_t (Values.DeleteHITRequest.make ~hITId ())
           (Some Values.DeleteHITResponse.to_json)
           (Some Values.DeleteHITResponse.error_to_json)])
let delete_qualification_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualificationTypeId =
         flag "qualification-type-id" (required string)
           ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_qualification_type
           (Values.DeleteQualificationTypeRequest.make ~qualificationTypeId
              ()) (Some Values.DeleteQualificationTypeResponse.to_json)
           (Some Values.DeleteQualificationTypeResponse.error_to_json)])
let delete_worker_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason = flag "reason" (optional string) ~doc:"STRING String"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING CustomerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_worker_block
           (Values.DeleteWorkerBlockRequest.make ?reason ~workerId ())
           (Some Values.DeleteWorkerBlockResponse.to_json)
           (Some Values.DeleteWorkerBlockResponse.error_to_json)])
let disassociate_qualification_from_worker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason = flag "reason" (optional string) ~doc:"STRING String"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING CustomerId"
       and qualificationTypeId =
         flag "qualification-type-id" (required string)
           ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_qualification_from_worker
           (Values.DisassociateQualificationFromWorkerRequest.make ?reason
              ~workerId ~qualificationTypeId ())
           (Some Values.DisassociateQualificationFromWorkerResponse.to_json)
           (Some
              Values.DisassociateQualificationFromWorkerResponse.error_to_json)])
let get_account_balance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_balance (Values.GetAccountBalanceRequest.make ())
           (Some Values.GetAccountBalanceResponse.to_json)
           (Some Values.GetAccountBalanceResponse.error_to_json)])
let get_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assignmentId =
         flag "assignment-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_assignment
           (Values.GetAssignmentRequest.make ~assignmentId ())
           (Some Values.GetAssignmentResponse.to_json)
           (Some Values.GetAssignmentResponse.error_to_json)])
let get_file_upload_u_r_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assignmentId =
         flag "assignment-id" (required string) ~doc:"STRING EntityId"
       and questionIdentifier =
         flag "question-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_file_upload_u_r_l
           (Values.GetFileUploadURLRequest.make ~assignmentId
              ~questionIdentifier ())
           (Some Values.GetFileUploadURLResponse.to_json)
           (Some Values.GetFileUploadURLResponse.error_to_json)])
let get_h_i_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hITId = flag "h-i-t-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_h_i_t (Values.GetHITRequest.make ~hITId ())
           (Some Values.GetHITResponse.to_json)
           (Some Values.GetHITResponse.error_to_json)])
let get_qualification_score =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualificationTypeId =
         flag "qualification-type-id" (required string)
           ~doc:"STRING EntityId"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING CustomerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_qualification_score
           (Values.GetQualificationScoreRequest.make ~qualificationTypeId
              ~workerId ())
           (Some Values.GetQualificationScoreResponse.to_json)
           (Some Values.GetQualificationScoreResponse.error_to_json)])
let get_qualification_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualificationTypeId =
         flag "qualification-type-id" (required string)
           ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_qualification_type
           (Values.GetQualificationTypeRequest.make ~qualificationTypeId ())
           (Some Values.GetQualificationTypeResponse.to_json)
           (Some Values.GetQualificationTypeResponse.error_to_json)])
let list_assignments_for_h_i_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize"
       and assignmentStatuses =
         flag "assignment-statuses" (optional json_arg)
           ~doc:"JSON AssignmentStatusList"
       and hITId = flag "h-i-t-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_assignments_for_h_i_t
           (Values.ListAssignmentsForHITRequest.make ?nextToken ?maxResults
              ?assignmentStatuses:(Option.map
                                     ~f:Values.AssignmentStatusList.of_json
                                     assignmentStatuses) ~hITId ())
           (Some Values.ListAssignmentsForHITResponse.to_json)
           (Some Values.ListAssignmentsForHITResponse.error_to_json)])
let list_bonus_payments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hITId = flag "h-i-t-id" (optional string) ~doc:"STRING EntityId"
       and assignmentId =
         flag "assignment-id" (optional string) ~doc:"STRING EntityId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bonus_payments
           (Values.ListBonusPaymentsRequest.make ?hITId ?assignmentId
              ?nextToken ?maxResults ())
           (Some Values.ListBonusPaymentsResponse.to_json)
           (Some Values.ListBonusPaymentsResponse.error_to_json)])
let list_h_i_ts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_h_i_ts
           (Values.ListHITsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListHITsResponse.to_json)
           (Some Values.ListHITsResponse.error_to_json)])
let list_h_i_ts_for_qualification_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize"
       and qualificationTypeId =
         flag "qualification-type-id" (required string)
           ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_h_i_ts_for_qualification_type
           (Values.ListHITsForQualificationTypeRequest.make ?nextToken
              ?maxResults ~qualificationTypeId ())
           (Some Values.ListHITsForQualificationTypeResponse.to_json)
           (Some Values.ListHITsForQualificationTypeResponse.error_to_json)])
let list_qualification_requests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualificationTypeId =
         flag "qualification-type-id" (optional string)
           ~doc:"STRING EntityId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_qualification_requests
           (Values.ListQualificationRequestsRequest.make ?qualificationTypeId
              ?nextToken ?maxResults ())
           (Some Values.ListQualificationRequestsResponse.to_json)
           (Some Values.ListQualificationRequestsResponse.error_to_json)])
let list_qualification_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and query = flag "query" (optional string) ~doc:"STRING String"
       and mustBeOwnedByCaller =
         flag "must-be-owned-by-caller" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize"
       and mustBeRequestable =
         flag "must-be-requestable" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_qualification_types
           (Values.ListQualificationTypesRequest.make ?query
              ?mustBeOwnedByCaller ?nextToken ?maxResults ~mustBeRequestable
              ()) (Some Values.ListQualificationTypesResponse.to_json)
           (Some Values.ListQualificationTypesResponse.error_to_json)])
let list_review_policy_results_for_h_i_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyLevels =
         flag "policy-levels" (optional json_arg)
           ~doc:"JSON ReviewPolicyLevelList"
       and retrieveActions =
         flag "retrieve-actions" (optional bool) ~doc:"BOOL Boolean"
       and retrieveResults =
         flag "retrieve-results" (optional bool) ~doc:"BOOL Boolean"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize"
       and hITId = flag "h-i-t-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_review_policy_results_for_h_i_t
           (Values.ListReviewPolicyResultsForHITRequest.make
              ?policyLevels:(Option.map
                               ~f:Values.ReviewPolicyLevelList.of_json
                               policyLevels) ?retrieveActions
              ?retrieveResults ?nextToken ?maxResults ~hITId ())
           (Some Values.ListReviewPolicyResultsForHITResponse.to_json)
           (Some Values.ListReviewPolicyResultsForHITResponse.error_to_json)])
let list_reviewable_h_i_ts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hITTypeId =
         flag "h-i-t-type-id" (optional string) ~doc:"STRING EntityId"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ReviewableHITStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reviewable_h_i_ts
           (Values.ListReviewableHITsRequest.make ?hITTypeId
              ?status:(Option.map ~f:Values.ReviewableHITStatus.of_json
                         status) ?nextToken ?maxResults ())
           (Some Values.ListReviewableHITsResponse.to_json)
           (Some Values.ListReviewableHITsResponse.error_to_json)])
let list_worker_blocks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_worker_blocks
           (Values.ListWorkerBlocksRequest.make ?nextToken ?maxResults ())
           (Some Values.ListWorkerBlocksResponse.to_json)
           (Some Values.ListWorkerBlocksResponse.error_to_json)])
let list_workers_with_qualification_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON QualificationStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultSize"
       and qualificationTypeId =
         flag "qualification-type-id" (required string)
           ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workers_with_qualification_type
           (Values.ListWorkersWithQualificationTypeRequest.make
              ?status:(Option.map ~f:Values.QualificationStatus.of_json
                         status) ?nextToken ?maxResults ~qualificationTypeId
              ())
           (Some Values.ListWorkersWithQualificationTypeResponse.to_json)
           (Some
              Values.ListWorkersWithQualificationTypeResponse.error_to_json)])
let notify_workers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subject = flag "subject" (required string) ~doc:"STRING String"
       and messageText =
         flag "message-text" (required string) ~doc:"STRING String"
       and workerIds =
         flag "worker-ids" (required json_arg) ~doc:"JSON CustomerIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.notify_workers
           (Values.NotifyWorkersRequest.make ~subject ~messageText
              ~workerIds:(Values.CustomerIdList.of_json workerIds) ())
           (Some Values.NotifyWorkersResponse.to_json)
           (Some Values.NotifyWorkersResponse.error_to_json)])
let reject_assignment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assignmentId =
         flag "assignment-id" (required string) ~doc:"STRING EntityId"
       and requesterFeedback =
         flag "requester-feedback" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_assignment
           (Values.RejectAssignmentRequest.make ~assignmentId
              ~requesterFeedback ())
           (Some Values.RejectAssignmentResponse.to_json)
           (Some Values.RejectAssignmentResponse.error_to_json)])
let reject_qualification_request =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reason = flag "reason" (optional string) ~doc:"STRING String"
       and qualificationRequestId =
         flag "qualification-request-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_qualification_request
           (Values.RejectQualificationRequestRequest.make ?reason
              ~qualificationRequestId ())
           (Some Values.RejectQualificationRequestResponse.to_json)
           (Some Values.RejectQualificationRequestResponse.error_to_json)])
let send_bonus =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and uniqueRequestToken =
         flag "unique-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and workerId =
         flag "worker-id" (required string) ~doc:"STRING CustomerId"
       and bonusAmount =
         flag "bonus-amount" (required string) ~doc:"STRING CurrencyAmount"
       and assignmentId =
         flag "assignment-id" (required string) ~doc:"STRING EntityId"
       and reason = flag "reason" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_bonus
           (Values.SendBonusRequest.make ?uniqueRequestToken ~workerId
              ~bonusAmount ~assignmentId ~reason ())
           (Some Values.SendBonusResponse.to_json)
           (Some Values.SendBonusResponse.error_to_json)])
let send_test_event_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notification =
         flag "notification" (required json_arg)
           ~doc:"JSON NotificationSpecification"
       and testEventType =
         flag "test-event-type" (required json_arg) ~doc:"JSON EventType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_test_event_notification
           (Values.SendTestEventNotificationRequest.make
              ~notification:(Values.NotificationSpecification.of_json
                               notification)
              ~testEventType:(Values.EventType.of_json testEventType) ())
           (Some Values.SendTestEventNotificationResponse.to_json)
           (Some Values.SendTestEventNotificationResponse.error_to_json)])
let update_expiration_for_h_i_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hITId = flag "h-i-t-id" (required string) ~doc:"STRING EntityId"
       and expireAt =
         flag "expire-at" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_expiration_for_h_i_t
           (Values.UpdateExpirationForHITRequest.make ~hITId
              ~expireAt:(Values.Timestamp.of_json expireAt) ())
           (Some Values.UpdateExpirationForHITResponse.to_json)
           (Some Values.UpdateExpirationForHITResponse.error_to_json)])
let update_h_i_t_review_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revert = flag "revert" (optional bool) ~doc:"BOOL Boolean"
       and hITId = flag "h-i-t-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_h_i_t_review_status
           (Values.UpdateHITReviewStatusRequest.make ?revert ~hITId ())
           (Some Values.UpdateHITReviewStatusResponse.to_json)
           (Some Values.UpdateHITReviewStatusResponse.error_to_json)])
let update_h_i_t_type_of_h_i_t =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hITId = flag "h-i-t-id" (required string) ~doc:"STRING EntityId"
       and hITTypeId =
         flag "h-i-t-type-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_h_i_t_type_of_h_i_t
           (Values.UpdateHITTypeOfHITRequest.make ~hITId ~hITTypeId ())
           (Some Values.UpdateHITTypeOfHITResponse.to_json)
           (Some Values.UpdateHITTypeOfHITResponse.error_to_json)])
let update_notification_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notification =
         flag "notification" (optional json_arg)
           ~doc:"JSON NotificationSpecification"
       and active = flag "active" (optional bool) ~doc:"BOOL Boolean"
       and hITTypeId =
         flag "h-i-t-type-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_notification_settings
           (Values.UpdateNotificationSettingsRequest.make
              ?notification:(Option.map
                               ~f:Values.NotificationSpecification.of_json
                               notification) ?active ~hITTypeId ())
           (Some Values.UpdateNotificationSettingsResponse.to_json)
           (Some Values.UpdateNotificationSettingsResponse.error_to_json)])
let update_qualification_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and qualificationTypeStatus =
         flag "qualification-type-status" (optional json_arg)
           ~doc:"JSON QualificationTypeStatus"
       and test = flag "test" (optional string) ~doc:"STRING String"
       and answerKey =
         flag "answer-key" (optional string) ~doc:"STRING String"
       and testDurationInSeconds =
         flag "test-duration-in-seconds" (optional json_arg) ~doc:"JSON Long"
       and retryDelayInSeconds =
         flag "retry-delay-in-seconds" (optional json_arg) ~doc:"JSON Long"
       and autoGranted =
         flag "auto-granted" (optional bool) ~doc:"BOOL Boolean"
       and autoGrantedValue =
         flag "auto-granted-value" (optional int) ~doc:"INT Integer"
       and qualificationTypeId =
         flag "qualification-type-id" (required string)
           ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_qualification_type
           (Values.UpdateQualificationTypeRequest.make ?description
              ?qualificationTypeStatus:(Option.map
                                          ~f:Values.QualificationTypeStatus.of_json
                                          qualificationTypeStatus) ?test
              ?answerKey
              ?testDurationInSeconds:(Option.map ~f:Values.Long.of_json
                                        testDurationInSeconds)
              ?retryDelayInSeconds:(Option.map ~f:Values.Long.of_json
                                      retryDelayInSeconds) ?autoGranted
              ?autoGrantedValue ~qualificationTypeId ())
           (Some Values.UpdateQualificationTypeResponse.to_json)
           (Some Values.UpdateQualificationTypeResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-qualification-request", accept_qualification_request);
    ("approve-assignment", approve_assignment);
    ("associate-qualification-with-worker",
      associate_qualification_with_worker);
    ("create-additional-assignments-for-h-i-t",
      create_additional_assignments_for_h_i_t);
    ("create-h-i-t", create_h_i_t);
    ("create-h-i-t-type", create_h_i_t_type);
    ("create-h-i-t-with-h-i-t-type", create_h_i_t_with_h_i_t_type);
    ("create-qualification-type", create_qualification_type);
    ("create-worker-block", create_worker_block);
    ("delete-h-i-t", delete_h_i_t);
    ("delete-qualification-type", delete_qualification_type);
    ("delete-worker-block", delete_worker_block);
    ("disassociate-qualification-from-worker",
      disassociate_qualification_from_worker);
    ("get-account-balance", get_account_balance);
    ("get-assignment", get_assignment);
    ("get-file-upload-u-r-l", get_file_upload_u_r_l);
    ("get-h-i-t", get_h_i_t);
    ("get-qualification-score", get_qualification_score);
    ("get-qualification-type", get_qualification_type);
    ("list-assignments-for-h-i-t", list_assignments_for_h_i_t);
    ("list-bonus-payments", list_bonus_payments);
    ("list-h-i-ts", list_h_i_ts);
    ("list-h-i-ts-for-qualification-type",
      list_h_i_ts_for_qualification_type);
    ("list-qualification-requests", list_qualification_requests);
    ("list-qualification-types", list_qualification_types);
    ("list-review-policy-results-for-h-i-t",
      list_review_policy_results_for_h_i_t);
    ("list-reviewable-h-i-ts", list_reviewable_h_i_ts);
    ("list-worker-blocks", list_worker_blocks);
    ("list-workers-with-qualification-type",
      list_workers_with_qualification_type);
    ("notify-workers", notify_workers);
    ("reject-assignment", reject_assignment);
    ("reject-qualification-request", reject_qualification_request);
    ("send-bonus", send_bonus);
    ("send-test-event-notification", send_test_event_notification);
    ("update-expiration-for-h-i-t", update_expiration_for_h_i_t);
    ("update-h-i-t-review-status", update_h_i_t_review_status);
    ("update-h-i-t-type-of-h-i-t", update_h_i_t_type_of_h_i_t);
    ("update-notification-settings", update_notification_settings);
    ("update-qualification-type", update_qualification_type)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let apply_pending_maintenance_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applyOn =
         flag "apply-on" (optional string) ~doc:"STRING InputString"
       and applyAction =
         flag "apply-action" (required string) ~doc:"STRING InputString"
       and optInType =
         flag "opt-in-type" (required json_arg) ~doc:"JSON OptInType"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING InputString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.apply_pending_maintenance_action
           (Values.ApplyPendingMaintenanceActionInput.make ?applyOn
              ~applyAction ~optInType:(Values.OptInType.of_json optInType)
              ~resourceArn ())
           (Some Values.ApplyPendingMaintenanceActionOutput.to_json)
           (Some Values.ApplyPendingMaintenanceActionOutput.error_to_json)])
let copy_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and copyTags = flag "copy-tags" (optional bool) ~doc:"BOOL Boolean"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and snapshotArn =
         flag "snapshot-arn" (required string) ~doc:"STRING String"
       and targetSnapshotName =
         flag "target-snapshot-name" (required string)
           ~doc:"STRING CopyClusterSnapshotInputTargetSnapshotNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_cluster_snapshot
           (Values.CopyClusterSnapshotInput.make ?copyTags ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~snapshotArn
              ~targetSnapshotName ())
           (Some Values.CopyClusterSnapshotOutput.to_json)
           (Some Values.CopyClusterSnapshotOutput.error_to_json)])
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int) ~doc:"INT Integer"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and shardInstanceCount =
         flag "shard-instance-count" (optional int) ~doc:"INT Integer"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON StringList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON StringList"
       and adminUserName =
         flag "admin-user-name" (required string) ~doc:"STRING String"
       and adminUserPassword =
         flag "admin-user-password" (required string) ~doc:"STRING Password"
       and authType = flag "auth-type" (required json_arg) ~doc:"JSON Auth"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and shardCapacity =
         flag "shard-capacity" (required int) ~doc:"INT Integer"
       and shardCount = flag "shard-count" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterInput.make ?backupRetentionPeriod
              ?clientToken ?kmsKeyId ?preferredBackupWindow
              ?preferredMaintenanceWindow ?shardInstanceCount
              ?subnetIds:(Option.map ~f:Values.StringList.of_json subnetIds)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?vpcSecurityGroupIds:(Option.map ~f:Values.StringList.of_json
                                      vpcSecurityGroupIds) ~adminUserName
              ~adminUserPassword ~authType:(Values.Auth.of_json authType)
              ~clusterName ~shardCapacity ~shardCount ())
           (Some Values.CreateClusterOutput.to_json)
           (Some Values.CreateClusterOutput.error_to_json)])
let create_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING String"
       and snapshotName =
         flag "snapshot-name" (required string)
           ~doc:"STRING CreateClusterSnapshotInputSnapshotNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster_snapshot
           (Values.CreateClusterSnapshotInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~clusterArn
              ~snapshotName ())
           (Some Values.CreateClusterSnapshotOutput.to_json)
           (Some Values.CreateClusterSnapshotOutput.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster (Values.DeleteClusterInput.make ~clusterArn ())
           (Some Values.DeleteClusterOutput.to_json)
           (Some Values.DeleteClusterOutput.error_to_json)])
let delete_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotArn =
         flag "snapshot-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster_snapshot
           (Values.DeleteClusterSnapshotInput.make ~snapshotArn ())
           (Some Values.DeleteClusterSnapshotOutput.to_json)
           (Some Values.DeleteClusterSnapshotOutput.error_to_json)])
let get_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cluster (Values.GetClusterInput.make ~clusterArn ())
           (Some Values.GetClusterOutput.to_json)
           (Some Values.GetClusterOutput.error_to_json)])
let get_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotArn =
         flag "snapshot-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cluster_snapshot
           (Values.GetClusterSnapshotInput.make ~snapshotArn ())
           (Some Values.GetClusterSnapshotOutput.to_json)
           (Some Values.GetClusterSnapshotOutput.error_to_json)])
let get_pending_maintenance_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING InputString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_pending_maintenance_action
           (Values.GetPendingMaintenanceActionInput.make ~resourceArn ())
           (Some Values.GetPendingMaintenanceActionOutput.to_json)
           (Some Values.GetPendingMaintenanceActionOutput.error_to_json)])
let list_cluster_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListClusterSnapshotsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and snapshotType =
         flag "snapshot-type" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cluster_snapshots
           (Values.ListClusterSnapshotsInput.make ?clusterArn ?maxResults
              ?nextToken ?snapshotType ())
           (Some Values.ListClusterSnapshotsOutput.to_json)
           (Some Values.ListClusterSnapshotsOutput.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListClustersInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersInput.make ?maxResults ?nextToken ())
           (Some Values.ListClustersOutput.to_json)
           (Some Values.ListClustersOutput.error_to_json)])
let list_pending_maintenance_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListPendingMaintenanceActionsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pending_maintenance_actions
           (Values.ListPendingMaintenanceActionsInput.make ?maxResults
              ?nextToken ())
           (Some Values.ListPendingMaintenanceActionsOutput.to_json)
           (Some Values.ListPendingMaintenanceActionsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let restore_cluster_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and shardCapacity =
         flag "shard-capacity" (optional int) ~doc:"INT Integer"
       and shardInstanceCount =
         flag "shard-instance-count" (optional int) ~doc:"INT Integer"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON StringList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON StringList"
       and clusterName =
         flag "cluster-name" (required string) ~doc:"STRING String"
       and snapshotArn =
         flag "snapshot-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_cluster_from_snapshot
           (Values.RestoreClusterFromSnapshotInput.make ?kmsKeyId
              ?shardCapacity ?shardInstanceCount
              ?subnetIds:(Option.map ~f:Values.StringList.of_json subnetIds)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?vpcSecurityGroupIds:(Option.map ~f:Values.StringList.of_json
                                      vpcSecurityGroupIds) ~clusterName
              ~snapshotArn ())
           (Some Values.RestoreClusterFromSnapshotOutput.to_json)
           (Some Values.RestoreClusterFromSnapshotOutput.error_to_json)])
let start_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_cluster (Values.StartClusterInput.make ~clusterArn ())
           (Some Values.StartClusterOutput.to_json)
           (Some Values.StartClusterOutput.error_to_json)])
let stop_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_cluster (Values.StopClusterInput.make ~clusterArn ())
           (Some Values.StopClusterOutput.to_json)
           (Some Values.StopClusterOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminUserPassword =
         flag "admin-user-password" (optional string) ~doc:"STRING Password"
       and authType = flag "auth-type" (optional json_arg) ~doc:"JSON Auth"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int) ~doc:"INT Integer"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and shardCapacity =
         flag "shard-capacity" (optional int) ~doc:"INT Integer"
       and shardCount = flag "shard-count" (optional int) ~doc:"INT Integer"
       and shardInstanceCount =
         flag "shard-instance-count" (optional int) ~doc:"INT Integer"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON StringList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON StringList"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster
           (Values.UpdateClusterInput.make ?adminUserPassword
              ?authType:(Option.map ~f:Values.Auth.of_json authType)
              ?backupRetentionPeriod ?clientToken ?preferredBackupWindow
              ?preferredMaintenanceWindow ?shardCapacity ?shardCount
              ?shardInstanceCount
              ?subnetIds:(Option.map ~f:Values.StringList.of_json subnetIds)
              ?vpcSecurityGroupIds:(Option.map ~f:Values.StringList.of_json
                                      vpcSecurityGroupIds) ~clusterArn ())
           (Some Values.UpdateClusterOutput.to_json)
           (Some Values.UpdateClusterOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("apply-pending-maintenance-action", apply_pending_maintenance_action);
    ("copy-cluster-snapshot", copy_cluster_snapshot);
    ("create-cluster", create_cluster);
    ("create-cluster-snapshot", create_cluster_snapshot);
    ("delete-cluster", delete_cluster);
    ("delete-cluster-snapshot", delete_cluster_snapshot);
    ("get-cluster", get_cluster);
    ("get-cluster-snapshot", get_cluster_snapshot);
    ("get-pending-maintenance-action", get_pending_maintenance_action);
    ("list-cluster-snapshots", list_cluster_snapshots);
    ("list-clusters", list_clusters);
    ("list-pending-maintenance-actions", list_pending_maintenance_actions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("restore-cluster-from-snapshot", restore_cluster_from_snapshot);
    ("start-cluster", start_cluster);
    ("stop-cluster", stop_cluster);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-cluster", update_cluster)]
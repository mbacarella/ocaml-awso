(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchDeleteTaxRegistration: (BatchDeleteTaxRegistrationRequest.t,
  BatchDeleteTaxRegistrationResponse.t,
  BatchDeleteTaxRegistrationResponse.error) t 
  | BatchGetTaxExemptions: (BatchGetTaxExemptionsRequest.t,
  BatchGetTaxExemptionsResponse.t, BatchGetTaxExemptionsResponse.error) t 
  | BatchPutTaxRegistration: (BatchPutTaxRegistrationRequest.t,
  BatchPutTaxRegistrationResponse.t, BatchPutTaxRegistrationResponse.error) t
  
  | DeleteSupplementalTaxRegistration:
  (DeleteSupplementalTaxRegistrationRequest.t,
  DeleteSupplementalTaxRegistrationResponse.t,
  DeleteSupplementalTaxRegistrationResponse.error) t 
  | DeleteTaxRegistration: (DeleteTaxRegistrationRequest.t,
  DeleteTaxRegistrationResponse.t, DeleteTaxRegistrationResponse.error) t 
  | GetTaxExemptionTypes: (GetTaxExemptionTypesRequest.t,
  GetTaxExemptionTypesResponse.t, GetTaxExemptionTypesResponse.error) t 
  | GetTaxInheritance: (GetTaxInheritanceRequest.t,
  GetTaxInheritanceResponse.t, GetTaxInheritanceResponse.error) t 
  | GetTaxRegistration: (GetTaxRegistrationRequest.t,
  GetTaxRegistrationResponse.t, GetTaxRegistrationResponse.error) t 
  | GetTaxRegistrationDocument: (GetTaxRegistrationDocumentRequest.t,
  GetTaxRegistrationDocumentResponse.t,
  GetTaxRegistrationDocumentResponse.error) t 
  | ListSupplementalTaxRegistrations:
  (ListSupplementalTaxRegistrationsRequest.t,
  ListSupplementalTaxRegistrationsResponse.t,
  ListSupplementalTaxRegistrationsResponse.error) t 
  | ListTaxExemptions: (ListTaxExemptionsRequest.t,
  ListTaxExemptionsResponse.t, ListTaxExemptionsResponse.error) t 
  | ListTaxRegistrations: (ListTaxRegistrationsRequest.t,
  ListTaxRegistrationsResponse.t, ListTaxRegistrationsResponse.error) t 
  | PutSupplementalTaxRegistration: (PutSupplementalTaxRegistrationRequest.t,
  PutSupplementalTaxRegistrationResponse.t,
  PutSupplementalTaxRegistrationResponse.error) t 
  | PutTaxExemption: (PutTaxExemptionRequest.t, PutTaxExemptionResponse.t,
  PutTaxExemptionResponse.error) t 
  | PutTaxInheritance: (PutTaxInheritanceRequest.t,
  PutTaxInheritanceResponse.t, PutTaxInheritanceResponse.error) t 
  | PutTaxRegistration: (PutTaxRegistrationRequest.t,
  PutTaxRegistrationResponse.t, PutTaxRegistrationResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchDeleteTaxRegistration -> `POST
  | BatchGetTaxExemptions -> `POST
  | BatchPutTaxRegistration -> `POST
  | DeleteSupplementalTaxRegistration -> `POST
  | DeleteTaxRegistration -> `POST
  | GetTaxExemptionTypes -> `POST
  | GetTaxInheritance -> `POST
  | GetTaxRegistration -> `POST
  | GetTaxRegistrationDocument -> `POST
  | ListSupplementalTaxRegistrations -> `POST
  | ListTaxExemptions -> `POST
  | ListTaxRegistrations -> `POST
  | PutSupplementalTaxRegistration -> `POST
  | PutTaxExemption -> `POST
  | PutTaxInheritance -> `POST
  | PutTaxRegistration -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchDeleteTaxRegistration ->
          (Format.kasprintf Uri.of_string) "/BatchDeleteTaxRegistration"
      | BatchGetTaxExemptions ->
          (Format.kasprintf Uri.of_string) "/BatchGetTaxExemptions"
      | BatchPutTaxRegistration ->
          (Format.kasprintf Uri.of_string) "/BatchPutTaxRegistration"
      | DeleteSupplementalTaxRegistration ->
          (Format.kasprintf Uri.of_string)
            "/DeleteSupplementalTaxRegistration"
      | DeleteTaxRegistration ->
          (Format.kasprintf Uri.of_string) "/DeleteTaxRegistration"
      | GetTaxExemptionTypes ->
          (Format.kasprintf Uri.of_string) "/GetTaxExemptionTypes"
      | GetTaxInheritance ->
          (Format.kasprintf Uri.of_string) "/GetTaxInheritance"
      | GetTaxRegistration ->
          (Format.kasprintf Uri.of_string) "/GetTaxRegistration"
      | GetTaxRegistrationDocument ->
          (Format.kasprintf Uri.of_string) "/GetTaxRegistrationDocument"
      | ListSupplementalTaxRegistrations ->
          (Format.kasprintf Uri.of_string)
            "/ListSupplementalTaxRegistrations"
      | ListTaxExemptions ->
          (Format.kasprintf Uri.of_string) "/ListTaxExemptions"
      | ListTaxRegistrations ->
          (Format.kasprintf Uri.of_string) "/ListTaxRegistrations"
      | PutSupplementalTaxRegistration ->
          (Format.kasprintf Uri.of_string) "/PutSupplementalTaxRegistration"
      | PutTaxExemption ->
          (Format.kasprintf Uri.of_string) "/PutTaxExemption"
      | PutTaxInheritance ->
          (Format.kasprintf Uri.of_string) "/PutTaxInheritance"
      | PutTaxRegistration ->
          (Format.kasprintf Uri.of_string) "/PutTaxRegistration")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | BatchDeleteTaxRegistration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("accountIds",
                           (BatchDeleteTaxRegistrationRequestAccountIdsList.to_value
                              req.BatchDeleteTaxRegistrationRequest.accountIds))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchGetTaxExemptions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("accountIds",
                           (BatchGetTaxExemptionsRequestAccountIdsList.to_value
                              req.BatchGetTaxExemptionsRequest.accountIds))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchPutTaxRegistration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("accountIds",
                           (BatchPutTaxRegistrationRequestAccountIdsList.to_value
                              req.BatchPutTaxRegistrationRequest.accountIds));
                      Some
                        ("taxRegistrationEntry",
                          (TaxRegistrationEntry.to_value
                             req.BatchPutTaxRegistrationRequest.taxRegistrationEntry))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteSupplementalTaxRegistration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("authorityId",
                           (GenericString.to_value
                              req.DeleteSupplementalTaxRegistrationRequest.authorityId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteTaxRegistration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DeleteTaxRegistrationRequest.accountId
                         ~f:(fun x -> ("accountId", (AccountId.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTaxExemptionTypes ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTaxInheritance ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTaxRegistration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetTaxRegistrationRequest.accountId
                         ~f:(fun x -> ("accountId", (AccountId.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTaxRegistrationDocument ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.GetTaxRegistrationDocumentRequest.destinationS3Location
                         ~f:(fun x ->
                               ("destinationS3Location",
                                 (DestinationS3Location.to_value x)));
                      Some
                        ("taxDocumentMetadata",
                          (TaxDocumentMetadata.to_value
                             req.GetTaxRegistrationDocumentRequest.taxDocumentMetadata))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSupplementalTaxRegistrations ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListSupplementalTaxRegistrationsRequest.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map
                        req.ListSupplementalTaxRegistrationsRequest.nextToken
                        ~f:(fun x ->
                              ("nextToken",
                                (PaginationTokenString.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTaxExemptions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListTaxExemptionsRequest.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListTaxExemptionsRequest.nextToken
                        ~f:(fun x ->
                              ("nextToken",
                                (PaginationTokenString.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTaxRegistrations ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListTaxRegistrationsRequest.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListTaxRegistrationsRequest.nextToken
                        ~f:(fun x ->
                              ("nextToken",
                                (PaginationTokenString.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutSupplementalTaxRegistration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("taxRegistrationEntry",
                           (SupplementalTaxRegistrationEntry.to_value
                              req.PutSupplementalTaxRegistrationRequest.taxRegistrationEntry))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutTaxExemption ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("accountIds",
                           (PutTaxExemptionRequestAccountIdsList.to_value
                              req.PutTaxExemptionRequest.accountIds));
                      Some
                        ("authority",
                          (Authority.to_value
                             req.PutTaxExemptionRequest.authority));
                      Some
                        ("exemptionCertificate",
                          (ExemptionCertificate.to_value
                             req.PutTaxExemptionRequest.exemptionCertificate));
                      Some
                        ("exemptionType",
                          (GenericString.to_value
                             req.PutTaxExemptionRequest.exemptionType))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutTaxInheritance ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.PutTaxInheritanceRequest.heritageStatus
                         ~f:(fun x ->
                               ("heritageStatus",
                                 (HeritageStatus.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutTaxRegistration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.PutTaxRegistrationRequest.accountId
                         ~f:(fun x -> ("accountId", (AccountId.to_value x)));
                      Some
                        ("taxRegistrationEntry",
                          (TaxRegistrationEntry.to_value
                             req.PutTaxRegistrationRequest.taxRegistrationEntry))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | BatchDeleteTaxRegistration ->
      if is_success
      then
        Ok
          (BatchDeleteTaxRegistrationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some BatchDeleteTaxRegistrationResponse.error_of_json))
  | BatchGetTaxExemptions ->
      if is_success
      then Ok (BatchGetTaxExemptionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some BatchGetTaxExemptionsResponse.error_of_json))
  | BatchPutTaxRegistration ->
      if is_success
      then
        Ok (BatchPutTaxRegistrationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some BatchPutTaxRegistrationResponse.error_of_json))
  | DeleteSupplementalTaxRegistration ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (DeleteSupplementalTaxRegistrationResponse.of_header_and_body
             (headers, ()))
      else
        Error
          (parse_aws_error
             (Some DeleteSupplementalTaxRegistrationResponse.error_of_json))
  | DeleteTaxRegistration ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteTaxRegistrationResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some DeleteTaxRegistrationResponse.error_of_json))
  | GetTaxExemptionTypes ->
      if is_success
      then Ok (GetTaxExemptionTypesResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetTaxExemptionTypesResponse.error_of_json))
  | GetTaxInheritance ->
      if is_success
      then Ok (GetTaxInheritanceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetTaxInheritanceResponse.error_of_json))
  | GetTaxRegistration ->
      if is_success
      then Ok (GetTaxRegistrationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetTaxRegistrationResponse.error_of_json))
  | GetTaxRegistrationDocument ->
      if is_success
      then
        Ok
          (GetTaxRegistrationDocumentResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetTaxRegistrationDocumentResponse.error_of_json))
  | ListSupplementalTaxRegistrations ->
      if is_success
      then
        Ok
          (ListSupplementalTaxRegistrationsResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListSupplementalTaxRegistrationsResponse.error_of_json))
  | ListTaxExemptions ->
      if is_success
      then Ok (ListTaxExemptionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTaxExemptionsResponse.error_of_json))
  | ListTaxRegistrations ->
      if is_success
      then Ok (ListTaxRegistrationsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTaxRegistrationsResponse.error_of_json))
  | PutSupplementalTaxRegistration ->
      if is_success
      then
        Ok
          (PutSupplementalTaxRegistrationResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some PutSupplementalTaxRegistrationResponse.error_of_json))
  | PutTaxExemption ->
      if is_success
      then Ok (PutTaxExemptionResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some PutTaxExemptionResponse.error_of_json))
  | PutTaxInheritance ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PutTaxInheritanceResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some PutTaxInheritanceResponse.error_of_json))
  | PutTaxRegistration ->
      if is_success
      then Ok (PutTaxRegistrationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some PutTaxRegistrationResponse.error_of_json))
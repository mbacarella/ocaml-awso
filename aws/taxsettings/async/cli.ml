(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_delete_tax_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg)
           ~doc:"JSON BatchDeleteTaxRegistrationRequestAccountIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_tax_registration
           (Values.BatchDeleteTaxRegistrationRequest.make
              ~accountIds:(Values.BatchDeleteTaxRegistrationRequestAccountIdsList.of_json
                             accountIds) ())
           (Some Values.BatchDeleteTaxRegistrationResponse.to_json)
           (Some Values.BatchDeleteTaxRegistrationResponse.error_to_json)])
let batch_get_tax_exemptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg)
           ~doc:"JSON BatchGetTaxExemptionsRequestAccountIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_tax_exemptions
           (Values.BatchGetTaxExemptionsRequest.make
              ~accountIds:(Values.BatchGetTaxExemptionsRequestAccountIdsList.of_json
                             accountIds) ())
           (Some Values.BatchGetTaxExemptionsResponse.to_json)
           (Some Values.BatchGetTaxExemptionsResponse.error_to_json)])
let batch_put_tax_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg)
           ~doc:"JSON BatchPutTaxRegistrationRequestAccountIdsList"
       and taxRegistrationEntry =
         flag "tax-registration-entry" (required json_arg)
           ~doc:"JSON TaxRegistrationEntry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_tax_registration
           (Values.BatchPutTaxRegistrationRequest.make
              ~accountIds:(Values.BatchPutTaxRegistrationRequestAccountIdsList.of_json
                             accountIds)
              ~taxRegistrationEntry:(Values.TaxRegistrationEntry.of_json
                                       taxRegistrationEntry) ())
           (Some Values.BatchPutTaxRegistrationResponse.to_json)
           (Some Values.BatchPutTaxRegistrationResponse.error_to_json)])
let delete_supplemental_tax_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorityId =
         flag "authority-id" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_supplemental_tax_registration
           (Values.DeleteSupplementalTaxRegistrationRequest.make ~authorityId
              ())
           (Some Values.DeleteSupplementalTaxRegistrationResponse.to_json)
           (Some
              Values.DeleteSupplementalTaxRegistrationResponse.error_to_json)])
let delete_tax_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tax_registration
           (Values.DeleteTaxRegistrationRequest.make ?accountId ())
           (Some Values.DeleteTaxRegistrationResponse.to_json)
           (Some Values.DeleteTaxRegistrationResponse.error_to_json)])
let get_tax_exemption_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tax_exemption_types
           (Values.GetTaxExemptionTypesRequest.make ())
           (Some Values.GetTaxExemptionTypesResponse.to_json)
           (Some Values.GetTaxExemptionTypesResponse.error_to_json)])
let get_tax_inheritance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tax_inheritance (Values.GetTaxInheritanceRequest.make ())
           (Some Values.GetTaxInheritanceResponse.to_json)
           (Some Values.GetTaxInheritanceResponse.error_to_json)])
let get_tax_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tax_registration
           (Values.GetTaxRegistrationRequest.make ?accountId ())
           (Some Values.GetTaxRegistrationResponse.to_json)
           (Some Values.GetTaxRegistrationResponse.error_to_json)])
let get_tax_registration_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationS3Location =
         flag "destination-s3-location" (optional json_arg)
           ~doc:"JSON DestinationS3Location"
       and taxDocumentMetadata =
         flag "tax-document-metadata" (required json_arg)
           ~doc:"JSON TaxDocumentMetadata" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tax_registration_document
           (Values.GetTaxRegistrationDocumentRequest.make
              ?destinationS3Location:(Option.map
                                        ~f:Values.DestinationS3Location.of_json
                                        destinationS3Location)
              ~taxDocumentMetadata:(Values.TaxDocumentMetadata.of_json
                                      taxDocumentMetadata) ())
           (Some Values.GetTaxRegistrationDocumentResponse.to_json)
           (Some Values.GetTaxRegistrationDocumentResponse.error_to_json)])
let list_supplemental_tax_registrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING PaginationTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_supplemental_tax_registrations
           (Values.ListSupplementalTaxRegistrationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListSupplementalTaxRegistrationsResponse.to_json)
           (Some
              Values.ListSupplementalTaxRegistrationsResponse.error_to_json)])
let list_tax_exemptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING PaginationTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tax_exemptions
           (Values.ListTaxExemptionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListTaxExemptionsResponse.to_json)
           (Some Values.ListTaxExemptionsResponse.error_to_json)])
let list_tax_registrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING PaginationTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tax_registrations
           (Values.ListTaxRegistrationsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListTaxRegistrationsResponse.to_json)
           (Some Values.ListTaxRegistrationsResponse.error_to_json)])
let put_supplemental_tax_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taxRegistrationEntry =
         flag "tax-registration-entry" (required json_arg)
           ~doc:"JSON SupplementalTaxRegistrationEntry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_supplemental_tax_registration
           (Values.PutSupplementalTaxRegistrationRequest.make
              ~taxRegistrationEntry:(Values.SupplementalTaxRegistrationEntry.of_json
                                       taxRegistrationEntry) ())
           (Some Values.PutSupplementalTaxRegistrationResponse.to_json)
           (Some Values.PutSupplementalTaxRegistrationResponse.error_to_json)])
let put_tax_exemption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg)
           ~doc:"JSON PutTaxExemptionRequestAccountIdsList"
       and authority =
         flag "authority" (required json_arg) ~doc:"JSON Authority"
       and exemptionCertificate =
         flag "exemption-certificate" (required json_arg)
           ~doc:"JSON ExemptionCertificate"
       and exemptionType =
         flag "exemption-type" (required string) ~doc:"STRING GenericString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_tax_exemption
           (Values.PutTaxExemptionRequest.make
              ~accountIds:(Values.PutTaxExemptionRequestAccountIdsList.of_json
                             accountIds)
              ~authority:(Values.Authority.of_json authority)
              ~exemptionCertificate:(Values.ExemptionCertificate.of_json
                                       exemptionCertificate) ~exemptionType
              ()) (Some Values.PutTaxExemptionResponse.to_json)
           (Some Values.PutTaxExemptionResponse.error_to_json)])
let put_tax_inheritance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and heritageStatus =
         flag "heritage-status" (optional json_arg)
           ~doc:"JSON HeritageStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_tax_inheritance
           (Values.PutTaxInheritanceRequest.make
              ?heritageStatus:(Option.map ~f:Values.HeritageStatus.of_json
                                 heritageStatus) ())
           (Some Values.PutTaxInheritanceResponse.to_json)
           (Some Values.PutTaxInheritanceResponse.error_to_json)])
let put_tax_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AccountId"
       and taxRegistrationEntry =
         flag "tax-registration-entry" (required json_arg)
           ~doc:"JSON TaxRegistrationEntry" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_tax_registration
           (Values.PutTaxRegistrationRequest.make ?accountId
              ~taxRegistrationEntry:(Values.TaxRegistrationEntry.of_json
                                       taxRegistrationEntry) ())
           (Some Values.PutTaxRegistrationResponse.to_json)
           (Some Values.PutTaxRegistrationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-delete-tax-registration", batch_delete_tax_registration);
    ("batch-get-tax-exemptions", batch_get_tax_exemptions);
    ("batch-put-tax-registration", batch_put_tax_registration);
    ("delete-supplemental-tax-registration",
      delete_supplemental_tax_registration);
    ("delete-tax-registration", delete_tax_registration);
    ("get-tax-exemption-types", get_tax_exemption_types);
    ("get-tax-inheritance", get_tax_inheritance);
    ("get-tax-registration", get_tax_registration);
    ("get-tax-registration-document", get_tax_registration_document);
    ("list-supplemental-tax-registrations",
      list_supplemental_tax_registrations);
    ("list-tax-exemptions", list_tax_exemptions);
    ("list-tax-registrations", list_tax_registrations);
    ("put-supplemental-tax-registration", put_supplemental_tax_registration);
    ("put-tax-exemption", put_tax_exemption);
    ("put-tax-inheritance", put_tax_inheritance);
    ("put-tax-registration", put_tax_registration)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING __stringMin1Max64PatternS"
       and tags =
         flag "tags" (optional json_arg)
           ~doc:"JSON __mapOf__stringMin0Max256PatternS"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and clusterName =
         flag "cluster-name" (required string)
           ~doc:"STRING __stringMin1Max64PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cluster
           (Values.CreateClusterRequest.make ?clientToken
              ?tags:(Option.map
                       ~f:Values.Zz__mapOf__stringMin0Max256PatternS.of_json
                       tags)
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType) ~clusterName ())
           (Some Values.CreateClusterResponse.to_json)
           (Some Values.CreateClusterResponse.error_to_json)])
let create_control_panel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING __stringMin1Max64PatternS"
       and tags =
         flag "tags" (optional json_arg)
           ~doc:"JSON __mapOf__stringMin0Max256PatternS"
       and clusterArn =
         flag "cluster-arn" (required string)
           ~doc:"STRING __stringMin1Max256PatternAZaZ09"
       and controlPanelName =
         flag "control-panel-name" (required string)
           ~doc:"STRING __stringMin1Max64PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_control_panel
           (Values.CreateControlPanelRequest.make ?clientToken
              ?tags:(Option.map
                       ~f:Values.Zz__mapOf__stringMin0Max256PatternS.of_json
                       tags) ~clusterArn ~controlPanelName ())
           (Some Values.CreateControlPanelResponse.to_json)
           (Some Values.CreateControlPanelResponse.error_to_json)])
let create_routing_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING __stringMin1Max64PatternS"
       and controlPanelArn =
         flag "control-panel-arn" (optional string)
           ~doc:"STRING __stringMin1Max256PatternAZaZ09"
       and clusterArn =
         flag "cluster-arn" (required string)
           ~doc:"STRING __stringMin1Max256PatternAZaZ09"
       and routingControlName =
         flag "routing-control-name" (required string)
           ~doc:"STRING __stringMin1Max64PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_routing_control
           (Values.CreateRoutingControlRequest.make ?clientToken
              ?controlPanelArn ~clusterArn ~routingControlName ())
           (Some Values.CreateRoutingControlResponse.to_json)
           (Some Values.CreateRoutingControlResponse.error_to_json)])
let create_safety_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assertionRule =
         flag "assertion-rule" (optional json_arg)
           ~doc:"JSON NewAssertionRule"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING __stringMin1Max64PatternS"
       and gatingRule =
         flag "gating-rule" (optional json_arg) ~doc:"JSON NewGatingRule"
       and tags =
         flag "tags" (optional json_arg)
           ~doc:"JSON __mapOf__stringMin0Max256PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_safety_rule
           (Values.CreateSafetyRuleRequest.make
              ?assertionRule:(Option.map ~f:Values.NewAssertionRule.of_json
                                assertionRule) ?clientToken
              ?gatingRule:(Option.map ~f:Values.NewGatingRule.of_json
                             gatingRule)
              ?tags:(Option.map
                       ~f:Values.Zz__mapOf__stringMin0Max256PatternS.of_json
                       tags) ())
           (Some Values.CreateSafetyRuleResponse.to_json)
           (Some Values.CreateSafetyRuleResponse.error_to_json)])
let delete_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cluster
           (Values.DeleteClusterRequest.make ~clusterArn ())
           (Some Values.DeleteClusterResponse.to_json)
           (Some Values.DeleteClusterResponse.error_to_json)])
let delete_control_panel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlPanelArn =
         flag "control-panel-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_control_panel
           (Values.DeleteControlPanelRequest.make ~controlPanelArn ())
           (Some Values.DeleteControlPanelResponse.to_json)
           (Some Values.DeleteControlPanelResponse.error_to_json)])
let delete_routing_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingControlArn =
         flag "routing-control-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_routing_control
           (Values.DeleteRoutingControlRequest.make ~routingControlArn ())
           (Some Values.DeleteRoutingControlResponse.to_json)
           (Some Values.DeleteRoutingControlResponse.error_to_json)])
let delete_safety_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and safetyRuleArn =
         flag "safety-rule-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_safety_rule
           (Values.DeleteSafetyRuleRequest.make ~safetyRuleArn ())
           (Some Values.DeleteSafetyRuleResponse.to_json)
           (Some Values.DeleteSafetyRuleResponse.error_to_json)])
let describe_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_cluster
           (Values.DescribeClusterRequest.make ~clusterArn ())
           (Some Values.DescribeClusterResponse.to_json)
           (Some Values.DescribeClusterResponse.error_to_json)])
let describe_control_panel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlPanelArn =
         flag "control-panel-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_control_panel
           (Values.DescribeControlPanelRequest.make ~controlPanelArn ())
           (Some Values.DescribeControlPanelResponse.to_json)
           (Some Values.DescribeControlPanelResponse.error_to_json)])
let describe_routing_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingControlArn =
         flag "routing-control-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_routing_control
           (Values.DescribeRoutingControlRequest.make ~routingControlArn ())
           (Some Values.DescribeRoutingControlResponse.to_json)
           (Some Values.DescribeRoutingControlResponse.error_to_json)])
let describe_safety_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and safetyRuleArn =
         flag "safety-rule-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_safety_rule
           (Values.DescribeSafetyRuleRequest.make ~safetyRuleArn ())
           (Some Values.DescribeSafetyRuleResponse.to_json)
           (Some Values.DescribeSafetyRuleResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let list_associated_route53_health_checks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and routingControlArn =
         flag "routing-control-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associated_route53_health_checks
           (Values.ListAssociatedRoute53HealthChecksRequest.make ?maxResults
              ?nextToken ~routingControlArn ())
           (Some Values.ListAssociatedRoute53HealthChecksResponse.to_json)
           (Some
              Values.ListAssociatedRoute53HealthChecksResponse.error_to_json)])
let list_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_clusters
           (Values.ListClustersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListClustersResponse.to_json)
           (Some Values.ListClustersResponse.error_to_json)])
let list_control_panels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_control_panels
           (Values.ListControlPanelsRequest.make ?clusterArn ?maxResults
              ?nextToken ()) (Some Values.ListControlPanelsResponse.to_json)
           (Some Values.ListControlPanelsResponse.error_to_json)])
let list_routing_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and controlPanelArn =
         flag "control-panel-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_routing_controls
           (Values.ListRoutingControlsRequest.make ?maxResults ?nextToken
              ~controlPanelArn ())
           (Some Values.ListRoutingControlsResponse.to_json)
           (Some Values.ListRoutingControlsResponse.error_to_json)])
let list_safety_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and controlPanelArn =
         flag "control-panel-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_safety_rules
           (Values.ListSafetyRulesRequest.make ?maxResults ?nextToken
              ~controlPanelArn ())
           (Some Values.ListSafetyRulesResponse.to_json)
           (Some Values.ListSafetyRulesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tags =
         flag "tags" (required json_arg)
           ~doc:"JSON __mapOf__stringMin0Max256PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Zz__mapOf__stringMin0Max256PatternS.of_json tags)
              ()) (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clusterArn =
         flag "cluster-arn" (required string)
           ~doc:"STRING __stringMin1Max256PatternAZaZ09"
       and networkType =
         flag "network-type" (required json_arg) ~doc:"JSON NetworkType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cluster
           (Values.UpdateClusterRequest.make ~clusterArn
              ~networkType:(Values.NetworkType.of_json networkType) ())
           (Some Values.UpdateClusterResponse.to_json)
           (Some Values.UpdateClusterResponse.error_to_json)])
let update_control_panel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlPanelArn =
         flag "control-panel-arn" (required string)
           ~doc:"STRING __stringMin1Max256PatternAZaZ09"
       and controlPanelName =
         flag "control-panel-name" (required string)
           ~doc:"STRING __stringMin1Max64PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_control_panel
           (Values.UpdateControlPanelRequest.make ~controlPanelArn
              ~controlPanelName ())
           (Some Values.UpdateControlPanelResponse.to_json)
           (Some Values.UpdateControlPanelResponse.error_to_json)])
let update_routing_control =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingControlArn =
         flag "routing-control-arn" (required string)
           ~doc:"STRING __stringMin1Max256PatternAZaZ09"
       and routingControlName =
         flag "routing-control-name" (required string)
           ~doc:"STRING __stringMin1Max64PatternS" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_routing_control
           (Values.UpdateRoutingControlRequest.make ~routingControlArn
              ~routingControlName ())
           (Some Values.UpdateRoutingControlResponse.to_json)
           (Some Values.UpdateRoutingControlResponse.error_to_json)])
let update_safety_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assertionRuleUpdate =
         flag "assertion-rule-update" (optional json_arg)
           ~doc:"JSON AssertionRuleUpdate"
       and gatingRuleUpdate =
         flag "gating-rule-update" (optional json_arg)
           ~doc:"JSON GatingRuleUpdate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_safety_rule
           (Values.UpdateSafetyRuleRequest.make
              ?assertionRuleUpdate:(Option.map
                                      ~f:Values.AssertionRuleUpdate.of_json
                                      assertionRuleUpdate)
              ?gatingRuleUpdate:(Option.map
                                   ~f:Values.GatingRuleUpdate.of_json
                                   gatingRuleUpdate) ())
           (Some Values.UpdateSafetyRuleResponse.to_json)
           (Some Values.UpdateSafetyRuleResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-cluster", create_cluster);
    ("create-control-panel", create_control_panel);
    ("create-routing-control", create_routing_control);
    ("create-safety-rule", create_safety_rule);
    ("delete-cluster", delete_cluster);
    ("delete-control-panel", delete_control_panel);
    ("delete-routing-control", delete_routing_control);
    ("delete-safety-rule", delete_safety_rule);
    ("describe-cluster", describe_cluster);
    ("describe-control-panel", describe_control_panel);
    ("describe-routing-control", describe_routing_control);
    ("describe-safety-rule", describe_safety_rule);
    ("get-resource-policy", get_resource_policy);
    ("list-associated-route53-health-checks",
      list_associated_route53_health_checks);
    ("list-clusters", list_clusters);
    ("list-control-panels", list_control_panels);
    ("list-routing-controls", list_routing_controls);
    ("list-safety-rules", list_safety_rules);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-cluster", update_cluster);
    ("update-control-panel", update_control_panel);
    ("update-routing-control", update_routing_control);
    ("update-safety-rule", update_safety_rule)]
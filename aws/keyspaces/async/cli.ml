(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_keyspace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and keyspaceName =
         flag "keyspace-name" (required string) ~doc:"STRING KeyspaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_keyspace
           (Values.CreateKeyspaceRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~keyspaceName
              ()) (Some Values.CreateKeyspaceResponse.to_json)
           (Some Values.CreateKeyspaceResponse.error_to_json)])
let create_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment = flag "comment" (optional json_arg) ~doc:"JSON Comment"
       and capacitySpecification =
         flag "capacity-specification" (optional json_arg)
           ~doc:"JSON CapacitySpecification"
       and encryptionSpecification =
         flag "encryption-specification" (optional json_arg)
           ~doc:"JSON EncryptionSpecification"
       and pointInTimeRecovery =
         flag "point-in-time-recovery" (optional json_arg)
           ~doc:"JSON PointInTimeRecovery"
       and ttl = flag "ttl" (optional json_arg) ~doc:"JSON TimeToLive"
       and defaultTimeToLive =
         flag "default-time-to-live" (optional int)
           ~doc:"INT DefaultTimeToLive"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and keyspaceName =
         flag "keyspace-name" (required string) ~doc:"STRING KeyspaceName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableName"
       and schemaDefinition =
         flag "schema-definition" (required json_arg)
           ~doc:"JSON SchemaDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_table
           (Values.CreateTableRequest.make
              ?comment:(Option.map ~f:Values.Comment.of_json comment)
              ?capacitySpecification:(Option.map
                                        ~f:Values.CapacitySpecification.of_json
                                        capacitySpecification)
              ?encryptionSpecification:(Option.map
                                          ~f:Values.EncryptionSpecification.of_json
                                          encryptionSpecification)
              ?pointInTimeRecovery:(Option.map
                                      ~f:Values.PointInTimeRecovery.of_json
                                      pointInTimeRecovery)
              ?ttl:(Option.map ~f:Values.TimeToLive.of_json ttl)
              ?defaultTimeToLive
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~keyspaceName
              ~tableName
              ~schemaDefinition:(Values.SchemaDefinition.of_json
                                   schemaDefinition) ())
           (Some Values.CreateTableResponse.to_json)
           (Some Values.CreateTableResponse.error_to_json)])
let delete_keyspace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyspaceName =
         flag "keyspace-name" (required string) ~doc:"STRING KeyspaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_keyspace
           (Values.DeleteKeyspaceRequest.make ~keyspaceName ())
           (Some Values.DeleteKeyspaceResponse.to_json)
           (Some Values.DeleteKeyspaceResponse.error_to_json)])
let delete_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyspaceName =
         flag "keyspace-name" (required string) ~doc:"STRING KeyspaceName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table
           (Values.DeleteTableRequest.make ~keyspaceName ~tableName ())
           (Some Values.DeleteTableResponse.to_json)
           (Some Values.DeleteTableResponse.error_to_json)])
let get_keyspace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyspaceName =
         flag "keyspace-name" (required string) ~doc:"STRING KeyspaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_keyspace (Values.GetKeyspaceRequest.make ~keyspaceName ())
           (Some Values.GetKeyspaceResponse.to_json)
           (Some Values.GetKeyspaceResponse.error_to_json)])
let get_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyspaceName =
         flag "keyspace-name" (required string) ~doc:"STRING KeyspaceName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table
           (Values.GetTableRequest.make ~keyspaceName ~tableName ())
           (Some Values.GetTableResponse.to_json)
           (Some Values.GetTableResponse.error_to_json)])
let list_keyspaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_keyspaces
           (Values.ListKeyspacesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListKeyspacesResponse.to_json)
           (Some Values.ListKeyspacesResponse.error_to_json)])
let list_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and keyspaceName =
         flag "keyspace-name" (required string) ~doc:"STRING KeyspaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tables
           (Values.ListTablesRequest.make ?nextToken ?maxResults
              ~keyspaceName ()) (Some Values.ListTablesResponse.to_json)
           (Some Values.ListTablesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ?maxResults
              ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let restore_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restoreTimestamp =
         flag "restore-timestamp" (optional json_arg) ~doc:"JSON Timestamp"
       and capacitySpecificationOverride =
         flag "capacity-specification-override" (optional json_arg)
           ~doc:"JSON CapacitySpecification"
       and encryptionSpecificationOverride =
         flag "encryption-specification-override" (optional json_arg)
           ~doc:"JSON EncryptionSpecification"
       and pointInTimeRecoveryOverride =
         flag "point-in-time-recovery-override" (optional json_arg)
           ~doc:"JSON PointInTimeRecovery"
       and tagsOverride =
         flag "tags-override" (optional json_arg) ~doc:"JSON TagList"
       and sourceKeyspaceName =
         flag "source-keyspace-name" (required string)
           ~doc:"STRING KeyspaceName"
       and sourceTableName =
         flag "source-table-name" (required string) ~doc:"STRING TableName"
       and targetKeyspaceName =
         flag "target-keyspace-name" (required string)
           ~doc:"STRING KeyspaceName"
       and targetTableName =
         flag "target-table-name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_table
           (Values.RestoreTableRequest.make
              ?restoreTimestamp:(Option.map ~f:Values.Timestamp.of_json
                                   restoreTimestamp)
              ?capacitySpecificationOverride:(Option.map
                                                ~f:Values.CapacitySpecification.of_json
                                                capacitySpecificationOverride)
              ?encryptionSpecificationOverride:(Option.map
                                                  ~f:Values.EncryptionSpecification.of_json
                                                  encryptionSpecificationOverride)
              ?pointInTimeRecoveryOverride:(Option.map
                                              ~f:Values.PointInTimeRecovery.of_json
                                              pointInTimeRecoveryOverride)
              ?tagsOverride:(Option.map ~f:Values.TagList.of_json
                               tagsOverride) ~sourceKeyspaceName
              ~sourceTableName ~targetKeyspaceName ~targetTableName ())
           (Some Values.RestoreTableResponse.to_json)
           (Some Values.RestoreTableResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ARN"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addColumns =
         flag "add-columns" (optional json_arg)
           ~doc:"JSON ColumnDefinitionList"
       and capacitySpecification =
         flag "capacity-specification" (optional json_arg)
           ~doc:"JSON CapacitySpecification"
       and encryptionSpecification =
         flag "encryption-specification" (optional json_arg)
           ~doc:"JSON EncryptionSpecification"
       and pointInTimeRecovery =
         flag "point-in-time-recovery" (optional json_arg)
           ~doc:"JSON PointInTimeRecovery"
       and ttl = flag "ttl" (optional json_arg) ~doc:"JSON TimeToLive"
       and defaultTimeToLive =
         flag "default-time-to-live" (optional int)
           ~doc:"INT DefaultTimeToLive"
       and keyspaceName =
         flag "keyspace-name" (required string) ~doc:"STRING KeyspaceName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table
           (Values.UpdateTableRequest.make
              ?addColumns:(Option.map ~f:Values.ColumnDefinitionList.of_json
                             addColumns)
              ?capacitySpecification:(Option.map
                                        ~f:Values.CapacitySpecification.of_json
                                        capacitySpecification)
              ?encryptionSpecification:(Option.map
                                          ~f:Values.EncryptionSpecification.of_json
                                          encryptionSpecification)
              ?pointInTimeRecovery:(Option.map
                                      ~f:Values.PointInTimeRecovery.of_json
                                      pointInTimeRecovery)
              ?ttl:(Option.map ~f:Values.TimeToLive.of_json ttl)
              ?defaultTimeToLive ~keyspaceName ~tableName ())
           (Some Values.UpdateTableResponse.to_json)
           (Some Values.UpdateTableResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-keyspace", create_keyspace);
    ("create-table", create_table);
    ("delete-keyspace", delete_keyspace);
    ("delete-table", delete_table);
    ("get-keyspace", get_keyspace);
    ("get-table", get_table);
    ("list-keyspaces", list_keyspaces);
    ("list-tables", list_tables);
    ("list-tags-for-resource", list_tags_for_resource);
    ("restore-table", restore_table);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-table", update_table)]
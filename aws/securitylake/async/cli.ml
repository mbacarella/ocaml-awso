(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_aws_log_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON AwsLogSourceConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_aws_log_source
           (Values.CreateAwsLogSourceRequest.make
              ~sources:(Values.AwsLogSourceConfigurationList.of_json sources)
              ()) (Some Values.CreateAwsLogSourceResponse.to_json)
           (Some Values.CreateAwsLogSourceResponse.error_to_json)])
let create_custom_log_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventClasses =
         flag "event-classes" (optional json_arg)
           ~doc:"JSON OcsfEventClassList"
       and sourceVersion =
         flag "source-version" (optional string)
           ~doc:"STRING CustomLogSourceVersion"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON CustomLogSourceConfiguration"
       and sourceName =
         flag "source-name" (required string)
           ~doc:"STRING CustomLogSourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_log_source
           (Values.CreateCustomLogSourceRequest.make
              ?eventClasses:(Option.map ~f:Values.OcsfEventClassList.of_json
                               eventClasses) ?sourceVersion
              ~configuration:(Values.CustomLogSourceConfiguration.of_json
                                configuration) ~sourceName ())
           (Some Values.CreateCustomLogSourceResponse.to_json)
           (Some Values.CreateCustomLogSourceResponse.error_to_json)])
let create_data_lake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and configurations =
         flag "configurations" (required json_arg)
           ~doc:"JSON DataLakeConfigurationList"
       and metaStoreManagerRoleArn =
         flag "meta-store-manager-role-arn" (required string)
           ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_lake
           (Values.CreateDataLakeRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~configurations:(Values.DataLakeConfigurationList.of_json
                                 configurations) ~metaStoreManagerRoleArn ())
           (Some Values.CreateDataLakeResponse.to_json)
           (Some Values.CreateDataLakeResponse.error_to_json)])
let create_data_lake_exception_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exceptionTimeToLive =
         flag "exception-time-to-live" (optional json_arg)
           ~doc:"JSON CreateDataLakeExceptionSubscriptionRequestExceptionTimeToLiveLong"
       and notificationEndpoint =
         flag "notification-endpoint" (required string)
           ~doc:"STRING SafeString"
       and subscriptionProtocol =
         flag "subscription-protocol" (required string)
           ~doc:"STRING SubscriptionProtocol" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_lake_exception_subscription
           (Values.CreateDataLakeExceptionSubscriptionRequest.make
              ?exceptionTimeToLive:(Option.map
                                      ~f:Values.CreateDataLakeExceptionSubscriptionRequestExceptionTimeToLiveLong.of_json
                                      exceptionTimeToLive)
              ~notificationEndpoint ~subscriptionProtocol ())
           (Some Values.CreateDataLakeExceptionSubscriptionResponse.to_json)
           (Some
              Values.CreateDataLakeExceptionSubscriptionResponse.error_to_json)])
let create_data_lake_organization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoEnableNewAccount =
         flag "auto-enable-new-account" (optional json_arg)
           ~doc:"JSON DataLakeAutoEnableNewAccountConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_lake_organization_configuration
           (Values.CreateDataLakeOrganizationConfigurationRequest.make
              ?autoEnableNewAccount:(Option.map
                                       ~f:Values.DataLakeAutoEnableNewAccountConfigurationList.of_json
                                       autoEnableNewAccount) ())
           (Some
              Values.CreateDataLakeOrganizationConfigurationResponse.to_json)
           (Some
              Values.CreateDataLakeOrganizationConfigurationResponse.error_to_json)])
let create_subscriber =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessTypes =
         flag "access-types" (optional json_arg) ~doc:"JSON AccessTypeList"
       and subscriberDescription =
         flag "subscriber-description" (optional string)
           ~doc:"STRING DescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sources =
         flag "sources" (required json_arg) ~doc:"JSON LogSourceResourceList"
       and subscriberIdentity =
         flag "subscriber-identity" (required json_arg)
           ~doc:"JSON AwsIdentity"
       and subscriberName =
         flag "subscriber-name" (required string)
           ~doc:"STRING CreateSubscriberRequestSubscriberNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscriber
           (Values.CreateSubscriberRequest.make
              ?accessTypes:(Option.map ~f:Values.AccessTypeList.of_json
                              accessTypes) ?subscriberDescription
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sources:(Values.LogSourceResourceList.of_json sources)
              ~subscriberIdentity:(Values.AwsIdentity.of_json
                                     subscriberIdentity) ~subscriberName ())
           (Some Values.CreateSubscriberResponse.to_json)
           (Some Values.CreateSubscriberResponse.error_to_json)])
let create_subscriber_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON NotificationConfiguration"
       and subscriberId =
         flag "subscriber-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscriber_notification
           (Values.CreateSubscriberNotificationRequest.make
              ~configuration:(Values.NotificationConfiguration.of_json
                                configuration) ~subscriberId ())
           (Some Values.CreateSubscriberNotificationResponse.to_json)
           (Some Values.CreateSubscriberNotificationResponse.error_to_json)])
let delete_aws_log_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON AwsLogSourceConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_aws_log_source
           (Values.DeleteAwsLogSourceRequest.make
              ~sources:(Values.AwsLogSourceConfigurationList.of_json sources)
              ()) (Some Values.DeleteAwsLogSourceResponse.to_json)
           (Some Values.DeleteAwsLogSourceResponse.error_to_json)])
let delete_custom_log_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceVersion =
         flag "source-version" (optional string)
           ~doc:"STRING CustomLogSourceVersion"
       and sourceName =
         flag "source-name" (required string)
           ~doc:"STRING CustomLogSourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_log_source
           (Values.DeleteCustomLogSourceRequest.make ?sourceVersion
              ~sourceName ())
           (Some Values.DeleteCustomLogSourceResponse.to_json)
           (Some Values.DeleteCustomLogSourceResponse.error_to_json)])
let delete_data_lake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regions =
         flag "regions" (required json_arg) ~doc:"JSON RegionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_lake
           (Values.DeleteDataLakeRequest.make
              ~regions:(Values.RegionList.of_json regions) ())
           (Some Values.DeleteDataLakeResponse.to_json)
           (Some Values.DeleteDataLakeResponse.error_to_json)])
let delete_data_lake_exception_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_lake_exception_subscription
           (Values.DeleteDataLakeExceptionSubscriptionRequest.make ())
           (Some Values.DeleteDataLakeExceptionSubscriptionResponse.to_json)
           (Some
              Values.DeleteDataLakeExceptionSubscriptionResponse.error_to_json)])
let delete_data_lake_organization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoEnableNewAccount =
         flag "auto-enable-new-account" (optional json_arg)
           ~doc:"JSON DataLakeAutoEnableNewAccountConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_lake_organization_configuration
           (Values.DeleteDataLakeOrganizationConfigurationRequest.make
              ?autoEnableNewAccount:(Option.map
                                       ~f:Values.DataLakeAutoEnableNewAccountConfigurationList.of_json
                                       autoEnableNewAccount) ())
           (Some
              Values.DeleteDataLakeOrganizationConfigurationResponse.to_json)
           (Some
              Values.DeleteDataLakeOrganizationConfigurationResponse.error_to_json)])
let delete_subscriber =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriberId =
         flag "subscriber-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subscriber
           (Values.DeleteSubscriberRequest.make ~subscriberId ())
           (Some Values.DeleteSubscriberResponse.to_json)
           (Some Values.DeleteSubscriberResponse.error_to_json)])
let delete_subscriber_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriberId =
         flag "subscriber-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subscriber_notification
           (Values.DeleteSubscriberNotificationRequest.make ~subscriberId ())
           (Some Values.DeleteSubscriberNotificationResponse.to_json)
           (Some Values.DeleteSubscriberNotificationResponse.error_to_json)])
let deregister_data_lake_delegated_administrator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_data_lake_delegated_administrator
           (Values.DeregisterDataLakeDelegatedAdministratorRequest.make ())
           (Some
              Values.DeregisterDataLakeDelegatedAdministratorResponse.to_json)
           (Some
              Values.DeregisterDataLakeDelegatedAdministratorResponse.error_to_json)])
let get_data_lake_exception_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_lake_exception_subscription
           (Values.GetDataLakeExceptionSubscriptionRequest.make ())
           (Some Values.GetDataLakeExceptionSubscriptionResponse.to_json)
           (Some
              Values.GetDataLakeExceptionSubscriptionResponse.error_to_json)])
let get_data_lake_organization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_lake_organization_configuration
           (Values.GetDataLakeOrganizationConfigurationRequest.make ())
           (Some Values.GetDataLakeOrganizationConfigurationResponse.to_json)
           (Some
              Values.GetDataLakeOrganizationConfigurationResponse.error_to_json)])
let get_data_lake_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accounts =
         flag "accounts" (optional json_arg) ~doc:"JSON AccountList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_lake_sources
           (Values.GetDataLakeSourcesRequest.make
              ?accounts:(Option.map ~f:Values.AccountList.of_json accounts)
              ?maxResults ?nextToken ())
           (Some Values.GetDataLakeSourcesResponse.to_json)
           (Some Values.GetDataLakeSourcesResponse.error_to_json)])
let get_subscriber =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriberId =
         flag "subscriber-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscriber
           (Values.GetSubscriberRequest.make ~subscriberId ())
           (Some Values.GetSubscriberResponse.to_json)
           (Some Values.GetSubscriberResponse.error_to_json)])
let list_data_lake_exceptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and regions =
         flag "regions" (optional json_arg) ~doc:"JSON RegionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_lake_exceptions
           (Values.ListDataLakeExceptionsRequest.make ?maxResults ?nextToken
              ?regions:(Option.map ~f:Values.RegionList.of_json regions) ())
           (Some Values.ListDataLakeExceptionsResponse.to_json)
           (Some Values.ListDataLakeExceptionsResponse.error_to_json)])
let list_data_lakes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regions =
         flag "regions" (optional json_arg) ~doc:"JSON RegionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_lakes
           (Values.ListDataLakesRequest.make
              ?regions:(Option.map ~f:Values.RegionList.of_json regions) ())
           (Some Values.ListDataLakesResponse.to_json)
           (Some Values.ListDataLakesResponse.error_to_json)])
let list_log_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accounts =
         flag "accounts" (optional json_arg) ~doc:"JSON AccountList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and regions =
         flag "regions" (optional json_arg) ~doc:"JSON RegionList"
       and sources =
         flag "sources" (optional json_arg) ~doc:"JSON LogSourceResourceList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_log_sources
           (Values.ListLogSourcesRequest.make
              ?accounts:(Option.map ~f:Values.AccountList.of_json accounts)
              ?maxResults ?nextToken
              ?regions:(Option.map ~f:Values.RegionList.of_json regions)
              ?sources:(Option.map ~f:Values.LogSourceResourceList.of_json
                          sources) ())
           (Some Values.ListLogSourcesResponse.to_json)
           (Some Values.ListLogSourcesResponse.error_to_json)])
let list_subscribers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscribers
           (Values.ListSubscribersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListSubscribersResponse.to_json)
           (Some Values.ListSubscribersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let register_data_lake_delegated_administrator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING SafeString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_data_lake_delegated_administrator
           (Values.RegisterDataLakeDelegatedAdministratorRequest.make
              ~accountId ())
           (Some
              Values.RegisterDataLakeDelegatedAdministratorResponse.to_json)
           (Some
              Values.RegisterDataLakeDelegatedAdministratorResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_data_lake =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metaStoreManagerRoleArn =
         flag "meta-store-manager-role-arn" (optional string)
           ~doc:"STRING RoleArn"
       and configurations =
         flag "configurations" (required json_arg)
           ~doc:"JSON DataLakeConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_lake
           (Values.UpdateDataLakeRequest.make ?metaStoreManagerRoleArn
              ~configurations:(Values.DataLakeConfigurationList.of_json
                                 configurations) ())
           (Some Values.UpdateDataLakeResponse.to_json)
           (Some Values.UpdateDataLakeResponse.error_to_json)])
let update_data_lake_exception_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exceptionTimeToLive =
         flag "exception-time-to-live" (optional json_arg)
           ~doc:"JSON UpdateDataLakeExceptionSubscriptionRequestExceptionTimeToLiveLong"
       and notificationEndpoint =
         flag "notification-endpoint" (required string)
           ~doc:"STRING SafeString"
       and subscriptionProtocol =
         flag "subscription-protocol" (required string)
           ~doc:"STRING SubscriptionProtocol" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_lake_exception_subscription
           (Values.UpdateDataLakeExceptionSubscriptionRequest.make
              ?exceptionTimeToLive:(Option.map
                                      ~f:Values.UpdateDataLakeExceptionSubscriptionRequestExceptionTimeToLiveLong.of_json
                                      exceptionTimeToLive)
              ~notificationEndpoint ~subscriptionProtocol ())
           (Some Values.UpdateDataLakeExceptionSubscriptionResponse.to_json)
           (Some
              Values.UpdateDataLakeExceptionSubscriptionResponse.error_to_json)])
let update_subscriber =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sources =
         flag "sources" (optional json_arg) ~doc:"JSON LogSourceResourceList"
       and subscriberDescription =
         flag "subscriber-description" (optional string)
           ~doc:"STRING DescriptionString"
       and subscriberIdentity =
         flag "subscriber-identity" (optional json_arg)
           ~doc:"JSON AwsIdentity"
       and subscriberName =
         flag "subscriber-name" (optional string)
           ~doc:"STRING UpdateSubscriberRequestSubscriberNameString"
       and subscriberId =
         flag "subscriber-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscriber
           (Values.UpdateSubscriberRequest.make
              ?sources:(Option.map ~f:Values.LogSourceResourceList.of_json
                          sources) ?subscriberDescription
              ?subscriberIdentity:(Option.map ~f:Values.AwsIdentity.of_json
                                     subscriberIdentity) ?subscriberName
              ~subscriberId ())
           (Some Values.UpdateSubscriberResponse.to_json)
           (Some Values.UpdateSubscriberResponse.error_to_json)])
let update_subscriber_notification =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON NotificationConfiguration"
       and subscriberId =
         flag "subscriber-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscriber_notification
           (Values.UpdateSubscriberNotificationRequest.make
              ~configuration:(Values.NotificationConfiguration.of_json
                                configuration) ~subscriberId ())
           (Some Values.UpdateSubscriberNotificationResponse.to_json)
           (Some Values.UpdateSubscriberNotificationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-aws-log-source", create_aws_log_source);
    ("create-custom-log-source", create_custom_log_source);
    ("create-data-lake", create_data_lake);
    ("create-data-lake-exception-subscription",
      create_data_lake_exception_subscription);
    ("create-data-lake-organization-configuration",
      create_data_lake_organization_configuration);
    ("create-subscriber", create_subscriber);
    ("create-subscriber-notification", create_subscriber_notification);
    ("delete-aws-log-source", delete_aws_log_source);
    ("delete-custom-log-source", delete_custom_log_source);
    ("delete-data-lake", delete_data_lake);
    ("delete-data-lake-exception-subscription",
      delete_data_lake_exception_subscription);
    ("delete-data-lake-organization-configuration",
      delete_data_lake_organization_configuration);
    ("delete-subscriber", delete_subscriber);
    ("delete-subscriber-notification", delete_subscriber_notification);
    ("deregister-data-lake-delegated-administrator",
      deregister_data_lake_delegated_administrator);
    ("get-data-lake-exception-subscription",
      get_data_lake_exception_subscription);
    ("get-data-lake-organization-configuration",
      get_data_lake_organization_configuration);
    ("get-data-lake-sources", get_data_lake_sources);
    ("get-subscriber", get_subscriber);
    ("list-data-lake-exceptions", list_data_lake_exceptions);
    ("list-data-lakes", list_data_lakes);
    ("list-log-sources", list_log_sources);
    ("list-subscribers", list_subscribers);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-data-lake-delegated-administrator",
      register_data_lake_delegated_administrator);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-data-lake", update_data_lake);
    ("update-data-lake-exception-subscription",
      update_data_lake_exception_subscription);
    ("update-subscriber", update_subscriber);
    ("update-subscriber-notification", update_subscriber_notification)]
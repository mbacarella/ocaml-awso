(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateSlackChannelConfiguration:
  (CreateSlackChannelConfigurationRequest.t,
  CreateSlackChannelConfigurationResult.t,
  CreateSlackChannelConfigurationResult.error) t 
  | DeleteAccountAlias: (DeleteAccountAliasRequest.t,
  DeleteAccountAliasResult.t, DeleteAccountAliasResult.error) t 
  | DeleteSlackChannelConfiguration:
  (DeleteSlackChannelConfigurationRequest.t,
  DeleteSlackChannelConfigurationResult.t,
  DeleteSlackChannelConfigurationResult.error) t 
  | DeleteSlackWorkspaceConfiguration:
  (DeleteSlackWorkspaceConfigurationRequest.t,
  DeleteSlackWorkspaceConfigurationResult.t,
  DeleteSlackWorkspaceConfigurationResult.error) t 
  | GetAccountAlias: (GetAccountAliasRequest.t, GetAccountAliasResult.t,
  GetAccountAliasResult.error) t 
  | ListSlackChannelConfigurations: (ListSlackChannelConfigurationsRequest.t,
  ListSlackChannelConfigurationsResult.t,
  ListSlackChannelConfigurationsResult.error) t 
  | ListSlackWorkspaceConfigurations:
  (ListSlackWorkspaceConfigurationsRequest.t,
  ListSlackWorkspaceConfigurationsResult.t,
  ListSlackWorkspaceConfigurationsResult.error) t 
  | PutAccountAlias: (PutAccountAliasRequest.t, PutAccountAliasResult.t,
  PutAccountAliasResult.error) t 
  | RegisterSlackWorkspaceForOrganization:
  (RegisterSlackWorkspaceForOrganizationRequest.t,
  RegisterSlackWorkspaceForOrganizationResult.t,
  RegisterSlackWorkspaceForOrganizationResult.error) t 
  | UpdateSlackChannelConfiguration:
  (UpdateSlackChannelConfigurationRequest.t,
  UpdateSlackChannelConfigurationResult.t,
  UpdateSlackChannelConfigurationResult.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateSlackChannelConfiguration -> `POST
  | DeleteAccountAlias -> `POST
  | DeleteSlackChannelConfiguration -> `POST
  | DeleteSlackWorkspaceConfiguration -> `POST
  | GetAccountAlias -> `POST
  | ListSlackChannelConfigurations -> `POST
  | ListSlackWorkspaceConfigurations -> `POST
  | PutAccountAlias -> `POST
  | RegisterSlackWorkspaceForOrganization -> `POST
  | UpdateSlackChannelConfiguration -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateSlackChannelConfiguration ->
          (Format.kasprintf Uri.of_string)
            "/control/create-slack-channel-configuration"
      | DeleteAccountAlias ->
          (Format.kasprintf Uri.of_string) "/control/delete-account-alias"
      | DeleteSlackChannelConfiguration ->
          (Format.kasprintf Uri.of_string)
            "/control/delete-slack-channel-configuration"
      | DeleteSlackWorkspaceConfiguration ->
          (Format.kasprintf Uri.of_string)
            "/control/delete-slack-workspace-configuration"
      | GetAccountAlias ->
          (Format.kasprintf Uri.of_string) "/control/get-account-alias"
      | ListSlackChannelConfigurations ->
          (Format.kasprintf Uri.of_string)
            "/control/list-slack-channel-configurations"
      | ListSlackWorkspaceConfigurations ->
          (Format.kasprintf Uri.of_string)
            "/control/list-slack-workspace-configurations"
      | PutAccountAlias ->
          (Format.kasprintf Uri.of_string) "/control/put-account-alias"
      | RegisterSlackWorkspaceForOrganization ->
          (Format.kasprintf Uri.of_string)
            "/control/register-slack-workspace-for-organization"
      | UpdateSlackChannelConfiguration ->
          (Format.kasprintf Uri.of_string)
            "/control/update-slack-channel-configuration")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateSlackChannelConfiguration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("channelId",
                           (ChannelId.to_value
                              req.CreateSlackChannelConfigurationRequest.channelId));
                      Option.map
                        req.CreateSlackChannelConfigurationRequest.channelName
                        ~f:(fun x ->
                              ("channelName", (ChannelName.to_value x)));
                      Some
                        ("channelRoleArn",
                          (RoleArn.to_value
                             req.CreateSlackChannelConfigurationRequest.channelRoleArn));
                      Option.map
                        req.CreateSlackChannelConfigurationRequest.notifyOnAddCorrespondenceToCase
                        ~f:(fun x ->
                              ("notifyOnAddCorrespondenceToCase",
                                (BooleanValue.to_value x)));
                      Some
                        ("notifyOnCaseSeverity",
                          (NotificationSeverityLevel.to_value
                             req.CreateSlackChannelConfigurationRequest.notifyOnCaseSeverity));
                      Option.map
                        req.CreateSlackChannelConfigurationRequest.notifyOnCreateOrReopenCase
                        ~f:(fun x ->
                              ("notifyOnCreateOrReopenCase",
                                (BooleanValue.to_value x)));
                      Option.map
                        req.CreateSlackChannelConfigurationRequest.notifyOnResolveCase
                        ~f:(fun x ->
                              ("notifyOnResolveCase",
                                (BooleanValue.to_value x)));
                      Some
                        ("teamId",
                          (TeamId.to_value
                             req.CreateSlackChannelConfigurationRequest.teamId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteAccountAlias ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteSlackChannelConfiguration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("channelId",
                           (ChannelId.to_value
                              req.DeleteSlackChannelConfigurationRequest.channelId));
                      Some
                        ("teamId",
                          (TeamId.to_value
                             req.DeleteSlackChannelConfigurationRequest.teamId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteSlackWorkspaceConfiguration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("teamId",
                           (TeamId.to_value
                              req.DeleteSlackWorkspaceConfigurationRequest.teamId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAccountAlias ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSlackChannelConfigurations ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListSlackChannelConfigurationsRequest.nextToken
                         ~f:(fun x ->
                               ("nextToken", (PaginationToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSlackWorkspaceConfigurations ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListSlackWorkspaceConfigurationsRequest.nextToken
                         ~f:(fun x ->
                               ("nextToken", (PaginationToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutAccountAlias ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("accountAlias",
                           (AwsAccountAlias.to_value
                              req.PutAccountAliasRequest.accountAlias))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RegisterSlackWorkspaceForOrganization ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("teamId",
                           (TeamId.to_value
                              req.RegisterSlackWorkspaceForOrganizationRequest.teamId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateSlackChannelConfiguration ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("channelId",
                           (ChannelId.to_value
                              req.UpdateSlackChannelConfigurationRequest.channelId));
                      Option.map
                        req.UpdateSlackChannelConfigurationRequest.channelName
                        ~f:(fun x ->
                              ("channelName", (ChannelName.to_value x)));
                      Option.map
                        req.UpdateSlackChannelConfigurationRequest.channelRoleArn
                        ~f:(fun x -> ("channelRoleArn", (RoleArn.to_value x)));
                      Option.map
                        req.UpdateSlackChannelConfigurationRequest.notifyOnAddCorrespondenceToCase
                        ~f:(fun x ->
                              ("notifyOnAddCorrespondenceToCase",
                                (BooleanValue.to_value x)));
                      Option.map
                        req.UpdateSlackChannelConfigurationRequest.notifyOnCaseSeverity
                        ~f:(fun x ->
                              ("notifyOnCaseSeverity",
                                (NotificationSeverityLevel.to_value x)));
                      Option.map
                        req.UpdateSlackChannelConfigurationRequest.notifyOnCreateOrReopenCase
                        ~f:(fun x ->
                              ("notifyOnCreateOrReopenCase",
                                (BooleanValue.to_value x)));
                      Option.map
                        req.UpdateSlackChannelConfigurationRequest.notifyOnResolveCase
                        ~f:(fun x ->
                              ("notifyOnResolveCase",
                                (BooleanValue.to_value x)));
                      Some
                        ("teamId",
                          (TeamId.to_value
                             req.UpdateSlackChannelConfigurationRequest.teamId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateSlackChannelConfiguration ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (CreateSlackChannelConfigurationResult.of_header_and_body
             (headers, ()))
      else
        Error
          (parse_aws_error
             (Some CreateSlackChannelConfigurationResult.error_of_json))
  | DeleteAccountAlias ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteAccountAliasResult.of_header_and_body (headers, ()))
      else
        Error (parse_aws_error (Some DeleteAccountAliasResult.error_of_json))
  | DeleteSlackChannelConfiguration ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (DeleteSlackChannelConfigurationResult.of_header_and_body
             (headers, ()))
      else
        Error
          (parse_aws_error
             (Some DeleteSlackChannelConfigurationResult.error_of_json))
  | DeleteSlackWorkspaceConfiguration ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (DeleteSlackWorkspaceConfigurationResult.of_header_and_body
             (headers, ()))
      else
        Error
          (parse_aws_error
             (Some DeleteSlackWorkspaceConfigurationResult.error_of_json))
  | GetAccountAlias ->
      if is_success
      then Ok (GetAccountAliasResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetAccountAliasResult.error_of_json))
  | ListSlackChannelConfigurations ->
      if is_success
      then
        Ok
          (ListSlackChannelConfigurationsResult.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListSlackChannelConfigurationsResult.error_of_json))
  | ListSlackWorkspaceConfigurations ->
      if is_success
      then
        Ok
          (ListSlackWorkspaceConfigurationsResult.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListSlackWorkspaceConfigurationsResult.error_of_json))
  | PutAccountAlias ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PutAccountAliasResult.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some PutAccountAliasResult.error_of_json))
  | RegisterSlackWorkspaceForOrganization ->
      if is_success
      then
        Ok
          (RegisterSlackWorkspaceForOrganizationResult.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some RegisterSlackWorkspaceForOrganizationResult.error_of_json))
  | UpdateSlackChannelConfiguration ->
      if is_success
      then
        Ok
          (UpdateSlackChannelConfigurationResult.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some UpdateSlackChannelConfigurationResult.error_of_json))
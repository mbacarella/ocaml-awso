(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_slack_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelName =
         flag "channel-name" (optional string) ~doc:"STRING channelName"
       and notifyOnAddCorrespondenceToCase =
         flag "notify-on-add-correspondence-to-case" (optional bool)
           ~doc:"BOOL booleanValue"
       and notifyOnCreateOrReopenCase =
         flag "notify-on-create-or-reopen-case" (optional bool)
           ~doc:"BOOL booleanValue"
       and notifyOnResolveCase =
         flag "notify-on-resolve-case" (optional bool)
           ~doc:"BOOL booleanValue"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING channelId"
       and channelRoleArn =
         flag "channel-role-arn" (required string) ~doc:"STRING roleArn"
       and notifyOnCaseSeverity =
         flag "notify-on-case-severity" (required json_arg)
           ~doc:"JSON NotificationSeverityLevel"
       and teamId = flag "team-id" (required string) ~doc:"STRING teamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_slack_channel_configuration
           (Values.CreateSlackChannelConfigurationRequest.make ?channelName
              ?notifyOnAddCorrespondenceToCase ?notifyOnCreateOrReopenCase
              ?notifyOnResolveCase ~channelId ~channelRoleArn
              ~notifyOnCaseSeverity:(Values.NotificationSeverityLevel.of_json
                                       notifyOnCaseSeverity) ~teamId ())
           (Some Values.CreateSlackChannelConfigurationResult.to_json)
           (Some Values.CreateSlackChannelConfigurationResult.error_to_json)])
let delete_account_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_alias (Values.DeleteAccountAliasRequest.make ())
           (Some Values.DeleteAccountAliasResult.to_json)
           (Some Values.DeleteAccountAliasResult.error_to_json)])
let delete_slack_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING channelId"
       and teamId = flag "team-id" (required string) ~doc:"STRING teamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_slack_channel_configuration
           (Values.DeleteSlackChannelConfigurationRequest.make ~channelId
              ~teamId ())
           (Some Values.DeleteSlackChannelConfigurationResult.to_json)
           (Some Values.DeleteSlackChannelConfigurationResult.error_to_json)])
let delete_slack_workspace_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and teamId = flag "team-id" (required string) ~doc:"STRING teamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_slack_workspace_configuration
           (Values.DeleteSlackWorkspaceConfigurationRequest.make ~teamId ())
           (Some Values.DeleteSlackWorkspaceConfigurationResult.to_json)
           (Some Values.DeleteSlackWorkspaceConfigurationResult.error_to_json)])
let get_account_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_alias (Values.GetAccountAliasRequest.make ())
           (Some Values.GetAccountAliasResult.to_json)
           (Some Values.GetAccountAliasResult.error_to_json)])
let list_slack_channel_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING paginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_slack_channel_configurations
           (Values.ListSlackChannelConfigurationsRequest.make ?nextToken ())
           (Some Values.ListSlackChannelConfigurationsResult.to_json)
           (Some Values.ListSlackChannelConfigurationsResult.error_to_json)])
let list_slack_workspace_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING paginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_slack_workspace_configurations
           (Values.ListSlackWorkspaceConfigurationsRequest.make ?nextToken ())
           (Some Values.ListSlackWorkspaceConfigurationsResult.to_json)
           (Some Values.ListSlackWorkspaceConfigurationsResult.error_to_json)])
let put_account_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountAlias =
         flag "account-alias" (required string) ~doc:"STRING awsAccountAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_alias
           (Values.PutAccountAliasRequest.make ~accountAlias ())
           (Some Values.PutAccountAliasResult.to_json)
           (Some Values.PutAccountAliasResult.error_to_json)])
let register_slack_workspace_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and teamId = flag "team-id" (required string) ~doc:"STRING teamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_slack_workspace_for_organization
           (Values.RegisterSlackWorkspaceForOrganizationRequest.make ~teamId
              ())
           (Some Values.RegisterSlackWorkspaceForOrganizationResult.to_json)
           (Some
              Values.RegisterSlackWorkspaceForOrganizationResult.error_to_json)])
let update_slack_channel_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelName =
         flag "channel-name" (optional string) ~doc:"STRING channelName"
       and channelRoleArn =
         flag "channel-role-arn" (optional string) ~doc:"STRING roleArn"
       and notifyOnAddCorrespondenceToCase =
         flag "notify-on-add-correspondence-to-case" (optional bool)
           ~doc:"BOOL booleanValue"
       and notifyOnCaseSeverity =
         flag "notify-on-case-severity" (optional json_arg)
           ~doc:"JSON NotificationSeverityLevel"
       and notifyOnCreateOrReopenCase =
         flag "notify-on-create-or-reopen-case" (optional bool)
           ~doc:"BOOL booleanValue"
       and notifyOnResolveCase =
         flag "notify-on-resolve-case" (optional bool)
           ~doc:"BOOL booleanValue"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING channelId"
       and teamId = flag "team-id" (required string) ~doc:"STRING teamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_slack_channel_configuration
           (Values.UpdateSlackChannelConfigurationRequest.make ?channelName
              ?channelRoleArn ?notifyOnAddCorrespondenceToCase
              ?notifyOnCaseSeverity:(Option.map
                                       ~f:Values.NotificationSeverityLevel.of_json
                                       notifyOnCaseSeverity)
              ?notifyOnCreateOrReopenCase ?notifyOnResolveCase ~channelId
              ~teamId ())
           (Some Values.UpdateSlackChannelConfigurationResult.to_json)
           (Some Values.UpdateSlackChannelConfigurationResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-slack-channel-configuration",
       create_slack_channel_configuration);
    ("delete-account-alias", delete_account_alias);
    ("delete-slack-channel-configuration",
      delete_slack_channel_configuration);
    ("delete-slack-workspace-configuration",
      delete_slack_workspace_configuration);
    ("get-account-alias", get_account_alias);
    ("list-slack-channel-configurations", list_slack_channel_configurations);
    ("list-slack-workspace-configurations",
      list_slack_workspace_configurations);
    ("put-account-alias", put_account_alias);
    ("register-slack-workspace-for-organization",
      register_slack_workspace_for_organization);
    ("update-slack-channel-configuration",
      update_slack_channel_configuration)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.rolesanywhere
let apiVersion = "2018-05-10"
let endpointPrefix = "rolesanywhere"
let serviceFullName = "IAM Roles Anywhere"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module MappingRuleSpecifierString =
  struct
    type nonrec t = string
    let context_ = "MappingRuleSpecifierString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:60) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MappingRuleSpecifierString" j
    let to_json = simple_to_json to_value
  end
module MappingRule =
  struct
    type nonrec t =
      {
      specifier: MappingRuleSpecifierString.t
        [@ocaml.doc
          "Specifier within a certificate field, such as CN, OU, or UID from the Subject field."]}
    let context_ = "MappingRule"
    let make ~specifier = fun () -> { specifier }
    let to_value x =
      structure_to_value
        [("specifier",
           (Some (MappingRuleSpecifierString.to_value x.specifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let specifier =
        MappingRuleSpecifierString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "specifier") in
      make ~specifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let specifier =
        field_map_exn json__ "specifier" MappingRuleSpecifierString.of_json in
      make ~specifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A single mapping entry for each supported specifier or sub-field."]
module InstancePropertyMapKeyString =
  struct
    type nonrec t = string
    let context_ = "InstancePropertyMapKeyString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstancePropertyMapKeyString" j
    let to_json = simple_to_json to_value
  end
module InstancePropertyMapValueString =
  struct
    type nonrec t = string
    let context_ = "InstancePropertyMapValueString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstancePropertyMapValueString" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module NotificationChannel =
  struct
    type nonrec t =
      | ALL 
      | Non_static_id of string 
    let make i = i
    let to_string = function | ALL -> "ALL" | Non_static_id s -> s
    let of_string = function | "ALL" -> ALL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration NotificationChannel" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"NotificationChannel" j)
    let to_json = simple_to_json to_value
  end
module NotificationEvent =
  struct
    type nonrec t =
      | CA_CERTIFICATE_EXPIRY 
      | END_ENTITY_CERTIFICATE_EXPIRY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CA_CERTIFICATE_EXPIRY -> "CA_CERTIFICATE_EXPIRY"
      | END_ENTITY_CERTIFICATE_EXPIRY -> "END_ENTITY_CERTIFICATE_EXPIRY"
      | Non_static_id s -> s
    let of_string =
      function
      | "CA_CERTIFICATE_EXPIRY" -> CA_CERTIFICATE_EXPIRY
      | "END_ENTITY_CERTIFICATE_EXPIRY" -> END_ENTITY_CERTIFICATE_EXPIRY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration NotificationEvent" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"NotificationEvent" j)
    let to_json = simple_to_json to_value
  end
module NotificationSettingDetailConfiguredByString =
  struct
    type nonrec t = string
    let context_ = "NotificationSettingDetailConfiguredByString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"NotificationSettingDetailConfiguredByString" j
    let to_json = simple_to_json to_value
  end
module NotificationSettingDetailThresholdInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:360) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for NotificationSettingDetailThresholdInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SourceDataX509CertificateDataString =
  struct
    type nonrec t = string
    let context_ = "SourceDataX509CertificateDataString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:16000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"SourceDataX509CertificateDataString" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module CertificateField =
  struct
    type nonrec t =
      | X509Subject 
      | X509Issuer 
      | X509SAN 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | X509Subject -> "x509Subject"
      | X509Issuer -> "x509Issuer"
      | X509SAN -> "x509SAN"
      | Non_static_id s -> s
    let of_string =
      function
      | "x509Subject" -> X509Subject
      | "x509Issuer" -> X509Issuer
      | "x509SAN" -> X509SAN
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CertificateField" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CertificateField" j)
    let to_json = simple_to_json to_value
  end
module MappingRules =
  struct
    type nonrec t = MappingRule.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MappingRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MappingRule.of_xml)
    let of_json j =
      list_of_json ~kind:"MappingRules" ~of_json:MappingRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module SyntheticTimestamp_date_time =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module InstancePropertyMap =
  struct
    type nonrec t =
      (InstancePropertyMapKeyString.t * InstancePropertyMapValueString.t)
        list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((InstancePropertyMapKeyString.of_string chopped),
                              (InstancePropertyMapValueString.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (InstancePropertyMapKeyString.to_value x) |>
                    (fun x ->
                       (InstancePropertyMapValueString.to_value y) |>
                         (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:InstancePropertyMapKeyString.of_string
        ~of_json:InstancePropertyMapValueString.of_json j
    let to_json v = composed_to_json to_value v
  end
module NotificationSettingDetail =
  struct
    type nonrec t =
      {
      enabled: Boolean.t option
        [@ocaml.doc "Indicates whether the notification setting is enabled."];
      event: NotificationEvent.t option
        [@ocaml.doc
          "The event to which this notification setting is applied."];
      threshold: NotificationSettingDetailThresholdInteger.t option
        [@ocaml.doc "The number of days before a notification event."];
      channel: NotificationChannel.t option
        [@ocaml.doc
          "The specified channel of notification. IAM Roles Anywhere uses CloudWatch metrics, EventBridge, and Health Dashboard to notify for an event. In the absence of a specific channel, IAM Roles Anywhere applies this setting to 'ALL' channels."];
      configuredBy: NotificationSettingDetailConfiguredByString.t option
        [@ocaml.doc
          "The principal that configured the notification setting. For default settings configured by IAM Roles Anywhere, the value is rolesanywhere.amazonaws.com, and for customized notifications settings, it is the respective account ID."]}
    let make ?enabled =
      fun ?event ->
        fun ?threshold ->
          fun ?channel ->
            fun ?configuredBy ->
              fun () -> { enabled; event; threshold; channel; configuredBy }
    let to_value x =
      structure_to_value
        [("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("event", (Option.map x.event ~f:NotificationEvent.to_value));
        ("threshold",
          (Option.map x.threshold
             ~f:NotificationSettingDetailThresholdInteger.to_value));
        ("channel", (Option.map x.channel ~f:NotificationChannel.to_value));
        ("configuredBy",
          (Option.map x.configuredBy
             ~f:NotificationSettingDetailConfiguredByString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuredBy =
        (Option.map ~f:NotificationSettingDetailConfiguredByString.of_xml)
          (Xml.child xml_arg0 "configuredBy") in
      let channel =
        (Option.map ~f:NotificationChannel.of_xml)
          (Xml.child xml_arg0 "channel") in
      let threshold =
        (Option.map ~f:NotificationSettingDetailThresholdInteger.of_xml)
          (Xml.child xml_arg0 "threshold") in
      let event =
        (Option.map ~f:NotificationEvent.of_xml) (Xml.child xml_arg0 "event") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      make ?configuredBy ?channel ?threshold ?event ?enabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuredBy =
        field_map json__ "configuredBy"
          NotificationSettingDetailConfiguredByString.of_json in
      let channel = field_map json__ "channel" NotificationChannel.of_json in
      let threshold =
        field_map json__ "threshold"
          NotificationSettingDetailThresholdInteger.of_json in
      let event = field_map json__ "event" NotificationEvent.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      make ?configuredBy ?channel ?threshold ?event ?enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The state of a notification setting. A notification setting includes information such as event name, threshold, status of the notification setting, and the channel to notify."]
module SourceData =
  struct
    type nonrec t =
      {
      x509CertificateData: SourceDataX509CertificateDataString.t option
        [@ocaml.doc
          "The PEM-encoded data for the certificate anchor. Included for trust anchors of type CERTIFICATE_BUNDLE."];
      acmPcaArn: String_.t option
        [@ocaml.doc
          "The root certificate of the Private Certificate Authority specified by this ARN is used in trust validation for temporary credential requests. Included for trust anchors of type AWS_ACM_PCA."]}
    let make ?x509CertificateData =
      fun ?acmPcaArn -> fun () -> { x509CertificateData; acmPcaArn }
    let to_value x =
      structure_to_value
        [("x509CertificateData",
           (Option.map x.x509CertificateData
              ~f:SourceDataX509CertificateDataString.to_value));
        ("acmPcaArn", (Option.map x.acmPcaArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acmPcaArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "acmPcaArn") in
      let x509CertificateData =
        (Option.map ~f:SourceDataX509CertificateDataString.of_xml)
          (Xml.child xml_arg0 "x509CertificateData") in
      make ?acmPcaArn ?x509CertificateData ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let acmPcaArn = field_map json__ "acmPcaArn" String_.of_json in
      let x509CertificateData =
        field_map json__ "x509CertificateData"
          SourceDataX509CertificateDataString.of_json in
      make ?acmPcaArn ?x509CertificateData ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The data field of the trust anchor depending on its type."]
module TrustAnchorType =
  struct
    type nonrec t =
      | AWS_ACM_PCA 
      | CERTIFICATE_BUNDLE 
      | SELF_SIGNED_REPOSITORY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWS_ACM_PCA -> "AWS_ACM_PCA"
      | CERTIFICATE_BUNDLE -> "CERTIFICATE_BUNDLE"
      | SELF_SIGNED_REPOSITORY -> "SELF_SIGNED_REPOSITORY"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWS_ACM_PCA" -> AWS_ACM_PCA
      | "CERTIFICATE_BUNDLE" -> CERTIFICATE_BUNDLE
      | "SELF_SIGNED_REPOSITORY" -> SELF_SIGNED_REPOSITORY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TrustAnchorType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TrustAnchorType" j)
    let to_json = simple_to_json to_value
  end
module AttributeMapping =
  struct
    type nonrec t =
      {
      certificateField: CertificateField.t option
        [@ocaml.doc
          "Fields (x509Subject, x509Issuer and x509SAN) within X.509 certificates."];
      mappingRules: MappingRules.t option
        [@ocaml.doc
          "A list of mapping entries for every supported specifier or sub-field."]}
    let make ?certificateField =
      fun ?mappingRules -> fun () -> { certificateField; mappingRules }
    let to_value x =
      structure_to_value
        [("certificateField",
           (Option.map x.certificateField ~f:CertificateField.to_value));
        ("mappingRules",
          (Option.map x.mappingRules ~f:MappingRules.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mappingRules =
        (Option.map ~f:MappingRules.of_xml)
          (Xml.child xml_arg0 "mappingRules") in
      let certificateField =
        (Option.map ~f:CertificateField.of_xml)
          (Xml.child xml_arg0 "certificateField") in
      make ?mappingRules ?certificateField ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let mappingRules = field_map json__ "mappingRules" MappingRules.of_json in
      let certificateField =
        field_map json__ "certificateField" CertificateField.of_json in
      make ?mappingRules ?certificateField ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A mapping applied to the authenticating end-entity certificate."]
module ManagedPolicyListMemberString =
  struct
    type nonrec t = string
    let context_ = "ManagedPolicyListMemberString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:200) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ManagedPolicyListMemberString" j
    let to_json = simple_to_json to_value
  end
module RoleArn =
  struct
    type nonrec t = string
    let context_ = "RoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[^:]+)?:iam(:.*){2}(:role.*)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RoleArn" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[ a-zA-Z0-9_.:/=+@-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[ a-zA-Z0-9_.:/=+@-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module CredentialSummary =
  struct
    type nonrec t =
      {
      seenAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 time stamp of when the certificate was last used in a temporary credential request."];
      serialNumber: String_.t option
        [@ocaml.doc "The serial number of the certificate."];
      issuer: String_.t option
        [@ocaml.doc
          "The fully qualified domain name of the issuing certificate for the presented end-entity certificate."];
      enabled: Boolean.t option
        [@ocaml.doc "Indicates whether the credential is enabled."];
      x509CertificateData: String_.t option
        [@ocaml.doc "The PEM-encoded data of the certificate."];
      failed: Boolean.t option
        [@ocaml.doc
          "Indicates whether the temporary credential request was successful."]}
    let make ?seenAt =
      fun ?serialNumber ->
        fun ?issuer ->
          fun ?enabled ->
            fun ?x509CertificateData ->
              fun ?failed ->
                fun () ->
                  {
                    seenAt;
                    serialNumber;
                    issuer;
                    enabled;
                    x509CertificateData;
                    failed
                  }
    let to_value x =
      structure_to_value
        [("seenAt",
           (Option.map x.seenAt ~f:SyntheticTimestamp_date_time.to_value));
        ("serialNumber", (Option.map x.serialNumber ~f:String_.to_value));
        ("issuer", (Option.map x.issuer ~f:String_.to_value));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("x509CertificateData",
          (Option.map x.x509CertificateData ~f:String_.to_value));
        ("failed", (Option.map x.failed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "failed") in
      let x509CertificateData =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x509CertificateData") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let issuer =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "issuer") in
      let serialNumber =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serialNumber") in
      let seenAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "seenAt") in
      make ?failed ?x509CertificateData ?enabled ?issuer ?serialNumber
        ?seenAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failed = field_map json__ "failed" Boolean.of_json in
      let x509CertificateData =
        field_map json__ "x509CertificateData" String_.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let issuer = field_map json__ "issuer" String_.of_json in
      let serialNumber = field_map json__ "serialNumber" String_.of_json in
      let seenAt =
        field_map json__ "seenAt" SyntheticTimestamp_date_time.of_json in
      make ?failed ?x509CertificateData ?enabled ?issuer ?serialNumber
        ?seenAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A record of a presented X509 credential from a temporary credential request."]
module InstanceProperty =
  struct
    type nonrec t =
      {
      seenAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 time stamp of when the certificate was last used in a temporary credential request."];
      properties: InstancePropertyMap.t option
        [@ocaml.doc "A list of instanceProperty objects."];
      failed: Boolean.t option
        [@ocaml.doc
          "Indicates whether the temporary credential request was successful."]}
    let make ?seenAt =
      fun ?properties ->
        fun ?failed -> fun () -> { seenAt; properties; failed }
    let to_value x =
      structure_to_value
        [("seenAt",
           (Option.map x.seenAt ~f:SyntheticTimestamp_date_time.to_value));
        ("properties",
          (Option.map x.properties ~f:InstancePropertyMap.to_value));
        ("failed", (Option.map x.failed ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "failed") in
      let properties =
        (Option.map ~f:InstancePropertyMap.of_xml)
          (Xml.child xml_arg0 "properties") in
      let seenAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "seenAt") in
      make ?failed ?properties ?seenAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failed = field_map json__ "failed" Boolean.of_json in
      let properties =
        field_map json__ "properties" InstancePropertyMap.of_json in
      let seenAt =
        field_map json__ "seenAt" SyntheticTimestamp_date_time.of_json in
      make ?failed ?properties ?seenAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A key-value pair you set that identifies a property of the authenticating instance."]
module NotificationSettingThresholdInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:360) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for NotificationSettingThresholdInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NotificationSettingDetails =
  struct
    type nonrec t = NotificationSettingDetail.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NotificationSettingDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NotificationSettingDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"NotificationSettingDetails"
        ~of_json:NotificationSettingDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceName =
  struct
    type nonrec t = string
    let context_ = "ResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () -> check_pattern i ~pattern:"[ a-zA-Z0-9-_]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end
module Source =
  struct
    type nonrec t =
      {
      sourceType: TrustAnchorType.t option
        [@ocaml.doc "The type of the trust anchor."];
      sourceData: SourceData.t option
        [@ocaml.doc
          "The data field of the trust anchor depending on its type."]}
    let make ?sourceType =
      fun ?sourceData -> fun () -> { sourceType; sourceData }
    let to_value x =
      structure_to_value
        [("sourceType",
           (Option.map x.sourceType ~f:TrustAnchorType.to_value));
        ("sourceData", (Option.map x.sourceData ~f:SourceData.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sourceData =
        (Option.map ~f:SourceData.of_xml) (Xml.child xml_arg0 "sourceData") in
      let sourceType =
        (Option.map ~f:TrustAnchorType.of_xml)
          (Xml.child xml_arg0 "sourceType") in
      make ?sourceData ?sourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sourceData = field_map json__ "sourceData" SourceData.of_json in
      let sourceType = field_map json__ "sourceType" TrustAnchorType.of_json in
      make ?sourceData ?sourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The trust anchor type and its related certificate data."]
module Uuid =
  struct
    type nonrec t = string
    let context_ = "Uuid"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:36) >>=
             (fun () ->
                (check_string_max i ~max:36) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:".*[a-f0-9]{8}-([a-z0-9]{4}-){3}[a-z0-9]{12}.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Uuid" j
    let to_json = simple_to_json to_value
  end
module AttributeMappings =
  struct
    type nonrec t = AttributeMapping.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AttributeMapping.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AttributeMapping.of_xml)
    let of_json j =
      list_of_json ~kind:"AttributeMappings"
        ~of_json:AttributeMapping.of_json j
    let to_json v = composed_to_json to_value v
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ManagedPolicyList =
  struct
    type nonrec t = ManagedPolicyListMemberString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ManagedPolicyListMemberString.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ManagedPolicyListMemberString.of_xml)
    let of_json j =
      list_of_json ~kind:"ManagedPolicyList"
        ~of_json:ManagedPolicyListMemberString.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProfileArn =
  struct
    type nonrec t = string
    let context_ = "ProfileArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[^:]+)?:rolesanywhere(:.*){2}(:profile.*)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProfileArn" j
    let to_json = simple_to_json to_value
  end
module RoleArnList =
  struct
    type nonrec t = RoleArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:250) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RoleArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RoleArn.of_xml)
    let of_json j =
      list_of_json ~kind:"RoleArnList" ~of_json:RoleArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module Blob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t [@ocaml.doc "The tag key."];
      value: TagValue.t [@ocaml.doc "The tag value."]}
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("key", (Some (TagKey.to_value x.key)));
        ("value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "key") in
      make ~value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map_exn json__ "value" TagValue.of_json in
      let key = field_map_exn json__ "key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A label that consists of a key and value you define."]
module CredentialSummaries =
  struct
    type nonrec t = CredentialSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CredentialSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CredentialSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"CredentialSummaries"
        ~of_json:CredentialSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceProperties =
  struct
    type nonrec t = InstanceProperty.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:InstanceProperty.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceProperty.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceProperties"
        ~of_json:InstanceProperty.of_json j
    let to_json v = composed_to_json to_value v
  end
module NotificationSettingKey =
  struct
    type nonrec t =
      {
      event: NotificationEvent.t
        [@ocaml.doc "The notification setting event to reset."];
      channel: NotificationChannel.t option
        [@ocaml.doc "The specified channel of notification."]}
    let context_ = "NotificationSettingKey"
    let make ?channel = fun ~event -> fun () -> { channel; event }
    let to_value x =
      structure_to_value
        [("event", (Some (NotificationEvent.to_value x.event)));
        ("channel", (Option.map x.channel ~f:NotificationChannel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channel =
        (Option.map ~f:NotificationChannel.of_xml)
          (Xml.child xml_arg0 "channel") in
      let event =
        NotificationEvent.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "event") in
      make ?channel ~event ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channel = field_map json__ "channel" NotificationChannel.of_json in
      let event = field_map_exn json__ "event" NotificationEvent.of_json in
      make ?channel ~event ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A notification setting key to reset. A notification setting key includes the event and the channel."]
module NotificationSetting =
  struct
    type nonrec t =
      {
      enabled: Boolean.t
        [@ocaml.doc "Indicates whether the notification setting is enabled."];
      event: NotificationEvent.t
        [@ocaml.doc
          "The event to which this notification setting is applied."];
      threshold: NotificationSettingThresholdInteger.t option
        [@ocaml.doc
          "The number of days before a notification event. This value is required for a notification setting that is enabled."];
      channel: NotificationChannel.t option
        [@ocaml.doc
          "The specified channel of notification. IAM Roles Anywhere uses CloudWatch metrics, EventBridge, and Health Dashboard to notify for an event. In the absence of a specific channel, IAM Roles Anywhere applies this setting to 'ALL' channels."]}
    let context_ = "NotificationSetting"
    let make ?threshold =
      fun ?channel ->
        fun ~enabled ->
          fun ~event -> fun () -> { threshold; channel; enabled; event }
    let to_value x =
      structure_to_value
        [("enabled", (Some (Boolean.to_value x.enabled)));
        ("event", (Some (NotificationEvent.to_value x.event)));
        ("threshold",
          (Option.map x.threshold
             ~f:NotificationSettingThresholdInteger.to_value));
        ("channel", (Option.map x.channel ~f:NotificationChannel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let channel =
        (Option.map ~f:NotificationChannel.of_xml)
          (Xml.child xml_arg0 "channel") in
      let threshold =
        (Option.map ~f:NotificationSettingThresholdInteger.of_xml)
          (Xml.child xml_arg0 "threshold") in
      let event =
        NotificationEvent.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "event") in
      let enabled =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "enabled") in
      make ?channel ?threshold ~event ~enabled ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let channel = field_map json__ "channel" NotificationChannel.of_json in
      let threshold =
        field_map json__ "threshold"
          NotificationSettingThresholdInteger.of_json in
      let event = field_map_exn json__ "event" NotificationEvent.of_json in
      let enabled = field_map_exn json__ "enabled" Boolean.of_json in
      make ?channel ?threshold ~event ~enabled ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Customizable notification settings that will be applied to notification events. IAM Roles Anywhere consumes these settings while notifying across multiple channels - CloudWatch metrics, EventBridge, and Health Dashboard."]
module TrustAnchorDetail =
  struct
    type nonrec t =
      {
      trustAnchorId: Uuid.t option
        [@ocaml.doc "The unique identifier of the trust anchor."];
      trustAnchorArn: String_.t option
        [@ocaml.doc "The ARN of the trust anchor."];
      name: ResourceName.t option
        [@ocaml.doc "The name of the trust anchor."];
      source: Source.t option
        [@ocaml.doc
          "The trust anchor type and its related certificate data."];
      enabled: Boolean.t option
        [@ocaml.doc "Indicates whether the trust anchor is enabled."];
      createdAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 timestamp when the trust anchor was created."];
      updatedAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 timestamp when the trust anchor was last updated."];
      notificationSettings: NotificationSettingDetails.t option
        [@ocaml.doc
          "A list of notification settings to be associated to the trust anchor."]}
    let make ?trustAnchorId =
      fun ?trustAnchorArn ->
        fun ?name ->
          fun ?source ->
            fun ?enabled ->
              fun ?createdAt ->
                fun ?updatedAt ->
                  fun ?notificationSettings ->
                    fun () ->
                      {
                        trustAnchorId;
                        trustAnchorArn;
                        name;
                        source;
                        enabled;
                        createdAt;
                        updatedAt;
                        notificationSettings
                      }
    let to_value x =
      structure_to_value
        [("trustAnchorId", (Option.map x.trustAnchorId ~f:Uuid.to_value));
        ("trustAnchorArn", (Option.map x.trustAnchorArn ~f:String_.to_value));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("source", (Option.map x.source ~f:Source.to_value));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("createdAt",
          (Option.map x.createdAt ~f:SyntheticTimestamp_date_time.to_value));
        ("updatedAt",
          (Option.map x.updatedAt ~f:SyntheticTimestamp_date_time.to_value));
        ("notificationSettings",
          (Option.map x.notificationSettings
             ~f:NotificationSettingDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationSettings =
        (Option.map ~f:NotificationSettingDetails.of_xml)
          (Xml.child xml_arg0 "notificationSettings") in
      let updatedAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let source =
        (Option.map ~f:Source.of_xml) (Xml.child xml_arg0 "source") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let trustAnchorArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "trustAnchorArn") in
      let trustAnchorId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "trustAnchorId") in
      make ?notificationSettings ?updatedAt ?createdAt ?enabled ?source ?name
        ?trustAnchorArn ?trustAnchorId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let notificationSettings =
        field_map json__ "notificationSettings"
          NotificationSettingDetails.of_json in
      let updatedAt =
        field_map json__ "updatedAt" SyntheticTimestamp_date_time.of_json in
      let createdAt =
        field_map json__ "createdAt" SyntheticTimestamp_date_time.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let source = field_map json__ "source" Source.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let trustAnchorArn = field_map json__ "trustAnchorArn" String_.of_json in
      let trustAnchorId = field_map json__ "trustAnchorId" Uuid.of_json in
      make ?notificationSettings ?updatedAt ?createdAt ?enabled ?source ?name
        ?trustAnchorArn ?trustAnchorId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The state of the trust anchor after a read or write operation."]
module SubjectSummary =
  struct
    type nonrec t =
      {
      subjectArn: String_.t option [@ocaml.doc "The ARN of the resource."];
      subjectId: Uuid.t option [@ocaml.doc "The id of the resource."];
      enabled: Boolean.t option
        [@ocaml.doc "The enabled status of the subject."];
      x509Subject: String_.t option
        [@ocaml.doc
          "The x509 principal identifier of the authenticating certificate."];
      lastSeenAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 time stamp of when the certificate was last used in a temporary credential request."];
      createdAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 time stamp of when the certificate was first used in a temporary credential request."];
      updatedAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 timestamp when the subject was last updated."]}
    let make ?subjectArn =
      fun ?subjectId ->
        fun ?enabled ->
          fun ?x509Subject ->
            fun ?lastSeenAt ->
              fun ?createdAt ->
                fun ?updatedAt ->
                  fun () ->
                    {
                      subjectArn;
                      subjectId;
                      enabled;
                      x509Subject;
                      lastSeenAt;
                      createdAt;
                      updatedAt
                    }
    let to_value x =
      structure_to_value
        [("subjectArn", (Option.map x.subjectArn ~f:String_.to_value));
        ("subjectId", (Option.map x.subjectId ~f:Uuid.to_value));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("x509Subject", (Option.map x.x509Subject ~f:String_.to_value));
        ("lastSeenAt",
          (Option.map x.lastSeenAt ~f:SyntheticTimestamp_date_time.to_value));
        ("createdAt",
          (Option.map x.createdAt ~f:SyntheticTimestamp_date_time.to_value));
        ("updatedAt",
          (Option.map x.updatedAt ~f:SyntheticTimestamp_date_time.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let lastSeenAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "lastSeenAt") in
      let x509Subject =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "x509Subject") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let subjectId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "subjectId") in
      let subjectArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "subjectArn") in
      make ?updatedAt ?createdAt ?lastSeenAt ?x509Subject ?enabled ?subjectId
        ?subjectArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt =
        field_map json__ "updatedAt" SyntheticTimestamp_date_time.of_json in
      let createdAt =
        field_map json__ "createdAt" SyntheticTimestamp_date_time.of_json in
      let lastSeenAt =
        field_map json__ "lastSeenAt" SyntheticTimestamp_date_time.of_json in
      let x509Subject = field_map json__ "x509Subject" String_.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let subjectId = field_map json__ "subjectId" Uuid.of_json in
      let subjectArn = field_map json__ "subjectArn" String_.of_json in
      make ?updatedAt ?createdAt ?lastSeenAt ?x509Subject ?enabled ?subjectId
        ?subjectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary representation of subjects."]
module ProfileDetail =
  struct
    type nonrec t =
      {
      profileId: Uuid.t option
        [@ocaml.doc "The unique identifier of the profile."];
      profileArn: ProfileArn.t option [@ocaml.doc "The ARN of the profile."];
      name: ResourceName.t option [@ocaml.doc "The name of the profile."];
      requireInstanceProperties: Boolean.t option
        [@ocaml.doc
          "Unused, saved for future use. Will likely specify whether instance properties are required in temporary credential requests with this profile."];
      enabled: Boolean.t option
        [@ocaml.doc "Indicates whether the profile is enabled."];
      createdBy: String_.t option
        [@ocaml.doc
          "The Amazon Web Services account that created the profile."];
      sessionPolicy: String_.t option
        [@ocaml.doc
          "A session policy that applies to the trust boundary of the vended session credentials."];
      roleArns: RoleArnList.t option
        [@ocaml.doc
          "A list of IAM roles that this profile can assume in a temporary credential request."];
      managedPolicyArns: ManagedPolicyList.t option
        [@ocaml.doc
          "A list of managed policy ARNs that apply to the vended session credentials."];
      createdAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc "The ISO-8601 timestamp when the profile was created."];
      updatedAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 timestamp when the profile was last updated."];
      durationSeconds: Integer.t option
        [@ocaml.doc
          "Used to determine how long sessions vended using this profile are valid for. See the Expiration section of the CreateSession API documentation page for more details. In requests, if this value is not provided, the default value will be 3600."];
      acceptRoleSessionName: Boolean.t option
        [@ocaml.doc
          "Used to determine if a custom role session name will be accepted in a temporary credential request."];
      attributeMappings: AttributeMappings.t option
        [@ocaml.doc
          "A mapping applied to the authenticating end-entity certificate."]}
    let make ?profileId =
      fun ?profileArn ->
        fun ?name ->
          fun ?requireInstanceProperties ->
            fun ?enabled ->
              fun ?createdBy ->
                fun ?sessionPolicy ->
                  fun ?roleArns ->
                    fun ?managedPolicyArns ->
                      fun ?createdAt ->
                        fun ?updatedAt ->
                          fun ?durationSeconds ->
                            fun ?acceptRoleSessionName ->
                              fun ?attributeMappings ->
                                fun () ->
                                  {
                                    profileId;
                                    profileArn;
                                    name;
                                    requireInstanceProperties;
                                    enabled;
                                    createdBy;
                                    sessionPolicy;
                                    roleArns;
                                    managedPolicyArns;
                                    createdAt;
                                    updatedAt;
                                    durationSeconds;
                                    acceptRoleSessionName;
                                    attributeMappings
                                  }
    let to_value x =
      structure_to_value
        [("profileId", (Option.map x.profileId ~f:Uuid.to_value));
        ("profileArn", (Option.map x.profileArn ~f:ProfileArn.to_value));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("requireInstanceProperties",
          (Option.map x.requireInstanceProperties ~f:Boolean.to_value));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("createdBy", (Option.map x.createdBy ~f:String_.to_value));
        ("sessionPolicy", (Option.map x.sessionPolicy ~f:String_.to_value));
        ("roleArns", (Option.map x.roleArns ~f:RoleArnList.to_value));
        ("managedPolicyArns",
          (Option.map x.managedPolicyArns ~f:ManagedPolicyList.to_value));
        ("createdAt",
          (Option.map x.createdAt ~f:SyntheticTimestamp_date_time.to_value));
        ("updatedAt",
          (Option.map x.updatedAt ~f:SyntheticTimestamp_date_time.to_value));
        ("durationSeconds",
          (Option.map x.durationSeconds ~f:Integer.to_value));
        ("acceptRoleSessionName",
          (Option.map x.acceptRoleSessionName ~f:Boolean.to_value));
        ("attributeMappings",
          (Option.map x.attributeMappings ~f:AttributeMappings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributeMappings =
        (Option.map ~f:AttributeMappings.of_xml)
          (Xml.child xml_arg0 "attributeMappings") in
      let acceptRoleSessionName =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "acceptRoleSessionName") in
      let durationSeconds =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "durationSeconds") in
      let updatedAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let managedPolicyArns =
        (Option.map ~f:ManagedPolicyList.of_xml)
          (Xml.child xml_arg0 "managedPolicyArns") in
      let roleArns =
        (Option.map ~f:RoleArnList.of_xml) (Xml.child xml_arg0 "roleArns") in
      let sessionPolicy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sessionPolicy") in
      let createdBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "createdBy") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let requireInstanceProperties =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "requireInstanceProperties") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let profileArn =
        (Option.map ~f:ProfileArn.of_xml) (Xml.child xml_arg0 "profileArn") in
      let profileId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "profileId") in
      make ?attributeMappings ?acceptRoleSessionName ?durationSeconds
        ?updatedAt ?createdAt ?managedPolicyArns ?roleArns ?sessionPolicy
        ?createdBy ?enabled ?requireInstanceProperties ?name ?profileArn
        ?profileId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let attributeMappings =
        field_map json__ "attributeMappings" AttributeMappings.of_json in
      let acceptRoleSessionName =
        field_map json__ "acceptRoleSessionName" Boolean.of_json in
      let durationSeconds =
        field_map json__ "durationSeconds" Integer.of_json in
      let updatedAt =
        field_map json__ "updatedAt" SyntheticTimestamp_date_time.of_json in
      let createdAt =
        field_map json__ "createdAt" SyntheticTimestamp_date_time.of_json in
      let managedPolicyArns =
        field_map json__ "managedPolicyArns" ManagedPolicyList.of_json in
      let roleArns = field_map json__ "roleArns" RoleArnList.of_json in
      let sessionPolicy = field_map json__ "sessionPolicy" String_.of_json in
      let createdBy = field_map json__ "createdBy" String_.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let requireInstanceProperties =
        field_map json__ "requireInstanceProperties" Boolean.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let profileArn = field_map json__ "profileArn" ProfileArn.of_json in
      let profileId = field_map json__ "profileId" Uuid.of_json in
      make ?attributeMappings ?acceptRoleSessionName ?durationSeconds
        ?updatedAt ?createdAt ?managedPolicyArns ?roleArns ?sessionPolicy
        ?createdBy ?enabled ?requireInstanceProperties ?name ?profileArn
        ?profileId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The state of the profile after a read or write operation."]
module CrlDetail =
  struct
    type nonrec t =
      {
      crlId: Uuid.t option
        [@ocaml.doc
          "The unique identifier of the certificate revocation list (CRL)."];
      crlArn: String_.t option
        [@ocaml.doc "The ARN of the certificate revocation list (CRL)."];
      name: String_.t option
        [@ocaml.doc "The name of the certificate revocation list (CRL)."];
      enabled: Boolean.t option
        [@ocaml.doc
          "Indicates whether the certificate revocation list (CRL) is enabled."];
      crlData: Blob.t option
        [@ocaml.doc
          "The state of the certificate revocation list (CRL) after a read or write operation."];
      trustAnchorArn: String_.t option
        [@ocaml.doc
          "The ARN of the TrustAnchor the certificate revocation list (CRL) will provide revocation for."];
      createdAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 timestamp when the certificate revocation list (CRL) was created."];
      updatedAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 timestamp when the certificate revocation list (CRL) was last updated."]}
    let make ?crlId =
      fun ?crlArn ->
        fun ?name ->
          fun ?enabled ->
            fun ?crlData ->
              fun ?trustAnchorArn ->
                fun ?createdAt ->
                  fun ?updatedAt ->
                    fun () ->
                      {
                        crlId;
                        crlArn;
                        name;
                        enabled;
                        crlData;
                        trustAnchorArn;
                        createdAt;
                        updatedAt
                      }
    let to_value x =
      structure_to_value
        [("crlId", (Option.map x.crlId ~f:Uuid.to_value));
        ("crlArn", (Option.map x.crlArn ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("crlData", (Option.map x.crlData ~f:Blob.to_value));
        ("trustAnchorArn", (Option.map x.trustAnchorArn ~f:String_.to_value));
        ("createdAt",
          (Option.map x.createdAt ~f:SyntheticTimestamp_date_time.to_value));
        ("updatedAt",
          (Option.map x.updatedAt ~f:SyntheticTimestamp_date_time.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let trustAnchorArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "trustAnchorArn") in
      let crlData =
        (Option.map ~f:Blob.of_xml) (Xml.child xml_arg0 "crlData") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let crlArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "crlArn") in
      let crlId = (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "crlId") in
      make ?updatedAt ?createdAt ?trustAnchorArn ?crlData ?enabled ?name
        ?crlArn ?crlId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt =
        field_map json__ "updatedAt" SyntheticTimestamp_date_time.of_json in
      let createdAt =
        field_map json__ "createdAt" SyntheticTimestamp_date_time.of_json in
      let trustAnchorArn = field_map json__ "trustAnchorArn" String_.of_json in
      let crlData = field_map json__ "crlData" Blob.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let name = field_map json__ "name" String_.of_json in
      let crlArn = field_map json__ "crlArn" String_.of_json in
      let crlId = field_map json__ "crlId" Uuid.of_json in
      make ?updatedAt ?createdAt ?trustAnchorArn ?crlData ?enabled ?name
        ?crlArn ?crlId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The state of the certificate revocation list (CRL) after a read or write operation."]
module UpdateProfileRequestDurationSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:43200) >>=
             (fun () -> check_int_min i ~min:900));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for UpdateProfileRequestDurationSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module UpdateProfileRequestSessionPolicyString =
  struct
    type nonrec t = string
    let context_ = "UpdateProfileRequestSessionPolicyString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"UpdateProfileRequestSessionPolicyString" j
    let to_json = simple_to_json to_value
  end
module UpdateCrlRequestCrlDataBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient access to perform this action."]
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource could not be found."]
module ValidationException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Validation exception error."]
module AmazonResourceName =
  struct
    type nonrec t = string
    let context_ = "AmazonResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1011) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AmazonResourceName" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TooManyTagsException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Too many tags."]
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module SubjectDetail =
  struct
    type nonrec t =
      {
      subjectArn: String_.t option [@ocaml.doc "The ARN of the resource."];
      subjectId: Uuid.t option [@ocaml.doc "The id of the resource"];
      enabled: Boolean.t option
        [@ocaml.doc "The enabled status of the subject."];
      x509Subject: String_.t option
        [@ocaml.doc
          "The x509 principal identifier of the authenticating certificate."];
      lastSeenAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 timestamp of the last time this subject requested temporary session credentials."];
      createdAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc "The ISO-8601 timestamp when the subject was created."];
      updatedAt: SyntheticTimestamp_date_time.t option
        [@ocaml.doc
          "The ISO-8601 timestamp when the subject was last updated."];
      credentials: CredentialSummaries.t option
        [@ocaml.doc
          "The temporary session credentials vended at the last authenticating call with this subject."];
      instanceProperties: InstanceProperties.t option
        [@ocaml.doc
          "The specified instance properties associated with the request."]}
    let make ?subjectArn =
      fun ?subjectId ->
        fun ?enabled ->
          fun ?x509Subject ->
            fun ?lastSeenAt ->
              fun ?createdAt ->
                fun ?updatedAt ->
                  fun ?credentials ->
                    fun ?instanceProperties ->
                      fun () ->
                        {
                          subjectArn;
                          subjectId;
                          enabled;
                          x509Subject;
                          lastSeenAt;
                          createdAt;
                          updatedAt;
                          credentials;
                          instanceProperties
                        }
    let to_value x =
      structure_to_value
        [("subjectArn", (Option.map x.subjectArn ~f:String_.to_value));
        ("subjectId", (Option.map x.subjectId ~f:Uuid.to_value));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("x509Subject", (Option.map x.x509Subject ~f:String_.to_value));
        ("lastSeenAt",
          (Option.map x.lastSeenAt ~f:SyntheticTimestamp_date_time.to_value));
        ("createdAt",
          (Option.map x.createdAt ~f:SyntheticTimestamp_date_time.to_value));
        ("updatedAt",
          (Option.map x.updatedAt ~f:SyntheticTimestamp_date_time.to_value));
        ("credentials",
          (Option.map x.credentials ~f:CredentialSummaries.to_value));
        ("instanceProperties",
          (Option.map x.instanceProperties ~f:InstanceProperties.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceProperties =
        (Option.map ~f:InstanceProperties.of_xml)
          (Xml.child xml_arg0 "instanceProperties") in
      let credentials =
        (Option.map ~f:CredentialSummaries.of_xml)
          (Xml.child xml_arg0 "credentials") in
      let updatedAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "createdAt") in
      let lastSeenAt =
        (Option.map ~f:SyntheticTimestamp_date_time.of_xml)
          (Xml.child xml_arg0 "lastSeenAt") in
      let x509Subject =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "x509Subject") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let subjectId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "subjectId") in
      let subjectArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "subjectArn") in
      make ?instanceProperties ?credentials ?updatedAt ?createdAt ?lastSeenAt
        ?x509Subject ?enabled ?subjectId ?subjectArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instanceProperties =
        field_map json__ "instanceProperties" InstanceProperties.of_json in
      let credentials =
        field_map json__ "credentials" CredentialSummaries.of_json in
      let updatedAt =
        field_map json__ "updatedAt" SyntheticTimestamp_date_time.of_json in
      let createdAt =
        field_map json__ "createdAt" SyntheticTimestamp_date_time.of_json in
      let lastSeenAt =
        field_map json__ "lastSeenAt" SyntheticTimestamp_date_time.of_json in
      let x509Subject = field_map json__ "x509Subject" String_.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let subjectId = field_map json__ "subjectId" Uuid.of_json in
      let subjectArn = field_map json__ "subjectArn" String_.of_json in
      make ?instanceProperties ?credentials ?updatedAt ?createdAt ?lastSeenAt
        ?x509Subject ?enabled ?subjectId ?subjectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The state of the subject after a read or write operation."]
module NotificationSettingKeys =
  struct
    type nonrec t = NotificationSettingKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NotificationSettingKey.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NotificationSettingKey.of_xml)
    let of_json j =
      list_of_json ~kind:"NotificationSettingKeys"
        ~of_json:NotificationSettingKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module NotificationSettings =
  struct
    type nonrec t = NotificationSetting.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:NotificationSetting.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NotificationSetting.of_xml)
    let of_json j =
      list_of_json ~kind:"NotificationSettings"
        ~of_json:NotificationSetting.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrustAnchorDetails =
  struct
    type nonrec t = TrustAnchorDetail.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TrustAnchorDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TrustAnchorDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"TrustAnchorDetails"
        ~of_json:TrustAnchorDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module SubjectSummaries =
  struct
    type nonrec t = SubjectSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SubjectSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SubjectSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SubjectSummaries" ~of_json:SubjectSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListRequestNextTokenString =
  struct
    type nonrec t = string
    let context_ = "ListRequestNextTokenString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ListRequestNextTokenString" j
    let to_json = simple_to_json to_value
  end
module ProfileDetails =
  struct
    type nonrec t = ProfileDetail.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ProfileDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProfileDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ProfileDetails" ~of_json:ProfileDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module CrlDetails =
  struct
    type nonrec t = CrlDetail.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CrlDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CrlDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"CrlDetails" ~of_json:CrlDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImportCrlRequestCrlDataBlob =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module TrustAnchorArn =
  struct
    type nonrec t = string
    let context_ = "TrustAnchorArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[^:]+)?:rolesanywhere(:.*){2}(:trust-anchor.*)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrustAnchorArn" j
    let to_json = simple_to_json to_value
  end
module SpecifierList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"SpecifierList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateProfileRequestDurationSecondsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:43200) >>=
             (fun () -> check_int_min i ~min:900));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for CreateProfileRequestDurationSecondsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module UpdateTrustAnchorRequest =
  struct
    type nonrec t =
      {
      trustAnchorId: Uuid.t
        [@ocaml.doc "The unique identifier of the trust anchor."];
      name: ResourceName.t option
        [@ocaml.doc "The name of the trust anchor."];
      source: Source.t option
        [@ocaml.doc
          "The trust anchor type and its related certificate data."]}
    let context_ = "UpdateTrustAnchorRequest"
    let make ?name =
      fun ?source ->
        fun ~trustAnchorId -> fun () -> { name; source; trustAnchorId }
    let to_value x =
      structure_to_value
        [("trustAnchorId", (Some (Uuid.to_value x.trustAnchorId)));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("source", (Option.map x.source ~f:Source.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let source =
        (Option.map ~f:Source.of_xml) (Xml.child xml_arg0 "source") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let trustAnchorId =
        Uuid.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "trustAnchorId") in
      make ?source ?name ~trustAnchorId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let source = field_map json__ "source" Source.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let trustAnchorId = field_map_exn json__ "trustAnchorId" Uuid.of_json in
      make ?source ?name ~trustAnchorId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a trust anchor. You establish trust between IAM Roles Anywhere and your certificate authority (CA) by configuring a trust anchor. You can define a trust anchor as a reference to an Private Certificate Authority (Private CA) or by uploading a CA certificate. Your Amazon Web Services workloads can authenticate with the trust anchor using certificates issued by the CA in exchange for temporary Amazon Web Services credentials. Required permissions: rolesanywhere:UpdateTrustAnchor."]
module UpdateProfileRequest =
  struct
    type nonrec t =
      {
      profileId: Uuid.t [@ocaml.doc "The unique identifier of the profile."];
      name: ResourceName.t option [@ocaml.doc "The name of the profile."];
      sessionPolicy: UpdateProfileRequestSessionPolicyString.t option
        [@ocaml.doc
          "A session policy that applies to the trust boundary of the vended session credentials."];
      roleArns: RoleArnList.t option
        [@ocaml.doc
          "A list of IAM roles that this profile can assume in a temporary credential request."];
      managedPolicyArns: ManagedPolicyList.t option
        [@ocaml.doc
          "A list of managed policy ARNs that apply to the vended session credentials."];
      durationSeconds: UpdateProfileRequestDurationSecondsInteger.t option
        [@ocaml.doc
          "Used to determine how long sessions vended using this profile are valid for. See the Expiration section of the CreateSession API documentation page for more details. In requests, if this value is not provided, the default value will be 3600."];
      acceptRoleSessionName: Boolean.t option
        [@ocaml.doc
          "Used to determine if a custom role session name will be accepted in a temporary credential request."]}
    let context_ = "UpdateProfileRequest"
    let make ?name =
      fun ?sessionPolicy ->
        fun ?roleArns ->
          fun ?managedPolicyArns ->
            fun ?durationSeconds ->
              fun ?acceptRoleSessionName ->
                fun ~profileId ->
                  fun () ->
                    {
                      name;
                      sessionPolicy;
                      roleArns;
                      managedPolicyArns;
                      durationSeconds;
                      acceptRoleSessionName;
                      profileId
                    }
    let to_value x =
      structure_to_value
        [("profileId", (Some (Uuid.to_value x.profileId)));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("sessionPolicy",
          (Option.map x.sessionPolicy
             ~f:UpdateProfileRequestSessionPolicyString.to_value));
        ("roleArns", (Option.map x.roleArns ~f:RoleArnList.to_value));
        ("managedPolicyArns",
          (Option.map x.managedPolicyArns ~f:ManagedPolicyList.to_value));
        ("durationSeconds",
          (Option.map x.durationSeconds
             ~f:UpdateProfileRequestDurationSecondsInteger.to_value));
        ("acceptRoleSessionName",
          (Option.map x.acceptRoleSessionName ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptRoleSessionName =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "acceptRoleSessionName") in
      let durationSeconds =
        (Option.map ~f:UpdateProfileRequestDurationSecondsInteger.of_xml)
          (Xml.child xml_arg0 "durationSeconds") in
      let managedPolicyArns =
        (Option.map ~f:ManagedPolicyList.of_xml)
          (Xml.child xml_arg0 "managedPolicyArns") in
      let roleArns =
        (Option.map ~f:RoleArnList.of_xml) (Xml.child xml_arg0 "roleArns") in
      let sessionPolicy =
        (Option.map ~f:UpdateProfileRequestSessionPolicyString.of_xml)
          (Xml.child xml_arg0 "sessionPolicy") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let profileId =
        Uuid.of_xml (Xml.child_exn ~context:context_ xml_arg0 "profileId") in
      make ?acceptRoleSessionName ?durationSeconds ?managedPolicyArns
        ?roleArns ?sessionPolicy ?name ~profileId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let acceptRoleSessionName =
        field_map json__ "acceptRoleSessionName" Boolean.of_json in
      let durationSeconds =
        field_map json__ "durationSeconds"
          UpdateProfileRequestDurationSecondsInteger.of_json in
      let managedPolicyArns =
        field_map json__ "managedPolicyArns" ManagedPolicyList.of_json in
      let roleArns = field_map json__ "roleArns" RoleArnList.of_json in
      let sessionPolicy =
        field_map json__ "sessionPolicy"
          UpdateProfileRequestSessionPolicyString.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let profileId = field_map_exn json__ "profileId" Uuid.of_json in
      make ?acceptRoleSessionName ?durationSeconds ?managedPolicyArns
        ?roleArns ?sessionPolicy ?name ~profileId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a profile, a list of the roles that IAM Roles Anywhere service is trusted to assume. You use profiles to intersect permissions with IAM managed policies. Required permissions: rolesanywhere:UpdateProfile."]
module UpdateCrlRequest =
  struct
    type nonrec t =
      {
      crlId: Uuid.t
        [@ocaml.doc
          "The unique identifier of the certificate revocation list (CRL)."];
      name: ResourceName.t option [@ocaml.doc "The name of the Crl."];
      crlData: UpdateCrlRequestCrlDataBlob.t option
        [@ocaml.doc
          "The x509 v3 specified certificate revocation list (CRL)."]}
    let context_ = "UpdateCrlRequest"
    let make ?name =
      fun ?crlData -> fun ~crlId -> fun () -> { name; crlData; crlId }
    let to_value x =
      structure_to_value
        [("crlId", (Some (Uuid.to_value x.crlId)));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("crlData",
          (Option.map x.crlData ~f:UpdateCrlRequestCrlDataBlob.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let crlData =
        (Option.map ~f:UpdateCrlRequestCrlDataBlob.of_xml)
          (Xml.child xml_arg0 "crlData") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let crlId =
        Uuid.of_xml (Xml.child_exn ~context:context_ xml_arg0 "crlId") in
      make ?crlData ?name ~crlId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let crlData =
        field_map json__ "crlData" UpdateCrlRequestCrlDataBlob.of_json in
      let name = field_map json__ "name" ResourceName.of_json in
      let crlId = field_map_exn json__ "crlId" Uuid.of_json in
      make ?crlData ?name ~crlId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the certificate revocation list (CRL). A CRL is a list of certificates that have been revoked by the issuing certificate authority (CA). IAM Roles Anywhere validates against the CRL before issuing credentials. Required permissions: rolesanywhere:UpdateCrl."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes tags from the resource. Required permissions: rolesanywhere:UntagResource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AmazonResourceName.t
        [@ocaml.doc "The ARN of the resource."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "A list of keys. Tag keys are the unique identifiers of tags."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (AmazonResourceName.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "tagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" AmazonResourceName.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes tags from the resource. Required permissions: rolesanywhere:UntagResource."]
module TrustAnchorDetailResponse =
  struct
    type nonrec t =
      {
      trustAnchor: TrustAnchorDetail.t option
        [@ocaml.doc
          "The state of the trust anchor after a read or write operation."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?trustAnchor = fun () -> { trustAnchor }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trustAnchor",
           (Option.map x.trustAnchor ~f:TrustAnchorDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trustAnchor =
        (Option.map ~f:TrustAnchorDetail.of_xml)
          (Xml.child xml_arg0 "trustAnchor") in
      make ?trustAnchor ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trustAnchor =
        field_map json__ "trustAnchor" TrustAnchorDetail.of_json in
      make ?trustAnchor ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a trust anchor to establish trust between IAM Roles Anywhere and your certificate authority (CA). You can define a trust anchor as a reference to an Private Certificate Authority (Private CA) or by uploading a CA certificate. Your Amazon Web Services workloads can authenticate with the trust anchor using certificates issued by the CA in exchange for temporary Amazon Web Services credentials. Required permissions: rolesanywhere:CreateTrustAnchor."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches tags to a resource. Required permissions: rolesanywhere:TagResource."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AmazonResourceName.t
        [@ocaml.doc "The ARN of the resource."];
      tags: TagList.t [@ocaml.doc "The tags to attach to the resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (AmazonResourceName.to_value x.resourceArn)));
        ("tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagList.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" AmazonResourceName.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches tags to a resource. Required permissions: rolesanywhere:TagResource."]
module SubjectDetailResponse =
  struct
    type nonrec t =
      {
      subject: SubjectDetail.t option
        [@ocaml.doc
          "The state of the subject after a read or write operation."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?subject = fun () -> { subject }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subject", (Option.map x.subject ~f:SubjectDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subject =
        (Option.map ~f:SubjectDetail.of_xml) (Xml.child xml_arg0 "subject") in
      make ?subject ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subject = field_map json__ "subject" SubjectDetail.of_json in
      make ?subject ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a subject, which associates a certificate identity with authentication attempts. The subject stores auditing information such as the status of the last authentication attempt, the certificate data used in the attempt, and the last time the associated identity attempted authentication. Required permissions: rolesanywhere:GetSubject."]
module ScalarTrustAnchorRequest =
  struct
    type nonrec t =
      {
      trustAnchorId: Uuid.t
        [@ocaml.doc "The unique identifier of the trust anchor."]}
    let context_ = "ScalarTrustAnchorRequest"
    let make ~trustAnchorId = fun () -> { trustAnchorId }
    let to_value x =
      structure_to_value
        [("trustAnchorId", (Some (Uuid.to_value x.trustAnchorId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trustAnchorId =
        Uuid.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "trustAnchorId") in
      make ~trustAnchorId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trustAnchorId = field_map_exn json__ "trustAnchorId" Uuid.of_json in
      make ~trustAnchorId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a trust anchor. Required permissions: rolesanywhere:DeleteTrustAnchor."]
module ScalarSubjectRequest =
  struct
    type nonrec t =
      {
      subjectId: Uuid.t [@ocaml.doc "The unique identifier of the subject."]}
    let context_ = "ScalarSubjectRequest"
    let make ~subjectId = fun () -> { subjectId }
    let to_value x =
      structure_to_value [("subjectId", (Some (Uuid.to_value x.subjectId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subjectId =
        Uuid.of_xml (Xml.child_exn ~context:context_ xml_arg0 "subjectId") in
      make ~subjectId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subjectId = field_map_exn json__ "subjectId" Uuid.of_json in
      make ~subjectId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a subject, which associates a certificate identity with authentication attempts. The subject stores auditing information such as the status of the last authentication attempt, the certificate data used in the attempt, and the last time the associated identity attempted authentication. Required permissions: rolesanywhere:GetSubject."]
module ScalarProfileRequest =
  struct
    type nonrec t =
      {
      profileId: Uuid.t [@ocaml.doc "The unique identifier of the profile."]}
    let context_ = "ScalarProfileRequest"
    let make ~profileId = fun () -> { profileId }
    let to_value x =
      structure_to_value [("profileId", (Some (Uuid.to_value x.profileId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let profileId =
        Uuid.of_xml (Xml.child_exn ~context:context_ xml_arg0 "profileId") in
      make ~profileId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let profileId = field_map_exn json__ "profileId" Uuid.of_json in
      make ~profileId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a profile. Required permissions: rolesanywhere:DeleteProfile."]
module ScalarCrlRequest =
  struct
    type nonrec t =
      {
      crlId: Uuid.t
        [@ocaml.doc
          "The unique identifier of the certificate revocation list (CRL)."]}
    let context_ = "ScalarCrlRequest"
    let make ~crlId = fun () -> { crlId }
    let to_value x =
      structure_to_value [("crlId", (Some (Uuid.to_value x.crlId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let crlId =
        Uuid.of_xml (Xml.child_exn ~context:context_ xml_arg0 "crlId") in
      make ~crlId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let crlId = field_map_exn json__ "crlId" Uuid.of_json in make ~crlId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a certificate revocation list (CRL). Required permissions: rolesanywhere:DeleteCrl."]
module ResetNotificationSettingsResponse =
  struct
    type nonrec t = {
      trustAnchor: TrustAnchorDetail.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?trustAnchor = fun () -> { trustAnchor }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trustAnchor",
           (Option.map x.trustAnchor ~f:TrustAnchorDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trustAnchor =
        (Option.map ~f:TrustAnchorDetail.of_xml)
          (Xml.child xml_arg0 "trustAnchor") in
      make ?trustAnchor ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trustAnchor =
        field_map json__ "trustAnchor" TrustAnchorDetail.of_json in
      make ?trustAnchor ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets the custom notification setting to IAM Roles Anywhere default setting. Required permissions: rolesanywhere:ResetNotificationSettings."]
module ResetNotificationSettingsRequest =
  struct
    type nonrec t =
      {
      trustAnchorId: Uuid.t
        [@ocaml.doc "The unique identifier of the trust anchor."];
      notificationSettingKeys: NotificationSettingKeys.t
        [@ocaml.doc
          "A list of notification setting keys to reset. A notification setting key includes the event and the channel."]}
    let context_ = "ResetNotificationSettingsRequest"
    let make ~trustAnchorId =
      fun ~notificationSettingKeys ->
        fun () -> { trustAnchorId; notificationSettingKeys }
    let to_value x =
      structure_to_value
        [("trustAnchorId", (Some (Uuid.to_value x.trustAnchorId)));
        ("notificationSettingKeys",
          (Some (NotificationSettingKeys.to_value x.notificationSettingKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationSettingKeys =
        NotificationSettingKeys.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "notificationSettingKeys") in
      let trustAnchorId =
        Uuid.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "trustAnchorId") in
      make ~notificationSettingKeys ~trustAnchorId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let notificationSettingKeys =
        field_map_exn json__ "notificationSettingKeys"
          NotificationSettingKeys.of_json in
      let trustAnchorId = field_map_exn json__ "trustAnchorId" Uuid.of_json in
      make ~notificationSettingKeys ~trustAnchorId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Resets the custom notification setting to IAM Roles Anywhere default setting. Required permissions: rolesanywhere:ResetNotificationSettings."]
module PutNotificationSettingsResponse =
  struct
    type nonrec t = {
      trustAnchor: TrustAnchorDetail.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?trustAnchor = fun () -> { trustAnchor }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("trustAnchor",
           (Option.map x.trustAnchor ~f:TrustAnchorDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trustAnchor =
        (Option.map ~f:TrustAnchorDetail.of_xml)
          (Xml.child xml_arg0 "trustAnchor") in
      make ?trustAnchor ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trustAnchor =
        field_map json__ "trustAnchor" TrustAnchorDetail.of_json in
      make ?trustAnchor ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches a list of notification settings to a trust anchor. A notification setting includes information such as event name, threshold, status of the notification setting, and the channel to notify. Required permissions: rolesanywhere:PutNotificationSettings."]
module PutNotificationSettingsRequest =
  struct
    type nonrec t =
      {
      trustAnchorId: Uuid.t
        [@ocaml.doc "The unique identifier of the trust anchor."];
      notificationSettings: NotificationSettings.t
        [@ocaml.doc
          "A list of notification settings to be associated to the trust anchor."]}
    let context_ = "PutNotificationSettingsRequest"
    let make ~trustAnchorId =
      fun ~notificationSettings ->
        fun () -> { trustAnchorId; notificationSettings }
    let to_value x =
      structure_to_value
        [("trustAnchorId", (Some (Uuid.to_value x.trustAnchorId)));
        ("notificationSettings",
          (Some (NotificationSettings.to_value x.notificationSettings)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationSettings =
        NotificationSettings.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "notificationSettings") in
      let trustAnchorId =
        Uuid.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "trustAnchorId") in
      make ~notificationSettings ~trustAnchorId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let notificationSettings =
        field_map_exn json__ "notificationSettings"
          NotificationSettings.of_json in
      let trustAnchorId = field_map_exn json__ "trustAnchorId" Uuid.of_json in
      make ~notificationSettings ~trustAnchorId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Attaches a list of notification settings to a trust anchor. A notification setting includes information such as event name, threshold, status of the notification setting, and the channel to notify. Required permissions: rolesanywhere:PutNotificationSettings."]
module PutAttributeMappingResponse =
  struct
    type nonrec t =
      {
      profile: ProfileDetail.t option
        [@ocaml.doc
          "The state of the profile after a read or write operation."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?profile = fun () -> { profile }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("profile", (Option.map x.profile ~f:ProfileDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let profile =
        (Option.map ~f:ProfileDetail.of_xml) (Xml.child xml_arg0 "profile") in
      make ?profile ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let profile = field_map json__ "profile" ProfileDetail.of_json in
      make ?profile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Put an entry in the attribute mapping rules that will be enforced by a given profile. A mapping specifies a certificate field and one or more specifiers that have contextual meanings."]
module PutAttributeMappingRequest =
  struct
    type nonrec t =
      {
      profileId: Uuid.t [@ocaml.doc "The unique identifier of the profile."];
      certificateField: CertificateField.t
        [@ocaml.doc
          "Fields (x509Subject, x509Issuer and x509SAN) within X.509 certificates."];
      mappingRules: MappingRules.t
        [@ocaml.doc
          "A list of mapping entries for every supported specifier or sub-field."]}
    let context_ = "PutAttributeMappingRequest"
    let make ~profileId =
      fun ~certificateField ->
        fun ~mappingRules ->
          fun () -> { profileId; certificateField; mappingRules }
    let to_value x =
      structure_to_value
        [("profileId", (Some (Uuid.to_value x.profileId)));
        ("certificateField",
          (Some (CertificateField.to_value x.certificateField)));
        ("mappingRules", (Some (MappingRules.to_value x.mappingRules)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mappingRules =
        MappingRules.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "mappingRules") in
      let certificateField =
        CertificateField.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "certificateField") in
      let profileId =
        Uuid.of_xml (Xml.child_exn ~context:context_ xml_arg0 "profileId") in
      make ~mappingRules ~certificateField ~profileId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let mappingRules =
        field_map_exn json__ "mappingRules" MappingRules.of_json in
      let certificateField =
        field_map_exn json__ "certificateField" CertificateField.of_json in
      let profileId = field_map_exn json__ "profileId" Uuid.of_json in
      make ~mappingRules ~certificateField ~profileId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Put an entry in the attribute mapping rules that will be enforced by a given profile. A mapping specifies a certificate field and one or more specifiers that have contextual meanings."]
module ProfileDetailResponse =
  struct
    type nonrec t =
      {
      profile: ProfileDetail.t option
        [@ocaml.doc
          "The state of the profile after a read or write operation."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?profile = fun () -> { profile }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("profile", (Option.map x.profile ~f:ProfileDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let profile =
        (Option.map ~f:ProfileDetail.of_xml) (Xml.child xml_arg0 "profile") in
      make ?profile ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let profile = field_map json__ "profile" ProfileDetail.of_json in
      make ?profile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a profile, a list of the roles that Roles Anywhere service is trusted to assume. You use profiles to intersect permissions with IAM managed policies. Required permissions: rolesanywhere:CreateProfile."]
module ListTrustAnchorsResponse =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "A token that indicates where the output should continue from, if a previous request did not show all results. To get the next results, make the request again with this value."];
      trustAnchors: TrustAnchorDetails.t option
        [@ocaml.doc "A list of trust anchors."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?trustAnchors -> fun () -> { nextToken; trustAnchors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("trustAnchors",
          (Option.map x.trustAnchors ~f:TrustAnchorDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trustAnchors =
        (Option.map ~f:TrustAnchorDetails.of_xml)
          (Xml.child xml_arg0 "trustAnchors") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?trustAnchors ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trustAnchors =
        field_map json__ "trustAnchors" TrustAnchorDetails.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?trustAnchors ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the trust anchors in the authenticated account and Amazon Web Services Region. Required permissions: rolesanywhere:ListTrustAnchors."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagList.t option
        [@ocaml.doc "A list of tags attached to the resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the tags attached to the resource. Required permissions: rolesanywhere:ListTagsForResource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AmazonResourceName.t
        [@ocaml.doc "The ARN of the resource."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (AmazonResourceName.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" AmazonResourceName.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the tags attached to the resource. Required permissions: rolesanywhere:ListTagsForResource."]
module ListSubjectsResponse =
  struct
    type nonrec t =
      {
      subjects: SubjectSummaries.t option [@ocaml.doc "A list of subjects."];
      nextToken: String_.t option
        [@ocaml.doc
          "A token that indicates where the output should continue from, if a previous request did not show all results. To get the next results, make the request again with this value."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?subjects = fun ?nextToken -> fun () -> { subjects; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("subjects", (Option.map x.subjects ~f:SubjectSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let subjects =
        (Option.map ~f:SubjectSummaries.of_xml)
          (Xml.child xml_arg0 "subjects") in
      make ?nextToken ?subjects ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let subjects = field_map json__ "subjects" SubjectSummaries.of_json in
      make ?nextToken ?subjects ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the subjects in the authenticated account and Amazon Web Services Region. Required permissions: rolesanywhere:ListSubjects."]
module ListRequest =
  struct
    type nonrec t =
      {
      nextToken: ListRequestNextTokenString.t option
        [@ocaml.doc
          "A token that indicates where the output should continue from, if a previous request did not show all results. To get the next results, make the request again with this value."];
      pageSize: Integer.t option
        [@ocaml.doc "The number of resources in the paginated list."]}
    let make ?nextToken = fun ?pageSize -> fun () -> { nextToken; pageSize }
    let to_value x =
      structure_to_value
        [("nextToken",
           (Option.map x.nextToken ~f:ListRequestNextTokenString.to_value));
        ("pageSize", (Option.map x.pageSize ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pageSize =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "pageSize") in
      let nextToken =
        (Option.map ~f:ListRequestNextTokenString.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?pageSize ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pageSize = field_map json__ "pageSize" Integer.of_json in
      let nextToken =
        field_map json__ "nextToken" ListRequestNextTokenString.of_json in
      make ?pageSize ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all certificate revocation lists (CRL) in the authenticated account and Amazon Web Services Region. Required permissions: rolesanywhere:ListCrls."]
module ListProfilesResponse =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "A token that indicates where the output should continue from, if a previous request did not show all results. To get the next results, make the request again with this value."];
      profiles: ProfileDetails.t option [@ocaml.doc "A list of profiles."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?profiles -> fun () -> { nextToken; profiles }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("profiles", (Option.map x.profiles ~f:ProfileDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let profiles =
        (Option.map ~f:ProfileDetails.of_xml) (Xml.child xml_arg0 "profiles") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?profiles ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let profiles = field_map json__ "profiles" ProfileDetails.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?profiles ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all profiles in the authenticated account and Amazon Web Services Region. Required permissions: rolesanywhere:ListProfiles."]
module ListCrlsResponse =
  struct
    type nonrec t =
      {
      nextToken: String_.t option
        [@ocaml.doc
          "A token that indicates where the output should continue from, if a previous request did not show all results. To get the next results, make the request again with this value."];
      crls: CrlDetails.t option
        [@ocaml.doc "A list of certificate revocation lists (CRL)."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?crls -> fun () -> { nextToken; crls }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("crls", (Option.map x.crls ~f:CrlDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let crls =
        (Option.map ~f:CrlDetails.of_xml) (Xml.child xml_arg0 "crls") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?crls ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let crls = field_map json__ "crls" CrlDetails.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      make ?crls ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all certificate revocation lists (CRL) in the authenticated account and Amazon Web Services Region. Required permissions: rolesanywhere:ListCrls."]
module ImportCrlRequest =
  struct
    type nonrec t =
      {
      name: ResourceName.t
        [@ocaml.doc "The name of the certificate revocation list (CRL)."];
      crlData: ImportCrlRequestCrlDataBlob.t
        [@ocaml.doc
          "The x509 v3 specified certificate revocation list (CRL)."];
      enabled: Boolean.t option
        [@ocaml.doc
          "Specifies whether the certificate revocation list (CRL) is enabled."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tags to attach to the certificate revocation list (CRL)."];
      trustAnchorArn: TrustAnchorArn.t
        [@ocaml.doc
          "The ARN of the TrustAnchor the certificate revocation list (CRL) will provide revocation for."]}
    let context_ = "ImportCrlRequest"
    let make ?enabled =
      fun ?tags ->
        fun ~name ->
          fun ~crlData ->
            fun ~trustAnchorArn ->
              fun () -> { enabled; tags; name; crlData; trustAnchorArn }
    let to_value x =
      structure_to_value
        [("name", (Some (ResourceName.to_value x.name)));
        ("crlData", (Some (ImportCrlRequestCrlDataBlob.to_value x.crlData)));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value));
        ("trustAnchorArn", (Some (TrustAnchorArn.to_value x.trustAnchorArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trustAnchorArn =
        TrustAnchorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "trustAnchorArn") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let crlData =
        ImportCrlRequestCrlDataBlob.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "crlData") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~trustAnchorArn ?tags ?enabled ~crlData ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let trustAnchorArn =
        field_map_exn json__ "trustAnchorArn" TrustAnchorArn.of_json in
      let tags = field_map json__ "tags" TagList.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let crlData =
        field_map_exn json__ "crlData" ImportCrlRequestCrlDataBlob.of_json in
      let name = field_map_exn json__ "name" ResourceName.of_json in
      make ~trustAnchorArn ?tags ?enabled ~crlData ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Imports the certificate revocation list (CRL). A CRL is a list of certificates that have been revoked by the issuing certificate Authority (CA).In order to be properly imported, a CRL must be in PEM format. IAM Roles Anywhere validates against the CRL before issuing credentials. Required permissions: rolesanywhere:ImportCrl."]
module DeleteAttributeMappingResponse =
  struct
    type nonrec t =
      {
      profile: ProfileDetail.t option
        [@ocaml.doc
          "The state of the profile after a read or write operation."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?profile = fun () -> { profile }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("profile", (Option.map x.profile ~f:ProfileDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let profile =
        (Option.map ~f:ProfileDetail.of_xml) (Xml.child xml_arg0 "profile") in
      make ?profile ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let profile = field_map json__ "profile" ProfileDetail.of_json in
      make ?profile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete an entry from the attribute mapping rules enforced by a given profile."]
module DeleteAttributeMappingRequest =
  struct
    type nonrec t =
      {
      profileId: Uuid.t [@ocaml.doc "The unique identifier of the profile."];
      certificateField: CertificateField.t
        [@ocaml.doc
          "Fields (x509Subject, x509Issuer and x509SAN) within X.509 certificates."];
      specifiers: SpecifierList.t option
        [@ocaml.doc
          "A list of specifiers of a certificate field; for example, CN, OU, UID from a Subject."]}
    let context_ = "DeleteAttributeMappingRequest"
    let make ?specifiers =
      fun ~profileId ->
        fun ~certificateField ->
          fun () -> { specifiers; profileId; certificateField }
    let to_value x =
      structure_to_value
        [("profileId", (Some (Uuid.to_value x.profileId)));
        ("certificateField",
          (Some (CertificateField.to_value x.certificateField)));
        ("specifiers", (Option.map x.specifiers ~f:SpecifierList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let specifiers =
        (Option.map ~f:SpecifierList.of_xml)
          (Xml.child xml_arg0 "specifiers") in
      let certificateField =
        CertificateField.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "certificateField") in
      let profileId =
        Uuid.of_xml (Xml.child_exn ~context:context_ xml_arg0 "profileId") in
      make ?specifiers ~certificateField ~profileId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let specifiers = field_map json__ "specifiers" SpecifierList.of_json in
      let certificateField =
        field_map_exn json__ "certificateField" CertificateField.of_json in
      let profileId = field_map_exn json__ "profileId" Uuid.of_json in
      make ?specifiers ~certificateField ~profileId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete an entry from the attribute mapping rules enforced by a given profile."]
module CrlDetailResponse =
  struct
    type nonrec t =
      {
      crl: CrlDetail.t option
        [@ocaml.doc
          "The state of the certificate revocation list (CRL) after a read or write operation."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?crl = fun () -> { crl }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("crl", (Option.map x.crl ~f:CrlDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let crl = (Option.map ~f:CrlDetail.of_xml) (Xml.child xml_arg0 "crl") in
      make ?crl ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let crl = field_map json__ "crl" CrlDetail.of_json in make ?crl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a certificate revocation list (CRL). Required permissions: rolesanywhere:DeleteCrl."]
module CreateTrustAnchorRequest =
  struct
    type nonrec t =
      {
      name: ResourceName.t [@ocaml.doc "The name of the trust anchor."];
      source: Source.t
        [@ocaml.doc
          "The trust anchor type and its related certificate data."];
      enabled: Boolean.t option
        [@ocaml.doc "Specifies whether the trust anchor is enabled."];
      tags: TagList.t option
        [@ocaml.doc "The tags to attach to the trust anchor."];
      notificationSettings: NotificationSettings.t option
        [@ocaml.doc
          "A list of notification settings to be associated to the trust anchor."]}
    let context_ = "CreateTrustAnchorRequest"
    let make ?enabled =
      fun ?tags ->
        fun ?notificationSettings ->
          fun ~name ->
            fun ~source ->
              fun () -> { enabled; tags; notificationSettings; name; source }
    let to_value x =
      structure_to_value
        [("name", (Some (ResourceName.to_value x.name)));
        ("source", (Some (Source.to_value x.source)));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value));
        ("notificationSettings",
          (Option.map x.notificationSettings ~f:NotificationSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notificationSettings =
        (Option.map ~f:NotificationSettings.of_xml)
          (Xml.child xml_arg0 "notificationSettings") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let source =
        Source.of_xml (Xml.child_exn ~context:context_ xml_arg0 "source") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?notificationSettings ?tags ?enabled ~source ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let notificationSettings =
        field_map json__ "notificationSettings" NotificationSettings.of_json in
      let tags = field_map json__ "tags" TagList.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let source = field_map_exn json__ "source" Source.of_json in
      let name = field_map_exn json__ "name" ResourceName.of_json in
      make ?notificationSettings ?tags ?enabled ~source ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a trust anchor to establish trust between IAM Roles Anywhere and your certificate authority (CA). You can define a trust anchor as a reference to an Private Certificate Authority (Private CA) or by uploading a CA certificate. Your Amazon Web Services workloads can authenticate with the trust anchor using certificates issued by the CA in exchange for temporary Amazon Web Services credentials. Required permissions: rolesanywhere:CreateTrustAnchor."]
module CreateProfileRequest =
  struct
    type nonrec t =
      {
      name: ResourceName.t [@ocaml.doc "The name of the profile."];
      requireInstanceProperties: Boolean.t option
        [@ocaml.doc
          "Unused, saved for future use. Will likely specify whether instance properties are required in temporary credential requests with this profile."];
      sessionPolicy: String_.t option
        [@ocaml.doc
          "A session policy that applies to the trust boundary of the vended session credentials."];
      roleArns: RoleArnList.t
        [@ocaml.doc
          "A list of IAM roles that this profile can assume in a temporary credential request."];
      managedPolicyArns: ManagedPolicyList.t option
        [@ocaml.doc
          "A list of managed policy ARNs that apply to the vended session credentials."];
      durationSeconds: CreateProfileRequestDurationSecondsInteger.t option
        [@ocaml.doc
          "Used to determine how long sessions vended using this profile are valid for. See the Expiration section of the CreateSession API documentation page for more details. In requests, if this value is not provided, the default value will be 3600."];
      enabled: Boolean.t option
        [@ocaml.doc "Specifies whether the profile is enabled."];
      tags: TagList.t option
        [@ocaml.doc "The tags to attach to the profile."];
      acceptRoleSessionName: Boolean.t option
        [@ocaml.doc
          "Used to determine if a custom role session name will be accepted in a temporary credential request."]}
    let context_ = "CreateProfileRequest"
    let make ?requireInstanceProperties =
      fun ?sessionPolicy ->
        fun ?managedPolicyArns ->
          fun ?durationSeconds ->
            fun ?enabled ->
              fun ?tags ->
                fun ?acceptRoleSessionName ->
                  fun ~name ->
                    fun ~roleArns ->
                      fun () ->
                        {
                          requireInstanceProperties;
                          sessionPolicy;
                          managedPolicyArns;
                          durationSeconds;
                          enabled;
                          tags;
                          acceptRoleSessionName;
                          name;
                          roleArns
                        }
    let to_value x =
      structure_to_value
        [("name", (Some (ResourceName.to_value x.name)));
        ("requireInstanceProperties",
          (Option.map x.requireInstanceProperties ~f:Boolean.to_value));
        ("sessionPolicy", (Option.map x.sessionPolicy ~f:String_.to_value));
        ("roleArns", (Some (RoleArnList.to_value x.roleArns)));
        ("managedPolicyArns",
          (Option.map x.managedPolicyArns ~f:ManagedPolicyList.to_value));
        ("durationSeconds",
          (Option.map x.durationSeconds
             ~f:CreateProfileRequestDurationSecondsInteger.to_value));
        ("enabled", (Option.map x.enabled ~f:Boolean.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value));
        ("acceptRoleSessionName",
          (Option.map x.acceptRoleSessionName ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let acceptRoleSessionName =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "acceptRoleSessionName") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let enabled =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "enabled") in
      let durationSeconds =
        (Option.map ~f:CreateProfileRequestDurationSecondsInteger.of_xml)
          (Xml.child xml_arg0 "durationSeconds") in
      let managedPolicyArns =
        (Option.map ~f:ManagedPolicyList.of_xml)
          (Xml.child xml_arg0 "managedPolicyArns") in
      let roleArns =
        RoleArnList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "roleArns") in
      let sessionPolicy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sessionPolicy") in
      let requireInstanceProperties =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "requireInstanceProperties") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?acceptRoleSessionName ?tags ?enabled ?durationSeconds
        ?managedPolicyArns ~roleArns ?sessionPolicy
        ?requireInstanceProperties ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let acceptRoleSessionName =
        field_map json__ "acceptRoleSessionName" Boolean.of_json in
      let tags = field_map json__ "tags" TagList.of_json in
      let enabled = field_map json__ "enabled" Boolean.of_json in
      let durationSeconds =
        field_map json__ "durationSeconds"
          CreateProfileRequestDurationSecondsInteger.of_json in
      let managedPolicyArns =
        field_map json__ "managedPolicyArns" ManagedPolicyList.of_json in
      let roleArns = field_map_exn json__ "roleArns" RoleArnList.of_json in
      let sessionPolicy = field_map json__ "sessionPolicy" String_.of_json in
      let requireInstanceProperties =
        field_map json__ "requireInstanceProperties" Boolean.of_json in
      let name = field_map_exn json__ "name" ResourceName.of_json in
      make ?acceptRoleSessionName ?tags ?enabled ?durationSeconds
        ?managedPolicyArns ~roleArns ?sessionPolicy
        ?requireInstanceProperties ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a profile, a list of the roles that Roles Anywhere service is trusted to assume. You use profiles to intersect permissions with IAM managed policies. Required permissions: rolesanywhere:CreateProfile."]
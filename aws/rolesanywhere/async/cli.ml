(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requireInstanceProperties =
         flag "require-instance-properties" (optional bool)
           ~doc:"BOOL Boolean"
       and sessionPolicy =
         flag "session-policy" (optional string) ~doc:"STRING String"
       and managedPolicyArns =
         flag "managed-policy-arns" (optional json_arg)
           ~doc:"JSON ManagedPolicyList"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT CreateProfileRequestDurationSecondsInteger"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and acceptRoleSessionName =
         flag "accept-role-session-name" (optional bool) ~doc:"BOOL Boolean"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and roleArns =
         flag "role-arns" (required json_arg) ~doc:"JSON RoleArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_profile
           (Values.CreateProfileRequest.make ?requireInstanceProperties
              ?sessionPolicy
              ?managedPolicyArns:(Option.map
                                    ~f:Values.ManagedPolicyList.of_json
                                    managedPolicyArns) ?durationSeconds
              ?enabled ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?acceptRoleSessionName ~name
              ~roleArns:(Values.RoleArnList.of_json roleArns) ())
           (Some Values.ProfileDetailResponse.to_json)
           (Some Values.ProfileDetailResponse.error_to_json)])
let create_trust_anchor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and notificationSettings =
         flag "notification-settings" (optional json_arg)
           ~doc:"JSON NotificationSettings"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and source = flag "source" (required json_arg) ~doc:"JSON Source" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_trust_anchor
           (Values.CreateTrustAnchorRequest.make ?enabled
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?notificationSettings:(Option.map
                                       ~f:Values.NotificationSettings.of_json
                                       notificationSettings) ~name
              ~source:(Values.Source.of_json source) ())
           (Some Values.TrustAnchorDetailResponse.to_json)
           (Some Values.TrustAnchorDetailResponse.error_to_json)])
let delete_attribute_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and specifiers =
         flag "specifiers" (optional json_arg) ~doc:"JSON SpecifierList"
       and profileId = flag "profile-id" (required string) ~doc:"STRING Uuid"
       and certificateField =
         flag "certificate-field" (required json_arg)
           ~doc:"JSON CertificateField" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_attribute_mapping
           (Values.DeleteAttributeMappingRequest.make
              ?specifiers:(Option.map ~f:Values.SpecifierList.of_json
                             specifiers) ~profileId
              ~certificateField:(Values.CertificateField.of_json
                                   certificateField) ())
           (Some Values.DeleteAttributeMappingResponse.to_json)
           (Some Values.DeleteAttributeMappingResponse.error_to_json)])
let delete_crl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crlId = flag "crl-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_crl (Values.ScalarCrlRequest.make ~crlId ())
           (Some Values.CrlDetailResponse.to_json)
           (Some Values.CrlDetailResponse.error_to_json)])
let delete_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_profile (Values.ScalarProfileRequest.make ~profileId ())
           (Some Values.ProfileDetailResponse.to_json)
           (Some Values.ProfileDetailResponse.error_to_json)])
let delete_trust_anchor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trustAnchorId =
         flag "trust-anchor-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_trust_anchor
           (Values.ScalarTrustAnchorRequest.make ~trustAnchorId ())
           (Some Values.TrustAnchorDetailResponse.to_json)
           (Some Values.TrustAnchorDetailResponse.error_to_json)])
let disable_crl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crlId = flag "crl-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_crl (Values.ScalarCrlRequest.make ~crlId ())
           (Some Values.CrlDetailResponse.to_json)
           (Some Values.CrlDetailResponse.error_to_json)])
let disable_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_profile
           (Values.ScalarProfileRequest.make ~profileId ())
           (Some Values.ProfileDetailResponse.to_json)
           (Some Values.ProfileDetailResponse.error_to_json)])
let disable_trust_anchor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trustAnchorId =
         flag "trust-anchor-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_trust_anchor
           (Values.ScalarTrustAnchorRequest.make ~trustAnchorId ())
           (Some Values.TrustAnchorDetailResponse.to_json)
           (Some Values.TrustAnchorDetailResponse.error_to_json)])
let enable_crl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crlId = flag "crl-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_crl (Values.ScalarCrlRequest.make ~crlId ())
           (Some Values.CrlDetailResponse.to_json)
           (Some Values.CrlDetailResponse.error_to_json)])
let enable_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_profile (Values.ScalarProfileRequest.make ~profileId ())
           (Some Values.ProfileDetailResponse.to_json)
           (Some Values.ProfileDetailResponse.error_to_json)])
let enable_trust_anchor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trustAnchorId =
         flag "trust-anchor-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_trust_anchor
           (Values.ScalarTrustAnchorRequest.make ~trustAnchorId ())
           (Some Values.TrustAnchorDetailResponse.to_json)
           (Some Values.TrustAnchorDetailResponse.error_to_json)])
let get_crl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crlId = flag "crl-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_crl (Values.ScalarCrlRequest.make ~crlId ())
           (Some Values.CrlDetailResponse.to_json)
           (Some Values.CrlDetailResponse.error_to_json)])
let get_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile (Values.ScalarProfileRequest.make ~profileId ())
           (Some Values.ProfileDetailResponse.to_json)
           (Some Values.ProfileDetailResponse.error_to_json)])
let get_subject =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subjectId = flag "subject-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subject (Values.ScalarSubjectRequest.make ~subjectId ())
           (Some Values.SubjectDetailResponse.to_json)
           (Some Values.SubjectDetailResponse.error_to_json)])
let get_trust_anchor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trustAnchorId =
         flag "trust-anchor-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_trust_anchor
           (Values.ScalarTrustAnchorRequest.make ~trustAnchorId ())
           (Some Values.TrustAnchorDetailResponse.to_json)
           (Some Values.TrustAnchorDetailResponse.error_to_json)])
let import_crl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and crlData =
         flag "crl-data" (required json_arg)
           ~doc:"JSON ImportCrlRequestCrlDataBlob"
       and trustAnchorArn =
         flag "trust-anchor-arn" (required string)
           ~doc:"STRING TrustAnchorArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_crl
           (Values.ImportCrlRequest.make ?enabled
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~crlData:(Values.ImportCrlRequestCrlDataBlob.of_json crlData)
              ~trustAnchorArn ()) (Some Values.CrlDetailResponse.to_json)
           (Some Values.CrlDetailResponse.error_to_json)])
let list_crls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListRequestNextTokenString"
       and pageSize = flag "page-size" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_crls (Values.ListRequest.make ?nextToken ?pageSize ())
           (Some Values.ListCrlsResponse.to_json)
           (Some Values.ListCrlsResponse.error_to_json)])
let list_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListRequestNextTokenString"
       and pageSize = flag "page-size" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profiles (Values.ListRequest.make ?nextToken ?pageSize ())
           (Some Values.ListProfilesResponse.to_json)
           (Some Values.ListProfilesResponse.error_to_json)])
let list_subjects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListRequestNextTokenString"
       and pageSize = flag "page-size" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subjects (Values.ListRequest.make ?nextToken ?pageSize ())
           (Some Values.ListSubjectsResponse.to_json)
           (Some Values.ListSubjectsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_trust_anchors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListRequestNextTokenString"
       and pageSize = flag "page-size" (optional int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_trust_anchors
           (Values.ListRequest.make ?nextToken ?pageSize ())
           (Some Values.ListTrustAnchorsResponse.to_json)
           (Some Values.ListTrustAnchorsResponse.error_to_json)])
let put_attribute_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId = flag "profile-id" (required string) ~doc:"STRING Uuid"
       and certificateField =
         flag "certificate-field" (required json_arg)
           ~doc:"JSON CertificateField"
       and mappingRules =
         flag "mapping-rules" (required json_arg) ~doc:"JSON MappingRules" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_attribute_mapping
           (Values.PutAttributeMappingRequest.make ~profileId
              ~certificateField:(Values.CertificateField.of_json
                                   certificateField)
              ~mappingRules:(Values.MappingRules.of_json mappingRules) ())
           (Some Values.PutAttributeMappingResponse.to_json)
           (Some Values.PutAttributeMappingResponse.error_to_json)])
let put_notification_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trustAnchorId =
         flag "trust-anchor-id" (required string) ~doc:"STRING Uuid"
       and notificationSettings =
         flag "notification-settings" (required json_arg)
           ~doc:"JSON NotificationSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_notification_settings
           (Values.PutNotificationSettingsRequest.make ~trustAnchorId
              ~notificationSettings:(Values.NotificationSettings.of_json
                                       notificationSettings) ())
           (Some Values.PutNotificationSettingsResponse.to_json)
           (Some Values.PutNotificationSettingsResponse.error_to_json)])
let reset_notification_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trustAnchorId =
         flag "trust-anchor-id" (required string) ~doc:"STRING Uuid"
       and notificationSettingKeys =
         flag "notification-setting-keys" (required json_arg)
           ~doc:"JSON NotificationSettingKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_notification_settings
           (Values.ResetNotificationSettingsRequest.make ~trustAnchorId
              ~notificationSettingKeys:(Values.NotificationSettingKeys.of_json
                                          notificationSettingKeys) ())
           (Some Values.ResetNotificationSettingsResponse.to_json)
           (Some Values.ResetNotificationSettingsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_crl =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and crlData =
         flag "crl-data" (optional json_arg)
           ~doc:"JSON UpdateCrlRequestCrlDataBlob"
       and crlId = flag "crl-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_crl
           (Values.UpdateCrlRequest.make ?name
              ?crlData:(Option.map
                          ~f:Values.UpdateCrlRequestCrlDataBlob.of_json
                          crlData) ~crlId ())
           (Some Values.CrlDetailResponse.to_json)
           (Some Values.CrlDetailResponse.error_to_json)])
let update_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and sessionPolicy =
         flag "session-policy" (optional string)
           ~doc:"STRING UpdateProfileRequestSessionPolicyString"
       and roleArns =
         flag "role-arns" (optional json_arg) ~doc:"JSON RoleArnList"
       and managedPolicyArns =
         flag "managed-policy-arns" (optional json_arg)
           ~doc:"JSON ManagedPolicyList"
       and durationSeconds =
         flag "duration-seconds" (optional int)
           ~doc:"INT UpdateProfileRequestDurationSecondsInteger"
       and acceptRoleSessionName =
         flag "accept-role-session-name" (optional bool) ~doc:"BOOL Boolean"
       and profileId = flag "profile-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_profile
           (Values.UpdateProfileRequest.make ?name ?sessionPolicy
              ?roleArns:(Option.map ~f:Values.RoleArnList.of_json roleArns)
              ?managedPolicyArns:(Option.map
                                    ~f:Values.ManagedPolicyList.of_json
                                    managedPolicyArns) ?durationSeconds
              ?acceptRoleSessionName ~profileId ())
           (Some Values.ProfileDetailResponse.to_json)
           (Some Values.ProfileDetailResponse.error_to_json)])
let update_trust_anchor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and source = flag "source" (optional json_arg) ~doc:"JSON Source"
       and trustAnchorId =
         flag "trust-anchor-id" (required string) ~doc:"STRING Uuid" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_trust_anchor
           (Values.UpdateTrustAnchorRequest.make ?name
              ?source:(Option.map ~f:Values.Source.of_json source)
              ~trustAnchorId ())
           (Some Values.TrustAnchorDetailResponse.to_json)
           (Some Values.TrustAnchorDetailResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-profile", create_profile);
    ("create-trust-anchor", create_trust_anchor);
    ("delete-attribute-mapping", delete_attribute_mapping);
    ("delete-crl", delete_crl);
    ("delete-profile", delete_profile);
    ("delete-trust-anchor", delete_trust_anchor);
    ("disable-crl", disable_crl);
    ("disable-profile", disable_profile);
    ("disable-trust-anchor", disable_trust_anchor);
    ("enable-crl", enable_crl);
    ("enable-profile", enable_profile);
    ("enable-trust-anchor", enable_trust_anchor);
    ("get-crl", get_crl);
    ("get-profile", get_profile);
    ("get-subject", get_subject);
    ("get-trust-anchor", get_trust_anchor);
    ("import-crl", import_crl);
    ("list-crls", list_crls);
    ("list-profiles", list_profiles);
    ("list-subjects", list_subjects);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-trust-anchors", list_trust_anchors);
    ("put-attribute-mapping", put_attribute_mapping);
    ("put-notification-settings", put_notification_settings);
    ("reset-notification-settings", reset_notification_settings);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-crl", update_crl);
    ("update-profile", update_profile);
    ("update-trust-anchor", update_trust_anchor)]
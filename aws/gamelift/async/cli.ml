(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_match =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ticketId =
         flag "ticket-id" (required string)
           ~doc:"STRING MatchmakingIdStringModel"
       and playerIds =
         flag "player-ids" (required json_arg) ~doc:"JSON StringList"
       and acceptanceType =
         flag "acceptance-type" (required json_arg)
           ~doc:"JSON AcceptanceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_match
           (Values.AcceptMatchInput.make ~ticketId
              ~playerIds:(Values.StringList.of_json playerIds)
              ~acceptanceType:(Values.AcceptanceType.of_json acceptanceType)
              ()) (Some Values.AcceptMatchOutput.to_json)
           (Some Values.AcceptMatchOutput.error_to_json)])
let claim_game_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameServerId =
         flag "game-server-id" (optional string) ~doc:"STRING GameServerId"
       and gameServerData =
         flag "game-server-data" (optional string)
           ~doc:"STRING GameServerData"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.claim_game_server
           (Values.ClaimGameServerInput.make ?gameServerId ?gameServerData
              ~gameServerGroupName ())
           (Some Values.ClaimGameServerOutput.to_json)
           (Some Values.ClaimGameServerOutput.error_to_json)])
let create_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string)
           ~doc:"STRING NonBlankAndLengthConstraintString"
       and routingStrategy =
         flag "routing-strategy" (required json_arg)
           ~doc:"JSON RoutingStrategy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_alias
           (Values.CreateAliasInput.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~routingStrategy:(Values.RoutingStrategy.of_json
                                  routingStrategy) ())
           (Some Values.CreateAliasOutput.to_json)
           (Some Values.CreateAliasOutput.error_to_json)])
let create_build =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and version =
         flag "version" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and storageLocation =
         flag "storage-location" (optional json_arg) ~doc:"JSON S3Location"
       and operatingSystem =
         flag "operating-system" (optional json_arg)
           ~doc:"JSON OperatingSystem"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_build
           (Values.CreateBuildInput.make ?name ?version
              ?storageLocation:(Option.map ~f:Values.S3Location.of_json
                                  storageLocation)
              ?operatingSystem:(Option.map ~f:Values.OperatingSystem.of_json
                                  operatingSystem)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ())
           (Some Values.CreateBuildOutput.to_json)
           (Some Values.CreateBuildOutput.error_to_json)])
let create_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and buildId =
         flag "build-id" (optional string) ~doc:"STRING BuildIdOrArn"
       and scriptId =
         flag "script-id" (optional string) ~doc:"STRING ScriptIdOrArn"
       and serverLaunchPath =
         flag "server-launch-path" (optional string)
           ~doc:"STRING LaunchPathStringModel"
       and serverLaunchParameters =
         flag "server-launch-parameters" (optional string)
           ~doc:"STRING LaunchParametersStringModel"
       and logPaths =
         flag "log-paths" (optional json_arg) ~doc:"JSON StringList"
       and eC2InboundPermissions =
         flag "e-c2-inbound-permissions" (optional json_arg)
           ~doc:"JSON IpPermissionsList"
       and newGameSessionProtectionPolicy =
         flag "new-game-session-protection-policy" (optional json_arg)
           ~doc:"JSON ProtectionPolicy"
       and runtimeConfiguration =
         flag "runtime-configuration" (optional json_arg)
           ~doc:"JSON RuntimeConfiguration"
       and resourceCreationLimitPolicy =
         flag "resource-creation-limit-policy" (optional json_arg)
           ~doc:"JSON ResourceCreationLimitPolicy"
       and metricGroups =
         flag "metric-groups" (optional json_arg) ~doc:"JSON MetricGroupList"
       and peerVpcAwsAccountId =
         flag "peer-vpc-aws-account-id" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and peerVpcId =
         flag "peer-vpc-id" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and fleetType =
         flag "fleet-type" (optional json_arg) ~doc:"JSON FleetType"
       and instanceRoleArn =
         flag "instance-role-arn" (optional string)
           ~doc:"STRING NonEmptyString"
       and certificateConfiguration =
         flag "certificate-configuration" (optional json_arg)
           ~doc:"JSON CertificateConfiguration"
       and locations =
         flag "locations" (optional json_arg)
           ~doc:"JSON LocationConfigurationList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING NonZeroAndMaxString"
       and eC2InstanceType =
         flag "e-c2-instance-type" (required json_arg)
           ~doc:"JSON EC2InstanceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet
           (Values.CreateFleetInput.make ?description ?buildId ?scriptId
              ?serverLaunchPath ?serverLaunchParameters
              ?logPaths:(Option.map ~f:Values.StringList.of_json logPaths)
              ?eC2InboundPermissions:(Option.map
                                        ~f:Values.IpPermissionsList.of_json
                                        eC2InboundPermissions)
              ?newGameSessionProtectionPolicy:(Option.map
                                                 ~f:Values.ProtectionPolicy.of_json
                                                 newGameSessionProtectionPolicy)
              ?runtimeConfiguration:(Option.map
                                       ~f:Values.RuntimeConfiguration.of_json
                                       runtimeConfiguration)
              ?resourceCreationLimitPolicy:(Option.map
                                              ~f:Values.ResourceCreationLimitPolicy.of_json
                                              resourceCreationLimitPolicy)
              ?metricGroups:(Option.map ~f:Values.MetricGroupList.of_json
                               metricGroups) ?peerVpcAwsAccountId ?peerVpcId
              ?fleetType:(Option.map ~f:Values.FleetType.of_json fleetType)
              ?instanceRoleArn
              ?certificateConfiguration:(Option.map
                                           ~f:Values.CertificateConfiguration.of_json
                                           certificateConfiguration)
              ?locations:(Option.map
                            ~f:Values.LocationConfigurationList.of_json
                            locations)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~eC2InstanceType:(Values.EC2InstanceType.of_json
                                  eC2InstanceType) ())
           (Some Values.CreateFleetOutput.to_json)
           (Some Values.CreateFleetOutput.error_to_json)])
let create_fleet_locations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and locations =
         flag "locations" (required json_arg)
           ~doc:"JSON LocationConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet_locations
           (Values.CreateFleetLocationsInput.make ~fleetId
              ~locations:(Values.LocationConfigurationList.of_json locations)
              ()) (Some Values.CreateFleetLocationsOutput.to_json)
           (Some Values.CreateFleetLocationsOutput.error_to_json)])
let create_game_server_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingPolicy =
         flag "auto-scaling-policy" (optional json_arg)
           ~doc:"JSON GameServerGroupAutoScalingPolicy"
       and balancingStrategy =
         flag "balancing-strategy" (optional json_arg)
           ~doc:"JSON BalancingStrategy"
       and gameServerProtectionPolicy =
         flag "game-server-protection-policy" (optional json_arg)
           ~doc:"JSON GameServerProtectionPolicy"
       and vpcSubnets =
         flag "vpc-subnets" (optional json_arg) ~doc:"JSON VpcSubnets"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupName"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IamRoleArn"
       and minSize = flag "min-size" (required int) ~doc:"INT WholeNumber"
       and maxSize =
         flag "max-size" (required int) ~doc:"INT PositiveInteger"
       and launchTemplate =
         flag "launch-template" (required json_arg)
           ~doc:"JSON LaunchTemplateSpecification"
       and instanceDefinitions =
         flag "instance-definitions" (required json_arg)
           ~doc:"JSON InstanceDefinitions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_game_server_group
           (Values.CreateGameServerGroupInput.make
              ?autoScalingPolicy:(Option.map
                                    ~f:Values.GameServerGroupAutoScalingPolicy.of_json
                                    autoScalingPolicy)
              ?balancingStrategy:(Option.map
                                    ~f:Values.BalancingStrategy.of_json
                                    balancingStrategy)
              ?gameServerProtectionPolicy:(Option.map
                                             ~f:Values.GameServerProtectionPolicy.of_json
                                             gameServerProtectionPolicy)
              ?vpcSubnets:(Option.map ~f:Values.VpcSubnets.of_json vpcSubnets)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~gameServerGroupName ~roleArn ~minSize ~maxSize
              ~launchTemplate:(Values.LaunchTemplateSpecification.of_json
                                 launchTemplate)
              ~instanceDefinitions:(Values.InstanceDefinitions.of_json
                                      instanceDefinitions) ())
           (Some Values.CreateGameServerGroupOutput.to_json)
           (Some Values.CreateGameServerGroupOutput.error_to_json)])
let create_game_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (optional string) ~doc:"STRING FleetIdOrArn"
       and aliasId =
         flag "alias-id" (optional string) ~doc:"STRING AliasIdOrArn"
       and name =
         flag "name" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and gameProperties =
         flag "game-properties" (optional json_arg)
           ~doc:"JSON GamePropertyList"
       and creatorId =
         flag "creator-id" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and gameSessionId =
         flag "game-session-id" (optional string) ~doc:"STRING IdStringModel"
       and idempotencyToken =
         flag "idempotency-token" (optional string)
           ~doc:"STRING IdStringModel"
       and gameSessionData =
         flag "game-session-data" (optional string)
           ~doc:"STRING LargeGameSessionData"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and maximumPlayerSessionCount =
         flag "maximum-player-session-count" (required int)
           ~doc:"INT WholeNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_game_session
           (Values.CreateGameSessionInput.make ?fleetId ?aliasId ?name
              ?gameProperties:(Option.map ~f:Values.GamePropertyList.of_json
                                 gameProperties) ?creatorId ?gameSessionId
              ?idempotencyToken ?gameSessionData ?location
              ~maximumPlayerSessionCount ())
           (Some Values.CreateGameSessionOutput.to_json)
           (Some Values.CreateGameSessionOutput.error_to_json)])
let create_game_session_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timeoutInSeconds =
         flag "timeout-in-seconds" (optional int) ~doc:"INT WholeNumber"
       and playerLatencyPolicies =
         flag "player-latency-policies" (optional json_arg)
           ~doc:"JSON PlayerLatencyPolicyList"
       and destinations =
         flag "destinations" (optional json_arg)
           ~doc:"JSON GameSessionQueueDestinationList"
       and filterConfiguration =
         flag "filter-configuration" (optional json_arg)
           ~doc:"JSON FilterConfiguration"
       and priorityConfiguration =
         flag "priority-configuration" (optional json_arg)
           ~doc:"JSON PriorityConfiguration"
       and customEventData =
         flag "custom-event-data" (optional string)
           ~doc:"STRING QueueCustomEventData"
       and notificationTarget =
         flag "notification-target" (optional string)
           ~doc:"STRING QueueSnsArnStringModel"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING GameSessionQueueName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_game_session_queue
           (Values.CreateGameSessionQueueInput.make ?timeoutInSeconds
              ?playerLatencyPolicies:(Option.map
                                        ~f:Values.PlayerLatencyPolicyList.of_json
                                        playerLatencyPolicies)
              ?destinations:(Option.map
                               ~f:Values.GameSessionQueueDestinationList.of_json
                               destinations)
              ?filterConfiguration:(Option.map
                                      ~f:Values.FilterConfiguration.of_json
                                      filterConfiguration)
              ?priorityConfiguration:(Option.map
                                        ~f:Values.PriorityConfiguration.of_json
                                        priorityConfiguration)
              ?customEventData ?notificationTarget
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateGameSessionQueueOutput.to_json)
           (Some Values.CreateGameSessionQueueOutput.error_to_json)])
let create_matchmaking_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and gameSessionQueueArns =
         flag "game-session-queue-arns" (optional json_arg)
           ~doc:"JSON QueueArnsList"
       and acceptanceTimeoutSeconds =
         flag "acceptance-timeout-seconds" (optional int)
           ~doc:"INT MatchmakingAcceptanceTimeoutInteger"
       and notificationTarget =
         flag "notification-target" (optional string)
           ~doc:"STRING SnsArnStringModel"
       and additionalPlayerCount =
         flag "additional-player-count" (optional int) ~doc:"INT WholeNumber"
       and customEventData =
         flag "custom-event-data" (optional string)
           ~doc:"STRING CustomEventData"
       and gameProperties =
         flag "game-properties" (optional json_arg)
           ~doc:"JSON GamePropertyList"
       and gameSessionData =
         flag "game-session-data" (optional string)
           ~doc:"STRING GameSessionData"
       and backfillMode =
         flag "backfill-mode" (optional json_arg) ~doc:"JSON BackfillMode"
       and flexMatchMode =
         flag "flex-match-mode" (optional json_arg) ~doc:"JSON FlexMatchMode"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING MatchmakingIdStringModel"
       and requestTimeoutSeconds =
         flag "request-timeout-seconds" (required int)
           ~doc:"INT MatchmakingRequestTimeoutInteger"
       and acceptanceRequired =
         flag "acceptance-required" (required bool) ~doc:"BOOL BooleanModel"
       and ruleSetName =
         flag "rule-set-name" (required string)
           ~doc:"STRING MatchmakingRuleSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_matchmaking_configuration
           (Values.CreateMatchmakingConfigurationInput.make ?description
              ?gameSessionQueueArns:(Option.map
                                       ~f:Values.QueueArnsList.of_json
                                       gameSessionQueueArns)
              ?acceptanceTimeoutSeconds ?notificationTarget
              ?additionalPlayerCount ?customEventData
              ?gameProperties:(Option.map ~f:Values.GamePropertyList.of_json
                                 gameProperties) ?gameSessionData
              ?backfillMode:(Option.map ~f:Values.BackfillMode.of_json
                               backfillMode)
              ?flexMatchMode:(Option.map ~f:Values.FlexMatchMode.of_json
                                flexMatchMode)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~requestTimeoutSeconds ~acceptanceRequired ~ruleSetName ())
           (Some Values.CreateMatchmakingConfigurationOutput.to_json)
           (Some Values.CreateMatchmakingConfigurationOutput.error_to_json)])
let create_matchmaking_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING MatchmakingIdStringModel"
       and ruleSetBody =
         flag "rule-set-body" (required string) ~doc:"STRING RuleSetBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_matchmaking_rule_set
           (Values.CreateMatchmakingRuleSetInput.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~ruleSetBody ())
           (Some Values.CreateMatchmakingRuleSetOutput.to_json)
           (Some Values.CreateMatchmakingRuleSetOutput.error_to_json)])
let create_player_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and playerData =
         flag "player-data" (optional string) ~doc:"STRING PlayerData"
       and gameSessionId =
         flag "game-session-id" (required string)
           ~doc:"STRING ArnStringModel"
       and playerId =
         flag "player-id" (required string) ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_player_session
           (Values.CreatePlayerSessionInput.make ?playerData ~gameSessionId
              ~playerId ()) (Some Values.CreatePlayerSessionOutput.to_json)
           (Some Values.CreatePlayerSessionOutput.error_to_json)])
let create_player_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and playerDataMap =
         flag "player-data-map" (optional json_arg) ~doc:"JSON PlayerDataMap"
       and gameSessionId =
         flag "game-session-id" (required string)
           ~doc:"STRING ArnStringModel"
       and playerIds =
         flag "player-ids" (required json_arg) ~doc:"JSON PlayerIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_player_sessions
           (Values.CreatePlayerSessionsInput.make
              ?playerDataMap:(Option.map ~f:Values.PlayerDataMap.of_json
                                playerDataMap) ~gameSessionId
              ~playerIds:(Values.PlayerIdList.of_json playerIds) ())
           (Some Values.CreatePlayerSessionsOutput.to_json)
           (Some Values.CreatePlayerSessionsOutput.error_to_json)])
let create_script =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and version =
         flag "version" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and storageLocation =
         flag "storage-location" (optional json_arg) ~doc:"JSON S3Location"
       and zipFile = flag "zip-file" (optional json_arg) ~doc:"JSON ZipBlob"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_script
           (Values.CreateScriptInput.make ?name ?version
              ?storageLocation:(Option.map ~f:Values.S3Location.of_json
                                  storageLocation)
              ?zipFile:(Option.map ~f:Values.ZipBlob.of_json zipFile)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ())
           (Some Values.CreateScriptOutput.to_json)
           (Some Values.CreateScriptOutput.error_to_json)])
let create_vpc_peering_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameLiftAwsAccountId =
         flag "game-lift-aws-account-id" (required string)
           ~doc:"STRING NonZeroAndMaxString"
       and peerVpcId =
         flag "peer-vpc-id" (required string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_peering_authorization
           (Values.CreateVpcPeeringAuthorizationInput.make
              ~gameLiftAwsAccountId ~peerVpcId ())
           (Some Values.CreateVpcPeeringAuthorizationOutput.to_json)
           (Some Values.CreateVpcPeeringAuthorizationOutput.error_to_json)])
let create_vpc_peering_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and peerVpcAwsAccountId =
         flag "peer-vpc-aws-account-id" (required string)
           ~doc:"STRING NonZeroAndMaxString"
       and peerVpcId =
         flag "peer-vpc-id" (required string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_peering_connection
           (Values.CreateVpcPeeringConnectionInput.make ~fleetId
              ~peerVpcAwsAccountId ~peerVpcId ())
           (Some Values.CreateVpcPeeringConnectionOutput.to_json)
           (Some Values.CreateVpcPeeringConnectionOutput.error_to_json)])
let delete_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasId =
         flag "alias-id" (required string) ~doc:"STRING AliasIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alias (Values.DeleteAliasInput.make ~aliasId ()) None
           None])
let delete_build =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and buildId =
         flag "build-id" (required string) ~doc:"STRING BuildIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_build (Values.DeleteBuildInput.make ~buildId ()) None
           None])
let delete_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleet (Values.DeleteFleetInput.make ~fleetId ()) None
           None])
let delete_fleet_locations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and locations =
         flag "locations" (required json_arg) ~doc:"JSON LocationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleet_locations
           (Values.DeleteFleetLocationsInput.make ~fleetId
              ~locations:(Values.LocationList.of_json locations) ())
           (Some Values.DeleteFleetLocationsOutput.to_json)
           (Some Values.DeleteFleetLocationsOutput.error_to_json)])
let delete_game_server_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteOption =
         flag "delete-option" (optional json_arg)
           ~doc:"JSON GameServerGroupDeleteOption"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_game_server_group
           (Values.DeleteGameServerGroupInput.make
              ?deleteOption:(Option.map
                               ~f:Values.GameServerGroupDeleteOption.of_json
                               deleteOption) ~gameServerGroupName ())
           (Some Values.DeleteGameServerGroupOutput.to_json)
           (Some Values.DeleteGameServerGroupOutput.error_to_json)])
let delete_game_session_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string)
           ~doc:"STRING GameSessionQueueNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_game_session_queue
           (Values.DeleteGameSessionQueueInput.make ~name ())
           (Some Values.DeleteGameSessionQueueOutput.to_json)
           (Some Values.DeleteGameSessionQueueOutput.error_to_json)])
let delete_matchmaking_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string)
           ~doc:"STRING MatchmakingConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_matchmaking_configuration
           (Values.DeleteMatchmakingConfigurationInput.make ~name ())
           (Some Values.DeleteMatchmakingConfigurationOutput.to_json)
           (Some Values.DeleteMatchmakingConfigurationOutput.error_to_json)])
let delete_matchmaking_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING MatchmakingRuleSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_matchmaking_rule_set
           (Values.DeleteMatchmakingRuleSetInput.make ~name ())
           (Some Values.DeleteMatchmakingRuleSetOutput.to_json)
           (Some Values.DeleteMatchmakingRuleSetOutput.error_to_json)])
let delete_scaling_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING NonZeroAndMaxString"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scaling_policy
           (Values.DeleteScalingPolicyInput.make ~name ~fleetId ()) None None])
let delete_script =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scriptId =
         flag "script-id" (required string) ~doc:"STRING ScriptIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_script (Values.DeleteScriptInput.make ~scriptId ()) None
           None])
let delete_vpc_peering_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameLiftAwsAccountId =
         flag "game-lift-aws-account-id" (required string)
           ~doc:"STRING NonZeroAndMaxString"
       and peerVpcId =
         flag "peer-vpc-id" (required string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_peering_authorization
           (Values.DeleteVpcPeeringAuthorizationInput.make
              ~gameLiftAwsAccountId ~peerVpcId ())
           (Some Values.DeleteVpcPeeringAuthorizationOutput.to_json)
           (Some Values.DeleteVpcPeeringAuthorizationOutput.error_to_json)])
let delete_vpc_peering_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId = flag "fleet-id" (required string) ~doc:"STRING FleetId"
       and vpcPeeringConnectionId =
         flag "vpc-peering-connection-id" (required string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_peering_connection
           (Values.DeleteVpcPeeringConnectionInput.make ~fleetId
              ~vpcPeeringConnectionId ())
           (Some Values.DeleteVpcPeeringConnectionOutput.to_json)
           (Some Values.DeleteVpcPeeringConnectionOutput.error_to_json)])
let deregister_game_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn"
       and gameServerId =
         flag "game-server-id" (required string) ~doc:"STRING GameServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_game_server
           (Values.DeregisterGameServerInput.make ~gameServerGroupName
              ~gameServerId ()) None None])
let describe_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasId =
         flag "alias-id" (required string) ~doc:"STRING AliasIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_alias (Values.DescribeAliasInput.make ~aliasId ())
           (Some Values.DescribeAliasOutput.to_json)
           (Some Values.DescribeAliasOutput.error_to_json)])
let describe_build =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and buildId =
         flag "build-id" (required string) ~doc:"STRING BuildIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_build (Values.DescribeBuildInput.make ~buildId ())
           (Some Values.DescribeBuildOutput.to_json)
           (Some Values.DescribeBuildOutput.error_to_json)])
let describe_e_c2_instance_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eC2InstanceType =
         flag "e-c2-instance-type" (optional json_arg)
           ~doc:"JSON EC2InstanceType"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_e_c2_instance_limits
           (Values.DescribeEC2InstanceLimitsInput.make
              ?eC2InstanceType:(Option.map ~f:Values.EC2InstanceType.of_json
                                  eC2InstanceType) ?location ())
           (Some Values.DescribeEC2InstanceLimitsOutput.to_json)
           (Some Values.DescribeEC2InstanceLimitsOutput.error_to_json)])
let describe_fleet_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetIds =
         flag "fleet-ids" (optional json_arg) ~doc:"JSON FleetIdOrArnList"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_attributes
           (Values.DescribeFleetAttributesInput.make
              ?fleetIds:(Option.map ~f:Values.FleetIdOrArnList.of_json
                           fleetIds) ?limit ?nextToken ())
           (Some Values.DescribeFleetAttributesOutput.to_json)
           (Some Values.DescribeFleetAttributesOutput.error_to_json)])
let describe_fleet_capacity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetIds =
         flag "fleet-ids" (optional json_arg) ~doc:"JSON FleetIdOrArnList"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_capacity
           (Values.DescribeFleetCapacityInput.make
              ?fleetIds:(Option.map ~f:Values.FleetIdOrArnList.of_json
                           fleetIds) ?limit ?nextToken ())
           (Some Values.DescribeFleetCapacityOutput.to_json)
           (Some Values.DescribeFleetCapacityOutput.error_to_json)])
let describe_fleet_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_events
           (Values.DescribeFleetEventsInput.make
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?limit ?nextToken ~fleetId ())
           (Some Values.DescribeFleetEventsOutput.to_json)
           (Some Values.DescribeFleetEventsOutput.error_to_json)])
let describe_fleet_location_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locations =
         flag "locations" (optional json_arg) ~doc:"JSON LocationList"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_location_attributes
           (Values.DescribeFleetLocationAttributesInput.make
              ?locations:(Option.map ~f:Values.LocationList.of_json locations)
              ?limit ?nextToken ~fleetId ())
           (Some Values.DescribeFleetLocationAttributesOutput.to_json)
           (Some Values.DescribeFleetLocationAttributesOutput.error_to_json)])
let describe_fleet_location_capacity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and location =
         flag "location" (required string) ~doc:"STRING LocationStringModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_location_capacity
           (Values.DescribeFleetLocationCapacityInput.make ~fleetId ~location
              ()) (Some Values.DescribeFleetLocationCapacityOutput.to_json)
           (Some Values.DescribeFleetLocationCapacityOutput.error_to_json)])
let describe_fleet_location_utilization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and location =
         flag "location" (required string) ~doc:"STRING LocationStringModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_location_utilization
           (Values.DescribeFleetLocationUtilizationInput.make ~fleetId
              ~location ())
           (Some Values.DescribeFleetLocationUtilizationOutput.to_json)
           (Some Values.DescribeFleetLocationUtilizationOutput.error_to_json)])
let describe_fleet_port_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_port_settings
           (Values.DescribeFleetPortSettingsInput.make ?location ~fleetId ())
           (Some Values.DescribeFleetPortSettingsOutput.to_json)
           (Some Values.DescribeFleetPortSettingsOutput.error_to_json)])
let describe_fleet_utilization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetIds =
         flag "fleet-ids" (optional json_arg) ~doc:"JSON FleetIdOrArnList"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_utilization
           (Values.DescribeFleetUtilizationInput.make
              ?fleetIds:(Option.map ~f:Values.FleetIdOrArnList.of_json
                           fleetIds) ?limit ?nextToken ())
           (Some Values.DescribeFleetUtilizationOutput.to_json)
           (Some Values.DescribeFleetUtilizationOutput.error_to_json)])
let describe_game_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn"
       and gameServerId =
         flag "game-server-id" (required string) ~doc:"STRING GameServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_game_server
           (Values.DescribeGameServerInput.make ~gameServerGroupName
              ~gameServerId ())
           (Some Values.DescribeGameServerOutput.to_json)
           (Some Values.DescribeGameServerOutput.error_to_json)])
let describe_game_server_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_game_server_group
           (Values.DescribeGameServerGroupInput.make ~gameServerGroupName ())
           (Some Values.DescribeGameServerGroupOutput.to_json)
           (Some Values.DescribeGameServerGroupOutput.error_to_json)])
let describe_game_server_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON GameServerInstanceIds"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_game_server_instances
           (Values.DescribeGameServerInstancesInput.make
              ?instanceIds:(Option.map
                              ~f:Values.GameServerInstanceIds.of_json
                              instanceIds) ?limit ?nextToken
              ~gameServerGroupName ())
           (Some Values.DescribeGameServerInstancesOutput.to_json)
           (Some Values.DescribeGameServerInstancesOutput.error_to_json)])
let describe_game_session_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (optional string) ~doc:"STRING FleetIdOrArn"
       and gameSessionId =
         flag "game-session-id" (optional string)
           ~doc:"STRING ArnStringModel"
       and aliasId =
         flag "alias-id" (optional string) ~doc:"STRING AliasIdOrArn"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and statusFilter =
         flag "status-filter" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_game_session_details
           (Values.DescribeGameSessionDetailsInput.make ?fleetId
              ?gameSessionId ?aliasId ?location ?statusFilter ?limit
              ?nextToken ())
           (Some Values.DescribeGameSessionDetailsOutput.to_json)
           (Some Values.DescribeGameSessionDetailsOutput.error_to_json)])
let describe_game_session_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and placementId =
         flag "placement-id" (required string) ~doc:"STRING IdStringModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_game_session_placement
           (Values.DescribeGameSessionPlacementInput.make ~placementId ())
           (Some Values.DescribeGameSessionPlacementOutput.to_json)
           (Some Values.DescribeGameSessionPlacementOutput.error_to_json)])
let describe_game_session_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names =
         flag "names" (optional json_arg)
           ~doc:"JSON GameSessionQueueNameOrArnList"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_game_session_queues
           (Values.DescribeGameSessionQueuesInput.make
              ?names:(Option.map
                        ~f:Values.GameSessionQueueNameOrArnList.of_json names)
              ?limit ?nextToken ())
           (Some Values.DescribeGameSessionQueuesOutput.to_json)
           (Some Values.DescribeGameSessionQueuesOutput.error_to_json)])
let describe_game_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (optional string) ~doc:"STRING FleetIdOrArn"
       and gameSessionId =
         flag "game-session-id" (optional string)
           ~doc:"STRING ArnStringModel"
       and aliasId =
         flag "alias-id" (optional string) ~doc:"STRING AliasIdOrArn"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and statusFilter =
         flag "status-filter" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_game_sessions
           (Values.DescribeGameSessionsInput.make ?fleetId ?gameSessionId
              ?aliasId ?location ?statusFilter ?limit ?nextToken ())
           (Some Values.DescribeGameSessionsOutput.to_json)
           (Some Values.DescribeGameSessionsOutput.error_to_json)])
let describe_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING InstanceId"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instances
           (Values.DescribeInstancesInput.make ?instanceId ?limit ?nextToken
              ?location ~fleetId ())
           (Some Values.DescribeInstancesOutput.to_json)
           (Some Values.DescribeInstancesOutput.error_to_json)])
let describe_matchmaking =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ticketIds =
         flag "ticket-ids" (required json_arg) ~doc:"JSON MatchmakingIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_matchmaking
           (Values.DescribeMatchmakingInput.make
              ~ticketIds:(Values.MatchmakingIdList.of_json ticketIds) ())
           (Some Values.DescribeMatchmakingOutput.to_json)
           (Some Values.DescribeMatchmakingOutput.error_to_json)])
let describe_matchmaking_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names =
         flag "names" (optional json_arg)
           ~doc:"JSON MatchmakingConfigurationNameList"
       and ruleSetName =
         flag "rule-set-name" (optional string)
           ~doc:"STRING MatchmakingRuleSetName"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_matchmaking_configurations
           (Values.DescribeMatchmakingConfigurationsInput.make
              ?names:(Option.map
                        ~f:Values.MatchmakingConfigurationNameList.of_json
                        names) ?ruleSetName ?limit ?nextToken ())
           (Some Values.DescribeMatchmakingConfigurationsOutput.to_json)
           (Some Values.DescribeMatchmakingConfigurationsOutput.error_to_json)])
let describe_matchmaking_rule_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names =
         flag "names" (optional json_arg)
           ~doc:"JSON MatchmakingRuleSetNameList"
       and limit = flag "limit" (optional int) ~doc:"INT RuleSetLimit"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_matchmaking_rule_sets
           (Values.DescribeMatchmakingRuleSetsInput.make
              ?names:(Option.map ~f:Values.MatchmakingRuleSetNameList.of_json
                        names) ?limit ?nextToken ())
           (Some Values.DescribeMatchmakingRuleSetsOutput.to_json)
           (Some Values.DescribeMatchmakingRuleSetsOutput.error_to_json)])
let describe_player_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameSessionId =
         flag "game-session-id" (optional string)
           ~doc:"STRING ArnStringModel"
       and playerId =
         flag "player-id" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and playerSessionId =
         flag "player-session-id" (optional string)
           ~doc:"STRING PlayerSessionId"
       and playerSessionStatusFilter =
         flag "player-session-status-filter" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_player_sessions
           (Values.DescribePlayerSessionsInput.make ?gameSessionId ?playerId
              ?playerSessionId ?playerSessionStatusFilter ?limit ?nextToken
              ()) (Some Values.DescribePlayerSessionsOutput.to_json)
           (Some Values.DescribePlayerSessionsOutput.error_to_json)])
let describe_runtime_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_runtime_configuration
           (Values.DescribeRuntimeConfigurationInput.make ~fleetId ())
           (Some Values.DescribeRuntimeConfigurationOutput.to_json)
           (Some Values.DescribeRuntimeConfigurationOutput.error_to_json)])
let describe_scaling_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statusFilter =
         flag "status-filter" (optional json_arg)
           ~doc:"JSON ScalingStatusType"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scaling_policies
           (Values.DescribeScalingPoliciesInput.make
              ?statusFilter:(Option.map ~f:Values.ScalingStatusType.of_json
                               statusFilter) ?limit ?nextToken ?location
              ~fleetId ())
           (Some Values.DescribeScalingPoliciesOutput.to_json)
           (Some Values.DescribeScalingPoliciesOutput.error_to_json)])
let describe_script =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scriptId =
         flag "script-id" (required string) ~doc:"STRING ScriptIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_script (Values.DescribeScriptInput.make ~scriptId ())
           (Some Values.DescribeScriptOutput.to_json)
           (Some Values.DescribeScriptOutput.error_to_json)])
let describe_vpc_peering_authorizations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_vpc_peering_authorizations
           (Values.DescribeVpcPeeringAuthorizationsInput.make ())
           (Some Values.DescribeVpcPeeringAuthorizationsOutput.to_json)
           (Some Values.DescribeVpcPeeringAuthorizationsOutput.error_to_json)])
let describe_vpc_peering_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId = flag "fleet-id" (optional string) ~doc:"STRING FleetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_vpc_peering_connections
           (Values.DescribeVpcPeeringConnectionsInput.make ?fleetId ())
           (Some Values.DescribeVpcPeeringConnectionsOutput.to_json)
           (Some Values.DescribeVpcPeeringConnectionsOutput.error_to_json)])
let get_game_session_log_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameSessionId =
         flag "game-session-id" (required string)
           ~doc:"STRING ArnStringModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_game_session_log_url
           (Values.GetGameSessionLogUrlInput.make ~gameSessionId ())
           (Some Values.GetGameSessionLogUrlOutput.to_json)
           (Some Values.GetGameSessionLogUrlOutput.error_to_json)])
let get_instance_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_access
           (Values.GetInstanceAccessInput.make ~fleetId ~instanceId ())
           (Some Values.GetInstanceAccessOutput.to_json)
           (Some Values.GetInstanceAccessOutput.error_to_json)])
let list_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingStrategyType =
         flag "routing-strategy-type" (optional json_arg)
           ~doc:"JSON RoutingStrategyType"
       and name = flag "name" (optional string) ~doc:"STRING NonEmptyString"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_aliases
           (Values.ListAliasesInput.make
              ?routingStrategyType:(Option.map
                                      ~f:Values.RoutingStrategyType.of_json
                                      routingStrategyType) ?name ?limit
              ?nextToken ()) (Some Values.ListAliasesOutput.to_json)
           (Some Values.ListAliasesOutput.error_to_json)])
let list_builds =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status = flag "status" (optional json_arg) ~doc:"JSON BuildStatus"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_builds
           (Values.ListBuildsInput.make
              ?status:(Option.map ~f:Values.BuildStatus.of_json status)
              ?limit ?nextToken ()) (Some Values.ListBuildsOutput.to_json)
           (Some Values.ListBuildsOutput.error_to_json)])
let list_fleets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and buildId =
         flag "build-id" (optional string) ~doc:"STRING BuildIdOrArn"
       and scriptId =
         flag "script-id" (optional string) ~doc:"STRING ScriptIdOrArn"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_fleets
           (Values.ListFleetsInput.make ?buildId ?scriptId ?limit ?nextToken
              ()) (Some Values.ListFleetsOutput.to_json)
           (Some Values.ListFleetsOutput.error_to_json)])
let list_game_server_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_game_server_groups
           (Values.ListGameServerGroupsInput.make ?limit ?nextToken ())
           (Some Values.ListGameServerGroupsOutput.to_json)
           (Some Values.ListGameServerGroupsOutput.error_to_json)])
let list_game_servers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_game_servers
           (Values.ListGameServersInput.make
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?limit ?nextToken ~gameServerGroupName ())
           (Some Values.ListGameServersOutput.to_json)
           (Some Values.ListGameServersOutput.error_to_json)])
let list_scripts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NonEmptyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_scripts
           (Values.ListScriptsInput.make ?limit ?nextToken ())
           (Some Values.ListScriptsOutput.to_json)
           (Some Values.ListScriptsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_scaling_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scalingAdjustment =
         flag "scaling-adjustment" (optional int) ~doc:"INT Integer"
       and scalingAdjustmentType =
         flag "scaling-adjustment-type" (optional json_arg)
           ~doc:"JSON ScalingAdjustmentType"
       and threshold = flag "threshold" (optional float) ~doc:"FLOAT Double"
       and comparisonOperator =
         flag "comparison-operator" (optional json_arg)
           ~doc:"JSON ComparisonOperatorType"
       and evaluationPeriods =
         flag "evaluation-periods" (optional int) ~doc:"INT PositiveInteger"
       and policyType =
         flag "policy-type" (optional json_arg) ~doc:"JSON PolicyType"
       and targetConfiguration =
         flag "target-configuration" (optional json_arg)
           ~doc:"JSON TargetConfiguration"
       and name =
         flag "name" (required string) ~doc:"STRING NonZeroAndMaxString"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and metricName =
         flag "metric-name" (required json_arg) ~doc:"JSON MetricName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_scaling_policy
           (Values.PutScalingPolicyInput.make ?scalingAdjustment
              ?scalingAdjustmentType:(Option.map
                                        ~f:Values.ScalingAdjustmentType.of_json
                                        scalingAdjustmentType) ?threshold
              ?comparisonOperator:(Option.map
                                     ~f:Values.ComparisonOperatorType.of_json
                                     comparisonOperator) ?evaluationPeriods
              ?policyType:(Option.map ~f:Values.PolicyType.of_json policyType)
              ?targetConfiguration:(Option.map
                                      ~f:Values.TargetConfiguration.of_json
                                      targetConfiguration) ~name ~fleetId
              ~metricName:(Values.MetricName.of_json metricName) ())
           (Some Values.PutScalingPolicyOutput.to_json)
           (Some Values.PutScalingPolicyOutput.error_to_json)])
let register_game_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionInfo =
         flag "connection-info" (optional string)
           ~doc:"STRING GameServerConnectionInfo"
       and gameServerData =
         flag "game-server-data" (optional string)
           ~doc:"STRING GameServerData"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn"
       and gameServerId =
         flag "game-server-id" (required string) ~doc:"STRING GameServerId"
       and instanceId =
         flag "instance-id" (required string)
           ~doc:"STRING GameServerInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_game_server
           (Values.RegisterGameServerInput.make ?connectionInfo
              ?gameServerData ~gameServerGroupName ~gameServerId ~instanceId
              ()) (Some Values.RegisterGameServerOutput.to_json)
           (Some Values.RegisterGameServerOutput.error_to_json)])
let request_upload_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and buildId =
         flag "build-id" (required string) ~doc:"STRING BuildIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.request_upload_credentials
           (Values.RequestUploadCredentialsInput.make ~buildId ())
           (Some Values.RequestUploadCredentialsOutput.to_json)
           (Some Values.RequestUploadCredentialsOutput.error_to_json)])
let resolve_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aliasId =
         flag "alias-id" (required string) ~doc:"STRING AliasIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resolve_alias (Values.ResolveAliasInput.make ~aliasId ())
           (Some Values.ResolveAliasOutput.to_json)
           (Some Values.ResolveAliasOutput.error_to_json)])
let resume_game_server_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn"
       and resumeActions =
         flag "resume-actions" (required json_arg)
           ~doc:"JSON GameServerGroupActions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_game_server_group
           (Values.ResumeGameServerGroupInput.make ~gameServerGroupName
              ~resumeActions:(Values.GameServerGroupActions.of_json
                                resumeActions) ())
           (Some Values.ResumeGameServerGroupOutput.to_json)
           (Some Values.ResumeGameServerGroupOutput.error_to_json)])
let search_game_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (optional string) ~doc:"STRING FleetIdOrArn"
       and aliasId =
         flag "alias-id" (optional string) ~doc:"STRING AliasIdOrArn"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and filterExpression =
         flag "filter-expression" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and sortExpression =
         flag "sort-expression" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonZeroAndMaxString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_game_sessions
           (Values.SearchGameSessionsInput.make ?fleetId ?aliasId ?location
              ?filterExpression ?sortExpression ?limit ?nextToken ())
           (Some Values.SearchGameSessionsOutput.to_json)
           (Some Values.SearchGameSessionsOutput.error_to_json)])
let start_fleet_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON FleetActionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_fleet_actions
           (Values.StartFleetActionsInput.make ?location ~fleetId
              ~actions:(Values.FleetActionList.of_json actions) ())
           (Some Values.StartFleetActionsOutput.to_json)
           (Some Values.StartFleetActionsOutput.error_to_json)])
let start_game_session_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameProperties =
         flag "game-properties" (optional json_arg)
           ~doc:"JSON GamePropertyList"
       and gameSessionName =
         flag "game-session-name" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and playerLatencies =
         flag "player-latencies" (optional json_arg)
           ~doc:"JSON PlayerLatencyList"
       and desiredPlayerSessions =
         flag "desired-player-sessions" (optional json_arg)
           ~doc:"JSON DesiredPlayerSessionList"
       and gameSessionData =
         flag "game-session-data" (optional string)
           ~doc:"STRING LargeGameSessionData"
       and placementId =
         flag "placement-id" (required string) ~doc:"STRING IdStringModel"
       and gameSessionQueueName =
         flag "game-session-queue-name" (required string)
           ~doc:"STRING GameSessionQueueNameOrArn"
       and maximumPlayerSessionCount =
         flag "maximum-player-session-count" (required int)
           ~doc:"INT WholeNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_game_session_placement
           (Values.StartGameSessionPlacementInput.make
              ?gameProperties:(Option.map ~f:Values.GamePropertyList.of_json
                                 gameProperties) ?gameSessionName
              ?playerLatencies:(Option.map
                                  ~f:Values.PlayerLatencyList.of_json
                                  playerLatencies)
              ?desiredPlayerSessions:(Option.map
                                        ~f:Values.DesiredPlayerSessionList.of_json
                                        desiredPlayerSessions)
              ?gameSessionData ~placementId ~gameSessionQueueName
              ~maximumPlayerSessionCount ())
           (Some Values.StartGameSessionPlacementOutput.to_json)
           (Some Values.StartGameSessionPlacementOutput.error_to_json)])
let start_match_backfill =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ticketId =
         flag "ticket-id" (optional string)
           ~doc:"STRING MatchmakingIdStringModel"
       and gameSessionArn =
         flag "game-session-arn" (optional string)
           ~doc:"STRING ArnStringModel"
       and configurationName =
         flag "configuration-name" (required string)
           ~doc:"STRING MatchmakingConfigurationName"
       and players =
         flag "players" (required json_arg) ~doc:"JSON PlayerList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_match_backfill
           (Values.StartMatchBackfillInput.make ?ticketId ?gameSessionArn
              ~configurationName ~players:(Values.PlayerList.of_json players)
              ()) (Some Values.StartMatchBackfillOutput.to_json)
           (Some Values.StartMatchBackfillOutput.error_to_json)])
let start_matchmaking =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ticketId =
         flag "ticket-id" (optional string)
           ~doc:"STRING MatchmakingIdStringModel"
       and configurationName =
         flag "configuration-name" (required string)
           ~doc:"STRING MatchmakingConfigurationName"
       and players =
         flag "players" (required json_arg) ~doc:"JSON PlayerList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_matchmaking
           (Values.StartMatchmakingInput.make ?ticketId ~configurationName
              ~players:(Values.PlayerList.of_json players) ())
           (Some Values.StartMatchmakingOutput.to_json)
           (Some Values.StartMatchmakingOutput.error_to_json)])
let stop_fleet_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON FleetActionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_fleet_actions
           (Values.StopFleetActionsInput.make ?location ~fleetId
              ~actions:(Values.FleetActionList.of_json actions) ())
           (Some Values.StopFleetActionsOutput.to_json)
           (Some Values.StopFleetActionsOutput.error_to_json)])
let stop_game_session_placement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and placementId =
         flag "placement-id" (required string) ~doc:"STRING IdStringModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_game_session_placement
           (Values.StopGameSessionPlacementInput.make ~placementId ())
           (Some Values.StopGameSessionPlacementOutput.to_json)
           (Some Values.StopGameSessionPlacementOutput.error_to_json)])
let stop_matchmaking =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ticketId =
         flag "ticket-id" (required string)
           ~doc:"STRING MatchmakingIdStringModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_matchmaking
           (Values.StopMatchmakingInput.make ~ticketId ())
           (Some Values.StopMatchmakingOutput.to_json)
           (Some Values.StopMatchmakingOutput.error_to_json)])
let suspend_game_server_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn"
       and suspendActions =
         flag "suspend-actions" (required json_arg)
           ~doc:"JSON GameServerGroupActions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.suspend_game_server_group
           (Values.SuspendGameServerGroupInput.make ~gameServerGroupName
              ~suspendActions:(Values.GameServerGroupActions.of_json
                                 suspendActions) ())
           (Some Values.SuspendGameServerGroupOutput.to_json)
           (Some Values.SuspendGameServerGroupOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING NonBlankAndLengthConstraintString"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and routingStrategy =
         flag "routing-strategy" (optional json_arg)
           ~doc:"JSON RoutingStrategy"
       and aliasId =
         flag "alias-id" (required string) ~doc:"STRING AliasIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_alias
           (Values.UpdateAliasInput.make ?name ?description
              ?routingStrategy:(Option.map ~f:Values.RoutingStrategy.of_json
                                  routingStrategy) ~aliasId ())
           (Some Values.UpdateAliasOutput.to_json)
           (Some Values.UpdateAliasOutput.error_to_json)])
let update_build =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and version =
         flag "version" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and buildId =
         flag "build-id" (required string) ~doc:"STRING BuildIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_build
           (Values.UpdateBuildInput.make ?name ?version ~buildId ())
           (Some Values.UpdateBuildOutput.to_json)
           (Some Values.UpdateBuildOutput.error_to_json)])
let update_fleet_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and newGameSessionProtectionPolicy =
         flag "new-game-session-protection-policy" (optional json_arg)
           ~doc:"JSON ProtectionPolicy"
       and resourceCreationLimitPolicy =
         flag "resource-creation-limit-policy" (optional json_arg)
           ~doc:"JSON ResourceCreationLimitPolicy"
       and metricGroups =
         flag "metric-groups" (optional json_arg) ~doc:"JSON MetricGroupList"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fleet_attributes
           (Values.UpdateFleetAttributesInput.make ?name ?description
              ?newGameSessionProtectionPolicy:(Option.map
                                                 ~f:Values.ProtectionPolicy.of_json
                                                 newGameSessionProtectionPolicy)
              ?resourceCreationLimitPolicy:(Option.map
                                              ~f:Values.ResourceCreationLimitPolicy.of_json
                                              resourceCreationLimitPolicy)
              ?metricGroups:(Option.map ~f:Values.MetricGroupList.of_json
                               metricGroups) ~fleetId ())
           (Some Values.UpdateFleetAttributesOutput.to_json)
           (Some Values.UpdateFleetAttributesOutput.error_to_json)])
let update_fleet_capacity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and desiredInstances =
         flag "desired-instances" (optional int) ~doc:"INT WholeNumber"
       and minSize = flag "min-size" (optional int) ~doc:"INT WholeNumber"
       and maxSize = flag "max-size" (optional int) ~doc:"INT WholeNumber"
       and location =
         flag "location" (optional string) ~doc:"STRING LocationStringModel"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fleet_capacity
           (Values.UpdateFleetCapacityInput.make ?desiredInstances ?minSize
              ?maxSize ?location ~fleetId ())
           (Some Values.UpdateFleetCapacityOutput.to_json)
           (Some Values.UpdateFleetCapacityOutput.error_to_json)])
let update_fleet_port_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inboundPermissionAuthorizations =
         flag "inbound-permission-authorizations" (optional json_arg)
           ~doc:"JSON IpPermissionsList"
       and inboundPermissionRevocations =
         flag "inbound-permission-revocations" (optional json_arg)
           ~doc:"JSON IpPermissionsList"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_fleet_port_settings
           (Values.UpdateFleetPortSettingsInput.make
              ?inboundPermissionAuthorizations:(Option.map
                                                  ~f:Values.IpPermissionsList.of_json
                                                  inboundPermissionAuthorizations)
              ?inboundPermissionRevocations:(Option.map
                                               ~f:Values.IpPermissionsList.of_json
                                               inboundPermissionRevocations)
              ~fleetId ())
           (Some Values.UpdateFleetPortSettingsOutput.to_json)
           (Some Values.UpdateFleetPortSettingsOutput.error_to_json)])
let update_game_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gameServerData =
         flag "game-server-data" (optional string)
           ~doc:"STRING GameServerData"
       and utilizationStatus =
         flag "utilization-status" (optional json_arg)
           ~doc:"JSON GameServerUtilizationStatus"
       and healthCheck =
         flag "health-check" (optional json_arg)
           ~doc:"JSON GameServerHealthCheck"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn"
       and gameServerId =
         flag "game-server-id" (required string) ~doc:"STRING GameServerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_game_server
           (Values.UpdateGameServerInput.make ?gameServerData
              ?utilizationStatus:(Option.map
                                    ~f:Values.GameServerUtilizationStatus.of_json
                                    utilizationStatus)
              ?healthCheck:(Option.map
                              ~f:Values.GameServerHealthCheck.of_json
                              healthCheck) ~gameServerGroupName ~gameServerId
              ()) (Some Values.UpdateGameServerOutput.to_json)
           (Some Values.UpdateGameServerOutput.error_to_json)])
let update_game_server_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and instanceDefinitions =
         flag "instance-definitions" (optional json_arg)
           ~doc:"JSON InstanceDefinitions"
       and gameServerProtectionPolicy =
         flag "game-server-protection-policy" (optional json_arg)
           ~doc:"JSON GameServerProtectionPolicy"
       and balancingStrategy =
         flag "balancing-strategy" (optional json_arg)
           ~doc:"JSON BalancingStrategy"
       and gameServerGroupName =
         flag "game-server-group-name" (required string)
           ~doc:"STRING GameServerGroupNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_game_server_group
           (Values.UpdateGameServerGroupInput.make ?roleArn
              ?instanceDefinitions:(Option.map
                                      ~f:Values.InstanceDefinitions.of_json
                                      instanceDefinitions)
              ?gameServerProtectionPolicy:(Option.map
                                             ~f:Values.GameServerProtectionPolicy.of_json
                                             gameServerProtectionPolicy)
              ?balancingStrategy:(Option.map
                                    ~f:Values.BalancingStrategy.of_json
                                    balancingStrategy) ~gameServerGroupName
              ()) (Some Values.UpdateGameServerGroupOutput.to_json)
           (Some Values.UpdateGameServerGroupOutput.error_to_json)])
let update_game_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maximumPlayerSessionCount =
         flag "maximum-player-session-count" (optional int)
           ~doc:"INT WholeNumber"
       and name =
         flag "name" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and playerSessionCreationPolicy =
         flag "player-session-creation-policy" (optional json_arg)
           ~doc:"JSON PlayerSessionCreationPolicy"
       and protectionPolicy =
         flag "protection-policy" (optional json_arg)
           ~doc:"JSON ProtectionPolicy"
       and gameSessionId =
         flag "game-session-id" (required string)
           ~doc:"STRING ArnStringModel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_game_session
           (Values.UpdateGameSessionInput.make ?maximumPlayerSessionCount
              ?name
              ?playerSessionCreationPolicy:(Option.map
                                              ~f:Values.PlayerSessionCreationPolicy.of_json
                                              playerSessionCreationPolicy)
              ?protectionPolicy:(Option.map
                                   ~f:Values.ProtectionPolicy.of_json
                                   protectionPolicy) ~gameSessionId ())
           (Some Values.UpdateGameSessionOutput.to_json)
           (Some Values.UpdateGameSessionOutput.error_to_json)])
let update_game_session_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timeoutInSeconds =
         flag "timeout-in-seconds" (optional int) ~doc:"INT WholeNumber"
       and playerLatencyPolicies =
         flag "player-latency-policies" (optional json_arg)
           ~doc:"JSON PlayerLatencyPolicyList"
       and destinations =
         flag "destinations" (optional json_arg)
           ~doc:"JSON GameSessionQueueDestinationList"
       and filterConfiguration =
         flag "filter-configuration" (optional json_arg)
           ~doc:"JSON FilterConfiguration"
       and priorityConfiguration =
         flag "priority-configuration" (optional json_arg)
           ~doc:"JSON PriorityConfiguration"
       and customEventData =
         flag "custom-event-data" (optional string)
           ~doc:"STRING QueueCustomEventData"
       and notificationTarget =
         flag "notification-target" (optional string)
           ~doc:"STRING QueueSnsArnStringModel"
       and name =
         flag "name" (required string)
           ~doc:"STRING GameSessionQueueNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_game_session_queue
           (Values.UpdateGameSessionQueueInput.make ?timeoutInSeconds
              ?playerLatencyPolicies:(Option.map
                                        ~f:Values.PlayerLatencyPolicyList.of_json
                                        playerLatencyPolicies)
              ?destinations:(Option.map
                               ~f:Values.GameSessionQueueDestinationList.of_json
                               destinations)
              ?filterConfiguration:(Option.map
                                      ~f:Values.FilterConfiguration.of_json
                                      filterConfiguration)
              ?priorityConfiguration:(Option.map
                                        ~f:Values.PriorityConfiguration.of_json
                                        priorityConfiguration)
              ?customEventData ?notificationTarget ~name ())
           (Some Values.UpdateGameSessionQueueOutput.to_json)
           (Some Values.UpdateGameSessionQueueOutput.error_to_json)])
let update_matchmaking_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NonZeroAndMaxString"
       and gameSessionQueueArns =
         flag "game-session-queue-arns" (optional json_arg)
           ~doc:"JSON QueueArnsList"
       and requestTimeoutSeconds =
         flag "request-timeout-seconds" (optional int)
           ~doc:"INT MatchmakingRequestTimeoutInteger"
       and acceptanceTimeoutSeconds =
         flag "acceptance-timeout-seconds" (optional int)
           ~doc:"INT MatchmakingAcceptanceTimeoutInteger"
       and acceptanceRequired =
         flag "acceptance-required" (optional bool) ~doc:"BOOL BooleanModel"
       and ruleSetName =
         flag "rule-set-name" (optional string)
           ~doc:"STRING MatchmakingRuleSetName"
       and notificationTarget =
         flag "notification-target" (optional string)
           ~doc:"STRING SnsArnStringModel"
       and additionalPlayerCount =
         flag "additional-player-count" (optional int) ~doc:"INT WholeNumber"
       and customEventData =
         flag "custom-event-data" (optional string)
           ~doc:"STRING CustomEventData"
       and gameProperties =
         flag "game-properties" (optional json_arg)
           ~doc:"JSON GamePropertyList"
       and gameSessionData =
         flag "game-session-data" (optional string)
           ~doc:"STRING GameSessionData"
       and backfillMode =
         flag "backfill-mode" (optional json_arg) ~doc:"JSON BackfillMode"
       and flexMatchMode =
         flag "flex-match-mode" (optional json_arg) ~doc:"JSON FlexMatchMode"
       and name =
         flag "name" (required string)
           ~doc:"STRING MatchmakingConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_matchmaking_configuration
           (Values.UpdateMatchmakingConfigurationInput.make ?description
              ?gameSessionQueueArns:(Option.map
                                       ~f:Values.QueueArnsList.of_json
                                       gameSessionQueueArns)
              ?requestTimeoutSeconds ?acceptanceTimeoutSeconds
              ?acceptanceRequired ?ruleSetName ?notificationTarget
              ?additionalPlayerCount ?customEventData
              ?gameProperties:(Option.map ~f:Values.GamePropertyList.of_json
                                 gameProperties) ?gameSessionData
              ?backfillMode:(Option.map ~f:Values.BackfillMode.of_json
                               backfillMode)
              ?flexMatchMode:(Option.map ~f:Values.FlexMatchMode.of_json
                                flexMatchMode) ~name ())
           (Some Values.UpdateMatchmakingConfigurationOutput.to_json)
           (Some Values.UpdateMatchmakingConfigurationOutput.error_to_json)])
let update_runtime_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetId =
         flag "fleet-id" (required string) ~doc:"STRING FleetIdOrArn"
       and runtimeConfiguration =
         flag "runtime-configuration" (required json_arg)
           ~doc:"JSON RuntimeConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_runtime_configuration
           (Values.UpdateRuntimeConfigurationInput.make ~fleetId
              ~runtimeConfiguration:(Values.RuntimeConfiguration.of_json
                                       runtimeConfiguration) ())
           (Some Values.UpdateRuntimeConfigurationOutput.to_json)
           (Some Values.UpdateRuntimeConfigurationOutput.error_to_json)])
let update_script =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and version =
         flag "version" (optional string) ~doc:"STRING NonZeroAndMaxString"
       and storageLocation =
         flag "storage-location" (optional json_arg) ~doc:"JSON S3Location"
       and zipFile = flag "zip-file" (optional json_arg) ~doc:"JSON ZipBlob"
       and scriptId =
         flag "script-id" (required string) ~doc:"STRING ScriptIdOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_script
           (Values.UpdateScriptInput.make ?name ?version
              ?storageLocation:(Option.map ~f:Values.S3Location.of_json
                                  storageLocation)
              ?zipFile:(Option.map ~f:Values.ZipBlob.of_json zipFile)
              ~scriptId ()) (Some Values.UpdateScriptOutput.to_json)
           (Some Values.UpdateScriptOutput.error_to_json)])
let validate_matchmaking_rule_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleSetBody =
         flag "rule-set-body" (required string) ~doc:"STRING RuleSetBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_matchmaking_rule_set
           (Values.ValidateMatchmakingRuleSetInput.make ~ruleSetBody ())
           (Some Values.ValidateMatchmakingRuleSetOutput.to_json)
           (Some Values.ValidateMatchmakingRuleSetOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-match", accept_match);
    ("claim-game-server", claim_game_server);
    ("create-alias", create_alias);
    ("create-build", create_build);
    ("create-fleet", create_fleet);
    ("create-fleet-locations", create_fleet_locations);
    ("create-game-server-group", create_game_server_group);
    ("create-game-session", create_game_session);
    ("create-game-session-queue", create_game_session_queue);
    ("create-matchmaking-configuration", create_matchmaking_configuration);
    ("create-matchmaking-rule-set", create_matchmaking_rule_set);
    ("create-player-session", create_player_session);
    ("create-player-sessions", create_player_sessions);
    ("create-script", create_script);
    ("create-vpc-peering-authorization", create_vpc_peering_authorization);
    ("create-vpc-peering-connection", create_vpc_peering_connection);
    ("delete-alias", delete_alias);
    ("delete-build", delete_build);
    ("delete-fleet", delete_fleet);
    ("delete-fleet-locations", delete_fleet_locations);
    ("delete-game-server-group", delete_game_server_group);
    ("delete-game-session-queue", delete_game_session_queue);
    ("delete-matchmaking-configuration", delete_matchmaking_configuration);
    ("delete-matchmaking-rule-set", delete_matchmaking_rule_set);
    ("delete-scaling-policy", delete_scaling_policy);
    ("delete-script", delete_script);
    ("delete-vpc-peering-authorization", delete_vpc_peering_authorization);
    ("delete-vpc-peering-connection", delete_vpc_peering_connection);
    ("deregister-game-server", deregister_game_server);
    ("describe-alias", describe_alias);
    ("describe-build", describe_build);
    ("describe-e-c2-instance-limits", describe_e_c2_instance_limits);
    ("describe-fleet-attributes", describe_fleet_attributes);
    ("describe-fleet-capacity", describe_fleet_capacity);
    ("describe-fleet-events", describe_fleet_events);
    ("describe-fleet-location-attributes",
      describe_fleet_location_attributes);
    ("describe-fleet-location-capacity", describe_fleet_location_capacity);
    ("describe-fleet-location-utilization",
      describe_fleet_location_utilization);
    ("describe-fleet-port-settings", describe_fleet_port_settings);
    ("describe-fleet-utilization", describe_fleet_utilization);
    ("describe-game-server", describe_game_server);
    ("describe-game-server-group", describe_game_server_group);
    ("describe-game-server-instances", describe_game_server_instances);
    ("describe-game-session-details", describe_game_session_details);
    ("describe-game-session-placement", describe_game_session_placement);
    ("describe-game-session-queues", describe_game_session_queues);
    ("describe-game-sessions", describe_game_sessions);
    ("describe-instances", describe_instances);
    ("describe-matchmaking", describe_matchmaking);
    ("describe-matchmaking-configurations",
      describe_matchmaking_configurations);
    ("describe-matchmaking-rule-sets", describe_matchmaking_rule_sets);
    ("describe-player-sessions", describe_player_sessions);
    ("describe-runtime-configuration", describe_runtime_configuration);
    ("describe-scaling-policies", describe_scaling_policies);
    ("describe-script", describe_script);
    ("describe-vpc-peering-authorizations",
      describe_vpc_peering_authorizations);
    ("describe-vpc-peering-connections", describe_vpc_peering_connections);
    ("get-game-session-log-url", get_game_session_log_url);
    ("get-instance-access", get_instance_access);
    ("list-aliases", list_aliases);
    ("list-builds", list_builds);
    ("list-fleets", list_fleets);
    ("list-game-server-groups", list_game_server_groups);
    ("list-game-servers", list_game_servers);
    ("list-scripts", list_scripts);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-scaling-policy", put_scaling_policy);
    ("register-game-server", register_game_server);
    ("request-upload-credentials", request_upload_credentials);
    ("resolve-alias", resolve_alias);
    ("resume-game-server-group", resume_game_server_group);
    ("search-game-sessions", search_game_sessions);
    ("start-fleet-actions", start_fleet_actions);
    ("start-game-session-placement", start_game_session_placement);
    ("start-match-backfill", start_match_backfill);
    ("start-matchmaking", start_matchmaking);
    ("stop-fleet-actions", stop_fleet_actions);
    ("stop-game-session-placement", stop_game_session_placement);
    ("stop-matchmaking", stop_matchmaking);
    ("suspend-game-server-group", suspend_game_server_group);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-alias", update_alias);
    ("update-build", update_build);
    ("update-fleet-attributes", update_fleet_attributes);
    ("update-fleet-capacity", update_fleet_capacity);
    ("update-fleet-port-settings", update_fleet_port_settings);
    ("update-game-server", update_game_server);
    ("update-game-server-group", update_game_server_group);
    ("update-game-session", update_game_session);
    ("update-game-session-queue", update_game_session_queue);
    ("update-matchmaking-configuration", update_matchmaking_configuration);
    ("update-runtime-configuration", update_runtime_configuration);
    ("update-script", update_script);
    ("validate-matchmaking-rule-set", validate_matchmaking_rule_set)]